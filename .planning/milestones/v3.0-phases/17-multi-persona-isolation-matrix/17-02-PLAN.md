---
phase: 17-multi-persona-isolation-matrix
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/teach_persona.py
  - tests/test_phase14_teaching.py
  - tests/test_lora_inject.py
autonomous: true
requirements: [ISO-06]

must_haves:
  truths:
    - "Three adapters can be trained at three distinct seeds through the committed training recipe, not a copy of it"
    - "A Phase 17 adapter's artifacts land under a phase17_ prefix, so provenance names the phase that produced them"
    - "The prefix reaches the function that WRITES the paths, not only the function that names them"
    - "Every existing Phase 14 arm trains bit-for-bit identically after the widening"
    - "A regression that returns any LoRA consumer to LoRAConfig() defaults turns a committed test red"
  artifacts:
    - path: "scripts/teach_persona.py"
      provides: "Additive seed= and prefix= keywords threaded train_arm -> build_arm_bins -> arm_outputs"
      contains: "prefix=prefix"
    - path: "tests/test_lora_inject.py"
      provides: "AST regression pinning the three runtime consumers to **artifact['lora_config']"
      contains: "lora_config"
  key_links:
    - from: "scripts/teach_persona.py"
      to: "personacore.seeding.seed_everything"
      via: "the parameter, not the module constant"
      pattern: "seed_everything\\(seed\\)"
    - from: "scripts/teach_persona.py"
      to: "checkpoints/phase17_{arm}_adapter.pt"
      via: "prefix threaded through both arm_outputs call sites"
      pattern: "arm_outputs\\(arm, prefix=prefix\\)"
    - from: "tests/test_lora_inject.py"
      to: "scripts/phase14_recall.py, scripts/personalize_demo.py"
      via: "AST scan of inject_lora call sites"
      pattern: "inject_lora"
---

<objective>
Widen the two committed instruments Phase 17 needs, additively and with defaults that preserve every
existing run bit-for-bit: a `seed=` keyword on `teach_persona.train_arm` / `build_arm_bins` (D-14 has
no other route — the training seed is a module constant today), a phase-aware `prefix=` threaded
through `train_arm` → `build_arm_bins` → `arm_outputs`, and an AST regression pinning the ISO-06 fix
that Phase 17 is about to multiply from three call sites to N+1.

Purpose: D-14 requires a distinct seed per persona, and RESEARCH F-10 measured that the seed is
hardcoded at three sites. Copying `train_arm` into a Phase 17 driver would be a second training
recipe that can drift from the one every published number came from — "import, never copy" (Phase 16
D-16). ISO-06 is already closed in the tree (quick task 260814-d0j); this plan spends no tasks
closing it and one task keeping it closed.

Output: the widened training instrument and the ISO-06 regression guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/17-multi-persona-isolation-matrix/17-CONTEXT.md
@.planning/phases/17-multi-persona-isolation-matrix/17-PATTERNS.md
@.planning/quick/260814-d0j-close-w1-lora-consumers-inject-config-de/SUMMARY.md
</context>

<interfaces>
Current signatures being widened (`scripts/teach_persona.py`, re-measured against the tree):

- `:99` `SEED = 1337`
- `:190` `def arm_outputs(arm):` returning a 5-key dict:
  - `bin` → `data/persona_{arm}_train.bin`, `mask` → `data/persona_{arm}_train_mask.bin` (NO phase prefix today)
  - `csv` → `results/phase14_{arm}/run.csv`
  - `checkpoint` → `checkpoints/phase14_{arm}_latest.pt`
  - `adapter` → `checkpoints/phase14_{arm}_adapter.pt`, **except** `arm == "real"` → `checkpoints/persona_adapter.pt`
- `:403` `def build_arm_bins(arm, facts, family_ids, *, second_person=False, replay_ratio=0.0):`
- `:409` `outputs = arm_outputs(arm)` — **the write target for the bins**
- `:412` `seed_everything(SEED)` — owns the teaching-bin construction order
- `:426` bins provenance print, emits `seed={SEED}`
- `:501` `def train_arm(arm, *, facts, family_ids, second_person=False, replay_ratio=0.0):`
- `:507` `verdict = _require_go_verdict(FACTSET_REPORT)` — Phase 14's report, hardcoded
- `:516` `paths = arm_outputs(arm)` — **the refuse-if-exists set and the export targets**
- `:519` `refuse_if_exists([bin, mask, csv, checkpoint, adapter])`
- `:539` `tok, stats, paths = build_arm_bins(arm, facts, family_ids, second_person=..., replay_ratio=...)`
  — and it REBINDS `paths`, so the bins call's return is what the export half writes to
- `:563` `seed_everything(SEED)` — owns the GPT build / LoRA init draw (D-14's actual target)
- `:603` `TrainConfig(..., seed=SEED)` — owns the training data order
- `:688` run provenance print, emits `seed={SEED}`
- `:478` `LORA_CFG = LoRAConfig()` — a PRODUCER site, correct as-is, D-20's anchor. Do not touch.
- `:1131` `arm_outputs(arm)` inside the report/reload path — reconstructs Phase 14 arm paths from a
  Phase 14 payload. Leave bare; the default keeps it byte-identical.

**Both internal call sites matter.** `arm_outputs` is called twice inside the module, and NEITHER is
reached by a caller of `train_arm`. Widening only `arm_outputs` gives a signature nobody can use:
`train_arm(arm, ..., seed=1338)` would still write `checkpoints/phase14_persona_a_adapter.pt`, and
plans 17-09/17-10 assert `checkpoints/phase17_*`. The prefix must be threaded through the chain.

The ISO-06 consumer sites this plan pins (verified in tree, RESEARCH F-01):
`scripts/phase14_recall.py:557`, `scripts/phase14_recall.py:1457`, `scripts/personalize_demo.py:448`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Additive seed= and prefix= keywords, threaded to both arm_outputs call sites</name>
  <files>scripts/teach_persona.py, tests/test_phase14_teaching.py</files>
  <read_first>
    - scripts/teach_persona.py lines 85-230 (`SEED`, `FACTSET_REPORT`, `_require_go_verdict`, `arm_outputs` and its `real`-arm exception, `refuse_if_exists`)
    - scripts/teach_persona.py lines 395-440 (`build_arm_bins` signature, its `arm_outputs(arm)` at :409, its `seed_everything(SEED)` at :412 and the bins provenance print at :426)
    - scripts/teach_persona.py lines 495-700 (`train_arm` signature, `_require_go_verdict` at :507, `arm_outputs(arm)` at :516, `refuse_if_exists` at :519, the `build_arm_bins` call at :539 that REBINDS `paths`, the ordering comment at :560-565, `seed_everything(SEED)` at :563, `TrainConfig(seed=SEED)` at :603, the run provenance print at :688)
    - scripts/teach_persona.py lines 1095-1135 (the reload path's bare `arm_outputs(arm)` at :1131 — left alone)
    - tests/test_phase14_teaching.py lines 20-60 and 473-520 (`_load`, `test_recipe_constants`, `test_arm_outputs_scoped`, `test_real_arm_adapter_is_the_shippable_path` — which pins `phase14_{arm}_adapter.pt` for every non-`real` arm)
    - .planning/phases/17-multi-persona-isolation-matrix/17-PATTERNS.md §`scripts/teach_persona.py`
  </read_first>
  <action>
Four additive edits to `scripts/teach_persona.py`, each defaulting to today's behaviour.

1. `arm_outputs(arm, *, prefix="phase14")`. Substitute the prefix into the three paths that carry
   the `phase14_` literal today — `adapter` (`checkpoints/{prefix}_{arm}_adapter.pt`), `csv`
   (`results/{prefix}_{arm}/run.csv`) and `checkpoint` (`checkpoints/{prefix}_{arm}_latest.pt`).
   Leave `bin` and `mask` alone: they carry no phase prefix today and inventing one would move an
   existing path. Leave the `arm == "real"` adapter exception UNCONDITIONAL on prefix — it is the
   shippable `checkpoints/persona_adapter.pt` that `phase14_recall.ADAPTER_PATH` and the Gradio demo
   both hardcode, and `test_real_arm_adapter_is_the_shippable_path` pins it. Phase 17 never passes
   `real`, so the exception is unreachable from this phase; note that in the docstring rather than
   making the exception prefix-aware.

2. `build_arm_bins(arm, facts, family_ids, *, second_person=False, replay_ratio=0.0, seed=SEED,
   prefix="phase14")`. Change `arm_outputs(arm)` at :409 to `arm_outputs(arm, prefix=prefix)`,
   `seed_everything(SEED)` at :412 to `seed_everything(seed)`, and the bins provenance print at :426
   from `seed={SEED}` to `seed={seed}` so the recorded provenance names the seed the run actually
   used (D-14's audit trail).

3. `train_arm(arm, *, facts, family_ids, second_person=False, replay_ratio=0.0, seed=SEED,
   prefix="phase14")`. Change `arm_outputs(arm)` at :516 to `arm_outputs(arm, prefix=prefix)`;
   replace `seed_everything(SEED)` at :563 and `TrainConfig(..., seed=SEED)` at :603 with the
   parameter, and the run provenance print at :688 likewise. **Preserve the ordering comment at
   :560-565 verbatim** — it records that this seed owns the training data order and that the bins
   build above consumed numpy RNG in its smoke draw, so seeding once at the top would make the data
   order depend on the bins path.

4. Thread BOTH keywords through the internal `build_arm_bins` call at :539:
   `build_arm_bins(arm, facts, family_ids, second_person=..., replay_ratio=..., seed=seed,
   prefix=prefix)`. This is the load-bearing edit. That call REBINDS `paths`, so the export half at
   the bottom of `train_arm` writes to whatever dict `build_arm_bins` returned — if the prefix is
   threaded at :516 but not at :539, `refuse_if_exists` guards the `phase17_` paths while the
   adapter export lands at `phase14_`. A caller passing `prefix="phase17", seed=1338` must get 1338
   and `phase17_` at every site, never a mix.

Do NOT change `_require_go_verdict(FACTSET_REPORT)` at :507. Phase 17's own blocking verdict is a
different report and the Phase 17 driver calls `teach_persona._require_go_verdict(PHASE17_REPORT)`
itself before `train_arm`, so both gates fire and nothing in the committed driver moves (RESEARCH
F-10). Do NOT touch `LORA_CFG = LoRAConfig()` at :478 — it is a producer site and D-20's anchor. Do
NOT touch the bare `arm_outputs(arm)` at :1131 — the reload path reconstructs Phase 14 paths from a
Phase 14 payload and the default keeps it byte-identical.

Extend `tests/test_phase14_teaching.py` with:

- `test_seed_parameter_defaults_to_the_module_constant` — assert via `inspect.signature` that both
  widened functions have a `seed` keyword whose default is `teach.SEED`, and AST-scan for a `Name`
  node `SEED` passed as an argument to `seed_everything` anywhere in the module, asserting none
  survives.
- `test_arm_outputs_prefix_is_additive` — assert `arm_outputs(arm)` is byte-identical to the paths
  `test_arm_outputs_scoped` already pins; assert `arm_outputs(arm, prefix="phase17")` moves exactly
  `adapter`, `csv` and `checkpoint` and leaves `bin` and `mask` equal to the default call's; assert
  `arm_outputs("real", prefix="phase17")["adapter"].name == "persona_adapter.pt"` (the exception is
  prefix-independent).
- `test_prefix_reaches_both_arm_outputs_call_sites` — the B1 regression, and the reason this task
  exists. AST-assert that EVERY `arm_outputs(...)` call inside the bodies of `build_arm_bins` and
  `train_arm` passes a `prefix=` keyword, and that the internal `build_arm_bins(...)` call inside
  `train_arm` passes both `seed=` and `prefix=`. Message must name the consequence: a widened
  signature whose callers never pass the keyword produces `checkpoints/phase14_persona_a_adapter.pt`
  from a Phase 17 run, and plans 17-09/17-10 assert `checkpoints/phase17_*`.
  </action>
  <verify>
    <automated>.venv/bin/pytest -q tests/test_phase14_teaching.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest -q tests/test_phase14_teaching.py -x` passes, and `test_arm_outputs_scoped` / `test_recipe_constants` / `test_real_arm_adapter_is_the_shippable_path` are unchanged and still green.
    - `.venv/bin/python -c "import ast;t=ast.parse(open('scripts/teach_persona.py').read());bad=[n for n in ast.walk(t) if isinstance(n,ast.Call) and getattr(n.func,'id','')=='seed_everything' and any(getattr(a,'id','')=='SEED' for a in n.args)];assert not bad, 'a seed_everything(SEED) call survived the widening'"` exits 0.
    - Both internal `arm_outputs` call sites pass the prefix: `.venv/bin/python -c "import ast;t=ast.parse(open('scripts/teach_persona.py').read());fns={f.name:f for f in ast.walk(t) if isinstance(f,ast.FunctionDef)};calls=[c for n in ('build_arm_bins','train_arm') for c in ast.walk(fns[n]) if isinstance(c,ast.Call) and getattr(c.func,'id','')=='arm_outputs'];assert len(calls)==2, len(calls);assert all('prefix' in {k.arg for k in c.keywords} for c in calls), 'arm_outputs called without prefix= inside the chain'"` exits 0.
    - The internal `build_arm_bins` call inside `train_arm` passes both keywords: `grep -n "seed=seed" scripts/teach_persona.py` and `grep -n "prefix=prefix" scripts/teach_persona.py` each return at least 3 hits (the two `arm_outputs` calls plus the `build_arm_bins` call).
    - `grep -n "seed={seed}" scripts/teach_persona.py` returns 2 hits (bins provenance and run provenance).
    - `grep -n "LORA_CFG = LoRAConfig()" scripts/teach_persona.py` still returns 1 hit at the producer site — D-20's anchor untouched.
    - `grep -n "_require_go_verdict(FACTSET_REPORT)" scripts/teach_persona.py` still returns 1 hit — Phase 14's gate unmoved.
    - `git diff -- scripts/teach_persona.py | grep "^-" | grep -v "^---"` shows only the four seed lines, the two provenance lines, the two `arm_outputs` call lines, the `build_arm_bins` call line, the three signatures and the three `arm_outputs` path lines — no deletion elsewhere.
    - End-to-end path proof recorded in the SUMMARY (no training, dry inspection): call `arm_outputs("persona_a", prefix="phase17")` and confirm `adapter` is `checkpoints/phase17_persona_a_adapter.pt` — the exact path `17-09` Task 1's `test -f` asserts.
    - `.venv/bin/pytest -q` full suite at or above 579 passed / 1 skipped.
  </acceptance_criteria>
  <done>`train_arm(arm, ..., seed=1338, prefix="phase17")` writes `checkpoints/phase17_{arm}_adapter.pt` from both internal call sites, and every existing Phase 14 arm resolves to byte-identical paths and the same seed as before.</done>
</task>

<task type="auto">
  <name>Task 2: Pin the ISO-06 consumer sites with an AST regression</name>
  <files>tests/test_lora_inject.py</files>
  <read_first>
    - tests/test_lora_inject.py lines 1-70 and 183-230 (`_tiny_config`, `_build_injected`, `_nudge_lora_b`, `test_load_adapter_weights_refuses_wrong_rank`, `test_load_adapter_weights_refuses_wrong_alpha` and its refusal-precedes-mutation + positive-control shape)
    - src/personacore/lora/inject.py lines 76-160 (`load_adapter_weights`'s key, shape and scale audits; `set_adapter_enabled`; `adapter_disabled`)
    - scripts/phase14_recall.py lines 550-565 and 1450-1465 (the two consumer call sites)
    - scripts/personalize_demo.py lines 440-455 (the third consumer call site)
    - tests/test_phase14_scoring.py lines 439-560 (the `_scanned_files` / `_call_sites` file-set scanner and the collapsed-glob guard)
  </read_first>
  <action>
Extend `tests/test_lora_inject.py` with one test,
`test_every_inject_lora_consumer_reads_the_artifact_config`. It is the regression half of ISO-06:
`src/personacore/lora/inject.py:119-129` already raises on a scale mismatch, but Phase 17 multiplies
the consumer call sites from 3 to N+1 and a new one that forgets `**artifact["lora_config"]` would
be caught only at runtime, in the middle of a sweep.

AST-scan `sorted((_REPO_ROOT / "scripts").glob("*.py")) + sorted((_REPO_ROOT / "src").rglob("*.py"))`
for every `inject_lora(...)` call. For each call, classify its second positional argument: a
`LoRAConfig(**<subscript>)` call whose `**` operand is a subscript with the literal key
`"lora_config"` is a CONSUMER; a bare `LoRAConfig()` with no arguments is a PRODUCER; anything else
is UNCLASSIFIED and fails.

Assert with HARD EQUALITY, never `in`:
- the consumer set equals `{("scripts/phase14_recall.py", "load_adapted_model"),
  ("scripts/phase14_recall.py", "run_bit_identity_control"),
  ("scripts/personalize_demo.py", "build_demo")}` plus any Phase 17 entries added when
  `scripts/phase17_isolation.py` lands (leave the allowlist as a module-level tuple so a future plan
  extends it by one visible line rather than by weakening the assertion);
- the producer set equals `{("scripts/teach_persona.py", ...), ("scripts/train_adapter_smoke.py",
  ...)}` — resolve the enclosing function names from the AST rather than typing them from memory;
- the unclassified set is empty;
- `len(scanned_files) >= 2` before any of the above, so a broken glob cannot make the guard green by
  scanning nothing.

Docstring must record the producer/consumer distinction and why the producer sites are correct: they
DEFINE the config an artifact will carry rather than read one, and `LoRAConfig()` there is D-20's
diagonal anchor (`r=8, alpha=16.0`). "Fix" them and Phase 17's diagonals stop being readable against
Phase 14's 0.3483 and Phase 16's 0.865385.
  </action>
  <verify>
    <automated>.venv/bin/pytest -q tests/test_lora_inject.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest -q tests/test_lora_inject.py -x` passes with 12 tests collected (11 existing + 1 new).
    - Deliberate-RED recorded in the SUMMARY: change `scripts/personalize_demo.py:448` to `inject_lora(model, LoRAConfig())`, watch the new test fail naming that site as a producer where a consumer is required, then revert to a byte-identical file (`git diff` empty).
    - `git diff -- tests/test_lora_inject.py | grep "^-" | grep -v "^---"` is empty — the change is purely additive.
    - `grep -n "PRODUCER\|producer" tests/test_lora_inject.py` shows the docstring recording why `teach_persona` and `train_adapter_smoke` keep `LoRAConfig()`.
  </acceptance_criteria>
  <done>A consumer call site that drops `**artifact["lora_config"]` fails a committed test, and the guard has been watched failing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adapter artifact → live model | `lora_config` crosses here; `alpha` is shape-invisible so key and shape audits are silent about it |
| training recipe → published number | Every Phase 14/16/17 number comes from one `train_arm` body; a copy would be a second recipe |
| phase label → written path | A Phase 17 run writing a `phase14_` path is a provenance claim that is simply false |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| TH-17-04 | Tampering | `inject_lora` consumer call sites | mitigate | Task 2's hard-equality AST scan over `scripts/*.py` + `src/**/*.py`, with a collapsed-glob guard, plus the existing runtime scale audit at `src/personacore/lora/inject.py:119-129` |
| TH-17-05 | Tampering | `teach_persona.train_arm` behaviour drift under the widening | mitigate | `seed=SEED` / `prefix="phase14"` defaults preserve every existing arm; the AST assertion that no `seed_everything(SEED)` call survives proves the parameter reaches all three sites |
| TH-17-06 | Repudiation | Phase 17 adapters landing under a `phase14_` path | mitigate | `prefix=` threaded through `train_arm` → `build_arm_bins` → both `arm_outputs` call sites, pinned by `test_prefix_reaches_both_arm_outputs_call_sites`; the default keeps `test_arm_outputs_scoped` byte-identical |
| TH-17-SC | Tampering | pip installs | accept | Zero packages installed. `tests/test_package.py:11,36` sha256-pins `pyproject.toml` |
</threat_model>

<verification>
- `.venv/bin/pytest -q tests/test_phase14_teaching.py tests/test_lora_inject.py -x` passes
- `.venv/bin/pytest -q` full suite at or above 579 passed / 1 skipped
- `make lint` clean
- `git diff -- pyproject.toml` empty (STAT-04)
- `git status --short results/` empty — this plan changes no recorded evidence
</verification>

<success_criteria>
Three adapters can be trained at three distinct seeds through the committed recipe with the Phase 14
arms provably unchanged, their artifacts land under `checkpoints/phase17_*`, and the ISO-06 fix
cannot silently regress as Phase 17 multiplies the consumer call sites.
</success_criteria>

<output>
Create `.planning/phases/17-multi-persona-isolation-matrix/17-02-SUMMARY.md` when done
</output>
</content>
