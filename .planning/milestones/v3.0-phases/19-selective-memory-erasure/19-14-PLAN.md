---
phase: 19-selective-memory-erasure
plan: 14
type: execute
wave: 12
depends_on: ["19-12", "19-13"]
files_modified:
  - results/phase19_representational.json
  - tests/test_phase19_erasure.py
autonomous: true
requirements: [ERASE-01, STAT-06]

must_haves:
  truths:
    - "Representational consistency is reported with its bounds and reaches no gate"
    - "The ΔW read distinguishes the ablated region from the preserved region, per cell"
    - "No p-value is computed anywhere on this path"
  artifacts:
    - path: "results/phase19_representational.json"
      provides: "per-cell ΔW cosine and Fisher overlap between the erased and preserved regions, DESCRIPTIVE"
      contains: "DESCRIPTIVE"
  key_links:
    - from: "results/phase19_representational.json"
      to: "scripts/extract_deltas.adapter_cells"
      via: "ΔW = scale·(B@A) with scale read from the artifact"
      pattern: "adapter_cells"
---

<objective>
Produce the representational read `ERASURE_DECISION_RULE`'s fourth clause requires — and prove
structurally that it cannot become a gate.

Purpose: the pre-registration reports cross-persona ΔW cosine and Fisher overlap between the erased
and preserved regions WITH their bounds and never converts them into pass/fail
(`scripts/erasure_gate.py:118-122`). At n=8 facts and n=3 personas the sample cannot support a
threshold, and gating what the sample cannot support is treated as a DEFECT in this project, not as
extra rigour. `ROADMAP.md:530-534` states it flatly: any plan that converts one of these into
pass/fail is violating the pre-registration.

The guard was written at 19-05, before the code it guards existed — the same order
`test_context_pressure_sweep_is_not_gated` landed in (`scripts/phase16_persistence.py:1289-1293`).
This plan is where it earns its keep.

Output: `results/phase19_representational.json`, and the guard re-run against the real read.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-selective-memory-erasure/19-CONTEXT.md
@.planning/phases/19-selective-memory-erasure/19-RESEARCH.md
@scripts/erasure_gate.py
@scripts/phase19_erasure.py
</context>

<interfaces>
`scripts/extract_deltas.py:174` `adapter_cells(adapter, scale, w0_model)` — `ΔW = scale · (B @ A)` per
`(layer, projection)`, with `scale` READ FROM THE ARTIFACT because `layer.py:27` names `self.scale`
the single source of truth for `alpha/r` and forbids recomputing it (PITFALLS P3). Its `KEYS` tuple
is the committed cell enumeration.
`:199` `fisher_cells(fisher)` — per-cell MEAN of the mean-normalized Fisher diagonal
(`FISHER_AGGREGATE = "mean"`, `:109`).

Fisher over the adapter, VERIFIED this session in §Q1 and its trap with it:
`estimate_fisher` (`src/personacore/continual/fisher.py:63`) iterates `model.named_parameters()` with
NO `requires_grad` filter (`:101-103`) and calls `torch.autograd.grad(loss, params)` (`:118`). Against
a LoRA-injected model: **with** `mark_only_lora_trainable(model)` it raises
`RuntimeError: One of the differentiated Tensors does not require grad`; **without** it, it succeeds
and returns `lora_A`/`lora_B` keys among the rest. So estimate Fisher BEFORE marking trainability and
filter the returned dict to `lora_` keys (P19-8). Two honest limits to record: `estimate_fisher` has
NO mask support (it scores every token of every window, including user turns), and the teaching bin
is tiny — `data/persona_real_train.bin` is 40,072 bytes = 20,036 `uint16` tokens (verified by `ls`) —
so windows at `block_size=256` overlap heavily.

M4 as a MECHANISM is OUT under D1. The Fisher read here is descriptive only.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Read ΔW and Fisher across the erased and preserved regions</name>
  <files>results/phase19_representational.json</files>
  <action>
Compute, per `(layer, projection)` cell over the committed `extract_deltas.KEYS`:

1. **ΔW cosine** between the taught adapter (`checkpoints/persona_adapter.pt`) and the M1 erased
   adapter (`checkpoints/phase19_m1_erased_adapter.pt`), and between the taught adapter and the M2
   retrain adapter (`checkpoints/phase19_m2_retrain_adapter.pt`) — three-way, so the surgical edit and
   the retrain can be seen against the same baseline. Use `adapter_cells` with `scale` read from each
   artifact; fp64, the `continual/fisher.py` statistics discipline.
2. **Fisher overlap** between the ablated components' cells and the preserved cells: estimate Fisher
   over the LoRA parameters of the taught adapter (inject, estimate, THEN mark trainability — never
   the reverse), filter to `lora_` keys, reduce per cell with `fisher_cells`, and report the mass in
   the ablated addresses against the mass in the rest.
3. **Cross-persona ΔW cosine** if Phase 17's three persona adapters are on disk — descriptive at
   n=3, with the n stated beside every number.

Every number carries its bounds and its n. Label the whole artifact `DESCRIPTIVE` in a required
top-level field, and record the two Fisher limits as required fields: no mask support (user turns are
scored) and heavy window overlap on a 20,036-token bin. Publishing a limitation as a field rather
than as prose is what stops it being edited out later.

Compute NO p-value. Call no `sign_test_exact`, no `holm`, no `wilson_upper_bound` on this path. A
second `sign_test_exact` call site IS a second hypothesis family and would reprice Holm to carry a
descriptive statistic (Phase 17's D-21 scan, `STATE.md:219`).
  </action>
  <verify>
    <automated>.venv/bin/python -c "import json; d=json.load(open('results/phase19_representational.json')); assert d['status']=='DESCRIPTIVE'; assert d['fisher_limits']; assert all('n' in v for v in d['reads'].values()); print(sorted(d['reads']))"</automated>
  </verify>
  <done>Per-cell ΔW cosines for both mechanisms and the Fisher overlap exist with bounds and n; the two Fisher limits are required fields; no p-value was computed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Re-run the not-gated guard against the real read</name>
  <files>tests/test_phase19_erasure.py</files>
  <behavior>
    - `test_representational_read_is_not_gated` passes against the now-real implementation, and `DESCRIPTIVE_ONLY_FUNCTIONS` has no dangling entry.
    - A test asserts `results/phase19_representational.json` carries `status == "DESCRIPTIVE"` and that no key in it is named like a verdict, a pass/fail or a threshold.
    - A test asserts no branch anywhere reads these numbers: grep the driver for reads of the representational artifact and require every one to be a report-rendering call site, in the register of Phase 18's "no branch anywhere reads these bounds — `rejected` comes from `holm` alone" (`results/phase18_extraction_report.md:170`).
    - The guard was watched RED by adding a threshold comparison on the real path, then restored byte-identically.
  </behavior>
  <action>
Extend the 19-05 guards to cover the concrete implementation. Watch RED by inserting a
`if cosine > 0.5: ...` on the representational path and confirming the AST guard fires; restore
byte-identically and record the mutation in the SUMMARY — the standing practice (`STATE.md:157,203`).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_phase19_erasure.py -k descriptive tests/test_phase16_prereg.py tests/test_package.py -x</automated>
  </verify>
  <done>The guard passes against the real read, was watched RED and restored; no consumer branch reads the representational numbers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a descriptive statistic → a verdict | the pre-registration forbids the crossing and the sample cannot support it |
| a Fisher estimate → an implied claim | no mask support and a tiny overlapping-window bin bound what it can say |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-61 | Elevation | representational read | mitigate | AST guard over `DESCRIPTIVE_ONLY_FUNCTIONS`, plus a no-consumer-branch check; both watched RED |
| T-19-62 | Spoofing | an overstated Fisher claim | mitigate | the two limits (no mask support, heavy window overlap on a 20,036-token bin) are required artifact fields, not prose |
| T-19-63 | Tampering | a recomputed `alpha/r` | mitigate | `adapter_cells` reads `scale` from the artifact; recomputing it is forbidden (`lora/layer.py:27`, PITFALLS P3) |
| T-19-64 | Denial of service | `mark_only_lora_trainable` + `estimate_fisher` | mitigate | estimate BEFORE marking trainability; the composition raises otherwise, verified in §Q1 (P19-8) |
| T-19-SC | Tampering | package installs | mitigate | zero installs; `tests/test_package.py` sha256 pin holds |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q tests/test_phase19_erasure.py tests/test_phase16_prereg.py tests/test_package.py`
- `grep -rn "sign_test_exact\|holm(" scripts/phase19_erasure.py` shows no call on the representational
  path.
</verification>

<success_criteria>
- The representational read exists with bounds and n, labelled DESCRIPTIVE in the artifact itself.
- It reaches no gate, proved by a guard that was watched RED against the real implementation.
- Its two honest limits travel as fields.
</success_criteria>

<output>
Create `.planning/phases/19-selective-memory-erasure/19-14-SUMMARY.md` when done.
</output>
