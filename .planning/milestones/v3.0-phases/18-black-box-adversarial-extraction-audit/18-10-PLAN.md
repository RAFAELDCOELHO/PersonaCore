---
phase: 18-black-box-adversarial-extraction-audit
plan: 10
type: execute
wave: 8
depends_on: ["18-09"]
files_modified:
  - scripts/phase18_extraction.py
  - tests/test_phase18_prereg.py
autonomous: true
requirements: [ATK-01, ATK-02, ATK-03, STAT-05]

must_haves:
  truths:
    - "D-07: the run dispatches the corpus's recorded `prompt_ids` ONCE PER ARM, so adapter-on/adapter-off divergence is impossible by construction rather than by review — there is no code path that rebuilds a prompt inside an arm"
    - "D-07: the run records the corpus sha256 in its results provenance, so a record can be tied to the exact corpus it was drawn from"
    - "D-12: the pre-flight smoke runs on the UN-ADAPTED BASE ONLY (`convbase_slim`, no adapter injected) — a different load from `adapter_disabled`, deliberately so — and covers all four prompt shapes, asserting encode/decode round-trip, `stop_ids` termination above a floor, distinct non-collapsed draws, and completions not dominated by the two MEASURED degeneration attractors (`<|assistant|>` leakage at 56/936 and the college-student attractor at 47/936 in Phase 17's base column), with ZERO preview of adapter-arm behaviour"
    - "D-28: because the pin now covers more new code than originally implied, the smoke additionally asserts finite NLL for every candidate in R across all 8 slots and spread-0 control agreement — a crash or a NaN discovered after 8.2h is the failure mode this buys out"
    - "D-12/OQ-4: the smoke measures `draws_per_min` PER PROMPT SHAPE, because 229.68 draws/min was measured on bare 14-id prompts while A3's persona span and A1's filler lengthen prefill — the 8.17h figure is a floor, not an estimate"
    - "D-09: family zero runs at exactly 9 draws with the UNSTRIDED index, preserving the `1337 + index + s` stream D-01's exact comparison requires, while the attack families pass `seed_index * K` (D-06)"
    - "ATK-02: the adapter-off arm loads the SAME way the adapter arm does and generates inside `adapter_disabled` — never a separately built un-adapted model, which would be a second load path free to differ"
    - "There is deliberately NO mode that runs more than one arm in one process — a convenience flag would turn the process split from a property of this driver into a convention an operator is trusted to follow"
  artifacts:
    - path: "scripts/phase18_extraction.py"
      provides: "build_parser, main, run_smoke, run_arm, record writer with clobber guard and provenance"
      contains: "def run_smoke"
    - path: "tests/test_phase18_prereg.py"
      provides: "one-corpus-two-arms structural proof, smoke scope proof, smoke NLL coverage proof"
      min_lines: 450
  key_links:
    - from: "scripts/phase18_extraction.py::run_arm"
      to: "phase14_recall.draw_all"
      via: "prompt_ids read from the corpus record, n_samples=K-1"
      pattern: "n_samples=K - 1|n_samples=K-1"
    - from: "scripts/phase18_extraction.py::run_smoke"
      to: "checkpoints/convbase_slim.pt"
      via: "un-adapted base load, no adapter injection"
      pattern: "convbase_slim"
---

<objective>
Add the run surface: the smoke mode, the two arm modes, the record writer, and the parser that
makes a two-arm process structurally impossible.

Purpose: this is the last driver plan. After it commits, `scripts/phase18_extraction.py` is
complete and the ancestry guard arms — every subsequent `results/phase18_*` first-add must be a
descendant of every commit in this file.
Output: `--smoke`, `--corpus`, `--arm {adapter-on,adapter-off}`, `--report`, each in its own
process, with provenance and clobber refusal. No artifact is produced by this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/18-black-box-adversarial-extraction-audit/18-CONTEXT.md
@.planning/phases/18-black-box-adversarial-extraction-audit/18-PATTERNS.md
</context>

<interfaces>
`scripts/phase14_recall.py:496-574` — `load_adapted_model(device)` returns
`(model, model_cfg, tok, forbid, artifact)`; `forbid = undecodable_ids_mask(tok, vocab_size).to(device)`.
`draw_all(model, tok, prompt_ids, device, forbid, index, *, n_samples=...)` (`:595`, widened by 18-02).

`scripts/phase17_isolation.py:990-1008` — the shipped adapter-off precedent: the base sweep loads
the SAME way the adapter sweeps do and generates inside `adapter_disabled`, because the context
manager is measured bit-identical to the un-adapted base (max abs diff exactly 0.0) and a separately
built model would be a second load path free to differ.
`:975-983` — the record-clobber refusal, called FIRST and cheapest, with no force flag.
`:960-1008` — the provenance block: `preflight_device(strict=True)`, `RuntimeConfig().device`,
`seed_everything(recall.SEED)`, `git_sha()`, `os.getpid()`, `forbid_digest(forbid)`, `wall_clock_min`.
`:2517-2570` — `main(argv=None)` with exhaustive dispatch, no default branch, and a final
`_prove(result is not None, ...)`.

`scripts/phase16_persistence.py:2557-2600` — `_USAGE` with the recorded refusal to run more than one
condition per process, and `build_parser` using `add_mutually_exclusive_group(required=True)` with
`choices=` constrained by the pre-registered tuple.

Phase 17's measured base-column degeneration priors: `<|assistant|>` leakage 56/936 and the
"i am a college student" attractor 47/936; both are published Phase 13 properties (79 naive /
70 EWC), not Phase 17 findings.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: run_smoke — four shapes, un-adapted base, measured throughput, NLL coverage</name>
  <files>scripts/phase18_extraction.py</files>
  <read_first>
    - `scripts/phase17_persona_gate.py` — `build_unadapted_base` and the ISO-01 pre-flight that ran
      on the pure un-adapted base at `04e724c6` and returned 24/24 clean over 416 completions
    - `scripts/phase14_recall.py:496-592` — `load_adapted_model` and `_complete`
    - `scripts/phase18_extraction.py` — `build_corpus` (18-05), `value_span_nll` /
      `reference_set_for` / `SPREAD_ZERO_CONTROL_SLOTS` (18-06)
    - `.planning/phases/18-black-box-adversarial-extraction-audit/18-CONTEXT.md` — D-12, D-28
  </read_first>
  <action>
    Add `SMOKE_REPORT_PATH = _REPO_ROOT / "results" / "phase18_preflight_report.md"`,
    `DEGENERATION_PRIORS` (a literal recording the two measured attractors and their 56/936 and
    47/936 base-column rates, with the note that these are published Phase 13 properties rather than
    Phase 18 findings), and `run_smoke(device)`.
    The smoke loads the UN-ADAPTED BASE — `convbase_slim`, no adapter injected — and never the
    adapter, never `adapter_disabled`; record in the docstring that this is a different load from
    the adapter-off arm and that D-12's zero-preview constraint makes it deliberately so.
    The smoke builds its prompts by calling `build_corpus(tok)` IN MEMORY and must NOT read
    `CORPUS_PATH`: `results/phase18_corpus.json` is not committed until 18-14, one wave after the
    smoke, so a file read here would abort the phase's most expensive gate on a missing artifact.
    Per prompt shape (A1-mild, A1-aggressive, A2, A3) on a small sample of that in-memory corpus it
    asserts:
    encode/decode round-trip on the prompt, `stop_ids` termination above a floor, draws distinct
    (non-collapsed), and completions not dominated by the two degeneration attractors, floored
    against `DEGENERATION_PRIORS` rather than an invented threshold. It measures and records
    `draws_per_min` PER SHAPE.
    Per D-28 it additionally asserts, still on the un-adapted base: `value_span_nll` returns FINITE
    values for every candidate in `reference_set_for(slot)` across all 8 slots under all three frames
    and both reductions, and that the two `SPREAD_ZERO_CONTROL_SLOTS` agree ordinally under sum and
    mean. Any NaN, inf or crash aborts through `_prove`.
    It emits the smoke report text but writing is the run's job in 18-13 — this task adds no
    file write at import and no call at module scope. Report no adapter-arm quantity of any kind.
  </action>
  <acceptance_criteria>
    - `grep -c "persona_adapter\|inject_lora\|adapter_disabled" scripts/phase18_extraction.py` —
      every occurrence must be inside `run_arm`, never inside `run_smoke`; assert with an AST walk
      via `.venv/bin/python -c` and record the result in the SUMMARY
    - `.venv/bin/pytest -q tests/test_phase18_prereg.py::test_smoke_scope_is_base_only -x` passes
    - `.venv/bin/pytest -q tests/test_phase18_prereg.py::test_smoke_covers_nll_path -x` passes
    - `grep -c "56\|47" scripts/phase18_extraction.py` — the two attractor priors appear as literals
      inside `DEGENERATION_PRIORS` with their `/936` denominators
    - `.venv/bin/python -c` AST walk asserts `run_smoke` contains no `CORPUS_PATH` reference and no
      `open(`/`json.load(` call, and does contain a `build_corpus` call — exits 0
    - `ls results/phase18_preflight_report.md 2>/dev/null` returns nothing — this plan writes no file
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/pytest -q tests/test_phase18_prereg.py</automated>
  </verify>
  <done>A smoke that can falsify the instrument before the pin, on the base only, with a measured rate.</done>
</task>

<task type="auto">
  <name>Task 2: run_arm — one prompt object, one arm per process, full provenance</name>
  <files>scripts/phase18_extraction.py</files>
  <read_first>
    - `scripts/phase17_isolation.py:934-1008` — `run_one_sweep`'s ordering contract, the clobber
      refusal called first, the adapter-off load comment, and the provenance block
    - `scripts/phase14_recall.py:595-637` — `draw_all` and its per-draw generator
    - `scripts/phase18_extraction.py` — `CORPUS_PATH`, `corpus_sha256` (18-05), `K`,
      `FAMILY_ZERO_DRAWS` (18-03)
    - `.planning/phases/18-black-box-adversarial-extraction-audit/18-CONTEXT.md` — D-06, D-07, D-09, ATK-02
  </read_first>
  <action>
    Add `ARM_RECORD_PATHS` derived from `ARMS` and `run_arm(arm, device)`. Call the record-clobber
    refusal FIRST and cheapest, with no force flag — a sweep record is recorded evidence and a rerun
    on drifted code would silently replace the completions every rate was scored from.
    Read `results/phase18_corpus.json` and dispatch each record's `prompt_ids` through
    `recall.draw_all(model, tok, prompt_ids, device, forbid, record["seed_index"] * K,
    n_samples=K - 1)` — the prompt is READ, never rebuilt, which is what makes the two arms paired
    by construction. Family zero passes the UNSTRIDED `seed_index` at `FAMILY_ZERO_DRAWS` draws,
    per D-09/D-01.
    Load once via `recall.load_adapted_model(device)` for both arms and generate inside
    `adapter_disabled` for `adapter-off` — never a second un-adapted model.
    Record per arm: `preflight_device(strict=True)`, the resolved device, `seed_everything(recall.SEED)`,
    `git_sha()`, `os.getpid()`, `forbid_digest(forbid)`, `wall_clock_min`, and the corpus `sha256`
    (D-07). Write nothing at import; the write happens only inside the mode.
  </action>
  <acceptance_criteria>
    - `.venv/bin/pytest -q tests/test_phase18_prereg.py::test_one_corpus_two_arms -x` passes — an AST
      proof that no prompt-construction function (`build_a1`/`build_a2_prompt`/`build_a3_prompt`/
      `build_recall_prompt`) is called anywhere inside `run_arm`
    - `grep -c "corpus_sha256" scripts/phase18_extraction.py` returns at least 2 (definition + the
      provenance record)
    - `.venv/bin/python -c` AST check: `run_arm` contains exactly one `draw_all` call site — exits 0
    - `grep -c "force" scripts/phase18_extraction.py` returns 0
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/pytest -q tests/test_phase18_prereg.py</automated>
  </verify>
  <done>One recorded prompt, two arms, two processes, one draw loop, full provenance.</done>
</task>

<task type="auto">
  <name>Task 3: build_parser and main with no two-arm mode</name>
  <files>scripts/phase18_extraction.py, tests/test_phase18_prereg.py</files>
  <read_first>
    - `scripts/phase16_persistence.py:2557-2600` — `_USAGE` and `build_parser`, including the recorded
      refusal to provide a convenience flag that runs two conditions
    - `scripts/phase17_isolation.py:2517-2570` — `main(argv=None)`, exhaustive dispatch, no default
      branch, and the closing `_prove(result is not None, ...)`
    - `.planning/phases/18-black-box-adversarial-extraction-audit/18-CONTEXT.md` — D-07, D-12
  </read_first>
  <action>
    Add `_USAGE` recording that there is deliberately NO mode running more than one arm, and why —
    the only structural way to guarantee the process split is to make a single process incapable of
    running two, because a convenience flag turns a property into a convention an operator is trusted
    to follow. Add `build_parser()` with `add_mutually_exclusive_group(required=True)` offering
    `--smoke`, `--corpus`, `--arm` (with `choices=ARMS`) and `--report`, and `main(argv=None)` with
    exhaustive dispatch, no default branch, and a closing
    `_prove(result is not None, "the selected mode produced no result ...")`. Keep the existing
    `__main__` self-check from 18-07 reachable — the self-check must still run without a model, so
    guard the CLI entry so `python scripts/phase18_extraction.py` with no arguments still exercises
    the admissibility self-check rather than erroring on a missing mode.
    Add `test_no_multi_arm_mode` asserting the parser's mode group has exactly four options, that
    `--arm` accepts only the two pre-registered names, and that no option accepts a list of arms.
  </action>
  <acceptance_criteria>
    - `.venv/bin/pytest -q tests/test_phase18_prereg.py -k parser -x` passes
    - `.venv/bin/python scripts/phase18_extraction.py --arm both` exits non-zero with an argparse
      error naming the two valid choices
    - `.venv/bin/python scripts/phase18_extraction.py` still runs the admissibility self-check and
      exits 0
    - `.venv/bin/pytest -q` full suite green and
      `.venv/bin/ruff check . && .venv/bin/ruff format --check .` clean
    - `ls results/phase18_* 2>/dev/null` returns nothing — no artifact may exist before 18-11 lands the renderer
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/pytest -q</automated>
  </verify>
  <done>The run surface is complete. The report renderer in 18-11 is the LAST driver commit; no `results/phase18_*` artifact may exist until it lands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| corpus artifact → arm process | The recorded `prompt_ids` are the only thing both arms share; anything rebuilt inside an arm is unpaired |
| smoke (base only) → pin decision | The smoke exists to inform K before the pin; any adapter-arm quantity it produced would be the preview D-12 forbids |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-10-01 | Tampering | A prompt rebuilt inside an arm silently unpairs the two arms | mitigate | AST proof that no prompt-construction call exists inside `run_arm`; the corpus sha256 is recorded per arm |
| T-18-10-02 | Information Disclosure | The smoke previews adapter-arm behaviour and contaminates the pin decision | mitigate | AST scope proof that `run_smoke` never references the adapter or `adapter_disabled` |
| T-18-10-03 | Tampering | An overwritten arm record replaces the completions a published rate was scored from | mitigate | Clobber refusal called first, no force flag, `grep` acceptance asserts zero occurrences of "force" |
| T-18-10-04 | Elevation of Privilege | A convenience flag runs both arms in one process, making the split a convention | mitigate | Parser exposes no multi-arm option; asserted by a committed test |
| T-18-10-05 | Denial of Service | A NaN or crash in the new NLL path discovered after 8.2h of GPU time | mitigate | The smoke asserts finite NLL for every candidate in R across all 8 slots before the pin |
| T-18-10-SC | Tampering | package installs | accept | Zero installs; `pyproject.toml` sha256-pinned |
</threat_model>

<verification>
- `.venv/bin/pytest -q` full suite green
- `.venv/bin/ruff check . && .venv/bin/ruff format --check .`
- `git log --oneline -- scripts/phase18_extraction.py | wc -l` recorded in the SUMMARY — this set grows
  once more in 18-11, and every `results/phase18_*` first-add must descend from all of it
</verification>

<success_criteria>
The run surface is inert at import; one prompt object reaches two arms in two processes; the smoke is
base-only, four-shape, throughput-measuring and NLL-covering.
</success_criteria>

<output>
Create `.planning/phases/18-black-box-adversarial-extraction-audit/18-10-SUMMARY.md` when done
</output>
