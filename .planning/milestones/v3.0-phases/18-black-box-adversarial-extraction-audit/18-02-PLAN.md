---
phase: 18-black-box-adversarial-extraction-audit
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - scripts/phase14_recall.py
  - tests/conftest.py
  - tests/test_phase18_draws.py
autonomous: true
requirements: [ATK-01, ATK-03, STAT-04]

must_haves:
  truths:
    - "D-09: family zero spends exactly 9 draws and the 9-draw prefix of a 63-seeded-sample run is byte-identical to a 9-draw run — proved of the real `draw_all` CODE PATH against a deterministic fake model, not of the seed arithmetic"
    - "D-06: attack draws use `SEED + index*K + s` by passing `src_index * K` as `draw_all`'s positional `index`; 216x64 strided seeds have zero collisions, while the unstrided form collapses 216x64 draw slots onto 279 distinct seeds"
    - "R-04: `draw_all` gains exactly one additive keyword `n_samples=N_SEEDED_SAMPLES`; every existing caller draws the same 9 completions from the same generators"
    - "The fake-model fixture is CPU-only and GPU-free — the D-09/D-06 proofs run in the standard suite with no checkpoint load"
  artifacts:
    - path: "scripts/phase14_recall.py"
      provides: "draw_all(..., index, *, n_samples=N_SEEDED_SAMPLES)"
      contains: "n_samples=N_SEEDED_SAMPLES"
    - path: "tests/conftest.py"
      provides: "deterministic fake model fixture satisfying GPT.forward(idx, targets=None) -> (logits, loss)"
      min_lines: 30
    - path: "tests/test_phase18_draws.py"
      provides: "test_prefix_is_budget_independent, test_strided_seeds_are_disjoint"
      min_lines: 60
  key_links:
    - from: "tests/test_phase18_draws.py"
      to: "scripts/phase14_recall.py::draw_all"
      via: "direct call at n_samples=63 and n_samples=8"
      pattern: "draw_all\\("
    - from: "scripts/phase14_recall.py::draw_all"
      to: "question_seed(index) + s"
      via: "fresh torch.Generator per draw"
      pattern: "question_seed\\(index\\) \\+ s"
---

<objective>
Widen `draw_all` with the one keyword D-06/D-09 need, and close D-09's stated gap with the
committed CPU proof it names.

Purpose: family zero runs 9 draws while the attacks run 64. D-09 accepts that only because the
9-draw prefix is bit-identical across budgets — a claim about the real loop, not about arithmetic.
This plan makes that claim a committed test before anything depends on it.
Output: `draw_all(..., n_samples=...)`, a deterministic fake model in `tests/conftest.py`, and the
two draw-layer proofs the validation contract names.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/18-black-box-adversarial-extraction-audit/18-CONTEXT.md
@.planning/phases/18-black-box-adversarial-extraction-audit/18-PATTERNS.md
</context>

<interfaces>
`scripts/phase14_recall.py:595-637` — verified signature
`draw_all(model, tok, prompt_ids, device, forbid, index)`; draw 0 is GREEDY via
`_complete(..., greedy=True)`, then `for s in range(N_SEEDED_SAMPLES)` with
`torch.Generator(device=device).manual_seed(question_seed(index) + s)`.
Constants: `SEED = 1337` (`:147`), `N_SEEDED_SAMPLES = 8` (`:152`),
`SAMPLE_TEMPERATURE = 0.8` (`:159`), `SAMPLE_TOP_P = 0.95` (`:160`),
`STOP_IDS = frozenset({8184, 8185})` (`:162`), `question_seed(index) -> SEED + index` (`:227`),
`_complete(model, prompt_ids, device, forbid, ...)` (`:577-592`).

`src/personacore/model/gpt.py:195-213` — the contract the fake model must satisfy:
`forward(idx, targets=None) -> (logits, loss)`; `logits` is `(B, T, V)`; `loss` is None when
`targets` is None.

`tests/conftest.py` currently holds one fixture, `simulate_pascal` (17 lines) — the shape to follow:
`@pytest.fixture`, `import torch` inside the body, docstring naming the guard it makes testable
on a CPU-only box.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add the n_samples keyword to draw_all</name>
  <files>scripts/phase14_recall.py</files>
  <read_first>
    - `scripts/phase14_recall.py:577-637` — `_complete` and `draw_all` in full, including the
      docstring sentence "A duplicated draw loop is how two arms silently stop being paired"
    - `scripts/phase14_recall.py:140-165` — `SEED`, `N_SEEDED_SAMPLES`, sampling constants
    - `.planning/phases/18-black-box-adversarial-extraction-audit/18-CONTEXT.md` — D-06, D-09
  </read_first>
  <action>
    Change the signature to `draw_all(model, tok, prompt_ids, device, forbid, index, *,
    n_samples=N_SEEDED_SAMPLES)` and replace `range(N_SEEDED_SAMPLES)` with `range(n_samples)`.
    Nothing else changes: draw 0 stays greedy, each seeded draw keeps its own fresh
    `torch.Generator` at `question_seed(index) + s`, and the returned `(completions, stopped)`
    shape is unchanged. Extend the docstring with two sentences — that Phase 18 passes
    `n_samples=K-1` so 1 greedy + 63 seeded equals K=64, and that the per-draw generator makes
    draw s independent of how many draws follow, which is what lets family zero run a 9-draw
    prefix of the same code path. Do not add a second loop, a batching path, or a
    `greedy=False` option. Do not change `question_seed` or add a seeding helper — D-06's stride
    is obtained by callers passing `src_index * K` as the positional `index`.
  </action>
  <acceptance_criteria>
    - `grep -n "n_samples=N_SEEDED_SAMPLES" scripts/phase14_recall.py` returns exactly 1 line
    - `grep -c "range(N_SEEDED_SAMPLES)" scripts/phase14_recall.py` returns 0
    - `.venv/bin/python -c "import inspect,sys; sys.path.insert(0,'scripts'); import phase14_recall as r; s=inspect.signature(r.draw_all); assert list(s.parameters)==['model','tok','prompt_ids','device','forbid','index','n_samples'], s; assert s.parameters['n_samples'].default==r.N_SEEDED_SAMPLES"`
      exits 0
    - `.venv/bin/pytest -q tests/test_phase14_scoring.py tests/test_phase16_driver.py` passes unchanged
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/pytest -q tests/test_phase14_scoring.py tests/test_phase16_driver.py</automated>
  </verify>
  <done>One additive keyword; default preserves every existing caller bit-for-bit.</done>
</task>

<task type="auto">
  <name>Task 2: Deterministic fake model fixture in tests/conftest.py</name>
  <files>tests/conftest.py</files>
  <read_first>
    - `tests/conftest.py` — the whole file (17 lines); `simulate_pascal` is the shape to follow
    - `src/personacore/model/gpt.py:195-213` — `forward(idx, targets=None) -> (logits, loss)`
    - `scripts/phase14_recall.py:577-592` — `_complete`, the single call site that defines what the
      fake must support (the forbid mask fill, the stop-id check, `max_new_tokens`)
    - `src/personacore/generation/` — the sampling entry point `_complete` calls, for the exact
      surface the fake must satisfy
  </read_first>
  <action>
    Add a `fake_lm` fixture returning a tiny deterministic `torch.nn.Module` whose
    `forward(idx, targets=None)` returns `(logits, loss)` with `logits` shaped `(B, T, V)` over a
    small vocab that still spans the real `STOP_IDS` (8184, 8185) and the role ids, so the stop path
    is exercisable. Make the logits a pure deterministic function of the input ids — e.g. a fixed
    integer hash of the last id broadcast into a fixed-shape score vector — with NO module
    parameters and no RNG of its own, so every run of the fixture produces the same distribution and
    the only randomness in `draw_all` is the generator under test. Return `loss=None` when
    `targets is None`, and a real `F.cross_entropy` when targets are given (18-06's NLL tests reuse
    this fixture). Keep `import torch` inside the fixture body, per `simulate_pascal`. The docstring
    must name the two guards it exists to make testable on a CPU-only box: D-09's prefix stability
    and D-29/D-30's span-NLL reductions. Do not load any checkpoint, do not touch MPS or CUDA, and
    do not add a GPU marker.
  </action>
  <acceptance_criteria>
    - `grep -n "def fake_lm" tests/conftest.py` returns exactly 1 line
    - `grep -c "cuda\|mps\|load_slim\|checkpoints/" tests/conftest.py` returns 0 outside the
      existing `simulate_pascal` fixture (verify with `grep -v simulate_pascal`)
    - `.venv/bin/pytest -q tests/test_phase18_draws.py` collects and passes with no GPU
    - Two calls to the fixture's `forward` on the same ids return tensors satisfying
      `torch.equal(a, b)` — asserted inside `tests/test_phase18_draws.py`
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/pytest -q tests/test_phase18_draws.py</automated>
  </verify>
  <done>A CPU-only, parameter-free, bit-deterministic stand-in for the 13.9M model.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The D-09 prefix-stability and D-06 strided-seed proofs</name>
  <files>tests/test_phase18_draws.py</files>
  <read_first>
    - `scripts/phase14_recall.py:595-637` — `draw_all` after Task 1
    - `scripts/phase14_recall.py:227` — `question_seed(index) -> SEED + index`
    - `tests/test_phase17_scoring.py:1-90` — the CPU-only test-module structure and the importlib
      driver-loading idiom to copy
    - `.planning/phases/18-black-box-adversarial-extraction-audit/18-CONTEXT.md` — D-06, D-09
  </read_first>
  <behavior>
    - `test_prefix_is_budget_independent`: `draw_all(..., index=i, n_samples=63)[0][:9]` equals
      `draw_all(..., index=i, n_samples=8)[0][:9]` element-for-element, and the same for the
      `stopped` list — run for at least two distinct `index` values
    - the same test asserts the run at n_samples=63 actually produced 64 completions, so a fixture
      that silently short-circuits cannot make the prefix claim vacuous
    - `test_strided_seeds_are_disjoint`: for `K=64` and 216 source indices, the set
      `{question_seed(i*K) + s for i in range(216) for s in range(K)}` has exactly `216*64` members,
      while the unstrided set `{question_seed(i) + s ...}` has 279 — the measured collapse D-06 names
    - `question_seed(i*K) == SEED + i*K` for `i` in {0, 1, 2, 111}
  </behavior>
  <action>
    Create the file with the two tests above, both driving the REAL `draw_all` (loaded via the
    `importlib.util.spec_from_file_location` idiom, not by copying the loop) against the `fake_lm`
    fixture on CPU. Use the real `question_seed` for the seed-set assertions — never a re-typed
    `1337 + i*64`. The prefix test must compare the decoded completion STRINGS, so a change to the
    generator, the sampling parameters or the loop order fails it. Add a module docstring recording
    that this file proves the CODE PATH, not the seed arithmetic, and that the arithmetic alone
    would have been provable without a test — which is exactly why D-09 asked for this one.
    Node ids must be exactly `test_prefix_is_budget_independent` and
    `test_strided_seeds_are_disjoint`, as named in 18-VALIDATION.md.
  </action>
  <acceptance_criteria>
    - `.venv/bin/pytest -q tests/test_phase18_draws.py::test_prefix_is_budget_independent -x` passes
    - `.venv/bin/pytest -q tests/test_phase18_draws.py::test_strided_seeds_are_disjoint -x` passes
    - Mutation proof, recorded in the SUMMARY: temporarily reseed `draw_all`'s generator once
      outside the loop, observe `test_prefix_is_budget_independent` RED, then restore the file
      byte-identically (`git diff --exit-code scripts/phase14_recall.py` returns 0)
    - `grep -c "1337" tests/test_phase18_draws.py` returns 0 — the seed is imported, never retyped
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/pytest -q tests/test_phase18_draws.py</automated>
  </verify>
  <done>D-09's stated gap is closed by a committed, mutation-proved CPU test; D-06's stride is proved collision-free at K=64.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| family zero (9 draws) → attack families (64 draws) | The positive control only controls the attacks if both traverse the same loop |
| per-question RNG → cross-question correlation | Unstrided seeds share generator streams across neighbouring questions, which a question-level bootstrap assumes away |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-02-01 | Tampering | `draw_all` widening changes the 9-draw stream every prior phase measured | mitigate | `n_samples` defaults to `N_SEEDED_SAMPLES`; signature asserted by an executable check; Phase 14/16 tests re-run |
| T-18-02-02 | Repudiation | The prefix-stability claim is asserted in prose but never observed failing | mitigate | Mutation proof required in the acceptance criteria — perturb the loop, watch RED, restore byte-identically |
| T-18-02-03 | Information Disclosure | A fake-model fixture that loads a real checkpoint would pull taught weights into the CPU suite | mitigate | `grep` acceptance forbids `checkpoints/`, `cuda`, `mps` in the new fixture |
| T-18-02-SC | Tampering | package installs | accept | Zero installs; `tests/test_package.py` sha256-pins `pyproject.toml` |
</threat_model>

<verification>
- `.venv/bin/pytest -q` full suite green
- `.venv/bin/ruff check . && .venv/bin/ruff format --check .`
- `git diff --exit-code scripts/phase14_recall.py` returns 0 after the mutation proof is reverted
</verification>

<success_criteria>
D-06's stride is measured collision-free and D-09's prefix stability is a committed, RED-proved
test; `draw_all` carries exactly one new keyword.
</success_criteria>

<output>
Create `.planning/phases/18-black-box-adversarial-extraction-audit/18-02-SUMMARY.md` when done
</output>
