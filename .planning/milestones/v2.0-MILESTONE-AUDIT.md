---
milestone: v2.0
milestone_name: Weight-Based Memory
audited: 2026-08-12
status: tech_debt
scores:
  requirements: 24/24
  phases: 7/7
  integration: 17/18
  flows: 3/3
  tests: 408 passed, 1 skipped, 0 failed
gaps: {}
open_items:
  human_uat:
    - phase: 15-figures-writeup
      file: .planning/phases/15-figures-writeup/15-HUMAN-UAT.md
      status: partial
      pending: 2
      items:
        - "GitHub inline legibility of the VIZ-03 disclosure line (phase15_fisher_ewc.png, 2250x750, downscaled 0.391 to ~3pt equivalent in GitHub's ~880px column)"
        - "Two-halves narrative independence in docs/REPORT.md (results 664-831 vs Decision sections 490-663)"
  stale_artifacts:
    - phase: 14-teach-then-recall-demo
      file: .planning/phases/14-teach-then-recall-demo/14-VERIFICATION.md
      issue: "frontmatter still reads status: gaps_found / 55-57, but both recorded gaps are closed in current code"
      evidence: "fresh-process import of scripts/personalize_demo.py scans 2378 loaded modules for the locked value: zero hits. phase14_recall.RECONCILIATION_A (now line 1451) no longer embeds it. Both enforcing tests were rewritten substring-aware: tests/test_phase14_scoring.py:_strings_in (depth-capped recursion over nested containers) and tests/test_phase14_demo.py:test_demo_process_is_fact_free (fresh-interpreter probe scanning all repo-owned modules for all 10 locked+soft values, asserting result['hits'] == [])."
      action: "re-stamp 14-VERIFICATION.md to passed (57/57) — no code change needed"
    - phases: [09, 10, 11, 12, 13]
      file: "*-VALIDATION.md status columns"
      issue: "status cells still carry pre-execution red marks (e.g. '❌ Wave 0 -> plan 09-01') meaning 'test not yet written'; never refreshed after execution"
      evidence: "all referenced test files exist and the full suite is green: 408 passed, 1 skipped (CUDA-only fp16 AMP smoke)"
      action: "cosmetic — nyquist_compliant: true is accurate in all 7"
tech_debt:
  - phase: 14-teach-then-recall-demo
    severity: warning
    id: W1
    item: "Runtime consumers inject with LoRAConfig() defaults instead of the artifact's own lora_config. phase14_recall.py:482 and personalize_demo.py:431. load_adapter_weights audits keys AND shapes, so a rank mismatch raises — but alpha produces no shape change, so scale = alpha/r would silently revert to default and apply the delta at the wrong magnitude with no error. Benign today only because teach_persona.py:478 also uses defaults. extract_deltas.py:295 and tests/test_lora_artifact.py:228 already do it correctly ('read FROM THE ARTIFACT')."
    requirements: [LORA-01, LORA-03, LORA-04]
    fix: "two-line change: LoRAConfig(**artifact['lora_config'])"
  - phase: 12-stage-2-conversational-fine-tune
    severity: warning
    id: W2
    carried_from: v1.0
    item: "DEBT-02's evaluate.py half is unwired. The requirement text names the file: 'forbid_ids policy threaded into evaluate.py/the retention-PPL path'. grep -c 'undecodable|forbid' scripts/evaluate.py returns 0; line 89 calls perplexity(...) with no mask. The retention half IS threaded consistently (finetune_dialog.py:206, finetune_ab.py:238, finetune_smoke.py, build_retention_bin.py). retention_anchors.json labels 2.1066 'historical unmasked reference only — NOT the curve anchor', so leaving evaluate.py unmasked may be deliberate — but no code or comment records that decision. NOTE: this identical item was logged as carried tech debt at v1.0 close (see v1.0-MILESTONE-AUDIT.md); DEBT-02 was raised in v2.0 to close it and closed only the retention half."
    requirements: [DEBT-02]
    fix: "either thread forbid_ids into evaluate.py, or record the deliberate exclusion in a comment + requirement amendment"
  - phase: 13-ewc-a-b-no-forgetting-experiment
    severity: warning
    id: W3
    item: "One of six VIZ-04 frontier points is a hand-entered literal. plot_phase13.py:48 LAMBDA0_POINT = {'dialog_ppl': 4.4453, 'retention_ppl': 5.9553}. Root cause confirmed: results/ft_lr_9e-5.csv has no retention_ppl column, so the lambda=0 point genuinely cannot be read from the CSV pipeline. Both numbers verified against results/finetune_smoke_report.md:83,116 — they match. But a re-run of the lambda=0 arm leaves the frontier silently stale."
    requirements: [VIZ-04, EWC-03]
  - phase: 15-figures-writeup
    severity: warning
    id: W4
    item: "No test pins the COMMITTED figures to a regeneration. test_plot_functions_write_pngs writes into tmp_path and never diffs against results/. All four match today (verified by regeneration: phase13_forgetting_curve, phase13_frontier, phase15_adapter_delta, phase15_fisher_ewc all byte-hash MATCH), but CI would not catch a stale committed PNG. Contrast results/phase15_norms.json, which DOES have test_extraction_reproduces_the_committed_artifact."
    requirements: [VIZ-01, VIZ-02, VIZ-03, VIZ-04]
    fix: "one assert per figure reusing the existing tmp_path output"
  - phase: 15-figures-writeup
    severity: warning
    id: W5
    item: "VIZ-04 never surfaces in the v2.0 narrative. grep -c phase13_frontier: README.md 0, docs/REPORT.md 0, demo_v2.ipynb 0; only results/phase13_ab_report.md (2). The lambda-search negative IS narrated at docs/REPORT.md:673-680 and quote L1 at :856-862, so no claim is unsupported — but the committed figure is reachable only from Phase 13's own report. VIZ-01 does cross the boundary (displayed in demo_v2.ipynb)."
    requirements: [VIZ-04, DOC-02]
  - phase: 09-lora-core
    severity: warning
    id: W6
    item: "The merge API has zero production consumers and Phase 15 duplicates its formula. merge_lora/unmerge_lora/merged_state_dict appear only in src/personacore/lora/ and tests/test_lora_merge.py. Test-only is intentional (REQUIREMENTS.md:85 — 'merge() ships as engineering completeness only') and the load-bearing half ('demo path stays unmerged') is verified WIRED. The real issue is the cross-phase contract: 09-02-SUMMARY advertises merged_state_dict as the 'Phase-15 deltaW building block', and Phase 15 does not use it — extract_deltas.py:174 adapter_cells re-implements the fold as scale * (b @ a). Two copies of the same formula in two phases, nothing pinning them equal."
    requirements: [LORA-04, VIZ-02]
  - phase: 13-ewc-a-b-no-forgetting-experiment
    severity: warning
    id: W7
    item: "A mislabeled provenance field crosses P13 -> P15. loop.py:498 and loop.py:524 save val_loss=final_loss into latest.pt, where final_loss is the last TRAIN micro-batch loss. Verified numerically: phase13_naive_latest.pt records 1.1526952981948853 = CSV train_loss@4000 (true val_loss@4000 is 1.4321022510528565); phase13_ewc_latest.pt records 1.4012203216552734 = CSV train_loss@4000 (true val_loss 1.5235939919948578). That value is copied verbatim into results/phase15_norms.json as blocks.{naive,ewc}.source_ckpt.fingerprint.val_loss. No headline number depends on it — convbase_best.pt uses the best-checkpoint save site, which records a true val loss, so the adapter's fingerprint is correct."
    requirements: [VIZ-02, VIZ-03]
  - phase: 11-conversational-data-pipeline
    severity: minor
    id: W8
    item: "render_document is exported from the personacore.dialogue barrel and unit-tested, but has zero consumers in scripts/ and is not called by encode_dialogue (the span-wise encoder is the single tokenization path). Dead-but-tested surface."
    requirements: [DATA-02]
nyquist:
  compliant_phases: 7
  partial_phases: 0
  missing_phases: 0
  overall: compliant
  note: "All 7 VALIDATION.md files declare nyquist_compliant: true. wave_0_complete is true for 14 and 15; the other five carry pre-execution status columns that were never refreshed — the referenced test files all exist and the suite is green."
security:
  files_present: [11, 12, 13, 14, 15]
  files_absent: [09, 10]
  note: "SECURITY.md exists for phases 11-15. Phases 9 and 10 predate the secure-phase step in this project's workflow; both are pure-library correctness phases with no external input surface."
---

# Milestone v2.0 "Weight-Based Memory" — Audit

**Status:** `tech_debt` — all requirements satisfied, no critical blockers, accumulated items need a decision.

**Scope:** Phases 9–15 · 39 plans · 24 requirements
**Audited:** 2026-08-12

---

## Verdict

The milestone's central claim chain is **wired end-to-end and independently corroborated**:

> frozen conversational base → LoRA adapter in the weights → fresh-process empty-prompt recall → live on/off toggle

Corroboration is not self-reported. It rests on matching SHA-256s, matching provenance trios, and byte-identical figure regeneration:

- `checkpoints/persona_adapter.pt` `base_fingerprint` is byte-identical to both `convbase_best.pt` and `convbase_slim.pt` provenance trios (`git_sha 04e724c6…`, `step 4000`, `val_loss 1.5235939979553224`), and the on-disk SHA-256 of both files matches the hashes committed verbatim in `results/phase14_recall_report.md`.
- All four committed figures regenerate to identical byte hashes from their committed CSV/JSON inputs.
- `phase13_ewc/run.csv` step-4000 `train_loss` **and** `ewc_penalty` are bit-identical to `finetune_prod.csv`, so the D-11 twin claim holds against the artifacts.
- Recall numbers land against pre-registered thresholds in a genuinely separate process (report records a distinct `pid` and wall clock): taught **0.4921** vs 0.2486 PASS, held-out **0.3483** vs 0.2000 PASS, closed-book control **0/2430**.

Full suite: **408 passed, 1 skipped, 0 failed** (the skip is `tests/test_train_loop.py:81` — fp16 AMP smoke needs a CUDA GPU).

---

## Requirements Coverage — 24/24

Cross-referenced across three independent sources (REQUIREMENTS.md traceability, phase VERIFICATION.md bodies, SUMMARY.md frontmatter). **No orphans** — every REQ-ID in the traceability table appears in its phase's VERIFICATION.md.

| REQ | Phase | Traceability | VERIFICATION | Integration | Final |
|-----|-------|--------------|--------------|-------------|-------|
| LORA-01 | 9 | `[x]` | passed | WIRED | satisfied |
| LORA-02 | 9 | `[x]` | passed | WIRED | satisfied |
| LORA-03 | 9 | `[x]` | passed | WIRED | satisfied |
| LORA-04 | 9 | `[x]` | passed | PARTIAL (W6) | satisfied |
| LORA-05 | 9 | `[x]` | passed | WIRED | satisfied |
| EWC-01 | 10 | `[x]` | passed | WIRED | satisfied |
| EWC-02 | 10 | `[x]` | passed | WIRED | satisfied |
| DATA-01 | 11 | `[x]` | passed | WIRED | satisfied |
| DATA-02 | 11 | `[x]` | passed | WIRED (W8 minor) | satisfied |
| DATA-03 | 11 | `[x]` | passed | WIRED | satisfied |
| DATA-04 | 11 | `[x]` | passed | WIRED | satisfied |
| DEBT-01 | 12 | `[x]` | passed | WIRED | satisfied |
| DEBT-02 | 12 | `[x]` | passed | **PARTIAL (W2)** | satisfied w/ caveat |
| EWC-03 | 12 | `[x]` | passed | PARTIAL (W3, W5) | satisfied |
| TUNE-01 | 12 | `[x]` | passed | WIRED | satisfied |
| TUNE-02 | 12 | `[x]` | passed | WIRED | satisfied |
| DEMO-04 | 13 | `[x]` | passed | WIRED | satisfied |
| VIZ-01 | 13 | `[x]` | passed | WIRED (W4) | satisfied |
| VIZ-04 | 13 | `[x]` | passed | PARTIAL (W3, W5) | satisfied |
| DEMO-05 | 14 | `[x]` | gaps_found → **stale** | WIRED | satisfied |
| DEMO-06 | 14 | `[x]` | gaps_found → **stale** | WIRED | satisfied |
| DEMO-07 | 14 | `[x]` | gaps_found → **stale** | WIRED | satisfied |
| VIZ-02 | 15 | `[x]` | human_needed | WIRED (W7, W4) | satisfied |
| VIZ-03 | 15 | `[x]` | human_needed | WIRED (W7, W4) | satisfied |
| DOC-02 | 15 | `[x]` | human_needed | WIRED | satisfied |

**Note on SUMMARY frontmatter:** 7 requirements (DATA-03, DATA-04, VIZ-01, VIZ-02, VIZ-03, VIZ-04, DOC-02) are not listed in any plan's `requirements-completed` frontmatter. All 7 are covered explicitly in their phase's VERIFICATION.md body, so this is a bookkeeping omission, not a coverage gap. Phase 13's SUMMARYs additionally put prose into the frontmatter list (e.g. `"[]  # DEMO-04 is DECLARED by this plan but NOT satisfied…"`), which parses as a string rather than an empty list.

---

## Phase Verification — 7/7

| Phase | Status | Score | Note |
|-------|--------|-------|------|
| 09 LoRA Core | passed | 13/13 | — |
| 10 EWC Core | passed | 12/12 | — |
| 11 Conversational Data Pipeline | passed | 16/16 | — |
| 12 Stage-2 Fine-Tune | passed | 5/5 | — |
| 13 EWC A/B | passed | 23/23 | re-verification pass 3; human UAT closed 1/1 |
| 14 Teach-Then-Recall | **gaps_found (stale)** | 55/57 | both gaps closed in code — see below |
| 15 Figures & Writeup | **human_needed** | 51/51 | 2 UAT items still pending |

### Phase 14 — verification stamp is stale, code is clean

`14-VERIFICATION.md` records two failed truths, both the same root cause: `phase14_recall.RECONCILIATION_A` embedded the locked fact value as report prose, so the "demo process holds no locked fact value" invariant was false, and both enforcing tests used exact-equality predicates structurally unable to see substring embedding.

Re-tested against current code during this audit:

- Fresh-process import of `scripts/personalize_demo.py`, scanning all **2378** loaded modules' globals substring-wise: **zero hits**. `RECONCILIATION_A` (now at line 1451) no longer embeds the value.
- `tests/test_phase14_scoring.py` now uses `_strings_in`, a depth-capped recursion that reaches strings nested inside tuples and dicts — not just bare attributes.
- `tests/test_phase14_demo.py:test_demo_process_is_fact_free` now spawns a **fresh interpreter**, scans every repo-owned module for all **10** locked + soft values, and asserts both that the scan actually reached `personalize_demo` and `phase14_recall` and that `result["hits"] == []`.

The fix landed after the 2026-08-02T14:40Z verification timestamp and the file was never re-stamped. **No code change is needed** — only a re-verification of Phase 14.

### Phase 15 — 2 human UAT items outstanding

`15-HUMAN-UAT.md` is `status: partial`, 0 passed / 2 pending. Neither can be settled from the filesystem:

1. **GitHub inline legibility.** `results/phase15_fisher_ewc.png` is 2250×750; GitHub's ~880px content column downscales it by 0.391, rendering the 8pt disclosure line at roughly 3pt equivalent. D-04's stated purpose is that the PNG cannot be misread when it travels alone. If the line is unreadable inline, the fix is a `figsize`/`fontsize` bump and a regeneration — not a claim change. (VIZ-02 downscales by 0.772, ~6.2pt — likely fine.)
2. **Two-halves narrative independence** in `docs/REPORT.md`. The structural precondition is verified (disjoint line ranges, asserted at :486-488), but "reads as a complete story" is prose judgment.

---

## Cross-Phase Integration — 17/18 wired, 3/3 flows

| Seam | Status |
|------|--------|
| P9 LoRA injection → P14 adapter load | WIRED — `r=8, alpha=16.0`, six `TARGET_PROJECTIONS`, 72 tensors / 331,776 params, identical at all three call sites |
| P10 `assemble_loss(extra_penalties=)` → P12/P13 | WIRED — not a dead seam; arms measurably diverge (naive penalty 1.593 constructed-but-never-applied vs EWC 0.134; retention 8.524 vs 3.891) |
| P11 bins → P12/P13/P14 | WIRED — `dialog_train.bin` 10,515,716 B ÷ 2 = 5,257,858 tokens; mask bin 5,257,858 B uint8, 1:1 aligned |
| P12 conversational base → P13/P14 | WIRED — fingerprints and SHA-256s match committed report |
| P12/P13 CSVs → P15 figures | WIRED — all four figures regenerate to identical hashes |
| P14 demo toggle → real adapter | WIRED — `memory_box.input` → `set_adapter_enabled` → 36 `LoRALinear.enabled` flags; `merge_lora` not imported by the demo |
| DEBT-02 → `evaluate.py` | **UNWIRED** (W2) |

**E2E flows:** FLOW-A (train→personalize→recall) COMPLETE · FLOW-B (no-forgetting evidence) COMPLETE · FLOW-C (offline CPU demo) COMPLETE.

---

## Tech Debt — 8 items

| ID | Phase | Severity | Item | REQs |
|----|-------|----------|------|------|
| W1 | 14 | warning | Consumers inject `LoRAConfig()` defaults, not the artifact's own config — `alpha` drift would be silent | LORA-01/03/04 |
| W2 | 12 | warning | DEBT-02's `evaluate.py` half unwired; requirement names the file. **Carried from v1.0** | DEBT-02 |
| W3 | 13 | warning | λ=0 frontier point is a hand-entered literal (source CSV lacks `retention_ppl`) | VIZ-04, EWC-03 |
| W4 | 15 | warning | No CI pin on committed figures — staleness undetectable | VIZ-01/02/03/04 |
| W5 | 15 | warning | VIZ-04 never surfaces in README/REPORT/notebook | VIZ-04, DOC-02 |
| W6 | 9 | warning | Merge API test-only; P15 re-implements the fold formula independently | LORA-04, VIZ-02 |
| W7 | 13 | warning | `latest.pt` records train loss under a `val_loss` key; copied into `phase15_norms.json` | VIZ-02, VIZ-03 |
| W8 | 11 | minor | `render_document` exported and tested, zero consumers | DATA-02 |

They cluster into three groups:

- **(a) Correctness-adjacent, cheap:** W1 — two lines, `LoRAConfig(**artifact["lora_config"])`.
- **(b) Staleness unpinned in CI:** W3, W4 — four asserts reusing existing `tmp_path` outputs.
- **(c) Documented-but-unclosed loose ends:** W2, W5, W6, W7, W8.

**W2 is the one repeat offender.** v1.0's audit logged "`forbid_ids` mask not threaded into `evaluate.py`" as carried debt. v2.0 raised DEBT-02 explicitly to close it, and closed only the retention half. If it carries into v3.0 unclosed, either do it or amend the requirement text to stop claiming `evaluate.py`.

---

## Nyquist Coverage — 7/7 compliant

| Phase | VALIDATION.md | `nyquist_compliant` | `wave_0_complete` |
|-------|---------------|---------------------|-------------------|
| 09 | exists | true | false |
| 10 | exists | true | false |
| 11 | exists | true | false |
| 12 | exists | true | false |
| 13 | exists | true | false |
| 14 | exists | true | true |
| 15 | exists | true | true |

The status columns in phases 9–13 still carry pre-execution red marks (`❌ Wave 0 → plan 09-01` = "test not yet written"). Every referenced test file now exists and the suite is green, so these are stale stamps rather than coverage gaps. Cosmetic.

---

## Security

`SECURITY.md` present for phases 11–15. Absent for 9 and 10, which predate the secure-phase step in this project's workflow; both are pure-library correctness phases with no external input surface.
