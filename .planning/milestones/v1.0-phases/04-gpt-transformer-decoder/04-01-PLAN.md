---
phase: 04-gpt-transformer-decoder
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_gpt_model.py
  - tests/test_gpt_attention_equiv.py
  - tests/test_gpt_layernorm.py
  - tests/test_gpt_weight_tying.py
  - tests/test_gpt_init.py
  - tests/test_gpt_param_count.py
  - tests/test_gpt_causality.py
  - tests/test_gpt_lora_seam.py
  - tests/test_gpt_overfit.py
autonomous: true
requirements: [MODEL-02, MODEL-03, MODEL-04, MODEL-05, MODEL-06, MODEL-07]
must_haves:
  truths:
    - "Nine RED test files exist, import from personacore.model.GPT, and FAIL (collection/ImportError) because gpt.py does not yet exist"
    - "Every MODEL-02..07 success criterion has a corresponding failing test asserting the exact contract (data_ptr identity, c_proj AND fc_out init std, causality non-vacuous, six named projections)"
    - "The overfit test reuses the Phase-3 train() scaffold verbatim, swapping only the model to GPT"
  artifacts:
    - path: "tests/test_gpt_model.py"
      provides: "Forward-contract (logits,loss) gate (MODEL-02)"
      contains: "def test_forward_contract"
    - path: "tests/test_gpt_attention_equiv.py"
      provides: "Manual-vs-sdpa equivalence gate (MODEL-02)"
      contains: "scaled_dot_product_attention"
    - path: "tests/test_gpt_layernorm.py"
      provides: "Hand-rolled LayerNorm vs nn.LayerNorm oracle (MODEL-02)"
      contains: "nn.LayerNorm"
    - path: "tests/test_gpt_weight_tying.py"
      provides: "data_ptr() identity gate (MODEL-03)"
      contains: "data_ptr"
    - path: "tests/test_gpt_init.py"
      provides: "Per-tensor init-std gate incl c_proj AND fc_out (MODEL-04)"
      contains: "fc_out"
    - path: "tests/test_gpt_param_count.py"
      provides: "data_ptr-dedup count in [10e6,15e6] (MODEL-05)"
      contains: "10_000_000"
    - path: "tests/test_gpt_causality.py"
      provides: "Perturbation guard, non-vacuous (MODEL-06)"
      contains: "allclose"
    - path: "tests/test_gpt_lora_seam.py"
      provides: "Six named nn.Linear reachable (MODEL-07)"
      contains: "q_proj"
    - path: "tests/test_gpt_overfit.py"
      provides: "Overfit-one-batch through existing loop (MODEL-02 SC#1)"
      contains: "fixed_batch"
  key_links:
    - from: "tests/test_gpt_*.py"
      to: "personacore.model.GPT"
      via: "import"
      pattern: "from personacore.model import GPT"
---

<objective>
Create the nine RED MODEL test files that gate the Phase-4 GPT decoder, mirroring the proven
Phase-3 seed-first / `torch.allclose` / CPU-only test idiom. These tests are the safety net for
the densest silent-bug cluster in the milestone (causal mask, weight tying, residual-scaled init).

Per MVP vertical-slice rules: the failing tests come FIRST. They define every contract the
implementation in Plan 02/03 must satisfy, so the executor builds against the contracts, not an
exploration of the codebase. All nine FAIL on `import GPT` until Plan 02 ships `gpt.py`.

Purpose: Lock the MODEL-02..07 acceptance contracts as executable RED tests before any model code.
Output: Nine `tests/test_gpt_*.py` files, all RED (ImportError on `personacore.model.GPT`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-gpt-transformer-decoder/04-CONTEXT.md
@.planning/phases/04-gpt-transformer-decoder/04-RESEARCH.md
@.planning/phases/04-gpt-transformer-decoder/04-PATTERNS.md
@.planning/phases/04-gpt-transformer-decoder/04-VALIDATION.md

<interfaces>
<!-- Contracts the tests assert against. The GPT is built in Plan 02 to satisfy these. -->

The model under test (not yet implemented — these tests are RED until Plan 02):
- `GPT(config: ModelConfig, attn_impl: str = "manual")` — constructor arg toggle (D-02; default manual)
- `forward(idx, targets=None) -> (logits, loss)` — LOCKED contract, identical to BigramLanguageModel
  (logits shape `(B, T, vocab_size)`; `targets is None` -> `(logits, None)`; else scalar CE)
- Six named `nn.Linear` per block: `q_proj`, `k_proj`, `v_proj`, `c_proj` (attn), `fc_in`, `fc_out` (MLP)
- Hand-rolled `LayerNorm` class (exported from `personacore.model.gpt`) for the oracle test
- `wte` (token embedding) and `lm_head` share ONE tensor: `lm_head.weight.data_ptr() == wte.weight.data_ptr()`
- Imported as `from personacore.model import GPT` (Plan 02 adds the barrel export)

ModelConfig defaults (config.py:74-80) the tests rely on:
`vocab_size=8192, eos_id=8184, block_size=256, n_layer=6, n_head=6, n_embd=384, dropout=0.0`
Residual-init std target: `0.02 / sqrt(2 * n_layer)` = `0.02/sqrt(12)` ≈ 0.005774
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED structural + contract gates (model, weight-tying, init, param-count, lora-seam)</name>
  <files>tests/test_gpt_model.py, tests/test_gpt_weight_tying.py, tests/test_gpt_init.py, tests/test_gpt_param_count.py, tests/test_gpt_lora_seam.py</files>
  <read_first>
    - tests/test_bigram_model.py (the seed-first construct-and-assert idiom; forward-contract shape asserts to mirror)
    - src/personacore/model/bigram.py (the LOCKED forward(idx,targets)->(logits,loss) contract and CE flatten the GPT replicates)
    - src/personacore/config.py (ModelConfig fields/defaults the tests construct: lines 64-80)
    - .planning/phases/04-gpt-transformer-decoder/04-PATTERNS.md (per-file analog + exact assert patterns, sections for test_gpt_model/weight_tying/init/param_count/lora_seam)
    - .planning/phases/04-gpt-transformer-decoder/04-RESEARCH.md (Pattern 2 init-std table lines 162-173; param-count example lines 283-302; Validation requirement→test map lines 392-402)
    - .planning/phases/04-gpt-transformer-decoder/04-VALIDATION.md (Wave 0 Requirements + per-task verification map)
  </read_first>
  <action>
    Write five RED test files, all importing `from personacore.model import GPT` and
    `from personacore.config import ModelConfig`. CPU-only, GPU-free, no `.cuda()`, no new fixture.

    `tests/test_gpt_model.py` — MODEL-02 forward contract. Include a `test_forward_contract`
    function: construct `GPT(ModelConfig(block_size=16))`, feed `idx = torch.randint(0, 8192, (2, 8))`;
    assert `logits.shape == (2, 8, 8192)` and `loss is None`; then with `targets` of the same shape
    assert `isinstance(loss, torch.Tensor)` and `loss.ndim == 0` (scalar CE over the B*T flatten).
    Mirror tests/test_bigram_model.py shape asserts. Do NOT add a tight loss-near-ln(vocab) band
    (GPT's tied/scaled init only loosely approaches it per RESEARCH A6) — if you assert a sanity
    band, make it generous.

    `tests/test_gpt_weight_tying.py` — MODEL-03. Construct `GPT(ModelConfig())`; assert
    `model.lm_head.weight.data_ptr() == model.wte.weight.data_ptr()` (true shared tensor, not a
    `.clone()` copy — RESEARCH Pitfall 2). This cross-checks the param-count test.

    `tests/test_gpt_init.py` — MODEL-04. `torch.manual_seed(0)`; construct `GPT(ModelConfig())`;
    iterate `model.named_parameters()`. Assert per-tensor std targets from the RESEARCH table:
    `wte.weight`, `wpe.weight`, `q_proj.weight`, `k_proj.weight`, `v_proj.weight`, `fc_in.weight`
    ≈ 0.02; and CRITICALLY both `c_proj.weight` AND `fc_out.weight` ≈ `0.02/sqrt(2*n_layer)` ≈
    0.005774 (D-04a — the singular "residual-scaled c_proj" roadmap phrasing is a trap; assert BOTH
    suffixes explicitly or a mis-init'd fc_out passes silently). Assert all `*.bias` are exactly 0.0
    and every LayerNorm `weight` (ln_1/ln_2/ln_f) is all ones. Use a loose tolerance for std (finite
    tensor std is noisy): e.g. `abs(p.std().item() - target) < target * 0.1`. Match by suffix
    (`name.endswith(...)`) so block-numbered params (`blocks.0.attn.c_proj.weight`) all match.

    `tests/test_gpt_param_count.py` — MODEL-05. Define a local `count_parameters(model)` that dedups
    by `p.data_ptr()` (dict keyed by data_ptr, value `p.numel()`, then `sum(values())`) so the tied
    wte/lm_head tensor counts exactly once. Assert `10_000_000 <= count_parameters(GPT(ModelConfig()))
    <= 15_000_000` (the BAND per D-11/A6, not the exact ~13.9M, so bias-count nuances do not break it).

    `tests/test_gpt_lora_seam.py` — MODEL-07. Construct `GPT(ModelConfig())`; collect
    `names = {n for n, m in model.named_modules() if isinstance(m, torch.nn.Linear)}`; for each block
    index `0..n_layer-1` assert all six projection names (`q_proj`, `k_proj`, `v_proj`, `c_proj`,
    `fc_in`, `fc_out`) are present as named `nn.Linear` modules reachable by name (suffix match). This
    guards the M2 LoRA bridge — no wrapper, no rank params this phase, just the named-module seam.
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && python -m pytest tests/test_gpt_model.py tests/test_gpt_weight_tying.py tests/test_gpt_init.py tests/test_gpt_param_count.py tests/test_gpt_lora_seam.py --collect-only 2>&1 | grep -qi "error\|cannot import\|no module" && echo RED-AS-EXPECTED</automated>
  </verify>
  <acceptance_criteria>
    - All five files exist and import `from personacore.model import GPT`.
    - Collection/run FAILS with ImportError (GPT not yet implemented) — this is the expected RED state.
    - `tests/test_gpt_init.py` asserts the `0.02/sqrt(2*n_layer)` std on BOTH `c_proj` AND `fc_out` (grep: `fc_out` AND `c_proj` both present).
    - `tests/test_gpt_param_count.py` dedups by `data_ptr()` and asserts the `[10_000_000, 15_000_000]` band.
    - `tests/test_gpt_lora_seam.py` asserts all six of `q_proj/k_proj/v_proj/c_proj/fc_in/fc_out`.
    - `make lint` passes (ruff check + format, line-length 100, import-sort).
  </acceptance_criteria>
  <done>Five RED structural/contract test files committed; all fail on `import GPT`; init test asserts both residual-output projections; param-count dedups by data_ptr.</done>
</task>

<task type="auto">
  <name>Task 2: RED numeric-equivalence + causality + overfit gates</name>
  <files>tests/test_gpt_attention_equiv.py, tests/test_gpt_layernorm.py, tests/test_gpt_causality.py, tests/test_gpt_overfit.py</files>
  <read_first>
    - tests/test_overfit_batch.py (the EXACT overfit scaffold to copy — swap only the model; train()/TrainConfig/seed_everything imports)
    - tests/test_tokenizer_oracle.py (the hand-rolled-vs-trusted-reference oracle idiom for layernorm + attention equivalence)
    - tests/test_bigram_model.py (seed-first + torch.allclose idiom)
    - src/personacore/training/loop.py (train() signature lines 129-145: keyword-only train_config/model/fixed_batch/return_final_loss; the loop is REUSED UNTOUCHED)
    - src/personacore/seeding.py (seed_everything used by the overfit + determinism tests)
    - .planning/phases/04-gpt-transformer-decoder/04-PATTERNS.md (per-file analog patterns for attention_equiv/layernorm/causality/overfit)
    - .planning/phases/04-gpt-transformer-decoder/04-RESEARCH.md (equivalence test lines 326-339; causality test lines 304-323; overfit gate lines 341-354; Pitfalls 4/5/6)
  </read_first>
  <action>
    Write four RED test files. CPU-only, GPU-free, seed-first.

    `tests/test_gpt_attention_equiv.py` — MODEL-02 manual-vs-sdpa equivalence. `torch.manual_seed(0)`;
    `cfg = ModelConfig(block_size=16)`; build `m_manual = GPT(cfg, attn_impl="manual").eval()` and
    `m_sdpa = GPT(cfg, attn_impl="sdpa").eval()`; share weights via
    `m_sdpa.load_state_dict(m_manual.state_dict())` (isolates the attn-path difference); feed
    `idx = torch.randint(0, cfg.vocab_size, (2, 12))` under `torch.no_grad()`; assert
    `torch.allclose(la, lb, atol=1e-5)`. `eval()` + dropout=0.0 are mandatory (Pitfall 4) or it flakes.
    The `1/sqrt(d_head)` scale and `(B, n_head, T, d_head)` layout must match sdpa (Pitfall 5) — this
    test is what proves it.

    `tests/test_gpt_layernorm.py` — MODEL-02 hand-rolled LayerNorm oracle. Import the hand-rolled
    `LayerNorm` from `personacore.model.gpt` (`from personacore.model.gpt import LayerNorm`).
    `torch.manual_seed(0)`; C=384; build `custom = LayerNorm(C)` and `ref = torch.nn.LayerNorm(C)`;
    feed identical random fp32 `x = torch.randn(2, 8, C)`; assert
    `torch.allclose(custom(x), ref(x), atol=1e-6)`. Fresh weight=1/bias=0 init means no param copy
    needed. The load-bearing subtlety (Pitfall 6): the hand-rolled impl uses population variance
    (`unbiased=False`) + `eps=1e-5` to match `nn.LayerNorm`; this test is the guard.

    `tests/test_gpt_causality.py` — MODEL-06, the highest-value silent-bug guard. `torch.manual_seed(1337)`;
    `model = GPT(ModelConfig(block_size=16)); model.eval()`. `B,T,V = 1, 8, model.config.vocab_size`;
    `idx = torch.randint(0, V, (B, T))`. Under `torch.no_grad()`: `logits_a, _ = model(idx)`; clone idx,
    set `t = 5`, perturb `idx2[0, t] = (idx2[0, t] + 1) % V`; `logits_b, _ = model(idx2)`. Assert BOTH:
    (1) `torch.allclose(logits_a[:, :t, :], logits_b[:, :t, :], atol=1e-6)` — past positions bit-identical;
    (2) `not torch.allclose(logits_a[:, t, :], logits_b[:, t, :], atol=1e-6)` — position t DID change.
    The second assertion is non-vacuous: without it, a model that ignores its input passes (RESEARCH line 323).

    `tests/test_gpt_overfit.py` — MODEL-02 SC#1. Copy tests/test_overfit_batch.py scaffold VERBATIM,
    swapping only `BigramLanguageModel(vocab_size=8192)` -> `GPT(ModelConfig())`. Keep
    `seed_everything(1337)` first, the `fixed_idx`/`fixed_targets` `(4,16)` randint batch, the
    `train(train_config=cfg, model=model, fixed_batch=(...), return_final_loss=True)` call, and the
    `assert float(final_loss) < math.log(8192) - 2.0` bound. The TrainConfig values (lr/max_steps) are
    a STARTING point — leave a comment that the Plan-03 executor tunes them for a 6-layer net (RESEARCH
    suggests lr≈1e-3 vs the bigram's 1e-1; Open Q1) and may use a reduced-block_size config keeping the
    architecture identical if CPU time is tight. Reuse train()/TrainConfig/seed_everything UNTOUCHED.
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && python -m pytest tests/test_gpt_attention_equiv.py tests/test_gpt_layernorm.py tests/test_gpt_causality.py tests/test_gpt_overfit.py --collect-only 2>&1 | grep -qi "error\|cannot import\|no module" && echo RED-AS-EXPECTED</automated>
  </verify>
  <acceptance_criteria>
    - All four files exist; equivalence/causality/overfit import `from personacore.model import GPT`; layernorm imports `from personacore.model.gpt import LayerNorm`.
    - Collection/run FAILS with ImportError (GPT/LayerNorm not yet implemented) — expected RED.
    - `tests/test_gpt_causality.py` contains BOTH the `allclose(...< t...)` and the `not allclose(...t...)` assertions (non-vacuous).
    - `tests/test_gpt_attention_equiv.py` uses `attn_impl="manual"` AND `attn_impl="sdpa"` and `load_state_dict` to share weights, `atol=1e-5`.
    - `tests/test_gpt_layernorm.py` compares against `torch.nn.LayerNorm` at `atol=1e-6`.
    - `tests/test_gpt_overfit.py` reuses `train(...)` with `fixed_batch` and `return_final_loss=True`; only the model changed.
    - `make lint` passes.
  </acceptance_criteria>
  <done>Four RED numeric/integration test files committed; all fail on import; causality test is non-vacuous; overfit reuses the untouched Phase-3 harness.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan adds only CPU-only pytest files for an offline, on-device model. No network, no untrusted input, no secrets, no file deserialization. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | (N/A) | Offline CPU test files | accept | No untrusted input / no network / no secrets / no deserialization — STRIDE surface is N/A for RED test scaffolding. No high-severity threats. Honest per security_context. |
| T-04-SC | Tampering | Package installs | accept | Zero new packages installed this phase (pytest already present per RESEARCH §Standard Stack). No legitimacy gate needed. |
</threat_model>

<verification>
Run `python -m pytest tests/test_gpt_*.py --collect-only` — all nine new files should error on
`import GPT` (RED, expected). Existing Phase-1/2/3 suite must still pass: `make test` should show
the prior tests green and only the new gpt tests failing on import. `make lint` clean.
</verification>

<success_criteria>
- Nine `tests/test_gpt_*.py` files exist and are RED (ImportError on `personacore.model.GPT`).
- Each MODEL-02..07 acceptance criterion has a failing test asserting its exact contract.
- `test_gpt_init.py` asserts residual-scaled init on BOTH `c_proj` and `fc_out`.
- `test_gpt_causality.py` is non-vacuous (past-unchanged AND position-t-changed).
- `test_gpt_overfit.py` reuses the Phase-3 `train()` scaffold verbatim, model swapped to GPT.
- `make lint` passes; no Phase-1/2/3 regression.
</success_criteria>

<output>
Create `.planning/phases/04-gpt-transformer-decoder/04-01-SUMMARY.md` when done.
</output>
