---
phase: 04-gpt-transformer-decoder
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/personacore/model/gpt.py
  - src/personacore/model/__init__.py
autonomous: true
requirements: [MODEL-02, MODEL-03, MODEL-04, MODEL-05, MODEL-06, MODEL-07]
must_haves:
  truths:
    - "GPT(ModelConfig) builds a ~13.9M-param GPT-2 decoder and forward(idx,targets) returns the locked (logits,loss) contract identical to the bigram, with the model staying pure (base CE only) and living at src/personacore/model/gpt.py beside bigram.py (satisfies D-05 locked forward + purity, D-06 ModelConfig sizing, D-07 module layout)"
    - "The hand-rolled manual attention path equals F.scaled_dot_product_attention(is_causal=True) within atol 1e-5, with attn_impl defaulting to manual everywhere and sdpa opt-in (satisfies D-01 dual-path equivalence, D-02 attn_impl toggle)"
    - "Hand-rolled LayerNorm equals nn.LayerNorm within atol 1e-6 (population variance) (satisfies D-09)"
    - "lm_head.weight and wte.weight are ONE shared tensor (identical data_ptr)"
    - "Init applies 0.02/sqrt(2*n_layer) std to BOTH c_proj and fc_out, 0.02 elsewhere (satisfies D-04 residual-scaled output projections / D-04a)"
    - "Perturbing token t cannot change logits at positions < t (causal mask before softmax)"
    - "All six per-block projections (q_proj/k_proj/v_proj/c_proj/fc_in/fc_out) are separate named nn.Linear modules reachable by name, not a fused c_attn (satisfies D-03 separate q/k/v+c_proj, D-04 named MLP fc_in/fc_out)"
  artifacts:
    - path: "src/personacore/model/gpt.py"
      provides: "Hand-rolled GPT-2 decoder (GPT, Block, CausalSelfAttention, MLP, LayerNorm)"
      min_lines: 120
      contains: "class GPT"
    - path: "src/personacore/model/__init__.py"
      provides: "Barrel export of GPT"
      contains: "from .gpt import GPT"
  key_links:
    - from: "src/personacore/model/gpt.py"
      to: "personacore.config.ModelConfig"
      via: "constructor reads sizing"
      pattern: "ModelConfig"
    - from: "src/personacore/model/gpt.py"
      to: "torch.nn.functional.cross_entropy"
      via: "locked CE flatten identical to bigram"
      pattern: "cross_entropy"
    - from: "src/personacore/model/__init__.py"
      to: "src/personacore/model/gpt.py"
      via: "from .gpt import GPT"
      pattern: "from .gpt import GPT"
---

<objective>
Implement the from-scratch ~13.9M-parameter GPT-2 decoder in `src/personacore/model/gpt.py` — the
central "I built a transformer" deliverable — turning the eight RED unit gates from Plan 01 GREEN.
This is the vertical slice: a single hand-rolled decoder (`GPT`, `Block`, `CausalSelfAttention`,
`MLP`, `LayerNorm`) that honors the LOCKED `forward(idx, targets=None) -> (logits, loss)` contract
so it drops into the already-proven Phase-3 harness with ZERO harness changes.

The model stays PURE: base cross-entropy only — no `assemble_loss`, no `generate`/`sample`, no
`torch.cuda.*`, no manual `.half()`/dtype casting (autocast-safe). Six named `nn.Linear` projections
per block leave the M2 LoRA seam open (naming only).

Purpose: Ship the correct from-scratch transformer math, gated by the silent-bug unit tests.
Output: `src/personacore/model/gpt.py` + the `GPT` barrel export; eight MODEL unit gates GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-gpt-transformer-decoder/04-CONTEXT.md
@.planning/phases/04-gpt-transformer-decoder/04-RESEARCH.md
@.planning/phases/04-gpt-transformer-decoder/04-PATTERNS.md

<interfaces>
<!-- Contracts extracted from the codebase. Build the GPT against these directly. -->

LOCKED forward contract to replicate UNCHANGED (src/personacore/model/bigram.py:31-39):
- `forward(self, idx, targets=None)`: build `logits (B,T,V)`; if `targets is None: return logits, None`;
  else `B, T, V = logits.shape; loss = F.cross_entropy(logits.view(B*T, V), targets.view(B*T)); return logits, loss`
- The loop calls `_, base_loss = model(xb, yb)` at loop.py:116 and assumes exactly this. Do NOT change
  the signature, the None slot, or the flatten.

ModelConfig the constructor reads (src/personacore/config.py:64-80) — NO new config layer (D-06):
- `vocab_size=8192, eos_id=8184, block_size=256, n_layer=6, n_head=6, n_embd=384, dropout=0.0`
- `d_head = n_embd // n_head = 384 // 6 = 64`

Constructor signature: `GPT(config: ModelConfig, attn_impl: str = "manual")` (RESEARCH Open Q2 —
constructor arg keeps the serialized ModelConfig free of a runtime-only flag; default "manual" per D-02).

Module to add to the barrel export (src/personacore/model/__init__.py:7-9): add
`from .gpt import GPT` and append `"GPT"` to `__all__`. This is the ONLY edit to an existing file.

Autocast-safety contract (loop.py:115-117): the loop wraps forward in `runtime.autocast()`. The model
must NOT call `.half()/.float()/.to(dtype)`, must NOT call `torch.cuda.*`; `float("-inf")` in
masked_fill is dtype-agnostic (OK); register the causal buffer as float.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hand-rolled LayerNorm + CausalSelfAttention (manual + sdpa) + MLP submodules</name>
  <files>src/personacore/model/gpt.py</files>
  <behavior>
    - tests/test_gpt_layernorm.py GREEN: hand-rolled LayerNorm == nn.LayerNorm at atol 1e-6 (population variance, eps=1e-5).
    - tests/test_gpt_attention_equiv.py GREEN: manual attention == F.scaled_dot_product_attention(is_causal=True) at atol 1e-5 with shared weights.
    - tests/test_gpt_causality.py GREEN: perturbing token t leaves logits < t bit-identical AND changes logits at t (requires the full GPT from Task 2; this task lands the mask-before-softmax mechanism the test exercises).
  </behavior>
  <read_first>
    - src/personacore/model/bigram.py (the purity rule + imports style; module docstring 9-17)
    - src/personacore/config.py (ModelConfig fields 64-80; RuntimeConfig.autocast for autocast-safety rules)
    - .planning/phases/04-gpt-transformer-decoder/04-RESEARCH.md (Pattern 3 causal buffer lines 177-192; Pattern 4 hand-rolled LayerNorm lines 194-210; CausalSelfAttention internals diagram lines 91-109; Pitfalls 5/6; Don't-Hand-Roll table lines 220-231)
    - .planning/phases/04-gpt-transformer-decoder/04-CONTEXT.md (D-01 manual+sdpa attention, D-02 attn_impl default manual, D-03 separate q/k/v_proj + c_proj, D-04 fc_in/fc_out, D-08 biases-on/GELU-tanh/dropout-wired-but-0, D-09 hand-roll LayerNorm)
    - .planning/phases/04-gpt-transformer-decoder/04-PATTERNS.md (test_gpt_layernorm + test_gpt_attention_equiv sections; autocast-safety notes)
  </read_first>
  <action>
    Create `src/personacore/model/gpt.py` with a module docstring stating the purity rule (base CE
    only; no assemble_loss/generate/torch.cuda; autocast-safe) mirroring bigram.py's docstring intent.
    Import `math`, `torch`, `torch.nn as nn`, `torch.nn.functional as F`, and `ModelConfig` from
    `..config`. Implement three submodules:

    `LayerNorm(nn.Module)` — hand-rolled (D-09). `__init__(self, ndim, eps=1e-5)`: `self.weight =
    nn.Parameter(torch.ones(ndim))`, `self.bias = nn.Parameter(torch.zeros(ndim))`, store `eps`.
    `forward(x)`: `mean = x.mean(dim=-1, keepdim=True)`; `var = x.var(dim=-1, keepdim=True,
    unbiased=False)` (population variance — MANDATORY to match nn.LayerNorm, Pitfall 6); return
    `(x - mean) / torch.sqrt(var + self.eps) * self.weight + self.bias`.

    `CausalSelfAttention(nn.Module)` — the from-scratch centerpiece (D-01). `__init__(self, config,
    attn_impl="manual")`: assert `config.n_embd % config.n_head == 0`; store `n_head`, `n_embd`,
    `attn_impl`, `d_head = n_embd // n_head`. Create FOUR separate named `nn.Linear(n_embd, n_embd)`
    (biases ON per D-08): `self.q_proj`, `self.k_proj`, `self.v_proj` (input projections — D-03, NOT a
    fused c_attn) and `self.c_proj` (output projection — residual writer). Add `self.attn_dropout` and
    `self.resid_dropout` as `nn.Dropout(config.dropout)`. Register the causal mask as a non-persistent
    buffer (Pattern 3): `self.register_buffer("tril", torch.tril(torch.ones(config.block_size,
    config.block_size)).view(1, 1, config.block_size, config.block_size), persistent=False)`.
    `forward(x)`: `B, T, C = x.shape`; project to q/k/v via the three modules; reshape each to
    `(B, n_head, T, d_head)` via `.view(B, T, n_head, d_head).transpose(1, 2)`. Branch on `attn_impl`:
    for `"manual"` — `att = (q @ k.transpose(-2, -1)) / math.sqrt(self.d_head)`; apply the mask BEFORE
    softmax: `att = att.masked_fill(self.tril[:, :, :T, :T] == 0, float("-inf"))`; `att = F.softmax(att,
    dim=-1)`; `att = self.attn_dropout(att)`; `y = att @ v`. For `"sdpa"` — `y =
    F.scaled_dot_product_attention(q, k, v, is_causal=True, dropout_p=0.0)`. Reassemble `y =
    y.transpose(1, 2).contiguous().view(B, T, C)`; `y = self.resid_dropout(self.c_proj(y))`; return y.
    The same `1/sqrt(d_head)` scale and `(B, n_head, T, d_head)` layout in both branches is what makes
    the equivalence test pass (Pitfall 5). Factor q/k/v projection OUTSIDE the branch so both paths
    consume identical tensors.

    `MLP(nn.Module)` — `__init__(self, config)`: `self.fc_in = nn.Linear(config.n_embd, 4 *
    config.n_embd)` and `self.fc_out = nn.Linear(4 * config.n_embd, config.n_embd)` (HF-style names,
    D-04; biases ON). `self.dropout = nn.Dropout(config.dropout)`. `forward(x)`: `x = self.fc_in(x)`;
    `x = F.gelu(x, approximate="tanh")` (GPT-2 gelu_new, D-08 — pure primitive, do NOT hand-roll);
    `x = self.fc_out(x)`; `x = self.dropout(x)`; return x.

    Do NOT yet add `GPT`/`Block`/init/tying — that is Task 2. This task makes the layernorm + attention
    equivalence gates GREEN once the GPT wrapper exists; commit the submodules first.
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && python -m pytest tests/test_gpt_layernorm.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_gpt_layernorm.py` passes (hand-rolled LayerNorm == nn.LayerNorm, atol 1e-6).
    - `gpt.py` contains `class LayerNorm`, `class CausalSelfAttention`, `class MLP`.
    - LayerNorm uses `unbiased=False` (population variance) — grep: `unbiased=False`.
    - Attention has separate `q_proj`, `k_proj`, `v_proj`, `c_proj` (NOT `c_attn`) — grep all four present, `c_attn` absent.
    - Mask is applied via `masked_fill(... == 0, float("-inf"))` BEFORE `F.softmax` in the manual path.
    - Causal buffer registered with `persistent=False`.
    - No `torch.cuda`, no `.half(`, no `.to(torch.float` in gpt.py — grep returns none.
    - `make lint` passes.
  </acceptance_criteria>
  <done>LayerNorm/CausalSelfAttention/MLP submodules land; layernorm oracle GREEN; manual attention has mask-before-softmax with separate q/k/v/c_proj; autocast-safe (no dtype/cuda calls).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GPT + Block assembly — init, weight-tying, forward contract, export</name>
  <files>src/personacore/model/gpt.py, src/personacore/model/__init__.py</files>
  <behavior>
    - tests/test_gpt_model.py GREEN: forward returns (logits (B,T,V), None) and (logits, scalar_loss).
    - tests/test_gpt_attention_equiv.py GREEN: full GPT manual == sdpa at atol 1e-5.
    - tests/test_gpt_causality.py GREEN: token-t perturbation guard (past unchanged, t changed).
    - tests/test_gpt_weight_tying.py GREEN: lm_head.weight.data_ptr() == wte.weight.data_ptr().
    - tests/test_gpt_init.py GREEN: 0.02 std on emb/input-proj/fc_in; 0.02/sqrt(2*n_layer) on c_proj AND fc_out; biases 0; LN weight 1.
    - tests/test_gpt_param_count.py GREEN: data_ptr-dedup count in [10e6, 15e6].
    - tests/test_gpt_lora_seam.py GREEN: six named nn.Linear per block reachable by name.
  </behavior>
  <read_first>
    - src/personacore/model/bigram.py (LOCKED forward + CE flatten lines 31-39 to replicate verbatim)
    - src/personacore/model/__init__.py (the barrel export to edit, lines 7-9; docstring already pre-specifies the change)
    - src/personacore/config.py (ModelConfig 64-80)
    - .planning/phases/04-gpt-transformer-decoder/04-RESEARCH.md (Pattern 1 init→tie→init-pass ordering lines 120-146; Pattern 2 _init_weights + per-tensor std table lines 148-175; system diagram lines 56-89; param-count example lines 283-302)
    - .planning/phases/04-gpt-transformer-decoder/04-CONTEXT.md (D-04a residual init on BOTH c_proj AND fc_out; D-05 model purity + locked forward; D-06 ModelConfig sizing; D-08 biases on; D-11 count tied once)
    - .planning/phases/04-gpt-transformer-decoder/04-PATTERNS.md (gpt.py model section + __init__.py export section + the locked-forward shared pattern)
  </read_first>
  <action>
    Add `Block` and `GPT` to `gpt.py`, then wire the export.

    `Block(nn.Module)` — pre-norm (D-08/MODEL-02). `__init__(self, config, attn_impl="manual")`:
    `self.ln_1 = LayerNorm(config.n_embd)`; `self.attn = CausalSelfAttention(config, attn_impl)`;
    `self.ln_2 = LayerNorm(config.n_embd)`; `self.mlp = MLP(config)`. `forward(x)`: `x = x +
    self.attn(self.ln_1(x))` (residual around attn); `x = x + self.mlp(self.ln_2(x))` (residual around
    MLP); return x. Norm BEFORE the sublayer, residual AROUND it.

    `GPT(nn.Module)` — `__init__(self, config: ModelConfig, attn_impl: str = "manual")`: store
    `self.config = config`. Build `self.wte = nn.Embedding(config.vocab_size, config.n_embd)`,
    `self.wpe = nn.Embedding(config.block_size, config.n_embd)`, `self.drop =
    nn.Dropout(config.dropout)`, `self.blocks = nn.ModuleList([Block(config, attn_impl) for _ in
    range(config.n_layer)])`, `self.ln_f = LayerNorm(config.n_embd)`, `self.lm_head =
    nn.Linear(config.n_embd, config.vocab_size, bias=False)` (bias-free head — Anti-Pattern: a head
    bias would be a second untied head param).

    THEN, in this exact ORDER (Pattern 1 — ordering is load-bearing):
    (1) `self.apply(self._init_weights)` — the base GPT-2 init pass over ALL params.
    (2) Residual-scaled override: iterate `self.named_parameters()`; for any param whose name ends with
        `c_proj.weight` OR `fc_out.weight`, re-init `torch.nn.init.normal_(p, mean=0.0, std=0.02 /
        math.sqrt(2 * config.n_layer))`. BOTH suffixes — D-04a; scaling only c_proj silently mis-inits
        fc_out (the single most error-prone fact; the init test asserts both).
    (3) WEIGHT TYING AFTER init: `self.lm_head.weight = self.wte.weight` (assign the SAME nn.Parameter
        object so `data_ptr()` is identical — NOT `.data.clone()`/`copy_`, which makes two tensors,
        Pitfall 2). Doing this AFTER init means the surviving tensor is the embedding init (std 0.02)
        and lm_head is never separately re-initialized.

    `_init_weights(self, module)` (Pattern 2): if `isinstance(module, nn.Linear)`:
    `torch.nn.init.normal_(module.weight, mean=0.0, std=0.02)`; if `module.bias is not None`:
    `torch.nn.init.zeros_(module.bias)`. elif `isinstance(module, nn.Embedding)`:
    `torch.nn.init.normal_(module.weight, mean=0.0, std=0.02)`. (Hand-rolled LayerNorm weight=1/bias=0
    are set in its own __init__ — no dispatch needed.)

    `forward(self, idx, targets=None)` — replicate the LOCKED bigram contract (D-05): `B, T = idx.shape`;
    `tok_emb = self.wte(idx)` (B,T,C); `pos = torch.arange(T, device=idx.device)`; `pos_emb =
    self.wpe(pos)` (T,C, broadcasts); `x = self.drop(tok_emb + pos_emb)`; loop `for block in self.blocks:
    x = block(x)`; `x = self.ln_f(x)`; `logits = self.lm_head(x)` (B,T,V). Then EXACTLY the bigram tail:
    `if targets is None: return logits, None`; else `B, T, V = logits.shape; loss =
    F.cross_entropy(logits.view(B * T, V), targets.view(B * T)); return logits, loss`. Do NOT add
    generate/sample/assemble_loss — the model stays pure (D-05).

    Finally edit `src/personacore/model/__init__.py`: add `from .gpt import GPT` and append `"GPT"` to
    `__all__` (the ONLY edit to an existing file).
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && python -m pytest tests/test_gpt_model.py tests/test_gpt_attention_equiv.py tests/test_gpt_causality.py tests/test_gpt_weight_tying.py tests/test_gpt_init.py tests/test_gpt_param_count.py tests/test_gpt_lora_seam.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - All seven listed unit gates pass (model contract, equivalence, causality, tying, init, param-count, lora-seam).
    - Weight tying is `self.lm_head.weight = self.wte.weight` (Parameter assignment) — grep present; no `.data.clone()`/`copy_` tying.
    - Residual init overrides BOTH `c_proj.weight` and `fc_out.weight` with `0.02 / math.sqrt(2 * config.n_layer)` — grep both suffixes in the override loop.
    - Tying happens AFTER `self.apply(self._init_weights)` and after the residual override (ordering correct).
    - `lm_head` is `bias=False`.
    - forward tail is the verbatim bigram CE flatten `logits.view(B*T, V)` / `targets.view(B*T)`.
    - `model/__init__.py` exports `GPT`: `from personacore.model import GPT` succeeds.
    - `make test` full suite GREEN (no Phase-1/2/3 regression); `make lint` passes.
  </acceptance_criteria>
  <done>GPT+Block assembled; eight unit gates GREEN; weight-tying is a true shared tensor; residual init on both output projections; forward honors the locked contract; GPT exported.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Pure-Python addition to an offline, on-device model package. No network, no untrusted runtime input (idx is produced by the trusted Phase-2 tokenizer), no secrets, no file deserialization (tests construct models in-process). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Denial of Service | `idx >= vocab_size` → embedding index crash | accept | The tokenizer (Phase 2) is the upstream validation boundary; the model assumes in-range idx. No in-hot-path validation (RESEARCH §Security V5 minimal). Not a high-severity threat for an offline library. |
| T-04-03 | (N/A) | Offline CPU model math | accept | No network / no secrets / no auth / no deserialization of untrusted data — STRIDE surface N/A for an offline from-scratch model. No high-severity threats. Honest per security_context. |
| T-04-SC | Tampering | Package installs | accept | Zero new packages installed (PyTorch-only, already present per RESEARCH §Standard Stack). No legitimacy gate needed. |
</threat_model>

<verification>
`make test` — the full CPU-only suite must be GREEN, including all eight Phase-4 unit gates and the
untouched Phase-1/2/3 tests (proves the GPT swap did not regress the harness). `make lint` clean.
Confirm `gpt.py` imports nothing from `training/` and contains no `assemble_loss`/`generate`/
`torch.cuda`/`.half(`.
</verification>

<success_criteria>
- `src/personacore/model/gpt.py` implements the hand-rolled GPT-2 decoder (GPT, Block,
  CausalSelfAttention, MLP, LayerNorm).
- Eight MODEL unit gates GREEN (layernorm, attention-equiv, model-contract, causality, weight-tying,
  init, param-count, lora-seam).
- Weight tying is a true shared tensor (data_ptr identity); residual init on BOTH c_proj and fc_out.
- Model is pure (base CE only) and autocast-safe; `GPT` exported from `personacore.model`.
- Full suite GREEN; `make lint` passes.
</success_criteria>

<output>
Create `.planning/phases/04-gpt-transformer-decoder/04-02-SUMMARY.md` when done.
</output>
