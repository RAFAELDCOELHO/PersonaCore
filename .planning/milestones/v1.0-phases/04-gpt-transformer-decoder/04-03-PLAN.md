---
phase: 04-gpt-transformer-decoder
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - tests/test_gpt_overfit.py
autonomous: true
requirements: [MODEL-02]
must_haves:
  truths:
    - "The real GPT(ModelConfig) overfits a single fixed batch through the UNTOUCHED training/loop.py, driving loss well below ln(8192)-2"
    - "The overfit gate proves the GPT swaps into the Phase-3 harness with zero harness changes (only the model differs)"
  artifacts:
    - path: "tests/test_gpt_overfit.py"
      provides: "GREEN overfit-one-batch integration gate for the GPT (MODEL-02 SC#1)"
      contains: "fixed_batch"
  key_links:
    - from: "tests/test_gpt_overfit.py"
      to: "personacore.training.loop.train"
      via: "train(model=GPT(...), fixed_batch=..., return_final_loss=True)"
      pattern: "train\\("
    - from: "tests/test_gpt_overfit.py"
      to: "personacore.model.GPT"
      via: "model swap"
      pattern: "GPT\\(ModelConfig"
---

<objective>
Turn the overfit-one-batch integration gate (`tests/test_gpt_overfit.py`) from RED to GREEN by tuning
the training hyperparameters so the real `GPT(ModelConfig())` memorizes a single fixed batch through
the EXISTING, UNTOUCHED `training/loop.py`. This is the MVP end-to-end proof — success criterion #1 —
that the from-scratch transformer drops into the proven Phase-3 harness with zero harness changes.

The bigram overfit used `lr=1e-1`; a 6-layer pre-norm GPT typically wants a smaller `lr` (~1e-3) over
a few hundred steps (RESEARCH Open Q1). The executor tunes `lr`/`max_steps` (and may use a reduced
`block_size` config keeping the architecture identical if CPU time is tight) until loss lands well
under `ln(8192) - 2`. Only the test file changes — `train()`, `TrainConfig`, `seed_everything` are
reused untouched.

Purpose: Prove the GPT learns end-to-end through the real loop (the harness-swap acceptance gate).
Output: `tests/test_gpt_overfit.py` GREEN; full suite GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-gpt-transformer-decoder/04-CONTEXT.md
@.planning/phases/04-gpt-transformer-decoder/04-RESEARCH.md

<interfaces>
<!-- The untouched harness the GPT plugs into. Tune the test against this signature. -->

train() keyword-only signature (src/personacore/training/loop.py:129-145) — REUSED UNTOUCHED:
`train(*, train_config, runtime_config=None, model=None, model_config=None, corpus_path=None,
eos_id=8184, fixed_batch=None, scaler=None, resume_from=None, checkpoint_path=None, log_path=None,
max_steps_override=None, eval_interval=1, return_final_loss=False)`
- `fixed_batch=(idx, targets)` drives the overfit path (one batch every step).
- `return_final_loss=True` returns the final training loss (the value the test asserts).

TrainConfig fields (src/personacore/config.py:83-94): `lr=3e-4, batch_size=64, max_steps=5000,
warmup_steps=100, grad_clip=1.0, grad_accum_steps=1, weight_decay=0.1, seed=1337`.

GPT (shipped in Plan 02): `GPT(ModelConfig())` — the real 6-layer config; honors the locked
`forward(idx, targets) -> (logits, loss)` contract so the loop trains it unchanged.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tune and green the GPT overfit gate through the existing loop</name>
  <files>tests/test_gpt_overfit.py</files>
  <read_first>
    - tests/test_gpt_overfit.py (the RED scaffold from Plan 01 — the file being tuned)
    - tests/test_overfit_batch.py (the proven bigram overfit reference: seed_everything(1337) first, (4,16) fixed batch, TrainConfig(lr=1e-1, warmup_steps=0, max_steps=300, ...), assert < ln(8192)-2)
    - src/personacore/training/loop.py (train() signature 129-145 and the overfit/fixed_batch path; REUSED UNTOUCHED)
    - src/personacore/config.py (TrainConfig fields 83-94; ModelConfig 64-80)
    - .planning/phases/04-gpt-transformer-decoder/04-RESEARCH.md (overfit gate lines 341-354; Open Q1 lr/max_steps/block_size guidance lines 472-475; Pitfall 4 eval/seed)
    - .planning/phases/04-gpt-transformer-decoder/04-CONTEXT.md (D-10 overfit gate reuse; D-06 ModelConfig sizing)
  </read_first>
  <action>
    Adjust `tests/test_gpt_overfit.py` so the real `GPT(ModelConfig())` reliably overfits the single
    fixed batch and the test passes on CPU within ~30-90s.

    Keep the scaffold structure: `seed_everything(1337)` FIRST (determinism, Pitfall 5); the fixed
    `(4, 16)` `torch.randint(0, 8192, ...)` `idx`/`targets` batch; the
    `train(train_config=cfg, model=model, fixed_batch=(fixed_idx, fixed_targets),
    return_final_loss=True)` call; and the assertion `float(final_loss) < math.log(8192) - 2.0`.

    Tune the `TrainConfig`: a 6-layer pre-norm GPT typically wants a SMALLER `lr` than the bigram's
    1e-1 — start at `lr=1e-3`, `warmup_steps=0`, `max_steps≈300-500`, `batch_size=4`,
    `grad_accum_steps=1`. Increase `max_steps` or adjust `lr` only as needed to clear the bound; do
    NOT loosen the `ln(8192) - 2.0` threshold (it is the acceptance contract). If the full
    `ModelConfig` overfit is too slow on CPU, you MAY construct the model with a reduced `block_size`
    (e.g. `ModelConfig(block_size=16)`) — the architecture (n_layer/n_head/n_embd) stays identical, so
    the harness-swap proof still holds; the success criterion is "the real GPT overfits through the
    existing loop," not a specific block_size. Document the chosen values in a brief comment.

    Reuse `train()`, `TrainConfig`, `seed_everything`, `GPT` UNTOUCHED — only this test file changes.
    Run the gate to confirm GREEN, then run the full suite to confirm no regression.
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && python -m pytest tests/test_gpt_overfit.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_gpt_overfit.py` PASSES: the GPT drives `final_loss < math.log(8192) - 2.0`.
    - The test still calls `train(..., fixed_batch=..., return_final_loss=True)` with the model swapped to `GPT(ModelConfig(...))` — no harness file touched (`git status` shows only the test changed).
    - `seed_everything(1337)` is called before model construction.
    - The `ln(8192) - 2.0` threshold is NOT loosened.
    - Full suite GREEN: `make test` passes (all nine Phase-4 gates + Phase-1/2/3).
    - `make lint` passes.
  </acceptance_criteria>
  <done>The GPT overfit gate is GREEN through the untouched loop; the harness-swap acceptance proof holds; full suite passes with no regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | A CPU-only pytest tuning change for an offline model. No network, no untrusted input, no secrets, no file deserialization. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | (N/A) | Offline CPU overfit test | accept | No untrusted input / no network / no secrets / no deserialization — STRIDE surface N/A for an offline integration test. No high-severity threats. Honest per security_context. |
| T-04-SC | Tampering | Package installs | accept | Zero new packages installed (PyTorch + pytest already present). No legitimacy gate needed. |
</threat_model>

<verification>
`python -m pytest tests/test_gpt_overfit.py -x` GREEN, then `make test` GREEN across the whole suite
(proves the GPT swap into the loop is correct end-to-end and regressed nothing). `git status` confirms
only `tests/test_gpt_overfit.py` changed in this plan. `make lint` clean.
</verification>

<success_criteria>
- `tests/test_gpt_overfit.py` is GREEN: the real GPT overfits one fixed batch through the untouched loop.
- The harness-swap proof holds — only the model differs from the bigram overfit; no harness file changed.
- The acceptance threshold `ln(8192) - 2.0` is unchanged.
- Full CPU-only suite GREEN; `make lint` passes.
</success_criteria>

<output>
Create `.planning/phases/04-gpt-transformer-decoder/04-03-SUMMARY.md` when done.
</output>
