---
phase: 07-evaluation
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/personacore/config.py
  - src/personacore/model/gpt.py
  - tests/test_ablation_config.py
  - scripts/evaluate.py
autonomous: true
requirements: [EVAL-01, EVAL-02, EVAL-03]
must_haves:
  truths:
    - "ModelConfig exposes weight_tying and use_pos_emb flags that default to True (reproducing today's architecture bit-for-bit)"
    - "weight_tying=False produces an untied lm_head (distinct data_ptr, +3,145,728 params)"
    - "use_pos_emb=False drops the learned positional embedding (forward still runs, -98,304 params)"
    - "The full existing model test suite stays green under the new defaults"
    - "scripts/evaluate.py loads the real best.pt, reports headline full-val perplexity WITH its token count, and emits curated qualitative samples"
  artifacts:
    - path: "src/personacore/config.py"
      provides: "Additive ModelConfig.weight_tying + ModelConfig.use_pos_emb flags"
      contains: "weight_tying: bool = True"
    - path: "src/personacore/model/gpt.py"
      provides: "Flag-gated weight-tie and positional-embedding branches (defaults = today)"
      contains: "if config.weight_tying"
    - path: "scripts/evaluate.py"
      provides: "Headline-PPL + curated-samples driver (no argparse, thin shell)"
      contains: "perplexity"
  key_links:
    - from: "src/personacore/model/gpt.py"
      to: "src/personacore/config.py weight_tying flag"
      via: "if config.weight_tying: self.lm_head.weight = self.wte.weight"
      pattern: "if config.weight_tying"
    - from: "src/personacore/model/gpt.py"
      to: "src/personacore/config.py use_pos_emb flag"
      via: "if self.config.use_pos_emb: x = x + self.wpe(pos)"
      pattern: "if self.config.use_pos_emb"
    - from: "scripts/evaluate.py"
      to: "personacore.evaluation.perplexity"
      via: "preflight_device(strict=True) gate, THEN runtime = RuntimeConfig(); perplexity(model, VAL_BIN, ModelConfig().block_size, runtime.device)"
      pattern: "perplexity\\("
    - from: "scripts/evaluate.py"
      to: "personacore.generation.generate_text_str"
      via: "generate_text_str(model, tok, prompt, ...)"
      pattern: "generate_text_str"
---

<objective>
Add the two additive, backward-compatible ModelConfig flags (`weight_tying`, `use_pos_emb`) and
gate the two hard-wired GPT seams on them, so the locked no-weight-tying and no-positional-embedding
ablations can EXIST at all — with defaults that reproduce today's architecture bit-for-bit. Then
ship the EVAL-01 headline-perplexity + EVAL-02 curated-samples driver (`scripts/evaluate.py`) that
runs the real `best.pt` end-to-end.

Purpose: This plan OWNS the one honest tension in this phase. CONTEXT says Phase 7 "does not change
the model," but two of the three locked ablations are impossible to express today —
`gpt.py:177` unconditionally ties `lm_head.weight = wte.weight` and `gpt.py:188-194`
unconditionally adds positional embeddings, and `ModelConfig` has no toggle. The resolution is an
ADDITIVE, default-preserving edit: the flags default to `True`/`True`, so `GPT(ModelConfig())` is
identical to today (same ~13.89M params, same tied `data_ptr`). The "does not change the model"
framing holds for the ARTIFACTS — `best.pt`, `tokenizer.json`, `loop.py`, the checkpoint schema,
and `generate()` are all untouched — and the affected task states this distinction explicitly.

Output: edited `config.py` + `gpt.py` (additive), `tests/test_ablation_config.py` green,
`scripts/evaluate.py` (the headline-PPL + samples driver).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-evaluation/07-RESEARCH.md
@.planning/phases/07-evaluation/07-PATTERNS.md
@.planning/phases/07-evaluation/07-01-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. Use directly — no codebase exploration required. -->

From src/personacore/config.py (ModelConfig, lines 86-92 — APPEND the two new fields, do NOT reorder):
  vocab_size: int = 8192
  eos_id: int = 8184
  block_size: int = 256
  n_layer: int = 6
  n_head: int = 6
  n_embd: int = 384
  dropout: float = 0.0
  # NEW: weight_tying: bool = True ; use_pos_emb: bool = True

From src/personacore/model/gpt.py:
  - line 177 (in __init__, currently UNCONDITIONAL): self.lm_head.weight = self.wte.weight
  - lines 188-194 (in forward, currently UNCONDITIONAL):
      tok_emb = self.wte(idx)
      pos = torch.arange(T, device=idx.device)
      pos_emb = self.wpe(pos)
      x = self.drop(tok_emb + pos_emb)
  - self.wpe = nn.Embedding(config.block_size, config.n_embd) stays defined regardless (only its
    use in forward is gated); the unused-but-present wpe under use_pos_emb=False is fine — its
    params just are not counted against the active forward (the -98,304 delta comes from the head/
    embedding accounting; verify the count test).

DEVICE RESOLUTION (two distinct objects — do NOT conflate; preflight returns a dict, NOT a config):
  from personacore.preflight import preflight_device  # preflight_device(strict=True) -> dict (GATE only)
  from personacore.config import RuntimeConfig        # RuntimeConfig() -> has .device (MPS-aware, D-02)
  - preflight_device(strict=True) is a side-effecting GATE: it asserts a usable accelerator and
    RAISES on a CPU-only box; it returns a summary DICT ({"device","cc","torch"}) — it is NOT the
    runtime config and has no usable .device contract for model placement.
  - RuntimeConfig() is the object that carries `.device` for `model.to(...)` / perplexity(...).
  - Canonical pattern (scripts/pretrain_tinystories.py:65 + 76 + 121):
      preflight_device(strict=True)   # gate (side-effect / raise); summary dict only
      runtime = RuntimeConfig()       # the device-carrying config
      model.to(runtime.device)        # placement uses runtime.device, NEVER the preflight dict

From src/personacore/evaluation (Plan 01):
  from personacore.evaluation import perplexity   # perplexity(model, bin_path, block_size, device) -> (ppl, total_tokens)

From src/personacore/generation/__init__.py:
  from personacore.generation import generate_text_str   # generate_text_str(model, tokenizer, prompt, **kw) -> str

From src/personacore/tokenizer/__init__.py:
  from personacore.tokenizer import from_json            # from_json("artifacts/tokenizer.json") -> BPETokenizer

From scripts/pretrain_tinystories.py (the thin-driver pattern to copy):
  - line 32: os.environ.setdefault("PYTORCH_ENABLE_MPS_FALLBACK", "1")  BEFORE import torch
  - lines 43-49: _REPO_ROOT = pathlib.Path(__file__).resolve().parent.parent ; VAL_BIN / TOKENIZER_PATH constants
  - line 65: preflight_device(strict=True)   # GATE (returns a summary dict; do NOT treat as the runtime)
  - line 76: runtime = RuntimeConfig()        # the device-carrying config (.device)
  - lines 117-121: blob = torch.load(BEST_PATH, weights_only=False) ; model.load_state_dict(blob["model"]) ; model.to(runtime.device)

From src/personacore/checkpoint.py:96 — load path only model.load_state_dict; it does NOT rebuild
  ModelConfig from the stored dict, so the existing 7-key best.pt loads unchanged. Prefer
  GPT(ModelConfig(**blob["model_config"])) — missing flag keys fall back to True/True (the trained arch).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Additive ModelConfig flags + flag-gated GPT seams (defaults = today, suite stays green)</name>
  <files>src/personacore/config.py, src/personacore/model/gpt.py</files>
  <read_first>
    - src/personacore/config.py:86-92 (the ModelConfig field block to append to — do NOT reorder existing fields)
    - src/personacore/model/gpt.py:174-177 (the unconditional weight-tie to gate)
    - src/personacore/model/gpt.py:188-194 (the unconditional positional add to gate)
    - tests/test_gpt_weight_tying.py:18-19 (the data_ptr identity gate that depends on weight_tying=True default)
    - tests/test_gpt_param_count.py:16-21 (count_parameters; the 10M-15M band depends on the tied default ~13.89M)
    - .planning/phases/07-evaluation/07-PATTERNS.md § config.py EDIT + § gpt.py EDIT (the exact two-line gates)
    - .planning/phases/07-evaluation/07-RESEARCH.md § Pattern 2 (the backward-compatibility reconciliation)
  </read_first>
  <behavior>
    - GPT(ModelConfig()) is bit-for-bit today's arch: lm_head.weight.data_ptr() == wte.weight.data_ptr() AND count_parameters == 13,891,584.
    - GPT(ModelConfig(weight_tying=False)): lm_head.weight.data_ptr() != wte.weight.data_ptr() AND count_parameters == 17,037,312 (untied lm_head adds vocab_size*n_embd = 3,145,728).
    - GPT(ModelConfig(use_pos_emb=False)): forward on a (1, T) input runs without error; count_parameters == 13,793,280.
    - The existing model suite (test_gpt_weight_tying, test_gpt_param_count, test_gpt_init, test_gpt_model, test_gpt_overfit, test_config, test_checkpoint, test_best_ckpt) stays green.
  </behavior>
  <action>
    HONEST FRAMING (state this in the SUMMARY): this is an ADDITIVE model-code edit. The phase
    "does not change the model" holds for the ARTIFACTS (best.pt, tokenizer.json, loop.py,
    checkpoint schema, generate() are untouched); it does change config.py + gpt.py additively,
    with defaults that reproduce today's architecture exactly. This is the minimum edit that lets
    the locked no-tie / no-pos ablations exist.
    In config.py, APPEND two fields to the ModelConfig dataclass body (after `dropout`, do NOT
    reorder): `weight_tying: bool = True` and `use_pos_emb: bool = True`, each with a one-line
    comment naming the ablation it enables (EVAL-03). In gpt.py __init__, wrap the weight-tie line
    in `if config.weight_tying:` — the default branch keeps `self.lm_head.weight = self.wte.weight`
    (today's tied behavior); under False, leave `lm_head.weight` as its own freshly-init'd tensor.
    In gpt.py forward, replace the unconditional positional add: set `x = tok_emb`, then
    `if self.config.use_pos_emb:` add `x = x + self.wpe(pos)` (with `pos = torch.arange(T,
    device=idx.device)`), then `x = self.drop(x)`. Keep `self.wpe = nn.Embedding(...)` defined in
    __init__ regardless (only its forward USE is gated). Do NOT touch the residual-scaled init, the
    attn_impl arg, or any other line. Verify `asdict(ModelConfig)` (used by save_checkpoint) simply
    gains two keys on FUTURE saves — the existing 7-key best.pt still loads via load_state_dict
    only. No fenced code in this action; edit the .py files directly.
  </action>
  <verify>
    <automated>python -m pytest tests/test_ablation_config.py tests/test_gpt_weight_tying.py tests/test_gpt_param_count.py tests/test_gpt_init.py tests/test_config.py tests/test_checkpoint.py tests/test_best_ckpt.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_ablation_config.py -x -q` exits 0 (test_defaults_unchanged, test_untie, test_no_pos all green).
    - `python -c "from personacore.config import ModelConfig; from personacore.model import GPT; m=GPT(ModelConfig()); assert m.lm_head.weight.data_ptr()==m.wte.weight.data_ptr()"` exits 0 (default still tied).
    - `python -c "from personacore.config import ModelConfig; from personacore.model import GPT; m=GPT(ModelConfig(weight_tying=False)); assert m.lm_head.weight.data_ptr()!=m.wte.weight.data_ptr()"` exits 0 (untie works).
    - `grep -c "if config.weight_tying" src/personacore/model/gpt.py` >= 1 AND `grep -c "if self.config.use_pos_emb" src/personacore/model/gpt.py` >= 1.
    - `make test` (full CPU suite) exits 0 — no regression in the model-flag blast radius.
  </acceptance_criteria>
  <done>The two flags exist defaulting to True; the three ablation-config tests are green; the existing model suite is still fully green; GPT(ModelConfig()) reproduces the trained architecture bit-for-bit.</done>
</task>

<task type="auto">
  <name>Task 2: scripts/evaluate.py — headline full-val PPL (EVAL-01) + curated samples (EVAL-02)</name>
  <files>scripts/evaluate.py</files>
  <read_first>
    - scripts/pretrain_tinystories.py (the whole file — the thin no-argparse driver shell: _REPO_ROOT constants, main(), PYTORCH_ENABLE_MPS_FALLBACK before import torch, the preflight_device(strict=True) GATE at line 65, the SEPARATE runtime = RuntimeConfig() at line 76, the best.pt load idiom at 117-121 with model.to(runtime.device))
    - src/personacore/evaluation/perplexity.py (the perplexity(model, path, block_size, device) -> (ppl, total_tokens) contract from Plan 01)
    - src/personacore/generation/text.py:39-102 (generate_text / generate_text_str — [eos_id]-seed, prompt-strip, EOS-stop; empty prompt seeds exactly [eos_id])
    - src/personacore/tokenizer/io.py:56 (from_json — load the FROZEN artifacts/tokenizer.json, never retrain)
    - .planning/phases/07-evaluation/07-PATTERNS.md § scripts/evaluate.py + § Shared Patterns (the best.pt load + sample loop idioms)
    - .planning/phases/07-evaluation/07-RESEARCH.md § Pitfall 5 (headline PPL != recorded 2.091 — present the full-sweep number as canonical)
  </read_first>
  <action>
    Create `scripts/evaluate.py` as a thin no-argparse driver (Phase-1 D-04), mirroring
    pretrain_tinystories.py. Set `os.environ.setdefault("PYTORCH_ENABLE_MPS_FALLBACK", "1")` BEFORE
    `import torch`. Define `_REPO_ROOT = pathlib.Path(__file__).resolve().parent.parent` and the
    constants `BEST_PATH = _REPO_ROOT/"checkpoints"/"best.pt"`,
    `VAL_BIN = _REPO_ROOT/"data"/"val.bin"`,
    `TOKENIZER_PATH = _REPO_ROOT/"artifacts"/"tokenizer.json"`, and a `RESULTS_DIR =
    _REPO_ROOT/"results"` (mkdir parents/exist_ok — a git-TRACKED path, NOT logs/ or checkpoints/
    which are gitignored). In `main()`, follow the canonical TWO-object device pattern (preflight
    returns a DICT, not the runtime — see the interfaces block): FIRST call
    `preflight_device(strict=True)` as a side-effecting GATE only (it asserts a usable accelerator
    and RAISES on a CPU-only box; do NOT capture its return for device placement), THEN build the
    device-carrying config with `runtime = RuntimeConfig()` and use `runtime.device` for all
    placement/scoring. Load `blob = torch.load(BEST_PATH, weights_only=False)` (own trusted file
    only — never a foreign checkpoint, per T-07-02); reconstruct
    `model = GPT(ModelConfig(**blob["model_config"]))` then `model.load_state_dict(blob["model"])`,
    `model.to(runtime.device)`, `model.eval()` — the existing 7-key best.pt config dict has missing
    flag keys that fall back to True/True (the trained arch). EVAL-01: call
    `ppl, total_tokens = perplexity(model, VAL_BIN, ModelConfig().block_size, runtime.device)` and
    PRINT both `ppl` AND `total_tokens` (D-03 — the auditable denominator); note in output that this
    deterministic full-sweep number is the canonical headline (distinct from best.pt's recorded
    random-batch val_loss 0.7378 / ppl 2.091, per Pitfall 5). EVAL-02: load
    `tok = from_json(TOKENIZER_PATH)`; define a FIXED prompt set (e.g. "Once upon a time", "The
    little robot", "Lily and Tom went to the", and "" for the free-running [eos_id]-seeded
    document-start); for each prompt generate BOTH a greedy sample (`generate_text_str(model, tok,
    p, max_new_tokens=200, greedy=True)`) AND a warm sample (`temperature=0.8, top_p=0.95`); write
    all samples to `RESULTS_DIR/"samples.md"` with the prompt, the settings, and an honest header
    note that samples are REPRESENTATIVE (not cherry-picked) per portfolio integrity. End with
    `if __name__ == "__main__": main()`. No argparse, no CLI flags (Phase-1 D-04). Do NOT use
    generate() for perplexity (PPL is teacher-forced forward CE, not sampling). No fenced code in
    this action; write the driver in the .py file.
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('scripts/evaluate.py').read())" && grep -c "perplexity(" scripts/evaluate.py && grep -c "generate_text_str" scripts/evaluate.py && grep -c "argparse" scripts/evaluate.py</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/evaluate.py` parses (ast.parse exits 0).
    - `grep -c "perplexity(" scripts/evaluate.py` >= 1 (EVAL-01 wired) AND `grep -c "generate_text_str" scripts/evaluate.py` >= 1 (EVAL-02 wired).
    - `grep -c "preflight_device(strict=True)" scripts/evaluate.py` >= 1 (GATE present) AND `grep -c "RuntimeConfig()" scripts/evaluate.py` >= 1 (SEPARATE device-carrying config — not the preflight dict).
    - `grep -c "argparse" scripts/evaluate.py` == 0 (thin no-argparse driver, Phase-1 D-04).
    - `grep -c "total_tokens\|total_token" scripts/evaluate.py` >= 1 (D-03 auditable denominator is printed).
    - `grep -c "weights_only=False" scripts/evaluate.py` >= 1 AND it loads only BEST_PATH (own trusted file — T-07-02).
    - `grep -c "results" scripts/evaluate.py` >= 1 (samples written to the tracked results/ path, not logs/).
    - MANUAL (M3, not CI): running `python scripts/evaluate.py` prints the headline PPL + token count and writes results/samples.md. Recorded in the SUMMARY.
  </acceptance_criteria>
  <done>evaluate.py loads the real best.pt, reports the deterministic full-val perplexity with its exact token count (EVAL-01), and emits representative greedy+warm qualitative samples to results/samples.md (EVAL-02). The long sweep + generation are a manual M3 artifact; the driver wiring is verified statically in CI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk → process (best.pt) | `best.pt` is the project's OWN trained checkpoint, loaded with `weights_only=False` (trusted file) |
| disk → process (tokenizer.json) | `tokenizer.json` is the project's OWN frozen JSON data-only artifact (no pickle, no code execution on load) |
| fixed prompt set → generate() | EVAL-02 prompts are an in-repo fixed constant, NOT user input |

Local, offline, no-network, no-untrusted-input measurement + generation. No auth, no sessions.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02 | Tampering | torch.load(best.pt, weights_only=False) | mitigate | Load `weights_only=False` ONLY for the project's own trusted best.pt — never a foreign checkpoint. This invariant is asserted in checkpoint.py's docstring and preserved here. |
| T-07-03 | Tampering | from_json(tokenizer.json) | accept | Tokenizer artifact is stdlib JSON data-only (validated schema_version + id-range on load, Phase 2 T-02-05/06) — executes no code on load. Own frozen artifact. |
| T-07-04 | Denial of Service | generate_text_str over fixed prompts | accept | `generate_text` enforces a `max_new_tokens` cap (text.py:67); prompts are a fixed in-repo set, not user input. No remote trigger. |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | Phase installs ZERO new packages (RESEARCH § Package Legitimacy Audit: N/A). No install task in this plan. |
</threat_model>

<verification>
- `pytest tests/test_ablation_config.py -x -q` green (the flags now exist).
- `make test` (full CPU suite) green — the additive flags introduce no regression in the model
  blast radius (test_gpt_weight_tying / test_gpt_param_count / test_gpt_init / test_checkpoint / test_best_ckpt).
- `scripts/evaluate.py` parses, wires perplexity() + generate_text_str(), gates with
  preflight_device(strict=True) THEN resolves runtime = RuntimeConfig(), prints total_tokens, no argparse.
- MANUAL (M3): `python scripts/evaluate.py` produces the headline PPL + token count + results/samples.md.
</verification>

<success_criteria>
- ModelConfig has `weight_tying`/`use_pos_emb` defaulting to True; defaults reproduce today's arch.
- GPT gates both seams on the flags; untie and no-pos behave as the verified param counts predict.
- The full existing model suite stays green.
- evaluate.py reports the deterministic headline perplexity WITH its auditable token count and emits
  representative curated samples to the tracked results/ path.
</success_criteria>

<output>
Create `.planning/phases/07-evaluation/07-02-SUMMARY.md` when done.
</output>
