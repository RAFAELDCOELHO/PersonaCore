---
phase: 03-bigram-baseline-training-harness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/bigram_corpus.txt
  - tests/test_bigram_model.py
  - tests/test_assemble_loss.py
  - tests/test_lr_schedule.py
  - tests/test_data_split.py
  - tests/test_train_loop.py
  - tests/test_overfit_batch.py
  - tests/test_resume_curve.py
autonomous: true
requirements: [MODEL-01, TRAIN-01, TRAIN-02, TRAIN-03, TRAIN-04, TRAIN-05, TRAIN-06]

must_haves:
  truths:
    - "A committed corpus fixture with >=2 eos-separated documents exists and encodes cleanly through the frozen tokenizer"
    - "Every Phase-3 seam has a RED test that names its requirement and fails for the right reason (missing module), not a typo"
    - "The AMP fp16 smoke test is GPU-conditional via inline skipif and never fails CI on a CPU box"
  artifacts:
    - path: "tests/fixtures/bigram_corpus.txt"
      provides: "D-06 committed fixture, >=2 docs separated by the <|endoftext|> literal"
      contains: "<|endoftext|>"
    - path: "tests/test_bigram_model.py"
      provides: "RED MODEL-01 forward-contract test"
    - path: "tests/test_assemble_loss.py"
      provides: "RED TRAIN-06 assemble_loss identity+additive test"
    - path: "tests/test_lr_schedule.py"
      provides: "RED TRAIN-01 warmup/cosine/state_dict test"
    - path: "tests/test_data_split.py"
      provides: "RED TRAIN-03 doc-split/no-leakage/get_batch test"
    - path: "tests/test_train_loop.py"
      provides: "RED TRAIN-01/02 grad-accum + AMP-ordering + GPU smoke test"
    - path: "tests/test_overfit_batch.py"
      provides: "RED TRAIN-05 overfit-one-batch gate"
    - path: "tests/test_resume_curve.py"
      provides: "RED TRAIN-04/06 resume-trajectory + curve-reproducibility test"
  key_links:
    - from: "tests/test_data_split.py"
      to: "artifacts/tokenizer.json"
      via: "from_json + CORPUS_PATH fixture"
      pattern: "from_json"
    - from: "tests/test_train_loop.py"
      to: "torch.cuda.is_available"
      via: "skipif guard on the fp16 smoke test"
      pattern: "skipif"
---

<objective>
Stand up the Wave-0 RED test scaffold and the committed corpus fixture that every later
slice in Phase 3 turns GREEN. This is the MVP "failing end-to-end test first" step: before
any harness code exists, write the executable contract for all seven seams (MODEL-01,
TRAIN-01..06) so each implementation plan has a precise, pre-written acceptance target.

Purpose: Lock the harness contract as tests before the disposable bigram or the loop exist,
so the seams fail loudly now (cheap, CPU) instead of during Phase-4 transformer bring-up.
Output: One committed fixture + seven RED test files that import not-yet-existing modules and
fail with ImportError/AttributeError (the right reason), with the GPU-only AMP smoke test
guarded so CI stays green on CPU.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-bigram-baseline-training-harness/03-CONTEXT.md
@.planning/phases/03-bigram-baseline-training-harness/03-RESEARCH.md
@.planning/phases/03-bigram-baseline-training-harness/03-PATTERNS.md
@.planning/phases/03-bigram-baseline-training-harness/03-VALIDATION.md
@tests/fixtures/tiny_corpus.txt
@tests/test_tokenizer_io.py
@tests/test_checkpoint.py
@src/personacore/config.py
@src/personacore/tokenizer/__init__.py

<interfaces>
<!-- Contracts these RED tests assert against. Extracted from existing code + locked decisions. -->
Frozen tokenizer surface (src/personacore/tokenizer/__init__.py):
  from personacore.tokenizer import from_json   # from_json("artifacts/tokenizer.json") -> BPETokenizer
  tok.encode(text, allowed_special="all") -> list[int]   # <|endoftext|> -> atomic eos_id 8184
  EOS_ID == 8184

Config surface (src/personacore/config.py):
  ModelConfig(vocab_size=8192, eos_id=8184, block_size=256, ...)
  TrainConfig(lr=3e-4, batch_size=64, max_steps=5000, warmup_steps=100,
              grad_clip=1.0, grad_accum_steps=1, weight_decay=0.1, seed=1337)
  RuntimeConfig(device, amp, amp_dtype).autocast()

Locked contracts the tests assert (do NOT implement here — just assert):
  personacore.model.BigramLanguageModel(vocab_size).forward(idx, targets=None) -> (logits, loss)   # D-02
  personacore.training.loss.assemble_loss(base_loss, extra_penalties=()) -> base + sum(extra)       # D-04
  personacore.training.schedule.build_lr_lambda(warmup_steps, max_steps, min_ratio=0.1)
  personacore.training.schedule.build_scheduler(optimizer, train_cfg) -> LambdaLR
  personacore.training.data.load_split(fixture_path, eos_id=8184, val_docs=1) -> (train_ids, val_ids)
  personacore.training.data.get_batch(arr, batch_size, block_size, device) -> (x, y)
  personacore.training.loop.train(...)  / sample(model, idx, max_new_tokens, temperature=1.0)

Resume-test analog: tests/test_checkpoint.py::test_resume_identical_trajectory (lines 43-80)
CSV-restart analog: tests/test_logging.py
GPU-sim conftest idiom: tests/conftest.py::simulate_pascal
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit the D-06 corpus fixture</name>
  <read_first>
    - tests/fixtures/tiny_corpus.txt (lexical style + UTF-8 plainness to mirror)
    - .planning/phases/03-bigram-baseline-training-harness/03-PATTERNS.md (the bigram_corpus.txt fixture section, ~line 412)
    - src/personacore/tokenizer/__init__.py (confirm EOS_TOKEN literal is "<|endoftext|>")
  </read_first>
  <files>tests/fixtures/bigram_corpus.txt</files>
  <action>
    Create a small TinyStories-style corpus of AT LEAST 2 documents separated by the literal
    string `<|endoftext|>` (D-06). Match the lexical register of tests/fixtures/tiny_corpus.txt
    (short simple sentences like "In the blue lake there lived a red owl.") so the FROZEN
    8192-vocab tokenizer encodes it cleanly. Put a document separator between docs and at the
    end of the last doc, so doc-level splitting yields >=1 whole document for val. Plain UTF-8,
    no BOM. The encoded array MUST contain id 8184 at every document boundary (Pitfall 6) — the
    test in Task 5 asserts this.
  </action>
  <verify>
    <automated>python3 -c "import pathlib; t=pathlib.Path('tests/fixtures/bigram_corpus.txt').read_text(encoding='utf-8'); assert t.count('<|endoftext|>') >= 2, t.count('<|endoftext|>')"</automated>
  </verify>
  <done>tests/fixtures/bigram_corpus.txt exists, is UTF-8, contains the <|endoftext|> literal at least twice (>=2 doc boundaries).</done>
</task>

<task type="auto">
  <name>Task 2: RED tests for the model+loss slice (MODEL-01, TRAIN-06)</name>
  <read_first>
    - tests/test_tokenizer_roundtrip.py (shape/contract assert style)
    - tests/test_config.py (pure-function behavior assert style)
    - .planning/phases/03-bigram-baseline-training-harness/03-PATTERNS.md (test_bigram_model.py + test_assemble_loss.py sections, ~lines 319-354)
    - .planning/phases/03-bigram-baseline-training-harness/03-RESEARCH.md (§Pattern 1, §Code Examples assemble_loss)
  </read_first>
  <files>tests/test_bigram_model.py, tests/test_assemble_loss.py</files>
  <action>
    Write `tests/test_bigram_model.py` (MODEL-01) asserting the LOCKED forward contract (D-02):
    `BigramLanguageModel(vocab_size)` from `personacore.model`; `forward(idx)` returns
    `(logits, None)` with `logits.shape == (B, T, vocab_size)`; `forward(idx, targets)` returns
    `(logits, loss)` where loss is a scalar tensor; and a CE-flatten sanity check (loss near
    `ln(vocab_size)` at random init, e.g. a uniform-prediction sanity bound). Use a tiny
    vocab_size (e.g. 32) and seeded `torch.randint` — CPU-only, no fixture.
    Write `tests/test_assemble_loss.py` (TRAIN-06 / D-04a) importing
    `personacore.training.loss.assemble_loss`: assert `assemble_loss(x, ())` equals `x`
    (identity) and `assemble_loss(x, (p,))` equals `x + p` for dummy scalar tensors. Both files
    MUST currently fail with ImportError (modules don't exist yet) — that is the RED state.
  </action>
  <verify>
    <automated>python3 -c "import ast; [ast.parse(open(f).read()) for f in ['tests/test_bigram_model.py','tests/test_assemble_loss.py']]" && ! python3 -c "import personacore.model" 2>/dev/null</automated>
  </verify>
  <done>Both files parse as valid Python, import the not-yet-existing modules, and assert the D-02/D-04a contracts. They fail RED for the right reason (missing module).</done>
</task>

<task type="auto">
  <name>Task 3: RED tests for the data+schedule slice (TRAIN-03, TRAIN-01)</name>
  <read_first>
    - tests/test_tokenizer_io.py (CORPUS_PATH constant + from_json + behavior asserts, lines 17-54)
    - tests/test_checkpoint.py (state_dict round-trip idiom + _build helper, lines 19-24)
    - .planning/phases/03-bigram-baseline-training-harness/03-PATTERNS.md (test_data_split.py + test_lr_schedule.py sections, ~lines 358-372)
    - .planning/phases/03-bigram-baseline-training-harness/03-RESEARCH.md (Pitfall 2, Pitfall 3, §Pattern 4)
  </read_first>
  <files>tests/test_data_split.py, tests/test_lr_schedule.py</files>
  <action>
    Write `tests/test_data_split.py` (TRAIN-03) with
    `CORPUS_PATH = pathlib.Path(__file__).parent / "fixtures" / "bigram_corpus.txt"` (mirror
    test_tokenizer_io.py:19). Import `load_split`/`get_batch` from `personacore.training.data`.
    Assert: `load_split` returns two disjoint arrays whose documents do NOT overlap (no leakage,
    Pitfall 3); id 8184 appears at document boundaries (Pitfall 6); `get_batch(arr, B, block, "cpu")`
    returns `x,y` of shape `(B, block)`, dtype `torch.int64`, with `y` shifted by one and all
    indices in-bounds (no overrun past `len(arr)-block-1`).
    Write `tests/test_lr_schedule.py` (TRAIN-01) importing `build_lr_lambda`/`build_scheduler`
    from `personacore.training.schedule`. Build a tiny `nn.Linear`+`AdamW` (like
    test_checkpoint._build) and assert: warmup ramps the multiplier from ~0 toward 1 over
    `warmup_steps`; after warmup the cosine decays toward `min_ratio`; LR after N optimizer steps
    matches the lambda at step N (NOT N*grad_accum — Pitfall 2); `scheduler.state_dict()` round-trips
    `last_epoch`. Both files RED (modules missing).
  </action>
  <verify>
    <automated>python3 -c "import ast; [ast.parse(open(f).read()) for f in ['tests/test_data_split.py','tests/test_lr_schedule.py']]" && ! python3 -c "import personacore.training.data" 2>/dev/null</automated>
  </verify>
  <done>Both files parse, reference CORPUS_PATH + the data/schedule contracts, and fail RED on missing modules.</done>
</task>

<task type="auto">
  <name>Task 4: RED tests for the loop slice (TRAIN-01/02/04/05/06)</name>
  <read_first>
    - tests/test_checkpoint.py::test_resume_identical_trajectory + _train_step (lines 27-80) — the PRIMARY resume analog
    - tests/test_logging.py (CSV restart / row-read helper)
    - tests/conftest.py (simulate_pascal device-monkeypatch idiom)
    - .planning/phases/03-bigram-baseline-training-harness/03-PATTERNS.md (test_train_loop.py / test_overfit_batch.py / test_resume_curve.py sections, ~lines 374-408)
    - .planning/phases/03-bigram-baseline-training-harness/03-RESEARCH.md (Pitfall 1, Pitfall 4, Pitfall 5, §Pattern 2)
  </read_first>
  <files>tests/test_train_loop.py, tests/test_overfit_batch.py, tests/test_resume_curve.py</files>
  <action>
    Write `tests/test_train_loop.py` (TRAIN-01/02) with: `test_amp_ordering` asserting the loop
    invokes `scaler.unscale_` BEFORE `clip_grad_norm_` BEFORE `scaler.step` BEFORE `scaler.update`,
    with exactly one `unscale_` per optimizer step (Pitfall 1) — record the op sequence by spying
    on a wrapped scaler/optimizer or a call-log, NOT a "loss decreased" check; a grad-accum
    equivalence test (N micro-batches ~= 1 big batch within tolerance); and
    `test_amp_fp16_smoke` guarded by inline `@pytest.mark.skipif(not torch.cuda.is_available())`
    (no `cuda` marker is registered in pyproject.toml — use inline skipif) running the real fp16
    path without inf/nan.
    Write `tests/test_overfit_batch.py` (TRAIN-05 / D-10): `seed_everything(seed)` for determinism
    (Pitfall 5); a DEDICATED small `TrainConfig` (warmup_steps=0, ~200-500 max_steps, higher lr
    ~1e-2..1e-1); reuse ONE fixed batch every step; assert final loss < a small threshold (well
    below `ln(8192)≈9.0`; the executor in Plan 04 pins the exact threshold per A2).
    Write `tests/test_resume_curve.py` (TRAIN-04/06) modeled on
    test_checkpoint.py::test_resume_identical_trajectory: a reference run vs a save->kill->resume
    run on `BigramLanguageModel` + real `build_scheduler`, asserting next-step loss AND a sampled
    param match within 1e-6; PLUS concatenate pre-kill + post-resume CSV rows and assert they
    equal an uninterrupted run's rows and the header appears exactly once (Pitfall 4). All RED.
  </action>
  <verify>
    <automated>python3 -c "import ast; [ast.parse(open(f).read()) for f in ['tests/test_train_loop.py','tests/test_overfit_batch.py','tests/test_resume_curve.py']]" && grep -q "skipif(not torch.cuda.is_available())" tests/test_train_loop.py</automated>
  </verify>
  <done>Three loop test files parse, encode the AMP-ordering / overfit / resume-curve contracts, the fp16 smoke test is skipif-guarded, and all fail RED on missing modules.</done>
</task>

<task type="auto">
  <name>Task 5: Confirm RED suite shape + fixture encodes through the frozen tokenizer</name>
  <read_first>
    - .planning/phases/03-bigram-baseline-training-harness/03-VALIDATION.md (Wave 0 Requirements checklist)
    - pyproject.toml ([tool.pytest.ini_options] — testpaths/pythonpath)
  </read_first>
  <files>tests/test_data_split.py</files>
  <action>
    Verify the whole RED scaffold is well-formed: run `pytest tests/test_bigram_model.py
    tests/test_assemble_loss.py tests/test_lr_schedule.py tests/test_data_split.py
    tests/test_train_loop.py tests/test_overfit_batch.py tests/test_resume_curve.py -q` and
    confirm collection errors are all "module not found"/import errors (RED for the right
    reason), NOT syntax errors or fixture-missing errors. Also confirm the committed fixture
    actually round-trips through the FROZEN tokenizer by encoding it via
    `from_json("artifacts/tokenizer.json").encode(text, allowed_special="all")` and asserting
    8184 appears >=2 times (the doc boundaries) — if this fails, fix the fixture (Task 1), never
    retrain the tokenizer (Pitfall 6). Do NOT add any production module to make tests pass — they
    stay RED until Plans 02-04.
  </action>
  <verify>
    <automated>python3 -c "from personacore.tokenizer import from_json; ids=from_json('artifacts/tokenizer.json').encode(open('tests/fixtures/bigram_corpus.txt',encoding='utf-8').read(), allowed_special='all'); assert ids.count(8184) >= 2, ids.count(8184)"</automated>
  </verify>
  <done>The seven test files collect with import-error-only failures (RED), and the fixture encodes to an id stream containing 8184 at >=2 document boundaries via the frozen tokenizer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed fixture -> tokenizer | tests/fixtures/bigram_corpus.txt is a trusted, committed, plain-UTF-8 file read by tests only |

Phase 3 is fully offline / on-device / zero-budget with no network, auth, storage, or
multi-user surface. This plan adds only a text fixture and pytest files; it introduces no new
dependency and no runtime data path.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | torch.load(weights_only=False) resume path exercised by tests | accept | Inherited & already documented TRUSTED-ONLY (own files); tests load only checkpoints they write to tmp_path. No change this plan. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages introduced this phase (RESEARCH §Package Legitimacy Audit — informational only). No install task exists. |
</threat_model>

<verification>
- `pytest tests/ -q` collects the seven new test files; all Phase-3 seam tests fail RED with
  import errors only (no syntax/fixture errors).
- The committed fixture encodes through the frozen tokenizer with 8184 at >=2 doc boundaries.
- Inherited Phase-1/2 suite remains green (this plan adds files, modifies no existing module).
</verification>

<success_criteria>
- tests/fixtures/bigram_corpus.txt committed with >=2 eos-separated docs that encode cleanly.
- Seven RED test files exist, parse, and encode the MODEL-01 / TRAIN-01..06 contracts exactly
  as VALIDATION.md specifies, with the fp16 smoke test skipif-guarded.
- No production harness module is created in this plan (tests stay RED until later waves).
</success_criteria>

<output>
Create `.planning/phases/03-bigram-baseline-training-harness/03-01-SUMMARY.md` when done.
</output>
