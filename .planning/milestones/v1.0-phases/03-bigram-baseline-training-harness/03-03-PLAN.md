---
phase: 03-bigram-baseline-training-harness
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/personacore/training/data.py
  - src/personacore/training/schedule.py
  - tests/test_data_split.py
  - tests/test_lr_schedule.py
autonomous: true
requirements: [TRAIN-03, TRAIN-01]

must_haves:
  truths:
    - "The committed fixture is encoded via the frozen tokenizer and split on document boundaries into disjoint train/val arrays with no leakage"
    - "get_batch draws random contiguous (x, y) windows that never overrun the array and never straddle the train/val boundary"
    - "Warmup+cosine LR is a resumable LambdaLR object whose state_dict() round-trips, satisfying the checkpoint.py scheduler contract"
  artifacts:
    - path: "src/personacore/training/data.py"
      provides: "load_split (doc-level, no leakage) + get_batch (nanoGPT window sampler)"
      contains: "def load_split"
    - path: "src/personacore/training/schedule.py"
      provides: "build_lr_lambda + build_scheduler -> LambdaLR (warmup+cosine)"
      contains: "LambdaLR"
  key_links:
    - from: "src/personacore/training/data.py"
      to: "artifacts/tokenizer.json"
      via: "from_json + encode(allowed_special='all')"
      pattern: "from_json"
    - from: "src/personacore/training/schedule.py"
      to: "checkpoint.py scheduler.state_dict() contract"
      via: "LambdaLR object (not a bare function)"
      pattern: "LambdaLR"
---

<objective>
Deliver the second vertical slice: the data path + LR schedule. After this plan a real user can
load the committed fixture into disjoint train/val token arrays, draw training batches, and
build a resumable warmup+cosine scheduler — the two inputs the loop (Plan 04) consumes.

Purpose: Implement the no-leakage doc-level split + nanoGPT `get_batch` (TRAIN-03) and the
hand-rolled warmup+cosine LR as a `LambdaLR` object (TRAIN-01 schedule half, D-08) that
satisfies the `scheduler.state_dict()` checkpoint contract (D-05). Turns the RED TRAIN-03 and
TRAIN-01(schedule) tests GREEN.
Output: `training/data.py`, `training/schedule.py`, and their two green tests.

This plan shares NO files with Plan 02 (model/loss) and runs in parallel within Wave 2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-bigram-baseline-training-harness/03-CONTEXT.md
@.planning/phases/03-bigram-baseline-training-harness/03-RESEARCH.md
@.planning/phases/03-bigram-baseline-training-harness/03-PATTERNS.md
@src/personacore/config.py
@src/personacore/checkpoint.py
@src/personacore/tokenizer/__init__.py
@scripts/train_tokenizer.py
@tests/fixtures/bigram_corpus.txt
@tests/test_data_split.py
@tests/test_lr_schedule.py

<interfaces>
<!-- Contracts to IMPLEMENT (RED tests from Plan 01 assert exactly these). -->
Frozen tokenizer (load, NEVER retrain — Pitfall 6):
  from personacore.tokenizer import from_json
  from_json("artifacts/tokenizer.json").encode(text, allowed_special="all") -> list[int]  # eos -> atomic 8184

D-06 data path:
  def load_split(fixture_path, eos_id=8184, val_docs=1) -> (train_ids, val_ids)  # np.uint16, doc-level split
  def get_batch(arr, batch_size, block_size, device) -> (x, y)                   # int64, random contiguous windows

D-08 LR schedule (MUST be a LambdaLR object — checkpoint.py:55 calls scheduler.state_dict()):
  def build_lr_lambda(warmup_steps: int, max_steps: int, min_ratio: float = 0.1) -> callable
  def build_scheduler(optimizer, train_cfg) -> LambdaLR

Config fields consumed: TrainConfig.warmup_steps (100), .max_steps (5000), .batch_size (64);
ModelConfig.block_size (256), .eos_id (8184).
checkpoint.py scheduler contract: save_checkpoint calls scheduler.state_dict(); load calls
scheduler.load_state_dict() — only `last_epoch` must restore (A1).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Doc-level split + get_batch data path (TRAIN-03)</name>
  <files>src/personacore/training/data.py</files>
  <read_first>
    - scripts/train_tokenizer.py (fixture read_text + frozen-tokenizer load pattern, lines 30-49)
    - tests/test_tokenizer_io.py (CORPUS_PATH + from_json usage)
    - .planning/phases/03-bigram-baseline-training-harness/03-PATTERNS.md (training/data.py section, lines 171-215)
    - .planning/phases/03-bigram-baseline-training-harness/03-RESEARCH.md (§Code Examples load_split + get_batch, lines 345-382; Pitfall 3, Pitfall 6)
    - tests/test_data_split.py (the RED no-leakage contract this turns GREEN)
  </read_first>
  <behavior>
    - load_split returns two np.uint16 arrays; the docs in train and val are disjoint (no leakage)
    - id 8184 appears at the document boundaries of the encoded stream
    - >=2 docs required (assert), val gets >=1 whole document (val_docs=1 default)
    - get_batch returns x,y of shape (batch_size, block_size), dtype torch.int64, y shifted +1
    - get_batch start indices are bounded to len(arr)-block_size-1 (no overrun)
  </behavior>
  <action>
    Create `src/personacore/training/data.py` with a module docstring naming D-06 / TRAIN-03 and
    the no-leakage property (bounded fixture ONLY — full memmap is Phase 5 / PRE-01). Implement
    `load_split(fixture_path, eos_id=8184, val_docs=1)`: load the FROZEN tokenizer via
    `from_json("artifacts/tokenizer.json")` (NEVER `.train()` — Pitfall 6); read the fixture
    UTF-8; `encode(text, allowed_special="all")` so `<|endoftext|>` maps atomically to 8184;
    split into per-document token lists on the `eos_id` boundary (keep eos as each doc's
    terminator); `assert len(docs) >= 2`; put the last `val_docs` whole documents in val and the
    rest in train (never straddle — Pitfall 3); return two `np.array(..., dtype=np.uint16)`
    arrays. Implement `get_batch(arr, batch_size, block_size, device)` (nanoGPT idiom): draw
    `ix = np.random.randint(0, len(arr) - block_size - 1, size=batch_size)`; stack
    `arr[i:i+block_size]` -> x and `arr[i+1:i+1+block_size]` -> y, casting `uint16`->`int64` for
    `nn.Embedding`/`cross_entropy`; move both to `device`. Storage stays uint16; index dtype int64.
  </action>
  <verify>
    <automated>pytest tests/test_data_split.py -x -q</automated>
  </verify>
  <done>test_data_split.py is GREEN: disjoint train/val docs (no leakage), 8184 at boundaries, get_batch shapes/dtype/bounds correct.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hand-rolled warmup+cosine LambdaLR (TRAIN-01 schedule)</name>
  <files>src/personacore/training/schedule.py</files>
  <read_first>
    - src/personacore/config.py (autocast() factory idiom + TrainConfig.warmup_steps/max_steps, lines 55-94)
    - src/personacore/checkpoint.py (the scheduler.state_dict()/load_state_dict contract, lines 55,87-88)
    - .planning/phases/03-bigram-baseline-training-harness/03-PATTERNS.md (training/schedule.py section, lines 133-167)
    - .planning/phases/03-bigram-baseline-training-harness/03-RESEARCH.md (§Pattern 4, lines 244-266; A1 caveat; Pitfall 2)
    - tests/test_lr_schedule.py (the RED warmup/cosine/state_dict contract this turns GREEN)
  </read_first>
  <behavior>
    - lr_lambda(step) is a multiplier on base lr: ramps 0->1 linearly over warmup_steps
    - after warmup, cosine decays 1 -> min_ratio across (max_steps - warmup_steps)
    - at/after max_steps the multiplier is min_ratio (floor)
    - LR after N optimizer steps matches the lambda at step N (NOT N*grad_accum — Pitfall 2)
    - build_scheduler returns a LambdaLR whose state_dict() round-trips last_epoch
  </behavior>
  <action>
    Create `src/personacore/training/schedule.py` with a module docstring naming D-08 / TRAIN-01
    and the load-bearing property (MUST be a `torch.optim.lr_scheduler` object because
    `checkpoint.py` calls `scheduler.state_dict()` — D-05; the lambda math is hand-written,
    LambdaLR only serializes the step counter — A1). Implement
    `build_lr_lambda(warmup_steps, max_steps, min_ratio=0.1)` returning a closure `lr_lambda(step)`:
    if `step < warmup_steps` return `(step + 1) / max(1, warmup_steps)`; if `step >= max_steps`
    return `min_ratio`; else `progress = (step - warmup_steps) / max(1, max_steps - warmup_steps)`,
    `cosine = 0.5 * (1 + math.cos(math.pi * progress))`, return
    `min_ratio + (1 - min_ratio) * cosine`. Implement `build_scheduler(optimizer, train_cfg)`
    returning `LambdaLR(optimizer, build_lr_lambda(train_cfg.warmup_steps, train_cfg.max_steps))`.
    The harness in Plan 04 MUST rebuild `build_scheduler(...)` identically before
    `load_checkpoint` so the `last_epoch` restore is meaningful (A1).
  </action>
  <verify>
    <automated>pytest tests/test_lr_schedule.py -x -q</automated>
  </verify>
  <done>test_lr_schedule.py is GREEN: warmup ramp, cosine decay to floor, per-optimizer-step LR matches the lambda, and state_dict() round-trips last_epoch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed fixture -> tokenizer | tests/fixtures/bigram_corpus.txt is a trusted committed file; data.py reads it and the frozen tokenizer artifact only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Tampering | tokenizer artifact load (from_json) | accept | artifacts/tokenizer.json is a committed, JSON-only (no pickle) Phase-2 artifact loaded read-only; from_json validates schema_version + id bounds (Phase-2 guarantee). No untrusted path. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages — torch + numpy only (pre-vetted). No install task (RESEARCH §Package Legitimacy Audit). |
</threat_model>

<verification>
- `pytest tests/test_data_split.py tests/test_lr_schedule.py -x -q` is GREEN.
- No train/val leakage: train and val documents are provably disjoint.
- Scheduler is a LambdaLR whose `state_dict()` round-trips (satisfies checkpoint.py contract).
- `make test` stays green.
</verification>

<success_criteria>
- Doc-level split on eos_id=8184 yields disjoint train/val arrays with no leakage (TRAIN-03).
- get_batch draws bounded, correctly-shifted, int64 windows (TRAIN-03).
- Warmup+cosine is a resumable LambdaLR object satisfying the scheduler.state_dict() contract
  (TRAIN-01 / D-05 / D-08).
- The frozen tokenizer is loaded, never retrained (Pitfall 6).
</success_criteria>

<output>
Create `.planning/phases/03-bigram-baseline-training-harness/03-03-SUMMARY.md` when done.
</output>
