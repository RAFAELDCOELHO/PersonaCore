---
phase: 06-generation-sampling
plan: 02
type: tdd
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/personacore/generation/core.py
  - src/personacore/generation/__init__.py
  - tests/test_generation.py
autonomous: true
requirements: [GEN-01, GEN-02, GEN-03]
must_haves:
  truths:
    - "One shared generator core yields each new token id one step at a time and stops on EOS without yielding it"
    - "collect() drains the core into a (1, prompt_len + n) LongTensor that tests and Phase-7 eval assert on"
    - "Generating past block_size never raises (context cropped to the last block_size ids each step)"
    - "Greedy decoding is bit-identical across two runs; seeded torch.Generator sampling is reproducible"
  artifacts:
    - path: "src/personacore/generation/core.py"
      provides: "generate(...) generator + collect(...) drain helper over the locked forward contract"
      exports: ["generate", "collect"]
      min_lines: 30
    - path: "src/personacore/generation/__init__.py"
      provides: "barrel now also re-exports generate, collect"
      contains: "from .core import"
  key_links:
    - from: "src/personacore/generation/core.py"
      to: "model.forward"
      via: "logits, _ = model(idx_cond) reading logits[:, -1, :]"
      pattern: "model\\(idx_cond\\)"
    - from: "src/personacore/generation/core.py"
      to: "personacore.generation.sampling.next_token"
      via: "delegates the logit->id decision"
      pattern: "next_token\\("
    - from: "src/personacore/generation/core.py"
      to: "model.config.block_size"
      via: "context crop idx[:, -bs:] before each forward"
      pattern: "block_size"
---

<objective>
Build the shared token-level generator core — the single decoding implementation that powers the
GEN-03 tests, Phase-7 evaluation, and (via the 06-03 wrapper) the Phase-8 demo. `core.py` provides
`generate(model, idx, ...)`, an `@torch.no_grad()` Python generator that yields each new token id one
step at a time, and `collect(model, idx, **kw)`, a drain helper that materializes the full
`(1, prompt_len + n)` LongTensor. This wave turns the five core-dependent GEN-02/GEN-03 tests GREEN
(EOS-stop, past-block_size crop, output shape, greedy determinism, seeded-sampling determinism).

Purpose: One decode path behind all three consumers (D-04), superseding training/loop.py::sample
(D-11) WITHOUT touching the training loop. EOS-stop-without-yield (D-05) simultaneously implements
GEN-02's "stop on EOS" and "trim the trailing token".

Output: `src/personacore/generation/core.py`, updated `generation/__init__.py` barrel, and the five
core tests un-skipped and passing in `tests/test_generation.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-generation-sampling/06-CONTEXT.md
@.planning/phases/06-generation-sampling/06-RESEARCH.md
@.planning/phases/06-generation-sampling/06-PATTERNS.md
@.planning/phases/06-generation-sampling/06-VALIDATION.md
@.planning/phases/06-generation-sampling/06-01-SUMMARY.md

<interfaces>
<!-- Locked contracts the executor needs. Use directly — no codebase exploration. -->

Model forward contract (src/personacore/model/gpt.py:188-204):
  def forward(self, idx, targets=None) -> (logits, loss); logits shape (B, T, vocab_size)
  - asserts T <= self.config.block_size (gpt.py:190) -> context crop is MANDATORY, not optional
  - self.config stored on the model -> model.config.block_size / model.config.eos_id

Sampling surface from 06-01 (src/personacore/generation/sampling.py):
  next_token(logits_last, *, temperature=1.0, top_k=None, top_p=None, greedy=False, generator=None)
    -> LongTensor shape (1,1); greedy=argmax (no RNG); else temp->top_k->top_p->softmax->multinomial(generator=g)

Superseded sampler to mirror (src/personacore/training/loop.py:105-119):
  @torch.no_grad() loop; logits, _ = model(idx); logits[:, -1, :]; torch.cat([idx, next_id], dim=1).
  core.py KEEPS the @torch.no_grad() decorator, the for _ in range(max_new_tokens) bound, the
  logits[:, -1, :] slice, and the torch.cat append; ADDS crop + yield + EOS-stop + next_token delegation.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: generate() generator core + collect() drain helper</name>
  <files>src/personacore/generation/core.py, src/personacore/generation/__init__.py</files>
  <behavior>
    - generate yields int(next_id) per step; stops after max_new_tokens OR the instant next_id == eos_id (returns BEFORE yielding/appending EOS — D-05).
    - Context crop: idx_cond = idx if idx.size(1) <= bs else idx[:, -bs:], where bs = block_size or model.config.block_size — generating past block_size never raises.
    - Greedy path (greedy=True) is deterministic; sampled path threads generator= into next_token.
    - eos_id defaults from model.config.eos_id when not passed; block_size defaults from model.config.block_size.
    - collect(model, idx, **kw) drains generate into the full (1, prompt_len + n) LongTensor (prompt + new ids concatenated — D-02), preserving idx.dtype and device.
  </behavior>
  <read_first>
    - .planning/phases/06-generation-sampling/06-RESEARCH.md (Pattern 1 nanoGPT core verbatim; Pattern 4 context crop; Pattern 5 generator+collect; Pitfall 1 crop crash; Pitfall 4 EOS leak; Pitfall 6 no_grad/eval)
    - .planning/phases/06-generation-sampling/06-PATTERNS.md (core.py section: what to keep identical from loop.py::sample, what to add)
    - src/personacore/training/loop.py (lines 105-119: the @torch.no_grad() sample idiom being superseded)
    - src/personacore/model/gpt.py (lines 188-204: forward contract + the assert T <= block_size that forces the crop)
    - src/personacore/generation/sampling.py (next_token signature from 06-01)
    - src/personacore/generation/__init__.py (current barrel from 06-01 — extend it)
  </read_first>
  <action>
    Create src/personacore/generation/core.py with a module docstring tagging GEN-01/GEN-02 and noting
    it supersedes training/loop.py::sample (D-11) without rewiring the loop.

    generate(model, idx, *, max_new_tokens, eos_id=None, temperature=1.0, top_k=None, top_p=None,
    greedy=False, generator=None, block_size=None) decorated with @torch.no_grad(). Resolve bs =
    block_size if block_size is not None else model.config.block_size, and eid = eos_id if eos_id is
    not None else model.config.eos_id (read from config — NEVER hardcode 256/8184). Loop
    for _ in range(max_new_tokens): idx_cond = idx if idx.size(1) <= bs else idx[:, -bs:];
    logits, _ = model(idx_cond); next_id = next_token(logits[:, -1, :], temperature=temperature,
    top_k=top_k, top_p=top_p, greedy=greedy, generator=generator); tok = int(next_id); if tok == eid:
    return (D-05 — stop, do NOT yield/append EOS); idx = torch.cat([idx, next_id], dim=1); yield tok.
    Single-sequence only (no batch — scope fence); assume idx is (1, T) LongTensor.

    collect(model, idx, **kw): out = idx; for tok in generate(model, idx, **kw): out =
    torch.cat([out, torch.tensor([[tok]], dtype=out.dtype, device=out.device)], dim=1); return out.
    Returns a (1, prompt_len + n) LongTensor on idx's device (D-02 — full sequence, prompt + new).

    Extend src/personacore/generation/__init__.py: add `from .core import collect, generate` and add
    "generate", "collect" to __all__ (keep the sampling exports from 06-01).

    Do NOT add a generate/sample method to GPT (anti-pattern — the class deliberately ships none,
    preserving the LoRA/EWC seam). Do NOT touch training/loop.py or its barrel.
  </action>
  <verify>
    <automated>.venv/bin/python -c "from personacore.generation import generate, collect"</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -c "from personacore.generation import generate, collect"` succeeds
    - grep confirms `@torch.no_grad()` decorates generate in core.py
    - grep confirms `idx[:, -bs:]` (or equivalent last-block_size crop) and `model.config.block_size` appear in core.py; NO literal `256` or `8184`
    - grep confirms generate returns BEFORE yielding when tok == eid (EOS not yielded — D-05)
    - grep confirms core.py calls `next_token(` (delegates logit->id) and does NOT add a method to GPT
  </acceptance_criteria>
  <done>generate is an @torch.no_grad() generator yielding new token ids, stopping on EOS without yielding it and after max_new_tokens; collect drains it into the full (1, prompt_len+n) LongTensor; both read block_size/eos_id from model.config; the barrel re-exports them; training/loop.py untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Un-skip and green the five core GEN-02/GEN-03 tests</name>
  <files>tests/test_generation.py</files>
  <read_first>
    - tests/test_generation.py (the RED scaffold from 06-01 — the five @pytest.mark.skip core tests)
    - .planning/phases/06-generation-sampling/06-VALIDATION.md (determinism idioms: greedy no-Generator; sampled two-Generator; EOS-stop forced-argmax)
    - .planning/phases/06-generation-sampling/06-RESEARCH.md (Validation Architecture: determinism test idioms; Pitfall 2 global-RNG flakiness)
    - tests/test_train_loop.py (lines 1-60: monkeypatch pattern for forcing controlled logits)
    - src/personacore/generation/core.py (generate/collect signatures from Task 1)
  </read_first>
  <action>
    Remove the @pytest.mark.skip markers from the five core-dependent tests and finalize their bodies
    against the now-existing generate/collect.

    test_output_shape: collect(model, idx, max_new_tokens=n, greedy=True) on a prompt of length p with
    no EOS fired returns a (1, p + n) LongTensor (dtype torch.long). Use a fixture/seed where EOS does
    not appear, or a small n.

    test_greedy_deterministic: under torch.manual_seed(s) construct the tiny model; call greedy
    collect() twice; assert torch.equal of the two tensors (argmax has no RNG so they must match).

    test_seeded_sampling_deterministic: g1 = torch.Generator().manual_seed(0);
    g2 = torch.Generator().manual_seed(0); collect(..., generator=g1) and collect(..., generator=g2)
    with identical sampling kwargs (e.g. top_k or top_p set, greedy=False); assert equal. Demonstrates
    seed isolation — do NOT use global torch.manual_seed for the sampled path (Pitfall 2).

    test_eos_stop: force eos_id to be the argmax at a known step — monkeypatch model.forward (or use a
    stub model) to return logits whose argmax == model.config.eos_id at some step; call collect with
    greedy=True; assert the collected sequence's LAST id != eos_id, and its length is strictly less
    than prompt_len + max_new_tokens (EOS fired early and was trimmed — D-05).

    test_past_block_size_no_crash: with the tiny model (block_size=8), collect a sequence whose total
    length exceeds block_size (e.g. prompt_len + max_new_tokens > 8) and assert no exception is raised
    (context crop works — GEN-02 success criterion 2). Wrap in a no-raise assertion.

    All tests CPU-only, GPU/MPS-free, on the tiny fixture — never best.pt or the real tokenizer.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_generation.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_generation.py -x -q` passes all 8 tests (0 skipped)
    - `.venv/bin/python -m pytest tests/test_generation.py::test_eos_stop tests/test_generation.py::test_past_block_size_no_crash -x` passes
    - `.venv/bin/python -m pytest tests/test_generation.py::test_seeded_sampling_deterministic -x` passes (uses two torch.Generator instances, not global seed)
    - grep confirms NO `@pytest.mark.skip` remains in tests/test_generation.py
    - grep confirms NO reference to `best.pt` in the file
  </acceptance_criteria>
  <done>All eight GEN-01/02/03 tests pass on CPU with zero skips; sampled determinism uses isolated torch.Generator instances; EOS-stop asserts the last id is never eos_id and the sequence is trimmed; the past-block_size test proves cropping prevents a crash.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller -> generate(max_new_tokens) | The only externally-influenced value is the integer loop bound; a trusted local in-process model is the other input. No network, no untrusted bytes, no deserialization in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02 | Denial of Service | generate() loop | mitigate | The loop is `for _ in range(max_new_tokens)` so it always terminates; max_new_tokens is a required keyword bound. The 06-03 text wrapper (caller-facing) is where an absurd-value cap is enforced; the core itself cannot loop unboundedly. |
| T-06-03 | Tampering | model loading | accept | This plan never loads a checkpoint — tests construct a tiny in-memory GPT. best.pt's weights_only=False trust assumption is unchanged and out of scope (slim weights_only=True load is Phase-8 DEMO-02). |

No high-severity threats. Surface is a local pure-compute decode loop over a trusted in-process model; no network, no auth, no DB, no untrusted input, no new serialization. No package installs — Package Legitimacy Gate not applicable.
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_generation.py -x -q` passes all 8 tests, 0 skipped.
- `make test` green (no regression to the existing suite; training/loop.py untouched).
- grep confirms no method was added to GPT and training/loop.py is unchanged.
</verification>

<success_criteria>
- core.py provides an @torch.no_grad() generate() generator (yields new ids, stops on EOS without yielding it, crops context, delegates to next_token) and a collect() drain returning the full (1, prompt_len+n) LongTensor.
- All five core GEN-02/GEN-03 tests pass; the full tests/test_generation.py is green with 0 skips.
- block_size/eos_id read from model.config (no magic literals); training loop and GPT class untouched (D-11).
</success_criteria>

<output>
Create `.planning/phases/06-generation-sampling/06-02-SUMMARY.md` when done.
</output>
