---
phase: 08-demo-writeup
plan: 08
type: execute
wave: 2
depends_on: ["08-07"]
files_modified:
  - README.md
  - docs/REPORT.md
  - pyproject.toml
autonomous: true
gap_closure: true
requirements: [DOC-01]
must_haves:
  truths:
    - "Wherever README/REPORT presented 'vocabulary 8192', the effective vocabulary is stated honestly: 547 live ids (256 bytes + 283 learned merges + 8 specials) of the 8192-row table; 7645 rows are dead/reserved (WR-01)"
    - "The parameter-count discussion acknowledges that 2,935,680 of the headline 13,891,584 parameters are embedding rows for ids that can never occur in training data or be decoded"
    - "The REPORT's demo section documents the CR-01 mitigation shipped in 08-07: undecodable ids are masked from the logits before sampling, so every in-UI slider setting is safe"
    - "The README quickstart works verbatim from an empty directory — git clone + cd precede the venv/install steps (WR-03)"
    - "pip install -e '.[cpu,notebook]' provides matplotlib, so the notebook's curve cells run from the extra that names them (WR-04)"
  artifacts:
    - path: "README.md"
      provides: "Honest effective-vocabulary claim + clone-first quickstart"
      contains: "547"
    - path: "docs/REPORT.md"
      provides: "Effective-vocabulary honesty in overview/tokenizer-decision/results + dead-row parameter sentence + CR-01 mitigation note"
      contains: "2,935,680"
    - path: "pyproject.toml"
      provides: "notebook extra that can actually run the notebook"
      contains: "matplotlib"
  key_links:
    - from: "README.md"
      to: "github.com/RAFAELDCOELHO/PersonaCore"
      via: "git clone step 0 in the quickstart"
      pattern: "git clone"
    - from: "docs/REPORT.md"
      to: "scripts/demo_app.py (08-07 fix)"
      via: "demo decision section documents the dead-id logits mask"
      pattern: "undecodable"
    - from: "pyproject.toml"
      to: "demo.ipynb matplotlib imports"
      via: "matplotlib pin in the notebook extra"
      pattern: "matplotlib~=3.10"
---

<objective>
Close the writeup half of the verification gap: WR-01 is folded into the gap's `missing` list
("state the effective vocabulary — 547 live ids of the 8192 table — wherever README/REPORT
claim 'vocabulary 8192'; same root cause as CR-01, honesty bar for a rigor-branded writeup"),
plus the two remaining cheap warnings the verifier said to fix alongside: WR-03 (README
quickstart lacks a clone step) and WR-04 (`notebook` extra lacks matplotlib).

The facts (single-sourced from 08-REVIEW, confirmed by the BPE trainer's own warning "corpus
exhausted: learned 283 of 7928 requested merges; vocab_size=8192 has 7645 dead ids"):
- Live ids: 547 = 256 byte ids + 283 learned merge ids (ids 0-538) + 8 specials (8184-8191)
- Dead ids: 7645 reserved rows — the bounded TinyStories corpus exhausted its mergeable pairs
- Dead embedding parameters: 7645 x 384 = 2,935,680 (~21% of the headline 13,891,584)

Runs in wave 2 AFTER 08-07 so the REPORT's new mitigation sentence documents shipped,
test-pinned behavior — the writeup never describes code that does not exist yet.

Purpose: DOC-01 — the rigor-branded writeup must not overclaim; the quickstart must work verbatim.
Output: honest README/REPORT vocabulary framing, clone-first quickstart, runnable notebook extra.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-demo-writeup/08-VERIFICATION.md
@.planning/phases/08-demo-writeup/08-REVIEW.md

<interfaces>
<!-- Exact defect sites (from 08-REVIEW WR-01/WR-03/WR-04 + IN-01, line numbers verified). -->

README.md:
  - Line 29-31: "**Byte-level BPE tokenizer** trained from scratch — vocabulary 8192, ..."
    (the WR-01 claim; the rest of the bullet — endoftext pinning, tiktoken oracle — stays)
  - Lines 48-63: the "Run the demo" fenced bash block starting at "# 1. Environment
    (Python 3.11)" / "python3.11 -m venv .venv" — no clone precedes it (WR-03)
  - Line 57 already cites the canonical repo URL:
    https://github.com/RAFAELDCOELHO/PersonaCore/releases/tag/m1-demo-v1

docs/REPORT.md (440 lines, 20 ## sections):
  - Line 35-36: overview bullet "vocabulary 8192, document separator <|endoftext|> pinned at
    id 8184"
  - Lines 47-67: section "## Decision: Byte-Level BPE from Scratch, Vocabulary Locked Before
    Model Sizing" — Choice/Rationale/Evidence structure; never states the merge exhaustion
  - Line 131: "At 384-dim embeddings and an 8192 vocabulary, an untied head costs an extra
    3,145,728 parameters" (table-shape fact — number is correct; phrasing is the only issue)
  - Lines 334-353: section "## Decision: An Offline Story-Completion Demo, Not a Fake
    Chatbot" — where the CR-01 mitigation sentence belongs
  - Lines 355-362: "## Results" — "**Model.** 13,891,584 parameters ... vocabulary 8192,
    dropout 0.0, ..." (the third WR-01 site; also where the dead-row parameter sentence goes)
  - Line 396: "the suite is CPU-only and green — 126 passed, 1 skipped" (IN-01: stale; 08-07
    adds 7 more tests, so pin NO number — use unpinned phrasing per the review's own fix)

pyproject.toml lines 15-19 (current extras):
  cpu = ["torch==2.7.*"]
  demo = ["gradio>=5,<6", "matplotlib~=3.10"]
  notebook = ["ipykernel~=7.3", "nbconvert~=7.17"]
  dev = ["pytest~=9.0", "ruff~=0.15", "tiktoken~=0.13"]
  WR-04 target: notebook = ["ipykernel~=7.3", "nbconvert~=7.17", "matplotlib~=3.10"]
  (matplotlib~=3.10 duplicates the pin already audited and installed via the demo extra —
  no new package, no install task, no legitimacy gate triggered)

Review's suggested WR-01 phrasing (adapt, keep the numbers exact):
  "vocab table 8192 (547 ids live: 256 bytes + 283 learned merges + 8 specials; the bounded
  training corpus exhausted its mergeable pairs — the remaining rows are reserved capacity)"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: WR-01 — state the effective vocabulary everywhere 8192 is claimed; acknowledge the dead embedding rows; document the CR-01 mask</name>
  <files>README.md, docs/REPORT.md</files>
  <read_first>
    - README.md (full file — 105 lines; the line-29 bullet and the prose register to preserve)
    - docs/REPORT.md lines 25-70, 120-145, 300-370, 385-405 (the four WR-01 sites, the weight-tying parameter discussion, the demo decision section, the Results section, the Reproducibility test-count line)
    - scripts/demo_app.py and src/personacore/generation/text.py AFTER the 08-07 merge (confirm the shipped mask mechanism — undecodable_ids_mask + forbid_ids — before describing it in prose)
  </read_first>
  <action>
    Apply the honesty fixes in the writeup's existing decision-driven, denominator-citing
    register. Numbers are LOCKED (do not recompute or round differently): 547 live ids =
    256 bytes + 283 learned merges + 8 specials; 7645 dead ids; trainer warning "learned 283
    of 7928 requested merges"; dead embedding parameters 2,935,680 = 7645 x 384 (~21% of
    13,891,584).

    README.md:
    1. Rewrite the line-29 bullet's vocabulary claim using the review's phrasing: vocab table
       8192 with 547 ids live (256 bytes + 283 learned merges + 8 specials) — the bounded
       TinyStories corpus exhausts its mergeable pairs and the remaining 7645 rows are
       reserved capacity. Keep the bullet's `<|endoftext|>` pinning and tiktoken-oracle
       clauses intact.

    docs/REPORT.md:
    2. Overview bullet (line 35-36): apply the same vocab-table-8192/547-live qualification;
       keep "document separator `<|endoftext|>` pinned at id 8184".
    3. "Decision: Byte-Level BPE from Scratch..." section (lines 47-67): add a short honest
       paragraph (Choice/Rationale register) stating that training learned 283 of the 7928
       requested merges before the corpus exhausted its mergeable pairs — the trainer itself
       warns "vocab_size=8192 has 7645 dead ids" — so the EFFECTIVE vocabulary is 547 live
       ids; the locked 8192 table is reserved capacity, and the trade-off is stated plainly:
       shape stability for every downstream checkpoint versus dead rows the model carries.
    4. Line 131: change "an 8192 vocabulary" to "an 8192-row vocab table" (the 3,145,728
       number is the table shape and stays exactly as is).
    5. Results "Model." paragraph (lines 357-359): change "vocabulary 8192" to "vocab table
       8192 (547 ids live)" and append ONE sentence acknowledging the dead rows: 2,935,680 of
       the 13,891,584 parameters (7645 dead rows x 384 dims, ~21%) are embedding rows for ids
       that can never occur in the training data or be decoded — counted in the headline
       because they are part of the shipped tensor.
    6. Demo decision section (lines 334-353): add ONE sentence documenting the shipped CR-01
       mitigation — the demo masks the 7645 tokenizer-undecodable ids out of the logits
       before sampling (an optional forbid-ids mask through the from-scratch sampling path),
       so every slider setting the UI offers — temperature 1.5 with top-k disabled included —
       can only produce decodable ids. Match what 08-07 actually shipped (read the code
       first); cite the regression test file if the section's Evidence paragraph is the
       natural home.
    7. Reproducibility section (line 396, IN-01): replace the stale pinned count "126 passed,
       1 skipped" with unpinned phrasing — the suite is CPU-only and green, with the only
       skip a CUDA-only fp16 smoke test skipped by design off-GPU. Pin NO number (08-07
       changes it again).

    Do NOT touch: the ablation cohort caveat (verbatim-locked), the PPL-with-denominator
    citations, the headline 2.1066 / 12,636,922 / 13,891,584 numbers, the M2 framing, the
    GIF hero, or ln(8192) at line 365 (mathematically about the table size at init — true
    as written).
  </action>
  <verify>
    <automated>test "$(grep -c '547' README.md)" -ge 1 && test "$(grep -c '547' docs/REPORT.md)" -ge 3 && grep -q '2,935,680' docs/REPORT.md && ! grep -qiE 'vocabulary 8192|an 8192 vocabulary' README.md docs/REPORT.md && ! grep -q '126 passed' docs/REPORT.md && grep -qi 'undecodable' docs/REPORT.md && grep -q '12,636,922' README.md && grep -q '12,636,922' docs/REPORT.md</automated>
  </verify>
  <done>Every former "vocabulary 8192" claim in README/REPORT now states 547 live ids of the 8192 table; the Results section quantifies the 2,935,680 dead-row parameters; the demo section documents the shipped logits mask; the stale test count is unpinned; all locked rigor signals (PPL denominator, ablation caveat, M2 honesty) survive untouched</done>
  <acceptance_criteria>
    - `grep -c "547" README.md` outputs >= 1 and `grep -c "547" docs/REPORT.md` outputs >= 3 (overview bullet, tokenizer decision section, Results)
    - `grep -q "2,935,680" docs/REPORT.md` succeeds
    - `grep -qiE "vocabulary 8192|an 8192 vocabulary" README.md docs/REPORT.md` FAILS (no unqualified claim remains; `vocab_size=8192` the config identifier and `ln(8192)` are allowed and unmatched by this pattern)
    - `grep -q "126 passed" docs/REPORT.md` FAILS (IN-01 unpinned)
    - `grep -qi "undecodable" docs/REPORT.md` succeeds (CR-01 mitigation documented)
    - `grep -q "NOT to the headline" docs/REPORT.md` still succeeds and `grep -c "2.1066" README.md docs/REPORT.md` is unchanged from pre-edit (locked rigor signals intact)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: WR-03 + WR-04 — clone-first quickstart and a notebook extra that can run the notebook</name>
  <files>README.md, pyproject.toml</files>
  <read_first>
    - README.md lines 43-68 (the "Run the demo" fenced bash block to renumber)
    - pyproject.toml (lines 15-19 — the extras table and its compatible-release pin style)
  </read_first>
  <action>
    WR-03 — README quickstart fails verbatim from an empty directory (the editable install of
    `.` and `gh release download` both require being inside a checkout):
    1. In the "Run the demo" bash block, add a clone step BEFORE the venv lines:
       a "# 1. Get the code" comment, then
       `git clone https://github.com/RAFAELDCOELHO/PersonaCore.git` and `cd PersonaCore`
       (exact URL — line 57 of the README already cites this repo path for the release).
       Renumber the existing step comments (`# 1. Environment` -> `# 2. Environment`,
       `# 2. Weights` -> `# 3. Weights`, `# 3. Launch` -> `# 4. Launch`). Change nothing else
       in the block — the install line, the gh release line, and the launch line are verified
       working from a clean clone.

    WR-04 — `demo.ipynb` imports matplotlib in two cells but the `notebook` extra ships only
    ipykernel + nbconvert, so `.[cpu,notebook]` fails at the training-curves cell:
    2. In pyproject.toml, change the notebook extra to exactly
       `notebook = ["ipykernel~=7.3", "nbconvert~=7.17", "matplotlib~=3.10"]` — the same
       compatible-release pin matplotlib already carries in the demo extra (line 17). Leave
       the demo extra unchanged (its matplotlib stays; the duplication is intentional so each
       extra is self-sufficient). No other pyproject changes. This adds no new package to the
       environment — matplotlib is already installed and audited via the demo extra — so no
       package-legitimacy gate applies.
  </action>
  <verify>
    <automated>grep -q 'git clone https://github.com/RAFAELDCOELHO/PersonaCore.git' README.md && .venv/bin/python -c "
import re, tomllib
clone = next(i for i, l in enumerate(open('README.md')) if 'git clone' in l)
venv = next(i for i, l in enumerate(open('README.md')) if 'python3.11 -m venv' in l)
assert clone < venv, (clone, venv)
nb = tomllib.load(open('pyproject.toml','rb'))['project']['optional-dependencies']['notebook']
assert any(p.startswith('matplotlib') for p in nb), nb
assert 'matplotlib~=3.10' in nb, nb
print('OK: clone precedes venv; notebook extra =', nb)
"</automated>
  </verify>
  <done>A newcomer pasting the README quickstart from an empty directory reaches the running demo (clone -> venv -> install -> weights -> launch); the notebook extra lists matplotlib~=3.10 so .[cpu,notebook] can execute demo.ipynb end-to-end</done>
  <acceptance_criteria>
    - `grep -n "git clone https://github.com/RAFAELDCOELHO/PersonaCore.git" README.md` finds the line inside the quickstart bash block, on an earlier line than `python3.11 -m venv .venv`, followed by a `cd PersonaCore` line
    - The tomllib check prints the notebook extra containing exactly the pin `matplotlib~=3.10` alongside `ipykernel~=7.3` and `nbconvert~=7.17`
    - `grep -c "matplotlib~=3.10" pyproject.toml` outputs 2 (demo extra unchanged + notebook extra added)
    - `pip install -e ".[cpu,demo]"` install line in README is byte-identical to before (only the step-number comments and the new clone step changed in the block)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reader → published claims | Portfolio reviewers act on README/REPORT claims; overclaiming is an integrity failure for a rigor-branded artifact |
| pip → pyproject extras | Extras resolve packages from PyPI at install time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08H-01 | Repudiation | README/REPORT vocabulary claims | mitigate | WR-01: "vocabulary 8192" overstates the shipped artifact (547 live ids); corrected with exact numbers + dead-row parameter accounting so every claim traces to the trainer's own warning |
| T-08H-02 | Tampering | pyproject `notebook` extra | accept | `matplotlib~=3.10` duplicates a pin already audited and installed via the `demo` extra — no new package, no install task in this plan, so the package-legitimacy gate is not triggered |
| T-08H-03 | Spoofing | README clone URL | mitigate | Exact canonical URL (RAFAELDCOELHO/PersonaCore) copied from the release link the README already carries — no typosquat surface introduced |
</threat_model>

<verification>
- Task 1 + Task 2 grep/tomllib gates all pass (see per-task automated verify)
- `.venv/bin/ruff check . && .venv/bin/ruff format --check .` — clean (markdown/toml edits must not break the tree)
- Locked rigor signals survive: `grep -q "NOT to the headline" docs/REPORT.md`, 2.1066 always adjacent to its 12,636,922 denominator in both files, M2 still labeled upcoming
</verification>

<success_criteria>
- The verification gap's `missing` item 3 (WR-01 fold-in) is closed: the effective vocabulary (547 live ids of the 8192 table) is stated wherever the writeup claimed "vocabulary 8192", with the dead embedding rows quantified
- WR-03 and WR-04 closed: quickstart works verbatim from an empty directory; `.[cpu,notebook]` can run the notebook
- No locked rigor signal regressed; no new overclaim introduced (the REPORT documents only the mask 08-07 actually shipped)
</success_criteria>

<output>
Create `.planning/phases/08-demo-writeup/08-08-SUMMARY.md` when done
</output>
