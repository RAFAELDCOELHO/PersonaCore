---
phase: 08-demo-writeup
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/personacore/generation/text.py
  - src/personacore/generation/__init__.py
  - tests/test_demo_callback.py
  - scripts/demo_app.py
autonomous: false
requirements: [DEMO-01]
must_haves:
  truths:
    - "python scripts/demo_app.py launches a Gradio chat UI on localhost:7860 that streams a TinyStories completion token-by-token on laptop CPU"
    - "The UI works with Wi-Fi off — zero outbound network calls (no analytics, no version ping, no CDN fonts)"
    - "Temperature, top-k, and max-new-tokens sliders change generation behavior; each message starts a fresh story (history never concatenated)"
    - "The chat bubble GROWS as tokens arrive (cumulative yields) — it never flickers lone fragments"
    - "The demo loads checkpoints/model_slim.pt via the weights_only=True path and pins CPU through RuntimeConfig — no ad-hoc device strings"
  artifacts:
    - path: "scripts/demo_app.py"
      provides: "Offline Gradio 5.50.0 story-completion demo launcher (DEMO-01)"
      contains: "share=False"
      min_lines: 50
    - path: "src/personacore/generation/text.py"
      provides: "generate_text_cumulative — Gradio-shaped (cumulative) adapter over the delta-streaming generate_text"
      exports: ["generate_text_cumulative"]
    - path: "tests/test_demo_callback.py"
      provides: "CPU-only, gradio-free unit tests for the cumulative-yield contract"
      contains: "def test_yields_are_cumulative"
  key_links:
    - from: "scripts/demo_app.py"
      to: "src/personacore/generation/text.py"
      via: "generate_text_cumulative consumed by the ChatInterface callback"
      pattern: "generate_text_cumulative"
    - from: "scripts/demo_app.py"
      to: "src/personacore/checkpoint.py"
      via: "load_slim(SLIM_PATH) — the weights_only=True choke point"
      pattern: "load_slim"
    - from: "scripts/demo_app.py"
      to: "gradio analytics kill-switch"
      via: "env var set BEFORE import gradio"
      pattern: "GRADIO_ANALYTICS_ENABLED"
---

<objective>
Ship the offline Gradio chat demo (DEMO-01): `scripts/demo_app.py` runs the 13.9M-param model
on laptop CPU at ~100 tok/s, fully offline, framed honestly as TinyStories STORY COMPLETION
(not a tuned chatbot — D-02 no-overclaim bar). The streaming adapter
(`generate_text_cumulative`) lives in the package and is unit-tested gradio-free so CI covers
the testable slice; the script is pure wiring per the thin-script convention.

Purpose: DEMO-01 — the live proof the from-scratch model runs on-device.
Output: `generate_text_cumulative` (tested), `scripts/demo_app.py`, and a human-verified
offline streaming session.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-demo-writeup/08-RESEARCH.md
@.planning/phases/08-demo-writeup/08-PATTERNS.md
@.planning/phases/08-demo-writeup/08-UI-SPEC.md

<interfaces>
<!-- Contracts the executor needs. Use directly — no codebase exploration required. -->

From src/personacore/generation/text.py (lines 28, 39-93 — the producer this plan adapts):
  generate_text(model, tokenizer, prompt, *, max_new_tokens, **gen_kw) -> Iterator[str]
  - max_new_tokens is KEYWORD-ONLY and REQUIRED, bounded to (0, 4096]
    (DEFAULT_MAX_NEW_TOKENS_CAP = 4096); out-of-range raises ValueError BEFORE the loop.
  - Yields string DELTAS (new suffixes); prompt already stripped; EOS never shown.
  - gen_kw threads: temperature, top_k, top_p, greedy, generator.
  - Calls model.eval() internally — no extra no_grad plumbing needed.
  generate_text_str(...) is the non-streaming collect variant (same kwargs).

From src/personacore/checkpoint.py (added by 08-01):
  load_slim(path, *, map_location="cpu") -> dict   # weights_only=True + schema assert
  Slim dict keys: schema_version, model, model_config, git_sha, step, val_loss

From scripts/evaluate.py (the wiring analog, lines 34-37, 46-50, 77-86, 114):
  - Env var set BEFORE the import it affects, late imports carry `# noqa: E402`
  - _REPO_ROOT = pathlib.Path(__file__).resolve().parent.parent
  - model_cfg = ModelConfig(**blob["model_config"]); model = GPT(model_cfg);
    model.load_state_dict(blob["model"]); model.eval()
  - tok = from_json(TOKENIZER_PATH)   # FROZEN artifact — never retrain

From tests/test_best_ckpt.py:34 (the CPU pin precedent — DEMO-01 says laptop CPU, so do NOT
let preflight resolve to MPS):
  CPU_RUNTIME = RuntimeConfig(device="cpu")

Gradio 5.50.0 facts (08-RESEARCH, verified from the wheel):
  - ChatInterface fn is a generator; each yield is the FULL cumulative response (replaces
    the displayed message). type="messages" (tuples is deprecated).
  - additional_inputs values are passed to fn positionally AFTER (message, history).
  - os.environ["GRADIO_ANALYTICS_ENABLED"] = "False" BEFORE import gradio kills telemetry
    AND the version-check HTTP ping; also pass analytics_enabled=False (belt and braces).
  - share=False (default) -> localhost 127.0.0.1:7860, no tunnel binary download.
  - Default theme uses wheel-bundled LocalFont woff2 — NO CDN. Do not pass theme=/css=.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: generate_text_cumulative adapter (package) + gradio-free unit tests</name>
  <files>src/personacore/generation/text.py, src/personacore/generation/__init__.py, tests/test_demo_callback.py</files>
  <read_first>
    - src/personacore/generation/text.py (the file being modified — generate_text's exact signature, the 4096 cap, the delta-yield design notes in the module docstring; the new function is ADDITIVE, nothing existing changes)
    - src/personacore/generation/__init__.py (current export list — add the new symbol in the same style)
    - tests/test_generation_text.py (the tiny-CPU-GPT fixture at lines 31-43 AND whatever tokenizer object those tests pass to generate_text — reuse the exact same fixture approach, do not invent a new one)
  </read_first>
  <behavior>
    tests/test_demo_callback.py (CPU-only, imports nothing from gradio or scripts/):
    - test_yields_are_cumulative: drive generate_text_cumulative on the tiny fixture with greedy=True, max_new_tokens=8; collect all yields; assert len(yields) >= 1, every yields[i+1].startswith(yields[i]) (monotone growth), and lengths are strictly non-decreasing with at least one strict increase.
    - test_final_yield_equals_collected_deltas: under identical args + greedy=True, the LAST cumulative yield equals "".join(generate_text(...)) — the adapter adds accumulation and nothing else.
    - test_kwargs_thread_through: max_new_tokens is keyword-only (calling positionally raises TypeError); max_new_tokens=0 raises ValueError (the existing (0, 4096] guard fires through the adapter, T-06-04 DoS lineage); temperature/top_k/generator pass through without error (one seeded warm call returns a str).
    - test_no_eos_literal_in_output: the final cumulative yield does not contain the literal "&lt;|endoftext|&gt;" (Phase-6 "no raw separator ever shown" carried guarantee).
  </behavior>
  <action>
    Add generate_text_cumulative(model, tokenizer, prompt, *, max_new_tokens, **gen_kw) to
    src/personacore/generation/text.py — ADDITIVE ONLY (CONTEXT phase boundary: generate()/
    generate_text are frozen; this wraps, never modifies). Implementation: iterate
    generate_text(...) accumulating deltas into a running string and yield the running string
    after each delta — the Gradio ChatInterface cumulative contract (08-RESEARCH Pitfall 1:
    yielding deltas makes the chat bubble flicker lone fragments). Docstring cites DEMO-01 and
    Pitfall 1 and states WHY the two yield shapes differ (delta = composable producer,
    cumulative = Gradio display contract). Export it from generation/__init__.py. Then write
    tests/test_demo_callback.py per the behavior block, with the standard module docstring
    (DEMO-01, CPU-only, GPU/MPS-free). Discretion note: the adapter lives in the package (not
    the script) so CI tests it without gradio installed and without importing scripts/ —
    "logic lives in src/personacore, scripts only wire" (Phase-1 D-04).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_demo_callback.py tests/test_generation_text.py -q exits 0; make lint exits 0</automated>
  </verify>
  <done>Adapter exported and green; existing generation tests untouched and green</done>
  <acceptance_criteria>
    - src/personacore/generation/text.py contains `def generate_text_cumulative(`
    - src/personacore/generation/__init__.py exports generate_text_cumulative
    - tests/test_demo_callback.py contains the four named tests and contains NO `import gradio`
    - `.venv/bin/python -m pytest tests/test_demo_callback.py -q` exits 0
    - `git diff src/personacore/generation/text.py` shows only ADDED lines around the new function (no modification to generate_text/generate_text_str bodies)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: scripts/demo_app.py — offline Gradio launcher per UI-SPEC</name>
  <files>scripts/demo_app.py</files>
  <read_first>
    - .planning/phases/08-demo-writeup/08-UI-SPEC.md (the LOCKED copy contract: exact title/description/placeholder/examples strings, slider labels+ranges, error message, hard no-theme rule — implement verbatim)
    - scripts/evaluate.py (thin-script shape: docstring style, env-var-before-import with `# noqa: E402`, _REPO_ROOT constants, main() guard — the script being created mirrors it)
    - src/personacore/checkpoint.py (load_slim signature added by 08-01)
    - .planning/phases/08-demo-writeup/08-RESEARCH.md § Pattern 1 (the verified ChatInterface wiring against the 5.50.0 wheel)
  </read_first>
  <action>
    Create scripts/demo_app.py (thin wiring, no argparse). Structure and concrete values:
    - Line 1 region: module docstring naming DEMO-01/D-02 and the offline guarantee; then
      os.environ["GRADIO_ANALYTICS_ENABLED"] = "False" BEFORE any gradio import (Pitfall 5);
      late imports carry `# noqa: E402`.
    - Constants: _REPO_ROOT, SLIM_PATH = _REPO_ROOT / "checkpoints" / "model_slim.pt",
      TOKENIZER_PATH = _REPO_ROOT / "artifacts" / "tokenizer.json".
    - build_demo() -> gr.ChatInterface (lazy — NO module-level model load, so importing the
      module is side-effect-free for tests/CI): if SLIM_PATH missing, raise
      FileNotFoundError with the exact UI-SPEC message ("checkpoints/model_slim.pt not found.
      Either download the release asset (see README quickstart) or regenerate it from a local
      best.pt: python scripts/export_slim.py"). Load via load_slim(SLIM_PATH) — the
      weights_only=True choke point; rebuild ModelConfig(**ckpt["model_config"]) -> GPT ->
      load_state_dict -> .eval(); pin CPU via RuntimeConfig(device="cpu") and model.to(...)
      (DEMO-01 says laptop CPU — never let device resolution drift to MPS); tokenizer via
      from_json(TOKENIZER_PATH) (frozen, data-only).
    - Callback tell_story(message, history, temperature, top_k, max_new_tokens): IGNORE
      history — fresh story per message (anti-pattern: zero dialogue tuning, concatenation
      overclaims chat ability); map top_k slider value 0 -> None; yield from
      generate_text_cumulative(model, tok, message, max_new_tokens=int(max_new_tokens),
      temperature=float(temperature), top_k=...).
    - gr.ChatInterface kwargs (UI-SPEC verbatim): type="messages", analytics_enabled=False,
      title="PersonaCore — TinyStories story completion (13.9M params, fully on-device)",
      description exactly per UI-SPEC Copywriting Contract, textbox=gr.Textbox(
      placeholder="Type a story opening…"), examples = the three exact UI-SPEC openings
      (cache_examples left at default False), additional_inputs = three gr.Slider:
      Temperature 0.1-1.5 default 0.8 step 0.05; "Top-k (0 = disabled)" 0-200 default 50
      step 1; "Max new tokens" 16-1024 default 400 step 16 (under the 4096 cap).
      HARD RULE: no theme=, no css=, no gr.themes.*, no fonts — the default theme is the only
      offline-verified configuration.
    - main(): build_demo().launch(share=False). `if __name__ == "__main__": main()`.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import importlib.util; spec=importlib.util.spec_from_file_location('demo_app','scripts/demo_app.py'); m=importlib.util.module_from_spec(spec); spec.loader.exec_module(m); d=m.build_demo(); print(type(d).__name__)" prints ChatInterface (loads the real slim artifact, constructs the UI, does NOT launch); make lint exits 0</automated>
  </verify>
  <done>build_demo() constructs a ChatInterface against the real slim checkpoint without launching; ruff clean</done>
  <acceptance_criteria>
    - scripts/demo_app.py sets GRADIO_ANALYTICS_ENABLED on an earlier line than `import gradio`
    - File contains `share=False`, `type="messages"`, `analytics_enabled=False`, `load_slim`, `RuntimeConfig(device="cpu")`, `generate_text_cumulative`
    - File contains the exact title string `PersonaCore — TinyStories story completion (13.9M params, fully on-device)` and the exact placeholder `Type a story opening…`
    - File contains NONE of: `theme=`, `css=`, `gr.themes`, `argparse`, `weights_only=False`
    - The importlib construction command prints `ChatInterface` and exits 0
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Offline streaming smoke — verify DEMO-01 live</name>
  <files>(none — verification checkpoint, no file changes)</files>
  <read_first>
    - .planning/phases/08-demo-writeup/08-UI-SPEC.md § Interaction Contract (what correct behavior looks like)
  </read_first>
  <action>
    Pause execution and present the how-to-verify steps below to the developer. Do not
    proceed to any later wave until the resume signal is received. If issues are reported,
    fix scripts/demo_app.py (or the adapter) and re-present this checkpoint.
  </action>
  <what-built>scripts/demo_app.py — an offline Gradio chat UI running the from-scratch 13.9M model on your laptop CPU, streaming TinyStories completions with temperature/top-k/max-tokens controls, loading the slim weights_only=True checkpoint.</what-built>
  <how-to-verify>
    1. `source .venv/bin/activate && python scripts/demo_app.py` — startup should NOT hang ~3s (a hang suggests a network timeout, i.e. the analytics kill failed)
    2. Open http://127.0.0.1:7860 in your browser
    3. NOW TURN WI-FI OFF (the offline proof)
    4. Click the example "Once upon a time, there was a little dog named Max." — expect: first text appears near-instantly, the bubble GROWS token-by-token (never flickers lone fragments), a full story completes in ~2 s, ends naturally (no raw &lt;|endoftext|&gt; visible)
    5. Open the additional-inputs accordion: set Temperature to 0.1 and resend — output should be more repetitive/deterministic; set 1.3 — wilder. Set Top-k to 0 (disabled) and confirm it still generates
    6. Send a second message — confirm it starts a FRESH story (no bleed-through from the previous turn)
    7. Confirm the page itself loaded fine with Wi-Fi off (fonts/UI intact = no CDN dependency). Turn Wi-Fi back on. Ctrl-C the server
  </how-to-verify>
  <verify>
    <human-check>Developer confirms: streams with Wi-Fi off, bubble grows cumulatively, sliders change behavior, fresh story per message, no EOS literal shown</human-check>
  </verify>
  <done>Resume signal "approved" received (or reported issues fixed and re-approved)</done>
  <resume-signal>Type "approved" or describe what failed (hang, flicker, network error, wrong copy)</resume-signal>
  <acceptance_criteria>
    - Human confirms: streams with Wi-Fi off, bubble grows cumulatively, sliders change behavior, fresh story per message, no EOS literal shown
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> Gradio server | User text prompt crosses into the generation path |
| Gradio server -> internet | Must NOT exist: telemetry, version ping, CDN, tunnel |
| disk -> torch.load | Slim checkpoint load at demo startup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering/EoP | demo checkpoint load | mitigate | load_slim only (weights_only=True); `weights_only=False` is grep-asserted absent from demo_app.py |
| T-08-02 | Information Disclosure | network exposure of the UI | mitigate | share=False + default 127.0.0.1 bind; never set GRADIO_SERVER_NAME/0.0.0.0; human smoke includes Wi-Fi-off proof |
| T-08-03 | Information Disclosure | Gradio telemetry/version ping | mitigate | GRADIO_ANALYTICS_ENABLED=False before import + analytics_enabled=False param (kills version_check thread too — verified in 5.50.0 source) |
| T-08-04 | DoS | unbounded generation request | mitigate | Existing (0, 4096] max_new_tokens guard raises before the loop (T-06-04); slider capped at 1024 makes it unreachable from the UI; concurrency_limit default 1 via queue |
| T-08-05 | Spoofing | prompt injection into model | accept | Prompt is data-only (encoded to token ids, never eval'd/templated); model has no tools/secrets; worst case is weird story text |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_demo_callback.py -q` green; full suite green after merge
- importlib build_demo() construction prints ChatInterface
- Human-verified offline streaming session (Wi-Fi off)
- `make lint` exits 0
</verification>

<success_criteria>
- DEMO-01 satisfied: gr.ChatInterface, share=False, localhost, laptop CPU, fully offline, temperature/top-k controls
- Honest framing per D-02: story-completion copy, fresh story per message, no chat-tuning claims
- The streaming contract is unit-tested (cumulative growth) and human-verified live
</success_criteria>

<output>
Create `.planning/phases/08-demo-writeup/08-02-SUMMARY.md` when done
</output>
