---
phase: 12-stage-2-conversational-fine-tune
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/personacore/training/loop.py
  - tests/test_masked_train_seam.py
  - tests/test_extra_eval_fns.py
autonomous: true
requirements: [DEBT-01, DEBT-02, TUNE-01, TUNE-02]
must_haves:
  truths:
    - "train(train_bin=..., train_mask_bin=...) draws training batches via get_batch_memmap_masked — targets carry -100 at user-turn positions"
    - "train(val_bin=..., val_mask_bin=...) makes the in-loop val_loss (and therefore best-checkpoint selection) measure assistant-token masked CE"
    - "train(extra_eval_fns={...}) appends one CSV column per key at every eval interval, and the model is back in train mode afterward"
    - "All three new kwargs default to None and reproduce the v1.0 trajectory bit-identically (golden fixture + full 250-test suite green)"
    - "DEBT-01 (true-token CSV column) and DEBT-02 (frozen retention policy) remain closed per commit ca14a89 — confirmed by their pinned tests, no re-implementation"
  artifacts:
    - path: "src/personacore/training/loop.py"
      provides: "train_mask_bin, val_mask_bin, extra_eval_fns additive kwargs"
      contains: "extra_eval_fns"
    - path: "tests/test_masked_train_seam.py"
      provides: "mask-seam routing + default-None v1.0 identity tests"
    - path: "tests/test_extra_eval_fns.py"
      provides: "extra-column logging, call-count, train-mode-restore, identity tests"
  key_links:
    - from: "src/personacore/training/loop.py"
      to: "src/personacore/training/data.py"
      via: "batch_fn calls get_batch_memmap_masked when train_mask_bin is set"
      pattern: "get_batch_memmap_masked"
    - from: "src/personacore/training/loop.py"
      to: "src/personacore/logging.py"
      via: "per-run fieldnames CSV_FIELDNAMES + sorted(extra_eval_fns) at CSVLogger construction"
      pattern: "sorted\\(extra_eval_fns"
---

<objective>
Add the two additive loop seams Phase 12 needs — masked-batch routing (`train_mask_bin` /
`val_mask_bin`) and per-interval extra eval columns (`extra_eval_fns`) — to the untouched v1.0
`train()`, with defaults reproducing v1.0 bit-for-bit, protected by the golden-trajectory fixture
exactly as the DEBT-01 telemetry edit was.

Purpose: RESEARCH's load-bearing finding is that `train()` cannot run this phase as-is — the
memmap branch hardcodes unmasked `get_batch_memmap` and there is no way to log retention PPL per
eval interval. "Untouched v1.0 train()" (TUNE-01) is read the DEBT-01 way: additive default-None
kwargs whose defaults are bit-identical. The converged v2.0 ARCHITECTURE pre-approved exactly
these seams — do not build a second training loop.

USER LOCK 3 (honor exactly): `val_mask_bin` SHIPS. Justification to record in the loop docstring
and here: the in-loop val_loss gates best.pt selection, and the checkpoint is selected FOR
assistant-token dialogue capability — unmasked CE would partially reward modeling user turns and
would make "best" mean something the report never measures. Gates elsewhere still use the
deterministic `masked_perplexity` sweep, never in-loop val_loss.

Output: modified `loop.py` + two new test files; full suite green; DEBT-01/02 pins confirmed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-stage-2-conversational-fine-tune/12-CONTEXT.md
@.planning/phases/12-stage-2-conversational-fine-tune/12-PATTERNS.md
@src/personacore/training/loop.py
@src/personacore/training/data.py
@tests/test_loop_penalty_fn.py

<interfaces>
Verified this session by the pattern-mapper (12-PATTERNS.md):

From src/personacore/training/data.py:93 —
`get_batch_memmap_masked(bin_path, mask_path, batch_size, block_size, device)` → `(x, y)` with
`y[m == 0] = -100` (target-space shifted-mask semantics, unit-tested in tests/test_masked_batch.py).

From src/personacore/training/loop.py —
- loop.py:280-290: Seam 1 memmap branch — `batch_fn(_micro)` closure calling `get_batch_memmap`.
- loop.py:97-101: `estimate_loss` val-draw routing (`if is_bin: get_batch_memmap(...)`), inside
  the `_rng_state()`/`_restore_rng` snapshot (loop.py:82/105) which must stay untouched.
- loop.py:49: `CSV_FIELDNAMES = ["step", "train_loss", "val_loss", "lr", "tokens", "wall_clock"]`
  (module constant — never mutate).
- loop.py:326: `csv = CSVLogger(log_path, fieldnames=CSV_FIELDNAMES) if log_path ...`
- loop.py:350-369: the eval-interval CSV block — insertion site for extra fns.
- loop.py:203-236: kwarg docstring register (copy the `penalty_fn` form: default None, states the
  seam, states "None reproduces v1.0 bit-for-bit").

From src/personacore/logging.py:24-38 — `CSVLogger(path, fieldnames)` writes header once
(new/empty file only); `DictWriter` raises on unknown keys (the enforcement mechanism).

Pitfall 4 (RESEARCH, verified perplexity.py:55): `perplexity()`/`retention_perplexity()` call
`model.eval()` and never restore — the extras block MUST restore `model.train()` after running fns.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: train_mask_bin / val_mask_bin seam in train() + tests/test_masked_train_seam.py</name>
  <files>src/personacore/training/loop.py, tests/test_masked_train_seam.py</files>
  <read_first>
    - src/personacore/training/loop.py (whole file — seam sites at the memmap branch ~280-290, estimate_loss ~97-101, docstrings ~203-236)
    - src/personacore/training/data.py (get_batch_memmap_masked signature + -100 semantics, ~93-126)
    - tests/test_loop_penalty_fn.py (the `_run_recipe` in-process identity idiom, ~73-124)
    - tests/test_retention_ppl.py (tiny-bin `ids.tofile(tmp_path)` fixture recipe, ~40-46)
    - .planning/phases/12-stage-2-conversational-fine-tune/12-PATTERNS.md (§ train_mask_bin/val_mask_bin seam)
  </read_first>
  <behavior>
    - Test 1 (routing): `train()` with a tiny uint16 bin + aligned uint8 mask bin in tmp_path and `train_mask_bin` set draws batches through `get_batch_memmap_masked`; assert -100 sentinels reach the targets (e.g., monkeypatch/wrap `personacore.training.loop`'s reference to `get_batch_memmap_masked` to capture a drawn `y` and assert `(y == -100).any()`).
    - Test 2 (guard): `train_mask_bin` set while `train_bin is None` raises ValueError with a message naming both kwargs.
    - Test 3 (val routing): with `val_mask_bin` set, `estimate_loss`'s val draws route through `get_batch_memmap_masked(val_bin, val_mask_bin, ...)` (capture-count idiom); the `_rng_state()`/`_restore_rng` wrapper still encloses the draw.
    - Test 4 (identity): omitted kwargs == explicit `train_mask_bin=None, val_mask_bin=None` — the `_run_recipe` triple (CSV text, final-loss repr, param sha256) is bitwise equal.
  </behavior>
  <action>
    Add two default-None kwargs to `train()` in src/personacore/training/loop.py: `train_mask_bin`
    and `val_mask_bin`. In the Seam-1 memmap branch (the `elif train_bin is not None:` block), when
    `train_mask_bin is not None` the `batch_fn` closure calls
    `get_batch_memmap_masked(train_bin, train_mask_bin, train_config.batch_size, model_cfg.block_size, runtime.device)`
    instead of `get_batch_memmap`. Raise ValueError if `train_mask_bin` is given without `train_bin`.
    Thread `val_mask_bin` (default None) into `estimate_loss`'s bin-draw routing point so val draws
    use `get_batch_memmap_masked(val_bin, val_mask_bin, ...)` when set — keep the existing
    `_rng_state()`/`_restore_rng` wrapper byte-untouched around it. Docstrings follow the
    `penalty_fn` register exactly: state the seam name, state "None reproduces v1.0 bit-for-bit",
    and for `val_mask_bin` record the lock-3 selection justification (in-loop val_loss gates
    best-checkpoint selection; best is selected FOR assistant-token dialogue capability; unmasked CE
    would partially reward modeling user turns). Do not modify `get_batch_memmap`, `CSV_FIELDNAMES`,
    or any LOCKED contract. TDD order: write tests/test_masked_train_seam.py first (RED), then the
    seam (GREEN).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_masked_train_seam.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "train_mask_bin" src/personacore/training/loop.py` shows the kwarg in the `train()` signature with default `None` and in the memmap-branch conditional
    - `grep -n "get_batch_memmap_masked" src/personacore/training/loop.py` shows at least two call/import sites (train batch_fn + estimate_loss val routing)
    - `.venv/bin/python -m pytest tests/test_masked_train_seam.py -q` exits 0 with ≥4 tests collected
    - `.venv/bin/python -m pytest tests/test_train_loop.py tests/test_resume_curve.py tests/test_loop_penalty_fn.py -q` exits 0 (golden trajectory + resume + penalty seam untouched)
  </acceptance_criteria>
  <done>Mask seam routes masked batches for train and val draws, guard raises, defaults are bit-identical to v1.0, all existing loop tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: extra_eval_fns seam in train() + tests/test_extra_eval_fns.py</name>
  <files>src/personacore/training/loop.py, tests/test_extra_eval_fns.py</files>
  <read_first>
    - src/personacore/training/loop.py (eval-interval CSV block ~350-369, CSV_FIELDNAMES ~49, CSVLogger construction ~326, `_rng_state`/`_restore_rng` ~52-62)
    - src/personacore/logging.py (CSVLogger header-once + unknown-key raise, ~24-38)
    - tests/test_loop_penalty_fn.py (call-counting idiom ~181-196; `_run_recipe` identity idiom)
    - .planning/phases/12-stage-2-conversational-fine-tune/12-PATTERNS.md (§ extra_eval_fns seam)
  </read_first>
  <behavior>
    - Test 1 (columns): `train(extra_eval_fns={"probe": fn}, log_path=...)` produces a CSV whose header is exactly `CSV_FIELDNAMES + ["probe"]` and every eval row carries the fn's float value; the module constant `CSV_FIELDNAMES` is unchanged after the run.
    - Test 2 (call count): a counting fn with `eval_interval=1, max_steps=5` is called exactly once per eval-logging event — assert the exact expected count by first observing how many eval rows the v1.0 loop emits for that recipe (including whether a step-0 row exists), and pin that number.
    - Test 3 (train-mode restore): an fn that calls `model.eval()` (mimicking `perplexity()`, Pitfall 4) — after `train()` returns, `model.training is True`, and mid-run the step following an eval interval trains in train mode.
    - Test 4 (RNG hygiene): an fn that consumes torch RNG (`torch.rand(1)`) yields the same final param sha256 and same `train_loss` column values as a run with a pure fn — the extras block is inside a `_rng_state()`/`_restore_rng` snapshot.
    - Test 5 (identity): omitted == `extra_eval_fns=None` — byte-identical CSV, final-loss repr, param sha256.
  </behavior>
  <action>
    Add `extra_eval_fns=None` (type: `dict[str, Callable[[torch.nn.Module], float]]`) to `train()`.
    At CSVLogger construction, when `extra_eval_fns` is provided, per-run fieldnames become
    `CSV_FIELDNAMES + sorted(extra_eval_fns)` — computed locally, module constant never mutated,
    never appended to an existing file (CSVLogger's unknown-key raise is the enforcement). Inside the
    existing eval-interval block, after `val_loss` is computed, run each fn as `fns[key](model)` and
    pass one `key=value` pair per key into `csv.log(...)`. Wrap the extras execution in the existing
    `_rng_state()`/`_restore_rng` snapshot idiom (resume-equality survives a non-pure fn) and call
    `model.train()` after the extras block (Pitfall 4 — `perplexity()` leaves the model in eval
    mode). Docstring in the `penalty_fn` register: names the seam (retention_ppl / dialog_ppl /
    ewc_penalty / role norms all ride it), "None reproduces v1.0 bit-for-bit". While writing Test 2,
    determine and record (as a code comment in the test) whether the v1.0 eval block fires at step 0
    — Plan 12-04's step-0 retention row depends on knowing this. TDD order: tests first (RED), then
    the seam (GREEN).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_extra_eval_fns.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "extra_eval_fns" src/personacore/training/loop.py` shows the kwarg (default None), the sorted-fieldnames construction, and the eval-block execution site
    - `grep -c "sorted(extra_eval_fns" src/personacore/training/loop.py` ≥ 1
    - `.venv/bin/python -m pytest tests/test_extra_eval_fns.py -q` exits 0 with ≥5 tests collected
    - tests/test_extra_eval_fns.py contains a comment stating whether the eval block logs a step-0 row (exact string "step 0" or "step-0" present in a comment)
    - `.venv/bin/python -m pytest tests/test_logging.py -q` exits 0 (CSVLogger untouched)
  </acceptance_criteria>
  <done>Extra eval fns log one column per key per eval interval, model returns to train mode, RNG snapshot protects resume-equality, defaults bit-identical to v1.0.</done>
</task>

<task type="auto">
  <name>Task 3: Purity confirmation — golden trajectory, full suite, DEBT-01/02 pins</name>
  <files>(verification only — no files modified)</files>
  <read_first>
    - tests/test_run_csv_tokens.py (DEBT-01 pin — read to know what it asserts)
    - tests/test_retention_ppl.py (DEBT-02 pin — read to know what it asserts)
  </read_first>
  <action>
    Confirm the loop edits kept every protection contract. DEBT-01 (true-token `run.csv` column)
    and DEBT-02 (`retention_perplexity()` frozen dead-id policy) were closed as pre-work in commit
    `ca14a89` (2026-07-31, golden trajectory recaptured bit-identical, headline 2.1066 re-verified)
    — this task is verification-by-reference ONLY, never re-implementation. Run the DEBT pins, the
    golden-trajectory/resume tests, and the full 250-test suite in the mandatory 3.11 venv. If
    anything is red, fix the Task 1/2 seams (not the tests, not the fixture) until green.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_run_csv_tokens.py tests/test_retention_ppl.py tests/test_train_loop.py tests/test_resume_curve.py tests/test_loop_penalty_fn.py -q && .venv/bin/python -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/ -q` exits 0 (full suite, ~250+ tests incl. the two new files)
    - `git log --oneline -1 ca14a89` resolves (DEBT pre-work commit exists — reference, not re-work)
    - No diff in tests/fixtures/golden_trajectory_v1.json (`git status --porcelain tests/fixtures/` is empty)
  </acceptance_criteria>
  <done>Full suite green including new seam tests; DEBT-01/02 confirmed closed by their pins; golden fixture byte-untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Pure in-repo Python edits + CPU-only tests; no network, no user input, no deserialization added |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | training trajectory (silent v1.0 behavior drift via loop edits) | mitigate | default-None kwargs + golden-trajectory fixture bit-identity + full-suite gate (Task 3) |
| T-12-02 | Repudiation | CSV telemetry (columns silently appended to old files) | mitigate | per-run fieldnames at CSVLogger construction; DictWriter unknown-key raise; never mutate CSV_FIELDNAMES |

No package installs this phase — supply-chain gate not applicable (RESEARCH Package Legitimacy Audit: N/A).
</threat_model>

<verification>
- Full suite green: `.venv/bin/python -m pytest tests/ -q` exits 0
- Golden fixture untouched; both new test files collected and green
- `train()` signature carries `train_mask_bin`, `val_mask_bin`, `extra_eval_fns`, all default None
</verification>

<success_criteria>
- All three seams exist, are additive, documented in the penalty_fn docstring register, and default to bit-identical v1.0 behavior
- DEBT-01/DEBT-02 confirmed closed via their pinned tests (reference to ca14a89, zero re-implementation)
- Full 250+-test suite green
</success_criteria>

<output>
Create `.planning/phases/12-stage-2-conversational-fine-tune/12-01-SUMMARY.md` when done.
Record: whether the eval block logs a step-0 row (Plan 12-04 consumes this fact).
</output>
