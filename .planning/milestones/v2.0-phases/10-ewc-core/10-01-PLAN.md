---
phase: 10-ewc-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/personacore/continual/__init__.py
  - src/personacore/continual/fisher.py
  - src/personacore/continual/ewc.py
  - tests/test_fisher.py
  - tests/test_ewc_penalty.py
autonomous: true
requirements: [EWC-01, EWC-02]

must_haves:
  truths:
    - "estimate_fisher produces a per-example (batch=1) empirical diagonal Fisher that matches a brute-force per-example oracle on a tiny fixture and provably DIFFERS from the batched-gradient estimate on the same windows (D-03: strict batch=1 loop, model's own CE)"
    - "The stored Fisher is mean-normalized (global mean over all deduped trainable coordinates == 1) with the raw normalizer recorded in fisher_meta (D-01/D-02)"
    - "Fisher and theta_star keys carry the tied wte/lm_head storage exactly once (wte.weight present, lm_head.weight absent)"
    - "EWCPenalty evaluates to exactly 0.0 (equality, not allclose) at the anchor and its gradient equals lambda*F*(theta-theta_star)"
    - "estimate_fisher is RNG-pure: global python/numpy/torch RNG states are bit-unchanged after the call"
  artifacts:
    - path: "src/personacore/continual/fisher.py"
      provides: "estimate_fisher -> (fisher, fisher_meta)"
      min_lines: 80
    - path: "src/personacore/continual/ewc.py"
      provides: "EWCPenalty callable (model) -> scalar tensor"
      min_lines: 40
    - path: "src/personacore/continual/__init__.py"
      provides: "package import surface exporting estimate_fisher + EWCPenalty"
      exports: ["estimate_fisher", "EWCPenalty"]
    - path: "tests/test_fisher.py"
      provides: "EWC-01 unit pins (oracle, anti-batched, normalization, dedup, RNG purity, determinism)"
      min_lines: 120
    - path: "tests/test_ewc_penalty.py"
      provides: "EWC-02 penalty pins (quadratic oracle, exact-zero, lambda linearity, gradient, fail-loud validation)"
      min_lines: 60
  key_links:
    - from: "src/personacore/continual/fisher.py"
      to: "model.forward (LOCKED CE tail)"
      via: "loss from model(x, y) — never a reimplemented CE"
      pattern: "model\\(x"
    - from: "src/personacore/continual/fisher.py"
      to: "per-example gradients"
      via: "torch.autograd.grad inside the batch=1 loop"
      pattern: "torch\\.autograd\\.grad"
    - from: "src/personacore/continual/ewc.py"
      to: "model parameters by name"
      via: "dict(model.named_parameters()) lookup in __call__"
      pattern: "named_parameters"
---

<objective>
Build the `continual/` package — the milestone's second headline from-scratch deliverable:
`estimate_fisher` (per-example empirical diagonal Fisher, D-01..D-05) and `EWCPenalty`
(the Kirkpatrick quadratic form), both fully test-pinned on CPU-only fixtures.

Purpose: EWC-01's estimation discipline (per-example, NOT batched-gradient squaring — the van de
Ven bug) and EWC-02's penalty math are the correctness core everything in Phases 12/13 trusts.
Output: `src/personacore/continual/{__init__,fisher,ewc}.py` + `tests/test_fisher.py` +
`tests/test_ewc_penalty.py`, full suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/10-ewc-core/10-CONTEXT.md
@.planning/phases/10-ewc-core/10-RESEARCH.md
@.planning/phases/10-ewc-core/10-PATTERNS.md

<interfaces>
<!-- LOCKED contracts this plan CREATES. Plans 10-02 and 10-03 (wave 2) build against these
     signatures verbatim — do not deviate without updating those plans. -->

From src/personacore/continual/fisher.py (NEW — this plan):
```python
def estimate_fisher(
    model, bin_path, *, n_examples, block_size, device, seed, normalize=True
) -> tuple[dict[str, "torch.Tensor"], dict]:
    """Per-example empirical diagonal Fisher at the model's current weights (EWC-01, D-03).

    Returns (fisher, fisher_meta):
      fisher: {param_name: fp32 CPU tensor, shape == param}, keys from model.named_parameters()
              (tied wte/lm_head tensor appears ONCE, under 'wte.weight' for the GPT)
      fisher_meta: primitives only (weights_only=True-safe), exact key set:
        variant, n_examples, seed, block_size, bin_path, normalized, normalizer,
        spearman_half, rel_mean_change_a, rel_mean_change_b, spearman_method
    """
```

From src/personacore/continual/ewc.py (NEW — this plan):
```python
class EWCPenalty:
    """(lam/2) * sum_n (F_n * (p_n - theta_star_n)**2).sum() — EWC-02."""
    def __init__(self, fisher, theta_star, lam, device): ...   # validates + moves to device ONCE
    def __call__(self, model) -> "torch.Tensor": ...           # scalar, differentiable w.r.t. params
```

From src/personacore/training/loss.py (EXISTING — consumed, never modified):
```python
def assemble_loss(base_loss, extra_penalties=()):  # identity on (), additive otherwise
```

From src/personacore/training/loop.py lines 48-58 (EXISTING — RNG-purity test idiom):
```python
def _rng_state():
    return (random.getstate(), np.random.get_state(), torch.get_rng_state())
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: estimate_fisher — per-example diagonal Fisher with mean-normalization and convergence stats</name>
  <files>src/personacore/continual/fisher.py, src/personacore/continual/__init__.py, tests/test_fisher.py</files>
  <read_first>
    - .planning/phases/10-ewc-core/10-RESEARCH.md (Pattern 1, Code Examples 1+6, Pitfalls 1-4, 7)
    - .planning/phases/10-ewc-core/10-PATTERNS.md (fisher.py section: window-draw idiom, eval-mode discipline, module docstring house style)
    - src/personacore/training/data.py (get_batch_memmap — the window-draw idiom to mirror privately; note it uses GLOBAL np.random, which fisher.py must NOT)
    - src/personacore/training/loop.py (lines 48-102: _rng_state/_restore_rng + estimate_loss — the eval-mode/restore discipline template)
    - src/personacore/lora/__init__.py (package-init shape to copy for continual/__init__.py)
    - tests/test_gpt_weight_tying.py and tests/test_ablation_config.py (data_ptr dedup test idioms)
    - tests/test_lora_artifact.py (tiny ModelConfig fixture pattern: block_size=32, n_layer=1, n_head=2, n_embd=16 with LOCKED vocab_size/eos_id defaults)
  </read_first>
  <behavior>
    Write tests/test_fisher.py FIRST (tests fail on missing module), then implement. All tests
    CPU-only, tiny GPT fixture `ModelConfig(block_size=32, n_layer=1, n_head=2, n_embd=16)`,
    tmp_path uint16 bin (a few thousand random tokens &lt; 8192) written via np.memmap or tofile:
    - Test 1 (oracle, EWC-01): with n_examples=4 and a fixed seed, re-derive the 4 window starts
      in the test using np.random.default_rng(seed) and the pinned draw pattern (one
      `int(rng.integers(0, data_len - block_size - 1))` call per example, in example order);
      compute expected raw Fisher by an explicit per-example loop (forward -> autograd.grad ->
      g**2, averaged over 4); assert estimate_fisher(..., normalize=False) matches per-tensor
      via torch.allclose(rtol=1e-6, atol=0).
    - Test 2 (anti-batched discriminator, Pitfall 2): on the SAME 4 windows compute the
      batched-gradient "Fisher" (one batch=4 forward, square the aggregated grads); assert the
      relative L2 distance between flattened per-example and batched estimates exceeds 1e-2 —
      pinning that the implementation cannot regress to the van de Ven bug.
    - Test 3: all Fisher entries finite and >= 0.
    - Test 4 (determinism): two calls with the same seed return torch.equal tensors and equal meta.
    - Test 5 (D-01/D-02 normalization): with normalize=True the global mean over ALL flattened
      coordinates equals 1.0 within 1e-5 (computed in fp64); fisher_meta["normalizer"] > 0;
      normalized * normalizer allclose the normalize=False result from an identical-seed call.
    - Test 6 (dedup, Pitfall 1): "lm_head.weight" not in fisher; "wte.weight" in fisher;
      len(fisher) == len({p.data_ptr() for p in model.parameters()}).
    - Test 7 (RNG purity, Pitfall 3): the (random.getstate(), np.random.get_state(),
      torch.get_rng_state()) triple is bit-equal before/after estimate_fisher (compare numpy
      state element-wise).
    - Test 8 (mode restore): model in train mode stays train after the call; model in eval stays eval.
    - Test 9 (meta contract): fisher_meta keys == {"variant","n_examples","seed","block_size",
      "bin_path","normalized","normalizer","spearman_half","rel_mean_change_a",
      "rel_mean_change_b","spearman_method"}; spearman_half is a float in [-1, 1].
  </behavior>
  <action>
    Implement estimate_fisher in src/personacore/continual/fisher.py per the LOCKED interface
    block above and RESEARCH Code Example 1 (D-03, EWC-01):
    (1) save was_training = model.training, call model.eval() (restore the PRIOR flag at the end
    — conditionally model.train(), never unconditionally);
    (2) named = list(model.named_parameters()) — tied tensor appears once by construction
    (session-verified); params = the tensor list;
    (3) strict batch=1 loop over n_examples: re-open np.memmap(bin_path, dtype=np.uint16,
    mode="r") per draw (RSS discipline from get_batch_memmap), start =
    int(rng.integers(0, data_len - block_size - 1)) from a LOCAL np.random.default_rng(seed)
    (this exact call pattern is pinned by the oracle test — document it in the docstring),
    x/y = consecutive windows cast uint16->int64 with a [None] batch dim, moved to device;
    `_, loss = model(x, y)` (the model's OWN CE — never reimplement the reduction, Pitfall 6);
    grads = torch.autograd.grad(loss, params); accumulate g.detach()**2 in fp32 on device into
    TWO half-accumulators (examples 0..n//2-1 -> acc_a, rest -> acc_b — D-05);
    (4) full = (acc_a + acc_b) / n_examples; guard: every tensor finite and raw global mean > 0,
    else raise ValueError naming the failure (Pitfall 7);
    (5) compute the global mean (the normalizer) in fp64 AFTER moving to CPU (MPS has no fp64 —
    Pitfall 4); if normalize=True divide every tensor by it (D-01/D-02);
    (6) convergence stats (D-05) in fp64 on CPU over the RAW half-estimates acc_a/(n//2) and
    acc_b/(n - n//2): spearman_half via the hand-rolled double-argsort ordinal-rank +
    np.corrcoef Spearman (RESEARCH Code Example 6 — scipy is NOT a dependency);
    rel_mean_change_a = abs(mean(half_a) - mean(full_raw)) / mean(full_raw), same for b;
    (7) return ({name: tensor.float().cpu()}, fisher_meta) with the exact meta key set from the
    interface block; variant string "empirical_diag_fisher/groundtruth_targets/mean_normalized",
    spearman_method "ordinal_double_argsort_no_tie_averaging"; bin_path stored as str.
    No @torch.no_grad (grads are the product), no autocast, no global RNG use anywhere.
    Module docstring: multi-paragraph house style naming EWC-01, D-01..D-05, Pitfalls 2/3/6/7.
    Create src/personacore/continual/__init__.py (minimal for now): module docstring naming
    Phase 10 / EWC-01..02, `from .fisher import estimate_fisher`, __all__ = ["estimate_fisher"]
    (Task 2 extends it). No pyproject change needed — the lora/ subpackage precedent shows the
    editable src-layout install picks up new subpackages.
  </action>
  <verify>
    <automated>source .venv/bin/activate && pytest tests/test_fisher.py -q -x</automated>
  </verify>
  <done>tests/test_fisher.py passes with all nine behaviors green; `from personacore.continual import estimate_fisher` imports.</done>
  <acceptance_criteria>
    - tests/test_fisher.py exits 0 via `pytest tests/test_fisher.py -q -x`
    - src/personacore/continual/fisher.py contains `def estimate_fisher(` with keyword-only `n_examples`, `block_size`, `device`, `seed`, `normalize=True`
    - fisher.py contains `torch.autograd.grad` and `np.random.default_rng` and does NOT contain `np.random.randint` or `torch.no_grad`
    - fisher.py contains no `import scipy` (hand-rolled Spearman only)
    - `python -c "from personacore.continual import estimate_fisher"` exits 0 inside .venv
    - Test file asserts `penalty`-free scope: covers oracle, anti-batched, non-negativity, determinism, normalization (mean==1 within 1e-5), dedup (lm_head.weight absent), RNG purity, mode restore, meta key set — one test function per behavior minimum
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: EWCPenalty — quadratic penalty with fail-loud validation + package export surface</name>
  <files>src/personacore/continual/ewc.py, src/personacore/continual/__init__.py, tests/test_ewc_penalty.py</files>
  <read_first>
    - .planning/phases/10-ewc-core/10-RESEARCH.md (Pattern 2, Code Example 4 — the Kirkpatrick quadratic form; exact-zero-at-anchor argument)
    - .planning/phases/10-ewc-core/10-PATTERNS.md (ewc.py section: assemble_loss seam contract, checkpoint.py fail-loud ValueError style from load_adapter lines 215-220)
    - src/personacore/training/loss.py (assemble_loss — the seam the returned scalar feeds; D-04: precomputed scalar tensors, no callables in the tuple)
    - tests/test_assemble_loss.py (exact-equality oracle test style to mirror)
    - src/personacore/continual/__init__.py (created in Task 1 — extend, don't rewrite)
    - src/personacore/lora/__init__.py (the full export-surface shape: docstring + sorted __all__)
  </read_first>
  <behavior>
    Write tests/test_ewc_penalty.py FIRST. CPU-only, toy modules (e.g., a 2-parameter nn.Module
    with known tensors) so oracles are hand-computable:
    - Test 1 (quadratic-form oracle): hand-compute (lam/2)*sum(F*(p-theta_star)**2) on a toy
      param dict; assert EWCPenalty output allclose within rtol=1e-7.
    - Test 2 (exact zero at anchor, EWC-02): theta_star = {n: p.detach().clone()} -> penalty.item()
      == 0.0 with `==` equality, NOT allclose (bitwise-zero subtraction in fp32).
    - Test 3 (lambda linearity): with lam=1.0 vs lam=2.0 on the same displaced params,
      penalty(2.0) == 2 * penalty(1.0) within rtol=1e-7 (powers of two scale exactly in fp32).
    - Test 4 (gradient): torch.autograd.grad(penalty, p) allclose lam * F * (p - theta_star)
      for a displaced param.
    - Test 5 (construction fail-loud): fisher/theta_star key mismatch raises ValueError whose
      message names the offending key(s) (pytest.raises match on the key name).
    - Test 6 (call fail-loud): a fisher key absent from model.named_parameters() raises
      ValueError naming the missing key when called.
    - Test 7 (integration with the seam): assemble_loss(base_loss, (EWCPenalty(...)(model),))
      equals base_loss + penalty exactly (torch.equal), and .backward() through the assembled
      total populates grads on displaced params.
  </behavior>
  <action>
    Implement EWCPenalty in src/personacore/continual/ewc.py per RESEARCH Code Example 4 and the
    LOCKED interface block:
    __init__(self, fisher, theta_star, lam, device): validate fisher.keys() == theta_star.keys()
    — on mismatch raise ValueError listing sorted symmetric-difference keys (mirror the
    load_adapter error style: name the gaps, never let a bare KeyError surface); validate
    per-key shape equality between fisher[n] and theta_star[n] (ValueError naming the key);
    store {n: t.to(device)} for BOTH dicts — moved exactly ONCE at construction
    (ARCHITECTURE anti-pattern 4: cross-device tensors crash mid-run on MPS); store lam.
    __call__(self, model): params = dict(model.named_parameters()); if any fisher key is absent
    raise ValueError naming the missing keys; total = sum over keys of
    (F * (p - theta_star) * (p - theta_star)).sum(); return (lam / 2.0) * total — a scalar
    tensor on the model's device, differentiable w.r.t. params (theta_star/F are buffers, no
    grad). The penalty consumes NO RNG and is deterministic in the params (the resume-equality
    contract survives).
    Module docstring: house style naming EWC-02, the Kirkpatrick 2017 quadratic form, the
    exact-zero-at-anchor property, and the assemble_loss/D-04 seam contract.
    Extend src/personacore/continual/__init__.py: add `from .ewc import EWCPenalty`, update the
    docstring to cover both deliverables, __all__ = sorted ["EWCPenalty", "estimate_fisher"].
    Then run the full suite + lint: all 190+ existing tests must stay green (this plan touches
    no v1.0 module, so any failure is environmental — investigate, don't skip).
  </action>
  <verify>
    <automated>source .venv/bin/activate && pytest tests/test_ewc_penalty.py -q -x && make test && make lint</automated>
  </verify>
  <done>tests/test_ewc_penalty.py green; full suite (190+ tests) green; lint clean; both symbols import from personacore.continual.</done>
  <acceptance_criteria>
    - `pytest tests/test_ewc_penalty.py -q -x` exits 0
    - `make test` exits 0 with zero failures (existing 190+ tests intact)
    - `make lint` exits 0
    - src/personacore/continual/ewc.py contains `class EWCPenalty` with `def __init__(self, fisher, theta_star, lam, device)` and `def __call__(self, model)`
    - tests/test_ewc_penalty.py contains an exact-equality anchor assertion using `== 0.0` (not allclose)
    - `python -c "from personacore.continual import estimate_fisher, EWCPenalty"` exits 0 inside .venv
    - src/personacore/continual/__init__.py `__all__` contains exactly "EWCPenalty" and "estimate_fisher"
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local filesystem -> np.memmap | fisher.py reads local uint16 bins (own trusted data; no untrusted input surface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | memmap bin read in estimate_fisher | accept | Local, own-generated training data; no parsing of untrusted content; finiteness guard fails loudly on corrupt data |
| T-10-02 | Denial of Service | unbounded n_examples loop | accept | Caller-controlled local compute; D-04 budget measured at <1 min for N=2000 |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies this phase (RESEARCH Package Legitimacy Audit: nothing to check); no install tasks exist |
</threat_model>

<verification>
- `pytest tests/test_fisher.py tests/test_ewc_penalty.py -q -x` green (the phase's EWC-01/EWC-02 unit pins)
- `make test` green — all pre-existing tests untouched and passing (no v1.0 module modified by this plan)
- `make lint` clean
- Both symbols importable from `personacore.continual`
</verification>

<success_criteria>
- Per-example Fisher matches the brute-force oracle AND provably differs from the batched-gradient estimate (roadmap success criterion 1, unit-scale half)
- mean(F)=1 normalization with recoverable raw normalizer (D-01/D-02)
- EWCPenalty exactly 0.0 at the anchor (roadmap success criterion 3, unit half)
- Tied-tensor dedup pinned by data_ptr-based tests (roadmap success criterion 2, key-structure half)
- Full suite green; estimate_fisher RNG-pure
</success_criteria>

<output>
Create `.planning/phases/10-ewc-core/10-01-SUMMARY.md` when done
</output>
