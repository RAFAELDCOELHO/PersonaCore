---
phase: 11-conversational-data-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/personacore/dialogue/__init__.py
  - src/personacore/dialogue/parse.py
  - src/personacore/dialogue/serialize.py
  - tests/fixtures/personachat_fb_fixture.txt
  - tests/test_dialogue_parse.py
  - tests/test_dialogue_serialize.py
autonomous: true
requirements: [DATA-01, DATA-02]
must_haves:
  truths:
    - "parse_episodes reads fb-dialog text into (persona, turns) episodes, including the last episode with no trailing boundary"
    - "encode_dialogue produces equal-length id/mask sequences with D-01 semantics: first <|user|>=0, subsequent <|user|>=1, assistant content=1, eos=1"
    - "Role tokens 8185-8187 encode atomically and round-trip through the frozen production tokenizer"
    - "Detokenizer rejoins contractions and closes spaced punctuation on format-faithful fixture lines"
  artifacts:
    - path: "src/personacore/dialogue/parse.py"
      provides: "fb-dialog episode parser (DATA-01, from scratch, stdlib)"
      exports: ["parse_episodes"]
    - path: "src/personacore/dialogue/serialize.py"
      provides: "detokenize (D-06) + render_document (D-04) + encode_dialogue with mask (D-01)"
      exports: ["detokenize", "render_document", "encode_dialogue"]
    - path: "tests/fixtures/personachat_fb_fixture.txt"
      provides: "committed format-faithful fb-dialog fixture (synthetic — never real corpus lines, D-00 no-re-host)"
    - path: "tests/test_dialogue_parse.py"
      provides: "DATA-01 parse unit tests"
    - path: "tests/test_dialogue_serialize.py"
      provides: "DATA-02 detok/render/mask/atomicity unit tests"
  key_links:
    - from: "src/personacore/dialogue/serialize.py"
      to: "src/personacore/tokenizer/special.py"
      via: "SPECIAL_TOKENS registry import (never retype ids)"
      pattern: "from personacore.tokenizer.special import"
    - from: "tests/test_dialogue_serialize.py"
      to: "artifacts/tokenizer.json"
      via: "frozen production tokenizer loaded with from_json"
      pattern: "from_json"
---

<objective>
Create the `src/personacore/dialogue/` package: from-scratch fb-dialog parser (DATA-01),
regex detokenizer (D-06), document renderer (D-04), and span-wise id+mask encoder (D-01) —
fully unit-tested against a committed format-faithful fixture, with role-token atomicity
proven through the frozen production tokenizer (DATA-02).

Purpose: every downstream artifact of this phase (inflation gate, bins, masked batches)
consumes exactly these functions — one `encode_dialogue` in the package prevents Pitfall 4
(gate and bins tokenizing differently).
Output: `dialogue/` package + 2 test files + 1 committed fixture. Pure code, no network, no bins.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-conversational-data-pipeline/11-CONTEXT.md
@.planning/phases/11-conversational-data-pipeline/11-RESEARCH.md
@.planning/phases/11-conversational-data-pipeline/11-PATTERNS.md

<interfaces>
From src/personacore/tokenizer/special.py (LOCKED registry — import, never retype):
SPECIAL_TOKENS maps "<|endoftext|>" -> 8184, "<|user|>" -> 8185, "<|assistant|>" -> 8186,
"<|system|>" -> 8187; EOS_ID = SPECIAL_TOKENS[EOS_TOKEN].

From src/personacore/tokenizer (package): `from_json(path)` loads the frozen production
tokenizer at `artifacts/tokenizer.json`; `tok.encode(text)` -> list[int];
`tok.encode(text, allowed_special="all")` maps special-token markers atomically;
`tok.decode(ids)` -> str.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: dialogue package + fb-dialog parser + committed fixture + parse tests</name>
  <files>src/personacore/dialogue/__init__.py, src/personacore/dialogue/parse.py, tests/fixtures/personachat_fb_fixture.txt, tests/test_dialogue_parse.py</files>
  <read_first>
    - .planning/phases/11-conversational-data-pipeline/11-RESEARCH.md (Pattern 2 — executable-grade parser, verified on the full corpus; fb-dialog format section)
    - .planning/phases/11-conversational-data-pipeline/11-PATTERNS.md (parse.py + test_dialogue_parse.py + __init__.py sections)
    - src/personacore/continual/__init__.py (package-init analog: requirement-tagged docstring, relative imports, __all__)
    - scripts/encode_corpus.py (_iter_documents streaming/flush discipline; docstring style)
    - tests/test_data_split.py (committed-fixture test style, fixture path constant, per-test invariant comments)
  </read_first>
  <behavior>
    - parse_episodes(fixture_path) returns exactly 3 episodes (fixture is built with 3)
    - Episode boundaries detected by line-number reset to "1"; the LAST episode (no trailing boundary) is flushed at EOF (Pitfall 5)
    - Persona lines ("your persona: ...") extracted with prefix stripped; dialogue lines split into exactly 4 tab-fields; turns = (user, assistant) pairs; candidates field ignored (D-04)
    - Every episode in the fixture has 3-5 persona lines and >=2 turn pairs; parser preserves order
  </behavior>
  <action>
    Create `src/personacore/dialogue/__init__.py` in the continual/__init__.py shape: one-paragraph
    docstring tagging DATA-01/DATA-02 and D-01/D-04/D-06, relative imports of the public surface
    (parse_episodes; detokenize/render_document/encode_dialogue arrive in Task 2 — add them to
    __all__ there), explicit __all__.

    Create `src/personacore/dialogue/parse.py` implementing `parse_episodes(path)` per RESEARCH
    Pattern 2 verbatim: `num, _, rest = line.rstrip("\n").partition(" ")`; flush current episode
    when `num == "1"` and buffers are non-empty; `rest.startswith("your persona: ")` -> persona
    line (strip the prefix); otherwise `rest.split("\t")` (exactly 4 fields — the verified corpus
    invariant, 0 exceptions in 73,520 lines) -> append (user, assistant), ignore candidates;
    flush-at-EOF for the final episode. Skip blank lines defensively. Stdlib only (from-scratch
    boundary — no third-party imports). Module docstring cites DATA-01, the verified corpus
    properties (8,939 train / 1,000 valid episodes, 4-field invariant), and Pitfall 5.

    Create `tests/fixtures/personachat_fb_fixture.txt`: 3 SYNTHETIC episodes that are
    format-faithful but NOT copied from the real corpus (D-00: PersonaChat has no explicit
    license — never re-host, not even snippets). Reproduce the verified format properties:
    numbered lines starting at 1 per episode; 3-5 `N your persona: <sentence>` lines with
    ATTACHED punctuation, including at least one line ending in the `!.` artifact; then
    dialogue lines `N user_utt\tassistant_reply\t\tcand1|cand2|cand3` with lowercase,
    space-separated punctuation ("hi , how are you ?"), zero apostrophes, pre-expanded
    contractions ("i am", "do not"); line numbers reset to 1 at each new episode; the final
    episode has no trailing boundary line.

    Create `tests/test_dialogue_parse.py` in the test_data_split.py style (module docstring
    naming DATA-01, "CPU-only, GPU-free", FIXTURE_PATH constant under tests/fixtures/, per-test
    one-line invariant comments). TDD order: write the tests first against the behavior list,
    watch them fail on import, then implement. Tests: episode count == 3; last-episode flush
    (the final episode's turns are present and complete); persona extraction (exact list for
    episode 1, prefix stripped, `!.` artifact preserved raw by the parser — normalization is
    the detokenizer's job); turn tuples exact for one episode; candidates absent from output.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_dialogue_parse.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/personacore/dialogue/parse.py` contains `def parse_episodes(` and imports nothing outside the stdlib
    - `tests/fixtures/personachat_fb_fixture.txt` exists, contains `your persona: ` and at least one `!.` line, contains no apostrophe character
    - `tests/test_dialogue_parse.py` asserts episode count == 3 and exact turn tuples for at least one episode
    - `.venv/bin/python -m pytest tests/test_dialogue_parse.py -x -q` exits 0
    - `grep -c "import" src/personacore/dialogue/parse.py` shows only stdlib imports (no numpy/torch)
  </acceptance_criteria>
  <done>Parser + fixture + tests committed; parse tests green; package importable as `from personacore.dialogue import parse_episodes`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: serialize.py — detokenizer (D-06) + render_document (D-04) + encode_dialogue with mask (D-01) + tests</name>
  <files>src/personacore/dialogue/serialize.py, src/personacore/dialogue/__init__.py, tests/test_dialogue_serialize.py</files>
  <read_first>
    - src/personacore/tokenizer/special.py (the LOCKED SPECIAL_TOKENS registry — import ids from here, never retype; Don't-Hand-Roll rule)
    - .planning/phases/11-conversational-data-pipeline/11-RESEARCH.md (Pattern 3 — span-wise encode_dialogue with the two mask subtleties; anti-patterns list)
    - tests/test_tokenizer_special.py (atomicity + round-trip test idiom for special tokens)
    - tests/test_data_split.py lines 20-30 (from_json load of artifacts/tokenizer.json in tests)
    - src/personacore/dialogue/parse.py (created in Task 1 — episode data shape)
  </read_first>
  <behavior>
    - detokenize: "i am fine , thanks !" -> "i am fine, thanks!" (space-before-punct closed); "do n't" -> "don't", "i ' m" and "' m" forms -> "i'm" (contraction rejoin — no-op on the primary corpus, covers the D-05 JSON fallback); "houses !." -> "houses!" (persona `!.` artifact normalized); text stays lowercase (no truecasing)
    - render_document(persona, turns): "<|system|>" + newline-joined persona + per-turn "<|user|>" + utt + "<|assistant|>" + reply, NO space after role tokens (Pitfall 4 — matches the research smoke), NO trailing eos marker in the string form
    - encode_dialogue(tok, persona, turns) returns (ids, mask) of equal length with EXACTLY: [SYSTEM]=0, persona ids=0, first [USER]=0, every subsequent [USER]=1, user-content ids=0, [ASSISTANT]=0, assistant-content ids=1, final [EOS]=1
    - Role ids 8185/8186/8187 and eos 8184 appear in ids as literal appended ids (content spans encoded with plain tok.encode(text), no allowed_special)
    - Frozen-tokenizer atomicity: tok.encode("a<|user|>b", allowed_special="all") contains id 8185 exactly once; decode round-trips the marker (same for 8186, 8187)
  </behavior>
  <action>
    Create `src/personacore/dialogue/serialize.py` with three functions. `detokenize(text)`:
    minimal from-scratch `re` rules per D-06 — rejoin split contractions (" n't" attaching to
    the preceding word; apostrophe-suffix forms 'm/'s/'re/'ve/'ll/'d with optional spaces
    around the quote), close space before `.,!?;:`, and normalize the `!.` persona artifact to
    `!` (resolves RESEARCH Open Question 2 — pinned in the fixture). `render_document(persona,
    turns)`: the D-04 string form used for word-count denominators — role-token markers with no
    trailing space, persona newline-joined, applied to already-detokenized text by callers.
    `encode_dialogue(tok, persona, turns)`: RESEARCH Pattern 3 verbatim — an inner
    `emit(token_ids, m)` helper extending parallel ids/mask lists; ids for SYSTEM/USER/
    ASSISTANT/EOS come from `SPECIAL_TOKENS` lookups (import from
    `personacore.tokenizer.special`), appended literally; content spans use plain
    `tok.encode(detokenize(text))` — encode_dialogue applies detokenize itself so every caller
    (gate AND bins) gets identical tokenization (Pitfall 4). Mask per D-01 exactly as in the
    behavior list. FORBIDDEN (anti-patterns): encoding the whole rendered string with
    allowed_special="all" and searching for role-token positions; any new id constants.

    Update `dialogue/__init__.py` __all__ to export detokenize, render_document, encode_dialogue.

    Create `tests/test_dialogue_serialize.py` (TDD order: tests first). Load the FROZEN
    tokenizer via `from_json("artifacts/tokenizer.json")` in a module fixture (the production
    registry is what matters — never a freshly trained tokenizer). Tests: (a) detok rules on
    format-faithful lines matching the committed fixture's style — spaced punctuation, `!.`
    line, plus JSON-fallback contraction forms ("do n't", "i ' m"); (b) role-token atomicity +
    decode round-trip for ids 8185-8187 in the test_tokenizer_special.py idiom; (c)
    encode_dialogue on a tiny 2-turn episode: assert len(ids) == len(mask), assert the exact
    mask value at each role-token position (first USER=0, second USER=1, ASSISTANT=0, EOS=1),
    assert ids contains each role id at the expected ordinal positions, assert every
    assistant-content id has mask 1 and every user-content/persona id has mask 0; (d)
    render_document output contains "<|system|>" followed immediately by the first persona
    character (no space) and role markers in turn order.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_dialogue_serialize.py tests/test_dialogue_parse.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `serialize.py` contains `def detokenize(`, `def render_document(`, `def encode_dialogue(` and the line `from personacore.tokenizer.special import` (no retyped id literals for role tokens in encode paths)
    - `tests/test_dialogue_serialize.py` asserts a mask value of 0 for the FIRST <|user|> position and 1 for a subsequent <|user|> position and 1 for the eos position (the three Pitfall-14 edge tokens)
    - `.venv/bin/python -m pytest tests/test_dialogue_serialize.py tests/test_dialogue_parse.py -x -q` exits 0
    - `make test` exits 0 (full suite stays green — 222 passed / 1 skipped baseline plus new tests)
  </acceptance_criteria>
  <done>serialize.py exports the single source of truth for detok/render/encode+mask; all serialize + parse tests green; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none in this plan | inputs are committed synthetic fixtures and the trusted frozen tokenizer artifact; no network, no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Tampering | parse.py consuming corpus text | accept (this plan) | real-corpus input is checksum-gated upstream in plan 11-03 before parse.py ever sees it; parser hard-fails on malformed lines (4-field split raises) |
| T-11-SC | Tampering | package installs | accept | no packages installed this phase (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_dialogue_parse.py tests/test_dialogue_serialize.py -x -q` green
- `make test` green (no regressions in the 222-test baseline)
- `make lint` green (ruff check + format)
</verification>

<success_criteria>
- `from personacore.dialogue import parse_episodes, detokenize, render_document, encode_dialogue` works in the .venv
- Mask semantics pinned by tests at all three D-01 edge tokens (first <|user|>, subsequent <|user|>, eos)
- Committed fixture contains zero real corpus lines (D-00 no-re-host)
</success_criteria>

<output>
Create `.planning/phases/11-conversational-data-pipeline/11-01-SUMMARY.md` when done.
</output>
