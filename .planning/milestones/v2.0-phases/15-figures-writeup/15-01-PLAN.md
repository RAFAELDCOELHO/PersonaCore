---
phase: 15-figures-writeup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phase15_stats.py
  - tests/test_phase15_stats.py
autonomous: true
requirements: [VIZ-03]
must_haves:
  truths:
    - "The correlation rule, its seed, its predicted sign, and its gate are committed BEFORE any correlation number exists anywhere in the repo"
    - "Spearman rho uses average (tie-corrected) ranks and matches the Wikipedia canonical value exactly"
    - "The same seed produces byte-identical permutation p and bootstrap CI across two calls"
    - "The gate returns MISS for a positive rho whose CI spans zero"
    - "The verdict section renderer exists and produces its markdown before it has ever seen real data"
  artifacts:
    - path: "scripts/phase15_stats.py"
      provides: "Pre-registered rule + pure-numpy rank statistics + gate + verdict renderer"
      contains: "PRE-REGISTRATION"
      min_lines: 180
    - path: "tests/test_phase15_stats.py"
      provides: "Known-answer, determinism, behavioral and gate-logic tests"
      min_lines: 90
  key_links:
    - from: "tests/test_phase15_stats.py"
      to: "scripts/phase15_stats.py"
      via: "importlib.util.spec_from_file_location"
      pattern: "spec_from_file_location"
---

<objective>
Commit the pre-registered decision rule for the "EWC dodges high-Fisher coordinates" claim —
the statistic, the seed, the resample counts, the predicted sign, the pass/miss gate, and the
markdown renderer that will later report the verdict — **before any correlation number exists**.

Purpose: D-09 requires git history itself to be the pre-registration proof, exactly as
`scripts/finetune_ab.py @ c3d942e` was committed before either A/B arm ran. Phase 15 research
deliberately did NOT compute the correlation, so the outcome is genuinely open. This plan is
the HARD BOUNDARY: nothing in this phase may read a delta grid or a Fisher grid until this
commit lands.

Output: `scripts/phase15_stats.py` (rule + statistics + gate + renderer) and
`tests/test_phase15_stats.py` (four known-answer/determinism/behavior/gate tests), landing in
ONE commit that contains no Phase 15 measurement of any kind.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-figures-writeup/15-CONTEXT.md
@.planning/phases/15-figures-writeup/15-RESEARCH.md
@.planning/phases/15-figures-writeup/15-PATTERNS.md
</context>

<critical_ordering_constraint>
**This plan's commit MUST precede any run of `scripts/extract_deltas.py` (Plan 15-02).**

Do NOT, at any point in this plan:
- read any file under `checkpoints/`
- read `results/phase15_norms.json` (it does not exist yet and must not be created here)
- compute, print, or estimate the Fisher/Δ correlation on real data

Every test in this plan uses synthetic fixtures. The module's `main()` reads the artifact at
runtime; at this commit that read fails loudly because the artifact does not exist. That failure
is the correct state.
</critical_ordering_constraint>

<tasks>

<task type="auto">
  <name>Task 1: Pre-registration constants, tie-correct rank statistics, and the D-11 gate</name>
  <files>scripts/phase15_stats.py</files>
  <read_first>
    - `scripts/finetune_ab.py` lines 1-25 (module docstring shape) and 66-116 (the
      `# ===== PRE-REGISTRATION (...) =====` block header, the per-constant provenance comment
      register, and `ewc_mitigates` as a pure gate function with the boundary spelled out)
    - `src/personacore/continual/fisher.py` lines 14-16, 40-60, 100-110, 160-170 — the fp64
      discipline, the "scipy is NOT a dependency" justification sentence, the
      `_SPEARMAN_METHOD` / `_VARIANT` method-string-in-the-record convention, and the
      `rng = np.random.default_rng(seed)  # LOCAL generator` idiom. **`_spearman` there is
      ORDINAL and is the WRONG transform for this module — read it to copy its discipline and
      to write the divergence note, never to reuse it.**
    - `.planning/phases/15-figures-writeup/15-RESEARCH.md` lines 556-692 — the validated
      `_rank` / `spearman` / `permutation_p` / `bootstrap_ci` recipe, its validation evidence
      table, its measured runtimes, and the D-11 sub-ambiguity section
    - `src/personacore/lora/config.py` line 16 — `TARGET_PROJECTIONS` order
    - `src/personacore/config.py` line 89 — `n_layer: int = 6`
    - `pyproject.toml` — confirm `numpy~=2.4`, no scipy, ruff `line-length 100`,
      `select = ["E","F","W","I"]`
  </read_first>
  <action>
Create `scripts/phase15_stats.py` following the `scripts/finetune_ab.py` register.

**Module docstring** in the fixed shape from 15-PATTERNS.md `## Shared Patterns`: (1) one-line
purpose naming VIZ-03 and D-09/D-10/D-11/D-12; (2) what it reads (`results/phase15_norms.json`)
and what it does NOT read (no checkpoints, no torch); (3) an indented input→output map;
(4) the decision-numbered constraints it honors; (5) a
`Run: ``python scripts/phase15_stats.py`` (inside the Python 3.11 venv)` line.

The docstring MUST carry a paragraph naming the divergence from
`src/personacore/continual/fisher.py::_spearman`: that module's transform is ordinal
double-argsort with no tie averaging and returns `1.0` on the fixture `a=[1,1,2,3], b=[1,2,3,4]`
where the correct answer is `0.9486832980505139`; this module uses average ranks; the two must
NOT be unified by a future reader. No `SECURITY:` paragraph is needed — this module performs no
deserialization.

**Import block:** `json`, `pathlib`, `numpy as np` only. Never `torch`, never `scipy`.
`_REPO_ROOT = pathlib.Path(__file__).resolve().parent.parent`;
`NORMS_JSON = _REPO_ROOT / "results" / "phase15_norms.json"`.

**Pre-registration block**, opened with a banner comment in `finetune_ab.py:66`'s exact form —
`# ===== PRE-REGISTRATION (D-09..D-12, locked before any Phase-15 correlation exists) =====` —
followed by the "committed BEFORE the artifact exists; git history order is the pre-registration
proof" sentence. Every constant carries its own comment stating which decision locked it and what
it is NOT:

  - `N_CELLS = 36` — 6 layers x 6 projections (`config.py:89`, `lora/config.py:16`); the same
    cells the figure draws, per D-10's per-block granularity.
  - `PROJECTIONS = ("q_proj", "k_proj", "v_proj", "c_proj", "fc_in", "fc_out")` — pins the cell
    iteration order so the correlation is reproducible; copy the order from
    `src/personacore/lora/config.py:16` exactly.
  - `PREDICTED_SIGN = 1` — D-10: POSITIVE. A negative or near-zero result is reported as plainly
    as a positive one and is NOT re-interpreted after the fact.
  - `PAIRING = "fisher_mean_per_cell vs (naive_ratio - ewc_ratio)"` — D-10: the Δ-reduction
    pairing, using both arms to isolate the penalty's effect.
  - `SEED = 1337` — the project's established seed (`finetune_ab.py`, `phase14_recall.py`);
    a LOCAL `np.random.default_rng` generator only, global RNG untouched.
  - `N_PERM = 100_000` (measured 1.4 s at n=36) and `N_BOOT = 10_000` (measured 0.4 s) —
    Claude's-discretion values under D-12, pinned here so the verdict is reproducible.
  - `CI_ALPHA = 0.05` — two-sided 95% percentile interval.
  - `SPEARMAN_METHOD = "average_rank_pearson_fp64"` and
    `CI_METHOD = "percentile_bootstrap"` — the `fisher.py` method-string-in-the-record
    convention; both strings are written into the verdict section.

**The R5 gate-arbitration sentence — this is the load-bearing addition and MUST be a
module-level comment inside the pre-registration block, committed here and never edited later:**
D-12 asks for both a permutation p and a bootstrap CI, and at n=36 they can disagree. D-11 read
literally makes the **bootstrap CI the LOAD-BEARING half of the gate** (gate = positive sign AND
CI excluding zero) and the **permutation p purely DESCRIPTIVE** — the p never overrides the CI
and never converts a miss into a pass. State this explicitly.

**The percentile-bootstrap honesty note**, also a pre-registration comment: the percentile
bootstrap is known to be biased and anti-conservative at small n; BCa would correct it at real
complexity cost; percentile is chosen for D-12's "~15 lines" and the known small-n bias is named
here rather than silently omitted or silently upgraded.

**Statistics functions**, transcribed from 15-RESEARCH.md lines 581-649 (already validated to
1 ulp against scipy and exactly against the Wikipedia canonical value):
  - `_rank(x)` — average/fractional ranks, fp64, stable argsort, explicit tie-averaging loop.
    Its docstring carries the "ordinal ranks silently break ties by input order, which inflates
    rho; the D-05 artifact rounds for readability, so ties are manufacturable" rationale.
  - `spearman(a, b)` — `float(np.corrcoef(_rank(a), _rank(b))[0, 1])`. Docstring carries D-12's
    "Spearman over Kendall on readability grounds — both are rank-based and therefore already
    robust to the heavy-tailed Fisher magnitudes, so there is no edge-case advantage that would
    justify trading away readability" rationale.
  - `permutation_p(a, b, *, n_perm, seed)` — rank ONCE, shuffle the ranks, two-sided on `abs`,
    add-one correction `(ge + 1) / (n_perm + 1)` so p is never reported as exactly 0. Returns
    `(obs, p)`. Docstring carries D-12's "resampling over closed-form Fisher-z because z's
    normality assumption is the shakiest option at n = 36" rationale.
  - `bootstrap_ci(a, b, *, n_boot, seed, alpha=CI_ALPHA)` — resample PAIRS with replacement,
    re-rank inside each resample, drop degenerate (zero-variance) resamples as `nan` and RETURN
    the dropped count rather than letting a nan propagate into a quantile. Returns
    `(lo, hi, n_degenerate)`.

**The D-11 gate as a pure function**, copying `ewc_mitigates`' shape including the boundary
sentence: `def ewc_dodges_high_fisher(rho, ci_lo, ci_hi):` returning
`rho > 0 and ci_lo > 0`. Its docstring states: the gate is positive sign AND a CI excluding zero;
the boundary is a FAIL (`ci_lo == 0.0` returns False); the coefficient's MAGNITUDE and the
permutation p are DESCRIPTIVE with NO gate; and a rho that is positive but whose CI spans zero is
a MISS reported in the "suggestive but not statistically demonstrated at n = 36" register —
reported, not discarded, and not softened into a passing verdict.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import importlib.util,pathlib;s=importlib.util.spec_from_file_location('p15s',pathlib.Path('scripts/phase15_stats.py'));m=importlib.util.module_from_spec(s);s.loader.exec_module(m);print(m.SEED,m.N_PERM,m.N_BOOT,m.PREDICTED_SIGN,m.N_CELLS);print(m.ewc_dodges_high_fisher(0.15,-0.1,0.4), m.ewc_dodges_high_fisher(0.45,0.11,0.70))"</automated>
  </verify>
  <acceptance_criteria>
    - The command above prints `1337 100000 10000 1 36` then `False True`.
    - `grep -qF 'PRE-REGISTRATION' scripts/phase15_stats.py` succeeds.
    - `grep -c 'import torch' scripts/phase15_stats.py` returns 0.
    - `grep -c 'scipy' scripts/phase15_stats.py` returns 0 except inside the docstring
      justification; verify with `grep -n scipy scripts/phase15_stats.py` that every hit is a
      comment or docstring line, never an import.
    - `.venv/bin/python -c "import importlib.util,pathlib;s=importlib.util.spec_from_file_location('m',pathlib.Path('scripts/phase15_stats.py'));m=importlib.util.module_from_spec(s);s.loader.exec_module(m);print(abs(m.spearman([106,86,100,101,99,103,97,113,112,110],[7,0,27,50,28,29,20,12,6,17]) - (-0.17575757575757575)) < 1e-15)"` prints `True`.
    - `.venv/bin/python -c "import importlib.util,pathlib;s=importlib.util.spec_from_file_location('m',pathlib.Path('scripts/phase15_stats.py'));m=importlib.util.module_from_spec(s);s.loader.exec_module(m);print(abs(m.spearman([1,1,2,3],[1,2,3,4]) - 0.9486832980505139) < 1e-12)"` prints `True` (this is the value `fisher.py::_spearman` gets wrong).
    - The module contains the R5 arbitration statement: `grep -qi 'load-bearing' scripts/phase15_stats.py` succeeds and the surrounding comment names the bootstrap CI as load-bearing and the permutation p as descriptive.
    - `make lint` passes.
    - No file under `checkpoints/` was read and `results/phase15_norms.json` does not exist.
  </acceptance_criteria>
  <done>The rule, seed, sign, resample counts, gate and R5 arbitration are committed as literals; Spearman is tie-correct and matches both canonical values; nothing has computed a real correlation.</done>
</task>

<task type="auto">
  <name>Task 2: Artifact reader and the dated verdict-section renderer</name>
  <files>scripts/phase15_stats.py</files>
  <read_first>
    - `scripts/phase15_stats.py` (current state after Task 1)
    - `scripts/phase14_recall.py` lines 1590-1643 — `SHIP_DECISION_HEADER`'s separate/dated/
      does-not-reopen-or-amend wording register, and the CR-02 section-anchored guard
    - `scripts/_verdict.py` — the ONE copy of the anchored `## Verdict` section read; the CR-02
      failure mode it documents
    - `results/phase13_ab_report.md` lines 24-47 (`## Pre-Registration` table form) and
      lines 358-372 (`## Evidence Index`, the append point and its row form)
    - `.planning/phases/15-figures-writeup/15-RESEARCH.md` lines 920-941 (the D-05 artifact's
      required content) and lines 674-692 (the R5 sub-ambiguity)
  </read_first>
  <action>
Extend `scripts/phase15_stats.py` with the artifact reader and the verdict renderer. Both are
committed here, before the artifact exists — so git history proves not only the rule but the
exact rendering was fixed in advance.

**`load_pairs(artifact)`** — takes the parsed `results/phase15_norms.json` dict and returns
`(fisher_values, reduction_values)`, two length-36 fp64 lists in a PINNED cell order: outer loop
`layer` 0..5, inner loop over `PROJECTIONS` in its declared order. `fisher_values[i]` is
`artifact["blocks"]["fisher"]["cells"][str(layer)][projection]`; `reduction_values[i]` is
`naive_cell - ewc_cell` for the same coordinate, read from the `naive` and `ewc` blocks. Fail
loud with `raise SystemExit(f"[phase15_stats] ...")` naming the offending block/coordinate if any
of the four blocks is absent, if any block does not carry exactly 36 cells, or if the projection
set of any layer differs from `PROJECTIONS`. Use the `plot_phase13.py:88-97` register — the error
names the offending file and key, never just the shape.

**`render_verdict_section(rho, p, ci_lo, ci_hi, n_degenerate, artifact, today)`** — returns the
complete markdown block that Plan 15-04 appends to `results/phase13_ab_report.md`. It MUST
contain, in this order:

  1. A horizontal rule, then the heading
     `## Phase 15 Addendum — Fisher/Δ Correlation Verdict (D-09/D-10/D-11)`.
  2. An HTML comment in `SHIP_DECISION_HEADER`'s exact register stating that this section is
     **Phase 15 material, dated after every Phase 13 result above, and does not reopen or amend
     Phase 13's pre-registered content**. Model the wording on `phase14_recall.py:1590-1598`.
  3. A bold dated line `**Recorded: {today}** — Phase 15 material appended to Phase 13 evidence.`
  4. A `### Pre-Registration (locked before the artifact existed)` table with one row per
     pre-registered constant: statistic, granularity/`N_CELLS`, `PAIRING`, `PREDICTED_SIGN`,
     `SEED`, `N_PERM`, `N_BOOT`, `CI_ALPHA`, `SPEARMAN_METHOD`, `CI_METHOD`, and the gate rule
     text. Plus the R5 sentence verbatim: the bootstrap CI is load-bearing, the permutation p is
     descriptive and never overrides it. Plus the percentile-bootstrap small-n bias note.
  5. A `### Result` block reporting rho, the CI, the permutation p, `n_degenerate`, and the
     source artifact's `git_sha` and `built` fields.
  6. A `### Verdict` line reading either `**GATE PASSES**` or `**GATE MISSES**` computed from
     `ewc_dodges_high_fisher`. On a MISS the renderer emits the D-11 miss policy verbatim: the
     result is recorded unamended, reported in the **"suggestive but not statistically
     demonstrated at n = 36"** register, is neither discarded nor softened into a passing
     verdict, and ROADMAP SC2's *"showing EWC visibly dodging high-Fisher coordinates"* wording
     narrows accordingly. On a PASS it states that the magnitude remains descriptive: *"the sign
     is the falsifiable claim; the magnitude is reported honestly given n = 36 and is not itself
     pass/fail."* Both branches are written NOW, before either is known to apply.
  7. An `### Evidence Index Addendum` one-row table in the same form as the existing
     `## Evidence Index` rows, citing `scripts/phase15_stats.py` @ its pre-registration commit
     and `results/phase15_norms.json`.

  **CR-02 constraint:** this section's prose quotes Phase 13 heading strings (`## Verdict`,
  `## Gate Verdict`). Any guard that reads a section of this report MUST be section-anchored via
  `scripts/_verdict.py::VERDICT_SECTION`, never `split("## Verdict")[-1]`. State that in the
  renderer's docstring so a future guard author sees it.

**`main()`** — reads `NORMS_JSON` (fail loud with a `raise SystemExit` naming the path and
telling the operator to run `scripts/extract_deltas.py` first if it is absent), calls
`load_pairs`, computes `spearman`/`permutation_p`/`bootstrap_ci` with the pre-registered
constants, and prints `render_verdict_section(...)` to stdout. Guard it with
`if __name__ == "__main__":` so an `importlib` load from a test renders nothing.
`today` defaults to `time.strftime("%Y-%m-%d", time.gmtime())`.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import importlib.util,pathlib;s=importlib.util.spec_from_file_location('m',pathlib.Path('scripts/phase15_stats.py'));m=importlib.util.module_from_spec(s);s.loader.exec_module(m);t=m.render_verdict_section(0.15,0.31,-0.1,0.4,0,{'git_sha':'deadbeef','built':'2026-08-02'},'2026-08-02');print('GATE MISSES' in t, 'suggestive but not statistically demonstrated at n = 36' in t, 'Phase 15 Addendum' in t)"</automated>
  </verify>
  <acceptance_criteria>
    - The command above prints `True True True`.
    - The same call with `(0.45, 0.001, 0.11, 0.70)` yields a string containing `GATE PASSES` and
      the phrase `the sign is the falsifiable claim`.
    - `.venv/bin/python scripts/phase15_stats.py` exits non-zero with a message beginning
      `[phase15_stats]` and naming `results/phase15_norms.json` — the correct state at this commit.
    - `grep -qF 'does not reopen or amend' scripts/phase15_stats.py` succeeds.
    - `grep -qF '_verdict' scripts/phase15_stats.py` succeeds (the CR-02 note names the shared
      anchored-section helper).
    - `make lint` passes.
  </acceptance_criteria>
  <done>The reader and both verdict branches render correctly from synthetic inputs; `main()` fails loudly and correctly because no artifact exists yet.</done>
</task>

<task type="auto">
  <name>Task 3: Known-answer, determinism, behavior and gate tests</name>
  <files>tests/test_phase15_stats.py</files>
  <read_first>
    - `scripts/phase15_stats.py` (final state after Tasks 1-2)
    - `tests/test_phase14_scoring.py` lines 1-54 — the docstring numbered-behavior-inventory
      register, the scripts-load justification paragraph for a pre-registration module, and the
      `importlib.util.spec_from_file_location` loader
    - `tests/test_phase13_plots.py` lines 1-32 — the same loader in its shortest form
    - `.planning/phases/15-figures-writeup/15-RESEARCH.md` lines 651-661 (the validation
      evidence table this test file pins) and lines 1441-1447 (Pitfall 4: perfect monotone
      returns `0.9999999999999999`, not exactly `1.0`)
    - `.planning/phases/15-figures-writeup/15-VALIDATION.md` — the four pytest node IDs this
      file must define, exactly as spelled there
  </read_first>
  <action>
Create `tests/test_phase15_stats.py`. CPU-only, GPU/MPS-free, **no torch, no checkpoint I/O, no
artifact**. Every fixture is synthetic and inline.

Module docstring in the `tests/test_phase14_scoring.py:1-27` register: opens with `VIZ-03 /
D-09..D-12`, then `CPU-only, GPU/MPS-free, no torch, no artifact`, then a numbered inventory
naming each test and the one-line failure it prevents, then the scripts-load justification —
the pre-registered constants and the gate MUST live in the committed driver for git history to
be the pre-registration proof (D-09), so the test `importlib`-loads `scripts/phase15_stats.py`
rather than duplicating the rules in the package; `main()` is `__main__`-guarded and every rule
is a module-level pure function or constant, so the load runs nothing.

Module-level loader `_load_stats()` + `st = _load_stats()`, copied from
`tests/test_phase13_plots.py:23-32` with the path changed to `scripts/phase15_stats.py`.

Define **exactly these four test functions**, spelled as in 15-VALIDATION.md:

  - `test_spearman_known_answers` — pins four values from the research validation table:
    the Wikipedia canonical IQ/TV example equals `-0.17575757575757575` to within `1e-15`;
    the tied fixture `a=[1,1,2,3], b=[1,2,3,4]` equals `0.9486832980505139` to within `1e-12`
    (**with a comment naming this as the exact case `fisher.py::_spearman` returns `1.0` for**);
    perfect monotone increasing is `pytest.approx(1.0, abs=1e-12)` and decreasing is
    `pytest.approx(-1.0, abs=1e-12)` — never `== 1.0`, per Pitfall 4.
    Also assert `_rank([1,1,2,3]) == [0.5, 0.5, 2.0, 3.0]` so the tie transform itself is pinned,
    not just its downstream effect.

  - `test_seeded_results_are_reproducible` — on a fixed synthetic 36-point pair, two calls to
    `permutation_p` with the same seed return equal `(obs, p)` tuples, and two calls to
    `bootstrap_ci` with the same seed return equal `(lo, hi, n_degenerate)` tuples. Use small
    `n_perm`/`n_boot` (e.g. 2000/1000) so the test stays under the 10 s feedback budget while
    still proving seeding. Additionally assert a DIFFERENT seed changes the result, so the test
    cannot pass by the functions ignoring randomness entirely.

  - `test_ci_behavior_on_null_and_signal` — on independent data (seeded `default_rng` draw,
    n=36) the CI spans zero (`lo < 0 < hi`); on strong monotone data the CI excludes zero
    (`lo > 0`). This is the behavioral proof that the CI can distinguish the two cases at all.

  - `test_gate_rule` — `ewc_dodges_high_fisher(0.15, -0.1, 0.4)` is `False` (positive rho, CI
    spans zero → MISS, the exact D-11 provenance case); `(0.45, 0.11, 0.70)` is `True`;
    `(-0.6, -0.9, -0.2)` is `False` (CI excludes zero but the sign is wrong);
    `(0.30, 0.0, 0.55)` is `False` (**boundary is a FAIL**). Also assert
    `st.PREDICTED_SIGN == 1`, `st.SEED == 1337`, `st.N_PERM == 100_000`,
    `st.N_BOOT == 10_000`, `st.N_CELLS == 36` and
    `st.PROJECTIONS == ("q_proj","k_proj","v_proj","c_proj","fc_in","fc_out")` — the exact
    pre-registered literals, in the `test_preregistration_constants` register.

Bare `assert` is correct here — the `raise SystemExit`-never-`assert` rule applies to `scripts/`
only.
  </action>
  <verify>
    <automated>.venv/bin/pytest -q tests/test_phase15_stats.py</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest -q tests/test_phase15_stats.py` reports 4 passed, 0 failed, in under 10 s.
    - All four node IDs from 15-VALIDATION.md resolve:
      `.venv/bin/pytest -q tests/test_phase15_stats.py::test_spearman_known_answers tests/test_phase15_stats.py::test_seeded_results_are_reproducible tests/test_phase15_stats.py::test_ci_behavior_on_null_and_signal tests/test_phase15_stats.py::test_gate_rule` reports 4 passed.
    - `grep -c 'import torch' tests/test_phase15_stats.py` returns 0.
    - `.venv/bin/pytest -q` reports at least 396 passed (392 baseline + 4 new), 1 skipped.
    - `make lint` passes.
  </acceptance_criteria>
  <done>Four tests green, the pre-registered literals are pinned by assertion, and no real Phase 15 data has been touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → `phase15_stats.main()` | `results/phase15_norms.json` is parsed as structured input; a malformed or truncated artifact must not silently produce a plausible verdict |
| git history → the pre-registration claim | The claim "the rule predates the number" is only as strong as commit ordering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-08 | Repudiation | `scripts/phase15_stats.py` commit ordering | mitigate | This plan's commit contains no measurement of any kind; `main()` provably cannot run because `results/phase15_norms.json` does not exist at this commit. Plan 15-02 is a strictly later commit. Git history is the audit trail, exactly as `finetune_ab.py @ c3d942e`. |
| T-15-07 | Tampering | `load_pairs` parsing the JSON artifact | mitigate | ASVS V5: validate 4 blocks x 36 cells and the exact projection set per layer; `raise SystemExit` naming the offending block/coordinate on any deviation, never a silent default or a partial correlation. |
| T-15-09 | Tampering | The gate resolved after seeing the number | mitigate | Both PASS and MISS branches of `render_verdict_section` are authored and committed before either is known to apply; the R5 CI-is-load-bearing arbitration is a committed literal, closing the "resolve it in whichever direction looks better" hazard one level deeper than D-11 already did. |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | This phase installs **zero** packages. `numpy 2.4.6` is already a declared core dependency in `pyproject.toml` and already present in `.venv`. No registry lookup and no legitimacy checkpoint is required; reaching for a package here (notably scipy) would itself be the drift signal. |
</threat_model>

<verification>
- `.venv/bin/pytest -q tests/test_phase15_stats.py` → 4 passed
- `.venv/bin/pytest -q` → ≥ 396 passed, 1 skipped (no regression against the 392 baseline)
- `make lint` → clean
- `test ! -f results/phase15_norms.json` → true (the artifact must NOT exist at this commit)
- `git log --oneline -1 -- scripts/phase15_stats.py` records a commit that touches no
  `results/phase15_*` file
</verification>

<success_criteria>
- `scripts/phase15_stats.py` and `tests/test_phase15_stats.py` are committed together, in a
  commit containing no Phase 15 measurement, no artifact, and no figure.
- Spearman is tie-correct: canonical `-0.17575757575757575` exact, tied fixture
  `0.9486832980505139`, and the divergence from `fisher.py::_spearman` is documented in the
  module docstring.
- `SEED=1337`, `N_PERM=100000`, `N_BOOT=10000`, `PREDICTED_SIGN=+1`, `N_CELLS=36` are committed
  literals pinned by test.
- The D-11 gate returns MISS for `(rho=+0.15, CI=(-0.1, 0.4))` and for the `ci_lo == 0.0`
  boundary; PASS only for positive rho with `ci_lo > 0`.
- The R5 arbitration (bootstrap CI load-bearing, permutation p descriptive) is a committed
  comment, not a runtime decision.
- Both verdict branches, including the D-11 verbatim miss policy, render before either is known
  to apply.
</success_criteria>

<output>
Create `.planning/phases/15-figures-writeup/15-01-SUMMARY.md` when done.
Record the commit SHA of the pre-registration commit — Plan 15-04 cites it in the Evidence Index
addendum row.
</output>
