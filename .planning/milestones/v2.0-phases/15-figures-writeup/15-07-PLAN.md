---
phase: 15-figures-writeup
plan: 07
type: execute
wave: 4
depends_on: ["15-03", "15-04"]
files_modified:
  - demo_v2.ipynb
  - demo.ipynb
  - .planning/ROADMAP.md
autonomous: true
requirements: [DOC-02]
must_haves:
  truths:
    - "A new v2.0 notebook runs standalone from a fresh clone without the v1.0 notebook's artifacts"
    - "Both notebooks state their mutual independence explicitly in their opening cells"
    - "The v1.0 notebook's existing cells are byte-unchanged — the independence statement is a PREPENDED new cell"
    - "The v2.0 notebook re-cites committed numbers and never recomputes them"
    - "All three DOC-02 honest-number categories appear: recall against its gate, the retention delta, and the tokenizer-inflation tax"
    - "The notebook names the Fisher estimator by its exact variant string, read from the artifact (ROADMAP SC3)"
    - "ROADMAP records that SC3's 'demo.ipynb is updated' wording is superseded by a new notebook, rather than silently absorbing the change"
  artifacts:
    - path: "demo_v2.ipynb"
      provides: "Milestone 2 evidence notebook — self-contained, re-cites committed numbers"
      contains: "re-cited, never recomputed"
    - path: "demo.ipynb"
      provides: "v1.0 notebook plus a prepended independence statement, every original cell untouched"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 15 entry with the recorded SC3 supersession"
  key_links:
    - from: "demo_v2.ipynb"
      to: "results/phase15_norms.json"
      via: "reads the committed artifact to render the figures narrative"
      pattern: "phase15"
---

<objective>
Ship a new, self-contained v2.0 evidence notebook; prepend an independence statement to the v1.0
notebook without touching any of its existing cells; and record in ROADMAP that SC3's wording is
superseded rather than silently reinterpreted.

Purpose: D-13 requires a **new** notebook, self-contained and runnable independently of the v1.0
one — no shared cell state, no inherited checkpoint dependency — so a reader can run either
standalone. Both notebooks say so in their opening cells, so nobody discovers it by hitting a
failure.

Output: `demo_v2.ipynb` (new), `demo.ipynb` (one prepended cell), `.planning/ROADMAP.md`
(supersession record).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-figures-writeup/15-CONTEXT.md
@.planning/phases/15-figures-writeup/15-RESEARCH.md
@.planning/phases/15-figures-writeup/15-PATTERNS.md
</context>

<resolved_decisions>
**R4 — the v1.0 notebook gets a PREPENDED cell, never an edit.**

CONTEXT's scope boundary excludes *"changes to `scripts/demo_app.py` or the v1.0 notebook's
existing cells"*, while D-13 requires **both** notebooks to state their independence in their
opening cells. Resolution: **prepend a NEW markdown cell** as `demo.ipynb`'s cell 0, pushing the
existing 8 cells down by one index. That satisfies D-13 literally and edits no existing cell, so
it violates CONTEXT's boundary in neither letter nor spirit.

**R4, second half — record the SC3 supersession explicitly.** ROADMAP SC3 says *"demo.ipynb is
updated with honest numbers"*. D-13 supersedes it: the honest v2.0 numbers go in a **new**
notebook, and `demo.ipynb` receives only the independence statement. **Record that supersession
explicitly** in ROADMAP, in the register `results/phase13_ab_report.md:303-305` uses for
"ROADMAP wording superseded" — **never silently absorb it**. A success criterion that quietly
changed meaning is indistinguishable from one that was not met.
</resolved_decisions>

<tasks>

<task type="auto">
  <name>Task 1: The v2.0 evidence notebook</name>
  <files>demo_v2.ipynb</files>
  <read_first>
    - `demo.ipynb` — all 8 cells. Copy the register, not the content: cell 0's title markdown that
      states the thesis then **what this notebook shows and what it does not claim**; cell 3's
      heading `## Headline evaluation (re-cited, never recomputed)` and its "the rigor lives in
      the audited script, not in an ad-hoc notebook cell" closing; cell 2's committed-CSV read
      with the comment explaining why the committed copy exists; cell 5's blockquote-a-committed-
      caveat pattern (*"From `results/results.md`, reproduced verbatim — the caveat is part of the
      result"*). Note `nbformat` 4, `nbformat_minor` 5, `kernelspec.name` `python3`,
      `language_info.version` `3.11.15`, and that outputs ship committed with `execution_count`
      set.
    - `results/phase13_ab_report.md` — `## 2×2 Result`, `## Gate Verdict`, and the
      `## Phase 15 Addendum` from Plan 15-04
    - `results/phase14_recall_report.md` — `## Recall Results — Core Tier` and the verdict
    - `results/inflation_report.md` — **read and use, do not merely cite**: the preamble
      (lines 8-9, the same-run-baseline-only qualifier), `## D-08 Metrics` metric 1
      (3.229 tokens/word over 4,800,385 / 1,486,754) and `## Baseline` (lines 30-31,
      TinyStories 2.860, ratio 1.129x). This is DOC-02's third honest-number category
    - `results/phase15_norms.json` — the artifact the notebook reads for the figures narrative
    - `.planning/phases/15-figures-writeup/15-03-SUMMARY.md` and `15-04-SUMMARY.md`
    - `.planning/phases/15-figures-writeup/15-05-PLAN.md` `<pinned_headings>` — the REPORT.md
      section headings to link to, pinned in plan text:
      `## Milestone 2 Results: What Three Experiments Showed`,
      `### The Two Signature Figures`, and
      `## Milestone 2 Limitations — Nine Honest Negatives, Quoted`.
      **Read the PLAN, not `15-05-SUMMARY.md`:** Plan 15-05 runs in this same wave and its
      SUMMARY may not exist yet. The headings are authored, not discovered.
  </read_first>
  <action>
Create `demo_v2.ipynb` — the Milestone 2 evidence artifact. `nbformat` 4 / `nbformat_minor` 5,
`kernelspec.name` `python3`, matching `demo.ipynb`'s metadata.

**Self-containment is the hard requirement (D-13).** The notebook must run top-to-bottom from a
fresh clone with **no shared cell state from `demo.ipynb`** and **no implicit checkpoint
dependency inherited from it**. Every input is a committed file under `results/` or the two
committed PNGs. If a cell would need a gitignored checkpoint, it does not belong here.

**Cell 0 — title markdown.** States the thesis and what this notebook shows and does not claim,
in `demo.ipynb` cell 0's register. It MUST carry the **independence statement** explicitly: this
is the Milestone 2 evidence artifact; it runs standalone and requires nothing from `demo.ipynb`;
`demo.ipynb` likewise runs standalone and requires nothing from this file; neither shares cell
state with the other.

**The re-cite principle, carried forward verbatim (D-13).** Use `demo.ipynb` cell 3's heading form
— `## ... (re-cited, never recomputed)` — and state that every number here is committed in
`results/` and re-cited **with its denominator**; this notebook never recomputes a headline
number, because the rigor lives in the audited script, not in an ad-hoc notebook cell.

**Content**, ordered as a reader would want it (cell count and exact ordering are Claude's
discretion):
  - The A/B result: naive **8.52417066884246** vs EWC **3.8911400839446597** retention PPL from
    the step-0 anchor **2.107553076833866**, with the drift figures and the margin multiple.
    Re-cited from `results/phase13_ab_report.md`, never recomputed.
  - The forgetting curve — display the committed `results/phase13_forgetting_curve.png`.
  - The teach-then-recall result: taught **496/1008 = 0.4921** vs **0.2486**, held-out
    **326/936 = 0.3483** vs **0.2000**, closed-book **0/2430**, verdict **ADAPT** — GO with two
    qualifications. Re-cited from `results/phase14_recall_report.md`. **Reproduce that verdict
    string exactly as `results/phase14_recall_report.md:575` carries it** — the literal is
    `**ADAPT** — GO with two qualifications.`, with the bold markers on `ADAPT` only, an em dash,
    and a trailing period. Do not re-emphasize the whole phrase and do not drop the period; the
    acceptance criterion below requires every number and verdict string to match its source.
  - **The tokenizer-inflation tax** — dialogue **3.229** tokens/word (4,800,385 utterance
    tokens over 1,486,754 whitespace words) against the TinyStories baseline **2.860**
    recomputed in the same run with the same tokenizer and the same word rule: a **1.129x**
    relative inflation, inside the ≤1.2x GO band at a measured fit of 0.9996. Re-cited from
    `results/inflation_report.md` (`## D-08 Metrics` metric 1, `## Baseline` lines 30-31),
    never recomputed. Re-cite its qualifier from that file's preamble (lines 8-9) **in the same
    breath as the number**: the ratio is **only meaningful against that same-run baseline** and
    is never comparable to another tokenizer. This is the third of the three honest-number
    categories ROADMAP SC3 and REQUIREMENTS DOC-02 name for this notebook — it is not optional.
  - The two v2.0 figures: display `results/phase15_adapter_delta.png` and
    `results/phase15_fisher_ewc.png`. A markdown cell alongside states, terse: which panels share
    a color scale and why the Fisher panel does not (units, not convenience), the specific
    layer/projection driving each range read from `results/phase15_norms.json`, and that VIZ-02 is
    **not comparable** to VIZ-03's panels — naming parameter count and training budget as the
    reasons. It MUST also **name the Fisher estimator by its exact variant string, verbatim
    from the artifact's `blocks.fisher.variant`** —
    `empirical_diag_fisher/groundtruth_targets/mean_normalized` — with its `n_examples` (2000)
    and `seed` (1234). ROADMAP SC3 names *the named Fisher variant* as one of this notebook's
    required disclosures, and the coarse `regime` label does not name it. One code cell may
    load `results/phase15_norms.json` to print the per-layer table **and the variant string**,
    so the notebook shows the numbers behind the pictures.
  - The correlation verdict — re-cited terse from `results/phase13_ab_report.md`'s
    `## Phase 15 Addendum`, with the pass/miss word and the rho/CI values from `15-04-SUMMARY.md`,
    linking to the full pre-registration table rather than restating it.
  - The honest-negatives cell, in `demo.ipynb` cell 5's register — blockquote a committed caveat
    verbatim with the preface that **the caveat is part of the result**. Quote at least the two
    that bound the headline numbers this notebook displays, and link to `docs/REPORT.md`'s
    Limitations section for all nine using the heading pinned in `15-05-PLAN.md`
    `<pinned_headings>` — `## Milestone 2 Limitations — Nine Honest Negatives, Quoted` — with a
    GitHub anchor derived from it. Plan 15-05 writes that heading in this **same wave**, so the
    anchor may still dangle when this plan finishes; Plan 15-08's wave-5
    `test_headline_numbers_match_sources` asserts it resolves against the finished
    `docs/REPORT.md`. Do not block on `docs/REPORT.md` here.

Notebook cells **may** call `plt.show()` — the `savefig`-only rule is a `scripts/` rule, not a
notebook rule.

**Execute the notebook and ship it with outputs committed**, `execution_count` set, matching
`demo.ipynb`'s convention of shipping executed. Verify it runs clean from a fresh kernel.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import json;nb=json.load(open('demo_v2.ipynb'));src=''.join(''.join(c['source']) for c in nb['cells']);v=json.load(open('results/phase15_norms.json'))['blocks']['fisher']['variant'];print(nb['nbformat']==4, len(nb['cells'])>=5, 're-cited, never recomputed' in src, 'phase15_fisher_ewc.png' in src, 'phase15_adapter_delta.png' in src, '0.3483' in src, '8.52417066884246' in src, all(s in src for s in ('3.229','2.860','1.129')), v in src, any(c['cell_type']=='code' and c.get('execution_count') for c in nb['cells']))"</automated>
  </verify>
  <acceptance_criteria>
    - The command above prints ten `True` values — including the three tokenizer-inflation
      numbers (`3.229` / `2.860` / `1.129`, DOC-02's third honest-number category) and ROADMAP
      SC3's named Fisher variant string, read from the artifact rather than retyped.
    - The inflation bullet carries its qualifier in the same cell: the ratio is only meaningful
      against the same-run TinyStories baseline and is never comparable to another tokenizer.
    - Cell 0 contains an explicit independence statement naming `demo.ipynb` and stating that
      neither notebook requires the other.
    - No gitignored checkpoint is read anywhere; every input is a committed file. Check the
      **code cells' `source` arrays only**, never `outputs`:
      `.venv/bin/python -c "import json;nb=json.load(open('demo_v2.ipynb'));print(sum('checkpoints/' in ''.join(c['source']) for c in nb['cells'] if c['cell_type']=='code'))"`
      returns `0`. A whole-file `grep -c 'checkpoints/' demo_v2.ipynb` is the **wrong** gate here:
      the permitted `results/phase15_norms.json` cell prints per-block `w0_source` /
      `source_ckpt` provenance fields whose values are `checkpoints/…` strings, so committed
      cell output would fail a whole-file grep on a notebook that never opened a checkpoint.
      Provenance strings echoed from a committed artifact are not checkpoint reads.
    - The notebook executes clean from a fresh kernel:
      `.venv/bin/jupyter nbconvert --to notebook --execute --stdout demo_v2.ipynb > /dev/null`
      exits 0. **Expected runtime ~20-40 s** — kernel start plus a handful of `json` /
      `matplotlib` cells; no training, no checkpoint read. This is one of two deliberate
      exceptions to 15-VALIDATION.md's 10 s per-task feedback budget, recorded there under
      `## Sampling Rate`; the task's own `<automated>` verify stays well under 10 s. Above
      ~90 s means a cell is doing work it should not. (If `jupyter` is unavailable in the venv,
      run each code cell's source in order via `.venv/bin/python` from the repo root and
      confirm no exception.)
    - Every number in the notebook matches its source report exactly.
    - `.venv/bin/pytest -q` shows no regression.
  </acceptance_criteria>
  <done>A self-contained executed v2.0 notebook that re-cites committed numbers, displays both signature figures with their terse disclosure, and states its independence from the v1.0 notebook.</done>
</task>

<task type="auto">
  <name>Task 2: Prepend the independence statement to the v1.0 notebook</name>
  <files>demo.ipynb</files>
  <read_first>
    - `demo.ipynb` — all 8 cells and the top-level metadata. The current cell order is:
      0 markdown (title), 1 code, 2 code, 3 markdown (`## Headline evaluation ...`), 4 code,
      5 markdown (ablation), 6 markdown (sampling tour), 7 code.
    - `.planning/phases/15-figures-writeup/15-CONTEXT.md` — D-13's both-notebooks-state-this
      clause, and the `<domain>` scope boundary excluding changes to the v1.0 notebook's
      **existing** cells
  </read_first>
  <action>
**Prepend a single new markdown cell as `demo.ipynb`'s cell 0.** Every existing cell is preserved
byte-for-byte and simply shifts down one index. Do not edit, reorder, re-execute or re-render any
existing cell; do not touch `execution_count` or any committed output.

The new cell states, briefly: this is the **Milestone 1** evidence notebook and it runs
**standalone** — it requires nothing from `demo_v2.ipynb` and shares no cell state with it; the
**Milestone 2** evidence (LoRA + EWC, the no-forgetting A/B, teach-then-recall, and the two
signature figures) lives in `demo_v2.ipynb`, which likewise runs standalone. Keep it to a few
sentences — it is a signpost, not a second title.

Edit the JSON directly rather than through a notebook UI, so the diff is exactly one inserted
cell object. Verify with `git diff` that the only change is the insertion — no whitespace churn,
no metadata rewrite, no `execution_count` change on any existing cell.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import json,subprocess;nb=json.load(open('demo.ipynb'));old=json.loads(subprocess.run(['git','show','HEAD:demo.ipynb'],capture_output=True,text=True).stdout);print(len(nb['cells'])==len(old['cells'])+1, nb['cells'][0]['cell_type']=='markdown', nb['cells'][1:]==old['cells'], nb['metadata']==old['metadata'], 'demo_v2.ipynb' in ''.join(nb['cells'][0]['source']))"</automated>
  </verify>
  <acceptance_criteria>
    - The command above prints five `True` values — exactly one cell added, it is markdown, **all
      eight original cells are byte-identical**, the notebook metadata is unchanged, and the new
      cell names `demo_v2.ipynb`.
    - The new cell states that each notebook runs standalone and shares no cell state with the
      other.
    - `git diff --numstat demo.ipynb` shows the change is dominated by additions; no existing
      cell's `source` or `outputs` array was modified.
    - `.venv/bin/pytest -q` shows no regression.
  </acceptance_criteria>
  <done>One markdown cell prepended; all eight original cells byte-identical; both notebooks now state their mutual independence.</done>
</task>

<task type="auto">
  <name>Task 3: Record the SC3 supersession in ROADMAP</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - `.planning/ROADMAP.md` — the `### Phase 15: Figures & Writeup` section (line 244), its three
      success criteria (249-253), and the Progress table
    - `results/phase13_ab_report.md` lines 303-305 — the in-repo register for "ROADMAP wording
      superseded", the form this record copies
    - `.planning/phases/15-figures-writeup/15-04-SUMMARY.md` — the correlation verdict, and on a
      miss the narrowed SC2 wording
    - `.planning/phases/15-figures-writeup/15-CONTEXT.md` D-11 and D-13
  </read_first>
  <action>
Record two wording changes in `.planning/ROADMAP.md`'s Phase 15 section, both as **explicit,
dated supersession notes** in the `results/phase13_ab_report.md:303-305` register — appended
beneath the affected criterion, never by silently editing the criterion's text out of existence.

**SC3 supersession (R4).** SC3 reads *"REPORT.md and README carry the v2.0 narrative and
demo.ipynb is updated with honest numbers…"*. D-13 supersedes the `demo.ipynb` clause: the honest
v2.0 numbers ship in a **new** `demo_v2.ipynb`, self-contained and runnable independently, and
`demo.ipynb` receives only a prepended independence statement — its existing cells are untouched
by design. Record this as a dated note stating what the criterion originally said, what
superseded it, and which decision did so. **Never silently absorb it** — a success criterion that
quietly changed meaning is indistinguishable from one that was not met.

**SC2 narrowing (D-11), only if the correlation gate MISSED.** Read the verdict from
`15-04-SUMMARY.md`. SC2 reads *"…showing EWC visibly dodging high-Fisher coordinates"*. If the
gate missed, append a dated note recording the narrowed wording exactly as `15-04-SUMMARY.md`
recorded it, in the same supersession register, citing D-11's miss policy and linking to
`results/phase13_ab_report.md`'s `## Phase 15 Addendum` for the full pre-registration table. If
the gate passed, add no SC2 note — but state in this plan's SUMMARY that the gate passed and no
narrowing was required, so the absence of a note is a recorded outcome rather than an omission.

Do not mark Phase 15 complete or update the Progress table here — `/gsd:verify-work` owns that.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import pathlib,re;t=pathlib.Path('.planning/ROADMAP.md').read_text();s=re.search(r'^### Phase 15:.*?(?=^### |\Z)',t,re.M|re.S).group(0);print('demo_v2.ipynb' in s, 'supersed' in s.lower(), 'D-13' in s)"</automated>
  </verify>
  <acceptance_criteria>
    - The command above prints `True True True`.
    - The SC3 note states what the criterion originally said, what superseded it (`demo_v2.ipynb`,
      self-contained), and which decision did so (D-13), and carries a date.
    - The original SC3 text is still present and readable — the note is appended beneath it, not a
      replacement.
    - If the correlation gate missed, an SC2 narrowing note is present citing D-11 and linking to
      the `## Phase 15 Addendum`. If it passed, the SUMMARY explicitly records that no narrowing
      was required.
    - The Phase 15 status line and the Progress table are unchanged — completion is
      `/gsd:verify-work`'s call.
  </acceptance_criteria>
  <done>ROADMAP records the SC3 supersession explicitly and, if the gate missed, the SC2 narrowing — both dated, neither silently absorbed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed `results/` artifacts → notebook cells | The notebook re-cites; any recomputation would create a second, unaudited source of truth |
| v1.0 notebook cells → the prepend | An accidental re-execution would rewrite committed outputs of shipped evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-20 | Tampering | An existing `demo.ipynb` cell silently modified or re-executed | mitigate | The verify command diffs every cell against `git show HEAD:demo.ipynb` and asserts `nb['cells'][1:] == old['cells']` plus unchanged notebook metadata — a byte-level equality check, not a visual review. |
| T-15-21 | Tampering | A ROADMAP success criterion quietly reinterpreted | mitigate | R4: the SC3 supersession is an explicit dated note in the `phase13_ab_report.md:303-305` register, with the original text preserved beneath it. A gate-missed SC2 narrowing is recorded the same way, and a gate-passed outcome is recorded in the SUMMARY so the absence of a note is deliberate. |
| T-15-22 | Tampering | The v2.0 notebook recomputing a headline number and diverging from the audited script | mitigate | D-13's re-cite principle carried forward verbatim: the notebook reads committed `results/` files only — asserted by scanning the **code cells' `source` arrays** for `checkpoints/`, never the whole file, because the permitted `phase15_norms.json` cell legitimately echoes `w0_source` / `source_ckpt` provenance strings into its committed output — and every displayed number is cross-checked against its source report. |
| T-15-23 | Denial of Service | The v2.0 notebook failing on a fresh clone due to an inherited dependency | mitigate | D-13 self-containment: every input is a committed file; the notebook is executed end-to-end from a fresh kernel as an acceptance criterion; and both notebooks state their independence so a reader never discovers it by hitting a failure. |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed; `matplotlib` and `jupyter` tooling are already present via existing extras. |
</threat_model>

<verification>
- `demo_v2.ipynb` executes clean from a fresh kernel with no gitignored input
- `demo.ipynb`'s eight original cells are byte-identical to `git show HEAD:demo.ipynb`
- Both notebooks' opening cells state mutual independence
- `.planning/ROADMAP.md`'s Phase 15 section carries the dated SC3 supersession note
- `.venv/bin/pytest -q` → no regression; `make lint` → clean
</verification>

<success_criteria>
- `demo_v2.ipynb` is a self-contained, executed Milestone 2 evidence notebook that re-cites
  committed numbers and never recomputes them.
- Both signature figures appear in it with the terse D-01/D-03/D-18 disclosure and the named
  Fisher variant string read from the artifact.
- All three DOC-02 honest-number categories are present — recall against its gate, the
  retention delta, and the tokenizer-inflation tax (3.229 / 2.860 / 1.129x, with its
  same-run-baseline-only qualifier).
- `demo.ipynb` gains exactly one prepended markdown cell; its eight original cells are untouched.
- ROADMAP records the SC3 supersession explicitly and dated, with the original criterion text
  preserved, and records the SC2 narrowing if the correlation gate missed.
</success_criteria>

<output>
Create `.planning/phases/15-figures-writeup/15-07-SUMMARY.md` when done.
Record whether the correlation gate passed (no SC2 note) or missed (SC2 note added, with the
narrowed wording as written).
</output>
