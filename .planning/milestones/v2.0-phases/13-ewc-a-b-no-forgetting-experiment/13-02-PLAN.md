---
phase: 13-ewc-a-b-no-forgetting-experiment
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - results/phase13_ewc/run.csv
  - results/phase13_naive/run.csv
autonomous: true
requirements: [DEMO-04]

must_haves:
  truths:
    - "Both arm CSVs exist under D-07 scoped paths with a step-0 row and a step-4000 final row"
    - "The EWC arm's step-4000 retention is within MARGIN (0.13786) of finetune_prod.csv's 3.891139975617828 (D-11), or a divergence investigation is recorded before proceeding"
    - "Phase-12 artifacts (finetune_prod.csv, convbase trio) are byte-untouched"
    - "Both arm endpoint checkpoints exist locally (checkpoints/phase13_{arm}_latest.pt, gitignored, kept per D-08)"
    - "Exactly two arms run at 4000 steps — naive (λ=0) and EWC (λ=0.01), no extra λ arms (D-04)"
  artifacts:
    - path: "results/phase13_ewc/run.csv"
      provides: "EWC arm forgetting-curve data, schema identical to finetune_prod.csv"
      contains: "retention_ppl"
    - path: "results/phase13_naive/run.csv"
      provides: "Naive (λ=0) arm collapse-curve data, identical schema (diagnostic ewc_penalty column)"
      contains: "retention_ppl"
  key_links:
    - from: "results/phase13_*/run.csv"
      to: "results/finetune_prod.csv"
      via: "identical CSV schema step,train_loss,val_loss,lr,tokens,wall_clock,dialog_ppl,ewc_penalty,retention_ppl"
      pattern: "dialog_ppl,ewc_penalty,retention_ppl"
---

<objective>
Execute both pre-registered arms as separate processes from the committed driver — EWC arm
first (its step-250 row is the free early twin-drift detector against prod), then the naive
arm — and commit both per-arm CSVs as evidence.

Purpose: produces the DEMO-04 raw data (both curves) and the D-11 reproduction cross-check.
Output: two committed run CSVs; two local endpoint checkpoints.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-ewc-a-b-no-forgetting-experiment/13-CONTEXT.md
@scripts/finetune_ab.py
@results/finetune_prod.csv
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the EWC arm with the step-250 early drift check</name>
  <files>results/phase13_ewc/run.csv</files>
  <read_first>
    - scripts/finetune_ab.py (guards, expected output paths, proof + D-11 cross-check behavior)
    - results/finetune_prod.csv (the twin target: step-250 row dialog_ppl 5.101385316571219 / retention_ppl 3.904004997455472; step-4000 row dialog_ppl 4.573349214207799 / retention_ppl 3.891139975617828)
  </read_first>
  <action>
    Launch `.venv/bin/python scripts/finetune_ab.py ewc` as a background process (run is ~37 min
    on M3/MPS — exceeds foreground Bash timeout; use run_in_background and capture stdout to a
    scratchpad log). Monitoring protocol:

    1. At ~4 minutes, read `results/phase13_ewc/run.csv` and compare the step-250 row against
       finetune_prod.csv's step-250 row (dialog_ppl 5.101385316571219, retention_ppl
       3.904004997455472). Exact match to full printed precision = twin intact. Any difference
       = twin broken (Pitfall 2): kill the run immediately, investigate the RNG call order in
       the driver against finetune_dialog.py, fix, delete the arm outputs, and relaunch.
       Do NOT let a drifted run complete and pass downstream.
       ESCAPE CLAUSE (bounded — this is not a loop): if the investigation confirms the
       driver's call order is identical to scripts/finetune_dialog.py (no extra RNG draw)
       and the step-250 difference is small (well inside the D-11 MARGIN 0.13786), the cause
       is inherent MPS non-determinism — a risk D-11 itself names. Relaunch at most ONCE; if
       drift recurs, let the run complete, rely on the pre-registered D-11 margin verdict at
       step 4000, and record the drift as an MPS-non-determinism finding in the SUMMARY.
    2. On completion, confirm the driver's end-of-run proofs and the D-11 side-by-side print
       succeeded (exit code 0; a nonzero exit with "D-11 DIVERGENCE" means the run data is
       saved but report finalization is blocked — record the divergence numbers in the
       SUMMARY as a REAL FINDING and investigate before Plan 13-04).
    3. Confirm `checkpoints/phase13_ewc_latest.pt` exists (kept locally, gitignored — D-08).

    Note on the `<automated>` verify below: it hard-fails on D-11 divergence BY DESIGN. A
    failure of that assert means "REAL FINDING — record the numbers and investigate per
    D-11", not "task botched"; do not weaken the assert to make it pass.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import csv; rows=list(csv.DictReader(open('results/phase13_ewc/run.csv'))); assert rows[0]['step']=='0' and int(float(rows[-1]['step']))==4000; assert abs(float(rows[-1]['retention_ppl'])-3.891139975617828)<=0.13786, rows[-1]['retention_ppl']"</automated>
  </verify>
  <acceptance_criteria>
    - results/phase13_ewc/run.csv exists with header `step,train_loss,val_loss,lr,tokens,wall_clock,dialog_ppl,ewc_penalty,retention_ppl`, step-0 row (retention_ppl 2.107553076833866), and final row step 4000
    - Step-250 row matches finetune_prod.csv step-250 row on dialog_ppl and retention_ppl to full printed precision, OR the bounded escape clause applies (call order verified identical, drift small, ≤1 relaunch) and the MPS-non-determinism finding is documented in the SUMMARY
    - `git status --porcelain results/finetune_prod.csv` is empty (Phase-12 evidence untouched)
    - checkpoints/phase13_ewc_latest.pt exists (`test -f` exits 0) and is NOT staged for commit
    - Driver exit code 0 (or a documented D-11 divergence finding if nonzero)
  </acceptance_criteria>
  <done>EWC arm complete; D-11 cross-check result recorded; CSV ready to commit.</done>
</task>

<task type="auto">
  <name>Task 2: Run the naive (λ=0) arm and commit both CSVs</name>
  <files>results/phase13_naive/run.csv</files>
  <read_first>
    - scripts/finetune_ab.py (same driver; arm arg "naive" flips only penalty_fn to None)
    - results/phase13_ewc/run.csv (Task 1 output — schema the naive CSV must match exactly)
  </read_first>
  <action>
    After Task 1 completes (sequential — avoid MPS contention between two live runs), launch
    `.venv/bin/python scripts/finetune_ab.py naive` in the background (~37 min). On completion:

    1. Confirm end-of-run proofs passed (exit 0). The naive arm has no D-11 check.
    2. Sanity-read the final row: retention_ppl is expected to EXCEED the 1250-step collapse
       figure 5.9553 (RESEARCH A3 — continued drift). This is measured, not gated: record the
       number whatever it is; a surprisingly low value is a finding to note, not a failure.
    3. Confirm the two CSVs have IDENTICAL headers (the naive ewc_penalty column is
       diagnostic-only — "measured, not applied"; the report footnote lands in Plan 13-04).
    4. Compute the raw gate inputs for the SUMMARY: naive_ret_4000, ewc_ret_4000, delta,
       MARGIN=0.13786, and the boolean ewc_mitigates verdict — but do NOT write any report
       section (Plan 13-04 owns the report).
    5. Commit both `results/phase13_ewc/run.csv` and `results/phase13_naive/run.csv`
       (checkpoints stay local/gitignored).
  </action>
  <verify>
    <automated>.venv/bin/python -c "import csv; n=list(csv.DictReader(open('results/phase13_naive/run.csv'))); e=list(csv.DictReader(open('results/phase13_ewc/run.csv'))); assert n[0].keys()==e[0].keys(); assert int(float(n[-1]['step']))==4000; import math; assert all(math.isfinite(float(r['retention_ppl'])) for r in n if r['retention_ppl'])"</automated>
  </verify>
  <acceptance_criteria>
    - results/phase13_naive/run.csv exists, final row step 4000, header identical to the EWC arm CSV
    - Both CSVs committed (`git ls-files results/phase13_naive/run.csv results/phase13_ewc/run.csv` lists both)
    - `git status --porcelain results/finetune_prod.csv results/finetune_smoke_report.md` is empty
    - checkpoints/phase13_naive_latest.pt exists locally and is not tracked by git
    - SUMMARY records naive_ret_4000, ewc_ret_4000, delta vs MARGIN, and the gate boolean
  </acceptance_criteria>
  <done>Both arms complete; identical schemas; evidence committed; gate inputs recorded for Plan 13-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| training runs → committed evidence | runs write results/; must never touch Phase-12 artifacts |
| driver → local checkpoints | trusted deserialization of best.pt / fisher cache |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04 | Tampering | arm output paths on relaunch | mitigate | refuse_if_exists guard fires on any rerun; drifted-run relaunch requires explicit deletion of the arm's own outputs only |
| T-13-05 | Repudiation | run provenance | mitigate | per-arm provenance echo (seed, config, penalty bit, git_sha) captured in the background-run log; D-11 cross-check printed side-by-side |
| T-13-06 | DoS | 37-min MPS run crash mid-flight | mitigate | PYTORCH_ENABLE_MPS_FALLBACK=1 set before torch import (Pitfall 5); sequential arms avoid contention |
| T-13-SC | Tampering | package installs | accept | none this phase |
</threat_model>

<verification>
- Both arm CSVs committed with identical schemas, step-0 and step-4000 rows
- D-11 cross-check outcome recorded (match, or divergence flagged as blocking finding)
- `git diff HEAD -- results/finetune_prod.csv` empty
</verification>

<success_criteria>
- Two 4000-step arms complete, differing only in penalty_fn, data-order twin verified at step 250
- Gate inputs available for the report; no Phase-12 evidence modified
</success_criteria>

<output>
Create `.planning/phases/13-ewc-a-b-no-forgetting-experiment/13-02-SUMMARY.md` when done
</output>
