---
phase: 13-ewc-a-b-no-forgetting-experiment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/finetune_ab.py
  - tests/test_phase13_driver.py
  - results/phase13_ab_report.md
autonomous: true
requirements: [DEMO-04]

must_haves:
  truths:
    - "The A/B driver refuses (SystemExit) to overwrite either arm's existing outputs on re-run"
    - "Gate verdict function returns False at delta == MARGIN and True strictly above it"
    - "Both arms build the identical TrainConfig; the ONLY code-path difference is penalty_fn (None vs EWCPenalty(0.01))"
    - "Pre-registration constants (K=2, DELTA_RET=0.068930, LAMBDA_EWC=0.01, 4000 steps, LR 9e-5, seed 1337) are committed in code AND report preamble BEFORE either arm runs (D-10)"
    - "LAMBDA_EWC=0.01 is the headline λ — the only sweep point moving both axes favorably (D-02)"
    - "The gate is retention-only: ewc_mitigates uses K=2 times the Phase-12 noise floor, with NO acquisition gate (D-06)"
    - "Both arms start fresh from checkpoints/best.pt with the recorded twin config — unmasked, LR 9e-5, seed 1337, 4000 steps, batch 32, accum 1 (D-01, D-03)"
  artifacts:
    - path: "scripts/finetune_ab.py"
      provides: "Arm-parameterized A/B driver cloned from finetune_dialog.py"
      contains: "def ewc_mitigates"
    - path: "tests/test_phase13_driver.py"
      provides: "Guard + gate-arithmetic + config-identicality units (CPU-only)"
    - path: "results/phase13_ab_report.md"
      provides: "Pre-registration preamble with constants table + commit SHAs + Pitfall-1 exception row"
      contains: "## Pre-Registration"
  key_links:
    - from: "scripts/finetune_ab.py"
      to: "personacore.training.train"
      via: "penalty_fn seam (the one-bit arm difference)"
      pattern: "penalty_fn="
    - from: "scripts/finetune_ab.py"
      to: "checkpoints/fisher_tinystories.pt"
      via: "load_fisher fingerprint-pinned cache"
      pattern: "load_fisher"
---

<objective>
Commit the pre-registered A/B driver, its unit tests, and the report preamble BEFORE either
arm runs (D-10: git history is the pre-registration proof). The driver is a clone of the
reviewed `finetune_dialog.py`, parameterized by arm name, with D-07 refuse-to-rerun guards
on name-scoped output paths.

Purpose: DEMO-04's central claim ("differs ONLY in the penalty") is only credible if the
gate rule and arm configs are locked in git before any number exists.
Output: `scripts/finetune_ab.py`, `tests/test_phase13_driver.py`, `results/phase13_ab_report.md` (preamble only).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-ewc-a-b-no-forgetting-experiment/13-CONTEXT.md
@.planning/phases/13-ewc-a-b-no-forgetting-experiment/13-RESEARCH.md
@.planning/phases/13-ewc-a-b-no-forgetting-experiment/13-PATTERNS.md
@scripts/finetune_dialog.py
@scripts/finetune_smoke.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the arm-parameterized A/B driver scripts/finetune_ab.py</name>
  <files>scripts/finetune_ab.py</files>
  <read_first>
    - scripts/finetune_dialog.py (the reviewed single-arm template — clone its structure, guards, seeding order, step-0 pre-seed, provenance print, _prove proofs verbatim where applicable)
    - scripts/finetune_smoke.py (lines ~77-150: the pre-registration constants-block precedent with citation comments)
    - results/finetune_smoke_report.md (Stage 0b table: DELTA_RET = 0.068930; the report displays K×Δ = 0.137861 from the unrounded floor — transcribe 0.068930 and compute MARGIN = K * DELTA_RET in code)
    - results/finetune_prod_run.log (the twin provenance block the λ=0 arm must match bit-for-bit except λ)
    - src/personacore/training/loop.py (train() signature, CSV_FIELDNAMES, RNG-snapshot semantics around extra_eval_fns, end-of-call checkpoint_path save)
  </read_first>
  <behavior>
    - ewc_mitigates(naive_ret, ewc_ret) returns (naive_ret - ewc_ret) > MARGIN; False when delta == MARGIN exactly
    - penalty_for_arm("naive", p) returns None; penalty_for_arm("ewc", p) returns p
    - refuse_if_exists(paths) raises SystemExit naming the offending path when any exists; no-op otherwise
    - arm_outputs(arm) returns (results/phase13_{arm}/run.csv, checkpoints/phase13_{arm}_latest.pt); never any Phase-12 path
    - build_train_config() returns the same TrainConfig on every call
  </behavior>
  <action>
    Clone `scripts/finetune_dialog.py` into `scripts/finetune_ab.py`, parameterized by a single
    positional arg `arm` in {"naive", "ewc"} (no argparse — `main(sys.argv[1])` under the
    `__main__` guard; reject any other value with SystemExit). Per D-01/D-03, both arms start
    fresh from `checkpoints/best.pt` with the recorded twin config. Structure requirements:

    1. **Header**: identical MPS-fallback header (`os.environ.setdefault("PYTORCH_ENABLE_MPS_FALLBACK", "1")`
       BEFORE `import torch` — Pitfall 5), `_REPO_ROOT` path constants, `[finetune_ab]` print prefix.
       Do NOT import `export_slim` (no slim artifact this phase).

    2. **Pre-registration constants block (D-10)** at module top, each with a citation comment
       (`finetune_smoke.py:77+` style, plus the "Hardcoded on purpose — the driver never parses
       the report for numbers" comment): `K = 2`, `DELTA_RET = 0.068930` (smoke report Stage 0b,
       seed-pair floor, masked arm, LR 9e-5, 1250 steps — regime named per D-05), `MARGIN = K * DELTA_RET`,
       `LAMBDA_EWC = 0.01` (D-02), `LR = 9e-5`, `MAX_STEPS = 4000`, `SEED = 1337`, `BATCH_SIZE = 32`,
       `GRAD_ACCUM = 1`, `EVAL_INTERVAL = 250`, `BLOCK_SIZE = 256`.

    3. **Module-level pure functions** (testable without running main — importlib test pattern):
       `ewc_mitigates(naive_ret, ewc_ret)` (D-06 gate), `penalty_for_arm(arm, penalty)` (THE
       one-bit difference), `arm_outputs(arm)` returning the D-07 name-scoped paths
       `results/phase13_{arm}/run.csv` and `checkpoints/phase13_{arm}_latest.pt`,
       `refuse_if_exists(paths)` (SystemExit naming the path + what to delete, WR-02 register),
       `build_train_config()` returning TrainConfig constructed with EXACTLY the same kwargs as
       finetune_dialog.py's cfg (lr=LR, batch_size=BATCH_SIZE, grad_accum_steps=GRAD_ACCUM,
       max_steps=MAX_STEPS, seed=SEED — defaults cover warmup 100/grad_clip/weight_decay as in prod).

    4. **main(arm)**: guard via refuse_if_exists(arm_outputs(arm)) — Phase-12 paths
       (finetune_prod.csv, convbase trio) must NEVER appear as write targets anywhere in this
       file (Pitfall 6); prerequisite FileNotFoundError checks cloned from finetune_dialog.py
       (dialog bins, DIALOG_VAL_MASK stays required for the acquisition metric even though
       train_mask_bin=None, retention_val.bin, best.pt, fisher cache, anchors JSON); then
       replicate the twin call ORDER exactly (Pitfall 2): preflight_device(strict=True) →
       trusted-only `torch.load(BEST_PATH, weights_only=False)` with the T-12-10 comment →
       `seed_everything(SEED)` IMMEDIATELY before `GPT(model_cfg)` build → load_state_dict →
       theta_star clones → `load_fisher(FISHER_CACHE, expected_fingerprint=...)` →
       `EWCPenalty(fisher, theta_star, LAMBDA_EWC, runtime.device)` constructed in BOTH arms
       (RNG-free; feeds the diagnostic ewc_penalty column — research constraint 5).
       No RNG draw may occur between seed_everything and train() beyond this sequence.

    5. **fns dict IDENTICAL in both arms**: dialog_ppl via masked_perplexity(DIALOG_VAL,
       DIALOG_VAL_MASK, forbid_ids), retention_ppl via retention_perplexity(RETENTION_BIN),
       ewc_penalty via float(penalty(m)). Step-0 row pre-seeded via _preseed_csv (measured
       dialog_ppl0 from best.pt; retention_ppl from anchors["retention_ppl_subbin_step0"] =
       2.107553076833866; ewc_penalty 0.0). `model.train()` before train() (Pitfall 7).

    6. **train() call**: build_train_config(), train_bin=DIALOG_TRAIN, val_bin=DIALOG_VAL,
       train_mask_bin=None (unmasked, D-03), val_mask_bin=DIALOG_VAL_MASK,
       penalty_fn=penalty_for_arm(arm, penalty), extra_eval_fns=fns,
       checkpoint_extra with fisher/theta_star/lambda/meta for the EWC arm ONLY (prod precedent),
       log_path=arm CSV, eval_interval=EVAL_INTERVAL, checkpoint_path=arm latest checkpoint,
       and OMIT best_checkpoint_path entirely (D-08 / RESEARCH Pattern 4: end-of-call save IS
       the step-4000 state; removes the WR-01 surface).

    7. **End-of-run proofs** (_prove, SystemExit style): step-0 row present, final row step ==
       MAX_STEPS, retention_ppl column all finite. For arm == "ewc" additionally the D-11
       reproduction cross-check: read `results/finetune_prod.csv` (READ-ONLY) final row, print
       a side-by-side endpoint table (this run vs prod: dialog_ppl 4.573349214207799,
       retention_ppl 3.891139975617828), and AFTER all outputs are saved raise
       SystemExit("[finetune_ab] D-11 DIVERGENCE — investigate before report finalization")
       if abs(ewc_retention_4000 - prod_retention_4000) > MARGIN. Match prints a confirmation line.

    8. **Provenance echo** per arm (identicality-proof discretion item): the
       finetune_dialog.py provenance block adapted — arm name, seed, TrainConfig repr,
       train_mask_bin=None, the penalty bit (None vs 0.01), anchor fingerprint, driver git_sha().

    Keep the file ruff-clean (`make lint` passes).
  </action>
  <verify>
    <automated>.venv/bin/python -c "import importlib.util,pathlib; s=importlib.util.spec_from_file_location('fab', 'scripts/finetune_ab.py'); m=importlib.util.module_from_spec(s); s.loader.exec_module(m); assert m.K==2 and m.LAMBDA_EWC==0.01 and not m.ewc_mitigates(5.0, 5.0-m.MARGIN)"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/finetune_ab.py contains `DELTA_RET = 0.068930`, `MARGIN = K * DELTA_RET`, `def ewc_mitigates(`, `def penalty_for_arm(`, `def arm_outputs(`, `def refuse_if_exists(`, `def build_train_config(`
    - `grep -c "finetune_prod\|convbase" scripts/finetune_ab.py` shows finetune_prod.csv referenced ONLY as a read path in the D-11 cross-check (no write/open-for-write); no convbase path appears as a write target
    - scripts/finetune_ab.py contains `best_checkpoint_path` NOWHERE (D-08: omitted entirely)
    - `seed_everything(SEED)` line appears immediately before the `GPT(` build line (no intervening RNG-drawing call)
    - Importing the module executes no guard/training (main under `if __name__ == "__main__":`)
    - `.venv/bin/ruff check scripts/finetune_ab.py` exits 0
  </acceptance_criteria>
  <done>Driver committed with pre-registration constants; import-safe module surface for tests; guards and twin call order in place.</done>
</task>

<task type="auto">
  <name>Task 2: Unit tests pinning guard, gate arithmetic, and config identicality</name>
  <files>tests/test_phase13_driver.py</files>
  <read_first>
    - scripts/finetune_ab.py (the module under test — written in Task 1)
    - tests/test_ablation_config.py (docstring-contract style analog)
    - .planning/phases/13-ewc-a-b-no-forgetting-experiment/13-PATTERNS.md (test_phase13_driver section — CONVENTION WARNING and the two options; this plan LOCKS option 1: importlib.util.spec_from_file_location load, justified in the module docstring because the driver's main() is __main__-guarded and the gate/guard logic is module-level pure functions per the finetune_smoke.py "gate formulas as pure functions" precedent)
  </read_first>
  <action>
    Create `tests/test_phase13_driver.py` (CPU-only, no checkpoint I/O, no training). Load the
    driver via a module-level `importlib.util.spec_from_file_location("finetune_ab", REPO_ROOT / "scripts/finetune_ab.py")`
    helper. Docstring states the pinned contracts and the scripts-import justification. Tests:

    1. `test_preregistration_constants` — exact literals: K == 2, DELTA_RET == 0.068930,
       MARGIN == 2 * 0.068930, LAMBDA_EWC == 0.01, LR == 9e-5, MAX_STEPS == 4000, SEED == 1337,
       BATCH_SIZE == 32, GRAD_ACCUM == 1, EVAL_INTERVAL == 250.
    2. `test_gate_boundary` — ewc_mitigates(5.0, 5.0 - MARGIN) is False (boundary fails);
       ewc_mitigates(5.0, 5.0 - MARGIN - 1e-3) is True; ewc_mitigates(5.0, 5.0) is False.
    3. `test_one_bit_difference` — penalty_for_arm("naive", sentinel) is None;
       penalty_for_arm("ewc", sentinel) is sentinel.
    4. `test_config_identical_across_arms` — build_train_config() called twice returns equal
       TrainConfig instances (dataclass equality), with lr == 9e-5, max_steps == 4000,
       batch_size == 32, grad_accum_steps == 1, seed == 1337.
    5. `test_arm_outputs_scoped` — for both arms, arm_outputs paths contain
       f"phase13_{arm}" and none of them contain "finetune_prod" or "convbase" (D-07/Pitfall 6).
    6. `test_refuse_to_rerun_guard` (tmp_path) — refuse_if_exists raises SystemExit whose
       message names the existing file when one tmp path exists; returns None when none exist.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase13_driver.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_phase13_driver.py exits 0 under `.venv/bin/python -m pytest tests/test_phase13_driver.py -x -q`
    - Full suite stays green: `.venv/bin/python -m pytest tests/ -x -q` exits 0 (275+ tests — purity contract, train() untouched)
    - File contains `spec_from_file_location` and a docstring line justifying the scripts-load choice
    - `test_gate_boundary` asserts False at exactly `5.0 - MARGIN` (boundary is a fail, not a pass)
  </acceptance_criteria>
  <done>Driver contracts pinned by 6 CPU-only tests; full suite green.</done>
</task>

<task type="auto">
  <name>Task 3: Report preamble with pre-registration table (committed before any arm runs)</name>
  <files>results/phase13_ab_report.md</files>
  <read_first>
    - results/finetune_smoke_report.md (layout precedent: opening blockquote "what these numbers are / are not", ## Pre-Registration table with Constant/Value/Rationale columns)
    - scripts/finetune_ab.py (the constants being transcribed — values must match exactly)
    - .planning/phases/13-ewc-a-b-no-forgetting-experiment/13-RESEARCH.md (Pitfall 1: the mandatory λ=0 exception-row wording)
  </read_first>
  <action>
    Create `results/phase13_ab_report.md` containing ONLY the preamble (results sections come in
    Plan 13-04 after the runs). Obtain the Task-1/2 commit SHA via
    `git log -1 --format=%h -- scripts/finetune_ab.py`. Contents:

    1. Title + opening blockquote in the smoke-report register: what the numbers will be
       (a pre-registered DEMONSTRATION of one λ=0 vs λ=0.01 comparison at 4000 steps,
       retention-gated), what they are not (NOT a λ search — see the reconciliation section,
       placeholder heading), and that every rule below was committed before either arm ran.
    2. `## Pre-Registration` table — one row per constant with value + rationale + the commit
       SHA where it was locked: K=2 (reused blind from Phase 12, not re-chosen), DELTA_RET=0.068930
       (regime named: seed-pair floor, masked arm, LR 9e-5, 1250 steps — D-05 obligation 1),
       MARGIN=K×DELTA_RET=0.137860 (note: the smoke report displays 0.137861 from the unrounded
       floor; this phase computes from the transcribed 0.068930), λ_EWC=0.01 (D-02: the only
       sweep point moving both axes favorably), arm config (unmasked, LR 9e-5, seed 1337,
       4000 steps, batch 32, accum 1 — D-03), end-of-run cells (D-08), gate rule
       "EWC mitigates forgetting iff naive_ret − ewc_ret > MARGIN; acquisition reported
       descriptively with NO gate" (D-06).
    3. **The Pitfall-1 provenance exception row** (MANDATORY, verbatim substance):
       "retention_ppl for λ=0 is absent from `ft_lr_9e-5.csv` (column not logged by the
       Stage-2 driver, pre-Phase-13 decision); value 5.9553 (and dialogue 4.4453) cited from
       `results/finetune_smoke_report.md` Stage 2/3 tables, commit 666d096 — not recomputed here."
    4. Empty placeholder section headings for Plan 13-04 to fill: `## 2×2 Result`,
       `## Gate Verdict`, `## D-11 Reproduction Cross-Check`, `## Threats to Validity`,
       `## Reconciliation: §8 Search vs Phase-13 Demonstration`, `## Figures`, `## Retention Samples`.
  </action>
  <verify>
    <automated>grep -q "## Pre-Registration" results/phase13_ab_report.md && grep -q "666d096" results/phase13_ab_report.md && grep -q "0.068930" results/phase13_ab_report.md</automated>
  </verify>
  <acceptance_criteria>
    - results/phase13_ab_report.md contains `## Pre-Registration`, the string `666d096`, `0.068930`, `0.01`, and a git short-SHA referencing the driver commit
    - The exception row names `ft_lr_9e-5.csv`, the value `5.9553`, and "not recomputed here"
    - The file contains `## Reconciliation` (placeholder for D-09)
    - No results numbers appear (no 2×2 values, no verdict) — preamble only
  </acceptance_criteria>
  <done>Pre-registration exists in both code and report preamble, committed before either arm runs (D-10 satisfied by git history).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| driver → committed Phase-12 evidence | write path could clobber recorded results (WR-02 failure mode) |
| driver → checkpoint files | pickle deserialization of local artifacts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | finetune_ab.py output paths | mitigate | refuse_if_exists guard on all arm outputs; Phase-12 paths never write targets; pinned by test_refuse_to_rerun_guard + test_arm_outputs_scoped |
| T-13-02 | Info Disclosure/EoP | torch.load of best.pt | mitigate | weights_only=False ONLY with the TRUSTED-only T-12-10 comment on the project's own checkpoint; Fisher via load_fisher (weights_only=True, fingerprint-pinned) |
| T-13-03 | Repudiation | pre-registration provenance | mitigate | constants committed before runs; git SHA recorded in report preamble + provenance echo with git_sha() |
| T-13-SC | Tampering | package installs | accept | no packages installed this phase (research Package Legitimacy Audit: nothing to check) |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/ -x -q` exits 0 (full suite, purity contract)
- `make lint` passes on new files
- git log shows driver + tests + preamble commits with NO arm CSV existing yet (pre-registration ordering)
</verification>

<success_criteria>
- Driver, tests, and report preamble committed; no arm has run
- Gate rule and arm config exist as hardcoded constants with citations
- All must_haves truths verifiable
</success_criteria>

<output>
Create `.planning/phases/13-ewc-a-b-no-forgetting-experiment/13-01-SUMMARY.md` when done
</output>
