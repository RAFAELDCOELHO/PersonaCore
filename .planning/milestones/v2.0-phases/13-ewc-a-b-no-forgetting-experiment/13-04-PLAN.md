---
phase: 13-ewc-a-b-no-forgetting-experiment
plan: 04
type: execute
wave: 4
depends_on: [13-01, 13-02, 13-03]
files_modified:
  - results/phase13_ab_report.md
autonomous: true
requirements: [DEMO-04]

must_haves:
  truths:
    - "The report's headline is a 2×2 table of end-of-run (step 4000) acquisition AND retention values for both arms — never retention-only, never best-checkpoint values"
    - "The gate verdict applies exactly ewc_mitigates: naive_ret − ewc_ret > MARGIN, with acquisition reported descriptively and NO acquisition gate"
    - "One D-09 reconciliation section explains §8 'not demonstrable' (search, dual margin, smoke budget) vs Phase 13 (demonstration, single pre-chosen comparison, production budget)"
    - "The D-11 cross-check table shows the fresh EWC arm vs finetune_prod.csv endpoints side by side, reported regardless of outcome"
    - "The D-05 threats-to-validity register names the floor's measurement regime, the not-reverified-at-4000-steps limitation, and the naive arm's within-run trajectory check"
  artifacts:
    - path: "results/phase13_ab_report.md"
      provides: "The complete committed A/B evidence report"
      contains: "## Reconciliation"
  key_links:
    - from: "results/phase13_ab_report.md"
      to: "results/phase13_naive/run.csv"
      via: "2×2 cells read from final CSV rows (end-of-run, D-08)"
      pattern: "phase13_naive"
---

<objective>
Finalize `results/phase13_ab_report.md`: fill the preamble's placeholder sections with the
measured 2×2 result, gate verdict, D-11 cross-check, D-05 threats-to-validity, and the D-09
reconciliation — completing DEMO-04's committed evidence.

Purpose: the phase's deliverable claim, in the project's honest-evidence register; Phase 15
consumes this narrative verbatim.
Output: completed A/B report.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-ewc-a-b-no-forgetting-experiment/13-CONTEXT.md
@results/phase13_ab_report.md
@results/finetune_smoke_report.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Results sections — 2×2, gate verdict, D-11 cross-check, trajectory check</name>
  <files>results/phase13_ab_report.md</files>
  <read_first>
    - results/phase13_ab_report.md (the committed preamble — fill its placeholders; do NOT alter the pre-registration table values)
    - results/phase13_naive/run.csv and results/phase13_ewc/run.csv (final rows = the 2×2 cells; full retention columns for the trajectory check)
    - results/finetune_prod.csv (D-11 comparison row: step 4000 dialog_ppl 4.573349214207799, retention_ppl 3.891139975617828, ewc_penalty 0.13435843586921692)
    - scripts/finetune_ab.py (MARGIN and ewc_mitigates — the verdict must be computed with these exact constants, not re-derived)
    - .planning/phases/13-ewc-a-b-no-forgetting-experiment/13-02-SUMMARY.md (recorded gate inputs and any D-11 divergence finding)
  </read_first>
  <action>
    If Plan 13-02's SUMMARY records a D-11 divergence beyond MARGIN, STOP: report finalization
    is blocked (D-11) — surface the finding instead of writing the report. Otherwise fill:

    1. `## 2×2 Result` — table with rows {naive (λ=0), EWC (λ=0.01)} × columns
       {acquisition: masked dialogue val PPL, retention: retention PPL (frozen sub-bin)},
       values = the step-4000 CSV rows (end-of-run, D-08 — state this explicitly in a line
       under the table, plus "both arm checkpoints kept under the scoped paths"). Add the
       step-0 reference row (dialog ~31.90, retention 2.107553) for context, and the naive-arm
       footnote: "the naive CSV's ewc_penalty column is measured, not applied" (research
       constraint 5).
    2. `## Gate Verdict` — show the arithmetic: naive_ret_4000 − ewc_ret_4000 = delta vs
       MARGIN = 2 × 0.068930 = 0.13786, and the pre-registered boolean verdict verbatim
       ("EWC mitigates forgetting" holds/does not hold). Acquisition delta reported in the
       same section DESCRIPTIVELY with an explicit "no pass/fail gate on acquisition (D-06)"
       sentence.
    3. `## D-11 Reproduction Cross-Check` — side-by-side table: fresh EWC arm step-4000 vs
       finetune_prod.csv step-4000 (dialog_ppl, retention_ppl, ewc_penalty), the absolute
       retention difference vs MARGIN, and the outcome sentence (match within margin = the
       twin-config reproduction held on MPS; reported regardless of outcome).
    4. **D-05 obligation 3 (free within-run check)** — compute the naive arm's
       interval-to-interval retention deltas from its own CSV (successive retention_ppl
       differences across the 250-step rows); report whether the trajectory is
       smooth/monotonic as the within-run stability signal supporting the floor's
       transferability, with the actual delta sequence (or a summary: min/max/sign changes)
       shown, not just asserted.
  </action>
  <verify>
    <automated>.venv/bin/python -c "t=open('results/phase13_ab_report.md').read(); assert '## 2×2 Result' in t and '## Gate Verdict' in t and '0.13786' in t and 'measured, not applied' in t and '3.891139975617828' in t"</automated>
  </verify>
  <acceptance_criteria>
    - `## 2×2 Result` table contains four measured step-4000 cells matching the final CSV rows of both arms to the precision printed
    - `## Gate Verdict` shows delta, MARGIN, and a verdict sentence using the pre-registered rule; contains the phrase "no pass/fail gate" for acquisition
    - `## D-11 Reproduction Cross-Check` table contains 4.573349214207799 and 3.891139975617828 (prod row) beside the fresh-arm values
    - The naive-arm within-run retention trajectory check appears with actual numbers
    - The pre-registration table from Plan 13-01 is byte-unchanged (git diff on that section empty)
  </acceptance_criteria>
  <done>All measured sections filled from committed CSVs; verdict computed with pre-registered constants only.</done>
</task>

<task type="auto">
  <name>Task 2: Narrative sections — threats to validity, D-09 reconciliation, figures/samples references</name>
  <files>results/phase13_ab_report.md</files>
  <read_first>
    - results/phase13_ab_report.md (current state after Task 1)
    - results/finetune_smoke_report.md (§8 verdict verbatim + Stage 0b regime details for accurate citation; the register/tone to match)
    - .planning/phases/13-ewc-a-b-no-forgetting-experiment/13-CONTEXT.md (D-05 obligations 1-2, D-09 framing — the exact substance the sections must carry)
  </read_first>
  <action>
    Fill the remaining sections:

    1. `## Threats to Validity` (D-05 obligations 1 and 2) — (a) the noise floor's measurement
       regime stated concretely: Δ_ret = 0.068930 measured on the seed-pair (1337/2024) masked
       arm at LR 9e-5 over 1250 steps (smoke Stage 0b) — show the check, not just the number;
       (b) the named limitation: the floor was NOT re-verified at the 4000-step production
       budget or within collapse dynamics; effect-size variance COULD scale with drift
       magnitude in a way a stable-regime floor wouldn't capture; the 30–60× observed margin
       ratio is stated as the reason this is judged acceptable — NOT as proof the risk doesn't
       exist; (c) MPS non-determinism named as a risk category, with the D-11 cross-check
       result referenced as the evidence-based (not guaranteed) determinism check; (d) single
       seed pair — one comparison, not a distribution.
    2. `## Reconciliation: §8 Search vs Phase-13 Demonstration` (D-09, ONE section, not
       scattered) — explain why §8's "EWC not demonstrable at the 1250-step budget" (a SEARCH
       over five λ values requiring BOTH a near-impossible dialogue margin AND the retention
       margin simultaneously at smoke budget — all-fail informative) and Phase 13's
       retention-gated result (a DEMONSTRATION of a single pre-chosen λ=0.01 comparison,
       retention-gated by the same validated noise floor, at production budget) are NOT in
       tension; quote §8's verdict phrasing accurately and note it stands unamended. Also state
       the D-02 trade-off framing: λ=0.01 is the headline because BOTH axes move favorably —
       λ=100 would show near-zero retention drift bought with destroyed acquisition (half the
       phenomenon). Note the ROADMAP "λ=0 vs λ*" wording is superseded (λ*=None recorded).
    3. `## Figures` — reference results/phase13_forgetting_curve.png (4000-step arms, dashed
       2.1066 v1.0 headline vs the 2.107553 sub-bin curve anchor distinction stated ONCE here
       — Pitfall 3) and results/phase13_frontier.png (1250-step sweep endpoints, six points,
       the λ=0 point's provenance exception already in the pre-registration table).
    4. `## Retention Samples` — reference results/phase13_retention_samples.md, restate the
       one-run/shared-prompts/representative protocol in one sentence, and summarize the
       measured proxies per arm (stop-id fraction, role-token leakage counts).
    5. Commit the finalized report.
  </action>
  <verify>
    <automated>.venv/bin/python -c "t=open('results/phase13_ab_report.md').read(); assert '## Threats to Validity' in t and '## Reconciliation' in t and 'not demonstrable' in t and '2.107553' in t and 'phase13_frontier.png' in t and 'phase13_retention_samples.md' in t"</automated>
  </verify>
  <acceptance_criteria>
    - `## Threats to Validity` names the floor regime (1337/2024, masked, LR 9e-5, 1250 steps), the not-reverified-at-4000 limitation, MPS non-determinism, and the 30–60× ratio framed as judgment-not-proof
    - `## Reconciliation` is a single section containing both the words "search" and "demonstration" and an accurate §8 quotation
    - The 2.1066-vs-2.107553 distinction is stated exactly once in the figures section
    - All four evidence artifacts (2 PNGs, samples md, both arm CSVs) are referenced by path
    - Report committed; `git status --porcelain results/` clean afterward
  </acceptance_criteria>
  <done>Report complete in the smoke-report register; DEMO-04 evidence chain closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| report → committed CSVs | every number must trace to a committed artifact or a cited exception |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-10 | Repudiation | report numbers provenance | mitigate | all cells read from committed CSVs; the single non-CSV number (λ=0 frontier) carries the pre-registration exception row; verdict computed from committed constants |
| T-13-11 | Tampering | pre-registration table drift | mitigate | acceptance criterion: pre-reg table byte-unchanged from the Plan 13-01 commit |
| T-13-SC | Tampering | package installs | accept | none this phase |
</threat_model>

<verification>
- Report contains all seven sections filled; grep checks in both tasks pass
- `.venv/bin/python -m pytest tests/ -x -q` exits 0 (nothing regressed)
</verification>

<success_criteria>
- 2×2 end-of-run headline + retention-only gate verdict + descriptive acquisition (D-06/D-08)
- D-09 reconciliation, D-05 three obligations, D-11 cross-check all present with measured numbers
- Every number CSV-traceable except the cited λ=0 exception
</success_criteria>

<output>
Create `.planning/phases/13-ewc-a-b-no-forgetting-experiment/13-04-SUMMARY.md` when done
</output>
