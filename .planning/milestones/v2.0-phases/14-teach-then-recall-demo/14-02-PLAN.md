---
phase: 14-teach-then-recall-demo
plan: 02
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - scripts/phase14_factset.py
  - scripts/phase14_factset_gate.py
  - results/phase14_factset_report.md
autonomous: false
requirements: [DEMO-05]

must_haves:
  truths:
    - "D-01: every candidate fact value is adversarially chosen so the un-adapted base cannot guess it, and is authored in the base's first-person persona register — never second person. Guessability is a validity failure of the whole demo; tokenizer cost is a nuisance to note"
    - "D-02(a) / D-04: every candidate's token count and byte-fallback round-trip is measured by direct encode/decode, never assumed — and the count is recorded as a CENSUS FIELD that can never reject a candidate"
    - "D-02(b): the un-adapted convbase_best.pt is probed with each candidate's reserved questions BEFORE anything is taught, and any fact it answers correctly or nearly correctly is rejected"
    - "D-03: rejection is a mechanical exact-match floor plus a recorded close call, and every close-call rejection quotes the specific base completion text that triggered it — a documented judgment, never a silent one"
    - "D-05: the pool is composed as a high-cardinality proper-noun core plus a labelled low-cardinality soft tier, so the two tiers can be reported separately"
    - "D-09(1) / D-21(1,2): the calibration pool and the second-person register-arm pool are disjoint from the real pool and from each other, and pass the SAME pre-flight gate — disposable as evidence sources, not exempt from the validity discipline"
    - "A human reads the report and records a GO/ADAPT/STOP verdict before any teaching set exists"
  artifacts:
    - path: "scripts/phase14_factset.py"
      provides: "The three candidate pools, the reserved gate probes, and the pure census helper"
      contains: "CANDIDATE_POOL"
    - path: "scripts/phase14_factset_gate.py"
      provides: "D-06 gated report driver — census + guessability probes + clobber guard"
      contains: "def main"
    - path: "results/phase14_factset_report.md"
      provides: "Committed evidence with a recorded verdict"
      contains: "## Verdict"
  key_links:
    - from: "scripts/phase14_factset_gate.py"
      to: "personacore.dialogue.build_recall_prompt"
      via: "every guessability probe prompt is an id sequence from the shared function"
      pattern: "build_recall_prompt\\("
    - from: "scripts/phase14_factset_gate.py"
      to: "checkpoints/convbase_best.pt"
      via: "trusted-own-file torch.load(weights_only=False)"
      pattern: "weights_only=False"
---

<objective>
Author the candidate fact pools and run the D-06 pre-flight gate: tokenizer census plus base-model
guessability probes against the un-adapted `convbase_best.pt`, written to
`results/phase14_factset_report.md` behind a BLOCKING user verdict.

Purpose: D-01 makes guessability a validity failure of the whole demo — a "successful recall" the
base could have guessed proves nothing. This gate measures that *before* any teaching set or template
grammar is authored (D-06), so a shrunken viable set is information brought back before locking
rather than discovered mid-phase.

Output: `scripts/phase14_factset.py` (pure data + pure functions, no torch),
`scripts/phase14_factset_gate.py` (the driver), and a committed report carrying a human verdict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-teach-then-recall-demo/14-CONTEXT.md
@.planning/phases/14-teach-then-recall-demo/14-RESEARCH.md
@.planning/phases/14-teach-then-recall-demo/14-PATTERNS.md
@scripts/measure_inflation.py
@scripts/make_transcripts.py

<interfaces>
From `personacore.dialogue` (shipped by plan 14-01):
- `build_recall_prompt(tok, question, persona=()) -> list[int]`
- `detokenize(text) -> str`, `encode_dialogue(tok, persona, turns) -> (ids, mask)`

From `personacore.generation`:
- `collect(model, idx, *, max_new_tokens, forbid_ids=None, stop_ids=None, **gen_kw) -> Tensor`
- `undecodable_ids_mask(tokenizer, vocab_size) -> (1, vocab_size) bool tensor`

From `personacore.tokenizer`: `from_json(path)` → the frozen BPE tokenizer (`.encode`, `.decode`,
`.vocab`, `.special_tokens`)

From `personacore.config`: `ModelConfig(**dict)`, `RuntimeConfig(device=None)`
From `personacore.model`: `GPT(model_config)`
From `personacore.preflight`: `preflight_device(strict=True) -> dict`
From `personacore.seeding`: `seed_everything(seed, *, strict=False)`
From `personacore.provenance`: `git_sha(default="unknown") -> str`

Measured base priors to pre-seed the close-call filter (14-CONTEXT D-01, greedy, `convbase_slim.pt`):
occupation → `cop` / `college student`; location → `the country`; favorite color → `red`;
dog name → `rose`.

Measured token census (14-RESEARCH F1, all round-trip exact): `Max`/`Lily` = 1; `dog` = 2;
`Fenn`/`Tarn`/`Vim` = 3; `Zorp`/`Kalo`/`Krix`/`7412`/`Voss`/`blue` = 4;
`Zibby`/`Halvo`/`Oberlin`/`Lisbon` = 5; `Rafael`/`Ipanema` = 6; `kombucha`/`Pemberly` = 7;
`Marrowgate` = 8; `accordion` = 9.

Measured no-op (14-RESEARCH F1): all 256 byte ids are live, so `encode()` can never emit a dead id.
PITFALLS-12's `forbid_ids` sub-filter of the tokenizer pre-flight is structurally unfireable —
report it as a no-op, do not perform it as theatre.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author scripts/phase14_factset.py — three candidate pools, reserved gate probes, census helper</name>
  <files>scripts/phase14_factset.py</files>
  <read_first>
    - .planning/phases/14-teach-then-recall-demo/14-CONTEXT.md (D-01 register + measured priors, D-02, D-03, D-04, D-05 phrase-example table, D-08, D-09.1, D-21.1)
    - .planning/phases/14-teach-then-recall-demo/14-RESEARCH.md (§Measured Findings F1/F3, §Architecture Pattern 5 for the family vocabulary this module will grow into)
    - scripts/finetune_ab.py (lines 53-142 — the repo-root path-constant idiom and the module-level pre-registration comment register)
  </read_first>
  <action>
Create `scripts/phase14_factset.py` as the phase's single committed fact-data module. It imports NO
torch and defines NO `main()` — it is pure data plus pure functions, so any consumer (the gate driver,
the teaching script, the recall harness, and the CPU test suite) can load it cheaply.

Module docstring must state, explicitly:
- what the module is (the committed fact-set data for Phase 14; pre-registration lives in the
  committed driver so git history is the proof — the Phase-13 13-01 precedent);
- the import mechanism: scripts run as `python scripts/<name>.py` put `scripts/` on `sys.path[0]`,
  so sibling scripts use a plain `import phase14_factset`; tests load it with
  `importlib.util.spec_from_file_location` after inserting `scripts/` on `sys.path`;
- the D-01 register lock: every taught phrasing and every fact value is authored as FIRST-PERSON
  self-description (`i have a dog named zorp.`), never second person (`your dog is named zorp.`),
  because the frozen base emits first-person PersonaChat self-description exclusively (5/5 probes,
  14-RESEARCH F3). Teaching in second person would spend 331,776 LoRA parameters installing a
  register the base has never produced;
- the D-04 rule: token count is a CENSUS FIELD, not a reject criterion. Only the D-03 exact-match +
  close-call guessability verdict can reject a candidate;
- the measured no-op: PITFALLS-12's `forbid_ids` sub-filter cannot fire (all 256 byte ids live), so
  it is reported as a no-op rather than performed.

Define a `Fact` NamedTuple with fields `id` (short stable string), `slot` (e.g. `pet_name`,
`person_name`, `place`, `year`, `favorite_color`), `value` (the invented string), and `tier`
(`"core"` or `"soft"`).

Define three DISJOINT pools:
- `CANDIDATE_POOL` — the real-set candidates. Author **16 core + 6 soft** so attrition to D-05's
  5–8 core + 2–3 soft survives a harsh gate. Core slots are high-cardinality proper-noun /
  identifier slots (invented person, pet, and place names; uncommon number combinations). Soft-tier
  slots are low-cardinality (favorite color, favorite food). Values must be invented or distinctive
  and must NOT collide with the measured base priors `cop`, `college student`, `the country`, `red`,
  `rose`. Do not use `Max`, `Lily`, or `blue` — 14-RESEARCH F1 shows cheap tokenization is the
  warning sign, not the reward.
- `CALIBRATION_POOL` — 10 core candidates for D-09's throwaway calibration set, with values disjoint
  from `CANDIDATE_POOL`. Mirror `CANDIDATE_POOL`'s slot mix so D-14's "calibration must mirror the
  real set's likely final shape" holds.
- `REGISTER_ARM_POOL` — 6 core candidates for D-21's second-person register arm, disjoint from both
  pools above. Same slot mix, so the arm measures register and nothing else.

Define `GATE_PROBES: dict[str, tuple[str, ...]]` — 4 hand-written direct recall questions per
candidate in all three pools, in the PersonaChat second-person-addressed question form
(`what is your dog's name?`) with first-person answers implied. A comment above it must state D-08:
these probes are PERMANENTLY BANNED from every teaching set and become seed members of DEMO-06's
never-seen split, carrying their base-failure provenance forward.

Define the pure helpers:
- `token_census(tok, value) -> tuple[int, bool]` returning `(len(tok.encode(value)), tok.decode(tok.encode(value)) == value)`
- `normalize_for_match(text) -> str` — lowercase, `detokenize`, collapse runs of whitespace to one
  space, strip leading/trailing punctuation. Used by the gate's exact-match floor. Import
  `detokenize` from `personacore.dialogue`; do not reimplement it.
- `exact_match_clean(completions, value) -> bool` — D-03's mechanical floor: returns True iff
  `normalize_for_match(value)` appears in ZERO of the normalized completions. Docstring states the
  boundary explicitly: one containment out of N is a FAIL.
- `all_pools() -> tuple[tuple[str, tuple[Fact, ...]], ...]` — named pools for report iteration.

Add `BASE_PRIOR_SEEDS: dict[str, tuple[str, ...]]` mapping slot → the measured base-prior answers
from 14-CONTEXT D-01, with a comment stating these are known close-call triggers identified BEFORE
the candidate pool was authored, not discoveries made during the gate run.
  </action>
  <verify>
    <automated>.venv/bin/python -c "
import importlib.util, pathlib, sys
sys.path.insert(0, 'scripts')
s = importlib.util.spec_from_file_location('phase14_factset', 'scripts/phase14_factset.py')
m = importlib.util.module_from_spec(s); s.loader.exec_module(m)
core = [f for f in m.CANDIDATE_POOL if f.tier=='core']; soft = [f for f in m.CANDIDATE_POOL if f.tier=='soft']
assert len(core)==16 and len(soft)==6, (len(core), len(soft))
pools = [set(f.value for f in p) for _, p in m.all_pools()]
for i in range(len(pools)):
    for j in range(i+1, len(pools)):
        assert not (pools[i] & pools[j]), (i, j, pools[i] & pools[j])
ids = [f.id for p in (m.CANDIDATE_POOL, m.CALIBRATION_POOL, m.REGISTER_ARM_POOL) for f in p]
assert len(ids)==len(set(ids))
assert all(len(m.GATE_PROBES[i])>=4 for i in ids), 'every candidate needs >=4 reserved probes'
import torch; assert 'torch' not in sys.modules or True
print('pools ok', len(ids))"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^import torch\|^import numpy" scripts/phase14_factset.py` returns 0 — the module is torch-free and numpy-free
    - `grep -c "def main" scripts/phase14_factset.py` returns 0 — it is a data module, not a driver
    - `CANDIDATE_POOL` has exactly 16 `core` and 6 `soft` facts; `CALIBRATION_POOL` has 10; `REGISTER_ARM_POOL` has 6
    - The three pools' `value` sets are pairwise disjoint and all `Fact.id`s across all pools are unique
    - `GATE_PROBES` has at least 4 questions for every fact id in every pool
    - No fact value equals `Max`, `Lily`, `blue`, `cop`, `college student`, `the country`, `red`, or `rose`
    - `exact_match_clean(["i am a cop."], "zorp")` is True; `exact_match_clean(["i have a dog named zorp."], "zorp")` is False
    - The module docstring contains the strings `first-person`, `D-04`, and `no-op`
    - `.venv/bin/ruff check scripts/phase14_factset.py && .venv/bin/ruff format --check scripts/phase14_factset.py` exits 0
  </acceptance_criteria>
  <done>`scripts/phase14_factset.py` holds three disjoint, first-person-register candidate pools with reserved gate probes and the pure census/match helpers, importable without torch.</done>
</task>

<task type="auto">
  <name>Task 2: Write scripts/phase14_factset_gate.py — census + guessability probes + gated report</name>
  <files>scripts/phase14_factset_gate.py</files>
  <read_first>
    - scripts/measure_inflation.py (lines 60-199 — the clobber guard at 66-75 and the report-shape f-string template at 123-192; D-06 names this file as the precedent)
    - scripts/make_transcripts.py (lines 23-31 preamble, 67-81 `_complete`, 99-115 setup/device/mask block, 146-183 report writing)
    - scripts/prepare_dialog_corpus.py (lines 62-83 — `_require_go_verdict`, the downstream half of the blocking gate)
    - scripts/phase14_factset.py (as written in Task 1)
    - .planning/phases/14-teach-then-recall-demo/14-CONTEXT.md (D-02, D-03, D-04, D-06, D-08)
  </read_first>
  <action>
Create `scripts/phase14_factset_gate.py`, the D-06 gated report driver. It is
`scripts/measure_inflation.py`'s shape with `scripts/make_transcripts.py`'s generation half.

Preamble (Shared Pattern 1): `import os`, `import pathlib`, then
`os.environ.setdefault("PYTORCH_ENABLE_MPS_FALLBACK", "1")` with the T-05-04 comment, then
`import torch  # noqa: E402`. Repo-root-anchored path constants (Shared Pattern 2):
`CONVBASE_BEST = _REPO_ROOT / "checkpoints" / "convbase_best.pt"`,
`TOKENIZER_PATH = _REPO_ROOT / "artifacts" / "tokenizer.json"  # FROZEN — never retrain`,
`REPORT_PATH = _REPO_ROOT / "results" / "phase14_factset_report.md"`.

Module docstring carries a `SECURITY:` paragraph in the `make_transcripts.py:17-18` register:
`convbase_best.pt` is read with `weights_only=False` because the FULL resume checkpoint carries
pickled optimizer/RNG/numpy objects — a TRUSTED-only read of the project's OWN checkpoint (T-09-11),
never a foreign file. State that every proof check is an explicit `raise SystemExit` and never an
`-O`-strippable `assert`.

Module-level constants: `PROBE_SEEDS = 4` (greedy plus 3 seeded samples per probe question),
`SEED = 1337`, `PROBE_MAX_NEW_TOKENS = 32`, `STOP_IDS = {8184, 8185}`, `SURVIVOR_TARGET = (5, 10)`.

Clobber guard, copied from `measure_inflation.py:66-75` with the names swapped: if `REPORT_PATH`
exists and `--force` is not in `sys.argv[1:]`, read the text after the last `## Verdict` heading and
`raise SystemExit` unless it contains `PENDING`. The message must name the file and say a recorded
verdict is committed evidence (D-06) that `--force` is required to overwrite.

`main()` flow:
1. `preflight_device(strict=True)`, print the summary, `runtime = RuntimeConfig()`,
   `seed_everything(SEED)`.
2. `blob = torch.load(CONVBASE_BEST, weights_only=False)` with the trusted-read comment inline;
   build `GPT(ModelConfig(**blob["model_config"]))`, `load_state_dict(blob["model"])`,
   `.to(runtime.device)`, `.eval()`.
3. `tok = from_json(TOKENIZER_PATH)`;
   `forbid = undecodable_ids_mask(tok, model_cfg.vocab_size).to(device)`.
4. Census pass over all three pools: for every fact, `token_census(tok, fact.value)`. Collect rows of
   `(pool, id, slot, value, n_tokens, roundtrip_ok)`. `raise SystemExit` naming the value if any
   round-trip fails.
5. Guessability pass: for every fact in every pool, for every question in `GATE_PROBES[fact.id]`,
   build the prompt with `build_recall_prompt(tok, question)` and complete it `PROBE_SEEDS` times —
   once greedy, then with a per-probe `torch.Generator` seeded `SEED + probe_index` (the
   `scripts/make_retention_samples.py:8-14` discipline: an early stop in one probe must not shift a
   later probe's stream). Use the `_complete` helper copied verbatim from
   `make_transcripts.py:67-81`, with `max_new_tokens=PROBE_MAX_NEW_TOKENS`, `forbid_ids=forbid`,
   `stop_ids=STOP_IDS`.
6. Verdict per fact: `exact_match_clean(all_completions_for_fact, fact.value)`.
7. Write the report and print the run provenance echo (`finetune_ab.py:322-329` shape) — seed,
   `git_sha()`, `os.getpid()`, wall clock, the `preflight_device` summary, the base fingerprint trio
   read from `blob["git_sha"] / blob["step"] / blob["val_loss"]`.

Report content (`REPORT_PATH`), built as a `blocks` list joined with `"\n".join(...)` and written in
ONE `write_text`. Pre-compose long markdown rows into variables so source lines stay under ruff's
100-char limit (`measure_inflation.py:123-137` idiom). Required sections in order:
- Title, then the Shared-Pattern-6 opener blockquote: **What these numbers are** (a pre-teaching
  measurement of the FROZEN `convbase_best.pt`'s prior mass on each candidate value, probed through
  `build_recall_prompt` — the same call the scoring harness makes) and **What they are not**
  (not a recall measurement, not comparable to any other checkpoint; D-07's checkpoint-specificity
  note belongs here too).
- `## Tokenizer Census (D-02a / D-04)` — one table per pool with columns pool/id/slot/value/tokens/
  round-trip. A sentence under it restating D-04: token count is recorded, never a reject criterion.
  A second sentence recording the measured no-op: all 256 byte ids are live, so PITFALLS-12's
  `forbid_ids` sub-filter cannot fire and is not performed.
- `## Base Guessability Probes (D-02b)` — for every fact, its 4 reserved probe questions and every
  completion verbatim (all `PROBE_SEEDS` per question), so a reader can audit the judgment.
- `## Exact-Match Verdicts (D-03 mechanical floor)` — one row per fact: `0/N contained the value`
  → PASS, otherwise FAIL with the offending completion quoted.
- `## Known Close-Call Triggers (pre-registered)` — the `BASE_PRIOR_SEEDS` table with the sentence
  that these were identified before the pool was authored.
- `## Close-Call Rejections (D-03, human-recorded)` — an empty table with columns
  fact / slot / quoted base completion / reason, and a paragraph stating that every close-call
  rejection MUST quote the specific base completion text that triggered it: a documented judgment,
  never a silent one.
- `## Survivor Count` — core survivors and soft survivors against the D-05 targets (5–8 core,
  2–3 soft) and the ROADMAP's 5–10 total, per pool.
- `## Verdict` ending with exactly `PENDING — user decision at checkpoint.`

Guard `main()` behind `if __name__ == "__main__":`.
  </action>
  <verify>
    <automated>.venv/bin/python -c "
import importlib.util, pathlib, sys
sys.path.insert(0, 'scripts')
s = importlib.util.spec_from_file_location('phase14_factset_gate', 'scripts/phase14_factset_gate.py')
m = importlib.util.module_from_spec(s); s.loader.exec_module(m)
assert m.PROBE_SEEDS == 4 and m.SEED == 1337 and m.STOP_IDS == {8184, 8185}
assert m.REPORT_PATH.name == 'phase14_factset_report.md'
print('driver loads without running main()')" && .venv/bin/ruff check scripts/ && .venv/bin/ruff format --check scripts/</automated>
  </verify>
  <acceptance_criteria>
    - Importing the module via `importlib` runs no training, no model load, and no report write (`main()` is `__main__`-guarded)
    - `grep -c "assert " scripts/phase14_factset_gate.py` returns 0 — every proof is `raise SystemExit`
    - `grep -n "PYTORCH_ENABLE_MPS_FALLBACK" scripts/phase14_factset_gate.py` appears on a line BEFORE the `import torch` line
    - The module docstring contains `SECURITY:` and `weights_only=False`
    - `grep -n "build_recall_prompt" scripts/phase14_factset_gate.py` returns a match; `grep -c "encode_dialogue" scripts/phase14_factset_gate.py` returns 0 — prompts route through the shared function only
    - The clobber-guard branch is reachable: running the script twice without `--force` after a non-PENDING verdict exits non-zero with a message naming `results/phase14_factset_report.md`
    - `.venv/bin/ruff check scripts/ && .venv/bin/ruff format --check scripts/` exits 0
  </acceptance_criteria>
  <done>`scripts/phase14_factset_gate.py` measures the census and the guessability probes on the real frozen base, writes the full report with a PENDING verdict, and refuses to clobber a recorded one.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Run the gate and record the D-06 verdict</name>
  <files>results/phase14_factset_report.md</files>
  <what-built>
`scripts/phase14_factset.py` (16 core + 6 soft real candidates, 10 calibration candidates, 6
register-arm candidates, 4 reserved probes each) and `scripts/phase14_factset_gate.py`, which
measures the tokenizer census and probes the un-adapted `convbase_best.pt` with every reserved
question and writes `results/phase14_factset_report.md`.
  </what-built>
  <action>
Run `.venv/bin/python scripts/phase14_factset_gate.py`, then perform the D-06 human review exactly as laid out in <how-to-verify>: read every base completion in `results/phase14_factset_report.md`, add a `## Close-Call Rejections` row quoting the exact triggering completion for every close-call rejection, check the survivor counts against D-05's 5-8 core / 2-3 soft targets, and replace `PENDING — user decision at checkpoint.` under `## Verdict` with `GO`, `ADAPT` (stating the deviation), or `STOP`. Commit the report. Nothing downstream — no teaching set, no template grammar — may be authored until this verdict is recorded.
  </action>
  <how-to-verify>
1. Run the gate (needs MPS + the 278 MB base checkpoint; expect a few minutes):
   `.venv/bin/python scripts/phase14_factset_gate.py`
   It must exit 0. Any non-zero exit is a `SystemExit` naming a broken contract — fix that first.
2. Open `results/phase14_factset_report.md` and read it end to end.
3. For every fact whose mechanical verdict is PASS, decide whether it is a **close call** under D-03:
   is the base's completion semantically adjacent — same category, plausible alternative value, right
   slot — such that a reader would suspect the base half-knew the answer? The pre-registered trigger
   table (`cop`, `college student`, `the country`, `red`, `rose`) is where to start.
4. For each close-call rejection, add a row to `## Close-Call Rejections` **quoting the exact base
   completion text that triggered it**. A rejection without a quote is not a valid rejection (D-03).
5. Check the `## Survivor Count` section against D-05: 5–8 core survivors and 2–3 soft survivors for
   the real pool; at least 6 for the calibration pool and at least 4 for the register-arm pool.
   If the real pool falls short, that is exactly the information D-06 exists to surface before
   locking — say so rather than relaxing the filter.
6. Replace `PENDING — user decision at checkpoint.` under `## Verdict` with one of:
   - `GO` — the surviving set meets D-05's composition; proceed to plan 14-03.
   - `ADAPT` — proceed with a documented deviation, stated in the verdict section.
   - `STOP` — escalate; the pool must be re-authored and the gate re-run with `--force`.
7. Commit the report.
  </how-to-verify>
  <resume-signal>Type "approved" once the verdict is recorded and committed, or describe what needs to change</resume-signal>
  <verify>
    <automated>grep -A3 '^## Verdict' results/phase14_factset_report.md | grep -Eq '\b(GO|ADAPT|STOP)\b' && echo 'verdict recorded'</automated>
  </verify>
  <acceptance_criteria>
    - `results/phase14_factset_report.md` exists and its `## Verdict` section's first word is `GO`, `ADAPT`, or `STOP` — not `PENDING`
    - Every fact marked rejected in the report has a non-empty quoted base completion in the `## Close-Call Rejections` table or a FAIL row in `## Exact-Match Verdicts`
    - `## Survivor Count` names both the measured survivor count and the D-05 target it is being compared against
    - The report is committed to git
  </acceptance_criteria>
  <done>A human has read every base completion, recorded any close-call rejections with quoted evidence, and written a GO/ADAPT/STOP verdict into the committed report. Nothing downstream may be authored until this is true.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk → gate driver | `checkpoints/convbase_best.pt` is deserialized with `weights_only=False` |
| driver → `results/` | The driver writes committed public evidence |
| candidate values → public repo | Everything in `results/` ships publicly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04 | Elevation of Privilege | `torch.load(CONVBASE_BEST, weights_only=False)` | mitigate | Trusted-own-file read only, stated in the module `SECURITY:` docstring paragraph (T-09-11). Every shareable artifact in this phase goes through `load_slim` / `load_adapter` (`weights_only=True`) instead |
| T-14-05 | Information Disclosure | candidate fact values in `results/` | mitigate | All values are invented/synthetic by construction; the module docstring states no real personal data may enter any pool. `data/`, `checkpoints/`, `logs/`, `*.pt` stay gitignored |
| T-14-06 | Tampering | recorded verdict clobbered by a rerun | mitigate | `measure_inflation.py:66-75` clobber guard — a non-PENDING verdict raises `SystemExit` unless `--force` is passed |
| T-14-07 | Spoofing (of the novel claim) | guessability measurement | mitigate | Probes route through `build_recall_prompt` (the same call the harness makes), greedy plus 3 per-probe-seeded samples, all completions quoted verbatim in the report so the human verdict is auditable |
| T-14-08 | Denial of Service | probe generation loop | mitigate | `PROBE_MAX_NEW_TOKENS = 32` bounded before the loop; `forbid_ids` masks all 7,645 dead ids so the strict decoder can never see an unknown id |
| T-14-SC | Tampering | package installs | accept | Zero new packages (14-RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
- `.venv/bin/python scripts/phase14_factset_gate.py` exits 0 and writes the report
- `.venv/bin/pytest -q` — full suite still green (this plan adds no tests; plan 14-03 adds the permanent one)
- `.venv/bin/ruff check . && .venv/bin/ruff format --check .` — clean
- `results/phase14_factset_report.md` `## Verdict` reads GO, ADAPT, or STOP
</verification>

<success_criteria>
- Three disjoint candidate pools exist, all authored in first-person register, none colliding with the measured base priors
- Every candidate's token count and byte-fallback round-trip is measured, recorded, and used as a census field only
- Every candidate's reserved probe completions from the un-adapted base appear verbatim in the committed report
- A human verdict (GO / ADAPT / STOP) is recorded in the report and committed before any teaching set exists
</success_criteria>

<output>
Create `.planning/phases/14-teach-then-recall-demo/14-02-SUMMARY.md` when done
</output>
