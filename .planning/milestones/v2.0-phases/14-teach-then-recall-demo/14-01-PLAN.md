---
phase: 14-teach-then-recall-demo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/personacore/dialogue/serialize.py
  - src/personacore/dialogue/__init__.py
  - src/personacore/generation/text.py
  - src/personacore/generation/__init__.py
  - tests/test_recall_prompt.py
autonomous: true
requirements: [DEMO-05, DEMO-07]

must_haves:
  truths:
    - "One function builds the clean-room recall prompt, and both the scoring harness and the demo will call it"
    - "A caller can stream decoded text from a supplied id sequence without the string-encode path"
    - "The cumulative yield shape lives in the package, so SC4's monotonic-growth claim has a CI regression test"
    - "The empty-question prompt is exactly [8187, 8185, 8186] — a bare system tag, an empty user turn, a handoff"
  artifacts:
    - path: "src/personacore/dialogue/serialize.py"
      provides: "build_recall_prompt — the D-18 single source of truth for recall prompts"
      contains: "def build_recall_prompt"
    - path: "src/personacore/generation/text.py"
      provides: "generate_text_from_ids + generate_text_from_ids_cumulative — id-space streaming decode (RESEARCH Gap G1)"
      contains: "def generate_text_from_ids"
    - path: "tests/test_recall_prompt.py"
      provides: "CPU-only pins for both new functions"
      min_lines: 90
  key_links:
    - from: "src/personacore/dialogue/serialize.py"
      to: "personacore.tokenizer.special.SPECIAL_TOKENS"
      via: "ASSISTANT_ID resolved from the locked registry, never the literal 8186"
      pattern: "SPECIAL_TOKENS\\[_ASSISTANT\\]"
    - from: "src/personacore/generation/text.py"
      to: "personacore.generation.core.generate"
      via: "id-space core driven from a caller-supplied prompt_ids list"
      pattern: "generate\\(model, idx"
---

<objective>
Ship the two package functions Phase 14 is missing: `build_recall_prompt` (RESEARCH Gap G2) and
`generate_text_from_ids` (RESEARCH Gap G1). Everything downstream in this phase — the fact-set gate,
the scoring harness, and the Gradio demo — routes its prompts through the first and its streaming
through the second.

Purpose: D-18 requires the harness and the UI to call *the same* prompt-construction function.
A shared function must live in the package (the `cap_persona` precedent), not be duplicated in two
scripts. Gap G1 exists because every current text wrapper builds ids as
`[eos_id] + tokenizer.encode(prompt)` — a string-space path that would make the committed context
dump lie about what the model received.

Output: two functions, two barrel updates, one CPU-only test module.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-teach-then-recall-demo/14-PATTERNS.md
@.planning/phases/14-teach-then-recall-demo/14-RESEARCH.md

<interfaces>
<!-- Extracted from the codebase. The executor should use these directly — no exploration needed. -->

From `src/personacore/dialogue/serialize.py` (the file being modified):
- `PERSONA_CAP = 140`
- `detokenize(text) -> str`
- `render_document(persona, turns) -> str`
- `encode_dialogue(tok, persona, turns) -> (ids, mask)` — span-wise; role ids from `SPECIAL_TOKENS`;
  mask semantics: persona=0, first `<|user|>`=0, user content=0, `<|assistant|>` trigger=0,
  assistant content=1, final eos=1
- `cap_persona(tok, persona) -> list[str]` — carries the shared-source-of-truth docstring to copy in register
- module-level imports already present: `from personacore.tokenizer.special import EOS_ID, SPECIAL_TOKENS`
- module-level names already present: `_USER = "<|user|>"`, `_ASSISTANT = "<|assistant|>"`, `_SYSTEM = "<|system|>"`

From `src/personacore/tokenizer/special.py`:
- `SPECIAL_TOKENS` — name→id dict; `SPECIAL_TOKENS["<|assistant|>"] == 8186`, `<|user|>` 8185,
  `<|system|>` 8187; `EOS_ID == 8184`

From `src/personacore/generation/text.py` (the file being modified):
- `DEFAULT_MAX_NEW_TOKENS_CAP = 4096`
- `undecodable_ids_mask(tokenizer, vocab_size) -> (1, vocab_size) bool tensor`
- `_model_device(model) -> torch.device`
- `generate_text(model, tokenizer, prompt, *, eos_id=None, max_new_tokens, max_new_tokens_cap=..., **gen_kw)`
  — yields NEW-text suffixes; contains the bounds guard (lines ~90-93) and the cumulative-decode
  block with the `UnicodeDecodeError: continue` idiom (lines ~100-116)
- `generate_text_cumulative(model, tokenizer, prompt, *, max_new_tokens, **gen_kw)`

From `src/personacore/generation/core.py`:
- `generate(model, idx, *, eos_id=None, max_new_tokens, **gen_kw)` — yields ids; `gen_kw` accepts
  `temperature`, `top_k`, `top_p`, `greedy`, `generator`, `block_size`, `forbid_ids`, `stop_ids`
- `collect(...)` — non-streaming tensor form
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add build_recall_prompt to dialogue/serialize.py and export it</name>
  <files>src/personacore/dialogue/serialize.py, src/personacore/dialogue/__init__.py</files>
  <read_first>
    - src/personacore/dialogue/serialize.py (full — copy `cap_persona`'s docstring register verbatim in tone; reuse the module's existing `_ASSISTANT` constant and `SPECIAL_TOKENS` import)
    - src/personacore/dialogue/__init__.py (full — the barrel's docstring uses the "Plan 11-01 ships …; Plan 11-03 adds …" form; `__all__` is alphabetically sorted)
    - .planning/phases/14-teach-then-recall-demo/14-RESEARCH.md (§Gaps in Existing Code → G2 for the exact 3-line body; §Measured Findings F2 for the id shape)
  </read_first>
  <action>
Add a module-level `ASSISTANT_ID = SPECIAL_TOKENS[_ASSISTANT]` beside the existing `PERSONA_CAP`
constant in `src/personacore/dialogue/serialize.py`, then add
`build_recall_prompt(tok, question, persona=())` returning `list[int]`.

Body: call `encode_dialogue(tok, list(persona), [(question, "")])`, discard the mask, and return the
id prefix truncated to END at the assistant tag — `ids[: ids.index(ASSISTANT_ID) + 1]`. Resolve the
assistant id through `ASSISTANT_ID` (which comes from the locked `SPECIAL_TOKENS` registry); never
write the literal 8186. The default `persona=()` produces a bare `<|system|>` with zero content —
the strongest form of "empty prompt": the context carries the question and nothing else.

Docstring requirements, in the register of `cap_persona`'s docstring (which is the strongest analog
in this phase — it already *is* the D-18 argument written for a different pair of callers):
- one summary line naming the shape it returns (`<|system|>[persona] <|user|>question <|assistant|>` — ids only);
- a "SINGLE source of truth" paragraph naming `scripts/phase14_recall.py` and
  `scripts/personalize_demo.py` as the two callers that both import THIS function, so the demo's
  live token panel and the harness's committed context dump can never diverge from what the model
  actually receives — by construction, never by convention. Cite D-18 and Pitfall 4;
- a sentence stating that `generate_text`'s `[eos_id] + tokenizer.encode(prompt)` path is FORBIDDEN
  for recall because it builds a prompt shape the model was never trained on.

Then update `src/personacore/dialogue/__init__.py`: add `build_recall_prompt` to the
`from .serialize import ...` line and to the alphabetically-sorted `__all__`, and append one sentence
to the module docstring in the established form — "Plan 14-01 adds `build_recall_prompt` (the D-18
clean-room recall prompt, shared by the scoring harness and the Gradio demo)."
  </action>
  <verify>
    <automated>.venv/bin/python -c "from personacore.dialogue import build_recall_prompt; from personacore.tokenizer import from_json; t=from_json('artifacts/tokenizer.json'); p=build_recall_prompt(t,''); assert p==[8187,8185,8186], p; q=build_recall_prompt(t,\"what is your dog's name?\"); assert q[0]==8187 and q[1]==8185 and q[-1]==8186 and len(q)==19, q; print('ok', len(q))"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "ASSISTANT_ID = SPECIAL_TOKENS" src/personacore/dialogue/serialize.py` returns a match
    - `grep -c "8186" src/personacore/dialogue/serialize.py` returns 0 (the literal never appears)
    - `build_recall_prompt(tok, "")` returns exactly `[8187, 8185, 8186]`
    - `build_recall_prompt(tok, "what is your dog's name?")` returns a 19-element list starting `[8187, 8185, ...]` and ending `8186` (14-RESEARCH F2)
    - The function's docstring contains the strings `phase14_recall.py`, `personalize_demo.py`, and `D-18`
    - `python -c "import personacore.dialogue as d; assert d.__all__ == sorted(d.__all__); assert 'build_recall_prompt' in d.__all__"` exits 0
    - `.venv/bin/ruff check src/personacore/dialogue/` exits 0
  </acceptance_criteria>
  <done>`build_recall_prompt` exists in `serialize.py`, is exported from the `personacore.dialogue` barrel, resolves the assistant id from the registry, and returns the measured id sequences for both the empty and the populated question.</done>
</task>

<task type="auto">
  <name>Task 2: Add generate_text_from_ids to generation/text.py and export it</name>
  <files>src/personacore/generation/text.py, src/personacore/generation/__init__.py</files>
  <read_first>
    - src/personacore/generation/text.py (full — `generate_text` lines ~62-116 are the template; copy the bounds guard and the cumulative-decode block verbatim, replacing only the prompt-ids construction)
    - src/personacore/generation/__init__.py (full — barrel shape and `__all__` ordering)
    - .planning/phases/14-teach-then-recall-demo/14-PATTERNS.md (§`src/personacore/generation/text.py` — names the exact line that must NOT be copied)
  </read_first>
  <action>
Add `generate_text_from_ids(model, tokenizer, prompt_ids, *, eos_id=None, max_new_tokens,
max_new_tokens_cap=DEFAULT_MAX_NEW_TOKENS_CAP, **gen_kw)` to
`src/personacore/generation/text.py`, immediately after `generate_text`. It is `generate_text`'s body
with exactly one line changed.

Keep verbatim from `generate_text`:
- `eid = eos_id if eos_id is not None else model.config.eos_id`
- the bounds guard raising `ValueError(f"max_new_tokens must be in (0, {max_new_tokens_cap}], got {max_new_tokens!r}")` BEFORE the loop (V5 / T-06-04 DoS guard; `tests/test_demo_callback.py::test_kwargs_thread_through` pins that it fires through wrappers)
- `idx = torch.tensor([prompt_ids], dtype=torch.long, device=_model_device(model))`
- `model.eval()`
- the whole cumulative-decode block: `emitted`/`buffer_ids`, `for tok in generate(...)`, the
  `try: text = tokenizer.decode(buffer_ids) / except UnicodeDecodeError: continue` guard with its
  Pitfall-3 comment, `new = text[len(emitted):]`, `emitted = text`, `if new: yield new`

Do NOT copy `prompt_ids = [eid] + (tokenizer.encode(prompt) if prompt else [])`. The caller supplies
`prompt_ids` as an already-built list of ints — accept it unchanged and do not prepend anything.

Then add `generate_text_from_ids_cumulative(model, tokenizer, prompt_ids, *, max_new_tokens,
**gen_kw)` immediately after it — the 4-line adapter that accumulates each delta into a running
string and yields the running string, copied in shape from `generate_text_cumulative`
(`text.py:128-147`). Its docstring states why both shapes exist, in the register the existing pair
already uses: the delta form is the composable producer, the cumulative form is the Gradio yield
shape, because Gradio REPLACES the displayed message with each yield (08-RESEARCH Pitfall 1).

**Why the cumulative sibling ships in the package rather than living in the demo callback (W-10).**
14-RESEARCH's Architectural Responsibility Map says "Package for every callback's logic," and
`tests/test_demo_callback.py:1-10` records the M1 precedent explicitly: the testable demo slice lives
in the package so CI covers it WITHOUT the demo extra installed. The demo prepends a constant
per-turn stamp to the accumulated string — the stamp PREFIXES the cumulative shape, it does not
discard it — so the callback stays thin. Keeping the accumulation inside `on_ask` would leave SC4's
"streams and grows monotonically" with no regression test at all.

Docstring requirements:
- name RESEARCH Gap G1 and state why the string wrappers are wrong here: the clean-room prompt is an
  `encode_dialogue` id sequence beginning `[8187, 8185, …]`, and the `[eos_id] + encode(prompt)`
  path would build a prompt the model was never trained on and make the committed context dump lie;
- state that `prompt_ids` is expected to come from `personacore.dialogue.build_recall_prompt`;
- state that only the continuation is yielded (the prompt is never decoded back out — D-02) and that
  the core stops on a stop id WITHOUT yielding it (D-05).

Then update `src/personacore/generation/__init__.py`: add `generate_text_from_ids` to the
`from .text import ...` line and to the alphabetically-sorted `__all__`, and extend the module
docstring's re-export sentence to name it.
  </action>
  <verify>
    <automated>.venv/bin/pytest -q tests/test_generation_text.py tests/test_demo_callback.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def generate_text_from_ids" src/personacore/generation/text.py` returns 2 — the delta form and the cumulative sibling
    - `grep -n "tokenizer.encode" src/personacore/generation/text.py | grep -c generate_text_from_ids` returns 0 — the new function never calls `tokenizer.encode`
    - Calling with `max_new_tokens=0` and with `max_new_tokens=4097` each raise `ValueError` whose message contains `(0, 4096]`
    - The function's docstring contains `G1` and `build_recall_prompt`
    - `python -c "import personacore.generation as g; assert g.__all__ == sorted(g.__all__); assert {'generate_text_from_ids','generate_text_from_ids_cumulative'} <= set(g.__all__)"` exits 0
    - `.venv/bin/pytest -q tests/test_generation_text.py tests/test_demo_callback.py -x` passes (existing wrappers unregressed)
  </acceptance_criteria>
  <done>`generate_text_from_ids` streams decoded deltas from a caller-supplied id list, keeps the bounds guard and the cumulative-decode crash guard, and is exported from the `personacore.generation` barrel.</done>
</task>

<task type="auto">
  <name>Task 3: Create tests/test_recall_prompt.py</name>
  <files>tests/test_recall_prompt.py</files>
  <read_first>
    - tests/test_demo_callback.py (lines 1-130 — the `_tiny_model` / `_force_sequence` / `_RecordingTokenizer` fixture block and the cumulative-stream assertions; the header docstring states the gradio-free house convention)
    - tests/test_dialogue_serialize.py (lines 1-80 — the module-scoped frozen-tokenizer fixture and the "what is pinned and why" docstring form)
    - src/personacore/dialogue/serialize.py (as written in Task 1)
    - src/personacore/generation/text.py (as written in Task 2)
  </read_first>
  <action>
Create `tests/test_recall_prompt.py` covering both new functions. CPU-only, GPU-free, checkpoint-free.

Module docstring: state what is pinned (the clean-room prompt shape and the id-space streaming
contract) and why, and close with the CPU-only line, matching `tests/test_dialogue_serialize.py`'s
header form.

Prompt half — uses the REAL frozen tokenizer (`artifacts/tokenizer.json` is git-tracked, so this
runs in CI). Reuse `test_dialogue_serialize.py`'s module-scoped `tok()` fixture shape verbatim,
including its "the FROZEN production artifact — never a freshly trained tokenizer (Pitfall 6)"
comment. Tests:
- `test_empty_question_scaffold` — `build_recall_prompt(tok, "")` equals the literal `[8187, 8185, 8186]`
- `test_prompt_ends_at_assistant` — for three question strings, the last element equals
  `SPECIAL_TOKENS["<|assistant|>"]` and it appears exactly once in the returned list
- `test_prompt_is_bare_system` — the first two elements are the literals `8187, 8185`, and no
  element between them exists (an empty persona span)
- `test_no_value_leaks_into_prompt` — for a question and a candidate value that is NOT in the
  question, the value's normalized string is absent from `tok.decode(prompt_ids)` and the value's
  encoded id sequence is not a contiguous subsequence of `prompt_ids`
- `test_persona_arg_appends_span` — passing a one-line persona makes the prompt strictly longer and
  still ends at the assistant tag (the argument exists only for the explicitly-labelled D-11.1
  fairness control; the recall path never uses it)

Streaming half — uses the tiny-GPT fixture block. Copy `_tiny_model`, `_force_sequence`, and
`_RecordingTokenizer` from `tests/test_demo_callback.py` unchanged (that file's docstring says the
block must not diverge from `tests/test_generation_text.py`'s copy). Tests:
- `test_streams_cumulative_prefix_free_deltas` — the joined deltas equal the full decoded
  continuation, and no delta contains any prompt character (the prompt is never decoded back out)
- `test_cumulative_grows_monotonically` — for `generate_text_from_ids_cumulative`, every yield starts
  with the previous yield (`nxt.startswith(prev)`) and the final yield equals the joined deltas,
  copied from `tests/test_demo_callback.py:97-127`. This is the CI regression test behind SC4's
  "streams and grows monotonically" claim
- `test_prompt_ids_used_verbatim` — the `_RecordingTokenizer`'s `encode` is never called
  (assert its recorded call list is empty), proving the string path is not taken
- `test_bounds_guard` — `max_new_tokens=0` and `max_new_tokens=4097` each raise `ValueError`
  before any forward pass (assert the forced-sequence counter is still 0)
- `test_gen_kwargs_thread_through` — pass `greedy=True` and a `forbid_ids` mask and assert the
  forbidden id never appears in the emitted ids
  </action>
  <verify>
    <automated>.venv/bin/pytest -q tests/test_recall_prompt.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest -q tests/test_recall_prompt.py -x` passes with at least 10 tests collected
    - `grep -c "import gradio" tests/test_recall_prompt.py` returns 0
    - `grep -c "checkpoints/" tests/test_recall_prompt.py` returns 0 — no checkpoint dependency
    - `grep -c "skipif" tests/test_recall_prompt.py` returns 0 — every assertion runs in CI
    - `.venv/bin/pytest -q` (full suite) passes
    - `.venv/bin/ruff check . && .venv/bin/ruff format --check .` exits 0
  </acceptance_criteria>
  <done>`tests/test_recall_prompt.py` pins the empty-question scaffold, the assistant-tag truncation, the no-value-leakage property, and the id-space streaming contract — all CPU-only, all unskipped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → `build_recall_prompt` | The `question` string is developer-authored in this phase, but the function is the sole gate that decides what enters the model's context — the clean-room claim rests on it |
| caller → `generate_text_from_ids` | `max_new_tokens` crosses from a Gradio slider in the demo; `prompt_ids` crosses from the harness |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Spoofing (of the novel claim) | `build_recall_prompt` | mitigate | One function, two callers, ids only. The `test_no_value_leaks_into_prompt` test asserts both string-level and token-level absence, so a prompt builder that quietly admitted a fact value would fail in CI |
| T-14-02 | Denial of Service | `generate_text_from_ids` | mitigate | The `(0, max_new_tokens_cap]` guard is copied verbatim from `generate_text` and fires BEFORE the loop; `test_bounds_guard` asserts no forward pass happens on rejection |
| T-14-03 | Tampering | `ASSISTANT_ID` resolution | mitigate | Resolved from the locked `SPECIAL_TOKENS` registry, never the literal 8186; asserted by `grep -c "8186" == 0` on the source |
| T-14-SC | Tampering | package installs | accept | This plan installs nothing. 14-RESEARCH's Package Legitimacy Audit records zero new packages for the phase; every import already exists in `pyproject.toml` |
</threat_model>

<verification>
- `.venv/bin/pytest -q` — full suite green (286 existing tests + the new module)
- `.venv/bin/ruff check . && .venv/bin/ruff format --check .` — clean
- `python -c "from personacore.dialogue import build_recall_prompt; from personacore.generation import generate_text_from_ids"` — both exported
</verification>

<success_criteria>
- `build_recall_prompt(tok, "")` returns exactly `[8187, 8185, 8186]`
- `build_recall_prompt(tok, "what is your dog's name?")` returns a 19-id sequence ending at 8186
- `generate_text_from_ids` never calls `tokenizer.encode` and never prepends `eos_id`, and its cumulative sibling yields strictly growing prefixes
- Both functions are exported from their package barrels with alphabetically-sorted `__all__`
- `tests/test_recall_prompt.py` has zero `skipif` markers — every assertion runs in CI
</success_criteria>

<output>
Create `.planning/phases/14-teach-then-recall-demo/14-01-SUMMARY.md` when done
</output>
