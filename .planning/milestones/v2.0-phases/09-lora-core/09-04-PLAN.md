---
phase: 09-lora-core
plan: 04
type: execute
wave: 3
depends_on: [09-01, 09-03]
files_modified:
  - tests/test_lora_training.py
  - scripts/train_adapter_smoke.py
autonomous: true
requirements: [LORA-02, LORA-05]

must_haves:
  truths:
    - "After adapter-training steps through the UNTOUCHED v1.0 train(), every param whose name contains lora_ has changed and every base param is bit-identical (torch.equal) to its pre-training snapshot"
    - "The train()-saved checkpoint's optimizer state holds entries only for the stepped A/B params (no state bloat for the frozen base)"
    - "Kill+resume of an adapter run reproduces the uninterrupted trajectory (final loss within 1e-6, lora params within 1e-6) via the deterministic vanilla→inject→freeze reconstruction order"
    - "lora_config rides the open-dict checkpoint **extra seam and reloads intact (D-04)"
    - "The smoke script trains best.pt + TinyStories bins on the preflight device, passes the params-actually-update canary on real weights, and exports a ~1.3 MB adapter.pt with the base fingerprint"
  artifacts:
    - path: "tests/test_lora_training.py"
      provides: "LORA-02 pins (canary, frozen-base bit-untouched, optimizer-state scope, kill+resume, **extra seam)"
      min_lines: 80
    - path: "scripts/train_adapter_smoke.py"
      provides: "Real-weights MPS smoke: best.pt -> inject -> freeze -> ~50 steps -> canary -> export_adapter"
      contains: "WEIGHT_DECAY = 0.0"
  key_links:
    - from: "scripts/train_adapter_smoke.py"
      to: "src/personacore/training/loop.py"
      via: "keyword-only train() call — loop NEVER modified"
      pattern: "train\\("
    - from: "scripts/train_adapter_smoke.py"
      to: "src/personacore/checkpoint.py"
      via: "export_adapter with base fingerprint read from best.pt (never recomputed)"
      pattern: "export_adapter"
    - from: "tests/test_lora_training.py"
      to: "src/personacore/lora/inject.py"
      via: "snapshot_params canary helper"
      pattern: "snapshot_params"
---

<objective>
Prove the frozen-base training discipline on the untouched v1.0 `train()`: CPU unit tests pin
grad isolation (canary + bit-untouched base), optimizer-state scope, the D-04 kill+resume
contract, and the `lora_config` **extra seam; then a thin gitignored smoke script proves the
same canary on the REAL `best.pt` + TinyStories bins on MPS and exports the first adapter.pt
persona file.

Purpose: LORA-02 end-to-end plus ROADMAP success criterion 5's "canary passes on a smoke run"
(LORA-05). This is where the MPS silent-failure class (PITFALLS P5) is actually hunted — on
real weights, on the real device — and where the ~1.3 MB persona-file story becomes a printed,
measurable artifact.

Output: `tests/test_lora_training.py` green on CPU; `scripts/train_adapter_smoke.py` exits 0
locally with checkpoints/adapter.pt exported (gitignored); full suite stays green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-lora-core/09-CONTEXT.md
@.planning/phases/09-lora-core/09-RESEARCH.md
@.planning/phases/09-lora-core/09-PATTERNS.md
@.planning/phases/09-lora-core/09-01-SUMMARY.md
@.planning/phases/09-lora-core/09-03-SUMMARY.md

<interfaces>
<!-- Key contracts extracted from the codebase. Use directly — no exploration needed. -->

From src/personacore/training/loop.py (NEVER edited — lines 150-174, keyword-only):
```python
def train(*, train_config, runtime_config=None, model=None, model_config=None,
          corpus_path=None, train_bin=None, val_bin=None, eos_id=8184, fixed_batch=None,
          scaler=None, resume_from=None, checkpoint_path=None, best_checkpoint_path=None,
          log_path=None, max_steps_override=None, eval_interval=1, checkpoint_interval=None,
          sample_interval=None, sample_prompt=None, tokenizer=None,
          sample_max_new_tokens=64, return_final_loss=False):
# line 234: optimizer = torch.optim.AdamW(model.parameters(), lr=..., weight_decay=...)
#   -> fresh optimizer per call; VERIFIED (09-RESEARCH): frozen (requires_grad=False) params get
#      no step, no decoupled weight decay, and NO optimizer-state entries.
# fixed_batch=(xb, yb) reuses one batch every step; train_bin/val_bin take memmap .bin PATHS.
# checkpoint_path: open-dict latest.pt saved end-of-call always (+ in-loop per checkpoint_interval).
```

From src/personacore/config.py: `TrainConfig` defaults include `weight_decay: float = 0.1`
(line 107 — adapter runs MUST override to 0.0) and `seed: int = 1337`.

From src/personacore/lora (09-01): `inject_lora`, `mark_only_lora_trainable`,
`snapshot_params(model) -> dict[str, Tensor]`, `lora_state_dict`, `LoRAConfig`.

From src/personacore/checkpoint.py (09-03): 
```python
export_adapter(path, *, adapter, lora_config, base_fingerprint) -> dict
save_checkpoint(path, *, model, optimizer, scheduler, step, model_config, train_config,
                git_sha, scaler=None, val_loss=None, **extra) -> None   # **extra = D-04 seam
load_checkpoint(path, *, model, optimizer=None, ...) -> dict            # returns extra keys too
```

From tests/test_resume_curve.py (lines 38-79 — the kill+resume template): reference
uninterrupted run vs half-run + checkpoint + fresh-build + resume_from; asserts
`abs(float(resumed) - float(ref)) < 1e-6` and param `torch.allclose(atol=1e-6)`.

From scripts/pretrain_tinystories.py: `os.environ.setdefault("PYTORCH_ENABLE_MPS_FALLBACK", "1")`
BEFORE `import torch` (lines 30-34); `_REPO_ROOT` no-CLI path constants (43-49); named tuned
constants block (51-59); main() = preflight_device(strict=True) -> FileNotFoundError on missing
inputs -> seed_everything -> configs -> train(...) keyword call; post-run own-file read
`torch.load(BEST_PATH, weights_only=False)` (trusted-only, lines 116-127).

CRITICAL canary math (derived from the 09-01 forward contract): with B=0 at init, dL/dA flows
THROUGH B, so lora_A's gradient is identically ZERO on the first optimizer step — and with
weight_decay=0.0 the A matrices are bit-unchanged after exactly 1 step. The canary therefore
needs >= 2 effective (nonzero-lr) steps: step 1 moves B off zero, step >= 2 moves A.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Grad-isolation, optimizer-scope, kill+resume, and **extra seam tests</name>
  <files>tests/test_lora_training.py</files>
  <read_first>
    - tests/test_resume_curve.py (FULL file — the kill+resume trajectory template to adapt; seed_everything + _build + max_steps_override idioms)
    - src/personacore/training/loop.py (train() signature lines 150-174 + AdamW construction 234-236 — consumed verbatim, NEVER edited)
    - src/personacore/lora/inject.py (snapshot_params, inject_lora, mark_only_lora_trainable from 09-01)
    - src/personacore/checkpoint.py (save_checkpoint **extra seam + load_checkpoint return dict)
    - .planning/phases/09-lora-core/09-RESEARCH.md (Pattern 3 — empirically verified AdamW frozen-param behavior; "Params-actually-update canary" code example lines 407-419)
    - src/personacore/config.py (TrainConfig fields — weight_decay default 0.1 must be overridden)
  </read_first>
  <behavior>
    tests/test_lora_training.py (write FIRST; CPU-only, GPU-free; module docstring citing
    LORA-02/D-04 and the B=0-makes-A's-first-step-grad-zero caveat; shared helpers:
    _tiny_cfg() = ModelConfig(block_size=32, n_layer=1, n_head=2, n_embd=16);
    _build_injected(lora_cfg) = seeded vanilla GPT -> inject_lora -> mark_only_lora_trainable
    (the deterministic reconstruction order — identical on resume);
    _fixed_batch(cfg) = (torch.randint(0, cfg.vocab_size, (4, cfg.block_size)),
    torch.randint(0, cfg.vocab_size, (4, cfg.block_size)));
    adapter TrainConfig = TrainConfig(lr=1e-2, warmup_steps=2, max_steps=6, batch_size=4,
    weight_decay=0.0) — weight_decay MUST be 0.0: the 0.1 default would decay frozen-adjacent
    dynamics AND fights low-rank updates (RESEARCH Pattern 3)):
    - Test 1 (canary + grad isolation — LORA-02, the phase's load-bearing training test):
      seed_everything(1234); model = _build_injected(LoRAConfig(r=4)); before =
      snapshot_params(model); train(train_config=adapter_cfg, model=model, model_config=_tiny_cfg(),
      fixed_batch=_fixed_batch(...)); after the run, for EVERY named parameter:
      if p.requires_grad: assert not torch.equal(p, before[n]) (trainable moved — canary);
      else: assert torch.equal(p, before[n]) (frozen base bit-untouched — LORA-02's core claim).
      The run is 6 steps precisely so A provably moves (>= 2 effective steps; see module docstring).
    - Test 2 (optimizer state scoped to A/B): same setup with checkpoint_path=tmp_path/"latest.pt";
      after train(), ckpt = torch.load(path, weights_only=False) (own trusted file);
      assert len(ckpt["optimizer"]["state"]) == 12 * cfg.n_layer (== 2 lora tensors x 6 wrapped
      modules; 12 for the tiny fixture) — AdamW held moments ONLY for stepped params
      (RESEARCH-verified behavior, now pinned against regression).
    - Test 3 (kill+resume identical trajectory — D-04, adapting test_resume_curve.py):
      REFERENCE: seed_everything(1234); ref_model = _build_injected(...); ref_loss =
      train(..., max_steps=6, return_final_loss=True); ref_B = first LoRALinear's
      lora_B.detach().clone(). RESUMED: seed_everything(1234); half_model = _build_injected(...)
      (SAME construction order -> same RNG consumption -> AdamW param indices re-associate
      deterministically); train(..., max_steps_override=3, checkpoint_path=ckpt_path); then
      fresh_model = _build_injected(...) and resumed_loss = train(..., resume_from=ckpt_path,
      return_final_loss=True); assert abs(float(resumed_loss) - float(ref_loss)) < 1e-6 and
      resumed lora_B torch.allclose(ref_B, atol=1e-6).
    - Test 4 (lora_config rides **extra — D-04 open-dict seam): save_checkpoint(tmp, model=model,
      optimizer=torch.optim.AdamW(model.parameters()), scheduler=None, step=1,
      model_config=_tiny_cfg(), train_config=adapter_cfg, git_sha="testsha",
      lora_config=asdict(LoRAConfig(r=4))); reload via torch.load(weights_only=False); assert
      ckpt["lora_config"]["r"] == 4 and tuple(ckpt["lora_config"]["targets"]) == TARGET_PROJECTIONS
      — the resume path can rebuild the module tree from the checkpoint alone.
  </behavior>
  <action>
    Create tests/test_lora_training.py per the behaviors above. Imports: seed_everything from
    personacore.seeding, train from personacore.training (or personacore.training.loop — match
    test_resume_curve.py's import), GPT from personacore.model, ModelConfig/TrainConfig from
    personacore.config, save_checkpoint from personacore.checkpoint, inject_lora/
    mark_only_lora_trainable/snapshot_params/LoRAConfig/TARGET_PROJECTIONS from personacore.lora.
    Keep train() and loop.py BYTE-UNTOUCHED — the entire point is that the v1.0 loop is already
    frozen-param safe (Open Q1 resolved: no checkpoint_extra kwarg in Phase 9; the caller knows its
    LoRAConfig). Document the >= 2-effective-steps canary requirement in the module docstring so a
    future refactor to a 1-step canary fails loudly in review, not silently in CI.
    Run .venv/bin/pytest tests/test_lora_training.py -q -x — green (CPU, seconds). Commands run
    inside .venv (Python 3.11).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_lora_training.py -q -x</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_lora_training.py contains the bit-level frozen assert `torch.equal` applied to params with requires_grad False, and a `not torch.equal` canary for trainables
    - tests/test_lora_training.py contains `weight_decay=0.0` (adapter-run override of the 0.1 default)
    - tests/test_lora_training.py contains `resume_from` and `max_steps_override` (the kill+resume pair) and an `abs(` final-loss comparison `< 1e-6`
    - tests/test_lora_training.py asserts the optimizer-state census: grep -q 'optimizer..\?..state' tests/test_lora_training.py succeeds and the count 12 appears
    - tests/test_lora_training.py contains `lora_config` passed through save_checkpoint **extra and read back
    - src/personacore/training/loop.py is unmodified: git diff --name-only includes no training/ paths after this task
    - `.venv/bin/pytest tests/test_lora_training.py -q -x` exits 0 with >= 4 tests collected
  </acceptance_criteria>
  <done>Grad isolation is test-pinned at bit level through the untouched v1.0 train(), optimizer state provably covers only A/B, adapter kill+resume reproduces the uninterrupted trajectory, and lora_config round-trips the open-dict seam</done>
</task>

<task type="auto">
  <name>Task 2: Real-weights adapter smoke script (best.pt + TinyStories bins, MPS canary, adapter export)</name>
  <files>scripts/train_adapter_smoke.py</files>
  <read_first>
    - scripts/pretrain_tinystories.py (FULL file — the primary analog: MPS env guard, _REPO_ROOT constants, tuned-constants block, main() skeleton, trusted post-run load)
    - scripts/export_slim.py (secondary analog — FileNotFoundError remedy style, print-what-shipped tail with MB size)
    - .planning/phases/09-lora-core/09-PATTERNS.md (scripts/train_adapter_smoke.py section — the composed skeleton including the Phase-9 insertion point)
    - src/personacore/checkpoint.py (export_adapter signature from 09-03; load semantics)
    - src/personacore/preflight.py (preflight_device) and src/personacore/seeding.py (seed_everything)
    - tests/test_lora_training.py (Task 1 — the canary assertion shape being reused on real weights)
  </read_first>
  <action>
    Create scripts/train_adapter_smoke.py — a THIN no-CLI driver (logic lives in the package):
    - Module docstring: the real-weights LORA-02/LORA-05 proof — this is where PITFALLS P5's MPS
      silent-failure class is hunted (canary on the actual training device); outputs are gitignored;
      resume note: a killed run resumes by re-running this script with resume_from semantics — the
      script's own LORA_CFG constant rebuilds the module tree (Open Q1 resolution; train() is never
      modified).
    - `os.environ.setdefault("PYTORCH_ENABLE_MPS_FALLBACK", "1")` BEFORE `import torch`
      (pretrain_tinystories.py lines 30-34 precedent, with the same noqa: E402 comment).
    - _REPO_ROOT path constants: BEST_PATH = checkpoints/best.pt, TRAIN_BIN = data/train.bin,
      VAL_BIN = data/val.bin, SMOKE_CKPT = checkpoints/adapter_smoke.pt, ADAPTER_PATH =
      checkpoints/adapter.pt, LOG_PATH = logs/adapter_smoke.csv (a NEW CSV — NEVER logs/run.csv,
      ARCHITECTURE anti-pattern 3).
    - Tuned constants block: LORA_CFG = LoRAConfig() (production defaults r=8/alpha=16.0 — the
      ~1.3 MB persona file), LR = 1e-3, WARMUP_STEPS = 5, MAX_STEPS = 50, BATCH_SIZE = 8,
      WEIGHT_DECAY = 0.0 (literal, with the adapter-run rationale comment).
    - main():
      1. runtime = preflight_device(strict=True)-backed RuntimeConfig (pretrain script precedent);
         FileNotFoundError with a "run scripts/pretrain_tinystories.py first" remedy if BEST_PATH
         or the bins are missing; seed_everything(1337).
      2. blob = torch.load(BEST_PATH, weights_only=False) — OWN trusted file (same comment as
         pretrain script's post-run read); model = GPT(ModelConfig(**blob["model_config"]));
         model.load_state_dict(blob["model"]) — LOAD BEFORE INJECT (the load-bearing ordering).
      3. n = inject_lora(model, LORA_CFG); assert n == 6 * blob["model_config"]["n_layer"] (== 36);
         mark_only_lora_trainable(model); assert the trainable census == LORA_CFG.r *
         blob["model_config"]["n_layer"] * 18 * blob["model_config"]["n_embd"] (== 331,776).
      4. model.to(runtime.device) BEFORE snapshot_params(model) — torch.equal raises on
         cross-device tensors, so snapshot and post-run params must share a device; before =
         snapshot_params(model).
      5. final = train(train_config=TrainConfig(lr=LR, warmup_steps=WARMUP_STEPS,
         max_steps=MAX_STEPS, batch_size=BATCH_SIZE, weight_decay=WEIGHT_DECAY),
         runtime_config=runtime, model=model, model_config=ModelConfig(**blob["model_config"]),
         train_bin=TRAIN_BIN, val_bin=VAL_BIN, log_path=LOG_PATH, checkpoint_path=SMOKE_CKPT,
         return_final_loss=True); assert math.isfinite(float(final)) (PITFALLS P5 NaN guard).
      6. CANARY on real weights (inline asserts — the script IS the proof; exits non-zero on
         failure per RESEARCH Open Q2): for every named param, requires_grad -> not torch.equal
         vs before[n]; frozen -> torch.equal (base bit-untouched at 13.9M scale).
      7. export_adapter(ADAPTER_PATH, adapter=lora_state_dict(model),
         lora_config=asdict(LORA_CFG), base_fingerprint={"git_sha": blob["git_sha"],
         "step": blob["step"], "val_loss": blob["val_loss"]}) — fingerprint READ from the base
         checkpoint, never recomputed (provenance-trio house rule, D-02).
      8. Print tail (export_slim style, "[train_adapter_smoke]" prefix): adapter path + size in MB
         (expected ~1.3 MB — the persona-file story made visible) + final loss.
    - if __name__ == "__main__": main().
    Run it: .venv/bin/python scripts/train_adapter_smoke.py (50 steps on MPS/CPU — minutes at
    most). Confirm exit 0, adapter.pt exists and is gitignored, and no new untracked files appear.
  </action>
  <verify>
    <automated>.venv/bin/python scripts/train_adapter_smoke.py && ls -la checkpoints/adapter.pt && git check-ignore -q checkpoints/adapter.pt logs/adapter_smoke.csv && make test</automated>
  </verify>
  <acceptance_criteria>
    - scripts/train_adapter_smoke.py sets PYTORCH_ENABLE_MPS_FALLBACK before importing torch: the env line appears earlier in the file than `import torch`
    - The script contains `WEIGHT_DECAY = 0.0`, `weights_only=False` with a trusted-own-file comment, and `export_adapter`
    - The script never references logs/run.csv: grep -q "run.csv" scripts/train_adapter_smoke.py finds no match
    - The script asserts the 331,776 census formula: grep -q "18" scripts/train_adapter_smoke.py succeeds, and asserts inject count == 6 * n_layer
    - `.venv/bin/python scripts/train_adapter_smoke.py` exits 0 and prints the adapter size in MB
    - checkpoints/adapter.pt exists after the run, sized roughly 1.3 MB (1.0-1.7 MB acceptable), and `git check-ignore` confirms adapter.pt and the smoke CSV are gitignored
    - `make test` exits 0 (the script is new and imports only; full suite unaffected)
  </acceptance_criteria>
  <done>The smoke script proves the canary and bit-untouched base on real best.pt weights over TinyStories bins on the preflight device, and exports a fingerprinted ~1.3 MB adapter.pt persona file; all outputs gitignored</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| best.pt → full pickle load | the smoke script loads the FULL training checkpoint with weights_only=False |
| training run → repo working tree | run outputs (checkpoints, CSV, adapter) must never enter version control |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-11 | Elevation of privilege | torch.load(best.pt, weights_only=False) | accept | TRUSTED-ONLY read of the project's own checkpoint (checkpoint.py module-docstring contract; pretrain script precedent) — documented inline; the SHAREABLE artifact path stays weights_only=True via export_adapter/load_adapter |
| T-09-12 | Information disclosure | run outputs committed to repo | mitigate | all outputs under gitignored checkpoints/ + logs/; verify step runs git check-ignore; smoke trains on public TinyStories only — no personal data exists in Phase 9 |
| T-09-13 | Tampering/Integrity | silent MPS training failure (flat adapter, "trained" artifact that learned nothing) | mitigate | params-actually-update canary on the real device + math.isfinite final-loss guard + bit-untouched base assert (PITFALLS P5); script exits non-zero on any failure |
| T-09-SC | Tampering | pip/npm installs | accept | zero packages installed this phase (RESEARCH Package Legitimacy Audit: nothing to install) |
</threat_model>

<verification>
- `.venv/bin/pytest tests/test_lora_training.py -q -x` green on CPU (< 60 s)
- `.venv/bin/python scripts/train_adapter_smoke.py` exits 0 locally; prints adapter size ~1.3 MB
- `make test` green — loop.py/gpt.py/checkpoint.py untouched by this plan
- `make lint` clean
- `git status --porcelain` shows no untracked run outputs (checkpoints/, logs/ gitignored)
</verification>

<success_criteria>
- ROADMAP success criterion 2 (only A/B change after training; every base param bit-untouched) pinned by unit test AND proven on real weights
- ROADMAP success criterion 5 (params-actually-update canary passes on a smoke run) satisfied by the script's inline asserts on MPS
- D-04 (full open-dict kill+resume for adapter training; lora_config rides **extra; final adapter exports via the D-01 artifact) implemented exactly as locked
- Zero edits to src/personacore/training/loop.py, model/gpt.py, or any v1.0 module
</success_criteria>

<output>
Create `.planning/phases/09-lora-core/09-04-SUMMARY.md` when done
</output>
