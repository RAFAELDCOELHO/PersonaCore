---
phase: 25-frontier-sweep-and-the-existence-gate-verdict
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phase25_epsilon.py
  - tests/test_phase25_epsilon.py
autonomous: true
requirements: [FRONT-02]

must_haves:
  truths:
    - "D-30: `point_epsilon`, `curve_total_epsilon` and `selection_accounted` are keyword-only with NO defaults — the gate's own 21-kwarg precedent, where the length IS the protection."
    - "D-30: enforcement is an AST WALK, never grep. Measured: `scripts/mitigation_gate.py` carries `epsilon` inside 25 string literals with an `ast.Name` count of 0 — a grep gate goes false-RED on it today."
    - "D-28: both multiplicities are named — 262.9437465865647 (the frozen pin's overlap rule) and 207.0180229382851 (first-token-owns-draw, the artifact rule) — because `phase21_multiplicity.json` records the discrepancy as 'RECORDED, NOT RESOLVED — the pin is frozen and is not edited'. Neither is hidden."
    - "D-29: the curve total is BASIC composition over the noised DP points actually PUBLISHED, at total δ = k·δ — conservative, computable from the existing stdlib accountant, zero new chosen constant. The total CROSSES both legs."
    - "D-29: `selection_accounted = False`, and once the σ=0 control is published NO joint bound over all published artifacts exists, because the control is an adapter trained on the same facts with no privacy at all."
    - "§C5 CORRECTED: `results/phase23_sigma_zero.json` has NO `epsilon` key at all (43 top-level keys, none named epsilon) — D-29's cited precedent does not exist. Phase 25 DECIDES: the control record writes `\"epsilon\": null` explicitly, never omits the key."
  artifacts:
    - path: "scripts/phase25_epsilon.py"
      provides: "the only sanctioned ε reporting surface: three required kwargs, the dual-granularity sentence with both multiplicities, and basic composition for the curve total"
      contains: "def report_epsilon"
    - path: "tests/test_phase25_epsilon.py"
      provides: "D-30's AST gate over the phase's modules, with grep-RED / AST-GREEN demonstrated on the real frozen gate"
      contains: "def test_grep_goes_false_red_where_the_ast_gate_is_green"
  key_links:
    - from: "scripts/phase25_epsilon.py"
      to: "src/personacore/privacy/accountant.py"
      via: "epsilon_for imported for the per-point ε; stdlib math only, no new constant"
      pattern: "epsilon_for"
    - from: "scripts/phase25_epsilon.py"
      to: "results/phase21_multiplicity.json"
      via: "both multiplicity figures read from the committed record, never retyped"
      pattern: "phase21_multiplicity"
---

<objective>
Build the one place an ε may be printed, and the AST gate that makes "one place" checkable.

Purpose: FRONT-02 is not "report two numbers" — it is *"an example-level ε can never be read as if it
bounded fact leakage"*. D-28 meets it in its strongest form by showing the **size** of the error that
confusing the granularities would make: 207.018 under the artifact's first-token-owns-draw rule and
262.944 under the frozen pin's overlap rule, both named, neither hidden, because
`phase21_multiplicity.json` itself records the discrepancy as unresolved and the pin as frozen. The
protection is structural: three required kwargs and an AST walk. Grep cannot do this job — the frozen
gate carries `epsilon` in 25 string literals with an `ast.Name` count of **0**.

Output: `scripts/phase25_epsilon.py` and `tests/test_phase25_epsilon.py`, with the grep-RED /
AST-GREEN contrast demonstrated on the real file rather than asserted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-frontier-sweep-and-the-existence-gate-verdict/25-CONTEXT.md
@.planning/phases/25-frontier-sweep-and-the-existence-gate-verdict/25-RESEARCH.md
@.planning/phases/25-frontier-sweep-and-the-existence-gate-verdict/25-VALIDATION.md
</context>

<environment>
**Python 3.11 venv is MANDATORY.** `.venv/bin/python -m pytest ...`. Baseline **1647 passed, 1 skipped**.

**`scripts/mitigation_gate.py`, `mitigation_accountant.py`, `mitigation_unit.py` and
`phase18_extraction.py` are ancestry-guarded and permanently uneditable.** This plan READS
`mitigation_gate.py` as the AST gate's demonstration target and must never write to it.

**NEVER grep any file whose prose discusses ε.** Measured on `mitigation_gate.py`: 2 string-literal
occurrences of `epsilon` inside `exists_clearing_point`, 23 inside `capacity_comparison`, `ast.Name`
count **0**. This is the file the demonstration runs on.
</environment>

<tasks>

<task type="auto">
  <name>Task 1: `scripts/phase25_epsilon.py` — the three-required-kwarg helper and the dual-granularity sentence</name>
  <files>scripts/phase25_epsilon.py</files>
  <read_first>
    - `src/personacore/privacy/accountant.py` — `epsilon_for`, `sigma_for`, `delta_closed`,
      `delta_quadrature`. stdlib `math` only. Measured: `epsilon_for(0.5, 200, 1e-5) = 519.6981942303134`,
      **bit-identical to the ε already committed in `results/phase23_noised_dp_n64_sigma0p500000.json`**;
      and `epsilon_for(0.0, 200, 1e-5)` returns **`inf`**, which is why the σ=0 control's ε is `None`
      rather than a number
    - `scripts/mitigation_unit.py` — `PRIVACY_UNIT`, `PRIVACY_UNIT_ARITHMETIC` (D-26's source: the
      with-replacement hazard stated verbatim), `SAMPLING_RATE_Q = 1.0`, `DELTA = 1e-5`,
      `REPLAY_OUTSIDE_N`, `privacy_n`. **READ ONLY**
    - `results/phase21_multiplicity.json` — `pin_discrepancy.pin_figure` `262.9437465865647`,
      `artifact_rule_figure` `207.0180229382851`, `status: "RECORDED, NOT RESOLVED — the pin is frozen
      and is not edited"`, `provenance.epsilon_computed: false`, `corpus_geometry` (176 clean episodes
      at n=8, 1408 at n=64)
    - `results/phase23_noised_dp_n64_sigma0p500000.json` — `epsilon`, `epsilon_rule`,
      `epsilon_comparison_made`, `epsilon_comparison_omitted_reason`: the four ε-bearing field names
      the point record already uses
    - `results/phase23_sigma_zero.json` — **43 top-level keys, none named `epsilon`** (§C5). D-29's
      cited precedent does not exist; this module decides the control's form
    - `scripts/mitigation_gate.py:637-831` — the 21-kwarg precedent D-30 cites, for the register the
      helper's signature copies
  </read_first>
  <action>
Create `scripts/phase25_epsilon.py`. stdlib plus `personacore.privacy.accountant` plus the frozen
`mitigation_unit`. No torch, no numpy, no network.

**(a) `report_epsilon(*, point_epsilon, curve_total_epsilon, selection_accounted)`.** Keyword-only,
**no defaults on any of the three**. It returns the single sanctioned rendering: a string carrying the
point ε, the curve-total ε, the selection-accounting flag, the privacy unit
(`mitigation_unit.PRIVACY_UNIT`), the sampler statement and the multiplicity — *all in the same
sentence*, which is SC3's literal wording. Raise `TypeError` naturally by omitting defaults; do not
add a manual arity check that could drift from the signature.

**(b) The dual granularity (D-28).** Write `dual_granularity_sentence(point_epsilon)` returning the
pairing of the **real fact-level ε that governs this path** with the **example-level counterfactual**
— what an example-level accounting *would* have claimed under the unaligned with-replacement sampler,
which is the flattering number a reader might wrongly assume applies. Both multiplicities are named:
read `pin_figure` and `artifact_rule_figure` from `results/phase21_multiplicity.json` (never retyped)
and state which rule each comes from — overlap (frozen pin) vs first-token-owns-draw (artifact rule).
Carry `phase21_multiplicity.json`'s own `status` string through verbatim, so the reader sees
`RECORDED, NOT RESOLVED` rather than a resolution this phase is not entitled to make. Quote
`mitigation_unit.PRIVACY_UNIT_ARITHMETIC`'s with-replacement clause as the reason the example-level
number says nothing about a fact.

**(c) The curve total (D-29).** Write `curve_total(published_point_epsilons, *, delta)`. Basic
composition: total ε is the plain sum over the **noised DP points actually PUBLISHED**, total δ is
`len(published) * delta`. No new constant is chosen; `delta` comes from `mitigation_unit.DELTA`.
Declare alongside, as module constants with `governs`-style strings:
  - `SELECTION_ACCOUNTED = False` and the reason: choosing a best point after seeing results would be
    unaccounted adaptive selection
  - `CONTROL_HAS_NO_EPSILON` — the σ=0 control carries no ε; it is an adapter trained on the same
    facts with **no privacy at all**, so once it is published **no joint bound over all published
    artifacts exists**
  - `TOTAL_CROSSES_BOTH_LEGS` — the 8 locked facts appear in n=8 *and* n=64, so a per-leg split would
    hide exactly the cumulative exposure that matters most
Refuse an input list containing a `None` ε (the control) with a message naming
`CONTROL_HAS_NO_EPSILON` — the control must be excluded deliberately, never summed as zero.

**(d) §C5's decision, made and recorded.** Pin `CONTROL_EPSILON_FIELD_FORM` stating that Phase 25's
control-point record writes the key `"epsilon"` with the value `null` **explicitly**, plus a sibling
`epsilon_omitted_reason`. Record the correction in place: CONTEXT's D-29 cites
`phase23_sigma_zero` as recording `epsilon: None`, and that record has **no `epsilon` key at all** —
43 top-level keys, none named `epsilon`. There is no precedent to inherit, so this is a decision.
State the reason for `null` over omission: FRONT-03's single-source promise and D-31's ordered
`point_keys` hard equality both want every point record to carry the same key set, and a missing key
is indistinguishable from a bug while an explicit null is a statement.

**(e) `EPSILON_NAMES`.** A committed tuple of the identifier names the AST gate resolves against —
the names that, if they reach a `print` / f-string / `.format` / `%` outside this module's own
functions, constitute a bare ε. Include at least `epsilon`, `point_epsilon`, `curve_total_epsilon`,
`eps`, `epsilon_total`, and the attribute form `epsilon_for`. State in the docstring that this is a
NAME set, not a phrase set, precisely because the gate is an AST walk.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import sys;sys.path.insert(0,'scripts');sys.path.insert(0,'src');import phase25_epsilon as e;print(e.report_epsilon(point_epsilon=519.6981942303134, curve_total_epsilon=1000.0, selection_accounted=False)[:200])"</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -c "import sys;sys.path.insert(0,'scripts');sys.path.insert(0,'src');import phase25_epsilon as e;e.report_epsilon(point_epsilon=1.0, curve_total_epsilon=2.0)"` exits non-zero with a `TypeError` naming `selection_accounted` — the third kwarg has no default
    - `.venv/bin/python -c "import sys;sys.path.insert(0,'scripts');sys.path.insert(0,'src');import phase25_epsilon as e;s=e.dual_granularity_sentence(519.6981942303134);assert '262.9437465865647' in s and '207.0180229382851' in s and 'RECORDED, NOT RESOLVED' in s;print('both multiplicities named')"` exits 0
    - `.venv/bin/python -c "import sys;sys.path.insert(0,'scripts');sys.path.insert(0,'src');import phase25_epsilon as e, mitigation_unit as u;print(e.curve_total([519.6981942303134, 159.44148628736576], delta=u.DELTA))"` prints a `(sum, 2*1e-5)` pair
    - `.venv/bin/python -c "import sys;sys.path.insert(0,'scripts');sys.path.insert(0,'src');import phase25_epsilon as e, mitigation_unit as u;e.curve_total([1.0, None], delta=u.DELTA)"` exits non-zero and names `CONTROL_HAS_NO_EPSILON`
    - `.venv/bin/python -c "import ast;t=ast.parse(open('scripts/phase25_epsilon.py').read());fn=[f for f in ast.walk(t) if isinstance(f,ast.FunctionDef) and f.name=='report_epsilon'][0];assert not fn.args.args and len(fn.args.kwonlyargs)==3 and all(d is None for d in fn.args.kw_defaults), (len(fn.args.kwonlyargs), fn.args.kw_defaults);print('3 kwonly, no defaults')"` exits 0
    - `.venv/bin/python -c "import sys;sys.path.insert(0,'scripts');sys.path.insert(0,'src');import phase25_epsilon as e;assert e.SELECTION_ACCOUNTED is False and 'null' in e.CONTROL_EPSILON_FIELD_FORM;print(len(e.EPSILON_NAMES))"` prints a number ≥ 5
    - `git diff --exit-code -- scripts/mitigation_gate.py scripts/mitigation_accountant.py scripts/mitigation_unit.py scripts/phase18_extraction.py` exits 0
  </acceptance_criteria>
  <done>The ε helper takes three required kwargs with no defaults, names both multiplicities from the committed record, computes the curve total by basic composition across both legs, and refuses to sum the control's absent ε.</done>
</task>

<task type="auto">
  <name>Task 2: `tests/test_phase25_epsilon.py` — D-30's AST gate, with grep-RED / AST-GREEN demonstrated</name>
  <files>tests/test_phase25_epsilon.py</files>
  <read_first>
    - `scripts/phase25_epsilon.py` — `EPSILON_NAMES` and the helper's own function bodies, which are
      the gate's only exemption
    - `scripts/mitigation_gate.py` — the demonstration target. **Do not open it with grep in the
      test**; the test's *point* is that a grep over it is wrong. Read it here to see the 25 string
      literals for yourself
    - `tests/test_phase24_correction.py:15-36` — the four mechanics, of which mechanic (4) is the one
      this file leans on: **resolve node ids and identifiers by AST, never by grep**, because the
      target module's own docstrings discuss the same names
    - `tests/test_phase22_dpsgd_ast.py` — the repo's register for an AST-only structural test: walk,
      collect, assert, and report the offending node's `lineno` in the failure message
    - `25-RESEARCH.md` §*Structurally checked vs merely declared*, D-30's row — the natural RED is the
      real file, today
  </read_first>
  <action>
Create `tests/test_phase25_epsilon.py`. CPU-only, stdlib plus `pytest` plus the one sibling script.

**(a) The gate itself.** `test_no_bare_epsilon_is_printed_outside_the_helper` — walk every module this
phase owns (`scripts/phase25_*.py` and `scripts/plot_phase25.py`, collected by glob so a module added
later is covered automatically) with `ast.parse`. For each node that is a `print` call, an
`ast.JoinedStr`, a `.format(` call or a `%` `ast.BinOp` on a string, collect the `ast.Name.id` and
`ast.Attribute.attr` names appearing in its operands. Fail if any of them is in
`phase25_epsilon.EPSILON_NAMES` **and** the enclosing function is not defined in
`scripts/phase25_epsilon.py`. The failure message must name the file, the line number, the function
and the offending identifier.

**(b) The demonstration — this is the natural RED, and it must be run.**
`test_grep_goes_false_red_where_the_ast_gate_is_green`, over the real
`scripts/mitigation_gate.py`:
  - the **text** channel: count occurrences of `epsilon` in the raw source with `str.count` (never
    `grep -c`, which counts lines) and assert the count is **≥ 25**, so a naive textual gate would
    flag this file
  - the **AST** channel: parse the file and count `ast.Name` nodes whose `id` contains `epsilon` plus
    `ast.arg` nodes likewise; assert that count is **0**
  - assert the two channels disagree, and put the consequence in the docstring: a grep gate applied to
    this file today reports a violation that does not exist, which is the class RPT-02 exists to
    close and which `.planning/REQUIREMENTS.md`'s RPT-02 row records four independent instances of in
    Phase 20 alone
  - additionally assert that the 25 occurrences are all inside `ast.Constant` string values, by
    walking the constants — so the test proves *where* they live rather than only that they exist

**(c) The gate's own RED, planted against a scratch copy in `tmp_path`.**
`test_the_epsilon_gate_fires_on_a_planted_bare_print` — write a synthetic module into `tmp_path`
containing `print(f"epsilon={point_epsilon}")` inside a function, point the walker at `tmp_path`, and
assert it fails naming that function and that line. **Never plant into a real repo file.** Assert
`git status --porcelain scripts/` is empty immediately afterwards.

**(d) The helper's exemption is narrow.** `test_the_exemption_is_the_helper_module_not_the_package` —
a synthetic module in `tmp_path` named `phase25_epsilon_helper.py` (a near-miss name) that prints a
bare ε must still fail. The exemption is resolved by the module's resolved path, not by a name
substring.

**(e) The three-kwarg contract, asserted structurally.**
`test_report_epsilon_has_three_keyword_only_args_with_no_defaults` — AST over the signature:
`args.args` empty, `len(args.kwonlyargs) == 3`, every entry of `args.kw_defaults` is `None`. Name the
21-kwarg precedent in the docstring: the length is the protection.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase25_epsilon.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_phase25_epsilon.py -v` exits 0 with **zero skipped**; quote the `N passed, M skipped` line
    - The SUMMARY quotes the two measured numbers from the demonstration: the textual `epsilon` count over `scripts/mitigation_gate.py` (**≥ 25**) and the `ast.Name` count (**0**)
    - The SUMMARY quotes the verbatim failure message from the planted bare-print RED in `tmp_path`, and states that `git status --porcelain scripts/` was empty immediately after
    - `.venv/bin/python -c "import ast;t=ast.parse(open('tests/test_phase25_epsilon.py').read());ns={f.name for f in ast.walk(t) if isinstance(f,ast.FunctionDef)};assert 'test_grep_goes_false_red_where_the_ast_gate_is_green' in ns and 'test_the_epsilon_gate_fires_on_a_planted_bare_print' in ns, sorted(ns);print('both demonstrations committed')"` exits 0
    - `.venv/bin/python -c "import subprocess,sys;r=subprocess.run([sys.executable,'-c','import ast;src=open(\"tests/test_phase25_epsilon.py\").read();t=ast.parse(src);calls=[n for n in ast.walk(t) if isinstance(n,ast.Call)];print(len(calls))'],capture_output=True,text=True);print(r.stdout.strip())"` exits 0
    - `.venv/bin/python -c "import ast;P='tests/test_phase25_epsilon.py';src=open(P).read();t=ast.parse(src);doc={i for n in ast.walk(t) if isinstance(n,(ast.Module,ast.ClassDef,ast.FunctionDef,ast.AsyncFunctionDef)) and n.body and isinstance(n.body[0],ast.Expr) and isinstance(n.body[0].value,ast.Constant) and isinstance(n.body[0].value.value,str) for i in range(n.body[0].lineno,n.body[0].end_lineno+1)};bad=[(i,l.strip()[:70]) for i,l in enumerate(src.splitlines(),1) if 'grep -c' in l and i not in doc and not l.lstrip().startswith('#')];assert not bad, bad;print('grep -c appears only in prose, never in executable code')"` exits 0 — the test does not itself use the counting form it is proving unsafe — **resolved by AST, not by `grep -c`.** `grep -c` counts LINES over a file whose own docstrings this plan requires to discuss the very token being counted, so the counting form goes FALSE-RED on its own prose. Measured at plan time: `grep -v '^\s*#' tests/test_phase23_cost.py | grep -c 'grep -c'` returns **3** while the AST gate over the same file exits 0. This repository produced four instances of that false-RED class in Phase 20 alone; RPT-02 exists to close it
    - `.venv/bin/python -m pytest tests/ -q` reports 0 failed; record the `N passed, M skipped` line
    - `git diff --exit-code -- scripts/mitigation_gate.py scripts/mitigation_accountant.py scripts/mitigation_unit.py scripts/phase18_extraction.py` exits 0; `make lint` exits 0
  </acceptance_criteria>
  <done>D-30's gate is an AST walk over every Phase-25 module, its RED has been watched on a scratch copy, and the grep-RED / AST-GREEN contrast has been measured on the real frozen gate with both counts recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| an example-level ε → a reader who assumes it bounds fact leakage | the whole reason FRONT-02 exists; the counterfactual must be shown with its size, not merely disclaimed |
| the frozen multiplicity pin → the artifact's own rule | 262.944 vs 207.018, recorded as unresolved; this phase names both and resolves neither |
| a textual guard → a file whose prose discusses the guarded term | measured: 25 string literals vs 0 `ast.Name` on the frozen gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-12 | Information Disclosure | a bare ε printed outside the helper, read as a fact-level bound | mitigate | three required kwargs with no defaults (structural, `TypeError` on omission) plus an AST walk over every `scripts/phase25_*.py`, with a planted RED watched in `tmp_path` |
| T-25-13 | Repudiation | a false-RED guard being disabled, taking the real protection with it | mitigate | the grep-RED / AST-GREEN contrast is measured on the real frozen gate and both counts are recorded, so the AST choice is evidence rather than preference |
| T-25-14 | Tampering | the σ=0 control's absent ε silently summed as 0 into the curve total | mitigate | `curve_total` refuses a `None` entry naming `CONTROL_HAS_NO_EPSILON`; the control record writes `"epsilon": null` explicitly rather than omitting the key |
| T-25-15 | Spoofing | one multiplicity published as if it were the resolved figure | mitigate | both figures are read from `results/phase21_multiplicity.json` and its own `RECORDED, NOT RESOLVED` status string is carried through verbatim |
| T-25-SC | Tampering | npm/pip/cargo installs | accept | Zero installs; `pyproject.toml` byte-unchanged (RPT-03) |
</threat_model>

<verification>
```bash
.venv/bin/python -m pytest tests/test_phase25_epsilon.py -v
.venv/bin/python -m pytest tests/ -q                       # record "N passed, M skipped"
git diff --exit-code -- scripts/mitigation_gate.py scripts/mitigation_accountant.py scripts/mitigation_unit.py scripts/phase18_extraction.py pyproject.toml
make lint
```
</verification>

<success_criteria>
- `report_epsilon` has exactly three keyword-only args with no defaults, proved by AST
- Both multiplicities and `phase21_multiplicity.json`'s `RECORDED, NOT RESOLVED` status appear in the dual-granularity sentence
- `curve_total` is basic composition at total δ = k·δ over published noised DP points, crossing both legs, and refuses the control's `None`
- §C5's correction is recorded and the control's `"epsilon": null` form is decided in code
- D-30's AST gate covers every `scripts/phase25_*.py` and has been watched firing on a scratch copy
- The grep-RED / AST-GREEN contrast on `scripts/mitigation_gate.py` is measured with both counts recorded
</success_criteria>

<output>
Create `.planning/phases/25-frontier-sweep-and-the-existence-gate-verdict/25-03-SUMMARY.md` when done.
</output>
