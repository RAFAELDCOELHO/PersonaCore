---
phase: 26-empirical-privacy-audit-canary
fixed_at: 2026-09-13T23:55:00Z
review_path: .planning/phases/26-empirical-privacy-audit-canary/26-REVIEW.md
iteration: 1
findings_in_scope: 8
fixed: 8
skipped: 0
status: all_fixed
---

# Phase 26: Code Review Fix Report

**Fixed at:** 2026-09-13T23:55:00Z
**Source review:** .planning/phases/26-empirical-privacy-audit-canary/26-REVIEW.md
**Iteration:** 1

**Summary:**
- Findings in scope: 8 (fix_scope = critical_warning: CR-01, WR-01..WR-07; IN-01..IN-03 out of scope)
- Fixed: 8
- Skipped: 0

**Execution mode.** Sequential, directly on `main` at base `4a3b2f0` (no worktree, per the orchestrator's override: the shared editable `.venv` points at this checkout's `src/`). Every fix was gated before its commit by `.venv/bin/python -m pytest -q tests/test_phase26_prereg.py tests/test_phase26_canary.py tests/test_phase25_venue.py` (the venue file spawns the full inner suite twice, ~13 min per run) and `ruff check` + `ruff format --check` on the three Python files. Baseline before any fix: 59 passed (43 phase-26 + 16 venue), ruff clean. Nothing in `scripts/phase26_prereg.py` or `results/phase26_canary.json` was touched; the artifact's sha256 is still `d2a71e2d…`. Pre-existing ` D .claude/scheduled_tasks.lock` left alone.

## Fixed Issues

### CR-01: The derived ubuntu skip register is off by one — CI goes red on the next push

**Files modified:** `tests/test_phase25_venue.py`
**Commit:** 52225b9
**Measured before editing (hard constraint):** ran `tests/test_phase26_canary.py` with `_ADAPTERS_ON_DISK` forced False via a session-scoped autouse plugin, plutil present on this host: `18 passed, 8 skipped` — seven `@needs_adapters` cases (lines 251, 272, 290, 384, 521, 563, 642) plus the undecorated in-body `pytest.skip` at `test_emit_refuses_a_partial_audit:315`. ubuntu adds `@needs_plutil` (`test_the_canary_agent_plist_lints`): 8 + 1 = 9 against a register of 8. The finding holds.
**Applied fix:** dated continuation (2026-09-13, 26-REVIEW CR-01) in the file's own convention, `_CANARY_HOST_ONLY_SKIPS = 9` (7 `@needs_adapters` + 1 in-body skip + 1 `@needs_plutil`); derived ubuntu counts become 71 / 71; M3 counts (39 / 4) unchanged because the in-body skip is never reached where the adapters are on disk. The comment keeps "DERIVED, NOT MEASURED — no CI run has executed a Phase-26 commit" honest.
**Gate:** 59 passed, ruff clean.

### WR-01: The committed artifact does not pin the sidecars it was assembled from

**Files modified:** `scripts/phase26_canary.py`, `tests/test_phase26_canary.py`, `results/phase26_canary_sources.json` (new, generated by the committed code in the same commit)
**Commit:** 6258e2f
**Applied fix:** (a) `emit()` now records `source_sha256` + `source_provenance` (`instrument_git_sha`, `scoring_seconds`, `device`, `torch_version`, `utc`) per point and `off_sidecar_sha256` / `off_sidecar_provenance` at top level, for any future emit. (b) The committed artifact is write-once (D-18) and was NOT re-emitted: new `pin_sources()` / `--pin-sources` writes the dated continuation `results/phase26_canary_sources.json`, pinning the OFF sidecar and all 16 point sidecars by sha256 and carried provenance and proving each file on disk re-derives the artifact's exclusions (OFF) and `fact_unit` reading (points) through the driver's own `_exclusions` / `_readings`. It publishes no git SHA (content hashes only), refuses to overwrite without `--force`. (c) Host-independent test over a fixture base file (`_complete_fake_audit`) covering the hashes, the overwrite refusal and the refusal of a re-scored sidecar under the same adapter hash. Generated against the committed artifact: all 17 sidecars re-derive; the sibling records the two distinct `instrument_git_sha` values WR-02 describes.
**Gate:** 60 passed, ruff clean.

### WR-02: `instrument_git_sha` names HEAD at write time, not the instrument that ran

**Files modified:** `scripts/phase26_canary.py`, `tests/test_phase26_canary.py`
**Commit:** a961933
**Applied fix:** `INSTRUMENT_GIT_SHA = git_sha()` resolved once at module import (the commit the running code was actually loaded from — the LaunchAgent imports once at process start); `_provenance` records it as `instrument_git_sha` and records the per-write `git_sha()` under its honest name `head_at_write`. Adapted from the review's "thread `instrument_sha` through `main()`" to an import-time constant, which cannot be stale by construction and needs no signature changes. Test monkeypatches `canary.git_sha` and proves `instrument_git_sha` stays the import-time value while `head_at_write` moves.
**Gate:** 61 passed, ruff clean.

### WR-03: `emit()` publishes `emitted_git_sha` from a dirty tree — no `refuse_if_dirty`

**Files modified:** `scripts/phase26_canary.py`, `tests/test_phase26_canary.py`
**Commit:** 9db7b82
**Applied fix:** `emit()` calls `personacore.provenance.refuse_if_dirty(who="phase26_canary", detail=…, pathspec=("scripts", "src", "results", ":(exclude)<record>"), cwd=_ROOT)` immediately after the overwrite check (the `phase25_record._write` order: refuse existing, then refuse dirty, then bytes), before any sidecar is read. The exclude is added only when the record sits inside the repo — measured: git aborts (`fatal: Invalid path`) on an `:(exclude)` outside the repository, and the tests emit to tmp dirs. No test precedent monkeypatches the guard (grep: only direct tests in `tests/test_phase21_unit_record.py`), so the phase-26 test module records it through an autouse `clean_tree` fixture, and `test_emit_refuses_a_dirty_tree_before_reading_any_sidecar` proves the exact call (`who`, `cwd`, pathspec with the exclude) and that a refusal propagates. The suite does not depend on a clean tree. Module docstring updated (`git_sha()` and `refuse_if_dirty()` are the only git reads).
**Gate:** 62 passed, ruff clean.

### WR-04: `emit()` trusts sidecar shape beyond the adapter hash; the exclusion rule can be silently weakened

**Files modified:** `scripts/phase26_canary.py`, `tests/test_phase26_canary.py`
**Commit:** 88a2948
**Applied fix:** new `_prove_shape(blob, name)` proves per tier `questions == EXPECTED_QUESTIONS[tier]` and `sum(per_fact.n_questions) == EXPECTED_QUESTIONS[tier]`, applied to the OFF sidecar and every point; every point's per-tier fact set is proved IDENTICAL to the OFF sidecar's (`_fact_ids`, identity not cardinality); the exclusion populations are taken from the OFF sidecar as measured and proved `8 / 56` instead of the hard-coded `56` / `8`. Measured before committing: all 17 real sidecars satisfy the new proofs. Test refuses a truncated OFF (fact removed), a renamed OFF fact (counts sum, population differs) and an ON sidecar with a wrong question count. The IN-01 tautological guard was left in place (Info, out of scope).
**Gate:** 63 passed, ruff clean.

### WR-05: `emit()` publishes the control's reproduction gate without proving it passed

**Files modified:** `scripts/phase26_canary.py`, `tests/test_phase26_canary.py`
**Commit:** bd06f0b
**Applied fix:** `emit()` proves the control sidecar's `reproduction_gate` is a dict with `passed is True`, `expected == observed == [REPRODUCTION_K, REPRODUCTION_N]`, and — stricter than the review's snippet — equal to the sidecar's own `in_taught` `k/n`. The real control sidecar satisfies it (790/1008). Because the wiring test's control sidecar previously recorded observed `[0, 1008]`, this fix also gives that test the control fake the review asks for under WR-06: `fake_complete` answers exactly 790 of the control's 1008 in-taught draws (9 × 87 questions + 7 draws of question 98, fact 7's first — 14 questions per fact, IN questions measured unique 112/112) so all 8 facts are members and the REAL `prove_reproduction` passes (the recorder now delegates to it). The fixture control carries a consistent gate. Test refuses a missing gate, `passed: false`, and a passed gate over counts that are not the pinned numbers.
**Gate:** 64 passed, ruff clean.

### WR-06: PRESENT-branch tests never re-derive a verdict; no `emit()` in the suite reaches CONSISTENT/BROKEN

**Files modified:** `tests/test_phase26_canary.py`
**Commit:** 7c421f0
**Applied fix:** new both-state test `test_the_committed_readings_verdicts_ceiling_and_power_gate_re_derive` recomputes from the committed artifact's own counts through the frozen module (only public names that exist: `epsilon_lower`, `point_verdict`, `auditor_ceiling`, `power_gate`, `power_threshold`, `noised_point_keys`, `audited_point_keys`, `VERDICTS`, `CONTROL_KEY`): every `fact_unit` and `question_unit` reading (whole-dict equality), every noised verdict with its `reasons` (whole-dict equality), `n_in`/`n_out` against `exclusions`, the auditor ceiling, the power gate from the control's reading and `power_threshold(frontier)`, `reachable_keys`, and the summary against the per-point verdicts. Measured before writing: all hold on the committed artifact. The wiring test now asserts `power_gate.passed is True`, `control_epsilon_lower == auditor_ceiling` and `summary == {CONSISTENT: 15}`, so `emit()`'s CONSISTENT branch (power passed) runs in the suite; the review's tautological `expected_verdict` check was left as is.
**Gate:** 65 passed, ruff clean.

### WR-07: `test_emit_refuses_to_overwrite_the_committed_artifact` is vacuously green in the absent state

**Files modified:** `tests/test_phase26_canary.py`
**Commit:** 2c2b404
**Applied fix:** `else: pass` replaced by `assert not _git("ls-files", "results/phase26_canary.json").strip()` with a message naming the tracked-but-absent state, matching the two sibling both-state tests.
**Gate:** 65 passed, ruff clean.

## Skipped Issues

None — every in-scope finding was fixed.

## Test count progression (phase gate: prereg + canary + venue)

| After | Passed | Skipped | Failed | Ruff |
|-------|--------|---------|--------|------|
| baseline (4a3b2f0) | 59 | 0 | 0 | clean |
| CR-01 (52225b9) | 59 | 0 | 0 | clean |
| WR-01 (6258e2f) | 60 | 0 | 0 | clean |
| WR-02 (a961933) | 61 | 0 | 0 | clean |
| WR-03 (9db7b82) | 62 | 0 | 0 | clean |
| WR-04 (88a2948) | 63 | 0 | 0 | clean |
| WR-05 (bd06f0b) | 64 | 0 | 0 | clean |
| WR-06 (7c421f0) | 65 | 0 | 0 | clean |
| WR-07 (2c2b404) | 65 | 0 | 0 | clean |

The six new tests (`pin_sources…`, `instrument_sha…`, `emit_refuses_a_dirty_tree…`, `emit_refuses_a_sidecar_whose_shape…`, `emit_refuses_a_control_without…`, `committed_readings…re_derive`) carry no host marker, so they add passes on every platform and change no entry in the venue skip register.

## Notes for the verifier

- `results/phase26_canary_sources.json` is a new tracked `results/phase26_*` file; the prereg ancestry guard (`test_phase26_prereg_is_frozen_before_every_phase26_result`) passed in every gate run after it was added.
- The ubuntu venue counts (71 / 71) remain derived; the first CI run over any Phase-26 commit is what measures them.
- IN-01, IN-02 and IN-03 were out of scope (`fix_scope: critical_warning`) and are untouched.

---

_Fixed: 2026-09-13T23:55:00Z_
_Fixer: Claude (gsd-code-fixer)_
_Iteration: 1_
