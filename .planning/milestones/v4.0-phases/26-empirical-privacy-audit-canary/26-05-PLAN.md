---
phase: 26-empirical-privacy-audit-canary
plan: 05
type: execute
wave: 5
depends_on: ["26-04"]
files_modified:
  - results/phase26_canary.json
  - results/phase26_operational_note.md
  - tests/test_phase26_canary.py
autonomous: false
requirements: [CANARY-01, CANARY-02]
must_haves:
  truths:
    - "results/phase26_canary.json is assembled ONLY when the OFF sidecar and all 16 point sidecars exist; otherwise a dated D-19 named-limitation entry lands and no artifact is written (D-19)"
    - "The artifact carries the frontier's sha256, the 16 adapter_sha256, auditor_ceiling, reachable_claims k/15, the power gate with its sentence, exclusions n/56, and a three-valued verdict with reasons for each of the 15 noised points — published whichever way it came out (D-03, D-05, D-07, D-13, D-18)"
    - "The artifact is committed by the OPERATOR by hand; the driver never commits; the frontier stays at exactly one commit (§O1 closed, D-18)"
    - "The machine is put back: agent booted out, launchctl list empty of phase26, assertion owners quoted (D-16)"
  artifacts:
    - path: "results/phase26_canary.json"
      provides: "The sibling verdict artifact pinned to the frontier both ways"
      contains: "frontier_sha256"
    - path: "results/phase26_operational_note.md"
      provides: "§8 close block (or the D-19 named limitation)"
      contains: "## 8."
  key_links:
    - from: "results/phase26_canary.json"
      to: "results/phase25_frontier.json"
      via: "frontier_sha256 == sha256(bytes on disk); points[k].adapter_sha256 == frontier points[k].adapter_sha256"
      pattern: "adapter_sha256"
    - from: "tests/test_phase26_canary.py"
      to: "results/phase26_canary.json"
      via: "test_the_sibling_is_pinned_to_the_frontier_both_ways / test_every_point_carries_its_reasons_and_the_ceiling_disclosure now in the PRESENT state"
      pattern: "RECORD.exists"
---

<objective>
Close the audit: when the 16th sidecar lands, run `--emit` once, prove the link and the verdict
register with the existing tests in their PRESENT state, have the operator commit the artifact by
hand, put the machine back, and record the close — or, if the run was cut, record the dated D-19
named limitation instead of assembling anything.

Purpose: SC3 — the verdict travels with the frontier (same directory, proven link), and a cut audit is
a named limitation, never silence.
Output: `results/phase26_canary.json` (operator-committed), note §8, tests in the present state green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-empirical-privacy-audit-canary/26-CONTEXT.md
@.planning/phases/26-empirical-privacy-audit-canary/26-04-SUMMARY.md
@results/phase26_operational_note.md

<interfaces>
From scripts/phase26_canary.py: RECORD = results/phase26_canary.json ; emit(out_path=RECORD, *, overwrite=False) ; main(["--emit"]) ; sidecar_path(key) ; off_sidecar_path()
From scripts/phase26_prereg.py: audited_point_keys(frontier) (16) ; VERDICTS ; POWER_SENTENCE ; PUBLICATION_OBLIGATION_CONTINUATION (last entry: "<artifact absent>" clause)
From tests/test_phase25_close.py: test_the_frontier_artifact_is_unchanged_since_its_single_write (one commit) ; test_the_git_surface_exception_is_closed
From results/phase25_operational_note.md §13 (:1104-1120): the close-block shape (bootout, launchctl list empty, pmset assertions by pid, caffeinate ps)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: When all 16 sidecars exist, --emit once and prove the artifact; otherwise record the D-19 named limitation</name>
  <files>results/phase26_canary.json, results/phase26_operational_note.md, tests/test_phase26_canary.py</files>
  <read_first>
    - results/phase26_operational_note.md (§1-§7 state)
    - scripts/phase26_canary.py (`emit` — the refusal order and the artifact fields)
    - tests/test_phase26_canary.py (`_NOTE_REQUIRED_BLOCKS`; the two both-state tests `test_the_sibling_is_pinned_to_the_frontier_both_ways`, `test_every_point_carries_its_reasons_and_the_ceiling_disclosure`)
    - logs/phase26_canary.out (the run's tail — did the driver exit 0 after the 16th point?)
    - .planning/phases/26-empirical-privacy-audit-canary/26-CONTEXT.md D-13, D-18, D-19
  </read_first>
  <action>
Precondition read, quoted into the note as `## 8. The close — <date>` (or `## 8. D-19 named limitation — <date>`): `ls data/phase26_canary_*.json | wc -l` (expected 17 = OFF + 16), `launchctl list | grep phase26` (the agent should have exited 0: `"PID" = -` / last exit status 0), `tail -5 logs/phase26_canary.out`.

BRANCH A — all 17 sidecars exist: run `PERSONACORE_SWEEP_ACTIVE= .venv/bin/python scripts/phase26_canary.py --emit` (the env var unset — the run is over; `--emit` loads no model). Quote its printed summary (`BROKEN/CONSISTENT/INCONCLUSIVE` counts, `reachable_claims`, the power-gate line with `POWER_SENTENCE`, `exclusions.out.n`, `auditor_ceiling`). Then run `.venv/bin/pytest -q tests/test_phase26_canary.py tests/test_phase26_prereg.py -x` — the two both-state tests now execute their PRESENT branch; `test_the_control_reproduced_the_published_reading` passes. Run `make test` (full suite; quote counts). Append `"## 8. The close"` to `_NOTE_REQUIRED_BLOCKS` in the test file. Do NOT commit the artifact — Task 2 is the operator's. Do NOT edit `results/phase26_canary.json` by hand; if `emit` refused, that refusal is Branch B.

BRANCH B — fewer than 17 sidecars (the operator cut the run, a kill was not resumed, or `emit` refused): do NOT run `--emit` a second time and do NOT assemble anything by hand (D-19: an artifact with 9 of 16 points is a subset chosen by the clock). Write `## 8. D-19 named limitation — <date>` in the note: the sidecar count and which keys exist (`ls`), why the run stopped (quoted log tail / operator's words), the sentence "the audit was not executed to completion; no `results/phase26_canary.json` exists; Phase 28 must say 'audit not executed / partial' beside EVERY ε it quotes" citing `phase26_prereg.PUBLICATION_OBLIGATION_CONTINUATION`'s `"<artifact absent>"` entry, and what would complete it (re-kickstart resumes by hash — the existing sidecars are reused, never re-scored). Append `"## 8. D-19 named limitation"` to `_NOTE_REQUIRED_BLOCKS`. The both-state tests stay in their ABSENT branch and must still pass.

Either branch: commit the note + test change (NOT the artifact) with a message naming the branch taken.

Threat refs: T-26-03 (`emit` refuses < 17 sidecars; Branch B writes the D-19 entry and nothing else; no `--force`), T-26-07 (`emit` re-checks the 16 `adapter_sha256` and the OFF `base_sha256`), T-26-11 (sibling artifact only; the frontier is never re-emitted — `test_the_frontier_artifact_is_unchanged_since_its_single_write` stays green).
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/pytest -q tests/test_phase26_canary.py tests/test_phase26_prereg.py tests/test_phase25_close.py -x && grep -c "^## 8\. \(The close\|D-19 named limitation\)" results/phase26_operational_note.md | grep -x 1</automated>
  </verify>
  <acceptance_criteria>
    - Branch A: `results/phase26_canary.json` exists, `python -c` shows `len(blob["audited_point_keys"]) == 16`, `set(p["verdict"]["verdict"] for k, p in blob["points"].items() if p["verdict"]) <= set(VERDICTS)`, `blob["summary"]` sums to 15, `blob["reachable_claims"]` matches `\d+/15`, `blob["power_gate"]["sentence"] == POWER_SENTENCE`, `blob["frontier_sha256"] == 1f182b40c9d7c316e57ecd69acd62c7f6407514b9c675bdf8ec677d3f0cb97d5`; `test_the_sibling_is_pinned_to_the_frontier_both_ways` and `test_every_point_carries_its_reasons_and_the_ceiling_disclosure` PASS in the present state; `test_the_control_reproduced_the_published_reading` PASSES; `make test` green
    - Branch B: `results/phase26_canary.json` does NOT exist; the note's §8 contains `audit not executed` and `<artifact absent>`; the both-state tests pass in the absent state
    - `git status --short results/` shows the artifact UNTRACKED (`??`) in Branch A — this task never commits it
    - `git log --oneline -- results/phase25_frontier.json` is one line; `git diff -- results/phase25_frontier.json scripts/phase26_prereg.py` empty
  </acceptance_criteria>
  <done>Artifact emitted once and proven (or the D-19 entry written); note + test committed; artifact left for the operator.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: The operator commits results/phase26_canary.json by hand (§O1: the driver's git surface is read-only)</name>
  <what-built>Branch A: `results/phase26_canary.json` is on disk, untracked, proven by the tests in their present state, with the verdict summary quoted in note §8. The driver never commits (T-26-05; `phase25_prereg.GIT_SURFACE_EXCEPTION` says "IT ENDS WITH THIS PHASE" about Phase 25's exception). The pre-registration's ancestry guard requires this artifact's first-add to be a descendant of every `scripts/phase26_prereg.py` commit — it is, by construction, if nothing touched that module since 26-01.</what-built>
  <how-to-verify>
    Branch A, from the repo root, in this order:
    1. `git status --short | grep -v '^??'` — empty (no peer session left staged changes; memory: concurrent agents edit the working tree).
    2. `git log --format=%H -- scripts/phase26_prereg.py` — the same commits as note §1 (no later touch).
    3. `git add results/phase26_canary.json && git commit -m "results(26): commit the canary audit artifact — <summary counts from §8> — operator commit, the driver's git surface is read-only"`.
    4. `.venv/bin/pytest -q tests/test_phase26_prereg.py tests/test_phase26_canary.py` — the ancestry guard now checks two tracked artifacts and passes; the link test passes.
    5. Reply with the commit SHA.
    Branch B: there is nothing to commit here; reply "limitation recorded" and Task 3 closes the machine.
  </how-to-verify>
  <resume-signal>Paste the artifact's commit SHA (Branch A) or type "limitation recorded" (Branch B).</resume-signal>
  <files>results/phase26_canary.json</files>
  <action>Human action, deliberately not automated (hard constraint 6; T-26-05): the operator stages and commits `results/phase26_canary.json` by hand with the five steps in how-to-verify, so the only git write in the audit's lifetime is a human's. Claude does not run `git add`/`git commit` for this file. Branch B has nothing to commit.</action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && { [ ! -f results/phase26_canary.json ] || { git ls-files results/phase26_canary.json | grep -q . && .venv/bin/pytest -q tests/test_phase26_prereg.py tests/test_phase26_canary.py -x; }; }</automated>
  </verify>
  <acceptance_criteria>
    - Branch A: `git ls-files results/phase26_canary.json` is non-empty; `git log --diff-filter=A --format=%H -- results/phase26_canary.json` is exactly one commit whose author is the operator; `test_phase26_prereg_is_frozen_before_every_phase26_result` passes with two tracked artifacts
    - Branch B: `results/phase26_canary.json` does not exist and the SUMMARY records "limitation recorded"
  </acceptance_criteria>
  <done>Artifact committed by the operator (SHA pasted) or Branch B acknowledged.</done>
</task>

<task type="auto">
  <name>Task 3: Put the machine back and record the close</name>
  <files>results/phase26_operational_note.md</files>
  <read_first>
    - results/phase25_operational_note.md §13 (:1104-1120) — the close-block shape
    - results/phase26_operational_note.md §8 (Task 1 state)
  </read_first>
  <action>
Run and quote under §8 (append a `### 8.x The machine put back` sub-block): `launchctl bootout gui/$UID/com.personacore.phase26.canary` (or `launchctl unload ~/Library/LaunchAgents/com.personacore.phase26.canary.plist`), `launchctl list | grep personacore` → expected: only the watcher if it was already loaded before this phase, else empty — bootout the watcher too only if plan 26-04 loaded it (say which), `pmset -g assertions | grep -i caffeinate` (expected: none owned by the driver; the harness's own `-i -t 300` may appear — name it), `pgrep -lf caffeinate`, `pmset -g | grep -E 'sleep|powernap'` (unchanged: `sleep 1 / disksleep 10 / powernap 1` — no privileged change was made in this phase, so nothing to revert), `git log --oneline -- results/phase25_frontier.json` (one line), `git log --oneline -1 -- results/phase26_canary.json` (Branch A: the operator's SHA from Task 2). State the wall-clock: kickstart time from §5 to the driver's exit from `logs/phase26_canary.out`, against §3's ≈ 25 h. Update §7's pending register to "nothing pending". Commit the note. Final gate: `make test` green (quote counts) and `.venv/bin/pytest -q tests/test_phase25_close.py` green.

Threat refs: T-26-04 (the supervisor is booted out and `launchctl list` is quoted empty of phase26 — nothing can re-enter a point after the close).
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && ! launchctl list | grep -q com.personacore.phase26.canary && .venv/bin/pytest -q tests/test_phase26_prereg.py tests/test_phase26_canary.py tests/test_phase25_close.py -x && grep -q "The machine put back" results/phase26_operational_note.md</automated>
  </verify>
  <acceptance_criteria>
    - `launchctl list | grep com.personacore.phase26.canary` is empty; no `caffeinate -dims` process whose parent is a `phase26_canary.py` process
    - §8 contains `The machine put back`, the quoted `launchctl list` and `pmset -g assertions` outputs, and (Branch A) the operator's commit SHA for `results/phase26_canary.json`
    - `git log --oneline -- results/phase25_frontier.json` has exactly one line; `tests/test_phase25_close.py` green
    - Branch A: `git ls-files results/phase26_canary.json` non-empty and `test_phase26_prereg_is_frozen_before_every_phase26_result` reports `checked == len(prereg_commits) * 2`
    - `make test` green; counts quoted in the SUMMARY with the delta over the Phase-25 close counts (2691 passed / 62 skipped on CI)
  </acceptance_criteria>
  <done>Machine put back, close recorded and committed, suite green; the phase is ready for `/gsd:verify-work 26`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| data/ sidecars → results/phase26_canary.json | 17 gitignored run-time records become one committed artifact through `emit` only |
| operator → git | The only write to git in the run's lifetime is the operator's commit of the artifact |
| results/phase26_canary.json → Phase 28 | Downstream may quote ε only beside its verdict (D-40 continuation) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-03 | Repudiation | partial / favourable assembly | mitigate | `emit` refuses < 17 sidecars; Branch B writes the D-19 entry and NOTHING else; write-once `_prove`; `--force` never used in this plan |
| T-26-05 | Elevation | driver git surface | mitigate | Task 2 is a human action; Task 1 asserts the artifact is UNTRACKED after emit |
| T-26-07 | Spoofing | wrong-weights records assembled | mitigate | `emit` re-checks all 16 `adapter_sha256` and the OFF `base_sha256` against the frontier and the live base |
| T-26-11 | Tampering | frontier re-emitted with the verdict | mitigate | Sibling artifact only (WR-03); `test_the_frontier_artifact_is_unchanged_since_its_single_write` stays green |
| T-26-SC | Tampering | package installs | accept | Nothing installed |
</threat_model>

<verification>
- Branch A: artifact emitted once, proven by tests in the present state, committed by the operator, machine put back
- Branch B: no artifact, dated D-19 entry with the publication clause, machine put back
- `make test` green; frontier at one commit; `phase26_prereg.py` untouched since 26-01
</verification>

<success_criteria>
- SC3 holds: the verdict travels with the frontier by proven digest link, or its absence is a named, dated limitation that Phase 28 is bound to repeat beside every ε
</success_criteria>

<output>
Create `.planning/phases/26-empirical-privacy-audit-canary/26-05-SUMMARY.md` when done
</output>
