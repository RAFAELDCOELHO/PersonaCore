---
phase: 22-dp-sgd-core-accountant-and-the-correctness-battery
plan: 17
type: execute
wave: 11
gap_closure: true
depends_on: ["22-12"]
files_modified:
  - src/personacore/privacy/accountant.py
  - tests/fixtures/phase22_reference.py
  - tests/test_phase22_accountant.py
autonomous: true
requirements: [DPSGD-03]
validates: [V-26, V-27]
closes_gaps:
  - "Route the erfc-SUBNORMAL band to the asymptotic series: change `_log_erfc`'s fast-path guard from `if e > 0.0` to the smallest-normal threshold"
  - "A guard that sweeps the ROUTING BOUNDARY rather than a fixed point list, so the next boundary move is caught without a verifier finding it"

must_haves:
  truths:
    - "`_log_erfc` no longer answers an erfc-SUBNORMAL input out of `math.log(math.erfc(x))`. MEASURED over x in [20, 30] step 0.05 against 120-dps truth: the worst CHOSEN-ROUTE relative error falls from 1.2369e-05 (at x=27.15) to 1.7586e-16 (at x=26.95), and 1.7586e-16 is the theoretical floor for ANY routing rule over that band — so no third blind band is opened"
    - "The two-oracle gap at sigma=0.414 / T=200 / delta=1e-5 falls from 1.9190e-03 to 1.0137e-11 against a 1e-9 budget that is NOT widened"
    - "The FROZEN pre-registration does not move: all 7 `GOLDEN_EPSILON` epsilons are `float.hex()`-BIT-IDENTICAL across the change, because exactly one of the 19 pinned points has `b >= 26.54325845425098` and that one is already on the series route"
    - "A guard sweeps x across ALL THREE `math.erfc` regimes — normal, subnormal, exactly 0.0 — and asserts the route `_log_erfc` ACTUALLY CHOSE is accurate at every point, so it reddens on a boundary move toward the lossy side WITHOUT naming the boundary"
    - "The residual sliver x in [26.54325845425098, ~26.70) — where the new boundary routes to the series and route L is marginally better — is PINNED with its measurement and with the reason the boundary is deliberately NOT moved to the crossover"
    - "The guard was WATCHED failing: reverting the predicate to `if e > 0.0` reddens named tests with their messages captured verbatim, and the file restores byte-identically by sha256"
  artifacts:
    - path: "src/personacore/privacy/accountant.py"
      provides: "a `_log_erfc` whose fast path is taken only where `math.erfc` still returns a NORMAL float"
      contains: "ldexp"
    - path: "tests/fixtures/phase22_reference.py"
      provides: "`LOG_ERFC_BAND` — committed 60-dps `log(erfc(x))` truths spanning all three erfc regimes"
      contains: "LOG_ERFC_BAND"
    - path: "tests/test_phase22_accountant.py"
      provides: "the band sweep asserting the chosen route is accurate, with a three-regime non-vacuity meta-guard"
      contains: "LOG_ERFC_BAND"
  key_links:
    - from: "src/personacore/privacy/accountant.py::_log_erfc"
      to: "the asymptotic series"
      via: "a fast-path predicate keyed on float64's smallest NORMAL rather than on strict positivity"
      pattern: "ldexp\\(1\\.0, -1022\\)"
    - from: "tests/test_phase22_accountant.py"
      to: "tests/fixtures/phase22_reference.py::LOG_ERFC_BAND"
      via: "a parametrized sweep over the committed band"
      pattern: "LOG_ERFC_BAND"
---

<objective>
Round 1 closed the band where `math.erfc(b)` underflows to exactly `0.0`. The band DIRECTLY BELOW
it — where `math.erfc` returns a SUBNORMAL, having already thrown away up to 52 of its 53 mantissa
bits — was untouched. `_log_erfc` routes on `if e > 0.0`, true throughout that range, so all 0.657
of it takes `math.log` of a float whose low bits are already gone, while the asymptotic series nine
lines below is more accurate there and never runs.

Purpose: this is worse than round 1 in two ways that matter. It is **reachable at this project's
own frozen delta** — T=200, delta=1e-5, sigma in [0.4135, 0.4185], 0.03 in sigma from the original
failure — and its direction is **privacy-UNDERSTATING** at four measured sigmas, so round 1's
"no published number is optimistic" no longer covers it. `delta_closed` in that band is
BIT-IDENTICAL to the pre-22-12 code: a sibling defect the fix stepped over, not a regression.

Output: a one-line predicate change whose worst case EQUALS the theoretical floor for any routing
rule, and — the real deliverable — a guard shaped so that the NEXT boundary move is caught by the
suite instead of by a verifier.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-dp-sgd-core-accountant-and-the-correctness-battery/22-VERIFICATION.md
@.planning/phases/22-dp-sgd-core-accountant-and-the-correctness-battery/22-12-SUMMARY.md
</context>

<environment>
**Python 3.11 venv is MANDATORY.** The dev box runs 3.14 and a pyenv 3.12.13 shadows it.

- `make test` is BROKEN (bare `pytest` reaches pyenv and produces ~83 `ModuleNotFoundError: torch`).
  Use `.venv/bin/python -m pytest -q` and `.venv/bin/ruff check . && .venv/bin/ruff format --check .`
  — `make lint` exits 0 but runs a different ruff over a different file set.
- The full suite takes **~222 s**; allow a **400 s+** timeout or it will look like a hang.
- Baseline at this plan's start: **1314 passed, 1 skipped**; ruff clean over 203 files. CPU only.
- `ruff` `line-length = 100`.
- **`scripts/mitigation_accountant.py` is a FROZEN pre-registration with NO correction path. NEVER
  edit it.** Read it; assert it did not move; never write it.
- `accountant.py` imports `math` and NOTHING else — `test_accountant_imports_math_only` asserts hard
  equality `== {"math"}` statically AND transitively (out of process). Do not reach for
  `sys.float_info` inside the module.
- **mpmath may ONLY be a one-off `.venv/bin/python -c` invocation whose OUTPUT is committed as a
  decimal string**, with the exact invocation and which `mp.mpf` form the inputs took recorded in the
  provenance comment. It is present transitively (torch to sympy to mpmath) and is declared in
  neither `pyproject.toml` nor `requirements.txt`;
  `tests/test_phase22_reference.py::test_no_phase22_test_imports_mpmath` enforces the no-import rule
  by AST over the whole `test_phase22_*` glob (RPT-03).
- **Commit-message hazard:** backticks inside `git commit -m "..."` are executed by zsh — in 22-12 a
  message containing a backticked phrase lost four words and created a stray file. Use
  `git commit -F -` with a QUOTED heredoc for every commit in this plan.
- **`gsd-sdk` mutation handlers corrupt planning frontmatter — nine consecutive sessions.** Use the
  `--flag` form only; write `22-17-SUMMARY.md` BEFORE `roadmap.update-plan-progress`; then
  `git diff .planning/`, hand-repair, and commit.
</environment>

<tasks>

<task type="auto">
  <name>Task 1: route the erfc-subnormal band to the series, and correct the three docstring premises the old predicate left false</name>
  <files>src/personacore/privacy/accountant.py</files>
  <read_first>
    - `src/personacore/privacy/accountant.py:148-225` — `_log_erfc` in full: the docstring's
      inertness paragraph, the `e > 0.0` predicate at `:196-198`, the `x <= 0.0` refusal and its
      "requires `x > 27.2`" reachability claim, and the optimal-truncation series
    - `src/personacore/privacy/accountant.py:283-345` — `delta_closed`'s `z`/`a`/`b`, the
      `_log_erfc(b)` call site at `:313` and the two refusals below it
    - `src/personacore/privacy/accountant.py:684-693` — `_delta_or_below_float64`'s paragraph
      arguing `_log_erfc`'s fifth `ValueError` is unreachable, which cites "erfc(x) == 0.0, which
      first happens at x ~ 27.2" as its premise
    - `.planning/phases/22-dp-sgd-core-accountant-and-the-correctness-battery/22-VERIFICATION.md`
      lines 179-212 — the measured band, the reachability table and the bit-identity finding
  </read_first>
  <action>
**Step 1 — MEASURE BEFORE CHANGING. Do not transcribe the figures below; reproduce them.**

Three planning-session measurements are reported here so you have a target to check against. If any
does not reproduce, report the divergence in the SUMMARY rather than smoothing it — five executors
in round 1 caught plan defects this way and two retracted their own claims after measuring.

(a) The band's edges, bisected: `math.erfc` first returns a SUBNORMAL at
x = **26.54325845425098** and first returns exactly `0.0` at x = **27.2**. So the defective band is
0.657 wide and the current predicate `e > 0.0` answers 100% of it from `math.log`.

(b) The crossover, adjudicated at `mp.dps = 120` over x in [20, 30] step 0.05, comparing BOTH
routes against truth — route L is `math.log(math.erfc(x))`, route S is the shipped asymptotic
series: the TRUE crossover (where S overtakes L and stays ahead) is at x ~ **26.70**. The worst
CHOSEN-ROUTE relative error is **1.2369e-05 at x=27.15** under the shipped predicate,
**1.7586e-16 at x=26.95** under the candidate, and the PERFECT-ROUTING floor — the maximum over x
of `min(relL, relS)` — is **1.7586e-16**, the same number. **The candidate equals the floor: no
routing rule can do better over that band, so no third blind band is opened.** This is the question
round 1 skipped; it is answered by measurement, and your job is to confirm it, not to re-derive it.

(c) The large-x tail, series against 120-dps truth at x in {30, 40, 60, 100, 150, 300, 1000, 1e4,
1e6}: worst **5.812e-17** (at x=1e6). The expansion is asymptotic and improves with x, so widening
the series' domain downward creates no tail defect.

Use a scratch file outside the repository for these; nothing measured here is committed by this task.

**Step 2 — change the predicate.**

Add a module-level constant beside the existing ones near the top of `accountant.py`, named for what
it is (float64's smallest NORMAL positive value) and DERIVED FROM `math` rather than spelled as a
magic literal: `math.ldexp(1.0, -1022)`. Verify it equals `2.2250738585072014e-308` and equals
`sys.float_info.min` in a throwaway interpreter — but do NOT import `sys` in the module.
`test_accountant_imports_math_only` asserts hard equality with `{"math"}` statically and out of
process, so an import added here fails loudly and correctly.

Change `_log_erfc`'s fast-path guard from `if e > 0.0:` to a comparison against that constant, so
the fast path runs only where `math.erfc` returned a float that has NOT already lost mantissa bits,
and the series answers everything below it. This is one hunk.

**Step 3 — correct three premises the old predicate leaves false. All three are load-bearing prose,
not decoration, and `22-VERIFICATION.md` records the first as a Blocker-severity anti-pattern.**

(i) `_log_erfc`'s docstring opens *"THE FAST PATH IS UNCONDITIONAL AND FIRST, AND THAT INERTNESS IS
THE LOAD-BEARING PROPERTY OF THIS FUNCTION."* Measured, that unconditional inertness is exactly what
preserved a 23.3% error across the subnormal band. The fast path is no longer unconditional. Rewrite
the paragraph so it states what is now true and why it is still safe for the frozen pin: the fast
path is taken wherever `math.erfc` returns a NORMAL float, which is bit-for-bit the arithmetic
`delta_closed` already performed, and every pinned point is in that region — measured, exactly one
of the 19 pinned points has `b >= 26.54325845425098` (the thirteenth `DELTA_FRONTIER` row at
b = 28.01573320140291) and it was already on the series route. Do not delete the historical record;
state what changed and why.

(ii) The `Args:` block says the series branch *"is reached only when `erfc(x)` has underflowed,
which requires `x > 27.2`"*. After this change it requires `x >= 26.54325845425098`. Correct it.

(iii) `_delta_or_below_float64`'s reachability paragraph argues `_log_erfc`'s `x <= 0.0` refusal is
unreachable from its premise *"`math.erfc(x)` to have underflowed to exactly `0.0`"* and the
measurement *"`math.erfc` first returns exactly `0.0` at x = 27.2"*. The CONCLUSION still holds —
`math.erfc` is monotonically decreasing with `erfc(x) >= 1.0` for every `x <= 0.0`, so
`erfc(x) < smallest-normal` cannot be true there either — but the PREMISE has changed. Restate it
against the new predicate with the new boundary measurement, so the argument reads true rather than
merely reaching a true conclusion from a stale fact.

**Step 4 — PIN THE RESIDUAL SLIVER, in a comment beside the predicate, with its measurement.**

The new boundary (x = 26.54325845425098, where `erfc` stops being normal) sits ~0.16 BELOW the true
crossover (~26.70). In the sliver between them the predicate routes to the series where route L is
in principle marginally better.

**MEASURE THE SLIVER YOURSELF; DO NOT TRANSCRIBE A FIGURE FROM THIS PLAN.** An earlier draft of this
step carried "route L at 5.706e-18 vs route S at 1.649e-16, a ~29x ratio". Those numbers are a
GRID ARTIFACT and are retracted here: they came from a sweep that accumulated `x += 0.05` from 20.0,
so its "26.65" was really `26.650000000000098`. Re-measured at clean grid points AND at that sweep's
own points, `series(x)` and `math.log(math.erfc(x))` are **BIT-IDENTICAL at every point in the
sliver** — a 1.0x ratio, not 29x. The conclusion strengthens (the sliver costs exactly zero on the
grid) but the figures were false, and a false figure in a committed comment is the precise thing
plan 22-19 exists to undo.

So: sweep both routes across `[26.54325845425098, 26.70)` against 120-dps truth, record what you
actually find with its grid stated, and put THAT in the comment. If you find a point where the two
routes differ, say so with the x that produced it. Whatever you measure, confirm it does not raise
the worst case above the 1.7586e-16 floor from step 1(b) — that is the property being pinned, and it
does not depend on the ratio.

Record, in the comment, WHY the boundary is deliberately not moved up to the crossover: the
smallest-normal threshold is a property of the FLOAT64 FORMAT, while the crossover is a property of
this platform's `math.erfc` implementation and would have to be re-measured on every libm it runs
against. Erring toward the numerically stable route at a cost bounded by machine epsilon is the
engineering choice; "optimising" the boundary upward re-opens the lossy side, which is the defect
this plan exists to close. Write it so a future reader cannot make that change without reading why.

**Step 5 — prove the frozen pin did not move, by capture rather than by argument.**

Before the edit, capture `float.hex()` of every point the module already answers: all 7
`scripts/mitigation_accountant.py::GOLDEN_EPSILON` epsilons re-derived through `epsilon_for`, and
every representable `tests/fixtures/phase22_reference.py::DELTA_FRONTIER` delta through
`delta_closed`. Re-capture after. `diff` of the two captures must be EMPTY. Paste both captures, or
the empty diff plus the count, into the SUMMARY — this is the single irrecoverable risk in the plan
and an argument is not evidence for it.

`git diff --exit-code -- scripts/mitigation_accountant.py` must exit 0 at the end of this task.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase22_accountant.py tests/test_phase22_reference.py -q</automated>
  </verify>
  <done>
`_log_erfc`'s fast path is guarded on a `math.ldexp(1.0, -1022)`-derived constant; the three stale
premises are corrected; the residual sliver is pinned with its measurement and its
do-not-optimise reason; the 193 accountant + reference tests still pass; the before/after
`float.hex()` diff over all 19 pinned points is EMPTY; `git diff --exit-code -- scripts/` exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: commit a 60-dps band truth table and sweep the ROUTING BOUNDARY with it</name>
  <files>tests/fixtures/phase22_reference.py, tests/test_phase22_accountant.py</files>
  <read_first>
    - `tests/fixtures/phase22_reference.py:1-21` and `:56-98` — the module contract (ZERO imports,
      ZERO executable logic, asserted by AST) and the thirteenth row's three-route provenance block,
      which is the format this task's provenance must follow
    - `tests/test_phase22_accountant.py:176-278` — `_erfc_b`, `_pinned_points`, `_inert_points`,
      `test_log_erfc_inert_points_are_not_empty` and
      `test_log_erfc_is_inert_where_erfc_is_healthy`: the guard this task's sweep sits BESIDE and
      must not duplicate
    - `tests/test_phase22_accountant.py:281-339` —
      `test_log_erfc_matches_the_committed_underflow_truth`: the single-point precedent, its
      meta-guard, its 1e-15 measured tolerance and the absolute-versus-relative note in its docstring
    - `tests/test_phase22_reference.py:117-138` — `test_reference_fixture_imports_nothing`, the AST
      guard your new constant must not break
  </read_first>
  <action>
**THE STRUCTURAL ASK LIVES IN THIS TASK.** Round 1 fixed a point and the next verifier found the
adjacent band. A guard that pins one more point produces round 3. What is needed is a guard whose
parametrization is the routing boundary's own neighbourhood, not a list of points.

**Step 0 — a shape that was tried while planning and MUST NOT be built, with the measurement that
killed it.** The obvious design is a locator that finds the boundary by bisecting on
`_log_erfc(x) == math.log(math.erfc(x))` — "bit-identical means the fast path ran" — and then
asserts something about the located x. **It is unsound.** MEASURED: under the candidate predicate,
that bisection reports the boundary at 26.65034741066756 instead of 26.54325845425098, because
**227 of the first 400 floats above the real crossing have `series(x) == math.log(math.erfc(x))`
BIT-FOR-BIT**, so the detector cannot tell the routes apart there and the bisection walks straight
through the boundary. Do not build a behavioural route detector. Do not build an AST one either:
it would pin the predicate's SYNTAX rather than its effect, and break on a rewrite that is
behaviourally identical.

**The shape that works needs no boundary locator at all.** The closure property is not "the
boundary is at x" — it is **"whatever route `_log_erfc` takes at x, that route is accurate at x."**
Assert that directly, over a committed band that spans every `math.erfc` regime end to end.

**Step 1 — commit the band truth table.**

Add a constant to `tests/fixtures/phase22_reference.py` — the module has ZERO imports and ZERO
executable logic and both are AST-asserted, so this is literal data with a provenance comment, in
the shape section 1 and section 2b already use.

Name it for what it is (the `log(erfc(x))` band around `_log_erfc`'s routing boundary). Each row is
an `(x, truth_string)` pair: `x` a float literal — the EXACT binary64 the implementation evaluates —
and the truth a DECIMAL STRING at 20 significant digits.

The band must contain, at minimum, points in all three `math.erfc` regimes. The planning session
used these **seventeen** x values, which measure as 4 normal / 9 subnormal / 4 exactly-zero on this box:
`24.0, 25.0, 26.0, 26.4, 26.54325845425098, 26.6, 26.65, 26.7, 26.8, 26.9, 27.0, 27.151124073213406,
27.19, 27.2, 27.6, 28.01573320140291, 29.0`. Three of them are deliberate: `26.54325845425098` is the
subnormal boundary itself; `27.151124073213406` is the `b` at sigma=0.414 / T=200 — the worst
reachable point on this project's own frontier, and the `b` plan 22-18's new frontier row will use;
and **`26.8` closes a granularity gap in the UP direction** found by the plan-checker.

**Why 26.8 is load-bearing, stated so nobody prunes it as redundant.** Without it the band jumps
26.7 → 26.9. Row 26.7 scores 4.54e-16 under route L and PASSES the 1e-15 budget; row 26.9 scores
1.67e-11 and reddens. So a boundary moved up to anywhere in (26.7, 26.9) would leave this sweep
GREEN — measured end-to-end with a hostile boundary at x≈26.89, sigma=0.4190/T=200 gives a delta
relative error of 2.403e-11 against a correct 3.6e-14. That residual is 41x INSIDE the 1e-9
two-oracle budget and eight orders better than the 1.9e-03 this round closes, so it is bounded and
sub-budget rather than a round-3 seed — but `must_haves.truths[3]` should not over-claim it.
Adding 26.8 (measured: 3.87e-17 under the candidate, passes; 3.02e-14 under an up-moved boundary,
reddens) shrinks the hideable window to (26.7, 26.8) and the residual to ~3e-13.

Derive the truths with ONE `.venv/bin/python -c` invocation and commit its OUTPUT. Record the exact
invocation in the provenance comment, INCLUDING which `mp.mpf` form the inputs took — 22-12 measured
`mp.mpf(float)` and `mp.mpf("string")` differing by 8.90e-15 and recorded it, and a provenance that
does not say which was used is not reproducible. The invocation is
`.venv/bin/python -c "from mpmath import mp; mp.dps=60; [print(repr(x), mp.nstr(mp.log(mp.erfc(mp.mpf(x))), 20)) for x in (24.0, 25.0, ...)]"` — inputs enter as `mp.mpf(<python float>)`, not as
`mp.mpf("<decimal string>")`.

Record in the provenance what this table does and does NOT buy: it is `mpmath`'s own `erfc` at 60
dps, so it catches a routing error and a float64 error and CANNOT catch an error in the asymptotic
expansion's derivation — both sides would have to be wrong together, and they come from different
implementations, which is why this is a real constraint and not a photograph. Say it plainly, as
`EPSILON_OVERFLOW_REGIME`'s own block already does for its narrower case.

**Step 2 — the sweep.**

Add ONE parametrized test to `tests/test_phase22_accountant.py`, near the two existing `_log_erfc`
guards. For each committed `(x, truth)` row it asserts that `_log_erfc(x)` — whatever route the
module chose — agrees with the committed truth to a RELATIVE tolerance.

MEASURE THE TOLERANCE FIRST. Planning measurement over the sixteen rows: worst **1.186e-16**
relative (at x=29.0) under the new predicate, against **1.591e-04** (at x=27.19) under the shipped
one — eleven orders apart. **1e-15** is the measured worst rounded up to the next decade and carries
**8.4x** of margin; it is also the tolerance the neighbouring single-point guard already uses, which
is why it is the right register here. Report your own measured worst in the docstring beside it. If
you measure worse than 2e-16, say so and take the next decade rather than silently widening — and if
the fix does not fit inside a tolerance the measurement supports, the fix is wrong, not the budget.

State the conversion in the docstring, as the neighbouring guard does: an ABSOLUTE error `d` in the
log is a RELATIVE error `d` in `exp(eps + log)`, so at |log| ~ 743 a 1e-15 relative bound here is a
~7.4e-13 relative bound on `delta_closed`'s second term — inside the frontier rows' own 1.5e-12
budget.

**Step 3 — the non-vacuity meta-guard, and it is what makes the sweep boundary-parametrized.**

A companion test (this file's established shape — `test_closed_form_frontier_parametrization_is_not_empty`, `test_round_trip_pairs_is_not_empty`,
`test_log_erfc_inert_points_are_not_empty`) that classifies every committed `x` by evaluating
`math.erfc(x)` AT RUN TIME — normal, subnormal, exactly zero — and asserts **all three classes are
non-empty**. Do NOT hardcode the 4/8/4 split: the classification is a property of the platform's
`math.erfc`, so computing it at run time is what keeps the guard true across libm versions, and
reporting the three counts in the assertion message is what makes a failure readable.

Write into that test's docstring WHY three non-empty classes is the whole mechanism: any predicate
`_log_erfc` could plausibly use draws its boundary somewhere between "erfc is a healthy normal
float" and "erfc is gone entirely", so a band covering all three regimes contains that boundary
wherever it is placed. The sweep therefore straddles the boundary WITHOUT ever naming it, and a
future edit that moves the boundary toward the lossy side puts previously-correct x on the wrong
route and reddens step 2 — automatically, with no plan and no verifier involved.

**Step 4 — state the guard's ONE direction honestly, in the same docstring.**

This sweep bounds the boundary from ABOVE only. A boundary moved DOWN — the series used where
`math.log(math.erfc(x))` is marginally better — leaves it GREEN, because below the crossover BOTH
routes are within ~2e-16 and neither breaches the tolerance. That direction is owned by the existing
`test_log_erfc_is_inert_where_erfc_is_healthy` and its hard-count companion, which 22-12 watched
under mutation M-B: deleting the fast path moves six of the seven pinned epsilons and drives four to
`0.0`. Name that test and that mutation in the docstring. The two guards bound the boundary from
opposite sides for two different reasons, and a reader who sees only one of them will believe this
one is broader than it is.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase22_accountant.py tests/test_phase22_reference.py -q</automated>
  </verify>
  <done>
`tests/fixtures/phase22_reference.py` carries a committed 60-dps `log(erfc(x))` band table with a
provenance block naming the invocation, the `mp.mpf` form and the table's limits; the fixture still
passes `test_reference_fixture_imports_nothing`; `tests/test_phase22_accountant.py` carries the
parametrized band sweep plus a companion asserting all three `math.erfc` regimes are non-empty at
run time; the sweep's tolerance is the measured worst rounded up, with the measurement and the
one-directional scope both stated in the docstring.
  </done>
</task>

<task type="auto">
  <name>Task 3: watch the predicate revert redden, restore byte-identically, and re-green the full suite</name>
  <files>src/personacore/privacy/accountant.py</files>
  <read_first>
    - `.planning/phases/22-dp-sgd-core-accountant-and-the-correctness-battery/22-12-SUMMARY.md`
      "Mutations Watched Failing" — the register format: real committed file, one shot each,
      sha256 before and after, verbatim assertion messages, DISTINCT-test counts separate from node
      id counts
    - `.planning/phases/22-dp-sgd-core-accountant-and-the-correctness-battery/22-14-SUMMARY.md:237-246`
      and `22-15-SUMMARY.md:321-326` — the finding this task must anticipate
  </read_first>
  <action>
**M-H — revert `_log_erfc`'s fast-path predicate to `if e > 0.0:`.** Apply it to the REAL committed
module, run the suite, capture the verbatim assertion messages, restore, and verify the restore by
`sha256` AND by `git diff --exit-code -- src/personacore/privacy/accountant.py` exiting 0.

**COUNT THE HUNKS BEFORE CLAIMING A CLEAN WATCH.** In round 1, TWO of three plans specified a
mutation as one hunk where the fix shipped as two independent layers (22-14's M-D, 22-15's M-E), and
in both cases applying one layer left the suite GREEN — a green line that looks like a blind guard
and is actually an inert mutation, which is the opposite finding. Here the fix is expected to be one
behavioural hunk (the predicate) plus one inert one (the constant's definition, which nothing else
reads). **Verify that expectation rather than inheriting it:** revert the predicate ALONE first and
record the result; if reverting the constant as well changes anything, report both results
separately as `M-H` and `M-H-both`, following the two precedents named above.

Expected from the planning measurement, to be confirmed rather than transcribed: M-H should redden
the Task 2 band sweep (worst 1.591e-04 against a 1e-15 budget — eleven orders). It should NOT redden
`test_log_erfc_matches_the_committed_underflow_truth`, because that guard's `b = 28.01573320140291`
has `erfc(b)` exactly `0.0` and takes the series under BOTH predicates — a green there is CORRECT
and must be reported as correct rather than "fixed" into reddening. It should also NOT redden
`test_log_erfc_is_inert_where_erfc_is_healthy`, because every pinned point in that sweep has a
NORMAL `erfc(b)` and both predicates route it to `math.log`; that guard is a no-move guard, not a
correctness one, and saying so is part of the register.

Record DISTINCT test functions and node ids separately, as 22-12 does.

**Then close the plan.** Run the FULL suite — `.venv/bin/python -m pytest -q`, ~222 s, allow 400 s+
— and account for the delta from the 1314-passed baseline EXACTLY: every new node id must be
attributable to the band sweep's row count plus the companion. An unexplained delta is a finding,
not a rounding error. Run `.venv/bin/ruff check . && .venv/bin/ruff format --check .`.

Assert, and put the outputs in the SUMMARY: `git diff --exit-code -- scripts/mitigation_accountant.py`
exits 0; `git diff --exit-code -- pyproject.toml requirements.txt` exits 0;
`grep -n "^import \|^from " src/personacore/privacy/accountant.py` returns the single `import math`;
and `grep -rn "float_info" src/` is empty.

**A brief premise that did NOT reproduce, recorded here so you do not repeat it.** This plan's
dispatch brief states the tree has ZERO uses of `sys.float_info` and that
`grep -rln "float_info" src/ tests/ scripts/` is empty. **Measured, it is not:** plan 22-15 shipped
`sys.float_info.max` at `tests/test_phase22_accountant.py:657`, under a docstring at `:646-649`
arguing explicitly that *"`sys.float_info` is available HERE and deliberately not in the module"*.
The binding constraint is therefore the MODULE's import ceiling, which is unchanged and which this
plan honours by deriving the constant from `math.ldexp`. `src/` and `scripts/` are genuinely clean.
Re-run the grep yourself and report what you find.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q</automated>
  </verify>
  <done>
M-H applied to the real committed module and WATCHED reddening with its verbatim messages captured,
its hunk count verified rather than assumed, and the two guards that correctly stay GREEN reported
as correct; the file restored with a matching `sha256` and a clean `git diff --exit-code`; full
suite green with the node-count delta accounted for exactly; ruff clean; the frozen pin,
`pyproject.toml` and the module's import ceiling all asserted unmoved.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `math.erfc`'s return value → a routing decision | A returned float carries no signal that it is subnormal. A predicate that reads only "is it positive" cannot tell a healthy value from one that has lost 52 of 53 mantissa bits. |
| a committed reference table → the tolerance a guard enforces | A table that silently truncates, or whose rows all land on one side of the boundary, leaves the sweep green while checking one route. |
| an accountant edit → a FROZEN pre-registration with no correction path | `GOLDEN_EPSILON` cannot be amended. A routing change that moves a pinned epsilon is unrecoverable rather than merely wrong. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-39 | Information Disclosure | `_log_erfc` answering an erfc-subnormal input from `math.log`, understating epsilon at four measured sigmas | mitigate | Fast-path predicate keyed on float64's smallest NORMAL; worst chosen-route error measured equal to the perfect-routing floor; watched reddening under M-H |
| T-22-40 | Tampering | a future edit moving the routing boundary back toward the lossy side | mitigate | Task 2's band sweep asserts the CHOSEN route is accurate across all three `math.erfc` regimes, so it reddens without naming the boundary; the residual-sliver comment states why the boundary is not moved toward the crossover |
| T-22-41 | Tampering | a boundary moved DOWN, sending pinned rows through the series | mitigate | Owned by the existing `test_log_erfc_is_inert_where_erfc_is_healthy` + hard-count companion (22-12's M-B: 6 of 7 pinned epsilons move, 4 to `0.0`); the scope limit is stated in Task 2's docstring rather than left implied |
| T-22-42 | Spoofing | the band table going vacuous — truncated, or all rows on one side of the boundary | mitigate | Companion test classifies every row by `math.erfc` AT RUN TIME and asserts all three regimes non-empty, with counts in the message |
| T-22-43 | Tampering | the frozen pre-registration moving | mitigate | `float.hex()` capture of all 19 pinned points before and after, diff asserted EMPTY; `git diff --exit-code -- scripts/mitigation_accountant.py` at every gate |
| T-22-44 | Tampering | `sys` or `mpmath` imported into `accountant.py` to reach the constant | mitigate | Constant derived from `math.ldexp(1.0, -1022)`; `test_accountant_imports_math_only` asserts hard equality statically and out of process; `grep -rn "float_info" src/` asserted empty |
| T-22-SC | Tampering | npm/pip/cargo installs | accept | No installs. `git diff --exit-code -- pyproject.toml requirements.txt` exits 0. mpmath enters only as a one-off shell invocation whose output is committed as data (RPT-03). |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q` — 1314 passed + this plan's new node ids, 1 skipped, delta accounted for exactly
- `.venv/bin/ruff check . && .venv/bin/ruff format --check .` — clean
- `git diff --exit-code -- scripts/mitigation_accountant.py` — exit 0
- `git diff --exit-code -- pyproject.toml requirements.txt` — exit 0
- before/after `float.hex()` capture over all 19 pinned points — diff EMPTY
- M-H watched RED with verbatim messages; restore verified by `sha256` and by `git diff --exit-code`
</verification>

<success_criteria>
- `_log_erfc`'s fast path is taken only where `math.erfc` returns a NORMAL float, via a
  `math.ldexp(1.0, -1022)`-derived constant and no new import
- The two-oracle gap at sigma=0.414 / T=200 / delta=1e-5 is inside the UNWIDENED 1e-9 budget
- All 7 `GOLDEN_EPSILON` epsilons and all representable `DELTA_FRONTIER` deltas are bit-identical
- A committed band table spans all three `math.erfc` regimes, and a sweep asserts the chosen route
  is accurate at every row, with run-time three-regime non-vacuity
- The residual sliver and the guard's one-directional scope are both PINNED with measurements
- M-H watched reddening on the real module, hunk count verified, byte-identical restore
</success_criteria>

<output>
Create `.planning/phases/22-dp-sgd-core-accountant-and-the-correctness-battery/22-17-SUMMARY.md`
when done.
</output>
