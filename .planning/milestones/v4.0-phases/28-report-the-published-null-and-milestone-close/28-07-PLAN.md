---
phase: 28-report-the-published-null-and-milestone-close
plan: 07
type: execute
wave: 6
depends_on: ["28-06"]
files_modified:
  - results/phase28_ledger.json
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
requirements: [RPT-01, RPT-03]
must_haves:
  truths:
    - "The developer pushed `main` to `origin/main` and a CI run on `origin/main` whose head contains the publishing commit (and therefore Phase 27's whole code surface) concluded `success`; Claude never ran `git push` (D-38)"
    - "The run id, URL, head SHA, conclusion and date land in `results/phase28_ledger.json::close.ci_run`; `rows` are byte-unchanged so the published block's rows digest still matches and `test_report_block_is_byte_identical` stays green (D-38, D-20, D-21)"
    - "RPT-01 and RPT-03 are ticked in `.planning/REQUIREMENTS.md` with traceability rows naming the record fields, the publishing commit, and the guard tests — text of the requirements unchanged; the ROADMAP Phase 28 plan list is ticked, its progress row updated, its phase checkbox left for `/gsd:verify-work 28`; `STATE.md` position and decisions updated — ALL BY HAND, snapshot before, `diff -u` after, zero `gsd-sdk` mutation handlers (D-34)"
    - "`PROJECT.md`, `MILESTONES.md` and the `v4.0` tag are NOT touched — they belong to `/gsd-complete-milestone` (D-34)"
    - "The full suite and `make lint` are green on the final committed tree, and `python scripts/phase28_report.py check` exits 0 (D-17)"
  artifacts:
    - path: "results/phase28_ledger.json"
      provides: "close.ci_run filled with the green run"
      contains: "\"ci_run\": {"
    - path: ".planning/REQUIREMENTS.md"
      provides: "RPT-01 / RPT-03 ticked with traceability rows"
      contains: "- [x] **RPT-01**"
  key_links:
    - from: "results/phase28_ledger.json::close.ci_run.head_sha"
      to: "the publishing commit"
      via: "git merge-base --is-ancestor <publishing sha> <head_sha> exits 0"
      pattern: "head_sha"
    - from: ".planning/REQUIREMENTS.md RPT-01 row"
      to: "docs/REPORT.md span + tests/test_phase28_*.py"
      via: "row names the sentinel stem, the publishing commit and the guard node ids"
      pattern: "PHASE28-REPORT"
---

<objective>
Close the phase where CONTEXT says it stops: the developer's push and a green CI run on
`origin/main` (a human checkpoint), the run id into the ledger's `close` block, and the hand-made
ticks of RPT-01 / RPT-03, the ROADMAP row and STATE.md. Milestone archival, `MILESTONES.md`,
`PROJECT.md` and the tag are the orchestrator's `/gsd-complete-milestone`.

Decisions implemented: D-34, D-38.

Purpose: D-38 — Phase 27's code and the report have never run in CI; the 25-UAT precedent found
eight real defects that way. The close precondition is measured, not assumed.
Output: the ledger `close` block, three hand-edited planning files with diffs, the final commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-report-the-published-null-and-milestone-close/28-CONTEXT.md
@.planning/phases/28-report-the-published-null-and-milestone-close/28-06-SUMMARY.md
@.planning/phases/27-relearning-attack/27-05-SUMMARY.md

<interfaces>
Ledger close shape (tests/test_phase28_ledger.py::test_close_block_shape): close.ci_run is null OR {"id": <str>, "url": <str>, "head_sha": <40-hex>, "conclusion": "success", "recorded": "YYYY-MM-DD"}; rows must not change (rows digest is pinned in the published block)
CI: .github/workflows/ci.yml — jobs `test` (fetch-depth: 0, Python 3.11, ruff, pytest -q, 21-24 min) and `demo-asset`; `gh run list --branch main --limit 3 --json databaseId,headSha,conclusion,status,url,createdAt`; `gh run watch <id>`; measured 2026-09-20: main is 31 commits ahead of origin/main (origin newest 8f43342)
Hand-edit anchors: REQUIREMENTS.md:449 `- [ ] **RPT-01**`, :455 `- [ ] **RPT-03**`, :579-580 traceability rows `| RPT-01 | Phase 28 | |` / `| RPT-03 | Phase 28 | |`; ROADMAP.md:158 `- [ ] **Phase 28: …` (phase checkbox — leave for verify-work), the `### Phase 28` plan list (filled by the planner), :1132 progress row `| 28. Report, the Published Null, and Milestone Close | v4.0 | 0/7 | Planned | - |`; STATE.md:6 `stopped_at`, :7 `last_updated`, :26 `## Current Position`, :1012 `### Decisions`, :1579 `## Session Continuity`
Precedent for the row shape: 27-05-PLAN.md Task 3 step 5 and 27-05-SUMMARY.md §"Planning-file edits"
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: The developer pushes main and waits for a green CI run on origin/main (D-38)</name>
  <what-built>Every Phase 28 commit is on local `main`: the docstring fixes and D-25 test (28-01), the stamp repairs (28-02), the ledger (28-03), the renderer and templates (28-04), the guards (28-05), and the publishing commit (28-06). `main` is ahead of `origin/main` by every Phase 27 and Phase 28 commit, so none of that code has run in CI. The local full suite is green (28-06 SUMMARY). Pushing is outward-facing and yours to take.</what-built>
  <how-to-verify>
    1. `git status --short` — empty (a peer session may have left changes; nothing unexpected should be staged).
    2. `git log --oneline origin/main..main | wc -l` and `git log --oneline -1` — confirm the publishing commit is the tip or below it.
    3. `git push origin main`.
    4. `gh run list --branch main --limit 1 --json databaseId,headSha,status,url` — note the id; `gh run watch <id> --exit-status` (21-24 min).
    5. On `success`, reply with the run id. On failure: paste the failing job's summary; Phase 28 code fixes are ordinary commits, but any change to the PUBLISHED blocks is a dated continuation via `append_addendum`, never a re-render (D-20) — then push again and repeat.
  </how-to-verify>
  <resume-signal>Paste the green run id (digits only) — or "failed: <summary>".</resume-signal>
  <files>results/phase28_ledger.json</files>
  <read_first>
    - .planning/phases/28-report-the-published-null-and-milestone-close/28-06-SUMMARY.md (publishing commit SHA, local suite counts)
    - .github/workflows/ci.yml (the two jobs the run must pass)
  </read_first>
  <action>Human action (D-38): the developer runs `git push origin main` and watches the CI run. Claude does not push. If the run fails on a Phase 28 test, fix in a new commit (tests/scripts) or a dated continuation (published blocks), ask the developer to push again, and repeat until green; every iteration is recorded in the SUMMARY.</action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && git fetch origin --quiet && test "$(git rev-list --count origin/main..main)" = "0" && gh run list --branch main --limit 1 --json conclusion,headSha --jq '.[0].conclusion' | grep -q "^success$" && test "$(gh run list --branch main --limit 1 --json headSha --jq '.[0].headSha')" = "$(git rev-parse origin/main)"</automated>
  </verify>
  <acceptance_criteria>
    - `git rev-list --count origin/main..main` is 0 after the push
    - `gh run list --branch main --limit 1 --json conclusion,headSha` reports `success` with `headSha == git rev-parse origin/main`, and `git merge-base --is-ancestor <publishing commit> <headSha>` exits 0
    - SUMMARY records the run id, URL, head SHA, and every failed iteration (if any) with its remedy
  </acceptance_criteria>
  <done>Phase 27 and the report have run green in CI on origin/main; the run id is known.</done>
</task>

<task type="auto">
  <name>Task 2: Run id into the ledger's close block; RPT ticks, ROADMAP row and STATE.md by hand; final commit and gates</name>
  <files>results/phase28_ledger.json, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - results/phase28_ledger.json (`close`, and the rows that must NOT change)
    - .planning/REQUIREMENTS.md:445-460 (RPT checkboxes) and :575-582 (the empty RPT-01/RPT-03 traceability rows; the filled RELRN rows above them as the shape)
    - .planning/ROADMAP.md "### Phase 28" block (plan list) and :1129-1132 (progress rows — Phase 26/27 entries at :1130-1131 as the closed shape; Phase 28's own row at :1132)
    - .planning/STATE.md:1-30, :1005-1020, :1575-1600
    - .planning/phases/27-relearning-attack/27-05-SUMMARY.md §"Planning-file edits (hand, diffed against a snapshot)"
    - .planning/phases/28-report-the-published-null-and-milestone-close/28-0[1-6]-SUMMARY.md (commit SHAs, counts, node ids to cite)
  </read_first>
  <action>
1. Ledger `close`: with `python -c`, load the ledger, set `close["ci_run"] = {"id": "<run id>", "url": "<url>", "head_sha": "<40-hex from gh>", "conclusion": "success", "recorded": "<today>"}`, assert `rows` unchanged (compare `json.dumps(rows, sort_keys=True)` before/after), re-dump byte-stably (`indent=2, sort_keys=True, ensure_ascii=False` + newline). Then `.venv/bin/pytest tests/test_phase28_ledger.py tests/test_phase28_report.py -q` — the byte-identity test MUST still pass (rows digest unchanged); if it fails, the rows changed — revert and redo.
2. Snapshot: `cp .planning/STATE.md .planning/ROADMAP.md .planning/REQUIREMENTS.md <scratchpad>/` . ZERO `gsd-sdk` mutation handlers (D-34; memory: eight consecutive sessions of frontmatter corruption). Edit with the Edit tool:
   - REQUIREMENTS.md: `- [ ] **RPT-01**` → `- [x] **RPT-01**`; `- [ ] **RPT-03**` → `- [x] **RPT-03**`; requirement TEXT lines unchanged. Traceability rows: `| RPT-01 | Phase 28 | **SATISFIED (plans 28-04, 28-05, 28-06).** The v4.0 section in docs/REPORT.md between <!-- PHASE28-REPORT-BEGIN/END --> (publishing commit <sha>) is rendered by scripts/phase28_report.py from results/phase25_frontier.json (verdicts.capacity_branch = null-at-both-capacities; verdicts.arm_existentials quoted verbatim), results/phase27_admission.json (cleared_counts; rows), results/phase26_canary.json and the phase23 records; the standing expectation is quoted from .planning/research/SUMMARY.md at c673b4c and tests/test_phase28_prereg.py proves c673b4c precedes every results/phase2[0-8]_* first-add. Guards: tests/test_phase28_report.py (byte-identity, template scan, obligation resolution, constants, digests, confound, lead), tests/test_phase28_prereg.py, tests/test_phase25_correction.py (normalized register). README Results-at-a-glance bullets rendered by the same mechanism (D-11). |` — write every clause from the SUMMARYs; no number that is not a quoted record value or a commit SHA. `| RPT-03 | Phase 28 | **SATISFIED (plans 28-01, 28-03).** [project].dependencies equal at v1.0/v2.0/v3.0/HEAD by tests/test_package.py::test_runtime_dependencies_identical_across_four_milestones (tomllib); the sha256 pin stays as a change detector under its true name; SC3's sha256 clause is recorded false as written (5065bc5, license = "MIT", ledger row SC3-SHA256-CLAUSE). results/phase28_ledger.json (commit <sha>) carries one disposition per open item — v3.0 tech-debt and stale-stamp counts by len() (quote the two measured counts from 28-03's SUMMARY as "measured: N / M") — closed domain guarded by tests/test_phase28_ledger.py; CI run <id> green on origin/main (D-38). |`.
   - ROADMAP.md: under `### Phase 28` tick each `- [ ] 28-0N-PLAN.md` → `- [x]` (the planner wrote the list); progress row → `| 28. Report, the Published Null, and Milestone Close | v4.0 | 7/7 | Plans complete — verification pending | <date> — v4.0 published as measured: null-at-both-capacities (docs/REPORT.md, publishing commit <sha>), README glance bullets rendered, ledger results/phase28_ledger.json with the measured row counts, CI run <id> green on origin/main; RPT-01/RPT-03 ticked by hand; zero gsd-sdk mutation handlers |`. The `- [ ] **Phase 28**` heading checkbox at :158 stays unticked for `/gsd:verify-work 28`.
   - STATE.md: frontmatter `stopped_at` → "Phase 28 COMPLETE (<date>) — published null-at-both-capacities (publishing commit <sha>), ledger + CI run <id> green on origin/main; next: /gsd:verify-work 28, then /gsd-complete-milestone v4.0" (keep the superseded stop record as the precedent does); `last_updated`; `## Current Position` → Phase 28, 7 of 7 plans complete; append `[Phase 28]` decisions under `### Decisions` (frozen-at-publish; rows digest excludes close; D-27 published as a one-liner; 24 stamp ruling; D-38 run id); `## Session Continuity` line. Do not touch `PROJECT.md` or `MILESTONES.md`.
3. `diff -u <snapshot> <file>` for each of the three; quote the three hunk counts and confirm every hunk is one of the edits above; repair any stray hunk by hand.
4. Gates on the committed tree: `make lint`; commit `git add results/phase28_ledger.json .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md && git commit -m "docs(28-07): close — CI run <id> green on origin/main recorded in the ledger; RPT-01/RPT-03 ticked by hand; ROADMAP + STATE updated"`; then `make test > <scratchpad>/28-07-make-test.log 2>&1 &` uncapped in the background; quote the summary line when done (`0 failed` expected); `.venv/bin/python scripts/phase28_report.py check` → exit 0.
5. Write `28-07-SUMMARY.md` with: the run id/URL/head SHA, the three diff summaries, the final suite line, and the sentence that milestone archival, `MILESTONES.md`, `PROJECT.md` and the `v4.0` tag are left to `/gsd-complete-milestone` (D-34). Commit the SUMMARY as `docs(28-07): summary`.
Threat references: T-28-07 (hand edits, snapshot/diff), T-28-02 (rows untouched; `check` exit 0), T-28-08 (no machine push).
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/python -c "
import json; L=json.load(open('results/phase28_ledger.json')); c=L['close']['ci_run']; assert c and c['conclusion']=='success' and len(c['head_sha'])==40 and c['id'].isdigit(), c" && grep -q "^- \[x\] \*\*RPT-01\*\*" .planning/REQUIREMENTS.md && grep -q "^- \[x\] \*\*RPT-03\*\*" .planning/REQUIREMENTS.md && grep -q "^| RPT-01 | Phase 28 | \*\*SATISFIED" .planning/REQUIREMENTS.md && grep -q "^| RPT-03 | Phase 28 | \*\*SATISFIED" .planning/REQUIREMENTS.md && grep -q "^- \[ \] \*\*Phase 28" .planning/ROADMAP.md && grep -q "| 28. Report, the Published Null, and Milestone Close | v4.0 | 7/7 |" .planning/ROADMAP.md && test "$(grep -c '^- \[x\] 28-0[1-7]-PLAN.md' .planning/ROADMAP.md)" = "7" && .venv/bin/python scripts/phase28_report.py check && .venv/bin/pytest tests/test_phase28_ledger.py tests/test_phase28_report.py -q -x && test -z "$(git status --short .planning/PROJECT.md .planning/MILESTONES.md)"</automated>
  </verify>
  <acceptance_criteria>
    - `results/phase28_ledger.json::close.ci_run` has `id` (digits), `url`, 40-hex `head_sha`, `conclusion == "success"`, `recorded`; `git diff <28-06 publishing commit> HEAD -- results/phase28_ledger.json` touches only the `close` block; `tests/test_phase28_report.py::test_report_block_is_byte_identical` passes
    - REQUIREMENTS.md: `- [x] **RPT-01**`, `- [x] **RPT-03**`; both traceability rows begin `**SATISFIED`; the RPT-01/RPT-03 requirement TEXT lines are byte-identical to the snapshot (quote `diff`)
    - ROADMAP.md: seven `- [x] 28-0N-PLAN.md` lines; progress row `7/7 | Plans complete — verification pending`; the `- [ ] **Phase 28` heading checkbox still unticked
    - STATE.md frontmatter parses (`head -25`), names the publishing commit and the run id; `PROJECT.md` and `MILESTONES.md` unchanged; no `v4.0` tag created (`git tag -l v4.0` empty)
    - Three `diff -u` summaries quoted; zero `gsd-sdk` mutation handlers named in the transcript
    - `make lint` clean; full-suite summary line from the background log quoted with `0 failed`; `scripts/phase28_report.py check` exit 0
  </acceptance_criteria>
  <done>Phase 28 is closed at its named boundary: report published, ledger complete with the green run, ticks by hand; ready for `/gsd:verify-work 28`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local main → origin/main | outward-facing push is a human action; CI is the first run of Phase 27 + 28 code off the publication host |
| ledger rows (frozen) → close block | only `close` changes after publish; the rows digest in the published block detects any row edit |
| planning ledgers ← hand edits | snapshot → edit → diff; zero gsd-sdk mutation handlers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-08 | Elevation | machine `git push` | mitigate | Task 1 is `checkpoint:human-action`; verify checks `origin/main` == `main` after the developer's push |
| T-28-02 | Repudiation | row edit after publish | mitigate | rows compared before/after the `close` write; byte-identity test re-run |
| T-28-07 | Tampering | gsd-sdk mutation handlers on STATE/ROADMAP/REQUIREMENTS | mitigate | none called; snapshot + `diff -u` quoted |
| T-28-18 | Repudiation | CI "green" on a head that lacks the report | mitigate | `merge-base --is-ancestor <publishing sha> <head_sha>` asserted; head_sha recorded |
| T-28-19 | Tampering | scope creep into `/gsd-complete-milestone`'s files | mitigate | `git status` on PROJECT.md / MILESTONES.md empty; no tag created |
| T-28-SC | Tampering | package installs | accept | none |
</threat_model>

<verification>
- CI green on origin/main with the report; run id in the ledger; RPT-01/RPT-03 ticked by hand; ROADMAP/STATE updated; full suite + lint green; `check` exit 0; PROJECT.md/MILESTONES.md/tag untouched
</verification>

<success_criteria>
- D-38's close precondition is measured and recorded; D-34's boundary is respected; the phase hands over to `/gsd:verify-work 28` and then `/gsd-complete-milestone`
</success_criteria>

<output>
Create `.planning/phases/28-report-the-published-null-and-milestone-close/28-07-SUMMARY.md` when done — include the run id/URL/head SHA, the three planning-file diff summaries, the final suite line, and the D-34 hand-over sentence.
</output>
