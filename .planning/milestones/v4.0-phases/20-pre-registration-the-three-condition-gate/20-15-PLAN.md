---
phase: 20-pre-registration-the-three-condition-gate
plan: 15
type: execute
wave: 14
depends_on: ["20-14"]
files_modified:
  - scripts/phase20_gate_coverage.py
  - tests/test_phase20_correction.py
autonomous: true
gap_closure: true
requirements: [GATE-02]
user_setup: []

must_haves:
  truths:
    - "`_prove_retention_floor(retention_noise_floor=0.06893*(1+2**-50), retention_floor_provenance={'regime':'adapter','seeds':(1337,2024)})` raises SystemExit — the one-ULP nudge that bought a bit-identical 4.029 no longer reaches a cap"
    - "`_prove_retention_floor(retention_noise_floor=5.0, retention_floor_provenance={'regime':'adapter','seeds':(1337,2024)})` raises SystemExit — a looser floor with no malformed input at all is refused"
    - "`_prove_retention_floor(retention_noise_floor=erasure_gate.V20_RETENTION_NOISE_FLOOR, ...)` still raises with a message carrying all three published numbers (ratio, borrowed cap, governing cap) — the `!=` named-value refusal fires FIRST and its existing assertion at tests/test_phase20_correction.py:810-816 is untouched"
    - "`_prove_retention_floor(retention_noise_floor=<results/phase20_retention_floor.json::retention_ppl_noise_floor>, ...)` returns None — the governing floor itself is admitted, so the bound is not vacuous"
    - "`test_mitigation_point_verdict_has_no_caller_outside_this_module` flags `from mitigation_gate import mitigation_point_verdict as mpv` in a synthetic AST and reports zero real bypassing imports"
    - "Both new guards were OBSERVED failing and the module restored so `shasum -a 256` matches the pre-break digest and `git diff --exit-code -- scripts/phase20_gate_coverage.py` returns 0"
    - "`git diff --exit-code -- scripts/mitigation_gate.py scripts/erasure_gate.py` returns 0; full suite at least 876 passed / 1 skipped with zero failures"
  artifacts:
    - path: "scripts/phase20_gate_coverage.py"
      provides: "the magnitude bound on the retention floor, alongside the named-value refusal"
      contains: "_MAX_ADMISSIBLE_RETENTION_FLOOR"
    - path: "tests/test_phase20_correction.py"
      provides: "the two D-38 tripwire cases, the governing-floor harness default (D-41), and the import census"
      contains: "DEFAULT_RETENTION_FLOOR"
  key_links:
    - from: "scripts/phase20_gate_coverage.py::_prove_retention_floor"
      to: "_ADAPTER_REGIME_RETENTION_FLOOR"
      via: "a magnitude bound computed from the module constant, never a retyped threshold"
      pattern: "_MAX_ADMISSIBLE_RETENTION_FLOOR"
    - from: "tests/test_phase20_correction.py::_corrected_call"
      to: "results/phase20_retention_floor.json::retention_ppl_noise_floor"
      via: "DEFAULT_RETENTION_FLOOR read from the committed artifact at module scope"
      pattern: "retention_ppl_noise_floor"
    - from: "tests/test_phase20_correction.py::test_mitigation_point_verdict_has_no_caller_outside_this_module"
      to: "ast.ImportFrom nodes naming mitigation_point_verdict"
      via: "the same walk, censusing the IMPORT as well as the call"
      pattern: "ast\\.ImportFrom"
---

<objective>
Close gap 2 (GATE-02's residual / T-20-19) and GC-06. Refuse the retention floor by MAGNITUDE as
well as by NAME (D-38), rewire the sanctioned route's test harness to supply the governing floor
(D-41), census the aliased IMPORT as well as the call, and watch every new guard fail before
trusting it.

Purpose: `_prove_retention_floor` (`:353-406`) refuses four things — missing provenance keys, the
wrong `regime` string, fewer than two distinct seeds, and one specific literal value by float `!=`.
It never constrains the floor's MAGNITUDE, and the harm T-20-19 names is "the looser cap a
borrowing buys". Measured against HEAD: `0.06893 * (1 + 2**-50)` passes the `!=` and
`retention_cap` returns a BIT-IDENTICAL `4.029`, the exact borrowed cap; and `5.0` with clean
`{"regime": "adapter", "seeds": (1337, 2024)}` provenance — no malformed input whatsoever — reaches
a `PASS` at cap `13.89114` against the governing `3.9085032379884783`. The control confirms the
unperturbed `0.06893` IS refused, so the guard exists and computes; its coverage is one bit wide.

Output: two derived module constants and one `_prove` in `scripts/phase20_gate_coverage.py`; a
governing-floor default plus four tripwire cases and an import census in
`tests/test_phase20_correction.py`; two watched-RED breaks with byte-identical restores.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/20-pre-registration-the-three-condition-gate/20-CONTEXT.md
@.planning/phases/20-pre-registration-the-three-condition-gate/20-VERIFICATION.md
@.planning/phases/20-pre-registration-the-three-condition-gate/20-REVIEW-GAP-CLOSURE.md
@.planning/phases/20-pre-registration-the-three-condition-gate/20-14-SUMMARY.md

<interfaces>
<!-- Resolved from the committed files. Quoted as they appear today. -->

`scripts/phase20_gate_coverage.py`:
  :100-104 `from erasure_gate import (MARGIN_K, V20_RETENTION_NOISE_FLOOR, wilson_upper_bound)`
  :105-108 `from mitigation_gate import (EXTRACTION_FLOOR_MIN_SEEDS, F_Y)`
  :332     `RETENTION_FLOOR_PROVENANCE_KEYS = ("regime", "seeds")`
  :333     `ADAPTER_REGIME = "adapter"`
  :339     `_ADAPTER_REGIME_RETENTION_FLOOR = 0.008681618994239138`
  :346     `_BORROWED_CAP = mitigation_gate.retention_cap(retention_noise_floor=V20_RETENTION_NOISE_FLOOR)`
  :347-349 `_GOVERNING_CAP = mitigation_gate.retention_cap(retention_noise_floor=_ADAPTER_REGIME_RETENTION_FLOOR)`
  :350     `_BORROWED_FLOOR_RATIO = V20_RETENTION_NOISE_FLOOR / _ADAPTER_REGIME_RETENTION_FLOOR`
  :353     `def _prove_retention_floor(*, retention_noise_floor, retention_floor_provenance):`
  :366-373 `_prove` — provenance mapping carries every key
  :374-385 `_prove` — `retention_floor_provenance["regime"] == ADAPTER_REGIME`
  :388-395 `_prove` — `distinct >= EXTRACTION_FLOOR_MIN_SEEDS`
  :396-406 `_prove(retention_noise_floor != V20_RETENTION_NOISE_FLOOR, ...)`  <-- the NAME refusal
  :508-511 `_prove_retention_floor(...)` called FIRST in `corrected_point_verdict`, before any compute

`tests/test_phase20_correction.py`:
  :69-74   `CORRECTION_REL`, `PAYLOAD_REL`, `RETENTION_FLOOR_REL = "results/phase20_retention_floor.json"`,
           `CORRECTION_PATH`, `PAYLOAD_PATH`, `RETENTION_FLOOR_PATH`
  :106-107 `DEFAULT_HELDOUT_SWEEP = (0.30, 0.20)`, `DEFAULT_RETENTION_PROVENANCE = {"regime": "adapter",
           "seeds": (1337, 2024)}`
  :119-141 `def _corrected_call(fixture_name, **overrides):` — its `base` dict at :136-139 already
           supplies "the two arguments the frozen fixtures cannot"; `base.update(overrides)` at :140
           then `dict(kwargs, **base)` at :141. THE MERGE FORM IS LOAD-BEARING.
  :759     `def test_the_retention_floor_tripwire_is_the_only_route_to_a_verdict():`
  :776-786 its `def refused(**overrides):` helper — drives everything THROUGH `corrected_point_verdict`
  :804-816 the borrowed-value case and the `repr(number) in borrowed` assertion over three published
           numbers. THIS ASSERTION MUST STAY GREEN — it is why the `!=` fires FIRST.
  :824-835 the WR-08 sign-check cases at both magnitudes
  :840-847 the positive control, asserting `verdict == "PASS"`
  :901     `def test_mitigation_point_verdict_has_no_caller_outside_this_module():`
  :920-921 `definition = _SCRIPTS / "mitigation_gate.py"`, `sanctioned = _SCRIPTS / "phase20_gate_coverage.py"`
  :925-936 the walk: `sorted(_SCRIPTS.rglob("*.py")) + sorted((_ROOT / "src").rglob("*.py"))`,
           matching `getattr(node.func, "id", None) or getattr(node.func, "attr", None)`
  :952-957 the non-vacuity assertion `inside_sanctioned >= 1`

MEASURED against HEAD — re-derive, do not transcribe:
  `mitigation_gate.FIXTURE_CLEARING_POINT/_DESTROYED_MODEL/_TRUNCATED_SWEEP` all carry
  `retention_noise_floor = 0.009`, and `0.009 > 0.008681618994239138 * (1.0 + 1e-9)` — D-41.
  `retention_cap(0.009) = 3.90914` vs governing `3.9085032379884783`; ratio `1.0366729991228745`.
  Every published verdict is BIT-UNCHANGED under the substitution: `direction_i` PASS,
  `direction_ii` INCONCLUSIVE, `direction_ii_on_clearing_fixture` INCONCLUSIVE,
  `heldout_coverage` INCONCLUSIVE.
  `ast.ImportFrom` census on `scripts/phase20_gate_coverage.py` and `scripts/mitigation_gate.py`
  returns ZERO hits today, so the import census needs its own non-vacuity control.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bound the retention floor by magnitude, and hand the sanctioned route's harness the governing floor</name>
  <files>scripts/phase20_gate_coverage.py, tests/test_phase20_correction.py</files>

  <read_first>
    - `scripts/phase20_gate_coverage.py:317-406` — the whole T-20-19 comment block, the three derived
      constants at :346-350 with their "DERIVED from the two floors, never retyped" justification at
      :341-345, and all four existing `_prove` calls with their messages.
    - `tests/test_phase20_correction.py:96-141` — the module-scope defaults and `_corrected_call`.
    - `.planning/phases/20-pre-registration-the-three-condition-gate/20-CONTEXT.md` D-38 and D-41
      (written at plan `20-13`). D-41 is the reason this task edits the test file too: without it the
      bound refuses the repo's own fixtures and five committed tests go red.
    - `.planning/phases/20-pre-registration-the-three-condition-gate/20-REVIEW-GAP-CLOSURE.md`
      GC-01 (:54-90) and GC-02 (:94-118).
  </read_first>

  <action>
Both files change in ONE task because they are one change: the bound and the harness default cannot
be split across commits without leaving the suite red in between.

**(a) `scripts/phase20_gate_coverage.py` — the magnitude bound.**

Beside the three derived constants at `:346-350`, add two module constants with these exact names:
`_RETENTION_FLOOR_RELATIVE_TOLERANCE = 1e-9` and
`_MAX_ADMISSIBLE_RETENTION_FLOOR = _ADAPTER_REGIME_RETENTION_FLOOR * (1.0 + _RETENTION_FLOOR_RELATIVE_TOLERANCE)`.
The threshold is a DERIVED value, never a typed one, in the same register as `_GOVERNING_CAP`; and
naming the tolerance separately makes it auditable rather than buried in an expression. Add a short
comment stating what the tolerance is FOR — absorbing float round-trip on a floor that equals the
committed artifact's value — and what it is NOT for: it is never widened to admit a floor a caller
happens to hold (D-41).

Add a fifth `_prove` to `_prove_retention_floor`, placed AFTER the existing `!=` `_prove` that ends
at `:406`. The ordering is load-bearing and must be stated in a comment: the `!=` names ONE value
and its message publishes the ratio, the borrowed cap and the governing cap — three numbers
`tests/test_phase20_correction.py:810-816` asserts by `repr` — so the borrowed floor must keep
reaching that message. The magnitude bound catches everything the name cannot: the one-ULP
neighbour, any looser value, and (D-41) this repository's own fixture floor.

Predicate: `retention_noise_floor <= _MAX_ADMISSIBLE_RETENTION_FLOOR`.

The message is an argument in this module's register and must state five things: (1) the offending
floor with `!r` and the admissible ceiling with `!r`; (2) that a LOOSER floor buys a LOOSER cap and
is therefore the substitution that buys an easier pass — the same sentence `:401-405` already makes
about the borrowed value, now made about the property rather than the name; (3) the two
reproductions, computed here rather than typed: `V20_RETENTION_NOISE_FLOOR * (1 + 2**-50)` passes
the `!=` above and yields a cap bit-identical to `_BORROWED_CAP`, and any clean-provenance value
such as `5.0` reaches a cap far above `_GOVERNING_CAP`; (4) that the bound admits any TIGHTER floor
a later phase measures — strictly conservative, so a real re-measurement is never blocked — and
refuses the whole looser class, of which `V20_RETENTION_NOISE_FLOOR` is one member rather than the
definition; (5) D-41's finding: `mitigation_gate`'s three committed fixtures carry `0.009`, which
this bound REFUSES at `1.0366729991228745x` the governing floor, so the bound's first catch is
inside this repository and the fixture floor is not a counter-example to the rule but the first
instance of it. Compute every number by calling the frozen `retention_cap` and the module
constants; type none of them.

Also correct `_prove_retention_floor`'s own docstring at `:353-364`, which currently claims the
fourth refusal means "a caller that lies about `regime` is still caught by the number itself" —
measured false at one ULP. Rewrite that sentence to say what the pair now proves: the named value
by identity, and the entire looser class by magnitude. Say `FOUR refusals` becomes `FIVE`.

**(b) `tests/test_phase20_correction.py` — the governing-floor default (D-41).**

At module scope, beside `DEFAULT_HELDOUT_SWEEP` and `DEFAULT_RETENTION_PROVENANCE` at `:106-107`,
add `DEFAULT_RETENTION_FLOOR` READ from `RETENTION_FLOOR_PATH`'s `retention_ppl_noise_floor` key —
the same read `:772-774` and `:649-651` already perform inside test bodies. Never retype the float.

Add `"retention_noise_floor": DEFAULT_RETENTION_FLOOR` to `_corrected_call`'s `base` dict at
`:136-139`, and extend that function's docstring: the frozen fixtures carry a `retention_noise_floor`
the sanctioned route now refuses, so this is a THIRD argument the fixtures cannot supply, joining
`sweep_heldout_recalls` and `retention_floor_provenance` for the same structural reason —
`scripts/mitigation_gate.py` is frozen and its fixtures cannot be edited. State the measurement that
makes the substitution safe: every published verdict is bit-unchanged under it. State the direction
of the drift catch honestly: the module's `_ADAPTER_REGIME_RETENTION_FLOOR` is retyped from this
same artifact (GC-07, out of scope here), so a drift that made the module constant TIGHTER than the
artifact would redden these tests, while a drift that made it LOOSER would not — a one-directional
strengthening, named as partial rather than claimed as GC-07's closure.

Because `base.update(overrides)` runs at `:140`, every existing `refused(retention_noise_floor=...)`
override still wins. Do not restructure `_corrected_call`.
  </action>

  <verify>
    <automated>.venv/bin/python -c "
import sys, json, pathlib; sys.path.insert(0,'scripts')
import phase20_gate_coverage as cov, erasure_gate as eg, mitigation_gate as mg
P={'regime':'adapter','seeds':(1337,2024)}
gov=json.loads(pathlib.Path('results/phase20_retention_floor.json').read_text())['retention_ppl_noise_floor']
assert cov._prove_retention_floor(retention_noise_floor=gov, retention_floor_provenance=dict(P)) is None
for bad in (eg.V20_RETENTION_NOISE_FLOOR*(1+2**-50), 5.0, 0.009, 0.06893):
    try: cov._prove_retention_floor(retention_noise_floor=bad, retention_floor_provenance=dict(P)); raise AssertionError(f'accepted {bad!r}')
    except SystemExit: pass
try: cov._prove_retention_floor(retention_noise_floor=eg.V20_RETENTION_NOISE_FLOOR, retention_floor_provenance=dict(P))
except SystemExit as e:
    m=str(e)
    for n in (eg.V20_RETENTION_NOISE_FLOOR/gov, mg.retention_cap(retention_noise_floor=eg.V20_RETENTION_NOISE_FLOOR), mg.retention_cap(retention_noise_floor=gov)):
        assert repr(n) in m, n
else: raise AssertionError('the named-value refusal did not fire, so the three repr assertions above never ran')
assert cov._MAX_ADMISSIBLE_RETENTION_FLOOR == cov._ADAPTER_REGIME_RETENTION_FLOOR*(1.0+cov._RETENTION_FLOOR_RELATIVE_TOLERANCE)
print('ok')" && .venv/bin/python -m pytest tests/test_phase20_correction.py tests/test_phase20_prereg.py -q 2>&1 | tail -2</automated>
  </verify>

  <acceptance_criteria>
    - The verify command above exits 0 and pytest reports `31 passed` with no `skipped` and no `xfail`.
    - `_prove_retention_floor(retention_noise_floor=0.06893*(1+2**-50), retention_floor_provenance={'regime':'adapter','seeds':(1337,2024)})` raises `SystemExit`.
    - `_prove_retention_floor(retention_noise_floor=5.0, retention_floor_provenance={'regime':'adapter','seeds':(1337,2024)})` raises `SystemExit`.
    - `_prove_retention_floor(retention_noise_floor=0.009, ...)` raises `SystemExit` — the committed fixture floor is refused, per D-41.
    - `_prove_retention_floor(retention_noise_floor=<artifact floor>, ...)` returns `None` — the bound is not vacuous.
    - The `V20_RETENTION_NOISE_FLOOR` case still raises via the `!=` and its message carries `repr()` of all three published numbers, so `tests/test_phase20_correction.py:810-816` is untouched and green.
    - `grep -n '0\.008681619002920757\|13\.89114\|3\.90914' scripts/phase20_gate_coverage.py` returns nothing — every published number is derived, not typed.
    - `git diff --exit-code -- scripts/mitigation_gate.py scripts/erasure_gate.py` returns 0.
    - `ruff check . && ruff format --check .` is clean.
  </acceptance_criteria>

  <done>The magnitude bound refuses the one-ULP neighbour, `5.0` and the fixture floor while admitting the governing floor; the harness supplies the governing floor from the committed artifact; all 31 phase-20 tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Arm the two D-38 tripwire cases and census the aliased import</name>
  <files>tests/test_phase20_correction.py</files>

  <read_first>
    - `tests/test_phase20_correction.py:759-847` — the whole retention tripwire body: the `refused`
      helper, the four provenance refusals, the seed cases asserting `"1 distinct"`, the borrowed
      case and its three-number `repr` assertion, WR-08 at both magnitudes, and the positive control.
      New cases go INSIDE this function, in its established shape. NOTE, measured: the phrases
      "The four provenance refusals" and "differing from the eight above" are INLINE COMMENTS at
      `:787-788` and `:841`, not docstring text — the docstring is `:760-772`. Edit the comments.
    - `tests/test_phase20_correction.py:901-957` — the census, its docstring's account of WR-07's
      `.id`-only hole, and the non-vacuity assertion.
    - `.planning/phases/20-pre-registration-the-three-condition-gate/20-REVIEW-GAP-CLOSURE.md`
      GC-06 (:244-277) — the review wrote the ImportFrom walk.
  </read_first>

  <action>
**(a) Two cases inside `test_the_retention_floor_tripwire_is_the_only_route_to_a_verdict` (`:759`).**

Add them AFTER the borrowed-value block that ends at `:816` and BEFORE the WR-08 block at `:818`,
so the provenance/value cases stay together. Both go through the existing `refused(...)` helper, so
both are driven through `corrected_point_verdict` — the reachability claim, not the
does-the-helper-exist claim.

  CASE 1 — the one-ULP nudge. `refused(retention_noise_floor=erasure_gate.V20_RETENTION_NOISE_FLOOR
  * (1 + 2**-50))`. Assert the message names the magnitude bound, and assert the CLAIM the case
  exists for: `mitigation_gate.retention_cap` on the nudged floor is `==` to `retention_cap` on the
  unperturbed `V20_RETENTION_NOISE_FLOOR` — bit-identical, both `4.029` — computed by calling the
  frozen function twice, never typed. That equality is what makes the one-ULP evasion a real
  evasion rather than a curiosity, and asserting it keeps the argument readable after the guard
  lands. Assert too that the nudged value passes the `!=` it defeats:
  `nudged != erasure_gate.V20_RETENTION_NOISE_FLOOR` is `True`.

  CASE 2 — the looser floor with no malformed input. `refused(retention_noise_floor=5.0)` with the
  clean `DEFAULT_RETENTION_PROVENANCE` untouched. Assert the message names the bound, and assert
  `mitigation_gate.retention_cap(retention_noise_floor=5.0) > mitigation_gate.retention_cap(
  retention_noise_floor=DEFAULT_RETENTION_FLOOR)` — the harm, stated as the inequality it is.

  Update the two INLINE COMMENTS that carry stale counts — `:787-788` ("The four provenance
  refusals … except the fourth, which has no counterpart on the extraction leg") and `:841`
  ("differing from the eight above") — to what the body now drives (ten cases), and add one sentence
  to the function's DOCSTRING (`:760-772`) on the name-versus-property pair: the `!=` refuses the
  named value, the bound refuses the class, and the two are asserted separately because either
  alone was measured insufficient.

**(b) The import census (GC-06), inside `test_mitigation_point_verdict_has_no_caller_outside_this_module`.**

Do NOT create a new test function — this name is cited verbatim in two shipped docstrings
(`scripts/phase20_gate_coverage.py:79` and `:500-502`) and in `20-SECURITY.md`'s T-20-48 row.
Extend the existing walk at `:925-936`: in the same `ast.walk` loop, also match
`isinstance(node, ast.ImportFrom) and node.module == "mitigation_gate"` and, for each `alias` in
`node.names` with `alias.name == "mitigation_point_verdict"`, append
`f"{path.relative_to(_ROOT)}:{node.lineno} (imported as {alias.asname or alias.name})"` to
`bypassing` when `path` is neither `sanctioned` nor `definition`. An alias can hide the CALL; it
cannot hide the IMPORT.

Add the non-vacuity control this half needs, in the recorded-state register the existing
`inside_sanctioned >= 1` assertion uses (`tests/test_phase16_prereg.py`'s `bool(checked) ==
bool(tracked)` discipline): the real tree yields ZERO import hits today, so an empty result proves
nothing about the matcher. Parse a SYNTHETIC source string with `ast.parse` containing
`from mitigation_gate import mitigation_point_verdict as mpv` and assert the same predicate flags
it and reports the alias `mpv`. Measured: it does, at lineno 1.

Extend the docstring to record what the census now covers and — honestly — what it still does not:
`getattr(mitigation_gate, "mitigation_point_verdict")(...)` remains invisible to the call matcher,
and the walk is scoped to `scripts/` and `src/` only, so a driver at the repo root or in `tools/`
is unpoliced. Both were named by GC-06 and neither is closed here; recording a state rather than
implying completeness is this file's register.
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase20_correction.py::test_the_retention_floor_tripwire_is_the_only_route_to_a_verdict tests/test_phase20_correction.py::test_mitigation_point_verdict_has_no_caller_outside_this_module -q 2>&1 | tail -3</automated>
  </verify>

  <acceptance_criteria>
    - The verify command reports `2 passed`.
    - `.venv/bin/python -m pytest tests/test_phase20_correction.py tests/test_phase20_prereg.py -q` reports `31 passed`, output containing neither `skipped` nor `xfail`.
    - `.venv/bin/python -c "import ast,pathlib; t=ast.parse(pathlib.Path('tests/test_phase20_correction.py').read_text()); assert any(isinstance(n,ast.Attribute) and n.attr=='ImportFrom' for n in ast.walk(t)), 'the import census is absent'"` exits 0.
    - `grep -c '2\*\*-50' tests/test_phase20_correction.py` reports at least 1.
    - The test function count is still 13 — the two retention cases went INSIDE the existing function and the census was EXTENDED, not duplicated: `.venv/bin/python -c "import ast,pathlib; t=ast.parse(pathlib.Path('tests/test_phase20_correction.py').read_text()); print(len([f for f in t.body if isinstance(f,ast.FunctionDef) and f.name.startswith('test_')]))"` prints `13`.
    - `git diff --exit-code -- scripts/mitigation_gate.py scripts/erasure_gate.py` returns 0.
  </acceptance_criteria>

  <done>Both D-38 cases are asserted through the route with their harms stated as inequalities, and the census flags an aliased import with a synthetic non-vacuity control.</done>
</task>

<task type="auto">
  <name>Task 3: Watch the magnitude bound and the import census RED, then restore byte-identically</name>
  <files>scripts/phase20_gate_coverage.py, tests/test_phase20_correction.py</files>

  <read_first>
    - `.planning/phases/20-pre-registration-the-three-condition-gate/20-SECURITY.md` Watched-RED
      evidence table — the shape each row records.
    - `.planning/phases/20-pre-registration-the-three-condition-gate/20-14-SUMMARY.md` — the two
      breaks recorded there, including any divergence from `20-14-PLAN.md`'s prediction.
    - `scripts/phase20_gate_coverage.py` and `tests/test_phase20_correction.py` as they stand after
      Tasks 1 and 2.
  </read_first>

  <action>
Record `shasum -a 256` for BOTH files first. Then three breaks in sequence, each observed and
restored before the next.

BREAK 1 — the magnitude bound. Delete the fifth `_prove` from `_prove_retention_floor`. Run
`.venv/bin/python -m pytest tests/test_phase20_correction.py -q` and record the exact output.
Expect `test_the_retention_floor_tripwire_is_the_only_route_to_a_verdict` to fail with
`DID NOT RAISE <class 'SystemExit'>`. Record the ACTUAL failing count and names; publish any
divergence from this prediction rather than smoothing it. Restore; confirm the digest and
`git diff --exit-code -- scripts/phase20_gate_coverage.py`.

BREAK 2 — the bound's tolerance, which is the failure mode D-41 forbids. Do NOT delete the `_prove`
this time; widen `_RETENTION_FLOOR_RELATIVE_TOLERANCE` from `1e-9` to `0.05`. MEASURED BEFORE WRITING, so treat
this as the expected path rather than a contingency: at `0.05` the ceiling becomes
`0.009115699943951094`, which ADMITS the `0.009` fixture floor while still refusing both tripwire
cases (`0.06893 * (1 + 2**-50)` and `5.0`) — so NOTHING reddens, and no committed test currently
distinguishes the tolerance from a rubber stamp. Run the command, record the green result as the
finding it is, then close it: add one assertion to
`test_the_retention_floor_tripwire_is_the_only_route_to_a_verdict` pinning
`coverage._MAX_ADMISSIBLE_RETENTION_FLOOR < mitigation_gate.FIXTURE_CLEARING_POINT["retention_noise_floor"]`
with a message naming D-41. READ the fixture floor, never retype `0.009` — this phase does not
retype committed numbers, and that value is annotated `# fabricated` in the frozen pin, which is
exactly why the harness may not inherit it. Re-run the break, confirm it now reddens, and record
BOTH states. Restore; confirm digest and `git diff --exit-code`.

BREAK 3 — the import census. In a scratch file `scripts/_wr07_probe.py` (created for the break,
DELETED before commit), write `from mitigation_gate import mitigation_point_verdict as mpv` and a
call `mpv(arm="dp")`. Run
`.venv/bin/python -m pytest tests/test_phase20_correction.py::test_mitigation_point_verdict_has_no_caller_outside_this_module -q`
and record the failure — it must name `scripts/_wr07_probe.py` and the alias `mpv`. Confirm this
break would NOT have reddened the census at HEAD by also recording that the call matcher alone sees
`ast.Name(id='mpv')` and not the literal name. Delete the probe; confirm `git status --porcelain
scripts/` is empty and `git diff --exit-code` returns 0 on both files.

Then run the full suite. Commit only after all three restores are verified.

Record all three in the SUMMARY in `20-SECURITY.md`'s Watched-RED table shape: what was broken, the
exact command, the exact observed output, and the restore proof.
  </action>

  <verify>
    <automated>shasum -a 256 scripts/phase20_gate_coverage.py tests/test_phase20_correction.py && git status --porcelain scripts/ tests/ && git diff --exit-code -- scripts/ tests/ && .venv/bin/python -m pytest tests/test_phase20_correction.py tests/test_phase20_prereg.py -q 2>&1 | tail -2</automated>
  </verify>

  <acceptance_criteria>
    - All three breaks were APPLIED and their pytest output OBSERVED; the SUMMARY quotes the actual failure lines.
    - After each restore, `shasum -a 256` equals the pre-break digest and `git diff --exit-code -- scripts/phase20_gate_coverage.py tests/test_phase20_correction.py` returns 0.
    - `scripts/_wr07_probe.py` does not exist at commit time: `test ! -e scripts/_wr07_probe.py` succeeds and `git status --porcelain scripts/` is empty.
    - `.venv/bin/python -m pytest tests/test_phase20_correction.py tests/test_phase20_prereg.py -q` reports `31 passed` in ~1s with no `skipped` and no `xfail` — the task-level gate. The full suite (~338s) runs once as the plan-level gate in `<verification>`, before commit.
    - `git diff --exit-code -- scripts/mitigation_gate.py scripts/erasure_gate.py` returns 0.
    - `ruff check . && ruff format --check .` is clean.
  </acceptance_criteria>

  <done>The magnitude bound, its tolerance and the import census were each observed failing and restored byte-identically; the scratch probe is gone; the suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a plan that says a thing will be done ↔ a guard that proves it was | `20-SECURITY.md:39`. T-20-19's closed row, GATE-02's residual discharge and `threats_open: 0` all rest on `_prove_retention_floor`. |
| measured floor ↔ borrowed floor | `20-SECURITY.md:33`. An already-published number from a different regime is the cheapest way to make a criterion look rigorous while being wrong. |
| a superseding route ↔ a caller that declines to invoke it | The census is the SOLE enforcement of the choke point per both modules' docstrings. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-75 | Spoofing | a looser retention floor reaching a v4.0 cap under clean provenance | mitigate | `_prove(retention_noise_floor <= _MAX_ADMISSIBLE_RETENTION_FLOOR, ...)` in `_prove_retention_floor`, which `corrected_point_verdict` calls FIRST at `:508` before any compute. Asserted through the route, not against the helper, so the claim proved is reachability |
| T-20-76 | Tampering | the bound's tolerance widened until a value already in hand passes | mitigate | D-41 records the rejection in writing; `_RETENTION_FLOOR_RELATIVE_TOLERANCE` is a named constant so a widening is visible in a diff; Break 2 widens it deliberately and requires a committed test to redden, adding one if none does |
| T-20-77 | Elevation of privilege | an aliased import bypassing the choke point invisibly | mitigate | The census matches `ast.ImportFrom` on `mitigation_gate` for the name `mitigation_point_verdict`, which no alias can hide, with a synthetic non-vacuity control because the real tree yields zero hits. The residual — getattr-dispatch and the `scripts/`+`src/` scope — is RECORDED in the docstring, not implied closed |
| T-20-78 | Tampering | the harness's substituted floor drifting from the committed artifact | mitigate | `DEFAULT_RETENTION_FLOOR` is READ from `results/phase20_retention_floor.json`, never retyped; `test_v4_retention_cap_reads_the_measured_adapter_regime_floor` (`:850`) re-derives that artifact's own arithmetic on both sides. The one-directional limit (a LOOSER module-constant drift is not caught) is named rather than claimed as GC-07's closure |
| T-20-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install in this plan. Both files are stdlib plus sibling scripts; `pyproject.toml` is sha256-pinned by `tests/test_package.py` (STAT-04) and untouched |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_phase20_correction.py tests/test_phase20_prereg.py -q` →
  `31 passed`, no `skipped`, no `xfail`.
- `.venv/bin/python -m pytest -q` → at least `876 passed, 1 skipped`, zero failures.
- `ruff check . && ruff format --check .` → clean.
- `git diff --exit-code -- scripts/mitigation_gate.py scripts/erasure_gate.py` → 0.
- `test ! -e scripts/_wr07_probe.py` → succeeds; `git status --porcelain scripts/ tests/` → empty.
- `grep -n '0\.008681619002920757' scripts/phase20_gate_coverage.py tests/test_phase20_correction.py`
  → nothing: the threshold is derived in both files, never typed.
</verification>

<success_criteria>
- The one-ULP neighbour, `5.0` and the fixture's `0.009` are all refused; the governing floor is
  admitted.
- The named-value refusal still fires first and still publishes its three derived numbers.
- The census flags an aliased import and proves its matcher non-vacuous on a synthetic tree.
- Three watched-RED breaks observed and restored byte-identically, with any divergence published.
- Both frozen files return 0 from `git diff --exit-code`.
</success_criteria>

<output>
Create `.planning/phases/20-pre-registration-the-three-condition-gate/20-15-SUMMARY.md` when done.
</output>
