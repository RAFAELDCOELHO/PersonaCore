---
phase: 27-relearning-attack
plan: 05
type: execute
wave: 4
depends_on: ["27-04"]
files_modified:
  - results/phase27_admission.json
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
requirements: [RELRN-01, RELRN-02, RELRN-03, RELRN-04, RELRN-05]
must_haves:
  truths:
    - "relearning_is_worth_attempting was invoked ONCE on the measured frontier through `python scripts/phase27_relearn.py admit`, wrote results/phase27_admission.json reading MOOT with 0 of 44 PASS, tallies {PASS 0, FAIL 32, INCONCLUSIVE 6, REFUSED 6}, cleared (a) 30 / (b) 4 / (c) 1, three zero overlaps and seven live digests — the milestone ships that finding (SC1, RELRN-01, D-04, D-07)"
    - "Each attack sub-mode — calibrate, curve, gate, structural-proof — was invoked once against the REAL record before AND after its commit and exited non-zero naming MOOT; the refusal stderr is quoted in the SUMMARY; no leg ran on MPS and nothing under data/phase27_*, data/phase25_phase27_* (the Phase-25-named draw caches) or data/persona_relearn_attacker_* (the attacker bins, no phase prefix) exists (D-09, D-37)"
    - "The operator committed results/phase27_admission.json BY HAND at a human-action checkpoint; the driver and Claude never ran git add / git commit on it; the ancestry guard now checks len(prereg_commits) × 1 and every both-state test runs its PRESENT branch green (D-15, D-04, D-06, D-35, D-36)"
    - "REQUIREMENTS.md: RELRN-01 ticked with the record's fields and guard tests; RELRN-02..05 left UNTICKED with the traceability row 'not admitted by the gate; apparatus built and guarded, never exercised on a mitigated arm' naming the guarding tests; ROADMAP Phase 27 plans ticked; STATE updated — all by hand, snapshotted before and diffed after, zero gsd-sdk mutation handlers; requirement text unchanged (D-05, D-38)"
    - "No LaunchAgent plist was created; pyproject.toml, scripts/teach_persona.py, results/phase25_frontier.json and scripts/phase27_prereg.py are byte-identical to their state at plan 27-01's commit; make test and make lint are green with the collected count equal to 2802 + the sum of the four plans' measured deltas (D-14, D-39)"
  artifacts:
    - path: "results/phase27_admission.json"
      provides: "The phase's only artifact: the MOOT verdict with generated reasons, 44 rows, tallies, cleared counts, X, thresholds, baselines, corpus pins, budget, apparatus (not exercised), disjointness, provenance — operator-committed"
      contains: "\"verdict\": \"MOOT\""
    - path: ".planning/REQUIREMENTS.md"
      provides: "RELRN-01 ticked; RELRN-02..05 named-limitation rows"
      contains: "never exercised on a mitigated arm"
  key_links:
    - from: "results/phase27_admission.json"
      to: "results/phase25_frontier.json"
      via: "frontier_sha256 == sha256(bytes); frontier at one commit — tests/test_phase27_prereg.py::test_the_record_is_pinned_to_the_frontier_both_ways in its PRESENT state"
      pattern: "frontier_sha256"
    - from: "tests/test_phase27_prereg.py::test_phase27_prereg_is_frozen_before_every_phase27_result"
      to: "git history"
      via: "the record's first-add strictly descends from every scripts/phase27_prereg.py commit; checked == len(prereg_commits) × 1"
      pattern: "merge-base"
    - from: ".planning/REQUIREMENTS.md RELRN-02..05 rows"
      to: "results/phase27_admission.json::apparatus"
      via: "the limitation sentence quotes apparatus.status / apparatus.reason and names the refusal + e2e node ids"
      pattern: "not admitted by the gate"
---

<objective>
Close the phase the way its gate reads: run `admit` ONCE on the real frontier (expected MOOT),
watch every attack sub-mode refuse on the real record (D-37), have the operator commit the record
by hand (D-15), flip the both-state guards to their present state, make the D-38 ledger edits by
hand, and pass the full suite and lint with the expected collected-count delta.

Decisions implemented: D-04, D-05, D-06, D-07, D-09, D-14, D-15, D-37, D-38, D-39.

## Decision resolutions

- No open question touches this plan. The record's `apparatus.train_path` (OQ1-B) and
  `fresh_curve_disclosure` (M9) are generated by plan 27-03's `build_record`; this plan quotes them,
  never edits them. If the operator overrides OQ1-B at execution, the override lands in
  `scripts/phase27_relearn.py` BEFORE this plan's `admit` (the record pins that module's digest).
- D-37 says "before the operator commits"; the tracked conjunct of `_require_admitted` fires only
  after the verdict conjunct, so the refusal on the UNTRACKED MOOT record names MOOT (Task 1) and the
  refusal on the COMMITTED MOOT record names MOOT again (Task 3). Both are quoted; neither trains.

Purpose: SC1 — "if no point cleared the frontier, relearning is MOOT, not a pass, and the milestone
ships that finding"; D-05 — RELRN-02..05 stay unticked with a named limitation Phase 28 quotes.
Output: `results/phase27_admission.json` (operator-committed), the three ledgers, the SUMMARY with
the quoted admit line, the eight refusal transcripts and the suite counts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-relearning-attack/27-CONTEXT.md
@.planning/phases/27-relearning-attack/27-01-SUMMARY.md
@.planning/phases/27-relearning-attack/27-02-SUMMARY.md
@.planning/phases/27-relearning-attack/27-03-SUMMARY.md
@.planning/phases/27-relearning-attack/27-04-SUMMARY.md
@.planning/phases/26-empirical-privacy-audit-canary/26-05-SUMMARY.md

<interfaces>
From scripts/phase27_relearn.py: RECORD = results/phase27_admission.json ; main(["admit"]) ; main([mode, "--leg", leg, ...]) ; APPARATUS_LEGS ; TRAIN_PATH ; PINNED_MODULES
From scripts/phase27_prereg.py: COMMITTED ; RECORDS_AT_COMMIT == 0 ; ARTIFACT_GLOB = "results/phase27_*" ; VERDICTS
Both-state tests that flip to PRESENT on commit:
  tests/test_phase27_prereg.py::test_phase27_prereg_is_frozen_before_every_phase27_result (checked 0 → len(prereg_commits) × 1)
  tests/test_phase27_prereg.py::test_the_record_is_pinned_to_the_frontier_both_ways
  tests/test_phase27_prereg.py::test_cleared_abc_re_derive_on_every_row (rows compared to the record)
  tests/test_phase27_relearn.py::test_admit_refuses_to_overwrite ; ::test_every_apparatus_node_id_exists ; ::test_provenance_digests_match_live_bytes ; ::test_the_record_re_derives_from_build_record
Clean-tree probes that go RED while the record is `??` (26-05-SUMMARY "The test counts"): tests/test_phase23_resume.py::test_production_resume_epsilon_bit_identical, tests/test_phase25_frontier.py::test_a_perturbed_per_point_count_breaks_the_aggregate — run the FULL suite only after the operator's commit
Ledger precedent: 26-05-SUMMARY §"Planning-file edits (hand, diffed against a snapshot)"; REQUIREMENTS.md traceability rows 574-578 (RELRN-01..05, currently empty); ROADMAP.md "### Phase 27" plan list
Baseline counts: 2802 collected at ff38d9a; expected phase delta = sum of the measured deltas in 27-01..27-04 SUMMARYs (about +30 / +5 / +24 / +9)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preconditions quoted, admit ONCE on the real frontier, every sub-mode watched to refuse on the real record, artifact left untracked</name>
  <files>results/phase27_admission.json</files>
  <read_first>
    - scripts/phase27_relearn.py (`admit` order; `_require_admitted`'s three refusals; `build_parser`)
    - .planning/phases/27-relearning-attack/27-04-SUMMARY.md (the measured collected count after 27-04)
    - .planning/phases/26-empirical-privacy-audit-canary/26-05-PLAN.md Task 1 (the precondition-read + one-emit shape) and 26-05-SUMMARY.md §"Issues Encountered" (the `??`-artifact residue)
    - .planning/phases/27-relearning-attack/27-CONTEXT.md D-37, D-15, D-09
  </read_first>
  <action>
ORDER MATTERS for the ancestry guard: `scripts/phase27_prereg.py` must not be touched in this plan; this plan's record will be the FIRST tracked `results/phase27_*` file and must land strictly after every prereg commit.

1. Preconditions, each command run and its output quoted verbatim into the SUMMARY: `git log --format='%h %ad %s' --date=short -- scripts/phase27_prereg.py` (expect exactly one commit); `git ls-files 'results/phase27_*'` (expect empty); `find data -maxdepth 1 \( -name 'phase27_*' -o -name 'phase25_phase27_*' -o -name 'persona_relearn_attacker_*' \)` (expect nothing — no leg has ever run); `git status --short -- scripts src results` (expect empty; `admit` refuses otherwise — do NOT stub `refuse_if_dirty` here, this is the live guard); `ls artifacts/*.plist | grep -c phase27` (expect 0 — D-14); `.venv/bin/pytest -q tests/test_phase27_prereg.py tests/test_phase27_relearn.py tests/test_phase27_on_draw.py -x` (green in the ABSENT state; quote the passed count).
2. The ONE gate call: `.venv/bin/python scripts/phase27_relearn.py admit` — exactly once, no `--force`. Quote its single stdout line (expected: `admitted? MOOT: 0 of 44 points PASS; ...` with `cleared (a) 30 / (b) 4 / (c) 1`). Then read the file back with `python -c` and quote: `verdict.verdict`, `verdict.reasons` (all lines), `tallies`, `cleared_counts`, `x.value`, `x.tolerance_sentence`, `disjointness`, `apparatus.status`, `apparatus.reason`, `apparatus.train_path`, `apparatus.fresh_curve_disclosure`, `apparatus.device_policy`, `apparatus.draw_cache` (the Phase-25 cache naming `data/phase25_phase27_*_draws.json` — disclosed so a reader knows which glob the "nothing under data/" checks cover), `provenance.git_sha`, `provenance.head_at_write`, `provenance.torch_version`, `len(rows)` and the 6 refused keys, `frontier_sha256` (must equal `1f182b40c9d7c316e57ecd69acd62c7f6407514b9c675bdf8ec677d3f0cb97d5` — assert by recompute, `shasum -a 256 results/phase25_frontier.json`). If the verdict is anything but MOOT, STOP and return to the orchestrator: the phase's remaining plans assume MOOT and an ADMITTED reading re-opens the attack schedule (the deferred LaunchAgent pattern) — nothing in this plan proceeds.
3. `git status --short results/` → the record shows `??`. Do NOT `git add` it (D-15; T-27-08). Run the quick suite again NOW with the file present-but-untracked: `.venv/bin/pytest -q tests/test_phase27_prereg.py tests/test_phase27_relearn.py -x` — the both-state tests take their PRESENT branch on the file (pins recomputed, rows re-derived, node ids collected, `test_admit_refuses_to_overwrite` now exercises the overwrite refusal against `RECORD`) while the ancestry guard and the tracked/added parity stay honest-with-zero; quote the passed count.
4. D-37 — watch every attack sub-mode refuse on the REAL record, once each, no training, on CPU: for each `mode` in `calibrate`, `curve`, `gate`, `structural-proof`: `.venv/bin/python scripts/phase27_relearn.py <mode> --leg n8` (plus `--baseline never_taught_1337` for `gate`); `echo "exit=$?"`. Each must exit non-zero with stderr containing `results/phase27_admission.json reads 'MOOT' — REFUSING to run this leg` and the first MOOT reason; paste all four stderr transcripts and exit codes into the SUMMARY verbatim. After the four calls: `find data -maxdepth 1 \( -name 'phase27_*' -o -name 'phase25_phase27_*' -o -name 'persona_relearn_attacker_*' \)` prints nothing (nothing was written), `git status --short` shows only the `??` record, and `pmset -g assertions | grep -c phase27` is 0 (nothing scheduled or held awake — D-09/D-14).
5. Write NO note file and NO SUMMARY yet — the SUMMARY is written at the end of Task 3 with all three tasks' transcripts. Commit nothing in this task.

Threat references: T-27-07 (the live `refuse_if_dirty` guard runs unstubbed at the one real `admit`), T-27-08 (no git write), T-27-04 (the refusals are watched on the real record, not a copy), T-27-02 (the verdict is MOOT with generated reasons, not INCONCLUSIVE — if INCONCLUSIVE, the frontier changed: STOP).
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && test -f results/phase27_admission.json && test -z "$(git ls-files results/phase27_admission.json)" && .venv/bin/python -c "import json; b=json.load(open('results/phase27_admission.json')); assert b['verdict']['verdict']=='MOOT', b['verdict']; assert b['cleared_counts']['a']==30 and b['cleared_counts']['b']==4 and b['cleared_counts']['c']==1; assert b['apparatus']['status']=='not exercised'; assert b['disjointness']['overlaps']=={'teaching':0,'trained_attack':0,'attacker_corpus':0}; print('MOOT OK')" && test -z "$(find data -maxdepth 1 \( -name 'phase27_*' -o -name 'phase25_phase27_*' -o -name 'persona_relearn_attacker_*' \))" && .venv/bin/python scripts/phase27_relearn.py calibrate --leg n8 2>&1 | grep -q "reads 'MOOT' — REFUSING"</automated>
  </verify>
  <acceptance_criteria>
    - `results/phase27_admission.json` exists, `git ls-files results/phase27_admission.json` is empty, and `python -c` confirms `verdict.verdict == "MOOT"`, 44 rows, cleared 30/4/1, `apparatus.status == "not exercised"`, `apparatus.reason == "gate read MOOT"`, three zero overlaps, `frontier_sha256` equal to `shasum -a 256 results/phase25_frontier.json`
    - The SUMMARY quotes the one `admit` stdout line, the record's verdict reasons in full, and FOUR refusal transcripts each containing `reads 'MOOT' — REFUSING to run this leg` with a non-zero exit code
    - `find data -maxdepth 1 \( -name 'phase27_*' -o -name 'phase25_phase27_*' -o -name 'persona_relearn_attacker_*' \)` prints nothing after the four refusals; `git log --format=%H -- scripts/phase27_prereg.py` is unchanged from step 1
    - `.venv/bin/pytest -q tests/test_phase27_prereg.py tests/test_phase27_relearn.py` green in the present-but-untracked state (count quoted)
    - No `git add` / `git commit` was run in this task (`git log -1 --format=%H` equals the SHA at the start of the task)
  </acceptance_criteria>
  <done>The gate has been called once; the record reads MOOT on disk, untracked; every leg refused on it; the operator has what Task 2 needs.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: The operator commits results/phase27_admission.json by hand (D-15 — the driver's git surface is read-only)</name>
  <what-built>`results/phase27_admission.json` is on disk, untracked, reading MOOT with generated reasons (0 of 44 PASS; cleared (a) 30 / (b) 4 / (c) 1; 6 REFUSED rows), pinned to the frontier by sha256, carrying seven live module digests, the disjointness zeros and the `apparatus: not exercised` block with `train_path`. Every attack sub-mode refused on it (Task 1 transcripts). The pre-registration's ancestry guard requires this file's first-add to be a strict descendant of the single `scripts/phase27_prereg.py` commit — it is, by construction, if nothing touched that module since plan 27-01. Precedent: Phase 26's `8652c15`, the only git write in that audit's lifetime.</what-built>
  <how-to-verify>
    From the repo root, in this order:
    1. `git status --short | grep -v '^??'` — empty (no peer session left staged changes; memory: concurrent agents edit the working tree).
    2. `git log --format=%H -- scripts/phase27_prereg.py` — exactly the one commit Task 1 quoted (no later touch).
    3. `.venv/bin/python -c "import json; b=json.load(open('results/phase27_admission.json')); print(b['verdict']['verdict'], b['cleared_counts'])"` — prints `MOOT {'a': 30, 'b': 4, 'c': 1, ...}`.
    4. `git add results/phase27_admission.json && git commit -m "results(27): commit the relearning admission record — MOOT, 0 of 44 PASS, cleared (a) 30 / (b) 4 / (c) 1, apparatus not exercised — operator commit, the driver's git surface is read-only"`.
    5. `.venv/bin/pytest -q tests/test_phase27_prereg.py tests/test_phase27_relearn.py -x` — the ancestry guard now checks one tracked artifact and passes; the both-ways pin, the provenance recompute, the node-id existence and the record re-derivation run their PRESENT branches.
    6. Reply with the commit SHA.
  </how-to-verify>
  <resume-signal>Paste the record's commit SHA.</resume-signal>
  <files>results/phase27_admission.json</files>
  <read_first>
    - .planning/phases/26-empirical-privacy-audit-canary/26-05-PLAN.md Task 2 (the operator-commit checkpoint precedent) and 26-05-SUMMARY.md "Task 2 (operator)" (commit 8652c15, author Rafael)
    - results/phase27_admission.json (read-only — the verdict and cleared counts quoted in the commit message)
    - tests/test_phase27_prereg.py::_assert_frozen_before (what the ancestry guard will check once the record is tracked)
  </read_first>
  <action>Human action, deliberately not automated (D-15; T-27-08): the operator stages and commits `results/phase27_admission.json` by hand with the six steps in how-to-verify, so the only git write to the record in the phase's lifetime is a human's. Claude does not run `git add` / `git commit` for this file. If the operator declines, Task 3 records the record as "written, not committed" and every both-state test stays honest-with-zero — nothing is assembled or ticked in that case.</action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && git ls-files results/phase27_admission.json | grep -q . && test "$(git log --diff-filter=A --format=%H -- results/phase27_admission.json | wc -l | tr -d ' ')" = "1" && .venv/bin/pytest -q tests/test_phase27_prereg.py::test_phase27_prereg_is_frozen_before_every_phase27_result tests/test_phase27_prereg.py::test_the_record_is_pinned_to_the_frontier_both_ways tests/test_phase27_relearn.py::test_provenance_digests_match_live_bytes tests/test_phase27_relearn.py::test_every_apparatus_node_id_exists tests/test_phase27_relearn.py::test_the_record_re_derives_from_build_record -x</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-files results/phase27_admission.json` is non-empty; `git log --diff-filter=A --format='%H %an' -- results/phase27_admission.json` is exactly one commit whose author is the operator (not a Claude-Session-attributed commit); the SHA is recorded in the SUMMARY with the timestamp
    - `git merge-base --is-ancestor <the prereg commit> <the record commit>` exits 0 and the two SHAs differ
    - The five named tests pass in the PRESENT state
  </acceptance_criteria>
  <done>The record is committed by the operator (SHA pasted).</done>
</task>

<task type="auto">
  <name>Task 3: Present-state guards, the refusals re-watched on the committed record, the D-38 ledger edits by hand, the full-suite and lint gate, the SUMMARY</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md lines 415-435 (the RELRN checkboxes) and 570-580 (the traceability rows RELRN-01..05 and the CANARY-01/02 rows as the filled-row shape)
    - .planning/ROADMAP.md "### Phase 27: Relearning Attack" (the plan list and progress row) and the Phase 26 entry as the closed shape
    - .planning/STATE.md frontmatter + "## Current Position" + "### Decisions"
    - .planning/phases/26-empirical-privacy-audit-canary/26-05-SUMMARY.md §"Planning-file edits (hand, diffed against a snapshot)" and §"The test counts, and the deltas"
    - results/phase27_admission.json (read-only — quote `apparatus`, `verdict.reasons`, `disjointness`)
    - .planning/phases/27-relearning-attack/27-CONTEXT.md D-05, D-38
  </read_first>
  <action>
1. Present-state guards, quoted: `.venv/bin/pytest -q tests/test_phase27_prereg.py tests/test_phase27_relearn.py tests/test_phase27_on_draw.py -x` — green; state that `test_phase27_prereg_is_frozen_before_every_phase27_result` now reports `checked == len(prereg_commits) * 1 == 1` (add a `-v` run of that one test and quote it), that `test_the_record_is_pinned_to_the_frontier_both_ways`, `test_cleared_abc_re_derive_on_every_row`, `test_provenance_digests_match_live_bytes`, `test_every_apparatus_node_id_exists` and `test_the_record_re_derives_from_build_record` took their PRESENT branches (grep each test's source for its `exists()` guard and say so). `git log --oneline -- results/phase25_frontier.json` is one line; `git log --oneline -- scripts/phase27_prereg.py` is one line.
2. D-37 on the COMMITTED record — the same four commands as Task 1 step 4, once each; the tracked conjunct is now satisfied and the verdict conjunct still refuses naming MOOT; paste the four transcripts (they should be byte-identical to Task 1's apart from nothing — say whether they are). `find data -maxdepth 1 \( -name 'phase27_*' -o -name 'phase25_phase27_*' -o -name 'persona_relearn_attacker_*' \)` still prints nothing.
3. The full-suite and lint gate, BEFORE any ledger edit (26-05's rule: run on the fully-tracked tree): `make lint` (quote the two ruff lines), then `make test` (quote `N passed / S skipped / F failed in T s` and `pytest --collect-only -q | tail -1`). Expected collected: `2802 + (27-01 delta) + 5 + (27-03 delta) + (27-04 delta)` — read the four measured deltas from the SUMMARYs, state the sum, and assert the tail-line equals it; `F == 0`. If `F != 0`, quote every failing node id and its cause; the clean-tree residue of 26-05 no longer applies (the record is committed), so any failure here is real — STOP and report rather than editing tests.
4. Byte-identity of the pinned inputs: `git diff --stat <prereg commit>..HEAD -- scripts/teach_persona.py results/phase25_frontier.json pyproject.toml scripts/phase27_prereg.py scripts/mitigation_gate.py scripts/erasure_gate.py` — empty; quote it. `ls artifacts/ | grep -c phase27` — 0 (D-14).
5. D-38 ledger edits — BY HAND, ZERO `gsd-sdk` mutation handlers (memory: the handlers corrupted frontmatter in five consecutive sessions). Snapshot first: `cp .planning/STATE.md .planning/ROADMAP.md .planning/REQUIREMENTS.md /private/tmp/claude-501/-Users-juliorcoelho-PersonaCore/dd990469-c5db-4dbd-b620-a3c00e907958/scratchpad/` (the session scratchpad). Then, with the Edit tool and asserted replacements:
   - `REQUIREMENTS.md`: tick `RELRN-01` (`- [ ]` → `- [x]`); RELRN-02, -03, -04, -05 checkboxes UNCHANGED and their requirement text UNCHANGED (D-05: never edited after seeing numbers). Traceability rows: `RELRN-01 | Phase 27 | **SATISFIED (plans 27-01, 27-03, 27-05).** <the gate invoked once through `scripts/phase27_relearn.py admit` on the measured frontier; `results/phase27_admission.json` (<operator SHA>) reads MOOT — 0 of 44 PASS, tallies {PASS 0, FAIL 32, INCONCLUSIVE 6, REFUSED 6}, cleared (a) 30 / (b) 4 / (c) 1 re-derived through the condition functions; X = extraction_ceiling by call; the recovery gate exists with `baseline=` KEYWORD_ONLY, no default, pinned keys only; Z rule in optimizer steps on the 50-step ladder capped at 400; guards: test names>` — every number quoted from the record, none authored; and for each of RELRN-02..05: `| RELRN-0N | Phase 27 | **NOT SATISFIED — named limitation:** not admitted by the gate; apparatus built and guarded, never exercised on a mitigated arm (`results/phase27_admission.json::apparatus.status == "not exercised"`, `reason == "gate read MOOT"`). Built as: <one clause per requirement — 02: cost-curve reducer + band + rung chain (`run_curve`, `band`, `z_rule`); 03: the curve is a finding block, `recovery_gate` takes no curve/band/rungs (`test_the_curve_cannot_reach_the_verdict`, `..._through_the_driver`); 04: one shared `TrainConfig`, off-disk diff, offset-stream sha256 (`test_off_disk_config_diff_is_empty`, `test_offset_stream_digests_prove_data_order`, `test_rung_chain_stream_equals_one_run`); 05: attacker corpus pinned by two sha256s, disjointness computed zero (`test_attacker_corpus_sha_re_renders`, `test_recovery_fixture_is_disjoint`)>; exercised end to end only on the CPU tiny fixture (`test_the_live_path_is_wired_end_to_end`). Carried to Phase 28 as a named limitation (D-05). |`.
   - `ROADMAP.md`: under "### Phase 27" set `**Plans:** 5 plans` and the list `- [x] 27-01-PLAN.md — ...` through `- [x] 27-05-PLAN.md — ...` (brief objectives), the progress row to `5/5 Plans complete — verification pending` with the MOOT finding in one sentence; the phase-level checkbox stays for `/gsd:verify-work 27`.
   - `STATE.md`: frontmatter `stopped_at` → "Phase 27 wave 4 COMPLETE (<date>) — admission record committed by the operator at <SHA>, verdict MOOT ...; next: /gsd:verify-work 27"; `last_updated`, `last_activity`; `completed_plans` += 5 and `total_plans` += 5 (keep the pre-existing `total_plans < completed_plans` inconsistency noted, not repaired); `## Current Position` → Phase 27 5 of 5 plans complete; append `[Phase 27] 27-05` decisions under `### Decisions` (MOOT shipped; OQ1-B train path disclosed in the record; RELRN-02..05 unticked by design; no MPS second spent); `## Session Continuity`.
   Diff each file against its snapshot (`diff -u`) and quote the three diffs' line counts and confirm every hunk is one of the intended edits; a stray hunk is repaired by hand before committing.
6. Write `27-05-SUMMARY.md` (this plan's) with: the three tasks' transcripts, the operator's SHA + timestamp, the eight refusal transcripts, the suite counts table (baseline 2802 → measured, per-plan deltas summed), the three ledger diff summaries, and the plain sentence: "the apparatus was never run on MPS in this phase; nothing trained on real data; the finding shipped is MOOT" (D-09). Commit the ledgers + SUMMARY as the plan's tracking commit (`docs(27-05): close — MOOT shipped, RELRN-01 ticked, 02–05 named limitation`); the record is NOT in this commit (it is already the operator's).

Threat references: T-27-08 (the machine's only commits are docs/tests — `git show --stat` of this task's commit lists no `results/` path), T-27-03 (frontier still at one commit), T-27-12 (provenance present-state green), T-27-01 (the prereg module has one commit and it precedes the record).
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && git ls-files results/phase27_admission.json | grep -q . && .venv/bin/pytest -q tests/test_phase27_prereg.py tests/test_phase27_relearn.py tests/test_phase27_on_draw.py tests/test_phase25_close.py tests/test_phase24_record.py -x && grep -q "^- \[x\] \*\*RELRN-01\*\*" .planning/REQUIREMENTS.md && test "$(grep -c '^- \[ \] \*\*RELRN-0[2-5]\*\*' .planning/REQUIREMENTS.md)" = "4" && test "$(grep -c 'never exercised on a mitigated arm' .planning/REQUIREMENTS.md)" = "4" && test -z "$(git diff --stat HEAD -- scripts/teach_persona.py results/phase25_frontier.json pyproject.toml scripts/phase27_prereg.py)" && test -z "$(find data -maxdepth 1 \( -name 'phase27_*' -o -name 'phase25_phase27_*' -o -name 'persona_relearn_attacker_*' \))" && test "$(ls artifacts | grep -c phase27)" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `make test` green on the fully-tracked tree with `pytest --collect-only -q | tail -1` equal to `2802 + Σ(measured deltas of 27-01..27-04)` — the arithmetic written out in the SUMMARY; `make lint` clean
    - `tests/test_phase27_prereg.py::test_phase27_prereg_is_frozen_before_every_phase27_result -v` reports PASSED with one tracked artifact; `git log --oneline -- results/phase25_frontier.json` and `-- scripts/phase27_prereg.py` each one line
    - Four refusal transcripts on the COMMITTED record quoted, each containing `reads 'MOOT' — REFUSING to run this leg`, exit non-zero; `find data -maxdepth 1 \( -name 'phase27_*' -o -name 'phase25_phase27_*' -o -name 'persona_relearn_attacker_*' \)` empty
    - `REQUIREMENTS.md`: `- [x] **RELRN-01**` present; `- [ ] **RELRN-02..05**` present (4 lines); four traceability rows contain `not admitted by the gate; apparatus built and guarded, never exercised on a mitigated arm`; the RELRN-02..05 requirement TEXT lines are byte-identical to the snapshot (quote `diff` showing only the traceability rows changed)
    - `ROADMAP.md` Phase 27 lists five `[x]` plans; `STATE.md` frontmatter parses (`head -20`) and names the operator's SHA
    - `git show --stat HEAD` (the tracking commit) lists only `.planning/` paths; `git log --diff-filter=A --format=%an -- results/phase27_admission.json` is the operator
    - `git diff --stat <prereg commit>..HEAD -- scripts/teach_persona.py results/phase25_frontier.json pyproject.toml scripts/phase27_prereg.py scripts/mitigation_gate.py scripts/erasure_gate.py` empty; `ls artifacts | grep -c phase27` is 0
  </acceptance_criteria>
  <done>Phase 27 closed on the MOOT finding: record committed by the operator, guards present-state green, ledgers hand-edited and diffed, full suite and lint green with the stated collected count; ready for `/gsd:verify-work 27`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → git | The only write of the record to git is a human's commit (D-15); the machine commits docs/tests only |
| results/phase27_admission.json → Phase 28 | Phase 28 quotes the record by sha256 (verdict reasons, apparatus block, disjointness); nothing here is authored prose |
| the real record → the four sub-modes | Watched refusals on the real record, before and after commit, no training, CPU only |
| ledgers ← hand edits | Snapshot → edit → diff; zero `gsd-sdk` mutation handlers (D-38) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | Tampering | prereg edited after the record | mitigate | ancestry guard PRESENT state: one prereg commit, strict ancestor of the record's first-add; `git log` quoted |
| T-27-02 | Repudiation | MOOT vs INCONCLUSIVE confusion | mitigate | Task 1 STOPS on any verdict but MOOT; reasons generated from counts quoted in full |
| T-27-03 | Tampering | frontier re-emitted / record hand-edited | mitigate | frontier one-commit assertion; `test_the_record_re_derives_from_build_record` present state; the record is never edited by hand |
| T-27-04 | Denial (of evidence) | legs silently proceeding on MOOT | mitigate | D-37 refusals watched twice on the real record (untracked and committed), transcripts quoted |
| T-27-07 | Repudiation | dirty-tree provenance | mitigate | the live `refuse_if_dirty` runs unstubbed at the one real `admit` |
| T-27-08 | Elevation | machine git write of the record | mitigate | Task 2 is a human action; Task 3's commit `--stat` lists only `.planning/` |
| T-27-12 | Tampering | provenance drift after commit | mitigate | `test_provenance_digests_match_live_bytes` present state; pinned-input `git diff --stat` empty |
| T-27-SC | Tampering | package installs | accept | `pyproject.toml` byte-identical (D-39) — nothing installed across the phase |
</threat_model>

<verification>
- The gate was called once; the record reads MOOT and is operator-committed; every both-state guard is present-state green
- Eight refusal transcripts (four before, four after the commit) name MOOT with non-zero exits; `data/phase27_*` / `data/phase25_phase27_*` / `data/persona_relearn_attacker_*` never came to exist
- `make test` and `make lint` green on the fully-tracked tree; the collected count equals the stated arithmetic
- RELRN-01 ticked; RELRN-02..05 unticked with the named limitation; requirement text unchanged; no plist; pinned inputs byte-identical
</verification>

<success_criteria>
- SC1 holds in its MOOT form: `relearning_is_worth_attempting` invoked once on measured frontier numbers, MOOT recorded and shipped as the finding; SC2–SC5 hold as guarded code with the record's `apparatus` block naming "not exercised" and the tests that guard each — carried to Phase 28 as a named limitation
</success_criteria>

<output>
Create `.planning/phases/27-relearning-attack/27-05-SUMMARY.md` when done
</output>
