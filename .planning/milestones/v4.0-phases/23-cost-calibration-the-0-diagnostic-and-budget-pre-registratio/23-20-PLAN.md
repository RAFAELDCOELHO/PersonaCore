---
phase: 23-cost-calibration-the-0-diagnostic-and-budget-pre-registratio
plan: 20
type: execute
wave: 12
depends_on: ["23-17"]
gap_closure: true
files_modified:
  - scripts/phase23_resume_prereg.py
  - tests/test_phase23_resume_prereg.py
  - scripts/phase23_run.py
  - tests/test_phase23_matched.py
  - results/phase23_matched_control.json
  - results/phase23_matched_control_seed2025/run.csv
  - results/phase23_matched_control_seed1339/run.csv
  - results/phase23_resume_run.log
  - data/phase23_run_state.json
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [DPSGD-06]

must_haves:
  truths:
    - "`scripts/phase23_matched_prereg.py` ends this plan BYTE-IDENTICAL to `c100388`, and `git log --oneline -- scripts/phase23_matched_prereg.py` still returns EXACTLY ONE commit. MEASURED at HEAD: `git merge-base --is-ancestor HEAD d99d2aa` exits NON-ZERO, so any second commit touching that file fails `_assert_ordering_holds`' second conjunct against `d99d2aa` (the earliest add of `results/phase23_matched_*`) and reddens `tests/test_phase23_matched_prereg.py` PERMANENTLY. `adds[-1]` takes the EARLIEST add, so delete-and-re-add cannot launder it."
    - "The narrowing is a NEW pre-registration in a NEW file — `scripts/phase23_resume_prereg.py` — arriving VISIBLY beside the frozen pin. `prove_first_attempt` is not edited, not wrapped, and NOT PASSED A FILTERED INPUT: the first-attempt branch is selected on `not scored` ALONE, so a tracked artifact always reaches the frozen rule with the real, unfiltered `git ls-files` result and is free to fire its own refusal. **STATED AT TRUE STRENGTH: this is NOT an instance of clause (4).** Clause (4) is about a comparator RENAMED OUT of `MATCHED_ARTIFACT_GLOB` (`results/phase23_rematch_*`); these artifacts keep their names and stay INSIDE the glob. What is borrowed is clause (4)'s REMEDY SHAPE — a second rule must arrive with a new pre-registration, VISIBLE rather than refused — not its case. 23-17-SUMMARY's `Notes for whoever plans the completion` item 3 names the same route."
    - "THE DISCRIMINATION IS THE RECORD'S WRITE-ORDERING AND NOTHING ELSE. `matched()` writes `results/phase23_matched_control.json` as its LAST act, AFTER the `for seed in seeds:` loop has scored EVERY seed in `SEED_LADDER`. A COMPLETED attempt therefore necessarily leaves `len(scored) == len(ladder)`. `len(scored) < len(ladder)` is a state the completion path CANNOT PRODUCE — and deleting a completed attempt's record does not un-score its seeds, because `_state_record` refuses to overwrite a recorded value at a different one. The argument is about the ORDER THE WRITER WRITES IN. It is not about `0.7837301587301587`, `0.7678571428571429` or `0.7718253968253969`, and it would be the same argument at any three values."
    - "THAT INDEPENDENCE IS STRUCTURAL, NOT ASSERTED, AND THE PROOF PERTURBS EVERY READING RATHER THAN THREE NAMED ONES. `prove_killed_run_continuation`'s signature carries NO rate, count, `k`, `n`, floor or reading — only seed COUNTS, seed SETS, trained/scored STATUS derived from KEY PRESENCE, and one boolean. A reader checks it by reading the signature. A test proves it by REBUILDING EVERY SEED BLOCK AS `{k: <arbitrary> for k in block}` — every value in the block replaced, KEY PRESENCE ALONE preserved — in three variants, so `heldout_on.rate`, `final_train_loss` and `training_seconds` are perturbed too and not only `primary.rate`/`.k`/`.n`. The derived arguments, the return value AND the refusal message are bit-identical across all four. THE FIXTURE IS A PINNED BLOB — `git show d99d2aa:data/phase23_run_state.json`, MEASURED to carry exactly `scored = ['1337','1338','2024']` and reachable forever (`git merge-base --is-ancestor d99d2aa HEAD` exits 0) — NEVER the live working-tree file, which Task 3 rewrites to FIVE scored seeds and commits. A fixture read live would flip this proof from ADMIT to REFUSE the moment Task 3 lands, AFTER the ~43-minute GPU run."
    - "SEED 2025 IS RETRAINED FROM SCRATCH, NOT MID-TRAIN RESUMED, AND THE REASON IS A COMMITTED REFUSAL RATHER THAN A PREFERENCE. `prove_matched_protocol` (`scripts/phase23_run.py:1181-1187`) refuses `resume_from` in the comparator's kwargs, and `phase23_matched_prereg.DP_FN_BRANCH_DISPOSITIONS` items 6 and 7 are dispositioned `unreached` PRECISELY because this scheduling resumes nothing. A mid-train resume would reach two branches the BLIND ledger declared unreachable — a protocol change, in the one place this phase cannot make one."
    - "N=5 IS NOT REOPENED AND NO FLOOR IS REDUCED OVER THREE READINGS. `SEED_LADDER` is untouched at `(1337, 2024, 1338, 2025, 1339)`, `matched()`'s inherited-seed-set proof against `state['cost']['seeds']` is untouched, `noise_floor` is CALLED and never inlined, and the floor is reduced over FIVE readings or not at all."
    - "The record DISCLOSES that it was a continuation. `attempt`, `continuation_rule`, `continuation_fingerprint` and `continuation_scope` travel WITH the artifact, beside the unedited `one_attempt_rule` / `one_attempt_scope` / `matched_glob_at_start` / `prior_scored_seeds_at_start`. The fingerprint carries `tracked` as well as the seed sets, so conjuncts 6 and 7 are RE-CHECKED on every suite run instead of being re-admitted vacuously against an empty list. It records `ladder` in LADDER ORDER (`list(SEED_LADDER)`), never sorted: `SEED_LADDER` is `(1337, 2024, 1338, 2025, 1339)`, conjuncts 3 and 4 INDEX it, and a sorted ladder makes every re-admission REFUSE. A reader who opens only this record learns it, exactly as `prove_control_record_declares_visibility`' docstring requires of the visibility disclosure."
    - "THE RUN CANNOT DIE TO A FOREGROUND TIMEOUT, A HARNESS PROCESS-GROUP KILL, OR LAPTOP SLEEP — AND THE DETACHMENT IS PROBED RATHER THAN ASSERTED. `setsid(1)` DOES NOT EXIST ON THIS MACHINE (`command -v setsid` exits 1; macOS never shipped util-linux's), so detachment is done by `os.setsid()` INSIDE the launched interpreter, which then `os.execv`s the real script — MEASURED: `pid == pgid == sid` and it SURVIVES the exec. `/usr/bin/caffeinate -is -w <pid>` holds a sleep assertion for exactly that pid. The probe is `os.getsid(pid)` from a second interpreter, NOT `ps -o sid` — BSD `ps` has no `sid` keyword and `ps -o pid,pgid,sid` exits with `ps: sid: keyword not found`, which would make the check pass vacuously or fail spuriously. 23-17's plan specified NO execution venue at all and its run was terminated externally at ~60 min at 3 of 5 seeds; that, not the protocol, is what failed."
    - "A SECOND KILL IS ADMITTED BY THE SAME RULE WITH NO FURTHER NARROWING, AND THE MECHANISM ITSELF SAYS HOW. Conjunct 5 compares the working tree's scored set against `git show HEAD:data/phase23_run_state.json`, so between a seed scoring and its state being committed the two DISAGREE and conjunct 5 REFUSES — the same self-contradiction 23-17 hit. The resolution is not a force flag and not a second narrowing: it is to COMMIT the state file and the newly completed seed directory BEFORE re-launching, and CONJUNCT 5'S OWN REFUSAL MESSAGE CARRIES THAT INSTRUCTION, so a future reader gets it from the mechanism rather than from plan prose."
  artifacts:
    - path: "scripts/phase23_resume_prereg.py"
      provides: "the continuation's OWN pre-registration — prove_killed_run_continuation, seed_status, CONTINUATION_SCOPE (FIVE clauses, the fifth disclosing this module's own missing freeze)"
      contains: "def prove_killed_run_continuation"
    - path: "tests/test_phase23_resume_prereg.py"
      provides: "every refusal WATCHED firing, the whole-block readings-invariance proof, the float-constant AST tripwire, the c100388 byte-identity check, and this module's own one-commit guard"
      contains: "def test_the_continuation_predicate_is_invariant_under_arbitrary_readings"
    - path: "results/phase23_matched_control.json"
      provides: "the protocol-matched comparator's FIVE per-seed readings and the RE-REDUCED floor"
      contains: "continuation_fingerprint"
    - path: "results/phase23_matched_control_seed1339/run.csv"
      provides: "the fifth and last owed seed's training curve"
    - path: "results/phase23_resume_run.log"
      provides: "the detached run's full stdout, whose FIRST LINE is the measured `pid/pgid/sid` — the detachment evidence, committed"
  key_links:
    - from: "scripts/phase23_run.py::matched"
      to: "scripts/phase23_resume_prereg.py::prove_killed_run_continuation"
      via: "the continuation branch, taken whenever the state holds any scored seed"
      pattern: "rp\\.prove_killed_run_continuation"
    - from: "scripts/phase23_run.py::matched"
      to: "scripts/phase23_matched_prereg.py::prove_first_attempt"
      via: "the first-attempt branch, selected on `not scored` ALONE and passed the UNFILTERED glob result"
      pattern: "mp\\.prove_first_attempt"
    - from: "scripts/phase23_resume_prereg.py"
      to: "git show HEAD:data/phase23_run_state.json"
      via: "committed_scored_seeds, passed in by the caller — history must agree with the working tree"
      pattern: "committed_scored_seeds"
    - from: "results/phase23_matched_control.json"
      to: "scripts/phase23_prereg.py::noise_floor"
      via: "the blind reduction CALLED over FIVE readings"
      pattern: "noise_floor"
---

<objective>
Complete the protocol-matched comparator's five-seed run by admitting the HARNESS-KILLED first
attempt's CONTINUATION — through a NEW pre-registration that arrives visibly beside the frozen pin,
never by editing it — and write `results/phase23_matched_control.json`, the input 23-18 and 23-19
have been waiting on since `39513dd`.

**Purpose.** 23-17's run was terminated externally at ~60 minutes with 3 of 5 seeds trained and
scored. Three refusals now stand, all correct as written, and 23-17's executor weakened none of
them. Seeds 2025 and 1339 are OWED. Without them there is no matched floor, no re-pin, and
**23-19's σ=0 VERDICT cannot be rendered.**

**Name the verdict, not the requirement.** MEASURED at `.planning/REQUIREMENTS.md:156-160`,
`DPSGD-06` is ALREADY `[x] SATISFIED (plan 23-10)` — its tick records that the diagnostic FIRED and
that D-04 halted the sweep, not a passing verdict. So "DPSGD-06 cannot close" does not reproduce and
is not this plan's stake. What is missing is 23-19's verdict artifact, and 23-18's re-pin before it.

**Output.** `scripts/phase23_resume_prereg.py` (new pin), its test file, a wired `matched()` call
site, the five-seed record, and the two owed seeds' artifacts — all committed.

**THIS PLAN RENDERS NO VERDICT AND REDUCES NO FLOOR OVER THREE READINGS.**
`phase23_prereg.sigma_zero_verdict` is 23-19's to call, against a floor 23-18 re-pins from this
record. `noise_floor` is applied to FIVE readings by `matched()`'s existing writer or not at all.
</objective>

<dispatch>
## HOW THIS PLAN IS DISPATCHED — MEASURED, AND ONE HAZARD HAS BEEN FIXED AT THE SOURCE

**The wave collision this section used to warn about is GONE, and it was fixed in the DAG rather
than in prose.** Recorded here because the fix is the evidence.

**Measured BEFORE the fix:** `gsd-sdk query phase-plan-index 23` recomputes `wave` from `depends_on`
and IGNORES the declared `wave:` frontmatter field (it emits a `declared wave: N but depends_on DAG
places it in wave M` warning for every plan from 23-15 on). With `23-18: depends_on ["23-17"]` and
`23-20: depends_on ["23-17"]`, both landed in **recomputed wave 9** — dispatchable IN PARALLEL,
because their `files_modified` do not overlap and file ownership therefore does not serialise them.

**That is a real ordering defect, not a documentation one, and it damages 23-20 three ways:**

1. 23-18's Task 2 runs a full `.venv/bin/python -m pytest -q`, and
   `tests/test_phase23_resume.py::test_production_resume_epsilon_bit_identical` ends at `:720-728`
   with a **repository-wide** `git status --porcelain results/ == ""`. Run while this plan's
   detached job is actively writing under `results/`, it goes FALSE-RED. 23-17-SUMMARY §Deviation 2
   already records that exact guard reddening from that exact cause (`1 failed, 1517 passed`).
2. A concurrent full suite contends for the same MPS device and corrupts the per-seed wall-clock
   this plan requires reported against a committed bound.
3. Two executors contend on `.git/index.lock`.

And 23-18 does **not** strictly fail closed on the missing record: its Task 1 step (a) is a dated
continuation comment that needs NO value from `results/phase23_matched_control.json`, so an executor
can get real work done before reaching the step (b) that does.

**FIXED:** `"23-20"` was added to `23-18-PLAN.md`'s `depends_on`. **Measured AFTER the fix:**
`waves: {"8": ["23-14","23-17"], "9": ["23-20"], "10": ["23-18"], "11": ["23-19"]}` — 23-20 is
ALONE in its wave and 23-18 cannot start until it finishes. Prose cannot fix a DAG; this was fixed
in the DAG.

**One residual, stated rather than hidden:** 23-18's *declared* `wave: 12` field was deliberately
left alone (only its `depends_on` was edited), so it now declares the same number as this plan.
The declared field is documentation the SDK ignores, and bumping 23-18 to 13 would collide it with
23-19's declared 13, cascading a worse inconsistency. The recomputed DAG above is what dispatches.

**The index trap, still live:** `gsd-sdk query phase-plan-index 23` reports `23-17 has_summary:
true` and omits `23-17` from `incomplete`, because it keys on the EXISTENCE of `23-17-SUMMARY.md`
and not on its `status: INCOMPLETE`.

**THE DISPATCH:** execute **this plan alone**, against this file, following
`@$HOME/.claude/get-shit-done/workflows/execute-plan.md`. `--wave 9` now also does the right thing.

**PRE-DISPATCH CHECK (run first, expect exactly this):**
```bash
test ! -f results/phase23_matched_control.json && echo "OK: record absent, 23-20 is the right plan"
git ls-files 'results/phase23_matched_*' | wc -l          # expect 4
git log --oneline -- scripts/phase23_matched_prereg.py | wc -l   # expect 1
gsd-sdk query phase-plan-index 23 | grep -A3 '"9"'        # expect 23-20 ALONE in wave 9
```
</dispatch>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@CLAUDE.md

@.planning/phases/23-cost-calibration-the-0-diagnostic-and-budget-pre-registratio/23-17-SUMMARY.md
@.planning/phases/23-cost-calibration-the-0-diagnostic-and-budget-pre-registratio/23-MECHANISM-ASSESSMENT.md

`23-MECHANISM-ASSESSMENT.md` **GOVERNS NOTHING** — it is priority and confidence information. It
does not reopen N, does not pin a floor and does not licence a change to any pre-registered
quantity.

`.planning/debug/sigma-zero-beats-control.md` is **READ-ONLY for this plan.** 23-19 owns its dated
continuation and gates on `grep -c "PROTOCOL-MATCHED COMPARATOR"` plus an append-only deletion
check. Writing there would satisfy a downstream plan's acceptance criterion with text that plan did
not produce.

<interfaces>
<!-- Extracted from live source at HEAD. Use these directly; do not go exploring for them. -->

scripts/phase23_matched_prereg.py — **FROZEN. IMPORT, NEVER EDIT.**
```python
MATCHED_CONTROL_RECORD = "results/phase23_matched_control.json"
MATCHED_VERDICT_RECORD = "results/phase23_matched_verdict.json"
MATCHED_ARTIFACT_GLOB  = "results/phase23_matched_*"
MATCHED_ARM_PREFIX     = "phase23"
MATCHED_GRAD_CLIP      = 1e6
def matched_arm(seed) -> str                 # 1337 -> "matched_control_seed1337"
def prove_first_attempt(tracked) -> True     # SystemExit if `tracked` is non-empty. :616
def prove_control_record_declares_visibility(record) -> True
SIGMA_ZERO_VISIBILITY_DISCLOSURE: str
```

**THE PER-SEED PATH, MEASURED — do not re-derive it, and do not guess.** `matched_arm` returns the
arm name WITHOUT the prefix, and `teach_persona.arm_outputs` composes `{prefix}_{arm}`. So:
```python
matched_arm(1337)                                    # -> 'matched_control_seed1337'
MATCHED_ARM_PREFIX                                   # -> 'phase23'
f"results/{MATCHED_ARM_PREFIX}_{matched_arm(1337)}/run.csv"
#   -> 'results/phase23_matched_control_seed1337/run.csv'   <- MEASURED, matches git ls-files
```
`f"results/{matched_arm(s)}/run.csv"` is **WRONG** — it drops the prefix and yields
`results/matched_control_seed1337/run.csv`, which matches nothing.

scripts/phase23_run.py — **NOT frozen. This is where the caller lives.**
```python
SEED_LADDER = (1337, 2024, 1338, 2025, 1339)                      # :137
STATE_PATH  = <repo>/data/phase23_run_state.json
_SCRIPTS    = str(pathlib.Path(__file__).resolve().parent)         # :80  <- __file__-dependent
_ROOT       = pathlib.Path(__file__).resolve().parent.parent       # :112 <- __file__-dependent
def _prove(condition, message)                                     # SystemExit register
def _state_load() -> dict                                          # :423
def _state_record(section, key, block)                             # :435 refuses a CHANGED value
def _already_trained(section, seed) -> bool                        # :506 adapter present + sha match
def train_matched_control(seed) -> dict                            # :1248
def matched_control_call(seed) -> (fields, kwargs)                 # :1042 — NO resume_from, NO dp_fn
def prove_matched_protocol() -> Counter                            # :1132 — REFUSES resume_from :1181
def matched()                                                      # :2121 — the sub-mode
def main(argv=None)                                                # :2441 — `_TABLE[argv[0]]()`, returns 0
# the call site this plan rewires:
#   :2138-2146  matched_glob_at_start = git ls-files MATCHED_ARTIFACT_GLOB; mp.prove_first_attempt(...)
#   :2168-2175  prior = _state_load().get("matched", {})
#               prior_scored_seeds_at_start = sorted(s for s, b in prior.items() if "primary" in b)
#               _prove(not prior_scored_seeds_at_start or record.exists(), ...)
#   :2395       path.write_text(...)   <- THE LAST ACT. This ordering IS the discrimination.
```

scripts/teach_persona.py
```python
def refuse_if_exists(paths, *, expected=())   # :370 — D-07's resume inversion already exists
def arm_outputs(arm, *, prefix) -> dict       # csv / checkpoint / adapter / bin / mask; {prefix}_{arm}
```

tests/test_phase23_matched.py — **the two assertions this plan MUST widen**
```python
:492  if record is None: return                              # the vacuity branch
:494  assert record["matched_glob_at_start"] == []          # goes RED on a continuation (4 tracked)
:498  assert record["prior_scored_seeds_at_start"] == []    # goes RED on a continuation (3 scored)
:502  assert record["one_attempt_rule"] == "phase23_matched_prereg.prove_first_attempt"   # KEEP
:558  test_the_matched_writer_does_not_inline_the_reduction # AST: NO max/min Call inside `matched`
```

tests/test_phase23_resume.py — **the repo-wide census that must not be run concurrently**
```python
:720-728  porcelain = git status --porcelain results/ ; assert porcelain == ""
```

tests/test_phase20_prereg.py — the freeze mechanism, for reference only
```python
def _assert_ordering_holds(*, root, prereg_artifact, artifact_glob, globs)   # :189
# CONJUNCT 1: prereg != first_add        CONJUNCT 2: merge-base --is-ancestor prereg first_add
# first_add = adds[-1] — the EARLIEST add. Delete-and-re-add cannot launder it.
```
</interfaces>

<measured_state>
Re-verify each of these yourself before Task 1; do not take them on trust.

| Fact | Command | Expected |
|---|---|---|
| the pin has ONE commit | `git log --oneline -- scripts/phase23_matched_prereg.py \| wc -l` | `1` (`c100388`) |
| the pin is unedited | `git diff --exit-code c100388 HEAD -- scripts/phase23_matched_prereg.py` | exit `0` |
| **a new pin commit would be FATAL** | `git merge-base --is-ancestor HEAD d99d2aa; echo $?` | **NON-ZERO** |
| the earliest matched add | `git log --oneline --diff-filter=A -- 'results/phase23_matched_*'` | `d99d2aa` |
| the closed pin is unedited | `git diff --exit-code c7de5d4 HEAD -- scripts/phase23_prereg.py` | exit `0` |
| tracked matched artifacts | `git ls-files 'results/phase23_matched_*' \| wc -l` | `4` (seeds 1337/1338/2024/2025 `run.csv`) |
| the record is absent | `test -f results/phase23_matched_control.json; echo $?` | `1` |
| scored, in HISTORY | `git show HEAD:data/phase23_run_state.json` → `matched` keys with `primary` | `['1337','1338','2024']` |
| scored, in the WORKING TREE | same, from `data/phase23_run_state.json` | `['1337','1338','2024']` — IDENTICAL |
| seed 2025 partial | `ls results/phase23_matched_control_seed2025/run.csv checkpoints/phase23_matched_control_seed2025_latest.pt` | both exist |
| seed 2025 has NO adapter | `ls checkpoints/phase23_matched_control_seed2025_adapter.pt` | No such file |
| seed 1339 never started | `ls results/phase23_matched_control_seed1339` | No such directory |
| `results/` is clean | `git status --porcelain results/` | empty |
| suite baseline | `.venv/bin/python -m pytest -q` | `1518 passed, 1 skipped` |
| lint baseline | `.venv/bin/ruff check . && .venv/bin/ruff format --check .` | clean, 217 files |
| **`sorted()` is NOT ladder order** | `sorted(['1337','1338','2024'])` vs `SEED_LADDER[:3]` | `['1337','1338','2024']` vs `(1337, 2024, 1338)` — **compare SETS, never sorted lists** |
| **the new module has NO history yet** | `git log --format=%H -- scripts/phase23_resume_prereg.py \| wc -l` | `0` — it becomes `1` at Task 1's commit and must STAY `1` |
| **`SEED_LADDER` ITSELF is NOT sorted** | `sorted((1337,2024,1338,2025,1339))` | `[1337, 1338, 1339, 2024, 2025]` — so `set(sorted(ladder)[:3])` is `{1337,1338,1339}`, NOT the real `{1337,2024,1338}`. **RECORD `ladder` IN LADDER ORDER — conjuncts 3 and 4 INDEX it.** |
| **the PINNED test fixture** | `git show d99d2aa:data/phase23_run_state.json` → `matched` keys with `primary` | `['1337','1338','2024']`, and `git merge-base --is-ancestor d99d2aa HEAD` exits `0`. **This blob CANNOT change. The live file DOES — Task 3 rewrites it to five scored seeds and commits it.** |
| **`path` is bound AFTER the gate** | `sed -n '2180p' scripts/phase23_run.py` | `path = _ROOT / mp.MATCHED_CONTROL_RECORD` — but Task 2 Step B rewires `:2134-2175`, so `path.exists()` there is a `NameError`. Use `(_ROOT / mp.MATCHED_CONTROL_RECORD).exists()`, exactly as `:2171` already does. |
| **the retained `_prove` is UNFIREABLE on its branch** | `sed -n '2169p' scripts/phase23_run.py` | `sorted(s for s, b in prior.items() if "primary" in b)` — the SAME predicate `seed_status` uses for `scored`, over the SAME dict. On `if not scored:` its first disjunct is tautologically true. Retain it; do not claim it refuses. |
| **there is NO `23-20` ROADMAP line** | `grep -c '23-20-PLAN.md' .planning/ROADMAP.md` | `0` — Task 3 **ADDS** it. "Tick it" is not a runnable instruction. |
| **the planning counts** | `grep -n 'total_plans\|completed_plans\|percent' .planning/STATE.md` | `total_plans: 66`, `completed_plans: 59`, `percent: 33` (phase-based `3/9` — STAYS) |

### Execution-venue facts — MEASURED ON THIS MACHINE, and two of them are traps

| Fact | Command | Result |
|---|---|---|
| **`setsid(1)` DOES NOT EXIST** | `command -v setsid` | **exit 1, no output.** macOS never shipped util-linux's `setsid`. Any `nohup setsid …` line exits **127** and the run never starts. |
| `os.setsid()` DOES work | `python -c "import os; os.setsid()"` in a backgrounded child | succeeds — the harness runs background children WITHOUT job control, so the child is not already a process-group leader |
| the session survives `execv` | probed with a stand-in | `pid == pgid == sid` **before and after** the exec |
| `__file__` is ABSOLUTE under `python script.py` | probed | absolute — so `_SCRIPTS`/`_ROOT` at `:80`/`:112` resolve correctly. **`runpy.run_path('scripts/phase23_run.py')` sets a RELATIVE `__file__`** and makes both cwd-dependent; `os.execv` does not. |
| **BSD `ps` HAS NO `sid` KEYWORD** | `ps -o pid,pgid,sid -p <pid>` | **`ps: sid: keyword not found`.** A detachment probe written with `ps -o …,sid` does not measure anything on this machine. Use `os.getsid(pid)` from a second interpreter. |
| `caffeinate` and `screen` DO exist | `ls /usr/bin/caffeinate /usr/bin/screen` | both present |
| `caffeinate -is -w <pid>` holds | `pmset -g assertions` | `PreventUserIdleSystemSleep … asserting on behalf of Process ID <pid>` |
</measured_state>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The continuation's OWN pre-registration, in a NEW file beside the frozen pin</name>

  <files>scripts/phase23_resume_prereg.py, tests/test_phase23_resume_prereg.py</files>

  <read_first>
    - `scripts/phase23_matched_prereg.py` — the WHOLE module docstring, `_prove` (:47), `matched_arm`
      (:91), `prove_first_attempt` (:616) including all four scope clauses, and the `__main__`
      self-check (:818). This is the register the new module is written in.
    - `scripts/phase23_run.py:2121-2250` — `matched()`, specifically that `path.write_text` at
      **:2395** is the LAST act after the `for seed in seeds:` loop. That ordering is the entire
      argument the new module makes.
    - `scripts/phase23_run.py:435-458` — `_state_record`'s no-overwrite refusal.
    - `tests/test_phase20_prereg.py:189-266` — `_assert_ordering_holds`, both conjuncts, and
      `adds[-1]`.
    - `tests/test_phase23_matched_prereg.py:1-110` — the register the new test file follows, and
      the `_ordering_guard` import-never-copy discipline.
  </read_first>

  <behavior>
    `prove_killed_run_continuation(*, tracked, ladder, trained_seeds, scored_seeds,
    committed_scored_seeds, record_exists)` returns `True` for EXACTLY ONE shape of state and
    raises `SystemExit` for every other. Watched firing, one test per refusal:

    - ADMITS: `record_exists=False`, `scored == set(ladder[:3])` = `{1337, 2024, 1338}`,
      `trained == scored`, `committed == scored`, `tracked` = the four per-seed `run.csv` paths.
      (Derived through `seed_status` — never typed — from a PINNED COMMIT,
      **`git show d99d2aa:data/phase23_run_state.json`**, NOT from the live working-tree file. See
      "THE FIXTURE IS PINNED" in `<action>`: Task 3 writes five scored seeds into the live file and
      commits it, so a fixture read live flips from ADMIT to REFUSE the moment Task 3 lands.)
    - ADMITS: the same with `trained == scored | {2025}` (one trained-but-unscored seed, and it is
      the NEXT ladder entry) — so a SECOND kill during the resume is admitted by the SAME rule
      with no further narrowing.
    - ADMITS: `scored == set(ladder[:4])` with `committed == scored` — the state one more scored
      seed leaves ONCE ITS STATE FILE HAS BEEN COMMITTED. The rule is written on PREFIXES, not on
      the number three.
    - REFUSES that same four-scored state while `committed == set(ladder[:3])` — i.e. the seed has
      scored but its state has NOT been committed yet. **This is the second-kill window, and the
      refusal is CORRECT.** The message must name the remedy: commit `data/phase23_run_state.json`
      and the newly completed seed directory, then re-launch. There is no force flag and no second
      narrowing.
    - REFUSES `len(scored) == len(ladder)`: a completed attempt. Message names the write-ordering.
    - REFUSES `record_exists=True`: the record is recorded evidence; `matched()` refuses it too.
    - REFUSES `scored == set()`: that is a FIRST attempt and `prove_first_attempt`'s business.
    - REFUSES a non-prefix scored set, e.g. `{1337, 1338}` (2024 skipped): the loop is sequential,
      so a killed run leaves a prefix.
    - REFUSES `trained - scored` larger than one seed, or not equal to the next ladder entry.
    - REFUSES `scored != committed_scored` in EITHER direction — the working tree disagreeing with
      history is what a hand-edited state file looks like, and also what an uncommitted second kill
      looks like; the message covers both.
    - REFUSES a `tracked` entry equal to `MATCHED_CONTROL_RECORD` or `MATCHED_VERDICT_RECORD`.
    - REFUSES a tracked `run.csv` for a seed outside `trained | {next}`, and any tracked path that
      is not a per-seed `run.csv` for an arm in the ladder.
    - IS INVARIANT under arbitrary readings: the derived arguments, the return value AND the refusal
      message are bit-identical when EVERY VALUE IN EVERY SEED BLOCK is replaced and only KEY
      PRESENCE is preserved.
  </behavior>

  <action>
Create `scripts/phase23_resume_prereg.py`. **A NEW FILE, and the reason is measured, not stylistic:**
`git merge-base --is-ancestor HEAD d99d2aa` exits NON-ZERO, so a second commit touching
`scripts/phase23_matched_prereg.py` fails `_assert_ordering_holds`' ancestry conjunct against
`d99d2aa` and reddens `tests/test_phase23_matched_prereg.py` permanently, with no recovery —
delete-and-re-add launders nothing because `adds[-1]` takes the EARLIEST add. This is the same
REMEDY SHAPE clause (4) of `prove_first_attempt`'s message describes (a second rule must `arrive
with a NEW pre-registration. That is VISIBLE, NOT REFUSED`) and the route `scripts/_addendum.py`'s
docstring documents for the phase-17/18 writers frozen by this same guard class. **State the borrow
at its true strength: clause (4)'s CASE is a comparator RENAMED OUT of `MATCHED_ARTIFACT_GLOB`, and
this is not that** — these artifacts keep their names and stay inside the glob. Record that argument
IN the module docstring; a route chosen for a measured reason and recorded as a preference is a
route the next reader re-litigates.

Stdlib only — `json` at most, no `torch`, no network, **no `subprocess`** (the CALLER runs git,
exactly as `prove_first_attempt` takes its caller's `git ls-files` result). Import
`MATCHED_CONTROL_RECORD`, `MATCHED_VERDICT_RECORD`, `MATCHED_ARM_PREFIX` and `matched_arm` from
`phase23_matched_prereg` rather than retyping them — a retyped path is a second source for one fact,
which is this phase's own subject. `_prove` raising `SystemExit`, never `assert`: `python -O` strips
`assert` and this module is entirely refusals.

**Write the module docstring's central section first, before any code — it is the deliverable.**

  * *Why this file exists* — the ancestry measurement above, stated as the command and its exit
    code.
  * *THE DISCRIMINATION, ARGUED FROM WRITE-ORDERING ALONE.* `matched()` writes
    `results/phase23_matched_control.json` at `scripts/phase23_run.py:2395`, as its LAST act, AFTER
    the `for seed in seeds:` loop has scored EVERY seed in `SEED_LADDER`. So a COMPLETED attempt
    necessarily leaves FIVE scored seeds. `len(scored) < len(ladder)` is a state the completion path
    CANNOT PRODUCE. A completed attempt whose record was then DELETED still leaves five, because
    deleting the record does not un-score the seeds and `_state_record` refuses to overwrite a
    recorded value at a different one; to read three it would additionally have to delete two seeds'
    state blocks, which is a VISIBLE DIFF against `git show HEAD:data/phase23_run_state.json` —
    which is what the `committed_scored_seeds` conjunct compares.
  * *WHY THIS ARGUMENT IS INDEPENDENT OF THE READINGS, AND HOW A REVIEWER CHECKS IT.* Three readings
    (`0.7837301587301587`, `0.7678571428571429`, `0.7718253968253969`) were on screen when this
    module was written, and narrowing a refusal after seeing its readings is indistinguishable from
    narrowing it BECAUSE of them unless the independence is structural. It is made structural in
    three ways, all checkable: **(a)** the signature carries no rate, count, `k`, `n`, floor or
    reading — only seed sets, seed counts, trained/scored STATUS derived from KEY PRESENCE, and one
    boolean, so a reading cannot enter; **(b)** `seed_status` reads `"primary" in block` and
    `"adapter_sha256" in block` — MEMBERSHIP, never a value; **(c)**
    `test_the_continuation_predicate_is_invariant_under_arbitrary_readings` REPLACES EVERY VALUE IN
    EVERY SEED BLOCK, preserving key presence alone, and proves the derived arguments, the return
    value and the refusal message bit-identical. Perturbing only `primary.rate`/`.k`/`.n` would not
    prove this: `heldout_on.rate` is also on screen, and so are `final_train_loss` and
    `training_seconds`, so a narrowing keyed on any of those would survive a three-field probe. The
    argument would be the same argument at any values, and the whole-block test is what makes that a
    fact rather than a claim.
  * *THE FIVE SCOPE CLAUSES — what this rule does NOT prove*, in `prove_first_attempt`'s register,
    stating the WEAKEST TRUE guarantee: (1) it binds on COMMITTED state plus the working tree, so an
    operator with a dirty tree and no commit is outside it exactly as clause (1) of the frozen rule
    describes; (2) it cannot distinguish a harness kill from a `SIGKILL` an operator sent — nor does
    it need to, since both leave the same partial state and neither produced a reading it did not
    keep; (3) it does NOT re-establish blindness — the blindness is SPENT, three readings are on
    screen, and this rule only says the continuation is the SAME attempt, never that it is a blind
    one; (4) it is scoped to `MATCHED_ARTIFACT_GLOB` and to `SEED_LADDER`, and a comparator under any
    other name is not refused by it; **(5) THIS MODULE IS NOT FROZEN, AND NOTHING FREEZES IT.**
    `scripts/phase23_matched_prereg.py` is held EDIT-ONCE by
    `tests/test_phase23_matched_prereg.py`'s `_assert_ordering_holds` against
    `MATCHED_ARTIFACT_GLOB`. **This module CANNOT be registered against that same glob**, because
    `adds[-1]` for that glob is `d99d2aa`, which PRECEDES any commit of this file, so conjunct 2
    (`merge-base --is-ancestor prereg first_add`) can never hold for it. What stands in for a freeze
    is strictly weaker and is named so: `test_the_resume_pin_has_exactly_one_commit` asserts
    `git log --format=%H -- scripts/phase23_resume_prereg.py` returns exactly ONE line, so a second
    edit reddens the suite VISIBLY — **detection after the fact, not prevention, and not the
    ancestry guarantee the frozen pin carries.** No override flag, no force flag, no warning branch,
    and none is to be added regardless of how the run turns out.

Then the code:

  * `_prove(condition, message)` — `SystemExit(f"[phase23_resume_prereg] {message}")`.
  * `seed_status(section)` -> `(trained, scored)` as **frozensets of ints**. `trained` = seeds whose
    block has `"adapter_sha256"`; `scored` = seeds whose block has `"primary"`. Key membership only.
    **Return sets, never sorted lists** — `sorted(['1337','1338','2024'])` is `['1337','1338','2024']`
    while `SEED_LADDER[:3]` is `(1337, 2024, 1338)`, so a sorted-list comparison against a ladder
    prefix is FALSE for the very state this rule exists to admit. MEASURED; it is in
    `<measured_state>`.
  * `CONTINUATION_SCOPE` — the five clauses above as a module-level string, so the record carries
    them verbatim rather than a paraphrase (`prove_control_record_declares_visibility`'s reason: a
    paraphrase is a second source for one statement, free to soften it).
  * `prove_killed_run_continuation(*, tracked, ladder, trained_seeds, scored_seeds,
    committed_scored_seeds, record_exists)` -> `True`. Keyword-only, so a caller cannot transpose
    `trained` with `scored`. **SEVEN NAMED CONJUNCTS — and the second is SPLIT IN TWO (`2a`/`2b`),
    so there are EIGHT `_prove` calls, each carrying its OWN message.** The split is FORCED by
    `<behavior>`, which demands two DISTINCT messages: `len(scored) == len(ladder)` must name the
    WRITE-ORDERING, while `scored == set()` must route the reader to `prove_first_attempt`'s
    business. One `_prove` over `0 < len(scored) < len(ladder)` cannot carry both, and conjunct 2's
    message is load-bearing below. **THE NUMBERING OF CONJUNCTS 3–7 IS UNCHANGED**, so every other
    reference in this plan (`conjuncts 3 and 4 INDEX the ladder`, `conjunct 5`, `conjuncts 6 and 7`)
    still points where it says. In this order: `record_exists is False`; **`2a`** `len(scored) > 0`;
    **`2b`** `len(scored) < len(ladder)`; `scored == set(ladder[:len(scored)])`;
    `scored <= trained` and `trained - scored` is empty or exactly `{ladder[len(scored)]}`;
    `scored == committed_scored`; no tracked path is `MATCHED_CONTROL_RECORD` or
    `MATCHED_VERDICT_RECORD`; every tracked path equals
    **`f"results/{MATCHED_ARM_PREFIX}_{matched_arm(s)}/run.csv"`** for some `s` in `trained | {next}`.
    Write that f-string EXACTLY — MEASURED, for `s=1337` it renders
    `results/phase23_matched_control_seed1337/run.csv`, which is what `git ls-files` returns.
    Dropping `MATCHED_ARM_PREFIX` yields `results/matched_control_seed1337/run.csv` and matches
    nothing, so conjunct 7 would refuse every real state.

    **TWO messages are load-bearing and must be written as messages, not as plan prose, because they
    are what a future reader will read instead of this plan:**

    - **Conjunct 2b** (`len(scored) < len(ladder)`) carries the write-ordering argument IN FULL.
      **Conjunct 2a** (`len(scored) > 0`) carries the OTHER message, which is why the split exists:
      an empty scored set is a FIRST attempt and `phase23_matched_prereg.prove_first_attempt`'s
      business, not this rule's.
    - **Conjunct 5 carries its own remedy.** When the working tree's scored set and HEAD's disagree,
      the message must state: *this is what a hand-edited state file looks like, AND it is also what
      a SECOND KILL looks like before its state is committed. If a seed has just scored and the
      state file is not yet committed, the fix is NOT a flag and NOT a second narrowing — it is to
      `git add data/phase23_run_state.json results/<the newly completed seed dir>` and commit, then
      re-launch. Committing earlier makes the residual MORE auditable, not less.* Print both sets in
      the message so the reader can see which seed is missing from history.
  * `if __name__ == "__main__":` self-check in `phase23_matched_prereg`'s exact register — a
    `_refused(fn, **kwargs)` helper, every refusal watched firing against a CONSTRUCTED input, the
    admitted case printed, and `_prove` rather than `assert` throughout. **CONSTRUCTED INPUTS ONLY —
    the self-check OPENS NO FILE**, and in particular never `open("data/phase23_run_state.json")`.
    The module has no `subprocess`, so it cannot reach the pinned blob either; and `<verification>`
    re-runs `.venv/bin/python scripts/phase23_resume_prereg.py` AFTER Task 3 has written five scored
    seeds into that file, so a self-check that read it would refuse then. (If nothing opens a file,
    `json` may turn out unused — let `ruff` decide rather than importing it defensively.)

**NO float literal anywhere in the module** (the AST tripwire below enforces it). There is no
quantity here that is not a seed, a count or a boolean.

Then `tests/test_phase23_resume_prereg.py`, CPU-only, GPU-free, no torch.

**THE FIXTURE IS PINNED, AND THE PIN IS THE POINT — THIS FILE READS NO LIVE STATE.** Every state it
derives comes from **`git show d99d2aa:data/phase23_run_state.json`** through `seed_status` — never
typed, never read from the working tree. MEASURED at HEAD: that blob carries exactly
`scored = ['1337', '1338', '2024']`, and `git merge-base --is-ancestor d99d2aa HEAD` exits `0`, so it
is reachable forever and its bytes cannot change. `d99d2aa` is NOT a new pin — it is already this
plan's most load-bearing SHA, and this adds a new USE of it, not a second source for a fact:
`<measured_state>` records it as the earliest add of `results/phase23_matched_*`; Task 2 Step A
recovers seed 2025's deleted `run.csv` from it; `CONTINUATION_SCOPE`'s clause (5) names it as the
`adds[-1]` that makes this module unfreezable; and **FOUR** `git diff --exit-code d99d2aa HEAD --`
guards cite it, IDENTIFIED BY WHAT EACH CHECKS rather than counted or line-numbered:
(i) the three scored seed dirs + the state file, in Task 2; (ii) `mitigation_gate.py` +
`mitigation_accountant.py`, in Task 2; (iii) `phase23_sigma_zero.json` +
`phase23_control_floor.json`, in Task 3; (iv) `mitigation_gate.py` +
`mitigation_accountant.py` again, in `<verification>`.
Do NOT verify this with `grep -c` over the guard form, and do NOT cite line numbers: the pattern
appears in THIS PARAGRAPH too, so the count exceeds `4`, and both the count and any line number
move whenever this prose is edited. Counting a guard form with a grep whose own prose matches it
is the false-count defect this plan refuses twice elsewhere (the float-constant AST tripwire, and
the anchored `grep -c '^- \[x\] 23-20-PLAN.md'`). The four descriptions above are stable under
editing, which is what makes them checkable.

*Why a pin and not the live file — MEASURED, and the cost of getting it wrong lands after the GPU
run.* **Task 3 writes FIVE scored seeds into `data/phase23_run_state.json` and commits it** (it is
force-tracked, so CI sees it too). A fixture read from the live file therefore re-derives a DIFFERENT
admitted case on every `pytest -q` from Task 3 onward and hits this module's own
`len(scored) == len(ladder)` refusal — reddening Task 3's own `<automated>` and its "full suite green"
criterion, ~43 GPU-minutes too late to be cheap. The refusing case has the mirror failure:
`[s for s in SEED_LADDER if str(s) not in section]` is `[]` after Task 3, so it cannot be constructed
at all. The pin makes both time-invariant. **A vacuity branch is NOT an acceptable substitute here** —
it would make this file silently stop testing at exactly the moment the run it guards has happened.

**AND `tracked` IS CONSTRUCTED, NOT LISTED — it is the UN-PINNED SIBLING of the state fixture and
it fails the same way, at the same cost.** Every `tracked` list this test file passes is BUILT from
conjunct 7's own formula — `f"results/{MATCHED_ARM_PREFIX}_{matched_arm(s)}/run.csv"` over the seeds
the case names — **NEVER from a live `git ls-files 'results/phase23_matched_*'`.** MEASURED: a live
glob is GREEN here (4 tracked) and still GREEN after Task 2's Step A (3 tracked), then goes **RED at
Task 3**, because once the record is written and committed that glob returns SIX entries INCLUDING
`results/phase23_matched_control.json` — and conjunct 6 refuses precisely that path. Identical cost
profile to the state fixture: discovered after the ~43 GPU-minutes. The criterion below and Task 3's
pre-launch check both assert the glob string is NOT a constant of this file.

*The two git refs are DIFFERENT ON PURPOSE — do not unify them.* Production conjunct 5 reads
`git show HEAD:data/phase23_run_state.json` (Task 2, Step B) and MUST stay `HEAD:`: its whole job is
"the working tree agrees with history **now**", a deliberately time-VARYING property. The test
fixture reads `d99d2aa:` and MUST stay pinned: its job is a fixed state to exercise the predicate
against. Collapsing either into the other breaks the one it was moved to.

One test per bullet in `<behavior>`, plus:

  * `test_the_continuation_predicate_is_invariant_under_arbitrary_readings` — **THE INDEPENDENCE
    PROOF, AND IT PERTURBS THE WHOLE BLOCK.** Load the PINNED state
    (`git show d99d2aa:data/phase23_run_state.json`, per "THE FIXTURE IS PINNED" above — **not** the
    working-tree file) and build three variants in which
    EVERY seed block is rebuilt as `{k: <arbitrary> for k in block}` — every value replaced, KEY
    PRESENCE ALONE preserved, so `heldout_on`, `final_train_loss` and `training_seconds` are
    perturbed exactly as `primary` is. Three arbitrary fillers: all-zero, a large sentinel, and a
    scrambled/`None` mix. Then, in all four states (pinned + three variants):
      1. derive `(trained, scored)` through `seed_status` and assert the derived arguments are
         IDENTICAL across all four;
      2. assert `prove_killed_run_continuation(...)` returns `True` in all four;
      3. **derive the refusing case FROM the substituted copy itself** — take each of the four
         states and add a block for EVERY ladder seed the section is missing, computed as
         `[s for s in SEED_LADDER if str(s) not in section]`, so `len(scored) == len(ladder)`; then
         call the predicate and assert `str(SystemExit)` is BIT-IDENTICAL across all four. (A
         refusing case constructed independently of the substitution would not be put through the
         substitution at all, and would prove nothing about it.) **COMPUTE the missing list; never
         write "the two missing seeds".** Against the pinned blob it is exactly `[2025, 1339]`, but a
         hardcoded pair is a second source for a fact the section already carries, and not having two
         sources for one fact is this plan's whole subject. Assert the computed list is NON-EMPTY
         first, so this test fails LOUDLY rather than passing vacuously if the fixture source is ever
         repointed at a five-scored state.
    Message: a predicate whose verdict or whose refusal text moves with a reading is a predicate
    narrowed by one.
  * `test_the_module_declares_no_float_constant` — **AST, not grep.** `ast.walk` the module, skip
    the docstring `Expr(Constant(str))` of Module/FunctionDef/ClassDef, and assert no remaining
    `ast.Constant` has a `float` value. The docstring discusses `0.7837301587301587` by name, so a
    grep over this file goes false-RED against its own prose — this repository has shipped exactly
    that defect.
  * `test_the_frozen_pin_is_byte_identical_to_c100388` — `git diff --exit-code c100388 HEAD --
    scripts/phase23_matched_prereg.py` exits 0 AND `git log --format=%H -- <pin>` returns exactly
    one line. **This is the check that proves the chosen route left the frozen file alone**, and it
    runs on every suite run from here on, not once at plan time.

  **NOT in this task:** `test_the_resume_pin_has_exactly_one_commit`. It asserts
  `git log --format=%H -- scripts/phase23_resume_prereg.py` returns exactly ONE line, and at this
  task's verify step the file is not yet committed, so it returns ZERO and the test would fail on
  its own creating run. It is added in **Task 2**, after Task 1's commit exists.

**FORBIDDEN in this task:** touching `scripts/phase23_matched_prereg.py`,
`scripts/phase23_prereg.py`, `scripts/mitigation_*.py`, `scripts/teach_persona.py`,
`src/personacore/training/loop.py`, or any `results/*.json`. No production wiring — the call site is
Task 2. No GPU second.

Commit: `feat(23-20): pre-register the killed run's continuation, beside the frozen pin`.
  </action>

  <verify>
    <automated>.venv/bin/python scripts/phase23_resume_prereg.py && .venv/bin/python -m pytest tests/test_phase23_resume_prereg.py tests/test_phase23_matched_prereg.py -q && .venv/bin/ruff check . && .venv/bin/ruff format --check .</automated>
  </verify>

  <acceptance_criteria>
    - `.venv/bin/python scripts/phase23_resume_prereg.py` exits 0 and PRINTS every refusal firing
      (one line per watched refusal, plus the admitted case).
    - `.venv/bin/python -m pytest tests/test_phase23_resume_prereg.py -q` passes; the file contains
      at least one test per bullet in `<behavior>`.
    - `.venv/bin/python -m pytest tests/test_phase23_matched_prereg.py -q` passes — the ancestry
      guard is still green at 4 tracked artifacts.
    - `git diff --exit-code c100388 HEAD -- scripts/phase23_matched_prereg.py` exits `0`.
    - `git log --format=%H -- scripts/phase23_matched_prereg.py | wc -l | tr -d ' '` prints `1`.
    - `git diff --exit-code c7de5d4 HEAD -- scripts/phase23_prereg.py` exits `0`.
    - The per-seed path formula in conjunct 7 renders a REAL tracked path:
      `.venv/bin/python -c "import sys;sys.path.insert(0,'scripts');import phase23_matched_prereg as mp;print(f'results/{mp.MATCHED_ARM_PREFIX}_{mp.matched_arm(1337)}/run.csv')"`
      prints `results/phase23_matched_control_seed1337/run.csv`, and that string is in
      `git ls-files 'results/phase23_matched_*'`.
    - AST, not grep — the module declares no float constant:
      `.venv/bin/python -c "import ast,pathlib;t=ast.parse(pathlib.Path('scripts/phase23_resume_prereg.py').read_text());d={n for p in ast.walk(t) if isinstance(p,(ast.Module,ast.FunctionDef,ast.ClassDef)) and p.body and isinstance(p.body[0],ast.Expr) and isinstance(p.body[0].value,ast.Constant) for n in [p.body[0].value]};print([n.value for n in ast.walk(t) if isinstance(n,ast.Constant) and n not in d and isinstance(n.value,float)])"`
      prints `[]`.
    - AST, not grep — the predicate's signature carries only the six declared names:
      `.venv/bin/python -c "import ast,pathlib;t=ast.parse(pathlib.Path('scripts/phase23_resume_prereg.py').read_text());f=[n for n in ast.walk(t) if isinstance(n,ast.FunctionDef) and n.name=='prove_killed_run_continuation'][0];print(sorted(a.arg for a in f.args.kwonlyargs), f.args.args, f.args.posonlyargs)"`
      prints
      `['committed_scored_seeds', 'ladder', 'record_exists', 'scored_seeds', 'tracked', 'trained_seeds'] [] []`.
    - AST, not grep — the module runs no subprocess:
      `.venv/bin/python -c "import ast,pathlib;t=ast.parse(pathlib.Path('scripts/phase23_resume_prereg.py').read_text());print(sorted({a.name.split('.')[0] for n in ast.walk(t) if isinstance(n,ast.Import) for a in n.names} | {n.module.split('.')[0] for n in ast.walk(t) if isinstance(n,ast.ImportFrom) and n.module}))"`
      does not contain `subprocess`, `torch`, `requests` or `urllib`.
    - `CONTINUATION_SCOPE` carries FIVE clauses and the fifth names this module's OWN missing freeze:
      `.venv/bin/python -c "import sys;sys.path.insert(0,'scripts');import phase23_resume_prereg as rp;s=rp.CONTINUATION_SCOPE.upper();print(all(c in s for c in ['(1)','(2)','(3)','(4)','(5)']), 'NOT FROZEN' in s, 'DETECTION' in s)"`
      prints `True True True`.
    - **The test fixture is PINNED, not live — AST, not grep** (the file's own docstrings name both
      paths, so `grep -c` over it would go false-RED against its own prose; an exact-equality
      constant check cannot, because a docstring MENTIONING a path is a longer string and is never
      EQUAL to it):
      `.venv/bin/python -c "import ast,pathlib;t=ast.parse(pathlib.Path('tests/test_phase23_resume_prereg.py').read_text());c=[n.value for n in ast.walk(t) if isinstance(n,ast.Constant) and isinstance(n.value,str)];print('d99d2aa:data/phase23_run_state.json' in c, 'data/phase23_run_state.json' in c, 'results/phase23_matched_*' in c)"`
      prints `True False False` — the pinned blob is opened, the live working-tree file is not,
      and `tracked` is not sourced from a live glob either (W-1: a live glob returns SIX entries
      after Task 3, including the record, and conjunct 6 refuses it).
    - The module never reads the live state file, so `<verification>` can re-run its self-check
      AFTER Task 3 has written five scored seeds into it — **AST exact-constant, which catches
      `open`, `pathlib.read_text` and every other mechanism at once, and cannot go false-RED against
      the docstring that names the path** (a docstring MENTIONING it is a longer string, never EQUAL
      to it):
      `.venv/bin/python -c "import ast,pathlib;t=ast.parse(pathlib.Path('scripts/phase23_resume_prereg.py').read_text());print([n.value for n in ast.walk(t) if isinstance(n,ast.Constant) and n.value=='data/phase23_run_state.json'])"`
      prints `[]`.
    - `git status --porcelain results/ data/` is empty — this task writes no artifact.
    - `.venv/bin/ruff check . && .venv/bin/ruff format --check .` clean.
    - Full suite: `.venv/bin/python -m pytest -q` reports `1518 + <new tests> passed, 1 skipped`,
      with the arithmetic stated explicitly in the summary. No pre-existing test changes status.
  </acceptance_criteria>

  <done>
`scripts/phase23_resume_prereg.py` and `tests/test_phase23_resume_prereg.py` are committed; every
refusal has been watched firing; the whole-block readings-invariance test passes; the fifth scope
clause discloses that this module is not frozen and that its one-commit guard is detection rather
than prevention; both frozen pins are byte-identical and the matched pin still has exactly one
commit; nothing under `results/` or `data/` moved.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the continuation, discard seed 2025's partial bytes, make the record disclose it</name>

  <files>scripts/phase23_run.py, tests/test_phase23_matched.py, tests/test_phase23_resume_prereg.py, results/phase23_matched_control_seed2025/run.csv (DELETED), checkpoints/phase23_matched_control_seed2025_latest.pt (DELETED)</files>

  <read_first>
    - `scripts/phase23_run.py:2121-2250` — `matched()`'s opening: both existing refusals, the
      inherited-seed-set proof, and the per-seed loop.
    - `scripts/phase23_run.py:2253-2400` — the record dict and its four pre-write proofs.
    - `scripts/phase23_run.py:1132-1200` — `prove_matched_protocol`, **especially :1181-1187**, the
      `resume_from` refusal. This is why seed 2025 cannot be mid-train resumed.
    - `scripts/phase23_run.py:1248-1280` — `train_matched_control`'s opening `refuse_if_exists`.
    - `scripts/teach_persona.py:370-400` — `refuse_if_exists` and D-07's `expected=` inversion.
    - `tests/test_phase23_matched.py:483-521` — the two assertions that go RED on a continuation,
      and the `if record is None: return` vacuity branch at `:492` that guards them.
    - `tests/test_phase23_matched.py:549-603` — the AST gate forbidding `max`/`min` inside `matched`.
  </read_first>

  <behavior>
    - With the record absent and the state empty, `matched()` still reaches
      `mp.prove_first_attempt(matched_glob_at_start)` UNCHANGED and refuses a tracked artifact
      exactly as `tests/test_phase23_matched.py::test_a_second_matched_attempt_is_refused` requires.
    - **With a tracked artifact AND an empty state, the frozen rule is REACHED and FIRES.** The
      branch predicate is `not scored` ALONE, so `prove_first_attempt` receives the real, non-empty
      `git ls-files` result and raises its own `ONE ATTEMPT — REFUSED`. It is never called with an
      argument that control flow has already proved empty.
    - With HEAD's real state, `matched()` reaches `rp.prove_killed_run_continuation` and is ADMITTED.
    - With a fabricated five-scored state and the record absent, `matched()` REFUSES.
    - Both branches are watched: a test constructs each and observes which named rule ran.
    - `train_matched_control(2025)` no longer trips `refuse_if_exists`, because the partial bytes are
      gone — not because the refusal was weakened.
  </behavior>

  <action>
**Step A — discard seed 2025's partial training bytes, in its OWN reviewed commit, FIRST.**

`git rm results/phase23_matched_control_seed2025/run.csv` and
`rm checkpoints/phase23_matched_control_seed2025_latest.pt`. Nothing else.

**Why this is not a laundering of the one-attempt rule, in four parts — put all four in the commit
message body, because the commit is where a future auditor will look:**

1. **Seed 2025 produced NO READING.** It has no `primary` block, no entry in
   `data/phase23_run_state.json`'s `matched` section at all, and no adapter. `matched()`'s own
   comment at `:2156-2158` states the governing distinction verbatim: *"THE PREDICATE IS 'SCORED',
   NOT 'TRAINED' … a training leg killed mid-run and resumed is LEGITIMATE and must stay
   legitimate. Only a seed that already produced a READING is evidence of a completed prior
   attempt."* What is being deleted is a 5-row training curve and a step-~50 optimizer state:
   evidence of WHERE THE KILL LANDED, not a measurement.
2. **A mid-train resume is REFUSED BY A COMMITTED MECHANISM, so discarding is the only admissible
   completion.** `prove_matched_protocol` refuses `resume_from` in the comparator's kwargs
   (`:1181-1187`), and `phase23_matched_prereg.DP_FN_BRANCH_DISPOSITIONS` items 6 and 7 are
   dispositioned `unreached` PRECISELY because this scheduling resumes nothing. Passing
   `resume_from` would reach two branches the BLIND ledger declared unreachable — a protocol change,
   in the one place this phase cannot make one. D-07's `refuse_if_exists(expected=...)` inversion
   exists and is deliberately NOT used here for that reason. Retraining seed 2025 from scratch under
   `seed_everything(2025)` on the same pinned corpus is the protocol the pin declares.
3. **The deletion is a VISIBLE DIFF, which is the whole point.** `run.csv` is tracked as of
   `d99d2aa`, so this removal appears in `git log --diff-filter=D`. Recovery is
   `git show d99d2aa:results/phase23_matched_control_seed2025/run.csv` — **name that command in the
   commit message.** The `latest.pt` is gitignored (`.gitignore:14`) and is NOT recoverable from
   git; it is deterministically regenerable from `seed_everything(2025)` plus the pinned corpus, and
   this plan and `23-17-SUMMARY.md:162` are its record. Say both halves; a recovery claim that omits
   the unrecoverable half is the overclaim this phase exists to refuse.
4. **NOTHING SCORED IS TOUCHED.** The three adapters, the three state blocks and the three
   `run.csv` files for seeds 1337/2024/1338 are byte-unchanged. Prove it in the same step:
   `git diff --exit-code HEAD~1 -- results/phase23_matched_control_seed{1337,2024,1338} data/phase23_run_state.json`.

Then, before going further, run `.venv/bin/python -m pytest tests/test_phase23_matched_prereg.py -q`
— the ancestry guard now sees THREE tracked artifacts and must stay green (`bool(checked) ==
bool(tracked)`); `adds[-1]` keeps the earliest add at `d99d2aa` when the file returns.

Commit: `revert(23-20): discard seed 2025's partial training bytes — no reading, no adapter`.

**Step B — wire the call site in `matched()` (`scripts/phase23_run.py`, ~:2134-2175).**

Import the new module beside `mp` (`import phase23_resume_prereg as rp`). Then:

- Compute `matched_glob_at_start` exactly as today (the `git ls-files` subprocess is unchanged).
- Compute `prior = _state_load().get("matched", {})` and `prior_scored_seeds_at_start` exactly as
  today — **the recorded field keeps its current shape and its current name.**
- Derive `trained`/`scored` as int sets through `rp.seed_status(prior)`.
- Read the COMMITTED scored set with a second `subprocess.run(["git","show",
  "HEAD:data/phase23_run_state.json"], ...)`, parse it, and pass it through the SAME
  `rp.seed_status`. **No SHA literal** — `data/phase23_run_state.json` is tracked, so `HEAD:`
  always resolves, and the property wanted is *the scored set is what history records, not a
  working-tree edit*.
- Branch on **`not scored` ALONE**:
  * `if not scored:` → `mp.prove_first_attempt(matched_glob_at_start)`, `attempt = "first"`,
    `continuation_rule = None`.
  * `else:` → `rp.prove_killed_run_continuation(tracked=matched_glob_at_start, ladder=SEED_LADDER,
    trained_seeds=trained, scored_seeds=scored, committed_scored_seeds=committed,
    record_exists=(_ROOT / mp.MATCHED_CONTROL_RECORD).exists())`, `attempt = "continuation"`.

    **Write `record_exists` EXACTLY as `(_ROOT / mp.MATCHED_CONTROL_RECORD).exists()` — NOT
    `path.exists()`.** MEASURED: `path` is first bound at `scripts/phase23_run.py:2180`, which is
    AFTER the `:2134-2175` gate this step rewires, so `path.exists()` here is an unbound name and
    raises `NameError` — on the CONTINUATION branch only, i.e. precisely the branch no CPU test of
    the first-attempt path would catch, and precisely the branch the real run takes. `:2171` already
    models the correct form INSIDE this same gate; copy that, do not invent a second one. Do NOT
    "fix" this by hoisting `path`'s binding above the gate: everything from `:2178` onward is
    UNCHANGED (below), and moving the binding moves the record-absent `_prove` with it.

  **DO NOT write `if not matched_glob_at_start and not scored:`.** That predicate can only ever
  call the frozen rule with an argument control flow has already proved empty, which is FILTERING
  BY CONTROL FLOW and makes `prove_first_attempt`'s refusal UNREACHABLE. `not scored` alone is one
  token shorter and strictly stronger: a tracked artifact sitting beside an empty state reaches the
  frozen rule with the real non-empty list and fires its own `ONE ATTEMPT — REFUSED`. This is what
  makes `must_haves.truths[1]`'s "not passed a filtered input" TRUE rather than asserted.

- Keep the existing `prior_scored_seeds_at_start` `_prove` (`:2170-2175`) **verbatim, byte-for-byte,
  and reachable on the first-attempt branch.** It is not deleted, not edited and not made conditional
  on a force flag.

  **AND STATE WHAT IT NOW IS, AT TRUE STRENGTH: it is RETAINED BUT UNFIREABLE on this branch, and
  writing "not softened" without that is the overclaim this plan's own register exists to refuse.**
  MEASURED: `rp.seed_status`'s `scored` and `prior_scored_seeds_at_start`
  (`scripts/phase23_run.py:2169`) are the SAME predicate — `"primary" in block` — over the SAME dict,
  `prior`. So `not scored` implies `not prior_scored_seeds_at_start`, and on the `if not scored:`
  branch the `_prove`'s FIRST DISJUNCT is tautologically true: it can never refuse. Record it in the
  code comment and in the summary as *"retained as a reader-visible marker of the rule that governed
  before the split, not as a live refusal — on this branch its first disjunct is tautologically true,
  because `not scored` and `not prior_scored_seeds_at_start` are the same predicate over the same
  dict."* The refusal it used to perform on a 3-scored state is now performed by
  `prove_killed_run_continuation`'s seven NAMED conjuncts, which is why the split exists; nothing is lost,
  and claiming it still fires would be claiming a guard the code does not have.

  **STATE THE RELATIONSHIP AT TRUE STRENGTH, IN THE CODE COMMENT AND IN THE SUMMARY — the new rule
  is NOT "strictly more demanding".** On the ONE state that matters (3 scored, record absent) the
  old `_prove` REFUSES and the new predicate ADMITS, so in OUTCOME it is strictly more PERMISSIVE:
  that is the entire point of it existing. What is stronger is its SHAPE — seven NAMED conjuncts against
  one, plus committed-vs-working-tree agreement, ladder-prefix shape, and tracked-path shape, none
  of which the old `_prove` checked at all. Say both halves. "Superseded by a stricter rule" is the
  overclaim; "superseded on this branch by a differently-shaped rule that admits this state and
  refuses more shapes of every other" is the true sentence.

- Everything from `census = prove_matched_protocol()` (`:2178`) onward is UNCHANGED, including the
  record-absent `_prove` and the inherited-seed-set proof against `state["cost"]["seeds"]`.

**HARD CONSTRAINT ON THE EDIT:** `tests/test_phase23_matched.py:558` AST-walks `matched` and fails
on ANY `max`/`min` `Call` node and on any `BinOp(Sub)` between two spreads. Use `len(...)`, set
comparisons and `sorted(...)`; **never `max`/`min` inside `matched`.** MEASURED today: zero such
nodes. Re-measure after the edit.

**Step C — the record discloses the continuation.** Add five keys to the record dict, beside the
four attempt-state keys that stay UNEDITED (`matched_glob_at_start`, `prior_scored_seeds_at_start`,
`one_attempt_rule`, `one_attempt_scope`):

- `"attempt"`: `"first"` or `"continuation"`
- `"continuation_rule"`: `"phase23_resume_prereg.prove_killed_run_continuation"` or `None`
- `"continuation_fingerprint"`: **the exact inputs the predicate was given, ALL SIX of them** —
  `tracked`, `trained_seeds`, `scored_seeds`, `committed_scored_seeds`, `ladder`, `record_exists`.

  **`ladder` IS RECORDED IN LADDER ORDER — `list(SEED_LADDER)` — AND IS NEVER SORTED.** MEASURED:
  `SEED_LADDER` is `(1337, 2024, 1338, 2025, 1339)`, which is NOT sorted, and
  `sorted(SEED_LADDER)` is `[1337, 1338, 1339, 2024, 2025]`. So a sorted `ladder` makes
  `set(ladder[:3])` read `{1337, 1338, 1339}` while the real scored set is `{1337, 2024, 1338}` —
  **conjunct 3 REFUSES** — and makes conjunct 4's `ladder[len(scored)]` read `2024`, a seed already
  scored. That breaks BOTH re-admissions that feed the recorded list straight back in as
  `ladder=tuple(f["ladder"])`: Step D's suite-run re-admission below, and Task 3's acceptance
  criterion that must print `True`. **`ladder` is the ONLY order-sensitive input** — conjuncts 3 and
  4 INDEX it. The three seed sets and `tracked` MAY be written as sorted lists (of `int` for the seed
  sets, of `str` for `tracked`) because every conjunct reads them as sets or as per-element
  membership; `record_exists` is a bool.

  **`tracked` is not optional.** Omitting it makes conjuncts 6 and 7 VACUOUS on every re-admission, because a
  re-admission that passes `tracked=[]` satisfies "no tracked path is the record" and "every tracked
  path is a per-seed run.csv" trivially — two of the seven NAMED conjuncts silently untested forever. A rule
  whose inputs go unrecorded is a rule taken on trust; this is the same discipline
  `matched_glob_at_start` already applies.
- `"continuation_scope"`: `rp.CONTINUATION_SCOPE` VERBATIM (never a paraphrase)
- `"continuation_discrimination"`: one sentence naming the write-ordering —
  `results/phase23_matched_control.json` is written as `matched()`'s LAST act, after every ladder
  seed has scored, so a completed attempt necessarily leaves five scored seeds and fewer than five
  is a state the completion path cannot produce.

**Step D — widen the two assertions that go RED, and only those two.**
`tests/test_phase23_matched.py::test_matched_record_records_the_attempt_state` currently asserts
`record["matched_glob_at_start"] == []` (:494) and `record["prior_scored_seeds_at_start"] == []`
(:498), under the `if record is None: return` vacuity branch at `:492`. On a continuation both are
non-empty and both would go RED. **Do not delete them — make them conditional on
`record["attempt"]`, and give the continuation branch assertions that are at least as hard:**

- `attempt == "first"` → both `== []`, exactly as today.
- `attempt == "continuation"` → `continuation_rule ==
  "phase23_resume_prereg.prove_killed_run_continuation"`; `continuation_scope ==
  rp.CONTINUATION_SCOPE` verbatim; `continuation_fingerprint["record_exists"] is False`;
  `set(continuation_fingerprint["scored_seeds"]) == set(continuation_fingerprint["committed_scored_seeds"])`;
  `len(fingerprint["scored_seeds"]) < len(phase23_run.SEED_LADDER)`; and
  `rp.prove_killed_run_continuation(...)` RE-ADMITS the recorded fingerprint **passing
  `tracked=fingerprint["tracked"]`, NOT `tracked=[]`** — so conjuncts 6 and 7 are exercised on every
  suite run and a hand-edited fingerprint goes red on any of the seven NAMED conjuncts, not on five of seven.
  **Convert the three seed lists back to `set(...)` and `ladder` back to `tuple(...)` WITHOUT
  sorting** — the ladder is the one order-sensitive input (conjuncts 3 and 4 INDEX it), so
  `tuple(sorted(f["ladder"]))` would make this assertion refuse. The canonical call is spelled out
  verbatim in Task 3's acceptance criteria; copy it rather than re-deriving it here.
- `record["one_attempt_rule"] == "phase23_matched_prereg.prove_first_attempt"` (:502) and the whole
  `one_attempt_scope` clause block (:504-521) stay **UNTOUCHED**. The frozen rule still governs; the
  continuation is an exception that arrived beside it, not a replacement for it.

Add one test that watches BOTH branches choose their rule against constructed state, so the branch
is a mechanism and not a comment. **Include the B4 case explicitly:** a constructed state with a
NON-EMPTY tracked glob and an EMPTY scored set must reach `prove_first_attempt` and raise
`SystemExit` whose message contains `ONE ATTEMPT — REFUSED`. Under the old
`not matched_glob_at_start and not scored` predicate that case was unreachable; this test is what
keeps it reachable.

**Also add, to `tests/test_phase23_resume_prereg.py`, the guard Task 1 could not run:**
`test_the_resume_pin_has_exactly_one_commit` — `git log --format=%H --
scripts/phase23_resume_prereg.py` returns exactly ONE line. It could not live in Task 1 (the file
was uncommitted at that task's verify step, so the count was 0); from here on it is green and is the
weaker-than-ancestry detection the module's fifth scope clause discloses. **Its failure message must
say what it is not:** this is detection after the fact, not the ancestry guarantee
`tests/test_phase23_matched_prereg.py` gives the frozen pin, because this module cannot be
registered against `MATCHED_ARTIFACT_GLOB` (`adds[-1]` is `d99d2aa`, which precedes it).

**DO NOT invoke `scripts/phase23_run.py matched` IN THIS TASK — TASK 3 OWNS THE RUN.** There is no
"just the gate" invocation of it: as the acceptance criterion below measures, every pre-loop proof
PASSES at HEAD and the very next thing `matched()` does is train seed 2025 on MPS. Check the
continuation with the GATE-ONLY command in `<acceptance_criteria>`, which calls
`prove_killed_run_continuation` and `prove_matched_protocol` directly and enters no loop.

Leave the record ABSENT at the end of this task. The suite must be fully green with all record
guards on their vacuity branch — everything provable on CPU is proved BEFORE any GPU second, which
is this phase's own register (23-16, 23-17).

Commit: `feat(23-20): admit the killed run's continuation at the call site, and record it`.
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase23_matched.py tests/test_phase23_resume_prereg.py tests/test_phase23_matched_prereg.py -q && .venv/bin/python -m pytest -q && .venv/bin/ruff check . && .venv/bin/ruff format --check .</automated>
  </verify>

  <acceptance_criteria>
    - Seed 2025's partial bytes are gone and the deletion is VISIBLE:
      `git log --diff-filter=D --name-only -1 --format= | grep -c phase23_matched_control_seed2025` is `1`,
      and `git show d99d2aa:results/phase23_matched_control_seed2025/run.csv | wc -l | tr -d ' '` still prints `6`
      (the recovery path named in the commit message actually works).
    - Nothing scored moved:
      `git diff --exit-code d99d2aa HEAD -- results/phase23_matched_control_seed1337 results/phase23_matched_control_seed2024 results/phase23_matched_control_seed1338 data/phase23_run_state.json`
      exits `0`.
    - The ancestry guard survives at 3 tracked artifacts:
      `.venv/bin/python -m pytest tests/test_phase23_matched_prereg.py -q` passes.
    - **Both branches are reachable and named** — AST, not grep (the file's prose names both
      functions): `.venv/bin/python -c "import ast,pathlib;t=ast.parse(pathlib.Path('scripts/phase23_run.py').read_text());m=[n for n in ast.walk(t) if isinstance(n,ast.FunctionDef) and n.name=='matched'][0];print(sorted({n.func.attr for n in ast.walk(m) if isinstance(n,ast.Call) and isinstance(n.func,ast.Attribute)} & {'prove_first_attempt','prove_killed_run_continuation'}))"`
      prints `['prove_first_attempt', 'prove_killed_run_continuation']`.
    - **The frozen rule's refusal is REACHABLE** — the branch predicate does not filter its argument.
      AST, not grep: the `If` test that guards the `prove_first_attempt` call is a single
      `UnaryOp(Not)` over the scored name, NOT a `BoolOp(And)`:
      `.venv/bin/python -c "
import ast,pathlib
t=ast.parse(pathlib.Path('scripts/phase23_run.py').read_text())
m=[n for n in ast.walk(t) if isinstance(n,ast.FunctionDef) and n.name=='matched'][0]
g=[n for n in ast.walk(m) if isinstance(n,ast.If) and any(isinstance(c,ast.Call) and isinstance(c.func,ast.Attribute) and c.func.attr=='prove_first_attempt' for c in ast.walk(n))][0]
print(type(g.test).__name__)"`
      prints `UnaryOp` — **not** `BoolOp`. And the constructed test case (non-empty tracked, empty
      scored) raises `SystemExit` containing `ONE ATTEMPT — REFUSED`, watched firing.
    - **No spread is typed in the writer** — the committed AST gate still passes:
      `.venv/bin/python -m pytest tests/test_phase23_matched.py::test_the_matched_writer_does_not_inline_the_reduction -q` passes.
    - **The continuation is ADMITTED against HEAD's real state, at zero GPU cost — BY CALLING THE
      GATE FUNCTIONS DIRECTLY, NEVER `matched()`.** MEASURED against live code: **`matched()` has NO
      separable gate.** After `prove_matched_protocol()` (`:2178`) every remaining pre-loop proof
      PASSES at HEAD — the record `_prove` (`:2181`, record absent), the `"cost" in state` `_prove`
      (`:2193`), and the inherited-seed-set `_prove` (`:2200`; MEASURED, `state["cost"]["seeds"]` is
      `[1337, 2024, 1338, 2025, 1339]`, equal to `SEED_LADDER`) — so control falls STRAIGHT INTO the
      `for seed in seeds:` loop at `:2208`. Seeds 1337/2024/1338 are skipped (`_already_trained` and
      `"primary" in block`), and then **seed 2025 reaches `train_matched_control(2025)` and FIRES
      MPS**: it has no state block at all, and Step A removed its `latest.pt`, so `refuse_if_exists`
      does not refuse either. A foreground 600 s cap kills that mid-score, seed 2025's adapter and
      state block get written, `results/phase23_matched_control_seed2025/` goes untracked, and this
      task's own `<automated>` goes FALSE-RED at `tests/test_phase23_resume.py:720-728`
      (`git status --porcelain results/ == ""`) — the identical failure `23-17-SUMMARY §Deviation 2`
      records — making this task's `<done>` ("full suite green with the record still absent")
      unreachable. So the check is this GATE-ONLY command, which enters no loop and starts no
      training:
      ```bash
      .venv/bin/python -c "
      import sys,subprocess,json; sys.path.insert(0,'scripts')
      import phase23_run as pr, phase23_matched_prereg as mp, phase23_resume_prereg as rp
      g=lambda a: subprocess.run(['git',*a],capture_output=True,text=True,check=True).stdout
      tracked=g(['ls-files',mp.MATCHED_ARTIFACT_GLOB]).split()
      trained,scored=rp.seed_status(pr._state_load().get('matched',{}))
      _,committed=rp.seed_status(json.loads(g(['show','HEAD:data/phase23_run_state.json'])).get('matched',{}))
      print('ADMITTED', rp.prove_killed_run_continuation(tracked=tracked,ladder=pr.SEED_LADDER,
        trained_seeds=trained,scored_seeds=scored,committed_scored_seeds=committed,
        record_exists=(pr._ROOT/mp.MATCHED_CONTROL_RECORD).exists()))
      print('census', sum(pr.prove_matched_protocol().values()))"
      ```
      prints `ADMITTED True` then `census 7` (MEASURED: `prove_matched_protocol` is the ZERO-COST
      AST preflight, its `Counter` sums to `7`, and it emits its own
      `matched preflight: 7 dp_fn branch(es) …` line first). Record the observed output in the
      summary.
    - **A fabricated five-scored state is REFUSED**, watched firing, message naming the
      write-ordering. Watched via `tests/test_phase23_resume_prereg.py`, not by mutating the live
      state file.
    - The resume pin's one-commit guard is present and green:
      `.venv/bin/python -m pytest tests/test_phase23_resume_prereg.py::test_the_resume_pin_has_exactly_one_commit -q` passes.
    - `git diff --exit-code c100388 HEAD -- scripts/phase23_matched_prereg.py` exits `0` and
      `git log --format=%H -- scripts/phase23_matched_prereg.py | wc -l | tr -d ' '` prints `1`.
    - `git diff --exit-code c7de5d4 HEAD -- scripts/phase23_prereg.py` exits `0`.
    - Frozen files untouched:
      `git diff --exit-code d99d2aa HEAD -- scripts/mitigation_gate.py scripts/mitigation_accountant.py scripts/mitigation_budget.py scripts/teach_persona.py src/personacore/training/loop.py results/phase23_sigma_zero.json results/phase23_control_floor.json`
      exits `0`.
    - D-04 holds: `git ls-files 'results/phase23_noised_*' | wc -l | tr -d ' '` prints `0`.
    - The record is still ABSENT: `test ! -f results/phase23_matched_control.json`.
    - Full suite green, with the arithmetic stated: `.venv/bin/python -m pytest -q` reports
      `1518 + <Task 1 new> + <Task 2 new> passed, 1 skipped`. No pre-existing test changes status
      except the two widened assertions, which are named individually in the summary.
    - `.venv/bin/ruff check . && .venv/bin/ruff format --check .` clean.
  </acceptance_criteria>

  <done>
Seed 2025's partial bytes are discarded in a visible, reviewed commit whose message carries all four
justifications and the working recovery command; `matched()` reaches BOTH one-attempt rules by
branch, the first-attempt branch is selected on `not scored` alone so the frozen refusal is reachable
with an unfiltered argument, and neither rule is weakened; the record dict carries the five
continuation keys and the fingerprint carries all six inputs including `tracked`; the two
would-be-RED assertions are conditional and the continuation branch re-admits with the real tracked
list; the resume pin's one-commit guard is green; the full suite is green with the record still
absent; both pins byte-identical and the matched pin still at one commit.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run seeds 2025 and 1339 DETACHED, write the five-seed record, commit</name>

  <files>results/phase23_matched_control.json, results/phase23_matched_control_seed2025/run.csv, results/phase23_matched_control_seed1339/run.csv, results/phase23_resume_run.log, data/phase23_run_state.json, .planning/STATE.md, .planning/ROADMAP.md</files>

  <read_first>
    - `23-17-SUMMARY.md` — the wall-clock table (per-seed train 154.21/165.79/163.20 s; score
      972.79/990.29/1109.24 s), the ~60-minute kill, and item 4 of `Notes for whoever plans the
      completion`: *"Whatever runs next needs an execution venue that does not terminate a
      100-minute process at 60 minutes — that, not the protocol, is what failed."*
    - `scripts/phase23_run.py:2208-2241` — the per-seed loop, `_already_trained`, and the grad-clip
      non-binding proof that runs BEFORE each seed is scored.
    - `scripts/phase23_run.py:2379-2406` — the four pre-write proofs and the write.
  </read_first>

  <action>
**THE EXECUTION VENUE IS A FIRST-CLASS REQUIREMENT OF THIS TASK, NOT AN ASIDE.** No foreground
command may hold this run. A foreground `Bash` call caps at 600 s and the previous attempt was
terminated externally at ~60 minutes; either would kill it again.

**`setsid(1)` DOES NOT EXIST ON THIS MACHINE — MEASURED. `command -v setsid` exits 1 with no
output; macOS never shipped util-linux's `setsid`.** A `nohup setsid …` line exits **127** and the
run never starts, which is the single thing this task exists to get right. Detachment is done with
`os.setsid()` INSIDE the launched interpreter, which then `os.execv`s the real script.

**Launch and probe in ONE Bash call**, because shell state does not persist between calls and `$!`
would be lost. The call returns in ~2 s — the launched process is already detached, so the harness
does not hold it and `run_in_background` is not needed:

```bash
cd /Users/juliorcoelho/PersonaCore
nohup .venv/bin/python -u -c "
import os, sys
os.setsid()
print('SESSION pid=%d pgid=%d sid=%d' % (os.getpid(), os.getpgid(0), os.getsid(0)), flush=True)
os.execv(sys.executable, [sys.executable, '-u', 'scripts/phase23_run.py', 'matched'])
" > results/phase23_resume_run.log 2>&1 < /dev/null &

# ---- THE DETACHMENT PROBE — BEFORE ANY GPU SECOND IS SPENT ----
# The pid comes from the LOG, never from `$!`. See "DO NOT USE $!" below.
sleep 2
PID=$(.venv/bin/python -c "
import os, pathlib, re
first = pathlib.Path('results/phase23_resume_run.log').read_text().splitlines()[0]
m = re.match(r'SESSION pid=(\d+) pgid=(\d+) sid=(\d+)$', first)
assert m, f'no SESSION line — the launch never started: {first!r}'
p, g, s = map(int, m.groups())
assert p == g == s, f'NOT DETACHED at launch: {first}'
assert (os.getpgid(p), os.getsid(p)) == (p, p), (
    f'NOT DETACHED now: pid={p} pgid={os.getpgid(p)} sid={os.getsid(p)}')
print(p)
") || { echo "PROBE REFUSED — kill any stray process and STOP"; exit 1; }
test -n "$PID" || { echo "PROBE produced no pid — STOP"; exit 1; }
nohup /usr/bin/caffeinate -is -w "$PID" > /dev/null 2>&1 &
echo "DETACHED OK — pid $PID is its own session leader; caffeinate holding"
head -1 results/phase23_resume_run.log
```

**DO NOT USE `$!` FOR THE PID — MEASURED, IT IS THE WRONG PROCESS.** A launch line written as
`cd … && nohup … &` makes bash fork a SUBSHELL for the `&&` list, so `$!` is the SUBSHELL's pid, not
the interpreter's. Probed on this machine: `$!` gave `35240` while the real run was `35242`. Two
failures follow, both silent — the probe checks the wrong pid and **false-FAILS a run that detached
correctly** (and this plan would then tell you to kill it), and `caffeinate -w` waits on a subshell
that exits immediately, so the sleep assertion drops within a second. The log's FIRST LINE is the
process reporting its OWN pid and is authoritative under any nesting; it is also the mechanism later
turns use to recover the pid, so there is ONE source for that fact rather than two. `cd` is its own
statement above for the same reason.

**Why each piece, all MEASURED on this machine, not assumed:**

- `os.setsid()` in a `-c` bootstrap: the harness runs background children WITHOUT job control, so
  the child is not already a process-group leader and `setsid` succeeds. Verified: `pid == pgid ==
  sid`.
- `os.execv` rather than `runpy.run_path`: exec PRESERVES the pid, so the probed pid stays the real
  run's pid, and it gives `scripts/phase23_run.py` an **absolute** `__file__`. `runpy.run_path(
  'scripts/phase23_run.py')` sets a RELATIVE `__file__`, and `:80`/`:112` derive `_SCRIPTS`/`_ROOT`
  from it — that makes both cwd-dependent for no benefit. Verified: session survives the exec.
- **The probe is `os.getsid(pid)`, NOT `ps -o pid,pgid,sid`.** BSD `ps` has no `sid` keyword;
  `ps -o pid,pgid,sid -p <pid>` exits with `ps: sid: keyword not found` and measures nothing.
  MEASURED. **The plan ASSERTING detachment without checking it is the defect this probe closes.**
- `caffeinate -is -w "$PID"`: a ~43-minute MPS run on a laptop is otherwise sleep-killable.
  Verified holding: `pmset -g assertions` shows `PreventUserIdleSystemSleep … asserting on behalf of
  Process ID <pid>`. **THE HONEST LIMIT, STATED: `caffeinate` cannot keep the machine awake with the
  LID CLOSED. Leave the lid open.** If caffeinate itself is killed the run continues — it only loses
  the sleep assertion, so this is degradation, not failure.
- `-u` so the log is unbuffered and observable mid-run. `< /dev/null` so it never blocks on stdin.
- **The log path is `results/phase23_resume_run.log` and NOT `results/phase23_matched_*`** —
  deliberately outside `MATCHED_ARTIFACT_GLOB`, so it cannot enter `prove_first_attempt`'s or the
  continuation predicate's `tracked` input, and so it does not become a member of the
  ancestry-guarded set.
- The log's FIRST LINE is `SESSION pid=… pgid=… sid=…`, so the pid is recoverable in later turns
  with `head -1 results/phase23_resume_run.log` and the detachment evidence is **committed** with
  the log rather than living only in a transcript.

Then observe with SHORT foreground reads between turns — `tail -5 results/phase23_resume_run.log`
and `test -f results/phase23_matched_control.json`. Never a blocking foreground wait.

**DO NOT RUN THE FULL SUITE WHILE THE RUN IS LIVE.**
`tests/test_phase23_resume.py::test_production_resume_epsilon_bit_identical` ends at `:720-728` with
a repository-wide `git status --porcelain results/ == ""`, and the live run is writing under
`results/`; it would go FALSE-RED, exactly as 23-17-SUMMARY §Deviation 2 records. It also contends
for the same MPS device and would corrupt the wall-clock this task must report against a bound.

**`tests/test_phase23_resume_prereg.py` IS NOT IN THIS TASK'S `<files>`, AND THAT IS A CLAIM WITH A
MECHANISM BEHIND IT.** This task writes FIVE scored seeds into `data/phase23_run_state.json` and
commits it, which WOULD have flipped that file's admitted case from ADMIT to REFUSE — except its
fixture is pinned to `git show d99d2aa:data/phase23_run_state.json` (Task 1, "THE FIXTURE IS
PINNED"), so it is time-invariant across this write and needs no edit and no vacuity branch. **Verify
the pin survived BEFORE launching, while the check is still free:**
`.venv/bin/python -c "import ast,pathlib;t=ast.parse(pathlib.Path('tests/test_phase23_resume_prereg.py').read_text());c=[n.value for n in ast.walk(t) if isinstance(n,ast.Constant) and isinstance(n.value,str)];print('d99d2aa:data/phase23_run_state.json' in c, 'data/phase23_run_state.json' in c, 'results/phase23_matched_*' in c)"`
must print `True False False` — the state fixture is the PINNED blob, the live state file is not
read, **and `tracked` is CONSTRUCTED from conjunct 7's formula rather than from a live
`git ls-files 'results/phase23_matched_*'`**. That third value is not decoration: after this task
commits the record the live glob returns SIX entries including
`results/phase23_matched_control.json`, and conjunct 6 refuses it — a test that listed the glob is
GREEN through Tasks 1 and 2 and RED only here, after the ~43 GPU-minutes. **If you later find
yourself needing to edit that test to make `pytest -q` green here, STOP — the pin was dropped in
Task 1.** Re-pin it. Do not widen the test to accommodate a
five-scored live file, and do not add a vacuity branch: a vacuity branch would silently disable this
guard at exactly the moment the run it guards has happened.

**Expected wall clock, bounded from 23-17's own measurements, not guessed.** Per seed the worst
observed legs are 165.79 s train + 1109.24 s score = 1275.03 s, so two seeds ≤ **2550.06 s ≈ 42.5
minutes**. An overrun is **NAMED, never absorbed** — 23-17 named seed 1338's +82.37 s scoring
overrun rather than smoothing it, and this task follows that.

**IF IT IS KILLED AGAIN: COMMIT FIRST, THEN RE-LAUNCH. NO SECOND NARROWING, NO FORCE FLAG.**

`prove_killed_run_continuation` is written on PREFIXES, not on the number three, so a `4 scored + 1
never-trained` state is still a proper ladder prefix and is admitted by the identical predicate.
**But conjunct 5 compares the working tree against `git show HEAD:data/phase23_run_state.json`, and
from the moment seed 2025 scores (~21 min into a ~42.5 min run) until its state is committed, the
tree has 4 scored and HEAD has 3 — so conjunct 5 REFUSES.** That refusal is CORRECT and it is not to
be repaired by narrowing the rule or adding a flag. The repair is an ordering:

1. **Commit what completed, BEFORE re-launching.** `git add data/phase23_run_state.json
   results/phase23_matched_control_seed<the seed that finished>` and commit. Now HEAD and the tree
   agree and conjunct 5 passes. Committing earlier makes the residual MORE auditable, not less —
   it lands the `matched` section in history sooner, which is exactly what 23-17's same-session
   commit argument buys. The "ONE same-session commit" instruction below is therefore satisfied
   *more strongly* by an intermediate commit, not violated by one.
2. **If the kill landed MID-TRAIN of the next seed**, that seed has a partial `run.csv` and
   `latest.pt`, no adapter and no `primary` block — so `_already_trained` is False, it will be
   retrained, and `train_matched_control`'s `refuse_if_exists` will refuse the leftover bytes.
   Delete them (`rm -rf results/phase23_matched_control_seed<N> checkpoints/…_seed<N>_latest.pt`)
   under the IDENTICAL four justifications Step A of Task 2 records — no reading was produced. These
   bytes were never committed, so this is a plain `rm`, not a `git rm`, and there is nothing to make
   visible beyond naming it in the summary.
   If the kill landed MID-SCORE (adapter present, no `primary`), delete NOTHING: `_already_trained`
   is True, training is skipped and scoring resumes.
3. Re-launch the identical command. `_state_record` refuses to overwrite a recorded value at a
   different one, so completed seeds cannot be silently re-read.

Conjunct 5's own refusal message carries instruction 1 verbatim, so a reader who never opens this
plan still gets it from the mechanism.

**IF IT REFUSES FOR ANY OTHER REASON, THE REFUSAL IS THE DELIVERABLE.** Do not add a force flag, an
override, a warning branch, or a second narrowing — regardless of how the run turns out. Record the
refusal verbatim in the summary and stop.

When the record exists, verify before committing:

- `results/phase23_matched_control.json` carries `n_seeds == 5`, `seeds == [1337, 2024, 1338, 2025,
  1339]`, five `readings`, `central_reading == readings[0]` at seed 1337, and a `floor` that
  re-derives under exact `==` from the recorded per-seed COUNTS through
  `phase23_prereg.noise_floor`.
- `attempt == "continuation"` and the five continuation keys are present and well-formed, with
  `continuation_fingerprint` carrying all SIX inputs including `tracked`.
- Every seed's `grad_clip_calls == 200`, `grad_clip_bound_count == 0`, `grad_clip_max_pre_clip_norm
  < 1e6` — checked BEFORE each seed was scored by the existing in-loop proof. **State seeds 2025 and
  1339's pre-clip norm ranges as measured**, including whether either exceeds the DP arm's recorded
  `2.278`, in the same stated-rather-than-smoothed register 23-17 used for seed 2024's `2.29014`.

Commit `results/phase23_matched_control.json`, both seed directories, `data/phase23_run_state.json`
and `results/phase23_resume_run.log` **in ONE same-session commit** (or, if a second kill forced an
intermediate commit, in the smallest number of same-session commits that ordering required), for the
reason 23-17 recorded: the tracked state file's `matched` section makes a later deletion a VISIBLE
DIFF, and tracking is NOT retroactive, so the same-session commit is what converts the residual from
invisible to auditable. **Committing the log is also what keeps `results/` CLEAN** —
`tests/test_phase23_resume.py::test_production_resume_epsilon_bit_identical` ends with a
repository-wide `git status --porcelain results/ == ""`, and 23-17 measured that an uncommitted
result directory reddens it (`1 failed, 1517 passed`).

Then update `.planning/STATE.md` and `.planning/ROADMAP.md`. **SIX EDITS across two files — FIVE
stale lines plus ONE line that does not exist yet — and a SEVENTH line that looks stale and is NOT.
Every one MEASURED at HEAD. Count them off, because an earlier revision of this plan said "three",
named four, and both it missed are real:**

- `.planning/STATE.md:6` reads `stopped_at: 23-17-PLAN.md INCOMPLETE (plan 17 of 19) …` → the phase
  has TWENTY plans now (`23-01`…`23-20`), so this becomes **`Completed 23-20-PLAN.md (plan 20 of
  20)`** with the D-04 halt and the `23-11..23-14` BLOCKED status UNCHANGED, and 23-18/23-19 now
  UNBLOCKED.
- `.planning/STATE.md:12` — `total_plans: 66` → **`67`** (this plan did not exist when that count
  was taken).
- `.planning/STATE.md:13` — `completed_plans: 59` → **`60`**. **THE NUMERATOR MOVES WITH THE
  DENOMINATOR.** Bumping `total_plans` alone silently reports one fewer plan done than was done; this
  plan completes, so `59 → 60`.
- `.planning/STATE.md:14` — `percent: 33` is **CORRECT AND STAYS PUT.** MEASURED: it is PHASE-based,
  not plan-based — `completed_phases 3 / total_phases 9` is `33` — and phase 23 does not complete
  here. Do not "helpfully" recompute it from the plan counts.
- `.planning/ROADMAP.md:589` — `**Plans**: 19 plans across 13 waves *(…)*` → **`20 plans across 13
  waves`**. **The WAVE count does NOT change**: 23-20 declares `wave: 12` and a `**Wave 12**` header
  already exists at `:666`. Extend the dated parenthetical to say why 23-20 exists (added 2026-08-27
  to complete 23-17's harness-killed run) rather than folding it into the `23-15…23-19` gap closure,
  which was a different round for a different cause.
- `.planning/ROADMAP.md:838` — the phase row `| 23. … | v4.0 | 13/19 | HALTED (D-04) — …` →
  **`14/20`**, with the status text updated to record that the matched record now EXISTS and that
  23-18/23-19 are unblocked. MEASURED: `ls .planning/phases/23-*/23-*-SUMMARY.md | wc -l` is `13`
  today, so `14` is `13 + this plan`, not a guess.

**AND ONE THING THAT IS NOT A TICK. MEASURED: `grep -n '23-20' .planning/ROADMAP.md` RETURNS
NOTHING.** There is no `23-20-PLAN.md` line in the ROADMAP to tick — it was never added. **ADD one**,
already checked (`- [x] 23-20-PLAN.md — …`), under the existing `**Wave 12**` header at `:666`,
beside 23-18. Describe it as: complete 23-17's harness-killed matched run — a NEW pin beside the
frozen one (`scripts/phase23_resume_prereg.py`), both one-attempt rules wired by branch, seed 2025's
reading-less partial bytes discarded, seeds 2025 + 1339 run detached to the five-seed record and the
re-reduced floor — and note parenthetically that the recomputed DAG places it BEFORE 23-18, which
`depends_on: ["23-17", "23-20"]`. **An instruction to "tick" a line that does not exist is one an
executor satisfies by doing nothing and reporting success, so the verb is load-bearing: ADD it, then
verify with the ANCHORED, CHECKED form — `grep -c '^- \[x\] 23-20-PLAN.md' .planning/ROADMAP.md`
prints `1`. Anchored deliberately: the `:589` parenthetical and the `:838` status text this same edit
rewrites may well NAME 23-20 in prose, and a bare `grep -c '23-20-PLAN.md'` would then count `2` and
go false-RED against text the instruction itself asked for.**

**`DPSGD-06` is NOT ticked by this plan, and it does not need to be** — MEASURED at
`.planning/REQUIREMENTS.md:156-160` it is ALREADY `[x] SATISFIED (plan 23-10)`. This delivers the
comparator record; the re-pin is 23-18's and the VERDICT is 23-19's, and nothing here renders one.

**Hand-verify the STATE/ROADMAP edits with `git diff` before committing.** This repository has a
recorded history of `gsd-sdk` `state.*` / `roadmap.*` mutation handlers corrupting planning
frontmatter; if one is used, `git diff .planning/STATE.md .planning/ROADMAP.md` and repair by hand.

Commit: `run(23-20): the protocol-matched comparator's five seeds, and the re-reduced floor`.
  </action>

  <verify>
    <automated>test -f results/phase23_matched_control.json && .venv/bin/python -c "
import json,sys,pathlib
sys.path.insert(0,'scripts')
import phase23_prereg as pz
r=json.load(open('results/phase23_matched_control.json'))
assert r['n_seeds']==5 and r['seeds']==[1337,2024,1338,2025,1339], r['seeds']
rates=[e['primary']['k']/e['primary']['n'] for e in r['per_seed']]
assert r['readings']==rates and r['floor']==pz.noise_floor(rates), 'floor does not re-derive'
assert r['central_reading']==r['readings'][0] and r['central_reading_seed']==1337
assert r['attempt']=='continuation' and r['continuation_rule']=='phase23_resume_prereg.prove_killed_run_continuation'
assert 'tracked' in r['continuation_fingerprint'], 'fingerprint omits tracked — conjuncts 6 and 7 would re-admit vacuously'
assert r['continuation_fingerprint']['ladder']==r['seeds'], 'ladder recorded out of LADDER ORDER — conjuncts 3 and 4 INDEX it and would refuse'
for k,v in r['grad_clip_evidence'].items():
    assert v['calls']==200 and v['bound_count']==0 and v['max_pre_clip_norm']<1e6, (k,v)
print('OK floor',r['floor'],'over',len(rates),'readings')
" && .venv/bin/python -m pytest -q && .venv/bin/ruff check . && .venv/bin/ruff format --check .</automated>
  </verify>

  <acceptance_criteria>
    - The run was launched DETACHED and no foreground call held it. `results/phase23_resume_run.log`
      exists, is committed, its FIRST line is `SESSION pid=<p> pgid=<p> sid=<p>` with all three
      EQUAL, and its tail shows the record write line rather than a truncated progress line.
    - The detachment PROBE was run before any GPU second and its output is quoted in the summary:
      `pid=<p> pgid=<p> sid=<p>` followed by `DETACHED OK`. A summary that asserts detachment without
      this output does not satisfy this criterion.
    - `results/phase23_matched_control.json` exists and the inline verification script above prints
      `OK floor <value> over 5 readings`.
    - `.venv/bin/python -m pytest tests/test_phase23_matched.py -q` passes with the record PRESENT —
      all record guards now on their LIVE branch, not their vacuity branch. State in the summary
      which guards changed from vacuous to live.
    - The recorded fingerprint re-admits under its own rule **with its own tracked list**, so
      conjuncts 6 and 7 are exercised rather than satisfied vacuously:
      `.venv/bin/python -c "import json,sys;sys.path.insert(0,'scripts');import phase23_resume_prereg as rp;f=json.load(open('results/phase23_matched_control.json'))['continuation_fingerprint'];print(rp.prove_killed_run_continuation(tracked=f['tracked'],ladder=tuple(f['ladder']),trained_seeds=set(f['trained_seeds']),scored_seeds=set(f['scored_seeds']),committed_scored_seeds=set(f['committed_scored_seeds']),record_exists=f['record_exists']))"`
      prints `True`.
    - `results/` is CLEAN: `git status --porcelain results/ data/` is empty.
    - `git ls-files 'results/phase23_matched_*' | wc -l | tr -d ' '` prints `6` (five per-seed `run.csv` plus the
      record).
    - `git diff --exit-code c100388 HEAD -- scripts/phase23_matched_prereg.py` exits `0` and
      `git log --format=%H -- scripts/phase23_matched_prereg.py | wc -l | tr -d ' '` prints `1`.
    - `git log --format=%H -- scripts/phase23_resume_prereg.py | wc -l | tr -d ' '` prints `1` — the resume pin
      was written once and not revisited during the run.
    - `git diff --exit-code c7de5d4 HEAD -- scripts/phase23_prereg.py` exits `0`.
    - D-04 holds: `git ls-files 'results/phase23_noised_*' | wc -l | tr -d ' '` prints `0`, and
      `git diff --exit-code d99d2aa HEAD -- results/phase23_sigma_zero.json results/phase23_control_floor.json`
      exits `0`.
    - NO verdict artifact was written: `test ! -f results/phase23_matched_verdict.json`.
    - Full suite green with the arithmetic stated. `ruff` clean.
    - `git diff .planning/STATE.md .planning/ROADMAP.md` was READ before committing and the edits
      are ALL SIX intended ones and no more: `STATE:6` "(plan 17 of 19)" → "(plan 20 of 20)";
      `STATE:12` `total_plans: 66 → 67`; `STATE:13` `completed_plans: 59 → 60`; `ROADMAP:589`
      `19 plans → 20 plans` (waves stay `13`); `ROADMAP:838` `13/19 → 14/20`; and a NEW
      `- [x] 23-20-PLAN.md` line ADDED under `**Wave 12**`. `STATE:14` `percent: 33` is UNCHANGED —
      it is phase-based (`3/9`). `DPSGD-06` is NOT re-ticked (it is already `[x]`).
    - The counts read back exactly, so the edit is checked and not just eyeballed. **The ROADMAP
      check is ANCHORED to the checked-box line, not to the bare filename** — the `:589` and `:838`
      edits may name 23-20 in prose, and a bare filename grep would count those and go false-RED:
      `grep -c '^- \[x\] 23-20-PLAN.md' .planning/ROADMAP.md` prints `1`, and
      `.venv/bin/python -c "import re,pathlib;s=pathlib.Path('.planning/STATE.md').read_text();print(re.search(r'total_plans: (\d+)',s)[1], re.search(r'completed_plans: (\d+)',s)[1], re.search(r'percent: (\d+)',s)[1])"`
      prints `67 60 33`.
  </acceptance_criteria>

  <done>
`results/phase23_matched_control.json` exists with five seeds and a floor that re-derives from its
own counts through the blindly-committed reduction; the run was launched detached with a PROBED
`pid == pgid == sid` and a held `caffeinate` assertion; both owed seeds' artifacts, the state file
and the run log are committed same-session; the full suite is green with every record guard on its
live branch; both pins byte-identical and the resume pin at one commit; D-04 untouched; STATE and
ROADMAP updated by hand-verified diff with the counts corrected; `DPSGD-06` untouched and 23-19's
verdict still unrendered.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a killed run → its continuation | the one place where "same attempt" and "second attempt" are told apart, and the only thing telling them apart is the state the writer's ORDERING can and cannot produce |
| the working tree → git history | `data/phase23_run_state.json` is force-tracked; a hand-edited working tree is what a laundered continuation would look like |
| a published record → its reader | a reader who opens only `results/phase23_matched_control.json` must learn it was a continuation |
| the harness → the detached run | a process group the harness can kill, versus a session it cannot |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-70 | Tampering | `prove_first_attempt` | mitigate | Not edited, not wrapped, and not passed a filtered input — the branch predicate is `not scored` ALONE, so the refusal is REACHABLE with a real non-empty glob and a test watches it fire. `git diff --exit-code c100388` and `git log \| wc -l == 1` run in the SUITE (Task 1), so the freeze is checked on every run, not once at plan time. |
| T-23-71 | Tampering | the narrowing itself | mitigate | The predicate's signature admits no reading; a WHOLE-BLOCK substitution test (every value replaced, key presence alone preserved) proves the derived args, the verdict and the refusal message bit-identical; an AST tripwire refuses any float constant in the module. |
| T-23-72 | Tampering | `data/phase23_run_state.json` | mitigate | `committed_scored_seeds` from `git show HEAD:` must EQUAL the working tree's scored set. A hand-edited state file refuses — and so does an uncommitted second kill, whose refusal message carries the commit-then-relaunch remedy rather than a flag. |
| T-23-73 | Repudiation | `results/phase23_matched_control.json` | mitigate | `attempt`, `continuation_rule`, `continuation_fingerprint` (all SIX inputs, `tracked` included), `continuation_scope` and `continuation_discrimination` travel with the artifact; the recorded fingerprint is re-admitted by its own rule on every suite run, passing its own `tracked` so conjuncts 6 and 7 are exercised rather than vacuous. |
| T-23-74 | Tampering | seed 2025's deleted bytes | mitigate | `git rm` in its own commit → visible in `--diff-filter=D`; the recovery command is in the commit message and is checked to work; the gitignored `latest.pt` is declared UNRECOVERABLE-from-git and regenerable from the seed. |
| T-23-75 | Denial of Service | the >60-min run | mitigate | `os.setsid()` inside the launched interpreter + `os.execv` (**`setsid(1)` does not exist here — MEASURED, `command -v setsid` exits 1**), PROBED with `os.getsid(pid)` asserting `pid == pgid == sid` before any GPU second, and `caffeinate -is -w <pid>` against laptop sleep. No foreground call holds it. A second kill is admitted by the same prefix-shaped predicate after a commit, with NO further narrowing. |
| T-23-77 | Tampering | `scripts/phase23_resume_prereg.py` itself | accept | **This module CANNOT be frozen by the phase-20 ancestry guard**: `adds[-1]` for `MATCHED_ARTIFACT_GLOB` is `d99d2aa`, which precedes any commit of this file, so conjunct 2 can never hold. What stands in is `test_the_resume_pin_has_exactly_one_commit` — DETECTION after the fact, strictly weaker than the frozen pin's guarantee. Accepted and DISCLOSED as clause (5) of `CONTINUATION_SCOPE`, not papered over. |
| T-23-76 | Elevation of Privilege | D-04's halt | accept | Untouched. `git ls-files 'results/phase23_noised_*'` is `0` at start and end; no verdict is rendered here. |
| T-23-SC | Tampering | npm/pip/cargo installs | mitigate | **NO package is installed by this plan.** Nothing is added to `pyproject.toml` or `requirements.txt`; the new module is stdlib-only. If any task finds itself needing an install, STOP — that is out of scope and requires a blocking human checkpoint. |
</threat_model>

<verification>
Every command runnable, from the repository root, with the venv. **Never `python3` — always
`.venv/bin/python`** (this box runs Python 3.14, which is not a supported target).

```bash
# --- the frozen pin was not touched, by the chosen route ---
git diff --exit-code c100388 HEAD -- scripts/phase23_matched_prereg.py   # exit 0
test "$(git log --format=%H -- scripts/phase23_matched_prereg.py | wc -l | tr -d ' ')" = 1
git diff --exit-code c7de5d4 HEAD -- scripts/phase23_prereg.py           # exit 0
.venv/bin/python -m pytest tests/test_phase23_matched_prereg.py -q       # the ancestry guard

# --- the new pin's OWN (weaker, disclosed) guard ---
test "$(git log --format=%H -- scripts/phase23_resume_prereg.py | wc -l | tr -d ' ')" = 1

# --- the narrowing is readings-independent, by AST and by whole-block substitution ---
.venv/bin/python -m pytest tests/test_phase23_resume_prereg.py -q
.venv/bin/python scripts/phase23_resume_prereg.py                        # every refusal watched

# --- the test fixture is PINNED, so it survives Task 3's five-seed write (AST, not grep) ---
.venv/bin/python -c "import ast,pathlib;t=ast.parse(pathlib.Path('tests/test_phase23_resume_prereg.py').read_text());c=[n.value for n in ast.walk(t) if isinstance(n,ast.Constant) and isinstance(n.value,str)];print('d99d2aa:data/phase23_run_state.json' in c, 'data/phase23_run_state.json' in c, 'results/phase23_matched_*' in c)"
                                                                         # True False False

# --- both one-attempt rules are reachable from the writer (AST, not grep) ---
.venv/bin/python -c "import ast,pathlib;t=ast.parse(pathlib.Path('scripts/phase23_run.py').read_text());m=[n for n in ast.walk(t) if isinstance(n,ast.FunctionDef) and n.name=='matched'][0];print(sorted({n.func.attr for n in ast.walk(m) if isinstance(n,ast.Call) and isinstance(n.func,ast.Attribute)} & {'prove_first_attempt','prove_killed_run_continuation'}))"

# --- the frozen rule's argument is UNFILTERED: the guard is UnaryOp(Not), never BoolOp(And) ---
.venv/bin/python -c "
import ast,pathlib
t=ast.parse(pathlib.Path('scripts/phase23_run.py').read_text())
m=[n for n in ast.walk(t) if isinstance(n,ast.FunctionDef) and n.name=='matched'][0]
g=[n for n in ast.walk(m) if isinstance(n,ast.If) and any(isinstance(c,ast.Call) and isinstance(c.func,ast.Attribute) and c.func.attr=='prove_first_attempt' for c in ast.walk(n))][0]
print(type(g.test).__name__)"                                            # UnaryOp

# --- the per-seed path formula renders a REAL tracked path ---
.venv/bin/python -c "import sys;sys.path.insert(0,'scripts');import phase23_matched_prereg as mp;print(f'results/{mp.MATCHED_ARM_PREFIX}_{mp.matched_arm(1337)}/run.csv')"
git ls-files 'results/phase23_matched_*' | grep -x 'results/phase23_matched_control_seed1337/run.csv'

# --- the record: five seeds, floor re-derives, continuation disclosed with tracked ---
.venv/bin/python -c "
import json,sys; sys.path.insert(0,'scripts'); import phase23_prereg as pz
r=json.load(open('results/phase23_matched_control.json'))
rates=[e['primary']['k']/e['primary']['n'] for e in r['per_seed']]
assert r['n_seeds']==5 and r['readings']==rates and r['floor']==pz.noise_floor(rates)
assert r['attempt']=='continuation' and r['continuation_fingerprint']['record_exists'] is False
assert 'tracked' in r['continuation_fingerprint']
print('floor', r['floor'])"

# --- the detached run's own evidence, committed with the log ---
head -1 results/phase23_resume_run.log     # SESSION pid=N pgid=N sid=N, all three EQUAL

# --- nothing else moved ---
git diff --exit-code d99d2aa HEAD -- scripts/mitigation_gate.py scripts/mitigation_accountant.py \
  scripts/mitigation_budget.py scripts/teach_persona.py src/personacore/training/loop.py \
  results/phase23_sigma_zero.json results/phase23_control_floor.json
test "$(git ls-files 'results/phase23_noised_*' | wc -l | tr -d ' ')" = 0
test ! -f results/phase23_matched_verdict.json
git status --porcelain results/ data/                                    # empty

# --- the suite and the linter (NEVER while the detached run is live) ---
.venv/bin/python -m pytest -q
.venv/bin/ruff check . && .venv/bin/ruff format --check .
```
</verification>

<success_criteria>
- [ ] `scripts/phase23_resume_prereg.py` exists as a **NEW** file; `scripts/phase23_matched_prereg.py`
      is byte-identical to `c100388` and still has exactly ONE commit; the ancestry guard is green.
- [ ] The narrowing is justified from the record's **write-ordering** — a completed attempt
      necessarily leaves five scored seeds — and NEVER from `0.7837301587301587`,
      `0.7678571428571429` or `0.7718253968253969`. The argument is in the module docstring and in
      the load-bearing refusal's own message.
- [ ] The independence is **checkable**: the predicate's signature carries no reading, and a
      WHOLE-BLOCK substitution test (every value replaced, key presence alone preserved, refusing
      case derived FROM the substituted copy) proves the derived arguments, the verdict and the
      refusal message bit-identical. An AST tripwire refuses a float constant in the module (grep
      would go false-RED against the docstring).
- [ ] `prove_first_attempt` is REACHED and UNCHANGED, and its refusal is **REACHABLE**: the branch
      predicate is `not scored` ALONE, never `not tracked and not scored`, so the frozen rule is
      never handed an argument control flow already proved empty. A test watches it fire on a
      non-empty glob with an empty state. No force flag, override or warning branch was added.
- [ ] **The relationship between the two rules is stated at TRUE strength**: the new predicate is
      more demanding in SHAPE (seven NAMED conjuncts vs one) and strictly more PERMISSIVE in OUTCOME on
      the one state that matters. "Strictly more demanding" is not written anywhere.
- [ ] **`CONTINUATION_SCOPE` has FIVE clauses**, the fifth disclosing that this module is NOT frozen,
      that it CANNOT be registered against `MATCHED_ARTIFACT_GLOB` (`adds[-1]` is `d99d2aa`, which
      precedes it), and that its one-commit guard is detection rather than prevention.
- [ ] The `continuation_fingerprint` carries all SIX inputs including `tracked`, and every
      re-admission passes `tracked=f["tracked"]` — conjuncts 6 and 7 are exercised, never vacuous.
      **`ladder` is recorded in LADDER ORDER (`list(SEED_LADDER)`), never sorted** — MEASURED,
      `sorted(SEED_LADDER)` is `[1337, 1338, 1339, 2024, 2025]`, and conjuncts 3 and 4 INDEX the
      ladder, so a sorted one makes every re-admission REFUSE.
- [ ] **The resume-prereg test's fixture is a PINNED BLOB** — `git show
      d99d2aa:data/phase23_run_state.json` — and never the live working-tree file, which Task 3
      rewrites to five scored seeds and commits. The predicate's PRODUCTION conjunct 5 still reads
      `HEAD:` (a deliberately time-VARYING property); the two refs are not unified. Checked by AST
      exact-constant equality, not by grep.
- [ ] Seed 2025's partial `run.csv` and `latest.pt` are discarded in their OWN visible commit whose
      message carries all four justifications and a recovery command that works; nothing SCORED is
      touched; the mid-train-resume alternative is refused by `prove_matched_protocol:1181-1187`, not
      declined by preference.
- [ ] The run was executed DETACHED **by a mechanism that exists on this machine** — `os.setsid()` +
      `os.execv`, never `setsid(1)`, which `command -v` proves absent — under `caffeinate -is -w`,
      and the detachment was **PROBED with `os.getsid(pid)`** (not `ps -o sid`, which BSD `ps` does
      not support) before any GPU second, with the probe output quoted in the summary.
- [ ] A second kill is admitted by the SAME prefix-shaped predicate with no further narrowing, via
      commit-then-relaunch — and conjunct 5's own refusal message carries that remedy.
- [ ] N=5 preserved: `SEED_LADDER` untouched, the inherited-seed-set proof untouched, the floor
      reduced over FIVE readings through the blindly-committed `noise_floor`, never over three.
- [ ] The dispatch hazards are addressed at the source: `"23-20"` was added to `23-18-PLAN.md`'s
      `depends_on`, so the SDK now recomputes `9: [23-20], 10: [23-18], 11: [23-19]` and the
      parallel-dispatch collision is GONE. The index trap (`23-17 has_summary: true`) is recorded.
- [ ] `results/phase23_matched_control.json` exists, carries the five continuation keys, and its
      floor re-derives from its own counts under exact `==`.
- [ ] Full suite green with the arithmetic stated; `ruff` clean; both frozen pins byte-identical;
      D-04 untouched and `git ls-files 'results/phase23_noised_*'` is `0`.
- [ ] **NO requirement is ticked.** `DPSGD-06` is ALREADY `[x] SATISFIED (plan 23-10)` and is not
      re-touched; what remains open is 23-19's VERDICT, against a floor 23-18 re-pins from this
      record, and no verdict is rendered here.
- [ ] STATE/ROADMAP arithmetic corrected in ALL SIX stale places, hand-verified by `git diff`:
      `STATE:6` "(plan 17 of 19)" → "(plan 20 of 20)"; `STATE:12` `total_plans: 66 → 67`;
      `STATE:13` `completed_plans: 59 → 60` (the numerator moves with the denominator); `ROADMAP:589`
      `19 plans → 20 plans`, waves unchanged at `13`; `ROADMAP:838` `13/19 → 14/20`; and a `23-20`
      line **ADDED** under `**Wave 12**` — MEASURED, `grep -n '23-20' .planning/ROADMAP.md` returns
      NOTHING today, so it is an ADD and not a tick, verified after by the ANCHORED
      `grep -c '^- \[x\] 23-20-PLAN.md'` returning `1` (anchored because the same edit may name 23-20
      in the `:589` and `:838` prose). `STATE:14` `percent: 33` is phase-based (`3/9`)
      and correctly UNCHANGED.
</success_criteria>

<output>
Create `.planning/phases/23-cost-calibration-the-0-diagnostic-and-budget-pre-registratio/23-20-SUMMARY.md`
when done.

**Report, at minimum:** which route was taken for the narrowing and the measured argument for it;
the exact refusal messages watched firing; the whole-block substitution test's result; the
detachment probe's verbatim `pid=/pgid=/sid=` output; seeds 2025 and 1339's readings,
training/scoring seconds against the ≤1275.03 s per-seed bound (overruns NAMED), and their pre-clip
norm ranges against the DP arm's recorded `2.278`; the five-reading floor as a value and as counts;
the suite arithmetic; and — if anything refused, was killed again, or diverged — the verbatim output
rather than a smoothed paraphrase.
</output>
