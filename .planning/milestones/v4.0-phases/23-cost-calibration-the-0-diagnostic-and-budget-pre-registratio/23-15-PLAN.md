---
phase: 23-cost-calibration-the-0-diagnostic-and-budget-pre-registratio
plan: 15
type: execute
wave: 9
depends_on: ["23-10"]
gap_closure: true
files_modified:
  - scripts/phase23_matched_prereg.py
  - tests/test_phase23_matched_prereg.py
autonomous: true
requirements: [DPSGD-06]

must_haves:
  truths:
    - "The protocol-matched comparator's RULES are committed BLIND — while `git ls-files 'results/phase23_matched_*'` returns NOTHING (measured 0 at planning time). The σ=0 arm's reading already exists, so the comparator's PROTOCOL is the only thing left that could be tuned to it; pinning the protocol before its readings exist is what removes that freedom."
    - "`scripts/phase23_prereg.py` is NOT edited. It is byte-identical to `c7de5d4` and stays so: `sigma_zero_verdict` and `noise_floor` are correct and blind, and the corrected comparator is a new INPUT to them, not a rule change."
    - "The difference ledger is checked BY CONSTRUCTION, not by care. 23-08 enumerated four residual differences in advance and MISSED `grad_clip` — the largest per-step effect after the packer. An AST census of every `dp_fn`-conditioned branch in `training/loop.py` (measured: EXACTLY 7), an AST read of `teach_persona.py`'s `dp_kwargs`/`dp_accum` key sets (measured: 7 keys), and an AST read of the production `train(...)` CALL's own keyword set (measured: 15 named + `**dp_kwargs` = 21 names) turn 'did we list them all?' into a refusal a future edit reddens."
    - "ONE ATTEMPT, STATED AT ITS TRUE STRENGTH — FOUR CLAUSES, NOT THREE. `prove_first_attempt` binds ACROSS COMMITS: it refuses to run once a matched artifact is TRACKED. (1) Inside the uncommitted window it does not bind at all, because `data/` and `checkpoints/` are gitignored (`.gitignore:17`, `:14`). (2) There, 23-17's `prior_scored_seeds_at_start` refuses only a delete-and-re-run that leaves the state file's `matched` section INTACT — a delete that ALSO removes that section is PREVENTED BY NOTHING in real time. (3) That case is NOT PREVENTED BUT AUDITABLE AFTER THE FACT: `data/phase23_run_state.json` is TRACKED as of `cfa2c87` with a committed baseline carrying NO `matched` section, so once 23-17's same-session commit lands that section a later deletion of it is a VISIBLE DIFF against that commit. Tracking is NOT retroactive on its own — before that commit a `git checkout -- data/phase23_run_state.json` leaves NO history at all — so auditability begins only AT THE COMMIT, and the same-session requirement is what CONVERTS the residual from invisible to auditable rather than being its only bound. It is still a DISCIPLINE, not a mechanism. (4) The rule is scoped to ONE filename glob. The disclosure states all four rather than claiming a guarantee the mechanism does not have."
    - "The disclosure that the σ=0 reading WAS ALREADY VISIBLE when this protocol was designed is a REQUIRED FIELD of BOTH records — the verdict record AND the control record whose protocol was designed with `0.7837301587301587` on screen. A disclosure that does not travel with the artifact it is about is a disclosure a reader of that artifact never sees."
    - "The module carries EVERY constant, path and rule the four downstream gap plans consume, ahead of need — including the full `VERDICT_REQUIRED_KEYS` tuple, because `_assert_ordering_holds`' `prereg != first_add` conjunct makes this module PERMANENTLY edit-once the moment 23-17's first artifact lands, and a key guessed later cannot be added."
  artifacts:
    - path: "scripts/phase23_matched_prereg.py"
      provides: "the blind protocol declaration, the two artifact paths, the non-binding grad clip, the three AST completeness gates, the one-attempt rule stated at its true strength, and the two visibility-disclosure refusals"
      contains: "DP_FN_BRANCH_COUNTS"
    - path: "tests/test_phase23_matched_prereg.py"
      provides: "the ancestry guard for the new pin, the live census against the real `loop.py`/`teach_persona.py`, and every refusal watched RED"
      contains: "def test_the_matched_prereg_precedes_every_matched_artifact"
  key_links:
    - from: "tests/test_phase23_matched_prereg.py"
      to: "tests/test_phase23_prereg.py::_ordering_guard"
      via: "IMPORTED and called, never copied — the same body Phase 20/21/22/23's live guards run"
      pattern: "_ordering_guard"
    - from: "scripts/phase23_matched_prereg.py"
      to: "src/personacore/training/loop.py"
      via: "an AST census of every branch whose test names `dp_fn`, asserted equal to a declared Counter"
      pattern: "dp_fn_branch_census"
---

<objective>
Commit, BLIND, every rule that will govern the protocol-matched comparator — before a single
comparator reading exists.

**Why this plan exists.** `.planning/debug/sigma-zero-beats-control.md` (status `root-caused`,
commit `263f5f8`) attributes the D-04 HALT to branch **(A) INVALID COMPARATOR** and *falsifies*
branch (B) by direct measurement (72/72 LoRA tensors agree with an ordinary grad-accum reference to
`2.178e-07` relative at σ=0 with non-binding `C=1e6`). The σ=0 arm read `790/1008` against a control
central of `566/1008` — deviation `0.2222222222222222` (exactly 2/9) against a floor of
`0.05357142857142849` (= `54/1008` AS COUNTS; NOT bit-equal to `float(3/56)` =
`0.05357142857142857` — verified), **4.1481×**, direction **BEATS**. One predicate,
`scripts/teach_persona.py:1389` `is_dp = arm in DP_ARMS`, switches the packer, the lot size AND the
gradient clip together, so σ=0 measured a DIFFERENT TRAINING PROTOCOL.

**The awkward fact this plan is built around.** `results/phase23_sigma_zero.json` already exists and
its reading `0.7837301587301587` is already on screen. The floor's reduction (`noise_floor`), the
central-reading rule (`control_readings[0]`) and the verdict (`sigma_zero_verdict`) are all still
blind — they were pinned at `c7de5d4`. What is **not** yet pinned is the comparator's own protocol,
and someone who knows σ=0 read 0.7837 could design a protocol that lands near it. So the protocol,
its difference ledger, its completeness gates, its one-attempt rule and its visibility disclosure
land HERE, in wave 9, while the glob they judge is empty.

Purpose: make "the comparator was not tuned to the number it is compared against" a fact about
git's object graph rather than a claim in a paragraph — and state exactly where that fact stops.
Output: `scripts/phase23_matched_prereg.py` + `tests/test_phase23_matched_prereg.py`, committed
while `git ls-files 'results/phase23_matched_*'` returns nothing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/debug/sigma-zero-beats-control.md
@.planning/phases/23-cost-calibration-the-0-diagnostic-and-budget-pre-registratio/23-CONTEXT.md
@scripts/phase23_prereg.py
@CLAUDE.md

<interfaces>
<!-- Resolved from source. Do NOT retype these; import or derive them. -->

MEASURED AT PLANNING TIME (2026-08-27), reproduce before relying on any of it:

  $ git ls-files 'results/phase23_matched*' 'results/phase23_rematch*' | wc -l
  0                      <- the glob this pin must precede is EMPTY today

  $ .venv/bin/python -c "AST-walk src/personacore/training/loop.py for If/IfExp whose test names dp_fn"
  _optimizer_step  L189   dp_fn is not None
  _optimizer_step  L211   dp_fn is not None
  _optimizer_step  L213   dp_fn is not None
  _optimizer_step  L220   dp_fn is None
  _dp_extra        L709   dp_fn is None
  train            L766   dp_fn is None and ckpt.get('dp_noise_rng') is not None
  train            L781   dp_fn is not None and ckpt.get('dp_noise_rng') is not None
  TOTAL 7
  (condition strings above are EXACTLY `ast.unparse(node.test)` output, single quotes included)

  scripts/teach_persona.py:1585   dp_accum  = dict(grad_accum_steps=...)                    -> 1 key
  scripts/teach_persona.py:1586   dp_kwargs = dict(fact_bin=, n_facts=, replay_bin=,
                                                   replay_mask_bin=, replay_windows=, dp_fn=)  -> 6 keys

  scripts/teach_persona.py:1613   the PRODUCTION train(...) call inside `train_arm`, AST-read:
    named kwargs (15): train_config, runtime_config, model, model_config, train_bin,
                       train_mask_bin, val_bin, val_mask_bin, penalty_fn, log_path, eval_interval,
                       checkpoint_path, checkpoint_interval, resume_from, return_final_loss
    splat        (1): **dp_kwargs        -> the 6 keys above
    UNION = 21 names. This is the set the comparator must reproduce minus {resume_from, dp_fn}.

  MEASURED, torch 2.7.1 (the grad-clip inertness bound in (b)):
    torch/nn/utils/clip_grad.py::_clip_grads_with_norm_ computes
      clip_coef = max_norm / (total_norm + 1e-6) ; clip_coef_clamped = torch.clamp(clip_coef, max=1.0)
      then torch._foreach_mul_(device_grads, clip_coef_clamped)
    At C=1e6 vs ||g|| ~ 2 the clamp returns exactly 1.0.
    3 trials x 65,536 float32 elements: CPU bitwise-identical INCLUDING planted subnormals; MPS
    bitwise-identical for normal-range grads but FLUSHES SUBNORMALS TO ZERO (1.401e-45 -> 0.0,
    5.000e-39 -> 0.0).
    float32 smallest normal = 1.1754944e-38 = 2**-126 (verified).
    LoRA trainable parameter count = 331,776 (verified: r=8, 6 targets, n_layer=6, n_embd=384 ->
      4x8x(384+384) + 8x(384+1536) + 8x(1536+384) = 55,296 per layer x 6), so the per-element
      gradient scale at ||g|| ~ 2 is 2/sqrt(331,776) = 2/576 = 3.47e-3, about 35 orders of
      magnitude above the subnormal ceiling.

From scripts/phase23_prereg.py (EDIT-ONCE, byte-identical to `c7de5d4` — DO NOT TOUCH):
  def noise_floor(readings) -> float                       # max - min; refuses < 2 or non-finite
  def sigma_zero_verdict(*, control_readings, sigma_zero_reading, floor, floor_provenance) -> "proceed" | SystemExit
  FLOOR_PROVENANCE_KEYS = ("record","record_sha256","git_sha","device","torch_version","seeds","reduction","governs")
  def choose_n_seeds(seconds_per_seed) -> int              # largest of (5,4,3) fitting 17_175 s
  def _prove(condition, message)                           # SystemExit register; NEVER `assert`

From tests/test_phase23_prereg.py:359:
  def _ordering_guard(*, prereg_artifact, artifact_glob) -> int   # vacuity-safe ancestry wrapper
From tests/test_phase20_prereg.py:189:
  def _assert_ordering_holds(*, root, prereg_artifact, artifact_glob, globs)
      CONJUNCT 1 is `prereg != first_add` for EVERY prereg commit against EVERY artifact's earliest
      add, and CONJUNCT 2 is `git merge-base --is-ancestor prereg first_add`. Together they make
      `scripts/phase23_matched_prereg.py` PERMANENTLY EDIT-ONCE from 23-17's first artifact: any
      later commit touching it is a prereg commit that is not an ancestor of that first add.
  def _git(*args, cwd=None) -> str

From scripts/phase23_run.py:
  SEED_LADDER = (1337, 2024, 1338, 2025, 1339)
  SIGMA_ZERO_CLIP_NORM = 1e6      # this repo's established non-binding bound
  PREFIX = "phase23"
  STATE_PATH = data/phase23_run_state.json          # `data/` is gitignored (.gitignore:17) BUT
                                        # THIS FILE IS FORCE-TRACKED as of `cfa2c87` (`git add
                                        # -f` overrides the ignore rule), with a committed
                                        # baseline carrying control/cost/never_taught/sigma_zero
                                        # and NO `matched` section — measured
  arm_outputs(arm, prefix="phase23") -> results/phase23_{arm}/run.csv,
                                        checkpoints/phase23_{arm}_adapter.pt   # checkpoints/ is
                                        # GITIGNORED (.gitignore:14)

Measured σ=0 / control numbers, all re-derivable from the two committed records:
  σ=0 reading           0.7837301587301587   (790/1008, seed 1337)
  old control central   0.5615079365079365   (566/1008, seed 1337)
  old floor             0.05357142857142849  (= 54/1008 AS COUNTS. NOT bit-equal to float(3/56)
                                              = 0.05357142857142857 — measured. Equal as exact
                                              rationals of the counts, unequal as floats; write
                                              counts, or verify bit-equality before saying "exactly")
  deviation             0.2222222222222222   (= 2/9 exactly — this one IS bit-equal, verified)
                                             -> 4.1481x
  DP arm pre-clip norms 1.538-2.278 over the first 25 steps (probe 1)
  control clip bound on 19/25 steps, mean shrink 0.8071
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the blind pre-registration module</name>
  <files>scripts/phase23_matched_prereg.py</files>
  <read_first>
    - scripts/phase23_prereg.py — the register this module copies: `_prove` (never `assert`, because
      `python -O` strips it), an artifact register declared ahead of need, rules as arithmetic over
      caller-supplied values, and a `__main__` self-check that WATCHES each refusal fire.
    - .planning/debug/sigma-zero-beats-control.md, sections "TASK 2 — THE FOUR RESIDUAL DIFFERENCES,
      MEASURED", "THE TWO DECISIVE PROBES" and "Resolution" — every disposition below is quoted from
      a measurement there, not invented here.
    - src/personacore/training/loop.py lines 180-232 (`_optimizer_step`), 700-715 (`_dp_extra`),
      760-790 (the resume DP-slot matrix) — the seven branches the ledger declares.
    - scripts/teach_persona.py lines 1564-1660 — the `dp_accum` / `dp_kwargs` dict literals AND the
      `train(...)` call at :1613, the source of the seven declared keys and of the 21-name call set.
    - .gitignore lines 14 and 17 — `checkpoints/` and `data/` are ignored. This is the measured
      premise behind the honest wording of `prove_first_attempt` in (i).
    - scripts/mitigation_budget.py module docstring — why a `mitigation_*.py` NAME would impose a
      zero-headroom `{pathlib, sys, erasure_gate}` import ceiling. This module is deliberately NOT
      named `mitigation_*`, so `ast`/`collections`/`math` are permitted, exactly as
      `scripts/phase23_prereg.py` imports `math`.
  </read_first>
  <action>
Create `scripts/phase23_matched_prereg.py`. Stdlib only (`ast`, `collections`, `math` as needed);
CPU-only, GPU-free, no torch, no network. Copy `phase23_prereg`'s `_prove(condition, message)`
register verbatim in shape — `raise SystemExit(f"[phase23_matched_prereg] {message}")` — and use it
for every refusal. NEVER `assert`, in code OR in prose: an acceptance criterion below AST-scans this
file for `Assert` nodes and for `torch`/`numpy` imports, so keep the word out of statements and keep
the docstrings free of import-looking strings.

Module docstring must state, in this order: (a) this module is EDIT-ONCE **from the moment
`results/phase23_matched_*` first matches**, because `_assert_ordering_holds`' `prereg != first_add`
conjunct plus its ancestry conjunct forbid any later commit that touches it — until then it is
technically still editable, but that window closes at THIS PLAN'S OWN COMMIT in practice and
structurally at 23-17's first artifact, so no downstream plan may treat it as a safety valve;
(b) it exists because the σ=0 reading was ALREADY VISIBLE when this protocol was designed, so the
protocol itself is the last remaining degree of freedom and it is being spent here, blind;
(c) `scripts/phase23_prereg.py`, `scripts/mitigation_gate.py` and `scripts/mitigation_accountant.py`
are NOT edited by this phase and this module adds no rule to them — the corrected comparator is a
new INPUT to `sigma_zero_verdict`.

Declare, as module-level constants and functions:

(a) THE ARTIFACT REGISTER — every path the four downstream gap plans will write, ahead of need:
    `MATCHED_CONTROL_RECORD = "results/phase23_matched_control.json"` (23-17: the comparator's N
    per-seed readings and the re-reduced floor);
    `MATCHED_VERDICT_RECORD = "results/phase23_matched_verdict.json"` (23-19: the re-test);
    `MATCHED_ARTIFACT_GLOB = "results/phase23_matched_*"`;
    `MATCHED_ARM_PREFIX = "phase23"` and
    `def matched_arm(seed)` returning `f"matched_control_seed{seed}"`, so
    `teach_persona.arm_outputs(matched_arm(1337), prefix=MATCHED_ARM_PREFIX)` renders
    `results/phase23_matched_control_seed1337/run.csv` and
    `checkpoints/phase23_matched_control_seed1337_adapter.pt` — the CSV inside the glob, the
    checkpoint inside gitignored `checkpoints/`. Refuse a non-integer / bool seed. Note in the
    comment that `bin`/`mask` carry no prefix (`arm_outputs`' documented non-widening), that this
    arm builds NO bins at all — it trains on `dp_n8`'s — and that `checkpoints/` being gitignored is
    exactly why (i)'s guarantee is scoped the way it is.

(b) `MATCHED_GRAD_CLIP = 1e6` — the comparator's `TrainConfig.grad_clip`. Comment must record:
    `config.py:105` defaults `grad_clip` to `1.0`; `loop.py:220-228` applies `clip_grad_norm_` iff
    `dp_fn is None`, so at the default the comparator would be clipped where the DP arm structurally
    is not — MEASURED as binding on 19 of the control's first 25 steps with mean shrink `0.8071`,
    against DP pre-clip norms of 1.538-2.278 that were never clipped. `1e6` is this repository's
    established non-binding bound (`tests/test_phase22_checkpoint.py:97` `_NON_BINDING_CLIP`,
    `tests/test_phase22_fakes.py:93`, `phase23_run.SIGMA_ZERO_CLIP_NORM`).
    **State the inertness claim EXACTLY THIS BOUNDED — this comment is edit-once and cannot be
    corrected later:** at `clip_coef = max_norm / (total_norm + 1e-6)` clamped to `max=1.0`, a
    non-binding bound multiplies every gradient by exactly `1.0`; MEASURED bitwise-inert on MPS
    (torch 2.7.1) over 3 x 65,536 normal-range float32 gradients, with one measured exception —
    MPS flushes subnormal entries (|g| < 2**-126 = 1.1754944e-38) to zero, a magnitude about 35
    orders below this run's per-element gradient scale of 2/sqrt(331,776) = 3.47e-3. Do NOT write
    the unqualified phrase "arithmetically inert"; write the measured claim with its exception.
    Add that "non-binding" is PROVEN at run time by 23-17 capturing the returned pre-clip norm,
    never assumed.
    **RE-MEASURE BEFORE YOU COMMIT THE COMMENT.** The subnormal-flush figure above was measured by
    the plan-checker, not by this executor, and the comment is edit-once — a number that cannot be
    corrected later must not be inherited on trust. Run the 3 x 65,536 float32 comparison on THIS
    machine (normal-range gradients plus planted subnormals, `clip_grad_norm_` at C=1e6, CPU and
    MPS, bitwise compare before/after) and paste YOUR measured result, with your torch version, into
    the comment. If your measurement disagrees with the figure above, write yours and record the
    disagreement in the SUMMARY — it takes seconds and the alternative is an uncorrectable claim.

(c) `DP_FN_BRANCH_COUNTS` — a dict keyed by `(enclosing_function_name, ast.unparse(node.test))`
    mapping to an int count, holding EXACTLY the seven-branch census in `<interfaces>` above:
    `("_optimizer_step", "dp_fn is not None"): 3`, `("_optimizer_step", "dp_fn is None"): 1`,
    `("_dp_extra", "dp_fn is None"): 1`,
    `("train", "dp_fn is None and ckpt.get('dp_noise_rng') is not None"): 1`,
    `("train", "dp_fn is not None and ckpt.get('dp_noise_rng') is not None"): 1`.
    A COUNTER and not an ordered sequence, deliberately: a pure reorder inside `loop.py` changes
    nothing about the protocol and must not redden, while a NEW branch or a REMOVED one must.

(d) `DP_FN_BRANCH_DISPOSITIONS` — a tuple of exactly `sum(DP_FN_BRANCH_COUNTS.values())` == 7 dicts,
    each `{"function", "condition", "site", "disposition", "evidence"}`, `disposition` drawn from the
    closed set `{"inert", "equalised-by-arithmetic", "equalised-by-constant", "declared-difference",
    "unreached"}`. The seven, with the evidence each must cite:
      1. `_optimizer_step` / `dp_fn is not None` / `begin_step()` — `inert`; zeroes the DP-owned
         accumulator, which the comparator does not have.
      2. `_optimizer_step` / `dp_fn is not None` / the `/accum` bypass at `loop.py:211` —
         `equalised-by-arithmetic`; PROBE 2 in the debug record: 72/72 LoRA tensors agree with the
         ordinary grad-accum reference, worst relative difference `2.178e-07`.
      3. `_optimizer_step` / `dp_fn is not None` / `absorb_record()` — `equalised-by-arithmetic`;
         same probe, clip coefficient exactly `1.0` at `clip_bind_count == 0`.
      4. `_optimizer_step` / `dp_fn is None` / `clip_grad_norm_` vs `finalize` —
         `equalised-by-constant`, via `MATCHED_GRAD_CLIP`. **THIS IS THE ONE 23-08 DID NOT
         ENUMERATE** and the evidence string must say so, naming the 19/25 and `0.8071` measurement.
      5. `_dp_extra` / `dp_fn is None` — `declared-difference`; the comparator's checkpoint carries
         no `dp_noise_rng` slot. Checkpoint CONTENT, downstream of every gradient, so it cannot move
         a reading.
      6. and 7. `train` / the two resume DP-slot conditions — `unreached`; this scheduling passes no
         `resume_from`.

(e) `def dp_fn_branch_census(source)` — parse `source` with `ast`, visit `If` and `IfExp`, and for
    every node whose `test` contains an `ast.Name` with `id == "dp_fn"` yield
    `(enclosing_function_name, ast.unparse(node.test))`. Return a `collections.Counter`. Track the
    enclosing function with a stack over `FunctionDef` and `AsyncFunctionDef`; use `"<module>"` when
    the stack is empty.

(f) `def prove_branch_ledger_complete(source)` — `_prove` that
    `dp_fn_branch_census(source) == collections.Counter(DP_FN_BRANCH_COUNTS)`, with a message that
    prints the symmetric difference in both directions and states why an undeclared branch is fatal:
    23-08 enumerated four differences by hand and missed the largest per-step one. Also `_prove` the
    census is NON-EMPTY (a walk that silently stopped working finds nothing and would pass while
    proving nothing) and that `len(DP_FN_BRANCH_DISPOSITIONS) == sum(DP_FN_BRANCH_COUNTS.values())`.
    Return the census.

(g) `DP_TRAIN_KEYS = ("grad_accum_steps",)` and
    `DP_KWARGS_KEYS = ("dp_fn", "fact_bin", "n_facts", "replay_bin", "replay_mask_bin",
    "replay_windows")`, plus
    `def dp_wiring_key_census(source)` — AST-read `scripts/teach_persona.py`'s source, find the
    `Assign` statements whose single target is `Name(id="dp_accum")` and `Name(id="dp_kwargs")`, and
    return `(frozenset_of_dp_accum_keyword_names, frozenset_of_dp_kwargs_keyword_names)`. Both are
    `IfExp` whose `body` is a `Call` to `dict`, so read `node.value.body.keywords` and take
    `kw.arg`. `_prove` each set is non-empty (the meta-guard) and
    `def prove_dp_wiring_keys(source)` `_prove`s they equal `set(DP_TRAIN_KEYS)` and
    `set(DP_KWARGS_KEYS)`. Comment must record WHY this is read by AST rather than retyped: plan
    22-10's own measurement, that `grad_accum_steps` appeared 9 times in `teach_persona.py`'s prose
    and 0 times in its code, so a wiring the measurement cannot see is worse than no wiring.

(h) `MATCHED_DIFFERENCES` — a tuple of dicts, each
    `{"difference", "disposition", "matched_quantity", "evidence"}`, declaring what REMAINS
    different between the matched comparator and the σ=0 arm after equalisation. At minimum: the
    `dp_noise_rng` checkpoint slot (item 5 above); the arm NAME and therefore the csv/checkpoint/
    adapter paths; the DP seam's own object graph (`DPSGD` constructed on one side, `None` on the
    other) whose ARITHMETIC is falsified as a difference by PROBE 2 but whose CODE PATH is not; and
    **the two end-of-run `masked_perplexity` sweeps**, which live in `teach_persona.train_arm`
    (:1705 and :1709) and therefore do NOT run for a comparator that calls `tp.train` directly — so
    `ppl_adapter_on`, `ppl_adapter_off`, `ppl_scored_targets`, `teaching_tokens`, `replay_tokens`
    and `replay_ratio` are ABSENT from the comparator's per-seed block where the OLD control record
    carries them. Disposition `declared-difference`: they are diagnostics measured AFTER training
    ends and they cannot move a reading, and adding them would spend scoring time the 23-17 budget
    does not hold.
    A required field `equalised` naming the three mechanisms this comparator DOES equalise —
    lot volume (65 windows vs 8, measured teaching-token exposure 1,689,600 vs 196,867 = 8.58x),
    teaching loss weight (1.0 vs `p = 2719/6262 = 0.4342`, 2.30x), and `grad_clip`.

(i) `def prove_first_attempt(tracked)` — `_prove(not tracked, ...)`; ONE ATTEMPT. The caller passes
    the result of `git ls-files MATCHED_ARTIFACT_GLOB`; this module runs no subprocess.
    **The message must state the guarantee at its TRUE strength, not a stronger one.** An
    overclaimed guarantee is precisely the defect this module exists to prevent, so the honest
    weaker sentence is mandatory:
      * this rule binds ACROSS COMMITS — once a matched artifact is TRACKED, no second protocol can
        be attempted without a visible deletion in git history;
      * it does NOT bind inside the UNCOMMITTED WINDOW between 23-17's run and 23-17's commit,
        because `.gitignore:17` ignores `data/` and `.gitignore:14` ignores `checkpoints/`, so an
        operator could delete `results/phase23_matched_control*`, the seed checkpoints and the
        `matched` section of `data/phase23_run_state.json` and leave no residue THIS FUNCTION can
        see (`data/phase23_run_state.json` is force-tracked despite `.gitignore:17` — which
        changes what a LATER READER can see, not what this function can; see the fourth bullet);
      * in that window 23-17's `prior_scored_seeds_at_start` refusal covers only HALF of that
        escape. It reads `data/phase23_run_state.json`'s `matched` section for SCORED seeds, so it
        refuses a delete that leaves that section INTACT (`scored == ['1337', ...]` with the record
        absent → refused). **A delete that ALSO removes that section — the fourth item in the list
        above — is PREVENTED BY NOTHING**: `prior` reads `{}`, `scored` reads `[]`, and a full
        second attempt is INDISTINGUISHABLE from a first AT RUN TIME. Both recorded lists read `[]`
        either way, so the artifact discloses nothing about which it was;
      * that full-delete case is **NOT PREVENTED, BUT AUDITABLE AFTER THE FACT** — and the message
        must carry BOTH halves, in the same breath. `data/phase23_run_state.json` is TRACKED as of
        `cfa2c87` (`git add -f`; tracking overrides `.gitignore:17`) and the committed baseline
        carries `control`, `cost`, `never_taught` and `sigma_zero` but NO `matched` section —
        measured. So once 23-17's same-session commit lands the `matched` section in history, a
        later deletion of it is a **VISIBLE DIFF** against that commit. **The honest limit:**
        tracking is NOT retroactive on its own — between the run and that commit a
        `git checkout -- data/phase23_run_state.json` reverts the working tree and leaves NO
        history at all, so auditability begins only AT THE COMMIT. The same-session commit
        requirement is therefore still load-bearing: it is now what CONVERTS the residual from
        invisible to auditable rather than being its only bound, and it is still a **DISCIPLINE,
        NOT A MECHANISM**. Do not write "closed", do not imply real-time prevention, and do not
        invent a new guard — an overclaimed guarantee is the exact defect this module exists to
        prevent, and this claim has already been overstated twice;
      * it is scoped to ONE filename glob: a comparator renamed to `results/phase23_rematch_*` is
        NOT refused by this function. What raises its cost is that this module is edit-once from
        23-17's first artifact, so a second comparator would have to arrive with a NEW
        pre-registration — that is VISIBLE, not REFUSED, and the message must say which of the two
        it is;
      * D-04's halt has no override flag and this rule has none either, and if the re-test breaches
        the FINDING is the deliverable.

(j) `SIGMA_ZERO_VISIBILITY_DISCLOSURE` — a multi-line string; and the **explicitly pinned**
    `VERDICT_REQUIRED_KEYS`, enumerated here rather than guessed downstream because this module is
    edit-once from 23-17's first artifact and a key added later cannot land. It is EXACTLY:
      `("record", "verdict", "verdict_rule", "halt_message", "reading", "control_readings",
        "central_reading", "central_reading_seed", "deviation", "floor", "floor_provenance",
        "sigma_zero_was_re_run", "sigma_zero_was_visible", "sigma_zero_visibility_disclosure")`
    Every name is drawn from what 23-19 already writes; 23-19 writes a superset and that is fine.
    Then `def prove_verdict_record_declares_visibility(record)` — `_prove` the record carries
    **every key in the whole `VERDICT_REQUIRED_KEYS` tuple**, naming the missing ones, and `_prove`
    `record["sigma_zero_was_visible"] is True`. Checking the whole tuple here is what puts the
    refusal in the MODULE rather than only in a test.
    And its sibling `def prove_control_record_declares_visibility(record)` — the same shape for
    23-17's control record, `_prove`ing `sigma_zero_was_visible is True` and
    `sigma_zero_visibility_disclosure` present and non-empty. It exists because
    `results/phase23_matched_control.json` is the artifact whose PROTOCOL was designed with
    `0.7837301587301587` on screen, and a disclosure that does not travel with the artifact it is
    about is one a reader of that artifact never sees.
    The disclosure text must state literally: the σ=0 reading `0.7837301587301587` was committed at
    `results/phase23_sigma_zero.json` BEFORE this comparator's protocol was designed; what remains
    blind is the reduction (`phase23_prereg.noise_floor`), the central-reading pin
    (`control_readings[0]`), the verdict (`phase23_prereg.sigma_zero_verdict`) and the seed ladder,
    all pinned at `c7de5d4`; what is NOT blind is the choice of which mechanisms to equalise, and
    that choice is pinned HERE while `git ls-files 'results/phase23_matched_*'` is empty and is
    limited by (i) — **with ALL FOUR of (i)'s named scope clauses restated, not omitted: the
    uncommitted window; the full-delete case NOTHING prevents in real time; that this same case is
    AUDITABLE AFTER THE FACT only FROM the same-session commit onward, because tracking is not
    retroactive and the commit is a discipline; and the one-glob scope**.

(k) `TRAIN_CALL_KEYS` and `def prove_train_call_keys(source)` — the ledger's third leg, and the one
    that covers what the other two do not. The two censuses above see `dp_fn`-conditioned branches
    and the two DP dicts; NEITHER sees the other 15 keywords of the production `train(...)` call. A
    future edit adding e.g. `extra_eval_fns=` at `teach_persona.py:1613` would silently un-match the
    comparator with both existing gates GREEN — the exact 23-08 failure shape, one level up.
    Declare `TRAIN_CALL_KEYS` as the frozen 21-name union measured in `<interfaces>`. Then
    `prove_train_call_keys(source)`: AST-find the `Call` to `train` inside `FunctionDef train_arm`,
    collect `{kw.arg for kw in call.keywords if kw.arg}`, resolve each `kw.arg is None` splat by
    looking up that name through `dp_wiring_key_census(source)` (`**dp_kwargs` -> the six keys), and
    `_prove` the union equals `set(TRAIN_CALL_KEYS)`, printing the symmetric difference both ways.
    `_prove` the named set is NON-EMPTY and that exactly one splat was seen — a walk that found no
    call, or a second splat this resolution cannot follow, must refuse rather than pass. Message
    must state that the comparator reproduces this set MINUS `{resume_from, dp_fn}` and that
    23-16's preflight is where that subtraction is checked. Roughly 15 lines.

(l) A `if __name__ == "__main__":` self-check using `_prove` (never `assert`, for the `-O` reason)
    that WATCHES each refusal fire against constructed inputs: a census with a planted extra branch
    refused; a census with a branch removed refused; a `dp_kwargs` key set missing a key refused; a
    `train(...)` call with an extra keyword refused by `prove_train_call_keys`; a non-empty `tracked`
    refused by `prove_first_attempt`; a verdict record missing one of `VERDICT_REQUIRED_KEYS`
    refused; a control record without `sigma_zero_was_visible` refused. Print a numbered line per
    case, `phase23_prereg.py`'s shape. Every input is CONSTRUCTED and labelled SYNTHETIC — no
    Phase-23 matched arm exists yet, which is the point of this module landing in wave 9.

Do NOT create, edit or import `scripts/phase23_prereg.py`, `scripts/mitigation_gate.py`,
`scripts/mitigation_accountant.py` or `scripts/mitigation_budget.py` from this module.
  </action>
  <verify>
    <automated>.venv/bin/python scripts/phase23_matched_prereg.py && .venv/bin/python -c "
import sys, pathlib, collections, ast
sys.path.insert(0, 'scripts')
import phase23_matched_prereg as m
src = pathlib.Path('src/personacore/training/loop.py').read_text()
c = m.prove_branch_ledger_complete(src)
assert sum(c.values()) == 7, c
assert len(m.DP_FN_BRANCH_DISPOSITIONS) == 7
tp_src = pathlib.Path('scripts/teach_persona.py').read_text()
m.prove_dp_wiring_keys(tp_src)
m.prove_train_call_keys(tp_src)
assert len(set(m.TRAIN_CALL_KEYS)) == 21, sorted(m.TRAIN_CALL_KEYS)
assert m.matched_arm(1337) == 'matched_control_seed1337'
assert len(m.VERDICT_REQUIRED_KEYS) == 14, m.VERDICT_REQUIRED_KEYS
t = ast.parse(pathlib.Path('scripts/phase23_matched_prereg.py').read_text())
assert not [n for n in ast.walk(t) if isinstance(n, ast.Assert)], 'ASSERT STATEMENT FOUND'
bad = [a.name for n in ast.walk(t) if isinstance(n, ast.Import) for a in n.names
       if a.name.split('.')[0] in {'torch','numpy'}]
bad += [n.module for n in ast.walk(t) if isinstance(n, ast.ImportFrom)
        and (n.module or '').split('.')[0] in {'torch','numpy'}]
assert not bad, bad
print('OK', sorted(c.items()))
" && git diff --exit-code -- scripts/phase23_prereg.py scripts/mitigation_gate.py scripts/mitigation_accountant.py</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python scripts/phase23_matched_prereg.py` exits 0 and prints one numbered line per
      watched refusal (at least 7).
    - `prove_branch_ledger_complete` on the REAL `src/personacore/training/loop.py` returns a Counter
      summing to exactly `7`, and `len(DP_FN_BRANCH_DISPOSITIONS) == 7`.
    - `prove_dp_wiring_keys` on the REAL `scripts/teach_persona.py` passes with
      `DP_TRAIN_KEYS == ("grad_accum_steps",)` and
      `set(DP_KWARGS_KEYS) == {"dp_fn","fact_bin","n_facts","replay_bin","replay_mask_bin","replay_windows"}`.
    - `prove_train_call_keys` on the REAL `scripts/teach_persona.py` passes and
      `len(set(TRAIN_CALL_KEYS)) == 21`.
    - `matched_arm(1337) == "matched_control_seed1337"`.
    - `MATCHED_GRAD_CLIP == 1e6`, `MATCHED_CONTROL_RECORD == "results/phase23_matched_control.json"`,
      `MATCHED_VERDICT_RECORD == "results/phase23_matched_verdict.json"`,
      `MATCHED_ARTIFACT_GLOB == "results/phase23_matched_*"`.
    - `VERDICT_REQUIRED_KEYS` has exactly the 14 names listed in (j), and
      `prove_verdict_record_declares_visibility` refuses a record missing ANY of them — not only the
      two visibility keys.
    - `prove_control_record_declares_visibility` exists and refuses a control record that omits
      `sigma_zero_was_visible` or sets it to anything but `True`.
    - **AST, not grep** — `ast.parse` of `scripts/phase23_matched_prereg.py` contains ZERO `Assert`
      nodes and ZERO `torch`/`numpy` imports. (An `assert`/`import` grep is measured on prose too;
      this file's docstrings discuss both words, so the gate reads the tree.)
    - `git diff --exit-code -- scripts/phase23_prereg.py scripts/mitigation_gate.py
      scripts/mitigation_accountant.py` exits 0.
  </acceptance_criteria>
  <done>The module exists, its self-check has been observed printing each refusal firing, all three
  AST gates pass against the real source files, and all three frozen files are byte-unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Guard the pin, and commit it BLIND</name>
  <files>tests/test_phase23_matched_prereg.py, scripts/phase23_matched_prereg.py</files>
  <read_first>
    - tests/test_phase23_prereg.py lines 28-60 (the `sys.path` insert + cross-file import register)
      and 340-410 (`_ordering_guard`, and the comment explaining why `globs=(artifact_glob,)` is
      passed rather than `V4_ARTIFACT_GLOBS`).
    - tests/test_phase20_prereg.py lines 185-240 (`_assert_ordering_holds`, its shallow-clone
      refusal, `adds[-1]` earliest-add and the `prereg != first_add` strictness conjunct).
    - scripts/phase23_matched_prereg.py — the module written in Task 1.
  </read_first>
  <action>
Create `tests/test_phase23_matched_prereg.py`. CPU-only, GPU-free, no torch, no network. Use
`tests/test_phase23_prereg.py`'s exact `sys.path` insert register (insert `scripts/` and `tests/`),
then `from test_phase23_prereg import _ordering_guard` and `from test_phase20_prereg import _git` —
IMPORTED AND CALLED, NEVER COPIED, for the reason `test_phase23_prereg.py:50-56` already records: a
lookalike copy would prove something about a different function than the one CI executes.

Tests to write:

1. `test_the_matched_prereg_precedes_every_matched_artifact` — call
   `_ordering_guard(prereg_artifact="scripts/phase23_matched_prereg.py",
   artifact_glob=phase23_matched_prereg.MATCHED_ARTIFACT_GLOB)`. Both values resolved from the
   module, never retyped. Docstring must state that this is GREEN-AND-VACUOUS today (the glob is
   empty) and becomes HARD from 23-17's first artifact, and that `_ordering_guard`'s closing
   `bool(checked) == bool(tracked)` is what stops the vacuity surviving the artifacts' arrival.

2. `test_the_branch_ledger_matches_the_live_loop` — run `prove_branch_ledger_complete` on the real
   `src/personacore/training/loop.py` read off disk. Assert the returned Counter sums to 7 and that
   the key `("_optimizer_step", "dp_fn is None")` is present with count 1 — that is the
   `clip_grad_norm_` branch 23-08 missed, named explicitly so a future reader sees it is watched.

3. `test_the_dp_wiring_keys_match_the_live_caller` — run `prove_dp_wiring_keys` on the real
   `scripts/teach_persona.py` read off disk.

4. `test_the_train_call_keys_match_the_live_caller` — run `prove_train_call_keys` on the real
   `scripts/teach_persona.py` and assert the resolved union has 21 names and contains both
   `resume_from` (which the comparator omits) and `dp_fn` (which is the point). Docstring names the
   gap this closes: the other two censuses cover `dp_fn` branches and the two DP dicts and would
   both stay GREEN while a 16th production keyword un-matched the comparator.

5. `test_a_planted_dp_fn_branch_is_refused` — WATCHED RED. Build a small synthetic source string
   containing the seven declared branches plus ONE extra `if dp_fn is None:` inside a function named
   `_optimizer_step`, and assert `prove_branch_ledger_complete` raises `SystemExit` whose message
   names the undeclared pair. A guard nobody has watched fail is not evidence.

6. `test_a_removed_dp_fn_branch_is_refused` — WATCHED RED, the other direction: the same synthetic
   source with one branch deleted, refused.

7. `test_a_dropped_dp_kwarg_is_refused` — WATCHED RED: a synthetic `teach_persona`-shaped source
   whose `dp_kwargs` dict omits `replay_windows`, refused.

8. `test_an_added_train_call_keyword_is_refused` — WATCHED RED: a synthetic `train_arm`-shaped source
   whose `train(...)` call carries one extra keyword (`extra_eval_fns=None`), refused by
   `prove_train_call_keys` with the new name in the message. This is the 23-08 failure shape
   reproduced deliberately.

9. `test_a_second_attempt_is_refused` — WATCHED RED: `prove_first_attempt(["results/phase23_matched_control.json"])`
   raises `SystemExit`, and `prove_first_attempt([])` returns without raising. Also assert the
   refusal message NAMES ALL FOUR of its scope clauses — (1) that it does not bind in the
   uncommitted window; (2) that inside that window a delete which ALSO removes the state file's
   `matched` section is PREVENTED BY NOTHING; (3) that this same case is nonetheless AUDITABLE
   AFTER THE FACT, because `data/phase23_run_state.json` is tracked with a `matched`-free
   baseline — but only FROM the same-session commit onward, since tracking is not retroactive and
   that commit is a DISCIPLINE, not a mechanism; (4) that it is scoped to one glob — because an
   overclaimed guarantee is the defect this module exists to prevent and the honesty of the
   wording is itself the deliverable. Assert (2) and (3) explicitly by name, and assert BOTH
   halves of (3) — the auditability AND its non-retroactive start point — because an
   auditability claim without its start point is the same overclaim in new clothes, and a test
   checking only three clauses passes against a message naming three of four, which is exactly
   how the previous overclaim survived a revision. This WIDENS test 9's assertions; it adds no
   test, so the file's test count is unchanged at 12.

10. `test_the_verdict_record_must_declare_every_required_key` — WATCHED RED three ways: a record
    missing `sigma_zero_was_visible` is refused; one setting it to `False` is refused; one missing
    any OTHER member of `VERDICT_REQUIRED_KEYS` (drive it over the whole tuple, dropping one key at
    a time) is refused. A complete record passes.

11. `test_the_control_record_must_declare_the_visibility` — WATCHED RED:
    `prove_control_record_declares_visibility` refuses a control record without
    `sigma_zero_was_visible`, refuses `False`, and passes a record carrying `True` plus a non-empty
    disclosure string.

12. `test_the_closed_preregistrations_are_untouched` — `_git("diff", "--stat", "c7de5d4", "HEAD",
    "--", "scripts/phase23_prereg.py")` returns an EMPTY string, and the same for
    `scripts/mitigation_gate.py` and `scripts/mitigation_accountant.py` against `HEAD` via
    `git diff --exit-code`-equivalent. This is the guard that makes "the new comparator is an INPUT,
    not a rule change" checkable.

Then run `ruff check . && ruff format --check .` and the full suite, and COMMIT both files in one
commit. The commit must land while `git ls-files 'results/phase23_matched_*'` still returns nothing
— record the command and its empty output in the SUMMARY, because that emptiness IS the blindness
claim.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase23_matched_prereg.py -q && .venv/bin/python -m pytest -q 2>&1 | tail -3 && .venv/bin/ruff check . && .venv/bin/ruff format --check .</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_phase23_matched_prereg.py -q` passes with at least 12
      tests collected.
    - The full suite reports `1500 passed, 1 skipped` (the pre-existing baseline `1488 passed,
      1 skipped` plus the 12 new tests). If the count differs, the SUMMARY records the delta and its
      cause rather than adjusting the expectation silently.
    - `.venv/bin/ruff check .` and `.venv/bin/ruff format --check .` both exit 0.
    - `git ls-files 'results/phase23_matched_*'` prints NOTHING at the moment of the commit, and the
      command plus its empty output are quoted in the SUMMARY.
    - `git diff --stat c7de5d4 HEAD -- scripts/phase23_prereg.py` prints nothing.
    - `git diff --exit-code -- scripts/mitigation_gate.py scripts/mitigation_accountant.py
      scripts/mitigation_budget.py` exits 0 — 23-18 is the plan that touches the budget, not this one.
  </acceptance_criteria>
  <done>Both files are committed in one commit; the ancestry guard is live and vacuity-safe; seven
  refusals have been watched RED; the emptiness of the matched glob at commit time is recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner/executor → published verdict | a protocol chosen with the σ=0 number visible crosses into a claim about DP-SGD |
| `loop.py` / `teach_persona.py` future edits → this ledger | a new `dp_fn` branch, or a new `train()` keyword, silently un-matches the comparator |
| the uncommitted window → the recorded first attempt | `checkpoints/` is gitignored and so is `data/`, but `data/phase23_run_state.json` is force-tracked (`cfa2c87`) — so a deleted-and-re-run attempt leaves no residue THIS FUNCTION can see, and none anywhere until 23-17's commit puts the `matched` section in history |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-60 | Tampering | the comparator protocol | mitigate | pinned in an edit-once module while `git ls-files 'results/phase23_matched_*'` is empty; `_ordering_guard` makes it a fact about git |
| T-23-61 | Repudiation | the difference ledger | mitigate | `prove_branch_ledger_complete` + `prove_dp_wiring_keys` + `prove_train_call_keys` — three AST censuses, not hand enumeration; all three watched RED |
| T-23-62 | Elevation of Privilege | a second attempt, ACROSS COMMITS | mitigate | `prove_first_attempt` refuses a non-empty tracked glob; no override parameter exists |
| T-23-62b | Elevation of Privilege | a second attempt inside the uncommitted window, `matched` state section LEFT INTACT | mitigate (in 23-17) | this module cannot see it — `data/`/`checkpoints/` are gitignored. 23-17's `prior_scored_seeds_at_start` refuses it: SCORED seeds recorded while the record is absent |
| T-23-62d | Elevation of Privilege | the same, with the `matched` state section ALSO DELETED | **mitigate (retroactive, from the commit) + disclose** | **PREVENTED BY NOTHING in real time** — `prior` reads `{}`, `scored` reads `[]`, and the case is indistinguishable from a first attempt at run time. What is new: `data/phase23_run_state.json` is TRACKED (`cfa2c87`) with a baseline carrying NO `matched` section, so once 23-17's same-session commit lands that section a later deletion is a VISIBLE DIFF — **auditable after the fact, not closed**. Tracking is not retroactive on its own: before that commit a `git checkout --` leaves no history, so the same-session requirement now CONVERTS the residual from invisible to auditable rather than being its only bound, and it is still a DISCIPLINE, not a mechanism. `prove_first_attempt`'s message and `SIGMA_ZERO_VISIBILITY_DISCLOSURE` state it at exactly that strength |
| T-23-62c | Elevation of Privilege | a RENAMED second comparator (`results/phase23_rematch_*`) | accept + disclose | not refused; the edit-once module raises its cost to "arrive with a new pre-registration", which is VISIBLE not REFUSED, and the message says which of the two it is |
| T-23-63 | Information Disclosure | the known σ=0 reading | accept + disclose | `sigma_zero_was_visible` is a REQUIRED key of BOTH records; `prove_verdict_record_declares_visibility` and `prove_control_record_declares_visibility` refuse its absence |
| T-23-SC | Tampering | npm/pip/cargo installs | mitigate | no package install in this plan; stdlib only |
</threat_model>

<verification>
- `.venv/bin/python scripts/phase23_matched_prereg.py` — the self-check prints each refusal firing.
- `.venv/bin/python -m pytest tests/test_phase23_matched_prereg.py -q` — 12+ passing.
- `.venv/bin/python -m pytest -q` — `1500 passed, 1 skipped` (or the delta explained).
- `git ls-files 'results/phase23_matched_*'` — empty at commit time.
- `git diff --stat c7de5d4 HEAD -- scripts/phase23_prereg.py` — empty.
</verification>

<success_criteria>
- Two new files, one commit, landing while the matched glob is empty.
- The seven-branch `loop.py` census, the seven-key `teach_persona.py` wiring census and the 21-name
  `train(...)` call census all pass against live source and all three are watched RED against
  planted defects.
- `MATCHED_GRAD_CLIP` (with its measured, exception-bounded inertness claim), both record paths, the
  full `VERDICT_REQUIRED_KEYS` tuple, the one-attempt rule stated at its true strength, and both
  visibility-disclosure refusals are pinned before any comparator reading exists.
- All three closed pre-registrations byte-unchanged.
- NO requirement is ticked. This plan closes no requirement and claims none: it makes a valid
  comparator possible, which is a precondition for CAL-01/CAL-02/CAL-05/CTRL-03, not a delivery of
  any of them.
</success_criteria>

<output>
Create `.planning/phases/23-cost-calibration-the-0-diagnostic-and-budget-pre-registratio/23-15-SUMMARY.md` when done.
</output>
