---
phase: 23-cost-calibration-the-0-diagnostic-and-budget-pre-registratio
plan: 06
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - tests/test_phase22_fakes.py
  - tests/test_phase23_mps_venue.py
autonomous: true
requirements: [DPSGD-06]

must_haves:
  truths:
    - "D-02: FULL RED-then-GREEN in the real execution environment. Each of the four DPSGD-04 fakes is re-applied to the REAL committed module WITH `device=\"mps\"`, observed RED in-process, and restored byte-identically. Phase 22's CPU-only result is recorded explicitly as 'not transferred to MPS', never as 'assumed equivalent'."
    - "D-02: the AST halves are EXEMPT and the exemption is WRITTEN INTO THE LEDGER, not silently skipped. `ast.parse` over source text has no device; `tests/test_phase22_dpsgd_ast.py` (16 tests) and `tests/test_phase22_accountant.py` (37 tests) remove 53 of 113 Phase-22 tests from the parametrization surface. A probe that claims a device pass it did not perform is exactly the defect D-02 exists to prevent."
    - "`_FAKE3_STD_RATIO_AT_N4 = 3.999986` is the single numeric constant most worth watching on MPS: FAKE 3's differential depends on the magnitudes of the DRAWN values, and those come off the seam's own generator on the model's device. If it moves outside its 0.01 band, the value is RE-RECORDED with both readings published — the band is NOT widened."
    - "D-01: the venue is local M3/MPS. This is the plan that converts 'the battery can run on MPS' (23-01) into 'the battery was watched failing on MPS'."
  artifacts:
    - path: "tests/test_phase22_fakes.py"
      provides: "the four fakes re-watched on MPS, a device column in the watched-RED ledger, and the AST-half exemption recorded with its measured count"
      contains: "_WATCHED_RED_NODE_IDS"
    - path: "tests/test_phase23_mps_venue.py"
      provides: "the venue-transfer ledger — per-probe device status, the two re-measured fitted constants, and the recorded skip count"
      contains: "def test_venue_transfer_ledger_is_recorded"
  key_links:
    - from: "tests/test_phase22_fakes.py::_WATCHED_RED_NODE_IDS"
      to: "the node ids observed reddening ON MPS"
      via: "a device-qualified register; a ledger citing a guard that has been renamed or deleted is this repository's most recurring defect class"
      pattern: "mps"
    - from: "tests/test_phase23_mps_venue.py"
      to: "the AST-half exemption"
      via: "named files and a measured count (53 of 113), so the exemption is stated rather than inferred from absence"
      pattern: "test_phase22_dpsgd_ast"
---

<objective>
Watch the four DPSGD-04 fake probes fail **on MPS**, and record what did and did not transfer as a
ledger rather than as a claim.

Purpose: DPSGD-04's deliverable was never "the tests pass" — it was the OBSERVED RED against the real
mutated module. That observation was made on CPU. D-02 requires it on the venue that will produce the
published ε, in full, and requires the parts that genuinely cannot differ by device to be recorded as
exempt rather than quietly skipped. 23-01 made the battery capable of running on MPS; this plan is
the observation.

Output: the four MPS RED observations, a device-qualified watched-RED register, a re-measurement of
the two fitted constants on MPS, and a venue-transfer ledger with the skip count as a number.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-cost-calibration-the-0-diagnostic-and-budget-pre-registratio/23-CONTEXT.md
@.planning/phases/23-cost-calibration-the-0-diagnostic-and-budget-pre-registratio/23-RESEARCH.md
@.planning/phases/23-cost-calibration-the-0-diagnostic-and-budget-pre-registratio/23-VALIDATION.md
</context>

<environment>
**Python 3.11 venv is MANDATORY.** `.venv/bin/python -m pytest ...`. This plan MUST be executed on
the M3 — every observation it produces is a device observation, and a CPU-only run produces none of
them.

**FROZEN — no write:** `scripts/mitigation_accountant.py`, `scripts/mitigation_gate.py`.
`src/personacore/privacy/dpsgd.py` is mutated **transiently** by the existing `_mutate` /
`_run_live_guard` register and must be byte-identical afterwards.
</environment>

<tasks>

<task type="auto">
  <name>Task 1: The four fakes, re-applied on MPS and observed RED</name>
  <files>tests/test_phase22_fakes.py</files>
  <read_first>
    - `tests/test_phase22_fakes.py:71-108` — `_real_source()`, `_mutate(source, target, replacement)`,
      `_run_live_guard(monkeypatch, tmp_path, source, guard, **kwargs)`. **This is the register that
      applies a mutation to the REAL committed module and observes the RED in-process; reuse it,
      do not write a second one**
    - `tests/test_phase22_fakes.py:191` `test_fake_averaged_gradient` (FAKE 1),
      `:324` `test_fake_wrong_sensitivity` (FAKE 2),
      `:492` `test_fake_noise_after_averaging` (FAKE 3),
      `:607` `test_fake_rng_reuse` (FAKE 4)
    - `tests/test_phase22_fakes.py:173-175` and `:459-469` — the two fitted constants and their bands
    - `tests/test_phase23_mps_venue.py` — `_DEVICES` / `_MPS_SKIP` from 23-01
    - `23-RESEARCH.md` §R1.4 — the per-probe interaction table: FAKE 1 blocked only by `_record`'s
      grad assignment; FAKE 2 lowest-risk (no generator); **FAKE 3 depends on the magnitudes of the
      drawn values, which come off `dp._g` on the model's device**; FAKE 4 rides the 44-byte CPU
      state tensors and works as written
  </read_first>
  <action>
Parametrize the four fake tests over `_DEVICES` (imported from `test_phase23_mps_venue`), threading
`device` into `_model(device=device)`, `_seam(..., runtime=RuntimeConfig(device=device))` and the
`_lot_sum` / `_released` / `_two_steps` helpers. `_record` already reads the device off `p.device`
after 23-01, so no call site passes a device.

For each fake, the MPS leg must perform the SAME two acts the CPU leg performs: apply the mutation to
the real committed `src/personacore/privacy/dpsgd.py` source via `_mutate`, run the guard through
`_run_live_guard`, observe the RED, and confirm the restore. Do not add a new mutation register.

**FAKE 3 is the one to watch, and its handling is prescribed rather than left open.** Run the
existing `_FAKE3_DIFFERENTIAL_SEES` parametrization on MPS and measure the realised std ratio at
`n_records = 4`.
  - If it lands inside `_FAKE3_STD_RATIO_AT_N4 ± _FAKE3_STD_RATIO_BAND` (3.999986 ± 0.01), record the
    measured MPS value beside the CPU one in a comment and leave both constants byte-unchanged.
  - If it lands outside, **RE-RECORD the value with both readings published and the device named on
    each. Do NOT widen the band.** `23-RESEARCH.md` Assumption A1 commits to this disposition in
    advance, so taking it is not a decision made after seeing the number.
  In either case the SUMMARY quotes the measured MPS ratio as a literal.

Do the same for `_FAKE1_LEAK_RATIO = 1.734481` (band 0.02): quote the realised MPS ratio as a
literal. `_record` keeps the CPU draw, so the expectation is bit-identity — **state the expectation
and then state what was measured**, in that order, so a divergence is visible rather than absorbed.

Also record the third measured transfer property as a committed assertion rather than a research
note: `_global_norm` over the 72-tensor LoRA-shaped fixture came back **bit-identical** between CPU
and MPS (`0.4707888662815094`). Add `test_global_norm_is_bit_identical_across_devices` asserting
`torch.equal` (or exact `==` on the scalar) between the CPU and MPS reductions of the same fixture,
gated by `_MPS_SKIP`. Its docstring must state the honest bound: **this is one fixture, not a proof**
— fp32 reductions can differ in the last ULPs by reduction order, and the two dependent probes carry
~1% bands against a divergence bounded well below that.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase22_fakes.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_phase22_fakes.py -v` exits 0 on the M3 with **zero skipped**; the literal `N passed, M skipped` line is quoted in the SUMMARY with `M == 0`
    - `.venv/bin/python -m pytest tests/test_phase22_fakes.py -k "fake_ and mps" -v` collects at least four cases and all pass
    - The SUMMARY quotes the measured MPS values for `_FAKE1_LEAK_RATIO` and `_FAKE3_STD_RATIO_AT_N4` as literals, each with the CPU value beside it and the delta stated
    - `git diff --exit-code -- src/personacore/privacy/dpsgd.py` exits 0 — every mutation was transient
    - `.venv/bin/python -m pytest "tests/test_phase22_fakes.py::test_global_norm_is_bit_identical_across_devices" -v` exits 0 and its docstring states the one-fixture bound
    - `.venv/bin/python -m pytest tests/ -q` reports 0 failed
  </acceptance_criteria>
  <done>All four fakes were observed RED on MPS against the real committed module, both fitted constants were re-measured with their MPS values published, and `dpsgd.py` is byte-unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: A device column in the watched-RED ledger, and the AST exemption recorded</name>
  <files>tests/test_phase22_fakes.py</files>
  <read_first>
    - `tests/test_phase22_fakes.py:697-760` — `_LEDGER_FAKES`, `_WATCHED_RED_NODE_IDS`,
      `_DISTINCT_RED_SIGNATURES = 9`, `test_every_fake_has_at_least_two_independent_detectors`,
      `test_watched_red_node_ids_resolve`
    - `tests/test_phase22_fakes.py:797-860` — `_LEDGER_SIGN_OFF_ITEMS`, `test_fakes_ledger_is_recorded`,
      `test_fakes_ledger_names_its_blind_spots`. **The sign-off's honest half, by name: a sign-off
      that lists only green is not a sign-off**
    - `tests/test_phase22_fakes.py:801` — the existing `rng["mps"]` sign-off row, currently a string
      literal describing a CPU-only blind spot
  </read_first>
  <action>
**(a) Device-qualify the register.** Change `_WATCHED_RED_NODE_IDS`'s values so each node id carries
the device it was observed reddening on — either by appending the parametrization id (which pytest
already produces, e.g. `...[mps-...]`) or by making the value a mapping `{device: (node_ids, ...)}`.
Choose whichever keeps `test_watched_red_node_ids_resolve` able to check that every registered id
still resolves; that test is the anti-stale-anchor guard and must keep working.

Update `_DISTINCT_RED_SIGNATURES` only if the observation genuinely produced a different count, and
if so record the new count with the measurement rather than adjusting it to make a test pass. The
existing comment's rule applies unchanged: **if two fakes ever trip the same guard with the same
message that is a COVERAGE GAP to name, not a second win to count.**

**(b) Record the exemption.** Add `_DEVICE_INVARIANT_HALVES` — a tuple naming
`tests/test_phase22_dpsgd_ast.py` (16 tests) and `tests/test_phase22_accountant.py` (37 tests), with
the measured statement that **53 of 113 Phase-22 tests are outside the parametrization surface**
because `ast.parse` over source text has no device and the accountant imports stdlib `math` only.
Add `test_the_device_invariant_halves_are_named_and_still_device_free`: assert each named file
contains **zero** `"cpu"`/`"mps"` device literals and imports no torch runtime at module scope (AST
scan). The exemption then rests on a re-checked property rather than on a sentence.

**(c) Extend the sign-off's honest half.** Add rows to `_LEDGER_SIGN_OFF_ITEMS` for the two facts
this phase learned and must not lose:
  - that the four fakes' **AST halves** were NOT re-run on MPS because they cannot differ by device,
    with the literal the SUMMARY must contain;
  - that `_FAKE3_STD_RATIO_AT_N4` was re-measured on MPS, with the literal naming the outcome.
Also **correct the existing `rng["mps"]` row**: after 23-01 and this plan the MPS legs are exercised
on the M3, so the row's claim of "required-but-UNEXERCISED in CPU-only CI" is now precise about CI
specifically rather than about the suite. Rewrite it to say exactly that — the slot is exercised on
the M3 and remains unexercised in CI — rather than deleting the honest disclosure.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase22_fakes.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest "tests/test_phase22_fakes.py::test_watched_red_node_ids_resolve" -v` exits 0 — every device-qualified node id still resolves
    - `.venv/bin/python -m pytest "tests/test_phase22_fakes.py::test_every_fake_has_at_least_two_independent_detectors" -v` exits 0
    - `.venv/bin/python -m pytest "tests/test_phase22_fakes.py::test_the_device_invariant_halves_are_named_and_still_device_free" -v` exits 0
    - `grep -n "53" tests/test_phase22_fakes.py` matches — the exemption's measured count is in the source
    - `grep -n "_LEDGER_SIGN_OFF_ITEMS" -A 12 tests/test_phase22_fakes.py` shows the two new rows and the rewritten `rng["mps"]` row
    - `.venv/bin/python -m pytest tests/ -q` reports 0 failed
  </acceptance_criteria>
  <done>The watched-RED register names the device each observation was made on, the AST exemption is asserted by a re-checked property with its measured count, and the sign-off's honest half gained the two facts this venue transfer produced.</done>
</task>

<task type="auto">
  <name>Task 3: The venue-transfer ledger, with the skip count as a number</name>
  <files>tests/test_phase23_mps_venue.py</files>
  <read_first>
    - `tests/test_phase23_mps_venue.py` — current state after 23-01
    - `tests/test_phase22_fakes.py:805-860` — `_summary_text()` and the two ledger tests. **The shape:
      skip gracefully while the SUMMARY does not exist, then assert hard once it does — a ledger that
      can silently go missing is not a ledger**
    - `.planning/phases/22-dp-sgd-core-accountant-and-the-correctness-battery/22-07-SUMMARY.md` — the
      CPU-only kill→resume proof D-02 re-watches, and its 5,056-bytes-in-both-columns table
    - `23-RESEARCH.md` §R1.3 — the measured transfer table this ledger converts into rows
  </read_first>
  <action>
Add `test_venue_transfer_ledger_is_recorded` to `tests/test_phase23_mps_venue.py`, following
`test_fakes_ledger_is_recorded`'s shape exactly: read this plan's SUMMARY, skip gracefully while it
does not exist, and assert hard once it does.

Required literals in the SUMMARY, each as its own assertion so a failure names which one is missing:

  1. `not transferred to MPS` — the phrase D-02 requires for Phase 22's CPU-only result. It must
     appear as a statement ABOUT the Phase-22 record, alongside the new MPS observations. **Never
     "assumed equivalent".**
  2. A per-probe status table with one row for each of: V-15 kill→resume ε bit-identity, FAKE 1,
     FAKE 2, FAKE 3, FAKE 4 — each naming the device the RED was observed on.
  3. `AST half` / `device-invariant` — the exemption, with the 53-of-113 count.
  4. The literal skip count line from
     `.venv/bin/python -m pytest tests/test_phase22_checkpoint.py tests/test_phase22_dpsgd.py
     tests/test_phase22_fakes.py tests/test_phase23_mps_venue.py -q`, with the skip number stated
     explicitly. **A pass count with no skip count beside it is the warning sign
     `23-RESEARCH.md` Pitfall 1 names, and this assertion is what makes it impossible.**
  5. The two re-measured fitted constants with their CPU and MPS readings and the delta.
  6. The 5,056 B / 44 B divergence quoted with both devices named, so no future reader repeats
     22-07-SUMMARY.md's table (which shows 5,056 in both columns because both were CPU).

Add a second assertion, `test_the_ledger_states_a_skip_count_of_zero`: parse the recorded skip line
out of the SUMMARY and assert the number is `0`. A ledger that records a non-zero skip count on the
M3 is recording the exact failure mode D-02 exists to prevent, and the assertion should say so.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase23_mps_venue.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_phase23_mps_venue.py -v` exits 0 with zero skips once the SUMMARY is written
    - `grep -c "not transferred to MPS" .planning/phases/23-*/23-06-SUMMARY.md` returns ≥ 1, and `grep -c "assumed equivalent" .planning/phases/23-*/23-06-SUMMARY.md` returns **0**
    - `grep -n "skipped" .planning/phases/23-*/23-06-SUMMARY.md` shows the literal pytest line with a skip count of `0`
    - `grep -n "5,056\|5056" .planning/phases/23-*/23-06-SUMMARY.md` and `grep -n "44" .planning/phases/23-*/23-06-SUMMARY.md` both match, each with its device named
    - `.venv/bin/python -m pytest tests/ -q` reports 0 failed; `make lint` exits 0
    - `git diff --exit-code -- src/personacore/privacy/dpsgd.py scripts/mitigation_accountant.py scripts/mitigation_gate.py` exits 0
  </acceptance_criteria>
  <done>The venue transfer is a ledger with per-probe device rows, a stated exemption, both re-measured constants, and a skip count asserted to be zero — enforced by a test rather than by a promise.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CPU-observed RED → MPS-published ε | DPSGD-04's deliverable is the observed failure, and it was observed on the wrong device for what this milestone publishes. |
| SUMMARY prose → enforceable evidence | A ledger nothing asserts against can silently lose the honest half. |
| a fitted constant → a device change | FAKE 1 and FAKE 3 assert on numbers fitted to CPU draws. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-28 | Repudiation | a probe claiming a device pass it did not perform | mitigate | Each of the four fakes is mutated and observed RED with `device="mps"` through the existing `_run_live_guard` register; the exemption is asserted by an AST property, not by absence |
| T-23-29 | Tampering | a band widened to absorb an MPS divergence | mitigate | The disposition was committed in advance (`23-RESEARCH.md` A1): re-record both readings, never widen. Acceptance criteria require both literals in the SUMMARY |
| T-23-30 | Repudiation | a green suite on the M3 that skipped the venue tests | mitigate | `test_the_ledger_states_a_skip_count_of_zero` parses the recorded line and asserts `0` |
| T-23-31 | Tampering | a transient mutation surviving into the commit | mitigate | `git diff --exit-code -- src/personacore/privacy/dpsgd.py` in every task's acceptance criteria |
| T-23-32 | Repudiation | the honest `rng["mps"]` disclosure deleted rather than made precise | mitigate | The row is REWRITTEN to distinguish CI from the M3, and `test_fakes_ledger_names_its_blind_spots` still requires the literal |
| T-23-SC | Tampering | npm/pip/cargo installs | accept | **Zero installs.** `pyproject.toml` byte-unchanged (RPT-03) |
</threat_model>

<verification>
```bash
.venv/bin/python -m pytest tests/test_phase22_fakes.py -v
.venv/bin/python -m pytest tests/test_phase23_mps_venue.py -v
.venv/bin/python -m pytest tests/test_phase22_checkpoint.py tests/test_phase22_dpsgd.py tests/test_phase22_fakes.py tests/test_phase23_mps_venue.py -q   # record "N passed, M skipped"
git diff --exit-code -- src/personacore/privacy/dpsgd.py scripts/mitigation_accountant.py scripts/mitigation_gate.py pyproject.toml
.venv/bin/python -m pytest tests/ -q
make lint
```
</verification>

<success_criteria>
- All four DPSGD-04 fakes observed RED on MPS against the real committed module, then restored byte-identically
- Both fitted constants re-measured on MPS with CPU and MPS readings published side by side
- The watched-RED register is device-qualified and every node id still resolves
- The AST exemption is asserted by a re-checked property with its measured count in the source
- The venue-transfer ledger is enforced by tests, carries "not transferred to MPS", never "assumed equivalent", and states a skip count of zero
</success_criteria>

<output>
Create `.planning/phases/23-cost-calibration-the-0-diagnostic-and-budget-pre-registratio/23-06-SUMMARY.md` when done.
</output>
