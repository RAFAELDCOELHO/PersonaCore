---
phase: 23-cost-calibration-the-0-diagnostic-and-budget-pre-registratio
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_phase23_mps_venue.py
  - tests/test_phase22_dpsgd.py
  - tests/test_phase22_fakes.py
  - tests/test_phase22_checkpoint.py
autonomous: true
requirements: [DPSGD-06]

must_haves:
  truths:
    - "D-01: the execution venue is local M3/MPS. Every device-dependent property this milestone publishes an ε against is exercised on MPS, not inherited from a CPU pass."
    - "D-02 (half 1 of 2): the Phase-22 battery is CPU-only BY DESIGN — `tests/test_phase22_fakes.py` has ZERO `\"cpu\"` literals and no device plumbing at all. D-02 therefore means device-PARAMETRIZING probes written CPU-only, then running them on MPS. This plan does the parametrization; 23-06 does the watched RED."
    - "DPSGD-06 keystone: `torch.normal(std=0.0)` on an MPS generator returns exact zeros AND ADVANCES the 44-byte state. `dpsgd.py`'s `torch.equal(pre, post)` refusal was measured on CPU; if the MPS state did not advance, the milestone's first real run would refuse at every step."
    - "The MPS leg is a COUNTABLE SKIP, never a silent absence — `pytest.param(\"mps\", marks=skipif(...))`, not a shrinking `_DEVICES` list. A green CI run that skipped the venue tests is the exact failure mode D-02 exists to prevent, and a skip can be counted while an absent param cannot."
    - "The cross-device generator-state refusal is WATCHED, not assumed: 5,056 B CPU vs 44 B MPS, mutually refused with named error text."
  artifacts:
    - path: "tests/test_phase23_mps_venue.py"
      provides: "the shared `_DEVICES` / `_MPS_SKIP` register, the DPSGD-06 generator keystone, the cross-device refusal watch"
      contains: "def test_sigma_zero_advances_the_mps_generator"
    - path: "tests/test_phase22_dpsgd.py"
      provides: "`_model(*, freeze=True, device=\"cpu\")` — additive widening whose default is byte-identical to today"
      contains: "device="
    - path: "tests/test_phase22_fakes.py"
      provides: "`_record` writing CPU-drawn gradients onto a device-resident parameter via `.to(device)`"
      contains: "def _record"
    - path: "tests/test_phase22_checkpoint.py"
      provides: "V-15 `test_resume_epsilon_bit_identical` parametrized over cpu and mps"
      contains: "def test_resume_epsilon_bit_identical"
  key_links:
    - from: "tests/test_phase22_fakes.py::_record"
      to: "the CPU generator"
      via: "the draw STAYS on CPU and only the tensor moves (`g.to(device)`), so `_FAKE1_LEAK_RATIO`/`_FAKE3_STD_RATIO_AT_N4` stay valid by construction"
      pattern: "\\.to\\("
    - from: "tests/test_phase22_checkpoint.py"
      to: "tests/test_phase23_mps_venue.py::_DEVICES"
      via: "one shared device register, imported — two copies of a device gate drift"
      pattern: "from test_phase23_mps_venue import"
---

<objective>
Make the Phase-22 correctness battery capable of running on the venue that will produce the
published ε, and commit the one MPS property whose failure would abort this phase's headline run.

Purpose: `tests/test_phase22_checkpoint.py:3` reads verbatim *"CPU-only, GPU-free, no network. One
MPS-touching test is `skipif`-gated."* and `tests/test_phase22_fakes.py` passes no device anywhere —
`grep -c '"cpu"'` returns **0**. Phase 22's kill→resume bit-identity proof and its four fake probes
were all watched on CPU, and the DP generator's state is **5,056 bytes on CPU and 44 bytes on MPS**,
mutually refused across devices. That proof does not transfer, and D-02 forbids recording it as
"assumed equivalent". This plan builds the plumbing; **23-06 performs the watched RED**.

Output: `tests/test_phase23_mps_venue.py` (the shared device register + the DPSGD-06 keystone +
the cross-device refusal watch), and three device-widened helper fixtures in the Phase-22 battery.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-cost-calibration-the-0-diagnostic-and-budget-pre-registratio/23-CONTEXT.md
@.planning/phases/23-cost-calibration-the-0-diagnostic-and-budget-pre-registratio/23-RESEARCH.md
@.planning/phases/23-cost-calibration-the-0-diagnostic-and-budget-pre-registratio/23-VALIDATION.md
</context>

<environment>
**Python 3.11 venv is MANDATORY.** Commands run as `.venv/bin/python -m pytest ...`. Verified live:
Python **3.11.15**, torch **2.7.1**, `torch.backends.mps.is_available()` → **True**.

**CI is `ubuntu-latest`, CPU wheel** (`.github/workflows/ci.yml:6,36`). Every MPS leg added here MUST
be `skipif`-gated or CI goes red.

**`checkpoints/` is gitignored.** No test may make an on-disk artifact a precondition.

**FROZEN — no file in this plan may name them in a write:** `scripts/mitigation_accountant.py`,
`scripts/mitigation_gate.py`.
</environment>

<tasks>

<task type="auto">
  <name>Task 1: `tests/test_phase23_mps_venue.py` — the shared device register and the DPSGD-06 keystone</name>
  <files>tests/test_phase23_mps_venue.py</files>
  <read_first>
    - `tests/test_mps_smoke.py:1-30` — the module-level `pytestmark` MPS gate precedent and the
      paragraph-length `reason=` discipline this repo enforces
    - `tests/test_phase22_checkpoint.py:660-690` — `_v3_case`'s
      `pytest.param(..., marks=pytest.mark.skipif(...))` register. Its `reason=` names **the
      non-skipping test that still carries the guarantee**; copy that shape
    - `src/personacore/privacy/dpsgd.py` — `DPSGD.__init__(model, *, sigma, clip_norm, seed, runtime)`,
      `noise_rng_state()`, `load_noise_rng_state()`, the private generator `_g`, and the
      `torch.equal(pre, post)` refusal whose comment reads *"At sigma = 0 the values are exact zeros
      BUT the state still moves (measured, torch 2.7.1)"* — **that measurement was taken on CPU**
    - `src/personacore/config.py:56-59` — `RuntimeConfig.__post_init__` forces `amp=False` for
      `cpu`/`mps`, so D-04 refusal 2 (live scaler) stays inert on MPS exactly as on CPU
    - `23-RESEARCH.md` §R1.3, §R1.4, §R1.5 — the measured transfer table, the exact cross-device
      error texts, and the keystone measurement this task converts into a committed test
  </read_first>
  <action>
Create `tests/test_phase23_mps_venue.py`. Module docstring must state, in the repo's register: this
file is the **single source of device truth for Phase 23's test battery**; the Phase-22 battery is
CPU-only by design and D-02 requires the transfer to be exercised rather than assumed; and CI is
`ubuntu-latest` so the MPS leg is a gated SKIP that the phase gate COUNTS.

**(a) The shared register.** Define `_MPS_AVAILABLE = torch.backends.mps.is_available()` and
`_MPS_SKIP = pytest.mark.skipif(not _MPS_AVAILABLE, reason=<paragraph>)`. The `reason=` string must
name: that CI is `ubuntu-latest` and CPU-only; that D-01 makes MPS the venue producing the published
ε; and which non-skipping CPU leg still carries the property when this skips.

Define `_DEVICES = (pytest.param("cpu"), pytest.param("mps", marks=_MPS_SKIP))`.

**This shape is chosen over a shrinking list and the reason must be written into the file.** A
`_DEVICES = ["cpu"] + (["mps"] if available else [])` register makes the MPS leg VANISH on CI rather
than SKIP, and Pitfall 1 in `23-RESEARCH.md` is that the phase gate must be able to count the skips.
An absent parametrization cannot be counted; a skipped one can.

**(b) The DPSGD-06 keystone — `test_sigma_zero_advances_the_mps_generator`.** Parametrized over
`_DEVICES`. Build a real `DPSGD` over `_model()`-shaped parameters (or a bare
`torch.Generator(device=device)` seeded identically — either is acceptable, but the assertion must
read the SEAM's own generator via `noise_rng_state()` so the property is about the mechanism that
ships). Then, at `std=0.0`:
  - capture `pre = dp.noise_rng_state()`
  - draw `torch.normal(mean=0.0, std=0.0, size=(4608,), device=device, generator=dp._g)`
  - capture `post = dp.noise_rng_state()`
  - assert every drawn value is **exactly** `0.0` (use `torch.count_nonzero(...) == 0`, not `allclose`)
  - assert `not torch.equal(pre, post)` — **the state ADVANCED**

Add a second parametrization over sizes `(1, 2, 4, 8, 16, 4608)` so the property is not asserted at
one width only; research measured it at all six.

The docstring must name the consequence: `dpsgd.py`'s continuity refusal reads
`torch.equal(pre, post)` and refuses when the state did not move. **DPSGD-06 makes σ=0 the DP arm's
first executed run on MPS.** If this property failed on MPS the first real run of the milestone would
refuse at every step, and the failure would look like a DP bug rather than a venue fact.

**(c) The cross-device refusal watch — `test_generator_state_is_mutually_refused_across_devices`.**
Gated by `_MPS_SKIP`. Assert:
  - a CPU `torch.Generator`'s `get_state()` has `numel() == 5056`, an MPS generator's has
    `numel() == 44`, and **both live on CPU** (so every `torch.equal(state_a, state_b)` in
    `dpsgd.py` and in the tests compares two CPU tensors and is device-safe as written — assert
    `state.device.type == "cpu"` for both)
  - `cpu_gen.set_state(mps_state)` raises `RuntimeError` whose message contains `5056`
  - `mps_gen.set_state(cpu_state)` raises `RuntimeError` whose message contains `wrong size`

Do NOT assert the full error strings — assert the discriminating substrings, so a torch patch
release rewording the message does not redden a property that still holds.

**(d) The MPS round-trip — `test_mps_generator_state_round_trips_fresh_and_midstream`.** Gated.
`torch.save` the state to `tmp_path`, `torch.load` it back, `set_state`, and assert both the state
BYTES and the NEXT DRAW are equal — from a fresh seed **and** after advancing the stream 10 draws.
This is D-07's mechanism; 23-07 wires it and this test is what makes the wiring rest on a watched
property rather than on a research note.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase23_mps_venue.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_phase23_mps_venue.py -v` exits 0 with **zero skipped** on the M3 (record the exact `N passed, M skipped` line; `M` must be `0`)
    - `grep -n 'pytest.param("mps"' tests/test_phase23_mps_venue.py` matches, and `grep -c 'is_available()' tests/test_phase23_mps_venue.py` is ≥ 1
    - `grep -n "count_nonzero\|== 0" tests/test_phase23_mps_venue.py` shows the zeros assertion is EXACT, and `grep -c "allclose" tests/test_phase23_mps_venue.py` returns 0
    - `grep -n "not torch.equal" tests/test_phase23_mps_venue.py` matches — the advance is asserted as a NEGATED equality, the same predicate `dpsgd.py` refuses on
    - `.venv/bin/python -m pytest tests/test_phase23_mps_venue.py -q -p no:cacheprovider --co -q | grep -c mps` is ≥ 4 (the register really produces MPS params)
    - `.venv/bin/python -m pytest tests/ -q` reports 0 failed
  </acceptance_criteria>
  <done>The keystone, the cross-device refusal and the mid-stream round-trip are committed tests that run GREEN on MPS with zero skips on the M3, and the `_DEVICES` register exists in exactly one place.</done>
</task>

<task type="auto">
  <name>Task 2: Device-widen `_model`, `_record` and the `RuntimeConfig(device=)` sites</name>
  <files>tests/test_phase22_dpsgd.py, tests/test_phase22_fakes.py</files>
  <read_first>
    - `tests/test_phase22_dpsgd.py:121` — `_model(*, freeze=True)`. Takes no device, never calls
      `.to()`. **Blast radius: 31 tests across two files** (`tests/test_phase22_fakes.py:46-50`
      imports `_FROZEN_PARAMS`, `_GRAD_SCALE`, `_model`)
    - `tests/test_phase22_fakes.py:118-127` — `_record`. Draws on a **CPU** generator and assigns
      `p.grad = g`, which raises on an MPS parameter:
      `RuntimeError: attempting to assign a gradient with device type 'cpu' to a tensor with device
      type 'mps'` (measured)
    - `tests/test_phase22_fakes.py:173-175` — `_FAKE1_HONEST_RATIO_MAX`, `_FAKE1_LEAK_RATIO =
      1.734481`, `_FAKE1_LEAK_BAND = 0.02`
    - `tests/test_phase22_fakes.py:459-469` — `_FAKE3_DIFFERENTIAL_SEES`, `_FAKE3_STD_RATIO_AT_N4 =
      3.999986`, `_FAKE3_STD_RATIO_BAND = 0.01`
    - `23-RESEARCH.md` §R1.1(b) and Pitfall 4 — why the draw must STAY on CPU
    - `tests/test_phase23_mps_venue.py` — the `_DEVICES` / `_MPS_SKIP` register created in Task 1
  </read_first>
  <action>
**(a) `_model`.** Widen to `_model(*, freeze=True, device="cpu")` and add `model.to(device)` after
`inject_lora` / `mark_only_lora_trainable`. **The default keeps every existing call byte-identical** —
the same `dp_sigma=None` sentinel shape `train_arm` already uses and whose reasoning
`teach_persona.py`'s docstring records. Say so in the docstring, and state the blast radius (31
tests) as a measured number rather than leaving it to be rediscovered.

**(b) `_record` — the load-bearing half.** Change `p.grad = g if p.grad is None else p.grad + g` to
move the tensor rather than re-draw it: keep `gen = torch.Generator().manual_seed(seed)` and
`g = torch.randn(p.shape, generator=gen) * _GRAD_SCALE` **on CPU**, then write
`g_dev = g.to(p.device)` and accumulate with `g_dev`. Signature stays `_record(params, seed)` —
the device is read off `p.device`, so no call site changes and there is nothing to pass wrong.

**Write the reason into the docstring**: `_FAKE1_LEAK_RATIO = 1.734481` (band `0.02`) and
`_FAKE3_STD_RATIO_AT_N4 = 3.999986` (band `0.01`) are FITTED constants. `torch.Generator(device=...)`
inside `_record` would draw **different gradient values** on MPS and put both constants at risk for a
reason that has nothing to do with the fake — a failure that would read as a fake detection. Keeping
the CPU draw makes the bytes identical to the CPU run and the constants valid **by construction**.
Add an assertion in `_record` that the returned tensors and `p.grad` share `p.device`, so a future
edit that reintroduces a device-local generator is caught here rather than in a band.

**(c) The `RuntimeConfig(device="cpu")` sites.** In `tests/test_phase22_dpsgd.py` there are **6**;
parametrize each over `_DEVICES` (imported from `test_phase23_mps_venue`) and thread `device` into
both `_model(device=device)` and `RuntimeConfig(device=device)`. Where a test's assertion is a pure
AST/source scan, do **not** parametrize it — see (d).

**(d) The exemption, written down rather than silently skipped.** `tests/test_phase22_dpsgd_ast.py`
(16 tests) and `tests/test_phase22_accountant.py` (37 tests) are `ast.parse` over source text and
stdlib `math` respectively; neither imports a torch runtime and neither can differ by device.
**Excluding them removes 53 of the 113 Phase-22 tests from the parametrization surface.** Record that
as a comment at the top of `tests/test_phase22_fakes.py`'s parametrization block naming both files
and the count. A probe that claims a device pass it did not perform is exactly the defect D-02 exists
to prevent; the exemption must be stated, not inferred from absence.

Do not touch `torch.load(..., map_location="cpu")` — 4 sites, all correct as written:
`load_checkpoint` handles placement and the RNG states are CPU tensors anyway.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase22_dpsgd.py tests/test_phase22_fakes.py tests/test_phase22_dpsgd_ast.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_phase22_dpsgd.py tests/test_phase22_fakes.py -v` exits 0 with **zero skipped** on the M3; record the exact `N passed, M skipped` line
    - `grep -n 'def _model' tests/test_phase22_dpsgd.py` shows `device="cpu"` as a keyword default
    - `grep -c 'torch.Generator(device' tests/test_phase22_fakes.py` returns **0** — Pitfall 4 held
    - `grep -n '_FAKE1_LEAK_RATIO = 1.734481' tests/test_phase22_fakes.py` and `grep -n '_FAKE3_STD_RATIO_AT_N4 = 3.999986' tests/test_phase22_fakes.py` both still match — **the fitted constants were not re-fitted**
    - `grep -n 'test_phase22_dpsgd_ast' tests/test_phase22_fakes.py` matches — the AST exemption is named in the file, not only in this plan
    - `.venv/bin/python -m pytest tests/ -q` reports 0 failed
  </acceptance_criteria>
  <done>`_model`, `_record` and the six `RuntimeConfig` sites run on both devices, the two fitted constants are byte-unchanged, and the AST exemption is recorded in the source with its measured count.</done>
</task>

<task type="auto">
  <name>Task 3: V-15 — parametrize `test_resume_epsilon_bit_identical` over cpu and mps</name>
  <files>tests/test_phase22_checkpoint.py</files>
  <read_first>
    - `tests/test_phase22_checkpoint.py:313-395` — `_TOTAL_STEPS = 4`, `_KILL_AT = 2`, `_ACCUM = 2`,
      `_dp_train`, `_seam`, `_count_composed_steps`, `_next_draw`
    - `tests/test_phase22_checkpoint.py:392` — `_next_draw` raises on MPS:
      `RuntimeError: Placeholder storage has not been allocated on MPS device!` because
      `torch.normal` without `device=` cannot use an MPS generator (measured). Adding `device=` makes
      it succeed and return an `mps:0` tensor
    - `tests/test_phase22_checkpoint.py:396` — `test_resume_epsilon_bit_identical`, already
      parametrized over `sigma ∈ {1.0, 0.0}`, and its docstring on why `==` and not `rel_tol`
    - `tests/test_phase22_checkpoint.py:290, :337, :357` — the three `RuntimeConfig(device="cpu")`
      sites in this file
    - `tests/test_phase23_mps_venue.py` — the `_DEVICES` / `_MPS_SKIP` register
  </read_first>
  <action>
**(a) `_next_draw`.** Widen to `_next_draw(dp, device="cpu")` and pass `device=device` into
`torch.normal`. Keep `std=1.0` — the existing docstring's reason is unchanged and load-bearing: at
σ=0 every released value is exactly zero, so probing at `std=1.0` makes the σ=0 row carry the same
evidence as the σ>0 one.

**(b) `_seam` and `_dp_train`.** Thread a `device` argument through both; `_seam` passes
`runtime=RuntimeConfig(device=device)` and `_dp_train` passes `runtime_config=RuntimeConfig(device=device)`.
Move `_BATCH`'s two tensors onto the device inside `_dp_train` (`t.to(device)`) rather than at module
scope — the module-scope `_FIXTURE_GEN` draw must stay on CPU so the fixture batch is byte-identical
across devices for the same reason `_record`'s draw stays on CPU.

**(c) The parametrization.** Add `@pytest.mark.parametrize("device", _DEVICES)` beside the existing
`sigma` parametrize on `test_resume_epsilon_bit_identical`, giving 4 cases (2 σ × 2 devices). Also
thread `device` into `_tiny_lora_model` so the model and the seam agree.

**(d) The cross-device boundary, watched.** Add `test_cpu_written_dp_noise_rng_is_refused_on_mps`,
gated by `_MPS_SKIP`: write a checkpoint whose `dp_noise_rng` was produced by a CPU seam, then
attempt `load_noise_rng_state` into an MPS seam and assert it raises `RuntimeError`. Research §R1.4
names this as a feature rather than a hazard — torch refuses loudly — and D-02's discipline is that
the boundary is **watched** rather than assumed. Its docstring must state that 23-07 converts this
raw `RuntimeError` into a `SystemExit` naming the arm and the file, and that this test asserts the
underlying torch refusal that guarantee rests on.

**(e) Record the skip count.** In the SUMMARY for this plan, quote the literal
`N passed, M skipped` line from `.venv/bin/python -m pytest tests/test_phase22_checkpoint.py
tests/test_phase22_dpsgd.py tests/test_phase22_fakes.py tests/test_phase23_mps_venue.py -q` run on
the M3, and state `M` explicitly. **A pass count with no skip count beside it is the warning sign
Pitfall 1 names.**
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase22_checkpoint.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest "tests/test_phase22_checkpoint.py::test_resume_epsilon_bit_identical" -v` collects **4** cases and all 4 pass on the M3
    - `.venv/bin/python -m pytest tests/test_phase22_checkpoint.py tests/test_phase22_dpsgd.py tests/test_phase22_fakes.py tests/test_phase23_mps_venue.py -q` reports a skip count of **0** on the M3, and that literal line is quoted in the SUMMARY
    - `grep -n "def _next_draw" tests/test_phase22_checkpoint.py` shows a `device` parameter
    - `grep -n "test_cpu_written_dp_noise_rng_is_refused_on_mps" tests/test_phase22_checkpoint.py` matches
    - `.venv/bin/python -m pytest tests/ -q` reports 0 failed; `make lint` exits 0
    - `git diff --exit-code -- scripts/mitigation_accountant.py scripts/mitigation_gate.py` exits 0
  </acceptance_criteria>
  <done>V-15 runs on MPS as well as CPU, the cross-device `dp_noise_rng` boundary is a watched refusal, and the phase's skip count is recorded as a number rather than implied by a pass count.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CPU test evidence → MPS published ε | The milestone publishes an ε produced on MPS from correctness proofs watched on CPU. Every device-dependent property must cross this boundary by measurement. |
| CI (`ubuntu-latest`) → the M3 phase gate | A green CI run necessarily skips every MPS leg. Reading that green as a venue pass is the defect. |
| torch generator state ↔ checkpoint file | 5,056 B CPU vs 44 B MPS, mutually refused. A cross-device resume is a real, reachable operator mistake. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Repudiation | a `skipif`-gated MPS leg reported as a venue pass | mitigate | `pytest.param(..., marks=skipif)` produces a COUNTABLE skip; the skip count is quoted as a literal in the SUMMARY and asserted `0` on the M3 |
| T-23-02 | Tampering | re-drawing gradients on an MPS generator inside `_record`, silently moving two fitted constants | mitigate | The draw stays on CPU and only the tensor moves; `grep -c 'torch.Generator(device'` must be 0; both fitted literals asserted byte-unchanged |
| T-23-03 | Information Disclosure | a published ε from a venue whose generator-continuity refusal was never exercised there | mitigate | The DPSGD-06 keystone is a committed test at six draw widths on MPS, not a research-doc measurement |
| T-23-04 | Spoofing | an AST-only probe claiming a device pass it did not perform | mitigate | The exemption for `test_phase22_dpsgd_ast.py` / `test_phase22_accountant.py` is written into the source with its measured count (53 of 113) |
| T-23-05 | Tampering | a CPU-written `dp_noise_rng` silently accepted by an MPS seam | mitigate | `test_cpu_written_dp_noise_rng_is_refused_on_mps` watches torch's refusal; 23-07 upgrades the message |
| T-23-SC | Tampering | npm/pip/cargo installs | accept | **Zero installs.** `pyproject.toml` byte-unchanged (RPT-03); `23-RESEARCH.md`'s Package Legitimacy Audit records N/A deliberately, and the `{pathlib, sys, erasure_gate}` import ceiling is unaffected by this plan |
</threat_model>

<verification>
```bash
.venv/bin/python -m pytest tests/test_phase23_mps_venue.py -v
.venv/bin/python -m pytest tests/test_phase22_checkpoint.py tests/test_phase22_dpsgd.py tests/test_phase22_fakes.py -v
.venv/bin/python -m pytest tests/test_phase22_checkpoint.py tests/test_phase22_dpsgd.py tests/test_phase22_fakes.py tests/test_phase23_mps_venue.py -q   # record "N passed, M skipped"
.venv/bin/python -m pytest tests/ -q
git diff --exit-code -- scripts/mitigation_accountant.py scripts/mitigation_gate.py pyproject.toml
make lint
```
</verification>

<success_criteria>
- The DPSGD-06 generator keystone is a committed MPS test at six draw widths, green
- The cross-device state refusal and the mid-stream round-trip are watched, with discriminating substrings rather than full error strings
- `_model`, `_record`, `_next_draw` and the nine `RuntimeConfig(device=)` sites run on both devices
- `_FAKE1_LEAK_RATIO` and `_FAKE3_STD_RATIO_AT_N4` are byte-unchanged
- The skip count on the M3 is **0** and is quoted as a literal in the SUMMARY
- The AST exemption is recorded in the source with its measured count
</success_criteria>

<output>
Create `.planning/phases/23-cost-calibration-the-0-diagnostic-and-budget-pre-registratio/23-01-SUMMARY.md` when done.
</output>
