---
phase: 23-cost-calibration-the-0-diagnostic-and-budget-pre-registratio
plan: 12
type: execute
wave: 15
depends_on: ["23-11"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - tests/test_phase23_cost.py
autonomous: true
requirements: [CAL-01, CAL-05]   # Task 1 ticks BOTH; the frontmatter listed only CAL-01

must_haves:
  truths:
    - "D-10: the falsified '~1,010×' cost claim is corrected by RETRACT-IN-PLACE — a dated additive continuation naming the measurement that falsified it, with the original left standing and visible."
    - "THE CONTINUATION'S CONTENT IS READ FROM `results/phase23_cost.json`, AT FULL STORED PRECISION, FROM PRE-REGISTERED FIELD PATHS. This plan pre-commits to NO figure and NO conclusion. Eleven field paths are pre-registered in Task 2; the continuation quotes each one's `repr()` rendering VERBATIM **and publishes NO OTHER long figure** — the set is closed in BOTH directions, and Task 1 item 3 names it as the complete publication list rather than saying 'every figure the record carries'. The record carries strictly more (the four `eval_over_training_floor` ratios and the per-seed / min / max / per-step timings); those stay in the record UNQUOTED. No rounding of any of the eleven is acceptable — 2.04 is not 2.035849685343305."
    - "THE GUARD IS EXACT STRING CONTAINMENT IN BOTH DIRECTIONS, OVER ONE PRE-REGISTERED SET. There is no extraction pipeline, no allow-list, and no span classification: the round-1..4 six-step apparatus is DELETED. Omission is caught by verbatim containment of each pre-registered rendering; invention is caught by a SHAPE rule — every numeric literal in the continuation with >= 8 fractional digits must be one of the pre-registered renderings. Short numerals (dates, plan ids, the quoted `1,010` and `17`, hex digests) are excluded by SHAPE, not by enumeration, so there is nothing to widen."
    - "THE '20.4 s / 843x' NON-DP FIGURE IS A LOOP-ONLY PROJECTION, NOT A MEASUREMENT — `23-RESEARCH.md:637-641`, and `:665-685` already RETRACTED IN PLACE that table's lower-bound status at n=8. It excludes `build_arm_bins`, 20 in-loop evals, 4 checkpoint writes, replay memmap I/O and two `masked_perplexity` sweeps. The continuation must not claim the sentence 'reproduces for the non-DP arm' unless a REAL run reproduces it."
    - "THE RE-SCOPING NAMES A PROTOCOL, NOT JUST AN ARM. Two measured non-DP protocols differ in wall clock by `training.non_dp.wall_clock_gap_vs_superseded` (MEASURED 2.035849685343305), so 'the non-DP arm' alone does not identify a number. Every figure the continuation quotes carries the `protocol` field the cost record attaches to it."
    - "The claim appears in THREE files — `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md` and `.planning/STATE.md`. A correction landing in one of three leaves two standing, which is the same partial-fix defect this project has measured before."
    - "The retraction is APPENDED to the row it corrects, in the established shape: `**RETRACTED IN PLACE <date> (plan 23-12).**` immediately after the original text, which is left byte-unchanged."
    - "The per-point h/point table is also a FLOOR, and the measured ceiling is disclosed in the same continuation. Leaving a floor-valued table standing beside a corrected ratio would reproduce exactly the defect CAL-05 exists to prevent."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "the dated additive continuation on the CAL preamble, with the original sentence and the h/point table left standing"
      # DISCRIMINATING BY CONSTRUCTION: `RETRACTED IN PLACE` is ALREADY TRUE at HEAD
      # (MEASURED: `grep -c` returns 1), so asserting it would pass against an untouched file.
      # `(plan 23-12)` is MEASURED absent at HEAD (returns 0) and can only go true if Task 1 ran.
      contains: "(plan 23-12)"
    - path: "tests/test_phase23_cost.py"
      provides: "the structural guard that the correction is additive, covers all three files, and quotes only the eleven pre-registered renderings — in BOTH directions over ONE pre-registered set of field paths, since record→continuation catches an omission and continuation→record catches an invention, and neither implies the other"
      contains: "def test_the_continuation_invents_no_figure"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "results/phase23_cost.json"
      via: "the continuation names the measuring artifact and its sha256; every figure it quotes is re-derivable from that record"
      pattern: "phase23_cost.json"
    - from: "tests/test_phase23_cost.py"
      to: "the original sentence"
      via: "asserted still present through the line-wrap-tolerant matcher, so the correction cannot become a rewrite"
      pattern: "1,010"
---

<objective>
Correct a committed repository figure that did not survive re-measurement, by the route this project
already uses: leave the original standing and append a dated continuation naming what measured it
false.

Purpose: `.planning/REQUIREMENTS.md` states *"Training is ~17 s per arm. **Evaluation costs ~1,010×
training** — it is the binding constraint by three orders of magnitude, and no sweep density may be
chosen without it."* 23-11 measured the training leg on real runs at three capacities and two non-DP
protocols. This plan lands the correction in the same phase, so the retraction and the evidence
supporting it arrive together.

CAL-01's own text already says the training leg is *"to be confirmed on the DP path with the seam
active"*, so measuring it and finding a different number is the requirement being satisfied rather
than a defect being found.

**This plan states no cost figure and reaches no conclusion about how far the claim moved.** The
continuation's content is READ from `results/phase23_cost.json`. What this plan fixes is the SHAPE
of the correction — additive, dated, complete across all three files, protocol-labelled, and
checkable.

Output: dated additive continuations in all three files carrying the claim, and a structural test
that the correction is additive, complete, and quotes only the eleven pre-registered renderings.
"Present somewhere in the record" is NOT the criterion and would be strictly weaker than the guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-cost-calibration-the-0-diagnostic-and-budget-pre-registratio/23-CONTEXT.md
@.planning/phases/23-cost-calibration-the-0-diagnostic-and-budget-pre-registratio/23-RESEARCH.md
@.planning/phases/23-cost-calibration-the-0-diagnostic-and-budget-pre-registratio/23-VALIDATION.md
</context>

<environment>
**Python 3.11 venv is MANDATORY.** `.venv/bin/python -m pytest ...`.

**QUOTE NOTHING THIS PLAN DID NOT MEASURE.** Two figures in circulation are NOT measurements and
must never enter a continuation as one:

| figure | what it actually is |
|---|---|
| `~17 s per arm` | research's own estimate, the thing being corrected |
| `20.4 s` / `843x` | a **loop-only PROJECTION** at accum=1 (section `### R3.A — CAL-01: training wall-clock on the DP path with the seam active`, `23-RESEARCH.md:637-641` — heading text first, range second, per W4), excluding `build_arm_bins`, 20 in-loop evals, 4 checkpoint writes, replay memmap I/O and two `masked_perplexity` sweeps. `23-RESEARCH.md:665-685` already **RETRACTED IN PLACE** that same table's lower-bound status after 23-10 measured the n=8 row 9.8% below it. No real `train_arm` or `tp.train` run in this repository has been shown to reproduce 20.4 s. |

Read every figure the continuation publishes from `results/phase23_cost.json` and quote it at that
record's own precision. **The published set is the eleven pre-registered paths (Task 1 item 3), not
everything the record carries** — the record also holds the four floor-side ratios and the per-seed /
min / max / per-step timings, which stay in the record unquoted.
Whether the sentence still holds for any arm at any protocol is a question the record answers, not
this plan.

**FULL STORED PRECISION IS THE RULE, AND A ROUNDING IS A DEFECT.** Every figure the continuation
publishes is quoted as `repr(value)` of a leaf resolved from a PRE-REGISTERED field path — the same
string the record's own bytes carry, because 23-11 writes the record with `json.dump`, which
serialises floats through `float.__repr__`. MEASURED from the committed source records, three of the
eleven renderings are already known:

| path | rendering |
|---|---|
| `training.non_dp.training_seconds_mean` | `161.12400419991462` |
| `training.non_dp_superseded_protocol.training_seconds_mean` | `79.14336965046823` |
| `training.non_dp.wall_clock_gap_vs_superseded` | `2.035849685343305` |

`161.12`, `79.14` and `2.04` are ROUNDINGS of those three. Earlier drafts of 23-11 and 23-12 modelled
them in prose; **they are not figures this plan may write anywhere in a continuation.**

**BE PRECISE ABOUT WHAT CATCHES THIS, BECAUSE THE MECHANISM IS NARROWER THAN THE RULE.** Task 2's
shape rule does not catch a rounding at all — `2.04` is short. The omission half catches a rounding
written **INSTEAD OF** the required figure, because the required full-precision string is then
absent. It does **NOT** catch a rounding written **ALONGSIDE** the required figure: if a continuation
carries both `2.035849685343305` and a loose `2.04`, both halves pass. That case falls inside Task 2's
stated residual and is caught, if at all, by the SUMMARY's sub-threshold numeral inventory and by
review — **not by a guard.** Do not write the rounding, and do not read this rule as if a mechanism
were stopping you.

**THE RE-SCOPING NAMES A PROTOCOL.** `results/phase23_cost.json` carries at least two non-DP
training blocks with different `protocol` fields and different `source_record` values, because two
protocol-distinct non-DP arms were measured and they disagree materially. "True of the non-DP arm"
is therefore an incomplete re-scoping — it does not identify a number. Every figure the continuation
quotes carries its `protocol`.

**THE CLAIM'S MULTIPLICATION SIGN IS U+00D7 `×`, NOT ASCII `x`. MEASURED at HEAD** —
`.venv/bin/python -c "import pathlib,re;[print(p,'U+%04X'%ord(m.group(1))) for p in ('.planning/REQUIREMENTS.md','.planning/ROADMAP.md','.planning/STATE.md') for m in re.finditer(r'1,010(.)', pathlib.Path(p).read_text())]"`
prints `U+00D7` at every hit. Item 2 below requires the claim **quoted verbatim**, and every
locate-by-text instruction in this plan searches for it, so an ASCII `x` both breaks the verbatim
quote and makes the locator miss. Copy the character from the file; do not retype it.

**Resolve every location by TEXT, never by line number.** The claim currently sits near
`.planning/REQUIREMENTS.md:176`, `.planning/ROADMAP.md:46` and `.planning/STATE.md:765`, and those
numbers drift the moment anything above them is edited. `tests/test_phase20_prereg.py:125-170`
records this repository's own lesson: *"A statement's TEXT and a symbol's NAME survive every edit to
the file they live in; a line number survives none."* Locate with `grep -n "1,010" .planning/*.md`.

**MEASURED baseline counts at HEAD, for the acceptance criteria:**
`grep -c "RETRACTED IN PLACE" .planning/REQUIREMENTS.md` = **1**.
`grep -c "1,010"` = REQUIREMENTS **1**, ROADMAP **2**, STATE **1**. **The ROADMAP's second hit is
`:645`, the plan-list line describing THIS plan — not a copy of the claim.** The completeness guard
matches the normalized CLAIM TEXT, never the bare string `1,010`, or it will demand a retraction
marker beside its own plan description.

**NO `gsd-sdk` MUTATION HANDLER MAY TOUCH THESE FILES.** 23-19 recorded that
`roadmap.update-plan-progress` keys on SUMMARY EXISTENCE and falsely ticked 23-17, and that every
planning edit was made by hand with `Edit`. Hand-edit all three files. If any `state.*` or
`roadmap.*` handler is used anyway, `git diff .planning/STATE.md .planning/ROADMAP.md` immediately
and hand-repair.

**FROZEN — no write:** `scripts/mitigation_accountant.py`, `scripts/mitigation_gate.py`,
`scripts/phase23_prereg.py`, `scripts/phase23_matched_prereg.py`, `scripts/phase23_resume_prereg.py`,
`scripts/mitigation_budget.py`, and every `results/phase23_*` artifact already committed.

**Suite baseline entering this plan: whatever 23-11's SUMMARY recorded.** Quote it; do not carry a
number forward from an older plan's prose.
</environment>

<tasks>

<task type="auto">
  <name>Task 1: The dated additive continuation, in all three files</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - `results/phase23_cost.json` — **the measuring artifact, and the FIRST thing to read.** Every
      figure quoted in the continuation must be READ from it by PATH, never retyped from any plan.
      Note the `protocol`, `source_record` and `source_record_sha256` on each training block, the
      `provenance_argument` on `training.non_dp`, and the NAMED numeric field
      `training.non_dp.wall_clock_gap_vs_superseded` — 23-11 stores the wall-clock protocol gap
      there precisely so this plan can quote it by path instead of lifting it out of a prose sentence
    - `.planning/phases/23-*/23-11-PLAN.md` — the table headed **THE PUBLISHED FIGURES ARE
      PRE-REGISTERED FIELD PATHS** (locate by that text). Its eleven paths and Task 2's
      `REQUIRED_FIGURE_PATHS` are the SAME list; if they have drifted, stop and escalate rather than
      reconciling them silently
    - `.planning/REQUIREMENTS.md` — the CAL preamble, the "~1,010×" sentence, the per-point K table
      (48 / 24 / 16 / 8 with h/point 4.77 / 2.45 / 1.67 / 0.90) and the CAL-01 / CAL-05 rows.
      **Locate by text**
    - `.planning/REQUIREMENTS.md` — the ONE existing `RETRACTED IN PLACE` continuation (find it with
      `grep -n "RETRACTED IN PLACE"`). **Read it in full**, and read `23-RESEARCH.md:665-685`'s too.
      Together they are the shape: the marker appended to the row it corrects, the original left
      unamended *"as the record of what was believed"*, the attribution named, and what closed it
      stated
    - `.planning/ROADMAP.md:40-60` — the milestone preamble's own copy of the claim; and `:645`, the
      plan-list line that mentions `1,010` WITHOUT carrying the claim
    - `.planning/STATE.md` — the `Evaluation binds at ~1,010×` sentence (locate by text)
    - `scripts/_addendum.py:56-100` — `append_addendum(path, addendum, *, pending, recorded)`: the
      placeholder must occur EXACTLY once, the produced bytes are re-checked for the original prefix,
      and the recorded `## Verdict` section must be unchanged. Use it where a `pending`/`recorded`
      pair genuinely exists; where the correction is an inline append to a prose paragraph with no
      such pair, follow the existing `RETRACTED IN PLACE` continuation's shape instead and say in
      the SUMMARY which route was used and why
    - `.planning/phases/23-*/23-11-SUMMARY.md` — 23-11's own statement of the `training.non_dp`
      provenance decision, so the continuation and the record agree on which protocol sizes what
  </read_first>
  <action>
Append one dated continuation per file. **The original text is left byte-unchanged in every case** —
that is the whole discipline, and Task 2 asserts it.

**THE WRITING CONVENTION — THREE RULES, AND THEY ARE THE WHOLE OF IT.** Rounds 1-4 grew a six-step
extraction pipeline with a marker-token strip, a backtick-refusal predicate, comma-stripping and an
allow-list, and each added step opened the next hole. **That apparatus is DELETED.** Task 2's guard
is now exact string containment over a pre-registered set, which needs the continuation to obey only
this:

  1. **DELIMIT THE CONTINUATION WITH SENTINELS.** In each of the three files, write the line
     `<!-- 23-12-CONTINUATION-BEGIN -->` on its own line immediately before the
     `**RETRACTED IN PLACE <today's date> (plan 23-12).**` marker, and
     `<!-- 23-12-CONTINUATION-END -->` on its own line immediately after the continuation's last
     line. HTML comments render as nothing in Markdown, so the document reads unchanged. **This is not
     decoration — it is what makes the guard's input exact.** MEASURED at HEAD, the three files
     already contain **69 / 20 / 86** numeric literals with >= 8 fractional digits, none of them
     leaves of `results/phase23_cost.json`; a guard that scanned whole files would RED on every one
     of them. Exactly one BEGIN and one END per file, BEGIN before END; Task 2 asserts that.

  2. **EVERY FIGURE IS THE `repr()` OF A PRE-REGISTERED LEAF, COPIED WHOLE.** No rounding, no
     truncation, **no thousands separators.** Write `161.12400419991462`, never `161.12` and never
     `1,487.60123456789`. The surrounding document does use separated figures (`42,480`, `84,960`,
     `~1,010×`) and those are untouched — the rule binds only on the figures this continuation
     publishes. A separated figure is CAUGHT, not laundered: MEASURED, `1,487.60123456789` extracts
     whole and fails the set membership.

  3. **BACKTICKS ARE FREE, AND CARRY NO MEANING FOR THE GUARD.** Put paths, symbols, formulas,
     digests and plan ids in backticks or not, as the register prefers. The guard does not look at
     backticks at all, so **a figure written inside backticks is checked exactly as one written in
     prose** — the laundering channel rounds 3 and 4 kept re-opening does not exist here, because
     nothing is stripped and no span is classified. The same is true of the marker line: nothing is
     dropped, so a figure written after the marker on its own line is in scope.

**WHY SHORT NUMERALS NEED NO ALLOW-LIST.** Every figure the continuation publishes is now full
precision, so an invented one would have to be a long decimal. The prose's legitimate numerals —
dates, `23-12`, plan ids, `D-03`, `CAL-05`, `SC2`, section and line citations, the quoted `1,010` and
`17`, the sha256 digest, the `4.77` / `2.45` / `1.67` / `0.90` table column — are all SHORT, and are
excluded by SHAPE rather than by enumeration. Task 2 states the threshold and the measured
separation. **There is nothing to widen, which is the point.**

**VERIFIED IN ROUND 5 — the two-function guard was IMPLEMENTED from Task 2's text and RUN against a
computed fixture record and six constructed continuations. This table is its literal output:**

| case | omission half | invention half | |
|---|---|---|---|
| compliant, full precision, all seven items | `[]` | `[]` | **PASS** |
| a required figure OMITTED (`h_per_point_ceiling` dropped) | `['generation.h_per_point_ceiling -> 7.391541088']` | `[]` | **RED** |
| an invented long figure, written BARE | `[]` | `['37.51234567890123']` | **RED** |
| an invented long figure, written INSIDE BACKTICKS | `[]` | `['37.51234567890123']` | **RED** |
| an invented long figure, written ON THE MARKER LINE | `[]` | `['37.51234567890123']` | **RED** |
| a ROUNDED figure (`2.04` where the gap is required) | `['training.non_dp.wall_clock_gap_vs_superseded -> 2.035849685343305']` | `[]` | **RED** |

Two further measured results, because they were the round-3/4 failure modes: a long figure that IS a
record leaf but is NOT pre-registered (`154.20959608256817`, a per-seed timing) REDs on the invention
half; and a hand-typed `1743.8820147753301` in the fixture round-tripped through `json.dump` to
`1743.8820147753302` and RED-ed an otherwise compliant continuation — **which is why 23-11 must write
every figure computed, never typed.**

Each continuation must carry, in the register the existing one uses:

  1. The marker `**RETRACTED IN PLACE <today's date> (plan 23-12).**`
  2. **The claim being corrected, quoted verbatim** so a reader does not have to reconstruct which
     sentence moved.
  3. **What was measured, with the artifact that measured it — EXACTLY THE ELEVEN PRE-REGISTERED
     FIELD PATHS, NO MORE AND NO FEWER, IN THE `.planning/REQUIREMENTS.md` CONTINUATION.** The
     `ROADMAP.md` and `STATE.md` continuations carry the record path, its sha256,
     `generation.h_per_point_ceiling`, and whichever of the eleven that document's own claim needs —
     forcing all eleven into a milestone preamble or a status line would make them copies of the
     first, which is what Task 2's half-one scope note refuses. Every figure they DO carry is still
     drawn from the eleven and is still bound by the invention half, which scans all three files.
     Quote the `repr()` rendering of each of the eleven paths
     pinned as Task 2's `REQUIRED_FIGURE_PATHS` — the same list as 23-11's **THE PUBLISHED FIGURES
     ARE PRE-REGISTERED FIELD PATHS** table your `read_first` already sent you to — **each training
     figure labelled with its own `protocol` field, and each ratio labelled with the `protocol` of
     the training block it divides** (the `ratios.*` blocks are derived and carry no `protocol` key
     of their own; `generation.h_per_point_floor` and `_ceiling` are not training figures and need
     none), with that record's path and sha256 named. Do not re-type the list from here: read it from those two places, and escalate if they
     disagree rather than reconciling them.

     **THE RECORD CARRIES MORE THAN THE ELEVEN, AND THE REST STAY IN THE RECORD UNQUOTED.**
     `results/phase23_cost.json` also stores the four FLOOR-side ratios
     (`ratios.<block>.eval_over_training_floor`, in 23-11 Part B under the heading **THE PUBLISHED
     FIGURES ARE PRE-REGISTERED FIELD PATHS** — resolve it by that text, never by line number) and the
     per-seed / min / max / per-optimizer-step training timings (`training_seconds_per_seed`,
     `training_seconds_min`, `training_seconds_max`, `seconds_per_optimizer_step`) — about two dozen
     further long leaves. They are real measurements, they stay in the record, and this continuation
     simply does not publish them. Referring to them by NAME is free; writing their NUMERALS is not.

     **THIS IS THE ROUND-6 BLOCKER, AND IT IS MEASURED, NOT ANTICIPATED.** An earlier draft of this
     item read *"every training figure and every `eval ÷ training` ratio `results/phase23_cost.json`
     carries"* — which orders precisely the figures Task 2's invention half refuses, because 23-11
     pre-registers only the FOUR `ratios.<block>.eval_over_training_ceiling` rows of that same
     **THE PUBLISHED FIGURES ARE PRE-REGISTERED FIELD PATHS** table while storing EIGHT. Both
     helpers were re-implemented from this plan's text and run against a continuation obeying that
     wording: `_long_figures_not_sourced` returned **24** violations — the four
     `eval_over_training_floor` ratios and twenty per-seed / min / max / per-step timings — while
     `_required_figures_missing` returned `[]`. The plan could not go green. Against a continuation
     obeying the wording ABOVE, both helpers return `[]`. **Publish the eleven.**

     **Read them; quote nothing this plan or 23-RESEARCH.md supplied.** In particular: if the record shows that no measured run reproduces
     the research projection's non-DP training figure, say that plainly — the projection is then a
     *third* thing the retraction corrects, not the surviving half of it. **Say it by CITATION, not
     by numeral:** name it as *"the research projection's non-DP training figure
     (`### R3.A — CAL-01: training wall-clock on the DP path with the seam active`,
     `23-RESEARCH.md:637-641`, itself already RETRACTED IN PLACE)"*.

     **W4 — CITE THE SECTION HEADING TEXT, NOT A BARE LINE RANGE.** An earlier draft mandated only
     `23-RESEARCH.md:637-641`. This continuation is a PERMANENT standing correction, and a bare line
     range in it contradicts this plan's own environment block (*"Resolve every location by TEXT,
     never by line number"*) and the drift this repository has already burned itself on
     (`23-11-PLAN.md`, the `_prove_noised_record_is_under_the_glob` note: a cited range started 17
     lines inside the function and missed the docstring stating the rule). Write the heading text
     FIRST and the range after it as a convenience. **MEASURED at HEAD:**
     `grep -c "R3.A — CAL-01: training wall-clock on the DP path with the seam active"
     .planning/phases/23-*/23-RESEARCH.md` returns **1** — the anchor is unique — and the projection
     table sits under it at `:637-641`. Verify the anchor by grep before writing it, and if the count
     is not 1, resolve the section by its own heading and record the new range.

     **Do NOT write the projection's numerals into the continuation.** They are not leaves of
     `results/phase23_cost.json` — nothing measured them. The numerals belong in the SUMMARY, where
     the criterion below already puts them.
  4. **The two non-DP protocols, disclosed side by side.** The record carries the superseded
     protocol's figure beside the reference one deliberately, exactly as 23-19 left both verdicts
     standing. Quote both `training_seconds_mean` fields at full precision, quote the gap **from the
     named field `training.non_dp.wall_clock_gap_vs_superseded`** rather than dividing them yourself
     or lifting a numeral out of a prose sentence, and name which protocol sizes the sweep and why.
     Cite 23-11's `provenance_argument` for the argument; take the number from the field.
  5. **The root cause, not a discrepancy**: `scripts/teach_persona.py`'s
     `grad_accum_steps = stats["n_facts"]` — one micro-batch per privacy record — and
     `src/personacore/training/loop.py`'s `ceil(4 * n_facts / batch_size)` replay micro-batches per
     optimizer step. The DP seam's own per-record overhead is small — **measured in research only
     (`23-RESEARCH.md:629`), not re-measured by this phase, so state it qualitatively and WITHOUT
     its numeral**; the multiple comes from the record-per-micro-step structure SC2 pre-registered.
     (The numeral is a research figure, not a leaf of `results/phase23_cost.json`, and this plan's
     environment block says to quote nothing it did not measure. Note honestly that Task 2's shape
     rule would NOT catch it — `6.21` is short — so this prohibition rests on the writing convention
     and on review, not on a mechanism. That is a disclosed residual, not a hole to patch with an
     allow-list. The mechanism claim — record-per-micro-step, not per-record seam overhead — needs no
     figure to stand.)
  6. **What does NOT change, stated plainly**: whether evaluation remains the binding constraint at
     every capacity is a question the record's own ratios answer — **read all EIGHT and say what
     they say.** If they still bind at every capacity, state that D-03's ordering, D-04's halt rule
     and the sweep sizing all stand and only the margin moves. If any ratio falls to or below 1, that
     is a decision-affecting finding and the SUMMARY must escalate it rather than absorb it.
     **READING IS NOT QUOTING (item 3).** The four floor-side ratios inform the sentence you write;
     their NUMERALS do not enter the continuation, because they are not among the eleven and half two
     REDs on them. Say *"the floor-side ratios bind at every capacity too"* qualitatively, or quote
     the floor-side figures in the SUMMARY, where the pre-registered set does not bind.
  7. **The h/point table is a FLOOR.** Disclose the measured `h_per_point_ceiling` from
     `results/phase23_cost.json` beside the table's floor-valued column, and state that Z is sized
     against the ceiling because the K ratchet at `scripts/mitigation_gate.py` only permits
     increases. **The table is left standing.** A corrected ratio sitting beside an uncorrected
     floor-valued table would reproduce exactly the defect CAL-05 exists to prevent.

Also tick `- [ ] **CAL-01**` and `- [ ] **CAL-05**` to `- [x]` with a one-line SATISFIED note naming
the plans and the artifact, following this table's existing convention for satisfied rows. If
`scripts/_addendum.py`'s `pending`/`recorded` pairing fits that tick, use it; the paired form exists
precisely so an append cannot silently become a rewrite.

**Do not touch any `results/phase23_*` artifact.** The correction lives in the planning documents;
the artifacts are the evidence it cites.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase23_cost.py -q</automated>
  </verify>
  <acceptance_criteria>
    - **W1 — THE COUNT IS `>= 2`, NOT `== 2`, BECAUSE THIS TASK'S OWN MANDATED CONTENT ADDS A THIRD LINE.** The baseline is **1**, MEASURED at HEAD — and that is 1 LINE, not 1 occurrence: `grep -c` counts lines, and `.planning/REQUIREMENTS.md:352` already carries TWO markers on that single table row. Task 1's new marker adds a line, and item 3 of the action above mandates the citation *"(`23-RESEARCH.md:637-641`, **itself already RETRACTED IN PLACE**)"*, which lands on a line of its own wherever the paragraph wraps — so the honest count is 2 or 3. An `== 2` criterion is then satisfiable only by DROPPING the D-10-mandated third-thing-corrected disclosure, which is the opposite of what this plan is for. Use `[ "$(grep -c 'RETRACTED IN PLACE' .planning/REQUIREMENTS.md)" -ge 2 ]`, exiting 0, and record the baseline, the final count and what each line is in the SUMMARY
    - **The third-thing-corrected disclosure is asserted POSITIVELY, so the count criterion above cannot be met by omitting it — and it is asserted on the HEADING TEXT as well as the range (W4):** `[ "$(grep -c 'R3.A — CAL-01: training wall-clock on the DP path with the seam active' .planning/REQUIREMENTS.md)" -ge 1 ]` exits 0 **and** `[ "$(grep -c '23-RESEARCH.md:637-641' .planning/REQUIREMENTS.md)" -ge 1 ]` exits 0. Both MEASURED absent at HEAD (`grep -c` returns **0** for each), so each can only go true if the citation was actually written. The heading anchor is MEASURED unique in `23-RESEARCH.md` (`grep -c` returns **1**); the range is a convenience beside it, and the text is what survives an edit above it
    - `grep -c "1,010" .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md` shows the original sentence still present in all three, at counts **>=** the HEAD baseline (1 / 2 / 1)
    - **W7 — the deletion count is FOUR, not two.** `git diff --numstat -- .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md` deletion counts are **0 apart from exactly four lines in `.planning/REQUIREMENTS.md`**: the two checkbox rows (`- [ ] **CAL-01**` → `- [x]`, `- [ ] **CAL-05**` → `- [x]`) **and the two traceability rows** (`| CAL-01 | Phase 23 | |` and `| CAL-05 | Phase 23 | |`), which the action's own instruction to follow this table's existing convention requires filling — a filled cell is a modified line, and a modified line is one deletion plus one insertion. An earlier draft said "except for the two checkbox ticks", which either false-REDs a correct execution or pushes the executor to skip the traceability cells that 23-13 and 23-14 then read as the convention to match. The SUMMARY accounts for all four by name
    - **Each continuation is delimited by exactly one BEGIN and one END sentinel, BEGIN first — COUNTED BY OCCURRENCE, NOT BY LINE.** `[ "$(grep -o '23-12-CONTINUATION-BEGIN' .planning/REQUIREMENTS.md | wc -l | tr -d ' ')" = 1 ]` exits 0, and likewise for `23-12-CONTINUATION-END` and for the other two files. **`grep -c` is WRONG here and is not accepted:** it counts LINES, so two sentinels emitted on one line satisfy it while the test's `split` then scans only the first span. MEASURED at HEAD, `.planning/REQUIREMENTS.md:352` carries **2** `RETRACTED IN PLACE` markers on one line and `grep -c` returns **1** — this is the same `grep -c` defect W1 documents, and it is live in this repository. Both sentinels are MEASURED absent at HEAD (0 occurrences in all three files), so these can only go true if Task 1 wrote them
    - Each continuation names `results/phase23_cost.json` and quotes its sha256; **every long figure it quotes is verified to be one of the ELEVEN PRE-REGISTERED renderings at FULL STORED PRECISION** by loading the record and comparing, and the SUMMARY records that check. **"Present somewhere in the record" is NOT the criterion and would be weaker than the guard** — a floor-side ratio or a per-seed timing is a real leaf at full precision and would satisfy it, while `_long_figures_not_sourced` REDs on it. Membership in the eleven is the criterion, in both places
    - **NO ROUNDING OF A PRE-REGISTERED FIGURE APPEARS IN ANY CONTINUATION — AND THIS ONE IS CHECKED BY READING, NOT BY A GUARD.** The three MEASURED-known renderings are `161.12400419991462`, `79.14336965046823` and `2.035849685343305`. Half one REDs on a rounding written INSTEAD of the required figure, but **passes** on one written ALONGSIDE it, and half two does not see a short numeral at all. **This is not patched with a grep**: a pattern like `2\.04` matches inside a legitimate long figure such as `152.0412345678` and would false-RED a correct continuation, which is the defect class this revision exists to stop shipping. It is covered instead by the sub-threshold numeral inventory criterion in Task 2 — every numeral in the slice with fewer than 8 fractional digits is listed in the SUMMARY with what it is, and a stray `2.04` has no legitimate answer in that column. **Read the inventory; do not add a pattern**
    - Each continuation contains the phrase `h_per_point_ceiling`, and names a `protocol` for every training figure it quotes
    - **The SUMMARY — NOT the continuation — states plainly whether any measured run reproduces the `20.4 s` / `843x` projection, and quotes both those numerals and the record's own figures for the comparison.** This is the one place the projection's numerals are written: the SUMMARY is a dated report of what this plan found, whereas the continuation is a standing correction whose every bare figure must load from the cost record. No figure from this plan's prose, from `23-RESEARCH.md`'s projection table, or from an older plan draft appears in any continuation
    - `git diff --exit-code -- results/ scripts/` exits 0
  </acceptance_criteria>
  <done>All three files carry a dated additive continuation quoting the original claim, the measured figures read from the cost record with their protocols named, the root cause, the two-protocol disclosure, what does not change, and the floor/ceiling disclosure — with the originals byte-unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: The structural guard — additive, complete across three files, and faithful to the record</name>
  <files>tests/test_phase23_cost.py</files>
  <read_first>
    - `tests/test_phase23_cost.py` — current state after 23-11
    - `scripts/_prose.py:35` — `normalized(text)`. **A phrase that wraps across lines is invisible to
      a naive `in` check**, and this repository has a committed guard for exactly that at
      `tests/test_phase20_prereg.py:515-560`
    - `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md` — as corrected
    - `.planning/ROADMAP.md:645` — the plan-list line mentioning `1,010` without carrying the claim.
      **The completeness scan must not demand a retraction marker beside it**
    - `results/phase23_cost.json`
  </read_first>
  <action>
Add `test_cost_claim_correction_is_additive` to `tests/test_phase23_cost.py`, parametrized over the
three files.

For each file assert:
  - **the original claim is still present**, matched through `scripts/_prose.normalized` — import it
    rather than writing a second normalizer; a second copy of a matcher is a second matcher, free to
    stop matching;
  - a `RETRACTED IN PLACE` marker exists **after** the original claim's position in the file, with a
    date and the string `23-12`;
  - the continuation names `results/phase23_cost.json` and contains that record's live sha256;
  - the continuation names a `protocol` for the non-DP figure it quotes, matching one of the
    `protocol` strings the record actually carries. **Derive the acceptable protocol strings from the
    record, never hardcode them** — the record is the source and a retyped protocol string is a
    second label free to drift.

**TWO HALVES, ONE PRE-REGISTERED SET. THE HALVES CATCH DIFFERENT DEFECTS AND NEITHER IMPLIES THE
OTHER.** This is the single most important structural point in this task, so it is stated before
either test:

| half | direction | catches | does NOT catch |
|---|---|---|---|
| `..._quotes_the_cost_record_faithfully` | record → continuation | an OMITTED figure, and any ROUNDING of a required one | an INVENTED extra figure |
| `..._invents_no_figure` | continuation → record | an INVENTED long figure | an omission |

must_haves truth 2 says *"any number in the continuation must be loadable from the record"* — that
is the **continuation → record** direction, and it is the defect class this whole plan exists to
close. A record → continuation test alone leaves it wide open: a continuation may quote every figure
the record carries AND three more that nothing measured, and pass.

**THE ROUND-1..4 APPARATUS IS DELETED. DO NOT REINTRODUCE ANY PART OF IT.** Gone, entirely: the
six-step extraction rule, the marker-TOKEN strip, the verbatim-quote span drop, the backtick
capture-and-refuse predicate, the comma-stripping, the identifier-shape strip, the closed-span-set
idea, and the allow-list — empty or otherwise. Three consecutive review rounds put a blocker inside
that one mechanism and each added step opened the next escape. **The escalating complexity WAS the
defect.** What replaces it is exact string containment over a pre-registered set, in two short
functions with nothing between the input and the assertion.

**THE PRE-REGISTERED SET — ELEVEN FIELD PATHS, DECLARED ONCE AS A MODULE-LEVEL CONSTANT
`REQUIRED_FIGURE_PATHS`, USED BY BOTH HALVES.** 23-11's Part B pins these names under the heading
*THE PUBLISHED FIGURES ARE PRE-REGISTERED FIELD PATHS* (locate it by that text); this is the same
list and the two must not drift: `training.non_dp.training_seconds_mean`,
`training.non_dp_superseded_protocol.training_seconds_mean`,
`training.non_dp.wall_clock_gap_vs_superseded`, `training.dp_n8.seconds_total`,
`training.dp_n64.seconds_total`, `generation.h_per_point_floor`, `generation.h_per_point_ceiling`,
`ratios.non_dp.eval_over_training_ceiling`,
`ratios.non_dp_superseded_protocol.eval_over_training_ceiling`,
`ratios.dp_n8.eval_over_training_ceiling`, `ratios.dp_n64.eval_over_training_ceiling`.

**THIS IS NOT AN ALLOW-LIST, AND THE POLARITY IS THE PROOF.** An allow-list is a set of tokens the
guard IGNORES, and it grows every time something legitimate trips it — which is why the previous
design kept widening. This is the set of figures the guard REQUIRES: every member must be PRESENT
(half one), and nothing long may be present that is not a member (half two). Adding an entry makes
the guard demand strictly more, never less. There is no direction in which growing it weakens
anything.

**THE CONTINUATION SLICE, AND WHICH FILES EACH HALF BINDS ON.** Both halves run on the text strictly
between `<!-- 23-12-CONTINUATION-BEGIN -->` and `<!-- 23-12-CONTINUATION-END -->`, which Task 1
writes. **Count with `text.count(SENTINEL)`, not with a line-based tool**, and assert the count is
exactly 1 for each sentinel in each file, with BEGIN's index before END's; a missing or duplicated
sentinel FAILS rather than silently scanning the wrong slice. `text.count` is required by name
because the obvious shell equivalent is wrong in exactly the way W1 documents two pages above:
`grep -c` counts LINES, so two BEGIN sentinels emitted on ONE line pass a `grep -c ... = 1` check
while `text.split(BEGIN)[1].split(END)[0]` then scans only the first span and the second goes
unguarded. **MEASURED at HEAD:** `.planning/REQUIREMENTS.md:352` carries **2** `RETRACTED IN PLACE`
markers on a single line and `grep -c` returns **1** — the defect is live in this repository's own
files, not hypothetical.

**They do NOT bind on the same set of files, and the asymmetry is deliberate.** Half one — every one
of the eleven figures PRESENT — binds on `.planning/REQUIREMENTS.md` **only**, because that is the
file whose continuation is required to publish the full set;
demanding all eleven inside the `.planning/ROADMAP.md` and `.planning/STATE.md` continuations would
force those two documents to become copies of the first, which is not what a correction to a
milestone preamble or a status line should look like. Half two — nothing long that is not sourced —
binds on **all three** slices, because an invented figure is just as false in a status line as in a
requirements preamble, and there is no reason to leave two of the three files unguarded. State this
split in both docstrings; an executor who binds half one on all three will be unable to satisfy it
without over-writing two documents, and one who binds half two on only one leaves two open. **Do not scan whole files.** MEASURED at HEAD,
`.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md` and `.planning/STATE.md` already contain
**69 / 20 / 86** numeric literals with 8 or more fractional digits — committed measurements from
earlier phases, none of them leaves of `results/phase23_cost.json` — so a whole-file scan would RED
on all 175 of them.

**THE RENDERING.** Resolve a dotted path by walking the loaded record's keys, then render the leaf
with `repr()`. Because 23-11 writes the record with `json.dump`, which serialises floats through
`float.__repr__`, that string is byte-identical to what the record file itself carries. Do not
format, do not round, do not add separators.

**HALF ONE — `test_the_correction_quotes_the_cost_record_faithfully` (record → continuation).** For
each of the eleven paths, assert its rendering appears in the slice by plain `in` containment. Put
the PATH and the rendering in the failure message, not just the number, so a RED says which figure
went missing. **This is also the half that catches a ROUNDING**: if the author writes `2.04` where
`2.035849685343305` is required, the required string is simply absent and this half REDs.

**HALF TWO — `test_the_continuation_invents_no_figure` (continuation → record) — the headline guard,
and the one truth 2 names.** Find every match of the module-level pattern in the slice and assert
every matched token is one of the eleven renderings. **That is the entire rule.** Nothing
is dropped, nothing is classified, no span is captured, nothing is exempted. Three consequences fall
out by construction rather than by a patch: a figure written inside backticks is scanned exactly like
one in prose, because backticks are not special to this rule; a figure written on the marker line is
in scope, because no line is dropped; and a figure carrying a thousands separator or a trailing unit
letter is CAUGHT rather than laundered, because the comma is inside the pattern and there is no
trailing lookahead to backtrack against.

**W4 — THE PATTERN IS BUILT FROM `MIN_FRACTIONAL_DIGITS`, NOT WRITTEN BESIDE IT.** An earlier draft
required the named constant in one place and gave the pattern as a literal `\d{8,}` in another, with
nothing tying them: retuning the constant would have changed nothing and the name would have been
decoration. Construct it once, at module level, from the constant —
`LONG_FIGURE = re.compile(rf"\d[\d,]*\.\d{{{MIN_FRACTIONAL_DIGITS},}}(?:[eE][+-]?\d+)?")` — and
use that object in both the guard and the tripwire. **VERIFIED: at `MIN_FRACTIONAL_DIGITS = 8` the
built pattern string is byte-identical to the literal `\d[\d,]*\.\d{8,}(?:[eE][+-]?\d+)?`, and
re-binding the constant to 12 yields `\d{12,}` — so the tie is real and the constant is load-bearing.**

**WHY 8, AND WHY SHAPE SEPARATES THE TWO POPULATIONS — MEASURED, NOT ASSUMED. State this in the
test's docstring.** Every float leaf of the four source records this phase reads
(`phase23_matched_control`, `phase23_control_floor`, `phase23_sigma_zero`,
`phase23_never_taught_training`) was histogrammed by fractional-digit count. The distribution is
BIMODAL with an empty band, and the threshold sits inside it:

| fractional digits | float leaves | what lives there |
|---:|---:|---|
| 1-5 | 130 | exact rationals — recall rates `k/n`, `0.0`, the `0.0003` LR, `1000000.0` |
| 6-11 | **0** | **empty — the separating band** |
| 12-18 | 259 | every measured continuous quantity — wall clock, gradient norms, perplexities, ratios |

Below the threshold sit all the numerals the prose legitimately carries: dates (`2026-08-28`), plan
ids (`23-12`), `D-03`, `CAL-05`, `SC2`, `sha256`, `dp_n64`, citations like `23-RESEARCH.md:637-641`,
the quoted `1,010` and `17`, and the h/point table's `4.77` / `2.45` / `1.67` / `0.90`. None has a
fractional part 8 digits long. **A sha256 digest is excluded STRUCTURALLY, not by exemption**: hex
digests carry long digit runs but never a `.` immediately preceded by a digit, so the pattern cannot
match inside one — MEASURED against a full 64-char digest, zero matches. The longest fractional
rendering this repository produces structurally is **6**, from
`phase23_prereg.noised_record_path`'s six-decimal sigma (`…_sigma0.500000.json`), which the threshold
clears with two digits of margin on one side and four on the other.

**WHAT THE SHAPE RULE DOES NOT CATCH — THE RESIDUAL, STATED RATHER THAN PATCHED.** It catches every
invented figure written as ASCII digits around an ASCII `.` with 8 or more fractional digits.
**W3 — TWO NARROW EXCEPTIONS TO THAT, BOTH MEASURED, BOTH STATED RATHER THAN PATCHED. An earlier
draft claimed it caught "every invented figure with 8 or more fractional digits"; that OVERSTATED
the coverage and is corrected here:**

  - **A SIGN FLIP IS NOT CAUGHT.** The pattern has no sign class, so `-2.035849685343305` matches as
    `2.035849685343305`, which IS a pre-registered rendering — MEASURED, half two returns `[]` — and
    the required string is present as a substring, so half one passes too. **Both halves go green on
    a sign-flipped figure.** It is NOT patched with `[-−]?`, and that is a measured decision, not an
    omission: MEASURED, adding the sign class makes a legitimate figure written in a hyphenated range
    (`79-161.1239542257311`) match as `-161.1239542257311` and FALSE-RED a correct continuation.
    Trading a narrow real hole for a false-RED channel is exactly the round-1..4 pattern this
    revision exists to stop. The eleven published quantities are seconds, hours and ratios — all
    positive — so a negative rendering of one is nonsense prose a reader catches, and the SUMMARY's
    sub-threshold inventory is not where it would hide. **Disclose it; do not patch it.**
  - **A NON-ASCII DECIMAL SEPARATOR IS NOT CAUGHT.** `\.` matches only U+002E, so
    `37٫51234567890123` (U+066B) and `37．51234567890123` (U+FF0E) never match — MEASURED, half two
    returns `[]` for both. Note the asymmetry, because it is the useful half: Unicode DIGITS around
    an ASCII point ARE caught, since `\d` is Unicode-aware by default — MEASURED, `٣٧.٥١٢٣٤٥٦٧٨`
    matches and REDs. Only the SEPARATOR is ASCII-only.

Both exceptions require the author to type a character or a sign that has no place in the register,
and neither can displace a required figure, since half one still demands all eleven renderings
verbatim. **Say both in the test docstring and in the SUMMARY. Do not widen the pattern to chase
them.**

Beyond those two, it does **not** catch an invented SHORT numeral —
an integer, a date-shaped number, or a decimal with 7 or fewer fractional digits. Shape cannot
separate those from the short numerals the prose legitimately carries, and closing that half would
require enumerating them, which is an allow-list: the thing being deleted. **A stated residual beats
a fourth patched hole**, so it is disclosed here and in the SUMMARY rather than mechanised away. It
is bounded two ways. First, every figure the continuation is REQUIRED to publish is pre-registered
and asserted present at full precision, so a short invention can only ever be an ADDITIONAL,
unrequired number — it cannot displace, corrupt or stand in for a required one. Second, the
acceptance criteria below require the SUMMARY to list every sub-threshold numeral in the slice and
say what each one is, which makes the residual auditable by a reader even though it is not
mechanised. A narrower second residual: if a pre-registered leaf's own rendering happened to fall
below the threshold, half one would still assert it verbatim but half two would not cover a forged
copy of it — MEASURED, no float leaf of the four source records lands in the 6-11 band at all, so
this case does not currently arise; if one ever does, disclose it, do not re-tune the threshold to
chase it.

**ONE SEAM, BOTH HALVES, SO THE TRIPWIRES ARE DRIVABLE.** Implement both as module-level helpers
taking the slice TEXT and the loaded RECORD as parameters — `_required_figures_missing(text, record)`
and `_long_figures_not_sourced(text, record)` — each returning a list of violations. The tests call
them on the committed files; the tripwires call them on `tmp_path` copies. A guard that reads a
hard-coded path takes no input and cannot be driven, which is the same seam requirement 23-11's
Task 1 tripwire carries.

Add `test_no_file_carrying_the_claim_was_left_uncorrected`: scan **`.planning/*.md`
non-recursively** — `Path(".planning").glob("*.md")`, never `rglob` — for the **normalized CLAIM
TEXT**, not for the bare string `1,010`, and assert every file that contains it also contains a
`RETRACTED IN PLACE` marker naming `23-12`. Matching the bare numeral would flag
`.planning/ROADMAP.md:645`, this plan's own entry in the plan list, which mentions the figure while
carrying no claim. **This is the guard against a partial fix** — the claim lives in three files and
a correction landing in one leaves two standing. Implement the scan over the directory rather than
over a hand-listed tuple, so a fourth copy appearing at `.planning/` top level later is caught.

**The scope is PINNED at one level, and the docstring must say why.** Made recursive, the same scan
sweeps `.planning/phases/**` and reaches artifacts that quote "~1,010×" as the record of what was
believed when they were written — the phase's `CONTEXT`, `RESEARCH`, `VALIDATION` and several PLAN
documents — and would demand a retraction marker inside each. Phase artifacts are dated records of a
phase's own reasoning; retro-editing them is the opposite of retract-in-place, and `23-RESEARCH.md`
is where the falsifying measurement is recorded in the first place. The documents a reader consults
for what is *currently* believed are exactly `.planning/*.md`. Say this in the docstring, so the
next person who notices the phase artifacts does not "fix" the scope by widening it.

Add `test_the_h_per_point_table_is_disclosed_as_a_floor`: assert the `.planning/REQUIREMENTS.md`
continuation contains `h_per_point_ceiling` and the measured ceiling value from the record, and that
the original table's `4.77` row is still present.

**Prefer AST or a scoped-path read over a bare repo-wide grep in every criterion here.** This test
file's own docstrings will contain `1,010`, `RETRACTED IN PLACE` and `h_per_point_ceiling`; a grep
criterion that does not scope its path will match its own prose. Where a `grep -c` is unavoidable,
scope it to the planning file and pipe `wc -l` output through `tr -d ' '`.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase23_cost.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest "tests/test_phase23_cost.py::test_cost_claim_correction_is_additive" -v` collects **3** cases (one per file) and all pass
    - `.venv/bin/python -m pytest "tests/test_phase23_cost.py::test_no_file_carrying_the_claim_was_left_uncorrected" -v` exits 0, and its implementation scans the directory rather than a hand-listed tuple — verified by AST, that a `glob` (not `rglob`) call is made on a `.planning` path: `.venv/bin/python -c "import ast,pathlib;t=ast.parse(pathlib.Path('tests/test_phase23_cost.py').read_text());f=[n for n in ast.walk(t) if isinstance(n,ast.FunctionDef) and n.name=='test_no_file_carrying_the_claim_was_left_uncorrected'][0];print(sorted({c.func.attr for c in ast.walk(f) if isinstance(c,ast.Call) and isinstance(c.func,ast.Attribute)} & {'glob','rglob'}))"` prints `['glob']`. **THE `grep -c "rglob"` HALF OF THIS CRITERION IS DELETED, DELIBERATELY:** the action above REQUIRES a docstring explaining why the scope is not recursive, that docstring necessarily contains the word `rglob`, and a `grep -c` over the file counts its own prose — the criterion would false-RED the very docstring this plan mandates. The AST check is strictly stronger anyway: it reads the CALL, not the text
    - The completeness scan matches the normalized CLAIM TEXT, not the bare numeral — demonstrated by the test passing while `.planning/ROADMAP.md:645` carries `1,010` with no retraction marker beside it
    - **THE PRE-REGISTERED SET IS THE SAME ELEVEN PATHS 23-11 PINS, AND DRIFT IS ASSERTED, NOT ASSUMED.** `.venv/bin/python -c "import ast,pathlib;t=ast.parse(pathlib.Path('tests/test_phase23_cost.py').read_text());v=[n for a in t.body if isinstance(a,ast.Assign) for n in a.targets if isinstance(n,ast.Name) and n.id=='REQUIRED_FIGURE_PATHS'];print(len(v)==1);print([e.value for e in [a for a in t.body if isinstance(a,ast.Assign) and any(getattr(n,'id','')=='REQUIRED_FIGURE_PATHS' for n in a.targets)][0].value.elts])"` prints `True` and the eleven dotted paths. The SUMMARY diffs that list against 23-11's table by eye and states they match; a mismatch is escalated, never silently reconciled
    - **BOTH HALVES PASS, and the SUMMARY names which defect each one catches:** `.venv/bin/python -m pytest "tests/test_phase23_cost.py::test_the_correction_quotes_the_cost_record_faithfully" "tests/test_phase23_cost.py::test_the_continuation_invents_no_figure" -v` exits 0. **The second is the criterion for must_haves truth 2** — the faithfulness half runs record → continuation and cannot catch an invented figure at all, and the invention half cannot catch an omission or a rounding
    - **W5 — BOTH HALVES ARE MODULE-LEVEL HELPERS TAKING TEXT AND RECORD, or the tripwires are undrivable.** `_required_figures_missing(text, record)` and `_long_figures_not_sourced(text, record)`, each returning a list of violations; the tests call them on the committed slices, the tripwires on `tmp_path` copies. Verified by AST that both are module-level `FunctionDef`s with exactly two positional parameters: `.venv/bin/python -c "import ast,pathlib;t=ast.parse(pathlib.Path('tests/test_phase23_cost.py').read_text());print({n.name:[a.arg for a in n.args.args] for n in t.body if isinstance(n,ast.FunctionDef) and n.name in ('_required_figures_missing','_long_figures_not_sourced')})"` prints both names with two args each
    - **THE TRIPWIRE IS PARAMETRIZED OVER FIVE CONSTRUCTED CASES — the three writing channels, plus a rounding and an omission — and each is watched RED on a `tmp_path` COPY, never on the committed file.** `.venv/bin/python -m pytest "tests/test_phase23_cost.py::test_the_guard_catches_a_constructed_defect" -v` collects **5** cases and all pass: (i) an invented long figure written BARE; (ii) the same figure INSIDE BACKTICKS; (iii) the same figure on the MARKER LINE; (iv) a required figure written as a ROUNDING (`2.04` for `2.035849685343305`); (v) a required figure OMITTED entirely. Cases (i)-(iii) must RED on `_long_figures_not_sourced`; cases (iv)-(v) must RED on `_required_figures_missing`. **The SUMMARY quotes the helper's literal return value for all five.** These outcomes were VERIFIED IN ROUND 5 by implementing the two helpers from this plan's text and running them against a computed fixture — Task 1 carries the six-row output table, including the compliant control that returns `[]` from both halves
    - **NO ALLOW-LIST EXISTS, AND ITS ABSENCE IS ASSERTED STRUCTURALLY.** `.venv/bin/python -c "import ast,pathlib;t=ast.parse(pathlib.Path('tests/test_phase23_cost.py').read_text());print([n.id for a in t.body if isinstance(a,ast.Assign) for n in a.targets if isinstance(n,ast.Name) and 'ALLOW' in n.id.upper()])"` prints `[]`. The SUMMARY quotes BOTH helper bodies **in full** — they are short enough that a reader can confirm by eye that nothing is dropped, no span is captured or classified, and no token is exempted
    - **THE THRESHOLD IS A NAMED CONSTANT, THE PATTERN IS BUILT FROM IT, AND THE TIE IS ASSERTED (W4).** `MIN_FRACTIONAL_DIGITS = 8` is a module-level constant and the pattern object is constructed from it in a module-level assignment, so retuning the constant retunes the guard. Verified by AST that the constant's NAME is loaded inside the pattern assignment, not merely present elsewhere in the file: `.venv/bin/python -c "import ast,pathlib;t=ast.parse(pathlib.Path('tests/test_phase23_cost.py').read_text());a=[n for n in t.body if isinstance(n,ast.Assign) and any(getattr(x,'id','')=='LONG_FIGURE' for x in n.targets)][0];print('MIN_FRACTIONAL_DIGITS' in {n.id for n in ast.walk(a.value) if isinstance(n,ast.Name)})"` prints `True`. A `grep` would not do — the docstring this plan mandates names the constant in prose, so a text check passes against a decorative constant beside a hard-coded `\d{8,}`. The test docstring states the bimodal separation. The SUMMARY re-derives the fractional-digit histogram over the float leaves of the four source records AND of `results/phase23_cost.json` itself, and quotes it. **If any leaf lands in the 6-11 band, say so and disclose it — do not re-tune the threshold to chase it**
    - **THE RESIDUAL IS DISCLOSED, NOT PATCHED.** The SUMMARY lists **every** numeric literal in the `.planning/REQUIREMENTS.md` slice with FEWER than 8 fractional digits and says what each one is (date / plan id / decision id / quoted falsified figure / table column / citation / digest). If any of them turns out to be a figure that should have been a pre-registered full-precision leaf, that is a Task 1 defect and the SUMMARY escalates it. **No allow-list is added in either case**
    - **THE SLICE IS THE SENTINEL SPAN, AND WHOLE-FILE SCANNING IS REFUSED.** The SUMMARY records the measured reason: `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md` and `.planning/STATE.md` carry **69 / 20 / 86** literals with 8+ fractional digits at HEAD, none of them cost-record leaves. Re-measure those three counts before Task 1 writes and quote them; a count that moved means an earlier phase's document changed and the slice discipline matters more, not less
    - The line-wrap-tolerant matcher is IMPORTED, not re-written — **by AST over the import nodes, not `grep`.** The action requires a docstring explaining the normalizer, and this plan's own criteria discuss `_prose` by name, so a `grep -n "_prose"` is satisfied by the file's own prose while a hand-rolled second normalizer sits beside it: `.venv/bin/python -c "import ast,pathlib;t=ast.parse(pathlib.Path('tests/test_phase23_cost.py').read_text());print(sorted({a.name for n in ast.walk(t) if isinstance(n,ast.Import) for a in n.names} | {n.module for n in ast.walk(t) if isinstance(n,ast.ImportFrom) and n.module}))"` includes `_prose`
    - `.venv/bin/python -m pytest tests/ -q` reports 0 failed; `make lint` exits 0
    - `git diff --exit-code -- results/ scripts/mitigation_accountant.py scripts/mitigation_gate.py scripts/phase23_prereg.py scripts/phase23_matched_prereg.py scripts/phase23_resume_prereg.py scripts/mitigation_budget.py` exits 0
    - `git log --format=%H -- scripts/phase23_matched_prereg.py | wc -l | tr -d ' '` still prints **1**
  </acceptance_criteria>
  <done>A directory-scanning guard proves every planning file carrying the claim also carries the dated retraction, the original text is asserted still present through the imported line-wrap-tolerant matcher, and every quoted figure and protocol label is checked against the cost record.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a falsified figure → a downstream budget | The "~1,010×" ratio is cited as the reason no sweep density may be chosen without it. A wrong margin propagates into Z. |
| a PROJECTION → a published correction | The `20.4 s` / `843x` pair is a loop-only projection whose lower-bound status research already retracted. Publishing it inside a retraction as the surviving half would launder an estimate into a measurement. |
| "the non-DP arm" → a number | Two measured non-DP protocols differ by `training.non_dp.wall_clock_gap_vs_superseded` = 2.035849685343305. An arm name alone does not identify a figure. |
| correction → rewrite | An "append" that moves or deletes the original destroys the record of what was believed. |
| one file → three files | The claim is copied into three planning documents; a partial correction leaves two standing and looks complete. |
| a claim-completeness scan → its own plan-list line | `.planning/ROADMAP.md:645` mentions `1,010` while carrying no claim. A numeral-matching scan would demand a retraction beside a plan description. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-64 | Tampering | the original claim edited in place instead of continued | mitigate | Deletion counts asserted 0 in `git diff --numstat` **apart from exactly FOUR lines in `.planning/REQUIREMENTS.md`** — the two checkbox rows and the two traceability rows, each a modify (one deletion + one insertion) — with every deletion accounted for by name; the original text asserted still present through the imported line-wrap-tolerant matcher |
| T-23-64b | Spoofing | a projection published inside the retraction as a measurement | mitigate | The plan names both non-measurements explicitly and forbids them; every quoted number must load from `results/phase23_cost.json`; the SUMMARY must state whether any real run reproduces the projection |
| T-23-64c | Repudiation | a re-scoping to "the non-DP arm" that names no protocol | mitigate | Every quoted training figure carries its `protocol`, and the test derives the acceptable protocol strings FROM the record rather than hardcoding them |
| T-23-65 | Repudiation | a partial correction covering one of three files | mitigate | `test_no_file_carrying_the_claim_was_left_uncorrected` scans `.planning/*.md` by glob, not by a hand-listed tuple; scope pinned non-recursive because `.planning/phases/**` artifacts quote the claim as the dated historical record |
| T-23-65b | Tampering | the completeness scan matching a numeral and flagging a plan description | mitigate | The scan matches the normalized CLAIM TEXT; the ROADMAP `:645` case is named in the plan and demonstrated passing |
| T-23-66 | Spoofing | a continuation quoting a figure no artifact contains | mitigate | `_long_figures_not_sourced` runs CONTINUATION → RECORD over the sentinel slice: every numeric literal with 8+ fractional digits must be one of the eleven pre-registered renderings. **No allow-list and no span classification** — the three writing channels (bare prose, inside backticks, on the marker line) are covered BY CONSTRUCTION because nothing is dropped and no span is captured. A 5-case tripwire is watched REDing. **Stated residuals, all disclosed rather than patched: (a) an invented SHORT numeral is not caught**, because shape cannot separate it from the dates, plan ids and quoted falsified figures the prose legitimately carries; it is bounded by half one (every required figure is asserted present at full precision, so a short invention can only be additional) and disclosed in the SUMMARY's sub-threshold inventory; **(b) a SIGN FLIP is not caught** — MEASURED, `-2.035849685343305` matches as the required rendering and passes both halves; `[-−]?` is deliberately NOT added because it MEASURABLY false-REDs a legitimate figure in a hyphenated range (`79-161.1239542257311`), and all eleven published quantities are positive so a negative rendering is nonsense a reader catches; **(c) a NON-ASCII DECIMAL SEPARATOR is not caught** — MEASURED, `37٫51234567890123` (U+066B) and `37．51234567890123` (U+FF0E) never match, though Unicode DIGITS around an ASCII point ARE caught since `\d` is Unicode-aware |
| T-23-66b | Tampering | a ROUNDING published in place of a measured figure | mitigate | `_required_figures_missing` asserts each pre-registered rendering appears VERBATIM; a rounding leaves the required string absent and REDs. Watched as tripwire case (iv), `2.04` against `2.035849685343305`. 23-11 stores the gap as a computed named field so there is a full-precision string to demand in the first place |
| T-23-66c | Tampering | the guard scanning the wrong text and passing vacuously | mitigate | The slice is the span between two sentinels Task 1 writes; exactly one of each per file, BEGIN before END, asserted. Whole-file scanning is refused with a MEASURED reason: the three files already carry 69 / 20 / 86 long literals from earlier phases |
| T-23-67 | Information Disclosure | a corrected ratio sitting beside an uncorrected floor-valued h/point table | mitigate | The continuation must disclose `h_per_point_ceiling` beside the table, asserted by its own test |
| T-23-68 | Tampering | a location resolved by line number that drifted | mitigate | Every location resolved by TEXT; the plan states the repository's own recorded lesson about line numbers |
| T-23-68b | Tampering | a `gsd-sdk` mutation handler corrupting STATE/ROADMAP frontmatter | mitigate | Hand-edit only, per 23-19's recorded decision; `git diff` on both files immediately if any handler is used |
| T-23-SC | Tampering | npm/pip/cargo installs | accept | **Zero installs.** `pyproject.toml` byte-unchanged (RPT-03) |
</threat_model>

<verification>
```bash
grep -c "RETRACTED IN PLACE" .planning/REQUIREMENTS.md | tr -d ' '
grep -c "1,010" .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md
git diff --numstat -- .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md
.venv/bin/python -c "import json;d=json.load(open('results/phase23_cost.json'));print(sorted(d['training']));print([b.get('protocol') for b in d['training'].values()]);print(d.get('ratios'))"
.venv/bin/python -m pytest tests/test_phase23_cost.py -v
git diff --exit-code -- results/ scripts/ pyproject.toml
git log --format=%H -- scripts/phase23_matched_prereg.py | wc -l | tr -d ' '
.venv/bin/python -m pytest tests/ -q
make lint
```
</verification>

<success_criteria>
- All three planning files carry a dated `RETRACTED IN PLACE (plan 23-12)` continuation
- Every original sentence is byte-unchanged; deletion counts are 0 apart from FOUR lines in `.planning/REQUIREMENTS.md` — the two checkbox rows (`CAL-01`, `CAL-05`) and the two traceability rows those ticks require filling — each accounted for by name
- **Every quoted figure is read from `results/phase23_cost.json` BY PRE-REGISTERED FIELD PATH, at FULL STORED PRECISION, and checked IN BOTH DIRECTIONS by exact string containment over that one set — record → continuation for omissions and roundings, continuation → record for inventions. The round-1..4 six-step extraction, allow-list and span classification are DELETED, and the guard is watched REDing on five constructed defects on a `tmp_path` copy: bare / backticked / marker-line inventions, a rounding, and an omission. No projection is quoted as a measurement and no figure comes from this plan's prose**
- **The invention half's coverage is bounded by SHAPE, not by enumeration, and the residual is STATED: an invented numeral with fewer than 8 fractional digits is not caught, because shape cannot separate it from the dates, plan ids and quoted falsified figures the prose legitimately carries. The threshold sits in a MEASURED empty band (no float leaf of the four source records has 6-11 fractional digits), and the SUMMARY inventories every sub-threshold numeral in the slice rather than allow-listing any of them**
- Every training figure the continuation quotes names its `protocol`, and both non-DP protocols are disclosed side by side with the gap stated
- What does not change is stated from all EIGHT of the record's own ratios, not asserted in advance — while only the FOUR pre-registered ceiling ratios are QUOTED, the floor side informing the sentence without its numerals entering the continuation
- The h/point table's floor status and the measured ceiling are disclosed with the table left standing
- A directory-scanning guard over the normalized claim text makes a fourth uncorrected copy impossible to miss, without flagging the plan-list line that merely mentions the numeral
- No `gsd-sdk` mutation handler touched STATE.md or ROADMAP.md
</success_criteria>

<output>
Create `.planning/phases/23-cost-calibration-the-0-diagnostic-and-budget-pre-registratio/23-12-SUMMARY.md` when done.
</output>
