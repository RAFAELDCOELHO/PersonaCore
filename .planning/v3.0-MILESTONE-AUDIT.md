---
milestone: v3.0
milestone_name: Adversarial Privacy Audit and Selective Memory Erasure
audited: 2026-08-19
status: tech_debt
status_history:
  - status: gaps_found
    date: 2026-08-19
    commit: 28b3c6e
    reason: "B1 open — Phase 19's retroactive scope limit on Phase 18's rank/exposure readings never reached either Phase 18 published artifact. Integration 15/16."
  - status: tech_debt
    date: 2026-08-19
    commit: ff8f4d4
    reason: "B1 CLOSED and re-verified substantively (not cosmetically) by an independent integration re-check — integration now 16/16. W1 CLOSED. No critical blocker remains. NOT `passed`: 16 tech-debt items across 4 phases, three PARTIAL Nyquist phases, and W1's own fix introduced a new WARNING-level defect (README republishes the retired four-defect undercount). `passed` requires minimal tech debt; 16 items is not minimal."
  - status: tech_debt
    date: 2026-08-19
    commit: 4ed15d9
    reason: "Third run. W2 and B1-b CLOSED and fact-checked clean against code and source. B1-a PARTIALLY CLOSED — the redirect is true and well-justified but still does not reach the deep-link reader it was written for, a structural consequence of the freeze discipline. Integration holds at 16/16. Still NOT `passed`: round 2 introduced N1 (a fresh miscount in correction text, contradicted by its own quotation eight lines above) and N2 (an unanchored distance figure). THREE consecutive remediation rounds have each introduced a new defect while closing an old one."
scores:
  requirements: 29/29
  phases: 4/4
  integration: 16/16
  flows: 3/3
  tests: 845 passed, 1 skipped, 0 failed (exit 0)
  lint: ruff check clean, 170 files formatted
gaps:
  requirements: []
  integration: []
  flows: []
closed_gaps:
  - id: B1
    closed: 2026-08-19
    commit: 7af6006
    from: 19-selective-memory-erasure
    to: 18-black-box-adversarial-extraction-audit
    requirements: ["ATK-04", "ERASE-01", "STAT-06"]
    status: closed_verified
    verification: >-
      Re-checked independently, judged on substance rather than on a grep hitting. The continuation
      at results/phase18_extraction_report.md:340-405 states the limit as a heading and in bold
      ("RETROACTIVE WEIGHT ON PHASE 18, stated rather than implied, and a real limit rather than a
      softening"), reaches the 73 zero-cells at :236 BY NAME AND LINE NUMBER while quoting item (4)
      verbatim ("Those 73 cells sit INSIDE this limit, not in its exemption"), and correctly names
      what is NOT affected (ASR ladder, 92/104, adapter-off 0/104, ATK-03 positive control,
      LEAKAGE_DEMONSTRATED, the (92,104,0,104) handoff — all generation-based, none retracted).
      Every number it cites was cross-checked against results/phase19_erasure_report.md and matched,
      including the correct 16-digit 77.6370113463966%. The `## Verdict` block is byte-identical
      pre/post (sha256 c7b5af3bc7b411ba both sides); LEAKAGE_DEMONSTRATED untouched.
    original_issue: >-
      Phase 19 authored a RETROACTIVE scope limit on Phase 18's rank/exposure readings and never
      propagated it to the Phase 18 published artifact.
  - id: W1-original
    closed: 2026-08-19
    commit: 24d49ad
    requirements: ["ATK-06", "ERASE-01"]
    status: closed_verified_but_introduced_W2
    verification: >-
      README.md:220-257 publishes 92 of 104 = 88.5% (rate 0.884615, one-sided 95% Wilson lower bound
      0.8231) against 0/104 at identical budget with the question unit stated; re-scopes the LoRA
      caveat ("What the caveat no longer does is explain a low number, because there was never a low
      number to explain"); and adds Phase 19's FAILURE / DO NOT SHIP. No linear-reader contradiction
      — the superseding heading is the very next line after the stale paragraph and names it
      explicitly. README:212-218 byte-identical to 28b3c6e. See warnings.W2 for the defect this fix
      introduced.
    original_evidence: >-
      At 28b3c6e: grep -ci "retroactive weight on phase 18"
      results/phase18_extraction_report.md -> 0. All 4 Phase-19 mentions in that file (lines 240,
      244, 250, 334) were the FORWARD erasure_is_worth_attempting handoff written 2026-08-17, two
      days before the limit existed. The file's own closing line calls Phase 18's evidentiary
      record "sound and ready to be cited". docs/REPORT.md:1140-1141 sends the reader OUT to that
      unqualified file specifically for "the exposure table with all three frames and both
      reductions". Its 73 measured zero-cells were published un-flagged at :236.
    fix_used: >-
      scripts/_addendum.py append_addendum under the IDENTITY marker pair
      (pending=recorded=EXTRACTION_SHIP_RECORDED_LINE — PENDING was already consumed at 0
      occurrences, making the identity call the only legal one and the replacement a provable
      no-op). Guarded by tests/test_phase18_docs.py::test_extraction_report_addendum_is_additive,
      which derives the pre-append revision from git history, asserts the byte prefix, and asserts
      the verdict block is unchanged with LEAKAGE_DEMONSTRATED still in it. +67 / -0.
warnings:
  - id: N1
    new: true
    introduced_by: c90d0c8
    severity: warning
    requirements: ["ATK-04"]
    issue: >-
      **Round 2's fix published a fresh miscount, contradicted by its own quotation eight lines
      above.** docs/REPORT.md:1283-1284 describes Phase 18's exposure table as carrying "the
      token-length spread and **the three reductions**". There are **TWO** reductions (`sum`,
      `mean`) across THREE frames (`ans1`, `f4_reversed`, `f3_bare`).
    evidence: >-
      Verified independently. The table header at results/phase18_extraction_report.md:145 carries
      six NLL columns — ans1/sum, ans1/mean, f4_reversed/sum, f4_reversed/mean, f3_bare/sum,
      f3_bare/mean = 3 frames x 2 reductions. phase18:141 states "All six frame x reduction NLLs";
      phase18:156 states "the TWO reductions give ordinally identical ranks". And
      docs/REPORT.md:1276 — eight lines ABOVE the error, inside the same appended section — quotes
      the pointer verbatim as "all three frames and BOTH reductions".
    not_load_bearing: >-
      The paragraph's actual point — that the table is a rank-only reading with no generation
      number beside it — is TRUE and independently verified. Only the count is wrong.
    fix: One word, "the three reductions" -> "both reductions", as a further dated continuation.
    resolved: true
    resolved_by: 5703bbe
    resolution: >-
      CLOSED. Corrected in a dated continuation at docs/REPORT.md:1316-1326 — corrections only, no
      new argument, so the prose-volume mechanism that produced W2/N1/N2 was not re-run. Verified
      before commit with a WHITESPACE-NORMALISED sweep, because the error is line-wrapped as
      "the three\nreductions" and a single-line `grep -c` returns 0 on it. Across all published
      surfaces the erroneous form occurred exactly ONCE (docs/REPORT.md) and nowhere else.
      12 insertions / 0 deletions; byte-prefix preserved; :1278 and :1283 left byte-identical as
      the additive discipline requires.
  - id: N2
    new: true
    introduced_by: c90d0c8
    severity: minor
    issue: >-
      docs/REPORT.md:1278 states the exposure table "sits **190 lines above** the dated
      continuation that scopes it". Measured: the table is at :145-154 and the continuation
      heading at :340, so the distance is 186 (from the last row) or 195 (from the header).
      190 matches no anchor — an approximation presented as exact, which this project's own
      evidence discipline does not permit.
    fix: One number, or drop the figure for "far above".
    resolved: true
    resolved_by: 5703bbe
    resolution: >-
      CLOSED in the same dated continuation. The false precision was DROPPED ("far above") rather
      than replaced with a corrected number — the figure was never load-bearing, and substituting
      one unverified number for another is what created it.
    origin: >-
      **This audit is the source of the error, not the executor.** The "190 lines" figure was
      first written in THIS file's own earlier B1-a warning text; the remediation copied it
      faithfully into docs/REPORT.md. An unverified figure in an audit artifact propagated into a
      published document — the same class of defect this audit exists to catch, committed by the
      audit itself.
  - id: B1-a
    status: partially_closed
    closed_by: c90d0c8
    severity: warning
    requirements: ["ATK-04"]
    issue: >-
      The redirect at docs/REPORT.md:1269-1314 is substantive and its structural claims are exact,
      but it does not change what the reader the warning named actually sees. The pointer is at
      :1140; the redirect is 128 lines below it at the end of the file. A reader following the deep
      link still lands on results/phase18_extraction_report.md:145 unscoped, and there is no
      forward pointer there either.
    structurally_forced: >-
      Both surfaces are frozen above their continuation lines — docs/REPORT.md:1145 asserts "No
      line above this heading is altered", and
      tests/test_phase18_docs.py::test_extraction_report_addendum_is_additive enforces the same on
      the Phase 18 report. The dated-continuation discipline leaves nowhere additive to put a
      pointer the deep-link reader would see. The fix states this tradeoff explicitly at :1300-1304.
      Recorded as debt rather than as a regression.
  - id: W2
    resolved: true
    resolved_by: 4012a61
    resolution: >-
      CLOSED and fact-checked clean. README:259-304 publishes the A-E table; all five labels,
      descriptions and pin line numbers were verified against BOTH
      results/phase19_erasure_report.md:568-574 AND the actual code in scripts/phase19_erasure.py
      (A :1562/:2948, B :3850-3855, C :2922, D :3811, E :3576 — each confirmed in its enclosing
      function). The four-vs-five distinction is confirmed by code: `_selected_components` has
      exactly two call sites, `_cmd_cal_erase` (:3553) and `_cmd_erase` (:3687), never
      `_cmd_report` — so E genuinely cannot be one of the four `_cmd_report` failures.
    introduced_by: 24d49ad
    requirements: ["ERASE-01"]
    severity: warning
    issue: >-
      **The W1 fix republished a number its own source had already retired.** README.md:253-255
      reads "the verdict was reached on a hand-driven path around FOUR published defects in that
      pin" and "ALL FOUR defects with their dated corrections stand exactly as published".
      results/phase19_erasure_report.md:578-584 identifies that exact phrasing and corrects it:
      "that phrase UNDERCOUNTS the phase ... the phase publishes FIVE. The fifth is E."
    evidence: >-
      Verified independently. Commit d898c7e (2026-08-19 18:35) exists SOLELY to retire the
      ordinal — "docs(19): give the five published pin defects unique labels A-E, as dated
      continuations". Commit 24d49ad (2026-08-19 19:49), 74 minutes later, reproduces the
      undercount. `grep -cE '\*\*E\*\*|defect E'` README.md -> 0; README carries 2 "four defects"
      phrases and names no letter. The erasure report grants ITSELF an in-file exemption (the four
      are the four that block the pin's own `_cmd_report`); README carries no such exemption, so
      "all four defects ... stand exactly as published" reads as a complete enumeration of
      published defects. It is not — defect E (`_selected_components`, pin :3576, published in
      results/phase19_reference_set_correction.md) is absent.
    why_it_matters: >-
      This is the SAME defect class W1 was raised for — a published figure that its source has
      since superseded, on the milestone's most-read surface. Fix is one dated line naming A-E
      and the four/five distinction, appended under the same additive discipline.
  - id: B1-b
    resolved: true
    resolved_by: 98d0a60
    resolution: >-
      CLOSED, and the mechanism claim verified TRUE — if anything understated. `exposure_rank`
      occurs exactly twice inside `null_result_is_admissible` (span :1527-1657): once in the
      docstring at :1567, once at :1636, where `is None` is the only operator ever applied to it.
      The terminal ternary at :1657 is verbatim
      `return (LEAKAGE_DEMONSTRATED if attack_successes > 0 else NULL_ADMISSIBLE), reasons`, and
      every path to INCONCLUSIVE returns above it (:1578, :1592, :1604, :1631, :1646). The
      continuation's claim is conservative — :1604-1611 also touches the rank key only as a
      presence/order check, reinforcing the mechanism.
    original_issue: >-
      B1's exemption was asserted, not shown. The continuation names item (4) as INSIDE the limit
      and LEAKAGE_DEMONSTRATED as exempt; those reconcile only through a mechanism the text never
      states. The mechanism is real and was verified in code — `null_result_is_admissible`
      (scripts/phase18_extraction.py:1527-1657) reads exposure only as a PRESENCE check
      (`zero_cells[key]["exposure_rank"] is None`), never as a value, and the terminal ternary is
      generation (`LEAKAGE_DEMONSTRATED if attack_successes > 0 else NULL_ADMISSIBLE`) — so the
      exemption is TRUE. The continuation flags the tension and then leaves it unresolved, so the
      skeptic's strongest objection ("item (4) gates the verdict, so how is the verdict exempt?")
      gets no answer.
tech_debt:
  - phase: 16-weight-vs-prompt-persistence-control
    items:
      - "I-1: stale `phase14_recall.py:1336` citations in phase16_persistence_report.md:111,226,257 — the function now lives at :1413. Claim independently confirmed true."
      - "I-2: phase16_persistence_report.md:105 prints a citable '~39 min' four-arm wall clock against four printed condition clocks summing to 137.2 min, and claims the sweep clock is 'reported separately' — no separate sweep clock is printed. WALL_CLOCK_NOTE is a pre-run constant."
      - "R-1: D-28's note is rendered as faithful English, not the verbatim Portuguese string, and is not read from `_CONTEXT_PATH` the way D-25's qualifier already is. Nothing ties the constant to 16-CONTEXT.md, so amending D-28 would not surface as a test failure."
      - "R-2: the pre-registration disclosure paragraph is itself post-run text (amended at 127d60d) and does not name itself in its own two-exception list."
      - "R-3: tests/test_phase16_driver.py:1782 `assert \"anomal\" in text.lower()` is also satisfied by the W-2 framing paragraph, so it is no longer independently load-bearing. The primary assertion carries the pin (mutation-proved)."
  - phase: 17-multi-persona-isolation-matrix
    items:
      - "DEF-17-01 (CONFIRMED OPEN, measured this session): `make lint` is red locally. Makefile:16 runs bare `ruff`, which resolves to a pyenv shim at ruff 0.1.15 (Jan 2024) while the project pins ruff~=0.15 and .venv/bin/ruff is 0.15.16. 9 files disagree on format; `ruff check` passes under both. CI is unaffected — it installs and runs the dev-extra 0.15.x. Fix: Makefile:16 -> `python -m ruff check . && python -m ruff format --check .` (intersects the recorded DEF-15-01 note that pointing at .venv/bin/ruff would break CI)."
      - "None of the 11 Phase-17 SUMMARY files carries a `requirements`/`requirements-completed` frontmatter field at all — they use requires/provides/affects/tags. The milestone 3-source cross-reference therefore has only TWO sources for ISO-01..07 and Phase 17's six STAT rows."
  - phase: 18-black-box-adversarial-extraction-audit
    items:
      - "W1 RESIDUAL (partially closed): the `## Pre-Registration` paragraph at results/phase18_extraction_report.md:5 still attributes `K = 48`, `ASR_RUNGS (1, 4, 16, 48)`, the four attack templates and `null_result_is_admissible()` to commit 13666c4. Measured at that commit: 272 lines, K = 64, ASR_RUNGS = (1, 4, 16, K), templates/ladder/admissibility function all absent. The 2026-08-17 dated continuation corrected only 2 of the 6 measured misattributions (licensed_conclusion, render_report). The load-bearing ORDERING property is intact and unaffected (12/12, and 63/63 milestone-wide)."
      - "Deferred: stale parenthetical in scripts/phase16_persistence.py:1605's docstring — `PERSONA_ALLOWLIST` 'stays at exactly two entries' became false when Phase 18's D-08 added its third sanctioned entry. Prose only; no test reads it."
  - phase: 19-selective-memory-erasure
    items:
      - "W1: PLAN frontmatter names 6 artifacts under stale names (phase19_cal_corpus, phase19_cal_adapter, phase19_calibration_arm, phase19_arm_m1, phase19_arm_m2, phase19_m2_retrain_adapter). All exist under the pin's own committed `phase19_arm_{arm}.json` / `phase19_erase_*` convention. `gsd-sdk query verify.artifacts` reports 6 missing artifacts that all exist. Cosmetic today, misleading to a future audit."
      - "Deferred: src/personacore/evaluation/perplexity.py:11-13 states the denominator as `corpus_len - n_windows`; the code strides by block_size so windows share their boundary token and the true denominator is `corpus_len - 1` (measured: 1000285 vs the docstring's 996378). Prose only — every committed PPL denominator is already the correct n-1, and the module's own test at :122 asserts it."
      - "Deferred: 19-13-SUMMARY.md ends with two stray literal `</content>` / `</invoke>` tags — tool-call scaffolding that leaked into the committed file."
  - phase: cross-cutting
    items:
      - "ERASE-01 and ERASE-02 are scoped BULLETS in REQUIREMENTS.md, not checkboxes, so `requirements mark-complete` returns not_found for both. Deliberate and correct (the Traceability rows carry far more than a checkbox would), but any future tooling that reads checkbox state will under-count v3.0 by two."
      - "16-04/16-05/16-06 SUMMARY `requirements-completed` values parse as a single string carrying an inline `#` comment rather than an empty list."
      - "The 77.6% dialogue-destruction figure exists in two forms. Every PUBLISHED surface carries `77.6370113463966%`, which is what `(1.2420966625043919 - 0.2777699357026435) / 1.2420966625043919` returns. A 17-digit variant `77.63701134639661%` — one spurious trailing digit — appears only in PLANNING artifacts (`19-12-SUMMARY.md`, `STATE.md`, quick-260819-r1u's PLAN). Published record is correct and self-consistent; origin is a closed phase's summary, so recorded rather than edited."
      - "`README.md`'s `## Results at a glance` (:13-50) still carries only v2.0 (M1/M2) numbers — no v3.0 headline, no DO NOT SHIP. The section built for skimming is the one that does not carry the milestone's result."
nyquist:
  compliant_phases: ["16"]
  partial_phases: ["17", "18", "19"]
  missing_phases: []
  overall: partial
human_gates:
  - phase: 17
    status: discharged
    detail: "All 4 items decided and executed 2026-08-15 (commits 1383773, 3c31648, a260d96). Item 4 exceeded its stated scope — five malformed frontmatters found, not the two reported, all five repaired."
  - phase: 18
    status: discharged_with_residual
    detail: "W2 (ship decision) CLOSED — results/phase18_extraction_report.md:307 now carries `## Dated continuation — 2026-08-17: the ship decision, recorded`. W3 (stale ledger) CLOSED — ROADMAP shows 18-15/18-16 checked, STATE.md reads milestone_complete 54/54. W1 PARTIALLY closed — see tech_debt."
  - phase: 19
    status: discharged_except_attestation
    detail: "W2 CLOSED at f98ea9f — docs/REPORT.md:1229-1234 now names SIX bounds including '(6) a verdict that is not mechanically reproducible by the pinned CLI alone', with a full `### Ship Decision — DO NOT SHIP` section at :1236-1260. W3 CLOSED — results/phase19_reference_set_correction.md:224-243 reconciles the duplicate ordinal ('Read every ordinal in THIS document as E'). I1 CLOSED at acd26a0. Human-verification item 1 — that the five checkpoint decisions (19-07, 19-09, 19-11, 19-12, 19-16) were made by the human at the time recorded — is an operator attestation no tool can discharge; every artifact is consistent with them."
---

# Milestone v3.0 — Adversarial Privacy Audit and Selective Memory Erasure

**Audited:** 2026-08-19 · **Status:** `gaps_found` · **Phases:** 16–19, all complete

**Definition of done (ROADMAP):** stop *asserting* that weight-based memory is private and start
**measuring** it — what weights actually buy over prompting, whether separately-taught personas
stay isolated under adversarial collision, and whether an adversary can extract taught facts
through a toggle that only ever controlled availability.

**That was achieved.** All 29 requirements are satisfied with independently re-derived evidence, all
four phases are verified, the pre-registration discipline held end-to-end, and the milestone closed
on a `FAILURE` verdict published unsoftened — which is the discipline working, not failing. One
cross-phase gap is open: a scope limit Phase 19 authored against Phase 18 never reached Phase 18's
published record.

---

## Requirements Coverage — 29/29 satisfied, 0 orphans

Cross-referenced across three sources: REQUIREMENTS.md traceability, each phase's VERIFICATION.md
requirements table, and each plan's SUMMARY.md `requirements-completed` frontmatter.

| Group | IDs | Phases | VERIFICATION | SUMMARY frontmatter | REQUIREMENTS.md | Final |
|---|---|---|---|---|---|---|
| Statistical discipline | STAT-01, 02, 05, 06 | 16, 17, 18, 19 | verified per phase | listed in 16/18/19 — **absent in all 17** | `[x]` | **satisfied** |
| Statistical discipline | STAT-03 | 17 | verified (Holm re-derived) | **absent** (17 carries no field) | `[x]` | **satisfied** (verified manually) |
| Statistical discipline | STAT-04 | 16, 17, 18 | verified | 16-01, 18-03/04/05 | `[x]` | **satisfied** |
| Persistence control | PERS-01 | 16 | verified (SC1) | **absent** — deliberately held back until the ladder RAN | `[x]` | **satisfied** (verified manually) |
| Persistence control | PERS-02…06 | 16 | verified | listed | `[x]` | **satisfied** |
| Isolation matrix | ISO-01…07 | 17 | verified (13/13 table, each re-derived) | **absent** (17 carries no field) | `[x]` | **satisfied** (verified manually) |
| Extraction audit | ATK-01…06 | 18 | verified (11/11) | listed | `[x]` | **satisfied** |
| Pre-registration | PREREG-01 | pre-milestone | `23a830c`, 1 commit, unamended | n/a | Complete | **satisfied** |
| Pre-registration | PREREG-02 | 16 | verified | 16-01 | `[x]` | **satisfied** |
| Erasure | ERASE-01, ERASE-02 | 19 | verified (6/6 must-haves) | 19-01/07/08/12/13/14/15 | Complete (bullets) | **satisfied** |

**No unsatisfied requirements. No orphans** — the union of every plan's declared requirements is
exactly the set assigned to its phase in ROADMAP.md, in all four phases.

**Two documentation-level cross-reference weaknesses**, neither of which changes a status:

1. **Phase 17's 11 SUMMARY files carry no requirements frontmatter field at all.** ISO-01…07 and
   Phase 17's six STAT rows are therefore cross-referenced against **two** sources, not three. Both
   remaining sources are strong: `17-VERIFICATION.md` carries a 13-row table naming each
   requirement's marking commit, claimant plan and a both-directions over/under-claim check, every
   row independently re-derived; and the integration check confirmed the wiring for all 13 today.
2. **PERS-01 appears in no Phase-16 SUMMARY.** This was deliberate and is recorded in three separate
   summaries (`16-04`, `16-05`, `16-06` each note "PERS-01 requires the ladder to RUN"), and `16-07`
   — the plan that ran it — closed nothing. `16-VERIFICATION.md` closed it as W-3 with the ladder at
   `5a17920` proved a git ancestor of the arm-JSON commit and of HEAD.

---

## Phase Verification — 4/4

| Phase | Status | Score | Notes |
|---|---|---|---|
| 16 — Weight-vs-Prompt Persistence Control | `passed` | 5/5 SC | Re-verification at `127d60d`; all 3 escalated warnings closed. Report **re-renders byte-identically** from the committed arm JSONs. 5 informational residuals. |
| 17 — Multi-Persona Isolation Matrix | `human_needed` → **discharged** | 5/5 SC, 13/13 reqs | All 4 human items decided and executed 2026-08-15. Gate re-derived to `True` and falsifies on both a short family and any single non-rejection. |
| 18 — Black-Box Adversarial Extraction Audit | `human_needed` → **discharged (1 residual)** | 5/5 SC, 11/11 reqs | Whole 48,511-byte report re-rendered byte-identically. W2 and W3 closed; W1 closed for 2 of its 6 measured misattributions. |
| 19 — Selective Memory Erasure | `human_needed` → **discharged except attestation** | 6/6 must-haves | Verdict `FAILURE` reproduced byte-identically through the committed gate. W2/W3/I1 all closed after the verification timestamp. |

**Neither the `FAILURE` verdict nor the `DO NOT SHIP` decision is scored as a defect.** The phase
contracted to run an erasure under a rule committed before any number existed and publish whatever
came out. It did. `erasure_succeeded` was called exactly once against `23a830c` (one commit,
unamended) and returned `FAILURE`; that FAILURE shipped at full strength, with the rank-vs-generation
co-headline published at equal weight. `DO NOT SHIP` withholds exactly one claim — that the verdict
is mechanically reproducible by the pinned CLI alone — and withdraws no measurement.

---

## Cross-Phase Integration — 15/16 links wired

Verified by re-derivation, not by reading claims.

### Wired

| Link | Evidence |
|---|---|
| **Shared statistics are single-sourced** (STAT-01/02/03/05/06) | Exactly **one** definition each of `cluster_bootstrap`, `sign_test_exact`, `holm` (`scripts/phase16_persistence.py:843/1088/1170`) and `wilson_upper_bound` (`scripts/erasure_gate.py:139`) across `scripts/`, `src/`, `tests/`. **Zero duplicate implementations, so zero drift is possible.** Phases 17/18/19 call them at real sites, and `phase17_personas.py:293` raises if Phase 16's family length differs from its own. |
| **Ancestry chain spans all four phases** (PREREG-01/02) | `23a830c` re-checked against the first-add commit of **all 63 tracked v3.0 artifacts** — 63/63 OK, 0 violations. `tests/test_phase16_prereg.py:56-61` globs all four phases, not Phase 16 alone. |
| **STAT-04 dependency freeze** | `git log 23a830c..HEAD -- pyproject.toml requirements.txt` → **empty**, milestone-wide. |
| **18 → 19 entry gate** | `erasure_is_worth_attempting(92, 104, 0, 104)` → `True` with the message **byte-matching** `phase18_extraction_report.md:250` and `docs/REPORT.md:1135`. `_handoff_counts` derives its denominator from the aggregation the sign test consumed, so a draw count cannot substitute for a question count. |
| **19 verdict chain single-sited** | `wilson_upper_bound(0, 27)` == `phase19_floor.TARGET_FLOOR` == `0.09107873950450847`. `erasure_succeeded` defined once, called once (`phase19_erasure.py:1961`), AST-guarded; the unpinned `phase19_run.py` provably abstains. |
| **Binding 270-question fixture** | `results/phase16_recall_sample.json` (112+104+54) read by phases 16, 18, 19 and four test modules; `seed_index` read off the fixture at `:333`, never re-enumerated (the PERS-05 defect class). |
| **Clean-room adversary reused, not rebuilt** | `assert_no_value_in_prompt` reached from 16/17/18/19; `assert_phase18_parity` called before the first draw of every Phase 19 arm. |
| **ISO-06 consumer form** | `LoRAConfig(**artifact["lora_config"])` at all 3 runtime sites. |
| **ATK-06 toggle sentence** | `TOGGLE_IS_AVAILABILITY` byte-identical in `README.md`, `docs/REPORT.md` and the live demo copy. |
| **ISO-07 by absence** | `0.2486` / `0.2000` appear in no Phase 17 driver or report. |
| **STAT-02 across published surfaces** | Bare `0%` count is **0** across all 7 published artifacts. |

### End-to-end flows — 3/3

1. **Teach → recall from an empty prompt** (the v2.0 core claim) survives four phases of instrument
   surgery — *run, not inferred*: `cand_person_quillon` recalled from `'finish this sentence. the
   name you go by is'` with `assert_no_value_in_prompt` passing first, so the value is **proved**
   absent from the prompt.
2. **Demo** — `scripts/personalize_demo.py` imports clean (gradio 5.50.0), toggle copy intact.
3. **Pre-registration → run → verdict → published report** unbroken for each of 16/17/18/19.

### Broken — B1

**Phase 19's retroactive scope limit never reaches Phase 18's published artifact.**
Affects **ATK-04, ERASE-01, STAT-06**. CONFIRMED.

`results/phase19_erasure_report.md:154` states that Phase 19 is direct evidence the rank/exposure
instrument "can report undisturbed while generation collapses underneath it," and that **any Phase 18
reading resting on rank or exposure bits alone must be re-read with that in view.**

That limit lives in exactly two files — `results/phase19_erasure_report.md:154` and
`docs/REPORT.md:1192`. It reaches neither Phase 18 artifact:

```
grep -ci "retroactive weight on phase 18" results/phase18_extraction_report.md  → 0
grep -ci "phase 19|retroactive"           results/phase18_preflight_report.md   → 0
```

All four Phase-19 mentions inside `results/phase18_extraction_report.md` (lines 240, 244, 250, 334)
are the **forward** `erasure_is_worth_attempting` handoff, written 2026-08-17 — two days before the
limit existed.

The break point is exact. `docs/REPORT.md:1140-1141` ends the Phase 18 section by sending the reader
*out* of the qualified document — "the exposure table with all three frames and both reductions …
`results/phase18_extraction_report.md`" — and that file's own closing line calls Phase 18's
evidentiary record **"sound and ready to be cited."** Its 73 measured zero-cells are published
un-flagged at `:236`. Those 73 nulls are ATK-04's product and their generation number is 0 by
construction, so "paired with a generation number" carries no discriminating information for them:
they sit squarely inside the limit, not its exemption.

**A reader of Phase 18 alone is misled.** In a milestone whose entire deliverable is trust in a
published measurement, and under this project's own standard — *"this project does not ship the
weaker claim under the stronger word"* — that is a blocker rather than debt.

**The fix already exists, is CI-enforced, and was simply never run for this limit:**
`scripts/_addendum.py::append_addendum` plus
`tests/test_phase18_docs.py::test_extraction_report_addendum_is_additive`. A dated continuation
appends without touching a pinned byte and without disturbing the 63/63 ancestry result — the guard
reads first-*add* commits. **`render_report` must not be used**:
`assert_extraction_report_not_clobbered` would refuse, and forcing past it would destroy
`LEAKAGE_DEMONSTRATED` and every appended section.

---

## Warning — W1: README is stale relative to Phases 18 and 19

Affects **ATK-06, ERASE-01**. CONFIRMED. Not a blocker.

`README.md:213-217` still speaks in the future tense about a run that finished 2026-08-17:

> *"…its caveat is recorded here **before its numbers exist**: **a low extraction rate** may be a
> property of LoRA … the measured result and its interval are appended to docs/REPORT.md … **when
> the run completes**."*

The run completed and measured the **opposite** — 92/104 = 88.5%, published as *"Personalization in
Weights Is Recoverable Under Black-Box Attack."* `grep -cin "phase 19|phase19" README.md` → **0**;
`DO NOT SHIP` → **0**. A README reader is left anticipating a low rate from a pending audit.

Not a blocker: ATK-06 requires only the toggle sentence in README, and that is verbatim-present at
`:204`; the forward pointer to `docs/REPORT.md` resolves. This is a discoverability defect on the
milestone's most-read surface, fixable by the same append-only convention.

---

## Nyquist Coverage

| Phase | VALIDATION.md | `nyquist_compliant` | `wave_0_complete` | Status | Action |
|---|---|---|---|---|---|
| 16 | exists | `true` | `true` | **COMPLIANT** | — |
| 17 | exists | `true` | `false` | **PARTIAL** | `/gsd:validate-phase 17` |
| 18 | exists (`draft`) | `false` | `false` | **PARTIAL** | `/gsd:validate-phase 18` |
| 19 | exists (`draft`) | `false` | `false` | **PARTIAL** | `/gsd:validate-phase 19` |

Discovery only — no validation was auto-run. Note that all three PARTIAL phases nonetheless shipped
green suites and mutation-proved guards; the frontmatter reflects that their VALIDATION.md was never
re-stamped after execution, not that validation was skipped.

---

## Test & Lint Baseline

```
.venv/bin/python -m pytest -q    → 845 passed, 1 skipped, 83 warnings in 199.03s   EXIT 0
   the one skip = tests/test_train_loop.py:81 "fp16 AMP smoke needs a CUDA GPU" (expected on MPS/CPU)
.venv/bin/ruff check .           → All checks passed!            EXIT 0
.venv/bin/ruff format --check .  → 170 files already formatted   EXIT 0
git status --porcelain           → clean
```

`make lint` is red on this dev box — see DEF-17-01 in tech debt. It is a stale PATH `ruff` (0.1.15
pyenv shim vs the project's 0.15.16), not a code defect; CI installs and runs 0.15.x.

---

## Tech Debt — 13 items across 4 phases + cross-cutting

Every item below is prose, documentation or tooling. **None touches a measured number, a gate, or a
requirement's status.**

**Phase 16 (5)** — stale `:1336` line citations (function moved to `:1413`); an unreconcilable
"~39 min" aggregate wall clock; D-28's note rendered as faithful English rather than the verbatim
Portuguese string and not mechanically tied to `16-CONTEXT.md`; the disclosure paragraph not listing
itself among its own exceptions; one test assertion no longer independently load-bearing.

**Phase 17 (2)** — DEF-17-01, `make lint` red from a stale PATH `ruff` (**confirmed still open,
measured this session**); and the 11 SUMMARY files carrying no requirements frontmatter field.

**Phase 18 (2)** — the `## Pre-Registration` paragraph's residual misattribution of `K = 48`,
`ASR_RUNGS`, the attack templates and `null_result_is_admissible()` to `13666c4` (the 2026-08-17
continuation corrected 2 of 6); and a stale `PERSONA_ALLOWLIST` parenthetical in
`phase16_persistence.py:1605`.

**Phase 19 (3)** — 6 artifacts named in PLAN frontmatter under stale names (all exist under the
pin's own convention, so `verify.artifacts` reports 6 false missing); a stale denominator docstring
in `perplexity.py:11-13`; two stray closing tags in `19-13-SUMMARY.md`.

**Cross-cutting (2)** — ERASE-01/02 are bullets not checkboxes, so checkbox-reading tooling
under-counts v3.0 by two; three Phase-16 SUMMARY frontmatters parse a comment as a string value.

---

## What this milestone actually established

The pre-registration discipline held for four consecutive phases and 63 published artifacts, and it
was the mechanism that authored Phase 19 rather than a judgement made after seeing the numbers:
`erasure_is_worth_attempting(92, 104, 0, 104)` returned `True` against a rule committed at `23a830c`
**before Phase 16 ran**, and the same gate returns MOOT on a null attack, an equal base arm and a
narrow margin. Every headline is re-derivable from committed raw records through committed
functions — Phase 16's report and Phase 18's entire 48,511-byte body both re-render **byte-identically**.

And the milestone's own strongest finding is a negative one it published against itself: **selective
erasure is not selective at 331,776 parameters.** (a) cleared exactly on its boundary with zero
headroom while all seven gated non-targets failed, four at total generation loss, and 77.6% of the
dialogue adaptation was destroyed — while the rank/exposure instrument reported all seven ruined
facts at rank 1 and ceiling exposure at every checkpoint. That co-headline is the source of B1: it
carries a retroactive scope limit onto Phase 18's own rank-based readings, and closing B1 is what
makes that limit reachable by the reader Phase 18's own artifact invites.

---

## Dated continuation — 2026-08-19: B1 and W1 closed

*Appended, not edited. The frontmatter `status: gaps_found` and every finding above stand as what
this audit measured at the time it ran. **This note records the remediation, it does not re-verify
the milestone** — flipping the verdict to `passed` requires a fresh `/gsd-audit-milestone` run over
all 29 requirements and all 16 integration links, and has not been done here.*

Quick task **`260819-r1u`** closed both open findings, each as a dated continuation with a
**pure-insertion diff and zero deletions**, and no frozen pin touched:

| Finding | Closure | Commit | Diff |
|---|---|---|---|
| **B1** | Phase 19's retroactive scope limit carried into `results/phase18_extraction_report.md` via `scripts/_addendum.append_addendum` | `7af6006` | **+67 / −0** |
| **W1** | `## v3.0 audit results (recorded 2026-08-19)` appended to `README.md` | `24d49ad` | **+39 / −0** |

**Flip gates, measured before and after:**

| Gate | Before | After |
|---|---|---|
| `grep -ci "retroactive weight on phase 18" results/phase18_extraction_report.md` | `0` | `1` |
| `grep -cin "phase 19" README.md` | `0` | `1` |
| `grep -c "DO NOT SHIP" README.md` | `0` | `2` |
| `README.md:212-218` vs `git show 28b3c6e:README.md` | — | **identical** |
| Ship-decision markers in the Phase 18 report (PENDING / RECORDED) | `0 / 1` | `0 / 1` — unchanged |
| Bare `0%` on both touched files (STAT-02) | `0` | `0` |

**One API correction worth recording**, because it is the class of defect this project keeps
paying for. `18-VERIFICATION.md` prescribed
`append_addendum(EXTRACTION_REPORT_PATH, text, placeholder=EXTRACTION_SHIP_RECORDED_LINE)`. That
signature no longer exists. The live writer is `scripts/_addendum.py:56` —
`append_addendum(path, addendum, *, pending, recorded)`, with **both** halves of the marker pair as
REQUIRED keywords, written as a third copy precisely because the Phase 17/18 twins are frozen by
the STAT-05 ancestry guards and could not be fixed in place. Because
`EXTRACTION_SHIP_PENDING_LINE` was already consumed (0 occurrences) the only legal call is the
**identity pair** `pending=recorded=EXTRACTION_SHIP_RECORDED_LINE`, which makes the replacement a
provable no-op and the whole write a pure append.

**Not fixed, recorded instead:** the `77.6…%` dialogue-destruction figure exists in two forms.
Every **published** surface (`README.md`, `docs/REPORT.md`, `results/phase18_extraction_report.md`,
`results/phase19_erasure_report.md`, `ROADMAP.md`) carries `77.6370113463966%`, which is what
`(1.2420966625043919 − 0.2777699357026435) / 1.2420966625043919` actually returns. A 17-digit
variant `77.63701134639661%` — one spurious trailing digit — appears only in **planning**
artifacts (`19-12-SUMMARY.md`, `STATE.md`, and this quick task's own PLAN). The published record is
correct and self-consistent; the discrepancy is confined to planning docs and originates in
`19-12-SUMMARY.md`, a closed phase's summary. Logged as tech debt rather than edited.

Full suite after both commits, re-run independently by the orchestrator: **845 passed, 1 skipped**,
`ruff check` clean, `170 files already formatted`.

**Still open from this audit:** the 13 tech-debt items above, the three PARTIAL Nyquist phases, and
Phase 19's human-verification item 1 (the five checkpoint decisions are an operator attestation no
tool can discharge).

---

## Re-audit — 2026-08-19 at `ff8f4d4`: `gaps_found` → `tech_debt`

*This is a full re-run of `/gsd-audit-milestone`, not a note. The frontmatter verdict IS updated,
and `status_history` records both states.*

**No audit input changed except the two remediated files.** `git log 28b3c6e..HEAD` over
`.planning/phases/*VERIFICATION.md`, `.planning/REQUIREMENTS.md`, `.planning/phases/*VALIDATION.md`
and `.planning/ROADMAP.md` is **empty** — so requirements (29/29, 0 orphans), phase verification
(4/4) and Nyquist (1 COMPLIANT / 3 PARTIAL) are unchanged and carry forward with their original
evidence. The only content changes are `README.md` (+39/−0) and
`results/phase18_extraction_report.md` (+67/−0).

### The blocker is genuinely gone — checked on substance, not on a grep

B1 was re-verified by an independent integration re-check instructed to say so plainly if the
closure was cosmetic. It is not:

| Test | Result |
|---|---|
| Does the limit actually land? | **Yes** — stated as a heading and in bold, mechanism first, then the scoping. Not hedged. |
| Does it reach the 73 zero-cells at `:236`? | **Yes, by name and line number**, quoting item (4) verbatim: *"Those 73 cells sit INSIDE this limit, not in its exemption."* |
| Is the exemption correct rather than over-broad? | **Yes** — ASR ladder, 92/104, adapter-off 0/104, ATK-03, `LEAKAGE_DEMONSTRATED` and the handoff each named as generation-based and **not** retracted. |
| Are its cited numbers true? | **Yes, every one**, cross-checked against `results/phase19_erasure_report.md` — including the correct 16-digit `77.6370113463966%`. |
| Did the append disturb the verdict? | **No** — `## Verdict` block byte-identical, sha256 `c7b5af3bc7b411ba` both sides; markers still PENDING ×0 / RECORDED ×1. |

Regression sweep clean: 0 deletions on both files, `README.md:212-218` byte-identical,
`TOGGLE_IS_AVAILABILITY` verbatim across all three surfaces and still inside README's
`## Claim correction` section, **0 files changed under `scripts/`, `src/`, `tests/`** (so no frozen
pin was touched and the STAT-05 guard is unreddened), ancestry **84 pairs over 63 artifacts, 0
violations, non-vacuous**, STAT-04 freeze intact, no bare `0%`, suite **845 passed / 1 skipped**,
ruff clean.

**Integration: 16/16.**

### Why this is `tech_debt` and not `passed`

`passed` requires *"all requirements met, no critical gaps, **and minimal tech debt**."* The first
two now hold. The third does not, on three counts:

1. **W1's own fix introduced a new defect (W2).** `README.md:253-255` republishes *"four published
   defects"* — the exact phrase `results/phase19_erasure_report.md:578-584` identifies as one that
   *"UNDERCOUNTS the phase … the phase publishes **five**."* Commit `d898c7e` (18:35) exists solely
   to retire that ordinal and label the defects A–E; `24d49ad` (19:49), **74 minutes later**,
   reproduces it with zero mentions of any letter. The erasure report grants itself an in-file
   exemption; README carries none, so *"all four defects … stand exactly as published"* reads as a
   complete enumeration when defect **E** is absent. **This is the same defect class W1 was raised
   for**, on the same surface, introduced by W1's remediation.
2. **B1's now-wired link has two quality limits** (B1-a, B1-b): the limit does not reach the
   deep-link reader `docs/REPORT.md:1141` actually creates, and its exemption is asserted without
   the mechanism that makes it true — a mechanism this audit verified in code but the artifact does
   not state.
3. **16 tech-debt items across four phases, plus three PARTIAL Nyquist phases.** Sixteen is not
   minimal.

None of the three is a blocker. All are documentation-integrity items in a milestone whose entire
deliverable is documentation integrity, which is exactly why they are recorded rather than waived.

**Precedent:** v2.0 closed the same way — `tech_debt` first, then `passed` once the open items were
closed before archive (`milestones/v2.0-MILESTONE-AUDIT.md` `status_history`). This is that same
intermediate state, not a downgrade.

### What would flip this to `passed`

W2 is one dated line in README naming A–E and the four-versus-five distinction, under the same
additive discipline that closed B1. B1-a is best fixed on the `docs/REPORT.md` side, by pointing the
outbound link at the continuation as well as the exposure table. B1-b is one sentence stating the
mechanism (`null_result_is_admissible` reads exposure as a presence check only; the terminal ternary
is generation). The remaining debt is either prose-only or tooling, and is a judgement call to accept
at archive.

---

## Third audit — 2026-08-19 at `4ed15d9`: `tech_debt` holds

**No audit input changed.** `git log 07391cd..HEAD` over `.planning/phases/*VERIFICATION.md`,
`.planning/REQUIREMENTS.md`, `.planning/phases/*VALIDATION.md` and `ROADMAP.md` is **empty**.
Requirements (29/29, 0 orphans), phases (4/4) and Nyquist (1 COMPLIANT / 3 PARTIAL) carry forward.
Content delta: `README.md` +47/−0, `docs/REPORT.md` +47/−0,
`results/phase18_extraction_report.md` +59/−0.

### Two of three closures are clean, and were fact-checked against code

**W2 — CLOSED.** README's A–E table was verified against **both** `phase19_erasure_report.md:568-574`
**and** the code: A `:1562`/`:2948`, B `:3850-3855`, C `:2922`, D `:3811`, E `:3576`, each confirmed
inside its enclosing function. The four-versus-five distinction is confirmed structurally —
`_selected_components` has exactly two call sites (`_cmd_cal_erase` `:3553`, `_cmd_erase` `:3687`) and
never `_cmd_report`, so **E genuinely cannot be one of the four `_cmd_report` failures**.

**B1-b — CLOSED, and the claim is conservative.** `exposure_rank` occurs exactly twice inside
`null_result_is_admissible` (`:1527-1657`) — docstring `:1567` and `:1636` — and `is None` is the only
operator ever applied to it. The terminal ternary at `:1657` is generation, and every path to
INCONCLUSIVE returns above it. `:1604-1611` touches the rank key only as a presence/order check,
reinforcing rather than undercutting the mechanism.

**B1-a — PARTIALLY CLOSED.** The redirect is substantive and its structural claims are exact, but it
does not change what the named reader sees: the pointer is at `:1140`, the redirect 128 lines below
at end of file. Structurally forced — both surfaces are frozen above their continuation lines, so
the discipline leaves nowhere additive to put a pointer the deep-link reader would encounter. The
fix states the tradeoff explicitly. **Debt, not regression.**

### The pattern is now the finding

**Three consecutive remediation rounds have each introduced a new defect while closing an old one.**

| Round | Closed | Introduced |
|---|---|---|
| 1 (`7af6006`, `24d49ad`) | B1, W1 | **W2** — README republished the retired four-defect undercount |
| 2 (`4012a61`, `c90d0c8`, `98d0a60`) | W2, B1-b | **N1** — "the three reductions"; there are two. **N2** — "190 lines above"; matches no anchor |

**N1** is the sharper one: `docs/REPORT.md:1283-1284` miscounts the reductions, and is contradicted
by its own verbatim quotation **eight lines above** at `:1276` ("all three frames and **both**
reductions"), by `phase18:141` ("All six frame x reduction NLLs"), and by `phase18:156` ("the two
reductions"). A miscount published inside an append chain whose subject is miscounts.

**N2's origin is this audit itself.** The "190 lines" figure was first written in **this file's own**
earlier B1-a warning text, and the remediation copied it faithfully into a published document. An
unverified figure in an audit artifact propagated outward — the same defect class this audit exists
to catch, committed by the audit.

**The mechanism is prose volume.** Each round appends ~150 lines of dense correction text to
published documents, and dense correction prose about counts is precisely what generates miscounts.
Closing N1/N2 by writing another explanatory section would re-run the mechanism. The only fix that
does not is one that changes the two figures and adds no new argument.

### Verdict: `tech_debt`, unchanged

Requirements 29/29 with 0 unsatisfied and 0 orphans, so the FAIL gate does not fire. Integration
16/16. No blocker. Suite 845 passed / 1 skipped, ruff clean, verdict block byte-identical
(`b7a444957f42db4c`), `docs/REPORT.md` `:1140`/`:1141`/`:1145` byte-identical, ancestry 64 pairs /
0 violations, 0 files changed under `scripts/`, `src/`, `tests/`.

`passed` requires *minimal* tech debt. With 16 catalogued items, three PARTIAL Nyquist phases, one
partially-closed warning and two fresh miscounts — one of them traceable to this audit — that
condition is not met.
