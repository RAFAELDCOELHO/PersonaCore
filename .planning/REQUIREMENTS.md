# Requirements — Milestone v4.0: Leakage Mitigation and Relearning Validation

**Defined:** 2026-08-20
**Milestone goal:** v3.0 measured that weight-based memory leaks 88.5% under prompt-only attack and
ran no mitigation arm. v4.0 builds training-time mitigation, maps the privacy/utility frontier for
two mechanisms across two corpus capacities, and proves adversarially — by relearning attack — that
what survives cannot be cheaply reverted.

**Research:** `.planning/research/SUMMARY.md` (synthesized from STACK / FEATURES / ARCHITECTURE /
PITFALLS, committed `c673b4c`).

**Standing expectation, recorded before any run:** research puts high prior probability on the DP
arm being a **pre-registered null** — fact-level noise-to-signal is `σ√d/L` = 72σ at L=8 facts, and
ε_fact ≤ 4 needs σ ≥ 15.3, a ratio near 1,100; Secret Sharer Table 3 is the direct precedent. The
milestone is scoped to **publish that null well**, at two capacities, not to avoid it.

---

## v4.0 Requirements

### Pre-registration (GATE)

The gate's entire evidentiary value is ordering. `erasure_gate.py` was committed at `23a830c`
*before Phase 16 ran*; the v4.0 gate must be committed before **any** v4.0 number exists — before the
cost calibration, not merely before the sweep.

- [x] **GATE-01**: A committed decision module returns `PASS` / `FAIL` / `INCONCLUSIVE` for a sweep
      point against **three** conditions — (a) extraction ≤ X, (b) taught-fact recall ≥ Y, (c)
      general capability ≥ C — with every argument keyword-only, no defaults, and every condition
      rendered into a reason string.
- [x] **GATE-02**: Condition (c) is computed from constants **imported** from `erasure_gate.py`
      (`V20_MASKED_DIALOGUE_VAL_PPL` 4.5733, `V20_EWC_RETENTION_PPL` 3.891140,
      `V20_RETENTION_NOISE_FLOOR` 0.068930, `MARGIN_K` 2) plus the measured
      `dialogue_ppl_noise_floor` (0.005214448168350039, `results/phase19_noise_floors.json`), never
      retyped as literals — yielding `dialogue_cap` 4.5837288963367 and `retention_cap` 4.029000.

  > **Amended by D-36 at plan `20-09`, and the amendment is TIGHTER.** The `retention_cap`
  > `4.029000` above is derived from `V20_RETENTION_NOISE_FLOOR = 0.068930`, a **Phase 12
  > FULL-FINE-TUNE** seed pair, and it is NOT what governs v4.0.
  > `scripts/mitigation_gate.py::retention_cap` deliberately does not import that constant — a test
  > asserts its absence from a five-name `from erasure_gate import` list checked by EXACT EQUALITY —
  > and takes the retention floor as a **required keyword argument** with no default instead (D-07).
  > The governing floor is the **adapter-regime** `0.008681618994239138`, measured at plan `20-07`
  > (`results/phase20_retention_floor.json`, two seeds, bit-identity control passed at
  > `abs_delta = 0.0`), so the governing v4.0 `retention_cap` is **`3.9085032379884783`**. The
  > borrowed floor is `7.939763314393305x` larger and its cap `4.029` correspondingly LOOSER, which
  > means this amendment makes condition (c) **harder** to clear — a self-serving amendment moves a
  > threshold the other way. `dialogue_cap` `4.5837288963367` is unchanged, and
  > `scripts/erasure_gate.py:246` still computes `4.029` for **Phase 19** verdicts and is
  > deliberately NOT corrected.
  >
  > **Why the pre-registration text is edited at all, when this repo's convention is that it is
  > not.** The supersession was already recorded in the traceability row below at plan `20-07`, but
  > a `[x]` is machine-readable and the note beside it is not, so an automated audit that counts
  > checkboxes reports GATE-02 satisfied AS WRITTEN. D-36 accepts an in-place edit on four grounds
  > that must hold together: it is DATED (2026-08-21), it is IN PLACE so a `grep` for `4.029000`
  > cannot miss it, it is ADDITIVE — every word of the original requirement above is byte-identical
  > — and it is provably TIGHTER by the arithmetic just given. ROADMAP SC1 already carries this
  > exact shape for this exact number.
  >
  > **The enforcement, so this is a mechanism and not prose.** `_prove_retention_floor` in
  > `scripts/phase20_gate_coverage.py` (plan `20-08`) refuses `0.06893` at the choke point every
  > corrected verdict passes through, and `tests/test_phase20_correction.py` (plan `20-11`) asserts
  > that `retention_cap` called with the measured floor equals the artifact's published `cap`
  > bit-exact.

- [x] **GATE-03**: Y is a **pair** (`Y_taught` and `Y_heldout`), because v2.0 published two recall
      numbers (0.4921 / 0.3483) and gating taught-only rewards memorization over generalization.
- [x] **GATE-04**: Y is locked as a **fraction of the retrained control** (e.g. `≥ 0.7 × control`),
      not derived from v2.0's published numbers — otherwise the retrained control is decorative.
- [x] **GATE-05**: An `INCONCLUSIVE` branch for zero-extraction-without-NLL — extraction at or near
      zero **without** a corroborating teacher-forced NLL is never a pass. Ports
      `zero_results_have_nll` semantics; `INCONCLUSIVE` takes precedence over `FAIL`.
- [x] **GATE-06**: A `FAILURE`-vs-`INCONCLUSIVE` discriminator for a truncated sweep — if the swept
      axis never produced points on both sides of X (or of Y), the curve cannot refute existence and
      the verdict is `INCONCLUSIVE`. This is exactly the failure a mis-set Z produces.
- [x] **GATE-07**: The gate returns **arm identity** — a DP point clearing carries a formal claim, an
      adversarial point clearing does not, and "∃ a point" over the union conflates them.
- [x] **GATE-08**: A clearing point is provisional until **replicated at a second seed**, required by
      the gate rather than reported beside it (Phase 17's worst-pair-at-k=3 pattern).
- [x] **GATE-09**: The `__main__` self-check exercises every branch including the failing ones, and a
      **"mitigation that destroyed the model" fixture is run through it and observed returning
      `FAIL`** — a branch nobody has watched fire is a branch nobody has verified.
- [x] **GATE-10**: The n=8-vs-n=64 capacity comparison rule is committed in the same module, before
      either run: if n=64 recovers recall that n=8 did not at equivalent ε_fact, that is a finding
      about where capacity stops destroying the mitigation; if it does not, the null is confirmed at
      two capacities. Both branches are publishable and neither may be chosen after seeing data.

### Privacy unit, DP data path, and corpus (UNIT)

The longest dependency chain in the milestone, and design work rather than code. "The unit was wrong"
invalidates every ε and no amount of re-running fixes it.

**All six SATISFIED in Phase 21** (verified 2026-08-24, 6/6 must-haves). Evidence, guards and the
findings each one survived are in the Traceability table below. **`21-VERIFICATION.md` returned
`human_needed`, not `passed`** — the goal is achieved and reproducible, but five review warnings
were open at verification time (WR-04 has since closed at `9a407d6`; WR-03, WR-05, WR-06, WR-07 and
IN-01/IN-02/IN-04 remain). Checking these boxes records requirement satisfaction, not phase closure.

- [x] **UNIT-01**: The privacy unit is **the fact**, not the 256-token window — recorded as a decision
      with its arithmetic, because `get_batch_memmap_masked` draws overlapping windows with
      replacement over a flat concatenated bin, so an example-level ε bounds nothing about a fact.
- [x] **UNIT-02**: Fact-aligned batching — each fact's shard padded to whole `block_size` windows so
      `grad_accum_steps = n_facts` makes the existing accumulation loop the per-example loop, with
      **no subsampling** (q=1) and therefore an exact accountant.
- [x] **UNIT-03**: The effective per-fact multiplicity in gradient steps is **measured**, not inferred
      — 22 rendered rows per fact is confirmed (176 rows / 8 `LOCKED_FACTS`), but `build_bins` window
      packing and `replay_ratio` determine what an ε actually rests on, and that is unmeasured.
- [x] **UNIT-04**: A recorded decision on whether PersonaChat replay participates in the DP lot —
      `replay_ratio=1.0` puts public data in the bin, and counting it in N shrinks q and produces a
      flatteringly small ε.
- [x] **UNIT-05**: δ is pinned as the literal **1e-5**. The `1/N^1.1` recipe is self-contradictory at
      a fact unit: at N=8 it yields δ=0.1015 and fails its own `δ·N < 0.01` assertion by ~80×.
- [x] **UNIT-06**: An n=64 corpus built from **unscored filler facts** that touch no ancestry-pinned
      fixture, so the capacity lever can be tested without disturbing any published instrument.

> **UNIT-03's own text is now partly superseded by what Phase 21 measured, and is corrected here
> rather than left standing.** "22 rendered rows per fact is confirmed (176 rows / 8 `LOCKED_FACTS`)"
> holds and reproduces at n=8. What the requirement could not know is that **the conservation law
> pins the per-fact MEAN at `total_draws / n_facts` = 200.0 by arithmetic**, so the measurement it
> asks for cannot be reported as a mean without restating its own input. The measured content is the
> spread: min **143** / max **229** / spread **86** at n=8 facts-only, and exactly 1/1/spread 0 on
> the fact-aligned rows — which is UNIT-02's q=1 claim appearing as data. See the UNIT-03 note below.

### DP-SGD core and accountant (DPSGD)

- [x] **DPSGD-01**: From-scratch per-example gradient clipping + Gaussian noise on the LoRA gradients
      only, base frozen, entering `train()` through a **new additive gradient-side seam** — the
      existing `penalty_fn` is a loss seam and cannot carry it.
- [x] **DPSGD-02**: The default path is proven **bit-identical when the seam is off**, against the
      Phase-10 golden-trajectory fixture — the `penalty_fn` playbook verbatim.
- [x] **DPSGD-03**: A from-scratch (ε, δ) accountant in stdlib `math`, exact under q=1 composition,
      validated against independent oracles (closed-form Gaussian, numerical quadrature).
- [x] **DPSGD-04**: A correctness battery that catches the known silent-non-privacy failures: clipping
      the averaged gradient instead of per-example (the cheapest possible fake DP-SGD is a two-line
      diff that converges fine), noise scaled to the wrong sensitivity, noise added after averaging,
      RNG reuse across steps.
      *Anchor corrected 2026-08-25 (plan 22-06). This line originally cited `loop.py:165` as the
      site that "already clips exactly the LoRA grads on the averaged gradient" — the same stale
      anchor ROADMAP SC4 carries. Measured against the pre-22-06 tree, `:165` was
      `accum = max(1, train_cfg.grad_accum_steps)` and the clip was `:181`. As of plan 22-06 the
      clip is `training/loop.py::_optimizer_step`'s single reachable
      `torch.nn.utils.clip_grad_norm_` call, inside an `if dp_fn is None:` branch — so it is
      structurally unreachable on the DP path and the sentence is cited by SYMBOL from here on.*
- [x] **DPSGD-05**: `checkpoint.py` gains an **MPS RNG slot**. Latent today because the loop consumes
      zero device RNG; DP noise is the first per-step consumer. Five lines now, a full sweep re-run
      later. Must land before any long run.
- [ ] **DPSGD-06**: The σ=0 point is the DP arm's **first executed run** — the only cheap diagnostic
      separating "DP is hard at this scale" from "the code is wrong", since every correctness bug in
      this class *improves* utility.
- [x] **DPSGD-07**: `LoRALinear` is **not** restructured into `nn.Linear` submodules — `lora_A`/
      `lora_B` are bare `nn.Parameter`s in an inline matmul (`lora/layer.py:41`), and restructuring
      would rename state-dict keys and invalidate `persona_adapter.pt` and every v3.0 checkpoint.

### Cost calibration and budget pre-registration (CAL)

**The evaluation leg is measured, and it decides the sweep.** Research reported evaluation
wall-clock as unmeasured because STACK's generation probe failed. The number already existed in the
repo: Phase 18's own D-12 pre-flight (`results/phase18_preflight_report.md`, committed with git_sha,
pid, seed and corpus sha256) measured per-shape throughput on `convbase_slim.pt` at MPS/torch 2.7.1
— A1-mild 145.01, A1-aggressive 134.54, A2 183.20, A3 140.85 draws/min. Replicating that report's
own projection from those rates reproduces its stated **84,960 draws / 9.54 h across both arms**
exactly, which validates the cost model against a committed artifact rather than against an estimate.

Therefore, per sweep point at full Phase-18 fidelity: **42,480 draws = 4.77 h**. Training is ~17 s
per arm. **Evaluation costs ~1,010× training** — it is the binding constraint by three orders of
magnitude, and no sweep density may be chosen without it.

| per-point K | draws/point | h/point | 16-point sweep | ASR rungs |
|---|---|---|---|---|
| 48 (Phase 18 fidelity) | 42,480 | 4.77 | 76.3 h = 3.2 days | (1, 4, 16, 48) |
| 24 | 21,744 | 2.45 | 39.2 h | (1, 4, 16, 24) |
| 16 | 14,832 | 1.67 | 26.8 h | (1, 4, 16, 16) |
| 8 | 7,920 | 0.90 | 14.4 h | (1, 4, 8) — loses rung 16 |

- [ ] **CAL-01**: The training leg is measured to complete the pair (~17 s per arm from research, to
      be confirmed on the DP path with the seam active). The evaluation leg is **already measured**
      above and is authoritative for sweep sizing.
- [ ] **CAL-02**: Z (sweep width, per-point draw budget K, step budget) is set **from** those
      measurements and committed in a module separate from the gate, with the separation structurally
      enforced so a reader cannot mistake a resource calibration for an outcome-threshold peek.
- [x] **CAL-04**: **Per-point K and the promotion rule are committed before any v4.0 artifact
      exists.** `phase18_extraction.py:88-92` records that reducing K *after* seeing a null is the
      weakening `ATK-03` and `P18-4` exist to prevent, and that pre-flight is "the one moment the pin
      leaves open for it." v4.0 is at that same moment. Pre-register: the curve K, the full-fidelity
      K reserved for gate-candidate points, and the rule that promotes a point from the first to the
      second — all three before the first point is drawn.
- [ ] **CAL-05**: The rate above was measured on the **un-adapted base**, where 45–56 of 64 draws
      per shape terminated on a stop id. A heavily-noised DP adapter that stops emitting EOS runs the
      full `max_new_tokens=48` on every draw — the slowest case — so 4.77 h/point is a **floor for
      noised points, not a mean**. The calibration must re-measure throughput on one noised adapter
      before the sweep width is finalised.
- [ ] **CAL-03**: The inference "**ε is independent of N at q=1**" — the premise the entire n=64 phase
      rests on — is confirmed by a small calibration run at n_facts=8 vs 64 at fixed σ **before** the
      expensive n=64 run is committed. Research marks it `[INFERENCE]`, not measured.

### Adversarial extraction-aware training (ADVT)

- [ ] **ADVT-01**: The adapter trained against the Phase 18 attack suite with **attack intensity as
      the sweep axis**, implemented as a `build_bins` mixture ratio — no new training seam required.
- [ ] **ADVT-02**: A pre-registered **leave-one-attack-family-out** split, with the held-out family
      named **before** training. The four families already exist separably in
      `phase18_extraction.py`; choosing the held-out family after seeing which the defense handles
      worst is the peek this project's discipline forbids.
- [ ] **ADVT-03**: Attack intensity is disclosed as **also a token-budget axis** — measured through
      the frozen 547-live-id tokenizer, the same 51-character sentence is 35 tokens clean, 49
      uppercased (1.40×), 1.17× role-play framed.

### Control arms (CTRL)

Both anchors precede every sweep point: a point at extraction 0.30 means nothing until the retrained
control's position and the never-taught floor are known.

- [ ] **CTRL-01**: A **retrained unmitigated control** at identical budget and seed protocol — v2.0's
      published 0.4921 / 0.3483 belong to a different run and cannot serve as this milestone's
      baseline without confounding the comparison with run-to-run variance.
- [ ] **CTRL-02**: The control is realised as a **sweep point** (`clip_norm=inf, noise_multiplier=0`)
      so it differs from every DP point by exactly the two DP parameters. Recorded explicitly: it is
      *not* bit-identical to the seam-off path (floating-point non-associativity), and chasing that
      identity would be a mistake.
- [ ] **CTRL-03**: A **never-taught fresh adapter** at identical budget and seed, serving double duty
      as frontier floor and relearning reference. Depends on nothing; scheduled early.

### Frontier sweep and verdict (FRONT)

- [ ] **FRONT-01**: A privacy/utility curve for both arms at **both capacities** (n=8 and n=64) — ε
      for DP-SGD, intensity for adversarial — swept to the never-taught floor and to σ→0 so the curve
      reconnects to the control at both ends.
- [ ] **FRONT-02**: Dual ε reporting — example-level **and** fact-level — so an example-level ε can
      never be read as if it bounded fact leakage.
- [ ] **FRONT-03**: A committed frontier JSON artifact carrying counts (not rates), ordered point
      keys, `accounting: null` on the adversarial arm as a structural statement that it makes no
      formal claim, and the gate/budget module sha256s. Figures are drawn **only** from this artifact.
- [ ] **FRONT-04**: The verdict is computed by **importing** the GATE module's constants, never by
      retyping a threshold in prose.

### Relearning attack (RELRN)

- [ ] **RELRN-01**: Absolute recovery ceiling as the **binary pre-registered gate** — recovered recall
      ≤ X within a fixed budget Z.
- [ ] **RELRN-02**: A **cost-to-recovery curve** (steps/examples to restore leakage) measured against
      the never-taught fresh adapter at identical budget and seed. Mitigated ≈ fresh means the
      information was removed rather than suppressed.
- [ ] **RELRN-03**: The cost curve **qualifies** the PASS/FAIL verdict as a recorded finding; it is
      not a second gate. Same "an instrument qualifies a gate's reading, it does not replace it"
      pattern v3.0 established.
- [ ] **RELRN-04**: "Identical budget and seed" is enforced **structurally** — one shared
      `TrainConfig` object, an evidence diff read back off disk, and a data-order sha256 proof — not
      by convention.
- [ ] **RELRN-05**: Recovery is measured on a fixture **disjoint** from anything the mitigation was
      trained against, and the attacker corpus is pre-registered, because the corpus definition *is*
      the threat model.

### Empirical privacy audit (CANARY)

The only mechanism in the whole research pass that tests the **guarantee** rather than the **code**.

- [ ] **CANARY-01**: One-run canary auditing produces an **empirical lower bound on ε**, built
      additively on the Phase 18 fixture, scorer, Wilson bound and 42,480-draw precedent.
- [ ] **CANARY-02**: A rule committed before the audit runs: **if the measured ε_lower exceeds the
      ε_upper claimed by the formal accounting, the implementation is declared provably broken** —
      stated with no room for a favourable reading afterward.

### Report and close (RPT)

- [ ] **RPT-01**: The milestone report publishes whichever way the numbers came out, including the
      expected DP null at both capacities, with the standing expectation above quoted as having been
      recorded before any run.
- [x] **RPT-02**: A whitespace-normalizing prose-search helper exists and is used for correction
      sweeps — v3.0's `grep -c` lesson (a line-wrapped phrase reported as absent on a file containing
      it) was recorded and never converted into a mechanism.
- [ ] **RPT-03**: Zero new runtime dependencies; `pyproject.toml` sha256-pinned state carries forward
      untouched, making it four milestones.

---

## Future Requirements

Deferred to a later milestone, recorded rather than dropped.

- Erasure at higher adapter rank or via a non-ablation mechanism (v3.0 candidate 2). Phase 19's
  `FAILURE` was bounded to one mechanism, one fact, one adapter at 331,776 parameters, and the
  rank-instrument co-headline says the rank reading alone cannot be trusted to report selectivity.
- The frozen tokenizer / retrain question (v3.0 candidate 3). Held out of v3.0 for the same reason it
  is held out here: it invalidates every published checkpoint and number, and needs its own
  conversation.
- The 16 tech-debt items, 3 `PARTIAL` VALIDATION.md files and 6 stale status stamps carried from
  v3.0. All are prose, documentation or tooling; none touches a measured number, a gate, or a
  requirement's status.
- **New debt found while measuring the evaluation leg (2026-08-20):** the comment block at
  `scripts/phase18_extraction.py:85-87` cites throughput rates of 146.98 / 127.70 / 178.74 / 130.31
  draws/min (blended 143.04, projecting 9.91 h) and attributes them to
  `results/phase18_preflight_report.md` — but that committed artifact reports 145.01 / 134.54 /
  183.20 / 140.85 and a total of **9.54 h**. A committed code comment carries four numbers its own
  cited evidence does not contain. **No published result changes** — the K=48 decision holds under
  either set, and both sit under the 13.12 h that K=64 was rejected at. It is recorded here rather
  than fixed because `phase18_extraction.py` is ancestry-guarded and the v3.0 lesson is that
  correction prose is denser in exactly the facts that are easy to get wrong. v4.0 uses the
  **artifact's** numbers.

## Out of Scope

- **HuggingFace PEFT / transformers model code, and `opacus`** — excluded by design. `opacus` is
  rejected even as a test oracle: it would drag `scipy` + `opt-einsum` into CPU-only CI for a 30-line
  function, and as an oracle it shares the same closed form and therefore the same failure modes.
- **Ghost clipping** — rejected on arithmetic by two independent derivations. At r=8, T=256 the
  crossover is T < 7.8 tokens, so it costs ~30–33× *more* than direct materialization here.
- **A Poisson loader on the critical path** — Poisson over 8 facts is degenerate (10% chance of an
  empty lot per step at q=0.25), and fact-alignment reaches the same ε_fact within ~10% at a δ two
  orders of magnitude smaller.
- **Restructuring `LoRALinear` to `nn.Linear` submodules** — renames state-dict keys and invalidates
  `persona_adapter.pt` and every v3.0 checkpoint.
- Databases, vector stores, RAG, external memory files; scaling beyond ~10–15M params; multi-GPU.

## Traceability

Every REQ-ID maps to exactly one phase. **48/48 mapped, 0 orphans, 0 duplicates**
(verified 2026-08-20 at roadmap creation).

| REQ-ID | Phase | Note |
|--------|-------|------|
| GATE-01 | Phase 20 | `mitigation_point_verdict` returns the three-name `V4_VERDICTS` domain, proved at import by the module-scope `_prove_verdict_domain()`, with four condition reason strings observed rendering. Guarded by `test_verdict_domain_stays_exactly_three` and by `test_every_gate_function_is_keyword_only_with_no_defaults`, whose AST walk covers ALL public functions in the module rather than the verdict one alone. |
| GATE-02 | Phase 20 | **The MECHANISM is discharged; the stated `retention_cap 4.029000` is SUPERSEDED by D-06.** `scripts/mitigation_gate.py::retention_cap` imports `V20_EWC_RETENTION_PPL` and `MARGIN_K` and retypes nothing, but it takes the retention floor as a REQUIRED keyword argument (D-07) and does NOT import `V20_RETENTION_NOISE_FLOOR = 0.068930` — that is a Phase 12 FULL-FINE-TUNE reading. The adapter-regime floor `0.008681618994239138` measured at plan `20-07` (`results/phase20_retention_floor.json`) is what governs v4.0, yielding `3.9085032379884783`. The requirement's `4.029000` is published beside it in the artifact as `borrowed_cap` so the supersession is visible rather than silent. `scripts/erasure_gate.py:246` still computes `4.029` for Phase 19 verdicts and is NOT corrected. **RESIDUAL — CLOSED at plan `20-12` (2026-08-21), against a re-run of the guard rather than against a SUMMARY.** The residual was that the supersession was recorded but not *enforced*: `extraction_ceiling` carried THREE `_prove` calls refusing a wrong-arm / <2-seed / missing-provenance floor while `retention_cap` carried ZERO, so — measured — `retention_cap(retention_noise_floor=0.068930)` returned `4.029`, the LOOSER cap, with no refusal at all. That was asymmetric against T-20-24, whose whole point is that `mitigation_point_verdict` calls `extraction_ceiling` itself so no path to a verdict skips the provenance check. The retention leg now has its equivalent choke point. `scripts/phase20_gate_coverage.py::_prove_retention_floor` (plan `20-08`) supplies the four refusals the FROZEN `retention_cap` cannot be given: three mirroring `extraction_ceiling`'s at `scripts/mitigation_gate.py:417` / `:425` / `:436` — missing provenance keys, wrong `regime`, fewer than two distinct seeds — plus a fourth refusing `V20_RETENTION_NOISE_FLOOR` BY IDENTITY (imported, never retyped) — a refusal whose coverage is exactly ONE bit pattern. **The clause that stood here — that a caller who lies about `regime` is therefore caught BY THE NUMBER — was MEASURED FALSE, and is corrected IN PLACE rather than left readable beside its correction: the additive dated register (D-36) exists so a superseded number stays visible, not so a falsified claim stays standing.** The measurement is `20-VERIFICATION.md:115` (issue at `:69`): `V20_RETENTION_NOISE_FLOOR * (1 + 2**-50)` passes the `!=` and `retention_cap` returns a BIT-IDENTICAL `4.029` — the exact borrowed cap — with the unperturbed value refused as the control. What that refusal proves is a NAME/PROPERTY pair: the named value by IDENTITY, and nothing else. It is a CHOKE POINT, not an advisory: it is called FIRST in `corrected_point_verdict`, before any compute, so no path through the sanctioned route reaches a verdict skipping it — the same structural discipline as the extraction leg, not merely documentation. Guarded by `tests/test_phase20_correction.py::test_the_retention_floor_tripwire_is_the_only_route_to_a_verdict` (plan `20-11`), which drives all TEN refusal cases THROUGH the verdict route with a positive control — RE-COUNTED at plan `20-16` by instrumenting `corrected_point_verdict` and counting the `SystemExit`s that actually escaped, rather than by trusting this sentence: **10**, from eight static `refused(...)` call sites two of which are loops. The `eight` this row carried was correct at `20-12` and predates `20-15`'s two magnitude cases; leaving a stale count inside the very edit that removes a falsified claim would be the same defect twice, and which was WATCHED going red: one `_prove` deleted from `_prove_retention_floor` produced `Failed: DID NOT RAISE <class 'SystemExit'>`, and the module was restored byte-identically. **The requirement's own text now carries this supersession too** — see the dated D-36 amendment beneath the `- [x] **GATE-02**` bullet, added at plan `20-09` (2026-08-21), so a reader who greps `4.029000` lands on the correction instead of on the superseded number alone. **AMENDED 2026-08-21 at plan `20-16` (D-38).** A FIFTH refusal now stands beside the fourth, and the pair is what the row claims rather than either half: the MAGNITUDE bound `retention_noise_floor <= _MAX_ADMISSIBLE_RETENTION_FLOOR` (`0.008681619002920757`, computed as the governing floor times the separately named `_RETENTION_FLOOR_RELATIVE_TOLERANCE = 1e-09`, never typed), placed AFTER the `!=` so the by-identity message — which publishes the ratio, the looser cap it buys and the governing cap it displaces — still fires first. **WHAT THE PAIR PROVES:** the NAMED value by IDENTITY, and the ENTIRE looser class by PROPERTY. Measured by which message returns, not by reading the source: `V20_RETENTION_NOISE_FLOOR * (1 + 2**-50)` → MAGNITUDE; `5.0` under clean adapter provenance over two distinct seeds → MAGNITUDE; `V20_RETENTION_NOISE_FLOOR` itself → IDENTITY, still first; the governing measured floor → ADMITTED, so the bound is not vacuous. It admits any TIGHTER floor a later phase measures. Guarded by `tests/test_phase20_correction.py::test_the_retention_floor_tripwire_is_the_only_route_to_a_verdict`, with both new cases WATCHED red-then-green (`20-15-SUMMARY.md`'s Watched-RED table, breaks 1 and 2b) rather than merely written. **D-41's consequence, recorded because it is against interest:** the bound refuses this repository's own committed fixtures, which carry `retention_noise_floor = 0.009` — annotated `# fabricated` in the frozen pin, `1.0366729991228745x` the governing floor. Widening the tolerance until that value passes was REJECTED; the sanctioned route's test harness supplies the governing floor READ from `results/phase20_retention_floor.json` instead, and every published verdict is bit-unchanged under the substitution while the one reason string that moves reports the TIGHTER cap. |
| GATE-03 | Phase 20 | Y is the pair `(y_taught, y_heldout)` computed inside `mitigation_point_verdict` at `:765-766`, each leg from `F_Y` times its own control, and both legs are read by condition (b). Guarded by `test_every_verdict_branch_fires`. |
| GATE-04 | Phase 20 | Each leg is `F_Y` × its OWN retrained control, never v2.0's published numbers: `0.4921` and `0.3483` are asserted absent as numeric constants by AST, and the `from erasure_gate import` list is asserted by EXACT EQUALITY to five names so `V20_TAUGHT_RECALL` / `V20_HELDOUT_RECALL` cannot reach `mitigation_point_verdict`. Guarded by `test_no_imported_baseline_is_retyped`. |
| GATE-05 | Phase 20 | The zero-extraction-without-NLL early return at `:730-745` fires BEFORE any reason is appended, so `mitigation_point_verdict` hands back a one-element reason list and INCONCLUSIVE precedes FAIL. Watched firing differentially against the FAIL it overrides by `test_every_verdict_branch_fires`. |
| GATE-06 | Phase 20 | **DISCHARGED at plan `20-12` (2026-08-21) — against a re-run of the guards in the discharging process, never against a SUMMARY.** THE MECHANISM SHIPPED AT PLAN `20-04` AND WAS DEFECTIVE ON BOTH AXES. **CR-01:** the discriminator at `scripts/mitigation_gate.py:798-812` decided sweep coverage on RAW rates while condition (a) at `:755` decides the same axis on `wilson_upper_bound(k, n)` against the same `ceiling` — two statistics, one ceiling. **WR-09:** there was no `sweep_heldout_recalls` parameter anywhere in the 21-kwarg signature, so the held-out leg SC2 makes load-bearing had no coverage check at all and no caller convention could supply one. BOTH DIRECTIONS WERE REPRODUCED, with their fixtures named, at n=104, X=`0.04535522866494124`: `FIXTURE_CLEARING_POINT` + `(1/104, 3/104)` returned `INCONCLUSIVE` where the sweep genuinely brackets X under the (a) rule (`wilson_upper_bound(1,104)=0.041950` clears, `wilson_upper_bound(3,104)=0.069999` fails), demoting a would-be `PASS`; `FIXTURE_DESTROYED_MODEL` + `(3/104, 11/104)` returned `FAIL` with NO GATE-06 reason on an axis where ZERO points clear X — "it did not work" published where the honest finding is "we could not tell". A THIRD CASE, in no prior report, is the sharpest: `FIXTURE_CLEARING_POINT` + `(3/104, 11/104)` returned `PASS` off that same truncated axis. **`scripts/mitigation_gate.py` was NOT edited** — it is byte-identical to its nine pinned commits, `git diff --exit-code` on it and on `scripts/erasure_gate.py` returns 0, and the ancestry guard is green, so the pre-registration survives its own correction. **THE DISCHARGE:** `scripts/phase20_gate_coverage.py::coverage_verdict` (plan `20-08`, UNPINNED) decides each axis on the statistic that axis's criterion is decided on — `wilson_upper_bound(k, n)` for extraction, raw recall for BOTH Y legs — covering the extraction axis and closing WR-09 in the same function (D-35). `corrected_point_verdict` is the sanctioned route to a v4.0 verdict and has NO `sweep_extraction_rates` parameter, so raw-rate space is unreachable through it by construction, and a raw rate handed to the count parameter is refused BY NAME (`SystemExit` naming both RATE and COUNT). **THE RECORD:** `results/phase20_gate_coverage_correction.json` — `governs` = sweep coverage (GATE-06), naming `coverage_verdict` as the computation and `corrected_point_verdict` as the route; `supersedes` = `scripts/mitigation_gate.py:798-812` — plus the D-24 dated continuation `results/phase20_gate_coverage_correction.md` (dated `2026-08-21`, the day it was written). **THE GUARD:** `tests/test_phase20_correction.py` (plan `20-11`), which asserts both directions RED against the frozen pin and GREEN through the correction in one differential body each, plus the AST caller census that reddens on the first Phase 23/25 caller bypassing the route. **BOUND DIRECTION (D-37), with its cost:** Wilson upper on the X ceiling, raw rates on the Y floors — because a Wilson LOWER bound on a floor would decide coverage on a statistic condition (b) does not read, re-introducing CR-01's defect class with the sign flipped. The Y legs therefore inherit condition (b)'s own lack of a confidence bound; that is recorded and deliberately NOT fixed, because fixing it would move a pre-registered threshold after seeing the data it governs. **AMENDED 2026-08-21 at plan `20-16` (D-40).** The discharge's Y half was STRUCTURAL when this row was written — both legs decided in one body by one rule — and not yet BEHAVIOURAL: the two legs were validated for LENGTH only and their values consumed RAW, while the extraction axis decided in the same function carried three per-element `_prove` calls. A coverage finding on either leg could therefore be produced by the INPUT rather than by the data. **MEASURED at `576b57d`** on `FIXTURE_CLEARING_POINT` at the `(1, 3)` / `(104, 104)` sweep: held-out `(0.30, 0.28)` returned `INCONCLUSIVE` with one GATE-06 reason and truncated axes `('heldout_recall',)`, while the STRICTLY MORE truncated `(nan, 0.28)` returned `PASS` with NO GATE-06 reason and `coverage_verdict (True, (), None)` — fully covered, zero truncated axes. The mechanism is one comparison: `nan >= 0.24499999999999997` is `False`, so the NaN was COUNTED as a failing point and MANUFACTURED the bracket the honest sweep lacks. CLOSED at plan `20-14` by a per-element `[0.0, 1.0]` `_prove` on BOTH legs, placed before the `x_uppers` comprehension so nothing reaches `wilson_upper_bound` unvalidated, together with the RATE-vs-COUNT guard enforced BY TYPE (`isinstance(k, int) and not isinstance(k, bool)`) — the old integral-float acceptance admitted the module's own rate-space `SUPERSEDED_SWEEP_SENTINEL (0.0, 1.0)` as counts, reading it as `[0, 1]` successes out of `[104, 104]` questions for a spurious `INCONCLUSIVE`. Guarded by `tests/test_phase20_correction.py::test_a_recall_outside_the_unit_interval_cannot_manufacture_y_coverage` and `tests/test_phase20_correction.py::test_the_modules_own_rate_space_sentinel_cannot_pass_as_counts`, both WATCHED red-then-green (`20-14-SUMMARY.md`'s Watched-RED table). The same record is published in the second dated continuation at `results/phase20_gate_coverage_correction.md` and in the `value_guards` block of `results/phase20_gate_coverage_correction.json`. |
| GATE-07 | Phase 20 | The returned 3-tuple carries `arm`, `exists_clearing_point` aborts on a mixed-arm point list so the ∃ cannot be formed over the union, and `ARM_CLAIMS` is proved equal to `ARMS` at import. Guarded by `test_every_verdict_branch_fires`. |
| GATE-08 | Phase 20 | A point clearing (a), (b) and (c) without second-seed replication returns INCONCLUSIVE over a would-be PASS at `:822-829`; `REPLICATION_PENDING_MARKER` is ONE constant read by both that branch and `promote_to_full_fidelity`, and no `provisional` identifier, string or comment exists anywhere (AST plus a `_prose.normalized` scan). Guarded by `test_promotion_rule_and_ratchet`. |
| GATE-09 | Phase 20 | Six outcomes observed firing in the `__main__` self-check, and `FIXTURE_DESTROYED_MODEL` is built from Phase 19's real published M1 readings with four fields asserted against the parsed artifact by `test_destroyed_model_fixture_matches_the_published_phase19_readings`; `test_gate_self_check_runs_clean_in_a_fresh_interpreter` re-runs the whole self-check as a subprocess twin in CI. |
| GATE-10 | Phase 20 | `_CAPACITY_DISPATCH` is proved TOTAL over all four `(small_cleared, large_cleared)` combinations by a module-scope `_prove`, `CAPACITY_BRANCHES` is closed at five, both named branches were observed firing, and the D-26 fallback raises with the tolerance unset in a message naming D-26. Guarded by `test_capacity_rule_commits_both_branches_and_refuses_the_unset_fallback`. |
| CAL-04 | Phase 20 | phase-zero: K + promotion rule pre-committed |
| RPT-02 | Phase 20 | **DEFERRED to Phase 25 — first half shipped, second half is the unmet conjunction.** The requirement is a conjunction: the helper must *exist* AND *be used for correction sweeps*. `scripts/_prose.py::normalized` exists and is committed at plan `20-03` (`ac4d781`), closing the first half and converting v3.0's `grep -c` lesson into a mechanism. The second half is NOT discharged: routing doc-consistency checks through `normalized` belongs to the phase that runs the first correction sweep. Recorded here rather than only in `20-03-SUMMARY.md`. NOTE — this phase produced FOUR independent instances of the exact defect class RPT-02 exists to close, each caught and worked around rather than papered over: 20-03 (`grep -c "shell=True"` matching 20-01's docstring saying shell=True is never used), 20-04 (`'V20_RETENTION_NOISE_FLOOR' in src` matching the docstring explaining why that name is never imported), 20-05 (a `__doc__` case-sensitivity mismatch), and 20-06 (`0.4921`/`0.3483`/`0.005214448168350039` present as source substrings inside comments stating those values must never be used). Every audit in `tests/test_phase20_prereg.py` is consequently an AST walk or goes through `normalized` — no `grep -c` or `in src` audit was committed. |
| UNIT-01 | Phase 21 | **SATISFIED.** `PRIVACY_UNIT = "one taught fact"` and `privacy_n_rule = "N = n_facts"` are pre-registered in `scripts/mitigation_unit.py` (added `8d3beb4`, plan 21-01), FROZEN at `sha256 45f37e152bb4035667b804c1463431b3f12fa5096c47de32b1dc27abbe000473` and verified byte-identical again at closure. `PRIVACY_UNIT_ARITHMETIC` is the requirement's "recorded as a decision **with its arithmetic**" half and it names the mechanism rather than gesturing at it: `src/personacore/training/data.py:117`, `np.random.randint`, "with replacement", "flat concatenated bin" — then writes the formula out with BOTH denominators, `1600 * (947.625 + 256) / (7581 - 256 - 1) = 262.9437465865647`, alongside the reading it is NOT (`1600*256/7581 = 54.03`). **The pin carries FIVE module-scope `_prove` guards** (`scripts/mitigation_unit.py:214`, `:220`, `:231`, `:238`, `:246`; the sixth `_prove(` in the file is the `def` at `:70`) — **four are load-bearing and one is decoration**: `_prove(SAMPLING_RATE_Q == 1.0)` compares a module literal to itself, so no edit anywhere in the repo can redden it (IN-01, OPEN; frozen, and not worth a continuation). Guarded by `tests/test_phase21_unit_pin.py::test_privacy_unit_is_one_taught_fact`, `::test_privacy_n_is_the_fact_count_and_q_is_one` (n=8 AND n=64), and `::test_pin_imports_nothing`, an AST walk enforcing the D-22 zero-import ceiling. Republished at `results/phase21_privacy_unit.json → unit` (`sha256 84d8f3bd…`), and `tests/test_phase21_unit_record.py::test_artifact_values_come_from_the_pin` asserts equality **against the imported module**, so the artifact retypes nothing. **THE 262.9437 vs 207.018 QUESTION, WHICH IS THE MOST LIKELY PLACE FOR THIS ROW TO BE WEAKER THAN IT READS, IS RESOLVED IN THE ARTIFACT AND NOT IN PROSE:** every row publishes `analytic_expectation.overlap_rule = 262.9437465865647` (the REJECTED rule — the one the pin's own arithmetic computes) beside `first_token_rule = 207.0180229382851` (the rule the rows are actually counted under), plus `rule_this_row_was_counted_under`, `which_one_matches_this_row`, `gap_between_the_two_rules = 55.92572364827963`, and a top-level `pin_discrepancy` block giving the EXACT reconciliation `262.9437465865647 - 1600 * 256 / 7324 = 207.0180229382851`. Neither figure is wrong; they answer different questions. **WR-07 REMAINS OPEN against this row:** the frozen pin still tells the reader to expect that difference "by sampling noise" when the gap is systematic — frozen, so the correction is owed to a continuation, not an edit. **IN-02 REMAINS OPEN:** the pin's anchors cite `teach_persona.py:523`/`:517` for `MAX_STEPS`/`BATCH_SIZE`; measured now they are `:955`/`:949`. **WR-04 IS CLOSED, and it was the wrong-unit defect class one level up, sitting in the module Phase 22's accountant imports for N.** The pin's `privacy_n` was unvalidated — MEASURED: `privacy_n(7.9) -> 7` (silently drops a privacy record), `privacy_n('8') -> 8`, `privacy_n(0) -> 0` (which makes `δ·N == 0` pass the ceiling trivially), `privacy_n(-3) -> -3` — and it had **two present-tense call sites** reaching it aliased as `mu.privacy_n`, so the blast radius was today's tree, not Phase 22's. Closed at `9a407d6` by `scripts/phase21_unit_continuation.py`, a **dated continuation rather than an edit**, because the pin's immutability is the thing being protected. Guarded by `tests/test_phase21_unit_continuation.py::test_the_continuation_refuses_what_the_pin_admits` (float / 0 / negative), `::test_bool_is_refused_although_it_is_an_int`, `::test_a_string_is_refused_although_the_pin_parses_it`, `::test_an_exact_float_is_refused_on_the_same_terms_as_an_inexact_one` (exact floats REFUSED, deliberately), and an AST import census `::test_privacy_n_has_no_route_through_the_pin_outside_this_module` that catches all six import forms including `from mitigation_unit import *` and function-local imports — whose single exemption is **PATH IDENTITY**, proved against a same-named decoy by `::test_the_exemption_is_path_identity_and_not_a_name`. The pin's own tests were scoped out, never weakened. |
| UNIT-02 | Phase 21 | **SATISFIED.** `teach_persona.build_bins(..., align_facts=None)` is a NEW kwarg on the existing packer, byte-identical to v2.0 when omitted. **The identity claim is only worth what its non-vacuity pair is worth** — `align_facts=None` is trivially satisfied by a kwarg nobody reads — so every `*_byte_identity` test is paired with an `*_is_wired` test, and the one kwarg that could NOT be made falsifiable (`render_family(question_bank=)`) was **DROPPED rather than shipped as a guard that cannot fail**. Baseline: `tests/fixtures/golden_build_bins_v2.json`, captured at `a18f675` (21-02) from a **git-clean, pre-edit** tree — enforced mechanically, not by convention, by `scripts/phase21_golden_capture.py::_refuse_if_dirty()` at module scope `:147` (BEFORE the sibling imports) and again at call time `:263`. Digests `token_bin_sha256 91c25493…`, `mask_bin_sha256 4a674423…`, `repr(stats)` exact, and `meta.tokenizer_sha256 e82e8e83…` proving the fixture is not stale — **all four independently re-derived by the verifier rather than read off a SUMMARY**. Guarded by `tests/test_phase21_aligned_bins.py::test_build_bins_byte_identity_default_matches_the_v2_golden`, `::test_build_bins_byte_identity_omitted_equals_align_facts_none`, and the non-vacuity `::test_align_facts_is_wired`. **THE STRUCTURAL CLAIM — no `block_size`-aligned window contains ids from two fact shards, therefore q=1 and an EXACT accountant — was measured on BOTH arms through the real CLI seam `build_arm_bins`, with `*_fact.bin` read back via `np.fromfile` rather than trusting the packer's offsets: INPUT-space impure windows `[]` and `[]`; max distinct fact ids in ANY window = 1 and 1; at 33 windows (`dp_n8`) and 316 windows (`dp_n64`).** `fact_window_impurities` defaults to `space="input"` (SC2's own wording), there is **no union mode**, and a repo-wide audit of all 7 `space=` call sites confirms no consumer overrides the default into vacuity. The TARGET-space count is stated POSITIVELY — exactly `n_facts - 1` boundary rows, 7 at n=8 and 63 at n=64, with every boundary token's mask asserted 0 — because that crossing is a property of the +1 label shift, not of padding. `SAMPLING_RATE_Q = 1.0` is pinned in the frozen module. `grad_accum_steps = n_facts` is **OBSERVED, not configured**: `tests/test_phase21_aligned_loader.py::test_grad_accum_steps_equals_n_facts` asserts the number of micro-steps one full lot of `get_batch_fact_aligned` produced equals the count of distinct fact indices the loader returned and equals `n_facts` (8 and 64). Geometry: whole-bin `(len-1) % 256 == 0` holds at 8,449 = 33×256+1 and 80,897 = 316×256+1; windows-per-fact is **ragged** `(4,4,4,4,4,5,4,4)` — never a common W — and spans partition all windows with no gap or overlap. **CR-01 IS THE FINDING THIS REQUIREMENT MOST NEEDED, AND IT WAS FOUND BY RUNNING THE ADVERSARY, NOT BY READING:** the whole-bin length contract was enforced at BUILD time (proof 7(a) reads back from disk) but **NOT at DRAW time** — with the label-shift tail removed from all three bins the loader silently served the last fact **half its windows** (record 7 got three instead of four) and raised nothing, while every neighbouring guard (three-bin 1:1, input-space purity, `n_facts`, span contiguity) passed. That changes what a privacy record IS, which is the exact invariant this requirement exists to make structurally true. Root-caused into **one** `_window_count()` at `src/personacore/training/data.py:129` with 3 callers (`:216`, `:261`, `:387`) — the shared-function fix, not a guard per caller. MEASURED after: `_window_count(16, 4, …)` RAISES, `_window_count(17, 4, …)` → 4, and every micro-step at both capacities serves the FULL span. Guarded by `::test_n7_all_three_bins_truncated_together_is_refused`. Adversarial cover: `tests/test_phase21_aligned_bins.py::test_window_purity_adversaries_input_space` (A0–A5, 5 cases) and `tests/test_phase21_aligned_bins.py::test_window_purity_adversaries_a4_raises_on_length` — the sharp one is `np.roll` by 1, invisible to an offset check, reported `[0, 2]`. D-06 run-time consumption: `tests/test_phase21_aligned_loader.py::test_fact_map_is_consumed_at_runtime` mutates the fact bin BETWEEN calls, and `::test_fact_bin_required_raises_distinguishably` proves deleted vs truncated raise **distinguishably**. **WR-02 CLOSED:** `dp_n8` / `dp_n64` were CLI-reachable but built the OLD flat bin; fixed by coupling the arm NAME to the packer at the one seam that writes bins (`DP_ARMS`, `scripts/teach_persona.py:260` at closure — resolve BY SYMBOL, this anchor has already drifted once from `:247`), with `tests/test_phase21_aligned_bins.py::test_published_arms_are_not_dragged_onto_the_aligned_path` as the negative control. |
| UNIT-03 | Phase 21 | **SATISFIED.** The multiplicity is MEASURED by `scripts/phase21_unit_record.py` (added `280e2c1`, plan 21-10) and committed as `results/phase21_multiplicity.json` (`sha256 e9e3b9bf…`, first added `c79b9bf`) — **five labelled rows at SEED=1337 / MAX_STEPS=200 / BATCH_SIZE=8, each carrying its OWN denominator**: `replay-in-bin @1.0` (n=8), `facts-only (D-10)` (n=8), `fact-aligned (D-01, D-05)` (n=8), `facts-only` (n=64), `fact-aligned` (n=64). `ATTRIBUTION_RULE` is named in ONE place and `count_aligned` reuses `data.fact_window_span` — the loader's own arithmetic — rather than re-deriving it. **THE MOST IMPORTANT PROPERTY OF THIS ROW IS WHAT THE NUMBERS CANNOT SAY: the conservation law pins the per-fact mean at `total_draws / n_facts` = 1600/8 = 200.0 BY ARITHMETIC, so publishing the mean would restate the input.** The information is therefore in the spread, and it is non-trivial: **min 143 / max 229 / spread 86** on the facts-only n=8 row, 14 / 36 / spread 22 at n=64, and exactly **1 / 1 / spread 0** on both fact-aligned rows — which is the q=1 claim of UNIT-02 showing up as measured data rather than as an assertion. The artifact says this out loud in `conservation_pinned_mean_note`. **The instrument is proven able to report ≠ 1** by `tests/test_phase21_multiplicity.py::test_instrument_can_report_not_one` (2 cases: a mis-built fixture AND a correct-control negative through the same call), which asserts the fixture really IS corrupted FIRST — a non-vacuity check on the non-vacuity check. Validated against **four independent oracles** (`-k oracle`, 4 tests): exact replay of the draw, one fact takes every draw, an undrawable fact counts zero, and a wrong attribution rule gives a different answer. Conservation is guarded at three denominators — `::test_conservation` (3 params), `::test_conservation_holds_at_the_real_budget_denominator`, `::test_aligned_conservation` — and denominator discipline by `::test_row_carries_its_denominator`; every published figure carries an explicit denominator field (`pad_fraction_denominator`, `ratio_denominator`, `draw_start_offsets_formula`, `total_tokens_formula`) and `tests/test_phase18_docs.py::test_no_bare_zero_percent_in_docs` is green. **CR-02 CLOSED, and it was a provenance defect in the two artifacts the phase exists to produce:** both recorded a `provenance.git_sha` under which their own emitter **did not exist** (verified by `git cat-file`). The phase had invented the right guard in 21-02 and not applied it to the artifact writer. Closed by `provenance.refuse_if_dirty` wired at `scripts/phase21_emit.py:77` and re-emitted at `eba0571`; the guard was **watched firing** (`SystemExit`, "REFUSING: the working tree is dirty", on an untracked file) rather than asserted, and the first-add commit is unchanged at `c79b9bf` so the ancestry guard did not move. **WR-01 CLOSED:** `n8_rows_reproduce_the_documented_table: true` was a hardcoded literal and false at the precision it claimed; it is now COMPUTED at `scripts/phase21_unit_record.py:887` over one derivation, and `tests/test_phase21_unit_record.py::test_the_n8_reproduction_claim_is_computed_and_could_have_been_false` asserts the same predicate returns **False** against the old tail-bearing denominator — the reconciliation 8448 vs 8449 was decided by UNIT INVARIANCE, not by whichever number made the flag green. **WR-05 REMAINS OPEN:** the artifact's headline D-10 finding is an arithmetic identity the same artifact elsewhere warns carries no information. |
| UNIT-04 | Phase 21 | **SATISFIED.** The decision is RECORDED with its ε consequence, not merely made: `REPLAY_OUTSIDE_N` (`scripts/mitigation_unit.py:144-165`) states that PersonaChat replay sits **outside** N, that q=1 and N = n_facts, and — the part that makes it a decision rather than a setting — spells out the consequence of the alternative, that counting public replay in N shrinks q and yields a flatteringly small ε. `results/phase21_privacy_unit.json` records the pair `replay_in_lot: true` / `replay_inside_privacy_n: false`, so "in the bin but not in the accountant" is unambiguous. **THE REAL RISK HERE IS A SIDE CHANNEL, AND IT IS CLOSED BY DIFFERENTIAL RATHER THAN BY ASSERTION.** `replay_window_budget` sizes the lot as `REPLAY_WINDOWS_PER_FACT * n_facts * BLOCK_SIZE` (`REPLAY_WINDOWS_PER_FACT = 4`, `scripts/teach_persona.py:177`) and **IGNORES `replay_ratio` and `teaching_tokens` ENTIRELY** — not multiplied in, not used as a cap, not consulted — because any residual dependence on `teaching_tokens` would make the public replay VOLUME a function of the private fact VALUES and reopen the channel. Precedence is total rather than blended, and both ignored inputs are named in the function's own reporting so a caller cannot mistake which sizing ran. Guarded by `tests/test_phase21_replay_volume.py::test_side_channel_closed` with a **live negative control** at `::test_side_channel_negative_control`; the differential runs the SAME call ONE KWARG APART (`replay_ratio=1.0, n_facts=8` versus `replay_ratio=1.0, n_facts=None`), which is the only thing making the two verdicts a property of the BRANCH rather than of two different fixtures. Quantization to whole windows is guarded at both capacities by `::test_window_quantized` (32 windows / 8,192 tokens per lot at n=8; 256 windows at n=64), and `::test_replay_constant_is_not_derived_from_the_corpus` pins that the constant is a chosen number, not a fitted one. The additive `train(replay_bin=, replay_mask_bin=, replay_windows=)` seam is proven **bit-identical when off** (`::test_replay_seam_off_is_bit_identical`) with its mandatory non-vacuity pair `::test_replay_seam_on_changes_the_trajectory`, plus `::test_replay_seam_refuses_partial_or_malformed_wiring` (5 cases) refusing partial wiring; `_prepend_replay`'s `n_facts` guard correctly rejects `bool`, which subclasses `int`. **IN-04 REMAINS OPEN AND IS SCOPED HONESTLY: the seam has NO production caller.** `train(replay_*)` and `_prepend_replay(..., n_facts=)` are reachable only from tests and from `phase21_unit_record`'s reporting; `main()` never wires them. **CLOSED 2026-08-26 (plan 22-10).** `scripts/teach_persona.py::train_arm` now passes `replay_bin=DIALOG_TRAIN_BIN`, `replay_mask_bin=DIALOG_TRAIN_MASK` and `replay_windows=replay_window_budget(stats['n_facts']) // BLOCK_SIZE` into `train()` on `dp_n8`/`dp_n64` (measured: 32 windows at n=8, 256 at n=64), reachable from `main()` via `python scripts/teach_persona.py {dp_n8|dp_n64} --sigma=<f> --clip-norm=<f>`. The docstring claim quoted below was corrected in the SAME diff so it names `train_arm` rather than an unnamed caller, and `tests/test_phase22_wiring.py::test_end_to_end_writes_no_scored_artifact` asserts the budget against `replay_window_budget` rather than a literal. The sentences above are left unamended as the record of what was true when Phase 21 closed. This is defensible as a declared Phase-22 seam (per-record clipping is DPSGD-01) and the off-path bit-identity is properly proven — but `replay_window_budget`'s own docstring claims "Every consumer calls this function: … `train()`'s replay seam **via its caller**", and there is no such caller in today's tree. **WR-03 REMAINS OPEN against this row, and it is an instance of the exact discipline this phase exists to enforce pointed at the phase itself:** `scripts/teach_persona.py:162-163` states in live source that the share "holds across capacities for free: 49.90% at n=64, because both sides scale with `n_facts`. Nothing re-tunes." The phase's own committed artifact records `documented_n64_claim_holds: false` and `0.44755244755244755`. Both sides do NOT scale — the padded teaching bin does not, because 56 filler facts pack to 5.054 windows each against the 8 locked facts' 4.125. **D-24 is NOT reopened:** `REPLAY_WINDOWS_PER_FACT = 4` was chosen on the n=8 table, which reproduces exactly. The file is editable and not ancestry-pinned, so the project's retract-in-place rule applies and nothing blocks the correction. |
| UNIT-05 | Phase 21 | **SATISFIED.** δ is pinned as the **literal** `DELTA = 1e-5` in the frozen `scripts/mitigation_unit.py`, beside `DELTA_TIMES_N_CEILING = 0.01`. What makes this row stronger than a recorded preference is that **the rejected recipe is kept RUNNABLE rather than described**: `rejected_delta(n) = n ** -1.1` is a live function in the same frozen module, so its self-contradiction is executable and re-measurable by anyone, and cannot rot into prose. MEASURED at closure, in `.venv`: at **N=8**, `8 ** -1.1 = 0.10153154954452942` and `δ·N = 0.8122523963562354` — the recipe fails **its own** `δ·N < 0.01` assertion by **81.2×**; at **N=64**, `0.010308655552913232` and `δ·N = 0.6597539553864469`, failing by **66.0×**. The pinned literal clears at both: `δ·N = 8e-05` and `0.00064`. **FOUR module-scope `_prove` guards carry this, sampled at N=8 AND N=64** — both capacities, so a guard cannot pass by being evaluated only where the arithmetic is friendly; plan 21-01 watched one go RED at n=64 only, which is the concrete reason the two-capacity sampling is not decoration. Guarded by `tests/test_phase21_unit_pin.py::test_prove_guards_pinned_delta_clears_the_ceiling`, `::test_prove_guards_rejected_recipe_fails_its_own_ceiling` and `::test_prove_guards_rejected_ratio_to_ceiling` (2 params each, n=8 and n=64), and — the test that makes the pair non-vacuous rather than two independent green lights — `::test_pinned_delta_and_the_rejected_recipe_cannot_both_be_green`. Republished at `results/phase21_privacy_unit.json → delta`, with `tests/test_phase21_unit_record.py::test_artifact_values_come_from_the_pin` asserting equality against the imported module so the artifact retypes no constant. Note that UNIT-05's own bullet text states "at N=8 it yields δ=0.1015 and fails its own `δ·N < 0.01` assertion by ~80×" — the measured figures above (`0.10153154954452942`, 81.2×) confirm the requirement's own arithmetic at full precision rather than merely at the rounding it was written with. |
| UNIT-06 | Phase 21 | **SATISFIED.** `scripts/phase21_filler.py` (added `fe9cabe`, plan 21-07) mints **56 unscored filler facts** across **8 filler-only slots**; 56 + 8 `LOCKED_FACTS` = **64**, which is the capacity lever. Disjointness is measured as set intersections and every one is empty: filler ids ∩ the 38 pooled ids from `all_pools()` = `[]`; filler ids ∩ `GATE_PROBES` (38) = `[]`; filler ids ∩ `_BY_ID` (38) = `[]`; filler slots ∩ the 11 published slots = `[]`; and filler values ∩ the 10-value leak vocabulary = `[]`. `refuse_collisions()` uses **containment in both directions, not equality**, against the 10 leak values, the 38 pool values and each other, and raises `SystemExit`. **THE STRONGER HALF IS REACHABILITY, NOT SET ARITHMETIC:** "unscored" is proven as a property of the IMPORT GRAPH — no scoring, teaching, evaluation or reporting module imports `phase21_filler` — because a value comparison can only say the filler is different, while the import census says it is *unreachable*. The before/after half of `tests/test_phase21_filler.py::test_outside_all_pools` runs in a **fresh interpreter**, since in-process the module is already in `sys.modules` and "before" could only compare a value to itself. Minting discipline is guarded by `::test_minting_discipline`, slot disjointness by `::test_slots_disjoint`. **SC5 NON-DISTURBANCE — the "touching no ancestry-pinned fixture" half — is two sha256 pins read as BYTES**, never as text, because a text read normalizes line endings and a CRLF rewrite would pass a text-mode hash while changing the file on disk: `scripts/phase18_extraction.py` = `d2b44806…` and the 270-question `results/phase16_recall_sample.json` = `407c4b93…`, both re-verified unchanged at closure. `phase18_extraction.py` carries a SECOND independent tier — it is ancestry-guarded, so a COMMITTED edit turns that guard permanently red and `adds[-1]` means a `git rm` + re-add cannot launder it; two independent tiers fire on one byte. Guarded by `tests/test_phase21_sc5.py::test_instruments_unchanged_byte_for_byte`, whose name is itself a correction — the plan's original name matched no `-k` selector. Leak scanning is bidirectional (`::test_no_filler_leak`), and tier composition is pinned by **EQUALITY** (`::test_locked_and_soft_tiers_are_unmoved`: `len(LOCKED_FACTS) == 8`, `len(SOFT_TIER_FACTS) == 2`, plus the ordered ids) because `tests/test_phase14_factset.py:101-103`'s inequalities `<= 8` / `<= 3` would admit a tier that SHRANK, silently changing what both capacities measure. **The `== 10` wall is 11 sites across 8 files** — censused mechanically with three independent grep patterns and, crucially, pinned as an EXECUTABLE `_EXPECTED_WALL` in `tests/test_phase21_sc5.py::test_wall_census_is_the_measured_set` rather than as a table; every documented figure before it (4 → 7 → 8 → 9) was low, and the count stopped drifting only when it became a test. The census excludes its own file by a mechanical `__file__` check (not a comment) and asserts every wall file is a member of `SC5_GUARD_SET`, so a wall site landing in a file the guard set does not run turns it RED. Corpus geometry, built through the real CLI seam and read back from disk: `dp_n64` = **1,408 episodes** (8 locked × 22 + 56 filler × 22 = 176 + 1,232), **72,093 teaching tokens**, 8,803 pad, **316 windows**, 80,897 total tokens, `grad_accum_steps = 64`. **TWO DEFECTS WERE FOUND BY EXECUTION AND ROOT-FIXED, BOTH RECORDED BECAUSE THEY ARE THE REASON THIS ROW IS TRUSTWORTHY:** (1) plan 21-09 found n=64 was **not reachable at all** — `build_arm_bins("dp_n64", …)` raised `KeyError: 'filler_boat_name'` — and it was fixed in the shared `_slot_forms_for` (`scripts/teach_persona.py:371`, reached from both `:417` and `:810`) rather than patched at the call site, so every arm resolves slot forms the same way; (2) `render_filler_episodes` iterated a frozenset without `sorted()`, and **three different orders were measured in three processes** — fixed to mirror `teach_persona.py:251` and guarded by `tests/test_phase21_filler.py::test_render_filler_episodes_is_order_stable` with a cross-process digest and a watched deliberate-RED. **WR-06 REMAINS OPEN against this row:** the `== 10` wall assertion in `scripts/phase21_filler.py:262` — by the module's own comment "the one file in the repo that could break it" — is a strippable `assert`, confirmed live by `python -O -c 'import phase21_filler'` importing clean, while every other refusal in the same file (`refuse_collisions`, `verify_round_trips`) correctly raises `SystemExit`. Latent, not live: the repo does not run under `-O` today. |
| DPSGD-01 | Phase 22 | **SATISFIED (plans 22-04, 22-06, 22-10, 22-11).** `src/personacore/privacy/dpsgd.py::DPSGD` clips **per RECORD** — `absorb_record` reads one micro-batch's `.grad`, computes a **GLOBAL** L2 norm across all 72 trainable LoRA tensors as one vector, scales by `coef`, adds into a DP-owned accumulator holding the **SUM**, and DRAINS `.grad`; one record therefore moves the sum by at most `C` and the sensitivity is exactly `C` independently of the lot size (D-02). Noise is drawn from a **dedicated** `torch.Generator` at `std = self.sigma * self.C` and added to the SUM, the `/N` happens LAST, and exactly ONE write per parameter combines the private and public terms. **LoRA-only with the base frozen is enforced as a PROPERTY OF THE MECHANISM, not trusted as a property of one caller:** `__init__` is a full pre-pass of nine refusals, including a `requires_grad` audit by `"lora_" in name` (`inject.py`'s own predicate) and a closed-form census `r*n_layer*18*n_embd` derived entirely from the LIVE model — measured, `inject_lora` alone leaves **172 tensors / 14,223,360 params** trainable and `+ mark_only_lora_trainable` gives **72 / 331,776**, and the unfrozen case is REFUSED (`[dp-refusal:unfrozen-base]`). **THE SEAM IS ADDITIVE AND GRADIENT-SIDE, and both halves are proven:** `training/loop.py::_optimizer_step` takes `dp_fn=` as its last parameter with the legacy averaged-gradient `clip_grad_norm_` reduced to **ONE reachable call site inside `if dp_fn is None:`** (observed 2 calls with the seam off, **0** with it on), and DPSGD-02's row records the off-path bit-identity on all three fingerprints. **A PRODUCTION CALLER EXISTS (plan 22-10):** `python scripts/teach_persona.py {dp_n8\|dp_n64} --sigma=<f> --clip-norm=<f>` routes the aligned corpus through `get_batch_fact_aligned` at `grad_accum_steps = n_facts` (8 / 64), draws 32 / 256 public replay windows per lot, and releases a per-record-clipped, Gaussian-noised gradient — proven end to end by a 0.94 s CPU run through `main()` with `dp_fn._records == 8` after it, each of the four wirings asserted by its OWN observation. **THAT THE CLIPPING IS GENUINELY PER-EXAMPLE IS MEASURED, NOT ASSERTED (plan 22-11):** the drain was deleted from the real committed module and the add/remove-one sensitivity — the quantity D-18 says the accountant is told — was measured at **1.734481 × C** against the honest mechanism's **1.000000 × C**, with `[dp-invariant:drain]` and `test_dpsgd_step_reaches_no_forbidden_call[absorb_record]` both watched refusing. σ and C are **keyword-only with NO default** at the constructor, at `train_arm` and at the CLI, so Phase 22 names no value for either and Phase 20's Z boundary is untouched. **Residual, stated rather than glossed:** the arm is wired and proven at FIXTURE scale (2 optimizer steps, `n_embd = 16`, a synthetic base checkpoint). **No real training run exists** — that is DPSGD-06's, and Phase 23's. |
| DPSGD-02 | Phase 22 | **SATISFIED (plan 22-06).** `train(dp_fn=None)` — and `dp_fn` omitted — reproduce `tests/fixtures/golden_trajectory_v1.json` (`meta.captured_at_sha 6a46441cc17b6fc3c951a12ee0b6620b88b82d91`) on **all three fingerprints**: exact CSV text (sha256 `2f4b95ac4c05add4…`, 6 rows), the final loss's `repr` (`9.435891151428223`), and the sha256 of the parameter bytes (`647f5981027bfce1ad1d38867c3f9aa293e95c9bb27101a3c096517d7ace23ac`). This is bit-identity, not a loss curve. `tests/test_phase22_dpsgd.py::test_seam_off_bit_identical` is platform-gated on `(system, machine, torch.__version__)` and **RAN rather than skipped** on the capture platform (Darwin/arm64/torch 2.7.1); `::test_seam_omitted_equals_seam_none` carries the same guarantee platform-independently and never skips; `::test_golden_fixture_is_the_phase10_one` is the meta-guard against a truncated fixture making the identity vacuous. The recipe is `tests/test_loop_penalty_fn.py::_run_recipe`, IMPORTED rather than re-written, so no second golden recipe can drift from the fixture's own `meta` block. Structurally, `git diff src/personacore/training/loop.py | grep -c "^-[^-]"` returns **6** — the `/accum` line, the clip line, and 4 rewritten docstring lines — with **zero** changed lines inside `replay_fn`, and `CKPT_SCHEMA_VERSION` unchanged at `1`. The full suite went 1219 → 1231 passed with no regression. **Residual, stated rather than glossed:** the seam is additive at `train()` and proven inert; it has no PRODUCTION caller yet — `scripts/teach_persona.py` still passes no `dp_fn` — which is D-08's wiring and plan 22-08's scope, and is DPSGD-01's remaining half, not this row's. |
| DPSGD-03 | Phase 22 | **SATISFIED (plans 22-03, 22-05, 22-09).** `src/personacore/privacy/accountant.py` implements the analytic Gaussian mechanism in **stdlib `math` only** — `delta_closed` (Balle–Wang Thm 8), `delta_quadrature` (an independent numerical route over a DERIVED integration range), `epsilon_for` and `sigma_for` — with the math-only property asserted **over the AST** rather than by inspection (`::test_accountant_imports_math_only`). **The two oracles are genuinely different mathematics and they agree**: `::test_two_oracles_agree` compares `delta_closed` against `delta_quadrature` under a **relative** tolerance of 1e-9 with `a != 0.0 and b != 0.0` asserted FIRST, so a pair of zeros cannot pass as agreement; `::test_closed_form_frontier` pins the closed form against a committed high-precision reference table shipped as **literal data**, so `mpmath` never becomes a test dependency (RPT-03). **Exactness under q=1 composition is proven as an IDENTITY, not a claim**: `::test_composition_identity` sweeps ≥20 `(σ, T)` pairs asserting `ε(σ,T,δ)` matches `ε(σ/√T,1,δ)` to a relative gap ≤ 1e-12 — never `==`, per RESEARCH F3 — and `::test_round_trip` closes `sigma_for(epsilon_for(σ,T,δ),T,δ) ≈ σ`. **The degenerate and low-privacy corners are covered rather than avoided**: `σ = 0 → ε = inf` explicitly (`::test_sigma_zero`), never a `ZeroDivisionError`; `::test_low_privacy_corner` shows the DERIVED integration range beating a fixed `[-14, 14]` at ε=8, μ=0.5, where the fixed range gives 1.0 relative error; and `::test_oracle_refuses` observes the non-vacuity refusal firing on all three conditions separately. **THE ANTI-PHOTOGRAPH GUARD IS THE LOAD-BEARING HALF (plan 22-09):** `GOLDEN_EPSILON`'s seven pinned rows in the frozen `scripts/mitigation_accountant.py` are **re-derived by bisecting `delta_quadrature` ALONE**, with "alone" asserted over the test's OWN AST (`delta_quadrature` in its called-name set, `epsilon_for` and `delta_closed` excluded) rather than promised in a docstring. Worst relative deviation **5.749506e-15** against `GOLDEN_EPSILON_REL_TOL = 1e-12` — **173.93× of margin** — over 356 oracle calls, 0 refusals, max `z` probed **7.5000** against the committed `ZERO_BOUNDARIES` boundary of `38.372164249`. Non-degeneracy in BOTH directions: every row deviates (min 1.6233e-15, so the pin is not a photograph of this float64 route) and the tolerance is bracketed by a measured floor and a `1e-11` ceiling, because the plan's own `10 * REL_TOL` control was **watched GREEN under the widening it existed to catch** and replaced. **D-18's definitional half is closed too**: the adjacency relation `add/remove one fact` and `SENSITIVITY_MULTIPLIER = 1.0` agree across the frozen pin, `accountant.py`'s docstring and `dpsgd.py`'s `std=` arithmetic (a two-operand `Mult` over `self.sigma`/`self.C` with **zero** numeric operands), presence asserted before agreement, under exactly the node id the pin cites by symbol. 16 mutations, 16 distinct REDs, byte-identical restores. |
| DPSGD-04 | Phase 22 | **SATISFIED (plan 22-11).** All four named silent-non-privacy fakes were applied to the **REAL committed** `src/personacore/privacy/dpsgd.py` on disk, each **WATCHED reddening** a named test with its assertion message captured verbatim, each restored to a byte-identical blob — pre- and post-mutation `sha256 140f51082ab188a06de5426e8e1827c85423f19c43e45d45bca90515a96013eb` and `git rev-parse HEAD:…` blob `f0e267c294b99f9be996b3dc15dc808a87e1bd52`, `git diff --exit-code` exit 0, all four times. **The deliverable is the OBSERVED RED, and every verdict is read from pytest's OWN summary line, never `$?` after a pipe.** Against a `78 passed, 2 skipped` baseline: **FAKE 1** (drain deleted → clipping the averaged gradient) `12 failed`, detectors `test_dpsgd_step_reaches_no_forbidden_call[absorb_record]` (*"reach {}, not exactly {'absorb_record': ['.grad=']}"*) and `[dp-invariant:drain]`; **FAKE 2** (`self._c2 = 2.0 * self.C` used as the clip bound) `5 failed`, **BOTH** `test_dpsgd_has_exactly_one_clip_constant` (*"treats ['C', '_c2'] as clip constants"*) and `[dp-invariant:sensitivity]` (*"the CLIPPED global norm is 0.0020000000949949026, above C * (1 + 1e-06) = 0.001000001"* — exactly 2C); **FAKE 3** (`/N` hoisted ahead of the draw) `6 failed`, detectors `test_dpsgd_draws_the_noise_before_it_divides` (*"draws the noise at statement 4 and divides by 'accum' at statement 1"*) and the σ>0 magnitude guard (*"std 1.0006904602050781 at N = 4, a ratio of 4.002762"*); **FAKE 4** (`self._g.manual_seed(0)` in `finalize`) `10 failed`, both AST guards and `[dp-invariant:generator]`. **Nine detectors produced NINE DISTINCT assertion messages**, with the one near-collision named rather than double-counted (`test_dpsgd_step_reaches_no_forbidden_call` catches FAKE 1 and FAKE 4, at two parametrizations reporting two different offender dicts); `tests/test_phase22_fakes.py` ships all four as **committed, repeatable** in-process RED-then-GREEN probes, each with an unmutated control in the same test, and the AST halves run the **live guard TEST FUNCTIONS CI runs** by repointing `_DPSGD_PATH` at a mutated copy. **THE ROW IS RECORDED WITH ITS BLIND SPOTS, because a battery that lists only green is not a battery.** (i) **D-17's own table is FALSE for FAKE 3** — it credits D-06's σ=0 identity, and under the real-module mutation `test_sum_then_noise_then_divide` and all three `test_sigma_zero_non_binding_clip_reproduces_the_default_path` parametrizations stayed **GREEN**: at σ=0 the draw is exact zeros, so `(S+0)/N` and `(S/N)+0` are the same bytes. (ii) A **second** FAKE-3 blind spot no prior plan had named: at `accum = 1` the divide is a no-op at every σ, so three of the four `(σ, N)` cells are blind and only `(σ>0, N>1)` detects — `TrainConfig.grad_accum_steps` defaults to `1`, and 22-10's arms run at 8 / 64, which is what keeps the detecting regime reachable. (iii) The runtime `C*(1+tol)` check is **ONE-SIDED**: measured, a second clip constant of `C/2` completes with **no refusal at all**; only the AST guard, which never looks at a number, catches both directions. All three are asserted as committed tables and controls, not described. |
| DPSGD-05 | Phase 22 | **SATISFIED (plan 22-07).** `checkpoint.py` carries `rng["mps"] = torch.mps.get_rng_state() if torch.backends.mps.is_available() else None`, beside `cuda` in the identical `None`-when-unavailable shape, and `CKPT_SCHEMA_VERSION` is unchanged at `1`. **The load-bearing half is the RESTORE FORM, and the asymmetry is deliberate:** `load_checkpoint` reads `rng.get("mps")` while the `rng["cuda"]` line one above keeps its subscript, because `cuda` has existed since schema v1 and `mps` arrives now — MEASURED on the real gitignored artifacts, `checkpoints/latest.pt` (step 50,000) and `checkpoints/phase14_real_latest.pt` both carry `rng` keys exactly `{python, numpy, torch, cuda}`, so a subscript would `KeyError` on every artifact this project has ever written. `ckpt.get("scaler")` in the same function is the recorded precedent. `tests/test_phase22_checkpoint.py::test_old_checkpoint_without_mps_slot_still_loads` builds that pre-Phase-22 shape **in-test** (save → delete `rng["mps"]` → re-save, with a meta-guard that the fixture genuinely lacks the key and that its key set equals the real one), so the property holds with `checkpoints/` empty; `::test_old_on_disk_checkpoint_still_loads` adds the genuinely-old artifact as a `skipif`-gated leg. Mutation M1 (`.get()` → subscript) was **watched RED on both legs**. **THE KILL→RESUME HALF IS PROVEN WITH THE BITS SHOWN, not with a loss curve:** `::test_resume_epsilon_bit_identical` drives `train(max_steps_override=2)` → *kill* (model + optimizer + `DPSGD` reconstructed fresh) → `train(resume_from=…, max_steps_override=4)` and compares the reported ε under **exact `==`** — `9.9972561464343` (`float.hex` `0x1.3fe985b8d5732p+3`) against itself at σ=1.0, and `inf` at σ=0.0, δ=1e-5. `==` rather than V-03's `rel_tol` is correct here because this is **the same call shape across two processes** (RESEARCH F3), with `lora/inject.py:113-118`'s *"a tolerance would only weaken this"* cited; both register entries are written down together so they cannot be conflated. Non-degeneracy at σ>0: `epsilon_for(1.0, 2, 1e-5) = 6.572970067030331` ≠ the above, so the equality is not green over a constant — and that control **cannot exist at σ=0** (ε is `inf` for every T), which is stated rather than omitted. **T is read from the COUNT OF COMPOSED STEPS, not from the checkpoint's `step` field**, because mutation M6 measured the field-read version incapable: with `start_step` reset to `0` the resumed arm composes 6 steps while its checkpoint still records 4, so a field-read ε is identical across the arms and **optimistic**. **ε BIT-IDENTITY ALONE DOES NOT PROVE THE RNG RESUMED** — ε is a function of (σ, T, δ) and not of the RNG — so the RNG claim carries its own control: the uninterrupted and resumed arms' next noise draws are `torch.equal` (`1.06912, -1.483205, -1.892289`), and the **negative control** deleting `dp_noise_rng` at the production boundary makes them diverge (`-1.255206, 1.22789, -0.570479`). The resume goes **only** through `train(resume_from=…)`; `grep` confirms no direct `load_noise_rng_state` call anywhere in the test. Mutations M4 (restore dropped) and M5 (END-OF-CALL splat dropped) both watched RED. **D-14 is recorded honestly rather than dressed up:** `rng["mps"]` is **required-but-UNEXERCISED by the DP path** — D-07 locked a dedicated generator whose draw does not move the global MPS state — and `dp_noise_rng` (an `**extra` key, 44 B on MPS / **5,056 B on CPU**) is the slot that actually fires. The MPS round-trip row **RAN rather than skipped** on the development box (Darwin/arm64, torch 2.7.1, `mps.is_available()` True) and **will skip in CPU-only CI**; both were executed and both are reported. |
| DPSGD-07 | Phase 22 | **SATISFIED (plan 22-07).** `src/personacore/lora/` is **byte-unchanged** — `git diff --exit-code -- src/personacore/lora/` exits 0 — and the requirement's own `lora/layer.py:41` anchor was **verified against HEAD before use**: it is the inline matmul `y = y + self.scale * (self.dropout(x) @ self.lora_A.T @ self.lora_B.T)`, with the bare `nn.Parameter` assignments at `:29-30`. The anchor is correct and needed no in-place correction. Three guards, none of which requires a gitignored file. `tests/test_phase22_checkpoint.py::test_lora_linear_holds_bare_parameters` walks a freshly injected model and asserts `lora_A`/`lora_B` are `torch.nn.Parameter` instances and that the set of child `nn.Linear` modules is **exactly `{"base"}`** — the property a restructuring breaks, checked directly rather than inferred. `::test_lora_state_dict_keys_survive_a_round_trip` builds a v3.0-shaped adapter **in-test**, pushes it through this phase's modified `checkpoint.py` (`export_adapter` → `load_adapter`) and asserts **hard set equality** (never a subset) against a second fresh model's `lora_state_dict` keys, with the closed-form `2 * 6 * n_layer > 0` meta-guard so two empty sets cannot pass, then applies it through `load_adapter_weights`' key+shape+scale audit. `::test_v3_on_disk_artifacts_still_load` adds three gated legs against the real artifacts — `checkpoints/persona_adapter.pt` (72 keys, `r=8`, `alpha=16.0`, `n_layer=6`, `n_embd=384`, all derived from the artifact rather than assumed), the smallest `*latest.pt`, and `model_slim.pt` — each `pytest.param(marks=skipif(not path.exists(), …))` so a missing file skips only itself, plus a collection meta-guard asserting ≥3 declared cases so an empty directory reports 3 skips rather than nothing. **`checkpoints/` IS GITIGNORED and V-17 is DPSGD-07's only validation row, so it was EXECUTED with the directory absent: 8 passed, 4 skipped, 0 failed.** **THE FINDING THAT CHANGED WHAT SHIPPED, recorded so no downstream plan inherits a false credit:** mutation M7 — the exact restructuring this requirement forbids, bare `nn.Parameter` → `nn.Linear` submodules — left the key-set round trip **GREEN**, because donor and comparison model are both built by the mutated code and both key sets become `…lora_A.weight`. Set-equality proves the export/load path preserves keys; it does *not* prove they are the historical v3.0 keys. The shipped **key FORM** is therefore pinned as a literal (`all(k.endswith(("lora_A", "lora_B")))`), measured against `persona_adapter.pt`'s own 72 keys, and M7 now reddens in **both** the local and the CI shape (8 failed / 5 failed). M8 (`lora_A` renamed out of the `"lora_"` filter) reddens 10 tests including `test_lora_artifact.py::test_no_base_weight_leak`. `layer.py`'s sha256 `f21c7847…` is identical before and after the probe. |
| CAL-01 | Phase 23 | |
| CAL-02 | Phase 23 | |
| CAL-03 | Phase 23 | |
| CAL-05 | Phase 23 | |
| DPSGD-06 | Phase 23 | sigma=0 is the DP arm's first executed run |
| CTRL-03 | Phase 23 | never-taught fresh adapter; depends on nothing, scheduled once |
| ADVT-01 | Phase 24 | |
| ADVT-02 | Phase 24 | |
| ADVT-03 | Phase 24 | |
| CTRL-01 | Phase 25 | run first, as a sweep point |
| CTRL-02 | Phase 25 | run first, as a sweep point |
| FRONT-01 | Phase 25 | |
| FRONT-02 | Phase 25 | |
| FRONT-03 | Phase 25 | |
| FRONT-04 | Phase 25 | |
| CANARY-01 | Phase 26 | |
| CANARY-02 | Phase 26 | |
| RELRN-01 | Phase 27 | |
| RELRN-02 | Phase 27 | |
| RELRN-03 | Phase 27 | |
| RELRN-04 | Phase 27 | |
| RELRN-05 | Phase 27 | |
| RPT-01 | Phase 28 | |
| RPT-03 | Phase 28 | |
