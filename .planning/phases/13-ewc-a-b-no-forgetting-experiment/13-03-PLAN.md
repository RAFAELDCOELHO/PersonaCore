---
phase: 13-ewc-a-b-no-forgetting-experiment
plan: 03
type: execute
wave: 3
depends_on: [13-02]
files_modified:
  - scripts/plot_phase13.py
  - tests/test_phase13_plots.py
  - results/phase13_forgetting_curve.png
  - results/phase13_frontier.png
  - scripts/make_retention_samples.py
  - results/phase13_retention_samples.md
autonomous: true
requirements: [VIZ-01, VIZ-04, DEMO-04]

must_haves:
  truths:
    - "The committed forgetting-curve figure shows retention PPL vs steps for both 4000-step arms with a dashed baseline at 2.1066 and an acquisition companion panel"
    - "The committed frontier figure has exactly SIX λ points (0, 0.01, 0.1, 1, 10, 100) labeled as 1250-step sweep endpoints"
    - "Retention samples from BOTH arm endpoints exist in one markdown file, generated in a single script run with a shared seeded prompt set and measured proxies"
  artifacts:
    - path: "scripts/plot_phase13.py"
      provides: "VIZ-01 + VIZ-04 figure generation from committed CSVs"
      contains: "LAMBDA0_POINT"
    - path: "results/phase13_forgetting_curve.png"
      provides: "VIZ-01 committed figure"
    - path: "results/phase13_frontier.png"
      provides: "VIZ-04 committed figure"
    - path: "scripts/make_retention_samples.py"
      provides: "D-12 one-run both-endpoints TinyStories continuation sampler"
    - path: "results/phase13_retention_samples.md"
      provides: "Representative retention-side samples with measured proxies"
      contains: "REPRESENTATIVE"
  key_links:
    - from: "scripts/plot_phase13.py"
      to: "results/ft_lam_0.01.csv"
      via: "final-row (retention_ppl, dialog_ppl) endpoint reads for the frontier"
      pattern: "ft_lam_"
    - from: "scripts/make_retention_samples.py"
      to: "checkpoints/phase13_naive_latest.pt"
      via: "both endpoint checkpoints loaded in ONE run (D-12)"
      pattern: "phase13_naive_latest"
---

<objective>
Produce the two committed figures (VIZ-01 forgetting curve, VIZ-04 λ frontier) and the D-12
retention-side qualitative samples from both arm endpoints.

Purpose: the visual + qualitative evidence the A/B report and Phase 15 consume.
Output: plot script + smoke test + two PNGs; sample script + committed samples markdown.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-ewc-a-b-no-forgetting-experiment/13-PATTERNS.md
@.planning/phases/13-ewc-a-b-no-forgetting-experiment/13-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Plot script + smoke test → both PNGs committed</name>
  <files>scripts/plot_phase13.py, tests/test_phase13_plots.py, results/phase13_forgetting_curve.png, results/phase13_frontier.png</files>
  <read_first>
    - demo.ipynb plot cells 2 and 4 (the only matplotlib precedent: stdlib csv.DictReader + default color cycle)
    - results/phase13_naive/run.csv and results/phase13_ewc/run.csv (arm curve data)
    - results/ft_lam_0.01.csv header + final row (sweep endpoint schema: step,train_loss,val_loss,lr,tokens,wall_clock,dialog_ppl,ewc_penalty,retention_ppl)
    - results/ft_lr_9e-5.csv header (NO retention_ppl column — Pitfall 1, load-bearing)
    - results/retention_anchors.json (2.107553076833866 anchor vs 2.1066 headline distinction — Pitfall 3)
    - scripts/evaluate.py lines ~46-59 (thin no-CLI main() + _REPO_ROOT constants pattern)
  </read_first>
  <behavior>
    - build_frontier_points() returns exactly 6 (label, dialog_ppl, retention_ppl) tuples: λ=0 hardcoded (4.4453, 5.9553), five read from ft_lam_{0.01,0.1,1,10,100}.csv final rows — verified values (dialog, retention): 0.01→(4.7298, 3.7813), 0.1→(5.6355, 3.0057), 1→(7.2144, 2.7734), 10→(10.5552, 2.1351), 100→(16.2112, 2.1082)
    - plot functions accept an output-dir argument and write phase13_forgetting_curve.png and phase13_frontier.png there
  </behavior>
  <action>
    Create `scripts/plot_phase13.py` — pure csv+matplotlib, no torch import, `matplotlib.use("Agg")`
    before pyplot (headless savefig; nothing in the repo calls show from scripts). Structure:
    `_REPO_ROOT` constants, two module-level functions taking an `out_dir` parameter
    (`plot_forgetting_curve(out_dir)`, `plot_frontier(out_dir)`), thin `main()` writing into
    `results/` (Open Q2 recommendation: PNG, dpi=150, next to the report — no figures/ dir).

    **VIZ-01** (`phase13_forgetting_curve.png`): two side-by-side panels, figsize ~(11, 4.5).
    Left panel: `retention_ppl` vs `step` from results/phase13_naive/run.csv (label "naive (λ=0)",
    C1) and results/phase13_ewc/run.csv (label "EWC (λ=0.01)", C0), plus
    `plt.axhline(2.1066, linestyle="--", color="gray", label="v1.0 headline 2.1066 (full-val, unmasked)")`.
    The dashed line is 2.1066 — NEVER the 2.107553 curve anchor (Pitfall 3; step-0 points in the
    CSVs already carry 2.107553076833866). Right panel (acquisition companion): `dialog_ppl` vs
    `step`, same two arms/colors. Figure title/caption text includes "4000-step arms" (Pitfall 4).

    **VIZ-04** (`phase13_frontier.png`): scatter of retention_ppl (y) vs dialog_ppl (x), one
    point per λ via `build_frontier_points()`: module constant
    `LAMBDA0_POINT = {"dialog_ppl": 4.4453, "retention_ppl": 5.9553}` with the citation comment
    (smoke report Stage 2/3, commit 666d096; ft_lr_9e-5.csv has no retention column — Pitfall 1;
    hardcode-with-citation register, never parse the report at runtime), plus the final rows of
    results/ft_lam_{0.01,0.1,1,10,100}.csv. Annotate each point with its λ value. Axis/title
    text states "1250-step sweep endpoints" (Pitfall 4 — never mix with the 4000-step arms).

    Create `tests/test_phase13_plots.py`: importlib-load the script (same justified pattern as
    tests/test_phase13_driver.py); one test asserting `len(build_frontier_points()) == 6`
    (the Pitfall-1 regression: a naive CSV-only read yields five) with λ=0 present at
    (4.4453, 5.9553); one tmp_path smoke test calling both plot functions and asserting both
    PNG files exist and are non-empty.

    Run `main()` once, commit both PNGs (.gitignore has no image exclusion — verified).
    Ruff-clean.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase13_plots.py -x -q && .venv/bin/python scripts/plot_phase13.py && test -s results/phase13_forgetting_curve.png && test -s results/phase13_frontier.png</automated>
  </verify>
  <acceptance_criteria>
    - scripts/plot_phase13.py contains `LAMBDA0_POINT`, `4.4453`, `5.9553`, `2.1066`, `axhline`, `1250-step sweep endpoints`, and `4000-step arms`
    - scripts/plot_phase13.py does NOT read retention_ppl from ft_lr_9e-5.csv (grep for `ft_lr` shows no such read)
    - tests/test_phase13_plots.py exits 0 and asserts six frontier points
    - Both PNGs committed (`git ls-files results/phase13_*.png` lists both)
    - Full suite green: `.venv/bin/python -m pytest tests/ -x -q` exits 0
  </acceptance_criteria>
  <done>VIZ-01 and VIZ-04 committed; six-point frontier pinned by test.</done>
</task>

<task type="auto">
  <name>Task 2: D-12 retention-side samples from both endpoints in one run</name>
  <files>scripts/make_retention_samples.py, results/phase13_retention_samples.md</files>
  <read_first>
    - scripts/make_transcripts.py (protocol precedent: MPS header, seeded default_rng selection, _complete helper with collect/stop_ids semantics, measured-proxies markdown header, seed_everything for warm sampling)
    - scripts/evaluate.py (samples section: greedy + warm block layout per prompt, "REPRESENTATIVE, not cherry-picked" register)
    - scripts/finetune_ab.py (arm_outputs — the checkpoint paths to load)
  </read_first>
  <action>
    Create `scripts/make_retention_samples.py` adapting make_transcripts.py to TinyStories
    continuations (RESEARCH Pattern 5). Requirements:

    1. Same MPS-fallback header, `_REPO_ROOT` constants, refuse-to-rerun guard on
       `results/phase13_retention_samples.md` (WR-02 style), trusted-only
       `torch.load(..., weights_only=False)` with the T-12-10 comment for BOTH
       `checkpoints/phase13_naive_latest.pt` and `checkpoints/phase13_ewc_latest.pt` —
       loaded in this ONE script run (D-12: never two separately curated passes).
    2. Pre-registered shared prompt set: split `data/TinyStoriesV2-GPT4-valid.txt` on
       `<|endoftext|>`, pick N_PROMPTS = 10 stories with a seeded LOCAL
       `np.random.default_rng(1337)` (global streams untouched), encode each through the
       frozen tokenizer (personacore.tokenizer.from_json — same tokenizer path as the driver)
       and truncate to the first 32 tokens as the prompt. NOT hand-formatted strings.
    3. Generation per checkpoint per prompt via `generation.core.collect` with
       `forbid_ids=undecodable_ids_mask(...)`, `STOP_IDS = {8184}` (eos only — story mode, no
       user-turn stop), MAX_NEW_TOKENS = 128; one greedy and one warm
       (temperature=0.8, top_p=0.95) completion per prompt, `seed_everything(1337)` before
       sampling for deterministic re-runs.
    4. Measured proxies over ALL generations, per arm (Phase-12 proxies applied per D-12):
       stop-id termination fraction (fewer than MAX_NEW_TOKENS generated = stopped — collect
       stops WITHOUT yielding the stop id) and mid-generation role-token leakage count for
       ids 8185/8186/8187 (role tokens mid-story = dialogue contamination of the base task —
       exactly the forgetting axis).
    5. Output `results/phase13_retention_samples.md`: header with the "REPRESENTATIVE, not
       cherry-picked" blockquote (seeded rng, shared prompts, one run, both arms), the proxies
       table per arm, then per-prompt sections showing the prompt text and both arms'
       greedy + warm continuations side by side.
    6. Run it, commit the markdown. Ruff-clean; full suite stays green.
  </action>
  <verify>
    <automated>.venv/bin/python -c "t=open('results/phase13_retention_samples.md').read(); assert 'REPRESENTATIVE' in t and 'naive' in t and 'ewc' in t and 'leakage' in t.lower()"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/make_retention_samples.py contains `STOP_IDS = {8184}`, `default_rng(1337)`, loads both `phase13_naive_latest.pt` and `phase13_ewc_latest.pt`, and guards its output file with SystemExit
    - results/phase13_retention_samples.md committed; contains proxies for BOTH arms (stop-id fraction + role-token leakage counts) and 10 prompt sections with greedy and warm blocks per arm
    - `git status --porcelain results/finetune_prod.csv` empty; no Phase-12 artifact touched
    - `.venv/bin/ruff check scripts/make_retention_samples.py` exits 0
  </acceptance_criteria>
  <done>Retention-side qualitative evidence committed from one run over both endpoints with measured proxies.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sample script → arm checkpoints | pickle deserialization of local artifacts |
| scripts → committed evidence | output guards prevent clobbering recorded samples/figures |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-07 | Info Disclosure/EoP | torch.load of arm checkpoints | mitigate | weights_only=False only on the project's OWN checkpoints with the TRUSTED-only comment |
| T-13-08 | Tampering | samples/figures overwrite | mitigate | refuse-to-rerun guard on the samples markdown; figures regenerable from committed CSVs (deterministic, low risk — regeneration is the intended path) |
| T-13-09 | Repudiation | cherry-picking accusation surface | mitigate | seeded prompt selection, one-run both-arms protocol, proxies measured over ALL generations, stated in the file header |
| T-13-SC | Tampering | package installs | accept | none this phase |
</threat_model>

<verification>
- Both PNGs + samples markdown committed; frontier six-point test green
- Full suite (`.venv/bin/python -m pytest tests/ -x -q`) exits 0
</verification>

<success_criteria>
- VIZ-01 committed with dashed 2.1066 + acquisition panel; VIZ-04 committed with six labeled 1250-step endpoints
- D-12 samples committed: shared prompts, one run, both arms, measured proxies
</success_criteria>

<output>
Create `.planning/phases/13-ewc-a-b-no-forgetting-experiment/13-03-SUMMARY.md` when done
</output>
