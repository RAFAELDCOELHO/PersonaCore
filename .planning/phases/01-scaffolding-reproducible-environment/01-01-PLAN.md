---
phase: 01-scaffolding-reproducible-environment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - requirements.txt
  - .gitignore
  - src/personacore/__init__.py
  - src/personacore/config.py
  - tests/conftest.py
  - tests/test_config.py
  - tests/test_package.py
autonomous: false
requirements: [ENV-01, ENV-02, ENV-03]
user_setup:
  - service: PyPI dependency set (torch, numpy, matplotlib, gradio, pytest, ruff, tqdm)
    why: "slopcheck legitimacy verification was sandbox-blocked during research; all deps are [ASSUMED] and require human confirmation before first install"
    dashboard_config:
      - task: "Confirm the seven packages are the genuine well-known projects on their registries"
        location: "pypi.org/project/{torch,numpy,matplotlib,gradio,pytest,ruff,tqdm}"

must_haves:
  truths:
    - "pip install -e \".[cpu,dev]\" succeeds in a clean Python 3.11 venv"
    - "import personacore resolves only after install (src-layout, not importable from repo root uninstalled)"
    - "RuntimeConfig defaults to fp32 (amp=False)"
    - "RuntimeConfig auto-disables AMP when device is cpu"
    - "RuntimeConfig with amp_dtype=bfloat16 on a Pascal/P100 device (capability < (7,0)) raises ValueError"
    - "ModelConfig and TrainConfig are dataclasses carrying the model/training hyperparameters"
  artifacts:
    - path: "pyproject.toml"
      provides: "PEP 621 installable package; torch excluded from core deps; [cpu]/[demo]/[dev] extras; requires-python >=3.10,<3.12; ruff + pytest config"
      contains: "[tool.setuptools.packages.find]"
    - path: "requirements.txt"
      provides: "documented venv dependency list, consistent with pyproject"
    - path: "src/personacore/config.py"
      provides: "RuntimeConfig (device/precision + bf16 guard), ModelConfig, TrainConfig dataclasses"
      contains: "class RuntimeConfig"
      min_lines: 40
    - path: "src/personacore/__init__.py"
      provides: "thin package init exposing __version__"
    - path: "tests/test_config.py"
      provides: "fp32-default, AMP-off-on-CPU, bf16-raises-on-Pascal unit tests"
  key_links:
    - from: "pyproject.toml"
      to: "src/personacore"
      via: "[tool.setuptools.packages.find] where=[\"src\"]"
      pattern: "where.*src"
    - from: "src/personacore/config.py RuntimeConfig.__post_init__"
      to: "torch.cuda.get_device_capability"
      via: "bf16 Pascal guard"
      pattern: "get_device_capability"
---

<objective>
Establish the installable `personacore` package (src-layout, PEP 621) and the code-first config layer — the foundation every later phase and plan imports. Delivers ENV-01 (installable, identical import everywhere), ENV-02 (reproducible venv via requirements.txt + documented CPU torch handling), and ENV-03 (the single `RuntimeConfig` with fp32 default, AMP-off-on-CPU, and the bf16-on-Pascal guard), plus the three split dataclasses (`RuntimeConfig`/`ModelConfig`/`TrainConfig`) per D-01/D-02.

Purpose: Nothing else in the phase can be built or tested until the package installs and the config contract exists. The torch-exclusion packaging decision (D-10) is load-bearing for Kaggle P100 survival. This plan also OWNS `pyproject.toml` entirely (incl. ruff + pytest config) so Wave-2 plans never edit it.
Output: `pyproject.toml`, `requirements.txt`, `.gitignore`, `src/personacore/{__init__,config}.py`, `tests/{conftest,test_config,test_package}.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-scaffolding-reproducible-environment/01-CONTEXT.md
@.planning/phases/01-scaffolding-reproducible-environment/01-RESEARCH.md
@.planning/research/ARCHITECTURE.md

<interfaces>
<!-- Contracts this plan CREATES for downstream plans/phases. Implement these signatures. -->
<!-- RuntimeConfig: dataclass. Fields: device:str (default "cuda" if available else "cpu"),
     amp:bool=False (fp32 default), amp_dtype:str="float16".
     __post_init__: if device=="cpu" -> amp=False; if amp_dtype=="bfloat16" and device is Pascal (capability major < 7) -> raise ValueError.
     Provide autocast() returning torch.autocast(device_type=..., dtype=getattr(torch, amp_dtype), enabled=amp).
     Helper _is_pascal(device:str)->bool using torch.cuda.get_device_capability(0)[0] < 7 (guarded by is_available + device.startswith("cuda")).
  ModelConfig: dataclass. Fields downstream phases will size the model with: vocab_size:int, block_size:int, n_layer:int, n_head:int, n_embd:int, dropout:float (sensible placeholder defaults; vocab_size locked by Phase 2).
  TrainConfig: dataclass. Fields: lr:float, batch_size:int, max_steps:int, warmup_steps:int, grad_clip:float, grad_accum_steps:int, weight_decay:float, seed:int (sensible defaults).
  Reference implementation skeleton: 01-RESEARCH.md § Code Examples §RuntimeConfig and §pyproject.toml. -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Dependency legitimacy gate (T-01-SC)</name>
  <read_first>
    - .planning/phases/01-scaffolding-reproducible-environment/01-RESEARCH.md § Package Legitimacy Audit
    - .planning/phases/01-scaffolding-reproducible-environment/01-RESEARCH.md § Standard Stack
  </read_first>
  <action>
    PAUSE before any pip install. Present the dependency set selected by research — torch, numpy, matplotlib, gradio (pin >=5,<6), pytest, ruff, tqdm — to the developer for legitimacy confirmation. slopcheck could NOT run during research (sandbox-blocked), so every package is [ASSUMED]. This single blocking-human gate covers the whole well-known set before Task 2's install runs. Do not auto-approve (workflow.auto_advance is ignored for legitimacy gates).
  </action>
  <what-built>
    Research selected the dependency set: torch, numpy, matplotlib, gradio (pin >=5,<6), pytest, ruff, tqdm. All [ASSUMED] (slopcheck unavailable). This gate covers the whole set before any install task runs.
  </what-built>
  <how-to-verify>
    1. Confirm each package is the genuine, authoritative project on PyPI:
       - torch -> pypi.org/project/torch (github.com/pytorch/pytorch)
       - numpy -> pypi.org/project/numpy (github.com/numpy/numpy)
       - matplotlib -> pypi.org/project/matplotlib (github.com/matplotlib/matplotlib)
       - gradio -> pypi.org/project/gradio (github.com/gradio-app/gradio)
       - pytest -> pypi.org/project/pytest (github.com/pytest-dev/pytest)
       - ruff -> pypi.org/project/ruff (github.com/astral-sh/ruff)
       - tqdm -> pypi.org/project/tqdm (github.com/tqdm/tqdm)
    2. Confirm none is a typosquat (exact spelling, expected maintainer, high download counts).
  </how-to-verify>
  <verify>
    <human-check>Developer confirms each of the seven packages on PyPI and types "approved" (or names a substitution/removal).</human-check>
  </verify>
  <acceptance_criteria>
    Developer types "approved" (or names a substitution/removal). This checkpoint is NOT auto-approvable — workflow.auto_advance is ignored. Do not run any pip install until approved.
  </acceptance_criteria>
  <done>Developer has confirmed the dependency set is legitimate (typed "approved") or specified substitutions; only then may Task 2 install.</done>
  <resume-signal>Type "approved" or describe required changes to the dependency set.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Installable package scaffold (pyproject + requirements + src-layout + gitignore)</name>
  <files>pyproject.toml, requirements.txt, .gitignore, src/personacore/__init__.py, tests/test_package.py</files>
  <read_first>
    - .planning/phases/01-scaffolding-reproducible-environment/01-RESEARCH.md § Code Examples §pyproject.toml and §GitHub Actions / Installation
    - .planning/phases/01-scaffolding-reproducible-environment/01-CONTEXT.md decisions D-09, D-10
    - .planning/research/ARCHITECTURE.md § Recommended Project Structure (Phase 1 ONLY subset)
  </read_first>
  <action>
    Create `pyproject.toml` (PEP 621): build-system = setuptools>=77 + setuptools.build_meta; `[project]` name="personacore", version="0.1.0", requires-python=">=3.10,<3.12" (Kaggle 3.11 parity — NOT the local 3.14 box, per D-10 and Environment Availability). `[project.dependencies]` lists `numpy~=2.4` ONLY — torch is deliberately ABSENT (D-10: never drag a cu128+ wheel onto Kaggle). `[project.optional-dependencies]`: `cpu = ["torch==2.7.*"]`, `demo = ["gradio>=5,<6", "matplotlib~=3.10"]`, `dev = ["pytest~=9.0", "ruff~=0.15"]`. Add `[tool.setuptools.packages.find] where=["src"]` for src-layout discovery. Add `[tool.pytest.ini_options]` with `testpaths=["tests"]`. Add a `[tool.ruff]` section (line-length 100 + a sane default rule set: pyflakes/pycodestyle/isort) so `ruff check`/`ruff format --check` pass on the committed source — this keeps `pyproject.toml` WHOLLY owned by this plan so Wave-2 Plan 03 (CI/Makefile) never edits it. Create `requirements.txt` consistent with pyproject (numpy, plus a commented CPU-torch install line using the CPU index `--extra-index-url https://download.pytorch.org/whl/cpu`, and an explicit comment: "On Kaggle never `pip install torch`"). Create `.gitignore` covering `*.egg-info/`, `__pycache__/`, `checkpoints/`, `*.pt`, `logs/`, `data/`, `.venv/`. Create thin `src/personacore/__init__.py` exposing `__version__ = "0.1.0"`. Create `tests/test_package.py` asserting `import personacore` succeeds and `personacore.__version__` is a non-empty string. Use the `[cpu]` extra mechanism — NO environment markers (markers cannot distinguish Kaggle-GPU from a Linux laptop; both are Linux).
  </action>
  <verify>
    <automated>python3.11 -m venv /tmp/pc-venv && /tmp/pc-venv/bin/pip install -e ".[cpu,dev]" --extra-index-url https://download.pytorch.org/whl/cpu && /tmp/pc-venv/bin/python -c "import personacore; print(personacore.__version__)" && /tmp/pc-venv/bin/pytest tests/test_package.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` contains `[tool.setuptools.packages.find]` with `where = ["src"]`.
    - `pyproject.toml` `[project].dependencies` does NOT contain `torch`; `[project.optional-dependencies].cpu` DOES contain a pinned torch.
    - `pyproject.toml` contains a `[tool.ruff]` section (Plan 03 must not edit pyproject).
    - `pip install -e ".[cpu,dev]"` into a clean Python 3.11 venv exits 0.
    - `python -c "import personacore"` exits 0 post-install.
    - `requirements.txt` exists and references no Kaggle torch install (comment present).
    - `.gitignore` contains `*.egg-info/` and `*.pt`.
  </acceptance_criteria>
  <done>Package installs cleanly in a Python 3.11 venv via the [cpu,dev] extras; `import personacore` resolves; torch is absent from core deps and present only as the [cpu] extra; pyproject carries ruff + pytest config.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Config layer — RuntimeConfig (bf16 guard) + ModelConfig + TrainConfig</name>
  <files>src/personacore/config.py, tests/conftest.py, tests/test_config.py</files>
  <read_first>
    - .planning/phases/01-scaffolding-reproducible-environment/01-RESEARCH.md § Code Examples §RuntimeConfig
    - .planning/phases/01-scaffolding-reproducible-environment/01-CONTEXT.md decisions D-01, D-02, D-03
    - .planning/research/ARCHITECTURE.md § Pattern 1 (Centralized Device + AMP Resolution)
    - pyproject.toml (the package this module ships in — written in Task 2)
  </read_first>
  <behavior>
    - Test 1 (test_fp32_default): `RuntimeConfig()` has `amp is False` (fp32 is the default per SUMMARY fp32-default decision).
    - Test 2 (test_amp_off_on_cpu): `RuntimeConfig(device="cpu", amp=True)` resolves `amp is False` after `__post_init__`.
    - Test 3 (test_bf16_raises_on_pascal): monkeypatch `torch.cuda.is_available`->True and `torch.cuda.get_device_capability`->(6,0); `RuntimeConfig(device="cuda", amp_dtype="bfloat16")` raises `ValueError`.
    - Test 4 (test_fp16_ok_on_pascal): same monkeypatch; `RuntimeConfig(device="cuda", amp_dtype="float16", amp=True)` does NOT raise.
    - Test 5 (test_configs_are_dataclasses): `ModelConfig()` and `TrainConfig()` instantiate with defaults and expose `vocab_size`/`block_size` (ModelConfig) and `lr`/`grad_accum_steps`/`seed` (TrainConfig).
  </behavior>
  <action>
    Create `src/personacore/config.py` with three `@dataclass` objects (D-02 split — keeps model/ pure of device concerns): `RuntimeConfig` (device, amp=False default, amp_dtype="float16"; `__post_init__` sets amp=False when device=="cpu" and RAISES `ValueError` when `amp_dtype=="bfloat16"` and the device is Pascal; an `autocast()` method returning `torch.autocast(device_type=device.split(":")[0], dtype=getattr(torch, amp_dtype), enabled=amp)`), `ModelConfig` (vocab_size, block_size, n_layer, n_head, n_embd, dropout — placeholder defaults; vocab_size is locked later by Phase 2, do not finalize), and `TrainConfig` (lr, batch_size, max_steps, warmup_steps, grad_clip, grad_accum_steps, weight_decay, seed). Implement the Pascal check via a module-level `_is_pascal(device)` helper: returns False unless `device.startswith("cuda")` and `torch.cuda.is_available()`, else `torch.cuda.get_device_capability(0)[0] < 7`. The bf16-guard error message must name Pascal/P100 and compute capability < 7.0 (per ENV-03 "clear error"). Do NOT implement the fp16-AMP+GradScaler training path here — that is Phase 3; Phase 1 only exposes the toggle + guard (per D-03 precision note). Create `tests/conftest.py` with a fixture/helper that monkeypatches device capability + availability for the Pascal-simulation tests (no GPU required). Create `tests/test_config.py` implementing the five behaviors above.
  </action>
  <verify>
    <automated>/tmp/pc-venv/bin/pytest tests/test_config.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_config.py` exits 0.
    - `RuntimeConfig()` has `amp is False`.
    - `RuntimeConfig(device="cpu", amp=True).amp is False`.
    - With `get_device_capability` monkeypatched to `(6,0)` and `is_available`->True, `RuntimeConfig(device="cuda", amp_dtype="bfloat16")` raises `ValueError` whose message mentions Pascal/P100.
    - `config.py` defines exactly three dataclasses: `RuntimeConfig`, `ModelConfig`, `TrainConfig`.
    - No argparse/CLI layer is added (D-04 deferred).
  </acceptance_criteria>
  <done>The three config dataclasses exist; fp32 is default; AMP auto-off on CPU; bf16 on a simulated Pascal device raises a clear ValueError; all five tests pass CPU-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pip install ← PyPI | Dependency resolution pulls third-party packages (supply-chain surface) |
| (none at runtime) | Phase 1 config code is an offline library — no network, no untrusted input, no secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | pip/PyPI installs (torch, numpy, gradio, pytest, ruff, matplotlib, tqdm) | mitigate | Task 1 blocking-human legitimacy gate — all deps are [ASSUMED] (slopcheck unavailable); developer verifies each on PyPI before first install. Not auto-approvable. |
| T-01-01 | Tampering | torch wheel on Kaggle P100 | mitigate | torch excluded from core `dependencies`; offered only as `[cpu]` extra; `requirements.txt` documents "never pip install torch on Kaggle" — prevents a cu128+ wheel silently bricking the P100. |
| T-01-02 | Denial of Service | bf16 selected on Pascal | mitigate | `RuntimeConfig.__post_init__` RAISES (not warns) on bf16+Pascal — fails loud before any run that would crash/fall back on hardware lacking bf16. |
| T-01-03 | Information Disclosure | committed build artifacts / future tokens | accept (low) | `.gitignore` covers `*.egg-info/`, `*.pt`, `logs/`, `data/`. No secrets exist in Phase 1; Kaggle token handling is a later-phase concern. |
</threat_model>

<verification>
- Clean Python 3.11 venv: `pip install -e ".[cpu,dev]"` exits 0; `import personacore` resolves.
- `pytest tests/test_config.py tests/test_package.py -q` green (CPU-only, no GPU).
- `pyproject.toml`: torch absent from core deps, present in `[cpu]` extra; `where=["src"]` + `[tool.ruff]` present.
- bf16-on-Pascal raises `ValueError`; fp32 is default; AMP off on CPU.
</verification>

<success_criteria>
- ENV-01: `pip install -e .` succeeds; `import personacore` resolves identically post-install (src-layout enforces install-parity).
- ENV-02: `requirements.txt` + documented venv exist; CPU torch handled via `[cpu]` extra/index; Kaggle "no torch install" documented.
- ENV-03: single `RuntimeConfig` — fp32 default, AMP off on CPU, bf16 guarded to error on Pascal/P100.
- D-01/D-02 honored: code-first dataclasses, three-way split, no YAML, no CLI.
</success_criteria>

<output>
Create `.planning/phases/01-scaffolding-reproducible-environment/01-01-SUMMARY.md` when done.
</output>
