---
phase: 01-scaffolding-reproducible-environment
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - Makefile
  - .github/workflows/ci.yml
  - CLAUDE.md
autonomous: true
requirements: [ENV-06, ENV-01, ENV-02]

must_haves:
  truths:
    - "make install / make test / make lint / make format run the documented commands"
    - "GitHub Actions CI installs .[cpu,dev] on Python 3.11 and runs ruff + pytest with NO GPU and NO training"
    - "CI pins python-version: \"3.11\" (Kaggle parity — NOT the local 3.14 dev box)"
    - "CLAUDE.md documents the project structure, the Kaggle-train / laptop-CPU-infer workflow, the 3.11 venv mandate, and the never-pip-install-torch-on-Kaggle rule, without clobbering the GSD-generated sections"
  artifacts:
    - path: "Makefile"
      provides: "install/test/lint/format targets (D-12)"
      contains: "install:"
    - path: ".github/workflows/ci.yml"
      provides: "CPU-only CI: setup-python 3.11 -> install .[cpu,dev] -> ruff -> pytest"
      contains: "python-version"
    - path: "CLAUDE.md"
      provides: "ENV-06 docs: structure + Kaggle/local workflow + 3.11 venv + torch rule, appended around GSD markers"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "Python 3.11"
      via: "actions/setup-python python-version pin"
      pattern: "3\\.11"
    - from: ".github/workflows/ci.yml"
      to: "pyproject [cpu,dev] extras"
      via: "pip install -e .[cpu,dev] --extra-index-url cpu wheel"
      pattern: "cpu,dev"
---

<objective>
Wire the dev-tooling and reproducibility documentation layer: a `Makefile` (install/test/lint/format), a minimal CPU-only GitHub Actions CI (ruff + pytest, Python 3.11 pinned, no GPU, no training), and the ENV-06 `CLAUDE.md` extension documenting project structure + the Kaggle-train / laptop-CPU-infer workflow. Delivers ENV-06 and provides the automated install-parity + green-suite gate that re-validates ENV-01/ENV-02 on a fresh runner.

Purpose: CI on a public repo is a strong portfolio signal AND the mechanism that proves `pip install -e .` works on a clean machine at the Kaggle-target Python 3.11 (the local dev box is 3.14 — must not be the validation target, per the critical research flag). This plan does NOT edit `pyproject.toml` — ruff/pytest config is owned by Plan 01, so Plan 03 runs parallel to Plan 02 in Wave 2 with zero file overlap.
Output: `Makefile`, `.github/workflows/ci.yml`, extended `CLAUDE.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-scaffolding-reproducible-environment/01-CONTEXT.md
@.planning/phases/01-scaffolding-reproducible-environment/01-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Consumes the package contract from 01-01 (pyproject.toml extras: [cpu], [dev]; ruff/pytest config). -->
<!-- Reference impls: 01-RESEARCH.md § Code Examples §GitHub Actions CI and §Makefile. -->
<!-- CLAUDE.md has GSD-managed marker blocks (e.g. <!-- GSD:project-start --> ... <!-- GSD:workflow-end -->).
     ENV-06 content must be ADDED (new section) without overwriting GSD-managed regions. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Makefile + CPU-only GitHub Actions CI (Python 3.11)</name>
  <files>Makefile, .github/workflows/ci.yml</files>
  <read_first>
    - .planning/phases/01-scaffolding-reproducible-environment/01-RESEARCH.md § Code Examples (§GitHub Actions CI, §Makefile) and § Environment Availability (3.14 vs 3.11)
    - .planning/phases/01-scaffolding-reproducible-environment/01-CONTEXT.md decision D-12
    - pyproject.toml (the [cpu]/[dev] extras + ruff config this tooling drives — from 01-01; do NOT edit it)
  </read_first>
  <action>
    Create `Makefile` with `.PHONY` targets: `install` -> `pip install -e ".[cpu,dev]" --extra-index-url https://download.pytorch.org/whl/cpu`; `test` -> `pytest -q`; `lint` -> `ruff check . && ruff format --check .`; `format` -> `ruff format . && ruff check --fix .`. Create `.github/workflows/ci.yml`: trigger `on: [push, pull_request]`; one job `test` on `ubuntu-latest`; steps = `actions/checkout@v4`, `actions/setup-python@v5` with `python-version: "3.11"` (MANDATORY — match the Kaggle target, NOT the local 3.14 box per the Environment Availability flag), then `pip install --upgrade pip`, `pip install -e ".[cpu,dev]" --extra-index-url https://download.pytorch.org/whl/cpu`, then a Lint step `ruff check . && ruff format --check .`, then a Test step `pytest -q`. CI must be CPU-only: NO GPU runner, NO training step, NO torch reinstall beyond the CPU wheel. Verify current action major tags (`checkout@v4`, `setup-python@v5`) are still current at execution time (flagged [ASSUMED] in research); bump if needed. Do NOT modify `pyproject.toml` — its `[tool.ruff]` section is owned by Plan 01; if `ruff check .` fails because of a missing rule, fix the offending source file, not pyproject.
  </action>
  <verify>
    <automated>/tmp/pc-venv/bin/ruff check . && /tmp/pc-venv/bin/ruff format --check . && /tmp/pc-venv/bin/pytest -q && python3 -c "import yaml; d=yaml.safe_load(open('.github/workflows/ci.yml')); steps=d['jobs']['test']['steps']; assert any(s.get('with',{}).get('python-version')=='3.11' for s in steps), 'CI must pin python 3.11'; print('CI pins 3.11 OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `make install`, `make test`, `make lint`, `make format` invoke the documented commands (grep the Makefile for each target).
    - `.github/workflows/ci.yml` pins `python-version: "3.11"`.
    - CI installs `.[cpu,dev]` with the CPU torch index and runs `ruff` + `pytest` — no GPU, no training step.
    - `ruff check .` and `ruff format --check .` pass on the repo.
    - Full `pytest -q` is green locally.
    - `pyproject.toml` is NOT in this plan's diff (owned by Plan 01).
  </acceptance_criteria>
  <done>Makefile drives install/test/lint/format; the CI workflow installs the CPU extras on Python 3.11 and runs ruff + pytest with no GPU and no training; the whole suite is lint-clean and green; pyproject untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Extend CLAUDE.md — structure + Kaggle/local workflow (ENV-06)</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (existing GSD-generated file — must NOT clobber the marker-bounded GSD sections)
    - .planning/phases/01-scaffolding-reproducible-environment/01-RESEARCH.md § Project Constraints (CLAUDE.md is a Phase-1 deliverable) and § Architecture Patterns (Recommended Project Structure)
    - .planning/phases/01-scaffolding-reproducible-environment/01-CONTEXT.md decisions D-05, D-06, D-08, D-10
  </read_first>
  <action>
    Append a new ENV-06 documentation section to `CLAUDE.md` (do NOT edit inside the `<!-- GSD:* -->` marker blocks — add the new content outside/after them so GSD guidance is preserved). Document: (1) the Phase-1 project structure (`src/personacore/` modules: config, checkpoint, seeding, provenance, preflight, logging; `scripts/`, `tests/`, `pyproject.toml`, `requirements.txt`, `Makefile`, `.github/workflows/ci.yml`) — note future module dirs are added by their own phases (D-11, no empty stubs); (2) the local laptop CPU setup: a **Python 3.11 virtual environment** is MANDATORY (the dev box runs 3.14 which is NOT a supported target), `python3.11 -m venv .venv`, then `pip install -e ".[cpu,dev]" --extra-index-url https://download.pytorch.org/whl/cpu`, then `make test`; (3) the Kaggle train workflow: `git clone` the public repo + `pip install -e .` (NO extras), and the explicit rule **never `pip install torch` on Kaggle** (D-10 — Kaggle's preinstalled torch is the Pascal-compatible wheel); record the commit SHA into the checkpoint (D-06), with the option to clone a pinned tag/SHA for the final pretrain run; (4) the two Kaggle run modes (D-08): headless "Save & Run All / Commit" for the long pretrain (checkpoint to `/kaggle/working`, persist to a Dataset) vs interactive sessions for short calibration; (5) the reproducibility discipline (seeds + git SHA + config-in-checkpoint; optional `strict=True` determinism toggle and its speed cost). Keep it concise and accurate; this is documentation, not code.
  </action>
  <verify>
    <automated>grep -q "3.11" CLAUDE.md && grep -qi "never.*pip install torch" CLAUDE.md && grep -q "GSD:project-start" CLAUDE.md && grep -qi "pip install -e" CLAUDE.md && echo "CLAUDE.md ENV-06 content present and GSD markers preserved"</automated>
  </verify>
  <acceptance_criteria>
    - `CLAUDE.md` documents the Phase-1 project structure.
    - `CLAUDE.md` mandates a Python 3.11 venv for local dev and states the dev box's 3.14 is not a target.
    - `CLAUDE.md` states the "never `pip install torch` on Kaggle" rule and the `git clone` + `pip install -e .` Kaggle workflow.
    - `CLAUDE.md` documents both run modes (headless commit vs interactive) and the SHA-recording + pin option.
    - The original GSD marker blocks (`<!-- GSD:project-start -->` ... `<!-- GSD:workflow-end -->`) remain intact (grep confirms `GSD:project-start` still present).
  </acceptance_criteria>
  <done>CLAUDE.md documents structure + the Kaggle-train / laptop-CPU-infer workflow + the 3.11 venv mandate + the no-torch-on-Kaggle rule, reproducible from a clean venv, with the GSD-generated sections preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner ← PyPI / GitHub Actions | CI pulls dependencies + third-party actions on a hosted runner |
| (none runtime) | Makefile/CI/docs are dev-time only — no runtime, no network, no secrets handled in Phase 1 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08 | Tampering | third-party GitHub Actions (checkout, setup-python) | accept (low) | Official, widely-used first-party `actions/*` pinned to major tags; public repo, no secrets exposed to CI; no deploy step. |
| T-01-09 | Denial of Service | CI validating on wrong Python (3.14 not 3.11) | mitigate | CI pins `python-version: "3.11"` (Kaggle parity); a verify step asserts the pin so install-parity (ENV-01) is validated against the real target, not the dev 3.14 box. |
| T-01-10 | Information Disclosure | accidental secret/token commit | accept (low) | No secrets in Phase 1; `.gitignore` (01-01) covers artifacts; CLAUDE.md documents using Kaggle Secrets (not committed) for later phases. |
</threat_model>

<verification>
- `ruff check . && ruff format --check .` pass; `pytest -q` green.
- `.github/workflows/ci.yml` pins Python 3.11, installs `.[cpu,dev]`, runs ruff + pytest, no GPU/training.
- `CLAUDE.md` contains the structure, 3.11-venv mandate, no-torch-on-Kaggle rule, both run modes — GSD markers intact.
- Manual (after push): GitHub Actions run is green on a fresh runner (re-validates ENV-01/02 install-parity).
</verification>

<success_criteria>
- ENV-06: `CLAUDE.md` (+ `requirements.txt` from 01-01) document structure + the Kaggle/local workflow, reproducible from a clean Python 3.11 venv.
- ENV-01/ENV-02 (re-validated): CI proves `pip install -e ".[cpu,dev]"` + import + green suite on a fresh Python 3.11 runner.
- D-12 honored: ruff + pytest + Makefile + minimal CPU-only CI. D-11 honored: no empty stub dirs documented. Local 3.14 is never the validation target.
</success_criteria>

<output>
Create `.planning/phases/01-scaffolding-reproducible-environment/01-03-SUMMARY.md` when done.
</output>
