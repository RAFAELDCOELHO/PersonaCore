---
phase: 05-tinystories-pretraining
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - tests/test_mps_smoke.py
  - tests/test_resume_memmap.py
  - tests/test_best_ckpt.py
  - src/personacore/training/loop.py
  - scripts/pretrain_tinystories.py
autonomous: false
requirements: [PRE-02, PRE-03]
user_setup: []

must_haves:
  truths:
    - "A finite-loss + overfit-one-batch smoke runs the real GPT on device=mps and proves loss stays finite and drives toward zero (D-01a sanity gate)"
    - "The memmap data source resumes bit-for-bit across a simulated kill (CPU determinism oracle)"
    - "best.pt holds the run's LOWEST val loss (not the final step), and perplexity = exp(best_val_loss) is recoverable from it"
    - "A calibration smoke MEASURES tokens/sec + the largest stable batch + a non-diverging LR on the real machine and writes the chosen run config"
    - "The long M3/MPS run produces best.pt, a val-loss plateau, a recorded perplexity, run.csv curves, and curated coherent TinyStories-register samples"
  artifacts:
    - path: "src/personacore/training/loop.py"
      provides: "4 additive seams: memmap data branch, estimate_loss path support, best-val-loss tracking + best.pt, periodic latest.pt + sample hook"
      contains: "best_val_loss"
    - path: "scripts/pretrain_tinystories.py"
      provides: "thin no-CLI run entry: preflight LOCAL data/*.bin, build configs, point train() at the memmaps, sample+decode"
      contains: "preflight_device"
    - path: "tests/test_mps_smoke.py"
      provides: "skipif-guarded MPS finite-loss + overfit smoke (PRE-02 / D-01a)"
      contains: "torch.backends.mps.is_available"
    - path: "tests/test_resume_memmap.py"
      provides: "bit-for-bit resume on the memmap data source (PRE-02)"
      contains: "resume_from"
    - path: "tests/test_best_ckpt.py"
      provides: "best.pt lowest-val tracking + perplexity (PRE-03)"
      contains: "val_loss"
  key_links:
    - from: "scripts/pretrain_tinystories.py"
      to: "data/train.bin / data/val.bin"
      via: "train(train_bin=..., val_bin=...) memmap seam"
      pattern: "train_bin"
    - from: "scripts/pretrain_tinystories.py"
      to: "preflight_device(strict=True)"
      via: "long-run device gate on LOCAL paths (not /kaggle/input)"
      pattern: "preflight_device"
    - from: "src/personacore/training/loop.py"
      to: "checkpoints/best.pt"
      via: "save_checkpoint when val_loss < best_val_loss (D-08)"
      pattern: "best_val_loss"
    - from: "src/personacore/training/loop.py::estimate_loss"
      to: "get_batch_memmap"
      via: "val_ids-as-path branch"
      pattern: "get_batch_memmap"
---

<objective>
The run slice: weave four additive seams into the proven `train(...)` loop (memmap data branch, `estimate_loss` path support, best-val-loss `best.pt` tracking, periodic `latest.pt` + qualitative sample hook), add the thin run-entry script that preflights the LOCAL memmaps, then run the MPS sanity gate → calibration smoke → resumable multi-hour local M3/MPS run to coherence. After this plan a real user has a trained `best.pt` checkpoint with recorded perplexity, `run.csv` curves, and curated coherent samples — the visceral proof the from-scratch LM works.

Purpose: PRE-02 (pretrain to coherence on local M3/MPS, resumable) + PRE-03 (record perplexity + curves). The loop seams MUST be additive — they must not touch the load-bearing AMP/accum/clip ordering in `_optimizer_step` or the resume RNG-restore contract.
Output: the 4 loop seams, `scripts/pretrain_tinystories.py`, 3 new tests (MPS smoke, resume-memmap, best-ckpt), the calibrated `TrainConfig`, and the shipped `checkpoints/best.pt` + `logs/run.csv` + curated samples.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-tinystories-pretraining/05-CONTEXT.md
@.planning/phases/05-tinystories-pretraining/05-RESEARCH.md
@.planning/phases/05-tinystories-pretraining/05-PATTERNS.md
@.planning/phases/05-tinystories-pretraining/05-VALIDATION.md
@.planning/phases/05-tinystories-pretraining/05-01-PLAN.md

<interfaces>
<!-- Exact contracts extracted from the codebase — use directly, no exploration. -->

From src/personacore/training/loop.py — the `train(...)` signature (kwargs the script passes; new kwargs are additive):
```
def train(*, train_config, runtime_config=None, model=None, model_config=None,
          corpus_path=None, eos_id=8184, fixed_batch=None, scaler=None,
          resume_from=None, checkpoint_path=None, log_path=None,
          max_steps_override=None, eval_interval=1, return_final_loss=False): ...
```
- 3-mode data branch lives at lines 197-231 (fixed_batch / corpus_path / synthetic). Add a 4th: memmap (train_bin/val_bin kwargs → get_batch_memmap).
- estimate_loss(model, val_ids, train_cfg, model_cfg, device, iters=20) at lines 60-81 — wraps draws in `rng=_rng_state()` … `_restore_rng(rng)` (KEEP) and calls `get_batch(val_ids, ...)`. Memmap path: when val source is a `.bin` PATH, call get_batch_memmap instead.
- best-val seam goes in the eval branch (lines 259-278, where `val_loss` is computed).
- periodic latest.pt + sample seam: the current loop only saves at end-of-call (lines 283-295); add an in-loop periodic save every K steps and a sample() print every S steps. sample(model, idx, max_new_tokens, temperature=1.0) is at lines 84-98.
- _optimizer_step (lines 101-126) and the resume RNG-restore (lines 233-239) are LOAD-BEARING — do NOT modify.

From src/personacore/checkpoint.py — save_checkpoint already serializes val_loss= and **extra (no format change for best.pt):
```
save_checkpoint(path, *, model, optimizer, scheduler, step, model_config, train_config,
                git_sha, scaler=None, val_loss=None, **extra)
```
load: `torch.load(path, weights_only=False)` (own trusted file).

From src/personacore/preflight.py — `preflight_device(strict=True)` resolves CUDA-P100→MPS→CPU; on M3 returns {"device":"mps",...} and never raises. NOTE: do NOT copy preflight_demo.py's /kaggle/input DATA_DIR constant — preflight the LOCAL data/*.bin paths.

From src/personacore/config.py — RuntimeConfig() resolves device="mps" on M3 and forces amp=False there. ModelConfig(): 6L/6H/384d, block_size=256, vocab 8192, eos 8184 (LOCKED). TrainConfig: lr=3e-4 default, batch_size, grad_accum_steps, warmup_steps, max_steps, grad_clip, weight_decay, seed.

From tests/test_gpt_overfit.py + tests/test_resume_curve.py — the overfit gate (cfg=TrainConfig(lr=1e-3, warmup_steps=0, max_steps=300, batch_size=4); fixed_batch=(randint, randint); assert final loss < math.log(8192)-2) and the resume idiom (seed_everything(1234) → ref run → half+checkpoint → fresh model → resume_from → assert |resumed-ref|<1e-6). GPT is imported from personacore.model.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — PRE-02/PRE-03 RED tests (MPS smoke, resume-memmap, best-ckpt)</name>
  <read_first>
    - tests/test_gpt_overfit.py (the overfit-one-batch gate to copy verbatim, changing only device via RuntimeConfig + adding the finite-loss assert)
    - tests/test_preflight.py (the torch.backends.mps device convention for the skipif guard)
    - tests/test_resume_curve.py (the resume idiom lines 38-79 + the torch.load(ckpt, weights_only=False) blob-inspection idiom lines 183-184)
    - src/personacore/checkpoint.py (val_loss field at line 64 — best.pt inspection target)
    - .planning/phases/05-tinystories-pretraining/05-PATTERNS.md §test_mps_smoke / §test_resume_memmap / §test_best_ckpt (exact structures)
    - .planning/phases/05-tinystories-pretraining/05-VALIDATION.md (exact node-ids)
  </read_first>
  <action>
    Create three test files (RED until Task 2's seams land):
      - `tests/test_mps_smoke.py` — module-level `pytestmark = pytest.mark.skipif(not torch.backends.mps.is_available(), reason="MPS not available (CPU-only CI)")`. Copy `test_gpt_overfit.py`'s overfit-one-batch body VERBATIM (real `GPT(ModelConfig())`, `fixed_batch=(randint, randint)`, `cfg=TrainConfig(lr=1e-3, warmup_steps=0, max_steps=300, batch_size=4)`, `train(..., return_final_loss=True)`), letting `RuntimeConfig()` auto-resolve to MPS (no manual `device=` — single-source-of-truth). Add `test_overfit_mps` (asserts `final_loss < math.log(8192) - 2.0`) and a finite-loss assertion `assert torch.isfinite(torch.tensor(final_loss))` (D-01a). This is the ONLY MPS-touching test; the whole module is skipif-guarded so CPU CI cleanly SKIPS (not ERRORS).
      - `tests/test_resume_memmap.py` — CPU-only; extend `test_resume_curve.py::test_resume_identical_trajectory` to the memmap source: build a small train/val `.bin` in `tmp_path` from the Plan-01 fixture, then run the same `seed_everything(1234)` → ref run → run-half + `checkpoint_path` → KILL → fresh `GPT(ModelConfig())` → `resume_from` structure, passing the new `train_bin=`/`val_bin=` kwargs instead of `corpus_path`. Assert `abs(float(resumed) - float(ref)) < 1e-6` and the param tensor matches within `atol=1e-6`. Use a tiny `TrainConfig(lr=1e-2, warmup_steps=2, max_steps=6, batch_size=4)`.
      - `tests/test_best_ckpt.py` — CPU-only; drive a short `train(...)` on the memmap source (or fixture) with a `best_checkpoint_path=tmp_path/"best.pt"` and an `eval_interval` such that val loss is observed multiple times; assert via `blob = torch.load(best_path, weights_only=False)` that `blob["val_loss"]` equals the LOWEST observed val loss (not the final step's), and assert `math.exp(blob["val_loss"])` is the recoverable perplexity (PRE-03). Engineer the run so val loss dips then rises (e.g. enough steps that the tiny-corpus val loss is non-monotonic) so the test proves best.pt captured the dip.
    Use the exact node-ids in 05-VALIDATION.md. Do NOT weaken assertions.
  </action>
  <verify>
    <automated>cd $(git rev-parse --show-toplevel) && python -m pytest tests/test_resume_memmap.py tests/test_best_ckpt.py -x 2>&1 | grep -E "TypeError|got an unexpected keyword|ImportError|FAILED|error" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_mps_smoke.py` exists with a module-level `skipif(not torch.backends.mps.is_available())` and node-ids including `test_overfit_mps`; on CPU it SKIPS (collection shows skipped, not errored).
    - `tests/test_resume_memmap.py` and `tests/test_best_ckpt.py` exist, CPU-only, and FAIL only because the new `train(...)` memmap/best seams (`train_bin`/`val_bin`/`best_checkpoint_path`) don't exist yet — RED for the right reason.
    - best-ckpt test asserts the LOWEST (not final) val loss and `exp(val_loss)` perplexity.
    - No tautological assertions.
  </acceptance_criteria>
  <done>Three RED PRE-02/PRE-03 tests committed; MPS smoke skips cleanly on CPU; resume + best tests fail only on the not-yet-added seams.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Four additive loop.py seams — turn resume/best tests green</name>
  <read_first>
    - src/personacore/training/loop.py (the WHOLE file — the 3-mode data branch lines 197-231, estimate_loss lines 60-81, eval branch lines 259-278, end-of-call save lines 283-295; the load-bearing _optimizer_step 101-126 and resume 233-239 must NOT change)
    - src/personacore/training/data.py (get_batch_memmap added in Plan 01)
    - src/personacore/checkpoint.py (save_checkpoint val_loss=/**extra reuse — no format change)
    - .planning/phases/05-tinystories-pretraining/05-PATTERNS.md §loop.py (the 4 seams, with the exact insertion points + the "do NOT touch AMP/accum/resume ordering" constraint)
    - .planning/phases/05-tinystories-pretraining/05-RESEARCH.md §Pattern 3 / §Pattern 4 (estimate_loss path support + best-val/sample hooks)
  </read_first>
  <behavior>
    - Seam 1 (memmap data branch): new `train_bin`/`val_bin` kwargs; when set, `batch_fn` calls `get_batch_memmap(train_bin, batch_size, block_size, device)` and the val source carries the `val_bin` PATH. Mirrors the existing corpus_path branch; the other 3 modes are unchanged.
    - Seam 2 (estimate_loss path support): when the val source is a `.bin` path, estimate_loss calls `get_batch_memmap(val_bin, ...)`; the `rng=_rng_state()`/`_restore_rng(rng)` snapshot wrapper is preserved verbatim (resume-equality contract).
    - Seam 3 (best-val tracking, D-08): `best_val_loss=float("inf")` init; in the eval branch, when `best_checkpoint_path` is set and `val_loss < best_val_loss`, update best_val_loss and `save_checkpoint(best_checkpoint_path, ..., val_loss=val_loss)` reusing the end-of-call save args verbatim.
    - Seam 4 (periodic latest.pt + sample hook): every `checkpoint_interval` (K) steps, fire the existing `save_checkpoint(checkpoint_path, ...)` IN-loop (kill-survivability, Pitfall 5); every `sample_interval` (S) steps, run `sample(model, seed_idx, max_new_tokens)` and print the decoded text (D-06). Both optional/guarded by their kwargs; default off (None) so existing tests/behavior are unchanged.
    - _optimizer_step AMP/accum/clip ordering and the resume RNG-restore block are byte-for-byte unchanged.
  </behavior>
  <action>
    Add the four additive seams to `src/personacore/training/loop.py`, all guarded by new optional kwargs (`train_bin=None`, `val_bin=None`, `best_checkpoint_path=None`, `checkpoint_interval=None`, `sample_interval=None`, `sample_prompt=None`, plus a tokenizer/decode handle for the sample print) so the existing 3 data modes and all current tests pass unchanged:
      - Seam 1: add a memmap branch to the data-source `if/elif` (mirror the `corpus_path` branch at 212-218) that sets `batch_fn` from `get_batch_memmap(train_bin, ...)` and sets `val_ids = val_bin` (the path).
      - Seam 2: branch `estimate_loss`'s inner draw on whether `val_ids` is a path/str vs an array, calling `get_batch_memmap(val_ids, ...)` for the path case; KEEP the `_rng_state()`/`_restore_rng()` wrapper exactly.
      - Seam 3: init `best_val_loss = float("inf")` before the loop; inside the eval branch (after `val_loss` is computed) add the `if best_checkpoint_path is not None and val_loss < best_val_loss:` save using `save_checkpoint(best_checkpoint_path, model=..., optimizer=..., scheduler=..., scaler=scaler, step=step, model_config=model_cfg, train_config=train_config, git_sha=git_sha(), val_loss=val_loss)`.
      - Seam 4: inside the `while` loop, `if checkpoint_path is not None and checkpoint_interval and step % checkpoint_interval == 0:` call the same `save_checkpoint(checkpoint_path, ...)` used at end-of-call; `if sample_interval and step % sample_interval == 0:` run `sample(...)` and `print(tok.decode(...))` (use the minimal `sample()` already in loop.py — NO Phase-6 generate()).
    Do NOT introduce a GradScaler/fp16 path (RuntimeConfig forces amp=False on MPS). Do NOT modify `_optimizer_step` or the resume RNG-restore block.
  </action>
  <verify>
    <automated>cd $(git rev-parse --show-toplevel) && python -m pytest tests/test_resume_memmap.py tests/test_best_ckpt.py -x && make test</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_resume_memmap.py` and `tests/test_best_ckpt.py` are GREEN.
    - `make test` fully green (all pre-existing CPU tests pass — no regression; the AMP/accum/resume seams unchanged).
    - `git diff src/personacore/training/loop.py` shows NO change inside `_optimizer_step` (lines around the scale/unscale_/clip/step/update/scheduler ordering) and NO change to the resume `load_checkpoint` block.
    - `grep -c "GradScaler\|fp16\|float16" src/personacore/training/loop.py` shows no NEW fp16/scaler path added (only the pre-existing scaler plumbing).
    - estimate_loss still contains both `_rng_state()` and `_restore_rng(`.
  </acceptance_criteria>
  <done>The 4 additive seams land; resume-memmap + best-ckpt tests green; full CPU suite green; load-bearing ordering provably untouched.</done>
</task>

<task type="auto">
  <name>Task 3: pretrain_tinystories.py — thin no-CLI run entry (preflight LOCAL memmaps)</name>
  <read_first>
    - scripts/train_bigram.py (the canonical train-entry idiom: seed → configs → train(...) → sample → decode → print; _REPO_ROOT-relative paths, NO argparse)
    - src/personacore/preflight.py (preflight_device(strict=True) — the long-run gate)
    - scripts/preflight_demo.py (the DATA_DIR=/kaggle/input constant to AVOID copying — preflight LOCAL data/*.bin instead)
    - src/personacore/training/loop.py (the new train(...) kwargs from Task 2: train_bin/val_bin/best_checkpoint_path/checkpoint_interval/sample_interval)
    - .planning/phases/05-tinystories-pretraining/05-PATTERNS.md §pretrain_tinystories.py
  </read_first>
  <action>
    Create `scripts/pretrain_tinystories.py` as a thin no-CLI entry (mirror `train_bigram.py`). `_REPO_ROOT`-relative constants for `TRAIN_BIN=data/train.bin`, `VAL_BIN=data/val.bin`, `TOKENIZER_PATH`, `LOG_PATH=logs/run.csv`, `CKPT_PATH=checkpoints/latest.pt`, `BEST_PATH=checkpoints/best.pt`. In `main()`: `preflight_device(strict=True)` to assert MPS is the active device BEFORE the long run, and assert the LOCAL `TRAIN_BIN`/`VAL_BIN` exist (do NOT reference `/kaggle/input`). `seed_everything(TrainConfig().seed)` (fresh-run only). `runtime = RuntimeConfig()` (auto-MPS), `model_cfg = ModelConfig()` (LOCKED 6L/6H/384d), `model = GPT(model_cfg)` (the real GPT, not the bigram). Build a `TrainConfig` from the CALIBRATED hyperparameters (lr/batch_size/grad_accum_steps/max_steps/eval_interval) — leave these as clearly-marked constants the calibration task (Task 4) fills in; do not invent final numbers here. Call `train(train_config=cfg, runtime_config=runtime, model=model, model_config=model_cfg, train_bin=TRAIN_BIN, val_bin=VAL_BIN, log_path=LOG_PATH, checkpoint_path=CKPT_PATH, best_checkpoint_path=BEST_PATH, checkpoint_interval=K, sample_interval=S, resume_from=...)` where `resume_from` points at `latest.pt` iff it already exists (kill-survivable restart). After training, load `best.pt`, `sample()` + `tok.decode(...)` a few seeds, print decoded stories + `perplexity = exp(best_val_loss)`. `mkdir(parents=True, exist_ok=True)` the logs/checkpoints dirs. Guard with `__main__`.
    Set `PYTORCH_ENABLE_MPS_FALLBACK=1` in the script's environment guidance (a module-level note or os.environ.setdefault) so an uncovered MPS op falls back to CPU rather than crashing the multi-hour run.
  </action>
  <verify>
    <automated>cd $(git rev-parse --show-toplevel) && python -c "import ast; ast.parse(open('scripts/pretrain_tinystories.py').read()); print('syntax-ok')" && grep -c "argparse" scripts/pretrain_tinystories.py | grep -qx 0 && grep -q "preflight_device" scripts/pretrain_tinystories.py && grep -q "train_bin" scripts/pretrain_tinystories.py && grep -qv "/kaggle/input" scripts/pretrain_tinystories.py && echo "static-checks-ok"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/pretrain_tinystories.py` parses; contains NO `argparse`; calls `preflight_device(`; passes `train_bin=`/`val_bin=`/`best_checkpoint_path=` to `train(...)`.
    - It references LOCAL `data/train.bin`/`data/val.bin` and does NOT contain `/kaggle/input`.
    - It builds `GPT(ModelConfig())` (the real model), not `BigramLanguageModel`.
    - It sets `PYTORCH_ENABLE_MPS_FALLBACK=1` (or documents it as a required env) and resumes from `latest.pt` when present.
  </acceptance_criteria>
  <done>The thin run-entry script exists, preflights LOCAL memmaps, wires the new memmap+best seams, and resumes from latest.pt; static checks pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: MPS sanity gate + calibration smoke (D-01a, blocking before the long run)</name>
  <read_first>
    - .planning/phases/05-tinystories-pretraining/05-RESEARCH.md §"Cheapest reliable MPS sanity gate" + §"Calibration / Smoke Protocol" (the 4-step gate + Stage 0-3 measurement protocol)
    - tests/test_mps_smoke.py (the finite-loss + overfit gate to run on the real M3)
    - scripts/pretrain_tinystories.py (the entry the smoke runs through, with a tiny max_steps override)
  </read_first>
  <what-built>
    The MPS smoke test + the run entry script are ready; this checkpoint runs the D-01a sanity gate and the calibration measurement on the real M3 so the long run is sized from MEASURED numbers, not invented ones. Calibration is a MEASUREMENT task — it MUST NOT hardcode LR/batch/steps.
  </what-built>
  <how-to-verify>
    On the M3, inside the Python 3.11 venv, with `PYTORCH_ENABLE_MPS_FALLBACK=1`:
    1. Stage 0 — `python -m pytest tests/test_mps_smoke.py -x`: confirm it RUNS (not skips) on the M3 and PASSES — overfit-one-batch on `device=mps` drives loss far below `ln(8192)-2` and `isfinite(loss)` holds. If it NaNs or fails to overfit while CPU passes, STOP and fall back to `device=cpu` for the run.
    2. Stage 1 — throughput probe (~200-500 steps on real `data/train.bin` via a small `max_steps` override): record ms/iter and tokens/sec = batch_size×block_size/iter_time; sweep `batch_size ∈ {8,16,32,...}` to find the largest stable (no OOM, finite loss) batch; confirm val loss decreases and a sampled story is non-garbage.
    3. Stage 2 — LR calibration: short ~300-step runs over `{3e-4, 5e-4, 1e-3}`; pick the highest LR with smooth (non-diverging) loss; set `grad_accum_steps` so the effective batch is healthy.
    4. Stage 3 — size the run: from measured tokens/sec estimate wall-clock/epoch (~500M tokens / tokens-per-sec); set a generous `max_steps` (quality-first, D-04), `eval_interval`, checkpoint-every-`K`, sample-every-`S` so a kill loses ≤ a few minutes.
    Write the chosen `lr / batch_size / grad_accum_steps / eval_interval / K / S / measured tokens-per-sec` into the calibration constants in `scripts/pretrain_tinystories.py` (and note them in the SUMMARY).
  </how-to-verify>
  <resume-signal>Type "calibrated" with the measured lr/batch/grad_accum/steps/tokens-per-sec, or describe a sanity-gate failure (NaN / no-overfit / OOM) and the chosen fallback (e.g. device=cpu / smaller batch).</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 5: Long resumable M3/MPS pretraining run to coherence (PRE-02/PRE-03)</name>
  <read_first>
    - scripts/pretrain_tinystories.py (now carrying the calibrated config from Task 4)
    - .planning/phases/05-tinystories-pretraining/05-RESEARCH.md §"Token Budget & Expected Val Loss" + §"Best-Val-Loss Tracking & Perplexity" (the val-loss ballpark ~1.5-1.8 / ppl ~5-6 coherence region; perplexity = exp(best_val_loss))
    - .planning/phases/05-tinystories-pretraining/05-CONTEXT.md (D-04 quality-first, D-06 plateau+sample stopping, D-07 acceptance bar, D-08 ship best.pt)
  </read_first>
  <what-built>
    An executor agent cannot babysit a multi-hour MPS run — this is inherently manual, real-hardware, and survives session kills via the resumable checkpoints (D-04). The run is launched and managed by the user; resume from `latest.pt` after any sleep/kill.
  </what-built>
  <how-to-verify>
    Run `python scripts/pretrain_tinystories.py` on the M3 (with `PYTORCH_ENABLE_MPS_FALLBACK=1`). Let it train quality-first ("as long as it takes", D-04) — re-launch after any laptop sleep/kill (it resumes bit-for-bit from `latest.pt`). Stop on a validation-loss plateau (D-06) confirmed by periodic qualitative sample prints. Acceptance (D-07 = BOTH):
      1. `checkpoints/best.pt` produced = the LOWEST val-loss step (D-08), not the final step.
      2. Recorded `perplexity = exp(best_val_loss)` + the `logs/run.csv` loss/lr/val curves (PRE-03).
      3. Curated samples from `best.pt` that read as coherent TinyStories-register text — grammatical, on-topic, simple narrative coherence with consistent characters/actions over a few sentences (the TinyStories paper's qualitative bar, D-07). This coherence judgement is the documented manual phase-gate check.
    Confirm a real session kill + resume worked at least once (D-04 resumability proof on the actual run).
  </how-to-verify>
  <resume-signal>Type "shipped" with the recorded perplexity (= exp(best_val_loss)), confirmation best.pt is the lowest-val checkpoint, that a kill+resume succeeded, and paste 2-3 curated coherent samples — or describe where coherence fell short and the plan (more tokens / LR change / CPU fallback).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checkpoint file → resume load | `latest.pt`/`best.pt` are the project's OWN trusted files; resume loads with `weights_only=False` (pickled optimizer/RNG state). No foreign checkpoint is ever loaded. |
| MPS device ops → numerical correctness | MPS op-coverage/correctness is the only "untrusted" compute surface (D-01a) — silent NaN/CPU-fallback risk, gated by the finite-loss + overfit smoke before the long run. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Tampering / RCE | `weights_only=False` checkpoint load on resume | accept | Own trusted local files only (established checkpoint.py contract); the Phase-8 slim inference checkpoint will use `weights_only=True`. Trust boundary stated explicitly; no foreign checkpoint loaded this phase. |
| T-05-04 | Denial of service | silent MPS NaN / CPU-fallback poisoning the multi-hour run | mitigate | `assert torch.isfinite(loss)` finite-loss gate + overfit-one-batch MPS smoke BEFORE the long run; `PYTORCH_ENABLE_MPS_FALLBACK=1` so a missing op falls back to CPU instead of crashing; periodic checkpoints cap lost work to ≤ a few minutes. |
| T-05-05 | Tampering | retraining the frozen tokenizer (vocab drift) | mitigate | Run entry + encode use `from_json(...)` only, never `.train()` (Pitfall 6); assert vocab_size==8192/eos==8184 via ModelConfig. |
| T-05-SC | Tampering | pip/package installs | mitigate | None: NO new packages installed (RESEARCH Package Legitimacy Audit — all pre-approved). No slopcheck checkpoint required. |
</threat_model>

<verification>
- `python -m pytest tests/test_resume_memmap.py tests/test_best_ckpt.py -x` green (PRE-02 resumability + PRE-03 best-val/perplexity).
- `tests/test_mps_smoke.py` SKIPS on CPU CI, PASSES on the real M3 (Task 4 gate).
- `make test` green — no CPU-suite regression; `_optimizer_step` and resume ordering provably unchanged.
- Long run (Task 5) produces `checkpoints/best.pt` (lowest val), `logs/run.csv` curves, recorded perplexity = exp(best_val_loss), and curated coherent samples; a kill+resume succeeded once.
</verification>

<success_criteria>
- The four loop seams are additive and the load-bearing AMP/accum/resume ordering is untouched (PRE-02 resumability holds on the memmap path).
- best.pt tracks the lowest val loss; perplexity = exp(best_val_loss) is recorded (PRE-03).
- The MPS sanity gate passed on real hardware; calibration MEASURED the run config (no invented hyperparameters).
- The local M3/MPS run reached the TinyStories coherence bar (D-07): recorded perplexity + curated coherent samples, surviving a real session kill (D-04).
</success_criteria>

<output>
Create `.planning/phases/05-tinystories-pretraining/05-02-SUMMARY.md` when done — include the calibrated config, the recorded perplexity = exp(best_val_loss), and the curated coherent samples.
</output>
