---
phase: 05-tinystories-pretraining
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/tinystories_fixture.txt
  - tests/test_memmap_data.py
  - src/personacore/training/data.py
  - src/personacore/training/__init__.py
  - scripts/encode_corpus.py
autonomous: true
requirements: [PRE-01]
user_setup: []

must_haves:
  truths:
    - "The full TinyStoriesV2-GPT4 corpus is encoded once into data/train.bin and data/val.bin as flat np.uint16 memmaps on local disk"
    - "There is exactly one EOS (8184) between documents — no doubled, no missing separators"
    - "get_batch_memmap draws in-bounds random (x,y) windows from a .bin path, cast uint16->int64 at batch time"
    - "train.bin and val.bin come from disjoint official files (zero leakage by construction)"
    - "decode(first N tokens of train.bin) round-trips back to the first raw document via the frozen tokenizer"
  artifacts:
    - path: "src/personacore/training/data.py"
      provides: "get_batch_memmap(bin_path, batch_size, block_size, device) memmap sampler alongside the existing in-RAM get_batch"
      contains: "def get_batch_memmap"
    - path: "scripts/encode_corpus.py"
      provides: "run-once streaming encode of the TinyStoriesV2 .txt files -> train.bin/val.bin, no CLI/argparse"
      contains: "from_json"
    - path: "tests/test_memmap_data.py"
      provides: "round-trip + one-EOS-between-docs + in-bounds get_batch_memmap + no-leakage tests (PRE-01)"
      contains: "get_batch_memmap"
    - path: "tests/fixtures/tinystories_fixture.txt"
      provides: "small multi-doc <|endoftext|>-separated fixture for GPU-free memmap tests"
  key_links:
    - from: "scripts/encode_corpus.py"
      to: "artifacts/tokenizer.json"
      via: "from_json (FROZEN — never .train())"
      pattern: "from_json"
    - from: "src/personacore/training/data.py::get_batch_memmap"
      to: "data/train.bin"
      via: "np.memmap re-opened per call"
      pattern: "np\\.memmap"
    - from: "src/personacore/training/__init__.py"
      to: "get_batch_memmap"
      via: "barrel re-export + __all__"
      pattern: "get_batch_memmap"
---

<objective>
Build the full-corpus data slice: a thin run-once encode script that turns the TinyStoriesV2-GPT4 `.txt` files into `data/train.bin`/`data/val.bin` (flat `np.uint16` memmaps, one EOS between docs), plus a `get_batch_memmap` sampler that mirrors the existing in-RAM `get_batch` indexing exactly. This is the thin end-to-end data slice: after this plan, a built memmap exists on disk and can be sampled into in-bounds `(x,y)` batches, fully tested CPU-only.

Purpose: PRE-01 — the corpus must be encoded once (never re-tokenized in the training loop) before any run can consume it. Plan 02's long run reads these `.bin` files.
Output: `data/train.bin`, `data/val.bin` (gitignored), `get_batch_memmap`, the encode script, the Wave-0 test scaffold (4 PRE-01 tests + shared fixture).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-tinystories-pretraining/05-CONTEXT.md
@.planning/phases/05-tinystories-pretraining/05-RESEARCH.md
@.planning/phases/05-tinystories-pretraining/05-PATTERNS.md
@.planning/phases/05-tinystories-pretraining/05-VALIDATION.md

<interfaces>
<!-- Exact contracts the executor needs. Extracted from the codebase — use directly, no exploration. -->

From src/personacore/training/data.py (the analog to mirror, lines 58-70):
```python
def get_batch(arr, batch_size, block_size, device):
    ix = np.random.randint(0, len(arr) - block_size - 1, size=batch_size)
    x = torch.stack([torch.from_numpy(arr[i : i + block_size].astype(np.int64)) for i in ix])
    y = torch.stack([torch.from_numpy(arr[i + 1 : i + 1 + block_size].astype(np.int64)) for i in ix])
    return x.to(device), y.to(device)
```
load_split (same file) shows the frozen-tokenizer load idiom:
`tok = from_json("artifacts/tokenizer.json")` then `tok.encode(text, allowed_special="all")` — the `<|endoftext|>` marker maps atomically to eos_id 8184.

From src/personacore/training/__init__.py — barrel re-export pattern:
`from .data import get_batch, load_split` and `get_batch` listed in `__all__`.

From scripts/train_bigram.py — the no-CLI thin-entry idiom:
`_REPO_ROOT = pathlib.Path(__file__).resolve().parent.parent`, `_REPO_ROOT / "artifacts" / "tokenizer.json"`, `def main()`, `if __name__ == "__main__": main()`. NO argparse.

Locked constants: vocab_size=8192, eos_id=8184, block_size=256. data/ is already gitignored.
TinyStoriesV2 download URLs (CLAUDE.md Sources):
  https://huggingface.co/datasets/roneneldan/TinyStories/resolve/main/TinyStoriesV2-GPT4-train.txt (~2.23 GB)
  https://huggingface.co/datasets/roneneldan/TinyStories/resolve/main/TinyStoriesV2-GPT4-valid.txt (~22.5 MB)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — PRE-01 RED test scaffold + shared fixture</name>
  <read_first>
    - tests/test_data_split.py (the data-path test idiom to copy verbatim — fixture path, frozen-tokenizer load, shape/dtype/in-bounds/leakage asserts)
    - tests/fixtures/bigram_corpus.txt (the existing multi-doc fixture shape to mirror)
    - .planning/phases/05-tinystories-pretraining/05-VALIDATION.md (the exact pytest node-ids each test must expose)
    - src/personacore/training/data.py (encode/eos idiom the round-trip + one-EOS tests assert against)
  </read_first>
  <action>
    Create `tests/fixtures/tinystories_fixture.txt`: a few short `<|endoftext|>`-separated micro-stories (mirror `tests/fixtures/bigram_corpus.txt` structure — at least 3 documents so train/val can be disjoint and EOS count is unambiguous). Create `tests/test_memmap_data.py` (CPU-only, GPU/MPS-free, frozen-tokenizer via `from_json("artifacts/tokenizer.json")`) with exactly these four node-ids from 05-VALIDATION.md, each building a small `.bin` in `tmp_path` from the fixture (or from disjoint slices) using the same encode discipline as `data.py::load_split` (`allowed_special="all"`, uint16 storage):
      - `test_encode_roundtrip` — decode the first N tokens of the built train `.bin` through the frozen tokenizer and assert it matches `tok.encode(first_raw_doc, allowed_special="all")` for the first document.
      - `test_one_eos_between_docs` — `np.count_nonzero(arr == 8184)` equals the document count in the fixture (no doubled, no missing EOS).
      - `test_get_batch_memmap_inbounds` — call `get_batch_memmap(bin_path, batch_size=4, block_size=16, device="cpu")`; assert shapes `(4,16)`, dtype `torch.int64`, `x.min() >= 0`, `x.max() < 8192`, and the start bound never overruns (`len < 16` guard like `test_data_split.py:77`).
      - `test_no_leakage_disjoint` — build train `.bin` from one set of fixture docs and val `.bin` from a disjoint doc; assert an 8-token window of the val stream never occurs verbatim in the train stream (adapt the leakage idiom from `test_data_split.py:44-54`).
    These import `from personacore.training.data import get_batch_memmap`, so they are RED until Task 2. Do NOT weaken any assertion to make them pass early.
  </action>
  <verify>
    <automated>cd $(git rev-parse --show-toplevel) && python -m pytest tests/test_memmap_data.py -x 2>&1 | grep -E "ImportError|ModuleNotFoundError|cannot import name 'get_batch_memmap'" </automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_memmap_data.py` exists and defines all four node-ids: `test_encode_roundtrip`, `test_one_eos_between_docs`, `test_get_batch_memmap_inbounds`, `test_no_leakage_disjoint`.
    - `tests/fixtures/tinystories_fixture.txt` exists with >=3 `<|endoftext|>`-separated documents.
    - Running `python -m pytest tests/test_memmap_data.py -x` fails ONLY on the missing `get_batch_memmap` import (RED for the right reason — not on a malformed test).
    - No assertion uses a tautology (e.g. `assert True`); each maps to a PRE-01 behavior above.
  </acceptance_criteria>
  <done>Four RED PRE-01 tests + fixture committed; failing solely because `get_batch_memmap` does not exist yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: get_batch_memmap (memmap sampler) — turn the data tests green</name>
  <read_first>
    - src/personacore/training/data.py (mirror `get_batch` lines 58-70 exactly; same len-block-1 bound, same uint16->int64 cast, same .to(device))
    - src/personacore/training/__init__.py (barrel re-export + __all__ pattern for get_batch)
    - .planning/phases/05-tinystories-pretraining/05-PATTERNS.md §"data.py (MODIFIED — add get_batch_memmap)" (the exact new-function body + the leak-avoidance note)
  </read_first>
  <behavior>
    - get_batch_memmap(bin_path, batch_size, block_size, device) returns (x, y) of shape (batch_size, block_size), dtype int64, on `device`.
    - It re-opens `np.memmap(bin_path, dtype=np.uint16, mode="r")` INSIDE the function every call (nanoGPT leak-avoidance — Pitfall 1), never holding a long-lived memmap.
    - Indexing is identical to the in-RAM get_batch: `ix = np.random.randint(0, len(data) - block_size - 1, size=batch_size)`; y is x shifted by one; cast `.astype(np.int64)` at draw time.
    - Plain `.to(device)` only — pin_memory/non_blocking are CUDA-only and must NOT appear.
  </behavior>
  <action>
    Add `get_batch_memmap(bin_path, batch_size, block_size, device)` to `src/personacore/training/data.py` directly below `get_batch`, line-for-line mirroring `get_batch`'s indexing with the in-RAM `arr` replaced by a freshly-opened `np.memmap(bin_path, dtype=np.uint16, mode="r")` (re-open EACH call). Use the locked `len(data) - block_size - 1` start bound and the `uint16 -> int64` cast at batch time; return `x.to(device), y.to(device)` with no `pin_memory`/`non_blocking`. Add `get_batch_memmap` to the `from .data import ...` line and the `__all__` list in `src/personacore/training/__init__.py`, mirroring how `get_batch` is exported.
  </action>
  <verify>
    <automated>cd $(git rev-parse --show-toplevel) && python -m pytest tests/test_memmap_data.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_memmap_data.py -x` is fully GREEN (all four node-ids pass).
    - `grep -n "np.memmap" src/personacore/training/data.py` shows the re-open inside `get_batch_memmap`.
    - `grep -c "pin_memory\|non_blocking" src/personacore/training/data.py` returns 0.
    - `python -c "from personacore.training import get_batch_memmap"` succeeds (barrel export wired).
    - `make test` still green (no regression in the existing CPU suite).
  </acceptance_criteria>
  <done>get_batch_memmap implemented and re-exported; all four PRE-01 data tests green; full CPU suite green.</done>
</task>

<task type="auto">
  <name>Task 3: encode_corpus.py — run-once streaming encode to train.bin/val.bin</name>
  <read_first>
    - scripts/train_bigram.py (the no-CLI thin-entry idiom: _REPO_ROOT-relative constants, def main(), __main__ guard, print summary — NO argparse)
    - src/personacore/training/data.py::load_split (the frozen-tokenizer load + allowed_special="all" + uint16 discipline to mirror)
    - .planning/phases/05-tinystories-pretraining/05-PATTERNS.md §"scripts/encode_corpus.py" (path constants, EOS_ID note, streaming + post-build asserts)
    - .planning/phases/05-tinystories-pretraining/05-RESEARCH.md §"Memmap Build Mechanics" + Pattern 1 (stream per-doc, accumulate uint16 shards, np.concatenate, .tofile — do NOT read 2.23 GB into one string)
  </read_first>
  <action>
    Create `scripts/encode_corpus.py` as a thin no-CLI entry (mirror `train_bigram.py`'s header). Define `_REPO_ROOT`-relative constants: `TOKENIZER_PATH`, `TRAIN_TXT`/`VAL_TXT` (the two `TinyStoriesV2-GPT4-*.txt` files under `data/`), `TRAIN_BIN`/`VAL_BIN` (`data/train.bin`/`data/val.bin`), and `EOS_ID = 8184`. Load the FROZEN tokenizer via `from_json(TOKENIZER_PATH)` (NEVER `.train()` — Pitfall 6). Write an `encode_to_bin(txt_path, bin_path)` that STREAMS the file by document boundary (`<|endoftext|>`), encodes each document with `tok.encode(doc, allowed_special="all")` so the marker maps atomically to 8184, accumulates `np.asarray(ids, dtype=np.uint16)` shards, `np.concatenate`s them, and writes with `arr.tofile(bin_path)`. Do NOT read the whole 2.23 GB into one Python string, and do NOT manually inject extra EOS ids (the source separators + atomic encoding already give exactly one EOS between docs). After each build, assert `np.count_nonzero(np.fromfile(bin_path, dtype=np.uint16) == EOS_ID) >= 1` and decode the first ~200 tokens as a sanity round-trip. `main()` encodes train then val and prints a token-count + EOS-count summary. Guard with `if __name__ == "__main__": main()`. Use `tqdm` for the long loop only if it is already importable (optional). This script is run manually on the real machine after the corpus is downloaded — do NOT run the 2.23 GB encode as part of automated verification.
  </action>
  <verify>
    <automated>cd $(git rev-parse --show-toplevel) && python -c "import ast,sys; ast.parse(open('scripts/encode_corpus.py').read()); print('syntax-ok')" && grep -c "argparse" scripts/encode_corpus.py | grep -qx 0 && grep -q "from_json" scripts/encode_corpus.py && grep -q "allowed_special" scripts/encode_corpus.py && echo "static-checks-ok"</automated>
    <human-check>After downloading the two TinyStoriesV2 .txt files into data/, run `python scripts/encode_corpus.py`; confirm it prints a token count in the hundreds-of-millions, an EOS count roughly equal to the document count, and a coherent decoded story prefix; confirm data/train.bin (~1.0-1.1 GB) and data/val.bin exist.</human-check>
  </verify>
  <acceptance_criteria>
    - `scripts/encode_corpus.py` parses (valid Python) and contains NO `argparse` (returns 0).
    - It calls `from_json(...)` (frozen tokenizer) and `encode(..., allowed_special="all")`; it never calls `.train()`.
    - No manual EOS injection (grep shows EOS only in the assert/constant, not appended to id lists).
    - It streams per-document (does not `read()` the whole train file into one string before encoding).
    - Manual run (human-check) produces non-empty `data/train.bin` and `data/val.bin` with EOS count ≈ doc count and a coherent decoded prefix.
  </acceptance_criteria>
  <done>encode_corpus.py exists as a thin no-CLI streaming encoder; static checks pass; manual run produces the two memmaps with verified one-EOS-between-docs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HF download → local disk | The two TinyStoriesV2 `.txt` files cross from a remote (trusted HuggingFace) source onto local disk; the only untrusted-origin input in this phase. |
| frozen tokenizer file → encode | `artifacts/tokenizer.json` is the project's own JSON data-only artifact (no pickle/code execution on load — established Phase 2). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | TinyStoriesV2 `.txt` corpus download | mitigate | Verify file size against the ~2.23 GB / ~22.5 MB CITED sizes and run the post-build decode round-trip sanity (a corrupt/truncated download fails the EOS-count and round-trip asserts before any training run). |
| T-05-02 | Information disclosure | data/train.bin, data/val.bin, raw .txt | accept | data/ is gitignored (verified) — corpus + memmaps never enter version control; local-only, no PII (public TinyStories). |
| T-05-SC | Tampering | pip/package installs | mitigate | None: this plan installs NO new packages (RESEARCH Package Legitimacy Audit — torch/numpy/tqdm/requests all pre-approved). No slopcheck checkpoint required. |
</threat_model>

<verification>
- `python -m pytest tests/test_memmap_data.py -x` green (4 PRE-01 node-ids).
- `make test` green (no CPU-suite regression; the MPS smoke is added in Plan 02 and is skipif-guarded).
- `scripts/encode_corpus.py` static checks pass; manual encode produces the two memmaps with EOS count ≈ doc count.
- `data/` remains gitignored; no `.bin`/`.txt` staged for commit.
</verification>

<success_criteria>
- The full corpus is encoded once into `data/train.bin`/`data/val.bin` (uint16, one EOS between docs) on local disk (PRE-01).
- `get_batch_memmap` samples in-bounds `(x,y)` int64 windows from a `.bin` path, re-opening the memmap per call.
- All four PRE-01 data tests are green; the encode script is a thin no-CLI streaming encoder using the frozen tokenizer.
</success_criteria>

<output>
Create `.planning/phases/05-tinystories-pretraining/05-01-SUMMARY.md` when done.
</output>
