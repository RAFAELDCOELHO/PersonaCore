---
phase: 29-v5-0-pre-registration-and-carried-debt
plan: 03
type: execute
wave: 2
depends_on: ["29-01"]
files_modified:
  - scripts/phase16_persistence.py
  - tests/test_phase16_driver.py
autonomous: true
requirements: [DEBT-02]

must_haves:
  truths:
    - "D-17: phase16_persistence.d28_note() reads the D-28 READING QUALIFICATION blockquote from 16-CONTEXT.md at runtime through the same parser as arm_d_qualifier(), whose output stays byte-identical"
    - "D-17: a test pins sha256(d28_note()) so an amended note reddens, and asserts d28_note() equals the test-side _context_blockquote('- **D-28:**') parse"
    - "D-17: the published results/phase16_persistence_report.md is not re-rendered; a test proves (verbatim kernel absent from the report) <=> ('TD-16-R1-REPORT' in phase29_prereg.NAMED_LIMITATIONS)"
  artifacts:
    - path: "scripts/phase16_persistence.py"
      provides: "_blockquote_after(anchor), D28_NOTE_ANCHOR, d28_note()"
      contains: "def d28_note"
    - path: "tests/test_phase16_driver.py"
      provides: "d28 digest / parity / report-absence tests"
  key_links:
    - from: "scripts/phase16_persistence.py::d28_note"
      to: ".planning/milestones/v3.0-phases/16-weight-vs-prompt-persistence-control/16-CONTEXT.md:267"
      via: "_CONTEXT_PATH read at call time, anchor '- **D-28:**'"
      pattern: "D28_NOTE_ANCHOR"
    - from: "tests/test_phase16_driver.py"
      to: "phase29_prereg.NAMED_LIMITATIONS"
      via: "import (scripts/ on sys.path)"
      pattern: "NAMED_LIMITATIONS"
---

<objective>
Close DEBT-02 (TD-16-R1) per D-17: the D-28 note is read verbatim from 16-CONTEXT.md at runtime so an amended note reddens a test; the published report bytes are kept, and the note's absence from the report is tied to the named-limitation entry Plan 01 committed.

Purpose: SC3's second clause.
Output: `d28_note()` in `scripts/phase16_persistence.py` and four tests in `tests/test_phase16_driver.py`.
Depends on Plan 01 only because the absence test reads `phase29_prereg.NAMED_LIMITATIONS["TD-16-R1-REPORT"]`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-v5-0-pre-registration-and-carried-debt/29-CONTEXT.md
@.planning/phases/29-v5-0-pre-registration-and-carried-debt/29-PATTERNS.md

<interfaces>
scripts/phase16_persistence.py (measured):
- :1830 LADDER_ANOMALY_CAVEAT (English rendering of D-28; emitted ~:2495) — do not change
- :2025 `_CONTEXT_PATH` = _REPO_ROOT/.planning/milestones/v3.0-phases/16-weight-vs-prompt-persistence-control/16-CONTEXT.md
- :2033 `ARM_D_QUALIFIER_ANCHOR = "- **D-25:**"`
- :2036-2060 `arm_d_qualifier()` — _prove(_CONTEXT_PATH.exists()), split on anchor once, collect lines where `stripped[:1] == ">"` (appending `stripped.lstrip(">").strip()`), break on the first non-`>` line after collecting, `_prove(lines, ...)`, return `" ".join(lines).strip('"')`
16-CONTEXT.md:267 `- **D-28:** ...` anchor; blockquote lines follow (:272+), kernel `*"o ladder licenciou span 2 …"*` at :276.
Measured (research): d28_note() via the same parser = 1402 chars, sha256 171725c69ff06241882a0d165c02172d80721b3b33d55ddf7c8bef8609ad4210 — RE-MEASURE on the implemented function before pinning. `'licenciou' in results/phase16_persistence_report.md` is False.
Source guards on phase16_persistence.py: tests/test_phase16_driver.py:643-648 forbid the substrings startswith, endswith, fuzz, levenshtein, SequenceMatcher, difflib ANYWHERE in the source (comments included); :629 requires source.count("0.125") == 1.
tests/test_phase16_driver.py: imports :23-30 (ast, importlib.util, json, pathlib, re, sys, torch — no hashlib); :32-35 `_REPO_ROOT`, `_SCRIPTS` inserted into sys.path; :38 `_CONTEXT_PATH`; source-guard tests `test_chance_floor_literal_matches_the_pool` (~:618) and `test_cosine_arm_is_scored_by_contains_value` (~:640); driver loaded via spec_from_file_location (:47-54) as `driver`; `_context_blockquote(anchor)` :90-106; `test_report_carries_every_verbatim_clause` :1313-1322 asserts `driver.arm_d_qualifier() == _context_blockquote("- **D-25:**")`.
scripts/phase29_prereg.py (Plan 01): `NAMED_LIMITATIONS["TD-16-R1-REPORT"]` with keys reason, ledger_rows.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add d28_note() through a shared _blockquote_after(anchor)</name>
  <files>scripts/phase16_persistence.py</files>
  <read_first>
    - scripts/phase16_persistence.py (lines 2015-2065)
    - tests/test_phase16_driver.py (lines 615-652 — the source guards)
    - .planning/milestones/v3.0-phases/16-weight-vs-prompt-persistence-control/16-CONTEXT.md (lines 260-285)
  </read_first>
  <action>
Per D-17: move the body of `arm_d_qualifier()` into a private `_blockquote_after(anchor)` that takes the anchor instead of reading `ARM_D_QUALIFIER_ANCHOR` (keep the `_prove` messages, substituting `{anchor!r}`; keep the `stripped[:1] == ">"` slice idiom and its existing comment). Then `arm_d_qualifier()` returns `_blockquote_after(ARM_D_QUALIFIER_ANCHOR)`. Add `D28_NOTE_ANCHOR = "- **D-28:**"` beside `ARM_D_QUALIFIER_ANCHOR` and `def d28_note()` returning `_blockquote_after(D28_NOTE_ANCHOR)`, with a docstring saying it is the D-28 READING QUALIFICATION read verbatim at runtime (TD-16-R1 / DEBT-02), that the published report is deliberately NOT re-rendered, and that its absence from the report is the named limitation `phase29_prereg.NAMED_LIMITATIONS["TD-16-R1-REPORT"]`.

Do not call `d28_note()` from any report-rendering path (no frozen report block is re-rendered). Do not write the forbidden substrings (startswith, endswith, fuzz, levenshtein, SequenceMatcher, difflib) or any new `0.125` anywhere, comments included.
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/python -c "import hashlib,importlib.util as u; s=u.spec_from_file_location('d','scripts/phase16_persistence.py'); m=u.module_from_spec(s); s.loader.exec_module(m); n=m.d28_note(); print(len(n), hashlib.sha256(n.encode()).hexdigest())" && .venv/bin/pytest -q -p no:cacheprovider tests/test_phase16_driver.py -k "verbatim or chance_floor or contains_value or qualifier"</automated>
  </verify>
  <acceptance_criteria>
    - The python probe prints a length and a sha256; if it is not `1402 171725c69ff06241882a0d165c02172d80721b3b33d55ddf7c8bef8609ad4210`, STOP and report the measured value (do not pin an unexplained digest).
    - The pytest selection exits 0 (existing source guards and the D-25 verbatim test stay green).
    - `.venv/bin/python -c "s=open('scripts/phase16_persistence.py').read(); print(s.count('0.125'), sum(s.count(t) for t in ('startswith','endswith','fuzz','levenshtein','SequenceMatcher','difflib')))"` prints `1 0`.
    - `git diff --quiet -- results/phase16_persistence_report.md` exits 0.
  </acceptance_criteria>
  <done>d28_note() exists, shares arm_d_qualifier()'s parser, and every existing phase16 source guard is green.</done>
</task>

<task type="auto">
  <name>Task 2: d28 tests — digest pin, parser parity, report-absence <=> named limitation</name>
  <files>tests/test_phase16_driver.py</files>
  <read_first>
    - tests/test_phase16_driver.py (lines 1-110, 1305-1330)
    - scripts/phase29_prereg.py (NAMED_LIMITATIONS)
  </read_first>
  <action>
Append four tests (names contain `d28` so `-k d28` hits all four); add `import hashlib` to the imports; the file already inserts `scripts/` into `sys.path` at :32-35 (`_REPO_ROOT`, `_SCRIPTS`), so add `import phase29_prereg  # noqa: E402` at the top-level imports block.
- `test_d28_note_is_read_verbatim_and_pinned`: `driver.d28_note() == _context_blockquote("- **D-28:**")` and `hashlib.sha256(driver.d28_note().encode("utf-8")).hexdigest()` equals the digest measured in Task 1 (pin the literal). The failure message says the D-28 note in 16-CONTEXT.md was amended.
- `test_d28_arm_d_qualifier_unchanged_by_the_refactor`: `driver.arm_d_qualifier() == _context_blockquote("- **D-25:**")`.
- `test_d28_report_absence_is_a_named_limitation`: kernel = the `*"…"*` span of `driver.d28_note()` extracted with `re.search(r'\*"(.+?)"\*', note, re.S).group(1)` (assert a match exists); `report = (_REPO_ROOT / "results/phase16_persistence_report.md").read_text(encoding="utf-8")`; `absent = " ".join(kernel.split()) not in " ".join(report.split())`; assert `absent == ("TD-16-R1-REPORT" in phase29_prereg.NAMED_LIMITATIONS)` and assert `absent` is True today (the measured state), so either a re-rendered report or a dropped entry reddens.
- `test_d28_amended_note_reddens(monkeypatch, tmp_path)` (watched RED on a tmp copy; the real 16-CONTEXT.md is never touched): copy 16-CONTEXT.md to `tmp_path`, change one character inside the D-28 blockquote, `monkeypatch.setattr(driver, "_CONTEXT_PATH", <copy>)`, and assert the sha256 of `driver.d28_note()` now differs from the pinned digest; assert the real file's bytes are unchanged afterwards.
No skipif; no `== 10`/`!= 10` text.
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/pytest -q -p no:cacheprovider tests/test_phase16_driver.py -k d28</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest -q -p no:cacheprovider tests/test_phase16_driver.py -k d28` exits 0 with 4 passed, 0 skipped.
    - `.venv/bin/pytest -q -p no:cacheprovider tests/test_phase16_driver.py` exits 0 (whole file).
    - `git diff --quiet -- .planning/milestones/v3.0-phases/16-weight-vs-prompt-persistence-control/16-CONTEXT.md` exits 0 after the run.
  </acceptance_criteria>
  <done>An amended D-28 note, a re-rendered report, or a dropped limitation entry each redden a test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 16-CONTEXT.md (planning doc) → runtime reader | a silent amendment of a pre-registered note |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-10 | Tampering | D-28 note in 16-CONTEXT.md | mitigate | runtime `d28_note()` + pinned sha256 test + watched-RED amendment test |
| T-29-11 | Repudiation | published report quietly re-rendered to "fix" history | mitigate | report bytes untouched; absence <=> `NAMED_LIMITATIONS["TD-16-R1-REPORT"]` test |
</threat_model>

<verification>
`.venv/bin/pytest -q -p no:cacheprovider tests/test_phase16_driver.py` exits 0; `results/phase16_persistence_report.md` unchanged.
</verification>

<success_criteria>
DEBT-02 closed: the D-28 note is read verbatim at runtime and any amendment reddens a test; the report's missing kernel is a named limitation, not rewritten history.
</success_criteria>

<output>
Create `.planning/phases/29-v5-0-pre-registration-and-carried-debt/29-03-SUMMARY.md` when done.
</output>
</content>
