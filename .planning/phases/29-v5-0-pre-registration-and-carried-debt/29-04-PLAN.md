---
phase: 29-v5-0-pre-registration-and-carried-debt
plan: 04
type: execute
wave: 3
depends_on: ["29-01", "29-02", "29-03"]
files_modified:
  - scripts/phase29_prereg.py
  - tests/test_phase29_prereg.py
  - .planning/phases/29-v5-0-pre-registration-and-carried-debt/29-VALIDATION.md
  - .planning/phases/29-v5-0-pre-registration-and-carried-debt/29-RESEARCH.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [PREREG-02, PREREG-03]

must_haves:
  truths:
    - "D-15: the developer's GATE-08 promotion ruling (option 1 or option 2) is recorded in 29-04-SUMMARY.md before any D-15-dependent line is written to scripts/phase29_prereg.py"
    - "D-06: admission is a pure function over a v5.0 frontier dict with EXPECTED_POINTS derived as len(ADVR_ARMS) * len(RATIO_GRID) == 12; INCONCLUSIVE (absent / wrong count / unknown verdict string / tallies that do not re-derive) takes precedence; ADMITTED iff >=1 stored PASS, naming every PASS key in POINT_KEYS() order"
    - "D-06/WR-05: recall_threshold(frontier, leg, arm) returns F_Y * k/n from the arm's OWN advr ratio-0 control counts and refuses a dp arm"
    - "D-07: every point REFUSED => admission REFUSED (never MOOT), carrying each leg's control reading; D-08: one leg REFUSED + the other measured with zero PASS => MOOT whose reasons name the refused leg and its counts"
    - "D-10/PREREG-02: relearning_scope(admission) maps ADMITTED -> RELRN-06..09 on every admitted point; MOOT -> RELRN-06..09 ship as a MOOT named limitation; REFUSED -> named limitation 'the frontier could not be measured'; plus the D-15-ruled reading"
    - "D-09: relearning parameters — rungs, band, K (MARGIN_K / CURVE_K / FULL_K), baselines, attacker corpus, recovery fixture — are imported from phase27_prereg by reference (asserted by an AST guard that each pin's assigned value is an `ast.Attribute` on `phase27_prereg`, plus `is` for the non-int, non-str pins — `is` alone is vacuous on CPython's small-int cache); only the five never_taught_* baselines are imported, the advr control baseline is pinned by Phase-32 record reference, and the recovery fixture is pinned by source reference (phase27_relearn.disjointness_report reading results/phase16_recall_sample.json), never copied (as confirmed or overridden at the checkpoint)"
    - "Option 1 only: a stored promotable verdict (PASS, or INCONCLUSIVE carrying REPLICATION_PENDING_MARKER) with no promotion_record reads INCONCLUSIVE, never ADMITTED and never MOOT; promotability is checked only on points whose verdict is in V4_VERDICTS and only after the closed-domain/count/tally checks, so an all-REFUSED or unknown-verdict frontier never reaches promote_to_full_fidelity (which _proves verdict in V4_VERDICTS and would raise SystemExit); the ruling is recorded as a required Phase-31 input and noted under ROADMAP Phases 31/32"
    - "Every Phase-29 test and the full suite are green on a committed tree"
  artifacts:
    - path: "scripts/phase29_prereg.py"
      provides: "admission contract + scope rule + D-09 pins + the D-15 branch"
      contains: "def admission"
    - path: "tests/test_phase29_prereg.py"
      provides: "admission branch/precedence tests on forged 12-point dicts"
  key_links:
    - from: "scripts/phase29_prereg.py::admission"
      to: "phase27_prereg.point_verdict_string"
      via: "by-reference import of the v4.0 per-point verdict reader"
      pattern: "point_verdict_string"
    - from: "scripts/phase29_prereg.py::recall_threshold"
      to: "frontier['verdicts']['control_readings']['advr_<leg>']"
      via: "arm-keyed control counts"
      pattern: "control_readings"
---

<objective>
Stop at the D-15 ruling checkpoint (GATE-08 / promotion) with the measured friction visible, then write the whole admission contract and conditional scope rule (PREREG-02) into `scripts/phase29_prereg.py` according to the ruling, and close the phase with a full-suite gate.

Purpose: SC2's scope-rule half, with D-06..D-10 frozen as code before any v5.0 number exists.
Output: admission contract + tests; VALIDATION.md signed off.

This plan runs last (wave 3) on purpose: every D-15-independent item (Plans 01-03: keys, paths, replay, refusal, all four debt items) is already committed when the developer is asked, so nothing independent waits on the checkpoint. Editing the prereg again here is legal: the ancestry guard only requires every prereg commit to precede the first `results/phase3*` commit (Phase 30).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-v5-0-pre-registration-and-carried-debt/29-CONTEXT.md
@.planning/phases/29-v5-0-pre-registration-and-carried-debt/29-RESEARCH.md
@.planning/phases/29-v5-0-pre-registration-and-carried-debt/29-PATTERNS.md
@.planning/phases/29-v5-0-pre-registration-and-carried-debt/29-01-SUMMARY.md

<interfaces>
scripts/phase27_prereg.py (PINNED — import by reference, never edit, never call its dp-hardwired functions on v5.0 data):
- by-reference pins for D-09: MARGIN_K (:81, = erasure_gate.MARGIN_K), CURVE_K (:82), FULL_K (:84), RUNGS (:106), RELEARN_CAP (:104), MAX_STEPS (:100), CHECKPOINT_INTERVAL (:101), DESIGNATED_SEED (:102), FRESH_SEEDS (:108) == (1337, 2024, 1338, 2025, 1339), POOLED_SEED_INDEX (:110), PINNED_BASELINES (:130-181; five never_taught_* entries + control_n8/control_n64 which are DP σ=0 adapters), ATTACKER_CORPUS (:199), first_clear (:493), z_rule (:515), band (:540), recovery_gate (:575), promote_at_z (:618), point_verdict_string (:240), cleared_abc (:261), REFUSED (:118) == "REFUSED"
- DO NOT call on v5.0 data: recall_threshold (:467, hard-reads dp_<leg>), extraction_ceiling_x, arm_of, admitted_point_keys, relearning_is_worth_attempting (:340; EXPECTED_POINTS=44, dp CONTROL_KEYS, ORDERED_POINT_KEYS). Copy the STRUCTURE of relearning_is_worth_attempting (:340-422), _tally (:330), _verdict (:335) — rewrite, don't call.
- recovery_gate refuses a baseline key outside PINNED_BASELINES (research Pitfall 6).
scripts/phase27_relearn.py: the recovery fixture has NO module constant — `disjointness_report()` (:176) reads the question text via `x18.CORPUS_SOURCE_FIXTURE.read_text(...)`, where `phase18_extraction.CORPUS_SOURCE_FIXTURE` (:705) = `_REPO_ROOT / "results" / "phase16_recall_sample.json"` (the path at :180/:215 is docstring/message prose only; the same file is `phase16_ladder.FIXTURE_PATH` :833 and `phase16_persistence.FIXTURE_PATH` :287). phase27_relearn runs `git_sha()` at import — never import it from phase29_prereg; tests may.
scripts/mitigation_gate.py: V4_VERDICTS (:85) == ("PASS","FAIL","INCONCLUSIVE"); REPLICATION_PENDING_MARKER (:149) == "clears all three conditions, replication pending"; GATE-08 at :822-829 (INCONCLUSIVE when (a)(b)(c) clear and not replicated_at_second_seed); promote_to_full_fidelity(*, verdict, reasons, curve_k, full_k) (:963) returns (promote, reason); ratchet_k (:917) accepts K only in (48, 24, 16, 8).
scripts/mitigation_budget.py: CURVE_K (16), FULL_FIDELITY_K (48).
Frontier schema (v4.0 shape, reused so point_verdict_string/cleared_abc apply unchanged): point_keys; points[k].verdict.{verdict, early_return_reason, <route kwargs>}; verdicts.{tallies, tallies_by_leg, control_readings[<leg>].recall_counts.{taught,heldout}}. v5.0 leg names: advr_n8 / advr_n64 (key.rsplit("_", 1)[0]).
scripts/phase29_prereg.py (Plan 01): ADVR_ARMS, LEGS, RATIO_GRID, F_Y, GATE_ROUTE, point_key, POINT_KEYS(), control_key(leg), POINT_RECORD_PREFIX, point_record_path, V5_RESULT_PATHS, ARTIFACT_PATHSPECS (derived), _prove, _prove_count, control_is_unlearnable, REFUSED_RECORD_FIELDS, refused_record, NAMED_LIMITATIONS. NO literal 4 / 0.7 / grid literal may appear (Plan 01's AST guards run on this file).
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: D-15 ruling — GATE-08 replication and the promotion route (plus three confirmations)</name>
  <decision>How can a v5.0 point ever read PASS, given the frozen gate returns INCONCLUSIVE for any point clearing (a)(b)(c) without second-seed replication (GATE-08, mitigation_gate.py:822-829)?</decision>
  <context>
Measured confirmation (29-RESEARCH §D-15): `corrected_point_verdict(..., replicated_at_second_seed=True)` returns PASS; the same call with False returns INCONCLUSIVE with reason "clears all three conditions, replication pending (GATE-08 / D-29)". The route passes GATE-08 through untouched; `arm="adversarial"` behaves identically. So without a replication input, D-06's "ADMITTED iff >=1 PASS" can never fire.

Import friction, piece by piece:

| Piece | Where | Imports cleanly for advr? | Friction |
|---|---|---|---|
| Promotion DECISION `promote_to_full_fidelity` + `ratchet_k` | mitigation_gate.py:963, :917 | Yes — pure, arm-agnostic; K from CURVE_K/FULL_FIDELITY_K (16 -> 48) | None |
| PROMOTION_RULE text | phase25_prereg.py:360 | Yes, as a rule statement | Silent on whether the CONTROL is re-trained at the second seed, and on WHICH second seed — both new decisions |
| phase25_promotion module (point_records, control_readings, curve_pass, build) | scripts/phase25_promotion.py | No | Hardwired to the 44 v4.0 keys and results/phase25_* paths; takes the adversarial control_gap from dp_n{cap}_sigma0 records (hidden DP dependency, contradicts ACTRL-01/WR-05); build() reads an accountant ε (contradicts DEBT-04); REPLICATED_AT_SECOND_SEED = False is a module constant |
| phase25_verdict.curve_verdicts | phase25_verdict.py:504 | Partially | Leg labels come from teach_persona.ADV_ARMS, so advr data would travel under an adv label; appending advr to ADV_ARMS makes it refuse |
| EXECUTION: K=48 redraw + second-seed training + replication record | — | Does not exist | v4.0 had zero candidates; no code trains a point at a second seed or sets replicated_at_second_seed=True from a record; phase25_run.run_point refuses advr_* keys |

Only the decision rule imports cleanly. The friction is real on both the execution axis and the DP-dependency axis.

Confirmations bundled here (so there is one stop, not four):
(b) D-09 clarification (research Pitfall 6 / A3): `phase27_prereg.PINNED_BASELINES["control_n8"/"control_n64"]` are DP σ=0 adapters. Recommendation: import the five never_taught_* baselines unchanged, and pin the advr relearning control BY REFERENCE to `results/phase32_point_<control_key(leg)>.json::adapter_sha256` (its digest cannot exist until Phase 32). This follows D-09's own "v5.0 changes only … the control source", but it is not a pure import.
(c) Filenames already pinned in Plan 01's `V5_RESULT_PATHS` (research A1): phase30_calibration.json, phase31_probe_point.json, phase31_probe_relearn.json, phase31_budget.json, phase32_point_<key>.json, phase32_frontier.json, phase33_admission.json, phase33_*, phase34_*. Accept or rename (renames are still legal — no results/phase3* file exists).
(d) Seam: `ADVR_ARMS` is declared in phase29_prereg and Phase 30 imports it (never appended to teach_persona.ADV_ARMS). Accept or override.
  </context>
  <options>
    <option id="option-1">
      <name>Pre-register promotion (adapt it for advr)</name>
      <pros>A v5.0 point can genuinely reach PASS and be ADMITTED; relearning (RELRN-06..09) can run on it.</pros>
      <cons>Not an import: new Phase-31/32 code for second-seed training, the K=48 redraw and a replication record; Phase 31's budget must cover it (cost unmeasured; v4.0's formula candidates x 2 x 3 x K16 h/point is DP-era). Needs three more choices NOW: the second seed (candidate FRESH_SEEDS[1] = 2024), whether the ratio-0 control is also replicated, and the promotion namespace (proposed results/phase32_promotion_<key>_k{FULL_FIDELITY_K}_seed<S>.json). D-14's no-retry test must then state that promotion keys are replications at a pinned seed and K, not retries.</cons>
    </option>
    <option id="option-2">
      <name>No promotion; a distinct CANDIDATE-UNREPLICATED admission reading</name>
      <pros>No new execution code; nothing new to budget; the frozen gate is imported untouched. A would-be PASS is still reported honestly as "cleared (a)(b)(c), replication pending", never MOOT.</pros>
      <cons>v5.0 can never ADMIT in practice; RELRN-06..09 then ship as a named limitation ("candidate unreplicated") if any candidate appears.</cons>
    </option>
  </options>
  <files>.planning/phases/29-v5-0-pre-registration-and-carried-debt/29-04-SUMMARY.md, .planning/phases/29-v5-0-pre-registration-and-carried-debt/29-RESEARCH.md</files>
  <action>Present the decision, the friction table and confirmations (b)-(d) above to the developer exactly as written, then STOP. Write nothing to scripts/phase29_prereg.py before the reply (CONTEXT D-15). On reply, record it verbatim under a "D-15 ruling" heading in 29-04-SUMMARY.md; then in 29-RESEARCH.md "## Open Questions" replace the `**PENDING:**` line under Q1 and under Q2 with `**RESOLVED (29-04 checkpoint, <date>):** <the ruling, one sentence>`, and replace Q3's existing "RESOLVED (planning)" line with `**RESOLVED (29-04 checkpoint, <date>):** <the confirmed/overridden (d) outcome>` (hand edit; no other byte of RESEARCH.md changes). Then proceed to Task 2.</action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && git diff --quiet -- scripts/phase29_prereg.py && grep -q "D-15 ruling" .planning/phases/29-v5-0-pre-registration-and-carried-debt/29-04-SUMMARY.md && test "$(grep -c 'RESOLVED (29-04 checkpoint' .planning/phases/29-v5-0-pre-registration-and-carried-debt/29-RESEARCH.md)" -ge 3</automated>
  </verify>
  <done>The developer's ruling on D-15 and (b)-(d) is recorded verbatim; the prereg is unchanged since Plan 01.</done>
  <resume-signal>Reply "option-1" (with: second seed, control replicated yes/no, namespace accept/override) or "option-2"; and accept/override (b) D-09 control baseline, (c) filenames, (d) ADVR_ARMS seam.</resume-signal>
  <acceptance_criteria>
    - The developer's verbatim reply is recorded in 29-04-SUMMARY.md under "D-15 ruling" before Task 2 starts.
    - `git diff --quiet -- scripts/phase29_prereg.py` exits 0 at the moment the ruling is received (nothing D-15-dependent written before it).
    - 29-RESEARCH.md Open Questions 1, 2 and 3 each carry a `RESOLVED (29-04 checkpoint` line.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Write the admission contract + scope rule per the ruling, with branch tests</name>
  <files>scripts/phase29_prereg.py, tests/test_phase29_prereg.py, .planning/ROADMAP.md (option 1 only)</files>
  <read_first>
    - scripts/phase29_prereg.py (current, from Plan 01)
    - scripts/phase27_prereg.py (lines 100-125, 237-340, 340-492)
    - tests/test_phase27_prereg.py (lines 186-270 — _forge, admits-only-pass, partial/inconsistent tests)
    - scripts/mitigation_gate.py (lines 140-160, 815-830, 960-1000)
    - scripts/phase27_relearn.py (lines 176-220 — disjointness_report and the fixture path)
    - 29-04-SUMMARY.md "D-15 ruling" section (written in Task 1)
  </read_first>
  <action>
Implement in `scripts/phase29_prereg.py`, citing the decision IDs in comments/docstrings. No literal 4, 0.7 or grid literal (Plan 01 AST guards); never call phase27_prereg's dp-hardwired functions.

Step 0 — apply the checkpoint's (c)/(d) overrides FIRST (skip if both were accepted): (c) a renamed filename -> edit that `V5_RESULT_PATHS` entry (and its requirement comment) in scripts/phase29_prereg.py and every literal expectation of it in tests/test_phase29_prereg.py; `ARTIFACT_PATHSPECS` stays derived, and if the rename leaves the `results/phase3[0-4]_*` set the Plan-01 pathspecs test must still pass unchanged, otherwise stop and report. (d) an overridden seam -> rename/relocate `ADVR_ARMS` as ruled and update every reference in both files (`point_key`, `POINT_KEYS`, `leg_keys`, `control_key`, `_V4_TWIN`, tests). Record in 29-04-SUMMARY.md which overrides were applied, with the diff stat; run the full test file before continuing.

Common to both options:
- Add `import phase27_prereg` (module level; torch-free, measured). D-09 by-reference pins: `MARGIN_K, CURVE_K, FULL_K, RUNGS, RELEARN_CAP, MAX_STEPS, CHECKPOINT_INTERVAL, DESIGNATED_SEED, FRESH_SEEDS, POOLED_SEED_INDEX, ATTACKER_CORPUS, first_clear, z_rule, band, recovery_gate, promote_at_z, point_verdict_string, cleared_abc` each `= phase27_prereg.<name>`; `REFUSED = phase27_prereg.REFUSED`; `V4_VERDICTS = mitigation_gate.V4_VERDICTS`. Recovery fixture (D-09) by SOURCE REFERENCE, never content: `RECOVERY_FIXTURE_SOURCE = ("phase27_relearn.disjointness_report", "results/phase16_recall_sample.json")` — a (reader, path) pair in the `REPLAY_SOURCE` register; do not import phase27_relearn here and do not read or copy the fixture. Baselines per the ruling's (b): if accepted, `NEVER_TAUGHT_BASELINES = {k: v for k, v in phase27_prereg.PINNED_BASELINES.items() if k.split("_")[0] == "never"}` (derive; the test asserts exactly five and that each value `is` the phase27 dict value), and `CONTROL_BASELINE_SOURCE = "results/phase32_point_{control_key}.json::adapter_sha256"` with a `control_baseline_source(leg)` function formatting it from `control_key(leg)`; if overridden, implement the developer's instruction and record it.
- `EXPECTED_POINTS = len(ADVR_ARMS) * len(RATIO_GRID)`; `_TALLY_NAMES = (*V4_VERDICTS, REFUSED)`; `VERDICTS = ("ADMITTED", "MOOT", "INCONCLUSIVE", REFUSED)` plus the option-2 reading if ruled.
- `FRONTIER_SCHEMA` docstring constant describing the frontier shape above (Phase 32 must emit it; D-06 "nothing decided after the frontier exists"). It MUST name `points[k].verdict.reasons` (a list of str; the v4.0 records already carry it, measured on results/phase25_frontier.json) because `promote_to_full_fidelity` reads `reasons[-1]` for the `REPLICATION_PENDING_MARKER` path.
- `recall_threshold(frontier, leg, arm)` (ADMIT-01 signature, D-06/WR-05): `_prove(arm == "advr", ...)` refusing any other (explicitly "dp"); `(k, n)` from `frontier["verdicts"]["control_readings"][f"advr_{leg}"]["recall_counts"]["taught"]`; `_prove_count` both; `0 <= k <= n`, `n > 0`; return `(F_Y * (k / n), k, n)`.
- `admission(frontier)` returning `{"verdict": ..., "reasons": [...], "admitted_point_keys": [...], "control_readings": {leg: {taught:[k,n], heldout:[k,n]}}}` — structure copied from `relearning_is_worth_attempting`, rewritten. Precedence (D-06, D-07, D-08), evaluated strictly in this order, first hit returns: (1a) INCONCLUSIVE if the frontier is absent/not a dict, or `point_keys` != `list(POINT_KEYS())`, or `set(points) != set(POINT_KEYS())`, or any `points[k]` is not a dict whose `verdict` is a dict containing a `verdict` key (returned, never a KeyError — T-29-14); (1b) INCONCLUSIVE if any point's verdict string (via `point_verdict_string`) is outside `_TALLY_NAMES` (e.g. "MAYBE", None), or any point whose verdict string is in `V4_VERDICTS` has `verdict.reasons` that is not a list of str; (1c) INCONCLUSIVE if `tallies` / `tallies_by_leg` do not re-derive from the entries; (1d) option 1 only — the promotability check below; it runs only after (1a)-(1c) have passed, and only on points whose verdict string is in `V4_VERDICTS` (REFUSED points are skipped, never passed to `promote_to_full_fidelity`); under option 2 (1d) does not exist; (2) ADMITTED iff >=1 PASS, `admitted_point_keys` = every PASS key in `POINT_KEYS()` order; (3) option-dependent step (below); (4) REFUSED iff every point is REFUSED — reasons carry each leg's control counts (D-07: "the frontier could not be measured", never "the mitigation held"); (5) MOOT otherwise — reasons name every fully-REFUSED leg with its control counts and state MOOT does not extend to that capacity (D-08).
- `SCOPE_RULE` dict + `relearning_scope(admission_result)` (D-10, PREREG-02): ADMITTED -> run RELRN-06..09 on each of `admitted_point_keys`; MOOT -> RELRN-06..09 ship as a MOOT named limitation; REFUSED -> RELRN-06..09 ship as a named limitation, reason "the frontier could not be measured"; INCONCLUSIVE -> refuse to proceed (malformed record) by `_prove`-raising SystemExit (test with `pytest.raises(SystemExit)`). `_prove` the verdict is in `VERDICTS`.

Option 1 only (as ruled): `PROMOTION_SEED = <ruled seed>` — if the ruled seed is `FRESH_SEEDS[1]`, write it as `FRESH_SEEDS[1]` (by reference, no literal); `PROMOTION_K = mitigation_budget.FULL_FIDELITY_K`; `PROMOTION_DECISION = mitigation_gate.promote_to_full_fidelity`; `CONTROL_REPLICATED = <ruled bool>`; `promotion_record_path(key)` -> `f"results/phase32_promotion_{key}_k{PROMOTION_K}_seed{PROMOTION_SEED}.json"` proving `key in POINT_KEYS()`; append `"results/phase32_promotion_*.json"` to `V5_RESULT_PATHS` (the derived pathspecs are unchanged — assert it). Step (1d) under option 1: for each `k` in `POINT_KEYS()` order whose verdict string `v` is in `V4_VERDICTS` (guard written as an explicit `if v in V4_VERDICTS` before the call — REFUSED and any unknown string never reach it; unknowns already returned at (1b)), call `PROMOTION_DECISION(verdict=v, reasons=points[k]["verdict"]["reasons"], curve_k=mitigation_budget.CURVE_K, full_k=PROMOTION_K)`; if `[0]` is True (a curve-K PASS, or an INCONCLUSIVE carrying `REPLICATION_PENDING_MARKER`) and `points[k].get("promotion_record")` != `promotion_record_path(k)` (absent or wrong), return INCONCLUSIVE with a reason naming the key ("promotable, promotion record missing"); it never falls through to MOOT or ADMITTED. `promotion_record` is the v5.0 field; v4.0 point records carry a separate top-level `promotion` key, and `FRONTIER_SCHEMA` states that Phase 32 writes `promotion_record` only and must not write the v4.0 `promotion` key on v5.0 points (admission ignores `promotion`). Step (3) is otherwise empty under option 1: `FRONTIER_SCHEMA` states that Phase 32 stores, for a promoted point, the route verdict computed with the replication outcome as `points[k].verdict.verdict` and cites its `promotion_record_path(k)` in `points[k].promotion_record`; admission reads stored verdicts only. Amend `test_no_retry_or_alternate_key_is_exposed` to state promotion paths are replications of the same key at pinned seed and K, and assert every promotion path stem parses back to a `POINT_KEYS()` member and never itself parses as a point key. ROADMAP propagation (option 1 only): record the ruling in 29-04-SUMMARY.md under "Required Phase-31 input" (second seed, control replicated, namespace, and that Phase 31's budget must cover promotion runs). Then snapshot `.planning/ROADMAP.md` to the scratchpad, hand-append (Edit tool; NEVER a gsd-sdk mutation handler) one line to the Phase 31 and Phase 32 sections: `**Phase-29 D-15 ruling (option 1):** promotion is pre-registered — <seed>, <control replicated>, results/phase32_promotion_*.json; Phase 31 must budget it and Phase 32 must run it before Phase 33 reads the frontier.` Then `diff` the snapshot against the file: exactly two added lines, zero removed, and every `v4.0` section/sentinel byte unchanged.
Option 2 only (as ruled): `CANDIDATE_UNREPLICATED = "CANDIDATE-UNREPLICATED"` added to `VERDICTS`; step (3): if no PASS and >=1 point whose stored verdict is INCONCLUSIVE and `mitigation_gate.promote_to_full_fidelity(verdict=..., reasons=..., curve_k=mitigation_budget.CURVE_K, full_k=mitigation_budget.FULL_FIDELITY_K)[0]` is True (the `REPLICATION_PENDING_MARKER` path), return CANDIDATE-UNREPLICATED naming those keys in `POINT_KEYS()` order — never MOOT. Step (3) runs after (1a)-(1c), so only V4_VERDICTS strings reach the call; guard it with `if v == "INCONCLUSIVE"` so REFUSED points are never passed. Under option 2 a stored PASS reads ADMITTED at step (2) regardless of any `promotion_record` field (none is defined). `SCOPE_RULE[CANDIDATE_UNREPLICATED]` -> RELRN-06..09 ship as a named limitation "candidate cleared (a)(b)(c), replication not pre-registered". Add a `NAMED_LIMITATIONS["GATE-08-NO-PROMOTION"]` entry recording the ruling.

Tests (append to `tests/test_phase29_prereg.py`; admission/scope/threshold test names contain `admission`, `scope` or `threshold`; the D-09 pin and recovery-fixture tests keep the names given below): build a minimal consistent 12-point frontier dict in the test (`_v5_frontier(verdicts_by_key, control_counts_by_leg, reasons_by_key=None, promoted=True)`): every point gets `verdict.reasons` (default `[]`); under option 1, when `promoted` is True, it stamps `points[k]["promotion_record"] = promotion_record_path(k)` on every point whose verdict is promotable (PASS, or INCONCLUSIVE whose reasons end with the marker) so the shared PASS->ADMITTED tests hold under both options; with a `_forge` that moves `tallies` and `tallies_by_leg` together; never load or deepcopy the 22 MB v4.0 frontier here. Cover: all-FAIL -> MOOT; one PASS -> ADMITTED naming it; two PASS -> both in key order; all-REFUSED -> REFUSED with both legs' counts; n64 all REFUSED + n8 zero PASS -> MOOT whose reasons name advr_n64 and its counts; count 11 / unknown string "MAYBE" / tampered tally -> INCONCLUSIVE (returned, never SystemExit) and INCONCLUSIVE beats a PASS; "MAYBE" alongside a PASS -> INCONCLUSIVE; a frontier with `point_keys` intact but one `points[k]` entry deleted -> INCONCLUSIVE (returned, not raised); FRONTIER_SCHEMA and every other public string use `<key>`/`<leg>` placeholders, never an illustrative `advr_n<N>_ratio...` fragment (Plan 01's retry census would redden); `test_admission_all_refused_does_not_raise` and `test_admission_mixed_refused_does_not_raise` (n64 all REFUSED + n8 all FAIL) return REFUSED / MOOT under the ruled option without raising (these are the cases that would hit `promote_to_full_fidelity`'s `_prove(verdict in V4_VERDICTS)` if the (1d) guard were missing); `recall_threshold(fr, "n8", "advr")` returns F_Y*k/n from advr_n8 counts and `recall_threshold(fr, "n8", "dp")` raises SystemExit; bool count refused; `relearning_scope` for every verdict in `VERDICTS`; the option-specific branch (option 1: promotion path tests; option 2: an INCONCLUSIVE point whose reasons carry `REPLICATION_PENDING_MARKER` -> CANDIDATE-UNREPLICATED, never MOOT); `test_d09_pins_are_attribute_references` (AST, same style as Plan 01's grid-retype guard): parse scripts/phase29_prereg.py; for each name in (MARGIN_K, CURVE_K, FULL_K, RUNGS, RELEARN_CAP, MAX_STEPS, CHECKPOINT_INTERVAL, DESIGNATED_SEED, FRESH_SEEDS, POOLED_SEED_INDEX, ATTACKER_CORPUS, first_clear, z_rule, band, recovery_gate, promote_at_z, point_verdict_string, cleared_abc, REFUSED) there is exactly one module-level `ast.Assign` targeting it and its value is `ast.Attribute(value=ast.Name(id="phase27_prereg"), attr=<same name>)`; likewise `V4_VERDICTS` -> `ast.Attribute` on `mitigation_gate`. This is the load-bearing pin check because the int pins (MARGIN_K, CURVE_K, FULL_K, MAX_STEPS, CHECKPOINT_INTERVAL, POOLED_SEED_INDEX, DESIGNATED_SEED, RELEARN_CAP) are small ints cached by CPython, so `is` on them is vacuous (`2 is int('2')` is True), and REFUSED is an interned str; keep `is` assertions only for the tuple/dict/function pins (RUNGS, FRESH_SEEDS, ATTACKER_CORPUS (a dict, measured), first_clear, z_rule, band, recovery_gate, promote_at_z, point_verdict_string, cleared_abc) as a secondary check; exactly five never_taught baselines; `test_recovery_fixture_is_pinned_by_reference`: import `phase27_relearn` and `inspect` in the test, assert `RECOVERY_FIXTURE_SOURCE[0].split(".")` resolves to `phase27_relearn.disjointness_report`, that `phase18_extraction.CORPUS_SOURCE_FIXTURE == phase18_extraction._REPO_ROOT / RECOVERY_FIXTURE_SOURCE[1]` and that the parsed AST of `inspect.getsource(phase27_relearn.disjointness_report)` contains an `ast.Attribute` with `attr == "CORPUS_SOURCE_FIXTURE"` (the real read path — never a substring match on source text, which would stay green on docstring prose), that `git ls-files --error-unmatch` on it succeeds, and (AST over scripts/phase29_prereg.py) exactly one `ast.Constant` node satisfies `node.value == RECOVERY_FIXTURE_SOURCE[1]` (equality match, so a docstring merely mentioning the path does not count) and no `ast.Call` exists whose callee is `ast.Name` or `ast.Attribute` named any of `open`, `read_text`, `read_bytes`, `load`, `loads` — the module names the fixture, never reads or copies it; option 1 additionally: a forged frontier with one stored PASS and `promoted=False` (no `promotion_record`) -> INCONCLUSIVE, the same with `promotion_record` set to another key's path -> INCONCLUSIVE, and one INCONCLUSIVE carrying `REPLICATION_PENDING_MARKER` without a `promotion_record` -> INCONCLUSIVE (never MOOT), and the same PASS with `promotion_record == promotion_record_path(k)` -> ADMITTED. No skipif; no `== 10`/`!= 10` text; `ratchet_k`-style K values in fixtures only from (48, 24, 16, 8).
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/pytest -q -p no:cacheprovider tests/test_phase29_prereg.py</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest -q -p no:cacheprovider tests/test_phase29_prereg.py -k "admission or scope or threshold"` exits 0 with at least 10 passed, 0 skipped.
    - The whole file passes, including Plan 01's AST guards and ancestry test re-run on the edited module (`.venv/bin/pytest -q -p no:cacheprovider tests/test_phase29_prereg.py` exits 0).
    - `.venv/bin/python -c "import sys; sys.path.insert(0,'scripts'); import phase29_prereg as p; print('torch' in sys.modules, p.EXPECTED_POINTS, 'REFUSED' in p.VERDICTS)"` prints `False 12 True`.
    - `git diff --quiet -- scripts/phase27_prereg.py scripts/phase25_promotion.py scripts/mitigation_gate.py scripts/phase20_gate_coverage.py scripts/phase25_record.py` exits 0.
    - `find results -name 'phase3*' | wc -l` prints 0.
    - `.venv/bin/pytest -q -p no:cacheprovider tests/test_phase29_prereg.py -k "d09_pins_are_attribute_references or recovery_fixture_is_pinned_by_reference or does_not_raise"` exits 0 with 0 skipped (the AST pin guard replaces a vacuous small-int `is` print).
    - `.venv/bin/python -c "import sys; sys.path.insert(0,'scripts'); import phase29_prereg as p, phase27_prereg as q; print(p.RUNGS is q.RUNGS, p.FRESH_SEEDS is q.FRESH_SEEDS, p.point_verdict_string is q.point_verdict_string, p.RECOVERY_FIXTURE_SOURCE[1])"` prints `True True True results/phase16_recall_sample.json`.
    - Option 1 only: the ROADMAP snapshot diff shows exactly 2 added lines and 0 removed; 29-04-SUMMARY.md has a "Required Phase-31 input" heading.
  </acceptance_criteria>
  <done>The full admission contract, scope rule and D-15 branch are frozen as code and every branch is tested on forged 12-point frontiers.</done>
</task>

<task type="auto">
  <name>Task 3: Phase gate — full suite on a committed tree; sign off VALIDATION.md</name>
  <files>.planning/phases/29-v5-0-pre-registration-and-carried-debt/29-VALIDATION.md</files>
  <read_first>
    - .planning/phases/29-v5-0-pre-registration-and-carried-debt/29-VALIDATION.md
  </read_first>
  <action>
Commit Tasks 1-2 first. Run `git status --porcelain` first (peer sessions may edit the tree) and stage these explicit paths only — never `git add -A`/`.`: `scripts/phase29_prereg.py`, `tests/test_phase29_prereg.py`, `.planning/phases/29-v5-0-pre-registration-and-carried-debt/29-04-SUMMARY.md`, `.planning/phases/29-v5-0-pre-registration-and-carried-debt/29-RESEARCH.md`, plus `.planning/ROADMAP.md` if option 1 was ruled (only if the Task-2 snapshot diff passed). After the commit assert the tree is clean except for the pre-existing lock deletion: `git status --porcelain | grep -v '^ D \.claude/scheduled_tasks\.lock$'` must print nothing (the lock is a tracked Claude-harness file deleted by the harness, outside `scripts/` `tests/` `results/`; the clean-tree probes are path-scoped — e.g. `git status --porcelain tests/` / `scripts/` — so it does not affect them; do not stage or restore it). Any other line -> stop, identify its owner, and do not launch the suite. Then, on the committed, clean tree (the 11 clean-tree probes need it), run the full suite in the background: `LOG="${TMPDIR:-/tmp}/phase29_suite.log"; nohup sh -c ".venv/bin/pytest -q -p no:cacheprovider > $LOG 2>&1; echo EXIT=\$? >> $LOG" &`, then wait with an `until grep -q '^EXIT=' "$LOG"; do sleep 30; done` loop in successive Bash calls each under 600 s (the suite takes ~25 min). Read the tail. If anything fails, diagnose it (a census biting a new file, a clean-tree probe run on a dirty tree) and fix the Phase-29 file, never a frozen module; re-run.

When green: in 29-VALIDATION.md set frontmatter `status: complete`, `nyquist_compliant: true`, `wave_0_complete: true`; fill each Per-Task Verification Map row's status with ✅ and the real command (note DEBT-03 lives in `tests/test_phase29_debt.py`, and the admission row maps to Plan 04 Task 2); tick the sign-off boxes; record the suite totals (passed / skipped / failed) and the skipped count compared with the last green run's skip count (no new skips allowed). After the Obsidian step below, commit the sign-off with explicit paths only: `.planning/phases/29-v5-0-pre-registration-and-carried-debt/29-VALIDATION.md` and `.planning/phases/29-v5-0-pre-registration-and-carried-debt/29-04-SUMMARY.md` (updated with the suite totals and the Obsidian outcome, both written before this commit), and re-assert the same filtered `git status --porcelain` is empty.

Finally (global CLAUDE.md): record the phase outcome in the Obsidian vault via the `obsidian` MCP server — read the PersonaCore project note in `01-Projects/`, append a dated entry (Phase 29 closed; the D-15 ruling; files and commit SHAs; suite totals; next step Phase 30). No secrets. If the MCP server is unavailable, say so explicitly in 29-04-SUMMARY.md and the final report ("Obsidian record pending") — never claim it was saved.
  </action>
  <verify>
    <automated>grep -q '^EXIT=0' "${TMPDIR:-/tmp}/phase29_suite.log" && tail -3 "${TMPDIR:-/tmp}/phase29_suite.log"</automated>
  </verify>
  <acceptance_criteria>
    - The suite log ends with `EXIT=0` and a pytest summary line with `0 failed` (or no "failed" token).
    - The skipped count equals the previous green run's skipped count (Phase 29 adds no skip).
    - 29-VALIDATION.md frontmatter reads `nyquist_compliant: true` and `status: complete`.
  </acceptance_criteria>
  <done>Phase 29 is green end to end on a committed tree and the validation contract is signed off.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| v5.0 frontier record → admission | Phase 33 feeds a Phase-32 artifact into this contract; a malformed or forged record must not admit |
| developer ruling → code | the D-15 choice must precede, not follow, the code that depends on it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-12 | Tampering | admission decided after the frontier exists | mitigate | whole contract committed now, ancestry-guarded (Plan 01 test re-runs on the edited module) |
| T-29-13 | Spoofing | a DP reading sourcing the adversarial threshold (WR-05) | mitigate | `recall_threshold(..., arm)` refuses non-advr arms; tested |
| T-29-14 | Tampering | malformed/forged frontier admitting a point | mitigate | INCONCLUSIVE precedence on count, unknown verdict strings and non-re-deriving tallies; tested with forged dicts |
| T-29-15 | Repudiation | all-refused frontier reported as "mitigation held" | mitigate | D-07 REFUSED verdict, D-08 MOOT reasons naming the refused leg; scope rule reasons fixed as strings |
| T-29-16 | Elevation of privilege | D-15-dependent code landing before the ruling | mitigate | blocking checkpoint first; `git diff --quiet -- scripts/phase29_prereg.py` at ruling time |
</threat_model>

<verification>
- `.venv/bin/pytest -q -p no:cacheprovider tests/test_phase29_prereg.py tests/test_phase29_debt.py` green; `tests/test_phase16_driver.py -k d28` and `tests/test_phase27_relearn.py -k untracked_record` green.
- Full suite EXIT=0 on a committed tree; no new skips; no `results/phase3*` file exists.
- The phase outcome is recorded in the Obsidian vault PersonaCore note under `01-Projects/` (or 29-04-SUMMARY.md states explicitly that the record is pending because the MCP server was unavailable).
</verification>

<success_criteria>
PREREG-02 frozen as code (four-plus verdicts, precedence, arm-keyed threshold, scope rule) per the developer's D-15 ruling; PREREG-03's REFUSED admission reading in place; the phase gate is green.
</success_criteria>

<output>
Create `.planning/phases/29-v5-0-pre-registration-and-carried-debt/29-04-SUMMARY.md` when done, including the verbatim D-15 ruling and the (b)/(c)/(d) confirmations.
</output>
</content>
