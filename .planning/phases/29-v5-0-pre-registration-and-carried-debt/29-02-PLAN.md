---
phase: 29-v5-0-pre-registration-and-carried-debt
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_phase27_relearn.py
  - tests/test_phase29_debt.py
  - .planning/milestones/v3.0-phases/17-multi-persona-isolation-matrix/17-01-SUMMARY.md
  - .planning/milestones/v3.0-phases/17-multi-persona-isolation-matrix/17-02-SUMMARY.md
  - .planning/milestones/v3.0-phases/17-multi-persona-isolation-matrix/17-03-SUMMARY.md
  - .planning/milestones/v3.0-phases/17-multi-persona-isolation-matrix/17-04-SUMMARY.md
  - .planning/milestones/v3.0-phases/17-multi-persona-isolation-matrix/17-05-SUMMARY.md
  - .planning/milestones/v3.0-phases/17-multi-persona-isolation-matrix/17-06-SUMMARY.md
  - .planning/milestones/v3.0-phases/17-multi-persona-isolation-matrix/17-07-SUMMARY.md
  - .planning/milestones/v3.0-phases/17-multi-persona-isolation-matrix/17-08-SUMMARY.md
  - .planning/milestones/v3.0-phases/17-multi-persona-isolation-matrix/17-09-SUMMARY.md
  - .planning/milestones/v3.0-phases/17-multi-persona-isolation-matrix/17-10-SUMMARY.md
  - .planning/milestones/v3.0-phases/17-multi-persona-isolation-matrix/17-11-SUMMARY.md
autonomous: true
requirements: [DEBT-01, DEBT-03]

must_haves:
  truths:
    - "D-16: test_a_leg_refuses_an_untracked_record_inside_the_repo writes its probe under a scratch git repo with relearn._ROOT monkeypatched to it, still asserts the 'not tracked' + 'REFUSING' refusal, and proves every tracked results/phase27_* file hashes unchanged and no stray appeared"
    - "D-18 (amended 2026-09-24): all 11 archived Phase-17 SUMMARYs carry top-level duration/completed copied from their nested metrics: values, metrics: untouched, and gsd-sdk frontmatter.validate --schema summary reports valid for each"
    - "D-18: a CI-safe test asserts for each of the 11 files: the 6 validator keys are present at top level, top-level == nested metrics values, and completed == the `git log --follow --diff-filter=A` first-add author date"
  artifacts:
    - path: "tests/test_phase27_relearn.py"
      provides: "DEBT-01 scratch-root probe test"
      contains: "monkeypatch.setattr(relearn, \"_ROOT\""
    - path: "tests/test_phase29_debt.py"
      provides: "DEBT-03 frontmatter test"
  key_links:
    - from: "tests/test_phase27_relearn.py"
      to: "scripts/phase27_relearn.py::_require_admitted"
      via: "module global _ROOT read at call time"
      pattern: "_require_admitted"
    - from: "tests/test_phase29_debt.py"
      to: "git log --follow --diff-filter=A"
      via: "subprocess"
      pattern: "--follow"
---

<objective>
Close DEBT-01 (IN-07) and DEBT-03 (TD-17-SUMMARY-FRONTMATTER). Both are independent of the pre-registration and of the D-15 ruling, so they run in wave 1.

Purpose: SC3's first and third clauses.
Output: an edited relearn test, 11 hand-edited archived SUMMARY frontmatters, and `tests/test_phase29_debt.py`.

The DEBT-03 test lives in its own file (not `tests/test_phase29_prereg.py`, which Plan 01 owns) so the two wave-1 plans share no file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-v5-0-pre-registration-and-carried-debt/29-CONTEXT.md
@.planning/phases/29-v5-0-pre-registration-and-carried-debt/29-RESEARCH.md
@.planning/phases/29-v5-0-pre-registration-and-carried-debt/29-PATTERNS.md

<interfaces>
tests/test_phase27_relearn.py (measured):
- :98-101 `_git(*args)` returns RAW stdout (no strip), cwd=_ROOT
- :109-116 autouse fixture `clean_tree(monkeypatch)` stubs relearn.refuse_if_dirty — unaffected
- :119-124 `_record(verdict, *, reasons=("forged",), baselines=None)`
- :182-194 `test_a_leg_refuses_an_untracked_record_inside_the_repo` — the offender: writes `_ROOT / "results/phase27_admission_probe_never_committed.json"` into the REAL repo
- :792-806 `_real_tree_strays()` — phase-27 write targets in the real tree
scripts/phase27_relearn.py — PINNED in its own PINNED_MODULES (:61-69); test_provenance_digests_match_live_bytes compares its bytes. NEVER EDIT. `_require_admitted(path)` (:128-170) reads module global `_ROOT` at call time (`path.resolve()`, `is_relative_to(_ROOT)`, `cwd=_ROOT`). `relearn.RECORD` was computed from the real root at import — always pass the probe path explicitly.
tests/test_phase25_driver.py:143-155 `_scratch_repo` — `git init -q -b main`, user.email/user.name config; no initial commit needed.

Phase-17 SUMMARY frontmatter (17-01 lines 1-51): top-level phase/plan/subsystem/tags present; `metrics:` block at :46-50 with `  duration: 34min` and `  completed: 2026-08-14`; closing `---` at :51.
Nested values (pattern map, measured) and --follow first-add dates (research, measured) — identical completed dates:
17-01 34min 2026-08-14 | 17-02 17min 2026-08-14 | 17-03 19min 2026-08-14 | 17-04 18min 2026-08-14 | 17-05 41min 2026-08-14 | 17-06 28min 2026-08-14 | 17-07 60min 2026-08-14 | 17-08 32min 2026-08-14 | 17-09 70min 2026-08-14 | 17-10 40min 2026-08-15 | 17-11 25min 2026-08-15
Plain `git log --diff-filter=A` on the archived path returns the archive commit 16a07d8 (2026-08-20) — WRONG; use `--follow`.
Validator: `~/.claude/get-shit-done/bin/lib/frontmatter.cjs:315` requires phase, plan, subsystem, tags, duration, completed at TOP LEVEL (presence only).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DEBT-01 — relearn probe under a scratch relearn._ROOT, with a no-touch guard (D-16)</name>
  <files>tests/test_phase27_relearn.py</files>
  <read_first>
    - tests/test_phase27_relearn.py (lines 1-130, 175-200, 785-810)
    - scripts/phase27_relearn.py (lines 30-70, 110-175 — read only)
    - tests/test_phase25_driver.py (lines 140-160)
  </read_first>
  <action>
Rewrite ONLY `test_a_leg_refuses_an_untracked_record_inside_the_repo` (lines 182-194) per D-16 / 27-REVIEW IN-07. Keep its name so the `-k untracked_record` selector still hits it; add `monkeypatch` and `tmp_path` params.

1. Before anything: snapshot `before = {rel: sha256(bytes)}` for every path in `_git("ls-files", "results/phase27_*").split()` (today only `results/phase27_admission.json`), and `strays_before = _real_tree_strays()`.
2. Build a scratch repo from `tmp_path.resolve()` (macOS /private symlink — `_require_admitted` resolves the path) — e.g. `scratch = tmp_path.resolve() / "repo"`; `git init -q -b main <scratch>` via subprocess with `check=True`; set user.email/user.name locally (the `_scratch_repo` precedent); `(scratch / "results").mkdir()`.
3. `monkeypatch.setattr(relearn, "_ROOT", scratch)`. Patch nothing else.
4. Write the forged record `json.dumps(_record("ADMITTED"))` to `scratch / "results/phase27_admission_probe_never_committed.json"`; call `relearn._require_admitted(probe)` with the path passed EXPLICITLY under `pytest.raises(SystemExit)`; assert `"not tracked"` and `"REFUSING"` in the message.
5. After: the same sha256 snapshot equals `before`; `_real_tree_strays() == strays_before`; `_git("status", "--porcelain", "--", "results/phase27_*").strip() == ""`; and the real-repo path `_ROOT / "results/phase27_admission_probe_never_committed.json"` does not exist.

Import `hashlib` if absent. Do not touch the adjacent test at ~:239 (out of IN-07 scope per research). No `skipif`; never write `== 10`/`!= 10`. Do not edit `scripts/phase27_relearn.py`.
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/pytest -q -p no:cacheprovider tests/test_phase27_relearn.py -k "untracked_record or provenance_digests"</automated>
  </verify>
  <acceptance_criteria>
    - The command exits 0 with at least 2 tests passed and 0 skipped.
    - `git diff --quiet -- scripts/phase27_relearn.py` exits 0.
    - `.venv/bin/python -c "import ast; t=ast.parse(open('tests/test_phase27_relearn.py').read()); f=[n for n in ast.walk(t) if isinstance(n, ast.FunctionDef) and n.name=='test_a_leg_refuses_an_untracked_record_inside_the_repo'][0]; s=ast.unparse(f); print('_ROOT' in s and 'monkeypatch.setattr' in s and 'git' in s and 'init' in s)"` prints `True`.
    - `git status --porcelain -- results/` prints nothing after the run.
  </acceptance_criteria>
  <done>The IN-07 probe never writes into the real repository; the refusal is still proven; a guard proves results/phase27_* is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: DEBT-03 — copy nested duration/completed to top level in 11 SUMMARYs, plus the test (D-18 amended)</name>
  <files>.planning/milestones/v3.0-phases/17-multi-persona-isolation-matrix/17-01-SUMMARY.md, .planning/milestones/v3.0-phases/17-multi-persona-isolation-matrix/17-02-SUMMARY.md, .planning/milestones/v3.0-phases/17-multi-persona-isolation-matrix/17-03-SUMMARY.md, .planning/milestones/v3.0-phases/17-multi-persona-isolation-matrix/17-04-SUMMARY.md, .planning/milestones/v3.0-phases/17-multi-persona-isolation-matrix/17-05-SUMMARY.md, .planning/milestones/v3.0-phases/17-multi-persona-isolation-matrix/17-06-SUMMARY.md, .planning/milestones/v3.0-phases/17-multi-persona-isolation-matrix/17-07-SUMMARY.md, .planning/milestones/v3.0-phases/17-multi-persona-isolation-matrix/17-08-SUMMARY.md, .planning/milestones/v3.0-phases/17-multi-persona-isolation-matrix/17-09-SUMMARY.md, .planning/milestones/v3.0-phases/17-multi-persona-isolation-matrix/17-10-SUMMARY.md, .planning/milestones/v3.0-phases/17-multi-persona-isolation-matrix/17-11-SUMMARY.md, tests/test_phase29_debt.py</files>
  <read_first>
    - .planning/milestones/v3.0-phases/17-multi-persona-isolation-matrix/17-01-SUMMARY.md (frontmatter, lines 1-55) and the first `---` block of each other file
    - .planning/milestones/v4.0-phases/27-relearning-attack/27-01-SUMMARY.md (lines 40-50 — top-level format precedent)
    - .planning/phases/29-v5-0-pre-registration-and-carried-debt/29-CONTEXT.md (D-18 and its 2026-09-24 amendment)
  </read_first>
  <action>
Per D-18 as AMENDED (developer ruling 2026-09-24): in each of the 11 files, inside the FIRST `---` frontmatter block, insert two top-level lines immediately before the closing `---`: `duration: <nested metrics.duration value>` and `completed: <nested metrics.completed value>`, copied verbatim from that file's own `metrics:` block (values in the interfaces table — but read each file, don't trust the table blindly). Leave `metrics:` and every other byte untouched. Do NOT write "not recorded at the time" (withdrawn by the amendment). Edit by hand with the Edit tool — NEVER `gsd-sdk frontmatter.set` or any gsd-sdk mutation handler (they corrupt frontmatter).

Create `tests/test_phase29_debt.py` (stdlib + subprocess; no skipif; no `== 10` text):
- `_frontmatter(path)` returns `(top, nested_metrics)`: split on the first two lines equal to `---`; top-level keys are lines matching `^([a-z_]+):\s*(.*)$` with no leading space; nested metrics are the indented `key: value` lines after a top-level `metrics:` line until the next top-level key.
- `SUMMARIES` = sorted glob of `.planning/milestones/v3.0-phases/17-multi-persona-isolation-matrix/17-*-SUMMARY.md`; assert exactly 11 (compare with `len(...) == 11`).
- `test_phase17_summary_frontmatter_validates` (parametrized over the 11): all of `phase, plan, subsystem, tags, duration, completed` in `top`; `top["duration"] == nested["duration"]`; `top["completed"] == nested["completed"]`; `top["completed"]` equals the LAST line of `git log --follow --diff-filter=A --format=%ad --date=short -- <path>` (the earliest add; plain `--diff-filter=A` returns the 2026-08-20 archive commit and is wrong).
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/pytest -q -p no:cacheprovider tests/test_phase29_debt.py && for f in .planning/milestones/v3.0-phases/17-multi-persona-isolation-matrix/17-*-SUMMARY.md; do gsd-sdk query frontmatter.validate "$f" --schema summary; done</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest -q -p no:cacheprovider tests/test_phase29_debt.py -k summary_frontmatter` exits 0 with 11 passed, 0 skipped.
    - Every one of the 11 `gsd-sdk query frontmatter.validate … --schema summary` outputs reports `"valid": true` with an empty `missing` list (read-only query; no mutation handler used).
    - Checked BEFORE committing (after the commit the working-tree diff is empty and the check is vacuous): `git diff --numstat -- .planning/milestones/v3.0-phases/17-multi-persona-isolation-matrix/` shows exactly `2	0` (2 added, 0 deleted) for each of the 11 files. If the edit is already committed, use `git diff --numstat HEAD~1 HEAD -- <that dir>` on the commit that made it instead.
    - `grep -rc "not recorded at the time" .planning/milestones/v3.0-phases/17-multi-persona-isolation-matrix/` reports 0 for every file.
  </acceptance_criteria>
  <done>All 11 archived Phase-17 SUMMARYs validate, with values that were recorded at the time, and a CI-safe test pins them to the nested values and the real first-add dates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → real repository | a test writing into `results/` of the real tree can pollute frozen artifacts or start ancestry clocks |
| archived planning files → validator | hand edits to milestone archives |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-07 | Tampering | results/phase27_* frozen records during tests | mitigate | scratch `relearn._ROOT`; sha256 before/after snapshot + `_real_tree_strays()` + `git status --porcelain` assertions |
| T-29-08 | Repudiation | fabricated durations/dates in archived SUMMARYs | mitigate | values copied from the files' own `metrics:`; test pins them to nested values and `--follow` first-add dates |
| T-29-09 | Tampering | frontmatter corruption by gsd-sdk mutation handlers | mitigate | hand edits only; read-only `frontmatter.validate` as acceptance; numstat shows +2/-0 per file |
</threat_model>

<verification>
- `.venv/bin/pytest -q -p no:cacheprovider tests/test_phase27_relearn.py -k "untracked_record or provenance_digests" tests/test_phase29_debt.py` exits 0.
- `git status --porcelain -- results/ scripts/phase27_relearn.py` is empty.
</verification>

<success_criteria>
DEBT-01 and DEBT-03 closed with green, CI-safe tests; no pinned module or frozen result touched.
</success_criteria>

<output>
Create `.planning/phases/29-v5-0-pre-registration-and-carried-debt/29-02-SUMMARY.md` when done.
</output>
</content>
