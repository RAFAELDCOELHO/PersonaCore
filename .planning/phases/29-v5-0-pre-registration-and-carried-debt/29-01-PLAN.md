---
phase: 29-v5-0-pre-registration-and-carried-debt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phase29_prereg.py
  - tests/test_phase29_prereg.py
autonomous: true
requirements: [PREREG-01, PREREG-03, PREREG-04, DEBT-04]

must_haves:
  truths:
    - "D-01: the 12 v5.0 point keys are advr_n8_ratio0p000000 … advr_n64_ratio1p909091, rendered by wrapping (never editing) phase25_record.point_key, and every v4.0 key parser (phase25_record.parse_point_key, phase27_prereg.arm_of) refuses every one of them"
    - "D-02/D-03: every v5.0 results path lives in ONE tuple V5_RESULT_PATHS; the ancestry pathspecs are DERIVED from it and equal exactly results/phase30_* … results/phase34_*; no calibration/probe path parses as a point key"
    - "PREREG-01 SC1: a CPU-only ancestry test proves every commit touching scripts/phase29_prereg.py is a strict ancestor of the first-add of every tracked results/phase3[0-4]_* file (honest-green with zero tracked files today)"
    - "D-04: replay_windows(8) == 32 and replay_windows(64) == 256 via teach_persona.replay_window_budget(n) // teach_persona.BLOCK_SIZE imported LAZILY; the module imports without torch; no numeric literal equal to REPLAY_WINDOWS_PER_FACT and no assignment of that name exists in the module (AST guard, watched RED on a tmp copy)"
    - "D-05: the gate route, F_Y and the grid are imported by reference (asserted with `is`); an AST guard reddens on a re-typed grid, a 0.7 literal, or a local definition/import of the gate"
    - "D-11/D-13: control_is_unlearnable agrees with phase20_gate_coverage.corrected_point_verdict's floor refusal on both sides of the boundary, and the REFUSED record carries k/n counts, the recipe identity and the v4.0 adv_n64 reading (taught 1/1008, held-out 0/648) re-read from results/phase25_frontier.json"
    - "D-14: the module exposes no key other than the 12 and no retry/alternate/rerun surface"
    - "D-12: leg_keys(leg) is derived from POINT_KEYS(), starts with control_key(leg) and has exactly len(RATIO_GRID) keys — the set a refused control short-circuits to REFUSED records"
    - "D-19: NAMED_LIMITATIONS carries the P22-WARNING-4/5 entry (reason, source, ledger rows, the transitive-load fact) and an AST census over scripts/phase29_*.py … phase34_*.py finds no accountant import or call"
  artifacts:
    - path: "scripts/phase29_prereg.py"
      provides: "v5.0 pre-registration: keys, paths, pathspecs, replay recipe, by-reference gate/grid/F_Y, refusal predicate + REFUSED record shape, named limitations"
      contains: "ADVR_ARMS"
    - path: "tests/test_phase29_prereg.py"
      provides: "ancestry guard, key/path tests, by-reference `is` tests, AST guards with watched RED, refusal differential, torch-free probe, accountant census"
  key_links:
    - from: "scripts/phase29_prereg.py"
      to: "phase25_record.point_key"
      via: "arm + phase25_record.point_key(twin, ratio)[len(twin):]"
      pattern: "phase25_record\\.point_key"
    - from: "scripts/phase29_prereg.py"
      to: "teach_persona.replay_window_budget"
      via: "lazy import inside replay_windows()"
      pattern: "replay_window_budget"
    - from: "tests/test_phase29_prereg.py"
      to: "phase29_prereg.ARTIFACT_PATHSPECS"
      via: "git ls-files per pathspec -> _assert_frozen_before"
      pattern: "ARTIFACT_PATHSPECS"
---

<objective>
Create the D-15-INDEPENDENT body of the v5.0 pre-registration, `scripts/phase29_prereg.py`, and its test file. It pins the 12 keys (D-01), every v5.0 results path and the derived ancestry pathspecs (D-02, D-03), the lazily imported replay recipe (D-04), the by-reference gate route/grid/F_Y (D-05), the unlearnable-own-control refusal predicate and REFUSED record shape (D-11..D-14), and the named limitations (D-19 DEBT-04, plus the D-17 TD-16-R1 report-absence entry that Plan 03's test reads).

Purpose: SC1 + SC2's refusal half + SC4 — frozen as code before any v5.0 number exists.
Output: `scripts/phase29_prereg.py`, `tests/test_phase29_prereg.py`.

NOT in this plan (D-15-dependent, Plan 04 after the ruling): the admission contract, verdict tuple, scope rule, D-09 relearning pins, any promotion path. Do not add them here — CONTEXT D-15 forbids committing anything D-15-dependent before the ruling.

Ancestry note: the guard only requires every prereg commit to precede the first `results/phase3*` commit (Phase 30). Plan 04's later edit to this module is legal; after Phase 30's first results commit, corrections go only through `scripts/_addendum.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/29-v5-0-pre-registration-and-carried-debt/29-CONTEXT.md
@.planning/phases/29-v5-0-pre-registration-and-carried-debt/29-RESEARCH.md
@.planning/phases/29-v5-0-pre-registration-and-carried-debt/29-PATTERNS.md

<interfaces>
Measured at HEAD (research ffa0f19, pattern map 95ed328). Use these exact names; do not explore for alternatives.

scripts/phase27_prereg.py (analog; PINNED — read, never edit):
- lines 1-38 docstring register (WHAT THIS FREEZES. / ANCESTRY-GUARDED. / CPU-ONLY AT IMPORT. / THE ROUTE, NEVER THE PIN. / Threats mitigated:)
- :45-56 _REPO_ROOT + sys.path insert of scripts/ + `import x  # noqa: E402`
- :64 COMMITTED, :65 RECORDS_AT_COMMIT = 0, :71 _prove(condition, message) -> SystemExit("[phase27_prereg] ...")
- :213 attacker_corpus_rows() — the lazy-import precedent (imports teach_persona inside the body)
- :484 _prove_count(name, value) — int-not-bool refusal
scripts/phase25_record.py (frozen): point_key(arm, value) :199-227 refuses unknown arms via AXIS_FOR_ARM (:161-166, only dp_*/adv_*); parse_point_key :230-248; ORDERED_POINT_KEYS() :253 (lazy function precedent). point_key("adv_n8", 0.0) == "adv_n8_ratio0p000000". Importing phase25_record loads phase25_epsilon -> personacore.privacy.accountant (line 82) — transitive, unavoidable.
scripts/mitigation_budget.py:633 ADVERSARIAL_RATIO_GRID == (0.0, 0.25, 0.5, 1.0, 1.5, 1.9090909090909092)
scripts/mitigation_gate.py: F_Y (:203) == 0.7; mitigation_point_verdict (:637) — NEVER import or call it by name (census tests/test_phase20_correction.py:1377)
scripts/phase20_gate_coverage.py:522 corrected_point_verdict(**24 kwargs); floor precondition 0.0 < F_Y*control_* <= 1.0 at :641-647, refusing with SystemExit
scripts/phase25_promotion.py:53 COVERAGE_FLOOR_REFUSAL_MARKERS == ('[phase20_gate_coverage] the recall floors came out', 'both must lie in (0.0, 1.0]') — importing phase25_promotion is torch-free (measured)
scripts/teach_persona.py (torch at import — LAZY ONLY): :99-100 DIALOG_TRAIN_BIN / DIALOG_TRAIN_MASK; :105 BLOCK_SIZE = 256; :179 REPLAY_WINDOWS_PER_FACT = 4; :182 replay_window_budget(n_facts, block_size=BLOCK_SIZE) returns TOKENS; DP call site :1968-1970 `replay_windows=replay_window_budget(stats["n_facts"]) // BLOCK_SIZE`; :310 ADV_ARMS = ("adv_n8", "adv_n64")
results/phase25_frontier.json (22 MB, never at import): verdicts.control_readings.adv_n64.recall_counts == {taught: [1, 1008], heldout: [0, 648]}; adv_n8 == [879,1008]/[482,648]
tests/test_phase27_prereg.py: _git :57-61; _assert_frozen_before :74-111 (copy UNCHANGED); torch-free probe :133-142; by-reference `is` :150-168; AST float scan :335-345; route kwargs :288-301; frontier module fixture :64-66
tests/test_phase27_relearn.py:392-406 — mutation-proved guard on a tmp copy (the real file is never touched)
tests/test_phase20_correction.py:1377-1458 — AST call census matching node.func .id and .attr, ImportFrom alias names, non-vacuity assertion
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write scripts/phase29_prereg.py (D-15-independent body)</name>
  <files>scripts/phase29_prereg.py</files>
  <read_first>
    - scripts/phase27_prereg.py (lines 1-125, 213-235, 467-492 — register, by-reference block, lazy import, _prove_count)
    - scripts/phase25_record.py (lines 150-260 — point_key/_axis_for/parse_point_key/ORDERED_POINT_KEYS)
    - scripts/phase20_gate_coverage.py (lines 522-700 — the route and the floor precondition)
    - scripts/phase25_promotion.py (lines 40-60 — COVERAGE_FLOOR_REFUSAL_MARKERS)
    - scripts/teach_persona.py (lines 95-110, 175-216, 1960-1975 — replay constants and DP call site)
    - .planning/phases/29-v5-0-pre-registration-and-carried-debt/29-RESEARCH.md (Patterns 1-6, §DEBT-04, §DEBT-02 absence finding)
  </read_first>
  <action>
Create `scripts/phase29_prereg.py`, stdlib-only at import, following the `phase27_prereg.py` register (read it; never edit it).

Module docstring with the headings WHAT THIS FREEZES. / ANCESTRY-GUARDED. (name `tests/test_phase29_prereg.py` and `ARTIFACT_PATHSPECS`) / CPU-ONLY AT IMPORT. (name the lazy `teach_persona` import) / THE ROUTE, NEVER THE PIN. / Threats mitigated:. State in it that (a) the admission contract, scope rule, D-09 relearning pins and any promotion path are added after the D-15 ruling (Plan 04) and (b) `personacore.privacy.accountant` is loaded TRANSITIVELY through `phase25_record` -> `phase25_epsilon` and no v5.0 call reaches it (D-19). Docstrings may mention any number; the AST guards only look at code constants.

Imports (module level, with the phase27 sys.path insert and `# noqa: E402`): `mitigation_budget`, `mitigation_gate`, `phase20_gate_coverage`, `phase25_promotion`, `phase25_record`. Never import `teach_persona`, `phase25_epsilon`, `personacore.privacy*` at module level, and never import or name `mitigation_point_verdict` (D-05).

Contents, in order:
1. `COMMITTED = "<execution date, date +%F>"`, `RECORDS_AT_COMMIT = 0`, `_prove(condition, message)` raising `SystemExit(f"[phase29_prereg] {message}")`, and `_prove_count(name, value)` copied in phase27's register (int and not bool).
2. By-reference aliases (D-05): `RATIO_GRID = mitigation_budget.ADVERSARIAL_RATIO_GRID`, `F_Y = mitigation_gate.F_Y`, `GATE_ROUTE = phase20_gate_coverage.corrected_point_verdict`, `COVERAGE_FLOOR_REFUSAL_MARKERS = phase25_promotion.COVERAGE_FLOOR_REFUSAL_MARKERS`.
3. Keys (D-01, D-14). `ADVR_ARMS = ("advr_n8", "advr_n64")` — RESOLVED CHOICE (research open question 3): this tuple is the seam Phase 30 must IMPORT from here; the docstring states it must never be appended to `teach_persona.ADV_ARMS` (that makes `phase25_verdict.curve_verdicts` refuse "matches two legs"). `_V4_TWIN = {"advr_n8": "adv_n8", "advr_n64": "adv_n64"}`. `LEGS = ("n8", "n64")`. `point_key(arm, ratio)`: `_prove(arm in ADVR_ARMS, ...)`, then return `arm + phase25_record.point_key(twin, ratio)[len(twin):]` (inherits the finite/negative/charset refusals). `POINT_KEYS()` — a FUNCTION (lazy, like `ORDERED_POINT_KEYS`), arm-major over `RATIO_GRID`, so each leg's ratio-0 control comes first (ACTRL-02 order). `control_key(leg)` returns `point_key(f"advr_{leg}", RATIO_GRID[0])` after `_prove(leg in LEGS)`. `leg_keys(leg)` (D-12 short-circuit set): after `_prove(leg in LEGS)`, returns `tuple(k for k in POINT_KEYS() if k.startswith(f"advr_{leg}_"))` — DERIVED from `POINT_KEYS()`, never a second rendering; a refused control means every key in `leg_keys(leg)` gets a REFUSED record and none is trained. No other key-producing function, and no public name containing retry/alternate/rerun (D-14).
4. Paths (D-02, D-03). `POINT_RECORD_PREFIX = "results/phase32_point_"`; `point_record_path(key)` proves `key in POINT_KEYS()` and returns `f"{POINT_RECORD_PREFIX}{key}.json"`. `V5_RESULT_PATHS` — RESOLVED CHOICE (research open question on Phase 30/31/33/34 filenames, assumption A1), one tuple, each entry commented with the requirement that writes it: `results/phase30_calibration.json` (ARECIPE-02), `results/phase31_probe_point.json` (ARCAL-01), `results/phase31_probe_relearn.json` (ARCAL-02), `results/phase31_budget.json` (ARCAL-03), `POINT_RECORD_PREFIX + "*.json"` (AFRONT-01), `results/phase32_frontier.json` (AFRONT-02), `results/phase33_admission.json` (ADMIT-02), `results/phase33_*` (RELRN-06..09 legs), `results/phase34_*` (RPT-04). Later phases import these names; they never retype them. `ARTIFACT_PATHSPECS` DERIVED, not typed: `tuple(sorted({p.split("_", 1)[0] + "_*" for p in V5_RESULT_PATHS}))` (a tuple, so the acceptance print below matches) (yields exactly `results/phase30_*` … `results/phase34_*`, i.e. D-02's `results/phase3[0-4]_*`).
5. Replay (D-04, PREREG-04). `replay_windows(n_facts)`: imports `teach_persona` INSIDE the body, computes `teach_persona.replay_window_budget(n_facts) // teach_persona.BLOCK_SIZE` (the DP expression at teach_persona.py:1970), proves it equals `teach_persona.REPLAY_WINDOWS_PER_FACT * int(n_facts)`, returns it. `REPLAY_SOURCE = ("teach_persona.DIALOG_TRAIN_BIN", "teach_persona.DIALOG_TRAIN_MASK")` — names, not paths. NO numeric literal equal to 4 anywhere in the module (no `range(4)`, no `[:4]`, no `== 4`), and no assignment to a name `REPLAY_WINDOWS_PER_FACT` (the Task-3 guard reddens on either).
6. Refusal (D-11, D-12, D-13, PREREG-03). `control_is_unlearnable(taught_k, taught_n, heldout_k, heldout_n)`: `_prove_count` all four, prove `0 <= k <= n` and `n > 0`, return `not all(0.0 < F_Y * (k / n) <= 1.0 for each pair)` — the route's own inequality (phase20_gate_coverage.py:641-647) on COUNTS, `F_Y` by reference. `V4_ADV_N64_READING = {"taught": (1, 1008), "heldout": (0, 648), "source": "results/phase25_frontier.json::verdicts.control_readings.adv_n64.recall_counts"}` (a pinned reading, re-read by a test; do not load the frontier at import). `REFUSED_RECORD_FIELDS = ("point_key", "control_key", "control_recall_counts", "recipe", "v4_adv_n64_reading", "rule")`. `refused_record(key, *, taught, heldout, recipe)`: proves `key in POINT_KEYS()`, `taught`/`heldout` are `(k, n)` count pairs, `control_is_unlearnable(*taught, *heldout)` is True, and `recipe` has exactly the keys `replay_windows`, `n_facts`, `seed`, `max_steps`; returns a dict with exactly `REFUSED_RECORD_FIELDS` (`control_key` = the key's leg control, `control_recall_counts = {"taught": [k, n], "heldout": [k, n]}`, `v4_adv_n64_reading = V4_ADV_N64_READING`, `rule = "PREREG-03"`). It writes nothing (Phase 32 writes the record, write-once).
7. `NAMED_LIMITATIONS` dict (D-19, D-17):
   - `"P22-WARNING-4/5"`: `reason` = "no v5.0 number uses the accountant; the adversarial arm carries no ε claim (Phase 25 D-01)", `source` = ".planning/milestones/v4.0-phases/22-*/22-VERIFICATION.md:149-183" (resolve the exact directory name with ls and write it out), `ledger_rows = ("P22-WARNING-4", "P22-WARNING-5")`, `transitive_load` = a sentence stating the accountant is loaded transitively via phase25_record -> phase25_epsilon and that no v5.0 module imports or calls it (census in tests/test_phase29_prereg.py).
   - `"TD-16-R1-REPORT"`: `reason` = the D-28 READING QUALIFICATION's verbatim kernel is absent from the published `results/phase16_persistence_report.md`; the published bytes are kept (D-17) and the note is read at runtime by `phase16_persistence.d28_note()`; `ledger_rows = ("TD-16-R1",)`.
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/python -c "import sys; sys.path.insert(0,'scripts'); import phase29_prereg as p; print('torch' in sys.modules, 'teach_persona' in sys.modules, len(p.POINT_KEYS()), p.ARTIFACT_PATHSPECS, p.replay_windows(8), p.replay_windows(64))"</automated>
  </verify>
  <acceptance_criteria>
    - The command above prints `False False 12 ('results/phase30_*', 'results/phase31_*', 'results/phase32_*', 'results/phase33_*', 'results/phase34_*')` before the replay call and `32 256` at the end (the replay call imports torch afterwards; the first two booleans are captured before it).
    - `.venv/bin/python -c "import ast,sys; t=ast.parse(open('scripts/phase29_prereg.py').read()); print([n.value for n in ast.walk(t) if isinstance(n, ast.Constant) and type(n.value) in (int,float) and n.value in (4, 0.7, 1.9090909090909092)])"` prints `[]`.
    - `.venv/bin/python -c "import sys; sys.path.insert(0,'scripts'); import phase29_prereg as p; print(p.POINT_KEYS()[0], p.POINT_KEYS()[-1], p.control_key('n64'))"` prints `advr_n8_ratio0p000000 advr_n64_ratio1p909091 advr_n64_ratio0p000000`.
    - `git diff --stat -- scripts/phase25_record.py scripts/phase27_prereg.py scripts/mitigation_gate.py scripts/phase20_gate_coverage.py scripts/teach_persona.py scripts/phase25_promotion.py` prints nothing.
    - No file matching `results/phase3*` exists (`find results -name 'phase3*' | wc -l` prints 0).
  </acceptance_criteria>
  <done>The module imports torch-free, renders the 12 keys and the derived pathspecs, and computes replay 32/256 through the lazily imported DP expression; no frozen module changed.</done>
</task>

<task type="auto">
  <name>Task 2: Tests — ancestry, keys, paths, by-reference, replay, refusal differential, D-14</name>
  <files>tests/test_phase29_prereg.py</files>
  <read_first>
    - tests/test_phase27_prereg.py (lines 1-185, 288-345)
    - scripts/phase29_prereg.py (as written in Task 1)
    - tests/test_phase20_correction.py (search `_corrected_call` for the route-kwargs shape)
  </read_first>
  <action>
Create `tests/test_phase29_prereg.py`. Header docstring enumerating what the file proves; `_ROOT`; insert `scripts/` and `src/` into `sys.path`; `import phase29_prereg  # noqa: E402`. Copy `_git` (phase27 test :57-61) and `_assert_frozen_before` (:74-111) UNCHANGED. NO `skipif`/`skip` anywhere (CI skip-count pin). Never write the text `== 10` or `!= 10` (tests/test_phase21_sc5.py census). Never write under `results/` — use `tmp_path` only (a `results/phase3*` file would start the ancestry clock).

Tests (name them so the `-k` selectors below hit them):
- `test_phase29_prereg_is_frozen_before_every_v5_result`: `tracked` = sorted union of `_git("ls-files", spec).split()` over `phase29_prereg.ARTIFACT_PATHSPECS`; call `_assert_frozen_before("scripts/phase29_prereg.py", tracked)` (honest-green with zero pairs today).
- `test_pathspecs_are_derived_and_cover_results_phase30_to_34` (D-02, D-03): `ARTIFACT_PATHSPECS` equals the tuple of `results/phase3N_*` for N in 0..4 (build the expected tuple with a loop over `range(5)`), and equals the derivation recomputed from `V5_RESULT_PATHS`; every `V5_RESULT_PATHS` entry matches one of them via `fnmatch`.
- `test_paths_are_distinct_from_every_v4_path`: no `V5_RESULT_PATHS` entry starts with `results/phase2`; no currently tracked file (`git ls-files results`) fnmatches any entry.
- `test_no_probe_or_calibration_path_parses_as_a_point_key` (ARCAL SC4): for each concrete entry (no `*`), the stem with `phase32_point_` removed is not in `POINT_KEYS()`, and `phase25_record.parse_point_key(stem)` raises SystemExit.
- `test_keys_*` (D-01): 12 unique keys; replacing the leading `advr` with `adv` gives exactly `phase25_record.point_key(twin, r)` for every arm/ratio; `phase25_record.parse_point_key(k)` and `phase27_prereg.arm_of(k)` raise SystemExit for every v5.0 key; `point_key("adv_n8", 0.25)`, `point_key("advr_n8", float("nan"))` and `point_key("advr_n8", -0.25)` raise SystemExit; each leg's first key is `control_key(leg)`.
- `test_leg_keys_are_the_d12_short_circuit_set` (D-12): for each leg, `leg_keys(leg)[0] == control_key(leg)`, `len(leg_keys(leg)) == len(RATIO_GRID)`, every member is in `POINT_KEYS()`, the two legs' tuples are disjoint and their concatenation in `LEGS` order equals `tuple(POINT_KEYS())`; `leg_keys("n16")` raises SystemExit.
- `test_constants_are_by_reference`: `F_Y is mitigation_gate.F_Y`, `RATIO_GRID is mitigation_budget.ADVERSARIAL_RATIO_GRID`, `GATE_ROUTE is phase20_gate_coverage.corrected_point_verdict`, `COVERAGE_FLOOR_REFUSAL_MARKERS is phase25_promotion.COVERAGE_FLOOR_REFUSAL_MARKERS`.
- `test_the_prereg_imports_without_torch`: subprocess probe (phase27 :133-142 shape) printing `'torch' in sys.modules, 'teach_persona' in sys.modules` after `import phase29_prereg` -> `["False", "False"]`.
- `test_replay_windows_equal_the_dp_expression` (D-04): import `teach_persona` inside the test; for n in (8, 64): `replay_windows(n) == teach_persona.replay_window_budget(n) // teach_persona.BLOCK_SIZE == teach_persona.REPLAY_WINDOWS_PER_FACT * n`; also assert the explicit 32 and 256; `REPLAY_SOURCE` names resolve by `getattr(teach_persona, name.split(".")[1])` to `data/dialog_train.bin` and `data/dialog_train_mask.bin` (compare the path tail).
- `test_unlearnable_predicate_agrees_with_the_route` (D-11): module-scope fixture loading `results/phase25_frontier.json` (the test may load it; the module may not). Take `points["adv_n64_ratio0p000000"]` route kwargs in the phase27 `_route_kwargs` shape; set the control heldout recall to 0/648 -> `control_is_unlearnable(1, 1008, 0, 648) is True` AND `GATE_ROUTE(**kwargs)` raises SystemExit whose text contains both `COVERAGE_FLOOR_REFUSAL_MARKERS`; set it to 1/648 -> predicate False and the route raises no floor-refusal (either returns or raises without the markers). Also assert predicate True for taught 0/1008.
- `test_v4_adv_n64_reading_re_reads_from_the_frontier` (D-13): `V4_ADV_N64_READING["taught"] == tuple(frontier["verdicts"]["control_readings"]["adv_n64"]["recall_counts"]["taught"])` and likewise heldout.
- `test_refused_record_*` (D-12, D-13): `refused_record(POINT_KEYS()[7], taught=(1,1008), heldout=(0,648), recipe={...4 keys...})` has keys == set(REFUSED_RECORD_FIELDS), counts as `[k, n]` lists, `control_key` == the leg's control; it raises SystemExit for a learnable control (heldout 482/648), a bool count, a key outside POINT_KEYS(), and a recipe with an extra key.
- `test_no_retry_or_alternate_key_is_exposed` (D-14): every public module attribute that is a str or tuple of str and matches the regex `advr_n\d+_ratio` is a member of `POINT_KEYS()`; no public name (via `dir(phase29_prereg)`, not grep) contains `retry`, `alternate`, `rerun` or `retune` (case-insensitive).
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/pytest -q -p no:cacheprovider tests/test_phase29_prereg.py</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest -q -p no:cacheprovider tests/test_phase29_prereg.py -k "frozen_before or pathspecs or paths or keys or leg_keys or by_reference or without_torch or replay or unlearnable or v4_adv or refused or retry"` exits 0 with 0 skipped.
    - AST check (not grep — the header docstring may mention skips): `.venv/bin/python -c "import ast; t=ast.parse(open('tests/test_phase29_prereg.py').read()); print([getattr(n,'id',None) or n.attr for n in ast.walk(t) if isinstance(n,(ast.Name,ast.Attribute)) and (getattr(n,'id',None) or getattr(n,'attr',None)) in ('skip','skipif','skipIf')])"` prints `[]`; `grep -cE "[=!]= 10" tests/test_phase29_prereg.py` prints 0.
    - `find results -name 'phase3*' | wc -l` prints 0 after the run.
  </acceptance_criteria>
  <done>All D-01..D-05, D-11..D-14 behaviours are asserted and green; the ancestry test runs with zero tracked v5.0 files.</done>
</task>

<task type="auto">
  <name>Task 3: AST guards with watched RED (D-04, D-05) and the DEBT-04 accountant census (D-19)</name>
  <files>tests/test_phase29_prereg.py</files>
  <read_first>
    - tests/test_phase27_relearn.py (lines 380-410 — mutation-proved guard on a tmp copy)
    - tests/test_phase20_correction.py (lines 1377-1458 — AST call/import census, non-vacuity)
    - scripts/phase25_epsilon.py (imports of the accountant — the non-vacuity target)
    - scripts/phase29_prereg.py
  </read_first>
  <action>
Append to `tests/test_phase29_prereg.py`. Each guard is a pure function `_x_failures(source, ...) -> list` run twice: on the real `scripts/phase29_prereg.py` source (expect `[]`) and on a planted copy written to `tmp_path` (expect non-empty), with an assertion that the planted string differs from the source and that the real file's bytes are unchanged afterwards. Never plant into the real file (natural-RED rule).

Name the three guard tests exactly `test_ast_replay_literal_guard`, `test_ast_grid_retype_guard`, `test_ast_gate_retype_guard` (each runs its `_x_failures` on the real source and on the planted copy).

- `_replay_literal_failures(source, value)` (D-04): every `ast.Constant` with `type(node.value) in (int, float)` and `node.value == value`, plus every module-level `Assign`/`AnnAssign` target named `REPLAY_WINDOWS_PER_FACT`. `value` = `teach_persona.REPLAY_WINDOWS_PER_FACT` imported inside the test. Plant: append a line assigning `REPLAY_WINDOWS_PER_FACT` a value, and separately a line with a literal computing `n * <value>` via `str(value)`.
- `_grid_retype_failures(source, grid)` (D-05): any `Tuple`/`List` whose all-constant elements equal `grid`; any float constant equal to `grid[-1]`; any module-level target named `ADVERSARIAL_RATIO_GRID` or `RATIO_GRID` whose value is not an `ast.Attribute` with attr `ADVERSARIAL_RATIO_GRID`. Plant: replace the `RATIO_GRID = mitigation_budget.ADVERSARIAL_RATIO_GRID` right-hand side with `repr(grid)`.
- `_gate_retype_failures(source, f_y)` (D-05): any float constant equal to `f_y`; any `FunctionDef` named `mitigation_point_verdict`, `corrected_point_verdict` or `cleared_abc`; any Import/ImportFrom alias or Call (`.id` or `.attr`) named `mitigation_point_verdict`. Plant: append a `def corrected_point_verdict` stub, and separately replace `mitigation_gate.F_Y` with `repr(f_y)`.
- `_accountant_failures(source)` (D-19, DEBT-04): any `Import`/`ImportFrom` whose module or alias is `phase25_epsilon` or starts with `personacore.privacy`; any `Name.id` or `Attribute.attr` in `{"epsilon_for", "sigma_for", "delta_closed", "delta_quadrature"}`. `test_no_v5_module_uses_the_accountant`: iterate over `sorted(p for n in range(29, 35) for p in (_ROOT / "scripts").glob(f"phase{n}_*.py"))` (derived by glob so Phases 30-34 are covered when they land); assert the list is non-empty and every file yields `[]`. Non-vacuity: `_accountant_failures(scripts/phase25_epsilon.py source)` is non-empty. Do NOT assert anything about `sys.modules` (importing phase25_record loads the accountant; that assertion would be false).
- `test_named_limitations_record_p22_warning_4_5`: the entry exists with the four fields; `ledger_rows` each appear as a row id in `results/phase28_ledger.json` (read only; locate rows by walking the JSON for those strings rather than assuming a schema key); the `source` file exists (glob-resolved).
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/pytest -q -p no:cacheprovider tests/test_phase29_prereg.py</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest -q -p no:cacheprovider tests/test_phase29_prereg.py::test_ast_replay_literal_guard tests/test_phase29_prereg.py::test_ast_grid_retype_guard tests/test_phase29_prereg.py::test_ast_gate_retype_guard tests/test_phase29_prereg.py::test_no_v5_module_uses_the_accountant tests/test_phase29_prereg.py::test_named_limitations_record_p22_warning_4_5` exits 0 with 5 passed.
    - The whole file passes: `.venv/bin/pytest -q -p no:cacheprovider tests/test_phase29_prereg.py` exits 0, 0 skipped.
    - `.venv/bin/pytest -q -p no:cacheprovider tests/test_phase20_correction.py::test_mitigation_point_verdict_has_no_caller_outside_this_module tests/test_phase21_sc5.py::test_wall_census_is_the_measured_set tests/test_phase25_driver.py::test_os_replace_appears_only_in_the_two_phase25_writers` exits 0 (repo censuses still green with the new files).
    - `git status --porcelain -- scripts/phase29_prereg.py` shows only the Task-1 creation (the guards never modified it).
  </acceptance_criteria>
  <done>Every "imported, never re-typed" claim and DEBT-04's "no v5.0 module uses the accountant" is an AST guard that is green on the real module and demonstrably RED on a planted copy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pre-registration → later phases | Phases 30-34 import keys/paths/recipe from this module; a post-hoc edit would re-tune after seeing data |
| key string → filesystem path | `point_record_path(key)` builds a results path from a key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01 | Tampering | scripts/phase29_prereg.py after data exists | mitigate | Ancestry test `test_phase29_prereg_is_frozen_before_every_v5_result` over DERIVED `ARTIFACT_PATHSPECS`; later corrections only via `scripts/_addendum.py` |
| T-29-02 | Tampering | re-tune after a refused control (D-14) | mitigate | 12 write-once keys, `test_no_retry_or_alternate_key_is_exposed`, `refused_record` requires an unlearnable control |
| T-29-03 | Tampering | path traversal via key | mitigate | `point_key` wraps `phase25_record.point_key` (charset/finite/negative refusals); `point_record_path` proves `key in POINT_KEYS()` |
| T-29-04 | Spoofing | DP reading used as the adversarial floor (WR-05) | mitigate | `control_key(leg)` is `advr`-keyed; v4.0 parsers refuse `advr_*` keys (tested) |
| T-29-05 | Tampering | re-typed gate/grid/replay constant drifts from the frozen source | mitigate | `is` tests + three AST guards, each watched RED on a tmp copy |
| T-29-06 | Information disclosure | accountant ε leaking into a v5.0 number | mitigate | AST census over scripts/phase29_*..phase34_*; entry states the transitive load honestly |
</threat_model>

<verification>
- `.venv/bin/pytest -q -p no:cacheprovider tests/test_phase29_prereg.py` exits 0, 0 skipped.
- Torch-free import probe prints `False False`.
- No frozen v4.0 module in the git diff; no `results/phase3*` file exists.
</verification>

<success_criteria>
PREREG-01 (keys, paths, ancestry, imported gate), PREREG-03 (refusal predicate + REFUSED record shape), PREREG-04 (lazy replay recipe) and DEBT-04 (named limitation + accountant census) are code and green tests, committed before any v5.0 results file.
</success_criteria>

<output>
Create `.planning/phases/29-v5-0-pre-registration-and-carried-debt/29-01-SUMMARY.md` when done. Record the two RESOLVED CHOICES (ADVR_ARMS seam; V5_RESULT_PATHS filenames) so the D-15 checkpoint in Plan 04 can present them for veto.
</output>
</content>
</invoke>
