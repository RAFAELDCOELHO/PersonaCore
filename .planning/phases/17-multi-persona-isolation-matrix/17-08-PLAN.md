---
phase: 17-multi-persona-isolation-matrix
plan: 08
type: execute
wave: 4
depends_on: ["17-06"]
files_modified:
  - scripts/phase17_isolation.py
  - tests/test_phase17_stats.py
autonomous: true
requirements: [STAT-01, STAT-02, STAT-03, STAT-06, ISO-03, ISO-05]

must_haves:
  truths:
    - "The gate verdict is computed by importing the constants committed in plan 17-01, never by retyping them"
    - "Signs are computed on the question rate, never the draw rate"
    - "Every proportion carries its denominator, a Wilson bound and — at zero — the rule of three, at both ends of the clustering assumption"
    - "The all-fail branch is committed before any number exists and fires on anything less than six Holm rejections"
    - "All six Holm rows are published whatever the outcome — publishing the rows is not clearing the gate"
    - "No aggregate rate over the nine cells is computed or printed"
  artifacts:
    - path: "scripts/phase17_isolation.py"
      provides: "--report mode: cross-process proof, scoring, matrix assembly, the inferential gate, descriptive CIs, and the report writer with the D-10 branch"
      contains: "def run_report_mode"
    - path: "tests/test_phase17_stats.py"
      provides: "Question-unit assertion, bare-zero-percent guard, all-fail branch, the D-21 six-pairs twin against the isolation driver"
      contains: "def test_signs_use_the_question_unit"
  key_links:
    - from: "scripts/phase17_isolation.py"
      to: "scripts/phase16_persistence.py"
      via: "fact_signs / sign_test_exact / holm / cluster_bootstrap / report_proportion — imported unchanged, STAT-04 satisfied by import"
      pattern: "sign_test_exact"
    - from: "scripts/phase17_isolation.py"
      to: "scripts/phase17_personas.py"
      via: "gate_cleared / HOLM_FAMILY_CELLS / ALL_FAIL_BRANCH — the verdict imports the pre-registration"
      pattern: "gate_cleared"
---

<objective>
Build the `--report` mode of `scripts/phase17_isolation.py`: the cross-process ISO-04 proof, the
scoring pass over recorded completions, the inferential gate assembled entirely from imported Phase
16 statistics, the descriptive per-cell intervals, and the report writer with the pre-registered D-10
all-fail branch.

Purpose: STAT-01 through STAT-06 and ISO-03/ISO-05's reporting discipline. The expensive part of a
statistic in this repo is not the arithmetic — it is the pre-registration discipline attached to it.
`sign_test_exact` is 20 lines carrying four committed decisions, each of which was a recorded defect
fix, and re-deriving the arithmetic loses the decisions. Nothing statistical is written here; every
function is imported and STAT-04 is satisfied literally by import.

Output: a driver that turns four recorded sweeps into `results/phase17_isolation_report.md`, and the
tests pinning that it cannot do so dishonestly. The run itself is plan 17-09.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-multi-persona-isolation-matrix/17-CONTEXT.md
@.planning/phases/17-multi-persona-isolation-matrix/17-PATTERNS.md
@.planning/phases/17-multi-persona-isolation-matrix/17-06-SUMMARY.md
</context>

<interfaces>
Imported unchanged from `scripts/phase16_persistence.py` (STAT-04 by import):

- `fact_signs(per_fact_by_arm, pair)` (:1056) — reads `["rate"]` off whatever dict it is handed and
  does nothing else. "Arms" are cell tuples and "facts" are slot strings; verified during research.
- `sign_test_exact(signs)` (:1088) — `SIGN_TEST_N = 8` already equals Phase 17's 8 slots. Measured
  values: 8/8 gives **0.0078125**, 7/8 gives 0.0703125, 6/8 gives 0.2890625, 4/8 gives 1.0.
- `holm(p_values)` (:1170) — step-down, rejection STOPS at the first failure. `m` is
  `len(HOLM_FAMILY_PAIRS)`, Phase 16's family. Step alphas at m=6: 0.0083333, 0.01, 0.0125,
  0.0166667, 0.025, 0.05. Margin at step 1 is **0.0005208**.
- `cluster_bootstrap(per_key_questions, ...)` (:843) — two-stage (cluster, then that cluster's
  questions), fully key-agnostic, slot keys work as-is.
- `report_proportion(successes, n_questions, n_draws)` (:930) — Wilson upper + rule-of-three + both
  denominators, and never renders a bare zero percentage.
- `WILSON_LABEL` (:770) — carry it with `slot` substituted for `fact`.

From `scripts/phase17_personas.py` (plan 17-01): `HOLM_FAMILY_CELLS`, `CELL_ALTERNATIVE`,
`SIGN_UNIT`, `gate_cleared`, `worst_pair`, `ALL_FAIL_BRANCH`, `GATE_AGGREGATION_RATIONALE`,
`assert_phase17_family_closed`, `assert_family_length_matches_phase16`.

Measured anchors for the D-10 branch (RESEARCH F-12, same 104-question `core_held_out` tier, 9
draws): Phase 16 `adapter-only` question-unit rate **0.865385**, cluster bootstrap 95%
(0.721154, 0.971154), Wilson upper 0.911252; `base-neither` **0/104**, Wilson upper 0.025355, rule of
three 0.028846. Phase 14's 0.3483 is the DRAW rate over the same run — do not conflate the units.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: The report mode — cross-process proof, scoring pass, and the imported gate</name>
  <files>scripts/phase17_isolation.py</files>
  <read_first>
    - scripts/phase16_persistence.py lines 840-960 (`cluster_bootstrap`, `report_proportion`, `rule_of_three`, `WILSON_LABEL`) and lines 1050-1210 (`fact_signs`, `sign_test_exact`, `assert_family_closed`, `holm`)
    - scripts/phase16_persistence.py lines 1200-1260 (`compare_arms` — the one function permitted to call `holm`, and its `assert_family_closed` call)
    - scripts/phase17_isolation.py (current state after 17-06)
    - scripts/phase17_personas.py (the pre-registration being imported)
    - .planning/phases/17-multi-persona-isolation-matrix/17-RESEARCH.md §Statistics Surface and §Code Examples (the gate reuse block)
  </read_first>
  <action>
Add `run_report_mode()` to `scripts/phase17_isolation.py`, CPU-only — no torch, no model, no
generation. Ordering is not incidental:

1. Refuse to clobber `results/phase17_isolation_report.md` if it carries a recorded non-PENDING
   verdict, via `_verdict.recorded_verdict`. FIRST.
2. Read the four `results/phase17_sweep_*.json` records. `_prove` there are exactly
   `len(SWEEPS)` of them and that their `sweep` fields equal `SWEEPS` as a set.
3. `assert_sweeps_ran_on_distinct_weights(records)` — BEFORE any scoring. Assembling the report is
   impossible unless the sweeps provably ran on different weights.
4. `base_texts_by_slot(base_record)`.
5. `assemble_matrix(adapter_records, values_by_slot(), base_texts)` — the question-unit per-cell
   dicts from 17-04, with the `{slot: {label: value}}` mapping built by 17-06's `values_by_slot`.
   The scoring core takes the values as a parameter and never reads `PERSONA_FACTS` itself.
6. Descriptive, per cell: `cluster_bootstrap(cell["per_slot"])` for the phase's own width, and
   `report_proportion(cell["n_answerable"], cell["n_questions"], SHARED_ARM_CONFIG.n_draws)` for
   the Wilson bound, the rule of three at zero, and both denominators. Additionally compute the
   SLOT-level zero bound `3/8 = 0.375` alongside the question-level `3/104 = 0.0288` and label both
   as the two ends of the clustering assumption — Phase 17's zeros are the headline, so publishing
   both is the honest form and the cluster bootstrap is the estimate of where between them the truth
   sits.
7. Inferential gate, in `compare_cells()` — the ONE function permitted to call `holm` or
   `sign_test_exact`, in the `compare_arms` register:
   - build `per_cell` keyed by cell tuple with `{"rate": n_answerable / n_questions}` per slot —
     the QUESTION unit. `_prove` the unit against the committed `SIGN_UNIT` literal;
   - `signs = {pair: fact_signs(per_cell, pair) for pair in HOLM_FAMILY_CELLS}`;
   - `p_values = {pair: sign_test_exact(s) for pair, s in signs.items()}`;
   - `assert_phase17_family_closed(tuple(p_values))` — the runtime half; the static AST scan catches
     a new call site, this catches a dynamically-built pair list, and either alone leaves the other
     open;
   - `assert_family_length_matches_phase16(len(p16.HOLM_FAMILY_PAIRS))` — F-08's coincidence pin;
   - `rows = p16.holm(p_values)`;
   - return the rows plus `gate_cleared(rows)`.
8. Hand the rows and the descriptive block to the writer (Task 2).

Do NOT compute or return an aggregate rate over the nine cells (STAT-06). Do NOT call
`aggregate_by_fact` — its `rate` is `sum(k)/sum(n)`, the DRAW rate, and STAT-01 says the question is
the unit of analysis. Do NOT order the personas by diagonal (D-15).
  </action>
  <verify>
    <automated>.venv/bin/pytest -q tests/test_phase17_stats.py -x</automated>
  </verify>
  <acceptance_criteria>
    - On a synthetic four-record set with a perfect diagonal and zero off-diagonals, `compare_cells` returns six rows, all rejected, each with p exactly `0.0078125`, and `gate_cleared(rows) is True`.
    - On a synthetic set where one slot ties in one comparison, that comparison's p is `0.0703125`, it is retained, and `gate_cleared(rows) is False`.
    - `run_report_mode` raises `SystemExit` before scoring when two sweep records share a `lora_b_sha256`.
    - `grep -c "aggregate_by_fact" scripts/phase17_isolation.py` returns 0.
    - `python -c "import ast;t=ast.parse(open('scripts/phase17_isolation.py').read());sites=[n for n in ast.walk(t) if isinstance(n,ast.Call) and (getattr(n.func,'id','') or getattr(n.func,'attr','')) in ('holm','sign_test_exact')]" ` shows those calls occur only inside `compare_cells` — asserted by `tests/test_phase17_stats.py`.
    - `.venv/bin/ruff check scripts/phase17_isolation.py` exits 0.
  </acceptance_criteria>
  <done>Four recorded sweeps produce six Holm rows and a gate verdict, with every statistic imported and the family closed at both the static and runtime level.</done>
</task>

<task type="auto">
  <name>Task 2: The report writer and the pre-registered D-10 all-fail branch</name>
  <files>scripts/phase17_isolation.py</files>
  <read_first>
    - scripts/phase17_isolation.py (current state after Task 1)
    - scripts/phase17_personas.py (`ALL_FAIL_BRANCH`, `GATE_AGGREGATION_RATIONALE`, `worst_pair`)
    - results/phase16_persistence_report.md (the committed report layout: per-fact table, pooled rates, the gate table, the verdict section, and how `WILSON_LABEL` travels with every number)
    - scripts/phase16_persistence.py lines 760-800 (`WILSON_LABEL` and the clustering caveat wording)
    - .planning/phases/17-multi-persona-isolation-matrix/17-CONTEXT.md (D-10, D-13, D-15, D-18)
  </read_first>
  <action>
Add `render_report(...)` writing `results/phase17_isolation_report.md`. The whole writer is committed
BEFORE it produces a number — a report whose text is written after the numbers is a report written to
fit them.

Sections, in order:

- `## Pre-Registration` — the family as six named comparisons with `CELL_ALTERNATIVE`'s declared
  direction each, `SIGN_UNIT`, the seeds, the Holm step alphas, and the **0.0005208 margin** stated
  as a known property of the design: only 8/8 slot unanimity clears any step, 7/8 gives 0.0703125
  which exceeds even the last step's alpha of 0.05, so the gate requires all 48 slot-level
  observations (6 comparisons x 8 slots) to favour the diagonal. Cite the commit that added
  `scripts/phase17_personas.py`.
- `## The Matrix` — the 3x3 table plus the adapter-off column, each cell as
  `report_proportion`'s `formatted` string so no cell can render a bare percentage, with the cluster
  bootstrap interval beside it. Carry `WILSON_LABEL` with `slot` substituted for `fact`, and the
  two-ends-of-clustering note (`3/104 = 0.028846` question-level, `3/8 = 0.375` slot-level).
- `## Categories` — per cell, the diagonal / leak / base_prior / confabulation counts, with the
  D-13 note that base_prior is derived post-hoc by coincidence against the adapter-off column, and
  the `BASE_PRIOR_SEEDS` sanity anchor: it covers 2 of 8 core slots (`pet_name`, `hometown`) and is a
  screening seed list, never an enumeration. If the column does not reproduce `rose` for `pet_name`
  and `the country` for `hometown`, print that as a **sweep problem to investigate before trusting
  the derivation on the other six slots** — do not suppress it and do not treat it as a finding.
- `## Gate` — all six Holm rows, ALWAYS, whatever the outcome, each with its comparison, its signs,
  its p, its step alpha and its rejected flag. Then `gate_cleared`'s verdict, quoting
  `GATE_AGGREGATION_RATIONALE` and stating that publishing the rows is not the same as clearing the
  gate (D-18).
- `## Verdict` — computed by importing `gate_cleared`, never retyped. When it is False, emit
  `ALL_FAIL_BRANCH` in full and then, as the branch requires: (a) the diagonal magnitude for each of
  the three personas with its bootstrap CI, and (b) the adapter-off column result. Render the fork
  explicitly — a low diagonal means the matrix has NO POWER to judge isolation and "not demonstrated"
  means "not judgeable", never "isolation failed"; a high diagonal with an off-diagonal high enough
  to block 8/8 is a real leakage finding and is reported as such. Include D-15's sentence: the three
  diagonals are three separate anchors, never a ranking, because n=1 seed per persona makes any
  ordering uninterpretable. Give the reader the comparison anchors (Phase 16's 0.865385 question-unit
  and Phase 14's 0.3483 draw-unit, each labelled with its unit) so "not judgeable" is decidable.
- `## Replication (ISO-05)` — `worst_pair`'s selected pair with its inputs shown, marked
  **descriptive only**, with the D-16 statement that min/max/median is reported and never a
  hypothesis test. Leave the numbers slot empty until plan 17-10 runs; render "not yet measured"
  rather than omitting the section.
- `## Provenance` — the four sweeps' `git_sha`, pids, devices, wall clocks, `lora_b_sha256` digests
  and `forbid_digest`.

Hard writer constraints: no bare `0%` may be typed into this module at all (a zero rate printed
without its denominator and its rule-of-three ceiling reads as proven absence); no aggregate
nine-cell rate anywhere; no sentence ordering the personas.
  </action>
  <verify>
    <automated>.venv/bin/pytest -q tests/test_phase17_stats.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import re;s=open('scripts/phase17_isolation.py').read();assert re.search(r'\b0(\.0+)?%',s) is None"` exits 0 (STAT-02 source hygiene).
    - `render_report` on a synthetic gate-cleared input emits six Holm rows and no `ALL_FAIL_BRANCH` text; on a synthetic five-of-six input it emits six Holm rows AND the full `ALL_FAIL_BRANCH` text plus a per-persona diagonal line and the adapter-off column line.
    - Every rendered proportion in both cases contains a `/` denominator and either a Wilson bound or a rule-of-three bound — assert by regex over the rendered string, not by eye.
    - The rendered `## Verdict` section is parseable by `_verdict.recorded_verdict`.
    - The `## Replication (ISO-05)` section exists in both cases and reads "not yet measured".
    - `grep -c "0.2486\|0.2000" scripts/phase17_isolation.py` returns 0 (ISO-07).
  </acceptance_criteria>
  <done>The full report text, including the all-fail branch and the verdict fork, is committed before a single Phase 17 number exists.</done>
</task>

<task type="auto">
  <name>Task 3: Extend tests/test_phase17_stats.py — unit, bounds, branch, and the D-21 six-pairs twin</name>
  <files>tests/test_phase17_stats.py</files>
  <read_first>
    - tests/test_phase17_stats.py (current state after 17-01)
    - tests/test_phase16_stats.py lines 600-660 (`test_holm_stops_at_the_first_failure` and the unanimity fixture) and lines 745-860 (the `_GATE_MODULES` twin, the identifier ban, the SystemExit-substring register)
    - tests/test_phase16_driver.py lines 310-325 (the bare-zero-percent source regex)
    - scripts/phase17_isolation.py (the module under test)
  </read_first>
  <action>
Extend `tests/test_phase17_stats.py`. `_GATE_MODULES` is already the
`scripts/phase17_*.py` glob from 17-01, so `phase17_isolation.py` entered the existing scans
automatically when 17-04 created it — confirm that by asserting `len(_GATE_MODULES) == 3` now that
all three drivers exist, with a message naming the F-08 blindness this closes.

- `test_nothing_outside_the_six_pairs_enters_the_verdict_path` (D-21) — the Phase 17 twin of
  `test_phase16_stats.py:747`. `_GATE_MODULES` in that file is file-scoped to Phase 16's two drivers,
  so a Phase 17 driver calling `holm` is today neither red nor covered; the discipline does not
  transfer by inheritance. Assert: `holm` is called from exactly `["compare_cells"]` in
  `phase17_isolation.py` and from nowhere in the other two Phase 17 modules; `sign_test_exact`
  likewise; and `compare_cells` calls `assert_phase17_family_closed`, because the static scan catches
  a new call site while only the runtime guard catches a dynamically-built pair list.
- `test_signs_use_the_question_unit` (STAT-01, RESEARCH F-11) — build a per-cell fixture where the
  draw rate and the question rate ORDER DIFFERENTLY across two cells, run the assembly, and assert
  the sign follows the question rate. A fixture where both units agree proves nothing. Also assert
  `phase17_personas.SIGN_UNIT == "question"` and that the driver `_prove`s against that literal.
- `test_no_bare_zero_percent` (STAT-02) — the `tests/test_phase16_driver.py:316` regex applied to
  each of the three Phase 17 driver sources, plus the same regex applied to a rendered report string
  produced from a synthetic all-zero off-diagonal fixture.
- `test_zero_cells_carry_both_clustering_ends` (STAT-02) — a rendered zero cell contains the
  question-level `3/104` bound, the slot-level `3/8` bound, and the label naming them as the two ends
  of the clustering assumption.
- `test_all_fail_branch` (D-10) — `ALL_FAIL_BRANCH` is a committed literal containing the "not
  judgeable" wording and the D-15 no-ranking sentence; the writer emits it for a five-of-six row set
  and does not emit it for six-of-six; and in the five-of-six case the rendered text also carries the
  three per-persona diagonal magnitudes and the adapter-off column.
- `test_report_refuses_a_clobber` — a report path carrying a recorded non-PENDING verdict makes
  `run_report_mode` raise before reading any sweep record.
  </action>
  <verify>
    <automated>.venv/bin/pytest -q tests/test_phase17_stats.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest -q tests/test_phase17_stats.py -x` passes with at least 14 tests collected.
    - `len(_GATE_MODULES) == 3` and the assertion message names all three driver filenames.
    - Deliberate-RED recorded: add a second `holm(...)` call in a new function in `scripts/phase17_isolation.py`, watch `test_nothing_outside_the_six_pairs_enters_the_verdict_path` fail naming that function, then revert byte-identically (`git diff` empty).
    - Deliberate-RED recorded for STAT-01: swap the assembly to the draw rate, watch `test_signs_use_the_question_unit` fail, revert byte-identically.
    - `.venv/bin/pytest -q` full suite at or above 579 passed / 1 skipped. Record the exact counts.
    - `make lint` clean.
  </acceptance_criteria>
  <done>Every statistical discipline this phase claims is pinned by a test that has been watched failing, and the Phase 17 drivers are inside the six-pairs scan rather than merely absent from Phase 16's.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recorded sweep JSON → published rate | The only input to every number in the report |
| pre-registered constant → rendered verdict | The verdict must be computed by import, never retyped in prose |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| TH-17-26 | Repudiation | a seventh comparison entering the Holm family | mitigate | `assert_phase17_family_closed` at runtime plus the D-21 static scan over all three Phase 17 drivers; at m=7 alpha is 0.0071429, below the achievable 0.0078125, so the headline dies arithmetically |
| TH-17-27 | Repudiation | a bare `0%` read as proven absence | mitigate | `report_proportion` for every rate, the source-level regex over all three drivers and over the rendered report, and both clustering ends published at every zero |
| TH-17-28 | Repudiation | a clobbered isolation report | mitigate | `_verdict.recorded_verdict` clobber guard FIRST in `run_report_mode`, before any sweep record is read |
| TH-17-29 | Spoofing | a fabricated matrix from sweeps that shared weights | mitigate | `assert_sweeps_ran_on_distinct_weights` runs before scoring; the report cannot be assembled otherwise |
| TH-17-30 | Tampering | STAT-01 silently reverting to the draw rate | mitigate | `_prove` against the committed `SIGN_UNIT` literal plus a test fixture where the two units order differently |
| TH-17-SC | Tampering | pip installs | accept | Zero packages installed. `tests/test_package.py:11,36` sha256-pins `pyproject.toml` |
</threat_model>

<verification>
- `.venv/bin/pytest -q tests/test_phase17_stats.py tests/test_phase17_scoring.py -x` passes
- `.venv/bin/pytest -q` full suite at or above 579 passed / 1 skipped
- `make lint` clean
- `git diff -- pyproject.toml` empty (STAT-04)
- `git status --short results/` empty — this plan writes no report
</verification>

<success_criteria>
The complete report text and verdict logic exist in git history before any Phase 17 number does, the
gate is assembled from imported statistics with the family closed at both levels, and every zero
carries its denominator and both ends of the clustering assumption.
</success_criteria>

<output>
Create `.planning/phases/17-multi-persona-isolation-matrix/17-08-SUMMARY.md` when done
</output>
