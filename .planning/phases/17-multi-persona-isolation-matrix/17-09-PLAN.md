---
phase: 17-multi-persona-isolation-matrix
plan: 09
type: execute
wave: 5
depends_on: ["17-02", "17-03", "17-07", "17-08"]
files_modified:
  - results/phase17_sweep_persona_a.json
  - results/phase17_sweep_persona_b.json
  - results/phase17_sweep_persona_c.json
  - results/phase17_sweep_base.json
  - results/phase17_isolation_report.md
  - results/phase17_training_run.log
autonomous: true
requirements: [ISO-02, ISO-03, ISO-04, STAT-05]

must_haves:
  truths:
    - "Three adapters exist, trained from the gated material at three distinct seeds, through the committed recipe"
    - "Four sweeps ran in four fresh processes on provably different weights under one git SHA"
    - "The 3x3 matrix and the adapter-off column exist as committed evidence with the gate verdict computed from imported constants"
    - "The pre-registration commit precedes every results/phase17_* artifact, and the guard that proves it is no longer vacuous"
  artifacts:
    - path: "results/phase17_sweep_persona_a.json"
      provides: "Raw completions, live lora_B digest, pid, git_sha, forbid digest for adapter A"
      contains: "lora_b_sha256"
    - path: "results/phase17_isolation_report.md"
      provides: "The matrix, the six Holm rows, the descriptive intervals, and the recorded verdict"
      contains: "## Verdict"
  key_links:
    - from: "checkpoints/phase17_persona_a_adapter.pt"
      to: "results/phase17_sweep_persona_a.json"
      via: "the live-weights lora_B digest recorded at load"
      pattern: "lora_b_sha256"
    - from: "results/phase17_isolation_report.md"
      to: "scripts/phase17_personas.py"
      via: "the verdict computed by importing gate_cleared, never retyped"
      pattern: "gate_cleared"
---

<objective>
Run the phase: train three adapters at three distinct seeds from the GO/ADAPT-gated material, run
four sweeps in four fresh processes, and assemble `results/phase17_isolation_report.md`.

Purpose: this is where the numbers arrive. Everything that judges them — the family, the direction
per comparison, the all-six rule, the all-fail branch, the report text — is already in git history
from Waves 1-4. RESEARCH's cost model puts the whole plan at roughly 25 minutes of MPS wall clock:
~4 min training, ~13-14 min for three adapter sweeps, ~5 min for the base column, seconds for
scoring. Compute is not a constraint and no rigour is traded for it.

Output: three adapters, four sweep records, and the committed isolation report with its verdict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-multi-persona-isolation-matrix/17-CONTEXT.md
@.planning/phases/17-multi-persona-isolation-matrix/17-07-SUMMARY.md
@.planning/phases/17-multi-persona-isolation-matrix/17-08-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Train the three adapters at three distinct seeds</name>
  <files>results/phase17_training_run.log</files>
  <read_first>
    - scripts/teach_persona.py lines 160-230 and 395-700 (`_require_go_verdict`, `arm_outputs`, `refuse_if_exists`, `build_arm_bins`, `train_arm` with the `seed=` keyword from 17-02, `sanity_check`'s six proofs)
    - scripts/phase17_personas.py (`PERSONA_FACTS`, `PERSONA_SEEDS`, `LORA_CONFIG_NOTE`)
    - results/phase17_personas_report.md (the recorded GO/ADAPT verdict this run depends on)
    - results/phase14_teaching_run.log (the shape of a recorded teaching run and its 81 s end-to-end timing)
  </read_first>
  <action>
Run the `--train` mode committed in plan 17-06, once per persona, in three separate invocations:
`--train persona_a`, `--train persona_b`, `--train persona_c`. Write no new driver code in this
plan — the whole pipeline was committed before it produced a number, and adding a training path here
would put code after the gate report it is supposed to obey.

Each invocation calls `teach_persona._require_go_verdict(results/phase17_personas_report.md)` before
anything else, then `teach_persona.train_arm(..., seed=PERSONA_SEEDS[persona])` with
`arm_outputs(..., prefix="phase17")`. Confirm from the console that the verdict gate fired and
reported the recorded GO or ADAPT.

Run all three, one invocation each, tee-ing stdout to `results/phase17_training_run.log`. Expect
roughly 81 s per adapter.

After the runs, verify the three artifacts are genuinely distinct: load each with
`personacore.checkpoint.load_adapter` and compare their `lora_B` tensors pairwise, asserting no two
are equal. Three adapters that trained to identical weights would mean the `seed=` parameter never
reached the init draw, and the ISO-04 canary would then fire in Task 2 after 18 minutes of sweeps
rather than now.

Confirm `teach_persona.sanity_check`'s proofs all passed for each arm — in particular proof 6, that
no held-out question appears as a contiguous id run in the teaching bin, which re-proves the
never-seen split for the new personas at no cost (RESEARCH F-06).
  </action>
  <verify>
    <automated>test -f checkpoints/phase17_persona_a_adapter.pt && test -f checkpoints/phase17_persona_b_adapter.pt && test -f checkpoints/phase17_persona_c_adapter.pt && .venv/bin/pytest -q tests/test_phase17_personas.py tests/test_phase14_teaching.py -x</automated>
  </verify>
  <acceptance_criteria>
    - Three adapter artifacts exist under `checkpoints/phase17_*_adapter.pt`, each ~1.35 MB with 331,776 trainable params (the `LoRAConfig()` default census).
    - Each artifact's `lora_config` records `r=8` and `alpha=16.0`.
    - Pairwise `lora_B` comparison across the three artifacts finds no two equal. State the check and its result explicitly in the SUMMARY.
    - Each run's provenance line in the log reads the persona's own seed (`seed=1337`, `seed=1338`, `seed=1339`) — proving the 17-02 widening reached the init draw and the data order.
    - `results/phase17_training_run.log` is committed and shows three completed runs with wall clocks.
    - `.venv/bin/pytest -q` full suite at or above 579 passed / 1 skipped.
  </acceptance_criteria>
  <done>Three adapters with provably different weights exist, each trained at its own pre-registered seed from GO/ADAPT-gated material.</done>
</task>

<task type="auto">
  <name>Task 2: Run the four sweeps, one fresh process each</name>
  <files>results/phase17_sweep_persona_a.json, results/phase17_sweep_persona_b.json, results/phase17_sweep_persona_c.json, results/phase17_sweep_base.json</files>
  <read_first>
    - scripts/phase17_isolation.py (`run_one_sweep`, `build_parser`, `load_adapter_with_canary`, the record payload)
    - results/phase17_training_run.log (the adapters this consumes)
    - results/phase16_persistence_report.md lines 20-60 and 130-180 (the measured per-question costs and the `base-neither` 0/104 result on this same tier)
    - .planning/phases/17-multi-persona-isolation-matrix/17-CONTEXT.md (D-13 — the BASE_PRIOR_SEEDS sanity anchor and what a miss means)
  </read_first>
  <action>
Run four separate invocations, each in its own fresh process, in this order:
`--sweep persona_a`, `--sweep persona_b`, `--sweep persona_c`, `--sweep base`.

Do not attempt to run two in one process; the parser is structurally incapable of it and that is the
point. Expect roughly 4.5 min per adapter sweep (2.4-2.7 s/question x 104) and ~5 min for the base
column (3.07 s/question), each with a ±15% band — the repo's own repeat-measurement spread.

After each run, confirm from its record: `lora_b_sha256` is present and non-empty; `pid` differs from
every earlier sweep; `git_sha` equals `git rev-parse HEAD` and is identical across all four;
`forbid_digest` is identical across all four; the question set is 104 entries covering 8 slots x 13
with the fixture's own `seed_index` values.

**Check the base column against its two warning signs BEFORE proceeding to Task 3 (RESEARCH Pitfall
4).** Phase 16's `base-neither` arm scored exactly 0/104 on this same tier, and the ISO-01 gate
already measured the base producing zero containments of each minted value, so Phase 17's base column
should be at or near zero on all three personas' values. A non-zero base column is either a gate
failure or a sweep bug — STOP and investigate; do not read the matrix. Separately, check the D-13
sanity anchor: the column should reproduce `rose` for `pet_name` and `the country` for `hometown`.
If it does not, that is a sweep problem to investigate before trusting the base-prior derivation on
the other six slots. Record both checks and their outcomes in the SUMMARY whichever way they land —
"the anchor reproduced" and "the anchor did not reproduce, investigated as follows" are both
publishable; silence is not.

Commit the four sweep records.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import json,glob;rs=[json.load(open(p)) for p in sorted(glob.glob('results/phase17_sweep_*.json'))];assert len(rs)==4;assert len({r['pid'] for r in rs})==4;assert len({r['git_sha'] for r in rs})==1;assert len({r['lora_b_sha256'] for r in rs})==4;print('four distinct sweeps, one codebase')"</automated>
  </verify>
  <acceptance_criteria>
    - Four `results/phase17_sweep_*.json` files exist and are committed.
    - Four distinct pids, one `git_sha`, one `forbid_digest`, four distinct `lora_b_sha256` values, and the base sweep's digest equals the all-zero-`lora_B` digest.
    - Every record's `(slot, seed_index, question)` triple set is identical across all four — verified by the automated check, not by eye.
    - The base column's containment count for each of the three personas' values is stated in the SUMMARY with its denominator. If it is non-zero, the SUMMARY records the investigation and its conclusion before Task 3 runs.
    - The D-13 sanity anchor result (`rose` / `the country`) is recorded in the SUMMARY either way.
    - Total wall clock recorded per sweep; the sum is inside the ~18-19 min band or the deviation is stated.
  </acceptance_criteria>
  <done>Four sweeps ran in four fresh processes on provably different weights, with the base column checked against both of its warning signs before anything was scored.</done>
</task>

<task type="auto">
  <name>Task 3: Assemble the report and record the verdict</name>
  <files>results/phase17_isolation_report.md</files>
  <read_first>
    - scripts/phase17_isolation.py (`run_report_mode`, `compare_cells`, `render_report`)
    - scripts/phase17_personas.py (`gate_cleared`, `ALL_FAIL_BRANCH`, `GATE_AGGREGATION_RATIONALE`, `CELL_ALTERNATIVE`)
    - results/phase16_persistence_report.md lines 130-190 (the measured anchors: `adapter-only` 0.865385 question unit with cluster bootstrap (0.721154, 0.971154); `base-neither` 0/104)
    - .planning/phases/17-multi-persona-isolation-matrix/17-CONTEXT.md (D-10, D-15, D-18)
  </read_first>
  <action>
Run `.venv/bin/python scripts/phase17_isolation.py --report`. It runs
`assert_sweeps_ran_on_distinct_weights` before scoring, so a fabricated matrix cannot reach the
writer.

Read the produced `results/phase17_isolation_report.md` and check it against the pre-registration
rather than against expectation:

- all six Holm rows are present with their comparison, signs, p, step alpha and rejected flag,
  whatever the outcome;
- the verdict matches `gate_cleared`'s return — do not retype it, do not soften it, do not add an
  investigate-the-instrument escape hatch;
- if fewer than six rejected, `ALL_FAIL_BRANCH` is present in full together with the three
  per-persona diagonal magnitudes with CIs and the adapter-off column result, and the fork is
  resolved explicitly against the anchors: a diagonal far below Phase 16's 0.865385 (question unit)
  means the matrix has NO POWER to judge isolation and "not demonstrated" means "not judgeable"; a
  high diagonal with an off-diagonal high enough to block 8/8 is a real leakage finding reported as
  such;
- no sentence orders the three personas by diagonal (D-15);
- no aggregate rate over the nine cells appears (STAT-06);
- no bare `0%` appears; every zero carries its denominator, its Wilson bound, its rule of three and
  both ends of the clustering assumption.

Do not amend any pre-registered constant after seeing a number. If the verdict is uncomfortable, that
is the pre-registration working — Phase 12 recorded `lambda*=None` and Phase 16 recorded "not
demonstrable at n=8"; this project records the outcome as written.

Commit the report. Then update `.planning/ROADMAP.md` Phase 17's Success Criteria with a dated line
recording the measured outcome against SC3, SC4 and the SC5 half this plan covers, and append the
phase's key findings to `.planning/STATE.md`'s Accumulated Context in the established
`[Phase 17]: 17-09: ...` form.
  </action>
  <verify>
    <automated>test -f results/phase17_isolation_report.md && .venv/bin/pytest -q tests/test_phase17_stats.py tests/test_phase16_prereg.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `results/phase17_isolation_report.md` exists, is committed, and its `## Verdict` section is parseable by `_verdict.recorded_verdict`.
    - The report contains exactly six Holm rows. Count them: `grep -c` the row marker returns 6.
    - `python -c "import re;s=open('results/phase17_isolation_report.md').read();assert re.search(r'\b0(\.0+)?%',s) is None"` exits 0 (STAT-02).
    - The verdict in the report equals `gate_cleared`'s return on the report's own six rows, re-derived independently in the SUMMARY.
    - `.venv/bin/pytest -q tests/test_phase16_prereg.py -x` passes AND its Phase 17 ordering guard now reports `checked > 0`. Record the count — this is the moment STAT-05 stops being vacuous.
    - The SUMMARY states the three diagonals, the six off-diagonals and the base column with denominators and units, and states plainly whether the gate cleared.
    - ROADMAP Phase 17 and STATE.md Accumulated Context are updated in the same commit.
    - `.venv/bin/pytest -q` full suite at or above 579 passed / 1 skipped; `make lint` clean.
  </acceptance_criteria>
  <done>The matrix, the gate and the verdict exist as committed evidence, computed by importing constants that were in git history before any adapter trained.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adapter artifact → sweep process | Three artifacts with identical key sets; only the live-weights digest distinguishes them |
| sweep record → published verdict | The four JSON files are the entire evidentiary basis of the report |
| local run → public repository | `results/` ships publicly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| TH-17-31 | Spoofing | a silent adapter swap producing a fabricated matrix | mitigate | Both canary layers from plan 17-06 fire in this run: in-process at each load, cross-process in `--report` before scoring. Task 1 additionally proves the three artifacts differ before 18 minutes of sweeps |
| TH-17-32 | Repudiation | a verdict softened after seeing the number | mitigate | The verdict is computed by importing `gate_cleared`; `ALL_FAIL_BRANCH` was committed in Wave 4; `tests/test_phase16_prereg.py` proves the pre-registration precedes every artifact and is non-vacuous from this commit |
| TH-17-33 | Elevation of Privilege | pickled `.pt` read during training and sweeps | mitigate | Adapters read at `weights_only=True` via `checkpoint.load_adapter`; the one `weights_only=False` read is the project's own `convbase_best.pt` full resume checkpoint, documented as TRUSTED (ASVS V5, T-09-11/T-14-04) |
| TH-17-34 | Repudiation | a clobbered sweep record or report | mitigate | Refuse-to-rerun on every sweep path and the `_verdict.recorded_verdict` clobber guard on the report, both running before anything expensive |
| TH-17-35 | Information Disclosure | personal data in a public artifact | mitigate | All 24 values invented (D-06) and human-reviewed at the 17-07 checkpoint before any of them reached a public artifact |
| TH-17-SC | Tampering | pip installs | accept | Zero packages installed. `tests/test_package.py:11,36` sha256-pins `pyproject.toml` |
</threat_model>

<verification>
- Four sweep records with four pids, one git SHA, four distinct live-weights digests
- `results/phase17_isolation_report.md` committed with six Holm rows and an imported verdict
- `.venv/bin/pytest -q tests/test_phase16_prereg.py -x` passes with the Phase 17 guard non-vacuous
- `.venv/bin/pytest -q` full suite at or above 579 passed / 1 skipped
- `make lint` clean
- `git diff -- pyproject.toml` empty (STAT-04)
</verification>

<success_criteria>
The isolation matrix exists as committed evidence — three diagonals, six off-diagonals, an
adapter-off column, six Holm rows and a verdict — and every constant that judged it was in git
history before the first adapter trained.
</success_criteria>

<output>
Create `.planning/phases/17-multi-persona-isolation-matrix/17-09-SUMMARY.md` when done
</output>
