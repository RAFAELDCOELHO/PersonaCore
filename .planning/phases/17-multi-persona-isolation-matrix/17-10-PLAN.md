---
phase: 17-multi-persona-isolation-matrix
plan: 10
type: execute
wave: 6
depends_on: ["17-09"]
files_modified:
  - results/phase17_replication.json
  - results/phase17_replication_run.log
  - results/phase17_isolation_report.md
  - tests/test_phase17_stats.py
autonomous: true
requirements: [ISO-05, STAT-02, STAT-06]

must_haves:
  truths:
    - "The replicated pair was selected by a function committed before the matrix was read"
    - "Each persona of that pair has three seeds, counting the original, and each seed's off-diagonal rate is reported"
    - "The replication is reported as min/max/median and reaches neither holm nor sign_test_exact"
    - "The isolation report is extended, never edited — its pre-addendum bytes are unchanged"
  artifacts:
    - path: "results/phase17_replication.json"
      provides: "Per-seed off-diagonal rates for the selected pair, with provenance per run"
      contains: "worst_pair"
    - path: "results/phase17_isolation_report.md"
      provides: "The Replication (ISO-05) section filled in as an addendum with zero deletions"
      contains: "Replication"
  key_links:
    - from: "results/phase17_replication.json"
      to: "scripts/phase17_personas.py"
      via: "worst_pair and REPLICATION_SEEDS, both pre-registered"
      pattern: "REPLICATION_SEEDS"
---

<objective>
Replicate the worst-colliding pair across k=3 seeds and report it descriptively, so seed variance is
not mistaken for interference.

Purpose: ISO-05. The selection rule and the seeds were both committed in plan 17-01, before the
matrix existed — selecting after the fact would be legitimate only because the output is descriptive,
but a committed rule costs nothing and removes the argument. D-19 fixes k=3 as the original seed plus
two more per persona of the selected pair: 4 extra adapters and 4 extra sweeps, ~23 min.

Output: per-seed off-diagonal rates for the selected pair, appended to the isolation report as an
addendum with zero deletions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-multi-persona-isolation-matrix/17-CONTEXT.md
@.planning/phases/17-multi-persona-isolation-matrix/17-09-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Select the pair mechanically and train the four replicate adapters</name>
  <files>results/phase17_replication_run.log</files>
  <read_first>
    - scripts/phase17_personas.py (`worst_pair`, `REPLICATION_SEEDS`, `PERSONA_SEEDS`)
    - results/phase17_isolation_report.md (the six recorded off-diagonal rates, in the question unit)
    - scripts/phase17_isolation.py (the `--train` mode and `run_one_sweep`)
    - .planning/phases/17-multi-persona-isolation-matrix/17-CONTEXT.md (D-16, D-19)
  </read_first>
  <action>
Call the committed `phase17_personas.worst_pair(off_diagonal_rates)` on the six recorded off-diagonal
question-unit rates read out of the sweep records — not out of the rendered report, and not
re-derived by hand. Print its inputs and its selection. If every off-diagonal is 0.0 the rule resolves
to `("persona_a", "persona_b")` by the pre-registered tie-break, which is the expected outcome in the
success case and must be recorded as a tie-break result rather than as a finding about persona A
and B.

For each persona of the selected pair, train the two ADDITIONAL seeds from
`REPLICATION_SEEDS[persona]` — the first entry is the seed already trained in 17-09 and is reused,
not retrained. Four new adapters total, at
`checkpoints/phase17_{persona}_seed{seed}_adapter.pt` via `arm_outputs(..., prefix="phase17")` with a
seed-scoped arm name so `refuse_if_exists` protects each independently and no two runs share a path.

Tee to `results/phase17_replication_run.log`. Expect ~81 s per adapter, ~5 min total.

Verify the four new adapters are pairwise distinct from each other AND from the two originals — six
artifacts, six distinct `lora_B` digests. Two seeds that produced identical weights would mean the
seed never reached the init draw, and the whole replication would be measuring nothing.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import sys;sys.path.insert(0,'scripts');import importlib.util as u;s=u.spec_from_file_location('p','scripts/phase17_personas.py');m=u.module_from_spec(s);s.loader.exec_module(m);print(m.worst_pair({(i,j):0.0 for i in m.PERSONAS for j in m.PERSONAS if i!=j}))"</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records the six off-diagonal rates that were fed to `worst_pair`, the pair it returned, and whether the tie-break decided it.
    - Four new adapter artifacts exist; six `lora_B` digests across the pair's two personas x three seeds are pairwise distinct.
    - Each replicate run's provenance line reads its own seed from `REPLICATION_SEEDS`.
    - `worst_pair` was CALLED, not reimplemented: `grep -c "worst_pair" ` on the driver shows the import and the call, and no local argmax over pair means exists.
    - `results/phase17_replication_run.log` is committed.
  </acceptance_criteria>
  <done>The replicated pair was chosen by a function that existed before the matrix did, and six provably distinct adapters back the replication.</done>
</task>

<task type="auto">
  <name>Task 2: Run the four replicate sweeps and record per-seed rates</name>
  <files>results/phase17_replication.json</files>
  <read_first>
    - scripts/phase17_isolation.py (`run_one_sweep`, the record payload, `assemble_matrix`)
    - results/phase17_sweep_persona_a.json (the record shape this must match)
    - .planning/phases/17-multi-persona-isolation-matrix/17-RESEARCH.md §Cost Model (the ISO-05 line: 4 sweeps, ~18 min)
  </read_first>
  <action>
Run one fresh process per replicate sweep — four invocations, four pids — against the four new
adapters, with the SAME questions, `question_seed(index)` values, `forbid_ids` mask and `stop_ids` as
the 17-09 sweeps. Nothing about the generation side varies; only the training seed does. That is what
makes the seed variance readable.

Score each replicate sweep with the same `assemble_matrix` path and extract, per seed, the two
off-diagonal rates that define the selected pair — `rate(i, j)` and `rate(j, i)` in the question unit.

Write `results/phase17_replication.json`: the selected pair, its inputs, the six per-seed cells (2
personas x 3 seeds, each with its own `lora_b_sha256`, pid, `git_sha` and wall clock), and the
min / max / median of the pair's mean off-diagonal rate across the three seeds. Use
`report_proportion` for every rate so no number appears without its denominator and its bound, and
carry the rule of three at every zero.

Do NOT compute a p-value, a sign test, a Holm row or any hypothesis test over the replication (D-16 /
ISO-05 / STAT-06). The output is min/max/median and nothing else. `tests/test_phase17_stats.py`'s
identifier ban already forbids any `replication`-named identifier reaching `holm` or
`sign_test_exact`, and it must stay green.
  </action>
  <verify>
    <automated>test -f results/phase17_replication.json && .venv/bin/pytest -q tests/test_phase17_stats.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `results/phase17_replication.json` exists and is committed, carrying six per-seed cells with six distinct `lora_b_sha256` values and six distinct pids.
    - Every rate in the file carries a denominator; every zero carries a Wilson upper bound and a rule-of-three bound.
    - The file contains `min`, `max` and `median` for the pair's mean off-diagonal rate and no p-value, no alpha and no rejected flag.
    - `.venv/bin/pytest -q tests/test_phase17_stats.py -k "replication or nine_cell" -x` passes.
    - Total wall clock recorded; the deviation from the ~18 min estimate is stated if it exceeds ±15%.
  </acceptance_criteria>
  <done>Seed variance on the worst-colliding pair is measured across three seeds per persona and reported with bounds, with no hypothesis test anywhere near it.</done>
</task>

<task type="auto">
  <name>Task 3: Append the ISO-05 addendum to the isolation report, with zero deletions</name>
  <files>results/phase17_isolation_report.md, tests/test_phase17_stats.py</files>
  <read_first>
    - results/phase17_isolation_report.md (the whole committed report, especially the `## Replication (ISO-05)` placeholder reading "not yet measured")
    - results/phase17_replication.json (the numbers to render)
    - .planning/STATE.md lines 148-155 (the Phase 15 D-13/R3 precedent: `docs/REPORT.md` extended, never edited — 549 insertions / 0 deletions, with the unchanged prefix pinned by test)
  </read_first>
  <action>
Extend `results/phase17_isolation_report.md` by APPENDING an ISO-05 addendum section. Do not
regenerate the report and do not use a `--force` flag on the clobber guard — a force flag that
becomes routine destroys the hand-recorded verdict it protects (Phase 16's 16-06 decision, and CR-02
before it). The committed verdict and every number above the addendum stay byte-identical.

The addendum must carry: the selected pair with the six off-diagonal rates that selected it and an
explicit statement of whether the pre-registered tie-break decided it; the per-seed table (2 personas
x 3 seeds) with denominators and bounds; the min / max / median; and the D-16 sentence marking the
whole section **descriptive only, never a hypothesis test**. Include D-15's constraint restated for
this context: with one seed per persona in the main matrix the three diagonals are three separate
anchors and never a ranking, and this replication is what makes seed variance readable rather than
what licenses an ordering.

Update the placeholder line in the existing `## Replication (ISO-05)` section from "not yet measured"
to a pointer at the addendum — that single line is the ONLY edit above the addendum, and the SUMMARY
must state its byte offset and the diff line count.

Add `test_report_addendum_is_additive` to `tests/test_phase17_stats.py`: read
`results/phase17_isolation_report.md` at the commit before this task (`git show`) and assert the
current file starts with the previous content up to the placeholder line, that the diff is
insertions plus that one line, and that `_verdict.recorded_verdict` returns the same verdict text
before and after. Also assert the addendum contains no `p =`, no `alpha`, no `rejected` and no bare
`0%`.

Finally, update `.planning/ROADMAP.md` Phase 17's SC5 with a dated line recording the ISO-05 outcome,
and append the finding to `.planning/STATE.md` in the `[Phase 17]: 17-10: ...` form.
  </action>
  <verify>
    <automated>.venv/bin/pytest -q tests/test_phase17_stats.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `git diff HEAD~1 -- results/phase17_isolation_report.md | grep -c "^-" ` returns 2 (the diff header plus the single replaced placeholder line) — everything else is an insertion.
    - `_verdict.recorded_verdict` on the report returns the same verdict word before and after the addendum.
    - `python -c "import re;s=open('results/phase17_isolation_report.md').read();assert re.search(r'\b0(\.0+)?%',s) is None"` exits 0.
    - `grep -ci "descriptive" results/phase17_isolation_report.md` shows the ISO-05 section is marked descriptive; `grep -c "rejected" ` inside the addendum block returns 0.
    - `.venv/bin/pytest -q tests/test_phase17_stats.py -x` passes with `test_report_addendum_is_additive` collected.
    - ROADMAP SC5 and STATE.md are updated in the same commit.
    - `.venv/bin/pytest -q` full suite at or above 579 passed / 1 skipped; `make lint` clean.
  </acceptance_criteria>
  <done>The ISO-05 replication is published inside the isolation report as an addendum, the recorded verdict is byte-unchanged, and the additive property is pinned by test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recorded matrix → pair selection | Selecting after the numbers are visible is the failure D-19's committed rule forecloses |
| committed report → addendum | The recorded verdict is evidence; an append must not become a rewrite |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| TH-17-36 | Repudiation | a post-hoc "worst pair" choice | mitigate | `worst_pair` was committed in Wave 1 with its tie-break; the SUMMARY records its inputs and whether the tie-break decided it. `REPLICATION_SEEDS` are derived from `PERSONA_SEEDS` and equally pre-registered |
| TH-17-37 | Repudiation | the isolation report's verdict rewritten under cover of an addendum | mitigate | Append-only, no `--force`, and `test_report_addendum_is_additive` asserts the prefix is byte-identical and the verdict text unchanged |
| TH-17-38 | Repudiation | a descriptive replication reported as a test | mitigate | The identifier ban in `tests/test_phase17_stats.py` keeps `replication`-named identifiers away from `holm` and `sign_test_exact`, and the addendum is asserted free of `p =` / `alpha` / `rejected` |
| TH-17-39 | Spoofing | two replicate seeds producing identical weights | mitigate | Six pairwise-distinct `lora_B` digests asserted in Task 1 before any sweep runs |
| TH-17-SC | Tampering | pip installs | accept | Zero packages installed. `tests/test_package.py:11,36` sha256-pins `pyproject.toml` |
</threat_model>

<verification>
- `results/phase17_replication.json` committed with six distinct per-seed runs
- `results/phase17_isolation_report.md` extended with zero deletions beyond the one placeholder line
- `.venv/bin/pytest -q` full suite at or above 579 passed / 1 skipped
- `make lint` clean
- `git diff -- pyproject.toml` empty (STAT-04)
</verification>

<success_criteria>
Seed variance on the worst-colliding pair is measured across k=3 seeds and published descriptively,
the pair was selected by a rule that predates the matrix, and the recorded verdict above the addendum
is byte-unchanged.
</success_criteria>

<output>
Create `.planning/phases/17-multi-persona-isolation-matrix/17-10-SUMMARY.md` when done
</output>
