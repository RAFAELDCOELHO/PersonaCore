---
phase: 17-multi-persona-isolation-matrix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phase17_personas.py
  - tests/test_phase17_stats.py
  - tests/test_phase16_prereg.py
autonomous: true
requirements: [STAT-03, STAT-04, STAT-05, STAT-06, ISO-05, ISO-07]

must_haves:
  truths:
    - "Every Phase 17 gate constant is a module-level literal in a committed driver, pushed before any adapter trains"
    - "The gate clears only when all six Holm comparisons reject — five of six fires the all-fail branch"
    - "worst_pair resolves deterministically to (persona_a, persona_b) at the all-zero three-way tie"
    - "A commit that edits the pre-registration after a results/phase17_* artifact exists turns a committed test red"
    - "Phase 14's 0.2486 / 0.2000 appear in no Phase 17 file"
  artifacts:
    - path: "scripts/phase17_personas.py"
      provides: "Pre-registration block: family, per-comparison direction, seeds, D-18 gate rule, D-19 worst_pair, D-10 branch text, the four minting filters"
      contains: "HOLM_FAMILY_CELLS"
    - path: "tests/test_phase17_stats.py"
      provides: "Family closure, all-six gate, tie-break, m==6 coincidence pin, ISO-07/STAT-04/STAT-06 scans"
      contains: "def test_gate_requires_all_six"
    - path: "tests/test_phase16_prereg.py"
      provides: "Phase 17 pre-registration ordering guard, derived from git history rather than a hand-pinned SHA"
      contains: "phase17_personas.py"
  key_links:
    - from: "scripts/phase17_personas.py"
      to: "scripts/phase16_persistence.py"
      via: "HOLM_FAMILY_PAIRS length coincidence proof (F-08)"
      pattern: "HOLM_FAMILY_PAIRS"
    - from: "tests/test_phase17_stats.py"
      to: ".planning/phases/17-multi-persona-isolation-matrix/17-CONTEXT.md"
      via: "blockquote read of the D-18 rationale, never a second hand-typed copy"
      pattern: "_context_blockquote"
---

<objective>
Commit Phase 17's pre-registration: the six-comparison Holm family, the per-comparison declared
direction, the three training seeds, the ISO-05 replication seeds, the D-18 all-six gate rule, the
D-19 `worst_pair` selection function, the D-10 all-fail branch text, and the four mechanical minting
filters — as module-level literals in `scripts/phase17_personas.py`.

Purpose: STAT-05. Nothing downstream in this phase may run until these constants exist in git
history. A gate constant written after a number is visible is not a pre-registration. This plan is
Wave 1 for exactly that reason — the values (17-03), the adapters (17-09) and every
`results/phase17_*` artifact land strictly after it.

Output: the committed pre-registration module, its tests, and a git-history guard that turns red if
the pre-registration is edited after a Phase 17 results artifact exists.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-multi-persona-isolation-matrix/17-CONTEXT.md
@.planning/phases/17-multi-persona-isolation-matrix/17-PATTERNS.md
</context>

<interfaces>
Imported unchanged from `scripts/phase16_persistence.py` (STAT-04 is satisfied by import):

- `HOLM_FAMILY_PAIRS` (:1003) — `tuple(itertools.combinations(CONDITION_ORDER, 2))`, length 6
- `HOLM_ALPHA = 0.05` (:1005), `SIGN_TEST_N = 8` (:1016)
- `fact_signs(per_fact_by_arm, pair)` (:1056), `sign_test_exact(signs)` (:1088), `holm(p_values)` (:1170)
- `cluster_bootstrap(...)` (:843), `report_proportion(successes, n_questions, n_draws)` (:930)
- `GATED_TIER = "held-out"` (:744), `WILSON_LABEL` (:770)

`holm` prices alpha as `HOLM_ALPHA / (len(HOLM_FAMILY_PAIRS) - index)` — Phase 16's family, not
Phase 17's. The agreement at 6 is a coincidence of `C(4,2) == 3x2` (RESEARCH F-08) and must be
proved, never assumed.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Commit the pre-registration block and the four minting filters</name>
  <files>scripts/phase17_personas.py</files>
  <read_first>
    - scripts/phase16_persistence.py lines 1001-1200 (the pre-registration register: derived family, spelled-out direction dict, `assert_family_closed`'s three proofs, `holm`)
    - scripts/phase16_persistence.py lines 110-180 (the `_prove` register and `ArmConfig`/`SHARED_ARM_CONFIG`)
    - scripts/phase14_factset.py lines 45-130 and 380-475 (`Fact` NamedTuple, pool-literal shape, `_BY_ID`/`_locked` resolve-by-id, `VALUE_TOKEN_CENSUS` transcribed-census comment)
    - scripts/phase14_recall.py lines 279-330 (`normalize`, `contains_value` — the filter primitives)
    - .planning/phases/17-multi-persona-isolation-matrix/17-CONTEXT.md (D-08, D-10, D-14, D-16, D-17, D-18, D-19, D-20)
    - .planning/phases/17-multi-persona-isolation-matrix/17-PATTERNS.md §`scripts/phase17_personas.py`
  </read_first>
  <action>
Create `scripts/phase17_personas.py`. Module docstring in the `phase16_persistence.py:1-20` register:
state that it is the Phase 17 PRE-REGISTRATION (STAT-05), that nothing executes at import, that
there is no torch import at module scope, and that an `importlib` load runs no guard and no model.

Commit these module-level names:

- `PERSONAS = ("persona_a", "persona_b", "persona_c")`.
- `GATED_TIER` — imported from `phase16_persistence`, not retyped. `SLOTS_EXPECTED = 8`,
  `QUESTIONS_PER_SLOT = 13` (RESEARCH F-02's measured fixture shape).
- `CORE_SLOTS` — the eight gated slot names as a committed tuple in fixture order: `person_name`,
  `pet_name`, `cat_name`, `sibling_name`, `hometown`, `street`, `birth_year`, `house_number`. It is
  the ONE canonical slot list: the fixture regrouping (plan 17-04) and the minted material (plan
  17-03) are each checked against it, rather than against each other. Two things checked against a
  third cannot drift into agreeing on a wrong answer. Derive `SLOTS_EXPECTED` from
  `len(CORE_SLOTS)`, never retype `8`.
- `HOLM_FAMILY_CELLS` — DERIVED with a comprehension over `PERSONAS`, one member per
  `((i, i), (i, j))` for `i in PERSONAS for j in PERSONAS if i != j`, giving 6. Carry the
  `phase16_persistence.py:1001` comment verbatim in spirit: a retyped family is a family that can
  stop matching the cells it claims to compare.
- `CELL_ALTERNATIVE` — a dict SPELLED OUT with six literal entries, one per member of
  `HOLM_FAMILY_CELLS`, each value a sentence declaring the direction of that comparison
  ("persona_a's own value exceeds persona_b's value under adapter persona_a"). Deliberately not
  generated: a reviewer audits six committed statements. Key-set equality with `HOLM_FAMILY_CELLS`
  is proved at runtime by `assert_phase17_family_closed`, never by construction.
- `SIGN_UNIT = "question"` with a comment citing RESEARCH F-11: `fact_signs` reads `["rate"]` off
  whatever dict it is handed, and `aggregate_by_fact`'s `rate` is the DRAW rate. Phase 17 hands it
  `n_answerable / n_questions`. STAT-01 lives at assembly and this constant is its declaration.
- `PERSONA_SEEDS = {"persona_a": 1337, "persona_b": 1338, "persona_c": 1339}` (D-14). Comment: the
  gated contrast is within adapter `i`, so the seed cancels inside it; D-15 forbids reading the three
  diagonals as a ranking.
- `REPLICATION_SEEDS` — DERIVED from `PERSONA_SEEDS`, `{persona: (seed, seed + 100, seed + 200)}`,
  k = 3 counting the original seed (D-19). Pre-registered here because ISO-05's replication runs
  after the matrix is read and its seeds must not be chosen then.
- `LORA_CONFIG_NOTE` — one line recording D-20: the three adapters train at `LoRAConfig()` defaults
  (`r=8, alpha=16.0`) so the diagonals stay readable against Phase 14's 0.3483 (draw unit) and
  Phase 16's 0.865385 (question unit). Do not construct a `LoRAConfig` here; there is no torch
  import in this module.
- `GATE_AGGREGATION_RATIONALE` — the D-18 Portuguese blockquote as a module constant, in the
  `phase16_persistence.py:1037-1044` implicit-concatenation shape. It must equal the CONTEXT.md
  blockquote after the `_context_blockquote` unwrap (lines joined by single spaces, surrounding
  double quotes stripped). Comment that the test asserts it against `17-CONTEXT.md` rather than
  against a second hand-typed copy.
- `ALL_FAIL_BRANCH` — the D-10 pre-registered text, committed before any number exists. It must
  name both required publications, (a) the diagonal magnitude with bootstrap CI for each of the three
  personas and (b) the adapter-off column result, and both forks: a low diagonal means the matrix has
  NO POWER to judge isolation and "not demonstrated" means "not judgeable", never "isolation failed";
  a high diagonal with an off-diagonal high enough to block 8/8 is a real leakage finding. Include the
  D-15 sentence forbidding any ordering of the three personas by diagonal.

Commit these pure functions (no torch, no I/O):

- `_prove(condition, message)` — this module's own, prefix `[phase17_personas]`. Never an
  `-O`-strippable `assert`. Never import another driver's.
- `assert_phase17_family_closed(entered_pairs)` — the Phase 16 `assert_family_closed:1142` three-proof
  structure, repriced: no duplicate pair; `set(entered) == set(HOLM_FAMILY_CELLS)`;
  `set(CELL_ALTERNATIVE) == set(HOLM_FAMILY_CELLS)`. The set-equality message must state the
  arithmetic consequence — at m = 7 Holm's first alpha is 0.0071429, below the achievable
  p of 0.0078125, so the headline dies at every possible outcome including perfect unanimity.
- `assert_family_length_matches_phase16(phase16_family_len)` — RESEARCH F-08. `_prove` that the
  argument equals `len(HOLM_FAMILY_CELLS)`, with a message stating that `holm` prices alpha from
  Phase 16's `HOLM_FAMILY_PAIRS` and that the two agreeing at 6 is a coincidence of `C(4,2) == 3x2`,
  so an edit to Phase 16's `CONDITION_ORDER` would silently reprice this phase's gate. Takes the
  length as a parameter so this module imports no sibling driver.
- `gate_cleared(holm_rows)` — D-18. Returns True only when `len(holm_rows) == len(HOLM_FAMILY_CELLS)`
  AND every row's rejected flag is True. Docstring quotes `GATE_AGGREGATION_RATIONALE` and states
  that anything less than six fires `ALL_FAIL_BRANCH`, and that all six Holm rows are published
  regardless — publishing the rows is not the same as clearing the gate.
- `worst_pair(off_diagonal_rates)` — D-19. Input is `{(i, j): rate}` over the six ordered
  off-diagonal cells in the question unit. Return the unordered pair `(i, j)` with `i` before `j` in
  `PERSONAS` order that maximises `mean(rate[(i, j)], rate[(j, i)])`, tie-broken by lowest index:
  smallest `i`, then smallest `j`. Implement as a `min` over a `(-mean, PERSONAS.index(i),
  PERSONAS.index(j))` key so the tie-break is the sort key rather than a branch. Docstring records
  that the hoped-for outcome is all-zero off-diagonals, which is a three-way tie exactly in the
  success case, so the tie-break is load-bearing and resolves to `("persona_a", "persona_b")`.
- The four mechanical minting filters (RESEARCH Pitfall 6), each pure and each raising through
  `_prove` with its own message: `filter_token_budget(census)` — every value at most
  `MAX_VALUE_TOKENS = 8`, with the derivation comment that `RECALL_MAX_NEW_TOKENS = 48` is
  `max(census) + 32 + 8` (`phase14_recall.py:120-145`) and a 9-token value forces the budget off 48,
  breaking parity with `SHARED_ARM_CONFIG` and every published Phase 14/16 number;
  `filter_roundtrip(tok, values)` — `tok.decode(tok.encode(v)) == v` and no id outside the live set;
  `filter_substring_disjoint(minted, forbidden)` — pairwise substring-disjointness under
  `phase14_recall.normalize` across the minted values UNION the forbidden set, with the message
  naming the concrete failure (persona A's value inside persona B's value makes every correct
  A-answer also register as a B-leak, manufacturing an off-diagonal hit out of nothing);
  `filter_absent_from_questions(minted, questions)` — no minted value appears in any fixture question
  under `phase14_recall.contains_value`. Import `normalize` / `contains_value` LAZILY inside the
  filter bodies (the module docstring's LAZY-IMPORT RULE), so no Phase 14 value string reaches this
  module's import-time surface.

Do not author any persona value in this task — 17-03 owns the material, and committing the rule
before the material is the point of the ordering.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import importlib.util,pathlib; s=importlib.util.spec_from_file_location('p17','scripts/phase17_personas.py'); m=importlib.util.module_from_spec(s); s.loader.exec_module(m); assert len(m.HOLM_FAMILY_CELLS)==6; assert set(m.CELL_ALTERNATIVE)==set(m.HOLM_FAMILY_CELLS); assert m.worst_pair({(i,j):0.0 for i in m.PERSONAS for j in m.PERSONAS if i!=j})==('persona_a','persona_b'); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^import torch\|^import torch$" scripts/phase17_personas.py` returns 0 — no torch at module scope.
    - `python -c "import ast,sys; t=ast.parse(open('scripts/phase17_personas.py').read()); print(any(isinstance(n,ast.Call) for n in t.body))"` prints `False` — nothing executes at import beyond literals and defs.
    - `HOLM_FAMILY_CELLS` is built by a comprehension, not a literal tuple of six: `python -c "import ast; t=ast.parse(open('scripts/phase17_personas.py').read()); a=[n for n in t.body if isinstance(n,ast.Assign) and getattr(n.targets[0],'id','')=='HOLM_FAMILY_CELLS']; assert a and isinstance(a[0].value,(ast.GeneratorExp,ast.Call)), 'family is retyped, not derived'"` exits 0.
    - `CELL_ALTERNATIVE` has exactly 6 entries and its key set equals `HOLM_FAMILY_CELLS`.
    - `len(CORE_SLOTS) == 8`, `SLOTS_EXPECTED == len(CORE_SLOTS)` (derived, not retyped), and the eight names match RESEARCH F-02's measured fixture order.
    - `gate_cleared` returns False for five-of-six rejected and True only for six-of-six.
    - `worst_pair` returns `("persona_a", "persona_b")` when all six off-diagonal rates are 0.0.
    - `assert_phase17_family_closed` raises `SystemExit` containing the literal `0.0071429` when a seventh pair is passed.
    - `grep -n "0.2486\|0.2000" scripts/phase17_personas.py` returns nothing (ISO-07).
    - `.venv/bin/ruff check scripts/phase17_personas.py && .venv/bin/ruff format --check scripts/phase17_personas.py` exits 0.
  </acceptance_criteria>
  <done>`scripts/phase17_personas.py` is committed carrying the full pre-registration block and the four filter functions, with no persona value in it.</done>
</task>

<task type="auto">
  <name>Task 2: Guard the pre-registration ordering from git history, not from a hand-pinned SHA</name>
  <files>tests/test_phase16_prereg.py</files>
  <read_first>
    - tests/test_phase16_prereg.py (all of it — the ancestry-not-dates docstring, `_git`, `V3_ARTIFACT_GLOBS`, the shallow-clone assertion, the `assert checked` empty-match guard, and the SHA identity test)
    - scripts/phase17_personas.py (the file whose history is the claim)
  </read_first>
  <action>
Extend `tests/test_phase16_prereg.py` additively — do not modify `PREREG_COMMIT`,
`V3_ARTIFACT_GLOBS`, `PREREG_ARTIFACT`, `_git`, or either existing test. The existing
`test_prereg_commit_precedes_every_v3_results_artifact` pins the ERASURE rule (`23a830c`) and is
trivially satisfied by anything Phase 17 writes; it does not pin Phase 17's own pre-registration.

Add `PHASE17_PREREG_ARTIFACT = "scripts/phase17_personas.py"` and one test,
`test_phase17_prereg_is_frozen_before_every_phase17_result`.

The rule is DERIVED from history rather than pinned to a SHA, and the docstring must say why: a
hand-pinned SHA needs a separate identity check to stop it pointing at an unrelated early commit,
and it still permits an edit to the pre-registration AFTER the numbers exist. The derived form is
both smaller and stronger — it is self-identifying and it catches the edit.

Implementation: collect every commit that touches `PHASE17_PREREG_ARTIFACT` via
`git log --format=%H -- <path>`; collect every tracked `results/phase17_*` path via
`git ls-files`; for each, take its first-add commit with `git log --diff-filter=A --format=%H --`
and take the LAST entry (git log is newest-first); then assert with
`git merge-base --is-ancestor <prereg_commit> <first_add>` for every (prereg commit, artifact) pair.
Count the pairs checked.

Three guards this test must carry, mirroring the ones already in the file:
- the shallow-clone assertion (reuse the existing wording and reason — assert, never skip);
- an assertion that `PHASE17_PREREG_ARTIFACT` is tracked and has at least one commit, so the guard
  is never scanning a driver that does not exist;
- `assert checked == len(prereg_commits) * len(tracked_artifacts)` so a broken `git ls-files`
  pattern cannot make it green by matching nothing while artifacts sit on disk.

Record in the docstring that the artifact set is EMPTY until plan 17-05 commits
`results/phase17_personas_report.md`, that the test is therefore a stated vacuous pass in Waves 1-3,
and that plan 17-09's acceptance criteria require `checked > 0`.
  </action>
  <verify>
    <automated>.venv/bin/pytest -q tests/test_phase16_prereg.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest -q tests/test_phase16_prereg.py -x` passes with 3 tests collected.
    - `git diff HEAD~1 -- tests/test_phase16_prereg.py | grep -c "^-"` shows only the diff header lines — the change is additive, `PREREG_COMMIT` and both existing tests are byte-unchanged.
    - `grep -n "PHASE17_PREREG_ARTIFACT" tests/test_phase16_prereg.py` shows the constant and its use in the new test.
    - `grep -n "is-shallow-repository" tests/test_phase16_prereg.py` returns 2 hits — the new test carries its own shallow-clone assertion.
    - Deliberate-RED proof recorded in the SUMMARY: temporarily `touch` and commit a dummy `results/phase17_probe.md`, confirm the test still passes (the prereg commit precedes it), then amend `scripts/phase17_personas.py` in a later commit and confirm the test FAILS naming the non-ancestor commit. Revert both; `git status` clean afterwards.
  </acceptance_criteria>
  <done>An edit to the Phase 17 pre-registration after a Phase 17 results artifact is committed turns `tests/test_phase16_prereg.py` red, proven by watching it fail.</done>
</task>

<task type="auto">
  <name>Task 3: Create tests/test_phase17_stats.py — family, gate rule, tie-break, and the four static scans</name>
  <files>tests/test_phase17_stats.py</files>
  <read_first>
    - tests/test_phase16_stats.py lines 1-130 (module header, `_load_driver`, `_tree`, `_function_def`, `_enclosing_functions`, `_call_sites(path, callee)`, `_context_blockquote`)
    - tests/test_phase16_stats.py lines 600-660 and 745-860 (`test_holm_reads_the_family_length_rather_than_a_retyped_six`, `test_holm_stops_at_the_first_failure`, the `_GATE_MODULES` twin target, the identifier-based ban, the `SystemExit`-substring register with its `else: raise AssertionError` branch)
    - tests/test_phase14_scoring.py lines 439-560 (the file-set scanner and the collapsed-glob guard)
    - scripts/phase17_personas.py (the module under test)
    - .planning/phases/17-multi-persona-isolation-matrix/17-CONTEXT.md (D-18's blockquote, at the `**D-18` anchor)
  </read_first>
  <action>
Create `tests/test_phase17_stats.py` in the `test_phase16_stats.py` register: CPU-only, GPU-free, no
torch, drivers loaded with `importlib.util.spec_from_file_location` so `main()` never runs. Copy
`_tree`, `_function_def`, `_enclosing_functions`, `_call_sites(path, callee)` and
`_context_blockquote(anchor)` from `tests/test_phase16_stats.py` — `_context_blockquote` reads
`17-CONTEXT.md` in this file.

Define `_PERSONAS_PATH = scripts/phase17_personas.py` and
`_GATE_MODULES = tuple(sorted((_REPO_ROOT / "scripts").glob("phase17_*.py")))`. Deriving the module
tuple from a glob rather than listing files means `scripts/phase17_isolation.py` and
`scripts/phase17_persona_gate.py` enter the scans automatically when plans 17-04 / 17-06 create them
— which is the F-08 blindness D-21 exists to close. Every scan asserts `len(_GATE_MODULES) >= 1`
before asserting anything about its contents (the collapsed-glob guard).

Write these tests:

- `test_gate_requires_all_six` (D-18) — build the six Holm rows shape by importing
  `phase16_persistence.holm` and feeding `HOLM_FAMILY_CELLS` keys: all six at `sign_test_exact` 8/8
  unanimity (0.0078125) must give `gate_cleared(...) is True`; five at 0.0078125 and one at 0.0703125
  (the achievable 7/8 p) must give `gate_cleared(...) is False`. Also assert `gate_cleared` returns
  False on a five-row list even when all five rejected, so a truncated family cannot clear.
- `test_gate_rationale_is_verbatim_from_context` — assert
  `personas.GATE_AGGREGATION_RATIONALE == _context_blockquote("**D-18")`. Do not retype the
  Portuguese anywhere in this file.
- `test_worst_pair_tiebreak` (D-19) — all six off-diagonal rates 0.0 gives
  `("persona_a", "persona_b")`; a rate set whose `(b, c)` mean is strictly highest gives
  `("persona_b", "persona_c")`; a two-way tie between `(a, c)` and `(b, c)` gives `("persona_a",
  "persona_c")`. Assert the returned pair is ordered by `PERSONAS` index.
- `test_family_closure_bites` — `assert_phase17_family_closed(HOLM_FAMILY_CELLS)` returns without
  raising (the positive control), and a seventh pair raises `SystemExit` whose text contains
  `0.0071429`, using the `try/except/else: raise AssertionError` shape so a silent no-raise fails.
- `test_the_phase16_family_length_coincidence_is_pinned` (F-08) — import `phase16_persistence`,
  assert `len(HOLM_FAMILY_PAIRS) == len(HOLM_FAMILY_CELLS) == 6`, and assert
  `assert_family_length_matches_phase16(len(HOLM_FAMILY_PAIRS))` does not raise while
  `assert_family_length_matches_phase16(7)` raises `SystemExit` naming `CONDITION_ORDER`.
- `test_no_phase14_thresholds` (ISO-07) — for every module in `_GATE_MODULES`, assert the literals
  `0.2486` and `0.2000` do not appear in the source, with a message stating they were derived on
  `CALIBRATION_POOL` and that reusing that pool as a persona makes the gate circular.
- `test_no_new_dependencies` (STAT-04, the genuinely-new half) — AST-walk every module in
  `_GATE_MODULES` for `ast.Import` / `ast.ImportFrom` at ANY depth (lazy function-local imports
  count) and assert every root module name is in
  `set(sys.stdlib_module_names) | {"torch", "numpy", "regex"} | {"personacore"} | {p.stem for p in
  (_REPO_ROOT / "scripts").glob("*.py")}`. Docstring must record that the file-hash half of STAT-04
  is already covered by `tests/test_package.py::test_pyproject_unchanged_since_v2_close`
  (`PYPROJECT_SHA256` at :11, compared at :36) and that a Phase 17 twin of it would be a second copy
  of one rule.
- `test_replication_is_not_gated` (ISO-05) and `test_no_nine_cell_aggregate` (STAT-06) — the
  identifier-based ban from `test_phase16_stats.py:798-823`, applied over `_GATE_MODULES` for callees
  `holm` and `sign_test_exact`, with forbidden substrings
  `("replication", "seed_rep", "aggregate", "overall", "matrix_rate", "isolation_rate")`. Assert
  additionally that none of those substrings appears in any module-level assignment target or
  function name in `_GATE_MODULES`, so a printed 9-cell aggregate is red even if it never reaches a
  test — a printed number gets quoted as a gate.
  </action>
  <verify>
    <automated>.venv/bin/pytest -q tests/test_phase17_stats.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest -q tests/test_phase17_stats.py -x` passes with at least 8 tests collected.
    - `grep -c "seis pares" tests/test_phase17_stats.py` returns 0 — the Portuguese is read from CONTEXT.md, never retyped.
    - `grep -n "import torch" tests/test_phase17_stats.py` returns nothing.
    - `.venv/bin/pytest -q` (full suite) reports at least 579 passed / 1 skipped — no test was lost. Record the exact counts in the SUMMARY.
    - Deliberate-RED recorded for `test_no_new_dependencies`: add a top-level `import pandas` to `scripts/phase17_personas.py`, watch the test fail naming `pandas`, then revert to a byte-identical file (`git diff` empty).
    - `.venv/bin/ruff check tests/test_phase17_stats.py && .venv/bin/ruff format --check tests/test_phase17_stats.py` exits 0.
  </acceptance_criteria>
  <done>Every pre-registered rule in `scripts/phase17_personas.py` has a committed test that fails when the rule changes, and the STAT-04 import scan has been watched failing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git history → published claim | The pre-registration's value rests entirely on ordering being verifiable from the commit DAG rather than asserted in prose |
| planning artifact → committed constant | D-18's rationale exists in CONTEXT.md and in the driver; two copies can diverge |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| TH-17-01 | Repudiation | `scripts/phase17_personas.py` gate constants | mitigate | Task 2's derived history guard: every commit touching the driver must be an ancestor of every `results/phase17_*` first-add commit. Ancestry, never committer dates |
| TH-17-02 | Tampering | `GATE_AGGREGATION_RATIONALE` vs `17-CONTEXT.md` | mitigate | `_context_blockquote("**D-18")` equality, so "verbatim" is never two agreeing hand-typed copies |
| TH-17-03 | Tampering | Phase 16's `CONDITION_ORDER` repricing Phase 17's alpha | mitigate | `assert_family_length_matches_phase16` + `test_the_phase16_family_length_coincidence_is_pinned` (F-08 option a — no edit to a v2.0 driver) |
| TH-17-SC | Tampering | pip installs | accept | Zero packages installed by this phase. STAT-04 requires `pyproject.toml` byte-identical and `tests/test_package.py:11,36` already sha256-pins it, so any install is a committed-test failure. RESEARCH §Package Legitimacy Audit records N/A |
</threat_model>

<verification>
- `.venv/bin/pytest -q tests/test_phase17_stats.py tests/test_phase16_prereg.py -x` passes
- `.venv/bin/pytest -q` full suite at or above the 579 passed / 1 skipped baseline
- `make lint` clean
- `git diff HEAD~N -- pyproject.toml` empty (STAT-04)
</verification>

<success_criteria>
The Phase 17 pre-registration is in git history before any persona value, any adapter and any
`results/phase17_*` artifact exists, and both the all-six gate rule (D-18) and the `worst_pair`
tie-break (D-19) are pinned by tests that have been watched failing.
</success_criteria>

<output>
Create `.planning/phases/17-multi-persona-isolation-matrix/17-01-SUMMARY.md` when done
</output>
