---
phase: 17-multi-persona-isolation-matrix
plan: 05
type: execute
wave: 3
depends_on: ["17-03"]
files_modified:
  - scripts/phase17_persona_gate.py
  - tests/test_phase17_personas.py
autonomous: true
requirements: [ISO-01]

must_haves:
  truths:
    - "The base model's guessability of all 24 minted values is measured with the same instrument Phase 14 and 16 used, imported and never copied"
    - "The probe runs on the fixture's own held-out questions, so it measures the base on the exact instrument the matrix scores on"
    - "The gate report cannot be silently overwritten once it carries a recorded verdict"
    - "Training refuses on a STOP or PENDING verdict"
  artifacts:
    - path: "scripts/phase17_persona_gate.py"
      provides: "GPU pre-flight: token census, four mechanical filters, cached-per-question guessability probe, markdown report with a PENDING verdict"
      contains: "probe_guessability"
    - path: "tests/test_phase17_personas.py"
      provides: "The verdict-blocking assertion — training cannot start on STOP or PENDING"
      contains: "def test_verdict_blocks"
  key_links:
    - from: "scripts/phase17_persona_gate.py"
      to: "scripts/phase14_factset_gate.py"
      via: "probe_guessability imported at its D-16 public entry point, never copied"
      pattern: "probe_guessability"
    - from: "scripts/phase17_persona_gate.py"
      to: "scripts/_verdict.py"
      via: "recorded_verdict — the one anchored `## Verdict` section read"
      pattern: "recorded_verdict"
---

<objective>
Build `scripts/phase17_persona_gate.py`: the GPU pre-flight that measures whether the un-adapted base
can already produce any of the 24 minted values, writes `results/phase17_personas_report.md` with a
`PENDING` verdict and every base completion quoted verbatim, and refuses to clobber that report once
a verdict is recorded.

Purpose: ISO-01's blocking human GO/ADAPT verdict (ROADMAP SC2) needs an artifact to judge. This plan
builds the instrument and its guard; plan 17-07 runs it and takes the verdict. RESEARCH F-13 records
what this measurement buys: if `probe_guessability` proves the base produces zero containments of
each minted value, an off-diagonal hit later cannot be the base's own prior — it must come from
adapter *i*. The ISO-03 column is then the higher-powered confirmation, not the only control.

Output: the committed pre-flight driver and the verdict-blocking test. No report yet — 17-07 owns
the run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17-multi-persona-isolation-matrix/17-CONTEXT.md
@.planning/phases/17-multi-persona-isolation-matrix/17-PATTERNS.md
@.planning/phases/17-multi-persona-isolation-matrix/17-03-SUMMARY.md
</context>

<interfaces>
Imported unchanged:

- `phase14_factset_gate.probe_guessability(model, tok, device, forbid, value, questions, *, start_index=0)`
  (:111) — the D-16 PUBLIC entry point, widened by Phase 16 *specifically* so Phase 17 imports one
  implementation rather than copying it. The caller supplies `questions`, so it holds no fact material
  of its own. `start_index` offsets the per-probe seeding so several batches get disjoint streams.
  Returns `{value, probes: [{question, prompt_ids, completions}], n_probes, n_completions, clean}` —
  every completion verbatim, so the caller can quote them.
- `phase14_factset_gate.assert_report_not_clobbered` (:161) — module-level, zero-arg, reads its
  report path and `sys.argv` at call time. That shape is what makes it monkeypatchable; an inline
  block in `main()` was unreachable from any test without a 278 MB checkpoint, which is how the first
  CR-02 fix left the defect alive.
- `_verdict.recorded_verdict(text)` (:27) — the ONE anchored `## Verdict` SECTION read.
  `VERDICT_SECTION` is `^## Verdict\b(.*?)(?=^## |\Z)` with `re.M | re.S`.
- `teach_persona._require_go_verdict(report_path)` (:166) — takes a path, so Phase 17 calls it with
  its own report. Do not write a third copy of the verdict read.
- `personacore.preflight.preflight_device`, `personacore.config.RuntimeConfig` / `ModelConfig`,
  `personacore.seeding.seed_everything`, `personacore.provenance.git_sha`,
  `personacore.tokenizer.from_json`.
- `phase17_personas.assert_material_passes_filters(tok, questions)`, `PERSONA_FACTS`,
  `VALUE_TOKEN_CENSUS`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Build the pre-flight gate driver</name>
  <files>scripts/phase17_persona_gate.py</files>
  <read_first>
    - scripts/phase14_factset_gate.py in full (the import block and MPS-fallback ordering at :35-60, `probe_guessability` at :111-160, `assert_report_not_clobbered` at :161-182, the census + probe-cache `main()` at :185-468)
    - scripts/_verdict.py (the anchored `VERDICT_SECTION` regex and `recorded_verdict`)
    - scripts/phase17_personas.py (the material, the filters, `assert_material_passes_filters`)
    - scripts/phase16_persistence.py lines 185-230 (`resolve_forbid` — build the mask once and thread it)
    - .planning/phases/17-multi-persona-isolation-matrix/17-RESEARCH.md §F-07 (probe with the fixture's own questions; the per-question cache) and §F-13
  </read_first>
  <action>
Create `scripts/phase17_persona_gate.py` following `scripts/phase14_factset_gate.py` near
line-for-line.

Import block and ordering copied exactly: `os.environ.setdefault("PYTORCH_ENABLE_MPS_FALLBACK", "1")`
BEFORE importing torch, with the T-05-04 comment explaining that an uncovered MPS op falls back to
CPU rather than crashing the run. Module paths: `CONVBASE_SLIM` for the `weights_only=True` load
path, `TOKENIZER_PATH = artifacts/tokenizer.json` marked FROZEN — never retrain, and
`REPORT_PATH = results/phase17_personas_report.md` marked COMMITTED evidence.

`_prove` with the `[phase17_persona_gate]` prefix.

`assert_report_not_clobbered()` — module-level, zero-arg, reading `REPORT_PATH` and `sys.argv` at
call time, using `_verdict.recorded_verdict` and refusing unless the recorded verdict contains
`PENDING`. Copy the docstring's reasoning: an inline block in `main()` is unreachable from any test
without a checkpoint. Support `--force` only as the documented escape, exactly as Phase 14 does.

`main()` under a `__main__` guard, ordered so the clobber guard runs BEFORE anything expensive — a
run that refuses to write its report at the end has already been wasted (RESEARCH Pitfall 7).
Sequence:

1. `assert_report_not_clobbered()`.
2. `preflight_device(strict=True)`; resolve device from `RuntimeConfig()`; `seed_everything` with
   `phase14_recall.SEED`, the instrument's own — ONE seed constant in play.
3. Load the frozen tokenizer and build `forbid_ids` ONCE via `phase16_persistence.resolve_forbid`,
   recording its sha256 through `phase16_persistence.forbid_digest` in the report. Never build a
   second mask.
4. Run the four mechanical filters via `phase17_personas.assert_material_passes_filters(tok,
   questions)` on CPU, BEFORE loading the model — they are seconds of CPU and they make the expensive
   GPU half fail fast on material that was never going to work.
5. Load the un-adapted base with `phase14_recall.load_adapted_model(device)` (no adapter path).
6. Probe. Per RESEARCH F-07, probe with **the fixture's own 13 `core_held_out` questions for that
   slot**, not `SLOT_QUESTION_BANK`: it measures the base on the exact instrument the matrix scores
   on and gives 13 x 4 = 52 completions per slot against Phase 14's 4 x 4 = 16. The base is
   stateless, so cache completions per question and reuse them across all three personas' values for
   that slot — the `probe_cache` idiom at `phase14_factset_gate.main():239-249`. Total generation is
   8 slots x 13 questions x 4 completions = 416, not 1,248. Thread
   `start_index=len(cache_so_far)` so the per-probe generator streams stay disjoint.
7. Write `results/phase17_personas_report.md`: a `## Tokenizer Census` table (the transcribed
   `VALUE_TOKEN_CENSUS` next to the live re-measurement, so a divergence is visible), a
   `## Guessability` section quoting **every** completion verbatim per value with its `clean` flag,
   a `## Filters` section recording which filter rejected what during minting (from the 17-03
   SUMMARY), a `## Provenance` section with `git_sha()`, pid, device, wall clock, the `forbid_ids`
   sha256 and the number masked, and finally `## Verdict` followed by `PENDING`.

Do not write a GO or ADAPT verdict from code. The verdict is a human judgment on quoted evidence
(ROADMAP SC2) and 17-07 records it by hand.
  </action>
  <verify>
    <automated>.venv/bin/pytest -q tests/test_phase17_personas.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import importlib.util;s=importlib.util.spec_from_file_location('g','scripts/phase17_persona_gate.py');m=importlib.util.module_from_spec(s);s.loader.exec_module(m);print('loaded, nothing ran')"` completes without loading a model, a tokenizer or a checkpoint.
    - `grep -n 'PYTORCH_ENABLE_MPS_FALLBACK' scripts/phase17_persona_gate.py` appears BEFORE the first `import torch` line number.
    - `grep -c 'split("## Verdict")' scripts/phase17_persona_gate.py` returns 0 — `recorded_verdict` is imported, never re-split (CR-02).
    - `grep -n "def probe_guessability" scripts/phase17_persona_gate.py` returns nothing — the instrument is imported, never copied (D-16).
    - `assert_report_not_clobbered` is a module-level zero-arg function: `python -c "import inspect,importlib.util;...;assert list(inspect.signature(m.assert_report_not_clobbered).parameters)==[]"` exits 0.
    - `.venv/bin/ruff check scripts/phase17_persona_gate.py && .venv/bin/ruff format --check scripts/phase17_persona_gate.py` exits 0.
    - `git status --short results/` is empty — this plan writes no report.
  </acceptance_criteria>
  <done>The pre-flight instrument is committed, imports `probe_guessability` rather than copying it, and cannot overwrite a recorded verdict.</done>
</task>

<task type="auto">
  <name>Task 2: Pin the blocking verdict — training refuses on STOP or PENDING</name>
  <files>tests/test_phase17_personas.py</files>
  <read_first>
    - tests/test_phase17_personas.py (current state after 17-03)
    - scripts/teach_persona.py lines 160-190 (`_require_go_verdict` and its SystemExit messages)
    - scripts/phase17_persona_gate.py (`REPORT_PATH`, `assert_report_not_clobbered`)
    - scripts/_verdict.py (the anchored section read, and why `split("## Verdict")[-1]` is forbidden)
  </read_first>
  <action>
Extend `tests/test_phase17_personas.py` with the verdict-gate half. No GPU, no checkpoint: write
synthetic report texts to `tmp_path` and call the real functions against them.

- `test_verdict_blocks` (ISO-01, ROADMAP SC2) — `teach_persona._require_go_verdict(path)` raises
  `SystemExit` for a report whose `## Verdict` section reads `PENDING`, for one reading `STOP`, and
  for a file with no `## Verdict` section at all; and returns the verdict word for `GO` and for
  `ADAPT`. Use the `try/except SystemExit/else: raise AssertionError` shape so a silent no-raise
  fails. Assert the message names the file, so an operator reading the abort knows which report to
  fix.
- `test_verdict_read_is_anchored_on_the_section` (CR-02 regression) — a report whose PROSE mentions
  the literal `## Verdict` inside a paragraph, followed by a real `## Verdict` section reading `GO`,
  resolves to `GO`. Keep the naive `text.split("## Verdict")[-1]` in the test as an explicit
  regression tripwire showing it returns a different answer on the same input, exactly as
  `tests/test_phase15_report.py` does for its twin.
- `test_gate_report_clobber_guard_bites` — monkeypatch `phase17_persona_gate.REPORT_PATH` to a
  `tmp_path` file carrying a recorded `GO` verdict and assert `assert_report_not_clobbered()` raises
  `SystemExit` naming the path; then assert it does NOT raise when the recorded verdict is `PENDING`
  (the positive control — without it the test also passes against a guard that refuses everything);
  then assert it does not raise when `--force` is in `sys.argv[1:]`.
  </action>
  <verify>
    <automated>.venv/bin/pytest -q tests/test_phase17_personas.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest -q tests/test_phase17_personas.py -x` passes with at least 12 tests collected.
    - `.venv/bin/pytest -q tests/test_phase17_personas.py -k verdict -x` collects at least 3 tests and passes.
    - The clobber-guard test has both a raising case and a non-raising positive control.
    - `grep -n 'split("## Verdict")' tests/test_phase17_personas.py` returns exactly 1 hit and it is inside the regression-tripwire assertion, never in driver code.
    - `.venv/bin/pytest -q` full suite at or above 579 passed / 1 skipped.
    - `.venv/bin/ruff check tests/ && .venv/bin/ruff format --check tests/` exits 0.
  </acceptance_criteria>
  <done>No adapter can be trained without a recorded GO or ADAPT verdict, and the enforcement has been watched refusing STOP, PENDING and a missing section.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checkpoint file → process | `torch.load` of a `.pt`; the pickle path is arbitrary code execution |
| gate report → training run | The report is the only thing standing between unscreened material and four hours of adapter runs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| TH-17-14 | Elevation of Privilege | checkpoint read in the pre-flight | mitigate | Load only through `phase14_recall.load_adapted_model`, which reaches `checkpoint.load_slim` at `weights_only=True` (the restricted unpickler). Introduce no direct `torch.load` on a shareable artifact (ASVS V5) |
| TH-17-15 | Repudiation | a clobbered gate report | mitigate | `assert_report_not_clobbered` runs FIRST in `main()`, reads the anchored `## Verdict` SECTION via `_verdict.recorded_verdict`, and `--force` is the only documented override |
| TH-17-16 | Spoofing | a prose mention of `## Verdict` read as a recorded verdict | mitigate | `VERDICT_SECTION` anchors on a heading at line start up to the next `## ` heading; the naive tail split is kept in the test as a tripwire, never in driver code |
| TH-17-17 | Tampering | a second copy of the guessability rule drifting from Phase 14/16's | mitigate | `probe_guessability` imported at its D-16 public entry point; acceptance criteria assert no local definition exists |
| TH-17-SC | Tampering | pip installs | accept | Zero packages installed. `tests/test_package.py:11,36` sha256-pins `pyproject.toml` |
</threat_model>

<verification>
- `.venv/bin/pytest -q tests/test_phase17_personas.py -x` passes
- `.venv/bin/pytest -q` full suite at or above 579 passed / 1 skipped
- `make lint` clean
- `git status --short results/` empty — no report is written by this plan
</verification>

<success_criteria>
The ISO-01 pre-flight instrument is committed and reuses Phase 14's guessability probe rather than
copying it, and training is structurally blocked until a human records GO or ADAPT in
`results/phase17_personas_report.md`.
</success_criteria>

<output>
Create `.planning/phases/17-multi-persona-isolation-matrix/17-05-SUMMARY.md` when done
</output>
