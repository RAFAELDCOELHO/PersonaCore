---
phase: 17-multi-persona-isolation-matrix
plan: 07
type: execute
wave: 4
depends_on: ["17-05"]
files_modified:
  - results/phase17_personas_report.md
autonomous: false
requirements: [ISO-01]

must_haves:
  truths:
    - "The un-adapted base's answers to all 24 minted values are measured and quoted verbatim in a committed artifact"
    - "A human read the quoted completions and recorded GO or ADAPT by hand"
    - "No adapter can train until that verdict exists"
  artifacts:
    - path: "results/phase17_personas_report.md"
      provides: "Token census, guessability evidence with every completion quoted, provenance, and the recorded human verdict"
      contains: "## Verdict"
  key_links:
    - from: "results/phase17_personas_report.md"
      to: "scripts/teach_persona.py"
      via: "_require_go_verdict reads this report before any adapter trains"
      pattern: "_require_go_verdict"
---

<objective>
Run `scripts/phase17_persona_gate.py` against the frozen base and the frozen tokenizer, and take the
blocking human GO/ADAPT verdict on the quoted evidence.

Purpose: ROADMAP SC2 makes the verdict a hard blocker. Its judgment is semantic proximity on quoted
base completions — precisely what exact-match cannot see (Phase 14's own reasoning) — so the
measurement is automated and only the decision is manual. RESEARCH F-13 records what a clean result
buys: the base producing zero containments of each minted value means an off-diagonal hit later
cannot be the base's own prior, it must come from adapter *i*.

Output: `results/phase17_personas_report.md`, committed, with a hand-recorded verdict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/17-multi-persona-isolation-matrix/17-CONTEXT.md
@.planning/phases/17-multi-persona-isolation-matrix/17-05-SUMMARY.md
@.planning/phases/17-multi-persona-isolation-matrix/17-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the pre-flight gate and commit the report with a PENDING verdict</name>
  <files>results/phase17_personas_report.md</files>
  <read_first>
    - scripts/phase17_persona_gate.py (the driver being run — read its `main()` ordering and what it writes)
    - results/phase14_factset_report.md (the layout, the quoted-completions register, and the shape of a recorded verdict section)
    - .planning/phases/17-multi-persona-isolation-matrix/17-RESEARCH.md §F-07 (416 completions, cached per question) and §Cost Model (~2-3 min)
  </read_first>
  <action>
Activate the 3.11 venv and run `.venv/bin/python scripts/phase17_persona_gate.py`, capturing stdout
to `results/phase17_personas_gate_run.log`. Expect roughly 2-3 minutes on MPS: 8 slots x 13 questions
x 4 completions = 416 generations, cached per question and reused across all three personas' values
for that slot.

Before running, confirm the preflight line reports `device=mps` and that
`results/phase17_personas_report.md` does not already exist. If it exists with a recorded verdict the
driver will refuse — that refusal is correct and must not be bypassed with `--force`; escalate
instead.

After the run, read the report and verify it is complete and internally consistent:
- the `## Tokenizer Census` table's live re-measurement matches the transcribed `VALUE_TOKEN_CENSUS`
  for all 24 entries, with no row diverging;
- the `## Guessability` section quotes every completion verbatim for every one of the 24 values, with
  its `clean` flag, and the completion counts match 13 questions x 4 draws per slot;
- the `## Provenance` section carries `git_sha`, pid, device, wall clock, the `forbid_ids` sha256 and
  the number of masked ids;
- `## Verdict` reads `PENDING`.

Commit the report and the log. Do not write GO or ADAPT — that is Task 2 and it is a human decision.

If any value comes back NOT clean, do not silently replace it. Record the failing value and its
quoted base completions in the SUMMARY and escalate to Task 2's checkpoint: the human decides
between ADAPT (replace that value and re-run) and STOP.
  </action>
  <verify>
    <automated>test -f results/phase17_personas_report.md && grep -q "^## Verdict" results/phase17_personas_report.md && grep -c "PENDING" results/phase17_personas_report.md</automated>
  </verify>
  <acceptance_criteria>
    - `results/phase17_personas_report.md` exists, is committed, and `recorded_verdict` on its text returns a section containing `PENDING`.
    - `grep -c "clean" results/phase17_personas_report.md` shows one flag per value (24), and the SUMMARY states how many came back clean out of 24.
    - The live census in the report matches `VALUE_TOKEN_CENSUS` for all 24 ids — state the comparison explicitly in the SUMMARY, not "looks right".
    - The provenance section's `git_sha` equals `git rev-parse HEAD` at run time.
    - `.venv/bin/pytest -q tests/test_phase16_prereg.py -x` passes, and its `checked` count is now greater than zero — the Phase 17 pre-registration ordering guard is live from this commit onward. Record the count in the SUMMARY.
    - `.venv/bin/pytest -q` full suite at or above 579 passed / 1 skipped.
  </acceptance_criteria>
  <done>The guessability evidence exists as a committed artifact with a PENDING verdict and every base completion quoted.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human GO / ADAPT verdict on the minted persona material</name>
  <files>results/phase17_personas_report.md</files>
  <read_first>
    - results/phase17_personas_report.md (the whole report, especially every quoted completion)
    - .planning/phases/17-multi-persona-isolation-matrix/17-CONTEXT.md (D-06 — why minting rather than selection, and what the gate is protecting)
  </read_first>
  <what-built>
    An automated pre-flight measured, for each of the 24 minted values, what the un-adapted base
    model says in response to the fixture's own held-out questions for that value's slot — 13
    questions x 4 completions per slot, every completion quoted verbatim. The four mechanical filters
    (token budget, round-trip, substring disjointness, absence from the questions) already ran and
    passed on CPU. The report carries a `PENDING` verdict and nothing downstream can run.
  </what-built>
  <how-to-verify>
    1. Open `results/phase17_personas_report.md`.
    2. Read the `## Guessability` section. For each of the 24 values, read the quoted base
       completions and judge SEMANTIC PROXIMITY, not exact match — that is the whole reason this
       step is human. A value is a problem if the base produces it, a near-variant of it, or an
       answer so close that a later off-diagonal hit would be ambiguous between "adapter i leaked
       persona j's value" and "the base was going to say something like that anyway".
    3. Check the `## Tokenizer Census` table: every value at 8 tokens or fewer, live count matching
       the transcribed count.
    4. Check that the three values in each slot genuinely contradict each other — a slot where two
       personas' values could both be read as the same answer is a collision that dilutes the
       adversarial density D-04 requires.
    5. Record the verdict by editing the `## Verdict` section of the report BY HAND to read exactly
       `GO` or `ADAPT`, followed by a short paragraph naming the reasoning and any value you are
       uneasy about. `ADAPT` means specific named values are replaced and Task 1 re-runs; `STOP`
       means the material is not usable and the phase escalates.
    6. Commit the edited report.
  </how-to-verify>
  <acceptance_criteria>
    - `results/phase17_personas_report.md` `## Verdict` section reads `GO` or `ADAPT` as the first word, verified by `python -c "from pathlib import Path; import sys; sys.path.insert(0,'scripts'); from _verdict import recorded_verdict; print(recorded_verdict(Path('results/phase17_personas_report.md').read_text()))"`.
    - The verdict section carries a written rationale, not a bare word.
    - `python -c "import sys;sys.path.insert(0,'scripts');import teach_persona as t;import pathlib;print(t._require_go_verdict(pathlib.Path('results/phase17_personas_report.md')))"` prints `GO` or `ADAPT` and exits 0 — the same enforcement `train_arm` will run.
    - If the verdict is `ADAPT`: the named values are replaced in `scripts/phase17_personas.py`, `VALUE_TOKEN_CENSUS` is re-measured, `tests/test_phase17_personas.py` passes, and Task 1 re-runs before this checkpoint is re-taken.
    - The report is committed. `git status --short results/` is empty afterwards.
  </acceptance_criteria>
  <action>
Present the report to the human and pause. Do not write GO, ADAPT or STOP from code and do not
auto-approve: ROADMAP SC2 makes this verdict a hard blocker and its judgment is semantic proximity on
quoted completions, which is exactly what exact-match cannot see. Follow the verification steps
below, wait for the human's decision, then commit the report they edited.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import sys,pathlib;sys.path.insert(0,'scripts');from _verdict import recorded_verdict;v=recorded_verdict(pathlib.Path('results/phase17_personas_report.md').read_text());assert v and v.strip().split()[0].upper() in ('GO','ADAPT'), f'verdict is {v!r}';print(v.strip().split()[0])"</automated>
  </verify>
  <done>`results/phase17_personas_report.md` carries a hand-written GO or ADAPT verdict with a rationale, and `teach_persona._require_go_verdict` on that path returns without raising.</done>
  <resume-signal>Type "approved" once the verdict is recorded and committed, or describe which values need replacing.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recorded verdict → training run | The report is the only gate between unscreened material and every downstream number |
| local artifact → public repository | `results/` ships publicly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| TH-17-23 | Repudiation | a verdict written by code rather than by a human | mitigate | The driver writes only `PENDING`; Task 2 is a blocking checkpoint and `teach_persona._require_go_verdict` refuses anything else. This checkpoint is never auto-approvable |
| TH-17-24 | Repudiation | a clobbered gate report | mitigate | `assert_report_not_clobbered` runs first in the driver; `--force` is explicitly refused here and escalated instead |
| TH-17-25 | Information Disclosure | personal data in a public artifact | mitigate | All 24 values are invented (D-06); the human read in step 2 is also the last check that nothing resembling a real identifier ships |
| TH-17-SC | Tampering | pip installs | accept | Zero packages installed. `tests/test_package.py:11,36` sha256-pins `pyproject.toml` |
</threat_model>

<verification>
- `results/phase17_personas_report.md` committed with a hand-recorded GO or ADAPT verdict
- `teach_persona._require_go_verdict` on that path returns without raising
- `.venv/bin/pytest -q tests/test_phase16_prereg.py -x` passes with `checked > 0`
- `.venv/bin/pytest -q` full suite at or above 579 passed / 1 skipped
</verification>

<success_criteria>
A human has read every quoted base completion for all 24 minted values and recorded GO or ADAPT by
hand, and that recorded verdict is what unblocks adapter training.
</success_criteria>

<output>
Create `.planning/phases/17-multi-persona-isolation-matrix/17-07-SUMMARY.md` when done
</output>
