---
phase: 14-teach-then-recall-demo
plan: 03
type: execute
wave: 3
depends_on: [14-02]
files_modified:
  - scripts/phase14_factset.py
  - tests/test_phase14_factset.py
autonomous: true
requirements: [DEMO-05]

must_haves:
  truths:
    - "The fact set that survived the gate is a committed constant, not a value living in a report"
    - "D-01 / D-04: no locked value collides with the measured base priors, and no candidate was rejected for token cost — the census is recorded, the guessability verdict is what rejects"
    - "D-05: composition is 5-8 proper-noun core facts plus 2-3 soft-tier facts, committed as separate constants so the soft tier can be excluded from the pre-registered gate"
    - "The locked values' token census and byte-fallback round-trip are re-validated forever by a CPU-only test"
    - "A future reader is told, in the test's own docstring, that the guessability half is NOT covered by this test"
  artifacts:
    - path: "scripts/phase14_factset.py"
      provides: "LOCKED_FACTS / SOFT_TIER_FACTS / GATE_REJECTED_CANDIDATES / VALUE_TOKEN_CENSUS constants"
      contains: "LOCKED_FACTS"
    - path: "tests/test_phase14_factset.py"
      provides: "D-07 permanent tokenizer-half regression test"
      min_lines: 80
  key_links:
    - from: "tests/test_phase14_factset.py"
      to: "artifacts/tokenizer.json"
      via: "module-scoped from_json fixture on the frozen production artifact"
      pattern: "from_json"
    - from: "scripts/phase14_factset.py"
      to: "results/phase14_factset_report.md"
      via: "FACTSET_GATE_SHA records the commit that produced the verdict (D-08 provenance)"
      pattern: "FACTSET_GATE_SHA"
---

<objective>
Record the gate's outcome as committed constants and pin the tokenizer half of the pre-flight
discipline with a permanent CPU-only test.

Purpose: D-07 splits the pre-flight gate in two. The tokenizer census and byte-fallback round-trip
are standing invariants of the frozen `artifacts/tokenizer.json` and belong in the CPU suite forever.
The guessability measurement is checkpoint-specific — tied to `convbase_best.pt`'s learned priors at
step 4000 — and has no meaning as a standing invariant. Without a test docstring saying so, a future
reader would assume the permanent test covers the full discipline when it structurally cannot.

Output: the locked constants in `scripts/phase14_factset.py` and `tests/test_phase14_factset.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/14-teach-then-recall-demo/14-CONTEXT.md
@.planning/phases/14-teach-then-recall-demo/14-PATTERNS.md
@results/phase14_factset_report.md
@scripts/phase14_factset.py
@tests/test_dialogue_serialize.py

<interfaces>
From `scripts/phase14_factset.py` (shipped by plan 14-02):
- `Fact` NamedTuple: `id`, `slot`, `value`, `tier`
- `CANDIDATE_POOL`, `CALIBRATION_POOL`, `REGISTER_ARM_POOL` — tuples of `Fact`
- `GATE_PROBES: dict[str, tuple[str, ...]]`
- `token_census(tok, value) -> (int, bool)`
- `normalize_for_match(text) -> str`
- `exact_match_clean(completions, value) -> bool`
- `BASE_PRIOR_SEEDS: dict[str, tuple[str, ...]]`
- `all_pools() -> tuple[tuple[str, tuple[Fact, ...]], ...]`

From `personacore.tokenizer`: `from_json(path)`
From `personacore.provenance`: `git_sha()`

Test fixture precedent (`tests/test_dialogue_serialize.py:29, 43-47`):
`TOKENIZER_PATH = "artifacts/tokenizer.json"` plus a `@pytest.fixture(scope="module")` returning
`from_json(TOKENIZER_PATH)` with the "the FROZEN production artifact — the registry that ships is
what matters, never a freshly trained tokenizer (Pitfall 6)" comment. `artifacts/tokenizer.json` is
git-tracked (unlike `checkpoints/`), which is exactly why D-07 splits the two halves.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record the gate outcome as locked constants in scripts/phase14_factset.py</name>
  <files>scripts/phase14_factset.py</files>
  <read_first>
    - results/phase14_factset_report.md (the recorded verdict, exact-match verdict rows, close-call rejection rows, survivor counts, and the census table — every locked number is transcribed from here)
    - scripts/phase14_factset.py (current state — the pools and helpers from plan 14-02)
    - .planning/phases/14-teach-then-recall-demo/14-CONTEXT.md (D-05 composition targets, D-07, D-08 provenance requirement, D-10's contradiction lexicon requirement)
  </read_first>
  <action>
Append a locked-constants block to `scripts/phase14_factset.py`, below the pools. Every value in it
is TRANSCRIBED from `results/phase14_factset_report.md` — the module never parses the report at
runtime (the Phase-13 `finetune_ab.py:66-106` register: hardcoded on purpose, git history order is
the pre-registration proof).

Block header comment must state: these constants were locked by the D-06 blocking verdict recorded in
`results/phase14_factset_report.md`; the surviving set is what the gate returned, and a shrunken set
is a reported outcome, not a failure to work around.

Constants to define:
- `FACTSET_GATE_SHA` — the git commit SHA that carries the recorded verdict, as a string literal.
  D-08 requires each reserved probe to carry `"held out AND measured base-failing at gate time,
  commit <SHA>"` into the DEMO-06 report; this is that SHA.
- `LOCKED_FACTS: tuple[Fact, ...]` — the surviving `core`-tier facts (D-05: 5–8).
- `SOFT_TIER_FACTS: tuple[Fact, ...]` — the surviving `soft`-tier facts (D-05: 2–3). A comment above
  it must state, not footnote, what the soft tier is FOR (narrative texture, breadth of
  personalization) and what it explicitly does NOT contribute (no bearing on DEMO-06's taught or
  held-out thresholds, precisely because low-cardinality slots could not reliably survive the D-03
  close-call filter).
- `CALIBRATION_FACTS: tuple[Fact, ...]` — the calibration-pool survivors (D-09.1).
- `REGISTER_ARM_FACTS: tuple[Fact, ...]` — the register-arm survivors (D-21.1/D-21.2).
- `GATE_REJECTED_CANDIDATES: tuple[Fact, ...]` — every candidate the gate rejected, from all three
  pools. A comment states this is D-10's contradiction-detector lexicon source: the detector's
  vocabulary is `LOCKED_VALUES | {f.value for f in GATE_REJECTED_CANDIDATES}` — committed,
  auditable, pre-existing material requiring zero new editorial judgment.
- `LOCKED_VALUES: tuple[str, ...]` — `tuple(f.value for f in LOCKED_FACTS)`.
- `VALUE_TOKEN_CENSUS: dict[str, int]` — fact id → measured token count, transcribed from the report's
  census table for every fact in `LOCKED_FACTS + SOFT_TIER_FACTS + CALIBRATION_FACTS +
  REGISTER_ARM_FACTS`. This dict is the sole input to plan 14-05's D-19 generation-budget derivation.
- `RESERVED_HELDOUT_PROBES: dict[str, tuple[str, ...]]` — `GATE_PROBES` restricted to the locked and
  soft-tier fact ids. A comment states D-08: these are permanently banned from every teaching set and
  are seed members of DEMO-06's never-seen split, carrying their measured base-failure provenance
  (`FACTSET_GATE_SHA` plus the quoted completion in the report) forward.

If the verdict was `ADAPT`, add a short `# ADAPT deviation:` comment stating the documented deviation
verbatim from the report's verdict section.
  </action>
  <verify>
    <automated>.venv/bin/python -c "
import importlib.util, sys
sys.path.insert(0, 'scripts')
s = importlib.util.spec_from_file_location('phase14_factset', 'scripts/phase14_factset.py')
m = importlib.util.module_from_spec(s); s.loader.exec_module(m)
assert 5 <= len(m.LOCKED_FACTS) <= 8, len(m.LOCKED_FACTS)
assert 2 <= len(m.SOFT_TIER_FACTS) <= 3, len(m.SOFT_TIER_FACTS)
assert all(f.tier == 'core' for f in m.LOCKED_FACTS)
assert all(f.tier == 'soft' for f in m.SOFT_TIER_FACTS)
assert m.LOCKED_VALUES == tuple(f.value for f in m.LOCKED_FACTS)
locked_ids = {f.id for f in m.LOCKED_FACTS + m.SOFT_TIER_FACTS + m.CALIBRATION_FACTS + m.REGISTER_ARM_FACTS}
assert locked_ids <= set(m.VALUE_TOKEN_CENSUS), locked_ids - set(m.VALUE_TOKEN_CENSUS)
assert len(m.FACTSET_GATE_SHA) >= 7
rej = {f.id for f in m.GATE_REJECTED_CANDIDATES}
assert not (rej & locked_ids), rej & locked_ids
print('locked', len(m.LOCKED_FACTS), 'core +', len(m.SOFT_TIER_FACTS), 'soft')"</automated>
  </verify>
  <acceptance_criteria>
    - `5 <= len(LOCKED_FACTS) <= 8` and `2 <= len(SOFT_TIER_FACTS) <= 3`
    - `GATE_REJECTED_CANDIDATES` ids are disjoint from all locked/soft/calibration/register-arm ids
    - `VALUE_TOKEN_CENSUS` has an entry for every locked, soft, calibration, and register-arm fact id
    - `FACTSET_GATE_SHA` is a non-empty string of at least 7 characters and matches a real commit: `git cat-file -e $FACTSET_GATE_SHA` exits 0
    - The `SOFT_TIER_FACTS` comment contains both what the tier is for AND the words "no bearing" on the DEMO-06 thresholds
    - The `GATE_REJECTED_CANDIDATES` comment names D-10's contradiction lexicon
    - `grep -c "read_text\|open(" scripts/phase14_factset.py` returns 0 — the module never parses the report at runtime
    - `.venv/bin/ruff check scripts/phase14_factset.py && .venv/bin/ruff format --check scripts/phase14_factset.py` exits 0
  </acceptance_criteria>
  <done>The gate's surviving fact set, the rejected candidates, the token census, and the reserved held-out probes are committed literals in `scripts/phase14_factset.py`, traceable to a named gate commit.</done>
</task>

<task type="auto">
  <name>Task 2: Create tests/test_phase14_factset.py — the D-07 permanent tokenizer-half test</name>
  <files>tests/test_phase14_factset.py</files>
  <read_first>
    - tests/test_dialogue_serialize.py (lines 1-80 — the header docstring form and the module-scoped frozen-tokenizer fixture to copy)
    - tests/test_phase13_driver.py (lines 13-41 — the scripts-load justification docstring and the `importlib.util.spec_from_file_location` loader)
    - scripts/phase14_factset.py (as written in Task 1)
    - .planning/phases/14-teach-then-recall-demo/14-CONTEXT.md (D-07 — the docstring paragraph this test is REQUIRED to carry)
  </read_first>
  <action>
Create `tests/test_phase14_factset.py`. CPU-only, GPU-free, checkpoint-free — it depends only on
`artifacts/tokenizer.json`, which is git-tracked, so every assertion runs in CI.

Module docstring — three required parts:
1. What is pinned and why: the locked fact set's token-count census and byte-fallback round-trip
   exactness against the FROZEN production tokenizer, forever (D-07).
2. **The mandated D-07 paragraph, stated plainly:** the guessability half of the D-02 pre-flight is
   deliberately NOT covered here and cannot be. That measurement is checkpoint-specific — tied to
   `convbase_best.pt`'s actual learned priors at git sha `04e724c`, step 4000 — and has no meaning
   as a standing invariant. Re-running it against a future checkpoint requires a **fresh gated
   measurement (`scripts/phase14_factset_gate.py --force` plus a new human verdict), not a test
   re-run.** Without this note a future reader would assume this test covers the full pre-flight
   discipline when it structurally cannot. Also state the constraint that forced the split: the
   guessability check needs the 278 MB `convbase_best.pt` on MPS and cannot enter the CPU-only,
   GPU-free suite.
3. The scripts-load justification, adapted from `tests/test_phase13_driver.py:13-21`: no other test
   imports from `scripts/`, but the locked fact set must live in the committed driver module for git
   history to be the pre-registration proof; `scripts/phase14_factset.py` defines no `main()` and
   imports no torch, so an `importlib` load runs nothing.

Loader: `_REPO_ROOT = pathlib.Path(__file__).resolve().parent.parent`, insert
`str(_REPO_ROOT / "scripts")` at `sys.path[0]` if absent, then
`importlib.util.spec_from_file_location("phase14_factset", _REPO_ROOT / "scripts" / "phase14_factset.py")`
and exec. Bind the module once at import time as `fs`.

Module-scoped `tok()` fixture returning `from_json(_REPO_ROOT / "artifacts" / "tokenizer.json")` with
the Pitfall-6 comment copied in register.

Tests:
- `test_token_census_matches_locked_literals(tok)` — for every id in `fs.VALUE_TOKEN_CENSUS`,
  `len(tok.encode(value)) == fs.VALUE_TOKEN_CENSUS[fact_id]`. The literal dict is the expectation;
  never recompute it in-test.
- `test_byte_fallback_roundtrip(tok)` — for every value across all locked/soft/calibration/
  register-arm/rejected facts, `tok.decode(tok.encode(v)) == v`.
- `test_no_dead_ids_emitted(tok)` — every encoded id is in `set(tok.vocab) | set(tok.special_tokens.values())`.
  The test's own docstring records this as the measured no-op: all 256 byte ids are live, so
  `encode()` cannot emit a dead id; the assertion exists to make that a standing fact rather than a
  claim (14-RESEARCH F1).
- `test_composition_targets` — `5 <= len(fs.LOCKED_FACTS) <= 8`, `2 <= len(fs.SOFT_TIER_FACTS) <= 3`,
  and `5 <= len(fs.LOCKED_FACTS) + len(fs.SOFT_TIER_FACTS) <= 10` (the ROADMAP SC1 band).
- `test_pools_and_locked_sets_disjoint` — the value sets of `LOCKED_FACTS`, `CALIBRATION_FACTS`, and
  `REGISTER_ARM_FACTS` are pairwise disjoint (D-09.1, D-21.1), and `GATE_REJECTED_CANDIDATES` shares
  no id with any locked set.
- `test_reserved_probes_cover_every_locked_fact` — every locked and soft fact id has at least 3
  entries in `fs.RESERVED_HELDOUT_PROBES` (D-08), and no reserved probe string contains any locked
  fact value (a probe that names the answer is not a probe).
- `test_locked_values_are_first_person_register_safe` — no locked or soft fact value appears in the
  measured base-prior list `{"cop", "college student", "the country", "red", "rose", "max", "lily",
  "blue"}` after `fs.normalize_for_match`. Docstring cites D-01: guessability is a validity failure
  of the whole demo, so this stays pinned even though the gate already checked it.
  </action>
  <verify>
    <automated>.venv/bin/pytest -q tests/test_phase14_factset.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest -q tests/test_phase14_factset.py -x` passes with at least 7 tests collected
    - `grep -c "skipif\|importorskip" tests/test_phase14_factset.py` returns 0 — every assertion runs in CI
    - `grep -c "checkpoints/" tests/test_phase14_factset.py` returns 0
    - The module docstring contains all of: `checkpoint-specific`, `fresh gated measurement`, `not a test re-run`, and `convbase_best.pt`
    - `grep -c "len(tok.encode" tests/test_phase14_factset.py` is at least 1 AND the expected counts come from `fs.VALUE_TOKEN_CENSUS`, not from a recomputation
    - `.venv/bin/pytest -q` (full suite) passes
    - `.venv/bin/ruff check . && .venv/bin/ruff format --check .` exits 0
  </acceptance_criteria>
  <done>`tests/test_phase14_factset.py` re-validates the locked census and round-trip forever, and its docstring tells a future reader exactly which half of the pre-flight discipline it does not and cannot cover.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| report → source constants | Numbers are transcribed by a human from committed evidence into code |
| frozen tokenizer artifact → test | `artifacts/tokenizer.json` is data-only JSON, git-tracked, never retrained |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-09 | Tampering | locked constants drifting from the report | mitigate | `FACTSET_GATE_SHA` pins the commit that produced the verdict; `git cat-file -e` on it is an acceptance criterion; the module never parses the report at runtime, so drift is visible in a diff rather than silent |
| T-14-10 | Spoofing (of the novel claim) | a future checkpoint silently inheriting a stale guessability verdict | mitigate | The D-07 docstring paragraph states in the test itself that the guessability half is checkpoint-specific and requires a fresh gated measurement, not a test re-run |
| T-14-11 | Information Disclosure | real personal data entering the locked set | mitigate | All values are invented; `test_locked_values_are_first_person_register_safe` additionally pins them away from the measured base priors |
| T-14-SC | Tampering | package installs | accept | Zero new packages |
</threat_model>

<verification>
- `.venv/bin/pytest -q tests/test_phase14_factset.py -x` — green
- `.venv/bin/pytest -q` — full suite green
- `.venv/bin/ruff check . && .venv/bin/ruff format --check .` — clean
- `git cat-file -e $(python -c "import importlib.util,sys; sys.path.insert(0,'scripts'); s=importlib.util.spec_from_file_location('f','scripts/phase14_factset.py'); m=importlib.util.module_from_spec(s); s.loader.exec_module(m); print(m.FACTSET_GATE_SHA)")` exits 0
</verification>

<success_criteria>
- The surviving fact set, the rejected candidates, and the measured token census are committed literals
- The permanent test re-validates the tokenizer half and passes in CI with zero skips
- The test docstring states, in its own words, that the guessability half is checkpoint-specific and not covered
- Composition matches D-05: 5–8 core, 2–3 soft, 5–10 total
</success_criteria>

<output>
Create `.planning/phases/14-teach-then-recall-demo/14-03-SUMMARY.md` when done
</output>
