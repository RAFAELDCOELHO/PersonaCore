---
phase: 14-teach-then-recall-demo
plan: 10
type: execute
wave: 8
depends_on: [14-09]
files_modified:
  - scripts/phase14_recall.py
autonomous: true
requirements: [DEMO-05, DEMO-06, DEMO-07]

must_haves:
  truths:
    - "All three D-11 controls run, and each report section opens by naming the specific ambiguity it closes"
    - "The D-20 three-part reconciliation is committed as report text BEFORE the run that produces the number it reconciles"
    - "Adapter-off logits are proven bit-identical to the un-adapted base on real weights, on CPU"
    - "D-12: a missed threshold is recorded unamended, and any ship decision is logged in a separate section dated after the verdict that explicitly does not reopen or amend the pre-registered threshold"
    - "D-05: the soft tier gets a NAMED report section stating what it is for and that it has no bearing on the DEMO-06 thresholds — not a footnote"
    - "D-22: the threats-to-validity section names the deliberate exclusion of reversed-direction phrasings from the held-out set, so a clean held-out result claims generalization within that scope only"
  artifacts:
    - path: "scripts/phase14_recall.py"
      provides: "The three D-11 controls plus the recall report writer with every pre-registered section"
      contains: "def write_recall_report"
  key_links:
    - from: "scripts/phase14_recall.py"
      to: "personacore.evaluation.masked_perplexity"
      via: "the D-11.2 collapse control uses THE frozen dialogue-val gate metric, never estimate_loss"
      pattern: "masked_perplexity\\("
    - from: "scripts/phase14_recall.py"
      to: "personacore.lora.adapter_disabled"
      via: "the D-11.3 bit-identity control compares logits with the adapter gated off, on CPU"
      pattern: "adapter_disabled\\("
---

<objective>
Add the three D-11 controls and the recall report writer to `scripts/phase14_recall.py`, including
D-20's three-part reconciliation committed as report text before the run that will produce the
negative it reconciles.

Purpose: 14-RESEARCH F4 measured that this base **cannot copy a fact from its own context** — with
`i have a dog named zorp.` in the `<|system|>` span it answered `i have a dog named rose.`, copying
the syntactic frame and substituting a wrong value. D-11.1's question-fairness control is therefore
expected to return a negative, and D-20 requires the reconciliation to be pre-registered rather than
composed after the result. Writing that text now, before the run, is the whole point.

Output: `run_fairness_control`, `run_collapse_control`, `run_bit_identity_control`, and
`write_recall_report` in `scripts/phase14_recall.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/14-teach-then-recall-demo/14-CONTEXT.md
@.planning/phases/14-teach-then-recall-demo/14-RESEARCH.md
@results/phase13_ab_report.md
@scripts/phase14_recall.py
@scripts/make_transcripts.py

<interfaces>
Already in `scripts/phase14_recall.py` (plans 14-05, 14-06, 14-09):
- `RECALL_MAX_NEW_TOKENS`, `SEED`, `N_SEEDED_SAMPLES`, `STOP_IDS`, `TAUGHT_THRESHOLD`,
  `HELDOUT_THRESHOLD`, `CALIBRATION_SHA`, `taught_gate(rate)`, `heldout_gate(rate)`
- `load_adapted_model`, `_complete`, `complete_question`, `echo_provenance`
- `run_scored_recall`, `run_closed_book_control`, `write_transcripts`
- `normalize`, `contains_value`, `score_question`, `find_contradictions`, `has_hedging`,
  `render_context_dump`, `assert_no_value_in_prompt`, `_prove`

From `personacore.evaluation`:
- `masked_perplexity(model, bin_path, mask_path, block_size, device, forbid_ids=None)` — THE frozen
  dialogue-val gate metric. `estimate_loss`'s random-batch mean is disallowed for gates (Phase 12
  12-02).
From `personacore.lora`: `adapter_disabled(model)` context manager, `set_adapter_enabled`
From `personacore.config`: `RuntimeConfig(device="cpu")` — the `demo_app.py:81` explicit-pin idiom
From `personacore.dialogue`: `build_recall_prompt(tok, question, persona=())` — the `persona`
argument exists for exactly one caller: the explicitly-labelled D-11.1 fairness control

Data available: `data/dialog_val.bin` + `data/dialog_val_mask.bin` (1.28 MB / 638 KB)

Measured evidence D-20 rests on (14-RESEARCH F4, first-party, 6 probes):
| fact placement | question | base completion |
|---|---|---|
| `<\|system\|>` persona `i have a dog named zorp.` | `what is your dog's name?` | `i am a cop, i am a cop. i am a cop.` |
| `<\|system\|>` persona `i live in oberlin.` | `where do you live?` | `i live in the country i live in the country.` |
| `<\|system\|>` persona + 1 warm turn | `what is your dog's name?` | `i have a dog named my name is cuddling.` |

14-RESEARCH Pitfall 11: the bit-identity comparison must run on **CPU**, not the preflight device.
Phase 13 measured eval PPL differing by ~3.6e-8 across processes on MPS. With the adapter disabled
the wrapper's forward is literally `self.base(x)`, so bit-identity is structural — but proving it on
the deterministic backend removes the only source of doubt.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Controls 1 and 3 — question fairness and adapter-off bit-identity</name>
  <files>scripts/phase14_recall.py</files>
  <read_first>
    - scripts/phase14_recall.py (current state — the completion helpers and the scored loop)
    - src/personacore/lora/inject.py (lines 109-160 — `set_adapter_enabled` and the `adapter_disabled` context manager)
    - src/personacore/dialogue/serialize.py (`build_recall_prompt`'s `persona` argument)
    - .planning/phases/14-teach-then-recall-demo/14-CONTEXT.md (D-11.1, D-11.3, D-20 in full)
    - .planning/phases/14-teach-then-recall-demo/14-RESEARCH.md (§Measured Findings F4, §Pitfall 2, §Pitfall 11)
  </read_first>
  <action>
Add two controls to `scripts/phase14_recall.py`.

`def run_fairness_control(model, tok, device, forbid, questions, facts)` — **D-11.1, the
question-fairness check.** For each fact, place its taught statement in the `<|system|>` persona span
by calling `build_recall_prompt(tok, question, persona=[<the fact's first-person statement>])`, then
complete greedy plus `N_SEEDED_SAMPLES` and score with `contains_value`. Return the per-question
`k/N` and the aggregate.

Two mandatory comments on this function:
- **This is the ONLY legitimate place a fact value appears in context.** Everywhere else in this
  phase, a fact in the prompt is the demo-killing failure `assert_no_value_in_prompt` exists to catch.
  Here it is the measurement itself, and the report labels it as such.
- **This is a question-VALIDITY check, explicitly NOT a mechanism comparison.** DEMO-F2 (deferred) is
  a comparative baseline measuring prompt-vs-weight recall *parity*; this is a one-directional check
  that validates the question set. Conflating them would read as DEMO-F2 smuggled in early.

`def run_bit_identity_control(tok)` — **D-11.3, adapter-off round-trip bit-identity on real weights.**
It must build its own CPU-pinned model rather than reuse the caller's device-resident one:
`RuntimeConfig(device="cpu")` — the `demo_app.py:81` explicit-pin idiom. 14-RESEARCH Pitfall 11 is the
reason: the claim must be proven on the deterministic backend, because Phase 13 measured MPS eval PPL
differing by ~3.6e-8 across processes.

Procedure:
1. Build model A: `load_slim(CONVBASE_SLIM)` → `GPT` → `load_state_dict` → **no injection**. This is
   the un-adapted conversational base.
2. Build model B: the same load, then `inject_lora` + `load_adapter_weights`, then
   `set_adapter_enabled(model_b, False)`.
3. For a fixed set of prompts (use `build_recall_prompt` over several held-out questions plus the
   empty-question scaffold), run both models under `torch.no_grad()` on CPU and compare the full
   logits tensors with `torch.equal`.
4. `_prove` that every comparison is exactly equal, with a message naming the failing prompt.
5. Also record the max absolute difference (which must be exactly 0.0) so the report can state the
   measured number rather than only the boolean.

Comment: with the adapter disabled the wrapper's forward is literally `self.base(x)`, so bit-identity
is structural — this control turns "inherited from Phase 9's fixture unit tests" into "measured on
the real 13.9M convbase and the real persona adapter," which is what makes the demo's central toggle
claim a measurement rather than an inheritance.
  </action>
  <verify>
    <automated>.venv/bin/python -c "
import importlib.util, sys, inspect
sys.path.insert(0, 'scripts')
s = importlib.util.spec_from_file_location('phase14_recall', 'scripts/phase14_recall.py')
m = importlib.util.module_from_spec(s); s.loader.exec_module(m)
f = inspect.getsource(m.run_fairness_control)
assert 'persona=' in f and 'NOT a mechanism comparison' in f
b = inspect.getsource(m.run_bit_identity_control)
assert 'RuntimeConfig(device=\"cpu\")' in b or \"RuntimeConfig(device='cpu')\" in b
assert 'torch.equal' in b and 'set_adapter_enabled' in b
print('controls 1 and 3 ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `run_fairness_control` is the only function in the file that passes a non-empty `persona=` to `build_recall_prompt`
    - Its source contains the string `NOT a mechanism comparison` and names DEMO-F2
    - `run_bit_identity_control` pins `RuntimeConfig(device="cpu")` and uses `torch.equal` on full logits tensors
    - It builds an un-injected model A and a disabled-adapter model B from the same slim checkpoint
    - It records the max absolute difference alongside the boolean and `_prove`s exact equality with the failing prompt named
    - `grep -c "estimate_loss" scripts/phase14_recall.py` returns 0
    - `.venv/bin/ruff check scripts/ && .venv/bin/ruff format --check scripts/` exits 0
  </acceptance_criteria>
  <done>The fairness control measures in-context answerability with the fact deliberately in the persona span and labels itself as a validity check, and the bit-identity control proves adapter-off equals the un-adapted base on CPU with real weights.</done>
</task>

<task type="auto">
  <name>Task 2: Control 2 — no-collateral-collapse on masked dialogue val PPL</name>
  <files>scripts/phase14_recall.py</files>
  <read_first>
    - scripts/phase14_recall.py (current state after Task 1)
    - src/personacore/evaluation/perplexity.py (lines 83-148 — `masked_perplexity`'s signature and its deterministic full-sweep semantics)
    - scripts/make_transcripts.py (lines 99-183 — transcript generation and the evidence-writing shape)
    - .planning/phases/14-teach-then-recall-demo/14-CONTEXT.md (D-11.2)
    - .planning/phases/14-teach-then-recall-demo/14-RESEARCH.md (§Don't Hand-Roll — the dialogue-quality metric row)
  </read_first>
  <action>
Add `def run_collapse_control(model, tok, device, forbid)` to `scripts/phase14_recall.py` —
**D-11.2, the no-collateral-collapse check.**

Measurement half: `masked_perplexity(model, DIALOG_VAL_BIN, DIALOG_VAL_MASK, BLOCK_SIZE, device,
forbid_ids=forbid)` with the adapter ON, then again inside `adapter_disabled(model)`. Return both
numbers and their fractional delta. Comment: `masked_perplexity` is THE frozen dialogue-val gate
metric — hand-counted-denominator oracle-proven, and `estimate_loss`'s random-batch mean is
disallowed for gates (Phase 12 12-02). Do not build a bespoke dialogue-quality score.

Transcript half: run a small set of UNRELATED PersonaChat-style questions (questions that touch none
of the locked slots) through the adapter ON and adapter OFF, greedy, and record both completions
side by side. FEATURES §4's claim is that the adapter still behaves like the conversational base on
unrelated input rather than a single-topic persona parrot, and a PPL number alone does not let a
reader see that. `_prove` that none of the unrelated questions contains a locked fact value, using
values passed in as an argument.

Return a record carrying both PPL numbers, the fractional delta, the paired transcripts, and a
boolean derived from `teach_persona.COLLAPSE_PPL_TRIGGER` — reported descriptively. Comment states
that the trigger is what D-15's replay verdict was derived from during calibration, so applying the
same threshold here keeps the two measurements on one scale.

**Import `teach_persona` LAZILY, inside `run_collapse_control` — never at module level**
(`14-05-PLAN.md` `<import_topology>` rule 5). Two reasons, both load-bearing: `teach_persona` imports
`phase14_recall` in the other direction, so a module-level edge here is an `ImportError`; and
`teach_persona` imports `phase14_factset` at module level, so hoisting this edge would drag
`LOCKED_VALUES` into `personalize_demo`'s address space through `phase14_recall` and break D-19,
threat T-14-17, and 14-08's must-have. **Do NOT resolve this by copying `COLLAPSE_PPL_TRIGGER` as a
literal into `phase14_recall`** — that also breaks the cycle, and it silently breaks the
"same scale" claim this comment makes, because the calibration verdict and this control would become
two independently-editable numbers. One definition, imported lazily.
  </action>
  <verify>
    <automated>.venv/bin/python -c "
import importlib.util, sys, inspect
sys.path.insert(0, 'scripts')
s = importlib.util.spec_from_file_location('phase14_recall', 'scripts/phase14_recall.py')
m = importlib.util.module_from_spec(s); s.loader.exec_module(m)
src = inspect.getsource(m.run_collapse_control)
assert 'masked_perplexity' in src and 'adapter_disabled' in src
assert 'estimate_loss' not in src
assert 'COLLAPSE_PPL_TRIGGER' in src
assert 'import teach_persona' in src or 'from teach_persona' in src, 'lazy import must live INSIDE run_collapse_control'
assert 'teach_persona' not in sys.modules, 'import_topology rule 5: the edge is lazy'
print('control 2 ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `run_collapse_control` calls `masked_perplexity` exactly twice — once with the adapter on and once inside `adapter_disabled`
    - It runs paired unrelated-question transcripts adapter ON and OFF and returns both completions
    - It `_prove`s no locked value appears in any unrelated question, with the values passed in as an argument
    - The returned record includes both PPL numbers, the fractional delta, and a descriptive boolean against `COLLAPSE_PPL_TRIGGER`
    - The `teach_persona` import statement appears inside `run_collapse_control`'s body, and `"teach_persona" not in sys.modules` still holds after an `importlib` load of `scripts/phase14_recall.py`
    - `grep -c "COLLAPSE_PPL_TRIGGER = " scripts/phase14_recall.py` returns 0 — the trigger is imported, never duplicated as a literal
    - `grep -c "estimate_loss" scripts/phase14_recall.py` returns 0
    - `.venv/bin/ruff check scripts/ && .venv/bin/ruff format --check scripts/` exits 0
  </acceptance_criteria>
  <done>The collapse control measures the frozen dialogue-val gate metric adapter-on versus adapter-off and pairs it with side-by-side transcripts on unrelated questions.</done>
</task>

<task type="auto">
  <name>Task 3: write_recall_report — every pre-registered section, including the D-20 reconciliation</name>
  <files>scripts/phase14_recall.py</files>
  <read_first>
    - results/phase13_ab_report.md (the pre-registration, descriptive-vs-gated metric, and reconciliation registers this report mirrors; and the "Production Config Decision — post-verdict, discretionary" section register D-12 requires verbatim)
    - scripts/phase14_recall.py (current state after Tasks 1 and 2)
    - .planning/phases/14-teach-then-recall-demo/14-CONTEXT.md (D-05 soft tier, D-08, D-10, D-11 shared framing requirement, D-12, D-20 all three parts plus the failure branch, D-22 condition 2)
    - .planning/phases/14-teach-then-recall-demo/14-RESEARCH.md (§Measured Findings F4, §Pitfall 2, §Pitfall 4)
  </read_first>
  <action>
Add `def write_recall_report(records, controls, provenance_lines)` writing
`results/phase14_recall_report.md`. Build a `blocks` list, `"\n".join(...)`, one `write_text`;
pre-compose long table rows into variables for ruff's 100-char limit. Carry the
`measure_inflation.py:66-75` clobber guard on the report path.

**All prose below is committed NOW, in this plan, before the run that produces the numbers it frames.**
That is the point of D-20's pre-registration: the framing cannot be chosen after seeing the result.
Write it as module-level string constants so a reviewer can diff the framing independently of the
numbers, and so `git log` shows it predates the run.

Required sections in order:

- Title, then the Shared-Pattern-6 opener blockquote: **What these numbers are** (fresh-process,
  empty-prompt recall of facts that live only in 331,776 adapter parameters, scored by a substring
  gate against thresholds pre-registered from a disjoint calibration set) and **What they are not**
  (not a chat-quality measure, not comparable to any other model, and not a claim that the model is
  good — only a claim about WHERE the memory lives).
- `## Pre-Registration` — `TAUGHT_THRESHOLD`, `HELDOUT_THRESHOLD`, `CALIBRATION_SHA`,
  `FACTSET_GATE_SHA`, `RECALL_MAX_NEW_TOKENS` and its derivation, `N_SEEDED_SAMPLES`, and the seed
  derivation. State that git history order is the pre-registration proof.
- `## Clean-Room Evidence (SC2)` — the process-boundary provenance, and a statement that every
  question's exact prompt token ids are in `results/phase14_transcripts.md`, that
  `assert_no_value_in_prompt` would have aborted the run if any locked value had appeared in any
  prompt, and that the live demo renders the same ids from the same function (D-18).
- `## Recall Results — Core Tier` — taught rate, held-out rate, per-question `k/N`, and the two gate
  verdicts from `taught_gate` / `heldout_gate`. **Taught and never-seen are reported separately**
  (SC3 / DEMO-06) and the section says which is learning and which could be memorization.
- `## Held-Out Provenance (D-08)` — for every reserved gate probe in the held-out split, the phrase
  "held out AND measured base-failing at gate time, commit `FACTSET_GATE_SHA`" with a pointer to the
  quoted base completion in `results/phase14_factset_report.md`. State that this makes the held-out
  split *proven* unguessable by the base, not merely assumed to be.
- `## Soft Tier — Excluded From The Gate (D-05)` — a NAMED section, not a footnote, stating what the
  soft tier is FOR (narrative texture, breadth of personalization) and what it explicitly does NOT
  contribute (no bearing on DEMO-06's taught or held-out thresholds, precisely because
  low-cardinality slots could not reliably survive the D-03 close-call filter).
- `## Contradiction Events (descriptive, no gate)` — the mechanical detector's counts with every
  contradiction traced to the exact completion text, plus the hedging count reported separately.
  State plainly that no gate is attached, in the same register Phase 13 used for the 79/70 role-token
  leakage that qualified what the retention gate could claim.
- `## Control 1 — Question Fairness (D-11.1)` — **opens by naming the gap it closes**: whether a
  failed closed-book answer means "the model has no memory of this fact" or "the question is
  unanswerable." Label it a question-VALIDITY check and state explicitly that it is NOT a mechanism
  comparison and not DEMO-F2. Then the **D-20 three-part reconciliation, committed verbatim now**:
  - **(a) What the control can no longer prove.** When the base ALSO fails with the fact in context,
    the inference is weakened: a closed-book failure can no longer be read as unambiguous evidence of
    absent memory, because this base demonstrably fails to surface a fact it can see. State this
    plainly as a limitation, without softening. Cite the measured probes, including
    `i have a dog named zorp.` → `i have a dog named rose.` (syntactic frame copied, value
    substituted).
  - **(b) Why the phase's central comparison survives anyway.** The core claim rests on adapter-on vs
    adapter-off, both closed-book, and in NEITHER arm is the fact in context. The base's in-context
    extraction weakness is therefore irrelevant to that specific comparison: both arms face identical
    conditions and the only difference between them is 331,776 adapter parameters, so a gap is
    attributable to the weights and nothing else. (a) constrains what a closed-book failure in
    isolation means; it does not touch the differential.
  - **(c) What the adapter's success is actually demonstrating.** Name the open question honestly:
    is a success (i) knowledge encoded in the weights and then extracted by ordinary reasoning, or
    (ii) stimulus–response pattern completion learned directly from the training paraphrases? These
    are meaningfully different claims and a taught-phrasing success alone cannot separate them.
    **Cite the link explicitly**: D-13's held-out template families generalizing is the evidence that
    distinguishes (i) from (ii), and point at the `## Recall Results — Core Tier` held-out number by
    section name. Do not leave it implicit.
- `## Pre-Registered Failure Branch (D-20)` — committed now, before the held-out number is known:
  if the held-out families ALSO fail, the report states (1) the phase cannot distinguish (i) from
  (ii) and the demo's claim NARROWS to "taught phrasings are recalled from weights"; (2) a
  taught-only success is still a real weights-memory result because it beats the adapter-off control,
  while explicitly DECLINING the stronger generalization claim; (3) that narrower claim cites the
  SAME comparison structure already locked for the core claim — the adapter-on/adapter-off
  closed-book comparison from D-11's controls — and introduces NO new metric invented for the
  fallback branch.
- `## Control 2 — No Collateral Collapse (D-11.2)` — opens by naming the gap it closes: whether the
  adapter turned the model into a single-topic persona parrot. Both PPL numbers, the delta, and the
  paired unrelated-question transcripts.
- `## Control 3 — Adapter-Off Bit Identity (D-11.3)` — opens by naming the gap it closes: whether
  "memory OFF" in the demo is really the un-adapted base. State that it ran on CPU and why
  (14-RESEARCH Pitfall 11), and report the measured max absolute logit difference.
- `## Threats To Validity` — three named items: (1) **D-22** — the held-out set is DELIBERATELY
  scoped to exclude reversed-direction phrasings, which hit the documented reversal curse
  (arxiv.org/abs/2309.12288). A clean held-out result therefore demonstrates generalization *within
  that scope*, not immunity to every documented fine-tuning limitation. (2) the soft tier's
  exclusion, cross-referencing its section. (3) the fairness control's limitation from (a),
  cross-referencing it. Every one names its scope narrowing explicitly.
- `## Verdict` — the two gate results stated mechanically, ending with
  `PENDING — user decision at checkpoint.`
- `## Ship Decision — post-verdict, discretionary` — an empty section with a header comment carrying
  **D-12 verbatim**: a missed threshold is recorded UNAMENDED above; any subsequent decision about
  whether or how the adapter still ships (retry with a different recipe, ship as-is with the miss
  documented, or not ship) is logged HERE, separate from the gate verdict, dated after it, and
  explicit that it does not reopen or amend the pre-registered threshold. Same register as Phase 12's
  "Production Config Decision — post-verdict, discretionary."

Finally, wire all three controls and both writers into `main()` in order: scored recall → closed-book
control → the three D-11 controls → `write_transcripts` → `write_recall_report`.
  </action>
  <verify>
    <automated>.venv/bin/python -c "
import importlib.util, sys, inspect
sys.path.insert(0, 'scripts')
s = importlib.util.spec_from_file_location('phase14_recall', 'scripts/phase14_recall.py')
m = importlib.util.module_from_spec(s); s.loader.exec_module(m)
text = inspect.getsource(m)
for token in ('Question Fairness','No Collateral Collapse','Bit Identity','Threats To Validity','Ship Decision','post-verdict, discretionary','Pre-Registered Failure Branch','Soft Tier','2309.12288','331,776'):
    assert token in text, token
mainsrc = inspect.getsource(m.main)
for fn in ('run_scored_recall','run_closed_book_control','run_fairness_control','run_collapse_control','run_bit_identity_control','write_transcripts','write_recall_report'):
    assert fn in mainsrc, fn
print('report writer + wiring ok')"</automated>
  </verify>
  <acceptance_criteria>
    - The D-20 three-part reconciliation and the pre-registered failure branch exist as module-level string constants, committed in this plan's commit — verifiable by `git log -S "Pre-Registered Failure Branch" -- scripts/phase14_recall.py` predating the run's report
    - Part (c) explicitly names D-13's held-out families as the evidence distinguishing (i) from (ii) and cross-references the results section by name
    - Each of the three control sections opens by naming the specific ambiguity or failure mode it closes
    - `## Soft Tier — Excluded From The Gate` contains both what the tier is for and the phrase `no bearing`
    - `## Threats To Validity` names D-22 with the `2309.12288` citation, the soft tier, and the fairness limitation
    - `## Ship Decision — post-verdict, discretionary` exists and its header text states it does not reopen or amend the pre-registered threshold
    - `## Verdict` ends with `PENDING — user decision at checkpoint.`
    - `main()` calls all seven functions in the stated order
    - `.venv/bin/pytest -q` (full suite) passes
    - `.venv/bin/ruff check . && .venv/bin/ruff format --check .` exits 0
  </acceptance_criteria>
  <done>The recall report writer carries every pre-registered section, and the D-20 reconciliation plus its failure branch are committed as text before the run that produces the number they frame.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fairness control → prompt | This is the one place a fact value legitimately enters context |
| adapter-off claim → the demo's central toggle | The bit-identity control is what turns a structural claim into a measurement |
| report framing → results | Framing chosen after seeing a result is not evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-32 | Spoofing (of the novel claim) | the fairness control's in-context fact | mitigate | It is the only caller passing a non-empty `persona=`, asserted by an acceptance criterion, and the report labels it as a validity check that is explicitly not a mechanism comparison. Everywhere else `assert_no_value_in_prompt` aborts the run |
| T-14-28 | Spoofing | a "memory off" that is still on | mitigate | `run_bit_identity_control` compares full logits with `torch.equal` on CPU between an un-injected model and a disabled-adapter model, on the real 13.9M base and the real adapter, and `_prove`s exact equality |
| T-14-33 | Repudiation | framing composed after the result | mitigate | The D-20 reconciliation and its failure branch are module-level string constants committed in this plan, before the run; `git log -S` on the constant is the check |
| T-14-34 | Repudiation | a missed threshold quietly amended | mitigate | D-12 verbatim: the verdict is recorded unamended and any ship decision lives in a separate, later-dated section that explicitly does not reopen the threshold |
| T-14-35 | Tampering | a bespoke dialogue-quality metric replacing the frozen one | mitigate | `masked_perplexity` only; `grep -c estimate_loss` returns 0 as an acceptance criterion (Phase 12 12-02) |
| T-14-06 | Tampering | a recorded recall verdict clobbered by a rerun | mitigate | The `measure_inflation.py:66-75` clobber guard on the report path |
| T-14-SC | Tampering | package installs | accept | Zero new packages |
</threat_model>

<verification>
- `.venv/bin/python -c "<importlib-load scripts/phase14_recall.py>"` — loads without running anything; all seven functions present and wired into `main()`
- `.venv/bin/pytest -q` — full suite green
- `.venv/bin/ruff check . && .venv/bin/ruff format --check .` — clean
- `git log -S "Pre-Registered Failure Branch" -- scripts/phase14_recall.py` — shows this plan's commit, before the run in plan 14-11
</verification>

<success_criteria>
- All three D-11 controls exist, each opening its report section by naming the ambiguity it closes
- The fairness control is the only place a fact enters context, and is labelled as a validity check, not DEMO-F2
- The bit-identity control runs on CPU with `torch.equal` on full logits and reports a measured max difference
- The collapse control uses `masked_perplexity` adapter on/off plus paired unrelated-question transcripts
- The D-20 three-part reconciliation and its pre-registered failure branch are committed before the run
- D-12's gate-miss policy and a threats-to-validity section naming D-22's deliberate scope narrowing are both present
</success_criteria>

<output>
Create `.planning/phases/14-teach-then-recall-demo/14-10-SUMMARY.md` when done
</output>
