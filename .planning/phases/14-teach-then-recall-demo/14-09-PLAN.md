---
phase: 14-teach-then-recall-demo
plan: 09
type: execute
wave: 7
depends_on: [14-06, 14-07, 14-08]
files_modified:
  - scripts/teach_persona.py
  - scripts/phase14_factset.py
  - scripts/phase14_recall.py
  - tests/test_phase14_scoring.py
  - results/phase14_calibration_report.md
autonomous: false
requirements: [DEMO-05, DEMO-06]

must_haves:
  truths:
    - "One calibration run answers four questions — threshold, family allocation, replay, and register — from one measured source"
    - "Every derived number comes out of a decision rule that was committed before the run"
    - "The recall thresholds stop being None and become committed literals before the real recall run exists"
    - "A human reads the calibration report before the real teaching run burns MPS time"
  artifacts:
    - path: "results/phase14_calibration_report.md"
      provides: "The three arms' measured numbers and the four derivations, with a recorded verdict"
      contains: "## Verdict"
    - path: "scripts/phase14_recall.py"
      provides: "TAUGHT_THRESHOLD and HELDOUT_THRESHOLD as committed literals"
      contains: "TAUGHT_THRESHOLD"
  key_links:
    - from: "results/phase14_calibration_report.md"
      to: "scripts/teach_persona.py"
      via: "every derived number is the output of a CALIBRATION_DECISION_RULE function named in the report"
      pattern: "CALIBRATION_DECISION_RULE"
    - from: "scripts/phase14_recall.py"
      to: "results/phase14_calibration_report.md"
      via: "the locked thresholds carry the calibration commit SHA in their comment"
      pattern: "CALIBRATION_SHA"
---

<objective>
Run the three calibration arms, write `results/phase14_calibration_report.md`, apply the
pre-committed decision rule, and commit the four derived numbers — recall thresholds, family
allocation, replay verdict, register verdict — behind a human review.

Purpose: D-09 + D-14 + D-15 + D-21 deliberately grew ONE calibration run to answer four questions
from one measured source instead of four separately-justified guesses. The rule that converts the
measurements was committed in plan 14-07, before any of these numbers existed; this plan is where the
numbers arrive and the rule is applied mechanically to them.

Output: the calibration report, the locked thresholds in `scripts/phase14_recall.py`, the derived
allocation in `scripts/phase14_factset.py`, and the replay/register verdicts in
`scripts/teach_persona.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/14-teach-then-recall-demo/14-CONTEXT.md
@.planning/phases/14-teach-then-recall-demo/14-RESEARCH.md
@results/phase13_ab_report.md
@results/inflation_report.md
@scripts/teach_persona.py
@scripts/phase14_recall.py

<interfaces>
From `scripts/teach_persona.py` (plans 14-04 and 14-07):
- `ARMS = ("cal_first_person", "cal_first_person_replay", "cal_second_person", "real")`
- `train_arm(arm, *, facts, family_ids, second_person=False, replay_ratio=0.0)` — returns the arm's
  measured record: final loss, `masked_perplexity` adapter ON and OFF on `dialog_val.bin`+mask, the
  bins stats including the measured mask fraction, and the arm paths
- `CALIBRATION_DECISION_RULE = (lock_thresholds, lock_family_allocation, replay_required,
  first_person_wins)` and the seven literals `CAL_MARGIN_K`, `THRESHOLD_DISCOUNT`,
  `THRESHOLD_FLOOR`, `SATURATION_DELTA`, `HELDOUT_VARIANCE_TRIGGER`, `COLLAPSE_PPL_TRIGGER`,
  `REGISTER_WIN_MARGIN`

From `scripts/phase14_recall.py` (plans 14-05 and 14-06):
- `load_adapted_model`, `run_scored_recall`, `run_closed_book_control`, `normalize`,
  `contains_value`, `score_question`, `RECALL_MAX_NEW_TOKENS`, `N_SEEDED_SAMPLES`,
  `TAUGHT_THRESHOLD = None`, `HELDOUT_THRESHOLD = None`

From `scripts/phase14_factset.py`: `CALIBRATION_FACTS`, `REGISTER_ARM_FACTS`, `TAUGHT_FAMILY_IDS`,
`HELDOUT_FAMILY_IDS`, `FAMILIES`, `FAMILIES_SECOND_PERSON`

Report register analogs: `results/phase13_ab_report.md` (pre-registration + descriptive-vs-gated
metrics + reconciliation sections) and `results/inflation_report.md` (gated verdict + clobber guard).

Arm design (locked by D-09/D-15/D-21):
- `cal_first_person` — `CALIBRATION_FACTS`, first-person register, no replay. The baseline.
- `cal_first_person_replay` — identical except `replay_ratio > 0`. D-15's paired comparison; the ONLY
  difference from the baseline arm is the replay slice.
- `cal_second_person` — `REGISTER_ARM_FACTS`, `second_person=True`, no replay. D-21's register arm;
  its facts are DISJOINT from both the real set and the calibration set, and they passed the same
  D-02/D-03 pre-flight gate.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the three calibration arms and write results/phase14_calibration_report.md</name>
  <files>scripts/teach_persona.py, results/phase14_calibration_report.md</files>
  <read_first>
    - scripts/teach_persona.py (full — `train_arm`, `arm_outputs`, `refuse_if_exists`, `CALIBRATION_DECISION_RULE`)
    - scripts/phase14_recall.py (full — the scoring entry points the calibration recall measurement calls)
    - results/phase13_ab_report.md (the pre-registration + descriptive-metric + reconciliation report register)
    - scripts/measure_inflation.py (lines 60-199 — the clobber guard and report-shape template)
    - .planning/phases/14-teach-then-recall-demo/14-CONTEXT.md (D-09, D-12, D-14, D-15, D-21)
  </read_first>
  <action>
Add a `run_calibration()` entry point to `scripts/teach_persona.py` (or a thin
`--calibration` mode on its `main()`) that executes the three calibration arms in order and writes
`results/phase14_calibration_report.md`.

Per arm:
1. `train_arm(...)` with that arm's facts, families, register flag, and replay ratio.
2. Score recall on the arm's adapter using `phase14_recall`'s scoring entry points — **imported
   lazily inside this scoring function, never at `teach_persona` module level** (`14-05-PLAN.md`
   `<import_topology>` rule 4; the two scripts reference each other in both directions, so a
   module-level edge is an `ImportError`) — over the
   calibration set's taught and held-out family questions, greedy plus `N_SEEDED_SAMPLES`, producing
   per-question `k/N` and the two aggregate rates.
3. Record the arm's `masked_perplexity` adapter ON and OFF on `dialog_val.bin` + mask — the exact
   D-11.2 instrument, so the replay comparison is measured on the collapse metric it is supposed to
   be measured on, not a proxy.
4. Record the per-family recall gain and the per-family held-out standard deviation — the two inputs
   `lock_family_allocation` consumes.

Carry the `measure_inflation.py:66-75` clobber guard on the report path: a recorded (non-PENDING)
verdict is committed evidence and a rerun must not silently reset it; `--force` is required.

Report content, in the `results/phase13_ab_report.md` register. Required sections in order:
- Title, then the Shared-Pattern-6 opener blockquote: **What these numbers are** (a measurement on a
  throwaway fact set DISJOINT from the real one, run to derive four numbers under a decision rule
  committed before the run) and **What they are not** (not a Phase-14 result, not the demo's recall
  rate, not comparable to `results/phase14_recall_report.md`).
- `## Pre-Registration (committed before this run)` — the seven `CALIBRATION_DECISION_RULE` literals
  with their comments, and the git SHA of the commit that introduced them. State that git history
  order is the pre-registration proof (D-09 condition 2).
- `## Arm Design` — the three arms, their fact sets, their register, their replay ratio, and the
  statement that the calibration facts passed the SAME D-02/D-03 pre-flight gate as the real set
  (D-09 condition 1: disposable as an evidence source, not exempt from the validity discipline —
  a calibration set with guessable facts produces an inflated, meaningless ceiling).
- `## Measured Results` — one table per arm: final loss, taught recall rate, held-out recall rate,
  per-family gain, per-family held-out std, masked dialogue val PPL adapter ON and OFF, measured mask
  fraction, corpus token count, wall clock.
- `## Derivation 1 — Recall Thresholds (D-09)` — the `lock_thresholds` inputs, the function's name,
  and its outputs. State the rule was applied mechanically and no number was chosen after seeing the
  results.
- `## Derivation 2 — Family Allocation (D-14)` — the `lock_family_allocation` inputs and outputs, and
  the FOUR invariants it preserved: sets disjoint **and their union still every key of `FAMILIES`**
  (B-02 — the allocation moves families, never drops one), `F4` stays taught (D-22), at least two
  families per side, and every locked fact's taught-instance count still inside
  `PARAPHRASES_PER_FACT_TARGET` (W-03). If saturation fired, say which family moved to held-out and
  what the taught-family count went from and to; if the variance trigger fired, say the held-out side
  grew and at whose expense. **If the paraphrase-band guard REFUSED a move, record it here: which
  family, which fact, and the taught-instance count the move would have produced.** This section is
  the one place that refusal lands — an unrecorded refusal is a silently altered allocation.
- `## Derivation 3 — PersonaChat Replay (D-15)` — the paired ON/OFF PPL from both first-person arms,
  the `replay_required` verdict, and the consequence. If replay is NOT required, state explicitly
  that the real run proceeds without it, preserving the full teaching signal rather than diluting it
  against an unconfirmed risk.
- `## Derivation 4 — Teaching Register (D-21)` — the first-person and second-person held-out rates,
  the `first_person_wins` verdict, and — per D-21 condition 4 — a paragraph citing BOTH the
  qualitative probe evidence (14-RESEARCH F3/F5, including the "structure copied, content not"
  finding where the base answered `i have a dog named my name is cuddling`) AND this quantitative
  delta. State that the measured head-to-head is what the D-01 decision was missing, NOT a
  replacement for the qualitative evidence already gathered. If the arm returns a negative, record it
  unamended under D-12's register and state that it does not reopen D-01 mid-phase.
- `## Verdict` ending with exactly `PENDING — user decision at checkpoint.`

Echo the full run provenance (seed, `git_sha()`, pid, wall clock, `preflight_device` summary,
per-arm paths, per-arm SHA-256 of the exported adapter).
  </action>
  <verify>
    <automated>.venv/bin/python -c "
import pathlib, re
p = pathlib.Path('results/phase14_calibration_report.md')
t = p.read_text(encoding='utf-8')
for h in ('## Pre-Registration','## Arm Design','## Measured Results','## Derivation 1','## Derivation 2','## Derivation 3','## Derivation 4','## Verdict'):
    assert h in t, h
for arm in ('cal_first_person','cal_first_person_replay','cal_second_person'):
    assert arm in t, arm
assert 'What these numbers are' in t and 'What they are not' in t
assert re.search(r'^## Verdict', t, flags=re.M)
print('calibration report shape ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `results/phase14_calibration_report.md` exists with all eight required section headings, in order
    - Each of the three arm names appears in the `## Measured Results` tables with a final loss, both recall rates, and both PPL numbers
    - `## Pre-Registration` lists all seven decision-rule literals and the git SHA of the commit that introduced them
    - `## Derivation 4` cites both the qualitative F3/F5 evidence (including the `my name is cuddling` probe) and the quantitative delta
    - The report's `## Verdict` section ends with `PENDING — user decision at checkpoint.`
    - Re-running without `--force` after a recorded verdict exits non-zero naming the report path
    - `results/phase14_cal_*/run.csv` exists per arm — one CSV per arm, fieldnames fixed at run start
    - Each arm's exported adapter exists at its arm-scoped path and the three paths are distinct
  </acceptance_criteria>
  <done>Three calibration arms have run, their measurements are committed, and all four derivations are recorded with the rule that produced them named in each section.</done>
</task>

<task type="auto">
  <name>Task 2: Commit the four derived numbers into the drivers</name>
  <files>scripts/phase14_recall.py, scripts/phase14_factset.py, scripts/teach_persona.py, tests/test_phase14_scoring.py</files>
  <read_first>
    - results/phase14_calibration_report.md (as written in Task 1 — every number below is transcribed from here)
    - scripts/phase14_recall.py (the `TAUGHT_THRESHOLD = None` / `HELDOUT_THRESHOLD = None` block from plan 14-05)
    - scripts/phase14_factset.py (the calibration-provisional allocation from plan 14-04)
    - scripts/teach_persona.py (the replay ratio and register constants)
    - tests/test_phase14_scoring.py (the `test_preregistration_constants` literals that must grow)
  </read_first>
  <action>
Transcribe the four derived numbers from the report into the drivers. Every one is a literal with its
provenance in a comment; no driver parses a report at runtime.

In `scripts/phase14_recall.py`:
- Replace `TAUGHT_THRESHOLD = None` and `HELDOUT_THRESHOLD = None` with the two float literals
  returned by `lock_thresholds`. The comment above them must name: `teach_persona.lock_thresholds`,
  the two calibration input rates, `THRESHOLD_DISCOUNT`, `THRESHOLD_FLOOR`, and the calibration
  report path.
- Add `CALIBRATION_SHA = "<git sha of the commit carrying the recorded calibration verdict>"` so the
  thresholds are traceable the same way `FACTSET_GATE_SHA` makes the fact set traceable.
- Add `def taught_gate(rate)` and `def heldout_gate(rate)` as pure functions returning
  `rate >= TAUGHT_THRESHOLD` / `rate >= HELDOUT_THRESHOLD`, each with the boundary named in its
  docstring (a rate exactly ON the threshold PASSES — state which comparison is used and why, so a
  future reader can tell `>` from `>=` without reading the test).

In `scripts/phase14_factset.py`:
- Replace the calibration-provisional `TAUGHT_FAMILY_IDS` / `HELDOUT_FAMILY_IDS` with the sets
  `lock_family_allocation` returned. The comment must state which trigger fired (saturation,
  variance, or neither), name the report section, and restate the preserved invariants: the sets are
  disjoint, `F4` is still taught (D-22), and each side keeps at least two families.

In `scripts/teach_persona.py`:
- Set `REAL_RUN_REPLAY_RATIO` to the value the `replay_required` verdict implies (0.0 when replay is
  not required), with a comment naming the measured ON/OFF PPL pair, `COLLAPSE_PPL_TRIGGER`, and the
  verdict.
- Set `REAL_RUN_SECOND_PERSON = False` with a comment naming the `first_person_wins` verdict and its
  two rate inputs. If the verdict was negative, the comment must say so plainly and state that D-01's
  register lock stands on the qualitative evidence and is NOT reopened mid-phase (D-12 register).

In `tests/test_phase14_scoring.py`:
- Extend `test_preregistration_constants` to assert the two threshold literals and `CALIBRATION_SHA`.
- Add `test_gate_boundary` in the `tests/test_phase13_driver.py:58-70` register: assert the exactness
  premise, then that a rate exactly ON each threshold returns the documented result and one hair
  either side flips it. This is the only thing that distinguishes `>` from `>=` in the gates.
  </action>
  <verify>
    <automated>.venv/bin/pytest -q tests/test_phase14_scoring.py tests/test_phase14_teaching.py -x && .venv/bin/python -c "
import importlib.util, sys
sys.path.insert(0, 'scripts')
def load(n):
    s = importlib.util.spec_from_file_location(n, f'scripts/{n}.py'); m = importlib.util.module_from_spec(s); s.loader.exec_module(m); return m
pr = load('phase14_recall'); fs = load('phase14_factset'); tp = load('teach_persona')
assert isinstance(pr.TAUGHT_THRESHOLD, float) and isinstance(pr.HELDOUT_THRESHOLD, float)
assert 0.0 < pr.TAUGHT_THRESHOLD <= 1.0 and 0.0 < pr.HELDOUT_THRESHOLD <= 1.0
assert len(pr.CALIBRATION_SHA) >= 7
assert not (fs.TAUGHT_FAMILY_IDS & fs.HELDOUT_FAMILY_IDS) and 'F4' in fs.TAUGHT_FAMILY_IDS
assert len(fs.TAUGHT_FAMILY_IDS) >= 2 and len(fs.HELDOUT_FAMILY_IDS) >= 2
assert isinstance(tp.REAL_RUN_REPLAY_RATIO, float) and tp.REAL_RUN_SECOND_PERSON is False
print('derived numbers committed')"</automated>
  </verify>
  <acceptance_criteria>
    - `TAUGHT_THRESHOLD` and `HELDOUT_THRESHOLD` are float literals in `(0.0, 1.0]`, no longer `None`
    - `CALIBRATION_SHA` is at least 7 characters and `git cat-file -e $CALIBRATION_SHA` exits 0
    - The comment above the thresholds names `lock_thresholds`, both input rates, and the report path
    - `TAUGHT_FAMILY_IDS` and `HELDOUT_FAMILY_IDS` are disjoint, `F4` is taught, and each side has at least 2 families
    - `REAL_RUN_REPLAY_RATIO` and `REAL_RUN_SECOND_PERSON` exist with comments naming the verdicts and their inputs
    - `test_preregistration_constants` asserts both thresholds and `CALIBRATION_SHA` as bare literals
    - `test_gate_boundary` contains an exactness-premise assertion and covers both sides of both thresholds
    - `.venv/bin/pytest -q` (full suite) passes
    - `.venv/bin/ruff check . && .venv/bin/ruff format --check .` exits 0
  </acceptance_criteria>
  <done>All four derived numbers are committed literals with named provenance, the gate boundaries are pinned by tests, and the drivers still parse no report at runtime.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Review the calibration report and record the verdict</name>
  <files>results/phase14_calibration_report.md</files>
  <what-built>
Three calibration arms have run on MPS (`cal_first_person`, `cal_first_person_replay`,
`cal_second_person`), `results/phase14_calibration_report.md` is written with all four derivations
applied mechanically from the pre-committed rule, and the derived numbers are transcribed into
`scripts/phase14_recall.py`, `scripts/phase14_factset.py`, and `scripts/teach_persona.py`.
  </what-built>
  <action>
Perform the calibration review exactly as laid out in <how-to-verify>: confirm via `git log -S CALIBRATION_DECISION_RULE -- scripts/teach_persona.py` that the decision rule was committed BEFORE the arms ran, sanity-check the three arms' measured numbers, diff each of the four derivation sections against the literals now in `scripts/phase14_recall.py` / `scripts/phase14_factset.py` / `scripts/teach_persona.py`, confirm a negative register result (if any) is recorded unamended, then replace `PENDING — user decision at checkpoint.` under `## Verdict` with `GO`, `ADAPT`, or `STOP`. Commit the report and the three drivers together.
  </action>
  <how-to-verify>
1. Confirm the arms actually ran and the report exists:
   `ls results/phase14_cal_*/run.csv && head -40 results/phase14_calibration_report.md`
2. Read `## Pre-Registration` and confirm its seven literals match what
   `git log -S CALIBRATION_DECISION_RULE -- scripts/teach_persona.py` shows was committed BEFORE the
   arms ran. If the rule was edited after the run, that is a pre-registration violation — stop and
   escalate rather than proceeding.
3. Read `## Measured Results`. Sanity-check three things:
   - the taught recall rate is above the held-out rate (if it is not, something is wrong with the
     split, not with the model);
   - the measured mask fraction sits inside `MASK_FRACTION_BAND` and is a plausible answer-heavy
     value (roughly 0.3–0.7 for this corpus shape);
   - the adapter-OFF masked dialogue val PPL is close to the conversational base's known value —
     adapter OFF should be the un-adapted base.
4. Read each of the four derivation sections and confirm the stated outputs match the literals now in
   the drivers. `git diff` the three scripts against the report's numbers.
5. Read `## Derivation 4` closely. If `first_person_wins` returned a negative, confirm the report
   records it unamended and states that D-01's register lock is not reopened mid-phase. A negative is
   an honest result here, not a reason to re-author the teaching set.
6. Decide whether the real run should proceed. Replace `PENDING — user decision at checkpoint.` under
   `## Verdict` with `GO`, `ADAPT` (with the deviation stated), or `STOP`.
7. Commit the report and the three drivers together, so the derived literals and the evidence that
   produced them land in one commit.
  </how-to-verify>
  <resume-signal>Type "approved" once the verdict is recorded and committed, or describe what needs to change</resume-signal>
  <verify>
    <automated>grep -A3 '^## Verdict' results/phase14_calibration_report.md | grep -Eq '\b(GO|ADAPT|STOP)\b' && echo 'calibration verdict recorded'</automated>
  </verify>
  <acceptance_criteria>
    - `results/phase14_calibration_report.md` `## Verdict` first word is `GO`, `ADAPT`, or `STOP`
    - The literals in `scripts/phase14_recall.py`, `scripts/phase14_factset.py`, and `scripts/teach_persona.py` match the numbers stated in the report's four derivation sections
    - `git log` shows the `CALIBRATION_DECISION_RULE` commit strictly precedes the calibration run's outputs
    - The report and the three drivers are committed together
  </acceptance_criteria>
  <done>A human has confirmed the decision rule predates the numbers, checked each derivation against the committed literals, and recorded a verdict. The real teaching run may not start until this is true.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| calibration measurements → locked thresholds | The conversion must be mechanical and rule-first |
| arm outputs → each other | Three arms write adapters, CSVs, and checkpoints in the same run |
| report → public repo | `results/` ships publicly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-20 | Tampering | thresholds chosen after seeing the numbers | mitigate | The decision rule was committed in plan 14-07; the report's `## Pre-Registration` section names the commit SHA, and the human checkpoint verifies `git log` ordering explicitly before recording a verdict |
| T-14-16 | Tampering | one arm's outputs overwriting another's | mitigate | `arm_outputs` name-scoping plus `refuse_if_exists`, both already CI-tested for pairwise disjointness |
| T-14-06 | Tampering | a recorded calibration verdict clobbered by a rerun | mitigate | The `measure_inflation.py:66-75` clobber guard on the report path; `--force` is required |
| T-14-31 | Repudiation | a derived number with no traceable input | mitigate | `CALIBRATION_SHA` in the driver plus a report section per derivation naming the rule function and its inputs; the human checkpoint diffs the literals against the report |
| T-14-05 | Information Disclosure | calibration fact values in a public report | mitigate | Calibration facts are invented and disjoint from the real set; they went through the same gate, so nothing personal exists to leak |
| T-14-SC | Tampering | package installs | accept | Zero new packages |
</threat_model>

<verification>
- `.venv/bin/pytest -q` — full suite green with the new threshold literals and boundary tests
- `.venv/bin/ruff check . && .venv/bin/ruff format --check .` — clean
- `results/phase14_calibration_report.md` `## Verdict` reads GO, ADAPT, or STOP
- `git log -S CALIBRATION_DECISION_RULE -- scripts/teach_persona.py` shows the rule commit predates the calibration outputs
</verification>

<success_criteria>
- Three calibration arms ran and their measurements are committed, one CSV per arm
- All four derivations are applied mechanically by the pre-committed rule functions and recorded per-section
- `TAUGHT_THRESHOLD` and `HELDOUT_THRESHOLD` are committed float literals with `CALIBRATION_SHA` provenance
- The family allocation is derived, disjoint, keeps `F4` taught, and keeps at least two families per side
- The replay and register verdicts are recorded, with a negative register result stated unamended if it occurred
- A human verdict is recorded before the real teaching run starts
</success_criteria>

<output>
Create `.planning/phases/14-teach-then-recall-demo/14-09-SUMMARY.md` when done
</output>
