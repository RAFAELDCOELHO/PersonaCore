---
phase: 14-teach-then-recall-demo
plan: 07
type: execute
wave: 5
depends_on: [14-04, 14-05]
files_modified:
  - scripts/teach_persona.py
  - tests/test_phase14_teaching.py
autonomous: true
requirements: [DEMO-05, DEMO-06]

must_haves:
  truths:
    - "The teaching run trains only the 331,776 LoRA parameters — every frozen base parameter is bit-identical afterward"
    - "The adapter ships with the base fingerprint READ from the base checkpoint, never recomputed"
    - "The decision rule that turns calibration numbers into the threshold, the family allocation, the replay verdict, and the register verdict is committed BEFORE the calibration run"
    - "An arm that has already produced outputs refuses to re-run"
    - "The family allocation moves families between sides and never drops one, so the two sides always cover every family"
  artifacts:
    - path: "scripts/teach_persona.py"
      provides: "The LoRA training half plus CALIBRATION_DECISION_RULE as four pure functions"
      contains: "def train_arm"
    - path: "tests/test_phase14_teaching.py"
      provides: "Boundary tests for all four decision-rule functions"
      contains: "test_decision_rule"
  key_links:
    - from: "scripts/teach_persona.py"
      to: "personacore.training.train"
      via: "train_bin + train_mask_bin + penalty_fn=None + weight_decay=0.0, no new loop seams"
      pattern: "train_mask_bin="
    - from: "scripts/teach_persona.py"
      to: "personacore.checkpoint.export_adapter"
      via: "base_fingerprint read from the loaded checkpoint blob, never recomputed"
      pattern: "base_fingerprint="
    - from: "scripts/teach_persona.py"
      to: "personacore.lora.snapshot_params"
      via: "params-actually-update canary raising SystemExit on any frozen param that moved"
      pattern: "snapshot_params\\("
---

<objective>
Write the LoRA training half of `scripts/teach_persona.py` and commit the calibration decision rule —
the four pure functions that turn calibration measurements into the recall threshold, the family
allocation, the replay verdict, and the register verdict — before the calibration run exists.

Purpose: D-09 condition 2 is that the decision rule is written down BEFORE calibration happens, never
chosen after seeing the numbers, the same blind-margin discipline as `k=2` in Phase 12's noise floor.
D-14, D-15, and D-21 extend the rule to family allocation, replay, and register, so ONE calibration
run answers four questions from one measured source instead of four separately-justified guesses.

Output: `train_arm` plus `CALIBRATION_DECISION_RULE` in `scripts/teach_persona.py`, with boundary
tests appended to `tests/test_phase14_teaching.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/14-teach-then-recall-demo/14-CONTEXT.md
@.planning/phases/14-teach-then-recall-demo/14-RESEARCH.md
@.planning/phases/14-teach-then-recall-demo/14-PATTERNS.md
@scripts/train_adapter_smoke.py
@scripts/finetune_ab.py

<interfaces>
Already in `scripts/teach_persona.py` (plan 14-04):
- `BLOCK_SIZE = 256`, `MASK_FRACTION_BAND = (0.15, 0.95)`, `REPLAY_RATIO`, `ARMS`
- `_require_go_verdict(report_path)`, `arm_outputs(arm)`, `refuse_if_exists(paths)`
- `render_episodes(facts, family_ids, *, second_person=False)`, `build_bins(...)`

From `personacore.training`: `train(*, train_config, runtime_config=None, model=None,
model_config=None, train_bin=None, val_bin=None, train_mask_bin=None, val_mask_bin=None,
penalty_fn=None, log_path=None, checkpoint_path=None, checkpoint_interval=None,
return_final_loss=False, ...)` — every argument is keyword-only. `train()` guards
[VERIFIED loop.py:277-288]: `train_mask_bin` requires `train_bin`; `val_mask_bin` requires
`train_bin` AND a `.bin` `val_bin`.

From `personacore.config`: `ModelConfig`, `RuntimeConfig`, `TrainConfig` (weight_decay default 0.1)
From `personacore.lora`: `inject_lora`, `mark_only_lora_trainable`, `snapshot_params`,
`lora_state_dict`, `LoRAConfig(r=8, alpha=16)`
From `personacore.checkpoint`: `export_adapter(path, *, adapter, lora_config, base_fingerprint)`
From `personacore.evaluation`: `masked_perplexity(model, bin_path, mask_path, block_size, device, forbid_ids=None)`
From `scripts/phase14_recall.py` (plan 14-05): `normalize`, `contains_value`, `score_question` —
**imported LAZILY, inside the calibration scoring function, never at module level.** See
`14-05-PLAN.md` `<import_topology>` rule 4: `teach_persona` and `phase14_recall` reference each other
in both directions, so a module-level edge here is an `ImportError`. `phase14_recall`'s mirror edge
(rule 5) is lazy for the same reason plus a second one — it would drag
`teach_persona -> phase14_factset -> LOCKED_VALUES` into the demo process.

Verbatim idioms to copy (14-PATTERNS.md quotes each with line numbers):
- `scripts/train_adapter_smoke.py:95-121` — load-order + census-canary block
- `scripts/train_adapter_smoke.py:141-159` — the keyword-only `train()` call with `weight_decay=0.0`
- `scripts/train_adapter_smoke.py:160-176` — the params-actually-update canary (MPS silent-freeze
  hunt, PITFALLS P5), including the frozen-param-changed branch
- `scripts/train_adapter_smoke.py:178-188` — `export_adapter` with the fingerprint READ from the base
- `scripts/finetune_ab.py:112-122` — gate formulas as pure functions with the boundary named
- `scripts/finetune_ab.py:157-165` — `_preseed_csv`; new CSV per arm, fieldnames fixed at run start

Structurally forced facts (14-RESEARCH Pattern 3, both verified):
1. With the base frozen, base θ never moves, so the EWC quadratic anchor is a constant — zero
   gradient into A/B, pure wasted compute, and a chart that would credit EWC with retention that
   frozen-base LoRA produces by construction (PITFALLS P7).
2. `inject_lora` renames every wrapped base parameter with a `.base.` infix
   [VERIFIED lora/layer.py:26 + inject.py:41], while the Fisher cache keys are vanilla-GPT names.
   `EWCPenalty.__call__` raises `ValueError` on ANY fisher key missing from `model.named_parameters()`
   [VERIFIED continual/ewc.py:63-70]. Passing the existing Fisher to an injected model is a hard
   crash, not a silent no-op.

Measured sizing (14-RESEARCH F5): an 8-fact × 30-paraphrase corpus is ≈ 8,200 tokens. At
`batch_size=8 × block_size=256 = 2,048` tokens/step each step sees ~25% of the corpus, so a 200-step
run is ≈ 50 epochs — the deliberate overfit ARCHITECTURE Anti-pattern 6 prescribes.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the LoRA training half of scripts/teach_persona.py</name>
  <files>scripts/teach_persona.py</files>
  <read_first>
    - scripts/train_adapter_smoke.py (full — the near-verbatim template: load → inject → freeze → train → canary → export, plus its docstring rationale for `raise SystemExit` over `assert`)
    - scripts/teach_persona.py (current state from plan 14-04 — the bins half and the section marker)
    - src/personacore/training/loop.py (lines 172-205 the signature block, 277-300 the mask-bin guards)
    - src/personacore/lora/inject.py (lines 29-108 — `inject_lora`, `mark_only_lora_trainable`, `snapshot_params`, `lora_state_dict`)
    - .planning/phases/14-teach-then-recall-demo/14-RESEARCH.md (§Pattern 3, §Pattern 4, §Pitfall 7, §Pitfall 8, §Open Question 3)
  </read_first>
  <action>
Fill in the training-half section of `scripts/teach_persona.py`. Copy
`scripts/train_adapter_smoke.py`'s structure exactly; do not blend it with the bins half's
`prepare_dialog_corpus.py` register.

Recipe constants, each with its provenance in a comment (Claude's Discretion, resolved here):
- `LORA_CFG = LoRAConfig()` — r=8, alpha=16, locked by LORA-01 / Phase 9. Comment records the
  expected census: 36 wrapped projections at 6 layers, 331,776 trainable parameters.
- `LR = 3e-4` — comment: the adapter-run learning rate band from 09-RESEARCH Pattern 3; A/B are the
  only trainable tensors, so the base cannot be damaged by an aggressive rate.
- `WEIGHT_DECAY = 0.0` — comment: established for adapter runs
  (`scripts/train_adapter_smoke.py`, 09-RESEARCH Pattern 3), overriding `TrainConfig`'s 0.1 default.
- `BATCH_SIZE = 8`, `MAX_STEPS = 200`, `WARMUP_STEPS = 20` — comment carries the F5 arithmetic:
  8 × 256 = 2,048 tokens/step against a ≈ 8,200-token corpus is ~25% per step, so 200 steps is
  ≈ 50 epochs — the deliberate overfit ARCHITECTURE Anti-pattern 6 prescribes. State that
  `MAX_STEPS` is one of the numbers the calibration run measures (Assumption A3), not a number this
  plan claims to know.
- `EVAL_INTERVAL`, `CHECKPOINT_INTERVAL` — modest values; a 200-step run needs no heavy cadence.

`def train_arm(arm, *, facts, family_ids, second_person=False, replay_ratio=0.0)`:
1. `_require_go_verdict(FACTSET_REPORT)` — bins and training may only run on GO/ADAPT.
   Then, **for `arm == "real"` ONLY**, also `_require_go_verdict(CALIBRATION_REPORT)` (W-02). The gate
   is arm-conditional because the calibration arms are what PRODUCE that report — gating them on it
   would be a deadlock. Comment says exactly that, so the asymmetry does not read as an oversight.
2. `paths = arm_outputs(arm)`; `refuse_if_exists(...)` on the CSV, checkpoint, and adapter paths.
3. `preflight_device(strict=True)`, print the summary, `runtime = RuntimeConfig()`,
   `seed_everything(SEED)` immediately before the GPT build (it owns data order — the
   `finetune_ab.py` provenance note).
4. Build the bins for this arm via `render_episodes` + `build_bins`, capturing the returned stats
   dict (including the measured mask fraction).
5. Load-order block, copied in structure from `train_adapter_smoke.py:95-121`:
   `blob = torch.load(CONVBASE_BEST, weights_only=False)` with the trusted-own-file comment;
   `model_cfg = ModelConfig(**blob["model_config"])`; `model = GPT(model_cfg)`;
   `model.load_state_dict(blob["model"])` — **LOAD BEFORE INJECT**, the load-bearing ordering;
   `n_wrapped = inject_lora(model, LORA_CFG)` with a `SystemExit` if it is not `6 * n_layer`;
   `mark_only_lora_trainable(model)`; a trainable-parameter census `SystemExit` against
   `LORA_CFG.r * n_layer * 18 * n_embd`; `model.to(runtime.device)` **before**
   `before = snapshot_params(model)` (torch.equal raises on cross-device tensors).
6. The `train()` call, keyword-only in the `train_adapter_smoke.py:141-159` shape:
   `train_config=TrainConfig(lr=LR, warmup_steps=WARMUP_STEPS, max_steps=MAX_STEPS,
   batch_size=BATCH_SIZE, weight_decay=WEIGHT_DECAY)`, `runtime_config=runtime`, `model=model`,
   `model_config=model_cfg`, `train_bin=<arm bin>`, `train_mask_bin=<arm mask bin>`,
   `val_bin=DIALOG_VAL_BIN`, `val_mask_bin=DIALOG_VAL_MASK`, `penalty_fn=None`,
   `log_path=<arm CSV>`, `checkpoint_path=<arm checkpoint>`,
   `checkpoint_interval=CHECKPOINT_INTERVAL`, `return_final_loss=True`.
   Two comments are mandatory here:
   - beside `train_mask_bin`: Phase 14 REVERSES Phase 12's unmasked verdict, by design not drift
     (PITFALLS-14: personalization/QA teaching covers ANSWER tokens only, or the model learns to
     imitate questions instead of answering);
   - beside `penalty_fn=None`: the two independent reasons from 14-RESEARCH Pattern 3, stated
     explicitly rather than left implicit — (a) with the base frozen the quadratic anchor is a
     constant, so it contributes zero gradient to A/B and would credit EWC with retention the frozen
     base produces by construction (PITFALLS P7); (b) `inject_lora` renames base params with a
     `.base.` infix while the Fisher keys are vanilla-GPT names, and `EWCPenalty.__call__` raises
     `ValueError` on any missing key — so passing the existing Fisher would be a hard crash, not a
     silent no-op. `penalty_fn=None` is structurally forced, not merely preferable.
   Also comment beside `val_bin`/`val_mask_bin`: `dialog_val.bin` + mask is used so the in-loop curve
   IS the collateral-collapse signal, giving D-11.2 and D-15 a per-step trace instead of only
   endpoint numbers (14-RESEARCH Open Q3). State that gate decisions still use the deterministic
   `masked_perplexity` sweep and never in-loop `val_loss` (Phase 12 12-02).
7. Canary block, copied verbatim in shape from `train_adapter_smoke.py:160-176`:
   `SystemExit` on a non-finite final loss (PITFALLS P5); then for every named parameter, `SystemExit`
   if a `requires_grad` parameter is `torch.equal` to its snapshot (silent training failure, MPS
   freeze class), and `SystemExit` if a frozen base parameter is NOT equal to its snapshot (grad
   isolation broken, LORA-02).
8. `export_adapter(paths["adapter"], adapter=lora_state_dict(model), lora_config=asdict(LORA_CFG),
   base_fingerprint={"git_sha": blob["git_sha"], "step": blob["step"], "val_loss": blob["val_loss"]})`
   — with the comment that the fingerprint is READ from the base checkpoint, never recomputed
   (provenance trio, 09-CONTEXT D-02).
9. Measure the arm's no-collateral-collapse endpoint with `masked_perplexity` on
   `dialog_val.bin` + mask, adapter ON and adapter OFF (via `adapter_disabled`), and return both plus
   the bins stats, the final loss, and the arm paths.
10. Print the provenance echo (seed, train config, base fingerprint trio, driver `git_sha()`, pid,
    wall clock, measured mask fraction, replay ratio, arm name).

Add a `main()` with a single positional CLI argument naming the arm (validated against `ARMS`, with
a `SystemExit` listing the valid names on a mismatch), `__main__`-guarded.
  </action>
  <verify>
    <automated>.venv/bin/python -c "
import importlib.util, sys, inspect
sys.path.insert(0, 'scripts')
s = importlib.util.spec_from_file_location('teach_persona', 'scripts/teach_persona.py')
m = importlib.util.module_from_spec(s); s.loader.exec_module(m)
assert m.WEIGHT_DECAY == 0.0 and m.BLOCK_SIZE == 256
src = inspect.getsource(m.train_arm)
assert src.index('load_state_dict') < src.index('inject_lora'), 'LOAD BEFORE INJECT'
assert src.index('model.to(') < src.index('snapshot_params'), 'move before snapshot'
assert 'penalty_fn=None' in src and 'train_mask_bin=' in src
assert 'weight_decay=WEIGHT_DECAY' in src or 'weight_decay=0.0' in src
print('training half ok')"</automated>
  </verify>
  <acceptance_criteria>
    - In `train_arm`'s source: `load_state_dict` precedes `inject_lora`, and `model.to(` precedes `snapshot_params`
    - The `train()` call passes `train_mask_bin`, `val_bin`, `val_mask_bin`, `penalty_fn=None`, and `weight_decay=0.0`
    - The comment beside `penalty_fn=None` states BOTH reasons and contains the strings `PITFALLS P7` and `.base.`
    - The comment beside `train_mask_bin` contains `PITFALLS-14` and `by design`
    - The canary block has three `SystemExit` branches: non-finite loss, a trainable that did not move, and a frozen param that did
    - `export_adapter`'s `base_fingerprint` is built from `blob["git_sha"] / blob["step"] / blob["val_loss"]` — `grep -c "git_sha()" ` inside the `export_adapter` call returns 0
    - `grep -c "assert " scripts/teach_persona.py` returns 0
    - `main()` rejects an unknown arm name with a `SystemExit` listing `ARMS`
    - `train_arm("real", ...)` gates on BOTH the fact-set and the calibration verdicts; the calibration arms gate on the fact-set verdict only (W-02)
    - `.venv/bin/ruff check scripts/ && .venv/bin/ruff format --check scripts/` exits 0
  </acceptance_criteria>
  <done>`train_arm` trains only the LoRA parameters on answer-masked bins with `penalty_fn=None` justified in place, proves the base is untouched with a loud canary, and exports an adapter whose fingerprint was read from the base checkpoint.</done>
</task>

<task type="auto">
  <name>Task 2: Commit CALIBRATION_DECISION_RULE — four pure functions, written before the run</name>
  <files>scripts/teach_persona.py</files>
  <read_first>
    - scripts/finetune_ab.py (lines 66-142 — the pre-registration constants block register and the gate-as-pure-function shape with its boundary named in the docstring)
    - scripts/teach_persona.py (current state after Task 1)
    - .planning/phases/14-teach-then-recall-demo/14-CONTEXT.md (D-09, D-14, D-15, D-21 in full — the four derivations this rule must cover)
    - .planning/phases/14-teach-then-recall-demo/14-RESEARCH.md (§Pattern 5 paraphrase-count guidance, §Assumptions A1/A3/A7, §Open Question 5)
  </read_first>
  <action>
Add the calibration decision rule to `scripts/teach_persona.py` as a banner-marked block of
module-level literals and pure functions, in the `finetune_ab.py:66-106` register. The banner comment
must state plainly: this block is committed BEFORE the calibration run produces a single number, and
git history order is the pre-registration proof (D-09 condition 2 — the rule is never chosen after
seeing the results). Every literal carries its provenance in its own comment.

Literals:
- `CAL_MARGIN_K = 2` — comment: reused BLIND from Phase 12's noise-floor discipline, the same
  deliberately conservative default, NOT re-chosen for Phase 14.
- `THRESHOLD_DISCOUNT = 0.60` — the fraction of the calibration ceiling the real run's threshold is
  set to. Comment: the calibration set is disjoint and disposable, so its rate is a ceiling estimate,
  not a target; discounting it is what keeps the real threshold from being a number chosen to be
  passed.
- `THRESHOLD_FLOOR = 0.20` — comment: below this the metric is not distinguishable from the
  closed-book control at `N_SEEDED_SAMPLES = 8`, so a lower "threshold" would be meaningless rather
  than lenient.
- `SATURATION_DELTA = 0.05` — comment: the recall gain below which adding taught families is judged
  saturated (D-14's "recall saturating with fewer taught instances than the literature's ~10-per-fact
  figure suggests"). 14-RESEARCH Pattern 5 records ~10/fact as a floor observed at 7B+ scale, not a
  target for 13.9M.
- `HELDOUT_VARIANCE_TRIGGER = 0.15` — comment: the per-family standard deviation of held-out recall
  above which the real set needs MORE held-out families, even at the cost of fewer taught families
  than the injection literature recommends (D-14, second clause).
- `COLLAPSE_PPL_TRIGGER = 0.10` — comment: the fractional increase in masked dialogue val PPL
  (adapter ON vs OFF, held-out PersonaChat) above which replay becomes MANDATORY for the real run
  (D-15). Below it, the real run proceeds WITHOUT replay, preserving the full teaching signal rather
  than diluting it against an unconfirmed risk.
- `REGISTER_WIN_MARGIN = 0.10` — comment: the absolute held-out recall margin by which first-person
  must beat second-person to count as a win (D-21 condition 3, written before the arm runs).

Pure functions, each with the boundary explicitly named in its docstring (the
`finetune_ab.py:112-122` register — a boundary value is a FAIL, and the docstring says so):
- `def lock_thresholds(cal_taught_rate, cal_heldout_rate)` — returns
  `(taught_threshold, heldout_threshold)`, each `max(THRESHOLD_FLOOR, round(rate * THRESHOLD_DISCOUNT, 4))`.
  Docstring: D-09 — the procedure is pre-registered, not a blind number; these become
  `phase14_recall.TAUGHT_THRESHOLD` / `HELDOUT_THRESHOLD` in plan 14-09.
- `def lock_family_allocation(per_family_gain, heldout_family_std, taught_ids, heldout_ids)` —
  returns `(taught_ids, heldout_ids)` after applying D-14. **It MOVES families between the two sides
  and NEVER drops one (B-02).** A family whose marginal recall gain is `< SATURATION_DELTA`
  (saturation) moves from taught to HELD-OUT; if `heldout_family_std > HELDOUT_VARIANCE_TRIGGER`, the
  lowest-gain taught family also moves to held-out.

  **Why move rather than drop, stated so the contract has one owner.** 14-04's
  `test_families_disjoint` asserts the union of the two sets is every key of `FAMILIES`, and that
  assertion is the authoritative allocation contract for the phase. Dropping a saturated family would
  shrink the union and turn that test red at wave 6 with nothing saying which contract wins. Moving
  is also the honest choice on the merits: a saturated taught family is precisely a family the model
  no longer needs teaching on, so moving it to held-out *increases* the evidence for the
  learning-vs-memorization split D-20(c) depends on, where dropping it would discard that evidence.

  Docstring states the FOUR invariants the function must preserve regardless of the numbers:
  1. the two sets stay disjoint AND their union stays every key of `FAMILIES` (B-02);
  2. `F4` stays on the taught side (D-22 — the reversal curse is a literature failure mode, and
     moving it would poison the evidence D-20(c) depends on);
  3. at least two families remain on each side;
  4. **the taught side keeps enough families that every locked fact's taught-instance count stays
     inside `phase14_factset.PARAPHRASES_PER_FACT_TARGET` (W-03).** A move that would push any fact
     below the band's lower bound is refused, and the refusal is recorded in the calibration report.
     Without this, a saturation-driven move to two taught families trips `build_bins` proof #5 and
     `SystemExit`s the wave-8 real run with no remedy in the plan.
- `def replay_required(ppl_adapter_off, ppl_adapter_on)` — returns True iff
  `(ppl_adapter_on - ppl_adapter_off) / ppl_adapter_off > COLLAPSE_PPL_TRIGGER`. Docstring names the
  boundary as a FAIL-to-trigger (exactly at the trigger, replay is NOT required) and cites D-15's
  paired-arm requirement.
- `def first_person_wins(fp_heldout_rate, sp_heldout_rate)` — returns True iff
  `(fp_heldout_rate - sp_heldout_rate) > REGISTER_WIN_MARGIN`. Docstring cites D-21 condition 3 and
  states that a False result does NOT reopen D-01's register lock — D-01 rests on measured
  qualitative evidence (F3/F5, including the "structure copied, content not" finding), and this arm
  measures the head-to-head delta the decision was missing. A negative is reported honestly under
  D-12's register, not used to re-author the teaching set mid-phase.

Expose them together as `CALIBRATION_DECISION_RULE = (lock_thresholds, lock_family_allocation,
replay_required, first_person_wins)` so plan 14-09 and the tests reference one name.
  </action>
  <verify>
    <automated>.venv/bin/python -c "
import importlib.util, sys
sys.path.insert(0, 'scripts')
s = importlib.util.spec_from_file_location('teach_persona', 'scripts/teach_persona.py')
m = importlib.util.module_from_spec(s); s.loader.exec_module(m)
assert m.CAL_MARGIN_K == 2 and m.THRESHOLD_DISCOUNT == 0.60 and m.THRESHOLD_FLOOR == 0.20
assert m.SATURATION_DELTA == 0.05 and m.HELDOUT_VARIANCE_TRIGGER == 0.15
assert m.COLLAPSE_PPL_TRIGGER == 0.10 and m.REGISTER_WIN_MARGIN == 0.10
assert m.lock_thresholds(1.0, 0.5) == (0.6, 0.3)
assert m.lock_thresholds(0.1, 0.1) == (0.2, 0.2)
assert m.replay_required(2.0, 2.2) is False
assert m.replay_required(2.0, 2.21) is True
assert m.first_person_wins(0.5, 0.4) is False
assert m.first_person_wins(0.5, 0.39) is True
assert len(m.CALIBRATION_DECISION_RULE) == 4
import importlib.util as _iu
_s = _iu.spec_from_file_location('phase14_factset', 'scripts/phase14_factset.py')
_fs = _iu.module_from_spec(_s); _s.loader.exec_module(_fs)
_t, _h = m.lock_family_allocation({k: 0.0 for k in _fs.FAMILIES}, 0.0, set(_fs.TAUGHT_FAMILY_IDS), set(_fs.HELDOUT_FAMILY_IDS))
assert not (_t & _h) and (_t | _h) == set(_fs.FAMILIES), 'B-02: allocation MOVES, never drops'
assert 'F4' in _t and len(_t) >= 2 and len(_h) >= 2
print('decision rule ok')"</automated>
  </verify>
  <acceptance_criteria>
    - All seven literals equal the values above exactly
    - `lock_thresholds(1.0, 0.5) == (0.6, 0.3)` and the floor clamps a low rate to `THRESHOLD_FLOOR`
    - `replay_required` returns False exactly ON the trigger and True one hair past it
    - `first_person_wins` returns False exactly ON the margin and True one hair past it
    - `lock_family_allocation` preserves all four invariants — disjoint sets whose union is exactly `set(FAMILIES)` (never drops a family), `F4` taught, at least two families per side, and every locked fact's taught-instance count inside `PARAPHRASES_PER_FACT_TARGET` — asserted for at least three synthetic inputs including one that must be refused
    - Each function's docstring names its boundary behavior explicitly
    - `first_person_wins`'s docstring states that a negative does not reopen D-01
    - The banner comment above the block contains `committed BEFORE` and `git history order`
    - `.venv/bin/ruff check scripts/ && .venv/bin/ruff format --check scripts/` exits 0
  </acceptance_criteria>
  <done>All four derivations — threshold, family allocation, replay, register — are committed pure functions with named boundaries, before any calibration number exists.</done>
</task>

<task type="auto">
  <name>Task 3: Append decision-rule and recipe tests to tests/test_phase14_teaching.py</name>
  <files>tests/test_phase14_teaching.py</files>
  <read_first>
    - tests/test_phase14_teaching.py (current state from plan 14-04 — the loader and the existing tests)
    - tests/test_phase13_driver.py (lines 44-111 — the literal-constants test, the boundary test with an exactness premise, and the SystemExit-names-the-offender pattern)
    - scripts/teach_persona.py (as written in Tasks 1 and 2)
  </read_first>
  <action>
Append a decision-rule test section to `tests/test_phase14_teaching.py`, using the module's existing
`tp` importlib binding. All CPU-only, checkpoint-free.

- `test_decision_rule_constants` — assert all seven literals as bare literals, in the
  `test_phase13_driver.py:44-56` register. Docstring states these were committed before the
  calibration run and that git history order is the pre-registration proof.
- `test_decision_rule_threshold_boundary` — assert the exactness premise first (the discount is a
  float multiplication over exactly-representable inputs, so the comparison is exact), then assert
  `lock_thresholds` at the floor, just above it, and well above it. The floor must clamp, not reject.
- `test_decision_rule_replay_boundary` — `replay_required` at exactly `COLLAPSE_PPL_TRIGGER` is
  False (boundary FAILS to trigger — dies under `>=`) and one hair past it is True.
- `test_decision_rule_register_boundary` — same shape for `first_person_wins` at exactly
  `REGISTER_WIN_MARGIN`.
- `test_decision_rule_allocation_invariants` — for at least three synthetic
  `(per_family_gain, heldout_family_std)` inputs including a high-variance one and a fully-saturated
  one, assert all four invariants: the returned sets are disjoint AND **their union is exactly
  `set(fs.FAMILIES)`** (B-02 — the same assertion `tests/test_phase14_teaching.py::test_families_disjoint`
  makes, so the two encode one contract); `"F4"` stays in the taught set; at least two families
  remain on each side; and every locked fact's taught-instance count under the returned taught set
  stays inside `fs.PARAPHRASES_PER_FACT_TARGET` (W-03). Include a case whose saturation input would
  push a fact below the band, and assert the function refuses that move rather than returning it.
- `test_recipe_constants` — assert `tp.WEIGHT_DECAY == 0.0`, `tp.LORA_CFG.r == 8`,
  `tp.LORA_CFG.alpha == 16`, `tp.BATCH_SIZE == 8`, `tp.BLOCK_SIZE == 256`. Docstring notes that
  `MAX_STEPS` is deliberately NOT pinned here because it is one of the numbers the calibration run
  measures (Assumption A3).
- `test_arm_outputs_scoped` — in the `test_phase13_driver.py::test_arm_outputs_scoped` register:
  every pair of arms in `tp.ARMS` produces fully disjoint output path sets.
- `test_refuse_if_exists_names_the_offender` — `pytest.raises(SystemExit)` on
  `tp.refuse_if_exists((missing, existing))` with `str(existing)` in `str(excinfo.value)`, using
  `tmp_path`.
- `test_require_go_verdict` — write PENDING, GO, ADAPT, and STOP report fixtures into `tmp_path` and
  assert `_require_go_verdict` returns the verdict for GO/ADAPT and raises `SystemExit` naming the
  verdict for PENDING/STOP.
  </action>
  <verify>
    <automated>.venv/bin/pytest -q tests/test_phase14_teaching.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest -q tests/test_phase14_teaching.py -x` passes with at least 17 tests collected (plan 14-04's 9 plus these)
    - `grep -c "skipif\|importorskip" tests/test_phase14_teaching.py` returns 0
    - Each boundary test contains an explicit exactness-premise assertion before the boundary assertions
    - `test_decision_rule_allocation_invariants` covers a high-variance input and a fully-saturated input
    - `test_require_go_verdict` covers all four verdict words
    - `.venv/bin/pytest -q` (full suite) passes
    - `.venv/bin/ruff check . && .venv/bin/ruff format --check .` exits 0
  </acceptance_criteria>
  <done>Every decision-rule literal, every boundary, the allocation invariants, the recipe constants, arm scoping, refuse-to-rerun, and the verdict gate are pinned in CI before the calibration run executes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk → training run | `checkpoints/convbase_best.pt` is deserialized with `weights_only=False` |
| training run → adapter artifact | The exported adapter is a shareable artifact other processes load |
| calibration numbers → locked thresholds | The rule that converts them must precede them |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04 | Elevation of Privilege | `torch.load(CONVBASE_BEST, weights_only=False)` | mitigate | Trusted-own-file read, stated in the module `SECURITY:` paragraph. The exported adapter goes out through `export_adapter`, so every consumer reads it under `weights_only=True` |
| T-14-25 | Tampering | base weights silently changed during teaching | mitigate | `snapshot_params` before training plus a canary raising `SystemExit` on any frozen parameter that moved (grad isolation broken, LORA-02) and on any trainable that did not (MPS silent-freeze class, PITFALLS P5) |
| T-14-23 | Tampering | adapter shipped with a recomputed or wrong fingerprint | mitigate | `base_fingerprint` is READ from the loaded checkpoint blob; an acceptance criterion greps that `git_sha()` is not used inside the `export_adapter` call |
| T-14-20 | Tampering | thresholds chosen after seeing calibration numbers | mitigate | `CALIBRATION_DECISION_RULE` is committed in this plan, before plan 14-09 runs anything; its literals and boundaries are pinned by CI tests, so a later edit shows as a failing test and a diff |
| T-14-16 | Tampering | one arm overwriting another's recorded evidence | mitigate | `arm_outputs` scoping plus `refuse_if_exists`, both CI-tested for pairwise disjointness |
| T-14-26 | Denial of Service | EWC penalty crashing an injected model | mitigate | `penalty_fn=None` is structurally forced and documented with both reasons; the Fisher-bearing checkpoint's extras are never passed to the injected model |
| T-14-SC | Tampering | package installs | accept | Zero new packages |
</threat_model>

<verification>
- `.venv/bin/pytest -q tests/test_phase14_teaching.py -x` — green
- `.venv/bin/pytest -q` — full suite green
- `.venv/bin/ruff check . && .venv/bin/ruff format --check .` — clean
- `.venv/bin/python scripts/teach_persona.py bogus_arm` exits non-zero with a message listing `ARMS`
- The arms themselves are executed by plan 14-09 (calibration) and plan 14-11 (real run)
</verification>

<success_criteria>
- `train_arm` loads before injecting, freezes to LoRA-only, trains on answer-masked bins with `penalty_fn=None` and `weight_decay=0.0`
- The canary proves every trainable moved and every frozen base parameter is bit-identical, with `SystemExit` on either violation
- The exported adapter carries the fingerprint trio read from the base checkpoint
- All four calibration derivations exist as committed pure functions with named boundaries, before any calibration number exists
- Every decision-rule literal and boundary is pinned by a CI test
</success_criteria>

<output>
Create `.planning/phases/14-teach-then-recall-demo/14-07-SUMMARY.md` when done
</output>
