---
phase: 14-teach-then-recall-demo
plan: 08
type: execute
wave: 5
depends_on: [14-05]
files_modified:
  - scripts/personalize_demo.py
  - tests/test_phase14_demo.py
  - .github/workflows/ci.yml
autonomous: true
requirements: [DEMO-07]

must_haves:
  truths:
    - "scripts/demo_app.py is byte-for-byte unchanged"
    - "The demo makes zero outbound network calls and serves zero remote stylesheets"
    - "The demo process holds no locked fact value — it imports an integer, not the answers"
    - "Memory ON/OFF flips 36 boolean flags in the same process, same prompt, and the token panel proves the context did not change"
    - "After Reset the toggle is visibly disabled and the app still chats with the bare conversational base"
  artifacts:
    - path: "scripts/personalize_demo.py"
      provides: "The gr.Blocks teach-then-recall demo: live toggle, Reset, token panel, budget-floored slider"
      contains: "def build_demo"
    - path: "tests/test_phase14_demo.py"
      provides: "D-17 mask parity, D-18 prompt byte-identity, and the offline stylesheet lock"
      min_lines: 150
    - path: ".github/workflows/ci.yml"
      provides: "demo extra installed so the demo tests actually run in CI"
      contains: "cpu,dev,demo"
  key_links:
    - from: "scripts/personalize_demo.py"
      to: "scripts/phase14_recall.py"
      via: "RECALL_MAX_NEW_TOKENS imported, never re-derived (D-19 / 14-UI-SPEC)"
      pattern: "RECALL_MAX_NEW_TOKENS"
    - from: "scripts/personalize_demo.py"
      to: "personacore.dialogue.build_recall_prompt"
      via: "the token panel renders through phase14_recall.render_context_dump — the same call the harness makes"
      pattern: "render_context_dump\\("
    - from: "scripts/personalize_demo.py"
      to: "personacore.lora.set_adapter_enabled"
      via: "the memory toggle is 36 boolean writes on the live model"
      pattern: "set_adapter_enabled\\("
---

<objective>
Ship `scripts/personalize_demo.py` — the `gr.Blocks` demo with a live memory ON/OFF toggle, a one-way
Reset, a live prompt-token-id panel, and a generation slider floored at the harness's budget — plus
the CI-enforced regression tests that hold the two duplicated-code claims together structurally.

Purpose: DEMO-07 is the frame a reviewer actually watches. D-16 fixes the shape (pre-trained adapter,
toggle plus Reset, no Teach tab). D-17 forbids touching `scripts/demo_app.py`, so the offline
boilerplate is duplicated and a test must catch the drift. D-18 requires the live token panel and the
committed dump to render from the same function. 14-UI-SPEC is the binding layout, copy, color, and
failure contract.

Output: the demo script, `tests/test_phase14_demo.py`, and a one-word CI extras change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/14-teach-then-recall-demo/14-CONTEXT.md
@.planning/phases/14-teach-then-recall-demo/14-UI-SPEC.md
@.planning/phases/14-teach-then-recall-demo/14-RESEARCH.md
@.planning/phases/14-teach-then-recall-demo/14-PATTERNS.md
@scripts/demo_app.py
@tests/test_forbid_ids.py
@tests/test_demo_callback.py

<interfaces>
From `scripts/phase14_recall.py` (plan 14-05):
- `RECALL_MAX_NEW_TOKENS: int` — the D-19 budget; the module's import-time surface holds integers only
- `render_context_dump(tok, question, *, source) -> str` — the D-18 three-line panel/dump renderer

From `personacore.checkpoint`: `load_slim`, `load_adapter`
From `personacore.lora`: `inject_lora`, `load_adapter_weights`, `set_adapter_enabled`,
`eject_adapter`, `LoRAConfig`
  - `set_adapter_enabled(model, enabled)` [inject.py:109-129] — a pre-pass refuses (`RuntimeError`)
    on any merged module BEFORE flipping a single flag, then flips all. Its docstring names Phase 14.
  - `eject_adapter(model) -> int` [inject.py:162-185] — removes every wrapper, returning the vanilla
    module tree and the wrapper count; refuses while merged. **One-way within the process.**
From `personacore.generation`: `generate_text_from_ids` (plan 14-01), `undecodable_ids_mask`
From `personacore.dialogue`: `build_recall_prompt`
From `personacore.config`: `ModelConfig`, `RuntimeConfig`
From `personacore.model`: `GPT`
From `personacore.tokenizer`: `from_json`

Verbatim boilerplate to copy from `scripts/demo_app.py` (READ-ONLY — D-17 forbids editing it):
- lines 29-42: the analytics kill-switch ordering (`os.environ["GRADIO_ANALYTICS_ENABLED"] = "False"`
  BEFORE `import gradio`) with the `# noqa: E402` comments carrying their reason
- lines 62-65 / 75-76: the named missing-artifact message constant raised before `launch()`
- lines 68-90: lazy `build_demo()`, `load_slim` under `weights_only=True`,
  `RuntimeConfig(device="cpu")` pinned explicitly, and
  `forbid_ids = undecodable_ids_mask(tok, model.config.vocab_size)` captured at build time with the
  CR-01 comment
- lines 124-129: `main()` → `build_demo().launch(share=False)`

Measured Gradio facts (14-UI-SPEC, re-verified by the UI checker with zero discrepancies):
- `gr.themes.Default()._stylesheets` → `['https://fonts.googleapis.com/css2?family=Source+Sans+Pro:wght@400;600&display=swap']`
- `gr.themes.Default(font=["ui-sans-serif","system-ui","sans-serif"])._stylesheets` → `[]`; `font` is
  the only theme var that differs
- `theme=` belongs on the `gr.Blocks(...)` constructor; `Blocks.launch()` in 5.50.0 does not accept it
  and the constructor emits a cosmetic `DeprecationWarning` naming the Gradio-6 migration
- `build_recall_prompt(tok, "")` → `[8187, 8185, 8186]`
- `int(undecodable_ids_mask(from_json("artifacts/tokenizer.json"), 8192).sum()) == 7645`
</interfaces>

<structural_gap_resolutions>
**S1 — how D-17, D-18, and the UI-SPEC offline lock are actually enforced, stated out loud.**

Three measured facts collide with the phase's locked decisions:
1. `.github/workflows/ci.yml:16` installs `.[cpu,dev]`; `gradio` lives in the `demo` extra
   (`pyproject.toml:15-19`). Importing `scripts/personalize_demo.py` is an `ImportError` in CI today.
2. `.gitignore:14` ignores `checkpoints/`, so `build_demo()` raises `FileNotFoundError` in CI before
   it ever reaches its `forbid_ids` line — for `demo_app.py` and `personalize_demo.py` alike.
3. `tests/test_demo_callback.py:3-6` states the gradio-free house convention;
   `tests/test_phase13_driver.py:13-21` is the only documented exception and its justification does
   not transfer to a demo script.

**Chosen option: (c) — both halves, with the CI half load-bearing.**

- **CI extras change.** `ci.yml` installs `.[cpu,dev,demo]`. One word. It buys gradio in CI, which is
  what makes any of the enforcement real. Cost: ~30s of CI install time.
- **Checkpoint-free module surface.** `personalize_demo.py` exposes `THEME`,
  `build_forbid_ids(tok, vocab_size)`, and `render_token_panel(tok, question, *, source)` as
  module-level functions that `build_demo()` itself calls. CI tests call those directly.
- **Local-only real-artifact half.** A `skipif`-gated test (the `tests/test_forbid_ids.py:196`
  precedent) builds BOTH real demos and compares the two `build_demo()`-captured masks with
  `torch.equal`, and asserts `personalize_demo.build_demo().stylesheets == []`.

**What this weakens, stated rather than papered over.** D-17's literal wording is "comparing the
resulting mask tensors" from both scripts, and that comparison **cannot** run in CI, because
`demo_app.build_demo()` needs `checkpoints/model_slim.pt` and `checkpoints/` is gitignored. What CI
enforces instead is: (a) `personalize_demo.build_forbid_ids` produces a tensor `torch.equal` to
`undecodable_ids_mask(tok, 8192)` — the exact call `demo_app.py:88` makes — with the same pinned
shape, dtype, and `7645` count `tests/test_forbid_ids.py` already pins for `demo_app`'s side; and
(b) a source assertion that `build_demo` calls `build_forbid_ids` and threads `forbid_ids=` into its
generation call. That catches the named ARCHITECTURE Anti-pattern-7 drift (a missing mask, a wrong
`vocab_size`, a mask not threaded through). What it does **not** catch in CI is a divergence
introduced inside `demo_app.build_demo()` itself — which is acceptable precisely because D-17 freezes
that file, so it cannot drift.

Same treatment for the UI-SPEC offline lock. CI asserts `gr.Blocks(theme=THEME).stylesheets == []`
on the real theme object plus a source assertion that `build_demo` passes `theme=THEME`; the literal
`build_demo().stylesheets == []` runs locally only. D-18's byte-identity test has no such weakening:
both sides route through `phase14_recall.render_context_dump`, which needs no checkpoint, so that one
is fully CI-enforced.

Every `skipif` in `tests/test_phase14_demo.py` must carry a comment naming which assertion is
local-only and why. A silently-skipped test is convention wearing a test's clothes.
</structural_gap_resolutions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the module surface of scripts/personalize_demo.py</name>
  <files>scripts/personalize_demo.py</files>
  <read_first>
    - scripts/demo_app.py (full — READ-ONLY reference; copy its offline/security boilerplate exactly and its layout not at all)
    - .planning/phases/14-teach-then-recall-demo/14-UI-SPEC.md (full — Design System, Copywriting Contract, Token Panel Contract, Generation Budget Slider Contract, Error & Failure-State Contract; every literal UI string comes from here verbatim)
    - scripts/phase14_recall.py (`RECALL_MAX_NEW_TOKENS`, `render_context_dump`)
    - .planning/phases/14-teach-then-recall-demo/14-CONTEXT.md (D-16, D-17, D-18, D-19)
  </read_first>
  <action>
Create `scripts/personalize_demo.py`. **Do not edit `scripts/demo_app.py` — not one byte (D-17).**

Import order is load-bearing: `import os`, `import pathlib`, `import sys`, `import warnings`, then
`os.environ["GRADIO_ANALYTICS_ENABLED"] = "False"` with the 08-RESEARCH Pitfall-5 comment stating it
must precede `import gradio`, then `import gradio as gr  # noqa: E402`, then the `personacore`
imports each with `# noqa: E402` and a reason.

Import `RECALL_MAX_NEW_TOKENS` and `render_context_dump` from `phase14_recall`. Because `scripts/` is
`sys.path[0]` only when the script is run directly, insert
`str(pathlib.Path(__file__).resolve().parent)` into `sys.path` before that import, and wrap it in a
`try/except (ImportError, AttributeError)` raising `SystemExit` with 14-UI-SPEC's exact message:
`Could not read RECALL_MAX_NEW_TOKENS from scripts/phase14_recall.py. This demo does not derive its
own token budget — it uses the scoring harness's, so the UI cannot be set below the measured
condition. Run the recall harness first.` Also `SystemExit` if the imported value is not a positive
int.

Module docstring — required content:
- what it is and how to run it (`python scripts/personalize_demo.py` → http://127.0.0.1:7860);
- the **honesty lock, verbatim** from 14-UI-SPEC's Copywriting Contract: `This is a 13.9M-parameter
  model. Its answers are short and often wrong. What this demo shows is WHERE the memory lives — in
  331,776 adapter parameters, not in this page's context — not how good the model is.`;
- an `OFFLINE GUARANTEE:` paragraph in `demo_app.py`'s register — analytics kill-switch before
  `import gradio`, `analytics_enabled=False`, `share=False`, and the theme font override that empties
  `_stylesheets`, with the one-line reason: the stock theme's body font is a `GoogleFont`, so an
  unmodified page instructs the browser to fetch fonts.googleapis.com (measured);
- a `SECURITY:` paragraph — `load_slim` and `load_adapter` under `weights_only=True`, zero code
  execution on load; `tokenizer.json` is data-only JSON; `max_new_tokens` is bounded and the slider
  maximum is 256;
- a `CLEAN ROOM:` paragraph — this process holds NO locked fact value, in no constant, config,
  example string, or comment. It imports an integer budget. Teaching happened in a different `python`
  invocation (`scripts/teach_persona.py`), which is what makes PITFALLS-11 step 1 true by
  construction (D-16). There is no Teach tab, and that is a closed decision;
- a `CONCURRENCY:` paragraph — the model is a module-scope global that the toggle mutates, so every
  model-touching event shares one `concurrency_id` and serializes; single-user local demo assumed;
  `share=False` plus localhost bounds the exposure;
- a line stating `scripts/demo_app.py` is untouched (D-17) and that the duplicated boilerplate is
  deliberate, with the `forbid_ids` drift test named as its mitigation.

Module-level constants:
- `_REPO_ROOT`-anchored `SLIM_PATH = checkpoints/convbase_slim.pt`,
  `ADAPTER_PATH = checkpoints/persona_adapter.pt`, `TOKENIZER_PATH = artifacts/tokenizer.json`.
- `THEME = gr.themes.Default(font=["ui-sans-serif", "system-ui", "sans-serif"])` with the comment
  recording the measurement (stock `_stylesheets` carries one Google Fonts URL; the override empties
  it; `font` is the only theme var that changes) and the note that `theme=` goes on the `gr.Blocks`
  constructor, that `launch()` does not accept it in 5.50.0, and that the constructor's
  `DeprecationWarning` is cosmetic under the `gradio>=5,<6` pin — note it, do not work around it.
- `MISSING_SLIM_MSG`, `MISSING_ADAPTER_MSG`, `MISSING_BUDGET_MSG` — the three exact strings from
  14-UI-SPEC's failure table.
- `TITLE`, `DESCRIPTION`, `HONESTY_LOCK`, `EMPTY_CHAT`, `EXAMPLES_CAPTION`, `MEMORY_LABEL`,
  `MEMORY_INFO`, `RESET_LABEL`, `STATUS_ON`, `STATUS_OFF`, `STATUS_DELETED`, `PANEL_LABEL`,
  `PANEL_CAPTION`, `ACCORDION_LABEL`, `SLIDER_INFO`, `MISMATCH_BANNER_TEMPLATE` — every one copied
  **verbatim** from 14-UI-SPEC's Copywriting Contract, Token Panel Contract, and Generation Budget
  Slider Contract. No measured number may appear in any UI string beyond the fixed structural ones
  already in the spec (13.9M, 331,776, 1.35 MB, 36) — no recall rates, ever; the header points at
  `results/phase14_recall_report.md` instead.
- `STAMP_ON = "**memory ON**"`, `STAMP_OFF = "**memory OFF**"`, `STAMP_DELETED = "**memory DELETED**"`.
- `EXAMPLES` — exactly 3 question strings drawn from TAUGHT template families (14-UI-SPEC's
  resolution rule). **No fact value may appear in any example string** — questions only. Author them
  by hand here from the taught-family question shapes; do NOT import `phase14_factset`, which would
  pull the locked values into this process.

Module-level functions (checkpoint-free, so CI can test them):
- `def build_forbid_ids(tok, vocab_size)` — returns `undecodable_ids_mask(tok, vocab_size)`. Docstring
  names ARCHITECTURE Anti-pattern 7 and D-17: this is the single call `build_demo()` makes, exposed
  as a function precisely so a CI test can compare its output tensor to the same
  `undecodable_ids_mask` call `scripts/demo_app.py:88` makes, without needing a checkpoint.
- `def render_token_panel(tok, question, *, source)` — returns
  `render_context_dump(tok, question, source=source)` unchanged. Docstring states D-18: no parallel
  prompt builder, no re-encoding for display, no pretty-printing that could reorder or elide ids —
  the panel renders from the exact function the scoring harness calls, and a byte-identity test
  pins it.
  </action>
  <verify>
    <automated>.venv/bin/python -c "
import importlib.util, sys, torch
sys.path.insert(0, 'scripts')
s = importlib.util.spec_from_file_location('personalize_demo', 'scripts/personalize_demo.py')
m = importlib.util.module_from_spec(s); s.loader.exec_module(m)
import gradio as gr
assert gr.Blocks(theme=m.THEME).stylesheets == [], gr.Blocks(theme=m.THEME).stylesheets
from personacore.tokenizer import from_json
t = from_json('artifacts/tokenizer.json')
mask = m.build_forbid_ids(t, 8192)
assert mask.shape == (1, 8192) and mask.dtype == torch.bool and int(mask.sum()) == 7645
p = m.render_token_panel(t, '', source='scaffold').splitlines()
assert len(p) == 3 and '[8187, 8185, 8186]' in p[0]
assert isinstance(m.RECALL_MAX_NEW_TOKENS, int) and m.RECALL_MAX_NEW_TOKENS > 0
assert len(m.EXAMPLES) == 3
print('module surface ok; budget', m.RECALL_MAX_NEW_TOKENS)"</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --quiet -- scripts/demo_app.py` exits 0 — `demo_app.py` is byte-for-byte unchanged
    - The `os.environ["GRADIO_ANALYTICS_ENABLED"]` line appears BEFORE the `import gradio` line in the source
    - `gr.Blocks(theme=THEME).stylesheets == []`
    - `build_forbid_ids(tok, 8192)` returns a `(1, 8192)` bool tensor with `sum() == 7645`
    - `render_token_panel(tok, "", source=...)` returns three lines whose first contains `[8187, 8185, 8186]`
    - Importing the module runs no model load and opens no window
    - The module docstring contains the honesty lock verbatim and the four labelled paragraphs `OFFLINE GUARANTEE:`, `SECURITY:`, `CLEAN ROOM:`, `CONCURRENCY:`
    - `grep -c "phase14_factset" scripts/personalize_demo.py` returns 0 — no fact values in this process
    - `len(EXAMPLES) == 3`
    - `.venv/bin/ruff check scripts/ && .venv/bin/ruff format --check scripts/` exits 0
  </acceptance_criteria>
  <done>The demo's module surface exists with verbatim UI-SPEC copy, an offline-verified theme, a checkpoint-free `forbid_ids` builder and token-panel renderer, and an imported budget integer — with `demo_app.py` untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Build the gr.Blocks UI, the callbacks, and main()</name>
  <files>scripts/personalize_demo.py</files>
  <read_first>
    - scripts/personalize_demo.py (current state after Task 1)
    - .planning/phases/14-teach-then-recall-demo/14-UI-SPEC.md (Layout Contract, Memory Toggle Visual Contract, Interaction Contract, Error & Failure-State Contract — the binding spec; do NOT extrapolate the layout from demo_app.py)
    - .planning/phases/14-teach-then-recall-demo/14-RESEARCH.md (§Architecture Pattern 7 for the wiring, §Code Examples "The live toggle", §Pitfalls 9 and 10)
    - src/personacore/lora/inject.py (lines 109-190 — `set_adapter_enabled` and `eject_adapter` semantics and their merge guards)
  </read_first>
  <action>
Add `build_demo()` and `main()` to `scripts/personalize_demo.py`. The layout contract is
14-UI-SPEC's, not `demo_app.py`'s — that file is `ChatInterface`-shaped and there is no in-repo
precedent for Blocks layout, tuple-yield streaming, post-Reset control disabling, or
`concurrency_id` (14-PATTERNS Structural Gap S3).

`build_demo() -> gr.Blocks`, lazy so importing the module loads no model:
1. Raise `FileNotFoundError(MISSING_SLIM_MSG)` if `SLIM_PATH` is absent and
   `FileNotFoundError(MISSING_ADAPTER_MSG)` if `ADAPTER_PATH` is absent — both before anything else,
   so no window ever opens on a broken demo.
2. `ckpt = load_slim(SLIM_PATH)` (`weights_only=True`);
   `model = GPT(ModelConfig(**ckpt["model_config"]))`; `model.load_state_dict(ckpt["model"])` —
   **LOAD BEFORE INJECT**, with that comment inline; `inject_lora(model, LoRAConfig())`;
   `artifact = load_adapter(ADAPTER_PATH, expected_fingerprint=<the slim trio from ckpt>)` inside
   `warnings.catch_warnings(record=True)` so the 09-CONTEXT D-02 fingerprint-mismatch `UserWarning`
   can be captured rather than swallowed; `load_adapter_weights(model, artifact)`.
   `runtime = RuntimeConfig(device="cpu")` — pin CPU explicitly, no ad-hoc device strings; device
   resolution must never drift to MPS in the demo path. `model.to(runtime.device)`; `model.eval()`.
3. `tok = from_json(TOKENIZER_PATH)` (FROZEN artifact — never retrain);
   `forbid_ids = build_forbid_ids(tok, model.config.vocab_size)` captured once at build time with the
   CR-01 comment.
4. Session state: a closure-local mutable `state = {"enabled": True, "ejected": False}`.

Layout, exactly per 14-UI-SPEC's Layout Contract, inside
`with gr.Blocks(theme=THEME, analytics_enabled=False, title=TITLE) as demo:`
- `gr.Markdown` header: the H1 title line, `DESCRIPTION`, then `HONESTY_LOCK`.
- `gr.Markdown` mismatch banner, `visible=False` by default; made visible with
  `MISMATCH_BANNER_TEMPLATE` filled from the two fingerprint trios when the captured warning fired.
  It renders as a Markdown blockquote with a bold lead — no color, no custom CSS (14-UI-SPEC: adding
  a red banner would put the destructive color on something that is not Reset).
- `gr.Row` → left `gr.Column(scale=3)`: `gr.Chatbot(type="messages", height=480,
  placeholder=EMPTY_CHAT, render_markdown=True)`; a `gr.Row` with
  `gr.Textbox(scale=5, placeholder="Ask me something about myself…", show_label=False,
  submit_btn=False)` and `gr.Button("Ask", variant="primary", scale=1)`;
  `gr.Examples(EXAMPLES, inputs=<textbox>)` with `EXAMPLES_CAPTION`, **no** `cache_examples`
  (caching would run the model at launch) and no submit binding — Examples fill the textbox only.
- Right `gr.Column(scale=2)`: `gr.Markdown` status banner initialised to `STATUS_ON`;
  `gr.Checkbox(label=MEMORY_LABEL, info=MEMORY_INFO, value=True)`;
  `gr.Button(RESET_LABEL, variant="stop")`;
  `gr.Code(label=PANEL_LABEL, language=None, lines=8, interactive=False, wrap_lines=True,
  show_line_numbers=False)` initialised to `render_token_panel(tok, "", source=<the empty-question
  scaffold source line from 14-UI-SPEC>)` so the panel is POPULATED at startup, never blank;
  `gr.Markdown(PANEL_CAPTION)`; `gr.Accordion(ACCORDION_LABEL, open=False)` containing
  `gr.Slider(minimum=RECALL_MAX_NEW_TOKENS, maximum=256, value=RECALL_MAX_NEW_TOKENS, step=8,
  label="Max new tokens", info=SLIDER_INFO)`. A comment on `minimum=` states why it is not zero:
  below the harness's budget an answer could be cut off before the value is uttered, so a working
  memory would look like a failure — D-19's false negative. The demo imports the integer and never
  re-derives it, because re-deriving would require the locked fact values in this process.

Callbacks:
- `on_ask(question, history, max_new_tokens)` — a generator yielding a **tuple**
  `(history, panel_text)` each step, because Gradio replaces the displayed message on every yield and
  the panel is a second output. It must: pick the stamp from `state` at turn start (`STAMP_DELETED`
  when ejected, else ON/OFF); compute
  `panel = render_token_panel(tok, question, source="build_recall_prompt(tok, question) — the same call the scoring harness makes")`
  ONCE at turn start and re-yield it unchanged (14-UI-SPEC: the panel is written once, before the
  first generated token, and not touched during streaming — updating mid-stream would imply, falsely,
  that the context grows during generation); build `prompt_ids = build_recall_prompt(tok, question)`;
  then accumulate deltas from `generate_text_from_ids(model, tok, prompt_ids,
  max_new_tokens=int(max_new_tokens), forbid_ids=forbid_ids, stop_ids={8184, 8185})` and yield
  `STAMP + "\n\n" + accumulated` as the assistant message content, so the bubble grows monotonically
  and never flickers fragments. **History is display-only and is NEVER concatenated into a prompt** —
  every turn is an independent `build_recall_prompt` call. Put that comment in: it is a clean-room
  requirement, not a stylistic choice, and the token panel proves it every turn because the id length
  does not grow with conversation depth.
- `on_toggle(enabled)` — `set_adapter_enabled(model, bool(enabled))`, update `state`, return the
  matching status banner string. Comment: 36 boolean writes, instantaneous by construction — no
  spinner, no loading state, no transition. It does not regenerate, re-annotate, or clear history;
  prior turns keep the stamp of the mode that actually produced them, forever, because
  re-annotating history on toggle would suggest the earlier answer was produced under the current
  setting.
- `on_reset()` — `eject_adapter(model)`, set `state["ejected"] = True`, and return the three updates
  14-UI-SPEC's Interaction Contract specifies: checkbox `gr.update(value=False, interactive=False)`,
  Reset button `gr.update(interactive=False)`, banner → `STATUS_DELETED`. Comment names 14-RESEARCH
  Pitfall 9: after `eject_adapter` the wrappers are gone, so a live toggle would either raise or
  silently do nothing, and silently re-injecting a fresh `B=0` adapter would be indistinguishable
  from OFF and would misrepresent what Reset did. A dead-but-visibly-disabled control plus the
  DELETED banner is the honest rendering. Also state that Reset touches no file on disk —
  `checkpoints/persona_adapter.pt` is unchanged and a restart restores everything, which is why
  there is no confirmation modal, and that the Ask button, textbox, and token panel stay fully live
  so the user can keep chatting with the bare conversational base.

Wire the textbox `submit` and the Ask button `click` to the same callback, clearing the textbox on
submit. Give **every model-touching event** the same `concurrency_id` so they serialize
(14-RESEARCH Pitfall 10: a toggle flip landing mid-generation would produce a half-ON/half-OFF answer
whose stamp and token panel both lie).

`main()` → `build_demo().launch(share=False)` with the localhost/no-tunnel comment,
`__main__`-guarded.
  </action>
  <verify>
    <automated>.venv/bin/python -c "
import importlib.util, sys, inspect
sys.path.insert(0, 'scripts')
s = importlib.util.spec_from_file_location('personalize_demo', 'scripts/personalize_demo.py')
m = importlib.util.module_from_spec(s); s.loader.exec_module(m)
src = inspect.getsource(m.build_demo)
for token in ('theme=THEME','build_forbid_ids','render_token_panel','concurrency_id','set_adapter_enabled','eject_adapter','forbid_ids=','minimum=RECALL_MAX_NEW_TOKENS'):
    assert token in src, token
assert 'cache_examples' not in src, 'cache_examples must NOT be used'
assert src.index('load_state_dict') < src.index('inject_lora'), 'LOAD BEFORE INJECT'
assert 'share=False' in inspect.getsource(m.main)
print('build_demo wiring ok')" && git diff --quiet -- scripts/demo_app.py && echo "demo_app.py untouched"</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --quiet -- scripts/demo_app.py` exits 0
    - `build_demo`'s source contains `theme=THEME`, `build_forbid_ids`, `render_token_panel`, `concurrency_id`, `set_adapter_enabled`, `eject_adapter`, `forbid_ids=`, and `minimum=RECALL_MAX_NEW_TOKENS`
    - `cache_examples` does not appear anywhere in the file
    - `load_state_dict` precedes `inject_lora` in `build_demo`'s source
    - `on_ask` yields a 2-tuple and computes the panel string exactly once, before the generation loop
    - `on_reset` returns three updates: checkbox `value=False, interactive=False`, Reset button `interactive=False`, banner `STATUS_DELETED`
    - `grep -c "merge_lora\|merged_state_dict" scripts/personalize_demo.py` returns 0 — the demo never merges
    - `main()` calls `launch(share=False)`
    - `.venv/bin/ruff check . && .venv/bin/ruff format --check .` exits 0
  </acceptance_criteria>
  <done>The Blocks demo streams tuple yields with a per-turn memory stamp, updates the token panel once per turn, toggles memory with 36 boolean writes, and ejects one-way with the controls honestly disabled.</done>
</task>

<task type="auto">
  <name>Task 3: Create tests/test_phase14_demo.py and add the demo extra to CI</name>
  <files>tests/test_phase14_demo.py, .github/workflows/ci.yml</files>
  <read_first>
    - tests/test_forbid_ids.py (lines 130-215 — the mask assertion patterns and the `skipif(not REAL_SLIM.exists())` real-artifact escape hatch at line 196)
    - tests/test_demo_callback.py (lines 1-10 — the gradio-free house convention this module must justify deviating from)
    - .github/workflows/ci.yml (the install step at line 16)
    - scripts/personalize_demo.py (as written in Tasks 1 and 2)
    - scripts/phase14_recall.py (`render_context_dump`)
  </read_first>
  <action>
Change `.github/workflows/ci.yml`'s install step from `pip install -e ".[cpu,dev]"` to
`pip install -e ".[cpu,dev,demo]"`, keeping the `--extra-index-url` flag unchanged. Add a comment
above it: the `demo` extra is installed so `tests/test_phase14_demo.py` can import
`scripts/personalize_demo.py` and enforce D-17's mask parity, D-18's prompt byte-identity, and
14-UI-SPEC's offline stylesheet lock in CI rather than only locally.

Create `tests/test_phase14_demo.py`.

Module docstring — required content:
- what is pinned (the three structural claims above);
- the **deviation justification**: `tests/test_demo_callback.py:3-6` states the gradio-free,
  scripts-free house convention. This module deviates deliberately, because D-17 and D-18 both
  justify themselves with "structurally enforced rather than by convention," and enforcement that
  cannot import the file it enforces is not enforcement. The `demo` extra is now installed in CI to
  make that possible;
- **the S1 honesty paragraph**: state exactly which assertions run in CI and which run only locally,
  and that the literal D-17 comparison of two real `build_demo()`-captured masks cannot run in CI
  because `checkpoints/` is gitignored. State what the CI half does catch (a missing mask, a wrong
  `vocab_size`, a mask not threaded through — ARCHITECTURE Anti-pattern 7) and what it does not
  (a divergence inside `demo_app.build_demo()`, which is acceptable only because D-17 freezes that
  file). A silently-skipped test is convention wearing a test's clothes, so every `skipif` below
  carries its own reason comment;
- the CPU-only line: no GPU, no generation, no `launch()`.

Loader: insert `str(_REPO_ROOT / "scripts")` on `sys.path`, then `importlib`-load
`personalize_demo` as `pd` and `phase14_recall` as `pr`. Module-scoped `tok()` fixture on
`artifacts/tokenizer.json`.

**CI-enforced tests (no skip markers):**
- `test_forbid_ids_parity` — `torch.equal(pd.build_forbid_ids(tok, 8192), undecodable_ids_mask(tok, 8192))`,
  plus the pinned real-artifact numbers `mask.shape == (1, 8192)`, `mask.dtype == torch.bool`,
  `int(mask.sum()) == 7645`, and `not bool(mask[0, 8184])` (eos is a registered special and must
  NEVER be masked). Docstring names D-17 and ARCHITECTURE Anti-pattern 7.
- `test_forbid_ids_threaded_through` — source assertions on `inspect.getsource(pd.build_demo)`:
  it calls `build_forbid_ids(` and passes `forbid_ids=` into its generation call. Docstring states
  this is the half of D-17 that a tensor comparison structurally cannot see.
- `test_prompt_ids_identical` — D-18's byte-identity check. For a list of at least five questions
  including the empty string, assert the `ids` line of `pd.render_token_panel(tok, q, source=A)` is
  character-identical to the `ids` line of `pr.render_context_dump(tok, q, source=B)`, and that the
  parsed id list equals `build_recall_prompt(tok, q)` element for element. Assert the empty question
  renders `(3) : [8187, 8185, 8186]`. Docstring states D-18: two code paths claiming to show the same
  thing stay true only if something enforces it structurally.
- `test_no_remote_stylesheets` — `gr.Blocks(theme=pd.THEME).stylesheets == []` and
  `"theme=THEME" in inspect.getsource(pd.build_demo)`. Docstring records the measured fact that the
  stock theme carries one Google Fonts URL and that the override empties `_stylesheets`, and states
  that the full `build_demo().stylesheets == []` assertion is the local-only test below.
- `test_no_fact_values_in_ui_chrome` — load `phase14_factset` separately, then assert no locked or
  soft fact value appears (after `normalize_for_match`) in any of `pd.EXAMPLES`, `pd.TITLE`,
  `pd.DESCRIPTION`, `pd.HONESTY_LOCK`, `pd.PANEL_CAPTION`, `pd.MEMORY_INFO`, `pd.SLIDER_INFO`, or the
  module's source text. Docstring states the clean-room posture: the demo process must never hold the
  fact strings, and one careless f-string is the whole failure mode.
- `test_no_recall_numbers_in_ui_copy` — assert none of the UI copy constants contains a `%` sign or a
  decimal fraction. 14-UI-SPEC's claim rules forbid any recall rate in the UI so the copy cannot go
  stale or overclaim; the header points at `results/phase14_recall_report.md` instead.
- `test_budget_import_is_an_integer` — `pd.RECALL_MAX_NEW_TOKENS is pr.RECALL_MAX_NEW_TOKENS` and is
  a positive int; and `inspect.getsource(pd)` contains no call to `derive_recall_budget`. Docstring
  states 14-UI-SPEC: the demo imports the integer and never re-derives it.

**Local-only tests, each with a `skipif` carrying its own reason comment:**
- `test_forbid_ids_parity_real_artifacts` — `skipif` unless `checkpoints/model_slim.pt`,
  `checkpoints/convbase_slim.pt`, and `checkpoints/persona_adapter.pt` all exist. Build both real
  demos, capture each one's `forbid_ids` tensor, and `torch.equal` them. Comment: this is D-17's
  literal form and it cannot run in CI because `checkpoints/` is gitignored.
- `test_build_demo_stylesheets_real` — same `skipif`; `pd.build_demo().stylesheets == []`. Comment:
  14-UI-SPEC's literal contract test, local-only for the same reason.
  </action>
  <verify>
    <automated>.venv/bin/pytest -q tests/test_phase14_demo.py -x -rs</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest -q tests/test_phase14_demo.py -x` passes with at least 9 tests collected
    - Exactly 2 tests carry `skipif` markers, and each has a comment on the line above naming why it is local-only
    - `grep -c "cpu,dev,demo" .github/workflows/ci.yml` returns 1
    - `test_prompt_ids_identical` covers at least 5 questions including the empty string
    - `test_forbid_ids_parity` asserts all four of shape, dtype, `7645`, and eos-not-masked
    - `test_no_fact_values_in_ui_chrome` fails if a locked fact value is pasted into `EXAMPLES` (verify by temporarily doing so, confirming red, then reverting)
    - The module docstring contains `runs in CI`, `local-only`, and `checkpoints/ is gitignored`
    - `.venv/bin/pytest -q` (full suite) passes
    - `.venv/bin/ruff check . && .venv/bin/ruff format --check .` exits 0
  </acceptance_criteria>
  <done>D-17's mask construction, D-18's prompt byte-identity, the offline stylesheet lock, and the no-fact-values-in-the-demo posture are all enforced in CI, with the two genuinely checkpoint-dependent assertions marked local-only and labelled as such.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Gradio server | localhost only, `share=False`, single local user |
| disk → demo process | `convbase_slim.pt` and `persona_adapter.pt` are deserialized at build time |
| served page → third-party origins | The page must instruct the browser to fetch nothing remote |
| demo process → locked fact values | The values must never enter this process |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-22 | Elevation of Privilege | checkpoint deserialization | mitigate | `load_slim` / `load_adapter` under `weights_only=True` — restricted unpickler, zero code execution (T-08-01). `torch.load` is never called directly |
| T-14-27 | Information Disclosure | network egress from the demo | mitigate | `GRADIO_ANALYTICS_ENABLED=False` set BEFORE `import gradio` (kills telemetry and the version-check ping), `analytics_enabled=False`, `share=False` (no tunnel binary download), and the theme font override pinned by `gr.Blocks(theme=THEME).stylesheets == []` in CI |
| T-14-17 | Information Disclosure | fact values leaking into the demo process | mitigate | The demo imports an integer, not the facts; `grep -c phase14_factset` on the source returns 0; `test_no_fact_values_in_ui_chrome` scans every copy constant and the module source in CI |
| T-14-28 | Spoofing | a "memory off" that is still on | mitigate | `set_adapter_enabled` refuses on any merged module before flipping a flag (09-REVIEW CR-01); the demo never calls `merge()`; plan 14-10's D-11.3 control measures adapter-off bit-identity on real weights |
| T-14-29 | Spoofing | the token panel showing something other than what the model received | mitigate | The panel renders through `phase14_recall.render_context_dump`, the same call the harness makes; `test_prompt_ids_identical` asserts character-identical `ids` lines in CI (D-18) |
| T-14-23 | Tampering | adapter loaded onto the wrong base | mitigate | `load_adapter(expected_fingerprint=...)`; the warn-not-error `UserWarning` is captured and surfaced as a persistent in-UI banner, because a terminal warning nobody reads is how a false demo gets recorded |
| T-14-06 | Denial of Service | unbounded generation from the UI | mitigate | Slider maximum 256, minimum `RECALL_MAX_NEW_TOKENS`; the `(0, 4096]` guard in `generate_text_from_ids` fires before the loop; `forbid_ids` masks all 7,645 dead ids |
| T-14-30 | Tampering | concurrent events mutating the shared model | mitigate | One `concurrency_id` across every model-touching event so they serialize; the single-user local assumption is stated in the module docstring |
| T-14-SC | Tampering | package installs | accept | The CI change installs `gradio>=5,<6`, already pinned in `pyproject.toml` and registry-audited in 08-RESEARCH (PyPI, wheel source inspected 2026-06-10). 14-RESEARCH's Package Legitimacy Audit records zero NEW packages for this phase |
</threat_model>

<verification>
- `.venv/bin/pytest -q tests/test_phase14_demo.py -x -rs` — green, with exactly two skips locally reported by name when checkpoints are absent
- `.venv/bin/pytest -q` — full suite green
- `.venv/bin/ruff check . && .venv/bin/ruff format --check .` — clean
- `git diff --quiet -- scripts/demo_app.py` — exits 0
- On-camera verification of the running demo is plan 14-11's checkpoint
</verification>

<success_criteria>
- `scripts/demo_app.py` is unchanged, verified by `git diff --quiet`
- `gr.Blocks(theme=THEME).stylesheets == []` and the demo makes zero outbound calls
- The token panel and the harness dump produce character-identical `ids` lines for the same question, asserted in CI
- The demo's `forbid_ids` tensor equals `undecodable_ids_mask(tok, 8192)` with `sum() == 7645`, asserted in CI
- No locked fact value appears anywhere in the demo source or UI copy, asserted in CI
- The slider's minimum and default are the imported `RECALL_MAX_NEW_TOKENS`, never re-derived
- `.github/workflows/ci.yml` installs the `demo` extra so all of the above actually runs in CI
</success_criteria>

<output>
Create `.planning/phases/14-teach-then-recall-demo/14-08-SUMMARY.md` when done
</output>
