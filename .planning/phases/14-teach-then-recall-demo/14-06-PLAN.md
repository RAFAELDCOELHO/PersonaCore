---
phase: 14-teach-then-recall-demo
plan: 06
type: execute
wave: 5
depends_on: [14-04, 14-05]
files_modified:
  - scripts/phase14_recall.py
autonomous: true
requirements: [DEMO-05, DEMO-06]

must_haves:
  truths:
    - "Every recall question is answered in a fresh process from a prompt that contains the question and nothing else"
    - "Every question's exact prompt token ids are written into committed evidence before the model is called"
    - "Any locked fact value appearing in any prompt aborts the run with SystemExit"
    - "Taught phrasings and never-seen phrasings are scored into separately aggregated tiers"
    - "Every completion is committed, failures included — nothing is filtered on its way to the transcript"
  artifacts:
    - path: "scripts/phase14_recall.py"
      provides: "run_scored_recall + write_transcripts + the fresh-process provenance echo"
      contains: "def run_scored_recall"
  key_links:
    - from: "scripts/phase14_recall.py"
      to: "personacore.checkpoint.load_slim"
      via: "weights_only=True choke point for the shareable base artifact"
      pattern: "load_slim\\("
    - from: "scripts/phase14_recall.py"
      to: "personacore.lora.load_adapter_weights"
      via: "adapter loaded through load_adapter with the base fingerprint trio"
      pattern: "load_adapter_weights\\("
    - from: "scripts/phase14_recall.py"
      to: "results/phase14_transcripts.md"
      via: "every completion written verbatim behind the REPRESENTATIVE-not-cherry-picked opener"
      pattern: "TRANSCRIPTS_PATH.write_text"
---

<objective>
Build the scored recall harness on top of plan 14-05's pure rules: model load, the fresh-process
provenance echo, per-question context dumps with the clean-room `SystemExit` proof, the greedy-plus-N
scoring loop over taught and held-out tiers, the base-without-adapter closed-book control, and
`results/phase14_transcripts.md`.

Purpose: SC2 requires fresh-process empty-prompt recall with a context-token dump proving no prompt
leakage, and SC3 requires taught and never-seen phrasings scored separately with all transcripts
committed — failures included. PITFALLS-11's clean room is satisfied by construction here because
teaching happened in a different `python` invocation; this harness records the evidence that makes
that auditable.

Output: the harness half of `scripts/phase14_recall.py`. The three D-11 controls and the recall
report are plan 14-10's.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/14-teach-then-recall-demo/14-CONTEXT.md
@.planning/phases/14-teach-then-recall-demo/14-RESEARCH.md
@.planning/phases/14-teach-then-recall-demo/14-PATTERNS.md
@scripts/make_transcripts.py
@scripts/make_retention_samples.py

<interfaces>
Already in `scripts/phase14_recall.py` (plan 14-05):
- `RECALL_MAX_NEW_TOKENS`, `SEED`, `N_SEEDED_SAMPLES`, `STOP_IDS`, `TAUGHT_THRESHOLD`,
  `HELDOUT_THRESHOLD`, `BUDGET_STEP`, `PREAMBLE_HEADROOM`, `TAIL_HEADROOM`
- `derive_recall_budget`, `question_seed`, `assert_values_fit`, `_prove`
- `normalize`, `contains_value`, `score_question`, `find_contradictions`, `has_hedging`
- `render_context_dump(tok, question, *, source)`, `assert_no_value_in_prompt(tok, question, values)`
- path constants `CONVBASE_SLIM`, `ADAPTER_PATH`, `TOKENIZER_PATH`, `TRANSCRIPTS_PATH`,
  `RECALL_REPORT_PATH`

From `scripts/phase14_factset.py` (plan 14-04 — **imported LAZILY inside `main()`**, per
`14-05-PLAN.md` `<import_topology>` rule 3):
- `LOCKED_FACTS`, `SOFT_TIER_FACTS`, `LOCKED_VALUES`, `GATE_REJECTED_CANDIDATES`,
  `TAUGHT_FAMILY_IDS`, `HELDOUT_FAMILY_IDS`, `render_family`, `heldout_questions`,
  `RESERVED_HELDOUT_PROBES`, `FACTSET_GATE_SHA`

From `personacore.checkpoint`: `load_slim(path, *, map_location="cpu") -> dict`,
`load_adapter(path, *, expected_fingerprint=None, map_location="cpu") -> dict`
From `personacore.lora`: `inject_lora(model, cfg) -> int`, `load_adapter_weights(model, artifact)`,
`set_adapter_enabled(model, enabled)`, `adapter_disabled(model)`, `LoRAConfig`
From `personacore.generation`: `collect`, `undecodable_ids_mask`
From `personacore.dialogue`: `build_recall_prompt`
From `personacore.preflight`: `preflight_device(strict=True)`
From `personacore.seeding`: `seed_everything`
From `personacore.provenance`: `git_sha`

Verbatim idioms to copy (14-PATTERNS.md quotes each with line numbers):
- `scripts/make_transcripts.py:67-81` — the `_complete(model, prompt_ids, device, forbid, **kw)`
  helper; `len(gen) < MAX_NEW_TOKENS` is how stop-fraction is measured without a second signal
- `scripts/make_transcripts.py:99-115` — the setup/device/forbid_ids preamble
- `scripts/make_transcripts.py:146-183` — build a `blocks` list, prepend a `header` list carrying the
  measured proxies, `"\n".join(...)`, one `write_text`; the "REPRESENTATIVE, not cherry-picked"
  opening blockquote is the register SC3 needs
- `scripts/make_retention_samples.py:8-14` — an explicit `torch.Generator` seeded per question, so an
  early stop in one question cannot shift a later question's stream
- `scripts/finetune_ab.py:322-329` — the run-level provenance echo

Measured base fingerprint (14-RESEARCH F6): `convbase_best.pt` and `convbase_slim.pt` both carry
`git_sha=04e724c67033f9a2ed8b705a07ad025c867a18c5`, `step=4000`,
`val_loss=1.5235939979553224`, so an adapter fingerprinted against the training checkpoint loads
against the slim one with no `UserWarning`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Model load, fresh-process provenance, and the per-question completion helper</name>
  <files>scripts/phase14_recall.py</files>
  <read_first>
    - scripts/make_transcripts.py (lines 60-120 — `_complete` and the setup/device/forbid_ids preamble)
    - scripts/finetune_ab.py (lines 300-360 — the provenance echo)
    - scripts/phase14_recall.py (current state from plan 14-05)
    - src/personacore/checkpoint.py (lines 180-260 — `load_slim` and `load_adapter` signatures and fingerprint semantics)
    - src/personacore/lora/inject.py (lines 29-135 — `inject_lora`, `mark_only_lora_trainable`, `load_adapter_weights`, `set_adapter_enabled`, `adapter_disabled`)
    - .planning/phases/14-teach-then-recall-demo/14-RESEARCH.md (§Architecture Pattern 8, §Pattern 4 load ordering)
  </read_first>
  <action>
Add the model-loading and completion machinery to `scripts/phase14_recall.py`.

`def load_adapted_model(device)` — returns `(model, model_cfg, tok, forbid, adapter_artifact)`:
1. `ckpt = load_slim(CONVBASE_SLIM)` — `weights_only=True`, zero code execution on load. Raise
   `SystemExit` with a message naming `checkpoints/convbase_slim.pt` and
   `python scripts/export_slim.py` if the file is absent.
2. `model_cfg = ModelConfig(**ckpt["model_config"])`; `model = GPT(model_cfg)`;
   `model.load_state_dict(ckpt["model"])` — **LOAD BEFORE INJECT**, the load-bearing ordering
   (ARCHITECTURE Anti-pattern 1: injecting first breaks every checkpoint key). Put that comment inline.
3. `n_wrapped = inject_lora(model, LoRAConfig())`; `_prove(n_wrapped == 6 * model_cfg.n_layer, ...)`
   naming both numbers.
4. `artifact = load_adapter(ADAPTER_PATH, expected_fingerprint=<the slim trio read from ckpt>)`;
   then `load_adapter_weights(model, artifact)`. Raise `SystemExit` naming
   `checkpoints/persona_adapter.pt` and `python scripts/teach_persona.py` if the adapter is absent.
   Capture whether `load_adapter` emitted the D-02 fingerprint-mismatch `UserWarning` (use
   `warnings.catch_warnings(record=True)`) and return it — plan 14-10's report must state it, and
   09-CONTEXT D-02 is warn-not-error, so the run continues.
5. `model.to(device)`, `model.eval()`.
6. `tok = from_json(TOKENIZER_PATH)` with the FROZEN-artifact comment;
   `forbid = undecodable_ids_mask(tok, model_cfg.vocab_size).to(device)` with the CR-01 /
   Anti-pattern-7 comment (`.to(device)` because `next_token` masked-fills logits in place on the
   model device).

`def _complete(model, prompt_ids, device, forbid, **kw)` — copied verbatim in shape from
`make_transcripts.py:67-81`, returning `(generated_ids, stopped_on_stop_id)` where the stop signal is
`len(gen) < RECALL_MAX_NEW_TOKENS`. Keep the comment explaining that `generate()` stops WITHOUT
yielding the stop id, so fewer than `max_new_tokens` generated tokens means a stop-id termination.
Use `max_new_tokens=RECALL_MAX_NEW_TOKENS`, `forbid_ids=forbid`, `stop_ids=set(STOP_IDS)`.

`def complete_question(model, tok, question, device, forbid, *, index)` — one question, all samples:
- build `prompt_ids = build_recall_prompt(tok, question)`;
- one greedy completion (`greedy=True`);
- `N_SEEDED_SAMPLES` sampled completions, each driven by its OWN
  `torch.Generator(device="cpu").manual_seed(question_seed(index) + s)`. Comment cites
  `make_retention_samples.py:8-14`: per-question generator seeding is what makes the run
  re-derivable and stops an early stop in one question from shifting a later question's stream.
- return a dict carrying the question, `prompt_ids`, the decoded completions in order, and the
  per-completion stop flags.

`def echo_provenance(runtime_summary, device, adapter_artifact)` — the run-level provenance block in
the `finetune_ab.py:322-329` register, printed and also returned as a list of lines so plan 14-10's
report can embed it. Must include: `SEED`, `git_sha()`, `os.getpid()`, wall-clock timestamp, the
`preflight_device` summary, `RECALL_MAX_NEW_TOKENS`, `N_SEEDED_SAMPLES`, the base fingerprint trio,
`FACTSET_GATE_SHA`, and the SHA-256 digests of `convbase_slim.pt` and the adapter file. State in a
comment that the PID plus timestamp plus the adapter file's on-disk existence between runs is what
makes the PROCESS BOUNDARY auditable (PITFALLS-11 step 1) — teaching happened in a different `python`
invocation, so the clean room is inherited rather than re-argued. Also state, per 14-RESEARCH
Pattern 8, that the harness deliberately does NOT spawn a subprocess per question: one fresh process
for the whole scored run with each question an independent prompt, never concatenated with any prior
turn, fully satisfies PITFALLS-11 and costs nothing.
  </action>
  <verify>
    <automated>.venv/bin/python -c "
import importlib.util, sys, inspect
sys.path.insert(0, 'scripts')
s = importlib.util.spec_from_file_location('phase14_recall', 'scripts/phase14_recall.py')
m = importlib.util.module_from_spec(s); s.loader.exec_module(m)
for fn in ('load_adapted_model','_complete','complete_question','echo_provenance'):
    assert callable(getattr(m, fn)), fn
src = inspect.getsource(m.load_adapted_model)
assert src.index('load_state_dict') < src.index('inject_lora'), 'LOAD BEFORE INJECT'
assert 'phase14_factset' not in sys.modules
print('harness load half ok')"</automated>
  </verify>
  <acceptance_criteria>
    - In `load_adapted_model`'s source, the `load_state_dict` call appears before the `inject_lora` call
    - `grep -c "torch.load(" scripts/phase14_recall.py` returns 0 in the model-loading path — the base and adapter go through `load_slim` / `load_adapter` only
    - `_prove(n_wrapped == 6 * n_layer, ...)` is present and its message names both the measured and the expected count
    - A missing `checkpoints/convbase_slim.pt` produces a `SystemExit` whose message names `scripts/export_slim.py`; a missing adapter produces one naming `scripts/teach_persona.py`
    - `complete_question` creates a distinct `torch.Generator` per sample, seeded from `question_seed(index)`
    - `echo_provenance`'s output contains all of: seed, git sha, pid, wall clock, `RECALL_MAX_NEW_TOKENS`, the base fingerprint trio, `FACTSET_GATE_SHA`, and two SHA-256 digests
    - `grep -c "assert " scripts/phase14_recall.py` still returns 0
    - `.venv/bin/ruff check scripts/ && .venv/bin/ruff format --check scripts/` exits 0
  </acceptance_criteria>
  <done>The harness loads the slim base and the adapter through the `weights_only=True` choke points in the load-before-inject order, completes one question with greedy plus N per-question-seeded samples, and echoes a process-boundary provenance block.</done>
</task>

<task type="auto">
  <name>Task 2: The scored recall loop with context dumps and the clean-room proof</name>
  <files>scripts/phase14_recall.py</files>
  <read_first>
    - scripts/phase14_recall.py (current state after Task 1)
    - scripts/phase14_factset.py (`render_family`, `heldout_questions`, `RESERVED_HELDOUT_PROBES`, `TAUGHT_FAMILY_IDS`, `HELDOUT_FAMILY_IDS`)
    - .planning/phases/14-teach-then-recall-demo/14-CONTEXT.md (D-08, D-10, D-13, D-18, D-19)
    - .planning/phases/14-teach-then-recall-demo/14-RESEARCH.md (§Architecture Pattern 8, §Code Examples "Scored recall on one question")
  </read_first>
  <action>
Add `def run_scored_recall(model, tok, device, forbid, facts, *, tier_label)` to
`scripts/phase14_recall.py`. It is the SC2/SC3 core.

Question-set construction, done once at the top of `main()` and passed in:
- **taught questions** — for each fact in `LOCKED_FACTS`, every question `render_family` produces for
  every id in `TAUGHT_FAMILY_IDS`;
- **held-out questions** — `heldout_questions()`, which is every held-out-family question unioned
  with `RESERVED_HELDOUT_PROBES` (D-08's proven-unguessable seeds);
- **soft-tier questions** — the same two splits computed over `SOFT_TIER_FACTS`, scored and reported
  in their own labelled tier and EXCLUDED from the pre-registered gate (D-05).
`_prove` that the taught and held-out question sets are disjoint after `normalize` on both sides.

Before any generation, run the D-19 fit guard: import `phase14_factset` lazily, then
`assert_values_fit(tok, [f.value for f in LOCKED_FACTS + SOFT_TIER_FACTS])`.

Per question, in order:
1. `prompt_ids = build_recall_prompt(tok, question)`.
2. Record `render_context_dump(tok, question, source="build_recall_prompt(tok, question) — the same call the demo panel makes")`
   into the run's evidence list. This happens BEFORE the model is called, so the committed dump is
   what the model actually received, not a reconstruction.
3. `assert_no_value_in_prompt(tok, question, all_values)` — the clean-room `SystemExit`. Comment
   states this is the mechanical form of the demo-killing failure mode in PITFALLS-11: if any locked
   value is in any prompt, the entire claim is falsified and the run must abort, not warn.
4. `complete_question(...)` for greedy plus `N_SEEDED_SAMPLES`.
5. Score with `score_question(completions, fact.value)` → `(k, n)`.
6. `find_contradictions(completion, fact.value, lexicon)` per completion, where
   `lexicon = set(LOCKED_VALUES) | {f.value for f in GATE_REJECTED_CANDIDATES}`; plus
   `has_hedging`. Both are recorded as DESCRIPTIVE counts with no gate attached.
7. Record the stop flag per completion so the run reports a stop-fraction proxy in the
   `make_transcripts.py` register.

Return a result record per tier: per-question `k/n`, the aggregate rate, contradiction count,
hedging count, stop fraction, and the full list of per-question dicts (question, prompt ids, dump
lines, every completion verbatim, per-completion hit flags).

Then add `def run_closed_book_control(model, tok, device, forbid, questions, values)` — SC2's
required base-without-adapter control. Use the `adapter_disabled(model)` context manager so the same
process, the same weights, and the same prompts are used with only the 36 boolean flags flipped.
Return the same shaped record. Comment states D-08: gate-time probes are evidence carried forward,
never a substitute for re-measuring at scoring time, so this control re-runs on the FULL final
question set.

Finally add `def main()` wiring: `preflight_device(strict=True)`, `RuntimeConfig()`,
`seed_everything(SEED)`, `load_adapted_model`, build the question sets, run the adapter-on tiers, run
the closed-book control, print the provenance echo, and call the transcripts writer from Task 3.
Guard with `if __name__ == "__main__":`. Leave a marked section comment where plan 14-10's three
D-11 controls and the recall-report writer will be inserted.
  </action>
  <verify>
    <automated>.venv/bin/python -c "
import importlib.util, sys, inspect
sys.path.insert(0, 'scripts')
s = importlib.util.spec_from_file_location('phase14_recall', 'scripts/phase14_recall.py')
m = importlib.util.module_from_spec(s); s.loader.exec_module(m)
for fn in ('run_scored_recall','run_closed_book_control','main'):
    assert callable(getattr(m, fn)), fn
src = inspect.getsource(m.run_scored_recall)
assert 'render_context_dump' in src and 'assert_no_value_in_prompt' in src
assert src.index('render_context_dump') < src.index('complete_question'), 'dump before generation'
assert 'adapter_disabled' in inspect.getsource(m.run_closed_book_control)
assert 'phase14_factset' not in sys.modules
print('scored loop ok')"</automated>
  </verify>
  <acceptance_criteria>
    - In `run_scored_recall`'s source, `render_context_dump` and `assert_no_value_in_prompt` both appear before the `complete_question` call
    - `run_closed_book_control` uses `adapter_disabled(model)` — the same process, same weights, same prompts
    - The taught and held-out question sets are `_prove`-checked disjoint after normalization
    - `assert_values_fit` is called before any generation
    - The contradiction lexicon is `LOCKED_VALUES | GATE_REJECTED_CANDIDATES` values, built inside the function, never at module level
    - Soft-tier questions produce their own labelled result record, separate from the core tiers
    - `main()` is `__main__`-guarded; importing the module via `importlib` runs nothing
    - `.venv/bin/ruff check scripts/ && .venv/bin/ruff format --check scripts/` exits 0
  </acceptance_criteria>
  <done>The scored loop dumps each prompt's ids before calling the model, aborts on any value found in a prompt, scores taught / held-out / soft tiers separately, and re-runs the closed-book control on the full final question set.</done>
</task>

<task type="auto">
  <name>Task 3: Write results/phase14_transcripts.md — every completion, failures included</name>
  <files>scripts/phase14_recall.py</files>
  <read_first>
    - scripts/make_transcripts.py (lines 146-183 — the header/blocks/join/write_text shape and the "REPRESENTATIVE, not cherry-picked" opening blockquote)
    - results/transcripts.md (the shipped format analog)
    - scripts/phase14_recall.py (current state after Task 2)
    - .planning/phases/14-teach-then-recall-demo/14-CONTEXT.md (D-05 soft-tier labelling, D-10, D-12, D-18)
  </read_first>
  <action>
Add `def write_transcripts(records, provenance_lines)` writing `results/phase14_transcripts.md` in
the `make_transcripts.py:146-183` shape: build a `blocks` list of markdown lines, prepend a `header`
list carrying the measured proxies, `"\n".join(header + blocks)`, one `write_text`. Pre-compose long
table rows into variables so source lines stay under ruff's 100-char limit.

Header requirements:
- Title `# PersonaCore — Phase 14 Teach-Then-Recall Transcripts (DEMO-05 / DEMO-06)`.
- The Shared-Pattern-6 opener blockquote, in the "REPRESENTATIVE, not cherry-picked" register but
  stronger: **every completion produced by the run appears below, failures included and unfiltered.**
  State that each prompt is a `build_recall_prompt` id sequence — never a hand-formatted string — so
  prompts tokenize identically to the training bins (Pitfall 4), and that the exact prompt token ids
  appear with every question (D-18, SC2's literal requirement).
- `## Run Provenance` — the `provenance_lines` from `echo_provenance`, verbatim. State that the PID
  and timestamp plus the adapter file's on-disk existence between the teaching run and this run are
  the auditable form of PITFALLS-11 step 1's process boundary.
- `## Measured Proxies` — stop-id termination fraction (`n_stopped/n_completions`), the greedy vs
  seeded split, `RECALL_MAX_NEW_TOKENS`, and `N_SEEDED_SAMPLES`.

Body, one section per tier, in this order — **core taught**, **core held-out**, **closed-book control
(adapter off)**, **soft tier (excluded from the pre-registered gate)**. Each section:
- opens with a one-line statement of what that tier measures and, for the soft tier, D-05's named
  exclusion: what it is FOR (narrative texture, breadth of personalization) and what it explicitly
  does NOT contribute (no bearing on DEMO-06's taught or held-out thresholds, precisely because
  low-cardinality slots could not reliably survive the D-03 close-call filter). This is a named
  section, not a footnote;
- then, per question: the question string, the three-line context dump verbatim from
  `render_context_dump`, and every completion in order with its hit flag, its stop flag, and any
  contradiction values found. Held-out questions that came from `RESERVED_HELDOUT_PROBES` are marked
  with their D-08 provenance: held out AND measured base-failing at gate time, commit
  `FACTSET_GATE_SHA`, with a pointer to the quoted base completion in
  `results/phase14_factset_report.md`.

Do not aggregate or rank anything here — `results/phase14_recall_report.md` (plan 14-10) owns the
verdict register. This file is raw evidence.
  </action>
  <verify>
    <automated>.venv/bin/python -c "
import importlib.util, sys, inspect
sys.path.insert(0, 'scripts')
s = importlib.util.spec_from_file_location('phase14_recall', 'scripts/phase14_recall.py')
m = importlib.util.module_from_spec(s); s.loader.exec_module(m)
src = inspect.getsource(m.write_transcripts)
for token in ('failures included', 'Run Provenance', 'Measured Proxies', 'build_recall_prompt', 'FACTSET_GATE_SHA'):
    assert token in src, token
assert src.count('write_text') == 1, 'one write_text'
print('transcripts writer ok')" && .venv/bin/ruff check scripts/ && .venv/bin/ruff format --check scripts/</automated>
  </verify>
  <acceptance_criteria>
    - `write_transcripts` performs exactly one `write_text` call
    - Its source contains the strings `failures included`, `REPRESENTATIVE`, `Run Provenance`, `Measured Proxies`, and `FACTSET_GATE_SHA`
    - The four tier sections are emitted in the order core taught, core held-out, closed-book control, soft tier
    - The soft-tier section's opening text contains both what the tier is for and the phrase `no bearing` on the DEMO-06 thresholds
    - Every question block includes the three-line `render_context_dump` output verbatim
    - No filtering, sorting by score, or truncation is applied to the completion lists
    - `.venv/bin/pytest -q` (full suite) passes — plan 14-05's importlib tests still load the driver cleanly
    - `.venv/bin/ruff check . && .venv/bin/ruff format --check .` exits 0
  </acceptance_criteria>
  <done>`write_transcripts` emits every completion with its exact prompt token ids and its D-08 provenance where applicable, in four labelled tiers, with no filtering.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk → harness | `convbase_slim.pt` and `persona_adapter.pt` are deserialized; both are shareable artifacts |
| locked values → prompts | The clean-room claim rests on no value ever crossing into a prompt |
| harness → `results/` | The transcripts are public committed evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-22 | Elevation of Privilege | shareable artifact deserialization | mitigate | `load_slim` and `load_adapter` are the `weights_only=True` choke points — restricted unpickler, zero code execution. `torch.load` is never called directly in the harness's load path |
| T-14-23 | Tampering | adapter loaded onto the wrong base | mitigate | `load_adapter(expected_fingerprint=<slim trio>)` checks the trio; `load_adapter_weights` audits keys and shapes before mutating a single tensor (09-REVIEW CR-02). The mismatch warning is captured and carried into the report rather than swallowed |
| T-14-18 | Spoofing (of the novel claim) | a fact value reaching a prompt | mitigate | `assert_no_value_in_prompt` runs per question BEFORE generation and raises `SystemExit`; the exact prompt ids are committed alongside every completion so a reader can check rather than trust |
| T-14-24 | Repudiation | cherry-picked transcripts | mitigate | Every completion is written unfiltered behind an explicit "failures included" opener; the greedy and seeded splits and the per-question seed derivation are recorded so the run is re-derivable |
| T-14-08 | Denial of Service | generation loop | mitigate | `RECALL_MAX_NEW_TOKENS` bounded before the loop; `forbid_ids` masks all 7,645 dead ids; `stop_ids={8184, 8185}` terminates turns |
| T-14-SC | Tampering | package installs | accept | Zero new packages |
</threat_model>

<verification>
- `.venv/bin/python -c "<importlib-load scripts/phase14_recall.py>"` — the module loads without running `main()` and without importing `phase14_factset`
- `.venv/bin/pytest -q` — full suite green (plan 14-05's driver tests still pass against the grown module)
- `.venv/bin/ruff check . && .venv/bin/ruff format --check .` — clean
- The end-to-end run itself is plan 14-11's task; this plan ships the code that run executes
</verification>

<success_criteria>
- The base and adapter load through `load_slim` / `load_adapter` in the load-before-inject order, with the fingerprint trio checked
- Every question's exact prompt token ids are recorded before the model is called
- Any locked value found in any prompt aborts the run with `SystemExit`
- Taught, held-out, closed-book, and soft tiers are scored and recorded separately
- `results/phase14_transcripts.md` carries every completion, failures included, with per-question context dumps and D-08 provenance on the reserved probes
</success_criteria>

<output>
Create `.planning/phases/14-teach-then-recall-demo/14-06-SUMMARY.md` when done
</output>
