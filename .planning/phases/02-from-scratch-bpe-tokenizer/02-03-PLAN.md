---
phase: 02-from-scratch-bpe-tokenizer
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - src/personacore/tokenizer/io.py
  - src/personacore/tokenizer/__init__.py
  - scripts/train_tokenizer.py
  - artifacts/tokenizer.json
autonomous: true
requirements: [TOK-04, TOK-05]
user_setup: []

must_haves:
  truths:
    - "The tokenizer freezes to a single self-contained JSON artifact and loads back to behaviorally-identical encode/decode"
    - "The frozen Tokenizer exposes a locked vocab_size == 8192 ready to size the model (TOK-04)"
    - "A committed production 8192 tokenizer.json artifact exists, trained on a bounded TinyStories sample (or committed fixture), reused FROZEN/unchanged by Phase 5 with no retrain (D-09)"
    - "The from-scratch encoder reproduces tiktoken gpt2 ids exactly on the oracle set (TOK-05, D-07 gpt2 algorithm-equivalence); tiktoken is test-only [dev] and never imported at runtime (D-07)"
  artifacts:
    - path: "src/personacore/tokenizer/io.py"
      provides: "save_json / from_json / SCHEMA_VERSION — JSON freeze/load (data-only, no pickle) (TOK-04)"
      contains: "SCHEMA_VERSION"
    - path: "scripts/train_tokenizer.py"
      provides: "Thin no-CLI entry: load bounded corpus -> train 8192 -> freeze frozen artifact reused by Phase 5 (D-09)"
      contains: "def main"
    - path: "artifacts/tokenizer.json"
      provides: "The FROZEN production 8192-vocab artifact (committed; Phase 5 reuses unchanged, no retrain — D-09)"
      contains: "schema_version"
  key_links:
    - from: "src/personacore/tokenizer/io.py"
      to: "src/personacore/tokenizer/bpe.py"
      via: "from_json -> BPETokenizer.frozen(...)"
      pattern: "frozen\\("
    - from: "scripts/train_tokenizer.py"
      to: "src/personacore/tokenizer/bpe.py + io.py"
      via: "BPETokenizer().train(...) then save_json(...)"
      pattern: "train\\(|save_json\\("
    - from: "tests/test_tokenizer_oracle.py"
      to: "personacore.tokenizer (from-scratch encoder)"
      via: "recover_merges from tiktoken -> inject -> assert exact id match"
      pattern: "recover_merges|encode_ordinary"
---

<objective>
Complete the tokenizer: freeze/load to a self-contained JSON artifact (TOK-04), train and commit the production 8192-vocab `tokenizer.json` that Phase 5 reuses FROZEN/unchanged with no retrain (D-09), and turn the TOK-05 tiktoken-equivalence oracle green (D-07). This is the final vertical slice: trained tokenizer → frozen artifact → reload → identical behavior, plus the algorithm-equivalence proof.

Purpose: TOK-04's locked, serializable artifact is what makes `vocab_size` immovable across the milestone (a later tokenizer change can never invalidate a trained checkpoint); the committed artifact is frozen and reused by Phase 5 (D-09). TOK-05 proves the from-scratch encode algorithm is correct against the authoritative tiktoken `gpt2` oracle (D-07) without ever depending on it at runtime.
Output: `src/personacore/tokenizer/io.py`, an extended `__init__.py`, `scripts/train_tokenizer.py`, and the committed `artifacts/tokenizer.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-from-scratch-bpe-tokenizer/02-CONTEXT.md
@.planning/phases/02-from-scratch-bpe-tokenizer/02-RESEARCH.md
@.planning/phases/02-from-scratch-bpe-tokenizer/02-PATTERNS.md
@.planning/phases/02-from-scratch-bpe-tokenizer/02-VALIDATION.md

<interfaces>
<!-- Implement to these exact names; the Plan 01 tests assert against them. -->

src/personacore/tokenizer/io.py:
  SCHEMA_VERSION: int = 1
  def save_json(tok, path) -> None   # writes {schema_version, pattern, vocab_size, special_tokens, eos_id, merges[[p0,p1,idx]...rank-ordered]}
  def from_json(path) -> BPETokenizer   # asserts schema_version, rebuilds via BPETokenizer.frozen(...)

src/personacore/tokenizer/bpe.py (from Plan 02 — consume, do not modify):
  BPETokenizer.train(text, vocab_size=8192)
  BPETokenizer.frozen(*, pattern, merges, special_tokens, eos_id, vocab_size)
  .merges  .vocab_size  .eos_id  .special_tokens  .pattern  .encode  .decode

tests/test_tokenizer_oracle.py (from Plan 01 — must go green/skip):
  test_tiktoken_gpt2_equivalence   # skip-on-offline; recover_merges -> inject -> exact id match
  test_no_runtime_tiktoken         # scans src/personacore/**/*.py for "tiktoken"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: JSON freeze/load (io.py) + extend package surface</name>
  <files>src/personacore/tokenizer/io.py, src/personacore/tokenizer/__init__.py</files>
  <read_first>
    - .planning/phases/02-from-scratch-bpe-tokenizer/02-RESEARCH.md (§"JSON freeze artifact (schema v1)"; §"Security Domain" — JSON data-only, validate ids in [0, vocab_size), never pickle/torch.load)
    - .planning/phases/02-from-scratch-bpe-tokenizer/02-PATTERNS.md (io.py analog: checkpoint.py schema-version constant + symmetric save/load; the critical divergence — stdlib json NOT torch.save)
    - src/personacore/checkpoint.py (CKPT_SCHEMA_VERSION + asserted-on-load + the security note to mirror)
    - src/personacore/tokenizer/bpe.py (BPETokenizer.frozen signature + .merges/.special_tokens/.eos_id/.pattern/.vocab_size)
    - src/personacore/tokenizer/__init__.py (current exports to extend)
  </read_first>
  <action>
    Create `src/personacore/tokenizer/io.py` with the Phase-1-style docstring citing TOK-04 and the security divergence (stdlib `json`, data-only — NEVER pickle/torch.load). Define `SCHEMA_VERSION = 1` (parallels `CKPT_SCHEMA_VERSION`). `save_json(tok, path)`: write a dict with `schema_version`, `pattern` (tok.pattern), `vocab_size` (tok.vocab_size = 8192), `special_tokens` (tok.special_tokens), `eos_id` (tok.eos_id), and `merges` serialized as `[[p0, p1, idx], ...]` triples in ascending rank order (`sorted(tok.merges.items(), key=lambda kv: kv[1])`) since JSON has no tuple keys; dump with `ensure_ascii=True`. `from_json(path)`: load JSON, `assert d["schema_version"] == SCHEMA_VERSION` (mirror checkpoint's schema assert), reconstruct `merges = {(p0,p1): idx for p0,p1,idx in d["merges"]}`, validate every id is in `[0, vocab_size)` (V5 input validation — reject out-of-range), and return `BPETokenizer.frozen(pattern=..., merges=..., special_tokens=..., eos_id=..., vocab_size=...)`. Extend `src/personacore/tokenizer/__init__.py` to also re-export `save_json`, `from_json`, `SCHEMA_VERSION` and add them to `__all__`.
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/python -m pytest tests/test_tokenizer_io.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_tokenizer_io.py` passes: save→load→identical encode/decode behavior, `loaded.vocab_size == 8192`, schema_version asserted on load (TOK-04).
    - `from_json` rejects ids outside `[0, vocab_size)` and never calls pickle/torch.load (`grep -E 'pickle|torch' src/personacore/tokenizer/io.py` returns nothing).
    - `from personacore.tokenizer import save_json, from_json, SCHEMA_VERSION` succeeds.
  </acceptance_criteria>
  <done>io.py freezes/loads the tokenizer as schema-versioned data-only JSON; reload is behaviorally identical with locked vocab_size=8192; TOK-04 test green.</done>
</task>

<task type="auto">
  <name>Task 2: Production train script + committed tokenizer.json artifact</name>
  <files>scripts/train_tokenizer.py, artifacts/tokenizer.json</files>
  <read_first>
    - .planning/phases/02-from-scratch-bpe-tokenizer/02-PATTERNS.md (scripts/train_tokenizer.py analog: preflight_demo.py — thin no-CLI main(), path constants, no argparse, reuse seed_everything)
    - scripts/preflight_demo.py (the exact thin-entry shape + module-docstring convention)
    - .planning/phases/02-from-scratch-bpe-tokenizer/02-RESEARCH.md (D-09 bounded corpus: committed fixture vs one-time requests GET; §"Recommended Project Structure" artifacts/ placement)
    - src/personacore/tokenizer/bpe.py (BPETokenizer.train)
    - src/personacore/tokenizer/io.py (save_json — created in Task 1)
    - tests/fixtures/tiny_corpus.txt (the committed offline corpus available as a fallback source)
  </read_first>
  <action>
    Create `scripts/train_tokenizer.py` as a thin no-CLI entry mirroring `preflight_demo.py`: module docstring citing the one-time train→freeze purpose, the D-09 frozen-artifact contract (train production 8192 tokenizer on a bounded TinyStories sample or committed fixture; Phase 5 reuses the FROZEN artifact with no retrain), and Phase-1 D-04 (no argparse; paths/hyperparams are module constants/kwargs). Define `CORPUS_PATH` and `ARTIFACT_PATH = "artifacts/tokenizer.json"` as path-convention constants. `main()`: `seed_everything(1337)` (reuse Phase-1 determinism), read the bounded corpus text, `tok = BPETokenizer().train(text, vocab_size=8192)`, then `save_json(tok, ARTIFACT_PATH)`. For the corpus source, use the committed `tests/fixtures/tiny_corpus.txt` as the default offline-safe path so the script runs with zero network (D-09 permits a committed fixture); leave a documented comment showing the optional one-time `requests` GET of the `TinyStoriesV2-GPT4` validation split as an alternative larger bounded-sample source (D-09), but do NOT add `requests` as a dependency. Run the script to produce `artifacts/tokenizer.json` and COMMIT that artifact (Phase 5 reuses it unchanged — D-09). Ensure `from_json("artifacts/tokenizer.json")` loads and reports `vocab_size == 8192`.
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/python scripts/train_tokenizer.py && .venv/bin/python -c "from personacore.tokenizer import from_json; t=from_json('artifacts/tokenizer.json'); assert t.vocab_size==8192; assert t.eos_id==8184; assert t.decode(t.encode('hello world')) == 'hello world'; print('ARTIFACT-OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/train_tokenizer.py` runs with no network and no argparse, producing `artifacts/tokenizer.json`.
    - `artifacts/tokenizer.json` exists, contains `schema_version`, `vocab_size: 8192`, the special-token map, `eos_id: 8184`, and rank-ordered merge triples; the probe prints `ARTIFACT-OK`.
    - `grep -c argparse scripts/train_tokenizer.py` returns 0 (no CLI — D-04).
  </acceptance_criteria>
  <done>A thin offline train script produces a committed self-contained tokenizer.json with locked vocab_size=8192 that loads and round-trips; Phase 5 reuses it unchanged (D-09).</done>
</task>

<task type="auto">
  <name>Task 3: Green the tiktoken gpt2 equivalence oracle (TOK-05)</name>
  <files>tests/test_tokenizer_oracle.py</files>
  <read_first>
    - tests/test_tokenizer_oracle.py (the red oracle test from Plan 01 — skip-on-offline + recover_merges + no-runtime-tiktoken guard)
    - .planning/phases/02-from-scratch-bpe-tokenizer/02-RESEARCH.md (Pattern 6: recover_merges/bpe, NO byte-shuffle for gpt2; Pitfall 2: get_encoding("gpt2") network; Pitfall 4: runtime-import guard)
    - .planning/phases/02-from-scratch-bpe-tokenizer/02-PATTERNS.md (oracle analog: monkeypatch/skip + import-guard scan)
    - src/personacore/tokenizer/bpe.py (BPETokenizer.frozen + _encode_ordinary to inject recovered merges into)
    - src/personacore/tokenizer/patterns.py (GPT2_SPLIT_PATTERN to configure the from-scratch encoder)
  </read_first>
  <action>
    Finalize `tests/test_tokenizer_oracle.py` (created red in Plan 01) so it goes green where tiktoken/network is available and skips cleanly offline. This is the D-07 contract: TOK-05 oracle = tiktoken `gpt2` algorithm-equivalence, with tiktoken kept test-only `[dev]` and NEVER imported at runtime (D-07). `test_tiktoken_gpt2_equivalence`: `try: import tiktoken; enc = tiktoken.get_encoding("gpt2") except Exception: pytest.skip(...)`. Implement `bpe()` and `recover_merges()` exactly per RESEARCH Pattern 6 to recover GPT-2's merge order from `enc._mergeable_ranks` (raw-byte keyed — NO byte-shuffle, that is GPT-4-only). Build a from-scratch encoder configured with `GPT2_SPLIT_PATTERN` whose `.merges == recover_merges(...)` and whose byte/merge vocab matches GPT-2's raw byte ranks (use `BPETokenizer.frozen` with no special tokens for the gpt2 vocab, or construct the encoder state directly — the merge ids must equal tiktoken's ranks). Assert `our_encoder.encode_ordinary(s) == enc.encode_ordinary(s)` exactly over the oracle string set (ASCII, leading-space, smart quotes, multi-byte) — algorithm-equivalence (D-07). Confirm `test_no_runtime_tiktoken` (scanning `src/personacore/**/*.py` for the substring `tiktoken`) passes (D-07 never-imported-at-runtime) — adjust the scan path/exclusions only if needed; do NOT weaken it. The oracle is the documented phase-gate manual verification (VALIDATION.md): it must be RUN ONCE with network/seeded `TIKTOKEN_CACHE_DIR` and the pass recorded in the SUMMARY as evidence; CI keeps skipping offline.
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/python -m pytest tests/test_tokenizer_oracle.py -x -q -rs 2>&1 | grep -Eq 'passed|skipped' && .venv/bin/python -m pytest tests/test_tokenizer_oracle.py::test_no_runtime_tiktoken -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `test_no_runtime_tiktoken` PASSES (no `tiktoken` substring under `src/personacore/`).
    - `test_tiktoken_gpt2_equivalence` either PASSES (network/cache available) or SKIPS with the offline-skip reason — never errors or hangs (Pitfall 2).
    - When network is available the from-scratch encoder reproduces `enc.encode_ordinary(s)` ids exactly (TOK-05) — record this run in the SUMMARY as phase-gate evidence.
    - `make test` (full CPU-only suite) is green with the oracle skipped offline.
  </acceptance_criteria>
  <done>The from-scratch encode algorithm is proven equivalent to tiktoken gpt2 (run at least once with network), tiktoken is never a runtime import, and CI stays green offline; TOK-05 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tokenizer.json on disk → from_json (load) | data-only JSON artifact, possibly edited/swapped |
| tiktoken gpt2 blob fetch (test-only) | one-time HTTPS download from Azure blob (test path only) |
| (no runtime network) | the frozen tokenizer and from_json never touch the network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Tampering / Elevation | untrusted tokenizer artifact deserialization (arbitrary code) | mitigate | artifact is stdlib `json` (data-only) — NEVER pickle/torch.load; cannot execute code (RESEARCH Security) |
| T-02-06 | Tampering | malformed/out-of-range ids in a swapped artifact | mitigate | `from_json` asserts `schema_version` and validates every id in `[0, vocab_size)` (V5 input validation) |
| T-02-07 | Tampering / DoS | tiktoken gpt2 blob fetch over network (MITM / unavailability) in the oracle test | accept | HTTPS fetch is test-only and skip-on-offline; never crosses into runtime; oracle never imported under `src/` |

> The runtime load path is data-only JSON with schema + id-range validation. No high-severity threat; the only network is a test-only, skip-on-offline oracle fetch. Noted explicitly.
</threat_model>

<verification>
- `make lint` passes on io.py, __init__.py, and the script.
- `pytest tests/test_tokenizer_io.py -x` is green (TOK-04).
- `artifacts/tokenizer.json` loads via `from_json` with `vocab_size==8192`, `eos_id==8184`, and round-trips.
- `make test` full CPU-only suite is green (oracle skips offline); the oracle has been run once with network and recorded.
</verification>

<success_criteria>
- The tokenizer saves/loads as a frozen self-contained JSON artifact exposing a locked `vocab_size == 8192` (TOK-04).
- A committed `artifacts/tokenizer.json` exists for Phase 5 to reuse unchanged (D-09).
- The from-scratch encoder reproduces tiktoken gpt2 ids exactly on the oracle set, with tiktoken never imported at runtime (TOK-05, D-07).
</success_criteria>

<output>
Create `.planning/phases/02-from-scratch-bpe-tokenizer/02-03-SUMMARY.md` when done. Record the one-time networked oracle run (pass evidence) as the TOK-05 phase gate, and confirm the full suite is green with the oracle skipped offline.
</output>
</output>
