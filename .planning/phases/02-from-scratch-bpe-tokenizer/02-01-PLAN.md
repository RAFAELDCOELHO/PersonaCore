---
phase: 02-from-scratch-bpe-tokenizer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - requirements.txt
  - src/personacore/config.py
  - tests/test_config.py
  - tests/fixtures/__init__.py
  - tests/fixtures/tricky_strings.py
  - tests/fixtures/tiny_corpus.txt
  - tests/test_tokenizer_train.py
  - tests/test_tokenizer_roundtrip.py
  - tests/test_tokenizer_special.py
  - tests/test_tokenizer_io.py
  - tests/test_tokenizer_oracle.py
autonomous: true
requirements: [TOK-01, TOK-02, TOK-03, TOK-04, TOK-05]
user_setup: []

must_haves:
  truths:
    - "ModelConfig.vocab_size == 8192 and ModelConfig.eos_id == 8184 (the locked deliverable)"
    - "regex is a declared core runtime dependency; tiktoken is a [dev]-only dependency"
    - "All five TOK requirement test files exist and FAIL (red) because the tokenizer package does not exist yet"
    - "A tricky-string round-trip fixture and a small committed offline corpus exist"
  artifacts:
    - path: "src/personacore/config.py"
      provides: "Locked vocab_size=8192 + eos_id=8184 on ModelConfig"
      contains: "vocab_size: int = 8192"
    - path: "tests/fixtures/tricky_strings.py"
      provides: "TRICKY_STRINGS round-trip corpus (TOK-02)"
      contains: "TRICKY_STRINGS"
    - path: "tests/fixtures/tiny_corpus.txt"
      provides: "Small committed offline training corpus (TOK-01)"
    - path: "tests/test_tokenizer_train.py"
      provides: "Determinism + lowest-rank-replay test (TOK-01)"
    - path: "tests/test_tokenizer_roundtrip.py"
      provides: "Round-trip + chunk-join invariant test (TOK-02)"
    - path: "tests/test_tokenizer_special.py"
      provides: "EOS atomicity + config eos_id test (TOK-03)"
    - path: "tests/test_tokenizer_io.py"
      provides: "Freeze/load + vocab_size==8192 + schema-version test (TOK-04)"
    - path: "tests/test_tokenizer_oracle.py"
      provides: "tiktoken gpt2 equivalence (skip-on-offline) + no-runtime-tiktoken guard (TOK-05)"
  key_links:
    - from: "tests/test_tokenizer_roundtrip.py"
      to: "tests/fixtures/tricky_strings.py"
      via: "import TRICKY_STRINGS"
      pattern: "from tests.fixtures.tricky_strings import"
    - from: "tests/test_tokenizer_*.py"
      to: "personacore.tokenizer"
      via: "import (fails until Plan 02/03 implement it)"
      pattern: "from personacore.tokenizer"
---

<objective>
Lock the load-bearing Phase-2 deliverable (`vocab_size = 8192`, `eos_id = 8184`) into `ModelConfig`, declare the new dependencies (`regex` core runtime, `tiktoken` `[dev]`-only), and stand up the complete Wave 0 test scaffolding — fixtures plus all five red TOK test files — so Plans 02 and 03 implement against a fixed, failing test surface.

Purpose: The locked `vocab_size` is THE Phase-2 deliverable Phases 3–4 size the model around; it must land first and never move. Writing the tests red (RED of RED→GREEN) makes the encode/decode/freeze contracts explicit before any tokenizer code exists, so downstream plans have a deterministic acceptance target.
Output: Updated `config.py` + `test_config.py`; `pyproject.toml` + `requirements.txt` dependency deltas; `tests/fixtures/` (tricky strings + tiny corpus); five `tests/test_tokenizer_*.py` files that import `personacore.tokenizer` and therefore FAIL until the package exists.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-from-scratch-bpe-tokenizer/02-CONTEXT.md
@.planning/phases/02-from-scratch-bpe-tokenizer/02-RESEARCH.md
@.planning/phases/02-from-scratch-bpe-tokenizer/02-PATTERNS.md
@.planning/phases/02-from-scratch-bpe-tokenizer/02-VALIDATION.md

<interfaces>
<!-- The public import surface that Plans 02/03 WILL create. Tests are written against
     these exact names so they go green once the package lands. Executor: do NOT create
     the package here — only the tests that import it (they must be RED). -->

From src/personacore/tokenizer/__init__.py (created in Plan 02/03):
  BPETokenizer        # class: .train(text, vocab_size), .encode(text), .decode(ids),
                      #        .frozen(...) classmethod, .vocab_size, .eos_id, .merges, .vocab, .pattern, .special_tokens
  GPT2_SPLIT_PATTERN  # str: the exact GPT-2 pre-tok regex
  SPECIAL_TOKENS      # dict[str,int]: name -> id, top-pinned 8184-8191
  EOS_TOKEN           # str: "<|endoftext|>"
  EOS_ID              # int: 8184

From src/personacore/tokenizer/io.py (created in Plan 03):
  save_json(tok, path)   # freeze to JSON (schema_version, pattern, vocab_size, special_tokens, eos_id, merges)
  from_json(path)        # -> frozen BPETokenizer
  SCHEMA_VERSION         # int: 1

From src/personacore/config.py (modified HERE):
  ModelConfig.vocab_size == 8192
  ModelConfig.eos_id == 8184
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock vocab_size + eos_id in config and update its test</name>
  <files>src/personacore/config.py, tests/test_config.py</files>
  <read_first>
    - src/personacore/config.py (the ModelConfig dataclass at lines 64-77 — the in-place edit target)
    - tests/test_config.py (test_configs_are_dataclasses at lines 36-46 — the in-place edit target)
    - .planning/phases/02-from-scratch-bpe-tokenizer/02-RESEARCH.md (§"config.py deltas — D-01 / TOK-03")
    - .planning/phases/02-from-scratch-bpe-tokenizer/02-PATTERNS.md (§"src/personacore/config.py (MODIFY — config)")
  </read_first>
  <behavior>
    - ModelConfig().vocab_size == 8192 (was 50304 placeholder)
    - ModelConfig().eos_id == 8184
    - eos_id flows into the checkpoint unchanged because save_checkpoint already asdict()s model_config (no checkpoint code change)
  </behavior>
  <action>
    In `src/personacore/config.py`, on the `ModelConfig` dataclass: change `vocab_size` default from `50304` to `8192` (D-01) and update its inline comment from `# placeholder — locked by Phase 2.` to `# LOCKED by Phase 2 (was 50304 placeholder).`. Add a new field `eos_id: int = 8184` directly after `vocab_size` with the comment `# shared EOS id, recorded in checkpoint (Phase-1 D-03); top-pinned at 8184 (D-03a).`. Update the `ModelConfig` class docstring (lines 66-70) to state vocab_size is now LOCKED by Phase 2 rather than a placeholder. Do NOT touch RuntimeConfig or TrainConfig. In `tests/test_config.py`, extend `test_configs_are_dataclasses` (or add a focused test) to assert `ModelConfig().vocab_size == 8192` and `ModelConfig().eos_id == 8184`. Preserve the CPU-only test-header convention.
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/python -m pytest tests/test_config.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_config.py` passes with assertions on `vocab_size == 8192` and `eos_id == 8184`.
    - `grep -n "vocab_size: int = 8192" src/personacore/config.py` matches; `grep -c "50304" src/personacore/config.py` returns 0.
    - `grep -n "eos_id: int = 8184" src/personacore/config.py` matches.
  </acceptance_criteria>
  <done>ModelConfig.vocab_size is locked at 8192 and eos_id=8184 is present; test_config.py asserts both and passes.</done>
</task>

<task type="auto">
  <name>Task 2: Declare regex (core) + tiktoken ([dev]) dependencies</name>
  <files>pyproject.toml, requirements.txt</files>
  <read_first>
    - pyproject.toml (the [project] dependencies block and [project.optional-dependencies] dev extra)
    - requirements.txt (the dependency list kept consistent with pyproject — Phase-1 D-09)
    - .planning/phases/02-from-scratch-bpe-tokenizer/02-RESEARCH.md (§"Standard Stack" / §"Installation (deltas to pyproject.toml)" / §"Package Legitimacy Audit")
  </read_first>
  <action>
    In `pyproject.toml` `[project].dependencies`, ADD `"regex~=2026.5"` as a CORE RUNTIME dependency with an inline comment noting it is the GPT-2 pre-tok engine for `\p{L}`/`\p{N}` (stdlib `re` cannot compile these) and is a pre-tok primitive, NOT a from-scratch violation. Keep `torch` absent from core (D-10 unchanged). In `[project.optional-dependencies].dev`, ADD `"tiktoken~=0.13"` to the existing `["pytest~=9.0", "ruff~=0.15"]` list, with a comment that it is the TOK-05 oracle and is test-only — NEVER runtime-imported. In `requirements.txt`, add `regex~=2026.5` to the core runtime section (under `numpy~=2.4`) with a matching one-line rationale comment, and add `tiktoken~=0.13` to the commented `# --- Dev tooling ---` block alongside the existing commented pytest/ruff entries (keep the dev-tooling block's commenting style consistent). Do NOT add `regex` or `tiktoken` to the `[cpu]` or `[demo]` extras. The optional `requests` dep is NOT added in this plan (Plan 03 decides fixture-vs-fetch).
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && grep -q 'regex~=2026.5' pyproject.toml && grep -q 'tiktoken~=0.13' pyproject.toml && grep -q 'regex~=2026.5' requirements.txt && python -c "import tomllib;d=tomllib.load(open('pyproject.toml','rb'));assert any('regex' in x for x in d['project']['dependencies']);assert any('tiktoken' in x for x in d['project']['optional-dependencies']['dev']);assert not any('tiktoken' in x for x in d['project']['dependencies']),'tiktoken must NOT be a core dep'" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `regex` appears in `[project].dependencies` (core) of pyproject.toml AND in requirements.txt.
    - `tiktoken` appears ONLY in the `[dev]` extra of pyproject.toml — never in `[project].dependencies`.
    - The tomllib assertion script prints `OK`.
  </acceptance_criteria>
  <done>regex is a declared core runtime dep (pyproject + requirements); tiktoken is dev-only; the tomllib guard passes.</done>
</task>

<task type="auto">
  <name>Task 3: Write Wave 0 fixtures + all five red TOK test files</name>
  <files>tests/fixtures/__init__.py, tests/fixtures/tricky_strings.py, tests/fixtures/tiny_corpus.txt, tests/test_tokenizer_train.py, tests/test_tokenizer_roundtrip.py, tests/test_tokenizer_special.py, tests/test_tokenizer_io.py, tests/test_tokenizer_oracle.py</files>
  <read_first>
    - .planning/phases/02-from-scratch-bpe-tokenizer/02-VALIDATION.md (§"Per-Requirement Verification Map" + §"Wave 0 Requirements" + the tricky-string fixture list)
    - .planning/phases/02-from-scratch-bpe-tokenizer/02-PATTERNS.md (the test-analog sections: test_tokenizer_train/roundtrip/special/io/oracle + tricky_strings fixture)
    - .planning/phases/02-from-scratch-bpe-tokenizer/02-RESEARCH.md (§"Tricky-string fixture set", Pattern 4, Pattern 6, Pitfall 2, Pitfall 4, Pitfall 6)
    - tests/test_seeding.py (the determinism repeat-and-compare + CPU-only header convention)
    - tests/test_checkpoint.py (tmp_path save->load->assert pattern; monkeypatch/skip pattern)
    - tests/conftest.py (fixture conventions)
  </read_first>
  <action>
    Create `tests/fixtures/__init__.py` (empty package marker so `from tests.fixtures.tricky_strings import ...` resolves). Create `tests/fixtures/tricky_strings.py` exporting a `TRICKY_STRINGS` list covering every case in VALIDATION.md §"Tricky-string fixture set": leading/trailing-space ASCII, smart quotes + em-dash + ellipsis, ZWJ emoji + flag sequences, multi-byte scripts (café/naïve/Ω/日本語/Привет), newline/tab/whitespace runs, digits+punctuation (e.g. `"price: $1,234.56 (≈ 2× more)"`), the embedded `"start<|endoftext|>end"` literal, and the empty + single-byte strings. Create `tests/fixtures/tiny_corpus.txt`: a small (a few hundred lines, well under ~1MB) committed plain-UTF-8 corpus of simple short sentences (TinyStories-style: short, simple-vocabulary English) sufficient to learn several hundred merges offline; include a couple of `<|endoftext|>` document separators so atomicity is exercised, and some non-ASCII characters. Then create the five test files, each opening with the CPU-only/GPU-free module-docstring header convention citing its TOK id:

    - `tests/test_tokenizer_train.py` (TOK-01): import `BPETokenizer` from `personacore.tokenizer`; train twice on `tests/fixtures/tiny_corpus.txt` at a small `vocab_size` (e.g. 512) and assert `a.merges == b.merges` (determinism / lowest-rank replay, Pitfall 1). Reuse `seed_everything` from Phase 1 in the test for discipline.
    - `tests/test_tokenizer_roundtrip.py` (TOK-02): `@pytest.mark.parametrize` over `TRICKY_STRINGS`; assert `tok.decode(tok.encode(s)) == s` for each. Add a `test_chunk_join_invariant` asserting that the concatenation of the pre-tok chunks equals `text.encode("utf-8")` (never strip/normalize — Pitfall 6). Provide a module-level `tok` pytest fixture that builds a trained tokenizer from the tiny corpus.
    - `tests/test_tokenizer_special.py` (TOK-03): assert `ModelConfig().eos_id == 8184` and `ModelConfig().vocab_size == 8192`; assert `tok.encode("a<|endoftext|>b")` contains exactly one occurrence of the EOS id and that it is never byte-split (atomicity, Pattern 4 / Pitfall 3); assert EOS round-trips.
    - `tests/test_tokenizer_io.py` (TOK-04): import `save_json`/`from_json`/`SCHEMA_VERSION` from `personacore.tokenizer.io`; use `tmp_path` to save then reload a trained tokenizer; assert `loaded.vocab_size == 8192`, schema_version asserted on load, and `loaded.encode(s) == tok.encode(s)` for sample strings.
    - `tests/test_tokenizer_oracle.py` (TOK-05 + Pitfall 4): a `test_tiktoken_gpt2_equivalence` that `try`s `import tiktoken; enc = tiktoken.get_encoding("gpt2")` and `pytest.skip("tiktoken gpt2 unavailable offline")` on any exception, then (per Pattern 6) recovers merges from `enc._mergeable_ranks`, injects them into a from-scratch encoder configured with `GPT2_SPLIT_PATTERN`, and asserts exact ID equality vs `enc.encode_ordinary(s)` over a small string set (NO byte-shuffle for gpt2). A separate `test_no_runtime_tiktoken` that scans `src/personacore/**/*.py` and asserts no file contains the substring `tiktoken`.

    Every test imports from `personacore.tokenizer` / `personacore.tokenizer.io`, which do not exist yet — so the suite is RED by design. Mark these as the expected-red Wave 0 state in the SUMMARY. Do NOT create any `src/personacore/tokenizer/` files in this plan.
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && test -f tests/fixtures/tricky_strings.py && test -f tests/fixtures/tiny_corpus.txt && for f in train roundtrip special io oracle; do test -f tests/test_tokenizer_$f.py || { echo "MISSING $f"; exit 1; }; done && .venv/bin/python -m pytest tests/test_tokenizer_roundtrip.py -q 2>&1 | grep -Eq 'error|ModuleNotFoundError|No module named' && echo "RED-AS-EXPECTED"</automated>
  </verify>
  <acceptance_criteria>
    - All five `tests/test_tokenizer_*.py` files plus `tests/fixtures/tricky_strings.py` and `tests/fixtures/tiny_corpus.txt` exist.
    - The tokenizer tests FAIL at collection/import with a `personacore.tokenizer` ModuleNotFoundError (RED as expected — no tokenizer package yet); verify prints `RED-AS-EXPECTED`.
    - `grep -c 'pytest.skip' tests/test_tokenizer_oracle.py` >= 1 (oracle skips offline) and `test_no_runtime_tiktoken` is present.
    - `tests/fixtures/tiny_corpus.txt` is under 1MB (`wc -c < tests/fixtures/tiny_corpus.txt` < 1000000).
  </acceptance_criteria>
  <done>Fixtures and all five TOK test files exist; the tokenizer suite is red pending the package; the oracle test skips offline and includes the no-runtime-tiktoken guard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package-manager install (regex/tiktoken) | new third-party packages enter the build |
| (no runtime input boundary in this plan) | this plan adds only config + tests + fixtures; no parsing of untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | tiktoken leaking into runtime import surface | mitigate | `test_no_runtime_tiktoken` scans `src/personacore/**/*.py`; tiktoken kept in `[dev]` extra only (Pitfall 4) |
| T-02-SC | Tampering | pip installs of `regex`, `tiktoken` | accept | Both are decade-old canonical PyPI packages, version-pinned (`regex~=2026.5`, `tiktoken~=0.13`); RESEARCH §"Package Legitimacy Audit" version-verified them on PyPI. slopcheck was unavailable but neither is a hallucination risk; no `[ASSUMED]`/`[SUS]` package requires a blocking checkpoint. |

> Realistic surface is narrow: this plan ships config edits, dependency declarations, and CPU-only/offline tests. No network at runtime, no untrusted-input execution path, no high-severity threat. Noted explicitly rather than invented.
</threat_model>

<verification>
- `make lint` passes (ruff E/F/W/I, line-length 100) on all changed/created files.
- `pytest tests/test_config.py -x` is green (vocab_size==8192, eos_id==8184).
- The tomllib dependency guard prints OK (regex core, tiktoken dev-only).
- The five tokenizer test files are RED with a `personacore.tokenizer` import error (expected Wave 0 state).
</verification>

<success_criteria>
- ModelConfig locks `vocab_size = 8192` and `eos_id = 8184`; test_config.py asserts both and passes.
- `regex` is a declared core runtime dependency in both pyproject.toml and requirements.txt; `tiktoken` is `[dev]`-only.
- Tricky-string fixture + tiny offline corpus exist; all five TOK test files exist and are red pending the tokenizer package.
</success_criteria>

<output>
Create `.planning/phases/02-from-scratch-bpe-tokenizer/02-01-SUMMARY.md` when done. Note explicitly that the five tokenizer test files are intentionally RED (Wave 0) and will go green in Plans 02 (train/roundtrip/special) and 03 (io/oracle).
</output>
