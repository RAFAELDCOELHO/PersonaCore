---
phase: 25-frontier-sweep-and-the-existence-gate-verdict
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - tests/test_phase25_correction.py
autonomous: true
requirements: [RPT-02, CTRL-02, FRONT-01]

must_haves:
  truths:
    - "§C3 CORRECTED: `_addendum.append_addendum` is the WRONG mechanism for planning-doc continuations. It has ZERO call sites against `.planning/*.md`; its 'refuses a second append' is a placeholder-COUNT rule, not a once-only lock; run live on a copy of REQUIREMENTS.md it SUCCEEDED on an accidental 'PENDING' substring; and `_verdict.recorded_verdict` returns None for planning markdown, so its verdict-preservation `_prove` is VACUOUS there."
    - "What actually shipped TWICE for planning-doc prose is sentinel-bounded dated blocks plus `scripts/_prose.normalized` guards — plans 23-12 and 24-03. Copy `tests/test_phase24_correction.py`'s four mechanics."
    - "D-02: SC1's binding comparator is superseded by a dated continuation to the Phase 23 MATCHED control (0.7837301587301587, floor MATCHED_CONTROL_NOISE_FLOOR = 0.0267857142857143) — the only comparator differing from a real DP point by exactly the two DP parameters. v2.0's 0.4921 / 0.3483 is retained as a DISCLOSED HISTORICAL REFERENCE with its four recipe deltas named. The original SC1 text stays visible, superseded, never deleted."
    - "D-19: FRONT-01's 'swept to the never-taught floor' is corrected to a DP-ARM property. The adversarial axis terminates at its POOL CEILING (1.9090909090909092 = the largest ratio at which the whole trained pool is used exactly once) — a NAMED STRUCTURAL ASYMMETRY, not a deficit. Both arms read against the SAME already-measured never-taught floor."
    - "CTRL-02's `clip_norm=inf` wording gets its own dated continuation: DPSGD's constructor REFUSES math.inf with [dp-refusal:clip-domain]. C = infinity is represented as a finite bound PROVEN not to bind (1e6 with clip_bind_count 0)."
    - "D-31: SC4's `results/phase2X_frontier.json` is a PLACEHOLDER, not a filename. The artifact is `results/phase25_frontier.json`."
    - "§C4 CORRECTED: RPT-02's second half is ALREADY DISCHARGED TWICE (plans 23-12 and 24-03). The RPT-02 checkbox already reads `[x]` while its traceability row still says DEFERRED — the two disagree at HEAD. D-38 stands but is a STALE-ROW REPAIR, not a mechanism build."
    - "D-38: RPT-02 is ADDED to ROADMAP Phase 25's Requirements line, making it a two-phase requirement — exactly the ADVT-01 repair already recorded under REQUIREMENTS Traceability, which is the template to copy."
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "SC1's dated continuation (D-02), SC4's artifact-path continuation (D-31), RPT-02 on Phase 25's Requirements line (D-38)"
      contains: "25-07-CONTINUATION-SC1-COMPARATOR-BEGIN"
    - path: ".planning/REQUIREMENTS.md"
      provides: "FRONT-01's scope continuation (D-19), CTRL-02's clip-domain continuation, and the RPT-02 traceability row repaired additively"
      contains: "25-07-CONTINUATION-FRONT01-SCOPE-BEGIN"
    - path: "tests/test_phase25_correction.py"
      provides: "the continuations guarded through _prose.normalized, each with the original claim asserted still standing"
      contains: "def test_every_original_claim_survives_beside_its_continuation"
  key_links:
    - from: "tests/test_phase25_correction.py"
      to: "scripts/_prose.py::normalized"
      via: "every claim and marker matched through normalized(), because the claims line-wrap in the planning docs"
      pattern: "normalized"
    - from: ".planning/REQUIREMENTS.md RPT-02 traceability row"
      to: "plans 23-12 and 24-03"
      via: "the two already-shipped correction sweeps recorded additively as the discharging evidence"
      pattern: "23-12"
---

<objective>
Ship the dated continuations this phase owes and repair RPT-02's stale traceability row — using the
mechanism that has actually shipped twice in this repository, not the one CONTEXT names.

Purpose: `_addendum.append_addendum` was measured this session against copies of the real planning
docs. It **succeeded on an accidental "PENDING" substring**, its "refuses a second append" is a
placeholder-count rule rather than a lock, and its verdict-preservation proof compares `None == None`
on planning markdown — vacuous. It has zero call sites against `.planning/*.md`. What did ship, twice,
is a sentinel-bounded dated block plus a `_prose.normalized` guard module: plan 23-12 into
`ROADMAP.md` / `REQUIREMENTS.md` / `STATE.md`, and plan 24-03 into `ROADMAP.md`.

**That is also RPT-02's discharge.** The requirement is a conjunction — the helper must exist AND be
used for correction sweeps — and both halves have been met twice already. The RPT-02 checkbox is `[x]`
while its traceability row still says DEFERRED. This plan makes the row agree with the checkbox,
additively, and adds Phase 25's third instance.

Output: five sentinel-bounded continuations across two planning documents, one Requirements-line
addition, and `tests/test_phase25_correction.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-frontier-sweep-and-the-existence-gate-verdict/25-CONTEXT.md
@.planning/phases/25-frontier-sweep-and-the-existence-gate-verdict/25-RESEARCH.md
@.planning/phases/25-frontier-sweep-and-the-existence-gate-verdict/25-VALIDATION.md
</context>

<environment>
**Python 3.11 venv is MANDATORY.** `.venv/bin/python -m pytest ...`. Baseline **1647 passed, 1 skipped**.

**ADDITIVE ONLY.** No original claim may be deleted, reworded, reflowed or moved. Every correction is a
dated block appended after the claim it supersedes, and the original stays visible as the record of
what was believed. The `git diff --numstat` deletion column for both planning files must be **0**.

**`gsd-sdk` mutation handlers must NOT be called** on `ROADMAP.md`, `REQUIREMENTS.md` or `STATE.md` —
they have corrupted planning frontmatter repeatedly. Hand-edit, snapshot before, diff after.

**Do NOT use `scripts/_addendum.py` in this plan.** Measured unsuitable for `.planning/*.md`.

**The four ancestry-guarded modules must stay byte-unchanged.**
</environment>

<tasks>

<task type="auto">
  <name>Task 1: Four dated continuations — SC1's comparator, SC4's artifact path, FRONT-01's scope, CTRL-02's clip domain</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/ROADMAP.md` — locate **by content, never by line number**: plan 23-12's
      `23-12-CONTINUATION-BEGIN` / `-END` pair and plan 24-03's `24-03-CONTINUATION-BEGIN` / `-END`
      pair. Those are the templates. Also locate §Phase 25's `**Requirements**` line, its five Success
      Criteria, SC1's `defensible neighbourhood of v2.0's 0.4921 / 0.3483` clause, and SC4's
      `results/phase2X_frontier.json` placeholder
    - `.planning/REQUIREMENTS.md` — the 23-12 sentinel pairs; the FRONT-01 body containing
      `swept to the never-taught floor and to σ→0`; the CTRL-02 body containing
      `clip_norm=inf, noise_multiplier=0`
    - `scripts/_prose.py` — `normalized(text)` returns `" ".join(text.split())`; the module imports
      nothing
    - `results/phase23_matched_control.json`, `results/phase23_matched_verdict.json` — D-02's
      substituted comparator: `0.7837301587301587`, floor `0.0267857142857143`, `deviation 0.0`
    - `results/phase23_control_floor.json` — the unmatched fact-aligned control `0.5615079365079365`,
      floor `0.05357142857142849`, evidence that v2.0's 0.4921 belongs to a different recipe
    - `scripts/mitigation_budget.py` — `ADVERSARIAL_RATIO_GRID`'s upper extreme
      `1.9090909090909092` and its `_PROVENANCE` `governs` string, for D-19's pool-ceiling claim
    - `src/personacore/privacy/dpsgd.py` — the **executable** constructor domain checks (resolve by
      content: the `clip_norm` finite/positive validation raising `[dp-refusal:clip-domain]`) and the
      single noise draw site computing `std` as `sigma * C`. **The `:52-80` range CONTEXT cites is
      module docstring, not code** — resolve by symbol and cite by symbol
  </read_first>
  <action>
Write four sentinel-bounded dated continuations. Each uses its own unique pair
`25-07-CONTINUATION-<TAG>-BEGIN` / `25-07-CONTINUATION-<TAG>-END` inside HTML comment markers, appears
**immediately after** the claim it supersedes, carries the date, and leaves that claim byte-unchanged.
Do not reflow the surrounding paragraph — a reflow makes the diff unreviewable and the original
unquotable.

**(a) `ROADMAP.md` §Phase 25 SC1 — tag `SC1-COMPARATOR` (D-02).** SC1 binds the control's recall to a
defensible neighbourhood of v2.0's `0.4921 / 0.3483`. State:
  - the binding comparator becomes the **Phase 23 MATCHED control**: `0.7837301587301587` with floor
    `MATCHED_CONTROL_NOISE_FLOOR = 0.0267857142857143`, cited to `results/phase23_matched_control.json`
    and `results/phase23_matched_verdict.json`
  - the reason: it is the only comparator differing from a real DP point by **exactly the two DP
    parameters** — a property v2.0's 0.4921 never had
  - the four named recipe deltas making v2.0 non-binding: held-out denominator **936 → 648**, 8×
    teaching-token exposure, replay-in-lot, fact-aligned bins
  - the fact-aligned unmatched control reads `0.5615079365079365`, a third distinct number — quoted so
    a reader can see the spread rather than take the substitution on trust
  - v2.0's 0.4921 / 0.3483 is **retained as a disclosed historical reference**, not deleted
  - SC1 stays an **ACTIVE** stop rule; only its comparator moved

**(b) `ROADMAP.md` §Phase 25 SC4 — tag `SC4-ARTIFACT-PATH` (D-31).** State that
`results/phase2X_frontier.json` was a **placeholder, not a filename**, that the artifact this phase
emits is `results/phase25_frontier.json`, and that every plan, test and figure in this phase resolves
the path from `scripts/phase25_record.py`'s module constant rather than from planning prose. Leave the
placeholder text standing.

**(c) `REQUIREMENTS.md` FRONT-01 — tag `FRONT01-SCOPE` (D-19).** State:
  - *"swept to the never-taught floor"* is a **DP-arm property**. The DP axis has a physical mechanism
    driving utility toward zero under high noise; the adversarial axis terminates at its **pool
    ceiling** by construction — `1.9090909090909092`, the largest ratio at which the whole trained
    pool is used exactly once
  - this is a **NAMED STRUCTURAL ASYMMETRY, not a deficit**
  - **both arms are read against the SAME already-measured never-taught floor** — 0/416 at 5 seeds,
    `results/phase23_never_taught.json` — as the plane's shared lower-left reference, so no adversarial
    point needs to reach it personally
  - the original wording stays standing and the σ→0 half of the sentence is unaffected

**(d) `REQUIREMENTS.md` CTRL-02 — tag `CTRL02-CLIP-DOMAIN`.** State:
  - `clip_norm=inf` is **REFUSED by the code**: the DPSGD constructor requires finite and positive and
    raises `[dp-refusal:clip-domain]` on `math.inf`, and equally on `0.0`, `-1.0` and `nan`. The check
    runs in the pre-pass **before** the model audit, so even an empty module triggers it
  - the forcing mechanism: the noise standard deviation is `sigma * C` at a **single** draw site with
    **no `sigma == 0` branch**, so `0.0 * inf` is `nan` and the draw raises
    `RuntimeError: normal expects std >= 0.0, but found std nan`
  - therefore **C = ∞ is represented as a finite bound PROVEN not to bind** — Phase 23 used
    `clip_norm = 1000000.0` with `clip_bind_count = 0`, and Phase 25's control reuses that same `1e6`
    so D-01's bit-level reproduction stays reachable
  - the citation correction: the constructor checks and the draw site are resolved **by symbol**; the
    line range CONTEXT cites is module docstring

Hand-edit both files. Snapshot both before the edit; `git diff --numstat` after and require a deletion
count of `0` on both.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase25_correction.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --numstat -- .planning/ROADMAP.md .planning/REQUIREMENTS.md` shows a deletion count of **0** for both files — insertions only
    - `.venv/bin/python -c "import pathlib; t=pathlib.Path('.planning/ROADMAP.md').read_text(); print([(g, t.count('25-07-CONTINUATION-'+g+'-BEGIN'), t.count('25-07-CONTINUATION-'+g+'-END')) for g in ('SC1-COMPARATOR','SC4-ARTIFACT-PATH')])"` prints `[('SC1-COMPARATOR', 1, 1), ('SC4-ARTIFACT-PATH', 1, 1)]` — counted with `str.count`, **never `grep -c`**, which counts lines
    - `.venv/bin/python -c "import pathlib; t=pathlib.Path('.planning/REQUIREMENTS.md').read_text(); print([(g, t.count('25-07-CONTINUATION-'+g+'-BEGIN'), t.count('25-07-CONTINUATION-'+g+'-END')) for g in ('FRONT01-SCOPE','CTRL02-CLIP-DOMAIN')])"` prints both tuples as `1, 1`
    - `.venv/bin/python -m pytest tests/test_phase25_correction.py -k originals -x` passes — every superseded original claim is asserted still present through `_prose.normalized`
    - `.venv/bin/python -m pytest tests/test_phase25_correction.py -k measured_values -x` passes — the continuations carry `0.0267857142857143`, `0.5615079365079365`, `1.9090909090909092`, `[dp-refusal:clip-domain]`, `1000000.0` and `results/phase25_frontier.json`
    - `grep -rn "append_addendum" .planning/ROADMAP.md .planning/REQUIREMENTS.md` returns nothing — the measured-unsuitable mechanism was not used
    - The SUMMARY records that **zero `gsd-sdk` mutation handlers were called** and quotes the two `git diff --numstat` lines
  </acceptance_criteria>
  <done>Four dated sentinel-bounded continuations sit immediately after the claims they supersede, every original survives byte-unchanged through `_prose.normalized`, and both diffs are insertions-only.</done>
</task>

<task type="auto">
  <name>Task 2: D-38 — RPT-02 onto Phase 25's Requirements line, and its stale traceability row repaired additively</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/ROADMAP.md` — §Phase 25's `**Requirements**` line, currently
      `CTRL-01, CTRL-02, FRONT-01, FRONT-02, FRONT-03, FRONT-04, ADVT-01`; and §Phase 20's
      `**Requirements**` line, which **already carries RPT-02** — that is why adding it here makes
      RPT-02 a two-phase requirement rather than a move
    - `.planning/REQUIREMENTS.md` — the `- [x] **RPT-02**` checkbox in the RPT section; the RPT-02
      **traceability row** beginning `DEFERRED to Phase 25 — first half shipped, second half is the
      unmet conjunction`; and the `AMENDED 2026-08-30 (phase 24 close)` block recording ADVT-01's
      two-phase span additively. **That block is the template**
    - `tests/test_phase23_cost.py` — the 23-12 correction sweep, resolved by content: the test
      asserting the original claim survives beside a dated marker in all three planning files, matched
      through `_prose.normalized`
    - `tests/test_phase24_correction.py` — the 24-03 correction sweep, the second discharging instance
    - `scripts/_prose.py` — the helper both sweeps route through
  </read_first>
  <action>
**(a) The ROADMAP Requirements line.** Append `, RPT-02` to §Phase 25's `**Requirements**` line.
Immediately after that line add a sentinel-bounded dated note, tag `RPT02-SPAN`, recording that RPT-02
now appears on **two** phase Requirements lines (Phase 20 and Phase 25) and pointing at the
REQUIREMENTS amendment for the reason — the same shape the ADVT-01 repair used.

**(b) The traceability row, repaired ADDITIVELY.** Do **not** rewrite the DEFERRED row. Append a dated
continuation to it, tag `RPT02-ROW`, recording the three measurements that supersede its premise:
  - the RPT-02 checkbox already reads `- [x]` while the row says DEFERRED — **the two disagreed at
    HEAD**; the row was written at Phase-20 close and never updated
  - the "second half" — routing doc-consistency checks through `normalized` — **shipped in plan 23-12
    and again in plan 24-03**. Name both plans, name their guard files
    (`tests/test_phase23_cost.py`'s correction sweep and `tests/test_phase24_correction.py`), and state
    that both are correction sweeps over `.planning/*.md` through `scripts/_prose.normalized`
  - Phase 25 adds the **third** instance: this plan's four continuations, guarded by
    `tests/test_phase25_correction.py`
State the disposition in one sentence: the row's *premise* — that no phase can currently tick RPT-02 —
is **false**; D-38's *decision* stands, and what it repairs is a **stale row**, not a missing
mechanism. Follow the additive doctrine the ADVT-01 amendment established: the dated 2026-08-20
"48/48 mapped, 0 orphans, 0 duplicates" line is never edited, only superseded in place.

**(c) The duplicate-count statement.** Beside the existing ADVT-01 amendment, append a dated note
(tag `RPT02-DUPLICATE-COUNT`) recording RPT-02 as the **second** ID appearing on two phase lines, so the
count statement stays honest without the original being touched.

Hand-edit. No `gsd-sdk` mutation handler. Snapshot and diff; deletions must be `0`.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase25_correction.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -c "import pathlib; t=pathlib.Path('.planning/ROADMAP.md').read_text(); s=t[t.index('### Phase 25'):t.index('### Phase 26')]; line=[l for l in s.splitlines() if l.startswith('**Requirements**')][0]; print(line); assert 'RPT-02' in line"` prints the line and exits 0
    - `.venv/bin/python -c "import pathlib; t=pathlib.Path('.planning/REQUIREMENTS.md').read_text(); print([(g, t.count('25-07-CONTINUATION-'+g+'-BEGIN')) for g in ('RPT02-ROW','RPT02-DUPLICATE-COUNT')])"` prints both counts as `1`
    - `.venv/bin/python -m pytest tests/test_phase25_correction.py -k rpt02 -x` passes — the row's continuation names `23-12`, `24-03`, both guard files, and the word `premise`
    - `git diff --numstat -- .planning/ROADMAP.md .planning/REQUIREMENTS.md` shows a deletion count of **0** for both files
    - `.venv/bin/python -c "import pathlib,sys; sys.path.insert(0,'scripts'); from _prose import normalized; q=normalized(pathlib.Path('.planning/REQUIREMENTS.md').read_text()); assert normalized('DEFERRED to Phase 25') in q; assert normalized('0 orphans, 0 duplicates') in q; print('both superseded originals survive')"` exits 0
    - `.venv/bin/python -m pytest tests/ -q` reports 0 failed; record the `N passed, M skipped` line
  </acceptance_criteria>
  <done>Phase 25's Requirements line carries RPT-02, the traceability row's false premise is superseded in place naming both already-discharging plans, and every original line — including the dated duplicate-count statement — survives.</done>
</task>

<task type="auto">
  <name>Task 3: `tests/test_phase25_correction.py` — the four mechanics, copied not re-derived</name>
  <files>tests/test_phase25_correction.py</files>
  <read_first>
    - `tests/test_phase24_correction.py` — read its module docstring, which **states the four mechanics
      verbatim**, and its body. Copy them rather than re-deriving:
      (1) match through `scripts/_prose.normalized`;
      (2) search for the marker **from the claim's index**, never from byte 0;
      (3) count sentinels with `str.count`, never `grep -c`, because `grep -c` counts LINES and two
          BEGIN sentinels on one line would satisfy a `grep -c ... = 1` check while the second span
          goes unguarded;
      (4) resolve node ids and identifiers **by AST**, never by grep
    - `tests/test_phase23_cost.py` — the 23-12 sweep, the first instance of the same shape
    - `.planning/ROADMAP.md` and `.planning/REQUIREMENTS.md` — the six sentinel tags written in Tasks 1
      and 2: `SC1-COMPARATOR`, `SC4-ARTIFACT-PATH`, `FRONT01-SCOPE`, `CTRL02-CLIP-DOMAIN`,
      `RPT02-SPAN`, `RPT02-ROW`, `RPT02-DUPLICATE-COUNT`
    - `scripts/_prose.py` — `normalized`
  </read_first>
  <action>
Create `tests/test_phase25_correction.py`. CPU-only: stdlib, `pytest`, and the one sibling script.
The module docstring must state that this file is the third instance of the register
`tests/test_phase23_cost.py` and `tests/test_phase24_correction.py` already hold — **and that this is
precisely what discharges RPT-02's second half for the third time**, under the requirement's own
regulation rather than beside it.

**(a) Sentinel integrity.** `test_each_continuation_is_bounded_by_exactly_one_sentinel_pair` —
parametrized over all seven tags. For each: `str.count(BEGIN) == 1` and `str.count(END) == 1`, and the
END index is greater than the BEGIN index. State mechanic (3) in the docstring as the reason
`str.count` is used.

**(b) The originals survive.** `test_every_original_claim_survives_beside_its_continuation` —
parametrized over the superseded claims, each matched through `normalized`:
  - `defensible neighbourhood of v2.0's 0.4921 / 0.3483`
  - `results/phase2X_frontier.json`
  - `swept to the never-taught floor`
  - `clip_norm=inf, noise_multiplier=0`
  - `DEFERRED to Phase 25`
  - the dated `0 orphans, 0 duplicates` line
Assert each is present **and** that its continuation's BEGIN marker appears **after** the claim's own
index — mechanic (2). Searching from byte 0 would assert something different and false-RED-prone.

**(c) The continuations carry their measured values.**
`test_the_continuations_carry_their_measured_values` — assert through `normalized` that the
continuation spans (sliced between their own sentinels, not the whole file) contain
`0.7837301587301587`, `0.0267857142857143`, `0.5615079365079365`, `936`, `648`,
`1.9090909090909092`, `[dp-refusal:clip-domain]`, `1000000.0` and `results/phase25_frontier.json`.
Slicing to the span is load-bearing: a value that happens to appear elsewhere in the document would
otherwise satisfy the assertion.

**(d) RPT-02's repair is evidenced, not asserted.** `test_rpt02_row_names_both_discharging_plans` —
the `RPT02-ROW` span contains `23-12`, `24-03`, `tests/test_phase23_cost.py`,
`tests/test_phase24_correction.py` and the word `premise`. Plus
`test_rpt02_is_on_phase_25s_requirements_line`, slicing the ROADMAP between the Phase 25 and Phase 26
headings before reading the line — never a whole-file search, which would match Phase 20's line.

**(e) The two guard files this one joins actually exist and still route through `normalized`.**
`test_the_register_is_three_files_wide` — resolve **by AST**: parse
`tests/test_phase23_cost.py` and `tests/test_phase24_correction.py`, and assert each contains a call
whose resolved name is `normalized`. Mechanic (4): grepping for the name in those files would match
their own docstrings, which discuss it.

**(f) The wrong mechanism is proved absent.** `test_no_continuation_was_written_by_append_addendum` —
AST over this test file and a literal scan of both planning documents for `append_addendum`, asserting
zero occurrences. Record the measured reason in the docstring: run live against a copy of
`REQUIREMENTS.md`, `append_addendum` **succeeded** on an accidental `PENDING` substring, and
`_verdict.recorded_verdict` returns `None` for planning markdown, making its verdict guard vacuous
there.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase25_correction.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_phase25_correction.py -v` exits 0 with **zero skipped**; quote the `N passed, M skipped` line
    - `.venv/bin/python -m pytest tests/test_phase25_correction.py -k sentinel -v` collects **7** cases (one per tag) and all pass
    - The SUMMARY quotes one deliberately-broken run: temporarily duplicate one BEGIN sentinel on a **copy** of the file in `tmp_path`, point the counter at it, and quote the resulting failure — the watched RED for mechanic (3)
    - `.venv/bin/python -c "import ast; t=ast.parse(open('tests/test_phase25_correction.py').read()); ns=sorted(f.name for f in ast.walk(t) if isinstance(f,ast.FunctionDef) and f.name.startswith('test_')); print(ns); assert len(ns)>=6"` exits 0
    - `grep -v '^\s*#' tests/test_phase25_correction.py | grep -c 'grep -c'` returns `0` — the file does not use the counting form its own mechanic (3) forbids
    - `.venv/bin/python -m pytest tests/test_phase23_cost.py tests/test_phase24_correction.py -q` reports 0 failed — the two earlier sweeps are untouched
    - `.venv/bin/python -m pytest tests/ -q` reports 0 failed; record the `N passed, M skipped` line and state the delta against the 1647/1 baseline
    - `git diff --exit-code -- scripts/mitigation_gate.py scripts/mitigation_accountant.py scripts/mitigation_unit.py scripts/phase18_extraction.py pyproject.toml` exits 0; `make lint` exits 0
  </acceptance_criteria>
  <done>Seven sentinel pairs are proved unique and ordered, six superseded originals are proved still standing with their continuations found from the claim's own index, and the register this file joins is proved three files wide by AST.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a superseded claim → the record of what was believed | deleting it erases the only trace the belief was held; every correction must be additive and the deletion count checkable |
| a correction mechanism → the document it corrects | `append_addendum` was measured splicing on an accidental substring in `REQUIREMENTS.md` with a vacuous verdict guard |
| a line-wrapped claim → a textual guard | a bare `in` check reports a false absence on a wrapped phrase; every match routes through `_prose.normalized` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-32 | Tampering | a correction spliced onto an accidental `PENDING` substring by `append_addendum`, with a vacuous verdict guard | mitigate | the sentinel + `_prose.normalized` mechanism from plans 23-12 and 24-03 is used instead; `append_addendum` is asserted absent from both planning files and from the guard |
| T-25-33 | Repudiation | a superseded claim quietly deleted, erasing the record of what was believed | mitigate | `git diff --numstat` deletion count asserted `0` on both files; six originals asserted still present through `normalized` |
| T-25-34 | Spoofing | two BEGIN sentinels on one line satisfying a `grep -c ... = 1` check while the second span goes unguarded | mitigate | mechanic (3): sentinels counted with `str.count`; a duplicated-sentinel RED is watched on a `tmp_path` copy |
| T-25-35 | Information Disclosure | SC1 published against a comparator that belongs to a different recipe (v2.0's 0.4921) | mitigate | the continuation names the matched control `0.7837301587301587`, its floor, the third distinct fact-aligned reading `0.5615079365079365`, and the four recipe deltas |
| T-25-36 | Tampering | `gsd-sdk` mutation handlers corrupting planning frontmatter | mitigate | hand-edit only; snapshot before and diff all touched files after; the SUMMARY records zero handler calls |
| T-25-SC | Tampering | npm/pip/cargo installs | accept | Zero installs; `pyproject.toml` byte-unchanged (RPT-03) |
</threat_model>

<verification>
```bash
.venv/bin/python -m pytest tests/test_phase25_correction.py -v
.venv/bin/python -m pytest tests/test_phase23_cost.py tests/test_phase24_correction.py -q
git diff --numstat -- .planning/ROADMAP.md .planning/REQUIREMENTS.md   # deletion column must be 0
.venv/bin/python -m pytest tests/ -q                                    # record "N passed, M skipped"
make lint
```
</verification>

<success_criteria>
- Seven sentinel-bounded dated continuations exist, each with exactly one BEGIN and one END, ordered
- Six superseded originals survive byte-unchanged, each located from its own index through `_prose.normalized`
- Both planning-file diffs are insertions-only (deletion count 0) and no `gsd-sdk` mutation handler was called
- RPT-02 is on Phase 25's Requirements line and its traceability row names 23-12, 24-03 and both guard files
- `append_addendum` appears nowhere in either planning document
- The duplicated-sentinel RED has been watched on a `tmp_path` copy and quoted
</success_criteria>

<output>
Create `.planning/phases/25-frontier-sweep-and-the-existence-gate-verdict/25-07-SUMMARY.md` when done.
</output>
