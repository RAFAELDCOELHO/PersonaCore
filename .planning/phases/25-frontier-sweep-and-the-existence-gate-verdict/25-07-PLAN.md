---
phase: 25-frontier-sweep-and-the-existence-gate-verdict
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/25-frontier-sweep-and-the-existence-gate-verdict/25-CONTEXT.md
  - tests/test_phase25_correction.py
autonomous: true
requirements: [RPT-02, CTRL-02, FRONT-01]

must_haves:
  truths:
    - "§C3 CORRECTED: `_addendum.append_addendum` is the WRONG mechanism for planning-doc continuations. It has ZERO call sites against `.planning/*.md`; its 'refuses a second append' is a placeholder-COUNT rule, not a once-only lock; run live on a copy of REQUIREMENTS.md it SUCCEEDED on an accidental 'PENDING' substring; and `_verdict.recorded_verdict` returns None for planning markdown, so its verdict-preservation `_prove` is VACUOUS there."
    - "What actually shipped TWICE for planning-doc prose is sentinel-bounded dated blocks plus `scripts/_prose.normalized` guards — plans 23-12 and 24-03. Copy `tests/test_phase24_correction.py`'s four mechanics."
    - "D-02: SC1's binding comparator is superseded by a dated continuation to the Phase 23 MATCHED control (0.7837301587301587, floor MATCHED_CONTROL_NOISE_FLOOR = 0.0267857142857143) — the only comparator differing from a real DP point by exactly the two DP parameters. v2.0's 0.4921 / 0.3483 is retained as a DISCLOSED HISTORICAL REFERENCE with its four recipe deltas named. The original SC1 text stays visible, superseded, never deleted."
    - "D-19: FRONT-01's 'swept to the never-taught floor' is corrected to a DP-ARM property. The adversarial axis terminates at its POOL CEILING (1.9090909090909092 = the largest ratio at which the whole trained pool is used exactly once) — a NAMED STRUCTURAL ASYMMETRY, not a deficit. Both arms read against the SAME already-measured never-taught floor."
    - "CTRL-02's `clip_norm=inf` wording gets its own dated continuation: DPSGD's constructor REFUSES math.inf with [dp-refusal:clip-domain]. C = infinity is represented as a finite bound PROVEN not to bind (1e6 with clip_bind_count 0)."
    - "D-31: SC4's `results/phase2X_frontier.json` is a PLACEHOLDER, not a filename. The artifact is `results/phase25_frontier.json`."
    - "§C4 CORRECTED: RPT-02's second half is ALREADY DISCHARGED TWICE (plans 23-12 and 24-03). The RPT-02 checkbox already reads `[x]` while its traceability row still says DEFERRED — the two disagree at HEAD. D-38 stands but is a STALE-ROW REPAIR, not a mechanism build."
    - "D-38: RPT-02 is ADDED to ROADMAP Phase 25's Requirements line, making it a two-phase requirement — exactly the ADVT-01 repair already recorded under REQUIREMENTS Traceability, which is the template to copy."
    - "D-51: D-35 is SUPERSEDED BY DATED CONTINUATION, original left standing. Its epsilon-scoping claim is CORRECT and UNCHANGED — `mitigation_point_verdict` genuinely contains zero `epsilon` / `accounting` occurrences and `capacity_comparison` remains the sole epsilon-dependent function. What is superseded is ONLY its closing 'Nothing to fix': the 21 kwargs are indeed 'counts, recalls, perplexities and floors', and THE PERPLEXITIES HAD NO PRODUCER. Seven of the 21 kwargs had zero producers across all 20 plans; six are condition (c)."
    - "D-51's routing is the SAME sentinel + `scripts/_prose.normalized` mechanism as the other continuations, NEVER `_addendum` — measured to have zero call sites against `.planning/*.md` and to have SUCCEEDED on an accidental 'PENDING' substring. It lands in `25-CONTEXT.md` immediately after D-35, whose text stays byte-unchanged, and it COUNTS TOWARD RPT-02's DISCHARGE — making this phase's tally four instances, not three."
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "SC1's dated continuation (D-02), SC4's artifact-path continuation (D-31), RPT-02 on Phase 25's Requirements line (D-38)"
      contains: "25-07-CONTINUATION-SC1-COMPARATOR-BEGIN"
    - path: ".planning/REQUIREMENTS.md"
      provides: "FRONT-01's scope continuation (D-19), CTRL-02's clip-domain continuation, and the RPT-02 traceability row repaired additively"
      contains: "25-07-CONTINUATION-FRONT01-SCOPE-BEGIN"
    - path: ".planning/phases/25-frontier-sweep-and-the-existence-gate-verdict/25-CONTEXT.md"
      provides: "D-51's dated continuation superseding D-35's closing 'Nothing to fix', with D-35's original text left standing"
      contains: "25-07-CONTINUATION-D35-CONDITION-C-BEGIN"
    - path: "tests/test_phase25_correction.py"
      provides: "the continuations guarded through _prose.normalized, each with the original claim asserted still standing"
      contains: "def test_every_original_claim_survives_beside_its_continuation"
  key_links:
    - from: "tests/test_phase25_correction.py"
      to: "scripts/_prose.py::normalized"
      via: "every claim and marker matched through normalized(), because the claims line-wrap in the planning docs"
      pattern: "normalized"
    - from: ".planning/REQUIREMENTS.md RPT-02 traceability row"
      to: "plans 23-12 and 24-03"
      via: "the two already-shipped correction sweeps recorded additively as the discharging evidence"
      pattern: "23-12"
---

<objective>
Ship the dated continuations this phase owes and repair RPT-02's stale traceability row — using the
mechanism that has actually shipped twice in this repository, not the one CONTEXT names.

Purpose: `_addendum.append_addendum` was measured this session against copies of the real planning
docs. It **succeeded on an accidental "PENDING" substring**, its "refuses a second append" is a
placeholder-count rule rather than a lock, and its verdict-preservation proof compares `None == None`
on planning markdown — vacuous. It has zero call sites against `.planning/*.md`. What did ship, twice,
is a sentinel-bounded dated block plus a `_prose.normalized` guard module: plan 23-12 into
`ROADMAP.md` / `REQUIREMENTS.md` / `STATE.md`, and plan 24-03 into `ROADMAP.md`.

**That is also RPT-02's discharge.** The requirement is a conjunction — the helper must exist AND be
used for correction sweeps — and both halves have been met twice already. The RPT-02 checkbox is `[x]`
while its traceability row still says DEFERRED. This plan makes the row agree with the checkbox,
additively, and adds Phase 25's **two** instances — the ROADMAP/REQUIREMENTS sweep and the
25-CONTEXT sweep — bringing the register to **four** (D-51's counting).

Output: **eight** sentinel-bounded continuations across **three** planning documents (five in Task 1 —
`SC1-COMPARATOR`, `SC4-ARTIFACT-PATH`, `FRONT01-SCOPE`, `CTRL02-CLIP-DOMAIN` and D-51's
`D35-CONDITION-C`; three in Task 2 — `RPT02-SPAN`, `RPT02-ROW`, `RPT02-DUPLICATE-COUNT`), one
Requirements-line addition, and `tests/test_phase25_correction.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-frontier-sweep-and-the-existence-gate-verdict/25-CONTEXT.md
@.planning/phases/25-frontier-sweep-and-the-existence-gate-verdict/25-RESEARCH.md
@.planning/phases/25-frontier-sweep-and-the-existence-gate-verdict/25-VALIDATION.md
</context>

<environment>
**Python 3.11 venv is MANDATORY.** `.venv/bin/python -m pytest ...`. Baseline **1647 passed, 1 skipped**.

**ADDITIVE ONLY.** No original claim may be deleted, reworded, reflowed or moved. Every correction is a
dated block appended after the claim it supersedes, and the original stays visible as the record of
what was believed.

**The deletion budget is a literal, not a zero.** `.planning/REQUIREMENTS.md`'s and
`25-CONTEXT.md`'s deletion columns must both be **0**. `.planning/ROADMAP.md`'s deletion column must be **exactly 1**, and that one deleted line must
be proved to be the pre-edit `**Requirements**` line — because Task 2 appends `, RPT-02` to an existing
line, and git records a line modification as one deletion plus one insertion. **Measured at HEAD:**
appending `, RPT-02` to `**Requirements**: CTRL-01, CTRL-02, FRONT-01, FRONT-02, FRONT-03, FRONT-04,
ADVT-01` produces `1	1	.planning/ROADMAP.md`. A `deletions == 0` gate over that file is unsatisfiable,
so the discipline is enforced by *identifying* the deleted line, not by forbidding it. Snapshot both
files to a scratch copy immediately before the edit and diff against the snapshot with
`git diff --no-index -U0` — the snapshot is the comparator, so a prior uncommitted task's insertions
cannot contaminate the reading.

**`gsd-sdk` mutation handlers must NOT be called** on `ROADMAP.md`, `REQUIREMENTS.md` or `STATE.md` —
they have corrupted planning frontmatter repeatedly. Hand-edit, snapshot before, diff after.

**Do NOT use `scripts/_addendum.py` in this plan.** Measured unsuitable for `.planning/*.md`.

**The four ancestry-guarded modules must stay byte-unchanged.**
</environment>

<tasks>

<task type="auto">
  <name>Task 1: Five dated continuations — SC1's comparator, SC4's artifact path, FRONT-01's scope, CTRL-02's clip domain, and D-51's supersession of D-35</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/25-frontier-sweep-and-the-existence-gate-verdict/25-CONTEXT.md</files>
  <read_first>
    - `.planning/ROADMAP.md` — locate **by content, never by line number**: plan 23-12's
      `23-12-CONTINUATION-BEGIN` / `-END` pair and plan 24-03's `24-03-CONTINUATION-BEGIN` / `-END`
      pair. Those are the templates. Also locate §Phase 25's `**Requirements**` line, its five Success
      Criteria, SC1's `defensible neighbourhood of v2.0's 0.4921 / 0.3483` clause, and SC4's
      `results/phase2X_frontier.json` placeholder
    - `.planning/REQUIREMENTS.md` — the 23-12 sentinel pairs; the FRONT-01 body containing
      `swept to the never-taught floor and to σ→0`; the CTRL-02 body containing
      `clip_norm=inf, noise_multiplier=0`
    - `.planning/phases/25-frontier-sweep-and-the-existence-gate-verdict/25-CONTEXT.md` — locate
      **by content**: D-35's paragraph under §Area 4, ending in the sentence `**Nothing to fix.**`;
      and §Area 7's D-51 (`D-35 is SUPERSEDED BY DATED CONTINUATION`), which is the text this
      continuation carries. The file already carries **TWO** additive amendments and together they are
      the register — read both:
      (i) the `> **AMENDED 2026-08-31 (condition-(c) reopening).**` blockquote near the top, which
          restates the decision count additively; it is prose, with **no sentinels**;
      (ii) the `<!-- 25-D48-CONTINUATION-BEGIN -->` / `<!-- 25-D48-CONTINUATION-END -->` pair in
          §Area 7, wrapping the `**SUPERSEDED IN PLACE 2026-08-31 (Phase-25 planning).**` block that
          corrects D-48's floor citation. **(ii) is the better in-file template for (e)** because it is
          a *sentinel pair* — the exact shape (e) must produce — while (i) is only a dated blockquote.
          Copy (ii)'s shape: HTML-comment sentinels, the dated `SUPERSEDED IN PLACE` opener, the
          original left standing above it, and an explicit statement of which counters the block does
          and does not sit on. That pair is written **during planning, by no plan**, so it is neither
          one of the eight continuations this plan writes nor an RPT-02 register instance; Task 3(a)
          picks it up as a **ninth guarded case** only
    - `scripts/_prose.py` — `normalized(text)` returns `" ".join(text.split())`; the module imports
      nothing
    - `results/phase23_matched_control.json`, `results/phase23_matched_verdict.json` — D-02's
      substituted comparator: `0.7837301587301587`, floor `0.0267857142857143`, `deviation 0.0`
    - `results/phase23_control_floor.json` — the unmatched fact-aligned control `0.5615079365079365`,
      floor `0.05357142857142849`, evidence that v2.0's 0.4921 belongs to a different recipe
    - `scripts/mitigation_budget.py` — `ADVERSARIAL_RATIO_GRID`'s upper extreme
      `1.9090909090909092` and its `_PROVENANCE` `governs` string, for D-19's pool-ceiling claim
    - `src/personacore/privacy/dpsgd.py` — the **executable** constructor domain checks (resolve by
      content: the `clip_norm` finite/positive validation raising `[dp-refusal:clip-domain]`) and the
      single noise draw site computing `std` as `sigma * C`. **The `:52-80` range CONTEXT cites is
      module docstring, not code** — resolve by symbol and cite by symbol
  </read_first>
  <action>
Write five sentinel-bounded dated continuations. Each uses its own unique pair
`25-07-CONTINUATION-<TAG>-BEGIN` / `25-07-CONTINUATION-<TAG>-END` inside HTML comment markers, appears
**immediately after** the claim it supersedes, carries the date, and leaves that claim byte-unchanged.
Do not reflow the surrounding paragraph — a reflow makes the diff unreviewable and the original
unquotable.

**(a) `ROADMAP.md` §Phase 25 SC1 — tag `SC1-COMPARATOR` (D-02).** SC1 binds the control's recall to a
defensible neighbourhood of v2.0's `0.4921 / 0.3483`. State:
  - the binding comparator becomes the **Phase 23 MATCHED control**: `0.7837301587301587` with floor
    `MATCHED_CONTROL_NOISE_FLOOR = 0.0267857142857143`, cited to `results/phase23_matched_control.json`
    and `results/phase23_matched_verdict.json`
  - the reason: it is the only comparator differing from a real DP point by **exactly the two DP
    parameters** — a property v2.0's 0.4921 never had
  - the four named recipe deltas making v2.0 non-binding: held-out denominator **936 → 648**, 8×
    teaching-token exposure, replay-in-lot, fact-aligned bins
  - the fact-aligned unmatched control reads `0.5615079365079365`, a third distinct number — quoted so
    a reader can see the spread rather than take the substitution on trust
  - v2.0's 0.4921 / 0.3483 is **retained as a disclosed historical reference**, not deleted
  - SC1 stays an **ACTIVE** stop rule; only its comparator moved

**(b) `ROADMAP.md` §Phase 25 SC4 — tag `SC4-ARTIFACT-PATH` (D-31).** State that
`results/phase2X_frontier.json` was a **placeholder, not a filename**, that the artifact this phase
emits is `results/phase25_frontier.json`, and that every plan, test and figure in this phase resolves
the path from `scripts/phase25_record.py`'s module constant rather than from planning prose. Leave the
placeholder text standing.

**(c) `REQUIREMENTS.md` FRONT-01 — tag `FRONT01-SCOPE` (D-19).** State:
  - *"swept to the never-taught floor"* is a **DP-arm property**. The DP axis has a physical mechanism
    driving utility toward zero under high noise; the adversarial axis terminates at its **pool
    ceiling** by construction — `1.9090909090909092`, the largest ratio at which the whole trained
    pool is used exactly once
  - this is a **NAMED STRUCTURAL ASYMMETRY, not a deficit**
  - **both arms are read against the SAME already-measured never-taught floor** — 0/416 at 5 seeds,
    `results/phase23_never_taught.json` — as the plane's shared lower-left reference, so no adversarial
    point needs to reach it personally
  - the original wording stays standing and the σ→0 half of the sentence is unaffected

**(d) `REQUIREMENTS.md` CTRL-02 — tag `CTRL02-CLIP-DOMAIN`.** State:
  - `clip_norm=inf` is **REFUSED by the code**: the DPSGD constructor requires finite and positive and
    raises `[dp-refusal:clip-domain]` on `math.inf`, and equally on `0.0`, `-1.0` and `nan`. The check
    runs in the pre-pass **before** the model audit, so even an empty module triggers it
  - the forcing mechanism: the noise standard deviation is `sigma * C` at a **single** draw site with
    **no `sigma == 0` branch**, so `0.0 * inf` is `nan` and the draw raises
    `RuntimeError: normal expects std >= 0.0, but found std nan`
  - therefore **C = ∞ is represented as a finite bound PROVEN not to bind** — Phase 23 used
    `clip_norm = 1000000.0` with `clip_bind_count = 0`, and Phase 25's control reuses that same `1e6`
    so D-01's bit-level reproduction stays reachable
  - the citation correction: the constructor checks and the draw site are resolved **by symbol**; the
    line range CONTEXT cites is module docstring

**(e) `25-CONTEXT.md` §Area 4's D-35 — tag `D35-CONDITION-C` (D-51).** Append the continuation
**immediately after** D-35's paragraph, leaving `**Nothing to fix.**` byte-unchanged. State:
  - D-35's **epsilon-scoping claim is correct and unchanged**: `mitigation_point_verdict` genuinely
    contains **zero** `epsilon` and `accounting` occurrences, and `capacity_comparison` remains the
    sole epsilon-dependent function, already scoped by D-23
  - what is superseded is **only** its closing `Nothing to fix`. The 21 kwargs are indeed *"counts,
    recalls, perplexities and floors"* — and **the perplexities had no producer**. Seven of the 21 had
    zero producers across all 20 committed plans; six of the seven are condition (c):
    `point_dialogue_ppl_on`, `point_dialogue_ppl_off`, `control_gap`, `gap_noise_floor`,
    `point_retention_ppl`, `retention_noise_floor`, and the seventh is `zero_extraction_has_nll`
  - no Phase 23 point record carries `dialogue_ppl`, `retention_ppl`, `ppl_on` or `ppl_off`, so the
    verdict was **uncomputable** rather than merely unwritten
  - the producers are plans **25-21** (the six) and **25-22** (the seventh); D-45 through D-50 are the
    decisions they implement
  - the correction is **additive**: D-35's original text stays standing as the record of what was
    believed, and this continuation is routed through the sentinel + `_prose.normalized` mechanism,
    **never `_addendum`** — which has zero call sites against `.planning/*.md` and was measured
    succeeding on an accidental `PENDING` substring

**WORDING CONSTRAINT on (e), and on every span this task writes.** Task 3(f) forbids the token
`append_addendum` **inside any of this plan's eight spans** and separately asserts the whole-document
total across the three documents stays exactly **2**. The prose above is safe by exactly one word:
write `_addendum`, the module name, and **never** the function name `append_addendum`. Writing the
natural phrase *"never written by `append_addendum`"* into a span reddens the span-scoped check **and**
inflates the whole-document total to 3 — two checks from one sentence. The rule is mechanical: the
26-character token `append_addendum` must not appear anywhere inside a sentinel-bounded span written by
this plan. `_addendum` is the form to write, in (e) and everywhere else
  - this is the phase's **fourth** dated continuation in CONTEXT's own counting and **counts toward
    RPT-02's discharge**

Hand-edit all three files. Snapshot all three before the edit; `git diff --numstat` after and require a
deletion count of `0` on each.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import pathlib,sys;sys.path.insert(0,'scripts');from _prose import normalized;C='.planning/phases/25-frontier-sweep-and-the-existence-gate-verdict/25-CONTEXT.md';SPEC=(('.planning/ROADMAP.md','SC1-COMPARATOR','0.0267857142857143'),('.planning/ROADMAP.md','SC4-ARTIFACT-PATH','results/phase25_frontier.json'),('.planning/REQUIREMENTS.md','FRONT01-SCOPE','1.9090909090909092'),('.planning/REQUIREMENTS.md','CTRL02-CLIP-DOMAIN','[dp-refusal:clip-domain]'),(C,'D35-CONDITION-C','zero_extraction_has_nll'));T={f:pathlib.Path(f).read_text() for f,_,_ in SPEC};B=lambda g:'<!-- 25-07-CONTINUATION-'+g+'-BEGIN -->';E=lambda g:'<!-- 25-07-CONTINUATION-'+g+'-END -->';bad=[(f,g) for f,g,v in SPEC if not (T[f].count(B(g))==1 and T[f].count(E(g))==1 and T[f].index(E(g))>T[f].index(B(g)))];assert not bad, ('SENTINEL',bad);miss=[(f,g,v) for f,g,v in SPEC if normalized(v) not in normalized(T[f][T[f].index(B(g)):T[f].index(E(g))])];assert not miss, ('VALUE MISSING FROM SPAN',miss);ORIG=(('.planning/ROADMAP.md',chr(34)+'defensible neighbourhood of v2.0'+chr(39)+'s 0.4921 / 0.3483'+chr(34)),('.planning/ROADMAP.md','results/phase2X_frontier.json'),('.planning/REQUIREMENTS.md','swept to the never-taught floor'),('.planning/REQUIREMENTS.md','clip_norm=inf, noise_multiplier=0'),(C,'Nothing to fix'));gone=[(f,c) for f,c in ORIG if normalized(c.strip(chr(34))) not in normalized(T[f])];assert not gone, ('ORIGINAL DELETED',gone);print('5 sentinel pairs unique and ordered; 5 measured values inside their own spans; 5 originals still standing')"</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --numstat -- .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/phases/25-frontier-sweep-and-the-existence-gate-verdict/25-CONTEXT.md` shows a deletion count of **0** for all three files — insertions only
    - `.venv/bin/python -c "import pathlib; t=pathlib.Path('.planning/ROADMAP.md').read_text(); print([(g, t.count('25-07-CONTINUATION-'+g+'-BEGIN'), t.count('25-07-CONTINUATION-'+g+'-END')) for g in ('SC1-COMPARATOR','SC4-ARTIFACT-PATH')])"` prints `[('SC1-COMPARATOR', 1, 1), ('SC4-ARTIFACT-PATH', 1, 1)]` — counted with `str.count`, **never `grep -c`**, which counts lines
    - `.venv/bin/python -c "import pathlib; t=pathlib.Path('.planning/REQUIREMENTS.md').read_text(); print([(g, t.count('25-07-CONTINUATION-'+g+'-BEGIN'), t.count('25-07-CONTINUATION-'+g+'-END')) for g in ('FRONT01-SCOPE','CTRL02-CLIP-DOMAIN')])"` prints both tuples as `1, 1`
    - `.venv/bin/python -c "import pathlib; t=pathlib.Path('.planning/phases/25-frontier-sweep-and-the-existence-gate-verdict/25-CONTEXT.md').read_text(); print([(t.count('25-07-CONTINUATION-D35-CONDITION-C-'+e)) for e in ('BEGIN','END')])"` prints `[1, 1]` — counted with `str.count`, **never `grep -c`**, which counts lines
    - **NEW-3 CLOSED — this task's verification is self-contained and never runs `pytest` against a file a LATER task in the same plan creates.** `tests/test_phase25_correction.py` does not exist until **Task 3**, so `pytest tests/test_phase25_correction.py` exits **4** (collection error) here and gives the task no working verification at all. Every criterion in this task is an inline `python -c` derived from this task's own criteria. The `-k originals` and `-k measured_values` checks that used to sit here now live in Task 3, where their file exists
    - The Task 1 `<verify>` command above is **assertive, not a bare `print`**: it asserts five unique ordered sentinel pairs, five measured values sliced to their **own spans** (a value appearing elsewhere in the document would otherwise satisfy it), and five superseded originals still present through `_prose.normalized`. **Executed at plan time against a simulated post-Task-1 tree it printed `5 sentinel pairs unique and ordered; 5 measured values inside their own spans; 5 originals still standing`, and shortening `1.9090909090909092` to `1.909` reddened it with `AssertionError: ('VALUE MISSING FROM SPAN', [('.planning/REQUIREMENTS.md', 'FRONT01-SCOPE', '1.9090909090909092')])`**
    - **B1 CLOSED — the `append_addendum` check is SPAN-SCOPED, never whole-document.** `.venv/bin/python -c "import pathlib;C='.planning/phases/25-frontier-sweep-and-the-existence-gate-verdict/25-CONTEXT.md';SPEC=(('.planning/ROADMAP.md','SC1-COMPARATOR'),('.planning/ROADMAP.md','SC4-ARTIFACT-PATH'),('.planning/REQUIREMENTS.md','FRONT01-SCOPE'),('.planning/REQUIREMENTS.md','CTRL02-CLIP-DOMAIN'),(C,'D35-CONDITION-C'));T={f:pathlib.Path(f).read_text() for f,_ in SPEC};bad=[(f,g) for f,g in SPEC if 'append_addendum' in T[f][T[f].index('<!-- 25-07-CONTINUATION-'+g+'-BEGIN -->'):T[f].index('<!-- 25-07-CONTINUATION-'+g+'-END -->')]];assert not bad, bad;print('0 of 5 Task-1 spans mention append_addendum;', sum(v.count('append_addendum') for v in T.values()), 'pre-existing whole-document occurrence(s), out of scope')"` exits 0 and reports **2** pre-existing occurrences. **MEASURED AT HEAD: `append_addendum` is ALREADY PRESENT in two of the three documents** — in `.planning/ROADMAP.md`, in Phase 20's plan-list bullet for `20-16-PLAN.md` (the `scripts/_addendum.py::append_addendum` clause), and in `25-CONTEXT.md`, in the `### Canonical References` bullet beginning `` `scripts/_addendum.py` — `append_addendum(path, addendum, *, pending, recorded)`, the ONLY legal ``; `.planning/REQUIREMENTS.md` has **0**. **Both are cited by CONTENT, never by line number** — this plan's own doctrine, and the CONTEXT occurrence has already moved twice under amendments written above it. A whole-document `grep -rn "append_addendum" ... returns nothing` criterion therefore goes RED for a reason unrelated to the property it checks — defect shape 5, which this plan's own doctrine forbids elsewhere. The property is *"no continuation THIS PLAN writes was written by `append_addendum`"*, so the check is sliced to this plan's own spans and the pre-existing count is asserted **unchanged**, never absent
    - The SUMMARY records that **zero `gsd-sdk` mutation handlers were called** and quotes the two `git diff --numstat` lines
  </acceptance_criteria>
  <done>Five dated sentinel-bounded continuations sit immediately after the claims they supersede, every original survives byte-unchanged through `_prose.normalized`, all three diffs are insertions-only, and this task's verification asserts rather than prints and depends on no file a later task creates.</done>
</task>

<task type="auto">
  <name>Task 2: D-38 — RPT-02 onto Phase 25's Requirements line, and its stale traceability row repaired additively</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/ROADMAP.md` — §Phase 25's `**Requirements**` line, currently
      `CTRL-01, CTRL-02, FRONT-01, FRONT-02, FRONT-03, FRONT-04, ADVT-01`; and §Phase 20's
      `**Requirements**` line, which **already carries RPT-02** — that is why adding it here makes
      RPT-02 a two-phase requirement rather than a move
    - `.planning/REQUIREMENTS.md` — the `- [x] **RPT-02**` checkbox in the RPT section; the RPT-02
      **traceability row** beginning `DEFERRED to Phase 25 — first half shipped, second half is the
      unmet conjunction`; and the `AMENDED 2026-08-30 (phase 24 close)` block recording ADVT-01's
      two-phase span additively. **That block is the template**
    - `tests/test_phase23_cost.py` — the 23-12 correction sweep, resolved by content: the test
      asserting the original claim survives beside a dated marker in all three planning files, matched
      through `_prose.normalized`
    - `tests/test_phase24_correction.py` — the 24-03 correction sweep, the second discharging instance
    - `scripts/_prose.py` — the helper both sweeps route through
  </read_first>
  <action>
**(a) The ROADMAP Requirements line.** Append `, RPT-02` to §Phase 25's `**Requirements**` line.
Immediately after that line add a sentinel-bounded dated note, tag `RPT02-SPAN`, recording that RPT-02
now appears on **two** phase Requirements lines (Phase 20 and Phase 25) and pointing at the
REQUIREMENTS amendment for the reason — the same shape the ADVT-01 repair used.

**(b) The traceability row, repaired ADDITIVELY.** Do **not** rewrite the DEFERRED row. Append a dated
continuation to it, tag `RPT02-ROW`, recording the three measurements that supersede its premise:
  - the RPT-02 checkbox already reads `- [x]` while the row says DEFERRED — **the two disagreed at
    HEAD**; the row was written at Phase-20 close and never updated
  - the "second half" — routing doc-consistency checks through `normalized` — **shipped in plan 23-12
    and again in plan 24-03**. Name both plans, name their guard files
    (`tests/test_phase23_cost.py`'s correction sweep and `tests/test_phase24_correction.py`), and state
    that both are correction sweeps over `.planning/*.md` through `scripts/_prose.normalized`
  - Phase 25 adds the **third and fourth** instances — D-51's own counting, applied here and in
    plan 25-20 without variation: instance **three** is this plan's ROADMAP/REQUIREMENTS sweep and
    instance **four** is its 25-CONTEXT sweep (D-51's `D35-CONDITION-C`), together eight
    continuations across three planning documents guarded by `tests/test_phase25_correction.py`.
    D-51 states the supersession counts toward this discharge, so the tally is **four instances in
    three guard files**, never three
State the disposition in one sentence: the row's *premise* — that no phase can currently tick RPT-02 —
is **false**; D-38's *decision* stands, and what it repairs is a **stale row**, not a missing
mechanism. Follow the additive doctrine the ADVT-01 amendment established: the dated 2026-08-20
"48/48 mapped, 0 orphans, 0 duplicates" line is never edited, only superseded in place.

**(c) The duplicate-count statement.** Beside the existing ADVT-01 amendment, append a dated note
(tag `RPT02-DUPLICATE-COUNT`) recording RPT-02 as the **second** ID appearing on two phase lines, so the
count statement stays honest without the original being touched.

Hand-edit. No `gsd-sdk` mutation handler. Snapshot both files to a scratch copy **before** the edit.
After the edit, diff against the snapshot: `.planning/REQUIREMENTS.md` must show **0** deletions, and
`.planning/ROADMAP.md` must show **exactly 1** — the pre-edit `**Requirements**` line, resolved from
the snapshot by content and compared, never retyped. That is the whole additive discipline for (a):
the line change is permitted, unlogged line changes are not.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import pathlib; t=pathlib.Path('.planning/ROADMAP.md').read_text(); s=t[t.index('### Phase 25'):t.index('### Phase 26')]; line=[l for l in s.splitlines() if l.startswith('**Requirements**')][0]; print(line); assert 'RPT-02' in line"</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -c "import pathlib; t=pathlib.Path('.planning/ROADMAP.md').read_text(); s=t[t.index('### Phase 25'):t.index('### Phase 26')]; line=[l for l in s.splitlines() if l.startswith('**Requirements**')][0]; print(line); assert 'RPT-02' in line"` prints the line and exits 0
    - `.venv/bin/python -c "import pathlib; t=pathlib.Path('.planning/REQUIREMENTS.md').read_text(); print([(g, t.count('25-07-CONTINUATION-'+g+'-BEGIN')) for g in ('RPT02-ROW','RPT02-DUPLICATE-COUNT')])"` prints both counts as `1`
    - `.venv/bin/python -m pytest tests/test_phase25_correction.py -k rpt02 -x` passes — the row's continuation names `23-12`, `24-03`, both guard files, and the word `premise`
    - Snapshot FIRST, before any edit in this plan: `cp .planning/ROADMAP.md /tmp/25-07-roadmap.pre && cp .planning/REQUIREMENTS.md /tmp/25-07-requirements.pre`. Then `.venv/bin/python -c "import subprocess,pathlib;S='/tmp/25-07-roadmap.pre';pre=pathlib.Path(S).read_text();s=pre[pre.index('### Phase 25'):pre.index('### Phase 26')];want=[l for l in s.splitlines() if l.startswith('**Requirements')][0];out=subprocess.run(['git','diff','--no-index','-U0','--',S,'.planning/ROADMAP.md'],capture_output=True,text=True).stdout;dl=[l[1:] for l in out.splitlines() if l.startswith('-') and not l.startswith('---')];assert dl==[want], (dl, want);print('exactly 1 deleted line and it IS the pre-edit Requirements line:', want)"` exits 0. **This exact command was executed at plan time against a simulated `, RPT-02` append and printed: `exactly 1 deleted line and it IS the pre-edit Requirements line: **Requirements**: CTRL-01, CTRL-02, FRONT-01, FRONT-02, FRONT-03, FRONT-04, ADVT-01`**
    - `git diff --no-index --numstat -- /tmp/25-07-requirements.pre .planning/REQUIREMENTS.md` shows a deletion count of **0** — REQUIREMENTS.md is insertions-only; only the ROADMAP `**Requirements**` line may change, and only in the way asserted above
    - `.venv/bin/python -c "import pathlib,sys; sys.path.insert(0,'scripts'); from _prose import normalized; q=normalized(pathlib.Path('.planning/REQUIREMENTS.md').read_text()); assert normalized('DEFERRED to Phase 25') in q; assert normalized('0 orphans, 0 duplicates') in q; print('both superseded originals survive')"` exits 0
    - `.venv/bin/python -m pytest tests/ -q` reports 0 failed; record the `N passed, M skipped` line
  </acceptance_criteria>
  <done>Phase 25's Requirements line carries RPT-02, the traceability row's false premise is superseded in place naming both already-discharging plans, and every original line — including the dated duplicate-count statement — survives.</done>
</task>

<task type="auto">
  <name>Task 3: `tests/test_phase25_correction.py` — the four mechanics, copied not re-derived</name>
  <files>tests/test_phase25_correction.py</files>
  <read_first>
    - `tests/test_phase24_correction.py` — read its module docstring, which **states the four mechanics
      verbatim**, and its body. Copy them rather than re-deriving:
      (1) match through `scripts/_prose.normalized`;
      (2) search for the marker **from the claim's index**, never from byte 0;
      (3) count sentinels with `str.count`, never `grep -c`, because `grep -c` counts LINES and two
          BEGIN sentinels on one line would satisfy a `grep -c ... = 1` check while the second span
          goes unguarded;
      (4) resolve node ids and identifiers **by AST**, never by grep
    - `tests/test_phase23_cost.py` — the 23-12 sweep, the first instance of the same shape
    - `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md` and
      `.planning/phases/25-frontier-sweep-and-the-existence-gate-verdict/25-CONTEXT.md` — the **eight**
      sentinel tags written in Tasks 1 and 2: `SC1-COMPARATOR`, `SC4-ARTIFACT-PATH`, `FRONT01-SCOPE`,
      `CTRL02-CLIP-DOMAIN`, `D35-CONDITION-C`, `RPT02-SPAN`, `RPT02-ROW`, `RPT02-DUPLICATE-COUNT` —
      each with the document it lives in, since the guard slices per document
    - `25-CONTEXT.md`'s pre-existing `<!-- 25-D48-CONTINUATION-BEGIN -->` / `-END` pair — **the ninth
      guarded case, written during planning by no plan.** Resolve it by content in §Area 7: it wraps
      the `**SUPERSEDED IN PLACE 2026-08-31 (Phase-25 planning).**` block correcting D-48's floor
      citation, and the block itself states *"Plan 25-07 Task 3(a) picks this pair up as a ninth
      guarded case"* — this task is what makes that sentence true. **Its sentinel STEM differs**:
      `25-D48-CONTINUATION`, not `25-07-CONTINUATION-<TAG>`, so the parametrization must carry the
      stem rather than assemble it from a tag
    - `scripts/_prose.py` — `normalized`
  </read_first>
  <action>
Create `tests/test_phase25_correction.py`. CPU-only: stdlib, `pytest`, and the one sibling script.
The module docstring must state that this file is the **third guard file** of the register
`tests/test_phase23_cost.py` and `tests/test_phase24_correction.py` already hold, and that it guards
the register's **third and fourth instances** — the ROADMAP/REQUIREMENTS sweep and the 25-CONTEXT
sweep — **so RPT-02's second half is discharged for the third and fourth times**, under the
requirement's own regulation rather than beside it. Four instances, three guard files: that is
D-51's counting and it is the only one used in this plan or in plan 25-20.

**(a) Sentinel integrity.** `test_each_continuation_is_bounded_by_exactly_one_sentinel_pair` —
parametrized over **NINE** `(document, sentinel_stem)` pairs. For each: `str.count(BEGIN) == 1` and
`str.count(END) == 1` **in that stem's own document**, and the END index is greater than the BEGIN
index. State mechanic (3) in the docstring as the reason `str.count` is used.

Parametrize on the **stem**, not on a tag, because the ninth stem does not follow the
`25-07-CONTINUATION-<TAG>` shape. Build the markers as `f"<!-- {stem}-BEGIN -->"` /
`f"<!-- {stem}-END -->"`. The nine:
  - eight written by this plan — `25-07-CONTINUATION-SC1-COMPARATOR`,
    `25-07-CONTINUATION-SC4-ARTIFACT-PATH`, `25-07-CONTINUATION-FRONT01-SCOPE`,
    `25-07-CONTINUATION-CTRL02-CLIP-DOMAIN`, `25-07-CONTINUATION-D35-CONDITION-C`,
    `25-07-CONTINUATION-RPT02-SPAN`, `25-07-CONTINUATION-RPT02-ROW`,
    `25-07-CONTINUATION-RPT02-DUPLICATE-COUNT`, each in its own document
  - **the ninth: `("25-CONTEXT.md", "25-D48-CONTINUATION")`**, which this plan does **not** write. It
    was written during planning, by no plan, and it shipped **unguarded**: nothing else in the phase
    asserts its span survives, its originals stand, or its sentinels are unique — the exact properties
    RPT-02's mechanism exists to hold. The block's own text says Task 3(a) picks it up; this makes that
    true

**Do NOT renumber the register.** RPT-02's discharge count is **unchanged at four instances in three
guard files** — 23-12, 24-03, and this plan's two sweeps. The ninth pair adds a **guarded case**, not a
register instance, and it is not one of the eight continuations this plan writes. Every "eight" in
Tasks 1 and 2 and in 3(f) stays eight; only 3(a), 3(b) and 3(c) see nine. Say so in the docstring, or
the next reader reconciles the two numbers by changing the wrong one.

**(b) The originals survive.** `test_every_original_claim_survives_beside_its_continuation` —
parametrized over the superseded claims, each matched through `normalized`:
  - `defensible neighbourhood of v2.0's 0.4921 / 0.3483`
  - `results/phase2X_frontier.json`
  - `swept to the never-taught floor`
  - `clip_norm=inf, noise_multiplier=0`
  - `DEFERRED to Phase 25`
  - the dated `0 orphans, 0 duplicates` line
  - `the floor can never be loosened after seeing results` — D-49's closing sentence in
    `25-CONTEXT.md`, standing **immediately above** the `25-D48-CONTINUATION` BEGIN sentinel and
    superseded in place by it. Assert it is still present and that the BEGIN marker appears **after**
    its index — the same mechanic (2) treatment as the eight, applied to the pair this plan inherits
    rather than writes
  - `Nothing to fix` — D-35's closing sentence in `25-CONTEXT.md`, superseded by D-51 and **left
    standing**. Assert it is still present, and assert **positively** that the `D35-CONDITION-C` span
    contains `SUPERSEDED` and names both producer plans. **Never assert its absence**: the
    retract-in-place doctrine keeps the superseded claim visible, and an absence-assertion over a
    claim the same plan requires written is the self-contradictory shape this repository has already
    hit
Assert each is present **and** that its continuation's BEGIN marker appears **after** the claim's own
index — mechanic (2). Searching from byte 0 would assert something different and false-RED-prone.

**(c) The continuations carry their measured values.**
`test_the_continuations_carry_their_measured_values` — assert through `normalized` that the
continuation spans (sliced between their own sentinels, not the whole file) contain
`0.7837301587301587`, `0.0267857142857143`, `0.5615079365079365`, `936`, `648`,
`1.9090909090909092`, `[dp-refusal:clip-domain]`, `1000000.0` and `results/phase25_frontier.json`;
and that the `D35-CONDITION-C` span contains `zero_extraction_has_nll`, `point_dialogue_ppl_on`,
`25-21` and `25-22`. Slicing to the span is load-bearing: a value that happens to appear elsewhere in
the document would otherwise satisfy the assertion.

Add the ninth span to the same test: the `25-D48-CONTINUATION` span must contain `0.068930`,
`results/phase20_retention_floor.json`, `0.005214448168350039` and `SUPERSEDED IN PLACE`. **All four
were verified present at plan time** by the criterion quoted below, so this is a survival guard on an
existing block, not a new authoring obligation — if it ever reddens, the block was edited or truncated.

**(d) RPT-02's repair is evidenced, not asserted.** `test_rpt02_row_names_both_discharging_plans` —
the `RPT02-ROW` span contains `23-12`, `24-03`, `tests/test_phase23_cost.py`,
`tests/test_phase24_correction.py` and the word `premise`. Plus
`test_rpt02_is_on_phase_25s_requirements_line`, slicing the ROADMAP between the Phase 25 and Phase 26
headings before reading the line — never a whole-file search, which would match Phase 20's line.

**(e) The two guard files this one joins actually exist and still route through `normalized`.**
`test_the_register_is_three_files_wide` — resolve **by AST**: parse
`tests/test_phase23_cost.py` and `tests/test_phase24_correction.py`, and assert each contains a call
whose resolved name is `normalized`. Mechanic (4): grepping for the name in those files would match
their own docstrings, which discuss it.

**(f) The wrong mechanism is proved absent FROM THIS PLAN'S OWN SPANS.**
`test_no_continuation_was_written_by_append_addendum` — AST over this test file (no call resolving to
`append_addendum`), plus a literal scan of the **eight continuation spans** written in Tasks 1 and 2,
each sliced between its own sentinel pair, asserting `append_addendum` appears in none of them.
**Eight here, not nine:** (f) is a check on what *this plan writes*, and the ninth pair predates it.
The ninth is not exempt in effect — it lives in `25-CONTEXT.md`, whose single pre-existing occurrence
is already inside the whole-document total of 2 asserted below, and that occurrence was measured to sit
**outside** the `25-D48-CONTINUATION` span.
**Never a whole-document absence-assertion.** MEASURED AT HEAD, `append_addendum` ALREADY OCCURS in
`.planning/ROADMAP.md` (Phase 20's plan-list bullet for `20-16-PLAN.md`, the
`scripts/_addendum.py::append_addendum` clause) and in `25-CONTEXT.md` (the `### Canonical References`
bullet for `scripts/_addendum.py`) — `.planning/REQUIREMENTS.md` has 0 — so a whole-document check goes
RED on prose that predates this plan entirely. **Locate both by content, never by line number:** the
CONTEXT occurrence has already shifted twice under amendments inserted above it, and a plan that
resolves by line number is the defect this file's own doctrine names. That is the
same self-invalidating shape this task refuses in (b). The second half of the guard is therefore a
**no-new-occurrence** check rather than an absence check: assert the whole-document total across the
three documents is exactly **2** and that the two occurrences sit at their pre-existing sites,
matched through `_prose.normalized` against the surrounding claim, so a ninth continuation written by
`append_addendum` still reddens the guard. Record the measured reason in the docstring: run live
against a copy of `REQUIREMENTS.md`, `append_addendum` **succeeded** on an accidental `PENDING`
substring, and `_verdict.recorded_verdict` returns `None` for planning markdown, making its verdict
guard vacuous there.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase25_correction.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_phase25_correction.py -v` exits 0 with **zero skipped**; quote the `N passed, M skipped` line
    - `.venv/bin/python -m pytest tests/test_phase25_correction.py -k sentinel -v` collects **9** cases (one per stem) and all pass — the eight this plan writes plus the pre-existing `25-D48-CONTINUATION` pair
    - **The ninth guarded case, verified at plan time against HEAD:** `.venv/bin/python -c "import pathlib,sys;sys.path.insert(0,'scripts');from _prose import normalized;C='.planning/phases/25-frontier-sweep-and-the-existence-gate-verdict/25-CONTEXT.md';t=pathlib.Path(C).read_text();B='<!-- 25-D48-CONTINUATION-BEGIN -->';E='<!-- 25-D48-CONTINUATION-END -->';assert t.count(B)==1 and t.count(E)==1 and t.index(E)>t.index(B), (t.count(B),t.count(E));span=t[t.index(B):t.index(E)];miss=[v for v in ('0.068930','results/phase20_retention_floor.json','0.005214448168350039','SUPERSEDED IN PLACE') if normalized(v) not in normalized(span)];assert not miss, miss;assert normalized('the floor can never be loosened after seeing results') in normalized(t[:t.index(B)]), 'the superseded original was deleted';assert 'append_addendum' not in span;print('ninth guarded pair: unique, ordered, values in span, original standing above it')"` exits 0. **EXECUTED AT PLAN TIME and printed `ninth guarded pair: unique, ordered, values in span, original standing above it`** — the pair exists at HEAD, so this criterion is a survival guard, and the test in (a)/(b)/(c) is its permanent form
    - RPT-02's register is still **four instances in three guard files**; the ninth pair is a guarded case, not an instance. Assert the docstring's own counting is unchanged and that Tasks 1, 2 and 3(f) still say **eight**
    - `.venv/bin/python -m pytest tests/test_phase25_correction.py -k originals -x` passes and collects **8** cases — every superseded original claim, **including D-35's `Nothing to fix`** and D-49's `the floor can never be loosened after seeing results` above the inherited ninth pair, is asserted still present through `_prose.normalized`. **Moved here from Task 1 (blocker NEW-3): this file does not exist until this task, so running it earlier exits 4**
    - `.venv/bin/python -m pytest tests/test_phase25_correction.py -k measured_values -x` passes — the continuations carry `0.0267857142857143`, `0.5615079365079365`, `1.9090909090909092`, `[dp-refusal:clip-domain]`, `1000000.0`, `results/phase25_frontier.json` and, in the `D35-CONDITION-C` span, `zero_extraction_has_nll` / `25-21` / `25-22`. **Moved here from Task 1 for the same reason**
    - The SUMMARY quotes one deliberately-broken run: temporarily duplicate one BEGIN sentinel on a **copy** of the file in `tmp_path`, point the counter at it, and quote the resulting failure — the watched RED for mechanic (3)
    - `.venv/bin/python -c "import ast; t=ast.parse(open('tests/test_phase25_correction.py').read()); ns=sorted(f.name for f in ast.walk(t) if isinstance(f,ast.FunctionDef) and f.name.startswith('test_')); print(ns); assert len(ns)>=6"` exits 0
    - `.venv/bin/python -c "import ast;P='tests/test_phase25_correction.py';src=open(P).read();t=ast.parse(src);doc={i for n in ast.walk(t) if isinstance(n,(ast.Module,ast.ClassDef,ast.FunctionDef,ast.AsyncFunctionDef)) and n.body and isinstance(n.body[0],ast.Expr) and isinstance(n.body[0].value,ast.Constant) and isinstance(n.body[0].value.value,str) for i in range(n.body[0].lineno,n.body[0].end_lineno+1)};bad=[(i,l.strip()[:70]) for i,l in enumerate(src.splitlines(),1) if 'grep -c' in l and i not in doc and not l.lstrip().startswith('#')];assert not bad, bad;print('grep -c appears only in prose, never in executable code')"` exits 0 — the file does not use the counting form its own mechanic (3) forbids — **resolved by AST, not by `grep -c`.** `grep -c` counts LINES over a file whose own docstrings this plan requires to discuss the very token being counted, so the counting form goes FALSE-RED on its own prose. Measured at plan time: `grep -v '^\s*#' tests/test_phase23_cost.py | grep -c 'grep -c'` returns **3** while the AST gate over the same file exits 0. This repository produced four instances of that false-RED class in Phase 20 alone; RPT-02 exists to close it
    - `.venv/bin/python -m pytest tests/test_phase23_cost.py tests/test_phase24_correction.py -q` reports 0 failed — the two earlier sweeps are untouched
    - `.venv/bin/python -m pytest tests/ -q` reports 0 failed; record the `N passed, M skipped` line and state the delta against the 1647/1 baseline
    - `git diff --exit-code -- scripts/mitigation_gate.py scripts/mitigation_accountant.py scripts/mitigation_unit.py scripts/phase18_extraction.py pyproject.toml` exits 0; `make lint` exits 0
  </acceptance_criteria>
  <done>Nine sentinel pairs are proved unique and ordered — the eight this plan writes plus the pre-existing `25-D48-CONTINUATION` pair, which shipped unguarded — **eight** superseded originals are proved still standing with their continuations found from the claim's own index, the `append_addendum` guard is span-scoped with the pre-existing whole-document count held at 2, and the register this file joins is proved three files wide by AST.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a superseded claim → the record of what was believed | deleting it erases the only trace the belief was held; every correction must be additive and the deletion count checkable |
| a correction mechanism → the document it corrects | `append_addendum` was measured splicing on an accidental substring in `REQUIREMENTS.md` with a vacuous verdict guard |
| a line-wrapped claim → a textual guard | a bare `in` check reports a false absence on a wrapped phrase; every match routes through `_prose.normalized` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-32 | Tampering | a correction spliced onto an accidental `PENDING` substring by `append_addendum`, with a vacuous verdict guard | mitigate | the sentinel + `_prose.normalized` mechanism from plans 23-12 and 24-03 is used instead; `append_addendum` is asserted absent from **this plan's eight continuation spans** and from the guard, with the whole-document count held unchanged at its pre-existing **2** (`ROADMAP.md`'s `20-16-PLAN.md` bullet, `25-CONTEXT.md`'s `### Canonical References` bullet — resolved by content, never by line number) rather than asserted to be zero |
| T-25-33 | Repudiation | a superseded claim quietly deleted, erasing the record of what was believed | mitigate | deletion budget asserted as a LITERAL against a pre-edit snapshot: `0` on `REQUIREMENTS.md`, exactly `1` on `ROADMAP.md` with that line proved identical to the pre-edit `**Requirements**` line; **eight** originals asserted still present through `normalized` |
| T-25-34 | Spoofing | two BEGIN sentinels on one line satisfying a `grep -c ... = 1` check while the second span goes unguarded | mitigate | mechanic (3): sentinels counted with `str.count`; a duplicated-sentinel RED is watched on a `tmp_path` copy |
| T-25-35 | Information Disclosure | SC1 published against a comparator that belongs to a different recipe (v2.0's 0.4921) | mitigate | the continuation names the matched control `0.7837301587301587`, its floor, the third distinct fact-aligned reading `0.5615079365079365`, and the four recipe deltas |
| T-25-36 | Tampering | `gsd-sdk` mutation handlers corrupting planning frontmatter | mitigate | hand-edit only; snapshot before and diff all touched files after; the SUMMARY records zero handler calls |
| T-25-36b | Repudiation | a task whose only verification is a bare `print` (always exit 0) or a `pytest` run against a file a LATER task in the same plan creates (always exit 4) — either way the task ships unverified | mitigate | Task 1's `<verify>` is an assertive inline `python -c` derived from Task 1's own criteria, executed at plan time in both a green and a RED configuration; the two `pytest` criteria moved to Task 3, where the file exists |
| T-25-36c | Repudiation | D-35's falsified premise silently absorbed rather than recorded, leaving "Nothing to fix" standing as a claim | mitigate | D-51's `D35-CONDITION-C` continuation supersedes it in place; the original is asserted STILL PRESENT and the span is asserted to name the seven producerless kwargs and both producer plans — a positive containment check, never an absence-assertion |
| T-25-SC | Tampering | npm/pip/cargo installs | accept | Zero installs; `pyproject.toml` byte-unchanged (RPT-03) |
</threat_model>

<verification>
```bash
.venv/bin/python -m pytest tests/test_phase25_correction.py -v
.venv/bin/python -m pytest tests/test_phase23_cost.py tests/test_phase24_correction.py -q
# snapshots taken BEFORE any edit in this plan
git diff --no-index --numstat -- /tmp/25-07-requirements.pre .planning/REQUIREMENTS.md  # deletions must be 0
git diff --no-index --numstat -- /tmp/25-07-roadmap.pre .planning/ROADMAP.md            # deletions must be exactly 1
git diff --no-index -U0 -- /tmp/25-07-roadmap.pre .planning/ROADMAP.md | grep '^-[^-]'  # must print the pre-edit **Requirements** line and nothing else
git diff --numstat -- .planning/phases/25-frontier-sweep-and-the-existence-gate-verdict/25-CONTEXT.md  # deletions must be 0
.venv/bin/python -m pytest tests/ -q                                    # record "N passed, M skipped"
make lint
```
</verification>

<success_criteria>
- Eight sentinel-bounded dated continuations exist across three planning documents, each with exactly one BEGIN and one END in its own document, ordered
- A **ninth** pair, `25-D48-CONTINUATION` in `25-CONTEXT.md`, is guarded by the same test even though this plan does not write it — its sentinels unique and ordered, its span carrying `0.068930` / `results/phase20_retention_floor.json` / `0.005214448168350039`, and the original above it still standing. It is a guarded case, **not** an RPT-02 register instance: the register stays four instances in three guard files
- **Eight** superseded originals survive byte-unchanged — including D-35's `Nothing to fix` and D-49's `the floor can never be loosened after seeing results`, the claim the inherited `25-D48-CONTINUATION` pair supersedes — each located from its own index through `_prose.normalized`
- `REQUIREMENTS.md`'s and `25-CONTEXT.md`'s diffs are insertions-only (deletion count 0); `ROADMAP.md`'s deletion count is exactly 1 and that line is proved to be the pre-edit `**Requirements**` line; no `gsd-sdk` mutation handler was called
- Task 1's verification is assertive and self-contained: no criterion in it runs `pytest` against `tests/test_phase25_correction.py`, which Task 3 creates (blocker NEW-3)
- RPT-02 is on Phase 25's Requirements line and its traceability row names 23-12, 24-03 and both guard files
- No continuation span this plan writes mentions `append_addendum`, and the whole-document count across the three documents is asserted **unchanged at 2** — the two pre-existing occurrences are the `20-16-PLAN.md` bullet in `.planning/ROADMAP.md` and the `### Canonical References` bullet in `25-CONTEXT.md`, located **by content, never by line number**, both predating this plan, so the guard is a no-new-occurrence check and never a whole-document absence-assertion
- The duplicated-sentinel RED has been watched on a `tmp_path` copy and quoted
</success_criteria>

<output>
Create `.planning/phases/25-frontier-sweep-and-the-existence-gate-verdict/25-07-SUMMARY.md` when done.
</output>
