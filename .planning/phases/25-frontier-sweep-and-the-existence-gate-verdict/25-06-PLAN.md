---
phase: 25-frontier-sweep-and-the-existence-gate-verdict
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phase25_venue.py
  - tests/conftest.py
  - tests/test_phase23_mps_venue.py
  - tests/test_mps_smoke.py
  - tests/test_phase25_venue.py
autonomous: true
requirements: [FRONT-01]

must_haves:
  truths:
    - "D-44: every MPS leg is `skipif`-gated on `torch.backends.mps.is_available()`, which is TRUE during the sweep — so they would RUN and CONTEND, not skip. An explicit sweep-active flag makes them skip with a reason NAMING the sweep."
    - "MEASURED: the composition point is TWO edits, not five — the single register `tests/test_phase23_mps_venue.py` (`_MPS_AVAILABLE` / `_MPS_SKIP` / `_DEVICES`) plus `tests/test_mps_smoke.py`'s own module-level `pytestmark`. Five importers inherit for free, including `test_phase23_cal03.py` and `test_phase23_resume.py` which CONTEXT omits."
    - "TRAP 1: `tests/test_phase23_resume.py` uses `_MPS_AVAILABLE` as a BRANCH VALUE, so the flag must flip `_MPS_AVAILABLE` itself — not only the mark — or that branch still touches MPS during the sweep."
    - "TRAP 2: `_MPS_SKIP`'s `reason` is a fixed string. D-44 requires the reason to name the sweep, so the mark must be built conditionally with TWO reasons."
    - "CONTEXT names two files that need NO separate work: `test_phase22_checkpoint.py`'s gate is on `_REAL_FULL is None` (artifact presence) and `test_phase22_dpsgd.py`'s is on a platform tuple — neither is MPS-gated."
    - "The skip count is STATED IN ADVANCE and asserted as a literal. A green run that silently skipped the MPS legs is the failure, not the pass — 23-VALIDATION's Pitfall 1."
    - "D-13: `sudo pmset -a sleep 0 disksleep 0 powernap 0` before the sweep, REVERTED to the measured current values `sleep 1 / disksleep 10 / powernap 1` at the end. The revert is a COMMITTED plan step, verifiable, never dependent on human memory."
    - "D-43 CORRECTED: `pmset -g`'s summary line enumerates ASSERTIONS, not processes — it listed five `caffeinate` entries while only three caffeinate processes existed. Verification must read `pmset -g assertions` listed by owning process and cross-check `pgrep -x caffeinate`."
  artifacts:
    - path: "scripts/phase25_venue.py"
      provides: "the committed pmset apply/revert commands with their measured revert targets, and an assertion reader that parses `pmset -g assertions` by owning process"
      contains: "PMSET_REVERT"
    - path: "tests/test_phase25_venue.py"
      provides: "the sweep-active skip count and reason text asserted as literals, and D-13's revert step proved committed"
      contains: "def test_the_sweep_active_skip_count_is_the_number_stated_in_advance"
  key_links:
    - from: "tests/conftest.py"
      to: "tests/test_phase23_mps_venue.py"
      via: "the `--sweep-active` option / env var read once, consumed at the single MPS register"
      pattern: "sweep.active|SWEEP_ACTIVE"
    - from: "tests/test_phase23_mps_venue.py::_MPS_AVAILABLE"
      to: "tests/test_phase23_resume.py"
      via: "the flag flips `_MPS_AVAILABLE` itself so the branch-value importer is covered, not only the mark"
      pattern: "_MPS_AVAILABLE"
---

<objective>
Make the test suite safe to run **during** a 4.5–6.3 day GPU sweep, and commit the `pmset` revert as a
verifiable step rather than a promise.

Purpose: during the sweep the machine's MPS device is saturated. Every MPS-parametrized node id is
gated on `torch.backends.mps.is_available()`, which is **TRUE** at that moment — so 42 device-touching
legs will run and contend, and a contention failure is indistinguishable from a genuine one. D-44's
skip must be **loud**: a reason naming the sweep, and a skip count stated in advance so it can be
compared rather than trusted. The silent alternative — MPS legs quietly lost inside a green count — is
the failure `23-VALIDATION.md` already names as Pitfall 1.

And `sudo pmset -a sleep 0 disksleep 0 powernap 0` is a system-wide privileged change to the
developer's own machine. It must be reverted to the measured prior values, and the revert must be a
step this phase can verify, not something someone remembers six days later.

Output: `scripts/phase25_venue.py`, a `--sweep-active` option in `tests/conftest.py`, two edits at the
composition points, and `tests/test_phase25_venue.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-frontier-sweep-and-the-existence-gate-verdict/25-CONTEXT.md
@.planning/phases/25-frontier-sweep-and-the-existence-gate-verdict/25-RESEARCH.md
@.planning/phases/25-frontier-sweep-and-the-existence-gate-verdict/25-VALIDATION.md
</context>

<environment>
**Python 3.11 venv is MANDATORY.** `.venv/bin/python -m pytest ...`. Baseline **1647 passed, 1 skipped**
at `8dd6415` — the skipped one is the pre-existing MPS-unavailable case, and this plan must leave that
number unchanged when the flag is **off**.

**CI is `ubuntu-latest`, CPU-only.** Every MPS leg must stay `skipif`-gated or CI goes red. The new
flag must be **additive** — with it unset, `pytest` on the M3 must still report **1 skipped**, not more.

**The four ancestry-guarded modules must stay byte-unchanged.**

**No `sudo` runs in this plan.** D-13's commands are *committed as data* here; a later plan applies
them behind a blocking human checkpoint.
</environment>

<tasks>

<task type="auto">
  <name>Task 1: `scripts/phase25_venue.py` — the pmset commands, their measured revert targets, and an assertion reader that is actually verifiable</name>
  <files>scripts/phase25_venue.py</files>
  <read_first>
    - `25-RESEARCH.md` §R5 — the measured `pmset -g` output at HEAD (`sleep 1`, `disksleep 10`,
      `powernap 1`; the summary line naming **five** `caffeinate` assertions against **three** actual
      caffeinate processes `7591 46029 58309`), and the deduped owners from `pmset -g assertions`
      (`pid 46029(caffeinate)`, `pid 58309(caffeinate)`, `pid 7591(caffeinate)`, `pid 578(dasd)`,
      `pid 70095(Claude)`)
    - `.planning/STATE.md` § the 23-20 launch discipline — `os.setsid()` + `os.execv`, pid read from
      the LOG never `$!`, probed with `os.getsid()`, `caffeinate -is -w <pid>`, `pid == pgid == sid`
      quoted **before any GPU second**. D-12's `caffeinate -dims` is a different and stronger form
      (it wraps and holds its OWN assertion rather than watching another pid) — name the change
    - `scripts/phase23_run.py` — the git surface at its four read-only call sites (`ls-files`, `show`,
      `merge-base`), for the register a `subprocess` caller in this repo writes in
    - `scripts/phase25_watch.py` — the sibling operational module, for its constant-plus-`_PROVENANCE`
      shape
  </read_first>
  <action>
Create `scripts/phase25_venue.py`. stdlib only (`subprocess`, `re`, `json`, `pathlib`). No torch.

**(a) The pmset pair, committed as data (D-13).**
  - `PMSET_APPLY = ("sudo", "pmset", "-a", "sleep", "0", "disksleep", "0", "powernap", "0")`
  - `PMSET_REVERT = ("sudo", "pmset", "-a", "sleep", "1", "disksleep", "10", "powernap", "1")`
  - `PMSET_REVERT_TARGETS = {"sleep": 1, "disksleep": 10, "powernap": 1}` with a `_PROVENANCE` sibling
    recording that these are the **measured** values read from `pmset -g` at HEAD `8dd6415`, not
    defaults — the revert restores what was actually there
Write into the module docstring, in the register this repo uses, that these are argv **tuples** rather
than shell strings so no shell interpolation exists, and that this module never invokes `PMSET_APPLY`
or `PMSET_REVERT` itself: applying and reverting are operator acts behind a blocking checkpoint, and
this module's job is to make them **checkable**.

**(b) `read_power_settings()`** — runs `pmset -g`, parses the three integers, returns a dict. Write
`prove_reverted()` which calls it and raises `SystemExit` unless every value equals
`PMSET_REVERT_TARGETS`. This is the mechanism that makes D-13's revert *verifiable rather than
remembered*.

**(c) `read_assertions()` — D-43's corrected verification method.** Run `pmset -g assertions` and
parse the **listed-by-owning-process** section, returning a list of `(pid, process_name, assertion)`.
Do **not** parse `pmset -g`'s summary "sleep prevented by" line: it enumerates assertions, not
processes, and was measured listing five `caffeinate` entries while `pgrep -x caffeinate` returned
three pids. Write that measurement into the docstring as the reason.

Write `prove_only_our_caffeinate(*, our_pid)` which cross-checks `read_assertions()` against
`pgrep -x caffeinate` and raises unless the only non-system caffeinate owner is `our_pid`. Pin
`SYSTEM_ASSERTION_OWNERS = ("dasd",)` from the measurement, and state that any other owner must be
named explicitly at launch time rather than tolerated silently — because without this, D-12's whole
mechanism could be masked by residue: the run would *appear* to hold the machine awake while a stray
process from an earlier session genuinely does it.

**(d) `CAFFEINATE_WRAP = ("caffeinate", "-dims")`** with a `_PROVENANCE` sibling naming the deliberate
change from 23-20's `caffeinate -is -w <pid>`: `-dims` **wraps** the run and holds its own display,
idle, system and disk assertions, where `-is -w` watches another pid's lifetime. Record the measured
hazard that motivates it: `pmset -g` reads `sleep 1` on AC **and** battery, and a macOS compute process
holds no idle-sleep assertion on its own.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import sys;sys.path.insert(0,'scripts');import phase25_venue as v;print(v.PMSET_REVERT_TARGETS);print(v.read_power_settings());print(len(v.read_assertions()))"</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -c "import sys;sys.path.insert(0,'scripts');import phase25_venue as v;assert v.PMSET_REVERT_TARGETS=={'sleep':1,'disksleep':10,'powernap':1};assert v.PMSET_APPLY[:3]==('sudo','pmset','-a') and isinstance(v.PMSET_APPLY,tuple);print('argv tuples, measured targets')"` exits 0
    - `.venv/bin/python -c "import sys;sys.path.insert(0,'scripts');import phase25_venue as v;print(v.read_power_settings())"` prints a dict with the three integer keys, read live from `pmset -g`
    - `.venv/bin/python -c "import sys;sys.path.insert(0,'scripts');import phase25_venue as v;a=v.read_assertions();assert all(len(r)==3 and isinstance(r[0],int) for r in a), a[:3];print(sorted({r[1] for r in a}))"` exits 0 and prints owner process names — proving the **assertions** section was parsed, not the summary line
    - `.venv/bin/python -c "import ast;t=ast.parse(open('scripts/phase25_venue.py').read());calls=[n for n in ast.walk(t) if isinstance(n,ast.Call) and isinstance(n.func,ast.Attribute) and n.func.attr in ('run','check_output','Popen')];args=[ast.dump(c) for c in calls];assert not any('sudo' in a for a in args), args;print('this module never invokes sudo')"` exits 0
    - `.venv/bin/python -c "import sys;sys.path.insert(0,'scripts');import phase25_venue as v;assert v.CAFFEINATE_WRAP==('caffeinate','-dims');print('own assertion, not a borrowed one')"` exits 0
    - `git diff --exit-code -- scripts/mitigation_gate.py scripts/mitigation_accountant.py scripts/mitigation_unit.py scripts/phase18_extraction.py` exits 0
  </acceptance_criteria>
  <done>The apply and revert commands are committed argv tuples with measured targets, `prove_reverted()` makes the revert checkable, and D-43's verification reads `pmset -g assertions` by owning process rather than the summary line that was measured to over-count.</done>
</task>

<task type="auto">
  <name>Task 2: D-44's sweep-active skip at its two composition points, loud and counted</name>
  <files>tests/conftest.py, tests/test_phase23_mps_venue.py, tests/test_mps_smoke.py</files>
  <read_first>
    - `tests/test_phase23_mps_venue.py` — the single register, resolved by content: the
      `_MPS_AVAILABLE = torch.backends.mps.is_available()` assignment, the `_MPS_SKIP =
      pytest.mark.skipif(not _MPS_AVAILABLE, reason=<paragraph>)` mark, and
      `_DEVICES = (pytest.param("cpu"), pytest.param("mps", marks=_MPS_SKIP))`.
      **Five importers:** `test_phase22_checkpoint.py`, `test_phase22_dpsgd.py`,
      `test_phase22_fakes.py`, `test_phase23_cal03.py`, `test_phase23_resume.py`
    - `tests/test_phase23_resume.py` — the site, resolved by content, where `_MPS_AVAILABLE` is used
      as a **branch value** rather than through the mark. **This is trap 1**: flipping only the mark
      leaves that branch touching MPS during the sweep
    - `tests/test_mps_smoke.py` — the standalone module-level `pytestmark = pytest.mark.skipif(not
      torch.backends.mps.is_available(), reason="MPS not available (CPU-only CI)")`. Second edit point
    - `tests/conftest.py` — the existing fixtures (`simulate_pascal`, `fake_lm`) and the module
      docstring; the `--sweep-active` option lands here
    - `tests/test_phase22_checkpoint.py` and `tests/test_phase22_dpsgd.py` — **read to confirm no work
      is needed**: the first's gate is `_REAL_FULL is None` (artifact presence), the second's is a
      `(system, machine, torch.__version__) != _CAPTURE_PLATFORM` tuple. Neither is MPS-gated
  </read_first>
  <action>
**(a) `tests/conftest.py` — one place the flag is read.** Add a `pytest_addoption` registering
`--sweep-active`, and a module-level helper `sweep_is_active()` returning `True` when either the
option is set **or** the environment variable `PERSONACORE_SWEEP_ACTIVE` is truthy. The env var is the
load-bearing half: the sweep runs as a LaunchAgent and the operator will run `make test` in a plain
shell, so the flag must be settable without editing a command line. Document both in the conftest
docstring.

Because `pytest_addoption`'s value is only available through the `config` object, expose the env-var
read as a plain module-level constant too — the MPS register is evaluated at **import** time, before
any fixture exists, so it can only consult the environment. Say so in a comment; this is why the env
var is not merely a convenience.

**(b) `tests/test_phase23_mps_venue.py` — edit point 1, and both traps.** Change `_MPS_AVAILABLE` to
`torch.backends.mps.is_available() and not <sweep-active env read>`, so the flag flips **the value
itself** and `test_phase23_resume.py`'s branch-value use is covered without touching that file
(trap 1). Then build `_MPS_SKIP` with **two reasons** chosen conditionally (trap 2):
  - when MPS is genuinely unavailable: the existing paragraph, unchanged
  - when the sweep is active: a new paragraph that **names the sweep explicitly**, states that the
    device is saturated by a 4.5–6.3 day 44-point run, states that a contention failure must never be
    readable as a genuine one, and names the flag that produced the skip so a reader can reproduce it
Keep the `pytest.param(..., marks=_MPS_SKIP)` shape — a shrinking `_DEVICES` list would make the leg
**vanish** rather than skip, and a vanished parametrization cannot be counted.

**(c) `tests/test_mps_smoke.py` — edit point 2.** Import the same availability value from the register
rather than re-spelling `torch.backends.mps.is_available()`, so there is exactly one definition. Give
its `pytestmark` the same two-reason treatment.

**(d) Record what needed no work, in source.** Add a comment at the register naming
`test_phase22_checkpoint.py` and `test_phase22_dpsgd.py` as **not MPS-gated** (artifact presence and
platform tuple respectively) and `test_phase23_cal03.py` / `test_phase23_resume.py` as covered **for
free** through the import. An exemption inferred from absence is the defect; an exemption stated is a
decision. Also name the five **name-only** node ids that are deliberately left running
(`test_lr_schedule.py`, `test_phase22_accountant.py`, `test_preflight.py`, and the two
`test_config.py` cases constructing a `RuntimeConfig(device="mps")` dataclass with no allocation) —
skipping them would inflate the count for no contention benefit and blur the phase-gate skip audit.

**(e) Measure and record the number.** With the flag set, run `pytest --collect-only -q` and then a
full `pytest -q`, and record the resulting skip count. That number is the one Task 3 pins as a literal.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase23_mps_venue.py tests/test_mps_smoke.py tests/test_phase23_resume.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/ -q` with the flag **unset** reports **1 skipped** on the M3 — byte-for-byte the pre-existing baseline; quote the literal `N passed, M skipped` line
    - `PERSONACORE_SWEEP_ACTIVE=1 .venv/bin/python -m pytest tests/ -q` reports a skip count **greater than 1**; quote the literal line and record the number
    - `PERSONACORE_SWEEP_ACTIVE=1 .venv/bin/python -m pytest tests/test_phase23_mps_venue.py -rs -q | grep -c -i "sweep"` returns a value ≥ 1 — the skip **reason names the sweep**
    - `.venv/bin/python -c "import ast;t=ast.parse(open('tests/test_phase23_mps_venue.py').read());a=[n for n in ast.walk(t) if isinstance(n,ast.Assign) and any(getattr(x,'id','')=='_MPS_AVAILABLE' for x in n.targets)][0];assert isinstance(a.value,ast.BoolOp), ast.dump(a.value);print('the flag flips the VALUE, not only the mark')"` exits 0 — trap 1 closed
    - `.venv/bin/python -c "import ast;t=ast.parse(open('tests/test_mps_smoke.py').read());calls=[n for n in ast.walk(t) if isinstance(n,ast.Call) and isinstance(n.func,ast.Attribute) and n.func.attr=='is_available'];assert not calls, [c.lineno for c in calls];print('one definition, imported')"` exits 0
    - `.venv/bin/python -c "import ast;src=open('tests/test_phase23_mps_venue.py').read();print(all(n in src for n in ('test_phase22_checkpoint','test_phase22_dpsgd','test_phase23_cal03','test_phase23_resume','test_config')))"` prints `True` — the exemptions are stated, not inferred
    - `.venv/bin/python -m pytest tests/ -q` reports 0 failed; `make lint` exits 0
  </acceptance_criteria>
  <done>Two edits at two composition points make 42 device-touching legs skip with a reason naming the sweep, `_MPS_AVAILABLE` itself flips so the branch-value importer is covered, and the exemptions are written into the register with their reasons.</done>
</task>

<task type="auto">
  <name>Task 3: `tests/test_phase25_venue.py` — the skip count pinned in advance, and D-13's revert proved committed</name>
  <files>tests/test_phase25_venue.py</files>
  <read_first>
    - `tests/conftest.py` and `tests/test_phase23_mps_venue.py` — the flag and the two-reason mark from
      Task 2, and the skip number measured there
    - `scripts/phase25_venue.py` — `PMSET_REVERT`, `PMSET_REVERT_TARGETS`, `prove_reverted`,
      `read_assertions`, `prove_only_our_caffeinate`
    - `tests/test_phase23_mps_venue.py`'s module docstring — the Pitfall-1 statement this file
      operationalises: a pass count with no skip count beside it is the warning sign
    - `25-RESEARCH.md` ledger rows 11 and 11a — the 47 node ids, the 7 device-touching files, the 5
      name-only ids
  </read_first>
  <action>
Create `tests/test_phase25_venue.py`. CPU-only; every MPS-touching assertion goes through the register
rather than the device.

**(a) The skip count, stated in advance.** Pin `SWEEP_ACTIVE_EXPECTED_SKIPS` as a **literal integer** —
the number measured in Task 2 — with a comment recording how it was obtained and what it covers (the 7
device-touching files; the 5 name-only ids deliberately excluded).
`test_the_sweep_active_skip_count_is_the_number_stated_in_advance` runs the full suite in a
**subprocess** with `PERSONACORE_SWEEP_ACTIVE=1` and `-q`, parses the trailing `N passed, M skipped`
line, and asserts `M == SWEEP_ACTIVE_EXPECTED_SKIPS`. Mark it slow if needed but do **not** skip it —
the whole point is the number. Use `-p no:cacheprovider` and `--collect-only`-free full execution so
the count is real.

Because that subprocess runs the whole suite (~6.5 min), give it a companion cheap test,
`test_the_sweep_active_collection_count_matches`, doing the same against `--collect-only -q` plus
`-rs` on the two register files only, so a fast signal exists per task commit.

**(b) The reason text, asserted not assumed.** `test_the_skip_reason_names_the_sweep` — run the two
register files with `-rs` under the flag and assert every SKIPPED line's reason contains the word
`sweep` and the flag's own name. `test_the_skip_reason_is_different_when_mps_is_simply_absent` —
assert the two reason strings are distinct, so the two-reason construction is proved rather than
declared.

**(c) The flag is additive.** `test_with_the_flag_unset_the_baseline_is_unchanged` — run the full suite
in a subprocess with the env var explicitly cleared and assert the skip count is exactly **1**, the
`8dd6415` baseline. A flag that silently changed the default would be worse than no flag.

**(d) D-13's revert, proved committed (structural).**
  - `test_the_pmset_revert_is_committed_with_its_measured_targets` — `PMSET_REVERT` is a tuple whose
    values match `PMSET_REVERT_TARGETS`, and `PMSET_REVERT_TARGETS` equals
    `{"sleep": 1, "disksleep": 10, "powernap": 1}`. Assert the module is **tracked**
    (`git ls-files scripts/phase25_venue.py` non-empty), which is what makes the revert a committed
    plan step rather than a memory
  - `test_prove_reverted_refuses_a_non_reverted_machine` — monkeypatch `read_power_settings` to return
    `{"sleep": 0, "disksleep": 0, "powernap": 0}` and assert `prove_reverted()` raises `SystemExit`
    naming all three fields. **This is the natural RED** and it must be watched
  - `test_the_assertion_reader_parses_owners_not_the_summary_line` — feed `read_assertions` a captured
    `pmset -g assertions` fixture and assert it returns `(pid, process, assertion)` triples; then feed
    it a `pmset -g` **summary** blob and assert it returns nothing, so the corrected method is enforced
    rather than described
  - `test_prove_only_our_caffeinate_refuses_a_stray` — a fixture with two caffeinate owners raises,
    naming the stray pid; with one matching `our_pid` it passes
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase25_venue.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_phase25_venue.py -v` exits 0; quote the `N passed, M skipped` line
    - The SUMMARY quotes **three** literal pytest summary lines: the flag-unset full run (`1 skipped`), the flag-set full run (`SWEEP_ACTIVE_EXPECTED_SKIPS` skipped), and the delta between them
    - The SUMMARY quotes one verbatim SKIPPED reason line captured under the flag, showing the sweep named in it
    - The SUMMARY quotes the verbatim `SystemExit` from `prove_reverted()` on a non-reverted machine
    - `.venv/bin/python -c "import ast;t=ast.parse(open('tests/test_phase25_venue.py').read());a=[n for n in ast.walk(t) if isinstance(n,ast.Assign) and any(getattr(x,'id','')=='SWEEP_ACTIVE_EXPECTED_SKIPS' for x in n.targets)][0];assert isinstance(a.value,ast.Constant) and isinstance(a.value.value,int), ast.dump(a.value);print('stated in advance as a literal:', a.value.value)"` exits 0
    - `.venv/bin/python -m pytest tests/ -q` with the env var unset reports 0 failed and **1 skipped**
    - `git diff --exit-code -- scripts/mitigation_gate.py scripts/mitigation_accountant.py scripts/mitigation_unit.py scripts/phase18_extraction.py pyproject.toml` exits 0; `make lint` exits 0
  </acceptance_criteria>
  <done>The sweep-active skip count is a committed literal that the suite checks against itself, the skip reason is asserted to name the sweep and to differ from the MPS-absent reason, and `prove_reverted()` has been watched refusing a non-reverted machine.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a saturated MPS device → the test suite | 42 device-touching legs run and contend during the sweep; a contention failure is indistinguishable from a genuine one |
| a privileged system-wide power change → the developer's machine | `sudo pmset -a` alters sleep behaviour for every process, not just this run |
| `pmset -g`'s summary line → the claim "only our caffeinate holds an assertion" | measured: the summary enumerates assertions, not processes, and over-counted 5 against 3 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-26 | Spoofing | a GPU-contention failure read as a genuine regression during a 6-day sweep | mitigate | the flag makes 42 legs skip with a reason **naming the sweep**, and the skip count is a committed literal the suite checks against itself |
| T-25-27 | Repudiation | MPS legs silently lost inside a green pass count (23-VALIDATION Pitfall 1) | mitigate | `pytest.param(..., marks=skipif)` keeps the leg countable rather than vanished; three literal summary lines are quoted in the SUMMARY |
| T-25-28 | Elevation of Privilege | `sudo pmset -a` left applied after the sweep, permanently disabling sleep on the developer's machine | mitigate | `PMSET_REVERT` and `PMSET_REVERT_TARGETS` are committed argv data with the **measured** prior values; `prove_reverted()` is watched refusing a non-reverted machine; plan 25-20 executes and verifies the revert |
| T-25-29 | Tampering | this module invoking `sudo` autonomously | mitigate | an AST assertion that no `subprocess` call in `scripts/phase25_venue.py` carries `sudo`; the apply/revert are operator acts behind a blocking checkpoint in a later plan |
| T-25-30 | Information Disclosure | claiming "the run holds the only wake assertion" while a stray process from an earlier session genuinely does | mitigate | `read_assertions()` parses the by-owning-process section and is asserted to return **nothing** for the summary blob; `prove_only_our_caffeinate` cross-checks `pgrep -x caffeinate` |
| T-25-31 | Denial of Service | trap 1 — `test_phase23_resume.py`'s branch-value use of `_MPS_AVAILABLE` still allocating on MPS during the sweep | mitigate | the flag flips `_MPS_AVAILABLE` itself, proved by an AST assertion that its assigned value is a `BoolOp` |
| T-25-SC | Tampering | npm/pip/cargo installs | accept | Zero installs; `pyproject.toml` byte-unchanged (RPT-03) |
</threat_model>

<verification>
```bash
.venv/bin/python -m pytest tests/test_phase25_venue.py -v
.venv/bin/python -m pytest tests/ -q                                       # expect "1 skipped"
PERSONACORE_SWEEP_ACTIVE=1 .venv/bin/python -m pytest tests/ -q            # expect the pinned count
PERSONACORE_SWEEP_ACTIVE=1 .venv/bin/python -m pytest tests/test_phase23_mps_venue.py -rs -q
git diff --exit-code -- scripts/mitigation_gate.py scripts/mitigation_accountant.py scripts/mitigation_unit.py scripts/phase18_extraction.py pyproject.toml
make lint
```
</verification>

<success_criteria>
- With the flag unset the suite still reports exactly **1 skipped** — the flag is additive
- With the flag set the skip count equals a committed literal, and the reason text names the sweep
- `_MPS_AVAILABLE` itself flips, so `test_phase23_resume.py`'s branch-value use is covered (trap 1)
- Two distinct reason strings exist, proved distinct (trap 2)
- The four exemptions and the five name-only ids are stated in the register with their reasons
- `PMSET_REVERT` is committed argv data with the measured targets, and `prove_reverted()` has been watched refusing
- `read_assertions()` parses `pmset -g assertions` by owning process and returns nothing for the summary blob
</success_criteria>

<output>
Create `.planning/phases/25-frontier-sweep-and-the-existence-gate-verdict/25-06-SUMMARY.md` when done.
</output>
