---
phase: 25-frontier-sweep-and-the-existence-gate-verdict
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phase25_verdict.py
  - tests/test_phase25_verdict.py
autonomous: true
requirements: [FRONT-04]

must_haves:
  truths:
    - "D-42 CORRECTED: condition (a) is ZERO TOLERANCE, not 'at most 2 successes of 416'. `tolerance_report(ceiling=wilson_upper_bound(0,416), n_questions=416)` returns `(0, 0.0, '… ZERO TOLERANCE …')` because (a) compares Wilson-bound to Wilson-bound (`mitigation_gate.py:755-756`), and `wilson_upper_bound(1,416) = 0.010702 > X = 0.006462`."
    - "D-42's real reason for rejecting the 5-seed pooling is STRUCTURAL UNSATISFIABILITY, not tightness: `tolerance_report` RAISES on it — 'no outcome clears it, not even a perfect one'."
    - "§ledger 9 CORRECTED: 'passed VERBATIM' means TWO fields from `pooled` plus TWO from the record's top level. `extraction_ceiling(**pooled)` raises TypeError."
    - "D-32 / FRONT-04: the null verdict is the gate's EXISTING `null-at-both-capacities` branch reached through a real call. Nothing new is authored in the phase that runs the sweep."
    - "D-23: `capacity_comparison` is a DP-ONLY instrument — it takes no `arm` argument (0 occurrences in its body) and the adversarial arm has no σ/δ/q. The absence of a committed adversarial capacity rule is NAMED, never patched."
    - "D-25: `capacity_comparison`'s `missing` check IGNORES extra keys — a 999× `clip_norm` divergence passed live with the reason string reading 'both points agree exactly on all 4'. The caller-side `_prove` is the ONLY thing standing between two differently-noised points and a 'comparable' verdict."
    - "D-35: `mitigation_point_verdict` contains ZERO occurrences of `epsilon` or `accounting` across its 195 lines (not 198) — the three-condition reading is arm-agnostic by construction and nothing needs fixing."
    - "§R3: `mitigation_point_verdict` takes WHOLE-CURVE inputs (`sweep_extraction_rates`, `sweep_taught_recalls`), so the verdict pass is a distinct CPU-only stage AFTER a leg's curve is complete — never part of a point."
  artifacts:
    - path: "scripts/phase25_verdict.py"
      provides: "the CPU-only verdict pass: 21-kwarg assembly, the caller-side clip_norm prove, DP-only scoping, and the arm existentials"
      contains: "def prove_clip_norm_equality"
    - path: "tests/test_phase25_verdict.py"
      provides: "the null branch reached live, the ZERO TOLERANCE sentence asserted, the 5-seed refusal watched, the clip_norm hole demonstrated then closed"
      contains: "def test_tolerance_report_says_zero_tolerance"
  key_links:
    - from: "scripts/phase25_verdict.py"
      to: "scripts/mitigation_gate.py"
      via: "import only — mitigation_point_verdict, exists_clearing_point, capacity_comparison, extraction_ceiling, tolerance_report, MECHANISM_KEYS, CAPACITY_BRANCHES"
      pattern: "import mitigation_gate|from mitigation_gate import"
    - from: "scripts/phase25_verdict.py"
      to: "results/phase23_never_taught.json"
      via: "the designated-seed pooled block's two count fields plus the record's top-level extraction_noise_floor and extraction_floor_provenance"
      pattern: "phase23_never_taught"
---

<objective>
Build the CPU-only verdict pass that turns 44 committed point records into a verdict — by importing
every threshold from the frozen gate, `_prove`ing the one equality the gate silently ignores, and
carrying the pre-registered null as a named branch rather than as an absence of output.

Purpose: FRONT-04 is met by **importing**, not by authoring. `null-at-both-capacities` already exists
in `CAPACITY_BRANCHES`, `_CAPACITY_DISPATCH[(False, False)]` is already dispatch-total at module
scope, and `exists_clearing_point` already carries its own denominator string. The only genuinely new
code here is the **caller-side `clip_norm` `_prove`** — and it is load-bearing, because a live
`capacity_comparison` call with `clip_norm` 1.0 against 999.0 returned
`('null-at-both-capacities', …)` with a reason reading *"both points agree exactly on all 4"*. Extra
mechanism keys are ignored, not refused.

Output: `scripts/phase25_verdict.py` and `tests/test_phase25_verdict.py`, with the zero-tolerance
sentence, the 5-seed refusal, and the `clip_norm` hole all watched rather than described.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-frontier-sweep-and-the-existence-gate-verdict/25-CONTEXT.md
@.planning/phases/25-frontier-sweep-and-the-existence-gate-verdict/25-RESEARCH.md
@.planning/phases/25-frontier-sweep-and-the-existence-gate-verdict/25-VALIDATION.md
</context>

<environment>
**Python 3.11 venv is MANDATORY.** `.venv/bin/python -m pytest ...`. Baseline **1647 passed, 1 skipped**.

**`scripts/mitigation_gate.py` IS FROZEN AND ANCESTRY-GUARDED.** Any commit to it after
`results/phase20_*` exists reddens the guard permanently — the guard takes `adds[-1]`, the EARLIEST
add, so `git rm` plus a re-add at the same path cannot launder it. `clip_norm` therefore **cannot**
join `MECHANISM_KEYS`, and it does not need to: the gap is closed caller-side. Same prohibition for
`mitigation_accountant.py`, `mitigation_unit.py`, `phase18_extraction.py`.

**Never grep `mitigation_gate.py` for `epsilon`.** Measured: 25 string-literal occurrences with an
`ast.Name` count of **0**. Any textual check over that file must be an AST assertion.
</environment>

<tasks>

<task type="auto">
  <name>Task 1: `scripts/phase25_verdict.py` — 21-kwarg assembly, the caller-side clip_norm prove, and the DP-only scoping</name>
  <files>scripts/phase25_verdict.py</files>
  <read_first>
    - `scripts/mitigation_gate.py:637-831` — `mitigation_point_verdict`, **21 keyword-only args, 0
      positional, 195 lines**. Read every kwarg name; the driver assembles all of them
    - `scripts/mitigation_gate.py:749-760` — where `control_extraction_successes` /
      `control_extraction_questions` go: straight into `extraction_ceiling`, and `:755-756` where
      condition (a) compares `wilson_upper_bound(point_successes, point_questions) <= ceiling`
    - `scripts/mitigation_gate.py:842-905` — `exists_clearing_point(*, points, arm)`: raises on an
      empty list, `_prove`s a mixed-arm list, and its `:900-905` f-string carries
      `"0 of {len(points)} point(s) examined returned PASS"`. `points` is a sequence of the
      **3-tuples** `mitigation_point_verdict` returns
    - `scripts/mitigation_gate.py:1026-1058` — `MECHANISM_KEYS = ("sigma","steps","delta","q")`,
      `CAPACITY_BRANCHES`, `_CAPACITY_DISPATCH[(False,False)] = "null-at-both-capacities"`, the
      module-scope totality `_prove`
    - `scripts/mitigation_gate.py:1061-1186` — `capacity_comparison`, **8 kwonly args**, zero
      occurrences of `arm` in its body; presence `_prove` at `:1110-1121`, exact-equality `differing`
      at `:1122-1135`. **Both loops iterate `for key in MECHANISM_KEYS` — there is no reverse check**
    - `results/phase23_never_taught.json` — the `pooled` block's real keys
      (`draws_per_question`, `nontarget_questions`, `nontarget_successes`, `pooling_rule`, `rate`,
      `seed`, `tier`, `total_draws`, `unit`) and the TOP-LEVEL `extraction_noise_floor` (`0.0`) and
      `extraction_floor_provenance` (`arm == 'never-taught'`, 5 seeds)
    - `scripts/erasure_gate.py` — `wilson_upper_bound`, `MARGIN_K = 2`.
      `mitigation_gate.wilson_upper_bound is erasure_gate.wilson_upper_bound` is **True**; D-42 requires
      object identity, so import the name the gate exposes and assert the identity rather than
      re-implementing
    - `results/phase23_matched_verdict.json` and `scripts/phase20_gate_coverage.py:370`
      (`_ADAPTER_REGIME_RETENTION_FLOOR = 0.008681618994239138`) — two of the anchors §R3 lists as
      import-never-retype
  </read_first>
  <action>
Create `scripts/phase25_verdict.py`. Stdlib plus the frozen gate modules. No torch, no numpy.

**(a) The never-taught anchors, imported with the §ledger-9 correction applied.** Write
`never_taught_anchors()` returning a dict with exactly four keys drawn from
`results/phase23_never_taught.json`: `control_extraction_successes` and
`control_extraction_questions` from `blob["pooled"]["nontarget_successes"]` /
`["nontarget_questions"]`, and `extraction_noise_floor` / `extraction_floor_provenance` from the
record's **top level**. Write into the docstring that `extraction_ceiling(**pooled)` raises
`TypeError: unexpected keyword argument 'draws_per_question'` — "verbatim" means these two fields,
never a splat — and that these counts are the **never-taught** arm's, not the σ=0 control's, because
`extraction_ceiling` `_prove`s `extraction_floor_provenance["arm"] == NEVER_TAUGHT_ARM`
(`mitigation_gate.py:426`). Add `_prove`-style assertions that `provenance["arm"]` equals
`mitigation_gate.NEVER_TAUGHT_ARM` and that `len(provenance["seeds"]) == 5`.

**(b) `X` and its published sentence (D-42).** Write `extraction_ceiling_and_tolerance()` calling
`mitigation_gate.extraction_ceiling(...)` with the four anchors, then
`mitigation_gate.tolerance_report(ceiling=X, n_questions=416)`. Return `(X, tolerated, rate, sentence)`.
The docstring must record the correction in place: CONTEXT's D-42 said *"tolerance = at most 2
successes of 416"*; the gate's own reporter says **0**, because (a) compares Wilson bound to Wilson
bound and `wilson_upper_bound(1, 416) = 0.010701849625219550 > X = 0.006461685297443485`. State that
the rejected 5-seed pooling is not merely 4.97× tighter — `tolerance_report` **RAISES** on it with
*"no outcome clears it, not even a perfect one, so there is no tolerance to report"* — which is a
stronger reason than the one D-42 gave, and the one that must be written into the pre-registration.
Assert `mitigation_gate.wilson_upper_bound is erasure_gate.wilson_upper_bound` at module import so
D-42's object-identity requirement is a runtime property, not a comment.

**(c) The caller-side `clip_norm` `_prove` (D-25).** Write
`prove_clip_norm_equality(small_mechanism, large_mechanism, *, point_pair)`. It raises `SystemExit`
unless `clip_norm` is present in BOTH dicts and compares exactly equal. The message must state the
measured hole verbatim: `capacity_comparison`'s presence and equality loops both iterate
`for key in MECHANISM_KEYS`, so extra keys are ignored; a live call with `clip_norm` 1.0 against
999.0 returned `'null-at-both-capacities'` with the reason *"both points agree exactly on all 4 of
('sigma','steps','delta','q')"*. Because `std = sigma * C`, two points at equal nominal σ and
different `C` carry different noise scale while passing the gate's equality check. This `_prove` runs
**before** every `capacity_comparison` call.

**(d) DP-only scoping (D-23).** Write `capacity_verdict(small_point, large_point)` which calls
`prove_clip_norm_equality` first, then refuses if either point's `arm` is not in
`teach_persona.DP_ARMS`, then calls `mitigation_gate.capacity_comparison(...)`. The refusal message
must state that `capacity_comparison` takes no `arm` argument and `_prove`s all four `MECHANISM_KEYS`
present and exactly equal, and that the adversarial arm has no σ/δ/q — the same fact
`accounting: null` states structurally. Add `ADVERSARIAL_CAPACITY_RULE_ABSENT`, the string the
artifact and the report carry so a reader does not trip over the absence.

**(e) The arm existentials (D-32 / FRONT-04).** Write `arm_existential(points, arm)` assembling
`points` as the 3-tuples `mitigation_point_verdict` returns and calling
`mitigation_gate.exists_clearing_point(points=points, arm=arm)`. Return the gate's own
denominator-carrying string unmodified — no reformatting, no rounding.

**(f) The whole-curve staging note (§R3).** Write `curve_verdicts(records, arm, capacity)` which
takes the committed per-point records for ONE leg, builds `sweep_extraction_rates` and
`sweep_taught_recalls` across the whole leg, and only then calls `mitigation_point_verdict` per point.
State in the docstring that a per-point verdict cannot be computed at point time — GATE-06's
did-the-curve-cross check reads whole-curve inputs at `mitigation_gate.py:695-699` — so this stage is
CPU-only, runs after a leg completes, and is cheap.

Import every threshold. Retype none. `F_Y`, `F_C`, `K_RUNGS`, `MARGIN_K`, `GATED_TIER`,
`REPORTED_TIER`, `retention_cap` and `_ADAPTER_REGIME_RETENTION_FLOOR` all come from their modules.
Note in a comment that the governing retention cap is **3.9085032379884783** derived live from
`mitigation_gate.retention_cap`, **not** the `4.029` that `.planning/STATE.md:1625-1628` records.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import sys;sys.path.insert(0,'scripts');import phase25_verdict as v;print(v.extraction_ceiling_and_tolerance())"</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -c "import sys;sys.path.insert(0,'scripts');import phase25_verdict as v;X,t,r,s=v.extraction_ceiling_and_tolerance();assert t==0 and r==0.0 and 'ZERO TOLERANCE' in s, (t,r,s);print(X)"` prints `0.006461685297443485`
    - `.venv/bin/python -c "import sys;sys.path.insert(0,'scripts');import phase25_verdict as v;a=v.never_taught_anchors();assert set(a)=={'control_extraction_successes','control_extraction_questions','extraction_noise_floor','extraction_floor_provenance'};assert (a['control_extraction_successes'],a['control_extraction_questions'])==(0,416);print(a['extraction_floor_provenance']['arm'])"` prints `never-taught`
    - `.venv/bin/python -c "import sys;sys.path.insert(0,'scripts');import phase25_verdict as v;v.prove_clip_norm_equality({'clip_norm':1.0},{'clip_norm':999.0},point_pair=('a','b'))"` exits non-zero, and its message contains `MECHANISM_KEYS` and `999`
    - `.venv/bin/python -c "import sys;sys.path.insert(0,'scripts');import phase25_verdict as v;v.prove_clip_norm_equality({'clip_norm':1.0},{'clip_norm':1.0},point_pair=('a','b'));print('equal ok')"` exits 0
    - `.venv/bin/python -c "import sys;sys.path.insert(0,'scripts');import mitigation_gate as g, erasure_gate as e;assert g.wilson_upper_bound is e.wilson_upper_bound and g.MARGIN_K is e.MARGIN_K;print('object identity')"` exits 0
    - `.venv/bin/python -c "import ast;t=ast.parse(open('scripts/phase25_verdict.py').read());nums={n.value for n in ast.walk(t) if isinstance(n,ast.Constant) and isinstance(n.value,float)};assert 0.7 not in nums and 0.5 not in nums and 4.029 not in nums, nums;print('no retyped thresholds')"` exits 0
    - `git diff --exit-code -- scripts/mitigation_gate.py scripts/mitigation_accountant.py scripts/mitigation_unit.py scripts/phase18_extraction.py` exits 0
  </acceptance_criteria>
  <done>The verdict pass imports X, the tolerance sentence, the null branch and both existentials from the frozen gate, and refuses a capacity comparison whose `clip_norm` diverges — the one hole the gate ignores.</done>
</task>

<task type="auto">
  <name>Task 2: `tests/test_phase25_verdict.py` — the null branch, the zero-tolerance sentence, and the clip_norm hole watched</name>
  <files>tests/test_phase25_verdict.py</files>
  <read_first>
    - `scripts/phase25_verdict.py` — everything built in Task 1
    - `tests/test_phase20_gate.py` (or whichever module holds
      `test_capacity_rule_commits_both_branches_and_refuses_the_unset_fallback`) — the register for
      reaching a `CAPACITY_BRANCHES` branch through a **real call** rather than by indexing the
      dispatch table
    - `scripts/mitigation_gate.py:1178` — where the dispatch is read, so the test proves the branch is
      REACHED and not merely present
    - `25-RESEARCH.md` §L8 — the reproduced `tolerance_report` output and the `ValueError` text for the
      5-seed ceiling, both to be asserted on discriminating substrings
    - `25-RESEARCH.md` ledger row 2c — the live divergent-`clip_norm` call and the exact reason string
      it returned; this test reproduces that hole before closing it
  </read_first>
  <action>
Create `tests/test_phase25_verdict.py`. CPU-only, no torch.

**(a) D-42's zero tolerance, asserted from the gate's own reporter.**
  - `test_tolerance_report_says_zero_tolerance` — `X == 0.006461685297443485` under exact `==`;
    `tolerance_report(ceiling=X, n_questions=416)` returns `(0, 0.0, sentence)` and the sentence
    contains `ZERO TOLERANCE` and `perfect erasure`
  - `test_one_leaked_question_already_exceeds_the_ceiling` — `wilson_upper_bound(0, 416) <= X` and
    `wilson_upper_bound(1, 416) > X`, both under exact comparison, with the two values asserted at
    full precision. This is the arithmetic that makes "at most 2" false
  - `test_the_five_seed_ceiling_is_refused_outright` — `tolerance_report(ceiling=wilson_upper_bound(0, 2080), n_questions=416)` raises `ValueError` whose message contains
    `no outcome clears it` and `must not be published as one`. **Watched, not described**
  - `test_x_comes_from_the_designated_seed_by_object_identity` — assert
    `mitigation_gate.wilson_upper_bound is erasure_gate.wilson_upper_bound`, and that
    `never_taught_anchors()["extraction_floor_provenance"]["arm"] == mitigation_gate.NEVER_TAUGHT_ARM`

**(b) FRONT-04's null branch, reached through a real call (D-32).**
  - `test_null_at_both_capacities_is_reached_by_a_real_call` — build two DP mechanism dicts agreeing
    on all four `MECHANISM_KEYS`, call `capacity_verdict` with `small_cleared=False,
    large_cleared=False`, and assert the returned branch string is exactly
    `mitigation_gate.CAPACITY_BRANCHES`' `null-at-both-capacities` member. Assert the branch string is
    **read from the module**, not retyped, by comparing against
    `mitigation_gate._CAPACITY_DISPATCH[(False, False)]`
  - `test_exists_clearing_point_carries_its_denominator` — call `arm_existential` with N non-PASS
    3-tuples and assert the returned string contains `0 of {N} point(s) examined returned PASS`, with
    `N` interpolated from the list length rather than hard-coded
  - `test_nothing_new_is_authored_for_the_null_verdict` — AST over `scripts/phase25_verdict.py`:
    no string constant in the module equals any member of `CAPACITY_BRANCHES`. The branch names are
    imported, never spelled

**(c) D-25's hole, demonstrated then closed. This is the natural RED.**
  - `test_capacity_comparison_ignores_a_999x_clip_norm_divergence` — call
    `mitigation_gate.capacity_comparison` DIRECTLY with `small_mechanism` carrying `clip_norm: 1.0`
    and `large_mechanism` carrying `clip_norm: 999.0`, all four `MECHANISM_KEYS` equal. Assert it
    returns a branch and that its reason string contains `agree exactly on all 4`. The docstring must
    state plainly that this test asserts a **defect in a frozen module that cannot be fixed there**,
    which is why the closure is caller-side
  - `test_the_caller_side_prove_refuses_what_the_gate_accepted` — the same two dicts through
    `prove_clip_norm_equality` raise `SystemExit`; and `capacity_verdict` with those dicts raises
    before ever reaching the gate (assert the gate is not called, e.g. by monkeypatching
    `mitigation_gate.capacity_comparison` to a function that fails the test if invoked)

**(d) D-23's DP-only scoping, structural.**
  - `test_capacity_comparison_is_never_called_with_an_adversarial_point` — AST walk over
    `scripts/phase25_run.py` **if it exists yet, else over `scripts/phase25_verdict.py` only**: every
    call to `capacity_comparison` must be lexically inside `capacity_verdict`, which is the function
    holding the arm refusal. Resolve by AST; `mitigation_gate.py`'s prose discusses the name
  - `test_an_adversarial_point_is_refused_before_the_gate` — `capacity_verdict` on two `adv_n8`
    points raises `SystemExit` naming `MECHANISM_KEYS` and `accounting: null`
  - `test_the_absent_adversarial_capacity_rule_is_a_named_string` — `ADVERSARIAL_CAPACITY_RULE_ABSENT`
    exists and its text is matched through `scripts/_prose.normalized`

**(e) D-35, asserted rather than assumed.**
  - `test_the_three_condition_reading_is_arm_agnostic` — AST over
    `mitigation_gate.mitigation_point_verdict`'s body: `epsilon` and `accounting` each occur **0**
    times as an `ast.Name` or `ast.arg`. Report the function's real length as **195** lines, correcting
    CONTEXT's 198. **This must be an AST assertion — a grep for `epsilon` over that file matches 25
    string literals and would go false-RED**
  - `test_a_full_21_kwarg_verdict_assembles` — a live `mitigation_point_verdict` call with the
    never-taught anchors returns a 3-tuple whose third element is `'dp'`
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase25_verdict.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_phase25_verdict.py -v` exits 0 with **zero skipped**; quote the `N passed, M skipped` line
    - `.venv/bin/python -m pytest tests/test_phase25_verdict.py -k "tolerance" -v` collects at least 3 cases and all pass
    - The SUMMARY quotes verbatim: the `tolerance_report` sentence (with `ZERO TOLERANCE` in it), the `ValueError` text from the 5-seed ceiling, and the `capacity_comparison` reason string that ignored the 999× `clip_norm` divergence
    - The SUMMARY states in one sentence that **CONTEXT's D-42 figure "at most 2 successes of 416" is superseded by a measured 0**, and that the reason for rejecting the 5-seed pooling is structural unsatisfiability, not a 4.97× tightening
    - `.venv/bin/python -c "import ast;t=ast.parse(open('tests/test_phase25_verdict.py').read());names={n.attr for n in ast.walk(t) if isinstance(n,ast.Attribute)};assert 'normalized' in names or True;print(sorted(f.name for f in ast.walk(t) if isinstance(f,ast.FunctionDef))[:5])"` exits 0
    - `grep -v '^\s*#' tests/test_phase25_verdict.py | grep -c 'at most 2'` returns `0` — the superseded figure appears nowhere
    - `.venv/bin/python -m pytest tests/ -q` reports 0 failed; record the `N passed, M skipped` line
    - `git diff --exit-code -- scripts/mitigation_gate.py scripts/mitigation_accountant.py scripts/mitigation_unit.py scripts/phase18_extraction.py` exits 0; `make lint` exits 0
  </acceptance_criteria>
  <done>The null branch is reached through a real call, the ZERO TOLERANCE sentence and the 5-seed refusal are captured verbatim, and the 999× `clip_norm` hole has been watched passing the gate and then refused caller-side.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a frozen gate that cannot be fixed → a caller that must compensate | `clip_norm` cannot join `MECHANISM_KEYS`; the equality must be proved before the gate is called or two differently-noised points read as comparable |
| a per-point record → a whole-curve verdict | `mitigation_point_verdict` reads `sweep_extraction_rates` / `sweep_taught_recalls`; computing a verdict at point time would read a truncated curve |
| the never-taught floor record → condition (a)'s ceiling | the counts must be the never-taught arm's, or `extraction_ceiling`'s provenance `_prove` is a lie |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-07 | Information Disclosure | publishing "at most 2 successes tolerated" beside an artifact whose own reason string says ZERO TOLERANCE | mitigate | `tolerance_report`'s sentence is asserted verbatim in a test; `grep -c 'at most 2'` over the test file returns 0; the correction is recorded in the module docstring and the SUMMARY |
| T-25-08 | Tampering | two points at equal nominal σ and different `C` compared as "comparable" | mitigate | `prove_clip_norm_equality` runs before every `capacity_comparison`; the hole is watched being accepted by the gate and then refused caller-side, with the gate monkeypatched to fail if reached |
| T-25-09 | Spoofing | an adversarial point entering a DP-only capacity instrument | mitigate | `capacity_verdict` refuses on arm before the gate call; an AST walk proves every `capacity_comparison` call is lexically inside that function |
| T-25-10 | Repudiation | a null result presented as "no result produced" | mitigate | the null is the gate's own `null-at-both-capacities` branch, reached through a real call and compared against `_CAPACITY_DISPATCH[(False, False)]` rather than retyped |
| T-25-11 | Tampering | a re-reduced X narrowing the ceiling on correlated evidence | mitigate | X is `extraction_ceiling` over the designated seed's pooled block; `wilson_upper_bound` object identity asserted; the 5-seed ceiling's `ValueError` watched |
| T-25-SC | Tampering | npm/pip/cargo installs | accept | Zero installs; `pyproject.toml` byte-unchanged (RPT-03) |
</threat_model>

<verification>
```bash
.venv/bin/python -m pytest tests/test_phase25_verdict.py -v
.venv/bin/python -m pytest tests/test_phase20_prereg.py -k import_graph -q
.venv/bin/python -m pytest tests/ -q                       # record "N passed, M skipped"
git diff --exit-code -- scripts/mitigation_gate.py scripts/mitigation_accountant.py scripts/mitigation_unit.py scripts/phase18_extraction.py pyproject.toml
make lint
```
</verification>

<success_criteria>
- `X == 0.006461685297443485` and `tolerance_report` returns `(0, 0.0, "… ZERO TOLERANCE …")`, asserted
- The 5-seed ceiling's `ValueError` is watched and quoted verbatim
- `null-at-both-capacities` is reached through a real `capacity_comparison` call, its name imported not spelled
- The 999× `clip_norm` divergence is watched passing the gate, then refused caller-side before the gate is reached
- `capacity_comparison` is provably never called with an adversarial point, by AST
- `mitigation_point_verdict` is asserted to contain zero `epsilon` / `accounting` names by AST (never grep), and reported at its real length of 195 lines
</success_criteria>

<output>
Create `.planning/phases/25-frontier-sweep-and-the-existence-gate-verdict/25-02-SUMMARY.md` when done.
</output>
