---
phase: 25-frontier-sweep-and-the-existence-gate-verdict
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/teach_persona.py
  - tests/test_phase25_wr.py
autonomous: true
requirements: [ADVT-01]

must_haves:
  truths:
    - "D-41: WR-01, WR-04, WR-06 and WR-08 are FIXED before any point runs. WR-02, WR-03 and WR-07 are DEFERRED with their unreachability RECORDED in source, never silenced."
    - "EVERY LINE NUMBER IN 24-REVIEW.md IS STALE BY ~+24. All four defects are still present at HEAD; each site is resolved by CONTENT at implementation time, never copied from the review."
    - "WR-01 is the 'guard refuses a NAME where the harm is a PROPERTY' class Phase 20 recorded twice: `build_bins` flat branch tests `if adversarial_ratio > 0` while the aligned branch tests `if adversarial_ratio:` — a negative or NaN ratio silently builds the control on the flat branch under an adversarial name."
    - "WR-04: on the flat branch `_mix_adversarial` runs and then `_prepend_replay` runs, so `replay_ratio` stops describing the bin when both ratios are set — against D-34's live mechanism assertion. The aligned branch already refuses the pair."
    - "WR-06: two independent `adversarial_episodes(tok)` / `adversarial_episode_families(tok)` calls are zipped by index with only a `len()` check between them; the paired form is available as `phase24_adversarial._adversarial_pool(tok)`. An index-paired mismatch would corrupt D-36's per-family counts."
    - "WR-08: both bins are written to disk with `tofile` BEFORE proof 1 (the 1:1 element check) and before `_prove_floor_and_band` — colliding with `refuse_if_exists` and with D-10's one-attempt rule, because a refused build has already left bytes."
    - "`train_arm` has NO `dp_fn` / `fact_bin` / `n_facts` kwargs — its real signature is `train_arm(arm, *, facts, family_ids, second_person, replay_ratio, adversarial_ratio, seed, prefix, dp_sigma, dp_clip_norm, resume_from)`. Those three belong to `personacore.training.loop.train`."
  artifacts:
    - path: "scripts/teach_persona.py"
      provides: "four refusals repaired: both branches agree on the ratio domain, replay+adversarial refused on the flat branch, one pool read, bins written only after the proofs pass"
      contains: "_adversarial_pool"
    - path: "tests/test_phase25_wr.py"
      provides: "four watched refusals, each RED observed before the fix"
      contains: "def test_wr01_a_negative_ratio_is_refused_on_both_branches"
  key_links:
    - from: "scripts/teach_persona.py::_mix_adversarial"
      to: "scripts/phase24_adversarial.py::_adversarial_pool"
      via: "ONE paired read replacing two index-zipped reads (WR-06)"
      pattern: "_adversarial_pool"
    - from: "scripts/teach_persona.py::build_bins"
      to: "the on-disk token and mask bins"
      via: "tofile moved AFTER proof 1 and _prove_floor_and_band (WR-08)"
      pattern: "tofile"
---

<objective>
Repair the four Phase-24 review defects that would corrupt a sweep point, before any sweep point
exists — and record the three deferred ones with their unreachability in source rather than silencing
them.

Purpose: `teach_persona.build_bins` and `_mix_adversarial` are the single production entry every one
of the 12 adversarial points goes through. Three of the four defects produce a bin that is wrong in a
way no downstream assertion catches: a negative ratio builds the control under an adversarial name; a
replay+adversarial pair makes `replay_ratio` stop describing the bin, which is precisely what D-34
asserts against at write time; and an index-paired pool read would corrupt D-36's per-family counts.
The fourth writes bytes before the proofs that decide whether the bytes should exist. All four are
CPU-testable in milliseconds. **Every line number in `24-REVIEW.md` is stale by roughly +24 — resolve
each site by content.**

Output: four repairs in `scripts/teach_persona.py` and `tests/test_phase25_wr.py` with four watched
REDs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-frontier-sweep-and-the-existence-gate-verdict/25-CONTEXT.md
@.planning/phases/25-frontier-sweep-and-the-existence-gate-verdict/25-RESEARCH.md
@.planning/phases/25-frontier-sweep-and-the-existence-gate-verdict/25-VALIDATION.md
</context>

<environment>
**Python 3.11 venv is MANDATORY.** `.venv/bin/python -m pytest ...`. Baseline **1647 passed, 1 skipped**.

**`scripts/teach_persona.py` is NOT frozen** — it is production code this phase repairs. The four
ancestry-guarded modules (`mitigation_gate.py`, `mitigation_accountant.py`, `mitigation_unit.py`,
`phase18_extraction.py`) must stay byte-unchanged.

**24-06's byte-identity baseline must survive.** `build_bins` at its defaults is byte-identical to the
pre-`adversarial_ratio` build: token digest `f146d426…`, mask digest `a2c4771f…`, 176 episodes /
7,581 tokens. Every existing test asserting that identity must still pass — the repairs are refusals
and an ordering change, not a change to what a *valid* call produces.

**`tests/test_phase24_*.py` must all stay green.** 24-06's ten wiring tests and 24-07's four-corner
band check run against this file.
</environment>

<tasks>

<task type="auto">
  <name>Task 1: WR-01 and WR-04 — the ratio domain agreed across both branches, and replay+adversarial refused on the flat branch</name>
  <files>scripts/teach_persona.py</files>
  <read_first>
    - `scripts/teach_persona.py` — `build_bins`. Resolve these sites **by content, not by line
      number**: the flat branch's `if adversarial_ratio > 0:` guard immediately before the
      `_mix_adversarial(...)` call; the `if replay_ratio > 0:` guard immediately before
      `_prepend_replay(...)`; and the aligned branch's two `SystemExit` refusals, which test
      `if replay_ratio:` and `if adversarial_ratio:` — **truthiness, not a domain check**
    - `scripts/teach_persona.py` — `_build_aligned_bins(tok, episodes, bin_path, mask_path,
      replay_ratio, align_facts, adversarial_ratio=0.0)`, the aligned twin whose refusal text already
      states the D-10 reason replay lives outside the teaching bin
    - `scripts/mitigation_budget.py` — `ADVERSARIAL_RATIO_GRID = (0.0, 0.25, 0.5, 1.0, 1.5,
      1.9090909090909092)`, **len 6**. The legal domain is exactly this tuple's closed range; the grid
      is the pin, so the refusal must reference it rather than inventing a bound
    - `.planning/phases/24-adversarial-extraction-aware-training-the-held-out-attack-fa/24-REVIEW.md`
      §Warnings — WR-01 and WR-04's **reasoning**. **Ignore every line number in that file**
    - `tests/test_phase24_grid.py` and `tests/test_phase24_split.py` — the existing tests that must
      stay green
  </read_first>
  <action>
**(a) WR-01 — one domain check, before either branch.** Add a single validation at the top of
`build_bins`, before the flat/aligned split, that refuses a non-finite or negative
`adversarial_ratio` and a non-finite or negative `replay_ratio` with `SystemExit`. Use
`math.isfinite` so NaN and ±inf are both caught; NaN is the load-bearing case, because
`float('nan') > 0` is `False` **and** `bool(float('nan'))` is `True`, so the two existing guards
disagree on exactly that value. The message must state the class explicitly: *the guard refuses a NAME
where the harm is a PROPERTY* — a negative or NaN ratio silently builds the CONTROL on the flat branch
while the arm is named `adv_n8`/`adv_n64`, which is the failure Phase 20 recorded twice. Name the
legal domain by reference to `mitigation_budget.ADVERSARIAL_RATIO_GRID`'s minimum and maximum rather
than by a fresh literal.

Then make the two branch guards agree: the flat branch keeps `> 0` semantics for *dispatch* (0.0 is
inert by design and must stay byte-identical), and the aligned branch's truthiness tests become the
same explicit `> 0` comparison so the two branches cannot disagree on any value that reaches them.

**(b) WR-04 — the flat branch refuses replay and adversarial together.** The aligned branch already
refuses the pair with a message naming D-10 and `grad_accum_steps = n_facts`. The flat branch runs
`_mix_adversarial` and then `_prepend_replay`, so after both, `replay_ratio` no longer describes the
bin. Add the mirrored refusal on the flat branch, raised **before** either mixer runs. Its message
must name D-34: every point record carries `records_per_lot` and `composed_lot_sizes` read live at
write time and asserted against the pin under exact equality, so a bin whose composition the recorded
`replay_ratio` does not describe would either halt the sweep late or, worse, publish an ε that does
not describe what happened. State that no sweep point in this phase sets both — `arm_spec` gives DP
arms a replay ratio and adversarial arms an adversarial ratio — so the refusal costs nothing and
closes the combination that would be silently wrong.

Do not change what a valid call produces. `adversarial_ratio=0.0` and `replay_ratio=0.0` must remain
byte-identical to today.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase24_grid.py tests/test_phase24_split.py tests/test_phase24_record.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -c "import ast;t=ast.parse(open('scripts/teach_persona.py').read());fn=[f for f in ast.walk(t) if isinstance(f,ast.FunctionDef) and f.name=='build_bins'][0];assert any(isinstance(n,ast.Attribute) and n.attr=='isfinite' for n in ast.walk(fn));print('domain check present')"` exits 0
    - `.venv/bin/python -c "import ast;t=ast.parse(open('scripts/teach_persona.py').read());fn=[f for f in ast.walk(t) if isinstance(f,ast.FunctionDef) and f.name=='build_bins'][0];bare=[c for c in ast.walk(fn) if isinstance(c,ast.If) and isinstance(c.test,ast.Name) and c.test.id in ('adversarial_ratio','replay_ratio')];assert not bare, [c.lineno for c in bare];print('no truthiness guards remain')"` exits 0
    - `.venv/bin/python -m pytest tests/test_phase24_grid.py tests/test_phase24_split.py tests/test_phase24_record.py -q` reports 0 failed
    - `.venv/bin/python -m pytest tests/ -q` reports 0 failed; record the `N passed, M skipped` line
    - The SUMMARY records the resolved-by-content location of each edited site as `symbol + surrounding expression`, and states explicitly that **no line number was copied from `24-REVIEW.md`**
    - `git diff --exit-code -- scripts/mitigation_gate.py scripts/mitigation_accountant.py scripts/mitigation_unit.py scripts/phase18_extraction.py` exits 0
  </acceptance_criteria>
  <done>A NaN or negative ratio is refused before either branch dispatches, both branches test the same comparison, and the flat branch refuses replay+adversarial with a message naming D-34's live mechanism assertion.</done>
</task>

<task type="auto">
  <name>Task 2: WR-06 and WR-08 — one paired pool read, and bins written only after the proofs pass</name>
  <files>scripts/teach_persona.py</files>
  <read_first>
    - `scripts/teach_persona.py` — `_mix_adversarial`. Resolve by content: the two consecutive lines
      calling `pa.adversarial_episodes(tok)` and `pa.adversarial_episode_families(tok)`, the
      `len(families) != pool_size` check between them whose own message says these two views are
      *"read POSITIONALLY and paired by index"*, and the `n_want = int(round(adversarial_ratio *
      n_clean))` line with its `n_want < 1` refusal below it
    - `scripts/phase24_adversarial.py` — `_adversarial_pool(tok)`, the function that returns the
      PAIRED views, and the view accessors built on it. `HELD_OUT_FAMILY = "A2"`,
      `TRAINED_FAMILIES = ('A1-mild','A1-aggressive','A3')`
    - `scripts/teach_persona.py` — `build_bins`'s write block: `ids_all.tofile(bin_path)` and
      `mask_all.tofile(mask_path)`, then proof 1 (`len(ids_all) != len(mask_all)`), then
      `_prove_floor_and_band(ids_all, mask_all)`. **The writes precede both proofs.** The aligned twin
      `_build_aligned_bins` has the same inversion — fix both
    - `scripts/teach_persona.py` — `refuse_if_exists` and `arm_outputs`, so the fix is consistent with
      the refusal that already guards re-runs
    - `scripts/phase25_prereg.py` (from plan 25-01) — **read if present: same-wave, not a dependency.**
      25-01 is wave 1 alongside this plan, so the file may not exist yet and nothing here is
      load-bearing on it. When present it states D-10's one-attempt rule, whose evidence is the
      committed record; a refused build that already wrote bytes muddies exactly that. When absent,
      D-10 in `25-CONTEXT.md` says the same thing and is the source 25-01 works from
  </read_first>
  <action>
**(a) WR-06 — one read, paired at the source.** Replace the two independent
`pa.adversarial_episodes(tok)` / `pa.adversarial_episode_families(tok)` calls in `_mix_adversarial`
with a single `pa._adversarial_pool(tok)` call returning the paired views, and derive both local
variables from that one result. Keep the length check as a defensive `_prove` but rewrite its message:
it now asserts an invariant of a single paired read rather than the agreement of two independent ones.
State in a comment why this matters concretely: the two views are paired **by index**, D-36 computes
`held_out_generalization` from per-family counts across all 44 points, and a one-off pairing would
mislabel every family count in the artifact without any assertion firing.

Leave `n_want` and its `n_want < 1` refusal exactly as they are, and add a comment recording D-41's
deferral: **WR-02 and WR-03 are DEFERRED, unreachability RECORDED not silenced** — the `n_want < 1`
path is unreachable at 176 clean episodes (n=8) and 1,408 (n=64) given
`ADVERSARIAL_RATIO_GRID`'s smallest non-zero entry `0.25`. Write the two products (`round(0.25*176)`
and `round(0.25*1408)`) into the comment as the reason, so the deferral carries its own evidence.

**(b) WR-08 — proofs before bytes, on both branches.** Move `ids_all.tofile(bin_path)` and
`mask_all.tofile(mask_path)` to run **after** proof 1 and after `_prove_floor_and_band` on the flat
branch, and make the identical move in `_build_aligned_bins`. Nothing else changes: the same arrays,
the same paths, the same order relative to each other. Add a comment naming the two collisions this
closes — `refuse_if_exists` (a refused re-run finding bytes a failed build left behind) and D-10 (a
point whose bins exist while no reading landed).

Verify the byte-identity baseline survives: at `adversarial_ratio=0.0, replay_ratio=0.0` the emitted
token digest must still be `f146d426…` and the mask digest `a2c4771f…` over 176 episodes and 7,581
tokens. If any existing 24-06 identity test moves, stop — the reordering was not behaviour-preserving
and the cause must be found before proceeding.

**(c) WR-07's deferral, recorded.** Add a one-line comment at the test-facing boundary naming WR-07 as
deferred: a test re-declares the trained/held-out split as literals — hygiene only, no effect on any
sweep number. Do not fix it in this plan.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase24_grid.py tests/test_phase24_split.py tests/test_phase24_record.py tests/test_phase23_resume.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -c "import ast;t=ast.parse(open('scripts/teach_persona.py').read());fn=[f for f in ast.walk(t) if isinstance(f,ast.FunctionDef) and f.name=='_mix_adversarial'][0];calls=[n.func.attr for n in ast.walk(fn) if isinstance(n,ast.Call) and isinstance(n.func,ast.Attribute)];assert calls.count('adversarial_episodes')==0 and calls.count('adversarial_episode_families')==0 and calls.count('_adversarial_pool')==1, calls;print('one paired read')"` exits 0
    - `.venv/bin/python -c "import ast;t=ast.parse(open('scripts/teach_persona.py').read());f={n.name:n for n in ast.walk(t) if isinstance(n,ast.FunctionDef)};ck=lambda fn,p:[n.lineno for n in ast.walk(fn) if isinstance(n,ast.Call) and (getattr(n.func,'attr',None)==p or getattr(n.func,'id',None)==p)];r=[(k,ck(f[k],'tofile'),ck(f[k],'_prove_floor_and_band')) for k in ('build_bins','_build_aligned_bins')];assert all(a and b and min(a)>max(b) for _,a,b in r), r;print('proofs precede bytes on both branches', r)"` exits 0. **Measured at HEAD before the fix this exits 1 with `[('build_bins', [583, 584], [593]), ('_build_aligned_bins', [782, 783, 784], [794])]` — that is WR-08's natural RED; quote it in the SUMMARY**
    - `.venv/bin/python -m pytest tests/test_phase24_grid.py tests/test_phase24_split.py tests/test_phase24_record.py -q` reports 0 failed — **the 24-06 byte-identity baseline survived**
    - The SUMMARY quotes the token and mask digests produced by a fresh `adversarial_ratio=0.0` build and states they equal `f146d426…` / `a2c4771f…`
    - `.venv/bin/python -c "import ast;src=open('scripts/teach_persona.py').read();t=ast.parse(src);print('WR-02' in src and 'WR-03' in src and 'WR-07' in src)"` prints `True` — the three deferrals are recorded in source
    - `.venv/bin/python -m pytest tests/ -q` reports 0 failed; record the `N passed, M skipped` line
    - `git diff --exit-code -- scripts/mitigation_gate.py scripts/mitigation_accountant.py scripts/mitigation_unit.py scripts/phase18_extraction.py` exits 0
  </acceptance_criteria>
  <done>`_mix_adversarial` reads the pool once through `_adversarial_pool`, both branches write bins only after the 1:1 check and the floor/band proof, and the 24-06 byte-identity digests are unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: `tests/test_phase25_wr.py` — four watched refusals, each RED observed before its fix</name>
  <files>tests/test_phase25_wr.py</files>
  <read_first>
    - `scripts/teach_persona.py` — the four repaired sites from Tasks 1 and 2
    - `tests/test_phase24_grid.py:1-45` — the register for a `teach_persona` unit test that does not
      need a GPU: a tiny fixture corpus, a `tmp_path` bin destination, and refusals asserted on
      discriminating substrings
    - `tests/test_phase24_split.py:29` — the `phase18_extraction` ancestry assertion, so the new file
      does not duplicate it
    - `scripts/phase24_adversarial.py` — `_adversarial_pool` and `HELD_OUT_FAMILY`, for the WR-06 test's
      expected pairing
    - `25-RESEARCH.md` ledger row 5a — the four defects located by content at HEAD, and the warning
      that every 24-REVIEW line number is stale
  </read_first>
  <action>
Create `tests/test_phase25_wr.py`. CPU-only. Each of the four tests must be **watched RED before the
fix is in place** — the executor runs the test against a `git stash`-ed working tree or a scratch copy
of the pre-fix function, records the failure verbatim, then confirms GREEN. Record which form was used
per test in the SUMMARY.

**(a) WR-01, both branches.** `test_wr01_a_negative_ratio_is_refused_on_both_branches` and
`test_wr01_a_nan_ratio_is_refused_on_both_branches`, each parametrized over the flat and the aligned
call shape. NaN is the load-bearing case and its docstring must say why: `float('nan') > 0` is `False`
while `bool(float('nan'))` is `True`, so the two pre-fix guards disagreed on exactly that value, and
the flat branch would have built the CONTROL under an adversarial arm name. Assert `SystemExit` and
that the message contains both the offending value and a reference to
`ADVERSARIAL_RATIO_GRID`'s bounds.

**(b) WR-04.** `test_wr04_replay_and_adversarial_together_are_refused_on_the_flat_branch` — assert
`SystemExit` and that the message names `replay_ratio`, `adversarial_ratio` and the live-mechanism
consequence. Add a companion asserting the aligned branch's pre-existing refusal still fires, so the
two branches are proved symmetric rather than assumed.

**(c) WR-06.** `test_wr06_the_adversarial_pool_is_read_exactly_once` — AST over
`_mix_adversarial`'s body counting `ast.Attribute.attr` call names, asserting `_adversarial_pool`
appears once and the two separate accessors zero times. Plus a behavioural companion,
`test_wr06_family_labels_stay_paired_with_their_episodes` — monkeypatch
`phase24_adversarial._adversarial_pool` to return a pool whose family labels are deliberately
permuted relative to the episodes and assert the per-family counts in `build_bins`' returned `stats`
change accordingly, so the test proves the pairing is load-bearing rather than decorative.

**(d) WR-08.** `test_wr08_no_bytes_land_when_a_proof_fails` — force `_prove_floor_and_band` to raise
(monkeypatch) and assert that neither `bin_path` nor `mask_path` exists afterwards in `tmp_path`.
**This is the natural RED**: before the fix the files exist. Add
`test_wr08_the_same_holds_on_the_aligned_branch`.

**(e) The deferrals, asserted as recorded rather than fixed.**
`test_wr02_wr03_wr07_are_recorded_as_deferred_with_their_reason` — read
`scripts/teach_persona.py`, match the three markers through `scripts/_prose.normalized` (the comments
line-wrap), and assert the WR-02/WR-03 comment carries both computed products
(`round(0.25 * 176)` and `round(0.25 * 1408)`) so the unreachability claim carries its own arithmetic.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase25_wr.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_phase25_wr.py -v` exits 0 with **zero skipped**; quote the `N passed, M skipped` line
    - The SUMMARY quotes **four** verbatim pre-fix failure messages — one per WR — and names for each whether the RED was observed against a stashed tree or a scratch copy of the pre-fix function
    - `.venv/bin/python -m pytest tests/test_phase25_wr.py -k "wr01" -v` collects at least 4 cases (2 tests × 2 branches) and all pass
    - `.venv/bin/python -c "import ast;t=ast.parse(open('tests/test_phase25_wr.py').read());ns=sorted(f.name for f in ast.walk(t) if isinstance(f,ast.FunctionDef) and f.name.startswith('test_'));assert sum(n.startswith('test_wr0') for n in ns)>=6, ns;print(ns)"` exits 0
    - `.venv/bin/python -m pytest tests/test_phase24_grid.py tests/test_phase24_split.py tests/test_phase24_record.py -q` reports 0 failed
    - `.venv/bin/python -m pytest tests/ -q` reports 0 failed; record the `N passed, M skipped` line and state the delta against the 1647/1 baseline
    - `git diff --exit-code -- scripts/mitigation_gate.py scripts/mitigation_accountant.py scripts/mitigation_unit.py scripts/phase18_extraction.py pyproject.toml` exits 0; `make lint` exits 0
  </acceptance_criteria>
  <done>All four repaired refusals have been watched failing before the fix and passing after, the pool pairing is proved load-bearing by permutation, and no bytes land when a proof fails on either branch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the sweep's ratio parameter → the bin actually built | a NaN or negative ratio crosses into `build_bins` and silently produces the control under an adversarial arm name |
| two positionally-paired corpus views → the artifact's per-family counts | an index mismatch corrupts D-36's `held_out_generalization` with no assertion firing |
| a failed build → the filesystem | bytes written before the proofs make `refuse_if_exists` and D-10's one-attempt rule read a state that never validated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-16 | Spoofing | a control bin published under an `adv_n8` / `adv_n64` arm name because the ratio was NaN or negative | mitigate | one `math.isfinite` domain check before either branch dispatches, bounds referenced from `ADVERSARIAL_RATIO_GRID`; NaN's `> 0` / truthiness disagreement asserted in a named test |
| T-25-17 | Information Disclosure | an ε published against a bin whose recorded `replay_ratio` does not describe it | mitigate | the flat branch refuses replay+adversarial together, mirroring the aligned branch; the message names D-34's live mechanism assertion |
| T-25-18 | Tampering | mislabelled per-family counts flowing into `held_out_generalization` | mitigate | one paired `_adversarial_pool` read, proved by AST, plus a permutation test proving the pairing is load-bearing |
| T-25-19 | Repudiation | a refused or failed build leaving bins on disk that a later run reads as a completed one | mitigate | `tofile` moved after proof 1 and `_prove_floor_and_band` on both branches; a monkeypatched proof failure is asserted to leave no files |
| T-25-20 | Tampering | a repair introducing a silent change to what a valid call produces | mitigate | the 24-06 byte-identity digests `f146d426…` / `a2c4771f…` are re-measured and quoted; the whole `tests/test_phase24_*` battery must stay green |
| T-25-SC | Tampering | npm/pip/cargo installs | accept | Zero installs; `pyproject.toml` byte-unchanged (RPT-03) |
</threat_model>

<verification>
```bash
.venv/bin/python -m pytest tests/test_phase25_wr.py -v
.venv/bin/python -m pytest tests/test_phase24_grid.py tests/test_phase24_split.py tests/test_phase24_record.py -q
.venv/bin/python -m pytest tests/ -q                       # record "N passed, M skipped"
git diff --exit-code -- scripts/mitigation_gate.py scripts/mitigation_accountant.py scripts/mitigation_unit.py scripts/phase18_extraction.py pyproject.toml
make lint
```
</verification>

<success_criteria>
- A NaN or negative ratio is refused once, before either branch, with the NaN disagreement asserted explicitly
- The flat branch refuses replay+adversarial together and the aligned branch's refusal still fires
- `_mix_adversarial` reads the pool exactly once via `_adversarial_pool`, proved by AST and by a permutation test
- No bytes land on either branch when a proof fails
- WR-02, WR-03 and WR-07 are recorded as deferred in source, WR-02/03 with their computed unreachability
- Four verbatim pre-fix failure messages are recorded, and the 24-06 byte-identity digests are unchanged
</success_criteria>

<output>
Create `.planning/phases/25-frontier-sweep-and-the-existence-gate-verdict/25-04-SUMMARY.md` when done.
</output>
