---
phase: 25-frontier-sweep-and-the-existence-gate-verdict
plan: 16
type: execute
wave: 9
depends_on: ["25-15"]
files_modified:
  - results/phase25_extremes_log.json
  - tests/test_phase25_extremes.py
autonomous: true
requirements: [FRONT-01, ADVT-01]

must_haves:
  truths:
    - "D-15: the EIGHT extremes run before any interior point, INTERLEAVED across the four legs — DP x {n8, n64} at sigma->0 and at the never-taught end, adversarial x {n8, n64} at ratio 0.0 and 1.9090909090909092. NOT one leg exhausted before the next. A structural problem at any of the four corners appears on day one."
    - "The low extremes are ALREADY DONE: the two sigma->0 points are D-01's controls, committed in plan 25-15. This plan runs the six that remain — two DP high extremes and four adversarial extremes — and records the interleave order for all eight."
    - "ADVT-01 IS SATISFIED BY THIS PLAN, not by Phase 24. The requirement's subject is *the adapter trained* against the attack suite with intensity as the sweep axis; Phase 24 built the seam and declared ADVT-01 unsatisfied in its own traceability row. The first adv_n8 / adv_n64 adapters at a non-zero ratio land here."
    - "D-19: the adversarial upper extreme 1.9090909090909092 is the POOL CEILING — the largest ratio at which the whole trained pool is used exactly once. It is an axis terminus, not a floor-reaching parameter, and its distance from the never-taught floor is a NAMED STRUCTURAL ASYMMETRY."
    - "D-22: the six adversarial ratios are IDENTICAL at both capacities. `multiplicity_at_upper_extreme` reaches 8.0x at adv_n64 — n=64 repeats the whole pool eight times where n=8 passes it once — and that is a REPORTED PROPERTY, not a grid variable."
    - "D-10: each point's committed record is its one-attempt evidence; each shape block's sha256 is committed as it lands, closing the delete-and-redraw leak since `data/` is gitignored."
    - "D-34 fires at every write: five mechanism fields read live and compared under exact equality, halting the WHOLE sweep on any divergence."
  artifacts:
    - path: "results/phase25_extremes_log.json"
      provides: "the eight extreme point keys, the interleave order actually executed, and the four-corner findings"
      contains: "interleave_order"
  key_links:
    - from: "results/phase25_extremes_log.json"
      to: "results/phase25_point_*.json"
      via: "one committed point record per extreme, referenced by key and sha256"
      pattern: "phase25_point_"
    - from: "the four adversarial extremes"
      to: "results/phase24_token_budget.json"
      via: "the four-corner band clearance 24-07 measured build-only is now confirmed by real trained adapters"
      pattern: "phase24_token_budget"
---

<objective>
Run the six remaining extreme points, interleaved across the four legs, so that a structural problem at
any corner of the sweep appears on day one instead of on day five.

Purpose: D-15's reason is scheduling as risk management. An empty frontier is the pre-registered null
and is live — ε is 519.698 at σ=0.5 — so most of the 44 points are expected to fail the three
conditions. What is *not* expected is a corner that cannot be run at all, and running one leg to
exhaustion before touching the next would hide that for days. The four corners are the two DP high
extremes and the four adversarial extremes; the two σ→0 low extremes are already committed as D-01's
controls.

This is also where **ADVT-01 becomes satisfiable**. Phase 24 built the mixture seam, the 336-episode
pool, the pinned grid and the four-corner band clearance, and declared ADVT-01 unsatisfied in its own
traceability row because no adapter had been trained. The first `adv_n8` and `adv_n64` adapters at a
non-zero ratio land here.

Output: six committed point records, an interleave log, and its guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-frontier-sweep-and-the-existence-gate-verdict/25-CONTEXT.md
@.planning/phases/25-frontier-sweep-and-the-existence-gate-verdict/25-RESEARCH.md
@.planning/phases/25-frontier-sweep-and-the-existence-gate-verdict/25-VALIDATION.md
</context>

<environment>
**Python 3.11 venv is MANDATORY.** Venue: local M3 / MPS, torch 2.7.1, fp32.

**Runs under the LaunchAgent from plan 25-14** with the heartbeat and the detect-never-act watcher
live. `PERSONACORE_SWEEP_ACTIVE=1` is set by the agent, so any `make test` during the run skips the MPS
legs loudly at the count committed in plan 25-06.

**Approximate spend: ≈ 18–22 h** for six points at the measured 2.03 h/point observed rate to the
3.15 h/point ceiling, plus training. High-σ points sit near the **ceiling** because a noised adapter
that stops emitting EOS runs every draw to the full token budget.

**`prove_reproduction` has already passed** (plan 25-15). If it had not, no point in this plan would be
permitted to run.

**The four ancestry-guarded modules and `scripts/mitigation_budget.py` must stay byte-unchanged.**
</environment>

<tasks>

<task type="auto">
  <name>Task 1: The two DP high extremes, interleaved with the four adversarial extremes</name>
  <files>results/phase25_extremes_log.json</files>
  <read_first>
    - `scripts/mitigation_budget.py` — `SIGMA_LADDER` (the top rung is the DP high extreme at both
      capacities), `EPSILON_LADDER`, `CLIP_NORM`, `ADVERSARIAL_RATIO_GRID` (`0.0` and
      `1.9090909090909092` are the adversarial extremes), `CURVE_K = 16`
    - `scripts/phase25_run.py` — `run_point`, `ORDERED_POINT_KEYS`, the resume port, the heartbeat, and
      `commit_point_record`
    - `scripts/phase25_record.py` — `build_point_record`, `MECHANISM_PIN_FIELDS`, `point_key`
    - `scripts/teach_persona.py` — `ADV_ARMS = ("adv_n8", "adv_n64")`, `DP_ARMS`, `arm_spec`,
      `train_arm`'s real signature, and the four WR repairs from plan 25-04 (the domain check, the
      flat-branch replay+adversarial refusal, the single `_adversarial_pool` read, proofs-before-bytes)
    - `results/phase24_token_budget.json` — the 12 committed rows, `mask_fraction` at the four corners
      (`adv_n8` upper 0.241009, `adv_n64` upper 0.251734), `cross_family_inflation` 3.73×, and
      `multiplicity_at_upper_extreme` reaching **8.0×** at `adv_n64`
    - `results/phase25_adversarial_throughput.json` — the measured per-extreme rates and the finalised
      schedule from plan 25-11, so the observed wall-clock can be compared against its own projection
    - `results/phase25_sigma_hi_probe.json` — the probed high anchor and the ratchet extension rule
    - `results/phase23_never_taught.json` — the shared floor both arms are read against (D-19)
  </read_first>
  <action>
Run six points through `scripts/phase25_run.py` under the LaunchAgent, **interleaved across the four
legs**. The order must alternate legs rather than exhaust one — for example DP-n8-high, adv-n8-0.0,
DP-n64-high, adv-n64-0.0, adv-n8-upper, adv-n64-upper. Record the order actually executed; the exact
permutation is discretionary, the interleaving is not.

**(a) The two DP high extremes.** At `SIGMA_LADDER[-1]` (the probed anchor) with
`dp_clip_norm=mitigation_budget.CLIP_NORM`, at both capacities. Their ε comes from
`EPSILON_LADDER[-1]`; assert it matches `epsilon_for` live before writing. Expect these points near the
**ceiling** rate, not the floor — record the observed wall-clock and the `stop_terminated_n` per shape
so the regime is measured rather than assumed.

**(b) The four adversarial extremes.** `adv_n8` and `adv_n64` at ratio `0.0` and at
`1.9090909090909092`. For each, record from the build's own `stats`: `mask_fraction`,
`adversarial_family_counts` across the three trained families, `episodes`, `tokens`,
`teaching_tokens`, and the multiplicity at that corner. Assert `mask_fraction` lands in the band
24-07 measured build-only — the four-corner check is now confirmed by **real trained adapters** rather
than by a build.

**(c) `accounting: null` on every adversarial record.** The adversarial arm has no σ, δ or q; the null
is the structural statement that it makes no formal claim, and `phase25_record` writes it. Also record
`epsilon: null` with its omission reason for the adversarial points, for the same reason the control
carries one — a missing key is indistinguishable from a bug.

**(d) D-19's named asymmetry, in the artifact.** Each adversarial record carries an
`axis_terminus` block: `1.9090909090909092` is the **pool ceiling**, the largest ratio at which the
whole trained pool is used exactly once; the adversarial axis terminates there by construction rather
than reaching the never-taught floor; and both arms are read against the **same** already-measured
floor (0/416, 5 seeds) as the plane's shared lower-left reference.

**(e) D-22's multiplicity as a reported property.** Record `multiplicity_at_upper_extreme` per
adversarial point from the measured build, keyed on `adv_n8` / `adv_n64` (the WR-05 corrected key), and
state that it is reported, not a grid variable — the six ratios are identical at both capacities.

**(f) The four-corner findings.** Write `results/phase25_extremes_log.json` carrying: the eight extreme
point keys (including the two controls from plan 25-15, referenced by key and record sha256), the
`interleave_order` actually executed with timestamps, the observed wall-clock per point against plan
25-11's projection, and a `four_corner_findings` list — anything structural that appeared, or the
explicit statement that nothing did. Commit each point record in its own single-path commit as it
lands; commit the log last.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase25_extremes.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-files 'results/phase25_point_*.json' | wc -l` returns `8` — the six new extremes plus the two controls
    - `.venv/bin/python -c "import json;b=json.load(open('results/phase25_extremes_log.json'));o=b['interleave_order'];legs=[e['leg'] for e in o];assert not any(legs[i]==legs[i+1] for i in range(len(legs)-1)), legs;print(legs)"` exits 0 — **no leg ran twice in a row**
    - `.venv/bin/python -c "import json;b=json.load(open('results/phase25_extremes_log.json'));assert len(b['extreme_point_keys'])==8 and all(e['record_sha256'] for e in b['extreme_point_keys']);print(len(b['extreme_point_keys']))"` prints `8`
    - `.venv/bin/python -c "import glob,json;\nfor p in glob.glob('results/phase25_point_adv_*.json'):\n    b=json.load(open(p));assert 'accounting' in b and b['accounting'] is None, p;assert 'epsilon' in b and b['epsilon'] is None, p;assert b['axis_terminus']['pool_ceiling']==1.9090909090909092, p\nprint('adversarial records: accounting null, epsilon null, pool ceiling named')"` exits 0
    - `.venv/bin/python -c "import glob,json;rows=[(p, json.load(open(p))['mask_fraction']) for p in glob.glob('results/phase25_point_adv_*.json')];print(rows);assert all(f>0.15 for _,f in rows)"` exits 0 — the band 24-07 measured build-only holds on **real trained adapters**
    - `.venv/bin/python -c "import glob,json,sys;sys.path.insert(0,'scripts');sys.path.insert(0,'src');from personacore.privacy.accountant import epsilon_for;import mitigation_budget as b, mitigation_unit as u;\nfor p in glob.glob('results/phase25_point_dp_*.json'):\n    r=json.load(open(p))\n    if r['epsilon'] is None: continue\n    assert r['epsilon']==epsilon_for(r['sigma'], b.STEP_BUDGET, u.DELTA), p\nprint('every published epsilon re-derives live')"` exits 0
    - Every new record's commit names exactly **one** path, verified with `git show --stat` per commit
    - The SUMMARY quotes the observed wall-clock per extreme against plan 25-11's projection, and the `stop_terminated_n` totals for the two DP high extremes
    - `git diff --exit-code -- scripts/mitigation_budget.py scripts/mitigation_gate.py scripts/mitigation_accountant.py scripts/mitigation_unit.py scripts/phase18_extraction.py` exits 0
  </acceptance_criteria>
  <done>Six extreme points are committed with no leg running twice in a row, every adversarial record carries `accounting: null` and the named pool ceiling, the four-corner mask-fraction band holds on real adapters, and the interleave order and observed rates are logged against their projections.</done>
</task>

<task type="auto">
  <name>Task 2: `tests/test_phase25_extremes.py` — the interleave, the corners, and ADVT-01's satisfaction all asserted from artifacts</name>
  <files>tests/test_phase25_extremes.py</files>
  <read_first>
    - `results/phase25_extremes_log.json` and the eight `results/phase25_point_*.json` records
    - `scripts/mitigation_budget.py` — `SIGMA_LADDER`, `EPSILON_LADDER`, `ADVERSARIAL_RATIO_GRID`,
      `CURVE_K`, and `ADVERSARIAL_RATIO_GRID_PROVENANCE`'s corrected
      `multiplicity_at_upper_extreme_corrected`
    - `results/phase24_token_budget.json` — the build-only four-corner band figures this plan's real
      adapters are compared against
    - `scripts/phase25_prereg.py` — `prove_first_attempt`, `POINT_RECORD_GLOB`
    - `scripts/phase25_record.py` — `MECHANISM_PIN_FIELDS`, `ORDERED_POINT_KEYS`
    - `results/phase23_never_taught.json` — the shared floor, for D-19's assertion
  </read_first>
  <action>
Create `tests/test_phase25_extremes.py`. CPU-only: it reads committed records and never retrains.

**(a) D-15's interleave, asserted structurally.**
  - `test_no_leg_ran_twice_consecutively` — over `interleave_order`'s `leg` sequence
  - `test_all_four_legs_appear_before_any_leg_finishes` — every one of the four legs has at least one
    completed point before any leg has all of its extremes done
  - `test_every_extreme_precedes_every_interior_point` — from `git log` timestamps over the point
    records: no record whose key is an interior rung has a commit older than the newest extreme's. **This
    is the assertion that makes D-15 a mechanism rather than an intention**, and it also guards plan
    25-17 from starting early

**(b) The corners hold.**
  - `test_the_mask_fraction_band_holds_on_real_adapters` — parametrized over the four adversarial
    extremes; each `mask_fraction` clears the floor `24-07` pinned, and the record's measured value is
    compared against `results/phase24_token_budget.json`'s build-only figure with the delta reported in
    the failure message
  - `test_all_three_trained_families_are_present_within_one` — from
    `adversarial_family_counts`, at every non-zero adversarial extreme
  - `test_the_held_out_family_is_absent_from_training` — A2 does not appear in
    `adversarial_family_counts`, and does appear in the scored per-family counts. **The split is
    exercised, which is what ADVT-02 promised and this plan uses**

**(c) D-19 and D-22, asserted from the artifact.**
  - `test_the_pool_ceiling_is_named_as_an_axis_terminus` — every adversarial record's `axis_terminus`
    block names `1.9090909090909092` and states the asymmetry, matched through `_prose.normalized`
  - `test_both_arms_reference_the_same_never_taught_floor` — the floor record path and sha256 recorded
    in a DP record equal those in an adversarial record
  - `test_the_six_ratios_are_identical_at_both_capacities` — the adversarial point keys at n=8 and n=64
    project onto the same ratio set, read from `ADVERSARIAL_RATIO_GRID`
  - `test_multiplicity_is_reported_not_swept` — the multiplicity appears as a record field and **not**
    as a component of any point key

**(d) Every ε re-derives, and no bare ε was printed.**
  - `test_every_published_epsilon_re_derives_from_its_sigma` — exact `==` against `epsilon_for`
  - `test_every_epsilon_bearing_reading_carries_its_k_inline` — D-21, on real records
  - `test_no_adversarial_record_carries_an_accounting_block` — `accounting is None` on all four

**(e) The one-attempt rule on eight real records.**
  - `test_every_point_record_has_exactly_one_commit` — parametrized over all eight
  - `test_every_commit_named_exactly_one_path` — parse `git show --stat` per commit; §O1's scoping
    verified across eight real unattended commits
  - `test_a_second_attempt_at_any_extreme_is_refused` — `prove_first_attempt` raises for each

**(f) ADVT-01's satisfaction, recorded.**
  - `test_adversarial_adapters_exist_at_a_non_zero_ratio` — at least one committed record has
    `arm` in `ADV_ARMS` and `adversarial_ratio > 0`, with a retained adapter sha256. Docstring: this is
    the fact Phase 24 structurally could not produce, and it is what makes ADVT-01 tickable
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase25_extremes.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_phase25_extremes.py -v` exits 0 with **zero skipped**; quote the `N passed, M skipped` line
    - `.venv/bin/python -m pytest tests/test_phase25_extremes.py -k "one_commit or exactly_one_path" -v` collects at least 16 cases (8 records × 2) and all pass
    - The SUMMARY quotes the four measured `mask_fraction` values from the real adapters beside 24-07's build-only figures, with the delta stated
    - The SUMMARY quotes the `adversarial_family_counts` at both upper extremes and states whether all three trained families are within one
    - The SUMMARY states in one sentence that **ADVT-01's subject — the adapter trained — now exists**, naming the record and its adapter sha256
    - `.venv/bin/python -m pytest tests/test_phase25_extremes.py -k "precedes_every_interior" -x` passes
    - `.venv/bin/python -m pytest tests/ -q` reports 0 failed with the sweep-active flag unset; record the `N passed, M skipped` line
    - `git diff --exit-code -- scripts/mitigation_budget.py scripts/mitigation_gate.py scripts/mitigation_accountant.py scripts/mitigation_unit.py scripts/phase18_extraction.py pyproject.toml` exits 0; `make lint` exits 0
  </acceptance_criteria>
  <done>The interleave is proved from the executed order and from commit timestamps, the four corners hold on real adapters against 24-07's build-only figures, D-19's asymmetry and D-22's reported multiplicity are asserted from the artifact, and eight real unattended commits each named exactly one path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a structural corner failure → five days of compute | running one leg to exhaustion hides a broken corner until the end; interleaving surfaces it on day one |
| a build-time band check → a real trained adapter | 24-07 measured the four corners build-only; only a trained adapter confirms it |
| an unattended commit → the repository on `main` | eight real commits land with no operator present |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-83 | Denial of Service | a broken corner discovered after days of interior points | mitigate | D-15's interleave asserted two ways — no leg twice consecutively in the executed order, and every extreme's commit older than every interior commit |
| T-25-84 | Tampering | a mask fraction outside the pinned band silently producing a degenerate adversarial bin | mitigate | the band is re-asserted on **real trained adapters** and compared against 24-07's build-only figures with the delta reported |
| T-25-85 | Information Disclosure | the adversarial arm read as making a formal privacy claim | mitigate | `accounting: null` and `epsilon: null` asserted present on all four adversarial records, and the pool-ceiling asymmetry named in each |
| T-25-86 | Spoofing | the held-out family leaking into training, invalidating ADVT-02's split | mitigate | A2 asserted absent from `adversarial_family_counts` and present in the scored per-family counts |
| T-25-87 | Repudiation | an extreme re-run after an unfavourable reading | mitigate | one commit per record asserted for all eight, `prove_first_attempt` watched refusing each |
| T-25-88 | Tampering | an unattended commit widening beyond the point record over six real commits | mitigate | `git show --stat` parsed per commit; exactly one path under `results/` each |
| T-25-SC | Tampering | npm/pip/cargo installs | accept | Zero installs; `pyproject.toml` byte-unchanged (RPT-03) |
</threat_model>

<verification>
```bash
.venv/bin/python -m pytest tests/test_phase25_extremes.py -v
git ls-files 'results/phase25_point_*.json' | wc -l           # must be 8
.venv/bin/python -m pytest tests/ -q                           # record "N passed, M skipped"
git diff --exit-code -- scripts/mitigation_budget.py scripts/mitigation_gate.py scripts/mitigation_accountant.py scripts/mitigation_unit.py scripts/phase18_extraction.py pyproject.toml
make lint
```
</verification>

<success_criteria>
- Six new extreme points are committed, giving eight extremes total with the two controls
- No leg ran twice consecutively, and every extreme's commit precedes every interior point's
- The mask-fraction band and the three-trained-family balance hold on real adapters, compared against 24-07's build-only figures
- Every adversarial record carries `accounting: null`, `epsilon: null` and the named pool-ceiling asymmetry
- Both arms reference the same never-taught floor record by path and sha256
- Every published ε re-derives from its σ under exact equality and carries its `k` inline
- Eight real unattended commits each named exactly one path
- ADVT-01's subject — a trained adversarial adapter at a non-zero ratio — exists and is named
</success_criteria>

<output>
Create `.planning/phases/25-frontier-sweep-and-the-existence-gate-verdict/25-16-SUMMARY.md` when done.
</output>
