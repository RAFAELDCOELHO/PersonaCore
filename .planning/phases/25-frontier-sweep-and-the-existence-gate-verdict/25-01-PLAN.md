---
phase: 25-frontier-sweep-and-the-existence-gate-verdict
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phase25_prereg.py
  - tests/test_phase25_prereg.py
autonomous: true
requirements: [CTRL-01, CTRL-02]

must_haves:
  truths:
    - "D-07: `prove_reproduction(k, n)` HALTS the sweep at ZERO points on a miss, under hard `==` on integer counts (k == 790, n == 1008) — a count has no tolerance."
    - "D-10: the one-attempt unit is the SWEEP POINT, not the sweep. A point with a committed record is never re-run; a point killed before any reading landed resumes as the SAME attempt."
    - "§C6 correction: `phase23_matched_prereg.prove_first_attempt` CANNOT be reused — its refusal message hard-codes `MATCHED_ARTIFACT_GLOB = 'results/phase23_matched_*'`, so a Phase-25 call emits a refusal naming the wrong glob, and that module is EDIT-ONCE and already spent."
    - "D-04: an armed tripwire FAILS if any later plan asserts bit-identity between the σ=0 point and the seam-off path. Resolution is by AST over the repo's own test and script bodies, never by grep."
    - "D-11: the promotion rule is committed BEFORE the curve runs — if more candidates clear than the budget holds, promote and replicate ALL of them, never a subset chosen after seeing which cleared."
    - "D-40: the publication obligation is committed BEFORE any point exists — the exact strings Phase 28's report must carry, so the report cannot be scoped after seeing the numbers."
    - "D-37(iii): the rule naming WHICH point Phase 26's canary audit targets is committed before any point exists; and only n=8 points have out-of-corpus canaries at all."
  artifacts:
    - path: "scripts/phase25_prereg.py"
      provides: "Phase 25's pre-registration: the reproduction HALT, the per-point one-attempt refusal, D-04's declared-differences import, and the four rules committed before any point exists"
      contains: "def prove_reproduction"
    - path: "tests/test_phase25_prereg.py"
      provides: "both branches of the reproduction gate watched, the one-attempt refusal watched, D-04's tripwire with a planted RED against a scratch copy"
      contains: "def test_reproduction_miss_halts_at_zero_sweep_points"
  key_links:
    - from: "scripts/phase25_prereg.py"
      to: "results/phase23_sigma_zero.json"
      via: "REPRODUCTION_K / REPRODUCTION_N read from the committed record, never retyped from prose"
      pattern: "phase23_sigma_zero"
    - from: "scripts/phase25_prereg.py"
      to: "results/phase23_matched_control.json"
      via: "declared_differences() imported by path + sha256 digest (D-04)"
      pattern: "declared_differences"
---

<objective>
Commit, before a single GPU second is spent, the four Phase-25 rules whose value depends entirely on
existing before the data does: the control's reproduction HALT, the per-point one-attempt refusal,
the promotion/replication rule, and the publication + canary-audit obligations.

Purpose: every one of these is worthless if written after the numbers exist. `prove_reproduction`
decides whether 43 further points are interpretable; `prove_first_attempt` decides whether a re-run
is evidence or laundering; D-11 decides which candidates get promoted *before* anyone knows which
cleared; D-40 decides what the report must say *before* anyone knows what it will say. This plan is
CPU-only and costs seconds — the whole point is that it is cheap and it is FIRST.

Output: `scripts/phase25_prereg.py` and `tests/test_phase25_prereg.py`, with three watched refusals
(two natural, one planted against a scratch copy).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-frontier-sweep-and-the-existence-gate-verdict/25-CONTEXT.md
@.planning/phases/25-frontier-sweep-and-the-existence-gate-verdict/25-RESEARCH.md
@.planning/phases/25-frontier-sweep-and-the-existence-gate-verdict/25-VALIDATION.md
</context>

<environment>
**Python 3.11 venv is MANDATORY.** Commands run as `.venv/bin/python -m pytest ...`.
Baseline to beat: **1647 passed, 1 skipped** at `8dd6415` (measured, 386.21 s).

**FOUR MODULES ARE ANCESTRY-GUARDED AND PERMANENTLY UNEDITABLE.** No file in this plan may write to
`scripts/mitigation_gate.py`, `scripts/mitigation_accountant.py`, `scripts/mitigation_unit.py` or
`scripts/phase18_extraction.py`. Import them; never modify them. `git diff --exit-code` over all four
must exit 0 at the end of every task.

**`scripts/phase23_matched_prereg.py` is EDIT-ONCE and already spent.** This plan must not modify it
and must not import from it.

**CI is `ubuntu-latest`, CPU-only.** Nothing in this plan may touch torch or MPS.
</environment>

<tasks>

<task type="auto">
  <name>Task 1: `scripts/phase25_prereg.py` — the reproduction HALT and the per-point one-attempt refusal</name>
  <files>scripts/phase25_prereg.py</files>
  <read_first>
    - `scripts/phase23_prereg.py` — `sigma_zero`, `noise_floor`, `H_PER_POINT_FLOOR_SECONDS`,
      `FLOOR_PROVENANCE_KEYS`. This is the register the new module writes in: module-level literal
      constants, a `_prove`-shaped refusal, and a docstring that states WHY before it states what
    - `scripts/phase23_matched_prereg.py` — `prove_first_attempt` and its **four scope clauses**, and
      `MATCHED_ARTIFACT_GLOB = "results/phase23_matched_*"`. Read clause (1) in particular: the
      uncommitted-window hole (`.gitignore:14,17`) applies verbatim to Phase 25 and must be restated
      with Phase 25's own paths. **This module is EDIT-ONCE, already spent, and must NOT be imported**
    - `results/phase23_sigma_zero.json` — `primary.k` = 790, `primary.n` = 1008, rate
      `0.7837301587301587`, `clip_norm` `1000000.0`, `clip_bind_count` `0`. The reproduction target
    - `results/phase23_matched_control.json` — the **four** `declared_differences` entries D-04 imports
      by path + digest, and `one_attempt_scope`
    - `results/phase23_matched_verdict.json` — `deviation 0.0`, `halt_message null`. The shape a
      passing reproduction verdict takes
    - `25-RESEARCH.md` §C6 — why `prove_first_attempt` cannot be reused as-is
  </read_first>
  <action>
Create `scripts/phase25_prereg.py`. Stdlib only plus `pathlib`/`json`/`hashlib`/`subprocess` — no
torch, no numpy, no network. The module docstring must open with the reason this file exists BEFORE
any data does, and must name the two facts that make it non-negotiable: a reproduction check written
after the reading is a rationalisation, and a one-attempt rule written after a re-run is an excuse.

**(a) The reproduction gate (D-07).** Pin `REPRODUCTION_K = 790`, `REPRODUCTION_N = 1008`,
`REPRODUCTION_SOURCE_RECORD = "results/phase23_sigma_zero.json"`, and a
`REPRODUCTION_PROVENANCE` sibling naming the record, its `primary.k` / `primary.n` path inside the
JSON, and the recipe fields that make the target legitimate (`clip_norm` `1000000.0`,
`clip_bind_count` `0`, `composed_steps` `200`, `composed_lot_sizes` `[8]`, `records_per_lot` `8`,
seed `1337`).

Write `prove_reproduction(k, n)`. It compares under **hard `==` on integers** — `k == REPRODUCTION_K`
and `n == REPRODUCTION_N`, no tolerance, no `math.isclose`, no rate comparison — because the reading
is a COUNT. On a match it returns silently. On a miss it raises `SystemExit` whose message must:
  - state that the sweep HALTS at **zero** sweep points, in those words
  - quote both the expected and the observed pair as `k/n`
  - name **suspect #1 explicitly**: the ratio-0.0 byte-identity assertion, digests `f146d426` and
    `a2c4771f`
  - list the **four** `declared_differences` from `results/phase23_matched_control.json` as the
    starting investigation list, read live from the record rather than retyped
  - mirror `phase23_prereg.sigma_zero`'s halt-message shape

Add `reproduction_source_reading()` which opens `REPRODUCTION_SOURCE_RECORD` and returns
`(primary["k"], primary["n"])`, so a caller can prove the pin still matches the record it names.

**(b) The per-point one-attempt rule (D-10).** Pin `POINT_RECORD_GLOB = "results/phase25_point_*.json"`
and `POINT_RECORD_GLOB_PROVENANCE`. Write `prove_first_attempt(tracked, *, point_key)` taking the
caller's `git ls-files` result — the same shape `phase23_matched_prereg.prove_first_attempt` takes —
but with **Phase 25's own glob in its own refusal text** and its own four scope clauses restated:
  1. the uncommitted-window hole: `data/` and `checkpoints/` are gitignored (`.gitignore:14,17`), so
     a point's draws and adapter can exist on disk with nothing tracked; the committed **record** is
     what makes "a reading landed" checkable, which is why the shape sha256s travel inside it
  2. the unit is the POINT, not the sweep — name that a killed point produced no reading and its
     resume is the SAME attempt
  3. the refusal names `point_key` and the exact tracked path that already exists
  4. the sanctioned re-run route is stated (delete the committed record in its own commit), so the
     refusal is not a dead end

**(c) D-04's declared differences, imported not retyped.** Write `declared_differences()` which opens
`results/phase23_matched_control.json`, returns its `declared_differences` list, and
`declared_differences_digest()` returning the sha256 of the record's bytes. `prove_reproduction`'s
halt message calls both. Pin `DECLARED_DIFFERENCES_SOURCE` as the path literal and assert inside the
function that the list length is **4** — if Phase 23's record ever grows a fifth, the halt message
must not silently drop it.

Do NOT import `scripts/phase23_matched_prereg.py` anywhere in this file.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import sys; sys.path.insert(0,'scripts'); import phase25_prereg as p; p.prove_reproduction(790,1008); print('pass ok'); print(p.reproduction_source_reading()); print(len(p.declared_differences()))"</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -c "import sys;sys.path.insert(0,'scripts');import phase25_prereg as p;p.prove_reproduction(790,1008)"` exits 0
    - `.venv/bin/python -c "import sys;sys.path.insert(0,'scripts');import phase25_prereg as p;p.prove_reproduction(789,1008)"` exits non-zero and its stderr contains `zero`, `f146d426` and `a2c4771f`
    - `.venv/bin/python -c "import sys;sys.path.insert(0,'scripts');import phase25_prereg as p;print(p.reproduction_source_reading())"` prints `(790, 1008)` — the pin matches the record it names
    - `.venv/bin/python -c "import sys;sys.path.insert(0,'scripts');import phase25_prereg as p;print(len(p.declared_differences()))"` prints `4`
    - `.venv/bin/python -c "import ast;t=ast.parse(open('scripts/phase25_prereg.py').read());print([n for n in ast.walk(t) if isinstance(n,(ast.Import,ast.ImportFrom)) and 'phase23_matched_prereg' in ast.dump(n)])"` prints `[]` — the spent module is not imported
    - `.venv/bin/python -c "import ast;src=open('scripts/phase25_prereg.py').read();t=ast.parse(src);names={n.id for n in ast.walk(t) if isinstance(n,ast.Name)}|{n.attr for n in ast.walk(t) if isinstance(n,ast.Attribute)};assert 'isclose' not in names and 'torch' not in names, names;print('no tolerance, no torch')"` prints the confirmation
    - `git diff --exit-code -- scripts/mitigation_gate.py scripts/mitigation_accountant.py scripts/mitigation_unit.py scripts/phase18_extraction.py scripts/phase23_matched_prereg.py` exits 0
  </acceptance_criteria>
  <done>`prove_reproduction` halts at zero points on a one-count miss with the four declared differences read live from Phase 23's record, and `prove_first_attempt` refuses with Phase 25's own glob in its own text.</done>
</task>

<task type="auto">
  <name>Task 2: The four rules that must exist before any point does (D-11, D-40, D-37, D-04's tripwire target set)</name>
  <files>scripts/phase25_prereg.py</files>
  <read_first>
    - `scripts/phase25_prereg.py` — the module created in Task 1; these constants land in it
    - `scripts/mitigation_gate.py` — `promote_to_full_fidelity(verdict=, reasons=, curve_k=, full_k=)`
      and `ratchet_k(fixed_k=, proposed_k=)`; `REPLICATION_PENDING_MARKER`; `exists_clearing_point`'s
      denominator string at `:900-905`. **READ ONLY — never written**
    - `scripts/mitigation_budget.py` — `CURVE_K = 16`, `FULL_FIDELITY_K = 48`, and the `_PROVENANCE`
      sibling shape every pin in this repo follows
    - `scripts/phase21_filler.py` — the 56 unscored filler facts and the 8 filler-only slots; the
      basis for D-37(ii)'s claim that **only n=8 points have out-of-corpus canaries**
    - `.planning/ROADMAP.md` §Phase 28 — RPT-01's scope, so D-40's obligation names strings that
      phase can actually carry
    - `25-CONTEXT.md` D-11, D-37, D-40 — the three rules, verbatim
  </read_first>
  <action>
Append four committed rules to `scripts/phase25_prereg.py`. Each is a module-level constant with a
`_PROVENANCE`-shaped sibling or an inline `governs` string stating what it decides and what it does
NOT decide. Each docstring must name the date this file was committed and the fact that **no point
record existed at that commit** — that is the property being purchased.

**(a) `PROMOTION_RULE` (D-11).** Lazy and candidate-triggered. State: all 44 curve points at
`mitigation_budget.CURVE_K` first (imported, not retyped); only points clearing all three conditions
are promoted to `mitigation_budget.FULL_FIDELITY_K` **and** replicated at a second seed **at K=48** —
never a lower-power replication than the claim, because `ratchet_k` is one-way. State the pre-registered
tail rule in the form the decision uses: **if more candidates clear than the budget holds, promote and
replicate ALL of them — never a subset chosen after seeing which cleared.** State that an empty
frontier (the pre-registered null, live given ε = 519.698 at σ = 0.5) means exactly **zero** tail cost.

**(b) `PUBLICATION_OBLIGATION` (D-40).** A tuple of the exact artifact-sourced strings Phase 28's
report MUST carry, each as a `(field_path, why)` pair so the report writer reads a path into the
frontier artifact rather than a sentence to paraphrase:
  - the arm-level existential **with its denominator** (`exists_clearing_point`'s own
    "N of M point(s) examined returned PASS" string)
  - the capacity branch name from `CAPACITY_BRANCHES`
  - the curve-total ε together with `selection_accounted`
  - the two named limitations: no committed capacity rule exists for the adversarial arm (D-23), and
    the curve total is unbounded once the σ=0 control is published (D-29)
State explicitly that Phase 25 does NOT write the report; Phase 28 does. This constant is the
contract, not the prose.

**(c) `CANARY_RESERVATIONS` (D-37).** Three fields:
  - `adapter_retention`: every point's adapter is retained on local disk with its sha256 recorded
    INSIDE the frontier artifact, because `checkpoints/` and `*.pt` are gitignored (`.gitignore:14-15`)
    and a deleted adapter cannot be re-derived without re-running the point
  - `canary_population_rule`: at n=8 the 56 filler facts are OUT and at n=64 all 64 are IN, so **only
    n=8 points have out-of-corpus canaries at all** — a structural constraint on Phase 26 written down
    before the sweep rather than discovered after
  - `audit_target_rule`: the committed rule naming WHICH point Phase 26 audits, stated so it resolves
    to exactly one point key from the artifact without any post-hoc choice
Record the measured disk correction beside it: an adapter is **1,352,069 B** (D-37's 1.35 MB is
right), but `arm_outputs` also names `checkpoints/{prefix}_{arm}_latest.pt` at **≈ 59,691,603 B** per
point, so the *precheck* must cover ≈ 2.7 GB, not 59 MB. Pin `DISK_PRECHECK_BYTES` at
`5_000_000_000` — hmm, the module is plain Python, so write it as a plain int literal `5000000000` —
and name that the 44 adapters plus 44 resume checkpoints plus headroom.

**(d) `BIT_IDENTITY_FORBIDDEN_ASSERTIONS` (D-04's tripwire target set).** A tuple of the identifier
and attribute names whose co-occurrence in a single test function body constitutes an assertion of
bit-identity between the σ=0 point and the seam-off path — for example `torch.equal`, `assert_close`
with `rtol=0`, and a sha256 equality between a σ=0 adapter and a `dp_fn=None` adapter. The tripwire in
Task 3 resolves this set **by AST**, so this constant names identifiers, not phrases. Beside it pin
`BIT_IDENTITY_EXPECTED_DISAGREEMENT` — Phase 23's measured n=8 figure, 72/72 LoRA tensors agreeing to
`2.178e-07` relative — and state that the correct assertion is *bounded disagreement*, never equality.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import sys;sys.path.insert(0,'scripts');import phase25_prereg as p;import mitigation_budget as b;assert p.PROMOTION_RULE['curve_k'] == b.CURVE_K and p.PROMOTION_RULE['full_k'] == b.FULL_FIDELITY_K;print(len(p.PUBLICATION_OBLIGATION), sorted(p.CANARY_RESERVATIONS), p.DISK_PRECHECK_BYTES)"</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -c "import sys;sys.path.insert(0,'scripts');import phase25_prereg as p, mitigation_budget as b;assert p.PROMOTION_RULE['curve_k'] is b.CURVE_K or p.PROMOTION_RULE['curve_k']==b.CURVE_K;assert p.PROMOTION_RULE['full_k']==b.FULL_FIDELITY_K;print('K imported')"` exits 0 — the rule reads the budget pins rather than retyping 16 and 48
    - `.venv/bin/python -c "import sys;sys.path.insert(0,'scripts');import phase25_prereg as p;print(len(p.PUBLICATION_OBLIGATION))"` prints `4` or more
    - `.venv/bin/python -c "import sys;sys.path.insert(0,'scripts');import phase25_prereg as p;ks=set(p.CANARY_RESERVATIONS);assert {'adapter_retention','canary_population_rule','audit_target_rule'} <= ks, ks;print('three reservations')"` exits 0
    - `.venv/bin/python -c "import sys;sys.path.insert(0,'scripts');import phase25_prereg as p;assert p.DISK_PRECHECK_BYTES >= 2_700_000_000, p.DISK_PRECHECK_BYTES;print(p.DISK_PRECHECK_BYTES)"` exits 0 — the precheck is sized for adapters **and** resume checkpoints, closing R10's 42× under-estimate
    - `.venv/bin/python -c "import sys;sys.path.insert(0,'scripts');import phase25_prereg as p;assert isinstance(p.BIT_IDENTITY_FORBIDDEN_ASSERTIONS, tuple) and len(p.BIT_IDENTITY_FORBIDDEN_ASSERTIONS)>=2;print(p.BIT_IDENTITY_EXPECTED_DISAGREEMENT)"` exits 0
    - `git log --oneline -- results/phase25_point_\*.json | wc -l` returns `0` — **no point record existed when these rules landed**, which is the entire property this task buys
    - `make lint` exits 0
  </acceptance_criteria>
  <done>The promotion rule, the publication obligation, the three canary reservations and the tripwire's target set are committed constants in a repository where `git ls-files 'results/phase25_point_*'` is still empty.</done>
</task>

<task type="auto">
  <name>Task 3: `tests/test_phase25_prereg.py` — three watched refusals, two natural and one planted</name>
  <files>tests/test_phase25_prereg.py</files>
  <read_first>
    - `scripts/phase25_prereg.py` — everything built in Tasks 1 and 2
    - `tests/test_phase23_prereg.py` — the register for testing a HALT: the refusal is INVOKED and its
      message asserted on discriminating substrings, never on the full string
    - `tests/test_phase24_correction.py:1-50` — the four mechanics this repo enforces, of which two
      apply here: resolve node ids and identifiers **by AST, never by grep**, and match prose through
      `scripts/_prose.normalized`
    - `tests/test_phase20_prereg.py` — `_assert_ordering_holds` and the `adds[-1]` ancestry guard, so
      the new test file does not accidentally assert anything about the four frozen modules' history
    - `25-RESEARCH.md` § *Structurally checked vs merely declared* — D-04's row is the ONLY one in
      this phase with **no natural RED**; it must be planted, and planted **against a scratch copy**
  </read_first>
  <action>
Create `tests/test_phase25_prereg.py`. CPU-only, stdlib plus `pytest` plus the one sibling script.

**(a) The reproduction gate, BOTH branches watched.**
  - `test_reproduction_passes_at_the_committed_reading` — `prove_reproduction(790, 1008)` returns
    without raising, and `reproduction_source_reading()` equals `(REPRODUCTION_K, REPRODUCTION_N)` so
    the pin is proved against the record it names rather than against itself
  - `test_reproduction_miss_halts_at_zero_sweep_points` — parametrized over `(789, 1008)`,
    `(791, 1008)` and `(790, 1007)`. Each raises `SystemExit`; assert the message contains `zero`,
    contains both digests `f146d426` and `a2c4771f`, and contains **four** declared-difference entries
    (count them from `declared_differences()`, do not retype them). **This is the natural RED D-07
    owes**: a one-count miss is enough
  - `test_reproduction_uses_no_tolerance` — AST over `scripts/phase25_prereg.py`: no `isclose`, no
    `abs(` difference comparison, no `<=`/`>=` against `REPRODUCTION_K`. Resolve by AST because the
    module's own docstring discusses tolerance

**(b) The per-point one-attempt rule, watched.**
  - `test_first_attempt_passes_on_an_unrecorded_point` — an empty tracked list for a fresh point key
  - `test_second_attempt_on_a_recorded_point_is_refused` — a tracked list containing
    `results/phase25_point_<key>.json`; assert `SystemExit` whose message contains that exact path and
    the literal `results/phase25_point_` glob
  - `test_the_refusal_names_phase_25s_glob_and_not_phase_23s` — assert `results/phase23_matched_` is
    **absent** from the refusal text. This is §C6's whole point and it must be asserted, not assumed
  - `test_phase25_prereg_does_not_import_the_spent_module` — AST over the import nodes

**(c) D-04's armed tripwire — the one planted RED in this phase.**
  - `test_no_committed_test_asserts_sigma_zero_seam_off_bit_identity` — walk **every** file under
    `tests/` and `scripts/` with `ast.parse`. For each `ast.FunctionDef`, collect the `ast.Name.id`
    and `ast.Attribute.attr` names in its body. Fail if a single function body contains BOTH a name
    from `BIT_IDENTITY_FORBIDDEN_ASSERTIONS` AND the σ=0 / seam-off pairing markers (resolve those by
    AST too — the constant names, not their prose). The failure message must name the file, the
    function and the offending identifier pair, and must quote
    `BIT_IDENTITY_EXPECTED_DISAGREEMENT` as what the correct assertion looks like.
  - `test_the_bit_identity_tripwire_fires_on_a_planted_violation` — **plant against a scratch copy in
    `tmp_path`, never against a real repo file.** Write a synthetic module into `tmp_path` containing
    one function that asserts `torch.equal` between a σ=0 adapter and a `dp_fn=None` adapter, point
    the walker at `tmp_path`, and assert it raises with that function's name in the message. Restore
    nothing — no real file was touched. The test docstring must state why the planted form is used
    here and nowhere else in this phase: there is **no natural RED** for D-04, and this repository has
    been burned by planted REDs landing on the wrong occurrence in a real file.

**(d) The committed rules are structurally asserted, not merely present.**
  - `test_promotion_rule_reads_the_budget_pins` — `PROMOTION_RULE`'s K values `==`
    `mitigation_budget.CURVE_K` / `FULL_FIDELITY_K`, and the tail clause's text is matched through
    `scripts/_prose.normalized` so a line wrap cannot report a false absence
  - `test_publication_obligation_names_four_artifact_paths` and
    `test_canary_reservations_carry_the_three_fields_and_a_sized_precheck`
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase25_prereg.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_phase25_prereg.py -v` exits 0 with **zero skipped**; quote the literal `N passed, M skipped` line in the SUMMARY with `M == 0`
    - `.venv/bin/python -m pytest tests/test_phase25_prereg.py -k "reproduction_miss" -v` collects **3** cases and all 3 pass
    - The SUMMARY quotes the verbatim `SystemExit` text produced by `prove_reproduction(789, 1008)` — the watched RED, captured rather than described
    - The SUMMARY quotes the verbatim failure message from the planted D-04 tripwire run in `tmp_path`, and states that `git status --porcelain tests/ scripts/` was clean immediately after that test
    - `.venv/bin/python -c "import ast;t=ast.parse(open('tests/test_phase25_prereg.py').read());assert any(isinstance(n,ast.FunctionDef) and n.name=='test_the_bit_identity_tripwire_fires_on_a_planted_violation' for n in ast.walk(t));print('planted RED committed')"` exits 0
    - `grep -v '^\s*#' tests/test_phase25_prereg.py | grep -c 'subprocess.*grep'` returns `0` — the tripwire resolves by AST, never by grep
    - `.venv/bin/python -m pytest tests/ -q` reports 0 failed; record the `N passed, M skipped` line
    - `git diff --exit-code -- scripts/mitigation_gate.py scripts/mitigation_accountant.py scripts/mitigation_unit.py scripts/phase18_extraction.py scripts/phase23_matched_prereg.py` exits 0
    - `make lint` exits 0
  </acceptance_criteria>
  <done>Both branches of the reproduction gate and both branches of the one-attempt rule have been watched firing with their messages captured verbatim, and D-04's tripwire has been watched firing against a scratch copy while the real tree stayed clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a killed sweep point → the committed record | "a reading landed" must be checkable from tracked bytes, not asserted. `data/` and `checkpoints/` are gitignored, so on-disk state proves nothing |
| Phase 23's committed readings → Phase 25's pins | 790/1008 must be read from `results/phase23_sigma_zero.json`, never retyped from CONTEXT prose |
| a rule written before the data → a rule written after | the only thing separating them is the commit graph, and it is checkable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Repudiation | a sweep point silently re-run after a bad reading, leaving no trace because `data/` is gitignored | mitigate | `prove_first_attempt(tracked, point_key=)` refuses on a committed record; clause (1) names the uncommitted-window hole; the sanctioned delete-in-its-own-commit route is stated so the refusal is auditable rather than bypassed |
| T-25-02 | Tampering | a reproduction target retyped from prose drifting from `phase23_sigma_zero.json` | mitigate | `reproduction_source_reading()` opens the record; a test asserts the pin `==` the record's `primary.k`/`primary.n` |
| T-25-03 | Information Disclosure | 43 uninterpretable points published because a control miss was softened into a warning | mitigate | `prove_reproduction` raises `SystemExit` and its message says **zero sweep points**; both miss directions watched |
| T-25-04 | Elevation of Privilege | a later plan "fixing" the σ=0 / seam-off non-identity, destroying the declared floating-point non-associativity record | mitigate | D-04's AST tripwire over `tests/` and `scripts/`, watched firing on a planted scratch-copy violation |
| T-25-05 | Tampering | promotion or replication scoped after seeing which candidates cleared | mitigate | `PROMOTION_RULE` is committed while `git ls-files 'results/phase25_point_*'` is empty; the all-or-none tail clause is asserted through `_prose.normalized` |
| T-25-06 | Denial of Service | the sweep filling the disk with 44 retained adapters plus 44 resume checkpoints | mitigate | `DISK_PRECHECK_BYTES` sized at 5 GB against the measured 1,352,069 B adapter + 59,691,603 B `latest.pt`, correcting D-37's 42× under-estimate |
| T-25-SC | Tampering | npm/pip/cargo installs | accept | **Zero installs.** `25-RESEARCH.md`'s Assumptions Log is empty and records the Package Legitimacy Audit as N/A because this phase installs nothing; `pyproject.toml` must stay byte-unchanged (RPT-03) |
</threat_model>

<verification>
```bash
.venv/bin/python -m pytest tests/test_phase25_prereg.py -v
.venv/bin/python -m pytest tests/ -q                       # record "N passed, M skipped"
git diff --exit-code -- scripts/mitigation_gate.py scripts/mitigation_accountant.py scripts/mitigation_unit.py scripts/phase18_extraction.py scripts/phase23_matched_prereg.py pyproject.toml
git ls-files 'results/phase25_point_*.json' | wc -l        # must be 0
make lint
```
</verification>

<success_criteria>
- `prove_reproduction` halts at **zero** sweep points on a one-count miss, watched, with the message captured verbatim
- `prove_first_attempt` refuses with **Phase 25's** glob and is asserted NOT to name Phase 23's
- D-04's tripwire is armed over `tests/` and `scripts/` and has been watched firing on a scratch copy
- `PROMOTION_RULE`, `PUBLICATION_OBLIGATION` and `CANARY_RESERVATIONS` are committed with `git ls-files 'results/phase25_point_*'` empty
- `DISK_PRECHECK_BYTES` is sized against the measured resume-checkpoint size, not D-37's adapter-only figure
- The four ancestry-guarded modules and `phase23_matched_prereg.py` are byte-unchanged
</success_criteria>

<output>
Create `.planning/phases/25-frontier-sweep-and-the-existence-gate-verdict/25-01-SUMMARY.md` when done.
</output>
