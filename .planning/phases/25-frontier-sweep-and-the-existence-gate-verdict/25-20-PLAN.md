---
phase: 25-frontier-sweep-and-the-existence-gate-verdict
plan: 20
type: execute
wave: 13
depends_on: ["25-19"]
files_modified:
  - results/phase25_operational_note.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - tests/test_phase25_close.py
autonomous: false
requirements: [FRONT-03, CTRL-01, CTRL-02, ADVT-01, RPT-02]

must_haves:
  truths:
    - "D-13: the `pmset` revert to the MEASURED prior values (sleep 1 / disksleep 10 / powernap 1) is a COMMITTED plan step, verifiable, NEVER dependent on human memory. `phase25_venue.prove_reverted()` is the mechanism that verifies it."
    - "D-43: the run's own `caffeinate -dims` assertion is released and no stray assertion is left behind, verified through `pmset -g assertions` BY OWNING PROCESS cross-checked with `pgrep -x caffeinate` — never through `pmset -g`'s summary line, which enumerates assertions rather than processes and was measured over-counting five against three."
    - "The LaunchAgents are unloaded. `KeepAlive` was false throughout and must be asserted still false in the committed plist at close, so nothing was quietly changed to shorten a recovery during the run."
    - "D-37: the three Phase-26 reservations are DISCHARGED as facts on disk — every point's adapter retained with its sha256 inside the frontier artifact, the per-point in/out canary population recorded, and the committed rule naming which point the audit targets, all written before any point existed."
    - "D-40: the publication obligation was committed in plan 25-01 before any point existed and travels inside the frontier artifact. Phase 25 does NOT write the report; Phase 28 executes it."
    - "SS-O1: the read-only-git-surface discipline was abandoned FOR THIS PHASE ONLY, named in the pre-registration. The close records that the exception ends here."
    - "Requirement ticks are evidenced, not asserted: each of CTRL-01, CTRL-02, FRONT-01..04, ADVT-01 and RPT-02 is ticked against a named artifact and a named guard, additively, in the traceability table."
  artifacts:
    - path: "results/phase25_operational_note.md"
      provides: "the closed operational record — the reverted pmset state read back, the released assertions, the unloaded agents, and the total measured spend"
      contains: "prove_reverted"
    - path: ".planning/REQUIREMENTS.md"
      provides: "eight requirement ticks with their evidence recorded additively in the traceability table"
      contains: "Phase 25"
  key_links:
    - from: "results/phase25_operational_note.md"
      to: "scripts/phase25_venue.py::prove_reverted"
      via: "the revert is verified by the committed mechanism, not by recollection"
      pattern: "prove_reverted"
    - from: ".planning/REQUIREMENTS.md traceability rows"
      to: "results/phase25_frontier.json"
      via: "every tick names the artifact field and the guard test that evidences it"
      pattern: "phase25_frontier"
---

<objective>
Put the machine back the way it was, discharge the reservations Phase 26 depends on, and tick eight
requirements against named evidence rather than against recollection.

Purpose: D-13's revert is the one obligation in this phase that a tired operator would forget six days
later, which is exactly why it was committed as argv data with a verifying function before the sweep
started. This plan executes it and proves it. D-43's release is the mirror: the run's own `caffeinate`
assertion must go, and the verification must use the **corrected** method — `pmset -g assertions` by
owning process, because the summary line enumerates assertions rather than processes and was measured
listing five against three.

The rest is closing honestly. Eight requirement ticks, each pointing at an artifact field and a guard
test. Three Phase-26 reservations discharged as facts on disk. And the §O1 exception explicitly ended:
the driver's widened git surface was for this phase only, and the close says so rather than letting it
drift into precedent.

Output: the closed operational note, eight evidenced ticks, and the close guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-frontier-sweep-and-the-existence-gate-verdict/25-CONTEXT.md
@.planning/phases/25-frontier-sweep-and-the-existence-gate-verdict/25-RESEARCH.md
@.planning/phases/25-frontier-sweep-and-the-existence-gate-verdict/25-VALIDATION.md
</context>

<environment>
**Python 3.11 venv is MANDATORY.** CPU-only.

**This plan is NOT autonomous.** The `sudo pmset` revert is a blocking human checkpoint.

**ADDITIVE ONLY ON PROSE.** No original *prose* line may be deleted, reworded or reflowed.

**The deletion budget is not zero — it is a literal, and every deleted line must be a checkbox toggle.**
Ticking a checkbox is a line modification, which git records as one deletion plus one insertion.
**Measured at HEAD:** seven of the eight requirement checkboxes read `- [ ]` (only RPT-02 is `[x]`) and
§Phase 25 carries 20 `- [ ] 25-NN-PLAN.md` lines, so this task's edit deletes **27** lines. A
`deletions == 0` gate is therefore unsatisfiable at exactly the point 100–150 h of compute has already
been spent — which is precisely when an executor skips the ticks to make the gate green. The rule here
is instead: **every deleted line must be a `- [ ]` line whose `- [x]` twin appears among the
insertions**, and all of them are enumerated in the SUMMARY. Snapshot both files to a scratch copy
before the edit and audit against the snapshot with `git diff --no-index -U0`. **No `gsd-sdk`
mutation handler** may be called on `ROADMAP.md`, `REQUIREMENTS.md` or `STATE.md` — hand-edit, snapshot
before, diff after.

**The four ancestry-guarded modules, `scripts/mitigation_budget.py` and `results/phase25_frontier.json`
must all stay byte-unchanged.**
</environment>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Execute D-13's committed revert and release the run's assertion</name>
  <files>none — this task changes no tracked file. It changes MACHINE STATE and produces transcript outputs that Task 2 transcribes into the operational note.</files>
  <read_first>
    - `scripts/phase25_venue.py` — `PMSET_REVERT`, `PMSET_REVERT_TARGETS`, `prove_reverted`,
      `read_power_settings`, `read_assertions`. The revert was committed as argv data with the
      **measured** prior values before the sweep started
    - `results/phase25_operational_note.md` — the before-state recorded in plan 25-14, which is what the
      revert restores
    - `artifacts/com.personacore.phase25.sweep.plist` and the watcher plist — the two agents to unload
    - `25-RESEARCH.md` §R5 — why `pmset -g`'s summary line cannot verify D-43: it enumerates
      **assertions, not processes**, and was measured listing five caffeinate entries against three pids
  </read_first>
  <action>
**EVERYTHING EXCEPT THE `sudo` CALL AND THE PROCESS-STATE INSPECTION IS AUTOMATED.** Plan 25-06
committed `PMSET_REVERT` and `PMSET_REVERT_TARGETS` as argv data with the measured prior values, plus
`prove_reverted()` as the verifying mechanism; plan 25-14 applied the change and recorded the
before-state. The sweep is finished and the artifact is committed. **Run the steps below in order and
paste every output verbatim** — Task 2 transcribes them into the operational note and a test asserts
each block is present.

**(a) Stop and unload both LaunchAgents.**
`launchctl bootout gui/$(id -u)/com.personacore.phase25.sweep` and the same for
`...phase25.watch`. Then `launchctl list | grep personacore` must return nothing.

**(b) Confirm the run's own `caffeinate` wrapper is gone and nothing stray was left behind** — using
D-43's **corrected** method, not the summary line. Run `pmset -g assertions` and `pgrep -x caffeinate`.
`pgrep -x caffeinate` must return nothing. If anything remains, identify its owner before proceeding.

**(c) Run the COMMITTED revert. Do not retype it.** Print it from the module first so the command
executed is provably the command that was committed:
`.venv/bin/python -c "import sys;sys.path.insert(0,'scripts');import phase25_venue as v;print(' '.join(v.PMSET_REVERT))"`
Then run exactly that command.

**(d) Verify with the committed mechanism, not by eye.**
`.venv/bin/python -c "import sys;sys.path.insert(0,'scripts');import phase25_venue as v;v.prove_reverted();print('reverted:', v.read_power_settings())"`
It must exit 0 and print `sleep 1`, `disksleep 10`, `powernap 1`. This is the whole reason the revert
was committed as data before the sweep began: it never depended on anyone remembering.
  </action>
  <verify>
    <human-check>The five command outputs below are pasted verbatim into the reply and each matches the expected value stated beside it.</human-check>
  </verify>
  <acceptance_criteria>
    - `launchctl list | grep personacore` is pasted and is **empty** — both agents unloaded
    - `pmset -g assertions` is pasted, and `pgrep -x caffeinate` is pasted and is **empty**
    - The revert command **as printed from the module** is pasted, and matches `phase25_venue.PMSET_REVERT` joined by spaces
    - `prove_reverted()` exits 0 and its `read_power_settings()` output is pasted showing `sleep 1`, `disksleep 10`, `powernap 1`
    - The reply states explicitly that the executed command was read from the module rather than retyped
  </acceptance_criteria>
  <resume-signal>Paste the five outputs above, then type "reverted" — or describe what differed.</resume-signal>
  <done>Both agents are unloaded, no caffeinate assertion remains, and the machine's power settings are back to the measured prior values — verified by the mechanism committed before the sweep started, not by recollection.</done>
</task>

<task type="auto">
  <name>Task 2: Close the operational note and discharge the three Phase-26 reservations</name>
  <files>results/phase25_operational_note.md</files>
  <read_first>
    - `results/phase25_operational_note.md` — the note opened in plan 25-14, with the before-state, the
      launch identity triple and the revert obligation
    - The Task 1 transcript — every figure appended here is transcribed from it, never invented
    - `scripts/phase25_venue.py` — `PMSET_REVERT`, `PMSET_REVERT_TARGETS`, `prove_reverted`,
      `read_assertions`, `read_power_settings`
    - `scripts/phase25_prereg.py` — `CANARY_RESERVATIONS` (the three D-37 fields),
      `PUBLICATION_OBLIGATION`, `GIT_SURFACE_EXCEPTION`, `DISK_PRECHECK_BYTES`
    - `results/phase25_frontier.json` — the per-point `adapter_sha256`, `adapter_path` and
      `canary_population` fields, and the embedded pre-registered commitments
    - `results/phase25_interior_log.json` — the kills, the stall records and the per-point wall-clock
      table, for the total measured spend
    - `artifacts/com.personacore.phase25.sweep.plist` — to re-read `KeepAlive` at close
  </read_first>
  <action>
Append a dated **close** section to `results/phase25_operational_note.md`. Every figure is a quoted
command output, never a paraphrase.

**(a) The revert, closed.** The `launchctl list | grep personacore` empty output; the `pmset -g
assertions` owner list after release; the empty `pgrep -x caffeinate`; the revert command **as printed
from the module**; and `prove_reverted()`'s output. State plainly that the revert was executed from the
committed argv tuple and verified by the committed mechanism, so it never depended on memory.

**(b) `KeepAlive` at close.** Re-read the committed plist and record that `KeepAlive` is **still false**.
This closes the possibility that it was quietly flipped mid-run to shorten a recovery — the temptation
D-12 names and the one that would have violated D-10.

**(c) The total spend, measured.** From `results/phase25_interior_log.json` and the earlier logs: total
wall-clock, number of kills, number of stall records, largest single loss in minutes, and the comparison
against the ~101 h observed / ~150 h ceiling envelope. Record whether the envelope reproduced.

**(d) D-37's three reservations, discharged as facts on disk.**
  - **(i) Adapter retention.** Count the retained adapters and confirm every point's `adapter_sha256`
    in the frontier artifact matches the file on disk, recomputed from bytes. Record the count and the
    total bytes, and note the measured correction: 44 adapters at 1,352,069 B is ≈ 59 MB as D-37 says,
    but the retained `checkpoints/{prefix}_{arm}_latest.pt` files at ≈ 59,691,603 B each bring the real
    footprint to ≈ 2.7 GB, which is why `DISK_PRECHECK_BYTES` was sized at 5 GB
  - **(ii) The canary population.** Confirm every point record carries its in/out split, and that the
    structural constraint holds in the data: **only n=8 points have out-of-corpus canaries** — 56 filler
    facts OUT at n=8, all 64 IN at n=64
  - **(iii) The audit-target rule.** Quote `CANARY_RESERVATIONS["audit_target_rule"]` and resolve it
    against the committed artifact to **exactly one** point key. Record that key. The rule was committed
    before any point existed, so the resolution is a lookup rather than a choice

**(e) D-40's obligation, restated as handed over.** Quote `PUBLICATION_OBLIGATION` and state that
Phase 25 does **not** write the report; Phase 28 does, and the obligation travels inside
`results/phase25_frontier.json` so it cannot be scoped after the numbers are known.

**(f) §O1's exception ends here.** State that the driver's `{add, commit}` git surface over the
point-record path was a deliberate, named exception **for this phase only**, that
`GIT_SURFACE_EXCEPTION` records it in the pre-registration, that the AST guard proved the scope across
44 real unattended commits, and that the read-only-git-surface discipline resumes for later phases. An
exception that is not explicitly closed becomes precedent.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import hashlib,json,pathlib,sys;sys.path.insert(0,'scripts');import phase25_venue as v;v.prove_reverted();b=json.load(open('results/phase25_frontier.json'));bad=[k for k,p in b['points'].items() if hashlib.sha256(pathlib.Path(p['adapter_path']).read_bytes()).hexdigest()!=p['adapter_sha256']];assert not bad, bad[:3];print(v.read_power_settings(), len(b['points']), 'adapters verified from bytes')"</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -c "import sys;sys.path.insert(0,'scripts');import phase25_venue as v;v.prove_reverted();print(v.read_power_settings())"` exits 0 and prints `sleep 1`, `disksleep 10`, `powernap 1`
    - `pgrep -x caffeinate` returns nothing, and the note quotes that empty output beside the `pmset -g assertions` owner list
    - `.venv/bin/python -c "import plistlib;d=plistlib.load(open('artifacts/com.personacore.phase25.sweep.plist','rb'));assert d['KeepAlive'] is False;print('KeepAlive still false at close')"` exits 0
    - `.venv/bin/python -c "import hashlib,json,pathlib;b=json.load(open('results/phase25_frontier.json'));bad=[k for k,p in b['points'].items() if hashlib.sha256(pathlib.Path(p['adapter_path']).read_bytes()).hexdigest()!=p['adapter_sha256']];assert not bad, bad[:3];print(len(b['points']), 'adapters verified from bytes')"` prints `44 adapters verified from bytes`
    - `.venv/bin/python -c "import json;b=json.load(open('results/phase25_frontier.json'));n8=[p for p in b['points'].values() if p['arm'].endswith('n8')];n64=[p for p in b['points'].values() if p['arm'].endswith('n64')];assert all(p['canary_population']['out']==56 for p in n8);assert all(p['canary_population']['out']==0 for p in n64);print('only n=8 points have out-of-corpus canaries')"` exits 0
    - `.venv/bin/python -m pytest tests/test_phase25_close.py -k audit_target -x` passes — the committed rule resolves to **exactly one** point key
    - The SUMMARY quotes the total measured wall-clock against the ~101 h / ~150 h envelope, the kill count, the stall count and the largest single loss in minutes
    - `git diff --exit-code -- results/phase25_frontier.json` exits 0 — the published artifact was **read**, never touched
  </acceptance_criteria>
  <done>The revert was executed from the committed argv tuple and verified by `prove_reverted()`, `KeepAlive` is proved still false, all 44 adapter digests verify from bytes, the n=8-only canary asymmetry holds in the data, the audit-target rule resolves to one point, and the §O1 exception is explicitly closed.</done>
</task>

<task type="auto">
  <name>Task 3: Tick eight requirements against named evidence, additively, and guard the close</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, tests/test_phase25_close.py</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` — the CTRL, FRONT, ADVT and RPT checkboxes and their traceability
      rows; the `AMENDED 2026-08-30 (phase 24 close)` block, which is the **additive** template every
      row in this task copies; and plan 25-07's `RPT02-ROW` continuation
    - `.planning/ROADMAP.md` — §Phase 25's `**Requirements**` line (now carrying RPT-02), its five
      Success Criteria, plan 25-07's four continuations, and the `**Plans**` list
    - `results/phase25_frontier.json` — the artifact every tick must cite a field of
    - `results/phase25_promotion.json` — the verdicts, existentials and capacity branch
    - `results/phase25_operational_note.md` — the closed operational record
    - `scripts/_prose.py` — `normalized`, for every prose assertion in the guard
    - `tests/test_phase25_correction.py` — the four mechanics; this task's guard reuses them rather than
      re-deriving
  </read_first>
  <action>
**(a) Eight evidenced ticks.** For each of CTRL-01, CTRL-02, FRONT-01, FRONT-02, FRONT-03, FRONT-04,
ADVT-01 and RPT-02: tick the checkbox and append a traceability row entry naming, in this order, **the
artifact field**, **the guard test** and **the plan** that produced it. Never a bare "SATISFIED". The
register the existing rows use is the register to write in — measured figures, named files, named
guards. Specifically:
  - **CTRL-01 / CTRL-02** — `results/phase25_point_dp_n8_sigma0p000000.json`'s `taught_recall` 790/1008
    and `reproduction_gate.passed`; `clip_bind_count == 0` at `clip_norm 1000000.0`;
    `tests/test_phase25_control.py`. **Record the correction**: D-01's stated justification — that the
    gate requires the control's extraction — was measured false; the decision stands on CTRL-02 +
    FRONT-03, and the artifact carries the corrected `scoring_justification`
  - **FRONT-01** — `SIGMA_LADDER` / `EPSILON_LADDER` / `ADVERSARIAL_RATIO_GRID`, 44 point records as a
    set equality, `N64_LEG_WITHDRAWN False`; `tests/test_phase25_grid.py`,
    `tests/test_phase25_interior.py`. Cite D-19's continuation for the adversarial axis's scope
  - **FRONT-02** — the artifact's `epsilon_report` with both multiplicities and the
    `RECORDED, NOT RESOLVED` status; `report_epsilon`'s three required kwargs;
    `tests/test_phase25_epsilon.py`'s AST gate with its measured grep-RED / AST-GREEN contrast
  - **FRONT-03** — `results/phase25_frontier.json`, ordered `point_keys` hard equality at the single
    write, `accounting: null`, both module sha256s, per-question counts inline;
    `tests/test_phase25_frontier.py`
  - **FRONT-04** — the capacity branch read from `CAPACITY_BRANCHES`, both arm existentials with their
    denominators; `tests/test_phase25_promotion.py`, `tests/test_phase25_verdict.py`. **Record the
    correction**: condition (a) is **ZERO TOLERANCE**, not "at most 2 successes of 416" —
    `tolerance_report` renders that sentence into every point's reason
  - **ADVT-01** — the trained adversarial adapters at non-zero ratios with their retained sha256s;
    `tests/test_phase25_extremes.py`. Note that Phase 24 built the seam and declared this unsatisfied in
    its own row, and that the subject — *the adapter trained* — exists only now
  - **RPT-02** — this phase's continuations guarded through `_prose.normalized` in
    `tests/test_phase25_correction.py`, the **third** instance after plans 23-12 and 24-03. Cite plan
    25-07's `RPT02-ROW` continuation and state that the row's original premise was false

**(b) ROADMAP hygiene.** Update §Phase 25's `**Plans**` list to the 20 plans with their one-line
objectives and their completion marks. Do **not** rewrite the Success Criteria — plan 25-07's
continuations already supersede what needed superseding, and the originals stay standing.

**(c) `tests/test_phase25_close.py`.** CPU-only, stdlib plus `pytest` plus `scripts/_prose.py`:
  - `test_every_phase_25_requirement_is_ticked` — parametrized over the eight IDs; the checkbox reads
    `[x]`
  - `test_every_tick_names_an_artifact_and_a_guard` — parametrized; each row, matched through
    `_prose.normalized`, contains a `results/` path and a `tests/test_phase25_*.py` path that both
    **exist on disk**. A tick citing a file that is not there is red
  - `test_the_two_corrections_are_recorded_not_absorbed` — the ZERO TOLERANCE correction and the
    control-extraction-justification correction both appear in the traceability rows, and each superseded
    phrasing (`at most 2 successes`, `the gate requires`) appears **only inside an explicit `SUPERSEDED:`
    quotation**, never as a standing claim. Resolve it positionally: for every occurrence of the phrasing
    in the Phase-25 rows, the nearest preceding `SUPERSEDED:` marker must be closer than the nearest
    preceding row boundary, and the correction sentence must follow within the same row. **An
    absence-assertion is the wrong shape here and was measured self-contradictory**: action (a) *orders*
    both phrasings written into these rows (`— was measured false`, `not "at most 2 successes of 416"`),
    so a test asserting they appear nowhere fails on the very rows the same task requires. This repository's
    retract-in-place doctrine keeps the superseded claim visible — see `.planning/ROADMAP.md:731`
    (`SUPERSEDED IN PLACE 2026-08-30 (plan 24-03)`) and `.planning/STATE.md:671`
    (`SUPERSEDED, quoted from its own record and NOT retracted`), which are the register to copy
  - `test_the_planning_diffs_are_additive` — audit both files against their pre-edit snapshots
    (`/tmp/25-20-requirements.pre`, `/tmp/25-20-roadmap.pre`) with `git diff --no-index -U0`: **every**
    deleted line must start `- [ ] ` and have its `- [x] ` twin among the insertions. Assert the two
    toggle counts are **7** and **20** and assert the enumerated toggle list — no deleted line may be
    prose. Watch it RED by deleting one unrelated prose line from a **copy** of `REQUIREMENTS.md` in
    `tmp_path` and asserting the audit names that line
  - `test_the_audit_target_rule_resolves_to_exactly_one_point` — from
    `phase25_prereg.CANARY_RESERVATIONS["audit_target_rule"]` against the committed artifact
  - `test_the_git_surface_exception_is_closed` — the operational note states the exception was for this
    phase only and that the discipline resumes, matched through `_prose.normalized`
  - `test_the_pmset_state_is_reverted` — call `phase25_venue.prove_reverted()` live
  - `test_the_frontier_artifact_is_unchanged_since_its_single_write` — `git log --oneline` for
    `results/phase25_frontier.json` returns exactly **1** commit

Hand-edit both planning files. **Snapshot first**: `cp .planning/REQUIREMENTS.md /tmp/25-20-requirements.pre
&& cp .planning/ROADMAP.md /tmp/25-20-roadmap.pre`, before any edit. After the edit, every deleted line
must be a `- [ ]` → `- [x]` toggle and nothing else. No `gsd-sdk` mutation handler.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase25_close.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_phase25_close.py -v` exits 0 with **zero skipped**; quote the `N passed, M skipped` line
    - `.venv/bin/python -m pytest tests/test_phase25_close.py -k "is_ticked or names_an_artifact" -q` collects **16** cases (8 IDs × 2) and all pass
    - `.venv/bin/python -c "import subprocess;g=lambda s,l:subprocess.run(['git','diff','--no-index','-U0','--',s,l],capture_output=True,text=True).stdout.splitlines();O=[(l,g(s,l)) for s,l in (('/tmp/25-20-requirements.pre','.planning/REQUIREMENTS.md'),('/tmp/25-20-roadmap.pre','.planning/ROADMAP.md'))];R=[(l,[x[1:] for x in o if x.startswith('-') and not x.startswith('---')],[x[1:] for x in o if x.startswith('+') and not x.startswith('+++')]) for l,o in O];bad=[(l,d) for l,dels,adds in R for d in dels if not (d.startswith('- [ ] ') and '- [x] '+d[6:] in adds)];assert not bad, bad;print('all deletions are [ ]->[x] toggles:',[(l,len(dels)) for l,dels,_ in R]);[print('  TOGGLED:',d[:70]) for l,dels,_ in R for d in dels]"` exits 0 and prints `[('.planning/REQUIREMENTS.md', 7), ('.planning/ROADMAP.md', 20)]`. **This exact command was executed at plan time against a simulated close and printed those two counts with all 27 toggles enumerated; deleting one unrelated prose line then reddened it, naming that line** — paste the 27-line enumeration into the SUMMARY
    - `git log --oneline -- results/phase25_frontier.json | wc -l` returns `1`
    - `.venv/bin/python -m pytest tests/test_phase25_close.py -k corrections -x` passes — both superseded phrasings appear **only** behind a `SUPERSEDED:` marker with their correction sentence in the same row, and neither stands as a claim
    - `.venv/bin/python -c "import pathlib,re;t=pathlib.Path('.planning/REQUIREMENTS.md').read_text();rows=[r for r in t.splitlines() if re.match(r'\| (CTRL-0[12]|FRONT-0[1-4]|ADVT-01|RPT-02) \|', r)];assert len(rows)==8, len(rows);hits=[(p,r) for p in ('at most 2 successes','the gate requires') for r in rows if p in r];assert {p for p,_ in hits}=={'at most 2 successes','the gate requires'}, sorted({p for p,_ in hits});bad=[(p,r[:90]) for p,r in hits if 'SUPERSEDED:' not in r];assert not bad, bad;print(len(hits),'superseded phrasings present, every one behind a SUPERSEDED: marker')"` exits 0. **NOT vacuous**: it first asserts all eight Phase-25 rows are found and that **both** phrasings are actually present, so an empty row match or a silently-dropped correction is RED, not green. **Run at plan time against a scratch REQUIREMENTS.md carrying eight rows with both phrasings — printed `2 superseded phrasings present, every one behind a SUPERSEDED: marker`; stripping one `SUPERSEDED:` marker reddened it naming that row, and deleting one correction reddened the presence assert**
    - The SUMMARY states, in one sentence each, the two corrections carried into the traceability: **condition (a) is ZERO TOLERANCE, not "at most 2 of 416"**, and **the gate never reads the control's extraction, so D-01 is justified by CTRL-02 + FRONT-03**
    - The SUMMARY records that **zero `gsd-sdk` mutation handlers were called** and quotes both `git diff --numstat` lines
    - `.venv/bin/python -m pytest tests/ -q` reports 0 failed and **1 skipped**; quote the literal line and state the delta against the 1647/1 baseline
    - `git diff --exit-code -- scripts/mitigation_budget.py scripts/mitigation_gate.py scripts/mitigation_accountant.py scripts/mitigation_unit.py scripts/phase18_extraction.py results/phase25_frontier.json pyproject.toml` exits 0; `make lint` exits 0
  </acceptance_criteria>
  <done>All eight requirements are ticked with each row naming an artifact field, a guard test that exists on disk, and its plan; both measured corrections are recorded rather than absorbed; every deleted planning line is proved to be a `- [ ]` → `- [x]` toggle with the 27 enumerated; and the frontier artifact still has exactly one commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a privileged system change → the developer's machine after the phase ends | `sudo pmset -a` persists indefinitely unless explicitly reverted |
| a deliberate exception → precedent | §O1's widened git surface becomes the norm unless the close explicitly ends it |
| a requirement tick → the evidence it claims | a tick citing a file that does not exist is worse than no tick |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-109 | Elevation of Privilege | `sudo pmset -a sleep 0` left applied, permanently disabling sleep on the developer's machine | mitigate | the revert is executed from the **committed** argv tuple printed from the module, then verified by `prove_reverted()` live; a test calls it again at close |
| T-25-110 | Elevation of Privilege | `KeepAlive` quietly flipped true mid-run to shorten a recovery, violating D-10 | mitigate | the committed plist is re-read at close and `KeepAlive is False` asserted |
| T-25-111 | Spoofing | a stray assertion left holding the machine awake after the run | mitigate | D-43's corrected method — `pmset -g assertions` by owning process plus an empty `pgrep -x caffeinate` — both quoted in the note |
| T-25-112 | Repudiation | a requirement ticked against evidence that does not exist | mitigate | every row must name a `results/` path and a `tests/test_phase25_*.py` path, both asserted present on disk |
| T-25-113 | Information Disclosure | the two measured corrections silently absorbed instead of recorded | mitigate | both corrections are required in the traceability rows and both superseded phrasings are asserted absent from the Phase-25 rows |
| T-25-114 | Tampering | the published artifact edited after its single write | mitigate | `git log` for `results/phase25_frontier.json` asserted to be exactly one commit, and `git diff --exit-code` over it in every criterion |
| T-25-115 | Elevation of Privilege | §O1's widened git surface drifting into precedent for later phases | mitigate | the close states the exception was for this phase only and that the read-only discipline resumes, asserted through `_prose.normalized` |
| T-25-116 | Tampering | `gsd-sdk` mutation handlers corrupting planning frontmatter at close | mitigate | hand-edit only; every deleted line proved to be a `- [ ]` → `- [x]` toggle against a pre-edit snapshot, counts asserted at 7 and 20 and enumerated in the SUMMARY; the SUMMARY records zero handler calls |
| T-25-117 | Repudiation | an unsatisfiable `deletions == 0` gate reached after 100–150 h of compute, pressuring the executor to skip the eight ticks so the gate goes green | mitigate | the deletion budget is a stated literal (27) with every deleted line proved a checkbox toggle, so ticking and passing the gate are the SAME action rather than opposed ones |
| T-25-SC | Tampering | npm/pip/cargo installs | accept | Zero installs across the whole phase; `pyproject.toml` byte-unchanged (RPT-03 carried forward) |
</threat_model>

<verification>
```bash
.venv/bin/python -c "import sys;sys.path.insert(0,'scripts');import phase25_venue as v;v.prove_reverted();print(v.read_power_settings())"
pgrep -x caffeinate ; launchctl list | grep personacore     # both must be empty
.venv/bin/python -m pytest tests/test_phase25_close.py -v
# snapshots taken BEFORE any edit in Task 3
git diff --no-index -U0 -- /tmp/25-20-requirements.pre .planning/REQUIREMENTS.md | grep '^-[^-]'  # 7 lines, every one a "- [ ] **ID**" toggle
git diff --no-index -U0 -- /tmp/25-20-roadmap.pre .planning/ROADMAP.md | grep '^-[^-]'            # 20 lines, every one a "- [ ] 25-NN-PLAN.md" toggle
git log --oneline -- results/phase25_frontier.json | wc -l             # must be 1
.venv/bin/python -m pytest tests/ -q                                    # expect 0 failed, 1 skipped
git diff --exit-code -- scripts/mitigation_budget.py scripts/mitigation_gate.py scripts/mitigation_accountant.py scripts/mitigation_unit.py scripts/phase18_extraction.py results/phase25_frontier.json pyproject.toml
make lint
```
</verification>

<success_criteria>
- `prove_reverted()` passes live and the note quotes the revert command as printed from the committed module
- `pgrep -x caffeinate` and `launchctl list | grep personacore` are both empty, quoted
- `KeepAlive` is proved still false in the committed plist at close
- All 44 adapter digests verify from bytes and the n=8-only canary asymmetry holds in the data
- The audit-target rule resolves to exactly one point key, and D-40's obligation is quoted as handed to Phase 28
- The §O1 git-surface exception is explicitly closed as phase-only
- Eight requirements are ticked, each naming an artifact field and a guard test that exists on disk
- Both measured corrections — ZERO TOLERANCE, and the gate not reading the control's extraction — are recorded in the traceability
- Both planning diffs delete only checkbox toggles — 7 in `REQUIREMENTS.md`, 20 in `ROADMAP.md`, all 27 enumerated in the SUMMARY, zero prose lines deleted — and `results/phase25_frontier.json` still has exactly one commit
</success_criteria>

<output>
Create `.planning/phases/25-frontier-sweep-and-the-existence-gate-verdict/25-20-SUMMARY.md` when done.
</output>
