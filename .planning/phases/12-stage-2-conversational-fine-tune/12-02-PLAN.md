---
phase: 12-stage-2-conversational-fine-tune
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/personacore/evaluation/perplexity.py
  - src/personacore/generation/core.py
  - tests/test_masked_perplexity.py
  - tests/test_stop_ids.py
autonomous: true
requirements: [TUNE-01]
must_haves:
  truths:
    - "masked_perplexity(model, bin_path, mask_path, block_size, device, forbid_ids=None) returns exp(sum-CE over mask==1 shifted targets / exact scored-target count) — proven by a hand-fixture oracle"
    - "masked_perplexity raises ValueError on token/mask length mismatch and on zero scored targets"
    - "generate(stop_ids=None) is bit-identical to v1.0 single-EOS behavior; a custom stop id halts generation without yielding the stop token"
  artifacts:
    - path: "src/personacore/evaluation/perplexity.py"
      provides: "masked_perplexity() — THE frozen dialogue-val gate metric for all Phase 12 arms"
      contains: "def masked_perplexity"
    - path: "src/personacore/generation/core.py"
      provides: "additive stop_ids kwarg on generate()"
      contains: "stop_ids"
    - path: "tests/test_masked_perplexity.py"
      provides: "hand-fixture oracle: CE over mask==1 targets only, exact denominator, mismatch raise"
    - path: "tests/test_stop_ids.py"
      provides: "default-equivalence + stop-without-yield tests"
  key_links:
    - from: "src/personacore/evaluation/perplexity.py"
      to: "perplexity() in the same file"
      via: "masked_perplexity mirrors the non-overlapping-window sum-CE sweep"
      pattern: "ignore_index=-100"
    - from: "src/personacore/generation/core.py"
      to: "collect()"
      via: "**kw pass-through (collect needs no change)"
      pattern: "stop_ids"
---

<objective>
Ship the two remaining additive code seams outside the training loop: the deterministic
masked dialogue-val PPL metric (`masked_perplexity`, the ONLY gate-grade dialogue metric for
every smoke arm — RESEARCH Pattern 2 / Pitfall 3) and the `stop_ids` kwarg in `generate()`
(Phase 11 D-03 lands here — needed for curated transcripts).

Purpose: D-01/D-02/D-05 gates compare arms against k×noise-floor margins; `estimate_loss`'s
20-random-batch mean would inject eval sampling noise into margins that measure training noise,
and masked vs unmasked arms train incommensurable objectives — one frozen deterministic
evaluation policy is mandatory before the first smoke number exists. `stop_ids` lets transcript
generation halt when the model starts a hallucinated user turn.

Output: two additive functions + two new test files; full suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-stage-2-conversational-fine-tune/12-CONTEXT.md
@.planning/phases/12-stage-2-conversational-fine-tune/12-PATTERNS.md
@src/personacore/evaluation/perplexity.py
@src/personacore/generation/core.py

<interfaces>
Verified this session by the pattern-mapper (12-PATTERNS.md):

From src/personacore/evaluation/perplexity.py —
- perplexity(model, val_bin_path, block_size, device, batch_size=32, forbid_ids=None) →
  (ppl, total_tokens); @torch.no_grad(); model.eval(); re-opens memmap per call; sum-CE over
  non-overlapping windows with the `end = min(i + block_size + 1, n)` shifted-slice idiom
  (perplexity.py:55-79).
- retention_perplexity(model, val_bin_path, block_size, device, tokenizer, batch_size=32) —
  the DEBT-02 frozen-policy docstring register to copy (perplexity.py:83-105); builds
  forbid_ids via `undecodable_ids_mask(tokenizer, logits.size(-1))`.

From src/personacore/training/data.py:93-126 — get_batch_memmap_masked mask semantics:
mask memmap is np.uint8, 1:1 aligned with tokens; the SHIFTED slice `mask[i+1:end]` pairs
with targets y; `y[m == 0] = -100`; length-mismatch raises with a loud message (data.py:112-116
— copy that raise register).

From src/personacore/generation/core.py:52-71 —
`eid = eos_id if eos_id is not None else model.config.eos_id`; the stop idiom is
`if tok == eid: return` (stop WITHOUT yielding/appending — D-05). `collect()` (core.py:74-83)
forwards `**kw`, needs no change.

From tests/test_retention_ppl.py:26-46 — `_UniformLogitsModel` stub (`forward(idx) ->
(zeros logits over VOCAB, None)`, uniform ⇒ per-token CE = ln(VOCAB)) and the
`ids.tofile(tmp_path / "val.bin")` fixture recipe.

From tests/test_generation.py:31-47, 121-138 — `_tiny_model()` recipe
(`ModelConfig(block_size=8, vocab_size=16, n_layer=1, n_head=1, n_embd=8, eos_id=15)`) and the
forced-argmax stub (`model.forward = _forward`) asserting stop-without-yield via
`out.shape[1] == prompt.shape[1]`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: masked_perplexity() + tests/test_masked_perplexity.py</name>
  <files>src/personacore/evaluation/perplexity.py, tests/test_masked_perplexity.py</files>
  <read_first>
    - src/personacore/evaluation/perplexity.py (whole file — the perplexity() sweep to mirror, retention_perplexity() docstring register)
    - src/personacore/training/data.py (mask semantics + length-mismatch raise, ~93-126)
    - tests/test_retention_ppl.py (_UniformLogitsModel stub + tofile fixture recipe, ~26-46)
    - .planning/phases/12-stage-2-conversational-fine-tune/12-PATTERNS.md (§ masked_perplexity)
    - .planning/phases/12-stage-2-conversational-fine-tune/12-RESEARCH.md (§ Code Examples "masked_perplexity core")
  </read_first>
  <behavior>
    - Test 1 (oracle): uniform-logits stub over VOCAB=16, hand-built uint16 token bin + aligned uint8 mask bin in tmp_path with a known mask==1 shifted-target count K; masked_perplexity returns (ppl, K) with ppl == pytest.approx(16.0) (uniform ⇒ CE = ln(16) per scored token) and total_tokens == K exactly (hand-counted, not derived from the implementation).
    - Test 2 (masking selectivity): change token values ONLY at mask==0 target positions — ppl and total_tokens are unchanged (masked positions contribute nothing).
    - Test 3 (length mismatch): mask bin one element shorter than token bin → ValueError naming both lengths.
    - Test 4 (zero scored): all-zeros mask → ValueError (total_tokens == 0 path).
    - Test 5 (forbid_ids): with a forbid mask over some never-target ids, ppl strictly decreases vs forbid_ids=None on the same fixture (softmax renormalization over fewer ids).
  </behavior>
  <action>
    Add `masked_perplexity(model, bin_path, mask_path, block_size, device, forbid_ids=None)` to
    src/personacore/evaluation/perplexity.py, decorated `@torch.no_grad()`, returning
    `(ppl, total_tokens)`. Mirror perplexity()'s non-overlapping-window sweep exactly (same
    `end = min(i + block_size + 1, n)` slicing, same re-open-memmap-per-call, same
    `model.eval()` entry). Additions: open the mask memmap as np.uint8; fail loud with
    ValueError if `len(mask) != len(data)` (copy the data.py:112-116 raise register, naming both
    lengths); slice the mask SHIFTED with the targets (`mask[i+1:end]`); build
    `y = y.masked_fill(m == 0, -100)`; apply forbid_ids to logits exactly as perplexity() does;
    compute `F.cross_entropy(..., reduction="sum", ignore_index=-100)`; accumulate
    `total_tokens += int((y != -100).sum())`; raise ValueError if total_tokens == 0. Docstring
    copies retention_perplexity()'s policy-freeze framing: this is THE frozen dialogue-val gate
    metric for all Phase 12 arms (Pitfall 3 incommensurability — evaluation policy is fixed
    independently of the training arm). Do NOT modify perplexity() or retention_perplexity().
    TDD order: write tests/test_masked_perplexity.py first (RED), then the function (GREEN).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_masked_perplexity.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def masked_perplexity" src/personacore/evaluation/perplexity.py` shows the signature `(model, bin_path, mask_path, block_size, device, forbid_ids=None)`
    - `grep -n "ignore_index=-100" src/personacore/evaluation/perplexity.py` matches inside masked_perplexity
    - `.venv/bin/python -m pytest tests/test_masked_perplexity.py -q` exits 0 with ≥5 tests collected
    - `.venv/bin/python -m pytest tests/test_perplexity.py tests/test_retention_ppl.py -q` exits 0 (existing metrics untouched; skip the first file if it does not exist)
  </acceptance_criteria>
  <done>Oracle-proven masked PPL with exact auditable denominator; loud raises on mismatch and zero-scored; existing eval functions untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: stop_ids kwarg in generate() + tests/test_stop_ids.py</name>
  <files>src/personacore/generation/core.py, tests/test_stop_ids.py</files>
  <read_first>
    - src/personacore/generation/core.py (whole file — EOS-stop idiom ~52-71, collect() ~74-83)
    - tests/test_generation.py (_tiny_model recipe ~31-47; test_eos_stop forced-argmax stub ~121-138)
    - .planning/phases/12-stage-2-conversational-fine-tune/12-PATTERNS.md (§ stop_ids)
  </read_first>
  <behavior>
    - Test 1 (default equivalence): stop_ids omitted — forced-EOS-argmax stub stops on the first step without yielding EOS, identical asserts to the existing test_eos_stop (out.shape[1] == prompt.shape[1]; last token != eos_id).
    - Test 2 (custom stop): force a non-EOS id (e.g., id 7 on the vocab-16 tiny model) as argmax with stop_ids={7} — generation stops without yielding id 7.
    - Test 3 (multi-id set): stop_ids={eos_id, 7} stops on whichever appears first; EOS still stops when it is the argmax.
    - Test 4 (EOS not implicitly included): with stop_ids={7} and EOS forced as argmax, generation does NOT treat EOS as a stop (it is yielded) — pins the exact "stops = stop_ids if stop_ids is not None else {eid}" replacement semantics.
  </behavior>
  <action>
    Add `stop_ids=None` to the `generate()` signature in src/personacore/generation/core.py.
    The entire change (RESEARCH Pattern 6): compute `stops = stop_ids if stop_ids is not None
    else {eid}` after the existing `eid` resolution, and replace the `if tok == eid:` check with
    `if tok in stops:` — stop-without-yield (D-05 idiom) semantics unchanged. `collect()` needs
    no change (`**kw` pass-through). Docstring in the additive-kwarg register: default None ⇒
    exact v1.0 single-EOS behavior; transcripts pass `stop_ids={8184, 8185}` (eos + `<|user|>`)
    so generation halts when the model starts a hallucinated user turn. generate() is not on the
    LOCKED contract list. TDD order: tests first (RED), then the kwarg (GREEN).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_stop_ids.py tests/test_generation.py -x -q && .venv/bin/python -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "stop_ids" src/personacore/generation/core.py` shows the kwarg with default None and the membership check
    - `.venv/bin/python -m pytest tests/test_stop_ids.py -q` exits 0 with ≥4 tests collected
    - `.venv/bin/python -m pytest tests/test_generation.py -q` exits 0 (v1.0 generation behavior untouched)
    - `.venv/bin/python -m pytest tests/ -q` exits 0 (full suite)
  </acceptance_criteria>
  <done>stop_ids ships additive; default bit-identical to v1.0; stop-without-yield on any member id; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Pure in-repo Python edits + CPU-only tests; no network, no deserialization, no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03 | Tampering | generate() (silent v1.0 behavior drift via stop_ids edit) | mitigate | default-equivalence test (Test 1) + existing tests/test_generation.py green |
| T-12-04 | Repudiation | gate-metric claim integrity (wrong denominator would corrupt every smoke verdict) | mitigate | hand-fixture oracle with hand-counted denominator (Test 1), masking-selectivity test (Test 2) |

No package installs this phase — supply-chain gate not applicable (RESEARCH Package Legitimacy Audit: N/A).
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/ -q` exits 0 (full suite incl. two new files)
- `masked_perplexity` and `stop_ids` both exist, additive, defaults preserve v1.0 behavior
</verification>

<success_criteria>
- The frozen dialogue-val gate metric exists and is oracle-proven before any smoke number is produced
- Transcript stopping machinery exists with pinned default-equivalence
- Full suite green
</success_criteria>

<output>
Create `.planning/phases/12-stage-2-conversational-fine-tune/12-02-SUMMARY.md` when done.
</output>
