---
phase: 12-stage-2-conversational-fine-tune
plan: 05
type: execute
wave: 3
depends_on: [12-04]
files_modified:
  - scripts/finetune_dialog.py
  - scripts/make_transcripts.py
  - results/finetune_prod.csv
  - results/transcripts.md
autonomous: true
requirements: [TUNE-01, TUNE-02, EWC-03]
must_haves:
  truths:
    - "The production driver refuses to run without a recorded non-PENDING GO verdict in results/finetune_smoke_report.md (D-07 gate enforcement)"
    - "checkpoints/convbase_best.pt exists with fisher/theta_star/ewc_lambda/fisher_meta embedded (self-contained resume); checkpoints/convbase_slim.pt loads under weights_only=True"
    - "results/finetune_prod.csv carries retention_ppl and dialog_ppl at every eval interval from a step-0 row — the forgetting curve falls out of the training log, not post-hoc reconstruction"
    - "results/transcripts.md commits 10-20 transcripts generated through the Phase 11 serialize path with stop_ids={8184, 8185}, with measurable proxies (stop-id termination fraction, zero mid-utterance role-token leakage) and the final masked dialogue val PPL"
    - "Seed, config, λ*, and data-order provenance are recorded so Phase 13 can run the identical-seed λ=0 twin (production run doubles as the DEMO-04 EWC arm)"
  artifacts:
    - path: "scripts/finetune_dialog.py"
      provides: "production fine-tune driver (GO-verdict gated)"
      contains: "convbase_best"
    - path: "scripts/make_transcripts.py"
      provides: "curated-transcript generator via serialize path + stop_ids"
      contains: "stop_ids"
    - path: "results/finetune_prod.csv"
      provides: "TUNE-02 production retention/dialog curve from step 0"
      contains: "retention_ppl"
    - path: "results/transcripts.md"
      provides: "TUNE-01 dialogue-format-adherence evidence"
  key_links:
    - from: "scripts/finetune_dialog.py"
      to: "src/personacore/training/loop.py"
      via: "train(train_mask_bin, val_mask_bin, penalty_fn=EWCPenalty(λ*), extra_eval_fns, checkpoint_extra)"
      pattern: "checkpoint_extra"
    - from: "scripts/finetune_dialog.py"
      to: "src/personacore/checkpoint.py"
      via: "export_slim(convbase_best.pt, convbase_slim.pt) — LOCKED weights_only=True contract"
      pattern: "export_slim"
    - from: "scripts/make_transcripts.py"
      to: "src/personacore/dialogue/serialize.py"
      via: "encode_dialogue / render_document — prompts tokenize identically to the bins"
      pattern: "encode_dialogue"
---

<objective>
Run the production fine-tune of `best.pt` on the PersonaChat bins with the D-07-approved config
(mask arm, LR*, λ*), producing the conversational-base checkpoint (Phase 13's EWC arm and
Phase 14's LoRA substrate), the step-0-anchored retention/dialog curve CSV, and the committed
transcript evidence.

Purpose: this is the phase's deliverable trio — TUNE-01 (dialogue-capable conversational base +
val PPL + transcripts), TUNE-02 (retention curve from step 0 in the run's own CSV), EWC-03
(λ* consumed in anger, sweep already retained). The run is designed to double as Phase 13's EWC
arm: identical-seed provenance recorded so the λ=0 twin differs in exactly one bit.

Output: `checkpoints/convbase_{latest,best}.pt` + `convbase_slim.pt` (gitignored),
`results/finetune_prod.csv`, `results/transcripts.md`, two driver scripts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-stage-2-conversational-fine-tune/12-CONTEXT.md
@.planning/phases/12-stage-2-conversational-fine-tune/12-PATTERNS.md
@.planning/phases/12-stage-2-conversational-fine-tune/12-04-SUMMARY.md
@scripts/estimate_fisher_tinystories.py
@scripts/evaluate.py

<interfaces>
Verified this session (12-PATTERNS.md / repo greps):

- Production invocation template: RESEARCH 12-RESEARCH.md § Code Examples "Production
  fine-tune invocation" is authoritative — train() with train_bin/train_mask_bin +
  val_bin/val_mask_bin, penalty_fn=EWCPenalty(fisher, theta_star, lam=λ*, device),
  extra_eval_fns={"retention_ppl": retention_perplexity on data/retention_val.bin,
  "dialog_ppl": masked_perplexity on dialog_val + mask, "ewc_penalty": float(penalty(m))},
  checkpoint_extra={"fisher": ..., "theta_star": ..., "ewc_lambda": λ*, "fisher_meta": ...},
  checkpoint_path=checkpoints/convbase_latest.pt, best_checkpoint_path=convbase_best.pt,
  log_path=results/finetune_prod.csv, eval_interval=250, checkpoint_interval=250.
- `export_slim(full_path, slim_path, map_location="cpu")` — checkpoint.py:148 (LOCKED
  weights_only=True shippable contract). `load_fisher(path, expected_fingerprint=...)` —
  checkpoint.py:295.
- GO-verdict gate register: `_require_go_verdict(report_path)` in
  scripts/prepare_dialog_corpus.py:61-82 (regex on `## Verdict`; refuses on PENDING/missing).
- Missing-prerequisite register: estimate_fisher_tinystories.py:66-74 (FileNotFoundError
  naming the command to run first).
- Transcript path: `parse_episodes(path)` (dialogue/parse.py:14) on
  data/raw/personachat/valid_self_revised.txt; `encode_dialogue(tok, persona, turns)`
  (serialize.py:55) / `render_document(persona, turns)` (serialize.py:42);
  `detokenize(text)`. Prompt = persona + first user turn, encoded so the id sequence ends at
  the `<|assistant|>` id 8186 — NEVER hand-formatted strings (inflation-report Pitfall 4:
  transcripts must tokenize identically to the bins).
- Generation: `generate()`/`collect()` with `stop_ids={8184, 8185}` (eos + `<|user|>`, Plan
  12-02) and `forbid_ids=undecodable_ids_mask(tok, vocab_size)`; sampling register from
  evaluate.py:114-159 (greedy + temperature 0.8 / top_p 0.95 seeded warm sample;
  `results/samples.md` markdown structure; "REPRESENTATIVE, not cherry-picked" header).
  Tokenizer: `from_json(artifacts/tokenizer.json)` — FROZEN.
- Driver registers: MPS-fallback env before torch import; preflight_device(strict=True);
  seed_everything(SEED) immediately before the GPT build; grad_accum_steps=1 (Pitfall 2);
  weights_only=False only on own checkpoints with trusted-only comment.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write scripts/finetune_dialog.py (GO-gated production driver)</name>
  <files>scripts/finetune_dialog.py</files>
  <read_first>
    - .planning/phases/12-stage-2-conversational-fine-tune/12-04-SUMMARY.md (approved mask arm, LR*, λ*, PROD_MAX_STEPS, SMOKE_STEPS, step-0-row mechanism — the constants this driver hardcodes)
    - results/finetune_smoke_report.md (the recorded verdict + Proposed Production Config section)
    - scripts/finetune_smoke.py (_load_anchor_model/_run_arm registers to reuse; step-0 CSV pre-seed technique)
    - scripts/prepare_dialog_corpus.py (_require_go_verdict register, ~61-82)
    - src/personacore/checkpoint.py (export_slim + load_fisher signatures)
  </read_first>
  <action>
    Create scripts/finetune_dialog.py in the house driver register. Constants (each with a
    comment citing the smoke report): MASK_ARM, LR_STAR, LAMBDA_STAR, PROD_MAX_STEPS (the
    D-07-approved values from 12-04-SUMMARY — hardcode the actual numbers, do not parse the
    report at runtime), EVAL_INTERVAL = 250, CHECKPOINT_INTERVAL = 250, SEED = 1337,
    batch_size 32, grad_accum_steps = 1. Flow: (1) `_require_go_verdict(
    results/finetune_smoke_report.md)` — refuse to train on PENDING/missing verdict (copy the
    prepare_dialog_corpus register). (2) FileNotFoundError registers for best.pt, fisher cache,
    dialog bins, retention_val.bin. (3) preflight_device(strict=True); runtime =
    RuntimeConfig(); seed_everything(SEED) immediately before the GPT build from the best.pt
    blob (weights_only=False trusted-only; load_state_dict; never resume_from); theta_star CPU
    clones; load_fisher with the fingerprint trio from the blob; EWCPenalty(fisher, theta_star,
    lam=LAMBDA_STAR, device=runtime.device) constructed ONCE. (4) Step-0 row per the 12-04
    mechanism (native or pre-seeded CSV) so results/finetune_prod.csv starts at step 0.
    (5) train() per the RESEARCH production-invocation template (interfaces above): masked bins
    per MASK_ARM, val_mask_bin always set, penalty_fn, the three extra_eval_fns, checkpoint_extra
    with fisher/theta_star/ewc_lambda/fisher_meta (self-contained resume — never depends on the
    Fisher sidecar), convbase_latest/convbase_best paths, log to results/finetune_prod.csv.
    (6) After training: `export_slim(checkpoints/convbase_best.pt,
    checkpoints/convbase_slim.pt)`; loud SystemExit proofs — final CSV row step ==
    PROD_MAX_STEPS, retention_ppl column finite everywhere, convbase_best blob contains the
    four extra keys, slim blob loads with weights_only=True. (7) Print a Phase-13 provenance
    block: seed, config dict, λ*, git_sha — recorded for the identical-seed λ=0 twin (DEMO-04).
  </action>
  <verify>
    <automated>.venv/bin/python -m ruff check scripts/finetune_dialog.py && grep -q "_require_go_verdict\|## Verdict" scripts/finetune_dialog.py && grep -q "export_slim" scripts/finetune_dialog.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "PENDING" scripts/finetune_dialog.py` matches inside the verdict-gate logic
    - `grep -vE '^\s*#' scripts/finetune_dialog.py | grep -c "resume_from"` == 0
    - `grep -n "checkpoint_extra" scripts/finetune_dialog.py` shows fisher, theta_star, ewc_lambda, fisher_meta keys
    - `grep -n "convbase_latest\|convbase_best\|convbase_slim" scripts/finetune_dialog.py` shows all three checkpoint names
    - `.venv/bin/python -m ruff check scripts/finetune_dialog.py` exits 0
  </acceptance_criteria>
  <done>Production driver exists, gated on the recorded D-07 verdict, with the approved constants hardcoded and provenance printed for Phase 13.</done>
</task>

<task type="auto">
  <name>Task 2: Run the production fine-tune; verify curve + artifact contract</name>
  <files>results/finetune_prod.csv</files>
  <read_first>
    - scripts/finetune_dialog.py (the driver just written)
  </read_first>
  <action>
    Run `.venv/bin/python scripts/finetune_dialog.py` in the 3.11 venv on the M3 (MPS fp32;
    multi-hour — kill+resume via convbase_latest.pt is bit-exact per the v1.0 resume contract,
    so laptop sleep is safe). Expected shape (record, don't gate): dialogue val loss falls to a
    best then overfits past it — best-checkpoint selection does the stopping (PITFALLS 15);
    retention_ppl drifts up from the step-0 anchor by an amount EWC is meant to bound. On
    completion verify the driver's own proofs passed, then commit results/finetune_prod.csv.
    Checkpoints stay gitignored. Record in the SUMMARY: best step, best masked val loss, final
    vs step-0 retention_ppl, wall-clock, and the Phase-13 provenance block.
  </action>
  <verify>
    <automated>.venv/bin/python -c "
import csv,math
rows=list(csv.DictReader(open('results/finetune_prod.csv')))
assert rows[0]['step']=='0', 'no step-0 row'
assert all(math.isfinite(float(r['retention_ppl'])) for r in rows if r.get('retention_ppl') not in (None,'')), 'non-finite retention'
assert 'dialog_ppl' in rows[0] and 'ewc_penalty' in rows[0], 'missing extras columns'
print(len(rows),'rows; step0 retention',rows[0]['retention_ppl'])
" && test -s checkpoints/convbase_best.pt && test -s checkpoints/convbase_slim.pt</automated>
  </verify>
  <acceptance_criteria>
    - `results/finetune_prod.csv` first data row has step 0 with a finite retention_ppl (TUNE-02 from-step-0)
    - Every eval row carries retention_ppl, dialog_ppl, ewc_penalty columns (finite)
    - `checkpoints/convbase_best.pt` exists; `.venv/bin/python -c "import torch;b=torch.load('checkpoints/convbase_best.pt',weights_only=False);assert all(k in b for k in ('fisher','theta_star','ewc_lambda','fisher_meta'));print(b['ewc_lambda'])"` prints λ*
    - `.venv/bin/python -c "import torch;torch.load('checkpoints/convbase_slim.pt',weights_only=True)"` exits 0 (LOCKED slim contract)
    - `results/finetune_prod.csv` committed to tracked results/
  </acceptance_criteria>
  <done>Conversational-base checkpoint exists with self-contained EWC extras; slim export ships; the production forgetting curve is in the training log from step 0.</done>
</task>

<task type="auto">
  <name>Task 3: scripts/make_transcripts.py → results/transcripts.md (TUNE-01 evidence)</name>
  <files>scripts/make_transcripts.py, results/transcripts.md</files>
  <read_first>
    - scripts/evaluate.py (samples.md generation register, ~114-159; TOKENIZER_PATH; forbid-ids usage)
    - src/personacore/dialogue/serialize.py (render_document/encode_dialogue — the ONLY prompt path)
    - src/personacore/dialogue/parse.py (parse_episodes signature)
    - src/personacore/generation/core.py (generate/collect + stop_ids final signature from Plan 12-02)
    - scripts/prepare_dialog_corpus.py (val split source path + persona capping, ~53-105)
  </read_first>
  <action>
    Create scripts/make_transcripts.py in the house driver register. Flow: load
    checkpoints/convbase_best.pt (weights_only=False trusted-only; FileNotFoundError register
    pointing at finetune_dialog.py); tok = from_json(artifacts/tokenizer.json);
    episodes = parse_episodes(data/raw/personachat/valid_self_revised.txt) — held-out split;
    select N_TRANSCRIPTS = 15 episodes with a seeded LOCAL rng (np.random.default_rng(1337)).
    Per episode: build the prompt id sequence via encode_dialogue (persona + first user turn
    only), truncated so it ENDS at the `<|assistant|>` id 8186 (the model completes the
    assistant turn) — never hand-formatted strings. Decode twice per prompt via collect():
    greedy, and seeded warm sample (temperature 0.8, top_p 0.95 — the evaluate.py register),
    both with forbid_ids=undecodable_ids_mask(tok, vocab_size) and stop_ids={8184, 8185},
    max_new_tokens 128. Compute measurable proxies over all generations: (a) fraction
    terminating on a stop id rather than max_new_tokens; (b) count of role-token ids
    (8185/8186/8187) appearing mid-generation (must be 0 — leakage check); (c) the final masked
    dialogue val PPL (one masked_perplexity call on convbase_best). Write
    results/transcripts.md in the samples.md register: header stating "REPRESENTATIVE, not
    cherry-picked" + the three proxy numbers + per-episode blocks (persona summary, user turn,
    greedy completion, warm completion, rendered via detokenize). Run it; commit script +
    results/transcripts.md.
  </action>
  <verify>
    <automated>.venv/bin/python -m ruff check scripts/make_transcripts.py && test -s results/transcripts.md && grep -c "^## " results/transcripts.md | awk '{exit ($1<10)}' && grep -qi "stop.id\|termination" results/transcripts.md</automated>
  </verify>
  <acceptance_criteria>
    - `results/transcripts.md` contains ≥ 10 transcript sections (10-20 target), each with greedy AND warm completions
    - The file reports the stop-id termination fraction, the mid-generation role-token leakage count (expected 0), and the masked dialogue val PPL number
    - `grep -n "encode_dialogue" scripts/make_transcripts.py` matches (serialize path, not string formatting)
    - `grep -n "stop_ids" scripts/make_transcripts.py` shows {8184, 8185}
    - `.venv/bin/python -m pytest tests/ -q` exits 0 (full suite green at phase end)
  </acceptance_criteria>
  <done>Committed transcript evidence with measurable adherence proxies; dialogue-format adherence demonstrated with numbers alongside text.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| own checkpoint deserialization | `torch.load(weights_only=False)` on best.pt / convbase_best.pt / fisher cache — own trusted artifacts only |
| shippable artifact | convbase_slim.pt MUST load under weights_only=True (LOCKED contract — the artifact others may load) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-12 | Elevation of privilege | slim artifact carrying pickled code | mitigate | export_slim + weights_only=True load proof in Task 2 acceptance |
| T-12-13 | Repudiation | production run bypassing the D-07 gate | mitigate | _require_go_verdict SystemExit on PENDING/missing verdict |
| T-12-14 | Tampering | Fisher/λ mismatch corrupting the EWC arm | mitigate | load_fisher fingerprint trio pinned to the anchor blob; checkpoint_extra makes resume self-contained |
| T-12-15 | Information disclosure | transcripts leaking non-corpus data | accept | generations conditioned only on public PersonaChat val episodes; no secrets in the pipeline |

No package installs — supply-chain gate not applicable.
</threat_model>

<verification>
- Production driver refused to run until the GO verdict existed; run completed with best-checkpoint selection
- convbase_best.pt (with 4 EWC extras) + convbase_slim.pt (weights_only=True) exist
- results/finetune_prod.csv committed with step-0-anchored retention/dialog columns
- results/transcripts.md committed with proxies; full pytest suite green
</verification>

<success_criteria>
- TUNE-01: conversational base reached dialogue-format adherence — masked val PPL reported, curated transcripts committed
- TUNE-02: retention curve from step 0 in the run's own per-run CSV
- EWC-03 consumed: λ* in the production penalty and embedded in the artifact; Phase 13/14 substrates ready (identical-seed provenance recorded)
</success_criteria>

<output>
Create `.planning/phases/12-stage-2-conversational-fine-tune/12-05-SUMMARY.md` when done.
Record: best step + best masked val loss, step-0 vs final retention_ppl, transcript proxy
numbers, and the Phase-13 provenance block (seed/config/λ*/git_sha).
</output>
