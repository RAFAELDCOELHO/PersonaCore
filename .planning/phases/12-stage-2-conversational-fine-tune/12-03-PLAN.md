---
phase: 12-stage-2-conversational-fine-tune
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/build_retention_bin.py
  - data/retention_val.bin
  - results/retention_anchors.json
autonomous: true
requirements: [TUNE-02]
must_haves:
  truths:
    - "data/retention_val.bin exists: a ~1.0M-token document-level (eos-boundary) subsample of data/val.bin, built with a seeded LOCAL rng, frozen for the milestone (rerun refuses with SystemExit)"
    - "results/retention_anchors.json commits all three step-0 numbers BEFORE any training step: measured masked sub-bin anchor (the curve anchor), measured masked full-val reference, and the 2.1066 unmasked headline recorded as historical reference only (Pitfall 1)"
    - "Masked full-val PPL < 2.1066 (dead-id renormalization proof check — loud SystemExit if violated)"
  artifacts:
    - path: "scripts/build_retention_bin.py"
      provides: "run-once sub-bin builder + step-0 anchor measurement"
      contains: "retention_val.bin"
    - path: "results/retention_anchors.json"
      provides: "committed step-0 anchors consumed by the smoke report (Plan 12-04)"
      contains: "retention_ppl_subbin_step0"
  key_links:
    - from: "scripts/build_retention_bin.py"
      to: "src/personacore/evaluation/perplexity.py"
      via: "retention_perplexity() — the ONLY sanctioned retention PPL (DEBT-02 frozen policy)"
      pattern: "retention_perplexity"
---

<objective>
Build the frozen TinyStories retention sub-bin and measure the step-0 anchors — before any
training step exists anywhere in the phase.

Purpose: full val is 12,636,923 tokens ≈ 49k single-window forwards per PPL point — minutes per
eval interval, unaffordable on the training cadence (RESEARCH Pattern 1). And 2.1066 is NOT the
retention curve's anchor: it is unmasked full-val, while `retention_perplexity()` masks dead ids
(renormalized softmax ⇒ strictly lower PPL) and the curve runs on a sub-bin (Pitfall 1). The
curve's true anchor must be MEASURED at step 0 and committed, or every downstream figure
(Phase 13 VIZ-01's dashed line) will mislead.

Output: `data/retention_val.bin` (frozen), `results/retention_anchors.json` (committed, tracked).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-stage-2-conversational-fine-tune/12-CONTEXT.md
@.planning/phases/12-stage-2-conversational-fine-tune/12-PATTERNS.md
@scripts/estimate_fisher_tinystories.py
@scripts/prepare_dialog_corpus.py

<interfaces>
Verified this session (12-PATTERNS.md / repo greps):

- `retention_perplexity(model, val_bin_path, block_size, device, tokenizer, batch_size=32)` →
  (ppl, total_tokens) — perplexity.py:83; builds forbid_ids internally via
  `undecodable_ids_mask(tokenizer, logits.size(-1))`. THE frozen policy — never reimplement.
- `perplexity(model, val_bin_path, block_size, device, batch_size=32, forbid_ids=None)` —
  perplexity.py:33 (unmasked; 2.1066 headline came from this on full val).
- Anchor load register (estimate_fisher_tinystories.py:89-103): `torch.load(BEST_PATH,
  weights_only=False)` with the TRUSTED-only comment; `ModelConfig(**blob["model_config"])`;
  `GPT(model_cfg)`; `load_state_dict(blob["model"])`; `.to(runtime.device)`.
- Refuse-to-rerun register (estimate_fisher_tinystories.py:78-82): SystemExit if the output
  artifact already exists.
- Local-rng discipline (estimate_fisher_tinystories.py:55): `np.random.default_rng(SEED)` —
  global streams untouched.
- Driver-script register (run_ablations.py:44-63): `os.environ.setdefault(
  "PYTORCH_ENABLE_MPS_FALLBACK", "1")` BEFORE `import torch` (# noqa: E402); no CLI flags;
  `_REPO_ROOT` pathlib constants; `preflight_device(strict=True)` gate then
  `runtime = RuntimeConfig()`; `[build_retention_bin]`-prefixed prints.
- Tokenizer: `from personacore.tokenizer import from_json`;
  `TOKENIZER_PATH = _REPO_ROOT / "artifacts" / "tokenizer.json"` (evaluate.py:44-49).
- Loud sanity-check register (prepare_dialog_corpus.py:134-160): SystemExit, never bare assert.
- EOS_ID = 8184; data/val.bin dtype np.uint16.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write scripts/build_retention_bin.py (build + anchors, run-once)</name>
  <files>scripts/build_retention_bin.py</files>
  <read_first>
    - scripts/estimate_fisher_tinystories.py (anchor-load, refuse-to-rerun, local-rng, loud-proof registers)
    - scripts/prepare_dialog_corpus.py (bin write via np tofile + SystemExit sanity checks, ~120-160)
    - src/personacore/evaluation/perplexity.py (retention_perplexity + perplexity signatures/docstrings)
    - .planning/phases/12-stage-2-conversational-fine-tune/12-PATTERNS.md (§ build_retention_bin.py, § Shared Patterns)
  </read_first>
  <action>
    Create scripts/build_retention_bin.py in the driver-script register (MPS-fallback env before
    torch import, no CLI flags, _REPO_ROOT constants, [build_retention_bin] prints,
    `if __name__ == "__main__": main()`). Constants with rationale comments: EOS_ID = 8184,
    TARGET_TOKENS = 1_000_000 (RESEARCH Pattern 1: ~3.9k windows ⇒ a retention point costs
    roughly a minute on MPS instead of minutes×10), SEED = 1337, VAL_BIN = data/val.bin,
    OUT_BIN = data/retention_val.bin, ANCHORS_JSON = results/retention_anchors.json,
    BEST_PATH = checkpoints/best.pt, TOKENIZER_PATH = artifacts/tokenizer.json, BLOCK_SIZE = 256.
    Flow: (1) refuse-to-rerun — SystemExit if OUT_BIN or ANCHORS_JSON exists (the sub-bin is
    FROZEN for the milestone; delete both to rebuild). (2) memmap VAL_BIN (np.uint16), find eos
    positions, split into documents at eos boundaries (each doc keeps its trailing eos), shuffle
    doc ORDER with `np.random.default_rng(SEED)` (local rng — global streams untouched),
    accumulate whole docs until total ≥ TARGET_TOKENS, concatenate, `.tofile(OUT_BIN)` as uint16.
    (3) Loud SystemExit sanity checks (never bare assert): eos count in OUT_BIN > 100; final
    token count within [TARGET_TOKENS, 1.1 × TARGET_TOKENS + max_doc_len]; decoded 200-char
    prefix printed via the frozen tokenizer. (4) Anchors: `preflight_device(strict=True)` then
    `runtime = RuntimeConfig()`; load best.pt (weights_only=False with the TRUSTED-only comment),
    build GPT, load_state_dict, to(device), tok = from_json(TOKENIZER_PATH); measure
    `retention_perplexity(model, OUT_BIN, 256, runtime.device, tok)` and
    `retention_perplexity(model, VAL_BIN, 256, runtime.device, tok)` (full val — one-time cost,
    minutes; print progress note). (5) Proof check: SystemExit unless masked full-val PPL <
    2.1066 (dead-id renormalization must lower PPL vs the unmasked headline on the same bin —
    dead ids never appear as targets). (6) Write ANCHORS_JSON with keys:
    retention_ppl_subbin_step0, retention_tokens_subbin, retention_ppl_fullval_step0,
    retention_tokens_fullval, headline_unmasked_fullval (literal 2.1066, annotated key
    headline_note = "historical unmasked reference only — NOT the curve anchor, Pitfall 1"),
    subbin_token_count, subbin_seed, anchor_val_loss (from blob), git_sha (provenance.git_sha()),
    built date. json.dump with indent=2.
  </action>
  <verify>
    <automated>.venv/bin/python -m ruff check scripts/build_retention_bin.py && grep -v '^\s*#' scripts/build_retention_bin.py | grep -c "SystemExit" | awk '{exit ($1<3)}'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "PYTORCH_ENABLE_MPS_FALLBACK" scripts/build_retention_bin.py` matches before the torch import line
    - `grep -n "default_rng" scripts/build_retention_bin.py` matches (local rng, not global seeding)
    - `grep -n "weights_only=False" scripts/build_retention_bin.py` matches with a trusted-only comment on the same or adjacent line
    - `grep -vE '^\s*#' scripts/build_retention_bin.py | grep -c "SystemExit"` ≥ 3 (refuse-to-rerun + sanity + proof check)
    - `.venv/bin/python -m ruff check scripts/build_retention_bin.py` exits 0
  </acceptance_criteria>
  <done>Run-once builder script exists in the house register with refuse-to-rerun, seeded doc-level subsample, loud proofs, and anchor measurement + JSON write.</done>
</task>

<task type="auto">
  <name>Task 2: Run the builder; commit the frozen bin metadata + anchors</name>
  <files>data/retention_val.bin, results/retention_anchors.json</files>
  <read_first>
    - scripts/build_retention_bin.py (the script just written)
    - .gitignore (confirm data/*.bin posture — commit the JSON regardless; commit the bin only if data bins are tracked, which they are for dialog_*.bin per `git ls-files data/`)
  </read_first>
  <action>
    Run `.venv/bin/python scripts/build_retention_bin.py` in the mandatory 3.11 venv on the M3
    (MPS fp32; full-val anchor sweep takes minutes — let it finish). Confirm all proofs print
    and the script exits 0. Check `git ls-files data/ | head` — if dialog bins are tracked,
    stage data/retention_val.bin too (it is ~2 MB); if data bins are gitignored, the frozen-ness
    guarantee is the refuse-to-rerun + seeded build (record that in the SUMMARY). Always commit
    results/retention_anchors.json (tracked results/, Pitfall 6). Verify rerun protection:
    a second invocation must SystemExit naming the existing artifact.
  </action>
  <verify>
    <automated>test -s data/retention_val.bin && .venv/bin/python -c "import json;d=json.load(open('results/retention_anchors.json'));import math;assert math.isfinite(d['retention_ppl_subbin_step0']) and d['retention_ppl_subbin_step0']>1;assert d['retention_ppl_fullval_step0']<2.1066;print(d['retention_ppl_subbin_step0'],d['retention_ppl_fullval_step0'])"</automated>
  </verify>
  <acceptance_criteria>
    - `data/retention_val.bin` exists with size between 2,000,000 and 2,300,000 bytes (uint16 × ~1.0-1.15M tokens)
    - `results/retention_anchors.json` parses; retention_ppl_subbin_step0 finite and > 1; retention_ppl_fullval_step0 < 2.1066; headline_unmasked_fullval == 2.1066
    - Second invocation of the script exits nonzero with a message containing "refus" (refuse-to-rerun verified)
    - `git status --porcelain results/retention_anchors.json` shows it staged/committed by task end
  </acceptance_criteria>
  <done>Frozen sub-bin built; all three step-0 anchor numbers measured and committed before any training step; rerun refuses.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| own checkpoint deserialization | `torch.load(weights_only=False)` on checkpoints/best.pt — project's OWN trusted artifact only (v1.0 T-07-05 discipline) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05 | Tampering | frozen sub-bin (silent rebuild would shift every curve point) | mitigate | refuse-to-rerun SystemExit + seeded local-rng build (bit-reproducible) |
| T-12-06 | Repudiation | anchor claim integrity (asserted-not-measured step-0 values, Pitfall 1) | mitigate | anchors measured by the script on best.pt, committed JSON with git_sha provenance; fullval < 2.1066 proof check |
| T-12-07 | Spoofing | untrusted pickle load | accept | weights_only=False only on own best.pt with trusted-only comment; no foreign files enter this path |

No package installs — supply-chain gate not applicable.
</threat_model>

<verification>
- results/retention_anchors.json committed with three anchor numbers; fullval masked < 2.1066
- data/retention_val.bin frozen (rerun refuses)
- `.venv/bin/python -m pytest tests/ -q` still exits 0 (no src/ changes in this plan)
</verification>

<success_criteria>
- The retention curve's true anchor is a measured, committed number — not an assertion — before any fine-tune step exists
- Every later curve point (smoke, λ arms, production) has a frozen bin + frozen policy to ride
</success_criteria>

<output>
Create `.planning/phases/12-stage-2-conversational-fine-tune/12-03-SUMMARY.md` when done.
Record: the three anchor values and sub-bin token count (Plan 12-04 embeds them in the smoke report).
</output>
