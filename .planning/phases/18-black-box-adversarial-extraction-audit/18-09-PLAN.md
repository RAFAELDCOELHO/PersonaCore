---
phase: 18-black-box-adversarial-extraction-audit
plan: 09
type: execute
wave: 7
depends_on: ["18-08", "18-01"]
files_modified:
  - scripts/phase18_extraction.py
  - tests/test_phase18_prereg.py
autonomous: true
requirements: [STAT-01, STAT-02, STAT-06, ATK-03, ATK-05]

must_haves:
  truths:
    - "D-01: family zero asserts EXACT HIT-VECTOR EQUALITY on the 112 `core_taught` questions, row-for-row against `results/phase14_recall_report.md`; `496/1008` is a DERIVED CONSEQUENCE and never an independent assertion, so a run diverging on one question of 112 fails the harness-sanity check even if the aggregate sum matches"
    - "D-01: a tolerance band is DECLINED — the quantity already reproduced exactly (0/112 per-question mismatches), and banding a quantity that reproduced exactly discards measured precision for a width with no derivation"
    - "D-31: the Holm family is m=4, dose-split (A1-mild, A1-aggressive, A2, A3), on `core_held_out` ONLY; `core_taught` enters NO family because it is the ATK-03 positive control, not an inferential claim; the call passes `family=HOLM_FAMILY` to the widened `holm`"
    - "D-02/D-27: the `core_held_out` verdict emits exactly `erasure_is_worth_attempting(attack_successes, attack_questions, base_successes, base_questions)` — four positional ints in the QUESTION unit — and the post-hoc max over families is pre-registered in advance because `ERASURE_DECISION_RULE` names Phase 18's BEST attack at its pre-registered budget"
    - "D-22: exposure contributes zero comparisons to the Holm family; it enters only through `null_result_is_admissible()`"
    - "DD-03/STAT-06: the fact-level cluster bootstrap at n=8 clusters is DESCRIPTIVE with its known undercoverage stated, and the exact paired sign test over 2^8 partitions is the only inferential instrument"
  artifacts:
    - path: "scripts/phase18_extraction.py"
      provides: "family_zero_matches, run_holm_family, assemble_verdict"
      contains: "def family_zero_matches"
    - path: "tests/test_phase18_prereg.py"
      provides: "family-zero vector comparison, Holm family call, erasure interface shape"
      min_lines: 360
  key_links:
    - from: "scripts/phase18_extraction.py::run_holm_family"
      to: "phase16_persistence.holm"
      via: "family=HOLM_FAMILY keyword"
      pattern: "holm\\(.*family=HOLM_FAMILY"
    - from: "scripts/phase18_extraction.py::assemble_verdict"
      to: "erasure_gate.erasure_is_worth_attempting"
      via: "four positional question-unit ints"
      pattern: "erasure_is_worth_attempting\\("
---

<objective>
Assemble the verdict: the D-01 positive-control comparison, the m=4 Holm family, and the
`erasure_is_worth_attempting` handoff — all pure, all unit-tested on synthetic records.

Purpose: ATK-03 converts "our attacks found nothing" from unfalsifiable into testable, and it only
does that if the control comparison is the exact hit vector rather than the aggregate that happens
to match. This is the last block of the driver's inferential layer.
Output: three pure functions plus their committed test cases; no artifact is written.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/18-black-box-adversarial-extraction-audit/18-CONTEXT.md
@.planning/phases/18-black-box-adversarial-extraction-audit/18-PATTERNS.md
</context>

<interfaces>
`scripts/phase16_persistence.py` — `sign_test_exact(signs)` (`:1088`) returns 1.0 whenever
positives <= `SIGN_TEST_N`/2 (D-29 convention, deliberate); `holm(p_values, *, family=...)`
(`:1170`, widened by 18-01) takes a `{pair: p}` MAPPING and compares STRICTLY (`p < alpha_at_step`);
`cluster_bootstrap(per_fact_questions, *, resamples, seed, alpha)` (`:843`) is two-stage —
facts resampled first (n=8), then that fact's questions.

`scripts/erasure_gate.py:173` —
`erasure_is_worth_attempting(attack_successes, attack_questions, base_successes, base_questions)`,
four POSITIONAL ints, question unit. Do not edit this file.

`results/phase14_recall_report.md` — the 112 per-question taught rows D-01 asserts against;
`:58` carries `496/1008`. Verified this session: filtering `results/phase16_arm_adapter-only.json`
to the 8 core slots reproduces `core_taught` 496/1008 and `core_held_out` 326/936, and a
per-question diff of all 112 taught rows against the arm's `hits` gave 0 mismatches.

From 18-03: `HOLM_FAMILY`, `GATED_TIER`, `REPORTED_TIER`, `VERDICTS`, `BEST_ACHIEVABLE_P`.
From 18-07: `null_result_is_admissible`, `licensed_conclusion`.
From 18-08: `score_records`, `asr_ladder`, `aggregate_questions`, `unique_successes`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: family_zero_matches — the exact hit-vector comparison</name>
  <files>scripts/phase18_extraction.py, tests/test_phase18_prereg.py</files>
  <read_first>
    - `results/phase14_recall_report.md` — the taught per-question table, to fix the parse target
      (column order, row count, the hit representation)
    - `scripts/phase16_persistence.py:380-460` — `PER_QUESTION_KEYS` and `assert_record_shape`, the
      explicit-schema register
    - `.planning/phases/18-black-box-adversarial-extraction-audit/18-CONTEXT.md` — D-01, D-09
  </read_first>
  <behavior>
    - `parse_phase14_taught_rows()` returns exactly 112 rows, each carrying its `seed_index`,
      `fact_id` and per-draw hit vector, and `_prove`s the count — a short parse is a silent pass
    - `family_zero_matches(recorded, reference)` returns `(True, [])` only when every one of the 112
      hit vectors is equal element-for-element
    - a single flipped draw on one question returns `(False, [...])` naming that question, EVEN WHEN
      the aggregate sum still equals 496 — the case that proves the aggregate is derived, not asserted
    - the derived sum is returned as a consequence field, never compared as the primary assertion
    - no tolerance parameter exists anywhere in the signature
  </behavior>
  <action>
    Add `PHASE14_TAUGHT_REPORT = _REPO_ROOT / "results" / "phase14_recall_report.md"`,
    `parse_phase14_taught_rows()` and `family_zero_matches(recorded_rows, reference_rows)`.
    Compare row-for-row on `(seed_index, fact_id, hit_vector)`, returning the mismatching questions
    by `seed_index` so an abort names which of the 112 diverged. Compute the derived
    `sum(hits) / sum(draws)` and return it as a labelled consequence field with a docstring sentence
    recording D-01: the aggregate is a derived consequence, never an independent assertion, and a
    tolerance band is declined because the quantity already reproduced exactly and a band's width
    would be a number with no derivation. Do not add a `tolerance=`, `atol=` or `band=` parameter.
    Record the PERS-05 scoping note: the seeding defect was scoped to `run_fairness_control`, not the
    scored adapter-on path, so reading STATE.md's "does not reproduce bit-for-bit" as covering the
    taught headline produces a phantom delta against the pooled 140-question split Phase 14 never
    published.
  </action>
  <acceptance_criteria>
    - `.venv/bin/pytest -q tests/test_phase18_prereg.py::test_family_zero_compares_the_vector -x` passes
    - The test includes the sum-preserving mismatch case (one hit moved between two questions, total
      still 496) and asserts `family_zero_matches` returns False
    - `.venv/bin/python -c` runs `parse_phase14_taught_rows()` against the real report and asserts
      `len(rows) == 112` and that the derived numerator is 496 over 1008 — recorded in the SUMMARY
    - `grep -cE "tolerance|atol=|band=" scripts/phase18_extraction.py` returns 0
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/pytest -q tests/test_phase18_prereg.py</automated>
  </verify>
  <done>The positive control compares 112 vectors; the headline rate is a consequence of that comparison.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: run_holm_family — m=4 dose-split on core_held_out only</name>
  <files>scripts/phase18_extraction.py, tests/test_phase18_prereg.py</files>
  <read_first>
    - `scripts/phase16_persistence.py:1088-1205` — `sign_test_exact`, `compare_arms` and the widened
      `holm`, including the strict-comparison docstring paragraph
    - `scripts/phase16_persistence.py:843-930` — `cluster_bootstrap` and its two-stage structure
    - `scripts/phase18_extraction.py` — `HOLM_FAMILY`, `GATED_TIER`, `BEST_ACHIEVABLE_P` from 18-03
    - `.planning/phases/18-black-box-adversarial-extraction-audit/18-CONTEXT.md` — D-22, D-31
  </read_first>
  <behavior>
    - `run_holm_family(per_fact_by_arm)` builds exactly four paired comparisons — one per member of
      `HOLM_FAMILY` — on `GATED_TIER` only, and calls `holm(..., family=HOLM_FAMILY)`
    - a call built on `core_taught` raises `SystemExit` naming D-31: the taught tier is the positive
      control and enters no inferential family
    - at 8/8 unanimity on all four the returned step alphas begin at 0.0125 and every comparison is
      rejected — the reachable success case
    - a five-member or three-member input raises through `holm`'s own family guard
    - the returned structure carries the descriptive cluster bootstrap interval per comparison and
      labels it descriptive, with its n=8 undercoverage stated
  </behavior>
  <action>
    Add `run_holm_family(per_fact_by_arm, *, tier=GATED_TIER)` producing the four dose-split
    comparisons and calling the imported `sign_test_exact` once per comparison — a second
    `sign_test_exact` call site anywhere else in this driver is a second hypothesis family, which
    17-08 recorded and the D-21 scan catches. `_prove` that `tier == GATED_TIER`, that the number of
    comparisons equals `len(HOLM_FAMILY)`, and that `BEST_ACHIEVABLE_P < HOLM_ALPHA / len(HOLM_FAMILY)`
    still holds at call time. Attach `cluster_bootstrap` intervals as DESCRIPTIVE fields carrying
    their own label — n=8 clusters has known undercoverage and the interval never converts a miss
    into a pass. Record the m=4-over-m=6 rationale in the docstring: m=6 clears by 0.00052, the same
    razor margin Phases 16 and 17 already paid for, while m=4 preserves the dose axis in the
    inferential layer and clears by 60%.
  </action>
  <acceptance_criteria>
    - `.venv/bin/pytest -q tests/test_phase18_prereg.py -k holm_family -x` passes with the unanimity
      case, the wrong-tier raise and the wrong-arity raise
    - `.venv/bin/python -c` asserts the first step alpha is `0.05/4 == 0.0125` and that
      `sign_test_exact((1,)*8) == 0.0078125 < 0.0125` — exits 0, recorded in the SUMMARY
    - `grep -c "sign_test_exact(" scripts/phase18_extraction.py` returns exactly 2 — one in the
      import-time `BEST_ACHIEVABLE_P` computation, one inside `run_holm_family`
    - `.venv/bin/pytest -q tests/test_phase18_prereg.py::test_holm_family_is_reachable -x` still passes
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/pytest -q tests/test_phase18_prereg.py</automated>
  </verify>
  <done>Four comparisons, one tier, one sign-test call site, a gate that can actually clear.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: assemble_verdict and the erasure_gate handoff</name>
  <files>scripts/phase18_extraction.py, tests/test_phase18_prereg.py</files>
  <read_first>
    - `scripts/erasure_gate.py:120-200` — `ERASURE_DECISION_RULE`, `VERDICTS`,
      `erasure_is_worth_attempting` and its four positional question-unit ints
    - `scripts/phase18_extraction.py` — `null_result_is_admissible`, `licensed_conclusion` from 18-07,
      `family_zero_matches` and `run_holm_family` from Tasks 1-2
    - `.planning/phases/18-black-box-adversarial-extraction-audit/18-CONTEXT.md` — D-02, D-27
  </read_first>
  <behavior>
    - `assemble_verdict(...)` calls `family_zero_matches` FIRST and, when it returns False,
      short-circuits to `("INCONCLUSIVE", [CONTROL_FAILED_REASON, ...])` — the pre-committed control
      failure branch. `VERDICTS` stays the D-27 triple; there is no fourth member and no verdict
      string invented after the failure is seen
    - it then calls `null_result_is_admissible(**kwargs)` and returns its `(verdict, reasons)`
      unchanged; it never re-derives, softens or overrides that verdict
    - the `core_held_out` handoff emits `(attack_successes, attack_questions, base_successes,
      base_questions)` as four ints in the QUESTION unit, taken from the BEST attack family — a
      pre-registered post-hoc max, selected by a committed rule rather than chosen at read time
    - passing draw-unit counts is caught: a `_prove` asserts each denominator equals the tier's
      question count (104 for `core_held_out`), not its draw count
    - the INCONCLUSIVE path returns a fully populated reasons list and no numeric claim
  </behavior>
  <action>
    Add `BEST_ATTACK_RULE`, a module literal naming how the best attack family is selected (highest
    question-unit rate on `GATED_TIER`, ties broken by the pre-registered `ATTACK_FAMILIES` order),
    with the D-27 sentence recording that `ERASURE_DECISION_RULE` names "Phase 18's best attack, run
    at its pre-registered budget", so the max is pre-registered in advance rather than chosen later.
    Add `assemble_verdict(...)` orchestrating: family-zero comparison — returning
    `("INCONCLUSIVE", [CONTROL_FAILED_REASON])` on a mismatch, using the string committed in 18-03
    rather than a new label — then `null_result_is_admissible`, then `run_holm_family`, then the
    `erasure_is_worth_attempting` handoff, then `licensed_conclusion`. `_prove` the question-unit denominators before the handoff.
    The function returns a single record carrying the verdict, the reasons, the Holm rows, the
    handoff tuple and the conclusion text — it computes no new statistic of its own and retypes no
    constant. Do not touch `scripts/erasure_gate.py`.
  </action>
  <acceptance_criteria>
    - `.venv/bin/pytest -q tests/test_phase18_prereg.py -k assemble_verdict -x` passes with the
      control-failure short-circuit, the INCONCLUSIVE path and a nominal path
    - `.venv/bin/python -c` asserts every verdict `assemble_verdict` can return is a member of
      `VERDICTS`, and that the control-failure path returns `INCONCLUSIVE` carrying
      `CONTROL_FAILED_REASON` verbatim — exits 0
    - `.venv/bin/python -c` asserts the handoff tuple is four `int`s and that passing 936 as a
      held-out denominator raises `SystemExit` — exits 0
    - `git diff --exit-code scripts/erasure_gate.py` returns 0
    - `.venv/bin/pytest -q` full suite green
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/pytest -q tests/test_phase18_prereg.py</automated>
  </verify>
  <done>One orchestrator that calls the committed gates and adds no judgement of its own.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| positive control → every downstream claim | If family zero does not reproduce, nothing measured afterwards is admissible; the comparison is the load-bearing check of the whole phase |
| Phase 18 output → Phase 19's pre-registered gate | `erasure_is_worth_attempting` consumes these four ints directly, and a unit error there propagates into a phase that has not been planned yet |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-09-01 | Repudiation | An aggregate that matches while individual questions diverge is read as reproduction | mitigate | Exact hit-vector equality on all 112 rows; the sum-preserving mismatch is its own test case |
| T-18-09-02 | Tampering | A tolerance band added after seeing a near-miss | mitigate | No tolerance parameter exists; `grep` acceptance forbids one |
| T-18-09-03 | Tampering | A second `sign_test_exact` call site silently doubles the hypothesis family | mitigate | Call-site count asserted at exactly 2, one of which is the import-time reachability computation |
| T-18-09-04 | Repudiation | Draw-unit counts handed to a question-unit interface, narrowing every downstream bound | mitigate | `_prove` on the denominators before the handoff; the 936-vs-104 case is a test |
| T-18-09-05 | Tampering | The best-attack max chosen after reading the results | mitigate | `BEST_ATTACK_RULE` is a module literal inside the ancestry-pinned file with a deterministic tie-break |
| T-18-09-SC | Tampering | package installs | accept | Zero installs; `pyproject.toml` sha256-pinned |
</threat_model>

<verification>
- `.venv/bin/pytest -q` full suite green, CPU-only
- `.venv/bin/ruff check . && .venv/bin/ruff format --check .`
- `ls results/phase18_* 2>/dev/null` returns nothing
</verification>

<success_criteria>
The control comparison is exact and vector-wise, the Holm family is four comparisons on one tier,
and the Phase 19 handoff is four question-unit ints selected by a committed rule.
</success_criteria>

<output>
Create `.planning/phases/18-black-box-adversarial-extraction-audit/18-09-SUMMARY.md` when done
</output>
