---
phase: 18-black-box-adversarial-extraction-audit
plan: 12
type: execute
wave: 6
depends_on: ["18-07"]
files_modified:
  - scripts/personalize_demo.py
  - README.md
  - docs/REPORT.md
  - tests/test_phase18_docs.py
autonomous: true
requirements: [ATK-06, STAT-02]

must_haves:
  truths:
    - "D-23: the corrected 'availability, not authorization' sentence is written DIRECTLY into the Gradio label with NO dated-supersession framing — there is no prior published claim in the demo to supersede, because `MEMORY_INFO` and `STATUS_OFF` are already availability-framed and mechanically honest ('Nothing is reloaded and nothing is recomputed — 36 boolean flags flip'; 'the adapter is loaded but gated off')"
    - "D-23: `README.md` and `docs/REPORT.md` DO carry published v2.0 text and therefore DO get dated continuations, Phase 15 style — additive with 0 deletions, the prior text byte-identical"
    - "D-23: `RESET_LABEL`'s 'delete the adapter from memory' is the one authorization-flavoured string in the demo and is OUTSIDE SC5's toggle scope — noted, not changed here"
    - "ATK-06/D-24: one committed sentence is reused VERBATIM across all three surfaces, and the threats-to-validity text records that a low extraction rate may be a LoRA property rather than a PersonaCore achievement — the literature's headline finding, not a hedge"
    - "STAT-02: no bare `0%` enters any committed doc surface — the same rendered-text scan the report uses applies to the continuations"
  artifacts:
    - path: "scripts/personalize_demo.py"
      provides: "corrected toggle copy in MEMORY_INFO / STATUS_OFF"
      contains: "availability"
    - path: "README.md"
      provides: "dated v3.0 continuation carrying the claim correction"
    - path: "docs/REPORT.md"
      provides: "dated v3.0 continuation carrying the claim correction and the ATK-06 caveat"
    - path: "tests/test_phase18_docs.py"
      provides: "verbatim-sentence identity across three surfaces, additive-continuation proof"
      min_lines: 120
  key_links:
    - from: "scripts/personalize_demo.py"
      to: "README.md / docs/REPORT.md"
      via: "the demo literal is the single source of truth; the other two surfaces are asserted to contain it verbatim"
      pattern: "availability, not authorization"
---

<objective>
Land SC5: correct the toggle claim in the demo directly, and extend README and `docs/REPORT.md`
with dated continuations carrying the same sentence verbatim.

Purpose: this is a claim correction, not a measurement, so it does not wait on the run. Landing it
early de-risks the end of the phase and lets the verbatim-identity test exist before the report
that has to satisfy it.
Output: three surfaces carrying one sentence, two of them extended additively with zero deletions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/18-black-box-adversarial-extraction-audit/18-CONTEXT.md
</context>

<interfaces>
`scripts/personalize_demo.py` — verified: `MEMORY_INFO` at `:304`, `RESET_LABEL` at `:309`,
`STATUS_OFF` at `:313`, consumed at `:510-511` and `:594`. Current copy already reads
*"Unchecked gates the adapter's contribution off... Nothing is reloaded and nothing is recomputed —
36 boolean flags flip"* and *"the adapter is loaded but gated off"*.

`README.md:86, :96, :177` — the published v2.0 claim text.
`docs/REPORT.md:424, :478` — already carries the dated-continuation pattern, so a v3.0 continuation
needs no new convention. Phase 15's shipped diff was 549 insertions / 0 deletions with the first 421
lines byte-identical.

`tests/test_phase15_docs.py:511-545` — the dated-additive-continuation test shape, anchored on the
SECTION and never on `split("## Verdict")[-1]`.

From 18-07: `licensed_conclusion` and the ATK-06 caveat sentence it requires as an adjacent sentence.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: The demo's corrected toggle copy (direct, no supersession framing)</name>
  <files>scripts/personalize_demo.py</files>
  <read_first>
    - `scripts/personalize_demo.py:295-330` — `MEMORY_INFO`, `RESET_LABEL`, `STATUS_OFF` and their
      surrounding literals
    - `scripts/personalize_demo.py:500-520` and `:585-600` — the two consumption sites
    - `.planning/phases/18-black-box-adversarial-extraction-audit/18-CONTEXT.md` — D-23
  </read_first>
  <action>
    Write the corrected sentence directly into `MEMORY_INFO` and `STATUS_OFF` so the toggle reads as
    AVAILABILITY, not authorization — the honest reading of what 36 boolean writes have always done.
    No dated marker, no "superseded", no "previously stated": there is no prior published claim in
    the demo to supersede, and adding supersession framing to already-honest copy would invent a
    correction that never happened. Keep the existing mechanically-honest clauses ("Nothing is
    reloaded and nothing is recomputed"; "the adapter is loaded but gated off") — the correction adds
    the framing sentence, it does not replace the mechanism description.
    Leave `RESET_LABEL` untouched: its "delete the adapter from memory" wording is the one
    authorization-flavoured string in the demo, Reset is a different mechanism from the toggle, and
    D-23 records it as noted-not-changed.
    The sentence must be identical, character for character, to the one landing in README and
    `docs/REPORT.md` in Task 2 — define it once and verify the identity by test, not by eye.
  </action>
  <acceptance_criteria>
    - `git diff --numstat scripts/personalize_demo.py` shows changes confined to `MEMORY_INFO` and
      `STATUS_OFF`; `RESET_LABEL` is byte-unchanged
    - `grep -c "availability, not authorization" scripts/personalize_demo.py` returns at least 1
    - `grep -cE "superseded|supersedes|previously (stated|claimed)" scripts/personalize_demo.py` returns 0
    - `.venv/bin/pytest -q tests/test_phase14_demo.py` passes unchanged
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/pytest -q tests/test_phase14_demo.py tests/test_phase18_docs.py</automated>
  </verify>
  <done>The demo's toggle copy states availability, with no invented supersession.</done>
</task>

<task type="auto">
  <name>Task 2: Dated additive continuations in README and docs/REPORT.md</name>
  <files>README.md, docs/REPORT.md</files>
  <read_first>
    - `README.md:80-100` and `:170-185` — the published v2.0 claim text the continuation must not edit
    - `docs/REPORT.md:415-490` — the two existing dated continuation markers, the convention to follow
    - `tests/test_phase15_docs.py:505-545` — the additive-continuation assertions
    - `.planning/phases/18-black-box-adversarial-extraction-audit/18-CONTEXT.md` — D-23, D-24
  </read_first>
  <action>
    Append a dated v3.0 continuation to each file carrying the same verbatim sentence as the demo,
    plus the ATK-06 caveat as an adjacent sentence — a low extraction rate may be a LoRA property
    rather than a PersonaCore achievement. Phase 15 style: additive, ZERO deletions, every prior line
    byte-identical, the marker dated and placed after the existing content. Do not edit the v2.0
    claim text in place, do not renumber sections, and do not remove or reword any prior sentence —
    the correction is a continuation, not a rewrite.
    The continuation states the claim correction only. It quotes no measured Phase 18 number, because
    no Phase 18 number exists yet; the results paragraph is appended later by 18-16 through the same
    additive path.
  </action>
  <acceptance_criteria>
    - `git diff --numstat README.md docs/REPORT.md` shows 0 deletions on both files
    - `git show HEAD~1:README.md > /tmp/prev_readme && head -c $(wc -c < /tmp/prev_readme) README.md | diff - /tmp/prev_readme` returns empty — the prior content is a byte-identical prefix
    - `grep -c "availability, not authorization" README.md docs/REPORT.md` returns at least 1 each
    - `grep -cE "LoRA property|property of LoRA" docs/REPORT.md` returns at least 1
    - `grep -nE "\b0(\.0+)?%" README.md docs/REPORT.md` returns no NEW match introduced by this diff
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/pytest -q tests/test_phase18_docs.py</automated>
  </verify>
  <done>Two published surfaces extended with 0 deletions, carrying the same sentence as the demo.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The verbatim-identity and additivity tests</name>
  <files>tests/test_phase18_docs.py</files>
  <read_first>
    - `tests/test_phase15_docs.py:505-545` — the anchored-section read and the additive assertions
    - `tests/test_phase17_stats.py:858-876` — `test_no_bare_zero_percent`'s source-plus-rendered shape
    - `.planning/phases/18-black-box-adversarial-extraction-audit/18-VALIDATION.md` — the node ids
      this file must expose
  </read_first>
  <behavior>
    - the corrected sentence appears character-for-character identical in all three surfaces —
      `scripts/personalize_demo.py`, `README.md`, `docs/REPORT.md`
    - `test_docs_continuation_is_additive` asserts every `## ` heading that existed in README and
      `docs/REPORT.md` before the continuation is still present and still in its original order — an
      append can never have displaced one
    - the heading scan is meta-guarded: the test asserts headings were FOUND before asserting
      anything about their order, so a broken scan is red rather than vacuously green
    - `test_no_bare_zero_percent_in_docs` scans both committed docs and finds no bare zero percentage
  </behavior>
  <action>
    Add the three tests above using the node ids named in 18-VALIDATION.md
    (`test_docs_continuation_is_additive`, `test_no_bare_zero_percent_in_docs`) plus a
    `test_claim_sentence_is_verbatim_in_three_surfaces`. Read the sentence from ONE source of truth —
    a module constant or the demo literal — and assert containment in the other two; never retype it
    in the test, because a retyped sentence is a fourth copy that can drift. Anchor every section read
    on the heading regex with a meta-guard, never on `split(...)[-1]`. Keep the file CPU-only with no
    checkpoint load; it must not import the Gradio app object, only the module's literals.
  </action>
  <acceptance_criteria>
    - `.venv/bin/pytest -q tests/test_phase18_docs.py::test_claim_sentence_is_verbatim_in_three_surfaces -x` passes
    - `.venv/bin/pytest -q tests/test_phase18_docs.py::test_docs_continuation_is_additive -x` passes
    - `.venv/bin/pytest -q tests/test_phase18_docs.py::test_no_bare_zero_percent_in_docs -x` passes
    - `.venv/bin/pytest -q tests/test_phase18_docs.py --collect-only | grep -c 'test_'` shows every
      node id in the module distinct — no later `def` shadows an earlier one, which would delete a
      guard silently
    - RED proof recorded in the SUMMARY: change one character of the sentence in `README.md`, observe
      the verbatim test fail, then restore (`git diff --exit-code README.md` returns 0)
    - `grep -c "split(\"## \|split('## " tests/test_phase18_docs.py` returns 0
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/pytest -q tests/test_phase18_docs.py</automated>
  </verify>
  <done>One sentence, three surfaces, proved identical and proved additive.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| published v2.0 claim → v3.0 correction | An in-place edit of shipped text erases the record that the claim was ever different |
| one sentence → three surfaces | Three hand-maintained copies are three chances for the published claim to disagree with itself |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-12-01 | Tampering | Shipped v2.0 text edited in place, destroying the audit trail | mitigate | Dated additive continuation, 0 deletions asserted by `git diff --numstat` and a byte-identical prefix check |
| T-18-12-02 | Repudiation | The three surfaces drift and the published claim disagrees with itself | mitigate | One source of truth for the sentence; verbatim identity asserted; watched RED |
| T-18-12-03 | Repudiation | Supersession framing invented for demo copy that was already honest | mitigate | `grep` acceptance forbids supersession language in the demo; D-23's verified premise recorded |
| T-18-12-04 | Repudiation | ATK-06's LoRA caveat omitted, letting a low rate read as an achievement | mitigate | Caveat required in `docs/REPORT.md` by `grep` acceptance and required adjacent in `licensed_conclusion` |
| T-18-12-SC | Tampering | package installs | accept | Zero installs; `pyproject.toml` sha256-pinned |
</threat_model>

<verification>
- `.venv/bin/pytest -q` full suite green
- `.venv/bin/ruff check . && .venv/bin/ruff format --check .`
- `git diff --numstat README.md docs/REPORT.md` shows 0 in the deletions column
</verification>

<success_criteria>
The toggle reads as availability on all three surfaces, the two published documents were extended
rather than edited, and the identity is enforced by a test that has been watched RED.
</success_criteria>

<output>
Create `.planning/phases/18-black-box-adversarial-extraction-audit/18-12-SUMMARY.md` when done
</output>
