---
phase: 18-black-box-adversarial-extraction-audit
plan: 11
type: execute
wave: 9
depends_on: ["18-10"]
files_modified:
  - scripts/phase18_extraction.py
  - tests/test_phase18_docs.py
autonomous: true
requirements: [STAT-01, STAT-02, STAT-06, ATK-04, ATK-06]

must_haves:
  truths:
    - "D-11: the report renderer NEVER imports the fact set — every cell is rendered from a pre-computed entry dict carrying `slot`, so no fact value enters the render path"
    - "D-18: the realized-injection distribution is PUBLISHED PER SLOT in the run's report — the measured outcome confirming the two 2-id slots injected 2 and the six 1-id slots injected 1 on the final token-merged prompt — turning the budget from a declared constant into a verified fact about what ran"
    - "D-24: the conclusion sentence is emitted by the committed `licensed_conclusion()` reading the threat-model literals and interpolating the measured rate, denominator and Wilson bound; scope cannot widen between driver and prose because the prose is generated from the same literals the run obeyed"
    - "D-30: both NLL reductions are published as required columns alongside each slot's token-length spread, and both confounds go in threats-to-validity uncorrected — sum injects length into the rank on 6 of 8 slots, mean can favour long memorized strings"
    - "D-29: all three frames are published as columns; only `ans1` is read by the gate, and the held-out F3 bare frame is labelled as published-but-excluded"
    - "STAT-02/Pitfall 8: every proportion renders through `report_proportion` with its denominator, its Wilson bound labelled as the independence-assuming width, and `3/n` at zero successes — and every zero carries BOTH the question-level and the fact-level (n=8) denominator, emitted together or not at all"
    - "STAT-02: no bare `0%` appears in any committed report OR figure label — proved by a scan of the RENDERED text, which is the half a source scan structurally cannot do"
    - "S-6: the report is written once and thereafter EXTENDED, never re-rendered — `render_report` rewrites the whole file and would destroy a recorded verdict"
  artifacts:
    - path: "scripts/phase18_extraction.py"
      provides: "render_report, assert_report_not_clobbered, append_addendum, prereg_commit"
      contains: "def render_report"
    - path: "tests/test_phase18_docs.py"
      provides: "no-bare-zero on rendered text with an all-zero fixture, additive-append proof"
      min_lines: 200
  key_links:
    - from: "scripts/phase18_extraction.py::render_report"
      to: "licensed_conclusion"
      via: "conclusion generated, never retyped beside the rows"
      pattern: "licensed_conclusion\\("
    - from: "scripts/phase18_extraction.py::assert_report_not_clobbered"
      to: "scripts/_verdict.py"
      via: "anchored recorded_verdict read, never split('## Verdict')[-1]"
      pattern: "_verdict"
---

<objective>
Write the report renderer — the last driver commit — including its clobber guard, its
append-only continuation path, and the two `_prove`s that run over the rendered text.

Purpose: Phase 16's register is that the report's text is committed before it produces a number,
because a report whose prose is written after the numbers is a report written to fit them. After
this plan the pin is complete and the ancestry guard arms.
Output: `render_report`, its guards, and the rendered-text tests. No artifact is written.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/18-black-box-adversarial-extraction-audit/18-CONTEXT.md
@.planning/phases/18-black-box-adversarial-extraction-audit/18-PATTERNS.md
</context>

<interfaces>
`scripts/phase17_isolation.py:1536-1548` — `_matrix_cell_text(entry)` takes a PRE-COMPUTED entry dict
and never touches the fact set; every cell renders through `report_proportion`'s `formatted` so no
cell can print a bare percentage.
`:1573-1609` — `render_report`'s docstring: the verdict is COMPUTED by importing the gate, never
retyped as prose beside the rows; two `_prove`s run over the RENDERED TEXT before it is written —
the report must carry a `## Verdict` section the clobber guard can anchor on, and it must contain no
bare zero percentage anywhere.
`:1159-1181` — `assert_isolation_report_not_clobbered`, anchored on the SECTION via
`_verdict.recorded_verdict`, never on a split of the heading literal; there is no force flag.
`:1857-1880` — `prereg_commit()`: `git log --diff-filter=A --format=%h`, `adds[-1]` (the earliest
add), `_prove`d non-empty.
`:2369-2418` — `append_addendum`: placeholder occurs exactly once, prefix carried through
byte-identically, recorded verdict proved unchanged ON THE PRODUCED BYTES.

`tests/test_phase17_stats.py:858-876` — `test_no_bare_zero_percent`: a source scan for
`\b0(\.0+)?%` across every gate module PLUS a rendered scan driven by an ALL-ZERO fixture — this
phase's hoped-for outcome and the one that would most tempt a bare zero.

From 18-06/18-07/18-08/18-09: `measure_exposure`, `EXPOSURE_THREATS_TO_VALIDITY`,
`licensed_conclusion`, `asr_ladder`, `cumulative_by_attempt`, `unique_successes`, `assemble_verdict`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: render_report — every published column, no fact value on the render path</name>
  <files>scripts/phase18_extraction.py</files>
  <read_first>
    - `scripts/phase17_isolation.py:1520-1620` — `_matrix_cell_text` and `render_report` in full
    - `scripts/phase16_persistence.py:930-980` — `report_proportion`, `WILSON_LABEL` and the
      zero-success `rule_of_three_upper` attachment
    - `scripts/phase18_extraction.py` — the scoring, exposure and verdict outputs from 18-06..18-09
    - `.planning/phases/18-black-box-adversarial-extraction-audit/18-CONTEXT.md` — D-11, D-18, D-24,
      D-29, D-30, and the Claude's Discretion note on report layout
  </read_first>
  <action>
    Add `EXTRACTION_REPORT_PATH = _REPO_ROOT / "results" / "phase18_extraction_report.md"` and
    `render_report(...)` taking ONLY pre-computed entry dicts — layout and figure choices are
    discretion, the following columns are not. Required sections: the ASR ladder per family per arm
    per tier with rung 1 labelled greedy; the cumulative-by-attempt curve per family and per arm;
    the tier split with `core_taught` published as the stronger attack surface and explicitly NOT
    merged into the formal verdict; the D-18 realized-injection distribution per slot beside the
    declared budget vector; the exposure table carrying, per slot, the rank, exposure, ceiling, |R|,
    the token-length spread and all three frames x both reductions, with `ans1`/mean marked as the
    admissible pair and the F3 bare frame marked published-but-excluded; the four Holm rows with
    their step alphas; the unique-success counts at the 9-draw prefix and at k=64, each labelled;
    a `## Threats to Validity` section carrying `EXPOSURE_THREATS_TO_VALIDITY`, the ATK-06 LoRA-property
    caveat, A2's prefill-shape weakness (its shape corresponds to F4's reply form, which is not in the
    fixture) and the 1-2 id prefix against the literature's 50-token convention; a `## Verdict`
    section holding the verdict and NOTHING else; and the closing paragraph from `licensed_conclusion`.
    Route EVERY proportion through `persistence.report_proportion`. Run two `_prove`s over the
    RENDERED TEXT before writing: a `## Verdict` section the clobber guard can anchor on exists, and
    `re.search(r"\b0(\.0+)?%", text)` is None. Import no fact-set module anywhere in the render path.
    Add `prereg_commit()` twinning Phase 17's, so the report cites a RESOLVED commit for the
    pre-registration rather than asserting one.
  </action>
  <acceptance_criteria>
    - `.venv/bin/python -c` AST check: no function reachable from `render_report` imports
      `phase14_factset` or `phase17_persona_facts` — exits 0, recorded in the SUMMARY
    - `.venv/bin/pytest -q tests/test_phase18_docs.py::test_no_bare_zero_percent -x` passes, driven
      by an ALL-ZERO synthetic fixture
    - `.venv/bin/python -c` renders the report from an all-zero fixture into a tmp path and asserts
      the text contains a `3/104` rule-of-three value, a Wilson label, both the question-level and
      fact-level denominators, and the D-18 per-slot distribution — exits 0
    - `grep -c "def render_report" scripts/phase18_extraction.py` returns 1
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/pytest -q tests/test_phase18_docs.py</automated>
  </verify>
  <done>Every required column is rendered from pre-computed entries; a bare zero cannot survive the renderer.</done>
</task>

<task type="auto">
  <name>Task 2: The clobber guard and the append-only continuation</name>
  <files>scripts/phase18_extraction.py, tests/test_phase18_docs.py</files>
  <read_first>
    - `scripts/phase17_isolation.py:1159-1181` and `:2369-2418` — the clobber guard and
      `append_addendum`, including its three `_prove`s on the produced bytes
    - `scripts/_verdict.py` — `recorded_verdict` and `VERDICT_SECTION`, the anchored read
    - `tests/test_phase15_docs.py:511-545` — the CR-02 regression tripwire recording why
      `split("## Verdict")[-1]` is forbidden
    - `.planning/phases/18-black-box-adversarial-extraction-audit/18-CONTEXT.md` — S-6 in the
      Established Patterns block
  </read_first>
  <action>
    Add `assert_extraction_report_not_clobbered()` anchored on the SECTION via
    `_verdict.recorded_verdict` — never a split of the heading literal — returning early when the
    file does not exist, and raising `SystemExit` once a non-PENDING verdict is recorded. There is no
    force flag: if the report genuinely must be regenerated, the honest path is deleting it in a
    reviewed commit so the removal is visible in the diff.
    Add `append_addendum(path, addendum)` twinning Phase 17's: the placeholder occurs exactly once
    (`_prove(found == 1, ...)`), the prefix is carried through byte-identically, and the recorded
    verdict is proved unchanged ON THE PRODUCED BYTES. This is the only path by which
    `results/phase18_extraction_report.md`, `README.md` or `docs/REPORT.md` may be extended after a
    verdict exists.
    Add `test_continuation_is_additive` in `tests/test_phase18_docs.py` proving on a synthetic file
    that the append preserves the prefix byte-for-byte, leaves the recorded verdict unchanged, and
    raises when the placeholder is absent or duplicated.
  </action>
  <acceptance_criteria>
    - `.venv/bin/pytest -q tests/test_phase18_docs.py::test_continuation_is_additive -x` passes
    - `grep -c "split(\"## Verdict\")\|split('## Verdict')" scripts/phase18_extraction.py` returns 0
    - `grep -c "force" scripts/phase18_extraction.py` returns 0
    - RED proof recorded in the SUMMARY: write a synthetic report carrying a recorded GO verdict,
      call the render path, observe `SystemExit`; then confirm a PENDING report renders normally
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/pytest -q tests/test_phase18_docs.py</automated>
  </verify>
  <done>A recorded verdict cannot be overwritten, and continuations are provably additive.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| computed numbers → published prose | A hand-written headline is a headline free to disagree with the instrument that produced it |
| recorded verdict → later edits | A re-render destroys the hand-recorded verdict and every dated addendum above it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-11-01 | Repudiation | A zero rendered without its denominator and ceiling states a certainty the sample does not have | mitigate | Every proportion through `report_proportion`; a `_prove` over the RENDERED text; the all-zero fixture is the test case |
| T-18-11-02 | Repudiation | Only the flattering question-level denominator is published | mitigate | Both ends of the clustering assumption emitted in the same row, both or neither |
| T-18-11-03 | Information Disclosure | A fact value reaches the render path via a fact-set import | mitigate | Entries carry `slot`; AST check that no render-path function imports the fact set |
| T-18-11-04 | Tampering | A re-render destroys a recorded verdict and the dated addenda | mitigate | Clobber guard anchored via `_verdict.recorded_verdict`, no force flag; `append_addendum` is the only post-verdict path |
| T-18-11-05 | Repudiation | The exposure numbers published without their length confound | mitigate | `EXPOSURE_THREATS_TO_VALIDITY` rendered into a required section; the spread is a required column |
| T-18-11-SC | Tampering | package installs | accept | Zero installs; `pyproject.toml` sha256-pinned |
</threat_model>

<verification>
- `.venv/bin/pytest -q` full suite green
- `.venv/bin/ruff check . && .venv/bin/ruff format --check .`
- `ls results/phase18_* 2>/dev/null` returns nothing — the driver is now COMPLETE and no artifact exists
- `git log --oneline -- scripts/phase18_extraction.py` recorded in the SUMMARY as the ancestor set
</verification>

<success_criteria>
The pinned driver is complete: templates, budget, ladder, instruments, admissibility, verdict and
report text are all committed before a single number exists.
</success_criteria>

<output>
Create `.planning/phases/18-black-box-adversarial-extraction-audit/18-11-SUMMARY.md` when done
</output>
