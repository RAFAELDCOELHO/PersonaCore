---
phase: 18-black-box-adversarial-extraction-audit
plan: 14
type: execute
wave: 11
depends_on: ["18-13"]
files_modified:
  - results/phase18_corpus.json
  - tests/test_phase18_corpus.py
autonomous: true
requirements: [ATK-01, STAT-05]

must_haves:
  truths:
    - "D-07: the committed `results/phase18_corpus.json` is the INPUT the run consumes, and a committed test RE-DERIVES it from the pinned generator and asserts BYTE-EQUALITY — a standing guard, NOT a precondition of dispatch, following 17-10's precedent of CALLING the pre-registered rule from the committed mode rather than re-deriving it at run time"
    - "D-07: the forced commit order holds — smoke, then pin, then the corpus artifact, then the run, then results; the corpus first-add descends from every driver commit"
    - "D-11: every committed entry carries `family`, `dose`, `fact_id`, `slot`, source `seed_index`, `prompt_ids` and the realized injection, so the report renderer never needs the fact set"
    - "D-16: the build-time guards ran on the real material — the strict no-value guard on every family's question portion and the bounded tail assertion on A2 — so the committed corpus is proved clean rather than assumed clean"
    - "D-03: the corpus is checked against the BYTES the model receives, via the widened `assert_no_value_in_prompt(prompt_ids=...)` path, not against a reconstruction"
  artifacts:
    - path: "results/phase18_corpus.json"
      provides: "864 attack prompts with full provenance and recorded prompt_ids"
    - path: "tests/test_phase18_corpus.py"
      provides: "test_corpus_rederives_byte_identical against the committed artifact"
  key_links:
    - from: "tests/test_phase18_corpus.py"
      to: "results/phase18_corpus.json"
      via: "re-derivation from the pinned builder, byte-equality"
      pattern: "test_corpus_rederives_byte_identical"
---

<objective>
Generate and commit the corpus artifact, then activate the standing byte-equality guard.

Purpose: the corpus is the INPUT. Committing it turns "both arms saw the same prompt" into a fact
about a tracked file with a recorded sha256, and the re-derivation test is what keeps that file
tied to the pinned generator that produced it.
Output: `results/phase18_corpus.json` plus the activated guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/18-black-box-adversarial-extraction-audit/18-CONTEXT.md
</context>

<interfaces>
`scripts/phase18_extraction.py --corpus` — added by 18-10, calling `build_corpus` (18-05) and writing
`CORPUS_PATH`. `corpus_sha256(corpus)` produces the digest the run records in its provenance.

`tests/test_phase16_fixture_regen.py:93-113` — `test_fixture_matches_the_generator`, the
field-by-field re-derivation shape. Phase 18's delta is that the assertion is on `prompt_ids`
byte-equality, and per D-07 it is a standing guard living in the TEST, never in the driver's run path.

From 18-05, already committed and green: `test_schema_and_reserved_family`,
`test_no_network_imports`, `test_corpus_builder_is_deterministic`,
`test_strict_guard_covers_every_family`, `test_a2_injection_within_budget`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Generate and commit results/phase18_corpus.json</name>
  <files>results/phase18_corpus.json</files>
  <read_first>
    - `scripts/phase18_extraction.py` — `build_corpus`, `CORPUS_PATH`, `corpus_sha256`, and the
      `--corpus` mode
    - `results/phase16_recall_sample.json` — the binding fixture the builder reads
    - `.planning/phases/18-black-box-adversarial-extraction-audit/18-CONTEXT.md` — D-07, D-11, D-16
  </read_first>
  <action>
    Run `.venv/bin/python scripts/phase18_extraction.py --corpus`. The build-time guards run on the
    real material here for the first time: if the strict no-value guard or the bounded tail assertion
    aborts, STOP and report — do not relax a guard to get an artifact.
    Commit the produced file. Record in the SUMMARY: the entry count (expected 864), the per-family
    counts (216 each), the tier split (448 taught-derived / 416 held-out-derived), the realized
    injection distribution per slot, and the `corpus_sha256` digest — that digest is what every arm
    record will carry, so it is the join key for the whole run.
    Do not hand-edit the artifact. Do not regenerate it after the arms run.
  </action>
  <acceptance_criteria>
    - `.venv/bin/python -c "import json; c=json.load(open('results/phase18_corpus.json')); assert len(c['prompts'])==864"` exits 0
    - The recorded `corpus_sha256` is written verbatim into the SUMMARY
    - `.venv/bin/pytest -q tests/test_phase18_corpus.py` passes in full
    - `.venv/bin/pytest -q tests/test_phase16_prereg.py -k phase18 -x` passes — the corpus first-add
      descends from every driver commit
    - `git diff --exit-code scripts/phase18_extraction.py` returns 0 — generating the artifact
      required no driver change
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/pytest -q tests/test_phase18_corpus.py tests/test_phase16_prereg.py</automated>
  </verify>
  <done>864 guarded prompts committed with a recorded digest.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Activate the byte-equality re-derivation guard</name>
  <files>tests/test_phase18_corpus.py</files>
  <read_first>
    - `tests/test_phase16_fixture_regen.py:52-113` — `_load`, `_regenerate` and the field-by-field
      comparison
    - `results/phase18_corpus.json` — the committed artifact
    - `.planning/phases/18-black-box-adversarial-extraction-audit/18-CONTEXT.md` — D-07
  </read_first>
  <behavior>
    - `test_corpus_rederives_byte_identical` rebuilds the corpus from the pinned `build_corpus` and
      asserts the canonical serialization is byte-identical to the committed file
    - the comparison is field-by-field before it is byte-wise, so a failure names WHICH field drifted
      rather than reporting a hash mismatch
    - `prompt_ids` are compared element-for-element, not by length or by decoded string
    - the test reads the committed artifact from disk and fails loudly if it is absent — no skip, no
      conditional pass
  </behavior>
  <action>
    Add `test_corpus_rederives_byte_identical` following `test_fixture_matches_the_generator`'s shape:
    re-derive through the pinned builder, compare `family`, `dose`, `fact_id`, `slot`, `tier`,
    `seed_index`, `realized_injection` and `prompt_ids` field-by-field with per-field failure
    messages, then assert the canonical serializations match byte-for-byte. Assert the artifact
    exists rather than skipping when it does not — a guard that skips is a guard that is green for
    the wrong reason. Keep this in the TEST, never in the driver's dispatch path: D-07 makes it a
    standing guard, not a precondition of dispatch, exactly as 17-10 CALLED the pre-registered
    `worst_pair` from the committed mode rather than re-deriving it.
  </action>
  <acceptance_criteria>
    - `.venv/bin/pytest -q tests/test_phase18_corpus.py::test_corpus_rederives_byte_identical -x` passes
    - RED proof recorded in the SUMMARY: flip one id in a copy of the artifact under a tmp path, point
      the test at it, observe a failure naming `prompt_ids` and the entry index; the committed file is
      untouched (`git diff --exit-code results/phase18_corpus.json` returns 0)
    - `grep -c "pytest.skip\|skipif" tests/test_phase18_corpus.py` returns 0
    - `grep -c "rederives_byte_identical" scripts/phase18_extraction.py` returns 0 — the guard is not
      in the dispatch path
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/pytest -q tests/test_phase18_corpus.py</automated>
  </verify>
  <done>The committed corpus is tied to the pinned generator by a guard that has been watched RED.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pinned generator → committed artifact | A corpus that no longer re-derives is a corpus nobody can attribute to a committed rule |
| committed artifact → both arm processes | The recorded `prompt_ids` and their sha256 are the join key that makes the two arms one comparison |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-14-01 | Tampering | The artifact is hand-edited or regenerated between the arms | mitigate | Byte-equality re-derivation guard; the run records the corpus sha256 per arm; regeneration after the run is forbidden by the action |
| T-18-14-02 | Information Disclosure | A build-time guard relaxed to get an artifact produced | mitigate | The action requires a STOP on any abort; the guards are the same ones proved RED in 18-04/18-05 |
| T-18-14-03 | Repudiation | The guard skips when the artifact is missing and reports green | mitigate | No skip permitted; `grep` acceptance forbids `pytest.skip` in the module |
| T-18-14-04 | Tampering | The corpus first-add predates a driver commit, breaking the pre-registration order | mitigate | `test_phase18_prereg_is_frozen_before_every_phase18_result` run as an acceptance criterion |
| T-18-14-SC | Tampering | package installs | accept | Zero installs; `pyproject.toml` sha256-pinned |
</threat_model>

<verification>
- `.venv/bin/pytest -q` full suite green
- `.venv/bin/pytest -q tests/test_phase16_prereg.py -k phase18` green with a `checked` value covering
  both `results/phase18_*` artifacts now tracked
</verification>

<success_criteria>
The corpus is committed, guarded, digest-recorded and provably re-derivable from the pinned builder.
</success_criteria>

<output>
Create `.planning/phases/18-black-box-adversarial-extraction-audit/18-14-SUMMARY.md` when done
</output>
