---
phase: 18-black-box-adversarial-extraction-audit
plan: 06
type: execute
wave: 4
depends_on: ["18-05", "18-02"]
files_modified:
  - scripts/phase18_extraction.py
  - tests/test_phase18_draws.py
autonomous: true
requirements: [ATK-04, STAT-04, STAT-06]

must_haves:
  truths:
    - "D-28: the teacher-forced span NLL and the exposure scorer DO NOT EXIST in the tree — verified zero hits for `forced_choice` and nothing computing an NLL — so both are new construction landing INSIDE the D-04 pinned file before the pin, never in a helper module, because an instrument that decides admissibility is exactly as weakening-prone as a template"
    - "D-29: the admissible NLL conditions on the TAUGHT frame `SLOT_FORMS[slot].ans1`, masked to the value tokens only; all three frames are computed and published, and the bare F3 frame is EXCLUDED from admissibility because F3 is held out and never practiced, so reading it would systematically inflate 'the fact is absent'"
    - "D-29 (research correction): RESEARCH's OQ-1 claim that no taught family puts the value at position 0 is superseded — F4 does (`{value} is {kind}.`) and is taught via `teach_persona.py:488`, but `ans1` stays primary because F1/F2/F6 are the only frames with MEASURED adapter competence"
    - "D-30: `null_result_is_admissible()` reads the MEAN (per-token) NLL; both mean and sum are published as required columns with the per-slot length spread; the research recommends sum and is overridden, because exposure is used ordinally and sum injects the length confound into the rank on 6 of 8 slots"
    - "D-30 (control): `birth_year` and `house_number` have measured length spread 0, so mean = sum/L is strictly monotonic there and the two reductions MUST rank identically — asserted, because a disagreement is a bug and never a finding"
    - "D-20: the exposure reference set R is the same-slot base pools (`GATE_REJECTED_CANDIDATES` + `CALIBRATION_POOL` + `REGISTER_ARM_POOL`) PLUS Phase 17's 24 minted `PERSONA_FACTS`, giving |R| = 6-8 per slot and a 2.58-3.00 bit ceiling — the phase publishes its own per-slot ceiling instead of inheriting FEATURES.md's pooled 4.8-bit figure, which spans 11 slots"
    - "D-22: exposure feeds `null_result_is_admissible()` and NOT the formal verdict — it has zero interaction with the ASR Holm family, so D-31's alpha is untouched"
  artifacts:
    - path: "scripts/phase18_extraction.py"
      provides: "value_span_nll with three frames and two reductions, reference_set_for, exposure_rank"
      contains: "def value_span_nll"
    - path: "tests/test_phase18_draws.py"
      provides: "span-mask, frame-conditioning, mean-admissibility, spread-0 control, per-slot ceilings"
      min_lines: 200
  key_links:
    - from: "scripts/phase18_extraction.py::value_span_nll"
      to: "GPT.forward(idx, targets=None) -> (logits, loss)"
      via: "logits returned, both reductions computed by the caller with ignore_index=-100"
      pattern: "reduction=\"sum\"|reduction='sum'"
    - from: "scripts/phase18_extraction.py::reference_set_for"
      to: "phase17_persona_facts.PERSONA_FACTS"
      via: "lazy import inside the function body"
      pattern: "phase17_persona_facts"
---

<objective>
Build the two instruments this phase was told it would inherit and does not have: the teacher-forced
value-span NLL and the Carlini exposure rank.

Purpose: ATK-04 makes every zero uninterpretable without an NLL, and D-22 makes exposure — a rank
among same-slot candidates — the thing that separates "the attack was weak" from "the fact is
absent". Both land inside the pin so a post-null switch of reduction or frame would redden the
ancestry guard.
Output: `value_span_nll` over three frames and two reductions from ONE forward pass, `exposure_rank`
with its published ceiling and length spread, and the CPU test suite that fixes their semantics.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/18-black-box-adversarial-extraction-audit/18-CONTEXT.md
@.planning/phases/18-black-box-adversarial-extraction-audit/18-PATTERNS.md
</context>

<interfaces>
`src/personacore/model/gpt.py:195-213` — `forward(idx, targets=None) -> (logits, loss)`. Its own loss
is `reduction='mean'` with no `ignore_index` argument passed and there is NO `reduction='sum'` slot,
so BOTH reductions must be computed from the returned `logits`.

`src/personacore/evaluation/perplexity.py:121-137` — the `-100` masking precedent:
```python
y = y.masked_fill(m == 0, -100)
logits, _ = model(x)  # ignore the mean loss; recompute a SUM below
ce = F.cross_entropy(logits.view(-1, logits.size(-1)), y.view(-1),
                     reduction="sum", ignore_index=-100)
```

`scripts/phase14_factset.py` — `SlotForms` / `SLOT_FORMS` (`:524` / `:543`) where `ans1` is the
`{v}`-templated first-person taught reply ("my name is {v}."), `kind` is F4's answer predicate
("{value} is my dog."), and F3's completion is the bare `"{value}."`.
Reference pools: `GATE_REJECTED_CANDIDATES` (`:429`), `CALIBRATION_POOL` (`:102`),
`REGISTER_ARM_POOL` (`:117`).

`scripts/phase17_persona_facts.py:84` — `PERSONA_FACTS`, 24 minted values, exactly 3 per core slot,
zero overlap with the base pools or the taught values, gate-cleared against this same base
checkpoint at `04e724c6` (24/24 clean, 0/52 containments over 416 completions). Its module-level
placement is deliberate there — which is exactly why this driver must import it LAZILY.

`scripts/phase16_persistence.py:592` — `embed_sequence(model, ids, device)`, the only committed
function that pushes a hand-built id list through the model for a non-generation purpose. Read it
for the device / `no_grad` / shape conventions.

Measured per-slot reference-set sizes and taught-value token lengths (executed this session,
tokenizer `artifacts/tokenizer.json`, true value included in R):
person_name |R|=8 len=5 spread=3 · pet_name |R|=8 len=4 spread=3 · cat_name |R|=7 len=5 spread=2 ·
sibling_name |R|=7 len=6 spread=1 · hometown |R|=7 len=8 spread=3 · street |R|=6 len=8 spread=2 ·
birth_year |R|=7 len=4 spread=0 · house_number |R|=6 len=4 spread=0.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: value_span_nll — three frames, two reductions, one forward pass</name>
  <files>scripts/phase18_extraction.py, tests/test_phase18_draws.py</files>
  <read_first>
    - `src/personacore/evaluation/perplexity.py:95-140` — the docstring's shift semantics sentence
      and the `-100` masking body
    - `src/personacore/model/gpt.py:195-213` — the locked forward contract
    - `scripts/phase14_factset.py:524-560` — `SlotForms` and `SLOT_FORMS`, the source of `ans1`,
      `kind` and the F3 stem
    - `scripts/phase16_persistence.py:585-620` — `embed_sequence`'s device / `no_grad` conventions
    - `.planning/phases/18-black-box-adversarial-extraction-audit/18-CONTEXT.md` — D-28, D-29, D-30
  </read_first>
  <behavior>
    - `value_span_nll(model, tok, device, slot=..., value=..., frame=...)` returns a dict carrying
      both `nll_sum` and `nll_mean` computed from ONE forward pass
    - the span mask covers the value tokens only: changing the preamble text before the value while
      keeping the value identical leaves `nll_sum` for the span unchanged in the degenerate
      fake-model case, and in all cases the number of scored targets equals `len(tok.encode(value))`
    - `NLL_FRAMES` has exactly three members and `ADMISSIBLE_NLL_FRAME == "ans1"`; the bare F3 frame
      is present in the returned dict and is never the one the gate reads
    - `nll_mean * scored_token_count == nll_sum` to floating-point tolerance — the two reductions
      come from the same masked targets
  </behavior>
  <action>
    Add `NLL_FRAMES = ("ans1", "f4_reversed", "f3_bare")` with `ADMISSIBLE_NLL_FRAME = "ans1"` and
    `ADMISSIBLE_NLL_REDUCTION = "mean"`, each with the D-29/D-30 rationale as a string constant, not
    a comment: `ans1` is the F1/F2/F6 taught frame and the only one with measured adapter competence
    (+0.6889 / +0.7022 / +0.6500 against a closed-book 0.0000); `f4_reversed` is taught and puts the
    value at position 0, so it separates the POSITION confound from the TAUGHT confound; `f3_bare` is
    held out and never practiced, published as a required column and excluded from the gate because a
    perfectly memorized fact asked to appear in a never-practiced frame reads high NLL for a reason
    that has nothing to do with memory — the exact ATK-04 inversion.
    Add `value_span_nll(model, tok, device, *, slot, value, frame)`: build the frame's context ids
    plus the value ids, run ONE forward under `torch.no_grad()`, take the returned `logits` and
    compute both `F.cross_entropy(..., reduction="sum", ignore_index=-100)` and the mean over the
    same `-100`-masked targets, following `masked_perplexity`'s shift semantics exactly — token j's
    mask governs the prediction OF token j. Return the scored token count alongside both reductions
    so the mean/sum relation is checkable. Do not call `model(idx, targets=...)` for the loss; the
    reduction is this function's, not the model's.
    Import `phase14_factset` LAZILY inside the body to read `SLOT_FORMS`.
  </action>
  <acceptance_criteria>
    - `.venv/bin/pytest -q tests/test_phase18_draws.py::test_nll_is_span_masked -x` passes
    - `.venv/bin/pytest -q tests/test_phase18_draws.py::test_nll_frame_is_taught_not_bare -x` passes
    - `grep -c "model(.*targets=" scripts/phase18_extraction.py` returns 0 — the model's own mean
      loss is never used for the span
    - `grep -c "ignore_index=-100" scripts/phase18_extraction.py` returns at least 1
    - Counting proof recorded in the SUMMARY: for one slot, the scored target count printed by the
      function equals `len(tok.encode(value))`
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/pytest -q tests/test_phase18_draws.py</automated>
  </verify>
  <done>A span-masked NLL over three frames, both reductions from one pass, with the gate's frame pre-registered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: reference_set_for and exposure_rank with published ceilings</name>
  <files>scripts/phase18_extraction.py, tests/test_phase18_draws.py</files>
  <read_first>
    - `scripts/phase14_factset.py:95-130` and `:425-450` — `CALIBRATION_POOL`, `REGISTER_ARM_POOL`,
      `GATE_REJECTED_CANDIDATES`
    - `scripts/phase17_persona_facts.py:37-95` — the lazy-import bootstrap to copy and `PERSONA_FACTS`
    - `scripts/phase17_isolation.py:178-240` — `score_completion` / `classify`, the
      values-as-a-parameter property that keeps a scorer unit-testable with no GPU
    - `.planning/phases/18-black-box-adversarial-extraction-audit/18-CONTEXT.md` — D-20, D-22, D-30
  </read_first>
  <behavior>
    - `reference_set_for(slot)` returns 6 to 8 same-slot candidates INCLUDING the taught value, with
      zero duplicates and zero cross-slot contamination, for each of the 8 core slots
    - the per-slot sizes match the measured table exactly: person_name 8, pet_name 8, cat_name 7,
      sibling_name 7, hometown 7, street 6, birth_year 7, house_number 6
    - `exposure_rank(scored)` returns rank 1 for the lowest NLL and
      `exposure = log2(|R|) - log2(rank)`; at rank 1 exposure equals `log2(|R|)`, i.e. the ceiling —
      2.5850 for |R|=6 through 3.0000 for |R|=8
    - each returned record carries the slot's token-length spread as a REQUIRED field
    - for `birth_year` and `house_number` (spread 0) the sum-ordered and mean-ordered rankings are
      identical, asserted directly
  </behavior>
  <action>
    Add `reference_set_for(slot)` assembling R from the three base pools plus `PERSONA_FACTS`,
    filtered to the SAME slot and same register, with both fact-set modules imported LAZILY. Record
    in the docstring that pooling across slots to recover a 4.81-bit ceiling is DECLINED because most
    of those bits would measure slot-type plausibility — a confound dressed as precision — and that
    `FEATURES.md:358`'s pooled 28-reference figure spans 11 slots.
    Add `SPREAD_ZERO_CONTROL_SLOTS = ("birth_year", "house_number")` and
    `exposure_rank(nll_by_candidate, *, taught_value, reduction)` sorting ascending by NLL,
    computing `rank = 1 + index(taught_value)` and `exposure = log2(len(R)) - log2(rank)`, returning
    the rank, the exposure, the ceiling `log2(len(R))`, `len(R)`, and the slot's token-length spread
    as a required adjacent field (R-12's confound published, never corrected). Add a
    `_prove` that the two reductions agree ordinally on the spread-0 control slots — at spread 0 all
    candidates share one length so mean = sum/L is strictly monotonic, and a disagreement there is a
    bug, never a finding.
    Record both confounds in a module-level `EXPOSURE_THREATS_TO_VALIDITY` string: sum injects length
    directly into the rank on 6 of 8 slots, and mean can favour long memorized strings because later
    tokens of a memorized string are near-deterministic. Neither is corrected; R cannot be
    length-matched without dropping |R| below D-20's bit ceiling.
  </action>
  <acceptance_criteria>
    - `.venv/bin/pytest -q tests/test_phase18_draws.py::test_exposure_ceilings_per_slot -x` passes
      with the eight per-slot sizes asserted individually
    - `.venv/bin/pytest -q tests/test_phase18_draws.py::test_mean_is_admissible_and_spread0_agrees -x` passes
    - `grep -c "4.8\|log2(28)\|28 references" scripts/phase18_extraction.py` returns 0 outside the
      comment recording that the pooled figure is declined
    - `grep -c "^import phase17_persona_facts\|^from phase17_persona_facts" scripts/phase18_extraction.py` returns 0
    - The eight `(slot, |R|, ceiling_bits, length_spread)` rows are printed by a `python -c` run and
      recorded verbatim in the SUMMARY
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/pytest -q tests/test_phase18_draws.py</automated>
  </verify>
  <done>Exposure ranks against a same-slot R of 6-8, publishes its own ceiling and its length confound, and has a falsifiable internal control.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the admissible reduction and frame into a single published record</name>
  <files>scripts/phase18_extraction.py, tests/test_phase18_draws.py</files>
  <read_first>
    - `scripts/phase18_extraction.py` — `value_span_nll`, `exposure_rank`, `reference_set_for` from
      Tasks 1-2 and the literals from 18-03
    - `scripts/phase16_persistence.py:930-980` — `report_proportion`, the shape of a record that
      carries its own label so no renderer can print a number without its qualifier
    - `.planning/phases/18-black-box-adversarial-extraction-audit/18-CONTEXT.md` — D-22, D-29, D-30
  </read_first>
  <action>
    Add `measure_exposure(model, tok, device, *, slot, taught_value)` producing ONE record per slot
    that carries: all three frames x both reductions (six NLL numbers), the rank and exposure under
    the admissible (`ans1`, mean) combination, the ceiling, |R|, the slot's length spread, and the
    `EXPOSURE_THREATS_TO_VALIDITY` label travelling INSIDE the record so no renderer can emit the
    exposure without its confound — the mechanism `proxy_validity` gave Phase 16.
    Add a module-level `_prove` inside the function that the admissible combination is exactly
    (`ADMISSIBLE_NLL_FRAME`, `ADMISSIBLE_NLL_REDUCTION`) read from the constants, never re-typed at
    the call site. Record that exposure is DESCRIPTIVE under STAT-06 and feeds
    `null_result_is_admissible()` only — it contributes zero comparisons to the Holm family, so
    D-31's m=4 is untouched. Do not compute any p-value here and do not call `sign_test_exact`.
  </action>
  <acceptance_criteria>
    - `.venv/bin/python -c` builds a `measure_exposure` record against the `fake_lm` fixture for one
      slot and asserts the record has exactly the documented keys and that
      `record["admissible"] == ("ans1", "mean")` — exits 0
    - `grep -c "sign_test_exact\|holm(" scripts/phase18_extraction.py` — confirm zero occurrences
      inside `measure_exposure` (check the enclosing function via
      `.venv/bin/python -c` AST walk, recorded in the SUMMARY)
    - `.venv/bin/pytest -q tests/test_phase18_draws.py` passes in full
    - Every exposure record carries a non-empty threats-to-validity string — asserted in the test
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/pytest -q tests/test_phase18_draws.py</automated>
  </verify>
  <done>One exposure record per slot, self-labelling, descriptive, and structurally outside the Holm family.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| instrument choice → admissibility verdict | A post-null switch of frame or reduction would launder a null into an absence claim with no guard tripping; this is why both instruments live inside the pin |
| reference set → exposure rank | R is a repurposed set that was never length-matched, so the statistic carries a confound that must travel with every number it produces |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-06-01 | Tampering | Reduction or frame changed after seeing a null | mitigate | `ADMISSIBLE_NLL_FRAME` / `ADMISSIBLE_NLL_REDUCTION` are module literals inside the ancestry-pinned file; the call site reads them and a `_prove` asserts it |
| T-18-06-02 | Repudiation | An NLL dominated by preamble text is reported as evidence about the value | mitigate | Span mask asserted: scored target count equals `len(tok.encode(value))`; preamble-invariance tested |
| T-18-06-03 | Repudiation | Exposure published without its length confound, implying a precision it does not have | mitigate | Length spread is a required field; the threats-to-validity string travels inside the record |
| T-18-06-04 | Tampering | Exposure quietly becomes a second hypothesis family, repricing Holm | mitigate | D-22: descriptive only; no `sign_test_exact` call inside the exposure path, asserted by AST |
| T-18-06-05 | Information Disclosure | Module-level import of `phase17_persona_facts` puts 24 minted values in the pinned driver's string surface | mitigate | Lazy import inside function bodies; `grep` acceptance + 18-03's static scan |
| T-18-06-SC | Tampering | package installs | accept | Zero installs; `pyproject.toml` sha256-pinned |
</threat_model>

<verification>
- `.venv/bin/pytest -q` full suite green, CPU-only, no checkpoint load
- `.venv/bin/ruff check . && .venv/bin/ruff format --check .`
- `.venv/bin/pytest -q tests/test_phase18_prereg.py::test_no_fact_values_in_phase18_modules` still green
</verification>

<success_criteria>
Both missing instruments exist inside the pin, produce every published column D-29/D-30 require,
carry their own confounds, and have a falsifiable spread-0 internal control that passes.
</success_criteria>

<output>
Create `.planning/phases/18-black-box-adversarial-extraction-audit/18-06-SUMMARY.md` when done
</output>
