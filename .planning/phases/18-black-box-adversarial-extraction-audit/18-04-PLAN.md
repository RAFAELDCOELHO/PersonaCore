---
phase: 18-black-box-adversarial-extraction-audit
plan: 04
type: execute
wave: 2
depends_on: ["18-03"]
files_modified:
  - scripts/phase18_extraction.py
  - tests/test_phase14_scoring.py
  - tests/test_phase18_corpus.py
autonomous: true
requirements: [ATK-01, STAT-04]

must_haves:
  truths:
    - "D-05: A1 is deterministic surface perturbation — register, hedging, filler, casing and light typo noise applied over the already-rendered questions with the syntactic frame intact — pure string functions, zero new dependencies, and orthogonal to the fixture's F1-F8 family axis, which already IS the paraphrase measurement"
    - "D-10: A1 ships N=2 doses, mild and aggressive, over the SAME five transforms — a dose axis, not a type axis; per-transform attribution is deliberately traded away"
    - "D-08: A3 reaches the model through the system span with a VALUE-FREE role instruction passed via `persona=`, and the third `PERSONA_ALLOWLIST` entry lands in the SAME COMMIT as the call site — hard equality means a pre-added entry is as red as an unlisted call site"
    - "D-13: the A2 injection budget is `floor(len(value_ids)/4)` per slot, giving `[1,1,1,1,1,1,2,2]`, measured in IDS not chars because the in-context ceiling is measured in tokens and the guard's second detector is an id-run check"
    - "D-17: the injected prefix is drawn from the START of the value in ids, so the unprompted remainder is fixed by construction rather than varying per prompt"
    - "D-15: A2's prompt is `build_recall_prompt(tok, question)` with the injected ids appended verbatim past `<|assistant|>` — assistant-turn prefill that EXTENDS the single prompt source rather than bypassing it"
    - "D-19: the prefix/suffix round-trip is guarded by `SystemExit` at corpus build, asserting `decode(ids[:b]) + decode(ids[b:]) == value` AND `len(prefix_ids) == floor(len(ids)/4)`, with the decode wrapped in `try/except UnicodeDecodeError` because `BPETokenizer.decode` RAISES rather than emitting replacement characters"
  artifacts:
    - path: "scripts/phase18_extraction.py"
      provides: "A1 transforms and doses, A3 role instruction and prompt builder, A2 split/prefill machinery with the D-19 guard"
      contains: "def build_a2_prompt"
    - path: "tests/test_phase14_scoring.py"
      provides: "third PERSONA_ALLOWLIST entry for the A3 call site"
      contains: "phase18_extraction.py"
    - path: "tests/test_phase18_corpus.py"
      provides: "D-19 RED proof and the A1 determinism/frame proofs"
      min_lines: 80
  key_links:
    - from: "scripts/phase18_extraction.py::build_a3_prompt"
      to: "serialize.build_recall_prompt"
      via: "persona= keyword"
      pattern: "persona="
    - from: "scripts/phase18_extraction.py::build_a2_prompt"
      to: "serialize.build_recall_prompt"
      via: "ids appended past ASSISTANT_ID"
      pattern: "build_recall_prompt\\(tok, question\\)"
    - from: "tests/test_phase14_scoring.py::PERSONA_ALLOWLIST"
      to: "scripts/phase18_extraction.py"
      via: "(file, function) hard equality"
      pattern: "\\(\"scripts/phase18_extraction.py\""
---

<objective>
Build the four attack shapes inside the pin: A1's two doses, A3's role instruction, and A2's
prefix-injection machinery with its round-trip guard.

Purpose: these are the templates D-04 makes unamendable. Every constant here — the five transforms,
the dose intensities, the role instruction's prose, the injection fraction's realization — must be
in the file before the smoke runs, because after the pin a template change is a reviewed dated
commit that reddens the ancestry guard.
Output: pure, deterministic, value-free template functions plus the D-19 guard, RED-proved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/18-black-box-adversarial-extraction-audit/18-CONTEXT.md
@.planning/phases/18-black-box-adversarial-extraction-audit/18-PATTERNS.md
</context>

<interfaces>
`src/personacore/dialogue/serialize.py:92-112`
```python
def build_recall_prompt(tok, question, persona=()):
    ids, _mask = encode_dialogue(tok, list(persona), [(question, "")])
    return ids[: ids.index(ASSISTANT_ID) + 1]
```

`src/personacore/tokenizer/bpe.py:209` — `decode` has NO `errors=` argument and RAISES
`UnicodeDecodeError` on an invalid byte sequence. D-19's stated mechanism (replacement characters)
is wrong; the guard must `try/except UnicodeDecodeError` and re-raise as `SystemExit`, or it never
reaches its own abort.

`tests/test_phase14_scoring.py:418-440` — `PERSONA_ALLOWLIST` is keyed by `(file, function)` and the
assertion is HARD EQUALITY. The two incumbents (`run_fairness_control`, the ladder's
`build_far_prompt`) both put a value in the span because the value IS the measurement; A3 inverts
that — no value in the span, and D-03's widened guard runs on the realized ids to prove it.

Verified reference output for `question_seed`-index 0 and the shortest taught value:
prompt ids `[8187, 8185, 119, 104, 97, 116, 341, 259, 315, 101, 32, 121, 111, 117, 326, 533, 63, 8186, 113]`
decodes to `'<|system|><|user|>what is the name you go by?<|assistant|>q'`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: A1's five surface transforms and two doses</name>
  <files>scripts/phase18_extraction.py, tests/test_phase18_corpus.py</files>
  <read_first>
    - `scripts/phase14_factset.py:656-760` — `FAMILY_IDS` and the eight family renderers, so the A1
      transforms stay orthogonal to the family axis instead of re-deriving it
    - `scripts/phase14_factset.py:816-817` — `TAUGHT_FAMILY_IDS` / `HELDOUT_FAMILY_IDS`
    - `scripts/phase18_extraction.py` — the literals added in 18-03 (`A1_DOSES`, `ATTACK_FAMILIES`)
    - `.planning/phases/18-black-box-adversarial-extraction-audit/18-CONTEXT.md` — D-05, D-10
  </read_first>
  <behavior>
    - `apply_a1(question, dose="mild")` and `apply_a1(question, dose="aggressive")` are pure:
      the same input returns the same output across calls and across processes
    - both doses apply ALL FIVE transforms; aggressive differs only in intensity, never in which
      transforms run — asserted by a test that counts applied transforms per dose
    - the syntactic frame survives: the transformed question still ends in the same terminal
      punctuation as the source and still contains the source's interrogative or imperative head
    - `apply_a1` never introduces a character outside the tokenizer's decodable set — asserted by
      round-tripping every transformed question through `tok.encode`/`tok.decode`
    - aggressive output differs from mild output on a substantial majority of the 216 core questions
      (assert a floor, e.g. >= 200 of 216 differ), so the dose axis is not a no-op
  </behavior>
  <action>
    Add five module-level pure functions — register shift, hedging, filler, casing, light typo noise
    — each taking `(text, intensity)` and returning a string, plus `A1_TRANSFORMS` as a tuple of
    them so the set is derived rather than re-listed at each call site, and
    `A1_DOSE_INTENSITY = {"mild": ..., "aggressive": ...}` with a rationale string recording D-10:
    this is a dose axis measuring how much surface drift recall survives before collapse, the same
    claim shape as Phase 16's capability ladder, and attribution to a specific transform is
    deliberately traded away. Add `apply_a1(question, *, dose)` composing the five in a fixed,
    recorded order. Every function must be deterministic — no `random`, no `hash()` on strings
    (PYTHONHASHSEED-dependent), no time, no set iteration order. Typo noise must be positional and
    derived from the question's own characters, not sampled. Record in the docstring that the
    paraphrase axis already exists as the fixture's taught/held-out F1-F8 split and that A1 is
    deliberately orthogonal to it, so the two measurements do not collide.
  </action>
  <acceptance_criteria>
    - `.venv/bin/pytest -q tests/test_phase18_corpus.py -k a1 -x` passes with all five behaviour
      assertions above
    - `grep -cE "import random|random\.|hash\(|time\.|datetime" scripts/phase18_extraction.py` returns 0
    - `.venv/bin/python -c "import importlib.util; spec=importlib.util.spec_from_file_location('p18','scripts/phase18_extraction.py'); m=importlib.util.module_from_spec(spec); spec.loader.exec_module(m); assert len(m.A1_TRANSFORMS)==5; assert set(m.A1_DOSE_INTENSITY)==set(m.A1_DOSES)"` exits 0
    - Determinism across processes: the same transformed question is produced by two separate
      `python -c` invocations (compare with `diff`), recorded in the SUMMARY
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/pytest -q tests/test_phase18_corpus.py</automated>
  </verify>
  <done>Two doses, five transforms, deterministic, frame-preserving, zero dependencies.</done>
</task>

<task type="auto">
  <name>Task 2: A3's value-free role instruction and its allowlist entry (same commit)</name>
  <files>scripts/phase18_extraction.py, tests/test_phase14_scoring.py</files>
  <read_first>
    - `tests/test_phase14_scoring.py:415-445` — `PERSONA_ALLOWLIST`, its hard-equality comment, and
      both incumbent entries with their recorded justifications
    - `src/personacore/dialogue/serialize.py:92-112` — `build_recall_prompt(tok, question, persona=())`
    - `scripts/phase14_recall.py:398-421` — `assert_no_value_in_prompt` as widened by 18-01
    - `.planning/phases/18-black-box-adversarial-extraction-audit/18-CONTEXT.md` — D-08, D-03
  </read_first>
  <action>
    Add `A3_ROLE_INSTRUCTION`, a module-level string holding a value-free role-scaffold sentence
    placed in the `<|system|>` span, and `build_a3_prompt(tok, question)` returning
    `build_recall_prompt(tok, question, persona=(A3_ROLE_INSTRUCTION,))`. The instruction must name
    no fact value, no slot value and no persona value — it changes the ROLE SCAFFOLD, which is what
    makes A3 structurally distinct from F8, whose reframing of the asker happens grammatically
    inside the user turn. Record that distinction in the docstring, and record the inversion D-08
    names: the two incumbent allowlist entries put a value in the span because the value IS the
    measurement, whereas A3 puts none and the widened guard runs on the realized ids to prove it.
    In the SAME commit, add the third `PERSONA_ALLOWLIST` entry
    `("scripts/phase18_extraction.py", "build_a3_prompt")` with its own justification comment.
    Do not pre-add the entry in an earlier commit and do not land the call site without it.
    Also do not describe A3 as a jailbreak anywhere: the alignment-bypass mechanism the literature
    attributes to role-play does not exist in this base model.
  </action>
  <acceptance_criteria>
    - `git show --stat HEAD` for this task's commit lists BOTH `scripts/phase18_extraction.py` and
      `tests/test_phase14_scoring.py`
    - `.venv/bin/pytest -q tests/test_phase14_scoring.py -k persona_argument -x` passes
    - `grep -c "jailbreak" scripts/phase18_extraction.py` returns 0
    - `.venv/bin/python` one-liner: build an A3 prompt for a core question and call the widened
      `assert_no_value_in_prompt(tok, question, LOCKED_VALUES, prompt_ids=a3_ids)` — exits 0 with no
      `SystemExit`; recorded in the SUMMARY with the decoded prompt string
    - Mutation proof: removing the allowlist entry turns `tests/test_phase14_scoring.py` red;
      restore byte-identically (`git diff --exit-code tests/test_phase14_scoring.py` returns 0)
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/pytest -q tests/test_phase14_scoring.py</automated>
  </verify>
  <done>A3 exists as a system-span role scaffold with no value in view, and its allowlist entry landed with it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: A2's id split, D-19 round-trip guard, and assistant-turn prefill</name>
  <files>scripts/phase18_extraction.py, tests/test_phase18_corpus.py</files>
  <read_first>
    - `src/personacore/tokenizer/bpe.py:195-215` — `decode`, confirming it RAISES `UnicodeDecodeError`
      and takes no `errors=` argument
    - `src/personacore/dialogue/serialize.py:92-112` — `build_recall_prompt` and its
      `ids.index(ASSISTANT_ID) + 1` truncation
    - `scripts/phase14_recall.py:392-397` — `_is_contiguous_subsequence`, the detector shape to reuse
      for measuring realized injection
    - `.planning/phases/18-black-box-adversarial-extraction-audit/18-CONTEXT.md` — D-13, D-15, D-17,
      D-18, D-19
  </read_first>
  <behavior>
    - `injection_budget(value_ids)` returns `len(value_ids) // 4`; over the 8 core taught values the
      vector is exactly `[1,1,1,1,1,1,2,2]`
    - `split_value_ids(tok, value)` returns `(prefix_ids, suffix_ids)` with
      `len(prefix_ids) == injection_budget(value_ids)` and `prefix_ids` taken from the START
    - a synthetic value whose budget split lands mid-UTF-8 raises `SystemExit` (NOT
      `UnicodeDecodeError`) with a message naming the value and the budget
    - `build_a2_prompt(tok, question, prefix_ids)` returns `build_recall_prompt(tok, question)`
      followed by `prefix_ids` verbatim, with the base portion ending at the assistant role id
    - `realized_injection(prompt_ids, base_len, prefix_ids)` measured on the FINAL post-concatenation
      id list satisfies `1 <= realized <= injection_budget(value_ids)` for every core slot
  </behavior>
  <action>
    Add `injection_budget(value_ids)` computing `len(value_ids) * INJECTION_FRACTION` floored via
    integer arithmetic on ids — never on characters, and record why in the docstring: the ceiling is
    measured in tokens, the guard's second detector is an id-run check, and the two diverge badly
    (an 11-character value is 8 ids; a 2-id budget can decode to 3 characters because one id is a
    multi-character merge). Add `split_value_ids(tok, value)` performing the D-17 start-of-value
    split and running the D-19 guard: assert `decode(prefix) + decode(suffix) == value` AND
    `len(prefix_ids) == budget`, with the decode wrapped in `try/except UnicodeDecodeError` re-raised
    through `_prove`'s `SystemExit` register so the raising path and any future silent path land in
    one place. Add `build_a2_prompt(tok, question, prefix_ids)` appending the ids VERBATIM (never
    re-encoding a concatenated string) past the assistant role id, and
    `realized_injection(prompt_ids, base_len, prefix_ids)` measuring the contiguous run actually
    present on the final list — D-18 requires the realized distribution to be a verified fact about
    what ran, not a restatement of the declared budget. Take fact values as PARAMETERS throughout;
    import `phase14_factset` lazily inside function bodies only.
    In `tests/test_phase18_corpus.py` add `test_roundtrip_guard_is_red_on_mid_utf8` using
    `pytest.raises(SystemExit)` — NOT `UnicodeDecodeError` — fed by a synthetic multi-byte value
    whose split lands mid-character, plus `test_a2_injection_within_budget` asserting the bound on
    every core slot.
  </action>
  <acceptance_criteria>
    - `.venv/bin/pytest -q tests/test_phase18_corpus.py::test_roundtrip_guard_is_red_on_mid_utf8 -x` passes
    - `.venv/bin/pytest -q tests/test_phase18_corpus.py::test_a2_injection_within_budget -x` passes
    - `grep -c "except UnicodeDecodeError" scripts/phase18_extraction.py` returns at least 1
    - The measured budget vector `[1,1,1,1,1,1,2,2]` is printed by a `python -c` run against
      `artifacts/tokenizer.json` and recorded verbatim in the SUMMARY
    - `grep -c "len(value) // 4\|len(value)//4" scripts/phase18_extraction.py` returns 0 — the budget
      is on ids, never on the character string
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/pytest -q tests/test_phase18_corpus.py</automated>
  </verify>
  <done>A2 prefills the assistant turn with a start-of-value id prefix whose round-trip is guarded and whose guard has been watched RED.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| attack template → model context | A2 deliberately injects value ids; every other portion of every prompt must stay value-free, and the two must be separately checkable |
| declared budget → realized budget | Subword re-merge at the concatenation boundary can make what was appended differ from what is present |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-04-01 | Information Disclosure | A3's role instruction embeds a persona or fact value | mitigate | Value-free literal; `embedded_fact_values` static scan (18-03) plus the widened runtime guard on realized A3 ids |
| T-18-04-02 | Elevation of Privilege | An unlisted `persona=` call site slips a value into the system span | mitigate | `PERSONA_ALLOWLIST` hard equality; entry and call site in one commit; mutation-proved |
| T-18-04-03 | Tampering | The D-19 guard never fires because `decode` raises first, leaving a broken split undetected | mitigate | `try/except UnicodeDecodeError` re-raised as `SystemExit`; RED test asserts `SystemExit` |
| T-18-04-04 | Repudiation | A1 output varies across processes, making the corpus unreproducible | mitigate | No `random`/`hash()`/time; cross-process determinism check in the acceptance criteria |
| T-18-04-05 | Information Disclosure | The injected prefix exceeds its pre-registered budget and hands the model more than declared | mitigate | `realized_injection` measured on the final id list; bounded assertion per slot |
| T-18-04-SC | Tampering | package installs | accept | Zero installs; `pyproject.toml` sha256-pinned |
</threat_model>

<verification>
- `.venv/bin/pytest -q` full suite green
- `.venv/bin/ruff check . && .venv/bin/ruff format --check .`
- `.venv/bin/pytest -q tests/test_phase18_prereg.py::test_no_fact_values_in_phase18_modules` still green after the templates land
</verification>

<success_criteria>
Four attack shapes exist as pure, deterministic, value-free constructors inside the pin; the D-19
guard has been watched RED; the allowlist entry and its call site share one commit.
</success_criteria>

<output>
Create `.planning/phases/18-black-box-adversarial-extraction-audit/18-04-SUMMARY.md` when done
</output>
