---
phase: 18-black-box-adversarial-extraction-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phase16_persistence.py
  - scripts/phase14_recall.py
  - tests/test_phase18_widenings.py
autonomous: true
requirements: [STAT-04, STAT-06, ATK-01]

must_haves:
  truths:
    - "D-31: `holm` can price a family of 4 — the dose-split A1-mild/A1-aggressive/A2/A3 family on `core_held_out` — without a Phase-18-local copy of the function"
    - "S-2/STAT-04: the statistics instrument is IMPORTED and widened, never re-implemented — a duplicated Holm is a rule that can drift"
    - "D-03: `assert_no_value_in_prompt` gains a `prompt_ids` path so the corpus is checked against the BYTES the model receives, not a reconstruction — signature-symmetric with its twin `assert_value_in_prompt(tok, prompt_ids, values)`"
    - "Both widenings are additive: every Phase 14/16/17 call site keeps bit-identical behaviour, and no assertion or branch is weakened"
  artifacts:
    - path: "scripts/phase16_persistence.py"
      provides: "holm(p_values, *, family=HOLM_FAMILY_PAIRS)"
      contains: "def holm(p_values, *, family="
    - path: "scripts/phase14_recall.py"
      provides: "assert_no_value_in_prompt with a prompt_ids path"
      contains: "prompt_ids=None"
    - path: "tests/test_phase18_widenings.py"
      provides: "default-preserved + widened-path proofs for both instruments"
      min_lines: 60
  key_links:
    - from: "scripts/phase16_persistence.py::holm"
      to: "family parameter"
      via: "m = len(family)"
      pattern: "m = len\\(family\\)"
    - from: "scripts/phase14_recall.py::assert_no_value_in_prompt"
      to: "caller-supplied prompt ids"
      via: "skips build_recall_prompt when prompt_ids is given"
      pattern: "prompt_ids"
---

<objective>
Widen the two shared instruments Phase 18 collides with, additively, before any Phase 18 driver
code calls them.

Purpose: `holm` hard-`_prove`s `len(p_values) == 6` off `HOLM_FAMILY_PAIRS`, so D-31's m=4 family
raises `SystemExit` on the verdict path. `assert_no_value_in_prompt` rebuilds the prompt from the
question string, so it cannot see A2's appended ids or A3's persona span. Both are fixed by an
additive keyword, per this project's import-never-copy discipline.
Output: two widened functions with defaults that preserve every existing caller, plus a committed
test file proving both halves.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-black-box-adversarial-extraction-audit/18-CONTEXT.md
</context>

<interfaces>
Verified against the working tree this session — use these directly, do not re-explore.

`scripts/phase16_persistence.py:1170-1202`
```python
def holm(p_values):                      # p_values is {pair: p}
    m = len(HOLM_FAMILY_PAIRS)           # == 6, from itertools.combinations(CONDITION_ORDER, 2)
    _prove(len(p_values) == m, "...the family is closed at {m} (D-09)...")
    ordered = sorted(p_values.items(), key=lambda item: item[1])
    # alpha_at_step = HOLM_ALPHA / (m - index); comparison is STRICT (p < alpha_at_step)
```
`HOLM_ALPHA = 0.05` (`:1005`), `SIGN_TEST_N = 8` (`:1016`), `sign_test_exact(signs)` (`:1088`),
`assert_family_closed` (`:1142`).

`scripts/phase14_recall.py:392-421`
```python
def _is_contiguous_subsequence(haystack, needle): ...        # :392
def assert_no_value_in_prompt(tok, question, values):        # :398
    ids = build_recall_prompt(tok, question)
    decoded = normalize(tok.decode(ids))
    for value in values:
        _prove(normalize(value) not in decoded, "...")
        _prove(not _is_contiguous_subsequence(ids, tok.encode(value)), "...")
def assert_value_in_prompt(tok, prompt_ids, values): ...     # :424  <- the signature twin
```
Neither `scripts/phase16_persistence.py` nor `scripts/phase14_recall.py` is under a STAT-05
ancestry pin — only `scripts/erasure_gate.py` (`PREREG_ARTIFACT`) and
`scripts/phase17_personas.py` (`PHASE17_PREREG_ARTIFACT`) are. Verified in
`tests/test_phase16_prereg.py:50-61`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen holm with an additive family= keyword</name>
  <files>scripts/phase16_persistence.py, tests/test_phase18_widenings.py</files>
  <read_first>
    - `scripts/phase16_persistence.py:1130-1205` — `assert_family_closed`, `holm`, and the docstring
      sentence "``m`` is ``len(HOLM_FAMILY_PAIRS)``, READ from the constant and never retyped"
    - `scripts/phase16_persistence.py:983-1035` — `HOLM_ALPHA`, `SIGN_TEST_N`, and the family-size
      arithmetic comment block that must stay true
    - `.planning/phases/18-black-box-adversarial-extraction-audit/18-CONTEXT.md` — D-31
  </read_first>
  <behavior>
    - `holm(p, family=HOLM_FAMILY_PAIRS)` with 6 p-values returns exactly what `holm(p)` returned
      before the widening — same tuples, same order, same alpha_at_step floats
    - `holm(p4, family=("A1-mild","A1-aggressive","A2","A3"))` with 4 p-values returns 4 rows whose
      first `alpha_at_step` is `0.05 / 4 == 0.0125`
    - `holm(p4)` — 4 p-values with the DEFAULT family — still raises `SystemExit` naming 6
    - `holm(p6, family=<4-tuple>)` raises `SystemExit` — the guard prices the passed family, not a
      hard-coded 6
  </behavior>
  <action>
    Change the signature to `holm(p_values, *, family=HOLM_FAMILY_PAIRS)` and replace the single
    line `m = len(HOLM_FAMILY_PAIRS)` with `m = len(family)`. Nothing else in the body changes —
    the sort, the `HOLM_ALPHA / (m - index)` step, the strict `<` comparison and the step-down
    latch stay byte-identical. Update the `_prove` message to interpolate the family it was given
    so an abort names the size that was actually priced. Extend the docstring with one sentence
    recording that Phase 18's D-31 family is m=4 dose-split on `core_held_out` and that the
    default keeps every Phase 16/17 caller on the closed 6-pair family; keep the existing sentence
    about `m` being read and never retyped, since it is still true of `family`.
    Do NOT touch `assert_family_closed`, `HOLM_FAMILY_PAIRS`, `HOLM_ALPHA` or `sign_test_exact`.
    Record in the docstring that the strict comparison is unaffected at m=4: the first step is
    0.0125 and the best achievable p at n=8 is 0.0078125.
  </action>
  <acceptance_criteria>
    - `grep -n "def holm(p_values, \*, family=HOLM_FAMILY_PAIRS)" scripts/phase16_persistence.py`
      returns exactly 1 line
    - `grep -c "m = len(HOLM_FAMILY_PAIRS)" scripts/phase16_persistence.py` returns 0
    - `git diff --numstat scripts/phase16_persistence.py` shows deletions confined to the signature
      line, the `m = ` line and the `_prove` message — no removed assertion
    - `.venv/bin/pytest -q tests/test_phase18_widenings.py -k holm` passes with at least 4 test
      cases (default-preserved, m=4 accepted, wrong-count-under-default raises, wrong-count-under-
      passed-family raises)
    - `.venv/bin/pytest -q tests/test_phase16_stats.py` passes unchanged
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/pytest -q tests/test_phase18_widenings.py tests/test_phase16_stats.py</automated>
  </verify>
  <done>`holm` prices whatever family it is handed; every existing caller is bit-identical; four cases committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the prompt_ids path to assert_no_value_in_prompt (D-03)</name>
  <files>scripts/phase14_recall.py, tests/test_phase18_widenings.py</files>
  <read_first>
    - `scripts/phase14_recall.py:392-460` — `_is_contiguous_subsequence`, `assert_no_value_in_prompt`,
      and `assert_value_in_prompt` with the docstring recording why the presence twin ORs its two
      detectors while the absence twin ANDs them
    - `src/personacore/dialogue/serialize.py:92-112` — `build_recall_prompt(tok, question, persona=())`
    - `.planning/phases/18-black-box-adversarial-extraction-audit/18-CONTEXT.md` — D-03, D-16
  </read_first>
  <behavior>
    - `assert_no_value_in_prompt(tok, q, values)` — no `prompt_ids` — behaves exactly as today:
      builds the prompt from `q`, raises `SystemExit` on a value in view, returns None otherwise
    - `assert_no_value_in_prompt(tok, q, values, prompt_ids=ids)` checks `ids` and never calls
      `build_recall_prompt`
    - A value present only in the passed ids (not in `q`) raises `SystemExit` — the widened path
      genuinely reads the passed bytes
    - A value present in `q` but absent from the passed ids still raises, because the decoded-string
      detector runs on the ids and the question is embedded in them
  </behavior>
  <action>
    Add a keyword-only `prompt_ids=None` parameter. When it is None, keep the existing
    `ids = build_recall_prompt(tok, question)` line verbatim; when it is supplied, use it directly.
    Both detectors — the normalized-string absence and the contiguous-id-run absence — keep running
    on `ids` and keep being ANDed. `question` stays a required positional and is still interpolated
    into both abort messages, so an abort names the source question even when the ids came from a
    caller-built A2 or A3 prompt. Extend the docstring with the D-03 sentence: the corpus is checked
    against the bytes the model receives, not a reconstruction; the widening is signature-symmetric
    with `assert_value_in_prompt(tok, prompt_ids, values)`. Do not weaken, reorder or make optional
    either detector, and do not touch `assert_value_in_prompt`.
  </action>
  <acceptance_criteria>
    - `grep -n "prompt_ids=None" scripts/phase14_recall.py` returns exactly 1 line, inside
      `assert_no_value_in_prompt`'s signature
    - `grep -c "_is_contiguous_subsequence(ids, tok.encode(value))" scripts/phase14_recall.py`
      returns 1 — the id-run detector was not duplicated or dropped
    - `.venv/bin/pytest -q tests/test_phase18_widenings.py -k no_value_in_prompt` passes with all
      four behaviour cases above
    - `.venv/bin/pytest -q tests/test_phase14_scoring.py tests/test_phase16_driver.py` passes unchanged
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/pytest -q tests/test_phase18_widenings.py tests/test_phase14_scoring.py</automated>
  </verify>
  <done>The runtime clean-room guard can be pointed at realized prompt ids; the question path is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| taught fact value → model context | A widened guard that stops checking is the only thing between a locked value and the prompt that is supposed to prove it is absent |
| pre-registered statistic → Phase 18 verdict | A second `holm` implementation is a second rule the report cannot attribute |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01-01 | Tampering | `holm` widening silently changes Phase 16/17 arithmetic | mitigate | Default `family=HOLM_FAMILY_PAIRS`; committed test asserts the 6-family return is byte-identical to the pre-widening tuples; `tests/test_phase16_stats.py` re-run |
| T-18-01-02 | Information Disclosure | `assert_no_value_in_prompt` weakened while being widened | mitigate | Both detectors kept ANDed; test asserts a value visible ONLY in the passed ids still raises `SystemExit` |
| T-18-01-03 | Repudiation | A Phase-18-local copy of `holm` would let the report cite a rule that is not the pinned one | mitigate | Widen in place; `grep` acceptance forbids a second `def holm` anywhere under `scripts/phase18_*.py` (enforced by 18-03's static scan) |
| T-18-01-SC | Tampering | package installs | accept | This phase installs nothing; STAT-04 pins `pyproject.toml` by sha256 in `tests/test_package.py`, so a dependency cannot arrive quietly |
</threat_model>

<verification>
- `.venv/bin/pytest -q` — full suite (652 tests today) green
- `.venv/bin/ruff check . && .venv/bin/ruff format --check .`
- `git diff --stat` shows only the two source files and the one new test file
</verification>

<success_criteria>
`holm` accepts a 4-member family and still refuses a mis-sized one; `assert_no_value_in_prompt`
accepts realized prompt ids; the full suite is green with zero changes to any existing test.
</success_criteria>

<output>
Create `.planning/phases/18-black-box-adversarial-extraction-audit/18-01-SUMMARY.md` when done
</output>
