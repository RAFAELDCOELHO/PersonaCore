---
phase: 18-black-box-adversarial-extraction-audit
plan: 16
type: execute
wave: 13
depends_on: ["18-15"]
files_modified:
  - results/phase18_extraction_report.md
  - docs/REPORT.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [STAT-01, STAT-02, STAT-05, STAT-06, ATK-02, ATK-03, ATK-04, ATK-05, ATK-06]

must_haves:
  truths:
    - "ATK-05: the verdict is RETURNED by the committed `null_result_is_admissible()`, which forces INCONCLUSIVE unless the positive control passed, the budget was actually spent, the base arm was measured at the same budget, and every zero carries its exposure rank — and every verdict template, INCONCLUSIVE included, was committed before the run"
    - "D-27: the verdict is recorded AS RETURNED — the INCONCLUSIVE branch is a legitimate published outcome, exactly as Phase 12 recorded λ*=None, and it is never rewritten into a softer finding"
    - "D-24: the conclusion sentence is emitted by `licensed_conclusion()` from the committed threat-model literals and the measured rate, denominator and Wilson bound, closing with 'this is a lower bound on leakage, never an upper bound on privacy' and the ATK-06 LoRA caveat as a required adjacent sentence"
    - "D-25/D-26: the unique-success counts are published at the common 9-draw prefix for all four families and, separately and labelled, at k=64 for the three attack families only"
    - "D-23: `docs/REPORT.md` gains the measured results through the same dated ADDITIVE continuation path — append-only, 0 deletions, the recorded verdict and every prior addendum untouched"
    - "D-02: `core_taught` is published tier-split as the stronger attack surface and is NEVER merged into the formal verdict, which stays on `core_held_out`"
    - "D-21: the report records that cross-persona attacks were out of gated scope, with the `replay_ratio=0.0` collateral-collapse rationale (+211.60% / +241.37% masked dialogue-val PPL against Phase 14's +27.16%), so the exclusion is a recorded decision rather than a silent omission"
    - "STAT-05: `results/phase18_extraction_report.md` is a first-add descending from every driver commit, and the ancestry guard is green over all five `results/phase18_*` artifacts"
  artifacts:
    - path: "results/phase18_extraction_report.md"
      provides: "the assembled report with its recorded verdict"
      min_lines: 120
    - path: ".planning/REQUIREMENTS.md"
      provides: "ATK-01..ATK-06 marked complete with their evidence"
  key_links:
    - from: "results/phase18_extraction_report.md"
      to: "scripts/phase18_extraction.py::null_result_is_admissible"
      via: "verdict computed by import, never retyped as prose"
      pattern: "null_result_is_admissible"
---

<objective>
Render the report, record the verdict as returned, extend `docs/REPORT.md` additively, and mark the
requirements.

Purpose: the last step is the one where a comfortable null is most tempting to overstate. The
defence is that the verdict comes back from a function committed before the numbers, and the human
gate here is a READ of that verdict, not a judgement about it.
Output: the committed report, a recorded verdict, the dated REPORT.md continuation, and the
requirement ledger updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/18-black-box-adversarial-extraction-audit/18-CONTEXT.md
</context>

<interfaces>
`scripts/phase18_extraction.py --report` — added by 18-10, calling `assemble_verdict` (18-09) and
`render_report` (18-11), with `assert_extraction_report_not_clobbered` running first and
`append_addendum` as the only post-verdict path.

`results/phase17_isolation_report.md:271,273` — the `replay_ratio=0.0` collateral-collapse figures
(+211.60% persona_a / +241.37% persona_c masked dialogue-val PPL) behind D-21's exclusion, against
Phase 14's +27.16% at `results/phase14_recall_report.md:462`.

`.planning/REQUIREMENTS.md:111-133` — ATK-01..ATK-06 and the status table at `:202-207`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Render the report and record what the gate returned</name>
  <files>results/phase18_extraction_report.md</files>
  <read_first>
    - `scripts/phase18_extraction.py` — `assemble_verdict`, `render_report`, `licensed_conclusion`,
      `assert_extraction_report_not_clobbered`
    - `results/phase18_arm_adapter-on.json` and `results/phase18_arm_adapter-off.json`
    - `results/phase17_isolation_report.md:265-280` — the collateral-collapse rows D-21 cites
    - `.planning/phases/18-black-box-adversarial-extraction-audit/18-CONTEXT.md` — D-02, D-21, D-24, D-27
  </read_first>
  <action>
    Run `.venv/bin/python scripts/phase18_extraction.py --report`. Record the verdict EXACTLY as
    returned — including INCONCLUSIVE, which is a legitimate pre-registered outcome and is recorded
    as-written, the way Phase 12 recorded λ*=None. Do not rewrite it, do not add a softening clause
    beside it, and do not run `--report` a second time to "pick up" a change: `render_report` rewrites
    the whole file and would destroy the recorded verdict.
    Verify on the RENDERED text before committing: no bare zero percentage; every proportion carries
    its denominator, its Wilson label and its `3/n` at zero successes; both the question-level and
    fact-level denominators appear together; the `core_taught` tier is present and explicitly not
    merged into the formal verdict; the D-18 realized-injection distribution is published per slot;
    the exposure table carries all three frames, both reductions and the per-slot length spread; the
    threats-to-validity section carries the ATK-06 LoRA caveat, both D-30 length confounds, A2's
    prefill-shape weakness and D-21's cross-persona exclusion with its collateral-collapse figures.
    Commit the report.
  </action>
  <acceptance_criteria>
    - `grep -cE "\b0(\.0+)?%" results/phase18_extraction_report.md` returns 0
    - `grep -c "INCONCLUSIVE\|NULL_ADMISSIBLE\|LEAKAGE_DEMONSTRATED" results/phase18_extraction_report.md`
      returns at least 1, and the `## Verdict` section holds the verdict and nothing else
    - `grep -c "lower bound on leakage" results/phase18_extraction_report.md` returns at least 1, with
      the LoRA caveat as an adjacent sentence
    - `grep -c "211.60\|241.37" results/phase18_extraction_report.md` returns at least 1 — D-21's
      exclusion is recorded with its measured rationale
    - `.venv/bin/pytest -q tests/test_phase16_prereg.py -k phase18 -x` passes over all five artifacts
    - `.venv/bin/pytest -q tests/test_phase18_docs.py` passes
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/pytest -q tests/test_phase18_docs.py tests/test_phase16_prereg.py</automated>
  </verify>
  <done>The report is committed with the verdict the committed gate returned.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human read of the recorded verdict</name>
  <what-built>
    `results/phase18_extraction_report.md` carries a verdict returned by `null_result_is_admissible()`
    and a conclusion emitted by `licensed_conclusion()` from the committed threat-model literals.
  </what-built>
  <how-to-verify>
    1. Open `results/phase18_extraction_report.md` and read the `## Verdict` section.
    2. Confirm the verdict string is one of the pre-registered `VERDICTS` members and matches the
       committed template verbatim — INCONCLUSIVE included.
    3. Confirm the reasons list explains which of the four D-27 conditions held and which did not.
    4. Confirm the closing paragraph claims no scope wider than `ATTACKER_HAS`/`ATTACKER_LACKS`, and
       that no sentence anywhere reads as an upper bound on privacy.
    5. Confirm no privacy claim in the report rests on a bound whose n is larger than 8 without the
       fact-level denominator beside it.
    6. Confirm the report contains neither "cannot" nor "impossible" inside any leakage claim.
  </how-to-verify>
  <read_first>
    - `results/phase18_extraction_report.md` — the whole file
    - `.planning/phases/18-black-box-adversarial-extraction-audit/18-CONTEXT.md` — D-24, D-27
  </read_first>
  <acceptance_criteria>
    - The verdict string and its reasons list are quoted verbatim into the SUMMARY
    - Each of the six checks above is recorded as confirmed or as an issue, individually
    - If any check fails, the correction lands as an `append_addendum` continuation — never a
      `--report` re-run, which would destroy the recorded verdict
  </acceptance_criteria>
  <files>results/phase18_extraction_report.md</files>
  <action>
    Present the `## Verdict` section and the closing paragraph for the six checks listed above, then
    stop for the read. Any correction lands through `append_addendum`, never a `--report` re-run.
  </action>
  <verify>
    <human-check>All six checks are individually recorded as confirmed or as issues, and the verdict string is quoted verbatim</human-check>
  </verify>
  <done>The recorded verdict has been read against the pre-registered template.</done>
  <resume-signal>Type "verdict confirmed" or list the failing checks</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Extend docs/REPORT.md with the measured results and mark the requirements</name>
  <files>docs/REPORT.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `docs/REPORT.md` — the v3.0 continuation landed by 18-12, and the two earlier dated markers
    - `results/phase18_extraction_report.md` — the numbers to quote
    - `.planning/REQUIREMENTS.md:105-135` and `:180-210` — ATK-01..ATK-06 and the status table
    - `.planning/phases/18-black-box-adversarial-extraction-audit/18-CONTEXT.md` — D-23
  </read_first>
  <action>
    Append the measured results to `docs/REPORT.md` through `append_addendum` — dated, additive, 0
    deletions, every prior line byte-identical including 18-12's claim-correction continuation. Quote
    the verdict, the headline rate with its denominator and both bounds, the tier split, and the
    ATK-06 caveat. Do not edit any prior sentence and do not renumber a section.
    Mark ATK-01 through ATK-06 complete in `.planning/REQUIREMENTS.md` — checkbox and status table —
    each with the artifact that evidences it. Mark a requirement complete only where this phase
    actually discharged it; if the verdict came back INCONCLUSIVE, say so against ATK-05 rather than
    marking a claim the evidence does not support, following Phase 17's recorded over-claim-avoidance
    pattern.
  </action>
  <acceptance_criteria>
    - `git diff --numstat docs/REPORT.md` shows 0 deletions
    - `.venv/bin/pytest -q tests/test_phase18_docs.py::test_continuation_is_additive -x` passes against
      the REAL artifact
    - `grep -c "^- \[x\] \*\*ATK-0" .planning/REQUIREMENTS.md` returns 6, and the status table rows for
      ATK-01..ATK-06 read Complete
    - `.venv/bin/pytest -q` full suite green and
      `.venv/bin/ruff check . && .venv/bin/ruff format --check .` clean
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/pytest -q</automated>
  </verify>
  <done>The published report carries the measured results additively and the requirement ledger matches the evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| returned verdict → published claim | The last place a null can be overstated; the defence is that the verdict is imported, not written |
| measured numbers → shipped documentation | An in-place edit of published text erases the record of what was claimed before |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-16-01 | Repudiation | An INCONCLUSIVE verdict softened into a privacy claim | mitigate | Verdict recorded as returned; blocking human read against the committed template; correction path is append-only |
| T-18-16-02 | Tampering | A second `--report` run destroys the recorded verdict and prior addenda | mitigate | Clobber guard armed the moment a non-PENDING verdict is recorded; `append_addendum` is the only post-verdict path |
| T-18-16-03 | Repudiation | A claim published at a denominator larger than the sample supports | mitigate | Both denominators in every row; the human read includes an explicit n>8 check |
| T-18-16-04 | Repudiation | A requirement marked complete that the evidence does not support | mitigate | Over-claim-avoidance: ATK-05 is marked against the actual verdict, INCONCLUSIVE included |
| T-18-16-05 | Tampering | `docs/REPORT.md` edited in place, erasing 18-12's continuation | mitigate | `append_addendum` with a byte-identical prefix proof; 0 deletions asserted |
| T-18-16-SC | Tampering | package installs | accept | Zero installs; `pyproject.toml` sha256-pinned |
</threat_model>

<verification>
- `.venv/bin/pytest -q` full suite green
- `.venv/bin/pytest -q tests/test_phase16_prereg.py -k phase18` green over all five `results/phase18_*`
  artifacts, with the `checked` value recorded
- `git diff --numstat docs/REPORT.md README.md` shows 0 in the deletions column
</verification>

<success_criteria>
The report is committed with the verdict its committed gate returned, the human read confirms it
against the pre-registered template, `docs/REPORT.md` is extended additively, and ATK-01..ATK-06 are
marked against real evidence.
</success_criteria>

<output>
Create `.planning/phases/18-black-box-adversarial-extraction-audit/18-16-SUMMARY.md` when done
</output>
