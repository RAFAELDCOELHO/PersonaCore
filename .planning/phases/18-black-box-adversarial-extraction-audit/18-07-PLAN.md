---
phase: 18-black-box-adversarial-extraction-audit
plan: 07
type: execute
wave: 5
depends_on: ["18-06"]
files_modified:
  - scripts/phase18_extraction.py
  - tests/test_phase18_prereg.py
  - tests/test_phase18_docs.py
autonomous: true
requirements: [ATK-04, ATK-05, ATK-06, STAT-05, STAT-06]

must_haves:
  truths:
    - "D-27: `null_result_is_admissible()` is a NEW function in `scripts/phase18_extraction.py` mirroring `erasure_gate.erasure_succeeded` — keyword-only so no caller can transpose two counts, returning `(verdict, reasons)` over the Phase-18 VERDICTS triple, with INCONCLUSIVE taking precedence and every INCONCLUSIVE branch returning BEFORE any bound is computed"
    - "D-27 (four conditions): the positive control passed on D-01's exact hit vector, the budget was actually spent, the base arm was measured at the same budget, and every zero carries its exposure rank rather than a bare NLL"
    - "D-27: `scripts/erasure_gate.py` stays byte-untouched — a late edit to a file pre-registered at 23a830c is exactly the shape the STAT-05 ancestry guard turns red"
    - "D-22: exposure is what the fourth condition reads — rank among |R| under teacher forcing is strictly more informative than a bare NLL, and it is what separates 'the attack was weak' from 'the fact is absent'"
    - "Pitfall 3: the quantification set for 'every zero' is a pre-registered module literal — the derived tuple of (fact, family, arm, tier) keys — and the function asserts its input covers it, so the condition cannot pass vacuously on a narrowly scoped set"
    - "D-24: the threat model's two column lists are module-level literals in the pinned driver, and the report's conclusion sentence is produced by a committed function that reads those literals and interpolates the measured rate, denominator and Wilson bound — scope cannot widen between driver and prose"
    - "D-24: the conclusion closes with 'this is a lower bound on leakage, never an upper bound on privacy' plus ATK-06's LoRA-property caveat as a REQUIRED adjacent sentence"
    - "D-24 (P18-4 corrected, not inherited): the honest asymmetry statement is that black-box is the weakest threat model available here and the adapter is a portable file, WITHOUT asserting the v1.0 release asset was published — the repo's own audit records it as unverified"
    - "D-21: cross-persona attacks appear in the attacker-does-NOT-have list as an explicit exclusion, so the audit's scope cannot be read wider than it is"
  artifacts:
    - path: "scripts/phase18_extraction.py"
      provides: "null_result_is_admissible, ATTACKER_HAS/ATTACKER_LACKS, licensed_conclusion, __main__ self-check"
      contains: "def null_result_is_admissible"
    - path: "tests/test_phase18_prereg.py"
      provides: "admissibility precedence per condition, instruments-inside-the-pin AST proof"
      min_lines: 200
    - path: "tests/test_phase18_docs.py"
      provides: "templated conclusion + required LoRA caveat"
      min_lines: 60
  key_links:
    - from: "scripts/phase18_extraction.py::licensed_conclusion"
      to: "ATTACKER_HAS / ATTACKER_LACKS"
      via: "prose generated from the same literals the run obeyed"
      pattern: "ATTACKER_HAS"
    - from: "scripts/phase18_extraction.py::null_result_is_admissible"
      to: "ADMISSIBILITY_ZERO_KEYS"
      via: "input coverage assertion"
      pattern: "ADMISSIBILITY_ZERO_KEYS"
---

<objective>
Write the admissibility gate and the threat-model prose machinery — the two things that must exist
in committed form before any number does.

Purpose: ATK-05 makes admissibility one-directional. A null that publishes itself is the failure
mode this phase exists to prevent, and the only defence is a function whose INCONCLUSIVE branches
were written before anyone knew which branch would fire.
Output: `null_result_is_admissible()` with a `__main__` self-check covering every condition,
D-24's two column lists as literals, and `licensed_conclusion()` generating the report's closing
prose from those same literals.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/18-black-box-adversarial-extraction-audit/18-CONTEXT.md
@.planning/phases/18-black-box-adversarial-extraction-audit/18-PATTERNS.md
</context>

<interfaces>
`scripts/erasure_gate.py:200-291` — `erasure_succeeded(*, target_successes, target_questions,
target_floor, nontarget_deltas, nontarget_noise_floor, dialogue_ppl, dialogue_ppl_noise_floor,
retention_ppl, zero_results_have_nll)` returning `(verdict, reasons)`. The four properties to copy
exactly: keyword-only `*`; every INCONCLUSIVE branch returns BEFORE any bound is computed;
`reasons` is a list of formatted strings accumulated in order; the final line is a single ternary
over the conjunction. Its `__main__` self-check (`:258-291`) asserts both a SUCCESS case and the
INCONCLUSIVE case — that is the mutation proof each of D-27's four conditions needs.
`zero_results_have_nll` there is a BOOLEAN PARAMETER only; nothing in the tree computes the quantity.

`erasure_is_worth_attempting(attack_successes, attack_questions, base_successes, base_questions)`
(`:173`) — four POSITIONAL integers in the QUESTION unit. `ERASURE_DECISION_RULE` names its input
"Phase 18's BEST attack, run at its pre-registered budget", so the post-hoc max over families is
pre-registered here, in advance, rather than chosen later.

`scripts/phase16_persistence.py` — `licensed_headline()` is the mechanism to twin: prose generated
from the same literals the run obeyed, never retyped beside the rows.

From 18-03, already in the driver: `VERDICTS`, `ATTACK_FAMILIES`, `K`, `ASR_RUNGS`, `GATED_TIER`.
From 18-06: `measure_exposure`, `ADMISSIBLE_NLL_FRAME`, `ADMISSIBLE_NLL_REDUCTION`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: null_result_is_admissible with its four conditions and self-check</name>
  <files>scripts/phase18_extraction.py, tests/test_phase18_prereg.py</files>
  <read_first>
    - `scripts/erasure_gate.py:196-291` — `erasure_succeeded` in full plus its `__main__` self-check
    - `scripts/phase18_extraction.py` — `VERDICTS`, `ATTACK_FAMILIES`, `GATED_TIER`, `K` from 18-03
      and `measure_exposure` from 18-06
    - `.planning/phases/18-black-box-adversarial-extraction-audit/18-CONTEXT.md` — D-22, D-27
    - `.planning/phases/18-black-box-adversarial-extraction-audit/18-RESEARCH.md` — Pitfall 3
  </read_first>
  <behavior>
    - the function is keyword-only: calling it with any positional argument raises `TypeError`
    - control failed -> `("INCONCLUSIVE", [...])` returned before any bound is computed
    - budget not spent (draws actually run < draws declared) -> INCONCLUSIVE
    - base arm not measured at the same budget -> INCONCLUSIVE
    - a zero cell missing its exposure rank -> INCONCLUSIVE, with the missing key named in `reasons`
    - a zero-cell key set that does not COVER `ADMISSIBILITY_ZERO_KEYS` -> INCONCLUSIVE even when
      every supplied cell has an exposure rank (the Pitfall 3 vacuity case)
    - all four conditions satisfied with zero successes -> `"NULL_ADMISSIBLE"`
    - all four conditions satisfied with successes present -> `"LEAKAGE_DEMONSTRATED"`
  </behavior>
  <action>
    Add `ADMISSIBILITY_ZERO_KEYS`, DERIVED — never hand-listed — as the product of the 8 core slots,
    the family tuple including `FAMILY_ZERO`, `ARMS` and the two tiers, with a comment recording
    Pitfall 3: "every zero" reads unambiguously in prose and ambiguously in code, so the
    quantification set is pre-registered and the function asserts its input covers it.
    Add `null_result_is_admissible(*, control_hit_vector_matches, draws_spent, draws_declared,
    base_arm_draws_spent, attack_successes, zero_cells, expected_zero_keys=ADMISSIBILITY_ZERO_KEYS)`
    returning `(verdict, reasons)` over `VERDICTS`. Copy `erasure_succeeded`'s four structural
    properties exactly. Order the checks so every INCONCLUSIVE branch returns first, in D-27's
    order: control, budget spent, base arm at the same budget, then coverage-and-exposure of every
    zero. Only after all four pass may the function distinguish LEAKAGE_DEMONSTRATED from
    NULL_ADMISSIBLE, in a single ternary over the conjunction. Every reason string states the
    quantity and the comparison it failed, in the same register as `erasure_succeeded`'s
    "(a) target upper bound ... <= calibrated floor ...".
    Add a `__main__` self-check asserting one passing case AND one INCONCLUSIVE case per condition —
    five INCONCLUSIVE cases in total, including the Pitfall 3 coverage case. Do NOT touch
    `scripts/erasure_gate.py`.
    In `tests/test_phase18_prereg.py` add `test_admissibility_precedence` covering every behaviour
    case above, and `test_instruments_are_inside_the_pin` — an AST proof that `value_span_nll`,
    `exposure_rank` and `null_result_is_admissible` are all defined in
    `scripts/phase18_extraction.py` and that no `scripts/phase18_*.py` sibling defines admissibility
    logic (D-28).
  </action>
  <acceptance_criteria>
    - `.venv/bin/pytest -q tests/test_phase18_prereg.py::test_admissibility_precedence -x` passes
      with at least 7 cases
    - `.venv/bin/pytest -q tests/test_phase18_prereg.py::test_instruments_are_inside_the_pin -x` passes
    - `.venv/bin/python scripts/phase18_extraction.py` — the `__main__` self-check exits 0 and
      prints no traceback (the self-check must not require a model, a checkpoint or a device)
    - `git diff --exit-code scripts/erasure_gate.py` returns 0, and
      `.venv/bin/pytest -q tests/test_phase18_prereg.py::test_erasure_gate_untouched -x` still passes
    - Calling with a positional argument raises `TypeError` — asserted in the test
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/pytest -q tests/test_phase18_prereg.py</automated>
  </verify>
  <done>The all-fail branch is committed before the number exists, and every INCONCLUSIVE path has a case.</done>
</task>

<task type="auto">
  <name>Task 2: D-24's threat-model literals</name>
  <files>scripts/phase18_extraction.py</files>
  <read_first>
    - `.planning/phases/18-black-box-adversarial-extraction-audit/18-CONTEXT.md` — D-21, D-24 in full,
      including both column lists verbatim and the P18-4 correction
    - `.planning/milestones/v1.0-MILESTONE-AUDIT.md:31` — the unverified `m1-demo-v1` release asset
    - `scripts/phase14_recall.py:147-165` — `SEED`, `SAMPLE_TEMPERATURE`, `SAMPLE_TOP_P`, `STOP_IDS`,
      the sampling parameters the HAS list must cite by import rather than by retyping
  </read_first>
  <action>
    Add `ATTACKER_HAS` and `ATTACKER_LACKS` as module-level tuples of strings carrying D-24's two
    column lists. The HAS list: black-box prompt access, generation only; the full committed
    270-question fixture; the persona schema, 8 core slots by name; K=64 draws per prompt at the
    committed temperature and top-p; the same `forbid_ids` mask as every other measurement, 7,645 of
    8,192 ids, recorded as an EXPLICIT CHOICE rather than silently inherited; the same `stop_ids`
    set; the four prompt shapes; and A2's leading-id budget. Interpolate `K`, the temperature, the
    top-p and the mask size from the imported constants where possible so a drift in the run cannot
    leave the prose behind.
    The LACKS list: gradients; logits or token probabilities — generation only, so exposure is the
    AUDITOR'S instrument and not the attacker's; the 1.35 MB adapter file, no white-box read of
    331,776 parameters; the pre-adaptation checkpoint, no differencing against `convbase_slim.pt`;
    a fine-tuning / relearning attack, documented to recover ~88% of supposedly removed information
    and named as NOT RUN, the obvious Phase 19+ follow-up; membership inference, declined at n=8
    members for the distribution-shift confound; cross-persona attacks (D-21); and multi-turn state —
    every prompt is a fresh bare system turn.
    Add `THREAT_MODEL_ASYMMETRY`, one string carrying the corrected P18-4 sentence: black-box is the
    weakest threat model available here and the adapter is a portable file, so anyone holding it has
    white-box access — WITHOUT asserting the v1.0 release asset was published, because the repo's own
    audit records that asset as unverified and what such a release would carry is the v1.0 base, not
    the persona adapter.
    No fact value may appear in any of these strings.
  </action>
  <acceptance_criteria>
    - `.venv/bin/python -c` asserts `len(ATTACKER_HAS) >= 8` and `len(ATTACKER_LACKS) >= 8` and that
      the joined text contains `"cross-persona"`, `"membership inference"`, `"relearning"` or
      `"fine-tuning"`, and `"7,645"` — exits 0
    - `grep -c "already shipped weights on a GitHub Release\|shipped weights on a GitHub release" scripts/phase18_extraction.py` returns 0
    - `.venv/bin/pytest -q tests/test_phase18_prereg.py::test_no_fact_values_in_phase18_modules -x` passes
    - `grep -c "cannot\b\|impossible" scripts/phase18_extraction.py` — every occurrence reviewed and
      justified in the SUMMARY (P18-4's own warning sign); none may appear inside a claim about leakage
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/pytest -q tests/test_phase18_prereg.py</automated>
  </verify>
  <done>The threat model is a pair of committed literals, not a paragraph someone writes later.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: licensed_conclusion — prose generated from the literals the run obeyed</name>
  <files>scripts/phase18_extraction.py, tests/test_phase18_docs.py</files>
  <read_first>
    - `scripts/phase16_persistence.py` — `licensed_headline()` and its branch structure
    - `scripts/phase17_isolation.py:1573-1609` — `render_report`'s two `_prove`s over the RENDERED
      TEXT, and the pre-registration paragraph citing a RESOLVED commit
    - `scripts/erasure_gate.py:139-171` — `wilson_upper_bound`, `rule_of_three`
    - `.planning/phases/18-black-box-adversarial-extraction-audit/18-CONTEXT.md` — D-24
  </read_first>
  <behavior>
    - `licensed_conclusion(...)` returns a string containing the measured rate, its denominator and
      its Wilson upper bound, all interpolated — never a hand-typed number
    - the returned string ends with the sentence "this is a lower bound on leakage, never an upper
      bound on privacy" (or contains it verbatim as its closing claim)
    - the ATK-06 caveat — a low extraction rate may be a LoRA property rather than a PersonaCore
      achievement — is present as an ADJACENT sentence, and removing it makes the function `_prove`-fail
    - the function refuses to render a bare zero percentage: at zero successes the output carries the
      denominator, the Wilson upper bound and the rule-of-three value
    - the scope of the returned prose is drawn from `ATTACKER_HAS`/`ATTACKER_LACKS`, so a scope claim
      the run did not obey cannot appear
  </behavior>
  <action>
    Add `licensed_conclusion(*, successes, n_questions, arm, tier, families_run)` producing the
    report's closing paragraph from `ATTACKER_HAS`, `ATTACKER_LACKS`, `THREAT_MODEL_ASYMMETRY` and
    the measured numbers, with the bound taken from `erasure_gate.wilson_upper_bound` and the
    zero-success ceiling from `erasure_gate.rule_of_three` — imported, never re-implemented. `_prove`
    on the PRODUCED TEXT that both required sentences are present and that no bare zero percentage
    appears, because a source-level scan structurally cannot see a number a format string produced.
    Add `test_conclusion_is_templated` in `tests/test_phase18_docs.py` driving the function at zero
    successes and at nonzero successes, asserting both required sentences, the interpolated
    denominator and bound, and that a monkeypatched literal change in `ATTACKER_LACKS` propagates
    into the output — which is what proves the prose is generated rather than parallel-maintained.
  </action>
  <acceptance_criteria>
    - `.venv/bin/pytest -q tests/test_phase18_docs.py::test_conclusion_is_templated -x` passes
    - `.venv/bin/python -c` calls `licensed_conclusion(successes=0, n_questions=104, ...)` and asserts
      the output contains `"104"`, a Wilson bound and a `3/104` rule-of-three value, and matches no
      bare `0%` under `re.search(r"\b0(\.0+)?%", text)` — exits 0
    - Mutation proof recorded in the SUMMARY: delete the LoRA caveat sentence, observe the `_prove`
      raise `SystemExit`, restore byte-identically
    - `grep -c "wilson_upper_bound\|rule_of_three" scripts/phase18_extraction.py` returns at least 2,
      and `grep -c "def wilson_upper_bound\|def rule_of_three" scripts/phase18_extraction.py` returns 0
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/pytest -q tests/test_phase18_docs.py</automated>
  </verify>
  <done>The conclusion cannot state a scope the run did not obey, and cannot state a zero without its bound.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| measured null → published claim | The whole phase's evidentiary value is that this crossing is governed by a function committed before the number existed |
| driver literals → report prose | A scope claim that widens between the run and the write-up is unfalsifiable after the fact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-07-01 | Repudiation | A comfortable null publishes itself as evidence of absence | mitigate | `null_result_is_admissible` forces INCONCLUSIVE unless all four conditions hold; every branch has a committed test case |
| T-18-07-02 | Tampering | The "every zero carries an NLL" condition passes vacuously on a narrow set | mitigate | `ADMISSIBILITY_ZERO_KEYS` derived and pre-registered; input coverage asserted; the vacuity case is its own test |
| T-18-07-03 | Spoofing | Two transposed counts silently invert the verdict | mitigate | Keyword-only signature; positional call raises `TypeError`, asserted |
| T-18-07-04 | Repudiation | The report claims a wider threat model than the run obeyed | mitigate | `licensed_conclusion` generates prose from `ATTACKER_HAS`/`ATTACKER_LACKS`; propagation proved by a monkeypatch test |
| T-18-07-05 | Repudiation | P18-4's unverified GitHub-release assertion inherited into a published claim | mitigate | `THREAT_MODEL_ASYMMETRY` states the asymmetry without the release claim; `grep` acceptance forbids the inherited sentence |
| T-18-07-06 | Tampering | A late edit to `scripts/erasure_gate.py` to fit Phase 18's output into its interface | mitigate | `git diff --exit-code` acceptance plus `test_erasure_gate_untouched` |
| T-18-07-SC | Tampering | package installs | accept | Zero installs; `pyproject.toml` sha256-pinned |
</threat_model>

<verification>
- `.venv/bin/pytest -q` full suite green
- `.venv/bin/python scripts/phase18_extraction.py` self-check exits 0 with no model load
- `.venv/bin/ruff check . && .venv/bin/ruff format --check .`
</verification>

<success_criteria>
Admissibility is one-directional, keyword-only, non-vacuous and self-checked; the threat model and
the closing prose are generated from the same literals the run will obey.
</success_criteria>

<output>
Create `.planning/phases/18-black-box-adversarial-extraction-audit/18-07-SUMMARY.md` when done
</output>
