---
phase: 21-the-privacy-unit-the-dp-data-path-and-the-n-64-corpus
plan: 06
type: execute
wave: 3
depends_on: ["21-04"]
files_modified:
  - src/personacore/training/data.py
  - tests/test_phase21_aligned_loader.py
autonomous: true
requirements: [UNIT-02]

must_haves:
  truths:
    - "get_batch_fact_aligned's output is a function of the fact bin's CURRENT on-disk contents, evaluated on EVERY access — proven by mutating the fact bin between two calls in one process"
    - "The loader RAISES, naming the fact bin, on a missing / truncated / rolled / interior-flipped fact bin"
    - "A negative control (unmutated) returns normally, and a token-bin mutation leaves fact attribution unchanged — proving WHICH file is being read"
    - "grad_accum_steps == n_facts is OBSERVED from the loop's actual micro-step count, not declared from a bin shape"
    - "One micro-step is one privacy record, so the ordinary backward hands back the per-record gradient directly and vmap leaves the critical path entirely (D-02)"
    - "D-03/D-04 are VERIFIED, not implemented: y[m == 0] = -100 plus the default reduction='mean' already makes the loss a MEAN over one fact's windows"
  artifacts:
    - path: "src/personacore/training/data.py"
      provides: "get_batch_fact_aligned — the fact-aligned sibling of get_batch_memmap_masked, three bins re-opened on every call"
      exports: ["get_batch_fact_aligned"]
    - path: "tests/test_phase21_aligned_loader.py"
      provides: "the N1-N6 mutate-between-calls adversarial set, fact_bin_required, grad_accum"
  key_links:
    - from: "src/personacore/training/data.py::get_batch_fact_aligned"
      to: "the *_fact.bin on disk"
      via: "np.memmap re-opened on every call, exactly as get_batch_memmap_masked does at :110-111"
      pattern: "memmap"
    - from: "get_batch_fact_aligned"
      to: "fact_window_impurities"
      via: "run-time purity check on the window it is about to hand back"
      pattern: "fact_window_impurities"
---

<objective>
Add `get_batch_fact_aligned` and prove the fact map is CONSUMED AT RUN TIME (D-06), not merely
asserted once at build time.

Purpose: D-06 is explicitly the claim that build time is not enough. A test that builds bins and
asserts the loader returns fact-aligned batches passes identically whether the loader reads
`*_fact.bin` or reconstructs the boundaries from its own copy of the padded lengths — one input
class, two implementations, no discrimination. The only instrument that separates them is a mutation
of the fact bin BETWEEN two calls in one process, with the other two bins proven byte-frozen.

Output: `get_batch_fact_aligned` in `src/personacore/training/data.py` and
`tests/test_phase21_aligned_loader.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-the-privacy-unit-the-dp-data-path-and-the-n-64-corpus/21-CONTEXT.md
@.planning/phases/21-the-privacy-unit-the-dp-data-path-and-the-n-64-corpus/21-VALIDATION.md
</context>

<interfaces>
<!-- Contracts the executor needs; do not go exploring. -->

`src/personacore/training/data.py:93-126` — `get_batch_memmap_masked`, the sibling to mirror:
- `:110-111` BOTH memmaps re-opened on EVERY call (documented at `:98-99` as the nanoGPT RSS-leak fix)
- `:112-116` raises `ValueError` on a length mismatch, naming BOTH paths and BOTH lengths (T-11-04)
- `:118-124` `x = data[i : i+B]`, `y = data[i+1 : i+1+B]`, `m = mask[i+1 : i+1+B]`
- `:125` `y[m == 0] = -100`
- `:126` `return x.to(device), y.to(device)`

From plan 21-04:
- `fact_window_impurities(fact_ids, block_size, *, space="both"|"input"|"target") -> list[int]`,
  raising on `(len(fact_ids) - 1) % block_size != 0`
- the aligned bins carry `n_windows * block_size + 1` elements each, all three 1:1
- padding slots carry the OWNING fact's index; there is no sentinel
- `scripts/teach_persona.py::fact_bin_path(bin_path)` derives the third bin's path
- `build_bins(..., align_facts=[(fact, episodes), ...])` — the aligned branch takes `(fact,
  episodes)` PAIRS, never bare `Fact` objects (shape pinned in plan 21-04 task 2)

MEASURED THIS SESSION (`21-RESEARCH.md` premise check P7), and the whole design of proof 1 hinges on
it: a freshly-opened `np.memmap(path, mode="r")` in the SAME process, after an on-disk rewrite,
returns the NEW bytes — a `uint16` element went `3 -> 999` across a re-open on Darwin 25.5.0 with
this venv's numpy. Assumption A1 records that this was measured on one platform only; the mitigation
is that the test asserts the mutation is VISIBLE (sha256 before/after) before asserting the loader's
behaviour, so a page-cache surprise fails with a message naming the cause instead of flaking.

D-02, measured on the real `GPT(ModelConfig())` + `inject_lora` (36 wrapped, 331,776 params), MPS,
sync-fenced: fact-aligned accumulation over RAGGED shards is 455.92 ms/step = 1.14x the batched
reference, versus 1.39x uniform-W accumulation and 1.35x vmap-over-uniform; `torch.stack` REFUSES a
ragged vmap batch outright (`[4,257]` entry 0 vs `[5,257]` entry 5). Per-fact gradient norms from the
accumulation loop and from vmap agree to 3.116e-08. **The cheap path is not the approximate path**,
and vmap is off the critical path entirely because one micro-step IS one privacy record.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: get_batch_fact_aligned — three bins, re-opened on every call, purity checked at draw time</name>
  <read_first>
    - src/personacore/training/data.py:93-126 — the sibling and its raise pattern
    - src/personacore/training/data.py — `fact_window_impurities` (plan 21-04)
    - scripts/teach_persona.py — `fact_bin_path` and the aligned branch of `build_bins` (plan 21-04)
    - .planning/phases/21-.../21-CONTEXT.md — D-01, D-03, D-04, D-05, D-06
  </read_first>
  <files>src/personacore/training/data.py</files>
  <action>
Add `get_batch_fact_aligned(bin_path, mask_path, fact_path, block_size, device, *, step, n_facts)`
beside `get_batch_memmap_masked`. It is a SIBLING, not a replacement — `get_batch_memmap_masked` is
byte-unchanged and remains the replay draw's path (D-10).

Semantics — deterministic, not random. `q = 1`, no subsampling:
  - the fact index for this micro-step is `step % n_facts`
  - the batch is EVERY window belonging to that fact, in bin order — 4 or 5 windows under D-01's
    ragged geometry, so the batch size VARIES by record and that is correct: it is what makes one
    micro-step one privacy record
  - `x`, `y`, `m` are sliced exactly as `get_batch_memmap_masked` does, so target-space semantics
    (`data.py:103-105`) are shared rather than re-derived
  - `y[m == 0] = -100`, unchanged

Open ALL THREE memmaps on EVERY call. Do not cache, do not hold a module-level handle. Record the two
reasons in the docstring: the nanoGPT RSS leak (`:98-99`), and D-06 — the fact map must be CONSUMED
at run time, so that "one window, one fact" is verifiable on every access instead of trusted from a
past construction. A cached handle would make the mutate-between-calls proof in task 2 impossible to
write, which is the same thing as making the property impossible to check.

Validation, all raising `ValueError` in the `:112-116` register — name every path and every length,
and make each failure DISTINGUISHABLE by its message:
  - fact bin missing -> raise naming `fact_path` (do not let `np.memmap`'s own `FileNotFoundError`
    carry the message; the caller needs to know WHICH of the three bins is absent)
  - the three lengths not equal -> raise naming all three paths and all three lengths
  - `(len(fact_ids) - 1) % block_size != 0` -> `fact_window_impurities` raises; let it, and do not
    wrap it into a generic message
  - the drawn window is impure -> raise naming the window index, the fact index expected, and the
    distinct ids observed. Check ONLY the windows being handed back, not the whole bin, so the
    per-call cost stays O(batch) rather than O(corpus). **Pass exactly the slice
    `fact_ids[start : start + k * block_size + 1]`** for the `k` windows being returned. The
    trailing `+1` is the LABEL-SHIFT TAIL and is REQUIRED, not optional: `fact_window_impurities`
    raises on `(len - 1) % block_size != 0` (plan 21-04), so a bare `k * block_size` slice would
    raise SPURIOUSLY on every valid draw — `(1024 - 1) % 256 == 255`. The tail is also exactly what
    the target-space half of the check reads. Do NOT relax the remainder assertion in the shared
    predicate to make a short slice fit: that assertion IS plan 21-04's A4 truncation adversary, and
    weakening it silently kills that guard for the packer and the tests as well as the loader.
  - `step`/`n_facts` inconsistent with the observed distinct fact-id count -> raise. This is the A5
    class from plan 21-04: content purity alone does not pin `n_facts`, so the loader must assert
    both.

Return `(x, y, fact_index)` — the third element is the run-time attribution the D-06 test needs and
that `grad_accum_steps = n_facts` needs in order to be meaningful rather than declared. Document that
returning the attribution is what makes the guarantee observable from outside the loader.

Docstring must also record, as VERIFICATION and not as new behaviour: within one fact the loss is a
MEAN over its windows (D-03) and this costs ZERO new loss code (D-04), because `y[m == 0] = -100`
here plus `F.cross_entropy`'s default `reduction="mean"` (`src/personacore/model/gpt.py:212`)
averages over non-ignored targets only, and with one fact per micro-step that IS
mean-over-the-record. Name the accepted cost: a 5-window fact's windows count 1/5 each and a
4-window fact's 1/4, which weights by PRIVACY RECORD rather than by how text packs into `block_size`.
  </action>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest -q tests/test_phase21_aligned_loader.py -k fact_bin_required` passes,
      matching `21-VALIDATION.md:72`
    - `.venv/bin/python -m pytest -q tests/test_masked_batch.py` still green —
      `get_batch_memmap_masked` is byte-unchanged (`git diff` shows only ADDED lines in `data.py`)
    - `.venv/bin/python -c "import inspect;from personacore.training.data import get_batch_fact_aligned as f;print(inspect.signature(f))"` shows keyword-only `step` and `n_facts`
    - CPU-only: the whole test file runs with no MPS/CUDA
    - `.venv/bin/python -m ruff check . && .venv/bin/python -m ruff format --check .`
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_masked_batch.py tests/test_phase21_aligned_bins.py</automated>
  </verify>
  <done>`get_batch_fact_aligned` exists, re-opens all three bins per call, returns the run-time fact attribution, and raises distinguishably on each malformed-input class.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The N1-N6 mutate-between-calls set — the only instrument that separates run time from build time</name>
  <read_first>
    - src/personacore/training/data.py — `get_batch_fact_aligned` (task 1)
    - .planning/phases/21-.../21-RESEARCH.md — `## §V.1`, the N1-N6 table and the executor-writable
      assertions
    - tests/test_phase21_aligned_bins.py — the tmp_path aligned-corpus builder to reuse, never re-write
  </read_first>
  <files>tests/test_phase21_aligned_loader.py</files>
  <behavior>
    Build a small aligned corpus under `tmp_path`, record `sha256` of all three bins, call the loader
    once, then mutate ONLY the fact bin and call again:
    - N1 fact bin DELETED           -> raises, message names the fact bin path
    - N2 fact bin TRUNCATED by 1    -> raises a length/remainder error naming all three lengths
    - N3 fact bin ROLLED by 1       -> raises impurity (length and multiset both unchanged, so only a
                                       positional read catches it)
    - N4 one interior element flipped to a neighbouring fact's id -> raises impurity on exactly one window
    - N5 fact bin UNMUTATED (negative control) -> returns normally, output equal to call 1
    - N6 TOKEN bin mutated, fact bin untouched -> fact attribution UNCHANGED (proves WHICH file is read)
    - In every case, `sha256` of the two non-mutated bins is asserted equal before and after
    - After each mutation, a byte-identical restore (`sha256` back to the recorded value) returns the
      loader to its call-1 output
  </behavior>
  <action>
Create `tests/test_phase21_aligned_loader.py`. CPU-only, GPU-free, no network. Everything under
`tmp_path`; nothing writes into `data/`.

`test_fact_map_is_consumed_at_runtime` is the load-bearing test. Structure it exactly as
`21-RESEARCH.md` §V.1's executor-writable block:
  1. record `original_fact_sha`, `tok_sha`, `mask_sha`
  2. `first = get_batch_fact_aligned(..., step=0, n_facts=n)`
  3. mutate ONE interior element of the fact bin on disk (`np.fromfile` -> edit -> `tofile`)
  4. assert `_sha(tokens) == tok_sha` and `_sha(mask) == mask_sha` — ONLY the fact bin moved. Put
     this BEFORE the behavioural assertion so a page-cache surprise (assumption A1) fails with a
     message naming the cause rather than flaking.
  5. assert `_sha(fact) != original_fact_sha` — the mutation is VISIBLE on disk
  6. `pytest.raises(ValueError)` on the second call, and assert `"fact"` appears in the message
     lower-cased — name the failing bin, never "something raised"
  7. restore the element, `tofile`, assert `_sha(fact) == original_fact_sha`, and assert the loader's
     output equals `first`. The restore is proven by SHA of the FILE, not by "we put the value back."

Parametrize N1-N4 over the mutation and the expected failure class, so each red is distinguished by
WHICH assertion fired rather than by "an exception occurred".

N5 and N6 are the two that stop this collapsing into "assert it raises". N5 is the standard negative
control — without it, a loader that always raised would pass N1-N4. N6 is the one that is easy to
omit and the one that proves the guard reads the FACT bin and not the token bin: mutate a token far
from any boundary, call again, and assert the returned `fact_index` is unchanged while `x` differs.

`test_grad_accum_steps_equals_n_facts` — run `n_facts` consecutive micro-steps, collect the returned
`fact_index` values, and assert they are exactly `range(n_facts)` with no repeat and no gap, and that
step `n_facts` wraps to 0. This is the OBSERVED count from the loop, not a declaration derived from a
bin shape. Assert the union of windows attributed across one lot equals every window in the bin
exactly once — the conservation law at the loader tier.

DELIBERATE-RED for this guard, run and recorded: in `get_batch_fact_aligned`, delete the
`np.memmap(fact_path, ...)` line and derive the fact boundaries from a `np.cumsum` of the padded
lengths instead. `test_fact_map_is_consumed_at_runtime` must return `first == second` -> RED, and the
RED means exactly "the map is not consumed". Restore with
`git checkout src/personacore/training/data.py`; `shasum -a 256` equals the pre-mutation value and
`git diff --exit-code` returns 0.
  </action>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest -q tests/test_phase21_aligned_loader.py -k consumed_at_runtime` passes,
      matching `21-VALIDATION.md:71`
    - `.venv/bin/python -m pytest -q tests/test_phase21_aligned_loader.py -k grad_accum` passes,
      matching `21-VALIDATION.md:70`
    - N1-N6 all present and passing; the N5 negative control and the N6 wrong-file control are named
      in the SUMMARY as separate observations
    - The deliberate-RED was OBSERVED (`first == second`) and its message recorded; restore is sha256-equal
    - `git status --porcelain data/` is EMPTY
    - `.venv/bin/python -m pytest -q` full suite green — `877 passed, 1 skipped` plus this phase's new
      tests. The one skip (`test_loop_penalty_fn::test_golden_trajectory_bit_identity`, platform-gated)
      is EXPECTED and a run reporting it is GREEN.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_phase21_aligned_loader.py tests/test_phase21_aligned_bins.py tests/test_masked_batch.py</automated>
  </verify>
  <done>All six mutation classes are committed and passing, the run-time consumption claim has been watched failing on a cumsum-derived implementation, and one lot's micro-steps observably cover every window exactly once.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build artifact -> training loop | The three bins are untrusted input. ASVS V5 applies narrowly here and `data.py:112-116` is the existing register: raise loudly, name every path and every length. |
| a past construction -> a present guarantee | "The bin was correct when it was built" and "the bin is correct on this access" are different claims; only the second is what an epsilon rests on. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-02 | Repudiation | a silently-dropped fact bin makes `grad_accum_steps != n_facts` and the published epsilon bounds nothing | mitigate | the mutate-between-calls proof (N1-N4) plus the deliberate-RED that replaces the memmap read with a `cumsum` and is OBSERVED returning `first == second` |
| T-21-12 | Denial of Service | a truncated / missing / mis-length fact bin reaches the loop | mitigate | N1 and N2 raise `ValueError` naming the fact bin path and all three lengths, in the `:112-116` register |
| T-21-20 | Tampering | correct offsets over wrong bytes (roll-by-1) | mitigate | N3 — length and `bincount` multiset both unchanged, caught only by the positional read |
| T-21-27 | Spoofing | the guard reads the TOKEN bin and reports fact attribution from it | mitigate | N6 — mutate a token, assert `fact_index` is unchanged while `x` differs |
| T-21-28 | Repudiation | a loader that always raises passes every adversarial case | mitigate | N5, the unmutated negative control, asserted returning normally and equal to call 1 |
| T-21-29 | Repudiation | platform-dependent memmap coherence makes the whole proof flaky rather than false | mitigate | the mutation's visibility is asserted by file sha256 BEFORE the behavioural assertion, so assumption A1 fails with a message naming the cause |
| T-21-24 | Tampering | a test writes into `data/` | mitigate | everything under `tmp_path`; `git status --porcelain data/` asserted empty |
| T-21-11 | Tampering | supply chain | accept | zero package installs; `tests/test_package.py:37` |
</threat_model>

<verification>
```bash
.venv/bin/python -m pytest -q tests/test_phase21_aligned_loader.py tests/test_phase21_aligned_bins.py tests/test_masked_batch.py tests/test_phase14_teaching.py
.venv/bin/python -m pytest -q                 # full suite; 877 passed + 1 expected skip + phase 21
git status --porcelain data/                  # MUST be empty
git ls-files 'results/phase21_*'              # MUST still be empty
git diff --exit-code scripts/mitigation_gate.py scripts/mitigation_unit.py scripts/phase18_extraction.py
.venv/bin/python -m ruff check . && .venv/bin/python -m ruff format --check .
```
</verification>

<success_criteria>
- `get_batch_fact_aligned` re-opens all three bins on every call and returns the run-time fact attribution
- N1-N6 all committed and passing, each red distinguished by which assertion fired
- `grad_accum_steps == n_facts` observed from the micro-step sequence, and one lot covers every window exactly once
- The cumsum deliberate-RED observed and restored sha256-equal
- `get_batch_memmap_masked` byte-unchanged (`git diff` shows only additions in `data.py`)
</success_criteria>

<output>
Create `.planning/phases/21-the-privacy-unit-the-dp-data-path-and-the-n-64-corpus/21-06-SUMMARY.md` when done.
Record: the N1-N6 outcomes with the exact raise message for each, the observed `fact_index` sequence
over one lot, the deliberate-RED observation, and whether the memmap-coherence assumption (A1) held
on this platform.
</output>
