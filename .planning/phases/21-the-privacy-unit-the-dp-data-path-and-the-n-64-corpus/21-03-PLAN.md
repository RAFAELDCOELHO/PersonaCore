---
phase: 21-the-privacy-unit-the-dp-data-path-and-the-n-64-corpus
plan: 03
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - tests/test_phase20_prereg.py
autonomous: true
requirements: [UNIT-01, UNIT-04, UNIT-05]

must_haves:
  truths:
    - "The results/phase21_* prefix has been OBSERVED matching a committed path, in a throwaway repository, rather than inferred from the pattern's text"
    - "The phase21 ordering guard has been watched RED at state 1 (no pin), RED at state 2 (pin second), GREEN at state 3 (no artifact), RED again at state 4 (delete-and-re-add), and GREEN at state 5 (artifact strictly after the pin)"
    - "Laundering is proven impossible across a real delete-and-re-add cycle: adds[-1] is byte-identical to the SHA state 1 recorded"
    - "The fixture calls the SAME _assert_ordering_holds the live guard calls — no lookalike copy"
    - "The real repository's git history is untouched: git log --diff-filter=A -- 'results/phase21_*' is EMPTY before and after"
  artifacts:
    - path: "tests/test_phase20_prereg.py"
      provides: "test_phase21_glob_sees_the_phase21_prefix_red_then_green — five states in a tmp_path repo"
      contains: "phase21_probe"
  key_links:
    - from: "the fixture"
      to: "_assert_ordering_holds"
      via: "root=tmp_path, the same function object the live guard calls"
      pattern: "_assert_ordering_holds\\("
    - from: "state 2"
      to: "git ls-files results/phase21_*"
      via: "a positive observation of the prefix matching, asserted BEFORE the ordering failure"
      pattern: "ls-files"
---

<objective>
Prove the `results/phase21_*` ancestry guard is LIVE, not vacuous, by driving it through five states
in a throwaway repository.

Purpose: an ordering guard armed before any artifact exists is green having compared nothing —
`checked == len(prereg_commits) * 0 == 0`. Reading a glob pattern and confirming it bites are
different acts. `21-RESEARCH.md` §V.3 measured that the Phase 20 live guard is no longer vacuous
(9 pin commits x 3 tracked artifacts = 27 checked pairs), so the equivalence assertion at `:178` has
already carried this exact transition once in production. Phase 21's guard will follow the same
trajectory the moment plan 21-11 lands the first artifact — and this fixture is what proves the
prefix bites BEFORE that happens, when it is still cheap to be wrong.

Output: `test_phase21_glob_sees_the_phase21_prefix_red_then_green` in `tests/test_phase20_prereg.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-the-privacy-unit-the-dp-data-path-and-the-n-64-corpus/21-CONTEXT.md
@.planning/phases/21-the-privacy-unit-the-dp-data-path-and-the-n-64-corpus/21-VALIDATION.md
</context>

<interfaces>
<!-- The fixture to copy, line by line. -->

`tests/test_phase20_prereg.py:281-441` — `test_phase20_glob_sees_the_phase20_prefix_red_then_green`.
Five states, each RED distinguished from the others by WHICH assertion fired:

| State | Setup | Expected | The assertion that makes it non-vacuous |
|---|---|---|---|
| 1 | commit `results/phase21_probe.json`, NO pin | `AssertionError` | `PHASE21_PREREG_ARTIFACT in str(excinfo.value)` — the expected red is the `assert prereg_commits` branch at `:144`, NOT "something raised" |
| 2 | commit the pin SECOND (the forbidden order) | `subprocess.CalledProcessError` | `_git("ls-files","results/phase21_*",cwd=tmp) == ["results/phase21_probe.json"]` asserted FIRST — the positive observation of the prefix; then `tuple(exc.value.cmd[:3]) == ("git","merge-base","--is-ancestor")`, else any git failure anywhere satisfies `raises` |
| 3 | `git rm` the probe | GREEN at `tracked = 0` | the red is reversible ONLY by having no artifact |
| 4 | re-add at the IDENTICAL path | `CalledProcessError` again | `len(adds) == 2` and `adds[-1] == state1_add` — laundering is impossible, measured across a real cycle |
| 5 | remove the probe; commit a real-shaped artifact AFTER the pin | GREEN, `checked` non-zero | the GREEN half of RED-then-GREEN |

Known gotchas already handled at `:387-389`: `git rm` of the last file in `results/` removes the
directory from the working tree, so the re-add needs `probe.parent.mkdir(exist_ok=True)`.
Git identity is set as LOCAL repo config (`:317-318`) so the fixture works on CI runners with no
global `user.email`.

`tests/test_phase20_prereg.py:105` — `_git(*args, cwd=_ROOT)`, keyword-only `cwd`. The argv tuple
goes to `subprocess` directly; `shell=True` is never used, so a glob containing a shell metacharacter
reaches git as a pathspec instead of being expanded.

From plan 21-01: `PHASE21_PREREG_ARTIFACT = "scripts/mitigation_unit.py"`, and
`V4_ARTIFACT_GLOBS = ("results/phase20_*", "results/phase21_*")`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: The five-state RED-then-GREEN fixture for the phase21 prefix</name>
  <read_first>
    - tests/test_phase20_prereg.py:281-441 — the fixture to copy VERBATIM with `phase20_` -> `phase21_`
      and `PHASE20_PREREG_ARTIFACT` -> `PHASE21_PREREG_ARTIFACT`
    - tests/test_phase20_prereg.py:105-183 — `_git` and `_assert_ordering_holds`
    - .planning/phases/21-.../21-CONTEXT.md — D-20, and its reflexivity note
  </read_first>
  <files>tests/test_phase20_prereg.py</files>
  <action>
Add `test_phase21_glob_sees_the_phase21_prefix_red_then_green(tmp_path)` immediately after the
existing Phase 20 fixture. This is a COPY of `:281-441`, not a new design — `21-RESEARCH.md`'s
Don't-Hand-Roll table names it explicitly: copy the fixture, call the existing
`_assert_ordering_holds`.

Substitutions and nothing else:
  - probe path `results/phase20_probe.json` -> `results/phase21_probe.json`
  - state-5 artifact `results/phase20_retention_floor.json` -> `results/phase21_privacy_unit.json`
    (the real filename plan 21-11 will commit — resolve it from that plan, not from a guess)
  - glob `"results/phase20_*"` -> `"results/phase21_*"`
  - `PHASE20_PREREG_ARTIFACT` -> `PHASE21_PREREG_ARTIFACT`
  - commit messages `state N: ...` keep their shape with `phase21` naming
  - git identity `phase20-fixture` -> `phase21-fixture`

Keep every assertion. In particular do NOT drop:
  - state 1's `assert PHASE21_PREREG_ARTIFACT in str(no_pin.value)` — distinguishing "the
    pre-registration does not exist" from "the ordering is violated" is the point
  - state 2's `ls-files` assertion BEFORE the `raises` — this is the only positive observation that
    the prefix matched, and without it a mutation swapping `artifact_glob` to `"results/phase20_*"`
    would be INVISIBLE (both are in `V4_ARTIFACT_GLOBS` after plan 21-01)
  - state 2's and state 4's `tuple(exc.value.cmd[:3]) == ("git","merge-base","--is-ancestor")`
  - state 4's `len(adds) == 2` and `adds[-1] == state1_add`

Docstring must carry forward, in the fixture's own words:
  - the reflexivity note measured on this repository: `git merge-base --is-ancestor X X` exits 0, so
    pin and artifact in the SAME commit PASS — "strictly after" is a discipline TIGHTER than the
    mechanism enforces, written down so a later reader reads it as neither a defect nor a licence;
  - that everything lives under `tmp_path` and the real repository's history is never touched,
    because a `phase21_`-named probe reaching the real history would PERMANENTLY corrupt the evidence
    this phase exists to produce (`:157` `adds[-1]` makes it unlaunderable);
  - that this fixture runs against the SAME `_assert_ordering_holds` the live guard calls,
    parameterized on `root` — a lookalike copy would prove something about a different function.
  </action>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest -q tests/test_phase20_prereg.py -k phase21_glob_red_then_green` passes,
      matching `21-VALIDATION.md:81`
    - `.venv/bin/python -m pytest -q tests/test_phase20_prereg.py` — all pass
    - `git log --diff-filter=A --format=%H -- 'results/phase21_*' | wc -l` returns 0 BEFORE and AFTER
      the test run (run it twice, record both)
    - `grep -c 'cwd=_ROOT' <the new test body>` is 0 — Pitfall 6's warning sign; every `_git` call
      inside the fixture passes `cwd=tmp_path`
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_phase20_prereg.py -k phase21</automated>
  </verify>
  <done>All five states observed; state 2's `ls-files` assertion proves the prefix matched; state 4 proves laundering impossible; the real history is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Watch the fixture fail on the two named mutations, then restore byte-identically</name>
  <read_first>
    - tests/test_phase20_prereg.py (the fixture from task 1)
    - .planning/phases/21-.../21-RESEARCH.md — the `Deliberate-RED for this guard` block in `## §V.3`
  </read_first>
  <files>tests/test_phase20_prereg.py</files>
  <action>
A guard nobody has watched fail is a guard nobody has verified. Run BOTH mutations named in
`21-RESEARCH.md` §V.3, in this order, and record the observed failure text in the SUMMARY.

Record `shasum -a 256 tests/test_phase20_prereg.py` BEFORE touching anything.

MUTATION A — the wrong-prefix mutation. In the NEW fixture only, change `artifact_glob` from
`"results/phase21_*"` to `"results/phase20_*"` at every call site inside that test. Expected: the
test FAILS at state 2's `ls-files` assertion, because `git ls-files "results/phase20_*"` matches
nothing in the throwaway repo. This is the exact demonstration that the state-2 `ls-files` assertion
is mandatory: WITHOUT it the mutation would be invisible, since both prefixes are in
`V4_ARTIFACT_GLOBS`. Record the failure message.

MUTATION B — the both-halves mutation, re-run at the fixture tier. Revert `V4_ARTIFACT_GLOBS` to
`("results/phase20_*",)` while keeping the new fixture. Expected: the fixture FAILS at `:129` with
the "watching two different sets of paths" message, on the FIRST `_assert_ordering_holds` call
(state 1). Record the failure message. Plan 21-01 ran this mutation against the LIVE guard; running
it again here proves both tiers depend on the glob addition, which is what "both halves are
load-bearing" means when stated as evidence rather than as prose.

After each mutation, restore with `git checkout tests/test_phase20_prereg.py` and prove the restore:
`shasum -a 256 tests/test_phase20_prereg.py` equals the pre-mutation value AND
`git diff --exit-code tests/test_phase20_prereg.py` returns 0. Neither mutation may be committed.

DO NOT commit any mutation and DO NOT create any `results/phase21_*` path in the real repository.
  </action>
  <acceptance_criteria>
    - Mutation A observed FAILING at state 2's `ls-files` assertion; failure text recorded in the SUMMARY
    - Mutation B observed FAILING at `:129` with the "watching two different sets of paths" message;
      failure text recorded in the SUMMARY
    - After each restore: `shasum -a 256 tests/test_phase20_prereg.py` equals the recorded
      pre-mutation value AND `git diff --exit-code tests/test_phase20_prereg.py` returns 0
    - `.venv/bin/python -m pytest -q tests/test_phase20_prereg.py` green after both restores
    - `git log --diff-filter=A --format=%H -- 'results/phase21_*' | wc -l` still returns 0
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_phase20_prereg.py tests/test_phase21_unit_pin.py tests/test_package.py && git diff --exit-code tests/test_phase20_prereg.py</automated>
  </verify>
  <done>Both mutations have been observed RED with their exact messages recorded, and both restores are sha256-equal to the pre-mutation file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| throwaway repo -> real repo | The fixture creates and commits `results/phase21_*` paths. If any of that reaches the real repository's history, `adds[-1]` makes the corruption permanent and unrecoverable. |
| a glob pattern's TEXT -> the paths it actually matches | Reading a pattern and confirming it bites are different acts; only a positive `ls-files` observation closes the gap. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-07 | Tampering | a `phase21_`-named probe reaching the real git history | mitigate | every `_git` call inside the fixture passes `cwd=tmp_path`; `grep -c 'cwd=_ROOT'` in the new test body is 0; `git log --diff-filter=A -- 'results/phase21_*'` asserted EMPTY before and after |
| T-21-06 | Tampering | ancestry laundering via delete-and-re-add | mitigate | state 4 asserts `len(adds) == 2` AND `adds[-1] == state1_add` across a real cycle |
| T-21-05 | Repudiation | the fixture guards the wrong prefix and nobody can tell | mitigate | Mutation A: swap to `results/phase20_*` and OBSERVE state 2's `ls-files` assertion firing |
| T-21-03 | Tampering | the glob addition silently reverted, leaving the fixture green | mitigate | Mutation B: revert `V4_ARTIFACT_GLOBS` and OBSERVE `:129` firing at both tiers |
| T-21-18 | Spoofing | a `CalledProcessError` from an unrelated git call satisfies `pytest.raises` | mitigate | `tuple(exc.value.cmd[:3]) == ("git","merge-base","--is-ancestor")` at states 2 and 4 |
| T-21-19 | Injection | a glob containing a shell metacharacter is expanded by a shell | mitigate | `_git` passes an argv tuple to `subprocess`; `shell=True` appears nowhere in the module (`:113-119` records why) |
| T-21-11 | Tampering | supply chain | accept | zero package installs; `tests/test_package.py:37` |
</threat_model>

<verification>
```bash
.venv/bin/python -m pytest -q tests/test_phase20_prereg.py tests/test_phase21_unit_pin.py tests/test_package.py
git log --diff-filter=A --format=%H -- 'results/phase21_*' | wc -l   # MUST be 0
git ls-files 'results/phase21_*'                                     # MUST be empty
git diff --exit-code scripts/mitigation_gate.py scripts/mitigation_unit.py scripts/phase18_extraction.py
.venv/bin/python -m ruff check . && .venv/bin/python -m ruff format --check .
```
</verification>

<success_criteria>
- `test_phase21_glob_sees_the_phase21_prefix_red_then_green` committed and green
- Five states each assert a DISTINCT observable, copied from `:281-441` without weakening
- Two named mutations observed RED with their messages recorded; both restores sha256-equal
- `git ls-files 'results/phase21_*'` still empty; `scripts/mitigation_unit.py` byte-unchanged
</success_criteria>

<output>
Create `.planning/phases/21-the-privacy-unit-the-dp-data-path-and-the-n-64-corpus/21-03-SUMMARY.md` when done.
Record: the exact failure text of Mutation A and Mutation B, the pre/post sha256 of
`tests/test_phase20_prereg.py`, and the `checked` count observed at state 5.
</output>
