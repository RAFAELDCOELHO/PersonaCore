---
phase: 21-the-privacy-unit-the-dp-data-path-and-the-n-64-corpus
plan: 11
type: execute
wave: 5
depends_on: ["21-01", "21-03", "21-09", "21-10"]
files_modified:
  - scripts/phase21_unit_record.py
  - tests/test_phase21_unit_record.py
  - tests/test_phase20_prereg.py
  - results/phase21_privacy_unit.json
  - results/phase21_multiplicity.json
autonomous: false
requirements: [UNIT-01, UNIT-03, UNIT-04, UNIT-05, UNIT-06]

must_haves:
  truths:
    - "results/phase21_privacy_unit.json records PRIVACY_UNIT with its arithmetic, the replay-outside-N decision with its epsilon consequence, and delta = 1e-5 with the rejected 1/N^1.1 recipe's self-contradiction at N=8 AND N=64 (SC1, SC4)"
    - "results/phase21_multiplicity.json records BOTH paths MEASURED on an instrumented loader at SEED=1337 / MAX_STEPS=200 / BATCH_SIZE=8, every row labelled with its exact bin composition and carrying its own denominator (SC3, D-26)"
    - "The n=64 corpus geometry is OBSERVED and recorded, not hard-coded — A3's assumption is discharged by measurement"
    - "Both artifacts are COMMITTED, in a commit that is a strict descendant of every commit touching scripts/mitigation_unit.py"
    - "The ancestry guard is now LIVE rather than vacuous: checked == len(pin_commits) * len(tracked_artifacts) and both sides are non-zero"
    - "No epsilon is computed anywhere in this phase — the artifact is a UNIT and the path that makes it real"
  artifacts:
    - path: "results/phase21_privacy_unit.json"
      provides: "SC1 + SC4's committed record"
      contains: "privacy_unit"
    - path: "results/phase21_multiplicity.json"
      provides: "SC3's three labelled measured rows plus the observed corpus geometry at both capacities"
      contains: "bin_composition"
    - path: "tests/test_phase21_unit_record.py"
      provides: "the artifact schema guards, the refuse-to-rerun check, and the reproduce-from-the-pin check"
  key_links:
    - from: "scripts/phase21_unit_record.py"
      to: "scripts/mitigation_unit.py"
      via: "an import of the frozen constants — the driver never retypes a pinned value"
      pattern: "mitigation_unit"
    - from: "results/phase21_*"
      to: "tests/test_phase20_prereg.py::test_phase21_prereg_is_frozen_before_every_phase21_result"
      via: "git ls-files results/phase21_* now matching, making checked non-zero"
      pattern: "results/phase21_"
---

<objective>
Write and COMMIT the two `results/phase21_*` artifacts, in that order and no other, and observe the
ancestry guard turn from vacuous to live.

Purpose: this is the irrevocable step. `git ls-files` is the guard's input, so an artifact becomes
WATCHED at the moment it is COMMITTED, not when it is written, and `tests/test_phase20_prereg.py:157`
(`adds[-1]`, the EARLIEST add) makes a wrong order permanent — `git rm` plus a re-add at the same path
cannot launder it, as state 4 of plan 21-03's fixture measured on a real cycle. From this commit
onward `scripts/mitigation_unit.py` is frozen and every correction is a dated continuation via
`scripts/_addendum.py`.

Output: `results/phase21_privacy_unit.json`, `results/phase21_multiplicity.json`, their schema
guards, and the removal of plan 21-01's now-superseded no-artifact-yet assertion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-the-privacy-unit-the-dp-data-path-and-the-n-64-corpus/21-CONTEXT.md
@.planning/phases/21-the-privacy-unit-the-dp-data-path-and-the-n-64-corpus/21-VALIDATION.md
</context>

<interfaces>
<!-- Contracts the executor needs; do not go exploring. -->

From plan 21-01, `scripts/mitigation_unit.py` (FROZEN from this plan's first commit onward):
`PRIVACY_UNIT`, `SAMPLING_RATE_Q`, `privacy_n(n_facts)`, `DELTA`, `DELTA_TIMES_N_CEILING`,
`REJECTED_DELTA_RECIPE`, `rejected_delta(n)`. IMPORT them; never retype a pinned value into the
driver, or the artifact and the pin become two sources that can disagree.

From plan 21-10, `scripts/phase21_unit_record.py`: `ATTRIBUTION_RULE`, `BIN_COMPOSITION_LABELS`,
`ARTIFACTS`, `count_unaligned`, `count_aligned`.

From plan 21-09: `arm_spec('dp_n8')` -> 8 facts, `arm_spec('dp_n64')` -> 64 facts, both at
`replay_ratio = 0.0`.

From plan 21-08: `replay_window_budget(n_facts)` -> `4 * n_facts * 256`.

From plan 21-04: `build_bins(..., align_facts=...)`, `fact_bin_path(bin_path)`.

`tests/test_phase20_prereg.py:121-183` — `_assert_ordering_holds`. `:166` asserts
`checked == len(prereg_commits) * len(tracked_artifacts)`; `:178` asserts
`bool(checked) == bool(tracked_artifacts)` — THE assertion that stops the vacuity surviving the
artifacts' arrival. Measured for Phase 20 during research: 9 pin commits x 3 tracked artifacts =
`checked = 27`, so this exact transition has already been carried once in production on this repo.

MEASURED, and the artifact must reproduce these from the pin rather than restating them:
`rejected_delta(8) * 8 = 0.8122523963562354` (81.2x the 0.01 ceiling);
`rejected_delta(64) * 64 = 0.6597539553864469` (66.0x);
`DELTA * 8 = 8e-05` (passes by 125.0x); `DELTA * 64 = 0.00064` (passes by 15.625x).

D-26's ANALYTIC expectations — for the artifact's `analytic_expectation` field, LABELLED as analytic
beside the measured value, never in place of it: replay-in-bin @1.0 (15,162 tok) 129.2; facts-only
(D-10) (7,581 tok) 262.9; fact-aligned 1 per step, deterministic.

Assumption A3 from `21-RESEARCH.md`: the 56 filler facts were assumed to render ~22 rows / ~4 windows
each, giving n=64 ~= 264 windows. That was `[ASSUMED — depends on values not yet minted]`. This plan
DISCHARGES it by MEASURING the actual total and recording the observed number. Do not hard-code 264.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write results/phase21_privacy_unit.json from the pin, and its schema guard</name>
  <read_first>
    - scripts/mitigation_unit.py (plan 21-01) — the constants to IMPORT
    - scripts/phase21_unit_record.py (plan 21-10) — `ARTIFACTS`, `refuse_if_exists`
    - scripts/teach_persona.py:236-244 — `refuse_if_exists`
    - .planning/phases/21-.../21-CONTEXT.md — D-07, D-08, D-23, D-24, D-25
  </read_first>
  <files>scripts/phase21_unit_record.py, tests/test_phase21_unit_record.py</files>
  <action>
Extend `scripts/phase21_unit_record.py` with the emitter for `results/phase21_privacy_unit.json`.
Every pinned value is IMPORTED from `mitigation_unit`, never retyped. `json` is available here
because this driver lives OUTSIDE the `scripts/mitigation_*.py` glob (D-22) — record that in the
docstring so a future reader does not "tidy" the two files together.

Schema, three top-level blocks matching SC1 and SC4:

`unit` (SC1 / UNIT-01):
  `privacy_unit` (from the pin), `rationale` — why an example-level epsilon bounds nothing about a
  fact: `get_batch_memmap_masked` draws `block_size` windows WITH REPLACEMENT over a flat
  concatenated bin (`src/personacore/training/data.py:117`) — and a pointer to the MEASURED
  multiplicity in the sibling artifact rather than a number restated here.

`lot` (SC4 / UNIT-04):
  `sampling_rate_q` (from the pin), `privacy_n_rule` `"N = n_facts"`, `replay_in_lot` `true`,
  `replay_inside_privacy_n` `false`, and `epsilon_consequence` stating D-07 in full: counting replay
  inside N would shrink q and produce a flatteringly small epsilon that does not reflect what the
  mechanism protects — the exact trap UNIT-04 names. Include D-08's counterweight so the record is
  not one-sided: replay is NOT dropped from the DP arms, because losing dialogue capability risks
  condition (c) and `f_C = 0.5` sits only 2.24x above the measured non-vacuity floor 0.2237.
  Include the D-24/D-25 replay-volume record: `replay_windows_per_fact` 4,
  `replay_tokens_per_fact` 1,024, `replay_tokens_at_n8` 8,192, `replay_tokens_at_n64` 65,536, the
  observed share of the padded bin at both capacities, `separate_pass_per_lot` `true`, and the
  rejected raw constant `947.625` with the reason `"7581 / 8 — read off private token lengths"`.

`delta` (SC4 / UNIT-05):
  `delta` (from the pin), `ceiling` (`DELTA_TIMES_N_CEILING`), `rejected_recipe`
  (`REJECTED_DELTA_RECIPE`), and a `capacities` list with one row per N in (8, 64), each carrying
  `n`, `pinned_delta_times_n`, `pinned_margin`, `rejected_delta`, `rejected_delta_times_n`,
  `rejected_overshoot_multiple`. COMPUTE every one of these by calling the pin's functions at write
  time. The n=64 row is what converts "the recipe is wrong at the small capacity" into "the recipe is
  wrong at BOTH capacities this milestone runs".

`provenance`: `git_sha` (via `personacore.provenance.git_sha`), `written_utc`, `python`, `seed`,
`pin_module` `"scripts/mitigation_unit.py"`, `pin_sha256`, and an explicit
`"epsilon_computed": false` field with the note that no epsilon is computed in this phase — the
artifact is a UNIT and the path that makes it real.

Guard the write with `teach_persona.refuse_if_exists` (imported, never copied): recorded evidence is
not silently replaced, and the abort names the file and the delete command.

Create `tests/test_phase21_unit_record.py` with:
  - `test_privacy_unit_artifact_schema` — every key above present; `epsilon_computed is False`
  - `test_artifact_values_come_from_the_pin` — re-import `mitigation_unit` and assert the artifact's
    `delta`, `sampling_rate_q`, `privacy_unit` and every `capacities` row are EQUAL to freshly
    computed values from the pin. This is what stops the artifact and the pin drifting into two
    sources of truth.
  - `test_driver_refuses_to_rerun` — call the emitter twice into the same path in `tmp_path`; the
    second call raises `SystemExit` naming the file.
  </action>
  <acceptance_criteria>
    - `.venv/bin/python -c "import json;d=json.load(open('results/phase21_privacy_unit.json'));print(d['delta']['delta'], [r['rejected_delta_times_n'] for r in d['delta']['capacities']], d['provenance']['epsilon_computed'])"` prints `1e-05`, `[0.8122523963562354, 0.6597539553864469]`, `False`
    - `.venv/bin/python -m pytest -q tests/test_phase21_unit_record.py` — all pass
    - `.venv/bin/python -m pytest -q tests/test_phase21_unit_pin.py` — the pin's own guards still green
    - **DO NOT COMMIT YET.** Task 3 owns the commit and its ordering check.
    - `.venv/bin/python -m ruff check . && .venv/bin/python -m ruff format --check .`
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_phase21_unit_record.py tests/test_phase21_unit_pin.py</automated>
  </verify>
  <done>`results/phase21_privacy_unit.json` is written (not yet committed), every pinned value in it was computed from the pin, and the refuse-to-rerun guard fires.</done>
</task>

<task type="auto">
  <name>Task 2: Measure both paths at both capacities and write results/phase21_multiplicity.json</name>
  <read_first>
    - scripts/phase21_unit_record.py — `count_unaligned`, `count_aligned`, `BIN_COMPOSITION_LABELS` (plan 21-10)
    - scripts/teach_persona.py — `arm_spec('dp_n8')`, `arm_spec('dp_n64')`, `build_bins(..., align_facts=...)`,
      `fact_bin_path`, `replay_window_budget`
    - .planning/phases/21-.../21-CONTEXT.md — D-01, D-10, D-15, D-26
  </read_first>
  <files>scripts/phase21_unit_record.py</files>
  <action>
Extend the driver with the multiplicity emitter. Build the bins under a temporary directory, never at
an `arm_outputs` path, so no recorded arm evidence is touched.

Measure THREE labelled rows at n=8, each using its `BIN_COMPOSITION_LABELS` literal:
  1. `"replay-in-bin @1.0"` — `build_bins` with `replay_ratio=1.0` and `n_facts=None` (the LEGACY
     path, deliberately, because this row exists to characterise the OLD loader against the OLD bin).
     If `data/dialog_train.bin` is absent on this machine, use a synthetic replay source of the same
     token count and LABEL the row as synthetic in the artifact — an unrunnable row silently omitted
     is worse than a row that says how it was produced.
  2. `"facts-only (D-10)"` — `build_bins` at `replay_ratio=0.0`, unaligned draws
  3. `"fact-aligned (D-01, D-05)"` — the aligned bins, `count_aligned`

Then repeat rows 2 and 3 at n=64 via `arm_spec('dp_n64')`, so the record covers BOTH capacities this
milestone runs.

Every row carries its denominator (D-26, and the repo's Established Pattern that measured numbers
travel with their denominator and their provenance): `bin_composition`, `n_facts`, `seed` (1337),
`max_steps` (200), `batch_size` (8), `total_draws`, `attribution_rule`, `bin_tokens`,
`window_count`, `min`, `max`, `mean`, `spread`, and `analytic_expectation` LABELLED as analytic
beside — never in place of — the measured value.

Add a `corpus_geometry` block, which is what DISCHARGES `21-RESEARCH.md` assumption A3:
  - per capacity: `n_facts`, `windows_per_fact` (the observed ragged tuple), `total_windows`,
    `total_tokens`, `pad_tokens`, `pad_fraction`, `grad_accum_steps` (= `n_facts`, ASSERTED equal to
    the observed micro-step count rather than declared), and `replay_windows_per_lot`
    (= `4 * n_facts`, from `replay_window_budget`)
  - RECORD THE OBSERVED n=64 total. A3 assumed ~264 windows from 56 filler facts at ~4 windows each;
    if the measurement differs, PUBLISH the measured number and say it differs. Do not adjust the
    corpus to hit 264.
  - at n=8, compare the observed geometry against D-01's measured table
    (`(4,4,4,4,4,5,4,4)`, 33 windows, 7,581 tokens, 867 pad = 10.26%) and record any divergence
    rather than smoothing it.

Add a `findings` block carrying the two results that appear in no source document and should survive
into the report:
  - the D-10/UNIT-03 interaction, with BOTH measured numbers: moving replay out of the teaching bin
    roughly DOUBLES the old path's per-fact multiplicity, because the same 1,600 draws now land on
    half as much data. A decision taken purely for honest accounting made the UNALIGNED number worse
    — which STRENGTHENS UNIT-01's indictment rather than weakening it, and only shows up because both
    numbers were computed instead of one.
  - the D-11 `teaching_tokens` side channel and its closure, with the observed per-fact token spread
    (867-1,041 across 8 facts) that made it measurable.

Record explicitly in the driver's docstring what is NOT in this artifact and why:
re-benchmarking D-02's ragged-vs-uniform ratios on the real bins is a DEFERRED item in
`21-CONTEXT.md`'s `<deferred>` list and is deliberately not planned here. Naming the exclusion stops
a later reader reading it as an oversight.
  </action>
  <acceptance_criteria>
    - `.venv/bin/python -c "import json;d=json.load(open('results/phase21_multiplicity.json'));print([r['bin_composition'] for r in d['rows']]);print({c['n_facts']: c['total_windows'] for c in d['corpus_geometry']})"` lists all three labels and both capacities
    - Every row has all 14 schema keys; assert it with a one-line test in `tests/test_phase21_unit_record.py`
    - The `fact-aligned` rows report `mean == 1.0`, `min == max == 1`, `spread == 0` at BOTH capacities
    - `corpus_geometry` at n=8 reports `windows_per_fact == [4,4,4,4,4,5,4,4]`, `total_windows == 33`,
      `pad_tokens == 867` — or, if it does not, the observed values are PUBLISHED and the divergence
      is called out in the SUMMARY
    - `grad_accum_steps` equals the OBSERVED micro-step count at both capacities, not a declared value
    - `git status --porcelain data/` is EMPTY
    - **DO NOT COMMIT YET.**
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_phase21_unit_record.py tests/test_phase21_multiplicity.py</automated>
  </verify>
  <done>Both paths are measured at both capacities, every row carries its denominator and its label, and A3's assumed n=64 window count is replaced by an observed one.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify the commit ORDER, then commit — the irrevocable step</name>
  <read_first>
    - tests/test_phase20_prereg.py:121-183 (`_assert_ordering_holds`), :186-212 (the Phase 20 live
      guard), and the `test_phase21_*` tests added by plans 21-01 and 21-03
    - .planning/phases/21-.../21-VALIDATION.md — `## Manual-Only Verifications`
  </read_first>
  <files>tests/test_phase20_prereg.py, results/phase21_privacy_unit.json, results/phase21_multiplicity.json</files>
  <what-built>
Both artifacts are written to the working tree and every automated guard is green:
`tests/test_phase21_unit_record.py`, `tests/test_phase21_unit_pin.py`,
`tests/test_phase21_multiplicity.py`, and `tests/test_phase20_prereg.py` (still vacuous-but-armed).
The remaining step is a COMMIT whose ordering is irrevocable, and the property is over git history
rather than over a working tree, so a test can assert the ordering holds but only the operator
controls which commit lands first.

Before pausing, the executor MUST have run and recorded:
  1. `git log --format=%H -- scripts/mitigation_unit.py` — the full list of pin commits
  2. for each, `git merge-base --is-ancestor <pin> HEAD; echo $?` — every one MUST be `0`
  3. `git ls-files 'results/phase21_*'` — MUST still be empty (nothing committed yet)
  4. `git status --porcelain results/` — MUST show exactly the two new artifacts, untracked
  5. `.venv/bin/python -m pytest -q tests/test_phase20_prereg.py` — armed and GREEN first (1.86s / 21 tests)

Also prepared, NOT yet applied: the removal of
`test_phase21_has_no_artifact_yet_so_the_arming_is_honest` (added by plan 21-01), which becomes false
by design at this commit. Its docstring already records that plan 21-11 removes it, so the deletion
is the recorded transition rather than an erasure. It is replaced in the same edit by
`test_phase21_guard_is_now_live`, asserting `checked` is non-zero — the guard has carried the
transition from vacuous to live, which is what `:178`'s equivalence exists to enforce.
  </what-built>
  <how-to-verify>
1. Confirm the recorded output of checks 1-5 above. In particular, confirm every pin commit is an
   ancestor of HEAD (`echo $?` was `0` for each).
2. Confirm you understand what this commit makes permanent: from this point on
   `scripts/mitigation_unit.py` is FROZEN. Any later edit to it turns
   `test_phase21_prereg_is_frozen_before_every_phase21_result` permanently RED, and `git rm` plus a
   re-add at the same path CANNOT undo it — `:157` takes `adds[-1]`, the earliest add, and plan
   21-03's fixture measured that across a real delete-and-re-add cycle. Corrections after this point
   are dated continuations via `scripts/_addendum.py`, never edits.
3. Note the reflexivity gap deliberately: `git merge-base --is-ancestor X X` exits 0, so pin and
   artifact in the SAME commit would PASS the mechanism. D-20's "strictly after" is a DISCIPLINE
   tighter than the guard enforces, and the plan honours the discipline: the artifacts go in their
   own commit, not amended into a pin commit.
4. Reply `approved` to commit, or describe what should change.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with STEP 1-5 below, or describe what should change. Do NOT `git add` any `results/phase21_*` path before this checkpoint is cleared — the ordering it protects is irrevocable.</resume-signal>
  <action>
AFTER approval, in this exact order:

STEP 1 — edit `tests/test_phase20_prereg.py`: delete
`test_phase21_has_no_artifact_yet_so_the_arming_is_honest` and add `test_phase21_guard_is_now_live`,
which calls `_assert_ordering_holds` with the phase21 arguments AND additionally asserts
`_git("ls-files", "results/phase21_*")` is non-empty. Its docstring records the transition: the guard
was vacuous by construction while `checked == n * 0 == 0`, and from this commit onward `:178`'s
equivalence demands a non-zero `checked` — the same trajectory the Phase 20 guard already carried in
production (measured during research: 9 pin commits x 3 artifacts = `checked = 27`).

STEP 2 — commit the test edit ALONE first, and confirm it is GREEN. This keeps the guard-side change
in a commit that is still a strict ancestor of the artifact commit, preserving the arm-then-write
discipline at the finest granularity available.

STEP 3 — `git add results/phase21_privacy_unit.json results/phase21_multiplicity.json` and commit
them in their OWN commit. Do not amend, do not squash into a pin commit.

STEP 4 — immediately verify the guard is now LIVE:
  - `git ls-files 'results/phase21_*'` lists exactly two paths
  - `.venv/bin/python -m pytest -q tests/test_phase20_prereg.py` is GREEN
  - record the observed `checked` value: `len(git log --format=%H -- scripts/mitigation_unit.py)` x 2
  - for each artifact, `git log --diff-filter=A --format=%H -- <path> | tail -1` is a DESCENDANT of
    every pin commit

STEP 5 — run the full suite and confirm `git diff --exit-code scripts/mitigation_unit.py
scripts/mitigation_gate.py scripts/phase18_extraction.py` returns 0.
  </action>
  <acceptance_criteria>
    - Every pin commit is an ancestor of the artifact commit, verified by
      `git merge-base --is-ancestor <pin> <artifact-commit>` exiting 0 for each pair
    - `git ls-files 'results/phase21_*'` lists exactly `results/phase21_privacy_unit.json` and
      `results/phase21_multiplicity.json`
    - `.venv/bin/python -m pytest -q tests/test_phase20_prereg.py -k phase21` GREEN, and the observed
      `checked` value is non-zero and recorded
    - `.venv/bin/python -m pytest -q` full suite green (the 1 platform-gated skip is expected)
    - `git diff --exit-code scripts/mitigation_unit.py scripts/mitigation_gate.py scripts/phase18_extraction.py results/phase16_recall_sample.json` returns 0
    - The two artifacts landed in a commit distinct from every `scripts/mitigation_unit.py` commit
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_phase20_prereg.py tests/test_phase21_unit_record.py tests/test_phase21_unit_pin.py && test "$(git ls-files 'results/phase21_*' | wc -l | tr -d ' ')" = "2"</automated>
    <human-check>Confirm the pin-ancestry output before committing; the ordering is irrevocable.</human-check>
  </verify>
  <done>Both artifacts are committed strictly after every pin commit, the ancestry guard is live with a non-zero `checked`, and the frozen files are byte-unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working tree -> git history | An artifact becomes WATCHED at COMMIT time. `adds[-1]` (`test_phase20_prereg.py:157`) makes a wrong first commit permanent; there is no recovery path and no force flag. |
| the pin -> the artifact | If the driver retypes a pinned value instead of importing it, the pre-registration and the published record become two sources that can disagree, and only one of them is frozen. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-03 | Tampering | an artifact committed BEFORE the pin, permanently reddening the ancestry guard | mitigate | the blocking human checkpoint verifies `git merge-base --is-ancestor <pin> HEAD` exits 0 for EVERY pin commit before any `git add` of a `results/phase21_*` path |
| T-21-06 | Tampering | attempting to launder a wrong order by `git rm` + re-add | mitigate | impossible by `:157`; measured in plan 21-03's state 4 across a real cycle, and the checkpoint states it so the operator knows there is no undo |
| T-21-50 | Tampering | the artifact restates a pinned value that later drifts from the pin | mitigate | `test_artifact_values_come_from_the_pin` recomputes every pinned field from `mitigation_unit` and asserts equality |
| T-21-51 | Repudiation | an analytic number published as the measurement UNIT-03 demands | mitigate | `analytic_expectation` is a separately named field beside the measured value, never in place of it; the aligned rows report an OBSERVED mean of 1.0 from a counter proven able to report otherwise (plan 21-10) |
| T-21-52 | Repudiation | A3's assumed 264-window n=64 geometry published as a measurement | mitigate | `corpus_geometry` records the OBSERVED total and the plan forbids adjusting the corpus to hit the assumed number |
| T-21-53 | Repudiation | a row silently omitted because `data/dialog_train.bin` is absent on this machine | mitigate | the replay-in-bin row falls back to a synthetic source and is LABELLED synthetic in the artifact rather than dropped |
| T-21-49 | Tampering | the guard stays vacuous after the artifacts land | mitigate | `test_phase21_guard_is_now_live` asserts `checked` non-zero and `ls-files` non-empty; `:178`'s equivalence would otherwise make an empty match set RED, which is the designed behaviour |
| T-21-54 | Repudiation | publishing an epsilon this phase has no basis for | mitigate | `provenance.epsilon_computed: false` is an asserted field; no accountant code exists in this phase |
| T-21-11 | Tampering | supply chain | accept | zero package installs; `tests/test_package.py:37` |
</threat_model>

<verification>
```bash
git log --format=%H -- scripts/mitigation_unit.py                       # every one must be an ancestor
git ls-files 'results/phase21_*'                                        # exactly 2 after task 3
.venv/bin/python -m pytest -q tests/test_phase20_prereg.py -k phase21
.venv/bin/python -m pytest -q                                           # full suite
git diff --exit-code scripts/mitigation_unit.py scripts/mitigation_gate.py scripts/phase18_extraction.py results/phase16_recall_sample.json
.venv/bin/python -m ruff check . && .venv/bin/python -m ruff format --check .
```
</verification>

<success_criteria>
- Both artifacts committed, strictly after every `scripts/mitigation_unit.py` commit
- The ancestry guard is LIVE: `checked` non-zero, `ls-files` non-empty, `:178` satisfied
- Every pinned value in `results/phase21_privacy_unit.json` was computed from the pin at write time
- `results/phase21_multiplicity.json` carries three labelled rows at n=8 and two at n=64, each with
  its full denominator, and the observed n=64 corpus geometry
- `epsilon_computed` is `false`; no epsilon is computed anywhere in this phase
- All frozen files byte-unchanged
</success_criteria>

<output>
Create `.planning/phases/21-the-privacy-unit-the-dp-data-path-and-the-n-64-corpus/21-11-SUMMARY.md` when done.
Record: the pin-commit list with its ancestry verification output; the observed `checked` value; the
measured multiplicity for all five rows with their denominators; the observed n=64 corpus geometry
against A3's assumed 264; and any divergence from D-01's measured n=8 table, published rather than
smoothed.
</output>
