---
phase: 21-the-privacy-unit-the-dp-data-path-and-the-n-64-corpus
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/mitigation_unit.py
  - tests/test_phase21_unit_pin.py
  - tests/test_phase20_prereg.py
autonomous: true
requirements: [UNIT-01, UNIT-04, UNIT-05]

must_haves:
  truths:
    - "scripts/mitigation_unit.py exists and states PRIVACY_UNIT = 'one taught fact' with the arithmetic that indicts the old loader (D-23 item 1, SC1/UNIT-01)"
    - "The replay-outside-N decision (q = 1, N = n_facts) is committed WITH its epsilon consequence (D-07, D-23 item 2, SC4/UNIT-04)"
    - "delta = 1e-5 is committed with the rejected 1/N**1.1 recipe's self-contradiction arithmetic at BOTH N=8 and N=64 (D-23 item 3, SC4/UNIT-05)"
    - "The module imports NOTHING — the D-22 ceiling {pathlib, sys, erasure_gate} is satisfied with room to spare, and the existing import-graph scan covers it for free"
    - "V4_ARTIFACT_GLOBS contains 'results/phase21_*' AND a live test calls _assert_ordering_holds with that glob — both halves, because the glob addition alone enforces nothing (D-20)"
    - "The glob addition was observed RED with the second half present, then restored byte-identically"
    - "No results/phase21_* artifact exists in git history at the end of this plan (arm-then-write)"
  artifacts:
    - path: "scripts/mitigation_unit.py"
      provides: "PRIVACY_UNIT, SAMPLING_RATE_Q, privacy_n, DELTA, rejected_delta, module-level _prove guards"
      contains: "PRIVACY_UNIT"
    - path: "tests/test_phase21_unit_pin.py"
      provides: "delta guards at N=8 and N=64, both recipes; the zero-import assertion"
    - path: "tests/test_phase20_prereg.py"
      provides: "PHASE21_PREREG_ARTIFACT, the widened V4_ARTIFACT_GLOBS, and the live phase21 ordering guard"
      contains: "results/phase21_*"
  key_links:
    - from: "tests/test_phase20_prereg.py"
      to: "_assert_ordering_holds"
      via: "a new test passing prereg_artifact=PHASE21_PREREG_ARTIFACT, artifact_glob='results/phase21_*'"
      pattern: "artifact_glob=\"results/phase21_\\*\""
    - from: "tests/test_phase20_prereg.py::V4_ARTIFACT_GLOBS"
      to: "the :129 consistency check"
      via: "'results/phase21_*' membership"
      pattern: "V4_ARTIFACT_GLOBS = \\("
---

<objective>
Commit the frozen pre-registration for the privacy unit and ARM the ancestry guard against
`results/phase21_*` — before any Phase 21 artifact exists.

Purpose: the freeze is irrevocable and one-directional. `tests/test_phase20_prereg.py:157` takes
`adds[-1]`, the EARLIEST add of each artifact, so an artifact committed before the pin can never be
laundered by `git rm` + re-add. That is why D-20 puts the arming in the phase's FIRST plan: every
commit touching the pin is watched from the moment there is something to miss.

Output: `scripts/mitigation_unit.py` (D-23's three settled decisions, zero imports, zero I/O),
`tests/test_phase21_unit_pin.py`, and two additive edits to `tests/test_phase20_prereg.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-the-privacy-unit-the-dp-data-path-and-the-n-64-corpus/21-CONTEXT.md
@.planning/phases/21-the-privacy-unit-the-dp-data-path-and-the-n-64-corpus/21-VALIDATION.md
</context>

<interfaces>
<!-- Contracts the executor needs; do not go exploring for these. -->

`tests/test_phase20_prereg.py` — the mechanism, read line-by-line during context:
- `:72`  `_GATE_MODULES = tuple(sorted((_ROOT / "scripts").glob("mitigation_*.py")))`
- `:91`  `PHASE20_PREREG_ARTIFACT = "scripts/mitigation_gate.py"` — a hand-written explicit path.
         THIS CONSTANT, and nothing else, is what freezes a module (D-19).
- `:102` `V4_ARTIFACT_GLOBS = ("results/phase20_*",)`
- `:121` `def _assert_ordering_holds(*, root, prereg_artifact, artifact_glob, globs)` — keyword-only,
         parameterized on `root`. CALL IT; a lookalike copy proves something about a different
         function (`:296-298`).
- `:129` `assert artifact_glob in globs` — the ONLY use of `globs` in the body.
- `:150` `tracked_artifacts = _git("ls-files", artifact_glob, ...)` — the loop runs on the SINGULAR
         `artifact_glob`.
- `:498` `imported` accumulates across ALL `_GATE_MODULES`; `:522` `allowed = {"pathlib","sys","erasure_gate"}`;
         `:538` `from_erasure_gate` asserted by EXACT equality to five names.
- `:574` `assert not ({"wilson_upper_bound","rule_of_three"} & defined)` — `defined` also accumulates
         across the glob. Do not define either name in the new module.
- `:40-50` the `sys.path.insert(0, str(_ROOT / "scripts"))` bootstrap idiom for importing a script.

`scripts/mitigation_gate.py:66` — `def _prove(condition, message)`, and module-level `_prove(...)`
calls at `:175` and `:1046`. That is the shape to mirror.

MEASURED THIS SESSION with `.venv/bin/python` (every number below travels with its command):
```
for n in (8,64): r = n ** -1.1; print(r, r*n, (r*n)/0.01)
N=8   rejected delta 0.10153154954452942  delta*N 0.8122523963562354  = 81.22523963562354 x the 0.01 ceiling
N=64  rejected delta 0.010308655552913232 delta*N 0.6597539553864469  = 65.97539553864469 x the 0.01 ceiling
DELTA=1e-5: delta*N = 8e-05 at N=8 (passes by 125.0x) and 0.00064 at N=64 (passes by 15.625x)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write the frozen pre-registration module scripts/mitigation_unit.py</name>
  <read_first>
    - scripts/mitigation_gate.py:1-180 — the docstring register, `_prove` at `:66`, and the
      module-level `_prove(...)` call at `:175`. Mirror this shape, do not copy its content.
    - tests/test_phase20_prereg.py:474-579 — the import-graph scan the new module joins for free.
    - .planning/phases/21-.../21-CONTEXT.md — D-07, D-19, D-21, D-22, D-23, D-26.
  </read_first>
  <files>scripts/mitigation_unit.py</files>
  <action>
Create `scripts/mitigation_unit.py`. The filename is named for its SUBJECT (the privacy unit), not
for its phase — the register `test_phase20_prereg.py:59-60` records for `mitigation_gate.py`.

Module docstring must state, in the repo's own register:
  (a) that this file is CLOSED at the first committed `results/phase21_*` artifact, that the
      correction path is a dated continuation via `scripts/_addendum.py` and never an edit, and that
      `adds[-1]` (`test_phase20_prereg.py:157`) makes a `git rm` + re-add unable to launder it;
  (b) D-22's ceiling and its consequence in one sentence: joining the `scripts/mitigation_*.py` glob
      caps this module's import surface at `{pathlib, sys, erasure_gate}`, `json` is unreachable, so
      this module does ZERO I/O and the artifact writer lives OUTSIDE the glob in
      `scripts/phase21_unit_record.py`;
  (c) that `_prove` is defined LOCALLY rather than imported from `mitigation_gate` because importing
      a sibling would add `mitigation_gate` to the accumulated `imported` set and turn `:523` RED —
      a forced consequence of the ceiling, recorded so a reader does not read it as a copied
      instrument;
  (d) D-21's convention, recorded and NOT acted on: constants Phase 22+ has not yet discovered land
      in an UNFROZEN `mitigation_*.py` sibling, unpinned until that phase arms it. Phase 21 does not
      create that file — the glob captures it the moment it genuinely exists (`:64-66`).

Import NOTHING. Not `pathlib`, not `sys`. The ceiling permits three names; using zero is the
cheapest way to stay inside it and to keep this file's surface minimal, which is what makes freezing
it cheap (D-22).

Contents — EXACTLY D-23's three settled decisions and nothing else. D-11's replay VOLUME constant is
explicitly EXCLUDED from this file (D-23) and lands in `scripts/teach_persona.py` in plan 21-08.

1. SC1 / UNIT-01 — `PRIVACY_UNIT = "one taught fact"`, plus a module-level prose constant carrying
   the arithmetic that indicts the old loader: `get_batch_memmap_masked` draws `block_size` windows
   with replacement over a flat concatenated bin (`src/personacore/training/data.py:117`), so a
   single fact is touched an unbounded number of times per epoch and an example-level epsilon bounds
   nothing about a fact. State the analytic expectation as ANALYTIC, with its denominator: at
   `MAX_STEPS = 200` x `BATCH_SIZE = 8` = 1,600 draws over the 7,581-token facts-only bin (D-10), the
   expected touches per fact is 262.9 — versus exactly 1 per micro-step, deterministic, under
   fact-aligned accumulation. Name it as an expectation that plan 21-11 replaces with a MEASURED row
   (D-26); this module must not be read as the source of the measurement.

2. SC4 / UNIT-04 — `SAMPLING_RATE_Q = 1.0` and `def privacy_n(n_facts)` returning `int(n_facts)`.
   Record D-07 verbatim in substance: replay is public PersonaChat serving a training-stability
   role, it sits OUTSIDE the privacy N, and counting it inside N would shrink q and produce a
   flatteringly small epsilon that does not reflect what the mechanism protects — the exact trap
   UNIT-04 names. Record the measured basis: at `replay_ratio = 1.0` replay is 7,581 tokens = exactly
   50% of today's bin. Record D-08 beside it: replay is NOT dropped from the DP arms, because losing
   dialogue capability risks condition (c) and `f_C = 0.5` sits only 2.24x above the measured
   non-vacuity floor 0.2237 (Phase 20 D-17).

3. SC4 / UNIT-05 — `DELTA = 1e-5`, `DELTA_TIMES_N_CEILING = 0.01`,
   `REJECTED_DELTA_RECIPE = "1/N**1.1"`, and `def rejected_delta(n)` returning `n ** -1.1`.
   `n ** -1.1` is an OPERATOR, so this needs no import — which is what makes it fit the ceiling.

Module-level `_prove` calls, executed at import so a wrong edit fails on import rather than in a
consumer. Five of them, each with a message naming the number and its ceiling:
  - `DELTA * 8 < DELTA_TIMES_N_CEILING`   (8e-05, passes by 125.0x)
  - `DELTA * 64 < DELTA_TIMES_N_CEILING`  (0.00064, passes by 15.625x)
  - `rejected_delta(8) * 8 >= DELTA_TIMES_N_CEILING`   (0.8122523963562354, FAILS its own assertion by 81.2x)
  - `rejected_delta(64) * 64 >= DELTA_TIMES_N_CEILING` (0.6597539553864469, FAILS by 66.0x)
  - `SAMPLING_RATE_Q == 1.0`
State in a comment beside the last two that the recipe is wrong at BOTH capacities this milestone
runs, not merely at the small one — that is a strictly stronger record than a single-N check.

`_prove(condition, message)` raises `SystemExit(message)` when `condition` is false — three lines,
mirroring `mitigation_gate.py:66`.
  </action>
  <acceptance_criteria>
    - `.venv/bin/python -c "import sys; sys.path.insert(0,'scripts'); import mitigation_unit as m; print(m.PRIVACY_UNIT, m.SAMPLING_RATE_Q, m.DELTA, m.privacy_n(64), m.rejected_delta(8))"` prints
      `one taught fact 1.0 1e-05 64 0.10153154954452942`
    - `.venv/bin/python -c "import ast;t=ast.parse(open('scripts/mitigation_unit.py').read());print([n for n in ast.walk(t) if isinstance(n,(ast.Import,ast.ImportFrom))])"` prints `[]`
    - `.venv/bin/python -m pytest -q tests/test_phase20_prereg.py` — all tests pass with the new
      module inside `_GATE_MODULES` (the import-graph scan at `:474` now covers it for free)
    - `grep -c "wilson_upper_bound\|rule_of_three" scripts/mitigation_unit.py` returns 0
    - `.venv/bin/python -m ruff check scripts/mitigation_unit.py && .venv/bin/python -m ruff format --check scripts/mitigation_unit.py`
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_phase20_prereg.py</automated>
  </verify>
  <done>`scripts/mitigation_unit.py` exists, imports nothing, holds exactly D-23's three decisions plus five module-level `_prove` guards, and `tests/test_phase20_prereg.py` is green with it in the glob.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tests/test_phase21_unit_pin.py — the pin's guards, sampled at BOTH capacities</name>
  <read_first>
    - scripts/mitigation_unit.py (written in task 1)
    - tests/test_phase20_prereg.py:32-50 — the `sys.path.insert` bootstrap idiom for importing a script
    - .planning/phases/21-.../21-RESEARCH.md — the `## Validation Architecture` block at `:175`,
      specifically the `_prove` guard sampling shape
  </read_first>
  <files>tests/test_phase21_unit_pin.py</files>
  <behavior>
    - `DELTA * n < 0.01` is True at n=8 AND at n=64 (parametrized, both rows present)
    - `rejected_delta(n) * n` equals 0.8122523963562354 at n=8 and 0.6597539553864469 at n=64
    - `rejected_delta(n) * n >= 0.01` at both capacities — the self-contradiction, ASSERTED
    - the ratio to the ceiling is 81.2x at n=8 and 66.0x at n=64 (rel=1e-2)
    - `privacy_n(8) == 8` and `privacy_n(64) == 64`; `SAMPLING_RATE_Q == 1.0`
    - the module's AST holds ZERO Import/ImportFrom nodes (D-22's ceiling, asserted directly rather
      than only inherited from the glob scan — a second tier, so a ceiling widening is caught here
      with a message naming this module rather than in an accumulated set naming all of them)
    - `PRIVACY_UNIT == "one taught fact"` exactly
  </behavior>
  <action>
Create `tests/test_phase21_unit_pin.py`. CPU-only, GPU-free, no torch, no network. Bootstrap
`scripts/` onto `sys.path` with the `test_phase20_prereg.py:44-50` idiom and `import mitigation_unit`.

Parametrize the delta tests over `[(8, ...), (64, ...)]`. The n=64 row is the one that matters most:
it converts "the recipe is wrong at the small capacity" into "the recipe is wrong at BOTH capacities
this milestone runs", which a single-N test cannot say.

Add a NON-VACUITY assertion for the pinned literal: assert that `DELTA` and `rejected_delta(8)`
differ by more than three orders of magnitude, so a future edit setting `DELTA = rejected_delta(N)`
cannot make both families of assertion green at once.

Add `test_pin_imports_nothing`: parse `scripts/mitigation_unit.py` with `ast` and assert the list of
`ast.Import`/`ast.ImportFrom` nodes is `[]`, with a message naming D-22 and the reason (`json` is
unreachable, so the artifact writer lives in `scripts/phase21_unit_record.py`).
  </action>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest -q tests/test_phase21_unit_pin.py` — all pass
    - `.venv/bin/python -m pytest -q tests/test_phase21_unit_pin.py -k prove_guards` collects at
      least 4 tests (the parametrized delta rows), matching `21-VALIDATION.md:78`
    - DELIBERATE-RED, run and recorded: temporarily set `DELTA = 1e-3` in `scripts/mitigation_unit.py`;
      `.venv/bin/python -c "import sys;sys.path.insert(0,'scripts');import mitigation_unit"` exits
      non-zero on the module-level `_prove` at N=64 (`1e-3 * 64 = 0.064 >= 0.01`). Restore with
      `git checkout scripts/mitigation_unit.py` and prove the restore byte-identical:
      `shasum -a 256 scripts/mitigation_unit.py` equals the value recorded before the mutation AND
      `git diff --exit-code scripts/mitigation_unit.py` returns 0. Record both shas in the SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_phase21_unit_pin.py</automated>
  </verify>
  <done>Every pinned number is asserted at n=8 and n=64, the zero-import ceiling is asserted directly, and the module-level guard has been WATCHED failing and restored byte-identically.</done>
</task>

<task type="auto">
  <name>Task 3: Arm the ancestry guard — BOTH halves, with the glob addition watched RED</name>
  <read_first>
    - tests/test_phase20_prereg.py:87-102 (the register constants and the D-33 comment on
      `V4_ARTIFACT_GLOBS`), `:121-183` (`_assert_ordering_holds`), `:186-212` (the live Phase 20 guard
      — the shape to mirror)
    - .planning/phases/21-.../21-CONTEXT.md — D-19, D-20
  </read_first>
  <files>tests/test_phase20_prereg.py</files>
  <action>
TWO additive edits to `tests/test_phase20_prereg.py`. Both are required; neither is sufficient.
Do not delete or reword any existing line — this file is not frozen, but every comment in it is a
recorded finding.

Edit 1 — beside `PHASE20_PREREG_ARTIFACT` at `:91`, add
`PHASE21_PREREG_ARTIFACT = "scripts/mitigation_unit.py"` with a comment stating that this hand-written
explicit path — and NOT the `mitigation_*.py` filename — is what confers the freeze (D-19: the glob
at `:72` covers content scans, `_MITIGATION_GATE_PATH` singular covers `:740/:805/:928/:991` and does
NOT extend to a sibling, and only a `prereg_artifact=` constant reaches `_assert_ordering_holds`).

Edit 2 — widen `V4_ARTIFACT_GLOBS` at `:102` to `("results/phase20_*", "results/phase21_*")`. Extend
the existing comment (do not replace it) with the Phase 21 finding recorded in D-20: this addition
creates NO ancestry check by itself. `globs` is used only for the `assert artifact_glob in globs`
consistency check at `:129`; the loop runs on the singular `artifact_glob` at `:150`. A phase doing
exactly what Phase 20 D-33 literally says would ship an UNENFORCED DECLARATION.

Edit 3 — add `test_phase21_prereg_is_frozen_before_every_phase21_result()`, a sibling of `:186`,
calling the SAME `_assert_ordering_holds` with `root=_ROOT`,
`prereg_artifact=PHASE21_PREREG_ARTIFACT`, `artifact_glob="results/phase21_*"`,
`globs=V4_ARTIFACT_GLOBS`. Its docstring must record three things: that it is vacuous today by
construction (`checked == n * 0 == 0`) and that this is deliberate — arming before the first artifact
is the point; that the closing equivalence at `:178` is what stops the vacuity surviving the
artifacts' arrival; and the reflexivity note measured on this repository — `git merge-base
--is-ancestor X X` exits 0, so a pin and an artifact in the SAME commit PASS, and D-20's "strictly
after" is a discipline tighter than the mechanism enforces, inherited deliberately.

Add `test_phase21_has_no_artifact_yet_so_the_arming_is_honest()`: assert
`_git("log", "--diff-filter=A", "--format=%H", "--", "results/phase21_*")` is EMPTY on this
repository at the moment the guard is armed. This is the arm-then-write claim made checkable rather
than promised, and it is the invariant Pitfall 6 protects (a `phase21_`-named probe reaching the real
history would permanently corrupt the evidence this phase exists to produce). Note in the docstring
that this test is EXPECTED to be deleted or inverted by plan 21-11, which commits the first artifact
— and that deleting it there is the recorded transition, not an erasure.

DO NOT create any `results/phase21_*` file in this plan. `git status --porcelain results/` must be
empty at commit time.
  </action>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest -q tests/test_phase20_prereg.py` — all pass (21+ tests)
    - `.venv/bin/python -m pytest -q tests/test_phase20_prereg.py -k phase21` collects at least 2
      tests and passes, matching `21-VALIDATION.md:80`
    - `git ls-files 'results/phase21_*'` is empty AND
      `git log --diff-filter=A --format=%H -- 'results/phase21_*' | wc -l` is 0
    - DELIBERATE-RED for the both-halves claim, run and recorded: revert `V4_ARTIFACT_GLOBS` to
      `("results/phase20_*",)` while KEEPING the new test. Re-run
      `.venv/bin/python -m pytest -q tests/test_phase20_prereg.py -k phase21` and confirm it fails at
      `:129` with the "watching two different sets of paths" message — this is the cheapest possible
      demonstration that half 1 is load-bearing. Restore and prove byte-identical:
      `shasum -a 256 tests/test_phase20_prereg.py` equals the pre-mutation value and
      `git diff --exit-code tests/test_phase20_prereg.py` returns 0.
    - `.venv/bin/python -m ruff check tests/ scripts/ && .venv/bin/python -m ruff format --check tests/ scripts/`
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_phase20_prereg.py tests/test_phase21_unit_pin.py tests/test_package.py</automated>
  </verify>
  <done>Both halves of D-20 are committed and green; the glob half has been WATCHED failing at `:129` and restored byte-identically; `git ls-files 'results/phase21_*'` is still empty.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git history -> published claim | The pre-registration's authority rests entirely on commit ordering. Anyone with a shell can rewrite committer dates; ancestry is the only property that costs a full object rewrite to falsify. |
| the `mitigation_*.py` glob -> the runtime dependency surface | A new module joining the glob widens an accumulated `imported` set that four assertions read. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-03 | Tampering | `scripts/mitigation_unit.py` post-hoc edit after an artifact lands | mitigate | `_assert_ordering_holds(prereg_artifact=PHASE21_PREREG_ARTIFACT, ...)`; `:143` reads EVERY commit touching the pin, so a later edit is caught and not merely a wrong first commit |
| T-21-06 | Tampering | ancestry laundering via `git rm` + re-add | mitigate | `:157` `adds[-1]` takes the EARLIEST add; already defeated by the existing mechanism, and this plan creates no path around it (no artifact is written) |
| T-21-05 | Repudiation | the glob addition ships as an unenforced declaration (a guard that cannot fail) | mitigate | Task 3's deliberate-RED reverts half 1 and OBSERVES `:129` firing; both halves land in one plan |
| T-21-13 | Elevation of Privilege | the new sibling widens `{pathlib, sys, erasure_gate}` for every glob member | mitigate | the module imports NOTHING; asserted twice — by the existing accumulated scan at `:523` and directly by `tests/test_phase21_unit_pin.py::test_pin_imports_nothing` |
| T-21-07 | Tampering | a `phase21_`-named probe reaching the real git history | mitigate | `test_phase21_has_no_artifact_yet_so_the_arming_is_honest` asserts `git log --diff-filter=A -- 'results/phase21_*'` is EMPTY |
| T-21-11 | Tampering | supply chain (npm/pip/cargo) | accept | zero package installs in this plan; `tests/test_package.py:37` sha256-pins `pyproject.toml` byte-for-byte |
| T-21-14 | Information Disclosure | a constant read off private data reaching the pin | accept | D-11's replay VOLUME constant is EXCLUDED from this module by D-23 and lands in plan 21-08 with its own differential guard; the pin holds no corpus-derived number |
</threat_model>

<verification>
```bash
.venv/bin/python -m pytest -q tests/test_phase20_prereg.py tests/test_phase21_unit_pin.py tests/test_package.py tests/test_masked_batch.py tests/test_phase14_teaching.py
git ls-files 'results/phase21_*'          # MUST be empty
git diff --exit-code scripts/mitigation_gate.py scripts/phase18_extraction.py   # MUST be 0
.venv/bin/python -m ruff check . && .venv/bin/python -m ruff format --check .
```
</verification>

<success_criteria>
- `scripts/mitigation_unit.py` committed, zero imports, five module-level `_prove` guards green
- `tests/test_phase21_unit_pin.py` green, delta sampled at n=8 AND n=64, both recipes
- `V4_ARTIFACT_GLOBS` contains `results/phase21_*` AND a live test calls `_assert_ordering_holds`
  with it — both halves
- The `:129` RED was observed with the second half present, and the restore is sha256-equal
- `git ls-files 'results/phase21_*'` is empty at the end of this plan
</success_criteria>

<output>
Create `.planning/phases/21-the-privacy-unit-the-dp-data-path-and-the-n-64-corpus/21-01-SUMMARY.md` when done.
Record: the two sha256 values from each deliberate-RED cycle, and the exact `git log --format=%H --
scripts/mitigation_unit.py` output so a later reader can check the pin's own history.
</output>
