---
phase: 21-the-privacy-unit-the-dp-data-path-and-the-n-64-corpus
plan: 07
type: execute
wave: 3
depends_on: ["21-05"]
files_modified:
  - scripts/phase21_filler.py
  - tests/test_phase21_filler.py
autonomous: true
requirements: [UNIT-06]

must_haves:
  truths:
    - "scripts/phase21_filler.py holds exactly 56 filler facts, so the n=64 corpus is 8 scored LOCKED_FACTS + 56 unscored filler (D-12)"
    - "The filler slot grammar is DISJOINT from the 11 published slots — no filler value is ever seated in a slot GATE-10 scores (D-16)"
    - "FILLER_FACTS live entirely OUTSIDE all_pools(); _BY_ID and GATE_PROBES gain no filler key (D-13)"
    - "The DETERMINISTIC half of the minting discipline runs IN FULL: token_census round-trip plus collision refusal against the forbidden 10, every published pool value, and the filler values against each other (D-17)"
    - "The base-model guessability probe is NOT run, and the waiver is recorded IN THE MODULE with its measured price — 1,792 generations — so a later reader cannot mistake it for an omission (D-17)"
    - "Every filler fact renders through the SAME render_family over TAUGHT_FAMILY_IDS, identical in FORM to a scored fact, so the capacity lever changes N and nothing else (D-15)"
    - "render_filler_episodes iterates sorted(family_ids), so two processes produce a BYTE-IDENTICAL n=64 bin — TAUGHT_FAMILY_IDS is a frozenset and its raw iteration order differs run to run"
    - "Collision refusal is over the PROPERTY (normalized containment, both directions), not the NAME (string equality)"
  artifacts:
    - path: "scripts/phase21_filler.py"
      provides: "FILLER_SLOT_FORMS, FILLER_FACTS (56), the re-implemented minting discipline, the D-17 waiver"
      contains: "FILLER_FACTS"
      min_lines: 200
    - path: "tests/test_phase21_filler.py"
      provides: "slots_disjoint, minting_discipline, outside_all_pools, the form-identity check, the cross-process determinism check"
  key_links:
    - from: "scripts/phase21_filler.py"
      to: "phase14_factset.render_family"
      via: "forms=FILLER_SLOT_FORMS, the additive kwarg from plan 21-05"
      pattern: "forms=FILLER_SLOT_FORMS"
    - from: "scripts/phase21_filler.py"
      to: "phase14_factset.token_census"
      via: "the imported round-trip instrument, never a local reimplementation"
      pattern: "token_census"
---

<objective>
Mint the 56 unscored filler facts that make an n=64 capacity arm possible without moving a single
published number.

Purpose: D-12's reason is stronger than "it would invalidate the chain" — `n=8` is PRE-REGISTERED
LITERALLY in four places, one of them (`REQUIREMENTS.md:84`, GATE-10) already `[x]` COMPLETE and
living inside the FROZEN `scripts/mitigation_gate.py`. So 64 fresh facts does not merely break a
chain; it contradicts a completed requirement in a file only a dated continuation may touch. The 8
stay and N grows around them.

Output: `scripts/phase21_filler.py` and its half of `tests/test_phase21_filler.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-the-privacy-unit-the-dp-data-path-and-the-n-64-corpus/21-CONTEXT.md
@.planning/phases/21-the-privacy-unit-the-dp-data-path-and-the-n-64-corpus/21-VALIDATION.md
</context>

<interfaces>
<!-- Contracts the executor needs; do not go exploring. All line numbers VERIFIED against the repo. -->

`scripts/phase14_factset.py`:
- `:51` `class Fact(NamedTuple)` with fields `id, slot, value, tier` — `value` is authored LOWERCASE
  as it will be taught
- `:524` `class SlotForms(NamedTuple)`; its eight fields are declared at `:533-540` —
  `np1, np2, stem, who, kind, ver_q, ans1, ans2`. `ans1`/`ans2` are `.format(v=value)` templates.
- `:543` `SLOT_FORMS` — THE 11 PUBLISHED SLOTS, and this list is the D-16 forcing evidence:
  `person_name, pet_name, cat_name, sibling_name, hometown, street, birth_year, house_number,
  favorite_color, favorite_food, favorite_drink`. EIGHT already hold a scored fact (the
  `LOCKED_FACTS` are one-per-distinct-slot), two hold soft, `favorite_drink` is empty.
- `:127` `all_pools()` — "the iteration order for every report"; `:380` `_BY_ID` built from it;
  `:290` `GATE_PROBES = _assign_probes()` built from it
- `:313` `token_census(tok, value) -> (len(ids), decode(ids) == value)` — TOKENIZER ONLY, ZERO generations
- `:323` `normalize_for_match(text)` — lowercase + `detokenize` + collapse whitespace + strip edge punct
- `:334` `exact_match_clean(completions, value)` — takes COMPLETIONS, therefore DEFINED OVER
  BASE-MODEL OUTPUT. This is the half D-17 waives.
- `:390-399` `LOCKED_FACTS` (8); `:410-413` `SOFT_TIER_FACTS` (2); `:429`
  `GATE_REJECTED_CANDIDATES` (its explanatory comment runs `:425-428`) — D-10's
  contradiction-detector LEXICON SOURCE, which is why the published pool values are load-bearing and
  not spare inventory
- `:816` `TAUGHT_FAMILY_IDS = frozenset({"F1","F2","F4","F5","F6"})`
- `:821` `PARAPHRASES_PER_FACT_TARGET = (20, 50)`
- `:824` `render_family(family_id, fact, *, second_person=False, forms=None)` — the `forms=` kwarg
  from plan 21-05

`scripts/teach_persona.py:247-253` — `render_episodes(facts, family_ids, *, second_person=False)`.
Its inner loop is `for family_id in sorted(family_ids)` at `:251`. **That `sorted()` is not
cosmetic** and `render_filler_episodes` must mirror it: `TAUGHT_FAMILY_IDS` is a `frozenset`, and its
raw iteration order was MEASURED differing across three separate processes this session —
`['F2','F6','F4','F5','F1']`, `['F6','F2','F5','F4','F1']`, `['F4','F6','F1','F2','F5']`. Unsorted
iteration makes the n=64 bin non-reproducible run to run, which would silently break every byte-level
guarantee downstream of it.

`scripts/phase14_factset_gate.py`: `:8` guessability IS DEFINED AS prompting the un-adapted
`convbase_best.pt`; `:62` `PROBE_SEEDS = 4`; `:111` `probe_guessability`; `:223,241,260,299,341,369,419`
the seven `all_pools()` loops that confer the Phase-14 discipline. **Pool membership, not module
location, is the gate (D-13)** — which is exactly why the discipline must be re-written here.

WIDE GLOBS THAT WILL PICK UP THIS NEW FILE, checked so the executor is not surprised:
`tests/test_lora_inject.py:279` and `tests/test_phase14_scoring.py:461` scan `scripts/*.py` +
`src/**/*.py` for `LoRAConfig()` and `build_recall_prompt(persona=)` call sites; `tests/test_phase19_erasure.py:1386`
censuses `retention_perplexity(...)` call sites over the same set; `tests/test_phase17_stats.py:327`
merely WIDENS an allow-set. This module calls none of those, so all four stay green — confirm it by
running those FOUR FILES explicitly, not by a bare `pytest -q`: same-wave 21-06 and 21-08 hold live
working-tree canaries during wave 3, so a full run here would collect a sibling's deliberate RED.
The full suite is a WAVE-CLOSE gate (`21-VALIDATION.md:47,52`).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: The filler slot grammar and the 56 values</name>
  <read_first>
    - scripts/phase14_factset.py:40-60 (`Fact`), :524-560 (`SlotForms` and the first `SLOT_FORMS`
      entries — copy the FORM, never the content), :380-440 (`_BY_ID`, `LOCKED_FACTS`,
      `SOFT_TIER_FACTS`, `GATE_REJECTED_CANDIDATES` at `:429` and its lexicon-source comment at `:425-428`)
    - .planning/phases/21-.../21-CONTEXT.md — D-12, D-13, D-15, D-16
  </read_first>
  <files>scripts/phase21_filler.py</files>
  <action>
Create `scripts/phase21_filler.py`. It imports `phase14_factset as fs` (sibling-script idiom) and
stdlib only. No torch, no numpy, no I/O at import.

Module docstring must state, up front, the three things a later reader will otherwise have to
reconstruct:
  (a) why filler exists — GATE-10's n=8-vs-n=64 capacity comparison needs N to grow while the SCORED
      8 stay fixed, so the comparison is unconfounded: the n=64 arm measures extraction over the SAME
      8 facts as n=8, with only the corpus around them changed;
  (b) why filler lives OUTSIDE `all_pools()` (D-13) and what that COSTS: `all_pools()` is iterated 7x
      by `scripts/phase14_factset_gate.py` and `_BY_ID` (`phase14_factset.py:380`) is built from it,
      so pool membership — not module location — is what confers the Phase-14 discipline. It is
      therefore re-implemented here explicitly rather than inherited;
  (c) the 11-slot ceiling that forced the disjoint grammar (D-16): only 11 slots exist and 8 already
      hold a scored fact, so spreading 56 filler over them would seat ~5 rival values inside each
      scored slot — "my name is quillon" taught beside five other names — making the corpus
      self-contradictory on exactly the 8 slots GATE-10 scores. n=64 recall would then fall from SLOT
      CONTENTION rather than from capacity, and the capacity verdict would measure the wrong thing.
      Contention inside FILLER space is harmless: nothing there is scored.

`FILLER_SLOT_FORMS: dict[str, fs.SlotForms]` — EIGHT slots, every key prefixed `filler_` and every
key DISJOINT from the 11 published slot names. Each `SlotForms` follows the published field
semantics exactly (`np1` a noun phrase, `stem` a sentence stem, `who` the wh-word, `kind` the
first-person kind phrase, `ver_q` the yes/no verification stem, `ans1`/`ans2` two `{v}` templates).
Choose subjects with no overlap with the scored eight — for example a boat, a bicycle, a houseplant,
a childhood teacher, a nearby river, a former school, a neighbour, a favourite trail. Pick your own;
the binding constraint is disjointness, not the particular nouns.

`FILLER_FACTS: tuple[fs.Fact, ...]` — EXACTLY 56, seven per filler slot. Every `Fact`:
  - `id` prefixed `filler_` and globally unique
  - `slot` one of the eight `FILLER_SLOT_FORMS` keys
  - `value` LOWERCASE, an invented distinctive proper noun or identifier, matching the scored tier's
    register (compare `quillon`, `zorp`, `zibby`, `orsala`, `brindlemoor`, `marrowgate`, `1987`,
    `7412` — invent NEW ones, reuse NOTHING)
  - `tier` the literal `"filler"` — a THIRD tier name, distinct from `"core"` and `"soft"`, so a
    tier-based filter anywhere in the repo cannot accidentally sweep filler into a scored set

Declare `FILLER_FACTS` as an ORDERED tuple literal, never built from a set or dict-keys iteration.
Everything downstream — the bin, its sha256, the per-record `grad_accum` index — is a function of
this order, and a set-derived order is not stable across processes.

Record beside the tuple, as a comment, the arithmetic D-15 measured: 8 scored + 56 filler = 64, each
rendering ~22 rows / ~4 windows, so n=64 is roughly 264 windows and `grad_accum_steps = 64` — about
8x the n=8 bin. Mark the ~22 and ~264 as ESTIMATES pending task 2's measurement, not as targets.
State that the lighter-renderer alternative was rejected on a CONFOUND and not on cost: filler and
scored records would then be DIFFERENT SIZES under one clip norm, so the capacity lever would change
N and per-record mass at once, confounding precisely the GATE-10 comparison it exists to feed.
  </action>
  <acceptance_criteria>
    - `.venv/bin/python -c "import sys;sys.path.insert(0,'scripts');import phase21_filler as pf,phase14_factset as fs;print(len(pf.FILLER_FACTS), len(pf.FILLER_SLOT_FORMS), len({f.id for f in pf.FILLER_FACTS}), len({f.value for f in pf.FILLER_FACTS}))"` prints `56 8 56 56`
    - `.venv/bin/python -c "import sys;sys.path.insert(0,'scripts');import phase21_filler as pf,phase14_factset as fs;print(set(pf.FILLER_SLOT_FORMS) & set(fs.SLOT_FORMS))"` prints `set()`
    - `.venv/bin/python -c "import sys;sys.path.insert(0,'scripts');import phase21_filler as pf;print({f.tier for f in pf.FILLER_FACTS})"` prints `{'filler'}`
    - Run this in TWO separate processes and diff the output — the fact ORDER must be identical:
      `.venv/bin/python -c "import sys;sys.path.insert(0,'scripts');import phase21_filler as pf;print([f.id for f in pf.FILLER_FACTS])"`
    - `.venv/bin/python -c "import ast;t=ast.parse(open('scripts/phase21_filler.py').read());print(sorted({(n.names[0].name if isinstance(n,ast.Import) else n.module) for n in ast.walk(t) if isinstance(n,(ast.Import,ast.ImportFrom))}))"` shows no `torch` and no `numpy`
    - `.venv/bin/python -m ruff check . && .venv/bin/python -m ruff format --check .`
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -c "import sys;sys.path.insert(0,'scripts');import phase21_filler as pf,phase14_factset as fs;assert len(pf.FILLER_FACTS)==56 and len(pf.FILLER_SLOT_FORMS)==8 and not (set(pf.FILLER_SLOT_FORMS)&set(fs.SLOT_FORMS));assert isinstance(pf.FILLER_FACTS, tuple);print('ok')"</automated>
  </verify>
  <done>56 filler facts in a stable declared order across 8 slots disjoint from the published 11, all `tier="filler"`, ids and values globally unique, no heavy imports.</done>
</task>

<task type="auto">
  <name>Task 2: The re-implemented minting discipline, and the recorded guessability waiver</name>
  <read_first>
    - scripts/phase14_factset.py:305-345 (`token_census` at `:313`, `normalize_for_match` at `:323`,
      `exact_match_clean` at `:334`)
    - scripts/teach_persona.py:247-253 — `render_episodes`, and the `sorted(family_ids)` at `:251`
    - scripts/phase14_factset_gate.py:1-70 (the guessability definition and `PROBE_SEEDS`)
    - scripts/phase21_filler.py (task 1)
    - .planning/phases/21-.../21-CONTEXT.md — D-17, D-18
  </read_first>
  <files>scripts/phase21_filler.py</files>
  <action>
Add the discipline. IMPORT the instruments, never copy them: `fs.token_census`,
`fs.normalize_for_match`. A second copy of an instrument is a SECOND instrument, and the day they
stop agreeing is the day a verdict depends on which one the caller reached.

Two module-level frozen sets, both DERIVED at import from `phase14_factset` and never retyped:
  - `FORBIDDEN_SCORED_VALUES` = normalized values of `fs.LOCKED_FACTS + fs.SOFT_TIER_FACTS`. Assert
    its length is 10 with the comment D-18 gives it: this is the leak vocabulary, and no tier is
    exempt from the scan. This is a NINTH site of the `== 10` wall and it belongs here, at the module
    that could break it.
  - `PUBLISHED_POOL_VALUES` = normalized values of every fact in `fs.all_pools()`. Do NOT hardcode
    28 — count it at runtime and record the observed count in a comment with the command that
    produced it. CONTEXT's 28 is the NON-LOCKED subtotal (12 rejected + 10 calibration + 6 register
    arm); `all_pools()`' union is a superset because `LOCKED_FACTS` are drawn from `CANDIDATE_POOL`,
    and the superset is the safer refusal.

`def refuse_collisions()` — runs at import, raising `SystemExit` naming the offending value, the set
it collided with, and the direction. Three refusals, each with its OWN message so a failure says
WHICH:
  1. against `FORBIDDEN_SCORED_VALUES` (the 10)
  2. against `PUBLISHED_POOL_VALUES`
  3. filler values against EACH OTHER
Every refusal is over the PROPERTY, not the NAME: check normalized SUBSTRING CONTAINMENT IN BOTH
DIRECTIONS (`a in b or b in a`), not string equality. `tests/test_phase14_scoring.py:352-358` records
why — a guard passed while its invariant was false because the predicate was whole-string equality
where the real leak was substring containment, and a taught pet name quoted three times inside a
1,302-character paragraph slipped through. `embedded_fact_values` and the extraction instrument both
scan by containment, so an equality-only refusal would mint a value that the leak scan later catches.

`def verify_round_trips(tok)` — for every filler fact, call `fs.token_census(tok, fact.value)` and
raise `SystemExit` naming the value if the round-trip flag is False. This half needs a tokenizer, so
it cannot run at import; the test in task 3 and the driver in plan 21-11 both call it with the frozen
`artifacts/tokenizer.json`. Cost: ZERO generations — `token_census` needs only the tokenizer.

`GUESSABILITY_WAIVER` — a module-level string constant, so the waiver is DATA a test can assert on
rather than a comment that can drift. It must state:
  - that the base-model guessability probe is deliberately NOT run for filler;
  - the reason: filler is never scored and never enters the leak vocabulary (D-18), so "the base
    already knew it" has nothing to corrupt;
  - the correction to the argument that was originally offered for it, recorded because a stated
    benefit was measured FALSE and the answer still stood: "zero extra base-model completion runs" is
    NOT a property of putting filler in its own module — `scripts/phase14_factset_gate.py:8` defines
    guessability as prompting the un-adapted `convbase_best.pt` and `fs.exact_match_clean` takes
    `completions`, so the cost attaches to DOING guessability at all, never to WHERE the code lives;
  - the measured price of the full probe had it been run: 8 questions/slot x `PROBE_SEEDS = 4` = 32
    generations per value, x 56 = **1,792 generations** on `convbase_best.pt`, about 4% of one
    Phase-18 arm's 42,480 draws.

Add `def render_filler_episodes(facts=FILLER_FACTS, family_ids=fs.TAUGHT_FAMILY_IDS)` returning the
`(question, answer)` pairs via `fs.render_family(fid, fact, forms=FILLER_SLOT_FORMS)` — the SAME
renderer over the SAME taught families as a scored fact (D-15), so filler is identical in FORM.

**Iterate `sorted(family_ids)`, mirroring `scripts/teach_persona.py:251` exactly, and iterate `facts`
in its declared tuple order.** This is not style. `TAUGHT_FAMILY_IDS` is a `frozenset`, and its raw
iteration order was MEASURED differing across three processes this session (`['F2','F6','F4','F5','F1']`,
`['F6','F2','F5','F4','F1']`, `['F4','F6','F1','F2','F5']`). Without `sorted()` the returned row
ORDER is process-dependent, the encoded n=64 bin is byte-different run to run, and every downstream
sha256 — the aligned bins in 21-04, the multiplicity record in 21-10/21-11 — becomes unreproducible
for a reason that never shows up as an error. Record that reason in the function's docstring, not
only here. `render_episodes` at `:251` already made this call for the scored path; this mirrors it.
  </action>
  <acceptance_criteria>
    - `.venv/bin/python -c "import sys;sys.path.insert(0,'scripts');import phase21_filler as pf;print(len(pf.FORBIDDEN_SCORED_VALUES), len(pf.PUBLISHED_POOL_VALUES))"` prints `10` and the observed pool count; record that count in the SUMMARY with this command
    - `.venv/bin/python -c "import sys;sys.path.insert(0,'scripts');import phase21_filler as pf,phase14_factset as fs;from personacore.tokenizer import from_json;import pathlib;tok=from_json(pathlib.Path('artifacts/tokenizer.json'));pf.verify_round_trips(tok);print('round-trips ok')"` exits 0
    - `.venv/bin/python -c "import sys;sys.path.insert(0,'scripts');import phase21_filler as pf;print(len(pf.render_filler_episodes()))"` — the per-fact row count for a FILLER fact has not been
      measured yet, so `56 * 22 == 1232` is an ESTIMATE, not a target. RECORD THE OBSERVED VALUE AND
      STOP if it differs: publish the observed total and the observed per-fact count, and do not
      reshape the grammar to land on 1,232. The binding constraint is the one that IS pre-registered —
      the per-fact count must sit inside `fs.PARAPHRASES_PER_FACT_TARGET == (20, 50)` — and task 3
      asserts that band plus equality with a `LOCKED_FACTS` member, which is the real D-15 claim.
    - CROSS-PROCESS DETERMINISM, run and recorded: in TWO separate processes emit
      `hashlib.sha256(json.dumps(pf.render_filler_episodes(), ensure_ascii=False, separators=(",",":")).encode()).hexdigest()`
      and assert the two digests are EQUAL. Then encode both to a bin with `tp.build_bins` under
      `tmp_path` and assert the token bins are byte-identical. A `sorted()` omission fails here and
      nowhere else.
    - DELIBERATE-RED, run and recorded: temporarily set one filler value to `"marrowgate"` (a scored
      value) — import must raise `SystemExit` from refusal 1 naming the value. Then set one to
      `"marrowgatex"` (containment, not equality) — import must STILL raise, which is the direct
      demonstration that the refusal is over the PROPERTY. Restore; `shasum -a 256 scripts/phase21_filler.py`
      equals the pre-mutation value and `git diff --exit-code scripts/phase21_filler.py` returns 0.
    - SECOND DELIBERATE-RED, run and recorded: replace `sorted(family_ids)` with `family_ids` in
      `render_filler_episodes` and re-run the cross-process digest check until it DIFFERS (the
      frozenset order varies per process, so repeat until two processes disagree — recording how many
      attempts it took is itself the evidence that this defect is intermittent and would not have been
      caught by a single-process test). Restore and prove sha256-equal.
    - `.venv/bin/python -m ruff check . && .venv/bin/python -m ruff format --check .`
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -c "import sys,pathlib,json,hashlib,subprocess;sys.path.insert(0,'scripts');import phase21_filler as pf;from personacore.tokenizer import from_json;pf.verify_round_trips(from_json(pathlib.Path('artifacts/tokenizer.json')));d=hashlib.sha256(json.dumps(pf.render_filler_episodes(),ensure_ascii=False,separators=(',',':')).encode()).hexdigest();o=subprocess.run([sys.executable,'-c','import sys,json,hashlib;sys.path.insert(0,\"scripts\");import phase21_filler as pf;print(hashlib.sha256(json.dumps(pf.render_filler_episodes(),ensure_ascii=False,separators=(\",\",\":\")).encode()).hexdigest())'],capture_output=True,text=True).stdout.strip();assert d==o,(d,o);print('ok',len(pf.render_filler_episodes()))"</automated>
  </verify>
  <done>The deterministic half of the discipline runs in full at import, the round-trip half runs on demand at zero generation cost, the waiver is a machine-readable constant, the rendered row order is process-stable and proven so by two separate processes, and containment-not-equality has been demonstrated by a watched RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The corpus tests — disjointness, discipline, and OUTSIDE all_pools()</name>
  <read_first>
    - scripts/phase21_filler.py (tasks 1 and 2)
    - tests/test_phase21_filler.py (plan 21-05 — extend it, do not replace it)
    - scripts/phase14_factset.py:120-135 (`all_pools`), :265-295 (`_assign_probes`, `GATE_PROBES`), :378-382 (`_BY_ID`)
  </read_first>
  <files>tests/test_phase21_filler.py</files>
  <behavior>
    - `set(FILLER_SLOT_FORMS) & set(fs.SLOT_FORMS) == set()` AND `set(FILLER_SLOT_FORMS) & set(fs.SLOT_QUESTION_BANK) == set()`
    - `{f.id for f in FILLER_FACTS} & {f.id for _n,p in fs.all_pools() for f in p} == set()`
    - `{f.id for f in FILLER_FACTS} & set(fs.GATE_PROBES) == set()`
    - `{f.id for f in FILLER_FACTS} & set(fs._BY_ID) == set()`
    - `len(fs.all_pools())` and the total pooled fact count are UNCHANGED by importing `phase21_filler`
    - every filler fact renders the SAME row count as a `LOCKED_FACTS` member over `TAUGHT_FAMILY_IDS`,
      and that count sits inside `PARAPHRASES_PER_FACT_TARGET` — the count is OBSERVED, never asserted
      against a literal
    - `render_filler_episodes()` is order-stable across two processes
    - `verify_round_trips(tok)` passes on the frozen tokenizer for all 56
    - the three collision refusals each fire on a synthetic offender and each name a DIFFERENT set
    - `len(FORBIDDEN_SCORED_VALUES) == 10` — this file's participation in the `== 10` wall
    - `GUESSABILITY_WAIVER` contains the string `1,792` and the phrase `convbase_best.pt`
  </behavior>
  <action>
Extend `tests/test_phase21_filler.py` (created in plan 21-05) — additively, leaving the
`render_family` identity/non-vacuity pair untouched.

`test_slots_disjoint` — both intersections above, plus the assertion that every filler slot key
starts with `filler_`. Note in the docstring that the prefix is a CONVENTION and the empty
intersection is the PROPERTY; assert both, and let the intersection be the one that fails on a
genuine collision.

`test_outside_all_pools` — the four set intersections above, plus a before/after check that importing
`phase21_filler` does not change `len(fs._BY_ID)` or `len(fs.GATE_PROBES)`. This is D-13 made
checkable: pool membership, not module location, is the gate, so the test asserts NON-membership
directly rather than trusting the file lives elsewhere.

`test_minting_discipline` — call `verify_round_trips` with the frozen tokenizer, then drive each of
the three collision refusals with a SYNTHETIC offender constructed in-test (a fact whose normalized
value contains a locked value; one that contains a published pool value; two filler facts where one
contains the other) and assert each raises with a message naming its OWN set. Three distinguishable
reds, never "something raised".

`test_filler_renders_identically_in_form_to_a_scored_fact` — this is the D-15 claim and it is a
COMPARISON, not a literal. For a `LOCKED_FACTS` member, measure `n_scored` = its row count over
`sorted(fs.TAUGHT_FAMILY_IDS)`. Then for EVERY filler fact assert its row count equals `n_scored` and
lies inside `fs.PARAPHRASES_PER_FACT_TARGET`. Do not hard-code 22: the number is whatever the two
grammars produce, and what D-15 requires is that they produce the SAME number. Put the observed
`n_scored`, the observed filler count, and the derived n=64 total in the failure message so a drift
reports numbers rather than a bare mismatch, and record all three in the SUMMARY.

`test_render_filler_episodes_is_order_stable` — spawn a second interpreter with
`subprocess.run([sys.executable, "-c", ...])`, have it print the sha256 of the serialized episode
list, and assert it equals this process's digest. One process can never catch a frozenset-order bug;
`TAUGHT_FAMILY_IDS` iterated raw was measured yielding three different orders in three runs.

`test_guessability_waiver_is_recorded` — assert `GUESSABILITY_WAIVER` names the price (`1,792`), the
model (`convbase_best.pt`), and the reason. A waiver that is not machine-checkable is a comment, and
D-17's whole point is that the waiver is a DECISION rather than a silence.
  </action>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest -q tests/test_phase21_filler.py -k slots_disjoint` passes, matching `21-VALIDATION.md:84`
    - `.venv/bin/python -m pytest -q tests/test_phase21_filler.py -k minting_discipline` passes, matching `21-VALIDATION.md:85`
    - `.venv/bin/python -m pytest -q tests/test_phase21_filler.py -k outside_all_pools` passes, matching `21-VALIDATION.md:86`
    - `.venv/bin/python -m pytest -q tests/test_phase21_filler.py -k order_stable` passes, and the
      SUMMARY records the shared digest
    - The SUMMARY records the OBSERVED `n_scored`, the OBSERVED filler per-fact count, and the n=64
      total row count — as measurements, against D-15's ~22 estimate. A divergence is published, not smoothed.
    - `.venv/bin/python -m pytest -q tests/test_phase21_filler.py` — all pass, including plan 21-05's
      `render_family_byte_identity` and `forms_is_wired`
    - The SC5 guard set, all 8 wall sites, still green:
      `.venv/bin/python -m pytest -q tests/test_phase14_scoring.py tests/test_phase16_driver.py tests/test_phase16_ladder.py tests/test_phase18_corpus.py tests/test_phase18_prereg.py tests/test_phase19_erasure.py tests/test_phase14_factset.py tests/test_phase14_demo.py`
    - `.venv/bin/python -m pytest -q tests/test_lora_inject.py tests/test_phase19_erasure.py tests/test_phase17_stats.py`
      green — the wide globs that pick up this new file. NOT a bare `pytest -q`: same-wave 21-06 and
      21-08 hold live canaries. The full suite is asserted at WAVE close (`21-VALIDATION.md:47,52`).
    - `git diff --exit-code scripts/phase18_extraction.py scripts/mitigation_gate.py scripts/mitigation_unit.py` returns 0
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_phase21_filler.py tests/test_phase14_scoring.py tests/test_phase16_driver.py tests/test_phase16_ladder.py tests/test_phase18_corpus.py tests/test_phase18_prereg.py tests/test_phase19_erasure.py tests/test_phase14_factset.py tests/test_phase14_demo.py</automated>
  </verify>
  <done>The corpus is provably disjoint, provably outside every published register, provably identical in FORM to a scored fact by measurement rather than by literal, provably order-stable across processes, and its three refusals have each been driven by a synthetic offender.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a new fact value -> every published instrument | The extraction instrument carries TWO fact surfaces: the taught/scored surface is `LOCKED_FACTS` only (8), while the leak-detection vocabulary is `LOCKED + SOFT` (10). A filler value reaching the second turns 8 assertions red AND would force an edit to the ancestry-guarded `scripts/phase18_extraction.py`, breaking SC5. |
| module location -> inherited discipline | `all_pools()` is what confers the Phase-14 gate, iterated 7x by `phase14_factset_gate.py`. Living in a new file inherits nothing. |
| an unordered container -> a byte-level guarantee | A `frozenset` iterated raw yields a different order per process. Every sha256 downstream of the n=64 bin inherits that nondeterminism, and it never surfaces as an error — only as an unreproducible artifact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-04 | Tampering (of the instrument) | a filler value enters the 10-value leak vocabulary, confounding the GATE-10 capacity comparison | mitigate | `FORBIDDEN_SCORED_VALUES` refusal at import over normalized CONTAINMENT in both directions; the full 8-site `== 10` wall re-run; plan 21-09's bidirectional `embedded_fact_values` scan is the independent second tier |
| T-21-30 | Tampering | a refusal over the NAME (string equality) while the harm is a PROPERTY (containment) | mitigate | the `"marrowgatex"` deliberate-RED — a value that equality would ADMIT and containment REFUSES, observed raising |
| T-21-31 | Tampering | filler seated in a slot GATE-10 scores, so n=64 recall falls from CONTENTION rather than CAPACITY | mitigate | 8 filler-only slots, `set(FILLER_SLOT_FORMS) & set(fs.SLOT_FORMS) == set()` asserted, and `forms=` missing-slot raises `KeyError` (plan 21-05) |
| T-21-32 | Spoofing | filler acquires the authority of a scored fact via a pool, `_BY_ID` or `GATE_PROBES` key | mitigate | four set-intersection assertions plus a before/after check that `len(fs._BY_ID)` and `len(fs.GATE_PROBES)` are unchanged |
| T-21-33 | Repudiation | the guessability waiver reads as an oversight rather than a decision | mitigate | `GUESSABILITY_WAIVER` is a module constant carrying its measured price (1,792 generations), asserted by `test_guessability_waiver_is_recorded` |
| T-21-34 | Tampering | filler and scored records differ in size under one clip norm, confounding N with per-record mass | mitigate | filler renders through the SAME `render_family` over `TAUGHT_FAMILY_IDS`; the row count is asserted EQUAL between a filler fact and a `LOCKED_FACTS` member and inside `PARAPHRASES_PER_FACT_TARGET`, both OBSERVED rather than compared to a literal |
| T-21-57 | Repudiation | `render_filler_episodes` iterates the `TAUGHT_FAMILY_IDS` frozenset raw, so the n=64 bin is byte-different every run and no sha256 downstream of it means anything | mitigate | `sorted(family_ids)` mirroring `teach_persona.py:251`; `test_render_filler_episodes_is_order_stable` compares digests across two interpreters; a deliberate-RED removes the `sorted()` and is watched producing two different digests |
| T-21-58 | Repudiation | an unmeasured row count (1,232) asserted as a target, so the grammar is reshaped to hit a number instead of the number being reported | mitigate | the 1,232 is labelled an ESTIMATE with a record-and-STOP clause; the binding assertion is EQUALITY with a scored fact's observed count and membership in `PARAPHRASES_PER_FACT_TARGET` |
| T-21-08 | Tampering | an edit reaches `scripts/phase18_extraction.py` or the FROZEN `scripts/mitigation_gate.py` | mitigate | `git diff --exit-code` on both |
| T-21-11 | Tampering | supply chain | accept | zero package installs; `tests/test_package.py:37` |
</threat_model>

<verification>
```bash
.venv/bin/python -m pytest -q tests/test_phase21_filler.py
.venv/bin/python -m pytest -q tests/test_phase14_scoring.py tests/test_phase16_driver.py tests/test_phase16_ladder.py tests/test_phase18_corpus.py tests/test_phase18_prereg.py tests/test_phase19_erasure.py tests/test_phase14_factset.py tests/test_phase14_demo.py
# NO bare `pytest -q` here. All three wave-3 plans (21-06, 21-07, 21-08) do WORKING-TREE
# deliberate-REDs on source their siblings' test runs import, and `pyproject` sets
# `testpaths = ["tests"]`, so a per-plan full suite would read a sibling's live canary and RED on
# the innocent plan. `21-VALIDATION.md:47,52` already sites the full suite at WAVE close and before
# `/gsd:verify-work` — not at plan close. Run it there, once wave 3 is complete and every canary is
# restored.
git diff --exit-code scripts/phase18_extraction.py scripts/mitigation_gate.py scripts/mitigation_unit.py
git status --porcelain results/             # MUST be empty
.venv/bin/python -m ruff check . && .venv/bin/python -m ruff format --check .
```
</verification>

<success_criteria>
- Exactly 56 filler facts, in a stable declared order, across 8 slots disjoint from the published 11; all `tier="filler"`
- Zero filler id or value reaches `all_pools()`, `_BY_ID`, `GATE_PROBES` or the 10-value leak vocabulary
- The deterministic discipline runs at import; the round-trip half runs at zero generation cost
- `render_filler_episodes` iterates `sorted(family_ids)` and two processes produce a byte-identical bin
- The filler per-fact row count is OBSERVED, equals a scored fact's, and sits inside `PARAPHRASES_PER_FACT_TARGET`
- The containment-not-equality refusal has been WATCHED refusing a value equality would admit
- `GUESSABILITY_WAIVER` carries its reason and its measured price
- All 8 `== 10` wall sites green; both frozen files byte-unchanged
</success_criteria>

<output>
Create `.planning/phases/21-the-privacy-unit-the-dp-data-path-and-the-n-64-corpus/21-07-SUMMARY.md` when done.
Record: the 56 filler ids and values in full and in order, the observed `len(PUBLISHED_POOL_VALUES)`
with its command, the OBSERVED per-fact taught row count for filler AND for a locked fact plus the
n=64 total (against D-15's ~22 estimate, published not smoothed), the cross-process digest of
`render_filler_episodes()`, and the three deliberate-RED observations (`marrowgate`, `marrowgatex`,
and the removed `sorted()`).
</output>
