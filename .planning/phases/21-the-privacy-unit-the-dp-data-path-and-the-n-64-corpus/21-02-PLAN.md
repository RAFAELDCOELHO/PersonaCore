---
phase: 21-the-privacy-unit-the-dp-data-path-and-the-n-64-corpus
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phase21_golden_capture.py
  - tests/fixtures/golden_build_bins_v2.json
  - tests/fixtures/golden_render_family_v2.json
autonomous: true
requirements: [UNIT-02, UNIT-06]

must_haves:
  truths:
    - "tests/fixtures/golden_build_bins_v2.json records the sha256 of the token and mask bins v2.0 produces TODAY, captured from a git-clean tree in which scripts/teach_persona.py is unedited"
    - "tests/fixtures/golden_render_family_v2.json records the sha256 of render_family's output over ALL 8 FAMILY_IDS x (LOCKED_FACTS + SOFT_TIER_FACTS) in BOTH registers, captured pre-edit"
    - "The capture script REFUSES to run if either source file is dirty in git — 'pre-edit' is mechanical, not a promise"
    - "Neither fixture depends on data/dialog_train.bin (gitignored, machine-local) — the build_bins capture runs at replay_ratio=0.0"
    - "Both fixtures carry the capture commit SHA and the tokenizer sha256, so a stale fixture is distinguishable from a code regression"
  artifacts:
    - path: "scripts/phase21_golden_capture.py"
      provides: "one-time capture of both v2.0 golden fixtures, with a git-cleanliness refusal"
    - path: "tests/fixtures/golden_build_bins_v2.json"
      provides: "the v2.0 build_bins byte-level baseline"
      contains: "token_bin_sha256"
    - path: "tests/fixtures/golden_render_family_v2.json"
      provides: "the v2.0 render_family byte-level baseline, both registers, all 8 families"
      contains: "first_person"
  key_links:
    - from: "scripts/phase21_golden_capture.py"
      to: "git status --porcelain"
      via: "subprocess refusal before any capture"
      pattern: "porcelain"
    - from: "tests/fixtures/golden_build_bins_v2.json"
      to: "artifacts/tokenizer.json"
      via: "meta.tokenizer_sha256"
      pattern: "tokenizer_sha256"
---

<objective>
Capture the two v2.0 golden fixtures BEFORE any source edit in this phase.

Purpose: `21-VALIDATION.md:125-128` states the constraint plainly — a golden fixture captured AFTER
the edit proves nothing. Plans 21-04 and 21-05 both add an `X=None` kwarg whose entire claim is
"byte-identical to v2.0 when `None`", and that claim is only checkable against bytes that predate the
kwarg. This plan is wave 1 for exactly that reason, and it makes the constraint MECHANICAL: the
capture script refuses to run against a dirty working tree.

Output: `scripts/phase21_golden_capture.py` and the two committed fixtures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-the-privacy-unit-the-dp-data-path-and-the-n-64-corpus/21-CONTEXT.md
@.planning/phases/21-the-privacy-unit-the-dp-data-path-and-the-n-64-corpus/21-VALIDATION.md
</context>

<interfaces>
<!-- Contracts the executor needs; do not go exploring. -->

`scripts/teach_persona.py`:
- `:99` `SEED = 1337`; `:100` `BLOCK_SIZE = 256`; `:123` `MASK_FRACTION_BAND = (0.15, 0.95)`
- `:92` `TOKENIZER_PATH = _REPO_ROOT / "artifacts" / "tokenizer.json"` (FROZEN — never retrain)
- `:247` `def render_episodes(facts, family_ids, *, second_person=False)`
- `:256` `def build_bins(tok, episodes, bin_path, mask_path, *, replay_ratio=0.0) -> dict`
        writes `ids_all.tofile(bin_path)` / `mask_all.tofile(mask_path)` at `:279-280`;
        returns the 12-key stats dict at `:311-324`.
- `:441` the loading idiom: `seed_everything(seed)` then `tok = from_json(TOKENIZER_PATH)`

`scripts/phase14_factset.py`:
- `:824` `def render_family(family_id, fact, *, second_person=False) -> list[tuple[str, str]]`
- `FAMILY_IDS` — all 8; `TAUGHT_FAMILY_IDS = frozenset({"F1","F2","F4","F5","F6"})` (`:816`);
  `HELDOUT_FAMILY_IDS = frozenset({"F3","F7","F8"})` (`:817`)
- `LOCKED_FACTS` (8, `:390-399`), `SOFT_TIER_FACTS` (2, `:410-413`)

`tests/test_package.py:34-35` — read files as BYTES, never text: a text read normalizes line endings
and a CRLF rewrite would pass a text-mode hash.

MEASURED baselines this phase must reproduce (D-01, from `21-CONTEXT.md:80`):
8 `LOCKED_FACTS` x 5 `TAUGHT_FAMILY_IDS` = 176 rows / 7,581 tokens. The `real`-arm capture below adds
`SOFT_TIER_FACTS`, so its totals will be LARGER — record whatever is measured, do not force a number.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: scripts/phase21_golden_capture.py with a git-cleanliness refusal</name>
  <read_first>
    - scripts/teach_persona.py:60-95 (imports, TOKENIZER_PATH), :247-324 (render_episodes, build_bins),
      :425-460 (build_arm_bins — the tokenizer-load idiom)
    - scripts/teach_persona.py:236-244 — `refuse_if_exists`, the repo's refuse-to-rerun helper
    - tests/test_package.py:1-40 — the bytes-not-text sha256 rule
  </read_first>
  <files>scripts/phase21_golden_capture.py</files>
  <action>
Create `scripts/phase21_golden_capture.py`. It is a ONE-TIME capture driver, deliberately OUTSIDE the
`scripts/mitigation_*.py` glob, so `json` and `subprocess` are available.

FIRST ACT, before any import of the modules being captured: assert the tree is clean for the two
files whose behaviour is being frozen. Run
`git status --porcelain -- scripts/teach_persona.py scripts/phase14_factset.py` and raise
`SystemExit` naming both paths if the output is non-empty. Message must say why: a golden fixture
captured after the edit proves nothing, and the whole point of this file is that the constraint is
mechanical rather than a promise.

Import `teach_persona as tp` and `phase14_factset as fs` via the `scripts/` sibling-import idiom
already used across this repo.

Capture recipe for `tests/fixtures/golden_build_bins_v2.json` — pin every input explicitly:
  - `tp.seed_everything(tp.SEED)` then `tok = tp.from_json(tp.TOKENIZER_PATH)`
  - `facts = fs.LOCKED_FACTS + fs.SOFT_TIER_FACTS`  (today's `arm_spec('real')` fact set, `teach_persona.py:418`)
  - `family_ids = fs.TAUGHT_FAMILY_IDS`
  - `second_person = False`
  - `replay_ratio = 0.0` — LOAD-BEARING. `_prepend_replay` reads `data/dialog_train.bin`, which is
    gitignored and machine-local (`.gitignore:17`), so a non-zero ratio would make the fixture
    unreproducible on CI. Record this reason in the script's docstring, not only here.
  - write the bins under `tempfile.TemporaryDirectory()`, never under `data/` and never at a
    `tp.arm_outputs(...)` path — this must not collide with recorded evidence.

Emit JSON with:
  `meta`: `captured_at_sha` (`git rev-parse HEAD`), `arm` `"real-facts-taught-families"`,
          `facts` (the tuple of fact ids), `family_ids` (sorted), `second_person`, `replay_ratio`,
          `tokenizer_sha256` (sha256 of `artifacts/tokenizer.json`, read as BYTES),
          `recipe` (the literal call expression, so a reader can re-derive without reading this script)
  `token_bin_sha256`, `mask_bin_sha256` — sha256 of the written files, read as BYTES
  `token_bin_bytes`, `mask_bin_bytes` — `os.path.getsize`
  `stats_repr` — `repr(stats)` of `build_bins`' returned dict

`tokenizer_sha256` lives in `meta` for a reason: the bins are a function of the tokenizer, and
`artifacts/tokenizer.json` is FROZEN. If it ever moves, the fixture is STALE rather than the code
being wrong, and the consuming test's message must be able to say which.

The two float stats (`float(np.mean(mask))` at `:270`, `float(np.mean(lengths))` at `:317`) are
float64 means over integer arrays — deterministic for a fixed array. They travel inside `stats_repr`
and are compared by exact `repr` equality. Record in the docstring that a drift there is a FINDING
worth surfacing, not noise worth tolerating, and that the bin sha256 values are asserted
unconditionally and separately so a float surprise cannot weaken the byte-level guarantee.

Refuse to overwrite an existing fixture: reuse `tp.refuse_if_exists` (import it, never copy it) and
name the delete command in the abort message.
  </action>
  <acceptance_criteria>
    - `.venv/bin/python scripts/phase21_golden_capture.py` exits 0 on a clean tree and writes both
      fixture paths
    - Re-running it exits non-zero via `refuse_if_exists`, naming both fixture files
    - With `scripts/teach_persona.py` deliberately dirtied (append a trailing comment), the script
      exits non-zero BEFORE writing anything, naming `scripts/teach_persona.py`. Restore with
      `git checkout scripts/teach_persona.py` and confirm `git diff --exit-code scripts/teach_persona.py` is 0.
    - `.venv/bin/python -c "import json;d=json.load(open('tests/fixtures/golden_build_bins_v2.json'));print(d['meta']['replay_ratio'], d['token_bin_bytes'], d['mask_bin_bytes'])"` prints `0.0` and two positive integers with `token_bin_bytes == 2 * mask_bin_bytes`
    - `grep -c "dialog_train" tests/fixtures/golden_build_bins_v2.json` returns 0
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -c "import json,pathlib;d=json.load(pathlib.Path('tests/fixtures/golden_build_bins_v2.json').open());assert d['token_bin_bytes']==2*d['mask_bin_bytes'];assert d['meta']['replay_ratio']==0.0;assert len(d['token_bin_sha256'])==64;print('ok',d['stats_repr'][:80])"</automated>
  </verify>
  <done>`tests/fixtures/golden_build_bins_v2.json` is committed, captured from a git-clean pre-edit tree, and reproducible without `data/`.</done>
</task>

<task type="auto">
  <name>Task 2: Capture golden_render_family_v2.json — all 8 families, BOTH registers</name>
  <read_first>
    - scripts/phase14_factset.py:755-780 (`_family_table`, `FAMILIES`, `FAMILIES_SECOND_PERSON`),
      :816-830 (`TAUGHT_FAMILY_IDS`, `HELDOUT_FAMILY_IDS`, `render_family`)
    - scripts/phase21_golden_capture.py (task 1)
    - .planning/phases/21-.../21-RESEARCH.md — the coverage argument in `## §V.4c`
  </read_first>
  <files>scripts/phase21_golden_capture.py, tests/fixtures/golden_render_family_v2.json</files>
  <action>
Extend the capture script with a second emitter for `tests/fixtures/golden_render_family_v2.json`.

Cover the FULL cross product, and record in the docstring why each axis is there rather than the
cheaper subset:
  - ALL 8 `fs.FAMILY_IDS`, not only the 5 taught. `HELDOUT_FAMILY_IDS = {F3, F7, F8}` feeds
    `heldout_questions()` (`phase14_factset.py:830-847`), which feeds the published held-out split.
    An additive edit that broke a held-out family would pass a taught-only fixture.
  - BOTH registers. `FAMILIES_SECOND_PERSON = _family_table(True)` (`:777`) is a live path for the
    `cal_second_person` arm (`teach_persona.py:411-412`), and `_family_table` closes over
    `second_person` PER FAMILY ID (`:763-769`) — so an additive kwarg must thread through BOTH
    closures or the second-person half silently keeps the old behaviour.
  - facts = `fs.LOCKED_FACTS + fs.SOFT_TIER_FACTS` (10), in that order.

For each register, build the ordered list
`[render_family(fid, fact, second_person=<register>) for fid in sorted(FAMILY_IDS) for fact in facts]`
and hash
`json.dumps(rendered, ensure_ascii=False, sort_keys=False, separators=(",", ":")).encode("utf-8")`.

Emit:
  `meta`: `captured_at_sha`, `family_ids` (all 8, sorted), `fact_ids` (the 10, in order),
          `serialization` (the literal `json.dumps` kwargs, so the consuming test cannot drift from
          the capture), `tokenizer_sha256` OMITTED here — `render_family` never touches the tokenizer,
          and including an irrelevant input would invite a false STALE reading later
  `first_person`:  `{"sha256": ..., "rows": <total pair count>}`
  `second_person`: `{"sha256": ..., "rows": <total pair count>}`

Assert at capture time that the two sha256 values DIFFER — if the two registers hashed identically
the fixture would be blind to a register bug, and that must fail here rather than pass silently.
  </action>
  <acceptance_criteria>
    - `.venv/bin/python -c "import json;d=json.load(open('tests/fixtures/golden_render_family_v2.json'));assert len(d['meta']['family_ids'])==8;assert len(d['meta']['fact_ids'])==10;assert d['first_person']['sha256']!=d['second_person']['sha256'];print(d['first_person'])"` exits 0
    - `git status --porcelain -- scripts/teach_persona.py scripts/phase14_factset.py` is EMPTY at the
      moment of capture (assert it, and record the `git rev-parse HEAD` in the SUMMARY)
    - Both fixtures are committed in a commit that is a strict ancestor of every commit produced by
      plans 21-04 and 21-05 — verify at plan close with
      `git log --oneline -1 -- tests/fixtures/golden_render_family_v2.json`
    - `.venv/bin/python -m ruff check scripts/ && .venv/bin/python -m ruff format --check scripts/`
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -c "import json;d=json.load(open('tests/fixtures/golden_render_family_v2.json'));assert len(d['meta']['family_ids'])==8 and len(d['meta']['fact_ids'])==10 and d['first_person']['sha256']!=d['second_person']['sha256'];print('ok')"</automated>
  </verify>
  <done>Both golden fixtures are committed, both carry their capture SHA, and the capture provably ran against an unedited `scripts/teach_persona.py` and `scripts/phase14_factset.py`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working tree -> committed baseline | A fixture captured from an edited tree encodes the NEW behaviour as the OLD baseline, silently converting every downstream identity assertion into a tautology. |
| machine-local `data/` -> CI | `data/` is gitignored, so any fixture depending on it is unreproducible where it matters most. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-05 | Repudiation | a golden fixture that certifies the change it was meant to detect | mitigate | the capture script refuses on non-empty `git status --porcelain -- scripts/teach_persona.py scripts/phase14_factset.py`, and the refusal itself is exercised in task 1's acceptance criteria |
| T-21-15 | Denial of Service | the fixture cannot be regenerated on CI because it reads `data/dialog_train.bin` | mitigate | `replay_ratio=0.0`; `grep -c "dialog_train"` on the fixture returns 0 |
| T-21-16 | Spoofing | a stale fixture read as a code regression | mitigate | `meta.tokenizer_sha256` and `meta.captured_at_sha` travel with the bytes so the consuming test's message can say WHICH |
| T-21-17 | Tampering | the capture overwrites recorded arm evidence under `data/` | mitigate | bins are written under `tempfile.TemporaryDirectory()`, never at a `tp.arm_outputs(...)` path; `refuse_if_exists` guards the fixtures themselves |
| T-21-11 | Tampering | supply chain | accept | zero package installs; `tests/test_package.py:37` sha256-pins `pyproject.toml` |
</threat_model>

<verification>
```bash
git status --porcelain -- scripts/teach_persona.py scripts/phase14_factset.py   # MUST be empty
.venv/bin/python -m pytest -q tests/test_package.py tests/test_phase14_teaching.py
git diff --exit-code scripts/mitigation_gate.py scripts/phase18_extraction.py   # MUST be 0
.venv/bin/python -m ruff check . && .venv/bin/python -m ruff format --check .
```
</verification>

<success_criteria>
- Both golden fixtures committed, each carrying `meta.captured_at_sha`
- `scripts/teach_persona.py` and `scripts/phase14_factset.py` are byte-unchanged by this plan
  (`git diff --exit-code` returns 0 for both)
- The dirty-tree refusal has been exercised and observed
- Neither fixture references `data/`
</success_criteria>

<output>
Create `.planning/phases/21-the-privacy-unit-the-dp-data-path-and-the-n-64-corpus/21-02-SUMMARY.md` when done.
Record: `captured_at_sha`, both bin sha256 values, both register sha256 values, the measured
`token_bin_bytes` / `mask_bin_bytes` / `stats_repr` episode+token counts, and the `tokenizer_sha256`.
</output>
