---
phase: 21-the-privacy-unit-the-dp-data-path-and-the-n-64-corpus
plan: 09
type: execute
wave: 5
# "21-10" is a SCHEDULING edge, not a data edge: this plan needs nothing 21-10 produces. It is
# declared because tasks 2 and 3 transiently mutate WORKING-TREE files this plan does NOT own
# (scripts/phase14_factset.py, scripts/phase21_filler.py, scripts/phase18_extraction.py) for
# deliberate-REDs, and 21-10's <verification> runs the FULL SUITE plus
# `git diff --exit-code scripts/phase18_extraction.py`. A transient mutation visible to a
# concurrent full-suite run is a flake, not a proof. Serializing behind 21-10 removes the overlap
# entirely. Consequence: 21-11 moves from wave 5 to wave 6.
depends_on: ["21-07", "21-08", "21-10"]
files_modified:
  - scripts/teach_persona.py
  - tests/test_phase21_sc5.py
# scripts/phase18_extraction.py, scripts/phase14_factset.py and scripts/phase21_filler.py are
# DELIBERATELY ABSENT from this list even though tasks 2 and 3 touch their working trees. They are
# not this plan's to edit: phase18_extraction.py is ANCESTRY-GUARDED
# (tests/test_phase16_prereg.py:322-403) and a COMMITTED edit reddens that guard irrecoverably via
# `adds[-1]`; phase14_factset.py belongs to plan 21-05 and phase21_filler.py to plan 21-07. Listing
# a guarded file under files_modified invites an executor to treat it as editable, which is the
# larger hazard. The concurrency problem it was previously declared to solve is solved by the
# depends_on edge above instead. Every mutation here is WORKING TREE ONLY and restored sha256-equal
# before the plan closes.
autonomous: true
requirements: [UNIT-06]

must_haves:
  truths:
    - "Both capacities are reachable from arm_spec: the n=8 arm draws LOCKED_FACTS ONLY and the n=64 arm draws 8 LOCKED + 56 filler — the soft tier is excluded ENTIRELY from both (D-14)"
    - "Both new arms carry replay_ratio = 0.0, because under D-10 replay LEAVES the teaching bin and arrives through train()'s seam — the teaching bin holds facts only, so grad_accum_steps = n_facts is literally true"
    - "No filler value reaches any published instrument, proven by the committed embedded_fact_values substring helper in BOTH directions"
    - "scripts/phase18_extraction.py is byte-unchanged at sha256 d2b44806a60228f0482851b737392299beef7206a93abdc2a2a0745204503d96 (SC5)"
    - "results/phase16_recall_sample.json is byte-unchanged at sha256 407c4b9304a74801ae15445360dcf3edc8002a65ce1011f9991c88b768307c55 (SC5)"
    - "All EIGHT len(...) == 10 wall sites across SEVEN files are sampled — not the four D-18 names nor the seven RESEARCH corrected to"
    - "Every deliberate-RED here mutates a WORKING TREE ONLY, is never committed, is restored sha256-equal, and runs in a wave where no concurrent plan reads those files"
  artifacts:
    - path: "scripts/teach_persona.py"
      provides: "the dp_n8 and dp_n64 arms in ARMS and arm_spec"
      contains: "dp_n64"
    - path: "tests/test_phase21_sc5.py"
      provides: "no_filler_leak (bidirectional), instruments_unchanged (two sha256 pins), the 8-site wall census"
  key_links:
    - from: "tests/test_phase21_sc5.py"
      to: "tests/test_phase14_scoring.py::embedded_fact_values"
      via: "an import of the committed helper, never a local `value in source` loop"
      pattern: "embedded_fact_values"
    - from: "scripts/teach_persona.py::arm_spec"
      to: "phase21_filler.FILLER_FACTS"
      via: "a lazy import inside the dp_n64 branch"
      pattern: "FILLER_FACTS"
---

<objective>
Make both capacities reachable, and prove the n=64 corpus disturbs no published instrument.

Purpose: SC5 is the constraint that makes the whole capacity comparison legitimate. The n=64 arm
measures extraction over the SAME 8 facts as n=8, with only the corpus around them changed — an
unconfounded comparison. That only holds if nothing else moved: not the 8 `LOCKED_FACTS`, not the
270-question fixture, not the ancestry-guarded `scripts/phase18_extraction.py`, and not the 10-value
leak vocabulary. "Unscored" therefore has a STRUCTURAL definition, not an intention (D-18).

Output: two new arms in `scripts/teach_persona.py` and `tests/test_phase21_sc5.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-the-privacy-unit-the-dp-data-path-and-the-n-64-corpus/21-CONTEXT.md
@.planning/phases/21-the-privacy-unit-the-dp-data-path-and-the-n-64-corpus/21-VALIDATION.md
</context>

<interfaces>
<!-- Contracts the executor needs; do not go exploring. -->

**LINE ANCHORS: resolve BY SYMBOL after waves 2 and 3 land.** Every `teach_persona.py:NN` below is
verified against the repo AS IT STANDS TODAY — before plan 21-04 (wave 2) adds `fact_bin_path` and
the aligned branch of `build_bins`, and before plan 21-08 (wave 3) adds the replay seam. Both shift
`ARMS`, `arm_outputs`, `arm_spec` and `USAGE` downward. Locate them BY NAME with grep; the numbers
say WHERE to look, not where they are. The `phase14_factset.py`, `mitigation_gate.py` and
`test_phase*.py` anchors are stable — no plan in this phase edits those files.

`scripts/teach_persona.py`:
- `:163` `ARMS = ("cal_first_person", "cal_first_person_replay", "cal_second_person", "real")`
- `:197` `arm_outputs(arm, *, prefix="phase14")` — scopes `bin`, `mask`, `csv`, `checkpoint`,
  `adapter` per arm name, with ONE deliberate exception (`real` -> `checkpoints/persona_adapter.pt`)
- `:405-422` `arm_spec(arm) -> (facts, second_person, replay_ratio)`; the `'real'` branch is
  `:413-421` and returns `fs.LOCKED_FACTS + fs.SOFT_TIER_FACTS` at `:418` — THE SIGNATURE D-14
  CHANGES. `:422` is the unknown-arm `SystemExit`.
- `:464` `USAGE` interpolates `ARMS`

`scripts/mitigation_gate.py:625` calls `n_facts=10, replay_ratio=1.0` *"the right REGIME, not a v4.0
ARM."* — the repo's own words for D-14. `scripts/phase14_factset.py:401-409` records that the soft
tier *"has NO BEARING on DEMO-06's taught or held-out thresholds and contributes nothing to the
headline claim."*

**The `== 10` wall is EIGHT sites across SEVEN files.** CONTEXT D-18 names four; `21-RESEARCH.md`
corrected it to seven-across-six; a direct grep run against this repo finds eight-across-seven:

| # | Site | Variable |
|---|------|----------|
| 1 | `tests/test_phase14_scoring.py:405` | `forbidden` |
| 2 | `tests/test_phase16_driver.py:313` | `forbidden` |
| 3 | `tests/test_phase16_ladder.py:443` | `forbidden` |
| 4 | `tests/test_phase16_ladder.py:711` | `forbidden` |
| 5 | `tests/test_phase18_prereg.py:127` | `forbidden` |
| 6 | `tests/test_phase19_erasure.py:625` | `forbidden` |
| 7 | `tests/test_phase18_corpus.py:430` | `values` |
| 8 | **`tests/test_phase14_demo.py:394`** | `values` — named in NEITHER CONTEXT.md NOR RESEARCH.md |

All eight construct the set the same way, `LOCKED_FACTS + SOFT_TIER_FACTS`, so all eight turn RED the
instant a filler fact enters either tier. Site 5 is the most valuable of the unnamed ones:
`tests/test_phase18_prereg.py:132` then scans every member of the `scripts/phase18_*.py` glob for
embedded fact values, so it is a COVERAGE-WIDENING guard, not merely a count.

`tests/test_phase14_scoring.py:349-364` — `embedded_fact_values(module, forbidden)`, the committed
helper. It takes a MODULE OBJECT (not a path) and does SUBSTRING CONTAINMENT over every string that
module holds, including strings nested in tuples and dicts, returning `(value, count)` pairs. Its
docstring records exactly why equality was not enough: a taught pet name quoted three times inside a
1,302-character report paragraph passed a whole-string equality predicate while the invariant was
false. That is precisely the shape a filler leak would take. `:398-402` is the committed idiom for
loading a module from a path (`importlib.util.spec_from_file_location` + `exec_module`) — reuse it
rather than inventing one.

MEASURED THIS SESSION, both with `shasum -a 256`:
```
d2b44806a60228f0482851b737392299beef7206a93abdc2a2a0745204503d96  scripts/phase18_extraction.py
407c4b9304a74801ae15445360dcf3edc8002a65ce1011f9991c88b768307c55  results/phase16_recall_sample.json
```
`results/phase16_recall_sample.json` is tracked in git and is THE binding 270-question evaluation
fixture. `tests/test_package.py:34-35` records the bytes-not-text rule: a text read normalizes line
endings, so a CRLF rewrite would pass a text-mode hash.

**Wave placement, and why it is part of the contract rather than scheduling trivia.** This plan runs
in WAVE 5, behind 21-10, because tasks 2 and 3 transiently mutate WORKING-TREE files this plan does
not own and 21-10's verification runs the full suite plus `git diff --exit-code` on one of them. The
three mutated paths are `scripts/phase14_factset.py` (owned by 21-05), `scripts/phase21_filler.py`
(owned by 21-07) and `scripts/phase18_extraction.py` (owned by NOBODY — it is ANCESTRY-GUARDED).
None of the three appears in this plan's `files_modified`, deliberately: a guarded file listed there
reads as permission to edit it, and a COMMITTED edit to `scripts/phase18_extraction.py` reddens
`tests/test_phase16_prereg.py:322-403` permanently through `adds[-1]`, which `git rm` + re-add
cannot launder (`tests/test_phase20_prereg.py:157`).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: The dp_n8 and dp_n64 arms — the soft tier excluded entirely</name>
  <read_first>
    - scripts/teach_persona.py:160-235 (`ARMS`, `_require_go_verdict`, `arm_outputs`), :405-470
      (`arm_spec`, `build_arm_bins`, `USAGE`)
    - scripts/phase21_filler.py — `FILLER_FACTS` (plan 21-07)
    - .planning/phases/21-.../21-CONTEXT.md — D-10, D-12, D-14, D-15
  </read_first>
  <files>scripts/teach_persona.py</files>
  <action>
Extend `ARMS` (`:163`) with `"dp_n8"` and `"dp_n64"`, and add two branches to `arm_spec` (`:405`).
Do not touch the four existing branches — the `real` arm's `fs.LOCKED_FACTS + fs.SOFT_TIER_FACTS`
at `:418` is a RECORDED v3.0 composition and its bins are committed evidence.

  - `dp_n8`  -> `(fs.LOCKED_FACTS, False, 0.0)`
  - `dp_n64` -> `(fs.LOCKED_FACTS + phase21_filler.FILLER_FACTS, False, 0.0)`

Use a LAZY import of `phase21_filler` inside the `dp_n64` branch rather than a module-scope import.
Record why: it keeps `teach_persona`'s import graph unchanged for every existing consumer, and the
filler module's import-time collision refusal still runs unconditionally in CI through
`tests/test_phase21_filler.py` and `tests/test_phase21_sc5.py`. Nothing is lost and one coupling is
avoided.

New arms rather than an `arm_spec(..., n_facts=)` parameter, and the reason is worth one line in the
docstring: `arm_outputs` (`:197`) already scopes `bin`, `mask`, `csv`, `checkpoint` and `adapter` by
arm NAME, so two arm names get disjoint paths and `refuse_if_exists` protection for free, while an
`n_facts` axis `arm_outputs` does not know about would collide with the `real` arm's recorded bins.

`replay_ratio = 0.0` on BOTH new arms is LOAD-BEARING, not a default. Record it inline: under D-10
replay LEAVES the teaching bin entirely and is drawn at TRAIN time from `data/dialog_train.bin` via
`train()`'s replay seam (plan 21-08) at the public volume `replay_window_budget(n_facts)`. The
teaching bin therefore holds FACTS ONLY, which is what makes `grad_accum_steps = n_facts` literally
true with no roadmap amendment. A non-zero `replay_ratio` here would put replay back in the bin and
silently reintroduce D-09's measured consequence: 7,581 replay tokens is roughly 30 windows against
33 fact windows, so an aligned loader turning every window into a micro-step would give
`grad_accum_steps = 63`, not 8 — falsifying SC2 by about 7.9x.

The soft tier is excluded ENTIRELY (D-14). Record the load-bearing reason rather than the weaker one:
not "v3.0's composition choice is not intrinsic to v4.0", but that v4.0 ALREADY PRE-REGISTERED its
small capacity as literally 8 — `REQUIREMENTS.md:84` (GATE-10, `[x]` COMPLETE inside the FROZEN
`scripts/mitigation_gate.py`), `REQUIREMENTS.md:173` / `ROADMAP.md:430-433` (CAL-03),
`REQUIREMENTS.md:206` (FRONT-01), and `ROADMAP.md:52`'s pre-registered null at L=8 facts — so n=10
contradicts GATE-10, CAL-03 and FRONT-01 at once. Note the free second benefit: exclusion keeps D-01
and D-02 valid EXACTLY AS MEASURED, both of which benchmarked 8 facts at ragged `(4,4,4,4,4,5,4,4)`;
at n=10 both soft facts are 5-window and both measurements would have needed redoing.

Update `USAGE` (`:464`) only insofar as it interpolates `ARMS` automatically — do not hand-edit the
string.
  </action>
  <acceptance_criteria>
    - `.venv/bin/python -c "import sys;sys.path.insert(0,'scripts');import teach_persona as tp;f8,_,r8=tp.arm_spec('dp_n8');f64,_,r64=tp.arm_spec('dp_n64');print(len(f8),r8,len(f64),r64)"` prints `8 0.0 64 0.0`
    - `.venv/bin/python -c "import sys;sys.path.insert(0,'scripts');import teach_persona as tp,phase14_factset as fs;f64,_,_=tp.arm_spec('dp_n64');print(set(fs.SOFT_TIER_FACTS) & set(f64))"` prints `set()`
    - `.venv/bin/python -c "import sys;sys.path.insert(0,'scripts');import teach_persona as tp;print(tp.arm_outputs('dp_n8')['bin'] != tp.arm_outputs('dp_n64')['bin'] != tp.arm_outputs('real')['bin'])"` prints `True`
    - `.venv/bin/python -m pytest -q tests/test_phase14_teaching.py tests/test_phase21_replay_volume.py` — green
    - `git diff scripts/teach_persona.py` shows the `real` branch at `:413-421` UNCHANGED
    - `.venv/bin/python -m ruff check . && .venv/bin/python -m ruff format --check .`
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -c "import sys;sys.path.insert(0,'scripts');import teach_persona as tp,phase14_factset as fs;f8,_,r8=tp.arm_spec('dp_n8');f64,_,r64=tp.arm_spec('dp_n64');assert len(f8)==8 and len(f64)==64 and r8==0.0 and r64==0.0 and not (set(fs.SOFT_TIER_FACTS)&set(f64));print('ok')"</automated>
  </verify>
  <done>Both capacities are reachable, both at `replay_ratio = 0.0`, the soft tier is in neither, and the recorded `real` arm is untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: no_filler_leak — the bidirectional substring scan, using the committed helper</name>
  <read_first>
    - tests/test_phase14_scoring.py:340-410 — `embedded_fact_values`, its recorded
      equality-vs-containment lesson, the `spec_from_file_location` module-loading idiom at
      `:398-402`, and wall site 1 at `:405`
    - scripts/phase21_filler.py — `FILLER_FACTS` (plan 21-07)
    - scripts/phase14_factset.py:120-135 (`all_pools`), :285-295 (`GATE_PROBES`)
    - .planning/phases/21-.../21-RESEARCH.md — `## §V.6`
  </read_first>
  <files>tests/test_phase21_sc5.py</files>
  <behavior>
    - Direction 1: no filler value appears inside the SOURCE of `scripts/phase18_extraction.py`,
      `scripts/mitigation_gate.py`, `scripts/mitigation_unit.py`, `scripts/phase14_factset.py`,
      `scripts/erasure_gate.py`, `scripts/phase19_erasure.py`
    - Direction 2: no filler value appears in the raw TEXT of `results/phase16_recall_sample.json`
    - Direction 3 (the reverse): no scored or soft value appears inside `scripts/phase21_filler.py`
    - Direction 4: no filler id reaches `all_pools()`, `_BY_ID` or `GATE_PROBES`
    - `len(filler) == 56` and `len(set(filler)) == 56`
    - `len(forbidden) == 10` — this file is the NINTH participant in the wall, and the census below
      EXCLUDES it by construction so the two do not contradict each other
  </behavior>
  <action>
Create `tests/test_phase21_sc5.py`. CPU-only, GPU-free, no model, no generation, sub-second.

IMPORT `embedded_fact_values` from `tests/test_phase14_scoring.py` rather than writing a
`value in source` loop. `21-RESEARCH.md`'s Don't-Hand-Roll table names it explicitly: the committed
helper walks nested tuples and dicts, returns `(value, count)`, and already learned the
equality-vs-containment lesson the hard way. A local variant is a SECOND instrument free to drift.
It takes a MODULE OBJECT, so load each scanned path with the committed
`importlib.util.spec_from_file_location` + `exec_module` idiom at `tests/test_phase14_scoring.py:398-402`.

`test_no_filler_leak` — the four directions above, each with its OWN assertion and its own message so
a failure says WHICH direction and WHICH file. Lowercase both sides before comparing, matching the
helper's own convention.

Direction 3 is where D-17's collision refusal gets its INDEPENDENT check: `phase21_filler`'s
import-time refusal proves the values were minted correctly from INSIDE the module that implements
it; this assertion proves the same property from OUTSIDE. Two tiers, and the outside one survives a
future edit that weakens the inside one.

Both directions matter and are different defects: "filler leaks into an instrument" and "a scored
value leaks into filler" are not the same failure, and only ONE of them is caught by the existing
`== 10` wall.

Add `test_wall_census_is_eight_sites` — grep `tests/` for `len(forbidden) == 10` and
`len(values) == 10` and assert the discovered site count is exactly 8 across 7 files, listing them.
Record in the docstring that CONTEXT.md named four and RESEARCH.md corrected it to seven, and that a
plan sampling fewer under-samples the wall SC5 rests on. If the discovered count is not 8, FAIL with
the observed list rather than adjusting the expected number — a new site is a finding, and a
disappeared site is a regression.

**THE CENSUS MUST EXCLUDE THE FILE IT LIVES IN, and the exclusion must be MECHANICAL — one
`if path.name == pathlib.Path(__file__).name: continue` in the walk, never a comment.** Without it
this test cannot pass as written and never could: the same task requires `test_phase21_sc5.py` to
carry `len(forbidden) == 10` as its own participation in the wall, so the census would discover
itself and measure 9 sites across 8 files against a hard-coded 8/7 — with an explicit instruction
NOT to adjust the number. Measured today: `grep -rn "len(forbidden) == 10\|len(values) == 10" tests/`
returns **8 hits across 7 files**; adding this file makes it 9/8.

Chosen over the alternative — re-siting the wall assertion in the non-matching form plan 21-05 uses
(`len(fs.LOCKED_FACTS + fs.SOFT_TIER_FACTS) == 10`, which genuinely does not match either grep
pattern) — because that form dodges the census by ACCIDENT of phrasing. A future edit rewording it
to the matching form would silently break the census again. `__file__` exclusion is explicit about
what the census measures: **the PRE-EXISTING wall, not this phase's additions to it.** Say that in
the docstring.

RECORD, as an INFO the census deliberately does NOT count: a NINTH assertion of the same class
exists and is named in no document — `tests/test_phase14_demo.py:568`,
`assert len(result["values"]) == 10  # all 8 locked + both soft — no tier is exempt`. It matches
NEITHER grep pattern (the expression is `len(result["values"])`), so the 8/7 count stays internally
consistent, and it IS executed, because the SC5 guard set runs that whole file. Record it in the
docstring so the next reader meets it as a known fact rather than re-discovering it as a regression.
  </action>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest -q tests/test_phase21_sc5.py -k no_filler_leak` passes, matching `21-VALIDATION.md:87`
    - `.venv/bin/python -m pytest -q tests/test_phase21_sc5.py -k wall_census` passes and its failure
      message would list every discovered site
    - DELIBERATE-RED, run and recorded: append a comment containing one filler value to
      `scripts/phase14_factset.py`. Direction 1 must go RED naming that file and that value.
      Restore with `git checkout scripts/phase14_factset.py`; `shasum -a 256` equals the
      pre-mutation value and `git diff --exit-code` returns 0.
    - SECOND DELIBERATE-RED: temporarily add a `Fact` to `FILLER_FACTS` in
      `scripts/phase21_filler.py` whose value CONTAINS a locked value. Confirm BOTH tiers fire —
      `phase21_filler`'s import-time refusal AND direction 3 — so the two-tier claim is observed
      rather than asserted. Restore and prove sha256-equal.
    - **WORKING TREE ONLY. DO NOT COMMIT EITHER MUTATION.** Neither `scripts/phase14_factset.py` nor
      `scripts/phase21_filler.py` is this plan's to edit — 21-05 and 21-07 own them respectively, and
      both landed in earlier waves. This plan runs in wave 5, behind 21-10, so no concurrent plan is
      reading either file while the mutation is live. `git status --porcelain` for both paths must be
      EMPTY before this task is marked done.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_phase21_sc5.py tests/test_phase21_filler.py</automated>
  </verify>
  <done>The leak scan covers both directions with the committed helper, the wall census is measured at 8 sites, both tiers have been watched firing on a synthetic leak, and both mutated working trees are restored sha256-equal and uncommitted.</done>
</task>

<task type="auto">
  <name>Task 3: instruments_unchanged — two sha256 pins and the full 8-site wall run</name>
  <read_first>
    - tests/test_package.py:1-40 — the bytes-not-text sha256 pin, the exact shape to copy
    - tests/test_phase16_prereg.py:49-95, :322-403 — the Phase 18 ancestry guard, the SECOND tier
      protecting `scripts/phase18_extraction.py`
    - .planning/phases/21-.../21-RESEARCH.md — `## §V.6`, "The byte-unchanged half"
  </read_first>
  <files>tests/test_phase21_sc5.py</files>
  <action>
Add `test_frozen_instruments_are_byte_unchanged` to `tests/test_phase21_sc5.py`, with the two
constants MEASURED against this repository:

  `P18_SHA256     = "d2b44806a60228f0482851b737392299beef7206a93abdc2a2a0745204503d96"`  # scripts/phase18_extraction.py
  `FIXTURE_SHA256 = "407c4b9304a74801ae15445360dcf3edc8002a65ce1011f9991c88b768307c55"`  # results/phase16_recall_sample.json

Read both as BYTES, never as text — copy the rule verbatim from `tests/test_package.py:34-35`, which
already records why: a text read normalizes line endings, so a CRLF rewrite would pass a text-mode
hash. Put that reason in this test's docstring too; a rule whose reason lives only in another file
drifts the first time someone edits this one.

Record what makes this pin STRONGER than the leak scan and why both are kept: the sha256 fails on ANY
edit for ANY reason, which is exactly what SC5's "unchanged" asks for, while `embedded_fact_values`
catches a leak that reached a file NOT in the pin list. Complementary, not redundant.

Record the SECOND TIER for `scripts/phase18_extraction.py`: it is ancestry-guarded via
`PHASE18_PREREG_ARTIFACT` (`tests/test_phase16_prereg.py:80`) and the live ordering guard at
`:322-403`, so an edit that is COMMITTED turns that guard permanently red as well. Two independent
tiers fire on one byte.

Add `test_locked_and_soft_tiers_are_unmoved` — assert `len(fs.LOCKED_FACTS) == 8`,
`len(fs.SOFT_TIER_FACTS) == 2`, the 8 locked facts are one-per-distinct-slot, and the ordered tuple
of locked fact ids is exactly the eight named at `phase14_factset.py:390-399`. `tests/test_phase14_factset.py:101-103`
caps these with inequalities (`<= 8`, `<= 3`); this asserts the EXACT composition, which is what
SC5's "the 8 `LOCKED_FACTS` ... unchanged" actually says.

DELIBERATE-RED, run and recorded: append `# canary` to `scripts/phase18_extraction.py` in the
WORKING TREE ONLY. `test_frozen_instruments_are_byte_unchanged` must go RED on `P18_SHA256`. Restore
with `git checkout scripts/phase18_extraction.py`; `shasum -a 256` equals `P18_SHA256` and
`git status --porcelain scripts/phase18_extraction.py` is EMPTY.

**Three constraints on that canary, all load-bearing:**
  1. **DO NOT COMMIT IT.** `scripts/phase18_extraction.py` is ancestry-guarded; a committed edit
     reddens `tests/test_phase16_prereg.py:322-403` permanently through `adds[-1]`, and `git rm` +
     re-add cannot launder it (`tests/test_phase20_prereg.py:157`).
  2. **Do not add the file to this plan's `files_modified`.** It is nobody's to edit; listing it
     there reads as permission. The frontmatter records this explicitly.
  3. **Run it only in this wave.** This plan sits in wave 5, behind 21-10, precisely because 21-10's
     `<verification>` runs the full suite and `git diff --exit-code scripts/phase18_extraction.py`.
     A canary live during a concurrent full-suite run is a flake attributed to the wrong plan. Do not
     re-parallelize this plan back into wave 4.
  </action>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest -q tests/test_phase21_sc5.py -k instruments_unchanged` passes,
      matching `21-VALIDATION.md:88-89`
    - The deliberate-RED on `scripts/phase18_extraction.py` was observed and its restore verified by
      BOTH `shasum -a 256` equality to `P18_SHA256` and an empty `git status --porcelain` for that path
    - `git log --oneline -1 -- scripts/phase18_extraction.py` names a commit that PREDATES this phase —
      proof the canary was never committed
    - The full SC5 guard set, all 8 wall sites across 7 files, green:
      `.venv/bin/python -m pytest -q tests/test_phase14_scoring.py tests/test_phase16_driver.py tests/test_phase16_ladder.py tests/test_phase18_corpus.py tests/test_phase18_prereg.py tests/test_phase19_erasure.py tests/test_phase14_factset.py tests/test_phase14_demo.py`
    - `.venv/bin/python -m pytest -q tests/test_phase16_prereg.py` green — the Phase 18 ancestry tier
    - `.venv/bin/python -m pytest -q` full suite green (the 1 platform-gated skip is expected)
    - `git diff --exit-code scripts/phase18_extraction.py scripts/mitigation_gate.py scripts/mitigation_unit.py results/phase16_recall_sample.json` returns 0
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_phase21_sc5.py tests/test_phase14_scoring.py tests/test_phase16_driver.py tests/test_phase16_ladder.py tests/test_phase18_corpus.py tests/test_phase18_prereg.py tests/test_phase19_erasure.py tests/test_phase14_factset.py tests/test_phase14_demo.py tests/test_phase16_prereg.py</automated>
  </verify>
  <done>Both frozen instruments are sha256-pinned and byte-unchanged, the locked/soft composition is asserted exactly, and the pin has been watched firing on a one-byte working-tree edit that was never committed and ran with no concurrent reader.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a new fact tier -> a published instrument | The extraction instrument carries TWO surfaces: taught/scored = `LOCKED_FACTS` only (8, `phase18_extraction.py:865-867,1184,3249,3594`), leak vocabulary = `LOCKED + SOFT` (10, `:872,3137,3598,4474`). A filler value in the second turns 8 assertions RED and would force an edit to an ancestry-guarded file. |
| a working-tree edit -> an irrevocable git record | Committing a mutation to `scripts/phase18_extraction.py` reddens the Phase 18 ancestry guard permanently; `adds[-1]` means `git rm` + re-add cannot undo it. |
| a transient mutation -> a concurrent reader | A working-tree canary is invisible to the scheduler unless something serializes it. File ownership does not, because the plan that READS the file need not DECLARE it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-04 | Tampering (of the instrument) | a filler value reaches the 10-value leak vocabulary, confounding GATE-10 | mitigate | bidirectional `embedded_fact_values` scan over six instrument sources plus the 270-question fixture text; all 8 wall sites re-run; `phase21_filler`'s import-time refusal is the independent second tier |
| T-21-39 | Tampering | a scored value leaks INTO the filler module — a different defect the `== 10` wall cannot see | mitigate | direction 3, asserted from outside the module that implements the refusal |
| T-21-08 | Tampering | any edit to `scripts/phase18_extraction.py` or `results/phase16_recall_sample.json` | mitigate | two sha256 pins read as BYTES; the Phase 18 ancestry guard at `test_phase16_prereg.py:322-403` is the second tier; the pin is WATCHED firing on `# canary` |
| T-21-55 | Denial of Service | a transient working-tree canary is live while a CONCURRENT same-wave plan runs the full suite or `git diff --exit-code` on that path, producing a flake attributed to the innocent plan | mitigate | this plan is serialized into wave 5 behind 21-10 by an explicit `depends_on` edge; the reason is recorded in the frontmatter, in `<interfaces>`, and in 21-10's `<verification>` block so neither side can silently re-parallelize |
| T-21-56 | Spoofing | an ancestry-guarded file listed in `files_modified` reads as permission to edit it | mitigate | `scripts/phase18_extraction.py`, `scripts/phase14_factset.py` and `scripts/phase21_filler.py` are DELIBERATELY absent from `files_modified`, with the reason recorded there; the `depends_on` edge carries the scheduling duty instead |
| T-21-40 | Tampering | a CRLF rewrite passes a text-mode hash | mitigate | files read as bytes, with the reason copied into this test's docstring rather than cited to another file |
| T-21-41 | Repudiation | sampling only D-18's four wall sites, leaving three of eight unsampled including the coverage-widening `test_phase18_prereg.py:127` | mitigate | `test_wall_census_is_eight_sites` discovers them by grep and FAILS with the observed list if the count moves |
| T-21-42 | Tampering | the n=64 arm silently reintroduces replay into the teaching bin, making `grad_accum_steps = 63` not 64 | mitigate | both new arms pin `replay_ratio = 0.0` and the acceptance criterion asserts it; D-09's measured 7.9x falsification is recorded inline |
| T-21-43 | Spoofing | a mutation committed rather than restored, permanently reddening an ancestry guard | mitigate | all three deliberate-REDs are explicitly working-tree-only, with `git status --porcelain` asserted empty after restore and `git log --oneline -1` asserted to predate this phase |
| T-21-11 | Tampering | supply chain | accept | zero package installs; `tests/test_package.py:37` |
</threat_model>

<verification>
```bash
.venv/bin/python -m pytest -q tests/test_phase21_sc5.py tests/test_phase21_filler.py
.venv/bin/python -m pytest -q tests/test_phase14_scoring.py tests/test_phase16_driver.py tests/test_phase16_ladder.py tests/test_phase18_corpus.py tests/test_phase18_prereg.py tests/test_phase19_erasure.py tests/test_phase14_factset.py tests/test_phase14_demo.py
.venv/bin/python -m pytest -q               # full suite
git diff --exit-code scripts/phase18_extraction.py scripts/mitigation_gate.py scripts/mitigation_unit.py results/phase16_recall_sample.json scripts/phase14_factset.py scripts/phase21_filler.py
git status --porcelain scripts/phase18_extraction.py scripts/phase14_factset.py scripts/phase21_filler.py   # MUST be empty — every canary restored
git ls-files 'results/phase21_*'            # MUST still be empty
.venv/bin/python -m ruff check . && .venv/bin/python -m ruff format --check .
```
</verification>

<success_criteria>
- `arm_spec('dp_n8')` returns 8 facts and `arm_spec('dp_n64')` returns 64, both at `replay_ratio=0.0`,
  the soft tier in neither
- No filler value reaches any published instrument, and no scored value reaches the filler module
- Both sha256 pins hold; both frozen files byte-unchanged
- The wall census is measured at 8 sites across 7 files, and all 8 are green
- Three deliberate-REDs observed and restored, none committed, none run beside a concurrent reader
</success_criteria>

<output>
Create `.planning/phases/21-the-privacy-unit-the-dp-data-path-and-the-n-64-corpus/21-09-SUMMARY.md` when done.
Record: the observed wall-site list from `test_wall_census_is_eight_sites`, the three deliberate-RED
messages, the post-restore `shasum -a 256` for every mutated file, and the
`git log --oneline -1 -- scripts/phase18_extraction.py` output proving no canary was committed.
</output>
