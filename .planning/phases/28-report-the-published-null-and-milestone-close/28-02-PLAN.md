---
phase: 28-report-the-published-null-and-milestone-close
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/quick/260819-r1u-close-b1-readme-v3-catchup/SUMMARY.md
  - .planning/quick/260819-sgh-close-w2-b1a-b1b/SUMMARY.md
  - .planning/quick/260902-dlo-pin-model-slim-pt-release-sha256-in-fetc/SUMMARY.md
  - .planning/debug/draw-all-utf8-decode-crash.md
  - .planning/milestones/v3.0-phases/19-selective-memory-erasure/19-08-PLAN.md
  - .planning/milestones/v3.0-phases/19-selective-memory-erasure/19-09-PLAN.md
  - .planning/milestones/v3.0-phases/19-selective-memory-erasure/19-12-PLAN.md
  - .planning/milestones/v3.0-phases/19-selective-memory-erasure/19-13-PLAN.md
  - .planning/milestones/v3.0-phases/19-selective-memory-erasure/19-16-PLAN.md
  - .planning/phases/25-frontier-sweep-and-the-existence-gate-verdict/25-HUMAN-UAT.md
  - .planning/phases/24-adversarial-extraction-aware-training-the-held-out-attack-fa/24-HUMAN-UAT.md
autonomous: false
requirements: [RPT-03]
must_haves:
  truths:
    - "Only artifacts that make a GSD tool report an ACTIVE error are edited: the three quick-task SUMMARYs (renamed to `SUMMARY.md`, `status: complete`), the debug session stamp, the six stale Phase-19 PLAN artifact names plus the 19-16 casing pattern; `19-13-SUMMARY.md`'s stray tags, the 17-digit percentage, and the archived 17/18/19 VERIFICATION stamps are NOT edited (D-31, D-39)"
    - "Every stamp change cites the commit(s) that prove the work is complete, quoted in the SUMMARY before the edit; anything that cannot be evidenced is left for a `RE-DEFERRED` row (D-39)"
    - "`gsd-sdk query audit-open` after this plan no longer lists the debug session or the three quick tasks; `verify.artifacts` on the five Phase-19 plans reports every tracked `results/` path as existing; the two gitignored `checkpoints/*.pt` entries are recorded, not renamed (D-31)"
    - "`25-HUMAN-UAT.md` frontmatter reads `status: complete` with zero pending items (D-36)"
    - "`24-HUMAN-UAT.md` items 2 and 3 carry dated resolutions — item 2 a traceability note naming `scripts/phase24_adversarial.py:289-292` and `:300`, item 3 `CLOSED-EARLIER` by `scripts/phase25_record.py:553` / `:574` — requirement prose untouched; its `status:` stamp is set by the developer's ruling at the checkpoint (D-37)"
    - "`23-VERIFICATION.md` and `27-VERIFICATION.md` still read `status: human_needed` (D-35)"
  artifacts:
    - path: ".planning/quick/260819-r1u-close-b1-readme-v3-catchup/SUMMARY.md"
      provides: "renamed quick-task summary with status: complete"
      contains: "status: complete"
    - path: ".planning/debug/draw-all-utf8-decode-crash.md"
      provides: "resolved debug session"
      contains: "status: resolved"
    - path: ".planning/phases/25-frontier-sweep-and-the-existence-gate-verdict/25-HUMAN-UAT.md"
      provides: "D-36 stamp"
      contains: "status: complete"
  key_links:
    - from: ".planning/milestones/v3.0-phases/19-selective-memory-erasure/19-*-PLAN.md artifacts"
      to: "scripts/phase19_erasure.py constants"
      via: "artifact names resolved from ERASURE_ARMS + arm_record_path(arm) / CALIBRATION_CORPUS_PATH"
      pattern: "results/phase19_"
    - from: "24-HUMAN-UAT.md item 2 note"
      to: "scripts/phase24_adversarial.py:289-292,:300"
      via: "dated traceability note naming filter + SystemExit"
      pattern: "phase24_adversarial.py:289"
---

<objective>
Repair the stale workflow stamps and archived-plan artifact names that make `gsd-sdk query
audit-open` and `verify.artifacts` report active errors, dispose Phase 24's two open UAT items and
Phase 25's stale stamp — every edit evidenced by a commit or a measured line, nothing else in the
archive touched.

Decisions implemented: D-31, D-35 (by non-edit), D-36, D-37, D-39.

Purpose: SC3's "6 deferred stale-stamp items … explicitly closed, re-deferred with a reason, or
recorded" — and the ledger (28-03) needs commit SHAs and before/after tool counts as `evidence`.
Output: the edits above, one commit, `audit-open` before/after JSON quoted, a checkpoint ruling on
`24-HUMAN-UAT.md`'s stamp.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/28-report-the-published-null-and-milestone-close/28-CONTEXT.md
@.planning/phases/28-report-the-published-null-and-milestone-close/28-RESEARCH.md

<interfaces>
Tool semantics (RESEARCH §3, measured): `audit-open` reads ONLY a file literally named `SUMMARY.md` in each quick-task dir (audit-open.js:78) and skips only `status === 'complete'` (:91); debug sessions close on `status: resolved` or `complete` (:43). Read-only queries are allowed; ZERO gsd-sdk mutation handlers (D-34).
Measured today (`gsd-sdk query audit-open`): total 9 = debug 1 + quick 3 + todos 1 + uat_gaps 2 (24 partial/2 pending, 25 partial/0 pending) + verification_gaps 2 (23, 27 human_needed).
`verify.artifacts` misses (measured today): 19-08 `results/phase19_cal_corpus.json`, `checkpoints/phase19_cal_adapter.pt`; 19-09 `results/phase19_calibration_arm.json`; 19-12 `results/phase19_arm_m1.json`; 19-13 `results/phase19_arm_m2.json`, `checkpoints/phase19_m2_retrain_adapter.pt`; 19-16 `contains: "Dated continuation"` (case) on results/phase19_erasure_report.md.
Real names (scripts/phase19_erasure.py): `ERASURE_ARMS = ("cal-erased", "erased", "replicate", "retrain")` :2540-2545 is the closed arm domain; `arm_record_path(arm)` :2560 `_prove`s `arm in ERASURE_ARMS` and returns `results/phase19_arm_<arm>.json` — so the record names are `phase19_arm_{cal-erased,erased,replicate,retrain}.json` and nothing else (the `CALIBRATION_ARM = "erase_calibration"` / `RETRAIN_ARM = "erase_reference"` constants are training-arm labels, NOT `arm_record_path` inputs — it would refuse them); `CALIBRATION_CORPUS_PATH = results/phase19_calibration_corpus.json` :3093; tracked: results/phase19_arm_{cal-erased,erased,replicate,retrain}.json, results/phase19_calibration_corpus.json — executor confirms each with `git ls-files`.
Evidence commits from STATE.md:1539-1571: r1u `7af6006`,`24d49ad`; sgh `4012a61`,`c90d0c8`,`98d0a60`; debug fix `c71bade` ("fix(14-10): draw_all's strict decode") + results/phase18_extraction_report.md:320.
D-37 lines: scripts/phase24_adversarial.py:289-292 (filter), :300 (SystemExit); scripts/phase25_record.py:553 (`score_refusal`), :574 (`clean_frame_probe_populations`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stale stamps — three quick tasks and the debug session, each evidenced before the edit</name>
  <files>.planning/quick/260819-r1u-close-b1-readme-v3-catchup/SUMMARY.md, .planning/quick/260819-sgh-close-w2-b1a-b1b/SUMMARY.md, .planning/quick/260902-dlo-pin-model-slim-pt-release-sha256-in-fetc/SUMMARY.md, .planning/debug/draw-all-utf8-decode-crash.md</files>
  <read_first>
    - .planning/STATE.md:1539-1571 (the "6 items" table with each item's evidence commits)
    - .planning/quick/260819-r1u-close-b1-readme-v3-catchup/260819-r1u-SUMMARY.md (frontmatter — no `status:`)
    - .planning/quick/260819-sgh-close-w2-b1a-b1b/260819-sgh-SUMMARY.md (frontmatter — no `status:`)
    - .planning/quick/260902-dlo-pin-model-slim-pt-release-sha256-in-fetc/260902-dlo-SUMMARY.md (already `status: complete`, still reported — filename rule)
    - .planning/quick/260802-h3g-anchor-the-verdict-section-clobber-guard/SUMMARY.md (the green shape: filename + `status: complete`)
    - .planning/debug/draw-all-utf8-decode-crash.md:1-8 and .planning/debug/sigma-zero-beats-control.md:1-6 (sibling's `status: resolved` shape)
    - .planning/phases/28-report-the-published-null-and-milestone-close/28-RESEARCH.md §3 "Refinement of D-39's premise" and Pitfall 7
  </read_first>
  <action>
1. Snapshot: `gsd-sdk query audit-open > <scratchpad>/audit-open-before.json`; quote `counts` in the SUMMARY.
2. Evidence first, per item (D-39): for each cited commit run `git log -1 --format='%h %ad %s' --date=short <sha>` and confirm it exists and matches the task's subject; for the debug session also `grep -n "decode-crash" results/phase18_extraction_report.md` (expect :320) and `git log --oneline -1 c71bade`. If any evidence is missing, do NOT change that stamp — leave it and report the item as `RE-DEFERRED` for 28-03.
3. Quick tasks: `git mv .planning/quick/260819-r1u-close-b1-readme-v3-catchup/260819-r1u-SUMMARY.md .planning/quick/260819-r1u-close-b1-readme-v3-catchup/SUMMARY.md` (same for `260819-sgh` and `260902-dlo`). Add `status: complete` as a frontmatter line to r1u and sgh (dlo already has it — leave its frontmatter unchanged). Add nothing else; the PLAN files keep their names (the tool reads only SUMMARY.md).
4. Debug session: change `status: fixing` → `status: resolved` in `.planning/debug/draw-all-utf8-decode-crash.md` and append ONE dated line at the end of the file: "Resolved 2026-09-<day> (Phase 28, D-39): fix landed at c71bade; both Phase 18 arms ran to completion (results/phase18_extraction_report.md:320). Stamp corrected from `fixing`; body above unchanged." Edit nothing above the appended line except the stamp.
5. Re-run `gsd-sdk query audit-open > <scratchpad>/audit-open-after-task1.json`; assert `counts.debug_sessions == 0` and `counts.quick_tasks == 0`; quote the counts. Do not commit yet (Task 2 commits everything together).
Threat references: T-28-07 (no mutation handler — `git mv` + Edit only), T-28-05 (a stamp is changed only with the commit that proves the work).
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && for d in 260819-r1u-close-b1-readme-v3-catchup 260819-sgh-close-w2-b1a-b1b 260902-dlo-pin-model-slim-pt-release-sha256-in-fetc; do test -f ".planning/quick/$d/SUMMARY.md" && grep -q "^status: complete" ".planning/quick/$d/SUMMARY.md" || exit 1; done && grep -q "^status: resolved" .planning/debug/draw-all-utf8-decode-crash.md && test "$(gsd-sdk query audit-open | .venv/bin/python -c 'import json,sys; c=json.load(sys.stdin)["counts"]; print(c["debug_sessions"]+c["quick_tasks"])')" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - Each of the three quick-task dirs contains `SUMMARY.md` (and no `<id>-SUMMARY.md`) with a `status: complete` frontmatter line; `git status --short .planning/quick` shows three renames (`R`) and no deletions
    - `.planning/debug/draw-all-utf8-decode-crash.md` line 3 reads `status: resolved`; the file contains `c71bade` and `phase18_extraction_report.md:320`; `git diff --stat` for it shows exactly one changed line plus appended lines
    - `gsd-sdk query audit-open` JSON: `counts.debug_sessions == 0`, `counts.quick_tasks == 0`; before/after `counts` objects quoted in the SUMMARY
    - SUMMARY lists, per item, the `git log -1` line of every evidence commit
  </acceptance_criteria>
  <done>Four stale stamps are true and the audit tool no longer reports them.</done>
</task>

<task type="auto">
  <name>Task 2: Phase-19 PLAN artifact names, 19-16 casing, 25-HUMAN-UAT stamp, 24-HUMAN-UAT item resolutions; one commit</name>
  <files>.planning/milestones/v3.0-phases/19-selective-memory-erasure/19-08-PLAN.md, .planning/milestones/v3.0-phases/19-selective-memory-erasure/19-09-PLAN.md, .planning/milestones/v3.0-phases/19-selective-memory-erasure/19-12-PLAN.md, .planning/milestones/v3.0-phases/19-selective-memory-erasure/19-13-PLAN.md, .planning/milestones/v3.0-phases/19-selective-memory-erasure/19-16-PLAN.md, .planning/phases/25-frontier-sweep-and-the-existence-gate-verdict/25-HUMAN-UAT.md, .planning/phases/24-adversarial-extraction-aware-training-the-held-out-attack-fa/24-HUMAN-UAT.md</files>
  <read_first>
    - The five Phase-19 PLAN files' frontmatter `must_haves.artifacts` blocks (paths listed in `<interfaces>`)
    - scripts/phase19_erasure.py:2540-2545 (`ERASURE_ARMS`), :2557-2562 (`arm_record_path` — the `_prove(arm in ERASURE_ARMS)`), :3093 (`CALIBRATION_CORPUS_PATH`)
    - results/phase19_erasure_report.md:355, :468, :551 (the lowercase "(dated continuation, 2026-08-19)" headings)
    - .planning/phases/25-frontier-sweep-and-the-existence-gate-verdict/25-HUMAN-UAT.md (frontmatter :1-6; every item's `result:` line; confirm zero `[pending]`)
    - .planning/phases/24-adversarial-extraction-aware-training-the-held-out-attack-fa/24-HUMAN-UAT.md:31-50 (items 2 and 3, `result: [pending]` at :40 and :50), :15-30 (item 1's RESOLVED shape to copy), :89-98 (`pending: 2` summary)
    - scripts/phase24_adversarial.py:285-302; scripts/phase25_record.py:550-576 (the lines D-37 cites — confirm the calls are there before quoting them)
    - .planning/phases/28-report-the-published-null-and-milestone-close/28-RESEARCH.md §3 row 10, Pitfall 8, Open Questions 2 and 4
  </read_first>
  <action>
1. Phase-19 PLAN artifact names (D-31, in scope — an existing file reported missing). For each stale `path:` map to the real tracked file and confirm with `git ls-files <real>` before editing: 19-08 `results/phase19_cal_corpus.json` → `results/phase19_calibration_corpus.json`; 19-09 `results/phase19_calibration_arm.json` → `results/phase19_arm_cal-erased.json` (`arm_record_path("cal-erased")` — `cal-erased` is the calibration arm in `ERASURE_ARMS`); 19-12 `results/phase19_arm_m1.json` → the M1 arm record (`arm_record_path("erased")` or `arm_record_path("replicate")` — resolve from the PLAN's own objective text against `ERASURE_ARMS`, quote the reasoning); 19-13 `results/phase19_arm_m2.json` → `results/phase19_arm_retrain.json` (`arm_record_path("retrain")`). Every target is one of the four `ERASURE_ARMS` records — confirm each with `git ls-files` before editing (all four plus the calibration corpus are tracked). Edit ONLY the `path:` value; keep `provides:` / `contains:` unless `contains:` fails against the new file (check with `gsd-sdk query verify.artifacts`). The two `checkpoints/*.pt` entries are NOT renamed (gitignored, can never verify from a clone — Pitfall 8); leave them and record in the SUMMARY for the ledger.
2. 19-16: change `contains: "Dated continuation"` → `contains: "dated continuation"` (the report's headings are lowercase).
3. Run `for p in 08 09 12 13 16; do gsd-sdk query verify.artifacts .planning/milestones/v3.0-phases/19-selective-memory-erasure/19-$p-PLAN.md; done` and quote each result; every `results/` path must read `exists: true` and 19-16's pattern must match; only the two `checkpoints/` entries may remain false.
4. D-36: in 25-HUMAN-UAT.md confirm `grep -c "\[pending\]"` is 0 and every item has a `result:` line; change frontmatter `status: partial` → `status: complete`, update `updated:` to today's ISO timestamp; touch nothing else.
5. D-37 in 24-HUMAN-UAT.md, mirroring item 1's RESOLVED shape (:15-30): item 2 — mark the heading `[RESOLVED]`, replace `result: [pending]` with `result: pass — traceability note (Phase 28, D-37, 2026-09-<day>)` and add a dated paragraph stating: requirement text stays as ticked; the mechanism is the filter at `scripts/phase24_adversarial.py:289-292` with the `SystemExit` at `:300` behind it — belt-and-braces, so the property the text states holds twice; requirement prose is never rewritten after the mechanism is known. Item 3 — mark `[RESOLVED]`, `result: pass — CLOSED-EARLIER by measurement (Phase 28, D-37, 2026-09-<day>)`, paragraph: `scripts/phase25_record.py:553` calls `score_refusal`, `:574` calls `clean_frame_probe_populations`, and every frontier point carries `refusal.by_family` counts (confirm with `python -c` over results/phase25_frontier.json for one point key and quote it). Update the `## Summary` block's `pending:` line to `pending: 0`. Do NOT change `status:` yet — Task 3 rules on it.
6. Confirm D-35 by non-edit: `grep -n "^status:" .planning/phases/23-*/23-VERIFICATION.md .planning/phases/27-relearning-attack/27-VERIFICATION.md` → both `human_needed`, quoted.
7. Commit Task 1 + Task 2 together: `git add` the explicit paths only (memory: concurrent agents edit the working tree — `git status` first, stage nothing you did not edit); message `docs(28-02): repair active tool errors — quick-task stamps, debug stamp, Phase-19 artifact names, 25-UAT complete, 24-UAT items 2-3 disposed`. Quote the SHA (28-03 cites it as evidence).
Threat references: T-28-07 (hand edits only), T-28-05 (each disposition is measured, not asserted).
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && grep -q '^status: complete' .planning/phases/25-frontier-sweep-and-the-existence-gate-verdict/25-HUMAN-UAT.md && test "$(grep -c '\[pending\]' .planning/phases/24-adversarial-extraction-aware-training-the-held-out-attack-fa/24-HUMAN-UAT.md)" = "0" && grep -q 'phase24_adversarial.py:289' .planning/phases/24-adversarial-extraction-aware-training-the-held-out-attack-fa/24-HUMAN-UAT.md && grep -q 'contains: "dated continuation"' .planning/milestones/v3.0-phases/19-selective-memory-erasure/19-16-PLAN.md && test "$(grep -c 'phase19_cal_corpus.json\|phase19_calibration_arm.json\|phase19_arm_m1.json\|phase19_arm_m2.json' .planning/milestones/v3.0-phases/19-selective-memory-erasure/19-0[89]-PLAN.md .planning/milestones/v3.0-phases/19-selective-memory-erasure/19-1[23]-PLAN.md | grep -vc ':0$')" = "0" && grep -q '^status: human_needed' .planning/phases/27-relearning-attack/27-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - None of the four stale names (`phase19_cal_corpus.json`, `phase19_calibration_arm.json`, `phase19_arm_m1.json`, `phase19_arm_m2.json`) remains in the four PLAN files; each replacement path returns non-empty from `git ls-files`
    - `verify.artifacts` output for 19-08/09/12/13/16 quoted: every `results/` entry `exists: true`, 19-16 pattern found; only `checkpoints/*.pt` entries false (two, recorded)
    - `25-HUMAN-UAT.md`: `status: complete`, `grep -c "\[pending\]"` == 0
    - `24-HUMAN-UAT.md`: items 2 and 3 headings end with `[RESOLVED]`, no `result: [pending]`, contains `phase24_adversarial.py:289-292`, `:300`, `phase25_record.py:553`, `:574`, `refusal.by_family`; summary `pending: 0`; frontmatter `status:` unchanged from `partial` at the end of this task
    - `23-VERIFICATION.md` and `27-VERIFICATION.md` both still `status: human_needed`; `19-13-SUMMARY.md` untouched (`git diff --quiet HEAD~1 -- .planning/milestones/v3.0-phases/19-selective-memory-erasure/19-13-SUMMARY.md` after commit)
    - One commit whose `git show --stat` lists only the eleven `files_modified` paths (renames counted as their new names); SHA quoted in the SUMMARY
  </acceptance_criteria>
  <done>Every active tool error in scope is closed with evidence; the 24 stamp awaits the ruling.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Ruling — 24-HUMAN-UAT.md's status stamp after D-37</name>
  <decision>With items 2 and 3 disposed, `24-HUMAN-UAT.md` has zero pending items. Does its stamp move `partial` → `complete` (D-36's own logic: the field records a workflow state and `partial` is false with zero pending), or stay `partial` with a ledger row explaining why?</decision>
  <context>CONTEXT is silent on 24's stamp (RESEARCH Open Question 2). `gsd-sdk query audit-open` will keep listing it as a `uat_gap` while `partial`. Either way the ledger row `UAT-24-STAMP` (28-03) records the ruling and the date. Item 2's resolution is a traceability note, not a re-test — a developer may prefer the stamp to say so.</context>
  <options>
    <option id="complete">
      <name>Move to `status: complete` now</name>
      <pros>Consistent with D-36; `audit-open` uat_gaps drops to 0 for Phase 24; the file's own definition of `partial` is honoured</pros>
      <cons>Item 2 was closed by a note about the mechanism rather than by a new test run</cons>
    </option>
    <option id="stay-partial">
      <name>Keep `status: partial`, record the reason in the ledger</name>
      <pros>Signals that item 2 was disposed by ruling, not by re-verification</pros>
      <cons>`audit-open` keeps reporting an open UAT gap after milestone close; contradicts D-36's reading of the field</cons>
    </option>
  </options>
  <resume-signal>Reply `complete` or `stay-partial`. On `complete` the executor edits the stamp + `updated:` and amends nothing else, committing as `docs(28-02): 24-HUMAN-UAT status complete — developer ruling <date>`; on `stay-partial` nothing is edited and the ruling text is passed to 28-03 verbatim.</resume-signal>
  <files>.planning/phases/24-adversarial-extraction-aware-training-the-held-out-attack-fa/24-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/24-adversarial-extraction-aware-training-the-held-out-attack-fa/24-HUMAN-UAT.md (as edited in Task 2)
    - .planning/phases/28-report-the-published-null-and-milestone-close/28-CONTEXT.md D-36, D-37
  </read_first>
  <action>Human ruling. After the reply, apply exactly the option chosen (see resume-signal), re-run `gsd-sdk query audit-open` and quote `counts.uat_gaps` in the SUMMARY together with the ruling text and date.</action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && grep -qE '^status: (complete|partial)$' .planning/phases/24-adversarial-extraction-aware-training-the-held-out-attack-fa/24-HUMAN-UAT.md && test "$(grep -c '\[pending\]' .planning/phases/24-adversarial-extraction-aware-training-the-held-out-attack-fa/24-HUMAN-UAT.md)" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records the ruling word (`complete` / `stay-partial`), the date, and the resulting `counts.uat_gaps`
    - If `complete`: frontmatter `status: complete`, `updated:` today, a commit whose `--stat` lists only 24-HUMAN-UAT.md
    - If `stay-partial`: `git status --short` shows no change to 24-HUMAN-UAT.md after Task 2's commit
  </acceptance_criteria>
  <done>The 24 stamp is intentional either way and the ledger will say which.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| archived planning artifacts → GSD tools | edits limited to what a tool actively misreports; the audit trail (SUMMARY prose, VERIFICATION verdicts) is not rewritten |
| developer → UAT stamp | a workflow stamp changes only on evidence or on an explicit human ruling |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-05 | Repudiation | stamps read as "work done" | mitigate | each stamp edit preceded by `git log -1 <evidence sha>` quoted; unevidenced items left for `RE-DEFERRED` |
| T-28-07 | Tampering | gsd-sdk mutation handlers corrupting frontmatter | mitigate | read-only queries only (`audit-open`, `verify.artifacts`); all edits via `git mv` / Edit tool |
| T-28-10 | Repudiation | re-stamping a verifier's verdict | mitigate | 23/27 VERIFICATION `human_needed` confirmed by grep, never edited (D-35) |
| T-28-11 | Tampering | archived prose rewritten under cover of a "repair" | mitigate | `files_modified` is the closed edit list; `19-13-SUMMARY.md` diff asserted empty |
| T-28-SC | Tampering | package installs | accept | no code, no installs in this plan |
</threat_model>

<verification>
- `gsd-sdk query audit-open`: debug 0, quick 0, uat_gaps per the ruling; `verify.artifacts` on the five Phase-19 plans: every `results/` path exists
- 23/27 VERIFICATION stamps unchanged; only `files_modified` paths appear in the commits
</verification>

<success_criteria>
- SC3's six stale-stamp items each have an evidenced disposition the ledger can cite by commit; no archived prose beyond the active-error set was touched
</success_criteria>

<output>
Create `.planning/phases/28-report-the-published-null-and-milestone-close/28-02-SUMMARY.md` when done — include the before/after `audit-open` counts, the five `verify.artifacts` results, every evidence `git log -1` line, the commit SHA(s), and the Task 3 ruling.
</output>
