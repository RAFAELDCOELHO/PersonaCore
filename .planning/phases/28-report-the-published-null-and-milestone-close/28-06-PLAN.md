---
phase: 28-report-the-published-null-and-milestone-close
plan: 06
type: execute
wave: 5
depends_on: ["28-05"]
files_modified:
  - docs/REPORT.md
  - README.md
  - tests/test_phase28_report.py
  - tests/test_phase25_correction.py
  - scripts/phase28_report.py
  - results/phase28_ledger.json
autonomous: false
must_haves:
  truths:
    - "`docs/REPORT.md` ends with ONE appended section between `<!-- PHASE28-REPORT-BEGIN -->` / `<!-- PHASE28-REPORT-END -->`, placed after every existing `## ` heading; every byte above the insertion point is unchanged (D-01, D-20)"
    - "`README.md`'s `## Results at a glance` list carries the rendered v3.0 and v4.0 bullets at its top between `<!-- PHASE28-GLANCE-BEGIN -->` / `<!-- PHASE28-GLANCE-END -->`, with zero deletions and no new `## ` heading (D-11)"
    - "`tests/test_phase28_report.py::test_report_block_is_byte_identical` and `::test_glance_block_is_byte_identical` compare `render_report()` / `render_glance()` with the committed spans using `==` — watched RED before the install (sentinels absent) and GREEN after (D-17, D-22)"
    - "`tests/test_phase25_correction.py`'s `normalized` register includes `tests/test_phase28_report.py`, `tests/test_phase28_prereg.py` and `tests/test_phase28_ledger.py` (all three CALL `normalized` on prose); its test name and module docstring no longer say `three files wide` / `THREE GUARD FILES` (D-22, D-26's rule)"
    - "The developer read both rendered blocks in the working tree BEFORE the publishing commit; after that commit the block is frozen and any change is a dated continuation via `append_addendum` (D-20)"
    - "`PUBLISHED` equals the publishing commit's date; `python scripts/phase28_report.py check` exits 0 on the committed tree (D-24)"
    - "`tests/test_phase18_docs.py` and `tests/test_phase15_docs.py` stay green (heading-prefix guard) and the full suite is green on the committed tree (D-01, D-11)"
  artifacts:
    - path: "docs/REPORT.md"
      provides: "the published v4.0 section"
      contains: "<!-- PHASE28-REPORT-BEGIN -->"
    - path: "README.md"
      provides: "rendered Results-at-a-glance bullets for v3.0 and v4.0"
      contains: "<!-- PHASE28-GLANCE-BEGIN -->"
    - path: "tests/test_phase28_report.py"
      provides: "byte-identity tests"
      contains: "test_report_block_is_byte_identical"
  key_links:
    - from: "tests/test_phase28_report.py::test_report_block_is_byte_identical"
      to: "docs/REPORT.md span"
      via: "_span(REPORT.md, PHASE28-REPORT) == phase28_report.render_report()"
      pattern: "render_report\\(\\)"
    - from: "docs/REPORT.md new section"
      to: "tests/test_phase18_docs.py::test_docs_continuation_is_additive"
      via: "section appended after the last existing ## heading"
      pattern: "PHASE28-REPORT-BEGIN"
requirements: [RPT-01]
---

<objective>
Publish: install both rendered blocks, prove byte-identity RED→GREEN, widen the `normalized`
register, have the developer read the rendered report once before it freezes, and make the
publishing commit. From that commit on, the block is pinned (D-20).

Decisions implemented: D-01, D-11, D-17, D-20, D-22, D-24 (publish side), D-26's naming rule
applied to the register test.

Purpose: RPT-01 — the null gets its own report surface, generated, frozen at publish.
Output: the appended section, the README bullets, the two byte-identity tests, the register update,
the publishing commit, a green full suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/28-report-the-published-null-and-milestone-close/28-CONTEXT.md
@.planning/phases/28-report-the-published-null-and-milestone-close/28-PATTERNS.md
@.planning/phases/28-report-the-published-null-and-milestone-close/28-04-SUMMARY.md
@.planning/phases/28-report-the-published-null-and-milestone-close/28-05-SUMMARY.md

<interfaces>
From scripts/phase28_report.py: PUBLISHED (edit to the publishing date BEFORE the commit — the only permitted edit after 28-04), REPORT_STEM, GLANCE_STEM, REPORT_PATH, README_PATH, GLANCE_HEADING, render_report(), render_glance(), install(path, stem, block, *, glance_heading=None), main(["write"]), main(["check"])
Sentinels: `<!-- PHASE28-REPORT-BEGIN -->` … `<!-- PHASE28-REPORT-END -->` (docs/REPORT.md, appended at EOF after :1326); `<!-- PHASE28-GLANCE-BEGIN -->` … `<!-- PHASE28-GLANCE-END -->` (README.md inside `## Results at a glance` at :109, before the first `- **Held-out recall` bullet at :111)
Heading-prefix guard: tests/test_phase18_docs.py:210-254 (`_README_HEADINGS_BEFORE`, `_REPORT_HEADINGS_BEFORE`), :354-379 `test_docs_continuation_is_additive`
Span comparator: tests/test_phase25_correction.py:104-127 (`_markers`, `_span` — exactly one pair, BEGIN before END)
Register: tests/test_phase25_correction.py:375-395 `_EARLIER_GUARD_FILES` + `test_the_register_is_three_files_wide`; module docstring :9-10 "FOUR INSTANCES, THREE GUARD FILES"
Post-publish corrections: scripts/_addendum.py:56 `append_addendum(path, addendum, *, pending, recorded)` — one append per file
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Byte-identity tests (RED first), register widened, blocks installed in the working tree — NOT committed</name>
  <files>tests/test_phase28_report.py, tests/test_phase25_correction.py, scripts/phase28_report.py, results/phase28_ledger.json, docs/REPORT.md, README.md</files>
  <read_first>
    - tests/test_phase28_report.py (28-05 — add to it, reuse its bootstrap)
    - tests/test_phase25_correction.py:1-20, :95-160, :370-396 (docstring, `_span`, the register)
    - tests/test_phase18_docs.py:205-260, :354-379 (what the append must keep green)
    - docs/REPORT.md:1310-1326 (the tail the section is appended after); README.md:105-125 (the glance list head)
    - scripts/phase28_report.py (`PUBLISHED`, `install`, `main`)
  </read_first>
  <action>
1. Add to `tests/test_phase28_report.py`: `_markers(stem)` and `_span(relative_path, stem)` copied from tests/test_phase25_correction.py:104-127; `test_report_sentinels_occur_exactly_once`, `test_glance_sentinels_occur_exactly_once`; `test_report_block_is_byte_identical` — `assert _span("docs/REPORT.md", phase28_report.REPORT_STEM) == phase28_report.render_report()` (plain `==`, NOT `normalized` — D-22's byte clause; on mismatch print a `difflib.unified_diff` in the message); `test_glance_block_is_byte_identical` likewise for README; `test_report_section_is_after_every_prior_heading` — the `## ` heading inside the span is the LAST `## ` in docs/REPORT.md; `test_glance_adds_no_heading` — no `\n## ` inside the README span; `test_glance_deleted_nothing` — the first line after `<!-- PHASE28-GLANCE-END -->` (skipping blanks) is the pre-existing first bullet, read from `git show <the commit before the publishing commit>:README.md` line after `## Results at a glance` (derive that commit as the newest commit touching README.md whose blob lacks the sentinel — the tests/test_phase18_docs.py:986-1060 shape), and the count of `- **` bullets inside `## Results at a glance` equals that older blob's count plus the span's own bullet count (zero deletions, non-vacuous after publish); `test_published_date_is_pinned_not_clocked` — `re.fullmatch(r"\d{4}-\d{2}-\d{2}", phase28_report.PUBLISHED)` and the rendered `##` title contains it.
2. Natural RED: run `.venv/bin/pytest tests/test_phase28_report.py -q -k "byte_identical or sentinels"` NOW (sentinels absent) — expect 4 failures with the "occurs 0 time(s)" message; paste the transcript into the SUMMARY.
3. Register: in `tests/test_phase25_correction.py` extend `_EARLIER_GUARD_FILES` with `"tests/test_phase28_report.py"`, `"tests/test_phase28_prereg.py"` and `"tests/test_phase28_ledger.py"` (D-22 — the ledger test's `test_named_limitation_reasons_bound_to_record_fields_match_the_records` compares prose under `normalized` and belongs in the register); rename `test_the_register_is_three_files_wide` → `test_the_register_still_routes_through_normalized` and fix its docstring/messages ("three files wide" → derive the width from `len(_EARLIER_GUARD_FILES) + 1` in the message, never a word-number); update the module docstring line "FOUR INSTANCES, THREE GUARD FILES" to state that Phase 28 widened the register and how many files it now names — again by pointing at `_EARLIER_GUARD_FILES` rather than typing a count. Run `.venv/bin/pytest tests/test_phase25_correction.py -q`; green (all three Phase 28 files CALL `normalized` — confirmed by AST).
4. Ledger flip BEFORE install (the published block pins the rows digest, so this is the last row edit): in `results/phase28_ledger.json` set row `TD-XC-README-GLANCE` to `disposition: "FIXED"`, `evidence: "<28-04 commit sha>; tests/test_phase28_report.py::test_glance_block_is_byte_identical"`, reason stating D-11's rendered bullets close v3.0 debt item 4 by mechanism; re-dump byte-stably; `.venv/bin/pytest tests/test_phase28_ledger.py -q` green (the node id is collectable from the working tree). No other row changes.
5. Set `PUBLISHED` in scripts/phase28_report.py to today's date (the date of the publishing commit in Task 3; if Task 3 slips a day, edit again BEFORE that commit — never after).
6. Install: `.venv/bin/python scripts/phase28_report.py write`. Then: `git diff --stat docs/REPORT.md README.md` (REPORT.md: only additions after line 1326; README.md: only additions between :110 and the first existing bullet); `git diff README.md | grep -c '^-[^-]'` must be 0 (zero deletions); `.venv/bin/pytest tests/test_phase18_docs.py tests/test_phase15_docs.py tests/test_phase28_report.py tests/test_phase28_prereg.py tests/test_phase28_ledger.py tests/test_phase25_correction.py -q` → all green (the byte-identity tests go GREEN here — paste the transcript); `.venv/bin/python scripts/phase28_report.py check` → exit 0. Commit NOTHING yet — Task 2 is the developer's read of the working tree.
Threat references: T-28-02 (byte-identity RED watched before GREEN), T-28-16 (heading-prefix guard run before commit).
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && grep -q "<!-- PHASE28-REPORT-BEGIN -->" docs/REPORT.md && grep -q "<!-- PHASE28-GLANCE-BEGIN -->" README.md && test "$(git diff README.md | grep -c '^-[^-]')" = "0" && .venv/bin/pytest tests/test_phase18_docs.py tests/test_phase15_docs.py tests/test_phase28_report.py tests/test_phase25_correction.py -q -x && .venv/bin/python scripts/phase28_report.py check && test "$(grep -c 'def test_the_register_is_three_files_wide' tests/test_phase25_correction.py)" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY carries the RED transcript (4 failing tests, sentinels absent) and the GREEN transcript after `write`
    - `docs/REPORT.md`: `grep -c "<!-- PHASE28-REPORT-BEGIN -->"` == 1, `grep -c "<!-- PHASE28-REPORT-END -->"` == 1; `git diff docs/REPORT.md` shows additions only, all after the former last line
    - `README.md`: one sentinel pair inside `## Results at a glance`; `git diff README.md | grep -c '^-[^-]'` == 0; `grep -c "^## " README.md` equals the count at HEAD
    - `tests/test_phase25_correction.py`: `_EARLIER_GUARD_FILES` grew by exactly the three Phase 28 test files (executor asserts `len(_EARLIER_GUARD_FILES)` in the working tree equals the HEAD count + 3, both measured with `python -c`, never typed); no `three files wide` / `THREE GUARD FILES` text remains; its tests pass
    - `scripts/phase28_report.py check` exits 0; `PUBLISHED` matches `\d{4}-\d{2}-\d{2}` and equals today's date
    - `git status --short` shows exactly: `M docs/REPORT.md`, `M README.md`, `M tests/test_phase28_report.py`, `M tests/test_phase25_correction.py`, `M scripts/phase28_report.py`, `M results/phase28_ledger.json` — nothing committed
    - `results/phase28_ledger.json` row `TD-XC-README-GLANCE` reads `FIXED` with a node id ending `test_glance_block_is_byte_identical`; `tests/test_phase28_ledger.py` green
  </acceptance_criteria>
  <done>The blocks exist in the working tree, provably byte-identical to a re-render, guarded — awaiting the developer's read.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Developer reads the rendered report and README bullets before they freeze</name>
  <what-built>`docs/REPORT.md` now ends with the v4.0 section (`null-at-both-capacities`) rendered from the committed records — lead, n=64 caveat, expectation row, ε-per-σ table beside canary verdicts, the recipe-confound quoted from the frontier and §12.5c, canary, relearning MOOT, self-corrections, figures, ship block, named-limitation register, v5.0 block, build pointers, provenance. `README.md`'s Results-at-a-glance list opens with the v3.0 and v4.0 bullets. Every number is a binding; the template scan and byte-identity tests are green. After Task 3's commit, any change to this prose is a dated continuation (D-20) — this is the last moment a wording change is a re-render.</what-built>
  <how-to-verify>
    1. `git diff docs/REPORT.md | less` — read the whole appended section. Check the first three lines say what CONTEXT's `<specifics>` demands: nothing cleared at either capacity, why (DP removed the memory with the leakage), and the n=64 control learned 87/1008.
    2. `git diff README.md` — two bullets, existing bullets untouched, links resolve (`docs/REPORT.md#…` anchor matches the new heading).
    3. Wording changes → edit `scripts/phase28_report.md.tmpl` / `scripts/phase28_glance.md.tmpl` ONLY (never the rendered files), then `.venv/bin/python scripts/phase28_report.py write` and re-run `.venv/bin/pytest tests/test_phase28_report.py -q` (the scan refuses any typed number).
    4. Reply "approved" when the working-tree blocks are what should freeze.
  </how-to-verify>
  <resume-signal>Type "approved", or describe the wording changes (template-level) to make first.</resume-signal>
  <files>docs/REPORT.md, README.md</files>
  <read_first>
    - docs/REPORT.md (the appended span), README.md (the glance span)
  </read_first>
  <action>Human read. On change requests: edit the templates, re-`write`, re-run the quick suite, and show the new diff; repeat until "approved". Record the date of approval in the SUMMARY.</action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/python scripts/phase28_report.py check && .venv/bin/pytest tests/test_phase28_report.py -q -x</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY records "approved <date>" and any template edits made during the review (with their re-render transcript)
    - `check` exits 0 and `tests/test_phase28_report.py` is green on the approved working tree
  </acceptance_criteria>
  <done>The developer has read what is about to freeze.</done>
</task>

<task type="auto">
  <name>Task 3: The publishing commit, then the full suite on the committed tree</name>
  <files>docs/REPORT.md, README.md, tests/test_phase28_report.py, tests/test_phase25_correction.py, scripts/phase28_report.py, results/phase28_ledger.json</files>
  <read_first>
    - scripts/phase28_report.py (`PUBLISHED` — must equal today)
    - scripts/_addendum.py:1-60 (the only post-publish correction route — read so the SUMMARY can name it)
  </read_first>
  <action>
1. `git status --short` — confirm only the six files (memory: concurrent agents edit the working tree; stage explicit paths only). If `PUBLISHED` is not today's date, edit it, re-`write`, re-run `tests/test_phase28_report.py` (still pre-publish).
2. `make lint`. Commit: `git add docs/REPORT.md README.md tests/test_phase28_report.py tests/test_phase25_correction.py scripts/phase28_report.py results/phase28_ledger.json && git commit -m "docs(28-06): publish v4.0 — null-at-both-capacities, rendered from committed records; README glance bullets; byte-identity guards (D-01, D-11, D-17, D-20)"`. Quote the SHA and `git show --stat HEAD`.
3. FROZEN from here (D-20): state in the SUMMARY that any further change to either block is `scripts/_addendum.py::append_addendum` with a dated continuation carrying its own bindings, never a re-render. Do not run `write` again in this phase.
4. Full suite: `make test > <scratchpad>/28-06-make-test.log 2>&1 &` uncapped in the background on the committed tree (21-24 min); when done, quote `pytest --collect-only -q | tail -1` and the summary line; expected `0 failed`. If a Phase 28 test fails against the published block, the fix is to the TEST or a dated continuation — not a re-render (say which was done). If an unrelated test fails, quote the node id and cause; do not edit the published blocks.
5. `python scripts/phase28_report.py check` once more on the committed tree → exit 0; `git log --oneline -1 -- docs/REPORT.md` is the publishing commit.
Threat references: T-28-02 (frozen at publish; `check` + byte-identity tests are the drift detectors), T-28-07 (no gsd-sdk mutation handler).
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && test -z "$(git status --short docs/REPORT.md README.md scripts/phase28_report.py tests/test_phase28_report.py tests/test_phase25_correction.py results/phase28_ledger.json)" && git log -1 --format=%s -- docs/REPORT.md | grep -q "publish v4.0" && .venv/bin/python scripts/phase28_report.py check && .venv/bin/pytest tests/test_phase28_report.py tests/test_phase28_prereg.py tests/test_phase28_ledger.py tests/test_phase18_docs.py tests/test_phase15_docs.py tests/test_phase25_correction.py -q -x</automated>
  </verify>
  <acceptance_criteria>
    - One publishing commit whose `--stat` lists exactly the six files; SHA quoted; `PUBLISHED` equals the commit's author date (YYYY-MM-DD)
    - `scripts/phase28_report.py check` exits 0 on the committed tree
    - Full-suite summary line quoted from the background log with `0 failed` (or every failing node id, cause, and the non-re-render remedy applied)
    - SUMMARY states the D-20 freeze and names `append_addendum` as the only correction route
  </acceptance_criteria>
  <done>v4.0 is published as it measured, frozen, byte-identical to its re-render, and the suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rendered block → publication surface | `install` writes between sentinels only; prefix proved on produced bytes; heading-prefix guard stays green |
| publishing commit → future edits | frozen: byte-identity test + `check`; corrections only via `append_addendum` |
| developer → freeze | a human reads the block before it is pinned |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-02 | Repudiation | post-publish re-render over published prose | mitigate | `test_report_block_is_byte_identical` + `check`; `write` not run after the commit; `append_addendum` is the correction route |
| T-28-16 | Tampering | clobbering prior REPORT/README content | mitigate | `install` prefix proof; `test_glance_deleted_nothing`; `tests/test_phase18_docs.py` heading-prefix guard run before the commit |
| T-28-13 | Tampering | clock in the block | mitigate | `PUBLISHED` pinned; `test_published_date_is_pinned_not_clocked` |
| T-28-07 | Tampering | gsd-sdk mutation handlers | mitigate | none called |
| T-28-17 | Denial (of review) | freezing prose nobody read | mitigate | Task 2 human-verify before Task 3 |
| T-28-SC | Tampering | package installs | accept | none |
</threat_model>

<verification>
- Byte-identity tests RED (pre-install) then GREEN (post-install), quoted; heading guards green; publishing commit made; `check` exit 0; full suite green on the committed tree
</verification>

<success_criteria>
- SC1: the null has its own report surface with the expectation quoted as recorded before any run; SC2: the section re-renders byte-identically and every doc-consistency check routes through `normalized`; v3.0 debt item 4 (README glance) is closed by mechanism
</success_criteria>

<output>
Create `.planning/phases/28-report-the-published-null-and-milestone-close/28-06-SUMMARY.md` when done — include the RED/GREEN transcripts, the publishing commit SHA and `--stat`, the approval date, the full-suite summary line, and the D-20 freeze statement.
</output>
