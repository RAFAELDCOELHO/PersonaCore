---
phase: 28-report-the-published-null-and-milestone-close
plan: 03
type: execute
wave: 2
depends_on: ["28-01", "28-02"]
files_modified:
  - results/phase28_ledger.json
  - tests/test_phase28_ledger.py
  - .planning/todos/resolved/phase28-carry-phase27-latent-review-findings.md
autonomous: false
requirements: [RPT-03]
must_haves:
  truths:
    - "ONE ledger, `results/phase28_ledger.json`, with a `rows` list where every row carries exactly `id`, `milestone`, `category`, `source`, `disposition`, `evidence`, `reason`, and a top-level `close` object (`ci_run: null` until 28-07) — SC3's '16 + 6' is `len()` over `milestone == v3.0` filtered by `category`, never a literal (D-28)"
    - "The disposition domain is CLOSED at six values — FIXED, CLOSED-EARLIER, RE-DEFERRED, NAMED-LIMITATION, ACCEPTED, FORBIDDEN-BY-GUARD — and every FIXED row's `evidence` names a commit `git cat-file -e` resolves AND a test node id `pytest --collect-only` collects (D-29)"
    - "Every v3.0 tech-debt item (the 16 in `v3.0-MILESTONE-AUDIT.md` `tech_debt:`), every v3.0 stale stamp (6), the three PARTIAL VALIDATION files, the seventh verify.artifacts error, RELRN-02..05, Phase 27's CR-01/CR-02/WR-01..06/IN-01..08, obligations (a)–(f), D-40's two limitations, the canary's could-not-have-failed disclosure, Phase 22 WARNING-4/5, FRONT-04's weaker form, 23-17 unticked, the adv_n64 refusal, the phase18 throughput comment, SC3's sha256 clause (D-27), DEF-17-01 (D-33), the 23/27 human_needed rulings (D-35), the 25 and 24 UAT stamps (D-36/D-37) each have exactly one row (D-28, D-30, D-32, D-33, D-35, D-36, D-37, Folded Todos)"
    - "Named limitations are rows with `disposition == NAMED-LIMITATION`; the report's register (28-04) is that filter, so no limitation text lives in two files (D-30)"
    - "`FORBIDDEN-BY-GUARD` rows name the guard (`V3_ARTIFACT_GLOBS` in tests/test_phase16_prereg.py or the published results `.md`) as evidence; `RE-DEFERRED` rows name a target (v5.0 or a phase) in `reason` (D-29, D-32)"
    - "Two regression tests back the D-39/D-31 FIXED rows: every `.planning/quick/*/` dir with a summary names it `SUMMARY.md` with `status: complete`, and every `results/` artifact in the five Phase-19 PLANs exists (D-29)"
    - "The folded todo moves to `.planning/todos/resolved/` with `status: resolved` once its rows exist (Folded Todos)"
    - "The developer reviewed the ledger rows before they are rendered into the report (Claude's Discretion — operator ledger review)"
  artifacts:
    - path: "results/phase28_ledger.json"
      provides: "the one disposition ledger"
      contains: "\"disposition\""
    - path: "tests/test_phase28_ledger.py"
      provides: "schema, closed domain, FIXED-has-test, len() counts, stamp/artifact regression tests"
      contains: "NAMED-LIMITATION"
  key_links:
    - from: "tests/test_phase28_ledger.py"
      to: "results/phase28_ledger.json"
      via: "json.load; set(row) == REQUIRED_KEYS; dispositions ⊆ DISPOSITIONS"
      pattern: "phase28_ledger"
    - from: "FIXED rows' evidence"
      to: "pytest node ids"
      via: "subprocess pytest --collect-only -q <nodeid> exit 0"
      pattern: "collect-only"
---

<objective>
Write the one ledger that turns "16 + 6 items closed, re-deferred or recorded" from prose into
data, with a closed disposition domain and a test that refuses a `FIXED` without a test; carry
Phase 27's ruling and every v4.0 open item into it as rows the report will render.

Decisions implemented: D-27, D-28, D-29, D-30 (data side), D-32, D-33, D-35, D-36, D-37, Folded
Todos (Rulings 1 and 2, obligations (a)–(f)).

Purpose: SC3 and the report's ship block / named-limitation register (D-13, D-30) read this file;
counts in prose are `len()` over it (D-28).
Output: `results/phase28_ledger.json`, `tests/test_phase28_ledger.py`, the resolved todo, one commit,
a developer review checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/28-report-the-published-null-and-milestone-close/28-CONTEXT.md
@.planning/phases/28-report-the-published-null-and-milestone-close/28-RESEARCH.md
@.planning/phases/28-report-the-published-null-and-milestone-close/28-01-SUMMARY.md
@.planning/phases/28-report-the-published-null-and-milestone-close/28-02-SUMMARY.md

<interfaces>
Ledger schema (planner's discretion, D-28 keys + two):
  top level: {"governs": <one sentence>, "schema": {"required_keys": [...], "dispositions": [...]}, "rows": [...], "close": {"ci_run": null}}
  row keys (exactly): id, milestone ("v3.0" | "v4.0"), category ("tech-debt" | "stale-stamp" | "found-by-measurement" | "requirement" | "review-finding" | "inherited-obligation" | "limitation" | "warning" | "refusal" | "premise"), source (file:line or artifact field path or REVIEW heading line), disposition, evidence (commit sha / test node id / file — for FIXED: BOTH a sha and a node id, separated by "; "), reason (prose; for NAMED-LIMITATION this is the text the register renders verbatim)
  dump: json.dumps(obj, indent=2, sort_keys=True, ensure_ascii=False) + "\n"  (byte-stable re-dump)
Disposition domain (D-29): FIXED, CLOSED-EARLIER, RE-DEFERRED, NAMED-LIMITATION, ACCEPTED, FORBIDDEN-BY-GUARD
Sources: .planning/milestones/v3.0-MILESTONE-AUDIT.md:209-235 (tech_debt: 5+2+2+3+4 = 16 — count with a parser, do not type 16); .planning/STATE.md:1539-1571 (the 6 stamps); .planning/phases/27-relearning-attack/27-REVIEW.md headings CR-01 :78, CR-02 :126, WR-01 :187, WR-02 :217, WR-03 :249, WR-04 :281, WR-05 :313, WR-06 :348, IN-01..IN-08 :376-460; .planning/todos/pending/phase28-carry-phase27-latent-review-findings.md (Rulings 1-2, obligations (a)-(f) — copy the six clauses VERBATIM into six rows); 27-HUMAN-UAT.md items 1-2; 25-CONTEXT.md D-40; results/phase25_frontier.json::verdicts.adversarial_capacity_rule_absent, ::epsilon_report.control_has_no_epsilon, ::verdicts.amended_criterion; results/phase26_canary.json::auditor_ceiling, ::reachable_claims; 22-VERIFICATION.md:149-183 (WARNING-4/5); REQUIREMENTS.md:424-433 (RELRN-02..05 unticked), :571 (FRONT-04 weaker form), :470-482 (3 PARTIAL VALIDATION files; phase18_extraction.py:85-87 comment debt); ROADMAP.md:706 (23-17 unticked)
Test node ids from 28-01 (cite in FIXED rows): tests/test_perplexity.py::test_docstring_states_the_true_denominator ; tests/test_phase16_driver.py::test_overwrite_statement_docstring_does_not_type_the_allowlist_size ; tests/test_package.py::test_runtime_dependencies_identical_across_four_milestones
Commits to cite: 28-01's and 28-02's SHAs (from their SUMMARYs); 7b38e38 (DEF-17-01, D-33); 5065bc5 (D-27); c71bade (debug fix); e5a2474 etc. as the SUMMARYs record
Census rules for tests/test_phase28_ledger.py: no literal `== 10`; no token `train_arm(`; no `os.replace`; clean-tree probes go RED while the ledger is untracked — commit before any full-suite run
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author results/phase28_ledger.json — every row from a named source, dispositions measured</name>
  <files>results/phase28_ledger.json</files>
  <read_first>
    - .planning/milestones/v3.0-MILESTONE-AUDIT.md:200-240 (the `tech_debt:` block — 16 items, source of the v3.0 tech-debt rows' `source` text)
    - .planning/STATE.md:1539-1571 (the 6 stale stamps and the v3.0 reasons for acknowledging rather than resolving)
    - .planning/phases/28-report-the-published-null-and-milestone-close/28-RESEARCH.md §3 (per-item table with candidate dispositions and today's locations), §4 (Phase 27 findings, D-40), Open Questions 1-5
    - .planning/todos/pending/phase28-carry-phase27-latent-review-findings.md (whole file — Ruling 1, Ruling 2, obligations (a)-(f) verbatim)
    - .planning/phases/27-relearning-attack/27-REVIEW.md (the 16 finding headings and each finding's first paragraph)
    - .planning/phases/28-report-the-published-null-and-milestone-close/28-01-SUMMARY.md and 28-02-SUMMARY.md (commit SHAs, tool before/after counts, the Task 3 ruling on 24's stamp, any item left RE-DEFERRED)
    - results/phase20_gate_coverage_correction.json (shape analog: prose-beside-data fields)
    - tests/test_phase16_prereg.py:50-62 (`V3_ARTIFACT_GLOBS` — the guard FORBIDDEN-BY-GUARD rows name)
  </read_first>
  <action>
Write `results/phase28_ledger.json` with the schema in `<interfaces>`. Author rows in this order and with these ids (the SUMMARY lists the final id set; if a source item is missing from this list, add it and say so — never drop one):

v3.0 tech-debt (category `tech-debt`, milestone `v3.0`, `source` = the audit item's own first clause + `v3.0-MILESTONE-AUDIT.md:<line>`): `TD-16-I1` FORBIDDEN-BY-GUARD (published results .md; evidence `results/phase16_persistence_report.md`); `TD-16-I2` FORBIDDEN-BY-GUARD; `TD-16-R1` RE-DEFERRED (target v5.0; behavioural, not prose); `TD-16-R2` ACCEPTED; `TD-16-R3` ACCEPTED (primary assertion mutation-proved — cite `tests/test_phase16_driver.py:1782`); `TD-17-DEF-17-01` CLOSED-EARLIER (evidence `7b38e38`; reason quotes D-33's `make lint` output measured again now — run `make lint` and paste the line); `TD-17-SUMMARY-FRONTMATTER` ACCEPTED or RE-DEFERRED — measure first: run `gsd-sdk query frontmatter.validate` on one archived 17-*-SUMMARY.md; if it reports an error the tool consumes, FIXED is out of scope (archived phase) → RE-DEFERRED with that output as evidence; else ACCEPTED; `TD-18-W1-RESIDUAL` FORBIDDEN-BY-GUARD (`results/phase18_extraction_report.md`); `TD-18-ALLOWLIST-PARENTHETICAL` FIXED (evidence `<28-01 sha>; tests/test_phase16_driver.py::test_overwrite_statement_docstring_does_not_type_the_allowlist_size`); `TD-19-W1-ARTIFACT-NAMES` FIXED (evidence `<28-02 sha>; tests/test_phase28_ledger.py::test_phase19_plan_result_artifacts_exist`; reason records the two gitignored `checkpoints/*.pt` entries as unverifiable from a clone by design); `TD-19-PERPLEXITY-DOCSTRING` FIXED (`<28-01 sha>; tests/test_perplexity.py::test_docstring_states_the_true_denominator`); `TD-19-13-STRAY-TAGS` ACCEPTED (record-only, D-31); `TD-XC-ERASE-CHECKBOXES` ACCEPTED; `TD-XC-REQCOMPLETED-STRING` ACCEPTED unless 28-02 measured a tool error (then FIXED with its evidence); `TD-XC-17-DIGIT` ACCEPTED (record-only, D-31); `TD-XC-README-GLANCE` — disposition `FIXED` with evidence `28-06 (pending); tests/test_phase28_report.py::test_glance_block_is_byte_identical` is NOT allowed before that test exists: write it as `RE-DEFERRED` now with reason "closed by D-11's rendered bullets in plan 28-06; row flipped to FIXED in 28-06 Task 1, before the publishing commit, with the 28-04 renderer commit + node id", and 28-06 flips it (the rows digest is pinned in the published block, so no row may change after publish).

v3.0 stale stamps (category `stale-stamp`): `SS-DEBUG-DRAW-ALL` FIXED (`<28-02 sha>; tests/test_phase28_ledger.py::test_debug_sessions_are_not_left_fixing`); `SS-QUICK-260819-R1U`, `SS-QUICK-260819-SGH` FIXED (`<28-02 sha>; tests/test_phase28_ledger.py::test_quick_task_summaries_are_named_for_the_audit_tool`); `SS-17-VERIFICATION`, `SS-18-VERIFICATION`, `SS-19-VERIFICATION` ACCEPTED (archived, not scanned by audit-open; discharge record sits beside the verdict — D-35 precedent). Any of these that 28-02 could not evidence → RE-DEFERRED with 28-02's reason.

v3.0 found-by-measurement (category `found-by-measurement`): `FM-QUICK-260902-DLO` FIXED (same node id as the quick-task test); `FM-19-16-CASE` FIXED (`<28-02 sha>; tests/test_phase28_ledger.py::test_phase19_plan_result_artifacts_exist`); `FM-17-VALIDATION-PLANNED`, `FM-18-VALIDATION-DRAFT`, `FM-19-VALIDATION-DRAFT` ACCEPTED (record-only; REQUIREMENTS.md:470-472 names them).

v4.0 (milestone `v4.0`): `RELRN-02`..`RELRN-05` NAMED-LIMITATION (category `requirement`; reason = the REQUIREMENTS traceability sentence "not admitted by the gate; apparatus built and guarded, never exercised on a mitigated arm"; evidence `results/phase27_admission.json::apparatus.status`); `CR-01`, `CR-02`, `WR-02`, `WR-03`, `WR-06` NAMED-LIMITATION (category `review-finding`; `source` = `27-REVIEW.md:<line>` + heading; reason = the todo's one-line description of each, verbatim); `WR-01`, `WR-04` NAMED-LIMITATION with reason prefixed "inconsequential — "; `WR-05` NAMED-LIMITATION, reason = Ruling 2 verbatim (no adv_* admission until v5.0 pins the arm's own control; DP-control calibration not accepted; 0 of 12 adversarial points admissible today: 6 INCONCLUSIVE, 6 REFUSED — write the counts as they appear in the ruling text, they are quoted, not computed); `IN-01`..`IN-08` ACCEPTED or RE-DEFERRED per finding (read each; a finding whose fix would edit `scripts/phase27_prereg.py` or the committed record is FORBIDDEN-BY-GUARD; say which); `OBLIG-A`..`OBLIG-F` NAMED-LIMITATION (category `inherited-obligation`; reason = the clause verbatim from the todo; source `27-HUMAN-UAT.md`); `D40-LIMITATION-1` NAMED-LIMITATION (source `results/phase25_frontier.json::verdicts.adversarial_capacity_rule_absent`, reason = that field's text copied by `python -c` — paste the printed value, never retype); `D40-LIMITATION-2` NAMED-LIMITATION (`epsilon_report.control_has_no_epsilon`, same rule); `CANARY-COULD-NOT-HAVE-FAILED` NAMED-LIMITATION (source `results/phase26_canary.json::auditor_ceiling` + `::reachable_claims`; reason = the record's reasons[-1] sentence for one unreachable point, pasted); `P22-WARNING-4`, `P22-WARNING-5` RE-DEFERRED (target v5.0; source `22-VERIFICATION.md:149-183`; reason: not on the publishing path); `FRONT-04-WEAKER-FORM` NAMED-LIMITATION (source `REQUIREMENTS.md:571`, reason = the row's "Satisfied in a weaker form…" sentence); `P23-17-UNTICKED` ACCEPTED (source `ROADMAP.md:706`; the box stays unticked by design, 23-20 completed the run; twice re-ticked by handlers and hand-reverted); `ADV-N64-REFUSED` NAMED-LIMITATION (source `results/phase25_frontier.json::verdicts.amended_criterion`, reason = the field's text pasted); `P18-THROUGHPUT-COMMENT` FORBIDDEN-BY-GUARD (source `scripts/phase18_extraction.py:85-87`, evidence `tests/test_phase16_prereg.py::V3_ARTIFACT_GLOBS` / the phase18 ancestry guard); `SC3-SHA256-CLAUSE` ACCEPTED (category `premise`; evidence `5065bc5`; reason = D-27's one-liner: the clause is false as written — one line `license = "MIT"` added 2026-09-01, pin re-set in the same commit, no dependency changed, D-25's test is the substantive guarantee); `UAT-25-STAMP` CLOSED-EARLIER (evidence `<28-02 sha>`; reason cites run `34403612853` per D-36); `UAT-24-ITEM-2` CLOSED-EARLIER (evidence `scripts/phase24_adversarial.py:289-292; :300`); `UAT-24-ITEM-3` CLOSED-EARLIER (`scripts/phase25_record.py:553; :574`); `UAT-24-STAMP` ACCEPTED, reason = 28-02 Task 3's ruling verbatim with its date; `VER-23-HUMAN-NEEDED`, `VER-27-HUMAN-NEEDED` ACCEPTED (D-35; evidence = the ruling records `23-HUMAN-UAT.md` / `27-HUMAN-UAT.md`).

Rules while writing: every `reason` that quotes a record field is produced by `python -c "import json; print(json.load(open(...))[...])"` and pasted; no count appears in any `reason` except counts quoted from a ruling or record text; `close` is `{"ci_run": null}`. Dump with the byte-stable form and verify `python -c` re-dump equals the file bytes. Print `len(rows)` and the per-disposition / per-category / per-milestone counts into the SUMMARY (measured, not typed).
Threat references: T-28-05 (closed domain; FIXED cites test + commit), T-28-01 (record fields pasted from `python -c`, never retyped).
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/python -c "
import json,pathlib
p=pathlib.Path('results/phase28_ledger.json'); raw=p.read_bytes(); L=json.loads(raw)
assert json.dumps(L,indent=2,sort_keys=True,ensure_ascii=False).encode()+b'\n'==raw, 're-dump differs'
D={'FIXED','CLOSED-EARLIER','RE-DEFERRED','NAMED-LIMITATION','ACCEPTED','FORBIDDEN-BY-GUARD'}
K={'id','milestone','category','source','disposition','evidence','reason'}
ids=[r['id'] for r in L['rows']]; assert len(ids)==len(set(ids)), 'dup ids'
for r in L['rows']: assert set(r)==K,(r.get('id'),set(r)^K); assert r['disposition'] in D, r['id']; assert r['milestone'] in ('v3.0','v4.0')
need={'RELRN-02','RELRN-03','RELRN-04','RELRN-05','CR-01','CR-02','WR-01','WR-02','WR-03','WR-04','WR-05','WR-06','IN-01','IN-08','OBLIG-A','OBLIG-F','D40-LIMITATION-1','D40-LIMITATION-2','CANARY-COULD-NOT-HAVE-FAILED','ADV-N64-REFUSED','SC3-SHA256-CLAUSE','TD-17-DEF-17-01','TD-XC-README-GLANCE','SS-DEBUG-DRAW-ALL','UAT-24-STAMP','VER-27-HUMAN-NEEDED'}
assert need<=set(ids), need-set(ids)
assert L['close']=={'ci_run':None}
v3=[r for r in L['rows'] if r['milestone']=='v3.0']
print('rows',len(L['rows']),'v3 tech-debt',len([r for r in v3 if r['category']=='tech-debt']),'v3 stale-stamp',len([r for r in v3 if r['category']=='stale-stamp']))
"</automated>
  </verify>
  <acceptance_criteria>
    - `results/phase28_ledger.json` parses; re-dump with `indent=2, sort_keys=True, ensure_ascii=False` + newline is byte-identical to the file
    - Every row has exactly the seven keys; every `disposition` is one of the six; ids unique; the id set includes every id named in the action (the verify script checks a representative subset — the SUMMARY lists all)
    - The v3.0 `tech-debt` filter has the same length as the number of items parsed from `v3.0-MILESTONE-AUDIT.md` `tech_debt:` (executor parses the YAML block with a tiny `python -c` and asserts equality; prints both)
    - The v3.0 `stale-stamp` filter has the same length as the number of rows in STATE.md's "6 items" table (parsed, not typed)
    - Every FIXED row's `evidence` contains `; tests/` and a 7-40 hex commit sha; no FIXED row cites a 28-06/28-07 artifact that does not exist yet
    - Every NAMED-LIMITATION row whose `source` is a record field path has a `reason` equal (under `_prose.normalized`) to the field's value — checked in Task 2's test
    - `close == {"ci_run": null}`
  </acceptance_criteria>
  <done>The ledger exists as data; the only counts anywhere are `len()` outputs in the SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: tests/test_phase28_ledger.py — schema, closed domain, FIXED-has-test, record-bound reasons, stamp/artifact regression tests; resolve the todo; commit</name>
  <files>tests/test_phase28_ledger.py, .planning/todos/resolved/phase28-carry-phase27-latent-review-findings.md</files>
  <read_first>
    - results/phase28_ledger.json (as written in Task 1)
    - tests/test_phase24_record.py:288-334 (collect-all-drift-then-assert loop)
    - tests/test_phase25_driver.py:341-365 (exact-set comparison shape)
    - tests/test_phase27_prereg.py:10-31, :57-61 (bootstrap + `_git`)
    - .planning/phases/28-report-the-published-null-and-milestone-close/28-PATTERNS.md §`tests/test_phase28_ledger.py` and §Shared Patterns (census table)
    - .planning/todos/pending/phase28-carry-phase27-latent-review-findings.md (frontmatter `status: pending`)
  </read_first>
  <action>
1. Create `tests/test_phase28_ledger.py` (bootstrap per PATTERNS; import `_prose`). Module constants: `LEDGER = _ROOT / "results/phase28_ledger.json"`, `REQUIRED_KEYS = frozenset({...seven...})`, `DISPOSITIONS = frozenset({...six...})`. Tests:
   - `test_every_row_has_exactly_the_required_keys` — collect violations, assert once.
   - `test_disposition_domain_is_closed` — `{r["disposition"]} <= DISPOSITIONS` and the file's own `schema.dispositions` equals `sorted(DISPOSITIONS)`.
   - `test_ids_are_unique_and_milestone_is_v3_or_v4`.
   - `test_fixed_rows_cite_a_commit_and_a_collectable_test` — for each FIXED row: regex a 7-40 hex sha from `evidence`, `git cat-file -e <sha>^{commit}` exit 0; regex `tests/[^ ;]+::[A-Za-z_0-9\[\]-]+`, run `[sys.executable, "-m", "pytest", "--collect-only", "-q", node_id]` with cwd=_ROOT and assert exit 0 and node_id in stdout. Collect all failures, assert once.
   - `test_re_deferred_rows_name_a_target` — `reason` contains `v5.0` or `Phase ` or `plan 28-`.
   - `test_forbidden_by_guard_rows_name_the_guard` — `evidence` contains `V3_ARTIFACT_GLOBS` or `results/phase1` (published .md) or `prereg`.
   - `test_named_limitation_reasons_bound_to_record_fields_match_the_records` — for rows whose `source` matches `results/(phase25_frontier|phase26_canary|phase27_admission)\.json::(.+)`, resolve the dotted path (split on `.`; `[]` indexing, never `.get`) and assert `_prose.normalized(field_value) in _prose.normalized(row["reason"])` (the `CANARY-COULD-NOT-HAVE-FAILED` row's source is two paths joined by ` + `; handle by asserting the `auditor_ceiling` repr and `reachable_claims` string both appear in the reason).
   - `test_v3_counts_derive_from_the_sources` — parse the `tech_debt:` block of `.planning/milestones/v3.0-MILESTONE-AUDIT.md` (count lines matching `^\s+- "` inside the block between `tech_debt:` and the next top-level key) and assert equality with `len([r for r in rows if r["milestone"]=="v3.0" and r["category"]=="tech-debt"])`; assert the stale-stamp filter length equals the number of table rows in STATE.md:1539-1571 that begin with `| debug` / `| quick` / `| \`1` (read the table by its header line, count body rows) — both sides computed, never a literal.
   - `test_close_block_shape` — `close` has exactly key `ci_run`; value is `None` or a dict with keys `{"id","url","head_sha","conclusion","recorded"}` and `conclusion == "success"`.
   - `test_quick_task_summaries_are_named_for_the_audit_tool` — for every dir under `.planning/quick/` that contains any `*SUMMARY.md`, assert the file is named exactly `SUMMARY.md` and its frontmatter has `status: complete` (regression for D-39).
   - `test_debug_sessions_are_not_left_fixing` — every `.planning/debug/*.md` frontmatter `status:` is in `{"resolved","complete"}`.
   - `test_phase19_plan_result_artifacts_exist` — for the five archived PLANs (19-08/09/12/13/16) parse every `- path: "..."` under `artifacts:`; for each path not starting with `checkpoints/`, assert `(_ROOT / path).is_file()`; for 19-16 additionally assert the `contains:` string is found in the file.
   - `test_ledger_redump_is_byte_stable`.
   Do not write `== 10`, `train_arm(`, or `os.replace` anywhere in the file. Every prose comparison uses `_prose.normalized`.
2. RED watch: before Task 1's file is complete this test would fail; instead record a planted RED: copy the ledger to `tmp_path`, set one row's disposition to `"fixed"` (lowercase), and assert the domain check fails — implement as `test_a_lowercase_disposition_is_red` using the same checker function on the planted copy. Also run the collect-only loop once against a deliberately wrong node id in a planted row (`test_a_missing_test_node_id_is_red`).
3. Move the folded todo: `mkdir -p .planning/todos/resolved && git mv .planning/todos/pending/phase28-carry-phase27-latent-review-findings.md .planning/todos/resolved/`; set frontmatter `status: resolved` and append one dated line naming the ledger row ids that carry it (CR-01, CR-02, WR-01..06, OBLIG-A..F). `gsd-sdk query audit-open` → `counts.todos == 0`; quote.
4. `.venv/bin/pytest tests/test_phase28_ledger.py -q -v` green; `make lint` clean. Commit ledger + test + todo: `git add results/phase28_ledger.json tests/test_phase28_ledger.py .planning/todos/` then `git commit -m "ledger(28-03): results/phase28_ledger.json — one disposition per open item across v3.0 and v4.0, closed domain, FIXED requires a test"`. Quote the SHA. Then run the full suite uncapped in the background on the committed tree (`make test > <scratchpad>/28-03-make-test.log 2>&1 &`), and report the tail line in the SUMMARY when it finishes (~21-24 min); if any test fails, quote the node id — the clean-tree probes must be green now that the ledger is tracked.
Threat references: T-28-05, T-28-07 (todo moved by `git mv`, no handler).
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/pytest tests/test_phase28_ledger.py -q -x && test -f .planning/todos/resolved/phase28-carry-phase27-latent-review-findings.md && grep -q "^status: resolved" .planning/todos/resolved/phase28-carry-phase27-latent-review-findings.md && test -z "$(ls .planning/todos/pending/ 2>/dev/null)" && git ls-files results/phase28_ledger.json | grep -q . && ! grep -q "os.replace\|train_arm(" tests/test_phase28_ledger.py && ! grep -qE "(==|!=)\s*10([^0-9_]|$)" tests/test_phase28_ledger.py</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_phase28_ledger.py` defines all fourteen test functions named in the action (twelve checks + two planted-RED probes); `grep -c "def test_" tests/test_phase28_ledger.py` >= 14
    - `.venv/bin/pytest tests/test_phase28_ledger.py -q` all passed; the two planted-RED probes assert a failure on the planted copy (they pass by catching the RED)
    - The file contains `normalized(` calls and no `== 10` / `train_arm(` / `os.replace` tokens
    - `.planning/todos/pending/` is empty; the resolved todo has `status: resolved`; `gsd-sdk query audit-open` `counts.todos == 0` (quoted)
    - `git ls-files results/phase28_ledger.json tests/test_phase28_ledger.py` both non-empty; commit SHA quoted
    - Full-suite result quoted from the background log: `N passed, S skipped, 0 failed` (or every failing node id listed with cause)
  </acceptance_criteria>
  <done>The ledger is guarded, tracked, and the todo that fed it is resolved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Developer reviews the ledger rows before they are rendered into the report</name>
  <what-built>`results/phase28_ledger.json` — one row per open item across v3.0 and v4.0 with a closed disposition domain; the NAMED-LIMITATION rows are exactly what plan 28-04 renders into the report's named-limitation register and ship block (D-13, D-30), and once 28-06 publishes, changing a row means a dated continuation, not an edit (D-20). This is the last cheap moment to change a disposition.</what-built>
  <how-to-verify>
    1. `.venv/bin/python -c "import json; L=json.load(open('results/phase28_ledger.json')); [print(r['id'], '|', r['milestone'], '|', r['disposition'], '|', r['reason'][:110]) for r in L['rows']]"` — read every row.
    2. Check the four dispositions most likely to be wrong: `TD-16-R1` (RE-DEFERRED vs ACCEPTED), the `IN-0x` rows, `UAT-24-STAMP` (matches your 28-02 ruling), `TD-17-SUMMARY-FRONTMATTER`.
    3. Confirm nothing you consider a limitation is missing from `[r for r in rows if r['disposition']=='NAMED-LIMITATION']`.
    4. If a row must change: edit the JSON by hand, re-run `.venv/bin/pytest tests/test_phase28_ledger.py -q`, and commit `ledger(28-03): developer review — <what changed>`.
  </how-to-verify>
  <resume-signal>Type "approved" (rows stand) or list the row ids to change and their new disposition/reason.</resume-signal>
  <files>results/phase28_ledger.json</files>
  <read_first>
    - results/phase28_ledger.json
  </read_first>
  <action>Human review. On changes: apply exactly the listed edits, re-run the ledger test, commit; quote the diff in the SUMMARY. On "approved": record the date in the SUMMARY.</action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/pytest tests/test_phase28_ledger.py -q -x && test -z "$(git status --short results/phase28_ledger.json)"</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY records "approved <date>" or the list of changed row ids with before/after dispositions and the review commit SHA
    - `git status --short results/phase28_ledger.json` empty (every change committed)
  </acceptance_criteria>
  <done>The ledger the report will render has been read by the developer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hand-authored rows → report | the ledger is the only hand-authored input the renderer reads; its schema and record-bound reasons are tested |
| "disposition recorded" → "fixed" | D-29's closed domain plus FIXED-requires-test keeps the two machine-distinguishable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigate Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Tampering | reasons quoting record fields | mitigate | `test_named_limitation_reasons_bound_to_record_fields_match_the_records` resolves the field by `[]` and compares under `normalized` |
| T-28-05 | Repudiation | FIXED without a test / disposition read as fix | mitigate | closed domain; `pytest --collect-only` on every FIXED node id; `git cat-file -e` on every FIXED sha |
| T-28-07 | Tampering | gsd-sdk mutation handlers | mitigate | none called; todo moved with `git mv` |
| T-28-12 | Denial (of evidence) | untracked ledger reddening clean-tree probes | mitigate | commit before the background full suite |
| T-28-SC | Tampering | package installs | accept | none installed |
</threat_model>

<verification>
- `tests/test_phase28_ledger.py` green; ledger tracked; full suite green on the committed tree; audit-open `todos == 0`
- Developer review recorded
</verification>

<success_criteria>
- SC3's "each explicitly closed, re-deferred with a reason, or recorded as a named limitation" is a row per item with a tested disposition, and the register 28-04 renders is a filter over this file
</success_criteria>

<output>
Create `.planning/phases/28-report-the-published-null-and-milestone-close/28-03-SUMMARY.md` when done — include the full id list with dispositions, the measured counts (rows, per milestone, per category, per disposition), the commit SHAs, the full-suite tail line, and the review outcome.
</output>
