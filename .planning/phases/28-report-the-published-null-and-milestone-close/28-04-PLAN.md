---
phase: 28-report-the-published-null-and-milestone-close
plan: 04
type: execute
wave: 3
depends_on: ["28-03"]
files_modified:
  - scripts/phase28_report.py
  - scripts/phase28_report.md.tmpl
  - scripts/phase28_glance.md.tmpl
autonomous: true
requirements: [RPT-01]
must_haves:
  truths:
    - "A stdlib-only renderer `scripts/phase28_report.py` reads the committed records DIRECTLY (frontier, canary, admission, phase23 cost/sigma_zero/control_floor, the ledger, two committed .md slices), builds a binding mapping, and fills two committed templates with `string.Template`-style `${dotted.binding}` placeholders; a missing binding raises, never renders blank (D-16, D-21, D-23's KeyError rule)"
    - "Every binding names a record field path, a module constant, a ledger row/filter, a verbatim quote verified against its source, or a `derived.*` function whose inputs are record fields — the template files contain NO bare numeral outside the D-19 grammar (D-16, D-18, D-19)"
    - "The report block is ordered lead (capacity_branch + both arm_existentials verbatim) → mechanism line (noised-DP cleared (a)/(b) derived from admission `rows` + frontier `sigma`) → n=64 caveat with `control_readings.dp_n64` vs `dp_n8` and the L=64 → 9σ quote → expectation row (quote + record bracket σ=12/σ=16 + `sigma_for(4.0, composed_steps, delta)`) → ε-per-σ table with `dp_n8` canary verdict and reasons per row + curve total beside `curve_total_is_context_only` + LIMITATION 2 → adversarial confound quoted from `adversarial_no_replay.*`, `amended_criterion`, §12.5c slice, `early_return_reason`, 32/256 replay windows from `dp_replay_source` → canary summary/reachable_claims/auditor_ceiling/power_gate → relearning MOOT (`admission.verdict.reasons`, `cleared_counts`, `apparatus`) → self-corrections bound to phase23 records + the 23-12 continuation slice → figures with record-bound captions → ship block rendered from ledger NAMED-LIMITATION rows + existentials → named-limitation register (ledger filter) → v5.0 block bound to the ROADMAP v5.0 entry + the WR-05 row → build-decision pointers → provenance table of source digests (D-01, D-02, D-03, D-04, D-06, D-07, D-08, D-09, D-10, D-12, D-13, D-14, D-15, D-30)"
    - "The renderer asserts at render time that `accountant.epsilon_for(16.0, steps, delta)` equals the frontier's σ=16 `epsilon` bit-identically and that `sigma_for(4.0, steps, delta)` is what it prints (D-06); it never reads `logs/` (D-10); it never calls `today()`/`now()`/`git rev-parse HEAD` — `PUBLISHED` is a pinned constant (D-24)"
    - "`install(path, stem, block)` writes only between `<!-- PHASE28-REPORT-BEGIN/END -->` (docs/REPORT.md, appended at EOF on first install) or `<!-- PHASE28-GLANCE-BEGIN/END -->` (README.md, inserted inside `## Results at a glance` before the first existing bullet, zero deletions), proves the prefix byte-identical on the PRODUCED text, uses `Path.write_text` (no `os.replace`), and refuses when either sentinel count is not equal or exceeds one (D-11, D-17, D-20)"
    - "The README glance bullets (v3.0 and v4.0) carry the same discipline: every number a binding, links to `results/phase25_frontier.json`, `results/phase27_admission.json`, and `docs/REPORT.md#<anchor>` of the new section (D-11)"
    - "`sha256` + byte size of every JSON record read and the canonical `rows` digest of the ledger appear in the block's provenance table; the ledger's `close` object is outside that digest so 28-07 can fill `ci_run` without breaking byte-identity (D-21, D-38)"
  artifacts:
    - path: "scripts/phase28_report.py"
      provides: "renderer: resolve, Bindings mapping, render_report, render_glance, install, main"
      exports: ["render_report", "render_glance", "install", "resolve", "PUBLISHED", "QUOTES", "SLICES", "REPORT_STEM", "GLANCE_STEM", "ledger_rows_digest"]
    - path: "scripts/phase28_report.md.tmpl"
      provides: "the report section template — placeholders only"
      contains: "${frontier.verdicts.capacity_branch}"
    - path: "scripts/phase28_glance.md.tmpl"
      provides: "the README bullets template"
      contains: "${frontier.verdicts.arm_existentials.dp}"
  key_links:
    - from: "scripts/phase28_report.py"
      to: "results/phase25_frontier.json"
      via: "json.load; resolve(record, 'verdicts.capacity_branch')"
      pattern: "phase25_frontier"
    - from: "scripts/phase28_report.py"
      to: "personacore.privacy.accountant"
      via: "sigma_for(4.0, composed_steps, delta); epsilon_for(16.0, ...) == record epsilon asserted"
      pattern: "sigma_for"
    - from: "scripts/phase28_report.py"
      to: "results/phase28_ledger.json"
      via: "rows filtered on disposition == NAMED-LIMITATION; ledger_rows_digest over rows only"
      pattern: "NAMED-LIMITATION"
    - from: "scripts/phase28_report.md.tmpl"
      to: "scripts/phase28_report.py Bindings"
      via: "${prefix.path} placeholders; prefixes frontier/canary/admission/sigma_zero/cost/control_floor/const/derived/table/quote/slice/ledger/meta"
      pattern: "\\$\\{[a-z_]+\\."
---

<objective>
Build the generation mechanism that makes SC2 structurally true: a renderer whose every output
number is addressed, not computed or typed, and two templates whose source contains no bare numeral.
Nothing is written into `docs/REPORT.md` or `README.md` in this plan — rendering goes to stdout /
the scratchpad; publishing is 28-06.

Decisions implemented: D-01, D-02, D-03, D-04 (quotes), D-06, D-07, D-08, D-09, D-10, D-11
(mechanism), D-12, D-13, D-14, D-15, D-16, D-19, D-21, D-24, D-30 (rendering side).

Purpose: the report is a prompt-free artifact — a different Claude re-rendering it gets the same
bytes (D-17, tested in 28-05/28-06).
Output: `scripts/phase28_report.py`, `scripts/phase28_report.md.tmpl`, `scripts/phase28_glance.md.tmpl`,
a dry render of both blocks in the scratchpad, one commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/28-report-the-published-null-and-milestone-close/28-CONTEXT.md
@.planning/phases/28-report-the-published-null-and-milestone-close/28-RESEARCH.md
@.planning/phases/28-report-the-published-null-and-milestone-close/28-PATTERNS.md
@.planning/phases/28-report-the-published-null-and-milestone-close/28-03-SUMMARY.md

<interfaces>
Records (paths are the renderer's constants; sha256 verified 2026-09-20):
  FRONTIER = results/phase25_frontier.json  (1f182b40…97d5, 22,311,714 B)
  CANARY   = results/phase26_canary.json    (d2a71e2d…8e19)
  ADMISSION= results/phase27_admission.json (065b2bc1…9199)
  SIGMA_ZERO = results/phase23_sigma_zero.json ; COST = results/phase23_cost.json ; CONTROL_FLOOR = results/phase23_control_floor.json
  LEDGER   = results/phase28_ledger.json  (rows + close; digest over rows only)
  OP_NOTE  = results/phase25_operational_note.md (§12.5c at :1036-1066, heading `### 12.5c FINDING, 2026-09-05 — …`)
  REQUIREMENTS = .planning/REQUIREMENTS.md (sentinels <!-- 23-12-CONTINUATION-BEGIN --> … END at :186/:271)
  ROADMAP = .planning/ROADMAP.md (v5.0 candidate bullet at :9-13 begins "- 🔮 **v5.0 (candidate, not planned)**")
Verified field shapes (RESEARCH §2 + measured today):
  frontier.arms = [dp_n8, dp_n64, adv_n8, adv_n64]; frontier.axis_for_arm; frontier.point_key_grammar = "{arm}_{axis}{value:.6f} with '.' written 'p'"
  frontier.verdicts.{capacity_branch, arm_existentials.{dp,adversarial}, arm_existential_counts, tallies, tallies_by_leg.<leg>.<V>, refused_points (6 keys), leg_refusals.adv_n64, amended_criterion, adversarial_no_replay.{code_source,dp_replay_source,finding,log_line,log_source,note}, pre_registered_null.{condition_a,epsilon_at_sigma_0p5,statement}, dp_recall_disclosure.{finding,heldout,taught}, control_readings.<leg>.recall_counts.{taught,heldout} = [k, n], extraction_ceiling.{X,n_questions,tolerated,tolerance_sentence}, capacity_per_sigma, curve_k 16, full_fidelity_k 48, ratchet_k 48, adversarial_capacity_rule_absent}
  frontier.epsilon_report.{curve_total_epsilon, k, delta, total_delta, selection_accounted, selection_accounted_reason, control_has_no_epsilon, rendered{key: sentence}, summand_keys (30)}
  frontier.points.<key>.{sigma, epsilon (null at σ=0), composed_steps 200, delta 1e-05, q 1.0, clip_norm, verdict.{verdict (null for adv_n64), early_return_reason, reasons[]}, refusal}
  canary.{summary{BROKEN,CONSISTENT,INCONCLUSIVE}, reachable_claims "4/15", reachable_keys, auditor_ceiling, power_gate{control_epsilon_lower,passed,sentence,threshold}, exclusions.{out{n,of},in{n,of}}, audited_point_keys (16 dp_n8), curve_total_epsilon, curve_total_is_context_only, unit, delta, points.<key>.{sigma, epsilon_upper, epsilon_sentence, verdict.{verdict, reasons[7]}}}
  admission.{verdict.{verdict "MOOT", reasons[]}, tallies, tallies_by_leg, cleared_counts{a,b,c,reached,refused,by_leg}, rows[44]{point_key,arm,leg,verdict,refused,cleared_a,cleared_b,cleared_c}, recall_thresholds{n8{k,n,threshold},n64{…}}, apparatus{status,reason,…}, budget{curve_k,full_k,f_y,margin_k,max_steps,relearn_cap}, x{value,n_questions,tolerated,tolerance_sentence}, frontier_sha256}
  sigma_zero.{reading, control_central_reading, deviation, floor, verdict "HALT", halt_message, floor_pin_module, floor_pin_symbol}; cost.{ratios.{dp_n8,dp_n64,non_dp}.{eval_over_training_ceiling,eval_over_training_floor}, projection_not_published}
Module constants (all import torch-free, verified): mitigation_gate.{CAPACITY_BRANCHES, V4_VERDICTS, F_Y 0.7, F_C 0.5, MARGIN_K 2}; mitigation_budget.{CURVE_K 16, FULL_FIDELITY_K 48, SIGMA_LADDER (16 floats), STEP_BUDGET 200, CLIP_NORM}; mitigation_unit.{DELTA 1e-05, SAMPLING_RATE_Q 1.0, PRIVACY_UNIT}; phase25_prereg.{PUBLICATION_OBLIGATION (7), CANARY_RESERVATIONS}; phase26_prereg.{PUBLICATION_OBLIGATION_CONTINUATION (7)}; accountant.{epsilon_for(sigma, steps, delta), sigma_for(target_epsilon, steps, delta)}; scripts/phase18_extraction.py `K = 48` at :93 — read by AST (ast.parse + module-level Assign), NOT imported.
Verified values (RESEARCH): sigma_for(4.0, 200, 1e-05) == 15.289937507119 ; epsilon_for(16.0, 200, 1e-05) == 3.7965357228934966 == points.dp_n8_sigma16p000000.epsilon ; points.dp_n8_sigma12p000000.epsilon == 5.299979064701441
Expectation quotes (D-04): .planning/research/SUMMARY.md at c673b4c, :38-42 ("…per-coordinate noise-to-signal ratio is `σ√d/L` = **72σ at L=8 facts** … reaching ε_fact ≤ 4 needs σ ≥ 15.3 … Secret Sharer Table 3 is the direct precedent …") and :386-397 (the `L=8 → 72σ, L=64 → 9σ, L=576 → 1σ` lever paragraph) — copy sentences VERBATIM from `git show c673b4c:.planning/research/SUMMARY.md`
Sentinel + writer shapes: PATTERNS §`scripts/phase28_report.py` (`_prove`, produced-bytes prefix check, `_markers(stem)`)
Census: no `os.replace`; never call/import `mitigation_point_verdict`; no `train_arm(` token; no torch import; ruff line length 100
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: scripts/phase28_report.py — records, bindings, derived values, tables, quotes, slices, install, CLI</name>
  <files>scripts/phase28_report.py</files>
  <read_first>
    - scripts/_addendum.py (whole file — bootstrap, `_prove`, produced-bytes check)
    - scripts/_prose.py (`normalized`)
    - tests/test_phase25_correction.py:104-127 (`_markers`, `_span`)
    - tests/test_phase18_docs.py:269-281 (`_anchored_section`)
    - scripts/phase25_prereg.py:410-495 and scripts/phase26_prereg.py:350-396 (obligation tuples; the `<key>` wildcard and `<artifact absent>` sentinel)
    - src/personacore/privacy/accountant.py:815-830, :985-1000 (`epsilon_for`, `sigma_for` signatures)
    - .planning/phases/28-report-the-published-null-and-milestone-close/28-RESEARCH.md §2 (field map), Pitfalls 1, 2, 4
    - results/phase28_ledger.json (row keys, `close`)
  </read_first>
  <action>
Create `scripts/phase28_report.py` (stdlib + repo modules only; bootstrap `scripts/` and `src/` onto `sys.path` as `_addendum.py` / `tests/test_phase27_prereg.py:27-29` do). Structure — names are binding:

- Constants: `PUBLISHED = "2026-09-<day>"` (pinned; 28-06 may edit it before the publishing commit, never after), `REPORT_STEM = "PHASE28-REPORT"`, `GLANCE_STEM = "PHASE28-GLANCE"`, `REPORT_PATH = docs/REPORT.md`, `README_PATH = README.md`, `GLANCE_HEADING = "## Results at a glance"`, `TEMPLATE_REPORT = scripts/phase28_report.md.tmpl`, `TEMPLATE_GLANCE = scripts/phase28_glance.md.tmpl`, record path constants from `<interfaces>`, `EXPECTATION_COMMIT = "c673b4c"`, `QUOTES: dict[str, tuple[str, str]]` mapping quote name → `(source, verbatim_text)` where source is `"git:c673b4c:.planning/research/SUMMARY.md"` or a HEAD path (`.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`, `.planning/phases/27-relearning-attack/27-HUMAN-UAT.md`); `SLICES: dict[str, tuple[path, kind, anchor]]` for `op_note_12_5c` (anchored heading `### 12.5c` stop `### `), `req_23_12_continuation` (sentinels `23-12-CONTINUATION`), `roadmap_v5` (anchored on the line starting `- 🔮 **v5.0` up to the next line starting `- ` or blank).
- `resolve(node, path)`: split on `.`; dict → `node[part]`; list → `node[int(part)]`; raise `KeyError(path)` on absence (never `.get`).
- `_fmt(value)`: `bool` → `true`/`false`; `int` → `str`; `float` → `repr` (full precision); `str` → as is; `list` of scalars → `", ".join(_fmt)`; anything else → `KeyError("unrenderable")`.
- `load()` → dict of parsed records `{frontier, canary, admission, sigma_zero, cost, control_floor, ledger}` read with `json.loads(path.read_bytes())` and a parallel `{name: (sha256_hex, n_bytes)}`; `ledger_rows_digest(ledger)` = sha256 of `json.dumps(ledger["rows"], sort_keys=True, ensure_ascii=False).encode()`.
- `class Bindings(collections.abc.Mapping)`: `__getitem__(key)` dispatches on the first dotted segment: `meta.published` → PUBLISHED; `frontier|canary|admission|sigma_zero|cost|control_floor` → `_fmt(resolve(record, rest))`; `const.<module>.<NAME>` → `getattr(module, NAME)` for `mitigation_gate|mitigation_budget|mitigation_unit|phase25_prereg|phase26_prereg`, plus `const.phase18_extraction.K` via AST (`ast.parse` the file, find module-level `Assign` to `K`, `ast.literal_eval`) — never import phase18_extraction; `derived.<name>` → `DERIVED[name](records)`; `table.<name>` → `TABLES[name](records)`; `quote.<name>` → `QUOTES[name][1]`; `slice.<name>` → the extracted text; `ledger.row.<ID>.<key>` → the row with that id; `ledger.count.<disposition>` → `len` of the filter. Unknown prefix or missing name → `KeyError`. `__iter__`/`__len__` may be trivial (Template only calls `__getitem__`).
- `class _Template(string.Template)`: `braceidpattern = r"[A-Za-z_][A-Za-z0-9_.:<>/-]*"` so `${frontier.points.dp_n8_sigma16p000000.epsilon}` is one identifier; render with `.substitute(bindings)` (KeyError on a missing binding — the RED D-23 wants); `$` alone must not appear in templates (use `$$` if ever needed).
- `DERIVED` registry (each documented one line, inputs named): `noised_dp_total` = count of admission `rows` with `arm == "dp"` and frontier `points[key].sigma > 0`; `noised_dp_cleared_a`, `noised_dp_cleared_b`, `noised_dp_cleared_c` = same filter with the flag true (D-02 line 2 — NOT `cleared_counts.b`); `recall_fraction.<leg>.<taught|heldout>` → `"k/n (k/n as repr float)"` from `control_readings`; `sigma_for_eps4` = `repr(accountant.sigma_for(4.0, p.composed_steps, p.delta))` with `p = frontier.points["dp_n8_sigma16p000000"]`, and in the same function `_prove(accountant.epsilon_for(16.0, p.composed_steps, p.delta) == p["epsilon"], …)` (D-06 — the render refuses if the accountant no longer re-derives the record); `sigma_zero_deviation_over_floor` = `repr(sigma_zero.deviation / sigma_zero.floor)`; `obligation_count` = `len(PUBLICATION_OBLIGATION) + len(PUBLICATION_OBLIGATION_CONTINUATION)`; `adv_n64_dialogue_range`, `adv_n64_retention_range` = min/max over the six `refused_points` of `points[k].verdict.point_dialogue_ppl_on` and `point_retention_ppl` (read the exact field names from one adv_n64 point with `python -c` first; if absent, drop these two bindings and quote `leg_refusals.adv_n64` only); `replay_windows_n8`, `replay_windows_n64` — NOT derived: quote `adversarial_no_replay.dp_replay_source` whole (it carries both figures); `ledger_v3_tech_debt_count`, `ledger_v3_stale_stamp_count`, `ledger_v4_count`, `ledger_named_limitation_count`, `ledger_total` = `len` over filters; `n_arms` = `len(frontier.arms)`; `capacity_values` = the leg suffixes from `frontier.arms` (`n8`, `n64` parsed after `_`) so `n=${derived.capacity_n8}` style labels bind to record keys (define `capacity_n8` → `"8"`, `capacity_n64` → `"64"` by stripping the `n`).
- `TABLES` registry, each returning a Markdown table string: `epsilon_by_sigma` (rows = `canary.audited_point_keys` sorted by `frontier.points[k].sigma`; columns σ | ε (frontier, or the literal text of `epsilon_report.control_has_no_epsilon`'s first clause for the σ=0 row — take it from the field, do not type) | gate verdict `dp_n8` | gate verdict for the `dp_n64` twin key (same σ; verdict text or `early_return_reason`) | canary verdict | canary `epsilon_sentence` | canary reasons joined by `; ` — plus a final note column stating n=64 shares the accountant value and is unaudited, worded once in the template, not per row); `tallies_by_leg` (leg × PASS/FAIL/INCONCLUSIVE/REFUSED from `frontier.verdicts.tallies_by_leg`); `cleared_by_leg` (from `admission.cleared_counts.by_leg`); `adv_points` (12 adversarial keys: verdict or `early_return_reason`, `reasons[0]`); `self_corrections` (two rows bound to `cost.ratios.*` and `sigma_zero.*`); `named_limitations` (ledger rows `disposition == NAMED-LIMITATION`, columns id | milestone | source | reason | evidence); `withheld_claims` (the NAMED-LIMITATION ids+reasons followed by the two `arm_existentials` strings); `ledger_by_disposition` (disposition | count via len); `sources` (record | sha256 | bytes | record-carried git_sha where present; ledger row as `rows-sha256`); `build_pointers` (four rows: privacy unit → `mitigation_unit.PRIVACY_UNIT` + `results/phase21_*`; per-example clipping → `results/phase22_*`; fact-aligned sampler → phase 21/25 records; pre-registration-as-phase-zero → `scripts/phase20_*prereg*` — executor names the real tracked files by `git ls-files`, one pointer each, no narrative).
- `render(template_path, records)` → `_Template(text).substitute(Bindings(records))`; `render_report()` / `render_glance()` call `load()` themselves (direct read, no caching — D-21).
- `install(path, stem, block, *, glance_heading=None)`: read text; `_prove(text.count(BEGIN) == text.count(END) <= 1)`; if present, replace the span; else if `glance_heading` — insert `BEGIN + block + END` immediately after the heading line and its following blank line, BEFORE the first existing `- ` bullet (zero deletions); else append `"\n\n" + BEGIN + block + END + "\n"` at EOF; `_prove(updated.startswith(prefix))` on the produced text where `prefix` is everything before the insertion point; `Path.write_text`. Refuse with a clear message if `text.count(glance_heading) != 1`.
- `main(argv)`: `render --stdout report|glance` (print), `render --out <dir>` (write both blocks to files), `write` (install both — PRE-PUBLISH ONLY; the docstring says so and names D-20), `check` (re-render and compare with the committed spans; exit 1 on drift, printing a unified diff). No `today()`, no `datetime.now`, no `git rev-parse HEAD`, no `logs/` path anywhere in the module.
Threat references: T-28-01 (records read from bytes, digests recorded), T-28-03 (numbers addressed, never typed), T-28-06 (`logs/` never read), T-28-02 (`check` is the drift detector; `write` documented pre-publish only).
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/python -c "
import sys; sys.path.insert(0,'scripts'); import phase28_report as r
assert not any(m.startswith('torch') for m in sys.modules), 'torch imported'
rec=r.load(); b=r.Bindings(rec)
assert b['frontier.verdicts.capacity_branch']=='null-at-both-capacities'
assert b['derived.noised_dp_total']=='30' and b['derived.noised_dp_cleared_a']=='30' and b['derived.noised_dp_cleared_b']=='0'
assert b['derived.sigma_for_eps4']=='15.289937507119'
assert b['const.mitigation_budget.FULL_FIDELITY_K']=='48' and b['const.phase18_extraction.K']=='48'
try: b['frontier.verdicts.no_such_field']; raise SystemExit('missing binding did not raise')
except KeyError: pass
src=open('scripts/phase28_report.py').read()
for bad in ('today()','datetime.now','rev-parse','os.replace','logs/','mitigation_point_verdict'): assert bad not in src, bad
print('renderer OK')" && .venv/bin/ruff check scripts/phase28_report.py && .venv/bin/ruff format --check scripts/phase28_report.py</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/phase28_report.py` exports `PUBLISHED`, `REPORT_STEM`, `GLANCE_STEM`, `QUOTES`, `SLICES`, `resolve`, `load`, `ledger_rows_digest`, `Bindings`, `DERIVED`, `TABLES`, `render_report`, `render_glance`, `install`, `main`
    - Importing the module leaves `torch` out of `sys.modules`; the source contains none of: `today()`, `datetime.now`, `rev-parse`, `os.replace`, `logs/`, `mitigation_point_verdict`, `import phase18_extraction`, `train_arm(`
    - `Bindings(load())['derived.noised_dp_cleared_b'] == '0'` and `['derived.noised_dp_total'] == '30'` (derived from `rows` + `sigma`, not from `cleared_counts.b`); `['derived.sigma_for_eps4'] == '15.289937507119'`
    - `resolve(record, "verdicts.no_such")` raises `KeyError`; `Bindings` on an unknown prefix raises `KeyError`
    - `DERIVED["sigma_for_eps4"]` contains the `epsilon_for(16.0, …) == p["epsilon"]` proof (grep `epsilon_for` in the module)
    - `ruff check` and `ruff format --check` clean on the file
  </acceptance_criteria>
  <done>Every quantity the report needs is addressable by name; nothing is computed in prose.</done>
</task>

<task type="auto">
  <name>Task 2: The two templates — placeholders only, D-01 order, no bare numeral</name>
  <files>scripts/phase28_report.md.tmpl, scripts/phase28_glance.md.tmpl</files>
  <read_first>
    - scripts/phase28_report.py (Task 1 — the binding names available)
    - docs/REPORT.md:1062-1145 (the Phase 18 section — house style for a quoted verdict) and :1316-1326 (the last section + the "Appended additively" line)
    - README.md:109-147 (`## Results at a glance` — existing bullet grammar: bold headline, qualifier, links to the record and to `docs/REPORT.md#<anchor>`)
    - tests/test_phase15_docs.py:355-370 (`_github_anchor` — how the REPORT anchor is derived from a heading)
    - .planning/phases/28-report-the-published-null-and-milestone-close/28-CONTEXT.md D-01..D-15, D-19, and the `<specifics>` block ("the null must be unskippable")
    - .planning/research/SUMMARY.md:36-42, :384-398 via `git show c673b4c:.planning/research/SUMMARY.md` (the sentences QUOTES must carry verbatim)
  </read_first>
  <action>
Write `scripts/phase28_report.md.tmpl` in D-01's order. Rules that the 28-05 scan enforces: the ONLY digits allowed outside `${…}` are in `Phase \d+`, hyphenated upper-case ids (`RELRN-02`, `D-40`, `WR-05`, `T-28-01`, `D-25-18-ADV64-REFUSED`), ISO dates, 7-40-hex SHAs, `§\d+(\.\d+)?[a-z]?`, plan ids `\d{2}-\d{2}`, milestone tags `v\d+\.\d+`, the literal token `sha256`, and 4-digit years. No ordered lists (`1.`), no `n=8`/`L=64`/`K=48`/`σ=0`/`0/648` typed — every one is a placeholder (`n=${derived.capacity_n8}`, `K=${const.mitigation_budget.FULL_FIDELITY_K}`, `σ=${frontier.points.dp_n8_sigma16p000000.sigma}`, `${derived.recall_fraction.dp_n64.taught}`). No `$` other than placeholders.

Section skeleton (headings are `###` under ONE new `##`; the `##` title carries the branch name as a binding):
- `## v4.0 — the published null: \`${frontier.verdicts.capacity_branch}\` (recorded ${meta.published})` then the house line `*Appended additively. No line above this heading is altered. Every number below is a binding to a committed record field or module constant, rendered by \`scripts/phase28_report.py\`; the template is scanned for hand-typed numerals.*`
- Lead, three lines, no subsection (D-02, D-03): line 1 quotes `${frontier.verdicts.capacity_branch}` and BOTH `${frontier.verdicts.arm_existentials.dp}` / `${frontier.verdicts.arm_existentials.adversarial}` verbatim; line 2 the mechanism: `${derived.noised_dp_cleared_a} of ${derived.noised_dp_total}` noised DP points cleared (a) and `${derived.noised_dp_cleared_b}` cleared (b), then `${frontier.verdicts.dp_recall_disclosure.finding}` — DP removed the leakage by removing the memory; line 3 the caveat: `dp_n64`'s own σ=${frontier.points.dp_n64_sigma0p000000.sigma} control learned `${derived.recall_fraction.dp_n64.taught}` (held-out `${derived.recall_fraction.dp_n64.heldout}`) against `dp_n8`'s `${derived.recall_fraction.dp_n8.taught}`, so the n=${derived.capacity_n64} branch is read against a control that barely learned; beside it `${quote.expectation_lever}` (the L=8 → 72σ / L=64 → 9σ sentence).
- `### The standing expectation, recorded before any run` (D-04, D-05, D-06): `${quote.expectation_l8}` with its source line (SUMMARY.md at `c673b4c`, `:38-42`, restated at `:386-397`; REQUIREMENTS.md `e144417`, ROADMAP.md `3c80037` cited as restatements — these SHAs are exempt tokens); ONE row publishing σ ≥ 15.3 three ways: the quote; the record bracket σ=${frontier.points.dp_n8_sigma12p000000.sigma} → ε ${frontier.points.dp_n8_sigma12p000000.epsilon}, σ=${frontier.points.dp_n8_sigma16p000000.sigma} → ε ${frontier.points.dp_n8_sigma16p000000.epsilon}; and `sigma_for(${const...}` — write `accountant.sigma_for(target ε = ${derived.target_epsilon_fact}, steps = ${frontier.points.dp_n8_sigma16p000000.composed_steps}, δ = ${frontier.points.dp_n8_sigma16p000000.delta}) = ${derived.sigma_for_eps4}` (add `target_epsilon_fact` to DERIVED as the literal argument the renderer passes — `"4.0"` — so the number lives in code beside the call, not in the template); one sentence that the renderer refuses to render unless `epsilon_for(σ=16)` re-derives the record's ε bit-identically; a pointer to the CPU test that proves `c673b4c` precedes every `results/phase2[0-8]_*` first-add (`tests/test_phase28_prereg.py`).
- `### ε per σ, each beside its canary verdict` (D-07): `${table.epsilon_by_sigma}`; then the curve total `${frontier.epsilon_report.curve_total_epsilon}` beside `${canary.curve_total_is_context_only}` and `${frontier.epsilon_report.control_has_no_epsilon}` (LIMITATION 2) and `${frontier.epsilon_report.selection_accounted_reason}`; one sentence that the accountant value is shared by n=${derived.capacity_n64} and that leg is structurally unauditable (`${const.phase25_prereg.CANARY_RESERVATIONS}` is a dict — bind its `canary_population_rule` via `const.phase25_prereg.CANARY_RESERVATIONS.canary_population_rule`; extend `const.` resolution to descend into dict constants).
- `### The adversarial arm is recipe-confounded, not a ratio result` (SC4, D-08, D-09, D-10): `${frontier.verdicts.adversarial_no_replay.finding}`, `${...log_line}` with `${...log_source}` and `${...code_source}`, `${...dp_replay_source}` (carries 32/256), `${...note}`; the §12.5c slice `${slice.op_note_12_5c}` in a blockquote; `${frontier.verdicts.amended_criterion}`; `${frontier.verdicts.leg_refusals.adv_n64}`; `${frontier.points.adv_n64_ratio0p000000.verdict.early_return_reason}` (confirm the key with `python -c`); `${table.adv_points}`; a sentence that condition (c) was never applied to the six `adv_n64` points and failed on the six `adv_n8` points on both the dialogue band and the retention cap (bound to the `reasons` column in the table, not typed); `${table.tallies_by_leg}`.
- `### Canary audit` : `${canary.summary.CONSISTENT}` CONSISTENT / `${canary.summary.BROKEN}` BROKEN / `${canary.summary.INCONCLUSIVE}` INCONCLUSIVE over `${derived.canary_audited_count}` (= `len(audited_point_keys)`, add to DERIVED) `dp_n8` points; `${canary.reachable_claims}`, `${canary.auditor_ceiling}`, `${canary.power_gate.sentence}` with `${canary.power_gate.control_epsilon_lower}` ≥ `${canary.power_gate.threshold}` (`${canary.power_gate.passed}`); `${canary.exclusions.out.n}` of `${canary.exclusions.out.of}` excluded; the could-not-have-failed disclosure `${ledger.row.CANARY-COULD-NOT-HAVE-FAILED.reason}`.
- `### Relearning: MOOT` : `${admission.verdict.verdict}`; `${admission.verdict.reasons.0}`; `${table.cleared_by_leg}`; `${admission.apparatus.status}` / `${admission.apparatus.reason}`; the RELRN-02..05 limitation `${ledger.row.RELRN-02.reason}` once (they share the sentence).
- `### What the milestone corrected about itself` (D-12): `${table.self_corrections}` (23-12 retraction: `${cost.projection_not_published}` and the measured ratios `${cost.ratios.dp_n8.eval_over_training_floor}`–`${...ceiling}`, `dp_n64` likewise; σ=0 halt: `${sigma_zero.verdict}`, `${sigma_zero.reading}` vs `${sigma_zero.control_central_reading}`, deviation `${sigma_zero.deviation}` = `${derived.sigma_zero_deviation_over_floor}` × floor `${sigma_zero.floor}` (`${sigma_zero.floor_pin_module}::${sigma_zero.floor_pin_symbol}`)); blockquote `${slice.req_23_12_continuation}` — if the slice is too long for the section (> ~40 lines), replace with a `quote.req_23_12_retraction` constant carrying its first paragraph verbatim and cite the sentinel; one sentence: a retraction is evidence the process works.
- `### Figures` (D-14): `![DP arm](../results/phase25_frontier_dp.png)` and `![Adversarial arm](../results/phase25_frontier_adversarial.png)` with captions bound to `${frontier.verdicts.tallies_by_leg.dp_n8.FAIL}` etc. and the commit `6af3fa0` as an exempt SHA token.
- `### Ship block` (D-13): ships — the published null (`${frontier.verdicts.capacity_branch}`) and the from-scratch apparatus (DP-SGD, accountant, canary, relearning apparatus as CPU-tested code — pointers to `src/personacore/privacy/`, `scripts/phase26_*.py`, `scripts/phase27_relearn.py`); withholds — `${table.withheld_claims}` (NAMED-LIMITATION rows + existentials); explicit sentences: no claim that a mitigation preserves weight-based memory; no conclusion about adversarial ratio.
- `### Named-limitation register` (D-30): `${table.named_limitations}` and `${derived.ledger_named_limitation_count}` rows of `${derived.ledger_total}` (`${derived.ledger_v3_tech_debt_count}` v3.0 tech-debt + `${derived.ledger_v3_stale_stamp_count}` v3.0 stale stamps + `${derived.ledger_v4_count}` v4.0); `${table.ledger_by_disposition}`; the D-27 one-liner `${ledger.row.SC3-SHA256-CLAUSE.reason}`.
- `### What v5.0 would measure` (D-15): `${slice.roadmap_v5}` and `${ledger.row.WR-05.reason}`; obligations (a)–(f) appear in the register already — one sentence pointing there.
- `### Build decisions — one pointer each` (D-01): `${table.build_pointers}`.
- `### Provenance` : `${table.sources}`; `${frontier.provenance.git_sha}`, `${canary.emitted_git_sha}`, `${admission.provenance.git_sha}` (confirm field names with `python -c`); the sentence that the rows digest excludes `close` so the CI run id (D-38) can land without a re-render.

Write `scripts/phase28_glance.md.tmpl` (README bullets, D-11): two bullets in the existing grammar, no heading, each ending with links to the record and to `docs/REPORT.md#<anchor>` where the anchor is `_github_anchor` of the new `##` title (compute it once with the same rule as tests/test_phase15_docs.py and bind as `derived.report_anchor` so it is not typed): v4.0 bullet — `**${frontier.verdicts.capacity_branch}**`, `${frontier.verdicts.arm_existentials.dp}`, `${derived.noised_dp_cleared_a} of ${derived.noised_dp_total}` cleared (a) / `${derived.noised_dp_cleared_b}` cleared (b), canary `${canary.summary.CONSISTENT}`/`${canary.summary.BROKEN}`, relearning `${admission.verdict.verdict}`, the n=${derived.capacity_n64} caveat in one clause, adversarial arm recipe-confounded (no ratio conclusion); v3.0 bullet — bound to the v3.0 records the README already links (`results/phase18_extraction_report.md`'s `LEAKAGE_DEMONSTRATED`, `results/phase19_erasure_report.md`'s `FAILURE` / DO NOT SHIP): bind through `slice.`/`quote.` entries whose source is those committed `.md` files (verbatim verdict lines, verified under `normalized` by 28-05), plus the count of the DO NOT SHIP defects if and only if a record field carries it — otherwise omit the count.

Dry render: `.venv/bin/python scripts/phase28_report.py render --out <scratchpad dir>/28-04-render/` and read both outputs end to end; fix any `KeyError` by adding the binding (never by typing the value). Run the numeral scan yourself before 28-05 makes it a test: `grep -nP '\d' scripts/phase28_report.md.tmpl | grep -vP '\$\{[^}]*\}'` and inspect every hit against the exempt list; paste the residual hits (expected: only exempt tokens) into the SUMMARY. Commit: `feat(28-04): phase28_report renderer + templates — every number a binding (D-16, D-19, D-21)`; `make lint` first.
Threat references: T-28-03 (template source carries no bare numeral), T-28-06 (`logs/` quoted through `log_source` only).
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && OUT=$(mktemp -d) && .venv/bin/python scripts/phase28_report.py render --out "$OUT" && RENDER_OUT="$OUT" .venv/bin/python - <<'EOF'
import re, pathlib
root = pathlib.Path('/Users/juliorcoelho/PersonaCore')
exempt = [r'\$\{[^}]*\}', r'Phase \d+', r'\b[A-Z][A-Z0-9]*(?:-[A-Z0-9]+)+\b', r'\b\d{4}-\d{2}-\d{2}\b', r'\b[0-9a-f]{7,40}\b', r'§\d+(?:\.\d+)?[a-z]?', r'\b\d{2}-\d{2}\b', r'\bv\d+\.\d+\b', r'sha256', r'\b(?:19|20)\d{2}\b']
for name in ('scripts/phase28_report.md.tmpl', 'scripts/phase28_glance.md.tmpl'):
    text = (root / name).read_text(encoding='utf-8')
    for pat in exempt:
        text = re.sub(pat, ' ', text)
    bad = [(i + 1, line) for i, line in enumerate(text.splitlines()) if re.search(r'\d', line)]
    assert not bad, (name, bad[:5])
    assert '${frontier.verdicts.capacity_branch}' in (root / name).read_text(encoding='utf-8')
import os
out = pathlib.Path(os.environ['RENDER_OUT'])
rep = next(out.glob('*report*')).read_text(encoding='utf-8'); gl = next(out.glob('*glance*')).read_text(encoding='utf-8')
assert 'null-at-both-capacities' in rep and '0 of 32 point(s) examined returned PASS' in rep and '2387.299119573244' in rep and '15.289937507119' in rep
assert rep.count('\n## ') == 1 and '\n## ' not in gl
assert '7,581 teaching + 0 replay' in rep and 'replay_windows=256' in rep
print('templates OK')
EOF</automated>
  </verify>
  <acceptance_criteria>
    - Both `.tmpl` files exist; after stripping `${…}` placeholders and the D-19 exempt grammar (the verify script's list), no line contains a digit
    - The rendered report contains, in this order: `null-at-both-capacities`, both `arm_existentials` strings verbatim, the `dp_recall_disclosure.finding` sentence, `87/1008`, the expectation quote text containing `72σ`, `15.289937507119`, the ε table with a row containing `3.7965357228934966`, `2387.299119573244`, `7,581 teaching + 0 replay`, `replay_windows=256`, `D-25-18-ADV64-REFUSED`, `MOOT`, `HALT`, `phase25_frontier_dp.png`, a `### Named-limitation register` heading, a `### What v5.0 would measure` heading, a `### Provenance` heading — executor asserts each with `grep -n` on the rendered file and pastes the line numbers
    - The rendered report has exactly one `## ` heading; the rendered glance has none
    - `python scripts/phase28_report.py check` exits non-zero at this point with a message that the sentinels are absent (pre-publish state), quoted
    - `make lint` clean; one commit containing the three files
  </acceptance_criteria>
  <done>Both blocks render from records alone; the template carries prose and bindings, nothing else.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed records → prose | the renderer is the only path; it indexes with `[]`, formats with `repr`, and records digests |
| template author → numerals | the template is scanned source; quantities can only enter through bindings |
| renderer → publication surfaces | `install` writes between sentinels only and proves the prefix on the produced bytes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Tampering | record edited to change the report | mitigate | digests recorded in `table.sources`; frontier one-commit guard exists (`tests/test_phase27_prereg.py:130`) |
| T-28-02 | Repudiation | post-publish re-render | mitigate | `write` documented pre-publish only; `check` detects drift; 28-06's byte-identity test |
| T-28-03 | Tampering | hand-typed numeral | mitigate | template-only digits are placeholders; the verify script's scan; 28-05 makes it a test with a planted RED |
| T-28-06 | Information disclosure | reading gitignored `logs/phase25_sweep.out` | mitigate | `logs/` absent from the module; the log line comes from `adversarial_no_replay.log_line` |
| T-28-13 | Tampering | clock or HEAD SHA in bindings | mitigate | `PUBLISHED` constant; source string checks for `today()` / `now` / `rev-parse` |
| T-28-14 | Tampering | mis-derived count (`cleared_counts.b == 4`) | mitigate | `noised_dp_cleared_b` derived from `rows` + `sigma > 0`; asserted `'0'` in verify |
| T-28-SC | Tampering | package installs | accept | stdlib only; nothing installed |
</threat_model>

<verification>
- Renderer imports torch-free; bindings resolve; dry render of both blocks reviewed end to end; template scan clean; lint clean; committed
</verification>

<success_criteria>
- SC2's "tables are generated from committed records" holds mechanically: every quantity in both rendered blocks is traceable to a `${…}` binding, and the template source has no bare numeral
</success_criteria>

<output>
Create `.planning/phases/28-report-the-published-null-and-milestone-close/28-04-SUMMARY.md` when done — include the binding-name inventory (every `${…}` used, grouped by prefix), the dry-render file paths, the residual scan hits, and the commit SHA.
</output>
