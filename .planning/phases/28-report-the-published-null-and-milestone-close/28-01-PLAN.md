---
phase: 28-report-the-published-null-and-milestone-close
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/personacore/evaluation/perplexity.py
  - scripts/phase16_persistence.py
  - tests/test_perplexity.py
  - tests/test_phase16_driver.py
  - tests/test_package.py
autonomous: true
requirements: [RPT-03]
must_haves:
  truths:
    - "tests/test_package.py carries a test that parses `[project].dependencies` from `git show v1.0:pyproject.toml`, `v2.0`, `v3.0` with stdlib `tomllib` and asserts equality with HEAD, refusing on a shallow clone or a tagless clone — the four-milestone claim is a check, not a sentence (D-25, RPT-03, SC3)"
    - "The whole-file sha256 pin stays as a change detector under a name and message that are true at HEAD; the name `test_pyproject_unchanged_since_v2_close` no longer exists in the file (D-26, D-27)"
    - "`src/personacore/evaluation/perplexity.py` docstring states the denominator `corpus_len - 1` and no longer states `corpus_len - n_windows`; a test asserts both facts on `perplexity.__doc__` (D-32)"
    - "`scripts/phase16_persistence.py`'s `build_overwrite_statement` docstring no longer types the allowlist size; a test binds the sentence to `len(PERSONA_ALLOWLIST)` from `tests/test_phase14_scoring.py` (D-32)"
    - "`pytest tests/test_phase16_prereg.py -q` is green after the `phase16_persistence.py` edit — the module was re-measured as unguarded before the `FIXED` disposition was assigned (D-32)"
  artifacts:
    - path: "tests/test_package.py"
      provides: "D-25 tomllib dependency-equality test; D-26 renamed sha pin test"
      contains: "tomllib"
    - path: "src/personacore/evaluation/perplexity.py"
      provides: "corrected denominator docstring"
      contains: "corpus_len - 1"
    - path: "tests/test_perplexity.py"
      provides: "docstring-text test for D-32"
      contains: "corpus_len - n_windows"
  key_links:
    - from: "tests/test_package.py"
      to: "git tags v1.0 v2.0 v3.0"
      via: "subprocess git show <tag>:pyproject.toml → tomllib.loads"
      pattern: "git.*show.*pyproject"
    - from: "tests/test_phase16_driver.py"
      to: "tests/test_phase14_scoring.py::PERSONA_ALLOWLIST"
      via: "len(PERSONA_ALLOWLIST) compared to the docstring's stated count word"
      pattern: "PERSONA_ALLOWLIST"
---

<objective>
Close the code-side debt items that Phase 28's ledger will mark `FIXED` — each with the regression
test D-29 demands — and turn RPT-03's "zero new runtime dependencies for a fourth milestone" from a
sentence into a `tomllib` test.

Decisions implemented: D-25, D-26, D-27 (measured, cited in the test message), D-32.

Purpose: D-29 says `FIXED` requires a commit AND a test. Plan 28-03 writes the ledger rows that cite
these tests by node id, so they must exist first.
Output: two docstring corrections, three test edits, all green in the quick suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/28-report-the-published-null-and-milestone-close/28-CONTEXT.md
@.planning/phases/28-report-the-published-null-and-milestone-close/28-PATTERNS.md

<interfaces>
From tests/test_package.py (verified 2026-09-20):
  PYPROJECT_SHA256 = "15ffd6b58e289447ac6460bdd6210c04d20d5ff5831f741bb3db3bdc0ca7926f"  (line 12)
  def test_pyproject_unchanged_since_v2_close():  (line 28) — reads pyproject.toml as BYTES (line 36)
  Message text at lines 38-46 claims "byte-identical at v3.0 close" — FALSE since 5065bc5 (+license = "MIT")
From tests/test_phase27_prereg.py:57-61 — `_git(*args)` helper; :77-81 — the shallow-clone refusal string
From tests/test_phase14_scoring.py:517 — PERSONA_ALLOWLIST (the count the phase16 docstring types)
From tests/test_perplexity.py:121-122 — `assert ntok == n_tokens - 1` (the true denominator)
Measured (RESEARCH §5): [project].dependencies at v1.0, v2.0, v3.0, HEAD == ["numpy~=2.4", "regex~=2026.5"]; tags present: m1-demo-v1 v1.0 v2.0 v3.0
Census rules for new test code (PATTERNS §Shared): never write the literal `== 10`; never write the token `train_arm(`; no `os.replace`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-25 tomllib dependency-equality test and D-26 rename of the sha pin</name>
  <files>tests/test_package.py</files>
  <read_first>
    - tests/test_package.py (whole file — the pin at :12, the test at :28-46, existing imports)
    - tests/test_phase27_prereg.py:57-81 (`_git` helper shape and the shallow-clone refusal message to copy)
    - .planning/phases/28-report-the-published-null-and-milestone-close/28-RESEARCH.md §5 and §Code Examples "D-25"
    - .planning/phases/28-report-the-published-null-and-milestone-close/28-CONTEXT.md D-25, D-26, D-27
  </read_first>
  <action>
1. Add `import subprocess` and `import tomllib` to tests/test_package.py. Add a module-level `_git(*args)` helper copied from tests/test_phase27_prereg.py:57-61 (cwd=_ROOT, check=True, stdout stripped) and a `_deps(rev)` helper that returns `tomllib.loads(_git("show", f"{rev}:pyproject.toml"))["project"]["dependencies"]`.
2. Add `test_runtime_dependencies_identical_across_four_milestones` (D-25): first assert `_git("rev-parse", "--is-shallow-repository") == "false"` with the Phase 26/27 refusal message (copy :77-81 verbatim, adjusting the file name); then assert `set(_git("tag", "-l").split()) >= {"v1.0", "v2.0", "v3.0"}` with a message naming `fetch-depth: 0` (RESEARCH A3 — a tagless clone must refuse loudly, not pass vacuously); then compute `head = tomllib.loads((_ROOT / "pyproject.toml").read_text(encoding="utf-8"))["project"]["dependencies"]` and assert `_deps("v1.0") == _deps("v2.0") == _deps("v3.0") == head`, with a message listing all four lists. Do NOT type the dependency names in the test — the equality across tags is the claim; the SUMMARY quotes the measured list.
3. D-26: rename `test_pyproject_unchanged_since_v2_close` → `test_pyproject_sha256_pin_detects_any_change`. Rewrite its docstring and assertion message so nothing in them is false at HEAD: state that the pin is a CHANGE DETECTOR, not RPT-03's proof (D-25's test is the proof); record D-27 as a dated one-liner inside the docstring: "2026-09-01, commit 5065bc5, added the single line `license = \"MIT\"` and re-pinned this constant in the same commit; the dependency table did not change — the reviewed decision stands." Keep `read_bytes()` and the "BYTES, never text" paragraph. Keep `PYPROJECT_SHA256` unchanged (verify `shasum -a 256 pyproject.toml` still equals it).
4. Run `.venv/bin/pytest tests/test_package.py -q -v` and `make lint`; quote both in the SUMMARY. Confirm `grep -c "test_pyproject_unchanged_since_v2_close" tests/test_package.py` is 0 and that no other file references the old name (`grep -rn "test_pyproject_unchanged_since_v2_close" tests/ scripts/ .planning/phases/28-*` → only planning prose, if any).
Threat references: T-28-04 (shallow/tagless clone vacuity refused), T-28-SC (dependency freeze is now mechanical).
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/pytest tests/test_package.py -q -x && test "$(grep -c 'test_pyproject_unchanged_since_v2_close' tests/test_package.py)" = "0" && grep -q "def test_runtime_dependencies_identical_across_four_milestones" tests/test_package.py && grep -q "^import tomllib" tests/test_package.py</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_package.py` contains `def test_runtime_dependencies_identical_across_four_milestones` and `def test_pyproject_sha256_pin_detects_any_change`; `grep -c "def test_pyproject_unchanged_since_v2_close"` returns 0
    - The new test contains the strings `--is-shallow-repository`, `"tag", "-l"`, `tomllib.loads`, and `["project"]["dependencies"]`
    - The renamed test's docstring contains `5065bc5` and `license = "MIT"` and does not contain the phrase `byte-identical at v3.0 close`
    - `.venv/bin/pytest tests/test_package.py -q` reports all passed; `make lint` prints `All checks passed!`
    - `shasum -a 256 pyproject.toml` equals `15ffd6b58e289447ac6460bdd6210c04d20d5ff5831f741bb3db3bdc0ca7926f` (pyproject.toml untouched)
  </acceptance_criteria>
  <done>RPT-03's guarantee is a tomllib check across four tags; the sha pin survives under a true name.</done>
</task>

<task type="auto">
  <name>Task 2: D-32 docstring corrections with regression tests, guard status re-measured</name>
  <files>src/personacore/evaluation/perplexity.py, scripts/phase16_persistence.py, tests/test_perplexity.py, tests/test_phase16_driver.py</files>
  <read_first>
    - src/personacore/evaluation/perplexity.py:1-30 (the module docstring; lines 11-13 carry `corpus_len - n_windows`)
    - tests/test_perplexity.py:112-126 (the `ntok == n_tokens - 1` assertion and the file's import style)
    - scripts/phase16_persistence.py:1596-1612 (the `build_overwrite_statement` docstring; :1605 types "exactly two entries")
    - tests/test_phase14_scoring.py:510-525 (`PERSONA_ALLOWLIST` definition)
    - tests/test_phase16_driver.py:1-40 (imports / sys.path bootstrap to reuse) and :1775-1790 (the R-3 weak assertion — read only, do not edit)
    - tests/test_phase16_prereg.py:50-70 (`V3_ARTIFACT_GLOBS` — confirms only `results/phase16_*` etc. are guarded, not `scripts/phase16_*.py`)
  </read_first>
  <action>
1. Re-measure guard status BEFORE editing (D-32's rule), and quote each command in the SUMMARY: `grep -rl "phase16_persistence.py" results/*.json` (expect nothing — no `module_sha256` names it); `grep -rn "phase16_persistence" tests/test_phase16_prereg.py` (expect nothing); `grep -rl "evaluation/perplexity.py" results/*.json tests/test_phase16_prereg.py tests/test_phase20_prereg.py` (expect nothing). If either file IS named by a record digest or an ancestry test, STOP: do not edit it; report `FORBIDDEN-BY-GUARD` for that item to the orchestrator so 28-03 records it instead.
1b. RED first: write the two regression tests of steps 3 and 5 BEFORE touching either docstring, run `.venv/bin/pytest tests/test_perplexity.py::test_docstring_states_the_true_denominator "tests/test_phase16_driver.py::test_overwrite_statement_docstring_does_not_type_the_allowlist_size" -q` against the CURRENT docstrings — both must FAIL (`corpus_len - n_windows` present; `exactly two entries` present); paste the two assertion lines into the SUMMARY as the natural RED. Only then do steps 2 and 4.
2. perplexity.py: rewrite the docstring bullet at :11-13 so it states the denominator is `corpus_len - 1` for a cleanly tiling corpus (token 0 is the only unscored token), matching tests/test_perplexity.py:121-122. Remove the phrase `corpus_len - n_windows` entirely. Change no code.
3. tests/test_perplexity.py: add `test_docstring_states_the_true_denominator` asserting `"corpus_len - 1" in perplexity.__doc__` and `"corpus_len - n_windows" not in perplexity.__doc__` (import the module object the file already imports from; if it imports names only, add `from personacore.evaluation import perplexity`).
4. scripts/phase16_persistence.py:1605: replace the typed parenthetical "stays at exactly two entries" with wording that names the invariant without a numeral — e.g. "keeps the entry count the widened D-21 guard in `tests/test_phase14_scoring.py` asserts". Change no code.
5. tests/test_phase16_driver.py: add `test_overwrite_statement_docstring_does_not_type_the_allowlist_size` that imports `build_overwrite_statement` (the file already imports from `phase16_persistence` — reuse its bootstrap), reads `build_overwrite_statement.__doc__`, asserts `"exactly two entries" not in doc`, asserts `"PERSONA_ALLOWLIST" in doc`, and asserts `len(PERSONA_ALLOWLIST)` (imported from `tests.test_phase14_scoring` or via `importlib` on the file path — pick whichever the existing tests use for cross-test imports; if none, load with `importlib.util.spec_from_file_location`) is an `int >= 1` — the count is read from the allowlist, never typed in prose or in the test.
6. Run `.venv/bin/pytest tests/test_perplexity.py tests/test_phase16_driver.py tests/test_phase16_prereg.py tests/test_phase18_prereg.py -q` (the phase16/18 prereg files are the ancestry guards that would redden if the module were frozen — green here is the measured evidence for `FIXED`) and `make lint`; quote both.
Threat references: T-28-05 (a `FIXED` disposition without a test is forbidden — both tests exist before the ledger row).
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/pytest tests/test_perplexity.py tests/test_phase16_driver.py tests/test_phase16_prereg.py -q -x && grep -q "corpus_len - 1" src/personacore/evaluation/perplexity.py && test "$(grep -c 'corpus_len - n_windows' src/personacore/evaluation/perplexity.py)" = "0" && test "$(grep -c 'exactly two entries' scripts/phase16_persistence.py)" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "corpus_len - n_windows" src/personacore/evaluation/perplexity.py` is 0 and `grep -c "corpus_len - 1"` is >= 1
    - `grep -c "exactly two entries" scripts/phase16_persistence.py` is 0
    - `tests/test_perplexity.py` contains `def test_docstring_states_the_true_denominator`; `tests/test_phase16_driver.py` contains `def test_overwrite_statement_docstring_does_not_type_the_allowlist_size` and the token `PERSONA_ALLOWLIST`
    - `git diff --stat -- src/personacore/evaluation/perplexity.py scripts/phase16_persistence.py` shows docstring-only hunks (executor quotes `git diff` and states no executable line changed)
    - `.venv/bin/pytest tests/test_perplexity.py tests/test_phase16_driver.py tests/test_phase16_prereg.py tests/test_phase18_prereg.py -q` all passed; `make lint` clean
    - SUMMARY quotes the three guard-status grep commands and their (empty) output
    - SUMMARY carries the natural-RED transcript of both new tests run before the docstring edits (2 failed), then the GREEN run after
  </acceptance_criteria>
  <done>Both D-32 docstrings are true, each guarded by a named test; guard status measured and quoted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git history → test | `git show <tag>:pyproject.toml` is evidence only if the clone has full history and tags |
| docstring → ledger | A `FIXED` row is only as true as the test that would catch the regression |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-04 | Repudiation | tests/test_package.py D-25 test | mitigate | `rev-parse --is-shallow-repository == "false"` and `git tag -l ⊇ {v1.0,v2.0,v3.0}` asserted before any comparison |
| T-28-05 | Repudiation | D-32 `FIXED` rows (written in 28-03) | mitigate | both docstring tests exist and are green in this plan before the ledger cites them |
| T-28-09 | Tampering | editing an ancestry-frozen module | mitigate | guard status re-measured by grep over `results/*.json` and the prereg tests before editing; STOP rule if named |
| T-28-SC | Tampering | package installs | mitigate | none installed; D-25 test makes any dependency drift RED across four tags |
</threat_model>

<verification>
- `.venv/bin/pytest tests/test_package.py tests/test_perplexity.py tests/test_phase16_driver.py tests/test_phase16_prereg.py -q` green; `make lint` clean
- `pyproject.toml` byte-identical (sha `15ffd6b5…926f`)
</verification>

<success_criteria>
- SC3's dependency clause is a tomllib test; the sha pin's name is true; two `FIXED` items each have a test node id 28-03 can cite
</success_criteria>

<output>
Create `.planning/phases/28-report-the-published-null-and-milestone-close/28-01-SUMMARY.md` when done — quote every guard-status command, the measured dependency list, and the test node ids created.
</output>
