---
phase: 28-report-the-published-null-and-milestone-close
plan: 05
type: execute
wave: 4
depends_on: ["28-04"]
files_modified:
  - tests/test_phase28_report.py
  - tests/test_phase28_prereg.py
autonomous: true
requirements: [RPT-01]
must_haves:
  truths:
    - "A test scans the TEMPLATE SOURCE files (never the rendered output by value) and is RED on any digit outside the D-19 grammar; its planted-RED probe writes a one-numeral template to `tmp_path` and asserts the scanner refuses it (D-18, D-19)"
    - "A test resolves EVERY `(field_path, why)` pair of `phase25_prereg.PUBLICATION_OBLIGATION` and `phase26_prereg.PUBLICATION_OBLIGATION_CONTINUATION` against the assembled records — count by `len()`, wildcard `points.<key>.*` expanded over `canary.audited_point_keys`, `<artifact absent>` satisfied by the canary file's existence — and an unresolvable path is RED (D-23)"
    - "A test asserts every ε / σ / C / q / δ / K the renderer binds equals the module constant it claims: `sigma_for(4.0, steps, δ) == 15.289937507119`-by-recompute, `epsilon_for(16.0, …) == record ε`, `CAPACITY_BRANCHES ∋ capacity_branch`, `CURVE_K == verdicts.curve_k`, `FULL_FIDELITY_K == verdicts.full_fidelity_k == AST-read phase18_extraction.K`, `DELTA == epsilon_report.delta == points.*.delta`, `STEP_BUDGET == composed_steps`, `SAMPLING_RATE_Q == points.*.q`, `CLIP_NORM == points.*.clip_norm`, `SIGMA_LADDER == sorted dp_n8 sigmas`, `F_Y == admission.budget.f_y`, `MARGIN_K == admission.budget.margin_k` (SC2, D-06)"
    - "A test recomputes every digest in the rendered provenance table from `read_bytes()` (records) and from the canonical rows dump (ledger) and collects all drift before asserting (D-21)"
    - "A test proves the confound is the records' own words: the rendered block contains, under `_prose.normalized`, `adversarial_no_replay.{finding,log_line,log_source,dp_replay_source}`, `amended_criterion`, the §12.5c anchored slice of `results/phase25_operational_note.md`, and the `adv_n64` `early_return_reason`; the renderer source never names `logs/` (SC4, D-08, D-09, D-10)"
    - "A test proves the lead: the first prose line after the `##` heading carries `capacity_branch` and both `arm_existentials`; the second carries the noised-DP (a)/(b) counts re-derived in the test from `admission.rows` + frontier `sigma`; the n=64 caveat with `control_readings.dp_n64.recall_counts.taught` precedes the first `###` (D-02, D-03)"
    - "A test proves every `QUOTES` entry is present under `normalized` in its named source — `git show c673b4c:.planning/research/SUMMARY.md` for the expectation quotes, HEAD files otherwise — and `c673b4c` is a strict ancestor of the earliest first-add of every tracked `results/phase2[0-8]_*` file, derived from git at test time, refusing on a shallow clone (D-04, D-05)"
    - "A test proves the renderer imports in a fresh interpreter without torch and contains no clock/HEAD call by AST (D-24)"
    - "Every prose comparison in both files is a `_prose.normalized(...) in _prose.normalized(...)` call; byte checks (28-06) use `==` (D-22)"
  artifacts:
    - path: "tests/test_phase28_report.py"
      provides: "template scan (+planted RED), obligation resolution, constants, provenance, confound, lead/caveat, torch-free + no-clock probes"
      contains: "PUBLICATION_OBLIGATION_CONTINUATION"
    - path: "tests/test_phase28_prereg.py"
      provides: "D-04 quotes at c673b4c; D-05 ancestry with shallow-clone refusal"
      contains: "merge-base"
  key_links:
    - from: "tests/test_phase28_report.py"
      to: "scripts/phase28_report.md.tmpl"
      via: "regex scan of the template source after stripping ${...} and the exempt grammar"
      pattern: "md.tmpl"
    - from: "tests/test_phase28_prereg.py"
      to: "git object c673b4c"
      via: "git show c673b4c:.planning/research/SUMMARY.md; git merge-base --is-ancestor"
      pattern: "c673b4c"
    - from: "tests/test_phase28_report.py"
      to: "scripts/phase25_prereg.py::PUBLICATION_OBLIGATION"
      via: "for path, why in PUBLICATION_OBLIGATION + CONTINUATION: resolve(record, path)"
      pattern: "PUBLICATION_OBLIGATION"
---

<objective>
Make SC2's guarantees tests: the template source cannot carry a typed number, every obligation path
resolves, every constant the report quotes equals its module pin, every digest recomputes, the
confound and the lead are the records' words, and the expectation is provably older than every
v4.0 result. The byte-identity tests (D-17) are added in 28-06 when the blocks exist to compare.

Decisions implemented: D-02, D-03, D-04, D-05, D-06, D-08, D-09, D-10, D-18, D-19, D-21, D-22, D-23,
D-24.

Purpose: RED before GREEN — each guard is watched refusing (naturally or planted) before it is
trusted.
Output: two new test files, one commit, quick-suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/28-report-the-published-null-and-milestone-close/28-CONTEXT.md
@.planning/phases/28-report-the-published-null-and-milestone-close/28-PATTERNS.md
@.planning/phases/28-report-the-published-null-and-milestone-close/28-04-SUMMARY.md

<interfaces>
From scripts/phase28_report.py (28-04): PUBLISHED, REPORT_STEM, GLANCE_STEM, TEMPLATE_REPORT, TEMPLATE_GLANCE, QUOTES {name: (source, text)}, SLICES, EXPECTATION_COMMIT = "c673b4c", resolve(node, path), load() -> records, ledger_rows_digest(ledger), Bindings(records), DERIVED, TABLES, render_report(), render_glance()
QUOTES source grammar: "git:<rev>:<path>" → compare against `git show <rev>:<path>`; otherwise a HEAD-relative path
From scripts/phase25_prereg.py:424-491: PUBLICATION_OBLIGATION (7 tuples on disk — len() it); scripts/phase26_prereg.py:354-395: PUBLICATION_OBLIGATION_CONTINUATION (7; includes `points.<key>.verdict.verdict`, `points.<key>.verdict.reasons`, `<artifact absent>`); phase26_prereg.VERDICTS
Module pins: mitigation_gate.{CAPACITY_BRANCHES, F_Y, F_C, MARGIN_K}; mitigation_budget.{CURVE_K, FULL_FIDELITY_K, SIGMA_LADDER, STEP_BUDGET, CLIP_NORM}; mitigation_unit.{DELTA, SAMPLING_RATE_Q}; accountant.{epsilon_for, sigma_for}; scripts/phase18_extraction.py `K = 48` (:93) — AST read, never import
Analogs: tests/test_phase27_prereg.py:57-115 (`_git`, `_assert_frozen_before` — copy), :133-140 (fresh-interpreter torch-free probe); tests/test_phase25_correction.py:104-141 (`_markers`, `_span`, `_called_names`); tests/test_phase24_record.py:318-331 (collect-then-assert digests); tests/test_phase18_docs.py:269-281 (`_anchored_section`)
Exempt grammar for the scan (D-19, mirrors 28-04's verify): `Phase \d+`, `\b[A-Z][A-Z0-9]*(?:-[A-Z0-9]+)+\b`, ISO date, `\b[0-9a-f]{7,40}\b`, `§\d+(?:\.\d+)?[a-z]?`, `\b\d{2}-\d{2}\b`, `\bv\d+\.\d+\b`, `sha256`, `\b(?:19|20)\d{2}\b`; placeholders `\$\{[^}]*\}` stripped first
Census: no `== 10`; no `train_arm(`; no `os.replace`; keep the test's own docstrings OUT of any scanned file (the scan reads the .tmpl files only); clean-tree probes need these files committed before any full-suite run
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: tests/test_phase28_report.py — scan, obligations, constants, provenance, confound, lead, probes</name>
  <files>tests/test_phase28_report.py</files>
  <read_first>
    - scripts/phase28_report.py, scripts/phase28_report.md.tmpl, scripts/phase28_glance.md.tmpl (28-04 output — binding names, TABLES["sources"] column layout, the `##` title line)
    - tests/test_phase25_correction.py:95-160 (`_markers`, `_span`, `_text`), :130-141 (`_called_names`)
    - tests/test_phase24_record.py:288-334
    - tests/test_phase27_prereg.py:10-31 (bootstrap), :133-140 (torch-free probe)
    - tests/test_phase18_docs.py:269-281 (`_anchored_section`)
    - scripts/phase25_prereg.py:410-495; scripts/phase26_prereg.py:350-396
    - .planning/phases/28-report-the-published-null-and-milestone-close/28-RESEARCH.md Pitfalls 1, 2, 5
  </read_first>
  <action>
Create `tests/test_phase28_report.py` with the PATTERNS bootstrap (`_ROOT`, `scripts/` and `src/` on `sys.path`, `import _prose`, `import phase28_report`). Module-level: `_EXEMPT = (...)` the nine regexes from `<interfaces>`, `_PLACEHOLDER = r"\$\{[^}]*\}"`, and `def _bare_numerals(text) -> list[tuple[int, str]]` that strips placeholders then exempt tokens and returns `(lineno, line)` for every remaining line containing `\d`. Tests (names binding):

- `test_template_scan_report_has_no_bare_numeral` / `test_template_scan_glance_has_no_bare_numeral` — `_bare_numerals(TEMPLATE.read_text()) == []`, message lists the hits.
- `test_template_scan_planted_numeral_is_red` — write `tmp_path / "planted.md.tmpl"` containing one line with a bare `32` beside a placeholder; assert `_bare_numerals(...)` returns exactly one hit naming that line. Also `test_template_scan_exempt_tokens_are_not_hits` — a planted line containing only `Phase 28`, `RELRN-02`, `D-25-18-ADV64-REFUSED`, `2026-09-05`, `c673b4c`, `§12.5c`, `23-12`, `v3.0`, `sha256` returns `[]`.
- `test_every_obligation_path_resolves` — `pairs = list(phase25_prereg.PUBLICATION_OBLIGATION) + list(phase26_prereg.PUBLICATION_OBLIGATION_CONTINUATION)`; `assert len(pairs) == len(phase25_prereg.PUBLICATION_OBLIGATION) + len(phase26_prereg.PUBLICATION_OBLIGATION_CONTINUATION)` (no literal); for each `(path, why)`: if `path == "<artifact absent>"` assert the canary file `is_file()`; elif `"<key>" in path` expand over `canary["audited_point_keys"]` and resolve against the canary (asserting `verdict.verdict ∈ phase26_prereg.VERDICTS` for that row); else resolve against the frontier first, the canary second (the continuation's paths are canary paths) — a `KeyError` from both is collected; assert no misses, message quoting `path` and `why`. Also assert the rendered report contains, under `normalized`, `_fmt`-ed value of every scalar obligation path (strings and numbers) — the obligation is to PUBLISH them.
- `test_constants_match_modules_and_record` — the equalities listed in must_haves; `phase18_extraction.K` read via `ast.parse((_ROOT/"scripts/phase18_extraction.py").read_text())` walking module-level `Assign` targets named `K` and `ast.literal_eval`; `SIGMA_LADDER` compared to `tuple(sorted(points[k]["sigma"] for k in frontier["point_keys"] if k.startswith("dp_n8_")))`; `sigma_for`/`epsilon_for` recomputed from `p = points["dp_n8_sigma16p000000"]` and compared to the rendered `derived.sigma_for_eps4` binding and to `p["epsilon"]`. Collect mismatches, assert once.
- `test_provenance_digests_recompute_from_bytes` — parse `TABLES["sources"](records)` rows (or the rendered `### Provenance` section via `_anchored_section`); for each JSON record row recompute `hashlib.sha256(path.read_bytes()).hexdigest()` and `stat().st_size`; for the ledger row recompute `phase28_report.ledger_rows_digest` independently as `sha256(json.dumps(ledger["rows"], sort_keys=True, ensure_ascii=False).encode())`; collect drift, assert once.
- `test_confound_is_quoted_from_the_records` — `block = render_report()`; for each of `adversarial_no_replay.finding/log_line/log_source/dp_replay_source`, `amended_criterion`, `leg_refusals.adv_n64`, and `points[<first refused key>].verdict.early_return_reason`: `assert _prose.normalized(value) in _prose.normalized(block)`; slice `### 12.5c` from `results/phase25_operational_note.md` with `_anchored_section(text, "### 12.5c", stop=r"### ")` and assert its normalized first paragraph is in the normalized block; assert `"logs/"` not in `phase28_report` source (read the .py) and `"phase25_sweep.out"` appears in the block ONLY as part of the `log_source`/`dp_replay_source` strings (count occurrences equals the count inside those two fields).
- `test_lead_is_the_gates_own_output` — split the block into lines; find the `## ` heading; the first non-empty, non-italic line after it contains normalized `capacity_branch`, `arm_existentials.dp`, `arm_existentials.adversarial`; the next non-empty line contains the (a)/(b) counts re-derived in the test: `a = sum(r["cleared_a"] for r in rows if r["arm"]=="dp" and frontier["points"][r["point_key"]]["sigma"] > 0)` etc., asserting `f"{a} of {total}"` and `str(b)` appear — the test computes, never types; `test_the_caveat_precedes_the_first_subsection` — the index of `"87/1008"`-style text built from `control_readings.dp_n64.recall_counts.taught` (`f"{k}/{n}"`) is less than the index of the first `\n### `.
- `test_renderer_imports_without_torch_in_a_fresh_interpreter` — copy tests/test_phase27_prereg.py:133-140 for `phase28_report`; `test_renderer_has_no_clock_and_no_head_sha` — AST walk of the renderer: no `Attribute` named `today`/`now`/`utcnow`, no string constant containing `rev-parse`, `logs/`, or `mitigation_point_verdict`.
- `test_every_quote_binding_is_used_or_removed` — every key of `QUOTES` appears as `${quote.<key>}` in one of the two templates (no dead quotes carrying unverified numerals).
Every prose comparison uses `_prose.normalized`; no `== 10`, `train_arm(`, `os.replace` in the file. Run `.venv/bin/pytest tests/test_phase28_report.py -q -v`; all must pass on the committed 28-04 tree — quote the count. Natural RED record: temporarily insert a bare `32` into the report template (working tree only), run the scan test, paste the RED assertion text into the SUMMARY, `git checkout scripts/phase28_report.md.tmpl`.
Threat references: T-28-03 (scan + planted RED), T-28-01 (digests from bytes), T-28-06, T-28-13, T-28-14 (lead counts re-derived).
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/pytest tests/test_phase28_report.py -q -x && test "$(grep -c 'def test_' tests/test_phase28_report.py)" -ge 13 && grep -q "PUBLICATION_OBLIGATION_CONTINUATION" tests/test_phase28_report.py && grep -q "_prose.normalized" tests/test_phase28_report.py && ! grep -q "os.replace\|train_arm(" tests/test_phase28_report.py && ! grep -qE "(==|!=)\s*10([^0-9_]|$)" tests/test_phase28_report.py && test -z "$(git status --short scripts/)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_" tests/test_phase28_report.py` >= 13 and the file defines every test name listed in the action
    - `.venv/bin/pytest tests/test_phase28_report.py -q` all passed on the committed 28-04 tree; count quoted
    - SUMMARY carries the natural-RED transcript of `test_template_scan_report_has_no_bare_numeral` with a planted `32` in the working tree, and `git status --short scripts/` is empty afterwards
    - The file contains no `== 10`, `train_arm(`, or `os.replace`; every `in` comparison over prose is wrapped in `_prose.normalized`
    - `test_every_obligation_path_resolves` uses `len(...)` on both tuples and no literal 7/8/14/15
  </acceptance_criteria>
  <done>The template scan, obligation resolution, constants, digests, confound and lead are all watched RED-then-GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: tests/test_phase28_prereg.py — D-04 quotes at c673b4c, D-05 ancestry with shallow refusal; commit both files</name>
  <files>tests/test_phase28_prereg.py</files>
  <read_first>
    - tests/test_phase27_prereg.py:57-115 (`_git`, `_assert_frozen_before`, the caller shape — copy the mechanism)
    - scripts/phase28_report.py (`QUOTES`, `EXPECTATION_COMMIT`)
    - .planning/phases/28-report-the-published-null-and-milestone-close/28-RESEARCH.md Pattern 4 (measured: earliest v4.0 add `9bb34ad` of `results/phase20_retention_floor.json`; `c673b4c` 2026-08-20 09:27; SUMMARY.md unchanged since)
    - .github/workflows/ci.yml:28 (`fetch-depth: 0`)
  </read_first>
  <action>
Create `tests/test_phase28_prereg.py` (same bootstrap; `import _prose`, `import phase28_report`). Copy `_git` and the shallow-clone refusal string verbatim from tests/test_phase27_prereg.py.

- `test_every_quote_is_verbatim_in_its_source` — for `name, (source, text) in phase28_report.QUOTES.items()`: if `source.startswith("git:")` → `rev, path = source[4:].split(":", 1)`, `haystack = _git("show", f"{rev}:{path}")`; else `haystack = (_ROOT / source).read_text(encoding="utf-8")`; `assert _prose.normalized(text) in _prose.normalized(haystack)`, collecting misses. Assert additionally that at least two quotes name `git:c673b4c:.planning/research/SUMMARY.md` (the expectation must be quoted from the pinned commit, not from HEAD) — express as `assert sum(src.startswith("git:c673b4c:") for src, _ in QUOTES.values()) >= 2`.
- `test_expectation_restated_at_milestone_level` — the two expectation quotes' key phrases (take the substrings `72σ` and `σ ≥ 15.3` from the quote text by slicing the quote, not by typing them in the test: e.g. `[fragment for fragment in text.split() if "σ" in fragment]`) appear under `normalized` in `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md` at HEAD (D-04's restatements).
- `test_expectation_commit_precedes_every_v4_result` (D-05 ii + iii): `assert _git("rev-parse", "--is-shallow-repository") == "false"` (refusal string); `full = _git("rev-parse", phase28_report.EXPECTATION_COMMIT)`; `tracked = _git("ls-files", "results/phase2[0-8]_*").split()`; `assert tracked` (green-and-blind guard); for each artifact `adds = _git("log", "--diff-filter=A", "--format=%H", "--", artifact).split()`, `first_add = adds[-1]`, assert `first_add != full`, `subprocess.run(("git","merge-base","--is-ancestor", full, first_add), cwd=_ROOT, check=True)`, `checked += 1`; finally `assert checked == len(tracked)`. Also assert the quoted file is byte-identical between `c673b4c` and HEAD is NOT required (HEAD may drift) — the quote check is at the pinned commit only; say so in the docstring.
- `test_ancestry_guard_refuses_a_shallow_clone_message_is_the_phase27_one` — assert the refusal string in this file equals the one in tests/test_phase27_prereg.py (read that file, `normalized` compare) so the message stays one copy.
- `test_a_planted_later_commit_is_red` — planted RED for conjunct (ii): call the same helper with `prereg = _git("rev-parse", "HEAD")` in place of `c673b4c` against a fake tracked list `[tracked[0]]`; HEAD is not a strict ancestor of an old first-add → `subprocess.CalledProcessError` expected (`pytest.raises`). Factor the loop into `_assert_recorded_before(commit, tracked)` so both tests share it.
Run `.venv/bin/pytest tests/test_phase28_prereg.py tests/test_phase28_report.py -q -v`; quote the counts. `make lint`. Commit both test files: `test(28-05): Phase 28 guards — template scan, obligation resolution, constants, digests, confound, lead, expectation ancestry`. Then run the full suite uncapped in the background on the committed tree and report the tail line (expected all green; clean-tree probes need the commit first).
Threat references: T-28-04 (shallow refusal + derived first-adds), T-28-03.
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/pytest tests/test_phase28_prereg.py tests/test_phase28_report.py -q -x && grep -q "is-shallow-repository" tests/test_phase28_prereg.py && grep -q "merge-base" tests/test_phase28_prereg.py && grep -q "diff-filter=A" tests/test_phase28_prereg.py && git ls-files tests/test_phase28_prereg.py tests/test_phase28_report.py | test "$(wc -l | tr -d ' ')" = "2"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_phase28_prereg.py` defines the five tests named in the action; contains `is-shallow-repository`, `merge-base`, `--is-ancestor`, `diff-filter=A`, `ls-files`, `git:c673b4c:` and no hardcoded first-add SHA (`grep -c 9bb34ad` == 0)
    - Both test files pass; counts quoted; both tracked in one commit (SHA quoted)
    - `make lint` clean; full-suite tail line quoted from the background log (`0 failed`, or every failing node id with cause)
  </acceptance_criteria>
  <done>"Recorded before any run" is a CPU test; every quote is checked against its pinned source.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| template source → scan | the test reads the .tmpl files, not Python files whose docstrings discuss numerals |
| git history → ancestry test | full clone required; refusal on shallow; first-adds derived, never hardcoded |
| record → rendered prose | confound and lead compared under `normalized` to the record's own strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-03 | Tampering | hand-typed numeral in template | mitigate | `_bare_numerals` scan of both .tmpl files + planted RED in `tmp_path` |
| T-28-04 | Repudiation | shallow clone / hardcoded ancestry pair | mitigate | `--is-shallow-repository == false`; first-adds from `git log --diff-filter=A`; planted HEAD-as-prereg RED |
| T-28-01 | Tampering | digest drift | mitigate | recompute from `read_bytes()` and canonical rows dump; collect-then-assert |
| T-28-06 | Information disclosure | reading `logs/` | mitigate | source-string and AST checks on the renderer |
| T-28-14 | Tampering | mis-derived (a)/(b) counts | mitigate | test re-derives from `rows` + `sigma` independently of the renderer |
| T-28-15 | Repudiation | scan matching the test's own prose | mitigate | scan reads .tmpl files only (memory: grep criteria measure prose) |
| T-28-SC | Tampering | package installs | accept | none |
</threat_model>

<verification>
- `pytest tests/test_phase28_report.py tests/test_phase28_prereg.py -q` green on the committed tree; natural and planted REDs recorded in the SUMMARY; full suite green in the background run
</verification>

<success_criteria>
- SC2 clauses one and three are tests (constants asserted; `normalized` on every prose check); SC1's "recorded before any run" is a git-derived test; SC4's confound is provably quoted
</success_criteria>

<output>
Create `.planning/phases/28-report-the-published-null-and-milestone-close/28-05-SUMMARY.md` when done — include the RED transcripts, test counts, commit SHA, and the full-suite tail line.
</output>
