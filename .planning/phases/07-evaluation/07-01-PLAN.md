---
phase: 07-evaluation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_perplexity.py
  - tests/test_ablation_config.py
  - src/personacore/evaluation/__init__.py
  - src/personacore/evaluation/perplexity.py
autonomous: true
requirements: [EVAL-01]
must_haves:
  truths:
    - "A deterministic perplexity() function exists that sweeps a token corpus in non-overlapping block_size windows and returns (ppl, total_tokens)"
    - "perplexity() matches a brute-force per-token CE reference on a tiny fixture"
    - "The reported token count equals corpus_len - n_windows (each window's first token is unpredictable), so the denominator is auditable"
    - "The final partial window is scored; a single dangling trailing token is skipped"
  artifacts:
    - path: "src/personacore/evaluation/perplexity.py"
      provides: "Deterministic full-val perplexity sweep (reduction='sum', exact denominator)"
      exports: ["perplexity"]
      min_lines: 25
    - path: "src/personacore/evaluation/__init__.py"
      provides: "evaluation package barrel re-exporting perplexity"
      contains: "from .perplexity import perplexity"
    - path: "tests/test_perplexity.py"
      provides: "EVAL-01 accounting tests (brute-force oracle, token-count audit, partial window)"
      contains: "def test_matches_bruteforce"
    - path: "tests/test_ablation_config.py"
      provides: "Wave-0 RED ablation-flag tests (defaults, untie, no-pos) — RED until Plan 02"
      contains: "def test_defaults_unchanged"
  key_links:
    - from: "src/personacore/evaluation/perplexity.py"
      to: "personacore.model.GPT.forward"
      via: "logits = model(x); F.cross_entropy(..., reduction='sum')"
      pattern: "cross_entropy.*reduction=.sum"
    - from: "src/personacore/evaluation/perplexity.py"
      to: "data/val.bin memmap idiom"
      via: "np.memmap(..., dtype=np.uint16, mode='r')"
      pattern: "np\\.memmap"
---

<objective>
Deliver the EVAL-01 perplexity slice end-to-end: a deterministic full-corpus perplexity
function whose token accounting is proven correct against a brute-force reference. Also lay down
the Wave-0 RED test scaffold for the EVAL-03 ablation flags (tests/test_ablation_config.py) which
Plan 02 will turn green.

Purpose: The headline quantitative number (EVAL-01) and every ablation-cohort PPL cell (EVAL-03)
flow through this single `perplexity()` function. The accounting details (the per-window `-1`, the
`reduction="sum"` requirement, the exact auditable denominator) are where the bugs hide, so they
are pinned by tests on a tiny CPU fixture before any real checkpoint is swept.

Output: `src/personacore/evaluation/{__init__,perplexity}.py`, `tests/test_perplexity.py`
(green), `tests/test_ablation_config.py` (RED — goes green in Plan 02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-evaluation/07-RESEARCH.md
@.planning/phases/07-evaluation/07-PATTERNS.md
@.planning/phases/07-evaluation/07-VALIDATION.md

<interfaces>
<!-- Contracts the executor needs. Use directly — no codebase exploration required. -->

From src/personacore/model/gpt.py (GPT.forward, lines 188-204):
  forward(idx, targets=None) -> (logits, loss)
  - logits shape: (B, T, vocab_size)
  - when targets is None: returns (logits, None)
  - when targets given: loss = F.cross_entropy(logits.view(B*T,V), targets.view(B*T)) — a MEAN.
    The perplexity sweep MUST IGNORE this mean loss and recompute CE with reduction="sum".

From src/personacore/model/__init__.py:
  from personacore.model import GPT

From src/personacore/config.py (ModelConfig, lines 86-92):
  ModelConfig(vocab_size=8192, eos_id=8184, block_size=256, n_layer=6, n_head=6, n_embd=384, dropout=0.0)

From src/personacore/training/data.py (line 84 — the memmap re-open idiom):
  data = np.memmap(bin_path, dtype=np.uint16, mode="r")   # re-open per call (nanoGPT RSS-leak avoidance)

From src/personacore/generation/__init__.py (tiny-fixture test style, test_generation.py:31-46):
  GPT(ModelConfig(block_size=8, vocab_size=16, n_layer=1, n_head=1, n_embd=8, eos_id=15))

From tests/test_gpt_param_count.py:16-21 (count_parameters dedup-by-data_ptr helper to reuse in test_ablation_config.py):
  seen = {}; for p in model.parameters(): seen[p.data_ptr()] = p.numel(); return sum(seen.values())

From tests/test_gpt_weight_tying.py:18-19 (data_ptr identity assert to reuse):
  model = GPT(ModelConfig()); assert model.lm_head.weight.data_ptr() == model.wte.weight.data_ptr()
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 RED test scaffold (test_perplexity.py + test_ablation_config.py)</name>
  <files>tests/test_perplexity.py, tests/test_ablation_config.py</files>
  <read_first>
    - tests/test_generation.py (tiny-CPU-fixture style — the `_tiny_model()` GPT idiom at lines 31-46; copy it, do NOT load best.pt/val.bin)
    - tests/test_gpt_weight_tying.py:18-19 (the data_ptr() identity assert)
    - tests/test_gpt_param_count.py:16-21 (the count_parameters dedup-by-data_ptr helper)
    - .planning/phases/07-evaluation/07-RESEARCH.md § Pattern 1 + § Common Pitfalls 1-2 (the -1 off-by-one and reduction="sum" accounting)
    - .planning/phases/07-evaluation/07-VALIDATION.md § Per-Task Verification Map (the six named test cases)
  </read_first>
  <behavior>
    test_perplexity.py (3 cases, all CPU, tiny fixtures):
    - test_matches_bruteforce: build a tiny GPT(ModelConfig(block_size=8, vocab_size=16, n_layer=1, n_head=1, n_embd=8, eos_id=15)) and a hand-built ~300-token np.uint16 array written to a tmp .bin. Assert perplexity(model, path, block_size=8, device="cpu")[0] == a brute-force reference: independently slide every non-overlapping window, sum F.cross_entropy(logits, y, reduction="sum") over all predicted tokens, exp(total/total_tokens). atol 1e-4.
    - test_token_count: assert returned total_tokens == corpus_len - n_windows where n_windows = number of windows that contained >=2 tokens (each scored window loses its first token as unpredictable).
    - test_partial_window: with a corpus length that does NOT divide evenly by block_size, assert the final partial window IS scored (contributes len(chunk)-1 transitions) and a length that leaves exactly one dangling token confirms that single trailing token is skipped (numel < 2 → continue).
    test_ablation_config.py (3 cases — RED now, GREEN after Plan 02):
    - test_defaults_unchanged: GPT(ModelConfig()) → lm_head.weight.data_ptr() == wte.weight.data_ptr() AND count_parameters == 13,891,584.
    - test_untie: GPT(ModelConfig(weight_tying=False)) → lm_head.weight.data_ptr() != wte.weight.data_ptr() AND count_parameters == 17,037,312.
    - test_no_pos: GPT(ModelConfig(use_pos_emb=False)) → forward on a (1, T) input runs without error AND count_parameters == 13,793,280.
  </behavior>
  <action>
    Create both test files. In test_perplexity.py import `perplexity` from `personacore.evaluation`
    (the module Task 2 creates) — these go RED until Task 2 lands, GREEN after. Build the fixture
    corpus with a fixed-seed numpy RNG over `range(vocab_size)` so the brute-force oracle and
    perplexity() see identical data; write it to a `tmp_path` .bin via `.astype(np.uint16).tofile()`.
    The brute-force oracle is hand-written inside the test (do NOT call perplexity() to check
    perplexity()) — it independently tiles non-overlapping windows, recomputes CE with
    `reduction="sum"`, and exponentiates the grand total over the exact token count. In
    test_ablation_config.py, reuse the literal `count_parameters` helper from
    tests/test_gpt_param_count.py:16-21 and assert the three verified param counts from RESEARCH
    (13,891,584 / 17,037,312 / 13,793,280). These ablation tests reference
    `ModelConfig(weight_tying=..., use_pos_emb=...)` — fields that do NOT exist yet, so this file is
    intentionally RED until Plan 02 adds the flags. Do NOT put fenced code blocks anywhere in a
    PLAN action; write actual test code in the .py files only. Keep every test CPU-only and
    fixture-based — never load `checkpoints/best.pt` or `data/val.bin`.
  </action>
  <verify>
    <automated>python -m pytest tests/test_perplexity.py tests/test_ablation_config.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_perplexity.py tests/test_ablation_config.py --collect-only -q` lists all 6 tests (3 + 3) without collection errors.
    - test_perplexity.py imports `from personacore.evaluation import perplexity` (will error at run until Task 2; collection still succeeds since the import is module-level only if guarded — if it errors on collection, the import is fine to leave as the RED signal that Task 2 resolves).
    - test_ablation_config.py references `ModelConfig(weight_tying=False)` and `ModelConfig(use_pos_emb=False)` — RED until Plan 02.
    - No test reads `checkpoints/best.pt` or `data/val.bin` (grep -L confirms: `grep -c "best.pt\|val.bin" tests/test_perplexity.py tests/test_ablation_config.py` returns 0 for each).
  </acceptance_criteria>
  <done>Both Wave-0 test files exist; test_perplexity cases pin the accounting oracle; test_ablation_config cases pin the flag semantics (RED until Plan 02).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: perplexity() module — deterministic non-overlapping full-val sweep</name>
  <files>src/personacore/evaluation/__init__.py, src/personacore/evaluation/perplexity.py</files>
  <read_first>
    - src/personacore/model/gpt.py:188-204 (the forward (logits, loss) contract — note line 203 returns a MEAN loss; do NOT accumulate it)
    - src/personacore/training/loop.py:70-102 (estimate_loss — the @torch.no_grad() + model.eval() posture to mirror; but it does 20 RANDOM batches, NOT a corpus sweep — a NEW function is required per D-02)
    - src/personacore/training/data.py:84 (the np.memmap mode="r" re-open idiom)
    - src/personacore/generation/__init__.py (the barrel re-export pattern to mirror for evaluation/__init__.py)
    - .planning/phases/07-evaluation/07-PATTERNS.md § perplexity.py (the verified reference implementation)
    - .planning/phases/07-evaluation/07-RESEARCH.md § Pattern 1 + Pitfalls 1-2 (accounting invariants)
  </read_first>
  <action>
    Create `src/personacore/evaluation/perplexity.py` exporting `perplexity(model, val_bin_path,
    block_size, device, batch_size=32)` decorated with `@torch.no_grad()`. It calls `model.eval()`,
    opens the corpus with `np.memmap(val_bin_path, dtype=np.uint16, mode="r")`, and tiles
    NON-OVERLAPPING windows with `for i in range(0, n - 1, block_size)`. For each window slice
    `data[i : min(i + block_size + 1, n)]` (the `+1` so the shifted target fits), convert via
    `.astype(np.int64)`, move to `device`, skip if `numel() < 2` (a single dangling token has
    nothing to predict), set `x = chunk[:-1].unsqueeze(0)` and `y = chunk[1:].unsqueeze(0)`, call
    `logits, _ = model(x)` (IGNORE the returned mean loss), then recompute
    `F.cross_entropy(logits.view(-1, V), y.view(-1), reduction="sum")` — `reduction="sum"` is
    MANDATORY per D-01/Pitfall 2. Accumulate `total_ce += ce.item()` and
    `total_tokens += y.numel()`. Return `(math.exp(total_ce / total_tokens), total_tokens)` so the
    denominator is auditable per D-03 (every predicted token counts, including inter-document
    eos_id=8184 — do NOT mask it). Create `src/personacore/evaluation/__init__.py` with
    `from .perplexity import perplexity` and `__all__ = ["perplexity"]`, mirroring the
    generation/__init__.py barrel. Do NOT add a strided/sliding-window variant (deferred — D-01
    locks non-overlapping). No fenced code in this action; write the implementation in the .py file.
  </action>
  <verify>
    <automated>python -m pytest tests/test_perplexity.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_perplexity.py -x -q` exits 0 (all 3 cases green).
    - `python -c "from personacore.evaluation import perplexity; print(perplexity)"` prints a function (barrel re-export works).
    - `grep -c 'reduction="sum"' src/personacore/evaluation/perplexity.py` >= 1 (summed CE, not the model's mean loss).
    - `grep -c "np.memmap" src/personacore/evaluation/perplexity.py` >= 1 (re-open idiom, not a bespoke reader).
    - test_token_count green proves denominator == corpus_len - n_windows.
  </acceptance_criteria>
  <done>perplexity() returns the correct (ppl, total_tokens) on the brute-force fixture; the three EVAL-01 accounting tests are green; the evaluation barrel re-exports it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk → process (token memmap) | `val.bin` / fixture `.bin` are the project's OWN generated files; not untrusted input |

This is a local, offline, no-network, no-untrusted-input measurement function. There is no auth,
no session, no external input, no untrusted data. The honest surface is minimal.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | np.memmap read of `val.bin` | accept | Own-generated file; `mode="r"` (read-only, no write-back). No external corpus is ever swept. Low-value local target. |
| T-07-01b | Denial of Service | full-corpus sweep over 12.6M tokens | accept | Run by hand on M3 (manual artifact, not CI); tests use a tiny fixture, not val.bin. No remote trigger exists. |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | Phase installs ZERO new packages (RESEARCH § Package Legitimacy Audit: N/A). No install task exists in this plan. |
</threat_model>

<verification>
- `make test` (full CPU suite) stays green — the new evaluation module adds no regression.
- `pytest tests/test_perplexity.py -x -q` green (the 3 EVAL-01 accounting cases).
- `tests/test_ablation_config.py` is RED here by design (references flags Plan 02 adds) — it must
  COLLECT cleanly but its assertions go green only after Plan 02.
</verification>

<success_criteria>
- `perplexity()` exists, is `@torch.no_grad()`, uses `reduction="sum"`, returns (ppl, total_tokens).
- The brute-force-oracle, token-count, and partial-window tests are green.
- The evaluation barrel re-exports `perplexity`.
- No new third-party dependency added.
</success_criteria>

<output>
Create `.planning/phases/07-evaluation/07-01-SUMMARY.md` when done.
</output>
