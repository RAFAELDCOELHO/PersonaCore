---
phase: 10-ewc-core
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - tests/fixtures/golden_trajectory_v1.json
  - src/personacore/training/loop.py
  - tests/test_loop_penalty_fn.py
autonomous: true
requirements: [EWC-02]

must_haves:
  truths:
    - "A golden trajectory fixture (per-step CSV text + final-param sha256) was captured from the UN-EDITED loop BEFORE any loop.py change and committed"
    - "train() with penalty_fn omitted AND with penalty_fn=None reproduces the golden trajectory bit-for-bit (exact CSV text equality + exact param sha256 equality)"
    - "The EWC penalty joins base_loss via assemble_loss BEFORE the /accum divide, contributing exactly one full penalty per optimizer step regardless of grad_accum_steps"
    - "checkpoint_extra=None threads additively into all three in-loop save_checkpoint call sites; default None produces v1.0-identical checkpoints"
    - "All 190+ existing tests stay green after the loop edit"
  artifacts:
    - path: "tests/fixtures/golden_trajectory_v1.json"
      provides: "Pre-edit golden trajectory (meta recipe + csv_text + final_loss_repr + param_sha256)"
      contains: "captured_at_sha"
    - path: "src/personacore/training/loop.py"
      provides: "penalty_fn=None + checkpoint_extra=None additive kwargs"
      contains: "penalty_fn"
    - path: "tests/test_loop_penalty_fn.py"
      provides: "bit-identity + once-per-accum-step + checkpoint_extra pins"
      min_lines: 100
  key_links:
    - from: "src/personacore/training/loop.py"
      to: "penalty_fn callable"
      via: "per-micro-batch evaluation inside _optimizer_step"
      pattern: "penalty_fn\\(model\\)"
    - from: "src/personacore/training/loop.py"
      to: "src/personacore/training/loss.py"
      via: "assemble_loss(base_loss, penalties) before the /accum divide"
      pattern: "assemble_loss\\(base_loss, penalties\\)"
    - from: "src/personacore/training/loop.py"
      to: "save_checkpoint **extra seam"
      via: "**(checkpoint_extra or {}) splat at all three save sites"
      pattern: "checkpoint_extra or \\{\\}"
---

<objective>
Splice the EWC penalty into the v1.0 training loop additively — `train(..., penalty_fn=None)`
threaded into `_optimizer_step`, penalty joining `base_loss` via `assemble_loss` BEFORE the
`/accum` divide — plus `checkpoint_extra=None` threading into the three in-loop
`save_checkpoint` sites (RESEARCH Open Q1 resolution), with v1.0 behavior proven bit-preserved
against a golden trajectory captured BEFORE the edit.

Purpose: EWC-02's "plugged in via assemble_loss" clause and roadmap success criterion 4 (the
bit-identity guarantee). The golden fixture turns "defaults reproduce v1.0" from a code-review
claim into executed evidence (Pitfall 6).
Output: committed golden fixture, additively-edited loop.py, tests/test_loop_penalty_fn.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/10-ewc-core/10-CONTEXT.md
@.planning/phases/10-ewc-core/10-RESEARCH.md
@.planning/phases/10-ewc-core/10-PATTERNS.md

<interfaces>
<!-- Contracts this plan CONSUMES (created by plan 10-01, wave 1) — use directly. -->

From src/personacore/continual (plan 10-01):
```python
class EWCPenalty:
    def __init__(self, fisher, theta_star, lam, device): ...
    def __call__(self, model) -> "torch.Tensor": ...  # scalar, differentiable
```

From src/personacore/training/loop.py (EXISTING — the splice site, verified lines 134-141):
```python
    for micro in range(accum):
        xb, yb = batch_fn(micro)
        with runtime.autocast():
            _, base_loss = model(xb, yb)
            total = assemble_loss(base_loss, ())  # identity in M1 (D-04)
            loss = total / accum
        scaler.scale(loss).backward()
        summed += float(base_loss.item())
```

The three save_checkpoint call sites to thread checkpoint_extra into (verified):
- lines ~342-353: best.pt save (Seam 3, gated on new lowest val_loss)
- lines ~362-373: in-loop latest.pt save (Seam 4a, every checkpoint_interval steps)
- lines ~386-397: end-of-call latest.pt save
All three share the identical kwarg shape ending `val_loss=...`.

MUST NOT change (spy-pinned by tests/test_train_loop.py::test_amp_ordering_unscale_clip_step_update):
the `scale -> backward x accum -> unscale_ -> clip -> step -> update -> scheduler.step` ordering,
and `summed += float(base_loss.item())` stays base-loss-only (logged train_loss keeps its v1.0 meaning).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture the pre-edit golden trajectory fixture (MUST run before any loop.py change)</name>
  <files>tests/fixtures/golden_trajectory_v1.json</files>
  <read_first>
    - src/personacore/training/loop.py (current, UN-edited state — confirm `git diff --quiet src/personacore/training/loop.py` passes before capture)
    - tests/test_resume_curve.py (the model-build + train-call recipe to replicate: its _build() helper, CORPUS_PATH, EOS_ID, seed_everything(1234) usage, and exact import paths)
    - src/personacore/config.py (TrainConfig/RuntimeConfig field names for the recipe)
  </read_first>
  <action>
    Capture a golden trajectory from the CURRENT, unmodified loop (Pitfall 6: this MUST precede
    Task 2's edit — verify `git diff --quiet src/personacore/training/loop.py` exits 0 first).
    Write a transient capture script OUTSIDE the repo (e.g. /tmp/gsd_capture_golden_10.py via
    the Write tool — do not commit it; the JSON's meta block is the durable record) that:
    (1) replicates tests/test_resume_curve.py's recipe exactly: seed_everything(1234), the same
    _build() model construction (Bigram model), corpus_path=tests/fixtures/bigram_corpus.txt,
    eos_id=8184, but with TrainConfig(lr=1e-2, warmup_steps=2, max_steps=5, batch_size=4),
    runtime_config=RuntimeConfig(device="cpu") passed EXPLICITLY (the replay test is CPU-only —
    never let device resolution pick MPS), log_path=a temp CSV, eval_interval=1,
    return_final_loss=True;
    (2) after train() returns, reads the CSV file's full text and computes
    hashlib.sha256 of the model's single parameter tensor bytes
    (weight.detach().cpu().contiguous().numpy().tobytes());
    (3) writes tests/fixtures/golden_trajectory_v1.json with this exact structure:
    {"meta": {"seed": 1234, "train_config": {"lr": 0.01, "warmup_steps": 2, "max_steps": 5,
    "batch_size": 4}, "model": "BigramLanguageModel via tests/test_resume_curve._build recipe",
    "corpus": "tests/fixtures/bigram_corpus.txt", "eos_id": 8184, "eval_interval": 1,
    "device": "cpu", "captured_at_sha": "<git rev-parse HEAD>", "loop_git_clean": true},
    "csv_text": "<full CSV content as one string>", "final_loss_repr": "<repr(float(final))>",
    "param_sha256": "<hex digest>"}.
    Run it with .venv/bin/python from the repo root. The JSON is committed; it is the v1.0
    behavioral contract that Task 2's test replays against.
  </action>
  <verify>
    <automated>source .venv/bin/activate && python -c "import json; d=json.load(open('tests/fixtures/golden_trajectory_v1.json')); assert set(d)=={'meta','csv_text','final_loss_repr','param_sha256'}; m=d['meta']; assert m['seed']==1234 and m['device']=='cpu' and m['train_config']['max_steps']==5; assert len(d['param_sha256'])==64 and d['csv_text'].count(chr(10))>=5; print('golden fixture OK')"</automated>
  </verify>
  <done>tests/fixtures/golden_trajectory_v1.json exists with the four top-level keys, a 64-hex param_sha256, >=5 CSV rows, and meta.captured_at_sha matching `git rev-parse HEAD` at capture time; loop.py was git-clean when captured.</done>
  <acceptance_criteria>
    - `git diff --quiet src/personacore/training/loop.py` exited 0 immediately before the capture run (loop un-edited)
    - tests/fixtures/golden_trajectory_v1.json top-level keys are exactly {"meta", "csv_text", "final_loss_repr", "param_sha256"}
    - meta contains keys: seed, train_config (with lr/warmup_steps/max_steps/batch_size), model, corpus, eos_id, eval_interval, device ("cpu"), captured_at_sha, loop_git_clean
    - param_sha256 is a 64-char hex string; csv_text contains a header row plus 5 step rows
    - meta.captured_at_sha equals the output of `git rev-parse HEAD` at capture time
    - No capture script was committed (git status shows only the JSON as new under tests/)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Additive loop.py edit (penalty_fn + checkpoint_extra) pinned by trajectory/accum/extra tests</name>
  <files>src/personacore/training/loop.py, tests/test_loop_penalty_fn.py</files>
  <read_first>
    - src/personacore/training/loop.py (full file — the splice at lines 134-141, _optimizer_step signature line 122, train() signature lines 150-174, docstring Args style at line ~195, the three save_checkpoint sites at ~342-353/~362-373/~386-397, the _optimizer_step call site at ~309-311)
    - tests/fixtures/golden_trajectory_v1.json (the contract Task 1 captured — replay its meta recipe exactly)
    - tests/test_resume_curve.py (trajectory-equality template + _build recipe)
    - tests/test_train_loop.py (test_grad_accum_equivalent_to_big_batch — the accum-equivalence idiom to reuse; test_amp_ordering_unscale_clip_step_update — the ordering pin that must stay green)
    - src/personacore/continual/ewc.py (EWCPenalty from plan 10-01 — used directly in the accum test)
    - tests/test_checkpoint.py (test_open_dict_extensible — the **extra round-trip idiom)
  </read_first>
  <behavior>
    Write tests/test_loop_penalty_fn.py FIRST (fails: penalty_fn kwarg doesn't exist yet), then
    edit loop.py. All tests CPU-only with explicit RuntimeConfig(device="cpu"):
    - Test 1 (golden bit-identity, roadmap criterion 4): replay the EXACT meta recipe from
      tests/fixtures/golden_trajectory_v1.json with penalty_fn OMITTED; assert the produced CSV
      text == golden csv_text (string equality), repr(float(final)) == final_loss_repr, and the
      param sha256 == param_sha256 (bitwise identity).
    - Test 2: same replay with penalty_fn=None explicitly -> identical results to Test 1
      (exact equality on final loss and param hash).
    - Test 3 (zero-penalty inertness): penalty_fn=lambda m: torch.tensor(0.0) -> exact equality
      with the penalty_fn=None run (x + 0.0 is bitwise identity for positive fp32 losses).
    - Test 4 (once-per-accum-step, Pitfall 5): mirror test_grad_accum_equivalent_to_big_batch's
      synthetic two-config comparison (accum=4/batch_size=4 vs accum=1/batch_size=16, max_steps=1,
      warmup_steps=0) but pass a real EWCPenalty built with a displaced theta_star (e.g.
      theta_star = current params minus a small constant, fisher = ones_like); assert final
      losses match within 1e-3 AND post-step parameters match within atol=1e-6 between the two
      configs — proving exactly one full penalty per optimizer step, before /accum.
    - Test 5 (penalty call count): a counting wrapper around a penalty fn passed to a 2-step run
      with grad_accum_steps=3 is called exactly 6 times (once per micro-batch).
    - Test 6 (checkpoint_extra threading): a short run with checkpoint_path + checkpoint_interval=1
      + checkpoint_extra={"fisher": {"x": torch.ones(2)}, "theta_star": {"x": torch.zeros(2)}}
      produces a latest.pt whose loaded dict contains both keys with torch.equal values (covers
      the in-loop AND end-of-call sites); a default run (checkpoint_extra omitted) produces a
      checkpoint WITHOUT a "fisher" key.
  </behavior>
  <action>
    Edit src/personacore/training/loop.py — ADDITIVE ONLY, no restructure (the bit-identity
    criterion forbids it):
    (1) `_optimizer_step(...)` gains trailing parameter `penalty_fn=None`; inside the micro-batch
    autocast block replace `total = assemble_loss(base_loss, ())` with
    `penalties = (penalty_fn(model),) if penalty_fn is not None else ()` then
    `total = assemble_loss(base_loss, penalties)` — `loss = total / accum` stays AFTER assembly
    (ARCHITECTURE Pattern 2 / anti-pattern 4: penalty joins BEFORE the /accum divide);
    `summed += float(base_loss.item())` is untouched (logged train_loss stays base-loss-only —
    the ewc_penalty CSV column is Phase 12's concern).
    (2) `train(*, ...)` gains `penalty_fn=None` and `checkpoint_extra=None` keyword-only params
    with docstring Args entries in the existing style (mirror the fixed_batch entry): penalty_fn
    documented as "callable (model) -> scalar tensor added to base_loss via assemble_loss per
    micro-batch (the M2 EWC seam, EWC-02); None reproduces v1.0 bit-for-bit"; checkpoint_extra
    as "dict splatted into every in-loop save_checkpoint call (the M2 fisher/theta_star carry,
    RESEARCH Open Q1); None reproduces v1.0 checkpoints exactly".
    (3) Thread penalty_fn into the `_optimizer_step(...)` call at ~309-311.
    (4) Splat `**(checkpoint_extra or {})` as the final kwargs into ALL THREE save_checkpoint
    call sites (best.pt ~342, in-loop latest.pt ~362, end-of-call latest.pt ~386).
    (5) The AMP+accum+clip ordering (scale -> backward -> unscale_ -> clip -> step -> update ->
    scheduler.step) must not move — it is spy-pinned by test_amp_ordering_unscale_clip_step_update.
    Module docstring of test_loop_penalty_fn.py: house style naming EWC-02, roadmap criterion 4,
    Pitfall 5/6, and the golden-fixture regeneration recipe (the meta block documents the exact
    capture procedure — note it must only ever be regenerated from a git-clean pre-M2 loop).
    After green: run the FULL suite + lint — all 190+ pre-existing tests must pass, especially
    test_train_loop.py and test_resume_curve.py.
  </action>
  <verify>
    <automated>source .venv/bin/activate && pytest tests/test_loop_penalty_fn.py -q -x && make test && make lint</automated>
  </verify>
  <done>tests/test_loop_penalty_fn.py green (golden bit-identity, None==omitted, zero-penalty inert, once-per-accum-step, call count, checkpoint_extra round-trip); full suite green; lint clean.</done>
  <acceptance_criteria>
    - `pytest tests/test_loop_penalty_fn.py -q -x` exits 0
    - `make test` exits 0 (zero failures across the 190+ pre-existing tests)
    - `make lint` exits 0
    - loop.py source assertion: `grep -v '^\s*#' src/personacore/training/loop.py | grep -c 'checkpoint_extra or {}'` outputs 3 (all three save sites threaded)
    - loop.py contains `penalty_fn(model)` and `assemble_loss(base_loss, penalties)` on the non-comment code path
    - loop.py contains `penalty_fn=None` in BOTH the _optimizer_step and train signatures
    - test_loop_penalty_fn.py asserts CSV-text string equality AND param sha256 equality against tests/fixtures/golden_trajectory_v1.json
    - test_loop_penalty_fn.py constructs a real personacore.continual.EWCPenalty in the accum-equivalence test
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checkpoint_extra -> torch.save | caller-supplied dict serialized into own checkpoint files (trusted, local) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Tampering | resume checkpoints written with extras (torch.save pickle) | accept | Own trusted files only; the established weights_only=False-for-own-resume posture is unchanged; shareable artifacts go through the weights_only=True choke points (plan 10-03) |
| T-10-04 | Repudiation | golden fixture provenance | mitigate | meta.captured_at_sha records the exact pre-edit commit; loop_git_clean recorded; regeneration procedure documented in the test module docstring |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies this phase; no install tasks exist |
</threat_model>

<verification>
- Golden fixture committed and structurally valid BEFORE the loop diff exists in history
- `pytest tests/test_loop_penalty_fn.py -q -x` green
- `make test` green (190+ pre-existing tests — the roadmap criterion 4 all-green bar)
- `make lint` clean
- Source assertions: 3 splatted save sites, penalty splice before `/accum`
</verification>

<success_criteria>
- Roadmap success criterion 4 proven by execution: penalty_fn=None and kwarg-omitted runs are bit-identical to the pre-edit golden trajectory (exact CSV text + exact param hash), full suite green
- Roadmap success criterion 3 (integration half): the penalty enters via assemble_loss, exactly once per optimizer step under grad accumulation
- checkpoint_extra threads additively into all three save sites with default-None v1.0 identity (Open Q1 resolved — Phase 12 never re-opens loop.py for this)
</success_criteria>

<output>
Create `.planning/phases/10-ewc-core/10-02-SUMMARY.md` when done
</output>
