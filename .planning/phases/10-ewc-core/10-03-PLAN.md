---
phase: 10-ewc-core
plan: 03
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/personacore/checkpoint.py
  - tests/test_fisher_checkpoint.py
  - scripts/estimate_fisher_tinystories.py
autonomous: true
requirements: [EWC-01]

must_haves:
  truths:
    - "Fisher, theta_star, ewc_lambda, and fisher_meta round-trip intact through save_checkpoint(**extra)/load_checkpoint with the tied tensor deduplicated (wte.weight once, lm_head.weight absent)"
    - "The Fisher cache loads through a single weights_only=True choke point with a schema-version gate and missing-key validation that fail loudly"
    - "Fisher is actually estimated at best.pt over TinyStories train.bin windows with N=2000 (D-04) and the production cache checkpoints/fisher_tinystories.pt exists with convergence stats reported (D-05)"
    - "The smoke script proves on real weights: Fisher finite/non-negative, mean(F)=1, penalty exactly 0.0 at the anchor, penalty > 0 under perturbation"
  artifacts:
    - path: "src/personacore/checkpoint.py"
      provides: "export_fisher/load_fisher + FISHER_SCHEMA_VERSION (load_adapter precedent)"
      contains: "FISHER_SCHEMA_VERSION"
    - path: "tests/test_fisher_checkpoint.py"
      provides: "**extra round-trip + dedup + cache schema-gate pins"
      min_lines: 80
    - path: "scripts/estimate_fisher_tinystories.py"
      provides: "real-weights Fisher run at best.pt + production cache writer (09-04 shape)"
      min_lines: 80
  key_links:
    - from: "src/personacore/checkpoint.py"
      to: "Fisher cache file"
      via: "weights_only=True safe-load choke point in load_fisher"
      pattern: "weights_only=True"
    - from: "scripts/estimate_fisher_tinystories.py"
      to: "src/personacore/continual"
      via: "estimate_fisher + EWCPenalty on real best.pt weights"
      pattern: "estimate_fisher"
    - from: "scripts/estimate_fisher_tinystories.py"
      to: "checkpoints/best.pt"
      via: "trusted weights_only=False anchor load + provenance fingerprint read"
      pattern: "weights_only=False"
---

<objective>
Land the EWC persistence story: Fisher/theta_star travel by value through the open-dict
`save_checkpoint(**extra)` seam (test-pinned round-trip with tied-tensor dedup), a
schema-versioned `weights_only=True`-safe Fisher cache gets `export_fisher`/`load_fisher` in
`checkpoint.py` (the `export_adapter`/`load_adapter` precedent — RESEARCH Open Q2 resolution),
and the production cache is actually produced: `scripts/estimate_fisher_tinystories.py` runs
the real N=2000 estimation at `best.pt` over TinyStories (roadmap success criterion 1's
real-weights half) with D-05 convergence stats reported.

Purpose: EWC-01's "stored with anchor theta_star via the open-dict checkpoint seam" clause, and
the shared Fisher cache Phases 12/13's A/B arms both consume (one estimation pass, two arms).
Output: checkpoint.py additions, tests/test_fisher_checkpoint.py, the smoke script, and the
gitignored `checkpoints/fisher_tinystories.pt` production cache.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/10-ewc-core/10-CONTEXT.md
@.planning/phases/10-ewc-core/10-RESEARCH.md
@.planning/phases/10-ewc-core/10-PATTERNS.md

<interfaces>
<!-- Contracts this plan CONSUMES (created by plan 10-01, wave 1) — use directly. -->

From src/personacore/continual (plan 10-01):
```python
def estimate_fisher(model, bin_path, *, n_examples, block_size, device, seed, normalize=True)
    -> tuple[dict[str, "torch.Tensor"], dict]
# fisher_meta keys: variant, n_examples, seed, block_size, bin_path, normalized, normalizer,
#                   spearman_half, rel_mean_change_a, rel_mean_change_b, spearman_method
class EWCPenalty:
    def __init__(self, fisher, theta_star, lam, device): ...
    def __call__(self, model) -> "torch.Tensor": ...
```

From src/personacore/checkpoint.py (EXISTING — extend, mirror exactly):
```python
CKPT_SCHEMA_VERSION = 1          # line 30
SLIM_SCHEMA_VERSION = 1          # line 31
ADAPTER_SCHEMA_VERSION = 1       # line 32  <- add FISHER_SCHEMA_VERSION = 1 alongside
def save_checkpoint(path, *, model, optimizer, scheduler, scaler=None, step, model_config,
                    train_config, git_sha, val_loss=None, **extra) -> None   # line 35
    # docstring line ~77 reserves: OPEN DICT — M2 may add "fisher"/"theta_star" with no format change
def load_checkpoint(path, ...)   # line 83 — returns the FULL dict (extras readable)
def export_adapter(path, *, adapter, lora_config, base_fingerprint) -> dict   # line 165 — the shape to mirror
def load_adapter(path, *, expected_fingerprint=None, map_location="cpu") -> dict  # line 192 — the loader to mirror
```

NEW contracts this plan CREATES (consumed by Phases 12/13):
```python
FISHER_SCHEMA_VERSION = 1
def export_fisher(path, *, fisher, fisher_meta, anchor_fingerprint) -> dict
    # writes {"schema_version": FISHER_SCHEMA_VERSION, "fisher": ..., "fisher_meta": ...,
    #         "anchor_fingerprint": ...} via torch.save; tensors+primitives ONLY
    # (weights_only=True bar); NO theta_star (recoverable from best.pt, which the
    # fingerprint pins); checkpoint.py NEVER imports continual/ (locked dependency direction)
def load_fisher(path, *, expected_fingerprint=None, map_location="cpu") -> dict
    # torch.load(..., weights_only=True); schema gate FIRST, then missing-key ValueError,
    # then optional fingerprint mismatch check — mirror load_adapter behavior exactly
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: export_fisher/load_fisher + checkpoint **extra round-trip pins</name>
  <files>src/personacore/checkpoint.py, tests/test_fisher_checkpoint.py</files>
  <read_first>
    - src/personacore/checkpoint.py (full file — the schema constants at lines 30-32, save_checkpoint **extra seam at 35-80, load_checkpoint at 83, export_adapter/load_adapter at 165-229 including the expected_fingerprint handling and ValueError message style to mirror verbatim)
    - tests/test_checkpoint.py (test_open_dict_extensible at lines ~83-105 — the **extra round-trip idiom this plan upgrades from placeholder {"x": 1} to real tensors)
    - tests/test_lora_artifact.py (lines ~97-138: the three cache schema-gate test shapes — safe-load-with-exact-keys, schema-mismatch-raises, parametrized missing-key-raises; plus the tiny ModelConfig fixture and fingerprint fixtures)
    - .planning/phases/10-ewc-core/10-RESEARCH.md (Pattern 3 + Pattern 4 cache format; Pitfall 1 dedup)
  </read_first>
  <behavior>
    Write tests/test_fisher_checkpoint.py FIRST. CPU-only, tiny GPT fixture
    `ModelConfig(block_size=32, n_layer=1, n_head=2, n_embd=16)`:
    - Test 1 (**extra round-trip, EWC-01 persistence): build theta_star =
      {n: p.detach().clone() for n, p in model.named_parameters()} and a small fisher dict
      (e.g. torch.rand_like per param); save_checkpoint(tmp, ..., fisher=fisher,
      theta_star=theta_star, ewc_lambda=1.0, fisher_meta={...primitives...}); reload via
      load_checkpoint into a second model; assert fisher/theta_star key sets equal the saved
      ones and every tensor reloads torch.equal (NOT data_ptr — pointers legitimately change
      across serialization); ewc_lambda and fisher_meta round-trip ==.
    - Test 2 (dedup pinned through the seam): "lm_head.weight" not in saved-and-reloaded
      fisher/theta_star; "wte.weight" present; len(theta_star) ==
      len({p.data_ptr() for p in model.parameters()}).
    - Test 3 (cache safe-load bar): export_fisher writes a file that loads via raw
      torch.load(path, map_location="cpu", weights_only=True) with key set exactly
      {"schema_version", "fisher", "fisher_meta", "anchor_fingerprint"}.
    - Test 4 (schema gate): a copy with schema_version=999 re-saved -> load_fisher raises
      ValueError, match="schema_version".
    - Test 5 (missing-key gate, parametrized over "fisher"/"fisher_meta"/"anchor_fingerprint"):
      dropping each key -> load_fisher raises ValueError whose message names the dropped key.
    - Test 6 (fingerprint check): load_fisher(path, expected_fingerprint=<mismatching trio>)
      raises ValueError; matching fingerprint loads fine (mirror load_adapter's exact semantics
      — read its implementation first and replicate its behavior shape).
    - Test 7 (no theta_star in cache): the exported dict contains no "theta_star" key.
  </behavior>
  <action>
    Extend src/personacore/checkpoint.py mirroring the adapter precedent symmetrically
    (RESEARCH Open Q2 resolution — discretion resolved in favor of checkpoint.py ownership):
    (1) add `FISHER_SCHEMA_VERSION = 1` directly below ADAPTER_SCHEMA_VERSION (line 32) with a
    same-style comment ("Fisher cache schema (EWC-01 / Phase 10); independent again.");
    (2) `def export_fisher(path, *, fisher, fisher_meta, anchor_fingerprint) -> dict` — build
    {"schema_version": FISHER_SCHEMA_VERSION, "fisher": fisher, "fisher_meta": fisher_meta,
    "anchor_fingerprint": anchor_fingerprint}, torch.save, return the dict. Docstring states:
    tensors and primitive containers exclusively so it round-trips through
    torch.load(weights_only=True); the caller supplies plain dicts — checkpoint.py NEVER
    imports continual/ (the locked dependency direction from export_adapter's docstring);
    theta_star is deliberately NOT in the cache (recoverable from best.pt, which
    anchor_fingerprint pins; the cache is an optimization only — resume never depends on it,
    ARCHITECTURE anti-pattern 2);
    (3) `def load_fisher(path, *, expected_fingerprint=None, map_location="cpu") -> dict` —
    torch.load(path, map_location=map_location, weights_only=True); schema gate FIRST raising
    ValueError in load_adapter's exact message style ("Unsupported fisher schema_version ...
    Re-export with personacore.checkpoint.export_fisher."); then missing-key check over
    {"fisher", "fisher_meta", "anchor_fingerprint"} raising ValueError naming sorted missing
    keys; then the expected_fingerprint comparison replicating load_adapter's semantics.
    save_checkpoint/load_checkpoint are NOT modified — the **extra seam is consumed verbatim.
    Module docstring of the test file: house style naming EWC-01, Pattern 3, Pitfall 1, and
    the T-10-05 safe-load bar.
  </action>
  <verify>
    <automated>source .venv/bin/activate && pytest tests/test_fisher_checkpoint.py -q -x && make test && make lint</automated>
  </verify>
  <done>tests/test_fisher_checkpoint.py green (round-trip, dedup, safe-load, schema gate, missing-key gate, fingerprint, no-theta_star); full suite green; lint clean.</done>
  <acceptance_criteria>
    - `pytest tests/test_fisher_checkpoint.py -q -x` exits 0
    - `make test` exits 0 (190+ pre-existing tests intact)
    - `make lint` exits 0
    - src/personacore/checkpoint.py contains `FISHER_SCHEMA_VERSION = 1`, `def export_fisher(` and `def load_fisher(`
    - load_fisher contains `weights_only=True` and does not anywhere fall back to weights_only=False
    - checkpoint.py contains no `from personacore.continual` / `import personacore.continual` (locked dependency direction)
    - tests assert reload equality via torch.equal and the absence of "lm_head.weight" from both fisher and theta_star dicts
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Real-weights Fisher run at best.pt — production cache + SystemExit proof checks</name>
  <files>scripts/estimate_fisher_tinystories.py</files>
  <read_first>
    - scripts/train_adapter_smoke.py (the 09-04 precedent — copy its shape section by section: MPS-fallback env before torch import + noqa E402, _REPO_ROOT constants, tuned-constants block, preflight gate, FileNotFoundError prerequisites, trusted weights_only=False load comment, SystemExit-not-assert checks, refuse-to-rerun semantics, provenance fingerprint READ from the blob, [script_name]-prefixed footer prints)
    - src/personacore/checkpoint.py (export_fisher just added in Task 1; the trusted-load comment style at lines ~95-98)
    - src/personacore/continual/fisher.py and src/personacore/continual/ewc.py (the plan 10-01 deliverables this script drives on real weights)
    - .planning/phases/10-ewc-core/10-RESEARCH.md (Pattern 4 — the six recommended proof checks; D-04/D-05 from the user_constraints block)
  </read_first>
  <action>
    Create scripts/estimate_fisher_tinystories.py mirroring train_adapter_smoke.py's shape
    exactly (thin script, logic in the package, no argparse — house D-04 pattern):
    HEADER: module docstring stating what it proves (EWC-01 real-weights half: Fisher actually
    estimated at best.pt over TinyStories windows, D-04 N=2000, D-05 convergence evidence;
    writes the production cache Phases 12/13 share);
    `os.environ.setdefault("PYTORCH_ENABLE_MPS_FALLBACK", "1")` BEFORE importing torch;
    noqa: E402 imports; constants `_REPO_ROOT`, `BEST_PATH = _REPO_ROOT / "checkpoints" /
    "best.pt"`, `TRAIN_BIN = _REPO_ROOT / "data" / "train.bin"`, `FISHER_CACHE = _REPO_ROOT /
    "checkpoints" / "fisher_tinystories.pt"` (gitignored — checkpoints/ is covered).
    TUNED CONSTANTS with rationale comments: `N_EXAMPLES = 2000` (D-04 — measured <1 min on
    MPS), `SEED = 1234`, `EWC_LAMBDA = 1.0` (Phase-10 placeholder convention — the real lambda
    is Phase 12's sweep, EWC-03).
    MAIN: (1) `preflight_device(strict=True)` gate, print the summary;
    (2) FileNotFoundError if BEST_PATH or TRAIN_BIN missing (name the producing script in the
    message); (3) refuse-to-rerun: if FISHER_CACHE exists raise SystemExit telling the user to
    delete it to re-estimate (keeps cache provenance stable for Phases 12/13 — RESEARCH Open Q3
    resolution); (4) trusted anchor load: `blob = torch.load(BEST_PATH, weights_only=False)`
    with the TRUSTED-own-file comment, rebuild ModelConfig(**blob["model_config"]), GPT,
    load_state_dict, move to the preflight device, model.eval();
    (5) theta_star = {n: p.detach().clone().cpu() for n, p in model.named_parameters()};
    (6) fisher, meta = estimate_fisher(model, TRAIN_BIN, n_examples=N_EXAMPLES,
    block_size=model_cfg.block_size, device=<preflight device>, seed=SEED);
    (7) PROOF CHECKS, each `raise SystemExit(...)` naming the requirement/pitfall, NEVER assert:
    [a] every fisher tensor finite and >= 0; [b] "lm_head.weight" absent and "wte.weight"
    present with exactly one entry per distinct param storage (Pitfall 1); [c] global mean of
    the normalized fisher == 1 within fp32 tolerance (compute in fp64 on CPU) and
    meta["normalizer"] > 0 (D-01); [d] EWCPenalty(fisher, theta_star, EWC_LAMBDA,
    device="cpu")(cpu copy of model — or move model to CPU for this check) returns exactly 0.0
    at the anchor; [e] perturb one parameter in-place under torch.no_grad (e.g. add 1e-3) ->
    penalty > 0 (the penalty sees drift); [f] print convergence stats (spearman_half,
    rel_mean_change_a/b) — REPORT, do not gate (D-05: "N is enough" is a measured claim);
    (8) export_fisher(FISHER_CACHE, fisher=fisher, fisher_meta=meta,
    anchor_fingerprint={"git_sha": blob["git_sha"], "step": blob["step"],
    "val_loss": blob["val_loss"]}) — fingerprint READ from the blob, never recomputed;
    (9) footer: print cache size in MB, normalizer, convergence stats, wall-clock with the
    [estimate_fisher_tinystories] prefix; `if __name__ == "__main__": main()`.
    THEN RUN IT: `.venv/bin/python scripts/estimate_fisher_tinystories.py` from the repo root
    (~1-2 min). Then prove the cache loads through the safe choke point and that re-running
    refuses. Do NOT git-add the cache (checkpoints/ is gitignored).
  </action>
  <verify>
    <automated>source .venv/bin/activate && rm -f checkpoints/fisher_tinystories.pt && python scripts/estimate_fisher_tinystories.py && test -f checkpoints/fisher_tinystories.pt && python -c "from personacore.checkpoint import load_fisher; d=load_fisher('checkpoints/fisher_tinystories.pt'); assert d['fisher_meta']['n_examples']==2000 and 'spearman_half' in d['fisher_meta'] and 'theta_star' not in d; print('cache OK:', d['anchor_fingerprint'])" && (python scripts/estimate_fisher_tinystories.py; test $? -ne 0 && echo "rerun correctly refused")</automated>
  </verify>
  <done>Script exits 0 on first run (any pre-existing cache removed beforehand so the first run actually estimates rather than refusing) with all proof checks passed and convergence stats printed; checkpoints/fisher_tinystories.pt exists and loads via load_fisher (weights_only=True) with n_examples=2000 and convergence stats in meta; a second run exits non-zero with the delete-to-re-estimate message; cache not committed.</done>
  <acceptance_criteria>
    - With no pre-existing cache (`rm -f checkpoints/fisher_tinystories.pt` first), `python scripts/estimate_fisher_tinystories.py` exits 0 and prints spearman_half plus both rel_mean_change values — the verify chain uses `&&` after this first invocation so a refused/failed first run cannot be masked by a stale cache
    - checkpoints/fisher_tinystories.pt exists and `load_fisher` returns a dict whose fisher_meta has n_examples==2000, normalizer>0, and whose top level has no "theta_star" key
    - Second invocation exits non-zero (refuse-to-rerun) with a message containing "delete"
    - Script source contains `PYTORCH_ENABLE_MPS_FALLBACK` before the torch import, `preflight_device(strict=True)`, `N_EXAMPLES = 2000`, and at least 5 `raise SystemExit(` proof checks
    - Script source contains no `assert ` statements used as proof checks and no argparse import
    - `git status --porcelain checkpoints/` shows nothing (cache untracked via existing gitignore)
    - `make lint` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem -> torch.load(best.pt) | full pickle deserialization of the anchor checkpoint (own trusted artifact) |
| filesystem -> load_fisher(cache) | the shareable Fisher cache crosses a load boundary in future phases |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05 | Tampering/Elevation | torch.load(BEST_PATH, weights_only=False) in the smoke script | mitigate | Established trusted-own-files-only posture: load only the repo-local, self-produced best.pt; TRUSTED-only comment at the call site (house pattern); never applied to foreign files |
| T-10-06 | Tampering/Elevation | Fisher cache deserialization | mitigate | ASVS V5 input validation at the single load_fisher choke point: weights_only=True (restricted unpickler), schema_version gate before consumption, missing-key ValueError, optional anchor fingerprint check — test-pinned (Tests 3-6) |
| T-10-07 | Information Disclosure | large derived artifacts committed to the repo | mitigate | checkpoints/ already gitignored; acceptance criterion asserts `git status --porcelain checkpoints/` is empty |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies this phase; no install tasks exist |
</threat_model>

<verification>
- `pytest tests/test_fisher_checkpoint.py -q -x` green
- `make test` green (full 190+ suite)
- `make lint` clean
- `python scripts/estimate_fisher_tinystories.py` exits 0 from a clean (no pre-existing cache) state; rerun refuses; cache loads via load_fisher with weights_only=True
</verification>

<success_criteria>
- Roadmap success criterion 1 (real-weights half): Fisher actually estimated at best.pt over TinyStories windows, N=2000, normalized, convergence measured and reported (D-04/D-05)
- Roadmap success criterion 2: Fisher + theta_star persist via the open-dict checkpoint seam and reload intact with data_ptr-deduplicated tied tensors (test-pinned)
- The production cache exists for Phases 12/13 to share, behind a schema-versioned weights_only=True choke point; resume checkpoints stay self-contained (no sidecar dependency)
</success_criteria>

<output>
Create `.planning/phases/10-ewc-core/10-03-SUMMARY.md` when done
</output>
