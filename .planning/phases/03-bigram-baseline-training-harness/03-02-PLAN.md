---
phase: 03-bigram-baseline-training-harness
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/personacore/model/__init__.py
  - src/personacore/model/bigram.py
  - src/personacore/training/loss.py
  - tests/test_bigram_model.py
  - tests/test_assemble_loss.py
autonomous: true
requirements: [MODEL-01, TRAIN-06]

must_haves:
  truths:
    - "A from-scratch bigram model returns (logits, loss) on the LOCKED forward contract and (logits, None) without targets (D-01 lookup-table bigram nn.Embedding(V,V); D-02 forward(idx,targets=None)->(logits,loss))"
    - "Cross-entropy is computed internally with the nanoGPT (B*T, V) flatten so the same call works for the Phase-4 GPT"
    - "assemble_loss is an identity on the empty tuple and additive on a non-empty penalty tuple — the M2 EWC seam, plumbing only (D-03 loss assembled in the training loop, model stays pure; D-04 identity-on-empty)"
  artifacts:
    - path: "src/personacore/model/bigram.py"
      provides: "BigramLanguageModel(nn.Embedding(V,V)) with forward(idx, targets=None) -> (logits, loss)"
      contains: "class BigramLanguageModel"
      min_lines: 15
    - path: "src/personacore/model/__init__.py"
      provides: "public BigramLanguageModel export"
      exports: ["BigramLanguageModel"]
    - path: "src/personacore/training/loss.py"
      provides: "assemble_loss(base_loss, extra_penalties=()) -> base + sum(extra)"
      contains: "def assemble_loss"
  key_links:
    - from: "src/personacore/model/bigram.py"
      to: "torch.nn.functional.cross_entropy"
      via: "logits.view(B*T, V) vs targets.view(B*T)"
      pattern: "cross_entropy"
    - from: "src/personacore/model/__init__.py"
      to: "src/personacore/model/bigram.py"
      via: "from .bigram import BigramLanguageModel"
      pattern: "from .bigram import"
---

<objective>
Deliver the first vertical slice: the model->loss contract. After this plan a real user can
import `BigramLanguageModel`, run a forward pass to get `(logits, loss)`, and assemble that
loss through the M2 EWC seam — the exact interface Phase-4's GPT will implement unchanged.

Purpose: De-risk the LOCKED `forward(idx, targets=None) -> (logits, loss)` contract (D-02) and
the loop-level `assemble_loss` extension point (D-03/D-04) with from-scratch code, turning the
RED MODEL-01 and TRAIN-06(loss) tests GREEN.
Output: `model/bigram.py`, `model/__init__.py`, `training/loss.py`, and their two green tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-bigram-baseline-training-harness/03-CONTEXT.md
@.planning/phases/03-bigram-baseline-training-harness/03-RESEARCH.md
@.planning/phases/03-bigram-baseline-training-harness/03-PATTERNS.md
@src/personacore/config.py
@src/personacore/seeding.py
@src/personacore/tokenizer/__init__.py
@tests/test_bigram_model.py
@tests/test_assemble_loss.py

<interfaces>
<!-- Contracts to IMPLEMENT (RED tests from Plan 01 assert exactly these). -->
D-02 / D-02a (model, pure base loss only — D-03):
  class BigramLanguageModel(nn.Module):
      def __init__(self, vocab_size: int): ...           # nn.Embedding(vocab_size, vocab_size)
      def forward(self, idx, targets=None):              # LOCKED signature
          logits = self.token_table(idx)                 # (B, T, V)
          # targets is None -> (logits, None)
          # else: loss = F.cross_entropy(logits.view(B*T, V), targets.view(B*T)); return (logits, loss)

D-04 (loss assembly lives in the loop module, NOT the model):
  def assemble_loss(base_loss, extra_penalties=()):      # () -> identity; (p,) -> base + p

Repo docstring convention (config.py:1-13, seeding.py:1-15): every module opens with a
"""..."""  naming its requirement/decision ID and the load-bearing property.
Barrel convention: src/personacore/tokenizer/__init__.py (docstring + relative import + __all__).
Typing convention (PATTERNS.md): annotate vocab_size: int; leave idx/targets tensor args untyped.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: From-scratch bigram model + package barrel (MODEL-01)</name>
  <files>src/personacore/model/bigram.py, src/personacore/model/__init__.py</files>
  <read_first>
    - src/personacore/tokenizer/bpe.py (repo module-docstring + imperative-docstring style)
    - src/personacore/tokenizer/__init__.py (barrel shape to copy)
    - .planning/phases/03-bigram-baseline-training-harness/03-PATTERNS.md (model/bigram.py + model/__init__.py sections, lines 32-87)
    - .planning/phases/03-bigram-baseline-training-harness/03-RESEARCH.md (§Pattern 1, lines 186-207)
    - tests/test_bigram_model.py (the RED contract this turns GREEN)
  </read_first>
  <behavior>
    - forward(idx) with no targets -> (logits, None), logits.shape == (B, T, vocab_size)
    - forward(idx, targets) -> (logits, loss), loss a scalar tensor from F.cross_entropy
    - CE uses the (B*T, V) vs (B*T) flatten (D-02a); loss ~ ln(vocab_size) at random init
    - assemble_loss / penalties are NOT present in this model file (D-03 — model stays pure)
  </behavior>
  <action>
    Create `src/personacore/model/bigram.py` with a module docstring naming MODEL-01 / D-01 /
    D-02 / D-03 (match config.py:1-13 style). Implement `BigramLanguageModel(nn.Module)` whose
    `__init__(self, vocab_size: int)` builds `self.token_table = nn.Embedding(vocab_size,
    vocab_size)` and whose `forward(self, idx, targets=None)` is the LOCKED contract: `logits =
    self.token_table(idx)` (shape `(B, T, V)`); if `targets is None` return `(logits, None)`;
    else unpack `B, T, V = logits.shape`, compute `loss = F.cross_entropy(logits.view(B*T, V),
    targets.view(B*T))` and return `(logits, loss)` — base CE ONLY, no penalties (D-03).
    Annotate only `vocab_size: int`; leave `idx`/`targets` untyped (PATTERNS typing convention).
    Create `src/personacore/model/__init__.py` as a thin barrel: docstring naming D-09,
    `from .bigram import BigramLanguageModel`, `__all__ = ["BigramLanguageModel"]` (Phase 4 adds
    `from .gpt import GPT` here later). Do NOT add a `generate`/`sample` method to the model
    (D-11 — minimal sampling lives in the loop in Plan 04).
  </action>
  <verify>
    <automated>pytest tests/test_bigram_model.py -x -q</automated>
  </verify>
  <done>test_bigram_model.py is GREEN: forward returns (logits, None) and (logits, loss) with correct shapes and the (B*T, V) CE flatten; `from personacore.model import BigramLanguageModel` resolves.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: assemble_loss seam — the M2 EWC hook (TRAIN-06)</name>
  <files>src/personacore/training/loss.py</files>
  <read_first>
    - src/personacore/seeding.py (thin pure-function module with decision-anchored docstring — the analog)
    - .planning/phases/03-bigram-baseline-training-harness/03-PATTERNS.md (training/loss.py section, lines 109-129)
    - .planning/phases/03-bigram-baseline-training-harness/03-RESEARCH.md (§Code Examples assemble_loss, lines 384-394; D-04)
    - tests/test_assemble_loss.py (the RED identity+additive contract this turns GREEN)
  </read_first>
  <behavior>
    - assemble_loss(x, ()) equals x (identity on empty tuple)
    - assemble_loss(x, (p,)) equals x + p for scalar tensors x, p
    - assemble_loss(x, (p, q)) equals x + p + q (additive over the whole tuple)
    - no callbacks / no lazy callables; each penalty is a precomputed scalar tensor (D-04)
  </behavior>
  <action>
    Create `src/personacore/training/loss.py` with a module docstring naming TRAIN-06 / D-03 /
    D-04 and stating the load-bearing property (loop-level seam, model stays pure, identity in
    M1, EWC adds `(fisher_penalty,)` in M2 with zero changes). Implement
    `assemble_loss(base_loss, extra_penalties=())` as: `total = base_loss`; iterate
    `for p in extra_penalties: total = total + p`; `return total`. M1 always passes `()`
    (identity). This file lives in `training/`, NEVER in the model (D-03). The `training/__init__.py`
    barrel is created by Plan 04 (which owns the full training surface) — do NOT create it here
    to avoid a same-phase file-ownership conflict; import via the full path
    `from personacore.training.loss import assemble_loss` in the test.
  </action>
  <verify>
    <automated>pytest tests/test_assemble_loss.py -x -q</automated>
  </verify>
  <done>test_assemble_loss.py is GREEN: identity-on-empty and additive-on-non-empty both hold; loss.py exists with the D-04 signature and no callbacks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This slice is pure in-process tensor math; no I/O, network, auth, or storage surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Tampering | assemble_loss extension seam (open to M2 penalty tensors) | accept | Penalties are precomputed scalar tensors produced by trusted own code (M2 EWC); no untrusted input crosses this boundary. Identity in M1. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages — only torch (pre-vetted). No install task exists (RESEARCH §Package Legitimacy Audit). |
</threat_model>

<verification>
- `pytest tests/test_bigram_model.py tests/test_assemble_loss.py -x -q` is GREEN.
- `python3 -c "from personacore.model import BigramLanguageModel"` resolves.
- `make test` (full CPU suite) stays green — this plan only adds modules.
</verification>

<success_criteria>
- BigramLanguageModel implements the LOCKED `forward(idx, targets=None) -> (logits, loss)`
  contract with internal CE and the (B*T, V) flatten (MODEL-01 / D-02 / D-02a).
- assemble_loss is identity-on-empty and additive (TRAIN-06 / D-04) and lives in `training/`.
- The model stays pure — no assemble_loss, penalties, or sampling inside bigram.py (D-03/D-11).
</success_criteria>

<output>
Create `.planning/phases/03-bigram-baseline-training-harness/03-02-SUMMARY.md` when done.
</output>
