---
phase: 03-bigram-baseline-training-harness
plan: 04
type: execute
wave: 3
depends_on: ["03-02", "03-03"]
files_modified:
  - src/personacore/training/loop.py
  - src/personacore/training/__init__.py
  - scripts/train_bigram.py
  - tests/test_train_loop.py
  - tests/test_overfit_batch.py
  - tests/test_resume_curve.py
autonomous: true
requirements: [TRAIN-01, TRAIN-02, TRAIN-04, TRAIN-05, TRAIN-06]

must_haves:
  truths:
    - "The bigram trains end-to-end through the loop and a minimal sampler produces decoded text output"
    - "The loop runs AdamW + warmup/cosine LR + grad clip + configurable grad accumulation, fp32 by default with an optional fp16-AMP+GradScaler path"
    - "AMP step ordering is scale->backward (per micro-batch) -> unscale_ -> clip -> step -> update, scheduler stepped once per optimizer step"
    - "Overfitting one fixed batch drives loss toward zero, proving harness correctness independent of attention"
    - "A save->kill->resume reproduces the loss curve across a restart from the CSV log within 1e-6 (D-05 reuse src/personacore/checkpoint.py unchanged — open-dict, full RNG restore, scheduler.state_dict() requires a LambdaLR object)"
  artifacts:
    - path: "src/personacore/training/loop.py"
      provides: "train() orchestration (AMP/accum/clip), estimate_loss(), minimal sample()"
      contains: "def train"
      min_lines: 40
    - path: "src/personacore/training/__init__.py"
      provides: "public training surface barrel"
      exports: ["train", "assemble_loss", "build_scheduler", "get_batch", "load_split"]
    - path: "scripts/train_bigram.py"
      provides: "thin no-argparse entry point wiring configs -> seed -> train()"
      contains: "def main"
  key_links:
    - from: "src/personacore/training/loop.py"
      to: "RuntimeConfig.autocast() + torch.amp.GradScaler"
      via: "runtime.autocast() + GradScaler(enabled=runtime.amp)"
      pattern: "autocast\\(\\)"
    - from: "src/personacore/training/loop.py"
      to: "checkpoint.py save_checkpoint / load_checkpoint"
      via: "resume reads step + restores RNG state; CSV re-opens same path"
      pattern: "save_checkpoint"
    - from: "src/personacore/training/loop.py"
      to: "scaler.unscale_ before clip_grad_norm_"
      via: "mandatory AMP ordering"
      pattern: "unscale_"
---

<objective>
Deliver the final vertical slice that makes Phase 3 end-to-end: the training loop + thin entry
script. After this plan a real user can run `python scripts/train_bigram.py` and watch the
bigram train through AdamW + warmup/cosine + grad-clip + grad-accum, log a CSV curve, checkpoint,
resume bit-identically after a kill, and print decoded sampled text — the whole tokenize -> train
-> eval -> sample -> see output harness the bigram exists to de-risk.

Purpose: Orchestrate the existing primitives (RuntimeConfig.autocast, GradScaler, save/load
checkpoint, CSVLogger, seed_everything) plus the Plan-02/03 modules into the loop, with the two
highest-risk seams correct: AMP step ordering under grad accumulation (TRAIN-02) and resume
trajectory + curve reproducibility (TRAIN-04). Turns the RED TRAIN-01/02/04/05/06(resume) tests
GREEN.
Output: `training/loop.py`, `training/__init__.py`, `scripts/train_bigram.py`, three green tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-bigram-baseline-training-harness/03-CONTEXT.md
@.planning/phases/03-bigram-baseline-training-harness/03-RESEARCH.md
@.planning/phases/03-bigram-baseline-training-harness/03-PATTERNS.md
@src/personacore/config.py
@src/personacore/checkpoint.py
@src/personacore/logging.py
@src/personacore/seeding.py
@src/personacore/provenance.py
@scripts/train_tokenizer.py
@scripts/preflight_demo.py
@tests/test_checkpoint.py
@tests/test_logging.py
@tests/test_train_loop.py
@tests/test_overfit_batch.py
@tests/test_resume_curve.py

<interfaces>
<!-- Contracts to CONSUME (Plan 02/03) and IMPLEMENT (loop + script). -->
From Plan 02:
  from personacore.model import BigramLanguageModel       # forward(idx, targets=None) -> (logits, loss)
  from personacore.training.loss import assemble_loss     # assemble_loss(base, ()) -> base (identity)
From Plan 03:
  from personacore.training.data import load_split, get_batch
  from personacore.training.schedule import build_scheduler

Existing primitives to REUSE UNCHANGED:
  RuntimeConfig(...).autocast()                           # single AMP source of truth; do NOT call torch.cuda.*
  torch.amp.GradScaler(device=runtime.device.split(":")[0], enabled=runtime.amp)
  save_checkpoint(path, *, model, optimizer, scheduler, step, model_config, train_config, git_sha, val_loss=None, **extra)
  load_checkpoint(path, *, model, optimizer=None, scheduler=None, map_location="cpu") -> ckpt dict (ckpt["step"])
  CSVLogger(path, fieldnames).log(**row)                  # append-only, header-once, restart-safe
  seed_everything(seed)                                   # FRESH run only — NEVER on resume
  git_sha()                                               # provenance.py

AMP ordering (load-bearing — RESEARCH §Pattern 2):
  optimizer.zero_grad(set_to_none=True)
  for micro in range(grad_accum_steps):
      with runtime.autocast(): logits, base = model(xb, yb); total = assemble_loss(base, ()); loss = total / grad_accum_steps
      scaler.scale(loss).backward()
  scaler.unscale_(optimizer)                              # BEFORE clip (mandatory)
  torch.nn.utils.clip_grad_norm_(model.parameters(), cfg.grad_clip)
  scaler.step(optimizer); scaler.update(); scheduler.step()   # scheduler ONCE per optimizer step

CSV fieldnames: ["step","train_loss","val_loss","lr","tokens","wall_clock"] (RESEARCH Open Q1)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Training loop + minimal sampler + training barrel (TRAIN-01/02/03/04/06)</name>
  <files>src/personacore/training/loop.py, src/personacore/training/__init__.py</files>
  <read_first>
    - tests/test_checkpoint.py::_train_step + test_resume_identical_trajectory (loop skeleton + resume analog, lines 27-80)
    - src/personacore/checkpoint.py (save/load signatures + RNG-restore semantics)
    - src/personacore/logging.py (CSVLogger append/header-once contract)
    - src/personacore/config.py (RuntimeConfig.autocast + amp flag; TrainConfig fields)
    - .planning/phases/03-bigram-baseline-training-harness/03-PATTERNS.md (training/loop.py + training/__init__.py sections, lines 91-104, 219-280)
    - .planning/phases/03-bigram-baseline-training-harness/03-RESEARCH.md (§Pattern 2/3, §Code Examples estimate_loss, Pitfalls 1/2/3/4)
    - tests/test_train_loop.py (RED AMP-ordering + grad-accum contract this turns GREEN)
  </read_first>
  <behavior>
    - One step records ops in order: scale_backward x grad_accum_steps, then unscale_, clip, step, update (AMP ordering)
    - Exactly one unscale_ per optimizer step; scheduler.step() once per optimizer step (not per micro-batch — Pitfall 2)
    - grad_accum_steps=N over micro-batches ~= one big batch within tolerance
    - estimate_loss runs under model.eval()+no_grad and restores model.train()
    - On resume: ckpt["step"] read and the loop counter continues; CSV re-opened on the same path (single header, no step reset — Pitfall 4)
    - sample(model, idx, max_new_tokens, temperature) returns idx extended by max_new_tokens
  </behavior>
  <action>
    Create `src/personacore/training/loop.py` (docstring naming TRAIN-01/02/04 and the AMP
    ordering + resume properties). Build `optimizer = torch.optim.AdamW(model.parameters(),
    lr=cfg.lr, weight_decay=cfg.weight_decay)`, `scheduler = build_scheduler(optimizer, cfg)`,
    `scaler = GradScaler(device=runtime.device.split(":")[0], enabled=runtime.amp)` — the loop
    MUST NOT call `torch.cuda.*` (RuntimeConfig is the single AMP/device source; config.py:44-61).
    Implement the per-step AMP ordering EXACTLY as the interface block: zero_grad ->
    for-micro( autocast forward -> assemble_loss(base, ()) -> divide by grad_accum_steps ->
    scaler.scale(loss).backward() ) -> scaler.unscale_(optimizer) -> clip_grad_norm_(grad_clip)
    -> scaler.step -> scaler.update -> scheduler.step (ONCE per optimizer step). Implement
    `estimate_loss(model, val_ids, cfg, model_cfg, device, iters=20)` under `@torch.no_grad()`
    with `model.eval()`/`model.train()` toggle. Implement `train(...)` that wires get_batch ->
    step loop -> periodic `estimate_loss` + `CSVLogger.log(step, train_loss, val_loss,
    lr=scheduler.get_last_lr()[0], tokens, wall_clock)` + `save_checkpoint(...)` every K steps,
    and supports resume: when a checkpoint exists call `load_checkpoint`, read `ckpt["step"]`,
    continue the counter, re-open the SAME csv path, and do NOT call `seed_everything` again
    (RNG state is restored — Anti-Pattern). Add the minimal `sample(model, idx, max_new_tokens,
    temperature=1.0)` free function HERE (D-11, Open Q2) — greedy/temperature next-token loop
    under `@torch.no_grad()`; do NOT build top-k/top-p/EOS-stop (that is Phase 6 / GEN). Create
    `src/personacore/training/__init__.py` barrel exporting `train`, `assemble_loss`,
    `build_lr_lambda`, `build_scheduler`, `get_batch`, `load_split` (and `sample`).
  </action>
  <verify>
    <automated>pytest tests/test_train_loop.py tests/test_resume_curve.py -x -q</automated>
  </verify>
  <done>test_train_loop.py (AMP ordering + grad-accum) and test_resume_curve.py (resume trajectory within 1e-6 + CSV curve reproduces, single header) are GREEN. The fp16 smoke test stays skipped on CPU.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Overfit gate + thin train_bigram.py entry point (TRAIN-05, MVP "see output")</name>
  <files>src/personacore/training/loop.py, scripts/train_bigram.py</files>
  <read_first>
    - scripts/train_tokenizer.py (thin no-argparse script pattern, lines 25-49)
    - scripts/preflight_demo.py (entry-point pattern, lines 27-36)
    - src/personacore/seeding.py (seed_everything — fresh run only)
    - .planning/phases/03-bigram-baseline-training-harness/03-PATTERNS.md (scripts/train_bigram.py + test_overfit_batch.py sections, lines 284-315, 385-389)
    - .planning/phases/03-bigram-baseline-training-harness/03-RESEARCH.md (Pitfall 5, A2, Open Q3 — overfit config)
    - tests/test_overfit_batch.py (RED overfit gate this turns GREEN)
  </read_first>
  <behavior>
    - With a dedicated small TrainConfig (warmup_steps=0, ~200-500 max_steps, lr ~1e-2..1e-1) and ONE fixed batch reused every step, final loss drops below a small threshold (well under ln(8192)≈9.0)
    - The overfit run is deterministic under seed_everything (same final loss across runs)
    - scripts/train_bigram.py runs end-to-end on the fixture and prints a decoded sample (no argparse)
  </behavior>
  <action>
    Implement the TRAIN-05 overfit gate to satisfy `tests/test_overfit_batch.py`: use
    `seed_everything(seed)` (Pitfall 5 — determinism), a DEDICATED small `TrainConfig`
    (`warmup_steps=0`, `max_steps` ~200-500, `lr` ~1e-2..1e-1) so warmup doesn't starve the short
    budget, reuse ONE fixed batch every step (a lookup-table bigram can memorize it), and pin the
    threshold empirically from an observed run (A2 — e.g. `< 0.5` CE; set it just above the
    observed converged value to avoid flakiness). If the loop's `train()` cannot reuse a fixed
    batch, expose a minimal `overfit_batch(model, xb, yb, cfg)` helper in loop.py for the test;
    otherwise drive the existing step function in the test directly.
    Create `scripts/train_bigram.py` (thin, NO argparse — Phase-1 D-04): module docstring naming
    D-09; `_REPO_ROOT = pathlib.Path(__file__).resolve().parent.parent`;
    `FIXTURE_PATH = _REPO_ROOT/"tests"/"fixtures"/"bigram_corpus.txt"`; `main()` calls
    `seed_everything(TrainConfig().seed)` FIRST (fresh run only), builds
    `RuntimeConfig`/`ModelConfig`/`TrainConfig`, `load_split(FIXTURE_PATH)`,
    `BigramLanguageModel(ModelConfig().vocab_size)`, calls `train(...)` for a small step budget,
    then runs `sample(...)` + tokenizer `decode` and `print`s the decoded text (MVP "see output").
    Logic lives in the package; the script only wires configs/paths. `if __name__ == "__main__": main()`.
    Confirm `.gitignore` already covers `*.pt` and `logs/` (it does) so checkpoint/CSV outputs
    from a run are never committed (A5).
  </action>
  <verify>
    <automated>pytest tests/test_overfit_batch.py -x -q && python scripts/train_bigram.py</automated>
  </verify>
  <done>test_overfit_batch.py is GREEN (loss < pinned threshold, deterministic). `python scripts/train_bigram.py` runs end-to-end on the fixture and prints decoded sampled text without argparse. Run outputs (*.pt, CSV) are gitignored.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| own checkpoint -> torch.load | loop.py resume calls load_checkpoint (weights_only=False, pickle) on files the harness itself wrote to a local/Kaggle working dir |
| committed fixture -> tokenizer | scripts/train_bigram.py reads the trusted committed fixture + frozen tokenizer artifact only |

Phase 3 stays fully offline / on-device / zero-budget — no network, auth, multi-user, or
external storage surface. No new dependency is introduced.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering / RCE | resume checkpoint load via torch.load(weights_only=False) | accept | Inherited & already documented TRUSTED-ONLY (checkpoint.py:16-21): load only own checkpoints; map_location="cpu". The slim weights_only=True inference checkpoint is Phase 8. No change here. |
| T-03-05 | Information Disclosure | training outputs (latest.pt, CSV log) committed by accident | mitigate | .gitignore already covers *.pt, logs/, checkpoints/ (verified) — Task 2 re-confirms outputs land in ignored paths so weight-based memory is never committed (project privacy requirement). |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages — torch/numpy only (pre-vetted, RESEARCH §Package Legitimacy Audit). No install task exists; the gate is informational. |
</threat_model>

<verification>
- `pytest tests/test_train_loop.py tests/test_overfit_batch.py tests/test_resume_curve.py -x -q`
  is GREEN; the fp16 smoke test is skipped on CPU.
- `make test` (full CPU suite incl. inherited Phase-1/2 tests) is green.
- The four hard-correctness gates pass: overfit convergence (TRAIN-05), resume curve
  reproducibility within 1e-6 (TRAIN-04), no train/val leakage (TRAIN-03, from Plan 03), AMP
  ordering (TRAIN-02).
- `python scripts/train_bigram.py` trains end-to-end and prints decoded sampled text.
</verification>

<success_criteria>
- The loop runs AdamW + warmup/cosine + grad-clip + configurable grad-accum, fp32 default with
  the optional fp16-AMP+GradScaler path and the correct scale->unscale_->clip->step->update
  ordering (TRAIN-01 / TRAIN-02).
- Periodic eval()+no_grad validation loss is logged to CSV and the curve reproduces across a
  kill+resume within 1e-6 (TRAIN-03/04).
- Overfitting one fixed batch drives loss below the pinned threshold deterministically (TRAIN-05).
- Loss is assembled via assemble_loss(base, ()) and checkpoints are open dicts driven
  end-to-end (TRAIN-06 — plumbing only, no Fisher penalty).
- A thin no-argparse `scripts/train_bigram.py` produces decoded output (MVP "see output", D-11).
</success_criteria>

<output>
Create `.planning/phases/03-bigram-baseline-training-harness/03-04-SUMMARY.md` when done.
</output>
