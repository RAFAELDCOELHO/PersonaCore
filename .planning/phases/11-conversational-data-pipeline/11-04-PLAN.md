---
phase: 11-conversational-data-pipeline
plan: 04
type: execute
wave: 3
depends_on: ["11-01", "11-02", "11-03"]
files_modified:
  - scripts/prepare_dialog_corpus.py
  - results/inflation_report.md
autonomous: true
requirements: [DATA-02, DATA-03]
must_haves:
  truths:
    - "dialog_{train,val}.bin (uint16) and dialog_{train,val}_mask.bin (uint8) exist, are gitignored, and are 1:1 length-aligned per split"
    - "eos 8184 appears exactly once per dialogue (document separator only) — eos count equals episode count per split"
    - "Bins are built ONLY after the recorded D-09 verdict, with the D-07 persona budget from the checkpoint applied"
    - "A masked batch drawn from the real bins via get_batch_memmap_masked contains -100 sentinels in y (end-to-end smoke)"
  artifacts:
    - path: "scripts/prepare_dialog_corpus.py"
      provides: "run-once parse -> encode_dialogue -> 4 bins (DATA-02/DATA-03), mirroring encode_corpus.py"
      contains: "encode_dialogue"
    - path: "results/inflation_report.md"
      provides: "appended ## Corpus Build section — final token counts + masked fraction (Phase 15 evidence)"
      contains: "## Corpus Build"
  key_links:
    - from: "scripts/prepare_dialog_corpus.py"
      to: "src/personacore/dialogue/serialize.py"
      via: "the SAME encode_dialogue the gate measured (Pitfall 4)"
      pattern: "encode_dialogue"
    - from: "scripts/prepare_dialog_corpus.py"
      to: "src/personacore/training/data.py"
      via: "post-build smoke draw through get_batch_memmap_masked"
      pattern: "get_batch_memmap_masked"
---

<objective>
Harden the fine-tune format per the recorded gate verdict and build the four training bins:
`data/dialog_{train,val}.bin` (uint16 tokens) + `data/dialog_{train,val}_mask.bin` (uint8),
honoring PersonaChat's native train/valid split at file level (no dialogue straddles the cut)
and shipping BOTH aligned bins so either Phase-12 arm (masked/unmasked, D-02) is ready.

Purpose: this is the phase's deliverable to Phase 12 — bins + masks that the gate already
priced. Runs ONLY after plan 11-03's checkpoint recorded a GO/ADAPT verdict.
Output: prepare script + 4 gitignored bins + build-stats section appended to the committed report.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-conversational-data-pipeline/11-CONTEXT.md
@.planning/phases/11-conversational-data-pipeline/11-RESEARCH.md
@.planning/phases/11-conversational-data-pipeline/11-PATTERNS.md

<interfaces>
From src/personacore/dialogue: parse_episodes, encode_dialogue (returns (ids, mask), eos NOT
appended by callers — confirm in serialize.py whether encode_dialogue already emits the final
eos with mask 1 per D-01; it does per plan 11-01).
From src/personacore/training/data.py: get_batch_memmap_masked(bin_path, mask_path,
batch_size, block_size, device).
From src/personacore/tokenizer.special: SPECIAL_TOKENS ("<|endoftext|>" -> 8184).
Verified counts: 8,939 train / 1,000 valid episodes.
Verdict + D-07 budget: read from results/inflation_report.md "## Verdict" (filled at 11-03's
checkpoint). If the verdict is STOP or the section still says PENDING, this plan MUST NOT run —
raise the blocker to the orchestrator.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: prepare_dialog_corpus.py — parse -> encode -> 4 bins with sanity block, then run it</name>
  <files>scripts/prepare_dialog_corpus.py</files>
  <read_first>
    - results/inflation_report.md (the "## Verdict" section — hard precondition; apply the recorded D-07 persona budget; abort if PENDING/STOP)
    - scripts/encode_corpus.py (the analog to mirror: thin no-CLI shell, shard-accumulate + tofile mechanics, post-build sanity block, run-once docstring)
    - src/personacore/dialogue/serialize.py (encode_dialogue signature + eos/mask semantics — the single tokenization path)
    - scripts/fetch_personachat.py (input paths under data/raw/personachat/)
    - src/personacore/training/data.py (get_batch_memmap_masked for the smoke draw)
  </read_first>
  <action>
    Create `scripts/prepare_dialog_corpus.py` mirroring encode_corpus.py's whole shape (thin
    no-CLI: _REPO_ROOT, UPPER_CASE path constants, main(), [prepare_dialog_corpus]-prefixed
    prints, no argparse; run-once docstring "Run manually AFTER scripts/fetch_personachat.py
    and AFTER the D-09 verdict is recorded — NOT part of automated verification"). Inputs:
    data/raw/personachat/{train,valid}_self_revised.txt. Outputs: data/dialog_train.bin +
    data/dialog_train_mask.bin (from train file), data/dialog_val.bin + data/dialog_val_mask.bin
    (from valid file) — file-level native split, so no dialogue straddles the cut by
    construction (Pattern 5). Preconditions as loud failures (FileNotFoundError with the remedy
    command for missing inputs; SystemExit if results/inflation_report.md's "## Verdict"
    section is missing, PENDING, or STOP — the gate governs this script). Apply the D-07
    persona budget recorded at the checkpoint (expected: keep all lines; if "cap at N", slice
    persona lists to N before encoding). Per split: parse_episodes -> for each episode
    encode_dialogue(tok, persona, turns) -> accumulate id shards as np.uint16 and mask shards
    as np.uint8 (identical shard/concatenate/tofile mechanics as encode_corpus.py) -> write
    both bins. Do NOT re-implement any tokenization/masking — encode_dialogue is the only path
    (Pitfall 4). Post-build sanity block per split, all as loud SystemExit (never bare assert):
    re-read both bins with np.fromfile; token length == mask length; eos-8184 count == episode
    count (8,939 train / 1,000 valid with keep-all — recompute expected from the parse if a
    persona cap changed nothing about episode counts, which it cannot); masked fraction =
    mask.mean() printed and hard-failed if outside 0.30-0.70 (Pitfall 14 warning: ~0%/~100%
    means the mask is wrong; expect ~45-55%); decode + print a 200-token prefix; end-to-end
    smoke: get_batch_memmap_masked(bin, mask_bin, batch_size=4, block_size=256, device="cpu")
    returns x/y of shape (4, 256), y contains -100, and x values are all &lt; 8192.

    Then RUN it: `.venv/bin/python scripts/prepare_dialog_corpus.py`. Confirm the four bins in
    data/ and that `git status --porcelain` shows nothing under data/.
  </action>
  <verify>
    <automated>.venv/bin/python scripts/prepare_dialog_corpus.py && test -s data/dialog_train.bin && test -s data/dialog_train_mask.bin && test -s data/dialog_val.bin && test -s data/dialog_val_mask.bin</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/prepare_dialog_corpus.py` imports encode_dialogue and parse_episodes from personacore.dialogue and contains no direct tok.encode call on rendered dialogue strings
    - Script source contains a verdict precondition referencing results/inflation_report.md and a SystemExit path for PENDING/STOP
    - Run output prints, per split: token count, eos count equal to 8939 (train) / 1000 (valid), masked fraction in [0.30, 0.70], and a decoded prefix containing "<|system|>"
    - All four data/dialog_* files exist and are non-empty; len(token bin) == len(mask bin) per split (printed and enforced)
    - The smoke draw line confirms y contains -100 (printed by the sanity block)
    - `git status --porcelain` shows no data/ entries
  </acceptance_criteria>
  <done>Four aligned bins exist locally, sanity-proven (alignment, eos-per-episode, masked fraction, end-to-end masked draw); script refuses to run without a recorded non-STOP verdict.</done>
</task>

<task type="auto">
  <name>Task 2: append Corpus Build evidence to the committed report + full-suite green</name>
  <files>results/inflation_report.md</files>
  <read_first>
    - results/inflation_report.md (current content — append, never rewrite the measured sections or the verdict)
    - Task 1's run output (the printed sanity numbers are the source values)
  </read_first>
  <action>
    Append a "## Corpus Build" section to results/inflation_report.md recording the final
    build against the verdict: per split — total tokens, eos/episode count, mask-bin length,
    masked-token fraction (4 decimal places), the applied D-07 persona budget, and the bin
    file names (noting bins are gitignored artifacts; the report is the committed evidence,
    same register as the Fisher N=2000 stats). Include the uint16/uint8 dtypes and
    block_size=256 context so Phase 12 and Phase 15 can read consumption parameters off one
    document. Then run the full suite and lint to close the phase's code surface.
  </action>
  <verify>
    <automated>make test</automated>
  </verify>
  <acceptance_criteria>
    - `results/inflation_report.md` contains "## Corpus Build" with per-split token counts, eos counts (8939/1000), and a masked fraction value; the pre-existing "## Verdict" section is unchanged
    - `make test` exits 0 (full suite green: v1.0 baseline + all Phase 11 tests)
    - `make lint` exits 0
  </acceptance_criteria>
  <done>Committed report carries measurement + verdict + build evidence end-to-end; full suite and lint green; phase deliverables complete for Phase 12.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| data/raw text -> parser | corpus text was checksum-gated at fetch (plan 11-03); treated as trusted-after-verification |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Tampering | corpus text consumed by prepare script | mitigate | input only ever comes from the sha256-verified extraction (T-11-01/02 in plan 11-03); parser hard-fails on malformed lines; sanity block cross-checks episode counts against verified constants |
| T-11-07 | Elevation (process) | bins built before the gate verdict | mitigate | SystemExit precondition on the report's Verdict section — verdict-before-format-hardening enforced in code, not convention |
| T-11-SC | Tampering | package installs | accept | no packages installed this phase |
</threat_model>

<verification>
- All four bins exist with printed sanity proofs (alignment, eos counts, masked fraction, smoke draw)
- `make test` and `make lint` green
- Report's Corpus Build section committed
</verification>

<success_criteria>
- Phase 12 can draw masked batches from real dialogue bins with one import and two paths
- eos 8184 is a document separator only (exactly one per dialogue — count-proven)
- Both bins shipped aligned so the D-02 masked-vs-unmasked calibration has both arms ready
</success_criteria>

<output>
Create `.planning/phases/11-conversational-data-pipeline/11-04-SUMMARY.md` when done.
</output>
