---
phase: 11-conversational-data-pipeline
plan: 03
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - scripts/fetch_personachat.py
  - src/personacore/dialogue/inflation.py
  - src/personacore/dialogue/__init__.py
  - scripts/measure_inflation.py
  - tests/test_dialogue_serialize.py
  - results/inflation_report.md
autonomous: false
requirements: [DATA-01, DATA-04]
must_haves:
  truths:
    - "personachat.tgz is fetched over HTTPS, sha256-verified against the pinned checksum BEFORE any extraction or parse, and only the two named members are extracted (no extractall)"
    - "The committed inflation report presents all four D-08 metrics with auditable denominators, the pre-registered D-09 bands, the TinyStories baseline, and a rendered verdict"
    - "The user's GO/ADAPT/STOP verdict is obtained at a blocking checkpoint BEFORE any bin is built, and recorded in the report"
    - "The D-05 endpoint substitution (S3 revised JSON 404 -> ParlAI tarball) is recorded in phase docs"
  artifacts:
    - path: "scripts/fetch_personachat.py"
      provides: "idempotent checksum-gated fetch + named-member extraction (DATA-01)"
      contains: "507cf8641d333240654798870ea584d854ab5261071c5e3521c20d8fa41d5622"
    - path: "src/personacore/dialogue/inflation.py"
      provides: "four D-08 metrics from one encode pass, each with its auditable denominator"
    - path: "scripts/measure_inflation.py"
      provides: "thin gate driver (D-10) rendering the D-09 verdict"
    - path: "results/inflation_report.md"
      provides: "COMMITTED evidence artifact — Phase 15 reads the tokenizer-tax number off it"
      contains: "tokens/word"
  key_links:
    - from: "scripts/measure_inflation.py"
      to: "src/personacore/dialogue/serialize.py"
      via: "the SAME encode_dialogue the bin-builder uses (Pitfall 4: gate and bins tokenize identically)"
      pattern: "encode_dialogue"
    - from: "scripts/fetch_personachat.py"
      to: "data/raw/"
      via: "gitignored precious raw cache (D-00 link-rot insurance)"
      pattern: "data.*raw"
---

<objective>
Acquire PersonaChat self_revised with a pinned checksum (DATA-01, D-05), run the committed
tokenizer-inflation measurement (DATA-04, D-08/D-10), and obtain the user's pre-registered
GO/ADAPT/STOP verdict (D-09, RELATIVE bands — baseline-corrected 2026-07-31) at a blocking
checkpoint — BEFORE any bin is built.

Under the corrected relative bands (GO ≤ 1.2× vs the TinyStories baseline measured in the same
run) the smoke signal is 1.135× → GO is the EXPECTED outcome; the checkpoint remains
first-class control flow — the user confirms the verdict on the full-corpus numbers.

Purpose: the phase's identity is verdict-before-format-hardening; plan 11-04 (bins) depends on
this plan's recorded verdict.
Output: fetch script + extracted corpus in data/raw/, inflation module + gate script, committed
results/inflation_report.md with the user's verdict. The D-07 persona cap is pinned at 140
tokens in CONTEXT.md (applied in 11-04 regardless of verdict — not decided at this checkpoint).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-conversational-data-pipeline/11-CONTEXT.md
@.planning/phases/11-conversational-data-pipeline/11-RESEARCH.md
@.planning/phases/11-conversational-data-pipeline/11-PATTERNS.md

<interfaces>
From src/personacore/dialogue (plan 11-01): `parse_episodes(path)` -> list of
(persona: list[str], turns: list[(user, assistant)]); `detokenize(text)` -> str;
`encode_dialogue(tok, persona, turns)` -> (ids, mask), applies detokenize internally.

From src/personacore/tokenizer: `from_json("artifacts/tokenizer.json")` -> frozen tokenizer.

Pinned facts (RESEARCH, verified 2026-07-31):
URL https://dl.fbaipublicfiles.com/parlai/personachat/personachat.tgz — 223,221,886 B,
sha256 507cf8641d333240654798870ea584d854ab5261071c5e3521c20d8fa41d5622.
Members to extract: personachat/train_self_revised.txt, personachat/valid_self_revised.txt.
Expected episode counts: 8,939 train / 1,000 valid.
Fallback (only if fetch/checksum fails): https://s3.amazonaws.com/datasets.huggingface.co/personachat/personachat_self_original.json (pin its sha256 on first successful fetch; record substitution).
Baseline context for the verdict: TinyStories tokens/word 2.864 (same frozen tokenizer);
relative dialogue inflation ~1.135x; block_size = 256 (ModelConfig, config.py:88).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: fetch_personachat.py — checksum-gated fetch + named-member extraction, then run it</name>
  <files>scripts/fetch_personachat.py</files>
  <read_first>
    - .planning/phases/11-conversational-data-pipeline/11-RESEARCH.md (Pattern 1 — fetch body; Verified Endpoints table; Security Domain)
    - scripts/estimate_fisher_tinystories.py (thin-script shell: _REPO_ROOT, UPPER_CASE constants, loud SystemExit/FileNotFoundError failures — never bare assert; refuse-to-clobber posture)
    - scripts/encode_corpus.py (run-once docstring discipline: "Run manually", "NOT part of automated verification")
    - .gitignore (confirm data/ is ignored — it is; no edit expected)
  </read_first>
  <action>
    Create `scripts/fetch_personachat.py` as a thin no-CLI script (house pattern: _REPO_ROOT =
    pathlib.Path(__file__).resolve().parent.parent, UPPER_CASE constants with one-line comments,
    main(), [fetch_personachat]-prefixed prints, no argparse). Constants: URL and SHA256 exactly
    as pinned in the interfaces block above (sha256 507cf864...d5622, pinned 2026-07-31);
    MEMBERS = the two named self_revised txt paths; DEST_DIR = _REPO_ROOT / "data" / "raw".
    Behavior, stdlib only (urllib.request, hashlib, tarfile, pathlib): (1) idempotent — if the
    tarball exists, skip download but ALWAYS re-verify sha256 (precious-artifact discipline,
    Pitfall 6); (2) hard-fail on checksum mismatch with raise SystemExit naming both digests —
    verification happens BEFORE any tarfile.open (V5 control: never parse unverified bytes);
    (3) extract ONLY the two named members via tf.extract per member — never extractall
    (path-traversal guard); (4) skip extraction if both txt files already exist; (5) print final
    paths + line counts. Module docstring: DATA-01, D-00 (precious sole-corpus cache — never
    re-host, no explicit license), D-05 disposition (S3 revised JSON 404s — ParlAI tarball is
    the primary; S3 personachat_self_original.json is the pre-registered fallback, to be used
    only if this fetch fails, pinning its sha256 on first successful fetch and recording the
    substitution in the SUMMARY).

    Then RUN it in the .venv: `.venv/bin/python scripts/fetch_personachat.py` (~223 MB one-time
    download). Confirm both txt files land under data/raw/personachat/ and that
    `git status --porcelain` shows no data/ entries (gitignored).
  </action>
  <verify>
    <automated>.venv/bin/python scripts/fetch_personachat.py && test -s data/raw/personachat/train_self_revised.txt && test -s data/raw/personachat/valid_self_revised.txt</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/fetch_personachat.py` contains the literal sha256 `507cf8641d333240654798870ea584d854ab5261071c5e3521c20d8fa41d5622` and contains `tf.extract` (or `.extract(`) but NOT `extractall`
    - Script contains no `import requests` / no HF imports (stdlib fetch discipline) and no bare top-level `assert` for the checksum (SystemExit/exception instead)
    - Running the script twice in a row succeeds both times (idempotent) and prints a checksum-verified message
    - `data/raw/personachat/train_self_revised.txt` and `valid_self_revised.txt` exist and are non-empty; `git status --porcelain` shows nothing under data/
  </acceptance_criteria>
  <done>Corpus fetched, checksum-verified, two members extracted to gitignored data/raw/; script is idempotent and re-verifies on every run.</done>
</task>

<task type="auto">
  <name>Task 2: inflation.py (four D-08 metrics) + measure_inflation.py gate + committed report</name>
  <files>src/personacore/dialogue/inflation.py, src/personacore/dialogue/__init__.py, scripts/measure_inflation.py, tests/test_dialogue_serialize.py, results/inflation_report.md</files>
  <read_first>
    - src/personacore/dialogue/serialize.py and parse.py (plan 11-01 output — the functions this module composes; encode_dialogue is the ONLY tokenization path, Pitfall 4)
    - src/personacore/evaluation/perplexity.py (metrics-module analog: pure function, docstring pinning accounting invariants, value returned WITH its auditable denominator)
    - scripts/estimate_fisher_tinystories.py (gate-script analog: precondition FileNotFoundError with remedy command; report-don't-gate — numbers PRINTED as evidence vs pre-registered bands, not asserted)
    - results/results.md (committed-report analog: framing blockquote, metric table with auditable denominators, ## Notes)
    - tests/test_dialogue_serialize.py (the file being extended with -k inflation tests)
  </read_first>
  <action>
    Create `src/personacore/dialogue/inflation.py`: one pure function
    `compute_inflation_metrics(episodes, tok, block_size)` doing ONE encode pass per episode via
    `encode_dialogue` (never a second tokenization path) and returning a dict of the four D-08
    metrics, each with its auditable denominator (perplexity.py posture): (1) tokens_per_word —
    utterance-span token count over whitespace word count of the detokenized utterance text,
    with total_tokens and total_words both returned; (2) persona_cost — p50/p90/max of the
    persona-span token cost per episode (system token + persona ids), with episode count;
    (3) fit_fraction — fraction of episodes where persona span + first user->assistant exchange
    (all ids incl. role tokens) &lt;= block_size, replacing DATA-04's literal %-over-block_size
    per D-08's window-utility rationale; (4) fragmentation — token counts for a fixed
    FRAGMENTATION_SAMPLES word list (include halloween, cheetah, remodel, mermaids, anchorage —
    the research samples — so numbers are comparable). numpy/stdlib only, no torch. Export from
    dialogue/__init__.py.

    Extend `tests/test_dialogue_serialize.py` with inflation tests (names containing
    "inflation" so `-k inflation` selects them): run compute_inflation_metrics on the committed
    fb fixture through the frozen tokenizer; assert the word-count denominator equals the
    hand-countable word total of the fixture's detokenized utterances; assert 1.0 &lt;
    tokens_per_word &lt; 10, 0.0 &lt;= fit_fraction &lt;= 1.0, persona p50 &gt;= 1; assert two
    calls return identical dicts (determinism).

    Create `scripts/measure_inflation.py`: thin no-CLI driver (D-10; estimate_fisher shell —
    _REPO_ROOT, BLOCK_SIZE = 256 constant commented "ModelConfig.block_size", precondition
    FileNotFoundError telling the user to run scripts/fetch_personachat.py first). Parse BOTH
    train and valid self_revised files, compute metrics on the full corpus (train+valid
    reported separately or combined — report must say which), ALSO recompute the TinyStories
    baseline tokens/word from data/TinyStoriesV2-GPT4-valid.txt with the same tokenizer and
    word-count rule (file verified present; if missing, cite 2.864 from research and say so in
    the report), and write `results/inflation_report.md`. Report format (results.md analog):
    framing blockquote stating what the numbers are and are not comparable to; metric table
    with auditable denominators ("3.2xx (over N tokens / M words)"); the D-09 RELATIVE bands
    table verbatim (ratio = dialogue tokens/word / TinyStories tokens/word, same run, same
    tokenizer and word-count rule: GO &lt;=1.2x AND fit &gt;=90%; ADAPT 1.2-1.5x or 70-90%;
    STOP &gt;1.5x or &lt;70%), plus a one-line note recording the 2026-07-31 baseline
    correction that superseded the absolute bands (original GO ceiling 2.5 sat below the
    tokenizer's own TinyStories baseline 2.864); the rendered verdict per band (smoke signal:
    3.251/2.864 = 1.135x -> GO); the note that turn truncation (the remaining ADAPT lever) can
    move metric 3 but mathematically cannot move tokens/word (property of tokenizer x
    register), and that the D-07 persona cap (140 tokens, pinned in CONTEXT) applies in 11-04
    regardless of verdict; a
    "## Verdict" section left as "PENDING — user decision at checkpoint (D-09)". Script prints
    the same numbers with [measure_inflation] prefixes. Run it; commit the report.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_dialogue_serialize.py -k inflation -x -q && test -s results/inflation_report.md</automated>
  </verify>
  <acceptance_criteria>
    - `inflation.py` contains `def compute_inflation_metrics(` and imports encode_dialogue/detokenize from the dialogue package (single tokenization path)
    - `.venv/bin/python -m pytest tests/test_dialogue_serialize.py -k inflation -x -q` exits 0
    - `results/inflation_report.md` exists, is git-tracked, and contains: a tokens/word value with an explicit denominator, the strings "GO", "ADAPT", "STOP" (bands table), a TinyStories baseline number, a relative-inflation ratio, and a "## Verdict" section
    - `scripts/measure_inflation.py` raises FileNotFoundError naming `scripts/fetch_personachat.py` when data/raw is absent (precondition check present in source)
    - Full-corpus run printed episode counts matching 8,939 train / 1,000 valid
  </acceptance_criteria>
  <done>Committed inflation report exists with all four metrics, bands, baseline context, and a PENDING verdict slot; gate ran on the full corpus before any bin exists.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: D-09 gate verdict — user GO/ADAPT/STOP decision before any bin is built</name>
  <files>results/inflation_report.md</files>
  <action>
    Present results/inflation_report.md to the user with the decision context below and await
    the pre-registered D-09 verdict. On receipt: fill the report's "## Verdict" section with
    the user's decision verbatim (verdict + D-07 budget + date), commit it, and stop — plan
    11-04 reads that section as its hard precondition. Do NOT build any bin in this plan.
  </action>
  <what-built>
    The committed DATA-04 inflation measurement (results/inflation_report.md) on the full
    PersonaChat self_revised corpus with the frozen production tokenizer, judged against the
    RELATIVE D-09 bands (baseline-corrected 2026-07-31). The smoke signal is 1.135x (GO band:
    &lt;=1.2x), so GO is the expected outcome — but the verdict is rendered on the full-corpus
    ratio, not the smoke.
  </what-built>
  <how-to-verify>
    1. Open results/inflation_report.md and review the four metrics against the relative D-09
       bands (locked before this full-corpus measurement ran).
    2. Decision context (in the report):
       - Metric 1 as a ratio: full-corpus dialogue tokens/word divided by the TinyStories
         baseline recomputed in the same run (smoke: 3.251/2.864 = 1.135x -> GO). The bands
         were corrected 2026-07-31 because the original absolute GO ceiling (2.5) sat below
         the tokenizer's own home-corpus baseline (2.864) — the correction is recorded in the
         report and in CONTEXT D-09.
       - Metric 3 (persona + first exchange fits block_size=256) is ~99.8% — deep GO. This is
         the metric that actually determines training-window utility under the packed regime.
       - Metric 2 (persona-span cost): the D-07 cap of 140 tokens (pinned in CONTEXT, near the
         smoke p90 of 131) applies in 11-04 regardless of this verdict; sanity-check the
         full-corpus p90 is not materially above 131, else flag D-07 for re-litigation.
       - Turn truncation (the remaining ADAPT lever) shortens documents but mathematically
         CANNOT move the tokens/word ratio.
    3. Render your verdict per D-09: GO (proceed to bins as designed), ADAPT (apply turn
       truncation — only affects metric 3, already green), or STOP (ratio &gt;1.5x or fit
       &lt;70%: halt the phase; no bins are built; escalate scope).
  </how-to-verify>
  <resume-signal>
    Reply with your verdict: "GO", "ADAPT: &lt;lever&gt;", or "STOP". The executor then fills
    the report's "## Verdict" section with your decision verbatim + the pinned D-07 cap
    (140 tokens) + date, commits it, and plan 11-04 becomes unblocked (or the phase halts on
    STOP).
  </resume-signal>
  <verify>
    <human-check>User replied with GO / ADAPT:&lt;lever&gt; / STOP</human-check>
    <automated>grep -A3 "## Verdict" results/inflation_report.md | grep -v "^#" | grep -c -E "GO|ADAPT|STOP"</automated>
  </verify>
  <done>The report's "## Verdict" section no longer says PENDING; it records the user's verdict, the pinned D-07 persona cap (140 tokens), and the date, and the change is committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internet -> data/raw/ | untrusted ~223 MB archive from an unowned CDN crosses into the repo working tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | downloaded personachat.tgz | mitigate | sha256 verified against the pinned constant (507cf864...d5622) BEFORE tarfile.open; hard SystemExit on mismatch; HTTPS URL only; re-verified on every run |
| T-11-02 | Tampering | tar extraction paths | mitigate | extract ONLY the two named members (`tf.extract` per literal member name); extractall forbidden by acceptance criteria — path-traversal guard |
| T-11-05 | Spoofing/DoS | endpoint dead or moved (link rot) | transfer/accept | pre-registered fallback endpoint (S3 self_original JSON, re-verified live 2026-07-31) with pin-on-first-fetch; local data/raw cache treated as precious (D-00) |
| T-11-06 | Info Disclosure | re-hosting an unlicensed corpus | mitigate | data/ is gitignored (verified); fixture files are synthetic; acceptance criteria assert no data/ entries in git status |
| T-11-SC | Tampering | package installs | accept | no packages installed this phase (RESEARCH audit: none) |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_dialogue_serialize.py -k inflation -x -q` green; `make test` green; `make lint` green
- `results/inflation_report.md` committed with a filled "## Verdict" section after the checkpoint
- No data/ paths tracked by git
</verification>

<success_criteria>
- Corpus cached locally with pinned checksum; extraction is named-members-only
- All four D-08 metrics committed with auditable denominators + baseline context; verdict recorded BEFORE plan 11-04 runs
- D-05 endpoint substitution recorded (script docstring + SUMMARY)
</success_criteria>

<output>
Create `.planning/phases/11-conversational-data-pipeline/11-03-SUMMARY.md` when done —
record the user's verdict, the D-07 budget, and the D-05 endpoint-substitution note.
</output>
