---
phase: 08-demo-writeup
plan: 05
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - assets/demo.gif
autonomous: false
requirements: [DOC-01]
must_haves:
  truths:
    - "assets/demo.gif shows one complete story streaming start-to-EOS in the Gradio UI on laptop CPU, in light mode, ~10-15 s"
    - "The GIF is 720px wide, 12 fps, and well under 10 MB so the README renders snappily"
    - "The committed GIF is the README hero asset proving 'it works on a laptop' in motion (D-04)"
  artifacts:
    - path: "assets/demo.gif"
      provides: "README hero GIF (D-04) — live CPU streaming proof"
  key_links:
    - from: "assets/demo.gif"
      to: "scripts/demo_app.py"
      via: "screen capture of the running demo"
      pattern: "(binary asset — verified by size/dimension checks, not grep)"
---

<objective>
Produce the README hero asset (D-04): an animated GIF of the Gradio demo streaming a
TinyStories completion token-by-token on laptop CPU. Screen capture is inherently interactive
(human-action); the ffmpeg conversion is scripted. The ffmpeg binary arrives via
imageio-ffmpeg ([ASSUMED] package — install gated behind a blocking human checkpoint).

Purpose: the single most convincing "fully on-device" artifact, placed at the top of the
README in 08-06.
Output: committed `assets/demo.gif` per the 08-UI-SPEC Asset Contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-demo-writeup/08-RESEARCH.md
@.planning/phases/08-demo-writeup/08-UI-SPEC.md

<interfaces>
Asset Contract (08-UI-SPEC, locked):
  - File: assets/demo.gif, committed (assets/ is NOT gitignored — verified)
  - Content: one complete story streaming start -> EOS, LIGHT MODE, ~10-15 s
  - Dimensions: 720px wide (scale=720:-1), 12 fps, two-pass palette recipe
  - Size budget: well under 10 MB
  - Alt text (used by 08-06): "Gradio chat demo streaming a TinyStories completion
    token-by-token on a laptop CPU"

ffmpeg two-pass palette recipe (08-RESEARCH Pattern 5):
  FF=$(.venv/bin/python -c "import imageio_ffmpeg; print(imageio_ffmpeg.get_ffmpeg_exe())")
  "$FF" -i <recording.mov> -vf "fps=12,scale=720:-1:flags=lanczos,palettegen" /tmp/palette.png
  "$FF" -i <recording.mov> -i /tmp/palette.png -filter_complex "fps=12,scale=720:-1:flags=lanczos[x];[x][1:v]paletteuse" assets/demo.gif
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate — imageio-ffmpeg ([ASSUMED] install)</name>
  <files>(none — package-legitimacy checkpoint, no file changes)</files>
  <read_first>
    - .planning/phases/08-demo-writeup/08-RESEARCH.md § Package Legitimacy Audit (imageio-ffmpeg row: PyPI ~7 yrs, high downloads, github.com/imageio/imageio-ffmpeg; slopcheck unavailable -> human gate required)
  </read_first>
  <action>
    Pause BEFORE any pip install of imageio-ffmpeg. Present the verification steps below and
    wait for the resume signal. This gate is never auto-approvable. The signal also selects
    the ffmpeg source for Task 3 ("approved" = pip wheel, "brew" = brew/system binary,
    "rejected" = stop and surface to the developer).
  </action>
  <what-built>Nothing installed yet. GIF conversion needs an ffmpeg binary; neither ffmpeg nor gifski is on this machine. The plan uses imageio-ffmpeg 0.6.x — a pip wheel bundling a static ffmpeg binary, keeping everything inside the documented .venv workflow. It is [ASSUMED] in the audit (slopcheck unavailable), so the first install needs your approval.</what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/imageio-ffmpeg/ — confirm maintainer is the imageio org, source repo github.com/imageio/imageio-ffmpeg, recent 0.6.x release
    2. Exact-name check: "imageio-ffmpeg" (no typosquat variant)
    3. Alternative: if you prefer `brew install ffmpeg` or an existing ffmpeg on PATH, say so — the conversion command is identical with a different binary path
  </how-to-verify>
  <verify>
    <human-check>Developer confirmed the PyPI page (imageio org, exact name) and explicitly selected an ffmpeg source</human-check>
  </verify>
  <done>Resume signal received: "approved", "brew", or "rejected"</done>
  <resume-signal>Type "approved" (pip imageio-ffmpeg), "brew" (use a brew/system ffmpeg instead), or "rejected"</resume-signal>
  <acceptance_criteria>
    - Human approved one ffmpeg source before any install runs
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Record the streaming demo (~10-15 s screen capture)</name>
  <files>(none — produces an uncommitted .mov at a human-provided path)</files>
  <read_first>
    - .planning/phases/08-demo-writeup/08-UI-SPEC.md § Asset Contract + § Color "Capture contract" (light mode requirement)
  </read_first>
  <action>
    Pause and hand the recording steps below to the developer. Screen recording has no CLI
    path that can frame, time, and stop the capture meaningfully — this is a genuine
    human-only step (08-RESEARCH Open Question 2). Resume only when a recording path is
    provided; pass that path to Task 3.
  </action>
  <what-built>The demo (scripts/demo_app.py) is verified working offline from plan 08-02. The remaining input is a ~10-15 s screen recording of one story streaming.</what-built>
  <how-to-verify>
    1. Set macOS to LIGHT appearance (System Settings -> Appearance -> Light) — the GIF must match the white-background notebook figures (UI-SPEC capture contract)
    2. `source .venv/bin/activate && python scripts/demo_app.py`, open http://127.0.0.1:7860, size the browser window comfortably (the GIF is downscaled to 720px wide — a clean ~1280-1440px window works well)
    3. Start recording: QuickTime (File -> New Screen Recording, select the browser window region) or `screencapture -v /tmp/personacore_demo.mov` in another terminal
    4. Click the example "Once upon a time, there was a little dog named Max." and let ONE complete story stream start to finish (~2-3 s of streaming; capture a beat before and after) — total recording ~10-15 s
    5. Stop the recording and save it as /tmp/personacore_demo.mov (or tell me the actual path)
  </how-to-verify>
  <verify>
    <human-check>A .mov screen recording exists at the human-provided path showing one full story streaming in light mode</human-check>
  </verify>
  <done>Resume signal received with a valid recording path</done>
  <resume-signal>Type "recorded: /path/to/recording.mov" (e.g. "recorded: /tmp/personacore_demo.mov")</resume-signal>
  <acceptance_criteria>
    - A .mov screen recording exists at a human-provided path showing one full story streaming in light mode
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Install approved ffmpeg source + two-pass palette conversion -> assets/demo.gif</name>
  <files>assets/demo.gif</files>
  <read_first>
    - .planning/phases/08-demo-writeup/08-RESEARCH.md § Pattern 5 (the exact two-pass recipe and the keep-it-short guidance)
  </read_first>
  <action>
    If Task 1 resolved "approved": `.venv/bin/pip install imageio-ffmpeg` and resolve the
    binary via imageio_ffmpeg.get_ffmpeg_exe(); if "brew": use the brew/system ffmpeg path
    instead. Create assets/ (net-new directory; it is not gitignored). Run the two-pass
    palette conversion from the interfaces block against the human-provided recording path:
    pass 1 palettegen to /tmp/palette.png, pass 2 paletteuse to assets/demo.gif, both with
    fps=12 and scale=720:-1 lanczos. Check the result: width exactly 720px, duration roughly
    10-15 s, size under 10 MB (if over, retry with fps=10 and/or trim the input with
    -ss/-t before re-running both passes — tune at capture time per RESEARCH A5). Commit
    assets/demo.gif. Do not commit the .mov or the palette.
  </action>
  <verify>
    <automated>test -f assets/demo.gif && .venv/bin/python -c "import struct,os; h=open('assets/demo.gif','rb').read(10); assert h[:6] in (b'GIF87a', b'GIF89a'), h[:6]; w=struct.unpack('<H', h[6:8])[0]; s=os.path.getsize('assets/demo.gif'); assert w == 720, w; assert s < 10_000_000, s; print('gif OK', w, 'px', s, 'bytes')"</automated>
  </verify>
  <done>assets/demo.gif committed: valid GIF, 720px wide, under 10 MB</done>
  <acceptance_criteria>
    - assets/demo.gif exists, magic bytes GIF87a/GIF89a, logical-screen width 720, size < 10,000,000 bytes
    - `git check-ignore assets/demo.gif` exits non-zero and the file is staged/committed
    - No .mov or palette.png committed (`git ls-files | grep -E '\.(mov|png)$'` shows no new entries from this plan)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI -> .venv | First install of imageio-ffmpeg ([ASSUMED]) — ships a binary executable |
| screen recording -> public repo | The GIF frames everything visible in the captured region |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-SC | Tampering | pip install imageio-ffmpeg (bundles an ffmpeg binary) | mitigate | Blocking human checkpoint (Task 1) verifies the PyPI page/org before install; brew/system ffmpeg offered as alternative; checkpoint is never auto-approvable |
| T-08-05 | Information Disclosure | committed GIF content | mitigate | Capture is window-scoped to the localhost demo UI (instructions say record the browser window region, not the full desktop); reviewer sees only the demo. Human confirms before the README ships in 08-06 |
</threat_model>

<verification>
- GIF structural check (magic bytes, 720px width, < 10 MB) passes
- assets/demo.gif committed; no recording/palette artifacts committed
</verification>

<success_criteria>
- D-04 hero asset exists per the UI-SPEC Asset Contract, ready for README placement in 08-06
</success_criteria>

<output>
Create `.planning/phases/08-demo-writeup/08-05-SUMMARY.md` when done
</output>
