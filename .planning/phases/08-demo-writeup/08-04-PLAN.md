---
phase: 08-demo-writeup
plan: 04
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - docs/REPORT.md
autonomous: true
requirements: [DOC-01]
must_haves:
  truths:
    - "docs/REPORT.md exists and is organized around design DECISIONS (choice -> rationale -> validating evidence), not a chronological tutorial walkthrough"
    - "The report leads with the PersonaCore thesis and is explicit that M1 is the foundation and M2 delivers the weight-memory mechanism — it never overclaims"
    - "Every PPL citation carries its token denominator; the ablation table appears with its not-comparable-to-headline caveat verbatim; samples are labeled representative-not-cherry-picked"
    - "The M2 seams (named nn.Linear projections, assemble_loss, open-dict checkpoints) are documented as evidence the roadmap is real"
  artifacts:
    - path: "docs/REPORT.md"
      provides: "D-03 decision-driven technical narrative consolidating the document-as-we-go notes"
      contains: "2.1066"
      min_lines: 200
  key_links:
    - from: "docs/REPORT.md"
      to: "results/results.md"
      via: "ablation table + verbatim caveat"
      pattern: "NOT to the headline"
    - from: "docs/REPORT.md"
      to: "results/samples.md"
      via: "qualitative samples with honest selection note"
      pattern: "representative"
---

<objective>
Write `docs/REPORT.md` (DOC-01, the deep-dive half of D-01): the decision-driven technical
narrative consolidating six phases of document-as-we-go VERIFICATION notes plus the committed
results artifacts. Each section presents a design choice, the rationale, and the evidence that
validates it (a unit test, an ablation row, or a curve) — engineering judgment, not a nanoGPT
tutorial (D-03). Vision-led framing per D-02: thesis first, demonstrated depth second, honest
M1/M2 boundary always.

Purpose: the serious reviewer's click-through from the README (08-06 links here).
Output: `docs/REPORT.md` (net-new file and net-new docs/ directory).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-demo-writeup/08-RESEARCH.md
@.planning/phases/08-demo-writeup/08-PATTERNS.md

<interfaces>
<!-- Single-source headline numbers (committed artifacts — re-cite, never recompute): -->
  - Exact param count: 13,891,584 (~13.9M; tied embedding counted once)
  - Headline: deterministic full-val perplexity 2.1066 over 12,636,922 scored target tokens
    (50k-step best.pt, scripts/evaluate.py) — ALWAYS cited with the denominator
  - Measured demo throughput: ~95-105 tok/s sustained on laptop CPU (manual attn ~95, sdpa
    ~105; a ~200-token story streams in ~2 s) — from 08-RESEARCH § Empirical Results
  - Ablation cohort (results/results.md): reduced-budget self-consistent 4-run cohort
    (baseline / no_tie / no_pos / depth_cut, max_steps=2500, seed 1337) — caveat lines 3-10
    reproduced VERBATIM wherever the table appears
  - Architecture: vocab 8192 (locked), eos_id 8184, block_size 256, n_layer 6, n_head 6,
    n_embd 384, dropout 0.0, weight tying, learned positional embeddings
  - Slim artifact: ~55.6 MB torch.save dict, weights_only=True load, carries
    model_config + git_sha (3a46815…, step 49000)

<!-- DOC-01 source material (the document-as-we-go evidence base — note Phase 1 unnumbered,
     and NO Phase-5 VERIFICATION.md exists): -->
  .planning/phases/01-scaffolding-reproducible-environment/VERIFICATION.md
  .planning/phases/02-from-scratch-bpe-tokenizer/02-VERIFICATION.md
  .planning/phases/03-bigram-baseline-training-harness/03-VERIFICATION.md
  .planning/phases/04-gpt-transformer-decoder/04-VERIFICATION.md
  .planning/phases/06-generation-sampling/06-VERIFICATION.md
  .planning/phases/07-evaluation/07-VERIFICATION.md
  results/results.md, results/samples.md, logs -> results/run.csv (curve facts)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: REPORT part 1 — thesis framing + foundation/tokenizer/harness/model decision sections</name>
  <files>docs/REPORT.md</files>
  <read_first>
    - .planning/PROJECT.md (the thesis wording and Key Decisions table — D-02's vision framing source)
    - .planning/phases/01-scaffolding-reproducible-environment/VERIFICATION.md
    - .planning/phases/02-from-scratch-bpe-tokenizer/02-VERIFICATION.md
    - .planning/phases/03-bigram-baseline-training-harness/03-VERIFICATION.md
    - .planning/phases/04-gpt-transformer-decoder/04-VERIFICATION.md
    - .planning/phases/08-demo-writeup/08-PATTERNS.md § README.md + docs/REPORT.md (the rigor-signal prose register)
  </read_first>
  <action>
    Create docs/ and write the first half of docs/REPORT.md. Structure (D-03: every section is
    Decision -> Rationale -> Evidence; cite the concrete test file / number that validates it):
    - Title + thesis lead (D-02): PersonaCore's claim — memory lives in the weights, privacy
      by design; Milestone 1 (this report) is the rigorously-built from-scratch foundation;
      Milestone 2 adds the weight-memory mechanism (LoRA + EWC) on seams that already exist.
      State the honesty bar explicitly: nothing here claims chat tuning or personalization yet.
    - "What was built" overview: ~13.9M-param GPT decoder, from-scratch BPE tokenizer,
      hand-rolled training loop, shared generate(), offline CPU demo — pure PyTorch, zero HF
      model code, zero budget, trained on-device (M3/MPS, fp32).
    - Decision sections (part 1), each with its evidence pointer:
      (a) Byte-level BPE from scratch, vocab_size=8192 locked BEFORE model sizing; atomic EOS
          8184; tiktoken used ONLY as a test oracle (evidence: round-trip + oracle tests,
          tests/test_tokenizer_*.py).
      (b) Bigram-baseline-first harness de-risking — prove train/checkpoint/sample loop before
          attention exists (evidence: overfit-a-single-batch gate, kill-and-resume trajectory
          equality within 1e-6).
      (c) Pre-norm decoder blocks, causal MHA masked before softmax scaled by 1/sqrt(d_head),
          GELU MLP, learned positional embeddings, final ln_f (evidence: causality
          perturbation test — future tokens cannot change earlier logits).
      (d) Manual attention implemented by hand WITH an sdpa equivalence path (evidence:
          tests/test_gpt_attention_equiv.py, atol 1e-5) — the "from scratch but verified
          against the primitive" posture.
      (e) Weight tying as a true shared tensor (evidence: data_ptr identity test + the no_tie
          ablation row showing the cost of untying).
      (f) GPT-2-style init including residual-scaled c_proj AND fc_out (evidence: per-tensor
          init-std check).
      (g) The M2 seams as M1 acceptance criteria: every adaptable projection a named
          nn.Linear, loss assembled via assemble_loss(..., extra_penalties=()), open-dict
          checkpoints — why these make LoRA/EWC additive, not a rewrite.
    Prose register: match the committed results.md tone — precise, numbers always qualified,
    no marketing language. Use ## headings per decision; ASCII apostrophes fine; no HTML.
  </action>
  <verify>
    <automated>test -f docs/REPORT.md && grep -c "^## " docs/REPORT.md | awk '{exit !($1 >= 6)}' && grep -Fq "Milestone 2" docs/REPORT.md && grep -Fq "data_ptr" docs/REPORT.md && grep -Fq "8192" docs/REPORT.md</automated>
  </verify>
  <done>Part-1 sections exist with decision/rationale/evidence structure and honest M1/M2 framing</done>
  <acceptance_criteria>
    - docs/REPORT.md exists with at least 6 `## ` sections
    - Contains `vocab` and `8192`, `assemble_loss`, `nn.Linear`, `data_ptr`, `Milestone 2`
    - Thesis paragraph appears before any architecture content
    - No section claims personalization/weight-memory as a working M1 feature
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: REPORT part 2 — training/evaluation/demo decisions, results, reproducibility, limitations + M2 roadmap</name>
  <files>docs/REPORT.md</files>
  <read_first>
    - docs/REPORT.md (the part-1 half just written — continue its heading style and register)
    - .planning/phases/06-generation-sampling/06-VERIFICATION.md
    - .planning/phases/07-evaluation/07-VERIFICATION.md
    - results/results.md (table + lines 3-10 caveat — copy VERBATIM)
    - results/samples.md (header phrasing + 1-2 short sample excerpts to quote)
    - .planning/phases/08-demo-writeup/08-RESEARCH.md § Empirical Results (the tok/s table and slim-checkpoint facts)
  </read_first>
  <action>
    Append the second half of docs/REPORT.md:
    - Decision sections (part 2):
      (h) fp32 on-device training on Apple Silicon (M3/MPS) as the PRIMARY run — thesis
          reinforcement (the model that claims on-device privacy was trained on-device), with
          the Kaggle P100 fp16-AMP path as documented fallback; resumable open-dict
          checkpoints surviving session kills (evidence: resume-curve test reproducing the
          loss curve row-for-row across a kill+resume).
      (i) AdamW + warmup/cosine LR (hand-rolled schedule), gradient clipping, gradient
          accumulation; offline CSV logging instead of SaaS dashboards (zero-budget/privacy
          posture; evidence: results/run.csv, 200 logged evals over 50k steps).
      (j) Deterministic full-val perplexity with an auditable denominator — perplexity()
          recomputes CE with reduction='sum' and returns (ppl, total_tokens) (evidence: the
          headline 2.1066 over 12,636,922 scored target tokens; brute-force oracle test).
      (k) The ablation cohort as the differentiator: reproduce the results/results.md table
          AND its caveat block verbatim; one paragraph interpreting each row (tying helps;
          positional embeddings matter; depth cut hurts) without overreaching.
      (l) One shared generate() (greedy/temperature/top-k/top-p, EOS stop, context crop)
          powering tests, notebook, and demo (evidence: determinism + EOS-stop unit tests).
      (m) The shippable artifact decision: slim torch.save dict loaded weights_only=True
          (never execute code on load — tokenizer.json precedent), config + git SHA traveling
          inside the file; ~55.6 MB; best.pt stays internal training state.
      (n) The demo decision: Gradio 5.50 ChatInterface, share=False/localhost, fully offline
          (analytics env-var kill, wheel-local fonts), story-completion framing, measured
          ~95-105 tok/s CPU streaming.
    - Results summary: param count 13,891,584; headline PPL with denominator; curve narrative
      from results/run.csv; 2-3 short quoted samples labeled with the samples.md
      "representative, not cherry-picked" note.
    - Reproducibility (QA-02): Python 3.11 venv discipline, seed_everything, git SHA + config
      embedded in every checkpoint (and in the slim artifact), RNG-state restore on resume.
    - Limitations + Milestone 2 roadmap (honest): TinyStories register, no dialogue tuning,
      no personalization yet; M2 = LoRA adapters on the named projections + EWC via
      assemble_loss + teach-then-recall demo. Optional one-line footnote that a
      strided/sliding-window PPL variant exists as future work (no new compute).
    - Close with a pointer to README.md (quickstart + demo) and demo.ipynb (executed
      evidence). Keep total length focused: ~250-400 lines.
  </action>
  <verify>
    <automated>grep -Fq "2.1066" docs/REPORT.md && grep -Fq "12,636,922" docs/REPORT.md && grep -Fq "13,891,584" docs/REPORT.md && grep -Fq "NOT to the headline" docs/REPORT.md && grep -Fq "weights_only=True" docs/REPORT.md && grep -Fiq "representative" docs/REPORT.md && grep -Fq "tok/s" docs/REPORT.md</automated>
  </verify>
  <done>REPORT complete: decisions a-n, results with rigor signals verbatim, reproducibility, honest limitations + M2 roadmap</done>
  <acceptance_criteria>
    - docs/REPORT.md contains `2.1066` AND `12,636,922` (PPL never appears without its denominator), `13,891,584`, `tok/s`, `weights_only=True`
    - The results.md caveat phrase `NOT to the headline` appears verbatim alongside the ablation table
    - A Limitations/Milestone-2 section exists and labels personalization as upcoming
    - Total `## ` section count >= 10; file >= 200 lines
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo docs -> public readers | The report is world-readable portfolio prose |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | Information Disclosure | docs/REPORT.md content | mitigate | Only committed-artifact numbers and decision narrative; no local paths outside the repo, no tokens, no machine identifiers beyond "Apple Silicon M3" |
| T-08-06 | Repudiation (trust/overclaim) | report claims vs running artifact | mitigate | D-02 honesty bar enforced by acceptance criteria: M2 features always labeled upcoming; PPL always with denominator; ablation caveat verbatim (Pitfall 9) |
</threat_model>

<verification>
- All acceptance greps pass on docs/REPORT.md
- Markdown renders (no broken tables) — spot-check via reading the final file
</verification>

<success_criteria>
- DOC-01 (report half) satisfied: a polished decision-driven technical writeup consolidating the document-as-we-go notes per D-01/D-02/D-03
- Rigor signals carried verbatim throughout
</success_criteria>

<output>
Create `.planning/phases/08-demo-writeup/08-04-SUMMARY.md` when done
</output>
