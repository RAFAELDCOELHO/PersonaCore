---
phase: 08-demo-writeup
plan: 06
type: execute
wave: 4
depends_on: ["08-03", "08-04", "08-05"]
files_modified:
  - README.md
autonomous: false
requirements: [DOC-01, QA-01]
must_haves:
  truths:
    - "README.md opens with the thesis line and the demo GIF, followed by the compact results block (13.9M params, PPL 2.1066 over 12,636,922 tokens, ~100 tok/s CPU) — motion + numbers above the fold (D-04)"
    - "A reviewer can follow the quickstart from a clean clone to a running demo (venv -> pip install -> obtain weights per the distribution decision -> python scripts/demo_app.py)"
    - "README links to docs/REPORT.md and demo.ipynb and never overclaims — M2 features are always labeled upcoming"
    - "The full per-component test suite runs green via pytest and reproducibility discipline holds (QA-01/QA-02 phase gate)"
    - "The slim weights are obtainable: published as a GitHub Release asset OR explicitly documented as regenerate-only (developer decision)"
  artifacts:
    - path: "README.md"
      provides: "D-01/D-04 front door: thesis, GIF, results block, quickstart, links"
      contains: "assets/demo.gif"
      min_lines: 60
  key_links:
    - from: "README.md"
      to: "assets/demo.gif"
      via: "hero image at top"
      pattern: "assets/demo\\.gif"
    - from: "README.md"
      to: "docs/REPORT.md"
      via: "deep-dive link"
      pattern: "docs/REPORT\\.md"
    - from: "README.md"
      to: "scripts/demo_app.py"
      via: "quickstart run command"
      pattern: "scripts/demo_app\\.py"
---

<objective>
Ship the front door and close Milestone 1: `README.md` per D-01/D-04 (GIF hero + compact
results + quickstart + REPORT link), the weights-distribution decision implemented (GitHub
Release asset vs regenerate-only — developer's call, flagged in 08-RESEARCH Open Q1), and the
final QA-01/QA-02 phase gate proving the full per-component suite green and the rigor signals
intact across every shipped artifact.

Purpose: DOC-01 (README half) + QA-01 consolidation — the last plan of Milestone 1.
Output: `README.md`, the distribution channel (Release asset if approved), and a green
phase-gate run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-demo-writeup/08-RESEARCH.md
@.planning/phases/08-demo-writeup/08-PATTERNS.md
@.planning/phases/08-demo-writeup/08-UI-SPEC.md

<interfaces>
Headline numbers (single-source, committed):
  13,891,584 params (~13.9M) · full-val PPL 2.1066 over 12,636,922 tokens · ~95-105 tok/s
  laptop-CPU streaming · slim artifact ~55.6 MB · trained on-device (M3/MPS, fp32)

Quickstart facts:
  python3.11 -m venv .venv && source .venv/bin/activate
  pip install -e ".[cpu,demo]" --extra-index-url https://download.pytorch.org/whl/cpu
  Weights: download Release asset -> checkpoints/model_slim.pt  (if Release chosen)
           OR regenerate from a local best.pt: python scripts/export_slim.py
  Run: python scripts/demo_app.py  ->  http://127.0.0.1:7860
  Tests: make test   (CPU-only suite)

GIF alt text (08-UI-SPEC Asset Contract, exact):
  "Gradio chat demo streaming a TinyStories completion token-by-token on a laptop CPU"

QA-01 baseline: 122 passed / 1 skipped pre-phase; this phase added
  tests/test_slim_checkpoint.py (4) and tests/test_demo_callback.py (4) -> expect ~130 passed,
  1 skipped (CUDA fp16 smoke) locally with the real slim artifact present.

gh CLI is authenticated and working (verified in 08-RESEARCH Environment Availability).
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Weights distribution — GitHub Release asset or regenerate-only?</name>
  <files>(none — decision checkpoint, no file changes)</files>
  <read_first>
    - .planning/phases/08-demo-writeup/08-RESEARCH.md § Open Questions 1 and § Pitfall 3 (why the slim .pt cannot simply be committed)
  </read_first>
  <action>
    Pause and present the decision below. Do NOT run gh release commands before an explicit
    "release" selection — publishing the weights is the developer's repo-publishing call
    (T-08-08). Carry the selection into Task 2 (it changes both the publishing step and the
    README quickstart wording).
  </action>
  <decision>How reviewers obtain checkpoints/model_slim.pt (~55.6 MB; checkpoints/ and *.pt are deliberately gitignored)</decision>
  <context>The README quickstart wording depends on this. A Release asset lets any reviewer run the demo in minutes — the strongest portfolio outcome. Regenerate-only keeps the weights unpublished, but a reviewer without your local best.pt cannot run the demo without retraining (~days), which weakens the "working demo" claim. Both paths ship scripts/export_slim.py for regeneration.</context>
  <options>
    <option id="release">
      <name>GitHub Release asset (recommended)</name>
      <pros>Reviewers run the demo immediately; 55.6 MB is well within Release limits; clone stays slim; provenance (git SHA in the artifact) ties the weights to the exact commit</pros>
      <cons>Publishes the trained weights publicly on the repo now</cons>
    </option>
    <option id="regenerate-only">
      <name>Regenerate-only</name>
      <pros>Nothing published; full control over when/whether weights go public</pros>
      <cons>README quickstart cannot deliver a runnable demo to strangers; the hero GIF becomes the only proof</cons>
    </option>
  </options>
  <verify>
    <human-check>Developer explicitly selected "release" or "regenerate-only"</human-check>
  </verify>
  <done>Distribution channel selected via the resume signal</done>
  <resume-signal>Select: release or regenerate-only</resume-signal>
  <acceptance_criteria>
    - A distribution channel is explicitly chosen by the developer
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement distribution + write README.md (D-01/D-04 front door)</name>
  <files>README.md</files>
  <read_first>
    - docs/REPORT.md (link target — mirror its thesis wording so README and REPORT agree)
    - .planning/phases/08-demo-writeup/08-UI-SPEC.md § Copywriting Contract + § Asset Contract (CTA copy, alt text, results-block content, placement)
    - .planning/phases/08-demo-writeup/08-PATTERNS.md § README.md + docs/REPORT.md (rigor-signal phrasing, quickstart command precedent)
    - .planning/PROJECT.md (Core Value wording for the thesis line)
  </read_first>
  <action>
    If Task 1 selected "release": publish via gh — `gh release create m1-demo-v1
    checkpoints/model_slim.pt --title "PersonaCore Milestone 1 — slim inference checkpoint"
    --notes` with a short body citing git SHA 3a46815…, step 49000, 13,891,584 params, and the
    weights_only=True load instruction; verify with `gh release view m1-demo-v1 --json assets`
    that the asset uploaded. If "regenerate-only": skip publishing.

    Write README.md (net-new) in this order (D-04: motion + numbers above the fold):
    1. `# PersonaCore` + one-line thesis: a conversational AI where memory lives in the model
       weights — no databases, no vector stores, privacy by design. One sentence of honest
       scoping: this repo is Milestone 1, the from-scratch foundation; the weight-memory
       mechanism (LoRA + EWC) is Milestone 2, and its seams are already built in.
    2. The hero GIF immediately under the title:
       ![Gradio chat demo streaming a TinyStories completion token-by-token on a laptop CPU](assets/demo.gif)
    3. Compact results block (bullet or table): 13.9M params (13,891,584 exact, tied
       embedding counted once) · deterministic full-val perplexity 2.1066 over 12,636,922
       tokens · ~100 tok/s streaming on a laptop CPU (measured 95-105) · trained on-device on
       Apple Silicon (fp32/MPS) · 100% from-scratch PyTorch (no HF model code) · ablation
       one-liner (weight tying and positional embeddings both earn their parameters — full
       cohort + caveat in docs/REPORT.md).
    4. "What is this" section: 4-6 bullets — from-scratch byte-level BPE (vocab 8192),
       hand-built GPT decoder (pre-norm, causal MHA, weight tying), hand-rolled training loop
       (AdamW, warmup/cosine, resumable open-dict checkpoints), shared generate() sampler,
       per-component pytest suite, offline Gradio demo.
    5. "Run the demo" quickstart (UI-SPEC primary CTA): the exact interfaces-block commands —
       Python 3.11 venv, pip install -e ".[cpu,demo]" with the cpu extra-index, the weights
       step worded per the Task-1 decision (Release download to checkpoints/model_slim.pt
       with the gh/browser URL, or regenerate via python scripts/export_slim.py from a local
       best.pt), then python scripts/demo_app.py -> http://127.0.0.1:7860. Note: fully
       offline — works with Wi-Fi off.
    6. Evidence links: docs/REPORT.md (the decision-driven deep dive), demo.ipynb (executed
       results notebook rendered by GitHub), results/ (curves, ablations, samples).
    7. Tests + reproducibility: `make test` (CPU-only, ~70 s), seeds/git-SHA/config-in-
       checkpoint discipline one-liner.
    8. Roadmap: Milestone 2 preview (teach-then-recall, EWC no-forgetting) clearly labeled
       upcoming — never present tense (Pitfall 9).
    Keep it skimmable (~80-140 lines). No HTML styling, no badges that require network
    services, no orange-accent styling (UI-SPEC: nothing custom painted accent).
  </action>
  <verify>
    <automated>grep -Fq "assets/demo.gif" README.md && grep -Fq "docs/REPORT.md" README.md && grep -Fq "demo.ipynb" README.md && grep -Fq "scripts/demo_app.py" README.md && grep -Fq "2.1066" README.md && grep -Fq "12,636,922" README.md && grep -Fq "Milestone 2" README.md && grep -Fq 'pip install -e ".[cpu,demo]"' README.md</automated>
  </verify>
  <done>README front door complete and honest; weights obtainable per the chosen channel</done>
  <acceptance_criteria>
    - README.md contains the GIF reference with the exact UI-SPEC alt text, the results block with `2.1066` AND `12,636,922` adjacent, `13,891,584`, `tok/s`, links to docs/REPORT.md and demo.ipynb, and the quickstart commands
    - The GIF/markdown image appears BEFORE the quickstart section (above the fold)
    - If "release": `gh release view m1-demo-v1 --json assets` lists model_slim.pt; README contains the release download step. If "regenerate-only": README contains `scripts/export_slim.py` as the documented weights path and no dead release link
    - README contains no present-tense personalization/weight-memory claims (M2 items always carry an upcoming/Milestone-2 qualifier)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Final QA-01/QA-02 phase gate — suite green, artifacts consistent, repo hygiene</name>
  <files>(verification only — no new files; fixes applied in place if a check fails)</files>
  <read_first>
    - .planning/phases/08-demo-writeup/08-VALIDATION.md (the phase-gate definition this task executes)
  </read_first>
  <action>
    Run the consolidated Milestone-1 quality gate and fix anything red before declaring the
    phase done:
    (1) QA-01: `.venv/bin/python -m pytest -q` — full per-component suite (tokenizer, model,
    training, generation, evaluation, slim checkpoint, demo callback) green; expect ~130
    passed, exactly 1 skipped (CUDA fp16 smoke). (2) `make lint` exits 0. (3) Notebook
    integrity: demo.ipynb committed WITH outputs, execution counts 1..N, < 5 MB (re-run the
    08-03 nbformat check). (4) Repo hygiene: `git ls-files` contains no *.pt/*.bin/*.mov
    files and no logs/ entries; assets/demo.gif < 10 MB; no secrets patterns in newly
    committed files (grep tracked new files for "api_key", "token", "KAGGLE"). (5) Rigor-
    signal consistency: every file citing 2.1066 also contains 12,636,922 (README.md,
    docs/REPORT.md, demo.ipynb), and "NOT to the headline" appears in both docs/REPORT.md
    and demo.ipynb. (6) QA-02 spot-check: `.venv/bin/python -m pytest
    tests/test_slim_checkpoint.py tests/test_checkpoint.py tests/test_seeding.py -q` green
    (config-in-checkpoint + seeds + SHA discipline, including the shipped artifact).
    Record the gate results in the SUMMARY (counts, sizes, any fixes applied).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q && make lint && for f in README.md docs/REPORT.md demo.ipynb; do grep -Fq "2.1066" "$f" && grep -Fq "12,636,922" "$f" || exit 1; done && git ls-files | grep -E "\.(pt|bin|mov)$" | wc -l | grep -q "^ *0$" && echo GATE-GREEN</automated>
  </verify>
  <done>Phase gate green: full suite passes, artifacts consistent, hygiene clean — Milestone 1 deliverables complete</done>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest -q` exits 0 with >= 128 passed and exactly 1 skipped
    - `make lint` exits 0
    - `git ls-files | grep -E '\.(pt|bin|mov)$'` outputs nothing
    - README.md, docs/REPORT.md, and demo.ipynb each contain both `2.1066` and `12,636,922`
    - docs/REPORT.md and demo.ipynb each contain `NOT to the headline`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo/Release -> public internet | README, GIF, notebook outputs, and (optionally) the weights go public |
| gh CLI -> GitHub API | Release creation uses the developer's authenticated gh session |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | Information Disclosure | committed/published artifacts | mitigate | Task-3 hygiene gate: no *.pt/*.bin/*.mov/logs in git, secrets-pattern grep on new files; slim artifact key set already test-asserted to contain only tensors/config/SHA |
| T-08-07 | Tampering | published Release weights | mitigate | Release notes pin git SHA + step + param count; load instruction mandates weights_only=True; consumers verify via the committed test |
| T-08-06 | Repudiation (overclaim) | README claims vs running artifact | mitigate | Acceptance criteria forbid present-tense M2 claims; PPL-with-denominator grep enforced across all three artifacts (Pitfall 9) |
| T-08-08 | EoP | gh release create publishing without intent | mitigate | Blocking decision checkpoint (Task 1) — publishing only happens on an explicit "release" selection |
</threat_model>

<verification>
- All Task-3 gate commands green (suite, lint, consistency greps, hygiene)
- README renders with GIF at top; links resolve to existing files
- Distribution decision implemented exactly as selected
</verification>

<success_criteria>
- DOC-01 satisfied: README + REPORT consolidated, honest, evidence-linked (D-01..D-04 complete)
- QA-01 satisfied: full per-component suite green via pytest as a first-class deliverable
- QA-02 re-verified at phase close: config-in-checkpoint, fixed seeds, git SHA — including the shipped slim artifact
- Phase 8 / Milestone 1 deliverables complete: demo + notebook + tests + writeup all shipped
</success_criteria>

<output>
Create `.planning/phases/08-demo-writeup/08-06-SUMMARY.md` when done
</output>
