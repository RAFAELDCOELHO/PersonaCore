---
phase: 08-demo-writeup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_slim_checkpoint.py
  - src/personacore/checkpoint.py
  - scripts/export_slim.py
autonomous: true
requirements: [DEMO-02, QA-01, QA-02]
must_haves:
  truths:
    - "checkpoints/model_slim.pt exists locally, is ~56 MB (not the ~159 MB of best.pt), and contains no optimizer/scheduler/scaler/rng/train_config state"
    - "torch.load(checkpoints/model_slim.pt, weights_only=True) succeeds — the slim artifact loads under the restricted unpickler with zero code execution"
    - "A GPT rebuilt from the slim artifact's embedded model_config generates text on laptop CPU, and weight tying survives the round-trip (data_ptr identity)"
    - "The slim artifact carries model_config + git_sha + step (QA-02: config and provenance travel with the shipped weights)"
    - "tests/test_slim_checkpoint.py is green in .venv, and the real-artifact test SKIPS (not errors) when checkpoints/model_slim.pt is absent (CI-safe)"
  artifacts:
    - path: "scripts/export_slim.py"
      provides: "Thin no-CLI export driver: best.pt -> checkpoints/model_slim.pt (DEMO-02)"
      contains: "export_slim"
      min_lines: 25
    - path: "src/personacore/checkpoint.py"
      provides: "export_slim + load_slim helpers (logic in package, script wires)"
      exports: ["export_slim", "load_slim", "SLIM_SCHEMA_VERSION"]
    - path: "tests/test_slim_checkpoint.py"
      provides: "DEMO-02 mechanism tests (tiny GPT) + skipif-gated real-artifact test + QA-02 provenance asserts"
      contains: "weights_only=True"
      min_lines: 60
  key_links:
    - from: "scripts/export_slim.py"
      to: "src/personacore/checkpoint.py"
      via: "from personacore.checkpoint import export_slim"
      pattern: "from personacore.checkpoint import.*export_slim"
    - from: "src/personacore/checkpoint.py (load_slim)"
      to: "checkpoints/model_slim.pt"
      via: "torch.load with the restricted unpickler"
      pattern: "weights_only=True"
    - from: "tests/test_slim_checkpoint.py"
      to: "personacore.model.GPT"
      via: "GPT(ModelConfig(**loaded['model_config'])) reconstruction + data_ptr tying assert"
      pattern: "data_ptr\\(\\)"
---

<objective>
Ship THE shippable model artifact (DEMO-02): a slim fp32 inference checkpoint exported from
`checkpoints/best.pt` — state_dict + model_config + git_sha only, no optimizer/scheduler/RNG —
that loads under the locked `weights_only=True` safe-load bar and generates on laptop CPU.
This is the single artifact the Gradio demo (08-02), the notebook (08-03), and the offline test
all converge on (CONTEXT "One shippable artifact"). Also installs the `[demo]` extra
(gradio 5.50.0 + matplotlib — both pre-approved pyproject-pinned packages) so Wave-2 plans
share one environment with no parallel pip races.

Purpose: DEMO-02 + the slim half of QA-02 (config + SHA travel with the shipped weights).
Output: `tests/test_slim_checkpoint.py` (green), `export_slim`/`load_slim` in checkpoint.py,
`scripts/export_slim.py`, and the real local artifact `checkpoints/model_slim.pt` (gitignored;
distribution decided in 08-06).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-demo-writeup/08-RESEARCH.md
@.planning/phases/08-demo-writeup/08-PATTERNS.md
@.planning/phases/08-demo-writeup/08-VALIDATION.md

<interfaces>
<!-- Contracts the executor needs. Use directly — no codebase exploration required. -->

From src/personacore/checkpoint.py (existing API + schema):
  CKPT_SCHEMA_VERSION = 1                         # line 29
  load_checkpoint(path, map_location=...) uses torch.load(..., weights_only=False)  # lines 92-95
  Full-checkpoint open-dict keys (lines 57-77): schema_version, model, model_config,
  git_sha, step, val_loss, PLUS optimizer/scheduler/scaler/rng/train_config (the slim
  format DROPS these five).
  Module docstring (lines 16-18) already reserves: "The slim INFERENCE checkpoint
  (Phase 8) will use ``weights_only=True``." — cite it.

Slim dict format (08-RESEARCH Pattern 2, VERIFIED end-to-end on 2026-06-10):
  {"schema_version": 1, "model": <state_dict>, "model_config": <plain dict>,
   "git_sha": <str>, "step": <int>, "val_loss": <float>}
  torch.save of this dict from best.pt -> 55.6 MB (tied wte/lm_head storage stored once);
  torch.load(path, map_location="cpu", weights_only=True) succeeds.

From src/personacore/config.py (ModelConfig, lines 86-94):
  ModelConfig(vocab_size=8192, eos_id=8184, block_size=256, n_layer=6, n_head=6,
              n_embd=384, dropout=0.0, weight_tying=True, use_pos_emb=True)

From src/personacore/model/gpt.py:
  Tying happens in GPT.__init__ at line 184: self.lm_head.weight = self.wte.weight
  state_dict keys are wte.weight / lm_head.weight — NO "transformer." prefix.

From tests/test_gpt_weight_tying.py:15-19 (the exact tying assert to reuse):
  assert model.lm_head.weight.data_ptr() == model.wte.weight.data_ptr()

From tests/test_gpt_param_count.py:16-21 (dedup-by-data_ptr param counter to reuse):
  seen = {}; for p in model.parameters(): seen[p.data_ptr()] = p.numel(); sum(seen.values())

Real best.pt facts (verified): git_sha starts "3a46815", step 49000, val_loss ~0.7378,
param count 13,891,584. File is 166,808,536 bytes (~159 MiB).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install [demo] extra + Wave-0 RED test scaffold (tests/test_slim_checkpoint.py)</name>
  <files>tests/test_slim_checkpoint.py</files>
  <read_first>
    - src/personacore/checkpoint.py (current full-checkpoint key set, CKPT_SCHEMA_VERSION, the weights_only comment style — the slim helpers land here in Task 2)
    - tests/test_best_ckpt.py (blob-inspection-via-tmp_path idiom lines 90-95; frozen tokenizer load line 38; CPU_RUNTIME pin line 34)
    - tests/test_generation_text.py (module-docstring convention; how generation is invoked in tests)
    - tests/test_mps_smoke.py:29-32 (the suite's only skipif precedent — clean SKIP, not ERROR)
    - tests/test_gpt_weight_tying.py:15-19 and tests/test_gpt_param_count.py:16-21 (asserts to reuse verbatim)
    - .planning/phases/08-demo-writeup/08-PATTERNS.md § tests/test_slim_checkpoint.py (the 4-idiom composite recipe)
  </read_first>
  <behavior>
    - test_export_strips_training_state: build a tiny GPT(ModelConfig(block_size=32, n_layer=1, n_head=2, n_embd=16)) — vocab_size/eos_id stay at locked defaults (8192/8184) so the frozen tokenizer is compatible. Hand-build a fake FULL checkpoint dict {schema_version: 1, model: state_dict, model_config: dataclasses.asdict(cfg), optimizer: {"state": {}}, scheduler: {}, scaler: None, rng: {"python": 0}, train_config: {}, git_sha: "deadbeef0", step: 7, val_loss: 1.23}, torch.save to tmp_path/"full.pt". Call export_slim(full_path, slim_path). Load the result with raw torch.load(slim_path, map_location="cpu", weights_only=True) — this load succeeding IS the safe-load assertion. Assert the key set is exactly {"schema_version", "model", "model_config", "git_sha", "step", "val_loss"} and that "optimizer"/"scheduler"/"scaler"/"rng"/"train_config" are absent.
    - test_slim_loads_and_generates_cpu: from the slim file written above, load via load_slim(slim_path); rebuild GPT(ModelConfig(**loaded["model_config"])); model.load_state_dict(loaded["model"]); assert model.lm_head.weight.data_ptr() == model.wte.weight.data_ptr() (tying survives — MODEL-03 lineage); run a short greedy generation (frozen tokenizer from artifacts/tokenizer.json, max_new_tokens=8) and assert the output is a non-empty str and deterministic across two calls.
    - test_slim_carries_provenance: loaded["git_sha"] == "deadbeef0", loaded["step"] == 7, loaded["val_loss"] == pytest.approx(1.23), loaded["schema_version"] == 1 — QA-02: provenance + config travel with the shipped artifact.
    - test_real_slim_artifact_generates_on_cpu: gated with @pytest.mark.skipif(not pathlib.Path("checkpoints/model_slim.pt").exists(), reason="real slim artifact not present (CI)"). Loads the real artifact via load_slim, asserts dedup-by-data_ptr param count == 13_891_584, loaded["git_sha"].startswith("3a46815"), and a 20-token greedy generation returns a non-empty string.
  </behavior>
  <action>
    First, install the demo environment (pre-approved packages only — gradio>=5,&lt;6 and
    matplotlib~=3.10 are already pinned in pyproject [demo] and Approved in the 08-RESEARCH
    Package Legitimacy Audit; NO [ASSUMED] packages in this task): run
    `.venv/bin/pip install -e ".[cpu,demo]" --extra-index-url https://download.pytorch.org/whl/cpu`
    and confirm gradio resolves to 5.50.x. NEVER use the system Python 3.14 (CLAUDE.md hard rule).

    Then write tests/test_slim_checkpoint.py implementing the four behaviors above. Module
    docstring names DEMO-02 + QA-02, states "CPU-only, GPU/MPS-free", and cites the locked
    decision: slim inference artifact loads weights_only=True (Phase-1 01-02 decision;
    T-02-05/T-07-02 never-execute-code-on-load lineage). Imports `export_slim` and `load_slim`
    from personacore.checkpoint — these do not exist yet, so the file is RED (ImportError) by
    design; Task 2 turns it green. Gate ONLY the real-artifact test with skipif (the mechanism
    tests must always run in CI). Keep the whole file CPU-only — no device plumbing beyond
    map_location="cpu".
  </action>
  <verify>
    <automated>.venv/bin/python -c "import gradio, matplotlib; print(gradio.__version__, matplotlib.__version__)" prints 5.50.x; then .venv/bin/python -m pytest tests/test_slim_checkpoint.py -q — EXPECT RED (collection-time ImportError on export_slim/load_slim), exit code non-zero</automated>
  </verify>
  <done>gradio 5.50.x + matplotlib import cleanly from .venv; tests/test_slim_checkpoint.py exists with the four named tests and fails only because export_slim/load_slim are unimplemented</done>
  <acceptance_criteria>
    - `.venv/bin/python -c "import gradio; print(gradio.__version__)"` prints a 5.50.x version
    - tests/test_slim_checkpoint.py contains `def test_export_strips_training_state`, `def test_slim_loads_and_generates_cpu`, `def test_slim_carries_provenance`, `def test_real_slim_artifact_generates_on_cpu`
    - tests/test_slim_checkpoint.py contains `weights_only=True` and `skipif` and `data_ptr()`
    - `.venv/bin/python -m pytest tests/test_slim_checkpoint.py -q` exits non-zero (RED) with an import/name error referencing export_slim or load_slim
    - The rest of the suite is untouched: `.venv/bin/python -m pytest -q --ignore=tests/test_slim_checkpoint.py` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: export_slim/load_slim in checkpoint.py + thin scripts/export_slim.py — mechanism tests green</name>
  <files>src/personacore/checkpoint.py, scripts/export_slim.py</files>
  <read_first>
    - src/personacore/checkpoint.py (the file being modified — full-dict key names, CKPT_SCHEMA_VERSION, docstring lines 16-18, the justified weights_only=False comment at lines 92-95 whose style the export read must copy)
    - scripts/evaluate.py (the thin-script analog: decision-citing module docstring lines 1-28, _REPO_ROOT path constants lines 46-50, missing-input FileNotFoundError guard + main() shape lines 62-75 and 162-163; do NOT copy its preflight_device(strict=True) gate — it raises on CPU-only boxes)
    - tests/test_slim_checkpoint.py (the contract written in Task 1 — implement exactly what it asserts)
    - .planning/phases/08-demo-writeup/08-PATTERNS.md § scripts/export_slim.py
  </read_first>
  <action>
    In src/personacore/checkpoint.py, add (additively — do NOT change save_checkpoint/
    load_checkpoint or the resume format, per CONTEXT phase boundary):
    - `SLIM_SCHEMA_VERSION = 1` module constant.
    - `export_slim(full_path, slim_path, *, map_location="cpu") -> dict`: loads the full
      checkpoint with torch.load(..., weights_only=False) carrying a T-07-02-style justifying
      comment (own trusted best.pt ONLY — never a foreign file); builds the slim dict with
      exactly the keys schema_version (=SLIM_SCHEMA_VERSION), model (the "model" state_dict),
      model_config, git_sha, step, val_loss (coerced via float()); torch.save's it to
      slim_path and returns the dict. Dropped on purpose: optimizer, scheduler, scaler, rng,
      train_config.
    - `load_slim(path, *, map_location="cpu") -> dict`: torch.load(path,
      map_location=map_location, weights_only=True) — the locked safe-load bar (T-08-01) —
      then asserts loaded["schema_version"] == SLIM_SCHEMA_VERSION with a clear error message.
      Every slim consumer (demo 08-02, notebook 08-03, tests) goes through this one helper.

    Create scripts/export_slim.py following the evaluate.py thin-script shape: module
    docstring naming DEMO-02/QA-02, citing the checkpoint.py docstring reservation ("the slim
    INFERENCE checkpoint (Phase 8) uses weights_only=True") and carrying the SECURITY note
    about the trusted-own-file read; _REPO_ROOT-relative constants BEST_PATH =
    _REPO_ROOT / "checkpoints" / "best.pt" and SLIM_PATH = _REPO_ROOT / "checkpoints" /
    "model_slim.pt"; no argparse (Phase-1 D-04); main() raises FileNotFoundError("Missing
    {BEST_PATH}. Run `python scripts/pretrain_tinystories.py` first.") if best.pt is absent,
    calls export_slim(BEST_PATH, SLIM_PATH), then prints the slim path, file size in MB, key
    set, git_sha, and step; `if __name__ == "__main__": main()`. No accelerator/preflight gate.
    Both files must pass ruff (line-length 100, E/F/W/I).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_slim_checkpoint.py -q exits 0 (3 passed, 1 skipped — real artifact not yet exported); make lint exits 0</automated>
  </verify>
  <done>Mechanism tests green; real-artifact test skips cleanly; ruff clean</done>
  <acceptance_criteria>
    - src/personacore/checkpoint.py contains `SLIM_SCHEMA_VERSION = 1`, `def export_slim(` and `def load_slim(`
    - load_slim's torch.load call contains `weights_only=True`; export_slim's read of the full file contains `weights_only=False` with a trusted-own-file comment
    - scripts/export_slim.py contains `_REPO_ROOT`, `BEST_PATH`, `SLIM_PATH`, `def main() -> None:` and does NOT contain `argparse` or `preflight_device`
    - `.venv/bin/python -m pytest tests/test_slim_checkpoint.py -q` reports 3 passed, 1 skipped
    - `make lint` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Export the real artifact — checkpoints/model_slim.pt loads weights_only=True and generates</name>
  <files>checkpoints/model_slim.pt (generated local artifact — gitignored by design, distribution decided in 08-06)</files>
  <read_first>
    - scripts/export_slim.py (the script just written — run it, don't rewrite it)
    - .planning/phases/08-demo-writeup/08-RESEARCH.md § Pitfall 3 (the gitignore posture: do NOT fight `checkpoints/`/`*.pt` ignores; the artifact stays local until the 08-06 distribution decision)
  </read_first>
  <action>
    Run `.venv/bin/python scripts/export_slim.py` to produce checkpoints/model_slim.pt from
    the real best.pt (step 49000, SHA 3a46815…). Verify the file size lands in the 50-65 MB
    band (expected ~55.6 MB — far below best.pt's ~159 MB, proving training state was
    stripped). Do NOT git-add the artifact and do NOT edit .gitignore (Pitfall 3 — the
    "never commit checkpoints" posture is deliberate). With the artifact present, the
    previously-skipped real-artifact test must now RUN and PASS (param count 13,891,584,
    SHA prefix 3a46815, CPU generation). Finish with the full suite to prove zero
    regressions (QA-01 baseline was 122 passed / 1 skipped; expect +4 new passing, the CUDA
    smoke still skipping).
  </action>
  <verify>
    <automated>.venv/bin/python scripts/export_slim.py && .venv/bin/python -c "import os; s=os.path.getsize('checkpoints/model_slim.pt'); assert 50_000_000 < s < 65_000_000, s; print(s)" && .venv/bin/python -m pytest tests/test_slim_checkpoint.py -q && .venv/bin/python -m pytest -q</automated>
  </verify>
  <done>checkpoints/model_slim.pt exists at ~56 MB; all 4 slim tests pass (0 skipped in this file locally); full suite green</done>
  <acceptance_criteria>
    - checkpoints/model_slim.pt exists with size between 50,000,000 and 65,000,000 bytes
    - `.venv/bin/python -m pytest tests/test_slim_checkpoint.py -q` reports 4 passed, 0 skipped
    - `.venv/bin/python -m pytest -q` exits 0 with exactly 1 skipped (the CUDA fp16 smoke)
    - `git status --porcelain` shows NO checkpoints/ or *.pt entries (gitignore intact)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk -> torch.load (slim artifact) | The shipped file will be downloaded by strangers; it must never execute code on load |
| disk -> torch.load (best.pt) | Training checkpoint read during export — trusted own file only |
| PyPI -> .venv | pip install of the [demo] extra |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering/EoP | load_slim / every slim consumer | mitigate | torch.load(..., weights_only=True) (restricted unpickler — tensors + primitive containers only); the load succeeding under that flag is itself a test assertion; load_slim is the single choke point |
| T-08-02 | Tampering | export_slim's read of best.pt | accept | weights_only=False is required for the optimizer/RNG pickles, used ONLY on the project's own trusted best.pt with a justifying comment (T-07-02 lineage); never applied to foreign files |
| T-08-03 | Information Disclosure | slim artifact contents | mitigate | Key set asserted exactly {schema_version, model, model_config, git_sha, step, val_loss} — no paths, tokens, or env data can ride along |
| T-08-SC | Tampering | pip install ".[cpu,demo]" | mitigate | Only pyproject-pinned, audit-Approved packages installed (gradio, matplotlib — both ~6-20 yrs old, registry-verified, wheel-inspected in 08-RESEARCH); no [ASSUMED] package is installed in this plan |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_slim_checkpoint.py -q` — 4 passed locally (3 passed / 1 skipped on CI without the artifact)
- `.venv/bin/python -m pytest -q` — full suite green, exactly 1 skip (CUDA smoke)
- `make lint` exits 0
- checkpoints/model_slim.pt ~56 MB, loads via `torch.load(..., weights_only=True)`
</verification>

<success_criteria>
- DEMO-02 satisfied: slim fp32 inference checkpoint (no optimizer state) loads under weights_only=True and generates on laptop CPU, verified by an offline CPU-only test
- QA-02 (slim half): model_config + git_sha + step embedded in the shipped artifact and asserted by test
- Wave-2 environment ready: gradio 5.50.x + matplotlib installed in .venv
- Suite green: no regression to the 122-passed baseline
</success_criteria>

<output>
Create `.planning/phases/08-demo-writeup/08-01-SUMMARY.md` when done
</output>
