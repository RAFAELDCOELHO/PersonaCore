---
phase: 08-demo-writeup
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - pyproject.toml
  - results/run.csv
  - demo.ipynb
autonomous: false
requirements: [DEMO-03, QA-02]
must_haves:
  truths:
    - "demo.ipynb executes end-to-end on CPU in .venv via one nbconvert pass and is committed WITH outputs (GitHub renders curves/samples to reviewers who never execute it)"
    - "The notebook loads the slim checkpoint via the weights_only=True path and prints the exact param count 13,891,584 plus git SHA and step (QA-02 evidence)"
    - "Training/val curves are plotted from the COMMITTED results/run.csv (not the gitignored logs/run.csv), so a fresh clone can re-run the cell"
    - "The ablation cohort is plotted from results/abl_*.csv and the results.md table appears with its not-comparable-to-headline caveat VERBATIM"
    - "The sampling settings tour (greedy vs temperature vs top-k vs top-p) is seeded and reproducible on the pinned env"
  artifacts:
    - path: "results/run.csv"
      provides: "Committed copy of the 50k-step training curve (DEMO-03 data source, Pitfall 2 fix)"
      contains: "step,train_loss,val_loss,lr,tokens,wall_clock"
    - path: "demo.ipynb"
      provides: "D-05..D-08 results-showcase notebook, executed outputs committed"
      contains: "13,891,584"
    - path: "pyproject.toml"
      provides: "notebook extra pinning ipykernel/nbconvert for reproducible execution"
      contains: "notebook = ["
  key_links:
    - from: "demo.ipynb"
      to: "checkpoints/model_slim.pt"
      via: "load_slim (D-08: notebook, demo, and test converge on one artifact)"
      pattern: "load_slim"
    - from: "demo.ipynb"
      to: "results/run.csv"
      via: "csv.DictReader over the committed curve"
      pattern: "results/run.csv"
    - from: "demo.ipynb"
      to: "personacore.generation"
      via: "generate_text_str settings tour with seeded torch.Generator"
      pattern: "manual_seed"
---

<objective>
Ship `demo.ipynb` (DEMO-03): the focused evidence artifact — slim-checkpoint load with exact
param count and provenance, training/val curves from the CSV log, the headline PPL re-cited,
the ablation cohort plots + table with caveat, and a seeded sampling settings tour. Fix the
repo-hygiene trap first: `logs/run.csv` is gitignored, so the curve is committed as
`results/run.csv` (consistent with the committed `abl_*.csv`). Notebook tooling
(ipykernel/nbconvert) is [ASSUMED] in the package audit, so its install is gated behind a
blocking human checkpoint.

Purpose: DEMO-03 + the notebook half of QA-02 (SHA/config printed from the shipped artifact).
Output: committed `results/run.csv`, a `notebook` extra in pyproject, and `demo.ipynb`
committed WITH executed outputs (D-06).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-demo-writeup/08-RESEARCH.md
@.planning/phases/08-demo-writeup/08-PATTERNS.md
@.planning/phases/08-demo-writeup/08-UI-SPEC.md

<interfaces>
<!-- Contracts the executor needs. Use directly — no codebase exploration required. -->

From src/personacore/checkpoint.py (added by 08-01):
  load_slim(path, *, map_location="cpu") -> dict   # weights_only=True + schema assert
  Slim keys: schema_version, model, model_config, git_sha, step, val_loss
  Real artifact facts: param count 13,891,584; git_sha startswith "3a46815"; step 49000.

From src/personacore/generation (Phase 6):
  generate_text_str(model, tokenizer, prompt, *, max_new_tokens, **gen_kw) -> str
  gen_kw: temperature, top_k, top_p, greedy, generator (a torch.Generator threads through)

CSV schema (verified): logs/run.csv = 201 lines total (1 header + 200 data rows),
  header exactly: step,train_loss,val_loss,lr,tokens,wall_clock
Ablation CSVs (committed, same schema): results/abl_baseline.csv, abl_no_tie.csv,
  abl_no_pos.csv, abl_depth_cut.csv

Headline numbers (re-cite, NEVER recompute — CONTEXT rigor signal):
  full-val PPL 2.1066 over 12,636,922 scored target tokens (50k-step best.pt);
  the ablation cohort caveat lives at results/results.md lines 3-10 — copy VERBATIM.

Plot contract (08-UI-SPEC, locked): matplotlib default cycle only; train=C0/val=C1;
  ablations baseline=C0, no_tie=C1, no_pos=C2, depth_cut=C3 with legend;
  figsize=(8, 4.5), dpi=100 (keeps committed ipynb under ~5 MB); no rcParams overrides.

pyproject extras today (lines 15-18):
  cpu = ["torch==2.7.*"]
  demo = ["gradio>=5,<6", "matplotlib~=3.10"]
  dev = ["pytest~=9.0", "ruff~=0.15", "tiktoken~=0.13"]
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate — ipykernel + nbconvert ([ASSUMED] installs)</name>
  <files>(none — package-legitimacy checkpoint, no file changes)</files>
  <read_first>
    - .planning/phases/08-demo-writeup/08-RESEARCH.md § Package Legitimacy Audit (why these two are gated: slopcheck was unavailable; registry checks done read-only)
  </read_first>
  <action>
    Pause BEFORE any pip install of ipykernel or nbconvert. Present the verification steps
    below to the developer and wait for the resume signal. This gate is never
    auto-approvable. On "rejected", skip Task 2's install and execute the notebook
    interactively in an editor instead (weaker D-06 reproducibility — note it in the SUMMARY).
  </action>
  <what-built>Nothing installed yet. The notebook plan needs two new packages that are not in pyproject today: ipykernel 7.3.x (Jupyter kernel for executing demo.ipynb in .venv) and nbconvert 7.17.x (headless `--execute --inplace` producing the committed-with-outputs artifact, D-06). Both are Jupyter-organization packages, ~10 years old, very high downloads — but the slopcheck audit tool was unavailable at research time, so per protocol the first install requires your eyes.</what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/ipykernel/ — confirm maintainer is the Jupyter/IPython org, source repo github.com/ipython/ipykernel, recent 7.3.x release
    2. Open https://pypi.org/project/nbconvert/ — confirm maintainer is the Jupyter org, source repo github.com/jupyter/nbconvert, recent 7.17.x release
    3. Sanity: both names are exact (no typosquat: "ipykernel" not "ipy-kernel"; "nbconvert" not "nb-convert")
  </how-to-verify>
  <verify>
    <human-check>Developer confirmed both PyPI pages (Jupyter org, exact names) and explicitly approved the install</human-check>
  </verify>
  <done>Resume signal "approved" (or "rejected" with the interactive-execution fallback noted)</done>
  <resume-signal>Type "approved" to allow the install, or "rejected" (the fallback is interactive cell execution in an editor — weaker reproducibility for D-06)</resume-signal>
  <acceptance_criteria>
    - Human has confirmed both PyPI pages and approved (or explicitly rejected) the install
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: notebook extra in pyproject + install + commit results/run.csv</name>
  <files>pyproject.toml, results/run.csv</files>
  <read_first>
    - pyproject.toml (the file being modified — the [project.optional-dependencies] table at lines 15-18; match the existing compatible-release pin style exactly)
    - .planning/phases/08-demo-writeup/08-RESEARCH.md § Pitfall 2 (why logs/run.csv must be copied into results/)
  </read_first>
  <action>
    In pyproject.toml [project.optional-dependencies], add one line after the demo extra:
    notebook = ["ipykernel~=7.3", "nbconvert~=7.17"] — same ~= pin style as the dev extra.
    Install with `.venv/bin/pip install -e ".[notebook]"` (approval granted in Task 1; NEVER
    the system Python 3.14). Then fix Pitfall 2: copy logs/run.csv to results/run.csv
    byte-for-byte (cp, then cmp to prove identity) so the notebook's required data source is
    committed alongside the abl_*.csv precedent — do NOT edit .gitignore. results/run.csv
    must have exactly 201 lines with the header step,train_loss,val_loss,lr,tokens,wall_clock.
  </action>
  <verify>
    <automated>cmp logs/run.csv results/run.csv && test "$(wc -l < results/run.csv)" -eq 201 && head -1 results/run.csv | grep -F "step,train_loss,val_loss,lr,tokens,wall_clock" && .venv/bin/jupyter nbconvert --version && .venv/bin/python -c "import ipykernel; print(ipykernel.__version__)"</automated>
  </verify>
  <done>notebook extra installed in .venv; results/run.csv committed and identical to logs/run.csv</done>
  <acceptance_criteria>
    - pyproject.toml contains `notebook = ["ipykernel~=7.3", "nbconvert~=7.17"]`
    - `cmp logs/run.csv results/run.csv` exits 0; `wc -l < results/run.csv` prints 201
    - `.venv/bin/jupyter nbconvert --version` exits 0
    - `git check-ignore results/run.csv` exits non-zero (the copy is trackable)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Author demo.ipynb (D-05..D-08) + execute headlessly + commit with outputs</name>
  <files>demo.ipynb</files>
  <read_first>
    - results/results.md (the ablation table AND its lines 3-10 caveat block — the caveat is reproduced VERBATIM in a markdown cell)
    - results/samples.md (the "representative, not cherry-picked" header phrasing to carry)
    - .planning/phases/08-demo-writeup/08-RESEARCH.md § Pattern 4 (cell order) and § Code Examples (the csv.DictReader curve snippet) and § Pitfalls 7-8 (size/seeding)
    - .planning/phases/08-demo-writeup/08-UI-SPEC.md § Spacing/Typography/Color (the matplotlib contract: figsize=(8,4.5), dpi=100, default color cycle, fixed series-color mapping)
    - scripts/evaluate.py lines 133-143 (the generate_text_str greedy/warm invocation idiom the settings tour copies)
  </read_first>
  <action>
    Author demo.ipynb with this exact cell sequence (D-05; markdown cells carry the narrative,
    code cells stay thin — no training, no PPL recomputation):
    1. MARKDOWN intro: PersonaCore thesis (memory lives in the weights, privacy by design),
       honest M1 framing (this is the from-scratch foundation; LoRA+EWC weight-memory is
       Milestone 2 — D-02 no-overclaim), and the reproducibility preamble: outputs produced on
       Python 3.11 / torch 2.7.1 / CPU — identical seeds give identical text only on this
       pinned env (Pitfall 8).
    2. CODE load: from personacore.checkpoint import load_slim; ckpt =
       load_slim("checkpoints/model_slim.pt") (D-08 — the SAME artifact the demo and test
       use); rebuild GPT(ModelConfig(**ckpt["model_config"])), load_state_dict, eval(); print
       the dedup-by-data_ptr param count (must print 13,891,584), ckpt["git_sha"],
       ckpt["step"] (QA-02 evidence cell); load the frozen tokenizer via
       from_json("artifacts/tokenizer.json").
    3. CODE curves: csv.DictReader over results/run.csv (the COMMITTED copy — never
       logs/run.csv); two figures: loss vs step and loss vs tokens; train=C0, val=C1, legend,
       axis labels; figsize=(8, 4.5), dpi=100; no rcParams/style overrides.
    4. MARKDOWN headline: re-cite (do NOT recompute) the deterministic full-val perplexity
       2.1066 over 12,636,922 scored target tokens, produced by scripts/evaluate.py — always
       the PPL WITH its token denominator (Phase-7 rigor signal).
    5. CODE+MARKDOWN ablations: plot val_loss curves of the four results/abl_*.csv on one
       axes in the fixed order baseline=C0, no_tie=C1, no_pos=C2, depth_cut=C3 with legend;
       then a markdown cell reproducing the results/results.md cohort table AND its caveat
       block VERBATIM (the numbers are comparable to EACH OTHER, NOT to the 50k headline).
    6. CODE settings tour (D-07): one fixed prompt — "Once upon a time, there was a little
       dog named Max." — generated four ways via generate_text_str with max_new_tokens=120:
       (a) greedy=True; (b) temperature=0.8; (c) temperature=0.8, top_k=50;
       (d) temperature=0.8, top_p=0.95. Each non-greedy call passes
       generator=torch.Generator().manual_seed(1337) (fresh per call). Print each labeled
       output so the committed cell shows HOW each sampling choice changes the text.
    Then execute headlessly: `.venv/bin/jupyter nbconvert --to notebook --execute --inplace
    demo.ipynb` (kernelspec: the in-venv default python3 kernel; if kernel resolution fails,
    register once via `.venv/bin/python -m ipykernel install --user --name personacore` and
    set that kernelspec). Verify execution counts are monotonic 1..N and file size < 5 MB
    (Pitfall 7), then commit the notebook WITH outputs (D-06).
  </action>
  <verify>
    <automated>.venv/bin/jupyter nbconvert --to notebook --execute demo.ipynb --output /tmp/demo_reexec.ipynb (exit 0 — re-runnable end-to-end); .venv/bin/python -c "import json,os; nb=json.load(open('demo.ipynb')); cc=[c for c in nb['cells'] if c['cell_type']=='code']; ec=[c['execution_count'] for c in cc]; assert ec==list(range(1,len(cc)+1)), ec; assert any(c.get('outputs') for c in cc); assert os.path.getsize('demo.ipynb') < 5_000_000; print('outputs OK', len(cc), 'code cells')"</automated>
  </verify>
  <done>demo.ipynb committed with clean sequential executed outputs; re-executes end-to-end on CPU</done>
  <acceptance_criteria>
    - demo.ipynb contains the strings `load_slim`, `results/run.csv`, `manual_seed(1337)`, `13,891,584` (in an output or markdown), `2.1066`, `12,636,922`
    - demo.ipynb contains the verbatim caveat phrase `NOT to the headline` (from results/results.md)
    - demo.ipynb does NOT contain `logs/run.csv`, `best.pt`, or any training-loop import
    - Code-cell execution_count values are exactly 1..N monotonic; file size < 5 MB
    - `jupyter nbconvert --to notebook --execute` re-run exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI -> .venv | First install of ipykernel/nbconvert ([ASSUMED] packages) |
| disk -> torch.load | Slim checkpoint load inside the notebook |
| notebook outputs -> public repo | Executed outputs are committed and world-readable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-SC | Tampering | pip install ipykernel/nbconvert | mitigate | Blocking human checkpoint (Task 1) verifies both on pypi.org before install (slopcheck unavailable → [ASSUMED] per audit protocol); exact-name pins in a pyproject extra prevent drift |
| T-08-01 | Tampering/EoP | notebook checkpoint load | mitigate | load_slim only (weights_only=True, D-08); the notebook never touches best.pt |
| T-08-05 | Information Disclosure | committed executed outputs | mitigate | Cells print only param count/SHA/loss numbers/sample text from committed CSVs and the model — no env vars, paths outside repo, or tokens; acceptance grep forbids logs/ and best.pt references |
</threat_model>

<verification>
- `cmp logs/run.csv results/run.csv` exits 0
- nbconvert re-execution exits 0 on CPU in .venv
- Execution counts 1..N; ipynb < 5 MB; outputs present in the committed file
- Full suite still green after merge: `.venv/bin/python -m pytest -q`
</verification>

<success_criteria>
- DEMO-03 satisfied: demo.ipynb reads the (committed) CSV log, shows training curves, sampling, and the exact parameter count
- D-05..D-08 honored: results showcase, executed outputs committed, seeded settings tour, slim-checkpoint load
- QA-02 (notebook half): git SHA + step + config printed from the shipped artifact
- Rigor signals carried verbatim: PPL with denominator; ablation caveat; representative-not-cherry-picked
</success_criteria>

<output>
Create `.planning/phases/08-demo-writeup/08-03-SUMMARY.md` when done
</output>
