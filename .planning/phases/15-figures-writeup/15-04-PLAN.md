---
phase: 15-figures-writeup
plan: 04
type: execute
wave: 3
depends_on: ["15-02"]
files_modified:
  - results/phase13_ab_report.md
autonomous: true
requirements: [VIZ-03]
must_haves:
  truths:
    - "The Fisher/Δ correlation is computed with the pre-registered seed and constants, and the number appears nowhere in the repo before this commit"
    - "The verdict — pass or miss — is recorded unamended, adjacent to the Phase 13 data it is computed from"
    - "The appended section is explicitly dated and marked Phase 15 material, visibly separate from Phase 13's pre-registered content"
    - "A gate miss is reported in the 'suggestive but not statistically demonstrated at n = 36' register, neither discarded nor softened"
    - "Every pre-existing Phase 13 heading and its text is byte-unchanged"
  artifacts:
    - path: "results/phase13_ab_report.md"
      provides: "Phase 13 evidence plus the dated Phase 15 correlation addendum"
      contains: "Phase 15 Addendum"
  key_links:
    - from: "results/phase13_ab_report.md"
      to: "scripts/phase15_stats.py"
      via: "Evidence Index addendum row citing the pre-registration commit SHA"
      pattern: "phase15_stats"
    - from: "results/phase13_ab_report.md"
      to: "results/phase15_norms.json"
      via: "the artifact the correlation was computed from, cited by git_sha"
      pattern: "phase15_norms"
---

<objective>
Compute the pre-registered Fisher/Δ correlation from the committed artifact and append the
verdict — pass or miss — to `results/phase13_ab_report.md` as a dated, explicitly-marked Phase 15
section.

Purpose: D-17 puts the verdict immediately adjacent to the Fisher and delta data it is computed
from. D-09's pre-registration is already committed (Plan 15-01), so this is the moment the number
first exists. The outcome is genuinely open — research deliberately did not compute it — and the
D-11 miss policy is a first-class path, not an error branch.

Output: `results/phase13_ab_report.md` extended with
`## Phase 15 Addendum — Fisher/Δ Correlation Verdict (D-09/D-10/D-11)`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-figures-writeup/15-CONTEXT.md
@.planning/phases/15-figures-writeup/15-RESEARCH.md
@.planning/phases/15-figures-writeup/15-PATTERNS.md
@.planning/phases/15-figures-writeup/15-01-SUMMARY.md
@.planning/phases/15-figures-writeup/15-02-SUMMARY.md
</context>

<outcome_is_open>
**Plan for a MISS as readily as for a PASS.** Research deliberately did not compute this
correlation, so neither outcome is expected. If the gate misses:
- the result is recorded **unamended**, per Phase 14 D-12's verbatim gate-miss policy;
- it is reported in the **"suggestive but not statistically demonstrated at n = 36"** register —
  reported, **not discarded**, and **not softened into a passing verdict**;
- ROADMAP SC2's wording *"showing EWC visibly dodging high-Fisher coordinates"* **narrows
  accordingly**, exactly as a gate miss would in any other phase, and that narrowing is recorded
  in this plan's SUMMARY for Plan 15-05 and Plan 15-07 to carry into REPORT.md and ROADMAP.

Do **not** re-run with a different seed, a different aggregate, a different pairing, or a
different resample count to see if the number moves. Every one of those is pre-registered in
`scripts/phase15_stats.py` and committed before the artifact existed. Changing one after seeing
the result is the exact contamination D-09 exists to prevent.
</outcome_is_open>

<tasks>

<task type="auto">
  <name>Task 1: Compute the correlation and append the dated Phase 15 verdict section</name>
  <files>results/phase13_ab_report.md</files>
  <read_first>
    - `results/phase13_ab_report.md` — the whole file, and specifically: the heading inventory
      (`## Pre-Registration` 24, `## 2×2 Result` 48, `## Gate Verdict` 106, `## Threats to
      Validity` 171, `## Reconciliation: §8 Search vs Phase-13 Demonstration` 261, `## Figures`
      307, `## Retention Samples` 335, `## Evidence Index` 358); the `## Pre-Registration` table
      form to mirror; the verbatim-quote-with-attribution register at lines 261-267 (*"Phase 12
      §8 concluded, verbatim and unamended:"* followed by a blockquote); and the closing
      `## Evidence Index` table whose rows map artifact → role, including the pre-registration
      provenance row for `scripts/finetune_ab.py` @ `c3d942e`
    - `scripts/phase15_stats.py` — the committed pre-registration, `render_verdict_section`, and
      `main()`
    - `scripts/phase14_recall.py` lines 1590-1598 — `SHIP_DECISION_HEADER`'s literal
      separate/dated-after/does-not-reopen-or-amend wording register
    - `scripts/_verdict.py` — the anchored `VERDICT_SECTION` regex and the CR-02 failure it
      documents
    - `.planning/phases/15-figures-writeup/15-01-SUMMARY.md` — the pre-registration commit SHA
      to cite in the Evidence Index addendum row
    - `results/phase15_norms.json` — its `git_sha` and `built` fields, cited in the Result block
  </read_first>
  <action>
Run `.venv/bin/python scripts/phase15_stats.py`. It reads `results/phase15_norms.json`, builds
the 36 Fisher/reduction pairs in the pinned cell order, computes Spearman rho, the permutation p
at `N_PERM=100000` and the percentile bootstrap CI at `N_BOOT=10000` with `SEED=1337`, evaluates
`ewc_dodges_high_fisher`, and prints the complete markdown section.

**Append the printed section verbatim to the end of `results/phase13_ab_report.md`**, after the
`## Evidence Index` table. Do not hand-edit the numbers, do not reformat the tables, and do not
re-order the section — the renderer was committed before the number existed precisely so the
presentation could not be tuned to the result.

**Every pre-existing line of `results/phase13_ab_report.md` stays byte-unchanged.** This file is
committed Phase 13 evidence; this phase appends to it and nothing else. Verify with
`git diff` that the diff is purely additive at the end of the file — zero deletions, zero
modifications above the append point.

The appended section MUST carry (the renderer produces these; confirm they survived the append):
  - a horizontal rule and the heading
    `## Phase 15 Addendum — Fisher/Δ Correlation Verdict (D-09/D-10/D-11)`;
  - an HTML comment in `SHIP_DECISION_HEADER`'s register stating that this is **Phase 15
    material, dated after every Phase 13 result above, and does not reopen or amend Phase 13's
    pre-registered content** — the same separation Phase 14 D-12 requires between a verdict and
    anything written after it. Appending must not blend into or appear to amend Phase 13's
    recorded results;
  - the bold dated `**Recorded: YYYY-MM-DD**` line;
  - the `### Pre-Registration (locked before the artifact existed)` table carrying the statistic,
    `N_CELLS=36`, the pairing, `PREDICTED_SIGN=+1`, `SEED=1337`, `N_PERM=100000`,
    `N_BOOT=10000`, `CI_ALPHA=0.05`, the method strings, the gate rule, the R5 sentence naming
    the bootstrap CI as load-bearing and the permutation p as descriptive, and the
    percentile-bootstrap small-n bias note;
  - the `### Result` block with rho, the CI, the permutation p, the degenerate-resample count,
    and the artifact's `git_sha` and `built`;
  - the `### Verdict` line with the pass/miss determination and, on a miss, the D-11 policy text
    verbatim;
  - the `### Evidence Index Addendum` row citing `scripts/phase15_stats.py` @ the Plan 15-01
    pre-registration commit SHA (take it from `15-01-SUMMARY.md`) and `results/phase15_norms.json`
    @ its recorded `git_sha`.

**CR-02 constraint.** This section's prose quotes Phase 13 heading strings — `## Verdict`,
`## Gate Verdict` — inside its own text. Any guard or test that later reads a section of this
report MUST anchor on the SECTION via `scripts/_verdict.py::VERDICT_SECTION`, never on the last
occurrence of a heading substring. `scripts/phase14_recall.py:1601-1643` records exactly this bug
being found and fixed. Note the constraint in this plan's SUMMARY so Plan 15-08's D-17 test is
written section-anchored from the start.

**Record in the SUMMARY, verbatim:** rho, the CI bounds, the permutation p, the pass/miss
verdict, and — if it missed — the narrowed SC2 wording, which Plan 15-05 cites in REPORT.md and
Plan 15-07 records in ROADMAP.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import pathlib,re;t=pathlib.Path('results/phase13_ab_report.md').read_text();m=re.search(r'^## Phase 15 Addendum\b(.*?)(?=^## |\Z)',t,re.M|re.S);print(m is not None and 'Recorded:' in m.group(1) and ('GATE PASSES' in m.group(1) or 'GATE MISSES' in m.group(1)) and 'does not reopen or amend' in m.group(1))"</automated>
  </verify>
  <acceptance_criteria>
    - The command above prints `True`.
    - `git diff --numstat results/phase13_ab_report.md` shows **0 deleted lines** — the change is
      purely additive.
    - The addendum heading is the LAST `## ` heading in the file:
      `.venv/bin/python -c "import pathlib,re;h=re.findall(r'^## .*', pathlib.Path('results/phase13_ab_report.md').read_text(), re.M);print(h[-1].startswith('## Phase 15 Addendum'))"` prints `True`.
    - The recorded date matches today:
      `grep -qF \"Recorded: $(date -u +%Y-%m-%d)\" results/phase13_ab_report.md` succeeds.
    - `grep -qF 'phase15_stats.py' results/phase13_ab_report.md` and
      `grep -qF 'phase15_norms.json' results/phase13_ab_report.md` both succeed.
    - If the gate MISSED:
      `grep -qF 'suggestive but not statistically demonstrated at n = 36' results/phase13_ab_report.md`
      succeeds, and the SUMMARY records the narrowed SC2 wording.
    - `.venv/bin/pytest -q` reports no regression (≥ 398 passed, 1 skipped — Plan 15-03 runs in
      parallel and adds its own).
  </acceptance_criteria>
  <done>The correlation exists for the first time, is recorded unamended in the register the renderer fixed in advance, and Phase 13's own text is byte-untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run reproducibility check on the recorded verdict</name>
  <files>results/phase13_ab_report.md</files>
  <read_first>
    - `results/phase13_ab_report.md` — the appended `## Phase 15 Addendum` section
    - `scripts/phase15_stats.py` — `SEED`, `N_PERM`, `N_BOOT`
    - `.planning/phases/15-figures-writeup/15-CONTEXT.md` D-12 — *"the gate verdict is
      reproducible byte-for-byte from the committed artifact, not dependent on whatever shuffle
      order a given run happens to draw"*
  </read_first>
  <action>
Re-run `.venv/bin/python scripts/phase15_stats.py` in a fresh process and diff its output against
the section now committed in `results/phase13_ab_report.md`. The rho, the permutation p, the CI
bounds and the degenerate-resample count MUST all match exactly — D-12 requires the verdict be
reproducible byte-for-byte from the committed artifact, not dependent on the shuffle order a
given run happens to draw. The `**Recorded:**` date line is the only field permitted to differ,
and only if the re-run crosses a UTC midnight.

If any statistic differs, the seeding is broken — stop and report it as a finding. Do **not**
pick whichever run produced the more favorable number.

Record the reproduction result in the SUMMARY: the two runs' rho/p/CI values side by side, and
an explicit statement that they matched.
  </action>
  <verify>
    <automated>.venv/bin/python -c "
import importlib.util, pathlib, json, re
s = importlib.util.spec_from_file_location('m', pathlib.Path('scripts/phase15_stats.py'))
m = importlib.util.module_from_spec(s); s.loader.exec_module(m)
art = json.loads(pathlib.Path('results/phase15_norms.json').read_text())
a, b = m.load_pairs(art)
rho, p = m.permutation_p(a, b, n_perm=m.N_PERM, seed=m.SEED)
lo, hi, nd = m.bootstrap_ci(a, b, n_boot=m.N_BOOT, seed=m.SEED)
txt = pathlib.Path('results/phase13_ab_report.md').read_text()
sec = re.search(r'^## Phase 15 Addendum\b(.*?)(?=^## |\Z)', txt, re.M|re.S).group(1)
print(f'{rho:.6f}' in sec, f'{lo:.6f}' in sec or f'{lo:.4f}' in sec, m.ewc_dodges_high_fisher(rho, lo, hi))
"</automated>
  </verify>
  <acceptance_criteria>
    - The recomputed rho appears in the committed section text (first two booleans `True`), and
      the third boolean matches the recorded `GATE PASSES` / `GATE MISSES` line.
    - Two independent runs of `.venv/bin/python scripts/phase15_stats.py` produce identical
      statistics; the SUMMARY records both runs' values side by side.
    - No pre-registered constant in `scripts/phase15_stats.py` was modified in this plan:
      `git diff --stat scripts/phase15_stats.py` shows no change since the Plan 15-01 commit.
    - `make lint` passes.
  </acceptance_criteria>
  <done>The recorded verdict is reproducible from the committed artifact with the pre-registered seed, and no pre-registered constant moved after the number appeared.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `results/phase15_norms.json` → the statistic | Structured input; a malformed artifact must abort rather than yield a partial correlation |
| Phase 13 committed evidence → a Phase 15 append | A prior phase's recorded results must not be blended into, amended, or reopened |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-09 | Tampering | The gate resolved, re-seeded or re-parameterized after seeing the number | mitigate | Every constant and both verdict branches were committed in Plan 15-01 before the artifact existed; this plan asserts `git diff --stat scripts/phase15_stats.py` shows no change, and the plan text explicitly forbids re-running with a different seed, aggregate, pairing or resample count. Git history is the audit trail. |
| T-15-13 | Tampering | The append blending into or appearing to amend Phase 13's recorded results | mitigate | D-17: the section carries an explicit Phase 15 marker, a date, and the `does not reopen or amend` comment in `SHIP_DECISION_HEADER`'s register; it lands after every pre-existing heading; and `git diff --numstat` must show zero deleted lines. Plan 15-08's `test_verdict_section_is_dated_and_separated` makes it permanent. |
| T-15-14 | Repudiation | A future guard reading `## Verdict` by substring and clobbering this evidence | mitigate | CR-02 precedent: this section quotes Phase 13 heading strings in its own prose, so any guard over this file must anchor on the SECTION via `scripts/_verdict.py::VERDICT_SECTION`, never `split("## Verdict")[-1]`. Recorded in the renderer docstring and in this plan's SUMMARY for Plan 15-08. |
| T-15-07 | Tampering | Malformed artifact yielding a partial correlation | mitigate | ASVS V5: `load_pairs` validates four blocks x 36 cells and the exact projection set per layer, and `raise SystemExit` names the offending block/coordinate. |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed; this plan runs an already-committed pure-numpy module. |
</threat_model>

<verification>
- `results/phase13_ab_report.md` carries a `## Phase 15 Addendum` section as its last `## `
  heading, dated today, containing a `GATE PASSES` or `GATE MISSES` line
- `git diff --numstat results/phase13_ab_report.md` → 0 deletions
- Two independent runs of `scripts/phase15_stats.py` produce identical rho / p / CI
- `git diff --stat scripts/phase15_stats.py` → unchanged since the Plan 15-01 commit
- `.venv/bin/pytest -q` → no regression
</verification>

<success_criteria>
- The Fisher/Δ Spearman rho, its permutation p and its bootstrap CI exist for the first time in
  this commit, computed with the seed and constants committed before the artifact existed.
- The verdict is recorded unamended, adjacent to the Phase 13 data it derives from, in a section
  explicitly dated and marked as Phase 15 material.
- A miss is reported in the "suggestive but not statistically demonstrated at n = 36" register —
  reported, not discarded, not softened — and the narrowed SC2 wording is recorded in the SUMMARY
  for downstream plans.
- Every pre-existing byte of `results/phase13_ab_report.md` is unchanged.
- The verdict reproduces exactly on a second run.
</success_criteria>

<output>
Create `.planning/phases/15-figures-writeup/15-04-SUMMARY.md` when done.
Record verbatim: rho, the CI bounds, the permutation p, the degenerate-resample count, the
pass/miss verdict, and — on a miss — the narrowed SC2 wording. Plans 15-05 and 15-07 both consume
these. Also record the CR-02 section-anchoring constraint for Plan 15-08's D-17 test.
</output>
