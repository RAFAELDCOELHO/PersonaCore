---
phase: 15-figures-writeup
plan: 08
type: execute
wave: 5
depends_on: ["15-05", "15-06", "15-07"]
files_modified:
  - tests/test_phase15_docs.py
autonomous: true
requirements: [DOC-02, VIZ-02, VIZ-03]
must_haves:
  truths:
    - "Every Limitations quote is proven a byte-exact substring of its cited source, so paraphrase-to-soften becomes a test failure"
    - "Every README headline number is proven to match the value in its cited source report"
    - "The appended Phase 15 verdict section is proven dated, Phase-15-marked, and positioned after every pre-existing Phase 13 heading"
    - "The vmax driver named in the report matches the artifact field the figure reads"
    - "The full suite is green with no regression against the 392-test baseline"
  artifacts:
    - path: "tests/test_phase15_docs.py"
      provides: "Doc-integrity tests converting D-15, D-16 and D-17 from policy into structure"
      min_lines: 140
  key_links:
    - from: "tests/test_phase15_docs.py"
      to: "docs/REPORT.md"
      via: "byte-exact substring assertion of each quote against its source report"
      pattern: "read_text"
    - from: "tests/test_phase15_docs.py"
      to: "scripts/_verdict.py"
      via: "section-anchored heading read, never split on a heading substring"
      pattern: "_verdict|re.compile"
---

<objective>
Convert D-15, D-16 and D-17 from prose policy into permanent CPU-only tests: every Limitations
quote is a byte-exact substring of its source, every README headline number matches its source
report, and the appended verdict section is dated, marked and separated.

Purpose: the Limitations section is the deliverable most able to be quietly softened, and a
README number is the claim most likely to drift from its source. This project's register is to
convert a claimed invariant into a structural one — this is the doc-side instance of the same
move D-07 makes for the plotting path.

Output: `tests/test_phase15_docs.py`, and a green full suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-figures-writeup/15-CONTEXT.md
@.planning/phases/15-figures-writeup/15-RESEARCH.md
@.planning/phases/15-figures-writeup/15-PATTERNS.md
</context>

<new_test_genre>
**This file has no direct analog in the repo.** No existing test asserts a committed markdown
file's prose against a source. Build it from three partial precedents:

- `tests/test_phase14_demo.py:126` — reading a committed file's text at module scope
  (`_DEMO_SOURCE = (...).read_text(encoding="utf-8")`) and asserting substrings against it.
- `tests/test_phase14_demo.py:580-596` — pinning a committed file with a diff against a recorded
  SHA, described as *"a diff against a pinned commit, re-run on every CI run, which catches a
  staged edit and outlives the phase that made the decision."*
- `tests/test_phase14_scoring.py:423-424` — the **meta-guard habit**: assert the scan found
  something before asserting it found nothing bad. This is the failure mode of every
  scan-based test and it is mandatory here.

`tests/test_phase14_scoring.py:551,608,614` asserts a heading is present in a report, but only in
a report the test itself just wrote into `tmp_path` — never against committed evidence. This file
is the first to do that.
</new_test_genre>

<tasks>

<task type="auto">
  <name>Task 1: D-15 verbatim-quote and D-16 headline-number integrity tests</name>
  <files>tests/test_phase15_docs.py</files>
  <read_first>
    - `docs/REPORT.md` — the Limitations section written by Plan 15-05, and its nine entries
    - `README.md` — the three headline bullets written by Plan 15-06
    - `.planning/phases/15-figures-writeup/15-05-SUMMARY.md` — the nine quote strings **as
      written**, and the exact section heading strings
    - `.planning/phases/15-figures-writeup/15-06-SUMMARY.md` — the three headline numbers as
      written and their source file/line
    - `.planning/phases/15-figures-writeup/15-RESEARCH.md` lines 1010-1260 — the authoritative
      quote sources with file, section and line for all nine, and the three headline numbers with
      their source tables
    - `tests/test_phase14_demo.py` lines 120-135 and 575-600 — the committed-file `read_text` at
      module scope and the pinned-SHA diff register
    - `tests/test_phase14_scoring.py` lines 1-27 (docstring inventory) and 405-449 (the meta-guard
      habit and the "why a convention needs a test" paragraph)
    - `tests/test_phase15_plots.py` — the existing Phase 15 test-module docstring register to
      match
  </read_first>
  <action>
Create `tests/test_phase15_docs.py`. CPU-only, GPU/MPS-free, no torch, no network — it reads
committed markdown and JSON only.

**Module docstring** in the `tests/test_phase14_scoring.py:1-27` register: opens with
`DOC-02 / D-15 / D-16 / D-17`, then `CPU-only, GPU/MPS-free, no torch`, then a numbered inventory
naming each test and the one-line failure it prevents. It MUST carry the "why a convention needs
a test" paragraph: D-15's exact-quote rule, D-16's inline-qualifier density and D-17's separation
are all policies a future edit can break silently while every other test stays green;
**paraphrase-to-shorten and severity-reordering are editorializing disguised as editing**, and
this file is what makes them a test failure instead of a proofreading miss.

**`test_limitations_quotes_are_verbatim`** — D-15. Define a module-level fixture list of the nine
`(limitation_id, source_path, quote)` triples, taking each `quote` **from `docs/REPORT.md` as
actually written** (read the committed blockquotes) and each `source_path` from the citation in
that entry. For each triple:
  - assert the quote appears in `docs/REPORT.md`;
  - assert the quote is a **byte-exact substring of its cited source file**;
  - for a **visibly-truncated** quote (one containing an ellipsis), split on the ellipsis and
    assert each fragment is a substring of the source **and that the fragments appear in source
    order** — a truncation that reorders is a paraphrase wearing an ellipsis.

  **Meta-guards, both mandatory:**
  - assert the fixture list has exactly **9** entries — the R2 count, so a future edit cannot
    quietly drop one back to eight;
  - assert each source file was actually read and is non-empty before asserting anything about
    its contents, so a renamed or missing report fails loudly rather than vacuously passing.

  Additionally assert the section's opening states **nine** and carries the provenance clause
  about the ninth entering the record during Phase 15 research (R2), and that L8's blockquote
  contains the literal phrase `the bounded TinyStories corpus` — the quote must remain
  unparaphrased — while the **following** text contains `train_tokenizer.py`, `tiny_corpus.txt`
  and `11,469`, so the correction is present, evidence-named, and outside the quote (R1).

  Assert the **claim-bound ordering**: the four claim-group lead-ins appear in `docs/REPORT.md` in
  the order EWC-mitigates-forgetting, memory-lives-in-the-weights, without-damaging-the-base,
  from-scratch-base — pinned by the index at which each group's first entry appears. Ordering by
  severity or by comfort is the editorializing D-15 forbids, and this assertion is what stops a
  future reshuffle.

**`test_headline_numbers_match_sources`** — D-16. Define a module-level list of the three
`(label, number_string, source_path)` triples from `15-06-SUMMARY.md`. For each:
  - assert the number string appears in `README.md`;
  - assert the same string appears in its cited source report — the number has not drifted.

  Then assert the **inline-qualifier** rule structurally: locate the README line or bullet
  containing each number and assert that the same bullet also contains at least one of that
  number's required qualifier keyphrases (for recall: a proper-noun-core or reversed-phrasing
  clause; for retention: teacher-forced and noise-floor clauses; for inflation: a same-run
  baseline clause). Also assert `README.md` contains **no** occurrence of the bare-pointer
  antipattern — the literal `see Limitations` — anywhere, per D-16.

  Assert R1's README half: `README.md` does **not** contain `bounded TinyStories corpus`, and
  **does** contain `tiny_corpus.txt` and `11,469`.

  Meta-guard: assert the triple list is non-empty and that `README.md` read non-empty before
  asserting absences.
  </action>
  <verify>
    <automated>.venv/bin/pytest -q tests/test_phase15_docs.py::test_limitations_quotes_are_verbatim tests/test_phase15_docs.py::test_headline_numbers_match_sources</automated>
  </verify>
  <acceptance_criteria>
    - Both node IDs pass.
    - The quote fixture has exactly 9 entries and the test asserts that count.
    - Each test carries a meta-guard asserting its scan found something before asserting it found
      nothing bad.
    - **RED proof:** temporarily alter one word inside one Limitations blockquote in
      `docs/REPORT.md`, confirm `test_limitations_quotes_are_verbatim` FAILS naming that
      limitation, then revert. Do the same for one README number and
      `test_headline_numbers_match_sources`. Record both RED observations in the SUMMARY — a
      guard that has never been seen to fail is a guard nobody has verified.
    - `grep -c 'import torch' tests/test_phase15_docs.py` returns 0.
    - `make lint` passes.
  </acceptance_criteria>
  <done>Nine quotes proven byte-exact against their sources with ordering and count pinned; three README numbers proven to match their sources with inline qualifiers present; both tests proven RED against a deliberate softening.</done>
</task>

<task type="auto">
  <name>Task 2: D-17 separation test, the vmax-driver consistency check, and the full-suite gate</name>
  <files>tests/test_phase15_docs.py</files>
  <read_first>
    - `tests/test_phase15_docs.py` (current state after Task 1 — append, do not rewrite)
    - `results/phase13_ab_report.md` — the `## Phase 15 Addendum` section from Plan 15-04 and
      every pre-existing Phase 13 heading
    - `scripts/_verdict.py` — **the ONE copy of the anchored section read**, its
      `VERDICT_SECTION` regex, and the CR-02 incident it documents: a guard that took the tail
      after the LAST occurrence of `## Verdict` was fooled because a later section quoted that
      heading in its own prose
    - `scripts/phase14_recall.py` lines 1601-1643 — `assert_report_not_clobbered` and the CR-02
      explanation
    - `.planning/phases/15-figures-writeup/15-04-SUMMARY.md` — the recorded CR-02 constraint and
      the verdict values
    - `results/phase15_norms.json` and `docs/REPORT.md` — the `vmax_driver` fields and the report
      text that names them
    - `.planning/phases/15-figures-writeup/15-VALIDATION.md` — the node ID
      `test_verdict_section_is_dated_and_separated`
  </read_first>
  <action>
Append two tests to `tests/test_phase15_docs.py` and extend its numbered docstring inventory.

**`test_verdict_section_is_dated_and_separated`** — D-17. Read
`results/phase13_ab_report.md` and assert:
  - the Phase 15 addendum section exists and is **read section-anchored**, using a regex of the
    form that anchors from a `^## ` heading up to the next `^## ` heading or end of file — the
    `scripts/_verdict.py::VERDICT_SECTION` shape. **Never `split("## Verdict")[-1]` or any
    last-occurrence substring read.** This is not a style preference: the addendum quotes Phase 13
    heading strings in its own prose, which is precisely the condition that produced CR-02. Import
    the shared helper from `scripts/_verdict.py` where it fits, or build the section regex in the
    same anchored form and carry a comment citing CR-02 as the reason;
  - the section carries an explicit **Phase 15** marker and a **date** in `YYYY-MM-DD` form;
  - it carries the does-not-reopen-or-amend statement;
  - it contains either `GATE PASSES` or `GATE MISSES`, and on a miss also the
    `suggestive but not statistically demonstrated at n = 36` register;
  - **it appears after every pre-existing Phase 13 heading** — collect all `^## ` headings in
    order and assert the addendum heading is the last one. This is the machine-checkable form of
    "visibly separate, appended, and not blended into Phase 13's recorded content";
  - the pre-existing Phase 13 headings are all still present: assert each of `## Pre-Registration`,
    `## 2×2 Result`, `## Gate Verdict`, `## Threats to Validity`, `## Figures`,
    `## Evidence Index` still appears, so an append can never have displaced one.

  Meta-guard: assert the collected heading list is non-empty before asserting anything about
  ordering.

**`test_report_names_the_artifact_vmax_driver`** — D-02 / D-18 / D-04, the consistency check that
makes "different amounts, never different things" structural. Load `results/phase15_norms.json`
and, for the blocks whose drivers the report discloses, assert that the `vmax_driver`'s
`projection` string and its layer index both appear in `docs/REPORT.md`'s figures subsection. The
figure reads that same field via `scripts/plot_phase15.py`, so this proves the caption and the
report cannot disagree without a test failure. Assert also that `docs/REPORT.md` names the
`nonpositive_cells` counts rather than letting the figure imply them (Pitfall 5).

**Then run the full gate and record it:**
  - `.venv/bin/pytest -q` — the whole suite, expected at least 392 baseline passes plus the Phase
    15 additions (4 stats + 7 plots + 4 docs), with the 1 pre-existing skip
    (`test_real_slim_artifact_generates_on_cpu`). Any *other* skip, and any regression against
    392, is attributable to Phase 15 and must be investigated, not accepted.
  - `make lint` — `ruff check . && ruff format --check .`, clean.
  - Confirm both PNGs, `results/phase15_norms.json`, and `demo_v2.ipynb` are tracked by git and
    committed.

Record the final suite counts and the lint result in the SUMMARY.
  </action>
  <verify>
    <automated>.venv/bin/pytest -q tests/test_phase15_docs.py && .venv/bin/pytest -q && make lint</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest -q tests/test_phase15_docs.py` reports 4 passed.
    - `.venv/bin/pytest -q` reports at least 407 passed (392 baseline + 4 stats + 7 plots + 4
      docs), exactly 1 skipped, 0 failed.
    - `make lint` exits 0.
    - The D-17 test reads its section **anchored**, not by last-occurrence substring: verify
      `grep -c 'split("## Verdict")' tests/test_phase15_docs.py` returns 0, and that the test
      carries a comment citing CR-02.
    - **RED proof:** temporarily move the `## Phase 15 Addendum` heading above
      `## Evidence Index` in `results/phase13_ab_report.md`, confirm
      `test_verdict_section_is_dated_and_separated` FAILS, then revert. Record it in the SUMMARY.
    - `git status --porcelain` shows no untracked Phase 15 deliverable — both PNGs,
      `results/phase15_norms.json`, `demo_v2.ipynb`, and all three test files are committed.
  </acceptance_criteria>
  <done>The verdict section's dating, marking and positioning are pinned by a section-anchored test; the report's vmax disclosure is pinned against the artifact field the figure reads; the full suite and lint are green with no regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed evidence reports → the narrative's quotes and numbers | The only place a recorded negative can be softened is in transcription; the source files are the authority and this test file is the enforcement |
| Phase 13 committed evidence → a Phase 15 append | A future edit could displace or blend into a prior phase's recorded results |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-05 | Tampering | A Limitations quote paraphrased, softened, truncated invisibly, or reordered | mitigate | `test_limitations_quotes_are_verbatim` asserts byte-exact substring against the cited source, checks each ellipsis fragment appears in source order, pins the count at nine, and pins the claim-bound ordering. Proven RED against a deliberate one-word alteration. |
| T-15-19 | Tampering | A README headline number drifting from its source report, or losing its inline qualifier | mitigate | `test_headline_numbers_match_sources` asserts each number appears in both README and its cited source, asserts the qualifier keyphrases share the bullet, and asserts the bare `see Limitations` pointer is absent. Proven RED against a deliberate value change. |
| T-15-13 | Tampering | The Phase 15 addendum blending into or displacing Phase 13's recorded content | mitigate | `test_verdict_section_is_dated_and_separated` asserts the addendum is the last `## ` heading, carries a Phase 15 marker, a date and the does-not-reopen-or-amend statement, and that all six pre-existing Phase 13 headings survive. Proven RED against a deliberate reposition. |
| T-15-14 | Repudiation | A guard fooled by a heading quoted in prose (the CR-02 failure) | mitigate | The section read is anchored from `^## ` to the next `^## ` or EOF, in `scripts/_verdict.py::VERDICT_SECTION`'s shape, with a comment citing CR-02. `grep -c 'split("## Verdict")'` returning 0 is an acceptance criterion. |
| T-15-24 | Tampering | The figure caption and the report text disagreeing about the vmax driver | mitigate | `test_report_names_the_artifact_vmax_driver` asserts the report names the same `vmax_driver` field the plotting module reads from `results/phase15_norms.json` — D-04's "different amounts, never different things" made structural rather than checked by proofreading. |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed; this plan adds a pytest module using only the standard library plus already-declared dependencies. |
</threat_model>

<verification>
- `.venv/bin/pytest -q tests/test_phase15_docs.py` → 4 passed
- `.venv/bin/pytest -q` → ≥ 407 passed, exactly 1 skipped, 0 failed
- `make lint` → clean
- All three doc-integrity tests observed RED against a deliberate violation, then GREEN after
  revert
- `git status --porcelain` → no untracked Phase 15 deliverable
</verification>

<success_criteria>
- Nine Limitations quotes are proven byte-exact substrings of their cited sources, with the count,
  the claim-bound ordering, the R2 nine-not-eight statement and the R1 unparaphrased-quote-plus-
  evidence-named-correction all pinned by assertion.
- Three README headline numbers are proven to match their source reports and to carry their
  qualifiers in the same bullet, with the bare-pointer antipattern proven absent.
- The Phase 15 verdict addendum is proven dated, marked, separated and last, via a
  section-anchored read that cannot be fooled by a heading quoted in prose.
- The report's vmax disclosure is proven to name the same artifact field the figure reads.
- The full CPU-only suite is green with no regression against the 392-test baseline, and
  `make lint` is clean.
</success_criteria>

<output>
Create `.planning/phases/15-figures-writeup/15-08-SUMMARY.md` when done.
Record: the final `pytest -q` counts, the `make lint` result, and the three RED observations
(quote alteration, number change, section reposition) with the failure message each produced.
</output>
