---
phase: 15-figures-writeup
plan: 03
type: execute
wave: 3
depends_on: ["15-02"]
files_modified:
  - scripts/plot_phase15.py
  - tests/test_phase15_plots.py
  - results/phase15_adapter_delta.png
  - results/phase15_fisher_ewc.png
autonomous: true
requirements: [VIZ-02, VIZ-03]
must_haves:
  truths:
    - "The weight-delta heatmap on the layer x six-projection grid with a log color scale is committed to the repo"
    - "The three-panel figure juxtaposing the Fisher heatmap with the naive and EWC delta heatmaps is committed"
    - "The naive and EWC panels use ONE norm object; the Fisher panel's differs, on a stated units argument"
    - "The shared range is the full data range — vmax is the largest cell across both arms, vmin the smallest nonzero"
    - "The plotting module has no code path that opens a .pt file, proven by AST and by a fresh-interpreter import"
    - "Each figure names the specific layer/projection driving its color range, read from the artifact rather than re-derived"
  artifacts:
    - path: "scripts/plot_phase15.py"
      provides: "Artifact-only figure generation for VIZ-02 and VIZ-03"
      exports: ["plot_adapter_delta", "plot_fisher_ewc"]
      min_lines: 160
    - path: "results/phase15_adapter_delta.png"
      provides: "VIZ-02 — adapter weight-delta heatmap"
    - path: "results/phase15_fisher_ewc.png"
      provides: "VIZ-03 — Fisher / naive-Δ / EWC-Δ three-panel figure"
  key_links:
    - from: "scripts/plot_phase15.py"
      to: "results/phase15_norms.json"
      via: "json.load of the committed artifact — the ONLY input"
      pattern: "phase15_norms"
    - from: "tests/test_phase15_plots.py"
      to: "scripts/plot_phase15.py"
      via: "AST walk + fresh-interpreter subprocess import"
      pattern: "ast.parse|subprocess"
---

<objective>
Render VIZ-02 (the adapter weight-delta heatmap) and VIZ-03 (the three-panel Fisher / naive-Δ /
EWC-Δ figure) from `results/phase15_norms.json` and nothing else, and prove structurally that the
plotting module cannot open a checkpoint.

Purpose: D-07 makes the extract/plot tier boundary the deliverable, not a convention. A committed
PNG that provably derives from committed numbers is regenerable from a fresh clone; one that
"temporarily" read a 278 MB gitignored checkpoint is not. The AST and subprocess guards are the
third instance of this project converting a claimed invariant into a structural one.

Output: `scripts/plot_phase15.py`, both committed PNGs, and the figure/structural half of
`tests/test_phase15_plots.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-figures-writeup/15-CONTEXT.md
@.planning/phases/15-figures-writeup/15-RESEARCH.md
@.planning/phases/15-figures-writeup/15-PATTERNS.md
@.planning/phases/15-figures-writeup/15-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: plot_phase15.py — shared LogNorm, full-range, terse in-figure disclosure</name>
  <files>scripts/plot_phase15.py</files>
  <read_first>
    - `scripts/plot_phase13.py` — the whole file. Copy five properties: the docstring register
      (lines 1-18, including the explicit "no torch, no checkpoints" sentence), the
      Agg-before-pyplot import block with its `# noqa: E402`-with-reason comments (lines 20-35),
      the fail-loud `_series` helper that threads the source path so the error names the
      offending file (lines 74-97, docstring rationale *"an empty panel under a titled axis is a
      figure that lies"*), the `out_dir`-parameterized plot function that RETURNS the written
      path and never calls `show()` (lines 121-123, 162-165), the in-figure disclosure mechanism
      `fig.text(0.5, 0.005, ..., ha="center", fontsize=8, color="gray")` with
      `fig.tight_layout(rect=(0, 0.03, 1, 1))` (lines 187-194), and the thin `__main__`-guarded
      `main()` printing each written path (lines 203-209)
    - `results/phase15_norms.json` — the committed artifact; note the exact key spellings, the
      four `vmax_driver` entries and the four `nonpositive_cells` counts
    - `.planning/phases/15-figures-writeup/15-RESEARCH.md` lines 695-762 — the verified shared
      `LogNorm` recipe, the `cmap.set_bad` requirement, the one-colorbar-spanning-two-axes call,
      and the mechanical vmax-driver formula; lines 1449-1469 (Pitfalls 5 and 6)
  </read_first>
  <action>
Create `scripts/plot_phase15.py` following `scripts/plot_phase13.py` verbatim in register.

**Module docstring** in the `plot_phase13.py:1-18` shape. Sentence 2 is the load-bearing one and
must be the strongest form of `plot_phase13.py`'s own claim: *pure `json` + `numpy` +
`matplotlib` — no torch, no checkpoints, no `.pt` file of any kind. Every number comes from the
COMMITTED `results/phase15_norms.json`, so both figures regenerate deterministically from the
repo alone.* Then the indented output map naming
`VIZ-02 → results/phase15_adapter_delta.png` and `VIZ-03 → results/phase15_fisher_ewc.png`, the
D-01/D-02/D-04/D-07 constraints honored, and a `Run:` line. State explicitly that D-07 converts
sentence 2 from a claim into a test (`tests/test_phase15_plots.py::test_plotting_module_never_opens_a_checkpoint`),
so a future reader knows the sentence is enforced, not aspirational.

**Import block:** `json`, `pathlib`, `numpy as np`, then `matplotlib`, then
`matplotlib.use("Agg")` with the plot_phase13 comment about writing files only, then
`import matplotlib.pyplot as plt  # noqa: E402` and
`from matplotlib.colors import LogNorm  # noqa: E402`, each `noqa` carrying its reason in the
same comment. **Never `import torch`. Never a string literal ending in `.pt`** — including in
comments and the docstring, since the AST guard checks string constants; refer to checkpoints as
"checkpoints" in prose, never by filename.

`_REPO_ROOT`, `RESULTS_DIR`, and `NORMS_JSON` pointing at `results/phase15_norms.json`.

**`_load_artifact()`** — reads and validates the JSON, failing loud in the
`plot_phase13.py:88-97` register with the offending path named. `raise SystemExit` if the file is
missing (telling the operator to run the extraction script), `raise ValueError` naming the
offending block/layer if any of the four blocks is absent, if any block does not carry exactly
36 cells, or if a layer's projection set differs from the artifact's declared `projections`.
An empty or partial panel under a titled axis is a figure that lies.

**`_grid(artifact, block)`** — returns a `(6, 6)` fp64 `np.ndarray` in the artifact's declared
`projections` column order and layer row order 0..5. This ordering must match Plan 15-01's
`load_pairs` exactly so the figure and the statistic describe the same cells.

**`_shared_norm(naive, ewc)`** — D-02, no clipping at either end: stack both grids,
`vmax` = the maximum over both, `vmin` = the minimum over the **strictly positive** entries of
both (a log scale needs a positive floor). No percentile clipping — under D-01, compression IS
data. Return one `LogNorm` object.

**`_cmap()`** — `plt.get_cmap("magma").copy()` (never the removed `plt.cm.get_cmap`) with
`set_bad(color="0.85")`. `LogNorm(0.0)` returns `masked` and does **not** raise, so without
`set_bad` a zero cell renders as a transparent hole indistinguishable from missing data
(Pitfall 5).

**`_annotate_axes(ax, projections)`** — layer indices 0..5 on the y axis, projection names on the
x axis rotated for legibility, so a reader can locate the `vmax_driver` cell by eye.

**`plot_adapter_delta(out_dir)`** — VIZ-02. Single panel, `imshow` of the adapter grid under its
own `LogNorm` over that grid's full nonzero range, `aspect="auto"`, one colorbar. Title names the
quantity `‖ΔW‖_F/‖W₀‖_F` and the regime. Returns
`pathlib.Path(out_dir) / "phase15_adapter_delta.png"` after `fig.savefig`; `plt.close(fig)`;
never `show()`.

  **D-18 + D-04 terse figure-side disclosure**, via the `fig.text(...)` mechanism, reading the
  artifact's `blocks.adapter.vmax_driver` fields — **never re-deriving the argmax here**, so the
  caption and the report provably read the same field (D-04: different amounts, never different
  things). Terse means one line, enough to prevent a misreading when the PNG travels alone into
  a slide or README, not a paragraph competing with the data. It must state:
  the color range is driven by layer N's `<projection>`; the adapter is 331,776 LoRA parameters
  over a different training budget than the full fine-tune, so this figure is **not comparable**
  to the VIZ-03 delta panels; and the full per-layer table is in `results/phase15_norms.json`.

**`plot_fisher_ewc(out_dir)`** — VIZ-03. Three panels in one row: naive Δ, EWC Δ, Fisher.
  - **D-01:** the naive and EWC panels take the SAME `LogNorm` object from `_shared_norm`, and
    they get **one colorbar spanning both axes** (`fig.colorbar(im, ax=[ax_naive, ax_ewc])`) —
    the shared scale rendered visibly as shared. If sharing one scale makes one panel look nearly
    flat, **that flatness IS the finding** (EWC constrains movement in absolute terms) and it is
    reported as such. Do not independently rescale either panel to make them look equally busy.
  - The **Fisher panel uses its own `LogNorm`** over its own full nonzero range and its own
    colorbar, on a **units argument**: squared-gradient magnitude is not commensurable with a
    weight-delta ratio. This is a units exemption, not a convenience exemption, and it must not
    be extended to anything else that looks inconvenient.
  - **Never plot `naiveΔ − ewcΔ`.** That signed reduction is the D-10 statistic's input, not a
    panel; it can legitimately be negative and a `LogNorm` would silently mask every such cell
    (Pitfall 6).

  **D-04 terse figure-side disclosure** via `fig.text(...)`, again reading artifact fields rather
  than re-deriving: state that the naive and EWC panels share one scale so they are directly
  comparable; that the Fisher panel has its own scale because its units differ; and name the
  layer/projection driving the shared `vmax`, read from whichever of
  `blocks.naive.vmax_driver` / `blocks.ewc.vmax_driver` holds the larger `value`.

**`main()`** — thin, `__main__`-guarded, calling both plot functions with `RESULTS_DIR` and
printing each returned path with the `[plot_phase15] wrote ...` prefix. The guard is load-bearing:
the test `importlib`-loads this module, so `main()` must render nothing at import.

**Do NOT copy** `plot_phase13.py`'s `HEADLINE_RETENTION` / `LAMBDA0_POINT`
hardcoded-with-citation constants. Every number here comes from the artifact by construction
(D-07); a hardcoded number in this module would be a second source of truth.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import importlib.util,pathlib,tempfile;s=importlib.util.spec_from_file_location('p',pathlib.Path('scripts/plot_phase15.py'));m=importlib.util.module_from_spec(s);s.loader.exec_module(m);d=tempfile.mkdtemp();a=m.plot_adapter_delta(d);b=m.plot_fisher_ewc(d);print(a.exists(),b.exists(),a.stat().st_size>0,b.stat().st_size>0)"</automated>
  </verify>
  <acceptance_criteria>
    - The command above prints `True True True True`.
    - `grep -c 'import torch' scripts/plot_phase15.py` returns 0.
    - `.venv/bin/python -c "import ast,pathlib;t=ast.parse(pathlib.Path('scripts/plot_phase15.py').read_text());print([n.value for n in ast.walk(t) if isinstance(n,ast.Constant) and isinstance(n.value,str) and n.value.endswith('.pt')])"` prints `[]`.
    - `grep -qF 'matplotlib.use("Agg")' scripts/plot_phase15.py` succeeds and the `use("Agg")`
      line precedes the `import matplotlib.pyplot` line.
    - `grep -c 'show()' scripts/plot_phase15.py` returns 0.
    - `grep -qF 'set_bad' scripts/plot_phase15.py` succeeds.
    - `grep -qF 'vmax_driver' scripts/plot_phase15.py` succeeds — the driver is READ from the
      artifact; `grep -c 'argmax' scripts/plot_phase15.py` returns 0 (never re-derived here).
    - Both plot functions return the path they wrote and neither calls `plt.show`.
    - `make lint` passes.
  </acceptance_criteria>
  <done>Both figures render headless into an arbitrary out_dir; the naive/EWC panels share one norm object; the module imports no torch and contains no checkpoint filename literal.</done>
</task>

<task type="auto">
  <name>Task 2: The D-07 structural guards and the figure contract tests</name>
  <files>tests/test_phase15_plots.py</files>
  <read_first>
    - `tests/test_phase15_plots.py` (current state — Plan 15-02 wrote the schema and reproduction
      halves; this task appends to the SAME file and must extend the docstring inventory rather
      than replace it)
    - `scripts/plot_phase15.py` (final state after Task 1)
    - `tests/test_phase14_scoring.py` lines 405-449 — the AST walk, its "AST rather than
      `inspect.getsource` string matching: a substring check cannot tell a call from a mention in
      a docstring" rationale, the "why a convention needs a test" paragraph, and **the meta-guard
      asserting the walk found something before asserting it found nothing bad**
    - `tests/test_phase13_plots.py` lines 23-32 and 59-68 — the `importlib` loader and the
      `tmp_path` smoke assertions (returned path, `.exists()`, `.stat().st_size > 0`)
    - `.planning/phases/15-figures-writeup/15-RESEARCH.md` lines 765-818 — the two complementary
      D-07 checks, verbatim
    - `.planning/phases/15-figures-writeup/15-VALIDATION.md` — the exact node IDs
      `test_plot_functions_write_pngs`, `test_ab_panels_share_one_norm`,
      `test_shared_range_is_full_data_range`, `test_vmax_driver_matches_argmax`,
      `test_plotting_module_never_opens_a_checkpoint`
  </read_first>
  <action>
Append to `tests/test_phase15_plots.py`. Extend the existing numbered docstring inventory with
the new tests; do not rewrite the D-08 "why extraction is not permanently tested" paragraph
Plan 15-02 already placed there.

Add the module-level `importlib` loader for `scripts/plot_phase15.py` in the
`tests/test_phase13_plots.py:23-32` form, with its justification sentence: the plotting rules
(which artifact field, which norm, which disclosure) belong in the committed script, so the test
`importlib`-loads it rather than duplicating them in the package; `main()` is `__main__`-guarded
so the load renders nothing.

Define **exactly these five test functions**, spelled as in 15-VALIDATION.md:

  - `test_plot_functions_write_pngs(tmp_path)` — covers VIZ-02 and VIZ-03. Both plot functions
    render headless into `tmp_path`, return the expected path (`phase15_adapter_delta.png` /
    `phase15_fisher_ewc.png`), and the files exist and are non-empty. Writing to `tmp_path`
    proves they never clobber `results/`.

  - `test_ab_panels_share_one_norm` — D-01. Build the naive and EWC grids from the committed
    artifact via the module's own `_grid`, call `_shared_norm`, and assert that ONE norm object
    is produced whose `vmin`/`vmax` apply to both. Then assert the Fisher panel's norm differs:
    construct the Fisher grid and assert its own range is NOT equal to the shared `(vmin, vmax)`
    pair — the units exemption is real and visible, not an accident. Additionally inspect the
    rendered figure's images: call `plot_fisher_ewc(tmp_path)` and assert via the returned
    figure's axes (or by having the plot function expose the norms it used) that the naive and
    EWC `imshow` calls received the **same norm instance** while the Fisher one did not. If the
    plot function does not already expose this, add a small module-level helper in
    `scripts/plot_phase15.py` that returns the three norms so the contract is testable rather
    than inferred — a claim that cannot be tested is the pattern this project keeps having to
    convert.

  - `test_shared_range_is_full_data_range` — D-02. The shared norm's `vmax` equals the maximum
    cell across BOTH arms to within `1e-12`, and its `vmin` equals the minimum **strictly
    positive** cell across both to within `1e-12`. Assert no clipping: neither bound equals a
    percentile of the stacked data unless that percentile coincidentally equals the extremum —
    the direct way to state this is that `vmax` equals the exact stacked maximum and `vmin` the
    exact stacked positive minimum, which a clipped implementation cannot satisfy.

  - `test_vmax_driver_matches_argmax` — D-02 / D-18. For each of the four blocks, the artifact's
    `vmax_driver` `(layer, projection)` pair identifies the cell that is in fact the argmax of
    that block's grid, and its `value` matches that cell to within `1e-12`. This pins the
    disclosure the figure captions and the report both read.

  - `test_plotting_module_never_opens_a_checkpoint` — D-07, **two complementary checks in one
    test**:
    (a) **AST**: parse `scripts/plot_phase15.py`, collect every top-level module name from
    `ast.Import` and `ast.ImportFrom`, assert `"torch"` is not among them; collect every string
    `ast.Constant` ending in `.pt` and assert the list is empty. Carry the
    `tests/test_phase14_scoring.py:400-403` rationale in the docstring: AST rather than substring
    matching, because a substring check cannot tell a call from a mention in a docstring and this
    module's docstring necessarily discusses checkpoints while explaining that it never opens
    one. **Include the meta-guard**: assert the collected import set is non-empty first, so a
    broken walk that finds nothing cannot pass vacuously.
    (b) **Subprocess**: in a FRESH interpreter (`sys.executable -c ...`, `cwd=_REPO_ROOT`),
    `importlib`-load the plotting module and exit non-zero if `"torch"` appears in `sys.modules`.
    This is strictly stronger than (a) — it catches a transitive import through a helper module
    that (a) cannot see — and it must run out-of-process because torch is already in
    `sys.modules` from sibling tests. Assert `returncode == 0` with the message
    "plotting module transitively imports torch — D-07 violated".

    The test docstring carries the "why a convention needs a test" paragraph in
    `tests/test_phase14_scoring.py:425-449`'s register: nothing in the plotting module itself
    prevents a future edit from adding a two-line checkpoint read when one artifact field is
    missing, so without this test *"reads only the artifact"* is a convention a future edit can
    break silently — and D-07's whole point is that the regenerability proof must hold by
    construction. Note that this guard is also a security control: it is what keeps the plotting
    path provably incapable of deserializing anything.

Bare `assert` is correct in tests.
  </action>
  <verify>
    <automated>.venv/bin/pytest -q tests/test_phase15_plots.py</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest -q tests/test_phase15_plots.py` reports 7 passed (2 from Plan 15-02 + 5
      here), 0 failed.
    - All five node IDs resolve individually:
      `.venv/bin/pytest -q tests/test_phase15_plots.py::test_plot_functions_write_pngs tests/test_phase15_plots.py::test_ab_panels_share_one_norm tests/test_phase15_plots.py::test_shared_range_is_full_data_range tests/test_phase15_plots.py::test_vmax_driver_matches_argmax tests/test_phase15_plots.py::test_plotting_module_never_opens_a_checkpoint` reports 5 passed.
    - The D-07 test genuinely fails when violated: temporarily add `import torch` to
      `scripts/plot_phase15.py`, confirm `test_plotting_module_never_opens_a_checkpoint` FAILS on
      both the AST and subprocess halves, then revert. Record the RED observation in the SUMMARY —
      a structural guard that has never been seen to fail is a guard nobody has verified.
    - `make lint` passes.
  </acceptance_criteria>
  <done>Five tests green; the D-07 guard is proven RED against a deliberate violation and then GREEN after revert.</done>
</task>

<task type="auto">
  <name>Task 3: Generate and commit both PNGs</name>
  <files>results/phase15_adapter_delta.png, results/phase15_fisher_ewc.png</files>
  <read_first>
    - `scripts/plot_phase15.py` (final state)
    - `results/phase15_norms.json` — the `vmax_driver` and `nonpositive_cells` fields the
      captions disclose
    - `.planning/phases/15-figures-writeup/15-CONTEXT.md` D-01, D-02, D-04, D-18 — the
      disclosure obligations the rendered figures must satisfy
  </read_first>
  <action>
Run `.venv/bin/python scripts/plot_phase15.py` and commit both
`results/phase15_adapter_delta.png` and `results/phase15_fisher_ewc.png` alongside the tracked
`results/phase13_*.png` figures.

Then **open both PNGs and confirm by eye**, recording each observation in the SUMMARY:
  - VIZ-02's grid is legible: 6 layer rows, 6 projection columns, labelled axes, a log colorbar.
  - VIZ-03's naive and EWC panels are visually under one colorbar, and the Fisher panel has its
    own. If the EWC panel looks nearly flat next to the naive panel under the shared scale,
    **that is the expected finding, not a rendering bug** — record it as an observation for Plan
    15-05's report text and do NOT rescale.
  - The `fig.text` disclosure lines are present, legible at the rendered DPI, and name the same
    layer/projection strings as the artifact's `vmax_driver` fields.
  - Note the observed dynamic range (decades between `vmin` and `vmax`) — Plan 15-05's outlier
    disclosure paragraph reports it.

Record in the SUMMARY, for Plan 15-05 to consume: the exact `vmax_driver` layer/projection for
each block, the `nonpositive_cells` counts, the shared `(vmin, vmax)` pair, and a one-sentence
description of what the EWC panel looks like relative to the naive panel under the shared scale.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import pathlib,json;p=[pathlib.Path('results/phase15_adapter_delta.png'),pathlib.Path('results/phase15_fisher_ewc.png')];print(all(x.exists() and x.stat().st_size>10000 for x in p))" && .venv/bin/pytest -q tests/test_phase15_plots.py</automated>
  </verify>
  <acceptance_criteria>
    - The command prints `True` and the test file reports 7 passed.
    - `git check-ignore results/phase15_adapter_delta.png` returns non-zero (both PNGs are
      tracked, matching `results/phase13_forgetting_curve.png`'s precedent).
    - Re-running `.venv/bin/python scripts/plot_phase15.py` a second time and comparing SHA-256
      digests before/after shows the regeneration is stable — the `260801-r9y` precedent of
      verifying both PNGs identical after a plotting-code change.
    - The SUMMARY records the four `vmax_driver` entries, the four `nonpositive_cells` counts,
      the shared `(vmin, vmax)` pair, and the EWC-vs-naive flatness observation.
    - `.venv/bin/pytest -q` reports at least 403 passed, 1 skipped.
  </acceptance_criteria>
  <done>Both PNGs are committed, stable across regeneration, and their disclosure text matches the artifact fields the report will cite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `results/phase15_norms.json` → plotting script | Structured input parsed into figure geometry; a malformed artifact must abort rather than render an empty or partial panel |
| plotting tier → checkpoint tier | The boundary D-07 makes structural; crossing it voids the regenerability proof |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02 | Repudiation | `scripts/plot_phase15.py` silently reading a checkpoint | mitigate | `test_plotting_module_never_opens_a_checkpoint` runs two complementary checks — an AST walk asserting no `torch` import and no `.pt` string constant, plus a fresh-interpreter subprocess import asserting `torch` never enters `sys.modules`. The subprocess half catches transitive imports the AST cannot see. This is also the security control that keeps the plotting path provably incapable of deserializing anything. |
| T-15-07 | Tampering | Malformed or truncated artifact rendering a partial figure | mitigate | ASVS V5: `_load_artifact` validates four blocks x 36 cells and each layer's projection set, and fails loud naming the offending block/layer in the `plot_phase13.py:88-97` register — *"an empty panel under a titled axis is a figure that lies."* |
| T-15-04 | Tampering | A committed PNG that no longer matches the committed JSON | mitigate | The `tmp_path` smoke test plus a manual regeneration-and-SHA-256 comparison before commit, following the `260801-r9y` precedent. Because the module reads only the artifact, any drift is a regeneration away from being detected. |
| T-15-12 | Tampering | A panel independently rescaled to look "equally busy" | mitigate | `test_ab_panels_share_one_norm` asserts one norm instance across the naive and EWC panels; `test_shared_range_is_full_data_range` asserts the bounds are the exact data extrema, which a percentile-clipped implementation cannot satisfy. Flatness is a finding, and the tests make hiding it a test failure. |
| T-15-06 | Denial of Service | matplotlib requiring a GUI backend in headless CI | mitigate | `matplotlib.use("Agg")` before the `pyplot` import, with the reason in the comment (`plot_phase13.py:27`); every plot function `savefig`s and never calls `show()`. |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed. `matplotlib 3.10.9` is already present via the `demo` and `notebook` extras and is therefore already in CI. No registry lookup and no legitimacy checkpoint required. |
</threat_model>

<verification>
- `.venv/bin/pytest -q tests/test_phase15_plots.py` → 7 passed
- `.venv/bin/pytest -q` → ≥ 403 passed, 1 skipped
- `make lint` → clean
- The D-07 guard was observed RED against a deliberate `import torch` and GREEN after revert
- Both PNGs are tracked and regenerate to identical SHA-256 digests
</verification>

<success_criteria>
- `results/phase15_adapter_delta.png` (VIZ-02) and `results/phase15_fisher_ewc.png` (VIZ-03) are
  committed to the repo.
- The naive and EWC panels share ONE `LogNorm` object under ONE colorbar; the Fisher panel has
  its own, on a stated units argument.
- The shared range is the exact full data range — largest cell across both arms, smallest
  strictly positive cell — with no percentile clipping at either end.
- Both figures carry a terse `fig.text` disclosure naming the `vmax_driver` layer/projection read
  from the artifact, and VIZ-02's additionally states it is not comparable to VIZ-03's panels.
- `scripts/plot_phase15.py` imports no torch, contains no checkpoint filename literal, and the
  claim is enforced by an AST check and a fresh-interpreter subprocess check.
- The signed `naiveΔ − ewcΔ` reduction is never plotted on a log axis.
</success_criteria>

<output>
Create `.planning/phases/15-figures-writeup/15-03-SUMMARY.md` when done.
Record for Plan 15-05: the four `vmax_driver` entries, the four `nonpositive_cells` counts, the
shared `(vmin, vmax)` pair and its span in decades, and the EWC-vs-naive flatness observation.
</output>
