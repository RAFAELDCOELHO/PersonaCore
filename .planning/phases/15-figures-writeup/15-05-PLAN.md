---
phase: 15-figures-writeup
plan: 05
type: execute
wave: 4
depends_on: ["15-03", "15-04"]
files_modified:
  - docs/REPORT.md
autonomous: true
requirements: [DOC-02]
must_haves:
  truths:
    - "Every v1.0 section of docs/REPORT.md is textually untouched"
    - "A dated Milestone 2 boundary marker appears before any stale future-tense v1.0 text a front-to-back reader would hit"
    - "A reader can skip the Decision sections and still follow the results narrative as a complete story, and vice versa"
    - "The Limitations section carries NINE honest negatives, each quoting its source exactly, ordered by which claim they bound"
    - "Limitation L8's known-wrong source wording is quoted verbatim and immediately followed by a dated, evidence-naming Phase 15 correction note"
    - "The figures' non-comparability and vmax drivers are stated in the report at full detail, matching the terse figure-side text"
  artifacts:
    - path: "docs/REPORT.md"
      provides: "v1.0 report plus the Milestone 2 boundary marker, v2.0 Decision sections, results narrative and nine-limitation section"
      contains: "Milestone 2"
  key_links:
    - from: "docs/REPORT.md"
      to: "results/phase13_ab_report.md"
      via: "terse citation of the correlation verdict, linking to the full pre-registration table"
      pattern: "phase13_ab_report"
    - from: "docs/REPORT.md"
      to: "results/phase15_norms.json"
      via: "the per-layer table / vmax driver disclosure"
      pattern: "phase15_norms"
---

<objective>
Extend `docs/REPORT.md` into the v2.0 technical report: a dated Milestone 2 boundary marker, new
`## Decision:` sections documenting the v2.0 choices, a results narrative documenting the
outcomes, and a Limitations section aggregating **nine** honest negatives, each quoted exactly
from its source.

Purpose: one report a reader goes through front to back. D-13 keeps every v1.0 word intact — the
`m1-demo-v1` release tag already preserves the file as shipped — while a boundary marker removes
the misreading that its future-tense Milestone 2 roadmap is still pending.

Output: `docs/REPORT.md`, extended and never edited above the boundary marker.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-figures-writeup/15-CONTEXT.md
@.planning/phases/15-figures-writeup/15-RESEARCH.md
@.planning/phases/15-figures-writeup/15-PATTERNS.md
</context>

<resolved_decisions>
Four orchestrator-resolved decisions apply to this plan. They are settled — implement them, do
not re-open them.

**R1 — limitation L8's known-wrong source wording.** `docs/REPORT.md:61-63` attributes BPE merge
exhaustion to *"the bounded TinyStories corpus"*, which is verifiably wrong. **Quote L8
VERBATIM** (D-15's exact-quote rule and D-13's untouched clause both require it), then
**immediately follow the quote with a dated, clearly-labeled Phase 15 correction note** in the
same dated / separate / does-not-amend register D-17 uses for the correlation verdict. The note
MUST name the specific verification performed, not merely assert the correction: *verified
against `scripts/train_tokenizer.py:31`, which trains on `tests/fixtures/tiny_corpus.txt`
(11,469 bytes), not the full TinyStories corpus the original text implies.* A correction is only
as trustworthy as the evidence behind it — the same standard as the eight limitation quotes it
sits next to. **Never silently paraphrase the quote to fix it** — that is the exact drift vector
D-15 closes.

**R2 — a NINTH honest negative joins D-15's eight.** `results/phase14_recall_report.md:585`
records a `+27.16%` off-topic collapse not among D-15's locked eight. Include it under the **same
policy as the original eight**: verbatim quote, visible truncation only. **Position it by its
claim-bounding role** within the locked claim-bound ordering — explicitly NOT appended at the end.
**The Limitations opening line states the count is NINE, not eight**, with a one-clause
provenance note that the ninth was surfaced during Phase 15 research from data predating this
phase. Not to diminish its standing, but for the same provenance transparency required of the L8
correction: a reader should be able to trace not just what each limitation says, but when and how
it entered the record.

**R3 — boundary marker placement.** Place the dated Milestone 2 boundary marker **before any
stale future-tense v1.0 text a front-to-back reader would hit** — immediately after
`## Reproducibility` ends (~line 421), i.e. directly before
`## Limitations and the Milestone 2 Roadmap` at line 422. That frames everything after it as "as
written on 2026-06-10", preserving every v1.0 word while removing the misreading. All v2.0
sections append at EOF.

**Correction to CONTEXT D-14:** the file has **14** `## Decision:` sections, not 15. Do not write
"the 16th Decision section".
</resolved_decisions>

<tasks>

<task type="auto">
  <name>Task 1: The dated Milestone 2 boundary marker and the v2.0 Decision sections</name>
  <files>docs/REPORT.md</files>
  <read_first>
    - `docs/REPORT.md` — the whole file. Note the heading inventory (14 `## Decision:` sections
      between lines 48 and 343, `## Results` 369, `## Reproducibility` 407,
      `## Limitations and the Milestone 2 Roadmap` 422, `## Where to Go Next` 448, EOF 456);
      the `## Decision: <the choice, stated as a claim>` heading form — a title-cased assertion
      naming the choice AND its constraint, never a topic label; and the paragraph register in
      `## Results` and `## Limitations` — a **bolded lead-in phrase** then plain prose
      (`**What this model is not.**`, `**What actually trained.**`)
    - `.planning/phases/15-figures-writeup/15-CONTEXT.md` D-13, D-14 — the untouched clause, the
      strictly-non-overlapping Decision-vs-narrative split, and the skip test
    - `.planning/phases/13-ewc-a-b-no-forgetting-experiment/13-CONTEXT.md` — the v2.0 decisions
      worth their own Decision sections (D-06 gated-vs-descriptive, D-08 end-of-run endpoints,
      D-09/D-10 pre-registration in committed code)
    - `.planning/phases/14-teach-then-recall-demo/14-CONTEXT.md` — D-12 gate-miss policy,
      D-17/D-18 structural enforcement, D-07 permanent-vs-checkpoint-specific test split
    - `.planning/phases/14-teach-then-recall-demo/14-LEARNINGS.md` — the declared-vs-structural
      guarantee failure mode named as the milestone's most recurring
    - `results/phase13_ab_report.md` `## Pre-Registration` and `## Gate Verdict` — the A/B
      decisions as recorded
  </read_first>
  <action>
**Step 1 — the boundary marker (R3).** Insert, immediately after `## Reproducibility` ends and
directly before `## Limitations and the Milestone 2 Roadmap` (line 422), a short dated marker
section. Use a horizontal rule and a heading such as
`## Milestone 1 Ends Here — Everything Below This Line Is As Written on 2026-06-10`. Its body
states, in two or three sentences: everything from here to `## Where to Go Next` is preserved
exactly as shipped with the `m1-demo-v1` release tag and is **not** amended; its
"Milestone 2 (upcoming)" bullets and its "it has no personalization yet" clause were true of
Milestone 1 as scoped and are now delivered; the Milestone 2 sections appended after
`## Where to Go Next` record what actually shipped. Link forward to the v2.0 results narrative by
its exact heading.

**Nothing above line 422 is edited.** Not a word, not a typo fix, not a link update. The
`## Limitations and the Milestone 2 Roadmap` section (422-447) and `## Where to Go Next`
(448-456) stay byte-identical.

**Step 2 — the v2.0 Decision sections**, appended at EOF, one per major v2.0 decision already on
record, in the existing `## Decision: <title-cased assertion>` form. Each documents the **CHOICE
and its rationale at the moment it was locked** — what was decided, why, and against what
alternative — and **never what resulted from it** (D-14: strictly non-overlapping with the
results narrative). Write one section for each of:

  - **Two-mechanism stage split** — full fine-tune with and without EWC as the A/B, LoRA
    personalization on the frozen conversational base; against the alternative of a single
    combined mechanism.
  - **The frozen tokenizer kept for v2.0** — no retrain, `best.pt` stays a valid M2 base, the
    dead-id mask handles the 547-live-id vocabulary, and the inflation tax was measured instead
    of assumed; against the alternative of retraining the tokenizer and invalidating every
    checkpoint.
  - **Pre-registration in committed code, before any number exists** — constants and gates as
    module-level literals in the committed driver, git history as the proof
    (`scripts/finetune_smoke.py`, `scripts/finetune_ab.py` at `c3d942e`,
    `scripts/phase15_stats.py`); against the alternative of choosing a threshold after seeing the
    data.
  - **Gating only the part of a claim the sample size supports** — Phase 13 D-06's retention gate
    with acquisition descriptive, and Phase 15 D-11's sign gate with magnitude descriptive;
    against a full exemption from gating and against gating a quantity n cannot support.
  - **Honest negatives stand unamended, discretionary continuations are logged separately and
    dated after** — the Phase 12 lambda-sweep verdict followed by the separate production-lambda
    decision; Phase 14 D-12's ship-decision separation; against amending a verdict in place.
  - **Structural enforcement over declared invariants** — the `forbid_ids` mask comparison, the
    token-id byte-identity check, and Phase 15's D-07 AST plus subprocess guard proving the
    plotting path cannot deserialize anything; against a docstring claiming the same thing. Name
    this as the response to the declared-vs-structural gap Phase 14's LEARNINGS identified as the
    milestone's most recurring failure mode.
  - **Extract once, plot from the committed artifact only** — the D-05/D-07 tier boundary that
    makes both figures regenerable from a fresh clone without the 914 MB of gitignored
    checkpoints; against a single convenient script that reads checkpoints and plots in one pass.

Each section is a few paragraphs in the existing register: a bolded lead-in phrase, then plain
prose. Keep the discipline the existing 14 sections have — the heading is an assertion, and the
body names the alternative that was rejected and why.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import pathlib,re;t=pathlib.Path('docs/REPORT.md').read_text();h=re.findall(r'^## .*',t,re.M);i=[n for n,x in enumerate(h) if x.startswith('## Milestone 1 Ends Here')][0];j=[n for n,x in enumerate(h) if 'Limitations and the Milestone 2 Roadmap' in x][0];print(i<j, sum(1 for x in h if x.startswith('## Decision:'))>=21)"</automated>
  </verify>
  <acceptance_criteria>
    - The command prints `True True` — the boundary marker precedes
      `## Limitations and the Milestone 2 Roadmap`, and there are now at least 21 `## Decision:`
      sections (14 pre-existing plus at least 7 new).
    - `git show HEAD:docs/REPORT.md` piped through `head -421` matches the first 421 lines of the
      working file byte-for-byte — zero deletions and zero modifications above the boundary.
    - The original `## Limitations and the Milestone 2 Roadmap` and `## Where to Go Next`
      sections are byte-identical to their `git show HEAD:docs/REPORT.md` versions.
    - The boundary marker carries the date `2026-06-10` and the string `m1-demo-v1`.
    - Every new heading matches the pattern `^## Decision: [A-Z]` — a title-cased assertion,
      never a topic label.
    - No new Decision section states an OUTCOME (a measured number, a pass/fail, a rate) — the
      D-14 non-overlap rule. Spot-check each new section for measured values; anything found
      belongs in Task 2's narrative instead.
  </acceptance_criteria>
  <done>The boundary marker is dated and placed before the stale future-tense text; at least seven new Decision sections document choices without outcomes; nothing above line 421 changed.</done>
</task>

<task type="auto">
  <name>Task 2: The v2.0 results narrative, the figure disclosures, and the terse verdict citation</name>
  <files>docs/REPORT.md</files>
  <read_first>
    - `docs/REPORT.md` (current state after Task 1) — and its existing `## Results` section
      (line 369) for the narrative register
    - `results/phase13_ab_report.md` — `## 2×2 Result` (53-64), `## Gate Verdict` (106-123),
      `## Reconciliation: §8 Search vs Phase-13 Demonstration` (261), and the
      `## Phase 15 Addendum` appended by Plan 15-04
    - `results/phase14_recall_report.md` — `## Recall Results — Core Tier` (56-60) and the
      verdict at line 575
    - `results/finetune_smoke_report.md` — `## Stage 3 — λ Sweep (EWC-03)` (142) and the
      verdict at line 159
    - `results/inflation_report.md` — `## D-08 Metrics` metric 1 (line 15) and `## Baseline`
      (30-31)
    - `results/phase15_norms.json` — `comparison_basis.note` and every block's `vmax_driver` and
      `nonpositive_cells`
    - `.planning/phases/15-figures-writeup/15-03-SUMMARY.md` — the recorded vmax drivers, the
      shared vmin/vmax pair and its span in decades, and the EWC-vs-naive flatness observation
    - `.planning/phases/15-figures-writeup/15-04-SUMMARY.md` — rho, the CI, the permutation p and
      the pass/miss verdict
    - `.planning/phases/15-figures-writeup/15-CONTEXT.md` D-01 through D-04, D-17, D-18
  </read_first>
  <action>
Append the v2.0 results narrative at EOF, after the Decision sections. It documents the
**OUTCOME** across the three experiments as a single story — what was tested, what the evidence
showed, what remains uncertain — and **never re-explains why a choice was made** (D-14). The
test: a reader must be able to skip every Decision section and still follow this as a complete
story, and skip this and still find each choice justified in its own section.

Use the heading `## Milestone 2 Results: What Three Experiments Showed`, and the existing
`## Results` register (bolded lead-in phrase, then prose). Cover, in narrative order:

  - **The lambda search that found nothing** — Phase 12's `λ* = None` verdict at the 1250-step
    budget: no lambda satisfied both the within-margin rule and the retention demonstrability
    guard. State that the production λ=0.01 was a separate, later, discretionary decision, not an
    amendment of that verdict.
  - **The A/B that did demonstrate it** — naive retention PPL **8.52417066884246** vs EWC
    **3.8911400839446597** from a step-0 anchor of **2.107553076833866**: drift **+6.416618** vs
    **+1.783587**, a **3.6×** difference, clearing `MARGIN = 2 × 0.068930 = 0.137860` at
    **33.61×**. EWC's acquisition cost was **+0.380556** PPL (~9.1% relative), **descriptive with
    no gate**. State the scope limit plainly: the claim is about teacher-forced retention PPL, not
    free-running story generation — 79 naive / 69 EWC mid-story role-token leakages were measured.
  - **The clean-room teach-then-recall** — taught **496/1008 = 0.4921** against a pre-registered
    **0.2486**; held-out **326/936 = 0.3483** against **0.2000**; closed-book control exactly
    **0/2430**. Thresholds came from a **disjoint** calibration fact set
    (`CALIBRATION_SHA 0425fdc4…`) before the run existed. Verdict: **ADAPT — GO with two
    qualifications.**
  - **The tokenizer-inflation tax** — dialogue **3.229** tokens/word over 4,800,385 utterance
    tokens and 1,486,754 whitespace words, against a TinyStories baseline of **2.860** recomputed
    in the same run: ratio **1.129×**, inside the ≤1.2× GO band at a measured fit of 0.9996.
    State the qualifier: the number is only meaningful against that same-run baseline and is
    never comparable to another tokenizer.

  - **The two figures.** Present VIZ-02 (`results/phase15_adapter_delta.png`) and VIZ-03
    (`results/phase15_fisher_ewc.png`) with the **full** report-side reasoning D-04 assigns here
    — the figures themselves carry only the terse form. This must include:
    - **D-01, stated explicitly:** which panels share a color scale and **why the Fisher panel
      does not** — squared-gradient magnitude is not commensurable with a weight-delta ratio, a
      **units argument, not a convenience argument**. A reader must never be left guessing whether
      the difference is intentional or an oversight. If the EWC panel looks nearly flat next to
      the naive panel under the shared scale, **say so and say that the flatness IS the finding** —
      EWC constrains movement in absolute terms — never that it is a rendering artifact.
    - **D-02 / D-18 outlier disclosure:** name the **specific layer and projection** driving each
      figure's color range, reading the `vmax_driver` values recorded in `15-03-SUMMARY.md` and
      `results/phase15_norms.json`. Use the pattern *"layer N's `<projection>` dominates the
      range; the full per-layer distribution is in `results/phase15_norms.json`."* State the
      `nonpositive_cells` counts explicitly rather than letting the figure imply them.
    - **D-03 non-comparability, naming the three ACTUAL confounds** — never "different regimes":
      (1) **parameter count** — 331,776 LoRA parameters vs 13,891,584 full-model parameters;
      (2) **training budget** — the LoRA teaching run vs the 4000-step full fine-tune;
      (3) **a smaller absolute ΔW magnitude for the adapter does NOT imply more conservative or
      less effective learning** — it reflects the adapter's parameter budget, not a quality
      comparison. Point at `results/phase15_norms.json`'s `comparison_basis` as the
      machine-readable statement of the same thing.
    - **D-04 consistency check:** a reader comparing the figure's terse text and this section must
      find **different amounts of detail, never different things.** Confirm the layer/projection
      strings written here are the same strings the figures' disclosure text renders.

  - **The correlation verdict, cited terse (D-17/D-04).** One or two sentences stating the
    pass/fail and the correlation value, **linking to the full pre-registration table in
    `results/phase13_ab_report.md`'s `## Phase 15 Addendum` — never restating the table.** Take
    the values verbatim from `15-04-SUMMARY.md`. If the gate MISSED, state it as plainly as a
    pass: report it in the **"suggestive but not statistically demonstrated at n = 36"** register,
    state that the roadmap's "EWC visibly dodging high-Fisher coordinates" wording narrows
    accordingly, and do not soften it into a passing verdict. If it PASSED, state that the sign is
    the falsifiable claim and the magnitude is reported honestly given n = 36 and is not itself
    pass/fail.

  - **What remains uncertain** — a closing paragraph handing off to the Limitations section by
    name, without duplicating its quotes.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import pathlib,json;t=pathlib.Path('docs/REPORT.md').read_text();a=json.load(open('results/phase15_norms.json'));d=a['blocks']['adapter']['vmax_driver'];print(all(s in t for s in ['0.4921','0.3483','8.52417066884246','3.8911400839446597','3.229','2.860','1.129','331,776','phase15_adapter_delta.png','phase15_fisher_ewc.png','phase13_ab_report.md','phase15_norms.json']), d['projection'] in t)"</automated>
  </verify>
  <acceptance_criteria>
    - The command above prints `True True`.
    - `grep -qF 'Milestone 2 Results: What Three Experiments Showed' docs/REPORT.md` succeeds.
    - The three D-03 confounds all appear: `grep -qF '331,776' docs/REPORT.md`,
      `grep -qF '13,891,584' docs/REPORT.md`, and a sentence containing both "parameter budget"
      and "not a quality comparison".
    - The D-01 Fisher-scale explanation appears and uses a units argument:
      `grep -qi 'units' docs/REPORT.md` succeeds within the figures subsection.
    - The correlation citation links to `results/phase13_ab_report.md` and does **not** restate
      the pre-registration table: the report contains no second copy of the `N_PERM` / `N_BOOT` /
      `SEED` row set.
    - The verdict wording matches `15-04-SUMMARY.md` exactly — same rho, same CI bounds, same
      pass/miss word.
    - The results narrative contains no "why we chose" rationale that duplicates a Decision
      section (the D-14 skip test).
  </acceptance_criteria>
  <done>The three experiments read as one story with the real numbers; both figures carry their full-detail D-01/D-02/D-03/D-18 disclosure; the verdict is cited terse and links to its full table.</done>
</task>

<task type="auto">
  <name>Task 3: The Limitations section — nine honest negatives, each quoted exactly</name>
  <files>docs/REPORT.md</files>
  <read_first>
    - `docs/REPORT.md` (current state after Task 2), and its existing
      `## Limitations and the Milestone 2 Roadmap` (422-447) for the bolded-lead-in register the
      new section mirrors — the new section is a **sibling**, never an edit of that one
    - `.planning/phases/15-figures-writeup/15-RESEARCH.md` lines 1010-1207 — **every one of the
      nine quotes is transcribed there byte-for-byte with its file, section and line number.**
      Read this section in full; it is the authoritative quote source for this task.
    - `results/finetune_smoke_report.md` line 159 (L1) and its `## Stage 3 — λ Sweep (EWC-03)`
      heading at 142
    - `results/phase14_recall_report.md` lines 378 (L4), 398-401 and 566-571 (L2), 334-336 and
      562-564 (L5), 548-558 (L6), 585 (L9 — the ninth)
    - `results/phase14_calibration_report.md` lines 311, 322 (L3) and **289-307 (the WR-01
      correction block)**
    - `results/phase13_ab_report.md` lines 209-214 and 222-223 (L7)
    - `docs/REPORT.md` lines 61-67 and 373-375 (L8)
    - `scripts/train_tokenizer.py` line 31 — `CORPUS_PATH` pointing at
      `tests/fixtures/tiny_corpus.txt`; and the file itself, to confirm its byte size
    - `results/phase13_ab_report.md` lines 261-267 — the in-repo verbatim-quote-with-attribution
      register (*"Phase 12 §8 concluded, verbatim and unamended:"* then a blockquote). Every entry
      uses this form.
  </read_first>
  <action>
Append at EOF a new sibling section — `## Milestone 2 Limitations — Nine Honest Negatives,
Quoted` — aggregating every bound on the v2.0 claims so a reader learns all of them without
opening `results/`.

**Opening line (R2):** state that the section carries **NINE** honest negatives, with a one-clause
provenance note that the ninth was surfaced during Phase 15 research from data that predates this
phase. That transparency is required for the same reason the L8 correction names its evidence: a
reader should be able to trace not just what each limitation says, but when and how it entered
the record.

**Per-entry format**, following `results/phase13_ab_report.md:261-267`: a **bolded lead-in
phrase** naming what the limitation bounds, an attribution sentence naming the source file and
its section heading, then a **blockquote of the source's exact wording**. Not summarized, not
softened, not reordered for flow. If an entry's original wording is long, **truncate visibly** —
ellipsis plus a link to the full passage — rather than paraphrase to shorten.
**Paraphrase-to-shorten is precisely the drift vector this rule exists to close.**

**Ordering — by WHICH CLAIM each limitation bounds**, never by severity and never by how
comfortable it is to read, so the ordering itself does not editorialize. Use exactly this order:

  1. **Bounding "EWC mitigates forgetting":**
     - **L1** — `λ* = None`. Source: `results/finetune_smoke_report.md`,
       `## Stage 3 — λ Sweep (EWC-03)`, line 159. Cite the section heading, **not** a section
       number — there is no `## 8` heading in that report; "§8" is the rule numbering inside
       `scripts/finetune_smoke.py`.
     - **L7** — the noise floor not re-verified at the production budget. Source:
       `results/phase13_ab_report.md`, `## Threats to Validity` →
       `### 2. The noise floor's measurement regime — and where it does not reach`, lines 209-214.
       Include the closing line from 222-223 about corroboration from a free check rather than a
       re-measurement.
  2. **Bounding "memory lives in the weights":**
     - **L4** — the 1/1944 question-fairness control. Source:
       `results/phase14_recall_report.md`, `## Control 1 — Question Fairness (D-11.1)`, line 378.
     - **L2** — the base cannot extract a fact from its own context. Source: same file,
       `## Threats To Validity` → `### 3. ...`, lines 566-571 (the threats restatement is shorter
       and self-contained — **prefer it**), with a link to part (a) at lines 398-401 for the full
       passage. L4 is the number and L2 is what it costs the claim: keep them separate but
       adjacent.
     - **L6** — the reversal-curse scope narrowing. Source: same file, `## Threats To Validity` →
       `### 1. The held-out set is deliberately scoped (D-22)`, lines 555-558.
     - **L5** — the soft tier excluded from the gate. Source: same file, `## Threats To Validity`
       → `### 2. ...`, lines 562-564 (the threats version states the consequence, not just the
       rule — **prefer it**).
  3. **Bounding "…without damaging the base":**
     - **L3** — the +224.81% PPL collapse without replay. Source:
       `results/phase14_calibration_report.md`, `## Derivation 3 — PersonaChat Replay (D-15)`,
       line 322. **Long — truncate visibly** after "…should not be read as 'replay solves it'."
       with an ellipsis and a link. **Pitfall 8 is mandatory here:** this number carries a WR-01
       correction block at lines 289-307 — the recorded figures are the unmasked ones and the
       dead-ids-forbidden re-measurement gives `+224.5330%`. **Quote +224.81% AND state that the
       correction block exists**, naming its line range. Quoting the number without its WR-01
       context is exactly the softening D-15 forbids.
     - **L9 (the ninth, R2)** — the residual +27.16% off-topic collapse. Source:
       `results/phase14_recall_report.md` line 585, verbatim quote, visible truncation only. It
       belongs **here**, adjacent to L3: L3 is the pre-replay measurement and L9 is the
       post-replay real-run measurement — arguably its residual half. Its lead-in notes that it
       was recorded alongside the passed gate as a named qualification, not folded into it.
  4. **Bounding "13.9M-parameter from-scratch base":**
     - **L8** — 547 live ids / 7645 dead embedding rows, carried forward from v1.0. Source:
       `docs/REPORT.md`, `## Decision: Byte-Level BPE from Scratch, Vocabulary Locked Before Model
       Sizing`, lines 61-67, plus the parameter-count consequence at lines 373-375 (2,935,680
       parameters, ~21% of the headline count, are dead embedding rows).

       **R1 — the correction note.** Quote lines 61-67 **verbatim, including the phrase "the
       bounded TinyStories corpus"**, then immediately follow the blockquote with a clearly
       labeled, dated Phase 15 correction note. The note MUST:
       - be visibly separate from the quote and dated (the D-17 register), and state that it
         corrects the attribution **without amending the quoted v1.0 text**, which stands as
         written per D-13;
       - **name the specific verification performed** — verified against
         `scripts/train_tokenizer.py:31`, which sets `CORPUS_PATH` to
         `tests/fixtures/tiny_corpus.txt` (**11,469 bytes**), not the full TinyStories corpus the
         original wording implies; `artifacts/tokenizer.json` (5,648 bytes, dated 2026-06-04) is
         the frozen production artifact built from that fixture;
       - record that this was a tracked, open tech-debt item whose recorded home was this phase
         (`.planning/STATE.md` Deferred Items; `.planning/milestones/v1.0-MILESTONE-AUDIT.md:18`
         WR-04/WARNING-3);
       - note that `README.md` carried the same misattribution and that its v2.0 rewrite states
         the corrected attribution directly, since README carries no D-13 protection.

       Confirm the 11,469-byte figure yourself before writing it — a correction that gets its own
       evidence wrong is worse than the error it corrects.

**Verify every quote is byte-exact** before finishing: for each of the nine, confirm the quoted
string is a literal substring of the cited source file (for a visibly-truncated quote, confirm
each fragment either side of the ellipsis is a substring, in order). Plan 15-08 makes this
permanent as `test_limitations_quotes_are_verbatim`; do the check by hand here so the section is
correct before the test exists.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import pathlib;r=pathlib.Path('docs/REPORT.md').read_text();src={'l1':('results/finetune_smoke_report.md','EWC not demonstrable at this budget'),'l4':('results/phase14_recall_report.md','1/1944 = 0.0005'),'l9':('results/phase14_recall_report.md','+27.16%'),'l3':('results/phase14_calibration_report.md','+224.81%'),'l7':('results/phase13_ab_report.md','NOT re-verified at the 4000-step')};print(all(q in r and q in pathlib.Path(f).read_text() for f,q in src.values()), 'NINE' in r or 'nine' in r, 'tiny_corpus.txt' in r, '11,469' in r)"</automated>
  </verify>
  <acceptance_criteria>
    - The command above prints `True True True True`.
    - The section states nine, not eight: `grep -qiE 'nine (honest )?negatives' docs/REPORT.md`
      succeeds, and the opening carries the one-clause provenance note about the ninth.
    - All nine entries are present and in the claim-bound order L1, L7, L4, L2, L6, L5, L3, L9, L8
      — verify by reading the section top to bottom and confirming the four claim-group lead-ins
      appear in the order: EWC mitigates forgetting, memory lives in the weights, without damaging
      the base, 13.9M from-scratch base.
    - L8's blockquote contains the literal phrase `the bounded TinyStories corpus` (the quote is
      NOT paraphrased to fix it) AND is immediately followed by a dated correction note naming
      `scripts/train_tokenizer.py:31`, `tests/fixtures/tiny_corpus.txt` and `11,469`.
    - L3's entry contains both `+224.81%` and a reference to the WR-01 correction block with its
      line range, plus the corrected `+224.5330%` figure.
    - Every quote is a byte-exact substring of its cited source. Check each of the nine with
      `grep -qF` against both `docs/REPORT.md` and the source file; for visibly-truncated quotes,
      check each fragment separately.
    - `git show HEAD:docs/REPORT.md` piped through `head -421` still matches the working file's
      first 421 lines byte-for-byte.
    - `.venv/bin/pytest -q` shows no regression.
  </acceptance_criteria>
  <done>Nine limitations, each a byte-exact quote of its source, ordered by which claim they bound; L8 quoted verbatim with a dated evidence-naming correction; L3 quoted with its WR-01 context.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed evidence reports → the report's quotes | Prose transcription is where a recorded negative can be quietly softened; the source files are the authority |
| v1.0 text → v2.0 extension | D-13 makes the pre-boundary text immutable; an edit above the marker is a silent rewrite of shipped evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-05 | Tampering | A limitation paraphrased to shorten, softened, or reordered by severity | mitigate | D-15: exact quotes with attribution in the `phase13_ab_report.md:261-267` register, visible truncation only, ordering fixed by which claim each limitation bounds. Every quote is hand-verified as a byte-exact substring in this plan and made permanent by `test_limitations_quotes_are_verbatim` in Plan 15-08. |
| T-15-15 | Tampering | A v1.0 section silently edited under cover of the v2.0 extension | mitigate | The first 421 lines are asserted byte-identical against `git show HEAD:docs/REPORT.md` in every task's acceptance criteria. The boundary marker is an insertion at 421, not a rewrite. |
| T-15-16 | Repudiation | The L8 correction asserted without evidence | mitigate | R1 requires the correction to name the specific verification — `scripts/train_tokenizer.py:31`, `tests/fixtures/tiny_corpus.txt`, 11,469 bytes — held to the same evidentiary standard as the nine quotes it sits beside, and the executor confirms the byte count independently before writing it. |
| T-15-17 | Tampering | Quoting +224.81% without its WR-01 correction block | mitigate | Pitfall 8 is written into the task action and into the acceptance criteria: the entry must carry both the recorded unmasked figure and the existence and line range of the correction block, plus the `+224.5330%` re-measurement. |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed; this plan writes markdown only. |
</threat_model>

<verification>
- `docs/REPORT.md`'s first 421 lines are byte-identical to `git show HEAD:docs/REPORT.md`
- The boundary marker sits between `## Reproducibility` and
  `## Limitations and the Milestone 2 Roadmap`, dated 2026-06-10, naming `m1-demo-v1`
- At least 21 `## Decision:` sections exist (14 original + at least 7 new)
- The results narrative carries the real recall, retention, inflation and correlation numbers and
  both figure paths
- The Limitations section carries nine byte-exact quotes in claim-bound order
- `.venv/bin/pytest -q` → no regression; `make lint` → clean
</verification>

<success_criteria>
- Every v1.0 word of `docs/REPORT.md` is intact, with a dated boundary marker placed before the
  stale future-tense Milestone 2 roadmap.
- The Decision sections and the results narrative are strictly non-overlapping: a reader can skip
  either and still get a complete artifact.
- The figures' full-detail disclosure names the three concrete D-03 confounds, the specific
  layer/projection driving each color range, and why the Fisher panel has its own scale.
- The correlation verdict is cited terse with a link to its full pre-registration table, and a
  miss is stated as plainly as a pass.
- Nine honest negatives, each a byte-exact quote of its source, ordered by which claim they bound,
  with L8 quoted verbatim and corrected by a dated, evidence-naming note.
</success_criteria>

<output>
Create `.planning/phases/15-figures-writeup/15-05-SUMMARY.md` when done.
Record: the exact heading strings of the new sections (Plan 15-06's README links to the
Limitations section by name), and the nine quote strings as written, for Plan 15-08's test.
</output>
