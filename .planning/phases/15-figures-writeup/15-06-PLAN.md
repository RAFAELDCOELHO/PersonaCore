---
phase: 15-figures-writeup
plan: 06
type: execute
wave: 4
depends_on: ["15-03", "15-04"]
files_modified:
  - README.md
autonomous: true
requirements: [DOC-02]
must_haves:
  truths:
    - "README presents PersonaCore as a shipped Milestone 2, not as a Milestone 1 with M2 upcoming"
    - "Three headline numbers — recall rate against its gate, retention delta from the A/B, and the tokenizer-inflation tax — each carry their qualifier in the same sentence or bullet"
    - "No headline number is followed by a bare 'see Limitations' pointer"
    - "The tokenizer bullet states the corrected training-corpus attribution directly"
    - "Both v2.0 figures appear on the front page"
  artifacts:
    - path: "README.md"
      provides: "v2.0 front door with three inline-qualified headline numbers"
      contains: "0.3483"
  key_links:
    - from: "README.md"
      to: "docs/REPORT.md"
      via: "terse-form headline numbers linking to the full-form Limitations section"
      pattern: "docs/REPORT.md"
    - from: "README.md"
      to: "results/phase15_fisher_ewc.png"
      via: "embedded figure"
      pattern: "phase15_"
---

<objective>
Rewrite `README.md` as the v2.0 front door: three headline numbers, each qualified inline at the
same density as the existing 547-live-ids bullet, plus the two signature figures.

Purpose: the front page is where a claim is most likely to be read without its context, so it is
where the caveat matters most. D-16 forbids the pattern of a bold number followed by a separate
"see Limitations" pointer — the qualifier goes inside the same breath as the claim.

Output: `README.md`, rewritten. Unlike `docs/REPORT.md`, README carries **no D-13 protection** —
it is fully rewritable, and its stale Milestone-1 framing must go.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-figures-writeup/15-CONTEXT.md
@.planning/phases/15-figures-writeup/15-RESEARCH.md
@.planning/phases/15-figures-writeup/15-PATTERNS.md
</context>

<resolved_decisions>
**R1 applies here in its README half.** `README.md:30-31` carries the same factually wrong
attribution as `docs/REPORT.md:61-63` — *"the bounded TinyStories corpus exhausts its mergeable
pairs"*. **README is NOT covered by D-13's untouched clause.** Its v2.0 rewrite therefore states
the **corrected attribution directly**, in the bullet itself: the production tokenizer was
trained on the 11,469-byte `tests/fixtures/tiny_corpus.txt` (`scripts/train_tokenizer.py:31`),
not on the full TinyStories corpus. No quote, no correction note — just the correct fact, stated
plainly, at the same density as everything around it.

The verbatim v1.0 wording plus its dated correction note lives in `docs/REPORT.md`'s Limitations
section (Plan 15-05), because that file IS D-13-protected. README simply gets it right.
</resolved_decisions>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the framing — Milestone 2 shipped, not upcoming</name>
  <files>README.md</files>
  <read_first>
    - `README.md` — the whole 111-line file. Note specifically: lines 4-7 (*"This repository is
      **Milestone 1** … the weight-memory mechanism itself (from-scratch LoRA + EWC) is
      **Milestone 2, upcoming**"*), lines 11-27 (`## Results at a glance`), lines 29-33 (the
      547-live-ids bullet — **the literal density target**), and lines 97-113
      (`## Roadmap — Milestone 2 (upcoming)`, closing with *"Until then, the demo above is exactly
      what it claims to be … no chat tuning, no personalization yet"*)
    - `README.md` lines 15-16 and 21-23 — the two other density registers: a number **with its
      denominator and the script that produced it** (*"2.1066 over 12,636,922 scored target tokens
      (50k-step `best.pt`, computed by `scripts/evaluate.py`)"*), and a claim **with its caveat and
      a link to the full form** (*"the full four-run cohort (with its honest reduced-budget caveat)
      is in [docs/REPORT.md]"*) — the second is exactly D-16's terse-form/full-form asymmetry
    - `.planning/phases/15-figures-writeup/15-CONTEXT.md` D-16 and D-04
    - `.planning/phases/15-figures-writeup/15-RESEARCH.md` lines 1261-1291 — the density target
      verbatim and the note that the rest of README is entirely M1-framed and must change
    - `.planning/PROJECT.md` — the core-value statement the new opening restates
  </read_first>
  <action>
Rewrite README's framing so PersonaCore reads as a project whose thesis is **demonstrated**, not
pending.

**Opening (lines 1-9).** Replace the "This repository is Milestone 1 … Milestone 2, upcoming"
paragraph. The new opening states the thesis and what is now proven: personalization lives in the
model weights — no databases, no vector stores, no external files — demonstrated by a
from-scratch LoRA adapter that recalls taught facts in a fresh process with an empty prompt, and
by a from-scratch EWC A/B showing the base is not destroyed in the process. Keep the demo GIF
reference. Name what Milestone 1 contributed (the correct, tested 13.9M from-scratch base) so the
v1.0 work is not erased — it is the foundation, not a superseded draft.

**`## Roadmap — Milestone 2 (upcoming)` (lines 97-113).** This section is now false. Replace it
with a section describing what Milestone 2 **delivered** — from-scratch LoRA over the six named
projections, from-scratch EWC through the `assemble_loss` seam, the unconfounded no-forgetting
A/B, and the clean-room teach-then-recall demo with its live on/off toggle — and a short
honest-next-steps note in its place. Delete the closing *"no chat tuning, no personalization yet"*
sentence; it is no longer true.

**Preserve** the v1.0 headline numbers that are still true (13.9M parameters, PPL 2.1066 over
12,636,922 scored tokens, ~100 tok/s on laptop CPU, trained on-device, 100% from-scratch) — they
remain the foundation's claims and keep their existing inline density.

**Embed both v2.0 figures** where a reader meets the thesis: `results/phase15_fisher_ewc.png`
(VIZ-03) with a one-line caption stating that the naive and EWC panels share one color scale and
the Fisher panel does not, and `results/phase15_adapter_delta.png` (VIZ-02) with a one-line
caption stating it is not comparable to the VIZ-03 panels. Each caption is the **terse** form;
the full reasoning lives in `docs/REPORT.md` (D-04 asymmetry).

Do not add a "see Limitations" pointer anywhere in this task — Task 2 places the qualifiers
inline where they belong.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import pathlib;t=pathlib.Path('README.md').read_text();print('Milestone 2, upcoming' not in t, 'no personalization yet' not in t, 'phase15_fisher_ewc.png' in t, 'phase15_adapter_delta.png' in t, '2.1066' in t, '12,636,922' in t)"</automated>
  </verify>
  <acceptance_criteria>
    - The command above prints six `True` values.
    - `grep -qi 'upcoming' README.md` returns no hit inside any Milestone 2 heading — verify with
      `grep -n -i upcoming README.md` that no remaining hit frames LoRA, EWC, teach-then-recall or
      the A/B as future work.
    - The surviving v1.0 claims keep their existing inline density — `2.1066` still appears with
      `12,636,922` and `scripts/evaluate.py` in the same bullet.
    - Both figure paths resolve to files that exist on disk.
    - Each figure caption is one line, not a paragraph (D-04 terse form).
  </acceptance_criteria>
  <done>README presents Milestone 2 as delivered, both v2.0 figures appear with terse captions, and no sentence still frames the weight-memory mechanism as future work.</done>
</task>

<task type="auto">
  <name>Task 2: The three headline numbers, each qualified inline at 547-live-ids density</name>
  <files>README.md</files>
  <read_first>
    - `README.md` (current state after Task 1), especially the 547-live-ids bullet — the literal
      density target: a bold claim, then **immediately, inside the same bullet, in parentheses**,
      the full arithmetic AND the thing that makes it honest. Not a footnote, not a link, not a
      following sentence.
    - `results/phase14_recall_report.md` `## Recall Results — Core Tier` (lines 56-60) and the
      verdict at line 575
    - `results/phase13_ab_report.md` `## 2×2 Result` (53-64) and `## Gate Verdict` (106-123)
    - `results/inflation_report.md` `## D-08 Metrics` metric 1 (line 15), `## Baseline` (30-31),
      and the preamble at lines 8-9 (the "only meaningful against the TinyStories baseline
      recomputed in this same run" qualifier)
    - `scripts/train_tokenizer.py` line 31 — `CORPUS_PATH`; and the fixture file, to confirm its
      byte size before writing 11,469
    - `.planning/phases/15-figures-writeup/15-05-PLAN.md` `<pinned_headings>` — the exact
      heading string of `docs/REPORT.md`'s new Limitations section,
      `## Milestone 2 Limitations — Nine Honest Negatives, Quoted`, for the full-form links.
      **Read the PLAN, not `15-05-SUMMARY.md`:** Plan 15-05 runs in this same wave, so its
      SUMMARY may not exist yet. The heading is authored, not discovered, so it is pinned in
      plan text and there is nothing to wait for.
    - `.planning/phases/15-figures-writeup/15-04-SUMMARY.md` — the correlation verdict, in case
      the front page mentions it
  </read_first>
  <action>
Place **three headline numbers** in `## Results at a glance`, each with its qualifier **in the
same sentence or bullet**, at the same density as the 547-live-ids line. **Never a bold number
followed by a separate "see Limitations" pointer.** Where a number carries one of the report's
nine limitations, that limitation's **short form belongs inline here**, with the full quoted
version still living in `docs/REPORT.md`'s Limitations section — README is the terse form,
Limitations is the full form (the same asymmetry D-04 locks for the figures).

  1. **Recall rate against its gate.** Held-out **0.3483** (326/936) against a pre-registered
     **0.2000**, and taught **0.4921** (496/1008) against **0.2486**, with the closed-book control
     at exactly **0/2430**. Thresholds came from a **disjoint** calibration fact set before the run
     existed. Inline qualifiers required — this number carries limitations L2, L4, L5 and L6 —
     covering, in the same bullet: the **proper-noun core only** (the soft preference tier is
     excluded from the gate), and the held-out set **deliberately omits reversed phrasings**, so
     the result demonstrates generalization within that scope and makes no claim about reversed
     recall. Link to the full form in `docs/REPORT.md` **in addition to**, never instead of, the
     inline qualifier.

  2. **Retention delta from the A/B.** Naive retention PPL **8.52417066884246** vs EWC
     **3.8911400839446597** from a step-0 anchor of **2.107553076833866** — drift **+6.416618** vs
     **+1.783587**, a **3.6×** difference, clearing the pre-registered margin at **33.61×**.
     Inline qualifiers required — this number carries L1 and L7 — covering, in the same bullet:
     the claim is **teacher-forced retention PPL, not free-running story generation** (both arms
     leak role tokens mid-story), and the **noise floor it is measured against was not re-verified
     at the 4000-step production budget**. Also state EWC's acquisition cost, **+0.380556** PPL,
     as descriptive with no gate.

  3. **The Phase 11 tokenizer-inflation tax.** Dialogue **3.229** tokens/word (4,800,385 utterance
     tokens over 1,486,754 whitespace words) against a TinyStories baseline of **2.860** recomputed
     in the same run with the same tokenizer and word rule — a **1.129×** relative inflation,
     inside the ≤1.2× GO band at a measured fit of 0.9996. Inline qualifier required: the ratio is
     **only meaningful against that same-run baseline** and is never comparable to another
     tokenizer.

**Also correct the tokenizer bullet (R1).** The existing 547-live-ids bullet keeps its structure
and density but states the **correct attribution**: the production tokenizer was trained on the
**11,469-byte `tests/fixtures/tiny_corpus.txt`** fixture (`scripts/train_tokenizer.py:31`) — not
on the full TinyStories corpus — which is why only 283 of 7,928 requested merges were learned and
7,645 of the 8,192 rows are reserved capacity. Keep the arithmetic (256 bytes + 283 learned merges
+ 8 specials = 547 live ids) and the tiktoken-oracle clause. Confirm the 11,469 byte count
yourself before writing it.

**Density self-check before finishing.** Read each of the three bullets aloud. If a reader who
stops at the end of the bullet would walk away with an unqualified claim, the qualifier is in the
wrong place. There must be **zero** occurrences of a headline number whose only caveat is a
pointer elsewhere.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import pathlib,re;t=pathlib.Path('README.md').read_text();nums=['0.3483','0.2000','0.4921','0.2486','0/2430','8.52417066884246','3.8911400839446597','3.229','2.860','1.129','11,469','tiny_corpus.txt'];print(all(n in t for n in nums), 'bounded TinyStories corpus' not in t)"</automated>
  </verify>
  <acceptance_criteria>
    - The command above prints `True True` — every headline value present, and the wrong
      attribution phrase `bounded TinyStories corpus` is gone from README.
    - Each of the three headline numbers appears in a bullet that also contains its qualifier.
      Verify by inspection: locate each of `0.3483`, `8.52417066884246` and `3.229`, and confirm
      the same bullet contains, respectively, a proper-noun-core/reversed-phrasing clause; a
      teacher-forced-not-free-running clause plus a noise-floor clause; and a
      same-run-baseline-only clause.
    - **Zero** headline numbers whose only caveat is a pointer: `grep -n 'see Limitations' README.md`
      returns nothing, and no bullet containing one of the three numbers ends without a qualifier.
    - Every number matches its source report exactly — cross-check `0.3483` and `0.4921` against
      `results/phase14_recall_report.md`, `8.52417066884246` and `3.8911400839446597` against
      `results/phase13_ab_report.md`, and `3.229` / `2.860` / `1.129` against
      `results/inflation_report.md`.
    - The link to `docs/REPORT.md`'s Limitations section uses the heading string pinned in
      `15-05-PLAN.md` `<pinned_headings>` —
      `## Milestone 2 Limitations — Nine Honest Negatives, Quoted` — with a GitHub anchor
      derived from it (lowercase, spaces to hyphens, drop characters that are neither
      alphanumeric nor hyphen). Plan 15-05 writes that heading in this **same wave**, so the
      anchor may still dangle when this plan finishes; Plan 15-08's wave-5
      `test_headline_numbers_match_sources` asserts it resolves against the finished
      `docs/REPORT.md`. Do not block on `docs/REPORT.md` here.
    - `.venv/bin/pytest -q` shows no regression.
  </acceptance_criteria>
  <done>Three headline numbers, each carrying its qualifier in the same bullet at 547-live-ids density; the tokenizer bullet states the corrected corpus attribution; no number relies on a pointer for its caveat.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed evidence reports → README claims | The front page is the most-read and least-contextualized surface; a number transcribed here without its qualifier is the highest-leverage overclaim in the repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-18 | Tampering | A headline number presented without its qualifier | mitigate | D-16: the qualifier goes in the same sentence or bullet, at 547-live-ids density; a bare "see Limitations" pointer is forbidden and is checked by grep in the acceptance criteria. Plan 15-08's `test_headline_numbers_match_sources` makes the value-matching permanent. |
| T-15-19 | Tampering | A README number drifting from its source report | mitigate | Every value is cross-checked against its source report file and line in this plan, and pinned permanently by `test_headline_numbers_match_sources`, which asserts each README number also appears in its cited source. |
| T-15-16 | Repudiation | Repeating the known-wrong tokenizer-corpus attribution | mitigate | R1: README is not D-13-protected, so it states the corrected attribution directly, naming `scripts/train_tokenizer.py:31` and the 11,469-byte fixture; the executor confirms the byte count independently. The acceptance criteria assert the wrong phrase is absent. |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed; this plan writes markdown only. |
</threat_model>

<verification>
- Every one of the three headline numbers appears in README with an inline qualifier in the same
  bullet
- `grep -n 'see Limitations' README.md` returns nothing
- `grep -F 'bounded TinyStories corpus' README.md` returns nothing
- Both v2.0 figure paths resolve to files on disk
- `.venv/bin/pytest -q` → no regression; `make lint` → clean
</verification>

<success_criteria>
- README presents the weight-memory thesis as demonstrated, with Milestone 1 named as the
  foundation rather than erased.
- Three headline numbers — recall against its gate, retention delta, tokenizer-inflation tax —
  each carry their qualifier in the same sentence or bullet at 547-live-ids density.
- Where a headline number carries one of the nine limitations, that limitation's short form is
  inline; the full quoted version stays in `docs/REPORT.md`.
- The tokenizer bullet states the corrected training-corpus attribution directly.
- Both v2.0 figures appear with terse one-line captions, with the full reasoning in the report.
</success_criteria>

<output>
Create `.planning/phases/15-figures-writeup/15-06-SUMMARY.md` when done.
Record the three headline numbers as written and their source file/line, for Plan 15-08's
`test_headline_numbers_match_sources`.
</output>
