---
phase: 22-dp-sgd-core-accountant-and-the-correctness-battery
plan: 10
type: execute
wave: 5
depends_on: ["22-05", "22-08"]
files_modified:
  - scripts/teach_persona.py
  - tests/test_phase22_wiring.py
autonomous: true
requirements: [DPSGD-01, DPSGD-03]
validates: [V-13, V-23]

must_haves:
  truths:
    - "D-08: Phase 22 wires FOUR PATHS at `scripts/teach_persona.py::main()` on the TWO DP arms (`dp_n8`, `dp_n64`) and proves it by an END-TO-END CPU fixture-scale run that completes and writes NO scored artifact"
    - "D-08: sigma and C arrive keyword-only with NO DEFAULT on both the `dp_fn` constructor and the CLI. NO literal exists anywhere in Phase 22's tree, so there is nothing for Phase 23 to override and nothing to drift — Phase 20's Z boundary stays untouched because Phase 22 never names a value"
    - "D-08's load-bearing reason: Phase 23's FIRST ACT IS A MEASUREMENT. If the wiring lands there, its first executed run is simultaneously the first test of four never-executed integration paths, so a wiring bug and a DP-correctness bug land in the same artifact, indistinguishable — destroying DPSGD-06's entire purpose"
    - "Phase 21 IN-04 is closed: `replay_window_budget`'s docstring claims a caller (`train()`'s replay seam via its caller`) that did not exist. This plan makes the claim TRUE and updates the docstring in the same edit"
    - "Wiring is not executing: this costs nothing against the roadmap's no-M3-time boundary"
    - "D-04: the seam's three property refusals see a model that has already been through `mark_only_lora_trainable` — the DP object is constructed AFTER the freeze, never before"
  artifacts:
    - path: "scripts/teach_persona.py"
      provides: "the four D-08 wirings on the DP arms plus the no-default sigma/C CLI branch"
      contains: "dp_fn"
    - path: "tests/test_phase22_wiring.py"
      provides: "V-23 — the end-to-end CPU run completes and results/ is unchanged"
      contains: "def test_end_to_end_writes_no_scored_artifact"
  key_links:
    - from: "scripts/teach_persona.py"
      to: "src/personacore/training/loop.py::train"
      via: "the four new kwargs on the DP arms: fact_bin/n_facts, grad_accum_steps, replay_*, dp_fn"
      pattern: "dp_fn="
    - from: "scripts/teach_persona.py::replay_window_budget"
      to: "train()'s replay seam"
      via: "replay_window_budget(n_facts) // BLOCK_SIZE"
      pattern: "replay_window_budget"
---

<objective>
Wire all four never-executed integration paths at the production caller, on the two DP arms, and
prove them by an end-to-end CPU fixture-scale run that completes and leaves `results/` unchanged.

Purpose: `python scripts/teach_persona.py dp_n8` is CLI-reachable today and produces a non-DP
adapter under a DP name, through the flat loader, at `grad_accum_steps = 1`, with no replay and no
DP. Phase 23 is structurally unable to proceed without this wiring — but **Phase 23's first act is a
measurement**, so if the wiring lands there its first executed run is simultaneously the first test
of four never-executed paths, and a wiring bug and a DP-correctness bug arrive in the same artifact,
indistinguishable. That destroys DPSGD-06's entire purpose.

Output: the four wirings plus the no-default σ/C CLI branch in `scripts/teach_persona.py`, and V-23
in `tests/test_phase22_wiring.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-dp-sgd-core-accountant-and-the-correctness-battery/22-CONTEXT.md
@.planning/phases/22-dp-sgd-core-accountant-and-the-correctness-battery/22-PATTERNS.md
@.planning/phases/22-dp-sgd-core-accountant-and-the-correctness-battery/22-VALIDATION.md
</context>

<environment>
**Python 3.11 venv is MANDATORY.** Commands run as `.venv/bin/python -m pytest ...`. CPU only —
this plan WIRES, it does not run a real training job. Wiring is not executing.
</environment>

<tasks>

<task type="auto">
  <name>Task 1: The four wirings at the `train()` call, on the DP arms only</name>
  <files>scripts/teach_persona.py</files>
  <read_first>
    - `scripts/teach_persona.py:1167-1204` — the `train()` call in full. **Confirm all four measured
      gaps against the real file**: no `grad_accum_steps=` inside `TrainConfig(...)`, no
      `replay_bin`/`replay_mask_bin`/`replay_windows`, no fact bin, no `dp_fn`. Three existing kwargs
      already carry multi-line reason comments — that is this call site's register
    - `scripts/teach_persona.py:1052-1075` — `train_arm`'s signature and its docstring's rule that
      new kwargs are threaded THROUGH rather than only used locally
    - `scripts/teach_persona.py:1140-1157` — `inject_lora` -> `mark_only_lora_trainable` -> the
      closed-form trainable census with `SystemExit`. **The DP object must be constructed AFTER
      `mark_only_lora_trainable`** or D-04's refusal 1 fires on the caller's own model
    - `scripts/teach_persona.py:180-204` — `replay_window_budget`. **It returns TOKENS**;
      `train(replay_windows=)` wants **WINDOWS**, hence `replay_window_budget(n_facts) // BLOCK_SIZE`.
      `:184`'s docstring claims *"``train()``'s replay seam via its caller"* — **false today**,
      made true by this plan. `REPLAY_WINDOWS_PER_FACT = 4` is at `:177`
    - `scripts/teach_persona.py:249-260` — `ARMS`, `DP_ARMS = ("dp_n8", "dp_n64")`
    - `scripts/teach_persona.py:874-887` — `arm_spec`'s DP branches; `replay_ratio = 0.0` is
      load-bearing under Phase 21 D-10
    - `scripts/teach_persona.py` — `build_arm_bins`'s returned `paths` dict. **Resolve the real key
      names for the token bin, the mask bin and the FACT bin from the function's own code.** Do not
      guess a key name; this project's plans have repeatedly named paths the code refuses
    - `src/personacore/training/loop.py` — the `fact_bin`/`n_facts`/`dp_fn` kwargs as landed
    - `src/personacore/privacy/dpsgd.py` — `DPSGD.__init__`'s keyword-only `sigma` / `clip_norm`
  </read_first>
  <action>
Thread `dp_sigma` and `dp_clip_norm` from `main()` through `train_arm(...)` (both **keyword-only with
no default** — `dp_sigma`, `dp_clip_norm`), following `train_arm`'s own docstring rule that a new
kwarg is threaded THROUGH rather than used only locally.

Add the four wirings to the `train()` call at `:1167`, **each guarded on `arm in DP_ARMS`** and each
with a comment naming its decision id (this call site's established register):

  1. **Fact-aligned routing (D-08 wiring 1).** `fact_bin=<the fact-bin path from the `paths` dict>`
     and `n_facts=len(facts)`. Resolve the `paths` key from `build_arm_bins`'s own return, not from
     memory. Comment records the measured gap: `get_batch_fact_aligned` had NO path through `train()`
     at all — zero hits for `fact_bin`/`fact_aligned`/`align_facts` in `loop.py`, and its sole
     non-test caller was the reporting driver `scripts/phase21_unit_record.py`.
  2. **`grad_accum_steps` (D-08 wiring 2).** Add `grad_accum_steps=len(facts)` inside the
     `TrainConfig(...)` literal. Comment records that the phrase appeared **9 times in this file's
     prose and 0 times in its code**, so the arm silently inherited `config.py:106`'s default `1` and
     SC2's "one micro-step = one privacy record" was prose at the only production caller.
     `loop.py`'s accum-agreement refusal (plan 22-08) now makes a disagreement loud.
  3. **The replay seam (D-08 wiring 3, closing IN-04).** `replay_bin=`, `replay_mask_bin=` from the
     replay corpus paths, and
     `replay_windows=replay_window_budget(len(facts)) // BLOCK_SIZE`. Comment names Phase 21
     D-11/D-24 and states the unit conversion explicitly: `replay_window_budget` returns TOKENS,
     `train()` wants WINDOWS. **In the same edit, update `replay_window_budget`'s docstring at
     `:184`** — its claim *"``train()``'s replay seam via its caller"* was false and is now true;
     say so, and name this plan, rather than leaving the claim false in the other direction.
  4. **`dp_fn` (D-08 wiring 4).** Construct `DPSGD(model, sigma=dp_sigma, clip_norm=dp_clip_norm,
     device=runtime.device)` **after** `mark_only_lora_trainable(model)` at `:1147` and after the
     existing census at `:1148-1156`, and pass it as `dp_fn=`. Comment records D-04's trap: the
     existing census here is a property of ONE CALLER; the seam re-checks it as a property of the
     MECHANISM, which is why both exist and neither is redundant.

On non-DP arms all four stay absent, so every v2.0/v3.0 arm's behaviour is byte-unchanged. Add a
single explicit `dp_kwargs = {...} if arm in DP_ARMS else {}` splat rather than four separate
conditionals, so the DP-vs-non-DP boundary is one readable line and a future reader cannot wire three
of four by accident.

**No numeric σ or C literal may appear anywhere in this file.** `dp_sigma` and `dp_clip_norm` are
required values that arrive from the CLI (Task 2). If either is `None` on a DP arm, refuse with
`SystemExit` naming Phase 20's Z boundary — Phase 23 supplies both from
`scripts/mitigation_budget.py`, and Phase 22 names no value anywhere in its tree.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase14_teaching.py tests/test_phase21_replay_volume.py tests/test_phase21_multiplicity.py tests/test_phase22_wiring.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "grad_accum_steps" scripts/teach_persona.py` now returns at least one CODE line inside `TrainConfig(...)`, not only prose
    - `grep -n "fact_bin=\|n_facts=\|replay_windows=\|dp_fn=" scripts/teach_persona.py` returns all four
    - `grep -nE "sigma\s*=\s*[0-9]|clip_norm\s*=\s*[0-9]" scripts/teach_persona.py` returns nothing
    - `replay_window_budget`'s docstring no longer claims a caller that does not exist; `grep -n "via its caller" scripts/teach_persona.py` shows updated text
    - the `DPSGD(...)` construction appears AFTER `mark_only_lora_trainable(model)` in file order
    - `.venv/bin/python -m pytest tests/ -q` reports 0 failed
    - `git diff --exit-code -- scripts/mitigation_gate.py scripts/mitigation_unit.py scripts/mitigation_accountant.py pyproject.toml` exits 0
  </acceptance_criteria>
  <done>All four paths are wired on `dp_n8`/`dp_n64` only, each with a decision-id comment; the DP object is constructed after the freeze; `replay_window_budget`'s false docstring claim is made true in the same diff; and no numeric σ or C exists in the file.</done>
</task>

<task type="auto">
  <name>Task 2: The CLI — sigma and C as REQUIRED arguments with no default</name>
  <files>scripts/teach_persona.py</files>
  <read_first>
    - `scripts/teach_persona.py:980-1014` — `USAGE` and `main()`. **`main()` is argv-slicing, NOT
      argparse.** `len(argv) != 1` at `:996` rejects extra tokens today, so σ/C need a new argv
      branch and `USAGE` must be extended in the same edit. `run_calibration(argv[1:])` at `:991` is
      the sub-mode precedent; `prefix="phase21" if arm in DP_ARMS else "phase14"` at `:1013` is the
      DP-arm branch idiom
    - `scripts/mitigation_gate.py` — its own discipline, quoted in D-08: *"every argument
      keyword-only, no defaults"*. This is the register to copy (read it; do NOT edit it — FROZEN)
  </read_first>
  <action>
Extend `main()`'s argv handling so the DP arms take σ and C as **required, explicitly-named,
no-default** arguments, and non-DP arms are byte-unchanged.

  - Keep the existing single-token path for non-DP arms exactly as it is: `len(argv) == 1` and
    `argv[0] in ARMS` and `argv[0] not in DP_ARMS` behaves identically to today.
  - For `argv[0] in DP_ARMS`, require exactly two additional `--name=value` tokens,
    `--sigma=` and `--clip-norm=`, in either order. Parse them by explicit string prefix matching in
    the argv-slicing register this file already uses; **do not introduce argparse** — the file's
    established shape is argv slicing and a second CLI idiom in one entry point is a maintenance
    trap. Refuse with `SystemExit(USAGE)` if either is missing, if an unknown token appears, or if
    either fails to parse as a float.
  - Refuse (`SystemExit`) on `sigma < 0` or `clip_norm <= 0` with a message naming the mechanism
    (the seam refuses these too — the CLI refusal is the early, cheap one, and the message should
    say the seam re-checks the property).
  - **No default value for either, anywhere.** The message on omission must state WHY: σ and C are
    Phase 23 resource parameters under Phase 20's Z boundary; Phase 22 names no value in its tree, so
    there is nothing for Phase 23 to override and nothing to drift. A default here would silently
    become the operating budget.
  - Extend `USAGE` with the DP form, e.g. a line showing
    `python scripts/teach_persona.py {dp_n8|dp_n64} --sigma=<float> --clip-norm=<float>`, built from
    `DP_ARMS` by the same `'|'.join(...)` interpolation `USAGE` already uses for `ARMS` — resolved
    from the tuple, never re-spelled as two literals.
  - Pass the parsed values into `train_arm(..., dp_sigma=..., dp_clip_norm=...)`.

Add CLI tests to `tests/test_phase22_wiring.py` (the file plan 22-08 created):
  - `test_dp_arm_requires_sigma_and_clip_norm` — `main(["dp_n8"])` raises `SystemExit`, and the
    message contains both flag names; same for each flag present alone.
  - `test_non_dp_arm_cli_is_unchanged` — the argv shape for a non-DP arm is accepted exactly as
    before (assert it does NOT raise `SystemExit(USAGE)`; stub the training call so the test does not
    train). This is the control that proves the new branch did not narrow the old one.
  - `test_usage_lists_the_dp_form` — `USAGE` contains every member of `DP_ARMS` and both flag names,
    with `DP_ARMS` read from the module rather than re-spelled.
  - `test_cli_names_no_sigma_or_clip_value` — AST-walk `scripts/teach_persona.py` and assert no
    `Constant` float is assigned to any name containing `sigma` or `clip`, with a meta-guard that the
    walk found at least one `Assign` node.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase22_wiring.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python scripts/teach_persona.py dp_n8` exits non-zero and prints a usage message naming both `--sigma` and `--clip-norm`
    - `grep -n "argparse" scripts/teach_persona.py` returns nothing
    - `USAGE` is built with `'|'.join(DP_ARMS)`-style interpolation, not two hand-typed arm names
    - `test_non_dp_arm_cli_is_unchanged` passes, proving the existing single-token path still works
    - `test_cli_names_no_sigma_or_clip_value` passes with its `Assign`-presence meta-guard
    - `.venv/bin/python -m pytest tests/test_phase22_wiring.py -x -q` exits 0
  </acceptance_criteria>
  <done>σ and C are required CLI arguments with no default, parsed in the file's existing argv-slicing register; `USAGE` names the DP form from `DP_ARMS`; the non-DP path is proven unchanged; and no numeric σ or C literal exists anywhere in the file.</done>
</task>

<task type="auto">
  <name>Task 3: V-23 — the end-to-end CPU run completes and writes NO scored artifact</name>
  <files>tests/test_phase22_wiring.py</files>
  <read_first>
    - `tests/test_phase22_wiring.py` — current state after Task 2
    - `tests/test_phase14_teaching.py` — how existing tests drive `teach_persona` at fixture scale
      without a full production run, and which module-level constants they monkeypatch
    - `scripts/teach_persona.py` — `train_arm`'s path construction and the `results/` write sites
    - `22-CONTEXT.md` D-08's "no scored artifact" boundary and `22-VALIDATION.md` V-23
  </read_first>
  <action>
Add `test_end_to_end_writes_no_scored_artifact` (**V-23**) to `tests/test_phase22_wiring.py`.

Drive `teach_persona`'s DP path at **fixture scale** on CPU — monkeypatch the module-level shape
constants (`MAX_STEPS`, `BATCH_SIZE`, `BLOCK_SIZE` and the model config) down to a size that runs in
seconds, following whatever `tests/test_phase14_teaching.py` already does; do not invent a second
scaling mechanism. Redirect every output path into `tmp_path`.

The test asserts, in this order:
  1. **`results/` is untouched.** Snapshot `sorted(os.listdir("results"))` and the output of
     `git status --porcelain results/` before the run; assert both are identical after. D-08's
     boundary is that Phase 22 writes NO scored artifact — and note in the docstring that this is
     also why `results/phase22_*` was deliberately NOT added to `V4_ARTIFACT_GLOBS` (plan 22-02):
     arming a prefix that never matches ships a guard vacuous by construction.
  2. **The run completed.** A checkpoint and a CSV exist under `tmp_path`, and the CSV has more than
     a header row.
  3. **All four paths actually executed** — not merely that the call did not crash. Assert each with
     its own observation:
       - a spy on `get_batch_fact_aligned` fired (fact routing);
       - the effective `grad_accum_steps` equalled `len(facts)` (read it off the `TrainConfig`
         actually constructed, e.g. by spying `train`'s kwargs);
       - `replay_windows` was passed and is `> 0`, equal to
         `replay_window_budget(len(facts)) // BLOCK_SIZE`;
       - `dp_fn` was passed and its `_records` counter is `> 0` after the run (**V-13**: D-16's four
         invariants fired inside a production-caller run, not only in unit tests).
     A test that only asserts "it didn't crash" would pass with three of four paths silently unwired,
     which is precisely the Phase-21 IN-04 failure this plan exists to prevent.
  4. **The invariants are live at the caller.** Assert the run raises when a fake is injected at this
     level too — specifically, run once with a deliberately mismatched `grad_accum_steps` and assert
     the `loop.py` accum-agreement refusal fires through the real caller path.
  5. **σ and C came from the CLI.** Invoke through `main([...])` with explicit `--sigma=` and
     `--clip-norm=` values chosen by the TEST (fixture values, local to the test, not committed
     constants), so the no-default contract is exercised end to end. State in the docstring that
     these are test fixtures and are not a budget — Phase 23 supplies the operating values from
     `scripts/mitigation_budget.py`.

Runtime budget: the whole test should finish well under 60 s on CPU. If it does not, reduce the
fixture scale rather than marking it slow.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase22_wiring.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `test_end_to_end_writes_no_scored_artifact` asserts `git status --porcelain results/` is byte-identical before and after
    - all four wirings are asserted by SEPARATE observations (loader spy, `grad_accum_steps`, `replay_windows`, `dp_fn._records > 0`)
    - the test drives `main([...])` with explicit `--sigma=` / `--clip-norm=` tokens
    - the mismatched-`grad_accum_steps` case is observed raising through the real caller path
    - `.venv/bin/python -m pytest tests/test_phase22_wiring.py -q` reports at least 14 tests passed and completes in under 60 s
    - `git status --porcelain results/` is empty after the full suite run
    - `.venv/bin/python -m pytest tests/ -q` reports 0 failed
    - `make lint` exits 0
  </acceptance_criteria>
  <done>An end-to-end CPU run through `main()` exercises all four wirings — each asserted by its own observation, not by absence of a crash — completes, fires D-16's invariants at the production caller, and leaves `results/` byte-unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → training run | σ and C enter here. A default would silently become the operating privacy budget without ever being pre-registered, crossing Phase 20's Z boundary invisibly. |
| Phase 22 → Phase 23 | Four never-executed integration paths would otherwise cross into a phase whose first act is a measurement, making a wiring bug and a DP-correctness bug indistinguishable in the same artifact. |
| test run → `results/` | The scored-artifact directory is pre-registration-guarded. A Phase-22 write under any `results/` prefix would change what the ancestry guards are measuring. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-48 | Spoofing | a DP-named arm producing a non-DP adapter | mitigate | All four wirings land together on `DP_ARMS`, each asserted by its own observation in V-23; three-of-four wired would fail the test |
| T-22-49 | Elevation of Privilege | a default σ or C silently becoming the operating budget | mitigate | No default on either, at the CLI or the constructor; `SystemExit` on omission naming Phase 20's Z boundary; an AST test asserts no numeric σ/clip constant exists in the file |
| T-22-50 | Tampering | a Phase-22 write under `results/` changing what the ancestry guards measure | mitigate | V-23 snapshots `git status --porcelain results/` before and after and asserts byte-identity; `results/phase22_*` is deliberately absent from `V4_ARTIFACT_GLOBS` |
| T-22-51 | Repudiation | an end-to-end test green because it only checks "no crash" | mitigate | Four separate positive observations plus a deliberate mismatch case observed raising through the real caller path |
| T-22-52 | Tampering | the new CLI branch narrowing the existing non-DP path | mitigate | `test_non_dp_arm_cli_is_unchanged` is the control; the full existing suite must stay green |
| T-22-53 | Repudiation | `replay_window_budget`'s docstring claiming a caller that does not exist | mitigate | The claim is made TRUE by wiring 3 and the docstring is corrected in the same diff — IN-04 closed in both directions |
| T-22-SC | Tampering | package installs | accept | No installs; `pyproject.toml` byte-unchanged (RPT-03); both frozen pins asserted unchanged |
</threat_model>

<verification>
```bash
.venv/bin/python -m pytest tests/test_phase22_wiring.py -x -q
.venv/bin/python -m pytest tests/ -q
git status --porcelain results/
git diff --exit-code -- scripts/mitigation_gate.py scripts/mitigation_unit.py scripts/mitigation_accountant.py pyproject.toml
make lint
```
</verification>

<success_criteria>
- All four D-08 wirings are live on `dp_n8` / `dp_n64` and each is asserted by its own observation
- σ and C are required CLI arguments with no default anywhere; an AST test proves no numeric literal exists
- The non-DP CLI path and every non-DP arm's behaviour are unchanged
- `replay_window_budget`'s docstring claim is true in both directions (IN-04 closed)
- The end-to-end CPU run completes and `git status --porcelain results/` is byte-identical before and after
- `dp_fn._records > 0` after a production-caller run — D-16's invariants fired outside unit tests
</success_criteria>

<output>
Create `.planning/phases/22-dp-sgd-core-accountant-and-the-correctness-battery/22-10-SUMMARY.md` when done.
</output>
