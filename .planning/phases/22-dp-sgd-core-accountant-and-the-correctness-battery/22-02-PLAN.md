---
phase: 22-dp-sgd-core-accountant-and-the-correctness-battery
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/mitigation_accountant.py
  - tests/test_phase20_prereg.py
autonomous: true
requirements: [DPSGD-03, DPSGD-04]
validates: [V-10, V-24]

must_haves:
  truths:
    - "D-09: the frozen half is `scripts/mitigation_accountant.py` — rule AND golden vectors in one file, ZERO imports, no executable formula"
    - "D-09: `REJECTED_FORM` names `sqrt(2*ln(1.25/delta))/sigma` as rejected WITHOUT transcribing its logic — a string constant and prose reason, never a runnable `rejected_epsilon()`"
    - "D-18: `NEIGHBOURING = \"add/remove one fact\"` and `SENSITIVITY_MULTIPLIER = 1.0` are pinned beside `REQUIRED_FORM`, inside the same zero-import ceiling"
    - "D-11: BOTH halves land — `V4_ARTIFACT_GLOBS` gains `results/phase23_*` AND a matching `_assert_ordering_holds(prereg_artifact=..., artifact_glob=\"results/phase23_*\")` call. Phase 21 D-20: the glob addition alone enforces nothing"
    - "Phase 20 D-21: the guard is green while zero `results/phase23_*` artifacts are tracked, and demands non-zero from the first one onward — the Phase-18 shape, never Phase 16's"
    - "The `T ** 0.5` composition proof lives in the pin as an OPERATOR expression inside a `_prove` message — no import, arithmetic computed rather than transcribed"
  artifacts:
    - path: "scripts/mitigation_accountant.py"
      provides: "the frozen (epsilon, delta) pre-registration pin — form, rejection, adjacency and golden outputs"
      contains: "NEIGHBOURING"
      min_lines: 120
    - path: "tests/test_phase20_prereg.py"
      provides: "V-10's zero-import/no-formula guard plus D-11's two halves and the phase23 prefix RED-then-GREEN fixture"
      contains: "PHASE22_PREREG_ARTIFACT"
  key_links:
    - from: "tests/test_phase20_prereg.py"
      to: "scripts/mitigation_accountant.py"
      via: "PHASE22_PREREG_ARTIFACT hand-written path constant (the freeze, not the glob)"
      pattern: "PHASE22_PREREG_ARTIFACT = \"scripts/mitigation_accountant\\.py\""
    - from: "tests/test_phase20_prereg.py::_assert_ordering_holds"
      to: "results/phase23_*"
      via: "artifact_glob keyword, matching the V4_ARTIFACT_GLOBS entry"
      pattern: "artifact_glob=\"results/phase23_\\*\""
---

<objective>
Write the frozen pre-registration pin for the accountant and arm the ancestry guard that keeps it
frozen from the first `results/phase23_*` artifact onward.

Purpose: **the pin precedes the first ε-bearing artifact by a whole phase.** D-09's risk analysis is
specific — ε is not an input to `mitigation_point_verdict`, so a re-derived accountant cannot flip a
point verdict; what it CAN move is the published ε label on every point plus the GATE-10 fallback
route. Because the exposure is ε's VALUE rather than a threshold comparison, the pin that bites is
an **output table**, which is also the only shape that fits inside the zero-import ceiling.

Output: `scripts/mitigation_accountant.py` (new, frozen-on-first-artifact), plus D-11's two halves,
V-10's zero-import/no-formula guard, and a phase23-prefix RED-then-GREEN fixture in
`tests/test_phase20_prereg.py`.

**This plan runs in Wave 1 and depends on nothing.** The pin holds literals transcribed from
`22-RESEARCH.md`'s oracle column; it does not import, call, or otherwise require `accountant.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-dp-sgd-core-accountant-and-the-correctness-battery/22-CONTEXT.md
@.planning/phases/22-dp-sgd-core-accountant-and-the-correctness-battery/22-PATTERNS.md
@.planning/phases/22-dp-sgd-core-accountant-and-the-correctness-battery/22-RESEARCH.md
</context>

<environment>
**Python 3.11 venv is MANDATORY.** Commands run as `.venv/bin/python -m pytest ...`.
`scripts/mitigation_gate.py`, `scripts/mitigation_unit.py` and `pyproject.toml` are FROZEN or
untouchable — **no task in this plan edits any of them.**
</environment>

<tasks>

<task type="auto">
  <name>Task 1: Write the frozen pin `scripts/mitigation_accountant.py`</name>
  <files>scripts/mitigation_accountant.py</files>
  <read_first>
    - `scripts/mitigation_unit.py` **in full** — the verbatim template. Mirror its four docstring
      headings, its local `_prove`, its `REJECTED_DELTA_RECIPE`/`REJECTED_DELTA_REASON` pair, and
      its module-scope guard block with arithmetic computed inside the message
    - `tests/test_phase20_prereg.py:54-72` — the naming rule (named for its SUBJECT, not its phase)
      and `_GATE_MODULES`, the live glob this file joins the moment it exists
    - `tests/test_phase20_prereg.py:888-923` — the accumulating `imported <= {"pathlib","sys","erasure_gate"}`
      ceiling. It is ALREADY ARMED against this file; one `import math` anywhere here turns it RED
    - `22-RESEARCH.md` § *Why `REJECTED_FORM` is rejected for a correct stated reason* and
      § *For `GOLDEN_EPSILON`'s pin tolerance* — the prose and the seven golden rows
    - `22-PATTERNS.md` § 4 including the ⚠ CONFLICT note and Stale Anchor Finding #4
  </read_first>
  <action>
Create `scripts/mitigation_accountant.py`. **Zero imports. No `math`. No `from` anything.**

Module docstring, four headings mirroring `mitigation_unit.py`:
  1. **Subject** — the (ε, δ) accounting rule for the v4.0 DP arm: which closed form is required,
     which is rejected, under which adjacency relation, and what ε those inputs produce.
  2. **Closed at the first artifact** — the freeze trigger is the first tracked `results/phase23_*`
     file, enforced by `tests/test_phase20_prereg.py::test_phase22_prereg_is_frozen_before_every_phase23_result`.
  3. **Why this module imports nothing** — the accumulated ceiling across every
     `scripts/mitigation_*.py`; `erfc` and `exp` are reachable by no operator, and hand-rolling a
     series for `exp` inside a frozen pin is strictly worse than the problem. This is D-10's forcing
     mechanism and it is why the COMPUTATION lives at `src/personacore/privacy/accountant.py`.
     Widening the allow-set was rejected on Phase 21 D-22's own words.
  4. **What is deliberately absent** — no operating σ, no clip norm `C`, no chosen ε target, and no
     runnable rejected formula. State explicitly that `GOLDEN_EPSILON`'s σ/T columns are
     **arithmetic test vectors for the accountant's own correctness (D-09), not a budget**: the
     v4.0 sweep's σ and C are Phase 23 resource parameters living in `scripts/mitigation_budget.py`
     (Phase 20's Z boundary), and nothing here pre-empts them.

**CITE BY NAME AND SYMBOL, NEVER BY LINE NUMBER.** `22-PATTERNS.md` Stale Anchor Finding #4 measured
four stale test-file line anchors inside the FROZEN `mitigation_unit.py`, which cannot now be
corrected except by a dated continuation. Reference tests as
`tests/test_phase20_prereg.py::test_name` and code as `module.SYMBOL`.

Contents, in this order:

  - `_prove(condition, message)` — copied verbatim from `mitigation_unit.py`, with the bracket name
    changed to `[mitigation_accountant]` and its docstring's "import the instrument, never copy it"
    override reason carried across, citing the ceiling test by NAME.
  - `REQUIRED_FORM` — a string naming the analytic Gaussian composition:
    Balle & Wang (arXiv:1805.06530) Theorem 8, independently confirmed by Dong, Roth & Su
    (arXiv:1905.02383) Corollary 2.13, with T-fold composition by DRS Corollary 3.3
    (`mu_eff = mu * sqrt(T)`, an exact equality of trade-off functions covering ADAPTIVE
    composition). Name the form; the executable version lives in `accountant.py`.
  - `REQUIRED_FORM_CONDITIONS` — a tuple of strings, the five composition preconditions from
    `22-RESEARCH.md`: `q = 1` no subsampling; homogeneous σ and Δ across all T steps; T fixed in
    advance, not a data-dependent stopping time; adaptivity permitted at no cost; and Δ is the
    per-step sensitivity under a FIXED adjacency relation. Conditions 2, 3 and 4 are stated in no
    other committed artifact — that is why they are here.
  - `REJECTED_FORM = "sqrt(2*ln(1.25/delta))/sigma"` — **a string, not a function.** There is NO
    `rejected_epsilon()`. `mitigation_unit.py` makes its rejected recipe runnable; D-09 forbids
    transcribing this one's logic, and `log` is unreachable under the ceiling anyway.
  - `REJECTED_FORM_REASON` — prose carrying BOTH halves of the rejection with their measured
    numbers: (i) FORMALLY, Dwork & Roth Theorem A.1 (restated as Balle–Wang Theorem 1) has
    `eps in (0, 1)` in its HYPOTHESIS, and every σ below ~4.85 at the frozen δ produces ε > 1,
    invoking it outside its own hypothesis; (ii) NUMERICALLY, past `mu = 1.737896746`
    (`sigma = 0.575408178`) it is not merely unsupported but wrong in the UNSAFE direction —
    measured `35.7x` the promised δ at σ = 0.3. State that this is a stronger rejection than
    "it is loose".
  - `NEIGHBOURING = "add/remove one fact"` (D-18) with a prose block: `mu = Delta/sigma` requires Δ
    under a FIXED relation; add/remove-one (unbounded DP) gives `Delta = C`, replace-one (bounded
    DP) gives `Delta = 2C`; D-02's own sensitivity argument — *one record moves the sum by at most
    `C`* — IS the add/remove-one argument; and "one fact" is `mitigation_unit.PRIVACY_UNIT`'s unit
    verbatim, so this introduces no second vocabulary. Record the stake: since ε is roughly linear
    in μ over the operating range, the alternative is roughly **2× on every published ε**.
  - `SENSITIVITY_MULTIPLIER = 1.0` (D-18) — the numeric consequence of `NEIGHBOURING`, with the
    warning sign `.planning/research/PITFALLS.md` P3 names: *the report says add/remove and the
    accountant's docstring says replace*. Note that the cross-site consistency check reading this
    constant, `accountant.py`'s documented relation and `dpsgd.py`'s noise line lives at
    `tests/test_phase22_dpsgd_ast.py::test_adjacency_relation_consistent` — and that the pin
    CANNOT import or be imported by either site (the ceiling runs one way, `src/` never imports
    `scripts/`), which is why the check is a multi-site source read.
  - `SIGMA_IS_THE_NOISE_MULTIPLIER` — a prose string recording F4: `sigma == sigma_noise / C`,
    unitless, so `mu = 1/sigma` and `mu_eff = sqrt(T)/sigma` and the accountant correctly needs no
    `C` argument. Cite `mitigation_gate.MECHANISM_KEYS` (FROZEN, four keys, its own comment saying
    there is no fifth key) as the basis. State that adding a `clip_norm=` parameter to `epsilon_for`
    would create that fifth key.
  - `GOLDEN_EPSILON` — a tuple of seven `(sigma, steps, epsilon)` triples, **outputs only, no
    executable formula**, at the frozen δ. Values are the ORACLE column from `22-RESEARCH.md`
    § *For `GOLDEN_EPSILON`'s pin tolerance*: `(20.0, 200, 2.943225239801352)`,
    `(14.142135623730951, 200, 4.377178095681209)`, `(10.0, 200, 6.572970067030306)`,
    `(5.0, 200, 15.456155822609244)`, `(2.0, 200, 54.376639014985045)`,
    `(1.0, 1, 4.377178095681209)`, `(8.0, 64, 4.377178095681209)`.
  - `GOLDEN_EPSILON_PROVENANCE` — prose stating these are derived from the INDEPENDENT QUADRATURE
    ORACLE, never snapshotted from `accountant.py`. D-13's stated reason: a golden table read off
    the implementation shares its failure modes by construction and would be *a photograph of the
    code rather than a constraint on it*. Also record `GOLDEN_EPSILON_REL_TOL = 1e-12` and why an
    exact float pin would be WRONG — the oracle and the implementation differ at ~1e-14 by
    construction (measured worst case `1.07e-14`).
  - `GOLDEN_EPSILON_REL_TOL = 1.0e-12`.
  - `GOLDEN_EPSILON_DELTA_SOURCE` — a string naming `scripts/mitigation_unit.py::DELTA` as the δ
    every row was computed at. Do NOT re-spell `1e-5` as a literal here and do NOT import it; the
    consuming test resolves it from the module.

Then the **module-scope guard block**, header comment copied in register from
`mitigation_unit.py`'s ("Module scope, so a wrong edit fails at IMPORT rather than inside a consumer
that has already spent compute"):

  - `_prove(SENSITIVITY_MULTIPLIER == 1.0, ...)` — message names `NEIGHBOURING` and states that a
    multiplier of 2.0 under this relation, or 1.0 under replace-one, is the P3 fake.
  - `_prove("add/remove" in NEIGHBOURING, ...)` — refuse a relation string silently edited to
    replace-one while the multiplier stays 1.0.
  - **The `T ** 0.5` composition proof.** The last three `GOLDEN_EPSILON` rows all have
    `mu_eff = sqrt(T)/sigma == 1.0` — `(14.142135623730951, 200)`, `(1.0, 1)`, `(8.0, 64)`. Prove
    it with operators only: assert each row's `steps ** 0.5 / sigma` agrees with the others to
    `1e-12` relative, AND that their pinned ε values are equal, with the computed margin written
    INSIDE the `_prove` message (the `mitigation_unit.py` idiom: `f"...{margin}x"`, arithmetic
    computed rather than transcribed). This is the composition identity appearing as data, proved
    inside the pin with no import.
  - `_prove` over `GOLDEN_EPSILON`'s shape: seven rows, every row a 3-tuple, every σ and ε strictly
    positive, and ε **strictly decreasing** as σ increases at fixed `steps = 200` (a monotonicity
    the analytic form guarantees; a transcription typo breaks it).
  - `_prove(REJECTED_FORM != REQUIRED_FORM, ...)` — the degenerate-edit refusal.

`ruff format` clean. Line length must match the repo's configured limit.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import sys, ast, pathlib; t=ast.parse(pathlib.Path('scripts/mitigation_accountant.py').read_text()); imports=[n for n in ast.walk(t) if isinstance(n,(ast.Import,ast.ImportFrom))]; assert t.body, 'empty parse'; assert not imports, imports; import importlib.util as u; s=u.spec_from_file_location('ma','scripts/mitigation_accountant.py'); m=u.module_from_spec(s); s.loader.exec_module(m); assert m.SENSITIVITY_MULTIPLIER==1.0 and len(m.GOLDEN_EPSILON)==7; print('pin ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `ast` walk of `scripts/mitigation_accountant.py` yields ZERO `Import` and ZERO `ImportFrom` nodes
    - executing the module (module-scope `_prove` guards run at import) exits 0 — no `SystemExit`
    - `scripts/mitigation_accountant.py` defines exactly one function, `_prove`; `grep -c "^def " scripts/mitigation_accountant.py` returns `1`
    - `grep -n "rejected_epsilon" scripts/mitigation_accountant.py` returns nothing
    - `NEIGHBOURING == "add/remove one fact"` and `SENSITIVITY_MULTIPLIER == 1.0`
    - `len(GOLDEN_EPSILON) == 7` and `GOLDEN_EPSILON_REL_TOL == 1e-12`
    - `grep -nE ":[0-9]+" scripts/mitigation_accountant.py` returns no line citing a test-file or source line number (Stale Anchor Finding #4 — cite by name and symbol only)
    - `.venv/bin/python -m pytest tests/test_phase20_prereg.py -x -q` exits 0 — the live `imported <= {"pathlib","sys","erasure_gate"}` ceiling still passes with the new module inside `_GATE_MODULES`
    - `git diff --exit-code -- scripts/mitigation_gate.py scripts/mitigation_unit.py pyproject.toml` exits 0
  </acceptance_criteria>
  <done>The pin exists with zero imports, one function, no runnable rejected formula, D-18's two constants, seven golden output rows and the operator-only composition proof; the pre-existing accumulating import ceiling is still green with the new file inside its glob.</done>
</task>

<task type="auto">
  <name>Task 2: D-11's two halves + the phase23 prefix RED-then-GREEN fixture</name>
  <files>tests/test_phase20_prereg.py</files>
  <read_first>
    - `tests/test_phase20_prereg.py:88-130` — the `PHASE20_PREREG_ARTIFACT` / `PHASE21_PREREG_ARTIFACT`
      block, the `:93-107` comment recording that the hand-written explicit path (not the filename)
      confers the freeze, and the `:110-130` `V4_ARTIFACT_GLOBS` comment block
    - `tests/test_phase20_prereg.py:149-225` — `_assert_ordering_holds`. The `assert artifact_glob in globs`
      consistency check is at **`:157`** and the ordering loop is at **`:181`**
    - `tests/test_phase20_prereg.py:257-289` — `test_phase21_prereg_is_frozen_before_every_phase21_result`,
      the live-ordering test to copy structure from (swap three literals)
    - `tests/test_phase20_prereg.py:657-810` — `test_phase21_glob_sees_the_phase21_prefix_red_then_green`,
      the five-state throwaway-repo prefix walk this plan writes a sibling of. **This is the correct
      analog**; `22-CONTEXT.md` names `:281-330`, which is the reflexivity proof, not this
    - `tests/test_phase20_prereg.py:133-146` — `_git`'s keyword-only `cwd`, which exists for the
      throwaway repo. Every `_git` call in the fixture passes `cwd=tmp_path`, never `_ROOT`
    - `22-PATTERNS.md` § 8 and Stale Anchor Findings #2 and #3
  </read_first>
  <action>
Four additive edits to `tests/test_phase20_prereg.py`. All four are required; three of them are
individually vacuous.

**(a) The path constant.** Add `PHASE22_PREREG_ARTIFACT = "scripts/mitigation_accountant.py"`
directly beneath `PHASE21_PREREG_ARTIFACT`. Extend the `:93-107` comment to say the same thing for
the third pin: the glob makes a `mitigation_*.py` sibling PROTECTED (import ceiling, rule/emission
split); only this hand-written explicit path confers the FREEZE.

**(b) Half 1 of D-11 — the glob.** `V4_ARTIFACT_GLOBS` becomes
`("results/phase20_*", "results/phase21_*", "results/phase23_*")`. There is deliberately **no**
`results/phase22_*` entry: under D-08 Phase 22 writes no scored artifact, and arming a prefix that
never matches would ship a guard vacuous by construction. Record that reasoning in the comment.

**(c) FIX THE STALE ANCHORS IN THE SAME DIFF.** The live comment at `:122-123` reads *"the
`assert artifact_glob in globs` consistency check at `:129`"* and refers to *"the ordering loop at
`:150`"*. Both are stale — measured against HEAD the consistency check is **`:157`** and the
ordering loop is **`:181`**. This is the Phase-21 IN-02 defect class recurring inside the file whose
entire purpose is pre-registration integrity, and Phase 22 is editing this exact comment block
anyway. Correct both numbers and add a one-line note that anchors inside this block are re-resolved
whenever it is edited. Extend the block's argument to the third prefix: with three prefixes live, a
mutation swapping a fixture's `artifact_glob` to a different member of `V4_ARTIFACT_GLOBS` still
satisfies the `in globs` consistency check and is INVISIBLE without a positive `ls-files`
observation — so the phase23 fixture in (e) is mandatory, not optional.

**(d) Half 2 of D-11 — the live ordering test.** Add
`test_phase22_prereg_is_frozen_before_every_phase23_result()`, structurally identical to
`test_phase21_prereg_is_frozen_before_every_phase21_result`, calling
`_assert_ordering_holds(root=_ROOT, prereg_artifact=PHASE22_PREREG_ARTIFACT,
artifact_glob="results/phase23_*", globs=V4_ARTIFACT_GLOBS)`. Its docstring states Phase 21 D-20 in
its own words — the glob addition alone enforces nothing, this call is the other half — and Phase 20
D-21's shape: green while zero `phase23_*` artifacts are tracked, demanding non-zero from the first
one onward (the Phase-18 shape, never Phase 16's). It also records that the pin precedes the first
ε-bearing artifact by a whole phase, the same discipline that has validated `erasure_gate.py` since
`23a830c`.

**(e) The phase23 prefix RED-then-GREEN fixture.** Add
`test_phase23_glob_sees_the_phase23_prefix_red_then_green(tmp_path)`, a sibling of the phase21
fixture at `:657-810`. Same five-state throwaway-repo walk, `_git(..., cwd=tmp_path)` throughout,
`results/phase23_probe.json` as the artifact and `PHASE22_PREREG_ARTIFACT` as the pin. Its docstring
must state the obligation the phase21 fixture states for its own prefix: the live ordering test in
(d) is **vacuous today BY CONSTRUCTION** — nothing matches `results/phase23_*` yet, so its product
assertion compares nothing — and reading a glob pattern and confirming it bites are different acts.
The fixture is what makes `results/phase23_*` OBSERVED matching something.

**(f) V-10 — the pin's zero-import/no-formula guard.** Add
`test_mitigation_accountant_pin_has_no_executable_formula()`. Assertions, each with its own message:
  - the module's `Import`/`ImportFrom` node set is EMPTY (stricter than the accumulated
    `imported <= allowed` subset, which the pin also satisfies vacuously);
  - every module-level `ast.Call` has `func.id == "_prove"` — the only executable statements in the
    file are the guards;
  - exactly one `ast.FunctionDef`, named `_prove` — this is what refuses a `rejected_epsilon()`
    arriving by pattern-match from `mitigation_unit.py` (D-09: name the rejected form WITHOUT
    transcribing its logic);
  - every module-level `ast.Assign` value is a literal — `ast.Constant`, or a `Tuple`/`List`/`Dict`
    of constants — with **no `ast.BinOp` anywhere in a constant's definition**. `BinOp` is permitted
    ONLY inside `_prove` call arguments, which is the sanctioned computed-margin idiom;
  - none of `"erfc"`, `"exp("`, `"math."` appears as a `Name`/`Attribute` in the tree;
  - collapsed-walk meta-guard: `assert tree.body`, and `assert PHASE22_PREREG_ARTIFACT in {p.as_posix() ... }`
    style check that the file the guard read is the file the pin constant names.

Reach the pin's values, where the test needs them, through the
`sys.path.insert(0, str(_ROOT / "scripts"))` idiom already at `:44-50`.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase20_prereg.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'PHASE22_PREREG_ARTIFACT = "scripts/mitigation_accountant.py"' tests/test_phase20_prereg.py` returns exactly 1 line
    - `grep -n 'V4_ARTIFACT_GLOBS = ' tests/test_phase20_prereg.py` shows a 3-tuple ending in `"results/phase23_*"` and containing NO `results/phase22_*`
    - `grep -cn 'artifact_glob="results/phase23_\*"' tests/test_phase20_prereg.py` returns at least 2 (the live test and the fixture)
    - `grep -n ':129' tests/test_phase20_prereg.py` returns nothing; `grep -n ':157' tests/test_phase20_prereg.py` returns the corrected comment line
    - `.venv/bin/python -m pytest tests/test_phase20_prereg.py -x -q` exits 0 with the test count strictly greater than before this plan
    - `.venv/bin/python -m pytest tests/test_phase20_prereg.py -k "phase23 or accountant" -q` selects at least 3 tests and exits 0 — **verify the selector is non-vacuous by reading the collected count, not by `$?` after a pipe** (a `| tail` reports tail's status; a vacuous `-k` exits 5)
    - `git diff --exit-code -- scripts/mitigation_gate.py scripts/mitigation_unit.py pyproject.toml` exits 0
  </acceptance_criteria>
  <done>All four D-11-adjacent edits land in one diff: the path constant, the third glob, the corrected `:157`/`:181` anchors, the live ordering call, the observed-RED-then-GREEN phase23 prefix fixture, and V-10's zero-import/no-formula guard. Full prereg file green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning artifact → frozen pin | The Balle–Wang form, the rejection reason and the golden outputs cross from `22-RESEARCH.md` into a file that becomes uneditable at the first Phase-23 artifact. A transcription error here is permanent (correctable only by a dated continuation). |
| future author → the pin | The adversary is an edit made AFTER an inconvenient ε has been seen. The ancestry guard is the only thing that makes that edit visible. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-05 | Tampering | `scripts/mitigation_accountant.py` edited after a Phase-23 ε exists | mitigate | D-11's BOTH halves — the `results/phase23_*` glob entry AND the matching `_assert_ordering_holds` call. Phase 21 D-20: the glob alone enforces nothing |
| T-22-06 | Repudiation | the ordering guard is vacuous until Phase 23 lands | mitigate | The throwaway-repo five-state prefix fixture OBSERVES `results/phase23_*` matching, so the pattern's bite is a test result rather than a reading |
| T-22-07 | Information Disclosure | an ε published under a 2×-optimistic adjacency relation | mitigate | `NEIGHBOURING = "add/remove one fact"` + `SENSITIVITY_MULTIPLIER = 1.0` pinned here with their stake stated; the cross-site check is plan 22-09 (V-25) |
| T-22-08 | Tampering | `GOLDEN_EPSILON` snapshotted from the implementation it constrains | mitigate | `GOLDEN_EPSILON_PROVENANCE` records oracle derivation; plan 22-09's V-06 re-derives every row from `delta_quadrature` alone |
| T-22-09 | Tampering | a formula smuggled into the pin, defeating the rule/emission split | mitigate | V-10 asserts one `FunctionDef`, `_prove`-only module-level calls, literal-only assigns and zero imports — stricter than the accumulated subset ceiling |
| T-22-SC | Tampering | package installs | accept | No installs. `pyproject.toml` untouched (RPT-03); both frozen pins asserted byte-unchanged by `git diff --exit-code` |
</threat_model>

<verification>
```bash
.venv/bin/python -m pytest tests/test_phase20_prereg.py -x -q
git diff --exit-code -- scripts/mitigation_gate.py scripts/mitigation_unit.py pyproject.toml
make lint
```
</verification>

<success_criteria>
- `scripts/mitigation_accountant.py` exists with zero imports, one function, no runnable rejected formula, D-18's two constants and seven golden output rows
- The `T ** 0.5` composition proof passes at import, with its margin computed inside the message
- `V4_ARTIFACT_GLOBS` carries `results/phase23_*` **and** a matching `_assert_ordering_holds` call exists
- The phase23 prefix has been OBSERVED matching in a throwaway repo, RED then GREEN
- The stale `:129`/`:150` anchors in the live comment block are corrected to `:157`/`:181`
- `mitigation_gate.py`, `mitigation_unit.py` and `pyproject.toml` are byte-unchanged
</success_criteria>

<output>
Create `.planning/phases/22-dp-sgd-core-accountant-and-the-correctness-battery/22-02-SUMMARY.md` when done.
</output>
