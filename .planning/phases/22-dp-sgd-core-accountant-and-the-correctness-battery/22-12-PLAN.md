---
phase: 22-dp-sgd-core-accountant-and-the-correctness-battery
plan: 12
type: execute
wave: 7
gap_closure: true
depends_on: ["22-11"]
files_modified:
  - src/personacore/privacy/accountant.py
  - tests/test_phase22_accountant.py
  - tests/fixtures/phase22_reference.py
  - tests/test_phase22_reference.py
autonomous: true
requirements: [DPSGD-03]
validates: [V-01, V-02]
closes_gaps:
  - "A `_log_erfc(x)` helper that stays in log space through the erfc underflow (asymptotic series for large positive x), so `second` is never silently dropped"
  - "At least one DELTA_FRONTIER row in the `b > 27.2` band with a healthy `a` (e.g. eps=775.786660, mu=35.3553) so `test_two_oracles_agree` covers it"
  - "A committed truth for `test_epsilon_for_survives_the_overflow_regime` to compare against, replacing the `> 700.0` liveness assertion"

must_haves:
  truths:
    - "`delta_closed` carries its second term through the `math.erfc` underflow instead of discarding it: at (eps=775.7866600701457, mu=35.35533905932738) it returns 8.8703030483e-06 rather than the shipped 9.99999999999972e-06, which is 12.7357% high against 60-dps ground truth"
    - "EVERY point the module already answers is BIT-IDENTICAL after the fix — all 7 GOLDEN_EPSILON epsilons and all 11 representable DELTA_FRONTIER deltas — because `_log_erfc` returns `math.log(math.erfc(x))` unchanged wherever `erfc(x) > 0.0`"
    - "`test_two_oracles_agree` now SWEEPS the `b > 27.2` band: a thirteenth DELTA_FRONTIER row with a healthy `a` (3.0157) and an underflowed `b` (28.0156)"
    - "`test_epsilon_for_survives_the_overflow_regime` compares against a committed 60-dps epsilon instead of asserting `got > 700.0`"
    - "The dropped-term mutation is WATCHED reddening the new coverage, and the file is restored byte-identically (sha256 equal, `git diff --exit-code` clean)"
  artifacts:
    - path: "src/personacore/privacy/accountant.py"
      provides: "module-level `_log_erfc` and a `delta_closed` whose second term survives the erfc cliff"
      contains: "def _log_erfc"
    - path: "tests/fixtures/phase22_reference.py"
      provides: "the thirteenth DELTA_FRONTIER row and EPSILON_OVERFLOW_REGIME, both as committed literal strings with provenance"
      contains: "EPSILON_OVERFLOW_REGIME"
  key_links:
    - from: "src/personacore/privacy/accountant.py::delta_closed"
      to: "_log_erfc"
      via: "the second term, computed in log space unconditionally"
      pattern: "_log_erfc\\(b\\)"
    - from: "tests/test_phase22_accountant.py::test_two_oracles_agree"
      to: "the b > 27.2 band"
      via: "the new DELTA_FRONTIER row's parametrization"
      pattern: "775.7866600701457"
---

<objective>
Close the SC3 / DPSGD-03 gap's largest defect: `delta_closed` silently discards its second term
wherever `math.erfc(b)` underflows, and no committed row makes the two-oracle cross-check look
there.

Purpose: DPSGD-03's entire mechanism of proof is that two oracles of different mathematics agree.
Measured in this tree, they disagree by **12.7357% relative** at
`(eps=775.7866600701457, mu=35.35533905932738)` — σ=0.40 / T=200 / δ=1e-5, the exact input the
line's own comment cites as reachable on this project's frontier. The quadrature oracle is right
(1.107e-11 from 60-dps truth); the closed form has **zero correct significant digits** against a
docstring promising at least twelve, and it does not refuse.

Output: a module-level `_log_erfc` that stays in log space through the underflow; a thirteenth
`DELTA_FRONTIER` row inside the failing band so V-01 and V-02 sweep it; a committed 60-dps epsilon
replacing the `> 700.0` liveness assertion; and the dropped-term mutation watched reddening all of
it, restored byte-identically.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-dp-sgd-core-accountant-and-the-correctness-battery/22-VERIFICATION.md
@.planning/phases/22-dp-sgd-core-accountant-and-the-correctness-battery/22-CONTEXT.md
</context>

<environment>
**Python 3.11 venv is MANDATORY.** The dev box runs 3.14 and a pyenv 3.12.13 is first on PATH.

- `make test` is BROKEN — it invokes bare `pytest`, which resolves to pyenv 3.12.13 and produces
  ~83 `ModuleNotFoundError: torch`. **Never use it.** Use `.venv/bin/python -m pytest -q`.
- `make lint` works but runs a different ruff over a different file set. Use
  `.venv/bin/ruff check . && .venv/bin/ruff format --check .`
- The full suite takes **~220 s**. Allow a **400 s+** timeout on every full-suite command.
- Baseline before this plan: **1280 passed, 1 skipped**; Phase-22 battery **253 passed**; ruff clean.
- `pyproject.toml` MUST stay byte-unchanged (RPT-03). `mpmath` is present in `.venv` only as a
  TRANSITIVE dependency of `torch` (torch → sympy → mpmath, version 1.3.0); it is declared nowhere
  and no test may import it.
- **`scripts/mitigation_accountant.py` is a FROZEN pre-registration and MUST NOT be edited, ever.**
  Editing it reddens the ancestry guard permanently and delete+re-add cannot undo it. This plan
  READS it (through the test module's existing `sys.path` insert) and never writes it.
- **`gsd-sdk` mutation handlers corrupt planning frontmatter.** Use the `--flag` form, never
  positional; write `22-12-SUMMARY.md` BEFORE calling `roadmap.update-plan-progress`; then
  `git diff .planning/` and hand-repair before committing.
</environment>

<tasks>

<task type="auto">
  <name>Task 1: `_log_erfc` — carry the second term through the erfc cliff without moving a single answered point</name>
  <files>src/personacore/privacy/accountant.py, tests/test_phase22_accountant.py</files>
  <read_first>
    - `src/personacore/privacy/accountant.py:82-125` — the module constants block (`_SQRT2`,
      `_EXP_OVERFLOW_ARG`, the two 1e-12 tolerance registers) and the comment conventions
    - `src/personacore/privacy/accountant.py:127-218` — `delta_closed` in full: the three refusals,
      the `z`/`a`/`b` derivation, and **`:180-186`, the defective line and the comment above it that
      asserts a property the code does not have**
    - `src/personacore/privacy/accountant.py:437-476` — `_delta_or_below_float64`, whose bare
      `except ValueError` will swallow ANY `ValueError` raised inside `delta_closed`'s call tree
    - `tests/test_phase22_accountant.py:700-732` —
      `test_delta_closed_still_ships_exactly_four_raises`, which counts `ast.Raise` nodes **inside
      `delta_closed`'s FunctionDef**
    - `tests/test_phase22_accountant.py:86-166` — `test_closed_form_frontier`, `_frontier_rel_tol`,
      `_sig_digits`, `_representable_rows`, and `test_closed_form_survives_high_epsilon`
  </read_first>
  <action>
**Step 1 — capture the before-state, because the highest risk in this plan is moving a frozen pin.**

Before editing anything, run a throwaway script under `.venv/bin/python` that prints
`float.hex()` of: `delta_closed(eps, mu)` for all 12 `DELTA_FRONTIER` rows (recording `REFUSED` for
the one that raises), and `epsilon_for(sigma, steps, mitigation_unit.DELTA)` for all 7
`scripts/mitigation_accountant.py::GOLDEN_EPSILON` rows. Save that output — it is the regression
baseline Step 4 compares against and it goes into the SUMMARY verbatim.

**Step 2 — add `_log_erfc(x)` at MODULE LEVEL.**

It MUST be a module-level function, not nested inside `delta_closed`.
`tests/test_phase22_accountant.py::test_delta_closed_still_ships_exactly_four_raises` counts
`ast.Raise` nodes inside `delta_closed`'s FunctionDef and asserts exactly 4; a nested helper's
`raise` would be counted and redden a guard that has nothing to do with this change.

Behaviour, in this order:
  1. `e = math.erfc(x)`; **if `e > 0.0`, return `math.log(e)` and nothing else.** This is the
     identical arithmetic `delta_closed:186` already performs on its `else` branch, and it is what
     makes the whole fix inert on every point the module currently answers. Say so in the docstring
     — the inertness is the load-bearing property, not a side effect.
  2. If `x <= 0.0`, `raise ValueError` naming the impossibility: `math.erfc` is monotonically
     decreasing with `erfc(x) >= 1.0` for every `x <= 0.0`, so an underflow at a non-positive
     argument means the argument is not what the caller thinks it is. Record in the same message
     that this branch is UNREACHABLE from `delta_closed`, with the reason: `b = (eps/mu + mu/2)/√2`
     and `erfc` first underflows at `x ≈ 27.2`, so the series branch implies `b > 27.2 > 0`. It is
     kept as a domain guard, in the same spirit as `delta_closed`'s own first two refusals which are
     also unreachable from the search.
  3. Otherwise the asymptotic expansion for large positive `x`:
     `log(erfc(x)) = -x*x - log(x * sqrt(pi)) + log(S)` where
     `S = 1 - 1/(2x²) + 3/(4x⁴) - 15/(8x⁶) + ...`, i.e. successive terms multiply by
     `-(2n-1) / (2x²)`. Accumulate terms with the standard OPTIMAL-TRUNCATION rule for a divergent
     asymptotic series: stop as soon as `abs(term)` fails to be strictly smaller than the previous
     term, or falls to `<= 1e-17 * abs(S)`, whichever comes first, with a hard cap of 60 terms so a
     pathological `x` cannot loop.

MEASURE the truncation before you trust it. Against `mpmath` at `mp.workdps(120)`
(`.venv/bin/python -c` one-liner — mpmath is a transitive torch dependency, present but never
imported by a test), evaluate the ABSOLUTE error in the returned log at
`x ∈ {27.2, 27.20000001, 27.3, 28.0, 29.0, 32.0, 45.0, 80.0, 150.0}` and record the worst. An
absolute error `d` in the log is a relative error `d` in `exp(eps + log)`, so this number IS the
relative error the second term inherits. Reference measurement taken while planning: worst
**7.637e-13** over that band, and **6.454e-14** at the `b = 28.0155` that matters. If your worst
exceeds 1e-11, STOP and report — the truncation rule is wrong, not the tolerance.

Also confirm no exception escapes at an extreme argument: `_log_erfc(1e200)` must return `-inf`
(float multiplication overflows to `inf` silently in CPython, so `-x*x` is `-inf`), and
`math.exp(eps + -inf)` is `0.0`, which is the correct second term there.

**Step 3 — wire it into `delta_closed`.**

Replace `second = 0.0 if eb == 0.0 else 0.5 * math.exp(eps + math.log(eb))` with the unconditional
log-space form using `_log_erfc(b)`. The `eb = math.erfc(b)` line becomes dead — delete it rather
than leaving a computed-and-unused local.

Rewrite the comment above it. The shipped comment ends *"Log space keeps the identical product
below the line"*, cited against σ=0.40/T=200 — which is precisely where the `else` branch never
executed. The verifier logged that comment as a Blocker: a comment asserting a property the code
does not have. The replacement must state, with the measured numbers: at
`(775.7866600701457, 35.35533905932738)` `math.erfc(b)` is exactly `0.0` while `math.exp(eps)` is
~8.3e336, so the true second term is `1.1297e-06` against a first term of `9.99999999999972e-06`;
treating the underflow as a negligible term returned `9.99999999999972e-06` against a 60-dps truth
of `8.870303048329795e-06`, **12.7357% high, zero correct significant digits**, without refusing;
and 19 of 72 (σ, T) cells on a δ=1e-5 grid disagreed above 1e-9, worst 11.36%. Record the direction
honestly: `second >= 0`, so dropping it OVER-states δ and therefore OVER-states ε — the conservative
direction, which is why this was a latent wrong number rather than a live privacy break.

**Step 4 — prove the pin did not move, and ship that proof as a test.**

Re-run Step 1's script. Every one of the 19 values must be BIT-IDENTICAL (`==` on the float, or
equal `float.hex()`), not merely within tolerance. Reference measurement taken while planning: all
7 golden epsilons and all 11 representable frontier deltas are bit-identical, and row 12 still
refuses. If ANY value moves, STOP and report — `scripts/mitigation_accountant.py` is frozen and its
`GOLDEN_EPSILON` cannot be corrected.

Then commit that property as `test_log_erfc_is_inert_where_erfc_is_healthy` in
`tests/test_phase22_accountant.py`: for every representable `DELTA_FRONTIER` row and for every
`GOLDEN_EPSILON` (sigma, steps) pair, compute `b = (eps/mu + mu/2) / sqrt(2)` (deriving `mu` as
`sqrt(steps)/sigma` for the golden rows and using the row's own `mu` for the frontier rows), assert
`math.erfc(b) > 0.0` — the meta-guard, so the test cannot pass vacuously on a row that underflows —
and then assert `_log_erfc(b) == math.log(math.erfc(b))` **by exact equality**. That is the
structural statement of "this fix cannot move a pinned row", and it reddens if a future edit
"simplifies" `_log_erfc` into using the series everywhere.

Add `test_log_erfc_matches_the_committed_underflow_truth`: assert `math.erfc(28.0155...)` is exactly
`0.0` (the meta-guard proving the test is in the underflow band at all), then assert
`_log_erfc(b)` at the defect row's `b` reproduces a committed 60-dps `log(erfc(b))` literal to a
relative tolerance you MEASURE and record. Derive the literal with the same mpmath one-liner used in
Step 2 and put its provenance in the test docstring.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase22_accountant.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "^def _log_erfc" src/personacore/privacy/accountant.py` returns exactly one line (module level, column 0)
    - `! grep -q "eb == 0.0" src/personacore/privacy/accountant.py` succeeds (`grep -c` EXITS 1 on zero matches, so it reads as failure under `&&`)
    - `.venv/bin/python -m pytest tests/test_phase22_accountant.py -q` exits 0 with 0 failures
    - `.venv/bin/python -m pytest tests/test_phase22_accountant.py -q -k "golden or four_raises or imports_math_only"` exits 0 — the frozen pin, the raise count and the import ceiling are all untouched
    - `.venv/bin/python -c` prints `delta_closed(775.7866600701457, 35.35533905932738)` within 1e-11 relative of `8.870303048329795e-06`
    - all 7 GOLDEN_EPSILON epsilons and all 11 representable DELTA_FRONTIER deltas are BIT-IDENTICAL to the Step 1 capture
    - `git diff --exit-code -- pyproject.toml scripts/mitigation_accountant.py scripts/mitigation_gate.py scripts/mitigation_unit.py` exits 0
  </acceptance_criteria>
  <done>`delta_closed` computes its second term in log space unconditionally; the defect row's 12.74% error is gone; and every point the module already answered returns the identical bits, asserted rather than assumed.</done>
</task>

<task type="auto">
  <name>Task 2: the thirteenth frontier row and a committed epsilon truth — make the cross-check look where it never looked</name>
  <files>tests/fixtures/phase22_reference.py, tests/test_phase22_reference.py, tests/test_phase22_accountant.py</files>
  <read_first>
    - `tests/fixtures/phase22_reference.py:23-62` — `DELTA_FRONTIER`, its provenance block, the
      decimal-STRING rule and `VACUOUS_AGREEMENT_ROW`
    - `tests/test_phase22_reference.py:46-112` — `test_reference_table_is_populated`, which asserts
      `len(DELTA_FRONTIER) == 12` and that exactly one row underflows
    - `tests/test_phase22_accountant.py:56-131` — `_sig_digits`, `_frontier_rel_tol`,
      `_representable_rows`, `test_closed_form_frontier` and its
      `test_closed_form_frontier_parametrization_is_not_empty` companion asserting `len(rows) == 11`
    - `tests/test_phase22_accountant.py:364-379` — `test_epsilon_for_survives_the_overflow_regime`,
      the test that visits the defect and never compares the number to anything
  </read_first>
  <action>
**Step 1 — derive the truth, and record how.**

`mpmath` 1.3.0 is present in `.venv` as a transitive dependency of `torch` (torch → sympy → mpmath).
It is declared in neither `pyproject.toml` nor `requirements.txt` and no test may import it, so
using it in a ONE-OFF `.venv/bin/python -c` invocation whose OUTPUT is committed as a literal string
is exactly the provenance route `tests/fixtures/phase22_reference.py`'s header already records for
every other row ("computed ONCE ... with mpmath at mp.dps = 60, and is committed here as data").
RPT-03 is untouched: nothing is installed, `pyproject.toml` does not move, and
`test_no_phase22_test_imports_mpmath` stays green because the import lives in a shell one-liner, not
in a test module.

At `mp.dps = 60` compute `0.5*erfc(a) - 0.5*exp(eps)*erfc(b)` with `a = (eps/mu - mu/2)/sqrt(2)`,
`b = (eps/mu + mu/2)/sqrt(2)` at `eps = 775.7866600701457`, `mu = 35.35533905932738`. Reference
value obtained while planning with `mp.mpf(<python float>)`: `8.870303048329795521072e-6`.

**Commit the exact invocation into the fixture's provenance comment, not just the result.**
`mp.mpf(775.7866600701457)` and `mp.mpf("775.7866600701457")` are different starting points and give
answers 8.9e-15 apart — both round to the same 13-digit literal, so the artifact is unaffected, but a
provenance that does not say which one was used is not reproducible, and this plan asks for
three-way agreement to specific figures. Record the one-liner verbatim, including which form of
`mp.mpf` the inputs went through.

**Cross-check it against two independent routes before committing it** and record all three numbers
in the fixture's provenance comment:
  - `delta_quadrature(775.7866600701457, 35.35533905932738)` — different mathematics, float64,
    measured `8.870303048231617e-06`, relative gap to the mpmath truth **1.107e-11**;
  - `delta_closed` after Task 1 — the erfc asymptotic route, measured relative gap **1.8e-14**.
A three-way agreement at those magnitudes is what licenses the literal; if any of the three moves by
more than an order of magnitude from these figures, STOP and report rather than committing a number
you cannot justify.

**Step 2 — add the row.**

Append `(775.7866600701457, 35.35533905932738, "8.870303048330e-6")` to `DELTA_FRONTIER` — 13
significant digits, matching the table's dominant width, and a decimal STRING like every other row.
Add a comment block above or beside it recording: why this row exists (RESEARCH F1's twin — past
`b ≈ 27.2` the SECOND `erfc` underflows while the FIRST is still healthy, so `a = 3.0157` and
`b = 28.0156`, and the closed form silently discarded a term worth 11.3% of the answer with the
cross-check structurally unable to see it), its inputs' meaning (σ=0.40 / T=200 / δ=1e-5, i.e.
`mu = sqrt(200)/0.40`), and the three-way provenance from Step 1.

Update the two count meta-guards, which will otherwise redden immediately:
  - `tests/test_phase22_reference.py::test_reference_table_is_populated`: `len(DELTA_FRONTIER) == 12`
    becomes `== 13`, with the message updated to say 13-point frontier.
  - `tests/test_phase22_accountant.py::test_closed_form_frontier_parametrization_is_not_empty`:
    `len(rows) == 11` becomes `== 12`.
Both messages must keep naming what a shrunken table would do. `VACUOUS_AGREEMENT_ROW` does NOT
change — the new row is representable, so the "exactly one row underflows" assertion still holds and
must stay a hard equality.

MEASURE and record the margins the new row lands with, before believing them:
  - `_frontier_rel_tol("8.870303048330e-6")` — measured while planning **1.5e-12**, and
    `test_closed_form_frontier`'s own sanity band `1e-12 < tol <= 1e-10` holds;
  - V-01 deviation of the fixed `delta_closed` — **state which denominator you used**, because the
    two differ and `accountant.py`'s own docstring warns about exactly this ("same tolerance,
    unrelated denominators"): against the FULL-PRECISION mpmath truth it is ~**1.8e-14** (~83x inside
    the tolerance), and against the COMMITTED 13-digit STRING, which is what the test actually
    compares to, it is ~**4.1e-14** (~36x). The test's denominator is the string;
  - V-02's two-oracle relative gap at the row — measured **1.109e-11** against the existing 1e-9
    budget, roughly 90x inside it. **Do not widen the 1e-9 budget**; if the measured gap does not
    fit, the fix is wrong, not the tolerance.

**Step 3 — give `test_epsilon_for_survives_the_overflow_regime` something to compare against.**

Add a new committed constant `EPSILON_OVERFLOW_REGIME` to `tests/fixtures/phase22_reference.py` as a
tuple of `(sigma, steps, epsilon_string)` rows for σ ∈ {0.40, 0.30} at T = 200, evaluated at the
frozen δ. Derive each epsilon by bisecting the SAME 60-dps mpmath δ used in Step 1 against the
target — never from `accountant.py`.

**State the limit of that independence in the constant's provenance rather than overclaiming it.**
This is a 60-dps evaluation of the SAME closed form the implementation uses, so it catches float64
and truncation error but NOT a formula error — which is precisely why D-13 (`22-CONTEXT.md`) has
`GOLDEN_EPSILON` bisected against the QUADRATURE oracle instead. Independence in this band is
carried by the thirteenth frontier row's V-02 cross-check, which compares the two oracles at exactly
this `(eps, mu)`; the coverage exists, but it comes from that row and not from this constant. Say so
in the comment. Reference values obtained while planning:
`774.84272158769968907` at σ=0.40 and `1311.2027907044055278` at σ=0.30. Record in the constant's
provenance block that the SHIPPED accountant returned `775.7866600701457` and `1312.1599912046381`
for these two before this plan — **1.218e-03 and 7.300e-04 relative, in a module whose two published
tolerances are both 1e-12** — and that the error is exactly zero at σ ≥ 0.42, so these two rows are
the whole reachable band.

Follow the fixture's own conventions: it must stay ZERO-import and ZERO-executable-logic
(`test_reference_fixture_imports_nothing` asserts both by AST), the delta must NOT be re-spelled as
a literal (say "the frozen delta" in prose and let the consuming test resolve
`mitigation_unit.DELTA`), and the epsilons are STRINGS for the same reason every other truth is.

Rewrite `test_epsilon_for_survives_the_overflow_regime` to parametrize over
`EPSILON_OVERFLOW_REGIME` instead of a bare sigma list. Keep the `math.isfinite` assertion — the
overflow-survival property is still worth asserting — and REPLACE `assert got > 700.0` with a
relative comparison against `float(truth)`. Set the tolerance from measurement: compute the actual
relative deviation of the fixed `epsilon_for` against each committed truth (measured while planning:
~1.4e-16 and ~2e-16), then use the module's existing 1e-12 register with the measured figure and its
margin stated in the docstring. If your measured deviation exceeds 1e-12, STOP and report — that
means `_log_erfc`'s truncation is not accurate enough for the inverse solve.

Update the test's docstring: it currently argues only that the search must "come back with a
number". It must now say what the number is and why a liveness assertion was not enough — the
shipped test parametrized over exactly the two sigmas where the dropped term bites and never
compared the result to anything.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase22_accountant.py tests/test_phase22_reference.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "775.7866600701457" tests/fixtures/phase22_reference.py` returns at least 1
    - `.venv/bin/python -m pytest tests/test_phase22_accountant.py tests/test_phase22_reference.py -q` exits 0
    - `.venv/bin/python -m pytest "tests/test_phase22_accountant.py::test_two_oracles_agree" -q` reports 12 passed
    - `.venv/bin/python -m pytest "tests/test_phase22_accountant.py::test_closed_form_frontier" -q` reports 12 passed
    - `grep -n "700.0" tests/test_phase22_accountant.py` returns nothing inside `test_epsilon_for_survives_the_overflow_regime`
    - `! grep -qE "^(import|from) " tests/fixtures/phase22_reference.py` succeeds — the fixture still imports nothing
    - `git diff --exit-code -- pyproject.toml scripts/` exits 0
  </acceptance_criteria>
  <done>The cross-check sweeps the `b > 27.2` band it was structurally unable to reach, and the test that walks into the overflow regime compares its epsilon against a 60-dps truth instead of asserting it is a finite number above 700.</done>
</task>

<task type="auto">
  <name>Task 3: watch the dropped term redden the new coverage, then restore byte-identically</name>
  <files>src/personacore/privacy/accountant.py, tests/test_phase22_accountant.py</files>
  <read_first>
    - `tests/test_phase22_fakes.py:1-90` — this phase's mutation-probe register: `_mutate` asserts
      the target appears EXACTLY ONCE and that the replacement applied, every probe carries an
      UNMUTATED CONTROL, and the verdict is read from pytest's own summary line
    - `.planning/phases/22-dp-sgd-core-accountant-and-the-correctness-battery/22-11-SUMMARY.md`
      § the four fakes — the evidence format: mutation, sha256 before/after, the verbatim RED
      assertion message, and the count of DISTINCT reds per mutation
  </read_first>
  <action>
A guard nobody has watched fail is a guard nobody has verified. Five plan-mandated guards in this
phase were already measured structurally INCAPABLE of catching the violation they existed for. Both
mutations below are ONE-SHOT probes against the REAL committed file, not committed tests.

Record `sha256` of `src/personacore/privacy/accountant.py` before starting.

**Mutation M-A — restore the dropped term.** Replace the `_log_erfc(b)` second-term line with the
shipped form `second = 0.0 if eb == 0.0 else 0.5 * math.exp(eps + math.log(eb))` plus its
`eb = math.erfc(b)` line. Run
`.venv/bin/python -m pytest tests/test_phase22_accountant.py tests/test_phase22_reference.py -q`.
EXPECTED to redden, and each must be confirmed by observation rather than assumed:
`test_closed_form_frontier` at the new row (relative deviation ~0.127 against a 1.5e-12 tolerance),
`test_two_oracles_agree` at the new row (~0.127 against 1e-9), and
`test_epsilon_for_survives_the_overflow_regime` at both sigmas — **three** distinct tests.

`test_log_erfc_matches_the_committed_underflow_truth` is expected to stay **GREEN** under M-A and
that is correct, not a miss: M-A reverts `delta_closed`'s second-term line and does not touch
`_log_erfc`, which that test calls directly. Do not "fix" it into reddening; M-G below is its
mutation.

Capture each failing test's assertion message VERBATIM and count how many DISTINCT reds the single
mutation produced. If fewer than three distinct tests redden, the new coverage is weaker than this
plan claims — report that number rather than rounding it up.

**Mutation M-B — the pin-moving mutation.** Delete `_log_erfc`'s `if e > 0.0: return math.log(e)`
fast path so the asymptotic series runs at EVERY argument. Run
`.venv/bin/python -m pytest tests/test_phase22_accountant.py -q`. EXPECTED to redden
`test_log_erfc_is_inert_where_erfc_is_healthy` AND `test_epsilon_for_matches_golden` — the frozen
pin's seven rows, whose `b` values sit at 3.19 … 7.94 where the asymptotic series is worth roughly
three digits. Observe and record whether `test_epsilon_for_matches_golden` actually reddens; that
observation is the entire evidence that the frozen `GOLDEN_EPSILON` is protected against a future
edit to `_log_erfc`, and it must not be assumed.

**Mutation M-G — the unwatched guard's own mutation.** Truncate `_log_erfc`'s asymptotic series to
its leading term — specifically **stop the series after one term**, i.e. `S = 1 - 1/(2x**2)`. Do NOT
instead drop the `log(S)` correction: the two are NOT equivalent and differ by ~500x (6.36e-04 vs
1.21e-06 absolute in the log), so only the one-term form matches the reference figure below.
Measured, the one-term form is worth **1.2144e-06** absolute in the log at `b = 28.01573320140291`
against the full series' **1.1369e-13**, so `test_log_erfc_matches_the_committed_underflow_truth`
reddens — and so do the new frontier row's V-01/V-02 legs and BOTH overflow-regime epsilon legs,
since 1.2144e-06 in the log is **1.5466e-07** relative in delta against tolerances of 1.5e-12 and
1e-9. All five were measured reddening, not predicted. Without this probe that test ships unwatched, which is the one thing this phase
does not accept. Observe which tests actually redden and report the count.

**Restore.** After each mutation, restore the file and prove it: `sha256` must equal the pre-probe
value and `git diff --exit-code -- src/personacore/privacy/accountant.py` must exit 0.

**Then correct the last false contract.** `delta_closed`'s `Returns:` block currently promises
"at least 12 significant digits of the 60-dps ground truth everywhere this function does not refuse"
and cites a worst deviation of 1.84e-12 over ELEVEN rows. Re-measure that claim over the now-TWELVE
representable rows against their committed truths, and rewrite the block with the measured worst
deviation, the row it occurs on, and the note that part of each budget is the reference string's own
quantization rather than this function's error. Do not restate a bound you have not just measured.

Finally run the FULL suite and record the count. Expected: the 1280-passed baseline plus the tests
this plan adds, with **zero** regressions. Allow a 400 s+ timeout.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - the SUMMARY records, for M-A, M-B and M-G: the exact edit, the pytest summary line, at least one verbatim assertion message per distinct RED, and the count of distinct reddened tests
    - M-A is recorded as leaving `test_log_erfc_matches_the_committed_underflow_truth` GREEN, and M-G as reddening it
    - `shasum -a 256 src/personacore/privacy/accountant.py` matches the pre-probe value after each restore
    - `git diff --exit-code -- src/personacore/privacy/accountant.py` exits 0 after each restore
    - `.venv/bin/python -m pytest -q` reports at least 1280 passed with 0 failed
    - `.venv/bin/ruff check . && .venv/bin/ruff format --check .` exits 0
    - `delta_closed`'s `Returns:` block states a worst deviation the SUMMARY also reports as measured in this session
  </acceptance_criteria>
  <done>The dropped-term mutation was watched reddening the coverage that now exists to catch it, the pin-moving mutation was watched reddening the frozen pin's own test, the series-truncation mutation was watched reddening the underflow-truth guard, all three restores are sha256-identical, and the docstring states a bound measured today rather than one inherited.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| float64 domain edge → published δ | `math.erfc` underflows to exactly `0.0` past x ≈ 27.2. On the FIRST erfc that is a refusal; on the SECOND it was silently a dropped term. Both boundaries are invisible in the return value. |
| a correction → a FROZEN pre-registration | `scripts/mitigation_accountant.py::GOLDEN_EPSILON` cannot be edited. Any change to `delta_closed` that moves a pinned epsilon is unrecoverable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-20 | Information Disclosure | `delta_closed`'s second term at `erfc(b) == 0.0` | mitigate | `_log_erfc` stays in log space through the underflow; the new DELTA_FRONTIER row makes V-01 and V-02 sweep the band; mutation M-A watched reddening ≥3 distinct tests |
| T-22-20b | Tampering | an over-truncated `_log_erfc` series | mitigate | Mutation M-G (series cut to its leading term) watched reddening `test_log_erfc_matches_the_committed_underflow_truth`, which no other mutation reaches |
| T-22-21 | Tampering | a `_log_erfc` edit moving a frozen `GOLDEN_EPSILON` row | mitigate | `test_log_erfc_is_inert_where_erfc_is_healthy` asserts exact equality with `math.log(math.erfc(b))` at every pinned `b`; mutation M-B watched reddening `test_epsilon_for_matches_golden` |
| T-22-22 | Tampering | a new `raise` inside `delta_closed` widening `_delta_or_below_float64`'s swallow | mitigate | `_log_erfc` is MODULE LEVEL so its raise is outside `delta_closed`'s FunctionDef; `test_delta_closed_still_ships_exactly_four_raises` stays green and is run explicitly |
| T-22-23 | Tampering | a reference truth derived from the implementation it judges | mitigate | The new row is an mpmath 60-dps value cross-checked against `delta_quadrature` (different mathematics) before commit. `EPSILON_OVERFLOW_REGIME` is a 60-dps evaluation of the SAME closed form — it catches float64 error, not formula error, and its provenance comment says so; independence in that band comes from the new row's V-02 leg. The fixture stays zero-import, zero-logic |
| T-22-SC | Tampering | package installs | accept | No installs. `mpmath` is already present as a torch transitive; `pyproject.toml` asserted byte-unchanged |
</threat_model>

<verification>
```bash
.venv/bin/python -m pytest tests/test_phase22_accountant.py tests/test_phase22_reference.py -q
.venv/bin/python -m pytest -q                       # allow 400s+
.venv/bin/ruff check . && .venv/bin/ruff format --check .
git diff --exit-code -- pyproject.toml scripts/
shasum -a 256 src/personacore/privacy/accountant.py
```
</verification>

<success_criteria>
- `delta_closed(775.7866600701457, 35.35533905932738)` agrees with the committed 60-dps truth to ≤ 1.5e-12 relative, where it was 12.7357% high
- All 7 `GOLDEN_EPSILON` epsilons and all 11 previously-representable `DELTA_FRONTIER` deltas are BIT-IDENTICAL to their pre-change values
- `DELTA_FRONTIER` carries 13 rows; `test_closed_form_frontier` and `test_two_oracles_agree` each sweep 12
- `test_epsilon_for_survives_the_overflow_regime` compares against a committed 60-dps epsilon; no `> 700.0` liveness assertion remains
- Mutations M-A, M-B and M-G were each WATCHED reddening (M-A on three distinct tests, and NOT on the underflow-truth guard, which is M-G's), with verbatim messages recorded and sha256-identical restores
- Full suite ≥ 1280 passed, 0 failed; ruff clean; `pyproject.toml` and `scripts/` byte-unchanged
</success_criteria>

<output>
Create `.planning/phases/22-dp-sgd-core-accountant-and-the-correctness-battery/22-12-SUMMARY.md` when done.
</output>
