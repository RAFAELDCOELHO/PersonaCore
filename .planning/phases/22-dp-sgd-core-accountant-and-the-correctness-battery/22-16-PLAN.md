---
phase: 22-dp-sgd-core-accountant-and-the-correctness-battery
plan: 16
type: execute
wave: 10
gap_closure: true
depends_on: ["22-12", "22-13", "22-14", "22-15"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/22-dp-sgd-core-accountant-and-the-correctness-battery/22-VALIDATION.md
  - .planning/phases/22-dp-sgd-core-accountant-and-the-correctness-battery/deferred-items.md
autonomous: true
requirements: [DPSGD-03]
validates: []
closes_gaps:
  - "DPSGD-03's REQUIREMENTS.md traceability row reads SATISFIED and is not — the verifier recorded that the row 'is not supported by the code outside the 12 committed frontier points'"

must_haves:
  truths:
    - "DPSGD-03's traceability row states what was measured FALSE and what closed it, with the original sentences left in place as the record of what was believed — the project's retract-in-place discipline, not a rewrite"
    - "The ROADMAP's Phase 22 plan count and wave list include the four gap-closure plans"
    - "22-VALIDATION.md carries a row for every guard the gap closure added, each with the command that runs it"
    - "WARNING-2 (no production driver can resume a DP arm) is recorded as carried forward to Phase 23 with its reasoning, not silently dropped"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "a DPSGD-03 row a reader can trust, carrying the measured numbers from all four closure plans"
      contains: "DPSGD-03"
    - path: ".planning/phases/22-dp-sgd-core-accountant-and-the-correctness-battery/deferred-items.md"
      provides: "WARNING-2's deferral with its reasoning"
      contains: "resume"
  key_links:
    - from: ".planning/REQUIREMENTS.md DPSGD-03 row"
      to: "plans 22-12 … 22-15"
      via: "the plan numbers and their measured figures"
      pattern: "22-12"
---

<objective>
Make the traceability tell the truth. `REQUIREMENTS.md`'s DPSGD-03 row currently reads
`[x] SATISFIED` and asserts *"the two oracles are genuinely different mathematics and they agree"*.
Measured, they disagreed by 12.7357% relative at an input the module's own docstring names as
reachable, the second oracle returned `+inf` and values above 1.0, and `epsilon_for` reported ε = 0
for a subnormal σ.

Purpose: this row is what a later phase reads instead of re-deriving. The verifier's own words:
*"REQUIREMENTS.md marks this row `[x] SATISFIED`; that row is not supported by the code outside the
12 committed frontier points."* Leaving it is how a false credit gets inherited.

Output: a corrected DPSGD-03 row with the measurements from all four closure plans, an updated
ROADMAP wave list, validation rows for the new guards, and WARNING-2 recorded as deferred rather
than dropped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-dp-sgd-core-accountant-and-the-correctness-battery/22-VERIFICATION.md
@.planning/phases/22-dp-sgd-core-accountant-and-the-correctness-battery/22-12-SUMMARY.md
@.planning/phases/22-dp-sgd-core-accountant-and-the-correctness-battery/22-13-SUMMARY.md
@.planning/phases/22-dp-sgd-core-accountant-and-the-correctness-battery/22-14-SUMMARY.md
@.planning/phases/22-dp-sgd-core-accountant-and-the-correctness-battery/22-15-SUMMARY.md
</context>

<environment>
**Python 3.11 venv is MANDATORY.** `make test` is BROKEN — use `.venv/bin/python -m pytest -q`
(~220 s, allow 400 s+). This plan changes no code; its verification is that the suite is still green
and that the documents it edits parse as the tools expect.

**`gsd-sdk` mutation handlers corrupt planning frontmatter** — this has been hit in all eleven prior
sessions of this phase. Use the `--flag` form, never positional. Write `22-16-SUMMARY.md` BEFORE
calling `roadmap.update-plan-progress`. Then `git diff .planning/STATE.md .planning/ROADMAP.md` and
hand-repair anything the handler mangled before committing. Prefer editing `.planning/ROADMAP.md` by
hand in this plan — the wave block is hand-shaped and the handler has no idea what a wave 7 is.
</environment>

<tasks>

<task type="auto">
  <name>Task 1: correct DPSGD-03's traceability, retract-in-place</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md:125-152` — the DPSGD requirement definitions and their checkboxes;
      DPSGD-03's is at `:132`
    - `.planning/REQUIREMENTS.md:342` — the DPSGD-03 traceability row, the one that claims the two
      oracles agree
    - `.planning/REQUIREMENTS.md:337` — the UNIT-04 row, which is this repository's worked example
      of the retract-in-place discipline: *"The sentences above are left unamended as the record of
      what was true when Phase 21 closed"*, with a dated **CLOSED** correction appended
    - the four gap-closure SUMMARYs, for the measured figures — do not transcribe the numbers in this
      plan's prose; read them from what actually shipped
  </read_first>
  <action>
Rewrite the DPSGD-03 row at `.planning/REQUIREMENTS.md:342` following the UNIT-04 shape at `:337`,
which is this project's own precedent and the only one a reader will trust.

Keep the existing sentences. They are the record of what was believed when Phase 22 closed and
deleting them destroys the audit trail — the same reason UNIT-04's original sentences survive its
correction. Append a dated block that states, in this order:

  1. **What was measured false**, with the numbers and where they came from (the verifier's own
     measurements, re-confirmed by the closure plans): the two oracles disagreed by **12.7357%
     relative in δ** at `(eps=775.7866600701457, mu=35.35533905932738)` — σ=0.40 / T=200 / δ=1e-5,
     the exact input `accountant.py`'s own comment cites as reachable — because `delta_closed`
     treated an `erfc` UNDERFLOW as a negligible term; the second oracle returned `+inf` and values
     above 1.0 for a quantity that is a probability; and `epsilon_for` reported ε = 0 for a subnormal
     σ, the privacy-understating direction. Say plainly that no published number was wrong, and why:
     all seven `GOLDEN_EPSILON` rows and all twelve `DELTA_FRONTIER` rows sat outside every defect
     band, the δ error's direction is conservative, and nothing in the tree had reported an ε at all.
  2. **Why the row said SATISFIED anyway**: the agreement test swept only the committed frontier
     rows, none of which was in the failing band, so the cross-check was structurally incapable of
     firing there. That is the transferable lesson, and it belongs in the row rather than in a
     SUMMARY nobody re-reads.
  3. **What closed it**, plan by plan, with the measured figures each SUMMARY reports: 22-12
     (`_log_erfc`, the thirteenth frontier row, the committed overflow-regime epsilons, and the
     bit-identity of all 7 golden + 11 frontier values across the fix), 22-14 (the `log(4*n)` Simpson
     headroom and the measured upper-bound refusal), 22-15 (the `mu` finiteness check and the
     narrowed swallow). Name the mutations that were watched reddening and how many distinct REDs
     each produced — this phase's standard of evidence is the observed RED, not the final green.

Then set DPSGD-03's checkbox at `:132` from the MEASURED state rather than from intent: it stays
`[x]` only if all four closure plans landed green and the full suite is clean at the time you write
it. If anything is outstanding, leave it `[ ]` and say what is outstanding, in the row. Do not mark
a requirement satisfied on the strength of a plan having been written.

Also correct the definition text at `:132-133` if it now understates the bar: the requirement says
"validated against independent oracles", and what this phase learned is that validation against
oracles is worth exactly the band the fixtures sweep. One clause naming that is enough; do not
rewrite the requirement.
  </action>
  <verify>
    <automated>grep -c "22-12" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "DPSGD-03" .planning/REQUIREMENTS.md` shows both the definition and the traceability row
    - the traceability row cites plans 22-12, 22-14 and 22-15 by number
    - the original claim sentences are still present, with the correction appended and dated — `git diff` shows additions dominating deletions in that row
    - the row states the 12.7357% figure, the `inf`/above-1 finding and the ε = 0 finding
    - the row states explicitly that no published number was wrong, with the reason
    - the checkbox state at `:132` matches what the four SUMMARYs and a fresh full-suite run actually show
  </acceptance_criteria>
  <done>A reader of DPSGD-03's row learns what was claimed, what measurement falsified it, why the guards could not see it, and what closed it — without having to open a SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: ROADMAP waves, validation rows, and WARNING-2's deferral</name>
  <files>.planning/ROADMAP.md, .planning/phases/22-dp-sgd-core-accountant-and-the-correctness-battery/22-VALIDATION.md, .planning/phases/22-dp-sgd-core-accountant-and-the-correctness-battery/deferred-items.md</files>
  <read_first>
    - `.planning/ROADMAP.md` — Phase 22's block IN FULL (locate it by the `### Phase 22:` heading,
      not by line number — the block moved when the gap-closure waves were added): the goal, the five
      success criteria, the `**Plans**:` count line, and the Wave 1 … Wave 10 checklists
    - `.planning/phases/22-dp-sgd-core-accountant-and-the-correctness-battery/deferred-items.md`
      § *`delta_quadrature` raises a bare `OverflowError` …* — the entry plan 22-14 made false
    - `.planning/phases/22-dp-sgd-core-accountant-and-the-correctness-battery/22-VALIDATION.md:56-84`
      — the V-ID table's columns and the V-01 / V-02 / V-05 / V-08 rows the closure extends
    - `.planning/phases/22-dp-sgd-core-accountant-and-the-correctness-battery/deferred-items.md` —
      the existing deferral format: what was found, where, why it was NOT fixed
  </read_first>
  <action>
**ROADMAP.** The count line and the Wave 7 … Wave 10 blocks ALREADY EXIST — they were written when
these plans were created (`**Plans**: 16 plans in 10 waves …`, and four wave blocks with unticked
boxes). **Read the file before editing it.** The remaining work is: tick each `[ ]` to `[x]` for
plans whose SUMMARY exists on disk, and correct any wave line whose one-clause objective no longer
matches what the plan shipped. Do not re-add blocks that are already there and do not touch Phase
22's five success criteria — they are what the phase was measured against, and rewriting them after
a failed verification is how a bar gets lowered to meet its result.

**22-VALIDATION.md.** Add a section headed for the gap closure, dated, holding one table row per
guard the four plans added, in the existing column shape (V-ID, Requirement, Behaviour validated,
Test type, Automated command, File Exists, Status). Continue the V-numbering from V-25. At minimum:
the `_log_erfc` inertness guard, the underflow-band truth, the thirteenth frontier row's V-01/V-02
coverage, the committed overflow-regime epsilons, the Simpson-sum headroom, the probability
refusal, the subnormal-σ band, the `_delta_or_below_float64` refusals, and the seamless-resume
refusal. Every `Automated command` must be a `.venv/bin/python -m pytest …` invocation that you have
RUN — not one you believe would work. Also tick the sign-off line
*"All four positive controls have their RED output recorded"* only if the four SUMMARYs record them;
this closure's own mutations are additional evidence, not a substitute.

**deferred-items.md.** Add WARNING-2 with its reasoning, in the file's existing shape. The content:
no production path can resume a DP arm at all — `scripts/teach_persona.py::train_arm` never passes
`resume_from`, and its `refuse_if_exists` on the checkpoint path actively blocks re-running a killed
DP arm — so SC5's kill→resume workflow is exercised only from tests today, through the production
`train(resume_from=…)` API. **Deliberately NOT closed here**, and the reason is that this is a
missing FEATURE rather than a defect in what Phase 22 shipped: closing it means adding a resume path
and relaxing a refusal that exists on purpose, which is a design decision belonging to the phase
whose first act is a real training run. Route it to Phase 23 beside DPSGD-06, and note that plan
22-13's refusal makes the eventual wiring safer — a DP arm resumed without its seam now refuses
rather than silently training non-privately.

Record in the same file, one line, that WARNING-1 was NOT deferred: its dangerous direction is
closed by plan 22-13 and its other direction is documented in `loop.py` as a deliberate
non-refusal, so nobody re-opens it as an unaddressed warning.

**And retract one entry that this closure made false.** `deferred-items.md` carries
`## delta_quadrature raises a bare OverflowError in a 0.92-wide negative-z band`, whose closing
paragraph reads *"Not fixed here, and the reason is scope rather than cost"* on the grounds that a
dedicated negative-`z` test would be a fourth case inside `test_oracle_refuses`, whose whole
assertion is that there are exactly three conditions with three distinct messages. Plan 22-14 fixed
that band (the `log(4*n)` headroom) and added exactly that dedicated test — as a SEPARATE test
function, so the stated obstacle never applied. Append a dated retraction under that heading naming
what closed it and correcting the reason; leave the original paragraph in place, per the same
retract-in-place discipline Task 1 applies to `REQUIREMENTS.md`. A deferral log that still says
"not fixed" about something that is fixed is the defect class this project retracts in place.

**Finally**, run the full suite once more and record the count in the SUMMARY as the phase's closing
measurement. Allow a 400 s+ timeout.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "plans in" .planning/ROADMAP.md` shows Phase 22's count matching the number of `22-*-PLAN.md` files on disk
    - `grep -c "22-1[2-6]-PLAN.md" .planning/ROADMAP.md` equals the number of gap-closure plans, and every box whose SUMMARY exists reads `[x]`
    - `deferred-items.md`'s OverflowError entry carries a dated retraction naming plan 22-14, with the original paragraph intact
    - Phase 22's five success criteria are byte-unchanged — `git diff .planning/ROADMAP.md` shows no deletion inside the `**Success Criteria**` block
    - 22-VALIDATION.md's new rows each carry a `.venv/bin/python -m pytest` command that exits 0 when run
    - deferred-items.md records WARNING-2 with its Phase-23 routing, the one-line note that WARNING-1 was closed rather than deferred, and the dated retraction of the OverflowError entry
    - `.venv/bin/python -m pytest -q` reports 0 failed
    - `git diff .planning/` reviewed by hand after any `gsd-sdk` call, with any mangled frontmatter repaired before committing
  </acceptance_criteria>
  <done>The roadmap, the validation contract and the deferral log all describe the tree that exists, and the two warnings the verifier logged are each accounted for — one closed, one routed, neither inherited as clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a traceability row → a downstream phase's assumption | `REQUIREMENTS.md` is read INSTEAD of re-deriving. A row that says SATISFIED when measurement says otherwise is a false credit that compounds. |
| a failed verification → a rewritten success criterion | The cheapest way to close a gap is to lower the bar it failed. Phase 22's five criteria are what the phase was measured against. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-35 | Repudiation | a corrected row that erases what was claimed | mitigate | Retract-in-place per the UNIT-04 precedent: original sentences kept, dated correction appended, `git diff` asserted to be additions-dominant |
| T-22-36 | Tampering | success criteria edited to match the result | mitigate | Acceptance criterion asserts zero deletions inside the `**Success Criteria**` block |
| T-22-37 | Repudiation | a warning dropped rather than decided | mitigate | WARNING-2 routed to Phase 23 with its reasoning; WARNING-1 recorded as closed by 22-13; both in `deferred-items.md` |
| T-22-37b | Repudiation | a deferral log still saying "not fixed" about something now fixed | mitigate | The `OverflowError` entry gets a dated retraction naming 22-14, original paragraph intact |
| T-22-38 | Tampering | `gsd-sdk` mangling planning frontmatter | mitigate | `--flag` form only, SUMMARY written before `roadmap.update-plan-progress`, `git diff .planning/` reviewed and hand-repaired before commit |
| T-22-SC | Tampering | package installs | accept | No installs; this plan changes no code |
</threat_model>

<verification>
```bash
.venv/bin/python -m pytest -q                       # allow 400s+
grep -n "DPSGD-03" .planning/REQUIREMENTS.md
git diff .planning/ROADMAP.md
git diff .planning/
```
</verification>

<success_criteria>
- DPSGD-03's traceability row states what was measured false, why the guards missed it, and what closed it, with the original claim preserved
- DPSGD-03's checkbox reflects a measured state, not an intended one
- The ROADMAP's Phase 22 plan count and wave list match the files on disk; its five success criteria are byte-unchanged
- 22-VALIDATION.md carries a runnable row for every guard the closure added
- WARNING-1 recorded as closed by 22-13; WARNING-2 routed to Phase 23 with its reasoning; the `OverflowError` deferral retracted in place
- Full suite green; `.planning/` frontmatter hand-verified after any SDK call
</success_criteria>

<output>
Create `.planning/phases/22-dp-sgd-core-accountant-and-the-correctness-battery/22-16-SUMMARY.md` when done.
</output>
