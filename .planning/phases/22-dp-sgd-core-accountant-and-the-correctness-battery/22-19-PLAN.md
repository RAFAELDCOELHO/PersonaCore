---
phase: 22-dp-sgd-core-accountant-and-the-correctness-battery
plan: 19
type: execute
wave: 13
gap_closure: true
depends_on: ["22-17", "22-18"]
files_modified:
  - tests/fixtures/phase22_reference.py
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/22-dp-sgd-core-accountant-and-the-correctness-battery/22-VALIDATION.md
autonomous: true
requirements: [DPSGD-03]
validates: []
closes_gaps:
  - "Correct `tests/fixtures/phase22_reference.py:186` and `.planning/REQUIREMENTS.md:350`: 'EXACTLY ZERO at sigma >= 0.42' is false as written. It is the FIX's delta that is zero there, not the error."

must_haves:
  truths:
    - "The false figure — *the error is EXACTLY ZERO at sigma >= 0.42* — is corrected in BOTH committed files by DATED RETRACT-IN-PLACE following the UNIT-04 precedent: the original sentence is left standing and a dated correction is appended, never a deletion or a silent rewrite"
    - "The correction states WHY the figure was false — it measured the FIX's DELTA (pre-fix versus post-fix SHIPPED values, genuinely bit-identical for sigma >= 0.4125) and not the error against truth — because that mis-measurement is the specific sentence that made the residual band look already covered"
    - "The correction carries the true measurement: the pre-fix error against 60-dps truth is 9.631e-12 at sigma=0.4185 and 1.100e-13 at sigma=0.4200, reaching machine epsilon only past sigma ~ 0.425; and the two `EPSILON_OVERFLOW_REGIME` rows were NOT the whole reachable band"
    - "`ZERO_BOUNDARIES['erfc_zero_x'] = 27.5` is corrected: measured, `math.erfc` first returns exactly 0.0 at 27.2, and the constant's own comment calls a subnormal 'still information' — the exact false premise this round closed. It is read by no test, which is why it rotted"
    - "DPSGD-03's row records round 2 with the same discipline round 1 used: what was measured false with its denominator, why the round-1 closure could not see it, what closed it, and an explicit statement of WHAT WOULD MAKE ROUND 3 NECESSARY"
    - "The ROADMAP's five Phase-22 success criteria are asserted BYTE-UNCHANGED by sha256, HEAD against worktree — the cheapest way to close a failed verification is to rewrite the criterion it failed"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "DPSGD-03's row carrying round 2's dated retract-in-place and the round-3 closure statement"
      contains: "2026-08-26"
    - path: "tests/fixtures/phase22_reference.py"
      provides: "a corrected EPSILON_OVERFLOW_REGIME provenance block and a corrected erfc_zero_x"
    - path: ".planning/phases/22-dp-sgd-core-accountant-and-the-correctness-battery/22-VALIDATION.md"
      provides: "validation rows for the guards plans 22-17 and 22-18 added, every Automated command RUN"
  key_links:
    - from: ".planning/REQUIREMENTS.md::DPSGD-03"
      to: "the retract-in-place precedent at UNIT-04 in the same table"
      via: "an appended dated block with the original left unamended"
      pattern: "RETRACTED IN PLACE"
---

<objective>
One sentence in this repository is the reason the erfc-SUBNORMAL band looked already covered:
*"The error is EXACTLY ZERO at sigma >= 0.42, so these two rows are the whole reachable band."* The
verifier flagged it as its OWN prior figure and false — it measured the FIX's delta, not the error
against truth — and the round-1 plans transcribed it faithfully into two committed files.

Purpose: a false figure that made a defect look covered is the same class of failure as the defect.
Round 1's closure was scoped to five `missing:` items rather than to the conjunct they were symptoms
of, and this sentence is why the sixth was not on the list. Correcting it is not bookkeeping.

Output: both files corrected by dated retract-in-place, DPSGD-03's row carrying round 2 with the
same honesty round 1's carried, and — the part that is new — an explicit statement of what would
make a round 3 necessary and why this plan set closes it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-dp-sgd-core-accountant-and-the-correctness-battery/22-VERIFICATION.md
@.planning/phases/22-dp-sgd-core-accountant-and-the-correctness-battery/22-16-SUMMARY.md
@.planning/phases/22-dp-sgd-core-accountant-and-the-correctness-battery/22-17-SUMMARY.md
@.planning/phases/22-dp-sgd-core-accountant-and-the-correctness-battery/22-18-SUMMARY.md
</context>

<environment>
**Python 3.11 venv is MANDATORY.** `make test` is BROKEN; use `.venv/bin/python -m pytest -q` and
`.venv/bin/ruff check . && .venv/bin/ruff format --check .`. Full suite ~222 s; allow 400 s+.

- Baseline is 22-18's closing count. **This plan changes no source code**, so the suite must be
  UNMOVED from it — any movement at all is a defect, not a tolerance question.
- **`scripts/mitigation_accountant.py` is FROZEN and MUST NOT be edited.**
- `tests/fixtures/phase22_reference.py` has ZERO imports and ZERO executable logic, AST-asserted.
  Only comments and literal data change here.
- **`requirements.mark-complete` leaves traceability cells EMPTY** and the whole deliverable of
  Task 2 is that cell's content. Five consecutive plans declined to call it. Write the row by hand.
- **Commit-message hazard:** `git commit -F -` with a QUOTED heredoc, never `-m` with backticks.
- **`gsd-sdk` mutation handlers corrupt planning frontmatter — nine sessions running.** `--flag`
  form only; write `22-19-SUMMARY.md` BEFORE `roadmap.update-plan-progress`; then
  `git diff .planning/`, hand-repair, commit. Record which hazards reproduced and which did not —
  22-16 recorded `roadmap.update-plan-progress` running CLEAN after four sessions of corruption, and
  a hazard log that only ever confirms itself is not measuring.
</environment>

<tasks>

<task type="auto">
  <name>Task 1: retract the false figure in place, in both committed files, and correct the constant that carries its premise</name>
  <files>tests/fixtures/phase22_reference.py, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `tests/fixtures/phase22_reference.py:137-191` — `EPSILON_OVERFLOW_REGIME`'s whole provenance
      block, ending in the false sentence at `:186`, and the block's own honest "WHAT THIS CONSTANT
      DOES **NOT** BUY" paragraph, which is the voice to match
    - `tests/fixtures/phase22_reference.py:193-220` — `ZERO_BOUNDARIES`, and specifically
      `erfc_zero_x` with its comment "erfc(27.00) = 5.237046e-319 (a subnormal, still information)"
    - `.planning/REQUIREMENTS.md` DPSGD-03's row — locate it by `grep -n "DPSGD-03"`, never by line
      number — and inside it the clause reading "EXACTLY ZERO at σ ≥ 0.42"
    - `.planning/REQUIREMENTS.md` UNIT-04's row — the repository's worked retract-in-place
      precedent, which round 1 followed literally and which this task follows again
    - `.planning/phases/.../22-VERIFICATION.md:92-101` — the verifier's own account of the false
      figure, which is the source this correction cites
  </read_first>
  <action>
**Step 1 — measure the replacement before writing it.**

You cannot re-measure the pre-fix behaviour by reverting the module — 22-17 has landed. Reproduce
the SHIPPED route in a scratch script outside the repository instead: compute `delta_closed`'s
second term with `math.log(math.erfc(b))` forced (the pre-22-17 arithmetic), bisect epsilon against
it at T=200 and the frozen delta, and compare to a 60-dps `mpmath` solve at the same `mu`.

Planning measurements, to confirm rather than transcribe — the pre-fix epsilon's relative error
against 60-dps truth:

| sigma | pre-fix relative epsilon error |
|---|---|
| 0.4185 | **9.6308e-12** |
| 0.4200 | **1.1001e-13** |
| 0.4250 | 9.9323e-17 |
| 0.4300 | 1.5526e-16 |
| 0.4500 | 3.0649e-16 |

So the error is not zero at sigma >= 0.42; it reaches machine epsilon only past sigma ~ 0.425, and
even there it is ~1e-16 rather than exactly zero. These figures agree with `22-VERIFICATION.md`'s
own table (9.631e-12 at 0.4185, 1.100e-13 at 0.4200) — say so, and if yours diverge, report the
divergence rather than smoothing it.

**Step 2 — correct `tests/fixtures/phase22_reference.py`.**

The final sentence of `EPSILON_OVERFLOW_REGIME`'s provenance block reads *"The error is EXACTLY ZERO
at sigma >= 0.42, so these two rows are the whole reachable band."* **Leave it standing.** Append a
dated correction below it, in the block's own voice, stating four things:

1. WHAT IT ACTUALLY MEASURED — the difference between the PRE-FIX and POST-FIX SHIPPED values, which
   are genuinely bit-identical for sigma >= 0.4125 — and not the error against truth. That is the
   whole of the mistake and it is a denominator error, the failure class `accountant.py`'s own
   docstring warns about ("same tolerance, unrelated denominators").
2. The true figures from step 1.
3. That the two rows were NOT the whole reachable band: [0.4135, 0.4185] is reachable at T=200 and
   was uncovered, and the closed form there was wrong by up to **1.9227e-03** relative while not
   refusing.
4. That the band is now covered by the fourteenth `DELTA_FRONTIER` row plan 22-18 added — name it by
   its `(eps, mu)` — so the correction points at the artifact that discharges it rather than only at
   the error.

Attribute the error where the verifier attributes it: it originated in the initial verification
report and the gap-closure plans transcribed it faithfully. Do not blame an executor for a figure
they were handed, and do not obscure that it entered this file through a plan.

**Step 3 — correct `ZERO_BOUNDARIES['erfc_zero_x']`, which carries the same false premise.**

It is set to **27.5** with the comment *"Bisected: erfc(27.00) = 5.237046e-319 (a subnormal, still
information), erfc(27.50) = 0.0."* Two things are wrong and one of them is this round's entire
subject:

- The value is not the boundary. MEASURED by bisection: `math.erfc` first returns exactly `0.0` at
  **27.2** (`erfc(27.199999999999996)` is still `1e-323`). Re-derive it yourself.
- *"a subnormal, still information"* is precisely the false premise this round closed. A subnormal
  `erfc` has lost up to 52 of its 53 mantissa bits; `math.log` of it was wrong by up to **0.2094
  absolute in the returned log**, a ~23.3% relative error in `delta_closed`'s second term.

Correct the value, retract the phrase in place with its measurement, and add the second boundary the
module now routes on — the x at which `math.erfc` first returns a SUBNORMAL, measured
**26.54325845425098** — so the table records both cliffs rather than only the one that was known.

**This constant is read by no test** — verified: only `delta_quadrature_zero_z` is referenced
anywhere, and that in a docstring. Say so in the comment. An unconsumed constant rots silently, which
is exactly how 22-12 found `WORST_RELATIVE_ERROR` stale, and a reader deserves to know which
constants the suite is actually holding.

**Step 4 — correct `.planning/REQUIREMENTS.md`'s DPSGD-03 row, retract-in-place.**

Inside the existing round-1 retraction block the row states: *"`22-VERIFICATION.md` measured the
induced ε error at 1.218e-03 relative at σ=0.40/T=200 and EXACTLY ZERO at σ ≥ 0.42"*. It sits inside
the **"NO PUBLISHED NUMBER WAS WRONG"** argument, which is what makes it load-bearing rather than
incidental — it is one of the four reasons that paragraph gives.

Follow UNIT-04's shape exactly, as round 1 did: leave every original sentence intact and append a
dated correction. State the same four points as step 2, and state one more that belongs only here:
the "no published number is optimistic" clause of that paragraph **no longer covers the subnormal
band**, because the direction there is not conservative — `22-VERIFICATION.md` measured the shipped
epsilon BELOW the 60-dps truth at sigma = 0.4150, 0.4165, 0.4170 and 0.4175. Round 1's defect
over-stated epsilon always; a subnormal's lost bits round both ways. That is a change in KIND and
the row must carry it.

Keep the row a single markdown table line with exactly 5 `|`-delimited fields (3 columns + 2
sentinels), as 22-16 verified. Check it after writing.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase22_reference.py tests/test_phase22_accountant.py -q</automated>
  </verify>
  <done>
Both committed files carry a dated correction of the "EXACTLY ZERO at sigma >= 0.42" figure with the
original text left standing, the true pre-fix measurements, the reason the figure was false, and a
pointer to the fourteenth frontier row that discharges it; `erfc_zero_x` reads 27.2 with the
subnormal boundary recorded beside it and the "still information" phrase retracted; DPSGD-03's row
additionally records that the conservative-direction argument no longer covers this band; the row is
still one line with 5 pipe-delimited fields.
  </done>
</task>

<task type="auto">
  <name>Task 2: record round 2 — the closure, the validation rows, and what would make round 3 necessary</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/phases/22-dp-sgd-core-accountant-and-the-correctness-battery/22-VALIDATION.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` DPSGD-03's round-1 retraction block — its three-part shape (what
      was measured false / why the row said SATISFIED anyway / what closed it plan by plan) and its
      closing sentence withholding the SC3 verdict, which this extension must preserve rather than
      overwrite
    - `.planning/ROADMAP.md` — the `### Phase 22:` heading, its five success criteria, the
      `**Plans**:` count line and the Wave 7 … Wave 10 blocks. Locate everything by heading, never
      by line number: 22-16 recorded this plan family's anchors going stale
    - `.planning/phases/.../22-VALIDATION.md` — the *Gap-Closure Addendum* and its V-26 … V-34 rows,
      the seven-column shape and the numbering to continue from
    - `.planning/phases/.../22-16-SUMMARY.md:165-198` — the sha256 method for proving the success
      criteria byte-unchanged, and the `grep '^-[^-]'` failure it records
  </read_first>
  <action>
**Step 1 — extend DPSGD-03's row with round 2, in round 1's own three-part shape.**

Append, dated, below the round-1 block. Do not rewrite round 1; it is the record of what was
believed then and it is honest.

- **What was measured false.** SC3's two-oracle agreement, still, one band over: at sigma=0.414 /
  T=200 / delta=1e-5 — this project's own frozen delta, 0.03 in sigma from the original failure —
  the two oracles disagreed by **1.9190e-03 relative against a 1e-9 budget**, with an 80-dps third
  computation naming the closed form as the wrong oracle. State the denominators separately, as
  round 1 was forced to: 1.9190e-03 is the two-ORACLE gap with the closed form as denominator;
  1.9227e-03 is `delta_closed` against the 60-dps truth. Record that `delta_closed` there was
  **BIT-IDENTICAL to the pre-22-12 code** — a sibling defect the fix stepped over, not a regression
  it introduced — and that the direction was **privacy-UNDERSTATING** at four measured sigmas, which
  is a change in KIND from round 1.
- **Why round 1's closure could not see it.** The five `missing:` items were all genuinely closed and
  independently re-verified; the criterion is not "five items were addressed". `_inert_points()`
  filtered on `erfc(b) > 0.0` and therefore certified the defective band as healthy; zero of the 22
  pinned points had a subnormal `erfc(b)`; `_round_trip_pairs()`'s smallest sigma was 0.5. The
  blindness was proven by EXECUTION, not inferred: `_log_erfc` returning `-12345.0` for every
  subnormal input left the full suite at `1314 passed, 1 skipped`, byte-identical to baseline. And
  one sentence in two committed files — Task 1's — is why the band looked covered.
- **What closed it, plan by plan, with each SUMMARY's measured figures and each mutation's
  distinct-RED count.** 22-17: the predicate, the crossover proof that the new worst case EQUALS the
  perfect-routing floor, the band sweep, M-H. 22-18: the fourteenth frontier row, the retargeted
  filter, the round-trip sigma, four count meta-guards, M-J. 22-19: this record.

**Step 2 — the part that is new, and the reason this plan exists rather than only Task 1.**

Add a short, explicitly-labelled statement of **WHAT WOULD MAKE A ROUND 3 NECESSARY**. Round 1 fixed
a point and the next verifier found the adjacent band; a set that fixes this band and adds one more
committed row produces the same outcome. Name the three specific things that would reopen it, each
with the guard that now watches it:

1. **The routing boundary moving toward the lossy side.** Watched by 22-17's band sweep, which
   asserts the CHOSEN route is accurate across all three `math.erfc` regimes and therefore reddens
   without naming the boundary — measured, the shipped predicate fails it by eleven orders.
2. **The boundary moving the other way, into the healthy band.** NOT watched by that sweep — state
   this limit rather than letting a reader infer breadth — and watched instead by
   `test_log_erfc_is_inert_where_erfc_is_healthy` plus its hard-count companion, under 22-12's
   mutation M-B (6 of 7 pinned epsilons move, 4 to `0.0`).
3. **A NEW band, in a different function, that no committed row sweeps.** This is the open one and it
   must be named rather than implied. `22-VERIFICATION.md` WARNING-4 already records it: outside the
   subnormal band a 30,000-draw log-uniform sweep found 46 further two-oracle disagreements above
   1e-9, worst **6.08e-09** at delta = 6.26e-237 — a DIFFERENT mechanism (cancellation in
   `0.5*erfc(a) - second` near the representability floor), 6x over budget, and **0 of them at a
   delta above 1e-12**. Record it, record that this project's frozen delta is 1e-5 and that
   `_MIN_TARGET_DELTA` floors the solver at 1e-300, and record that it is therefore not a blocker for
   any epsilon this project can publish. **Do not claim it is closed. It is not.**

Then state plainly what this plan set does and does not establish: it closes the band the
re-verification measured, and it converts the guard from a fixed point list into a sweep of the
predicate's own neighbourhood, so a boundary move is caught by data. It does not prove the
accountant correct everywhere; a bound measured on a band is a statement about that band, which is
this phase's own central finding pointed at its own closure.

**Step 3 — withhold the verdict, as every plan since 22-12 has.**

The row records measured state. Whether SC3 holds is a `/gsd:verify-phase 22` re-verification's call.
Say so explicitly, in the round-1 block's own words, and do not call `requirements.mark-complete`.
Keep `requirements-completed: []` and `requirements-contributed: [DPSGD-03]` in the SUMMARY
frontmatter, matching the position all five round-1 plans held.

**Step 4 — ROADMAP: tick the boxes, correct the narrative, and PROVE the criteria did not move.**

**READ HEAD BEFORE EDITING — most of this is already done.** At planning time the Wave 11, Wave 12
and Wave 13 blocks were added, the `**Plans**:` line was set to `19 plans in 13 waves` with a
corrected parenthetical, and the phase-list entry at `:125` was corrected to record that the
2026-08-26 re-verification returned `gaps_found` again. 22-16 hit exactly this — its plan told it to
add blocks that already existed — so **verify against the file rather than against this paragraph.**

What is genuinely left: 22-17 and 22-18 each tick their own box as they execute (the established
pattern — 22-12 … 22-15 were each ticked by their own plan), so confirm both are `[x]`, tick 22-19's,
and update both narratives with what the round actually measured. Locate everything by heading
(`### Phase 22:`), never by line number.

**Then prove the five success criteria are byte-unchanged**, by extracting the block and comparing
**sha256** between `HEAD` and the worktree — not by reading a diff and believing it. The extraction
must be SCOPED TO PHASE 22: an `awk` that matches every `**Success Criteria**` heading in the file
spans all nine phases and hashes to something else entirely (measured while planning — it produced
`47d39f14...` instead of the recorded value). Anchor on `/^### Phase 22: DP-SGD Core/` first, then
take the block from `**Success Criteria** (what must be TRUE):` up to but excluding `**Plans**:`.
That yields **24 lines** with **5** `(DPSGD-` markers and sha256
`73a316f4aaff10371ea2e6a605810af7d3b6990f56c4324413ef7068d0ccd968` — 22-16's recorded value,
re-confirmed against both HEAD and the worktree at planning time AFTER the ROADMAP edits above.
Confirm yours matches.

Do NOT filter a diff with `grep '^-[^-]'` to look for deletions: 22-16 recorded that it silently
drops every deleted line beginning `- `, i.e. every checkbox and bullet in the file, and reported
two deletions where there were four.

**Step 5 — `22-VALIDATION.md`: one row per guard this round added, every command RUN first.**

Continue the numbering from V-34 in the existing seven-column shape. One row each for: 22-17's band
sweep, its three-regime non-vacuity companion, 22-18's fourteenth frontier row (its V-01 and V-02
legs), the retargeted `_inert_points` and its count companion, and the in-band round-trip sigma.
**Execute each `Automated command` before writing its row** and put that run's own pass count into
the Status cell — 22-16's discipline, and the only thing separating a validation contract from a
wish list. Note in the addendum that the table's planning-time *Full suite command* cell says
`make test`, which is broken in this tree, and that every run used `.venv/bin/python -m pytest`.

**Step 6 — close.** Full suite (~222 s, allow 400 s+) — it must be UNMOVED from 22-18's closing
count, because this plan changes no code. `.venv/bin/ruff check . && .venv/bin/ruff format --check .`.
Then assert with outputs in the SUMMARY: `git diff --exit-code -- src/ scripts/ pyproject.toml
requirements.txt` exits 0 (this plan's blast radius is markdown plus one fixture's comments);
`grep -c "RETRACTED IN PLACE" .planning/REQUIREMENTS.md` finds both retractions; and every new
V-row has the same `|`-field count as the header.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q</automated>
  </verify>
  <done>
DPSGD-03's row carries a dated round-2 block in round 1's three-part shape, an explicitly-labelled
statement of what would make round 3 necessary with WARNING-4 named as genuinely open, and the SC3
verdict still withheld; the ROADMAP carries Waves 11-13 with a corrected count and narrative, and
its five success criteria are proven byte-unchanged by sha256; `22-VALIDATION.md` carries one row
per new guard with every command run first; the suite is unmoved and `git diff --exit-code -- src/
scripts/` exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a committed figure → a later reader's belief about coverage | A number in a provenance block or a requirements row is read instead of re-derived. A false one makes a defect look covered, which is how this band survived round 1. |
| a failed success criterion → the criterion's own text | The cheapest way to close a failed verification is to rewrite the bar it failed. |
| an unconsumed constant → the suite's apparent coverage | `ZERO_BOUNDARIES` is read by no test. Nothing reddens when it goes stale. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-51 | Repudiation | a correction that erases what was claimed | mitigate | Dated retract-in-place per the UNIT-04 precedent: every original sentence left standing, the correction appended; `git diff --numstat` reported per file with additions dominating |
| T-22-52 | Tampering | success criteria edited to match the result | mitigate | Phase-22 criteria block extracted from HEAD and worktree, compared by **sha256** against 22-16's recorded `73a316f4…`; the `grep '^-[^-]'` shortcut explicitly forbidden with the reason |
| T-22-53 | Spoofing | a validation row whose command was never run | mitigate | Every `Automated command` executed before its row is written, that run's own pass count in the Status cell |
| T-22-54 | Repudiation | round 3's open risk inherited silently rather than recorded | mitigate | WARNING-4's 46 disagreements (worst 6.08e-09 at delta=6.26e-237, 0 above delta=1e-12) named in the row as GENUINELY OPEN, with the reason it is not a blocker for any publishable epsilon and no claim that it is closed |
| T-22-55 | Repudiation | a stale unconsumed constant carrying the retracted premise | mitigate | `erfc_zero_x` corrected from 27.5 to the measured 27.2, the subnormal boundary added beside it, and the fact that no test reads it recorded in the comment |
| T-22-SC | Tampering | npm/pip/cargo installs | accept | No installs; no code changes at all. `git diff --exit-code -- src/ scripts/ pyproject.toml requirements.txt` exits 0. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q` — UNMOVED from 22-18's closing count
- `.venv/bin/ruff check . && .venv/bin/ruff format --check .` — clean
- `git diff --exit-code -- src/ scripts/ pyproject.toml requirements.txt` — exit 0
- Phase-22 success-criteria block sha256, HEAD vs worktree — identical, and equal to 22-16's
  recorded `73a316f4aaff10371ea2e6a605810af7d3b6990f56c4324413ef7068d0ccd968`
- Every new `22-VALIDATION.md` row's `Automated command` run, exit 0, pass count in the Status cell
- `grep -n "DPSGD-03" .planning/REQUIREMENTS.md` — still ONE row line, 5 pipe-delimited fields
</verification>

<success_criteria>
- The "EXACTLY ZERO at sigma >= 0.42" figure is corrected in BOTH committed files by dated
  retract-in-place, with the true measurement and the reason it was false
- `erfc_zero_x` reads the measured 27.2 with the subnormal cliff recorded beside it
- DPSGD-03 carries round 2 in round 1's three-part shape, plus an explicit round-3 statement naming
  WARNING-4 as open
- The ROADMAP's five success criteria are proven byte-unchanged by sha256
- Every new validation row's command was executed before the row was written
- The SC3 verdict is withheld; `requirements.mark-complete` is not called
</success_criteria>

<output>
Create `.planning/phases/22-dp-sgd-core-accountant-and-the-correctness-battery/22-19-SUMMARY.md`
when done.
</output>
