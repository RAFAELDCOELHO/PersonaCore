---
phase: 22-dp-sgd-core-accountant-and-the-correctness-battery
plan: 09
type: execute
wave: 4
depends_on: ["22-02", "22-04", "22-05"]
files_modified:
  - tests/test_phase22_accountant.py
  - tests/test_phase22_dpsgd_ast.py
autonomous: true
requirements: [DPSGD-03, DPSGD-04]
validates: [V-06, V-25]

must_haves:
  truths:
    - "D-13 consequence: `GOLDEN_EPSILON` is derived from the QUADRATURE ORACLE, never snapshotted from `accountant.py`. DPSGD-03 requires oracles that cannot share the implementation's failure modes; a golden table read off the implementation shares them by construction and turns the pin into a PHOTOGRAPH OF THE CODE rather than a constraint on it"
    - "D-18: the cross-site consistency test is built HERE, in Phase 22, not deferred — it reads the relation as pinned in `scripts/mitigation_accountant.py`, as documented at `accountant.py`, and as used at `dpsgd.py`'s noise line, and refuses on disagreement"
    - "D-18: deferring this test would ship a definition with nothing enforcing it, into the one phase whose entire purpose is that guards are watched failing before they are believed"
    - "D-18 / RESEARCH F6: D-17 makes the wrong-sensitivity fake impossible at the CODE level but not at the DEFINITIONAL level. An implementation can pass all four D-05 axes and all four D-16 invariants while publishing an epsilon that is 2x optimistic, because every guard compares C against C"
    - "PITFALLS P3's stated warning sign is exactly what this test refuses: the report says add/remove and the accountant's docstring says replace"
    - "The pin cannot be IMPORTED by either site — the ceiling runs one way and `src/` never imports `scripts/` — so the check is a test that READS all three sources"
  artifacts:
    - path: "tests/test_phase22_accountant.py"
      provides: "V-06 — every pinned golden epsilon re-derived from delta_quadrature alone"
      contains: "def test_golden_epsilon_from_oracle"
    - path: "tests/test_phase22_dpsgd_ast.py"
      provides: "V-25 — the three-site adjacency consistency check"
      contains: "def test_adjacency_relation_consistent"
  key_links:
    - from: "tests/test_phase22_dpsgd_ast.py::test_adjacency_relation_consistent"
      to: "scripts/mitigation_accountant.py, src/personacore/privacy/accountant.py, src/personacore/privacy/dpsgd.py"
      via: "a multi-site SOURCE read, because no import path connects them"
      pattern: "NEIGHBOURING"
---

<objective>
Close the two claims that no single-module test can reach: that `GOLDEN_EPSILON` constrains the
implementation rather than photographing it, and that the adjacency relation the report will publish
is the same one the noise line actually implements.

Purpose: **D-18 is the definitional half D-17's guards structurally cannot touch.** Single-sourcing
`self.C` proves the code is self-consistent; it does not prove `C` is the right sensitivity for the
adjacency the report claims. Every existing guard compares `C` against `C`, so an implementation can
pass all four D-05 axes and all four D-16 invariants while publishing an ε that is **2× optimistic**.
And `GOLDEN_EPSILON` only constrains anything if it was derived somewhere other than the code it
constrains.

Output: V-06 in `tests/test_phase22_accountant.py`, V-25 in `tests/test_phase22_dpsgd_ast.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-dp-sgd-core-accountant-and-the-correctness-battery/22-CONTEXT.md
@.planning/phases/22-dp-sgd-core-accountant-and-the-correctness-battery/22-RESEARCH.md
@.planning/phases/22-dp-sgd-core-accountant-and-the-correctness-battery/22-PATTERNS.md
</context>

<environment>
**Python 3.11 venv is MANDATORY.** Commands run as `.venv/bin/python -m pytest ...`.
</environment>

<tasks>

<task type="auto">
  <name>Task 1: V-06 — re-derive every pinned golden epsilon from the ORACLE alone</name>
  <files>tests/test_phase22_accountant.py</files>
  <read_first>
    - `tests/test_phase22_accountant.py` — current state after plan 22-05
    - `scripts/mitigation_accountant.py` — `GOLDEN_EPSILON`, `GOLDEN_EPSILON_REL_TOL`,
      `GOLDEN_EPSILON_PROVENANCE`, `GOLDEN_EPSILON_DELTA_SOURCE`
    - `src/personacore/privacy/accountant.py` — `delta_quadrature`'s signature and refusals
    - `22-CONTEXT.md` D-13's closing paragraph (**CONSEQUENCE of D-09 + DPSGD-03, recorded so a
      planner does not invert it**) and `22-RESEARCH.md` § *For `GOLDEN_EPSILON`'s pin tolerance*
    - `tests/test_phase20_prereg.py:44-50` — the `sys.path.insert(0, str(_ROOT / "scripts"))` idiom
  </read_first>
  <action>
Add `test_golden_epsilon_from_oracle` (**V-06**) to `tests/test_phase22_accountant.py`.

For each `(sigma, steps, epsilon_pinned)` in `GOLDEN_EPSILON`:
  - compute `mu_eff = math.sqrt(steps) / sigma`;
  - bisect ε on `delta_quadrature(eps, mu_eff)` — **the oracle ONLY**. The test-local bisection must
    call neither `delta_closed` nor `epsilon_for`. State in the docstring that this is not a
    contradiction of D-12's single-choke-point rule: D-12 forbids improvising bisection in
    *production* code (`mitigation_budget.py`, a driver), while D-13 *requires* a re-derivation by an
    independent route here. A golden table read off the implementation shares its failure modes by
    construction and would be **a photograph of the code rather than a constraint on it**;
  - read δ from `mitigation_unit.DELTA` via the `sys.path.insert` idiom — never re-spell `1e-5`;
  - assert `abs(eps_oracle - epsilon_pinned) <= GOLDEN_EPSILON_REL_TOL * epsilon_pinned`, with the
    tolerance read from the pin rather than re-typed;
  - state in the docstring why an exact float pin would be **wrong**: the oracle and the
    implementation differ at ~1e-14 by construction, measured worst case 1.07e-14, so `1e-12` carries
    ~2 orders of margin and ~2 orders of tightness against any real implementation error.

Three meta-guards, without which this test can be green over nothing:
  1. `assert len(GOLDEN_EPSILON) == 7` — a truncated pin cannot make the loop vacuous.
  2. Assert the test module contains **no call to `epsilon_for` inside this test's own body** — walk
     the test function's own AST (`ast.parse` of this file, locate the `FunctionDef`) and assert the
     called-name set excludes `epsilon_for`, `delta_closed`. That makes "derived from the oracle"
     structural rather than a claim in a docstring, and it is the assertion that reddens if a future
     edit "simplifies" the test by calling the implementation.
  3. Assert the bisection actually converged — the final bracket width is below a stated relative
     threshold — rather than returning its initial guess.

Add `test_golden_epsilon_would_catch_a_moved_accountant` — the negative control. Take one pinned row,
perturb the target ε by `10 * GOLDEN_EPSILON_REL_TOL` relative, and assert the comparison the main
test performs would FAIL on that value. Without it, a tolerance accidentally widened to 1e-3 would
leave the test green while constraining nothing.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase22_accountant.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `test_golden_epsilon_from_oracle` calls `delta_quadrature` and calls neither `epsilon_for` nor `delta_closed` — asserted by the test's own AST meta-guard, not only by inspection
    - all seven pinned rows re-derive to within `GOLDEN_EPSILON_REL_TOL`
    - `GOLDEN_EPSILON_REL_TOL` is READ from the pin, not re-typed as a literal in the test
    - δ is read from `mitigation_unit.DELTA`
    - `test_golden_epsilon_would_catch_a_moved_accountant` passes, proving the tolerance is not vacuously wide
    - `.venv/bin/python -m pytest tests/test_phase22_accountant.py -q` reports at least 23 tests passed
  </acceptance_criteria>
  <done>Every pinned golden ε is re-derived by bisection over the quadrature oracle alone — structurally asserted, not merely claimed — and a perturbation negative control proves the tolerance actually bites.</done>
</task>

<task type="auto">
  <name>Task 2: V-25 — the three-site adjacency consistency check (D-18)</name>
  <files>tests/test_phase22_dpsgd_ast.py</files>
  <read_first>
    - `tests/test_phase22_dpsgd_ast.py` — current state after plan 22-04
    - `scripts/mitigation_accountant.py` — `NEIGHBOURING`, `SENSITIVITY_MULTIPLIER` and their prose
    - `src/personacore/privacy/accountant.py` — its module docstring's adjacency statement
    - `src/personacore/privacy/dpsgd.py` — its module docstring's adjacency statement and the
      `torch.normal(..., std=...)` noise line
    - `tests/test_phase14_scoring.py:455-463` — the file-set helper and its **"deliberately not
      cached"** reason: the deliberate-RED probes that prove these guards bite add and remove files,
      and a cache would make the guards blind to exactly the thing they are being tested against
    - `22-CONTEXT.md` D-18 in full; `22-RESEARCH.md` F6
    - `.planning/research/PITFALLS.md:143-165` — pitfall P3 and its prescribed test
  </read_first>
  <action>
Add `test_adjacency_relation_consistent` (**V-25**) to `tests/test_phase22_dpsgd_ast.py`.

It reads **three sites** and refuses on disagreement. No import connects them — the `mitigation_*.py`
ceiling allows only `{pathlib, sys, erasure_gate}`, and `src/` never imports `scripts/` — so this is a
multi-site **source read**, which is the same shape D-05 axis 1 already builds.

Site A — `scripts/mitigation_accountant.py`, reached by the `sys.path.insert(0, str(_ROOT /
"scripts"))` idiom: read `NEIGHBOURING` (a string) and `SENSITIVITY_MULTIPLIER` (a float).

Site B — `src/personacore/privacy/accountant.py`: `ast.get_docstring` on the parsed module, then
locate the adjacency statement.

Site C — `src/personacore/privacy/dpsgd.py`: `ast.get_docstring` on the parsed module for the
documented relation, **and** the noise line itself.

Assertions, in this order, each separately messaged:
  1. **Presence at every site FIRST.** Assert the `NEIGHBOURING` phrase (normalized: lowercased,
     whitespace collapsed) occurs in site B's docstring and in site C's docstring, each with its own
     message. **Absent must never count as agreement** — that is the vacuous-guard failure this
     ordering exists to prevent, and it is the same discipline as V-02's `a != 0.0 and b != 0.0`
     precondition.
  2. **Agreement.** The three normalized relation statements name the SAME relation. Also assert no
     site contains the string `"replace-one"` or `"replace one"` while `SENSITIVITY_MULTIPLIER` is
     `1.0` — that combination is PITFALLS P3's stated warning sign verbatim: *the report says
     add/remove and the accountant's docstring says replace*.
  3. **The multiplier matches the code, not just the prose.** Walk `dpsgd.py`'s AST, find the
     `torch.normal` call's `std=` keyword, and assert its expression is exactly the product of
     `self.sigma` and `self.C` with **no additional numeric factor** — i.e. a `BinOp(Mult)` over two
     `Attribute` nodes on `self`, and no `Constant` operand. A `2 * self.sigma * self.C` would be
     `SENSITIVITY_MULTIPLIER = 2.0` in code while the pin says `1.0`, and that is exactly the 2×
     divergence D-18 exists to refuse. Message names D-18 and states the stake: since ε is roughly
     linear in μ over the operating range, the alternative is roughly **2× on every published ε**.
  4. **`SENSITIVITY_MULTIPLIER == 1.0`** read from the pin, with the message naming
     add/remove-one (unbounded DP, Δ = C) versus replace-one (bounded DP, Δ = 2C), and stating that
     D-02's own sensitivity argument — *one record moves the sum by at most `C`* — IS the
     add/remove-one argument.

Meta-guards, non-negotiable (`22-PATTERNS.md` § *Meta-guard: prove the scan is not scanning nothing*):
  - assert all three source files exist and each parses to a non-empty `tree.body`;
  - assert each docstring is non-empty and `len(NEIGHBOURING) > 10`, so a relation degraded to `""`
    cannot make every substring check pass trivially;
  - assert the `torch.normal` call was FOUND in `dpsgd.py` (`assert std_node is not None, "the noise
    call is gone — this guard would check nothing"`).

Add `test_adjacency_check_bites` — the RED half, using the plan-22-01 discipline of feeding **text**
rather than a path. Factor the docstring/noise-line comparison into a helper taking three source
strings, so this test can pass a mutated `dpsgd.py` string (relation swapped to replace-one; and
separately, `std=2.0 * self.sigma * self.C`) and observe the helper raising. The live test passes the
real bytes into the SAME helper — `tests/test_phase20_prereg.py:153-155`'s rule: a guard proved
correct on mutated text and the guard CI runs must be the same code.

Also add a note in the test docstring recording D-18's provenance honestly: `.planning/research/
PITFALLS.md` P3 already prescribed these two constants and assigned them to *"P20 (constant), P21
(accountant consumes it)"*; measured against HEAD before this phase,
`grep -rn "NEIGHBOURING\|SENSITIVITY_MULTIPLIER" scripts/ src/ tests/` returned **zero hits**. It is a
carry-forward gap closed here, not a new question.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase22_dpsgd_ast.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `test_adjacency_relation_consistent` asserts PRESENCE at each of the three sites before asserting agreement
    - the `std=` AST check asserts a two-operand `Mult` over `self.sigma` and `self.C` with no `Constant` operand
    - `SENSITIVITY_MULTIPLIER` and `NEIGHBOURING` are read from the pin, never re-typed in the test
    - `test_adjacency_check_bites` observes the helper raising on BOTH a replace-one relation swap and a `2.0 *` noise-line factor
    - the live test and the RED test call the SAME helper function
    - three meta-guards present: files parse non-empty, docstrings non-empty with `len(NEIGHBOURING) > 10`, and the `torch.normal` node was found
    - `.venv/bin/python -m pytest tests/test_phase22_dpsgd_ast.py -q` reports at least 17 tests passed
    - `.venv/bin/python -m pytest tests/ -q` reports 0 failed
    - `make lint` exits 0
  </acceptance_criteria>
  <done>The adjacency relation is enforced across all three sites — the frozen pin, the accountant's documented relation and the noise line's actual arithmetic — with presence asserted before agreement, the multiplier checked in code rather than only in prose, and the check observed refusing on both a relation swap and a 2× noise factor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| definition → published number | The adjacency relation is a definition, not a code artifact. Nothing in a training loop records which one was meant, and papers use both. This is the only boundary in the phase where every code-level guard is structurally blind. |
| implementation → its own pin | If `GOLDEN_EPSILON` were snapshotted from `accountant.py`, the pin and the implementation would agree by construction and constrain nothing. |
| three unconnected modules | No import path links the pin, the accountant and the mechanism — the ceiling runs one way and `src/` never imports `scripts/`. Consistency can only be checked by reading sources. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-43 | Information Disclosure | an ε published under replace-one while the code implements add/remove-one — roughly 2x optimistic on every point | mitigate | V-25's three-site read: presence asserted first, agreement second, and the multiplier verified in the `std=` AST expression rather than only in prose |
| T-22-44 | Repudiation | a consistency check that passes because the relation is ABSENT at one site | mitigate | Presence at each site is asserted BEFORE agreement, each with its own message — the same ordering discipline as V-02's non-zero precondition |
| T-22-45 | Tampering | `GOLDEN_EPSILON` becoming a photograph of `accountant.py` | mitigate | V-06 bisects over `delta_quadrature` only, asserted structurally by walking the test's own AST for forbidden callees, plus a perturbation negative control proving the tolerance bites |
| T-22-46 | Tampering | a `2.0 *` factor slipped into the noise `std` (definitional wrong-sensitivity) | mitigate | The `std=` AST check refuses any `Constant` operand; `test_adjacency_check_bites` observes it raising on exactly that mutation |
| T-22-47 | Repudiation | the D-18 constants shipped with nothing enforcing them | mitigate | The test is built HERE rather than deferred — D-18's explicit requirement, on the stated ground that deferring it would ship a constant nothing enforces into the phase whose purpose is watched guards |
| T-22-SC | Tampering | package installs | accept | No installs; `pyproject.toml` byte-unchanged (RPT-03) |
</threat_model>

<verification>
```bash
.venv/bin/python -m pytest tests/test_phase22_accountant.py tests/test_phase22_dpsgd_ast.py -x -q
.venv/bin/python -m pytest tests/ -q
git diff --exit-code -- scripts/mitigation_accountant.py scripts/mitigation_gate.py scripts/mitigation_unit.py pyproject.toml
make lint
```
</verification>

<success_criteria>
- All seven `GOLDEN_EPSILON` rows re-derive from `delta_quadrature` alone, with the "no `epsilon_for`/`delta_closed`" restriction asserted structurally
- A perturbation negative control proves `GOLDEN_EPSILON_REL_TOL` is not vacuously wide
- The adjacency relation agrees across the pin, `accountant.py`'s docstring and `dpsgd.py`'s docstring, with presence asserted before agreement
- The noise `std=` expression is verified to be `self.sigma * self.C` with no numeric factor
- The consistency helper is observed RAISING on a relation swap and on a `2.0 *` factor, through the same code the live test runs
- `scripts/mitigation_accountant.py` is byte-unchanged by this plan
</success_criteria>

<output>
Create `.planning/phases/22-dp-sgd-core-accountant-and-the-correctness-battery/22-09-SUMMARY.md` when done.
</output>
