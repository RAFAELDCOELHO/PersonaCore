---
phase: 22-dp-sgd-core-accountant-and-the-correctness-battery
plan: 07
type: execute
wave: 4
depends_on: ["22-05", "22-06"]
files_modified:
  - src/personacore/checkpoint.py
  - tests/test_phase22_checkpoint.py
autonomous: true
requirements: [DPSGD-05, DPSGD-07]
validates: [V-15, V-16, V-17]

must_haves:
  truths:
    - "DPSGD-05: `checkpoint.py` carries an MPS RNG slot with BACKWARD-COMPATIBLE load — every pre-Phase-22 checkpoint still resumes"
    - "D-14: `rng[\"mps\"]` restore uses `rng.get(\"mps\")`, NOT `rng[\"mps\"]`. `rng[\"cuda\"]` at `checkpoint.py:142` uses `[]`, and an old checkpoint under that form raises `KeyError` — which is exactly the backward compatibility DPSGD-05 requires"
    - "D-14: TWO separately-named slots coexist and neither is collapsed into the other — `rng[\"mps\"]` is DPSGD-05's literal requirement, recorded honestly as REQUIRED-BUT-UNEXERCISED; `dp_noise_rng` is the 44-byte slot the DP path actually fires"
    - "D-14: a DEDICATED generator's draw does not change the global MPS state, so the 'guard that never fires' problem is decided by WHICH SLOT carries the DP state, not by the generator's device"
    - "DPSGD-05: a kill->resume reproduces a BIT-IDENTICAL reported epsilon — exact `==` is correct here because it compares the SAME call shape across two processes (RESEARCH F3), unlike V-03's rel_tol"
    - "DPSGD-07: `LoRALinear` stays bare `nn.Parameter`s. `checkpoints/persona_adapter.pt` and every v3.0 checkpoint load unchanged, and the state-dict key set is byte-identical"
    - "`dp_noise_rng` arrives through `**extra` with NO format change, exactly as `fisher`/`theta_star` did. `CKPT_SCHEMA_VERSION` does not bump"
  artifacts:
    - path: "src/personacore/checkpoint.py"
      provides: "the mps RNG slot beside cuda, with a None-safe backward-compatible restore"
      contains: "rng.get(\"mps\")"
    - path: "tests/test_phase22_checkpoint.py"
      provides: "V-15 resume-epsilon bit-identity, V-16 mps round-trip + back-compat, V-17 LoRA key-set stability"
      contains: "def test_resume_epsilon_bit_identical"
  key_links:
    - from: "src/personacore/checkpoint.py::load_checkpoint"
      to: "rng.get(\"mps\")"
      via: "the None-safe read that keeps pre-Phase-22 checkpoints loadable"
      pattern: "rng\\.get\\(\"mps\"\\)"
    - from: "tests/test_phase22_checkpoint.py"
      to: "checkpoints/persona_adapter.pt"
      via: "a real committed v3.0 artifact loaded unchanged (DPSGD-07)"
      pattern: "persona_adapter\\.pt"
---

<objective>
Land the MPS RNG slot with a backward-compatible load, and prove the two things DPSGD-05 actually
gates on: every pre-Phase-22 checkpoint still resumes, and a kill→resume reproduces a **bit-identical
reported ε** rather than merely a matching loss curve.

Purpose: the slot is five lines now and a full sweep re-run later. The subtlety is that
`checkpoint.py:142` restores `rng["cuda"]` with a **subscript**, not `.get()` — copying that form for
`mps` would make every checkpoint written before this phase raise `KeyError` on resume, which is
precisely the backward compatibility the requirement demands. And D-14's honest half: the DP path
uses the separately-named `dp_noise_rng` slot, so `rng["mps"]` is recorded as
**required-but-unexercised** rather than dressed up as load-bearing.

Output: the `mps` slot in `checkpoint.py`, and `tests/test_phase22_checkpoint.py` (V-15, V-16, V-17).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-dp-sgd-core-accountant-and-the-correctness-battery/22-CONTEXT.md
@.planning/phases/22-dp-sgd-core-accountant-and-the-correctness-battery/22-PATTERNS.md
@.planning/phases/22-dp-sgd-core-accountant-and-the-correctness-battery/22-VALIDATION.md
</context>

<environment>
**Python 3.11 venv is MANDATORY.** Commands run as `.venv/bin/python -m pytest ...`.
CI is CPU-only; the MPS-touching assertion is `skipif`-gated and honestly recorded as such.
</environment>

<tasks>

<task type="auto">
  <name>Task 1: The `rng["mps"]` slot — saved beside `cuda`, restored None-safely (V-16)</name>
  <files>src/personacore/checkpoint.py, tests/test_phase22_checkpoint.py</files>
  <read_first>
    - `src/personacore/checkpoint.py:28-32` — `CKPT_SCHEMA_VERSION = 1`. **It must not bump.**
    - `src/personacore/checkpoint.py:40-54` and `:85-90` — `_RESERVED_CKPT_KEYS` and the clash
      refusal. `dp_noise_rng` is not reserved, so it arrives through `**extra` with no format change
    - `src/personacore/checkpoint.py:102-107` — the `rng` dict. `:106`'s `cuda` line is the exact
      form the `mps` line mirrors, including the `None`-when-unavailable shape
    - `src/personacore/checkpoint.py:114-145` — `load_checkpoint`. **`:138-143`: `rng["cuda"]` uses a
      SUBSCRIPT.** `:122-124`'s docstring already records the `ckpt.get("scaler")` precedent verbatim
      — *"a pre-fix (scaler-less) checkpoint resumes cleanly without it — the open-dict format stays
      backward compatible"*
    - `tests/test_checkpoint.py` and `tests/test_resume_curve.py` — the existing round-trip shapes
    - `tests/test_loop_penalty_fn.py:96-107` — the skipif REGISTER to copy: a long `reason=` string
      naming why the skip is honest and what still carries the guarantee
  </read_first>
  <action>
**(a) Save side.** Add to the `rng` dict at `checkpoint.py:102-107`, directly beneath the `cuda`
line and in the identical form:
`"mps": (torch.mps.get_rng_state() if torch.backends.mps.is_available() else None),`
with a comment naming DPSGD-05 and D-14: this is DPSGD-05's literal requirement, latent until now
because the loop consumed zero device RNG and DP noise is the first per-step consumer.

**(b) Restore side.** In `load_checkpoint`, beneath the `cuda` restore:
`if rng.get("mps") is not None and torch.backends.mps.is_available(): torch.mps.set_rng_state(rng["mps"])`
**`.get()`, never a subscript.** Add a comment stating the reason explicitly and pointing at the
sibling: `rng["cuda"]` uses `[]` because the `cuda` key has existed since schema v1, but `mps`
arrives now, so every checkpoint written before Phase 22 lacks it — a subscript here would raise
`KeyError` on exactly the checkpoints DPSGD-05 requires to still load. `ckpt.get("scaler")` is the
recorded precedent in this same function.

**(c) The honest record (D-14).** Add a paragraph to `save_checkpoint`'s or the module's docstring
naming BOTH slots and refusing to collapse them:
  - `rng["mps"]` — DPSGD-05's literal requirement, **required-but-unexercised**: the DP path uses a
    DEDICATED generator (D-07), and a dedicated generator's draw does not change the global MPS
    state. Measured: a real forward+backward on MPS also leaves `torch.mps.get_rng_state()`
    unchanged. So this slot is correct to carry and honest to describe as not fired by the DP path.
  - `dp_noise_rng` — 44 bytes, arriving through `**extra`, **the slot the DP path actually fires**.
    `set_state` round-trips exactly.
  Also record the measurement that decided the generator's device: CPU and MPS `torch.Generator`s
  do NOT agree at the same seed, a CPU generator cannot fill an MPS tensor
  (`RuntimeError: Expected a 'mps' device type for generator but found 'cpu'`), and the cost delta
  at `MAX_STEPS = 200` is **1.76 s per arm against 4.77 h/point evaluation — 0.01%, so cost decides
  nothing.** The decision was made on which SLOT carries the DP state, not on the generator's device.

**`CKPT_SCHEMA_VERSION` stays `1`.** The `rng` dict is already open in practice and `dp_noise_rng`
rides `**extra`; nothing about the format changes.

**(d) Tests — create `tests/test_phase22_checkpoint.py`** with V-16's two halves:
  - `test_old_checkpoint_without_mps_slot_still_loads` (**the binding half, runs everywhere**) —
    save a checkpoint, then **delete the `"mps"` key from its `rng` dict** and re-save, simulating a
    pre-Phase-22 artifact. Assert `load_checkpoint` succeeds and raises no `KeyError`. Then do the
    same against a **real committed artifact**: load one of `checkpoints/*_latest.pt` written before
    this phase and assert it resumes. Meta-guard: assert the chosen real file exists and that its
    `rng` dict genuinely lacks `"mps"`, so the test cannot pass by loading an artifact that already
    has the key.
  - `test_mps_rng_slot_round_trips` (**MPS-gated**) — `@pytest.mark.skipif(not
    torch.backends.mps.is_available(), reason=...)` where the `reason=` string is a full paragraph in
    `test_loop_penalty_fn.py:98-106`'s register: it must state that CI is CPU-only, that D-14 records
    this slot as **required-but-unexercised on purpose** because the DP path uses the separately-named
    `dp_noise_rng` slot, and what still carries the guarantee when this skips (the back-compat test
    above, which does not skip). This is the FIRST MPS-gated test in `tests/` — there is no analog,
    so the register is what makes the skip honest rather than a hole.
  - `test_dp_noise_rng_rides_extra_without_a_schema_bump` — save with
    `dp_noise_rng=<44-byte state>` through `**extra`; assert it round-trips byte-identically, that
    `ckpt["schema_version"] == 1`, and that passing `dp_noise_rng` does NOT trip the
    `_RESERVED_CKPT_KEYS` clash refusal. Also assert the clash refusal still fires for a genuinely
    reserved key (e.g. `rng=`), so the test proves the guard is live rather than absent.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase22_checkpoint.py tests/test_checkpoint.py tests/test_resume_curve.py tests/test_fisher_checkpoint.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'rng.get("mps")' src/personacore/checkpoint.py` returns the restore line; `grep -n 'rng\["mps"\]' src/personacore/checkpoint.py` shows the subscript ONLY inside the `.get()`-guarded body
    - `grep -n "CKPT_SCHEMA_VERSION = 1" src/personacore/checkpoint.py` still matches — no bump
    - `.venv/bin/python -m pytest tests/test_phase22_checkpoint.py tests/test_checkpoint.py tests/test_resume_curve.py tests/test_fisher_checkpoint.py -x -q` exits 0
    - `test_old_checkpoint_without_mps_slot_still_loads` asserts the chosen real `checkpoints/*_latest.pt` genuinely lacks the `"mps"` key before asserting the load
    - the MPS skipif `reason=` string contains the words "required-but-unexercised" and names `dp_noise_rng` as the slot the DP path actually fires
    - `.venv/bin/python -m pytest tests/ -q` reports 0 failed
  </acceptance_criteria>
  <done>The `mps` slot saves beside `cuda` and restores through `.get()`; a real pre-Phase-22 committed checkpoint is proven to still load; `dp_noise_rng` rides `**extra` with no schema bump; and the MPS-gated assertion carries an honest `reason=` naming what still holds when it skips.</done>
</task>

<task type="auto">
  <name>Task 2: V-15 — a kill→resume reproduces a bit-identical reported epsilon</name>
  <files>tests/test_phase22_checkpoint.py</files>
  <read_first>
    - `tests/test_phase22_checkpoint.py` — current state after Task 1
    - `tests/test_resume_curve.py` — the existing kill→resume shape to follow
    - `src/personacore/training/loop.py` — the `_dp_extra()` refresh landed by plan 22-06
    - `src/personacore/privacy/accountant.py` — `epsilon_for(sigma, steps, delta)`
    - `22-RESEARCH.md` F3's closing paragraph — *"DPSGD-05 requires a kill->resume to reproduce a
      bit-identical reported eps. That is achievable and should be kept as `==` — it compares THE
      SAME CALL SHAPE across two processes, which is deterministic. The tolerance applies only to
      comparing two DIFFERENT call shapes. The plan must not conflate them."*
    - `src/personacore/lora/inject.py:113-118` — the recorded reasoning for exact equality:
      the same operation on the same operands gives a bit-identical float, and *"a tolerance would
      only weaken this"*. Cite it when justifying `==` here
  </read_first>
  <action>
Add `test_resume_epsilon_bit_identical` (**V-15**) to `tests/test_phase22_checkpoint.py`.

Shape: run a short CPU GPT+LoRA training with the `dp_fn` seam active for `k` steps, checkpoint,
**kill** (drop the process state by constructing everything fresh from the checkpoint, following
`tests/test_resume_curve.py`'s existing pattern), resume for the remaining steps, and compare the
**reported ε** from the uninterrupted run against the resumed run.

Required properties, each asserted rather than assumed:
  1. **Exact `==`, not a tolerance.** `epsilon_a == epsilon_b`, byte-for-byte on the float. Docstring
     must state WHY this differs from V-03's `rel_tol=1e-12`: this compares **the same call shape**
     (`epsilon_for(sigma, steps, delta)` with identical arguments) across two processes, which is
     deterministic — while V-03 compares two DIFFERENT call shapes whose double-rounding disagrees
     bitwise 19.9% of the time. Cite `src/personacore/lora/inject.py:113-118`'s reasoning that the
     same operation on the same operands gives a bit-identical float and *"a tolerance would only
     weaken this"*. **The two registers must not be conflated; one is `==`, the other is `rel_tol`.**
  2. **σ = 0 is included as a parametrized case.** D-12's premise correction identifies this as
     exactly where the forward direction bites: without the explicit branch, Phase 23's first
     executed run crashes at report time rather than in the mechanism. Assert
     `math.isinf(epsilon_a) and epsilon_a == epsilon_b` there too (`inf == inf` is True).
  3. **The `dp_noise_rng` state is what makes the resume real.** Assert that the resumed
     `DPSGD._g.get_state()` is `torch.equal` to the state saved at the kill point, and — the load-
     bearing negative control — that a resume which **skips** `load_noise_rng_state` produces a
     DIFFERENT subsequent noise draw. Without that control the test would pass even if the state were
     never restored, because ε is a function of (σ, steps, δ) and not of the RNG. Say so in the
     docstring: **ε bit-identity alone does not prove the RNG resumed**; the negative control is what
     closes that gap.
  4. A meta-guard asserting the run actually took the DP path — e.g. `dp._records > 0` and the
     checkpoint contains a non-empty `dp_noise_rng` — so a resume test over a run where `dp_fn` was
     silently `None` cannot pass.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase22_checkpoint.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `test_resume_epsilon_bit_identical` uses `==` on the two ε values, with no `pytest.approx` and no `rel_tol`
    - the docstring states both tolerance registers and cites `lora/inject.py:113-118`
    - the σ=0 case is a parametrization and asserts `math.isinf`
    - the negative control (resume WITHOUT `load_noise_rng_state`) is asserted to produce a different noise draw
    - the meta-guard asserts the checkpoint carries a non-empty `dp_noise_rng`
    - `.venv/bin/python -m pytest tests/test_phase22_checkpoint.py -x -q` exits 0
  </acceptance_criteria>
  <done>A kill→resume reproduces the reported ε under exact float equality, including at σ=0; the generator state is proven to be what carries the resume, via a negative control that fails when the state is not restored.</done>
</task>

<task type="auto">
  <name>Task 3: V-17 — `LoRALinear` unrestructured; every v3.0 checkpoint and `persona_adapter.pt` still load</name>
  <files>tests/test_phase22_checkpoint.py</files>
  <read_first>
    - `src/personacore/lora/layer.py:41` — `lora_A` / `lora_B` as bare `nn.Parameter` in an inline
      matmul. **DPSGD-07 forbids restructuring into `nn.Linear` submodules**; module hooks do not
      reach them, which is a second reason D-01 rejected the hook design
    - `tests/test_lora_artifact.py` and `tests/test_lora_inject.py` — the existing key-set /
      round-trip assertions this task extends by ONE
    - `checkpoints/persona_adapter.pt` — the shippable committed artifact
    - `src/personacore/lora/inject.py:96-118` — the key-set / shape / scale audit trio
  </read_first>
  <action>
Add two tests to `tests/test_phase22_checkpoint.py`.

`test_lora_state_dict_keys_unchanged` (**V-17**) — build a fresh `GPT(ModelConfig())` +
`inject_lora` + `mark_only_lora_trainable`, take `lora_state_dict(model)`, and assert its key set
**exactly equals** the key set inside `checkpoints/persona_adapter.pt`'s adapter payload. Hard
equality, never a subset — a subset passes while a key silently disappears. Message names DPSGD-07:
restructuring `LoRALinear` into `nn.Linear` submodules would rename every state-dict key and
invalidate `persona_adapter.pt` and every v3.0 checkpoint. Meta-guard: assert the key set is
non-empty and that `len(keys) == 2 * 6 * n_layer` (an `A` and a `B` per wrapped projection), so a
collapsed load cannot pass by comparing two empty sets.

`test_v3_checkpoints_still_load` (**V-17**) — parametrize over at least three real committed
artifacts of different shapes (an adapter, a `*_latest.pt` full checkpoint, and
`checkpoints/persona_adapter.pt`), asserting each loads through the production loader after this
phase's `checkpoint.py` change. Meta-guard: assert every parametrized path exists on disk before
asserting the load, and fail with the missing path named — a parametrization over absent files would
collect zero cases and report green.

Add a third, cheap assertion that makes DPSGD-07 structural rather than observational:
`test_lora_linear_holds_bare_parameters` — walk a fresh injected model and assert every `LoRALinear`
has `lora_A` and `lora_B` as `torch.nn.Parameter` instances and has **no child `nn.Linear` other
than `base`**. That is the property a restructuring would break, checked directly rather than
inferred from a key set.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase22_checkpoint.py tests/test_lora_artifact.py tests/test_lora_inject.py tests/test_lora_layer.py tests/test_lora_merge.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - the key-set assertion uses `==` on two sets, never `<=` or `issubset`
    - `test_v3_checkpoints_still_load` asserts each parametrized path exists before loading, and covers at least 3 real committed artifacts including `checkpoints/persona_adapter.pt`
    - `test_lora_linear_holds_bare_parameters` asserts `lora_A`/`lora_B` are `nn.Parameter` and that the only child `nn.Linear` is `base`
    - `git diff --exit-code -- src/personacore/lora/` exits 0 — `LoRALinear` is byte-unchanged
    - `.venv/bin/python -m pytest tests/ -q` reports 0 failed
    - `make lint` exits 0
  </acceptance_criteria>
  <done>DPSGD-07 is proven three ways: the adapter key set matches the committed artifact exactly, three real v3.0 artifacts load through the modified checkpoint path, and `LoRALinear` is asserted to still hold bare `nn.Parameter`s with `base` as its only child `nn.Linear`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pre-Phase-22 checkpoint → new loader | Every artifact ever written by this project crosses here. A subscript read of a key that did not exist then breaks all of them at once. |
| process kill → resumed process | The reported ε must survive a process boundary. ε is a function of (σ, steps, δ), so ε bit-identity alone does NOT prove the RNG resumed — that is a separate claim needing its own control. |
| requirement wording → what actually fires | DPSGD-05 names an MPS RNG slot; the DP path fires a different, dedicated one. Describing the first as load-bearing would be a false claim in the phase whose purpose is that guards are watched before they are believed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-32 | Denial of Service | `rng["mps"]` subscript raising `KeyError` on every pre-Phase-22 checkpoint | mitigate | Restore via `rng.get("mps")`, with the `ckpt.get("scaler")` precedent cited; a REAL committed `checkpoints/*_latest.pt` is loaded in the test and asserted to genuinely lack the key first |
| T-22-33 | Repudiation | a resume test green while the noise RNG was never restored | mitigate | Negative control: a resume that skips `load_noise_rng_state` must produce a DIFFERENT noise draw. Stated in the docstring — ε bit-identity alone does not prove the RNG resumed |
| T-22-34 | Tampering | `==` and `rel_tol` conflated between V-15 and V-03 | mitigate | V-15 uses exact `==` with `lora/inject.py:113-118` cited; both registers written down in the same docstring |
| T-22-35 | Repudiation | `rng["mps"]` presented as load-bearing when the DP path does not fire it | mitigate | D-14's honest record in the module docstring and in the skipif `reason=`, naming it required-but-unexercised and naming `dp_noise_rng` as the slot that fires |
| T-22-36 | Tampering | a `LoRALinear` restructuring silently invalidating every v3.0 artifact | mitigate | Hard key-set equality against `checkpoints/persona_adapter.pt`, three real artifacts loaded, and a direct structural assertion that `lora_A`/`lora_B` are bare `nn.Parameter`s |
| T-22-37 | Tampering | a schema bump breaking forward compatibility for no reason | mitigate | `CKPT_SCHEMA_VERSION` asserted still `1`; `dp_noise_rng` rides `**extra` and is proven not to trip the `_RESERVED_CKPT_KEYS` clash |
| T-22-SC | Tampering | package installs | accept | No installs; `pyproject.toml` byte-unchanged (RPT-03) |
</threat_model>

<verification>
```bash
.venv/bin/python -m pytest tests/test_phase22_checkpoint.py -x -q
.venv/bin/python -m pytest tests/test_checkpoint.py tests/test_resume_curve.py tests/test_fisher_checkpoint.py tests/test_lora_artifact.py -x -q
.venv/bin/python -m pytest tests/ -q
git diff --exit-code -- src/personacore/lora/ pyproject.toml
make lint
```
</verification>

<success_criteria>
- `rng["mps"]` saves beside `cuda`; the restore uses `.get()` and a real pre-Phase-22 committed checkpoint is proven to still load
- `CKPT_SCHEMA_VERSION` is unchanged and `dp_noise_rng` rides `**extra` without tripping the reserved-key clash
- A kill→resume reproduces the reported ε under exact `==`, including at σ=0, with a negative control proving the generator state is what carries it
- Both tolerance registers (`==` here, `rel_tol` in V-03) are written down together
- The two RNG slots are named apart, with `rng["mps"]` recorded as required-but-unexercised
- `LoRALinear` is byte-unchanged; the adapter key set matches `checkpoints/persona_adapter.pt` exactly and three real v3.0 artifacts load
</success_criteria>

<output>
Create `.planning/phases/22-dp-sgd-core-accountant-and-the-correctness-battery/22-07-SUMMARY.md` when done.
</output>
