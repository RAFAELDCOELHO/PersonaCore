---
phase: 22-dp-sgd-core-accountant-and-the-correctness-battery
plan: 13
type: execute
wave: 7
gap_closure: true
depends_on: []
files_modified:
  - src/personacore/training/loop.py
  - tests/test_phase22_dpsgd.py
autonomous: true
requirements: [DPSGD-04, DPSGD-05]
validates: [V-15]
closes_gaps:
  - "WARNING-1 (verification): `loop.py:735-736` is a silent no-op fallback, and the SYMMETRIC hole nobody had named — `dp_fn is None` with `ckpt['dp_noise_rng']` PRESENT — is unguarded and worse"

must_haves:
  truths:
    - "Resuming WITHOUT the DP seam from a checkpoint a DP run wrote is REFUSED, not silently permitted: `dp_fn is None` plus a present `dp_noise_rng` slot means the run that wrote it was private and this one is not"
    - "The OTHER direction — `dp_fn` set, slot absent — stays a non-refusal, and the code now records WHY, so a future reader does not 'fix' it into a refusal and redden two committed controls"
    - "The refusal was WATCHED failing: deleting the raise reddens the new test, and the file is restored byte-identically (sha256 equal, `git diff --exit-code` clean)"
  artifacts:
    - path: "src/personacore/training/loop.py"
      provides: "a refusal on the seamless-resume-of-a-DP-checkpoint direction, plus the recorded reachability argument for the other direction"
      contains: "dp_noise_rng"
    - path: "tests/test_phase22_dpsgd.py"
      provides: "the three-way boundary test: refuse / restore / resume-silently"
      contains: "def test_resume_without_the_seam_refuses_a_dp_checkpoint"
  key_links:
    - from: "src/personacore/training/loop.py::train"
      to: "the resume_from block"
      via: "a ValueError when dp_fn is None and the checkpoint carries dp_noise_rng"
      pattern: "dp_fn is None and ckpt"
---

<objective>
Close the symmetric half of WARNING-1: a run resumed WITHOUT the DP seam from a checkpoint a DP run
wrote continues training with no clipping and no noise, silently, while the checkpoint's own
`dp_noise_rng` slot says the mechanism that produced it was private.

Purpose: the verifier traced the review's CR-04 framing and REJECTED it — all three
`save_checkpoint` sites splat `_dp_extra()` (`loop.py:877`, `:900`, `:927`), so every checkpoint a
DP run writes carries the slot, and a MISSING slot therefore means the prior run was not a DP run
and released nothing, which makes fresh seeding correct rather than a replay. What the verifier
found instead is the direction nobody had named, and it is worse: `dp_fn is None` with the slot
PRESENT is equally unguarded and turns a private run into a non-private one without a sound.

Output: one refusal covering the dangerous direction, the safe direction recorded in the code with
its reachability argument and its two committed controls named, and the refusal watched failing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-dp-sgd-core-accountant-and-the-correctness-battery/22-VERIFICATION.md
@.planning/phases/22-dp-sgd-core-accountant-and-the-correctness-battery/22-CONTEXT.md
</context>

<environment>
**Python 3.11 venv is MANDATORY.** The dev box runs 3.14 and a pyenv 3.12.13 is first on PATH.

- `make test` is BROKEN (bare `pytest` → pyenv 3.12.13 → ~83 `ModuleNotFoundError: torch`). Use
  `.venv/bin/python -m pytest -q`. `make lint` works but over a different file set — use
  `.venv/bin/ruff check . && .venv/bin/ruff format --check .`
- The full suite takes **~220 s**; allow a **400 s+** timeout. Baseline: **1280 passed, 1 skipped**.
- `src/personacore/lora/` must stay byte-unchanged (DPSGD-07): `git diff --exit-code -- src/personacore/lora/`.
- `CKPT_SCHEMA_VERSION` must NOT bump — `dp_noise_rng` rides `**extra` by design.
- **`gsd-sdk` mutation handlers corrupt planning frontmatter.** Use the `--flag` form, never
  positional; write `22-13-SUMMARY.md` BEFORE calling `roadmap.update-plan-progress`; then
  `git diff .planning/` and hand-repair before committing.
</environment>

<tasks>

<task type="auto">
  <name>Task 1: refuse the seamless resume of a DP checkpoint, and record why the other direction is not a refusal</name>
  <files>src/personacore/training/loop.py</files>
  <read_first>
    - `src/personacore/training/loop.py:705-737` — `_dp_extra()` and the whole `resume_from` block,
      including the twenty-line comment above the restore that names the consequence and then
      permits it
    - `src/personacore/training/loop.py:870-930` — the THREE `save_checkpoint` call sites, each
      splatting `**_dp_extra()`; this is the measurement the safe direction's argument rests on
    - `tests/test_phase22_dpsgd.py:1035-1058` — the BACK-COMPAT leg that deliberately resumes a
      DP seam from a checkpoint WITHOUT the slot and asserts the generator is left untouched
    - `tests/test_phase22_checkpoint.py:522-540` — the NEGATIVE CONTROL that strips `dp_noise_rng`
      and resumes with a DP seam, asserting the stream diverges
  </read_first>
  <action>
Add ONE refusal to `train()`'s `resume_from` block, immediately before the existing restore
condition: when `dp_fn is None` and `ckpt.get("dp_noise_rng") is not None`, `raise ValueError`.

The message must state the whole thing rather than gesture at it: the checkpoint carries a
`dp_noise_rng` slot, which only a run with a live DP seam writes (all three `save_checkpoint` sites
splat `_dp_extra()`, so the slot's presence IS the provenance); resuming it with `dp_fn=None`
continues training the same parameters with no per-record clip and no Gaussian noise while every
downstream artifact still reads as a DP run's continuation; and nothing else in the tree can notice
— `DPSGD`'s runtime invariants are not constructed at all on this path, so there is no seam to
refuse. Name the fix in the message: pass the `dp_fn=` seam, or resume from a checkpoint a non-DP
run wrote.

**Do NOT implement the review's CR-04 fix.** CR-04 proposes refusing the OTHER direction — `dp_fn`
set with the slot absent. That inverts two committed assertions:
`tests/test_phase22_dpsgd.py::test_dp_noise_rng_round_trips_through_a_kill_and_resume`'s BACK-COMPAT
leg and `tests/test_phase22_checkpoint.py::test_resume_epsilon_bit_identical`'s negative control both
drive exactly that case and assert it is TOLERATED. It is tolerated on purpose: `.get()` rather than
a subscript is D-14's backward-compatibility decision, every checkpoint written before Phase 22
lacks the key, and a freshly seeded generator that has released nothing is not replaying anything.
Refusing it would break two guards to close a hole that measurement says is not there.

Rewrite the comment above the restore so both directions are stated and neither can be misread:
  - **Slot present, seam live** → restore. The existing paragraph on replaying released noise stays;
    it is correct and it is the reason the restore exists.
  - **Slot absent, seam live** → seed fresh, deliberately, NOT a refusal. Give the reachability
    argument with its evidence: the three splat sites mean a DP run's checkpoints always carry the
    slot, so an absent slot means the prior run was not DP; and name the two committed tests that
    pin this as a FEATURE, so the next reader knows that "fixing" it costs two guards.
  - **Slot present, seam absent** → refuse, the new branch, with a one-line statement of why it is
    the worse direction: the first two continue a private mechanism, this one silently stops being
    one.

Nothing else in `loop.py` changes. Do not touch `_dp_extra`, the save sites, or the schema version.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase22_dpsgd.py tests/test_phase22_checkpoint.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "dp_fn is None and ckpt" src/personacore/training/loop.py` returns 1
    - `.venv/bin/python -m pytest tests/test_phase22_dpsgd.py tests/test_phase22_checkpoint.py -q` exits 0 with 0 failures — in particular `test_dp_noise_rng_round_trips_through_a_kill_and_resume` and `test_resume_epsilon_bit_identical` still pass, unmodified
    - `grep -n "CKPT_SCHEMA_VERSION" src/personacore/checkpoint.py` still shows `1`
    - `git diff --exit-code -- src/personacore/lora/` exits 0
    - the comment above the restore names all THREE (seam, slot) combinations and cites the two committed tests that pin the tolerated one
  </acceptance_criteria>
  <done>The dangerous direction refuses with a message that states the provenance argument; the tolerated direction is documented with the measurement that makes it correct and the two tests that would redden if someone refused it.</done>
</task>

<task type="auto">
  <name>Task 2: pin the boundary in all three directions, and watch the refusal fail</name>
  <files>tests/test_phase22_dpsgd.py, src/personacore/training/loop.py</files>
  <read_first>
    - `tests/test_phase22_dpsgd.py:965-1058` —
      `test_dp_noise_rng_round_trips_through_a_kill_and_resume` in full: `_model`, `_seam`, the
      `max_steps_override` kill idiom, and how a checkpoint blob is edited and re-saved in-test
    - `tests/test_phase22_fakes.py:1-90` — the mutation-probe register: exactly-once target
      assertion, an unmutated control, and the verdict read from pytest's own summary line
  </read_first>
  <action>
Add `test_resume_without_the_seam_refuses_a_dp_checkpoint` to `tests/test_phase22_dpsgd.py`,
reusing that module's existing `_model` / `_seam` / `train(...)` idiom rather than inventing a
second harness.

Three legs, in one test, so the guard's boundary is pinned rather than only its positive case:
  1. **The refusal.** Run one step of `train(dp_fn=<seam>, checkpoint_path=…, max_steps_override=1)`
     to write a real DP checkpoint. Assert `"dp_noise_rng" in blob` FIRST — the meta-guard, because
     a test that resumes a checkpoint lacking the key would be asserting nothing. Then call
     `train(resume_from=<that checkpoint>, dp_fn=None, …)` inside
     `pytest.raises(ValueError, match="dp_noise_rng")`.
  2. **The tolerated direction, still tolerated.** Delete the key from the blob, re-save, resume
     WITH a seam, and assert it completes and leaves the fresh generator state untouched — the same
     property the BACK-COMPAT leg already pins, asserted here so this test documents the boundary on
     both sides rather than only the side it adds.
  3. **The ordinary non-DP resume.** Resume a checkpoint with no `dp_noise_rng` and `dp_fn=None`,
     and assert it completes without raising. Without this leg the new refusal could be over-broad
     (refusing every seamless resume) and still pass leg 1.

**Then watch it fail.** Record `sha256` of `src/personacore/training/loop.py`. Delete the new
`raise` statement (leaving the condition or removing both — state which), re-run
`.venv/bin/python -m pytest tests/test_phase22_dpsgd.py -q`, and confirm leg 1 reddens. Capture the
pytest summary line and the verbatim failure message. Restore, and prove the restore: `sha256` equal
to the pre-probe value and `git diff --exit-code -- src/personacore/training/loop.py` exits 0.
Report how many DISTINCT tests the single mutation reddened — if the answer is one, say one.

Run the FULL suite and record the count: expected `1280 passed` plus this plan's new test, zero
regressions. Allow a 400 s+ timeout.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest "tests/test_phase22_dpsgd.py::test_resume_without_the_seam_refuses_a_dp_checkpoint" -q` exits 0
    - the SUMMARY records the mutation, the pytest summary line under it, the verbatim RED message, and the count of distinct reddened tests
    - `shasum -a 256 src/personacore/training/loop.py` matches the pre-probe value after the restore
    - `git diff --exit-code -- src/personacore/training/loop.py` exits 0 after the restore
    - `.venv/bin/python -m pytest -q` reports at least 1281 passed with 0 failed
    - `.venv/bin/ruff check . && .venv/bin/ruff format --check .` exits 0
  </acceptance_criteria>
  <done>All three (seam, slot) combinations are asserted, the refusal was observed failing when its raise was deleted, and the restore is sha256-identical.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a checkpoint's provenance → the mechanism that continues it | `dp_noise_rng`'s presence is the only record that the run which wrote a checkpoint was private. Nothing else in the blob distinguishes a DP run from a non-DP one. |
| a silent branch → a published privacy claim | `train()`'s `resume_from` block had no `else` in either direction; a wrong pairing produced no log line, no warning and no refusal. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-24 | Spoofing | a DP checkpoint continued by a non-DP run | mitigate | `raise ValueError` when `dp_fn is None` and the slot is present; pinned by leg 1 and watched reddening when the raise is deleted |
| T-22-25 | Tampering | a future "symmetry" edit refusing the tolerated direction | mitigate | The comment names the three splat sites, the reachability argument and the two committed tests that would redden; leg 2 and leg 3 pin the tolerated cases explicitly |
| T-22-26 | Denial of Service | an over-broad refusal blocking every seamless resume | mitigate | Leg 3 resumes an ordinary non-DP checkpoint with `dp_fn=None` and asserts it completes |
| T-22-SC | Tampering | package installs | accept | No installs; `pyproject.toml` untouched |
</threat_model>

<verification>
```bash
.venv/bin/python -m pytest tests/test_phase22_dpsgd.py tests/test_phase22_checkpoint.py -q
.venv/bin/python -m pytest -q                       # allow 400s+
.venv/bin/ruff check . && .venv/bin/ruff format --check .
git diff --exit-code -- src/personacore/lora/ pyproject.toml scripts/mitigation_accountant.py
shasum -a 256 src/personacore/training/loop.py
```
</verification>

<success_criteria>
- Resuming a DP-written checkpoint with `dp_fn=None` raises `ValueError` naming `dp_noise_rng`
- Resuming a slot-less checkpoint WITH a seam still succeeds, and both pre-existing controls pass unmodified
- Resuming an ordinary non-DP checkpoint with `dp_fn=None` still succeeds
- The refusal was watched reddening with its raise deleted; sha256-identical restore
- Full suite ≥ 1281 passed, 0 failed; ruff clean; `src/personacore/lora/` and `pyproject.toml` byte-unchanged
</success_criteria>

<output>
Create `.planning/phases/22-dp-sgd-core-accountant-and-the-correctness-battery/22-13-SUMMARY.md` when done.
</output>
