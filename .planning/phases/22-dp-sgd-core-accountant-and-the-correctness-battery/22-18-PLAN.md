---
phase: 22-dp-sgd-core-accountant-and-the-correctness-battery
plan: 18
type: execute
wave: 12
gap_closure: true
depends_on: ["22-17"]
files_modified:
  - src/personacore/privacy/accountant.py
  - tests/fixtures/phase22_reference.py
  - tests/test_phase22_accountant.py
  - tests/test_phase22_reference.py
autonomous: true
requirements: [DPSGD-03]
validates: [V-01, V-02, V-07, V-26]
closes_gaps:
  - "A DELTA_FRONTIER row whose `b` is SUBNORMAL, so `test_two_oracles_agree` sweeps the band instead of stepping over it"
  - "Retarget `_inert_points()` from `math.erfc(b) > 0.0` to the smallest-normal threshold — the current filter calls the defective band 'healthy'"
  - "At least one `_round_trip_pairs()` sigma inside [0.4135, 0.4185] at T=200, so ROUND_TRIP_REL_TOL is a bound over a band that includes its own worst case"

must_haves:
  truths:
    - "A fourteenth `DELTA_FRONTIER` row lives INSIDE the erfc-subnormal band — `b = 27.151124073213406`, `erfc(b) = 1.43e-322` — so V-01 and V-02 sweep the band where 0 of 22 pinned points previously entered it"
    - "`_inert_points()` no longer certifies the defective band as healthy: its filter is the smallest-normal threshold, so the new row is EXCLUDED from the inertness sweep for the RIGHT reason instead of included for the wrong one"
    - "`_round_trip_pairs()` includes sigma=0.414, whose T=200 leg deviated by 2.0703e-05 — 2.07e+07x `ROUND_TRIP_REL_TOL` — before 22-17 and by 2.682e-16 after, so the documented budget is now a bound over a band containing its own worst case"
    - "ALL FOUR count meta-guards move together, verified against the code rather than against a brief: `len(DELTA_FRONTIER)`, `len(rows)` in V-01's non-empty companion, `len(pairs)` and the distinct-sigma count in V-07's. A missed one leaves the new row or the new sigma silently uncounted"
    - "Three stale accuracy bounds inside `accountant.py` that the new row and the new sigma falsify are RE-MEASURED rather than left standing — the failure mode 22-12 found twice"
    - "The FROZEN pre-registration is untouched and all 7 `GOLDEN_EPSILON` epsilons stay bit-identical: this plan adds test data and changes no arithmetic"
  artifacts:
    - path: "tests/fixtures/phase22_reference.py"
      provides: "the fourteenth DELTA_FRONTIER row, at the worst reachable point on this project's own frontier"
      contains: "728.2043182233367"
    - path: "tests/test_phase22_accountant.py"
      provides: "an `_inert_points` filtered on the smallest normal, a round-trip sigma inside the band, and three updated count meta-guards"
      contains: "float_info.min"
    - path: "tests/test_phase22_reference.py"
      provides: "the DELTA_FRONTIER count meta-guard, moved with the table"
  key_links:
    - from: "tests/test_phase22_accountant.py::test_two_oracles_agree"
      to: "the erfc-subnormal band"
      via: "the fourteenth DELTA_FRONTIER row's parametrization"
      pattern: "728\\.2043182233367"
    - from: "tests/test_phase22_accountant.py::_inert_points"
      to: "sys.float_info.min"
      via: "a filter that stops classifying a lossy erfc as healthy"
      pattern: "float_info\\.min"
---

<objective>
`22-VERIFICATION.md` proved the suite's blindness by EXECUTION, not by inference: patching
`_log_erfc` to return `-12345.0` for every input whose `erfc` is subnormal left the full suite at
`1314 passed, 1 skipped`, byte-identical to baseline. Zero of the twenty-two pinned points has a
subnormal `erfc(b)`; `_round_trip_pairs()`'s smallest sigma is 0.5; and `_inert_points()` filters on
`erfc(b) > 0.0`, so **the filter classifies the defective band as healthy — it encodes the defect.**

Purpose: 22-17 fixes the arithmetic. This plan makes the suite able to SEE the band, so the fix is
watched by data rather than by the plan that shipped it. The transferable finding, now recorded
twice, is that *an oracle cross-check is worth exactly the band its parametrization sweeps* —
`test_two_oracles_agree` was always honest, with an honest relative budget and a real non-vacuity
guard, and structurally incapable of firing where the defect was.

Output: a frontier row in the band, a filter that stops certifying it, a round-trip sigma that
reaches its own worst case, four count meta-guards moved together, and three stale accuracy bounds
re-measured.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-dp-sgd-core-accountant-and-the-correctness-battery/22-VERIFICATION.md
@.planning/phases/22-dp-sgd-core-accountant-and-the-correctness-battery/22-17-SUMMARY.md
@.planning/phases/22-dp-sgd-core-accountant-and-the-correctness-battery/22-12-SUMMARY.md
</context>

<environment>
**Python 3.11 venv is MANDATORY.** The dev box runs 3.14 and a pyenv 3.12.13 shadows it.

- `make test` is BROKEN. Use `.venv/bin/python -m pytest -q` and
  `.venv/bin/ruff check . && .venv/bin/ruff format --check .`.
- The full suite takes **~222 s**; allow a **400 s+** timeout.
- Baseline at this plan's start is 22-17's closing count, NOT 1314 — read it from
  `22-17-SUMMARY.md` and account for this plan's delta against THAT number.
- `ruff` `line-length = 100`.
- **`scripts/mitigation_accountant.py` is FROZEN and MUST NOT be edited.**
- **mpmath may ONLY be a one-off `.venv/bin/python -c` invocation whose OUTPUT is committed** as a
  decimal string, with the invocation and the `mp.mpf` form recorded beside the constant.
  `test_no_phase22_test_imports_mpmath` enforces the no-import rule by AST (RPT-03).
- `tests/fixtures/phase22_reference.py` has **ZERO imports and ZERO executable logic**, both
  AST-asserted by `test_reference_fixture_imports_nothing`. It is literal data.
- **`sys.float_info` in the TEST module is sanctioned and has a precedent in this very file:**
  `tests/test_phase22_accountant.py:657` uses `sys.float_info.max`, under a docstring at `:646-649`
  arguing that it *"is available HERE and deliberately not in the module"*. The import ceiling binds
  `accountant.py`, not the tests. (Note: this plan's dispatch brief claimed the tree has zero
  `float_info` uses — measured, that is false and this is the counter-example.)
- **Commit-message hazard:** use `git commit -F -` with a QUOTED heredoc; backticks in a `-m` string
  are executed by zsh.
- **`gsd-sdk` mutation handlers corrupt planning frontmatter.** `--flag` form only; write
  `22-18-SUMMARY.md` BEFORE `roadmap.update-plan-progress`; then `git diff .planning/` and repair.
</environment>

<tasks>

<task type="auto">
  <name>Task 1: a fourteenth frontier row INSIDE the subnormal band, with both count meta-guards moved</name>
  <files>tests/fixtures/phase22_reference.py, tests/test_phase22_reference.py, tests/test_phase22_accountant.py</files>
  <read_first>
    - `tests/fixtures/phase22_reference.py:23-98` — `DELTA_FRONTIER`, the decimal-string rationale,
      and the thirteenth row's THREE-ROUTE provenance block, which is the exact format this row's
      provenance must follow
    - `tests/test_phase22_reference.py:46-114` — `test_reference_table_is_populated`: the
      `len(DELTA_FRONTIER)` guard, the per-row type assertions and the hard-equality underflow
      assertion against `VACUOUS_AGREEMENT_ROW`
    - `tests/test_phase22_accountant.py:64-138` — `_sig_digits`, `_frontier_rel_tol`,
      `_representable_rows`, `test_closed_form_frontier` and its
      `test_closed_form_frontier_parametrization_is_not_empty` companion with the `len(rows)` guard
    - `tests/test_phase22_accountant.py:361-393` — `test_two_oracles_agree` and its UNWIDENED 1e-9
      budget, plus the docstring paragraph recording where the current worst gap sits
  </read_first>
  <action>
**Step 1 — derive the row, do not transcribe it.**

The row is `(eps, mu, truth)` where `eps = 728.2043182233367` and `mu = math.sqrt(200) / 0.414`.
Both are deliberate and both follow the thirteenth row's precedent:

- `eps` is the epsilon the SHIPPED (pre-22-17) accountant returned at sigma=0.414 / T=200 against
  the frozen delta. Using the shipped value rather than the post-fix one is what makes the row a
  STABLE literal: a row keyed on the fixed accountant's own output would have to be re-derived
  every time the accountant changes, which is the photograph-of-the-code failure `D-13` exists to
  prevent. The thirteenth row uses `775.7866600701457` for exactly this reason.
- sigma=0.414 is the WORST point in the reachable band — measured two-oracle gap **1.9190e-03**
  before 22-17, at this project's own frozen delta, 0.03 in sigma from the point that produced the
  original failure.

Compute `mu` as `math.sqrt(200) / 0.414` and commit its `repr`. Planning measurement:
**34.159747883408095**. Verify it; a transcribed float that is one ulp off makes the committed
truth a truth about a different input.

Then verify the row is the row it claims to be, and put all four figures in the SUMMARY:
`a = 2.9965347012327306` with `erfc(a) = 2.257809999067321e-05` — **healthy, which is why
`delta_closed` does not refuse here and returns a plausible wrong number instead** — and
`b = 27.151124073213406` with `erfc(b) = 1.43e-322`, which is strictly between `0.0` and float64's
smallest normal, i.e. genuinely SUBNORMAL. If `erfc(b)` is not subnormal on your box the row is in
the wrong band and the whole task is void; assert it rather than assuming it.

**Step 2 — the 60-dps truth, as a ONE-OFF invocation whose output is committed.**

Use the thirteenth row's provenance block as the template, including its three-route structure.
Route 1 is `mpmath` at `mp.dps = 60`; route 2 is `delta_quadrature` at the same inputs — DIFFERENT
mathematics, sharing no transcendental with the closed form beyond `exp`, and it is route 2 that
LICENSES the literal; route 3 is the fixed `delta_closed`. Record all three, their pairwise relative
gaps, and the fact that inputs enter as `mp.mpf(<python float>)` rather than `mp.mpf("<string>")`.

Planning measurements, to confirm rather than copy:

| route | value | relative to route 1 |
|---|---|---|
| 1. mpmath, 60 dps | `0.000009980810076964806559419972` | — |
| 2. `delta_quadrature` | `9.980810076863458e-06` | 1.0154e-11 |
| 3. fixed `delta_closed` (post-22-17) | `9.980810076964634e-06` | 1.7281e-14 |
| — shipped `delta_closed` (pre-22-17) | `1.0000000000000345e-05` | **1.9227e-03** |

Commit the truth at **13 significant digits** — `9.980810076965e-6` — matching the sibling rows'
width. That width is load-bearing: `_frontier_rel_tol` reads it off the string, and 13 digits gives
`1e-12 + 5e-13 = 1.5e-12`, which sits inside `test_closed_form_frontier`'s own
`1e-12 < tol <= 1e-10` sanity band. Confirm both.

**Step 3 — confirm the two legs are SATISFIABLE before committing the row.**

Measured after 22-17: V-01 (`delta_closed` against the committed 13-digit string) lands at
**3.666e-14** against the 1.5e-12 tolerance — **40.9x** inside. V-02 (the two-oracle gap) lands at
**1.0137e-11** against the UNWIDENED 1e-9 budget — **98.6x** inside. **The 1e-9 budget must NOT be
widened.** If the row does not fit inside it, the fix is wrong, not the tolerance — that is the line
round 1 held and it holds here.

Also confirm the row does not disturb `test_reference_table_is_populated`'s hard-equality underflow
assertion: `float("9.980810076965e-6")` is strictly positive, so `VACUOUS_AGREEMENT_ROW` remains the
only underflowing row.

**Step 4 — move BOTH count meta-guards, and read their current literals from the CODE.**

Adding a row to `DELTA_FRONTIER` silently uncounts itself unless two separate guards move. Locate
them by reading the files, not by trusting these line numbers:

- `tests/test_phase22_reference.py` — `test_reference_table_is_populated`'s `len(DELTA_FRONTIER)`
  assertion (currently `== 13`, and its docstring still says "12 delta rows", which was already
  stale before this plan — correct it too).
- `tests/test_phase22_accountant.py` — `test_closed_form_frontier_parametrization_is_not_empty`'s
  `len(rows)` assertion over `_representable_rows()` (currently `== 12`).

Measured after adding the row: 14 and 13 respectively. Update each assertion's MESSAGE as well as
its number, so a future failure still names which row the sweep exists to carry.

**Step 5 — write the row's own comment block explaining why it exists**, in the thirteenth row's
voice: F1's finding was that past z ~ 38.47 the FIRST erfc underflows and both oracles return 0.0;
the thirteenth row is the twin one cliff earlier, where the SECOND erfc reaches exactly 0.0; **this
row is the twin one cliff earlier still — where the second erfc is a SUBNORMAL and has silently lost
up to 52 of its 53 mantissa bits while remaining strictly positive.** Record that the shipped code
returned `1.0000000000000345e-05` here without refusing, that this is ~2.7 correct significant
digits, and — the part that distinguishes it from the thirteenth row — that the direction is
**NOT conservative**: `22-VERIFICATION.md` measured the shipped epsilon BELOW the 60-dps truth at
sigma = 0.4150, 0.4165, 0.4170 and 0.4175, the privacy-UNDERSTATING direction.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase22_reference.py tests/test_phase22_accountant.py -q</automated>
  </verify>
  <done>
`DELTA_FRONTIER` carries a fourteenth row at `(728.2043182233367, 34.159747883408095,
"9.980810076965e-6")` with a three-route provenance block; `erfc(b)` at that row is asserted
subnormal; V-01 and V-02 both pass against tolerances that were NOT widened; both count meta-guards
read 14 and 13 with updated messages; `test_reference_table_is_populated`'s underflow hard-equality
still names exactly `VACUOUS_AGREEMENT_ROW`.
  </done>
</task>

<task type="auto">
  <name>Task 2: retarget the inertness filter, put a round-trip sigma in the band, and move the other two count guards</name>
  <files>tests/test_phase22_accountant.py</files>
  <read_first>
    - `tests/test_phase22_accountant.py:185-278` — `_pinned_points`, `_inert_points` and its
      `erfc(b) > 0.0` filter, `test_log_erfc_inert_points_are_not_empty` with its `len(points) == 18`
      and its HARD-EQUALITY exclusion list, and `test_log_erfc_is_inert_where_erfc_is_healthy`
    - `tests/test_phase22_accountant.py:626-675` — `test_epsilon_for_answers_inf_in_the_subnormal_sigma_band`:
      the `sys.float_info` precedent, and its "a hardcoded sigma list is unsatisfiable across these
      four step counts" finding, which this task must not repeat
    - `tests/test_phase22_accountant.py:869-914` — `_round_trip_pairs`, `test_round_trip` and
      `test_round_trip_pairs_is_not_empty` with its `len(pairs) == 48` and distinct-sigma `== 12`
    - `src/personacore/privacy/accountant.py:116-125` — `ROUND_TRIP_REL_TOL` and the comment
      claiming "8.29e-15 worst relative deviation over 48 (sigma, T) pairs"
  </read_first>
  <action>
**Step 1 — retarget `_inert_points()`.**

Its filter is `math.erfc(_erfc_b(...)) > 0.0`, which is TRUE throughout the defective band, so it
**classifies that band as healthy — the filter encodes the defect it should exclude**
(`22-VERIFICATION.md` records this as Blocker severity). Change the threshold to float64's smallest
normal, using `sys.float_info.min` — sanctioned in this module by the precedent at `:646-649`, and
`sys` is already imported here.

Update the docstring: the filter's meaning is no longer "erfc has not underflowed to zero" but
"erfc still carries its full mantissa", which is the property that makes `math.log(math.erfc(b))`
the right answer for a point. Say that, because it is the sentence that stops someone widening it
back.

**Step 2 — move the count guard, and note the coupling that makes it self-enforcing.**

`test_log_erfc_inert_points_are_not_empty` asserts `len(points) == 18` and asserts by HARD EQUALITY
which points are excluded. Measured with Task 1's row present:

- under the OLD filter the count becomes **19** — the new row's subnormal `erfc(b)` is `> 0.0`, so
  the filter sweeps it into the "healthy" set and the guard REDDENS. That coupling is worth
  recording: the count guard cannot silently tolerate an unretargeted filter once the row exists.
- under the NEW filter the count is **18 again** — unchanged, because the 11 healthy frontier rows
  and the 7 `GOLDEN_EPSILON` rows all sit far below the threshold.

So the number does not move; the **exclusion list does**, from one entry to two. Update the hard
equality to name BOTH excluded rows and update the message to say what a third entry would mean: a
pinned answer has started being produced by the asymptotic series, and `GOLDEN_EPSILON` is FROZEN,
so such a move is unrecoverable. Verify all of this by running it — do not take the 18/19 split on
this plan's word.

**Step 3 — put a round-trip sigma inside the band, and DERIVE its scope rather than over-claiming.**

Add `0.414` to `_round_trip_pairs()`'s extra-sigma list. The list crosses with `(1, 64, 200, 1000)`
automatically, so this adds four pairs.

**The band is a function of T and the assertion must not pretend otherwise.** This is the same trap
22-15 documented: it shipped after finding that a hardcoded sigma list was unsatisfiable across four
step counts because the boundary moves with T. Here, measured BEFORE 22-17:

| sigma | T=1 | T=64 | T=200 | T=1000 |
|---|---|---|---|---|
| 0.414 | 2.68e-16 | 5.36e-16 | **2.0703e-05** | 5.36e-16 |

Only the T=200 leg was ever in the band — at T=1, 64 and 1000 the solution's `b` never reaches the
subnormal region. Measured AFTER 22-17, all four legs land at or below **5.4e-16**, comfortably
inside `ROUND_TRIP_REL_TOL` = 1e-12. So: the sigma is added unconditionally, `test_round_trip` is
satisfiable at all four step counts, and the SUMMARY must state that the mutation in Task 3 reddens
the T=200 node id specifically rather than all four. **Do not write an assertion or a claim that
requires all four legs to be in the band.**

**Step 4 — move the OTHER TWO count meta-guards, which the brief for this plan did not name.**

`test_round_trip_pairs_is_not_empty` carries two: `len(pairs) == 48` and
`len({sigma for sigma, _ in pairs}) == 12`. With a thirteenth sigma these become **52** and **13**.
Read both current literals from the code and confirm the new values by running the locator. A missed
count guard makes the new sigma silently uncounted, which is precisely the failure this whole plan
exists to close.

That is four count meta-guards across this plan: two in Task 1, two here. Enumerate all four in the
SUMMARY with their before and after values.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase22_accountant.py -q</automated>
  </verify>
  <done>
`_inert_points()` filters on `sys.float_info.min` with a docstring stating the new meaning; the
inertness count guard reads 18 with a TWO-entry hard-equality exclusion list; `0.414` is in
`_round_trip_pairs()`; `len(pairs)` and the distinct-sigma count read 52 and 13; all four legs of
the new sigma pass `ROUND_TRIP_REL_TOL`; the T-dependence of the band is recorded rather than
over-claimed.
  </done>
</task>

<task type="auto">
  <name>Task 3: re-measure the three bounds this plan falsifies, watch the filter mutation, close the suite</name>
  <files>src/personacore/privacy/accountant.py, tests/fixtures/phase22_reference.py</files>
  <read_first>
    - `src/personacore/privacy/accountant.py:116-125` — `ROUND_TRIP_REL_TOL`'s "48 (sigma, T) pairs"
      comment
    - `src/personacore/privacy/accountant.py:244-260` — `delta_closed`'s `Returns:` accuracy block,
      scoped to "the TWELVE float64-representable DELTA_FRONTIER rows"
    - `src/personacore/privacy/accountant.py:925-929` — `sigma_for`'s `Returns:` "48 (sigma, T)
      pairs is 8.29e-15"
    - `tests/fixtures/phase22_reference.py:285-296` — `WORST_RELATIVE_ERROR`, scoped to "the whole
      13-row frontier"
    - `.planning/phases/22-dp-sgd-core-accountant-and-the-correctness-battery/22-12-SUMMARY.md:267-276`
      — "Two stale bounds the new row falsified", the precedent for this task
  </read_first>
  <action>
**Step 1 — re-measure four documented bounds that this plan's own additions falsify.**

22-12 found exactly this failure twice and named the reason: a bound consumed by no test rots
silently. Each of these is now scoped to a sweep that has grown:

(a) `ROUND_TRIP_REL_TOL`'s comment: "8.29e-15 worst relative deviation over 48 (sigma, T) pairs —
the seven GOLDEN_EPSILON sigmas plus 0.5, 0.7, 1.5, 3.0 and 50.0". There are now 52 pairs and 13
sigmas. Re-measure the worst over all 52 and update both the figure and the enumeration. **Do not
tighten the tolerance to the measurement** — the comment already says so in its own words ("the
number above is one machine's 48 points, not a bound") and that reasoning is unchanged.

(b) `sigma_for`'s `Returns:` repeats "48 (sigma, T) pairs is 8.29e-15". Same correction.

(c) `delta_closed`'s `Returns:` block is scoped to "the TWELVE float64-representable DELTA_FRONTIER
rows" and names its two binding cases. There are now thirteen. Re-measure the largest deviation and
the largest on a 13-digit row, and record whether the new row moves either — the planning
measurement says it lands at 3.666e-14 against its committed string and is therefore NOT binding, so
the sweep gets wider without the bound getting worse, which is the same shape 22-12 recorded. Confirm
it rather than transcribing it.

(d) `tests/fixtures/phase22_reference.py::WORST_RELATIVE_ERROR` = 1.2e-11, commented as "across the
whole 13-row frontier". Re-measure over fourteen. The planning measurement puts the quadrature's
error at the new row at **1.0154e-11**, below the current holder (1.107e-11 at the thirteenth row),
so the CONSTANT should not need to move — but its comment's row count must, and the constant is
consumed by no test, which is exactly why 22-12 caught it rotting. Report your measured value.

**Step 2 — M-J: revert `_inert_points()`'s filter to `math.erfc(...) > 0.0`.**

This is a TEST-side mutation and must be labelled as one: it watches the FILTER, not the module.
Apply it, run `tests/test_phase22_accountant.py`, capture the verbatim message, restore, and verify
by `sha256` plus `git diff --exit-code`. Expected: `test_log_erfc_inert_points_are_not_empty`
reddens on the count (19 against 18), because the new frontier row's subnormal `erfc(b)` is `> 0.0`
and the old filter sweeps it in. That RED is the evidence that the retarget is load-bearing rather
than cosmetic.

**Step 3 — M-H re-applied, for the DELTA this plan contributes.**

22-17 already watched M-H (revert `_log_erfc`'s predicate to `if e > 0.0`) and recorded its RED set.
Re-apply it here and record only the ADDITIONAL distinct tests and node ids this plan's data
contributes. Expected from the planning measurement, to confirm: the new frontier row's
`test_closed_form_frontier` leg (1.9227e-03 against 1.5e-12), its `test_two_oracles_agree` leg
(1.9190e-03 against 1e-9), and `test_round_trip`'s `0.414-200` node id (2.0703e-05 against 1e-12 —
**2.07e+07x** the budget). Capture each message verbatim.

**Verify the hunk count before claiming a clean watch.** Round 1 found twice that a plan-specified
mutation was one hunk where the fix shipped as two layers, and applying one left the suite green.
State explicitly how many hunks M-H and M-J each required here.

**Step 4 — close the plan.** Full suite `.venv/bin/python -m pytest -q` (~222 s, allow 400 s+).
Account for the node-count delta against 22-17's closing baseline EXACTLY: +2 for the new frontier
row's V-01 and V-02 legs, +4 for the new round-trip sigma's four step counts, and nothing else. An
unexplained delta is a finding.

Then `.venv/bin/ruff check . && .venv/bin/ruff format --check .`, and assert with their outputs in
the SUMMARY: `git diff --exit-code -- scripts/mitigation_accountant.py` exits 0;
`git diff --exit-code -- pyproject.toml requirements.txt` exits 0; and the 7 `GOLDEN_EPSILON`
epsilons are `float.hex()`-identical to 22-17's closing capture — this plan changes no arithmetic,
so any movement at all is a defect rather than a tolerance question.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q</automated>
  </verify>
  <done>
Four stale bounds re-measured with their sweep sizes corrected; M-J watched reddening the inertness
count guard and labelled as a test-side mutation; M-H re-applied with only this plan's ADDITIONAL
REDs recorded and each message captured verbatim; both mutations' hunk counts stated; full suite
green with a +6 delta accounted for exactly; ruff clean; frozen pin and `GOLDEN_EPSILON` unmoved.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a test's locator filter → the claim "this point is healthy" | `_inert_points` decides which pinned points the inertness guard covers. A filter whose predicate matches the defect certifies the defective band as safe. |
| a fixture table → the band a cross-check sweeps | `test_two_oracles_agree` is worth exactly the band `DELTA_FRONTIER` reaches. Rows are the guard's entire reach. |
| a documented accuracy bound → a reader's expectation | A bound scoped to a fixture set that has since grown is a false statement about the current code, and nothing runs it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-45 | Spoofing | `_inert_points`' `erfc(b) > 0.0` filter certifying the defective band as healthy | mitigate | Threshold retargeted to `sys.float_info.min`; watched reddening under M-J (count 19 against 18) with the new row present |
| T-22-46 | Information Disclosure | a cross-check that cannot reach the band it is supposed to prove | mitigate | Fourteenth `DELTA_FRONTIER` row at `b = 27.151124073213406`, `erfc(b)` asserted SUBNORMAL at run time; V-01 and V-02 legs both inside UNWIDENED tolerances |
| T-22-47 | Repudiation | a new row or sigma landing silently uncounted | mitigate | All FOUR count meta-guards moved together and enumerated in the SUMMARY with before/after values, each literal read from the code |
| T-22-48 | Tampering | `ROUND_TRIP_REL_TOL` documented as a bound over a sweep that avoids its own worst case | mitigate | sigma=0.414 added to `_round_trip_pairs()`; the T-dependence of the band stated rather than over-claimed; the comment's pair count and worst figure re-measured over 52 |
| T-22-49 | Repudiation | stale accuracy bounds surviving a widened sweep because no test reads them | mitigate | Four documented bounds re-measured with their scopes corrected, following 22-12's precedent for the same failure |
| T-22-50 | Tampering | the 1e-9 two-oracle budget widened to admit the new row | mitigate | Budget asserted UNCHANGED; the row measured 98.6x inside it; the plan states that a row which does not fit means the fix is wrong |
| T-22-SC | Tampering | npm/pip/cargo installs | accept | No installs. `git diff --exit-code -- pyproject.toml requirements.txt` exits 0. mpmath enters only as a one-off shell invocation whose output is committed as data. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q` — 22-17's closing count **+6** (2 frontier legs + 4 round-trip
  legs), 1 skipped, delta accounted for exactly
- `.venv/bin/ruff check . && .venv/bin/ruff format --check .` — clean
- `git diff --exit-code -- scripts/mitigation_accountant.py` — exit 0
- `git diff --exit-code -- pyproject.toml requirements.txt` — exit 0
- 7 `GOLDEN_EPSILON` epsilons `float.hex()`-identical to 22-17's closing capture
- M-J and M-H both watched RED with verbatim messages, hunk counts stated, byte-identical restores
</verification>

<success_criteria>
- A fourteenth `DELTA_FRONTIER` row sits inside the erfc-SUBNORMAL band with a three-route provenance
- `_inert_points()` no longer classifies that band as healthy
- `_round_trip_pairs()` reaches a sigma whose pre-fix T=200 deviation was 2.07e+07x the budget
- All four count meta-guards moved, each literal read from the code
- Four stale accuracy bounds re-measured against their widened sweeps
- No tolerance widened anywhere; the 1e-9 two-oracle budget explicitly unchanged
</success_criteria>

<output>
Create `.planning/phases/22-dp-sgd-core-accountant-and-the-correctness-battery/22-18-SUMMARY.md`
when done.
</output>
