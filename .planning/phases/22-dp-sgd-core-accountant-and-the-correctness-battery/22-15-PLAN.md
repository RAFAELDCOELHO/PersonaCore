---
phase: 22-dp-sgd-core-accountant-and-the-correctness-battery
plan: 15
type: execute
wave: 9
gap_closure: true
depends_on: ["22-12", "22-14"]
files_modified:
  - src/personacore/privacy/accountant.py
  - tests/test_phase22_accountant.py
autonomous: true
requirements: [DPSGD-03]
validates: [V-08]
closes_gaps:
  - "A finiteness check on `mu` in `epsilon_for` after `mu = math.sqrt(steps) / sigma`, refusing rather than falling through to `_delta_or_below_float64`"

must_haves:
  truths:
    - "`epsilon_for` no longer reports epsilon = 0 — perfect privacy — for a near-zero sigma: measured, every sigma below `math.sqrt(steps) / sys.float_info.max` (7.866824069956795e-308 at T=200) overflowed the quotient to `inf` and returned `0.0`"
    - "The answer at an overflowed `mu` is `math.inf`, CONTINUOUS with the explicit `sigma == 0.0` branch, so the discontinuity the verifier measured (sigma=0 gives inf, the next representable float gives 0.0) is REMOVED rather than relocated"
    - "`_delta_or_below_float64`'s docstring premise — '`mu` is a finite strictly-positive number the caller computed' — is ESTABLISHED by a check rather than asserted: the helper refuses a non-finite or non-positive `mu` BEFORE the `try`, so its bare `except ValueError` can only mean what its contract says"
    - "Every point the function already answered is BIT-IDENTICAL: all 7 GOLDEN_EPSILON epsilons and the 48 round-trip pairs are unreachable from the changed branch"
    - "The guard was WATCHED failing: deleting the finiteness check restores the measured `0.0` and reddens the new test; the file restored byte-identically"
  artifacts:
    - path: "src/personacore/privacy/accountant.py"
      provides: "an `epsilon_for` whose subnormal-sigma band answers +inf, and a `_delta_or_below_float64` whose premise is checked"
      contains: "math.isfinite(mu)"
  key_links:
    - from: "src/personacore/privacy/accountant.py::epsilon_for"
      to: "the sigma == 0.0 branch's answer"
      via: "an overflowed mu returning the same math.inf"
      pattern: "isfinite\\(mu\\)"
---

<objective>
Close the third SC3 defect: `epsilon_for(5e-308, 200, 1e-5)` returns `0.0` — PERFECT PRIVACY for a
mechanism whose noise standard deviation is essentially zero.

Purpose: this is the privacy-UNDERSTATING direction, the one direction
`.planning/research/PITFALLS.md` calls unsound. `mu = math.sqrt(steps) / sigma` overflows to `inf`
with no finiteness check on the QUOTIENT; `_delta_or_below_float64`'s bare `except ValueError`
swallows `delta_closed`'s GARBAGE-INPUT refusal; and the caller reads `None` as "delta is below
float64's range, therefore below the target". The helper's own docstring proves this cannot happen,
from a premise — *"`mu` is a finite strictly-positive number the caller computed"* — that nothing
establishes, and the companion guard `test_delta_closed_still_ships_exactly_four_raises` protects the
raise COUNT the argument rests on while being structurally incapable of noticing the other premise
never held.

Output: the quotient checked, the answer made continuous with the σ=0 branch, and the helper's
swallow narrowed so it can only mean one thing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-dp-sgd-core-accountant-and-the-correctness-battery/22-VERIFICATION.md
@.planning/phases/22-dp-sgd-core-accountant-and-the-correctness-battery/22-12-SUMMARY.md
</context>

<environment>
**Python 3.11 venv is MANDATORY.** The dev box runs 3.14 and a pyenv 3.12.13 is first on PATH.

- `make test` is BROKEN. Use `.venv/bin/python -m pytest -q` and
  `.venv/bin/ruff check . && .venv/bin/ruff format --check .`
- The full suite takes **~220 s**; allow a **400 s+** timeout.
- `pyproject.toml` byte-unchanged (RPT-03). `accountant.py` imports `math` and NOTHING else —
  `test_accountant_imports_math_only` asserts hard equality statically and transitively, so do not
  reach for `sys.float_info` inside the module. Derive the boundary from `math` alone, or state it
  in prose and let the TEST compute it.
- **`scripts/mitigation_accountant.py` is FROZEN and MUST NOT be edited.**
- **`gsd-sdk` mutation handlers corrupt planning frontmatter.** Use the `--flag` form; write
  `22-15-SUMMARY.md` BEFORE `roadmap.update-plan-progress`; `git diff .planning/` and hand-repair.
</environment>

<tasks>

<task type="auto">
  <name>Task 1: check the quotient, answer +inf, and narrow the swallow to what its contract says</name>
  <files>src/personacore/privacy/accountant.py</files>
  <read_first>
    - `src/personacore/privacy/accountant.py:437-476` — `_delta_or_below_float64` in full: the
      contract, the four-raise unreachability argument, and the bare `except ValueError`
    - `src/personacore/privacy/accountant.py:538-611` — `epsilon_for`'s body: the input refusals,
      the explicit `sigma == 0.0` branch and its 60-dps justification comment, the `mu` quotient at
      `:571`, the eps=0 shortcut, the doubling walk and its cap refusal
    - `src/personacore/privacy/accountant.py:127-170` — `delta_closed`'s first two refusals, the
      ones `_delta_or_below_float64` argues are unreachable
    - the `_log_erfc` helper added by plan 22-12 — its `x <= 0.0` refusal is a `ValueError` raised
      inside `delta_closed`'s call tree and is therefore in scope for the swallow argument
  </read_first>
  <action>
**Step 1 — reproduce and bound the band before changing anything.**

Confirm `epsilon_for(5e-308, 200, 1e-5)` returns `0.0` and that `math.sqrt(200) / 5e-308` is `inf`.
Then locate the band's exact edge rather than sampling it: the quotient overflows exactly when
`sigma < math.sqrt(steps) / <largest finite float>`. Reference measurements taken while planning at
T=200: the boundary is **7.866824069956795e-308**; below it `epsilon_for` returns `0.0`; AT it and
just above it `mu` is finite but enormous and the EXISTING doubling-bracket refusal already fires
with *"the upper bracket failed to close in 200 doublings"*, which is a loud and correct failure.
(Note the verification's brief cites 4.450147717014403e-308 as the widest σ in the band; that is the
widest σ its sampling VISITED, not the boundary. Record the correction with your measurement.)

**Step 2 — check the quotient.**

Immediately after `mu = math.sqrt(steps) / sigma`, add a finiteness check on `mu`. Return
`math.inf` — do NOT raise.

The verification's `missing` item says "refusing"; this returns instead, and the deviation must be
recorded in the SUMMARY with its reason, which is the verifier's OWN artifact note: the defect it
names is a discontinuity — *"σ=0.0 returns `inf`, the next representable float returns `0.0`"* — and
a refusal would relocate that discontinuity rather than remove it. `math.inf` is also the
mathematically correct value by the identical argument the `sigma == 0.0` branch already carries
eighteen lines above: as `mu → inf` the closed form's first term goes to 1 and its second to 0, so
`delta → 1` for EVERY finite eps (measured at 60 dps in that branch's own comment: exactly 1.0 at
mu ≥ 100), no finite eps satisfies a target below 1, and the infimum over admissible eps is `+inf`.
A σ whose quotient overflows is a mechanism that releases a deterministic function of the data, and
`(inf, delta)`-DP is what that earns.

The comment must state the boundary in terms a reader can re-derive (`sqrt(steps)` over the largest
finite float64) with your measured value at T=200 beside it, and must say what happens immediately
above it — the bracket refusal — so nobody later "extends" this branch upward into a band that is
already handled loudly.

**Step 3 — establish the helper's premise instead of asserting it.**

In `_delta_or_below_float64`, add a refusal on a non-finite or non-positive `mu` alongside the
existing non-finite `eps` check, BEFORE the `try`. Its message must say the same thing the `eps`
one says, in the other variable: `delta_closed`'s `mu <= 0.0` refusal means "this input is garbage",
NOT "delta is below float64's range", and reading one as the other is exactly the conflation this
helper's contract forbids.

Then rewrite the docstring paragraph headed *"WHY THE CAUGHT ValueError CANNOT BE A DIFFERENT
REFUSAL"*. It currently argues from a premise nothing checked. It must now argue from the checks
above it, name what changed, and — because plan 22-12 added `_log_erfc` — extend the argument to
cover it: `_log_erfc` raises only when `math.erfc(x)` underflows at `x <= 0.0`, and `erfc(x) >= 1.0`
for every `x <= 0.0`, so that branch is unreachable from any call; `delta_closed` reaches it with
`b = (eps/mu + mu/2)/sqrt(2)` and `erfc` first underflows near x = 27.2, so the series branch implies
`b > 27.2 > 0`. State it as a reachability argument with its measurement, not as a promise.

Leave `test_delta_closed_still_ships_exactly_four_raises` alone: nothing in this task adds a `raise`
inside `delta_closed`'s FunctionDef. Run it explicitly to confirm.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase22_accountant.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -c` shows `epsilon_for(5e-308, 200, 1e-5)` returning `inf`
    - `.venv/bin/python -c` shows `epsilon_for(0.0, 200, 1e-5)` still returning `inf` — the two branches agree
    - `.venv/bin/python -m pytest tests/test_phase22_accountant.py -q` exits 0 with 0 failures
    - `.venv/bin/python -m pytest tests/test_phase22_accountant.py -q -k "golden or round_trip or four_raises or sigma_zero or domain_refusals"` exits 0
    - all 7 `GOLDEN_EPSILON` epsilons are BIT-IDENTICAL to their pre-change values
    - `grep -c "float_info" src/personacore/privacy/accountant.py` returns 0 — the module still imports `math` and nothing else
    - `git diff --exit-code -- pyproject.toml scripts/` exits 0
  </acceptance_criteria>
  <done>The quotient is checked, the subnormal-sigma band answers `+inf` continuously with the σ=0 branch, and the helper that reads a caught `ValueError` as an ordering fact now refuses the two inputs that would make that reading false.</done>
</task>

<task type="auto">
  <name>Task 2: pin the band and watch the guard fail</name>
  <files>tests/test_phase22_accountant.py, src/personacore/privacy/accountant.py</files>
  <read_first>
    - `tests/test_phase22_accountant.py:301-324` — `test_sigma_zero`, the existing σ=0 boundary test
      and the shape to mirror
    - `tests/test_phase22_accountant.py:382-432` — `test_epsilon_for_domain_refusals` and
      `test_epsilon_for_refusals_carry_distinct_messages`, whose case lists must stay coherent with
      any new behaviour
  </read_first>
  <action>
Add `test_epsilon_for_answers_inf_in_the_subnormal_sigma_band` to `tests/test_phase22_accountant.py`.

It must COMPUTE the boundary rather than spell it: `sys.float_info.max` is available to the test
(the import ceiling applies to `accountant.py`, not to the test module, which already imports `ast`,
`math`, `pathlib`, `random`, `subprocess` and `sys`). Derive `boundary = math.sqrt(steps) /
sys.float_info.max` and assert three things:

  1. **Inside the band**: for several sigmas strictly below the boundary — include `5e-324`, the
     smallest positive float64, and `5e-308` — `epsilon_for(sigma, steps, delta)` is `+inf`. Assert
     the meta-guard first, that `math.sqrt(steps) / sigma` really is non-finite for each, so the test
     cannot pass while testing something else.
  2. **Continuity at the boundary of the boundary**: `epsilon_for(0.0, steps, delta)` is also `+inf`,
     so the two branches give one answer and the measured discontinuity is gone.
  3. **The band's upper edge is somebody else's**: at a sigma just above the boundary the quotient is
     finite and the EXISTING doubling-bracket refusal fires — assert `pytest.raises(ValueError)` and
     match on the bracket message, so a future edit that swallows that refusal reddens here. Measure
     which sigma does it before writing the literal.

Parametrize over the same step counts `test_sigma_zero` uses, so the band is pinned at every T that
test already covers.

Add `test_delta_or_below_float64_refuses_the_inputs_it_may_not_read_as_ordering`: call the private
helper directly with a non-finite `mu` and with `mu <= 0.0` and assert `ValueError` in both cases,
with a docstring stating the conflation being prevented — `delta_closed`'s garbage-input refusals
mean "this input is wrong", not "delta is below float64's range", and the helper's whole contract is
that the caught exception can only mean the latter.

**Then watch it fail.** Record `sha256` of `src/personacore/privacy/accountant.py`. Two one-shot
mutations against the real committed file:
  - **M-E**: delete the `math.isfinite(mu)` check in `epsilon_for`. The new band test must redden,
    and the observed value must be `0.0` — capture that verbatim, because "returns 0.0 where it
    should return inf" is the entire defect and the RED is its proof.
  - **M-F**: delete the new `mu` refusal in `_delta_or_below_float64`. The direct helper test must
    redden. If it does not, the refusal is not where you think it is.

After each, restore and prove it: `sha256` equal to the pre-probe value and
`git diff --exit-code -- src/personacore/privacy/accountant.py` exits 0. Report how many DISTINCT
tests each mutation reddened.

Run the full suite and record the count; expect no regression against the wave-8 baseline. Allow a
400 s+ timeout.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest "tests/test_phase22_accountant.py::test_epsilon_for_answers_inf_in_the_subnormal_sigma_band" -q` exits 0
    - the band test derives its boundary from `sys.float_info.max`, not from a transcribed literal
    - M-E is recorded with its mutation, the pytest summary line, the verbatim `0.0` observation, and a sha256-identical restore
    - M-F is recorded in the same shape
    - `.venv/bin/python -m pytest -q` reports no regression against the wave-8 baseline, 0 failed
    - `.venv/bin/ruff check . && .venv/bin/ruff format --check .` exits 0
  </acceptance_criteria>
  <done>The subnormal band is pinned at every step count `test_sigma_zero` covers, its upper edge is pinned to the pre-existing bracket refusal, and both new guards were observed failing under the mutation each exists to catch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a caller-supplied sigma → a derived `mu` | Both operands are validated and the QUOTIENT is not. `sigma` finite and `> 0` plus `steps >= 1` does not imply `sqrt(steps)/sigma` is finite. |
| a caught exception → an ordering fact | `_delta_or_below_float64` converts a `ValueError` into "delta is below the target". Any refusal it can catch that does not mean that is a silently wrong published epsilon. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-31 | Information Disclosure | `epsilon_for` returning 0.0 for a near-zero sigma | mitigate | Finiteness check on the quotient returning `math.inf`; pinned across every step count; watched reddening under M-E with the `0.0` captured verbatim |
| T-22-32 | Spoofing | a garbage-input refusal read as "delta below float64 range" | mitigate | `_delta_or_below_float64` refuses non-finite / non-positive `mu` before the `try`; watched reddening under M-F |
| T-22-33 | Tampering | a new refusal in `delta_closed`'s call tree silently widening the swallow | mitigate | The docstring's reachability argument is extended to `_log_erfc` with its measurement; `test_delta_closed_still_ships_exactly_four_raises` run explicitly |
| T-22-34 | Tampering | an import added to reach `sys.float_info` inside the module | mitigate | The boundary is derived in the TEST; `grep -c float_info` on the module returns 0 and `test_accountant_imports_math_only` asserts hard equality |
| T-22-SC | Tampering | package installs | accept | No installs; `pyproject.toml` asserted byte-unchanged |
</threat_model>

<verification>
```bash
.venv/bin/python -m pytest tests/test_phase22_accountant.py -q
.venv/bin/python -m pytest -q                       # allow 400s+
.venv/bin/ruff check . && .venv/bin/ruff format --check .
git diff --exit-code -- pyproject.toml scripts/
shasum -a 256 src/personacore/privacy/accountant.py
```
</verification>

<success_criteria>
- `epsilon_for` answers `+inf` for every sigma whose `sqrt(steps)/sigma` overflows, at every step count `test_sigma_zero` covers
- `epsilon_for(0.0, T, delta)` and `epsilon_for(<subnormal>, T, delta)` give the same answer — the discontinuity is removed, not relocated
- `_delta_or_below_float64` refuses the two inputs its contract may not read as an ordering fact
- All 7 `GOLDEN_EPSILON` epsilons bit-identical; the 48 round-trip pairs unaffected
- M-E and M-F each watched reddening with sha256-identical restores
- Full suite green, ruff clean, module still imports `math` and nothing else
</success_criteria>

<output>
Create `.planning/phases/22-dp-sgd-core-accountant-and-the-correctness-battery/22-15-SUMMARY.md` when done.
</output>
