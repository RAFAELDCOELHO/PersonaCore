---
phase: 16-weight-vs-prompt-persistence-control
plan: 03
type: execute
wave: 2
depends_on: ["16-02"]
files_modified:
  - tests/test_phase14_scoring.py
autonomous: true
requirements: [PERS-06]

must_haves:
  truths:
    - "The persona= guard scans scripts/*.py and src/ in full, so a new Phase 17 file carrying persona= fails the suite instead of being silently unscanned"
    - "The guard still asserts HARD EQUALITY against an explicit (file, function) allowlist — widening the scan did not weaken the assertion"
    - "Every draw_all call site sits in a function that also asserts either assert_value_in_prompt or assert_no_value_in_prompt — no drawing path has a skip mode"
    - "Both widened guards have been observed RED against a deliberately added violation before landing"
  artifacts:
    - path: "tests/test_phase14_scoring.py"
      provides: "the widened D-21 structural guards"
      contains: "PERSONA_ALLOWLIST"
  key_links:
    - from: "tests/test_phase14_scoring.py"
      to: "scripts/*.py and src/**/*.py"
      via: "ast.parse over a rglob-ed file set"
      pattern: "rglob|glob"
    - from: "the persona= allowlist"
      to: "hard equality"
      via: "sorted(found) == sorted(PERSONA_ALLOWLIST)"
      pattern: "=="
---

<objective>
Widen the `persona=` AST guard in SCOPE — `scripts/*.py` and `src/` in full, with an explicit
`(file, function)` allowlist — while keeping its assertion at hard equality, and add its missing
sibling: every `draw_all` call site must assert something.

Purpose: today `tests/test_phase14_scoring.py::test_persona_argument_is_scoped_to_the_fairness_control`
parses exactly one file, hard-coded. A new Phase 17 file with `persona=` would simply not be
scanned and would pass in silence — the guard technically green and substantively blind. D-21 is
literal about this: widening is deliberate and visible; deletion is forbidden.

Output: two widened structural guards in `tests/test_phase14_scoring.py`, each observed RED before
landing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/16-weight-vs-prompt-persistence-control/16-CONTEXT.md
@.planning/phases/16-weight-vs-prompt-persistence-control/16-RESEARCH.md
@.planning/phases/16-weight-vs-prompt-persistence-control/16-02-SUMMARY.md

**The failure mode this plan must NOT commit** (RESEARCH Pitfall 3): widening the scan naively
forces relaxing hard equality into a membership check. That is the guard getting *weaker* while
looking bigger. If your diff replaces `==` with `in` or `issubset`, you have introduced the defect
this plan exists to prevent.

**Landing this guard BEFORE the Phase 16 driver files exist is deliberate.** Plans 16-04, 16-05 and
16-07 each add a `persona=` call site, and each must add its own allowlist entry in the same commit
as the call site. That is D-21's "sem exceção por conveniência de arquivo novo" made operational: a
new file does not get a pass for being new.

<interfaces>
Current guard — read it before widening:

`tests/test_phase14_scoring.py:405` _build_recall_prompt_call_sites()
  parses exactly ONE file: _REPO_ROOT / "scripts" / "phase14_recall.py"
  returns list of (enclosing_function_name, {kw.arg for kw in call.keywords})

`tests/test_phase14_scoring.py:425` test_persona_argument_is_scoped_to_the_fairness_control()
  :441  assert sites, "no build_recall_prompt call sites found — the AST walk stopped working"
  :444  assert with_persona == ["run_fairness_control"]        <- HARD EQUALITY. Keep it.
  :447  assert [name for name, kwargs in sites if kwargs and "persona" not in kwargs] == []
  :448  for expected in ("complete_question", "render_context_dump", "assert_no_value_in_prompt"):
            assert expected in {name for name, _ in sites}     <- the POSITIVE half. Keep it.

The current single legitimate persona= call site:
  scripts/phase14_recall.py :: run_fairness_control   (line 1187)

`draw_all` call sites today (both in scripts/phase14_recall.py):
  complete_question       (line 603)  — its enclosing SCORED path asserts via assert_no_value_in_prompt
                                        in run_scored_recall, NOT inside complete_question itself
  run_fairness_control    (line 1193) — asserts via assert_value_in_prompt after 16-02

`src/personacore/dialogue/serialize.py:92` build_recall_prompt(tok, question, persona=())
  — the argument defaults to () and any caller may pass it; nothing in the function enforces scope.

`tests/test_phase14_demo.py` already pins the matching property for the demo process
  (persona= absent from scripts/personalize_demo.py entirely). Do not duplicate that here.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen the persona= guard's SCAN to scripts/ and src/, keeping hard equality</name>
  <files>tests/test_phase14_scoring.py</files>
  <read_first>
    - `tests/test_phase14_scoring.py` lines 380-452 (`_build_recall_prompt_call_sites`, the guard, and the `_fake_question` helper that follows)
    - `scripts/phase14_recall.py` lines 1180-1195 (the one legitimate `persona=` call site, as edited by 16-02)
    - `src/personacore/dialogue/serialize.py` lines 92-113 (`build_recall_prompt` — confirm `persona` is the keyword name and that nothing enforces scope inside it)
    - `.planning/phases/16-weight-vs-prompt-persistence-control/16-RESEARCH.md` §Common Pitfalls → Pitfall 3
  </read_first>
  <action>
    Rewrite `_build_recall_prompt_call_sites()` to walk a FILE SET rather than one hard-coded path.

    File set: `sorted((_REPO_ROOT / "scripts").glob("*.py"))` plus
    `sorted((_REPO_ROOT / "src").rglob("*.py"))`. Return entries keyed by
    `(relative_posix_path, enclosing_function_name, frozenset_of_keyword_names)`. Keep using
    `ast.parse` + `ast.walk` — a substring check cannot tell a call from a docstring mention, and
    the docstrings in `phase14_recall.py` discuss `persona=` at length precisely because it is the
    dangerous argument.

    Two details the current implementation gets wrong once the scan widens:
    - It only matches `ast.FunctionDef`. Add `ast.AsyncFunctionDef`, and handle a call at module
      scope (outside any function) by recording the enclosing name as `"<module>"` — a module-level
      `persona=` is the most dangerous placement and must not be invisible.
    - It only matches `getattr(inner.func, "id", None) == "build_recall_prompt"`, which misses an
      attribute-form call such as `serialize.build_recall_prompt(...)`. Also match
      `getattr(inner.func, "attr", None) == "build_recall_prompt"`.

    Add a module-level `PERSONA_ALLOWLIST` constant: a tuple of `(file, function)` pairs, sorted,
    with a comment above each entry naming WHY that site is allowed to place a fact value in
    context. The only entry at this point is
    `("scripts/phase14_recall.py", "run_fairness_control")`.

    Rewrite `test_persona_argument_is_scoped_to_the_fairness_control` to:
    - keep `assert sites, "..."` (the walk-stopped-working tripwire), and additionally assert the
      scanned file count is `>= 2` so a broken glob cannot silently collapse the scan back to one file
    - assert HARD EQUALITY: `sorted(with_persona) == sorted(PERSONA_ALLOWLIST)` where `with_persona`
      is the list of `(file, function)` pairs whose call passes `persona`. Not `in`, not `issubset`,
      not `set(...) <= set(...)`
    - keep the "everything else is the bare form" assertion: any call site with keywords but without
      `persona` is an empty list
    - keep the POSITIVE half — `complete_question`, `render_context_dump` and
      `assert_no_value_in_prompt` are still present as call sites — extended to also require
      `build_recall_prompt`'s own definition file to be reachable. A guard that only forbids can be
      satisfied by deleting every call site.

    Update the test's docstring to state the widened scope, name D-21, and state in one sentence
    that a future phase adding a `persona=` call site must add its allowlist entry in the same
    commit — the guard has no convenience exemption for new files.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_phase14_scoring.py::test_persona_argument_is_scoped_to_the_fairness_control -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/test_phase14_scoring.py -q` exits 0
    - `grep -c "PERSONA_ALLOWLIST" tests/test_phase14_scoring.py` returns >= 2 (definition + use)
    - The guard's core assertion uses `==` against the allowlist. Verified by AST:
      `.venv/bin/python -c "import ast,pathlib; t=ast.parse(pathlib.Path('tests/test_phase14_scoring.py').read_text()); f=[n for n in ast.walk(t) if isinstance(n,ast.FunctionDef) and n.name=='test_persona_argument_is_scoped_to_the_fairness_control'][0]; cmps=[c for c in ast.walk(f) if isinstance(c,ast.Compare)]; assert any(isinstance(o,ast.Eq) for c in cmps for o in c.ops)"` exits 0
    - `grep -c "issubset\|<= set(" tests/test_phase14_scoring.py` returns 0
    - `_build_recall_prompt_call_sites` scans more than one file: the test asserts a scanned-file
      count `>= 2`, and that assertion is present in the source
    - The AST walk handles `ast.AsyncFunctionDef` and the `<module>` scope case — both names appear
      in `_build_recall_prompt_call_sites`
    - **Deliberate-RED, both directions, observed and recorded:**
      (a) add `persona=[]` to the `build_recall_prompt` call inside `complete_question`
      (`scripts/phase14_recall.py:602`), confirm the test FAILS, revert to a byte-identical file
      (`git diff scripts/phase14_recall.py` empty);
      (b) create a throwaway `scripts/_red_probe.py` containing a function that calls
      `build_recall_prompt(tok, q, persona=["x"])`, confirm the test FAILS naming that file, then
      delete it. (b) is the one that proves the WIDENING, not just the guard.
  </acceptance_criteria>
  <done>A `persona=` call site anywhere in `scripts/` or `src/` outside the named allowlist fails the suite immediately.</done>
</task>

<task type="auto">
  <name>Task 2: Every draw_all call site must assert something</name>
  <files>tests/test_phase14_scoring.py</files>
  <read_first>
    - `scripts/phase14_recall.py` lines 542-611 (`draw_all` and `complete_question` — note that `complete_question` itself does NOT assert; its caller `run_scored_recall` does)
    - `scripts/phase14_recall.py` lines 789-810 (`run_scored_recall`'s `assert_no_value_in_prompt` call)
    - `scripts/phase14_recall.py` lines 1180-1200 (`run_fairness_control`'s `assert_value_in_prompt` call, as edited by 16-02)
    - `tests/test_phase14_scoring.py` — the `_build_recall_prompt_call_sites` helper as rewritten in Task 1, which this task reuses
  </read_first>
  <action>
    Add `test_every_draw_all_call_site_asserts_something()` to `tests/test_phase14_scoring.py`.

    Build a second AST walker over the same file set as Task 1. For every `ast.Call` whose callee
    resolves to `draw_all`, record `(file, enclosing_function)`. For each such site, require that
    **either**:
    - the enclosing function itself contains a call to `assert_value_in_prompt` or
      `assert_no_value_in_prompt`, **or**
    - the site is listed in an explicit module-level `DRAW_ALL_ASSERTED_BY` mapping from
      `(file, function)` to the name of the caller function that performs the assertion on its
      behalf, AND that named caller is itself found in the scan and does contain one of the two
      assertions.

    The `DRAW_ALL_ASSERTED_BY` mapping exists for exactly one real case today —
    `("scripts/phase14_recall.py", "complete_question")` is asserted by `run_scored_recall` — and
    every future entry costs an explicit, reviewed line. Write that rationale as a comment above the
    mapping: an indirection is allowed only when it is *named*, because an unnamed indirection is
    indistinguishable from a missing assertion.

    Assert there is **no skip mode**: no `draw_all` site may sit inside a function whose name starts
    with `_skip` or that is decorated with anything, and the mapping must contain no entry whose
    named asserter is absent from the scan (a dangling entry is a silent exemption).

    Finish with `assert sites, "no draw_all call sites found — the AST walk stopped working"` and an
    assertion that the found site count is `>= 2` (today: `complete_question` and
    `run_fairness_control`).

    Docstring: state that this is PERS-06's positive half. `assert_no_value_in_prompt` alone proves
    values stay OUT of scored prompts; `assert_value_in_prompt` proves values are IN the prompts
    that are supposed to carry them. A path that draws without asserting either is a path where
    neither property is checked — and RESEARCH records that Phase 16's distance-~2 ladder rungs
    carry their value inside the `question` string, which makes them INVISIBLE to the `persona=`
    guard. This test is what covers them.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_phase14_scoring.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/test_phase14_scoring.py -q` exits 0
    - `grep -c "DRAW_ALL_ASSERTED_BY" tests/test_phase14_scoring.py` returns >= 2
    - The test asserts a found-site count `>= 2` and has a non-empty tripwire assertion
    - **Deliberate-RED, observed and recorded:** create a throwaway
      `scripts/_red_probe2.py` with a function that calls `draw_all(...)` and asserts nothing,
      confirm the test FAILS naming that file and function, then delete the file
    - **Second deliberate-RED:** add a dangling entry to `DRAW_ALL_ASSERTED_BY` pointing at a
      nonexistent asserter function, confirm the test FAILS, then remove it
    - `make test` green after both probes are removed, and `git status --porcelain scripts/` is empty
  </acceptance_criteria>
  <done>No path in `scripts/` or `src/` can draw completions without a named in-prompt assertion covering it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| structural guard → the code it claims to cover | A guard that scans less than it claims is green-but-blind; the invariant is false while the suite reports true |
| existing scanned files → new phase files | Phases 17 and 18 add files to `scripts/`. A guard keyed to one hard-coded path does not see them |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-09 | Spoofing | the `persona=` guard's coverage | mitigate | Task 1 widens the SCAN to `scripts/*.py` + `src/**/*.py`; the test asserts scanned-file count >= 2 so a broken glob cannot collapse it back to one file; observed RED against a new out-of-allowlist file, which is the proof the widening is real |
| T-16-10 | Elevation of privilege | the guard's assertion strength | mitigate | Task 1 keeps `==` against `PERSONA_ALLOWLIST`. An AST acceptance check asserts an `ast.Eq` is present and `grep` asserts `issubset`/`<= set(` are absent — the "scan more, assert less" regression is itself pinned |
| T-16-11 | Information disclosure | ladder rungs that carry the value inside `question` | mitigate | Task 2: the distance-~2 rungs are invisible to a `persona=`-keyed guard by construction (RESEARCH, Ladder wiring). The every-`draw_all`-asserts guard is what covers them, and it keys on the drawing call, not on the argument name |
| T-16-12 | Tampering | the assertion indirection | mitigate | Task 2's `DRAW_ALL_ASSERTED_BY` requires the named asserter to exist in the scan; a dangling entry fails, so an exemption cannot be created by pointing at nothing |
</threat_model>

<verification>
```
.venv/bin/pytest tests/test_phase14_scoring.py -q
make test
git status --porcelain          # no leftover _red_probe*.py
git diff --stat scripts/        # empty — the RED probes were reverted byte-identically
```
</verification>

<success_criteria>
- Both guards scan `scripts/*.py` and `src/**/*.py`
- Hard equality preserved; no `in`/`issubset` relaxation anywhere in the guard
- Four deliberate-RED observations recorded (two per task)
- No probe files left behind; `scripts/` byte-unchanged
- `make test` green
</success_criteria>

<output>
Create `.planning/phases/16-weight-vs-prompt-persistence-control/16-03-SUMMARY.md` when done.
Record all four RED outputs verbatim and state the current `PERSONA_ALLOWLIST` contents, so plans
16-04/16-05/16-07 know exactly which line they must extend.
</output>
