---
phase: 16-weight-vs-prompt-persistence-control
plan: 04
type: execute
wave: 3
depends_on: ["16-03"]
files_modified:
  - scripts/phase14_factset_gate.py
  - scripts/phase16_ladder.py
  - tests/test_phase16_ladder.py
autonomous: true
requirements: [PERS-01, STAT-01, STAT-02, STAT-05]

must_haves:
  truths:
    - "The capability ladder's per-cell pass threshold is a module-level integer literal committed before the run it judges"
    - "That literal is pinned to its derivation by a test, so it cannot silently drift from the committed Phase 14 floor it is anchored to"
    - "The cell statistic counts QUESTIONS (n_answerable), never draws"
    - "Every reported cell proportion carries a denominator and a bound; a zero cell reports rule_of_three and never a bare 0%"
    - "licensed_headline() is total over the rung lattice and its all-fail branch licenses only the SC1 capability-deficit statement"
    - "phase14_factset_gate.py exposes a public entry point taking an arbitrary string, so the guessability instrument is imported and never copied"
  artifacts:
    - path: "scripts/phase14_factset_gate.py"
      provides: "public guessability probe over an arbitrary string (D-16)"
      contains: "def probe_guessability"
    - path: "scripts/phase16_ladder.py"
      provides: "the PERS-01 pre-registration: constants, cell arithmetic, licensed_headline"
      contains: "LADDER_CELL_PASS_K"
      min_lines: 200
    - path: "tests/test_phase16_ladder.py"
      provides: "derivation pinning + totality proof"
      contains: "test_licensed_headline_is_total"
  key_links:
    - from: "scripts/phase16_ladder.py"
      to: "scripts/erasure_gate.py"
      via: "import wilson_upper_bound / rule_of_three"
      pattern: "from erasure_gate import"
    - from: "tests/test_phase16_ladder.py"
      to: "LADDER_CELL_PASS_K"
      via: "recompute the minimum k from the bound and assert equality with the literal"
      pattern: "LADDER_CELL_PASS_K"
---

<objective>
Commit the PERS-01 capability ladder's pre-registration: the threshold constants, the cell
arithmetic that reads them, and `licensed_headline()` — all before the run they judge (STAT-05).
Widen `scripts/phase14_factset_gate.py`'s public surface so the guessability instrument can vet an
arbitrary string (D-16), deliberately and visibly, in the same register as the PERS-06 guard
widening.

Purpose: the ladder is what licenses — or refuses to license — the phase's headline. Its thresholds
must be derived from material already published in this repository, pushed before any Phase 16
measurement exists, and computed by *importing* the constants rather than retyping them in prose.
The all-fail branch is a first-class expected outcome (RESEARCH Q2: Phase 14 measured 1 of 216
questions on this exact model with this exact prompt builder), not a broken instrument.

Output: `scripts/phase16_ladder.py` (constants + pure functions, nothing executes at import),
`tests/test_phase16_ladder.py`, and a widened `scripts/phase14_factset_gate.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-weight-vs-prompt-persistence-control/16-CONTEXT.md
@.planning/phases/16-weight-vs-prompt-persistence-control/16-RESEARCH.md
@.planning/phases/16-weight-vs-prompt-persistence-control/16-03-SUMMARY.md

## Three planner choices recorded here, deliberately, before the run

RESEARCH left three items open with a recommendation each. All three are settled below and must be
implemented exactly as stated; they are pre-registration, not preference.

**Choice 1 — cell size `n` (RESEARCH Assumptions Log A1).** `LADDER_CELL_QUESTIONS = 216`, the full
core set (112 `core_taught` + 104 `core_held_out` in `results/phase16_recall_sample.json`). Reason:
identical to the floor's `n` and to the top rung's `n`, which is what makes the comparison against
`1/216` apples-to-apples and what D-15's proxy-validity check requires. Cost is ~80 min of ladder
wall clock on top of the ~39 min four-arm run; D-05 records the run as not cost-constrained, and
this is the recorded choice rather than a discovered one.

**Choice 2 — multiplicity (RESEARCH Assumptions Log A4).** Price it into the per-cell `z`:
`LADDER_CELL_Z = 2.393979799818510`, the one-sided `1 - 0.05/6` quantile. Reason: a false pass
licenses the STRONGER headline, which is the over-licensing direction the whole milestone exists to
avoid; the cost is two extra questions. This is a CHOICE OF LITERAL, not a hypothesis test — no
p-value is computed here and no verdict is emitted, so D-09's Holm family stays closed at exactly
the 6 arm pairs and STAT-06 is untouched.

**Choice 3 — top rung threshold (RESEARCH Open Question 1).** The same literal applies to all 7
rungs. `k_min` is 10 at `n = 216` whether multiplicity is priced at 6 cells or 7 rungs
(`z = 2.393980` and `z = 2.449998` both yield 10), so this costs nothing and removes an ambiguity
that would otherwise be resolved after seeing a number.

## Anti-patterns this plan must not commit (RESEARCH §Anti-Patterns)

- Re-anchoring the threshold to the post-fix fairness-control re-run. The anchor is the COMMITTED
  `results/phase14_recall_report.md:378` number (1/1944 draws ≡ 1/216 questions). The re-run
  produces a *reported delta* (D-13), never a constant.
- Different `n` across cells.
- Fewer draws in ladder cells to save time — `n_answerable` at 4 draws is a different quantity from
  `n_answerable` at 9 and is not comparable to the floor.
- Copying `phase14_factset_gate` logic into the Phase 16 driver. D-16 forbids it: widen the public
  surface instead.

**`DEGEN-2` does not exist in this repository (D-10). Do not propagate it.**

<interfaces>
Import these; do not reimplement any of them.

From `scripts/erasure_gate.py` (PREREG-01, committed at 23a830c, stdlib only):
  wilson_upper_bound(successes, n, z=_Z_ONE_SIDED_95) -> float     # :139
  rule_of_three(n) -> float                                         # :161
  # lower-bound-by-complement idiom, established at erasure_is_worth_attempting:186 —
  #   lower = 1.0 - wilson_upper_bound(n - k, n, z)

From `scripts/phase14_factset.py` (already PUBLIC — only the guessability half needs widening):
  token_census(tok, value) -> tuple[int, bool]                      # :313
  normalize_for_match(text) -> str                                  # :322
  exact_match_clean(completions, value) -> bool                     # :334

From `scripts/phase14_factset_gate.py` (currently PRIVATE — this plan widens it):
  _complete(model, prompt_ids, device, forbid, **kw) -> list[int]   # :73
  _probe(model, tok, device, forbid, question, index) -> (prompt_ids, [text, ...])   # :87
  PROBE_SEEDS = 4, SEED = 1337, PROBE_MAX_NEW_TOKENS = 32, TEMPERATURE = 0.8, TOP_P = 0.95

  **CRITICAL:** this module does `import phase14_factset as fs` at MODULE level (line 44). Any
  module-level import of `phase14_factset_gate` therefore pulls the locked fact strings into the
  importing module's address space. The Phase 16 ladder driver MUST import it INSIDE functions.

From `scripts/phase14_recall.py`:
  N_SEEDED_SAMPLES = 8   # :152 — 1 greedy + 8 seeded = 9 draws
  run_fairness_control(...)["n_answerable"]  # :1219 — the STAT-01-legal question-unit numerator

Committed numbers this plan anchors to (do NOT re-measure):
  results/phase14_recall_report.md:378  — Phase 14 Control 1 scored 1/1944 draws
  1944 == 216 questions x 9 draws;  fixture core count 112 + 104 == 216
  wilson_upper_bound(1, 216) == 0.020481915502612365
  rule_of_three(216) == 0.013888...
  NormalDist().inv_cdf(1 - 0.05/6) == 2.393979799818510

From `tests/test_phase14_scoring.py`:
  embedded_fact_values(module, forbidden) -> list      # :345, shared with tests/test_phase14_demo.py
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-16 — widen phase14_factset_gate.py with a public guessability entry point</name>
  <files>scripts/phase14_factset_gate.py, tests/test_phase16_ladder.py</files>
  <read_first>
    - `scripts/phase14_factset_gate.py` lines 30-140 (module imports incl. the module-level `import phase14_factset as fs`, the constants block, `_complete`, `_probe`, `_quote`, `assert_report_not_clobbered`)
    - `scripts/phase14_factset_gate.py` lines 260-300 (the measured `forbid_ids` no-op that closes RESEARCH Pitfall 5)
    - `scripts/phase14_factset.py` lines 305-345 (`token_census`, `normalize_for_match`, `exact_match_clean` — already public)
    - `.planning/phases/16-weight-vs-prompt-persistence-control/16-CONTEXT.md` D-16
  </read_first>
  <action>
    Add a public function to `scripts/phase14_factset_gate.py`, placed immediately after `_probe`:

    `probe_guessability(model, tok, device, forbid, value, questions, *, start_index=0) -> dict`

    Behavior: for each `question` in `questions`, call the existing private `_probe` with index
    `start_index + i`, collect `(question, prompt_ids, completions)`. Compute
    `clean = exact_match_clean(all_completions_flattened, value)` using the already-public
    `phase14_factset.exact_match_clean`. Return a dict with keys `value`, `probes` (one entry per
    question carrying the question, the prompt ids, and the verbatim completions), `n_probes`,
    `n_completions`, and `clean`.

    It calls `_probe` — it does not duplicate the probe loop, the seeding, or the stop-id set. The
    whole point of the widening is that there is one probe implementation.

    Do NOT change `_probe`, `_complete`, `main()`, any constant, or the module's existing imports.
    This is an additive widening.

    Docstring must state: D-16 and the ISO-01 precedent (import this instrument, never copy it);
    that the guessability rule's objective half is `exact_match_clean` — ONE containment out of N is
    a FAIL, deliberately unforgiving; that the caller supplies the probe questions so this function
    holds no fact material of its own; and that Phase 17's ISO-01 consumes the same entry point,
    which is why the widening lands here rather than in a Phase 16 file.

    Create `tests/test_phase16_ladder.py` with its first test:
    `test_factset_gate_exposes_a_public_guessability_probe()` — asserts `probe_guessability` exists
    on the module, is public, its parameter list is exactly
    `(model, tok, device, forbid, value, questions)` plus keyword-only `start_index`, and that
    `value` is a plain parameter with no default. Load the module via `importlib` with `scripts/`
    on `sys.path` the way the existing test modules do; if importing it requires torch and a
    checkpoint, assert against the parsed AST instead of an executed import — an AST assertion on a
    module-level `FunctionDef` is sufficient and keeps the test CPU-only and GPU-free.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_phase16_ladder.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/test_phase16_ladder.py -q` exits 0
    - `grep -c "def probe_guessability" scripts/phase14_factset_gate.py` returns 1
    - `python3 -c "import ast,pathlib; t=ast.parse(pathlib.Path('scripts/phase14_factset_gate.py').read_text()); f=[n for n in ast.walk(t) if isinstance(n,ast.FunctionDef) and n.name=='probe_guessability'][0]; names=[a.arg for a in f.args.args]; assert names==['model','tok','device','forbid','value','questions'], names; assert [a.arg for a in f.args.kwonlyargs]==['start_index']"` exits 0
    - `probe_guessability` calls `_probe` (AST assertion) and does not contain its own
      `torch.Generator` construction
    - `git diff scripts/phase14_factset_gate.py` shows **0 deletions** — the widening is purely additive
    - `.venv/bin/pytest tests/test_phase14_factset.py -q` still exits 0
  </acceptance_criteria>
  <done>The guessability instrument has a public entry point taking an arbitrary string; Phase 16 and Phase 17 both import it rather than copying it.</done>
</task>

<task type="auto">
  <name>Task 2: The ladder pre-registration constants and the cell arithmetic</name>
  <files>scripts/phase16_ladder.py, tests/test_phase16_ladder.py</files>
  <read_first>
    - `scripts/erasure_gate.py` lines 1-60 and 130-200 (module docstring, `wilson_upper_bound`, `rule_of_three`, `erasure_is_worth_attempting`'s lower-bound-by-complement idiom)
    - `scripts/finetune_ab.py` lines 100-140 (the "gate as module-level pure function + constants" precedent this file follows — Phase 13 13-01)
    - `.planning/phases/16-weight-vs-prompt-persistence-control/16-RESEARCH.md` §Architecture Patterns → Q1 in full, including the constants block, the option table, and the verified threshold table
    - `scripts/phase14_recall.py` lines 1210-1221 (`n_answerable` — the statistic every cell reports)
  </read_first>
  <action>
    Create `scripts/phase16_ladder.py`. Nothing executes at import: constants and pure functions
    only, with `main()` under a `__main__` guard added in plan 16-05. Module docstring states this
    is the PERS-01 pre-registration and that every number in it is derived from material already
    published in this repository, none of it from any Phase 16 measurement.

    **`import phase14_factset_gate` and `import phase14_factset` belong INSIDE functions only.**
    `phase14_factset_gate` imports the fact set at module level; a module-level import here would
    put locked fact strings into this driver's address space and into its docstring scan surface.

    Module-level constants, each with the comment block RESEARCH specifies:

    - `LADDER_FLOOR_SOURCE = "results/phase14_recall_report.md:378 (Phase 14 Control 1, 1/1944 draws)"`
    - `LADDER_FLOOR_ANSWERABLE = 1`
    - `LADDER_FLOOR_QUESTIONS = 216`
    - `LADDER_FLOOR_UPPER_95 = 0.020481915502612365`
    - `LADDER_CELL_QUESTIONS = 216`
    - `LADDER_CELL_DRAWS = 9`
    - `LADDER_CELL_Z = 2.393979799818510`
    - `LADDER_CELL_PASS_K = 10`

    The comments must carry the reasoning, not just the numbers: that `1944 = 216 x 9` and a single
    hit across all draws means exactly one question had `k >= 1`, so the conversion to the
    STAT-01-legal question unit is ARITHMETIC on a committed number and not a re-measurement; that
    the anchor is deliberately the COMMITTED floor and never the post-fix re-run; and Choices 1, 2
    and 3 from this plan's context section, each with its stated reason.

    The rung lattice, also module-level and pre-committed:
    - `LADDER_SPANS = (1, 2, 5)` — token lengths
    - `LADDER_DISTANCES = (2, 30)` — approximate token distance from the value to the
      `<|assistant|>` trigger
    - `TOP_RUNG = "fairness-control-rerun"` — real taught values, distance ~30, span median 5
    - `RUNG_DIFFICULTY_ORDER` — an explicit tuple, easiest to hardest, span-major then distance:
      `(1, 2)`, `(1, 30)`, `(2, 2)`, `(2, 30)`, `(5, 2)`, `(5, 30)`, `TOP_RUNG`.
      Comment the ordering rationale: span length is the primary suspect for where capability dies
      (the real values are 4-8 tokens over a 547-id near-character vocabulary in a 6-layer model),
      so span dominates distance in the licensing order. This order is pre-committed precisely so
      "the highest passed rung" is not decided after seeing which cells passed.

    Pure functions:

    `cell_passed(n_answerable, n_questions=LADDER_CELL_QUESTIONS)` -> `(bool, float)`. The bool is
    `n_answerable >= LADDER_CELL_PASS_K`; the float is
    `1.0 - wilson_upper_bound(n_questions - n_answerable, n_questions, LADDER_CELL_Z)`, the
    one-sided lower bound. Docstring: this is a LICENSING decision, not a hypothesis test (STAT-06 /
    D-09); the integer literal is the gate and the bound is its derivation.

    `cell_report(n_answerable, n_questions=LADDER_CELL_QUESTIONS)` -> dict with `answerable`,
    `questions`, `rate`, `wilson_upper_95`, `lower_bound`, `passed`, and — only when
    `n_answerable == 0` — `rule_of_three_upper`. STAT-02: denominator and bound always, never a
    bare 0%.

    `format_cell(row)` -> str. The report line for one cell. It must FORMAT the constants
    (`f"{row['answerable']}/{row['questions']}"`), never contain a typed literal such as `"10/216"`
    or `"0.0463"`.

    Tests to add to `tests/test_phase16_ladder.py` (CPU-only, no torch):
    - `test_pass_k_is_the_derived_minimum()` — recompute, in the test, the smallest `k` in
      `range(0, LADDER_CELL_QUESTIONS + 1)` for which
      `1 - wilson_upper_bound(LADDER_CELL_QUESTIONS - k, LADDER_CELL_QUESTIONS, LADDER_CELL_Z) >
      LADDER_FLOOR_UPPER_95`, and assert it equals `LADDER_CELL_PASS_K`. Import
      `wilson_upper_bound` from `scripts/erasure_gate.py`; do not reimplement it.
    - `test_floor_constants_reproduce_the_committed_denominator()` — assert
      `LADDER_FLOOR_QUESTIONS * LADDER_CELL_DRAWS == 1944` and
      `LADDER_FLOOR_UPPER_95 == wilson_upper_bound(LADDER_FLOOR_ANSWERABLE, LADDER_FLOOR_QUESTIONS)`
      exactly (equality, not `pytest.approx` — the literal was produced by that call).
    - `test_pass_k_is_insensitive_to_family_size()` — assert the derived minimum is also 10 at
      `z = NormalDist().inv_cdf(1 - 0.05/7)`, so the literal cannot be moved by re-arguing whether
      multiplicity is priced at 6 cells or 7 rungs.
    - `test_cell_statistic_counts_questions_not_draws()` — STAT-01. Feed `cell_report` a case where
      the question count and the draw count diverge (`n_answerable=10`, `n_questions=216`) and
      assert the reported `questions` is 216 and that no key in the returned dict equals 1944 or
      derives its rate from a draw count.
    - `test_no_bare_zero_percent()` — STAT-02. `cell_report(0)` contains `rule_of_three_upper`
      equal to `rule_of_three(216)`, and `format_cell(cell_report(0))` does not match the regex
      `\b0%|\b0\.0%|\b0\.00%`.
    - `test_rung_difficulty_order_is_a_permutation_of_the_lattice()` — the order contains every
      `(span, distance)` pair in `LADDER_SPANS x LADDER_DISTANCES` exactly once, plus `TOP_RUNG`,
      and has length 7.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_phase16_ladder.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/test_phase16_ladder.py -q` exits 0 with >= 7 tests collected
    - `scripts/phase16_ladder.py` contains the literals `2.393979799818510`, `0.020481915502612365`,
      `216`, `9`, and `LADDER_CELL_PASS_K = 10`
    - `python3 -c "import ast,pathlib; t=ast.parse(pathlib.Path('scripts/phase16_ladder.py').read_text()); mods=[n for n in t.body if isinstance(n,(ast.Import,ast.ImportFrom))]; bad=[n for n in mods if 'phase14_factset' in (getattr(n,'module','') or '') or any('phase14_factset' in a.name for a in getattr(n,'names',[]))]; assert not bad, bad"` exits 0 (no module-level fact-set import, direct or via the gate)
    - `grep -c "scipy" scripts/phase16_ladder.py` returns 0
    - `grep -c "from erasure_gate import\|import erasure_gate" scripts/phase16_ladder.py` returns >= 1
    - Importing `scripts/phase16_ladder.py` executes no model load and no generation — an
      `importlib` load in the test completes in under 2 seconds
    - Deliberate-RED, observed and recorded: change `LADDER_CELL_PASS_K` to 9, confirm
      `test_pass_k_is_the_derived_minimum` FAILS, restore
  </acceptance_criteria>
  <done>The ladder's licensing thresholds are committed literals whose derivation is pinned by test, and every cell report carries a denominator and a bound.</done>
</task>

<task type="auto">
  <name>Task 3: licensed_headline() — total over the rung lattice, computed by importing constants</name>
  <files>scripts/phase16_ladder.py, tests/test_phase16_ladder.py</files>
  <read_first>
    - `scripts/phase16_ladder.py` as written in Task 2 (`RUNG_DIFFICULTY_ORDER`, `cell_passed`, `cell_report`)
    - `.planning/phases/16-weight-vs-prompt-persistence-control/16-CONTEXT.md` D-14 (the verbatim clause) and D-11 (why the grid is 2-D)
    - `.planning/ROADMAP.md` §Phase 16 SC1 (the capability-deficit branch's exact scope)
    - `.planning/phases/16-weight-vs-prompt-persistence-control/16-RESEARCH.md` §Q2 (why all-cells-fail is a normal outcome, and why the `(span 1, distance ~2)` cell is the real discriminator)
  </read_first>
  <action>
    Add `licensed_headline(passed_rungs)` to `scripts/phase16_ladder.py`. `passed_rungs` is an
    iterable of rung keys drawn from `RUNG_DIFFICULTY_ORDER`. The function returns a dict with
    `branch` (a stable identifier string), `statement` (the licensed prose), and `highest_passed`.

    It branches on the HIGHEST passed rung in `RUNG_DIFFICULTY_ORDER`, and on nothing else. Five
    branches, each a module-level string constant so the branch text is auditable independently of
    the dispatch logic:

    1. `no_rung_passed` — no rung passed. Licenses ONLY the SC1 capability-deficit statement: the
       base model cannot use a fact placed in its own context window at this scale, so a
       prompt-arm floor result is evidence about CAPABILITY and licenses no claim that weights beat
       prompting. This is a normal, pre-registered outcome — RESEARCH Q2 records that Phase 14
       measured 1 of 216 questions on this exact model with this exact prompt builder. There is no
       "ladder failed, investigate the instrument" escape hatch; adding one would be an unwritten
       branch discovered after seeing the result.
    2. `span_1` — highest passed rung has span 1. Licenses: the base can use a 1-token in-context
       span, and cannot sustain a multi-token copy. Explicitly scoped to span 1.
    3. `span_2` — highest passed rung has span 2. Same shape, scoped to span 2.
    4. `span_5_synthetic` — highest passed is a span-5 synthetic cell. Licenses: the base can
       sustain a 5-token in-context copy at that distance, so the prompt arm is off the floor and a
       four-arm comparison measures mechanism rather than capability deficit.
    5. `top_rung_real` — the top rung (real taught values) passed. The strongest branch: the base can
       recover a real taught value from its own persona span, so the comparison is a mechanism
       comparison outright.

    Every statement string must state what it does NOT license as well as what it does. None of them
    may contain a retyped numeric threshold — where a number is needed, format it from the constants.

    Add `monotonicity_anomalies(cell_results)` -> list. Returns each pair `(easier, harder)` in
    `RUNG_DIFFICULTY_ORDER` where the harder rung passed and the easier one did not. Per D-14 this
    is recorded as a named instrument anomaly in the report and does NOT stop the run and does NOT
    change the licensed branch. Docstring says exactly that, and says that non-monotonicity — not
    failure — is the instrument-broken signal (RESEARCH Pitfall 6).

    Tests in `tests/test_phase16_ladder.py`:
    - `test_licensed_headline_is_total()` — enumerate all `2**7 = 128` subsets of
      `RUNG_DIFFICULTY_ORDER`; for each, assert `licensed_headline` returns a dict whose `branch` is
      one of the five known identifiers and whose `statement` is a non-empty string; assert the
      branch is a pure function of the highest passed rung by comparing against an
      independently-computed expectation in the test.
    - `test_all_fail_branch_is_the_sc1_capability_deficit_statement()` — `licensed_headline(())`
      returns branch `no_rung_passed`, and its statement mentions capability and explicitly refuses
      the weights-beat-prompting claim.
    - `test_licensed_headline_retypes_no_threshold_literal()` — AST scan of
      `scripts/phase16_ladder.py`: collect every `ast.Constant` whose value is a `str` (excluding
      `ast.JoinedStr` formatted values, which ARE the import path) and assert none contains any of
      the substrings `"10/216"`, `"0.0463"`, `"0.020481"`, `"2.39397"`, `"1/1944"`. The one
      permitted exception is `LADDER_FLOOR_SOURCE`, which is a citation of a committed report line
      and must be allowlisted by name in the test with a comment saying why.
    - `test_monotonicity_anomalies_do_not_change_the_branch()` — construct a deliberately
      non-monotone result set (a hard rung passing, an easy one failing), assert
      `monotonicity_anomalies` returns a non-empty list AND `licensed_headline` returns the same
      branch it would have returned for the highest passed rung alone.
    - `test_ladder_driver_holds_no_fact_strings_at_import()` — reuse `embedded_fact_values` from
      `tests/test_phase14_scoring.py` against an `importlib`-loaded `scripts/phase16_ladder.py`,
      with the same 10 locked+soft values that test uses. Docstrings included.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_phase16_ladder.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/test_phase16_ladder.py -q` exits 0 with >= 12 tests collected
    - `test_licensed_headline_is_total` exercises all 128 subsets (assert the loop count in the test
      source, or assert a collected-branch counter equals 128)
    - `grep -c "def licensed_headline" scripts/phase16_ladder.py` returns 1
    - `grep -c "def monotonicity_anomalies" scripts/phase16_ladder.py` returns 1
    - `.venv/bin/pytest tests/test_phase14_factset.py tests/test_phase14_scoring.py -q` exits 0 —
      the new driver did not break the clean-room scan
    - Deliberate-RED, observed and recorded: add the string `"10/216"` into one of the branch
      statement constants, confirm `test_licensed_headline_retypes_no_threshold_literal` FAILS,
      restore to a byte-identical file
  </acceptance_criteria>
  <done>The headline this phase is allowed to publish is computed by a committed function that imports its thresholds, with a first-class all-fail branch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pre-registered threshold → measured result | A threshold set or moved after seeing data is not a threshold |
| licensing function → published prose | A retyped number in a verdict string can drift from the constant it claims to state |
| fact set → the ladder driver's address space | A module-level import of `phase14_factset_gate` pulls locked values into this driver by a transitive edge |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-13 | Tampering | `LADDER_CELL_PASS_K` | mitigate | Task 2's `test_pass_k_is_the_derived_minimum` recomputes the literal from `erasure_gate.wilson_upper_bound` and asserts equality; `test_pass_k_is_insensitive_to_family_size` blocks moving it by re-arguing the family size |
| T-16-14 | Repudiation | the anchor number | mitigate | `LADDER_FLOOR_SOURCE` cites the committed report line; `test_floor_constants_reproduce_the_committed_denominator` asserts `216 x 9 == 1944`, so the anchor cannot silently become the post-fix re-run |
| T-16-15 | Tampering | verdict prose | mitigate | Task 3's AST scan forbids retyped threshold digits in any string constant; the verdict is computed by formatting the imported constants |
| T-16-16 | Information disclosure | locked fact values reaching the ladder driver at import | mitigate | Lazy import of `phase14_factset_gate` enforced by an AST acceptance check; `test_ladder_driver_holds_no_fact_strings_at_import` reuses the existing `embedded_fact_values` substring scan, docstrings included |
| T-16-17 | Elevation of privilege | scope creep from licensing into inference | mitigate | `cell_passed` computes no p-value and emits no verdict; D-09's Holm family stays closed at 6 pairs. Task 3's branch set has exactly five members and no "investigate the instrument" escape hatch |
| T-16-SC | Tampering | dependency surface | mitigate | Zero installs. `grep -c scipy scripts/phase16_ladder.py == 0`; every bound comes from `scripts/erasure_gate.py` (STAT-04) |
</threat_model>

<verification>
```
.venv/bin/pytest tests/test_phase16_ladder.py tests/test_phase14_scoring.py tests/test_phase14_factset.py -q
make test
git diff scripts/phase14_factset_gate.py | grep -c '^-[^-]'   # MUST be 0 — additive widening only
```
</verification>

<success_criteria>
- `probe_guessability` is public, additive, and calls `_probe`
- Every ladder constant is a committed module-level literal with its derivation in the comment
- `test_pass_k_is_the_derived_minimum` passes and has been observed RED at `k = 9`
- `licensed_headline` is total over 128 subsets with a first-class all-fail branch
- No fact string reaches `scripts/phase16_ladder.py` at import time
- `make test` green
</success_criteria>

<output>
Create `.planning/phases/16-weight-vs-prompt-persistence-control/16-04-SUMMARY.md` when done.
Record the three planner choices (n = 216, z priced at 0.05/6, one literal for all 7 rungs) with
their reasons, the two RED observations, and the exact `RUNG_DIFFICULTY_ORDER` so plan 16-05 wires
the grid against it without re-deriving it.
</output>
