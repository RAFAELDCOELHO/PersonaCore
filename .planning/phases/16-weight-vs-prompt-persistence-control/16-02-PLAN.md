---
phase: 16-weight-vs-prompt-persistence-control
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phase14_recall.py
  - tests/test_phase14_scoring.py
autonomous: true
requirements: [PERS-05, PERS-06]

must_haves:
  truths:
    - "run_fairness_control seeds each draw from the question's OWN seed_index, not its position in the list it was handed (D-17)"
    - "The inline unnamed in-prompt assertion at phase14_recall.py:1188-1192 is a named function assert_value_in_prompt, the logical twin of assert_no_value_in_prompt (D-18)"
    - "assert_value_in_prompt takes its values as a PARAMETER and checks at both the normalized-string and contiguous-id-run levels"
    - "Phases 17 and 18 inherit both fixes because both live in the shared instrument, not in a Phase 16 file (D-20)"
  artifacts:
    - path: "scripts/phase14_recall.py"
      provides: "the fixed shared instrument"
      contains: "def assert_value_in_prompt"
    - path: "tests/test_phase14_scoring.py"
      provides: "behavioural + AST proof of both fixes"
      contains: "seed_index"
  key_links:
    - from: "scripts/phase14_recall.py::run_fairness_control"
      to: "draw_all"
      via: "item.seed_index, never enumerate position"
      pattern: "draw_all\\(.*item\\.seed_index"
    - from: "scripts/phase14_recall.py::assert_value_in_prompt"
      to: "_is_contiguous_subsequence"
      via: "token-level leak check mirroring assert_no_value_in_prompt"
      pattern: "_is_contiguous_subsequence"
---

<objective>
Fix the two defects in the SHARED instrument before anything in v3.0 measures anything: the
`enumerate(questions)` pairing defect in `run_fairness_control` (PERS-05), and the unnamed inline
in-prompt assertion that should be `assert_no_value_in_prompt`'s logical twin (PERS-06).

Purpose: Phase 16 is the first phase to *compare* the fairness-control arm against anything. Phase
14 never did, which is why the defect survived. A comparison scored on unpaired seeds is not a
paired comparison, and PERS-02 claims pairing by `item.seed_index` explicitly. Both fixes land in
`scripts/phase14_recall.py` (D-20) because Phases 17 and 18 consume that same file — a fix that
does not live there is a fix they do not inherit, which is literally the failure mode PERS-05
exists to prevent.

Output: a corrected `scripts/phase14_recall.py` and the behavioural + AST tests that pin both fixes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-weight-vs-prompt-persistence-control/16-CONTEXT.md
@.planning/phases/16-weight-vs-prompt-persistence-control/16-RESEARCH.md

**D-19, read before you panic about a changed number.** The fix CHANGES WHICH SEEDS ARE DRAWN, so
the `1/1944` the Phase 14 code produced does not reproduce bit-for-bit afterwards. That is the
definition of the defect, not a regression. Do **not** "preserve" the old behavior behind a flag,
and do **not** amend `results/phase14_recall_report.md` — the committed Phase 14 number stays
exactly as published. Phase 16 measures the delta separately (D-13) as a *reported* measurement of
this fix's impact, never as a silent assertion that it did not matter.

**LAZY-IMPORT RULE.** `import phase14_factset` belongs only inside functions. Locked fact strings
must never reach module import time — `test_no_fact_strings_at_import` scans docstrings too, so
this constrains how you write comments and docstrings in this file.

<interfaces>
Current state of the code being changed — read these exact lines before editing:

`scripts/phase14_recall.py:1147` run_fairness_control(model, tok, device, forbid, questions, statements)
  :1184  for index, item in enumerate(questions):      <- THE DEFECT (PERS-05)
  :1187  prompt_ids = build_recall_prompt(tok, item.question, persona=[statement])
  :1188-1192  _prove(contains_value(tok.decode(prompt_ids), item.fact.value), "...")   <- the unnamed twin (PERS-06)
  :1193  completions, stopped = draw_all(model, tok, prompt_ids, device, forbid, index)
  :1219  "n_answerable": sum(1 for entry in asked if entry["k"] > 0)

`scripts/phase14_recall.py:398` assert_no_value_in_prompt(tok, question, values)  <- the shape to mirror
  builds ids via build_recall_prompt(tok, question)
  checks normalize(value) not in normalize(tok.decode(ids))
  checks not _is_contiguous_subsequence(ids, tok.encode(value))
  raises via _prove (SystemExit), values is a PARAMETER

`scripts/phase14_recall.py:392` _is_contiguous_subsequence(haystack, needle) -> bool
`scripts/phase14_recall.py:221` _prove(condition, message)  -> raises SystemExit on falsity
`scripts/phase14_recall.py:227` question_seed(index)
`scripts/phase14_recall.py:542` draw_all(model, tok, prompt_ids, device, forbid, index)
`scripts/phase14_recall.py:672` class RecallItem(NamedTuple): fact, question, split, reserved, seed_index=-1
`scripts/phase14_recall.py:692` stamp_seed_indices(items) -> tuple  (stamps 0..n-1 per ARM)
`scripts/phase14_recall.py:769` run_scored_recall(...)  — lines 795-800 show the established
  "`-1` means stamp_seed_indices was never called" _prove guard to mirror

`scripts/phase14_recall.py:1120-1128` main() builds `core_taught + core_held_out` for the fairness
  control — both arms already pass through stamp_seed_indices at :1063-1065, so seed_index is
  already populated on every item the control receives.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PERS-05 — seed the fairness control from item.seed_index</name>
  <files>scripts/phase14_recall.py, tests/test_phase14_scoring.py</files>
  <read_first>
    - `scripts/phase14_recall.py` lines 1147-1221 (`run_fairness_control` in full, including its docstring)
    - `scripts/phase14_recall.py` lines 672-712 (`RecallItem`, `stamp_seed_indices` — the CR-01 fix this mirrors)
    - `scripts/phase14_recall.py` lines 789-808 (`run_scored_recall`'s `-1` sentinel guard — copy this shape)
    - `scripts/phase14_recall.py` lines 1110-1130 (`main`'s call into the control, to confirm the items are already stamped)
    - `tests/test_phase14_scoring.py` lines 452-500 (`_fake_question` and the surrounding fixture idiom)
  </read_first>
  <action>
    In `run_fairness_control`, replace `for index, item in enumerate(questions):` with
    `for item in questions:` and pass `item.seed_index` to `draw_all` in place of the positional
    `index`. No other call in that function may consume a positional index.

    Before the loop body draws, add a `_prove(item.seed_index >= 0, ...)` sentinel guard mirroring
    `run_scored_recall`'s at lines 795-800, with a message naming PERS-05: an unstamped item means
    `stamp_seed_indices` was skipped and the control would draw from a stream the scored arm never
    used.

    Record `"seed_index": item.seed_index` in the per-question dict appended to `asked`, matching
    what `run_scored_recall` already records at line 822 — a pairing claim that is not in the record
    is not auditable.

    Extend `run_fairness_control`'s docstring with a paragraph naming the defect and D-19: the seed
    each draw uses now comes off the item, so the number this function produces does not reproduce
    the committed Phase 14 `1/1944` bit-for-bit. State that this is the defect being fixed, not a
    regression, and that the committed Phase 14 report is NOT amended. Do not name any locked fact
    value in the docstring.

    Add to `tests/test_phase14_scoring.py`:
    - `test_fairness_control_seeds_from_the_item_not_the_loop_position()` — behavioural. Build a
      list of fake `RecallItem`s whose `seed_index` values are deliberately NON-positional (e.g.
      `(7, 3, 11)` for positions 0, 1, 2). Monkeypatch `draw_all` on the loaded module to record the
      `index` argument it receives and return a fixed `(completions, stopped)` pair; monkeypatch
      `build_recall_prompt`, `contains_value`, `score_question` and `adapter_disabled` as needed so
      no model is required. Assert the recorded indices are `[7, 3, 11]`, not `[0, 1, 2]`.
    - `test_fairness_control_has_no_enumerate_over_questions()` — AST. Parse
      `scripts/phase14_recall.py`, locate the `run_fairness_control` FunctionDef, and assert no
      `ast.Call` inside it has `func.id == "enumerate"`. AST rather than substring: the docstring
      discusses the defect by name.
    - `test_fairness_control_refuses_an_unstamped_item()` — assert `SystemExit` is raised when an
      item carries `seed_index == -1`.

    Load the module the way this test file already does (importlib from `scripts/`), not by
    `sys.path` mutation, so the LAZY-IMPORT boundary is preserved.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_phase14_scoring.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/test_phase14_scoring.py -q` exits 0
    - `.venv/bin/python -c "import ast,pathlib; t=ast.parse(pathlib.Path('scripts/phase14_recall.py').read_text()); f=[n for n in ast.walk(t) if isinstance(n,ast.FunctionDef) and n.name=='run_fairness_control'][0]; assert not [c for c in ast.walk(f) if isinstance(c,ast.Call) and getattr(c.func,'id',None)=='enumerate']"` exits 0
    - `grep -n "item.seed_index" scripts/phase14_recall.py` shows a hit inside the line range of
      `run_fairness_control` (>= 1147)
    - The `asked` dict appended in `run_fairness_control` contains a `"seed_index"` key
    - `run_fairness_control`'s returned dict still contains `"n_answerable"` computed as
      `sum(1 for entry in asked if entry["k"] > 0)` — this is the STAT-01-legal question-unit
      numerator every ladder cell in this phase compares against, and it must not change shape
    - `results/phase14_recall_report.md` is byte-unchanged: `git diff --stat results/` is empty
    - Deliberate-RED check: revert the `item.seed_index` edit in a scratch copy, confirm
      `test_fairness_control_seeds_from_the_item_not_the_loop_position` FAILS, restore. Record the output.
  </acceptance_criteria>
  <done>The fairness-control arm draws each question's own seeded stream, so it is paired with the scored arms rather than merely comparable.</done>
</task>

<task type="auto">
  <name>Task 2: PERS-06 — extract assert_value_in_prompt as the named twin</name>
  <files>scripts/phase14_recall.py, tests/test_phase14_scoring.py</files>
  <read_first>
    - `scripts/phase14_recall.py` lines 392-422 (`_is_contiguous_subsequence` and `assert_no_value_in_prompt` — the exact shape to mirror)
    - `scripts/phase14_recall.py` lines 1182-1195 (the inline unnamed assertion being extracted)
    - `scripts/phase14_recall.py` lines 279-313 (`normalize`, `contains_value` — the normalization the twin must reuse, never reimplement)
    - `tests/test_phase14_scoring.py` — search for existing `assert_no_value_in_prompt` tests and mirror their structure
  </read_first>
  <action>
    Add `assert_value_in_prompt(tok, prompt_ids, values)` to `scripts/phase14_recall.py`, placed
    immediately after `assert_no_value_in_prompt` (around line 422) so the twins read as a pair.

    Signature discipline, non-negotiable:
    - `values` is a **PARAMETER**, never a module-level constant. This module holds no fact strings
      at import time (LAZY-IMPORT RULE); the caller passes them in.
    - It takes `prompt_ids` (already built) rather than a `question` string, because its callers —
      the fairness control and every Phase 16 ladder rung — build prompts with a persona span or
      with the value inside the question text, and rebuilding from a bare question would check a
      different prompt than the one drawn from.

    Behavior: for each value in `values`, `_prove` BOTH levels, mirroring the twin exactly but with
    the polarity inverted:
    - `normalize(value) in normalize(tok.decode(prompt_ids))`
    - `_is_contiguous_subsequence(prompt_ids, tok.encode(value))`

    Failure messages must name what the absence means: the control or rung measures nothing if the
    fact is not actually in view, so this aborts rather than warns — the same `_prove`/`SystemExit`
    register as its twin.

    Docstring must state (a) it is the logical twin of `assert_no_value_in_prompt`, (b) why both
    levels are checked (the normalized-string check can pass on a value that detokenizes
    differently; the contiguous-id-run check catches what the string check does not, and vice
    versa — a value split across a BPE merge boundary is present in the string but not as a
    contiguous id run, which is still "in view" for the model), and (c) that a *both-levels* check
    is what makes the ladder's low rungs auditable at all. Name no locked fact value.

    Rewrite `run_fairness_control`'s inline `_prove(contains_value(...))` block to call
    `assert_value_in_prompt(tok, prompt_ids, [item.fact.value])` instead. The inline block is
    deleted, not left alongside — two assertions with different strictness is exactly how the
    stricter one silently stops being the one that runs.

    Add to `tests/test_phase14_scoring.py`:
    - `test_assert_value_in_prompt_is_the_named_twin()` — the function exists, is public (no leading
      underscore), and its signature's third parameter is named `values` (inspect the AST or
      `inspect.signature`).
    - `test_assert_value_in_prompt_holds_no_module_level_values()` — AST: no default value on the
      `values` parameter, and no module-level constant in `phase14_recall.py` is referenced as its
      argument inside the module.
    - `test_assert_value_in_prompt_checks_both_levels()` — feed a synthetic tokenizer double where
      the string check passes but the id run does not (and the converse), assert `SystemExit` in
      both directions. Use a fake `tok` with `encode`/`decode`, no real tokenizer needed.
    - `test_fairness_control_calls_the_named_twin()` — AST: `run_fairness_control` contains a call to
      `assert_value_in_prompt`, and contains no bare `_prove(contains_value(...))` call.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_phase14_scoring.py tests/test_phase14_factset.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/test_phase14_scoring.py tests/test_phase14_factset.py -q` exits 0
      (the factset run proves `test_no_fact_strings_at_import` still passes with the new docstrings)
    - `grep -c "def assert_value_in_prompt" scripts/phase14_recall.py` returns 1
    - `.venv/bin/python -c "import inspect,importlib.util,pathlib; s=importlib.util.spec_from_file_location('r','scripts/phase14_recall.py'); m=importlib.util.module_from_spec(s); s.loader.exec_module(m); p=list(inspect.signature(m.assert_value_in_prompt).parameters); assert p[-1]=='values', p"` exits 0
    - `grep -v '^#' scripts/phase14_recall.py | grep -c "_prove(\s*$" ` — the inline
      `_prove(contains_value(...))` block inside `run_fairness_control` is gone: an AST walk of
      `run_fairness_control` finds a call to `assert_value_in_prompt` and zero calls to
      `contains_value` in an assertion position
    - `_is_contiguous_subsequence` is called by `assert_value_in_prompt` (AST assertion), so the
      token-level half is not skipped
    - Deliberate-RED check: temporarily change `assert_value_in_prompt` to check only the string
      level, confirm `test_assert_value_in_prompt_checks_both_levels` FAILS, restore. Record the output.
  </acceptance_criteria>
  <done>The in-prompt assertion is a named, parameterized, two-level function that Phases 17 and 18 inherit from the shared instrument.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fact set → model context | A locked value crossing into a SCORED prompt falsifies the phase's central claim at the exact moment it is demonstrated |
| model context → fact set (inverse) | A value failing to cross into an INTENDED-context prompt makes the fairness control and every ladder rung measure nothing while reporting a number |
| Phase 16 instrument → Phases 17/18 | A fix placed outside the shared file is a fix the downstream phases silently do not inherit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-05 | Information disclosure | `run_fairness_control` prompt construction | mitigate | Task 2: `assert_value_in_prompt` aborts via `_prove`/`SystemExit` when the value is NOT in view; its twin `assert_no_value_in_prompt` already aborts when it IS in view on scored paths. Every `draw_all` call site is covered by one of the two |
| T-16-06 | Tampering | seed stream pairing | mitigate | Task 1: seeds come off `item.seed_index`; a `-1` sentinel `_prove` refuses an unstamped item, so a silently-unpaired comparison cannot run at all |
| T-16-07 | Repudiation | the changed Phase 14 number | accept, documented | D-19: the fix changes drawn seeds by design. Mitigation is disclosure, not prevention — `results/phase14_recall_report.md` stays byte-unchanged and Phase 16 reports the delta explicitly (D-13). Silently re-running Phase 14 to "match" would be the actual defect |
| T-16-08 | Elevation of privilege | assertion strictness | mitigate | Task 2 DELETES the inline weaker assertion rather than leaving it beside the named one — two assertions of differing strictness is how the stricter one stops being the one that runs |
</threat_model>

<verification>
```
.venv/bin/pytest tests/test_phase14_scoring.py tests/test_phase14_factset.py tests/test_phase16_fixture_regen.py -q
make test
git diff --stat results/    # MUST be empty
```
</verification>

<success_criteria>
- `run_fairness_control` contains no `enumerate` and draws from `item.seed_index`
- `assert_value_in_prompt` exists, is public, takes `values` as a parameter, checks both levels
- `results/phase14_recall_report.md` and `results/phase16_recall_sample.json` are byte-unchanged
- `make test` green
- Both deliberate-RED observations recorded in the SUMMARY
</success_criteria>

<output>
Create `.planning/phases/16-weight-vs-prompt-persistence-control/16-02-SUMMARY.md` when done.
Record both RED outputs verbatim, and state explicitly that the committed Phase 14 report was not
amended (D-19).
</output>
