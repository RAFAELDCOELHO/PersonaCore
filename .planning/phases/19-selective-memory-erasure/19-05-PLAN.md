---
phase: 19-selective-memory-erasure
plan: 05
type: execute
wave: 5
depends_on: ["19-04"]
files_modified:
  - scripts/phase19_erasure.py
  - tests/test_phase19_erasure.py
autonomous: true
requirements: [ERASE-01, STAT-02, STAT-05, STAT-06]

must_haves:
  truths:
    - "Representational consistency cannot become a gate — enforced structurally, not by convention"
    - "erasure_succeeded is called, never re-implemented; there is no second verdict path"
    - "Every comparability parameter is asserted equal to Phase 18's committed value"
  artifacts:
    - path: "scripts/phase19_erasure.py"
      provides: "PARITY_KEYS, assert_phase18_parity(), the report text constants, the ship-decision marker PAIR, render_verdict()"
      contains: "ERASURE_SHIP_PENDING_LINE"
    - path: "tests/test_phase19_erasure.py"
      provides: "AST guards: descriptive-not-gated, single verdict path, parity"
  key_links:
    - from: "tests/test_phase19_erasure.py"
      to: "scripts/phase19_erasure.py"
      via: "ast.walk over the representational read"
      pattern: "sign_test_exact"
    - from: "scripts/phase19_erasure.py"
      to: "scripts/erasure_gate.erasure_succeeded"
      via: "the ONE verdict call"
      pattern: "erasure_succeeded\\("
---

<objective>
Close the three structural failure modes before any number can exploit them: a descriptive statistic
becoming a gate, a second verdict path, and comparability drift against Phase 18.

Purpose: `ERASURE_DECISION_RULE`'s fourth clause makes representational consistency DESCRIPTIVE and
EXPLICITLY NOT GATED (`scripts/erasure_gate.py:118-122`), and `ROADMAP.md:530-534` says any plan that
converts one of these into pass/fail is violating the pre-registration. This project enforces that
structurally rather than by convention — `test_context_pressure_sweep_is_not_gated` landed BEFORE the
code it guards (`scripts/phase16_persistence.py:1289-1293`), and Phase 17's D-21 scan catches a
second `sign_test_exact` call site as a second hypothesis family (`STATE.md:219`). Phase 19 ships the
equivalent, in the same wave as the read it guards.

The verdict guard exists because STAT-05 requires the verdict to be computed by IMPORTING the
committed constants rather than retyping them, and because the whole phase is worthless if a second
`(a)/(b)/(c)` evaluation exists anywhere to disagree with the committed one.

The parity guard exists because if the post-erasure run uses a different corpus, mask, `stop_ids`, K,
temperature or seed stride, the comparison against 92/104 is void (Q7.8).

Output: the AST guards, the parity assertions, the report text and the ship-decision marker PAIR
that `scripts/_addendum.py` will need in 19-16.

Still NO `results/phase19_*` artifact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-selective-memory-erasure/19-CONTEXT.md
@.planning/phases/19-selective-memory-erasure/19-RESEARCH.md
@scripts/erasure_gate.py
@scripts/_addendum.py
@scripts/_verdict.py
@scripts/phase19_erasure.py
</context>

<interfaces>
`scripts/_addendum.py` **ALREADY EXISTS** — committed `f8441ec` with `tests/test_phase19_docs.py`
proving additivity RED→GREEN over three mutations. It is the D3 continuation writer. Treat it as an
existing dependency; do NOT build, re-derive or duplicate it.

```python
append_addendum(path, addendum, *, pending, recorded)   # scripts/_addendum.py:56
```
BOTH marker halves are REQUIRED keywords — that pairing is the entire reason the module exists.
Phase 18's twin parameterised the placeholder it REPLACES but hard-coded the line it replaces it
WITH, so appending to a Phase 19 document through it injects a Phase 18 ship-decision sentence: a
false provenance claim from a helper whose purpose is to not alter published text. The placeholder
must occur EXACTLY once (`:70-77`); zero and more-than-one both raise `SystemExit`.

`scripts/_verdict.py:24` — `VERDICT_SECTION = re.compile(r"^## Verdict\b(.*?)(?=^## |\Z)", re.M|re.S)`;
`:27` `recorded_verdict(text)` returns the FIRST section's body, `None` when there is no such
section. The ONE anchored read, shared by every clobber guard.

Comparability parameters to assert (Q7.8):
`K = 48` (`scripts/phase18_extraction.py:93,146`), `ASR_RUNGS = (1,4,16,48)`,
`STOP_IDS = {8184, 8185}` (`scripts/phase14_recall.py:162`), `SAMPLE_TEMPERATURE = 0.8` /
`SAMPLE_TOP_P = 0.95` (`:159-160`), the `seed_index * K` stride
(`scripts/phase18_extraction.py:3640`), `corpus_sha256` (`:758`) over `canonical_json` (`:746`),
`forbid_digest` (`scripts/phase16_persistence.py:191`).

Descriptive-read tooling: `scripts/extract_deltas.py:174` `adapter_cells` (ΔW = scale·(B@A) per
(layer, projection), `scale` read from the artifact), `:199` `fisher_cells` (per-cell MEAN,
`FISHER_AGGREGATE = "mean"`, `:109`).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make descriptive-not-gated structural, and prove there is one verdict path</name>
  <files>tests/test_phase19_erasure.py, scripts/phase19_erasure.py</files>
  <behavior>
    - `test_representational_read_is_not_gated` walks the AST of `scripts/phase19_erasure.py` and fails if any function on the representational path calls `sign_test_exact`, `holm`, or `wilson_upper_bound`, or contains a `Compare` node against a module-level threshold constant.
    - The guard names the functions it scanned, so a renamed function silently escaping the scan fails rather than passes.
    - `test_verdict_is_called_never_reimplemented` fails if `erasure_succeeded` is not imported from `scripts/erasure_gate.py`, if it is called more than once outside tests, or if any of `V20_MASKED_DIALOGUE_VAL_PPL`, `V20_EWC_RETENTION_PPL`, `V20_RETENTION_NOISE_FLOOR`, `MARGIN_K` appears as a numeric literal anywhere in the driver.
    - Both guards were watched RED by deliberate mutation and restored byte-identically.
  </behavior>
  <action>
Add both guards to `tests/test_phase19_erasure.py`, modelled on
`tests/test_phase16_stats.py::test_context_pressure_sweep_is_not_gated` and on Phase 17's D-21 scan.
Scan by `ast.walk` over the parsed driver, not by regex over its text — a comment or a docstring
mentioning a threshold must not fail, and a real comparison must not pass by being spelled unusually.

Add to `scripts/phase19_erasure.py` the module-level tuple `DESCRIPTIVE_ONLY_FUNCTIONS`, naming the
functions the first guard scans. The guard asserts every name in it resolves to a real function in
the module — a DANGLING entry fails the guard rather than quietly excusing a missing scan target,
the discipline `DRAW_ALL_ASSERTED_BY` already uses (`tests/test_phase14_scoring.py:576-582`).

Record in the module docstring why this is structural: at n=8 facts and n=3 personas the sample
cannot support a threshold, and gating what the sample cannot support is treated as a DEFECT in this
project, not as extra rigour (`scripts/erasure_gate.py:118-122`). A second `sign_test_exact` call
site IS a second hypothesis family and would reprice Holm to carry a descriptive statistic.

Watch both RED: add a throwaway `if cosine > 0.5:` on the representational path, confirm guard 1
fails; add a second `erasure_succeeded(` call, confirm guard 2 fails; restore byte-identically.
Record both mutations in the SUMMARY.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_phase19_erasure.py -k "descriptive or verdict" -x</automated>
  </verify>
  <done>Both guards pass, both were watched RED and restored, and `DESCRIPTIVE_ONLY_FUNCTIONS` has no dangling entry.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin the Phase 18 parity assertions</name>
  <files>scripts/phase19_erasure.py, tests/test_phase19_erasure.py</files>
  <behavior>
    - `PARITY_KEYS` is an ordered tuple covering corpus digest, forbid digest, K, ASR rungs, stop ids, sample temperature, sample top-p and seed stride.
    - `assert_phase18_parity(config)` raises `SystemExit` naming the first mismatching key, and passes on the committed Phase 18 arm record's own `config` block read from `results/phase18_arm_adapter-on.json`.
    - Every expected value is IMPORTED from its owning module, never retyped in the driver.
    - A config missing a `PARITY_KEYS` entry raises rather than skipping it — an absent key must not read as agreement.
  </behavior>
  <action>
Implement `assert_phase18_parity(config)` over `PARITY_KEYS`, importing each expected value from its
owning module (`phase18_extraction`, `phase14_recall`, `phase16_persistence`) so a constant that
moves upstream turns this red rather than diverging silently. `corpus_sha256` is compared against the
digest of `results/phase18_corpus.json` recomputed through the committed `canonical_json` +
`corpus_sha256` pair (`phase18_extraction.py:746,758`), never against a pasted hex string. The Phase
19 target arms REUSE `results/phase18_corpus.json` verbatim rather than rebuilding it — that is what
makes the post-erasure numbers directly comparable to the committed `adapter-on` per-fact rates and
removes the whole class of drift Q7.8 describes.

Record in the docstring that Phase 18 evidences its pairing by digest and by two distinct pids, not
by assertion (`results/phase18_extraction_report.md:260-265`), and that Phase 19 records the same.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_phase19_erasure.py -k parity -x</automated>
  </verify>
  <done>`assert_phase18_parity` passes against the committed Phase 18 arm config; each of the eight keys was individually mutated in a synthetic config and each raised naming itself; no expected value is a literal in the driver.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pin the report text and the ship-decision marker PAIR</name>
  <files>scripts/phase19_erasure.py, tests/test_phase19_erasure.py</files>
  <behavior>
    - `ERASURE_SHIP_PENDING_LINE` and `ERASURE_SHIP_RECORDED_LINE` both exist, both name Phase 19 explicitly, and neither contains the string "Phase 18".
    - `render_report(...)` emits exactly one `## Verdict` section and exactly one occurrence of `ERASURE_SHIP_PENDING_LINE`, so `append_addendum`'s exactly-once requirement is satisfiable by construction.
    - `assert_erasure_report_not_clobbered(path)` reads the verdict body through `_verdict.recorded_verdict` and refuses to re-render when a verdict has been recorded; it distinguishes `None` (no section — not this writer's output) from an empty body.
    - The placeholder rewrite is CONDITIONAL on the addendum actually containing the thing it claims — Phase 18's W2 defect, where the rewrite ran unconditionally and silently converted "not yet recorded" into "recorded in the dated continuation" with no decision written (`18-VERIFICATION.md:226-250`).
    - No bare `0%` can be emitted: every proportion helper carries denominator + Wilson + `3/n` at zero (STAT-02).
  </behavior>
  <action>
Add the report text constants and `render_report`. The verdict section must publish, side by side and
in this order: the verdict returned by the committed `erasure_succeeded`; (a)'s upper bound with its
denominator and `rule_of_three(27)` beside it; (b)'s per-fact table with per-fact denominators, never
pooled; (c)'s dialogue PPL **pre-erasure and post-erasure** with the cap and the noise floor that
produced it; the floor with BOTH directions' values (`lock_erasure_floor` and
`literal_phase14_floor`) and the bound branch; and the representational read labelled DESCRIPTIVE.

Add the marker pair. Both halves must name Phase 19 — the entire reason `scripts/_addendum.py` makes
them required keywords is that Phase 18's twin let one half travel with the caller and hard-coded the
other, so appending through it wrote a Phase 18 provenance sentence into a document that was not
Phase 18's (`scripts/_addendum.py:13-27`). Add a test asserting neither line contains "Phase 18".

Add `assert_erasure_report_not_clobbered`, importing `_verdict.recorded_verdict` — never a
`text.split("## Verdict")[-1]`, the CR-02 defect that read a prose mention as a recorded verdict and
made `--force` the only way through (`scripts/_verdict.py:6-14`). No `--force`, no override flag:
recovery is deleting the file in a reviewed commit so the removal is visible in the diff.

Pin `D8_PUBLICATION_POSTURE` as a prose constant, decided now and before the number exists: if
ablating enough to erase the target also destroys non-targets, the finding is *"selective erasure is
not selective at 331,776 parameters"* and it ships UNSOFTENED, in the register Phase 18 shipped
`LEAKAGE_DEMONSTRATED`. And if the fact falls out on the first ablation, that is a MEASUREMENT and
not an absence — the paired instruments (target rank 1 → rank k, non-targets stay at rank 1,
capability unchanged) are what make "trivial" a result, and the framing for that branch is written
here rather than after the number (Q7.4).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_phase19_erasure.py tests/test_phase19_docs.py tests/test_phase16_prereg.py tests/test_package.py -x</automated>
  </verify>
  <done>A rendered synthetic report carries exactly one `## Verdict` and exactly one pending line; `append_addendum` succeeds against it with the Phase 19 pair and raises against a doubled placeholder; the clobber guard refuses a re-render after a recorded verdict; no marker line mentions Phase 18.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a descriptive statistic → the verdict | the pre-registration forbids the crossing; convention will not hold it |
| a published report → a later write | a recorded verdict must be extended, never re-rendered |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-18 | Elevation | representational read | mitigate | AST guard over `DESCRIPTIVE_ONLY_FUNCTIONS`: no `sign_test_exact`, no `holm`, no threshold `Compare`; dangling entries fail the guard |
| T-19-19 | Spoofing | a second verdict path | mitigate | AST guard: `erasure_succeeded` imported and called exactly once; the four v2.0 baselines forbidden as numeric literals in the driver |
| T-19-20 | Tampering | comparability with Phase 18 | mitigate | `assert_phase18_parity` over eight keys, each expected value imported from its owning module; corpus digest recomputed, never pasted |
| T-19-21 | Tampering | a published report | mitigate | `assert_erasure_report_not_clobbered` via `_verdict.recorded_verdict`; no `--force`; append-only through the committed `_addendum.append_addendum` |
| T-19-22 | Repudiation | a false Phase 18 provenance line | mitigate | both marker halves required keywords (already enforced by `_addendum.py`), plus a test that neither Phase 19 line mentions Phase 18 |
| T-19-SC | Tampering | package installs | mitigate | zero installs; `tests/test_package.py` sha256 pin holds |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q tests/test_phase19_erasure.py tests/test_phase19_docs.py tests/test_phase16_prereg.py tests/test_package.py`
- `git ls-files 'results/phase19_*'` still returns EMPTY.
</verification>

<success_criteria>
- Representational consistency cannot become a gate — proved by a guard, not promised by a comment.
- Exactly one verdict path exists, and it imports the committed rule.
- Every comparability parameter is asserted against Phase 18's own committed value.
- The report's marker pair names Phase 19 in both halves, and the clobber guard has no force flag.
</success_criteria>

<output>
Create `.planning/phases/19-selective-memory-erasure/19-05-SUMMARY.md` when done.
</output>
