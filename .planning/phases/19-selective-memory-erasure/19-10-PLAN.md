---
phase: 19-selective-memory-erasure
plan: 10
type: execute
wave: 9
depends_on: ["19-08"]
files_modified:
  - results/phase19_noise_floors.json
  - checkpoints/phase19_noise_seed_b_adapter.pt
autonomous: true
requirements: [ERASE-01, STAT-01, STAT-02, STAT-06]

must_haves:
  truths:
    - "An adapter-regime dialogue-PPL noise floor exists, measured under the pre-registered estimator"
    - "A per-fact recall noise floor exists for all seven non-targets, none pooled"
    - "Retention PPL of a LoRA-adapted model has been measured for the first time in this repository"
  artifacts:
    - path: "results/phase19_noise_floors.json"
      provides: "dialogue |ΔPPL| seed pair, per-fact |Δrate| for the seven non-targets, pre-erasure retention PPL"
      contains: "dialogue_ppl_noise_floor"
  key_links:
    - from: "results/phase19_noise_floors.json"
      to: "scripts/phase19_erasure.DIALOGUE_NOISE_FLOOR_ESTIMATOR"
      via: "the pre-registered seed pair"
      pattern: "DIALOGUE_NOISE_FLOOR_ESTIMATOR"
    - from: "results/phase19_noise_floors.json"
      to: "results/phase18_arm_adapter-on.json"
      via: "seed-stride replicate paired against committed per-fact rates"
      pattern: "phase18_arm_adapter-on"
---

<objective>
Measure the two noise floors `erasure_succeeded` requires and does not supply, plus the retention PPL
that has never existed — all under estimators pinned before this plan could see a number.

Purpose: `nontarget_noise_floor` must be "the noise floor measured in the SAME run"
(`scripts/erasure_gate.py:107-110`). `dialogue_ppl_noise_floor` is a required keyword with no
committed default (`:208`), and the only value in the repo — Δ_dialog = 0.001704
(`results/finetune_smoke_report.md:49-57`) — is from a full-fine-tune regime and is the wrong size
for the regime the number is read in. And `retention_perplexity` has four call sites, none on a
LoRA-adapted model (§Q4, verified by exhaustive grep), so the pre-erasure half of (c)'s retention
condition does not exist either.

This plan shares no files with 19-09 and does not depend on it; both sit in wave 9. On a single M3
they execute one after the other regardless.

Output: `results/phase19_noise_floors.json` with all three quantities and their provenance.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-selective-memory-erasure/19-CONTEXT.md
@.planning/phases/19-selective-memory-erasure/19-RESEARCH.md
@scripts/erasure_gate.py
@scripts/phase19_erasure.py
</context>

<interfaces>
Committed and unamendable: `DIALOGUE_NOISE_FLOOR_ESTIMATOR` (estimator, seed pair, arm config),
`dialogue_noise_floor()`, `dialogue_cap()`, `NONTARGET_NOISE_FLOOR_ESTIMATOR`, `nontarget_deltas()`,
`SEED_STRIDE_OFFSET`, `RETENTION_MEASUREMENT`, `assert_phase18_parity()`.

`scripts/phase14_recall.py:1383` `run_collapse_control(model, tok, device, forbid, values)` — the
on/off dialogue PPL pair in ONE process, `_prove(n_targets == n_targets_off)` at `:1442-1446`.
`src/personacore/evaluation/perplexity.py:83` `masked_perplexity` — THE frozen dialogue-val gate
metric; the training loop's 20-random-batch estimator is DISALLOWED for gates (`STATE.md:119`).
`:148` `retention_perplexity(model, val_bin_path, block_size, device, tokenizer, batch_size=32)` over
`data/retention_val.bin` (2,000,572 B = 1,000,286 `uint16` tokens on disk, ~3,900 windows at block
256); needs a live tokenizer for the dead-id mask.

**Pre-erasure per-fact rates are ALREADY COMMITTED** in `results/phase18_arm_adapter-on.json` at
A2/K=48 over the same corpus, so the (b) replicate is ONE run and not two: 216 core questions × 48
draws = 10,368 draws ≈ 60 min at the measured 172 draws/min
(`results/phase18_extraction_report.md:264` — 42,480 draws in 246.5 min).

Retention cap needs no measurement to compute — it is fully determined at
`3.891140 + 2 × 0.068930 = 4.029000` (`scripts/erasure_gate.py:246`). What does not exist is the
adapted model's retention PPL to compare against it.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Measure the adapter-regime dialogue-PPL noise floor (~3 min)</name>
  <files>checkpoints/phase19_noise_seed_b_adapter.pt, results/phase19_noise_floors.json</files>
  <action>
Retrain the production fact set at the SECOND seed named in `DIALOGUE_NOISE_FLOOR_ESTIMATOR`, every
other recipe constant identical (LR 3e-4, weight_decay 0.0, batch 8, 200 steps, warmup 20,
`REAL_RUN_SECOND_PERSON=False`, `REAL_RUN_REPLAY_RATIO=1.0`) — 81 s. Measure its masked dialogue-val
PPL through `run_collapse_control`'s adapter-ON arm.

Re-measure the seed-1337 adapter's PPL in the SAME process and assert agreement with the published
**5.8154** (`results/phase14_recall_report.md:462`, +27.16% over 270,203 scored targets) — a floor
computed across two processes with two loads is a different quantity than one computed within one,
and the assertion is what proves this is the former rather than assumes it. Expect the documented
WR-01 divergence of ~0.008% against the teaching run's unmasked-instrument twin
(`results/phase14_teaching_run.log:15` reads 4.5737 / 5.8176); allow that tolerance and no more.

`dialogue_ppl_noise_floor = |PPL_seed_a − PPL_seed_b|` via the committed `dialogue_noise_floor()`.
Record it beside `dialogue_cap(floor)`, the pre-erasure 5.8154, and the excess or headroom. Record
Δ_dialog = 0.001704 and its cap 4.576708 in the same block, so both readings are present before
either is read against a verdict, and the reader can see which one the gate takes.

Do NOT measure `dialogue_ppl` with the adapter off as a substitute: `adapter_disabled` is
bit-identical to the un-adapted base at max abs diff exactly 0.0 (`STATE.md:142`), so it trivially
passes while measuring nothing — the "green and blind" shape this project names as its recurring
defect (§Q4 option 3, refused).

Publish whichever way it lands. If the measured floor does not admit 5.8154 — it would need to reach
0.62105 — that is the finding §Q4 named in advance: the conversational cost was paid at TEACHING
time, already published as a named limitation (`results/phase14_recall_report.md:585`), and (c) as
written cannot discriminate an erasure at 13.9M parameters. Do not soften it and do not re-choose the
estimator.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import json; d=json.load(open('results/phase19_noise_floors.json')); f=d['dialogue_ppl_noise_floor']; assert f['value']>=0 and f['seed_a']!=f['seed_b'] and 'cap' in f and 'delta_dialog_reference' in f; print(f)"</automated>
  </verify>
  <done>The floor is measured from a genuine same-recipe seed pair in one process; the re-measured seed-1337 PPL agrees with the published 5.8154 inside the WR-01 tolerance; both the measured floor and Δ_dialog are recorded with their caps.</done>
</task>

<task type="auto">
  <name>Task 2: Measure the (b) per-fact recall noise floor (~60 min)</name>
  <files>results/phase19_noise_floors.json</files>
  <action>
Seed-stride replicate: re-score the UNERASED taught adapter (`checkpoints/persona_adapter.pt`) over
`results/phase18_corpus.json` at A2/K=48 with `SEED_STRIDE_OFFSET`, then compute per-fact `|Δrate|`
against the committed per-fact question-unit rates in `results/phase18_arm_adapter-on.json` through
the committed `nontarget_deltas()`.

Record per fact WITH its own denominator — never pooled. The rule takes `max(nontarget_deltas)`
(`erasure_gate.py:237`), so one destroyed fact fails (b), which is the intent: a pooled rate can hide
one destroyed fact behind seven intact ones (`ROADMAP.md:518-522`). Under STAT-06 nothing at n=8
becomes a p-value on this path.

Call `assert_phase18_parity` on the config before the first draw. Assert `SEED_STRIDE_OFFSET` does
not collide with Phase 18's `seed_index * K` stride (`scripts/phase18_extraction.py:3640`) — a
colliding offset re-reads the same generator states and reports a noise floor of exactly zero, which
would silently drive (b)'s margin to zero and turn any degradation whatsoever into a failure. A
measured zero after a proved non-collision is a finding; a measured zero from a collision is a bug,
and the assertion is what separates them.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import json; d=json.load(open('results/phase19_noise_floors.json')); r=d['nontarget_noise_floor']; assert len(r['per_fact'])==7, r['per_fact']; assert all('n_questions' in v for v in r['per_fact'].values()); print(r['value'])"</automated>
  </verify>
  <done>Seven per-fact deltas exist, each with its own denominator, none pooled; the stride non-collision is asserted; parity holds against Phase 18's committed config.</done>
</task>

<task type="auto">
  <name>Task 3: Measure retention PPL on a LoRA-adapted model — the first time in this repo</name>
  <files>results/phase19_noise_floors.json</files>
  <action>
Call `retention_perplexity(model, RETENTION_BIN, 256, device, tok)` on the taught adapter
(`checkpoints/persona_adapter.pt`, adapter ON) and again inside `adapter_disabled`, in the same
process. This is the PRE-erasure half of (c)'s retention condition and it does not exist today: four
call sites, none adapted (`build_retention_bin.py:145,151`; `finetune_dialog.py:206`;
`finetune_ab.py:238`; `finetune_smoke.py:299,335`).

Record both readings, the fully-determined cap 4.029000, and whether the pre-erasure adapted model
already sits above or below it. This mirrors the dialogue half exactly: a (c) failure that predates
the erasure and a (c) failure caused by it are different findings, and the only thing that separates
them is publishing both numbers (§Q4 response 4, mandatory under any of the four).

Record `n_windows` and the tokenizer's dead-id mask digest, so the number is reproducible rather than
merely reported. The retention wrapper's masking policy is FROZEN (DEBT-02) and is the only sanctioned
PPL for curve points — do not substitute the unmasked `perplexity`.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import json; d=json.load(open('results/phase19_noise_floors.json')); r=d['retention_ppl_pre_erasure']; assert r['adapter_on']>0 and r['adapter_off']>0 and r['cap']==4.029, r; print(r)"</automated>
  </verify>
  <done>Retention PPL of the adapted model exists for the first time, on and off, with the cap and the pre-erasure standing recorded; `n_windows` and the mask digest travel with it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a measured noise floor → the (b)/(c) margins | a floor chosen after seeing the cap is the failure `23a830c` exists to prevent |
| a seed stride → an apparent zero | a colliding stride reports zero variance and silently zeroes (b)'s margin |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-41 | Repudiation | `dialogue_ppl_noise_floor` | mitigate | estimator, seed pair and arm config committed at 19-04, before this plan could see a PPL; the ancestry guard proves the order |
| T-19-42 | Tampering | a zero (b) noise floor | mitigate | `SEED_STRIDE_OFFSET` non-collision with Phase 18's `seed_index * K` asserted before drawing; a collision-derived zero is a bug and is separated from a measured zero |
| T-19-43 | Tampering | a green-and-blind (c) | mitigate | the adapter-OFF dialogue substitution is refused in writing; the on/off pair is measured for context and the ON arm is what the gate reads |
| T-19-44 | Tampering | (b) pooling | mitigate | `nontarget_deltas` refuses anything but seven per-fact entries with their own denominators |
| T-19-45 | Spoofing | a bespoke PPL metric | mitigate | `masked_perplexity` and `retention_perplexity` are the frozen gate metrics; the in-loop estimator and the unmasked `perplexity` are both disallowed |
| T-19-SC | Tampering | package installs | mitigate | zero installs; `tests/test_package.py` sha256 pin holds |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q tests/test_phase16_prereg.py tests/test_phase19_erasure.py tests/test_package.py`
- `results/phase19_noise_floors.json` carries all three blocks with denominators; `grep -rn '0%'
  results/phase19_*` returns nothing (STAT-02).
- `git ls-files 'scripts/phase19_floor.py'` still returns EMPTY — the constants are measured but not
  locked.
</verification>

<success_criteria>
- The adapter-regime dialogue noise floor exists, measured under the pinned estimator, published
  whichever way it lands.
- Seven per-fact (b) deltas exist with their denominators, and the stride non-collision is proved.
- Retention PPL on a LoRA-adapted model exists — the measurement §Q4 found missing.
- No constant has been locked yet.
</success_criteria>

<output>
Create `.planning/phases/19-selective-memory-erasure/19-10-SUMMARY.md` when done.
</output>
