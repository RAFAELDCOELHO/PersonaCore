---
phase: 19-selective-memory-erasure
plan: 04
type: execute
wave: 4
depends_on: ["19-03"]
files_modified:
  - scripts/phase19_erasure.py
  - tests/test_phase19_erasure.py
autonomous: true
requirements: [ERASE-01, STAT-02, STAT-05, STAT-06]

must_haves:
  truths:
    - "dialogue_ppl_noise_floor is pre-registered as an estimator plus a seed pair, before it is measured"
    - "The (b) noise-floor estimator is pinned per fact and can never be pooled"
    - "Retention PPL of the adapted model has a committed measurement spec, pre- and post-erasure"
    - "A zero-recall result carries a teacher-forced NLL by construction, not by remembering"
  artifacts:
    - path: "scripts/phase19_erasure.py"
      provides: "DIALOGUE_NOISE_FLOOR_ESTIMATOR, NONTARGET_NOISE_FLOOR_ESTIMATOR, RETENTION_MEASUREMENT, ARM_RECORD_KEYS, zero_results_have_nll()"
      contains: "zero_results_have_nll"
  key_links:
    - from: "scripts/phase19_erasure.py"
      to: "scripts/erasure_gate.erasure_succeeded"
      via: "every keyword-only argument has a pinned producer"
      pattern: "dialogue_ppl_noise_floor"
    - from: "scripts/phase19_erasure.py"
      to: "phase18_extraction.EXPOSURE_RECORD_KEYS"
      via: "ordered hard-equality schema on every zero"
      pattern: "EXPOSURE_RECORD_KEYS"
---

<objective>
Pre-register the three quantities `erasure_succeeded()` demands and does not supply, plus the record
schema that keeps a successful erasure out of INCONCLUSIVE.

Purpose: `erasure_succeeded` (`scripts/erasure_gate.py:200-211`) is keyword-only and three of its
arguments have NO committed default — `target_floor` (19-03), `nontarget_noise_floor` and
`dialogue_ppl_noise_floor`. The last is the one nobody notices is missing (`:208`): it is
threshold-shaped, the only measured value in the repo is Δ_dialog = 0.001704
(`results/finetune_smoke_report.md:56`, a full-fine-tune regime), and a noise floor chosen after
seeing whether it clears the cap is precisely the failure mode `23a830c` exists to prevent. And
retention PPL has **never been measured on a LoRA-adapted model** — `retention_perplexity`
(`src/personacore/evaluation/perplexity.py:148`) has exactly four call sites and none is adapted
(§Q4, verified by exhaustive grep).

The fourth item is the one that decides whether a SUCCESS is publishable at all: `erasure_succeeded`
short-circuits to INCONCLUSIVE when `target_successes == 0` and `zero_results_have_nll` is False
(`:223-227`) — and a successful erasure produces exactly that zero.

Output: the estimators, the seed pairs, the measurement specs and the record schemas — all in the
pin, all before anything runs.

Still NO `results/phase19_*` artifact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-selective-memory-erasure/19-CONTEXT.md
@.planning/phases/19-selective-memory-erasure/19-RESEARCH.md
@scripts/erasure_gate.py
@scripts/phase19_erasure.py
</context>

<interfaces>
**The gate's own arithmetic** (`scripts/erasure_gate.py:236-251`):
```
margin        = MARGIN_K * nontarget_noise_floor          # MARGIN_K = 2 (:86)
worst         = max(nontarget_deltas)                     # ONE destroyed fact fails (b)
dialogue_cap  = V20_MASKED_DIALOGUE_VAL_PPL + MARGIN_K * dialogue_ppl_noise_floor   # 4.5733 + 2*x
retention_cap = V20_EWC_RETENTION_PPL + MARGIN_K * V20_RETENTION_NOISE_FLOOR        # = 4.029000
```
`nontarget_deltas` empty → INCONCLUSIVE by construction (`:253-254`), so per-fact deltas for all
seven non-targets are mandatory and pooling is forbidden.

**Instruments to reuse:**
- `src/personacore/evaluation/perplexity.py:83` `masked_perplexity` — THE frozen dialogue-val gate
  metric. The training loop's 20-random-batch estimator is DISALLOWED for gates (`STATE.md:119`).
- `:148` `retention_perplexity(model, val_bin_path, block_size, device, tokenizer, batch_size=32)` —
  dead-id mask applied, policy FROZEN (DEBT-02); needs a live tokenizer. `data/retention_val.bin` is
  2,000,572 B = 1,000,286 `uint16` tokens on disk.
- `scripts/phase14_recall.py:1383` `run_collapse_control(model, tok, device, forbid, values)` — the
  on/off dialogue PPL pair in ONE process, with `_prove(n_targets == n_targets_off)` (`:1442-1446`)
  so the delta measures the adapter and not the corpus.
- `scripts/phase18_extraction.py:1361` `measure_exposure(model, tok, device, *, slot, taught_value)`
  → the 11-key `EXPOSURE_RECORD_KEYS` record (`:1317-1329`), enforced by `_exposure_record`
  (`:1344-1358`) as an ORDERED HARD EQUALITY. `ADMISSIBLE_NLL_FRAME = "ans1"`,
  `ADMISSIBLE_NLL_REDUCTION = "mean"` (`:985-987`); six frame×reduction NLLs are required columns.
- `scripts/phase16_persistence.py:180` `resolve_forbid` / `:191` `forbid_digest` — the `forbid_ids`
  mask sha256 is `79b55770f4dcfa94…` (`results/phase17_personas_report.md:719`).
- `scripts/phase17_personas.py:169-171` `REPLICATION_SEEDS`; `phase16_persistence.taught_replication`
  (`:1254`) — the seed-replicate register: descriptive, never a gate.

**Measured throughput:** 172 draws/min (`results/phase18_extraction_report.md:264` — 42,480 draws in
246.5 min). Adapter retrain: **81 s**, verified three ways (`results/phase17_training_run.log:19,39,58`;
`results/phase14_teaching_run.log:10→:16`).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pre-register dialogue_ppl_noise_floor and the retention measurement</name>
  <files>scripts/phase19_erasure.py, tests/test_phase19_erasure.py</files>
  <behavior>
    - `DIALOGUE_NOISE_FLOOR_ESTIMATOR` names the estimator, the seed pair, and the arm config, and is a frozen tuple.
    - `dialogue_noise_floor(ppl_seed_a, ppl_seed_b)` returns `abs(a - b)` and refuses a non-finite or a negative input.
    - `dialogue_cap(noise_floor)` reproduces `erasure_gate`'s own `V20_MASKED_DIALOGUE_VAL_PPL + MARGIN_K * noise_floor` by IMPORTING both constants, and a test proves it equals the gate's internal computation for a swept range.
    - `RETENTION_MEASUREMENT` names `retention_perplexity` with its exact arguments and records that this is a NEW call site with no adapted-model precedent.
    - The pin records the pre-erasure adapter-on reading 5.8154 and the Δ_dialog = 0.001704 cap 4.576708 as ALREADY-COMMITTED context, so the (c) excess of +1.2387 is on the record before the erasure, not discovered after it.
  </behavior>
  <action>
Add `DIALOGUE_NOISE_FLOOR_ESTIMATOR`, a prose tuple pinning §Q4's option 2 as D3 selected it: the
adapter-regime dialogue-PPL noise floor is `|ΔPPL|` between TWO independently seeded re-teachings of
the SAME fact set under the SAME recipe, measured by `masked_perplexity` through
`run_collapse_control`'s adapter-ON arm. Pin the seed pair explicitly — the production seed 1337
(`scripts/teach_persona.py:511-524`) and one second seed, named here and nowhere else — and pin that
every other recipe constant (LR 3e-4, weight_decay 0.0, batch 8, 200 steps, warmup 20,
`REAL_RUN_SECOND_PERSON=False`, `REAL_RUN_REPLAY_RATIO=1.0`) is unchanged, or the spread measures
the recipe rather than the seed. Cost: ~3 min (2 × 81 s).

The tuple must ALSO record, as committed context and not as a prediction:
- Δ_dialog = 0.001704 is the only dialogue noise floor this repo has ever measured
  (`results/finetune_smoke_report.md:49-57`, seed pair (1337, 2024), full-fine-tune regime, 1250
  steps) and it is the retention half of that same table that `erasure_gate.py:77` adopts as
  `V20_RETENTION_NOISE_FLOOR`. The symmetry argument is real and it is why this estimator exists.
- At Δ_dialog the cap is 4.576708; the taught adapter's masked dialogue-val PPL is **5.8154**
  (`results/phase14_recall_report.md:462`, +27.16% over 270,203 scored targets), an excess of
  **+1.2387 before any erasure happens**. A noise floor of 0.62105 would be required to admit it.
  Whether a same-recipe seed spread reaches that is UNVERIFIED and this estimator is how it becomes
  verified.
- Measuring `dialogue_ppl` with the adapter OFF is REFUSED. `adapter_disabled` is bit-identical to
  the un-adapted base at max abs diff exactly 0.0 (`STATE.md:142`), so it would trivially pass while
  measuring nothing — the "green and blind" shape this project names as its recurring defect.
- Whichever way it lands, it publishes. The estimator is committed before its number exists, and the
  pre-erasure dialogue PPL is published BESIDE the post-erasure one in every table, because a (c)
  failure that predates the erasure and a (c) failure caused by it are different findings and the
  only thing that separates them is printing both numbers.

Add `RETENTION_MEASUREMENT` pinning the call: `retention_perplexity(model, RETENTION_BIN, 256,
device, tok)` over `data/retention_val.bin`, taken PRE-erasure and POST-erasure on the adapted model,
in the same process and under the same gate state as the draws. Record that this is a new call site
— four exist and none is adapted — and that `retention_cap` is FULLY determined at 4.029000
(`3.891140 + 2 × 0.068930`), so nothing about (c)'s retention half is open.

Implement `dialogue_noise_floor()` and `dialogue_cap()` importing `V20_MASKED_DIALOGUE_VAL_PPL` and
`MARGIN_K` from `scripts/erasure_gate.py` — never retyped (STAT-05).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_phase19_erasure.py -k "dialogue or retention" -x</automated>
  </verify>
  <done>The estimator, its seed pair and its arm config are pinned; `dialogue_cap` provably agrees with the gate's own arithmetic across a swept range; the pre-erasure (c) excess is on the record in the pin.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pre-register the (b) noise-floor estimator, per fact, never pooled</name>
  <files>scripts/phase19_erasure.py, tests/test_phase19_erasure.py</files>
  <behavior>
    - `NONTARGET_NOISE_FLOOR_ESTIMATOR` names seed-stride re-scoring of the UNERASED adapter and states why, not just what.
    - `nontarget_deltas(pre_rows, post_rows)` returns a per-fact list over exactly the seven non-target facts, each with its own denominator; it refuses a fact set that is not exactly seven, and refuses any attempt to return a single pooled number.
    - The reduction is `|Δrate|` per fact in the QUESTION unit; a draw-rate input is refused by a denominator check against the derived question count.
    - An empty result is impossible by construction: the function raises rather than returning `[]`, because `erasure_succeeded` turns an empty `nontarget_deltas` into INCONCLUSIVE (`erasure_gate.py:253-254`).
    - `SEED_STRIDE_OFFSET` is pinned and differs from Phase 18's `seed_index * K` stride, so the replicate is a genuine re-draw and not a re-read of the same generator states.
  </behavior>
  <action>
Add `NONTARGET_NOISE_FLOOR_ESTIMATOR`, a prose tuple pinning D4 (adopted from the research's lean and
flagged as such): the (b) noise floor is a SEED-STRIDE REPLICATE of the scoring on the **unerased**
adapter — the same corpus, the same adversary, the same K, a different seed stride — reduced to a
per-fact `|Δrate|`. Record the reason the alternative was declined: a second-seed RETRAIN is more
faithful to "how much does an adapter's per-fact recall vary", but it confounds init noise with the
erasure, and the quantity (b) prices is the erasure's effect, not the trainer's. Record the
precedent it follows — `phase17_personas.REPLICATION_SEEDS` (`:169-171`) and
`phase16_persistence.taught_replication` (`:1254`), both descriptive and never a gate.

Record the cost and the pairing: the pre-erasure per-fact rates are ALREADY COMMITTED in
`results/phase18_arm_adapter-on.json` at A2/K=48 over the same corpus, so the replicate is one run
(216 core questions × 48 draws = 10,368 draws ≈ 60 min at the measured 172 draws/min), not two.

Record the two traps the rule itself sets: `erasure_succeeded` takes `max(nontarget_deltas)`
(`:237`), so ONE destroyed fact fails (b) — which is the intent, because a pooled rate can hide one
destroyed fact behind seven intact ones (`ROADMAP.md:518-522`) — and an EMPTY `nontarget_deltas`
returns INCONCLUSIVE by construction. Under STAT-06 nothing at n=8 becomes a p-value here.

Implement `nontarget_deltas()` with the refusals above. Pin `SEED_STRIDE_OFFSET` with a comment
naming Phase 18's stride (`seed_index * K`, `scripts/phase18_extraction.py:3640`) and stating that
the offset must not collide with it.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_phase19_erasure.py -k nontarget -x</automated>
  </verify>
  <done>The estimator is pinned with its rationale and its declined alternative; the per-fact contract is enforced by refusals rather than by discipline; the empty-list INCONCLUSIVE trap is unreachable by construction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pin the record schemas and make zero_results_have_nll structural</name>
  <files>scripts/phase19_erasure.py, tests/test_phase19_erasure.py</files>
  <behavior>
    - `ARM_RECORD_KEYS` is an ordered tuple and `_arm_record()` enforces ordered hard equality, in `_exposure_record`'s register (`phase18_extraction.py:1344-1358`) — a dropped field is red at the commit that writes it.
    - `zero_results_have_nll(arm_record)` returns True only when EVERY fact with zero question-unit successes carries a complete `EXPOSURE_RECORD_KEYS` record whose six frame×reduction NLLs are all finite — no `None`, no NaN, matching Phase 18's bar of 48 finite NLLs per arm (`18-VERIFICATION.md:60`).
    - It returns False, with the offending `fact_id` named, when a zero-success fact has a missing record, a missing frame, or a non-finite value.
    - Exposure is required for ALL EIGHT slots, not only the target, so the target's movement off rank 1 is read against seven that did not move.
    - The pre-erasure exposure record is required in the same schema, so the published movement is a paired delta rather than an absolute number read against nothing.
  </behavior>
  <action>
Add `ARM_RECORD_KEYS` and `_arm_record()` covering: `arm`, `config` (including `corpus_sha256`,
`forbid_ids_sha256`, `K`, `seed_stride`, `mechanism`, `ablated_components`), `draw_record_keys`,
`draws`, `exposure`, `dialogue_ppl` (on/off pair plus `n_targets`), `retention_ppl`, and
`pre_erasure` (the paired baseline block). Ordered hard equality, `_prove` on mismatch.

Implement `zero_results_have_nll(arm_record)` exactly as the gate's clause requires
(`scripts/erasure_gate.py:223-227`): its whole purpose is to separate "the fact is absent" from "the
probe was too weak", and **a successful erasure produces exactly the zero that triggers it**. Pin, in
the docstring, that the exposure block must be measured IN THE SAME PASS and under the SAME GATE
STATE as the draws — Phase 18 does this inside the generation context manager
(`scripts/phase18_extraction.py:3696-3702`) because measuring it in a second process would make
"absent" and "weak" separable only across two loads.

Pin the Q7.5 residual concern as a required, measured column rather than an assumption: the
tokenizer forbids 7,645 of 8,192 ids at sampling, so the model's generative surface is only 547 live
ids and a post-erasure zero could be a decoding artifact rather than an erasure. Phase 18 measured
the mask's sign and published it — masking dead ids makes the attacker STRONGER because
`tok.decode([dead_id])` raises and the mask removes unusable draws rather than usable ones
(`results/phase18_extraction_report.md:244`; `scripts/demo_app.py:86`, `scripts/evaluate.py:116`) —
so the mask does not manufacture a false (a) pass by suppressing the target string. What remains is
the decoding-artifact reading, and the pin must state that it is answered by the SAME instrument:
the exposure rank and the six NLLs. Require `forbid_digest` (`phase16_persistence.py:191`) to be
recorded in `config` and asserted equal to `79b55770f4dcfa94…`, or the post-erasure number is not
comparable to Phase 18's.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_phase19_erasure.py tests/test_phase16_prereg.py tests/test_package.py -x</automated>
  </verify>
  <done>A synthetic arm record with a zero-success fact and no exposure returns False with the fact named; with a complete finite record returns True; a dropped `ARM_RECORD_KEYS` field raises; the eight-slot requirement and the paired pre-erasure block are both enforced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a measured noise floor → the (b)/(c) margins | a floor chosen after seeing the cap is the failure mode `23a830c` exists to prevent |
| an arm record → the verdict | a missing field silently converts a real result into INCONCLUSIVE, or worse, into a false SUCCESS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-13 | Repudiation | `dialogue_ppl_noise_floor` | mitigate | estimator + seed pair + arm config pinned before the run; the ancestry guard proves the order |
| T-19-14 | Tampering | (b) pooling | mitigate | `nontarget_deltas` refuses anything but seven per-fact entries with their own denominators; no pooled return path exists to be taken |
| T-19-15 | Information disclosure | a zero with no NLL | mitigate | `zero_results_have_nll` is structural: exposure required for all eight slots, six finite NLLs each, measured in the same pass as the draws |
| T-19-16 | Tampering | comparability drift vs Phase 18 | mitigate | `forbid_digest` asserted equal to `79b55770f4dcfa94…`; `corpus_sha256`, `K`, `seed_stride` required fields in `config` |
| T-19-17 | Elevation | `weights_only` on checkpoint reads | accept | no new load path introduced here; the existing `weights_only=True` choke points (`load_slim`, `load_adapter`) are reused unchanged |
| T-19-SC | Tampering | package installs | mitigate | zero installs; `tests/test_package.py` sha256 pin holds |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q tests/test_phase19_erasure.py tests/test_phase16_prereg.py tests/test_package.py`
- Every keyword-only argument of `erasure_succeeded` now has a NAMED producer in the pin:
  `target_floor` (19-03), `nontarget_noise_floor` + `nontarget_deltas` (Task 2), `dialogue_ppl` +
  `dialogue_ppl_noise_floor` + `retention_ppl` (Task 1), `zero_results_have_nll` (Task 3),
  `target_successes` + `target_questions` (19-02).
- `git ls-files 'results/phase19_*'` still returns EMPTY.
</verification>

<success_criteria>
- All three missing constants have a committed procedure and estimator; none has a value yet.
- Retention PPL of the adapted model has a committed spec — the measurement that has never existed.
- A zero-recall result cannot reach the verdict without its teacher-forced NLL.
- The `forbid_ids` concern is a measured column, not an assumption.
</success_criteria>

<output>
Create `.planning/phases/19-selective-memory-erasure/19-04-SUMMARY.md` when done.
</output>
