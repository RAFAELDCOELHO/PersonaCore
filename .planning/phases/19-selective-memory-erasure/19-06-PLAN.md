---
phase: 19-selective-memory-erasure
plan: 06
type: execute
wave: 6
depends_on: ["19-05"]
files_modified:
  - scripts/phase19_erasure.py
  - scripts/phase14_recall.py
  - tests/test_phase19_erasure.py
  - tests/test_phase14_scoring.py
  - tests/test_phase18_corpus.py
autonomous: true
requirements: [ERASE-01, ERASE-02, STAT-01, STAT-05]

must_haves:
  truths:
    - "Phase 19 has its own arm runner; scripts/phase18_extraction.py is imported and never edited"
    - "The M1 stopping rule is deterministic, ordinal, and pinned before the calibration runs"
    - "The M2 retrain arm is LOCKED_FACTS + SOFT_TIER_FACTS minus exactly one, everything else unchanged"
    - "A calibration corpus can be built over CALIBRATION_POOL with a derived denominator, and the fact to erase is chosen by a blind pinned rule"
    - "The bit-identity control takes an adapter path, so it can measure the erased adapter rather than the production one"
    - "The pin carries its own CLI, so no run plan needs to commit to it"
  artifacts:
    - path: "scripts/phase19_erasure.py"
      provides: "run_erasure_arm(), select_ablation_prefix(), ABLATION_STOP_RULE, retrain_arm_spec(), build_calibration_corpus(), reference_set_for_calibration()"
      contains: "ABLATION_STOP_RULE"
    - path: "tests/test_phase14_scoring.py"
      provides: "the Phase 19 DRAW_ALL_ASSERTED_BY entry"
      contains: "phase19_erasure"
  key_links:
    - from: "scripts/phase19_erasure.py"
      to: "scripts/phase18_extraction.py"
      via: "import only — the file is frozen by STAT-05"
      pattern: "import phase18_extraction"
    - from: "scripts/phase19_erasure.py"
      to: "scripts/teach_persona.arm_spec"
      via: "retrain_arm_spec drops exactly one fact"
      pattern: "LOCKED_FACTS"
---

<objective>
Build the Phase 19 run surface — the arm runner, the M1 stopping rule, the M2 retrain arm and the
calibration corpus builder — and close the pin.

Purpose: `scripts/phase18_extraction.py` is FROZEN. Verified this session: its last commit is
`99716e0` (2026-08-16 11:55:09) and the earliest `results/phase18_*` first-add is `2d7151e`
(2026-08-16 11:57:58), so ANY new commit to it fails
`test_phase18_prereg_is_frozen_before_every_phase18_result` immediately. `run_arm` cannot be widened,
not even additively (P19-1). Phase 19 imports its instruments and writes its own runner.

The M1 stopping rule is the mechanism's PARAMETERS, which D1 requires pinned before calibration "so
the mechanism cannot become a knob swapped after a disappointing floor". It must be deterministic
and must not invent a threshold — so it is ORDINAL: stop at the smallest prefix that moves the target
off rank 1 in its same-slot reference set, using the already-committed `exposure_rank`.

Output: the run surface, complete. After this plan the pin is finished and 19-07 gates it.

Still NO `results/phase19_*` artifact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-selective-memory-erasure/19-CONTEXT.md
@.planning/phases/19-selective-memory-erasure/19-RESEARCH.md
@scripts/erasure_gate.py
@scripts/phase19_erasure.py
</context>

<interfaces>
**IMPORT ONLY from `scripts/phase18_extraction.py`** (frozen):
`span_nll_from_ids` (`:1050`) `(model, context_ids, value_ids, device) -> {n_scored, nll_sum, nll_mean}`,
asserts `n_scored == len(value_ids)` (`:1097-1102`) ·
`value_span_nll` (`:1110`) `(model, tok, device, *, slot, value, frame)`, `frame ∈ ("ans1",
"f4_reversed", "f3_bare")` (`:981`) — `value` is a PARAMETER, never a lookup (`:1117`), so it works
on calibration facts ·
`exposure_rank` (`:1230`) `(nll_by_candidate, *, taught_value, reduction, length_spread)`, pure,
CPU-testable, `log2(|R|) − log2(rank)`, ties broken on the candidate string (`:1235-1236`) ·
`measure_exposure` (`:1361`) · `reference_set_for` (`:1159`) — **RAISES on a slot carrying no taught
fact** (`:1185-1190`), so a calibration slot needs a Phase 19 twin ·
`apply_a1` (`:474`), `build_a3_prompt` (`:545`), `injection_budget` (`:565`), `split_value_ids`
(`:594`), `build_a2_prompt` (`:640`), `realized_injection` (`:656`) — A2 budget
`INJECTION_FRACTION = 0.25` (`:117`), realized multiset `[1,1,1,1,1,1,2,2]` (`:3761`) ·
`canonical_json` (`:746`), `corpus_sha256` (`:758`), `_corpus_entry` (`:769`) ·
`score_records` (`:1919`), `asr_ladder` (`:2095`), `cumulative_by_attempt` (`:2165`),
`aggregate_questions` (`:2223`), `_handoff_counts` (`:2782`).

**Widenable shared statistics:** `scripts/phase16_persistence.py` is NOT frozen — Phase 18 widened it
at `1161593` (2026-08-15) AFTER `results/phase16_persistence_report.md` landed (`e9b921a`,
2026-08-14). It is the D-16 "import, widen, never copy" surface.

**Teaching / retrain:** `scripts/teach_persona.py:405-422` `arm_spec` returns
`(facts, second_person, replay_ratio)`; the `real` arm returns `fs.LOCKED_FACTS + fs.SOFT_TIER_FACTS`
at `REAL_RUN_SECOND_PERSON=False`, `REAL_RUN_REPLAY_RATIO=1.0` (`:417-421`).
`render_episodes(facts, family_ids)` (`:247`) and `build_arm_bins(..., seed=, prefix=)` (`:425-461`)
already take the fact list as a parameter — Phase 17 threaded `seed=`/`prefix=` additively for
exactly this class of reuse (`STATE.md:201`). Base checkpoint `checkpoints/convbase_best.pt`
(`:90`), provably untouched by teaching — the canary raises if any frozen base parameter moves
(`:689-699`). `refuse_if_exists` guards all five training outputs before a token is written
(`:560-562`).

**Fact pools** (verified this session by import):
`phase14_factset.CALIBRATION_POOL` = 10 core facts across the same 8 slots, disjoint from
`CANDIDATE_POOL` by construction (`:98-113`), gate-cleared by the SAME guessability gate.
`render_family(family_id, fact)` (`:756`) works on ANY fact — verified on `cal_person_varek`.
`TAUGHT_FAMILY_IDS = {F1,F2,F4,F5,F6}`, `HELDOUT_FAMILY_IDS = {F3,F7,F8}` (`:816-817`).
**VERIFIED CONSTRAINT:** `RESERVED_HELDOUT_PROBES` is keyed by `cand_*` only — calibration facts have
NO reserved probes, so a calibration fact's question count is strictly less than 27 and must be
DERIVED per fact, never assumed. The self-naming filter (`contains_value(question, fact.value)`,
`scripts/phase14_recall.py:858-861`) drops further questions and its exclusions must be recorded with
their family id, never silently.

**Draw-site guard:** `tests/test_phase14_scoring.py:579` `DRAW_ALL_ASSERTED_BY` — every `draw_all`
call site must assert something in-prompt, in place or via a NAMED indirection whose asserter must
exist; a DANGLING entry fails the guard rather than excusing its site.
`phase14_recall.assert_no_value_in_prompt` (`:398`) ANDs a substring check and a contiguous-id-run
check (`:428-438`); `prompt_ids=` is the D-03 widening that checks the ids actually dispatched.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The Phase 19 arm runner and the M1 stopping rule</name>
  <files>scripts/phase19_erasure.py, tests/test_phase19_erasure.py, tests/test_phase14_scoring.py</files>
  <behavior>
    - `ABLATION_STOP_RULE` is a prose tuple; `select_ablation_prefix()` implements it and is deterministic given a fixed model and adapter.
    - Single-component contribution scores are computed for all 288 addresses by ablate-one-and-re-score against the target's `ans1`/mean value-span NLL; ties in the score break on the `(layer, projection, j)` address so the ordering is total.
    - `select_ablation_prefix` stops at the smallest prefix k for which `exposure_rank` of the target value within its same-slot reference set is no longer 1, and returns `(k, ordered_addresses, curve_rows)`.
    - The hard cap is `len(component_index())`, DERIVED; reaching it without leaving rank 1 returns the cap with `stopped = False` recorded, never an exception and never a silently truncated search.
    - `run_erasure_arm()` writes an `ARM_RECORD_KEYS`-shaped record, calls `assert_phase18_parity` on its own config before drawing, and measures exposure + dialogue PPL + retention PPL in the SAME pass and gate state as the draws.
    - Every `draw_all` call site in the runner asserts in-prompt, and its `DRAW_ALL_ASSERTED_BY` entry (if it uses a named indirection) resolves to a real asserting function.
  </behavior>
  <action>
Pin `ABLATION_STOP_RULE`, stating: components are ordered by their single-component contribution to
the TARGET's teacher-forced `ans1`/mean value-span NLL — the same instrument the verdict's
`zero_results_have_nll` clause depends on, which is what makes the selection signal and the evidence
one measurement rather than two. The stopping condition is ORDINAL and therefore invents no
threshold: stop at the smallest prefix that moves the target off **rank 1** in its same-slot
reference set, read by the committed `exposure_rank`. A rank change is a fact about an ordering; a
"NLL above X" rule would be a Phase 19 threshold and the pre-registration forbids one. State that the
cap is the full component index and that reaching it means ΔW is zeroed entirely — the adapter-off
endpoint — which is a legitimate, publishable outcome under D8 and is recorded as `stopped = False`
rather than dressed up.

Implement `select_ablation_prefix()` and `run_erasure_arm()`. The runner REUSES
`results/phase18_corpus.json` verbatim for target arms (digest asserted by `assert_phase18_parity`),
takes the corpus path as a parameter so the calibration corpus can be passed instead, and refuses to
overwrite an existing arm record with no force flag — the register of
`phase18_extraction.py:3536-3542` and `teach_persona.refuse_if_exists` (`:560-562`).

Record the collateral curve at every prefix in `CURVE_CHECKPOINTS = (1,2,4,8,16,32,64,128,cap)`
intersected with `[1, k]`: target rank + `ans1`/mean, all eight slots' rank + `ans1`/mean, and
dialogue PPL. This is Q7.3's mandatory measurement — 331,776 parameters carry ten facts plus whatever
conversational adaptation survives, the 288 components are shared across all of them, and there is no
reason to expect fact-localised structure at this capacity. Do NOT assume localisation. The curve is
publishable whichever shape it has, and if it is a cliff, the cliff IS the finding.

Add the Phase 19 entry to `DRAW_ALL_ASSERTED_BY` (`tests/test_phase14_scoring.py:579`) if and only if
the runner uses a named indirection; prefer asserting in place, which needs no entry.

**B2 — widen the bit-identity control additively, BEFORE the pin closes.**
`run_bit_identity_control(tok, questions)` (`scripts/phase14_recall.py:1480`) takes NO adapter
parameter: it loads `ADAPTER_PATH` (`:84`), the hard-coded production
`checkpoints/persona_adapter.pt`. Called as-is at 19-12 it would compare the PRODUCTION adapter
against the base and return max abs diff 0.0 no matter what M1 did to the erased artifact — the
control passing while measuring the wrong object, which is the green-and-blind shape this project
names as its recurring defect, sitting on the one measurement the demo's entire ON/OFF claim rests on.

`scripts/phase14_recall.py` is under NO ancestry guard (only `phase17_personas.py`,
`phase18_extraction.py` and `erasure_gate.py` are pinned in `tests/test_phase16_prereg.py`), so an
additive widening is legal here exactly as Phase 17 threaded `seed=`/`prefix=` through
`build_arm_bins` (`STATE.md:201`). Thread `adapter_path=None` defaulting to `ADAPTER_PATH`, in
`load_adapted_model`'s own register (`:513`, already parameterised for precisely this reason). The
default keeps every existing call site byte-identical in behaviour, and
`tests/test_lora_inject.py:262` `INJECT_LORA_CONSUMERS` — a hard-equality allowlist of
`(file, function)` pairs — still matches, because neither the file nor the function name moves.

Do this in THIS plan, before 19-07. Discovering it at 19-12 would mean editing a driver after the
target artifacts exist, and the only clean recovery would be a Phase 19 twin nobody planned for.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_phase19_erasure.py -k "ablation or arm" tests/test_phase14_scoring.py -x</automated>
  </verify>
  <done>`select_ablation_prefix` is deterministic on a toy model across two runs; the cap branch returns `stopped = False` rather than raising; the arm record refuses to clobber; the draw-site guard is green with no dangling entry.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The M2 retrain arm (ERASE-02) and its caveat</name>
  <files>scripts/phase19_erasure.py, tests/test_phase19_erasure.py</files>
  <behavior>
    - `retrain_arm_spec(target_fact_id)` returns `(facts, second_person, replay_ratio)` where `facts` is `LOCKED_FACTS + SOFT_TIER_FACTS` minus EXACTLY the target, `len(facts) == len(real_facts) - 1`, and `second_person`/`replay_ratio` equal the `real` arm's values byte-for-byte.
    - Passing a `fact_id` not in the real arm raises rather than returning the full set unchanged.
    - A test asserts that every other recipe constant the arm consumes — LR, weight_decay, batch, steps, warmup, seed, base checkpoint path — is read from `teach_persona`'s own module constants and not redeclared in the driver.
  </behavior>
  <action>
Implement `retrain_arm_spec`. ~15 lines: `arm_spec`'s fact tuple minus one, with a distinct `prefix=`
so `build_arm_bins` writes its own bins and cannot collide with the production ones. Everything else
identical, or the comparison measures the recipe rather than the omission.

Pin `ERASE_02_REFERENCE_ARM` as a prose tuple recording all three of what M2 is and is not:
- It is the closest available operationalisation of "what would the model look like if it had never
  learned this fact" — and reporting the surgical result as *indistinguishable from* it would violate
  the goal framing, which explicitly refuses that claim (`scripts/erasure_gate.py:33-36`). It stays
  DESCRIPTIVE, a reference point beside three others: the taught adapter
  (`checkpoints/persona_adapter.pt`, the pre-erasure state), the M1 erased adapter, and Phase 18's
  measured adapter-off floor of 0/104 (`results/phase18_extraction_report.md:41-44`), which is what
  "never learned it" actually reads.
- It is simultaneously a legitimate MECHANISM at 81 s — verified three ways
  (`results/phase17_training_run.log:19,39,58`; `results/phase14_teaching_run.log:10→:16`, a window
  that also contains two full `masked_perplexity` sweeps) — which is why ERASE-02 calls it a genuine
  option rather than an aspiration (`REQUIREMENTS.md:171-173`).
- Its one real weakness, carried rather than buried: a retrain is a DIFFERENT adapter, not an edited
  one. `seed_everything(seed)` owns the data order (`teach_persona.py:605-610`), and dropping one
  fact changes the episode count and therefore the batch composition at every step, so its non-target
  recall differs from the taught adapter's by seed and data-order noise as well as by the omission.
  Two adapters at two seeds would bound that noise; one does not. This is precisely why (b)'s noise
  floor (19-04 Task 2) must exist before this arm can be interpreted at all.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_phase19_erasure.py -k retrain -x</automated>
  </verify>
  <done>The arm spec drops exactly one fact and nothing else; an unknown `fact_id` raises; no recipe constant is redeclared in the driver.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The calibration corpus builder and the reference-set twin</name>
  <files>scripts/phase19_erasure.py, tests/test_phase19_erasure.py</files>
  <behavior>
    - `build_calibration_corpus(fact)` produces `_corpus_entry`-shaped rows for one `CALIBRATION_POOL` fact over `TAUGHT_FAMILY_IDS + HELDOUT_FAMILY_IDS` via `render_family`, applying the self-naming `contains_value` filter and RETURNING the exclusions with their family ids.
    - Its denominator is DERIVED per fact and recorded; the builder never assumes 27 and `_prove`s that at least one taught and one held-out question survive the filter.
    - The corpus is digested through the imported `canonical_json` + `corpus_sha256`, so a Phase 19 corpus is digested the same way as Phase 18's.
    - `reference_set_for_calibration(slot, fact)` returns a same-slot candidate set with the same `6 <= |R| <= 8` proof as `reference_set_for` (`phase18_extraction.py:1210-1215`), assembled from the committed pools, and RAISES outside that range.
    - The A1/A2/A3 prompts built over calibration facts pass `assert_no_value_in_prompt` at both the string and the contiguous-id-run level.
  </behavior>
  <action>
Implement both. `reference_set_for` RAISES when the slot carries no taught fact (`:1185-1190`), and a
calibration slot is exactly that case — so the twin is unavoidable and cannot be a widening of the
frozen Phase 18 function. ~20 lines, same shape, same proof, importing the same pools
(`CALIBRATION_POOL`, `REGISTER_ARM_POOL` `:117-124`, `GATE_REJECTED_CANDIDATES` `:429-444`, and
Phase 17's 24 minted values, gate-cleared 24/24 over 416 completions, `STATE.md:222`).

Pin `CALIBRATION_COMMENSURABILITY` as a prose constant: the floor caps a number produced by the A2
adversary at K=48, so the CALIBRATION fact must be scored by the SAME adversary at the SAME budget
(§Q2's hard constraint, P19-4). What may differ is the DENOMINATOR — calibration facts carry no
`RESERVED_HELDOUT_PROBES` entry (verified: that dict is keyed `cand_*` only), so their question count
is strictly below 27. That is admissible precisely because `lock_erasure_floor` consumes a RATE, not
an upper bound (19-03 Task 1), and the calibration denominator is published beside the rate.

**B4 — pin the calibration fact's selection rule, blind.** Add
`CALIBRATION_TARGET_SELECTION_RULE`: the calibration fact that gets erased is the FIRST ELIGIBLE
member of `phase14_factset.CALIBRATION_POOL` in its committed order (`:102-113`), where eligible
means the fact survives `build_calibration_corpus`' self-naming filter with at least one taught and
one held-out question; tie-break — impossible by construction since the order is total — is the
lexicographically smallest `fact_id`, stated in the same commit anyway so the rule is complete on its
face.

The determinism is CONDITIONAL and the pinned text must say so rather than imply otherwise: this
rule is deterministic **given this pin**, not given the repo as it stood before it. "First eligible"
depends on `build_calibration_corpus`' question-family set, which is committed in THIS file and did
not exist beforehand; the filter itself is mechanical and long-committed (`contains_value`,
`teach_persona.py:1031`), so no judgment enters, but the selected fact's identity is not derivable
from the pre-pin repo alone. Record that qualification inside
`CALIBRATION_TARGET_SELECTION_RULE` itself. An unqualified "deterministic" is a sentence that does
not survive checking, and after 19-07 it cannot be corrected — the same failure mode W1 and
W-new-2 were caught for. User decision, 2026-08-17; see `19-CONTEXT.md` § B4.

`FLOOR_CEILING` stays **0.20**, inherited from Phase 14's `THRESHOLD_FLOOR`, not reconsidered (user
decision, 2026-08-17). Record its known consequence beside it: `lock_erasure_floor` saturates for
every `cal_rate >= 0.3333`, so above that the blind calibration stops discriminating and every rate
yields the identical floor. Phase 14's calibration arm measured 0.4143 taught / 0.2506 held-out
(`results/phase14_recall_report.md:20-21`), so a similar single-fact rate lands the floor at 0.20 —
the MORE PERMISSIVE end of the 0.091079–0.20 range. Accepted deliberately and recorded before the
number exists, so it can never be presented afterwards as a surprise or as a design win.

It must be BLIND in the strong sense: it may read NO Phase 18 per-fact recall and NO Phase 19
calibration result. D7's principle is the register — a rule uses the known past, never a result still
to come — and here the "known past" is the committed pool order itself, fixed since Phase 14. The
target fact's own rule (19-02) reads committed Phase 18 numbers because those cannot move; the
calibration fact's cannot read anything at all, because its own number is precisely what the floor is
derived from and choosing the fact after seeing candidate rates is the manoeuvre `23a830c` exists to
forbid. The pool is disjoint from `CANDIDATE_POOL` by construction (`:98-101`), so the target can
never be selected here.

The stake is not cosmetic: the floor exceeds `ERASURE_FLOOR_MIN` only when `cal_rate > 0.1518`, so an
unpinned choice among ten facts swings (a)'s bar between 0.0911 and 0.20 — a 2.2x move in the
criterion, selectable after the fact.

Record why a fresh calibration adapter is retrained rather than reusing
`checkpoints/phase14_cal_first_person_replay_adapter.pt` (D6, adopted from the research's lean): the
committed adapter was trained under the `real` arm's replay configuration and its recall was measured
by `score_items`, not by the Phase 18 adversary. Reuse costs an argument about instrument and recipe
provenance; a fresh 81 s retrain costs 81 seconds (P19-9).

**B7 — commit the invocation surface, in the pin, now.** Every run plan from 19-08 onward says "run
the committed X()". Without an entry point the natural executor move is to add a subcommand to
`scripts/phase19_erasure.py` after the first artifact exists, which reddens the ancestry guard
permanently and destroys the phase's central claim. So pin `main(argv)` plus
`if __name__ == "__main__":` with EVERY subcommand named up front — `cal-corpus`, `cal-train`,
`cal-erase`, `noise-floors`, `erase`, `retrain`, `representational`, `report` — including the ones
whose plans have not run yet. Naming a subcommand costs nothing now and costs the guard later.

State the rule in the module docstring: **invocation is `python scripts/phase19_erasure.py
<subcommand>` and nothing else.** No plan from 19-08 onward may add a subcommand, a flag, or any other
line to this file. If a run genuinely needs something the pinned CLI cannot express, the answer is an
UNPINNED throwaway (`python -c`, or a new `scripts/phase19_run.py`) — never a commit here.

**W3 — add the Phase 19 driver to the network scan.** `test_no_network_imports` lives in
`tests/test_phase18_corpus.py` (verified). Extend its scanned file set to cover
`scripts/phase19_erasure.py`, discharging `T-19-28` where the test actually is rather than where the
threat register guessed.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_phase19_erasure.py tests/test_phase14_scoring.py tests/test_phase18_corpus.py tests/test_phase16_prereg.py tests/test_package.py -x</automated>
  </verify>
  <done>A calibration corpus builds for at least one `CALIBRATION_POOL` fact with a derived, non-zero denominator in both tiers; the calibration-fact rule is pinned and blind; the CLI names every subcommand the phase will ever call; the Phase 19 driver is inside `test_no_network_imports`' scan; `make test` is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a fact value → a scored prompt | a value leaking into the prompt makes recall unmeasurable |
| the frozen Phase 18 driver → Phase 19 | import is free; a single additive commit reddens a committed guard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-23 | Repudiation | `scripts/phase18_extraction.py` | mitigate | import only; `test_phase18_prereg_is_frozen_before_every_phase18_result` stays green because Phase 19 makes zero commits to it |
| T-19-24 | Information disclosure | calibration prompts | mitigate | `assert_no_value_in_prompt` at both the string and the contiguous-id-run level (`phase14_recall.py:398,428-438`), with `prompt_ids=` on the ids actually dispatched |
| T-19-25 | Tampering | the mechanism's parameters | mitigate | `ABLATION_STOP_RULE` ordinal and pinned before calibration; the cap is derived from `component_index()`, not typed |
| T-19-26 | Tampering | an existing arm record or bins | mitigate | refuse-if-exists on every written output, no force flag; a distinct `prefix=` keeps M2's bins off production paths |
| T-19-27 | Information disclosure | personal data in a public artifact | accept | T-14-05: every value is invented; everything under `results/` ships publicly (`phase14_factset.py:38-39`) |
| T-19-28 | Information disclosure | network egress during a measurement | mitigate | ATK-01 forbids external APIs; Task 3 adds `scripts/phase19_erasure.py` to `test_no_network_imports`' scanned file set, which lives in `tests/test_phase18_corpus.py` (verified) and is in this plan's `files_modified` |
| T-19-SC | Tampering | package installs | mitigate | zero installs; `tests/test_package.py` sha256 pin holds |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q tests/test_phase19_erasure.py tests/test_phase14_scoring.py tests/test_phase16_prereg.py tests/test_package.py`
- `make test` — full suite green.
- `git log --format=%H -- scripts/phase18_extraction.py | wc -l` UNCHANGED from its pre-Phase-19 value.
- `git ls-files 'results/phase19_*'` still returns EMPTY. **The pin is now complete.**
</verification>

<success_criteria>
- Phase 19 has its own arm runner, its own corpus builder and its own reference-set twin; the frozen
  Phase 18 driver has zero new commits.
- The M1 stopping rule is deterministic, ordinal and pinned; the cap is derived.
- The M2 arm drops exactly one fact and carries its own caveat in writing (ERASE-02).
- Every `erasure_succeeded` keyword now has a committed producer, and the pin is closed.
</success_criteria>

<output>
Create `.planning/phases/19-selective-memory-erasure/19-06-SUMMARY.md` when done.
</output>
