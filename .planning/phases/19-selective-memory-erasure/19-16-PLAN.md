---
phase: 19-selective-memory-erasure
plan: 16
type: execute
wave: 14
depends_on: ["19-15"]
files_modified:
  - results/phase19_erasure_report.md
  - docs/REPORT.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: false
requirements: [ERASE-01, ERASE-02]

must_haves:
  truths:
    - "The literal (c) FAILURE and its root-cause diagnosis are published side by side; neither replaces the other"
    - "The continuation is appended by the existing scripts/_addendum.py, not by a new writer"
    - "23a830c is unamended; the correction is dated and beside it"
    - "The ship decision is recorded by a human, and the placeholder rewrite is conditional on the addendum containing it"
  artifacts:
    - path: "results/phase19_erasure_report.md"
      provides: "the dated continuation diagnosing (c)'s root cause, appended after the recorded verdict"
      contains: "Dated continuation"
  key_links:
    - from: "scripts/_addendum.append_addendum"
      to: "results/phase19_erasure_report.md"
      via: "pending/recorded marker PAIR, both required keywords"
      pattern: "append_addendum"
---

<objective>
Publish the D3 diagnosis beside the literal verdict, record the ship decision, and close the phase.

Purpose: condition (c) as pre-registered caps against `V20_MASKED_DIALOGUE_VAL_PPL` = 4.5733, the
**adapter-OFF** baseline — while the correct comparison for post-erasure capability preservation is
against the **adapter-present** baseline, the one Phase 14 already measured and kept descriptive
(5.8154, +27.16%, `COLLAPSE_PPL_TRIGGER` tripped, no gate;
`results/phase14_recall_report.md:462,585`). D3 locks the response: the literal FAILURE against the
criterion as written is published, a dated continuation diagnosing the root cause is added BESIDE it,
and both readings publish side by side with neither replacing the other. `23a830c` is not amended —
permanently out of scope.

The append mechanism is **already built and proved**: `scripts/_addendum.py` plus
`tests/test_phase19_docs.py`, committed `f8441ec`, RED→GREEN over three mutations against the
committed bytes. Do NOT re-derive it, do not duplicate it, do not write a fourth twin.

Output: the continuation, the recorded ship decision, and the phase closed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-selective-memory-erasure/19-CONTEXT.md
@scripts/erasure_gate.py
@scripts/_addendum.py
@scripts/_verdict.py
@.planning/REQUIREMENTS.md
@.planning/STATE.md
</context>

<interfaces>
```python
append_addendum(path, addendum, *, pending, recorded)      # scripts/_addendum.py:56
```
BOTH marker halves are REQUIRED keywords. That pairing is the entire reason the module exists:
`phase18_extraction.append_addendum` parameterised the placeholder it REPLACES but hard-coded the line
it replaces it WITH, so appending to a Phase 19 document through it injects
`**Phase 18 ship decision: recorded in the dated continuation at the end of this file.**` — a false
provenance claim from a helper whose whole purpose is to not alter published text
(`scripts/_addendum.py:13-27`). Both Phase 18 twins are inside ancestry-frozen files and could not be
fixed in place; deleting-and-re-adding to get around that would launder the very ordering those
guards prove (`:6-12`).

The placeholder must occur EXACTLY once (`:63-77`): zero means the file is not the shape this writer
was committed against, more than one means choosing between them, and choosing is how an append
silently becomes a rewrite. `SystemExit` either way. No override flag, no rewrite path.

Phase 18's W2, the defect to not repeat (`18-VERIFICATION.md:226-250`): the placeholder rewrite ran
UNCONDITIONALLY, silently converting "not yet recorded" into "recorded in the dated continuation" when
no decision had been written. The Phase 19 rewrite must be CONDITIONAL on the addendum actually
containing the thing it claims.

`scripts/_verdict.py:27` `recorded_verdict(text)` — the ONE anchored `## Verdict` read; `None` and an
empty body are deliberately different.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Append the dated (c) diagnosis beside the literal verdict</name>
  <files>results/phase19_erasure_report.md</files>
  <action>
Compose the continuation and append it through the EXISTING `_addendum.append_addendum`, passing the
Phase 19 `pending`/`recorded` pair committed at 19-05. Do not write a new appender.

The continuation must say, with its numbers:
- What (c) as written compares against: `V20_MASKED_DIALOGUE_VAL_PPL` = 4.5733, the Phase 12
  production fine-tune's **adapter-OFF** dialogue val PPL (`results/finetune_prod.csv` final row,
  step 4000, `scripts/erasure_gate.py:75`).
- What the correct comparison for post-erasure capability preservation is: the **adapter-present**
  baseline, 5.8154, +27.16% over 270,203 scored targets, which Phase 14 already measured, published
  as a named limitation, and deliberately kept DESCRIPTIVE — `COLLAPSE_PPL_TRIGGER` tripped and no
  gate fired (`results/phase14_recall_report.md:462,585`).
- The arithmetic consequence, stated without softening: run through the committed gate, a PERFECT
  erasure — 0 successes, zero non-target degradation, retention comfortably under cap — returns
  FAILURE on (c) alone, for a reason that predates the erasure by an entire phase. The conversational
  cost was paid at TEACHING time, not at erasure time.
- What that is a finding ABOUT: at 13.9M parameters with a 331,776-parameter adapter, (c) as written
  cannot discriminate an erasure. That is a real result about LoRA at this capacity, not an
  embarrassment and not a defect in the erasure.
- What is explicitly NOT being claimed: that the rule was wrong to be written that way, or that it
  should be amended. `23a830c` was committed before any v3.0 number existed and that is what makes
  every number in this milestone worth anything. The literal verdict stands as published; this
  continuation sits beside it.
- Both readings, side by side, in one table: the literal (c) against 4.5733 + 2×floor, and the
  diagnostic (c) against 5.8154 + 2×floor, with the pre- and post-erasure dialogue PPL in both rows.
  Label which one the committed gate read. Neither replaces the other.

Date the section. Assert afterwards, on the PRODUCED BYTES: the pre-existing text is unchanged except
for the single replaced placeholder line; the `## Verdict` section body read by
`_verdict.recorded_verdict` is byte-identical to before the append; and the continuation contains the
diagnosis it claims to contain.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_phase19_docs.py -x</automated>
  </verify>
  <done>The continuation is appended by the existing writer with the Phase 19 marker pair; the verdict body is byte-identical before and after; both (c) readings appear side by side with the gate's choice labelled; no line mentions Phase 18's ship decision.</done>
</task>

<task type="auto">
  <name>Task 2: Continue docs/REPORT.md and mark the requirements</name>
  <files>docs/REPORT.md, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <action>
Append a dated additive continuation to `docs/REPORT.md` — the register 18-12 used — carrying the
Phase 19 headline in one paragraph: the verdict, the collateral curve's shape, and the (c) diagnosis,
unsoftened. If the curve was a cliff, the sentence is *"selective erasure is not selective at 331,776
parameters"* and it ships as written under `D8_PUBLICATION_POSTURE`.

Mark ERASE-01 and ERASE-02 complete in `.planning/REQUIREMENTS.md`, each with the artifact that
discharges it — ERASE-02 by the RUN (`results/phase19_arm_m2.json`,
`results/phase19_m2_training.log`), never by the written explanation the ROADMAP allows as the
alternative. Update the Traceability table.

Update `.planning/STATE.md` — position, plan count, and the phase's key decisions, via
`gsd-sdk query state.update` where it applies.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_phase19_docs.py tests/test_phase16_prereg.py tests/test_package.py -x</automated>
  </verify>
  <done>`docs/REPORT.md` carries a dated additive continuation with nothing rewritten above it; ERASE-01 and ERASE-02 are marked with their discharging artifacts; STATE.md reflects the closed phase.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Record the ship decision</name>
  <action>
PAUSE. Present <what-built> and walk the operator through <how-to-verify> item by item. Do not proceed, do not commit anything further, and do not begin the next plan until the <resume-signal> is given.
  </action>
  <what-built>
`results/phase19_erasure_report.md` with the committed verdict and the dated (c) diagnosis beside it;
`docs/REPORT.md`'s dated continuation; ERASE-01 and ERASE-02 marked.
  </what-built>
  <how-to-verify>
1. Read the `## Verdict` section. It is the committed rule's return value, verbatim. Confirm you are
   shipping it unsoftened — including if it is INCONCLUSIVE, which is a real outcome and not a failure
   to reach one.
2. Read the dated continuation. Confirm it diagnoses (c)'s root cause WITHOUT amending `23a830c`, and
   that both readings sit side by side with the gate's choice labelled.
3. Confirm the headline matches the committed `D8_PUBLICATION_POSTURE` branch you selected at 19-12,
   and that no mitigating adjective was added afterwards.
4. Record the ship decision. The placeholder rewrite from pending to recorded must happen ONLY if you
   actually write one — Phase 18's W2 was a rewrite that ran unconditionally and converted "not yet
   recorded" into "recorded" with nothing behind it.
5. Confirm `git log --format=%H -- scripts/erasure_gate.py` is unchanged: `23a830c` is still the only
   commit, unamended, and every Phase 19 number descends from it.
  </how-to-verify>
  <resume-signal>Type "approved" with the ship decision text, or describe what to change before it is recorded.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a published verdict → a later append | the append must extend and never rewrite |
| a marker pair → provenance | half a pair traveling with the caller writes another phase's sentence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-71 | Repudiation | amending `23a830c` | mitigate | the correction is a dated continuation beside the original; `git log -- scripts/erasure_gate.py` asserted unchanged |
| T-19-72 | Tampering | the recorded verdict | mitigate | `append_addendum` replaces exactly one placeholder and asserts on the produced bytes; the `## Verdict` body is compared byte-identical before and after |
| T-19-73 | Repudiation | a false "recorded" claim | mitigate | the placeholder rewrite is CONDITIONAL on the addendum containing what it claims — Phase 18's W2, closed rather than repeated |
| T-19-74 | Repudiation | a false Phase 18 provenance line | mitigate | both marker halves are required keywords in `scripts/_addendum.py`; the 19-05 test asserts neither Phase 19 line mentions Phase 18 |
| T-19-75 | Repudiation | ERASE-02 discharged by prose | mitigate | marked complete against the run artifacts, not against the written-explanation alternative |
| T-19-SC | Tampering | package installs | mitigate | zero installs; `tests/test_package.py` sha256 pin holds |
</threat_model>

<verification>
- `make test` — full suite green.
- `git log --format=%H -- scripts/erasure_gate.py | wc -l` equals 1 (`23a830c`).
- `git log --format=%H -- scripts/phase18_extraction.py | wc -l` unchanged from its pre-Phase-19 value.
- `results/phase19_erasure_report.md` contains exactly one `## Verdict` and its body is unchanged from
  19-15.
</verification>

<success_criteria>
- The literal (c) FAILURE and its root-cause diagnosis publish side by side; neither replaces the
  other.
- The continuation was written by the existing `scripts/_addendum.py`, with both marker halves.
- `23a830c` is unamended and `scripts/phase18_extraction.py` has no new commits.
- ERASE-01 and ERASE-02 are discharged by artifacts, and the ship decision is recorded by a human.
</success_criteria>

<output>
Create `.planning/phases/19-selective-memory-erasure/19-16-SUMMARY.md` when done.
</output>
