---
phase: 19-selective-memory-erasure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phase19_erasure.py
  - tests/test_phase16_prereg.py
  - tests/test_phase19_erasure.py
autonomous: true
requirements: [ERASE-01, STAT-05]

must_haves:
  truths:
    - "A Phase 19 ancestry guard exists and watches results/phase19_* before any such artifact exists"
    - "Zeroing a rank-1 ΔW component leaves the adapter artifact loadable through the unchanged audits"
    - "The adapter-off bit-identity control is unaffected by the ablation operator"
  artifacts:
    - path: "scripts/phase19_erasure.py"
      provides: "the PIN: ordering contract, M1 mechanism identity, the 288-address component index, the ablation operator"
      contains: "MECHANISM_ID"
    - path: "tests/test_phase16_prereg.py"
      provides: "test_phase19_prereg_is_frozen_before_every_phase19_result + results/phase19_* in V3_ARTIFACT_GLOBS"
      contains: "results/phase19_*"
    - path: "tests/test_phase19_erasure.py"
      provides: "CPU-only proofs for the ablation operator's representability"
  key_links:
    - from: "tests/test_phase16_prereg.py"
      to: "scripts/phase19_erasure.py"
      via: "git log --format=%H -- PHASE19_PREREG_ARTIFACT"
      pattern: "PHASE19_PREREG_ARTIFACT"
    - from: "scripts/phase19_erasure.py"
      to: "src/personacore/lora/inject.load_adapter_weights"
      via: "post-ablation artifact round-trip"
      pattern: "load_adapter_weights"
---

<objective>
Open the Phase 19 pre-registration file and arm the ancestry guard that makes it unamendable, in the
same commit — then prove the one mechanism fact the pin depends on: that a rank-1 ΔW component can
be zeroed without leaving the rank-8 artifact format.

Purpose: `scripts/phase19_erasure.py` must be an ancestor of every `results/phase19_*` first-add
(§Q6, `tests/test_phase16_prereg.py:231-312` is the twin to copy). Arming the guard first means
every later pin commit is watched from the start rather than retro-fitted. The ablation-operator
proof is research assumption **A5** (`19-RESEARCH.md:943`) — "structurally true but **not run**" —
and it decides whether M1 is representable at all, so it must settle before the mechanism is pinned.

Output: the pin file with its ordering contract, `MECHANISM_ID`, the derived 288-address component
index and `ablate_components`; the Phase 19 ancestry twin; the CPU-only representability proofs.

NO `results/phase19_*` artifact may be committed by this plan or by plans 19-02..19-07. That
ordering IS the scientific guarantee of this phase, not bookkeeping.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-selective-memory-erasure/19-CONTEXT.md
@.planning/phases/19-selective-memory-erasure/19-RESEARCH.md
@scripts/erasure_gate.py
@tests/test_phase16_prereg.py
@src/personacore/lora/layer.py
@src/personacore/lora/inject.py
</context>

<interfaces>
Extracted from the repository — the executor should use these directly, not go looking.

`src/personacore/lora/layer.py:21-42` — `LoRALinear` computes `y = base(x) + scale · (x @ A^T @ B^T)`,
i.e. `ΔW = scale · (B @ A)` with `B: (out, r)`, `A: (r, in)`, `scale = alpha/r` fixed at `__init__`
(`:27`, named the single source of truth for alpha/r; recomputing it is forbidden — PITFALLS P3).

`src/personacore/lora/config.py:23-24` — production `r=8, alpha=16.0`.
`results/phase14_teaching_run.log:12` — 36 wrapped projections, 331,776 trainable parameters.
36 × r=8 = **288 rank-1 components**, each addressable by `(layer, projection, j)`.

`src/personacore/lora/inject.py:76-131` — `load_adapter_weights` audits keys, shapes and **scale**.
`src/personacore/checkpoint.py:196` — `export_adapter`; `:223` — `load_adapter`.
`scripts/extract_deltas.py:174` — `adapter_cells(adapter, scale, w0_model)` reads `scale` FROM the
artifact and folds `ΔW = scale * (b @ a)`; its `KEYS` tuple is the committed (layer, projection, key)
enumeration to reuse rather than re-derive.
`scripts/phase14_recall.py:1480` — `run_bit_identity_control`, measured max abs diff exactly 0.0
(`STATE.md:142`).

`tests/test_phase16_prereg.py:54` — `V3_ARTIFACT_GLOBS = ("results/phase16_*", "results/phase17_*",
"results/phase18_*")`; its own comment: "a new phase writing results under a fourth prefix must be
added here". `:231-312` — `test_phase18_prereg_is_frozen_before_every_phase18_result`, the twin,
including the shallow-clone assert, the `prereg_commits` non-empty assert, the `--diff-filter=A`
earliest-add selection, and the `bool(checked) == bool(tracked_artifacts)` non-vacuity tie.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Open the pin and arm its ancestry guard in one commit</name>
  <files>scripts/phase19_erasure.py, tests/test_phase16_prereg.py</files>
  <action>
Create `scripts/phase19_erasure.py` with, in order: (1) a module docstring stating the ORDERING
CONTRACT verbatim — this file is the Phase 19 pre-registration, every commit touching it must be an
ancestor of every `results/phase19_*` first-add, and no Phase 19 result artifact may be committed
until the pin is complete at plan 19-07; (2) the `_prove(condition, message)` helper raising
`SystemExit`, in `phase18_extraction`/`_addendum` register (never a strippable `assert`, per
`phase14_recall.py:221-224`); (3) `MECHANISM_ID = "M1-rank1-component-ablation"` and
`MECHANISM_RULE`, a prose tuple naming M1 as the primary mechanism and M2 (retrain-without) as the
ERASE-02 reference arm, and recording that M3/M4/M5/M6 are DECLINED per D1 — not deferred; (4) a
`component_index()` that DERIVES the 288 `(layer, projection, j)` addresses by importing
`extract_deltas.KEYS` and the committed `r` from `lora/config.py`, with a `_prove` that
`len(index) == len(KEYS) * r` — never a typed 288; (5) `ablate_components(adapter, components)`
returning a NEW state dict with `lora_B[:, j]` and `lora_A[j, :]` zeroed for each addressed
component, leaving every key, shape and `scale` untouched.

Do NOT add the floor rule, the target fact, the denominators, the estimators or the report text —
those are plans 19-02..19-05. This plan opens the file and arms the guard.

In `tests/test_phase16_prereg.py`: append `"results/phase19_*"` to `V3_ARTIFACT_GLOBS` (`:54`); add
`PHASE19_PREREG_ARTIFACT = "scripts/phase19_erasure.py"`; add
`test_phase19_prereg_is_frozen_before_every_phase19_result` as a line-for-line twin of
`test_phase18_prereg_is_frozen_before_every_phase18_result` (`:231-312`) over
`artifact_glob = "results/phase19_*"`, carrying all four of its protections unchanged: the
`artifact_glob in V3_ARTIFACT_GLOBS` cross-check, the shallow-clone assert, the non-empty
`prereg_commits` assert, the `--diff-filter=A` earliest-add selection, and the
`bool(checked) == bool(tracked_artifacts)` non-vacuity tie. Its docstring must state that it is
vacuous today by construction (zero tracked Phase 19 artifacts) and that the non-vacuity tie is what
stops that surviving the artifacts' arrival — the recorded-state discipline of the Phase 18 twin.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_phase16_prereg.py -x</automated>
  </verify>
  <done>`git log --format=%H -- scripts/phase19_erasure.py` returns exactly one commit; the new twin passes with `checked == 0` and `tracked_artifacts == []`; `results/phase19_*` is in `V3_ARTIFACT_GLOBS`; `python scripts/phase19_erasure.py` (or an import) runs the module-scope proofs without raising.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove the ablation operator is exactly representable (A5)</name>
  <files>tests/test_phase19_erasure.py</files>
  <behavior>
    - `component_index()` returns exactly `len(extract_deltas.KEYS) * r` distinct addresses, and every address is `(layer, projection, j)` with `0 <= j < r`.
    - Ablating any subset leaves the returned state dict's key set, every tensor shape, and the recorded `scale`/`alpha` byte-identical to the input's.
    - After `export_adapter` → `load_adapter_weights`, an ablated artifact is accepted with no key, shape or scale complaint.
    - Ablating the full 288 addresses yields `ΔW == 0` everywhere; the resulting model's outputs equal `adapter_disabled` outputs to max abs diff exactly 0.0, so the bit-identity control is untouched.
    - `ablate_components` does not mutate its input adapter (the caller's pre-erasure state survives for the paired delta).
  </behavior>
  <action>
Create `tests/test_phase19_erasure.py`. CPU-only, GPU-free — build a toy `LoRAConfig` model via the
existing injection path rather than loading `checkpoints/persona_adapter.pt`, so the suite stays
under `make test`'s budget. Prove each behaviour above. For the ΔW==0 case, compare
`scale * (B @ A)` per `(layer, projection)` against a zero tensor with `torch.equal`, and assert the
forward-pass equality against `adapter_disabled` at max abs diff exactly 0.0 — the same bar
`run_bit_identity_control` (`scripts/phase14_recall.py:1480`) already meets on real weights
(`STATE.md:142`), because M1 stays inside the rank-8 decomposition specifically so this holds
(`19-CONTEXT.md` Specific Ideas).

Watch the shape audit RED once by deliberately zeroing only `lora_B[:, j]` and leaving `lora_A[j,:]`
non-zero, confirm the ΔW==0 assertion fails, then restore byte-identically — the standing practice
(`STATE.md:157,203`). Record the mutation and the restoration in the plan SUMMARY.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_phase19_erasure.py tests/test_phase16_prereg.py tests/test_package.py -x</automated>
  </verify>
  <done>All behaviours pass on CPU with no checkpoint load; A5 is settled by measurement rather than by argument; `make test` is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git history → the ordering claim | the only evidence that the pin preceded the numbers |
| adapter artifact → the loader | a malformed post-ablation artifact must be refused, not silently accepted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Repudiation | `scripts/phase19_erasure.py` | mitigate | `test_phase19_prereg_is_frozen_before_every_phase19_result` — every commit touching the pin asserted an ancestor of every artifact's earliest add via `git merge-base --is-ancestor`; no skip path, no force flag |
| T-19-02 | Tampering | `tests/test_phase16_prereg.py` glob list | mitigate | the twin asserts `"results/phase19_*" in V3_ARTIFACT_GLOBS`, so this test and that tuple cannot drift into naming two different path sets (the Phase 18 twin's own protection) |
| T-19-03 | Tampering | the ablated adapter artifact | mitigate | round-trip through `export_adapter`/`load_adapter_weights` with its key + shape + **scale** audits unchanged (`inject.py:76-131`) |
| T-19-04 | Elevation | checkpoint load path | accept | no new load site — CPU toy model only; the `weights_only=True` choke points are untouched |
| T-19-SC | Tampering | package installs | mitigate | Phase 19 installs ZERO packages; `tests/test_package.py` pins `pyproject.toml` by sha256 read as bytes (STAT-04, `STATE.md:173`). No `[ASSUMED]`/`[SUS]` package exists to gate |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q tests/test_phase19_erasure.py tests/test_phase16_prereg.py tests/test_package.py`
- `git ls-files 'results/phase19_*'` returns EMPTY — the pin must precede every artifact and this
  plan must not create one.
- `git log --format=%H -- scripts/phase19_erasure.py | wc -l` equals the number of commits this plan
  made to it, all before any Phase 19 artifact.
</verification>

<success_criteria>
- The pin file exists, carries the ordering contract, `MECHANISM_ID`, the derived component index and
  `ablate_components`, and holds no floor constant, no target name and no measured number.
- The Phase 19 ancestry twin exists, passes, and is non-vacuously tied to the tracked artifact set.
- A5 is proved: rank-1 ablation is exactly representable and the bit-identity control survives.
- `results/phase19_*` is still empty.
</success_criteria>

<output>
Create `.planning/phases/19-selective-memory-erasure/19-01-SUMMARY.md` when done.
</output>
