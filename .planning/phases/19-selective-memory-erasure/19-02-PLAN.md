---
phase: 19-selective-memory-erasure
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - scripts/phase19_erasure.py
  - tests/test_phase19_erasure.py
autonomous: true
requirements: [ERASE-01, STAT-01, STAT-05]

must_haves:
  truths:
    - "The target fact is named in the pin, chosen by a deterministic rule over already-committed Phase 18 data"
    - "The rule's tie-break is specified in the same commit as the rule"
    - "n=27 is derived from the committed fixture, never typed"
  artifacts:
    - path: "scripts/phase19_erasure.py"
      provides: "TARGET_SELECTION_RULE, select_target_fact(), TARGET_FACT_ID, TARGET_RANKING, N_TARGET_QUESTIONS"
      contains: "TARGET_SELECTION_RULE"
  key_links:
    - from: "scripts/phase19_erasure.py"
      to: "results/phase18_arm_adapter-on.json"
      via: "select_target_fact over committed draws"
      pattern: "phase18_arm_adapter-on"
    - from: "scripts/phase19_erasure.py"
      to: "results/phase16_recall_sample.json"
      via: "derived per-fact question count"
      pattern: "phase16_recall_sample"
---

<objective>
Pin the target fact and pin the denominator — both DERIVED from already-committed data, neither
typed as a literal.

Purpose: D7 locks the target as "the fact with the highest recall measured in Phase 18, among the 8
core gated slots, by a deterministic rule pinned BEFORE any calibration or erasure runs, with a
tie-break specified in the same commit". D5 locks the (a) denominator at n=27 — held-out (13) plus
taught (14) per fact, pooled. Both must land in the pin now, because after the calibration runs
neither can be honestly chosen.

Output: `TARGET_SELECTION_RULE`, `select_target_fact()`, the derived `TARGET_FACT_ID`, the full
published `TARGET_RANKING`, and `N_TARGET_QUESTIONS` derived from the binding fixture.

Still NO `results/phase19_*` artifact. The ranking is published as a module constant proved by a
test against the committed Phase 18 arm record — writing it to `results/` before the pin is complete
would break the ancestry guard armed in 19-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-selective-memory-erasure/19-CONTEXT.md
@.planning/phases/19-selective-memory-erasure/19-RESEARCH.md
@scripts/erasure_gate.py
@scripts/phase19_erasure.py
</context>

<interfaces>
`scripts/phase18_extraction.py` is **FROZEN** (§Q3; verified this session: last commit `99716e0`
2026-08-16 11:55:09, earliest artifact add `2d7151e` 2026-08-16 11:57:58 — any new commit to it
reddens `tests/test_phase16_prereg.py:231`). IMPORT ONLY.

- `.score_records(records, values)` (`:1919`) — `values` is a `{fact_id: value}` PARAMETER, never an
  import; a `fact_id` with no supplied value aborts rather than scoring every draw a miss. A2 is
  scored on `prefix_text + completion`, every other family on the completion alone.
- `.aggregate_questions(scored, *, tier)` (`:2223`) — per-fact rows in the QUESTION unit;
  `rate = n_answerable / n_questions`, `draw_rate` kept under a name that says which unit it is in.
  Hard-`_prove`s a single tier and refuses duplicate `(fact_id, seed_index)` pairs.
- `._handoff_counts(question_counts, per_fact_by_family, family, arm)` (`:2782`) — the register for
  proving a denominator against a DERIVED quantity, never a literal.
- `.best_attack_family(question_counts)` (`:2808`), `BEST_ATTACK_RULE` (`:2767`) — A2 was selected
  (`results/phase18_extraction_report.md:240`).

Committed inputs, read-only:
- `results/phase18_arm_adapter-on.json` — `{arm, config, draw_record_keys, draws, exposure}`;
  `draw_record_keys = [family, dose, fact_id, slot, tier, arm, seed_index, prefix_text, completions]`;
  976 draw records (verified this session).
- `results/phase16_recall_sample.json` — the binding 270-question fixture: `core_taught` 112,
  `core_held_out` 104, `soft` 54; row keys `seed_index, fact_id, question, reserved`; 13 held-out and
  14 taught per core fact (`scripts/phase16_persistence.py:290`, re-derived in §Q2).
- `results/phase18_extraction_report.md:141-152` — the committed `adapter-on` exposure table: every
  one of the eight slots at **rank 1**, exposure = ceiling, `ans1`/mean from 0.1337 (`pet_name`) to
  3.1255 (`hometown`).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin the target-selection rule, its tie-break, and the derived target</name>
  <files>scripts/phase19_erasure.py, tests/test_phase19_erasure.py</files>
  <behavior>
    - `select_target_fact(per_fact_rows, exposure)` returns a single `fact_id` and is a pure function of already-committed inputs.
    - Re-running it on `results/phase18_arm_adapter-on.json` reproduces the pinned `TARGET_FACT_ID` exactly.
    - `TARGET_RANKING` holds all eight core facts with their `(successes, n_questions, rate)`, so the choice is checkable rather than assertable.
    - A synthetic two-way tie on rate resolves by the first tie-break; a synthetic tie on BOTH resolves by the second; a synthetic three-way tie is still deterministic.
    - The rule refuses a rows set that does not carry exactly the eight core gated facts.
  </behavior>
  <action>
Add to `scripts/phase19_erasure.py`:

`TARGET_SELECTION_RULE` — a prose tuple, in `ERASURE_DECISION_RULE`'s register, stating: the target
is the core gated fact with the HIGHEST question-unit recall measured by Phase 18's best attack
family (A2, selected by the committed `BEST_ATTACK_RULE`) at K=48 on the `core_held_out` tier, read
off `results/phase18_arm_adapter-on.json` — data already committed on 2026-08-17, the known past,
which is the only reason this is pre-registrable at all (D7). Tie-break 1: LOWEST published
`ans1`/mean value-span NLL in the same arm record's exposure block (most exposed wins — the fact the
adversary holds most tightly is the hardest erasure and therefore the honest target). Tie-break 2:
lexicographically smallest `fact_id`. State in the same tuple that choosing the target after seeing
a calibration result is forbidden, and that the rule was committed before the calibration adapter
existed.

`select_target_fact(per_fact_rows, exposure)` — pure, stdlib-plus-imports only. Build
`per_fact_rows` by importing `phase18_extraction.score_records` and `.aggregate_questions` and
running them over the committed arm record's `draws` filtered to `family == "A2"`, `tier ==
"core_held_out"`. `_prove` the row set is exactly the eight core gated `fact_id`s and that every row
carries `n_questions == 13` (the fixture's per-fact held-out count) — a denominator proved against
the aggregation the sign test consumed, never typed, in `_handoff_counts`' register.

`TARGET_FACT_ID` and `TARGET_RANKING` — the derived results, written as module constants with a
provenance comment naming `results/phase18_arm_adapter-on.json` and its first-add SHA `9a923d6`
(2026-08-17 11:19:38, verified this session), the `CALIBRATION_SHA` pattern from
`scripts/phase14_recall.py:191-197`.
Do NOT hand-type the ranking from the report. Derive it, print it, paste the derived values, and let
the test below be what keeps them honest.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_phase19_erasure.py -k target -x</automated>
  </verify>
  <done>`TARGET_FACT_ID` equals `select_target_fact()`'s output on the committed arm record; `TARGET_RANKING` lists all eight facts with successes/denominator/rate; all three tie-break behaviours pass on synthetic input; the rule text names the tie-break in the same commit as the rule.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Derive n=27 from the fixture and pin it as the (a) denominator</name>
  <files>scripts/phase19_erasure.py, tests/test_phase19_erasure.py</files>
  <behavior>
    - `target_question_counts(fixture)` returns `{"core_taught": 14, "core_held_out": 13, "pooled": 27}` for the pinned target, computed from `results/phase16_recall_sample.json` rows.
    - `N_TARGET_QUESTIONS == 27` and equals the pooled count, with no literal 27 anywhere on the derivation path.
    - A fixture with a duplicated `(fact_id, seed_index)` row raises rather than inflating the count.
    - `wilson_upper_bound(0, N_TARGET_QUESTIONS)` is computed by the COMMITTED `erasure_gate` function, never re-derived.
  </behavior>
  <action>
Add `DENOMINATOR_RULE` — a prose tuple recording D5 verbatim in effect: the (a) denominator is the
target fact's taught AND held-out questions POOLED, n=27, because the pre-registration's unit is the
question and both tiers ask questions about the same fact
(`scripts/erasure_gate.py:52,101-106,139-158`). It must also record what is being declared: Phase 18
kept the tiers separate for its own Holm pricing, and pooling them here is a stated departure, not a
silent one. It must record the arithmetic that forced the choice — at n=13 the best attainable
Wilson upper bound is 0.172267, so nothing below that is clearable at ANY outcome including a
perfect erasure; at n=27 it is 0.091079 (§Q2's denominator table, computed with the committed
`wilson_upper_bound`). And it must record that n=52/108 is REFUSED: four phrasings of one question
are clustered exactly the way nine draws of one question are, and inflating n by 4 reintroduces the
modelling error STAT-01 exists to forbid, one level up.

`target_question_counts(fixture_path)` — count fixture rows per tier for `TARGET_FACT_ID`, `_prove`
no duplicate `(fact_id, seed_index)`, return the three counts. `N_TARGET_QUESTIONS` is the pooled
value, assigned from the function's output rather than typed.

Import `wilson_upper_bound` from `scripts/erasure_gate.py` — never re-implement it, and never write
a Wald interval (`erasure_gate.py:144-149`: Wald degenerates to `[0,0]` at zero successes, the exact
case (a) cares about most).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_phase19_erasure.py -k "denominator or target" tests/test_phase16_prereg.py -x</automated>
  </verify>
  <done>`N_TARGET_QUESTIONS` is 27, derived; the duplicate-row refusal is proved by a synthetic fixture; `grep -n "27" scripts/phase19_erasure.py` shows no literal on the derivation path (prose in `DENOMINATOR_RULE` is fine and expected).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed Phase 18 artifact → the target choice | the only thing that makes the choice pre-registrable is that its input cannot move |
| committed fixture → the denominator | a draw count arriving here divides the same numerator by 9× the denominator |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-05 | Repudiation | `TARGET_FACT_ID` | mitigate | derived by a committed pure function from a committed artifact, with the artifact's first-add SHA `9a923d6` recorded; the test re-derives on every run so a hand-edited constant goes red |
| T-19-06 | Tampering | the (a) denominator | mitigate | `target_question_counts` derives it from the fixture and `_prove`s no duplicate `(fact_id, seed_index)`; `N_TARGET_QUESTIONS` is assigned, never typed (P19-5) |
| T-19-07 | Information disclosure | fact values on the import surface | mitigate | the LAZY-IMPORT RULE — no fact strings at module import; `values` reaches `score_records` as a parameter (`phase18_extraction.py:1919`), guarded by `tests/test_phase14_scoring.py:367` |
| T-19-08 | Spoofing | a re-derived Wilson bound | mitigate | `wilson_upper_bound` IMPORTED from `scripts/erasure_gate.py:139`, never retyped (STAT-05) |
| T-19-SC | Tampering | package installs | mitigate | zero installs; `tests/test_package.py` sha256 pin holds |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q tests/test_phase19_erasure.py tests/test_phase16_prereg.py tests/test_package.py`
- `git ls-files 'results/phase19_*'` still returns EMPTY.
</verification>

<success_criteria>
- The target fact is named in the pin, with the rule AND the tie-break in the same commit.
- The full eight-fact ranking is published so a reader can check the choice rather than trust it.
- n=27 is derived from the committed fixture and the pooling departure is declared, not implied.
- No calibration, no erasure and no Phase 19 artifact has run or been written.
</success_criteria>

<output>
Create `.planning/phases/19-selective-memory-erasure/19-02-SUMMARY.md` when done.
</output>
