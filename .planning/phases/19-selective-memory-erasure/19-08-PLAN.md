---
phase: 19-selective-memory-erasure
plan: 08
type: execute
wave: 8
depends_on: ["19-07"]
files_modified:
  - results/phase19_cal_corpus.json
  - checkpoints/phase19_cal_adapter.pt
  - results/phase19_cal_training.log
autonomous: true
requirements: [ERASE-01, STAT-01, STAT-05]

must_haves:
  truths:
    - "A calibration adapter exists, trained on CALIBRATION_POOL under Phase 19's own recipe"
    - "A calibration corpus exists, digested the same way Phase 18's was"
    - "The pin is proved to precede both, by the ancestry guard rather than by intention"
  artifacts:
    - path: "results/phase19_cal_corpus.json"
      provides: "A1/A2/A3 prompts over the calibration fact set, with derived per-fact denominators"
      contains: "corpus_sha256"
    - path: "checkpoints/phase19_cal_adapter.pt"
      provides: "the blind calibration adapter, 331,776 parameters, r=8"
  key_links:
    - from: "results/phase19_cal_corpus.json"
      to: "scripts/phase19_erasure.build_calibration_corpus"
      via: "the committed builder"
      pattern: "phase19_erasure"
    - from: "tests/test_phase16_prereg.py"
      to: "results/phase19_cal_corpus.json"
      via: "the ancestry guard stops being vacuous here"
      pattern: "results/phase19_\\*"
---

<objective>
Teach the blind calibration adapter and build its corpus — the first two `results/phase19_*`
artifacts, and the moment the Phase 19 ancestry guard stops being vacuous.

Purpose: this is the `0425fdc` analogue in Phase 14's verified chain (§Q2, step 2). The floor's
inputs must come from a fact set DISJOINT from the target, measured by the same adversary at the same
budget, under a recipe whose provenance is not borrowed. D6 (research lean, adopted) retrains rather
than reusing `checkpoints/phase14_cal_first_person_replay_adapter.pt`: 81 seconds, and it removes an
argument about instrument and recipe provenance that would otherwise have to be litigated in the
report (P19-9).

Output: `results/phase19_cal_corpus.json`, `checkpoints/phase19_cal_adapter.pt`, and the training log.
No erasure and no scoring yet — those are 19-09, and keeping them in a separate commit is what makes
"the corpus was built before the calibration was scored" checkable rather than asserted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-selective-memory-erasure/19-CONTEXT.md
@.planning/phases/19-selective-memory-erasure/19-RESEARCH.md
@scripts/phase19_erasure.py
</context>

<interfaces>
Everything this plan calls was committed in 19-01..19-06 and is unamendable from here:
`build_calibration_corpus(fact)`, `reference_set_for_calibration(slot, fact)`,
`CALIBRATION_COMMENSURABILITY`, and the corpus digest path
(`phase18_extraction.canonical_json` `:746` + `.corpus_sha256` `:758`, imported).

Training path: `scripts/teach_persona.py` — `arm_spec` (`:405-422`), `render_episodes(facts,
family_ids)` (`:247`), `build_arm_bins(..., seed=, prefix=)` (`:425-461`), base
`checkpoints/convbase_best.pt` (`:90`, fingerprint `git_sha=04e724c6…, step=4000,
val_loss=1.5235939979553224`, `results/phase14_teaching_run.log:16`), the frozen-base canary
(`:689-699`), `refuse_if_exists` (`:560-562`), `seed_everything` (`:605-610`).
Measured cost: **81 s** per adapter, verified three ways.

Device: `RuntimeConfig` resolves CUDA-P100 → MPS → CPU via `preflight_device`; on this M3 it lands on
MPS, fp32, no AMP, no `GradScaler`, no `torch.compile` (`CLAUDE.md`).
`provenance.git_sha()` records the commit into the checkpoint (QA-02).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Build and commit the calibration corpus</name>
  <files>results/phase19_cal_corpus.json</files>
  <action>
Run the committed `build_calibration_corpus` over `phase14_factset.CALIBRATION_POOL`. Emit the
corpus with its `corpus_sha256` computed through the imported `canonical_json`, its per-fact DERIVED
denominators per tier, and the self-naming exclusions listed with their family ids — every dropped
question is recorded with its family, never silently (`scripts/phase14_recall.py:840-861`).

Record in the artifact, as required fields: the calibration fact chosen for erasure and the rule that
chose it (it must be pinned in `scripts/phase19_erasure.py`, not decided here — if the pin does not
name a calibration-fact selection rule, STOP and report a blocker rather than choosing one now); the
`forbid_ids` digest; and the A2 injection budget realised per slot.

Assert `assert_no_value_in_prompt` over every built prompt at both the string and the
contiguous-id-run level, with `prompt_ids=` on the ids actually dispatched.

This is the FIRST `results/phase19_*` artifact. Commit it, then immediately run
`tests/test_phase16_prereg.py` and confirm `test_phase19_prereg_is_frozen_before_every_phase19_result`
now reports a NON-ZERO `checked` — the non-vacuity tie firing for the first time. If it still reports
zero, the guard is green and blind and that is a blocker.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_phase16_prereg.py -x</automated>
  </verify>
  <done>`results/phase19_cal_corpus.json` is committed with its digest, per-fact derived denominators and its exclusions; the ancestry guard now checks a non-zero number of pairs.</done>
</task>

<task type="auto">
  <name>Task 2: Retrain the calibration adapter (D6)</name>
  <files>checkpoints/phase19_cal_adapter.pt, results/phase19_cal_training.log</files>
  <action>
Train a fresh LoRA adapter on `CALIBRATION_POOL` from `checkpoints/convbase_best.pt`, using
`teach_persona`'s existing arm path with a Phase 19 `prefix=` so the bins cannot collide with the
production ones, and every other recipe constant identical to the `real` arm (LR 3e-4, weight_decay
0.0, batch 8, 200 steps, warmup 20, seed 1337, `REAL_RUN_SECOND_PERSON=False`,
`REAL_RUN_REPLAY_RATIO=1.0`). Expect ~81 s on MPS.

Assert: 331,776 trainable parameters over 36 wrapped projections (the census formula asserted at
`scripts/teach_persona.py:627`); the frozen-base canary did not fire; `provenance.git_sha()` is
recorded in the checkpoint; `refuse_if_exists` guarded the write.

Record the wall time in `results/phase19_cal_training.log` in the `results/phase17_training_run.log`
format (`=== END … rc=0 wall=NNs ===`), so the 81 s figure ERASE-02 rests on gains a fourth
independent measurement rather than being quoted a fourth time.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import torch,sys; sd=torch.load('checkpoints/phase19_cal_adapter.pt', weights_only=True); n=sum(v.numel() for k,v in sd.items() if 'lora_' in k); print(n); sys.exit(0 if n==331776 else 1)"</automated>
  </verify>
  <done>The adapter exists at 331,776 LoRA parameters, the base canary did not fire, the git SHA is in the checkpoint, and the wall time is logged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the pin → the first artifact | the ancestry guard becomes load-bearing at this commit |
| a checkpoint file → the loader | `weights_only=True` at every shareable choke point |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-31 | Repudiation | the pin's ordering claim | mitigate | the first `results/phase19_*` commit makes the guard non-vacuous; the run confirms `checked > 0` rather than assuming it |
| T-19-32 | Tampering | an existing adapter or bins | mitigate | `refuse_if_exists` before a token is written (`teach_persona.py:560-562`); a Phase 19 `prefix=` keeps the bins off production paths |
| T-19-33 | Information disclosure | a calibration value in a prompt | mitigate | `assert_no_value_in_prompt` at string and contiguous-id-run level, with `prompt_ids=` on the dispatched ids |
| T-19-34 | Tampering | the frozen base weights | mitigate | the teaching canary raises if any frozen base parameter moves (`teach_persona.py:689-699`) |
| T-19-35 | Elevation | pickled checkpoint load | mitigate | `weights_only=True` at the shareable choke points; `weights_only=False` only on the project's own trusted resume checkpoints with the reason recorded |
| T-19-SC | Tampering | package installs | mitigate | zero installs; `tests/test_package.py` sha256 pin holds |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q tests/test_phase16_prereg.py tests/test_phase19_erasure.py tests/test_package.py`
- `test_phase19_prereg_is_frozen_before_every_phase19_result` reports `checked > 0`.
- `git log --format=%H -- scripts/phase19_erasure.py` — every commit is an ancestor of
  `results/phase19_cal_corpus.json`'s first-add.
</verification>

<success_criteria>
- The calibration corpus and adapter exist, both under Phase 19's own recipe and instrument.
- The ancestry guard is non-vacuous and green.
- Nothing has been erased and nothing has been scored — the floor's inputs are still unmeasured.
</success_criteria>

<output>
Create `.planning/phases/19-selective-memory-erasure/19-08-SUMMARY.md` when done.
</output>
