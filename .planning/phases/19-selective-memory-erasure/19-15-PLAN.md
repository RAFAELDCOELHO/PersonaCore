---
phase: 19-selective-memory-erasure
plan: 15
type: execute
wave: 13
depends_on: ["19-14"]
files_modified:
  - results/phase19_erasure_report.md
autonomous: true
requirements: [ERASE-01, ERASE-02, STAT-01, STAT-02, STAT-06]

must_haves:
  truths:
    - "The verdict is returned by the committed erasure_succeeded(), called exactly once"
    - "Condition (c) runs literally as pre-registered at 23a830c, with no amendment"
    - "The report publishes pre-erasure beside post-erasure in every capability table"
    - "The verdict ships unsoftened, whichever of SUCCESS / FAILURE / INCONCLUSIVE it is"
  artifacts:
    - path: "results/phase19_erasure_report.md"
      provides: "the committed verdict, (a)/(b)/(c) with denominators and bounds, both floor directions, the collateral curve, the M2 reference arm, the DESCRIPTIVE representational read"
      contains: "## Verdict"
  key_links:
    - from: "results/phase19_erasure_report.md"
      to: "scripts/erasure_gate.erasure_succeeded"
      via: "the ONE verdict call in the phase"
      pattern: "erasure_succeeded"
---

<objective>
Call the committed verdict once, and render the report.

Purpose: `erasure_succeeded` is unamendable and is CALLED, never re-implemented
(`scripts/erasure_gate.py:200-255`). Condition (c) runs EXACTLY as pre-registered at `23a830c`, with
no amendment to the original text — that is D3, locked. The literal result is published whatever it
is, and `23a830c` is not touched.

The research computed, this session, what the committed gate returns for a PERFECT erasure under
Δ_dialog: FAILURE on (c) alone, at a cap of 4.576708 against a pre-erasure adapter-on dialogue PPL of
5.8154, an excess of +1.2387 that has nothing to do with the erasure. Whether the measured
adapter-regime floor from 19-10 changes that is the phase's open question, and either way the number
is printed beside the pre-erasure one so a reader can tell a (c) failure that predates the erasure
from one caused by it. The diagnosis of the root cause is 19-16's dated continuation, published
beside this verdict and never in place of it.

Output: `results/phase19_erasure_report.md` with a `## Verdict` section, the ship-decision placeholder
line, and every table carrying its denominator.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-selective-memory-erasure/19-CONTEXT.md
@.planning/phases/19-selective-memory-erasure/19-RESEARCH.md
@scripts/erasure_gate.py
@scripts/phase19_erasure.py
@scripts/phase19_floor.py
</context>

<interfaces>
The single verdict call, with every keyword's producer now measured:
```python
erasure_succeeded(
    target_successes=...,        # results/phase19_arm_m1.json, question unit, n=27 pooled
    target_questions=...,        # phase19_erasure.N_TARGET_QUESTIONS, derived
    target_floor=...,            # phase19_floor.TARGET_FLOOR
    nontarget_deltas=[...],      # seven per-fact values, never pooled
    nontarget_noise_floor=...,   # phase19_floor.NONTARGET_NOISE_FLOOR
    dialogue_ppl=...,            # post-erasure adapter-ON masked dialogue val PPL
    dialogue_ppl_noise_floor=..., # phase19_floor.DIALOGUE_PPL_NOISE_FLOOR
    retention_ppl=...,           # post-erasure adapted retention PPL
    zero_results_have_nll=...,   # phase19_erasure.zero_results_have_nll(arm)
)
```
INCONCLUSIVE takes precedence over FAILURE by construction (`:216-217`), because "we could not tell"
and "it did not work" are different findings and collapsing them is the mistake this project's
honest-negatives discipline exists to prevent. An empty `nontarget_deltas` returns INCONCLUSIVE
(`:253-254`).

Committed report machinery from 19-05: `render_report()`, `assert_erasure_report_not_clobbered()`,
`ERASURE_SHIP_PENDING_LINE` / `ERASURE_SHIP_RECORDED_LINE`, `D8_PUBLICATION_POSTURE`.
`scripts/_verdict.py:27` `recorded_verdict(text)` — the ONE anchored `## Verdict` section read.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Call the verdict once and record it</name>
  <files>results/phase19_erasure_report.md</files>
  <action>
Assemble every argument from the committed artifacts — `results/phase19_arm_m1.json`,
`results/phase19_noise_floors.json`, `scripts/phase19_floor.py`, `scripts/phase19_erasure.py` — and
call `erasure_succeeded` EXACTLY ONCE. Import it; do not retype any of `V20_MASKED_DIALOGUE_VAL_PPL`,
`V20_EWC_RETENTION_PPL`, `V20_RETENTION_NOISE_FLOOR` or `MARGIN_K` (STAT-05, and the AST guard from
19-05 fails if you do).

Run condition (c) LITERALLY, as pre-registered. Do not substitute the adapter-present baseline, do
not adjust the cap, do not amend `23a830c`. The correction path in this phase is a dated continuation
beside the original and never an edit over it (D3, and `19-CONTEXT.md` Deferred Ideas: any amendment
to `23a830c` is permanently out of scope).

Record the returned `(verdict, reasons)` verbatim into the `## Verdict` section. If the verdict is
INCONCLUSIVE, publish it as a REAL OUTCOME and not as a failure to reach one — it is the required
verdict whenever the precondition was unmet, a required measurement is missing, or a zero arrived
with no teacher-forced NLL (`scripts/erasure_gate.py:123-127`, ROADMAP SC5).

Also render the M2 verdict inputs as a SECOND, clearly labelled descriptive block — the reference arm
read against the same floor for context. It is not a second verdict: `erasure_succeeded` is called
once, on M1, and the M2 block prints its inputs and their comparison without invoking the gate.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import sys; sys.path.insert(0,'scripts'); import _verdict; t=open('results/phase19_erasure_report.md').read(); b=_verdict.recorded_verdict(t); assert b is not None and any(v in b for v in ('SUCCESS','FAILURE','INCONCLUSIVE')), b; assert t.count('## Verdict')==1; print(b.strip().splitlines()[0])"</automated>
  </verify>
  <done>Exactly one `erasure_succeeded` call exists in the phase; the returned verdict and reasons are recorded verbatim; (c) ran literally with no amendment to `23a830c`.</done>
</task>

<task type="auto">
  <name>Task 2: Render the report — every number with its denominator, both directions of the floor</name>
  <files>results/phase19_erasure_report.md</files>
  <action>
Render through the committed `render_report()`, with `assert_erasure_report_not_clobbered` running
first. Sections, in order:

1. **Pre-registration** — the pin's SHA chain: `23a830c` (the rule), the `scripts/phase19_erasure.py`
   commits, `scripts/phase19_floor.py` and its evidence SHAs. The commit order that makes the
   calibration blind, so a reader can re-derive rather than trust.
2. **(a) TARGET FORGOTTEN** — successes over n=27 pooled, the Wilson upper bound, `rule_of_three(27)`
   beside it at zero, the floor, and BOTH floor directions (`TARGET_FLOOR` and
   `LITERAL_PHASE14_FLOOR`) with the bound branch named, per D2. State the pooling departure D5 locked
   and why n=52/108 was refused. No bare `0%` (STAT-02).
3. **(b) NON-TARGET PRESERVED** — seven per-fact rows, each with its own denominator, never pooled;
   the noise floor and the `k=2` margin; `max()` named as what the gate reads.
4. **(c) CAPABILITY PRESERVED** — dialogue PPL and retention PPL, each with the PRE-erasure value
   beside the POST-erasure value, the cap, and the noise floor that produced it. Both
   `DIALOGUE_PPL_NOISE_FLOOR` and Δ_dialog = 0.001704 printed, with which one the gate read stated
   explicitly. This is §Q4's mandatory response 4 and it is the only thing that separates a (c)
   failure that predates the erasure from one caused by it.
5. **The collateral curve** — Q7.3's measurement, target and all seven non-targets and dialogue PPL at
   every checkpoint. Say plainly whether it is a gradient or a cliff.
6. **Canary exposure** — the eight-slot table pre and post, against Phase 18's committed rank-1
   baseline (`results/phase18_extraction_report.md:141-152`).
7. **ERASE-02 reference arm** — M2's numbers, its caveat and its framing constraint, both read from
   the record's own fields rather than restated.
8. **Representational consistency** — labelled DESCRIPTIVE, with bounds and n, and the sentence that
   no branch reads it.
9. **Threats to validity** — including, named rather than implied: the `forbid_ids` mask leaves only
   547 live generative ids, and Phase 18 measured that masking makes the attacker STRONGER rather than
   weaker (`results/phase18_extraction_report.md:244`), so the mask does not manufacture a false (a)
   pass; the residual decoding-artifact reading is what `zero_results_have_nll` and the exposure rank
   exist to separate. Also: a retrain is a different adapter, not an edited one; and "we could not
   extract it" is not "it is gone" — a few fine-tuning samples can recover supposedly-erased knowledge
   (Hu et al., arXiv:2406.13356), which is why this phase's claim is a bounded one and why the
   relearning attack is named as absent and as the obvious follow-up.
10. **Verdict** — from Task 1, verbatim.
11. **Ship decision** — `ERASURE_SHIP_PENDING_LINE`, exactly once, so 19-16's `append_addendum` has an
    unambiguous line to replace.
12. **Provenance** — corpus digest, forbid digest, K, ASR rungs, stop ids, temperature, top-p, seed
    stride, device, torch version, git SHA, wall times, and the pids — evidencing the pairing by digest
    and by distinct pids rather than by assertion, Phase 18's standard
    (`results/phase18_extraction_report.md:260-265`).

Publish unsoftened per `D8_PUBLICATION_POSTURE`, in the register Phase 18 shipped
`LEAKAGE_DEMONSTRATED`. Do not add a mitigating adjective the pinned posture does not contain.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_phase19_erasure.py tests/test_phase19_docs.py tests/test_phase16_prereg.py tests/test_package.py -x && ! grep -qn '0%' results/phase19_erasure_report.md && test $(grep -c 'Ship decision' results/phase19_erasure_report.md) -ge 1</automated>
  </verify>
  <done>All twelve sections render; every proportion carries its denominator and its bound; both floor directions and both dialogue noise floors are printed with the gate's choice named; the pending line occurs exactly once.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the measured numbers → the verdict | one call, one committed rule, no second path |
| the report → later writes | a recorded verdict is extended by addendum, never re-rendered |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-65 | Spoofing | a re-implemented verdict | mitigate | `erasure_succeeded` imported and called exactly once; the 19-05 AST guard fails on a second call site or on a retyped baseline literal |
| T-19-66 | Repudiation | amending (c) | mitigate | (c) runs literally; `23a830c` is untouched; the correction path is 19-16's dated continuation beside the original |
| T-19-67 | Tampering | the published report | mitigate | `assert_erasure_report_not_clobbered` via `_verdict.recorded_verdict`; no `--force`; recovery is a reviewed delete visible in the diff |
| T-19-68 | Information disclosure | a bare `0%` | mitigate | STAT-02 enforced by grep in the verification command and by the proportion helpers |
| T-19-69 | Repudiation | a softened headline | mitigate | `D8_PUBLICATION_POSTURE` was committed at 19-05 before the number existed; the renderer reads it rather than restating it |
| T-19-70 | Information disclosure | personal data in a public artifact | accept | every value is invented (T-14-05, `phase14_factset.py:38-39`); everything under `results/` ships publicly by design |
| T-19-SC | Tampering | package installs | mitigate | zero installs; `tests/test_package.py` sha256 pin holds |
</threat_model>

<verification>
- `make test` — full suite green.
- `grep -c 'erasure_succeeded(' scripts/*.py` shows exactly one non-test call site.
- `grep -rn '0%' results/phase19_*` returns nothing.
- `git log --format=%H -- scripts/erasure_gate.py` UNCHANGED — `23a830c` remains the only commit
  Phase 19 is judged by, unamended.
</verification>

<success_criteria>
- The verdict is the committed rule's return value, recorded verbatim, whichever of the three it is.
- (c) ran literally; `23a830c` was not amended.
- Every capability table prints pre-erasure beside post-erasure.
- Both floor directions and both dialogue noise floors are published with the gate's choice named.
</success_criteria>

<output>
Create `.planning/phases/19-selective-memory-erasure/19-15-SUMMARY.md` when done.
</output>
