---
phase: 19-selective-memory-erasure
plan: 07
type: execute
wave: 7
depends_on: ["19-06"]
files_modified: []
autonomous: false
requirements: [ERASE-01, STAT-05]

must_haves:
  truths:
    - "A human has read the pin before it becomes unamendable"
    - "Every erasure_succeeded keyword has a named producer in the pin"
    - "No results/phase19_* artifact exists at the moment the pin closes"
  artifacts: []
  key_links:
    - from: "the human"
      to: "scripts/phase19_erasure.py"
      via: "the last reviewable moment"
      pattern: "checkpoint"
---

<objective>
The gate before the pin becomes unamendable.

Purpose: from the first `results/phase19_*` commit onward, every commit touching
`scripts/phase19_erasure.py` must be an ancestor of that artifact's first-add, and a post-hoc edit is
not an ancestor of a commit that came before it — so `git merge-base --is-ancestor` exits non-zero
and `subprocess.run(..., check=True)` raises. There is no skip path and no force flag. Recovery is a
reviewed, dated commit that leaves the redness visible, which is the point
(`tests/test_phase16_prereg.py:202-204`, §Q6).

This is the last moment the pre-registration can be corrected without that cost. It is a
`checkpoint:human-verify` for that reason alone, not for duration.

Output: an approval, or a list of corrections which are applied to the pin before any run starts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-selective-memory-erasure/19-CONTEXT.md
@scripts/erasure_gate.py
@scripts/phase19_erasure.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render the pin audit</name>
  <files>(read-only)</files>
  <action>
Produce, to stdout only — writing this under `results/phase19_*` would itself close the pin — a
single audit block covering:

1. **Every keyword-only argument of `erasure_succeeded`** (`scripts/erasure_gate.py:200-211`) beside
   the pinned function or constant that produces it: `target_successes`, `target_questions`,
   `target_floor`, `nontarget_deltas`, `nontarget_noise_floor`, `dialogue_ppl`,
   `dialogue_ppl_noise_floor`, `retention_ppl`, `zero_results_have_nll`. Any row without a named
   producer is a blocker.
2. **The six CONTEXT decisions and where each lands in the pin.** D1 → `MECHANISM_RULE` +
   `ABLATION_STOP_RULE` + `ERASE_02_REFERENCE_ARM`; D7 → `TARGET_SELECTION_RULE` + `TARGET_FACT_ID` +
   `TARGET_RANKING` + the tie-break; D5 → `DENOMINATOR_RULE` + `N_TARGET_QUESTIONS`; D2 →
   `ERASURE_FLOOR_RULE` + `lock_erasure_floor` + `literal_phase14_floor`; D4 →
   `NONTARGET_NOISE_FLOOR_ESTIMATOR`; D6 → `CALIBRATION_COMMENSURABILITY`; D3 →
   `DIALOGUE_NOISE_FLOOR_ESTIMATOR` + the marker pair; D8 → `D8_PUBLICATION_POSTURE`.
3. **The reachability proof's output** — `assert_erasure_floor_reachable` run live, printing the
   attainable bound it proved against and `ERASURE_FLOOR_MIN`.
4. **The named target and the full eight-fact ranking**, so the choice is visible rather than
   asserted.
5. **`git ls-files 'results/phase19_*'`** — must be empty, and
   `git log --format='%h %ci' -- scripts/phase19_erasure.py`, the commits that will be checked for
   ancestry from the next artifact onward.
6. **`git log --format=%H -- scripts/phase18_extraction.py | wc -l`** — unchanged; Phase 19 made zero
   commits to the frozen driver.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_phase19_erasure.py tests/test_phase16_prereg.py tests/test_package.py -x && test -z "$(git ls-files 'results/phase19_*')"</automated>
  </verify>
  <done>The audit prints with no blocker row; `results/phase19_*` is empty; the frozen Phase 18 driver has no new commits.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Approve the pin before it becomes unamendable</name>
  <action>
PAUSE. Present <what-built> and walk the operator through <how-to-verify> item by item. Do not proceed, do not commit anything further, and do not begin the next plan until the <resume-signal> is given.
  </action>
  <what-built>
The complete Phase 19 pre-registration: `scripts/phase19_erasure.py` (mechanism identity and
parameters, target fact and its selection rule and tie-break, n=27 denominator, mirrored floor
operator with its reachability proof, the (b) and (c) estimators and seed pairs, the record schemas,
the parity assertions, the report text and the ship-decision marker pair, the arm runner, the M2
retrain arm, the calibration corpus builder), plus the ancestry guard in
`tests/test_phase16_prereg.py` that will hold it unamendable from the next commit onward.
  </what-built>
  <how-to-verify>
1. Read the audit block printed by Task 1. Every `erasure_succeeded` keyword must name a producer.
2. Read `ERASURE_FLOOR_RULE` and check D2's mirror is what you intended: the adjustment makes (a)
   HARDER, never easier, and both directions' values are computable and will be published.
3. Read `TARGET_SELECTION_RULE` and the printed eight-fact ranking. Confirm the named target is the
   one the rule produces and that the tie-break is stated in the same commit.
4. Read `assert_erasure_floor_reachable`'s printed output. Confirm the floor is clearable at n=27 by
   the named attainable outcome, and that when the reachability clamp binds, (a) clears ONLY on a
   perfect erasure — that severity is intended.
5. Read `DIALOGUE_NOISE_FLOOR_ESTIMATOR`. This is the phase's biggest single decision (D3): confirm
   the estimator, the seed pair and the arm config are what you want measured, knowing that the
   pre-erasure adapter already exceeds the Δ_dialog cap by +1.2387 and that the literal (c) result is
   published as FAILURE regardless of how the floor lands.
6. Confirm `git ls-files 'results/phase19_*'` is empty. After the next plan, corrections to any of
   the above cost a reviewed dated commit that leaves the ancestry guard visibly red.
  </how-to-verify>
  <resume-signal>Type "approved" to close the pin, or list the corrections to apply first.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the pin → every number that follows | after this checkpoint the pre-registration cannot be corrected without a visible cost |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-29 | Repudiation | the pre-registration | mitigate | human review at the last amendable moment, with the audit enumerating every producer and every locked decision's landing site |
| T-19-30 | Information disclosure | writing the audit to `results/` | mitigate | stdout only — an audit file under `results/phase19_*` would itself close the pin before the human read it |
| T-19-SC | Tampering | package installs | mitigate | zero installs; `tests/test_package.py` sha256 pin holds |
</threat_model>

<verification>
- Full suite green: `make test`.
- `git ls-files 'results/phase19_*'` empty at the moment of approval.
</verification>

<success_criteria>
- A human has read the pin and approved it, or the corrections were applied before any run started.
- Every `erasure_succeeded` keyword has a named producer.
- The pin is closed. From here, the ordering is enforced by git ancestry rather than by intention.
</success_criteria>

<output>
Create `.planning/phases/19-selective-memory-erasure/19-07-SUMMARY.md` when done.
</output>
