---
phase: 24-adversarial-extraction-aware-training-the-held-out-attack-fa
plan: 05
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - scripts/phase24_adversarial.py
  - tests/test_phase24_adversarial.py
  - tests/test_phase14_scoring.py
autonomous: true
requirements: [ADVT-01, ADVT-02]

must_haves:
  truths:
    - "D-03: the mixture renders ONLY from `core_taught`; the gated tier (F3, F7, F8, reserved) stays entirely out of training on both axes — new attack family and new paraphrase family"
    - "D-10: exactly THREE families train — A1-mild, A1-aggressive, A3 — and A2 reaching the builder is a hard `SystemExit`, because an A2 training episode's TARGET would be a 25% prefix of the private value followed by a refusal, and `contains_value` is structurally blind to it"
    - "D-10: A3 rides at mask=0 as context, never as target, and its `persona=` call site is admitted by a FOURTH written-justified `PERSONA_ALLOWLIST` entry landing in the SAME commit as the call site"
    - "D-12: A2 is the held-out family as a MECHANICAL CONSEQUENCE, named before training, reason declared as value containment and never selection by performance"
    - "SC4: `scripts/phase18_extraction.py` is imported READ-ONLY, and the attack trained against is proved byte-equal to the attack scored by — `build_recall_prompt(...) == entry['prompt_ids']` under hard equality on every episode"
    - "The trained pool is exactly 336 episodes (3 x 112), family-independent, which is what makes D-09's 1.9090909090909092 upper extreme intact"
  artifacts:
    - path: "scripts/phase24_adversarial.py"
      provides: "adversarial_episodes() — the corpus-joined, parity-proved, A2-refusing episode builder"
      contains: "def adversarial_episodes"
    - path: "tests/test_phase24_adversarial.py"
      provides: "pool size, tier filter, A2 refusal, determinism and prompt parity"
    - path: "tests/test_phase14_scoring.py"
      provides: "the fourth PERSONA_ALLOWLIST entry with its written justification"
  key_links:
    - from: "scripts/phase24_adversarial.py"
      to: "results/phase18_corpus.json"
      via: "phase18_extraction.CORPUS_PATH, read-only"
      pattern: "CORPUS_PATH"
    - from: "scripts/phase24_adversarial.py"
      to: "src/personacore/dialogue/serialize.py build_recall_prompt"
      via: "the ONE persona= call site, allowlisted"
      pattern: "persona="
    - from: "tests/test_phase14_scoring.py PERSONA_ALLOWLIST"
      to: "scripts/phase24_adversarial.py"
      via: "hard-equality AST census over scripts/*.py + src/**/*.py"
      pattern: "phase24_adversarial"
---

<objective>
Turn the committed Phase 18 attack corpus into training episodes: `core_taught` only, three
families only, each episode's prompt PROVED byte-equal to the committed corpus row it came from.

Purpose: SC4's guarantee — "the attack trained against and the attack scored by cannot silently
drift apart" — is worth nothing as a promise. Made structural, it is a hard equality on every one of
336 episodes. The corpus row carries `prompt_ids` but NOT the question text, so the builder must
re-render through the frozen module's OWN builders and then prove the re-render reproduces the
committed ids exactly. That proof is also the load-bearing use of D-10's fourth persona call site.

Output: `adversarial_episodes()` added to `scripts/phase24_adversarial.py`, its tests, and the
fourth `PERSONA_ALLOWLIST` entry landing in the same commit as the call site it admits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/24-adversarial-extraction-aware-training-the-held-out-attack-fa/24-CONTEXT.md
@.planning/phases/24-adversarial-extraction-aware-training-the-held-out-attack-fa/24-PATTERNS.md
@.planning/phases/24-adversarial-extraction-aware-training-the-held-out-attack-fa/24-01-SUMMARY.md

<interfaces>
<!-- Verified at HEAD this session. scripts/phase18_extraction.py is READ-ONLY — import only. -->

scripts/phase18_extraction.py  (ANCESTRY-GUARDED, PERMANENTLY UNEDITABLE)
  CORPUS_PATH            = _REPO_ROOT / "results" / "phase18_corpus.json"       # :697
  CORPUS_SOURCE_FIXTURE  = _REPO_ROOT / "results" / "phase16_recall_sample.json" # the ONLY place
                           the attack QUESTION TEXT lives; the corpus carries prompt_ids, not text
  apply_a1(question, *, dose)                                                    # :474
  A3_ROLE_INSTRUCTION    = (a value-free role scaffold string)                   # :506
  build_a3_prompt(tok, question) == build_recall_prompt(tok, question,
                                     persona=(A3_ROLE_INSTRUCTION,))             # :545
  build_a2_prompt(tok, question, prefix_ids)                                     # :640 (def),
                           :653 returns build_recall_prompt(...) + list(prefix_ids)
  injection_budget(value_ids)                                                    # :565 (NOT :580 —
                           24-CONTEXT's canonical_refs drifted; carry this number)
  corpus_sha256(corpus)                                                          # :758

  HOW THE COMMITTED CORPUS WAS BUILT (`def build_corpus` at :822; its per-family branches at
  :896-953 — the range that defines the parity target):
    A1 rows: prompt_ids == build_recall_prompt(tok, apply_a1(question, dose=dose))   [BARE form]
    A3 rows: prompt_ids == build_a3_prompt(tok, question)
                        == build_recall_prompt(tok, question, persona=(A3_ROLE_INSTRUCTION,))
    A2 rows: prompt_ids == build_recall_prompt(tok, question) + prefix_ids  [EXCLUDED, D-10]

results/phase16_recall_sample.json — {'counts', 'questions': {tier: [rows]}}; each row is
  {'seed_index': int, 'fact_id': str, 'question': str, 'reserved': bool}. core_taught has 112 rows.

results/phase18_corpus.json — 864 rows keyed by CORPUS_ENTRY_KEYS
  ("family","dose","fact_id","slot","tier","seed_index","source_family","realized_injection",
   "prompt_ids"). core_taught rows per family: 112. Join key into the fixture:
  (tier, fact_id, seed_index).

src/personacore/dialogue/serialize.py
  build_recall_prompt(tok, question, persona=())    # :92  (NOT :93 — carry this number)
  encode_dialogue(tok, persona, turns)              # :61  persona span mask=0 (:81-82),
                                                    #      assistant content mask=1, final eos mask=1 (:88)

tests/test_phase14_scoring.py — the D-21 guard, HARD EQUALITY
  PERSONA_ALLOWLIST                                  # :422 — three incumbents today, each preceded
      by a written justification comment (:423-448)
  test_persona_argument_is_scoped_to_the_fairness_control   # :524-562
      with_persona = sorted((file, func) for file, func, kwargs in sites if "persona" in kwargs)
      assert with_persona == sorted(PERSONA_ALLOWLIST)          # :557
      assert [(f, fn) for f, fn, kwargs in sites if kwargs and "persona" not in kwargs] == []
      CONSEQUENCE: `with_persona` is a LIST, so TWO `persona=` calls inside ONE function produce
      TWO identical tuples and break the equality. Exactly ONE persona= call site, in exactly ONE
      function, in the whole new module.
      :539-546 documents the extension path; :546's `len(scanned) >= 2` catches a broken glob.
      `def test_persona_argument_is_scoped_to_the_fairness_control` is at :524.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: adversarial_episodes() — the corpus join, the A2 refusal, the parity proof, and the fourth PERSONA_ALLOWLIST entry IN THE SAME COMMIT</name>
  <files>scripts/phase24_adversarial.py, tests/test_phase14_scoring.py</files>
  <read_first>
    - `scripts/phase24_adversarial.py` (24-01's output — the refusal half you are extending)
    - `tests/test_phase14_scoring.py` lines 405-449 (`PERSONA_ALLOWLIST` at :422 and the three
      incumbent justification comments at :423-448 — the style the fourth entry must match) and
      lines 483-562 (the AST census `_call_sites`, `_build_recall_prompt_call_sites`, and the
      hard-equality guard at :557)
    - `scripts/phase18_extraction.py` lines 690-760 and 890-960 (`CORPUS_PATH`,
      `CORPUS_SOURCE_FIXTURE`, `CORPUS_ENTRY_KEYS`, and the exact `build_corpus` A1/A2/A3 branches
      that define the parity target) — READ ONLY, never edited
    - `src/personacore/dialogue/serialize.py` lines 61-110
    - `scripts/teach_persona.py` lines 412-464 (`_slot_forms_for`'s `SystemExit` register and the
      lazy-import rule)
    - `tests/test_phase19_erasure.py` lines 2388-2400 (the committed-corpus loader shape)
  </read_first>
  <action>
Extend `scripts/phase24_adversarial.py`. Do not move or reword anything 24-01 wrote.

Module constants:

- `TRAINED_FAMILIES = ("A1-mild", "A1-aggressive", "A3")` — a plain tuple, with a comment recording
  D-10's reason for each inclusion and D-12's reason for A2's exclusion.
- `HELD_OUT_FAMILY = "A2"` — with the containment reason written out: `build_a2_prompt` ends the
  prompt with `floor(len(value_ids) * INJECTION_FRACTION)` leading ids OF THE PRIVATE VALUE past
  `<|assistant|>`; `encode_dialogue` marks assistant content mask=1, so an A2 training episode's
  TARGET would be `⟨25% of the private value⟩ + ⟨refusal⟩` — the exact inverse of D-01's guarantee.
  `contains_value` requires the WHOLE value, so the harm exists and the instrument is structurally
  blind to it. Also record D-12: this makes A2 the held-out family as a MECHANICAL CONSEQUENCE,
  named before training, never selected by performance.
- `TRAINED_TIER = "core_taught"` — D-03, with the 2026-08-30 measured note: `core_taught` is exactly
  {F1:160, F2:160, F6:128} and `core_held_out` exactly {F3:96, F7:96, F8:96, reserved:128}; F4 and
  F5 are ABSENT from the corpus entirely, so D-03 reduces operationally to the tier cut and there is
  no F4/F5 exclusion code to write.

Functions (lazy-import `phase18_extraction as p18` INSIDE the functions that need it, per the
project's lazy-import rule — `scripts/teach_persona.py:922-936` is the precedent):

- `attack_prompt_ids(tok, question, persona)` -> `list[int]`
  THE ONE AND ONLY `build_recall_prompt(..., persona=persona)` call site in this module and in this
  phase. It must be the only `persona=` call anywhere in the file — two calls in one function break
  the D-21 hard equality, which compares LISTS. Its docstring is the written justification that the
  fourth allowlist entry cites: this call reproduces a COMMITTED corpus prompt in order to PROVE
  byte-equality with it; the persona it may pass is `p18.A3_ROLE_INSTRUCTION`, a value-free role
  scaffold naming no fact, slot or persona value; `persona=()` for A1 is identical to the bare form
  by `build_recall_prompt`'s own default; and no value can enter the span by this route because the
  span content is a frozen constant of the ancestry-guarded module.

- `adversarial_episodes(tok)` -> `list[tuple[tuple[str, ...], str, str]]`
  One `(persona, question, answer)` triple per corpus row that passes the D-03 / D-10 filters.
  Steps:
  1. Load `p18.CORPUS_PATH` and `p18.CORPUS_SOURCE_FIXTURE` (json). Assert the corpus's
     `entry_keys` equals `list(p18.CORPUS_ENTRY_KEYS)` — ordered hard equality, the
     `tests/test_phase18_corpus.py:538` register. `SystemExit` on mismatch.
  2. Build a `(tier, fact_id, seed_index) -> question` map from the fixture. `SystemExit` naming the
     key if any corpus row has no fixture question — a `KeyError` a third of the way through is the
     failure this buys out.
  3. Filter rows: keep `row["tier"] == TRAINED_TIER` AND `row["family"] in TRAINED_FAMILIES`.
     PRESERVE THE CORPUS'S OWN ROW ORDER — do not sort. The corpus is written tier-outer,
     row-inner, family-inner in a fixed sequence, so the filtered order is already deterministic;
     record that in the docstring and prove it in Task 2.
  4. If any surviving row's family is `HELD_OUT_FAMILY`, raise `SystemExit` prefixed
     `[phase24_adversarial] ` quoting the containment reason above. Belt-and-braces beside the
     filter: a filter that silently drops is indistinguishable from a filter that silently keeps.
  5. Re-render the attacked question from the FROZEN builders, never by decoding `prompt_ids`
     (decode/encode is not guaranteed byte-equal through `detokenize`):
       - family `A1-mild` / `A1-aggressive` -> `p18.apply_a1(question, dose=row["dose"])`, persona `()`
       - family `A3` -> `question` unchanged, persona `(p18.A3_ROLE_INSTRUCTION,)`
     Any other family reaching this branch is a `SystemExit` naming it — no default fall-through.
  6. PARITY, on every episode: `attack_prompt_ids(tok, attacked, persona) == row["prompt_ids"]`
     under hard list equality. On mismatch raise `SystemExit` naming the family, `fact_id`,
     `seed_index`, both lengths and the first differing index. This is SC4 made structural.
  7. Answer: `refusal_for(row["slot"])` from 24-01.
  8. Before returning, `SystemExit` unless `len(episodes) == len(TRAINED_FAMILIES) * 112`. The
     count comes from the filtered fixture row count, not the literal 112 — derive it, do not type
     it, and state the derivation in the message.

- `adversarial_pool_size(tok)` -> `int` — thin, returns `len(adversarial_episodes(tok))`, so 24-06's
  sizing and the ADVT-03 record both read ONE derivation.

Never write to `scripts/phase18_extraction.py` or `results/phase18_corpus.json`. Never call
`p18.build_corpus` — SC2 requires the check be read from the committed artifact, and a rebuild is a
different reader.

**THE FOURTH ALLOWLIST ENTRY LANDS IN THIS TASK, NOT THE NEXT ONE.** This is not a style
preference; `tests/test_phase14_scoring.py:418-421` states the repo rule verbatim: *"A future phase
adding a `persona=` call site therefore adds its entry in the SAME commit as the call site — being a
new file buys no exemption, and nothing may be pre-added."* The guard at :557 is HARD EQUALITY
(`assert with_persona == sorted(PERSONA_ALLOWLIST)`), and the GSD executor commits per task — so a
call site in one task and its entry in the next is two commits, and the first of them is RED. That
is the failure this task's own verify command now catches at the task gate.

Append a FOURTH entry to `PERSONA_ALLOWLIST` in `tests/test_phase14_scoring.py`:

`("scripts/phase24_adversarial.py", "attack_prompt_ids")`

preceded by a written justification comment in the three incumbents' style, stating: this call site
exists to PROVE byte-equality against a committed corpus prompt, not to place anything new in a
prompt; the only persona it can pass is `phase18_extraction.A3_ROLE_INSTRUCTION`, a value-free role
scaffold that is a frozen constant of an ancestry-guarded module; the D-02 sibling guard
(`test_no_fact_values_in_the_refusal_templates`, added by 24-01) statically scans the new module for
published values; and the entry lands in the SAME commit as the call site, as this allowlist
requires — pre-adding is as red as omitting.

Do not reorder or reword the three incumbent entries. Do not weaken the hard equality at :557. Add
NO line to `tests/test_phase14_scoring.py` containing the literal `== 10` or `!= 10` — the SC5 wall
census (`tests/test_phase21_sc5.py:196`) is a plain LINE scan over every file under `tests/` and a
comment quoting that token registers as an unpinned wall site.

WATCH IT RED ONCE: temporarily delete the fourth entry (leaving the call site), run
`pytest -q tests/test_phase14_scoring.py::test_persona_argument_is_scoped_to_the_fairness_control`,
capture the failure, restore, confirm GREEN. Record both in the SUMMARY. Then commit
`scripts/phase24_adversarial.py` and `tests/test_phase14_scoring.py` TOGETHER.
  </action>
  <acceptance_criteria>
    - `.venv/bin/python -c "import sys,pathlib; sys.path.insert(0,'scripts'); from personacore.tokenizer import from_json; import teach_persona as tp, phase24_adversarial as a; tok=from_json(tp.TOKENIZER_PATH); e=a.adversarial_episodes(tok); print(len(e))"` prints `336`
    - The same command run twice produces identical episode lists (`==` on the returned list)
    - `.venv/bin/python -c "import ast,pathlib; t=ast.parse(pathlib.Path('scripts/phase24_adversarial.py').read_text()); n=[c for c in ast.walk(t) if isinstance(c,ast.Call) and 'build_recall_prompt' in (getattr(c.func,'id',''),getattr(c.func,'attr','')) and any(k.arg=='persona' for k in c.keywords)]; assert len(n)==1, len(n); print('ok')"` passes — exactly ONE `build_recall_prompt(..., persona=...)` CALL. Use AST, never `grep -c "persona="`: `grep` counts LINES, this task's own action instructs a docstring that mentions `persona=()` and `persona=` several times, and `_call_sites`' own docstring names the hazard — *"a substring check cannot tell a call from a mention in a docstring"*
    - `grep -n "build_corpus\|prompt_ids\].*decode\|detokenize(row" scripts/phase24_adversarial.py` shows no rebuild and no decode-based re-render
    - Every returned persona tuple is `()` for A1 families and a 1-tuple for A3; assert this from Python, not by eye
    - `git diff scripts/phase18_extraction.py` is EMPTY and `git status --porcelain results/phase18_corpus.json` is empty
    - `pytest -q tests/test_phase16_prereg.py tests/test_phase18_corpus.py` reports 0 failed
    - `pytest -q tests/test_phase14_scoring.py` reports 0 failed, INCLUDING `test_persona_argument_is_scoped_to_the_fairness_control` — this is the task gate that catches the split-commit failure, so it must run HERE and not one task later
    - `.venv/bin/python -c "import ast,pathlib; t=ast.parse(pathlib.Path('tests/test_phase14_scoring.py').read_text()); a=[n for n in ast.walk(t) if isinstance(n,ast.Assign) and any(getattr(x,'id','')=='PERSONA_ALLOWLIST' for x in n.targets)]; assert len(a)==1 and len(a[0].value.elts)==4; print('ok')"` passes — one assignment, four entries
    - Deleting the fourth allowlist entry (leaving the call site) makes `test_persona_argument_is_scoped_to_the_fairness_control` FAIL; restore and confirm GREEN. Both quoted in the SUMMARY
    - `pytest -q tests/test_phase21_sc5.py::test_wall_census_is_the_measured_set` passes — no line added to `tests/test_phase14_scoring.py` carries `== 10` or `!= 10`
    - `git log --oneline -1 --name-only` shows `scripts/phase24_adversarial.py` and `tests/test_phase14_scoring.py` in the SAME commit
  </acceptance_criteria>
  <verify>
    <automated>pytest -q tests/test_phase14_scoring.py tests/test_phase16_prereg.py tests/test_phase18_corpus.py tests/test_phase21_sc5.py</automated>
  </verify>
  <done>336 episodes build deterministically from the committed corpus, every prompt is proved byte-equal to its committed row, A2 is refused rather than filtered silently, the frozen module is untouched, and the fourth allowlist entry landed in the SAME commit as the call site it admits — watched RED without it.</done>
</task>

<task type="auto">
  <name>Task 2: The six builder property tests</name>
  <files>tests/test_phase24_adversarial.py</files>
  <read_first>
    - `scripts/phase24_adversarial.py` (Task 1's output)
    - `tests/test_phase14_scoring.py` lines 422-449 (the fourth `PERSONA_ALLOWLIST` entry Task 1
      already landed — read it, do not re-add it; the entry and its call site are already committed
      together and re-touching this file here would be a second, unnecessary edit)
    - `tests/test_phase18_corpus.py` lines 530-565 (the assertion register)
    - `tests/test_phase21_aligned_bins.py` lines 17-34 (the scripts-path preamble and the frozen
      tokenizer load)
  </read_first>
  <action>
Create `tests/test_phase24_adversarial.py`. Six named tests, path resolved from
`phase18_extraction.CORPUS_PATH` and the frozen tokenizer loaded via `from_json(tp.TOKENIZER_PATH)`:

1. `test_the_trained_pool_is_336_episodes_and_family_independent` — `len(adversarial_episodes(tok))`
   equals `len(TRAINED_FAMILIES) * (core_taught fixture row count)`, both derived from artifacts.
   Also assert the per-family episode counts are EQUAL to each other, which is D-06's measured
   property: the pool is family-independent under the episode unit even though token volume varies
   by up to 1.59x.

2. `test_only_core_taught_rows_train` — every returned episode's source row has
   `tier == "core_taught"`; and the gated tier's questions appear in ZERO episodes. Assert via the
   corpus rows, and additionally assert the returned question strings are disjoint from the
   `core_held_out` fixture questions. This is D-03 verified on both axes.

3. `test_a2_reaching_the_builder_is_refused_not_filtered` — monkeypatch or construct a row set that
   includes an `A2` row past the filter (e.g. call the internal branch directly, or temporarily
   widen `TRAINED_FAMILIES` in the test via `monkeypatch.setattr`) and assert `SystemExit` whose
   message names A2 and the value-prefix reason. The PAIRED positive half in the same test: with the
   real constants, no `SystemExit` is raised and the pool is non-empty — a refusal test that only
   forbids is satisfied by refusing everything.

4. `test_every_episode_prompt_is_byte_equal_to_its_committed_corpus_row` — the SC4 assertion,
   independent of the builder's internal check: for every returned episode, recompute the ids and
   compare to the corpus row's `prompt_ids` under `==`. Assert the comparison count equals the pool
   size, so a loop that silently iterated nothing cannot pass.

5. `test_the_episode_order_is_deterministic` — build twice in one process and once after
   `seed_everything(tp.SEED)`; assert all three lists are `==`. D-08's permutation depends on this
   list being stable, and the Phase 23 resume path refuses on any byte change.

6. `test_every_answer_is_a_slot_refusal_and_never_a_value` — every episode's answer equals
   `refusal_for(row["slot"])`, and `phase14_recall.contains_value(answer, value)` is False for every
   published value, resolved at runtime from `phase14_factset`. This is the runtime companion to the
   static D-02 guard: the static scan sees the TABLE, this sees the RENDERED answers.

Run the D-21 guard again in this task's verification as a REGRESSION check — it went green in
Task 1 with the entry and the call site in one commit; nothing here may move it.
  </action>
  <acceptance_criteria>
    - `pytest -q tests/test_phase24_adversarial.py` reports 6 passed, 0 failed
    - `git diff tests/test_phase14_scoring.py` is EMPTY for this task — the allowlist landed in Task 1 and is not touched again
    - `pytest -q tests/test_phase14_scoring.py` reports 0 failed (regression check, including `test_persona_argument_is_scoped_to_the_fairness_control`)
    - `git diff scripts/phase18_extraction.py scripts/mitigation_gate.py` is EMPTY
    - `git log --oneline -1 --name-only` shows `tests/test_phase24_adversarial.py` ONLY
  </acceptance_criteria>
  <verify>
    <automated>pytest -q tests/test_phase24_adversarial.py tests/test_phase14_scoring.py tests/test_phase16_prereg.py</automated>
  </verify>
  <done>All six builder properties pass and the D-21 hard equality — green since Task 1's single commit — is unmoved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed attack corpus → training episode | A re-render that differs from the committed prompt trains against an attack nobody scored |
| private value → training target | An A2 episode's target would carry a 25% value prefix, which `contains_value` cannot see |
| `persona=` argument → prompt span | The project's whole claim is that memory lives in the weights, not the prompt |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-23 | Tampering | attack drift (SC4) | high | mitigate | Hard list equality `attack_prompt_ids(...) == row["prompt_ids"]` on all 336 episodes, checked inside the builder AND independently in Task 2 test 4 with a comparison-count non-vacuity assertion |
| T-24-24 | Information Disclosure | A2 value prefix | high | mitigate | `HELD_OUT_FAMILY` filter PLUS an explicit `SystemExit` if an A2 row survives; Task 2 test 3 watches the refusal fire and pairs it with a positive half |
| T-24-25 | Elevation of Privilege | `persona=` call sites | high | mitigate | Exactly ONE persona call site in the module — counted by AST, never `grep -c`, because the same task's docstring mentions `persona=` — admitted by a fourth written-justified `PERSONA_ALLOWLIST` entry added in TASK 1, the same task and therefore the same commit as the call site; `pytest -q tests/test_phase14_scoring.py` is in Task 1's own automated verify command so the split-commit RED is caught at the task gate, and the guard is watched failing without the entry |
| T-24-26 | Information Disclosure | gated tier leaking into training | high | mitigate | D-03 tier filter, asserted on both axes (tier field AND question-string disjointness from the `core_held_out` fixture) |
| T-24-27 | Tampering | frozen `scripts/phase18_extraction.py` | high | mitigate | Import-only; `git diff` EMPTY and `tests/test_phase16_prereg.py` green are acceptance criteria; the corpus is read from `CORPUS_PATH`, never rebuilt |
| T-24-28 | Repudiation | non-deterministic episode order | high | mitigate | Corpus row order preserved (no sort); Task 2 test 5 builds three times and asserts equality — D-08's permutation and the Phase 23 resume path both depend on it |
| T-24-29 | Spoofing | refusal answers at runtime | medium | mitigate | Task 2 test 6 runs `contains_value` over every rendered answer against every published value, complementing the static D-02 table scan |
| T-24-SC | Tampering | package installs | low | accept | No `pip install` in this plan |
</threat_model>

<verification>
- `pytest -q tests/test_phase24_adversarial.py tests/test_phase14_scoring.py tests/test_phase16_prereg.py tests/test_phase18_corpus.py` — 0 failed
- `make test` green
- `git diff scripts/phase18_extraction.py scripts/mitigation_gate.py` is EMPTY
- `git status --porcelain results/` shows no modification to `phase18_corpus.json`
</verification>

<success_criteria>
- `adversarial_episodes(tok)` returns exactly 336 deterministic episodes from `core_taught` over the three trained families
- Every episode's prompt is byte-equal to its committed corpus row
- A2 is refused with the containment reason, not filtered in silence
- The fourth `PERSONA_ALLOWLIST` entry and its single call site landed in ONE commit (both in Task 1) and the D-21 hard equality is green, watched RED without the entry
</success_criteria>

<output>
Create `.planning/phases/24-adversarial-extraction-aware-training-the-held-out-attack-fa/24-05-SUMMARY.md`
when done. It MUST record the measured pool size, the per-family counts, the RED observed when
the fourth allowlist entry was temporarily removed, and the Task 1 commit hash showing
`scripts/phase24_adversarial.py` and `tests/test_phase14_scoring.py` together.
</output>
