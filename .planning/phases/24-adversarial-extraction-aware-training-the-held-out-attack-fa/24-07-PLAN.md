---
phase: 24-adversarial-extraction-aware-training-the-held-out-attack-fa
plan: 07
type: execute
wave: 4
depends_on: ["24-02", "24-06"]
files_modified:
  - tests/test_phase24_band.py
  - scripts/phase24_record.py
  - results/phase24_token_budget.json
  - tests/test_phase24_record.py
  - scripts/phase24_adversarial.py
autonomous: true
requirements: [ADVT-01, ADVT-02, ADVT-03]

must_haves:
  truths:
    - "D-05: the mask fraction is measured at ALL FOUR grid corners — {adv_n8, adv_n64} x {0.0, 1.9090909090909092} — and the worst corner (n=8 at the upper extreme) sits inside MASK_FRACTION_BAND with real margin, not borderline"
    - "D-05: the measurement converts a post-compute build-time `SystemExit` into a four-second CPU red test, which is the entire point of measuring before any sweep point runs"
    - "ADVT-03: scored-token counts are reported PER ARM in a COMMITTED record — today they are stdout-only at teach_persona.py:1063 / :1730 / :877 and no committed record holds a v4.0 arm's mask fraction"
    - "ADVT-03: attack intensity is disclosed as ALSO a token-budget axis, so an intensity effect and a budget effect are separable in the report rather than confounded — the cross-family spread is 3.73x (A3/A2), which is NOT the 1.40x single-sentence figure and must never be reported as if it were"
    - "D-07: multiplicity travels in the same row as the point, because Phase 25 SC3 requires it in the same sentence as epsilon"
    - "SC4: no new attack corpus is created by this phase, so the inflation-report obligation is discharged EXPLICITLY in the record rather than silently"
  artifacts:
    - path: "tests/test_phase24_band.py"
      provides: "the D-05 four-corner build-only band check with margin"
    - path: "scripts/phase24_record.py"
      provides: "TOKEN_BUDGET_RECORD path constant plus the write-once, refuse-if-dirty emitter"
      contains: "TOKEN_BUDGET_RECORD"
    - path: "results/phase24_token_budget.json"
      provides: "per-arm x per-grid-point scored-token counts, multiplicity, mask fractions, corpus sha256 and provenance"
    - path: "tests/test_phase24_record.py"
      provides: "the record's keys, its re-derivation, and its band clearance at every point"
  key_links:
    - from: "tests/test_phase24_record.py"
      to: "results/phase24_token_budget.json"
      via: "phase24_record.TOKEN_BUDGET_RECORD — resolved from the owning module, never a test literal"
      pattern: "TOKEN_BUDGET_RECORD"
    - from: "scripts/phase24_record.py"
      to: "scripts/mitigation_budget.py ADVERSARIAL_RATIO_GRID"
      via: "the grid is imported, never retyped"
      pattern: "ADVERSARIAL_RATIO_GRID"
    - from: "scripts/phase24_record.py"
      to: "results/phase18_corpus.json"
      via: "phase18_extraction.corpus_sha256 recorded into provenance"
      pattern: "corpus_sha256"
---

<objective>
Measure the mask fraction at all four D-05 corners before any sweep point exists, and persist
ADVT-03's per-arm scored-token counts into a committed record — closing the gap 24-RESEARCH found:
nothing in this repository persists a v4.0 arm's scored-token counts or mask fraction today.

Purpose: `_prove_floor_and_band` `SystemExit`s at BUILD time. Without this measurement the failure
surfaces after a sweep point's compute is already spent. With it, the failure is a four-second CPU
test. And a token-budget disclosure that can only be read off stdout is not a measurement — Phase 25
SC4 makes `results/phase2X_frontier.json` the single source of truth, and every figure it draws must
be re-derivable from a committed file.

Output: the four-corner guard, the committed record, and the test that re-derives it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/24-adversarial-extraction-aware-training-the-held-out-attack-fa/24-CONTEXT.md
@.planning/phases/24-adversarial-extraction-aware-training-the-held-out-attack-fa/24-RESEARCH.md
@.planning/phases/24-adversarial-extraction-aware-training-the-held-out-attack-fa/24-PATTERNS.md
@.planning/phases/24-adversarial-extraction-aware-training-the-held-out-attack-fa/24-06-SUMMARY.md

<interfaces>
<!-- Every figure below was MEASURED at HEAD this session. Do not re-derive; do not substitute an
     older figure that contradicts one of these. -->

THE OPERATING POINT (flat pack — which is what the adversarial arm uses):
  adversarial_ratio = 0.0  ->  mask_fraction 0.358660 at n=8, 0.390163 at n=64
  MASK_FRACTION_BAND = (0.15, 0.95)  (scripts/teach_persona.py:127), enforced on the AGGREGATE mean
    by _prove_floor_and_band (:528, check at :549), SystemExit at BUILD time.
  Only the 0.15 FLOOR binds — both effects of the mixture push the fraction DOWN, so 0.95 is
    unreachable on this axis. The per-episode minimum (0.188406) never reaches the guard.

  TWO TRAPS, named so they are not stepped in:
    * the RECORDED 0.321813 / 0.347701 are the ALIGNED dp_n8 / dp_n64 packs. `_build_aligned_bins`
      pads each fact shard with mask=0 (dp_n8 carries 867 pad tokens), depressing the fraction by
      ~0.037 and modelling a padding term the adversarial arm does not have.
    * the v3.0 figures 0.3426 / 0.3854 / 0.3778 are the CAL arms, reachable only through
      `_arm_rows` (:2192) via `run_calibration` over CAL_ARMS (:1960) — structurally unreachable for
      any v4.0 arm.
    Neither set may be substituted for the flat operating point.

  HEADROOM at the upper extreme, L = scored tokens per refusal:
    n=8   frac(L) = (2719  + 336*L)  / (33635  + 336*L)
    n=64  frac(L) = (28128 + 2688*L) / (280525 + 2688*L)
    L=7 -> 0.1409 (SystemExit)   L=8 -> 0.1489 (SystemExit)   L=9 -> 0.1567 (ok, borderline)
    L=15 -> 0.2006   L=20 -> 0.2339   L=25 -> 0.2645
    The BINDING corner is (n=8, 1.9090909090909092): it needs L >= 9 where n=64 needs L >= 7.
    A D-01-shaped refusal measures 17-28 scored tokens across all 11 SLOT_FORMS -> 0.214-0.282.

PER-FAMILY TOKEN LENGTHS, core_taught, from results/phase18_corpus.json:
  A1-mild        n=112  mean 44.45   total 4,978    1.00x
  A1-aggressive  n=112  mean 69.66   total 7,802    1.57x
  A2             n=112  mean 31.77   total 3,558    0.72x   (HELD OUT — not trained)
  A3             n=112  mean 118.52  total 13,274   2.67x
  Trained pool: 336 episodes, 26,054 prompt tokens.
  CROSS-FAMILY INFLATION IS 3.73x (A3/A2) — NOT the 1.40x ADVT-03 quotes for one uppercased
  51-character sentence. The two figures describe different things and must be reported as such.
  Leave-one-out token spread at identical episode count: up to 1.59x.

scripts/teach_persona.py  (after 24-06)
  build_bins(tok, episodes, bin_path, mask_path, *, replay_ratio=0.0, align_facts=None,
             adversarial_ratio=0.0, seed=SEED)
  additive stats keys on the non-default branch: adversarial_ratio, clean_episodes,
    adversarial_episodes, adversarial_pool_size, adversarial_multiplicity,
    adversarial_family_counts (the SELECTED prefix's per-family episode counts), adversarial_tokens,
    adversarial_scored_tokens, adversarial_permutation_seed, and an overriding `episodes`
  arm_spec("adv_n8")  -> (fs.LOCKED_FACTS, False, 0.0)
  arm_spec("adv_n64") -> (fs.LOCKED_FACTS + phase21_filler.FILLER_FACTS, False, 0.0)
  render_episodes(facts, family_ids, *, second_person=False)
  MASK_FRACTION_BAND (:127), SEED, TOKENIZER_PATH — the FROZEN production tokenizer, never retrained

scripts/phase24_adversarial.py  (24-01 + 24-05)
  MIN_REFUSAL_SCORED_TOKENS, MASK_FRACTION_MARGIN, refusal_for(slot),
  adversarial_episodes(tok), adversarial_pool_size(tok), TRAINED_FAMILIES, HELD_OUT_FAMILY

scripts/mitigation_budget.py  (24-02)
  ADVERSARIAL_RATIO_GRID = (0.0, 0.25, 0.5, 1.0, 1.5, 1.9090909090909092)
  ADVERSARIAL_RATIO_GRID_PROVENANCE = { ... }

scripts/phase21_unit_record.py — the writer analog
  :168-172  ARTIFACTS path register
  :246      refuse_existing_artifacts(paths=None)
  :269      refuse_dirty_publication(path)
  :702-726  _provenance(**extra) — git_sha + wall clock + interpreter + seed + pin sha256
  :729-753  _write(path, document) — refuse-to-rerun and refuse-if-dirty BEFORE the bytes land
  :1237-1259 _corpus_geometry — EVERY measured figure carries a sibling *_formula / *_denominator /
             *_source string. Copy that discipline for the scored-token rows.

tests/test_phase23_budget.py:359-380 — `_cost_record()`: one small parser per record, path resolved
  from the owning module's constant, never a test-local literal.

scripts/phase18_extraction.py — READ ONLY: CORPUS_PATH (:697), corpus_sha256(corpus) (:758)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The D-05 four-corner band check</name>
  <files>tests/test_phase24_band.py, scripts/phase24_adversarial.py</files>
  <read_first>
    - `tests/test_phase24_bins.py` (24-06's output — the `_build` fixture and scripts-path preamble
      to reuse)
    - `scripts/teach_persona.py` lines 120-130 and 528-560 (`MASK_FRACTION_BAND`,
      `_prove_floor_and_band` and the aggregate check at :549)
    - `scripts/phase24_adversarial.py` (`MIN_REFUSAL_SCORED_TOKENS`, `MASK_FRACTION_MARGIN`)
    - `.planning/phases/24-.../24-RESEARCH.md` § "Band bounds and headroom" and § "The D-05
      calibration: exact headroom at the upper extreme"
  </read_first>
  <action>
Create `tests/test_phase24_band.py`. Build-only, CPU-only, no training and no GPU (~4 s per corner).

Parametrize over the FOUR corners `{"adv_n8", "adv_n64"} x {ADVERSARIAL_RATIO_GRID[0],
ADVERSARIAL_RATIO_GRID[-1]}`, with the ratios IMPORTED from `mitigation_budget` and never retyped.
D-05 says "both extremes"; D-07 runs the same nominal grid at both capacities, so the grid has FOUR
corners and the binding one is `(adv_n8, upper)`.

Three named tests:

1. `test_all_four_grid_corners_clear_the_mask_fraction_floor_with_margin` — for each corner resolve
   the arm's facts via `tp.arm_spec(arm)`, render with `tp.render_episodes(facts,
   fs.TAUGHT_FAMILY_IDS)`, build under `tmp_path` with `align_facts=None` and the corner's
   `adversarial_ratio`, then assert
   `stats["mask_fraction"] >= tp.MASK_FRACTION_BAND[0] + phase24_adversarial.MASK_FRACTION_MARGIN`.
   Band and margin are both IMPORTED. The assert message prints the corner, the measured fraction,
   the floor, the margin and the shortfall, so a red test is self-explaining.
   Also assert `stats["mask_fraction"] <= tp.MASK_FRACTION_BAND[1]`, with a comment recording that
   the ceiling is unreachable on this axis (both effects push down) and that the assertion exists so
   the claim is checked rather than argued.

2. `test_the_binding_corner_is_n8_at_the_upper_extreme` — assert the measured fraction at
   `(adv_n8, upper)` is STRICTLY LESS than at `(adv_n64, upper)`. This pins 24-RESEARCH's refinement
   as a property: n=64's larger clean bin dilutes the attack pool's unmasked prompts relative to its
   own scored mass, so pinning against n=64 would be pinning against the easier corner. If this ever
   inverts, the corner analysis needs redoing and the message must say so.

3. `test_the_control_corner_reproduces_the_measured_flat_operating_point` — at
   `adversarial_ratio == 0.0` assert `stats["mask_fraction"]` equals 0.358660 (adv_n8) and
   0.390163 (adv_n64) to six decimal places. The docstring names these as the FLAT-pack figures
   measured at HEAD and names both traps explicitly — the aligned 0.321813 / 0.347701 (depressed by
   mask=0 padding) and the v3.0 CAL figures 0.3426 / 0.3854 / 0.3778 (a different arm set,
   structurally unreachable for a v4.0 arm) — stating that neither may be substituted.

If any corner FAILS test 1, that is D-05 doing its job. The remedy is to LENGTHEN the refusal
templates in `scripts/phase24_adversarial.py` — raising `MIN_REFUSAL_SCORED_TOKENS` and the per-slot
noun phrases until the worst corner clears — then re-run `tests/test_phase24_refusal.py` and the
D-02 sibling guard. Record the calibration act and the before/after fractions in the SUMMARY.
Do NOT widen `MASK_FRACTION_BAND`; the band is Phase 14's and is not this phase's to move.
  </action>
  <acceptance_criteria>
    - `pytest -q tests/test_phase24_band.py` reports 0 failed and completes in under 60 seconds. This EXCEEDS 24-VALIDATION.md's 20-second feedback contract, deliberately and with the reason recorded there: the contract governs the after-every-task-commit SAMPLING command (the three-file quick run, ~15 s), while this module builds four bins, the largest of which packs 1408 clean + 2688 adversarial episodes. Order the parametrization so the BINDING corner `(adv_n8, upper)` runs FIRST, so the corner most likely to fail fails in the first few seconds
    - `pytest -q "tests/test_phase24_band.py::test_all_four_grid_corners_clear_the_mask_fraction_floor_with_margin[adv_n8-upper]"` (or the equivalent generated node id) completes in under 20 seconds on its own — the fast path for the binding corner, which is what 24-VALIDATION.md's contract actually needs
    - `.venv/bin/python -c "import ast,pathlib; t=ast.parse(pathlib.Path('tests/test_phase24_band.py').read_text()); bad=[n.value for n in ast.walk(t) if isinstance(n,ast.Constant) and isinstance(n.value,float) and n.value in (0.15,0.95,1.9090909090909092)]; assert not bad, bad; print('ok')"` passes — no band bound and no grid value is retyped as a float literal; both come from `tp.MASK_FRACTION_BAND` and `mitigation_budget.ADVERSARIAL_RATIO_GRID`. **AST, not `grep -n "0\.15\|0\.95\|1\.909"`:** this same task instructs a comment recording that the 0.95 ceiling is unreachable and a docstring naming the band, and a line grep cannot tell a retyped constant from prose about one. The two control figures `0.358660` / `0.390163` ARE float literals and are deliberately allowed — they are the measured operating point test 3 pins
    - The measured fraction at each of the four corners is recorded in the SUMMARY
    - The `(adv_n8, upper)` fraction is at least `0.15 + MASK_FRACTION_MARGIN`; if it was not on first run, the SUMMARY records the template lengthening and the before/after figures
    - `pytest -q tests/test_phase24_refusal.py tests/test_phase14_scoring.py` stays green after any template change
    - The run succeeds with no CUDA device and without touching `data/`
  </acceptance_criteria>
  <verify>
    <automated>pytest -q tests/test_phase24_band.py tests/test_phase24_refusal.py</automated>
  </verify>
  <done>All four corners are measured and clear the floor with the pinned margin; the binding corner is confirmed to be n=8 at the upper extreme; the control corner reproduces the flat operating point to six decimals.</done>
</task>

<task type="auto">
  <name>Task 2: Emit the committed ADVT-03 per-arm scored-token record</name>
  <files>scripts/phase24_record.py, results/phase24_token_budget.json</files>
  <read_first>
    - `scripts/phase21_unit_record.py` lines 160-200, 240-305, 695-760 and 1230-1300 (the
      `ARTIFACTS` register at :168, `_PUBLICATION_PATHSPEC` at :183, `_DIRTY_DETAIL` at :188,
      `refuse_existing_artifacts` at :246 with its reusable `paths=` parameter,
      `is_publication_target` at :257 and `refuse_dirty_publication` at :269 — **read :257-267 and
      see for yourself that it returns `None` for any path outside `ARTIFACTS`**, `_provenance`, and
      `_corpus_geometry`'s "every figure carries its own denominator and its own source label" row
      shape)
    - `src/personacore/provenance.py` lines 47-80 (`refuse_if_dirty(*, who, detail, pathspec=(),
      cwd=None)` — the refusal you call directly, and its docstring on why callers name their own
      pathspec scope)
    - `scripts/teach_persona.py` lines 465-530 (the stats dict and the additive keys 24-06 added)
    - `scripts/mitigation_budget.py` (`ADVERSARIAL_RATIO_GRID` and its provenance dict)
    - `scripts/phase18_extraction.py` lines 693-700 and 755-770 (`CORPUS_PATH`, `corpus_sha256`) —
      READ ONLY
    - `.planning/ROADMAP.md` — **locate by HEADING, never by line number.** Read `### Phase 25:
      Frontier Sweep and the Existence-Gate Verdict`, then its Success Criteria bullets **3** (every
      ε reported at both granularities, with its unit, sampler and multiplicity in the same
      sentence) and **4** (`results/phase2X_frontier.json` as the single source of truth — counts
      never rates, `accounting: null` on the adversarial arm, module sha256s travelling in the
      artifact). Those are what this record must feed. A line range would be wrong by the time you
      read it: plan 24-03 runs in wave 1 and INSERTS a dated continuation block into Phase 24's SC2,
      shifting every line below it.
  </read_first>
  <action>
Create `scripts/phase24_record.py`, in `scripts/phase21_unit_record.py`'s register.

Path constant, resolved once and exported so every reader uses it:
`TOKEN_BUDGET_RECORD = _ROOT / "results" / "phase24_token_budget.json"`.
No test and no downstream module may spell that path again.

The emitter builds ONE document with these top-level blocks, `provenance` LAST:

- `"grid"` — `list(mitigation_budget.ADVERSARIAL_RATIO_GRID)` plus its provenance dict, IMPORTED.
- `"rows"` — one row per `(arm, ratio)` over `{"adv_n8", "adv_n64"} x ADVERSARIAL_RATIO_GRID`, so
  12 rows. Each row builds the bins under a scratch directory (never `data/`, never `results/`)
  through `tp.build_bins(..., align_facts=None, adversarial_ratio=ratio, seed=tp.SEED)` and records:
    `arm`, `n_facts`, `adversarial_ratio`,
    `clean_episodes`, `adversarial_episodes`, `adversarial_pool_size`, `adversarial_multiplicity`,
    `total_tokens`, `teaching_tokens`,
    `scored_tokens` — an INTEGER COUNT read off the written mask bin
      (`int(np.fromfile(mask_path, dtype=np.uint8).sum())`), never `mask_fraction * total_tokens`,
    `scored_tokens_denominator` — "total_tokens (the concatenated flat pack; no padding on this
      branch)",
    `scored_tokens_source` — "MEASURED: sum over the mask bin actually written to disk",
    `adversarial_tokens`, `adversarial_scored_tokens`,
    `mask_fraction`, `mask_fraction_source` — "build_bins stats; the AGGREGATE mean
      _prove_floor_and_band checks at teach_persona.py:549",
    `mask_fraction_margin_over_floor`, `mask_fraction_band`.
  Counts, never rates, wherever a count exists. Every measured figure carries a sibling
  `*_denominator` / `*_source` / `*_formula` string, per `_corpus_geometry`'s discipline.
- `"band_corners"` — the four D-05 corners named, with their measured fractions, the band, the
  margin constant, and which corner is binding.
- `"token_budget_disclosure"` — ADVT-03's confound, stated so it can never be silently collapsed:
  the per-family `core_taught` mean and total prompt tokens for A1-mild / A1-aggressive / A3 and,
  listed SEPARATELY as held out, A2; `cross_family_inflation` = 3.73 with a
  `cross_family_inflation_note` stating in words that this is A3/A2 and is NOT the 1.40x figure
  ADVT-03 quotes for one uppercased 51-character sentence, and that the two must never be reported
  as if they were the same number; and `leave_one_out_token_spread` = 1.59 at identical episode
  count, which is D-06's reason for choosing the episode unit and pushing token variation into this
  report rather than into the swept axis.
- `"attack_corpus"` — `path` (from `p18.CORPUS_PATH`, relative to the repo root), `sha256` (from
  `p18.corpus_sha256` on the loaded corpus), `trained_families`, `held_out_family`,
  `held_out_reason` = value containment (D-10/D-12, never selection by performance), and the SC4
  discharge: `new_attack_corpus: false`, `inflation_report_required: false`, and an
  `inflation_report_discharge` string stating that SC4's inflation-report obligation attaches to a
  NEW corpus and this phase creates none — it reuses `results/phase18_corpus.json` verbatim,
  read-only, with the digest above travelling in this record.
- `"provenance"` — `_provenance`'s shape: `git_sha`, `written_utc`, `python`, `seed`, plus
  `tokenizer_sha256` (of `tp.TOKENIZER_PATH`) and the sha256 of `scripts/phase24_adversarial.py`
  and `scripts/mitigation_budget.py`, so the templates and the grid that produced these numbers are
  pinned to their bytes.

Write through a `_write(path, document)` that runs BOTH refusals BEFORE the bytes land, then
`json.dumps(..., indent=2, sort_keys=False) + "\n"`.

  * **Refuse-to-rerun:** `phase21_unit_record.refuse_existing_artifacts(paths=[TOKEN_BUDGET_RECORD])`
    — IMPORTED, never reinvented. Its `paths=` parameter is what makes it reusable here.

  * **Refuse-if-dirty: do NOT call `phase21_unit_record.refuse_dirty_publication`.** MEASURED at
    HEAD: it opens with `if not is_publication_target(path): return None`
    (`scripts/phase21_unit_record.py:257-267`), and `is_publication_target` compares the resolved
    path against `ARTIFACTS.values()`, which holds ONLY `phase21_privacy_unit.json` and
    `phase21_multiplicity.json`. For `results/phase24_token_budget.json` it returns `None`
    immediately and protects nothing — a green-and-blind guard, and T-24-42's mitigation would be a
    line that cannot fire. Call the underlying refusal DIRECTLY instead:

        from personacore.provenance import refuse_if_dirty

    with `who="phase24_record"`, a `detail` string in `phase21_unit_record._DIRTY_DETAIL`'s register
    (`provenance.git_sha()` records HEAD at write time, so a record written from a dirty tree names
    a commit that does NOT contain the code that produced it — cite 21-REVIEW.md CR-02 as the
    precedent that this is not hypothetical), `cwd=_ROOT`, and a PHASE-24 pathspec derived from the
    record path rather than retyped:

        pathspec = (".", f":(exclude){TOKEN_BUDGET_RECORD.relative_to(_ROOT)}")

    The exclusion is what makes the guard reachable at all — re-emitting requires deleting the
    previous artifact first, which is itself a dirty tree. Same reasoning, same shape, as
    `_PUBLICATION_PATHSPEC` (`:183-186`).

  * **WATCH IT FIRE, cheaply and BEFORE the real emission.** With the artifact still absent (so
    refuse-to-rerun passes and control reaches the dirty check), touch a scratch file under
    `scripts/` and call `_write(TOKEN_BUDGET_RECORD, {"probe": True})` directly from `python -c` —
    do NOT run `main()` for this, it would build all twelve bins to reach a guard that fires in a
    millisecond. Capture the `[phase24_record] REFUSING: the working tree is dirty.` output, delete
    the scratch file, and confirm no bytes landed. Record the captured refusal in the SUMMARY
    exactly as the refuse-to-rerun one is. A dirty-tree guard whose RED was never observed is the
    shape this plan is correcting.

Provide a `main()` behind `if __name__ == "__main__":` so the record is regenerable.

**COMMIT ORDER IS LOAD-BEARING, and it is a consequence of the guard, not a workaround for it.**
`refuse_if_dirty` counts UNTRACKED files as dirty (`src/personacore/provenance.py:62-65`: "A `.py`
that is not in HEAD cannot be at the recorded SHA either"). So `scripts/phase24_record.py` must be
COMMITTED FIRST, and only then may the emitter run — otherwise the emitter refuses to write a record
whose `provenance.git_sha` would name a commit that does not contain the emitter. The order is:

  1. commit `scripts/phase24_record.py` (with the two refusals watched firing on the scratch probe
     above, which happens before this commit and needs no clean tree — it is asserting the RED),
  2. run `main()` on the now-clean tree to produce `results/phase24_token_budget.json`,
  3. commit the artifact.

Do NOT work around a refusal by writing to a temporary path and copying the file into `results/` —
`phase21_unit_record._DIRTY_DETAIL` names that exact workaround as reproducing CR-02 with extra
steps.

Never write into `data/`. Never touch `results/phase18_corpus.json`,
`scripts/phase18_extraction.py` or `scripts/mitigation_gate.py`.
  </action>
  <acceptance_criteria>
    - `results/phase24_token_budget.json` exists, its last top-level key is `provenance`, and `rows` has 12 entries
    - `git log --oneline` shows `scripts/phase24_record.py` committed BEFORE `results/phase24_token_budget.json` — the emitter refuses an untracked-code tree, so a record committed in the same commit as its emitter would mean the guard was bypassed
    - `.venv/bin/python -c "import json,pathlib,subprocess; d=json.loads(pathlib.Path('results/phase24_token_budget.json').read_text()); sha=d['provenance']['git_sha']; out=subprocess.run(['git','cat-file','-e',f'{sha}:scripts/phase24_record.py'],capture_output=True); assert out.returncode==0, 'the recorded git_sha does not contain the emitter that wrote this record'; print('ok')"` passes — CR-02's defect, checked rather than trusted
    - Every row carries a positive integer `scored_tokens` and a non-empty `scored_tokens_denominator`
    - `attack_corpus.new_attack_corpus` is `false` and `attack_corpus.held_out_family` is `"A2"`
    - Re-running the emitter without deleting the artifact RAISES (refuse-to-rerun observed once and quoted in the SUMMARY)
    - Running the emitter with a scratch file present under `scripts/` RAISES with `[phase24_record] REFUSING: the working tree is dirty.` (refuse-if-dirty observed once and quoted in the SUMMARY) — this is the acceptance criterion that would have caught `refuse_dirty_publication` silently returning `None` for a phase-24 path
    - `grep -n "refuse_dirty_publication" scripts/phase24_record.py` returns NOTHING — it is a no-op outside `phase21_unit_record.ARTIFACTS`; `refuse_if_dirty` is called directly instead
    - `grep -n "refuse_if_dirty\|refuse_existing_artifacts" scripts/phase24_record.py` shows both refusals present and both IMPORTED, never redefined
    - `git status --porcelain data/` is empty; `git diff scripts/phase18_extraction.py scripts/mitigation_gate.py` is EMPTY
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -c "import json,pathlib; d=json.loads(pathlib.Path('results/phase24_token_budget.json').read_text()); assert list(d)[-1]=='provenance'; assert len(d['rows'])==12; assert all(isinstance(r['scored_tokens'],int) and r['scored_tokens']>0 for r in d['rows']); assert d['attack_corpus']['new_attack_corpus'] is False; print('ok')"</automated>
  </verify>
  <done>The record exists with 12 per-arm-per-point rows carrying integer scored-token counts and their denominators, the token-budget confound is disclosed with both figures kept distinct, SC4's inflation obligation is discharged explicitly, and the writer refuses to rerun over its own artifact.</done>
</task>

<task type="auto">
  <name>Task 3: Re-derive the record and assert its band clearance</name>
  <files>tests/test_phase24_record.py</files>
  <read_first>
    - `scripts/phase24_record.py` (Task 2's output)
    - `results/phase24_token_budget.json` (Task 2's output)
    - `tests/test_phase23_budget.py` lines 355-380 and 499-560 (`_cost_record`, and the
      re-derivation register: exact `==`, never a pre-reduced scalar read back out)
    - `scripts/mitigation_budget.py`
  </read_first>
  <action>
Create `tests/test_phase24_record.py`. One small parser helper resolving the path from
`phase24_record.TOKEN_BUDGET_RECORD` — the path is never spelled twice.

Five named tests:

1. `test_the_record_covers_every_grid_point_at_both_capacities` — the set of
   `(row["arm"], row["adversarial_ratio"])` equals the full cross product of `{"adv_n8","adv_n64"}`
   with `mitigation_budget.ADVERSARIAL_RATIO_GRID`, under hard set equality. Non-vacuity: assert the
   grid is non-empty and that both arms appear.

2. `test_every_row_reports_counts_with_a_denominator` — for every row: `scored_tokens` is an `int`
   in `[0, total_tokens]`; `scored_tokens_denominator`, `scored_tokens_source` and
   `mask_fraction_source` are non-empty strings; and `scored_tokens / total_tokens` agrees with
   `mask_fraction` to within 1e-9. This is the counts-never-rates discipline made checkable — the
   rate is derivable from the counts, and the counts are what is stored.

3. `test_scored_tokens_re_derive_from_a_rebuild` — pick the two control rows
   (`adversarial_ratio == 0.0`), rebuild those bins under `tmp_path` through the same
   `tp.build_bins` call, and assert the rebuilt `scored_tokens` equals the recorded value under
   exact `==`. A hand-edited number goes red here. Rebuild only the two cheap control rows so the
   test stays inside the sampling budget; state that scoping in the docstring.

4. `test_every_point_clears_the_band` — for every row,
   `mask_fraction >= MASK_FRACTION_BAND[0] + phase24_adversarial.MASK_FRACTION_MARGIN`, and
   `mask_fraction` is monotonically NON-INCREASING in `adversarial_ratio` within each arm. The
   monotonicity assertion is the measured claim that only the floor binds; if it ever inverts, the
   "both effects push down" reasoning behind ignoring the 0.95 ceiling stopped holding.

5. `test_the_token_budget_confound_keeps_both_figures_distinct` — assert
   `token_budget_disclosure["cross_family_inflation"]` is 3.73 and that its note string names A3/A2
   and states it is not the 1.40x single-sentence figure; assert 1.40 does NOT appear as the value
   of any inflation field. Also assert `attack_corpus["held_out_family"]` is absent from
   `attack_corpus["trained_families"]` and that `attack_corpus["sha256"]` equals
   `p18.corpus_sha256(json.load(p18.CORPUS_PATH))` recomputed live — never pasted.

Finally run the phase-wide suite and confirm `make test` is green.
  </action>
  <acceptance_criteria>
    - `pytest -q tests/test_phase24_record.py` reports 5 passed, 0 failed
    - `.venv/bin/python -c "import ast,pathlib; t=ast.parse(pathlib.Path('tests/test_phase24_record.py').read_text()); docs={id(n.body[0].value) for n in ast.walk(t) if isinstance(n,(ast.Module,ast.FunctionDef,ast.AsyncFunctionDef,ast.ClassDef)) and n.body and isinstance(n.body[0],ast.Expr) and isinstance(n.body[0].value,ast.Constant) and isinstance(n.body[0].value.value,str)}; bad=[n.value for n in ast.walk(t) if isinstance(n,ast.Constant) and isinstance(n.value,str) and 'phase24_token_budget' in n.value and id(n) not in docs]; assert not bad, bad; print('ok')"` passes — no EXECUTABLE string literal spells the record path; it is resolved from `phase24_record.TOKEN_BUDGET_RECORD`. **AST with docstrings excluded, not `grep -n "phase24_token_budget"`:** this module will plausibly name its subject record in a docstring, and naming it in prose is not the defect — spelling the path a second time in code is
    - `grep -c "TOKEN_BUDGET_RECORD" tests/test_phase24_record.py` is >= 1 — the path is resolved from the owning module's constant
    - Hand-editing one `scored_tokens` value in the committed record makes `test_scored_tokens_re_derive_from_a_rebuild` FAIL; restore and confirm GREEN, and record both in the SUMMARY
    - `make test` reports 0 failed
    - `git diff scripts/phase18_extraction.py scripts/mitigation_gate.py` is EMPTY
  </acceptance_criteria>
  <verify>
    <automated>pytest -q tests/test_phase24_record.py tests/test_phase24_band.py</automated>
  </verify>
  <done>The record's coverage, counts, denominators and band clearance are all asserted; the scored-token counts re-derive from a live rebuild under exact equality; the hand-edit was watched failing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stdout figure → published report | A number that cannot be re-read from a committed file is not a measurement |
| build-time band guard → spent compute | `_prove_floor_and_band` fires AFTER a sweep point's investment unless it is measured first |
| two different inflation figures → one report | 3.73x (cross-family) and 1.40x (one uppercased sentence) describe different things and are trivially conflated |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-38 | Repudiation | ADVT-03 provenance loss | high | mitigate | `results/phase24_token_budget.json` is committed with per-arm integer counts, denominators, source labels, corpus sha256 and module sha256s; Task 3 test 3 re-derives the control rows under exact `==` and the hand-edit is watched failing |
| T-24-39 | Denial of Service | build-time `SystemExit` on the band | high | mitigate | Task 1 measures all four D-05 corners in a four-second CPU test before any sweep point exists; the margin comes from `MASK_FRACTION_MARGIN`, never a retyped literal |
| T-24-40 | Spoofing | mask-fraction substitution | high | mitigate | Task 1 test 3 pins the control corner to the FLAT figures to six decimals and names both trap figure sets in its docstring so a future reader cannot substitute the aligned or v3.0 values |
| T-24-41 | Spoofing | inflation-figure conflation | medium | mitigate | Task 3 test 5 asserts the note distinguishes 3.73x from 1.40x and that 1.40 is not the value of any inflation field |
| T-24-42 | Tampering | write-once artifact | medium | mitigate | `phase21_unit_record.refuse_existing_artifacts(paths=[TOKEN_BUDGET_RECORD])` AND `personacore.provenance.refuse_if_dirty` called DIRECTLY with a phase-24 pathspec, both BEFORE the bytes land. `phase21_unit_record.refuse_dirty_publication` is deliberately NOT used: `is_publication_target` (`:257-267`) compares against `ARTIFACTS.values()` — two phase-21 paths — so it returns `None` for `results/phase24_token_budget.json` and protects nothing. BOTH refusals are watched firing once and quoted in the SUMMARY |
| T-24-43 | Tampering | frozen inputs | high | mitigate | `git diff scripts/phase18_extraction.py scripts/mitigation_gate.py` EMPTY is an acceptance criterion in all three tasks' verification; the corpus digest is recomputed live, never pasted |
| T-24-44 | Repudiation | SC4 inflation-report obligation | medium | mitigate | Discharged EXPLICITLY in the record (`new_attack_corpus: false` + a written discharge string) rather than by silence |
| T-24-SC | Tampering | package installs | low | accept | No `pip install` in this plan; `json`, `hashlib`, `platform`, `datetime` are stdlib and `numpy` is already a core dependency |
</threat_model>

<verification>
- `pytest -q tests/test_phase24_band.py tests/test_phase24_record.py tests/test_phase24_refusal.py tests/test_phase24_bins.py` — 0 failed
- `make test` green
- `results/phase24_token_budget.json` is committed and its 12 rows carry integer scored-token counts
- `git diff scripts/phase18_extraction.py scripts/mitigation_gate.py` is EMPTY
- `git status --porcelain data/` is empty
</verification>

<success_criteria>
- All four D-05 corners are measured and clear the 0.15 floor with the pinned margin, binding corner confirmed as n=8 at the upper extreme
- `results/phase24_token_budget.json` reports scored-token counts per arm at every grid point, with multiplicity and denominators in the same row
- The token-budget confound is disclosed with the 3.73x cross-family figure kept distinct from ADVT-03's 1.40x single-sentence figure
- SC4's inflation-report obligation is discharged explicitly, and the corpus digest travels in the record
</success_criteria>

<output>
Create `.planning/phases/24-adversarial-extraction-aware-training-the-held-out-attack-fa/24-07-SUMMARY.md`
when done. It MUST record the four measured corner fractions, the refuse-to-rerun output, the refuse-if-dirty
output, the measured wall-clock of `pytest -q tests/test_phase24_band.py` (and of the binding corner
alone), and the hand-edit RED observation from Task 3.
</output>
