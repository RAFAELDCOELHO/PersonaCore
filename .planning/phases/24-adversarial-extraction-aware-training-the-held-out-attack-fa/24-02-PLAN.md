---
phase: 24-adversarial-extraction-aware-training-the-held-out-attack-fa
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/mitigation_budget.py
  - tests/test_phase24_grid.py
autonomous: true
requirements: [ADVT-01, ADVT-03]

must_haves:
  truths:
    - "D-09: the grid's upper extreme is 1.9090909090909092 = 336/176, a MEASURED public quantity (3 trained attack families x 112 core_taught prompts, over 176 clean episodes) — not a round number chosen because it is round"
    - "D-09: the lower extreme is 0.0, the control that is byte-identical to v2.0 and reconnects the curve by construction"
    - "The grid is a resource parameter: it sizes the spend and decides no outcome, which is why it lives in scripts/mitigation_budget.py behind the AST guard forbidding the gate from importing the budget"
    - "The pin is a FLOAT LITERAL — never the expression 336/176, which is an ast.BinOp and reddens tests/test_phase23_budget.py:463"
  artifacts:
    - path: "scripts/mitigation_budget.py"
      provides: "ADVERSARIAL_RATIO_GRID + ADVERSARIAL_RATIO_GRID_PROVENANCE, additive, zero deletions"
      contains: "ADVERSARIAL_RATIO_GRID"
    - path: "tests/test_phase24_grid.py"
      provides: "re-derivation of both extremes from the committed corpus and the committed episode geometry"
  key_links:
    - from: "tests/test_phase24_grid.py"
      to: "results/phase18_corpus.json"
      via: "phase18_extraction.CORPUS_PATH, counted for the 336 numerator"
      pattern: "CORPUS_PATH"
    - from: "tests/test_phase24_grid.py"
      to: "results/phase21_multiplicity.json"
      via: "corpus_geometry episodes, the 176 denominator"
      pattern: "corpus_geometry"
---

<objective>
Pin D-09's adversarial sweep grid into `scripts/mitigation_budget.py` as literal constants, and
prove both extremes re-derive from committed artifacts rather than from an author's preference.

Purpose: sweep width is a RESOURCE parameter. The ROADMAP's pre-registration boundary already places
resource parameters in the budget module, behind the guard that forbids the gate from importing it.
Pinning the extremes before any point runs is what makes D-05's calibration order structurally
honest — the template length cannot have been tuned after seeing a real point if the points were
named first.

Output: two literal assignments and a test that re-derives them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/24-adversarial-extraction-aware-training-the-held-out-attack-fa/24-CONTEXT.md
@.planning/phases/24-adversarial-extraction-aware-training-the-held-out-attack-fa/24-PATTERNS.md

<interfaces>
<!-- Verified at HEAD. Two hard landmines; both are load-bearing. -->

tests/test_phase23_budget.py::test_budget_holds_only_literal_constants   # :430
  * iterates `tree.body` after the module docstring and asserts EVERY node is `ast.Assign`.
    An ANNOTATED assignment (`X: tuple = (...)`) is `ast.AnnAssign` and FAILS. Plain assignment only.
  * calls `ast.literal_eval(node.value)` on every assignment (:463) — "raises on anything that is
    not a literal". `336 / 176` is an `ast.BinOp` and RAISES. The value must be the float literal
    `1.9090909090909092`, with the derivation living in the comment block above it.
  * bans `Import, ImportFrom, FunctionDef, AsyncFunctionDef, ClassDef, If, For, While, With, Try`
    anywhere in the tree (:466-477). The allow-set across all `scripts/mitigation_*.py` is ALREADY
    exactly `{pathlib, sys, erasure_gate}` with ZERO headroom — one `import math` here turns a
    committed guard RED.
  * asserts the module still OPENS with its docstring (that docstring carries the zero-import
    ceiling justification and may not be displaced).

scripts/mitigation_budget.py — the shape to copy
  :360-374  a five-line `input / rule / output / evidence` comment block, then the constant
  :374      SWEEP_POINTS = 16
  :376-403  SWEEP_POINTS_PROVENANCE = { record, record_sha256, git_sha, derivation, sized_against,
            selected_by, selected_value, selected_reply_verbatim, governs }
  :11-14    the module docstring carries its own DATED CONTINUATION rather than an edit

MEASURED at HEAD this session (do not re-derive, do not trust older figures):
  results/phase18_corpus.json — 864 prompts; families A1-mild / A1-aggressive / A2 / A3, 216 each,
      112 of them `core_taught` each. The three D-10 TRAINED families give 3 * 112 = 336.
  results/phase21_multiplicity.json `corpus_geometry` — clean episodes 176 (dp_n8) / 1408 (dp_n64).
  336 / 176 = 1.9090909090909092  (the n=8 no-repetition pool ceiling)
  n=64 no-repetition ceiling = 336 / 1408 = 0.2386...; at ratio 1.9090909090909092 the n=64
      multiplicity is 8.0x, reported per point by the D-07 instrument.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin ADVERSARIAL_RATIO_GRID and its provenance as literals</name>
  <files>scripts/mitigation_budget.py</files>
  <read_first>
    - `scripts/mitigation_budget.py` in full (it is short) — especially the module docstring's
      zero-import ceiling paragraph, the `input / rule / output / evidence` comment block at
      :360-374, and `SWEEP_POINTS_PROVENANCE` at :376-403
    - `tests/test_phase23_budget.py` lines 415-500 (`test_budget_holds_only_literal_constants`)
  </read_first>
  <action>
APPEND to `scripts/mitigation_budget.py`. Zero deletions. Do not reflow, reorder or reword any
existing constant, comment or docstring line.

Add one five-line `input / rule / output / evidence` comment block in the file's own register,
then:

`ADVERSARIAL_RATIO_GRID = (0.0, 0.25, 0.5, 1.0, 1.5, 1.9090909090909092)`

- Plain assignment, NO annotation (`ast.AnnAssign` fails the guard).
- Every element a float literal. `1.9090909090909092` is written out; the derivation `336 / 176`
  belongs in the comment block, NEVER in code — a `BinOp` raises inside the guard's
  `ast.literal_eval`.
- Six points. The two EXTREMES are D-09 and are not discretionary: `0.0` (the control, byte-identical
  to v2.0) and `1.9090909090909092` (the n=8 no-repetition pool ceiling). The four interior points
  and their spacing are the planner's discretion under 24-CONTEXT § Claude's Discretion; `1.0` is
  included because episode parity is the legible midpoint and `0.25` because it is the first point
  above n=64's own no-repetition ceiling (0.2386), i.e. the first point at which D-07 multiplicity
  becomes non-trivial at the large capacity.

`ADVERSARIAL_RATIO_GRID_PROVENANCE = { ... }` — a plain dict literal, in
`SWEEP_POINTS_PROVENANCE`'s shape, carrying at minimum these string/number/tuple literal keys:

- `"unit"`: that the ratio is adversarial EPISODES per clean EPISODE (D-06), and that token volume
  floats as a consequence and is reported after the fact by the ADVT-03 record, never swept.
- `"upper_extreme"`: `1.9090909090909092`
- `"upper_extreme_derivation"`: the sentence "3 trained attack families x 112 core_taught prompts
  = 336 adversarial episodes, over 176 clean episodes at n=8 (results/phase21_multiplicity.json
  corpus_geometry) — the largest point n=8 reaches without repeating anything"
- `"upper_extreme_sources"`: the two record paths, as strings
- `"lower_extreme"`: `0.0`, with `"lower_extreme_meaning"` naming it the control that is
  byte-identical to v2.0 and reconnects the curve by construction
- `"trained_families"`: `("A1-mild", "A1-aggressive", "A3")`
- `"held_out_family"`: `"A2"`, with `"held_out_reason"` = value containment (D-10/D-12), NEVER
  selection by performance
- `"point_count"`, `"point_count_selected_by"`: that the interior count and spacing are a RESOURCE
  parameter chosen by the planner under Claude's Discretion, that they size the spend and decide no
  outcome, and that only the two extremes are pre-registered by D-09
- `"multiplicity_at_upper_extreme"`: `{"dp_n8": 1.0, "dp_n64": 8.0}` — Phase 25 SC3 requires
  multiplicity to travel in the same sentence
- `"governs"`: the number of adversarial sweep points per capacity in Phase 25's frontier
- `"git_sha"`: the commit sha this pin lands at, as a string literal

Do NOT add any import, function, class, `if`, `for` or `try` to this file. Do NOT register this
file as a `prereg_artifact=` anywhere — `test_the_budget_module_is_protected_but_not_frozen`
(`tests/test_phase23_budget.py:726`) asserts it is protected but NOT frozen, and a freeze is
irrevocable.
  </action>
  <acceptance_criteria>
    - `pytest -q tests/test_phase23_budget.py` reports 0 failed
    - `pytest -q tests/test_phase23_budget.py::test_budget_holds_only_literal_constants` passes
    - `pytest -q tests/test_phase23_budget.py::test_the_import_ceiling_still_has_zero_headroom` passes
    - `pytest -q tests/test_phase23_budget.py::test_the_budget_module_is_protected_but_not_frozen` passes
    - `.venv/bin/python -c "import ast,pathlib; t=ast.parse(pathlib.Path('scripts/mitigation_budget.py').read_text()); print(all(isinstance(n,ast.Assign) for n in t.body[1:]))"` prints `True`
    - `git diff --numstat scripts/mitigation_budget.py` shows `0` deletions
    - `.venv/bin/python -c "import sys; sys.path.insert(0,'scripts'); import mitigation_budget as b; print(b.ADVERSARIAL_RATIO_GRID)"` prints a 6-tuple whose first element is `0.0` and last is `1.9090909090909092`
  </acceptance_criteria>
  <verify>
    <automated>pytest -q tests/test_phase23_budget.py</automated>
  </verify>
  <done>Both constants are literal assignments, the AST guard is green, the import ceiling is unmoved, and the module has zero deleted lines.</done>
</task>

<task type="auto">
  <name>Task 2: Re-derive both extremes from the committed artifacts</name>
  <files>tests/test_phase24_grid.py</files>
  <read_first>
    - `scripts/mitigation_budget.py` (Task 1's output)
    - `tests/test_phase23_budget.py` lines 355-380 (`_cost_record` — one small parser per record,
      path resolved from the OWNING MODULE's constant, never a test-local literal)
    - `tests/test_phase19_erasure.py` lines 2390-2400 (the only existing reader of
      `results/phase18_corpus.json`, and the `phase18_extraction.CORPUS_PATH` resolution rule at
      `scripts/phase18_extraction.py:697`)
  </read_first>
  <action>
Create `tests/test_phase24_grid.py`. Every path resolves from its owning module's constant:
`phase18_extraction.CORPUS_PATH` for the corpus; the Phase 21 multiplicity record's path from
`scripts/phase21_unit_record.py`'s `ARTIFACTS` register (`:168-172`) — never a test-local string.

Four named tests:

1. `test_the_upper_extreme_re_derives_from_the_committed_corpus` — count `core_taught` rows in
   `results/phase18_corpus.json` whose `family` is in
   `mitigation_budget.ADVERSARIAL_RATIO_GRID_PROVENANCE["trained_families"]`; assert that count is
   336. Read the n=8 clean episode count from the multiplicity record's `corpus_geometry` row whose
   `arm == "dp_n8"`; assert 176. Then assert
   `ADVERSARIAL_RATIO_GRID[-1] == counted_adversarial / counted_clean` under exact `==`, so a
   hand-edited digit cannot reach a consumer. Non-vacuity: assert the family filter selected a
   non-empty subset AND that it EXCLUDED something (the corpus must still contain `A2` rows), or the
   filter is green by matching everything.

2. `test_the_lower_extreme_is_the_control` — `ADVERSARIAL_RATIO_GRID[0] == 0.0` exactly, and the
   grid is strictly increasing (`list(grid) == sorted(grid)` and no duplicates), so "the extremes
   run first" is well-defined at both ends.

3. `test_the_held_out_family_is_not_a_trained_family` — the provenance dict's `held_out_family` is
   absent from its `trained_families`, and the union of the two equals the four family labels the
   corpus actually carries. This is the D-09/D-10/D-12 consistency check at the PIN; the ADVT-02
   structural check over the corpus itself is plan 24-03's and is deliberately separate.

4. `test_multiplicity_at_the_upper_extreme_re_derives` — for each capacity in the provenance's
   `multiplicity_at_upper_extreme`, recompute `ADVERSARIAL_RATIO_GRID[-1] * clean_episodes /
   adversarial_pool` from the two committed records and assert it equals the pinned value. At
   `dp_n64` that is 8.0. Phase 25 SC3 requires multiplicity to travel in the same sentence as the
   point, so it must be re-derivable, not asserted.

Use `pytest.approx` nowhere for the extreme itself — the pin is exact by construction
(`336/176` is representable and the literal was written at full float precision). Approximate
comparison is acceptable only inside test 4's multiplicity, and if used must state the tolerance.
  </action>
  <acceptance_criteria>
    - `pytest -q tests/test_phase24_grid.py` reports 4 passed, 0 failed
    - `grep -n "results/phase18_corpus.json" tests/test_phase24_grid.py` returns NOTHING (the path is resolved from `phase18_extraction.CORPUS_PATH`)
    - `grep -n "1.909" tests/test_phase24_grid.py` returns NOTHING (the extreme is imported from `mitigation_budget`, never retyped)
    - Editing the last element of `ADVERSARIAL_RATIO_GRID` by one ULP makes `test_the_upper_extreme_re_derives_from_the_committed_corpus` FAIL (observe once, then revert; `git diff --exit-code scripts/mitigation_budget.py` clean afterwards)
    - `git diff scripts/phase18_extraction.py` is EMPTY
  </acceptance_criteria>
  <verify>
    <automated>pytest -q tests/test_phase24_grid.py tests/test_phase23_budget.py</automated>
  </verify>
  <done>Both extremes re-derive from committed artifacts under exact equality, the one-ULP nudge was watched failing, and no path or figure is spelled twice.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed artifact → pinned constant | A number copied by hand can drift from the measurement it claims to summarise |
| pinned constant → the AST literal guard | A constant written as an expression silently disarms the zero-import ceiling |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-06 | Tampering | `ADVERSARIAL_RATIO_GRID` | high | mitigate | Task 2 test 1 re-derives the extreme from `results/phase18_corpus.json` + `results/phase21_multiplicity.json` under exact `==`; a one-ULP nudge is watched failing |
| T-24-07 | Denial of Service | `tests/test_phase23_budget.py:463` AST guard | high | mitigate | float literal, plain `ast.Assign`, zero imports/functions/branches; the whole budget suite is the acceptance command |
| T-24-08 | Elevation of Privilege | budget module freeze status | medium | mitigate | No `prereg_artifact=` registration is added; `test_the_budget_module_is_protected_but_not_frozen` is an acceptance criterion |
| T-24-09 | Repudiation | pre-registration order | high | mitigate | The extremes are pinned in wave 1, BEFORE the D-05 calibration (24-07) and before any training; the provenance dict records that only the extremes are pre-registered and the interior is a resource choice |
| T-24-10 | Information Disclosure | provenance dict | low | mitigate | The dict carries family LABELS and counts only — no fact value, no slot value. `scripts/mitigation_budget.py` is inside the `mitigation_*` register scanned by the import-ceiling guards |
| T-24-SC | Tampering | package installs | low | accept | No `pip install` in this plan |
</threat_model>

<verification>
- `pytest -q tests/test_phase24_grid.py tests/test_phase23_budget.py` — 0 failed
- `make test` green
- `git diff --numstat scripts/mitigation_budget.py` shows 0 deletions
- `git diff scripts/phase18_extraction.py scripts/mitigation_gate.py` is EMPTY
</verification>

<success_criteria>
- `ADVERSARIAL_RATIO_GRID` is a 6-tuple of float literals with `0.0` first and `1.9090909090909092` last
- Both extremes re-derive from committed artifacts under exact `==`
- The budget module still holds only literal assignments, still imports nothing, and is still not frozen
</success_criteria>

<output>
Create `.planning/phases/24-adversarial-extraction-aware-training-the-held-out-attack-fa/24-02-SUMMARY.md`
when done. It MUST record the counted 336 / 176 and the one-ULP RED observation.
</output>
