---
phase: 24-adversarial-extraction-aware-training-the-held-out-attack-fa
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phase14_recall.py
  - tests/test_phase24_refusal_rate.py
autonomous: true
requirements: [ADVT-01]

must_haves:
  truths:
    - "D-04: `contains_refusal(completion, templates)` mirrors `contains_value` exactly — same module, same `normalize`, same substring direction — so the frame column is a mirror, not an invention"
    - "D-04: the under-attack refusal rate and the CLEAN-frame refusal rate separate three outcomes the mixture alone confounds: refused under attack / answered wrong / refused everywhere"
    - "D-04: the live risk is named, not hidden — gate condition (b) is scored on the CLEAN recall prompt, so a refusal that generalizes from the attack frame to the clean frame collapses recall and returns FAIL"
    - "D-11: the fact-keyed-vs-frame-keyed ambiguity at n=64 is probed on the ALREADY-EXISTING Phase 21 filler recall prompts in the CLEAN frame, compared against clean LOCKED prompts — no new corpus, no new attack builder, no SC4 inflation report"
    - "D-11 reading rule, pinned in code: locked elevated + filler at the floor means the refusal is FACT-KEYED; both elevated means generic clean-frame contamination, a DIFFERENT finding"
    - "D-11 comparability, pinned in code: BOTH populations are built from the SAME explicitly pinned family set `CLEAN_FRAME_PROBE_FAMILY_IDS = (\"F1\", \"F2\", \"F6\")` — the taught set MINUS F4 and F5, which `render_family`'s docstring (:694-695) says NAME THE VALUE INSIDE THE QUESTION — and no published value appears in any returned question, asserted at runtime because the module-level D-02 scan cannot see a function's return"
  artifacts:
    - path: "scripts/phase14_recall.py"
      provides: "contains_refusal + score_refusal + CLEAN_FRAME_PROBE_FAMILY_IDS + the D-11 clean-frame probe population selector"
      contains: "def contains_refusal"
    - path: "tests/test_phase24_refusal_rate.py"
      provides: "mirror-equivalence with contains_value, normalizer agreement, and the pinned probe populations"
  key_links:
    - from: "scripts/phase14_recall.py contains_refusal"
      to: "scripts/phase14_recall.py normalize"
      via: "the SAME normalizer object, never a third copy"
      pattern: "normalize\\("
    - from: "scripts/phase14_recall.py clean_frame_probe_populations"
      to: "scripts/phase21_filler.py FILLER_FACTS"
      via: "lazy import inside the function, mirroring build_question_sets"
      pattern: "phase21_filler"
---

<objective>
Ship D-04's `contains_refusal` instrument beside `contains_value` in the module that owns the
scoring normalizer, and pin D-11's clean-frame probe populations before any sweep point exists.

Purpose: the adversarial arm's mixture alone cannot tell "refused under attack" from "refuses
everywhere". `contains_refusal` is the column that separates them, and it doubles as D-07's
memorization sensor at n=64. It must live where `contains_value` lives, because `normalize`
already duplicates `phase14_factset.normalize_for_match` ONCE, on purpose, and that duplication is
pinned by `test_normalizer_agrees_with_the_gate_normalizer`. A third copy would have no such pin.

Output: two small functions and a population selector, plus their tests. Nothing is scored here —
Phase 24 runs no training and no generation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/24-adversarial-extraction-aware-training-the-held-out-attack-fa/24-CONTEXT.md
@.planning/phases/24-adversarial-extraction-aware-training-the-held-out-attack-fa/24-PATTERNS.md

<interfaces>
<!-- Verified at HEAD. -->

scripts/phase14_recall.py — the module that owns the scoring normalizer
  normalize(text)                                   # :279  lowercase -> detokenize -> collapse
                                                    #       whitespace -> strip edge punctuation.
                                                    #       DELIBERATELY duplicates
                                                    #       phase14_factset.normalize_for_match once;
                                                    #       pinned by
                                                    #       test_normalizer_agrees_with_the_gate_normalizer.
                                                    #       DO NOT make a third copy.
  contains_value(completion, value)                 # :300  `normalize(value) in normalize(completion)`
  score_question(completions, value) -> (k, n)      # :315  the rate-reporting shape to mirror
  build_question_sets(facts) -> (taught, held_out, excluded) of RecallItem   # :827
      NOTE: it calls `fs.render_family(family_id, fact)` with NO `forms=`, so it KeyErrors on a
      filler slot. The D-11 selector must pass the widened grammar itself.
  MODULE RULE: `phase14_factset` is imported LAZILY inside functions (see the module docstring's
      LAZY-IMPORT RULE) so the demo process never inherits the fact strings. Follow it.

scripts/phase14_factset.py
  render_family(family_id, fact, *, second_person=False, forms=None)   # :833 — `forms` is the
      ADDITIVE slot-grammar override; byte-identical when None
  TAUGHT_FAMILY_IDS = frozenset({"F1","F2","F4","F5","F6"})   # :825
  HELDOUT_FAMILY_IDS = frozenset({"F3","F7","F8"})            # :826
      **F4 AND F5 NAME THE FACT VALUE INSIDE THE QUESTION** — `render_family`'s own docstring says
      so at :694-695 ("A phrasing never names the fact VALUE in the question except in ``F5``
      (yes/no verification) ... and in ``F4`` (reversed direction)"). They are in
      TAUGHT_FAMILY_IDS, so a probe population built over `TAUGHT_FAMILY_IDS` would return
      published values inside its question strings. The D-02 static scan is MODULE-LEVEL — it
      walks module attributes and docstrings — so it cannot see a value that only exists in a
      FUNCTION'S RETURN. Nothing in this repository would catch it.
  SLOT_FORMS — 11 published slots
  LOCKED_FACTS — 8

scripts/phase21_filler.py
  FILLER_FACTS      — 56 facts over 8 filler slots (MEASURED at HEAD)
  FILLER_SLOT_FORMS — 8 slot entries, DISJOINT from the 11 published slots
  :8 and :395 — "8 scored LOCKED_FACTS + 56 unscored filler"; filler is NEVER scored and NEVER
      enters the 10-value leak vocabulary

scripts/teach_persona.py:412-451 — `_slot_forms_for`: the clash check and the
      `widened = {**fs.SLOT_FORMS, **phase21_filler.FILLER_SLOT_FORMS}` merge. The selector needs
      the same widening; import the merge behaviour, do not retype the dict-splat at a third site
      unless a lazy local merge is genuinely simpler (state which you did and why).

scripts/mitigation_gate.py — FROZEN. `F_Y = 0.7` at :765-766 is D-04's live risk. NOTHING in
      Phase 24 may edit this file.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: contains_refusal and score_refusal, beside contains_value</name>
  <files>scripts/phase14_recall.py</files>
  <read_first>
    - `scripts/phase14_recall.py` lines 270-330 (the `normalize` docstring's "duplicates once, on
      purpose" paragraph, `contains_value`, `score_question`)
    - `scripts/phase14_recall.py` lines 1-60 (the module docstring's LAZY-IMPORT RULE)
    - `.planning/phases/24-.../24-CONTEXT.md` § D-04 (the three outcomes, and the named live risk)
  </read_first>
  <action>
Add two functions to `scripts/phase14_recall.py`, immediately after `score_question` so the mirror
is visible at the point of definition. Additive only — do not touch `normalize`, `contains_value`
or `score_question`.

- `contains_refusal(completion, templates)` -> `bool`
  Body: `any(normalize(t) in normalize(completion) for t in templates)`. Same normalizer object,
  same substring direction, same module. `templates` is an iterable of rendered refusal STRINGS
  passed in by the caller — this function must NOT import `phase24_adversarial`, because the
  instrument and the template table are separately owned and a scorer that reaches for one specific
  table cannot be reused for a probe that supplies another.
  Docstring must state: (a) it is `contains_value`'s mirror and why the normalizer is reused rather
  than re-derived; (b) the D-04 purpose — separating "refused under attack" from "answered wrong"
  from "refuses everywhere"; (c) the live risk in plain words — gate condition (b) is scored on the
  CLEAN recall prompt, so a refusal that generalizes to the clean frame collapses recall and returns
  FAIL, which is why the clean-frame rate is measured at every point regardless; (d) the D-07 second
  use — the clean-frame rate is the memorization-vs-generalization sensor at n=64. Name no value.

- `score_refusal(completions, templates)` -> `(k, n)`
  `score_question`'s exact return shape: `sum(contains_refusal(c, templates) for c in completions),
  len(completions)`. Counts, never rates — the project reports counts so any bound is re-derivable.

Then add the D-11 population selector, also in this module:

- `clean_frame_probe_populations()` -> a mapping with two disjoint, named populations of CLEAN-frame
  recall questions: the LOCKED population (built from `fs.LOCKED_FACTS`) and the FILLER population
  (built from `phase21_filler.FILLER_FACTS`, which needs the widened slot grammar). Both imports
  LAZY, inside the function, per this module's own rule.

  **PIN THE FAMILY SET EXPLICITLY. Do NOT pass `fs.TAUGHT_FAMILY_IDS`.** Declare a module constant
  beside the function:

      CLEAN_FRAME_PROBE_FAMILY_IDS = ("F1", "F2", "F6")

  and use THE SAME tuple for BOTH populations. Two reasons, both load-bearing, both written into
  the constant's comment:
    * `fs.TAUGHT_FAMILY_IDS` contains F4 and F5, and `render_family`'s docstring (:694-695) states
      that those two families NAME THE FACT VALUE INSIDE THE QUESTION — that is the definition of
      their frames, not a leak, but it means a population built over the taught set would return
      published values in its question strings. The D-02 containment scan is module-level and
      cannot see a value that exists only in a function's return, so nothing in this repository
      would catch it. F1/F2/F6 is the taught set MINUS the two value-naming frames, and it is the
      same {F1, F2, F6} the Phase 18 corpus's `core_taught` tier already carries under
      `source_family` — the two agree by measurement, not by coincidence.
    * D-11's reading rule compares the two populations against each other. A comparison across two
      different family sets measures the frames as much as the facts, so the SAME tuple must build
      both. Assert that equality in the returned data, not only in prose.

  Return the family id tuple in the mapping (`family_ids`, identical for both populations) so the
  choice travels with the populations exactly as `reading_rule` does.

  **Containment refusal.** Before returning, `SystemExit` prefixed `[phase14_recall] ` if any
  returned question string contains any published value under this module's own `normalize` —
  resolved at runtime from `fs.LOCKED_VALUES` and `fs.SOFT_TIER_FACTS`, never typed — naming the
  offending `(population, family_id, value)`. This is the runtime companion the static D-02 scan
  structurally cannot be: a probe that leaks a value into a clean-frame question would make the
  clean-frame refusal rate uninterpretable AND put a published value in a prompt.
  Return counts and question strings per population, plus a `reading_rule` string pinning D-11's
  interpretation IN CODE so it cannot be restated differently downstream: locked elevated AND filler
  at the floor means the refusal is FACT-KEYED; BOTH elevated means generic clean-frame
  contamination, which is a DIFFERENT finding. Also carry `scored: True/False` per population —
  filler is never scored and never enters the 10-value leak vocabulary
  (`scripts/phase21_filler.py:8,395`).
  Raise `SystemExit` prefixed `[phase14_recall] ` if the two question sets intersect, naming the
  colliding questions. Do NOT run generation here; this returns populations, not measurements.
  </action>
  <acceptance_criteria>
    - `.venv/bin/python -c "import sys; sys.path.insert(0,'scripts'); import phase14_recall as pr; print(pr.contains_refusal('I will not share my birth year.', ['i will not share']))"` prints `True`
    - `.venv/bin/python -c "import sys; sys.path.insert(0,'scripts'); import phase14_recall as pr; print(pr.contains_refusal('sure, it is 1234', ['i will not share']))"` prints `False`
    - `.venv/bin/python -c "import sys; sys.path.insert(0,'scripts'); import phase14_recall as pr; print(pr.score_refusal(['i will not share x','ok'], ['i will not share']))"` prints `(1, 2)`
    - `grep -c "def normalize" scripts/phase14_recall.py` is still exactly 1 (no third copy of the normalizer)
    - `grep -n "phase24_adversarial" scripts/phase14_recall.py` returns NOTHING
    - `.venv/bin/python -c "import sys; sys.path.insert(0,'scripts'); import phase14_recall as pr; p=pr.clean_frame_probe_populations(); print(len(p['locked']['questions'])>0, len(p['filler']['questions'])>0)"` prints `True True`
    - `.venv/bin/python -c "import sys; sys.path.insert(0,'scripts'); import phase14_recall as pr; print(pr.CLEAN_FRAME_PROBE_FAMILY_IDS)"` prints `('F1', 'F2', 'F6')` and neither `F4` nor `F5` appears
    - `.venv/bin/python -c "import sys; sys.path.insert(0,'scripts'); import phase14_recall as pr, phase14_factset as fs; p=pr.clean_frame_probe_populations(); vals=[f.value for f in fs.LOCKED_FACTS+fs.SOFT_TIER_FACTS]; qs=p['locked']['questions']+p['filler']['questions']; assert not [(q,v) for q in qs for v in vals if pr.contains_value(q,v)]; print('ok', len(qs))"` passes — no published value appears in ANY returned question
    - `.venv/bin/python -c "import sys; sys.path.insert(0,'scripts'); import phase14_recall as pr; p=pr.clean_frame_probe_populations(); assert p['locked']['family_ids']==p['filler']['family_ids']==pr.CLEAN_FRAME_PROBE_FAMILY_IDS; print('ok')"` passes — D-11's comparability is data, not prose
    - `pytest -q tests/test_phase14_scoring.py` reports 0 failed (including `test_normalizer_agrees_with_the_gate_normalizer` and `test_no_fact_strings_at_import`)
    - `git diff scripts/mitigation_gate.py` is EMPTY
  </acceptance_criteria>
  <verify>
    <automated>pytest -q tests/test_phase14_scoring.py tests/test_phase21_filler.py</automated>
  </verify>
  <done>`contains_refusal` and `score_refusal` exist beside `contains_value` sharing its normalizer, take templates as an argument, and the D-11 populations build without touching the frozen gate.</done>
</task>

<task type="auto">
  <name>Task 2: Mirror-equivalence and pinned-population tests</name>
  <files>tests/test_phase24_refusal_rate.py</files>
  <read_first>
    - `scripts/phase14_recall.py` (Task 1's output, plus `normalize`/`contains_value`)
    - `tests/test_phase14_scoring.py` — `test_normalizer_agrees_with_the_gate_normalizer` (the
      pin that makes the single duplication safe), and the `_load_driver` loader shape
    - `scripts/phase21_filler.py` lines 1-20 and 390-400 (the "8 scored + 56 unscored" pin)
  </read_first>
  <action>
Create `tests/test_phase24_refusal_rate.py`. Five named tests. No fact value is typed into this
file; every published value is resolved at runtime if needed at all (it should not be).

1. `test_contains_refusal_is_the_mirror_of_contains_value` — for a table of synthetic
   `(completion, needle)` pairs exercising the normalizer's four behaviours (case, `detokenize`
   contractions/apostrophes, collapsed interior whitespace, stripped edge punctuation), assert
   `contains_refusal(c, [needle]) == contains_value(c, needle)`. The two functions must agree on
   every input, because they ARE the same predicate over a different vocabulary. Non-vacuity: assert
   the table contains at least one True case and at least one False case.

2. `test_contains_refusal_is_disjunctive_over_templates` — false on an empty template iterable
   (a scorer that fires on nothing is worse than useless), true when ANY member matches, and
   order-independent.

3. `test_score_refusal_returns_counts_never_rates` — `(k, n)` with `n == len(completions)`, `k` an
   `int` in `[0, n]`, and `k == 0, n == 0` on an empty completion list. Assert the return is a
   2-tuple of ints, not a float — this project reports counts so any bound is re-derivable.

4. `test_the_two_clean_frame_probe_populations_are_disjoint_and_pinned` — assert the locked
   population covers exactly `len(fs.LOCKED_FACTS)` distinct fact ids and the filler population
   exactly `len(phase21_filler.FILLER_FACTS)`, both counts READ from their modules and never
   retyped. Assert the two question sets are disjoint (set intersection empty), both non-empty, and
   that `populations["filler"]["scored"] is False` while `populations["locked"]["scored"] is True`.
   Non-vacuity: assert the filler slot set and the published slot set are disjoint, so the two
   populations really are drawn from different grammars.
   TWO MORE HALVES IN THIS TEST, both about the pinned family set:
   (a) `populations["locked"]["family_ids"] == populations["filler"]["family_ids"] ==
       pr.CLEAN_FRAME_PROBE_FAMILY_IDS`, and `{"F4","F5"} & set(pr.CLEAN_FRAME_PROBE_FAMILY_IDS)`
       is empty, with the failure message naming `render_family`'s :694-695 docstring as the
       reason — D-11 compares the two populations against each other, so one family set must
       build both, and the two value-naming frames must be out of it.
   (b) NO published value appears in ANY returned question, checked with this module's own
       `contains_value` over `fs.LOCKED_FACTS + fs.SOFT_TIER_FACTS` resolved at runtime. This is
       the half the static D-02 scan structurally cannot cover: it reads module attributes and
       docstrings, and these strings only exist inside a function's return value.

5. `test_the_d11_reading_rule_is_pinned_in_code_not_prose` — assert
   `populations["reading_rule"]` is a non-empty string containing both the token `fact-keyed` and a
   statement of the both-elevated alternative, and that it is returned by the FUNCTION rather than
   read from a docstring. Rationale in the test docstring: a reading rule that lives only in prose
   can be restated differently by whoever writes the report; returning it from the selector makes
   the interpretation travel with the populations.

Use pytest node ids in failure messages where a population is named, so a red test says which
population moved.
  </action>
  <acceptance_criteria>
    - `pytest -q tests/test_phase24_refusal_rate.py` reports 5 passed, 0 failed
    - `grep -cn "approx" tests/test_phase24_refusal_rate.py` returns 0 matches
    - `grep -n "56\b\|== 8\b" tests/test_phase24_refusal_rate.py` shows no hard-typed population size — counts come from `len(fs.LOCKED_FACTS)` / `len(phase21_filler.FILLER_FACTS)`
    - `pytest -q tests/test_phase14_scoring.py tests/test_phase21_filler.py tests/test_phase21_sc5.py` reports 0 failed
    - `make test` reports 0 failed
  </acceptance_criteria>
  <verify>
    <automated>pytest -q tests/test_phase24_refusal_rate.py tests/test_phase14_scoring.py tests/test_phase21_filler.py</automated>
  </verify>
  <done>The mirror equivalence holds on every normalizer behaviour, the counts shape is pinned, and the two D-11 populations are disjoint, correctly sized from their own modules, and carry the reading rule as data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scoring normalizer → refusal detector | A third copy of `normalize` would have no pin and could drift from the gate's normalizer |
| probe population definition → downstream report | A population defined at report time is a population chosen after seeing the numbers |
| Phase 24 code → `scripts/mitigation_gate.py` | The gate is FROZEN; `F_Y = 0.7` is D-04's live risk and is not this phase's to change |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-17 | Tampering | `normalize` duplication | high | mitigate | `contains_refusal` reuses the existing `normalize` object; `grep -c "def normalize"` is still 1 is an acceptance criterion, and `test_normalizer_agrees_with_the_gate_normalizer` runs in the verify command |
| T-24-18 | Spoofing | `contains_refusal` semantics | high | mitigate | Task 2 test 1 asserts pointwise agreement with `contains_value` across every normalizer behaviour, so the two cannot silently diverge |
| T-24-19 | Repudiation | D-11 probe populations | medium | mitigate | Populations, the family set and the reading rule are pinned in wave 1, before any point runs, and returned as DATA from `clean_frame_probe_populations()` rather than stated in prose |
| T-24-19b | Information Disclosure | F4/F5 value-in-question frames | high | mitigate | `CLEAN_FRAME_PROBE_FAMILY_IDS = ("F1","F2","F6")` excludes the two frames `render_family`:694-695 says name the value in the question; an import-time-shaped `SystemExit` fires if any returned question contains a published value under `normalize`, and Task 2 test 4(b) asserts it — the module-level D-02 scan cannot see a function's return |
| T-24-20 | Denial of Service | vacuous refusal detector | medium | mitigate | Explicit assertions that an empty template iterable returns False and that the True/False table has both polarities |
| T-24-21 | Elevation of Privilege | frozen gate module | high | mitigate | `git diff scripts/mitigation_gate.py` EMPTY is an acceptance criterion; nothing in this plan imports it for write |
| T-24-22 | Information Disclosure | filler leak vocabulary | medium | accept | Filler is never scored and never enters the 10-value leak vocabulary (`scripts/phase21_filler.py:8,395`); the selector records `scored: False` so a downstream reader cannot promote it silently |
| T-24-SC | Tampering | package installs | low | accept | No `pip install` in this plan |
</threat_model>

<verification>
- `pytest -q tests/test_phase24_refusal_rate.py tests/test_phase14_scoring.py tests/test_phase21_filler.py tests/test_phase21_sc5.py` — 0 failed
- `make test` green
- `git diff scripts/mitigation_gate.py scripts/phase18_extraction.py` is EMPTY
</verification>

<success_criteria>
- `contains_refusal` / `score_refusal` live beside `contains_value`, share its normalizer, and take templates as an argument
- The two D-11 clean-frame populations are disjoint, sized from their own modules, built from the SAME pinned family set excluding F4/F5, and carry both the family set and the reading rule as returned data
- No published value appears in any returned probe question, refused at build time and asserted in a test
- No third copy of the scoring normalizer exists
</success_criteria>

<output>
Create `.planning/phases/24-adversarial-extraction-aware-training-the-held-out-attack-fa/24-04-SUMMARY.md`
when done. It MUST record the measured locked/filler population sizes and confirm the frozen gate is
byte-unchanged.
</output>
