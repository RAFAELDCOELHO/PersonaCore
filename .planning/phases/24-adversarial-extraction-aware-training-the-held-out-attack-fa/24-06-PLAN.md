---
phase: 24-adversarial-extraction-aware-training-the-held-out-attack-fa
plan: 06
type: execute
wave: 3
depends_on: ["24-02", "24-05"]
files_modified:
  - scripts/teach_persona.py
  - tests/test_phase24_bins.py
autonomous: true
requirements: [ADVT-01, ADVT-03]

must_haves:
  truths:
    - "SC1/ADVT-01: `build_bins(..., adversarial_ratio=0.0)` is BYTE-IDENTICAL to v2.0 — same token bin sha256, same mask bin sha256, same `repr(stats)` — with no loop change, no accountant and no per-record machinery"
    - "SC1 load-bearing half: `adversarial_ratio` is actually READ. A byte-identity guard over an unwired kwarg is vacuously green, so the wiring sibling was WATCHED RED before the kwarg existed"
    - "D-06: the ratio is adversarial EPISODES per clean EPISODE — sized from `len(clean_episodes)`, NEVER from `teaching_tokens`, which is the side channel `test_replay_constant_is_not_derived_from_the_corpus` exists to police"
    - "D-07: the same nominal grid runs at both capacities, repetition permitted, and multiplicity is REPORTED PER POINT in the stats dict"
    - "D-08: adversarial episodes are INTERLEAVED among the clean ones by a permutation that is a PURE FUNCTION of the existing seed — never a fresh runtime RNG, because the Phase 23 D-07 resume path rebuilds the bins and refuses on any byte change"
    - "D-08 confirmed at the mechanism: `DP_ARMS` is a literal closed 2-tuple, `aligned = arm in DP_ARMS` has no prefix matching, so `adv_n8`/`adv_n64` pack FLAT — declared residue 2 is closed"
    - "Additive-seam discipline: the adversarial stats keys appear ONLY on the non-default branch, because `tests/test_phase21_aligned_bins.py:225` pins `repr(stats)` against the golden fixture"
    - "D-10 at the SELECTED prefix: `stats[\"adversarial_family_counts\"]` reports the per-family counts of the episodes actually placed, and every non-zero grid point is asserted to carry all three trained families within 1 of each other — the full-pool assertion in 24-05 cannot see a corpus row reorder, and `(pool * ceil(n_want/len(pool)))[:n_want]` would then train ONE family while D-10's truth stayed green"
  artifacts:
    - path: "scripts/teach_persona.py"
      provides: "build_bins adversarial_ratio seam + seed-derived interleave + additive stats + arm rows + threading through build_arm_bins and train_arm"
      contains: "adversarial_ratio"
    - path: "tests/test_phase24_bins.py"
      provides: "the wiring sibling (watched RED), the byte-identity half, the permutation-purity proof, the D-06 sizing-unit proof, and the per-grid-point family-balance proof over the SELECTED prefix"
  key_links:
    - from: "scripts/teach_persona.py build_bins"
      to: "scripts/phase24_adversarial.py adversarial_episodes"
      via: "lazy import inside the ratio>0 branch"
      pattern: "phase24_adversarial"
    - from: "scripts/teach_persona.py build_arm_bins"
      to: "scripts/teach_persona.py build_bins"
      via: "adversarial_ratio threaded unchanged on both branches"
      pattern: "adversarial_ratio="
---

<objective>
Add `adversarial_ratio` to `build_bins` as a build-time mixture ratio, interleaved by a seed-derived
permutation, byte-identical at its default — and prove the kwarg is actually read before claiming
the byte-identity means anything.

Purpose: ADVT-01's whole claim is "no new training seam". `replay_ratio` already established the
shape: a mixture baked into the bin rather than into the loop, so `train()` stays untouched and the
ratio is an auditable committed number. The dangerous half is the guard: a byte-identity assertion
over a kwarg nobody reads is trivially true. `test_align_facts_is_wired` exists in this repo for
exactly that reason, and this plan copies it.

Output: the seam, the two new arm rows, and the test module that watched it go from red to green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/24-adversarial-extraction-aware-training-the-held-out-attack-fa/24-CONTEXT.md
@.planning/phases/24-adversarial-extraction-aware-training-the-held-out-attack-fa/24-PATTERNS.md
@.planning/phases/24-adversarial-extraction-aware-training-the-held-out-attack-fa/24-RESEARCH.md
@.planning/phases/24-adversarial-extraction-aware-training-the-held-out-attack-fa/24-05-SUMMARY.md

<interfaces>
<!-- Every line number re-verified at HEAD this session. -->

scripts/teach_persona.py
  :127   MASK_FRACTION_BAND = (0.15, 0.95)
  :249   ARMS = ("cal_first_person","cal_first_person_replay","cal_second_person","real",
                 "dp_n8","dp_n64")
  :270   DP_ARMS = ("dp_n8","dp_n64")   — literal closed 2-tuple, NO prefix matching
  :304   arm_outputs(arm, *, prefix="phase14")     — paths scoped by arm NAME
  :355   arm_bin_targets(arm, outputs)             — 3 paths for a DP arm, 2 for a flat one
  :412   _slot_forms_for(facts)                    — lazy import + clash + undeclared refusals
  :455   render_episodes(facts, family_ids, *, second_person=False) -> list[(question, answer)]
  :467   def build_bins(tok, episodes, bin_path, mask_path, *, replay_ratio=0.0, align_facts=None)
  :485-498  the flat shard loop, then:
              teaching_tokens = int(sum(lengths))
              replay_tokens = 0
              if replay_ratio > 0: replay_tokens = _prepend_replay(...)
              ids_all = np.concatenate(id_shards); mask_all = np.concatenate(mask_shards)
          <-- the adversarial mix goes AFTER `teaching_tokens = ...` and BEFORE the replay block,
              so `teaching_tokens` stays CLEAN-ONLY and `_prepend_replay` keeps prepending at index 0
  :487   `encode_dialogue(tok, [], [(question, answer)])` — the HARDCODED EMPTY PERSONA. D-10's A3
          episodes need a per-episode persona; an empty persona stays byte-identical so SC1 survives
  :512   `    return {` — a BARE RETURN OF A DICT LITERAL, not a `stats` variable. The twelve
          keys are :513-524 and the closing brace is :525. There is nothing named `stats` in this
          function to `.update()`; Task 2 rebinds :512 and adds an explicit `return stats`.
          The dict is byte-pinned by `repr(stats)` in `tests/test_phase21_aligned_bins.py:225`
  :528   _prove_floor_and_band(ids_all, mask_all)  — checks the AGGREGATE mean (:549), SystemExit at
          BUILD time
  :560   _refuse_ambiguous_aligned_input(episodes, replay_ratio, align_facts)
  :609   _build_aligned_bins(...)                  — the precedent: additive keys ONLY on the
          non-default branch (docstring :481-483)
  :751   _prepend_replay(id_shards, mask_shards, replay_ratio, teaching_tokens, *, n_facts=None)
          — mutates the shard lists IN PLACE via `.insert(0, ...)` and returns the token count;
          `SystemExit`s with the sizing formula spelled out in the message
  :831   sanity_check(tok, arm, bin_path, mask_path, facts, stats)  — proof 5 (paraphrase band)
          at :846, proof 6 (held-out questions absent from the WRITTEN bin at token level) at :861
  :882   arm_spec(arm) -> (facts, second_person, replay_ratio); the "``replay_ratio = 0.0`` is
          LOAD-BEARING" paragraph is at :897; `dp_n64`'s row uses a LAZY `import phase21_filler`
          (:922-931); the unknown-arm `raise SystemExit` naming ARMS is at :932
  :945   build_arm_bins(arm, facts, family_ids, *, second_person=False, replay_ratio=0.0, seed=SEED,
                        prefix="phase14", resume_from=None)
  :1005  aligned = arm in DP_ARMS
  :1021-1029 the `stats = build_bins(...)` call; `sanity_check(...)` immediately after at :1030
  :1031-1047 the resume rebuild-and-compare; `raise SystemExit(` at :1038 and the determinism tuple
          "(facts, family_ids, second_person, replay_ratio, seed=...)" at :1044-1045
  :1087  USAGE — built by interpolation from ARMS/DP_ARMS; NO manual edit needed
  :1151  main(); the non-DP path enforces `len(argv) != 1` at :1169 (`raise SystemExit(USAGE)`)
  :1293-1305 train_arm(arm, *, facts, family_ids, second_person=False, replay_ratio=0.0, seed=SEED,
                   prefix="phase14", dp_sigma=None, dp_clip_norm=None, resume_from=None)
  :1464  train_arm's build_arm_bins call

scripts/phase24_adversarial.py (24-05)
  adversarial_episodes(tok) -> list[(persona: tuple[str,...], question: str, answer: str)]  — 336
  adversarial_pool_size(tok) -> int
  MIN_REFUSAL_SCORED_TOKENS, MASK_FRACTION_MARGIN                                          (24-01)

scripts/mitigation_budget.py (24-02)
  ADVERSARIAL_RATIO_GRID = (0.0, 0.25, 0.5, 1.0, 1.5, 1.9090909090909092)

tests/test_phase21_aligned_bins.py
  :151-183  _build(tmp_path, name, **kwargs) and _sha256(path) — the fixture shape to copy
  :17-34    the scripts-path preamble
  :200-226  test_build_bins_byte_identity_default_matches_the_v2_golden — includes the STALE-FIXTURE
            escape hatch (:203-210). DO NOT WEAKEN IT and do not edit build_bins to chase a digest
  :229-241  test_align_facts_is_wired — "THE LOAD-BEARING HALF. Without this the identity guard
            above is vacuous." The sibling to copy.

tests/test_phase21_replay_volume.py:260
  test_replay_constant_is_not_derived_from_the_corpus — left UNTOUCHED as a regression tripwire.
  D-06's episode unit sidesteps it by construction.

MEASURED at HEAD (do not re-derive): clean episodes 176 (n=8) / 1408 (n=64); pool 336;
  round(1.9090909090909092 * 176) = 336 (multiplicity 1.0);
  round(1.9090909090909092 * 1408) = 2688 (multiplicity 8.0).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the wiring sibling and the byte-identity half, and WATCH THEM RED</name>
  <files>tests/test_phase24_bins.py</files>
  <read_first>
    - `tests/test_phase21_aligned_bins.py` lines 1-60 and 145-245 (preamble, `_sha256`, `_build`,
      the byte-identity test with its stale-fixture escape hatch, and `test_align_facts_is_wired`)
    - `scripts/teach_persona.py` lines 465-530 (`build_bins`' signature, flat path and stats dict)
    - `scripts/phase24_adversarial.py` (24-05's output)
  </read_first>
  <action>
Create `tests/test_phase24_bins.py` BEFORE touching `scripts/teach_persona.py`. Copy
`tests/test_phase21_aligned_bins.py:17-34`'s scripts-path preamble and its `_sha256` / `_build`
helpers in shape (a `tmp_path` build returning `(stats, bin_path, mask_path)`; `seed_everything`
then `from_json(tp.TOKENIZER_PATH)`; never train or fake a tokenizer).

SIX named tests. **Stated once, plainly: ALL SIX must FAIL right now**, every one of them with
`TypeError: build_bins() got an unexpected keyword argument 'adversarial_ratio'` (or the equivalent
for `build_arm_bins`). There is no exception — test 2 passes `adversarial_ratio=0.0` explicitly, and
Python raises on the explicit kwarg before any assertion in it runs. What IS observable pre-wiring
is test 2's FIRST HALF (the no-kwarg build's sha256s), which the test computes before the explicit
call; capture that digest in the RED run and record it in the SUMMARY as the pre-wiring baseline the
byte-identity claim is measured against.

1. `test_adversarial_ratio_is_wired` — THE LOAD-BEARING HALF, in
   `test_align_facts_is_wired`'s register, with a docstring saying in as many words: without this,
   every `adversarial_ratio=0.0` byte-identity assertion in this module is VACUOUS. Build the same
   176 clean episodes twice, once at `adversarial_ratio=0.0` and once at
   `mitigation_budget.ADVERSARIAL_RATIO_GRID[-1]`. Assert the two token-bin sha256s DIFFER, the two
   mask-bin sha256s DIFFER, and `stats["episodes"]` differs by exactly `round(ratio * 176)` = the
   pool size. Failure message must say the kwarg is not read.

2. `test_the_default_path_is_byte_identical_to_the_no_kwarg_call` — SC1. Build once with no
   `adversarial_ratio` argument at all and once with `adversarial_ratio=0.0`; assert both sha256s
   are equal and `repr(stats)` is equal. This is the only test here that is green before the seam
   exists (Python raises on the explicit kwarg — so write it so the no-kwarg build is captured
   first, and note in the SUMMARY which of the two halves was observable pre-wiring).

3. `test_the_interleave_permutation_is_a_pure_function_of_the_seed` — D-08 and the Phase 23 resume
   constraint. At a non-zero ratio: two builds at the SAME seed produce IDENTICAL sha256s; two
   builds at DIFFERENT seeds produce DIFFERENT sha256s (proving `seed` actually drives the
   permutation rather than a constant); and at ratio `0.0` two DIFFERENT seeds produce IDENTICAL
   sha256s (proving the seam is inert at the default). Failure message must name the Phase 23
   rebuild-and-compare refusal as the consequence.

4. `test_adversarial_episodes_are_interleaved_not_appended` — D-08's actual content claim. Decode or
   scan the mask bin: assert the adversarial shards are NOT a contiguous run at either end. A cheap
   sufficient check: locate the episode boundaries from the shard lengths reported in stats, or
   assert that the first and last `BLOCK_SIZE`-sized windows of the mask bin BOTH contain scored
   tokens attributable to clean and to adversarial material. State in the docstring which observable
   you chose and why it is sufficient — an "appended" layout would concentrate the frame contrast at
   one seam, which is the shape D-08 rejected.

5. `test_the_mixture_is_sized_from_episode_count_not_teaching_tokens` — D-06. Build at a fixed ratio
   over two episode lists with the SAME episode count but different token totals (e.g. the 176 clean
   episodes, versus the same list with each answer duplicated), and assert `stats
   ["adversarial_episodes"]` is IDENTICAL across the two. If the sizing ever consulted
   `teaching_tokens`, this diverges. The docstring must name
   `tests/test_phase21_replay_volume.py::test_replay_constant_is_not_derived_from_the_corpus` as the
   shape being avoided, and that test is left untouched as a tripwire.

6. `test_every_grid_point_trains_all_three_families_in_balance` — D-10's "three families train"
   made observable AT THE SELECTED PREFIX, which is where it can actually break. Parametrize over
   `mitigation_budget.ADVERSARIAL_RATIO_GRID[1:]` (the non-zero points) at n=8, and assert from
   `stats["adversarial_family_counts"]`: all three of `phase24_adversarial.TRAINED_FAMILIES` are
   present with a count `>= 1`; `max(counts) - min(counts) <= 1`; and `sum(counts.values()) ==
   stats["adversarial_episodes"]`. Docstring must say why the FULL-POOL assertion in
   `tests/test_phase24_adversarial.py` (24-05 test 1) is not enough: `_mix_adversarial` takes
   `(pool * ceil(n_want/len(pool)))[:n_want]`, so balance in the selected prefix is a property of
   the committed corpus's ROW ORDER. Measured at HEAD 2026-08-30 that order is a STRICT 3-cycle and
   every point lands 15/15/14 or better — but if a rebuild ever reordered the rows, every point
   below ratio ~0.64 would train ONE family while the full-pool assertion stayed green. Failure
   message must name the corpus row order as the thing that moved.

RUN THE MODULE NOW and capture the failing output. Record in the SUMMARY: which tests were RED,
with the exact error. A wiring guard whose RED was never observed proves nothing.
  </action>
  <acceptance_criteria>
    - `pytest -q tests/test_phase24_bins.py` reports 6 failed, 0 passed at this point, and the captured output names `adversarial_ratio` as an unexpected keyword argument
    - The captured RED output for `test_adversarial_ratio_is_wired` is quoted in the SUMMARY, together with test 2's pre-wiring no-kwarg sha256 baseline
    - `pytest -q tests/test_phase21_aligned_bins.py tests/test_phase21_replay_volume.py` is GREEN (nothing in `scripts/` was touched yet)
    - `git diff --stat` shows exactly one path: `tests/test_phase24_bins.py`
  </acceptance_criteria>
  <verify>
    <automated>pytest -q tests/test_phase21_aligned_bins.py tests/test_phase21_replay_volume.py</automated>
  </verify>
  <done>Six tests exist, all six are RED with the same `TypeError`, the RED output and test 2's pre-wiring no-kwarg sha256 baseline are captured, and no production file has moved.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the adversarial_ratio seam inside build_bins</name>
  <files>scripts/teach_persona.py</files>
  <read_first>
    - `scripts/teach_persona.py` lines 465-560 (`build_bins` in full, `_prove_floor_and_band`,
      `_refuse_ambiguous_aligned_input`). **Note :512 is `    return {` — a bare return of a dict
      literal. There is no `stats` variable to `.update()`; you rebind that ONE line.**
    - `scripts/teach_persona.py` lines 745-815 (`_prepend_replay` — the in-place shard mutation,
      the returned token count, the `SystemExit` with the sizing formula in the message)
    - `scripts/teach_persona.py` lines 600-660 (`_build_aligned_bins`'s additive-key precedent)
    - `scripts/phase24_adversarial.py` (24-05's `adversarial_episodes` / `adversarial_pool_size`)
    - `src/personacore/dialogue/serialize.py` lines 61-95
  </read_first>
  <action>
Edit `scripts/teach_persona.py`. Additive only.

**Signature.** `def build_bins(tok, episodes, bin_path, mask_path, *, replay_ratio=0.0,
align_facts=None, adversarial_ratio=0.0, seed=SEED):`

Both new parameters keyword-only with today's behaviour as the default. `seed` is read ONLY inside
the `adversarial_ratio > 0` branch, so the default path is unchanged; it exists because D-08's
permutation must be a pure function of the EXISTING seed and `build_bins` had no access to one.

**Docstring.** Extend the existing docstring in its own register: state that when
`adversarial_ratio` is `0.0` this function is BYTE-IDENTICAL to v2.0, name
`tests/test_phase24_bins.py::test_the_default_path_is_byte_identical_to_the_no_kwarg_call` and
`tests/test_phase21_aligned_bins.py::test_build_bins_byte_identity_default_matches_the_v2_golden` as
the tests that prove it rather than argue it, and name
`tests/test_phase24_bins.py::test_adversarial_ratio_is_wired` as the load-bearing half. State that
the additive keys appear ONLY on the non-default branch, and why (`repr(stats)` is byte-pinned).

**Refusal.** Widen `_refuse_ambiguous_aligned_input` (`:560`) with an `adversarial_ratio` parameter
and refuse `align_facts is not None and adversarial_ratio > 0` with a `SystemExit` prefixed
`[teach_persona] ` explaining that the adversarial arm makes no formal claim (`accounting: null`),
packs FLAT by the `DP_ARMS` name rule, and has no home in the ragged fact-aligned layout — a
fact-independent episode has no fact shard. Widen the helper rather than branching at the call site:
`refuse_if_exists`' docstring (`:376-404`) is this file's precedent for "two copies of a guard
drift".

**The mixture helper.** Add `_mix_adversarial(tok, id_shards, mask_shards, lengths, fractions,
adversarial_ratio, n_clean, seed)` in `_prepend_replay`'s shape — mutates the shard lists in place,
returns a small dict of counts, and `SystemExit`s with the sizing formula spelled out in the
message. Behaviour:

1. `import phase24_adversarial` LAZILY inside the function, for the same reason `arm_spec`'s
   `phase21_filler` import is lazy (keeps `teach_persona`'s import graph unchanged for the clean-room
   scan). Build the pool with `adversarial_episodes(tok)`.
2. `n_want = int(round(adversarial_ratio * n_clean))` — the D-06 EPISODE unit. `n_clean` is
   `len(episodes)`, NEVER `teaching_tokens`. Refuse with `SystemExit` if `n_want < 1` at a non-zero
   ratio (a ratio too small to place a single episode is a silently inert sweep point).
3. Repetition (D-07): take `(pool * ceil(n_want / len(pool)))[:n_want]`. Deterministic, in pool
   order. Compute `multiplicity = n_want / len(pool)` and report it — Phase 25 SC3 requires
   multiplicity to travel in the same sentence as the point.
4. Encode each selected episode with `encode_dialogue(tok, persona, [(question, answer)])`, passing
   the episode's OWN persona tuple. This is D-10's change to the hardcoded `[]` at `:487`: the clean
   loop keeps passing `[]` and stays byte-identical; only adversarial episodes carry a persona, at
   mask=0.
5. Append the new shards to `id_shards` / `mask_shards` / `lengths` / `fractions`, then permute
   `id_shards` and `mask_shards` TOGETHER with `random.Random(seed)` over an index list — a PURE
   FUNCTION of `seed`, never `np.random` global state and never an unseeded RNG. Record in a comment
   that by this point the ambient global RNG has already been consumed by tokenizer load and
   rendering, which is exactly why a private `Random` instance is required.
6. Return counts: adversarial token total, adversarial SCORED token total (`sum` of the appended
   masks), `n_want`, `pool_size`, `multiplicity`, AND `family_counts` — a `dict[str, int]` of the
   per-family episode counts of THE SELECTED PREFIX (`collections.Counter` over the selected
   episodes' families, ordered by `phase24_adversarial.TRAINED_FAMILIES`). Reason, written into a
   comment: the selection is `(pool * ceil(n_want/len(pool)))[:n_want]`, so family balance in the
   SELECTED prefix is a property of the committed corpus's ROW ORDER, not of this code. Measured at
   HEAD 2026-08-30 the committed `core_taught` rows are a STRICT 3-cycle
   `[A1-mild, A1-aggressive, A3] * 112`, so every grid point selects counts differing by at most 1
   (ratio 0.25 at n=8 gives 15/15/14) — but nothing asserts that ordering, and if a rebuild ever
   reordered the rows every point below ratio ~0.64 would silently train ONE family while D-10's
   "three families train" truth stayed green. Reporting the counts is what makes Task 1 test 6 able
   to see it.
   The episode->family mapping is available from `adversarial_episodes`' own source rows; if the
   returned triples do not carry the family, add a sibling `adversarial_episode_families(tok)` in
   `scripts/phase24_adversarial.py` returning the family list in the SAME order — do not re-derive
   the join here.

**Call site.** Insert the `if adversarial_ratio > 0:` block AFTER `teaching_tokens = int(sum(lengths))`
and BEFORE the `if replay_ratio > 0:` block, so `teaching_tokens` stays CLEAN-ONLY and
`_prepend_replay` keeps prepending replay at index 0, outside the teaching content.

**Stats.** Line :512 today is `    return {` — a BARE RETURN OF A DICT LITERAL. There is no
`stats` variable in this function, so `stats.update(...)` has nothing to update. Make exactly one
rebinding edit:

  * change line :512 from `    return {` to `    stats = {` (the twelve KEY lines :513-524 and the
    closing brace :525 stay byte-unchanged),
  * add, after the closing brace, the conditional block and then an explicit `return stats`.

ONLY under `if adversarial_ratio > 0:`, `stats.update({...})` with: `adversarial_ratio`,
`clean_episodes`, `adversarial_episodes`, `adversarial_pool_size`, `adversarial_multiplicity`,
`adversarial_family_counts` (the SELECTED prefix's per-family counts from `_mix_adversarial`),
`adversarial_tokens`, `adversarial_scored_tokens`, `adversarial_permutation_seed`, and an
overriding `episodes` equal to clean + adversarial. Nothing is added on the default path —
`tests/test_phase21_aligned_bins.py:225` asserts `repr(stats) == GOLDEN["stats_repr"]` and a new key
there turns SC1's own guard red without moving a single bin byte.

Now run `tests/test_phase24_bins.py` and confirm GREEN, then run
`tests/test_phase21_aligned_bins.py` and confirm the golden fixture is still GREEN.
  </action>
  <acceptance_criteria>
    - `pytest -q tests/test_phase24_bins.py` reports 6 passed, 0 failed
    - `pytest -q tests/test_phase21_aligned_bins.py` reports 0 failed, including `test_build_bins_byte_identity_default_matches_the_v2_golden` and `test_align_facts_is_wired`
    - `pytest -q tests/test_phase21_replay_volume.py` reports 0 failed and `git diff tests/test_phase21_replay_volume.py` is EMPTY
    - `.venv/bin/python -c "import sys,inspect; sys.path.insert(0,'scripts'); import teach_persona as tp; p=inspect.signature(tp.build_bins).parameters; print(p['adversarial_ratio'].default, p['adversarial_ratio'].kind, p['seed'].default)"` shows `0.0`, `KEYWORD_ONLY`
    - `.venv/bin/python -c "import ast,pathlib; t=ast.parse(pathlib.Path('scripts/teach_persona.py').read_text()); fn=next(n for n in ast.walk(t) if isinstance(n,ast.FunctionDef) and n.name=='_mix_adversarial'); names={n.id for n in ast.walk(fn) if isinstance(n,ast.Name)}; attrs={f\"{getattr(n.value,'id',None)}.{n.attr}\" for n in ast.walk(fn) if isinstance(n,ast.Attribute)}; assert 'teaching_tokens' not in names, 'D-06: teaching_tokens is read inside the sizing helper'; assert 'np.random' not in attrs, attrs; assert not (attrs & {'random.random','random.shuffle','random.seed','random.choice','random.sample','random.randint'}), attrs; print('ok')"` passes. **AST, not grep, on purpose:** `grep -n \"np.random\"` over this file already returns a hit today — `scripts/teach_persona.py:537` contains `np.random.randint` inside a COMMENT — and "grep shows `teaching_tokens` is not an input to the sizing" is a human judgement dressed as a command. The AST walk sees only the helper's own body and only real name/attribute nodes
    - `.venv/bin/python -c "import ast,pathlib; t=ast.parse(pathlib.Path('scripts/teach_persona.py').read_text()); fn=next(n for n in ast.walk(t) if isinstance(n,ast.FunctionDef) and n.name=='_mix_adversarial'); attrs={n.attr for n in ast.walk(fn) if isinstance(n,ast.Attribute)}; assert 'Random' in attrs, 'the permutation must use a private random.Random(seed) instance'; print('ok')"` passes
    - `git diff -U0 scripts/teach_persona.py | grep '^-[^-]'` shows deletions confined to: the `build_bins` signature line, the `_refuse_ambiguous_aligned_input` signature and its call, the docstring, and EXACTLY ONE line inside the stats block — line :512's `    return {`, rebound to `    stats = {`. None of the twelve KEY lines (:513-524) and not the closing brace (:525) is deleted
    - `pytest -q tests/test_phase21_aligned_bins.py::test_build_bins_byte_identity_default_matches_the_v2_golden` passes — the real pin on `repr(stats)`, and the reason the twelve keys must not move
  </acceptance_criteria>
  <verify>
    <automated>pytest -q tests/test_phase24_bins.py tests/test_phase21_aligned_bins.py tests/test_phase21_replay_volume.py</automated>
  </verify>
  <done>The seam is wired, all five new tests are green, the golden byte-identity fixture is untouched and still green, and the permutation is a pure function of `seed`.</done>
</task>

<task type="auto">
  <name>Task 3: Thread the ratio to the arm level and add the two adversarial arms</name>
  <files>scripts/teach_persona.py</files>
  <read_first>
    - `scripts/teach_persona.py` lines 245-275 (`ARMS`, `DP_ARMS` and their comments)
    - `scripts/teach_persona.py` lines 882-935 (`arm_spec` — its `def` is at :882, NOT :890; the
      "``replay_ratio = 0.0`` on both DP arms is LOAD-BEARING" paragraph at :897; the `dp_n64` row's
      LAZY `import phase21_filler` at :922-931; the unknown-arm `raise` at :932)
    - `scripts/teach_persona.py` lines 945-1060 (`build_arm_bins` in full: `aligned = arm in
      DP_ARMS` at :1005, the `stats = build_bins(...)` call at :1021-1029, `sanity_check` at :1030,
      and the resume rebuild-and-compare at :1031-1047 whose `raise SystemExit(` is at :1038)
    - `scripts/teach_persona.py` lines 1293-1320 (`train_arm`'s signature is at :1293-1305, NOT
      1400-1470 — the old window missed the `def` entirely) and lines 1455-1475 (its
      `build_arm_bins` call at :1464)
    - `tests/test_phase22_wiring.py` lines 545-590 and 880-900 (`test_non_dp_arm_cli_is_unchanged`,
      `test_usage_lists_the_dp_form`, and the `next(a for a in tp.ARMS ...)` selection)
    - `tests/test_phase14_teaching.py` lines 485-600 (the `tp.ARMS`-parametrized path scoping tests)
  </read_first>
  <action>
**A. Arms.** APPEND `"adv_n8"` and `"adv_n64"` to the END of `ARMS` (`:249`). Appending, not
inserting: `tests/test_phase22_wiring.py:887` selects `next(a for a in tp.ARMS if a not in
tp.DP_ARMS and a != "real")` and a prepend would silently move that test's subject. Add a comment
recording that these two arms are DELIBERATELY not in `DP_ARMS`: `aligned = arm in DP_ARMS` (`:1005`)
is a membership test against a literal closed 2-tuple with no prefix matching, so any other name
packs FLAT — which is correct here, because the adversarial arm makes no formal privacy claim
(Phase 25 SC4 pins `accounting: null` on it) and a fact-independent adversarial episode has no home
in the ragged fact-aligned layout. This closes 24-CONTEXT's declared residue 2 as a measurement on
the mechanism rather than an inference.

`USAGE` (`:1087`) is built by interpolation from `ARMS`; do NOT hand-edit it.

**B. `arm_spec` rows.** Add two `if arm == ...: return` triples before the final `raise`:
  - `adv_n8`  -> `(fs.LOCKED_FACTS, False, 0.0)`
  - `adv_n64` -> `(fs.LOCKED_FACTS + phase21_filler.FILLER_FACTS, False, 0.0)` with the SAME LAZY
    `import phase21_filler` the `dp_n64` row uses, and the same comment reason.
`replay_ratio = 0.0` on both is LOAD-BEARING and must be commented as such: 24-RESEARCH's entire
mask-fraction headroom table is computed at `replay_ratio = 0.0`; a non-zero ratio moves the
measured fraction from 0.359 to 0.403 on the `real` arm and invalidates every figure D-05 pins
against.

**C. Threading.** Add `adversarial_ratio=0.0` as a keyword-only parameter to `build_arm_bins`
(`:945`) and to `train_arm` (`:1400+`), each threaded UNCHANGED to the next call
(`build_arm_bins` -> `build_bins` at `:1021-1029`; `train_arm` -> `build_arm_bins` at `:1464`).
`build_arm_bins` also passes its existing `seed` through to `build_bins`' new `seed` parameter, so
the permutation and the RNG seeding agree by construction rather than by coincidence.

Update the resume rebuild-and-compare raise message (the f-string at `:1044-1045`, inside the
`raise SystemExit(` that opens at `:1038`) so its determinism tuple reads
`(facts, family_ids, second_person, replay_ratio, adversarial_ratio, seed=...)`. A message naming a
determinism claim narrower than the one the code now makes is worse than no message.

Extend `build_arm_bins`' docstring with one paragraph in its own register: `adversarial_ratio` is
`0.0` for every existing caller and then this function is BYTE-IDENTICAL to before; it is threaded
UNCHANGED on both branches deliberately, so the day someone sets a non-zero ratio on a `dp_*` arm the
widened `_refuse_ambiguous_aligned_input` raises instead of quietly building an incoherent bin.

**D. No CLI flag.** `main()` is unchanged: the non-DP path still enforces `len(argv) != 1`. Phase 25's
sweep driver calls `train_arm` programmatically, exactly as `scripts/phase17_isolation.py:1138`,
`scripts/phase19_run.py:1646` and `scripts/phase19_erasure.py:3528` already do. Record this choice in
the `ARMS` comment so a later reader does not mistake it for an omission.

**Scope note (deliberate, not an oversight).** This task carries ~8 edit sites in one 2600-line
file. It was CONSIDERED for a split into "arm rows" + "threading" and deliberately left whole:
splitting would make this plan a 4-task plan (over the 2-3 ceiling) or force a new plan number,
which would move the wave graph, the 24-VALIDATION per-task map and the ROADMAP plan list — all
checks that currently pass. Every site below is a one-to-three-line additive edit against a named
anchor, so work them in the listed order (A -> B -> C -> E) and run the verify command after C
before starting E.

**E. The end-to-end smoke.** Run `build_arm_bins("adv_n8", fs.LOCKED_FACTS, fs.TAUGHT_FAMILY_IDS,
adversarial_ratio=mitigation_budget.ADVERSARIAL_RATIO_GRID[-1])` once from a throwaway script or
`python -c`, confirming it completes and that `sanity_check`'s proof 6 (no held-out question appears
in the written bin as a contiguous id run) PASSES on a bin that now contains attacked prompts. The
bins land in gitignored `data/`; delete them afterwards and confirm `git status --porcelain` is
clean of `data/`. Record the printed `mask_fraction` in the SUMMARY.
  </action>
  <acceptance_criteria>
    - `.venv/bin/python -c "import sys; sys.path.insert(0,'scripts'); import teach_persona as tp; print(tp.ARMS[-2:], tp.DP_ARMS)"` shows `('adv_n8','adv_n64')` and `DP_ARMS` unchanged
    - `.venv/bin/python -c "import sys; sys.path.insert(0,'scripts'); import teach_persona as tp; print(tp.arm_spec('adv_n8')[1:], len(tp.arm_spec('adv_n64')[0]))"` prints `(False, 0.0)` and `64`
    - `.venv/bin/python -c "import sys,inspect; sys.path.insert(0,'scripts'); import teach_persona as tp; print(inspect.signature(tp.build_arm_bins).parameters['adversarial_ratio'].kind, inspect.signature(tp.train_arm).parameters['adversarial_ratio'].kind)"` prints `KEYWORD_ONLY KEYWORD_ONLY`
    - `pytest -q tests/test_phase14_teaching.py tests/test_phase22_wiring.py tests/test_phase23_resume.py` reports 0 failed
    - The `build_arm_bins("adv_n8", ..., adversarial_ratio=<upper extreme>)` smoke completes without `SystemExit`, and its printed `mask_fraction` is recorded in the SUMMARY
    - `git status --porcelain data/` is empty after the smoke
    - `grep -n "adversarial_ratio" scripts/teach_persona.py` shows it reaching `build_bins`, `build_arm_bins`, `train_arm` and the resume determinism message
    - `make test` reports 0 failed
  </acceptance_criteria>
  <verify>
    <automated>pytest -q tests/test_phase14_teaching.py tests/test_phase22_wiring.py tests/test_phase23_resume.py tests/test_phase24_bins.py</automated>
  </verify>
  <done>Both adversarial arms exist and pack flat, the ratio reaches `build_bins` from `train_arm`, the resume determinism message names it, and a real `adv_n8` build at the upper extreme passes every `sanity_check` proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kwarg default → committed golden fixture | An additive stats key on the default path reddens SC1's own guard without moving a bin byte |
| runtime RNG → committed bin bytes | A fresh RNG makes the pack non-deterministic and the Phase 23 resume path refuses — or worse, resumes on a different corpus |
| arm name → packer selection | The packer is chosen by arm NAME; a name landing in `DP_ARMS` would silently route to the fact-aligned path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-30 | Repudiation | vacuous byte-identity guard | high | mitigate | Task 1 writes `test_adversarial_ratio_is_wired` FIRST and captures its RED; the SUMMARY quotes the failure |
| T-24-31 | Tampering | `repr(stats)` golden fixture | high | mitigate | Additive keys only under `if adversarial_ratio > 0`; `tests/test_phase21_aligned_bins.py` is in the verify command for Tasks 2 and 3 |
| T-24-32 | Tampering | resume corpus identity | high | mitigate | Permutation is `random.Random(seed)` — a pure function of the existing seed; Task 1 test 3 proves same-seed determinism and different-seed divergence; `tests/test_phase23_resume.py` is in the verify command; the determinism tuple in the resume raise gains `adversarial_ratio` |
| T-24-33 | Information Disclosure | D-06 side channel | high | mitigate | `n_want` is computed from `len(episodes)`; Task 1 test 5 holds episode count fixed while varying token totals; `test_replay_constant_is_not_derived_from_the_corpus` is left untouched as a tripwire |
| T-24-34 | Elevation of Privilege | packer-by-arm-name | medium | mitigate | `adv_*` names appended outside `DP_ARMS`, with the closed-2-tuple membership rule recorded in a comment; `_refuse_ambiguous_aligned_input` widened to refuse aligned+adversarial outright |
| T-24-35 | Denial of Service | build-time `SystemExit` on the band | high | transfer | Detected here only incidentally by Task 3's smoke; the systematic four-corner guard is plan 24-07's `tests/test_phase24_band.py` |
| T-24-36 | Information Disclosure | held-out questions in the teaching bin | high | mitigate | `sanity_check` proof 6 runs on the real `adv_n8` build in Task 3E; D-03's tier filter is asserted in 24-05 |
| T-24-37 | Tampering | hardcoded empty persona at `:487` | medium | mitigate | The CLEAN loop keeps passing `[]` and stays byte-identical; only adversarial episodes carry a persona, at mask=0, and the call site is the allowlisted one from 24-05 |
| T-24-SC | Tampering | package installs | low | accept | No `pip install` in this plan; `random` and `math` are stdlib and `scripts/teach_persona.py` carries no import ceiling |
</threat_model>

<verification>
- `pytest -q tests/test_phase24_bins.py tests/test_phase21_aligned_bins.py tests/test_phase21_replay_volume.py tests/test_phase14_teaching.py tests/test_phase22_wiring.py tests/test_phase23_resume.py` — 0 failed
- `make test` green
- `git diff scripts/phase18_extraction.py scripts/mitigation_gate.py` is EMPTY
- `git status --porcelain data/` is empty
</verification>

<success_criteria>
- `build_bins(..., adversarial_ratio=0.0)` is byte-identical to the no-kwarg call, proved against the v2.0 golden fixture
- The wiring sibling was watched RED and is now green
- The interleave permutation is a pure function of `seed`, proved in three directions
- Every non-zero grid point selects all three trained families within 1 of each other, asserted from `stats["adversarial_family_counts"]`
- `adv_n8` / `adv_n64` exist, pack flat, return `replay_ratio = 0.0`, and build cleanly at the upper extreme
</success_criteria>

<output>
Create `.planning/phases/24-adversarial-extraction-aware-training-the-held-out-attack-fa/24-06-SUMMARY.md`
when done. It MUST quote the Task 1 RED output (all six tests), test 2's pre-wiring no-kwarg sha256 baseline,
the per-grid-point `adversarial_family_counts`, and the `mask_fraction` printed by the Task 3E
`adv_n8` smoke at the upper extreme.
</output>
