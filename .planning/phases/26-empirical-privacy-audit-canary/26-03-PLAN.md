---
phase: 26-empirical-privacy-audit-canary
plan: 03
type: execute
wave: 3
depends_on: ["26-02"]
files_modified:
  - tests/test_phase26_canary.py
autonomous: true
requirements: [CANARY-01, CANARY-02]
must_haves:
  truths:
    - "The items builder equals calibration_items exactly on LOCKED_FACTS and renders OUT through the filler grammar at 784/504 (D-06, D-08)"
    - "The dry-run and reuse paths never import torch; the driver has no top-level torch-touching import and no torch.load (D-16, T-26-01)"
    - "A sidecar is reused only when its adapter_sha256 equals the frontier's; a partial set makes emit() refuse naming the note; a forged power-gate pass goes RED on a copy (D-16, D-19, D-03)"
    - "The LIVE path is wired: main(argv) → score_off_once → score_point → emit runs end to end on stubbed draws, producing real producer records the consumer accepts, and the control's IN-taught sum reaches prove_reproduction (D-15; memory: dry-run tests hide an unwired driver)"
    - "The sibling artifact, once it exists, is pinned to the frontier both ways and the frontier stays at one commit (D-18)"
    - "The driver builds no git argv; the plist mirrors the recall agent (§O1 closed, D-16)"
  artifacts:
    - path: "tests/test_phase26_canary.py"
      provides: "CPU tests for the driver, the wiring proof, the plist, the link, the power-gate RED"
      min_lines: 250
  key_links:
    - from: "tests/test_phase26_canary.py"
      to: "scripts/phase26_canary.py"
      via: "main([...]) with monkeypatched phase14_recall.load_adapted_model / complete_question and SIDECAR_DIR=tmp_path"
      pattern: "canary\\.main\\("
    - from: "tests/test_phase26_canary.py"
      to: "results/phase26_canary.json ↔ results/phase25_frontier.json"
      via: "hashlib.sha256 of frontier bytes == artifact.frontier_sha256; 16 adapter_sha256 equal"
      pattern: "frontier_sha256"
---

<objective>
Pin the driver with CPU-only tests in the shape of `tests/test_phase25_recall.py`, and add the one
test that plan-checking memory demands: the live path (not `--dry-run`) traced from `main()` through
`score_off_once` / `score_point` into `emit()`, with the consumer fed real producer records.

Purpose: CANARY-01/02 verification contract (RESEARCH §Validation Architecture) and T-26-01/05/07
mitigations made structural.
Output: `tests/test_phase26_canary.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-empirical-privacy-audit-canary/26-CONTEXT.md
@.planning/phases/26-empirical-privacy-audit-canary/26-RESEARCH.md
@.planning/phases/26-empirical-privacy-audit-canary/26-PATTERNS.md
@.planning/phases/26-empirical-privacy-audit-canary/26-02-SUMMARY.md

<interfaces>
From scripts/phase26_canary.py (plan 26-02):
  RECORD, OPERATIONAL_NOTE, SIDECAR_DIR, PREREG_MODULE, INSTRUMENT
  sidecar_path(key) ; off_sidecar_path() ; frontier() (lru_cache) ; point_record(key)
  _items(facts, family_ids, forms=None) ; _lists() ; _score_list(model, tok, device, forbid, items, *, label, heartbeat_state=None)
  score_off_once(*, dry_run=False, heartbeat_path=None) -> (status, blob)
  score_point(point_key, *, dry_run=False, heartbeat_path=None) -> (status, blob)
  emit(out_path=RECORD, *, overwrite=False) -> blob
  build_parser() ; main(argv=None) -> 0

From scripts/phase14_recall.py (monkeypatch targets on the MODULE object — the driver does `import phase14_recall as pr` lazily and calls `pr.<name>` at call time):
  load_adapted_model(device, adapter_path=None) -> (model, cfg, tok, forbid, artifact)
  complete_question(model, tok, question, device, forbid, *, index) -> {"question","prompt_ids","completions","stopped"}
  score_question(completions, value) -> (k, n)     (REAL — do not stub; it is the boundary rule)
  CONVBASE_SLIM

From src/personacore/lora/inject.py: adapter_disabled(model) — no-op on a bare torch.nn.Module (prior == {})
From tests/conftest.py: sweep_is_active() ; SWEEP_ACTIVE_ENV_VAR = "PERSONACORE_SWEEP_ACTIVE"
From tests/test_phase25_driver.py:105-140: _git_argv_subcommands(path), _enclosing_function(tree, node)
From tests/test_phase25_recall.py: needs_adapters / needs_plutil markers, _plist(path), the subprocess dry-run script shape (:100-134)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Structural tests — items, imports, dry-run, sidecars, emit refusal, git surface, plist, torch.load</name>
  <files>tests/test_phase26_canary.py</files>
  <read_first>
    - tests/test_phase25_recall.py (WHOLE file: markers :9-43, `_fake_sidecar` :46-72, dry-run subprocess + AST :79-136, sidecar reuse/refusal :151-171, emit refusal :182-212, plist :218-248, write-once :282-296)
    - tests/test_phase25_driver.py lines 105-140 (`_git_argv_subcommands`, `_enclosing_function`) and 373-420 (planted-push RED on a tmp_path copy)
    - scripts/phase26_canary.py (plan 26-02 output)
    - artifacts/com.personacore.phase26.canary.plist and artifacts/com.personacore.phase25.recall.plist
    - tests/conftest.py lines 40-60 (`sweep_is_active`)
  </read_first>
  <action>
Create `tests/test_phase26_canary.py` with the sibling `sys.path` insert, `import phase26_canary as canary`, `import phase26_prereg`, `import phase25_record`, `import phase25_run`, a module-scoped `frontier` fixture (`json.loads(phase25_record.FRONTIER_RECORD.read_text())`), `_KEYS = phase26_prereg.audited_point_keys(frontier)` computed at import from the same one-time load, `_ADAPTERS_ON_DISK` (checkpoints dir exists and every `_ROOT / points[k]["adapter_path"]` exists for the 16 keys, and `phase14_recall.CONVBASE_SLIM` exists — import `phase14_recall` INSIDE a helper so collection stays torch-free), `needs_adapters`, `needs_plutil`, and `_fake_sidecar(key, *, sha=None)` building a blob with the four lists' `per_fact`/`per_question` shape from the driver's own `_score_list` return keys (8 IN facts, 56 OUT facts) plus `_fake_off_sidecar(*, base_sha)`.

Tests, EXACT names:
- `test_in_items_equal_calibration_items_exactly`: `canary._items(fs.LOCKED_FACTS, fs.TAUGHT_FAMILY_IDS) == tp.calibration_items(...)` and the held-out pair; lengths 112 / 72.
- `test_out_items_render_through_the_filler_grammar`: OUT lists 784 / 504; every OUT item's fact has `tier == "filler"`; no IN fact value appears (via `pr.contains_value`) in any OUT question and no OUT value in any IN question.
- `test_the_driver_imports_no_torch_touching_module_at_top_level`: AST of `scripts/phase26_canary.py` — no top-level import of `torch, teach_persona, phase14_factset, phase14_recall, phase21_filler, phase18_extraction`.
- `test_the_driver_never_calls_torch_load_directly` (T-26-01): no `ast.Attribute(attr="load")` on a `Name(id="torch")` anywhere in the driver.
- `test_the_dry_run_walks_off_and_sixteen_and_never_imports_torch` (`needs_adapters`): fresh-interpreter subprocess in the `tests/test_phase25_recall.py:101-134` shape running `canary.main(["--dry-run"])` with `SIDECAR_DIR` redirected to a temp dir and asserting `"torch" not in sys.modules` at the end; stdout contains exactly 17 `DRY RUN` lines, the 16 keys in `_KEYS` order after the OFF line.
- `test_a_matching_sidecar_is_reused` / `test_a_sidecar_for_a_different_adapter_is_refused` (`needs_adapters`, `monkeypatch.setattr(canary, "SIDECAR_DIR", tmp_path)`): `score_point("dp_n8_sigma0p500000", dry_run=True)` returns `("reused", blob)` on a matching fake sidecar; with `sha="0"*64` raises `SystemExit` containing `"REFUSED, not reused"`. Same pair for the OFF sidecar via `score_off_once(dry_run=True)` (`base_sha256` mismatch refused).
- `test_emit_refuses_a_partial_audit` (CPU, tmp SIDECAR_DIR): (a) nothing present → `SystemExit` naming `phase26_operational_note.md`, no file written; (b) OFF sidecar (with the REAL base hash if `_ADAPTERS_ON_DISK`, else mark the sub-case `needs_adapters`) + 15 fake point sidecars → message names the single missing key; no file written.
- `test_emit_refuses_to_overwrite_the_committed_artifact` (both-state idiom): if `canary.RECORD.exists()` → `emit()` raises with `"REFUSING to overwrite"` and `"--force"`, bytes unchanged; else assert the note either does not exist yet or carries no `## 7.` close block — never a silent pass without an assertion.
- `test_the_driver_never_commits` (T-26-05): `_git_argv_subcommands(_DRIVER) == []` (copy the helper from `tests/test_phase25_driver.py:105-125` into this file; import-by-copy is the established shape there); plus the planted-push RED: write a `tmp_path` copy of the driver with one appended line building `["git", "push"]` and assert the helper finds `("push", lineno, fn)`.
- `test_the_canary_agent_mirrors_the_recall_agent`: `plistlib` — `Label == "com.personacore.phase26.canary"`, `KeepAlive is False`, `RunAtLoad is False`, `ProgramArguments[:3]` equal to the recall plist's, `[3].endswith("scripts/phase26_canary.py")`, `--heartbeat` value equal to the recall plist's, `WorkingDirectory` equal, `StandardOutPath` differs, `EnvironmentVariables["PERSONACORE_SWEEP_ACTIVE"] == "1"`; `@needs_plutil` sub-test `plutil -lint` returncode 0.
- `test_sidecar_paths_refuse_a_path_separator` (T-26-06): `canary.sidecar_path("../x")` and `canary.point_record("dp_n8_sigma0p5")` (not an audited key) raise `SystemExit`.
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/pytest -q tests/test_phase26_canary.py -x -k "not live_path and not routes_its_in_taught and not power_gate and not sibling and not reproduced" && .venv/bin/ruff check tests/test_phase26_canary.py</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest -q tests/test_phase26_canary.py` on this host: 0 failed; the `needs_adapters` tests RUN here (adapters are on disk), not skipped
    - `PERSONACORE_SWEEP_ACTIVE=1 .venv/bin/pytest -q tests/test_phase26_canary.py` also 0 failed (no test in this task needs MPS)
    - `test_the_driver_never_commits` finds zero git argv literals in the driver and finds `push` in the planted copy
    - `test_emit_refuses_a_partial_audit` asserts both sub-cases and that no artifact file was created
    - `grep -c "def test_" tests/test_phase26_canary.py` ≥ 12 after this task
  </acceptance_criteria>
  <done>Structural tests committed and green on the host.</done>
</task>

<task type="auto">
  <name>Task 2: The wiring proof, the control's reproduction routing, the power-gate RED, the link test, the live D-15 test</name>
  <files>tests/test_phase26_canary.py</files>
  <read_first>
    - tests/test_phase26_canary.py (Task 1 state)
    - scripts/phase26_canary.py (`main`, `score_off_once`, `score_point`, `emit` — the kwargs each passes/accepts)
    - scripts/phase14_recall.py lines 300-325 (`score_question` stays REAL), 895-927 (`complete_question`'s return shape to stub)
    - src/personacore/lora/inject.py lines 157-184 (`adapter_disabled` on a bare module)
    - tests/test_phase25_calibrate.py lines 570-593 (watched-RED on a `tmp_path` COPY, never the tree)
    - tests/test_phase25_frontier.py lines 449-459 (digest-link assertions)
    - tests/test_phase25_close.py lines 321-324 (frontier single-commit)
    - scripts/phase25_prereg.py lines 176-215 (`prove_reproduction` signature; the driver looks it up as `phase25_prereg.prove_reproduction` at call time, so `monkeypatch.setattr(phase25_prereg, "prove_reproduction", recorder)` intercepts it)
  </read_first>
  <action>
Append to `tests/test_phase26_canary.py`:

- `test_the_live_path_is_wired_end_to_end` (`needs_adapters` — the adapter hash checks read real files; stub ONLY the model and the draws): `monkeypatch.setattr(canary, "SIDECAR_DIR", tmp_path)`; import `phase14_recall as pr` and `torch`; stub `pr.load_adapted_model` → `(torch.nn.Module(), None, None, frozenset(), None)`; stub `pr.complete_question(model, tok, question, device, forbid, *, index)` → `{"question": question, "prompt_ids": [], "completions": [f"answer {index}"] * 9, "stopped": [True]*9}` — signature identical to the real one (keyword-only `index`), so a kwarg the driver passes that the real function lacks would ALSO fail here; leave `pr.score_question` REAL; stub `phase25_prereg.prove_reproduction` with a recorder that asserts both args are `int` and not `bool` and records `(k, n)` (the stubbed draws cannot hit 790 — the live D-15 test below covers the real count); redirect heartbeat to `tmp_path/"hb.jsonl"`. Run `canary.main(["--points", phase26_prereg.CONTROL_KEY, "dp_n8_sigma80p000000", "--heartbeat", str(tmp_path/"hb.jsonl")])` and assert it returns 0, the OFF sidecar and the two point sidecars exist, each blob carries the four list keys with `per_fact` sizes 8/8/56/56 and `questions` 112/72/784/504, `draws_per_question == 9`, the control blob has `reproduction_gate.observed == [recorded k, recorded n]`, and the recorder was called exactly once with `n == 1008`. Then build the other 14 sidecars by COPYING the real σ=80 producer blob and substituting `point_key`, `adapter_sha256`, `adapter_path`, `sigma`, `epsilon_upper` from the frontier (so the consumer is fed the producer's real shape, not a hand-written one), and call `canary.emit(tmp_path/"canary.json")`: assert the returned blob has `audited_point_keys == list(_KEYS)`, `reachable_claims` matches `r"\d+/15"`, `auditor_ceiling` is a float, `power_gate` has keys `threshold, control_epsilon_lower, passed, sentence` with `sentence == phase26_prereg.POWER_SENTENCE`, `exclusions.out.of == 56`, every noised point's `verdict.verdict in phase26_prereg.VERDICTS`, the control's `verdict is None` and `comparison` starts with `"VACUOUS BY CONSTRUCTION"`, every point's `epsilon_sentence == frontier["epsilon_report"]["rendered"][key]`, every `verdict.reasons` string list is non-empty and mentions `"/"` counts, unreachable points (`epsilon_upper >= auditor_ceiling`) carry the D-13 clause via `_prose.normalized`, `frontier_sha256 == hashlib.sha256(FRONTIER_RECORD.read_bytes()).hexdigest()`, `prereg_module_sha256 == sha256(scripts/phase26_prereg.py)`, `summary` counts sum to 15. Also assert `heartbeat` file has ≥ 3 lines with `stage` values including `"score"` and `"done"`.
- `test_the_control_routes_its_in_taught_sum_through_prove_reproduction` (`needs_adapters`): same stubs; call `canary.score_point(phase26_prereg.CONTROL_KEY, heartbeat_path=tmp_path/"hb.jsonl")` with a recorder that raises `SystemExit("gate")` — assert `SystemExit` propagates AND `canary.sidecar_path(CONTROL_KEY)` does NOT exist afterwards (the gate runs BEFORE the write, D-15 "refused if it fails"); with a passing recorder the sidecar exists and the recorded `(k, n)` equals `(blob["in_taught"]["k"], blob["in_taught"]["n"])`.
- `test_the_power_gate_goes_red_on_a_forged_pass`: on a `tmp_path` COPY of an emitted artifact (reuse the wiring test's emission via a module-scoped helper, or emit again into `tmp_path`), set `power_gate["passed"] = True` and `power_gate["control_epsilon_lower"] = 0.1` (below the threshold), then re-derive with `phase26_prereg.power_gate(0.1, blob["power_gate"]["threshold"])["passed"]` and assert it is `False` and that every noised point re-verdicted through `phase26_prereg.point_verdict(point["fact_unit"], point["epsilon_upper"], power=re-derived gate, auditor_ceiling=blob["auditor_ceiling"])["verdict"]` is `"INCONCLUSIVE"` or `"BROKEN"` (never CONSISTENT); assert the real `canary.RECORD` bytes (if it exists) are unchanged.
- `test_the_sibling_is_pinned_to_the_frontier_both_ways` (both-state, D-18): if `canary.RECORD.exists()`: `frontier_sha256` equals the live hash of `results/phase25_frontier.json`, `frontier_bytes == 22311714`, all 16 `points[k].adapter_sha256` equal the frontier's, `prereg_module_sha256` equals the live module hash, `git log --oneline -- results/phase25_frontier.json` is exactly one line, and `git ls-files results/phase26_canary.json` is non-empty only if `git log --diff-filter=A -- results/phase26_canary.json` is non-empty; else: assert `git ls-files results/phase26_canary.json` is empty (absent on disk ⇒ absent in git — `bool(checked) == bool(tracked)` honesty).
- `test_every_point_carries_its_reasons_and_the_ceiling_disclosure` (both-state on the REAL artifact; the tmp emission is already asserted in the wiring test): if `RECORD` exists, every noised point has non-empty `reasons`, unreachable ones carry the D-13 clause, `reachable_claims` equals `f"{len(reachable_keys)}/15"`, and `summary` sums to 15; else assert the artifact is untracked.
- `test_the_control_reproduced_the_published_reading` (`needs_adapters` + `pytest.mark.skipif(sweep_is_active(), reason=...)` + skip if `canary.sidecar_path(CONTROL_KEY)` does not exist yet with reason "control sidecar not yet scored — lands during the 26-04 run"): read the REAL control sidecar and assert `in_taught.k, in_taught.n == 790, 1008`, `reproduction_gate.passed is True`, `device == "mps"`, `torch_version` present. This test never draws; it reads.
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/pytest -q tests/test_phase26_canary.py -x && PERSONACORE_SWEEP_ACTIVE=1 .venv/bin/pytest -q tests/test_phase26_canary.py tests/test_phase26_prereg.py && .venv/bin/ruff check tests/test_phase26_canary.py && .venv/bin/ruff format --check tests/test_phase26_canary.py</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest -q tests/test_phase26_canary.py::test_the_live_path_is_wired_end_to_end` PASSES on this host and, in its SUMMARY, the executor quotes the emitted tmp artifact's `power_gate`, `auditor_ceiling`, `reachable_claims` and one noised point's `verdict.reasons`
    - `test_the_control_routes_its_in_taught_sum_through_prove_reproduction` proves the gate precedes the write (no sidecar after a `SystemExit` from the recorder)
    - `test_the_power_gate_goes_red_on_a_forged_pass` passes and asserts no CONSISTENT survives a failed gate
    - `test_the_sibling_is_pinned_to_the_frontier_both_ways` and `test_every_point_carries_its_reasons_and_the_ceiling_disclosure` pass in the ABSENT state today with a real assertion (`git ls-files results/phase26_canary.json` empty), not a bare `return`
    - `test_the_control_reproduced_the_published_reading` is reported as SKIPPED today with the "not yet scored" reason
    - Full suite `make test` green on the host (report counts in the SUMMARY; the expected delta is +≈20 passed / +1 skipped over the Phase-25 close counts)
  </acceptance_criteria>
  <done>All tests committed; live path proven on stubbed draws through the real `main()`; the consumer accepted real producer records.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tests → driver | Tests stub only the model and the draw primitive; the boundary rule (`score_question`), hashing and writes stay real |
| repo → artifact link | `results/phase26_canary.json` ↔ `results/phase25_frontier.json` by sha256 both ways |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Tampering | checkpoint deserialisation | mitigate | `test_the_driver_never_calls_torch_load_directly` (AST) |
| T-26-03 | Repudiation | post-hoc favourable reading | mitigate | `test_the_power_gate_goes_red_on_a_forged_pass` (copy, never the tree); `test_emit_refuses_a_partial_audit`; link test keeps the frontier at one commit |
| T-26-05 | Elevation | driver git surface | mitigate | `test_the_driver_never_commits` — empty git argv set, planted `push` RED on a copy |
| T-26-06 | Tampering | key-derived paths | mitigate | `test_sidecar_paths_refuse_a_path_separator` |
| T-26-07 | Spoofing | wrong-weights sidecar | mitigate | `test_a_sidecar_for_a_different_adapter_is_refused` (+ OFF `base_sha256` pair) |
| T-26-08 | Information disclosure | sidecars / artifact | accept | No PII: 8 locked persona facts and 56 synthetic filler facts already published in the frontier |
| T-26-SC | Tampering | package installs | accept | Nothing installed |
</threat_model>

<verification>
- `.venv/bin/pytest -q tests/test_phase26_canary.py tests/test_phase26_prereg.py` green on the host with the adapters present
- `PERSONACORE_SWEEP_ACTIVE=1` run also green (skips only the live D-15 read)
- `make test` green; `tests/test_phase25_close.py` green
</verification>

<success_criteria>
- Every behaviour in RESEARCH §Validation Architecture has a named test that exists and passes (or skips with the pre-run reason)
- The live path is exercised by a test, not only `--dry-run`
</success_criteria>

<output>
Create `.planning/phases/26-empirical-privacy-audit-canary/26-03-SUMMARY.md` when done
</output>
