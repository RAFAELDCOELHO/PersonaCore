---
phase: 26-empirical-privacy-audit-canary
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phase26_prereg.py
  - tests/test_phase26_prereg.py
autonomous: true
requirements: [CANARY-01, CANARY-02]
must_haves:
  truths:
    - "The committed audit_target_rule, imported by reference from phase25_prereg, resolves against the frontier to dp_n8_sigma0p000000 and its extension names all 15 noised dp_n8 points in point_keys order — never a subset (D-01, D-12)"
    - "epsilon_lower(members, n_in, nonmembers, n_out) reproduces the six hand-computed rows to 1e-4 from the two IMPORTED Wilson bounds with delta = mitigation_unit.DELTA, and names every degenerate case instead of clipping (D-09, D-11)"
    - "The power threshold is derived from the artifact (min epsilon_upper over the 15 noised points) and asserted equal to the sigma=80 record and to epsilon_for(80.0, 200, DELTA) (D-03, D-04)"
    - "The verdict domain is exactly BROKEN / CONSISTENT / INCONCLUSIVE, one-sided, with reasons carrying the numbers and the D-13 ceiling clause (D-05, D-13)"
    - "The deciding tier (taught), the D-07 exclusion scope (either tier), the membership rule (>= 1 question) and the D-17 waiver continuation are pre-registered as module constants with a rationale each (D-07, D-10, D-14)"
    - "Every commit touching scripts/phase26_prereg.py precedes the first-add of every tracked results/phase26_* file, and scripts/phase25_prereg.py stays byte-identical since the frontier (D-12)"
  artifacts:
    - path: "scripts/phase26_prereg.py"
      provides: "The dated continuation: rule by reference, resolver, extension, power gate, D-09 arithmetic, ceiling, verdict domain, pre-registered constants, D-40 publication-obligation continuation"
      exports: ["RULE", "COMMITTED", "SIDECARS_AT_COMMIT", "CONTROL_KEY", "resolve_audit_target", "audited_point_keys", "noised_point_keys", "power_threshold", "POWER_SENTENCE", "epsilon_lower", "auditor_ceiling", "VERDICTS", "verdict", "point_verdict", "power_gate", "DECIDING_TIER", "EXCLUSION_SCOPE", "MEMBERSHIP_RULE", "JOINT_COVERAGE", "WAIVER_CONTINUATION", "PUBLICATION_OBLIGATION_CONTINUATION"]
      min_lines: 200
    - path: "tests/test_phase26_prereg.py"
      provides: "Ancestry guards (both prereg modules), rule resolution, extension, threshold, six-row formula table, degenerate cases, verdict domain, continuation data"
      min_lines: 150
  key_links:
    - from: "scripts/phase26_prereg.py"
      to: "scripts/phase25_prereg.py"
      via: "RULE = phase25_prereg.CANARY_RESERVATIONS['audit_target_rule'] (by reference, never retyped)"
      pattern: "CANARY_RESERVATIONS\\[\"audit_target_rule\"\\]"
    - from: "scripts/phase26_prereg.py"
      to: "scripts/erasure_gate.py + scripts/phase20_gate_coverage.py"
      via: "wilson_upper_bound / wilson_lower_bound imported, never re-implemented"
      pattern: "phase20_gate_coverage\\.wilson_lower_bound|erasure_gate\\.wilson_upper_bound"
    - from: "tests/test_phase26_prereg.py"
      to: "git history"
      via: "git log -- scripts/phase26_prereg.py vs git log --diff-filter=A -- results/phase26_*"
      pattern: "merge-base"
---

<objective>
Commit the pre-registration BEFORE any number exists: `scripts/phase26_prereg.py` is the dated
continuation of `phase25_prereg.CANARY_RESERVATIONS["audit_target_rule"]` (imported by reference),
carrying the extension to all 15 noised `dp_n8` points, the power gate, the D-07 precondition, the
D-09 arithmetic, the deciding unit, the membership rule, the three-valued verdict domain, the D-13
ceiling disclosure and the D-40 publication-obligation continuation — plus the tests that freeze it
by git ancestry.

Purpose: CANARY-02 requires the rule "ε_lower > ε_upper ⇒ provably broken" committed before the
audit runs, with no room for a favourable reading afterward. The ancestry guard makes that a proof.
Output: `scripts/phase26_prereg.py`, `tests/test_phase26_prereg.py`, both committed; zero tracked
`results/phase26_*` files at the prereg commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-empirical-privacy-audit-canary/26-CONTEXT.md
@.planning/phases/26-empirical-privacy-audit-canary/26-RESEARCH.md
@.planning/phases/26-empirical-privacy-audit-canary/26-PATTERNS.md

<interfaces>
<!-- Verified at HEAD efb4f0f (RESEARCH §Standard Stack). Use directly; no exploration needed. -->

From scripts/phase25_prereg.py:
  COMMITTED = "2026-08-31"                       (:54)
  POINT_RECORDS_AT_COMMIT = 0                    (:55)
  REPRODUCTION_K = 790 ; REPRODUCTION_N = 1008   (:88-89)
  def prove_reproduction(k, n) -> None | SystemExit   (:176)  ints only, bool refused
  def point_record_path(point_key) -> str        (:276)  charset refusal: alnum, "-", "_"
  PUBLICATION_OBLIGATION = ((field_path, why), ...)   (:424)
  CANARY_RESERVATIONS = {"adapter_retention":..., "canary_population_rule":..., "audit_target_rule":..., "disk_precheck_bytes":...}   (:496)
  GIT_SURFACE_EXCEPTION (contains "IT ENDS WITH THIS PHASE")   (:614)

From scripts/erasure_gate.py:
  _Z_ONE_SIDED_95 = 1.6448536269514722           (:90)
  VERDICTS = ("SUCCESS", "FAILURE", "INCONCLUSIVE")   (:136)
  def wilson_upper_bound(successes, n, z=_Z_ONE_SIDED_95) -> float   (:139)  min(1.0, ...); ValueError on n <= 0

From scripts/phase20_gate_coverage.py:
  def wilson_lower_bound(successes, n, z=erasure_gate._Z_ONE_SIDED_95) -> float   (:124)  successes == 0 -> 0.0 exactly

From scripts/mitigation_unit.py:
  PRIVACY_UNIT = "one taught fact"   (:85)
  DELTA = 1e-5                       (:171)

From src/personacore/privacy/accountant.py:
  def epsilon_for(sigma, steps, delta) -> float   (:819)  three positional args

From scripts/phase25_record.py:
  FRONTIER_RECORD = _ROOT / "results" / "phase25_frontier.json"   (:107)
  def ORDERED_POINT_KEYS() -> list[str]  (44 keys; a FUNCTION)   (:253)

From scripts/phase21_filler.py:
  GUESSABILITY_WAIVER   (:391)  the D-17 waiver as data

From scripts/_prose.py:
  def normalized(text) -> str   (:35)  " ".join(text.split())

Frontier facts (results/phase25_frontier.json, sha256 1f182b40c9d7c316e57ecd69acd62c7f6407514b9c675bdf8ec677d3f0cb97d5):
  the 16 dp_n8 keys in point_keys order: dp_n8_sigma0p000000, 0p500000, 0p700000, 1p000000, 1p500000,
  2p000000, 3p000000, 4p000000, 6p000000, 8p000000, 12p000000, 16p000000, 24p000000, 32p000000,
  50p000000, 80p000000. points[k].epsilon is None at the control (epsilon_omitted_reason present);
  points["dp_n8_sigma80p000000"].epsilon == 0.6339783761989397. points[k].arm endswith "n8";
  points[k].canary_population.has_out_of_corpus_canaries is True on every n8 point.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write scripts/phase26_prereg.py — the dated continuation and the instrument's arithmetic</name>
  <files>scripts/phase26_prereg.py</files>
  <read_first>
    - scripts/phase21_unit_continuation.py (lines 1-60 and 112-196: the continuation-by-reference shape, `SUPERSEDES` as data, `_prove` -> SystemExit with the `-O` rationale)
    - scripts/phase25_prereg.py (lines 43-135: `COMMITTED`/`POINT_RECORDS_AT_COMMIT` header and `_prove`; 176-215: `prove_reproduction`'s int-only refusal register; 276-296: `point_record_path`; 414-470: `PUBLICATION_OBLIGATION_SCOPE` + `PUBLICATION_OBLIGATION` (field_path, why) shape; 496-540: `CANARY_RESERVATIONS`)
    - tests/test_phase25_close.py (lines 274-297: `_resolve_audit_target` — the resolver to lift, converting `assert` to `_prove`)
    - scripts/erasure_gate.py (lines 86-175: `_Z_ONE_SIDED_95`, `VERDICTS`, `wilson_upper_bound`)
    - scripts/phase20_gate_coverage.py (lines 95-99 sibling-import idiom; 124-186 `wilson_lower_bound`)
    - scripts/mitigation_unit.py (lines 85-90, 168-180: `PRIVACY_UNIT`, `DELTA`)
    - scripts/phase21_filler.py (lines 391-415: `GUESSABILITY_WAIVER`)
    - .planning/phases/26-empirical-privacy-audit-canary/26-RESEARCH.md §"Code Examples" 4 (the D-09 body and the six hand-computed pins) and §"Open Questions" 2-3
  </read_first>
  <action>
Create `scripts/phase26_prereg.py` as a CPU-only, stdlib + sibling-scripts module (NO torch, NO import of `phase14_*`, `teach_persona`, `phase21_filler`'s neighbours at module scope — `phase21_filler` is imported lazily inside nothing; reference the waiver by its dotted NAME string, not by import, so this module stays importable without `phase14_factset`). Use the `sys.path.insert(0, _REPO_ROOT/"scripts")` idiom and `_prove(condition, message) -> SystemExit("[phase26_prereg] ...")` (never `assert`).

Module docstring: a DATED CONTINUATION of `phase25_prereg.CANARY_RESERVATIONS["audit_target_rule"]` and of `phase21_filler.GUESSABILITY_WAIVER`; both originals stay byte-identical and visible (D-01, D-07, D-12); this module is ancestry-guarded — every commit touching it must precede the first-add of every tracked `results/phase26_*` file; corrections after that point go in a further continuation module, never an edit (memory: pin-corrections-are-dated-continuations).

Declare, in this order, with these exact names (the driver in plan 26-02 and the tests import them):

1. `COMMITTED = "<today's ISO date>"`, `SIDECARS_AT_COMMIT = 0` (mirror `phase25_prereg.py:54-55`; the certified property: `git ls-files 'results/phase26_*'` returned nothing at this commit).
2. `SUPERSEDES = ("phase25_prereg.CANARY_RESERVATIONS['audit_target_rule']", "phase21_filler.GUESSABILITY_WAIVER")`.
3. `RULE = phase25_prereg.CANARY_RESERVATIONS["audit_target_rule"]` — BY REFERENCE, never retyped.
4. `CONTROL_KEY = "dp_n8_sigma0p000000"`, `AUDITED_ARM_PREFIX = "dp_n8"`.
5. `def resolve_audit_target(frontier) -> str`: the lifted resolver (n=8 keys in `point_keys` order; `_prove` every n8 point has `canary_population.has_out_of_corpus_canaries`; first PASS else first n8). `_prove(result == CONTROL_KEY, ...)` so the module itself proves the committed rule yields the control (D-12).
6. `def audited_point_keys(frontier) -> tuple[str, ...]`: the dp_n8 subsequence of `frontier["point_keys"]` (16, control first); `_prove` it equals `tuple(k for k in phase25_record.ORDERED_POINT_KEYS() if k.startswith(AUDITED_ARM_PREFIX))` and `len == 16` and `[0] == CONTROL_KEY`. `def noised_point_keys(frontier)` = `audited_point_keys(frontier)[1:]` (15). Extension text `EXTENSION` (D-01): all 15 noised n=8 points, each against its OWN `epsilon` at δ=1e-5 (D-02), never a subset chosen after a result; the curve total is context only.
7. `def power_threshold(frontier) -> float`: `min(points[k]["epsilon"] for k in noised_point_keys(frontier))`; `_prove(== points["dp_n8_sigma80p000000"]["epsilon"])` (D-04, "derived by rule, asserted equal to the σ=80 record"). `POWER_SENTENCE = "The instrument must resolve at least the smallest claim it checks."` `def power_gate(control_epsilon_lower, threshold) -> dict` = `{"threshold", "control_epsilon_lower", "passed": control_epsilon_lower is not None and control_epsilon_lower >= threshold, "sentence": POWER_SENTENCE}` (D-03/D-04; None from a degenerate control reading fails the gate).
8. Pre-registered constants with a one-line rationale each (RESEARCH Open Questions 2-3, decided here, not left open): `DECIDING_TIER = "taught"` (`DECIDING_TIER_RATIONALE`: the CONTEXT's arithmetic — n_IN = 112 taught questions, ~14 per fact — is the taught tier; held-out is REPORTED beside it at both units, never decides); `EXCLUSION_SCOPE = "either"` (`EXCLUSION_SCOPE_RATIONALE`: D-07 says ANY adapter-off success — a fact with ≥ 1 adapter-off answered question in EITHER tier is excluded from its population, the strictest reading; counted and published as `excluded n / 56`, same rule for the 8 IN); `MEMBERSHIP_RULE` (D-14: a fact is ANSWERED under adapter-on when ≥ 1 of its DECIDING_TIER questions has k > 0 — existential, as `per_fact.n_answerable`; a question is answered when ≥ 1 of its 9 draws contains the value); `UNIT = mitigation_unit.PRIVACY_UNIT` by reference (D-10: the fact decides; the question unit — n_IN = 112, n_OUT ≤ 784 — travels beside it and never decides; draws are never a unit).
9. `Z = erasure_gate._Z_ONE_SIDED_95` by reference (D-11, no new z); `DELTA = mitigation_unit.DELTA` by reference; `JOINT_COVERAGE = ">= 0.90 (Bonferroni over two one-sided 95% Wilson bounds, z = erasure_gate._Z_ONE_SIDED_95)"`.
10. `def epsilon_lower(members, n_in, nonmembers, n_out, *, delta=None) -> dict` — RESEARCH Code Example 4 exactly: refuse non-`int`/`bool` counts with `_prove` (the `prove_reproduction` register); `tpr_lb = phase20_gate_coverage.wilson_lower_bound(members, n_in)`; `fpr_ub = erasure_gate.wilson_upper_bound(nonmembers, n_out)`; direction 1 = `ln((tpr_lb - delta)/fpr_ub)` unless `tpr_lb - delta <= 0` (name `"TPR_lb <= delta: direction 1 undefined"`); direction 2 = `ln((1 - fpr_ub - delta)/(1 - tpr_lb))` unless `1 - fpr_ub - delta <= 0` (name `"FPR_ub >= 1 - delta: direction 2 undefined"`) or `tpr_lb >= 1` (name `"TPR_lb == 1: direction 2 undefined"`, unreachable at z > 0, named anyway); `epsilon_lower = max(finite directions)` or `None` when both are undefined — NEVER `max(0, ...)` clipping (D-09 "named, not clipped"). Return keys: `members_answered, n_in, nonmembers_answered, n_out, tpr_lb, fpr_ub, direction_1, direction_2, epsilon_lower, degenerate (list), z, delta, joint_coverage`.
11. `def auditor_ceiling(n_in, n_out) -> float` = `epsilon_lower(n_in, n_in, 0, n_out)["epsilon_lower"]` (D-13: TPR = n_IN/n_IN, FPR = 0/n_OUT on the REAL n's after D-07 exclusions).
12. `VERDICTS = ("BROKEN", "CONSISTENT", "INCONCLUSIVE")` (register of `erasure_gate.VERDICTS`, D-05). `def verdict(eps_lower, eps_upper, *, power_passed) -> str`: BROKEN iff `eps_lower is not None and eps_lower > eps_upper`; else CONSISTENT iff `power_passed`; else INCONCLUSIVE. `_prove(result in VERDICTS)`.
13. `def point_verdict(reading, epsilon_upper, *, power, auditor_ceiling) -> dict` = `{"verdict": verdict(...), "reasons": [...]}` where `reasons` (strings carrying the numbers, D-05) name in order: `members_answered/n_in` and `nonmembers_answered/n_out` at the fact unit; `TPR_lb` and `FPR_ub` with `z`; both directions (or the degenerate name); `epsilon_lower` vs `epsilon_upper` with the one-sided clause ("this test can only accuse; CONSISTENT is not 'verified correct'"); `JOINT_COVERAGE`; when `epsilon_upper >= auditor_ceiling` the D-13 sentence `"epsilon_upper >= auditor_ceiling: this comparison could not have failed"`; when the power gate failed, `POWER_SENTENCE` and the observed control reading vs threshold. No bare ε rendering is authored here beyond these numeric reasons — the sanctioned sentence (`phase25_epsilon.report_epsilon`) is attached by the driver's `emit` beside each point (plan 26-02).
14. `WAIVER_CONTINUATION = {"supersedes": "phase21_filler.GUESSABILITY_WAIVER", "why": "its premise 'filler is never scored' is false from this phase: the adapter-off arm IS the guessability probe (D-07); any filler fact with an adapter-off hit in either tier is excluded, counted and published", "committed": COMMITTED}`.
15. `PUBLICATION_OBLIGATION_CONTINUATION` — the D-40 continuation of `phase25_prereg.PUBLICATION_OBLIGATION` in the SAME `(field_path, why)` tuple shape, `SUPERSEDES_OBLIGATION = "phase25_prereg.PUBLICATION_OBLIGATION"` (extends by reference; the original tuple is untouched). Entries address paths INTO `results/phase26_canary.json`: `power_gate.passed` (+ sentence), `auditor_ceiling`, `reachable_claims`, `exclusions.out.n`, `points.<key>.verdict.verdict` ("Phase 28 may quote any ε ONLY beside this verdict and its reasons", D-18), `points.<key>.verdict.reasons`, and the D-19 clause as a final entry whose field_path is the literal `"<artifact absent>"` with why = "if `results/phase26_canary.json` does not exist, the report must say 'audit not executed / partial' beside EVERY ε it quotes, citing the dated D-19 entry in results/phase26_operational_note.md". State in the docstring why this is a Python constant and not a `scripts/_addendum.py` append: `_addendum.append_addendum` is a markdown writer requiring a `pending` placeholder that the Phase-25 note does not carry, and `phase21_unit_continuation.py:14-20` records that a Python contract is continued by a Python module (T-26-03 mitigation: the obligation is data a Phase-28 test resolves, not prose).
16. `ARTIFACT_GLOB = "results/phase26_*"` (the ancestry guard's tracked set) — a constant the test reads rather than retypes.

Threat references this task mitigates: T-26-03 (post-hoc favourable reading — every threshold, scope and formula is committed data before any sidecar exists).
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/python -c "import sys; sys.path.insert(0,'scripts'); import json, phase26_prereg as p, phase25_prereg, phase25_record, mitigation_unit; f=json.load(open('results/phase25_frontier.json')); assert p.RULE is phase25_prereg.CANARY_RESERVATIONS['audit_target_rule']; assert p.resolve_audit_target(f)=='dp_n8_sigma0p000000'; assert len(p.audited_point_keys(f))==16 and len(p.noised_point_keys(f))==15; assert p.power_threshold(f)==f['points']['dp_n8_sigma80p000000']['epsilon']; r=p.epsilon_lower(8,8,0,56); assert abs(r['epsilon_lower']-2.7859)<1e-4, r; assert p.epsilon_lower(0,1008,0,784)['direction_1'] is None; assert p.VERDICTS==('BROKEN','CONSISTENT','INCONCLUSIVE'); assert 'torch' not in sys.modules; print('OK')" && .venv/bin/ruff check scripts/phase26_prereg.py && .venv/bin/ruff format --check scripts/phase26_prereg.py</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -c "import sys; sys.path.insert(0,'scripts'); import phase26_prereg, sys as s; assert 'torch' not in s.modules and 'phase14_factset' not in s.modules"` exits 0 (CPU-only at import)
    - `phase26_prereg.RULE is phase25_prereg.CANARY_RESERVATIONS["audit_target_rule"]` is True (by reference)
    - `resolve_audit_target(frontier) == "dp_n8_sigma0p000000"`; `audited_point_keys(frontier)[0] == "dp_n8_sigma0p000000"`; `len(noised_point_keys(frontier)) == 15`; `audited_point_keys(frontier) == tuple(k for k in phase25_record.ORDERED_POINT_KEYS() if k.startswith("dp_n8"))`
    - `power_threshold(frontier) == frontier["points"]["dp_n8_sigma80p000000"]["epsilon"] == 0.6339783761989397`
    - `epsilon_lower(8, 8, 0, 56)["epsilon_lower"] == pytest.approx(2.7859, abs=1e-4)`; `epsilon_lower(790, 1008, 0, 784)` gives direction_1 ≈ 5.4003 and direction_2 ≈ 1.4306; `epsilon_lower(0, 1008, 0, 784)` has `direction_1 is None`, `"TPR_lb <= delta" in degenerate[0]`, `direction_2 == pytest.approx(-0.0035, abs=1e-4)` and `epsilon_lower == pytest.approx(-0.0035, abs=1e-4)` (not clipped to 0)
    - `epsilon_lower(True, 8, 0, 56)` and `epsilon_lower(8.0, 8, 0, 56)` raise `SystemExit` (int-only register)
    - `verdict(3.0, 2.4, power_passed=True) == "BROKEN"`; `verdict(1.0, 2.4, power_passed=True) == "CONSISTENT"`; `verdict(1.0, 2.4, power_passed=False) == "INCONCLUSIVE"`; `verdict(None, 2.4, power_passed=False) == "INCONCLUSIVE"`
    - `DECIDING_TIER == "taught"`, `EXCLUSION_SCOPE == "either"`, `Z is erasure_gate._Z_ONE_SIDED_95`, `DELTA == mitigation_unit.DELTA`, `SIDECARS_AT_COMMIT == 0`, `WAIVER_CONTINUATION["supersedes"] == "phase21_filler.GUESSABILITY_WAIVER"`, `SUPERSEDES_OBLIGATION == "phase25_prereg.PUBLICATION_OBLIGATION"`, every entry of `PUBLICATION_OBLIGATION_CONTINUATION` is a 2-tuple of non-empty strings and the last entry's field_path is `"<artifact absent>"`
    - `POWER_SENTENCE == "The instrument must resolve at least the smallest claim it checks."`
    - `ruff check` and `ruff format --check` pass on the file; `git ls-files 'results/phase26_*'` is empty at the commit that adds this file
  </acceptance_criteria>
  <done>Module committed (commit message names it as the Phase-26 pre-registration); zero tracked results/phase26_* at that commit; the one-liner verify prints OK.</done>
</task>

<task type="auto">
  <name>Task 2: Write tests/test_phase26_prereg.py — ancestry guards, resolution, extension, threshold, formula table, verdict domain</name>
  <files>tests/test_phase26_prereg.py</files>
  <read_first>
    - scripts/phase26_prereg.py (as committed by Task 1)
    - tests/test_phase16_prereg.py (lines 160-215 `_git` helper; 322-400 the ancestry guard body with `adds[-1]`, `merge-base --is-ancestor`, `checked == len(prereg_commits) * len(tracked)` and `bool(checked) == bool(tracked)`)
    - tests/test_phase20_prereg.py (lines 222-242: the strictly-after conjunct `prereg != first_add`)
    - tests/test_phase25_close.py (lines 274-297 rule-resolution prose clauses through `_prose.normalized`; 321-324 the frontier's single-commit assertion)
    - tests/test_phase25_frontier.py (lines 62-64: module-scoped `artifact` fixture that loads the frontier ONCE)
    - .github/workflows/ci.yml line 28 (`fetch-depth: 0`) — the shallow-repo assertion depends on it
  </read_first>
  <action>
Create `tests/test_phase26_prereg.py` with the sibling `sys.path` insert, a module-scoped `artifact` fixture loading `phase25_record.FRONTIER_RECORD` once, and the `_git(*args)` helper. Tests, with these EXACT names (the VALIDATION map carries them):

- `test_phase26_prereg_is_frozen_before_every_phase26_result`: the Phase-18-mould guard over `scripts/phase26_prereg.py` vs `git ls-files <phase26_prereg.ARTIFACT_GLOB>`; assert `rev-parse --is-shallow-repository == "false"` first; `prereg_commits` non-empty; for each tracked artifact `first_add = adds[-1]`; the strictly-after conjunct `prereg != first_add`; `merge-base --is-ancestor prereg first_add` with `check=True`; `checked == len(prereg_commits) * len(tracked)`; `bool(checked) == bool(tracked)` (honest with zero artifacts today).
- `test_phase25_prereg_is_byte_identical_since_the_frontier` (D-12): same loop with prereg path `scripts/phase25_prereg.py` and tracked set `["results/phase25_frontier.json"] + git ls-files results/phase26_*`; plus `git diff -- scripts/phase25_prereg.py` empty and `git log --oneline -- results/phase25_frontier.json` exactly one line.
- `test_the_committed_rule_resolves_to_the_control`: `phase26_prereg.RULE is phase25_prereg.CANARY_RESERVATIONS["audit_target_rule"]`; the three prose clauses from `tests/test_phase25_close.py:286-291` via `_prose.normalized`; `resolve_audit_target(artifact) == "dp_n8_sigma0p000000" == phase26_prereg.CONTROL_KEY`.
- `test_the_extension_is_all_fifteen_in_point_keys_order`: `audited_point_keys(artifact)` has 16 entries, control first, equals the dp_n8 subsequence of `artifact["point_keys"]` and of `ORDERED_POINT_KEYS()`; `noised_point_keys` is the last 15; every noised point's `epsilon` is a float and the control's is `None`.
- `test_the_power_threshold_is_the_smallest_audited_claim`: `power_threshold(artifact) == artifact["points"]["dp_n8_sigma80p000000"]["epsilon"] == pytest.approx(0.6339783761989397) == accountant.epsilon_for(80.0, 200, mitigation_unit.DELTA)`; `power_gate(2.7859, threshold)["passed"] is True`; `power_gate(0.5, threshold)["passed"] is False`; `power_gate(None, threshold)["passed"] is False`; `power_gate(...)["sentence"] == POWER_SENTENCE`.
- `test_epsilon_lower_matches_the_hand_computed_table` parametrized over the six rows `(members, n_in, nonmembers, n_out) -> (tpr_lb, fpr_ub, d1, d2)`: `(790,1008,0,784)->(0.7617,0.0034,5.4003,1.4306)`, `(1008,1008,0,784)->(0.9973,0.0034,5.6699,5.9196)`, `(8,8,0,56)->(0.7473,0.0461,2.7859,1.3283)`, `(7,8,0,56)->(0.5889,0.0461,2.5476,0.8416)`, `(8,8,1,56)->(0.7473,0.0762,2.2836,1.2962)`, `(0,1008,0,784)->(0.0,0.0034,None,-0.0035)`; `pytest.approx(abs=1e-4)`; `epsilon_lower == max` of the finite directions; `z == erasure_gate._Z_ONE_SIDED_95`; `delta == mitigation_unit.DELTA`.
- `test_zero_members_names_direction_one_undefined`: `(0, 1008, 0, 784)` → `direction_1 is None`, `"TPR_lb <= delta" in degenerate[0]`, `epsilon_lower == direction_2` (negative, NOT clipped).
- `test_all_nonmembers_names_direction_two_undefined`: `(8, 8, 56, 56)` → `fpr_ub == 1.0`, `direction_2 is None`, `"FPR_ub >= 1 - delta" in degenerate`.
- `test_counts_are_ints_only`: `bool` and `float` counts raise `SystemExit`.
- `test_auditor_ceiling_is_the_perfect_reading`: `auditor_ceiling(8, 56) == pytest.approx(2.7859, abs=1e-4)`; `auditor_ceiling(8, 50) < auditor_ceiling(8, 56)` (Pitfall 6: exclusions lower the ceiling); with the artifact, exactly 4 of the 15 noised `epsilon` values are `< auditor_ceiling(8, 56)` (σ 24, 32, 50, 80).
- `test_verdict_domain_is_three_valued_and_one_sided`: `VERDICTS == ("BROKEN","CONSISTENT","INCONCLUSIVE")`; the four `verdict(...)` cases from Task 1's criteria; `point_verdict(epsilon_lower(8,8,0,56), 2.3957449097512216, power=power_gate(2.7859, 0.634), auditor_ceiling=2.7859)["verdict"] == "BROKEN"` and the reasons list contains `"8/8"`, `"0/56"`, `"TPR_lb"`, `"FPR_ub"`, `"epsilon_lower"`, the joint-coverage clause; with `epsilon_upper = 519.6981942303134` the verdict is CONSISTENT and a reason contains `_prose.normalized("epsilon_upper >= auditor_ceiling: this comparison could not have failed")`; with a failed power gate the verdict is INCONCLUSIVE and a reason contains `POWER_SENTENCE`.
- `test_the_continuations_are_data`: `SUPERSEDES`, `WAIVER_CONTINUATION` (keys `supersedes`, `why`, `committed == COMMITTED`; `phase21_filler.GUESSABILITY_WAIVER` unchanged — compare by importing `phase21_filler` INSIDE the test), `DECIDING_TIER == "taught"`, `EXCLUSION_SCOPE == "either"`, `SIDECARS_AT_COMMIT == 0`, `SUPERSEDES_OBLIGATION == "phase25_prereg.PUBLICATION_OBLIGATION"`, every `PUBLICATION_OBLIGATION_CONTINUATION` entry a 2-tuple of non-empty strings, the last field_path `"<artifact absent>"`, and no field_path of the continuation collides with one already in `phase25_prereg.PUBLICATION_OBLIGATION`.

Threat references: T-26-03 (guards make a post-hoc edit a RED test forever).
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/pytest -q tests/test_phase26_prereg.py -x && .venv/bin/ruff check tests/test_phase26_prereg.py && .venv/bin/pytest -q tests/test_phase25_close.py tests/test_phase16_prereg.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest -q tests/test_phase26_prereg.py` reports 0 failed and ≥ 16 passed (6 parametrized rows + 10 named tests), 0 skipped
    - `.venv/bin/pytest -q tests/test_phase26_prereg.py::test_phase26_prereg_is_frozen_before_every_phase26_result tests/test_phase26_prereg.py::test_phase25_prereg_is_byte_identical_since_the_frontier` passes with `git ls-files 'results/phase26_*'` empty
    - Planted RED watched once, on a THROWAWAY branch or a `tmp_path` clone — never on `main`: committing a one-byte change to `scripts/phase26_prereg.py` AFTER a fake `results/phase26_x.json` first-add makes the ancestry guard fail with the strictly-after / ancestor message; record the observed failure text in the SUMMARY and discard the branch
    - `tests/test_phase25_close.py` and `tests/test_phase16_prereg.py` still green
    - `grep -c "def test_" tests/test_phase26_prereg.py` ≥ 12
  </acceptance_criteria>
  <done>Test file committed; quick command green; the planted-RED observation recorded in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| results/phase25_frontier.json → phase26_prereg | Committed artifact read as data; pinned by sha256 and single commit |
| git history → ancestry guard | The ordering proof relies on a non-shallow clone |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-03 | Repudiation | scripts/phase26_prereg.py (thresholds, formula, scope) | mitigate | Committed before any sidecar; `test_phase26_prereg_is_frozen_before_every_phase26_result` (earliest add, strictly-after conjunct, `bool(checked) == bool(tracked)`); `phase25_prereg.py` byte-identity guard; corrections only by further continuation module |
| T-26-06 | Tampering | key-derived paths | mitigate | Keys come only from `audited_point_keys(frontier)` (16 literal frontier keys); the driver additionally routes every key through `phase25_prereg.point_record_path` (plan 26-02) |
| T-26-09 | Tampering | Wilson bounds / z | mitigate | Both bounds and `z` IMPORTED by reference; the six-row table pins the arithmetic; int-only counts (bool/float refused) |
| T-26-SC | Tampering | package installs | accept | No package is installed in this phase (RESEARCH: "Package Legitimacy Audit: not applicable"); stdlib + sibling scripts only |
</threat_model>

<verification>
- `.venv/bin/pytest -q tests/test_phase26_prereg.py tests/test_phase25_close.py tests/test_phase16_prereg.py` green
- `git ls-files 'results/phase26_*'` empty at both commits of this plan
- `scripts/phase25_prereg.py`, `scripts/teach_persona.py`, `scripts/phase18_extraction.py`, `results/phase25_frontier.json` untouched (`git diff --stat HEAD~2 -- <those four>` empty)
</verification>

<success_criteria>
- The pre-registration exists as committed code with every D-01..D-14 item declared as data or a pure function, and the two open questions decided as constants with rationale
- Both ancestry guards pass today and were watched RED on a throwaway branch
- No torch at import; no re-implemented bound; no retyped rule text
</success_criteria>

<output>
Create `.planning/phases/26-empirical-privacy-audit-canary/26-01-SUMMARY.md` when done
</output>
