---
phase: 26-empirical-privacy-audit-canary
plan: 04
type: execute
wave: 4
depends_on: ["26-03"]
files_modified:
  - results/phase26_operational_note.md
  - tests/test_phase26_canary.py
autonomous: false
requirements: [CANARY-01, CANARY-02]
must_haves:
  truths:
    - "The operational note's first-add commit is a descendant of every commit touching scripts/phase26_prereg.py, and records git ls-files results/phase26_* == 0 at the prereg commits (D-12 ancestry)"
    - "The launch posture is recorded before any MPS second: caffeinate owners, pmset, the ≈ 25 h budget, the wiring-proof test run on this host, the plist installed, PERSONACORE_SWEEP_ACTIVE=1 (D-16)"
    - "The run is started by launchctl kickstart under the LaunchAgent, and the early-run gate — OFF sidecar landed, emit() refused naming the 16 missing keys, control printed REPRODUCTION GATE PASSED 790/1008 — is observed and approved by the operator before the run is left unattended (D-15, D-17, D-19)"
  artifacts:
    - path: "results/phase26_operational_note.md"
      provides: "Dated launch record with quoted command outputs; the D-19 entry lands here if the audit is cut"
      contains: "## 5. The launch record"
    - path: "artifacts/com.personacore.phase26.canary.plist"
      provides: "Installed copy under ~/Library/LaunchAgents (not tracked — the repo copy is)"
  key_links:
    - from: "results/phase26_operational_note.md"
      to: "tests/test_phase26_canary.py"
      via: "_NOTE_REQUIRED_BLOCKS heading pins through _prose.normalized"
      pattern: "_NOTE_REQUIRED_BLOCKS"
    - from: "launchctl"
      to: "scripts/phase26_canary.py"
      via: "kickstart -k gui/$UID/com.personacore.phase26.canary → caffeinate -dims → .venv python driver"
      pattern: "com.personacore.phase26.canary"
---

<objective>
Put the machine into a state a ≈ 25 h MPS run survives, record that state, launch the audit under
the LaunchAgent, and hold at a human checkpoint until the early-run gate (OFF sidecar → consumer
refusal on a real record → control's 790/1008 reproduction) has been observed.

Purpose: D-16 (venue), D-15 (the auditor is checked against the artifact before it is trusted on
any noised point), D-19 (the named-limitation home exists before the clock can cut anything).
Output: `results/phase26_operational_note.md` §1-§6, the agent running, the operator's approval.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-empirical-privacy-audit-canary/26-CONTEXT.md
@.planning/phases/26-empirical-privacy-audit-canary/26-RESEARCH.md
@.planning/phases/26-empirical-privacy-audit-canary/26-03-SUMMARY.md
@results/phase25_operational_note.md

<interfaces>
From scripts/phase25_venue.py: launch_banner() (:540) ; prove_only_our_caffeinate (:311) ; launch_identity (:593)
From scripts/phase25_run.py: HEARTBEAT_PATH = data/phase25_heartbeat.jsonl (:286)
From scripts/phase26_canary.py: RECORD, OPERATIONAL_NOTE, off_sidecar_path(), sidecar_path(key), emit()
From scripts/phase26_prereg.py: CONTROL_KEY, COMMITTED, SIDECARS_AT_COMMIT
Installed agents (~/Library/LaunchAgents): com.personacore.phase25.{n64floor,recall,rehearsal,sweep,watch}.plist — none loaded at research time
Budget (RESEARCH Pitfall 4 / Open Question 1): OFF ≈ 61 min, control ON ≈ 61 min, each noised point ≈ 93 min × 15 ≈ 23 h → ≈ 25 h total
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-launch record, note skeleton + heading test, plist install, wiring proof on this host, kickstart</name>
  <files>results/phase26_operational_note.md, tests/test_phase26_canary.py</files>
  <read_first>
    - results/phase25_operational_note.md (§1-§4 shape :13-360, §12 launch record :884-905 — quoted command outputs, numbered `##` blocks)
    - tests/test_phase25_launch.py lines 460-530 (`_REQUIRED_BLOCKS`, module-scoped `note` fixture, parametrized heading test)
    - artifacts/com.personacore.phase26.canary.plist
    - scripts/phase25_venue.py lines 300-330 (`prove_only_our_caffeinate`) and 585-620 (`launch_identity`)
    - tests/test_phase26_canary.py (to append the note test)
    - .planning/phases/26-empirical-privacy-audit-canary/26-RESEARCH.md §Pitfall 4, 8, 9 and §Code Example 5
  </read_first>
  <action>
ORDER MATTERS for the ancestry guard: `scripts/phase26_prereg.py` must not be touched in this plan or later; this task's commit adds the FIRST tracked `results/phase26_*` file and therefore must come after every prereg commit (it does, by wave order — verify with `git log --format=%H -- scripts/phase26_prereg.py` before committing and quote it in §1).

1. Create `results/phase26_operational_note.md` with a dated title (`# Phase 26 operational note — the canary audit: launch, run and close`) and these blocks, each a quoted command transcript, nothing authored as a number:
   - `## 1. The pre-registration state` — `git log --format='%h %ad %s' --date=short -- scripts/phase26_prereg.py`; `git ls-files 'results/phase26_*'` (empty BEFORE this commit — say so: this note is the first tracked Phase-26 result and lands strictly after every prereg commit); `SIDECARS_AT_COMMIT == 0` quoted from the module; `ls data/phase26_canary_*.json` (expected: none).
   - `## 2. The assertion owners before launch` — `pgrep -lf caffeinate` with owners named (Pitfall 9: the harness's own `caffeinate -i -t 300` and any stray; do NOT kill anything you do not own — record it), `pmset -g | grep -E 'sleep|powernap'` (expected `sleep 1 / disksleep 10 / powernap 1`; the LaunchAgent's `caffeinate -dims` is what carries the run, per D-16), `launchctl list | grep personacore` (expected empty before load).
   - `## 3. The budget as measured` — quote RESEARCH Pitfall 4's arithmetic: per-question 3.4–4.3 s on noised adapters from `results/phase25_recall.json` `scoring_seconds`, 1472 ON questions per noised point ≈ 93 min, × 15 ≈ 23 h, + OFF ≈ 61 min + control ≈ 61 min ⇒ ≈ 25 h; state plainly that the CONTEXT's ≈ 11 h was derived from the control's 914 s and is superseded by this measured figure; D-08 (both tiers) is locked so nothing is cut at plan time.
   - `## 4. The wiring proof` — run and quote: `.venv/bin/pytest -q tests/test_phase26_canary.py::test_the_live_path_is_wired_end_to_end tests/test_phase26_canary.py::test_the_control_routes_its_in_taught_sum_through_prove_reproduction -x` (must PASS here on the host), `.venv/bin/python scripts/phase26_canary.py --dry-run | tail -18`, and the emit refusal with an empty sidecar set (from the 26-02 verify one-liner) — the consumer refused naming the note.
   - `## 5. The launch record — <date>` — filled at the end of this task (step 3).
   - `## 6. The early-run gate` — heading present with the word PENDING and the three items it will quote (OFF sidecar path + `base_sha256`; `emit()` refusal naming exactly the 16 missing point keys with the OFF sidecar present; the control's `REPRODUCTION GATE PASSED 790/1008` line from `logs/phase26_canary.out`). Filled in Task 3.
   - `## 7. Pending` — register: §6 pending; the close (or the D-19 entry) is plan 26-05's.
2. Append to `tests/test_phase26_canary.py`: `_NOTE_REQUIRED_BLOCKS = ("## 1. The pre-registration state", "## 2. The assertion owners before launch", "## 3. The budget as measured", "## 4. The wiring proof", "## 5. The launch record", "## 6. The early-run gate")` and `test_the_operational_note_carries_every_required_block` parametrized over it through `_prose.normalized` (module-scoped `note` fixture reading `canary.OPERATIONAL_NOTE`).
3. Install and launch (Claude does this via CLI — D-16): `cp artifacts/com.personacore.phase26.canary.plist ~/Library/LaunchAgents/`; `launchctl load ~/Library/LaunchAgents/com.personacore.phase26.canary.plist`; confirm the watcher `com.personacore.phase25.watch` is loaded (`launchctl list | grep personacore`; load it from `~/Library/LaunchAgents/com.personacore.phase25.watch.plist` if not — it is detect-never-act, T-26-04); rotate/clear `data/phase25_heartbeat.jsonl` stall history only if the Phase-25 close left it (record what was done); `launchctl kickstart -k gui/$UID/com.personacore.phase26.canary`; within 60 s quote in §5: `launchctl list | grep personacore`, the `launch_banner()` line from `logs/phase26_canary.out`, `pmset -g assertions | grep -i -E 'caffeinate|PreventUserIdleSystemSleep'` with pid → args via `ps -o pid,ppid,args -p <pids>` (memory: the `caffeinate -dims` wrapper is the driver's CHILD — find it by `ppid == driver pid`), the first heartbeat line `tail -1 data/phase25_heartbeat.jsonl`, and `git rev-parse HEAD` (the SHA the run's `instrument_git_sha` will carry — commit steps 1-2 BEFORE kickstart so the SHA in the sidecars is the committed one).
4. Commit `results/phase26_operational_note.md` + the test append with a message naming it the first tracked Phase-26 result; then run `.venv/bin/pytest -q tests/test_phase26_prereg.py` — the ancestry guard now has one tracked artifact and must PASS (`checked == len(prereg_commits) * 1`).
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/pytest -q tests/test_phase26_prereg.py tests/test_phase26_canary.py -k "prereg_is_frozen or byte_identical or operational_note" -x && launchctl list | grep -q com.personacore.phase26.canary && test -f logs/phase26_canary.out && grep -c "phase26_canary\|launch" logs/phase26_canary.out | grep -v '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - `git log --diff-filter=A --format=%H -- results/phase26_operational_note.md` is one commit and `git merge-base --is-ancestor <every prereg commit> <that commit>` exits 0 for each; `test_phase26_prereg_is_frozen_before_every_phase26_result` passes with `checked == len(prereg_commits)`
    - `results/phase26_operational_note.md` carries all six `_NOTE_REQUIRED_BLOCKS` headings; §4 quotes a PASSED run of `test_the_live_path_is_wired_end_to_end` on this host; §3 states `≈ 25 h` and names the CONTEXT's 11 h as superseded
    - `launchctl list | grep com.personacore.phase26.canary` shows the agent with a live PID; `logs/phase26_canary.out` begins with the `launch_banner()` line; `pmset -g assertions` shows a `caffeinate` whose parent is the driver PID
    - The `git rev-parse HEAD` quoted in §5 equals the commit that contains the note (steps 1-2 committed before kickstart)
    - `scripts/phase26_prereg.py` has no commit after the note's first-add (`git log --format=%H -- scripts/phase26_prereg.py` unchanged from §1)
  </acceptance_criteria>
  <done>Note committed as the first tracked Phase-26 result, agent kickstarted, §1-§5 filled with quoted outputs, §6 PENDING.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Early-run gate — approve leaving the ≈ 25 h run unattended</name>
  <what-built>The audit agent `com.personacore.phase26.canary` is running under `caffeinate -dims` with `PERSONACORE_SWEEP_ACTIVE=1`. Its first two legs are the adapter-off arm (≈ 61 min, one sidecar `data/phase26_canary_off.json`) and the σ=0 control (≈ 61 min) whose IN-taught sum must reproduce 790/1008 through `phase25_prereg.prove_reproduction` — the auditor is checked against the artifact before any noised point is trusted (D-15). The remaining 15 points take ≈ 23 h.</what-built>
  <how-to-verify>
    Return roughly 2 h 15 min after kickstart (or when both files below exist). Run from the repo root:
    1. `ls -la data/phase26_canary_off.json data/phase26_canary_dp_n8_sigma0p000000.json` — both exist.
    2. `grep -n "REPRODUCTION GATE PASSED" logs/phase26_canary.out` — exactly one line, `790/1008`. If instead the log shows a `SystemExit` from `prove_reproduction`, the halt IS the designed outcome (D-15): reply "halted" and plan 26-05 records it under D-19; do not re-run anything.
    3. `PERSONACORE_SWEEP_ACTIVE=1 .venv/bin/python -c "import sys; sys.path.insert(0,'scripts'); sys.path.insert(0,'src'); import phase26_canary as c; c.emit(c.SIDECAR_DIR/'_never_written.json')"` — expect `SystemExit` naming the 15 missing noised keys and `results/phase26_operational_note.md`; the consumer has now been fed the REAL OFF and control records and refused for the right reason. Confirm `data/_never_written.json` does not exist.
    4. `tail -2 data/phase25_heartbeat.jsonl` — `point` is `dp_n8_sigma0p500000` (or later), `stage` is `score`.
    5. `pmset -g assertions | grep caffeinate` — the agent's `caffeinate -dims` still holds the assertions.
    6. `PERSONACORE_SWEEP_ACTIVE=1 .venv/bin/pytest -q tests/test_phase26_canary.py::test_the_control_reproduced_the_published_reading` — PASSES now (it reads the control sidecar; it does not draw).
  </how-to-verify>
  <resume-signal>Type "approved" to leave the run unattended (Task 3 records §6), "halted" if the reproduction gate refused, or describe what differs.</resume-signal>
  <files>results/phase26_operational_note.md</files>
  <action>Human gate, not automation: the operator runs the six commands in how-to-verify on the MPS host ≈ 2 h after kickstart and decides whether the ≈ 25 h run continues unattended. Claude prepares nothing further here; the outputs are quoted into note §6 by Task 3. Threat refs: T-26-03 (the consumer is fed real producer records before the run is trusted), T-26-05 (nothing is committed at this gate).</action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && test -f data/phase26_canary_off.json && test -f data/phase26_canary_dp_n8_sigma0p000000.json && grep -c "REPRODUCTION GATE PASSED 790/1008" logs/phase26_canary.out | grep -x 1</automated>
  </verify>
  <acceptance_criteria>
    - The operator replied "approved" or "halted" (recorded in the SUMMARY with the timestamp)
    - Approved path: both sidecars exist and the log carries exactly one `REPRODUCTION GATE PASSED 790/1008` line; `data/_never_written.json` does not exist after the emit refusal
  </acceptance_criteria>
  <done>Operator decision recorded; Task 3 has the six quoted outputs to file under §6.</done>
</task>

<task type="auto">
  <name>Task 3: Record §6 — the early-run gate, quoted</name>
  <files>results/phase26_operational_note.md</files>
  <read_first>
    - results/phase26_operational_note.md (Task 1 state, §6 PENDING)
    - logs/phase26_canary.out (the OFF and control legs)
    - data/phase26_canary_off.json and data/phase26_canary_dp_n8_sigma0p000000.json (read-only; quote `base_sha256`, `host_adapter_sha256`, `in_taught.k/n`, `reproduction_gate`, `scoring_seconds`, `device`, `torch_version`)
  </read_first>
  <action>
Replace §6's PENDING with the quoted outputs of the checkpoint's six commands (OFF sidecar path + `base_sha256` == `550bb8b08f65cbb8442fa2c44b1e905aeb51ae7afc39b012c35b957459e1f056`; the control's `REPRODUCTION GATE PASSED 790/1008` log line and its `reproduction_gate` block; the `emit()` refusal text naming the 15 missing keys and the note; the heartbeat tail; the assertion owner; the `test_the_control_reproduced_the_published_reading` PASS line), plus the measured OFF and control `scoring_seconds` against §3's estimate (restate the remaining-time projection from the measured control figure — a number computed from quoted figures, with the arithmetic shown). Move §6 out of §7's pending register; §7 now names only the close. If the operator answered "halted": write §6 as the halt transcript and add `## 8. D-19 named limitation — <date>: the reproduction gate refused the auditor` stating that no artifact will be assembled and that Phase 28 must say "audit not executed" beside every ε, citing `phase26_prereg.PUBLICATION_OBLIGATION_CONTINUATION`'s last entry; then `launchctl bootout gui/$UID/com.personacore.phase26.canary` and quote it. Commit the note (the note is the ONLY file in this commit; sidecars are gitignored and the driver never commits — T-26-05).
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && PERSONACORE_SWEEP_ACTIVE=1 .venv/bin/pytest -q tests/test_phase26_canary.py tests/test_phase26_prereg.py -x && ! grep -n "^## 6.*PENDING\|## 6\..*\n.*PENDING" results/phase26_operational_note.md && grep -c "REPRODUCTION GATE PASSED 790/1008\|D-19 named limitation" results/phase26_operational_note.md | grep -v '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - §6 contains the literal `REPRODUCTION GATE PASSED 790/1008` (approved path) OR a `## 8. D-19 named limitation` block (halted path); never both PENDING and filled
    - §6 quotes the `emit()` refusal containing `phase26_operational_note.md` and the 15 noised keys
    - `git show --stat HEAD` lists only `results/phase26_operational_note.md`
    - `PERSONACORE_SWEEP_ACTIVE=1 .venv/bin/pytest -q tests/test_phase26_canary.py tests/test_phase26_prereg.py` green while the agent runs (MPS-needing legs skipped under the env var; the control read test passes)
  </acceptance_criteria>
  <done>§6 recorded from quoted outputs and committed; the run continues unattended (or the halt is recorded and the agent booted out).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator console → launchd | Plist copied to ~/Library/LaunchAgents and kickstarted by hand; absolute paths, minimal PATH |
| running agent → repo | Writes only gitignored `data/` sidecars and `logs/`; reads `checkpoints/` and the frontier |
| peer Claude/Cursor sessions → working tree | A `git checkout` by another session moves nothing for this driver (it never commits), but would change `git_sha()` in later sidecars — recorded, not prevented |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-02 | DoS | run killed by sleep/jetsam | mitigate | `caffeinate -dims` under the LaunchAgent; sidecar-per-point resume by hash; the watcher detects and never acts |
| T-26-03 | Repudiation | note authored after the fact | mitigate | Every block is a quoted command output; the note's first-add is ancestry-guarded after the prereg; §5's SHA is the commit that contains the note |
| T-26-04 | Tampering | supervisor re-entering | mitigate | `KeepAlive false`; kickstart is the only start; `launchctl list` quoted before/after |
| T-26-05 | Elevation | driver git surface | mitigate | The agent's environment has no git write path in the driver; only the operator commits, and only the note in this plan |
| T-26-10 | Information disclosure | logs/ | accept | Log lines carry prompts of published synthetic facts only; `logs/` gitignored |
| T-26-SC | Tampering | package installs | accept | Nothing installed |
</threat_model>

<verification>
- The ancestry guard passes with one tracked artifact
- The agent is running with its own caffeinate; §1-§6 filled from quoted outputs
- The consumer (`emit`) refused on real producer records for the right reason before the run was left unattended
</verification>

<success_criteria>
- The run is under the LaunchAgent with the measured ≈ 25 h budget stated, the auditor reproduced 790/1008 at the control, and the operator approved leaving it — or the halt is a dated D-19 entry
</success_criteria>

<output>
Create `.planning/phases/26-empirical-privacy-audit-canary/26-04-SUMMARY.md` when done
</output>
