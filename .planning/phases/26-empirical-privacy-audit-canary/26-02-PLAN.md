---
phase: 26-empirical-privacy-audit-canary
plan: 02
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - scripts/phase26_canary.py
  - artifacts/com.personacore.phase26.canary.plist
autonomous: true
requirements: [CANARY-01, CANARY-02]
must_haves:
  truths:
    - "One instrument scores IN (8 LOCKED_FACTS) and OUT (56 FILLER_FACTS rendered with forms=FILLER_SLOT_FORMS), adapter-on vs adapter-off, with contains_value / complete_question / score_question IMPORTED and the same enumerate index per list as score_items (D-06, D-08, D-15)"
    - "The adapter-off arm is measured ONCE on the sha256-pinned base and written to its own sidecar; every point's sidecar is pinned to the frontier's adapter_sha256 and reused only when the hash matches (D-16, D-17)"
    - "At the control the IN-taught sum is routed through phase25_prereg.prove_reproduction before the sidecar is written (D-15)"
    - "emit() refuses unless the OFF sidecar and all 16 point sidecars exist, computes exclusions, ceiling and power gate BEFORE any verdict, writes results/phase26_canary.json write-once, and names results/phase26_operational_note.md when it refuses (D-13, D-18, D-19)"
    - "The driver builds no git argv at all; the plist mirrors the recall agent (caffeinate -dims, KeepAlive false, PERSONACORE_SWEEP_ACTIVE=1)"
  artifacts:
    - path: "scripts/phase26_canary.py"
      provides: "Driver: _items, _score_list, score_off_once, score_point, emit, build_parser, main"
      exports: ["RECORD", "SIDECAR_DIR", "INSTRUMENT", "sidecar_path", "off_sidecar_path", "frontier", "_items", "_score_list", "score_off_once", "score_point", "emit", "build_parser", "main"]
      min_lines: 250
    - path: "artifacts/com.personacore.phase26.canary.plist"
      provides: "LaunchAgent: caffeinate -dims → .venv python scripts/phase26_canary.py --heartbeat data/phase25_heartbeat.jsonl"
      contains: "com.personacore.phase26.canary"
  key_links:
    - from: "scripts/phase26_canary.py"
      to: "scripts/phase26_prereg.py"
      via: "audited_point_keys / power_threshold / epsilon_lower / auditor_ceiling / point_verdict / power_gate"
      pattern: "phase26_prereg\\.(audited_point_keys|power_gate|point_verdict|auditor_ceiling)"
    - from: "scripts/phase26_canary.py"
      to: "scripts/phase14_recall.py"
      via: "pr.load_adapted_model / pr.complete_question / pr.score_question (lazy import inside the scoring functions)"
      pattern: "pr\\.complete_question\\(.*index=index"
    - from: "scripts/phase26_canary.py"
      to: "scripts/phase25_run.py"
      via: "atomic_write_json / beat / start_heartbeat / device CALLED"
      pattern: "phase25_run\\.(atomic_write_json|start_heartbeat|beat|device)"
    - from: "scripts/phase26_canary.py"
      to: "scripts/phase25_prereg.py"
      via: "prove_reproduction at the control; point_record_path charset on every key"
      pattern: "phase25_prereg\\.(prove_reproduction|point_record_path)"
---

<objective>
Build the audit driver `scripts/phase26_canary.py` line-for-line on `scripts/phase25_recall.py`
(sidecar per point under `data/`, sha-pinned reuse-or-refuse, heartbeat, atomic writes, `--dry-run`,
`--emit`, `--force`), swapping `score_arm` for the per-fact two-call loop over IN and OUT, adding the
once-only OFF arm, and the D-19/D-13/D-03 assembly in `emit`. Ship the LaunchAgent plist beside it.

Purpose: CANARY-01 — the empirical ε_lower built additively on the existing scorer, Wilson bound and
draw budget, with questions (never draws) as a unit and the fact as the deciding unit.
Output: `scripts/phase26_canary.py`, `artifacts/com.personacore.phase26.canary.plist`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-empirical-privacy-audit-canary/26-CONTEXT.md
@.planning/phases/26-empirical-privacy-audit-canary/26-RESEARCH.md
@.planning/phases/26-empirical-privacy-audit-canary/26-PATTERNS.md
@.planning/phases/26-empirical-privacy-audit-canary/26-01-SUMMARY.md

<interfaces>
<!-- Verified at HEAD efb4f0f (RESEARCH §Standard Stack, PATTERNS §driver). Use directly. -->

From scripts/phase26_prereg.py (plan 26-01):
  CONTROL_KEY, DECIDING_TIER ("taught"), EXCLUSION_SCOPE ("either"), POWER_SENTENCE, JOINT_COVERAGE, VERDICTS
  audited_point_keys(frontier) -> 16 keys control first ; noised_point_keys(frontier) -> 15
  power_threshold(frontier) -> float ; power_gate(control_epsilon_lower, threshold) -> dict
  epsilon_lower(members, n_in, nonmembers, n_out) -> dict ; auditor_ceiling(n_in, n_out) -> float
  point_verdict(reading, epsilon_upper, *, power, auditor_ceiling) -> {"verdict", "reasons"}
  RULE, COMMITTED, WAIVER_CONTINUATION, PUBLICATION_OBLIGATION_CONTINUATION

From scripts/teach_persona.py (IMPORT-ONLY; digest-pinned — NEVER modified):
  calibration_items(facts, family_ids)   (:2381)  render_family WITHOUT forms= at :2396
  score_items(...)                       (:2403)  loop: `for index, (family_id, fact, question) in enumerate(items):
                                                  drawn = pr.complete_question(model, tok, question, device, forbid, index=index)
                                                  k, n = pr.score_question(drawn["completions"], fact.value)`
  from personacore.lora import adapter_disabled   (:76)

From scripts/phase14_recall.py:
  CONVBASE_SLIM = checkpoints/convbase_slim.pt   (:81)   sha256 550bb8b08f65cbb8442fa2c44b1e905aeb51ae7afc39b012c35b957459e1f056
  N_SEEDED_SAMPLES = 8 (9 draws)   (:152)
  def contains_value(completion, value) -> bool   (:300)
  def score_question(completions, value) -> (k, n)   (:315)
  def load_adapted_model(device, adapter_path=None) -> (model, cfg, tok, forbid, artifact)   (:712)  weights_only=True inside; SystemExit if adapter missing
  def complete_question(model, tok, question, device, forbid, *, index) -> {"question","prompt_ids","completions","stopped"}   (:901)

From scripts/phase14_factset.py:
  LOCKED_FACTS (8 Fact(id, slot, value, tier))   (:390)
  TAUGHT_FAMILY_IDS = frozenset({"F1","F2","F4","F5","F6"}) ; HELDOUT_FAMILY_IDS = frozenset({"F3","F7","F8"})   (:825-826)  iterate sorted(...)
  def render_family(family_id, fact, *, second_person=False, forms=None) -> [(question, answer), ...]   (:833)

From scripts/phase21_filler.py (imports phase14_factset at module scope -> LAZY in the driver):
  FILLER_FACTS (56)   (:175) ; FILLER_SLOT_FORMS   (:61)

From src/personacore/lora/inject.py:
  @contextmanager def adapter_disabled(model)   (:157)  no-op on a model without LoRALinear modules

From scripts/phase25_run.py:
  def atomic_write_json(path, blob) -> path   (:118)
  HEARTBEAT_PATH = _ROOT/"data"/"phase25_heartbeat.jsonl"   (:286)
  def beat(heartbeat_path, *, point, stage, shape, draw_index) -> dict   (:298)
  def start_heartbeat(heartbeat_path, state, *, seconds=None) -> (stop_event, thread)   (:350)
  def device() -> str   (:407)
  READ_ONLY_GIT_ACTIONS = ("ls-files", "show", "rev-parse", "status")   (:753)

From scripts/phase25_recall.py (the whole-file template, 313 lines):
  _prove / _utc / _sha256 / _rel / sidecar_path / point_record / score_point / emit / build_parser / main

From scripts/phase25_epsilon.py:
  def report_epsilon(*, point_epsilon, curve_total_epsilon, selection_accounted) -> str   (:297)  accepts point_epsilon=None (the frontier's epsilon_report.rendered carries the control)

From scripts/phase25_venue.py:  def launch_banner() -> str   (:540)
From scripts/phase25_gate05.py: FILLER_EXPOSURE_OMITTED   (:280)
From scripts/phase18_extraction.py: CLUSTER_DENOMINATOR_RATIONALE   (:2010)  (import-only; module is ancestry-guarded)
From src/personacore/provenance.py: def git_sha() -> str

Verified counts (RESEARCH Code Example 1): IN taught 112 / held-out 72 ; OUT taught 784 / held-out 504.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Driver scoring half — _items, _score_list, score_off_once, score_point (copy phase25_recall.py, swap the scorer)</name>
  <files>scripts/phase26_canary.py</files>
  <read_first>
    - scripts/phase25_recall.py (WHOLE file — the template: imports :33-60, helpers :71-91, score_point :114-185, heartbeat try/finally :156-163, blob provenance :165-181)
    - scripts/teach_persona.py lines 2381-2400 (`calibration_items`) and 2403-2471 (`score_items`, `score_arm`'s ON/OFF passes) — READ ONLY; this file is digest-pinned by results/phase24_token_budget.json and asserted by tests/test_phase24_record.py:289-332
    - scripts/phase14_recall.py lines 80-90 (`CONVBASE_SLIM`), 221-240 (`question_seed`, `SEED + index`), 300-325 (`contains_value`, `score_question`), 698-731 (`load_adapted_model`), 895-927 (`complete_question`)
    - scripts/phase14_factset.py lines 825-874 (`TAUGHT_FAMILY_IDS`, `HELDOUT_FAMILY_IDS`, `render_family`)
    - scripts/phase21_filler.py lines 55-75 (`FILLER_SLOT_FORMS`), 170-180 (`FILLER_FACTS`), 416-439 (`render_filler_episodes` — `sorted(family_ids)` is load-bearing)
    - scripts/phase25_run.py lines 105-130 (`atomic_write_json`), 286-300 (`HEARTBEAT_PATH`, `beat`), 350-370 (`start_heartbeat`), 407-430 (`device`)
    - scripts/phase25_prereg.py lines 176-215 (`prove_reproduction`), 276-296 (`point_record_path`)
    - scripts/phase26_prereg.py (plan 26-01 output — every constant the driver consumes)
    - .planning/phases/26-empirical-privacy-audit-canary/26-RESEARCH.md §Code Examples 1-3, §Pitfalls 1, 2, 8
  </read_first>
  <action>
Create `scripts/phase26_canary.py` by copying `scripts/phase25_recall.py`'s frame and changing only what follows. Module scope imports ONLY `argparse, datetime, hashlib, json, pathlib, platform, sys, time, functools`, the `sys.path` inserts for `scripts/` and `src/`, and the CPU-safe siblings `phase25_prereg, phase25_record, phase25_run, phase25_venue, phase26_prereg` plus `from personacore.provenance import git_sha`. `phase14_factset`, `phase14_recall`, `phase21_filler`, `teach_persona`, `personacore.lora` and `torch` are imported ONLY inside `_items`, `_score_list`, `score_off_once`, `score_point` (comment each `# LAZY — torch-touching`); the AST test in plan 26-03 asserts this and the `--dry-run` path must never reach them.

Constants: `RECORD = _ROOT/"results"/"phase26_canary.json"`; `OPERATIONAL_NOTE = _ROOT/"results"/"phase26_operational_note.md"`; `SIDECAR_DIR = _ROOT/"data"` (gitignored — `.gitignore` has `data/`); `INSTRUMENT = "phase14_recall.complete_question + phase14_recall.score_question (teach_persona.score_items' two calls, replayed per fact with the same enumerate index)"`; `PREREG_MODULE = _ROOT/"scripts"/"phase26_prereg.py"`. `_prove` prefix `[phase26_canary]`.

Paths (T-26-06): `sidecar_path(point_key)` FIRST calls `phase25_prereg.point_record_path(point_key)` (discarding the return — its charset `_prove` is the refusal) then returns `SIDECAR_DIR / f"phase26_canary_{point_key}.json"`; `off_sidecar_path()` returns `SIDECAR_DIR / "phase26_canary_off.json"`. `frontier()` = `functools.lru_cache`d `json.loads(phase25_record.FRONTIER_RECORD.read_text())` (22 MB — read once per process). `point_record(key)` = `frontier()["points"][key]` after `_prove(key in phase26_prereg.audited_point_keys(frontier()))`.

`_items(facts, family_ids, forms=None)` — RESEARCH Code Example 1 verbatim in behaviour: `for fact in facts: for family_id in sorted(family_ids): for question, _answer in fs.render_family(family_id, fact, forms=forms): skip when pr.contains_value(question, fact.value); append (family_id, fact, question)`. On `LOCKED_FACTS` with `forms=None` this must equal `teach_persona.calibration_items(...)` as an exact list (that equality is what makes the seeds identical to the published run).

`_lists()` returns the four item lists as a dict in this fixed key order: `in_taught = _items(fs.LOCKED_FACTS, fs.TAUGHT_FAMILY_IDS)`, `in_heldout = _items(fs.LOCKED_FACTS, fs.HELDOUT_FAMILY_IDS)`, `out_taught = _items(pf.FILLER_FACTS, fs.TAUGHT_FAMILY_IDS, forms=pf.FILLER_SLOT_FORMS)`, `out_heldout = _items(pf.FILLER_FACTS, fs.HELDOUT_FAMILY_IDS, forms=pf.FILLER_SLOT_FORMS)`; `_prove` the lengths are exactly 112, 72, 784, 504.

`_score_list(model, tok, device, forbid, items, *, label, heartbeat_state=None)` — RESEARCH Code Example 2: ONE `enumerate(items)` from 0 per list (Pitfall 1 — never concatenate lists); the two calls `pr.complete_question(model, tok, question, device, forbid, index=index)` then `pr.score_question(drawn["completions"], fact.value)`; keep `per_question` rows `{"fact", "family", "index", "k", "n", "answered": k > 0}` and `per_fact[fact.id] = {"answered_questions", "n_questions", "k", "n", "member": answered_questions >= 1}` (D-14 existential); return `{"per_question", "per_fact", "k", "n", "questions": len(items), "questions_answered": count of k > 0, "draws_per_question": pr.N_SEEDED_SAMPLES + 1, "unit_rationale": phase18_extraction.CLUSTER_DENOMINATOR_RATIONALE}` (import `phase18_extraction` lazily too — it is heavy). When `heartbeat_state` is given, update `heartbeat_state["shape"] = label` and `heartbeat_state["draw_index"] = index` each iteration so the already-running `start_heartbeat` thread reports progress.

`score_off_once(*, dry_run=False, heartbeat_path=None)` (D-07 probe, D-17 once): host adapter = the control's `adapter_path` (hash-checked against its `adapter_sha256` exactly as `phase25_recall.py:120-128`); if `off_sidecar_path()` exists, `_prove(blob["base_sha256"] == _sha256(pr.CONVBASE_SLIM))` → return `("reused", blob)` else `SystemExit("... REFUSED, not reused")`; `dry_run` → print `DRY RUN — would score the adapter-off arm on <host>` and return `("dry_run", None)`; live: `pr.load_adapted_model(device, adapter_path=host)`, then `with adapter_disabled(model):` score all four lists via `_score_list`; blob = `{"arm": "off", "base_path": _rel(pr.CONVBASE_SLIM), "base_sha256", "host_point_key": CONTROL_KEY, "host_adapter_sha256", "in_taught", "in_heldout", "out_taught", "out_heldout", "scoring_seconds", "instrument": INSTRUMENT, "instrument_git_sha": git_sha(), "device", "torch_version": torch.__version__, "platform": platform.platform(), "utc"}`; `phase25_run.atomic_write_json(off_sidecar_path(), blob)` (T-26-02); heartbeat `beat`/`start_heartbeat`/`try-finally stop.set(); thread.join()` exactly as the template with `point="off"`.

`score_point(point_key, *, dry_run=False, heartbeat_path=None)` — the template's frame with: `record = point_record(point_key)`; adapter exists + `_sha256(adapter) == record["adapter_sha256"]` (T-26-07); sidecar reuse only on hash equality else `SystemExit("... REFUSED, not reused")`; `dry_run` return before any lazy import; live: `pr.load_adapted_model(device, adapter_path=adapter)`, score the four lists adapter-ON; at `point_key == phase26_prereg.CONTROL_KEY` call `phase25_prereg.prove_reproduction(int(in_taught["k"]), int(in_taught["n"]))` BEFORE the sidecar is written and print `REPRODUCTION GATE PASSED 790/1008` (D-15 — the halt is the designed outcome); blob = `{"point_key", "arm", "sigma": record["sigma"], "adapter_path", "adapter_sha256", "epsilon_upper": record["epsilon"], "delta": record["delta"], "in_taught", "in_heldout", "out_taught", "out_heldout", "reproduction_gate": {"passed": True, "expected": [790, 1008], "observed": [k, n]} at the control only, "scoring_seconds", "instrument", "instrument_git_sha", "device", "torch_version", "platform", "utc"}`; `atomic_write_json`; print `taught IN k/n, OUT k/n in Xs`.

Do NOT compute any ε_lower, verdict or exclusion in this task — no verdict is read before all 16 sidecars exist (D-17/D-19; `emit` in Task 2 is the only reader). Do NOT call `torch.load` anywhere (T-26-01: `load_adapted_model` is the only checkpoint-deserialisation path, `weights_only=True` inside). No `["git", ...]` argv anywhere in the file (T-26-05).
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/python -c "
import ast, pathlib, sys
src = pathlib.Path('scripts/phase26_canary.py').read_text()
tree = ast.parse(src)
top = {a.name.split('.')[0] for n in tree.body if isinstance(n, ast.Import) for a in n.names} | {n.module.split('.')[0] for n in tree.body if isinstance(n, ast.ImportFrom) and n.module}
assert not top & {'torch','teach_persona','phase14_factset','phase14_recall','phase21_filler','phase18_extraction'}, top
assert not any(isinstance(n, ast.Attribute) and n.attr=='load' and isinstance(n.value, ast.Name) and n.value.id=='torch' for n in ast.walk(tree))
sys.path.insert(0,'scripts'); sys.path.insert(0,'src')
import phase26_canary as c
assert 'torch' not in sys.modules
import phase14_factset as fs, teach_persona as tp, phase21_filler as pf
assert c._items(fs.LOCKED_FACTS, fs.TAUGHT_FAMILY_IDS) == tp.calibration_items(fs.LOCKED_FACTS, fs.TAUGHT_FAMILY_IDS)
assert c._items(fs.LOCKED_FACTS, fs.HELDOUT_FAMILY_IDS) == tp.calibration_items(fs.LOCKED_FACTS, fs.HELDOUT_FAMILY_IDS)
assert len(c._items(pf.FILLER_FACTS, fs.TAUGHT_FAMILY_IDS, forms=pf.FILLER_SLOT_FORMS)) == 784
assert len(c._items(pf.FILLER_FACTS, fs.HELDOUT_FAMILY_IDS, forms=pf.FILLER_SLOT_FORMS)) == 504
print('OK')" && .venv/bin/ruff check scripts/phase26_canary.py</automated>
  </verify>
  <acceptance_criteria>
    - AST: no top-level `import`/`from` of `torch`, `teach_persona`, `phase14_factset`, `phase14_recall`, `phase21_filler`, `phase18_extraction`; no `torch.load` attribute call anywhere in the file; no list/tuple literal whose first element is the string `"git"`
    - `import phase26_canary` leaves `"torch" not in sys.modules`
    - `_items(LOCKED_FACTS, TAUGHT_FAMILY_IDS) == teach_persona.calibration_items(LOCKED_FACTS, TAUGHT_FAMILY_IDS)` (exact list equality) and the same for `HELDOUT_FAMILY_IDS`; OUT lists have lengths 784 and 504
    - `sidecar_path("../x")` raises `SystemExit` (charset refusal via `phase25_prereg.point_record_path`); `sidecar_path("dp_n8_sigma0p500000") == _ROOT/"data"/"phase26_canary_dp_n8_sigma0p500000.json"`; `off_sidecar_path() == _ROOT/"data"/"phase26_canary_off.json"`
    - `score_point("dp_n8_sigma0p500000", dry_run=True)` on this host returns `("dry_run", None)` and prints `DRY RUN`; `score_off_once(dry_run=True)` returns `("dry_run", None)`
    - The source contains exactly one call site of `phase25_prereg.prove_reproduction`, inside `score_point`, guarded by `point_key == phase26_prereg.CONTROL_KEY`, and it precedes the `atomic_write_json` call in that function (AST line order)
    - `_score_list` contains exactly one `for` loop over `enumerate(items)` and passes `index=index` to `pr.complete_question`
    - `git diff --stat -- scripts/teach_persona.py scripts/phase25_prereg.py scripts/phase18_extraction.py scripts/phase21_filler.py` is empty
  </acceptance_criteria>
  <done>Scoring half committed; the verify one-liner prints OK; ruff clean.</done>
</task>

<task type="auto">
  <name>Task 2: Driver assembly half — emit, build_parser, main; the LaunchAgent plist</name>
  <files>scripts/phase26_canary.py, artifacts/com.personacore.phase26.canary.plist</files>
  <read_first>
    - scripts/phase26_canary.py (Task 1 state)
    - scripts/phase25_recall.py lines 213-313 (`emit`'s write-once refusal as the FIRST statement, set-equality, provenance block, `build_parser`, `main` printing `launch_banner()` first)
    - scripts/phase26_prereg.py (`audited_point_keys`, `noised_point_keys`, `power_threshold`, `power_gate`, `epsilon_lower`, `auditor_ceiling`, `point_verdict`, `DECIDING_TIER`, `EXCLUSION_SCOPE`, `JOINT_COVERAGE`, `RULE`, `COMMITTED`, `WAIVER_CONTINUATION`)
    - scripts/phase25_epsilon.py lines 238-260 and 297-345 (`dual_granularity_sentence`, `report_epsilon` — three keyword-only args)
    - scripts/phase25_gate05.py lines 280-292 (`FILLER_EXPOSURE_OMITTED`); scripts/phase18_extraction.py lines 2010-2020 (`CLUSTER_DENOMINATOR_RATIONALE`)
    - artifacts/com.personacore.phase25.recall.plist (whole, 58 lines — the plist to copy) and artifacts/com.personacore.phase25.watch.plist (the watcher that polls data/phase25_heartbeat.jsonl)
    - .planning/phases/26-empirical-privacy-audit-canary/26-PATTERNS.md §"results/phase26_canary.json" (required top-level fields) and §plist
  </read_first>
  <action>
Append to `scripts/phase26_canary.py`:

`_population(off_blob, tier_lists, facts_key)` helpers are optional; what `emit(out_path=RECORD, *, overwrite=False)` MUST do, in this order (D-19, D-13, D-03, D-05):
1. FIRST statement: `_prove(overwrite or not out_path.exists(), "... REFUSING to overwrite it ... Pass --force ...")` (write-once, T-26-03).
2. `fr = frontier()`; `pinned = phase26_prereg.audited_point_keys(fr)` (16); `off = off_sidecar_path()`; `_prove(off.exists(), "... the adapter-off sidecar is missing — the audit is not complete; record the dated D-19 named limitation in results/phase26_operational_note.md instead of assembling a partial artifact")`; load it; `_prove(off_blob["base_sha256"] == _sha256(pr.CONVBASE_SLIM))` — import `phase14_recall` lazily here ONLY for the constant (or read `off_blob["base_path"]` and hash that path; either way `--emit` must not load a model).
3. The 16-sidecar loop: for each key, `_prove(sidecar.exists(), f"{key}: no sidecar at ... — not scored; missing keys: [...]; a partial artifact is NEVER assembled (D-19) — add the dated named-limitation entry to results/phase26_operational_note.md")` — compute the full missing list first so the message names ALL missing keys; `_prove(blob["adapter_sha256"] == fr["points"][key]["adapter_sha256"])`; collect. Then set-equality against `pinned`.
4. Exclusions (D-07, `EXCLUSION_SCOPE == "either"`): `excluded_out = sorted(fid for tier in ("out_taught", "out_heldout") for fid, f in off_blob[tier]["per_fact"].items() if f["answered_questions"] > 0)`; same for IN over `("in_taught", "in_heldout")`; `n_out = 56 - len(excluded_out)`, `n_in = 8 - len(excluded_in)`; `_prove(n_out > 0 and n_in > 0)`; publish `exclusions = {"rule": EXCLUSION_SCOPE (+ rationale), "out": {"excluded": [...], "n": len, "of": 56}, "in": {"excluded": [...], "n": len, "of": 8}}`.
5. `ceiling = phase26_prereg.auditor_ceiling(n_in, n_out)` BEFORE any verdict; `reachable = [k for k in noised if fr["points"][k]["epsilon"] < ceiling]`; `reachable_claims = f"{len(reachable)}/15"`.
6. Per point (control included), from its sidecar, at the DECIDING_TIER (`in_taught` / `out_taught`) restricted to non-excluded facts: `members_answered = count of IN facts with per_fact[fid]["member"]`; `nonmembers_answered = count of OUT facts with member`; `fact_unit = phase26_prereg.epsilon_lower(members_answered, n_in, nonmembers_answered, n_out)`; `question_unit = epsilon_lower(in_taught["questions_answered"] over non-excluded facts, their n_questions sum (112 when nothing excluded), out_taught questions_answered, out n_questions (784))` — REPORTED, never decides (D-10); `heldout` readings at both units likewise under a `"heldout_reported"` key.
7. Power gate at the control: `power = phase26_prereg.power_gate(control_fact_unit["epsilon_lower"], phase26_prereg.power_threshold(fr))`; print `POWER_SENTENCE` and the numbers.
8. Verdicts for the 15 noised keys in order: `point_verdict(fact_unit, epsilon_upper, power=power, auditor_ceiling=ceiling)`; the control's entry carries `verdict: None` and `comparison: "VACUOUS BY CONSTRUCTION: the control publishes no epsilon (" + fr["points"][CONTROL_KEY]["epsilon_omitted_reason"] + "); its reading is the power gate's and feeds nothing else (D-01)"`.
9. Every point's `epsilon_sentence = phase25_epsilon.report_epsilon(point_epsilon=fr["points"][k]["epsilon"], curve_total_epsilon=fr["epsilon_report"]["curve_total_epsilon"], selection_accounted=False)` and `_prove(epsilon_sentence == fr["epsilon_report"]["rendered"][k])` (a free cross-check; the frontier already renders all 16 including the control with `None`).
10. Top level of the blob: `governs` (a dict quoting `phase25_gate05.FILLER_EXPOSURE_OMITTED` for why NLL cannot score filler and `phase18_extraction.CLUSTER_DENOMINATOR_RATIONALE` for both denominators — import both lazily inside `emit`), `instrument`, `audit_target_rule: phase26_prereg.RULE`, `resolved_target: phase26_prereg.resolve_audit_target(fr)`, `extension: phase26_prereg.EXTENSION`, `audited_point_keys` (16), `deciding_tier`, `membership_rule`, `unit`, `frontier_path: _rel(FRONTIER_RECORD)`, `frontier_sha256: _sha256(FRONTIER_RECORD)`, `frontier_bytes`, `base_sha256`, `off_sidecar: _rel(off)`, `prereg_module_sha256: _sha256(PREREG_MODULE)`, `prereg_committed: phase26_prereg.COMMITTED`, `waiver_continuation`, `exclusions`, `n_in`, `n_out`, `auditor_ceiling`, `reachable_claims`, `reachable_keys`, `power_gate`, `joint_coverage`, `z`, `delta`, `curve_total_epsilon` (context only, never the comparator — D-02), `emitted_utc`, `emitted_git_sha: git_sha()`, `points: {key: {adapter_sha256, adapter_path, sigma, epsilon_upper, fact_unit, question_unit, heldout_reported, verdict, epsilon_sentence, source: _rel(sidecar)}}`, `summary: {"BROKEN": n, "CONSISTENT": n, "INCONCLUSIVE": n}` over the 15.
11. `phase25_run.atomic_write_json(out_path, blob)`; print the summary counts, `reachable_claims`, and the power-gate line; return blob. The driver NEVER commits (T-26-05) — the operator does (plan 26-05).

`build_parser()`: `--points nargs="+"` (default: all 16 in order), `--heartbeat` (default `str(phase25_run.HEARTBEAT_PATH)` — the installed watcher's path), `--dry-run`, `--emit`, `--force`, `--skip-off` (store_true; for `--points` reruns when the OFF sidecar already exists — `score_off_once` reuses it anyway, so this flag only skips the hash-check print; omit the flag if it adds a branch you would not test — reuse-by-hash already makes it unnecessary). `main(argv=None)`: print `phase25_venue.launch_banner()` first, flush; `--emit` → `emit(overwrite=args.force)`; otherwise `score_off_once(dry_run=..., heartbeat_path=hb)` FIRST (D-17: once, before any point), then `for key in points: score_point(key, dry_run=args.dry_run, heartbeat_path=hb)`; default `points = phase26_prereg.audited_point_keys(frontier())`. Every kwarg `main` passes must be a parameter the callee declares (plan 26-03 traces this; memory: dry-run-tests-hide-an-unwired-driver).

Plist: copy `artifacts/com.personacore.phase25.recall.plist` to `artifacts/com.personacore.phase26.canary.plist` changing exactly: header comment (name Phase 26, ≈ 25 h of MPS budget, D-16), `Label` → `com.personacore.phase26.canary`, `ProgramArguments[3]` → `/Users/juliorcoelho/PersonaCore/scripts/phase26_canary.py`, `StandardOutPath`/`StandardErrorPath` → `/Users/juliorcoelho/PersonaCore/logs/phase26_canary.out` / `.err`. Keep `caffeinate -dims`, `--heartbeat /Users/juliorcoelho/PersonaCore/data/phase25_heartbeat.jsonl`, `KeepAlive false` (T-26-04, with its comment), `RunAtLoad false`, `WorkingDirectory`, `ProcessType Interactive`, `EnvironmentVariables` (`PERSONACORE_SWEEP_ACTIVE=1`, minimal `PATH`, `PYTHONUNBUFFERED=1`).
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/python scripts/phase26_canary.py --dry-run 2>&1 | tail -20 && .venv/bin/python -c "
import sys, json, pathlib, tempfile; sys.path.insert(0,'scripts'); sys.path.insert(0,'src')
import phase26_canary as c
c.SIDECAR_DIR = pathlib.Path(tempfile.mkdtemp())
try:
    c.emit(c.SIDECAR_DIR/'x.json'); raise AssertionError('emit did not refuse')
except SystemExit as e:
    assert 'phase26_operational_note.md' in str(e), e
assert not (c.SIDECAR_DIR/'x.json').exists(); print('EMIT REFUSAL OK')" && plutil -lint artifacts/com.personacore.phase26.canary.plist && .venv/bin/ruff check scripts/phase26_canary.py && .venv/bin/ruff format --check scripts/phase26_canary.py</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python scripts/phase26_canary.py --dry-run` exits 0, its first line is the `launch_banner()` line, then prints one `DRY RUN` line for the OFF arm and one per each of the 16 audited keys in `ORDERED_POINT_KEYS()` order, and `python -X importtime` / `sys.modules` shows torch never imported (checked structurally in plan 26-03)
    - `emit()` with an empty `SIDECAR_DIR` raises `SystemExit` whose message contains `phase26_operational_note.md` and writes no file; with the OFF sidecar present and 15 point sidecars the message lists the one missing key
    - AST: `emit`'s first statement is the overwrite `_prove`; `emit` contains a call to `phase26_prereg.auditor_ceiling` whose line number precedes every call to `phase26_prereg.point_verdict`; `emit` calls `phase25_epsilon.report_epsilon` with keywords `point_epsilon`, `curve_total_epsilon`, `selection_accounted`
    - AST: `main` calls `score_off_once` before the loop calling `score_point`; every keyword `main` passes to `score_off_once`, `score_point` and `emit` is a declared parameter of that function (`inspect.signature`)
    - `plutil -lint artifacts/com.personacore.phase26.canary.plist` exits 0; `plistlib.load` gives `Label == "com.personacore.phase26.canary"`, `KeepAlive is False`, `RunAtLoad is False`, `ProgramArguments[:3] == ["/usr/bin/caffeinate", "-dims", "/Users/juliorcoelho/PersonaCore/.venv/bin/python"]`, `ProgramArguments[3].endswith("scripts/phase26_canary.py")`, the `--heartbeat` value equals the recall plist's, `EnvironmentVariables["PERSONACORE_SWEEP_ACTIVE"] == "1"`, `StandardOutPath` differs from the recall plist's
    - No list/tuple literal beginning with the string `"git"` anywhere in `scripts/phase26_canary.py`
    - `ruff check` + `ruff format --check` clean on the driver; `.venv/bin/pytest -q tests/test_phase25_recall.py tests/test_phase25_close.py` still green
  </acceptance_criteria>
  <done>Driver complete with `--dry-run`, `--emit`, `--force`; plist lints; refusal watched live with no bytes landing; both files committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checkpoints/*.pt → driver | Untrusted pickle bytes cross into torch only via `phase14_recall.load_adapted_model` (`weights_only=True`) |
| data/ sidecars → emit | Gitignored run-time JSON re-read after kills/restarts; pinned by sha256 to the frontier's adapters |
| CLI argv (`--points`) → sidecar paths | Key-derived filesystem paths |
| launchd → driver | Unattended ≈ 25 h process on `main` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Tampering | checkpoint deserialisation | mitigate | Only `pr.load_adapted_model` loads weights (`weights_only=True` at `phase14_recall.py:712-731`); the driver contains no `torch.load` call (AST-asserted in 26-03) |
| T-26-02 | Tampering / DoS | sidecar + artifact writes on kill | mitigate | Every write goes through `phase25_run.atomic_write_json` (temp in dest dir, fsync, `os.replace`) |
| T-26-03 | Repudiation | post-hoc favourable reading | mitigate | `emit` computes exclusions → ceiling → power gate → verdicts only after OFF + 16 sidecars exist; write-once `_prove` first; `--force` is explicit; `prereg_module_sha256` travels in the artifact |
| T-26-04 | Tampering | supervisor re-entering a point | mitigate | `KeepAlive false`, `RunAtLoad false`; the installed `com.personacore.phase25.watch` agent is detect-never-act |
| T-26-05 | Elevation of privilege | driver git surface | mitigate | No `["git", ...]` argv literal in the driver; `git_sha()` is the only read; the operator commits the artifact (26-05) |
| T-26-06 | Tampering | key-derived paths | mitigate | `sidecar_path` routes every key through `phase25_prereg.point_record_path`'s charset refusal AND `point_record` requires membership in `audited_point_keys(frontier)` |
| T-26-07 | Spoofing | a sidecar from other weights reused | mitigate | Sidecar `adapter_sha256` must equal the frontier's; OFF sidecar `base_sha256` must equal the live base hash; mismatches are REFUSED, not reused |
| T-26-SC | Tampering | package installs | accept | Nothing is installed; no new dependency |
</threat_model>

<verification>
- `.venv/bin/python scripts/phase26_canary.py --dry-run` walks OFF + 16 and imports no torch
- `emit()` refuses an empty and a 15-of-16 sidecar set, naming the note and the missing keys, writing nothing
- `plutil -lint` passes; the plist mirrors the recall agent's wrapper and heartbeat path
- `tests/test_phase25_recall.py`, `tests/test_phase25_close.py`, `tests/test_phase26_prereg.py` green
</verification>

<success_criteria>
- One instrument for IN and OUT, seeds aligned per list, OFF arm once, sidecars pinned, live path complete (not only `--dry-run`)
- No verdict computed anywhere but in `emit`, and only after OFF + 16 sidecars exist
- Pinned modules untouched; driver git surface read-only
</success_criteria>

<output>
Create `.planning/phases/26-empirical-privacy-audit-canary/26-02-SUMMARY.md` when done
</output>
