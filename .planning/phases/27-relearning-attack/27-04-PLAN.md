---
phase: 27-relearning-attack
plan: 04
type: execute
wave: 3
depends_on: ["27-03"]
files_modified:
  - tests/test_phase27_relearn.py
autonomous: true
requirements: [RELRN-01, RELRN-02, RELRN-03, RELRN-04, RELRN-05]
must_haves:
  truths:
    - "One CPU end-to-end run drives relearn.main() through calibrate → curve → gate → structural-proof on a forged ADMITTED record in a tmp tree: phase25_run._DEVICE pinned to 'cpu' (every readings JSON and leg output records device == 'cpu'), a tiny random-init 2-layer GPT with the REAL frozen tokenizer served to BOTH base readers (relearn.BASE_SLIM and phase14_recall.CONVBASE_SLIM), 2 synthetic facts rendered through the real render_family / build_arm_bins path, ≥ 2 optimizer steps per rung through tp.train() with the ONE shared TrainConfig, the real recall and extraction scorers unstubbed, a forged control extraction count that puts X at 1.0 by call so the CURVE_K reading promotes and the FULL_K re-score lands under its own k-suffixed draw cache, and a recovery_gate verdict in RECOVERY_VERDICTS with every gate-output field present (D-09, D-10, D-21, D-31, D-32)"
    - "Read back OFF DISK, every arm's readings JSON carries train_config == checkpoint_train_config, the designated-seed arms' configs are identical on every field, the extra fresh seed differs only in seed, and the offset-stream sha256 is equal across the designated-seed arms and differs across seeds (D-26 i/ii/iii, RELRN-04)"
    - "Every refusal_node_id and e2e_node_id the apparatus block names is a line of pytest --collect-only -q (D-36); every provenance.module_sha256 in the record (when it exists) equals a bytes recompute with ALL drifted modules named at once (D-35); pyproject.toml is byte-identical to its committed blob (D-39)"
    - "The scored fixture's QUESTION STRINGS are disjoint from the teaching rows, the adversarial arm's trained rows (phase24_adversarial.adversarial_episodes) and the attacker corpus on the REAL fact set — 416 gated prompts behind 104 questions — and planting one teaching row into the scored set turns the count non-zero (D-17, RELRN-05)"
    - "The recovery_gate call inside run_gate carries exactly {recovered_successes, recovered_questions, x, z, baseline} — the curve, band and rungs cannot reach the verdict (RELRN-03)"
    - "When results/phase27_admission.json exists, build_record(frontier()) re-derives its verdict, rows, tallies, cleared_counts, x, recall_thresholds, baselines and budget exactly; when absent, the record is untracked (both-state)"
  artifacts:
    - path: "tests/test_phase27_relearn.py"
      provides: "The wiring proof, off-disk config diff, disjointness proof, node-id existence, provenance recompute, pyproject byte-identity, the driver-side RELRN-03 AST pin, the record re-derivation tripwire"
      min_lines: 450
  key_links:
    - from: "tests/test_phase27_relearn.py"
      to: "scripts/phase27_relearn.py"
      via: "relearn.main(['calibrate' | 'curve' | 'gate' | 'structural-proof', '--record', forged, '--leg', 'n8', '--out-dir', tmp, ...]) under _e2e_env with phase27_prereg budget symbols monkeypatched to 1 / 2 / (1, 2), phase25_run._DEVICE = 'cpu', phase25_run.DRAWS_DIR and phase14_recall.CONVBASE_SLIM under tmp"
      pattern: "relearn\\.main\\(\\[\"calibrate\""
    - from: "tests/test_phase27_relearn.py"
      to: "src/personacore/training/loop.py"
      via: "tp.train() reached through train_relearn_arm — spy on tp.train records train_config identity across arms"
      pattern: "monkeypatch\\.setattr\\(tp, \"train\""
    - from: "tests/test_phase27_relearn.py"
      to: "scripts/phase25_run.py"
      via: "monkeypatch.setattr(phase25_run, '_DEVICE', 'cpu') and monkeypatch.setattr(phase25_run, 'DRAWS_DIR', root / 'data') — the device cache and the draw-cache root, both read at call time by the real draw loop"
      pattern: "\"_DEVICE\""
    - from: "tests/test_phase27_relearn.py"
      to: "results/phase27_admission.json"
      via: "both-state: provenance digests recomputed from bytes; apparatus node ids vs pytest --collect-only -q; build_record re-derivation"
      pattern: "collect-only"
---

<objective>
Prove the driver is WIRED (D-10): one CPU end-to-end run on a tiny fixture through the real
`main()` → every leg → the real train path and the real scorers, ending in a recovery-gate
verdict; then the structural, provenance and node-id guards the record's `apparatus` and
`provenance` blocks promise, each both-state on the not-yet-committed record.

Decisions implemented: D-09 (CPU pin), D-10 (e2e half), D-11, D-17, D-21 (promotion measured),
D-26 (i)/(ii)/(iii), D-31, D-32, D-35, D-36, D-39; RELRN-03 pinned on the driver side.

## Decision resolutions

- The train-path resolutions (OQ1-B, OQ2, OQ3) and the D-26×D-27 seed reading are plan 27-03's; this
  plan MEASURES them: the e2e spies `tp.train` and asserts the `train_config` object passed for the
  designated-seed arms is the SAME instance (`is`), and the off-disk diff reads what `train()` wrote.
- D-32's "2 synthetic facts rendered through the real `render_family` + Phase-18 fixture paths":
  `phase18_extraction.build_corpus` binds `CORPUS_SOURCE_FIXTURE` rows to `LOCKED_FACTS` by id
  (RESEARCH A2 — verified this session at `phase18_extraction.py:873-880`: it reads
  `fixture["questions"][tier]` and `_prove`s `len(rows) == fixture["counts"][tier]`), so the env
  writes a tmp fixture JSON in the real fixture's schema for the two fake ids and points
  `CORPUS_SOURCE_FIXTURE` at it. No real `LOCKED_FACTS` string appears in `tests/`.
- D-09 on this host: `phase25_run.device()` (`:404-437`) caches `RuntimeConfig().device` in
  `_DEVICE` and resolves MPS on the dev box; the driver's legs and `phase25_run._draw_one_shape`
  (`:524`) both call it. The env pins `phase25_run._DEVICE = "cpu"` BEFORE any leg runs — the one
  switch that keeps the whole e2e on CPU (CLAUDE.md: tests must not require a GPU). No RED is ever
  taken by omitting this pin: that run would train on MPS.
- D-21 on a tiny fixture: the real X = `wilson_upper_bound(0, 416) + MARGIN_K × 0.0 = 0.006462`
  makes ANY reading with fewer than 416 gated questions FAIL (`wilson_upper_bound(0, n) > X` for
  every n < 416), so `promote_to_full_fidelity` (`mitigation_gate.py:963-1008`: PASS or gate-candidate
  INCONCLUSIVE only) would never promote and the FULL_K path would never be exercised. The env forges
  the CONTROL extraction counts in the forged frontier — `control_extraction_successes = control_extraction_questions`
  (416) on BOTH control points, because `extraction_ceiling_x` `_prove`s the two agree — so X by
  call is `wilson_upper_bound(416, 416) = 1.0` exactly (measured), and `wilson_upper_bound(n, n) ≤ 1.0`
  for every n ≥ 2 (measured n = 2..16): the provisional verdict is PASS by construction, the promotion
  fires, and the FULL_K re-score is exercised. The e2e reads X BY CALL (`phase27_prereg.extraction_ceiling_x(fr)`),
  never from `fr["verdicts"]["extraction_ceiling"]["X"]` (a summary field the forge does not rewrite).
- The both-ways frontier pin (`test_the_record_is_pinned_to_the_frontier_both_ways`) and the
  attacker-corpus re-render (`test_attacker_corpus_sha_re_renders`) already live in plan 27-01's
  `tests/test_phase27_prereg.py`; this plan adds the record-side complement
  (`test_the_record_re_derives_from_build_record`) rather than a duplicate node id.
- Nyquist: the shared `e2e_run` fixture is the phase's one disclosed exception to the 30 s
  per-command target (VALIDATION "Max feedback latency": ≤ 120 s). Keep `-k` selection during
  development; quote `--durations=3` in the SUMMARY.

Purpose: Phase 25 shipped 23 green `--dry-run` tests around a driver whose live path raised on its
first real call; D-10 is the structural answer and this plan is where it is executed.
Output: the second half of `tests/test_phase27_relearn.py`. Expected collected-test delta: +9 over
plan 27-03's count (record the measured number).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-relearning-attack/27-CONTEXT.md
@.planning/phases/27-relearning-attack/27-RESEARCH.md
@.planning/phases/27-relearning-attack/27-PATTERNS.md
@.planning/phases/27-relearning-attack/27-03-SUMMARY.md

<interfaces>
From scripts/phase27_relearn.py (plan 27-03):
  RECORD, STREAM_DIR, BASE_SLIM, PINNED_MODULES, TRAIN_PATH, DRAW_CACHE, APPARATUS_LEGS, ARMS, SUB_MODES, DISPATCH
  frontier() (lru_cache — monkeypatch relearn.frontier to a lambda returning the forged copy) ; admission(record_path)
  _require_admitted(record_path) — exists → ADMITTED → baselines == JSON-normalised PINNED_BASELINES → tracked ; disjointness_report() ; build_record(frontier_blob) ; admit(out_path, *, overwrite=False)
  make_recorder(stream_path, *, teaching_bin) ; stream_digest(state) ; shared_train_config() ; model_from_adapter(start_adapter, *, expected_sha256, device)
  train_relearn_arm(*, arm, leg, seed, cfg, start_adapter, expected_sha256, out_dir, device) -> readings dict (writes phase27_{leg}_{arm}_seed{seed}_readings.json carrying `device`, _rung{NNNN}_adapter.pt, _offsets.bin); builds personacore.config.RuntimeConfig(device=device) itself — never tp.RuntimeConfig
  score_rung(*, point_label, adapter_path, k, out_dir, device, facts, values) — point_label of the form phase27_{leg}_{key}_rung{NNNN}_k{K}; caches at phase25_run.draws_path(point_label) = DRAWS_DIR / f"phase25_{point_label}_draws.json"
  run_calibrate / run_curve / run_structural_proof(*, record, leg, out_dir) ; run_gate(*, record, leg, out_dir, baseline) — each resolves device = phase25_run.device() and records it
  outputs under out_dir: phase27_{leg}_calibration.json {z, fresh, control, threshold, fresh_readings_at_each_rung, device}, phase27_{leg}_curve.json {points: {key: {rungs, reading_at_z}}, device}, phase27_{leg}_gate.json {points: {key: {verdict, reasons, provisional, recovered, promoted, promotion_reason, prefix_identical}}, x, device}, phase27_{leg}_structural.json {shared_config, off_disk, data_order}
  build_parser() ; main(argv) -> 0

From scripts/phase27_prereg.py (plan 27-01) — monkeypatch targets (module attributes read at call time by the driver):
  MAX_STEPS, CHECKPOINT_INTERVAL, RELEARN_CAP, RUNGS, CURVE_K, FULL_K, FRESH_SEEDS, DESIGNATED_SEED, POOLED_SEED_INDEX, PINNED_BASELINES, BASELINE_KEYS
  CONTROL_KEYS = {"n8": "dp_n8_sigma0p000000", "n64": "dp_n64_sigma0p000000"} ; extraction_ceiling_x(frontier) reads frontier["points"][CONTROL_KEYS[leg]]["verdict"]["control_extraction_successes" / "control_extraction_questions" / "extraction_noise_floor" / "extraction_floor_provenance"] for BOTH legs and _prove's them equal
  recovery_gate reads PINNED_BASELINES at call time; RECOVERY_VERDICTS = ("PASS","FAIL","INCONCLUSIVE")

From scripts/phase25_run.py: DRAWS_DIR = _ROOT / "data" (:175) — REDIRECT under tmp ; draws_path(point_key) -> DRAWS_DIR / f"phase25_{point_key}_draws.json" (:180) — so the e2e's caches are named data/phase25_phase27_*_draws.json ; load_draws k identity (:216-230) ; _DEVICE = None (:404) / device() (:407) — PIN _DEVICE = "cpu" ; _draw_one_shape calls recall.load_adapted_model(device(), adapter) (:524)
From scripts/phase14_recall.py: CONVBASE_SLIM (:81) read at CALL time by load_adapted_model(device, adapter_path=None) (:712; exists-check + load_slim at :735-740) — the base BOTH scorers load (tp.score_arm at teach_persona.py:2459; phase25_run._draw_one_shape at :524); REDIRECT it under tmp beside relearn.BASE_SLIM
From tests/test_phase22_wiring.py lines 703-783: _E2E_CFG, _e2e_env(root, monkeypatch) — convbase blob, decodable-id bins via undecodable_ids_mask(from_json(tp.TOKENIZER_PATH), 8192), factset report "GO", tp.* redirections (MAX_STEPS=2, WARMUP_STEPS=1, BATCH_SIZE=1, EVAL_INTERVAL=1, CHECKPOINT_INTERVAL=1, preflight_device, RuntimeConfig). Its `tp.RuntimeConfig` ZERO-ARG lambda (:783) is inert for this driver, which builds personacore.config.RuntimeConfig(device=device) directly; copy it verbatim or drop it — never rely on it
From tests/test_phase21_replay_volume.py:119: fs.Fact(f.id, f.slot, v, f.tier) — the synthetic-fact shape
From tests/test_phase26_canary.py lines 284-300: fresh-interpreter subprocess shape (used here for pytest --collect-only -q)
From tests/test_phase24_record.py lines 288-335: the all-drifted provenance recompute
From tests/test_phase25_calibrate.py lines 570-586: watched RED on a tmp COPY
From src/personacore/checkpoint.py: export_slim(full_path, slim_path) (:205) ; export_adapter(path, *, adapter, lora_config, base_fingerprint) (:253)
From src/personacore/lora: LoRAConfig, inject_lora, lora_state_dict
From scripts/phase18_extraction.py: CORPUS_SOURCE_FIXTURE (:705) — results/phase16_recall_sample.json schema: {"questions": {tier: [{seed_index, fact_id, question, reserved}]}, "counts": {tier: n, ...}, ...}; build_corpus (:822) reads fixture["questions"][tier] and _prove's len(rows) == fixture["counts"][tier] (:873-880) — mirror BOTH keys for the tiers it reads, for 2 fake ids; corpus entries carry prompt_ids and NO question text
From scripts/erasure_gate.py: wilson_upper_bound(k, n) — wilson_upper_bound(416, 416) == 1.0 and wilson_upper_bound(n, n) <= 1.0 for n in 2..16 (measured this session)
From scripts/phase25_points.py: scoring_values() reads fs.LOCKED_FACTS + fs.SOFT_TIER_FACTS at call time
pyproject.toml — sha256 at ff38d9a to be read via `git show HEAD:pyproject.toml` bytes, never a literal
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The e2e environment and the wiring proof — calibrate → curve → gate → structural-proof on the tiny fixture, on CPU; the off-disk diff; the disjointness proof</name>
  <files>tests/test_phase27_relearn.py</files>
  <read_first>
    - tests/test_phase27_relearn.py (plan 27-03 state — reuse `_forge`, `clean_tree`, `_git`)
    - scripts/phase27_relearn.py (WHOLE — the leg functions, what each reads and writes, the sidecar names, the `_k{K}` cache labels, the `device` fields)
    - tests/test_phase22_wiring.py lines 703-783 (`_E2E_CFG`, `_e2e_env`) and 799-860 (`test_end_to_end_writes_no_scored_artifact` — the 2-step e2e precedent through `tp.main`; the `_spy_train` idiom :821-831)
    - tests/test_phase21_replay_volume.py lines 110-130 (synthetic `fs.Fact` construction)
    - scripts/phase18_extraction.py lines 822-900 (`build_corpus` — the fixture schema `questions` / `counts` and the per-tier count check at :873-880) and results/phase16_recall_sample.json (the first ~40 lines — its `questions` tier keys, `counts`, row shape)
    - scripts/phase25_run.py lines 175-192 (`DRAWS_DIR`, `draws_path` — the cache root to redirect and the `phase25_` file prefix), 195-232 (`load_draws` k identity), 404-437 (`_DEVICE`, `device()` — the cache to pin), 508-530 (`_draw_one_shape` — `recall.load_adapted_model(device(), adapter)`)
    - scripts/phase14_recall.py lines 81 (`CONVBASE_SLIM`) and 712-745 (`load_adapted_model` — reads `CONVBASE_SLIM` at call time)
    - scripts/teach_persona.py lines 2440-2470 (`score_arm` → `pr.load_adapted_model(device, adapter_path=...)` at :2459 — what it needs from each `Fact`)
    - scripts/mitigation_gate.py lines 352-370 (`extraction_ceiling` — X from the control counts) and 963-1008 (`promote_to_full_fidelity`)
    - src/personacore/checkpoint.py lines 205-236 (`export_slim`) and 253-279 (`export_adapter`)
    - .planning/phases/27-relearning-attack/27-PATTERNS.md §"tests/test_phase27_relearn.py" (the live-path frame, Analog B `_e2e_env`)
    - .planning/phases/27-relearning-attack/27-RESEARCH.md §"Code Examples" 6, §"Assumptions Log" A2, §"Common Pitfalls" 6 and 9 (Code Example 5's set (ii) is superseded — plan 27-03 `<objective>`)
  </read_first>
  <action>
Append to `tests/test_phase27_relearn.py` (module-scope additions — `copy` and `numpy as np` are already imported by the plan-27-03 half, do not re-add them: `dataclasses.asdict`, `torch`, `personacore.config.{ModelConfig, RuntimeConfig}`, `personacore.model.GPT`, `personacore.generation.undecodable_ids_mask`, `personacore.tokenizer.from_json`, `personacore.checkpoint as ckpt_mod`, `personacore.lora.{LoRAConfig, inject_lora, lora_state_dict}`, `erasure_gate` (scripts/ is on sys.path) — torch is already imported by this file's plan-27-03 tests only inside functions; here the e2e needs it at module scope, which is fine for a test module).

1. `_E2E_CFG = ModelConfig(block_size=<tp.BLOCK_SIZE>, n_layer=2, n_head=2, n_embd=16)` (D-31: two layers; `block_size` must match the packer — import `teach_persona as tp` inside `_e2e_env` and read `tp.BLOCK_SIZE`).

2. `def _e2e_env(root, monkeypatch) -> dict` — copy `tests/test_phase22_wiring.py:705-783` as a LOCAL helper (never imported across test modules; its `tp.preflight_device` / `tp.RuntimeConfig` lambdas may stay verbatim — they are inert for this driver, which builds `personacore.config.RuntimeConfig(device=device)` itself and never calls `tp.RuntimeConfig`), then extend, in this order:
   (a') CPU FIRST (D-09): `import phase25_run` (already a module import) and `monkeypatch.setattr(phase25_run, "_DEVICE", "cpu")` — `phase25_run.device()` (`:404-437`) returns the cached `_DEVICE` and would otherwise resolve MPS on the dev box for every leg and for `_draw_one_shape` (`:524`); this line precedes every other line so no code path can run before it.
   (a) after writing `convbase.pt`, `ckpt_mod.export_slim(convbase, root / "convbase_slim.pt")`, then BOTH base readers: `monkeypatch.setattr(relearn, "BASE_SLIM", root / "convbase_slim.pt")` (the driver's `model_from_adapter`) AND `import phase14_recall as pr` + `monkeypatch.setattr(pr, "CONVBASE_SLIM", root / "convbase_slim.pt")` (the base `tp.score_arm` → `pr.load_adapted_model` at `teach_persona.py:2459` and `phase25_run._draw_one_shape` at `:524` load — read at CALL time at `phase14_recall.py:735-740`). Without the second patch the first `score_rung` loads the real 13.9M base against the tiny adapter (or `SystemExit`s in CI where `checkpoints/convbase_slim.pt` is absent). The env NEVER touches `pr.load_adapted_model`, `tp.score_arm`, `phase25_run.draw_point_shapes`, `_draw_one_shape` or `score_point` — the scorers stay real.
   (b) two synthetic facts via `fs.Fact(...)` mirroring two real entries' `slot`/`tier` with new ids and values (`tests/test_phase21_replay_volume.py:119`), `monkeypatch.setattr(fs, "LOCKED_FACTS", fakes)` and `monkeypatch.setattr(fs, "SOFT_TIER_FACTS", ())`;
   (c) a tmp corpus fixture in `results/phase16_recall_sample.json`'s schema — `{"questions": {tier: rows}, "counts": {tier: len(rows), ...}}` for every tier `build_corpus` reads, rows `{"seed_index", "fact_id", "question", "reserved": false}` naming only the two fake ids — with `monkeypatch.setattr(x18, "CORPUS_SOURCE_FIXTURE", that path)`; the per-tier count `_prove` at `phase18_extraction.py:873-880` reads `counts[tier]` (A2 is verified HERE; if `build_corpus` needs more, the SUMMARY names what);
   (d) `monkeypatch.setattr(phase25_run, "DRAWS_DIR", root / "data")` — `draws_path(label)` (`:180`) is `DRAWS_DIR / f"phase25_{label}_draws.json"`, so under the real tree the e2e's caches would land as `data/phase25_phase27_*_draws.json`, invisible to a `data/phase27_*` glob;
   (e) tiny budget on the PREREG module: `monkeypatch.setattr(phase27_prereg, "MAX_STEPS", 1)`, `"CHECKPOINT_INTERVAL", 1`, `"RELEARN_CAP", 2`, `"RUNGS", (1, 2)`, `"CURVE_K", 2`, `"FULL_K", 4`, `"FRESH_SEEDS", (1337, 2024)`, `"DESIGNATED_SEED", 1337` (unchanged), and `tp.MAX_STEPS/WARMUP_STEPS/BATCH_SIZE/EVAL_INTERVAL/CHECKPOINT_INTERVAL` as the Phase-22 env sets them;
   (f) three tiny start adapters: build `GPT(_E2E_CFG)` + `inject_lora(model, tp.LORA_CFG)` under `torch.manual_seed(s)` for `s in (1337, 2024)` and one more for the control, `ckpt_mod.export_adapter(root / f"{name}_adapter.pt", adapter=lora_state_dict(model), lora_config=asdict(tp.LORA_CFG), base_fingerprint=<the slim blob's fingerprint, read under the key export_slim stores>)`; `pins = {"never_taught_1337": {...path, sha256, seed 1337, source "e2e"}, "never_taught_2024": {...}, "control_n8": {..., "point_key": "dp_n8_sigma0p000000"}}`; `monkeypatch.setattr(phase27_prereg, "PINNED_BASELINES", pins)` and `"BASELINE_KEYS", tuple(pins)`;
   (g) the mitigated adapter: a fourth export `mitigated_adapter.pt` (D-32: the forged point's adapter is the tiny model's own saved bytes);
   (h) a FORGED FRONTIER: `fr = copy.deepcopy(relearn.frontier())`; set `fr["points"]["dp_n8_sigma0p500000"]["adapter_path"] = str(mitigated path)` and `["adapter_sha256"]` to its digest; set `fr["verdicts"]["control_readings"]["dp_n8"]["recall_counts"]["taught"] = [0, 1008]` so `recall_threshold(fr, "n8")` is `0.0` and every rung clears (the tiny model can never reach 0.5486; a threshold of 0 lets the e2e reach `z_rule`, the promotion and `recovery_gate` with a real Z — say so in a comment); AND, for `leg in ("n8", "n64")`: `v = fr["points"][phase27_prereg.CONTROL_KEYS[leg]]["verdict"]`; `v["control_extraction_successes"] = v["control_extraction_questions"]` (416 — both controls, because `extraction_ceiling_x` `_prove`s they agree) so `phase27_prereg.extraction_ceiling_x(fr) == erasure_gate.wilson_upper_bound(416, 416) + MARGIN_K × 0.0 == 1.0` (measured) and the CURVE_K provisional verdict is PASS by construction (`wilson_upper_bound(n, n) ≤ 1.0` for every n ≥ 2) — write the comment: the real X = 0.006462 would FAIL any fixture with fewer than 416 gated questions and `promote_to_full_fidelity` (`mitigation_gate.py:963-1008`) would never fire; `monkeypatch.setattr(relearn, "frontier", lambda: fr)`;
   (i) the FORGED ADMITTED record at `root / "phase27_admission.json"`: `{"verdict": {"verdict": "ADMITTED", "reasons": ["forged for the wiring proof"]}, "admitted_point_keys": ["dp_n8_sigma0p500000"], "baselines": pins}` (the `baselines` equality conjunct of `_require_admitted` holds because (f) patched `PINNED_BASELINES` to the same `pins`). Return `{"root", "record", "out", "pins", "facts", "mitigated", "frontier": fr}`.

3. `@pytest.fixture(scope="module") def e2e_run(tmp_path_factory)` — ONE run shared by the tests below (pytest's `MonkeyPatch.context()` for a module-scoped monkeypatch): `env = _e2e_env(root, mp)`; `seen = []`; spy `tp.train` with `_spy_train(**kwargs)` appending `kwargs["train_config"]`, `kwargs.get("on_draw")` and `kwargs["runtime_config"].device` then delegating (the `:821-831` idiom); drive `relearn.main(["calibrate", "--record", str(env["record"]), "--leg", "n8", "--out-dir", str(env["out"])]) == 0`, then `["curve", ...] == 0`, `["gate", ..., "--baseline", "never_taught_1337"] == 0`, `["structural-proof", ...] == 0`; return `{**env, "train_calls": seen}`.

4. `test_the_live_path_is_wired_end_to_end(e2e_run)` (D-09, D-10, D-21, T-27-04): structure only, never numbers — (a) `train_calls`: `len == (2 fresh seeds + 1 control + 1 mitigated) × len(RUNGS) == 8`; every call had a callable `on_draw` and a `runtime_config.device == "cpu"`; the `train_config` objects for the seed-1337 fresh, control and mitigated calls are the SAME instance (`is`) within a leg run — collect by `seed` attribute — and the seed-2024 config equals it on every field but `seed` (D-26(i)); (b) files under `out`: for each of `fresh_seed1337`, `fresh_seed2024`, `control_seed1337`, `mitigated_seed1337`: `phase27_n8_{arm}_seed{S}_readings.json` with `["device"] == "cpu"`, `_rung0001_adapter.pt`, `_rung0002_adapter.pt`, `_offsets.bin`; plus `phase27_n8_calibration.json`, `phase27_n8_curve.json`, `phase27_n8_gate.json`, `phase27_n8_structural.json`, each with `["device"] == "cpu"` where the driver records it; (c) calibration: `z == 1` (both arms clear at the first rung under threshold 0), `fresh.first_clear == 1`, `control.first_clear == 1`, `threshold.value == 0.0` with `k == 0, n == 1008`, `train_path == relearn.TRAIN_PATH`; (d) curve: `points["dp_n8_sigma0p500000"]["rungs"]` has 2 entries each with int `steps` ∈ `(1, 2)`, int `scored_tokens == mask_ones × steps` (recompute from the readings' `mask_ones`), `taught_recall` / `heldout_recall` with int `numerator`/`denominator`, `extraction` with int `successes`/`questions`, a `band` dict with `floor, half_width, fresh, lo, hi, inside, margin_k == 2`, a `draws_cache` ending in `_k2_draws.json`; `reading_at_z` is the `steps == 1` entry; `finding` mentions "not a second gate"; (e) gate: `x == phase27_prereg.extraction_ceiling_x(e2e_run["frontier"])` and `x >= 1.0` (BY CALL — never `fr["verdicts"]["extraction_ceiling"]["X"]`, which the forge does not rewrite); the point's `provisional.verdict == "PASS"` with `provisional.k == 2`, `promoted is True`, `promotion_reason` starts with `"PROMOTE"`, `recovered.k == 4` (FULL_K after promotion), `recovered.draws_cache` ends in `_k4_draws.json`, `verdict in phase27_prereg.RECOVERY_VERDICTS`, `reasons` non-empty with a `"/"` count, `prefix_identical is True`, `baseline == "never_taught_1337"`, `z == 1`, `taught_recall_reported` present, `finding_is_not_a_gate is True`; (f) every rung adapter loads through `ckpt_mod.load_adapter` and its `adapter_sha256` in the readings equals a bytes recompute; (g) BOTH draw caches exist under `root / "data"` — `phase25_phase27_n8_dp_n8_sigma0p500000_rung0001_k2_draws.json` and `..._rung0001_k4_draws.json` (the `k` identity of `load_draws`, `phase25_run.py:216-230`, is why two files exist) — and NOTHING was written under the real `_ROOT / "data"` matching `phase27_*`, `phase25_phase27_*` OR `persona_relearn_attacker_*`; `relearn.RECORD` is untouched (both-state: bytes equal before/after if it exists, else still absent); (h) the scorers were real: `pr.load_adapted_model.__module__ == "phase14_recall"` and `inspect.getsourcefile(pr.load_adapted_model)` ends with `scripts/phase14_recall.py`; same for `tp.score_arm` (`teach_persona`) and `phase25_run.draw_point_shapes` / `_draw_one_shape` / `score_point` (`phase25_run`). Quote the gate's `verdict` + `reasons[0]` in the SUMMARY.

5. `test_off_disk_config_diff_is_empty(e2e_run)` (D-26(ii), RELRN-04): read the four readings JSONs OFF DISK (`json.loads` of the files, not the fixture's return values): for each, `train_config == checkpoint_train_config` (what the driver recorded vs what `train()` wrote into `latest.pt`); the three seed-1337 `train_config`s are equal; the seed-2024 one differs from them in exactly `{"seed"}`; `structural.json["shared_config"]["differing_fields"]` reports `[]` for the designated-seed arms and `["seed"]` for fresh 2024; `structural.json["off_disk"]` reports every file equal; `structural.json["data_order"]["devices"]` is `"cpu"` for every arm. Watched RED on a tmp COPY: rewrite one readings file's `train_config.max_steps` to `99` and re-run the same comparison over the copies → the diff names `max_steps`.

6. `test_offset_stream_digests_prove_data_order(e2e_run)` (D-26(iii), D-30): from the readings: `fresh_seed1337.offset_stream.sha256 == control_seed1337... == mitigated_seed1337...` when their `bin_path` lengths are equal (assert the lengths are equal — the same 2 facts render the same bin for every arm) and `!= fresh_seed2024...`; `draws == 2 rungs × (1 teaching + ceil(replay_windows / batch_size) replay)` per arm (compute from the readings' `replay_windows` and `tp.BATCH_SIZE == 1` under the env); `structural.json["data_order"]["equal_across_arms_at_designated_seed"] is True` and `["differs_across_seeds"] is True`; each `_offsets.bin` has length `draws × (1 + 8 × batch)` bytes (tag byte + uint64 per draw index), and its tag bytes are `0` for exactly `2 rungs × 1` draws (the teaching bin, tagged by IDENTITY against `offset_stream.teaching_bin`) and `1` for the rest (the replay bin — both bins end in `_train.bin`, so a suffix rule would have tagged every draw `0`).

7. `test_recovery_fixture_is_disjoint()` (D-17, RELRN-05 — the REAL fact set, no env): `report = relearn.disjointness_report()`; assert `report["overlaps"] == {"teaching": 0, "trained_attack": 0, "attacker_corpus": 0}`, `report["scored"]["gated_prompts"] == 416`, `report["scored"]["gated_questions"] == len({row["question"] for row in json.loads(x18.CORPUS_SOURCE_FIXTURE.read_text())["questions"][phase27_prereg.GATED_TIER]})` (104 at HEAD — recomputed, not retyped), `report["scored"]["heldout_recall"] > 0`, `report["trained_attack_rows"] > 0`, `report["unit"]` names "question strings", `report["held_out_family"] == phase27_prereg.HELD_OUT_FAMILY` and `HELD_OUT_FAMILY not in report["trained_families"]` (read, never spelled — the literal `"A2"` appears nowhere in this file); planted RED (a monkeypatch, never the tree): wrap `tp.render_episodes` so its output gains one pair whose question is a scored gated QUESTION STRING — take it from the fixture, `json.loads(x18.CORPUS_SOURCE_FIXTURE.read_text())["questions"][GATED_TIER][0]["question"]` (NOT from `x18.build_corpus`, whose entries carry `prompt_ids` and no text) — and assert `disjointness_report()["overlaps"]["teaching"] == 1`; assert `relearn.admit(tmp_path / "z.json")` under that same planted wrapper raises `SystemExit` (a non-zero overlap is a refused admission).

Runtime target: the shared `e2e_run` fixture under 120 s on CPU (8 tiny trainings of ≤ 2 steps, scoring at K = 2 / 4 over 2 facts) — the phase's one disclosed Nyquist exception; run with `-k` selection during development and record the `--durations=3` line in the SUMMARY.

Natural RED to record: run `test_the_live_path_is_wired_end_to_end` once with `_e2e_env` complete EXCEPT (h)'s `control_extraction_successes` forge — against the real X = 0.006462 the CURVE_K provisional verdict is FAIL, `promote_to_full_fidelity` returns False and (e)'s `promoted is True` fails quoting the driver's `promotion_reason` (`NO PROMOTION: verdict 'FAIL' ...`); quote it, then add the forge and watch GREEN. If (d)'s `DRAWS_DIR` redirect is omitted instead, (g) fails naming a `data/phase25_phase27_*` file under the real tree — also acceptable to quote. NEVER take a RED by omitting (a')'s `_DEVICE` pin: that run would train and score on MPS (D-09).

Threat references: T-27-04 (the live path reaches `recovery_gate` through `main()` on the real scorers), T-27-11 (rung adapters load through `load_adapter`; sha recomputed), T-27-05 (baseline reaches the gate from the CLI choice only).
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/pytest -q tests/test_phase27_relearn.py -x --durations=3 -k "wired_end_to_end or off_disk_config or offset_stream_digests or fixture_is_disjoint" && .venv/bin/ruff check tests/test_phase27_relearn.py && .venv/bin/ruff format --check tests/test_phase27_relearn.py && test -z "$(find data -maxdepth 1 \( -name 'phase27_*' -o -name 'phase25_phase27_*' -o -name 'persona_relearn_attacker_*' \))"</automated>
  </verify>
  <acceptance_criteria>
    - `test_the_live_path_is_wired_end_to_end` PASSES on this host: 8 `tp.train` calls seen, every `runtime_config.device == "cpu"`, every readings JSON `device == "cpu"`, the designated-seed `train_config` is one instance by `is`, the gate output's `provisional.verdict == "PASS"`, `promoted is True`, `recovered.k == 4`, `prefix_identical is True`, `x == extraction_ceiling_x(fr) >= 1.0`, `verdict ∈ ("PASS", "FAIL", "INCONCLUSIVE")`; two draw caches (`_k2_` and `_k4_`) exist under the tmp `data/`; the SUMMARY quotes the verdict and `reasons[0]`
    - `test_off_disk_config_diff_is_empty` asserts `train_config == checkpoint_train_config` for all four arms from files re-read off disk and the tmp-copy RED names `max_steps`
    - `test_offset_stream_digests_prove_data_order` asserts three equal digests at seed 1337 and a different one at 2024, the byte-length arithmetic, and the `0`/`1` tag counts
    - `test_recovery_fixture_is_disjoint` asserts 416 gated prompts, the recomputed unique gated-question count (104 at HEAD), three zero overlaps on the real fact set, `"A2"` absent from the test file (`grep -c '"A2"' tests/test_phase27_relearn.py` prints 0), and the planted-row RED (a fixture question string) yields `teaching == 1` plus an `admit` refusal
    - `find data -maxdepth 1 \( -name 'phase27_*' -o -name 'phase25_phase27_*' -o -name 'persona_relearn_attacker_*' \)` prints nothing after the run (no stray sidecar or draw cache under the real `data/`); `git status --short results/` shows no `phase27_admission.json`
    - The shared fixture's wall time is under 120 s (`--durations=3` quoted in the SUMMARY); the natural RED quoted is the no-promotion or stray-cache one, never an MPS run
  </acceptance_criteria>
  <done>The live path is proven through `main()` on the real train path and the real, unstubbed scorers, on CPU; the FULL_K promotion is exercised under its own cache; the three structural proofs are read back off disk; disjointness is a computed zero over question strings on the real fact set with its RED watched.</done>
</task>

<task type="auto">
  <name>Task 2: Node-id existence, provenance recompute, pyproject byte-identity, the driver-side RELRN-03 pin, the record re-derivation tripwire</name>
  <files>tests/test_phase27_relearn.py</files>
  <read_first>
    - tests/test_phase27_relearn.py (Task 1 state)
    - tests/test_phase26_canary.py lines 284-300 (fresh-interpreter subprocess shape)
    - tests/test_phase24_record.py lines 288-335 (the all-drifted provenance recompute — copy the failure-message shape)
    - tests/test_phase25_calibrate.py lines 570-586 (one-byte-edited tmp copy)
    - scripts/phase27_relearn.py `run_gate` (the single `recovery_gate(` call site inside its local `_verdict` helper) and `APPARATUS_LEGS`
    - tests/test_phase27_prereg.py (plan 27-01 — `test_the_record_is_pinned_to_the_frontier_both_ways`, to avoid duplicating its assertions)
  </read_first>
  <action>
Append to `tests/test_phase27_relearn.py`:

- `test_every_apparatus_node_id_exists` (D-36, T-27-04): `completed = subprocess.run([sys.executable, "-m", "pytest", "--collect-only", "-q", "tests/test_phase27_relearn.py"], cwd=_ROOT, capture_output=True, text=True)`; `collected = set(completed.stdout.splitlines())`; for every leg in `relearn.APPARATUS_LEGS`: `leg["refusal_node_id"] in collected` and `leg["e2e_node_id"] in collected`; both-state on the record: if `relearn.RECORD.exists()`, the record's `apparatus.legs` equals `list(relearn.APPARATUS_LEGS)` and every node id there is in `collected`; else assert `git ls-files results/phase27_admission.json` is empty. Watched RED on a copy: a `tmp_path` list of legs with one node id's test name misspelled is NOT a subset of `collected`.
- `test_provenance_digests_match_live_bytes` (D-35, T-27-12; `tests/test_phase24_record.py:288-335` shape, both-state): if the record exists: `pins = record["provenance"]["module_sha256"]`; `assert pins`; `"scripts/phase27_relearn.py" in pins` (the emitter pins itself); `set(pins) == set(relearn.PINNED_MODULES)`; `drifted = [(name, recorded, live) ...]` with `live = hashlib.sha256((_ROOT / name).read_bytes()).hexdigest()` — never `relearn._sha256`; `assert not drifted, <message naming ALL drifted with recorded and live in full>`; else assert the record is untracked. In BOTH states the one-byte RED on a tmp copy of `scripts/phase27_prereg.py` (`tests/test_phase25_calibrate.py:570-586`): the copy's digest differs from the live one and the live file is unchanged byte for byte.
- `test_pyproject_is_byte_identical` (D-39, RPT-03's fourth milestone): `live = (_ROOT / "pyproject.toml").read_bytes()`; `committed = subprocess.run(["git", "show", "HEAD:pyproject.toml"], cwd=_ROOT, capture_output=True, check=True).stdout`; `assert hashlib.sha256(live).hexdigest() == hashlib.sha256(committed).hexdigest()`; also `git log --oneline -- pyproject.toml` has no commit newer than the Phase-26 close (read the newest commit's date and assert it is before `phase27_prereg.COMMITTED`).
- `test_the_curve_cannot_reach_the_verdict_through_the_driver` (RELRN-03, D-19): `ast` over `scripts/phase27_relearn.py`: collect every `Call` whose func is `Attribute(attr="recovery_gate")`; exactly one, enclosed (transitively) by `run_gate` — plan 27-03 writes it once in a local helper `_verdict` nested inside `run_gate`, so walk `run_gate`'s subtree; its keyword names == `{"recovered_successes", "recovered_questions", "x", "z", "baseline"}` and it has no positional args and no `**` splat; `inspect.signature(phase27_prereg.recovery_gate).parameters` is the same set (the prereg pin from 27-01 restated from the caller's side). Also: no `Call` to `band(` or `first_clear(` inside `run_gate`, and `run_gate`'s subtree contains a `Call` to `extraction_ceiling_x` and no `Subscript` whose constant slice is `"extraction_ceiling"` (X by call, never the summary field).
- `test_the_record_re_derives_from_build_record` (D-33/D-34 tripwire, both-state): if the record exists: `fresh = relearn.build_record(relearn.frontier())`; for each key in `("verdict", "admitted_point_keys", "tallies", "tallies_by_leg", "cleared_counts", "rows", "frontier_sha256", "frontier_bytes", "x", "recall_thresholds", "baselines", "fresh_seeds", "designated_seed", "attacker_corpus", "budget")`: `record[key] == fresh[key]` (the record was generated, never authored — anything hand-edited goes RED); `record["apparatus"]["legs"] == fresh["apparatus"]["legs"]`; else assert untracked. In BOTH states: `fresh["verdict"]["verdict"] == "MOOT"` on the committed frontier and a deep copy of `fresh` with one row's `cleared_a` flipped is `!= build_record(...)` (watched RED on a copy).

Commit (`test(27-04): wiring proof on CPU, off-disk diff, disjointness, node ids, provenance, pyproject, RELRN-03 caller pin`).

Threat references: T-27-04, T-27-12, T-27-03 (the record re-derivation complements the frontier both-ways pin), T-27-SC (pyproject unchanged — nothing installed).
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/pytest -q tests/test_phase27_relearn.py -x --durations=3 && .venv/bin/ruff check tests/test_phase27_relearn.py && .venv/bin/ruff format --check tests/test_phase27_relearn.py && .venv/bin/pytest -q tests/test_phase27_prereg.py tests/test_phase27_on_draw.py tests/test_phase24_record.py tests/test_phase25_close.py tests/test_phase20_correction.py tests/test_phase23_resume.py::test_resume_from_none_is_inert -x && test -z "$(find data -maxdepth 1 \( -name 'phase27_*' -o -name 'phase25_phase27_*' -o -name 'persona_relearn_attacker_*' \))"</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest -q tests/test_phase27_relearn.py` reports 0 failed, 0 skipped, ≥ 35 passed on this host
    - `test_every_apparatus_node_id_exists` finds all 4 `refusal_node_id`s and the `e2e_node_id` in a fresh-interpreter `--collect-only -q` listing, and the misspelled-copy RED fails the subset check
    - `test_provenance_digests_match_live_bytes` and `test_the_record_re_derives_from_build_record` pass in the ABSENT state with a real assertion (`git ls-files results/phase27_admission.json` empty) and the copy-RED in both states
    - `test_pyproject_is_byte_identical` passes against `git show HEAD:pyproject.toml`
    - `test_the_curve_cannot_reach_the_verdict_through_the_driver` finds exactly one `recovery_gate(` call with exactly the five keywords inside `run_gate`'s subtree, an `extraction_ceiling_x(` call, and no `"extraction_ceiling"` subscript
    - `.venv/bin/pytest --collect-only -q | tail -1` reports plan 27-03's count + 9; the measured number is written in the SUMMARY
    - `git ls-files 'results/phase27_*'` empty; `find data -maxdepth 1 \( -name 'phase27_*' -o -name 'phase25_phase27_*' -o -name 'persona_relearn_attacker_*' \)` empty
  </acceptance_criteria>
  <done>Every guard the record's `apparatus` and `provenance` blocks promise exists, passes both-state today, and is ready to flip to the present state when the operator commits the record in plan 27-05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tests → driver → train path | The e2e stubs NOTHING on the train/score path; only paths (`BASE_SLIM`, `CONVBASE_SLIM`, `DRAWS_DIR`, `CORPUS_SOURCE_FIXTURE`, `tp._REPO_ROOT`), budgets, the device cache (`phase25_run._DEVICE = "cpu"`), the pins and the frontier copy (forged control counts) are redirected under tmp |
| tmp tree ↔ real tree | Every write lands under `tmp_path`; the real `data/` (`phase27_*`, `phase25_phase27_*` and `persona_relearn_attacker_*`), `results/` and the record are asserted untouched |
| record ↔ suite | Node ids and module digests are checked against a fresh interpreter and live bytes, both-state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-03 | Tampering | hand-edited record | mitigate | `test_the_record_re_derives_from_build_record` (generated, never authored); 27-01's frontier both-ways pin |
| T-27-04 | Denial (of evidence) | unwired live path | mitigate | `test_the_live_path_is_wired_end_to_end` through `main()` on the real train path and the real, unstubbed scorers (asserted by module); `test_every_apparatus_node_id_exists` |
| T-27-05 | Tampering | baseline after data | mitigate | e2e passes `--baseline never_taught_1337` and asserts it in the gate output; the AST caller pin |
| T-27-11 | Spoofing | wrong-weights rung adapters | mitigate | rung adapters re-hashed from bytes and loaded via `load_adapter` in the e2e |
| T-27-12 | Tampering | provenance drift | mitigate | `test_provenance_digests_match_live_bytes` (all drifted named; bytes recompute; one-byte copy RED) |
| T-27-13 | Information disclosure | tmp sidecars | accept | Synthetic facts; tmp trees |
| T-27-SC | Tampering | package installs | accept | `test_pyproject_is_byte_identical` — nothing installed (D-39) |
</threat_model>

<verification>
- `.venv/bin/pytest -q tests/test_phase27_relearn.py tests/test_phase27_prereg.py tests/test_phase27_on_draw.py` green in under 3 minutes
- `tests/test_phase24_record.py`, `tests/test_phase25_close.py`, `tests/test_phase20_correction.py`, `tests/test_phase23_resume.py::test_resume_from_none_is_inert` green
- `find data -maxdepth 1 \( -name 'phase27_*' -o -name 'phase25_phase27_*' -o -name 'persona_relearn_attacker_*' \)` empty; `git ls-files 'results/phase27_*'` empty
</verification>

<success_criteria>
- SC2–SC5 are TRUE as guarded code: the gate with its required pinned baseline and X by call, the cost curve as a qualifying finding, the FULL_K promotion under its own cache, the three structural proofs and the disjoint fixture are each exercised by a CPU test through the real driver on CPU, and every node id / digest the record will name is checked both-state
</success_criteria>

<output>
Create `.planning/phases/27-relearning-attack/27-04-SUMMARY.md` when done
</output>
