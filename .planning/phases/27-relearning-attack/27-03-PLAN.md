---
phase: 27-relearning-attack
plan: 03
type: execute
wave: 2
depends_on: ["27-01", "27-02"]
files_modified:
  - scripts/phase27_relearn.py
  - tests/test_phase27_relearn.py
autonomous: true
requirements: [RELRN-01, RELRN-02, RELRN-03, RELRN-04, RELRN-05]
must_haves:
  truths:
    - "python scripts/phase27_relearn.py admit is the ONE gate call: it refuses an existing results/phase27_admission.json (write-once, --force only), refuses a dirty scripts/ src/ results/ tree BEFORE hashing anything, calls phase27_prereg.relearning_is_worth_attempting(frontier) once, and atomic-writes a record whose 44 rows, tallies, tallies_by_leg and cleared 30/4/1 are re-derived and _prove'd at the write; the driver builds no git argv beyond the read-only ls-files conjunct and never touches git (D-04, D-08, D-14, D-15, D-33, D-34)"
    - "Every attack leg — calibrate, curve, gate, structural-proof — opens with _require_admitted(record): the record must exist, read verdict.verdict == 'ADMITTED', carry baselines equal to the JSON-normalised phase27_prereg.PINNED_BASELINES, and (when inside the repo) be git-tracked; on MOOT / INCONCLUSIVE / absent / moved pins / untracked the leg exits non-zero with a message naming what it read and 'REFUSING', and nothing under --out-dir is written (D-08, D-12)"
    - "The record's apparatus block lists the four legs × {name, sub_mode, refusal_node_id, e2e_node_id, train_path} with status 'not exercised' and reason 'gate read MOOT', plus fresh_curve_disclosure, device_policy and draw_cache (the Phase-25 cache naming data/phase25_phase27_*_draws.json); provenance.module_sha256 covers seven modules by repo-relative path, git_sha, head_at_write, written_utc and torch_version; disjointness carries computed overlap counts over QUESTION STRINGS, all zero, with set (ii) = phase24_adversarial.adversarial_episodes rows (D-11, D-17, D-35, D-36)"
    - "The three arms (fresh, control, mitigated) train through teach_persona.train() called directly with ONE shared TrainConfig instance at DESIGNATED_SEED, a personacore.config.RuntimeConfig(device=device) built by the driver, and bins from teach_persona.build_arm_bins over the pre-registered attacker corpus with the recipe's replay window; rungs are a resume chain with max_steps_override at every CHECKPOINT_INTERVAL up to RELEARN_CAP, each rung exported as its own adapter; the offset stream is recorded through on_draw, tagged by bin IDENTITY (teaching vs replay) and hashed per arm (D-13 resolved per OQ1-B, D-20, D-25 resolved per OQ2, D-26 i/iii, D-29, D-30)"
    - "gate calls phase27_prereg.recovery_gate with exactly recovered_successes / recovered_questions / x / z / baseline — x by call (extraction_ceiling_x), baseline from a required --baseline CLI choice over BASELINE_KEYS — on the FULL_K-promoted core_held_out extraction counts at Z, re-scored under a k-suffixed draw-cache label so load_draws' k identity never refuses; taught recall travels in the same output row and never decides; the cost curve is a sibling finding block (D-09, D-16, D-21, RELRN-01, RELRN-03)"
    - "main() dispatches through a mode → function dict with explicit keyword arguments, so every kwarg main() passes exists in the leg's signature and every required kwarg is supplied — checked by the kwargs-trace test without training (D-10)"
    - "The driver imports no torch-touching module at module scope and never calls torch.load by name; every adapter and base load goes through personacore.checkpoint.load_slim / load_adapter (weights_only=True) and every attacked adapter is refused on an adapter_sha256 mismatch against its pin; the driver's only device resolver is phase25_run.device(), which the e2e pins to CPU (D-09, D-22, T-27-06, T-27-11)"
  artifacts:
    - path: "scripts/phase27_relearn.py"
      provides: "CLI driver: admit + the four legs, _require_admitted, train_relearn_arm (train() direct, rung chain, recorder), scoring at CURVE_K with FULL_K promotion under k-suffixed cache labels, record schema, apparatus block, provenance"
      exports: ["RECORD", "STREAM_DIR", "PINNED_MODULES", "TRAIN_PATH", "APPARATUS_LEGS", "BASE_SLIM", "ARMS", "SUB_MODES", "frontier", "admission", "_require_admitted", "disjointness_report", "build_record", "admit", "make_recorder", "stream_digest", "shared_train_config", "model_from_adapter", "train_relearn_arm", "score_rung", "run_calibrate", "run_curve", "run_gate", "run_structural_proof", "DISPATCH", "build_parser", "main"]
      min_lines: 450
    - path: "tests/test_phase27_relearn.py"
      provides: "Structural CPU tests: per-leg refusal on forged MOOT / INCONCLUSIVE / absent / moved-pins / untracked records, write-once, dirty tree, kwargs trace, AST import + torch.load + git-surface guards, apparatus shape, BASE_SLIM pin"
      min_lines: 200
  key_links:
    - from: "scripts/phase27_relearn.py"
      to: "scripts/phase27_prereg.py"
      via: "relearning_is_worth_attempting(frontier()) in admit; cleared_abc / cleared_counts / point_verdict_string for the rows; recovery_gate / z_rule / first_clear / band / scored_tokens / promote_at_z / recall_threshold / extraction_ceiling_x in the legs — every constant read as phase27_prereg.<NAME> at call time"
      pattern: "phase27_prereg\\.relearning_is_worth_attempting\\("
    - from: "scripts/phase27_relearn.py"
      to: "scripts/teach_persona.py"
      via: "tp.train(train_config=CFG, runtime_config=RuntimeConfig(device=device), ..., max_steps_override=rung, resume_from=..., on_draw=recorder) and tp.build_arm_bins(...) inside train_relearn_arm — lazy import"
      pattern: "tp\\.train\\("
    - from: "scripts/phase27_relearn.py"
      to: "scripts/phase24_adversarial.py"
      via: "disjointness_report's set (ii) = {q for _persona, q, _a in phase24_adversarial.adversarial_episodes(tok)}"
      pattern: "adversarial_episodes\\("
    - from: "scripts/phase27_relearn.py"
      to: "results/phase27_admission.json"
      via: "phase25_run.atomic_write_json after the refuse_if_exists-shaped _prove and refuse_if_dirty"
      pattern: "atomic_write_json\\("
    - from: "tests/test_phase27_relearn.py"
      to: "scripts/phase27_relearn.py"
      via: "relearn.main([mode, '--record', forged, '--leg', 'n8', '--out-dir', tmp]) raising SystemExit naming the verdict; ast + inspect.signature over main / DISPATCH / run_<leg>"
      pattern: "relearn\\.main\\("
---

<objective>
Build the CLI driver `scripts/phase27_relearn.py` — the single `admit` gate call that writes
`results/phase27_admission.json` write-once, and the four attack legs (`calibrate`, `curve`,
`gate`, `structural-proof`) that each refuse unless the COMMITTED record reads ADMITTED — plus the
structural CPU tests that watch every refusal RED, pin the write-once / dirty-tree order, trace the
kwargs from `main()` into each leg, and keep torch and `torch.load` out of the driver's module
scope.

Decisions implemented: D-04, D-08, D-09, D-10 (kwargs-trace half), D-11, D-12 (the record's
pins re-checked at every leg), D-13, D-14, D-15, D-16, D-17 (the record's computed field), D-20,
D-21, D-22, D-25, D-26 (i)/(ii)/(iii), D-27, D-28, D-29, D-30, D-33, D-34, D-35, D-36, D-39.

## Decision resolutions

Visible resolutions (the operator may override at execution; none reinterprets a decision silently):
- **D-13 / D-26(i) resolved per RESEARCH OQ1 option B:** the attacker, fresh and control arms call
  `teach_persona.train()` DIRECTLY with ONE shared `TrainConfig` instance built from `tp.LR`,
  `tp.WARMUP_STEPS`, `tp.BATCH_SIZE`, `tp.WEIGHT_DECAY` and `phase27_prereg.RELEARN_CAP`, with bins
  from `tp.build_arm_bins` — the shape `phase23_run.train_never_taught` (`scripts/phase23_run.py:612-699`)
  used to produce the very baseline D-12 pins. `train_arm` is not called (it builds its own config at
  `teach_persona.py:1978` and the module is digest-pinned). The record's `apparatus.legs[*].train_path`
  and the module constant `TRAIN_PATH` state this so Phase 28 generates the disclosure from the record.
- **D-25 resolved per RESEARCH OQ2:** rungs are a resume chain — one `tp.train()` call per rung with
  `max_steps_override=rung` and `resume_from=<latest.pt>` after the first — each rung's LoRA state
  exported as its own adapter file. Plan 27-02's `test_rung_chain_stream_equals_one_run` is the
  measured proof that the chain equals one uninterrupted run.
- **D-29 resolved per RESEARCH OQ3:** the recorder is passed as `on_draw=` to `tp.train()` (plan 27-02's
  passthrough), never by rebinding the loop module.
- **D-26(i) × D-27 (five fresh seeds) resolved:** the ONE shared instance `CFG` carries
  `seed=phase27_prereg.DESIGNATED_SEED`; the arms compared at identical seed (fresh@1337,
  control@1337, mitigated@1337) receive that SAME object. The four extra fresh seeds D-27 locks
  receive `dataclasses.replace(CFG, seed=s)`, and the off-disk diff (`structural-proof`, plan 27-04's
  test) asserts their `train_config` equals `asdict(CFG)` on every field EXCEPT `seed`, naming the
  seed. `TrainConfig` has a `seed` field, so "one instance for five seeds" is not expressible; this is
  the smallest reading that keeps both decisions.
- **D-17 set (ii) resolved against the code, superseding RESEARCH Code Example 5:** the "adversarial
  arm's trained families" are the rows that arm TRAINS ON — `phase24_adversarial.adversarial_episodes(tok)`
  (`scripts/phase24_adversarial.py:392`, the pool `teach_persona.build_bins` consumes; `TRAINED_TIER`
  rows in `TRAINED_FAMILIES`, A2-free by construction). Code Example 5 defined set (ii) as the scored
  corpus's OWN A1/A3 entries, which intersect the gated set in 312 of 416 prompts by construction and
  would make `admit` refuse on the real frontier. Measured at HEAD this session on the real fact set:
  gated questions ∩ `adversarial_episodes` questions = 0; gated ∩ teaching = 0.
- **D-17 unit resolved:** the comparison unit on every side is the QUESTION STRING. `build_corpus`
  entries carry `prompt_ids` and NO text (`scripts/phase24_adversarial.py:28-32` states it; the
  fixture `results/phase16_recall_sample.json` is the only place the text lives), so the scored gated
  set is `{row["question"] for row in fixture["questions"][GATED_TIER]}` — 104 unique strings behind
  the 416 gated prompts (104 × 4 attack families; measured). Both counts are recorded.
- **D-09 device:** the driver's ONLY device resolver is `phase25_run.device()` (`:404-437`, cached
  `_DEVICE`), which resolves MPS on the dev box. No leg may proceed past `_require_admitted` in this
  phase (the record reads MOOT); the e2e (plan 27-04) pins `phase25_run._DEVICE = "cpu"` and every
  readings JSON records `device`. The driver builds `personacore.config.RuntimeConfig(device=device)`
  itself and never calls `tp.RuntimeConfig` (the Phase-22 e2e helper rebinds that name to a zero-arg
  lambda).
- **OQ6:** REFUSED rows carry `refused: true`, `cleared_a/b/c: null`, `verdict: "REFUSED"`, no reason text.

Purpose: D-08's "every attack leg refuses unless the committed record reads ADMITTED" and D-15's
"the driver writes the file and never touches git" become properties of the code; D-10's kwargs
trace makes a renamed kwarg RED without training (the Phase 25 unwired-driver incident).
Output: `scripts/phase27_relearn.py`, `tests/test_phase27_relearn.py` (structural half; plan 27-04
appends the wiring proof). Expected collected-test delta: about +25 over plans 27-01/27-02's total
(record the measured count in the SUMMARY).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-relearning-attack/27-CONTEXT.md
@.planning/phases/27-relearning-attack/27-01-SUMMARY.md
@.planning/phases/27-relearning-attack/27-02-SUMMARY.md

<!-- 27-RESEARCH.md and 27-PATTERNS.md are deliberately NOT @-included whole (the plan+context
     payload exceeded 200 KB); each task's <read_first> names the sections to read. -->

<interfaces>
<!-- Verified at HEAD b70daa8 this session; plan 27-01 / 27-02 outputs as specified there. -->

From scripts/phase27_prereg.py (plan 27-01; CPU-only at import — read every constant as phase27_prereg.<NAME> at CALL time so the e2e test's monkeypatches take):
  COMMITTED, RECORDS_AT_COMMIT, ARTIFACT_GLOB, VERDICTS, EXPECTED_POINTS, REFUSED, LEGS = ("n8","n64"), CONTROL_KEYS = {"n8": "dp_n8_sigma0p000000", "n64": "dp_n64_sigma0p000000"}
  MARGIN_K, CURVE_K = 16, FULL_K = 48, F_Y, GATED_TIER, ATTACK_FAMILIES, HELD_OUT_FAMILY, TRAINED_FAMILIES, FRONTIER_RECORD
  MAX_STEPS = 200, CHECKPOINT_INTERVAL = 50, DESIGNATED_SEED = 1337, RELEARN_CAP = 400, RUNGS, FRESH_SEEDS, POOLED_SEED_INDEX
  PINNED_BASELINES (7 keys), BASELINE_KEYS, ATTACKER_ARM, ATTACKER_PREFIX = "phase27", ATTACKER_REPLAY_RATIO, ATTACKER_CORPUS
  NOT_EXERCISED, FRESH_CURVE_DISCLOSURE, RECOVERY_VERDICTS
  point_verdict_string(point) ; cleared_abc(kw) -> (a,b,c) | (None,None,None) ; cleared_counts(frontier) ; leg_of(key) ; arm_of(key)
  extraction_ceiling_x(frontier) -> float — mitigation_gate.extraction_ceiling(**) on frontier["points"][CONTROL_KEYS["n8"]]["verdict"]'s control_extraction_successes / control_extraction_questions / extraction_noise_floor / extraction_floor_provenance, _prove'd equal on CONTROL_KEYS["n64"] (BY CALL — a forged frontier's summary field verdicts.extraction_ceiling.X is NOT what it returns)
  relearning_is_worth_attempting(frontier) -> (verdict, reasons) ; admitted_point_keys(frontier)
  recall_threshold(frontier, leg) -> (threshold, k, n) ; first_clear(rung_readings, threshold) ; z_rule(*, fresh_first_clear, control_first_clear)
  band(*, mitigated, fresh_readings) -> dict ; scored_tokens(*, mask_ones, steps) ; recovery_gate(*, recovered_successes, recovered_questions, x, z, baseline) ; promote_at_z(verdict, reasons) ; attacker_corpus_rows()

From src/personacore/training/loop.py (plan 27-02): train(*, ..., resume_from=None, max_steps_override=None, checkpoint_path=None, checkpoint_interval=None, on_draw=None, return_final_loss=False)
From src/personacore/training/data.py (plan 27-02): get_batch_memmap_masked(..., *, on_draw=None) calls on_draw(bin_path, ix)
From src/personacore/config.py: RuntimeConfig(device="cpu") (:48) — the driver builds RuntimeConfig(device=device) from THIS module, lazily, never via tp.RuntimeConfig

From scripts/teach_persona.py (torch at import — LAZY inside legs only):
  SEED = 1337 (:104) ; BLOCK_SIZE = 256 (:105) ; CONVBASE_BEST (:95) ; TOKENIZER_PATH (:96) ; DIALOG_TRAIN_BIN = data/dialog_train.bin / DIALOG_TRAIN_MASK (:99-100) ; DIALOG_VAL_BIN/MASK (:101-102)
  LR = 3e-4 (:1570) ; WEIGHT_DECAY = 0.0 (:1574) ; BATCH_SIZE = 8 (:1576) ; WARMUP_STEPS = 20 (:1583) ; EVAL_INTERVAL = 10 (:1584) ; MAX_STEPS = 200 (:1582) ; CHECKPOINT_INTERVAL = 50 (:1585) ; LORA_CFG (:1565)
  def replay_window_budget(n_facts, block_size=BLOCK_SIZE) -> TOKENS (:182)  — train() wants WINDOWS: // BLOCK_SIZE (:1970-1972)
  def arm_outputs(arm, *, prefix="phase14") -> {"bin","mask","csv","checkpoint","adapter"} (:344) — the teaching bin is data/persona_{arm}_train.bin (NO phase prefix — arm_outputs docstring; the attacker's lands at data/persona_relearn_attacker_{leg}_{arm}_seed{seed}_train.bin), so BOTH the teaching bin and DIALOG_TRAIN_BIN end in "_train.bin" (never tag a draw by name suffix)
  def refuse_if_exists(paths, *, expected=()) (:410)
  def render_episodes(facts, family_ids, *, second_person=False) -> [(q, a)] (:495)
  def build_arm_bins(arm, facts, family_ids, *, second_person=False, replay_ratio=0.0, adversarial_ratio=0.0, seed=SEED, prefix="phase14", resume_from=None) -> (tok, stats, paths) (:1279); stats["n_facts"]
  def score_arm(arm, facts, adapter_path, device) -> {"on_taught","on_heldout","off_taught","off_heldout", ...} (:2440) — loads the base through phase14_recall.load_adapted_model(device, adapter_path=...) at :2459, i.e. phase14_recall.CONVBASE_SLIM; read counts as scripts/phase25_recall.py:159-175 does (`_tier(scored["on_taught"])` → numerator/denominator)
  tp.train, tp.TrainConfig, tp.GPT, tp.ModelConfig, tp.inject_lora, tp.mark_only_lora_trainable, tp.seed_everything, tp.snapshot_params, tp.torch re-exported (phase23_run.py:612-690 uses them through tp)

From src/personacore/checkpoint.py:
  def load_slim(path, *, map_location="cpu") -> dict (:237; weights_only=True) ; def export_slim(full_path, slim_path, *, map_location="cpu") (:205)
  def export_adapter(path, *, adapter, lora_config, base_fingerprint) -> dict (:253) ; def load_adapter(path, *, expected_fingerprint=None, map_location="cpu") -> dict (:280; weights_only=True)
  def load_checkpoint(path, *, model, optimizer=None, scheduler=None, scaler=None, map_location="cpu") -> ckpt dict with "train_config", "model_config", "step", "git_sha" (:156; RESTORES global RNG state as a side effect)
  save_checkpoint stores "train_config": asdict(train_config) (:135)
From src/personacore/lora/inject.py: lora_state_dict(model) (:67) ; load_adapter_weights(model, artifact) (:76)
From src/personacore/provenance.py: git_sha(default="unknown") (:28) ; refuse_if_dirty(*, who, detail, pathspec=(), cwd=None) (:47)

From scripts/phase14_recall.py (torch at import — LAZY): CONVBASE_SLIM = checkpoints/convbase_slim.pt (:81) — read at CALL time by load_adapted_model(device, adapter_path=None) -> (model, cfg, tok, forbid, artifact) (:712; the CONVBASE_SLIM exists-check and load_slim at :735-740), so a test redirects the base by monkeypatching phase14_recall.CONVBASE_SLIM ; N_SEEDED_SAMPLES = 8 (:152) ; draw_all (:846) ; class RecallItem(NamedTuple) with fields (fact, question, split, reserved, seed_index) (:986) ; build_question_sets(facts) -> (taught, held_out, excluded) lists of RecallItem (:1026) — read `.question`
From scripts/phase14_factset.py (no torch): Fact NamedTuple (:51) ; LOCKED_FACTS (:390) ; SOFT_TIER_FACTS ; TAUGHT_FAMILY_IDS / HELDOUT_FAMILY_IDS (:825-826) ; render_family(family_id, fact) (:833)
From scripts/phase18_extraction.py (torch at import — LAZY): K = 48 (:93) ; ATTACK_FAMILIES (:146) ; GATED_TIER = "core_held_out" (:173) ; CORPUS_SOURCE_FIXTURE = results/phase16_recall_sample.json (:705) whose schema is {"questions": {tier: [{seed_index, fact_id, question, reserved}]}, "counts": {tier: n, ...}, ...} — build_corpus(tok) (:822) reads fixture["questions"][tier], _prove's len(rows) == fixture["counts"][tier] (:873-880) and binds rows to factset.LOCKED_FACTS by id; corpus["prompts"] entries carry EXACTLY the keys dose, fact_id, family, prompt_ids, realized_injection, seed_index, slot, source_family, tier — NO question text ; corpus_sha256(corpus) (:758) ; score_records(records, values) (:1919)
From scripts/phase25_run.py (CPU-safe at import): atomic_write_json(path, blob) (:118) ; DRAWS_DIR = _ROOT / "data" (:175) ; draws_path(point_key) -> DRAWS_DIR / f"phase25_{point_key}_draws.json" (:180; charset via phase25_prereg.point_record_path — alphanumerics, "-", "_") ; load_draws(path, *, adapter_sha256, corpus_sha256, k) (:195) REFUSES a cache whose recorded k differs (:216-230 — the K=16 → K=48 promotion is the named case) ; write_draws (:233) ; _DEVICE = None (:404) ; device() (:407; resolves RuntimeConfig().device ONCE and caches it in _DEVICE — MPS on the dev box) ; draw_point_shapes(point_key, *, adapter, adapter_sha256, corpus, corpus_sha256, k, state, dry_run=False) -> (blob, digests) (:458; caches at draws_path(point_key)) ; _draw_one_shape (:508; calls recall.load_adapted_model(device(), adapter) at :524) ; score_point(blob, values) -> (per_question, per_fact, scored) (:586; rows keyed by tier ∈ {GATED_TIER, REPORTED_TIER}) ; READ_ONLY_GIT_ACTIONS = ("ls-files","show","rev-parse","status") (:753)
From scripts/phase25_points.py: scoring_values() (:655) = [fact.value for LOCKED_FACTS + SOFT_TIER_FACTS]
From scripts/phase24_adversarial.py (no torch at import; reads results/phase18_corpus.json + the fixture lazily): TRAINED_TIER = "core_taught" (:180) ; TRAINED_FAMILIES = ("A1-mild", "A1-aggressive", "A3") (:191) ; HELD_OUT_FAMILY = "A2" (:214) ; _adversarial_pool(tok) (:248; questions re-rendered from the FIXTURE text for TRAINED_TIER rows in TRAINED_FAMILIES) ; adversarial_episodes(tok) -> list[(persona_tuple, question, answer)] (:392; 336 rows measured at HEAD)
From scripts/phase26_canary.py (the driver analog): _prove/_utc/_sha256/_rel (:90-106) ; frontier() lru_cache (:128) ; emit order (:475-504, :680-715) ; build_parser/main (:793-828)
From scripts/phase23_run.py: the tracked-set conjunct (:860-875)
From tests/test_phase25_driver.py: _git_argv_subcommands(path), _enclosing_function(tree, node) (:88-131)
Frontier: results/phase25_frontier.json — sha256 1f182b40c9d7c316e57ecd69acd62c7f6407514b9c675bdf8ec677d3f0cb97d5, 22,311,714 bytes, one commit 4030d0e
.gitignore covers checkpoints/, logs/, data/ (:14-17) — every per-arm sidecar AND every Phase-25-named draw cache (data/phase25_phase27_*_draws.json) lands under data/
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the driver's CPU half — imports, _require_admitted, the record schema, admit, parser and DISPATCH</name>
  <files>scripts/phase27_relearn.py</files>
  <read_first>
    - scripts/phase26_canary.py lines 46-131 (imports, INSTRUMENT_GIT_SHA, RECORD/SIDECAR_DIR/PREREG_MODULE, `_prove`, `_utc`, `_sha256`, `_rel`, `frontier()`), 475-504 and 680-716 (`emit` — overwrite refusal FIRST, `refuse_if_dirty` SECOND, blob, `atomic_write_json`, the printed summary), 793-828 (`build_parser`, `main`)
    - scripts/phase23_run.py lines 858-878 (the tracked-set conjunct via `["git", "ls-files", ...]`)
    - scripts/phase27_prereg.py (plan 27-01 output — WHOLE file; the functions listed in `<interfaces>`)
    - scripts/phase25_run.py lines 118-140 (`atomic_write_json`), 175-192 (`DRAWS_DIR`, `draws_path` — the cache naming the apparatus block discloses), 745-760 (`READ_ONLY_GIT_ACTIONS`)
    - scripts/phase24_adversarial.py lines 20-40 (why the corpus has no question text and the fixture is the only place it lives), 180-214 (`TRAINED_TIER`, `TRAINED_FAMILIES`, `HELD_OUT_FAMILY`), 248-300 and 392-400 (`_adversarial_pool`, `adversarial_episodes` — the (persona, question, answer) rows that are set (ii))
    - scripts/phase14_recall.py lines 986-1000 (`RecallItem`) and 1026-1060 (`build_question_sets` — the held-out list is index 1; read `.question`)
    - results/phase16_recall_sample.json (the first 40 lines — `questions` keyed by tier, `counts`)
    - src/personacore/provenance.py lines 28-79 (`git_sha`, `refuse_if_dirty` and its pathspec semantics)
    - results/phase24_token_budget.json `provenance` block and results/phase26_canary.json top-level keys (the record shapes)
    - .planning/phases/27-relearning-attack/27-PATTERNS.md §"scripts/phase27_relearn.py" Analog A and B, §"results/phase27_admission.json"
    - .planning/phases/27-relearning-attack/27-RESEARCH.md §"Open Questions" 1 and 6. RESEARCH Code Example 5's set (ii) is SUPERSEDED — use `adversarial_episodes` rows (see `<objective>` "D-17 set (ii)"); do not re-import Code Example 5's `trained_attack` definition
  </read_first>
  <action>
Create `scripts/phase27_relearn.py`. Module scope imports ONLY: stdlib (`argparse`, `collections`, `dataclasses`, `datetime`, `functools`, `hashlib`, `importlib.metadata`, `json`, `pathlib`, `subprocess`, `sys`), the sibling `sys.path` inserts for `scripts/` AND `src/` (`phase26_canary.py:46-60` verbatim), then `erasure_gate`, `mitigation_gate`, `phase24_adversarial`, `phase25_record`, `phase25_run`, `phase27_prereg` and `from personacore.provenance import git_sha, refuse_if_dirty`. NEVER at module scope: `torch`, `numpy`, `teach_persona`, `phase14_factset`, `phase14_recall`, `phase18_extraction`, `phase25_points`, `personacore.training.loop`, `personacore.lora`, `personacore.checkpoint`, `personacore.config`, `personacore.model`, `personacore.tokenizer` — all imported inside functions (Task 4's AST test enforces the set). Do not write the substring `train_arm(` anywhere in this file (the `_TRAIN_ARM_CALL_SITES` register, `tests/test_phase23_resume.py:60-130`); write "train_arm" without the paren when prose needs it. Never call `mitigation_gate.mitigation_point_verdict` (census at `tests/test_phase20_correction.py:1377`). Read every prereg constant as `phase27_prereg.<NAME>` at call time — no `from phase27_prereg import ...` — so the e2e test's `monkeypatch.setattr(phase27_prereg, ...)` reaches the driver.

Module docstring: the driver's git surface is READ-ONLY (`ls-files` only, `phase25_run.READ_ONLY_GIT_ACTIONS`); the operator commits the record by hand (D-15); `admit` is the phase's one gate call; every leg is gated on the COMMITTED record (D-08); the train path is `TRAIN_PATH` (OQ1-B), disclosed in the record; nothing here schedules anything (D-14 — no plist) and no leg runs on MPS in this phase (D-09) because the record reads MOOT — the legs are exercised only by the CPU wiring proof on a tiny fixture, which pins `phase25_run._DEVICE = "cpu"`; the only device resolver in this module is `phase25_run.device()`.

Declare, with these exact names:

1. `INSTRUMENT_GIT_SHA = git_sha()` (import-time, the `phase26_canary.py:104` idiom); `RECORD = _ROOT / "results" / "phase27_admission.json"`; `STREAM_DIR = _ROOT / "data"`; `BASE_SLIM = _ROOT / "checkpoints" / "convbase_slim.pt"` (spelled here because `phase14_recall` imports torch; Task 4 asserts `== phase14_recall.CONVBASE_SLIM` — the scorers load the base through `phase14_recall.CONVBASE_SLIM`, the driver's `model_from_adapter` through `BASE_SLIM`; the two must name one file); `PINNED_MODULES = ("scripts/phase27_prereg.py", "scripts/phase27_relearn.py", "scripts/mitigation_gate.py", "scripts/erasure_gate.py", "scripts/teach_persona.py", "src/personacore/training/data.py", "src/personacore/training/loop.py")` (D-35's five plus the two train-path files the OQ1-B disclosure names); `ARMS = ("fresh", "control", "mitigated")`; `TRAIN_PATH = "teach_persona.train() called directly with ONE shared TrainConfig and bins from teach_persona.build_arm_bins — the shape of phase23_run.train_never_taught (27-RESEARCH OQ1 option B); teach_persona.train_arm is not called"`; `SUB_MODES = ("admit", "calibrate", "curve", "gate", "structural-proof")`; `DRAW_CACHE = "phase25_run.draws_path(point_label) under phase25_run.DRAWS_DIR — data/phase25_phase27_{leg}_{point_key}_rung{NNNN}_k{K}_draws.json; k is part of the cache identity (phase25_run.load_draws), so the FULL_K re-score never reuses CURVE_K draws"`.
2. `APPARATUS_LEGS` — a tuple of four dicts in the order calibrate, curve, gate, structural-proof, each `{"name", "sub_mode", "refusal_node_id", "e2e_node_id", "train_path": TRAIN_PATH}` with `refusal_node_id = f"tests/test_phase27_relearn.py::test_each_leg_refuses_unless_admitted[{sub_mode}-MOOT]"` and `e2e_node_id = "tests/test_phase27_relearn.py::test_the_live_path_is_wired_end_to_end"` (both node ids are asserted to exist in plan 27-04's collect-only test; Task 4's parametrize ids are spelled `{mode}-{verdict}` to match).
3. Helpers copied from `phase26_canary.py:90-106`: `_prove(condition, message)` → `SystemExit("[phase27_relearn] ...")`, `_utc()`, `_sha256(path)`, `_rel(path)`. Plus `@functools.lru_cache(maxsize=1) def frontier()` reading `phase25_record.FRONTIER_RECORD` once, and an UNCACHED `def admission(record_path=RECORD)` returning the parsed record (tests forge it under `tmp_path`).
4. `def _require_admitted(record_path=RECORD) -> dict` — the FIRST statement of every leg (D-08 + D-12, three conjuncts then the tracked one, `phase23_run.py:860-875` shape): `_prove(path.exists(), f"{_rel(path)} is absent — run `admit` first; every leg is gated on the COMMITTED record")`; `read = blob["verdict"]["verdict"]`; `_prove(read == "ADMITTED", f"{_rel(path)} reads {read!r} — REFUSING to run this leg: nothing is admitted ({first reason})")`; `_prove(blob.get("baselines") == json.loads(json.dumps(phase27_prereg.PINNED_BASELINES)), f"{_rel(path)} carries baselines that differ from phase27_prereg.PINNED_BASELINES — REFUSING: D-12's pins moved after the record was written")` (JSON-normalised on the prereg side so tuples compare as lists; the record's `baselines` field is `build_record`'s copy of the pins, so on the real record and on every forged record built from the pins this is an equality); then, ONLY when `path.is_relative_to(_ROOT)`, the tracked conjunct `subprocess.run(["git", "ls-files", _rel(path)], cwd=_ROOT, capture_output=True, text=True, check=True).stdout.split()` and `_prove(tracked, f"{_rel(path)} is not tracked — REFUSING: a leg runs only against the COMMITTED record, never a live re-read")`. This is the driver's ONLY git argv (T-27-08; Task 4's git-surface test allows exactly `READ_ONLY_GIT_ACTIONS`).
5. `def disjointness_report() -> dict` (D-17, computed at admit; lazy imports of `teach_persona as tp`, `phase14_factset as fs`, `phase14_recall as pr`, `phase18_extraction as x18`, `from personacore.tokenizer import from_json`; `phase24_adversarial` is already a module-scope import). The comparison unit on EVERY side is the QUESTION STRING — `build_corpus` entries carry `prompt_ids` and no text (`phase24_adversarial.py:28-32`; entry keys listed in `<interfaces>`), and the fixture is the only place the text lives; say this in the docstring. `tok = from_json(tp.TOKENIZER_PATH)`; `fixture = json.loads(x18.CORPUS_SOURCE_FIXTURE.read_text(encoding="utf-8"))`; `corpus = x18.build_corpus(tok)`; `gated_prompts = [e for e in corpus["prompts"] if e["tier"] == phase27_prereg.GATED_TIER]` (416 entries = 104 questions × 4 attack families, measured); `scored_gated = {row["question"] for row in fixture["questions"][phase27_prereg.GATED_TIER]}` (104 unique strings, measured); `scored_heldout = {item.question for item in pr.build_question_sets(fs.LOCKED_FACTS)[1]}` (the held-out `RecallItem` list — families `fs.HELDOUT_FAMILY_IDS`, read never spelled); `teaching = {q for q, _a in tp.render_episodes(fs.LOCKED_FACTS, fs.TAUGHT_FAMILY_IDS)}`; `trained_attack = {q for _persona, q, _a in phase24_adversarial.adversarial_episodes(tok)}` — set (ii) is the adversarial ARM's trained rows (the pool `teach_persona.build_bins` trains from; `TRAINED_TIER` rows in `TRAINED_FAMILIES`, A2-free by construction), NOT the scored corpus's own A1/A3 entries (RESEARCH Code Example 5 — superseded; those intersect the gated set in 312 prompts by construction); `attacker = {q for q, _a in phase27_prereg.attacker_corpus_rows()}`; `_prove(phase27_prereg.HELD_OUT_FAMILY not in phase27_prereg.TRAINED_FAMILIES, ...)` (the family assertion). With `scored = scored_gated | scored_heldout` (the two halves of the fixture share the held-out questions — that union is the scored set, not an overlap D-17 counts), return `{"unit": "question strings — the corpus carries prompt_ids, not text; results/phase16_recall_sample.json is the only place the text lives", "scored": {"gated_prompts": len(gated_prompts), "gated_questions": len(scored_gated), "heldout_recall": len(scored_heldout)}, "overlaps": {"teaching": len(scored & teaching), "trained_attack": len(scored & trained_attack), "attacker_corpus": len(scored & attacker)}, "checked": len(scored), "held_out_family": phase27_prereg.HELD_OUT_FAMILY, "trained_families": list(phase27_prereg.TRAINED_FAMILIES), "trained_attack_rows": <len of the adversarial_episodes list>}`. Measured at HEAD this session on the real fact set: `gated_prompts 416, gated_questions 104, overlaps 0 / 0 / 0, trained_attack_rows 336`. `admit` `_prove`s every overlap `== 0` (a non-zero overlap is a refused admission, not a field).
6. `def build_record(frontier_blob) -> dict` (pure over the frontier + prereg; no torch): `verdict, reasons = phase27_prereg.relearning_is_worth_attempting(frontier_blob)`; rows = for each key in `point_keys` order: `{"point_key", "arm": phase27_prereg.arm_of(key), "leg": phase27_prereg.leg_of(key), "verdict": phase27_prereg.point_verdict_string(point), "cleared_a", "cleared_b", "cleared_c"` from `phase27_prereg.cleared_abc(point["verdict"])`, `"refused": <verdict string == phase27_prereg.REFUSED>}` (OQ6: nulls on the six, no reason text); `counts = phase27_prereg.cleared_counts(frontier_blob)`; `_prove` at the write that `collections.Counter(row verdicts) == frontier verdicts.tallies`, that per-leg counters equal `tallies_by_leg`, that `sum(cleared_a) == counts["a"]` (same for b, c), and that `len(rows) == phase27_prereg.EXPECTED_POINTS` (D-33). `x = phase27_prereg.extraction_ceiling_x(frontier_blob)`; `tolerated, fraction, sentence = mitigation_gate.tolerance_report(ceiling=x, n_questions=<the n8 control point's point_extraction_questions>)`. Return the blob with EXACTLY these top-level keys: `governs` (one sentence quoting `phase27_prereg.COMMITTED` and `RECORDS_AT_COMMIT`), `verdict: {verdict, reasons}`, `admitted_point_keys` (list from `phase27_prereg.admitted_point_keys`), `tallies`, `tallies_by_leg`, `cleared_counts` (a, b, c, reached, refused, by_leg), `rows`, `frontier_path`, `frontier_sha256`, `frontier_bytes`, `x: {value, n_questions, tolerated, fraction, tolerance_sentence}`, `recall_thresholds: {leg: {threshold, k, n}}` over `phase27_prereg.LEGS` (from `recall_threshold`), `baselines: phase27_prereg.PINNED_BASELINES`, `fresh_seeds`, `designated_seed`, `attacker_corpus: dict(phase27_prereg.ATTACKER_CORPUS)`, `budget: {max_steps, checkpoint_interval, relearn_cap, rungs, curve_k, full_k, margin_k, f_y}` (all read from prereg), `apparatus: {status, reason, legs: list(APPARATUS_LEGS), train_path: TRAIN_PATH, fresh_curve_disclosure: phase27_prereg.FRESH_CURVE_DISCLOSURE, device_policy: "never run on MPS in this phase (D-09); the only resolver is phase25_run.device(), pinned to cpu by the wiring proof", draw_cache: DRAW_CACHE}` where `status = phase27_prereg.NOT_EXERCISED` and `reason = f"gate read {verdict}"` unless `verdict == "ADMITTED"` (then `status = "pending"`, `reason = "gate read ADMITTED — legs run against the committed record"`), `disjointness: None` (filled by `admit`), `provenance: None` (filled by `admit`).
7. `def admit(out_path=RECORD, *, overwrite=False) -> dict` in THIS order (T-27-07, `phase26_canary.py:475-504`): (1) `_prove(overwrite or not out_path.exists(), f"{_rel(out_path)} exists — REFUSING to overwrite it. The sanctioned route deletes it in its own commit, then re-runs against a clean tree. Pass --force to overwrite deliberately.")` (the `teach_persona.refuse_if_exists` :410 semantics reproduced in four lines — that module imports torch); (2) `refuse_if_dirty(who="phase27_relearn", detail=<what a dirty tree would falsify>, pathspec=("scripts", "src", "results", f":(exclude){_rel(out_path)}") when `out_path` is inside `_ROOT` else without the exclude, cwd=_ROOT)`; (3) `blob = build_record(frontier())`; (4) `blob["disjointness"] = disjointness_report()` and `_prove` all three overlaps are 0; (5) `blob["provenance"] = {"module_sha256": {rel: _sha256(_ROOT / rel) for rel in PINNED_MODULES}, "git_sha": INSTRUMENT_GIT_SHA, "head_at_write": git_sha(), "written_utc": _utc(), "torch_version": importlib.metadata.version("torch"), "prereg_committed": phase27_prereg.COMMITTED}`; (6) `phase25_run.atomic_write_json(out_path, blob)`; print ONE line `[phase27_relearn] admitted? {verdict}: {reasons[0]} — wrote {_rel(out_path)}; cleared (a) {a} / (b) {b} / (c) {c}`; return blob. `admit` never builds a git argv.
8. `def build_parser()`: `sub = parser.add_subparsers(dest="mode", required=True)`; `admit` takes `--force` and `--out` (default `str(RECORD)`); each of `calibrate`, `curve`, `gate`, `structural-proof` takes `--record` (default `str(RECORD)`), `--leg` (`choices=phase27_prereg.LEGS`, required), `--out-dir` (default `str(STREAM_DIR)`); `gate` additionally takes `--baseline` (`choices=phase27_prereg.BASELINE_KEYS`, REQUIRED — the CLI cannot be invoked without it, RELRN-01 / D-09).
9. `DISPATCH = {"admit": admit, "calibrate": run_calibrate, "curve": run_curve, "gate": run_gate, "structural-proof": run_structural_proof}` (module-level dict placed after the leg functions) and `def main(argv=None) -> int`: parse; build `kwargs` per mode with EXPLICIT keyword names — `admit(out_path=pathlib.Path(args.out), overwrite=args.force)`; legs `(record=pathlib.Path(args.record), leg=args.leg, out_dir=pathlib.Path(args.out_dir))` plus `baseline=args.baseline` for `gate` — and call `DISPATCH[args.mode](**kwargs)`; return 0. `if __name__ == "__main__": raise SystemExit(main())`.

Until Task 3 fills them, define the four `run_*` functions with the Task 3 signatures whose FIRST statement is `blob = _require_admitted(record)` followed by `raise NotImplementedError("plan 27-03 Task 3")` — this is the natural intermediate state Task 4's refusal tests are watched against (see Task 4).

Threat references: T-27-06 (no `torch.load` by name — the CPU half loads nothing), T-27-07 (`refuse_if_dirty` before any digest is computed), T-27-08 (read-only git surface), T-27-12 (seven module digests from bytes at the write).
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/python -c "import sys; sys.path.insert(0,'scripts'); sys.path.insert(0,'src'); import phase27_relearn as r; assert 'torch' not in sys.modules and 'teach_persona' not in sys.modules, 'torch at import'; b=r.build_record(r.frontier()); assert b['verdict']['verdict']=='MOOT'; assert len(b['rows'])==44 and sum(1 for x in b['rows'] if x['refused'])==6; assert b['cleared_counts']['a']==30 and b['cleared_counts']['b']==4 and b['cleared_counts']['c']==1; assert [l['sub_mode'] for l in b['apparatus']['legs']]==['calibrate','curve','gate','structural-proof']; assert all(l['train_path']==r.TRAIN_PATH for l in b['apparatus']['legs']); assert b['apparatus']['status']=='not exercised'; assert 'phase25_phase27_' in b['apparatus']['draw_cache']; assert set(r.DISPATCH)==set(r.SUB_MODES); src=open('scripts/phase27_relearn.py').read(); assert 'adversarial_episodes(' in src; print('OK')" && .venv/bin/ruff check scripts/phase27_relearn.py && .venv/bin/ruff format --check scripts/phase27_relearn.py</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -c "import sys; sys.path.insert(0,'scripts'); sys.path.insert(0,'src'); import phase27_relearn; assert 'torch' not in sys.modules and 'teach_persona' not in sys.modules and 'phase18_extraction' not in sys.modules"` exits 0
    - `grep -c "train_arm(" scripts/phase27_relearn.py` prints 0; `grep -c "mitigation_point_verdict" scripts/phase27_relearn.py` prints 0; `grep -c "from phase27_prereg import" scripts/phase27_relearn.py` prints 0; `grep -v "^\s*#" scripts/phase27_relearn.py | grep -c "adversarial_episodes("` ≥ 1
    - `build_record(frontier())` returns `verdict.verdict == "MOOT"`, 44 rows in `point_keys` order with exactly 6 `refused: true` rows whose `cleared_a/b/c` are `None` and `verdict == "REFUSED"`, `cleared_counts == {"a": 30, "b": 4, "c": 1, "reached": 38, "refused": 6, "by_leg": {...}}`, `x.value == frontier["verdicts"]["extraction_ceiling"]["X"]` (equal on the REAL frontier because the summary field was computed by the same call), `x.tolerated == 0`, `recall_thresholds.n8.k == 790` and `.n64.k == 87`, `apparatus.legs` of length 4 each with the five keys and `train_path == TRAIN_PATH`, `apparatus.status == "not exercised"`, `apparatus.reason == "gate read MOOT"`, `apparatus.draw_cache == DRAW_CACHE`
    - `admit(tmp_path / "probe.json")` (with `refuse_if_dirty` monkeypatched to a recorder) writes a file whose JSON has all top-level keys listed in item 6 plus filled `disjointness` (three overlaps all 0; `scored.gated_prompts == 416`; `scored.gated_questions == 104`; `checked >= 104`; `unit` names "question strings") and `provenance.module_sha256` with exactly the seven `PINNED_MODULES` keys, each equal to `hashlib.sha256(bytes).hexdigest()` of the live file; the recorder saw `pathspec == ("scripts", "src", "results")` (no exclude for a tmp path) and `who == "phase27_relearn"`
    - `_require_admitted(tmp_path / "absent.json")` raises `SystemExit` containing `"absent"`; a tmp record with `verdict.verdict == "MOOT"` raises `SystemExit` containing `"MOOT"` and `"REFUSING"`; a tmp record reading `"ADMITTED"` whose `baselines` are the pins returns the blob without running git (the path is outside `_ROOT`); a tmp record reading `"ADMITTED"` with one pin's `sha256` altered raises `SystemExit` containing `"baselines"` and `"REFUSING"`
    - `build_parser().parse_args(["gate", "--leg", "n8"])` raises `SystemExit` (missing required `--baseline`); `parse_args(["gate", "--leg", "n8", "--baseline", "made_up"])` raises `SystemExit` (not a `BASELINE_KEYS` choice)
    - `ruff check` / `ruff format --check` clean; the one-liner verify prints OK
  </acceptance_criteria>
  <done>The CPU half exists: `admit` writes a complete record to a tmp path, every leg name refuses through `_require_admitted`, `DISPATCH` and `main` are wired; NOT yet committed (Task 3 completes the module; commit at the end of Task 3).</done>
</task>

<task type="auto">
  <name>Task 2: Write the train/score helpers — make_recorder (tag by bin identity), shared_train_config, model_from_adapter, train_relearn_arm (train() direct, rung chain, driver-built RuntimeConfig), score_rung (k-suffixed cache label)</name>
  <files>scripts/phase27_relearn.py</files>
  <read_first>
    - scripts/phase27_relearn.py (Task 1 state)
    - scripts/phase23_run.py lines 612-699 (`train_never_taught` — the model build order LOAD BEFORE INJECT, the `_prove`s on `n_wrapped` and `trainable`, the `tp.train(...)` call, the moved/frozen canary after training) — READ ONLY
    - scripts/teach_persona.py lines 95-105 (`DIALOG_TRAIN_BIN` = `data/dialog_train.bin`), 182-200 (`replay_window_budget`), 344-360 (`arm_outputs` — the teaching bin is `data/persona_{arm}_train.bin`, NO phase prefix), 1279-1300 (`build_arm_bins` signature and what it writes), 1960-1975 (the replay kwargs shape), 2440-2470 (`score_arm`: `pr.load_adapted_model(device, adapter_path=adapter_path)` at :2459 — the base comes from `phase14_recall.CONVBASE_SLIM`) and 2470-2520 (its return dict)
    - scripts/phase14_recall.py lines 81 (`CONVBASE_SLIM`) and 712-745 (`load_adapted_model` — reads `CONVBASE_SLIM` at CALL time)
    - scripts/phase25_recall.py lines 114-185 (adapter sha refusal before scoring :124-128; reading `score_arm` counts :159-175)
    - scripts/phase25_run.py lines 175-250 (`DRAWS_DIR`, `draws_path`, `load_draws` and its THREE-field identity incl. `k` :216-230, `write_draws`), 404-437 (`_DEVICE`, `device()` — cached, resolves MPS on the dev box), 458-585 (`draw_point_shapes`, `_draw_one_shape` — `recall.load_adapted_model(device(), adapter)` at :524), 586-640 (`score_point`); scripts/phase25_points.py lines 650-665 (`scoring_values`)
    - scripts/phase18_extraction.py lines 822-900 (`build_corpus` — the fixture rows, the per-tier count check, the entry keys)
    - src/personacore/config.py lines 40-60 (`RuntimeConfig(device=...)` — the driver builds this directly)
    - src/personacore/checkpoint.py lines 156-200 (`load_checkpoint` — RESTORES RNG state), 205-236 (`export_slim` — the key the base fingerprint is stored under), 237-300 (`load_slim`, `export_adapter`, `load_adapter`)
    - src/personacore/lora/inject.py lines 60-100 (`lora_state_dict`, `load_adapter_weights`)
    - src/personacore/training/loop.py lines 235-275 (the `train()` signature incl. `on_draw`) and 790-810 (`target_steps`)
    - tests/test_phase27_on_draw.py (plan 27-02 — the recorder shape `_recorder()` the driver's `make_recorder` mirrors, EXCEPT the tag rule: identity, not suffix)
    - tests/test_phase22_wiring.py lines 780-783 (the `tp.RuntimeConfig` ZERO-ARG lambda the e2e helper copies — why the driver must not call `tp.RuntimeConfig(device=...)`)
    - .planning/phases/27-relearning-attack/27-PATTERNS.md §"scripts/phase27_relearn.py" Analog C and D, §"No Analog Found"
    - .planning/phases/27-relearning-attack/27-RESEARCH.md §"Code Examples" 4, §"Open Questions" 1–3, §"Common Pitfalls" 5, 7, 9
  </read_first>
  <action>
Append to `scripts/phase27_relearn.py` (the Task 1 `NotImplementedError` stubs STAY — Task 3 replaces them). Every function below does its torch-touching imports INSIDE its body (`import teach_persona as tp`, `import phase14_factset as fs`, `import phase14_recall as pr`, `import phase18_extraction as x18`, `import phase25_points`, `import numpy as np`, `from personacore import checkpoint as ckpt_mod`, `from personacore.config import RuntimeConfig`, `from personacore.lora import lora_state_dict, load_adapter_weights`, `from personacore.tokenizer import from_json`). Never `torch.load` by name; `tp.torch` is NOT to be used for loading either — every load is `ckpt_mod.load_slim` / `ckpt_mod.load_adapter` / `ckpt_mod.load_checkpoint`.

1. `def make_recorder(stream_path, *, teaching_bin) -> (callback, state)` (D-29/D-30, the 27-02 `_recorder` shape): `state = {"sha": hashlib.sha256(), "draws": 0, "path": stream_path, "teaching_bin": pathlib.Path(teaching_bin).resolve()}`; `callback(bin_path, ix)` writes `ix.astype("<u8").tobytes()` prefixed by ONE tag byte — `0` when `pathlib.Path(bin_path).resolve() == state["teaching_bin"]` (IDENTITY against `paths["bin"]` captured by the caller), else `1` (the replay bin) — to an open append-mode file AND updates the sha with the same bytes; `state["draws"] += 1`. NEVER tag by name suffix: the teaching bin `data/persona_{arm}_train.bin` (`arm_outputs`, `teach_persona.py:344`) and the replay bin `data/dialog_train.bin` (`DIALOG_TRAIN_BIN`, `:99`) BOTH end in `_train.bin`, so `name.endswith("_train.bin")` would tag every draw `0` and make teaching and replay draws indistinguishable (D-30); write that sentence in the docstring and do not write the substring `endswith(` in this function. `def stream_digest(state) -> str`. The file is `out_dir / f"phase27_{leg}_{arm}_seed{seed}_offsets.bin"`; the digest is D-26(iii)'s proof and the bytes let a differing digest be located by offset (D-30).
2. `def shared_train_config() -> TrainConfig` (D-26(i)): `tp.TrainConfig(lr=tp.LR, warmup_steps=tp.WARMUP_STEPS, max_steps=phase27_prereg.RELEARN_CAP, batch_size=tp.BATCH_SIZE, weight_decay=tp.WEIGHT_DECAY, seed=phase27_prereg.DESIGNATED_SEED)` — every budget symbol imported, never retyped (`phase23_run.py:661-668`). Each leg builds this ONCE and passes the same object to every arm at the designated seed; the extra fresh seeds get `dataclasses.replace(cfg, seed=s)` (the D-26×D-27 resolution in `<objective>`).
3. `def model_from_adapter(start_adapter, *, expected_sha256, device)` (D-22, T-27-06, T-27-11): `_prove(_sha256(start_adapter) == expected_sha256, f"{_rel(start_adapter)} hashes to ... not the pinned ...; REFUSING to attack the wrong weights")` FIRST (`phase25_recall.py:124-128`); `blob = ckpt_mod.load_slim(BASE_SLIM)`; `model_cfg = tp.ModelConfig(**blob["model_config"])`; `model = tp.GPT(model_cfg)`; `model.load_state_dict(blob["model"])` (LOAD BEFORE INJECT); `n_wrapped = tp.inject_lora(model, tp.LORA_CFG)`; `_prove(n_wrapped == 6 * model_cfg.n_layer)`; `load_adapter_weights(model, ckpt_mod.load_adapter(start_adapter))`; `tp.mark_only_lora_trainable(model)`; `model.to(device)`; return `(model, model_cfg, <the base fingerprint read from the slim blob — read the key name from checkpoint.py:205-236 and name it in a comment>)`.
4. `def train_relearn_arm(*, arm, leg, seed, cfg, start_adapter, expected_sha256, out_dir, device) -> dict` (D-13/OQ1-B, D-20, D-25/OQ2, D-26, D-29): `_prove(arm in ARMS)`; `tok, stats, paths = tp.build_arm_bins(f"{phase27_prereg.ATTACKER_ARM}_{leg}_{arm}_seed{seed}", fs.LOCKED_FACTS, fs.TAUGHT_FAMILY_IDS, replay_ratio=phase27_prereg.ATTACKER_REPLAY_RATIO, seed=seed, prefix=phase27_prereg.ATTACKER_PREFIX)` (the pre-registered attacker corpus — D-18; the bins land under `tp._REPO_ROOT / "data"`, which the e2e test redirects); `_prove(_sha256(paths["bin"]) == phase27_prereg.ATTACKER_CORPUS["bin_sha256"], ...)` UNLESS `fs.LOCKED_FACTS` is not the committed set (compare `len` and ids; the tiny fixture's 2 facts skip this pin and record `corpus_pin_checked: false` — the real run checks it); Create the three write parents now, right after `build_arm_bins` returns `paths` — before `make_recorder(` (its callback appends under `out_dir`) and so before the first `tp.train(`, as the cited analog does (`phase23_run.py:656-657`): `paths["csv"].parent.mkdir(parents=True, exist_ok=True)`, `paths["checkpoint"].parent.mkdir(parents=True, exist_ok=True)` and `out_dir.mkdir(parents=True, exist_ok=True)` — `CSVLogger` plain-`open`s its path (`src/personacore/logging.py:29`), nothing creates `results/{prefix}_{arm}/`, and the recorder appends under `out_dir`, so without them the first live call raises `FileNotFoundError`. `tp.seed_everything(seed)`; `model, model_cfg, fingerprint = model_from_adapter(...)`; `before = tp.snapshot_params(model)`; `recorder, rstate = make_recorder(out_dir / f"phase27_{leg}_{arm}_seed{seed}_offsets.bin", teaching_bin=paths["bin"])`; `mask_ones = int(np.fromfile(paths["mask"], dtype=np.uint8).sum())` (D-23's count, `phase24` rule); `runtime = RuntimeConfig(device=device)` — built from `personacore.config` DIRECTLY, never `tp.RuntimeConfig(...)`: the Phase-22 e2e helper (`tests/test_phase22_wiring.py:783`) rebinds `tp.RuntimeConfig` to a ZERO-ARG lambda, so `tp.RuntimeConfig(device=device)` raises `TypeError` under any test that copies that helper; say so in a comment. Then the RUNG CHAIN: for `rung in phase27_prereg.RUNGS`: `final = tp.train(train_config=cfg, runtime_config=runtime, model=model, model_config=model_cfg, train_bin=paths["bin"], train_mask_bin=paths["mask"], val_bin=tp.DIALOG_VAL_BIN, val_mask_bin=tp.DIALOG_VAL_MASK, replay_bin=tp.DIALOG_TRAIN_BIN, replay_mask_bin=tp.DIALOG_TRAIN_MASK, replay_windows=tp.replay_window_budget(stats["n_facts"]) // tp.BLOCK_SIZE, penalty_fn=None, log_path=paths["csv"], eval_interval=tp.EVAL_INTERVAL, checkpoint_path=paths["checkpoint"], checkpoint_interval=phase27_prereg.CHECKPOINT_INTERVAL, resume_from=paths["checkpoint"] if rung > phase27_prereg.RUNGS[0] else None, max_steps_override=rung, return_final_loss=True, on_draw=recorder)`; `_prove(math.isfinite(float(final)))`; `rung_adapter = out_dir / f"phase27_{leg}_{arm}_seed{seed}_rung{rung:04d}_adapter.pt"`; `ckpt_mod.export_adapter(rung_adapter, adapter=lora_state_dict(model), lora_config=dataclasses.asdict(tp.LORA_CFG), base_fingerprint=fingerprint)`; append `{"steps": rung, "scored_tokens": phase27_prereg.scored_tokens(mask_ones=mask_ones, steps=rung), "adapter_path": _rel(rung_adapter), "adapter_sha256": _sha256(rung_adapter)}` to `rungs`. After the chain: the moved/frozen canary from `phase23_run.py:685-699` (every trainable moved, every frozen param equal to `before`); then `ck = ckpt_mod.load_checkpoint(paths["checkpoint"], model=model)` — a READ of the checkpoint `train()` just wrote (its RNG restore is benign after training; say so in a comment) — and `checkpoint_train_config = ck["train_config"]`. Write `out_dir / f"phase27_{leg}_{arm}_seed{seed}_readings.json"` via `phase25_run.atomic_write_json` with `{arm, leg, seed, device, start_adapter: _rel, start_sha256, train_config: dataclasses.asdict(cfg), checkpoint_train_config, bin_path: _rel(paths["bin"]), bin_sha256, mask_ones, n_facts: stats["n_facts"], replay_windows, rungs, offset_stream: {path: _rel, sha256: stream_digest(rstate), draws: rstate["draws"], teaching_bin: _rel(paths["bin"])}, train_path: TRAIN_PATH, corpus_pin_checked}` (D-26(ii)'s off-disk record; `device` is the string the leg resolved — the e2e asserts `"cpu"`) and return it.
5. `def score_rung(*, point_label, adapter_path, k, out_dir, device, facts, values) -> dict` (D-16, D-21): recall REPORTED via `tp.score_arm(point_label, facts, adapter_path, device)` read as `phase25_recall.py:159-175` reads it (`taught_recall: {numerator, denominator}`, `heldout_recall: {...}`); extraction DECIDES via `tok = from_json(tp.TOKENIZER_PATH)` (the signature carries no tokenizer — bind it here, as `disjointness_report` does) and `corpus = x18.build_corpus(tok)`, `blob, digests = phase25_run.draw_point_shapes(point_label, adapter=adapter_path, adapter_sha256=_sha256(adapter_path), corpus=corpus, corpus_sha256=x18.corpus_sha256(corpus), k=k, state={})` and `per_question, per_fact, scored = phase25_run.score_point(blob, values)`; `extraction = {"successes": <count of successes over per_question[GATED_TIER]>, "questions": len(per_question[GATED_TIER])}` — read the per-question success field name from `phase25_run.py:600-625` and name it in a comment. THE CACHE LABEL: `draw_point_shapes` caches at `phase25_run.draws_path(point_label)` = `phase25_run.DRAWS_DIR / f"phase25_{point_label}_draws.json"` (`:175-192`) and `load_draws` REFUSES a cache whose recorded `k` differs from the call's (`:216-230`), so the callers (Task 3) pass labels of the form `f"phase27_{leg}_{point_key}_rung{rung:04d}_k{k}"` — `k` IN the label — and `score_rung` `_prove`s `point_label.endswith(f"_k{k}")` so a CURVE_K and a FULL_K reading of the same rung never share a file. Under the real tree those caches are `data/phase25_phase27_*_draws.json` (Phase 25's naming, gitignored); the e2e redirects them by monkeypatching `phase25_run.DRAWS_DIR`, and every stray check in plans 27-04/27-05 is `find data -maxdepth 1` over `phase27_*`, `phase25_phase27_*` and `persona_relearn_attacker_*` (the attacker bins carry no phase prefix; `find`, not a multi-glob `ls`, because zsh aborts the whole `ls` when any one pattern is empty and the guard goes blind). The label charset (`phase25_prereg.point_record_path`: alphanumerics, `-`, `_`) admits this form — measured this session: `draws_path("phase27_n8_dp_n8_sigma0p500000_rung0001_k2")` resolves. BASE FOR SCORING: both scorers load the base through `phase14_recall.CONVBASE_SLIM` (`tp.score_arm` → `pr.load_adapted_model` at `teach_persona.py:2459`; `_draw_one_shape` → `recall.load_adapted_model(device(), adapter)` at `phase25_run.py:524`), NOT through `BASE_SLIM`; and `_draw_one_shape` resolves its device from `phase25_run.device()`, not from the `device` argument — the docstring says both, so a reader knows which two names a fixture must redirect. Return `{"k": k, "point_label": point_label, "draws_cache": _rel(phase25_run.draws_path(point_label)), "taught_recall", "heldout_recall", "extraction"}` — counts only, never rates.

Threat references: T-27-04 (real train path, not stubs), T-27-06 (no `torch.load` by name), T-27-11 (adapter sha refused before any training or scoring), T-27-13 (sidecars and draw caches under gitignored `data/`).
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/python -c "import inspect, sys; sys.path.insert(0,'scripts'); sys.path.insert(0,'src'); import phase27_relearn as r; assert 'torch' not in sys.modules; src=open('scripts/phase27_relearn.py').read(); assert 'NotImplementedError(\"plan 27-03 Task 3\")' in src, 'leg stubs must still be in place'; p=inspect.signature(r.make_recorder).parameters; assert p['teaching_bin'].kind is inspect.Parameter.KEYWORD_ONLY; s=inspect.getsource(r.make_recorder); assert 'endswith(' not in s and 'resolve()' in s; assert set(inspect.signature(r.train_relearn_arm).parameters)=={'arm','leg','seed','cfg','start_adapter','expected_sha256','out_dir','device'}; t=inspect.getsource(r.train_relearn_arm); assert 'RuntimeConfig(device=device)' in t and 'tp.RuntimeConfig' not in t and 'on_draw=recorder' in t and t.count('mkdir(parents=True, exist_ok=True)')>=3 and 'max_steps_override=rung' in t and 'teaching_bin=paths[\"bin\"]' in t; assert set(inspect.signature(r.score_rung).parameters)=={'point_label','adapter_path','k','out_dir','device','facts','values'}; u=inspect.getsource(r.score_rung); assert 'draw_point_shapes(' in u and '_k{k}' in u and 'from_json(tp.TOKENIZER_PATH)' in u; assert not inspect.signature(r.shared_train_config).parameters; print('OK')" && .venv/bin/ruff check scripts/phase27_relearn.py && .venv/bin/ruff format --check scripts/phase27_relearn.py</automated>
  </verify>
  <acceptance_criteria>
    - `inspect.signature(shared_train_config)` has no parameters; `inspect.getsource(shared_train_config)` contains `tp.LR`, `tp.WARMUP_STEPS`, `tp.BATCH_SIZE`, `tp.WEIGHT_DECAY`, `phase27_prereg.RELEARN_CAP`, `phase27_prereg.DESIGNATED_SEED` and no numeric literal other than `0`/`1` (budget imported, never retyped)
    - `inspect.getsource(make_recorder)` contains `resolve()` and `teaching_bin` and does not contain `endswith(`; a recorder built with `teaching_bin=tmp/"phase27_x_train.bin"` tags a draw from that path `0` and a draw from `tmp/"dialog_train.bin"` `1` (a two-line check in the SUMMARY)
    - `inspect.getsource(train_relearn_arm)` contains `tp.train(`, `RuntimeConfig(device=device)`, `max_steps_override=rung`, `resume_from=`, `on_draw=recorder`, `tp.build_arm_bins(`, `replay_window_budget(`, `export_adapter(`, `"device"` and three `mkdir(parents=True, exist_ok=True)` calls (csv parent, checkpoint parent, `out_dir`) before `make_recorder(`; it does not contain `train_arm(` or `tp.RuntimeConfig`
    - `inspect.getsource(score_rung)` contains `draw_point_shapes(`, `score_point(`, `score_arm(`, `from_json(tp.TOKENIZER_PATH)` and `_k{k}`; the docstring names `phase14_recall.CONVBASE_SLIM` and `phase25_run.device()` as the two names a fixture must redirect
    - `grep -c "torch.load" scripts/phase27_relearn.py` prints 0; `grep -c "load_slim\|load_adapter(\|load_checkpoint(" scripts/phase27_relearn.py` ≥ 3; `grep -c "from personacore.config import RuntimeConfig" scripts/phase27_relearn.py` ≥ 1 and that line is inside a function (not at module scope — the import test in Task 4)
    - The four `run_*` stubs from Task 1 are untouched (`NotImplementedError("plan 27-03 Task 3")` still present); the one-liner verify prints OK; `ruff` clean
  </acceptance_criteria>
  <done>The five helpers exist on the real train path with the shared config, the driver-built `RuntimeConfig`, the rung chain, the identity-tagged recorder and the k-suffixed cache label; the legs are still stubs; nothing committed yet (Task 3 commits the driver).</done>
</task>

<task type="auto">
  <name>Task 3: Write the four legs — run_calibrate / run_curve / run_gate (X by call, FULL_K under its own cache label) / run_structural_proof — and commit the driver</name>
  <files>scripts/phase27_relearn.py</files>
  <read_first>
    - scripts/phase27_relearn.py (Task 2 state — the helper signatures)
    - scripts/phase27_prereg.py `recall_threshold`, `first_clear`, `z_rule`, `band`, `recovery_gate`, `promote_at_z`, `extraction_ceiling_x`, `admitted_point_keys` (plan 27-01 output)
    - scripts/mitigation_gate.py lines 352-370 (`extraction_ceiling` — X is `wilson_upper_bound(control) + MARGIN_K × floor`, read BY CALL) and 963-1008 (`promote_to_full_fidelity` promotes PASS or the gate-candidate INCONCLUSIVE only; `ratchet_k` inside it)
    - scripts/phase25_run.py lines 195-232 (`load_draws` — the `k` identity that makes a second cache label necessary) and 404-437 (`device()`)
    - .planning/phases/27-relearning-attack/27-PATTERNS.md §"scripts/phase27_relearn.py" Analog C and D
    - .planning/phases/27-relearning-attack/27-RESEARCH.md §"Open Questions" 1–3, §"Common Pitfalls" 5, 7, 9
  </read_first>
  <action>
Replace the four Task 1 stubs in `scripts/phase27_relearn.py` (torch-touching imports inside each body, as in Task 2):

6. `def run_calibrate(*, record, leg, out_dir) -> dict` (D-24, D-25, D-27, D-28, SC2): `blob = _require_admitted(record)` FIRST; `device = phase25_run.device()` (the driver's ONLY device resolver — cached `_DEVICE`, `phase25_run.py:404-437`; it resolves MPS on the dev box, which is why no leg may proceed past `_require_admitted` in this phase (D-09) and why the e2e pins `phase25_run._DEVICE = "cpu"` — comment this); `cfg = shared_train_config()`; `threshold, k, n = phase27_prereg.recall_threshold(frontier(), leg)`; for the FRESH arm over `phase27_prereg.FRESH_SEEDS` (cfg for `FRESH_SEEDS[POOLED_SEED_INDEX]` is `cfg` itself, the others `dataclasses.replace(cfg, seed=s)`): start adapter = `blob["baselines"][f"never_taught_{s}"]` (path + sha256 read from the RECORD, which copied the prereg pins), `train_relearn_arm(...)`, then `score_rung(point_label=f"phase27_{leg}_fresh_seed{s}_rung{rung:04d}_k{phase27_prereg.CURVE_K}", ..., k=phase27_prereg.CURVE_K, ...)` for each rung → `(steps, taught_k, taught_n)` triples; for the CONTROL arm at the designated seed: start adapter = `blob["baselines"][f"control_{leg}"]`, labels `phase27_{leg}_control_seed{seed}_rung{rung:04d}_k{CURVE_K}`. `fresh_first = phase27_prereg.first_clear(pooled fresh triples, threshold)`; `control_first = phase27_prereg.first_clear(control triples, threshold)`; `z, z_reasons = phase27_prereg.z_rule(fresh_first_clear=fresh_first, control_first_clear=control_first)`. Write `out_dir / f"phase27_{leg}_calibration.json"`: `{leg, threshold: {value: threshold, k, n, f_y}, fresh: {per_seed: {seed: rung readings}, pooled_seed, first_clear}, control: {readings, first_clear}, z, z_reasons, relearn_cap, rungs, curve_k, fresh_readings_at_each_rung: {rung: [taught rate per seed]} (the D-19 noise-floor inputs), train_path: TRAIN_PATH, device}`; print `[phase27_relearn] calibrate {leg}: Z = {z} steps ({z_reasons[0]})`; return it.
7. `def run_curve(*, record, leg, out_dir) -> dict` (D-19, D-21, D-22, D-23, SC3): `_require_admitted` FIRST; `_prove((out_dir / calibration file).exists(), "run calibrate first")` and read Z; `device = phase25_run.device()`; `cfg = shared_train_config()`; for EVERY key in `blob["admitted_point_keys"]` whose `phase27_prereg.leg_of(key) == leg`, in that order (D-22 — never a subset): the mitigated start adapter is `frontier()["points"][key]["adapter_path"]` with `adapter_sha256` from the same entry (refused on mismatch inside `model_from_adapter`); `train_relearn_arm(arm="mitigated", ...)` at the designated seed; `score_rung(point_label=f"phase27_{leg}_{key}_rung{rung:04d}_k{phase27_prereg.CURVE_K}", k=phase27_prereg.CURVE_K, ...)` per rung; per rung the band: `phase27_prereg.band(mitigated=<taught rate at this rung>, fresh_readings=<the five fresh taught rates at this rung from the calibration file>)` (D-19; `inside` names whether the rung sits in the band). Write `out_dir / f"phase27_{leg}_curve.json"`: `{leg, z, points: {key: {rungs: [{steps, scored_tokens, taught_recall, heldout_recall, extraction, band, draws_cache}], reading_at_z: <the rung == z entry or null>}}, finding: "the cost curve QUALIFIES the gate's reading; it is not a second gate (RELRN-03)", curve_k, device}`; return it.
8. `def run_gate(*, record, leg, out_dir, baseline) -> dict` (RELRN-01, D-09, D-16, D-21): `_require_admitted` FIRST; read the calibration and curve files (`_prove` both exist); `device = phase25_run.device()`; `x = phase27_prereg.extraction_ceiling_x(frontier())` — BY CALL, never `frontier()["verdicts"]["extraction_ceiling"]["X"]` (a forged frontier's summary field is not recomputed; the call reads the control point's counts, which is what the e2e forges); for every admitted point in this leg: `reading = curve["points"][key]["reading_at_z"]`; if `z is None` → verdict via `phase27_prereg.recovery_gate(recovered_successes=0, recovered_questions=<n>, x=x, z=None, baseline=baseline)` (INCONCLUSIVE by the prereg's rule, the counts still recorded); else the PROMOTION: `provisional, provisional_reasons = phase27_prereg.recovery_gate(recovered_successes=<CURVE_K successes>, recovered_questions=<questions>, x=x, z=z, baseline=baseline)`; `promote, why = phase27_prereg.promote_at_z(provisional, provisional_reasons)`; when promoted, re-score the Z-rung adapter at `phase27_prereg.FULL_K` through `score_rung(point_label=f"phase27_{leg}_{key}_rung{z:04d}_k{phase27_prereg.FULL_K}", k=phase27_prereg.FULL_K, ...)` — its OWN cache label: `load_draws` (`phase25_run.py:216-230`) refuses a cache whose recorded `k` differs, so re-using the CURVE_K label would `SystemExit`; that refusal is Phase 25's feature and the k-suffix is the driver's answer to it — then `prefix_identical = <for every gated question, the CURVE_K blob's draws equal the first CURVE_K draws of the FULL_K blob>` (D-21's bit-identical-prefix claim, read from the two cache blobs; record the measured boolean, never `_prove` it), and `verdict, reasons = phase27_prereg.recovery_gate(recovered_successes=<FULL_K successes>, recovered_questions=<questions>, x=x, z=z, baseline=baseline)`; when NOT promoted, `verdict, reasons = provisional, provisional_reasons` and `recovered.k == CURVE_K`. Every `recovery_gate` call passes EXACTLY those five keywords — nothing from the curve or the band reaches it (RELRN-03; plan 27-04's AST test pins the keyword set and counts the calls inside `run_gate`: write the call ONCE in a local helper `_verdict(successes, questions, z_value)` so the AST test finds exactly one `recovery_gate(` call site). Write `out_dir / f"phase27_{leg}_gate.json"`: `{leg, baseline, x, z, points: {key: {verdict, reasons, provisional: {verdict, reasons, k}, recovered: {successes, questions, k, draws_cache}, taught_recall_reported: {...}, promoted, promotion_reason, prefix_identical}}, cost_curve_ref: <curve file name>, finding_is_not_a_gate: true, device}`; print one line per point `[phase27_relearn] gate {leg} {key}: {verdict} — {reasons[0]}`; return it.
9. `def run_structural_proof(*, record, leg, out_dir) -> dict` (RELRN-04, D-26 i/ii/iii, SC5): `_require_admitted` FIRST; glob `out_dir / f"phase27_{leg}_*_readings.json"` (`_prove` ≥ 2 arms present); (i) `shared_config: all readings' train_config equal to the designated-seed fresh arm's on every field` — the four extra fresh seeds may differ ONLY in `seed` (list the differing field names per file under `differing_fields`; `_prove` the set is `⊆ {"seed"}` for `fresh_seed≠designated` and `== set()` otherwise); (ii) `off_disk: for every file, train_config == checkpoint_train_config` (the record the driver wrote vs the record `train()` wrote) — `_prove`d; (iii) `data_order: {arm: offset_stream.sha256}` and `equal_across_arms_at_designated_seed: <fresh@1337 == control@1337 == mitigated@1337 digests>` (equal seed + equal bin LENGTH ⇒ equal stream — record the bin lengths beside it; unequal lengths are disclosed, not proved equal) plus `differs_across_seeds: <all five fresh digests distinct>` and `devices: {arm: readings.device}`. Write `out_dir / f"phase27_{leg}_structural.json"` with the three blocks and `train_path: TRAIN_PATH`; return it.

Natural RED for the SUMMARY: after writing `run_calibrate` but BEFORE `run_curve`/`run_gate`/`run_structural_proof` replace their Task 1 stubs, plan 27-04's e2e test (if written early) fails at `NotImplementedError("plan 27-03 Task 3")` on `curve` — quote it; otherwise quote Task 4's kwargs-trace RED.

Commit the driver at the end of this task (`feat(27-03): phase27_relearn driver — admit + calibrate/curve/gate/structural-proof, train() direct with one shared TrainConfig`).

Threat references: T-27-04 (the legs are real code on the real train path, not stubs), T-27-05 (`baseline` reaches `recovery_gate` only as a pinned key from the CLI), T-27-06 (no `torch.load` by name), T-27-11 (adapter sha refused before any training or scoring), T-27-13 (sidecars under gitignored `data/`).
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/python -c "import ast, inspect, sys; sys.path.insert(0,'scripts'); sys.path.insert(0,'src'); import phase27_relearn as r; assert 'torch' not in sys.modules; src=open('scripts/phase27_relearn.py').read(); tree=ast.parse(src); assert not [n for n in ast.walk(tree) if isinstance(n, ast.Attribute) and n.attr=='load' and isinstance(n.value, ast.Name) and n.value.id=='torch'], 'torch.load by name'; assert 'NotImplementedError' not in src; assert all(set(inspect.signature(getattr(r,n)).parameters)=={'record','leg','out_dir'} for n in ('run_calibrate','run_curve','run_structural_proof')), 'leg signatures'; assert set(inspect.signature(r.run_gate).parameters)=={'record','leg','out_dir','baseline'}; g=inspect.getsource(r.run_gate); assert g.count('recovery_gate(')==1 and 'band' not in g.split('recovery_gate(')[1].split(')')[0]; assert 'extraction_ceiling_x(' in g and '[\"extraction_ceiling\"]' not in g; assert 'FULL_K}' in g and 'CURVE_K}' in inspect.getsource(r.run_curve); assert 'phase25_run.device()' in inspect.getsource(r.run_calibrate); print('OK')" && .venv/bin/ruff check scripts/phase27_relearn.py && .venv/bin/ruff format --check scripts/phase27_relearn.py && .venv/bin/pytest -q tests/test_phase23_resume.py::test_resume_from_none_is_inert tests/test_phase20_correction.py -x</automated>
  </verify>
  <acceptance_criteria>
    - Every `run_*` function's FIRST statement is `blob = _require_admitted(record)` (`ast`: the first statement of each `FunctionDef` body after the docstring is an `Assign` whose value is a `Call` to `_require_admitted`) — plan 27-04's test pins this; check it by hand here
    - `inspect.getsource(run_gate)` contains exactly one `recovery_gate(` call whose keyword set is `{recovered_successes, recovered_questions, x, z, baseline}`, contains `extraction_ceiling_x(` and does not contain `["extraction_ceiling"]`; the FULL_K re-score label ends in `_k{phase27_prereg.FULL_K}` and the CURVE_K labels in `run_calibrate`/`run_curve` end in `_k{phase27_prereg.CURVE_K}`; `inspect.getsource(run_curve)` contains `band(` and `admitted_point_keys`
    - Every leg resolves `device` through `phase25_run.device()` and records it in its output file (`grep -c "phase25_run.device()" scripts/phase27_relearn.py` ≥ 4)
    - `grep -c "torch.load" scripts/phase27_relearn.py` prints 0; `grep -c "train_arm(" scripts/phase27_relearn.py` prints 0
    - `.venv/bin/pytest -q tests/test_phase23_resume.py::test_resume_from_none_is_inert tests/test_phase20_correction.py` green (the `train_arm(` register and the pin census untouched)
    - `git log --oneline -1 -- scripts/phase27_relearn.py` shows this task's commit; `git ls-files 'results/phase27_*'` still empty
  </acceptance_criteria>
  <done>The four legs exist on the real train path with the shared config, the rung chain, the recorder, X by call and the pinned-baseline gate with a k-suffixed promotion; the driver is committed; nothing has trained on real data (the legs refuse — the record does not exist yet).</done>
</task>

<task type="auto">
  <name>Task 4: Write tests/test_phase27_relearn.py — per-leg refusals watched RED, moved pins, write-once, dirty tree, kwargs trace, AST guards, git surface, apparatus shape</name>
  <files>tests/test_phase27_relearn.py</files>
  <read_first>
    - scripts/phase27_relearn.py (Task 3 state)
    - tests/test_phase26_canary.py lines 1-70 (imports, `_DRIVER`, `_FRONTIER`, `needs_adapters`, the autouse `clean_tree` stub of `refuse_if_dirty`), 252-281 (the two AST tests), 361-399 (dirty-tree wiring proof + write-once both-state)
    - tests/test_phase25_driver.py lines 88-131 (`_enclosing_function`, `_git_argv_subcommands`) and 373-420 (planted-push RED on a tmp copy)
    - tests/test_phase23_resume.py lines 355-359 (`inspect.signature` on a keyword-only parameter)
    - tests/test_phase25_calibrate.py lines 570-586 (watched RED on a tmp COPY)
    - .planning/phases/27-relearning-attack/27-PATTERNS.md §"tests/test_phase27_relearn.py" Analog A (all excerpts) and Analog E
  </read_first>
  <action>
Create `tests/test_phase27_relearn.py` with the sibling `sys.path` inserts (scripts AND src), module-scope imports `ast`, `copy`, `hashlib`, `inspect`, `json`, `pathlib`, `subprocess`, `sys`, `numpy as np`, `pytest`, `phase25_record`, `phase25_run`, `phase27_prereg`, `import phase27_relearn as relearn`; `_DRIVER = _ROOT / "scripts" / "phase27_relearn.py"`; `_git(*args)`; a module-scoped `frontier` fixture (`relearn.frontier()`); an autouse `clean_tree(monkeypatch)` fixture replacing `relearn.refuse_if_dirty` with a recorder returning `""` (the `tests/test_phase26_canary.py:63-69` idiom — the suite runs on dirty trees; the guard is RECORDED, never exercised, except in the one test that plants a refusing stub); a helper `_forge(tmp_path, verdict, *, reasons=("forged",), baselines=None) -> pathlib.Path` writing `{"verdict": {"verdict": verdict, "reasons": list(reasons)}, "admitted_point_keys": [], "baselines": baselines if baselines is not None else phase27_prereg.PINNED_BASELINES}` to `tmp_path / "phase27_admission.json"`. Torch-importing modules (`phase14_recall`, `teach_persona`) only inside tests. No `train_arm(` substring in the file.

Tests, EXACT names (the `apparatus` block and the VALIDATION map carry them):

- `test_each_leg_refuses_unless_admitted` — ONE `@pytest.mark.parametrize(("mode", "verdict"), [...], ids=[...])` over the 12 pairs `mode ∈ ("calibrate", "curve", "gate", "structural-proof")` × `verdict ∈ ("MOOT", "INCONCLUSIVE", "absent")` with ids `f"{mode}-{verdict}"` (so `test_each_leg_refuses_unless_admitted[calibrate-MOOT]` is a real node id — `APPARATUS_LEGS` names exactly these). Body: `forged = _forge(tmp_path, verdict)` or, for `"absent"`, `tmp_path / "phase27_admission.json"` never written; `argv = [mode, "--record", str(forged), "--leg", "n8", "--out-dir", str(tmp_path / "out")]` plus `["--baseline", "never_taught_1337"]` for `gate`; `with pytest.raises(SystemExit) as exc: relearn.main(argv)`; assert `"REFUSING" in str(exc.value)` and (`verdict` in the message for MOOT/INCONCLUSIVE, `"absent"` for the absent case); assert `not (tmp_path / "out").exists() or not any((tmp_path / "out").iterdir())` (nothing trained, nothing written); assert the refusal happened before any `tp.` call by spying: `monkeypatch.setattr(relearn, "shared_train_config", lambda: pytest.fail("leg ran past the gate"))` and `monkeypatch.setattr(phase25_run, "device", lambda: pytest.fail("leg resolved a device past the gate"))` (D-09: no refused leg may even resolve a device).
- `test_a_record_with_moved_pins_is_refused` (D-12, the third conjunct): `pins = copy.deepcopy(phase27_prereg.PINNED_BASELINES)`; alter one entry's `sha256` by one hex digit; `path = _forge(tmp_path, "ADMITTED", baselines=pins)`; `relearn._require_admitted(path)` raises `SystemExit` containing `"baselines"` and `"REFUSING"`; the same forge with the unaltered pins returns the blob (the JSON round trip of the pins is an equality — tuples compare as lists on both sides).
- `test_a_leg_refuses_an_untracked_record_inside_the_repo` (the tracked conjunct): write a forged ADMITTED record to `_ROOT / "results" / "phase27_admission_probe_never_committed.json"` inside a `try/finally` that unlinks it; `relearn._require_admitted(that path)` raises `SystemExit` containing `"not tracked"` and `"REFUSING"`; assert `git ls-files` of that path is empty before and after.
- `test_an_admitted_tmp_record_passes_the_gate_without_git`: `_forge(tmp_path, "ADMITTED")` → `relearn._require_admitted(path)` returns the blob; spy `subprocess.run` via monkeypatch on `relearn.subprocess` to assert it was NOT called (the tracked conjunct is skipped outside `_ROOT`).
- `test_admit_refuses_to_overwrite` (both-state, `tests/test_phase26_canary.py:385-398`): if `relearn.RECORD.exists()` → `relearn.admit()` raises with `"REFUSING to overwrite"` and `"--force"`, bytes unchanged; else assert `git ls-files results/phase27_admission.json` is empty AND `admit(tmp_path / "x.json")` followed by a second `admit(tmp_path / "x.json")` raises the overwrite refusal with the file's bytes unchanged. (The first `admit` call imports torch through `disjointness_report` — mark this test with the same `needs_adapters`-style skip ONLY if `artifacts/tokenizer.json`, `results/phase16_recall_sample.json` or `results/phase18_corpus.json` is missing; all three are tracked, so it runs everywhere.)
- `test_admit_refuses_a_dirty_tree_before_hashing` (`tests/test_phase26_canary.py:361-383`): call `relearn.admit(_ROOT / "results" / "phase27_probe_never_written.json")` with the autouse recorder → assert the recorder saw `who == "phase27_relearn"` and `pathspec == ("scripts", "src", "results", ":(exclude)results/phase27_probe_never_written.json")` — then a second stub that raises `SystemExit("[probe] REFUSING: the working tree is dirty")` → `admit(tmp_path / "y.json")` raises with `"dirty"` and NO file exists afterwards; also monkeypatch `relearn.disjointness_report` to `pytest.fail` to prove the dirty refusal precedes every digest and every torch import.
- `test_admit_writes_the_full_schema_to_a_tmp_path`: `blob = relearn.admit(tmp_path / "rec.json")`; assert the file re-reads equal to `blob`; the top-level key set equals the 21 keys listed in Task 1 item 6 plus `disjointness` and `provenance`; `blob["verdict"]["verdict"] == "MOOT"`; `len(rows) == 44`; `rows` verdict Counter equals `frontier["verdicts"]["tallies"]`; `disjointness["overlaps"] == {"teaching": 0, "trained_attack": 0, "attacker_corpus": 0}`, `disjointness["scored"]["gated_prompts"] == 416`, `disjointness["scored"]["gated_questions"] == 104`, `disjointness["checked"] >= 104`, `disjointness["trained_attack_rows"] > 0`; `disjointness["held_out_family"] == phase27_prereg.HELD_OUT_FAMILY`; `set(provenance["module_sha256"]) == set(relearn.PINNED_MODULES)` and each digest equals `hashlib.sha256((_ROOT / rel).read_bytes()).hexdigest()` (recomputed from bytes here, never via `relearn._sha256`); `provenance["torch_version"]` is a non-empty string; `apparatus["legs"]` has four entries whose `refusal_node_id`s are all `f"tests/test_phase27_relearn.py::test_each_leg_refuses_unless_admitted[{m}-MOOT]"` and whose `train_path == relearn.TRAIN_PATH` mentions `"train()"` and `"build_arm_bins"`; `apparatus["status"] == "not exercised"`; `apparatus["draw_cache"]` contains `"phase25_phase27_"`. Also the watched RED on a deep copy: flip one row's `cleared_a`, recompute via `phase27_prereg.cleared_abc` on the frontier's entry and assert the comparison fails (T-27-01/D-33).
- `test_main_passes_only_kwargs_the_legs_accept` (D-10 kwargs trace, `27-PATTERNS` Analog E): walk `ast.parse(_DRIVER)` for the `FunctionDef` named `main`; collect every `ast.Dict` literal or `ast.Call` keyword set assigned per mode (the driver builds `kwargs` dicts with string keys — collect the string keys under each `mode` branch, and the keywords of any direct `DISPATCH[...](...)`/`run_*(...)` call); for every `mode, fn in relearn.DISPATCH.items()`: `params = inspect.signature(fn).parameters`; assert the collected key set for that mode `⊆ set(params)` and that every parameter without a default is in the collected set; assert `set(relearn.DISPATCH) == set(relearn.SUB_MODES)`. Watched RED on a `tmp_path` COPY of the driver: rewrite one kwarg key in the `gate` branch (`"baseline"` → `"baselne"`) and re-run the walk over the copy against the real signatures → the assertion fails naming the kwarg.
- `test_every_leg_opens_with_require_admitted`: `ast` over each `run_*` `FunctionDef`: after the docstring, the first statement is an `Assign` whose value is `Call(func=Name("_require_admitted"))`.
- `test_the_driver_imports_no_torch_touching_module_at_top_level` (`tests/test_phase26_canary.py:252-267` + the extended set): top-level imports ∩ `{"torch", "numpy", "teach_persona", "phase14_factset", "phase14_recall", "phase18_extraction", "phase25_points", "personacore.training.loop", "personacore.lora", "personacore.checkpoint", "personacore.config", "personacore.model", "personacore.tokenizer"}` is empty; and a fresh-interpreter subprocess `import phase27_relearn` prints `'torch' in sys.modules → False`.
- `test_the_driver_never_calls_torch_load_directly` (T-27-06): the `tests/test_phase26_canary.py:270-281` walk finds no `Attribute(attr="load")` on `Name(id="torch")`; additionally no `Attribute(attr="load")` on `Attribute(value=Name("tp"), attr="torch")`.
- `test_the_drivers_git_surface_is_read_only` (T-27-08): copy `_git_argv_subcommands` + `_enclosing_function` from `tests/test_phase25_driver.py:88-131`; every found subcommand ∈ `phase25_run.READ_ONLY_GIT_ACTIONS` and the only one present is `"ls-files"` inside `_require_admitted`; planted RED: a `tmp_path` copy with an appended line building `["git", "add", "x"]` → the helper finds `("add", lineno, fn)`.
- `test_base_slim_is_phase14s_choke_point`: import `phase14_recall as pr` inside; `relearn.BASE_SLIM == pr.CONVBASE_SLIM` (the scorers load the base through `pr.CONVBASE_SLIM`; the driver's `model_from_adapter` through `BASE_SLIM`; one file).
- `test_the_recorder_tags_by_bin_identity_not_suffix` (D-30): `cb, state = relearn.make_recorder(tmp_path / "s.bin", teaching_bin=tmp_path / "phase27_x_train.bin")`; call `cb(tmp_path / "phase27_x_train.bin", np.arange(2, dtype=np.int64))` then `cb(tmp_path / "dialog_train.bin", np.arange(2, dtype=np.int64))` (both names end in `_train.bin`); the stream file's bytes are `b"\x00" + 16 bytes + b"\x01" + 16 bytes`; `state["draws"] == 2`; `inspect.getsource(relearn.make_recorder)` does not contain `endswith(`.
- `test_pinned_modules_exist_and_include_the_emitter`: every `PINNED_MODULES` entry is a file; `"scripts/phase27_relearn.py"` and `"scripts/phase27_prereg.py"` and `"scripts/teach_persona.py"` are members.
- `test_gate_cli_requires_a_pinned_baseline`: `relearn.build_parser().parse_args(["gate", "--leg", "n8"])` raises `SystemExit`; `["gate", "--leg", "n8", "--baseline", "made_up"]` raises `SystemExit`; every key of `phase27_prereg.BASELINE_KEYS` parses.

Natural RED to record (not planted): run `test_each_leg_refuses_unless_admitted` and `test_every_leg_opens_with_require_admitted` against the Task 1 commit state if it was committed separately — the stubs refuse correctly (GREEN) — then the natural intermediate for the kwargs trace is the driver BEFORE `DISPATCH` gained `"structural-proof"`: `set(DISPATCH) == set(SUB_MODES)` fails naming the missing mode. If all three driver tasks are already complete, take the RED from `test_main_passes_only_kwargs_the_legs_accept`'s tmp-copy branch (a copy, never the tree) and say so.

Commit (`test(27-03): driver refusals, moved pins, write-once, dirty tree, kwargs trace, AST and git-surface guards`).

Threat references: T-27-04 (refusals watched RED per leg; kwargs trace), T-27-06, T-27-07, T-27-08, T-27-12 (digests recomputed from bytes in the tmp emission).
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/pytest -q tests/test_phase27_relearn.py -x --durations=3 && .venv/bin/ruff check tests/test_phase27_relearn.py && .venv/bin/ruff format --check tests/test_phase27_relearn.py scripts/phase27_relearn.py && .venv/bin/pytest -q tests/test_phase27_prereg.py tests/test_phase27_on_draw.py tests/test_phase23_resume.py::test_resume_from_none_is_inert tests/test_phase20_correction.py tests/test_phase25_close.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest -q tests/test_phase27_relearn.py` reports 0 failed and ≥ 26 passed (12 refusal parametrizations + ≥ 14 others), 0 skipped on this host
    - `.venv/bin/pytest --collect-only -q tests/test_phase27_relearn.py | grep -c "test_each_leg_refuses_unless_admitted\["` prints 12 and the ids include `[calibrate-MOOT]`, `[curve-MOOT]`, `[gate-MOOT]`, `[structural-proof-MOOT]` verbatim (the `APPARATUS_LEGS` node ids)
    - `test_admit_writes_the_full_schema_to_a_tmp_path` asserts all three overlaps are 0, `gated_prompts == 416`, `gated_questions == 104` and all seven digests equal a bytes recompute; `test_admit_refuses_to_overwrite` passes in the ABSENT state with a real assertion (`git ls-files results/phase27_admission.json` empty + the tmp double-write refusal)
    - `test_a_record_with_moved_pins_is_refused` asserts the altered-pin refusal names `baselines` and the unaltered pins pass
    - `test_the_recorder_tags_by_bin_identity_not_suffix` asserts the `\x00` / `\x01` tags on two `_train.bin`-suffixed paths
    - `test_main_passes_only_kwargs_the_legs_accept` asserts the tmp-copy RED names the misspelled kwarg
    - `grep -c "train_arm(" tests/test_phase27_relearn.py scripts/phase27_relearn.py` prints 0 for both; `grep -c '"A2"' tests/test_phase27_relearn.py` prints 0; `tests/test_phase23_resume.py::test_resume_from_none_is_inert` green
    - `.venv/bin/pytest --collect-only -q | tail -1` reports 2802 + (27-01's count) + 5 + (this file's count); the measured number is written in the SUMMARY
    - `git ls-files 'results/phase27_*'` is empty; `git diff --stat HEAD~3 -- scripts/teach_persona.py scripts/phase27_prereg.py pyproject.toml results/phase25_frontier.json` is empty (the prereg is untouched by this plan — the ancestry guard's premise)
  </acceptance_criteria>
  <done>Structural tests committed and green; every leg's refusal watched on forged MOOT / INCONCLUSIVE / absent / moved-pins / untracked records; the kwargs trace guards `main()` → `run_<leg>()`; the driver is torch-free at import with a read-only git surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| results/phase27_admission.json → every leg | The committed record is the only admission input; a leg reads it, never the frontier, and refuses on verdict, moved pins or tracking |
| driver → git | Read-only: one `ls-files` argv; the operator commits (D-15) |
| checkpoints/ + frontier adapter paths → model_from_adapter | Every adapter loaded through `load_adapter` (weights_only) after a sha256 pin check; base through `load_slim` |
| driver → data/ | Sidecars (bins, rung adapters, offset streams, per-leg JSON) and the Phase-25-named draw caches (`data/phase25_phase27_*_draws.json`) under the gitignored `data/` only |
| driver → device | One resolver, `phase25_run.device()`; no refused leg reaches it; the wiring proof pins it to CPU (D-09) |
| tests → driver | Tests forge records in tmp and stub only `refuse_if_dirty`; the gate, hashing and writes stay real |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | Tampering | record rows shaped after data | mitigate | rows re-derived through `phase27_prereg.cleared_abc` and `_prove`d against tallies at the single write; flip-one-row RED on a copy |
| T-27-04 | Denial (of evidence) | unwired live path behind green refusals | mitigate | `test_main_passes_only_kwargs_the_legs_accept` (AST + `inspect.signature`, tmp-copy RED); `test_every_leg_opens_with_require_admitted`; plan 27-04's e2e |
| T-27-05 | Tampering | baseline chosen after data | mitigate | `--baseline` is a required argparse choice over `BASELINE_KEYS`; `run_gate` passes it verbatim to `recovery_gate`; `_require_admitted` refuses a record whose pins moved |
| T-27-06 | Tampering | pickle deserialisation of adapters / base | mitigate | `load_slim` / `load_adapter` / `load_checkpoint` only; `test_the_driver_never_calls_torch_load_directly` (incl. `tp.torch.load`) |
| T-27-07 | Repudiation | provenance from a dirty tree | mitigate | `refuse_if_dirty` is `admit`'s second statement, before any digest; `test_admit_refuses_a_dirty_tree_before_hashing` |
| T-27-08 | Elevation | driver git surface | mitigate | `test_the_drivers_git_surface_is_read_only` — only `ls-files` inside `_require_admitted`; planted `add` RED on a copy |
| T-27-09 | Tampering | point keys / arm names / paths | mitigate | `admitted_point_keys` from the prereg (subsequence-proved); leg/arm names from `LEGS`/`ARMS` tuples; sidecar and cache labels composed from those, the seed, the rung and `k` |
| T-27-11 | Spoofing | wrong-weights adapter attacked or scored | mitigate | `model_from_adapter` refuses on `adapter_sha256` mismatch against the record's pin / the frontier's entry before any training (D-22) |
| T-27-12 | Tampering | provenance drift | mitigate | seven `PINNED_MODULES` digests from bytes at the write; recomputed from bytes in the test; plan 27-04's live-bytes guard |
| T-27-13 | Information disclosure | data/ sidecars | accept | Synthetic persona facts already published in the frontier; `data/` gitignored |
| T-27-SC | Tampering | package installs | accept | Nothing installed (D-39) |
</threat_model>

<verification>
- `.venv/bin/pytest -q tests/test_phase27_relearn.py tests/test_phase27_prereg.py tests/test_phase27_on_draw.py tests/test_phase23_resume.py::test_resume_from_none_is_inert tests/test_phase20_correction.py tests/test_phase25_close.py` green
- `git ls-files 'results/phase27_*'` empty; `scripts/phase27_prereg.py`, `scripts/teach_persona.py`, `pyproject.toml`, `results/phase25_frontier.json` untouched
- A fresh interpreter imports the driver without torch
</verification>

<success_criteria>
- `admit` writes the full record to a tmp path with re-derived rows, zero overlaps over question strings (set (ii) = `adversarial_episodes` rows) and seven live digests; every leg refuses on MOOT / INCONCLUSIVE / absent / moved pins / untracked with the reading named; `main()`'s kwargs trace to each leg's signature
- The train path is `train()` direct with one shared `TrainConfig` and a driver-built `RuntimeConfig`, disclosed in `TRAIN_PATH` and in every apparatus leg; the recovery gate receives exactly its five keywords with X by call; the FULL_K promotion has its own cache label
</success_criteria>

<output>
Create `.planning/phases/27-relearning-attack/27-03-SUMMARY.md` when done
</output>
