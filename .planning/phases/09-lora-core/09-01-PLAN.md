---
phase: 09-lora-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/personacore/lora/__init__.py
  - src/personacore/lora/config.py
  - src/personacore/lora/layer.py
  - src/personacore/lora/inject.py
  - tests/test_lora_layer.py
  - tests/test_lora_inject.py
autonomous: true
requirements: [LORA-01, LORA-02, LORA-05]

must_haves:
  truths:
    - "Wrapping a seeded nn.Linear with LoRALinear at init leaves outputs bit-identical to the bare Linear (B=0 identity gate, torch.equal)"
    - "inject_lora wraps exactly 6 projections per block (n == 6 * n_layer) and nothing else; lm_head and wte are never LoRALinear"
    - "Post-injection, lm_head.weight.data_ptr() == wte.weight.data_ptr() still holds"
    - "After mark_only_lora_trainable, the trainable-param census equals r * n_layer * 18 * n_embd"
    - "Load-then-inject preserves logits bit-identical to the pre-injection model on a fixed batch"
  artifacts:
    - path: "src/personacore/lora/config.py"
      provides: "LoRAConfig dataclass (r=8, alpha=16.0, dropout=0.0, targets) + TARGET_PROJECTIONS canonical allowlist"
      contains: "TARGET_PROJECTIONS"
    - path: "src/personacore/lora/layer.py"
      provides: "LoRALinear composition wrapper (A-Gaussian std 0.02, B-zero, scale=alpha/r single source, flag-gated delta branch)"
      contains: "class LoRALinear"
    - path: "src/personacore/lora/inject.py"
      provides: "inject_lora, mark_only_lora_trainable, snapshot_params, lora_state_dict, load_adapter_weights"
      exports: ["inject_lora", "mark_only_lora_trainable", "snapshot_params", "lora_state_dict", "load_adapter_weights"]
    - path: "src/personacore/lora/__init__.py"
      provides: "Public import surface for the lora package"
    - path: "tests/test_lora_layer.py"
      provides: "LORA-01 layer-level pins (identity, scale, dropout placement, contiguity, state-dict hygiene)"
      min_lines: 60
    - path: "tests/test_lora_inject.py"
      provides: "LORA-01/05 injection pins (allowlist cross-pin, tied data_ptr, ordering, param-count formula, key audit)"
      min_lines: 80
  key_links:
    - from: "src/personacore/lora/inject.py"
      to: "src/personacore/lora/layer.py"
      via: "relative import"
      pattern: "from \\.layer import LoRALinear"
    - from: "src/personacore/lora/layer.py"
      to: "forward delta gating"
      via: "flag-gated branch (D-05)"
      pattern: "self\\.enabled and not self\\.merged"
    - from: "tests/test_lora_inject.py"
      to: "tests/test_gpt_lora_seam.py"
      via: "allowlist cross-pin assert"
      pattern: "TARGET_PROJECTIONS"
---

<objective>
Ship the from-scratch LoRA core: `LoRAConfig`, the `LoRALinear` composition wrapper, and the
post-load injection/freeze machinery — every correctness property pinned by CPU-only unit tests.

Purpose: This is the load-bearing deliverable of Phase 9 (LORA-01) and the mechanism every later
plan builds on. The B=0 identity gate and the six-name allowlist are the two highest-value
correctness properties in the whole phase — if either is wrong, every baseline downstream is
poisoned.

Output: New `src/personacore/lora/` package (config.py, layer.py, inject.py, __init__.py) +
`tests/test_lora_layer.py` + `tests/test_lora_inject.py`, all green; full existing suite stays green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-lora-core/09-CONTEXT.md
@.planning/phases/09-lora-core/09-RESEARCH.md
@.planning/phases/09-lora-core/09-PATTERNS.md

<interfaces>
<!-- Key contracts extracted from the codebase. Use directly — no exploration needed. -->

From src/personacore/model/gpt.py (NEVER edited — purity rule):
```python
self.wte = nn.Embedding(config.vocab_size, config.n_embd)   # line 159 — token embedding (tied head)
self.lm_head = nn.Linear(config.n_embd, config.vocab_size, bias=False)  # line 169
self.lm_head.weight = self.wte.weight                        # line 184 — SAME tensor (data_ptr)
# Six named projections per block: attn.{q_proj,k_proj,v_proj,c_proj} (384->384, lines 71-74),
# mlp.{fc_in (384->1536), fc_out (1536->384)} (lines 120-121)
```

From tests/test_gpt_lora_seam.py (line 16 — the canonical allowlist to cross-pin):
```python
PROJECTIONS = ("q_proj", "k_proj", "v_proj", "c_proj", "fc_in", "fc_out")
```

From tests/test_slim_checkpoint.py (lines 49-52 — the tiny CPU fixture, reuse verbatim):
```python
def _tiny_config() -> ModelConfig:
    return ModelConfig(block_size=32, n_layer=1, n_head=2, n_embd=16)
```

From src/personacore/config.py — house dataclass pattern: `@dataclass` with defaulted fields,
inline comments tying fields to requirement/decision IDs, travels via `dataclasses.asdict()`.

Param-count closed form (VERIFIED in 09-RESEARCH.md): trainable = r * n_layer * 18 * n_embd
(production defaults: 8 * 6 * 18 * 384 = 331,776 ≈ 1.33 MB fp32; tiny fixture at r=8: 8*1*18*16 = 2,304).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: LoRAConfig + LoRALinear wrapper + layer-level tests</name>
  <files>src/personacore/lora/__init__.py, src/personacore/lora/config.py, src/personacore/lora/layer.py, tests/test_lora_layer.py</files>
  <read_first>
    - .planning/phases/09-lora-core/09-RESEARCH.md (Pattern 1 + "Code Examples → LoRALinear core" — the verified reference implementation to copy)
    - .planning/phases/09-lora-core/09-PATTERNS.md (lora/config.py and lora/layer.py sections — house dataclass/module/docstring style)
    - src/personacore/config.py (ModelConfig/TrainConfig dataclass pattern to mirror)
    - src/personacore/model/gpt.py (LayerNorm lines 32-49 as small-module template; MLP dropout-as-member lines 118-129; _init_weights std 0.02 lines 186-191; autocast-safety docstring rule lines 12-14)
    - src/personacore/model/__init__.py (package __init__ analog: docstring + relative imports + __all__)
    - tests/test_slim_checkpoint.py (_tiny_config fixture lines 49-52; seeding convention)
    - tests/test_ablation_config.py (construct-and-assert bit-identity test style)
  </read_first>
  <behavior>
    tests/test_lora_layer.py (write FIRST, CPU-only, GPU-free, module docstring citing LORA-01/D-05):
    - Test 1 (B=0 identity gate — highest-value test): seed torch.manual_seed(1234), build nn.Linear(16, 16),
      wrap in LoRALinear(base, r=4, alpha=8.0); on a fixed random batch, wrapped(x) is torch.equal to base(x).
    - Test 2 (scale single source): LoRALinear(..., r=8, alpha=16.0).scale == 2.0; scale is a plain float
      attribute set once in __init__ (no second alpha/r computation anywhere in forward).
    - Test 3 (A-Gaussian / B-zero init): lora_B is all zeros (torch.count_nonzero == 0); lora_A is not all
      zeros and has shape (r, in_features); lora_B has shape (out_features, r).
    - Test 4 (contiguity — PITFALLS P5): lora_A.is_contiguous() and lora_B.is_contiguous() are True.
    - Test 5 (dropout placement): with dropout=0.5, module in .train() mode produces stochastic outputs
      across two forward calls on the same input once lora_B is nudged nonzero, while .eval() mode is
      deterministic AND the base path is unaffected (disabled delta branch output equals base(x) regardless
      of dropout). nn.Dropout is applied to x on the LoRA branch only, before A.
    - Test 6 (state-dict hygiene): wrapped module state_dict keys are exactly {"base.weight", "base.bias",
      "lora_A", "lora_B"} (bias present when base has bias); "enabled"/"merged" never appear in state_dict.
    - Test 7 (delta actually fires when nonzero): nudge lora_B via nn.init.normal_, assert wrapped(x) is
      NOT torch.equal to base(x) while self.enabled is True.
  </behavior>
  <action>
    Create the lora package per D-01..D-08 lineage and the verified RESEARCH reference (09-RESEARCH.md
    lines 365-384 — copy that skeleton; it already encodes scale-single-source, D-05 flag gating, and
    dropout placement).

    src/personacore/lora/config.py:
    - Module docstring citing LORA-01 and the discretion resolution (LoRAConfig follows the ModelConfig
      house pattern).
    - TARGET_PROJECTIONS = ("q_proj", "k_proj", "v_proj", "c_proj", "fc_in", "fc_out") as a module-level
      tuple constant — the canonical allowlist (PITFALLS P1: never a class-based isinstance scan).
    - @dataclass LoRAConfig with fields: r: int = 8, alpha: float = 16.0, dropout: float = 0.0,
      targets: tuple = TARGET_PROJECTIONS (tuple default is immutable — direct default is fine).
      Inline comments tie r/alpha defaults to the ~1.3 MB persona-file story (331,776 params at
      production shape). Config travels as dataclasses.asdict() primitives (checkpoint **extra and the
      adapter artifact both consume it).

    src/personacore/lora/layer.py:
    - class LoRALinear(nn.Module), constructor signature (base: nn.Linear, r: int, alpha: float,
      dropout: float = 0.0). Members: self.base (the frozen original, composition not inheritance);
      self.scale = alpha / r computed ONCE (single source of truth for forward AND the Plan-03 merge —
      PITFALLS P3); self.lora_A = nn.Parameter on a fresh torch.empty(r, base.in_features) then
      nn.init.normal_(mean=0.0, std=0.02) (house init std, A-Gaussian per LORA-01); self.lora_B =
      nn.Parameter(torch.zeros(base.out_features, r)) (identity gate, PITFALLS P2); self.dropout =
      nn.Dropout(dropout); self.enabled = True and self.merged = False as PLAIN Python bool attributes
      (never buffers/Parameters — they must not enter state_dict). A/B are fresh explicit-shape tensors,
      never weight.T views (PITFALLS P5 / MPS contiguity).
    - forward(x): y = self.base(x); when self.enabled and not self.merged, add
      self.scale * (self.dropout(x) @ self.lora_A.T @ self.lora_B.T). The merged guard exists NOW even
      though merge() lands in plan 09-03 — the flag is part of the forward contract (D-05).
    - Inherit gpt.py's autocast-safety rule verbatim: no torch.cuda.* calls, no manual dtype casts.

    src/personacore/lora/__init__.py:
    - Docstring naming the phase ("Phase 9 — from-scratch LoRA (LORA-01..05)"), relative imports,
      explicit __all__ re-exporting LoRAConfig, TARGET_PROJECTIONS, LoRALinear (Task 2 extends this).

    Run .venv/bin/pytest tests/test_lora_layer.py -q -x — all green. NOTE: all commands run inside the
    Python 3.11 venv (.venv) — the system Python 3.14 is an unsupported target.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_lora_layer.py -q -x</automated>
  </verify>
  <acceptance_criteria>
    - src/personacore/lora/config.py contains `TARGET_PROJECTIONS = ("q_proj", "k_proj", "v_proj", "c_proj", "fc_in", "fc_out")` and `class LoRAConfig` with defaults r=8, alpha=16.0, dropout=0.0
    - src/personacore/lora/layer.py contains `class LoRALinear(nn.Module)` and the literal guard expression `self.enabled and not self.merged` in forward
    - layer.py contains `self.scale = alpha / r` exactly once; grep -c "alpha / r" src/personacore/lora/layer.py returns 1
    - layer.py contains `torch.zeros(base.out_features, r)` (B-zero) and `nn.init.normal_` with `std=0.02` (A-Gaussian)
    - `enabled` and `merged` are plain attribute assignments (no register_buffer / nn.Parameter for them): grep -n "register_buffer" src/personacore/lora/layer.py returns no matches
    - `.venv/bin/pytest tests/test_lora_layer.py -q -x` exits 0 with >= 7 tests collected
  </acceptance_criteria>
  <done>LoRALinear wraps any nn.Linear with bit-identical output at init (torch.equal), scale/init/dropout/state-dict properties all test-pinned, package importable as `from personacore.lora import LoRAConfig, LoRALinear`</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Post-load injection, freeze discipline, key-audited adapter apply + injection tests</name>
  <files>src/personacore/lora/inject.py, src/personacore/lora/__init__.py, tests/test_lora_inject.py</files>
  <read_first>
    - .planning/phases/09-lora-core/09-RESEARCH.md (Pattern 2 + Pattern 4 key-audit paragraph + "Code Examples → Injection + freeze" lines 388-404 — verified reference)
    - .planning/phases/09-lora-core/09-PATTERNS.md (lora/inject.py section — the three composed in-repo idioms: name-suffix traversal gpt.py:175-177, module-tree scan seam-test:22, snapshot/restore loop.py:48-58)
    - src/personacore/lora/layer.py (just created — the wrapper being injected)
    - src/personacore/lora/config.py (just created — TARGET_PROJECTIONS + LoRAConfig)
    - tests/test_gpt_lora_seam.py (PROJECTIONS tuple line 16 — cross-pin target)
    - tests/test_gpt_weight_tying.py (data_ptr assert lines 15-19 — reuse verbatim post-injection)
    - tests/test_gpt_param_count.py (count_parameters dedup-by-data_ptr idiom lines 16-21 — house precedent restates the helper per test file)
  </read_first>
  <behavior>
    tests/test_lora_inject.py (write FIRST, CPU-only, tiny fixture ModelConfig(block_size=32, n_layer=1, n_head=2, n_embd=16)):
    - Test 1 (wrap count): inject_lora(model, LoRAConfig(r=4)) returns exactly 6 * cfg.n_layer; every
      blocks[i].attn.{q,k,v,c}_proj and blocks[i].mlp.{fc_in,fc_out} is an instance of LoRALinear.
    - Test 2 (allowlist cross-pin): TARGET_PROJECTIONS equals the seam test's PROJECTIONS tuple
      ("q_proj","k_proj","v_proj","c_proj","fc_in","fc_out") — import from tests.test_gpt_lora_seam or
      restate the literal; production code never imports from tests/.
    - Test 3 (tied-tensor safety — LORA-05): post-injection, model.lm_head.weight.data_ptr() ==
      model.wte.weight.data_ptr(); neither model.lm_head nor model.wte is a LoRALinear.
    - Test 4 (load→inject ordering pin): build seeded vanilla tiny GPT, capture logits on a fixed batch
      (torch.randint, torch.no_grad), inject_lora, assert post-injection logits torch.equal pre-injection
      logits (B=0 + loaded base ⇒ bit-identity).
    - Test 5 (param-count formula — LORA-05): after mark_only_lora_trainable, the requires_grad census
      equals cfg.r * model_cfg.n_layer * 18 * model_cfg.n_embd; every trainable param name contains
      "lora_"; every param without "lora_" in its name has requires_grad False.
    - Test 6 (lora_state_dict filter): lora_state_dict(model) keys all contain "lora_", cover exactly
      2 * 6 * n_layer tensors, and no ".base." weight appears (base weights never leak into the persona file).
    - Test 7 (key-audited apply — load_adapter_weights): build a second identically-injected model,
      apply lora_state_dict(model_a) to model_b via load_adapter_weights, assert model_b logits
      torch.equal model_a logits on a fixed batch; a corrupted dict (one key removed or one renamed)
      raises ValueError BEFORE any weight is loaded (PITFALLS P4 — no bare strict=False).
    - Test 8 (snapshot helper): snapshot_params(model) returns a dict of detached clones — mutating a
      model param afterwards does not change the snapshot.
  </behavior>
  <action>
    Create src/personacore/lora/inject.py per the verified RESEARCH reference (09-RESEARCH.md lines
    388-404). Module docstring cites LORA-01/LORA-02/PITFALLS P1/P4. Relative imports only
    (from .config import LoRAConfig, TARGET_PROJECTIONS; from .layer import LoRALinear); inject.py never
    imports checkpoint.py (locked dependency direction).

    Functions:
    - inject_lora(model, cfg: LoRAConfig) -> int: iterate model.modules() as parents; for each name in
      cfg.targets, getattr(parent, name, None); when isinstance(child, nn.Linear), setattr(parent, name,
      LoRALinear(child, cfg.r, cfg.alpha, cfg.dropout)) and count. Return the count (callers assert
      n == 6 * n_layer). Explicit allowlist iteration — NEVER isinstance-scan all Linears (picks up the
      tied lm_head, PITFALLS P1).
    - mark_only_lora_trainable(model) -> None: model.requires_grad_(False), then re-enable
      p.requires_grad_(True) for every named param whose name contains "lora_" (name-suffix traversal
      idiom, gpt.py:175-177 shape).
    - snapshot_params(model) -> dict[str, Tensor]: {n: p.detach().clone() for n, p in
      model.named_parameters()} — the params-actually-update canary helper, reused by tests and the
      Plan-04 smoke script.
    - lora_state_dict(model) -> dict[str, Tensor]: filter model.state_dict() for keys containing
      "lora_" — produces the artifact's adapter dict (base weights never leak).
    - load_adapter_weights(model, artifact: dict) -> None: compute expected = set of model.state_dict()
      keys containing "lora_"; got = set(artifact["adapter"].keys()); raise ValueError naming the
      symmetric difference when they differ; only then model.load_state_dict(artifact["adapter"],
      strict=False) — the key-set audit is what makes strict=False legitimate (PITFALLS P4).

    Extend src/personacore/lora/__init__.py __all__ with: inject_lora, mark_only_lora_trainable,
    snapshot_params, lora_state_dict, load_adapter_weights.

    Run the new tests, then the FULL suite (make test) — gpt.py/checkpoint.py untouched by this plan,
    all existing tests must stay green.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_lora_inject.py tests/test_lora_layer.py -q -x && make test</automated>
  </verify>
  <acceptance_criteria>
    - src/personacore/lora/inject.py defines all five functions: grep -c "^def " src/personacore/lora/inject.py returns 5
    - inject.py contains no `isinstance(child, LoRALinear)`-free bare Linear scan over all modules: the traversal iterates cfg.targets (grep -q "cfg.targets\|config.targets" src/personacore/lora/inject.py succeeds) and `lm_head` appears nowhere in inject.py
    - inject.py does not import personacore.checkpoint: grep -n "checkpoint" src/personacore/lora/inject.py returns no matches
    - load_adapter_weights raises ValueError on key-set mismatch before loading: test asserts pytest.raises(ValueError) and the target model's lora tensors are unchanged after the failed call
    - Trainable census assert `r * n_layer * 18 * n_embd` present in tests/test_lora_inject.py (grep -q "18" tests/test_lora_inject.py)
    - `.venv/bin/pytest tests/test_lora_inject.py -q -x` exits 0 with >= 8 tests collected
    - `make test` exits 0 (full existing suite green — no v1.0 regression)
  </acceptance_criteria>
  <done>Vanilla→load→inject→freeze ordering produces a model with exactly 6*n_layer LoRALinear wrappers, bit-identical logits, intact weight tying, the closed-form trainable census, and a key-audited adapter-apply path; full suite green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adapter dict → model.load_state_dict | adapter tensors (eventually from a shared persona file) cross into model weights here |
| injection target selection | a wrong allowlist silently corrupts the tied embedding for every token |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | inject_lora target selection | mitigate | explicit TARGET_PROJECTIONS allowlist (never isinstance scan); post-injection data_ptr test pins lm_head/wte untouched (Task 2 Test 3) |
| T-09-02 | Tampering | load_adapter_weights | mitigate | exact key-set equality audit raising ValueError before any load; bare strict=False banned (Task 2 Test 7) |
| T-09-03 | Information disclosure | lora_state_dict | mitigate | filter pins that only "lora_" keys leave the model — base weights never enter the shareable adapter dict (Task 2 Test 6) |
| T-09-SC | Tampering | pip/npm installs | accept | zero packages installed this phase (RESEARCH Package Legitimacy Audit: nothing to install; pinned v1.0 venv only) |
</threat_model>

<verification>
- `.venv/bin/pytest tests/test_lora_layer.py tests/test_lora_inject.py -q -x` green (< 30 s)
- `make test` green — all existing v1.0 tests pass (no module outside `lora/` and `tests/` was touched)
- `make lint` clean (ruff check + format, line-length 100)
- `from personacore.lora import LoRAConfig, LoRALinear, inject_lora, mark_only_lora_trainable, snapshot_params, lora_state_dict, load_adapter_weights` imports without error in `.venv`
</verification>

<success_criteria>
- ROADMAP success criterion 1 (init bit-identity) pinned: injected-at-init logits torch.equal vanilla
- ROADMAP success criterion 2 (tied embedding never wrapped) pinned by post-injection data_ptr test
- ROADMAP success criterion 5 (param-count formula + load→inject→freeze ordering) pinned by unit tests
- Zero edits to src/personacore/model/gpt.py, training/loop.py, or checkpoint.py
</success_criteria>

<output>
Create `.planning/phases/09-lora-core/09-01-SUMMARY.md` when done
</output>
