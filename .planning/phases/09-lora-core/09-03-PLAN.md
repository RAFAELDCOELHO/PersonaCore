---
phase: 09-lora-core
plan: 03
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - src/personacore/checkpoint.py
  - tests/test_lora_artifact.py
autonomous: true
requirements: [LORA-03]

must_haves:
  truths:
    - "adapter.pt loads under torch.load(weights_only=True) through the load_adapter choke point with exact key set {schema_version, adapter, lora_config, base_fingerprint}"
    - "load_adapter raises ValueError on a wrong schema_version, naming the actual value and the expected ADAPTER_SCHEMA_VERSION"
    - "a mismatched base_fingerprint emits a UserWarning but the artifact still loads (warn-but-load, D-02); a matching fingerprint emits no warning"
    - "two-artifact load (load_slim + load_adapter + inject + key-audited apply) reproduces the exporting model's logits bit-identically (torch.equal) — the D-03 slim-contract compatibility, with no merged-slim export"
    - "no base weight ever appears in the artifact's adapter dict — every adapter key contains lora_"
  artifacts:
    - path: "src/personacore/checkpoint.py"
      provides: "ADAPTER_SCHEMA_VERSION + export_adapter/load_adapter beside the slim pair (additive only)"
      contains: "ADAPTER_SCHEMA_VERSION"
    - path: "tests/test_lora_artifact.py"
      provides: "LORA-03 pins (weights_only round-trip, schema raise, fingerprint warn, no-leak, two-artifact load)"
      min_lines: 90
  key_links:
    - from: "src/personacore/checkpoint.py"
      to: "restricted unpickler safe-load bar (D-01)"
      via: "load_adapter choke point"
      pattern: "weights_only=True"
    - from: "src/personacore/checkpoint.py"
      to: "D-02 warn-but-load"
      via: "warnings.warn on fingerprint mismatch"
      pattern: "warnings\\.warn"
    - from: "tests/test_lora_artifact.py"
      to: "src/personacore/lora/inject.py"
      via: "key-audited apply (load_adapter_weights from 09-01)"
      pattern: "load_adapter_weights"
---

<objective>
Ship the persona-file artifact: `export_adapter`/`load_adapter` in `checkpoint.py` beside the
slim pair, with `ADAPTER_SCHEMA_VERSION`, the `weights_only=True` safe-load bar (D-01), the
base-fingerprint warn-but-load contract (D-02), and the end-to-end two-artifact load
(`load_slim` + `load_adapter` + inject) that defines "compatible with the slim contract" (D-03).

Purpose: LORA-03. The adapter artifact IS the privacy narrative made concrete — a small,
swappable, deletable persona file that meets the same zero-code-execution load bar as
`model_slim.pt`. Phase 14 loads user-taught adapters through exactly this choke point.

Output: additive `src/personacore/checkpoint.py` change (+1 constant, +2 functions) and
`tests/test_lora_artifact.py`, all green; every existing checkpoint/slim test stays green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-lora-core/09-CONTEXT.md
@.planning/phases/09-lora-core/09-RESEARCH.md
@.planning/phases/09-lora-core/09-PATTERNS.md
@.planning/phases/09-lora-core/09-01-SUMMARY.md
@src/personacore/checkpoint.py

<interfaces>
<!-- Key contracts extracted from the codebase. Use directly — no exploration needed. -->

From src/personacore/checkpoint.py (the file being extended — additive ONLY):
```python
CKPT_SCHEMA_VERSION = 1                                     # line 29
SLIM_SCHEMA_VERSION = 1  # slim INFERENCE artifact schema   # line 30 — add ADAPTER_SCHEMA_VERSION below

def export_slim(full_path, slim_path, *, map_location="cpu") -> dict:   # line 115
    # builds dict with "schema_version" FIRST, torch.save, RETURNS the dict for print/inspect
    # provenance trio carried: "model_config" / "git_sha" / "step" (+ "val_loss")     # lines 135-142

def load_slim(path, *, map_location="cpu") -> dict:         # line 147 — the template to mirror
    loaded = torch.load(path, map_location=map_location, weights_only=True)
    if loaded.get("schema_version") != SLIM_SCHEMA_VERSION:
        raise ValueError(
            f"Unsupported slim checkpoint schema_version {loaded.get('schema_version')!r} in "
            f"{path} (expected {SLIM_SCHEMA_VERSION}). Re-export with scripts/export_slim.py."
        )
    return loaded
```
Existing imports in checkpoint.py: `import random`, `from dataclasses import asdict`,
`import numpy as np`, `import torch` — add `import warnings` to the stdlib group.

From src/personacore/lora (09-01 — used by the tests, NEVER imported by checkpoint.py):
```python
inject_lora(model, cfg: LoRAConfig) -> int
lora_state_dict(model) -> dict[str, Tensor]       # filters keys containing "lora_"
load_adapter_weights(model, artifact: dict) -> None  # exact key-set audit, then strict=False load;
                                                     # reads artifact["adapter"]
LoRAConfig(r=8, alpha=16.0, dropout=0.0, targets=TARGET_PROJECTIONS)
```

Artifact schema (VERIFIED weights_only=True round-trip in 09-RESEARCH.md Pattern 4):
```python
{
  "schema_version": 1,                                   # ADAPTER_SCHEMA_VERSION
  "adapter": {"blocks.0.attn.q_proj.lora_A": Tensor, ...},
  "lora_config": {"r": 8, "alpha": 16.0, "dropout": 0.0, "targets": ("q_proj", ...)},  # asdict()
  "base_fingerprint": {"git_sha": "<sha>", "step": 5000, "val_loss": 0.7378},          # D-02
}
```

From tests/test_slim_checkpoint.py: _tiny_config (lines 49-52), module-scoped
tmp_path_factory artifact fixture (lines 71-95), raw-safe-load-is-the-assertion idiom
(lines 98-104), skipif-gated real-artifact test (lines 168-178).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: export_adapter/load_adapter choke point in checkpoint.py + artifact contract tests</name>
  <files>src/personacore/checkpoint.py, tests/test_lora_artifact.py</files>
  <read_first>
    - src/personacore/checkpoint.py (FULL file — the additive change must mirror export_slim/load_slim style verbatim; module docstring's trusted-vs-safe-load split)
    - .planning/phases/09-lora-core/09-RESEARCH.md (Pattern 4 + "Code Examples → Adapter artifact save/load" lines 421-441 — verified reference implementation)
    - .planning/phases/09-lora-core/09-PATTERNS.md (checkpoint.py section + test_lora_artifact.py section — fixture shape, ADAPTER_KEYS constant style, raise style)
    - tests/test_slim_checkpoint.py (module-scoped artifact fixture lines 71-95; raw safe-load assertion lines 98-104; provenance round-trip lines 122-129)
    - src/personacore/lora/config.py (LoRAConfig — what asdict() produces for the metadata)
  </read_first>
  <behavior>
    tests/test_lora_artifact.py (write FIRST; CPU-only; module docstring citing LORA-03/D-01/D-02;
    module-level constant ADAPTER_KEYS = {"schema_version", "adapter", "lora_config", "base_fingerprint"};
    module-scoped fixture via tmp_path_factory: torch.manual_seed(1234), tiny GPT
    ModelConfig(block_size=32, n_layer=1, n_head=2, n_embd=16), inject_lora(LoRAConfig(r=4)),
    nudge every lora_B nonzero, FP = {"git_sha": "abc1234", "step": 5000, "val_loss": 0.7378},
    art = export_adapter(path, adapter=lora_state_dict(model), lora_config=asdict(lora_cfg),
    base_fingerprint=FP); fixture yields (model, lora_cfg, path, art, FP)):
    - Test 1 (raw safe-load IS the assertion — T-09-07): torch.load(path, map_location="cpu",
      weights_only=True) SUCCEEDS (restricted unpickler accepts the whole artifact) and
      set(loaded.keys()) == ADAPTER_KEYS.
    - Test 2 (no base-weight leak): every key in loaded["adapter"] contains "lora_"; no key contains
      ".base."; tensor count == 2 * 6 * cfg.n_layer.
    - Test 3 (schema-version raise): torch.save a copy of the artifact with schema_version 999 to a
      second tmp path; pytest.raises(ValueError, match="schema_version") on load_adapter.
    - Test 4 (fingerprint warn-but-load — D-02): load_adapter(path, expected_fingerprint=
      {"git_sha": "different", "step": 1, "val_loss": 0.0}) raises NO exception, emits
      pytest.warns(UserWarning), and the returned dict's adapter tensors torch.equal the originals.
    - Test 5 (matching fingerprint is silent): with warnings.catch_warnings(record=True) as w +
      warnings.simplefilter("always"): load_adapter(path, expected_fingerprint=FP); assert len(w) == 0.
    - Test 6 (export returns the dict + config round-trip): export_adapter's return value is the dict
      it wrote (export_slim precedent); LoRAConfig(**loaded["lora_config"]) reconstructs with r == 4,
      alpha == its export value, and tuple(targets) == TARGET_PROJECTIONS.
    - Test 7 (provenance trio round-trip): loaded["base_fingerprint"] == FP — git_sha/step/val_loss
      travel through the artifact byte-for-byte.
  </behavior>
  <action>
    Extend src/personacore/checkpoint.py — ADDITIVE ONLY (save/load_checkpoint, export_slim,
    load_slim are byte-untouched; tests/test_checkpoint.py + tests/test_slim_checkpoint.py must
    stay green):
    - Add `import warnings` to the stdlib import group (ruff I ordering).
    - Add `ADAPTER_SCHEMA_VERSION = 1` directly below SLIM_SCHEMA_VERSION (line 30), with an
      inline comment: adapter "persona file" schema (LORA-03 / D-01), independent of full + slim.
    - def export_adapter(path, *, adapter, lora_config, base_fingerprint) -> dict: build
      {"schema_version": ADAPTER_SCHEMA_VERSION, "adapter": adapter, "lora_config": lora_config,
      "base_fingerprint": base_fingerprint}, torch.save, return the dict (export_slim's
      return-what-shipped precedent). Docstring: the persona file — A/B tensors + primitive metadata
      only, designed to be swapped/shared/deleted, meets the slim safe-load bar (D-01); the caller
      supplies plain dicts — checkpoint.py NEVER imports lora/ (locked dependency direction; mirrors
      export_slim taking a path, not a model). Callers produce `adapter` via
      personacore.lora.lora_state_dict and `lora_config` via dataclasses.asdict.
    - def load_adapter(path, *, expected_fingerprint=None, map_location="cpu") -> dict:
      torch.load(path, map_location=map_location, weights_only=True) — the SINGLE choke point
      (D-01; every adapter consumer goes through here, verbatim load_slim discipline). Validate
      `loaded.get("schema_version") != ADAPTER_SCHEMA_VERSION` -> raise ValueError naming the actual
      value, the expected value, and the remedy ("re-export with personacore.checkpoint.export_adapter"),
      mirroring load_slim's message shape. When expected_fingerprint is not None and
      loaded["base_fingerprint"] != expected_fingerprint: warnings.warn(...) naming BOTH fingerprints
      and "loading anyway (D-02 — base evolves mid-milestone)"; then return loaded. NO hard error on
      mismatch (D-02 is locked: warn but load).
    Run .venv/bin/pytest tests/test_lora_artifact.py -q -x — green. Commands run inside .venv
    (Python 3.11; system 3.14 is unsupported).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_lora_artifact.py -q -x</automated>
  </verify>
  <acceptance_criteria>
    - src/personacore/checkpoint.py contains `ADAPTER_SCHEMA_VERSION = 1`, `def export_adapter(`, and `def load_adapter(`
    - load_adapter loads with the restricted unpickler via the verbatim call site: `grep -c "torch.load(path, map_location=map_location, weights_only=True)" src/personacore/checkpoint.py` returns 2 (load_slim + the new load_adapter; the bare literal also lives in docstrings/comments, so the count anchors on the full call expression)
    - checkpoint.py contains `warnings.warn` and `import warnings`
    - checkpoint.py never imports the lora package: grep -n "lora" src/personacore/checkpoint.py shows no `import` line referencing personacore.lora (comments/docstrings mentioning lora_config metadata are fine)
    - tests/test_lora_artifact.py defines ADAPTER_KEYS == {"schema_version", "adapter", "lora_config", "base_fingerprint"} and contains pytest.warns(UserWarning)
    - `.venv/bin/pytest tests/test_lora_artifact.py -q -x` exits 0 with >= 7 tests collected
    - `.venv/bin/pytest tests/test_checkpoint.py tests/test_slim_checkpoint.py -q -x` exits 0 (existing artifact tests untouched by the additive change)
  </acceptance_criteria>
  <done>The persona-file artifact round-trips under weights_only=True through the load_adapter choke point with schema-version raise, D-02 warn-but-load fingerprint semantics, no base-weight leak, and byte-identical provenance</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Two-artifact load end-to-end (D-03) + real-slim skipif variant + full suite</name>
  <files>tests/test_lora_artifact.py</files>
  <read_first>
    - tests/test_lora_artifact.py (Task 1 state — extend the same file, reuse its fixture/constants)
    - tests/test_slim_checkpoint.py (full-ckpt fixture construction for export_slim input; skipif real-artifact test lines 168-178)
    - src/personacore/checkpoint.py (save_checkpoint signature — model/optimizer/scheduler/step/model_config/train_config/git_sha; export_slim/load_slim; the new export_adapter/load_adapter)
    - src/personacore/lora/inject.py (load_adapter_weights — the key-audited apply from 09-01; inject_lora)
  </read_first>
  <behavior>
    Extend tests/test_lora_artifact.py with the D-03 "compatible with the slim contract" proof:
    - Test 8 (two-artifact mechanism load, tiny fixtures, tmp_path):
      (1) DONOR: torch.manual_seed(1234), tiny GPT; save_checkpoint(full_path, model=..., optimizer=
      torch.optim.AdamW(model.parameters()), scheduler=None, step=0, model_config=cfg,
      train_config=TrainConfig(), git_sha="testsha"); export_slim(full_path, slim_path).
      (2) loaded_slim = load_slim(slim_path); donor = GPT(ModelConfig(**loaded_slim["model_config"]));
      donor.load_state_dict(loaded_slim["model"]); inject_lora(donor, LoRAConfig(r=4)); nudge every
      lora_B nonzero; export_adapter(adapter_path, adapter=lora_state_dict(donor),
      lora_config=asdict(lora_cfg), base_fingerprint={"git_sha": loaded_slim["git_sha"],
      "step": loaded_slim["step"], "val_loss": loaded_slim["val_loss"]}); capture donor logits on a
      fixed batch under torch.no_grad.
      (3) CONSUMER (fresh objects, nothing shared): load_slim(slim_path) -> rebuild GPT from the
      embedded model_config -> load_state_dict -> art = load_adapter(adapter_path,
      expected_fingerprint=<same trio>) -> inject_lora(consumer, LoRAConfig(**art["lora_config"]))
      -> load_adapter_weights(consumer, art) -> consumer logits torch.equal donor logits
      (bit-identity: same base tensors + same adapter tensors + same fp32 math).
    - Test 9 (real-artifact variant, skipif-gated): @pytest.mark.skipif(not REAL_SLIM.exists(),
      reason="real slim artifact not present (CI)") where REAL_SLIM = Path("checkpoints/model_slim.pt");
      load_slim(REAL_SLIM) -> rebuild the real 13.9M GPT -> inject_lora(LoRAConfig()) asserting the
      return value == 6 * model_config["n_layer"] -> build + export + load_adapter + 
      load_adapter_weights through a tmp adapter -> one no_grad forward on a small batch succeeds
      (shape sanity (B, T, vocab_size)); CPU-only.
  </behavior>
  <action>
    Add the two tests above to tests/test_lora_artifact.py. Imports: GPT from personacore.model,
    ModelConfig/TrainConfig from personacore.config, save_checkpoint/export_slim/load_slim/
    export_adapter/load_adapter from personacore.checkpoint, inject_lora/lora_state_dict/
    load_adapter_weights/LoRAConfig from personacore.lora (tests use absolute imports per house
    convention). The two-artifact flow is the LITERAL D-03 contract — load_slim(model_slim.pt) +
    load_adapter(adapter.pt) + inject; no merged-slim export anywhere (deferred per CONTEXT).
    Then run the FULL suite (make test) and make lint — checkpoint.py is the only v1.0 module this
    plan touches and the change is additive, so every existing test must stay green.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_lora_artifact.py -q -x && make test && make lint</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_lora_artifact.py contains a test calling load_slim, load_adapter, inject_lora, AND load_adapter_weights in one flow (the D-03 two-artifact load) ending in a torch.equal logits assert
    - tests/test_lora_artifact.py contains `@pytest.mark.skipif` gated on checkpoints/model_slim.pt existence
    - grep -q "export_slim" tests/test_lora_artifact.py succeeds (slim-contract compatibility is exercised, not assumed)
    - `.venv/bin/pytest tests/test_lora_artifact.py -q -x` exits 0 with >= 9 tests collected
    - `make test` exits 0 and `make lint` exits 0
  </acceptance_criteria>
  <done>Two-artifact load (load_slim + load_adapter + inject + key-audited apply) reproduces the exporting model's logits bit-identically on tiny fixtures, and the real model_slim.pt variant passes locally; full suite + lint green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adapter.pt → load_adapter | the persona file is the artifact DESIGNED to be swapped/shared — foreign bytes cross into the process here |
| artifact metadata → injection config | lora_config from the file drives module-tree mutation on the consumer's model |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-07 | Elevation of privilege | load_adapter deserialization | mitigate | torch.load(weights_only=True) single choke point — restricted unpickler, zero code execution on load (D-01); the raw safe-load success IS a test assertion (Task 1 Test 1) |
| T-09-08 | Tampering | schema/key smuggling in artifact | mitigate | schema_version ValueError before any consumption; downstream apply goes through 09-01's load_adapter_weights exact key-set audit; wrong shapes raise in load_state_dict (Task 1 Test 3, Task 2 Test 8) |
| T-09-09 | Tampering/Integrity | adapter loaded onto wrong base | mitigate | D-02 base fingerprint (git_sha + step + val_loss) compared on load; UserWarning names both fingerprints — warn-but-load is the LOCKED user decision, not an oversight (Task 1 Tests 4/5) |
| T-09-10 | Information disclosure | base weights leaking into the shareable file | mitigate | adapter dict produced only by lora_state_dict's "lora_" filter; no-".base."-key + tensor-count test (Task 1 Test 2) |
| T-09-SC | Tampering | pip/npm installs | accept | zero packages installed this phase (RESEARCH Package Legitimacy Audit: nothing to install) |
</threat_model>

<verification>
- `.venv/bin/pytest tests/test_lora_artifact.py -q -x` green (< 30 s)
- `make test` green — tests/test_checkpoint.py and tests/test_slim_checkpoint.py prove the checkpoint.py change is purely additive
- `make lint` clean
- `from personacore.checkpoint import ADAPTER_SCHEMA_VERSION, export_adapter, load_adapter` imports without error in `.venv`
- `grep -c "torch.load(path, map_location=map_location, weights_only=True)" src/personacore/checkpoint.py` returns 2 (the load_slim + load_adapter call sites — exactly the two safe-load choke points). The check anchors on the verbatim call expression because the bare literal `weights_only=True` already appears in docstrings/comments (8 matches pre-change); never strip documented safety comments to satisfy a count
</verification>

<success_criteria>
- ROADMAP success criterion 3 (adapter saves/loads as a separate small artifact compatible with open-dict checkpoints and the LOCKED weights_only=True slim contract) pinned end-to-end
- D-01 (safe-load bar + ADAPTER_SCHEMA_VERSION + choke point), D-02 (fingerprint warn-but-load), D-03 (two-artifact load, no merged-slim export) implemented exactly as locked
- checkpoint.py change is additive: zero edits to save_checkpoint/load_checkpoint/export_slim/load_slim; checkpoint.py does not import lora/
</success_criteria>

<output>
Create `.planning/phases/09-lora-core/09-03-SUMMARY.md` when done
</output>
