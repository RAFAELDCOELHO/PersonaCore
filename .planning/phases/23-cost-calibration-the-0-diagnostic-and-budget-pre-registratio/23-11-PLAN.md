---
phase: 23-cost-calibration-the-0-diagnostic-and-budget-pre-registratio
plan: 11
type: execute
wave: 14
depends_on: ["23-19", "23-20"]
files_modified:
  - tests/test_phase23_matched.py
  - scripts/phase23_run.py
  - tests/test_phase23_cost.py
  - results/phase23_cost.json
  - results/phase23_noised_dp_n64_sigma<σ>.json
  - results/phase23_cost_run.log
  - data/phase23_run_state.json
autonomous: true
requirements: [CAL-01, CAL-05]

must_haves:
  truths:
    - "THE D-04 GATE READS `results/phase23_matched_verdict.json`, NEVER `results/phase23_sigma_zero.json`. MEASURED at HEAD: the σ=0 record carries `verdict == 'HALT'` and the matched-verdict record carries `verdict == 'proceed'`. 23-19 left the σ=0 record byte-unchanged ON PURPOSE so a reader sees both verdicts side by side, so the σ=0 record will NEVER say `proceed` and a gate pointed at it can never open."
    - "THE GATE IS TWO CONJUNCTS, NOT ONE. `results/phase23_matched_verdict.json`'s own `governs` field says *'THIS RECORD DOES NOT UNBLOCK ANYTHING … unblocking them is a separate, later act taken by a human'*. So the verdict alone is not the release condition: the release condition is the verdict AND the committed human unblock act (`.planning/STATE.md` + `.planning/ROADMAP.md` at `746ecf6`, dated 2026-08-28). A gate reading only the verdict would contradict the record it reads."
    - "`test_no_noised_point_exists` BINDS on this plan and is corrected ADDITIVELY, never deleted or weakened. It is WIDENED — the `== []` assertion is retained verbatim on the still-blocked branch, and a new branch, reachable only from the COMMITTED human act, asserts strictly more. 23-20's own precedent: *'One pre-existing test was widened, not deleted.'*"
    - "CAL-01: the training leg is measured ON THE DP PATH WITH THE SEAM ACTIVE, per CAPACITY. `grad_accum_steps = n_facts` makes one optimizer step cost n_facts backward passes plus ceil(4*n_facts/batch_size) replay micro-batches."
    - "CAL-01: EVERY timing key in the cost record carries a `protocol` field naming which training protocol produced it. A timing that does not say which protocol it timed is not a usable figure — the two measured non-DP protocols differ in wall clock by `training.non_dp.wall_clock_gap_vs_superseded`, MEASURED 2.035849685343305."
    - "EVERY FIGURE THIS PHASE PUBLISHES IS A NAMED NUMERIC FIELD AT FULL STORED PRECISION, COMPUTED AND NEVER TYPED. The wall-clock gap between the two non-DP protocols previously existed ONLY inside the prose of `training.non_dp.provenance_argument` — where it is not a scalar leaf, cannot be quoted from, and cannot be re-derived. It is now the field `training.non_dp.wall_clock_gap_vs_superseded`, computed at write time as `training.non_dp.training_seconds_mean / training.non_dp_superseded_protocol.training_seconds_mean`. A ROUNDING is not a figure this phase may publish: 2.04 is not 2.035849685343305, and 161.12 is not 161.12400419991462."
    - "CAL-05: 4.77 h/point is a FLOOR for noised points, not a mean. The record carries `h_per_point_floor` AND `h_per_point_ceiling` as distinct required keys with no bare mean, and Z is sized against the CEILING because the K ratchet only permits increases."
    - "This run is a REAL SWEEP POINT and is filed as one. It exports an adapter, scores real questions and runs the unmonkeypatched production budget, so it fails all three legs of `scripts/phase23_prereg.py:37-42`'s `sweep_point: false` exemption and cannot claim it."
  artifacts:
    - path: "results/phase23_cost.json"
      provides: "the CAL-01 training legs per capacity WITH THEIR PROTOCOLS, the CAL-05 floor/ceiling bracket, and every published figure as a NAMED numeric field at full stored precision — including the computed `wall_clock_gap_vs_superseded`"
      # DISCRIMINATING BY CONSTRUCTION: `h_per_point_ceiling` is forced anyway by
      # `phase23_cost.GENERATION_RECORD_KEYS`, so asserting it proves nothing about THIS
      # revision.
      # W5 — THE JUSTIFICATION IS THE ARTIFACT, NOT THE REPO. An earlier version of this
      # comment claimed `grep -rc wall_clock_gap_vs_superseded results/ scripts/ tests/
      # .planning/` returns 0 EVERYWHERE. That was true when written and is now FALSE:
      # RE-MEASURED at HEAD it returns 11 in `23-11-PLAN.md` and 7 in `23-12-PLAN.md`,
      # because both plans now name the key in prose. The `contains:` is unaffected and
      # still discriminating — it binds on `results/phase23_cost.json`, which does NOT
      # exist at HEAD — so this key can only appear there if the named-numeric-field
      # decision was actually implemented. Comment corrected; the criterion is unchanged.
      contains: "wall_clock_gap_vs_superseded"
    - path: "tests/test_phase23_matched.py"
      provides: "the additively widened blocking guard, with the human unblock act named and its own watched-RED tripwire"
      contains: "def test_the_unblock_branch_needs_the_committed_act"
      # DISCRIMINATING BY CONSTRUCTION: `def test_no_noised_point_exists` is ALREADY true at HEAD
      # (`tests/test_phase23_matched.py:911`), so asserting it would pass against an untouched file.
      # The tripwire's name is MEASURED absent at HEAD (`grep -c` returns 0), so this contains:
      # can only go true if Task 1 actually ran.
    - path: "scripts/phase23_run.py"
      provides: "the `noised` and `throughput` sub-modes"
      contains: "h_per_point_floor"
  key_links:
    - from: "scripts/phase23_run.py"
      to: "results/phase23_matched_verdict.json"
      via: "the D-04 gate reads the MATCHED verdict record's `verdict` field, and cross-checks its `sigma_zero_record_file_sha256` against the live σ=0 file so a re-run σ=0 record cannot manufacture a release"
      pattern: "matched_verdict"
    - from: "scripts/phase23_run.py"
      to: "scripts/phase23_cost.py::validate_record"
      via: "every emitted record is validated before it is written; a bare mean key or a missing denominator refuses"
      pattern: "validate_record"
    - from: "results/phase23_cost.json"
      to: "results/phase18_preflight_report.md"
      via: "the measured floor-side rate is cross-validated against the committed artifact's per-shape rates, and the agreement percentage is recorded"
      pattern: "phase18_preflight"
---

<objective>
Open the D-04 gate against the artifact that actually carries the verdict, correct the committed
guard whose premise the human unblock act falsified, and then take the two measurements that size
the sweep: the DP training leg at the expensive capacity, and generation throughput on a genuinely
noised adapter — bracketed floor to ceiling rather than reported as a mean.

Purpose: CAL-01 and CAL-05 are the last two open cost requirements. The roadmap prices a 16-point
n=64 sweep's training leg at `16 x 17 s`; nothing measured in this phase reproduces 17 s for any
real arm. The committed 4.77 h/point generation figure was measured on the un-adapted base where
45-56 of 64 draws terminated on a stop id; a noised adapter that stops emitting EOS runs every draw
to the full 48 tokens. Both legs need a real measurement and this plan takes them.

Both live in one plan because one `dp_n64` run at σ > 0 produces both: the training wall clock
CAL-01 asks for, and the noised adapter CAL-05 must re-measure throughput on.

Output: `results/phase23_cost.json`, the noised run's own record under
`phase23_prereg.noised_record_path(...)`, and the widened blocking guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-cost-calibration-the-0-diagnostic-and-budget-pre-registratio/23-CONTEXT.md
@.planning/phases/23-cost-calibration-the-0-diagnostic-and-budget-pre-registratio/23-RESEARCH.md
@.planning/phases/23-cost-calibration-the-0-diagnostic-and-budget-pre-registratio/23-VALIDATION.md
@.planning/phases/23-cost-calibration-the-0-diagnostic-and-budget-pre-registratio/deferred-items.md
</context>

<environment>
**Python 3.11 venv is MANDATORY — `.venv/bin/python`, never a bare `python`. This plan MUST run on
the M3 (D-01).**

**THE GATE ARTIFACT — MEASURED AT HEAD, DO NOT TRUST A RETYPED PATH.**

| record | `verdict` |
|---|---|
| `results/phase23_sigma_zero.json` | `"HALT"` |
| `results/phase23_matched_verdict.json` | `"proceed"` |

The σ=0 record was deliberately left byte-unchanged by 23-19 so a reader sees both verdicts side by
side, so it will never say `proceed`. **Every D-04 gate in this plan reads
`results/phase23_matched_verdict.json`.** Re-verify both with
`.venv/bin/python -c "import json;print(json.load(open('results/phase23_sigma_zero.json'))['verdict'], json.load(open('results/phase23_matched_verdict.json'))['verdict'])"`
before Task 1 writes a line.

**THE RUN MUST BE LAUNCHED DETACHED.** `dp_n64` training plus the throughput probe is well over the
600 s foreground cap, and 23-17's run was harness-killed at ~60 min. Task 2 reuses 23-20's
**measured** launch recipe verbatim (`23-20-PLAN.md:985-1012`). Three machine facts that recipe
exists for: `setsid(1)` does not exist on macOS (`command -v setsid` exits 1, so `nohup setsid …`
exits 127 while still printing a plausible pid); BSD `ps` has **no `sid` keyword** and
`ps -o pid,pgid,sid` fails with **exit 0**, so a probe written that way passes vacuously; and `$!`
after `cd … && nohup … &` is the SUBSHELL's pid, not the run's. The pid comes from the log's first
line. **NOTE: 23-20's recipe is documented in that PLAN, not in `scripts/phase23_run.py` — measured,
`grep -c "setsid" scripts/phase23_run.py` returns 0. Copy it from the plan.**

**THE LOG PATH IS `results/phase23_cost_run.log` AND MUST NOT MATCH `results/phase23_noised_*`.**
Same reasoning 23-20 used for `results/phase23_resume_run.log`: a log inside
`phase23_prereg.NOISED_RECORD_GLOB` becomes a member of the glob every ordering guard binds on, and
`tests/test_phase23_prereg.py`'s `_prove_noised_record_is_under_the_glob` machinery would then be
reasoning about a text file.

**Expected wall clock, stated as an interval and not as a bound.** Research projects `dp_n64` at
~30 min, but 23-10 MEASURED that the same projection over-states the `dp_n8` figure by at least
10.8% (`23-RESEARCH.md:665-685`, `RETRACTED IN PLACE`), so it is unreliable in both directions.
Budget 30-60 min for training and 15-25 min for the throughput probe. Detached either way.

**FROZEN — no write, and every instruction in this plan is read-only against them:**
`scripts/phase23_prereg.py` (byte-identical to `c7de5d4`), `scripts/phase23_matched_prereg.py`
(byte-identical to `c100388`, EXACTLY 1 commit — a second commit permanently reddens
`_assert_ordering_holds`' ancestry conjunct and delete-and-re-add cannot undo it),
`scripts/phase23_resume_prereg.py` (1 commit, same hazard), `scripts/mitigation_accountant.py`,
`scripts/mitigation_gate.py`, `scripts/phase18_extraction.py`, `scripts/mitigation_budget.py`
(23-13's, not this plan's), and every committed `results/phase23_*` artifact.

**Suite baseline at HEAD: `1549 passed, 1 skipped`. `ruff` clean over 219 files.** A different count
is recorded with its cause in the SUMMARY.
</environment>

<tasks>

<task type="auto">
  <name>Task 1: Widen the blocking guard additively, and build the two-conjunct D-04 gate</name>
  <files>tests/test_phase23_matched.py</files>
  <read_first>
    - `tests/test_phase23_matched.py:911-929` — `test_no_noised_point_exists`. **Read the docstring
      in full.** It is deliberately UNCONDITIONAL on the verdict and states why: *"A `proceed` does
      not unblock the sweep — unblocking is a separate, later human act."*
    - `.planning/STATE.md:62-101` — the dated `UNBLOCKED 2026-08-28` record and the paragraph above
      it explaining what `proceed` did NOT do. **This is the human act the guard was waiting for.**
      Locate the sentinel by TEXT, never by line number
    - `git show --stat 746ecf6` and `git log -1 --format='%H %ad %s' 746ecf6` — the commit that IS
      the human act: `docs(23): pre-register CONTROL PROVENANCE, and unblock 23-11..23-14`,
      2026-08-28, four planning files, **no source file changed**
    - `scripts/phase23_prereg.py:37-42` — the `CAL03_WIRING_RECORD` exemption and its stated
      criterion. **Read it before deciding whether this plan's run is exempt**
    - `tests/test_phase23_prereg.py:578` — `_prove_noised_record_is_under_the_glob`'s `def`, and its
      watched cases at `:636` and `:648`. **MEASURED — the earlier draft cited `:595-665`, which
      starts 17 lines INSIDE the function and misses the docstring that states the rule.** Resolve
      by SYMBOL NAME, not by these numbers either. This is the committed rule that a σ>0 record
      either lives under the glob or declares `sweep_point: false`; there is no third door
    - `results/phase23_matched_verdict.json` — `verdict`, `governs`, `sigma_zero_record_file_sha256`,
      `control_record_file_sha256`, `floor`, `deviation`
    - `.planning/phases/23-*/23-20-SUMMARY.md` — the "widened, not deleted" precedent on
      `test_matched_record_records_the_attempt_state`, and T-23-77's disclosure register
  </read_first>
  <action>
**(a) Establish whether the guard binds, in the SUMMARY, before editing anything.** The answer is
YES and it is measurable three ways; record all three:

  1. This plan's Task 2 emits its record at `phase23_prereg.noised_record_path("dp_n64", σ)`, which
     is by construction under `NOISED_RECORD_PREFIX`, and commits it. `git ls-files` goes non-empty
     at that commit and the guard goes RED. The plan's old frontmatter did not list that path; the
     plan body always did, and the body is what runs.
  2. The `CAL03_WIRING_RECORD` precedent does NOT exempt it. That exemption's stated criterion is
     three substantive legs plus one declarative: *exports no adapter, scores no question, runs a
     toy `ModelConfig` under `max_steps_override`* — *and its own record declares `sweep_point:
     false` to say so; that declaration is what exempts it.* This run exports an adapter with its
     sha256, scores real questions on the real attack shapes in Task 3, and runs the unmonkeypatched
     production budget. It fails all three substantive legs and cannot honestly declare exemption.
  3. `tests/test_phase23_prereg.py`'s committed rule leaves no third door: a σ>0 record either lives
     under the glob or declares `sweep_point: false`, and this one may not declare it.

**(b) WIDEN the guard. Do not delete it, do not weaken it, do not add a force flag.**
`tests/test_phase23_matched.py` is a normal test file — MEASURED: 4 commits, and no pin registry or
ancestry guard names it (`_assert_ordering_holds` binds `scripts/phase23_*prereg.py` against the
`results/phase2{0,1,3}_*` globs, not this file). So 23-20's edit-once hazard does not apply here,
and the applicable precedent is 23-20's own on a non-pin test: *"widened, not deleted."*

Keep the function name `test_no_noised_point_exists`. Keep its existing `git ls-files` call and its
`tracked == []` assertion **verbatim on the still-blocked branch**. Add a dated block to the
docstring recording: what changed (the premise "unblocking is a separate, later human act" is now
satisfied), WHO changed it (the user, 2026-08-28, commit `746ecf6`), and that the original
assertion is retained rather than replaced.

The branch predicate is **the committed human act, never the verdict**. Define a module-level
sentinel constant holding a distinctive phrase from `.planning/STATE.md`'s dated unblock record, and
resolve the act from git rather than from the working tree: `git log -S<sentinel> --format=%H --
.planning/STATE.md` must return at least one commit, and that commit must be an ancestor of `HEAD`
(`git merge-base --is-ancestor`). Also require the sentinel present in `git show HEAD:.planning/STATE.md`
— the COMMITTED file, not the working tree — so an uncommitted edit cannot open the branch.

**W17 — `-S` RETURNS A SET, NOT A COMMIT, AND THIS PHASE'S OWN WORK GROWS IT.** MEASURED at HEAD:
`git log --oneline 746ecf6..HEAD -- .planning/STATE.md` is **EMPTY**, so today the search resolves to
exactly one commit and a `[0]`-style read happens to work. **That is a fact about today, not a
property.** `-S` reports every commit that changed the number of occurrences of the string, and
**23-12 Task 1 lists `.planning/STATE.md` in its `<files>`** and appends a continuation quoting this
record — so once 23-12 lands, and again on any later STATE.md edit touching it, the set has two or
more members and a positional read silently binds a different commit. **Never read an element by
position.** Assert `_UNBLOCK_COMMIT` is **AMONG** the returned shas (membership), and apply the
ancestry and act-shape conjuncts to `_UNBLOCK_COMMIT` itself — never to whichever sha the search
happened to list first. Put the returned set's size in the failure message and disclose it in the
SUMMARY, so a set that grew is visible rather than silently absorbed.

Resolve by TEXT throughout; `tests/test_phase20_prereg.py:125-170` records this repository's own
lesson that a line number survives no edit.

**THOSE THREE CONJUNCTS ARE NOT ENOUGH ON THEIR OWN, AND THE GAP IS EXPLOITABLE BY ROUTINE WORK.**
Nothing in `-S<sentinel>` + ancestry + `git show HEAD:` binds the resolved commit to the HUMAN act:
**ANY** commit that introduces that phrase into `.planning/STATE.md` satisfies all three, and agent
commits to `.planning/STATE.md` are routine in this phase — 23-12's own Task 1 lists that exact file
in its `<files>`. A guard that a downstream plan can satisfy as a side effect is not a provenance
guard. Add **both** of these, as two more conjuncts:

  - **PIN THE SHA.** A module-level constant
    `_UNBLOCK_COMMIT = "746ecf699904e7c97bf73614e1c617a646da30ad"` — the full 40-char sha, MEASURED,
    with a provenance comment naming the message (`docs(23): pre-register CONTROL PROVENANCE, and
    unblock 23-11..23-14`), the date (2026-08-28) and WHO (the user). Assert the commit resolved
    from the sentinel search **is** that sha. This is the same restate-a-frozen-identity-as-a-pin
    route the repository already uses for `scripts/phase23_prereg.py == c7de5d4`, and it fails loudly
    if history is ever rewritten under it, which is the correct behaviour for a release gate.
  - **ASSERT THE SHAPE OF THE ACT.** `git show --name-only --format= <sha>` must list **no path
    under `scripts/` or `src/`**. MEASURED at HEAD, `746ecf6` touched exactly four paths, all
    planning documents: `.planning/ROADMAP.md`, `.planning/STATE.md` and two phase
    `deferred-items.md`. A human unblock act is a documentation act; an agent's routine STATE.md
    commit carrying code alongside it is refused by this conjunct even if the sha pin were somehow
    satisfied.

Keep the ancestry check and the `git show HEAD:` check exactly as they are — they are what stops an
uncommitted or off-branch edit, and the sha pin does not subsume either.

On the UNBLOCKED branch assert **strictly more** than `== []` did, five conjuncts:

  1. the unblock commit exists, **is `_UNBLOCK_COMMIT`**, is an ancestor of HEAD, **touched no path
     under `scripts/` or `src/`**, and its date is recorded in the failure message — the human act,
     named and bound. All five sub-checks from (b), not just the sentinel search;
  2. `results/phase23_matched_verdict.json` carries `verdict == "proceed"` — the evidence the human
     read, read from the artifact whose `governs` field says the verdict alone unblocks nothing;
  3. that record's `sigma_zero_record_file_sha256` equals the LIVE sha256 of
     `results/phase23_sigma_zero.json` — so a re-run σ=0 record cannot manufacture a release;
  4. every tracked `results/phase23_noised_*` path is REPRODUCED by
     `phase23_prereg.noised_record_path(arm, sigma)` called with that record's own `arm` and `sigma`
     fields — a hand-typed sweep-point path is refused;
  5. `results/phase23_sigma_zero.json`'s first git add strictly precedes every noised record's
     (`git log --diff-filter=A`, taking the EARLIEST add on each side) — DPSGD-06's ordering,
     re-asserted at the one moment it can first be violated.

**(c) The tripwire.** Add `test_the_unblock_branch_needs_the_committed_act` — the watched RED,
parametrized over **three** constructed inputs, never by deleting or editing the real record:

  1. an absent sentinel (a scratch string) — proves sensitivity to PRESENCE;
  2. a sentinel-bearing commit sha that is **not** `_UNBLOCK_COMMIT` — proves sensitivity to
     PROVENANCE, which case 1 does not test at all;
  3. a changed-path list containing a `scripts/` path — proves the act-shape conjunct fires.

Assert the predicate reports "still blocked" in all three. **Case 1 alone is the defect this list
exists to close:** an absent-sentinel drive proves the guard notices a missing phrase, and says
nothing about whether some other commit could supply it. Same shape as 23-20's whole-block
substitution control, for the same reason: a branch nobody has watched refuse is a branch nobody has
tested. Drive the predicate through a seam that takes **three** arguments — the SENTINEL, the sha and
the changed-path list — so all three cases are constructed inputs rather than repository states.
**The sentinel must be the third parameter, not a module constant read from inside the predicate:**
case 1 drives an ABSENT sentinel, and a predicate that closes over `_UNBLOCK_SENTINEL` internally
gives the test no way to supply a scratch string short of monkeypatching the module — which tests the
patch, not the guard. Two-parameter seams satisfy cases 2 and 3 and leave case 1 undrivable.

**(d) The gate is now written once and reused.** Task 2's runtime `_prove` calls the same two
conjuncts — matched `verdict == "proceed"` AND the committed unblock act — so the test and the
driver cannot drift into two different gates.

**Prefer AST over grep in any structural criterion here.** This test file's own prose contains the
sentinel phrase and the substring `proceed`; a grep criterion over it would match its own docstring.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase23_matched.py -v -k "noised or unblock"</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY establishes BINDS-OR-NOT first, with all three measurements from (a) quoted, and states the route chosen (widen) with 23-20's precedent named
    - `.venv/bin/python -m pytest "tests/test_phase23_matched.py::test_no_noised_point_exists" -v` exits 0
    - `.venv/bin/python -m pytest "tests/test_phase23_matched.py::test_the_unblock_branch_needs_the_committed_act" -v` collects **3** cases (absent sentinel / wrong sha / a `scripts/` path in the act) and all pass
    - The sha is PINNED, not merely resolved: `.venv/bin/python -c "import ast,pathlib;t=ast.parse(pathlib.Path('tests/test_phase23_matched.py').read_text());print([n.value.value for n in t.body if isinstance(n,ast.Assign) and isinstance(n.value,ast.Constant) and isinstance(n.value.value,str) and n.value.value.startswith('746ecf69')])"` prints the full 40-char `746ecf699904e7c97bf73614e1c617a646da30ad`
    - The act-shape conjunct is real, verified against the pinned commit. **W4 — `grep -c` printing `0` EXITS 1**, so a bare `grep -c … ` reads as a FAILURE at exactly the moment it is reporting a PASS; capture the count and compare it instead: `[ "$(git show --name-only --format= 746ecf6 | grep -c -E '^(scripts|src)/')" = 0 ]` exits 0, and the SUMMARY quotes the four paths it did touch
    - The original assertion survives **BY IDENTITY, not by count.** The count check alone is satisfiable by six BRAND-NEW asserts with the original deleted, so it is kept only as a secondary reading and this identity check is the binding one — the `tracked == []` comparison must still exist inside that function: `.venv/bin/python -c "import ast,pathlib;t=ast.parse(pathlib.Path('tests/test_phase23_matched.py').read_text());f=[n for n in ast.walk(t) if isinstance(n,ast.FunctionDef) and n.name=='test_no_noised_point_exists'][0];print(any(isinstance(c,ast.Compare) and isinstance(c.left,ast.Name) and c.left.id=='tracked' and len(c.ops)==1 and isinstance(c.ops[0],ast.Eq) and isinstance(c.comparators[0],ast.List) and c.comparators[0].elts==[] for c in ast.walk(f)))"` prints **True**
    - Secondary reading, not a substitute for the identity check above: `.venv/bin/python -c "import ast,pathlib;t=ast.parse(pathlib.Path('tests/test_phase23_matched.py').read_text());f=[n for n in ast.walk(t) if isinstance(n,ast.FunctionDef) and n.name=='test_no_noised_point_exists'][0];print(sum(1 for n in ast.walk(f) if isinstance(n,ast.Assert)))"` prints **6 or more** (1 retained + 5 new)
    - `git diff --numstat -- tests/test_phase23_matched.py` shows the deletion count, and the SUMMARY accounts for **every** deleted line by name. A deletion touching the retained `tracked == []` assertion or its message is a FAILURE of this task
    - No force flag, override parameter or `skip`/`xfail` marker was added: `.venv/bin/python -c "import ast,pathlib;src=pathlib.Path('tests/test_phase23_matched.py').read_text();t=ast.parse(src);print([d.func.attr for n in ast.walk(t) if isinstance(n,ast.FunctionDef) for d in n.decorator_list if isinstance(d,ast.Call) and isinstance(d.func,ast.Attribute) and d.func.attr in ('skip','skipif','xfail')])"` prints `[]`
    - `git diff --exit-code -- scripts/phase23_prereg.py scripts/phase23_matched_prereg.py scripts/phase23_resume_prereg.py scripts/mitigation_gate.py scripts/mitigation_accountant.py scripts/mitigation_budget.py results/` exits 0
    - `git log --format=%H -- scripts/phase23_matched_prereg.py | wc -l | tr -d ' '` still prints **1**
  </acceptance_criteria>
  <done>The blocking guard is widened rather than deleted: the original `== []` assertion stands verbatim on the blocked branch, a new branch reachable only from the COMMITTED human unblock act asserts five stronger conjuncts, a constructed-input tripwire is watched refusing to open it, and every frozen pin is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: The first noised run — `dp_n64` at σ > 0, gated, timed, DETACHED</name>
  <files>scripts/phase23_run.py</files>
  <read_first>
    - `results/phase23_matched_verdict.json` — **the gate record.** `verdict`, `governs`,
      `sigma_zero_record_file_sha256`. **Read it first; stop if `verdict != "proceed"`**
    - `.planning/STATE.md` — the dated `UNBLOCKED 2026-08-28` record, resolved by TEXT
    - `scripts/phase23_run.py:2770-2800` — `_TABLE` and `USAGE`. **`cost` is ALREADY TAKEN** by
      23-08's control-scoring sub-mode; the new names are `noised` and `throughput`
    - `scripts/phase23_run.py:850-875` — `_prove_no_noised_record_exists`. **Do NOT call it from the
      `noised` sub-mode.** It refuses when the glob is non-empty, which is correct standing in front
      of σ=0 and wrong standing in front of the sweep point that fills the glob. Assert the ORDERING
      instead, which is the durable property
    - `scripts/phase23_run.py:1868-2126` — `sigma_zero()`, the closest structural analogue: the
      timing bracket, `captured_dp_seam`, `_count_composed_steps`, the record writer, `_state_record`
    - `scripts/teach_persona.py:851-900` — `arm_spec("dp_n64")` and the LAZY `phase21_filler` import.
      **`arm_spec` alone makes n=64 declarable, not buildable** — `_slot_forms_for` supplies the
      widened slot grammar and without it the first filler fact raises `KeyError:
      'filler_boat_name'`. Read `:381-424` so this is not discovered mid-run
    - `scripts/teach_persona.py:178-215` — `REPLAY_WINDOWS_PER_FACT = 4`, `replay_window_budget`
    - `scripts/teach_persona.py:1340-1360` — `dp_accum` and the replay-window conversion
      (`replay_window_budget(n_facts) // BLOCK_SIZE`); the UNIT CONVERSION is the one thing to get
      wrong
    - `src/personacore/training/loop.py:685-699` — `ceil(4 * n_facts / batch_size)` replay
      micro-batches per optimizer step
    - `scripts/phase23_cost.py:89-107` — `TRAINING_RECORD_KEYS`. **Seventeen required keys, and a
      missing one is REFUSED, never defaulted.** `results/phase23_sigma_zero.json`'s `training` block
      is a worked example that already satisfies them
    - `scripts/phase23_prereg.py:100-135` — `noised_record_path`, and `:96-97` `NOISED_RECORD_GLOB`
    - `.planning/phases/23-*/23-20-PLAN.md:976-1044` — **the measured detached-launch recipe.**
      Copy it; it is not in `scripts/phase23_run.py`
  </read_first>
  <action>
Add a `noised` sub-mode to `scripts/phase23_run.py` and register it in `_TABLE` and `USAGE`.

**(a) The gate — two conjuncts, the same two Task 1's test asserts.** Load
`results/phase23_matched_verdict.json` and `_prove` `verdict == "proceed"`. Then `_prove` the human
unblock act is COMMITTED, **by the same git route Task 1 uses, INCLUDING the sha pin and the
changed-path shape check** — the sentinel search plus ancestry plus `git show HEAD:` is forgeable by
any commit that introduces the phrase, and this driver must not run on a weaker gate than the test
asserts. Import or re-use Task 1's predicate rather than writing a second copy: a second copy of a
gate is a second gate, free to drift. On any failure, `SystemExit` naming D-04 and stop. Also `_prove` the gate record's `sigma_zero_record_file_sha256` matches the live
digest of `results/phase23_sigma_zero.json`, and that both records are committed
(`git ls-files`), so the gate reads committed artifacts rather than working-tree files.

**Do not read `results/phase23_sigma_zero.json`'s own `verdict` as the gate.** It is `HALT`, it will
stay `HALT`, and 23-19 left it that way deliberately. Print both verdicts side by side at the top of
the run so the log records that the reader saw both.

**(b) Choose σ, and record why.** σ must be strictly positive and outside the accountant's measured
breach regime.

**GROUND THIS ON THE LIVE CONSTRAINT, NOT ON THE RETRACTED ONE.** An earlier draft justified σ by
*"σ >= 0.42 is where two-oracle agreement holds"*. **That figure was RETRACTED by 22-19**
(`22-19-SUMMARY.md:123`: the sentence is *"false under both readings"* — the error is not zero at
σ >= 0.42 and neither is the fix's delta; `ROADMAP.md:125` carries the same retraction). Citing a
retracted figure as a live premise inside the very phase whose 23-12 enforces retract-in-place is
not acceptable, whatever the σ it happens to select. Use the two live, measured constraints instead:

  - **WARNING-5's breach regime is the thing to stay out of**, and it is bounded away from any
    plausible sweep σ — `22-VERIFICATION.md:301` records that the PUBLISHING path
    (`sigma_for → epsilon_for → _delta_or_below_float64 → delta_closed → _log_erfc`) does not reach
    `delta_quadrature` at all, and `:310` records the published ε correct to ~1e-13 at every point
    measured **including WARNING-5's worst**.
  - **22-17's measured two-oracle gap at the frozen δ is `1.0152e-11`, inside an UNWIDENED `1e-9`
    budget** (`ROADMAP.md:125`). That is the live agreement figure; quote it rather than the
    retracted band edge.

Read both from the artifacts rather than from this paragraph, and record which constraint the chosen
σ is checked against. σ should also be one a real sweep point would plausibly use, so the noised
adapter's stop behaviour is representative rather than pathological.
Record the chosen σ, the resulting ε via `epsilon_for(sigma, T, DELTA)`, and the reasoning. **This σ
is a resource parameter, not an outcome threshold** — it belongs to Z, not to the gate. σ must
round-trip through `noised_record_path`'s six-decimal rendering; that function refuses if it does
not.

**(c) The run.** `train_arm("dp_n64", facts=<arm_spec's facts>, family_ids=..., second_person=False,
replay_ratio=0.0, seed=SEED, prefix="phase23_noised", dp_sigma=<σ>, dp_clip_norm=<C>)` at the full
production budget, `MAX_STEPS` unmonkeypatched. Bracket the training leg with the 23-05 timing
helper — `torch.mps.synchronize()` at both boundaries. `dp_n64` writes its OWN bins
(`data/persona_dp_n64_train*.bin`), so there is no corpus collision with `dp_n8`.

Record every key in `phase23_cost.TRAINING_RECORD_KEYS`, plus: `grad_accum_steps` (must be **64**),
`replay_micro_batches_per_step` (must be `ceil(4*64/8) = 32`), `records_per_lot` (`dp_fn._records`),
`clip_bind_count`, and the composed step count T via the `_count_composed_steps` shadow. Add a
`protocol` field naming this arm's training protocol — see Task 4's provenance rule, which applies
to every timing this phase publishes.

**(d) The T cross-check, at zero extra cost.** Compare this run's composed step count T against the
one `results/phase23_sigma_zero.json` recorded for n=8 at the same 200-step budget (MEASURED at
HEAD: `composed_steps = 200`, `t_source = "_count_composed_steps"`), and assert `t_n64 == t_n8`
**directly**. This asks CAL-03's N-leak question of two REAL production runs rather than of a toy
probe. State plainly in the record that the ε comparison is NOT made here — the two runs are at
different σ and D-05 requires a fixed σ — so this leg tests T only, and
`results/phase23_cal03_wiring.json` remains the record CAL-03's verdict is read from (MEASURED at
HEAD: `verdict = true`, `t_n8 = t_n64 = 4`, `sweep_point = false`).

**(e) Emit the noised run's record** at `phase23_prereg.noised_record_path("dp_n64", sigma)` —
**call the function, never retype the string** — carrying the training block, the adapter path with
its sha256, σ, C, ε, T and full provenance. Validate with
`phase23_cost.validate_record(..., kind="training")` before writing. Commit it. This is the first
`results/phase23_noised_*` artifact; run `pytest tests/test_phase23_prereg.py tests/test_phase23_matched.py -q`
immediately after the commit and record it green.

**(f) LAUNCH DETACHED.** Copy 23-20's recipe from `23-20-PLAN.md:985-1012` verbatim, substituting
the sub-mode name and the log path `results/phase23_cost_run.log`. **The log path must not match
`results/phase23_noised_*`.** Take the pid from the log's FIRST LINE, never from `$!`. Probe with
`os.getsid(pid)` from a second interpreter, never `ps -o sid`. Hold with
`/usr/bin/caffeinate -is -w "$PID"`, lid open. Commit the log as the detachment evidence, exactly as
23-20 committed `results/phase23_resume_run.log`.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase23_prereg.py tests/test_phase23_matched.py tests/test_phase23_cost.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY quotes BOTH verdicts read before the run — `results/phase23_sigma_zero.json` = `HALT`, `results/phase23_matched_verdict.json` = `proceed` — and states that the gate read the second
    - The SUMMARY records the committed human unblock act by FULL commit sha (asserted `== 746ecf699904e7c97bf73614e1c617a646da30ad`) and date, the four planning paths it touched with zero under `scripts/` or `src/`, and the `sigma_zero_record_file_sha256` match against the live file
    - The detachment probe's output is quoted verbatim BEFORE any GPU second, and `head -1 results/phase23_cost_run.log` shows a `SESSION pid=N pgid=N sid=N` line with all three equal
    - The SUMMARY quotes measured `seconds_total` and `seconds_per_optimizer_step` for `dp_n64` with denominators, beside research's ~30 min projection AND 23-10's retraction of that projection's lower-bound status, with the delta stated in both directions
    - `grad_accum_steps` is **64** and `replay_micro_batches_per_step` is **32**, both recorded
    - `t_n64 == t_n8` is asserted and recorded with both values quoted; the record states that no ε comparison is made across the two σ values
    - The noised record exists at the path returned by `phase23_prereg.noised_record_path("dp_n64", sigma)` — verify by CALLING the function and comparing to the on-disk path, never by retyping
    - `git ls-files 'results/phase23_cost_run.log' | wc -l | tr -d ' '` prints **1**, and `git ls-files 'results/phase23_noised_*' | wc -l | tr -d ' '` prints **1** — the log is committed and is NOT in the noised glob
    - `git log --diff-filter=A --format='%H %ad' -- results/phase23_sigma_zero.json results/phase23_noised_*.json` shows the σ=0 record strictly earlier
    - `.venv/bin/python -m pytest tests/test_phase23_prereg.py tests/test_phase23_matched.py -q` exits 0 with the first noised record present
    - `git diff --exit-code -- scripts/mitigation_accountant.py scripts/mitigation_gate.py scripts/phase23_prereg.py scripts/phase23_matched_prereg.py scripts/phase23_resume_prereg.py scripts/mitigation_budget.py scripts/phase18_extraction.py pyproject.toml` exits 0
  </acceptance_criteria>
  <done>The first noised run of the milestone executed at n=64, detached and probed, gated on the MATCHED verdict record plus the committed human unblock act, its training leg timed with denominators and its protocol named, T matching the n=8 run's, and the record committed under the derived noised path.</done>
</task>

<task type="auto">
  <name>Task 3: CAL-05 throughput floor and ceiling, then emit `results/phase23_cost.json`</name>
  <files>scripts/phase23_run.py, results/phase23_cost.json, tests/test_phase23_cost.py</files>
  <read_first>
    - `results/phase18_preflight_report.md:23-28, 35-38, 71-81` — the committed per-shape rates
      (A1-mild 145.01, A1-aggressive 134.54, A2 183.20, A3 140.85 draws/min), the stop-terminated
      table (**56/64, 45/64, 56/64, 51/64**) and the 84,960-draw / 9.54 h projection. **This is the
      cross-validation target**
    - `scripts/phase18_extraction.py:60-96` — the four attack families, `K = 48`,
      `ASR_RUNGS = (1, 4, 16, K)`, and `:88-92`'s recorded reason a K may not be reduced after seeing
      a null. **Import read-only; ancestry-guarded and permanently uneditable**
    - `scripts/phase14_recall.py:143` `RECALL_MAX_NEW_TOKENS = 48`, `:152` `N_SEEDED_SAMPLES = 8`,
      `:162` `STOP_IDS = frozenset({8184, 8185})`, and the `forbid_ids` mask
    - `src/personacore/generation/core.py:64-80` — the per-token Python loop at batch 1, and `:79`'s
      `tok = int(next_id)` device->host sync. Wall clock is close to linear in tokens emitted, which
      is what makes the floor-vs-ceiling question directly measurable
    - `scripts/phase17_persona_gate.py::build_unadapted_base` — how the base is built for a scoring
      pass; the noised adapter loads through `phase14_recall.load_adapted_model`
    - `scripts/phase23_cost.py:115-134` — `GENERATION_RECORD_KEYS` (**eighteen required keys**);
      `:158-163` `FORBIDDEN_MEAN_KEYS` (`h_per_point`, `draws_per_min`, `mean_h_per_point`,
      `h_per_point_mean` — refused at ANY nesting depth); `:204-285` `validate_record`'s four
      refusals; `:287-380` `size_sweep` and its `draws_at_k = draws_per_point + questions * (k -
      k_per_question)` scaling
    - `scripts/phase23_prereg.py:92` `COST_RECORD`, `:343` `H_PER_POINT_FLOOR_SECONDS = 17175`
    - `results/phase23_matched_control.json` — `training_seconds_per_seed`, which is a **DICT keyed
      by seed STRING** (MEASURED: `{'1337': ..., '1338': ..., '1339': ..., '2024': ..., '2025': ...}`),
      **not a list** — iterate `.values()` and `float()` each; and the per-seed `grad_accum_steps` /
      `replay_micro_batches_per_step` / `dp_seam_active`
    - `results/phase23_control_floor.json` — the OLD control protocol's per-seed `training_seconds`
    - `results/phase23_never_taught_training.json` — the seed count N the sizing must price
    - `23-RESEARCH.md` §R3.B — the measured bracket (1.536x wall, 1.751x token) and its stated bounds
    - `23-RESEARCH.md:637-641, 665-685` — the projection table AND the `RETRACTED IN PLACE` block
      above it. **The 20.4 s non-DP figure is a LOOP-ONLY PROJECTION, not a `train_arm` measurement**
  </read_first>
  <action>
**PART A — the `throughput` sub-mode.** Measure the noised adapter from Task 2 under **two
conditions**, on the **real attack shapes**:

  - **FLOOR condition** — `STOP_IDS` active, the Phase-18 condition.
  - **CEILING condition** — the stop set emptied, so every draw runs the full
    `RECALL_MAX_NEW_TOKENS = 48`. This is the worst case a noised adapter that stops emitting EOS
    produces, and it is what makes the ceiling a measurement rather than an unbounded worry.

Protocol, per condition, per attack shape: discard **>= 4** warm-up draws, then time **>= 64** draws
(research used N = 64 per condition); `torch.mps.synchronize()` at both boundaries via the 23-05
helper; record `draws_per_min_floor` / `draws_per_min_ceiling`, `mean_tokens_floor` /
`mean_tokens_ceiling`, `stop_terminated_n_floor` / `stop_terminated_n_ceiling` and `n_draws` **per
shape**. **Never a bare `draws_per_min`** — `validate_record` refuses it at any depth.

Cover the Phase-18 shapes rather than one prompt: their prefill lengths span 134.54-183.20
draws/min, a **1.36x spread**. The multiplier is a ratio at fixed prefill so the spread largely
cancels, but CAL-05's own measurement is required to reproduce it across the real shapes.

Derive and record: `wall_multiplier` and `token_multiplier` per shape and pooled; `h_per_point_floor`
and `h_per_point_ceiling`, each computed from `draws_per_point / draws_per_min` with
`draws_per_point`, `k_per_question` and `questions` recorded as separate keys so the arithmetic is
auditable from the record alone; and the **cross-validation** against
`results/phase18_preflight_report.md` — measure the FLOOR condition on the un-adapted base too,
compare per-shape against the committed rates, and record the agreement percentage. Research
reproduced A1-mild within **1.5%** five weeks later; a large divergence means the hardware or the
stack moved and the committed cost artifact needs revisiting before Z is sized on it.

**Bound the probe honestly in the record**, as research did for its own: name the number of prompt
shapes, the draws per condition, the single process, the one adapter and the one σ. State that the
noised adapter's stop rate is the quantity the bracket exists to capture and record both extremes.

This leg is part of the same detached run as Task 2 — do not launch it in the foreground.

**PART B — assemble and commit `results/phase23_cost.json`** (path from
`phase23_prereg.COST_RECORD`).

**THE `training.non_dp` PROVENANCE DECISION — MADE HERE, EXPLICITLY, AND ITS ARGUMENT WRITTEN INTO
THE RECORD.** Three non-DP figures exist and they disagree materially. MEASURED at HEAD, **at FULL
STORED PRECISION — these are the renderings, not roundings of them:**

| source | protocol | training |
|---|---|---:|
| `23-RESEARCH.md:637-641` | accum=1, loop-only PROJECTION, never a real run | 20.4 s — research's own rounding, quoted here AS a projection |
| `results/phase23_control_floor.json` | OLD control: 8-window lots, teaching weight 0.4342, `grad_clip` binding on 19 of 25 steps | 79.14336965046823 s mean / 5 seeds |
| `results/phase23_matched_control.json` | PROTOCOL-MATCHED: accum=8, 32 replay windows/step, weight 1.0, `grad_clip` non-binding, `dp_seam_active=False` | 161.12400419991462 s mean / 5 seeds |

**THE TWO MEASURED MEANS ARE COMPUTED, NOT READ FROM THIS TABLE.** Take the mean of
`results/phase23_matched_control.json`'s `training_seconds_per_seed` (a DICT — `.values()`) and of
`results/phase23_control_floor.json`'s `per_seed[i].training_seconds` in code. The table exists to
show that the three sources disagree and by how much; a figure retyped out of a plan's prose is the
defect this revision closes, and a retyped mean would differ from the computed one in the last
digits without looking wrong. **The `20.4 s` row is research's own published rounding of a
projection** — quoted here as the thing being characterised, and it may never enter a record or a
continuation as a figure.

**The decision: `training.non_dp` comes from `results/phase23_matched_control.json`.** Write this
argument into the record's `training.non_dp.provenance_argument` field, in these terms. **Its one
numeric claim is carried by a NAMED FIELD, not by the prose** — that is the round-5 correction: a
number that lives only inside a prose paragraph is not a scalar leaf, cannot be quoted by 23-12, and
cannot be re-derived by any test.

  1. **It is a REFERENCE quantity, by use.** Its only consumers in this phase are the `ratios` block
     and 23-12's retraction, both of which compare non-DP against DP. A ratio is meaningless unless
     numerator and denominator describe the same experiment.
  2. **The three mechanisms that invalidated the old control as a comparator are wall-clock
     mechanisms too, and the size of the effect is MEASURED rather than predicted.** The measured
     wall-clock protocol gap is the field `training.non_dp.wall_clock_gap_vs_superseded` — MEASURED
     from the two committed records as 2.035849685343305, and COMPUTED at write time from the two
     `training_seconds_mean` fields rather than typed. The old control does not time the same amount
     of work.

     **Do NOT write the naive per-step-work form of this argument.** An earlier draft said *"8.125x
     the lot volume is 8.125x the work per optimizer step. The measurement confirms it."* It does
     not confirm it: **2.035849685343305 is not 8.125**, so the measurement REFUTES the per-step-work
     equality rather than supporting it. The conclusion — that the two protocols do not time the same
     work and therefore cannot share a ratio denominator — needs only the measured gap and survives
     without the equality. State the gap as what it is: the measured wall-clock protocol gap, at the
     precision the field stores.
  3. **The consequence is the finding, not a rounding.** Against the old control the DP/non-DP
     training multiple is `training.dp_n8.seconds_total` ÷
     `training.non_dp_superseded_protocol.training_seconds_mean`; against the matched comparator it
     is `training.dp_n8.seconds_total` ÷ `training.non_dp.training_seconds_mean`. Both are
     computable from the record's own fields, so name them that way and let a reader divide.
     Publishing the first as if it were the DP seam's cost would attribute to "the DP seam" a factor
     that is mostly the packer and lot difference the comparator equalises — precisely the error
     `.planning/debug/sigma-zero-beats-control.md` root-caused. **Write no rounded numeral for
     either multiple.** 2.60 and 1.28 are roundings of quantities the record already carries at full
     precision, and a rounding is not a figure this phase publishes.

**State the scope of the binding rule honestly and do not overstate it.** `deferred-items.md`'s
**CONTROL PROVENANCE** rule governs the formal gate's three UTILITY fields —
`control_taught_recall`, `control_heldout_recall`, `control_gap` — and requires them to come from
`results/phase23_matched_control.json`. **Timing is not one of those three.** This decision is made
and argued here on its own merits; it is not inherited from that rule. Say so in the record.

**The old protocol's figure is RECORDED BESIDE IT, never deleted**, under a distinct key
`training.non_dp_superseded_protocol`, with its protocol named and the gap carried by the
`training.non_dp.wall_clock_gap_vs_superseded` field rather than restated as a numeral. A reader
sees both, exactly as 23-19 left both verdicts standing.

**THE PUBLISHED FIGURES ARE PRE-REGISTERED FIELD PATHS, AND 23-12 QUOTES THEM BY PATH.** These
eleven paths are the CONTRACT between this plan and 23-12's continuation. 23-12 pre-registers the
identical list and asserts each one's full-precision rendering appears VERBATIM in the correction; a
key renamed here silently breaks that guard, so these names are FIXED:

| path | what it is |
|---|---|
| `training.non_dp.training_seconds_mean` | mean of the five matched-control per-seed timings |
| `training.non_dp_superseded_protocol.training_seconds_mean` | mean of the five old-control per-seed timings |
| `training.non_dp.wall_clock_gap_vs_superseded` | the quotient of the two above, COMPUTED at write time |
| `training.dp_n8.seconds_total` | from `results/phase23_sigma_zero.json`'s `training` block |
| `training.dp_n64.seconds_total` | Task 2's measurement |
| `generation.h_per_point_floor` | Part A, stop ids active |
| `generation.h_per_point_ceiling` | Part A, stop set emptied |
| `ratios.non_dp.eval_over_training_ceiling` | one per training block, ceiling-derived |
| `ratios.non_dp_superseded_protocol.eval_over_training_ceiling` | " |
| `ratios.dp_n8.eval_over_training_ceiling` | " |
| `ratios.dp_n64.eval_over_training_ceiling` | " |

`training_seconds_mean` is a NEW name and is deliberately not one of `FORBIDDEN_MEAN_KEYS` (MEASURED:
`('h_per_point', 'draws_per_min', 'mean_h_per_point', 'h_per_point_mean')`). That set exists to stop
a bare per-point mean standing in for the floor/ceiling bracket, not to stop a five-seed training
mean riding beside `seconds_total` with its own denominator. The name follows
`results/phase23_never_taught_training.json`'s committed extra-field shape
(`training_seconds_per_seed` / `_min` / `_max` / `_total`), which carries no mean — this adds one.

**COMPUTED, NEVER TYPED — AND THE FILE'S BYTES MUST EQUAL `repr()`.** Every one of these eleven is
produced by arithmetic on values loaded from the source records and written with `json.dump`, which
serialises floats through `float.__repr__`. **That identity is the whole basis of 23-12's guard:**
the bytes in `results/phase23_cost.json` and the string a test gets from `repr(json.load(...)[path])`
are the SAME string — but only if the record was written by `json.dump` from the computed float.
**A hand-edited digit anywhere in this record breaks 23-12's guard**, and breaks it as a RED on a
figure that looks correct, which is the hardest kind to diagnose. MEASURED while drafting this
revision: a hand-typed `1743.8820147753301` round-tripped to `1743.8820147753302` and RED-ed an
otherwise compliant continuation.

**EVERY timing block carries a `protocol` field.** A timing that does not say which protocol it
timed is not a usable figure. Blocks and their sources:

  - `training.non_dp` — `results/phase23_matched_control.json`, protocol `"protocol-matched non-DP
    comparator"`, with the per-seed list, mean, min, max and n_seeds as the denominator. **Do not
    re-run a non-DP arm.** Cite the record and its sha256.
  - `training.non_dp_superseded_protocol` — `results/phase23_control_floor.json`, protocol
    `"old unmitigated control (superseded as a comparator)"`, cited with its sha256.
  - `training.dp_n8` — `results/phase23_sigma_zero.json`'s `training` block, protocol
    `"dp_n8, seam active, sigma=0"`, cited with its sha256.
  - `training.dp_n64` — this plan's Task 2 measurement, protocol `"dp_n64, seam active, sigma>0"`.
  - `generation` — Part A's measurement, carrying `h_per_point_floor` and `h_per_point_ceiling` as
    distinct required keys.
  - `ratios` — for each training block, `eval_hours / training_hours` computed from the record's own
    fields, stored under the pre-registered names above:
    `ratios.<block>.eval_over_training_ceiling` using the MEASURED `generation.h_per_point_ceiling`
    for the eval term, and `ratios.<block>.eval_over_training_floor` using
    `generation.h_per_point_floor` beside it. **Never the pinned `H_PER_POINT_FLOOR_SECONDS`** —
    that pin is the floor-side reference, not a measurement. The measured ceiling exceeds the pin, so
    every ceiling-derived ratio comes out SMALLER than a pin-derived one; that is expected and is not
    a defect. **All four blocks are stored so the "~1,010×" claim's scope is visible in the data
    rather than argued in prose.**
    **Record the ratios as they come out. Do not assume any of them reproduces 843x** — the 20.4 s
    that produced 843x is a loop-only projection whose lower-bound status 23-10 already retracted,
    and no measured `train_arm` or `tp.train` run in this repository reproduces it.
  - `sizing` — the output of `phase23_cost.size_sweep(...)` at each candidate `K` in
    `mitigation_gate.K_RUNGS`, so 23-13 selects a rung from a table that already exists. Include the
    never-taught floor's scoring cost as its own line item: `N_seeds x h_per_point_ceiling_at_k` at
    each K, with the seed count read from `results/phase23_never_taught_training.json` (MEASURED: 5).
    **A sizing that prices 16 sweep points and forgets the N-seed control floor is short by N points.**
  - Provenance for every block: `git_sha`, `device`, `torch_version`, `python_version`, timestamps,
    and the source record path + sha256 for every borrowed figure.

**W6 — WHICH MAPPINGS `validate_record` RUNS AGAINST, AND WHAT A FIVE-SEED AGGREGATE PUTS IN THE
SCALAR FIELDS.** `validate_record(record, *, kind)` takes a MAPPING and checks `TRAINING_RECORD_KEYS`
**inside that mapping**, so *"validate for both kinds"* is not an instruction until the mappings are
named. Run it on the **four `training.*` blocks with `kind="training"`** — `non_dp`,
`non_dp_superseded_protocol`, `dp_n8`, `dp_n64` — and on the **`generation` block with
`kind="generation"`**. `ratios` and `sizing` are derived and are covered by Part C's re-derivation
test instead.

**NEITHER BORROWED NON-DP BLOCK CAN BE SATISFIED BY COPYING ITS SOURCE. MEASURED at HEAD, not
anticipated:** `results/phase23_matched_control.json` is missing ALL THIRTEEN of `arm`,
`capacity_n_facts`, `grad_accum_steps`, `replay_micro_batches_per_step`, `max_steps`, `batch_size`,
`block_size`, `seconds_total`, `seconds_per_optimizer_step`, `warmup_iterations_discarded`,
`timed_iterations`, `seed` and `dp_seam_active` at top level. (`results/phase23_sigma_zero.json`'s
`training` block, by contrast, is missing NONE — it is the worked example.) They are all reachable,
but from three different levels, so the block is ASSEMBLED:

  - from `per_seed[i]` — `arm`, `grad_accum_steps` (**8**), `replay_micro_batches_per_step` (**4**),
    `max_steps` (**200**), `batch_size` (**8**), `block_size` (**256**), `dp_seam_active`
    (**False**), and `n_facts` (**8**) as `capacity_n_facts`;
  - from the TOP level — `device`, `torch_version`, `python_version`, `git_sha`, `n_seeds` (**5**),
    and the five timings in `training_seconds_per_seed`;
  - computed — `seconds_total`, `seconds_per_optimizer_step`, `timed_iterations`,
    `warmup_iterations_discarded`, `seed`.

**THE AGGREGATE CONVENTION IS NOT INVENTED HERE — IT IS COPIED FROM THE COMMITTED PRECEDENT.**
`results/phase23_never_taught_training.json` is itself a five-seed aggregate that already satisfies
`TRAINING_RECORD_KEYS` at top level, and MEASURED at HEAD its convention is: `seed` is the **LIST**
`[1337, 2024, 1338, 2025, 1339]`, never a scalar; `seconds_total` is the **SUM** across seeds
(176.7093129986897 — the sum of its five per-seed values, NOT their mean 35.341862599737944);
`timed_iterations` is the **SUM** of timed steps (**1000** = 5 x 200);
`seconds_per_optimizer_step` is `seconds_total / timed_iterations`; `warmup_iterations_discarded`
is **0**. Follow it exactly for both borrowed blocks, and read those five values from that record
rather than from this paragraph. The per-seed list, mean, min and max ride BESIDE the required keys
as extra fields — never instead of them, and never under a `FORBIDDEN_MEAN_KEYS` name.

**`results/phase23_control_floor.json` IS THINNER STILL, AND THE GAP IS A FINDING, NOT A DEFAULT.**
MEASURED: `batch_size` (**8**), `block_size` (**256**) and `max_steps` (**200**) are available only
from its `recipe.budget_constants`.

**W2 — THE OTHER THREE ARE MISSING AS KEYS, NOT AS VALUES, AND THE DIFFERENCE DECIDES WHAT THE
EXECUTOR DOES.** An earlier draft said `grad_accum_steps`, `replay_micro_batches_per_step` and
`dp_seam_active` are *"absent from that record entirely"*. **MEASURED, that is not true.** The KEYS
are absent; the record states the VALUES in its own prose, and that prose is the citable source:

  - `grad_accum_steps` = **1** — `residual_differences[1].difference` reads verbatim
    *"grad_accum_steps is 1 here and `n_facts` on the DP path"*. MEASURED: the string
    `grad_accum_steps` occurs **twice** in that record's JSON text, both inside
    `residual_differences[1]`.
  - `replay_micro_batches_per_step` = **0** — `residual_differences[0].difference` reads *"replay
    lives IN the teaching bin here; it is drawn at TRAIN time on the DP path"*. This protocol has no
    separate replay pass, so the per-optimizer-step count is zero. MEASURED: the key name itself
    occurs **0** times in that record.
  - `dp_seam_active` = **False** — `residual_differences[3].why_not_eliminable` reads *"`DPSGD` is
    constructed only when `is_dp`"*, and this arm is the non-DP control. MEASURED: the key name
    occurs **0** times.

Cite the `residual_differences` index each value came from, in the record and in the SUMMARY.
**Reading a value out of a record's own prose is SOURCING; writing the same value with no citation is
INVENTING** — and the two are indistinguishable in the output unless the citation is there.

**W3 — AN UNSOURCEABLE KEY ESCALATES; IT IS NOT NARRATED PAST.** An earlier draft said *"if one
cannot be sourced at all, say so rather than inventing it"*. This plan's own acceptance criteria
refuse that: `validate_record` must pass on all four training blocks, and it **refuses a missing key
rather than defaulting one**, so a block with an unsourced key never gets written and there is no
state in which the executor "says so" and proceeds. If a required key cannot be sourced from the
source record, from its prose, or from the protocol the block names: **STOP. Do not write the
record. Escalate to the developer in the SUMMARY with the key named and the three sources shown
failing.** A silently-manufactured shape field on a superseded comparator is exactly the unlabelled
figure this phase exists to stop publishing.

Validate with `phase23_cost.validate_record` for both kinds before writing; a missing key or a bare
mean refuses. Commit it.

**PART C — tests, added to `tests/test_phase23_cost.py`:**
  - `test_the_committed_cost_record_validates` — load the real committed record and run
    `validate_record` on both blocks.
  - `test_the_cost_record_ratios_re_derive` — recompute each ratio from the record's own fields and
    assert exact equality with the stored value. A stored ratio that no longer re-derives is a
    number nobody can check. **Cover `training.non_dp.wall_clock_gap_vs_superseded` in this same
    test** — it is the same shape (a quotient of two stored fields) and it is the field 23-12
    quotes, so it is the one that most needs to be un-typeable. Assert `==`, never
    `pytest.approx`: the field is written from exactly this quotient in exactly this float order, so
    any inequality means it was typed rather than computed.
  - `test_borrowed_figures_cite_a_record_and_a_digest` — for every figure sourced from another
    artifact, assert the block carries `source_record` and `source_record_sha256` and that the digest
    matches the file on disk. This is what stops a re-measured number silently replacing a cited one.
  - `test_every_timing_block_names_its_protocol` — assert every block under `training` carries a
    non-empty `protocol` string, and that `training.non_dp` and
    `training.non_dp_superseded_protocol` carry DIFFERENT protocol strings and DIFFERENT
    `source_record` values. **Implement as a structural walk over the record's own keys, not a
    hardcoded list**, so a fifth timing block added later is caught rather than missed.
  - `test_the_sizing_table_prices_the_never_taught_floor` — assert the `sizing` block contains a line
    item derived from the never-taught record's seed count at every `K_RUNGS` entry.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase23_cost.py -v</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY quotes, per attack shape, `draws_per_min_floor`, `draws_per_min_ceiling`, `stop_terminated_n_floor`, `stop_terminated_n_ceiling`, `mean_tokens_floor`, `mean_tokens_ceiling` and `n_draws`
    - The SUMMARY quotes the pooled `wall_multiplier` and `token_multiplier` beside research's pre-sizing bracket (1.536x / 1.751x) with the delta stated
    - The SUMMARY quotes the un-adapted-base cross-validation against `results/phase18_preflight_report.md`'s four rates with the agreement percentage per shape
    - **The SUMMARY states the `training.non_dp` provenance decision explicitly, with all three candidate figures, their protocols, and the three-point argument** — and states plainly that `deferred-items.md`'s CONTROL PROVENANCE rule governs the three utility fields and NOT timing, so this decision was made here rather than inherited
    - `.venv/bin/python -c "import json;d=json.load(open('results/phase23_cost.json'));t=d['training'];print(sorted(t));print([b.get('protocol') for b in t.values()]);print(d['generation']['h_per_point_floor'], d['generation']['h_per_point_ceiling'])"` prints all four training blocks, four distinct non-empty protocol strings, and both bracket keys
    - `.venv/bin/python -c "import json;d=json.load(open('results/phase23_cost.json'));n=d['training']['non_dp'];print(n['source_record'])"` prints `results/phase23_matched_control.json`
    - **W6 — `validate_record` was run against NAMED mappings, and the assembled blocks satisfy it.** `.venv/bin/python -c "import sys,json;sys.path.insert(0,'scripts');import phase23_cost as c;d=json.load(open('results/phase23_cost.json'));[c.validate_record(d['training'][k], kind='training') for k in d['training']];c.validate_record(d['generation'], kind='generation');print('OK', sorted(d['training']))"` prints `OK` and all four training block names. The SUMMARY states, for `training.non_dp` and `training.non_dp_superseded_protocol`, where EVERY required key came from — `per_seed[i]`, the source record's top level, `recipe.budget_constants`, or computed — and quotes the aggregate convention it copied from `results/phase23_never_taught_training.json` (`seed` a LIST, `seconds_total` and `timed_iterations` SUMS, `seconds_per_optimizer_step` their quotient, `warmup_iterations_discarded` 0)
    - `.venv/bin/python -m pytest "tests/test_phase23_cost.py::test_the_cost_record_ratios_re_derive" "tests/test_phase23_cost.py::test_borrowed_figures_cite_a_record_and_a_digest" "tests/test_phase23_cost.py::test_every_timing_block_names_its_protocol" "tests/test_phase23_cost.py::test_the_sizing_table_prices_the_never_taught_floor" -v` exits 0
    - **THE ELEVEN PRE-REGISTERED PATHS ALL RESOLVE AT FULL PRECISION, AND THE GAP RE-DERIVES FROM THE TWO MEANS.** `.venv/bin/python -c "import json,functools,operator;d=json.load(open('results/phase23_cost.json'));P=('training.non_dp.training_seconds_mean','training.non_dp_superseded_protocol.training_seconds_mean','training.non_dp.wall_clock_gap_vs_superseded','training.dp_n8.seconds_total','training.dp_n64.seconds_total','generation.h_per_point_floor','generation.h_per_point_ceiling','ratios.non_dp.eval_over_training_ceiling','ratios.non_dp_superseded_protocol.eval_over_training_ceiling','ratios.dp_n8.eval_over_training_ceiling','ratios.dp_n64.eval_over_training_ceiling');g=lambda p:functools.reduce(operator.getitem,p.split('.'),d);print([repr(g(p)) for p in P]);assert g('training.non_dp.wall_clock_gap_vs_superseded')==g('training.non_dp.training_seconds_mean')/g('training.non_dp_superseded_protocol.training_seconds_mean');print('GAP RE-DERIVES')"` prints eleven renderings and `GAP RE-DERIVES`. **MEASURED, three of them are already known**: the gap is `2.035849685343305`, the two means are `161.12400419991462` and `79.14336965046823`. A rendering that comes out as a shorter rounding of one of these means the field was typed
    - The SUMMARY quotes ALL four `eval ÷ training` ratios as literals **at the record's own precision** and states which arm, at which protocol, the "~1,010×" sentence is or is not true of — **as measured, with no figure carried over from the plan's prose, and no rounding**
    - `.venv/bin/python -m pytest tests/ -q` reports 0 failed; `make lint` exits 0
    - `git diff --exit-code -- .planning/REQUIREMENTS.md` exits 0 — **the correction is 23-12's, not this plan's**
    - `git diff --exit-code -- scripts/phase18_extraction.py scripts/phase14_recall.py scripts/phase23_prereg.py scripts/mitigation_budget.py scripts/mitigation_gate.py` exits 0
  </acceptance_criteria>
  <done>Throughput is measured on a real noised adapter across the real attack shapes under both stop conditions and cross-validated against the committed Phase-18 artifact; one cost record carries four training legs each naming its protocol, the floor/ceiling generation bracket, all four ratios, and a K-rung sizing table that prices the never-taught floor — with the `training.non_dp` provenance decision argued in the record itself.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the σ=0 record → the D-04 gate | The σ=0 record carries `HALT` permanently and by design. A gate pointed at it can never open; a gate pointed at it and *made* to open would have to edit a frozen artifact. |
| a measurement verdict → a human release | `results/phase23_matched_verdict.json`'s own `governs` field forbids reading itself as an unblock. The release is verdict AND committed human act. |
| a committed guard → a changed premise | `test_no_noised_point_exists` stands committed with a premise the 2026-08-28 unblock falsified. A silent deletion would destroy the record of what was believed. |
| a non-DP timing → a published ratio | Two measured non-DP protocols differ in wall clock by 2.035849685343305. A ratio built on the wrong one attributes protocol overhead to the DP seam. |
| a figure living only in prose → a downstream quotation | A number written into `provenance_argument`'s paragraph is not a scalar leaf: nothing can quote it by path and no test can re-derive it. 23-12 must quote the gap, so the gap must be a field. |
| a floor measured on the un-adapted base → a noised adapter's cost | 45-56 of 64 draws stop-terminated on the base; a noised adapter that stops emitting EOS runs every draw to 48 tokens. |
| a foreground harness call → a 45-minute GPU run | 23-17's run was harness-killed at ~60 min at 3 of 5 seeds. |
| sizing table → the K ratchet | K may only increase. A sweep sized short has no rescue in the cheap direction. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-57 | Elevation of Privilege | a noised point run despite an unopened D-04 gate | mitigate | Two-conjunct gate as the first act of Task 2 — matched `verdict == "proceed"` AND the COMMITTED unblock act — read from committed records, `SystemExit` naming D-04 |
| T-23-57d | Spoofing | the human-act conjunct FORGED by a routine agent commit | mitigate | Sentinel + ancestry + `git show HEAD:` is satisfied by ANY commit introducing the phrase, and 23-12 Task 1 commits `.planning/STATE.md`. Bound to the human act by a pinned 40-char sha (`746ecf69…`) and a `git show --name-only` check that no path under `scripts/` or `src/` was touched; both watched refusing in the 3-case tripwire |
| T-23-57b | Spoofing | the gate pointed at the wrong artifact and therefore never openable, or opened by editing one | mitigate | Both verdicts printed side by side at run start; the gate record's `sigma_zero_record_file_sha256` checked against the live σ=0 file; `git diff --exit-code -- results/` asserted |
| T-23-57c | Repudiation | a committed guard silently deleted once its premise changed | mitigate | Task 1 widens rather than deletes; the original assertion asserted still present by AST; the deletion count accounted for line by line; a constructed-input tripwire watches the new branch refuse |
| T-23-58 | Tampering | the floor read as a mean and the sweep sized short | mitigate | `validate_record` refuses bare mean keys at any depth; per-shape floor and ceiling fields; sizing computed by `size_sweep`, which uses the ceiling |
| T-23-58b | Spoofing | a non-DP timing published without its protocol | mitigate | `protocol` required on every timing block; `test_every_timing_block_names_its_protocol` walks the record structurally; the superseded protocol recorded beside, never deleted |
| T-23-58c | Repudiation | a published figure that exists only as prose, or only as a rounding | mitigate | Eleven pre-registered field paths, every one a scalar leaf written by `json.dump` from a computed float; the gap is COMPUTED from the two `training_seconds_mean` fields and re-derived by `test_the_cost_record_ratios_re_derive` with `==`; 23-12 quotes by path and asserts verbatim containment, so a rounding fails as an OMISSION |
| T-23-59 | Repudiation | a rate published without its shape, denominator or stop count | mitigate | Per-shape `n_draws`, `stop_terminated_n_*`, `mean_tokens_*` are required keys; the probe's bounds are stated in the record |
| T-23-60 | Tampering | a borrowed figure diverging from the record it cites | mitigate | `test_borrowed_figures_cite_a_record_and_a_digest` recomputes each digest against the file on disk |
| T-23-61 | Information Disclosure | a hardware or stack shift invalidating the committed Phase-18 cost artifact unnoticed | mitigate | The un-adapted-base floor condition is cross-validated per shape against `results/phase18_preflight_report.md` with the agreement percentage recorded |
| T-23-62 | Tampering | a sizing table that forgets the N-seed never-taught floor | mitigate | `test_the_sizing_table_prices_the_never_taught_floor` asserts a line item at every `K_RUNGS` entry |
| T-23-63 | Repudiation | the "~1,010×" claim silently carried onto the DP path, or a projection cited as a measurement | mitigate | All four ratios stored and re-derivable from measured fields; the plan forbids carrying 843x forward as a premise; the correction itself is 23-12's, with `git diff --exit-code -- .planning/REQUIREMENTS.md` asserted here |
| T-23-63b | Denial of Service | the long run killed by a foreground harness cap | mitigate | 23-20's measured detached recipe copied verbatim; pid from the log's first line; `os.getsid` probe before any GPU second; log committed as evidence; log path kept OUT of the noised glob |
| T-23-SC | Tampering | npm/pip/cargo installs | accept | **Zero installs.** `pyproject.toml` byte-unchanged (RPT-03) |
</threat_model>

<verification>
```bash
.venv/bin/python -c "import json;print('sigma_zero:', json.load(open('results/phase23_sigma_zero.json'))['verdict'], '| matched:', json.load(open('results/phase23_matched_verdict.json'))['verdict'])"
git log -1 --format='%H %ad %s' 746ecf6
.venv/bin/python -m pytest tests/test_phase23_cost.py tests/test_phase23_prereg.py tests/test_phase23_matched.py -v
head -1 results/phase23_cost_run.log
git ls-files 'results/phase23_*' | wc -l | tr -d ' '
git log --diff-filter=A --format='%H %ad' -- results/phase23_sigma_zero.json results/phase23_noised_*.json
git log --format=%H -- scripts/phase23_matched_prereg.py | wc -l | tr -d ' '
git log --format=%H -- scripts/phase23_resume_prereg.py | wc -l | tr -d ' '
git diff --exit-code c7de5d4 HEAD -- scripts/phase23_prereg.py
git diff --exit-code c100388 HEAD -- scripts/phase23_matched_prereg.py
git diff --exit-code -- scripts/mitigation_accountant.py scripts/mitigation_gate.py scripts/mitigation_budget.py scripts/phase18_extraction.py .planning/REQUIREMENTS.md pyproject.toml
.venv/bin/python -m pytest tests/ -q
make lint
```
</verification>

<success_criteria>
- Every D-04 gate in this plan reads `results/phase23_matched_verdict.json`, including the acceptance commands; nothing gates on the σ=0 record's `verdict`
- The gate is two conjuncts — the matched verdict AND the committed human unblock act — because the verdict record's own `governs` field forbids reading it as an unblock; and the human-act conjunct is bound to `746ecf6` by a pinned sha AND a no-`scripts/`-no-`src/` changed-path check, so a routine agent commit to `.planning/STATE.md` cannot satisfy it
- `test_no_noised_point_exists` is WIDENED: the original assertion stands verbatim on the blocked branch, the new branch asserts five stronger conjuncts, and a tripwire is watched refusing to open it
- The long run was launched DETACHED using 23-20's measured recipe, with the probe output quoted before any GPU second, and the log kept out of the noised glob
- `dp_n64` training is measured at the production shape with `grad_accum_steps=64`, 32 replay micro-batches, both synchronize boundaries and every denominator
- T matches between the n=8 and n=64 production runs, recorded, with no cross-σ ε claim made
- Throughput is measured on the real noised adapter across the real attack shapes under both stop conditions, cross-validated against the committed Phase-18 rates
- The `training.non_dp` provenance decision is made explicitly, argued in the record itself, with the superseded protocol's figure recorded beside it and every timing block naming its protocol
- Every published figure is a NAMED numeric field at full stored precision, COMPUTED and never typed — including `training.non_dp.wall_clock_gap_vs_superseded`, which re-derives exactly from the two `training_seconds_mean` fields. The eleven pre-registered paths 23-12 quotes all resolve
- Every borrowed figure cites its source record and digest; no projection is published as a measurement
- Every frozen pin is byte-unchanged and the two one-commit pins are still one commit
</success_criteria>

<output>
Create `.planning/phases/23-cost-calibration-the-0-diagnostic-and-budget-pre-registratio/23-11-SUMMARY.md` when done.
</output>
