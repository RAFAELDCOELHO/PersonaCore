---
phase: 23-cost-calibration-the-0-diagnostic-and-budget-pre-registratio
plan: 14
type: execute
wave: 17
# W2 — WAVE CORRECTED FROM 16, following 23-12 (15) -> 23-13 (16) -> 23-14 (17). Two reasons:
# 23-13 moved to wave 16, so this plan's `depends_on: 23-13` forces 17; and 23-12, 23-13 and this
# plan each write `.planning/REQUIREMENTS.md` (CAL-01/CAL-05, CAL-02, CTRL-03 respectively), which
# is a collision unless they are serialised.
depends_on: ["23-08", "23-13"]
files_modified:
  - scripts/phase23_run.py
  - tests/test_phase23_ctrl.py
  - results/phase23_never_taught.json
  - results/phase23_never_taught_run.log
  - data/phase23_run_state.json
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [CTRL-03]

must_haves:
  truths:
    - "CTRL-03: the never-taught fresh adapters are trained ONCE at identical budget and seed protocol (23-08) and SCORED here — serving as both the frontier's lower-left floor and the relearning reference. Scheduled once, consumed twice."
    - "D-08: the scoring is at the SAME N seeds the control used, so the extraction floor's provenance names `arm=\"never-taught\"` with >= `EXTRACTION_FLOOR_MIN_SEEDS` (MEASURED: 2) distinct seeds, and the FROZEN gate's `_prove` calls pass BY CONSTRUCTION rather than by luck two phases later."
    - "THIS IS THE PHASE'S LARGEST WALL-CLOCK ITEM BY AN ORDER OF MAGNITUDE — `N_CONTROL_SEEDS x h_per_point_ceiling_at_CURVE_K`, with N = 5. It MUST be launched DETACHED and MUST be resumable per seed: a kill may cost at most one seed, never the run. 23-17's harness kill at 3 of 5 seeds is the measured precedent."
    - "The unit is QUESTIONS, never draws. `mitigation_gate.extraction_ceiling` refuses a non-question denominator, and a draw-denominated count both deflates the rate and narrows the bound, in the same direction, so the error is invisible in the output."
    - "The scoring budget is the K that 23-13 pinned — the never-taught floor is scored at the same fidelity the sweep points will be, or the comparison is between two different instruments."
    - "The extraction NOISE FLOOR is reduced by `phase23_prereg.noise_floor`, the same blind-committed function that reduced the control floor and the matched floor. The reduction is never chosen in the writer."
    - "Phase 23 does NOT publish X. `extraction_ceiling` is called from a TEST only, to prove the record's provenance passes the frozen gate's refusals; the returned bound is not recorded and Phase 25 remains the phase that computes it."
    - "CTRL-03 IS TICKED HERE. It is one of ROADMAP:562's six requirements and NO OTHER PLAN OWNS IT — 23-12 ticks CAL-01 and CAL-05, 23-13 ticks CAL-02. This is the phase's LAST plan: a CTRL-03 left unticked here has nothing downstream that can close it."
    - "PER-SEED COMMIT NEEDS A MECHANISM, NOT AN INTENTION. MEASURED: `scripts/phase23_run.py` uses git READ-ONLY — `ls-files` at :859 and :2145, `show` at :2191 — and has no commit path at all. The mechanism is ONE DETACHED LAUNCH PER SEED with the operator committing between launches; the sub-mode scores one unscored seed and exits."
  artifacts:
    - path: "results/phase23_never_taught.json"
      provides: "the scored never-taught extraction readings, the reduced extraction noise floor, and the provenance mapping `mitigation_gate.extraction_ceiling` consumes"
      contains: "never-taught"
    - path: "tests/test_phase23_ctrl.py"
      provides: "the gate-provenance test driving the FROZEN `extraction_ceiling` with the record's own values"
      contains: "def test_never_taught_provenance_satisfies_the_gate"
    - path: ".planning/REQUIREMENTS.md"
      provides: "the CTRL-03 row ticked SATISFIED — the phase's LAST open requirement — and NOTHING else changed. Task 4 only"
      # DISCRIMINATING BY CONSTRUCTION: bare `CTRL-03` is ALREADY TRUE at HEAD (MEASURED: 2 hits —
      # the unticked checkbox row and its traceability row). The TICKED form is MEASURED absent at
      # HEAD (0), so this can only go true if Task 4 actually ran.
      contains: "- [x] **CTRL-03**"
  key_links:
    - from: "results/phase23_never_taught.json"
      to: "results/phase23_never_taught_training.json"
      via: "the scored record cites the training record and its sha256 — one scheduling, two consumers"
      pattern: "never_taught_training"
    - from: "tests/test_phase23_ctrl.py"
      to: "scripts/mitigation_gate.py::extraction_ceiling"
      via: "the FROZEN gate is IMPORTED and called with the record's own values; its refusals are the acceptance criterion"
      pattern: "extraction_ceiling"
---

<objective>
Score the never-taught adapters trained in 23-08, at the K pinned in 23-13, and commit a record whose
provenance the frozen Phase-25 gate will accept.

Purpose: `scripts/mitigation_gate.py:352-441` is FROZEN and holds the `_prove` calls at the one choke
point where an extraction floor's provenance is checked. It requires the floor to name
`arm == "never-taught"` (`:341`) and to report at least `EXTRACTION_FLOOR_MIN_SEEDS` distinct seeds
(`:346`, MEASURED: 2), with its own message stating that *"a single-seed floor is NOT a noise floor,
it is ONE DRAW."* That refusal fires in Phase 25 and the gate cannot be relaxed. D-08 resolved this
in the cheapest phase that can resolve it by scheduling the never-taught arm at the same N seeds as
D-03's control; this plan produces the scored record that satisfies it.

The frontier's lower-left floor and the relearning reference are the two consumers named in the
training record. Both read this one scored artifact — that is what "scheduled once and consumed
twice" means, and no second training call exists anywhere in the phase.

Output: `results/phase23_never_taught.json`, committed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-cost-calibration-the-0-diagnostic-and-budget-pre-registratio/23-CONTEXT.md
@.planning/phases/23-cost-calibration-the-0-diagnostic-and-budget-pre-registratio/23-RESEARCH.md
@.planning/phases/23-cost-calibration-the-0-diagnostic-and-budget-pre-registratio/23-VALIDATION.md
</context>

<environment>
**Python 3.11 venv is MANDATORY. This plan MUST run on the M3 (D-01).**

**THE D-04 RELEASE IS 23-11's, AND IT READS `results/phase23_matched_verdict.json`
(`verdict == "proceed"`), NEVER `results/phase23_sigma_zero.json` (`verdict == "HALT"`, permanently
and by design — 23-19 left it byte-unchanged so a reader sees both).** This plan inherits that
release and opens no gate of its own.

**THIS IS THE PHASE'S LARGEST WALL-CLOCK ITEM AND IT MUST BE LAUNCHED DETACHED.** The scoring cost is
`N_CONTROL_SEEDS x h_per_point_ceiling_at_CURVE_K`, N = 5. **Write it that way everywhere.** Bare
`h_per_point` is a member of `scripts/phase23_cost.py:158-163`'s `FORBIDDEN_MEAN_KEYS` — refused at
any nesting depth, precisely because an unsuffixed rate asserts a stop condition it did not measure —
and this plan's own must_have states the suffixed form. An earlier draft wrote the bare key here. At the K=48 ceiling that is tens of
hours; at a lower rung it is single-digit hours. Either way it exceeds the 600 s foreground cap by
orders of magnitude, and 23-17's run was harness-killed at ~60 min at 3 of 5 seeds.

**Two requirements follow, and neither is optional:**

  1. **DETACHED LAUNCH**, using 23-20's MEASURED recipe verbatim from `23-20-PLAN.md:985-1012`,
     substituting the sub-mode name and the log path `results/phase23_never_taught_run.log`. Three
     machine facts that recipe exists for: `setsid(1)` does not exist on macOS (`command -v setsid`
     exits 1, so `nohup setsid …` exits 127 while still printing a plausible pid); BSD `ps` has **no
     `sid` keyword** and `ps -o pid,pgid,sid` fails with **exit 0**, so a probe written that way
     passes vacuously; and `$!` after `cd … && nohup … &` is the SUBSHELL's pid. Take the pid from
     the log's FIRST LINE and probe with `os.getsid(pid)` from a second interpreter. Hold with
     `/usr/bin/caffeinate -is -w "$PID"`, **lid open** — caffeinate cannot hold with the lid closed.
     **NOTE: the recipe lives in that PLAN, not in `scripts/phase23_run.py` — measured,
     `grep -c "setsid" scripts/phase23_run.py` returns 0.**
  2. **PER-SEED RESUMABILITY — AND THE MECHANISM THAT DELIVERS IT, NAMED.** Score seed by seed and
     persist each seed's block through `scripts/phase23_run.py`'s existing
     `_state_record("never_taught", seed, block)` as soon as it completes, then **commit
     `data/phase23_run_state.json` and that seed's outputs before starting the next seed.** A kill
     must cost at most one seed. This is the exact failure 23-17 hit and 23-20's continuation
     pre-registration had to repair afterwards at real cost.

     **HOW THE COMMIT ACTUALLY HAPPENS. MEASURED: `scripts/phase23_run.py` HAS NO COMMIT PATH.** Its
     entire git surface is READ-ONLY — `ls-files` at `:859` and `:2145`, `show` at `:2191`, and
     nothing else. An earlier draft said "commit before starting the next seed" while the run
     launched as ONE detached process, so nothing could have performed that commit and the
     discipline was unimplementable as written. **The mechanism is:**

       **The `never-taught` sub-mode scores exactly ONE not-yet-scored seed, writes its
       `_state_record` block, and EXITS. The operator runs the detached-launch recipe ONCE PER SEED,
       committing `data/phase23_run_state.json` and that seed's outputs between launches.**

     Why this shape and not a git call inside the driver: it adds **zero** new code to a driver that
     deliberately touches git read-only; it introduces **no new flags and no new arguments** —
     `main()` dispatches on a single sub-mode token and this is one more token, not a new CLI shape;
     and the existing `_already_trained`-style predicate is exactly the skip-what-is-done logic a
     per-seed re-launch needs, already committed and already tested. The commit boundary becomes a
     real process boundary rather than a hoped-for one.

     **W3 — `never-taught` MUST BE REGISTERED IN `_TABLE` AND `USAGE`, AND AN EARLIER DRAFT READ AS
     FORBIDDING IT.** That draft said the shape needs "no CLI surface changes", which is true of
     *flags and arguments* and false of *registration*. MEASURED at HEAD, `_TABLE` holds exactly six
     keys — `cost`, `schedule`, `floor`, `sigma-zero`, `matched`, `matched-verdict` — **`never-taught`
     is not among them**, and `main()` is `if len(argv) != 1 or argv[0] not in _TABLE: raise
     SystemExit(USAGE)`. An unregistered token therefore raises `SystemExit(USAGE)` immediately: the
     **first** detached launch would exit before a single GPU second, after the probe had already
     been quoted as evidence of a healthy run. Task 1 registers the sub-mode in **both** `_TABLE` and
     `USAGE`, exactly as `23-11` does for its own `noised` / `throughput` sub-modes. (MEASURED: the
     name is free — it collides with neither the six existing keys nor 23-11's two.)

     `data/phase23_run_state.json` **is tracked** — MEASURED, `git ls-files` returns it, and
     `scripts/phase23_run.py:37-45` carries the dated retraction of the older "deliberately
     gitignored" claim. So the per-seed commits are visible in `git log` and the criterion below can
     count them.

     **Do not delete a partial seed's outputs to make a restart look like a first attempt** —
     23-20's `a629d93` is the precedent for discarding partial bytes VISIBLY, in their own commit,
     with the justification in the message.

**THE LOG PATH MUST NOT MATCH `results/phase23_noised_*`.** Same reasoning 23-20 used for
`results/phase23_resume_run.log`: a log inside `phase23_prereg.NOISED_RECORD_GLOB` becomes a member
of the glob every ordering guard binds on. `results/phase23_never_taught_run.log` is safe.

**FROZEN — no write:** `scripts/mitigation_accountant.py`, `scripts/mitigation_gate.py`,
`scripts/phase18_extraction.py`, `scripts/phase23_prereg.py` (byte-identical to `c7de5d4`),
`scripts/phase23_matched_prereg.py` (byte-identical to `c100388`, EXACTLY 1 commit — a second commit
permanently reddens the ancestry guard and delete-and-re-add cannot undo it),
`scripts/phase23_resume_prereg.py` (1 commit, same hazard), `scripts/mitigation_budget.py` (23-13's
— read `CURVE_K` and `N_CONTROL_SEEDS` from it, never write), and every committed `results/phase23_*`
artifact.

**Read `deferred-items.md`'s CONTROL PROVENANCE rule before writing any provenance field.** It
governs the formal gate's three utility fields and requires them to come from
`results/phase23_matched_control.json`. This plan writes none of those three, so the rule does not
bind its fields — but the record it produces is a floor the same gate consumes, so the discipline
applies: name the protocol every borrowed figure came from.
</environment>

<tasks>

<task type="auto">
  <name>Task 1: Project the cost, launch detached, then score the N never-taught adapters at the pinned K</name>
  <files>scripts/phase23_run.py</files>
  <read_first>
    - `results/phase23_never_taught_training.json` — the adapter paths, their sha256 values, the seed
      list and the recipe. **MEASURED at HEAD: `n_seeds = 5`, `seeds = [1337, 2024, 1338, 2025,
      1339]`, `consumers = ["frontier lower-left floor", "relearning reference"]`. The adapters are
      consumed from here; nothing is retrained**
    - `scripts/mitigation_budget.py` — `CURVE_K`, `N_CONTROL_SEEDS` and their `_PROVENANCE` siblings,
      as pinned by 23-13
    - `results/phase23_cost.json` — `generation.h_per_point_floor` / `h_per_point_ceiling` and the
      `sizing` block containing the never-taught line item at each rung
    - `scripts/phase23_run.py` — `_TABLE`, `USAGE` and `main()`'s dispatch (locate `_TABLE` by
      SYMBOL, not by line number). **MEASURED at HEAD: six keys — `cost`, `schedule`, `floor`,
      `sigma-zero`, `matched`, `matched-verdict` — and `never-taught` is NOT among them.** `main()`
      is `if len(argv) != 1 or argv[0] not in _TABLE: raise SystemExit(USAGE)`, so the sub-mode must
      be registered in BOTH before any launch (W3)
    - `scripts/phase23_run.py:429-469` — `_state_load`, `_state_write`, `_state_record`; and `:512`
      `_already_trained`. **This is the per-seed resumability the plan requires; it already exists,
      do not reinvent it**
    - `scripts/phase23_run.py:728-790` — `score_adapter`, `score_control`. The scoring pipeline a
      real scored run uses
    - `scripts/phase18_extraction.py:60-96` — the four attack families, `K = 48`,
      `ASR_RUNGS = (1, 4, 16, K)`, and `:88-92`'s reason a K may not be reduced after seeing a null.
      **Import read-only — ancestry-guarded and permanently uneditable, so the attack trained against
      and the attack scored by cannot silently diverge**
    - `scripts/mitigation_gate.py:352-441` — `extraction_ceiling`'s parameter names and the exact
      unit requirement: *"`nontarget_questions` MUST be a count of QUESTIONS, never draws"*.
      **Read only**
    - `scripts/phase14_recall.py` — `load_adapted_model`, `STOP_IDS`, `RECALL_MAX_NEW_TOKENS`,
      `N_SEEDED_SAMPLES`, the `forbid_ids` mask
    - `scripts/phase23_cost.py:395` — `time_iterations`, the timing helper with its synchronize
      boundaries
    - `.planning/phases/23-*/23-20-PLAN.md:976-1044` — **the measured detached-launch recipe**
  </read_first>
  <action>
**(a) Project first, and record the projection — before any GPU second.** Compute
`N_CONTROL_SEEDS x h_per_point_ceiling_at_CURVE_K` and the floor-side equivalent, both read from the
pinned constants and the committed cost record. Print and record both. If the ceiling projection
materially exceeds what `results/phase23_cost.json`'s `sizing` block priced for this line item, stop
and report the discrepancy rather than proceeding — the sizing table is what 23-13's K selection was
made from, and a projection that disagrees with it means one of the two is wrong.

**Then size the SESSION against the projection and say so out loud.** If the projected total exceeds
a few hours, the run spans multiple sessions by construction, and the per-seed commit discipline in
the environment block is what makes that survivable. State in the SUMMARY how many seeds you expect
per session and what a kill would cost.

**(a2) REGISTER THE SUB-MODE FIRST — before the first launch, not after the first failure.** Add
`never-taught` to `scripts/phase23_run.py`'s `_TABLE` and write its `USAGE` paragraph in the register
the six existing entries use, stating that it scores exactly ONE not-yet-scored seed and exits, and
that the operator commits `data/phase23_run_state.json` between launches. **MEASURED at HEAD the key
is absent and `main()` raises `SystemExit(USAGE)` for an unregistered token** (W3), so an unregistered
launch dies instantly while still having printed a plausible `SESSION` line. Verify before launching:
`.venv/bin/python scripts/phase23_run.py --help` lists `never-taught`, and
`.venv/bin/python -c "import sys;sys.path.insert(0,'scripts');import phase23_run;print(sorted(phase23_run._TABLE))"`
includes it.

**(b) Launch detached — ONCE PER SEED.** Copy 23-20's recipe from `23-20-PLAN.md:985-1012` verbatim,
with the sub-mode name and the log path `results/phase23_never_taught_run.log`. Quote the probe's
output — `pid == pgid == sid`, taken from the log's first line, never from `$!` — **before any GPU
second is spent**, on **every** launch. Commit the log as detachment evidence, exactly as 23-20
committed `results/phase23_resume_run.log`.

**The loop is: launch → probe → wait → commit → launch.** The sub-mode scores one unscored seed and
exits (see the environment block's mechanism note); repeat until every seed is scored. Append to the
same log rather than truncating it, so all N `SESSION` lines survive as one evidence trail. The
driver has no commit path — the commit is the operator's act between launches, and that is what
makes "a kill costs at most one seed" true rather than intended.

**(c) Score each adapter.** For each of the N never-taught adapters, load it through
`phase14_recall.load_adapted_model` — the same `weights_only=True` load-before-inject path a real
scored run uses, so a never-taught number and a sweep-point number come off the same pipeline — and
run the Phase-18 attack suite at `CURVE_K` draws per question, across the same attack families a
sweep point will be scored on.

**Record COUNTS, not rates**, and record the denominators beside them:
  - `nontarget_successes` and `nontarget_questions` per seed, **both counts of QUESTIONS**. A
    question counts as a success under the same success predicate `phase18_extraction` applies —
    import it, do not re-implement it. A second copy of a scoring predicate is a second predicate,
    free to stop matching.
  - `draws_per_question` (`CURVE_K`), `total_draws`, per-family breakdown, and the wall clock per
    seed with the timing helper's synchronize boundaries.

**The unit is the thing most likely to go wrong and it is invisible when it does.** A
draw-denominated count both deflates the rate and narrows the bound, in the same direction, so the
error does not surface in the output. Assert `0 <= nontarget_successes <= nontarget_questions` and
that `nontarget_questions` equals the question count the attack suite enumerated — not `total_draws`,
and not `total_draws // CURVE_K` computed after the fact.

**Persist and commit after EVERY seed** — `_state_record("never_taught", seed, block)`, then the
process EXITS and the operator commits `data/phase23_run_state.json` and that seed's outputs before
the next launch. Do not batch the writes to the end, and do not attempt the commit from inside the
driver: it has no commit path and adding one is not this plan's business.

**(d) Reduce the extraction noise floor** by calling `phase23_prereg.noise_floor` on the N per-seed
success RATES (successes ÷ questions per seed). **The driver must not compute a spread itself** —
the same discipline the control floor and the matched floor followed, through the same
blind-committed function. `tests/test_phase23_matched.py` already carries a committed guard that the
writer does not inline the reduction; expect an equivalent one here.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase23_ctrl.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records the projected scoring cost at BOTH the floor and the ceiling BEFORE the run, the sizing-block figure it was checked against, and the actual measured wall clock after, with the delta
    - **W3 — the sub-mode is REGISTERED before the first launch:** `.venv/bin/python -c "import sys;sys.path.insert(0,'scripts');import phase23_run;print(sorted(phase23_run._TABLE))"` includes `never-taught`, and `.venv/bin/python scripts/phase23_run.py --help` lists it with its own `USAGE` paragraph. MEASURED at HEAD it is absent from `_TABLE`, and `main()` raises `SystemExit(USAGE)` on an unregistered token — so without this the first detached launch exits before any GPU second while still printing a plausible `SESSION` line
    - The detachment probe's output is quoted verbatim before any GPU second, and `head -1 results/phase23_never_taught_run.log` shows a `SESSION pid=N pgid=N sid=N` line with all three equal
    - **BINDING, NOT PROSE-SATISFIABLE:** `git log --oneline -- data/phase23_run_state.json | wc -l | tr -d ' '` equals the pre-run baseline **plus N**, one commit per scored seed. Record the baseline BEFORE the first launch (MEASURED at HEAD: **3**) and the post-run count, and quote both. **The earlier draft's escape clause "or the SUMMARY names and justifies any batching" is DELETED** — a prose justification is not a mechanism, and on a multi-hour item whose direct precedent (23-17) died at 3 of 5 seeds it is the wrong thing to make optional
    - One detached launch per seed, each with its own probe: `grep -c "SESSION pid=" results/phase23_never_taught_run.log` (or the per-seed log set) equals N, and the SUMMARY quotes each probe line
    - The SUMMARY records, per seed, `nontarget_successes`, `nontarget_questions`, `draws_per_question` and `total_draws`, and states `nontarget_questions` is a QUESTION count with the source of that count named
    - No adapter was retrained: `git diff --exit-code -- results/phase23_never_taught_training.json` exits 0 and each adapter's live sha256 matches the training record's
    - The success predicate is IMPORTED, verified by AST over `scripts/phase23_run.py`'s import nodes (not grep — the file's own prose names `phase18_extraction`): `.venv/bin/python -c "import ast,pathlib;t=ast.parse(pathlib.Path('scripts/phase23_run.py').read_text());print(sorted({n.module for n in ast.walk(t) if isinstance(n,ast.ImportFrom) and n.module} | {a.name for n in ast.walk(t) if isinstance(n,ast.Import) for a in n.names}))"` includes `phase18_extraction`
    - `git ls-files 'results/phase23_noised_*' | wc -l | tr -d ' '` is unchanged from 23-11's count — this plan writes no sweep point, and the log is not in that glob
    - `git diff --exit-code -- scripts/phase18_extraction.py scripts/mitigation_gate.py scripts/mitigation_budget.py scripts/phase23_prereg.py scripts/phase23_matched_prereg.py scripts/phase23_resume_prereg.py` exits 0
  </acceptance_criteria>
  <done>The N never-taught adapters from 23-08 are scored at the pinned K on the real attack suite, launched detached and probed, committed seed by seed so a kill costs at most one seed, with question-denominated counts, an imported success predicate, and the projection recorded before the run.</done>
</task>

<task type="auto">
  <name>Task 2: Emit and commit `results/phase23_never_taught.json`</name>
  <files>results/phase23_never_taught.json, scripts/phase23_run.py</files>
  <read_first>
    - `scripts/mitigation_gate.py:341-350` — `NEVER_TAUGHT_ARM = "never-taught"`,
      `EXTRACTION_FLOOR_MIN_SEEDS = 2`, `EXTRACTION_FLOOR_PROVENANCE_KEYS = ("arm", "seeds")`.
      **Read only.** The provenance mapping this record emits must carry every key in that tuple
    - `results/phase19_noise_floors.json` — the committed floor-record shape
    - `results/phase23_matched_control.json` — the current best example of a floor record with a full
      provenance block and a named reduction symbol
    - `scripts/phase23_prereg.py:85` `NEVER_TAUGHT_RECORD`, `:81` `NEVER_TAUGHT_TRAINING_RECORD`,
      `:143` `noise_floor`, `:195` `FLOOR_PROVENANCE_KEYS`
    - `scripts/phase23_cost.py:204` — `validate_record`
  </read_first>
  <action>
Emit `results/phase23_never_taught.json` — **path resolved by CALLING
`phase23_prereg.NEVER_TAUGHT_RECORD`, never retyped** — carrying:

  - `extraction_floor_provenance` — a mapping carrying **every key in
    `EXTRACTION_FLOOR_PROVENANCE_KEYS`**: `arm` (the literal `"never-taught"`, read from
    `mitigation_gate.NEVER_TAUGHT_ARM` rather than typed) and `seeds` (the N distinct seed values),
    plus `record`, `record_sha256`, `git_sha`, `device`, `torch_version`, `reduction`
    (`"phase23_prereg.noise_floor"` — the symbol), `k`, `questions` and `governs`. This is the
    mapping Phase 25 hands to `extraction_ceiling`, and its `_prove` calls are the only choke point
    at which a floor's provenance is checked.
  - `per_seed` — per seed: `seed`, `adapter`, `adapter_sha256`, `nontarget_successes`,
    `nontarget_questions`, `rate`, per-family breakdown, `draws_per_question`, `total_draws`,
    `seconds`.
  - `extraction_noise_floor` — the reduction's output.
  - `pooled` — the pooled `nontarget_successes` and `nontarget_questions` a Phase-25 consumer would
    pass as the floor's point estimate, with the pooling rule named explicitly. **State whether the
    pooled counts are a sum across seeds or a single designated seed**, because the two give
    different Wilson bounds and an unstated choice is a choice made with the numbers visible.
  - `source_training_record` and `source_training_record_sha256` — one scheduling, two consumers.
  - `consumers` — the two names, matching the training record's list exactly.
  - Full provenance: `git_sha`, `device`, `torch_version`, `python_version`, `timestamp`, `curve_k`
    and the budget module's constants it was scored at, each cited to `scripts/mitigation_budget.py`.

Validate before writing; **a record missing any provenance key is REFUSED, not defaulted.** Commit it.

**Phase 23 does not publish X.** Do not compute or record `extraction_ceiling`'s return value in this
artifact. The gate's own D-13 note records that the extraction floor arrives as a required kwarg and
that X is never a literal; Phase 25 computes it. What this plan proves is that the record's
provenance is ACCEPTABLE to the gate, and that proof lives in Task 3's test.

**Assert "X is not published" STRUCTURALLY, not with a text grep.** A `governs` field will naturally
NAME `mitigation_gate.extraction_ceiling` as the consumer, and a `grep -c "extraction_ceiling"`
would then go non-zero and fail a correct record — the same false-RED class this repository has hit
with `MATCHED_CONTROL_NOISE_FLOOR` inside `CONTROL_NOISE_FLOOR` and `isclose` inside `disclosed`.
Instead walk the loaded JSON's keys at every depth and assert **no KEY** is `extraction_ceiling`,
`extraction_bound` or `x`, and that no numeric leaf sits under a key containing `ceiling` or `bound`.
Naming the symbol in a string VALUE is provenance and is allowed; storing a number under it is the
publication this forbids.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase23_ctrl.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `results/phase23_never_taught.json` is committed and `.venv/bin/python -c "import json;d=json.load(open('results/phase23_never_taught.json'));p=d['extraction_floor_provenance'];print(p['arm'], len(set(p['seeds'])), sorted(p))"` prints `never-taught`, a distinct-seed count **>= 2**, and every required key
    - `.venv/bin/python -c "import json;d=json.load(open('results/phase23_never_taught.json'));print(d['pooled'])"` prints question-denominated counts, and the pooling rule is named in the record
    - **X is not published, checked structurally — BOTH conjuncts.** The criterion at `<action>` requires (i) no key at any depth is `extraction_ceiling` / `extraction_bound` / `x` AND (ii) no numeric leaf sits under a key containing `ceiling` or `bound`. **The earlier draft's one-liner implemented only (i)** — verified against a constructed record, it returned `['a.extraction_ceiling', 'b.x']` and MISSED `c.nontarget_ceiling: 0.4`, a numeric leaf under a `ceiling` key. Use this instead, which was verified catching all three while leaving a `governs` string VALUE naming the symbol untouched:
      ```bash
      .venv/bin/python -c "
      import json;B={'extraction_ceiling','extraction_bound','x'};bad=[]
      def w(n,p=()):
          if hasattr(n,'keys'):
              for k,v in n.items():
                  k=str(k);q=p+(k,)
                  if k in B: bad.append('KEY '+'.'.join(q))
                  if ('ceiling' in k or 'bound' in k) and isinstance(v,(int,float)) and not isinstance(v,bool): bad.append('NUM '+'.'.join(q))
                  w(v,q)
          elif isinstance(n,list):
              [w(v,p+(str(i),)) for i,v in enumerate(n)]
      w(json.load(open('results/phase23_never_taught.json')));print(bad)
      "
      ```
      prints `[]`. Task 3's permanent test `test_x_is_not_published_in_phase_23` implements the same two conjuncts and is the durable guard; this command is the one-shot check
    - The record's path was resolved by calling `phase23_prereg.NEVER_TAUGHT_RECORD` — the SUMMARY quotes the constant's value beside the on-disk path
    - `.venv/bin/python -m pytest tests/test_phase23_prereg.py -q` exits 0 with this artifact present
    - `git diff --exit-code -- scripts/mitigation_gate.py scripts/mitigation_accountant.py scripts/phase23_prereg.py scripts/phase23_matched_prereg.py scripts/phase23_resume_prereg.py scripts/mitigation_budget.py` exits 0
  </acceptance_criteria>
  <done>The scored never-taught record is committed with the frozen gate's required provenance keys, question-denominated counts, a named pooling rule, and a citation back to the single training scheduling — without publishing X, proven by a structural key walk rather than a grep that would match its own provenance string.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the record passes the FROZEN gate's refusals</name>
  <files>tests/test_phase23_ctrl.py</files>
  <read_first>
    - `scripts/mitigation_gate.py:352-441` — `extraction_ceiling`'s `ValueError` domain checks and
      its `_prove` calls, with their messages. **Read every one; each is a case below.**
      **MEASURED at HEAD by AST: FIVE refusals — 2 `raise` at `:405` and `:410`, 3 `_prove` at
      `:417`, `:425`, `:436`.** Re-count from the source rather than trusting this line, and
      parametrize over the count you find
    - `tests/test_phase23_ctrl.py` — current state after 23-08; in particular
      `test_never_taught_is_trained_once` (`:62`) and
      `test_never_taught_seed_count_satisfies_the_frozen_gate` (`:102`), which this task extends
      rather than duplicates
    - `results/phase23_never_taught.json`, `results/phase23_never_taught_training.json`
    - `scripts/phase23_prereg.py:143` — `noise_floor`
  </read_first>
  <action>
Add to `tests/test_phase23_ctrl.py`:

  - `test_never_taught_provenance_satisfies_the_gate` — import `mitigation_gate`, load the committed
    record, and call `extraction_ceiling(nontarget_successes=..., nontarget_questions=...,
    extraction_noise_floor=..., extraction_floor_provenance=...)` with the record's OWN values.
    **Assert it does not raise.** Do not assert on the returned bound and do not record it — the test
    proves acceptability, not a published X. State that scoping in the docstring.
  - `test_the_gate_refuses_a_degraded_version_of_this_record` — the watched RED, parametrized over
    **every refusal `extraction_ceiling` contains**, each driven from a copy of the real record:
      (i) `nontarget_questions = 0` → `ValueError`;
      (ii) `nontarget_successes > nontarget_questions` → `ValueError`;
      (iii) provenance with the `arm` key dropped → refusal;
      (iv) `arm` set to anything other than `mitigation_gate.NEVER_TAUGHT_ARM` → refusal, and the
           message names D-12's borrowed Phase-19 floor;
      (v) `seeds` reduced to one distinct value → refusal, and the message contains the one-draw
          reasoning.
    **MEASURED at HEAD: five refusals, and the five cases above are exactly them.** Re-count from
    the source anyway; if the count has moved, cover what is there and say so in the SUMMARY rather
    than matching this list. **All cases come
    from the same real record**, so the test proves the gate refuses degradations of THIS artifact
    rather than of a synthetic one, and the committed artifact is never edited to produce them.
  - `test_the_extraction_floor_re_derives` — recompute `extraction_noise_floor` from the record's
    per-seed rates through `phase23_prereg.noise_floor` and assert exact `==`.
  - `test_the_never_taught_record_cites_one_training_scheduling` — assert `source_training_record`
    equals `phase23_prereg.NEVER_TAUGHT_TRAINING_RECORD`, that `source_training_record_sha256`
    matches the file's live digest, that `consumers` equals the training record's `consumers`
    exactly, and that the seed list is identical in both records. Together with
    `test_never_taught_is_trained_once` from 23-08 this is what makes "scheduled once and consumed
    twice" a checked property.
  - `test_the_denominator_is_questions_not_draws` — assert `nontarget_questions != total_draws`
    whenever `draws_per_question > 1`, and that
    `total_draws == nontarget_questions * draws_per_question`. The unit error is invisible in the
    output, so it needs its own assertion rather than a comment.
  - `test_x_is_not_published_in_phase_23` — the structural key walk from Task 2, as a permanent
    guard: no key at any depth is `extraction_ceiling` / `extraction_bound` / `x`, and no numeric
    leaf sits under a key containing `ceiling` or `bound`. **Docstring must say why this is a key
    walk and not a grep**: the record's own `governs` string names the consuming symbol, so a text
    match would fail a correct record.

**AST or key walks, never grep, in every structural criterion.** This test file's own docstrings will
contain `extraction_ceiling`, `never-taught` and `nontarget_questions`.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase23_ctrl.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_phase23_ctrl.py -v` exits 0 with zero skips
    - `.venv/bin/python -m pytest "tests/test_phase23_ctrl.py::test_the_gate_refuses_a_degraded_version_of_this_record" -v` collects **5** cases (MEASURED: 2 `raise` + 3 `_prove` in `extraction_ceiling`) and all pass; the SUMMARY quotes the count re-read from the source with the AST command shown
    - `.venv/bin/python -m pytest "tests/test_phase23_ctrl.py::test_the_extraction_floor_re_derives" "tests/test_phase23_ctrl.py::test_the_denominator_is_questions_not_draws" "tests/test_phase23_ctrl.py::test_the_never_taught_record_cites_one_training_scheduling" "tests/test_phase23_ctrl.py::test_x_is_not_published_in_phase_23" -v` exits 0
    - The frozen gate is IMPORTED and its constants never retyped, verified by AST over the test module's import nodes: `.venv/bin/python -c "import ast,pathlib;t=ast.parse(pathlib.Path('tests/test_phase23_ctrl.py').read_text());print(sorted({a.name for n in ast.walk(t) if isinstance(n,ast.Import) for a in n.names} | {n.module for n in ast.walk(t) if isinstance(n,ast.ImportFrom) and n.module}))"` includes `mitigation_gate`
    - `git diff --exit-code -- results/phase23_never_taught.json` exits 0 after the refusal cases run — the degradations are made on COPIES, never on the committed artifact
    - `.venv/bin/python -m pytest tests/ -q` reports 0 failed; `make lint` exits 0
    - `git diff --exit-code -- scripts/mitigation_gate.py scripts/mitigation_accountant.py scripts/phase23_prereg.py scripts/phase23_matched_prereg.py scripts/phase23_resume_prereg.py scripts/mitigation_budget.py scripts/phase18_extraction.py pyproject.toml` exits 0
    - `git log --format=%H -- scripts/phase23_matched_prereg.py | wc -l | tr -d ' '` still prints **1**
  </acceptance_criteria>
  <done>The committed record is accepted by the frozen gate's real code path, every one of its refusals is observed firing against degraded copies of that same record, the floor re-derives, the one-scheduling-two-consumers claim is a checked property, and "Phase 23 does not publish X" is a permanent structural guard rather than a comment.</done>
</task>

<task type="auto">
  <name>Task 4: Tick CTRL-03 — the phase's last open requirement</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/ROADMAP.md:562` — `**Requirements**: CAL-01, CAL-02, CAL-03, CAL-05, DPSGD-06,
      CTRL-03`. **This is the phase's contract and this plan closes its last open row.** By the time
      this task runs: CAL-03 `[x]` (23-04), DPSGD-06 `[x]` (23-10), CAL-01 + CAL-05 `[x]` (23-12),
      CAL-02 `[x]` (23-13). CTRL-03 is the only one left, **and this is the phase's last plan**
    - `.planning/REQUIREMENTS.md` — the `- [ ] **CTRL-03**` checkbox row and the
      `| CTRL-03 | Phase 23 | never-taught fresh adapter; depends on nothing, scheduled once |`
      traceability row. **Locate both by TEXT, never by line number**
    - `.planning/REQUIREMENTS.md` — a SATISFIED row already written in this phase's convention (the
      `| CAL-03 | Phase 23 | **SATISFIED (plan 23-04).** …` row, or the `| DPSGD-06 | …` row).
      **Read one in full**; the tick is the checkbox AND the traceability cell, in that register
    - `.planning/phases/23-*/23-13-SUMMARY.md` — how 23-13 ticked CAL-02 one wave earlier; match its
      shape so the phase's ticks read as one convention
    - `results/phase23_never_taught.json` and `results/phase23_never_taught_training.json` — the
      evidence the note cites, with their sha256 digests
  </read_first>
  <action>
**WHY THIS TASK EXISTS.** An earlier draft of this plan never mentioned `.planning/REQUIREMENTS.md`
at all — not in `files_modified`, not in any task, zero grep hits — while carrying
`requirements: [CTRL-03]` in its frontmatter. CTRL-03 would have finished the phase unticked, and
since this is the phase's **last** plan there is nothing downstream that could close it.

Tick `- [ ] **CTRL-03**` to `- [x]`, and fill the `| CTRL-03 | Phase 23 |` traceability cell with a
SATISFIED note in this table's existing convention, naming:

  - the plans (**23-08** trained, **23-14** scored) and the artifact
    `results/phase23_never_taught.json` with its sha256;
  - **"scheduled once, consumed twice" as a CHECKED property, not a claim** — the scored record
    cites `results/phase23_never_taught_training.json` by path and digest, `consumers` matches that
    record's list exactly, the seed lists are identical in both, and
    `test_the_never_taught_record_cites_one_training_scheduling` plus 23-08's
    `test_never_taught_is_trained_once` are what prove it;
  - the N distinct seeds and that the count clears the FROZEN `EXTRACTION_FLOOR_MIN_SEEDS` (2),
    with the gate's own reason quoted: *"a single-seed floor is NOT a noise floor, it is ONE DRAW"*;
  - that the record is accepted by the frozen `mitigation_gate.extraction_ceiling`'s real code path,
    and that **every one of its refusals was watched firing** on degraded COPIES of that same record;
  - that counts are **question-denominated**, with the `total_draws == nontarget_questions *
    draws_per_question` identity asserted;
  - that the noise floor was reduced by the blind-committed `phase23_prereg.noise_floor` and
    re-derives under exact `==`;
  - **that Phase 23 does NOT publish X** — `extraction_ceiling` is called from a test only, its
    return value is not recorded, and the structural key walk is a permanent guard.

**Hand-edit with `Edit`. NO `gsd-sdk` mutation handler may touch this file** — 23-19 recorded that
`roadmap.update-plan-progress` keys on SUMMARY EXISTENCE and falsely ticked 23-17. If one is used
anyway, `git diff .planning/REQUIREMENTS.md` immediately and hand-repair.

**This is the ONLY task in this plan that may modify `.planning/REQUIREMENTS.md`.** Tasks 1, 2 and 3
never touch it.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase23_ctrl.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^- \[x\] \*\*CTRL-03\*\*" .planning/REQUIREMENTS.md | tr -d ' '` returns **1**
    - The traceability row is filled: `.venv/bin/python -c "import pathlib;print([l for l in pathlib.Path('.planning/REQUIREMENTS.md').read_text().splitlines() if l.startswith('| CTRL-03 |')][0][:120])"` shows a third cell containing `SATISFIED (plans 23-08, 23-14)`
    - **B-1 — THE DIFF FILTER IS `^[<>]` ON SWAPPED INDICATORS, NEVER `^[+-][^+-]`.** An earlier draft filtered changed lines with `grep -E '^[+-][^+-]'`, which requires the SECOND character not to be `+`/`-`. **Every requirement checkbox is a markdown bullet `- `**, so its diff lines are `-- [ ] **CTRL-03**…` and `+- [x] **CTRL-03**…` — both dropped as if they were `---`/`+++` file headers. REPRODUCED on a scratch repo: with the intended tick PLUS an illicit re-tick of a closed row, **both guards printed `0` (PASS)**. (`--output-indicator-old` / `--output-indicator-new` need git >= 2.22; MEASURED here: 2.50.1. An older git rejects the flag and the guard fails LOUDLY rather than silently passing.) Swapping the diff's own line indicators is unambiguous — the `--- a/…` / `+++ b/…` headers keep their `-`/`+` while changed lines become `<`/`>`, so no content line can be mistaken for a header (a prefix filter such as `grep -vE '^(\+\+\+ |--- )'` still drops a genuine deleted line beginning `-- `, MEASURED reporting 1 where 2 was correct). **W4 — `grep -c` printing `0` EXITS 1**, so both guards are wrapped in a string comparison rather than read by exit status. VERIFIED: on the scratch violation the two commands below return `2` and `2`; on a clean CTRL-03-only tick they return `0` and `0`.
    - **THE NARROW FREEZE — every changed line in this file names CTRL-03:**
      ```bash
      [ "$(git diff -U0 --output-indicator-old='<' --output-indicator-new='>' -- .planning/REQUIREMENTS.md | grep -E '^[<>]' | grep -cv 'CTRL-03')" = 0 ]
      ```
      exits 0. (This filters a DIFF, whose lines are only the changed ones — not a source file whose prose would match itself.)
    - **No already-closed requirement is re-ticked — and the guard can now SEE an un-tick, which under the old filter it could not:**
      ```bash
      [ "$(git diff -U0 --output-indicator-old='<' --output-indicator-new='>' -- .planning/REQUIREMENTS.md | grep -E '^[<>]' | grep -c -E 'CAL-0[1235]|DPSGD-06')" = 0 ]
      ```
      exits 0. The state assertion below is the independent backstop: a diff guard proves nothing was *changed*, the state check proves the resulting table is *right*
    - **ALL SIX of `ROADMAP.md:562`'s requirements are now `[x]`, quoted in the SUMMARY with their owning plan:** `.venv/bin/python -c "import pathlib,re;t=pathlib.Path('.planning/REQUIREMENTS.md').read_text();print({r:bool(re.search(r'^- \[x\] \*\*'+r+r'\*\*',t,re.M)) for r in ('CAL-01','CAL-02','CAL-03','CAL-05','DPSGD-06','CTRL-03')})"` prints `True` for every one
    - No `gsd-sdk` mutation handler touched the file; the SUMMARY says so explicitly
    - `git diff --exit-code -- scripts/ tests/ results/ .planning/ROADMAP.md .planning/STATE.md pyproject.toml` exits 0 — this task edits ONE file
  </acceptance_criteria>
  <done>CTRL-03 is ticked with a SATISFIED note naming both plans, the scored artifact and its digest, the checked scheduled-once-consumed-twice property, the seed count against the frozen minimum, the gate acceptance with every refusal watched, the question denominator, the re-derived floor and the not-publishing-X guard — and the ONLY lines changed in `.planning/REQUIREMENTS.md` are CTRL-03's, with all six of the phase's requirements now closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 23's floor record → Phase 25's frozen gate | `_prove` calls at one choke point, two phases later, in a file with no correction path. A record that fails there fails after the sweep has been run. |
| draws → questions | `extraction_ceiling`'s locked unit. A draw-denominated count deflates the rate AND narrows the bound in the same direction, so the error never surfaces in the output. |
| never-taught floor → sweep points | Scored at a different K, the floor and the points are two different instruments and the frontier's lower-left corner is meaningless. |
| a scored artifact → a published bound | Phase 23 measures the floor; Phase 25 computes X from it. |
| a multi-hour run → a foreground harness call | The phase's largest wall-clock item. 23-17 lost 3 of 5 seeds to exactly this. |
| a provenance string → a text-matching guard | The record NAMES `extraction_ceiling` as its consumer. A grep-based "X is not published" check fails a correct record. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-75 | Spoofing | a single-seed floor refused two phases later | mitigate | N seeds from 23-08's one scheduling; the distinct-seed count asserted against the IMPORTED `EXTRACTION_FLOOR_MIN_SEEDS`; the gate's own refusal watched firing on a degraded copy |
| T-23-76 | Tampering | a draw-denominated denominator | mitigate | `test_the_denominator_is_questions_not_draws` asserts the identity `total_draws == questions * draws_per_question` and the inequality with `questions` |
| T-23-77 | Repudiation | a re-implemented success predicate diverging from the attack suite's | mitigate | `phase18_extraction`'s predicate is imported read-only, verified by AST; the file is ancestry-guarded and asserted byte-unchanged |
| T-23-78 | Tampering | a second never-taught training call making "trained once" false | mitigate | Adapters consumed from the training record by path and sha256; `test_the_never_taught_record_cites_one_training_scheduling` plus 23-08's AST scan |
| T-23-79 | Information Disclosure | Phase 23 publishing X and pre-empting the gate | mitigate | `extraction_ceiling` is called from a TEST only and its return value is not recorded; `test_x_is_not_published_in_phase_23` walks the record's KEYS at every depth rather than matching text |
| T-23-79b | Spoofing | a text-matching X guard failing a correct record because provenance names the symbol | mitigate | The guard is a key walk; naming the symbol in a string VALUE is explicitly allowed and stated in the docstring |
| T-23-80 | Repudiation | an unstated pooling rule chosen with the numbers visible | mitigate | The pooling rule is a required named field in the record, and the choice between summing and designating is stated explicitly |
| T-23-80b | Denial of Service | the phase's largest run killed mid-way, losing every scored seed | mitigate | Detached launch via 23-20's measured recipe with the pid probed before any GPU second, **once per seed**: the sub-mode scores ONE unscored seed and exits, and the operator commits between launches, so the commit boundary is a process boundary. MEASURED: the driver's git surface is read-only (`ls-files` :859/:2145, `show` :2191), so an in-process commit was never available. The criterion counts commits against a recorded baseline instead of accepting a prose justification |
| T-23-81 | Repudiation | CTRL-03 left unticked by the phase's LAST plan, with nothing downstream able to close it | mitigate | Task 4 owns `.planning/REQUIREMENTS.md` and ticks it; a narrow diff guard proves every changed line names CTRL-03 and none re-ticks an already-closed row; an acceptance command asserts all six of `ROADMAP.md:562`'s requirements are `[x]` |
| T-23-SC | Tampering | npm/pip/cargo installs | accept | **Zero installs.** `pyproject.toml` byte-unchanged (RPT-03) |
</threat_model>

<verification>
```bash
head -1 results/phase23_never_taught_run.log
.venv/bin/python -m pytest tests/test_phase23_ctrl.py -v
.venv/bin/python -c "import json;d=json.load(open('results/phase23_never_taught.json'));p=d['extraction_floor_provenance'];print(p['arm'], sorted(set(p['seeds'])), d['pooled'])"
.venv/bin/python -m pytest tests/test_phase23_prereg.py tests/test_phase23_budget.py -q
git log --oneline -- data/phase23_run_state.json | head
git ls-files 'results/phase23_*' | wc -l | tr -d ' '
git log --format=%H -- scripts/phase23_matched_prereg.py | wc -l | tr -d ' '
git diff --exit-code -- scripts/mitigation_gate.py scripts/mitigation_accountant.py scripts/phase23_prereg.py scripts/phase23_matched_prereg.py scripts/phase23_resume_prereg.py scripts/mitigation_budget.py scripts/phase18_extraction.py pyproject.toml
# Task 4's narrow guards on .planning/REQUIREMENTS.md. B-1: the filter is `^[<>]` on SWAPPED diff
# indicators — `^[+-][^+-]` drops every `- [ ] **CTRL-03**` checkbox row as if it were a `---`/`+++`
# header, and was MEASURED returning 0 (PASS) against an illicit re-tick. W4: `grep -c` printing 0
# EXITS 1, so both are wrapped in a string comparison instead of read by exit status. Both exit 0:
[ "$(git diff -U0 --output-indicator-old='<' --output-indicator-new='>' -- .planning/REQUIREMENTS.md | grep -E '^[<>]' | grep -cv 'CTRL-03')" = 0 ]
[ "$(git diff -U0 --output-indicator-old='<' --output-indicator-new='>' -- .planning/REQUIREMENTS.md | grep -E '^[<>]' | grep -c -E 'CAL-0[1235]|DPSGD-06')" = 0 ]
# The state backstop — all six now [x]:
.venv/bin/python -c "import pathlib,re;t=pathlib.Path('.planning/REQUIREMENTS.md').read_text();print({r:bool(re.search(r'^- \[x\] \*\*'+r+r'\*\*',t,re.M)) for r in ('CAL-01','CAL-02','CAL-03','CAL-05','DPSGD-06','CTRL-03')})"
.venv/bin/python -m pytest tests/ -q
make lint
```
</verification>

<success_criteria>
- The run was launched DETACHED using 23-20's measured recipe, **once per seed**, with the probe output quoted before any GPU second on every launch, and committed between launches so a kill costs at most one seed — proven by a commit count against a recorded baseline, not by a prose justification
- The N never-taught adapters from 23-08's single scheduling are scored at the pinned `CURVE_K`, with the projection recorded before the run and checked against the sizing block
- Counts are question-denominated, with the identity against `total_draws` asserted
- `results/phase23_never_taught.json` is accepted by the frozen `extraction_ceiling`'s real code path, and every one of its refusals is observed firing on degraded COPIES of that same record
- The extraction noise floor re-derives through `phase23_prereg.noise_floor` under exact `==`
- "Scheduled once, consumed twice" is a checked property across both records
- X is not published in Phase 23, proven by a structural key walk that a provenance string cannot false-RED
- **CTRL-03 IS TICKED** (Task 4), and all six of `ROADMAP.md:562`'s requirements are now `[x]` with their owning plan named — CAL-01/CAL-05 (23-12), CAL-02 (23-13), CAL-03 (23-04), DPSGD-06 (23-10), CTRL-03 (this plan)
- The only lines changed in `.planning/REQUIREMENTS.md` are CTRL-03's; no already-closed row is re-ticked
- Every frozen pin is byte-unchanged and the two one-commit pins are still one commit
</success_criteria>

<output>
Create `.planning/phases/23-cost-calibration-the-0-diagnostic-and-budget-pre-registratio/23-14-SUMMARY.md` when done.
</output>
