---
phase: 23-cost-calibration-the-0-diagnostic-and-budget-pre-registratio
plan: 04
type: execute
wave: 2
depends_on: ["23-03"]
files_modified:
  - tests/test_phase23_cal03.py
  - results/phase23_cal03_wiring.json
autonomous: true
requirements: [CAL-03]

must_haves:
  truths:
    - "D-05: the decision rule is BIT-IDENTICAL ε between n_facts=8 and n_facts=64 at fixed σ under `==`, AND the composed step count T asserted equal DIRECTLY. Never a relative tolerance."
    - "D-05: `epsilon_for(sigma, steps, delta)` takes NO N parameter — ε is N-independent BY CONSTRUCTION of the accountant, so this run cannot test the math. It tests the WIRING: whether N leaks into T. A tolerance would admit exactly the leak the check exists to catch."
    - "D-05: the T assertion adds NO detection power (ε is monotone in T at fixed σ, so ε equality already implies T equality). It exists to name WHERE a leak lives when one fires."
    - "T is read from `_count_composed_steps` — a per-instance shadow of `DPSGD.finalize` counting real invocations — never from `ckpt[\"step\"]`. A mutation made the checkpoint report 4 while 6 steps composed; a field read is OPTIMISTIC, not merely imprecise."
    - "A synthetic N-leak into T is WATCHED reddening both assertions. A guard nobody has seen fail is not evidence."
    - "D-06: falsified ⇒ the n=64 leg is NOT committed, withdrawn with the measurement that withdrew it recorded; the n=8 leg stays intact and publishable. The withdrawal rule is `phase23_prereg.n64_leg_is_committable`, committed blind in 23-03."
    - "This is a WIRING probe, not a sweep point: toy `ModelConfig`, `max_steps_override`, no adapter exported, no question scored. Its record declares `sweep_point: false` and sits outside `NOISED_RECORD_GLOB` by design."
  artifacts:
    - path: "tests/test_phase23_cal03.py"
      provides: "ε/T bit-identity across capacity, the watched N-leak positive control, and the structural check that no N-dependent value reaches the accountant"
      contains: "def test_epsilon_is_bit_identical_across_capacity"
    - path: "results/phase23_cal03_wiring.json"
      provides: "the CAL-03 verdict with full provenance — the record D-06's withdrawal (or non-withdrawal) is decided from"
      contains: "sweep_point"
  key_links:
    - from: "tests/test_phase23_cal03.py"
      to: "tests/test_phase22_checkpoint.py::_count_composed_steps"
      via: "imported and used to read T, never `ckpt[\"step\"]`"
      pattern: "_count_composed_steps"
    - from: "tests/test_phase23_cal03.py"
      to: "scripts/phase23_prereg.py::n64_leg_is_committable"
      via: "the blind-committed withdrawal rule decides the verdict; the test never re-implements the comparison"
      pattern: "n64_leg_is_committable"
---

<objective>
Confirm — by a run, not by inference — the premise the entire n=64 leg rests on: that ε does not
depend on N at q=1. Record the verdict in a committed artifact, and let 23-13 read it rather than
re-derive it.

Purpose: `.planning/REQUIREMENTS.md` marks *"ε is independent of N at q=1"* as `[INFERENCE]`, and the
whole n=64 capacity is built on it. Measured this session: `epsilon_for(sigma, steps, delta)` takes
**no N parameter**, so ε is N-independent by construction of the accountant and this run **cannot test
the math**. What it can test — and what actually breaks — is the **wiring**: whether N leaks into the
composed step count T. `scripts/teach_persona.py:1352` sets `grad_accum_steps = stats["n_facts"]` and
`training/loop.py:685-699` adds `ceil(4*n_facts/batch_size)` replay micro-batches per optimizer step,
so N is threaded through two independent paths and either could reach T.

Output: `tests/test_phase23_cal03.py` and `results/phase23_cal03_wiring.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-cost-calibration-the-0-diagnostic-and-budget-pre-registratio/23-CONTEXT.md
@.planning/phases/23-cost-calibration-the-0-diagnostic-and-budget-pre-registratio/23-RESEARCH.md
@.planning/phases/23-cost-calibration-the-0-diagnostic-and-budget-pre-registratio/23-VALIDATION.md
</context>

<environment>
**Python 3.11 venv is MANDATORY.** `.venv/bin/python -m pytest ...`.

**This plan writes the FIRST `results/phase23_*` artifact**, which makes two guards stop being
vacuous at the same commit: `tests/test_phase20_prereg.py:332` (accountant strictly ancestral) and
`tests/test_phase23_prereg.py::test_the_prereg_rule_precedes_every_phase23_result`. Both must be run
and seen GREEN in this plan, and their transition from vacuous to live must be recorded in the SUMMARY.

**FROZEN — no write:** `scripts/mitigation_accountant.py`, `scripts/mitigation_gate.py`.
</environment>

<tasks>

<task type="auto">
  <name>Task 1: ε and T, bit-identical across capacity at fixed σ</name>
  <files>tests/test_phase23_cal03.py</files>
  <read_first>
    - `tests/test_phase22_checkpoint.py:313-395` — `_TINY = ModelConfig(block_size=32, n_layer=1,
      n_head=2, n_embd=16)`, `_TOTAL_STEPS = 4`, `_NON_BINDING_CLIP = 1e6`, `_DELTA = 1e-5`,
      `_dp_train`, `_seam`, and **`_count_composed_steps` at `:361` with its docstring recording the
      mutation where `ckpt["step"]` said 4 while 6 composed**
    - `src/personacore/privacy/accountant.py:819` — `epsilon_for(sigma, steps, delta)`. **Three
      parameters, no N.** Confirm this live with `inspect.signature` inside the test rather than
      trusting the plan
    - `src/personacore/training/loop.py:235-270` — `train()`'s signature: `fact_bin`, `n_facts`,
      `replay_bin`, `replay_mask_bin`, `replay_windows`, `dp_fn`, `max_steps_override`, `fixed_batch`
    - `src/personacore/training/loop.py:685-699` — the `ceil(4 * n_facts / batch_size)` replay
      micro-batch computation, the second path N travels
    - `scripts/teach_persona.py:1340-1380` — `dp_accum = dict(grad_accum_steps=stats["n_facts"])` and
      the `dp_kwargs` block. **This is the production wiring the probe stands in for; read it so the
      probe's `TrainConfig(grad_accum_steps=N)` matches what production does**
    - `scripts/phase23_prereg.py` — `n64_leg_is_committable`, `CAL03_WIRING_RECORD`
  </read_first>
  <action>
Create `tests/test_phase23_cal03.py`. Module docstring must state the three facts that shape the
instrument: `epsilon_for` takes no N (so the math cannot be tested), N reaches the loop through TWO
paths (`grad_accum_steps` and the replay micro-batch count), and T is read from the mechanism rather
than from a field.

**The probe shape.** A helper `_run_capacity(n_facts, *, sigma, device)` that:
  - builds a `_TINY`-shaped model with LoRA injected (import the existing `_tiny_lora_model` from
    `test_phase22_checkpoint` rather than writing a second one),
  - builds a `DPSGD` seam at the given `sigma` and `clip_norm=_NON_BINDING_CLIP`,
  - installs `calls = _count_composed_steps(dp)`,
  - calls `train(...)` with `grad_accum_steps=n_facts`, `n_facts=n_facts`, `dp_fn=dp`,
    `fixed_batch=<a micro-batch of size batch_size>`, `max_steps_override=<a small step count>`,
  - returns `(len(calls), <the checkpoint dict>)`.

**`max_steps_override` is REQUIRED and its use must be justified in the docstring**, not glossed: at
the production shape one n=8 arm costs ≈ 3.8 min and one n=64 arm ≈ 30.0 min (measured on MPS), so a
full-length pair costs 34 minutes to test a property that is fully expressed in 4 optimizer steps.
`test_resume_epsilon_bit_identical` already uses exactly this shape at `_TOTAL_STEPS = 4`. State
plainly that the probe runs at reduced steps and on a toy `ModelConfig`, so no reader mistakes it for
a full-fidelity arm.

**Tests:**

  - `test_epsilon_for_takes_no_n_parameter` — assert
    `list(inspect.signature(epsilon_for).parameters) == ["sigma", "steps", "delta"]`. This is the
    measured premise the whole instrument rests on and it is asserted, not assumed.
  - `test_composed_step_count_is_equal_across_capacity` — run both capacities at fixed σ, assert
    `t_n8 == t_n64` **directly**, and assert both equal the requested step count. The failure message
    must say that an inequality names the leak's location: `grad_accum_steps` and the replay
    micro-batch count are the two paths N travels.
  - `test_epsilon_is_bit_identical_across_capacity` — `epsilon_for(sigma, t_n8, _DELTA) ==
    epsilon_for(sigma, t_n64, _DELTA)` under exact `==`. The docstring must carry D-05's reason
    verbatim in substance: the two arms are the SAME call shape at fixed σ, not two independent
    mathematics; a tolerance would admit exactly the leak the check exists to catch; Phase 22
    rejected this reasoning once already in DPSGD-05.
  - `test_the_verdict_uses_the_blind_committed_rule` — assert the test file computes its verdict by
    calling `phase23_prereg.n64_leg_is_committable(...)` and never by an inline comparison. Implement
    the check with an `ast.parse` over this file asserting no `==` comparison between the two epsilon
    names outside the rule call, or more simply by having only ONE code path produce the verdict and
    asserting `n64_leg_is_committable` is in the module's call graph.

Run at a fixed σ that is comfortably inside the accountant's well-conditioned range (σ ≥ 0.42 is the
regime the Phase-22 band work characterized); record the chosen σ as a module constant with a comment
naming why it was chosen, and assert `math.isfinite(epsilon)` for both arms so a degenerate `+inf`
pair cannot pass as "bit-identical".
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase23_cal03.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_phase23_cal03.py -v` exits 0 with zero skips
    - `grep -c "rel_tol\|isclose\|approx\|pytest.approx" tests/test_phase23_cal03.py` returns **0** — no tolerance anywhere
    - `grep -n "_count_composed_steps" tests/test_phase23_cal03.py` matches and `grep -c 'ckpt\["step"\]' tests/test_phase23_cal03.py` returns 0
    - `grep -n "max_steps_override" tests/test_phase23_cal03.py` matches, and the module docstring states the reduced-step scope in words
    - `grep -n "isfinite" tests/test_phase23_cal03.py` matches — a `+inf == +inf` pair cannot pass
    - `.venv/bin/python -m pytest tests/ -q` reports 0 failed
  </acceptance_criteria>
  <done>T is counted from the mechanism, ε is compared under exact `==` at fixed σ, both are finite, and the verdict comes from the blind-committed rule.</done>
</task>

<task type="auto">
  <name>Task 2: The watched N-leak positive control</name>
  <files>tests/test_phase23_cal03.py</files>
  <read_first>
    - `tests/test_phase23_cal03.py` — current state after Task 1
    - `tests/test_phase22_fakes.py:81-108` — `_mutate(source, target, replacement)` and
      `_run_live_guard(monkeypatch, tmp_path, source, guard, **kwargs)`: the register this repository
      uses to apply a mutation to a REAL committed module, observe the RED in-process, and restore
    - `tests/test_phase22_fakes.py:690-760` — `_WATCHED_RED_NODE_IDS` and
      `test_every_fake_has_at_least_two_independent_detectors`. The ledger discipline: two independent
      detectors per fake, and a shared signature is a COVERAGE GAP to name, not a second win to count
  </read_first>
  <action>
Add `test_an_n_leak_into_t_is_detected`. This is the positive control D-05's whole claim depends on:
without it, a green ε/T comparison is indistinguishable from a comparison that could never fail.

Implement the leak as a **synthetic seam wrapper**, not a source mutation — the leak being modelled
is in the caller's wiring, not in `dpsgd.py`. Define a local `_LeakySeam` that subclasses or wraps
`DPSGD` and composes an extra `finalize` for every `k` records (so T becomes a function of N), then:

  - run both capacities through `_run_capacity` with the leaky seam,
  - assert `t_n8 != t_n64` — **the T assertion reddens**,
  - assert `epsilon_for(sigma, t_n8, _DELTA) != epsilon_for(sigma, t_n64, _DELTA)` — **the ε
    assertion reddens too**,
  - assert `phase23_prereg.n64_leg_is_committable(...)` returns `False` on the leaky pair.

**Both assertions must be observed reddening, separately.** The ledger discipline in
`tests/test_phase22_fakes.py` is that a fake with one detector is one rename away from vacuous; the
same standard applies here, and the two detectors are genuinely independent (T is a count of
mechanism invocations, ε is a function evaluated on that count).

Add a second, weaker-leak case: a leak that changes T by exactly **one** step. Assert it is still
caught. A leak large enough to be obvious proves less than the smallest one that matters, and ε's
monotonicity in T at fixed σ means a one-step difference is detectable under `==` while it would
vanish under any plausible `rel_tol` — assert that too, by computing the relative difference between
the two epsilons and recording it in the failure message so the SUMMARY can quote the number a
tolerance would have swallowed.

Register the observed node ids for both cases in a module-level `_WATCHED_RED` mapping following
`tests/test_phase22_fakes.py:704`'s shape, and add a test asserting every registered node id
resolves (the stale-anchor defect class this repository has measured seven times).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase23_cal03.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest "tests/test_phase23_cal03.py::test_an_n_leak_into_t_is_detected" -v` exits 0 and collects **both** the gross-leak and one-step-leak cases
    - The SUMMARY quotes the measured relative ε difference for the one-step leak as a literal number, with the sentence naming what a `rel_tol` of that magnitude would have swallowed
    - `grep -n "_WATCHED_RED" tests/test_phase23_cal03.py` matches, and a test asserting every registered node id resolves is present and green
    - `.venv/bin/python -m pytest tests/ -q` reports 0 failed
  </acceptance_criteria>
  <done>Both detectors are observed reddening under a synthetic N-leak, at a gross leak and at a one-step leak, with the relative ε difference recorded as a number.</done>
</task>

<task type="auto">
  <name>Task 3: Emit and commit `results/phase23_cal03_wiring.json`</name>
  <files>results/phase23_cal03_wiring.json, tests/test_phase23_cal03.py</files>
  <read_first>
    - `scripts/phase23_prereg.py` — `CAL03_WIRING_RECORD`, `n64_leg_is_committable`. **Resolve the
      output path from this constant; never retype the string**
    - `results/phase19_noise_floors.json` — the committed record shape to follow: `git_sha`,
      `record`, `record_sha256`, `recipe`, `estimator`, `reduction`, `governs`, per-seed readings
    - `src/personacore/provenance.py` — `git_sha()`
    - `tests/test_phase20_prereg.py:332-378` — the guard that becomes LIVE at this commit
    - `tests/test_phase23_prereg.py` — the three ordering guards that become live at this commit
  </read_first>
  <action>
Emit `results/phase23_cal03_wiring.json` and commit it. The record must carry, at minimum:

  - `sweep_point: false` and `exports_adapter: false` — **the structural declaration that this record
    is a wiring probe, not a noised sweep point**, which is why it sits outside
    `NOISED_RECORD_GLOB` and does not violate DPSGD-06's "σ=0 is the DP arm's first executed run".
    The record must also carry a `scope` string saying so in words.
  - `verdict`: the return of `phase23_prereg.n64_leg_is_committable(...)`, plus `epsilon_n8`,
    `epsilon_n64`, `t_n8`, `t_n64`, `sigma`, `delta` as raw values.
  - Provenance, every key required and **REFUSED if missing rather than defaulted**: `git_sha`,
    `device`, `torch_version`, `python_version`, `model_config` (the toy shape), `max_steps_override`,
    `batch_size`, `clip_norm`, `n_facts_arms`, and `t_source: "_count_composed_steps"`.
  - `governs`: a sentence naming what this record decides — whether the n=64 leg is committed in
    `scripts/mitigation_budget.py` (D-06) — and naming `23-13` as the consumer.

Write the record with a small emitter inside the test module (a `--emit` `__main__` block or a
dedicated `test_emit_cal03_record` gated on an env var is acceptable; a plain module-level
`if __name__ == "__main__":` emitter is preferred, matching `scripts/`' register) so the numbers in
the artifact come from the same code path the assertions run on. **Do not hand-write the JSON.**

Add `test_the_cal03_record_is_complete_and_declares_its_scope`: load the committed record, assert
every provenance key is present, assert `sweep_point is False`, assert the verdict field equals
`n64_leg_is_committable` re-evaluated on the record's own stored values, and assert the file path
equals `phase23_prereg.CAL03_WIRING_RECORD`.

**Then run and record the two guards that stop being vacuous at this commit:**
`.venv/bin/python -m pytest tests/test_phase20_prereg.py -k phase23_result -q` and
`.venv/bin/python -m pytest tests/test_phase23_prereg.py -q`. Quote both results in the SUMMARY,
together with `git ls-files 'results/phase23_*'` before and after, so the vacuous→live transition is
a recorded observation.

**If the verdict is `False`** (CAL-03 falsified): record it as-written, do not retry, do not adjust
σ to find a passing pair. Note in the SUMMARY that D-06 then applies — the n=64 leg is withdrawn in
23-13 with this record as the withdrawing measurement, and the n=8 leg stays intact and publishable.
That branch is a legitimate pre-registered outcome, exactly as Phase 12 recorded `lambda*=None`.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase23_cal03.py tests/test_phase23_prereg.py tests/test_phase20_prereg.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `results/phase23_cal03_wiring.json` exists, is tracked by git, and `.venv/bin/python -c "import json,pathlib;d=json.load(open('results/phase23_cal03_wiring.json'));print(sorted(d))"` shows every required provenance key
    - `.venv/bin/python -m pytest "tests/test_phase23_cal03.py::test_the_cal03_record_is_complete_and_declares_its_scope" -v` exits 0
    - `.venv/bin/python -m pytest tests/test_phase20_prereg.py -k phase23_result -q` exits 0, and the SUMMARY quotes `git ls-files 'results/phase23_*'` before (empty) and after (one file)
    - `.venv/bin/python -m pytest tests/test_phase23_prereg.py -q` exits 0 — the three ordering guards survive the first artifact
    - `git log --diff-filter=A --format=%H -- results/phase23_cal03_wiring.json` returns a commit strictly after the `scripts/phase23_prereg.py` and `scripts/mitigation_accountant.py` commits (verify with `git merge-base --is-ancestor`)
    - `git diff --exit-code -- scripts/mitigation_accountant.py scripts/mitigation_gate.py` exits 0
    - `.venv/bin/python -m pytest tests/ -q` reports 0 failed; `make lint` exits 0
  </acceptance_criteria>
  <done>The CAL-03 verdict is a committed artifact produced by the same code path the assertions run on, its scope is declared structurally, and the two guards it makes non-vacuous are observed green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N (capacity) → T (composed steps) | The only place the "ε is independent of N" premise can break. N travels two paths: `grad_accum_steps` and the replay micro-batch count. |
| a wiring probe → a sweep point | A probe record read as an executed noised point would falsify DPSGD-06's "first executed run" claim without anyone editing a number. |
| research `[INFERENCE]` → published ε label | The n=64 leg's entire ε is currently justified by an inference. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-17 | Tampering | a relative tolerance admitting exactly the leak the check exists to catch | mitigate | `grep -c "rel_tol\|isclose\|approx"` asserted 0; the one-step-leak case records the relative difference a tolerance would swallow |
| T-23-18 | Repudiation | a green comparison that could never fail | mitigate | The N-leak positive control reddens BOTH detectors, at a gross leak and at a one-step leak |
| T-23-19 | Spoofing | T read from `ckpt["step"]` reporting a composition that never ran | mitigate | T comes from `_count_composed_steps`, the per-instance `finalize` shadow; `grep -c 'ckpt\["step"\]'` asserted 0 |
| T-23-20 | Repudiation | a degenerate `+inf == +inf` pair passing as bit-identical | mitigate | `math.isfinite` asserted on both epsilons |
| T-23-21 | Spoofing | the wiring record counted as a noised sweep point | mitigate | `sweep_point: false` / `exports_adapter: false` in the record, the path outside `NOISED_RECORD_GLOB`, and the scope stated in the record's own text |
| T-23-22 | Tampering | σ retried until a passing pair is found | mitigate | σ is a module constant chosen with a stated reason before the run; a `False` verdict is recorded as-written per D-06 |
| T-23-SC | Tampering | npm/pip/cargo installs | accept | **Zero installs.** `pyproject.toml` byte-unchanged (RPT-03) |
</threat_model>

<verification>
```bash
.venv/bin/python -m pytest tests/test_phase23_cal03.py -v
.venv/bin/python -m pytest tests/test_phase23_prereg.py -q
.venv/bin/python -m pytest tests/test_phase20_prereg.py -k "phase23_result or import_graph" -q
git ls-files 'results/phase23_*'
git diff --exit-code -- scripts/mitigation_accountant.py scripts/mitigation_gate.py pyproject.toml
.venv/bin/python -m pytest tests/ -q
make lint
```
</verification>

<success_criteria>
- ε and T are compared under exact `==` at fixed σ, both finite, T read from the mechanism
- A synthetic N-leak reddens both detectors, including a one-step leak, with the relative ε difference recorded
- The verdict comes from `phase23_prereg.n64_leg_is_committable`, committed blind in 23-03
- `results/phase23_cal03_wiring.json` is committed, complete, and declares itself a non-sweep-point wiring probe
- The two guards that become non-vacuous at this commit are observed green, with the before/after `git ls-files` recorded
</success_criteria>

<output>
Create `.planning/phases/23-cost-calibration-the-0-diagnostic-and-budget-pre-registratio/23-04-SUMMARY.md` when done.
</output>
