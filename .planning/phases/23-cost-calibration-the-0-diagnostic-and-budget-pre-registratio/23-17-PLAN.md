---
phase: 23-cost-calibration-the-0-diagnostic-and-budget-pre-registratio
plan: 17
type: execute
wave: 3
depends_on: ["23-16"]
gap_closure: true
files_modified:
  - scripts/phase23_run.py
  - tests/test_phase23_matched.py
  - results/phase23_matched_control.json
  - results/phase23_matched_control_seed1337/run.csv
  - results/phase23_matched_control_seed2024/run.csv
  - results/phase23_matched_control_seed1338/run.csv
  - results/phase23_matched_control_seed2025/run.csv
  - results/phase23_matched_control_seed1339/run.csv
autonomous: true
requirements: [DPSGD-06]

must_haves:
  truths:
    - "The protocol-matched comparator is TRAINED AND SCORED at the same five seeds the old control used — `SEED_LADDER` = (1337, 2024, 1338, 2025, 1339), the prefix `phase23_prereg.choose_n_seeds` already selected at N=5 on a measured 996.27 s scoring cost. The seed set is INHERITED, not re-chosen: the scoring instrument is byte-identical (`score_adapter` is the same function), so re-measuring its cost would move nothing and would spend a seed to learn it."
    - "The floor is RE-REDUCED over the NEW comparator's readings by the SAME blind function — `phase23_prereg.noise_floor`, CALLED and never re-implemented. No `max`, no `min`, no spread expression appears in the writer, and an AST test asserts that."
    - "`scripts/phase23_prereg.py` is not edited: the new readings are a new INPUT. `git diff --stat c7de5d4 HEAD -- scripts/phase23_prereg.py` stays empty."
    - "`grad_clip` is proven non-binding on EVERY seed BEFORE that seed is scored — `MATCHED_GRAD_CLIP = 1e6` against pre-clip norms the run captures itself, with the call count asserted equal to `MAX_STEPS` so a branch that was never taken cannot pass as a non-binding one."
    - "The record carries the declared difference ledger from `phase23_matched_prereg.MATCHED_DIFFERENCES` and the seven-branch `loop.py` census, so what remains different is disclosed BY CONSTRUCTION rather than enumerated by hand — the exact failure mode 23-08 hit when it listed four differences and missed `grad_clip`."
    - "This plan runs NO σ=0 arm, renders NO verdict, and writes nothing under `results/phase23_noised_*` or over `results/phase23_sigma_zero.json`. The σ=0 arm's protocol did not change; only its comparator did."
  artifacts:
    - path: "results/phase23_matched_control.json"
      provides: "five per-seed protocol-matched readings with their k/n denominators, the re-reduced floor, all eight FLOOR_PROVENANCE_KEYS, the difference ledger, the branch census and the per-seed grad-clip evidence"
      contains: "grad_clip_checked_before_scoring"
    - path: "scripts/phase23_run.py"
      provides: "the `matched` sub-mode: preflight -> train -> score -> reduce -> write"
      contains: "def matched"
  key_links:
    - from: "scripts/phase23_run.py::matched"
      to: "scripts/phase23_prereg.py::noise_floor"
      via: "the reduction is CALLED; the record names the SYMBOL, never the formula"
      pattern: "noise_floor"
    - from: "scripts/phase23_run.py::matched"
      to: "scripts/phase23_run.py::score_adapter"
      via: "ONE scoring function, now three callers — a reading produced by a second scoring path would not be comparable to the sigma-zero arm's"
      pattern: "score_adapter"
---

<objective>
Run the protocol-matched comparator at five seeds and re-reduce the noise floor over its readings.

**What is being replaced and why.** `scripts/mitigation_budget.py`'s `CONTROL_NOISE_FLOOR =
0.05357142857142849` was reduced over the OLD control protocol — 8 random windows per step drawn from
a bin that is 51.94% replay, a teaching loss weight of `p = 2719/6262 = 0.4342`, and `grad_clip = 1.0`
binding on 19 of 25 steps at mean shrink `0.8071`. That is not the protocol the σ=0 arm ran, so the
floor describes the wrong comparator. The corrected comparator equalises all three mechanisms
together; this plan measures what it actually reads.

**Wall clock, sized from committed measurements rather than estimated.** Per seed the training leg is
bounded ABOVE by the σ=0 arm's own measured `seconds_total = 205.44225783273578`
(`results/phase23_sigma_zero.json` → `training`), because the comparator draws the identical
65-window lot in the identical 12 micro-batches while dropping `absorb_record`'s per-record clip +
sum + drain over 72 LoRA tensors (1,600 calls per run) and `finalize`; it adds 200 `clip_grad_norm_`
calls and 1,600 `/accum` divides, both negligible against that. The scoring leg is the identical
instrument and measured `983.21`-`1026.87` s across the five old control seeds
(`results/phase23_control_floor.json` → `scoring_seconds_per_seed`), plus `952.07` s on the σ=0 arm.
**So per seed ≤ 205.44 + 1026.87 = 1,232.31 s, and five seeds ≤ 6,161.6 s ≈ 103 minutes** on MPS.
For scale: 23-08's scheduling took ~2h50m and 23-10's σ=0 leg ~95 min end to end.

Purpose: produce a floor that describes the comparator the σ=0 arm will actually be judged against.
Output: `results/phase23_matched_control.json` plus five run CSVs, committed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-cost-calibration-the-0-diagnostic-and-budget-pre-registratio/23-16-SUMMARY.md
@.planning/debug/sigma-zero-beats-control.md
@CLAUDE.md

<interfaces>
<!-- Resolved from source. Derive, do not retype. -->

scripts/phase23_run.py — reused UNCHANGED:
  SEED_LADDER = (1337, 2024, 1338, 2025, 1339)
  _preconditions()          # the five files every leg needs, checked before any run
  _state_load / _state_record(section, key, block) / _already_trained(section, seed)
  score_adapter(arm, adapter, *, seed) -> dict with primary/heldout_on/taught_off/heldout_off,
                                          each carrying k, n, rate, questions, draws_per_question
  provenance() -> {git_sha, device, torch_version, python_version, timestamp}
  device() ; _rel(path) ; _sha256(path) ; _prove(cond, msg)
  DP_N8_BIN_SHA256 ; prove_bins_match(expected_sha256)
  residual_differences()    # THE OLD LEDGER. Leave it standing; the matched record carries
                            # phase23_matched_prereg.MATCHED_DIFFERENCES instead, and CITES the old
                            # one by name so a reader sees both.
  _prove_no_noised_record_exists()   # the subprocess `git ls-files` shape to copy
  _TABLE = {"cost","schedule","floor","sigma-zero"} ; USAGE

Added by 23-16 and consumed here:
  matched_control_call(seed) -> (train_config_fields, train_kwargs)
  prove_matched_protocol() -> collections.Counter          # the two AST gates
  captured_grad_clip() ctx -> {"norms": [...]}
  train_matched_control(seed) -> dict                      # returns the grad_clip_* evidence fields

scripts/phase23_prereg.py (EDIT-ONCE, byte-identical to c7de5d4 — DO NOT TOUCH):
  noise_floor(readings) -> max - min
  FLOOR_PROVENANCE_KEYS = ("record","record_sha256","git_sha","device","torch_version","seeds",
                           "reduction","governs")

scripts/phase23_matched_prereg.py (23-15):
  MATCHED_CONTROL_RECORD = "results/phase23_matched_control.json"
  MATCHED_ARTIFACT_GLOB  = "results/phase23_matched_*"
  MATCHED_ARM_PREFIX ; matched_arm(seed) ; MATCHED_GRAD_CLIP = 1e6
  MATCHED_DIFFERENCES ; prove_first_attempt(tracked) ; dp_fn_branch_census(source)

THE `record_sha256` SHAPE IS AN INPUTS DIGEST, matching phase23_run.py:1291-1293 exactly:
  hashlib.sha256(json.dumps(per_seed, sort_keys=True, default=str).encode("utf-8")).hexdigest()

MEASURED BUDGET INPUTS (all re-derivable from committed records):
  results/phase23_sigma_zero.json   training.seconds_total              205.44225783273578
                                    training.seconds_per_optimizer_step   1.0272112891636789
                                    scoring_seconds                     952.0680559994653
                                    reading                             0.7837301587301587
  results/phase23_control_floor.json scoring_seconds_per_seed           983.21 .. 1026.87 (5 seeds)
                                    per_seed[].training_seconds         78.4 .. 80.3
                                    seed_count_rule.measured_seconds_per_seed  996.2667346671224
                                    seed_count_rule.fits_the_bound      true  (5 x 996.27 <= 17175)
                                    floor                               0.05357142857142849
                                    central_reading                     0.5615079365079365
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The `matched` sub-mode, its record writer and the record's guards</name>
  <files>scripts/phase23_run.py, tests/test_phase23_matched.py</files>
  <read_first>
    - scripts/phase23_run.py lines 1255-1394 (`floor()`) — the record-writing register this task
      copies: the reduction CALLED not inlined, the `FLOOR_PROVENANCE_KEYS` completeness `_prove`,
      the re-derivation `_prove` before the write, the refuse-if-exists on the record path, and the
      per-seed print block.
    - scripts/phase23_run.py lines 1076-1255 (`schedule()`) — the multi-seed loop register:
      `_already_trained` reuse, `_state_record` per seed, and how a partially-completed scheduling
      resumes without re-measuring.
    - scripts/phase23_run.py lines 815-840 (`_prove_no_noised_record_exists`) — the
      `subprocess.run(["git","ls-files", GLOB], cwd=_ROOT, capture_output=True, text=True,
      check=True).stdout.split()` shape to copy for the one-attempt gate.
    - scripts/phase23_run.py lines 1638-1676 — `_TABLE` and `USAGE`, both of which this task extends.
    - scripts/phase23_matched_prereg.py — `MATCHED_CONTROL_RECORD`, `MATCHED_ARTIFACT_GLOB`,
      `MATCHED_DIFFERENCES`, `prove_first_attempt`, `dp_fn_branch_census`.
  </read_first>
  <action>
Edit `scripts/phase23_run.py`.

(a) Add `def matched()` — the sub-mode. In order:

  1. `_preconditions()`.
  2. THE ONE-ATTEMPT GATE. Copy `_prove_no_noised_record_exists`'s subprocess shape verbatim against
     `mp.MATCHED_ARTIFACT_GLOB`, pass the resulting list to `mp.prove_first_attempt(...)`, and print
     the empty result exactly as that function prints its own.
  3. `census = prove_matched_protocol()` — the two AST gates, before any GPU second is spent.
  4. `path = _ROOT / mp.MATCHED_CONTROL_RECORD`; `_prove(not path.exists(), "... it is recorded
     evidence and there is no force flag")`.
  5. `seeds = list(SEED_LADDER)`. `_prove` a `cost` block exists in `_state_load()` (naming
     `python scripts/phase23_run.py cost` if absent) and that
     `seeds == [int(s) for s in state["cost"]["seeds"]]` — the seed set is INHERITED from the same
     `choose_n_seeds` application the old control used, and a drift between the two would compare two
     different denominators.
  6. Per seed, in `seeds` order:
       - `if not _already_trained("matched", seed): _state_record("matched", seed,
         train_matched_control(seed))`;
       - re-read the block, then **assert the grad-clip evidence BEFORE scoring that seed**:
         `_prove(block["grad_clip_calls"] == tp.MAX_STEPS and block["grad_clip_bound_count"] == 0
         and block["grad_clip_max_pre_clip_norm"] < mp.MATCHED_GRAD_CLIP, ...)`. The message states
         that a seed whose clip BOUND differs from the σ=0 arm by CLIPPING rather than by protocol,
         that a call count below `MAX_STEPS` means the branch was never taken so the equalisation was
         never applied, and that the check runs before scoring for the same reason
         `train_sigma_zero`'s `clip_bind_count == 0` does;
       - `if "primary" not in block:` score it with
         `score_adapter(mp.matched_arm(seed), _ROOT / block["adapter"], seed=seed)` and merge via
         `_state_record`.
  7. `per_seed = [state["matched"][str(seed)] for seed in seeds]`;
     `readings = [entry["primary"]["rate"] for entry in per_seed]`;
     `measured_floor = noise_floor(readings)` — CALLED, never inlined. No `max`, `min` or spread
     expression may appear anywhere in this function; a test asserts that by AST.
  8. Build the record. Required fields, at minimum:
       `record` (= `mp.MATCHED_CONTROL_RECORD`), `record_sha256` (the INPUTS digest expression
       above), `floor`, `reduction` = `"phase23_prereg.noise_floor"`, `estimator`, `governs`,
       `primary_reading`, `seeds`, `n_seeds`, `central_reading`, `central_reading_seed`, `readings`,
       `per_seed`, `questions_taught`, `questions_heldout`, `draws_per_question`,
       `scoring_seconds_per_seed`, `training_seconds_per_seed`, `**provenance()`.
     Plus a `protocol` block: `arm_prefix`, `n_facts`, `grad_accum_steps`, `grad_clip`,
       `grad_clip_symbol` = `"phase23_matched_prereg.MATCHED_GRAD_CLIP"`, `replay_windows_per_step`,
       `replay_micro_batches_per_step`, `max_steps`, `batch_size`, `block_size`,
       `dp_seam_active: False`, `corpus_sha256` (the three dp_n8 digests via `_rel`),
       `corpus_is_the_sigma_zero_arms_own_bins: True`, `budget_constants` (the same six `tp.*`
       symbols `floor()` records), `lora_config` = `asdict(tp.LORA_CFG)`, `base_checkpoint` =
       `_rel(tp.CONVBASE_BEST)`.
     Plus the disclosure blocks:
       `equalised_mechanisms` (the three, with their measured magnitudes),
       `declared_differences` = `mp.MATCHED_DIFFERENCES`,
       `superseded_ledger` = `"phase23_run.residual_differences (23-08) — four entries, and it did
       NOT enumerate grad_clip"`,
       `dp_fn_branch_census` — the Counter from step 3 rendered JSON-safe as a sorted list of
       `{"function", "condition", "count"}`,
       `grad_clip_evidence` — per seed: `calls`, `max_pre_clip_norm`, `min_pre_clip_norm`,
       `bound_count`, `checked_before_scoring`.
     **`governs` says the same thing the old record's does, restated for this comparator:** the
     TAUGHT RECALL RATE WITH THE ADAPTER ON (`per_seed[].primary.k / .n`, a count over QUESTIONS) and
     NOTHING ELSE; every other reading in the record is secondary, carries its own denominator and
     was NOT reduced.
  9. `_prove` no key in `FLOOR_PROVENANCE_KEYS` is missing (copy `floor()`'s message verbatim in
     shape), and `_prove` the recorded floor re-derives from the recorded per-seed counts under exact
     `==`.
  10. Write with `json.dumps(record, indent=2, sort_keys=True)`, then print the per-seed
      `k/n = rate` block and the floor line, `floor()`'s shape.

(b) Register `"matched": matched` in `_TABLE` and add its USAGE paragraph, stating that it trains and
    scores the protocol-matched comparator at all five seeds, writes
    `results/phase23_matched_control.json`, RUNS NO σ=0 arm, and renders NO verdict.

(c) Extend `tests/test_phase23_matched.py` with the record's structural guards, written NOW so they
    are live the moment the run lands. Each uses the vacuity shape `floor()`'s guards use — when the
    record is absent the test asserts the sub-mode is registered and returns, naming that state in
    its message. **No `pytest.skip` anywhere in the file**: a silently-skipped guard is the
    "declared invariant silently becomes false" defect this project keeps naming.
      - `test_matched_record_floor_re_derives` — `phase23_prereg.noise_floor` over the record's own
        per-seed `k/n` equals the recorded `floor` under exact `==`, and each recorded `rate` equals
        its own `k/n`.
      - `test_matched_record_carries_every_floor_provenance_key` — all eight.
      - `test_matched_record_grad_clip_was_non_binding_on_every_seed` — every seed's `bound_count`
        is 0, `calls == tp.MAX_STEPS`, and `max_pre_clip_norm < MATCHED_GRAD_CLIP`.
      - `test_matched_record_declares_the_branch_census` — the recorded census sums to 7 and equals a
        LIVE `dp_fn_branch_census` of `src/personacore/training/loop.py`.
      - `test_the_matched_writer_does_not_inline_the_reduction` — AST-scan the `matched` function's
        body in `scripts/phase23_run.py` and assert it contains no `Call` to `max` or `min` and no
        `BinOp(Sub)` over such calls, with a non-empty meta-guard so a walk that stopped working
        cannot pass by finding nothing.
      - `test_matched_record_names_the_superseded_ledger` — `superseded_ledger` mentions both
        `residual_differences` and `grad_clip`.
  </action>
  <verify>
    <automated>.venv/bin/python -c "
import sys; sys.path.insert(0,'scripts')
import phase23_run as r
assert 'matched' in r._TABLE and 'matched' in r.USAGE
r.prove_matched_protocol()
print('OK')
" && .venv/bin/python -m pytest tests/test_phase23_matched.py -q && .venv/bin/ruff check . && .venv/bin/ruff format --check .</automated>
  </verify>
  <acceptance_criteria>
    - `"matched" in phase23_run._TABLE` and the string `matched` appears in `phase23_run.USAGE`.
    - `.venv/bin/python scripts/phase23_run.py --help` lists `matched` in the mode list.
    - `tests/test_phase23_matched.py` passes and `grep -c "pytest.skip" tests/test_phase23_matched.py`
      returns 0.
    - `.venv/bin/ruff check .` and `.venv/bin/ruff format --check .` exit 0.
    - `git diff --exit-code -- scripts/phase23_prereg.py scripts/phase23_matched_prereg.py
      scripts/mitigation_gate.py scripts/mitigation_accountant.py scripts/mitigation_budget.py
      scripts/teach_persona.py src/personacore/training/loop.py` exits 0.
    - `git ls-files 'results/phase23_matched_*'` still prints NOTHING at the end of this task.
  </acceptance_criteria>
  <done>The `matched` sub-mode and its six record guards exist and pass; nothing has been run yet and
  the matched glob is still empty.</done>
</task>

<task type="auto">
  <name>Task 2: Run the comparator on MPS and commit the record</name>
  <files>results/phase23_matched_control.json, results/phase23_matched_control_seed1337/run.csv, results/phase23_matched_control_seed2024/run.csv, results/phase23_matched_control_seed1338/run.csv, results/phase23_matched_control_seed2025/run.csv, results/phase23_matched_control_seed1339/run.csv</files>
  <read_first>
    - scripts/phase23_run.py — the `matched` sub-mode as written in Task 1.
    - results/phase23_control_floor.json — the old per-seed block shape, so the new record's
      `per_seed` entries are directly comparable field for field.
    - CLAUDE.md, "Local laptop / Apple Silicon (PRIMARY)" — `.venv/bin/python` is MANDATORY; the
      system `python3` is 3.14 and is not a supported target.
  </read_first>
  <action>
Run, on MPS, from the repo root, with the Python 3.11 venv:

    .venv/bin/python scripts/phase23_run.py matched

**Expected wall clock ≤ 103 minutes** (5 x [≤205.44 s train + ≤1026.87 s score]), derived in the
objective from the two committed records. If the run exceeds ~2 h, do NOT kill it silently — record
the overrun with its per-seed breakdown in the SUMMARY. `data/phase23_run_state.json` makes a killed
run resumable per seed: `_already_trained` re-uses a completed adapter only after re-hashing it
against the recorded digest, so a resume proves reuse rather than assuming it.

DO NOT, under any circumstance:
  * re-run a seed to get a different number, widen a band, add a seed, or change `MATCHED_GRAD_CLIP`
    after seeing a reading;
  * touch `results/phase23_sigma_zero.json`, `results/phase23_control_floor.json` or
    `scripts/mitigation_budget.py` — the floor re-pin is 23-18, deliberately a separate commit so the
    pin lands as its own reviewable act, exactly as 23-09 preceded 23-10;
  * edit `scripts/phase23_prereg.py`, `scripts/phase23_matched_prereg.py`,
    `scripts/mitigation_gate.py` or `scripts/mitigation_accountant.py`.

After the run: re-run the full suite, run ruff, and commit the record plus the five run CSVs in ONE
commit whose message names the measured floor and the five readings.

Record in the SUMMARY, as the deliverable's evidence:
  * the five `k/n = rate` primary readings in seed order, each with its `n` stated;
  * the reduced floor, and its exact rational form if one exists;
  * the OLD floor `0.05357142857142849` beside it with the ratio, and one line on what moved and what
    did not;
  * the σ=0 reading `0.7837301587301587` and its deviation from the NEW central reading —
    **REPORTED ONLY. This plan renders no verdict.** `sigma_zero_verdict` is called in 23-19 and the
    decision is the rule's and only the rule's;
  * per-seed training seconds and scoring seconds against the ≤205.44 / ≤1026.87 bounds budgeted
    here, with any overrun named rather than absorbed;
  * per-seed `grad_clip` max pre-clip norm and `bound_count`, and the comparison against the DP arm's
    measured 1.538-2.278 range;
  * whether the held-out (never-taught) reading moved in the same direction as the taught one — the
    debug record's tell. This is DISCLOSURE, not a verdict.
  </action>
  <verify>
    <automated>.venv/bin/python -c "
import json, sys, pathlib
sys.path.insert(0,'scripts')
import phase23_prereg as p, phase23_matched_prereg as mp
r = json.loads(pathlib.Path(mp.MATCHED_CONTROL_RECORD).read_text())
assert r['n_seeds'] == 5 and len(r['per_seed']) == 5, r['n_seeds']
rates = [e['primary']['k'] / e['primary']['n'] for e in r['per_seed']]
assert r['floor'] == p.noise_floor(rates), (r['floor'], p.noise_floor(rates))
assert r['readings'] == rates and r['central_reading'] == rates[0]
missing = [k for k in p.FLOOR_PROVENANCE_KEYS if k not in r]
assert not missing, missing
g = r['grad_clip_evidence']
assert all(v['bound_count'] == 0 and v['calls'] == 200 for v in g.values()), g
assert all(v['max_pre_clip_norm'] < mp.MATCHED_GRAD_CLIP for v in g.values()), g
print('floor', r['floor'], 'central', r['central_reading'], 'readings', rates)
" && .venv/bin/python -m pytest -q 2>&1 | tail -3 && .venv/bin/ruff check . && git diff --stat c7de5d4 HEAD -- scripts/phase23_prereg.py</automated>
  </verify>
  <acceptance_criteria>
    - `results/phase23_matched_control.json` exists with `n_seeds == 5` and five `per_seed` entries,
      each carrying `primary.k`, `primary.n`, `primary.rate`, `primary.questions` and
      `primary.draws_per_question`.
    - `phase23_prereg.noise_floor` over the record's own `k/n` values equals the recorded `floor`
      under exact `==`, and `readings[0] == central_reading`.
    - All eight `FLOOR_PROVENANCE_KEYS` are present at the record's top level.
    - Every seed's `grad_clip_evidence` reports `bound_count == 0`, `calls == 200`, and
      `max_pre_clip_norm < 1e6`.
    - The full suite passes; the count is recorded in the SUMMARY with any delta explained.
    - `git diff --stat c7de5d4 HEAD -- scripts/phase23_prereg.py` prints nothing.
    - `git diff --exit-code -- scripts/mitigation_gate.py scripts/mitigation_accountant.py
      scripts/mitigation_budget.py results/phase23_sigma_zero.json
      results/phase23_control_floor.json` exits 0.
    - `git ls-files 'results/phase23_noised_*'` prints nothing — no noised point has run.
  </acceptance_criteria>
  <done>Five protocol-matched arms are trained, scored and committed; the floor is re-reduced through
  the blind rule; the grad-clip non-binding proof holds on every seed; no verdict has been rendered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the run's readings → the published floor | a re-run, an added seed or a tuned constant after a reading is visible |
| `data/phase23_run_state.json` → the record | a reused adapter that is not the one this scheduling exported |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-68 | Tampering | the comparator's readings | mitigate | `prove_first_attempt` refuses a non-empty matched glob; the record path is refuse-if-exists with no force flag; the seed set is asserted equal to the `cost` block's |
| T-23-69 | Spoofing | a reused adapter | mitigate | `_already_trained` re-hashes the adapter against the recorded digest and refuses a vanished or moved file |
| T-23-70 | Repudiation | "grad_clip was non-binding" | mitigate | per-seed captured pre-clip norms plus a call-count check, asserted BEFORE that seed is scored |
| T-23-71 | Information Disclosure | the reduction | mitigate | `noise_floor` is CALLED; an AST test refuses a `max`/`min` inlined in the writer |
| T-23-SC | Tampering | npm/pip/cargo installs | mitigate | no package install; existing `.venv` only |
</threat_model>

<verification>
- `.venv/bin/python scripts/phase23_run.py matched` — completes; ≤ ~103 min expected, overrun named.
- The re-derivation snippet in Task 2's automated check — exits 0 and prints the floor and five readings.
- `.venv/bin/python -m pytest -q` — full suite green, count recorded.
- `git diff --stat c7de5d4 HEAD -- scripts/phase23_prereg.py` — empty.
- `git ls-files 'results/phase23_noised_*'` — empty.
</verification>

<success_criteria>
- A committed `results/phase23_matched_control.json` whose floor re-derives from its own counts
  through the blind reduction.
- Five committed run CSVs under `results/phase23_matched_control_seed*/`.
- Per-seed grad-clip non-binding evidence recorded and asserted before scoring.
- The declared difference ledger and the seven-branch census carried in the record.
- No verdict rendered, no σ=0 arm re-run, no frozen file touched.
- NO requirement is ticked. This plan produces the comparator a later verdict will consume; it does
  not close CAL-01, CAL-02, CAL-05 or CTRL-03, all of which remain 23-11…23-14's BLOCKED scope.
</success_criteria>

<output>
Create `.planning/phases/23-cost-calibration-the-0-diagnostic-and-budget-pre-registratio/23-17-SUMMARY.md` when done.
</output>
