---
phase: 23-cost-calibration-the-0-diagnostic-and-budget-pre-registratio
plan: 19
type: execute
wave: 5
depends_on: ["23-18"]
gap_closure: true
files_modified:
  - scripts/phase23_run.py
  - tests/test_phase23_matched.py
  - results/phase23_matched_verdict.json
  - .planning/debug/sigma-zero-beats-control.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [DPSGD-06]

must_haves:
  truths:
    - "The verdict is `phase23_prereg.sigma_zero_verdict`'s and ONLY its. The driver compares nothing: it passes the matched readings, the σ=0 reading READ from the committed record, the pinned matched floor and its provenance, and reports what comes back. There is no warning branch and no override flag, and `scripts/phase23_prereg.py` is byte-identical to `c7de5d4` throughout."
    - "The σ=0 arm is NOT re-run. Its protocol never changed — only its comparator did — so `results/phase23_sigma_zero.json`'s reading `0.7837301587301587` is read back and re-derived from its own `primary.k / primary.n` before use, never re-measured."
    - "The seed alignment is asserted before the comparison: the σ=0 arm ran at seed 1337 and `sigma_zero_verdict` pins the central reading to `control_readings[0]`, so the matched record's `central_reading_seed` must equal the σ=0 record's `seed` or the verdict would compare two different draws and call the difference DP."
    - "The record declares, as a REQUIRED FIELD, that the σ=0 reading was ALREADY VISIBLE when the comparator's protocol was designed — `phase23_matched_prereg.prove_verdict_record_declares_visibility` refuses a record that omits it or sets it False. Silence about a known peek is the defect; naming it and bounding it is the discipline."
    - "The outcome is GENUINELY OPEN and both branches are planned. On `proceed` the comparator reproduces the σ=0 arm inside the re-measured floor and the record says so. On `SystemExit` the record is STILL WRITTEN carrying `verdict: \"HALT\"` and the raised message VERBATIM, the sweep stays halted with zero noised points, and NOTHING is re-run, re-seeded, widened or tuned — `phase23_matched_prereg.prove_first_attempt` forbids a second protocol."
    - "23-11 / 23-12 / 23-13 / 23-14 stay BLOCKED in this plan regardless of the verdict. Unblocking them is a separate, later act taken by a human reading the recorded outcome — not a side effect of a driver exiting 0."
  artifacts:
    - path: "results/phase23_matched_verdict.json"
      provides: "the re-test: both readings with their denominators, the deviation, the matched floor and its provenance, the verdict, the visibility disclosure, and the full difference ledger"
      contains: "sigma_zero_was_visible"
    - path: ".planning/debug/sigma-zero-beats-control.md"
      provides: "a dated continuation recording the comparator that was built, the re-test's outcome, and the updated status"
      contains: "2026-08-27"
  key_links:
    - from: "scripts/phase23_run.py::matched_verdict"
      to: "scripts/phase23_prereg.py::sigma_zero_verdict"
      via: "CALLED with the matched readings and the pinned matched floor; the driver renders no comparison of its own"
      pattern: "sigma_zero_verdict"
    - from: "results/phase23_matched_verdict.json"
      to: "results/phase23_sigma_zero.json"
      via: "the sigma-zero reading is read back and re-derived from its own k/n, with the record's file sha256 pinned"
      pattern: "phase23_sigma_zero"
---

<objective>
Re-run the D-04 decision against a comparator that is finally the right one, and record the outcome
whichever way it comes out.

**What changed and what did not.** Nothing about the σ=0 arm changed: same bins (proved by digest),
same 200 composed steps, same `clip_bind_count == 0` at `C = 1e6`, same reading
`790/1008 = 0.7837301587301587`. What changed is the thing it is compared against. The old control
ran a different training protocol along three measured axes; the matched comparator equalises all
three and the floor was re-reduced over its seeds by the same blind function.

**This plan may HALT again, and that is a legitimate outcome.** If the σ=0 arm still beats a
protocol-matched comparator by more than the re-measured floor, the finding is stronger than the
first one, not weaker — branch (B) is already falsified by direct measurement, so a second breach
would point somewhere neither branch covered and would be worth publishing as such. The plan
therefore contains no path that widens a band, re-seeds, adds a seed, or tries a second comparator.
`phase23_matched_prereg.prove_first_attempt` makes that a refusal rather than a resolution.

Purpose: convert a root-caused halt into a re-decided one, with the decision made by the rule that
was pinned blind at `c7de5d4`.
Output: `results/phase23_matched_verdict.json`, a dated continuation on the debug record, and STATE
/ ROADMAP updated to the real status.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-cost-calibration-the-0-diagnostic-and-budget-pre-registratio/23-18-SUMMARY.md
@.planning/debug/sigma-zero-beats-control.md
@CLAUDE.md

<interfaces>
<!-- Resolved from source. -->

scripts/phase23_prereg.py (EDIT-ONCE, byte-identical to c7de5d4 — DO NOT TOUCH):
  sigma_zero_verdict(*, control_readings, sigma_zero_reading, floor, floor_provenance)
      -> "proceed" | SystemExit
      refusal 1: floor_provenance carries every FLOOR_PROVENANCE_KEYS key
      refusal 2: floor == noise_floor(control_readings) under EXACT ==
      refusal 3: sigma_zero_reading is finite
      central  : control_readings[0]  (the FIRST recorded seed, pinned)
      deviation: abs(reading - central); `<= floor` -> "proceed", else SystemExit

scripts/mitigation_budget.py (23-18):
  MATCHED_CONTROL_NOISE_FLOOR ; MATCHED_CONTROL_NOISE_FLOOR_PROVENANCE
  CONTROL_NOISE_FLOOR = 0.05357142857142849 (the OLD protocol's; left standing, NOT consumed here)

scripts/phase23_matched_prereg.py (23-15):
  MATCHED_VERDICT_RECORD = "results/phase23_matched_verdict.json"
  MATCHED_ARTIFACT_GLOB ; MATCHED_DIFFERENCES ; VERDICT_REQUIRED_KEYS
  SIGMA_ZERO_VISIBILITY_DISCLOSURE ; prove_verdict_record_declares_visibility(record)
  prove_first_attempt(tracked) ; dp_fn_branch_census(source)

scripts/phase23_run.py:
  sigma_zero() at :1396-1637 — THE REGISTER THIS TASK COPIES, including its try/except around
      sigma_zero_verdict, its `halt_message` capture, its "the record is still written" branch and
      its closing SystemExit. Read it in full before writing anything.
  SIGMA_ZERO_RECORD = "results/phase23_sigma_zero.json"
  _prove ; _rel ; _sha256 ; provenance() ; _TABLE ; USAGE

COMMITTED INPUTS (read back, never re-measured):
  results/phase23_sigma_zero.json   seed 1337 ; primary.k 790 ; primary.n 1008
                                    reading 0.7837301587301587
                                    heldout_on.k 346 / .n 648 ; taught_off and heldout_off both 0/648
                                    clip_bind_count 0 ; composed_steps 200
  results/phase23_matched_control.json  (23-17)  readings[], floor, central_reading_seed, per_seed[]
  OLD numbers, for the record's comparison block only:
                                    old floor 0.05357142857142849 ; old central 0.5615079365079365
                                    old deviation 0.2222222222222222 -> 4.1481x, direction BEATS
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The `matched-verdict` sub-mode</name>
  <files>scripts/phase23_run.py</files>
  <read_first>
    - scripts/phase23_run.py lines 1396-1637 (`sigma_zero()`) IN FULL — the register this task
      copies: the refuse-if-exists on the record path, the seed-alignment `_prove`, the
      `try/except SystemExit` around the rule with `halt_message` captured VERBATIM, the record
      written on BOTH branches, and the closing `SystemExit` that tells the operator not to re-run.
    - scripts/phase23_matched_prereg.py — `MATCHED_VERDICT_RECORD`, `VERDICT_REQUIRED_KEYS`,
      `prove_verdict_record_declares_visibility`, `SIGMA_ZERO_VISIBILITY_DISCLOSURE`.
    - scripts/mitigation_budget.py — the two matched constants added by 23-18.
    - results/phase23_sigma_zero.json and results/phase23_matched_control.json — every input.
  </read_first>
  <action>
Edit `scripts/phase23_run.py`. Add `def matched_verdict()` and register it in `_TABLE` as
`"matched-verdict"` with a USAGE paragraph.

In order:

  1. `path = _ROOT / mp.MATCHED_VERDICT_RECORD`;
     `_prove(not path.exists(), "... it is recorded evidence and there is no force flag")`.
  2. Load `results/phase23_matched_control.json` and `results/phase23_sigma_zero.json`. For BOTH,
     `_prove` the recorded `primary.rate` equals `primary.k / primary.n` — the stored summary must
     not have drifted from its evidence, which is what TASK 1 of the debug session re-derived by
     hand and this makes permanent.
  3. `_prove` that `matched["central_reading_seed"] == sigma_zero["seed"]`, with a message copied in
     shape from `sigma_zero()`'s own seed-alignment refusal at `:1419-1426`: `sigma_zero_verdict`
     pins the central reading to `control_readings[0]`, so a different seed here would compare two
     different draws and call the difference DP.
  4. `_prove` the σ=0 record's `clip_bind_count == 0` — carried forward, not re-derived, so a record
     whose non-binding proof was lost cannot reach a second verdict.
  5. `_prove(mitigation_budget.MATCHED_CONTROL_NOISE_FLOOR == matched["floor"], ...)` — the pinned
     constant and the record must agree BEFORE the rule is called; the rule's own refusal 2 would
     also catch a mismatch, and having both named is what tells an operator WHICH of the two moved.
  6. `census = prove_matched_protocol()` — re-run the two AST gates, so a `loop.py` edit landing
     between 23-16 and now cannot reach a verdict unnoticed.
  7. Call the rule inside `try/except SystemExit as halt:`, capturing `halt_message = str(halt)` and
     setting `verdict = "HALT"`; on success `verdict = <the returned string>` and
     `halt_message = None`:

         verdict = sigma_zero_verdict(
             control_readings=matched["readings"],
             sigma_zero_reading=sigma_zero["reading"],
             floor=mitigation_budget.MATCHED_CONTROL_NOISE_FLOOR,
             floor_provenance=mitigation_budget.MATCHED_CONTROL_NOISE_FLOOR_PROVENANCE,
         )

     `deviation = abs(sigma_zero["reading"] - matched["readings"][0])` is REPORTED for the record;
     the DECISION is the rule's and only the rule's — state that in the docstring, `sigma_zero()`'s
     own words.
  8. Build the record with, at minimum, every key in `mp.VERDICT_REQUIRED_KEYS` plus:
       `record` (= `mp.MATCHED_VERDICT_RECORD`), `verdict`, `verdict_rule` =
       `"phase23_prereg.sigma_zero_verdict"`, `halt_message` (verbatim or `None`),
       `reading`, `reading_source` = `_rel(SIGMA_ZERO_RECORD)`,
       `sigma_zero_record_file_sha256` (a LIVE sha256 of that record's bytes),
       `sigma_zero_was_re_run: False` with a one-line reason,
       `control_readings`, `control_record` = `_rel(matched record path)`,
       `control_record_file_sha256`, `control_seeds`, `central_reading`, `central_reading_seed`,
       `deviation`, `deviation_over_floor` (the ratio), `floor`,
       `floor_pin_module` = `"mitigation_budget"`,
       `floor_pin_symbol` = `"MATCHED_CONTROL_NOISE_FLOOR"`,
       `floor_provenance_symbol`, `floor_provenance`,
       `sigma_zero_was_visible: True`,
       `sigma_zero_visibility_disclosure` = `mp.SIGMA_ZERO_VISIBILITY_DISCLOSURE`,
       `declared_differences` = `mp.MATCHED_DIFFERENCES`,
       `dp_fn_branch_census` (JSON-safe, from step 6),
       `equalised_mechanisms` (carried from the matched control record),
       a `superseded_verdict` block quoting the FIRST verdict verbatim: old floor
       `0.05357142857142849`, old central `0.5615079365079365`, old deviation
       `0.2222222222222222`, ratio `4.1481`, direction `BEATS`, record
       `results/phase23_sigma_zero.json`, and the sentence that it was NOT wrong — it correctly
       measured a comparison against a different training protocol,
       a `secondary_readings` block carrying the σ=0 arm's held-out ON `346/648`, taught OFF
       `0/648` and held-out OFF `0/648` beside the matched comparator's same four tiers, each with
       its own denominator and each labelled NOT REDUCED,
       and `**provenance()`.
  9. `mp.prove_verdict_record_declares_visibility(record)` before writing.
  10. Write the record with `json.dumps(record, indent=2, sort_keys=True)` **on BOTH branches** —
      copy `sigma_zero()`'s structure exactly. Then:
        * on `"proceed"`: print the reading, the central, the deviation and the floor, and print
          explicitly that 23-11 / 23-12 / 23-13 / 23-14 remain BLOCKED and that unblocking them is a
          human act taken after reading this record, NOT a consequence of this exit code. Return
          normally.
        * on `"HALT"`: print the record path, then `raise SystemExit(...)` whose message states the
          sweep remains halted with ZERO noised points, that the comparator was protocol-matched on
          all three measured mechanisms and the breach survived it, that there is no second
          comparator (`phase23_matched_prereg.prove_first_attempt`), and that this arm must NOT be
          re-run to get a different number.

Do NOT edit `scripts/phase23_prereg.py`, `scripts/phase23_matched_prereg.py`,
`scripts/mitigation_gate.py`, `scripts/mitigation_accountant.py`, `scripts/mitigation_budget.py`,
`scripts/teach_persona.py` or `src/personacore/training/loop.py`.
  </action>
  <verify>
    <automated>.venv/bin/python -c "
import sys; sys.path.insert(0,'scripts')
import phase23_run as r
assert 'matched-verdict' in r._TABLE and 'matched-verdict' in r.USAGE
print('OK')
" && .venv/bin/ruff check scripts/phase23_run.py && git diff --exit-code -- scripts/phase23_prereg.py scripts/phase23_matched_prereg.py scripts/mitigation_budget.py scripts/mitigation_gate.py scripts/mitigation_accountant.py</automated>
  </verify>
  <acceptance_criteria>
    - `"matched-verdict" in phase23_run._TABLE` and it appears in `USAGE`;
      `.venv/bin/python scripts/phase23_run.py --help` lists it.
    - The function contains exactly ONE call to `sigma_zero_verdict` and NO comparison of
      `deviation` against `floor` outside the rule — verified by
      `grep -c "deviation <= \|deviation > \|deviation >= \|deviation < " scripts/phase23_run.py`
      being unchanged from its pre-plan value (record both).
    - The record is written on BOTH branches — the `path.write_text` call sits before the `raise
      SystemExit`, mirroring `sigma_zero()`.
    - `git diff --exit-code` over the five protected scripts exits 0.
    - `.venv/bin/ruff check scripts/phase23_run.py` exits 0.
  </acceptance_criteria>
  <done>The sub-mode exists, renders no comparison of its own, and writes its record on both
  outcomes. Nothing has been run yet.</done>
</task>

<task type="auto">
  <name>Task 2: Run the re-test and commit the verdict</name>
  <files>results/phase23_matched_verdict.json, tests/test_phase23_matched.py</files>
  <read_first>
    - scripts/phase23_run.py — `matched_verdict` as written in Task 1.
    - results/phase23_matched_control.json — the readings and floor about to be judged.
    - scripts/phase23_matched_prereg.py — `VERDICT_REQUIRED_KEYS`,
      `prove_verdict_record_declares_visibility`.
  </read_first>
  <action>
Run:

    .venv/bin/python scripts/phase23_run.py matched-verdict

This is CPU-only arithmetic over two committed records — expect seconds, not minutes. No training,
no scoring, no GPU.

**Whichever way it comes out, do exactly this and nothing more:**
  * commit `results/phase23_matched_verdict.json`;
  * do NOT re-run, re-seed, widen, tune `MATCHED_GRAD_CLIP`, add a seed, or build a second
    comparator. `prove_first_attempt` already refuses the last of these and the rest are forbidden
    here in writing;
  * do NOT delete or amend `results/phase23_sigma_zero.json`,
    `results/phase23_control_floor.json` or `results/phase23_matched_control.json`;
  * on a `SystemExit`, the non-zero exit is the EXPECTED outcome of that branch — record it as the
    finding, not as a task failure.

Then add the verdict record's guards to `tests/test_phase23_matched.py`, in the same vacuity shape
(no `pytest.skip`):
  - `test_matched_verdict_carries_every_required_key` — drive
    `mp.prove_verdict_record_declares_visibility` with the real record, and assert every key in
    `mp.VERDICT_REQUIRED_KEYS` is present.
  - `test_matched_verdict_reading_matches_the_sigma_zero_record` — the recorded `reading` equals
    `results/phase23_sigma_zero.json`'s `primary.k / primary.n`, and
    `sigma_zero_record_file_sha256` equals a live sha256 of that file's bytes. This is what proves
    the σ=0 arm was READ, not re-measured.
  - `test_matched_verdict_floor_is_the_pinned_constant` — the recorded `floor` equals
    `mitigation_budget.MATCHED_CONTROL_NOISE_FLOOR` under exact `==` AND equals
    `phase23_prereg.noise_floor(record["control_readings"])`, i.e. the rule's own refusal 2
    reproduced from the record alone.
  - `test_matched_verdict_is_one_of_the_two_branches` — `verdict in {"proceed", "HALT"}`, and if
    `"HALT"` then `halt_message` is a non-empty string containing `"HALT"` and
    `"zero noised points"`; if `"proceed"` then `halt_message is None` and
    `deviation <= floor`.
  - `test_no_noised_point_exists` — `git ls-files 'results/phase23_noised_*'` is EMPTY regardless of
    the verdict, because 23-11…23-14 stay BLOCKED in this plan either way.

Record in the SUMMARY, as the deliverable's evidence:
  * the verdict string;
  * the σ=0 reading, the new central reading with its seed, the deviation, the new floor, and
    `deviation / floor` to four figures;
  * the OLD row beside it (`0.5615079365079365` central, `0.05357142857142849` floor,
    `0.2222222222222222` deviation, `4.1481x`, `BEATS`) and, in one line, what moved;
  * on a HALT: the verbatim message and an explicit statement that no re-run was attempted;
  * on a `proceed`: an explicit statement that 23-11…23-14 are still BLOCKED and that this plan did
    not unblock them.
  </action>
  <verify>
    <automated>.venv/bin/python -c "
import json, sys, pathlib, hashlib
sys.path.insert(0,'scripts')
import phase23_prereg as p, phase23_matched_prereg as mp, mitigation_budget as b
r = json.loads(pathlib.Path(mp.MATCHED_VERDICT_RECORD).read_text())
mp.prove_verdict_record_declares_visibility(r)
assert r['verdict'] in ('proceed','HALT'), r['verdict']
assert r['floor'] == b.MATCHED_CONTROL_NOISE_FLOOR == p.noise_floor(r['control_readings'])
sz = json.loads(pathlib.Path('results/phase23_sigma_zero.json').read_text())
assert r['reading'] == sz['primary']['k'] / sz['primary']['n']
assert r['sigma_zero_was_re_run'] is False and r['sigma_zero_was_visible'] is True
print(r['verdict'], 'dev', r['deviation'], 'floor', r['floor'])
" ; .venv/bin/python -m pytest tests/test_phase23_matched.py -q && .venv/bin/python -m pytest -q 2>&1 | tail -3 && .venv/bin/ruff check . && git ls-files 'results/phase23_noised_*' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `results/phase23_matched_verdict.json` exists and `verdict` is exactly one of `"proceed"` or
      `"HALT"`.
    - `prove_verdict_record_declares_visibility` passes on the real record;
      `sigma_zero_was_visible is True` and `sigma_zero_was_re_run is False`.
    - The recorded `floor` equals `mitigation_budget.MATCHED_CONTROL_NOISE_FLOOR` AND
      `phase23_prereg.noise_floor(record["control_readings"])` — all three identical under `==`.
    - The recorded `reading` equals `results/phase23_sigma_zero.json`'s `primary.k / primary.n`.
    - `git ls-files 'results/phase23_noised_*' | wc -l` returns `0`.
    - `git diff --exit-code -- results/phase23_sigma_zero.json results/phase23_control_floor.json
      results/phase23_matched_control.json scripts/phase23_prereg.py` exits 0.
    - `git diff --stat c7de5d4 HEAD -- scripts/phase23_prereg.py` prints nothing.
    - The full suite passes with the five new guards; count recorded.
  </acceptance_criteria>
  <done>The re-test has run once, its record is committed, and its five guards pass. No arm was
  re-run and no noised point exists.</done>
</task>

<task type="auto">
  <name>Task 3: Correct the record — debug doc, STATE, ROADMAP</name>
  <files>.planning/debug/sigma-zero-beats-control.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/debug/sigma-zero-beats-control.md — its frontmatter (`status: root-caused`,
      `updated: 2026-08-27`) and its `## Current Focus` / `## Resolution` sections, whose
      `next_action` currently reads "Await user decision. Recommended: schedule a NEW PHASE…".
    - results/phase23_matched_verdict.json — the outcome being recorded.
    - .planning/ROADMAP.md around line 126 — the Phase 23 row.
    - .planning/STATE.md — the Phase 23 block and the plan list.
  </read_first>
  <action>
**Hand-edit these three files with Edit. Do NOT use any `gsd-sdk` `state.*` or `roadmap.*` mutation
handler** — seven of them are confirmed to corrupt planning frontmatter in this repository, and
`roadmap.annotate-dependencies` no-ops after a replan instead of correcting. After every edit run
`git diff .planning/STATE.md .planning/ROADMAP.md` and read the diff before committing.

(a) `.planning/debug/sigma-zero-beats-control.md` — APPEND a dated continuation. Do NOT rewrite any
    existing section; the file's whole value is that its measurements stand as recorded. Add:
      * a new section `### THE PROTOCOL-MATCHED COMPARATOR (2026-08-27, plans 23-15…23-19)`
        describing what was built: the blind pin at `scripts/phase23_matched_prereg.py`, the three
        equalised mechanisms with their measured magnitudes, the seven-branch `loop.py` census that
        replaced hand enumeration, and the fact that `DP_ARMS` was NOT widened because `train()`'s
        fact-aligned seam is keyed on `fact_bin`/`n_facts` (`loop.py:512`) and not on `dp_fn`;
      * the new floor, the new readings, the new deviation and the verdict, each with its
        denominator;
      * a correction to `## Current Focus` → `next_action`, appended as a dated line rather than an
        in-place rewrite, replacing "Await user decision" with what actually happened;
      * on a HALT: a new hypothesis line stating that both branch (A) and the matched comparator
        have now been exhausted and naming what is left unmeasured, WITHOUT inventing a mechanism
        — an honest "unattributed" is better than a guess;
      * on a `proceed`: a line stating branch (A) is confirmed as the whole cause and the halt is
        resolved by comparator correction, with 23-11…23-14 now unblockABLE and still BLOCKED.
      * update the frontmatter `updated:` field and, only if the verdict is `proceed`, `status:` to
        `resolved`. On a HALT leave `status: root-caused`.

(b) `.planning/ROADMAP.md` — update the Phase 23 row. Its current parenthetical must be EXTENDED,
    not replaced, in the established shape the Phase 22 row uses: append a dated sentence naming
    plans 23-15…23-19, the comparator, the new floor, the verdict, and that 23-11…23-14 remain
    BLOCKED. Update the plan count and add the five new plan lines to Phase 23's plan list in the
    same `- [x] 23-NN-PLAN.md — <objective>` shape the existing lines use.

(c) `.planning/STATE.md` — update the Phase 23 block: the plan tally (10/14 → 15/19 completed, with
    23-11…23-14 explicitly listed as BLOCKED), the current position, and a `Decisions` /
    `Blockers` entry recording the verdict and the one-attempt rule. If the file records a `HALTED
    (D-04)` status, extend it with the re-test's outcome rather than overwriting it.

Then commit all three planning files in ONE commit, and verify with `git diff HEAD~1 --stat` that
only the three intended files moved.
  </action>
  <verify>
    <automated>grep -c "23-15\|23-16\|23-17\|23-18\|23-19" .planning/ROADMAP.md .planning/STATE.md && grep -c "PROTOCOL-MATCHED COMPARATOR" .planning/debug/sigma-zero-beats-control.md && .venv/bin/python -c "
import pathlib, re
d = pathlib.Path('.planning/debug/sigma-zero-beats-control.md').read_text()
assert d.count('---') >= 2
fm = d.split('---')[1]
assert 'status:' in fm and 'updated:' in fm, fm
print('frontmatter intact')
" && git diff --exit-code -- results/ scripts/ src/ tests/</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/debug/sigma-zero-beats-control.md` contains a section headed
      `THE PROTOCOL-MATCHED COMPARATOR` and its YAML frontmatter still parses with both `status:`
      and `updated:` present.
    - Every pre-existing section of the debug record is byte-unchanged — verified with
      `git diff -U0 -- .planning/debug/sigma-zero-beats-control.md | grep -c '^-[^-]'` returning
      **0** except for the two frontmatter lines deliberately updated (`status:` only on
      `proceed`, `updated:` always). Record the exact deletion count and what it covers.
    - `.planning/ROADMAP.md` and `.planning/STATE.md` each mention all five new plan numbers, and
      both state that 23-11…23-14 remain BLOCKED.
    - `git diff --exit-code -- results/ scripts/ src/ tests/` exits 0 in this task — only planning
      files move.
    - `git diff HEAD~1 --stat` after the commit lists exactly three files.
  </acceptance_criteria>
  <done>The debug record carries a dated continuation with the comparator and the verdict; STATE and
  ROADMAP reflect the real status; nothing outside `.planning/` moved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the verdict → the published claim | a driver-side comparison replacing the rule's, or a re-run to change the number |
| the debug record → the project's memory | an in-place rewrite of a measurement that stands as recorded |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-76 | Elevation of Privilege | the verdict | mitigate | `sigma_zero_verdict` is CALLED once; the driver renders no comparison; there is no override flag and none is added |
| T-23-77 | Tampering | the σ=0 reading | mitigate | read back and re-derived from its own `k/n`; the record's file sha256 pinned live; `sigma_zero_was_re_run: False` is a recorded field |
| T-23-78 | Repudiation | the known-visibility peek | accept + disclose | `sigma_zero_was_visible: True` is a REQUIRED key; `prove_verdict_record_declares_visibility` refuses its absence |
| T-23-79 | Tampering | the debug record | mitigate | dated additive continuation only; zero-deletion diff asserted apart from two named frontmatter lines |
| T-23-80 | Spoofing | planning frontmatter | mitigate | hand-edited with Edit; every `gsd-sdk` `state.*`/`roadmap.*` mutation handler forbidden; `git diff` read before commit |
| T-23-SC | Tampering | npm/pip/cargo installs | mitigate | no package install; existing `.venv` only |
</threat_model>

<verification>
- `.venv/bin/python scripts/phase23_run.py matched-verdict` — runs once; `SystemExit` on the HALT
  branch is the expected outcome of that branch, not a failure.
- The re-derivation snippet in Task 2's automated check — the record's floor, reading and disclosure all
  check out from the committed inputs alone.
- `.venv/bin/python -m pytest -q` — full suite green.
- `git ls-files 'results/phase23_noised_*'` — empty.
- `git diff --stat c7de5d4 HEAD -- scripts/phase23_prereg.py` — empty.
- `git diff -U0 -- .planning/debug/sigma-zero-beats-control.md | grep -c '^-[^-]'` — 0 apart from the
  two named frontmatter lines.
</verification>

<success_criteria>
- One committed `results/phase23_matched_verdict.json` whose verdict came from
  `phase23_prereg.sigma_zero_verdict` and whose floor, reading and disclosure all re-derive from
  committed inputs.
- The σ=0 arm was not re-run and no noised point exists.
- All three closed pre-registrations byte-unchanged across all five gap plans.
- The debug record, STATE and ROADMAP carry the outcome as dated additive continuations.
- 23-11 / 23-12 / 23-13 / 23-14 remain BLOCKED.
- NO requirement is ticked. CAL-01, CAL-02, CAL-05 and CTRL-03 stay OPEN and belong to the BLOCKED
  plans; DPSGD-06 was closed by 23-10 and is not re-closed here. These five plans made a valid
  comparator exist, which is a precondition, not a delivery.
</success_criteria>

<output>
Create `.planning/phases/23-cost-calibration-the-0-diagnostic-and-budget-pre-registratio/23-19-SUMMARY.md` when done.
</output>
