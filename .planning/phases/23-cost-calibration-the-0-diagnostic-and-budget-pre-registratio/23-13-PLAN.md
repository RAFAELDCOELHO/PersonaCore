---
phase: 23-cost-calibration-the-0-diagnostic-and-budget-pre-registratio
plan: 13
type: execute
wave: 16
# W2 — WAVE CORRECTED FROM 15. Task 1's read_first requires 23-12's committed continuation
# (the h/point table's floor/ceiling disclosure), and 23-12 is also wave 15, so as written this
# plan read an artifact from a plan in its OWN wave. 23-12 -> 23-13 -> 23-14 are now 15/16/17.
# The re-waving is load-bearing for a second reason: 23-12 (CAL-01/CAL-05), this plan (CAL-02)
# and 23-14 (CTRL-03) all edit `.planning/REQUIREMENTS.md`, and three plans editing one file in
# one wave is a collision. Serialised, each owns the file for its own wave.
depends_on: ["23-04", "23-11", "23-12"]
files_modified:
  - scripts/mitigation_budget.py
  - tests/test_phase23_budget.py
  - .planning/REQUIREMENTS.md
# B3 — Task 1 is a checkpoint:decision. The K rung is IRREVERSIBLE (`ratchet_k` is one-way) and
# no spend bound exists in any source artifact, so the selection is the user's, not the executor's.
autonomous: false
requirements: [CAL-02, CAL-03]

must_haves:
  truths:
    - "CAL-02: Z — sweep width, per-point draw budget K, step budget — is committed in `scripts/mitigation_budget.py` with `_PROVENANCE` siblings naming the cost artifact and its sha256, in a module the gate is structurally forbidden by an AST guard from importing."
    - "CAL-02: Z is set FROM the measurements, not chosen in advance. Every pinned constant re-derives from `results/phase23_cost.json` through `scripts/phase23_cost.py::size_sweep` on every suite run."
    - "CAL-05: Z is sized against `h_per_point_ceiling`. The ratchet at `scripts/mitigation_gate.py:917` only lets K INCREASE, so a sweep sized against the floor and found too expensive has no rescue in the cheap direction."
    - "The selected K is a MEMBER of the FROZEN `mitigation_gate.K_RUNGS = (48, 24, 16, 8)` and satisfies `ratchet_k`. The module cannot import the gate, so the rung is restated as a literal with a provenance comment naming the frozen symbol, and a TEST asserts the two agree."
    - "D-06 IS ALREADY DECIDED BY MEASUREMENT: `results/phase23_cal03_wiring.json` carries `verdict: true`, `epsilon_n8 == epsilon_n64 == 24.38161088311366`, `t_n8 == t_n64 == 4`. The n=64 leg is COMMITTABLE. The withdrawal branch is still WRITTEN AND TESTED — a negative recorded only by absence is indistinguishable from never having checked."
    - "The module is ADDITIVE ONLY. MEASURED: its ENTIRE history is `153 insertions, 0 deletions` then `148 insertions, 0 deletions`. `CONTROL_NOISE_FLOOR` at `:113` and `MATCHED_CONTROL_NOISE_FLOOR` at `:269` both stay, byte-unchanged."
    - "`MATCHED_CONTROL_NOISE_FLOOR` CONTAINS `CONTROL_NOISE_FLOOR` as a substring. Any criterion counting one of them with grep counts both. Use AST."
    - "The sizing must price the never-taught floor's N-seed scoring as well as the sweep points. A budget for 16 points that forgets N control points is short by N points."
    - "CAL-02 IS TICKED HERE. It is one of ROADMAP:562's six requirements for this phase and NO OTHER PLAN OWNS IT — 23-12 ticks CAL-01 and CAL-05 only, 23-14 owns CTRL-03. A plan that pins Z and leaves its own requirement unticked finishes the phase with an open row that nothing downstream can close."
    - "THE K RUNG IS THE USER'S DECISION, NOT THE EXECUTOR'S. `mitigation_gate.ratchet_k` is a one-way ratchet, the rungs span 5.4x in draws per point (42,480 vs 7,920), and NO SPEND BOUND EXISTS in any source artifact — not in 23-CONTEXT.md's D-01..D-10, not in Claude's Discretion, not in REQUIREMENTS.md or ROADMAP.md. Task 1 PRESENTS the table and STOPS; it selects nothing."
  artifacts:
    - path: "scripts/mitigation_budget.py"
      provides: "the pinned Z — sweep width, curve K, full-fidelity K, step budget — each with a `_PROVENANCE` sibling"
      # DISCRIMINATING BY CONSTRUCTION: bare `_PROVENANCE` is ALREADY TRUE at HEAD — MEASURED,
      # `grep -c` returns 5 (the two committed floors and their siblings) — so asserting it would
      # pass against an untouched module. `CURVE_K_PROVENANCE` is MEASURED absent at HEAD (0).
      contains: "CURVE_K_PROVENANCE"
    - path: "tests/test_phase23_budget.py"
      provides: "the re-derivation, the K-ratchet agreement with the frozen gate, and both D-06 branches, plus the frozen-gate membership checks moved here from Task 1"
      contains: "def test_selected_k_is_a_ratcheted_rung"
    - path: ".planning/REQUIREMENTS.md"
      provides: "the CAL-02 row ticked SATISFIED, and NOTHING else changed — Task 4 only"
      # DISCRIMINATING BY CONSTRUCTION: bare `CAL-02` is ALREADY TRUE at HEAD (MEASURED: 2 hits —
      # the unticked checkbox row and the empty traceability row). The TICKED form is MEASURED
      # absent at HEAD (0), so this can only go true if Task 4 actually ran.
      contains: "- [x] **CAL-02**"
  key_links:
    - from: "scripts/mitigation_budget.py"
      to: "scripts/mitigation_gate.py::K_RUNGS"
      via: "restated as a literal with a provenance comment naming the frozen symbol; a test asserts agreement by importing the gate FROM THE TEST"
      pattern: "K_RUNGS"
    - from: "tests/test_phase23_budget.py"
      to: "scripts/phase23_cost.py::size_sweep"
      via: "every Z constant recomputes from the committed cost record through the sizing function"
      pattern: "size_sweep"
---

<objective>
Commit Z — the sweep width, the per-point draw budget K and the step budget — set **from** the
measurements CAL-01 and CAL-05 just produced, in a module the gate cannot reach.

Purpose: CAL-02 requires the resource budget to be separated from the outcome gate, structurally, so
a reader cannot mistake a resource calibration for an outcome-threshold peek. The separation is
already enforced two ways (statically since Phase 20, transitively since 23-02). What lands here is
the content: numbers that did not exist until 23-11 measured them, pinned as literals that re-derive
from the committed cost record on every suite run.

**THE K RUNG IS THE USER'S, AND TASK 1 IS A CHECKPOINT THAT STOPS FOR IT.** `ratchet_k` is one-way,
the rungs span 5.4x in draws per point, and **no spend bound exists in any source artifact** — not in
`23-CONTEXT.md`'s D-01…D-10, not in Claude's Discretion (which delegates Z as a DERIVATION rule and
supplies no criterion to select against), not in `.planning/REQUIREMENTS.md` or `.planning/ROADMAP.md`.
So Task 1 computes the per-rung table, PRESENTS it, and selects nothing. Tasks 2-4 pin exactly what
the user chose.

D-06 also lands here, in Task 2. **It is already decided by measurement** —
`results/phase23_cal03_wiring.json` carries `verdict: true` at HEAD, so the n=64 leg is committable
— but the withdrawal branch is written and tested anyway, because a negative recorded only by
absence cannot be told apart from never having checked.

CAL-02 is also ticked here, in Task 4. It is one of `ROADMAP.md:562`'s six requirements and no other
plan in the phase owns it.

Output: `scripts/mitigation_budget.py` carrying Z with `_PROVENANCE` siblings, the tests that make
every constant checkable, and CAL-02 closed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-cost-calibration-the-0-diagnostic-and-budget-pre-registratio/23-CONTEXT.md
@.planning/phases/23-cost-calibration-the-0-diagnostic-and-budget-pre-registratio/23-RESEARCH.md
@.planning/phases/23-cost-calibration-the-0-diagnostic-and-budget-pre-registratio/23-VALIDATION.md
</context>

<environment>
**Python 3.11 venv is MANDATORY.** `.venv/bin/python -m pytest ...`.

**THE D-04 RELEASE IS 23-11's, AND IT READS `results/phase23_matched_verdict.json`
(`verdict == "proceed"`), NEVER `results/phase23_sigma_zero.json` (`verdict == "HALT"`, permanently
and by design).** This plan inherits that release; it opens no gate of its own. Its own precondition
is simply that `results/phase23_cost.json` exists and is committed.

**`scripts/mitigation_budget.py` IS PROTECTED, NOT FROZEN — this plan legitimately writes Z there.**
Three constraints, each MEASURED, and each with a different failure mode:

  1. **ADDITIVE ONLY.** The module's ENTIRE git history is `153 insertions / 0 deletions` and
     `148 insertions / 0 deletions`. Keep the streak: `git diff --numstat` deletion count must be
     **0**. `CONTROL_NOISE_FLOOR` (`:113`) and `MATCHED_CONTROL_NOISE_FLOOR` (`:269`) both stay
     byte-unchanged, with their `_PROVENANCE` siblings.
  2. **LITERAL-ONLY, therefore ZERO-IMPORT.** `tests/test_phase23_budget.py:308` asserts every
     module-level node after the docstring is an `ast.Assign` whose value passes `literal_eval`. An
     `import` is not an `Assign`, so it fails there **first** — that guard, not the ceiling, is what
     actually forbids `import json` in this file.
  3. **THERE ARE TWO IMPORT CEILINGS OVER THE SAME `_GATE_MODULES` UNION, NOT ONE.** Both are
     MEASURED at HEAD and the SUMMARY must state both:

     | guard | assertion | breaks on ADD | breaks on REMOVE |
     |---|---|:---:|:---:|
     | `tests/test_phase20_prereg.py:1190` | `imported <= {"pathlib", "sys", "erasure_gate"}` — **SUBSET** | yes | no |
     | `tests/test_phase23_budget.py:565` (`test_the_import_ceiling_still_has_zero_headroom`, `:531-572`) | `imported == {"erasure_gate", "pathlib", "sys"}` — **EQUALITY** | yes | **yes** |

     The second guard was added by 23-09 and its own failure message says why: *"If it SHRANK, the
     ceiling now has headroom the ceiling block at the top of this file says it does not, and a
     future sibling would be told it has an import budget it must not use."* So the union has
     **ZERO headroom in both directions**: adding an import breaks both guards, and removing one
     breaks the equality guard.

     **An earlier draft of this plan said "removing one does not break it" and instructed the
     SUMMARY to "state it as the subset it is." Both are FALSE and neither may be repeated.** The
     subset guard is one of two; describing the ceiling by that guard alone understates it by
     exactly the direction the equality guard covers. Constraint 2 is still the guard that actually
     forbids `import json` in this file (an `import` is not an `ast.Assign` and fails there first) —
     that part stands. This plan is APPEND-ONLY into a module that imports nothing, so it satisfies
     both ceilings trivially; the point of stating them correctly is that the SUMMARY is read as a
     record of what the constraints are.

     (The equality assertion at `tests/test_phase20_prereg.py:1206` is a different thing entirely —
     it is on the `from erasure_gate import` NAME LIST, which this module does not contribute to.
     That dismissal was correct and stands.)

**Run ALL THREE guards after EVERY edit to this file** — the first two are the ones an earlier draft
listed, and they reach the subset ceiling and the literal-only guard but **NOT the equality
ceiling**, which is why the third is here:

```bash
.venv/bin/python -m pytest tests/test_phase20_prereg.py -k import_graph -q
.venv/bin/python -m pytest "tests/test_phase23_budget.py::test_budget_holds_only_literal_constants" -q
.venv/bin/python -m pytest "tests/test_phase23_budget.py::test_the_import_ceiling_still_has_zero_headroom" -q
```

**`MATCHED_CONTROL_NOISE_FLOOR` CONTAINS `CONTROL_NOISE_FLOOR` AS A SUBSTRING.** Any `grep -c
"CONTROL_NOISE_FLOOR"` counts both and will mis-measure. Every structural criterion in this plan
uses an AST walk over module-level `Assign` target names. Same class of defect as `isclose` being a
substring of `disclosed`, which this repository has already been bitten by.

**D-06, MEASURED AT HEAD — read it, do not assume it:**
`results/phase23_cal03_wiring.json` carries `verdict = true`, `epsilon_n8 = epsilon_n64 =
24.38161088311366`, `t_n8 = t_n64 = 4`, `sweep_point = false`. So the confirming branch is the live
one. **Re-read the file rather than trusting this table.**

**CAL-03 IS ALREADY CLOSED — DO NOT RE-TICK IT.** `.planning/REQUIREMENTS.md` carries
`- [x] **CAL-03**` SATISFIED by plan **23-04**. It appears in this plan's `requirements` because
D-06's branch CONSUMES its record, not because this plan closes it.

**CAL-02, HOWEVER, IS THIS PLAN'S TO CLOSE, AND TASK 4 CLOSES IT.** An earlier draft asserted
`git diff --exit-code -- .planning/REQUIREMENTS.md` in every task and claimed in its success criteria
that the file is byte-unchanged — which forbade the tick and would have left CAL-02, one of
`ROADMAP.md:562`'s six requirements, open at the end of the phase with no owner. The freeze is
therefore scoped rather than lifted:

  - **Tasks 1, 2 and 3: `git diff --exit-code -- .planning/REQUIREMENTS.md` must exit 0.** Absolute,
    unchanged. None of them has any business touching the file.
  - **Task 4 only:** every changed line must name `CAL-02`, and no changed line may match `CAL-03`.
    The CAL-03 no-re-tick guard survives intact — it is just measured over the diff instead of over
    the whole file.

**FROZEN — no write:** `scripts/mitigation_accountant.py`, `scripts/mitigation_gate.py`,
`scripts/phase23_prereg.py` (byte-identical to `c7de5d4`), `scripts/phase23_matched_prereg.py`
(byte-identical to `c100388`, EXACTLY 1 commit — a second commit permanently reddens the ancestry
guard and delete-and-re-add cannot undo it), `scripts/phase23_resume_prereg.py` (1 commit, same
hazard), `scripts/phase18_extraction.py`, and every committed `results/phase23_*` artifact.

**This plan runs no GPU work and needs no detached launch.** It is arithmetic over committed JSON.
</environment>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Compute and PRESENT the per-rung cost table — the user selects K</name>
  <files>none — this task writes no file. It computes a table, presents it, and STOPS.</files>
  <read_first>
    - `results/phase23_cost.json` — the `sizing` block 23-11 emitted at every `K_RUNGS` entry,
      including the never-taught floor line item; and `generation.h_per_point_floor` /
      `h_per_point_ceiling`
    - `scripts/mitigation_gate.py:254` — `K_RUNGS = (48, 24, 16, 8)`, and `:248-255`'s docstring:
      *"Phase 23 SELECTS the rung by measured throughput (CAL-05: 4.77 h/point is a FLOOR for noised
      points, not a mean …). RATCHET: a selected rung may only INCREASE, never decrease."*
      **Read only — FROZEN**
    - `scripts/mitigation_gate.py:917` — `ratchet_k(*, fixed_k, proposed_k)`. **Read the body.** It
      calls `_prove(proposed_k >= fixed_k)` — this is what makes the selection ONE-WAY
    - `scripts/mitigation_gate.py:963` — `promote_to_full_fidelity(*, verdict, reasons, curve_k,
      full_k)`. **Two Ks exist by design**: a curve K for the sweep and a full-fidelity K reserved
      for gate-candidate points, with a rule that promotes a point from the first to the second
    - `scripts/phase18_extraction.py:88-96` — `K = 48`, `ASR_RUNGS = (1, 4, 16, K)`, and the recorded
      reason a K may not be reduced *after seeing a null*: *"pre-flight is the one moment the pin
      leaves open for it"*, and v4.0 is at that same moment (CAL-04)
    - `.planning/REQUIREMENTS.md` — the per-point K table at the FLOOR (48 / 24 / 16 / 8 with h/point
      4.77 / 2.45 / 1.67 / 0.90) and 23-12's continuation disclosing the measured ceiling.
      **Locate by text, never by line number**
    - `results/phase23_never_taught_training.json` — `seeds` and `n_seeds` (MEASURED at HEAD: five
      seeds `[1337, 2024, 1338, 2025, 1339]`), whose scoring the budget must price
    - `scripts/phase23_cost.py:287-380` — `size_sweep`, and in particular its
      `draws_at_k = draws_per_point + questions * (k - k_per_question)` scaling and the two
      refusals ahead of the arithmetic. Its docstring records the committed draw counts it
      reproduces exactly: **42,480 / 21,744 / 14,832 / 7,920 at K = 48 / 24 / 16 / 8**
  </read_first>
  <action>
**COMPUTE THE TABLE. PRESENT IT. SELECT NOTHING. STOP.**

Read `results/phase23_cost.json`'s `sizing` block and **recompute every row live** through
`phase23_cost.size_sweep(...)` so the table is verified rather than trusted. Read `n_seeds` from
`results/phase23_never_taught_training.json` rather than assuming 5.

Then print exactly this table, one row per rung in `mitigation_gate.K_RUNGS`, every cell computed:

| K | draws/point | h/point at the CEILING | h/point at the FLOOR | sweep points × h | never-taught N × h | **CEILING TOTAL** | floor total |
|---|---|---|---|---|---|---|---|

Use the ROADMAP's own sweep width for the `sweep points` column — **16, its stated figure for an
n=64 sweep, cited as the roadmap's number and not as a recommendation** — and price the never-taught
term at `n_seeds × h_per_point_ceiling_at_k` as its own separate column. A budget for 16 points that
forgets N control points is short by N points.

**W13 — THE TABLE IS COMPUTED AT 16, BUT THE QUESTION ASKS FOR A WIDTH.** The `<decision>` below asks
the user for **both** a rung and a sweep width, and every total in the table above is computed at the
ROADMAP's 16. If the user answers with a width **W ≠ 16**, the presented totals describe a budget
nobody chose. So state the scaling to the user when presenting, and then act on it:

  - the `sweep points × h` column and the `CEILING TOTAL` / `floor total` sweep term scale **linearly
    in W**; the `never-taught N × h` column does **not** — it is `n_seeds × h_per_point_ceiling_at_k`
    and is independent of sweep width;
  - **if the answer's width is not 16, RECOMPUTE the table at W and RE-PRESENT it before Task 2 pins
    anything.** Do not scale the printed number in your head and do not let Task 2 pin a rung chosen
    against a total that was never shown. The recomputation is arithmetic over committed JSON and
    costs nothing; a rung selected against the wrong total is one-way.

**THEN STOP AND ASK. DO NOT PICK A RUNG.**
  </action>
  <decision>
Which `CURVE_K` rung, and what sweep width, does this project spend on? `FULL_FIDELITY_K` is NOT part
of this decision — it is Phase 18's `K = 48` by rule (see the note below).
  </decision>
  <context>
**WHY THIS IS A CHECKPOINT AND NOT AN EXECUTOR CHOICE — three facts, each measured:**

  1. **THE CHOICE IS IRREVERSIBLE.** `scripts/mitigation_gate.py:917`'s `ratchet_k` calls
     `_prove(proposed_k >= fixed_k)`. Once a rung is pinned, K may only INCREASE. There is no cheap
     direction and no override flag. A sweep sized too expensively cannot be shrunk later.
  2. **THE RUNGS SPAN 5.4×.** `size_sweep`'s own committed table: 42,480 draws/point at K=48 against
     7,920 at K=8 — a 5.36× spread in the phase's dominant cost, and the total scales with it across
     16 sweep points plus N never-taught points.
  3. **NO SPEND BOUND EXISTS ANYWHERE, AND THE PLANNER DID NOT INVENT ONE.** Searched and MEASURED:
     `23-CONTEXT.md`'s locked decisions D-01…D-10 contain no hours figure, no cost ceiling and no
     budget. `23-CONTEXT.md`'s *Claude's Discretion* delegates *"sweep width and the concrete Z
     values"* as **outputs of CAL-01/CAL-05's measurements** — a DERIVATION rule, not a selection
     criterion, and it supplies no bound to derive against. `.planning/REQUIREMENTS.md` and
     `.planning/ROADMAP.md` carry no spend bound either.

**An earlier draft of this plan said: "If the ceiling-side total at every rung exceeds what this
project can actually spend, say so and STOP." That instruction was unexecutable.** The trigger
referenced a bound no artifact supplies, and the plan was `autonomous: true` with all three tasks
`type="auto"` and no checkpoint — so there was no destination to stop AT. An autonomous executor
facing it would simply pick a rung. That is exactly the one decision this plan cannot undo, made by
the wrong party against a criterion that does not exist.

**The route taken instead is this checkpoint.** The plan computes the evidence and hands it over.
It does **not** name a default rung, does **not** recommend one, and does **not** embed a spend
number the project never stated.

**`FULL_FIDELITY_K` is NOT open for selection.** It is Phase 18's `K = 48`, reserved for
gate-candidate points, because reducing a full-fidelity K after seeing a null is the weakening
`ATK-03` and `P18-4` exist to prevent. State this to the user as a fact of the pre-registration
rather than presenting it as an option.

**Cost of this checkpoint: near zero.** No GPU second is spent before the answer arrives — the whole
task is arithmetic over committed JSON.
  </context>
  <options>
    <option id="per-rung-table">
      <name>Each row of the computed table is an option</name>
      <pros>Every cell is derived live from `results/phase23_cost.json` through `size_sweep`, at both the ceiling and the floor, with the never-taught term priced separately</pros>
      <cons>The options cannot be enumerated in this document: `results/phase23_cost.json` does not exist until 23-11 runs, and writing candidate numbers here would be the invented-figure defect 23-12 exists to close</cons>
    </option>
  </options>
  <resume-signal>Reply with the `CURVE_K` rung (one of 48 / 24 / 16 / 8) and the sweep width. Task 2 pins exactly what is chosen and nothing else.</resume-signal>
  <verify>
    <automated>.venv/bin/python -c "import sys;sys.path.insert(0,'scripts');import json,mitigation_gate as g,phase23_cost as c;r=json.load(open('results/phase23_cost.json'));n=json.load(open('results/phase23_never_taught_training.json'))['n_seeds'];[print(k, c.size_sweep(generation_record=r['generation'], sweep_points=16, k=k)) for k in g.K_RUNGS];print('never_taught_seeds', n)"</automated>
  </verify>
  <acceptance_criteria>
    - The full per-rung table is presented to the user with every cell computed live through `phase23_cost.size_sweep`, at BOTH the ceiling and the floor, with the never-taught term as its own column priced from `results/phase23_never_taught_training.json`'s seed count
    - **The plan named no default rung and made no recommendation.** The SUMMARY states explicitly that no spend bound exists in `23-CONTEXT.md` D-01…D-10, in Claude's Discretion, in `.planning/REQUIREMENTS.md` or in `.planning/ROADMAP.md`, and that none was invented
    - The one-way nature of the choice is stated to the user, quoting `ratchet_k`'s `_prove(proposed_k >= fixed_k)` at `scripts/mitigation_gate.py:917`
    - `FULL_FIDELITY_K = 48` is presented as a RULE (Phase 18 / CAL-04 / `ATK-03` / `P18-4`), not as an option
    - **W10 — Execution STOPPED here, and the selection's persistence across the gate is NAMED.** An earlier draft required the answer "recorded verbatim in the SUMMARY before Task 2 writes a literal", but `<output>` creates the SUMMARY at plan **end** and this task's own last criterion forbids it writing any file — so there was no artifact to record it into. The chain is: (1) the user's reply is carried verbatim in the execution transcript across the blocking gate — that is what a `checkpoint:decision` gate *is*; (2) it becomes **durable** at Task 2 as the `selected_by` / `selected_value` fields of `CURVE_K_PROVENANCE` and `SWEEP_POINTS_PROVENANCE`, which are the **first bytes this plan writes**, so the answer is persisted by the same write that consumes it and cannot be pinned without being recorded; (3) the SUMMARY quotes it verbatim at plan end beside the pinned literals. If the transcript answer is missing or ambiguous when Task 2 begins, **STOP and re-ask** — do not reconstruct it from the table
    - `git diff --exit-code -- scripts/ tests/ results/ .planning/REQUIREMENTS.md` exits 0 — **this task writes nothing at all**
  </acceptance_criteria>
  <done>The per-rung cost table is computed live at the ceiling and the floor, the never-taught floor is priced as its own term, the ratchet's one-way nature is stated, and execution has STOPPED with the user's chosen `CURVE_K` and sweep width recorded — selected by the user, not by the executor, and against no invented bound.</done>
</task>

<task type="auto">
  <name>Task 2: Pin Z with `_PROVENANCE` siblings, literal-only and zero-import</name>
  <files>scripts/mitigation_budget.py</files>
  <read_first>
    - `scripts/mitigation_budget.py` — current state after 23-18. **301 lines, four module-level
      assignments** (`CONTROL_NOISE_FLOOR` `:113`, `CONTROL_NOISE_FLOOR_PROVENANCE` `:135`,
      `MATCHED_CONTROL_NOISE_FLOOR` `:269`, `MATCHED_CONTROL_NOISE_FLOOR_PROVENANCE` `:277`) plus the
      docstring. Read the docstring's zero-import and protected-but-not-frozen justification — it
      may not be displaced, and the literal-only guard asserts the file still OPENS with it
    - `scripts/phase19_floor.py:75-175` — the per-constant layout: banner, derivation comment naming
      the input / the rule / the output, the literal, then the `_PROVENANCE` / `_EVIDENCE_SHA`
      siblings
    - `scripts/mitigation_accountant.py:335` — `GOLDEN_EPSILON_PROVENANCE`. **Read only**
    - `tests/test_phase23_budget.py:308-376` — `test_budget_holds_only_literal_constants`. This is
      the guard the new constants must satisfy, and reading it first is cheaper than tripping it
    - `results/phase23_cost.json` — the source for every figure pinned here, and its sha256
    - `results/phase23_never_taught_training.json` — **the single source for `N_CONTROL_SEEDS`**
      (W12), and its sha256. MEASURED at HEAD: `n_seeds = 5`,
      `seeds = [1337, 2024, 1338, 2025, 1339]`. Task 3 asserts the constant against **this same**
      record; do not pin it from `results/phase23_control_floor.json`
    - `scripts/teach_persona.py:1220` — `MAX_STEPS = 200`, the source of `STEP_BUDGET`. **Cite this
      line, not research's stale `:1128`**
    - `scripts/phase18_extraction.py:88-96` — `K = 48`, the source of `FULL_FIDELITY_K`, and the
      recorded reason a full-fidelity K may not be reduced after seeing a null (`ATK-03`, `P18-4`)
    - **Task 1's checkpoint answer** — the user's selected `CURVE_K` and sweep width. This is an
      INPUT to this task, not something to re-derive
  </read_first>
  <action>
Add Z to `scripts/mitigation_budget.py`, in the same layout as `CONTROL_NOISE_FLOOR`. **Nothing
about the module's shape changes: literal assignments and nothing else, zero imports, no functions,
no `if __name__`. APPEND — the existing four assignments and the docstring stay byte-unchanged.**

Constants, each with a banner block and a `_PROVENANCE` sibling dict:

  - `SWEEP_POINTS` — the number of frontier points per leg. **Exactly the width the user gave at
    Task 1's checkpoint.**
  - `CURVE_K` — the per-point draw budget for curve points. **Exactly the rung the user selected at
    Task 1's checkpoint. Do not re-derive it, do not adjust it, and do not substitute a different
    rung because the arithmetic looks better at one.** The selection was the user's and `ratchet_k`
    makes it one-way; this task's job is to pin what was chosen. If the answer is missing or
    ambiguous, STOP and re-ask rather than choosing.
  - `FULL_FIDELITY_K` — the budget reserved for gate-candidate points. **This one is NOT the user's
    choice and was never presented as an option: it is Phase 18's `K = 48`
    (`scripts/phase18_extraction.py:88-96`), because reducing a full-fidelity K after seeing a null
    is the weakening `ATK-03` and `P18-4` exist to prevent.** Pin `48` with the rule as its
    derivation comment.
  - `STEP_BUDGET` — the per-arm optimizer-step budget: `teach_persona.MAX_STEPS`
    (`scripts/teach_persona.py:1220`, value **200**), restated as a literal with a provenance comment
    naming the symbol. Task 3's `test_the_step_budget_agrees_with_the_production_constant` is the
    other half of the restate-and-assert route; without it the restatement is an unchecked copy.
  - `N_CONTROL_SEEDS` — D-03/D-08's chosen N, because the never-taught floor's scoring cost is part
    of Z and must be priced from a pinned number rather than re-read at call time.
    **W12 — THE SOURCE IS `results/phase23_never_taught_training.json`, AND ONLY THAT.** An earlier
    draft sourced this constant from `results/phase23_control_floor.json`'s seed list here while
    Task 3's `test_the_never_taught_floor_is_priced_in_z` asserted it against
    `results/phase23_never_taught_training.json` — one constant with two cited sources, and neither
    record in this task's `read_first`. The never-taught training record is the binding one: its
    seeds are the adapters 23-14 actually scores, and this constant exists to price **that** scoring.
    MEASURED at HEAD both records carry `seeds = [1337, 2024, 1338, 2025, 1339]` and `n_seeds = 5`,
    so the two agree today — which is exactly why the divergence would have gone unnoticed. Pin from
    the never-taught record, name that record in `_PROVENANCE.record`, and let Task 3 assert against
    the same one. D-08's same-N rule is the REASON the control's list matches; it is not a second
    source.
  - `N64_LEG_WITHDRAWN` plus its provenance sibling, in **both** branches — `False` with the
    confirming record named if D-06 confirmed, `True` if it did not. The negative case is recorded so
    a reader cannot tell "confirmed" from "never checked". **W11 — THE SIBLING IS SPELLED
    `N64_LEG_WITHDRAWN_PROVENANCE` IN BOTH BRANCHES.** An earlier draft named it
    `N64_LEG_WITHDRAWAL_PROVENANCE` here while the acceptance criterion below checks
    `hasattr(b, n + "_PROVENANCE")` over every uppercase name — which resolves to
    `N64_LEG_WITHDRAWN_PROVENANCE`, so the withdrawal branch would have failed the plan's own
    criterion. The universal `<NAME>_PROVENANCE` convention wins; there is no second spelling.

**D-06, RESOLVED HERE FROM A LIVE READ.** (This was in the old Task 1; it moved because Task 1 is
now a checkpoint that writes nothing, and this is the task that writes the constant.) It is a
measurement read, not a decision — nothing about it belongs at a checkpoint.

Read `results/phase23_cal03_wiring.json`'s `verdict` live. **MEASURED at HEAD it is `true`**, so the
n=64 leg is committable and `N64_LEG_WITHDRAWN = False` with a provenance sibling naming the
confirming record, its sha256, both epsilons and both T values. **If the live read disagrees with
that table, the live read wins**: the n=64 leg is not committed, `SWEEP_POINTS` and every Z constant
describe the **n=8 leg only**, and `N64_LEG_WITHDRAWN = True` plus `N64_LEG_WITHDRAWN_PROVENANCE`
record the withdrawing measurement. **Do not withdraw the n=8 leg**, and do not halt: D-06's scope
distinction is explicit, and D-04's halt rule is a different rule for a different failure. Either way
BOTH branches are written and tested — the negative case is recorded too, so a reader cannot tell
"confirmed" from "never checked" by absence.

Every `_PROVENANCE` dict carries at minimum: `record` (`"results/phase23_cost.json"` or the relevant
sibling), `record_sha256`, `git_sha`, `derivation` (the SYMBOL — `"phase23_cost.size_sweep"` — never
the formula), and `governs`.

**W9 — `sized_against` IS SCOPED, NOT UNIVERSAL, BECAUSE ON THREE CONSTANTS IT WOULD BE FALSE.**
An earlier draft required `sized_against == "h_per_point_ceiling"` on **every** new `_PROVENANCE`.
That is a true statement about the ceiling-side budget's multiplicands and a **false** one about the
rest, and a provenance field that lies is worse than one that is absent:

  - **CARRY `sized_against` (required):** `SWEEP_POINTS`, `CURVE_K`, `N_CONTROL_SEEDS`. These are the
    three terms of the ceiling-side total — `SWEEP_POINTS × h_ceiling(CURVE_K)` plus
    `N_CONTROL_SEEDS × h_ceiling(CURVE_K)` — so each was genuinely sized against
    `h_per_point_ceiling`, and each is a place a floor-sized number could hide.
  - **MUST NOT carry it (the field is absent, not empty):** `STEP_BUDGET` (restated from
    `teach_persona.MAX_STEPS`), `FULL_FIDELITY_K` (from `phase18_extraction.K` by the `ATK-03` /
    `P18-4` rule) and `N64_LEG_WITHDRAWN` (from the CAL-03 wiring record's verdict). **No throughput
    figure participates in any of the three.** They carry `derivation` naming their actual source
    symbol instead, which is the honest field.

Task 3's `test_z_was_sized_against_the_ceiling` asserts the field on exactly the first group and
asserts it **absent** on the second, so neither a missing one nor an invented one passes.

Above each constant, a derivation comment in `phase19_floor.py`'s register: the INPUT (the measured
figures with their denominators), the RULE (the symbol), and the OUTPUT. For `CURVE_K` and
`FULL_FIDELITY_K` the comment must additionally name `scripts/mitigation_gate.py:254::K_RUNGS` as
the frozen source of the admissible rungs and state that this module **cannot import it** — the
literal-only guard at `tests/test_phase23_budget.py:308` refuses any node that is not an
`ast.Assign`, and TWO accumulated ceilings bind the same union — `tests/test_phase20_prereg.py:1190`
asserts `imported <= {pathlib, sys, erasure_gate}` (SUBSET) and `tests/test_phase23_budget.py:565`
asserts `imported == {erasure_gate, pathlib, sys}` (EQUALITY, zero headroom in BOTH directions) — so
the value is restated and the agreement is asserted by a test instead.

Run **all three** guards immediately after saving — the third is the EQUALITY ceiling, which the
other two do not reach and which an earlier draft of this plan omitted:

```bash
.venv/bin/python -m pytest tests/test_phase20_prereg.py -k import_graph -q
.venv/bin/python -m pytest "tests/test_phase23_budget.py::test_budget_holds_only_literal_constants" -q
.venv/bin/python -m pytest "tests/test_phase23_budget.py::test_the_import_ceiling_still_has_zero_headroom" -q
```
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase20_prereg.py -k import_graph -q && .venv/bin/python -m pytest tests/test_phase23_budget.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -c "import ast,pathlib; t=ast.parse(pathlib.Path('scripts/mitigation_budget.py').read_text()); print(sorted({type(n).__name__ for n in t.body}))"` prints exactly `['Assign', 'Expr']`
    - `git diff --numstat -- scripts/mitigation_budget.py` shows a deletion count of **0** — the module's whole history is additive and this plan keeps it so
    - **AST, never grep** (`MATCHED_CONTROL_NOISE_FLOOR` contains `CONTROL_NOISE_FLOOR`): `.venv/bin/python -c "import ast,pathlib;t=ast.parse(pathlib.Path('scripts/mitigation_budget.py').read_text());print([g.id for n in t.body if isinstance(n,ast.Assign) for g in n.targets])"` lists the four pre-existing names FIRST and unchanged, followed by the new Z names
    - **ALL THREE guards exit 0** — `.venv/bin/python -m pytest tests/test_phase20_prereg.py -k import_graph -q` (SUBSET ceiling), `.venv/bin/python -m pytest "tests/test_phase23_budget.py::test_budget_holds_only_literal_constants" -q` (literal-only, the binding one for this file) and `.venv/bin/python -m pytest "tests/test_phase23_budget.py::test_the_import_ceiling_still_has_zero_headroom" -q` (EQUALITY ceiling, zero headroom in both directions)
    - `CURVE_K` and `SWEEP_POINTS` equal the user's checkpoint answer **verbatim**, quoted side by side in the SUMMARY with Task 1's recorded reply; `FULL_FIDELITY_K` is **48** with the `ATK-03` / `P18-4` rule as its derivation comment; `STEP_BUDGET` is **200** citing `scripts/teach_persona.py:1220`
    - Every Z constant has a matching `*_PROVENANCE` sibling: `.venv/bin/python -c "import sys;sys.path.insert(0,'scripts');import mitigation_budget as b;ns=[n for n in dir(b) if n.isupper() and not n.endswith('_PROVENANCE')];print([n for n in ns if not hasattr(b, n+'_PROVENANCE')])"` prints `[]`
    - **W9 — `sized_against` is present and equal to `"h_per_point_ceiling"` on exactly `SWEEP_POINTS`, `CURVE_K` and `N_CONTROL_SEEDS` (the three multiplicands of the ceiling-side total), and ABSENT on `STEP_BUDGET`, `FULL_FIDELITY_K` and `N64_LEG_WITHDRAWN`, none of which is derived from any throughput figure.** Requiring it universally would have written a false provenance field on three constants
    - `N64_LEG_WITHDRAWN` exists with a provenance sibling, whichever branch fired
    - The SUMMARY quotes `results/phase23_cal03_wiring.json`'s LIVE `verdict`, both epsilons and both T values, and states which D-06 branch fired and that the n=8 leg stays intact
    - The pinned `CURVE_K` and `SWEEP_POINTS` are **exactly** the values the user selected at Task 1's checkpoint, quoted side by side in the SUMMARY with the checkpoint's recorded answer
    - `git diff --exit-code -- scripts/mitigation_gate.py scripts/mitigation_accountant.py scripts/phase23_prereg.py scripts/phase23_matched_prereg.py scripts/phase23_resume_prereg.py results/ .planning/REQUIREMENTS.md` exits 0
    - `git log --format=%H -- scripts/phase23_matched_prereg.py | wc -l | tr -d ' '` still prints **1**
  </acceptance_criteria>
  <done>Z is pinned as literals with a `_PROVENANCE` sibling per constant, `sized_against == "h_per_point_ceiling"` carried by exactly the three ceiling-side multiplicands (`SWEEP_POINTS`, `CURVE_K`, `N_CONTROL_SEEDS`) and ABSENT on `STEP_BUDGET`, `FULL_FIDELITY_K` and `N64_LEG_WITHDRAWN` where no throughput figure participates and the field would have been false (W9), appended with zero deletions to a module that still holds nothing but literals and imports nothing.</done>
</task>

<task type="auto">
  <name>Task 3: Make every Z constant checkable — re-derivation, the ratchet, and both D-06 branches</name>
  <files>tests/test_phase23_budget.py</files>
  <read_first>
    - `tests/test_phase23_budget.py` — current state after 23-18. In particular
      `test_budget_constants_re_derive` (`:377`), which is EXTENDED not replaced, and
      `test_a_hand_edited_matched_floor_is_detected` (`:790`), the perturbation-control shape
    - `scripts/mitigation_budget.py` — as written in Task 2
    - `results/phase23_cost.json`, `results/phase23_cal03_wiring.json`,
      `results/phase23_never_taught_training.json`, `results/phase23_control_floor.json`
    - `scripts/mitigation_gate.py:917, :963` — `ratchet_k`, `promote_to_full_fidelity`. **Import from
      the TEST, never from the budget module**
    - `scripts/phase23_cost.py:287` — `size_sweep`
  </read_first>
  <action>
Extend `tests/test_phase23_budget.py`:

  - `test_budget_constants_re_derive` (extend the existing test) — for **every** Z constant,
    recompute it from its cited record through the cited derivation symbol and assert exact `==`. A
    constant whose provenance names a record it cannot be recomputed from is a number nobody can
    check. **Do not touch the existing floor cases in that test** — they belong to 23-09 and 23-18.
  - `test_selected_k_is_a_ratcheted_rung` — **this is where the frozen-gate verification lives, and
    the ONLY place it lives.** The old Task 1 listed the same `CURVE_K in K_RUNGS` / `ratchet_k` /
    `promote_to_full_fidelity` assertions in its action while declaring
    `tests/test_phase23_budget.py` as a file it wrote and referencing constants **Task 2** creates —
    so its pytest ran against an unmodified file and its `<done>` claimed work it could not do. Task
    1 is now a checkpoint that writes nothing; these assertions are here. Import `mitigation_gate` in
    the test; assert
    `CURVE_K in K_RUNGS` and `FULL_FIDELITY_K in K_RUNGS`, call `ratchet_k` with the pinned pair and
    assert it does not refuse, and call `promote_to_full_fidelity` with the pinned pair and a
    synthetic verdict and assert it does not refuse. **The frozen constants are imported, never
    retyped.**
  - `test_the_step_budget_agrees_with_the_production_constant` — assert
    `mitigation_budget.STEP_BUDGET == teach_persona.MAX_STEPS`. This is the restate-and-assert
    route's second half; without it the restatement is an unchecked copy.
  - `test_z_was_sized_against_the_ceiling` — assert `sized_against == "h_per_point_ceiling"` on
    exactly `SWEEP_POINTS`, `CURVE_K` and `N_CONTROL_SEEDS`, and assert the field is **absent** on
    `STEP_BUDGET`, `FULL_FIDELITY_K` and `N64_LEG_WITHDRAWN` (W9). Then assert the projected hours
    implied by `SWEEP_POINTS` at `CURVE_K` **recompute from `h_per_point_ceiling`** — identity, by
    exact `==` against the ceiling-derived figure — and that the ceiling-derived total is
    **`>=`** the floor-derived one.

    **W8 — `>` IS REFUTED BY THIS PLAN SET'S OWN PREMISE AND MUST NOT BE ASSERTED.** `23-11`'s
    Task 3 states the reason the ceiling exists: *a noised adapter that stops emitting EOS runs every
    draw to the full 48 tokens.* In that regime **no draw stop-terminates, the floor condition and
    the ceiling condition measure the same thing, and `h_per_point_floor == h_per_point_ceiling`** —
    so a strict `>` fails against a perfectly correct measurement. The discriminating check is the
    **identity** (the total re-derives from the ceiling field, not the floor field), which holds in
    both regimes; `>=` is the sanity conjunct.

    **Equality must be EARNED, not merely tolerated.** When
    `h_per_point_ceiling == h_per_point_floor`, additionally assert the cost record discloses the
    measurement that makes them equal — `stop_terminated_n_ceiling == 0` and the floor condition's
    stop counts equal to the ceiling condition's — so a degenerate bracket has to come from the
    measurement rather than from a copy-paste of one field into the other. Record which branch fired
    in the SUMMARY with both figures quoted.
  - `test_n64_leg_matches_the_cal03_verdict` — parametrized over both branches, driven from the
    committed `results/phase23_cal03_wiring.json`:
      (i) if its verdict is falsified, assert `N64_LEG_WITHDRAWN is True`, assert the withdrawal
          provenance names the record and its live sha256, and assert **no Z constant describes an
          n=64 leg**;
      (ii) if its verdict is confirmed, assert `N64_LEG_WITHDRAWN is False` and that its provenance
          names the confirming record and its live sha256. **Both branches are asserted**, so
          "confirmed" and "never checked" are distinguishable. Drive the inactive branch from a
          CONSTRUCTED copy of the real record with the verdict flipped, never by editing the
          committed artifact.
  - `test_the_never_taught_floor_is_priced_in_z` — assert `N_CONTROL_SEEDS` equals the distinct-seed
    count in `results/phase23_never_taught_training.json`, and that the never-taught term
    `N_CONTROL_SEEDS x h_per_point_ceiling_at_k` recomputes and appears in
    `results/phase23_cost.json`'s `sizing` block at every rung.

**Watched RED, permanent.** Add `test_a_z_constant_that_does_not_re_derive_is_detected(tmp_path)`:
copy `scripts/mitigation_budget.py`, perturb `SWEEP_POINTS` by one, load the copy through
`importlib.util.spec_from_file_location`, and assert the re-derivation comparison fails against it.
Same shape as 23-18's one-ULP matched-floor case, for the same reason.

**AST, not grep, in every structural assertion here.** `MATCHED_CONTROL_NOISE_FLOOR` contains
`CONTROL_NOISE_FLOOR`; this test file's own docstrings will contain `h_per_point_ceiling`,
`K_RUNGS` and `N64_LEG_WITHDRAWN`. A grep criterion over either file matches its own prose.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase23_budget.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_phase23_budget.py -v` exits 0 with zero skips
    - `.venv/bin/python -m pytest "tests/test_phase23_budget.py::test_selected_k_is_a_ratcheted_rung" -v` exits 0 and the test IMPORTS `mitigation_gate` — verify by AST over the test module's import nodes, not by grep
    - `.venv/bin/python -m pytest "tests/test_phase23_budget.py::test_z_was_sized_against_the_ceiling" "tests/test_phase23_budget.py::test_the_step_budget_agrees_with_the_production_constant" "tests/test_phase23_budget.py::test_the_never_taught_floor_is_priced_in_z" -v` exits 0
    - `.venv/bin/python -m pytest "tests/test_phase23_budget.py::test_n64_leg_matches_the_cal03_verdict" -v` collects **2** cases and both pass — both D-06 branches asserted, the inactive one from a constructed copy
    - `.venv/bin/python -m pytest "tests/test_phase23_budget.py::test_a_z_constant_that_does_not_re_derive_is_detected" -v` exits 0
    - The 23-09 / 23-18 floor cases in `test_budget_constants_re_derive` still pass and were EXTENDED, not rewritten: `git diff --numstat -- tests/test_phase23_budget.py` deletions are accounted for by name in the SUMMARY
    - `.venv/bin/python -m pytest tests/ -q` reports 0 failed; `make lint` exits 0
    - `git diff --exit-code -- scripts/mitigation_gate.py scripts/mitigation_accountant.py scripts/phase23_prereg.py scripts/phase23_matched_prereg.py scripts/phase23_resume_prereg.py scripts/phase18_extraction.py results/ .planning/REQUIREMENTS.md pyproject.toml` exits 0
  </acceptance_criteria>
  <done>Every Z constant re-derives from its cited record, the rungs are checked against the frozen gate by import, the ceiling-sizing is proven by re-derivation identity against the ceiling field plus `>=` against the floor-derived total (W8 — `>` is refuted by the plan set's own premise that a noised adapter running every draw to 48 tokens makes floor == ceiling), both D-06 branches are asserted with the inactive one driven from a constructed copy, and a perturbed constant is observed being caught.</done>
</task>

<task type="auto">
  <name>Task 4: Tick CAL-02 — the requirement this plan closes</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/ROADMAP.md:562` — `**Requirements**: CAL-01, CAL-02, CAL-03, CAL-05, DPSGD-06,
      CTRL-03`. **These six are the phase's contract.** MEASURED at HEAD: CAL-03 `[x]` (23-04),
      DPSGD-06 `[x]` (23-10), and CAL-01 / CAL-02 / CAL-05 / CTRL-03 all `- [ ]`
    - `.planning/REQUIREMENTS.md` — the `- [ ] **CAL-02**` checkbox row and the `| CAL-02 | Phase 23 |`
      traceability row. **Locate both by TEXT, never by line number**
    - `.planning/REQUIREMENTS.md` — a SATISFIED row already written in this phase's convention, e.g.
      the `| CAL-03 | Phase 23 | **SATISFIED (plan 23-04).** …` row and the `| DPSGD-06 | …` row.
      **Read one in full**; the tick is the checkbox AND the traceability cell, in that register
    - `.planning/phases/23-*/23-12-SUMMARY.md` — how 23-12 ticked CAL-01 and CAL-05 one wave earlier.
      Match its shape so the four ticks read as one convention
    - Task 1's recorded checkpoint answer, and Tasks 2 and 3's outputs — the evidence the tick cites
  </read_first>
  <action>
**WHY THIS TASK EXISTS.** CAL-02 is one of `ROADMAP.md:562`'s six requirements for this phase, and
**no other plan in the phase owns it**: 23-12 ticks CAL-01 and CAL-05 only, and 23-14 owns CTRL-03.
An earlier draft of this plan asserted `git diff --exit-code -- .planning/REQUIREMENTS.md` in every
task and stated in its success criteria that the file is byte-unchanged — which **forbade** the tick
outright. These are the phase's last four plans; CAL-02 would have finished the phase unticked with
nothing downstream able to close it.

Tick `- [ ] **CAL-02**` to `- [x]`, and fill the `| CAL-02 | Phase 23 | |` traceability cell with a
SATISFIED note in this table's existing convention, naming:

  - the plan (**23-13**) and the artifact (`scripts/mitigation_budget.py`), with the constants pinned;
  - that the separation CAL-02 requires is enforced by AST rather than by convention, and by **two**
    ceilings — the SUBSET guard at `tests/test_phase20_prereg.py:1190` and the EQUALITY guard at
    `tests/test_phase23_budget.py:565` — plus the literal-only guard at
    `tests/test_phase23_budget.py:308`;
  - that every constant re-derives from `results/phase23_cost.json` through
    `phase23_cost.size_sweep` under exact `==`, with a perturbation control watched catching a change;
  - that Z was sized against `h_per_point_ceiling` — proven by re-derivation identity against the
    ceiling field, with `>=` against the floor-derived total and the degenerate floor == ceiling case
    disclosed if it fired — because the ratchet has no cheap direction; and that `sized_against` is
    carried by the three ceiling-side multiplicands (`SWEEP_POINTS`, `CURVE_K`, `N_CONTROL_SEEDS`)
    and deliberately ABSENT on `STEP_BUDGET`, `FULL_FIDELITY_K` and `N64_LEG_WITHDRAWN`, where no
    throughput figure participates and the field would have been false;
  - **that the `CURVE_K` rung was selected BY THE USER at Task 1's checkpoint**, with the presented
    table's ceiling total quoted. CAL-02 says Z is set *from* the measurements; the honest record is
    that the measurement produced the table and the user picked the rung.

**DO NOT WRITE THE LITERAL STRING `CAL-03` ANYWHERE IN THE CAL-02 NOTE.** CAL-03 was closed by 23-04
and re-ticking it here is the defect T-23-74b names. The no-re-tick guard below counts changed diff
lines matching `CAL-03`, and it must stay at 0 — so cite 23-04's evidence as *"the wiring record
`results/phase23_cal03_wiring.json`"* rather than by requirement id.

**Hand-edit with `Edit`. NO `gsd-sdk` mutation handler may touch this file** — 23-19 recorded that
`roadmap.update-plan-progress` keys on SUMMARY EXISTENCE and falsely ticked 23-17. If one is used
anyway, `git diff .planning/REQUIREMENTS.md` immediately and hand-repair.

**This is the ONLY task in this plan that may modify `.planning/REQUIREMENTS.md`.** Tasks 1, 2 and 3
keep their strict `git diff --exit-code` on the file, unchanged. Scoping the write to one final task
is why the freeze stays absolute everywhere else instead of being loosened plan-wide.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase23_budget.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^- \[x\] \*\*CAL-02\*\*" .planning/REQUIREMENTS.md | tr -d ' '` returns **1**
    - The traceability row is filled: `.venv/bin/python -c "import pathlib;print([l for l in pathlib.Path('.planning/REQUIREMENTS.md').read_text().splitlines() if l.startswith('| CAL-02 |')][0][:120])"` shows a non-empty third cell containing `SATISFIED (plan 23-13)`
    - **B-1 — THE DIFF FILTER IS `^[<>]` ON SWAPPED INDICATORS, NEVER `^[+-][^+-]`.** An earlier draft filtered changed lines with `grep -E '^[+-][^+-]'`, which requires the SECOND character not to be `+`/`-`. **Every requirement checkbox is a markdown bullet `- `**, so its diff lines are `-- [ ] **CAL-02**…` and `+- [x] **CAL-02**…` — and both are dropped as if they were `---`/`+++` file headers. REPRODUCED on a scratch repo: with the intended CAL-02 tick PLUS an illicit CAL-03 checkbox rewrite, the old filter surfaced only the traceability rows and **both guards printed `0` (PASS)**. (`--output-indicator-old` / `--output-indicator-new` need git >= 2.22; MEASURED here: 2.50.1. An older git rejects the flag and the guard fails LOUDLY rather than silently passing.) Swapping the diff's own line indicators is unambiguous — the `--- a/…` / `+++ b/…` headers keep their `-`/`+` while changed lines become `<`/`>`, so no content line can be mistaken for a header (a prefix filter such as `grep -vE '^(\+\+\+ |--- )'` still drops a genuine deleted line beginning `-- `, MEASURED reporting 1 where 2 was correct). **W4 — `grep -c` printing `0` EXITS 1**, so both guards are wrapped in a string comparison rather than read by exit status. VERIFIED: on the scratch violation the two commands below return `2` and `2`; on a clean CAL-02-only tick they return `0` and `0`.
    - **THE NARROW FREEZE — every changed line in this file names CAL-02:**
      ```bash
      [ "$(git diff -U0 --output-indicator-old='<' --output-indicator-new='>' -- .planning/REQUIREMENTS.md | grep -E '^[<>]' | grep -cv 'CAL-02')" = 0 ]
      ```
      exits 0. (This filters a DIFF, whose lines are only the changed ones — not a source file whose prose would match itself.)
    - **THE CAL-03 NO-RE-TICK GUARD, KEPT — and now able to SEE the checkbox row that T-23-74b names:**
      ```bash
      [ "$(git diff -U0 --output-indicator-old='<' --output-indicator-new='>' -- .planning/REQUIREMENTS.md | grep -E '^[<>]' | grep -c 'CAL-03')" = 0 ]
      ```
      exits 0 — CAL-03 was closed by 23-04 and this plan does not touch its row. Under the old filter this guard could not see a CAL-03 checkbox rewrite at all
    - **THE ALL-SIX STATE BACKSTOP — the diff guards prove nothing was *changed*; this proves the resulting STATE is right.** A diff-only guard cannot catch an un-tick that happens to mention CAL-02, so assert the whole table's state directly. At this plan's point in the phase (23-12 has ticked CAL-01/CAL-05; 23-14 has not yet ticked CTRL-03) the expected state is exactly five ticked and CTRL-03 open: `.venv/bin/python -c "import pathlib,re;t=pathlib.Path('.planning/REQUIREMENTS.md').read_text();print({r:bool(re.search(r'^- \[x\] \*\*'+r+r'\*\*',t,re.M)) for r in ('CAL-01','CAL-02','CAL-03','CAL-05','DPSGD-06','CTRL-03')})"` prints `True` for `CAL-01`, `CAL-02`, `CAL-03`, `CAL-05` and `DPSGD-06`, and `False` for `CTRL-03`. This is the backstop `23-14` already carries at its own point in the sequence; 23-13 asserted only CAL-02 and could not see an un-tick of a row it never mentions
    - All six of `ROADMAP.md:562`'s requirements are now accounted for, quoted in the SUMMARY: CAL-01 / CAL-05 (23-12), CAL-02 (this plan), CAL-03 (23-04), DPSGD-06 (23-10), CTRL-03 (23-14, next wave)
    - No `gsd-sdk` mutation handler touched the file; the SUMMARY says so explicitly
    - `git diff --exit-code -- scripts/ tests/ results/ .planning/ROADMAP.md .planning/STATE.md pyproject.toml` exits 0 — this task edits ONE file
  </acceptance_criteria>
  <done>CAL-02 is ticked with a SATISFIED note naming the artifact, both import ceilings, the re-derivation, the ceiling-sizing inequality and the user's checkpoint selection — and the ONLY line changed in `.planning/REQUIREMENTS.md` is the CAL-02 row, with the CAL-03 no-re-tick guard still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| resource budget → outcome gate | CAL-02's whole point; enforced statically since Phase 20 and transitively since 23-02. |
| measured cost → pinned Z | Z drives a sweep the ratchet forbids shrinking. A number sized on the wrong field is unrecoverable in the direction that matters. |
| an executor → an irreversible spend decision | `ratchet_k` is one-way and the rungs span 5.4x in draws/point. No spend bound exists in ANY source artifact, so there is no criterion an executor could select against. The selection crosses to the user at Task 1's checkpoint. |
| frozen `K_RUNGS` → a restated literal | The budget module cannot import the gate, so the rung is a copy, and a copy is free to stop matching. |
| CAL-03's verdict → the n=64 leg | A committed n=64 leg on a falsified premise publishes an ε for a composition the wiring does not produce. |
| a substring name → a structural criterion | `MATCHED_CONTROL_NOISE_FLOOR` contains `CONTROL_NOISE_FLOOR`; a grep count measures the wrong thing and reports a pass. |
| an existing pinned floor → an appended constant | Two floors already live in this module. An edit that touches either destroys 23-18's byte-unchanged guarantee. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-69b | Elevation of Privilege | an autonomous executor picking the irreversible K rung against a bound no artifact supplies | mitigate | Task 1 is `checkpoint:decision`, `gate="blocking"`, and the plan is `autonomous: false`. It computes and PRESENTS the per-rung table and selects nothing; `workflow.auto_advance` is `false` in `.planning/config.json`, so the stop is real. The plan names no default rung and embeds no spend number |
| T-23-69 | Tampering | Z sized against the floor and unrescuable by the ratchet | mitigate | `sized_against` required on the three ceiling-side multiplicands and asserted ABSENT on the three constants no throughput figure feeds (W9 — a universal requirement would have written a false provenance field on `STEP_BUDGET`, `FULL_FIDELITY_K` and `N64_LEG_WITHDRAWN`); `test_z_was_sized_against_the_ceiling` asserts the total RE-DERIVES from `h_per_point_ceiling` under exact `==` — the check that discriminates in both regimes — plus `>=` against the floor-derived figure. W8: a strict `>` is refuted by this plan set's own premise, since a noised adapter that stops emitting EOS runs every draw to 48 tokens and makes floor == ceiling; equality must instead be EARNED by a disclosed `stop_terminated_n_ceiling == 0` |
| T-23-70 | Spoofing | a restated `K_RUNGS` value that drifted from the frozen gate | mitigate | `test_selected_k_is_a_ratcheted_rung` imports the gate and calls `ratchet_k` and `promote_to_full_fidelity` with the pinned pair |
| T-23-71 | Repudiation | a hand-written Z constant with no derivation | mitigate | Every constant re-derives from its cited record through the cited symbol under exact `==`, with a permanent perturbation control |
| T-23-72 | Elevation of Privilege | an n=64 leg committed on a falsified CAL-03 premise | mitigate | `test_n64_leg_matches_the_cal03_verdict` drives both branches from the committed wiring record, the inactive one from a constructed copy; the negative case is recorded explicitly so absence is not ambiguous |
| T-23-73 | Tampering | an import or a non-literal node reaching the budget module | mitigate | `test_budget_holds_only_literal_constants` refuses any non-`Assign` node (this is the binding guard); BOTH accumulated ceilings are run after every edit — the SUBSET one at `test_phase20_prereg.py:1190` and the EQUALITY one at `test_phase23_budget.py:565`, which has zero headroom in both directions and which an earlier draft omitted |
| T-23-73b | Tampering | a deletion breaking the module's all-additive history | mitigate | `git diff --numstat` deletion count asserted 0; the four pre-existing assignment names asserted present and first, by AST |
| T-23-73c | Spoofing | a grep criterion counting `MATCHED_CONTROL_NOISE_FLOOR` as `CONTROL_NOISE_FLOOR` | mitigate | Every structural criterion is an AST walk over module-level `Assign` target names |
| T-23-74 | Repudiation | a budget that forgets the N-seed never-taught floor | mitigate | `N_CONTROL_SEEDS` pinned with its provenance and `test_the_never_taught_floor_is_priced_in_z` recomputing the term at every rung |
| T-23-74b | Repudiation | CAL-03 re-ticked by a plan that did not close it | mitigate | `git diff --exit-code -- .planning/REQUIREMENTS.md` asserted in Tasks 1-3; Task 4 (the CAL-02 tick) replaces it with a narrow diff guard — every changed line names `CAL-02` and NO changed line matches `CAL-03` — and the action forbids writing the string `CAL-03` in the note. The environment block states 23-04 closed it |
| T-23-74c | Repudiation | CAL-02 left unticked because the plan's own freeze forbade the tick | mitigate | Task 4 exists and owns the file; the freeze is scoped to Tasks 1-3 rather than applied plan-wide. All six of `ROADMAP.md:562`'s requirements are enumerated with their owning plan in Task 4's acceptance criteria |
| T-23-SC | Tampering | npm/pip/cargo installs | accept | **Zero installs.** The module imports nothing at all |
</threat_model>

<verification>
```bash
.venv/bin/python -m pytest tests/test_phase20_prereg.py -k import_graph -q
.venv/bin/python -m pytest tests/test_phase23_budget.py -v
.venv/bin/python -c "import ast,pathlib; t=ast.parse(pathlib.Path('scripts/mitigation_budget.py').read_text()); print(sorted({type(n).__name__ for n in t.body})); print([g.id for n in t.body if isinstance(n,ast.Assign) for g in n.targets])"
git diff --numstat -- scripts/mitigation_budget.py
.venv/bin/python -c "import sys;sys.path.insert(0,'scripts');import mitigation_gate as g;print(g.K_RUNGS)"
.venv/bin/python -c "import json;d=json.load(open('results/phase23_cal03_wiring.json'));print(d['verdict'], d['epsilon_n8'], d['epsilon_n64'], d['t_n8'], d['t_n64'])"
git log --format=%H -- scripts/phase23_matched_prereg.py | wc -l | tr -d ' '
git diff --exit-code -- scripts/mitigation_gate.py scripts/mitigation_accountant.py scripts/phase23_prereg.py scripts/phase23_matched_prereg.py scripts/phase23_resume_prereg.py scripts/phase18_extraction.py results/ pyproject.toml
# .planning/REQUIREMENTS.md is NOT in the line above: Task 4 legitimately writes the CAL-02 row.
# Its narrow guards, run after Task 4. B-1: the filter is `^[<>]` on SWAPPED diff indicators —
# `^[+-][^+-]` drops every `- [ ] **CAL-0X**` checkbox row as if it were a `---`/`+++` header, and
# was MEASURED returning 0 (PASS) against an illicit CAL-03 re-tick. W4: `grep -c` printing 0 EXITS
# 1, so both are wrapped in a string comparison instead of read by exit status. Both must exit 0:
[ "$(git diff -U0 --output-indicator-old='<' --output-indicator-new='>' -- .planning/REQUIREMENTS.md | grep -E '^[<>]' | grep -cv 'CAL-02')" = 0 ]
[ "$(git diff -U0 --output-indicator-old='<' --output-indicator-new='>' -- .planning/REQUIREMENTS.md | grep -E '^[<>]' | grep -c 'CAL-03')" = 0 ]
# The state backstop — five ticked, CTRL-03 still open (23-14 closes it next wave):
.venv/bin/python -c "import pathlib,re;t=pathlib.Path('.planning/REQUIREMENTS.md').read_text();print({r:bool(re.search(r'^- \[x\] \*\*'+r+r'\*\*',t,re.M)) for r in ('CAL-01','CAL-02','CAL-03','CAL-05','DPSGD-06','CTRL-03')})"
.venv/bin/python -m pytest tests/ -q
make lint
```
</verification>

<success_criteria>
- Z is committed in `scripts/mitigation_budget.py` with a `_PROVENANCE` sibling per constant naming the cost artifact and its sha256
- The module stays literal-only and zero-import, and the edit is ADDITIVE — deletion count 0, both pre-existing floors byte-unchanged
- BOTH import ceilings pass and the SUMMARY states both at their true strength: the SUBSET guard (`test_phase20_prereg.py:1190`) and the EQUALITY guard (`test_phase23_budget.py:565`), the latter with zero headroom in both directions
- Every structural criterion is an AST gate, because `MATCHED_CONTROL_NOISE_FLOOR` contains `CONTROL_NOISE_FLOOR`
- Every constant re-derives from its cited record through the cited symbol, with a perturbation control observed catching a change
- `CURVE_K` and `FULL_FIDELITY_K` are members of the frozen `K_RUNGS` and accepted by `ratchet_k` and `promote_to_full_fidelity`, checked by import
- Z is proven sized against `h_per_point_ceiling` by re-derivation identity against the ceiling field, plus `>=` against the floor-derived total, with the degenerate floor == ceiling case earned by a disclosed stop rate rather than assumed away (W8); `sized_against` is carried by exactly the three ceiling-side multiplicands and absent where it would be false (W9)
- D-06's branch is resolved from a LIVE read and BOTH outcomes are recorded, so absence is never ambiguous
- The never-taught floor's N-seed scoring is priced into the budget at every rung
- CAL-03 is not re-ticked, and no changed line in `.planning/REQUIREMENTS.md` matches `CAL-03`
- **CAL-02 IS TICKED** (Task 4), with a SATISFIED note in the table's convention, and the ONLY lines changed in `.planning/REQUIREMENTS.md` are the CAL-02 checkbox and its traceability row — Tasks 1-3 leave the file byte-unchanged
- Task 1 STOPPED for the user and selected nothing: no default rung, no recommendation, and no spend bound that no source artifact supplies
</success_criteria>

<output>
Create `.planning/phases/23-cost-calibration-the-0-diagnostic-and-budget-pre-registratio/23-13-SUMMARY.md` when done.
</output>
