---
phase: 23-cost-calibration-the-0-diagnostic-and-budget-pre-registratio
plan: 18
type: execute
wave: 4
depends_on: ["23-17"]
gap_closure: true
files_modified:
  - scripts/mitigation_budget.py
  - tests/test_phase23_budget.py
autonomous: true
requirements: [DPSGD-06]

must_haves:
  truths:
    - "`scripts/mitigation_budget.py` is PROTECTED-BUT-NOT-FROZEN BY DESIGN (23-09), specifically so the floor can be re-pinned. `test_the_budget_module_is_protected_but_not_frozen` reddens if a phase freezes it by accident, and this plan is the first to use the middle ground that test exists to preserve."
    - "The old `CONTROL_NOISE_FLOOR = 0.05357142857142849` is left BYTE-UNCHANGED and standing. It is not wrong; it correctly describes the OLD protocol. A DATED ADDITIVE CONTINUATION is appended naming what re-scoped it, in the established retract-in-place shape — the same discipline used at `src/personacore/privacy/dpsgd.py` on 2026-08-27 (commit `8c2eea2`, 29 insertions / 0 deletions) and at `.planning/REQUIREMENTS.md`'s UNIT-04 rows."
    - "The NEW floor re-derives on EVERY SUITE RUN from `results/phase23_matched_control.json`'s own per-seed counts, through `phase23_prereg.noise_floor` — the BLINDLY-COMMITTED reduction, CALLED — under exact `==`. A hand-edited number goes red, including a one-ULP nudge, and that refusal is WATCHED rather than asserted."
    - "THE IMPORT CEILING HAS ZERO HEADROOM, MEASURED. The allow-set across every `scripts/mitigation_*.py` is EXACTLY `{pathlib, sys, erasure_gate}` and `test_the_import_ceiling_still_has_zero_headroom` asserts EQUALITY. One `import json` here turns a committed guard RED, and so does `math`, and so does `dataclasses`. The new constants are LITERAL ASSIGNMENTS ONLY."
    - "The new provenance dict carries all eight `phase23_prereg.FLOOR_PROVENANCE_KEYS` because 23-19 passes it straight into `sigma_zero_verdict`, which REFUSES a floor whose artifact, commit, device, seeds, reduction or scope is unstated."
    - "This pin lands as its OWN commit, BEFORE the re-test runs — the same ordering 23-09 held before 23-10. The ordering buys less here than it did then (the σ=0 reading already exists and 23-15 discloses that), and the plan says so rather than implying a blindness it does not have."
  artifacts:
    - path: "scripts/mitigation_budget.py"
      provides: "MATCHED_CONTROL_NOISE_FLOOR + its eight-key provenance sibling, and a dated continuation re-scoping the original pin to the OLD protocol"
      contains: "MATCHED_CONTROL_NOISE_FLOOR"
    - path: "tests/test_phase23_budget.py"
      provides: "the new floor's re-derivation under exact ==, the watched one-ULP refusal, and the unchanged zero-headroom ceiling"
      contains: "def test_matched_floor_re_derives"
  key_links:
    - from: "scripts/mitigation_budget.py::MATCHED_CONTROL_NOISE_FLOOR"
      to: "results/phase23_matched_control.json"
      via: "recomputed by CALLING phase23_prereg.noise_floor on the record's own per-seed counts, never read back as a pre-reduced scalar"
      pattern: "phase23_matched_control"
    - from: "scripts/mitigation_budget.py"
      to: "tests/test_phase20_prereg.py::test_mitigation_gate_import_graph_is_stdlib_and_erasure_gate_only"
      via: "the zero-import ceiling; the new constants add no import and the equality guard stays green"
      pattern: "erasure_gate"
---

<objective>
Pin the protocol-matched noise floor beside the original, without editing the original and without
touching the import ceiling.

**Why this is a new constant and not an edit.** `CONTROL_NOISE_FLOOR = 0.05357142857142849` is a
CORRECT measurement of the OLD control protocol — 8 random windows per step from a 51.94%-replay bin,
teaching loss weight `0.4342`, `grad_clip = 1.0` binding on 19 of 25 steps. It is not falsified; it
is RE-SCOPED. Editing it in place would destroy a true reading and would break
`test_budget_constants_re_derive`, which recomputes it from `results/phase23_control_floor.json`.
So the original stays byte-unchanged with a dated continuation naming what re-scoped it, and the new
floor lands beside it — the repository's retract-in-place register, applied unchanged.

**Why this file may be edited at all.** 23-09 deliberately did NOT register
`scripts/mitigation_budget.py` as a `prereg_artifact=`. Being named `mitigation_*.py` makes it
PROTECTED (it joins `_GATE_MODULES`, so the import ceiling and the rule/emission split police it) and
confers NO freeze; only a hand-written explicit path reaching `_assert_ordering_holds` confers one.
`test_the_budget_module_is_protected_but_not_frozen` makes that a checked property. This plan is the
first consumer of that middle ground and must leave it intact.

Purpose: give 23-19 a floor that describes the comparator it will judge against, pinned as its own
reviewable commit.
Output: two new literal constants in `scripts/mitigation_budget.py` plus their guards.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-cost-calibration-the-0-diagnostic-and-budget-pre-registratio/23-17-SUMMARY.md
@scripts/mitigation_budget.py
@CLAUDE.md

<interfaces>
<!-- Resolved from source. -->

scripts/mitigation_budget.py TODAY — the WHOLE file is a docstring plus two assignments:
  CONTROL_NOISE_FLOOR = 0.05357142857142849
  CONTROL_NOISE_FLOOR_PROVENANCE = { record, record_sha256, record_file_sha256, git_sha, device,
                                     torch_version, seeds (a TUPLE), reduction, governs }
  ZERO imports. The docstring's "WHAT THIS FILE IS" block names the floor as "ONE number"; the
  continuation this plan appends is what stops that sentence going stale.

tests/test_phase23_budget.py — the seven guards this plan must keep green and extend:
  :161 test_gate_does_not_transitively_load_the_budget
  :191 test_the_transitive_probe_detects_a_module_that_does_load_the_budget
  :275 test_budget_holds_only_literal_constants        # docstring head + literal Assigns only
  :344 test_budget_constants_re_derive                 # CALLS phase23_prereg.noise_floor
  :440 test_the_budget_module_is_protected_but_not_frozen
  :498 test_the_import_ceiling_still_has_zero_headroom # asserts EQUALITY == {erasure_gate,pathlib,sys}
  :534 test_a_hand_edited_floor_is_detected(tmp_path)  # WATCHED one-ULP refusal under tmp_path

scripts/phase23_prereg.py (EDIT-ONCE, DO NOT TOUCH):
  noise_floor(readings) -> max - min
  FLOOR_PROVENANCE_KEYS = ("record","record_sha256","git_sha","device","torch_version","seeds",
                           "reduction","governs")

scripts/phase23_matched_prereg.py (23-15):
  MATCHED_CONTROL_RECORD = "results/phase23_matched_control.json"

The `record_sha256` in the provenance is the record's OWN inputs-digest field
(sha256 over json.dumps(per_seed, sort_keys=True, default=str)); `record_file_sha256` is the sha256
of the committed record's BYTES, checked live against `read_bytes()`. They are different things and
the existing dict already carries both — mirror that.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append the matched floor and its dated continuation</name>
  <files>scripts/mitigation_budget.py</files>
  <read_first>
    - scripts/mitigation_budget.py in FULL — the whole file is 149 lines. Note the shape of the
      existing comment block above `CONTROL_NOISE_FLOOR` (input / rule / output / evidence, then
      "WHAT IT GOVERNS, AND WHAT IT DOES NOT", then "THE ORDERING IS A FACT ABOUT GIT"), and the
      provenance dict's nine keys.
    - src/personacore/privacy/dpsgd.py — locate the 2026-08-27 dated continuation (commit `8c2eea2`,
      29 insertions / 0 deletions) and copy its shape: original text kept VERBATIM, a dated block
      APPENDED below it, nothing deleted.
    - results/phase23_matched_control.json — the source of every number and digest pinned here.
    - tests/test_phase23_budget.py lines 275-344 — the literal-only walk, so the new assignments are
      written in a shape it already admits.
  </read_first>
  <action>
Edit `scripts/mitigation_budget.py` ONLY. **NO IMPORTS.** Literal assignments and comments only — no
function, no `__main__`, no expression. The file's import ceiling has ZERO measured headroom and one
`import json` reddens a committed guard.

(a) APPEND a dated continuation immediately BELOW the existing `CONTROL_NOISE_FLOOR_PROVENANCE`
    dict — as a comment block, since only literal assignments may exist here. Leave every existing
    line, including the module docstring, BYTE-UNCHANGED. The continuation must open with the
    established marker `# RE-SCOPED IN PLACE 2026-08-27 (plan 23-18).` and state:
      * the constant above is NOT falsified — it correctly measures the OLD control protocol;
      * what re-scoped it: `.planning/debug/sigma-zero-beats-control.md` attributed the D-04 HALT to
        branch (A) INVALID COMPARATOR and FALSIFIED branch (B) (72/72 LoRA tensors agree with an
        ordinary grad-accum reference to `2.178e-07` relative at σ=0 with non-binding `C=1e6`);
      * the three mechanisms that made the old control a different protocol, with their measured
        magnitudes: lot volume 65 vs 8 windows (teaching-token exposure 1,689,600 vs 196,867 =
        8.58x); teaching loss weight 1.0 vs `p = 2719/6262 = 0.4342` = 2.30x; `grad_clip = 1.0`
        applied to the control and STRUCTURALLY ABSENT from the DP arm (`loop.py:220-228` gates on
        `dp_fn is None`), measured binding on 19 of 25 control steps at mean shrink `0.8071`;
      * that `CONTROL_NOISE_FLOOR` therefore governs the OLD protocol and
        `MATCHED_CONTROL_NOISE_FLOOR` below governs the corrected comparator, and which one 23-19
        consumes;
      * that it is left standing because deleting a true measurement to make a later one look tidy
        is the opposite of the discipline this file exists to hold, and because
        `test_budget_constants_re_derive` still recomputes it from
        `results/phase23_control_floor.json` on every suite run.
    Also append, at the END of the module docstring's "WHAT THIS FILE IS" paragraph, a dated
    one-line continuation noting that the file now holds TWO floors — because that paragraph
    currently says "Today that is ONE number" and a stale sentence in a pre-registration-adjacent
    file is exactly the defect 22-19 corrected. **The rest of the docstring stays byte-unchanged.**

(b) Add `MATCHED_CONTROL_NOISE_FLOOR = <the float from results/phase23_matched_control.json's
    `floor` field, transcribed EXACTLY, full repr>` under its own `=====` section header, with the
    same comment block shape the original uses:
      * `input`  : the five per-seed taught-recall-ON readings, each as `k/n` with its denominator
        and its seed, in the recorded seed order, and the total scored draws, device `mps`,
        torch version — all read off the record, none retyped from memory;
      * `rule`   : `phase23_prereg.noise_floor`, committed BLIND in 23-03 at `c7de5d4` and CALLED by
        23-17's writer rather than re-implemented, which is why the record's `reduction` field names
        the symbol instead of restating the formula;
      * `output` : `max_reading - min_reading` written out with both operands;
      * `evidence`: `results/phase23_matched_control.json`;
      * a `WHAT IT GOVERNS, AND WHAT IT DOES NOT` paragraph mirroring the original's: the TAUGHT
        RECALL RATE WITH THE ADAPTER ON and nothing else; every other reading in that record is
        secondary, carries its own denominator, and was NOT reduced;
      * a `WHAT ORDERING THIS PIN DOES AND DOES NOT BUY` paragraph, and it must be HONEST: unlike
        the original, this pin lands with the σ=0 reading `0.7837301587301587` ALREADY COMMITTED at
        `results/phase23_sigma_zero.json`. What is still blind is the reduction, the central-reading
        rule (`control_readings[0]`), the verdict function and the seed ladder — all pinned at
        `c7de5d4` — and the comparator's PROTOCOL, pinned at `scripts/phase23_matched_prereg.py`
        while `git ls-files 'results/phase23_matched_*'` was empty. What is NOT blind is that the
        protocol was chosen knowing the σ=0 number, and `phase23_matched_prereg`'s
        `SIGMA_ZERO_VISIBILITY_DISCLOSURE` plus its ONE-ATTEMPT rule are the bounds on that.

(c) Add `MATCHED_CONTROL_NOISE_FLOOR_PROVENANCE = { ... }` mirroring
    `CONTROL_NOISE_FLOOR_PROVENANCE`'s exact shape and key set — `record`, `record_sha256`,
    `record_file_sha256`, `git_sha`, `device`, `torch_version`, `seeds` (a TUPLE), `reduction`,
    `governs` — every value transcribed from `results/phase23_matched_control.json` and from
    `sha256` of its committed bytes. `governs` must be the record's own `governs` string verbatim,
    because Task 2 asserts the two are equal. Add two matched-specific keys beside them:
    `protocol` naming `"phase23_matched_prereg"` and `sigma_zero_was_visible: True`.

Do not touch any other file in this task.
  </action>
  <verify>
    <automated>.venv/bin/python -c "
import ast, sys, json, hashlib, pathlib
sys.path.insert(0,'scripts'); sys.path.insert(0,'tests')
src = pathlib.Path('scripts/mitigation_budget.py').read_text()
tree = ast.parse(src)
assert not [n for n in ast.walk(tree) if isinstance(n,(ast.Import, ast.ImportFrom))], 'IMPORT ADDED'
assert not [n for n in tree.body if not isinstance(n,(ast.Assign, ast.Expr))], 'NON-ASSIGN NODE'
import mitigation_budget as b, phase23_prereg as p, phase23_matched_prereg as mp
r = json.loads(pathlib.Path(mp.MATCHED_CONTROL_RECORD).read_text())
rates = [e['primary']['k']/e['primary']['n'] for e in r['per_seed']]
assert b.MATCHED_CONTROL_NOISE_FLOOR == p.noise_floor(rates), (b.MATCHED_CONTROL_NOISE_FLOOR, p.noise_floor(rates))
assert b.CONTROL_NOISE_FLOOR == 0.05357142857142849, 'ORIGINAL MOVED'
pv = b.MATCHED_CONTROL_NOISE_FLOOR_PROVENANCE
assert set(pv) >= set(p.FLOOR_PROVENANCE_KEYS), [k for k in p.FLOOR_PROVENANCE_KEYS if k not in pv]
assert pv['governs'] == r['governs']
assert pv['record_file_sha256'] == hashlib.sha256(pathlib.Path(mp.MATCHED_CONTROL_RECORD).read_bytes()).hexdigest()
print('OK', b.MATCHED_CONTROL_NOISE_FLOOR)
" && git diff -U0 -- scripts/mitigation_budget.py | grep -c '^-[^-]' </automated>
  </verify>
  <acceptance_criteria>
    - `ast.parse` of `scripts/mitigation_budget.py` contains ZERO `Import`/`ImportFrom` nodes and its
      `body` holds only `Expr` (the docstring) and `Assign` nodes.
    - `mitigation_budget.CONTROL_NOISE_FLOOR == 0.05357142857142849` — the original constant is
      unmoved.
    - `git diff -U0 -- scripts/mitigation_budget.py | grep -c '^-[^-]'` returns **0** — the diff is
      PURELY ADDITIVE, zero deletions. This is the retract-in-place property, measured.
    - `mitigation_budget.MATCHED_CONTROL_NOISE_FLOOR` equals `phase23_prereg.noise_floor` over
      `results/phase23_matched_control.json`'s own per-seed `k/n` values under exact `==`.
    - `MATCHED_CONTROL_NOISE_FLOOR_PROVENANCE` carries all eight `FLOOR_PROVENANCE_KEYS`, its
      `governs` equals the record's `governs` string, and its `record_file_sha256` equals a live
      `sha256` of the record's bytes.
    - The string `RE-SCOPED IN PLACE 2026-08-27 (plan 23-18).` appears in the file.
  </acceptance_criteria>
  <done>Two new literal constants exist beside the originals; the diff is purely additive with zero
  deletions; no import was added; the new floor re-derives from the committed record.</done>
</task>

<task type="auto">
  <name>Task 2: Guard the new pin and re-measure the ceiling</name>
  <files>tests/test_phase23_budget.py</files>
  <read_first>
    - tests/test_phase23_budget.py in full, and in particular `_control_record()` /
      `_control_readings(record)` (the helpers `test_budget_constants_re_derive` uses),
      `test_budget_holds_only_literal_constants`'s meta-guard, and
      `test_a_hand_edited_floor_is_detected`'s `tmp_path` construction — the new tests reuse all
      three shapes rather than inventing new ones.
    - scripts/mitigation_budget.py as written in Task 1.
  </read_first>
  <action>
Edit `tests/test_phase23_budget.py`. Add matched-floor siblings to the existing guards; do NOT
rewrite or weaken any existing test.

1. `_matched_record()` / `_matched_readings(record)` — helpers mirroring `_control_record` /
   `_control_readings`, resolving the path from `phase23_matched_prereg.MATCHED_CONTROL_RECORD`
   rather than from a string literal.

2. `test_matched_floor_re_derives` — the sibling of `test_budget_constants_re_derive`, in the same
   order: first assert every recorded `rate` equals its own `k/n` (the stored summary has not
   drifted from its evidence); then assert
   `mitigation_budget.MATCHED_CONTROL_NOISE_FLOOR == phase23_prereg.noise_floor(readings)` under
   exact `==`; then cross-check the record's own `floor` field against the SAME recomputation, as
   evidence that 23-17's writer called the blind rule too — never as the source of the pin.

3. `test_matched_floor_provenance_is_complete` — all eight `FLOOR_PROVENANCE_KEYS` present;
   `record` equals `MATCHED_CONTROL_RECORD`; `record_sha256` equals the record's own
   `record_sha256` field; `record_file_sha256` equals a LIVE `sha256` of `read_bytes()`;
   `seeds` is a tuple equal to `tuple(record["seeds"])`; `reduction ==
   "phase23_prereg.noise_floor"`; `governs` equals the record's `governs` VERBATIM;
   `sigma_zero_was_visible is True`.

4. `test_a_hand_edited_matched_floor_is_detected(tmp_path)` — WATCHED RED, the sibling of
   `test_a_hand_edited_floor_is_detected`. Under `tmp_path`, nudge the floor by one ULP
   (`math.nextafter` in the TEST, never in the module) and OBSERVE the exact-`==` comparison
   refusing it. A guard nobody has watched fail is not evidence, and Phase 20 MEASURED this exact
   case defeating a float comparison at GATE-02.

5. `test_the_original_floor_is_left_standing_and_re_scoped` — assert
   `mitigation_budget.CONTROL_NOISE_FLOOR == 0.05357142857142849` (still re-deriving from
   `results/phase23_control_floor.json` via the existing guard), AND that the module source contains
   the marker `RE-SCOPED IN PLACE 2026-08-27 (plan 23-18).`, AND that the original comment block's
   distinctive sentence is still present BYTE-IDENTICALLY (pick one exact substring from the
   pre-edit file and assert it). This is what stops the continuation becoming a rewrite — the same
   property `test_cost_claim_correction_is_additive` is designed around in 23-12.

6. Extend `test_the_import_ceiling_still_has_zero_headroom`'s docstring ONLY (not its assertion) with
   a dated line recording that 23-18 added two constants and zero imports, and that the measured
   union is unchanged. The assertion itself stays `== {"erasure_gate", "pathlib", "sys"}`.

7. Confirm `test_the_budget_module_is_protected_but_not_frozen` still passes UNCHANGED — this plan
   must not register the module as a `prereg_artifact=`. If it fails, STOP: something froze the file
   and the middle ground 23-09 preserved is gone.

Then run ruff and the full suite, and commit `scripts/mitigation_budget.py` and
`tests/test_phase23_budget.py` in ONE commit. The commit message names the new floor and states that
the original is unchanged.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase23_budget.py -q && .venv/bin/python -m pytest -q 2>&1 | tail -3 && .venv/bin/ruff check . && .venv/bin/ruff format --check . && git diff --exit-code -- scripts/phase23_prereg.py scripts/phase23_matched_prereg.py scripts/mitigation_gate.py scripts/mitigation_accountant.py results/phase23_control_floor.json results/phase23_sigma_zero.json results/phase23_matched_control.json</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_phase23_budget.py -q` passes with 5 more tests than
      before (record both counts in the SUMMARY).
    - `test_the_import_ceiling_still_has_zero_headroom` and
      `test_the_budget_module_is_protected_but_not_frozen` pass UNCHANGED in assertion.
    - `test_budget_holds_only_literal_constants` passes — the new constants are literal assignments.
    - The one-ULP nudge is OBSERVED being refused (the test names the observed exception/comparison
      in its assertion message).
    - Full suite green; the count and any delta are recorded in the SUMMARY.
    - `git diff --exit-code` over `scripts/phase23_prereg.py`, `scripts/phase23_matched_prereg.py`,
      `scripts/mitigation_gate.py`, `scripts/mitigation_accountant.py` and all three
      `results/phase23_*` records exits 0.
    - `.venv/bin/ruff check .` and `.venv/bin/ruff format --check .` exit 0.
  </acceptance_criteria>
  <done>Five new guards pass; the new floor re-derives on every suite run and a one-ULP nudge has
  been watched refused; the original pin, the zero-headroom ceiling and the protected-but-not-frozen
  status are all intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the measured floor → the pinned literal | a transcription slip or a hand nudge between record and constant |
| `scripts/mitigation_budget.py` → the gate | an import here would breach a ceiling with zero measured headroom |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-72 | Tampering | `MATCHED_CONTROL_NOISE_FLOOR` | mitigate | re-derived on every suite run by CALLING `phase23_prereg.noise_floor` on the record's own counts under exact `==`; a one-ULP nudge is WATCHED refused |
| T-23-73 | Repudiation | the superseded original | mitigate | zero-deletion diff asserted by `git diff -U0 \| grep -c '^-[^-]'` returning 0, plus a byte-identical-substring test |
| T-23-74 | Elevation of Privilege | freezing the budget module | mitigate | `test_the_budget_module_is_protected_but_not_frozen` left unchanged and required to pass; no `prereg_artifact=` is added |
| T-23-75 | Denial of Service | the import ceiling | mitigate | AST assertion of zero `Import`/`ImportFrom` nodes, plus the unchanged equality guard |
| T-23-SC | Tampering | npm/pip/cargo installs | mitigate | no package install; existing `.venv` only |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_phase23_budget.py -q` — all guards including the five new.
- `git diff -U0 -- scripts/mitigation_budget.py | grep -c '^-[^-]'` — returns 0.
- The AST snippet in Task 1's automated check — zero imports, literal-only body, both floors correct.
- `.venv/bin/python -m pytest -q` — full suite green.
- `git diff --exit-code` over the four frozen/pinned scripts and the three records — exits 0.
</verification>

<success_criteria>
- `MATCHED_CONTROL_NOISE_FLOOR` and its eight-key provenance pinned as literals, re-deriving on every
  suite run from `results/phase23_matched_control.json`.
- A purely additive diff — zero deletions — with the original floor and the module docstring intact
  apart from two appended dated continuations.
- Zero new imports; the zero-headroom ceiling equality guard still green.
- The module still protected-but-NOT-frozen.
- NO requirement is ticked.
</success_criteria>

<output>
Create `.planning/phases/23-cost-calibration-the-0-diagnostic-and-budget-pre-registratio/23-18-SUMMARY.md` when done.
</output>
