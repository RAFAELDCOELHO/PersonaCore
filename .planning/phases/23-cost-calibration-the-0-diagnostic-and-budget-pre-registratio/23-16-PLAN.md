---
phase: 23-cost-calibration-the-0-diagnostic-and-budget-pre-registratio
plan: 16
type: execute
wave: 2
depends_on: ["23-15"]
gap_closure: true
files_modified:
  - scripts/phase23_run.py
  - tests/test_phase23_matched.py
autonomous: true
requirements: [DPSGD-06]

must_haves:
  truths:
    - "The structural obstacle DISSOLVES without widening `DP_ARMS` and without editing `teach_persona.py`. `train()`'s fact-aligned seam is keyed on `fact_bin`/`n_facts` (`loop.py:512`, `_fact = {\"fact_bin\": fact_bin, \"n_facts\": n_facts}`) and its replay seam on `replay_windows` — NEITHER is keyed on `dp_fn`. So a `dp_fn=None` call carrying the same six data kwargs reaches the identical loader, the identical lot size and the identical replay pass."
    - "The comparator trains on the SAME BYTES as the σ=0 arm, not on an equivalent rebuild. `prove_bins_match(DP_N8_BIN_SHA256)` pins `data/persona_dp_n8_train{,_mask,_fact}.bin` to 23-07's recorded digests before a single step runs. Bin identity, not bin equivalence."
    - "`train_never_taught` (`phase23_run.py:574-690`) already established the direct-`train()` register for an arm `build_arm_bins` cannot express. The comparator reuses that register unchanged — same `refuse_if_exists([csv, checkpoint, adapter])`, same canary, same `export_adapter` with a base fingerprint READ from the checkpoint."
    - "`grad_clip` is equalised by CONSTANT and PROVEN non-binding at run time, never assumed. `captured_grad_clip()` shadows `torch.nn.utils.clip_grad_norm_` — the same shadow-at-the-seam register `captured_dp_seam()` uses for `DPSGD.finalize` — and records every pre-clip global norm, so `max(norms) < MATCHED_GRAD_CLIP` is an OBSERVATION."
    - "Every protocol value is DERIVED from the same symbol the DP arm reads, never retyped: `n_facts` from `len(tp.arm_spec('dp_n8')[0])`, `replay_windows` from `tp.replay_window_budget(n_facts) // tp.BLOCK_SIZE`, `fact_bin` from `tp.fact_bin_path(...)`, the budget from `tp.LR`/`tp.WARMUP_STEPS`/`tp.MAX_STEPS`/`tp.BATCH_SIZE`/`tp.WEIGHT_DECAY`."
    - "The two AST completeness gates run BEFORE the first GPU second is spent, so an undeclared `dp_fn` branch or a dropped `dp_kwargs` key refuses at zero cost instead of after 100 minutes."
  artifacts:
    - path: "scripts/phase23_run.py"
      provides: "the matched comparator's kwargs builder, the grad-clip capture bracket, the training leg, and the completeness preflight"
      contains: "def train_matched_control"
    - path: "tests/test_phase23_matched.py"
      provides: "the CPU-provable half — kwargs equality against the DP arm's own symbols, the grad-clip capture watched, and both AST gates driven"
      contains: "def test_matched_kwargs_carry_every_dp_wiring_key"
  key_links:
    - from: "scripts/phase23_run.py::matched_control_call"
      to: "scripts/phase23_matched_prereg.py::prove_dp_wiring_keys"
      via: "the matched call's key set is checked against an AST read of teach_persona's own dp_kwargs literal, not against a retyped list"
      pattern: "prove_dp_wiring_keys"
    - from: "scripts/phase23_run.py::captured_grad_clip"
      to: "torch.nn.utils.clip_grad_norm_"
      via: "module-attribute shadow, restored in finally — the register captured_dp_seam already uses"
      pattern: "clip_grad_norm_"
---

<objective>
Build the protocol-matched comparator's training leg and its completeness preflight, and prove
everything provable on CPU — before any GPU time is spent.

**The obstacle the planning brief flagged, and why it dissolves.** The brief warns that "a non-DP arm
cannot reach the `dp_kwargs` seam today — `build_arm_bins` reads `DP_ARMS` and nothing else, and
widening `DP_ARMS` would force the arm to carry a sigma." That is true of `build_arm_bins` and FALSE
of `train()`. Read `src/personacore/training/loop.py:508-548`: the fact-aligned seam's three refusals
are gated on `_fact = {"fact_bin": fact_bin, "n_facts": n_facts}` — *"Gated on the FACT half only"* —
and `loop.py:683` gates the replay pass on `replay_windows is not None`. Neither mentions `dp_fn`.
So the seam change the brief asks to be planned is **no seam change at all**: the comparator calls
`tp.train(...)` directly with the six data kwargs and `dp_fn` absent, exactly as
`phase23_run.train_never_taught` already calls `tp.train(...)` directly for an arm
`build_arm_bins` cannot express. `DP_ARMS` is not widened; `teach_persona.py` is not edited.

What remains genuinely new is small and all of it is in `scripts/phase23_run.py`: one kwargs
builder, one capture bracket for `clip_grad_norm_`, one training leg, and a preflight that runs
23-15's two AST gates.

Purpose: make the comparator exist as code whose protocol match is checked by construction, with
every refusal reachable at zero GPU cost.
Output: `train_matched_control(seed)` + `matched_control_call(seed)` + `captured_grad_clip()` in
`scripts/phase23_run.py`, and `tests/test_phase23_matched.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/debug/sigma-zero-beats-control.md
@.planning/phases/23-cost-calibration-the-0-diagnostic-and-budget-pre-registratio/23-15-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Resolved from source at planning time. Derive, do not retype. -->

scripts/phase23_run.py — existing symbols this plan reuses UNCHANGED:
  _ROOT, _prove(condition, message), _sha256(path), _rel(path)
  PREFIX = "phase23";  SEED_LADDER = (1337, 2024, 1338, 2025, 1339)
  SIGMA_ZERO_ARM = "dp_n8";  SIGMA_ZERO_CLIP_NORM = 1e6
  DP_N8_BIN_SHA256 = {abs data/persona_dp_n8_train{,_mask,_fact}.bin -> sha256}   # 23-07's digests
  prove_bins_match(expected_sha256) -> int          # refuses on any digest mismatch
  rebuild_arm_bins_verifying_sha256(arm, *, facts, family_ids, seed, expected_sha256) -> int
  device() -> str ; synchronized_seconds(box) ctx ; provenance() -> dict
  _state_load() / _state_write(doc) / _state_record(section, key, block)
  _already_trained(section, seed) -> bool ; _preconditions()
  captured_dp_seam() ctx      # THE SHADOW REGISTER TO COPY (phase23_run.py:780-812)
  score_adapter(arm, adapter, *, seed) -> dict     # arm is a LABEL only; reuse verbatim
  train_never_taught(seed) -> dict                 # THE DIRECT-train() REGISTER TO COPY (574-690)
  _TABLE = {"cost","schedule","floor","sigma-zero"}   # add the new sub-mode here
  USAGE                                                # and document it there

scripts/teach_persona.py — every value the comparator derives, with its source:
  arm_spec("dp_n8") -> (fs.LOCKED_FACTS, False, 0.0)      # facts, second_person, replay_ratio
  arm_outputs(arm, *, prefix) -> {"bin","mask","csv","checkpoint","adapter"}
       adapter -> checkpoints/{prefix}_{arm}_adapter.pt ; csv -> results/{prefix}_{arm}/run.csv
       bin/mask carry NO prefix: data/persona_{arm}_train{,_mask}.bin
  fact_bin_path(bin_path) -> data/persona_{arm}_train_fact.bin
  replay_window_budget(n_facts, block_size=BLOCK_SIZE) -> REPLAY_WINDOWS_PER_FACT*n_facts*block_size
  REPLAY_WINDOWS_PER_FACT = 4 ; BLOCK_SIZE = 256 ; BATCH_SIZE = 8 ; MAX_STEPS = 200
  LR = 3e-4 ; WARMUP_STEPS = 20 ; WEIGHT_DECAY = 0.0 ; EVAL_INTERVAL = 10 ; CHECKPOINT_INTERVAL = 50
  LORA_CFG ; CONVBASE_BEST ; DIALOG_TRAIN_BIN ; DIALOG_TRAIN_MASK ; DIALOG_VAL_BIN ; DIALOG_VAL_MASK
  refuse_if_exists(paths, *, expected=()) ; export_adapter ; lora_state_dict ; snapshot_params
  inject_lora ; mark_only_lora_trainable ; seed_everything ; GPT ; ModelConfig ; RuntimeConfig
  TrainConfig(lr, warmup_steps, max_steps, batch_size, weight_decay, seed, grad_accum_steps, grad_clip)

src/personacore/config.py:105-106   grad_clip: float = 1.0 ; grad_accum_steps: int = 1
src/personacore/training/loop.py:220-221
       if dp_fn is None: torch.nn.utils.clip_grad_norm_(model.parameters(), train_cfg.grad_clip)

DERIVED PROTOCOL VALUES (compute them, do not paste them; these are the expected results):
  n_facts        = len(tp.arm_spec("dp_n8")[0])                     -> 8
  grad_accum     = n_facts                                          -> 8
  replay_windows = tp.replay_window_budget(n_facts)//tp.BLOCK_SIZE  -> 32
  lot            = 33 teaching windows + 32 replay windows          -> 65 (vs the old control's 8)
  recorded in results/phase23_sigma_zero.json: grad_accum_steps 8, replay_windows_per_step 32,
      replay_micro_batches_per_step 4, batch_size 8, block_size 256, max_steps 200

scripts/phase23_matched_prereg.py (23-15) — CONSUMED, never re-derived:
  MATCHED_GRAD_CLIP, MATCHED_CONTROL_RECORD, MATCHED_VERDICT_RECORD, MATCHED_ARTIFACT_GLOB,
  MATCHED_ARM_PREFIX, matched_arm(seed), DP_TRAIN_KEYS, DP_KWARGS_KEYS,
  prove_branch_ledger_complete(source), prove_dp_wiring_keys(source), prove_first_attempt(tracked)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The matched comparator's call, capture bracket and training leg</name>
  <files>scripts/phase23_run.py</files>
  <read_first>
    - scripts/phase23_run.py lines 574-690 (`train_never_taught`) — the direct-`train()` register:
      `refuse_if_exists` on the three non-bin targets, load-before-inject, the `n_wrapped` and
      trainable-census `_prove`s, `snapshot_params` before, the canary after, and
      `export_adapter(base_fingerprint=...)` READ from the base checkpoint.
    - scripts/phase23_run.py lines 780-812 (`captured_dp_seam`) — the shadow-at-the-seam register:
      capture the real callable, install a wrapper, restore in `finally`, refuse a second install.
    - scripts/phase23_run.py lines 194-290 (`prove_bins_match`, `rebuild_arm_bins_verifying_sha256`)
      and 841-900 (`train_sigma_zero`'s bin handling) — how the dp_n8 bins are pinned and rebuilt.
    - src/personacore/training/loop.py lines 508-548 — the fact-aligned seam's three refusals, so
      the `grad_accum_steps == n_facts` agreement is satisfied by construction.
    - scripts/phase23_matched_prereg.py — the constants and gates this task consumes.
  </read_first>
  <action>
Edit `scripts/phase23_run.py` only. Import the new pin at module scope beside the existing
`phase23_prereg` import: `import phase23_matched_prereg as mp` (or a `from ... import` of the named
symbols, matching the file's existing style).

Add, in a new clearly-headed section:

(a) `def matched_control_call(seed)` — returns a 2-tuple `(train_config_fields, train_kwargs)` of
    plain dicts, computing every value from the symbols named in `<interfaces>`:
    `n_facts = len(tp.arm_spec(SIGMA_ZERO_ARM)[0])`;
    `arm = mp.matched_arm(seed)`; `paths = tp.arm_outputs(arm, prefix=mp.MATCHED_ARM_PREFIX)`;
    `dp_paths = tp.arm_outputs(SIGMA_ZERO_ARM, prefix=PREFIX)` for the BIN paths only (bins carry no
    prefix, so any prefix yields the same `data/persona_dp_n8_train*.bin`; say so in a comment).
    `train_config_fields = dict(lr=tp.LR, warmup_steps=tp.WARMUP_STEPS, max_steps=tp.MAX_STEPS,
    batch_size=tp.BATCH_SIZE, weight_decay=tp.WEIGHT_DECAY, seed=seed,
    grad_accum_steps=n_facts, grad_clip=mp.MATCHED_GRAD_CLIP)`.
    `train_kwargs = dict(train_bin=dp_paths["bin"], train_mask_bin=dp_paths["mask"],
    fact_bin=tp.fact_bin_path(dp_paths["bin"]), n_facts=n_facts,
    replay_bin=tp.DIALOG_TRAIN_BIN, replay_mask_bin=tp.DIALOG_TRAIN_MASK,
    replay_windows=tp.replay_window_budget(n_facts) // tp.BLOCK_SIZE,
    val_bin=tp.DIALOG_VAL_BIN, val_mask_bin=tp.DIALOG_VAL_MASK, penalty_fn=None,
    log_path=paths["csv"], checkpoint_path=paths["checkpoint"],
    eval_interval=tp.EVAL_INTERVAL, checkpoint_interval=tp.CHECKPOINT_INTERVAL,
    return_final_loss=True)`.
    **`dp_fn` IS DELIBERATELY ABSENT** — write a comment saying `train()`'s default is `None` and
    that passing `dp_fn=None` explicitly would be equivalent but would put a `dp_fn` key in the
    diff for no reason. Docstring must state the three equalised mechanisms with their measured
    magnitudes (lot volume 65 vs 8 windows, teaching-token exposure 1,689,600 vs 196,867 = 8.58x;
    teaching loss weight 1.0 vs 0.4342 = 2.30x; `grad_clip` bound on 19/25 control steps at mean
    shrink 0.8071 and structurally absent from the DP arm), and that matching token count alone is
    explicitly insufficient.

(b) `def prove_matched_protocol()` — the ZERO-COST PREFLIGHT. Reads
    `src/personacore/training/loop.py` and `scripts/teach_persona.py` off disk (paths resolved from
    `_ROOT`), calls `mp.prove_branch_ledger_complete(...)` and `mp.prove_dp_wiring_keys(...)`, then
    `_prove`s that the union of `matched_control_call(SEED_LADDER[0])`'s two dicts contains every
    name in `mp.DP_TRAIN_KEYS + mp.DP_KWARGS_KEYS` EXCEPT `"dp_fn"`, and that `"dp_fn"` is NOT a key
    of `train_kwargs`. Message must state that a dropped data kwarg would silently return the
    comparator to the old random-window protocol — the exact defect this whole gap closure exists to
    correct. Returns the census for the record.

(c) `@contextmanager def captured_grad_clip()` — copy `captured_dp_seam`'s shape exactly. Capture
    `real = torch.nn.utils.clip_grad_norm_` (reach it as `tp.torch.nn.utils.clip_grad_norm_`, the
    same module object `loop.py` resolves at call time), install a wrapper that calls `real`,
    appends `float(returned_norm)` to `box["norms"]` and returns the value unchanged, and RESTORE in
    `finally`. Yield `box = {"norms": []}`. Docstring must state: `loop.py:221` resolves
    `torch.nn.utils.clip_grad_norm_` by attribute at call time, so a module-attribute shadow is
    visible to it without editing `loop.py`; `clip_grad_norm_` RETURNS the PRE-clip global norm, so
    the captured list is exactly the quantity probe 1 measured; and this is what turns "non-binding"
    from an assumption into an observation.

(d) `def train_matched_control(seed)` — the training leg. In order:
    1. `arm = mp.matched_arm(seed)`; `paths = tp.arm_outputs(arm, prefix=mp.MATCHED_ARM_PREFIX)`;
       `tp.refuse_if_exists([paths["csv"], paths["checkpoint"], paths["adapter"]])`. The dp_n8 bins
       are INPUTS, not targets — do not pass them.
    2. If any path in `DP_N8_BIN_SHA256` is missing, call `rebuild_arm_bins_verifying_sha256(
       SIGMA_ZERO_ARM, facts=tp.arm_spec(SIGMA_ZERO_ARM)[0], family_ids=fs.TAUGHT_FAMILY_IDS,
       seed=SEED_LADDER[0], expected_sha256=DP_N8_BIN_SHA256)`; otherwise call
       `prove_bins_match(DP_N8_BIN_SHA256)` directly. Either way `_prove` the returned count equals
       `len(DP_N8_BIN_SHA256)`. **The seed used to REBUILD the bins is `SEED_LADDER[0]`, not the
       per-arm `seed`** — the bins are the σ=0 arm's corpus and must be byte-identical at every
       comparator seed; only the model init and data order vary with `seed`. State this in a comment;
       it is the one place a seed could silently change the corpus.
    3. Build the model exactly as `train_never_taught` does — `RuntimeConfig()`, load
       `tp.CONVBASE_BEST` with `weights_only=False`, `ModelConfig(**blob["model_config"])`,
       `tp.seed_everything(seed)`, `tp.GPT(model_cfg)`, `load_state_dict` BEFORE `inject_lora`, the
       `n_wrapped == 6 * n_layer` `_prove`, `mark_only_lora_trainable`, the trainable-census `_prove`,
       `model.to(runtime.device)`, `snapshot_params`.
    4. `fields, kwargs = matched_control_call(seed)`; call
       `tp.train(train_config=tp.TrainConfig(**fields), runtime_config=runtime, model=model,
       model_config=model_cfg, **kwargs)` inside `with captured_grad_clip() as clip_box:` inside
       `with synchronized_seconds(box):`.
    5. **THE NON-BINDING PROOF, BEFORE ANY READING EXISTS.** `_prove(len(clip_box["norms"]) ==
       tp.MAX_STEPS, ...)` — one `clip_grad_norm_` call per optimizer step, so a different count
       means the branch was not taken and the equalisation was never applied. Then
       `_prove(max(clip_box["norms"]) < mp.MATCHED_GRAD_CLIP, ...)` with a message stating that a
       binding clip makes this arm differ from the σ=0 arm by CLIPPING rather than by protocol, and
       that this check runs before scoring for the same reason `train_sigma_zero`'s
       `clip_bind_count == 0` does.
    6. The same canary `train_never_taught` runs (every trainable moved, every frozen base param
       bit-identical), the finite-loss `_prove`, `export_adapter` with the base fingerprint READ
       from `blob`, `del model`.
    7. Return a block carrying: `seed`, `arm`, `training_seconds`, `final_train_loss`, `adapter`
       (via `_rel`), `adapter_sha256`, `adapter_bytes`, `csv` (via `_rel`), `base_fingerprint`,
       `n_facts`, `grad_accum_steps`, `grad_clip`, `replay_windows_per_step`,
       `replay_micro_batches_per_step` (`math.ceil(replay_windows / tp.BATCH_SIZE)`), `max_steps`,
       `batch_size`, `block_size`, `dp_seam_active: False`,
       `grad_clip_calls: len(clip_box["norms"])`,
       `grad_clip_max_pre_clip_norm: max(...)`, `grad_clip_min_pre_clip_norm: min(...)`,
       `grad_clip_bound_count: sum(1 for n in norms if n > mp.MATCHED_GRAD_CLIP)` (which must be 0),
       `grad_clip_checked_before_scoring: True`,
       `corpus_sha256: {_rel(p): d for p, d in DP_N8_BIN_SHA256.items()}`.

Do NOT add the record writer or the sub-mode entry in this task — 23-17 does that. Do NOT edit
`scripts/teach_persona.py`, `src/personacore/training/loop.py`, `scripts/phase23_prereg.py`,
`scripts/mitigation_*.py` or `scripts/phase23_matched_prereg.py`.
  </action>
  <verify>
    <automated>.venv/bin/python -c "
import sys; sys.path.insert(0,'scripts')
import phase23_run as r, teach_persona as tp
c = r.prove_matched_protocol()
f, k = r.matched_control_call(1337)
assert f['grad_accum_steps'] == 8 and f['grad_clip'] == 1e6, f
assert k['n_facts'] == 8 and k['replay_windows'] == 32, k
assert 'dp_fn' not in k
assert k['fact_bin'].name == 'persona_dp_n8_train_fact.bin', k['fact_bin']
assert k['log_path'].as_posix().endswith('results/phase23_matched_control_seed1337/run.csv'), k['log_path']
assert k['checkpoint_path'].parent.name == 'checkpoints'
print('OK', sum(c.values()), f['grad_accum_steps'], k['replay_windows'])
" && .venv/bin/ruff check scripts/phase23_run.py</automated>
  </verify>
  <acceptance_criteria>
    - `matched_control_call(1337)` returns `grad_accum_steps == 8`, `grad_clip == 1e6`,
      `n_facts == 8`, `replay_windows == 32`, and `'dp_fn' not in train_kwargs`.
    - `train_kwargs["fact_bin"]` resolves to `data/persona_dp_n8_train_fact.bin` and
      `train_kwargs["train_bin"]` to `data/persona_dp_n8_train.bin` — the σ=0 arm's own bins.
    - `train_kwargs["log_path"]` ends `results/phase23_matched_control_seed1337/run.csv` and
      `train_kwargs["checkpoint_path"]` sits under `checkpoints/`.
    - `prove_matched_protocol()` returns a Counter summing to `7` and raises nothing.
    - `git diff --exit-code -- scripts/teach_persona.py src/personacore/training/loop.py
      scripts/phase23_prereg.py scripts/phase23_matched_prereg.py scripts/mitigation_gate.py
      scripts/mitigation_accountant.py scripts/mitigation_budget.py` exits 0.
    - `grep -n "DP_ARMS" scripts/teach_persona.py | wc -l` is unchanged from its pre-plan value
      (record the number in the SUMMARY) — `DP_ARMS` is not widened.
    - `.venv/bin/ruff check scripts/phase23_run.py` exits 0.
  </acceptance_criteria>
  <done>`matched_control_call`, `prove_matched_protocol`, `captured_grad_clip` and
  `train_matched_control` exist in `scripts/phase23_run.py`; every derived value matches the σ=0
  record's own fields; no other source file changed.</done>
</task>

<task type="auto">
  <name>Task 2: Prove on CPU everything that can be proven before the GPU run</name>
  <files>tests/test_phase23_matched.py</files>
  <read_first>
    - tests/test_phase23_matched_prereg.py — the sys.path register and the watched-RED style from
      23-15.
    - scripts/phase23_run.py — `matched_control_call`, `prove_matched_protocol`,
      `captured_grad_clip` as written in Task 1.
    - results/phase23_sigma_zero.json — the `training` block's `grad_accum_steps`,
      `replay_windows_per_step`, `replay_micro_batches_per_step`, `batch_size`, `block_size`,
      `max_steps` and `capacity_n_facts`; these are the values the comparator must reproduce.
  </read_first>
  <action>
Create `tests/test_phase23_matched.py`. CPU-only, GPU-free, no network — no test here may train.
Import `phase23_run` and `phase23_matched_prereg` through the established `sys.path` insert.

1. `test_matched_kwargs_carry_every_dp_wiring_key` — read `scripts/teach_persona.py` off disk, call
   `mp.dp_wiring_key_census(source)`, and assert every key it reports EXCEPT `"dp_fn"` appears in
   `matched_control_call(1337)`'s two dicts, and that `"dp_fn"` appears in NEITHER. Read the key set
   from the AST census, never from a literal in the test — that is the whole point of the gate.

2. `test_matched_protocol_reproduces_the_sigma_zero_records_own_shape` — load
   `results/phase23_sigma_zero.json`, and assert field-by-field:
   `training["grad_accum_steps"] == fields["grad_accum_steps"]`,
   `training["capacity_n_facts"] == kwargs["n_facts"]`,
   `training["replay_windows_per_step"] == kwargs["replay_windows"]`,
   `training["batch_size"] == fields["batch_size"]`,
   `training["max_steps"] == fields["max_steps"]`,
   `training["block_size"] == tp.BLOCK_SIZE`.
   The committed σ=0 record is the source of truth; a hand-typed 8/32/200 in this test would be a
   second source for one fact.

3. `test_the_matched_arm_trains_on_the_sigma_zero_arms_own_bins` — assert
   `kwargs["train_bin"]`, `kwargs["train_mask_bin"]` and `kwargs["fact_bin"]` are exactly the three
   keys of `phase23_run.DP_N8_BIN_SHA256` (compare resolved absolute strings). This is bin IDENTITY,
   not equivalence, and it must be a test rather than a comment.

4. `test_the_matched_grad_clip_is_the_non_binding_bound` — assert
   `fields["grad_clip"] == mp.MATCHED_GRAD_CLIP == phase23_run.SIGMA_ZERO_CLIP_NORM` and that it is
   strictly greater than the largest pre-clip norm the debug record measured (`2.278`) by at least
   five orders of magnitude. Docstring names the 19/25 measurement it exists to prevent recurring.

5. `test_the_default_grad_clip_would_not_be_equalised` — WATCHED RED in the sense that matters:
   assert `personacore.config.TrainConfig().grad_clip == 1.0` and that
   `1.0 < 2.278` — i.e. the DEFAULT would have bound on the comparator exactly as it bound on the
   old control. This test is what stops a future edit dropping `grad_clip` from
   `matched_control_call` and silently restoring the asymmetry.

6. `test_captured_grad_clip_observes_and_restores` — install the bracket around a two-parameter toy
   `torch` module: build a tiny `nn.Linear`, give it a `.grad`, call
   `torch.nn.utils.clip_grad_norm_(params, 1e6)` INSIDE the bracket, assert one norm was captured and
   equals the value the call returned, then assert OUTSIDE the bracket that
   `torch.nn.utils.clip_grad_norm_` is the original callable again (identity check against the object
   captured before entering). CPU tensors only.

7. `test_a_second_matched_attempt_is_refused` — drive `mp.prove_first_attempt` with a non-empty list
   and assert `SystemExit`; with `[]` and assert it returns.

8. `test_the_matched_preflight_runs_against_live_source` — call `phase23_run.prove_matched_protocol()`
   and assert the returned census sums to 7. This is the same gate the GPU run will hit, exercised
   here at zero cost.

Then run ruff and the full suite.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase23_matched.py -q && .venv/bin/python -m pytest -q 2>&1 | tail -3 && .venv/bin/ruff check . && .venv/bin/ruff format --check .</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_phase23_matched.py -q` passes with at least 8 tests.
    - No test in the file constructs a `GPT`, loads `checkpoints/convbase_best.pt`, or calls
      `tp.train` — verified by `grep -c "tp.train(\|CONVBASE_BEST\|tp.GPT(" tests/test_phase23_matched.py`
      returning 0.
    - The full suite reports `1505 passed, 1 skipped` (23-15's `1497` plus the 8 new tests); a
      different count is recorded with its cause in the SUMMARY rather than accepted silently.
    - `.venv/bin/ruff check .` and `.venv/bin/ruff format --check .` exit 0.
    - `git diff --exit-code -- scripts/phase23_prereg.py scripts/phase23_matched_prereg.py
      scripts/mitigation_gate.py scripts/mitigation_accountant.py scripts/mitigation_budget.py
      scripts/teach_persona.py src/personacore/training/loop.py` exits 0.
  </acceptance_criteria>
  <done>Eight CPU-only tests pass; the comparator's shape is asserted against the committed σ=0
  record rather than against retyped constants; the grad-clip capture is watched working and
  restoring; the preflight has been exercised at zero GPU cost.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `phase23_run.matched_control_call` → the trained comparator | a dropped or retyped kwarg silently restores the old protocol |
| `torch.nn.utils.clip_grad_norm_` shadow → the live loop | a shadow not restored would leak into every later test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-64 | Tampering | `matched_control_call` | mitigate | key set checked against an AST census of `teach_persona.py`'s own `dp_kwargs` literal; values asserted against the committed σ=0 record |
| T-23-65 | Spoofing | the comparator's corpus | mitigate | `prove_bins_match(DP_N8_BIN_SHA256)` before any step; the rebuild seed is pinned to `SEED_LADDER[0]`, never the per-arm seed |
| T-23-66 | Denial of Service | the `clip_grad_norm_` shadow | mitigate | restored in `finally`; a dedicated test asserts identity restoration outside the bracket |
| T-23-67 | Repudiation | "grad_clip is non-binding" | mitigate | proven by captured PRE-clip norms with a call-count check, before scoring — never asserted |
| T-23-SC | Tampering | npm/pip/cargo installs | mitigate | no package install; existing venv only |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_phase23_matched.py -q` — 8+ passing, no GPU, no training.
- `.venv/bin/python -m pytest -q` — `1505 passed, 1 skipped` or the delta explained.
- `.venv/bin/python -c "import sys;sys.path.insert(0,'scripts');import phase23_run as r;r.prove_matched_protocol()"` — exits 0.
- `git diff --exit-code` over the seven protected/derived files listed above — exits 0.
</verification>

<success_criteria>
- The comparator's `train()` call exists, derives every protocol value from the DP arm's own
  symbols, and is asserted equal to the committed σ=0 record's fields.
- `DP_ARMS` is NOT widened and `scripts/teach_persona.py` and `src/personacore/training/loop.py` are
  byte-unchanged.
- The grad-clip equalisation is instrumented so that "non-binding" will be an observation, not an
  assumption, the moment the run happens.
- Both AST completeness gates run against live source at zero GPU cost.
- NO requirement is ticked.
</success_criteria>

<output>
Create `.planning/phases/23-cost-calibration-the-0-diagnostic-and-budget-pre-registratio/23-16-SUMMARY.md` when done.
</output>
