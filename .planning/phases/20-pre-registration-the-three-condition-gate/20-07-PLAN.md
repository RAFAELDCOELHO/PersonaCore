---
phase: 20-pre-registration-the-three-condition-gate
plan: 07
type: execute
wave: 6
depends_on: ["20-05", "20-06"]
files_modified:
  - scripts/phase20_run.py
  - results/phase20_retention_floor.json
  - .planning/phases/20-pre-registration-the-three-condition-gate/20-VALIDATION.md
autonomous: false
requirements: [GATE-02, CAL-04]

must_haves:
  truths:
    - "D-08: `results/phase20_retention_floor.json` is committed STRICTLY AFTER `scripts/mitigation_gate.py` is committed AND pushed. The rule is tighter than the mechanism requires — `git merge-base --is-ancestor` is reflexive, so same-commit would pass — and it is recorded as a discipline so nobody later reads same-commit as either a defect or as permission."
    - "D-32: the artifact is PRODUCED BY A COMMITTED DRIVER, never transcribed. The seed-2024 reading `4.2284415113307245` exists in no committed artifact; transcribing it would publish a number whose only provenance is a discussion transcript."
    - "D-32: the driver is UNPINNED, on the `scripts/phase19_run.py` precedent, because writing a measurement driver into the pin would make the pin a non-ancestor of the artifact it produced and turn the ancestry guard permanently red with no recovery."
    - "D-06: the bit-identity control runs FIRST — seed 1337 must reproduce `results/phase19_noise_floors.json`'s published `adapter_off = 3.891139975617828`, `adapter_on = 4.219759892336485` and `n_scored_tokens = 1000285` before any new number is produced. The instrument is verified, not assumed."
    - "D-06: the measured adapter-regime retention noise floor is `0.008681618994239138`, and the borrowed Phase 12 full-fine-tune value `0.068930` is `7.939763314393305x` larger. The re-measurement makes the cap TIGHTER (`3.9085032379884783` vs `4.029000`), so it is not a change that buys an easier pass."
    - "D-06: both stated bounds travel INSIDE the artifact, not in prose beside it — `n = 2` seeds with no confidence interval, and measured on the v3.0 persona recipe (`n_facts=10`, `replay_ratio=1.0`), the right REGIME rather than a v4.0 ARM."
    - "The `governs` field states the mirror of Phase 19's own: the measured adapter-regime floor is what the v4.0 gate reads, and `V20_RETENTION_NOISE_FLOOR = 0.068930` does NOT govern any v4.0 verdict."
    - "The instrument trap is avoided: `retention_perplexity` builds its own dead-id mask internally and takes `tokenizer`, NOT `forbid_ids` — the `forbid` returned by `load_adapted_model` is discarded, exactly as `scripts/phase19_run.py:803` does."
    - "`checkpoints/` is gitignored, so CI can never re-derive this artifact — every reading, denominator, seed, adapter path, git SHA and content digest is EMBEDDED in the JSON."
    - "D-24: after the artifact's first add, `scripts/mitigation_gate.py` is permanently uneditable. Corrections are dated continuations via `scripts/_addendum.py::append_addendum(path, addendum, *, pending, recorded)` — both keywords required — never edits."
    - "The ancestry guard stops being vacuous here: `bool(checked) == bool(tracked_artifacts)` now demands a non-zero `checked`, and it is GREEN."
  artifacts:
    - path: "scripts/phase20_run.py"
      provides: "The unpinned MPS retention-floor driver with its bit-identity control"
      contains: "UNPINNED"
    - path: "results/phase20_retention_floor.json"
      provides: "The adapter-regime retention noise floor with full embedded provenance"
      contains: "retention_ppl_noise_floor"
  key_links:
    - from: "scripts/phase20_run.py"
      to: "personacore.evaluation.perplexity.retention_perplexity"
      via: "ON reading, then OFF inside `adapter_disabled(model)`, one process, shared denominator"
      pattern: "retention_perplexity\\("
    - from: "results/phase20_retention_floor.json"
      to: "scripts/mitigation_gate.py::retention_cap"
      via: "the floor this artifact publishes is the required kwarg that function consumes"
      pattern: "retention_cap"
---

<objective>
Measure the adapter-regime retention noise floor with a committed unpinned driver, and land its
artifact strictly after the pin is committed and pushed.

Purpose: D-06's floor governs condition (c)'s retention leg, and its second input — the seed-2024
reading `4.2284415113307245` — exists in exactly one file on disk, `20-CONTEXT.md`. Transcribing it
would publish a number whose only provenance is a discussion transcript. It must come out of real
code, on the same instrument, with the seed-1337 bit-identity control run first.

This is also the plan where the ordering becomes irreversible. Everything after the artifact's first
add is a dated continuation, never an edit.

Output: `scripts/phase20_run.py` (committed first, no results file) and
`results/phase20_retention_floor.json` (committed last).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-pre-registration-the-three-condition-gate/20-CONTEXT.md
@.planning/phases/20-pre-registration-the-three-condition-gate/20-PATTERNS.md
@.planning/phases/20-pre-registration-the-three-condition-gate/20-RESEARCH.md
@.planning/phases/20-pre-registration-the-three-condition-gate/20-06-SUMMARY.md
@results/phase19_noise_floors.json
@results/phase19_dialogue_floor.json
</context>

<interfaces>
<!-- Resolved from source this session. Every signature below exists TODAY. -->
- `personacore.evaluation.perplexity.retention_perplexity` at `src/personacore/evaluation/perplexity.py:148`
  — `(model, val_bin_path, block_size, device, tokenizer, batch_size=32)` returning
  `(ppl, total_tokens)`, decorated `@torch.no_grad()` at `:147`. **It builds its own dead-id mask
  internally at `:165-170` and does NOT take `forbid_ids=`.**
- `personacore.lora.adapter_disabled` at `src/personacore/lora/inject.py:157` — a
  `@contextlib.contextmanager` over `(model)`.
- `scripts/phase14_recall.py::load_adapted_model` at `:513` — `(device, adapter_path=None)` returning
  `(model, cfg, tok, forbid, artifact)`. Load-before-inject, `weights_only=True`, enforced at `:542-545`.
- `personacore.preflight.preflight_device` — returns a dict; the device is at `["device"]`.
- `personacore.provenance.git_sha`.
- `scripts/phase19_erasure.py` (import as `pin`): `RETENTION_BIN` at `:2555`
  (`_REPO_ROOT / "data" / "retention_val.bin"`), `JSON_INDENT` at `:2394`,
  `ModelConfig.block_size` (= 256).

Inputs, all verified present locally and all gitignored:
- `checkpoints/phase19_erase_dialogue_floor_seed1337_adapter.pt` (1,352,991 bytes)
- `checkpoints/phase19_erase_dialogue_floor_seed2024_adapter.pt` (1,352,991 bytes)
- `data/retention_val.bin` (2,000,572 bytes)

Names this plan CREATES: `scripts/phase20_run.py` with `RETENTION_FLOOR_PATH`, `SEED_ADAPTERS`,
`PUBLISHED_SEED_1337`, `RECIPE`, `_sha256`, `_refuse`, `retention_floor`, `_TABLE`; and
`results/phase20_retention_floor.json`. `RECIPE` is consumed by `retention_floor()`'s artifact dict
as the `recipe` key — it is the D-06 second-bound block, and it is a module constant so the
"measured on the v3.0 persona recipe" bound travels as machine-readable data rather than as prose.
</interfaces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: BLOCKING — confirm the pin is final, committed and PUSHED before any artifact exists</name>
  <read_first>
    - `scripts/mitigation_gate.py` — the whole file, end to end. This is the last moment an edit is
      legal and reversible.
    - `20-CONTEXT.md` D-08 (`:101-105`) and D-24 (`:245-250`) — the strictly-after discipline, and
      why a correction after the artifact must be a dated continuation rather than an edit.
    - `20-RESEARCH.md` L3 — the MEASURED irreversibility: `adds[-1]` is the earliest add, `git rm`
      plus re-add produces a byte-identical earliest add, and there is no recovery.
    - `.planning/ROADMAP.md:133-137` — committed AND pushed, stated as a constraint rather than a
      preference.
    - `.planning/phases/20-pre-registration-the-three-condition-gate/20-05-SUMMARY.md` and
      `20-06-SUMMARY.md` — the recorded sha256 values and the watched-RED observations.
  </read_first>
  <action>
Run all nine commands listed below, report their REAL output verbatim, and then pause for the human.
Do not describe a command instead of executing it, and do not summarise an output as "clean" without
showing it. `git fetch --quiet` is the only network call in this phase and exists solely so
`git rev-parse @{u}` can answer whether the pin is pushed. Make no commit and no file change in this
task.
  </action>
  <what-built>
Plans 20-01 through 20-06 completed `scripts/mitigation_gate.py` and put it under static and
behavioural guard. Before this task pauses, the executor MUST have run and reported all of the
following (do not ask the human to do anything a command can answer):

  1. `.venv/bin/python -m pytest -q` — full suite, exit 0.
  2. `.venv/bin/python scripts/mitigation_gate.py` — exit 0, six outcomes named.
  3. `.venv/bin/ruff check . && .venv/bin/ruff format --check .` — exit 0.
  4. `git status --porcelain` — clean, and `pyproject.toml` untouched.
  5. `git log --oneline -- scripts/mitigation_gate.py` — the complete commit list for the pin.
  6. `git log --diff-filter=A -- 'results/phase20_*'` — must print NOTHING.
  7. `git ls-files 'results/phase20_*'` — must print NOTHING.
  8. `git fetch --quiet && git rev-parse HEAD && git rev-parse @{u}` — the two SHAs, so the human can
     see whether HEAD is pushed.
  9. `shasum -a 256 scripts/mitigation_gate.py` — the exact bytes about to become permanent.
  </what-built>
  <how-to-verify>
1. Read the reported output of all nine commands above.
2. Confirm `git rev-parse HEAD` equals `git rev-parse @{u}` — the pin is PUSHED, not merely
   committed. `ROADMAP.md:133-137` requires committed AND pushed.
3. Confirm `git log --diff-filter=A -- 'results/phase20_*'` printed nothing.
4. **The judgement only a human can make:** is `scripts/mitigation_gate.py` FINAL? Read it once
   more end to end. After the next task's artifact commit, editing this file turns
   `test_phase20_prereg_is_frozen_before_every_phase20_result` permanently red, and `git rm` plus
   re-add cannot undo it — the guard takes `adds[-1]`, the EARLIEST add. There is no second chance.
   The only correction path after this point is a dated continuation via
   `scripts/_addendum.py::append_addendum(path, addendum, *, pending, recorded)` plus an armed
   tripwire test (D-24).
5. If anything needs changing, say so NOW. Fix it, re-run this checkpoint, and only then proceed.
  </how-to-verify>
  <acceptance_criteria>
    - All nine commands have been run and their real output reported — no command is described rather than executed.
    - `git rev-parse HEAD` and `git rev-parse @{u}` print the same 40-character SHA.
    - `git log --diff-filter=A -- 'results/phase20_*'` and `git ls-files 'results/phase20_*'` both print nothing.
    - `.venv/bin/python -m pytest -q` exits 0 and `.venv/bin/python scripts/mitigation_gate.py` exits 0.
    - `git status --porcelain` prints nothing.
    - The sha256 of `scripts/mitigation_gate.py` is recorded verbatim in the resume message.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -q && .venv/bin/python scripts/mitigation_gate.py && .venv/bin/ruff check . && .venv/bin/ruff format --check .</automated>
    <automated>cd /Users/juliorcoelho/PersonaCore && git fetch --quiet && test "$(git rev-parse HEAD)" = "$(git rev-parse @{u})" && test -z "$(git ls-files 'results/phase20_*')" && test -z "$(git log --diff-filter=A --format=%H -- 'results/phase20_*')" && echo "pin pushed, no phase20 artifact yet"</automated>
  </verify>
  <done>The human has read the pin end to end, confirmed HEAD equals its upstream, confirmed no `results/phase20_*` path is tracked or has ever been added, and approved the pin as final.</done>
  <resume-signal>Type "approved — pin is final and pushed" to continue, or describe what must change first.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: scripts/phase20_run.py — the unpinned retention-floor driver with its bit-identity control</name>
  <files>scripts/phase20_run.py</files>
  <read_first>
    - `scripts/phase19_run.py:787-882` — **THE analog**, `retention()`. Read the whole function. Copy
      five things exactly: every import is FUNCTION-LOCAL including `torch` and `numpy`, so the module
      stays inert at import; `device = preflight_device(strict=True)["device"]` (a dict, the
      `["device"]` subscript is required); `recall.load_adapted_model(device, <path>)` with the
      adapter path POSITIONAL as the second argument; the ON reading first then the OFF reading
      inside `with adapter_disabled(model):`, in ONE process; and the denominator refusal as an
      inline `raise SystemExit`, which is this function's committed form.
    - **`scripts/phase19_run.py:803` — the instrument trap.** The fourth return of
      `load_adapted_model` is bound as `_forbid` and DISCARDED; `retention_perplexity` is passed
      `tok`, not `forbid`, because it builds its own `undecodable_ids_mask` internally at
      `src/personacore/evaluation/perplexity.py:165-170`. A driver written by analogy to
      `phase19_erasure.dialogue_ppl_pair` (`:2704-2729`, where `masked_perplexity` requires
      `forbid_ids=` explicitly) would pass `forbid` into the `batch_size` slot and silently corrupt
      the measurement. Copy `retention()`, not `dialogue_ppl_pair`.
    - `scripts/phase19_run.py:814-826` — the WINDOW ACCOUNTING, checked rather than quoted:
      `n_corpus = len(np.memmap(...))`, `n_windows = len(range(0, n_corpus - 1, block_size))`, and
      the `n_corpus - 1 != on_tokens` `SystemExit`.
    - `scripts/phase19_run.py:1-8` — the UNPINNED header register, where each numbered reason is a
      MEASURED justification for why the pinned module cannot do the job.
    - `scripts/phase19_run.py:244-273` — `_sha256`, `_merge_block` and `_refuse`. A single-artifact
      driver wants `_refuse` plus `path.write_text(json.dumps(..., indent=..., sort_keys=True))`;
      `sort_keys=True` is what makes the committed JSON diff-stable. No force flag.
    - `scripts/phase19_run.py:3014-3036` — the subcommand dispatch: a dict over `sys.argv[1]`, no
      argparse, and a usage `SystemExit` naming the table's keys.
    - `scripts/phase19_run.py:177-180` — the `sys.path.insert` bootstrap and the `# noqa: E402`
      comments.
    - `results/phase19_noise_floors.json` — the `retention_ppl_pre_erasure` block, for the published
      seed-1337 values the control must reproduce and the key register the artifact must mirror.
  </read_first>
  <action>
Create `scripts/phase20_run.py`. Commit it in this task. **Do NOT create any `results/` file here.**

Module docstring in the `scripts/phase19_run.py:1-8` register, opening with the word UNPINNED. State
the measured reason it exists as a separate file rather than inside the pin: `scripts/mitigation_gate.py`
is CLOSED, and any commit to it after `results/phase20_retention_floor.json` exists would make the pin
a non-ancestor of the artifact it produced, turning
`test_phase20_prereg_is_frozen_before_every_phase20_result` permanently RED with no recovery — the
guard takes `adds[-1]`, the EARLIEST add, so a delete-and-re-add cycle cannot launder it. State that
this file is watched by no ancestry guard because `V4_ARTIFACT_GLOBS` watches `results/phase20_*`,
not `scripts/`, and that the split is the same one `scripts/phase19_floor.py:10-48` records.

Bootstrap: `sys.path.insert(0, str(pathlib.Path(__file__).resolve().parent))` then
`import phase19_erasure as pin  # noqa: E402`. Module-level imports are limited to `hashlib`, `json`,
`pathlib`, `sys` plus that one. **THIS DRIVER'S OWN torch/numpy/personacore imports are
FUNCTION-LOCAL.**

State that property in exactly those terms and NO wider. Do NOT write "the module stays inert at
import and never pulls torch": `import phase19_erasure as pin` DOES pull torch, at
`scripts/phase19_erasure.py:120`. The AST criterion below would not catch the overstatement — it
scans only this file's own top-level nodes — so a false property would ship silently in the one file
whose entire purpose is provenance. The honest wording: this driver's own torch / numpy / personacore
imports are function-local, and `pin` is the single module-scope import that does pull torch
transitively. Name that as the ACCEPTED cost of reading `RETENTION_BIN`, `JSON_INDENT` and
`ModelConfig.block_size` from the pinned module instead of retyping them.

Module constants:
  - `RETENTION_FLOOR_PATH = pathlib.Path(__file__).resolve().parents[1] / "results" / "phase20_retention_floor.json"`
  - `SEED_ADAPTERS` — an ordered mapping from seed `1337` and `2024` to
    `checkpoints/phase19_erase_dialogue_floor_seed{seed}_adapter.pt`, built from the seed rather than
    typed twice.
  - `PUBLISHED_SEED_1337` — the three values the bit-identity control asserts, each with an inline
    comment naming `results/phase19_noise_floors.json` `retention_ppl_pre_erasure` as their source:
    `adapter_off = 3.891139975617828`, `adapter_on = 4.219759892336485`, `n_scored_tokens = 1000285`.
  - `RECIPE` — the D-06 second-bound block, mirroring `results/phase19_dialogue_floor.json`'s
    `recipe`: `arm_spec`, `arms` (the two `erase_dialogue_floor_seed*` names), `n_facts = 10`,
    `prefix = "phase19"`, `replay_ratio = 1.0`, `second_person = false`. This is where "measured on
    the v3.0 persona recipe" becomes machine-readable rather than prose.

Copy `_sha256(path)` and `_refuse(path)` from `scripts/phase19_run.py:246-247` and `:265-273`, with a
`[phase20_run]` prefix. `_refuse` has NO force flag: an existing artifact is recorded evidence and
the honest recovery is a reviewed deletion commit.

`retention_floor()`:
  1. `_refuse(RETENTION_FLOOR_PATH)` FIRST, before loading anything — an hour of MPS is not spent to
     discover the output already exists.
  2. Function-local imports: `numpy as np`, `phase14_recall as recall`, `torch`, and from
     `personacore`: `evaluation.perplexity.retention_perplexity`, `lora.adapter_disabled`,
     `preflight.preflight_device`, `provenance.git_sha`.
  3. `device = preflight_device(strict=True)["device"]`;
     `block_size = pin.ModelConfig.block_size`.
  4. For each seed in `SEED_ADAPTERS`, in ascending seed order:
     `model, cfg, tok, _forbid, _artifact = recall.load_adapted_model(device, str(adapter_path))` —
     the adapter path POSITIONAL, and `_forbid` bound and DISCARDED. Then
     `on_ppl, on_tokens = retention_perplexity(model, pin.RETENTION_BIN, block_size, device, tok)`
     and, inside `with adapter_disabled(model):`, the OFF pair with identical arguments. Pass `tok`,
     never `forbid` — see the read_first trap. `raise SystemExit` when `on_tokens != off_tokens`,
     with the message naming both denominators and stating that the delta would otherwise measure the
     corpus rather than the adapter.
  5. Window accounting, CHECKED not quoted, copied from `:814-826`: compute `n_corpus` from
     `np.memmap(pin.RETENTION_BIN, dtype=np.uint16, mode="r")`, `n_windows`, and `raise SystemExit`
     when `n_corpus - 1 != on_tokens`.
  6. THE BIT-IDENTITY CONTROL, before any new number is trusted: for seed 1337, `raise SystemExit`
     unless `adapter_off`, `adapter_on` and `n_scored_tokens` are EXACTLY equal (`==`, not a
     tolerance) to `PUBLISHED_SEED_1337`. The message must state that the instrument is verified, not
     assumed, and that a seed-2024 number produced by an unverified instrument has no provenance.
  7. Derive: `gap = on - off` per seed; `floor = abs(gap_1337 - gap_2024)`;
     `cap = pin.V20_EWC_RETENTION_PPL + pin.MARGIN_K * floor` — read through `pin` so the anchor is
     the IMPORTED `3.891140`, never the measured `3.891139975617828`;
     `borrowed_ratio = pin.V20_RETENTION_NOISE_FLOOR / floor`.
  8. Build the artifact dict, mirroring the `scripts/phase19_run.py:830-879` key register. It MUST
     carry: `call` (the literal invocation, so the reading is re-runnable from the artifact alone);
     per-seed `adapter_off` / `adapter_on` / `delta_on_minus_off` / `n_scored_tokens`, keyed by
     STRING seed as `results/phase19_dialogue_floor.json` does; `adapter` paths and their `_sha256`
     digests; `adapter_off_identical_across_seeds` (the D-04 identity flag);
     `seed_1337_remeasured_vs_published` in the `:578-593` shape with `abs_delta` and an exact
     `bit_identical` boolean; `retention_ppl_noise_floor` (the floor); `cap`; `cap_derivation` as a
     STRING with its citation, in the `"3.89114 + 2 x <floor> (scripts/erasure_gate.py:246)"` register;
     `borrowed_floor` and `borrowed_floor_ratio`; `borrowed_cap` (4.029) beside `cap` so the reader
     sees the re-measurement is TIGHTER, not looser; `bin`, `bin_bytes`, `corpus_tokens`,
     `n_windows`, `block_size`; `recipe` (the `RECIPE` constant); `seeds`; `device`, `torch`,
     `git_sha()`; and `"driver": "scripts/phase20_run.py retention-floor (UNPINNED)"`.
  9. Two long prose fields, the `adapted_precedent` slot's register:
     - `bounds` — D-06's two stated bounds, not glossed: `n = 2` seeds, one difference between two
       draws, no confidence interval (the same status the committed `dialogue_ppl_noise_floor`
       already has); and measured on the v3.0 persona recipe, the right REGIME rather than a v4.0
       ARM. Both are why the floor stays a required kwarg in the gate (D-07).
     - `governs` — the mirror of `scripts/phase19_run.py:602-607`: the MEASURED adapter-regime floor
       above is what `scripts/mitigation_gate.py::retention_cap` reads as its `retention_noise_floor`
       kwarg; `V20_RETENTION_NOISE_FLOOR = 0.068930` is the Phase 12 FULL-FINE-TUNE reading published
       beside it and it does NOT govern any v4.0 verdict. Also state that `erasure_gate`'s own
       `retention_cap` still uses `0.068930` and that this is NOT a correction to `23a830c`, which
       must not be amended.
  10. `print` one line per key in a small named subset (both seeds' readings, the floor, the cap,
      `n_scored_tokens`) before writing, in the `:880-881` register, then write with
      `json.dumps(..., indent=pin.JSON_INDENT, sort_keys=True)`.

`_TABLE = {"retention-floor": retention_floor}` plus the `:3030-3036` `__main__` dispatch: a usage
`SystemExit` when `len(sys.argv) != 2` or the key is unknown.

Note for the executor: this is a new `scripts/*.py` file and therefore enters the repo-wide AST scans
that already glob `scripts/*.py` (for example `tests/test_phase16_stats.py`'s `persona=` guard over
69 files). The full-suite acceptance command is what catches that; do not skip it.
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/python -c "
import ast,pathlib
t=ast.parse(pathlib.Path('scripts/phase20_run.py').read_text())
top=set()
for n in t.body:
    if isinstance(n,ast.Import): top.update(a.name.split('.')[0] for a in n.names)
    elif isinstance(n,ast.ImportFrom): top.add((n.module or '').split('.')[0])
assert 'torch' not in top and 'numpy' not in top and 'personacore' not in top, top
print('module-scope imports:', sorted(top))
src=pathlib.Path('scripts/phase20_run.py').read_text()
assert 'forbid_ids' not in src, 'retention_perplexity takes tokenizer, not forbid_ids'
assert 'UNPINNED' in src
print('OK')"</automated>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/python scripts/phase20_run.py 2>&1 | grep -q "usage" && echo "dispatch usage message OK"</automated>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/python -m pytest -q && .venv/bin/ruff check . && .venv/bin/ruff format --check .</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/phase20_run.py` exists; its module-scope imports are a subset of `{"hashlib", "json", "pathlib", "sys", "phase19_erasure"}` — no `torch`, `numpy` or `personacore` at module scope, verified by the AST command above.
    - `grep -c "forbid_ids" scripts/phase20_run.py` outputs `0` — the instrument trap is closed structurally, because `retention_perplexity` takes a tokenizer and has no `forbid_ids` parameter at all.
    - Both `retention_perplexity` call sites pass exactly five positional arguments with `tok` fifth and zero keyword arguments, verified by AST rather than by grep: `.venv/bin/python -c "import ast;t=ast.parse(open('scripts/phase20_run.py').read());c=[n for n in ast.walk(t) if isinstance(n,ast.Call) and getattr(n.func,'id',None)=='retention_perplexity'];assert len(c)==2,len(c);assert all(len(x.args)==5 and not x.keywords and getattr(x.args[4],'id',None)=='tok' for x in c),[(len(x.args),[getattr(a,'id',None) for a in x.args]) for x in c];print('two call sites, tok fifth positional')"`. A grep on the full single-line call CANNOT hold here: at the OFF call's indentation that line is 105 characters against `pyproject.toml`'s `line-length = 100`, so `ruff format` wraps it — which is exactly why `scripts/phase19_run.py` carries the ON call on one line at `:806` and the OFF call wrapped at `:808`.
    - `.venv/bin/python scripts/phase20_run.py` with no argument exits non-zero and prints a usage line naming `retention-floor`.
    - The module docstring contains the literals `UNPINNED` and `adds[-1]`.
    - `PUBLISHED_SEED_1337` contains exactly `3.891139975617828`, `4.219759892336485` and `1000285`.
    - `git ls-files 'results/phase20_*'` still prints nothing after this task's commit — the driver lands with NO results file.
    - `.venv/bin/python -m pytest -q` exits 0 and `.venv/bin/ruff check . && .venv/bin/ruff format --check .` exit 0.
    - `git status --porcelain pyproject.toml` prints nothing.
  </acceptance_criteria>
  <done>The unpinned driver is committed with function-local heavy imports, the `forbid`-into-`batch_size` trap structurally avoided, the bit-identity control armed, and no `results/` file created.</done>
</task>

<task type="auto">
  <name>Task 3: Run the driver on MPS, land the artifact strictly after the pin, and watch the guard stop being vacuous</name>
  <files>results/phase20_retention_floor.json</files>
  <read_first>
    - `results/phase19_noise_floors.json` — the `retention_ppl_pre_erasure` block; the published
      seed-1337 values the control reproduces are `adapter_off = 3.891139975617828`,
      `adapter_on = 4.219759892336485`, `delta_on_minus_off = 0.3286199167186572`,
      `n_scored_tokens = 1000285`. Note the value lives at
      `.dialogue_ppl_noise_floor.value`, NOT at the top level, if the dialogue floor is consulted.
    - `results/phase19_dialogue_floor.json` — the two-seed protocol shape with STRING seed keys.
    - `20-CONTEXT.md` D-06 (`:73-97`) — the expected readings: seed 1337 `adapter_on =
      4.219759892336485`, seed 2024 `adapter_on = 4.2284415113307245`, `adapter_off =
      3.891139975617828` for both, `n_targets = 1000285` each; floor `0.008681618994239138`; ratio
      `7.939763314393305x`; cap `3.9085032379884783`.
    - `20-CONTEXT.md` D-08 (`:101-105`) — strictly after, and why the discipline is tighter than the
      mechanism.
    - `tests/test_phase20_prereg.py` — the guard about to stop being vacuous.
  </read_first>
  <action>
Run `.venv/bin/python scripts/phase20_run.py retention-floor` on this machine (MPS). One run, no
retraining, no training of any kind — it re-reads two already-committed-arm adapters with a second
instrument, which is the same legitimacy D-13 granted the original retention probe.

Before committing anything, verify the produced JSON against the readings D-06 states, and treat any
disagreement as a STOP rather than as something to reconcile in prose:
  - seed 1337 `adapter_on == 4.219759892336485` and `adapter_off == 3.891139975617828` and
    `n_scored_tokens == 1000285` — the bit-identity control, which the driver already enforced;
  - seed 2024 `adapter_on == 4.2284415113307245`;
  - `retention_ppl_noise_floor == 0.008681618994239138`;
  - `cap == 3.9085032379884783`;
  - `adapter_off_identical_across_seeds` is `true`.

If seed 2024 does NOT reproduce, STOP and report. Do not adjust the expected value, do not widen a
tolerance, and do not commit the artifact. `20-RESEARCH.md` assumption A1 names this exact risk: the
seed-2024 reading has never been persisted, and if it is not reproducible then D-06's floor has no
committed provenance and the phase needs a decision, not a fix.

Then, and only then, `git add results/phase20_retention_floor.json` and commit it as its OWN commit —
D-08's strictly-after discipline. Do NOT amend, do NOT squash it into the driver's commit, and do NOT
touch `scripts/mitigation_gate.py` in the same commit or any later one.

After the commit, run the ancestry guard and record that it has stopped being vacuous: `checked` is
now non-zero and `bool(checked) == bool(tracked_artifacts)` is satisfied on the demanding side rather
than the trivial side.

Finally, update
`.planning/phases/20-pre-registration-the-three-condition-gate/20-VALIDATION.md`:
  - frontmatter `nyquist_compliant: true`, `wave_0_complete: true`, `status: complete`;
  - its Manual-Only Verifications table — record the D-32 driver run as DONE with the measured
    seed-1337 and seed-2024 readings, the floor and the cap (that row currently reads as pending);
  - its Per-Task Verification Map — flip every `Status` cell to done and every `File Exists` cell to
    the real state, since the map was filled at planning time against files that did not yet exist;
  - its § "Wave 1 is this phase's Wave 0" checklist — tick the `tests/test_phase20_prereg.py`
    created box, the only unticked one there and the thing `wave_0_complete: true` asserts;
  - its Validation Sign-Off checkboxes, and the `Approval:` line, which the planner left recording
    that the runtime and nyquist flags were pending this plan.
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/python -c "
import json
d=json.load(open('results/phase20_retention_floor.json'))
r=d['retention_ppl']
assert r['1337']['adapter_on']==4.219759892336485, r['1337']
assert r['1337']['adapter_off']==3.891139975617828, r['1337']
assert r['1337']['n_scored_tokens']==1000285, r['1337']
assert r['2024']['adapter_on']==4.2284415113307245, r['2024']
assert d['adapter_off_identical_across_seeds'] is True
assert d['retention_ppl_noise_floor']==0.008681618994239138, d['retention_ppl_noise_floor']
assert d['cap']==3.9085032379884783, d['cap']
assert 'UNPINNED' in d['driver']
assert 'does NOT govern' in d['governs'] or 'does not govern' in d['governs']
assert 'n = 2' in d['bounds'] or 'n=2' in d['bounds']
assert d['recipe']['replay_ratio']==1.0 and d['recipe']['n_facts']==10
print('artifact OK')"</automated>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/python -m pytest -q tests/test_phase20_prereg.py && .venv/bin/python -m pytest -q</automated>
    <automated>cd /Users/juliorcoelho/PersonaCore && test "$(git log --diff-filter=A --format=%H -- 'results/phase20_retention_floor.json' | tail -1)" != "$(git log --format=%H -- scripts/mitigation_gate.py | head -1)" && git merge-base --is-ancestor "$(git log --format=%H -- scripts/mitigation_gate.py | head -1)" "$(git log --diff-filter=A --format=%H -- 'results/phase20_retention_floor.json' | tail -1)" && echo "ordering: pin strictly precedes artifact"</automated>
  </verify>
  <acceptance_criteria>
    - `results/phase20_retention_floor.json` exists and every assertion in the JSON-verification command above passes, including `retention_ppl["2024"]["adapter_on"] == 4.2284415113307245` — the reading that previously existed only in `20-CONTEXT.md` now has re-runnable code provenance.
    - `retention_ppl_noise_floor == 0.008681618994239138` and `cap == 3.9085032379884783` — the cap anchored on the imported `3.891140`, matching `scripts/mitigation_gate.py::retention_cap(retention_noise_floor=0.008681618994239138)` exactly.
    - `adapter_off_identical_across_seeds` is `true`, and `seed_1337_remeasured_vs_published.bit_identical` is `true`.
    - `borrowed_floor_ratio == 7.939763314393305` and `borrowed_cap == 4.029`, both present beside `cap` so the re-measurement reads as TIGHTER, not looser.
    - `driver` contains `UNPINNED`; `governs` states that `V20_RETENTION_NOISE_FLOOR = 0.068930` does NOT govern any v4.0 verdict and that `23a830c` is not amended; `bounds` states both `n = 2` seeds and the v3.0 persona recipe.
    - The artifact is its OWN commit: `git log --diff-filter=A --format=%H -- results/phase20_retention_floor.json` returns a SHA that is NOT in `git log --format=%H -- scripts/mitigation_gate.py`, and `git merge-base --is-ancestor <newest pin commit> <artifact first add>` exits 0.
    - `git show --stat <artifact commit>` lists exactly one file.
    - `.venv/bin/python -m pytest -q tests/test_phase20_prereg.py` exits 0 AND the ancestry guard is no longer vacuous: `git ls-files 'results/phase20_*'` now returns exactly 1 path and `checked` is non-zero.
    - `.venv/bin/python -m pytest -q` (full suite) exits 0.
    - `git log --oneline -- scripts/mitigation_gate.py` is UNCHANGED from the sha256/commit list recorded at Task 1's checkpoint — the pin was not touched.
    - `20-VALIDATION.md` frontmatter now reads `nyquist_compliant: true` and `wave_0_complete: true`, its runtime row carries the measured number from plan 20-06, and its Manual-Only Verifications table records the driver run as DONE with the measured values.
  </acceptance_criteria>
  <done>The retention floor is measured, verified against the seed-1337 bit-identity control, published with its full embedded provenance and both stated bounds, and committed in its own commit strictly after a pushed and unmodified pin. The ancestry guard now demands a non-zero `checked` and is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a number in a discussion transcript ↔ a number in a committed artifact | The seed-2024 reading currently exists only in `20-CONTEXT.md`. Publishing it without re-running would give it no re-runnable provenance. |
| the pin's commit ↔ the artifact's first add | The single boundary the whole phase exists to establish, and the moment it becomes irreversible. |
| gitignored inputs ↔ CI | `checkpoints/` is gitignored, so CI can never re-derive this artifact; the JSON must carry everything a reader needs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-40 | Repudiation | a v4.0 number committed before the rule that judges it | mitigate | Task 1 is a BLOCKING checkpoint proving the pin is committed AND pushed and that `git log --diff-filter=A -- 'results/phase20_*'` is empty; Task 3 commits the artifact as its own commit and re-asserts the ancestry mechanically |
| T-20-41 | Tampering | the pin edited after the artifact exists | mitigate | Task 1 forces the human to declare the pin final while an edit is still legal; Task 3's acceptance re-checks `git log --oneline -- scripts/mitigation_gate.py` against the checkpoint's recorded list. After this plan the only correction path is `_addendum.py` plus a tripwire (D-24) |
| T-20-42 | Spoofing | a transcribed number passing as a measurement | mitigate | D-32 — the artifact is produced by a committed driver; the seed-1337 bit-identity control (`==`, not a tolerance) runs BEFORE seed 2024 is trusted, so the instrument is verified rather than assumed |
| T-20-43 | Tampering | `forbid` silently bound to `batch_size`, corrupting the floor | mitigate | The driver is written off `scripts/phase19_run.py::retention`, not `dialogue_ppl_pair`; `grep -c "forbid_ids"` must output `0` and the two call sites are AST-asserted to pass `tok` as the fifth positional — a grep cannot hold, because `ruff format` wraps the OFF call at `line-length = 100` |
| T-20-44 | Tampering | an ON/OFF pair scored over different denominators | mitigate | Inline `raise SystemExit` on `on_tokens != off_tokens`, plus the window-accounting check `n_corpus - 1 == on_tokens` — checked rather than quoted |
| T-20-45 | Information disclosure | an artifact unreproducible from a fresh clone | accept | `checkpoints/` is gitignored by design (`.gitignore:14-15`). Accepted and mitigated by EMBEDDING every reading, denominator, seed, adapter path, adapter sha256, git SHA, torch version and device in the JSON |
| T-20-46 | Repudiation | a result silently reconciled when seed 2024 fails to reproduce | mitigate | Task 3's action makes non-reproduction a STOP, naming `20-RESEARCH.md` assumption A1: do not adjust the expected value, do not widen a tolerance, do not commit |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q` exits 0 — with the guard now non-vacuous.
- `.venv/bin/python scripts/mitigation_gate.py` exits 0.
- `.venv/bin/ruff check . && .venv/bin/ruff format --check .` exit 0.
- `git status --porcelain pyproject.toml` prints nothing.
- `git log --diff-filter=A --format=%H -- 'results/phase20_*'` returns exactly ONE SHA, and every
  commit in `git log --format=%H -- scripts/mitigation_gate.py` is an ancestor of it.
- `git show --stat` on that SHA lists exactly one file.
</verification>

<success_criteria>
- `results/phase20_retention_floor.json` exists, reproduces `seed_1337 = 4.219759892336485` and
  `seed_2024 = 4.2284415113307245` from real code, and publishes
  `retention_ppl_noise_floor = 0.008681618994239138` with `cap = 3.9085032379884783`.
- The pin was committed and pushed BEFORE the artifact's first add, and was not touched afterwards.
- `test_phase20_prereg_is_frozen_before_every_phase20_result` is green with `tracked_artifacts == 1`
  and `checked` non-zero — the guard has stopped being vacuous.
- `scripts/phase20_run.py` is unpinned, inert at import, and pulls torch only inside its function.
- `pyproject.toml` is still byte-unchanged (RPT-03, four milestones).
</success_criteria>

<output>
Create `.planning/phases/20-pre-registration-the-three-condition-gate/20-07-SUMMARY.md` when done.
Record: the driver's full stdout; the two seeds' four readings with their shared denominator; the
seed-1337 bit-identity comparison; the artifact's commit SHA and the pin's newest commit SHA with the
`merge-base --is-ancestor` result between them; the guard's `checked` and `tracked_artifacts` counts
before and after; and an explicit statement that `scripts/mitigation_gate.py` is now permanently
uneditable and every correction from here is a dated continuation via `scripts/_addendum.py`.
</output>
