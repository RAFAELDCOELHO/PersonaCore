---
phase: 20-pre-registration-the-three-condition-gate
plan: 03
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - scripts/_prose.py
  - tests/test_phase20_prereg.py
autonomous: true
requirements: [RPT-02]

must_haves:
  truths:
    - "RPT-02 / D-23: `scripts/_prose.py::normalized` exists, is phase-neutral, imports NOTHING, and finds a line-wrapped phrase that a single-line `grep -c` reports as absent — v3.0's recorded lesson converted into a mechanism instead of staying a retrospective note."
    - "The RPT-02 proof is DIFFERENTIAL: the naive method is shown returning 0 on the same bytes on which `normalized(phrase) in normalized(text)` is True. Without the negative control the test degrades into 'a string containment check passes'."
    - "The fixture literal is the REAL v3.0 incident string `the three\\nreductions` (`RETROSPECTIVE.md:179-181`, `v3.0-MILESTONE-AUDIT.md:104-111`), never a synthetic one — the D-30 precedent that a defect which actually happened is not hypothetical."
    - "D-23: the LEADING UNDERSCORE is the mechanism that keeps `_prose.py` outside every pin's scanned set, verified by fnmatch against `phase16_*.py` through `phase20_*.py`. A phase-neutral utility must keep serving Phases 21-28 without inheriting an immutability that only makes sense for the judgment rule."
    - "D-22: `V4_ARTIFACT_GLOBS` is proven to SEE the `phase20_*` prefix by a committed RED-then-GREEN four-state fixture in a throwaway repo, re-executed every CI run — not assumed correct by reading the pattern."
    - "D-22: the fixture calls the SAME `_assert_ordering_holds` the live guard calls, parameterized on repo root, so what is proven is the real guard rather than a lookalike."
    - "D-22 / D-24: the four measured states are reproduced — probe-before-pin RED via `assert prereg_commits` (a different red), pin-second RED with the ordering failure, `git rm` GREEN at tracked=0 (the red is reversible only by not having the artifact), re-add at the identical path RED again because `adds[-1]` is unchanged (laundering is impossible)."
    - "The real repository's history stays clean: no v4.0-named probe ever appears in `git log --diff-filter=A -- 'results/phase20_*'`."
  artifacts:
    - path: "scripts/_prose.py"
      provides: "The ONE whitespace-normalizing prose read, phase-neutral and import-free"
      contains: "def normalized"
    - path: "tests/test_phase20_prereg.py"
      provides: "The RPT-02 differential proof and the D-22 throwaway-repo RED-then-GREEN fixture"
      contains: "tmp_path"
  key_links:
    - from: "tests/test_phase20_prereg.py"
      to: "scripts/_prose.py::normalized"
      via: "flat-module import after the sys.path insert"
      pattern: "import _prose"
    - from: "the D-22 fixture"
      to: "_assert_ordering_holds"
      via: "the same helper the live guard calls, with root=tmp_path"
      pattern: "_assert_ordering_holds"
---

<objective>
Ship the whitespace-normalizing prose read v3.0 recorded and never built, and prove the ordering
guard actually sees the `phase20_*` prefix by watching it go RED and GREEN in a throwaway repo.

Purpose: RPT-02 exists because a single-line `grep -c "three reductions"` returned 0 on a file that
contained the phrase, line-wrapped. `.planning/research/PITFALLS.md:1048` already prescribes the
five-line fix and names this phase to build it in. D-22 exists because a guard nobody has watched
fail is a guard nobody has verified — and reading a glob pattern is not the same as demonstrating it
matches.

Output: `scripts/_prose.py`, plus two tests appended to `tests/test_phase20_prereg.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-pre-registration-the-three-condition-gate/20-CONTEXT.md
@.planning/phases/20-pre-registration-the-three-condition-gate/20-PATTERNS.md
@.planning/phases/20-pre-registration-the-three-condition-gate/20-RESEARCH.md
@.planning/phases/20-pre-registration-the-three-condition-gate/20-01-SUMMARY.md
</context>

<interfaces>
<!-- Created by plan 20-01, in tests/test_phase20_prereg.py. Spell these exactly. -->
`_ROOT`, `_git(*args, cwd=_ROOT)`, `PHASE20_PREREG_ARTIFACT = "scripts/mitigation_gate.py"`,
`V4_ARTIFACT_GLOBS = ("results/phase20_*",)`,
`_assert_ordering_holds(*, root, prereg_artifact, artifact_glob, globs)`,
`test_phase20_prereg_is_frozen_before_every_phase20_result`.

IMPORT ACCUMULATION (20-01's ledger, authoritative): `tests/test_phase20_prereg.py` currently
imports EXACTLY `pathlib` and `subprocess`, and has NO `sys.path` bootstrap. **Task 1 of THIS plan
adds `ast`, `sys`, the bootstrap and `import _prose`; Task 2 adds `pytest`.** Each lands with its
first consumer because `pyproject.toml:44` sets `select = ["E", "F", "W", "I"]` — a name imported
ahead of its consumer is an F401 and every task here gates on `ruff check .` exiting 0.

Names this plan CREATES: `scripts/_prose.py::normalized`; in the test module `_PROSE_PATH`,
`WRAPPED_INCIDENT_TEXT`, `WRAPPED_INCIDENT_PHRASE`,
`test_normalized_finds_a_line_wrapped_phrase_grep_reports_absent`,
`test_prose_module_imports_nothing`,
`test_phase20_glob_sees_the_phase20_prefix_red_then_green`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: scripts/_prose.py::normalized and its differential RPT-02 proof</name>
  <files>scripts/_prose.py, tests/test_phase20_prereg.py</files>
  <read_first>
    - `scripts/_verdict.py` — read in full (30 lines). This is the exact structural analog: a
      phase-neutral `scripts/` helper with a leading underscore, one import, one compiled constant
      and one function. Copy four properties: the docstring opening "The ONE copy of ..." naming
      every sharer; a "**Why one copy.**" paragraph naming the MEASURED defect it closes; the
      "Phase-neutral and dependency-free on purpose" sentence; and one exported name.
    - `.planning/RETROSPECTIVE.md:179-181` — the primary record of the incident: "three reductions"
      is line-wrapped in the source so `grep -c "three reductions"` returns 0 on a file that
      contains it; the pre-correction sweep had to be whitespace-normalised to see it.
    - `.planning/milestones/v3.0-MILESTONE-AUDIT.md:104-111` — defect W2's resolution, which gives
      the concrete failing string as the literal `"the three\nreductions"`.
    - `.planning/research/PITFALLS.md:1048` — the prescribed implementation, verbatim, including
      "build it in P20 so it is available all milestone".
    - `20-RESEARCH.md` §5 — the four assertions the RPT-02 test must make, and why assertion 1 (the
      negative control) is what stops the test degrading.
    - `scripts/_addendum.py:47` — how a sibling in `scripts/` imports a leading-underscore helper
      (`import _verdict  # noqa: E402`).
  </read_first>
  <action>
Create `scripts/_prose.py` with exactly one exported name, `normalized(text)`, returning
`" ".join(text.split())`. Zero imports — `str.split()` with no argument splits on any run of
whitespace (spaces, tabs, `\n`, `\r\n`, form feeds) and discards empty strings, collapsing newlines,
runs of spaces and leading/trailing whitespace in one operation. No `re`, no `search()` wrapper, no
case-fold flag, no `count()` helper: callers write `normalized(phrase) in normalized(text)`. Add more
only when a second call site actually needs it.

Module docstring, in the `scripts/_verdict.py` register:
  - Opens "The ONE copy of the whitespace-normalizing prose read, shared by every correction sweep."
  - A "**Why one copy.**" paragraph naming the measured defect: `RETROSPECTIVE.md:179-181` records
    that `grep -c "three reductions"` returned 0 on a file containing the phrase because it is
    line-wrapped as `"the three\nreductions"`; the v3.0 pre-correction sweep had to be
    whitespace-normalised to see it, and the lesson was recorded and never converted into a
    mechanism. RPT-02 is that conversion.
  - The D-23 sentence: phase-neutral and dependency-free ON PURPOSE, and the LEADING UNDERSCORE is
    what keeps this file outside every pin's scanned set (`phase16_*.py` ... `phase20_*.py` match it
    with none of their globs), so a general utility does not freeze behind the first artifact of one
    specific phase and can keep serving Phases 21-28. Record the honest caveat this repository
    already states: the precedent is STRUCTURAL (fnmatch), not historical — `_addendum.py`'s last
    commit predates the first `results/phase19_*` add, so neither helper has actually been edited
    post-artifact.

Append to `tests/test_phase20_prereg.py`:
  - `_PROSE_PATH = _ROOT / "scripts" / "_prose.py"`.
  - `import ast` and `import sys` — this task is their FIRST consumer in the test module. Plan
    20-01 Task 3 imported only `pathlib` and `subprocess` and deliberately left these out, because an
    import ahead of its consumer is an F401 against a `ruff check .` gate. `sys` is needed for the
    bootstrap below; `ast` for `test_prose_module_imports_nothing`.
  - The `sys.path` insert itself: plan 20-01 needed none, so add it here in the
    `tests/test_phase18_prereg.py:48-52` shape, then `import _prose  # noqa: E402`.
  - `WRAPPED_INCIDENT_TEXT` and `WRAPPED_INCIDENT_PHRASE` as module-level constants built from the
    REAL v3.0 incident: the text embeds the literal `"the three\nreductions"` inside a sentence, and
    the phrase is `"the three reductions"`. Comment them with the `RETROSPECTIVE.md:179-181` and
    `v3.0-MILESTONE-AUDIT.md:104-111` citations and the D-30 register — a defect that actually
    happened is not hypothetical.
  - `test_normalized_finds_a_line_wrapped_phrase_grep_reports_absent()` asserting all four of
    `20-RESEARCH.md` §5: (1) the NEGATIVE CONTROL — `WRAPPED_INCIDENT_TEXT.count(WRAPPED_INCIDENT_PHRASE) == 0`,
    with an assertion message stating that this is what proves the naive method fails; (2) the
    positive — `_prose.normalized(WRAPPED_INCIDENT_PHRASE) in _prose.normalized(WRAPPED_INCIDENT_TEXT)`;
    (3) idempotence — `normalized(normalized(t)) == normalized(t)`; (4) tabs and repeated spaces
    collapse identically to newlines, so the helper is not newline-specific.
  - `test_prose_module_imports_nothing()` — an AST scan over `_PROSE_PATH` asserting the set of
    `ast.Import` and `ast.ImportFrom` nodes is EMPTY. This is the RPT-03 layer for this file: the
    `pyproject.toml` sha256 pin catches a declared dependency, only an AST scan catches a
    stdlib-adjacent import inside a helper.
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/python -m pytest -q tests/test_phase20_prereg.py -k "normalized or imports_nothing"</automated>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/python -c "import sys;sys.path.insert(0,'scripts');import _prose;t='a the three\nreductions b';p='the three reductions';assert t.count(p)==0;assert _prose.normalized(p) in _prose.normalized(t);assert _prose.normalized(_prose.normalized(t))==_prose.normalized(t);print('OK')" && grep -c "^import\|^from" scripts/_prose.py | grep -qx 0 && echo "zero imports"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/_prose.py` exists and `.venv/bin/python -c "import ast;t=ast.parse(open('scripts/_prose.py').read());print(sum(1 for n in ast.walk(t) if isinstance(n,(ast.Import,ast.ImportFrom))))"` prints `0`.
    - `.venv/bin/python -c "import ast;t=ast.parse(open('scripts/_prose.py').read());print(sorted(n.name for n in ast.walk(t) if isinstance(n,ast.FunctionDef)))"` prints `['normalized']` — exactly one function.
    - `_prose.normalized("a\nb")` returns the exact string `"a b"`; `_prose.normalized("  a \t\n b  ")` returns `"a b"`.
    - On the committed fixture: `WRAPPED_INCIDENT_TEXT.count(WRAPPED_INCIDENT_PHRASE) == 0` is `True` AND `normalized(WRAPPED_INCIDENT_PHRASE) in normalized(WRAPPED_INCIDENT_TEXT)` is `True`.
    - `scripts/_prose.py` docstring contains the literals `the three` and `D-23` and the phrase `dependency-free`.
    - `.venv/bin/python -m pytest -q tests/test_phase20_prereg.py` exits 0 — the per-task gate (`20-VALIDATION.md` § Sampling Rate); the FULL suite runs once at this plan's `<verification>` wave boundary.
    - `.venv/bin/python -c "import ast;t=ast.parse(open('tests/test_phase20_prereg.py').read());print(sorted({a.name.split('.')[0] for n in ast.walk(t) if isinstance(n,ast.Import) for a in n.names} | {(n.module or '').split('.')[0] for n in ast.walk(t) if isinstance(n,ast.ImportFrom)}))"` prints exactly `['_prose', 'ast', 'pathlib', 'subprocess', 'sys']` — `pytest` still absent, it arrives in Task 2 with `pytest.raises`.
    - `.venv/bin/ruff check . && .venv/bin/ruff format --check .` exit 0 — in particular no F401.
    - `git status --porcelain pyproject.toml` prints nothing.
  </acceptance_criteria>
  <done>`normalized` exists as a zero-import one-liner, and its test proves the naive method returning 0 on the same bytes where the helper succeeds — the differential, not a bare containment check.</done>
</task>

<task type="auto">
  <name>Task 2: The D-22 RED-then-GREEN four-state fixture in a throwaway repo</name>
  <files>tests/test_phase20_prereg.py</files>
  <read_first>
    - `tests/test_phase16_prereg.py:406-497` — again, and specifically the `adds[-1]` comment at
      `:468-470` and the EQUIVALENCE assert at `:492`, because the fixture's four states are exactly
      the four ways those two lines can be exercised.
    - `20-RESEARCH.md` §3f — the four states MEASURED in throwaway repos this session, the fixture
      recipe (`tempfile` / pytest `tmp_path`, `GIT_AUTHOR_*` and `GIT_COMMITTER_*` env), the measured
      reflexivity of `git merge-base --is-ancestor X X` (exit 0), and TWO gotchas: `git rm` of the
      last file in `results/` removes the directory so a re-add needs `mkdir -p`; and the ordering
      assertion is `subprocess.run(check=True)`, so a violation surfaces as a bare
      `CalledProcessError` with no message.
    - `20-CONTEXT.md` D-22 (`:218-233`) — the four-state table with its expected results, and the
      requirement that the real project history stays clean.
    - `tests/conftest.py` — read it to confirm it provides only `simulate_pascal` and `fake_lm`;
      neither applies. Use pytest's built-in `tmp_path`.
  </read_first>
  <action>
Append `test_phase20_glob_sees_the_phase20_prefix_red_then_green(tmp_path)` to
`tests/test_phase20_prereg.py`.

Add `import pytest` in this task — it is `pytest.raises`'s first consumer in the module (20-01 and
Task 1 above needed none). Nothing else is added: `ast`, `sys`, `pathlib`, `subprocess` and `_prose`
are already there.

Build a throwaway repository under `tmp_path` with `git init -q` and a fixed identity env
(`GIT_AUTHOR_NAME` / `GIT_AUTHOR_EMAIL` / `GIT_COMMITTER_NAME` / `GIT_COMMITTER_EMAIL` all set to a
test value), so the fixture never depends on the developer's global git config. Every git call goes
through the existing `_git(..., cwd=tmp_path)` — the SAME helper the live guard uses. No `rm -rf`,
no `shell=True`, no touching of the real repository at any point.

Drive the four measured states, asserting each in turn against
`_assert_ordering_holds(root=tmp_path, prereg_artifact=PHASE20_PREREG_ARTIFACT,
artifact_glob="results/phase20_*", globs=V4_ARTIFACT_GLOBS)` — the SAME implementation the live guard
calls, so what is proven here is the real guard and not a lookalike:

  1. **Probe committed, no pin yet.** Add and commit `results/phase20_probe.json` in the throwaway
     repo. The helper must fail on `assert prereg_commits` — assert `pytest.raises(AssertionError)`
     and assert the message names `scripts/mitigation_gate.py`. Record in a comment that this is a
     DIFFERENT red from the ordering red, which is the point of distinguishing them.
  2. **Pin committed second.** Add and commit `scripts/mitigation_gate.py` in the throwaway repo
     (any content). The helper must now fail on the ORDERING check — assert
     `pytest.raises(subprocess.CalledProcessError)`. **This is the proof the glob sees the
     `phase20_` prefix**: an ordering failure can only be reached if `git ls-files
     "results/phase20_*"` matched the probe. Assert additionally that
     `_git("ls-files", "results/phase20_*", cwd=tmp_path)` is non-empty at this state, so the proof
     is stated rather than inferred.
  3. **`git rm` the probe, do not re-add.** Commit the deletion. The helper must now PASS, and
     `_git("ls-files", "results/phase20_*", cwd=tmp_path)` must be empty — tracked=0, so the red is
     reversible only by not having the artifact.
  4. **Re-add at the identical path.** `mkdir -p` the `results/` directory first (measured gotcha:
     `git rm` of the last file removes it), write the same path, commit. The helper must be RED
     again with `subprocess.CalledProcessError`, and assert that
     `_git("log", "--diff-filter=A", "--format=%H", "--", "results/phase20_probe.json", cwd=tmp_path).split()[-1]`
     is byte-identical to the SHA captured at state 1 — proving `adds[-1]` is the earliest add and
     that delete-and-re-add cannot launder the ordering.

Then add a fifth, closing assertion: after deleting the probe again and committing a real-shaped
artifact whose first add comes AFTER the pin commit, the helper PASSES with exactly one tracked
artifact. That is the GREEN half of RED-then-GREEN.

Docstring must record (a) that this fixture is re-executed every CI run rather than being a one-time
manual observation, (b) the measured reflexivity of `git merge-base --is-ancestor X X` (exit 0), so
gate and artifact in the same commit would PASS and D-08's strictly-after rule is a DISCIPLINE
tighter than the mechanism — recorded so a later reader treats same-commit as neither a defect nor a
licence, and (c) that the real repository's history is never touched: every probe lives in
`tmp_path` and `git log --diff-filter=A -- 'results/phase20_*'` on the real repo must stay empty.
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/python -m pytest -q tests/test_phase20_prereg.py</automated>
    <automated>cd /Users/juliorcoelho/PersonaCore && git log --diff-filter=A --format=%H -- 'results/phase20_*' | wc -l | tr -d ' ' | grep -qx 0 && git ls-files 'results/phase20_*' | wc -l | tr -d ' ' | grep -qx 0 && echo "real history still clean"</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest -q tests/test_phase20_prereg.py` exits 0 and reports at least 4 passing tests.
    - The fixture asserts `pytest.raises(AssertionError)` at state 1 and `pytest.raises(subprocess.CalledProcessError)` at states 2 and 4 — three distinct observed failures, two of them a DIFFERENT failure type from the other.
    - At state 2 the fixture asserts `_git("ls-files", "results/phase20_*", cwd=tmp_path)` is non-empty before asserting the ordering failure.
    - At state 4 the fixture asserts the earliest add SHA equals the SHA captured at state 1 (string equality on the full 40-character hash).
    - The fixture calls `_assert_ordering_holds` — confirmed by `.venv/bin/python -c "src=open('tests/test_phase20_prereg.py').read();print(src.count('_assert_ordering_holds'))"` printing at least `6` (one def, one live call, and the fixture's per-state calls).
    - `grep -c "shell=True" tests/test_phase20_prereg.py` outputs `0` and `grep -c "rm -rf" tests/test_phase20_prereg.py` outputs `0`.
    - After the full test run, on the REAL repository: `git log --diff-filter=A -- 'results/phase20_*'` prints nothing and `git ls-files 'results/phase20_*'` prints nothing.
    - `.venv/bin/python -c "import ast;t=ast.parse(open('tests/test_phase20_prereg.py').read());print(sorted({a.name.split('.')[0] for n in ast.walk(t) if isinstance(n,ast.Import) for a in n.names} | {(n.module or '').split('.')[0] for n in ast.walk(t) if isinstance(n,ast.ImportFrom)}))"` prints exactly `['_prose', 'ast', 'pathlib', 'pytest', 'subprocess', 'sys']` — `pytest` added here, `fnmatch` / `json` / `erasure_gate` / `mitigation_gate` still absent until 20-06.
    - `.venv/bin/ruff check .` exits 0 — no F401 from an import that outran its consumer.
  </acceptance_criteria>
  <done>The four measured guard states are reproduced as a committed, CI-re-executed fixture against the real `_assert_ordering_holds`; the prefix is demonstrated rather than assumed; laundering is shown impossible; and the real repository's history is verified untouched before and after.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the guard's pattern ↔ the guard's actual match set | A glob that reads correctly and matches nothing is green over nothing. This is the boundary D-22 exists to instrument. |
| the throwaway repo ↔ the real repository | Every probe must live entirely inside `tmp_path`; a v4.0-named probe reaching the real history would permanently corrupt the phase's own evidence. |
| prose in a report ↔ the check that reads it | A whitespace-naive read silently reports a real defect as absent. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-12 | Repudiation | `V4_ARTIFACT_GLOBS` never demonstrated to match | mitigate | The four-state fixture asserts a NON-EMPTY `git ls-files` at state 2 before asserting the ordering failure — the prefix match is stated, not inferred |
| T-20-13 | Tampering | delete-and-re-add to reset a red guard | mitigate | State 4 asserts the earliest-add SHA is byte-identical to state 1's. Measured irreversible; the assertion makes it a standing CI fact, not a one-time observation |
| T-20-14 | Tampering | a v4.0-named probe entering the real history | mitigate | Every probe is created under pytest's `tmp_path`; no `shell=True`, no `rm -rf`; `git log --diff-filter=A -- 'results/phase20_*'` is re-verified empty as an acceptance criterion after the test run |
| T-20-15 | Repudiation | a correction sweep reporting a real defect as absent | mitigate | `scripts/_prose.py::normalized` plus the DIFFERENTIAL test that shows the naive method returning 0 on the same bytes |
| T-20-16 | Tampering | `_prose.py` acquiring a dependency | mitigate | AST scan asserting zero `Import`/`ImportFrom` nodes; `tests/test_package.py`'s `pyproject.toml` sha256 pin covers the declared surface (RPT-03) |
| T-20-17 | Elevation of privilege | gate and artifact landing in the SAME commit | accept | `git merge-base --is-ancestor X X` is reflexive (measured, exit 0), so same-commit passes. D-08's strictly-after rule is a recorded discipline; the fixture's docstring states this so a later reader treats same-commit as neither a defect nor a licence |
</threat_model>

<verification>
WAVE BOUNDARY. Per `20-VALIDATION.md` § Sampling Rate the per-TASK gate is
`.venv/bin/python -m pytest -q tests/test_phase20_prereg.py` (seconds); the FULL suite runs
once here. Measured pre-phase baseline: `845 passed, 1 skipped` in `201.99s`.

- `.venv/bin/python -m pytest -q` exits 0.
- `.venv/bin/ruff check . && .venv/bin/ruff format --check .` exit 0.
- `git status --porcelain pyproject.toml` prints nothing.
- `git log --diff-filter=A -- 'results/phase20_*'` prints nothing (checked BEFORE and AFTER the run).
</verification>

<success_criteria>
- `scripts/_prose.py` exists, exports exactly `normalized`, and imports nothing.
- The RPT-02 test proves the naive read returns 0 where the normalized read succeeds, on the real
  v3.0 incident string.
- All four D-22 states are reproduced against the shared `_assert_ordering_holds`, including the
  earliest-add SHA identity at state 4.
- The real repository's `results/phase20_*` add history is still empty.
</success_criteria>

<output>
Create `.planning/phases/20-pre-registration-the-three-condition-gate/20-03-SUMMARY.md` when done.
Record the four state outcomes with the exception type observed at each, the state-1 and state-4 add
SHAs, and the before/after emptiness of `git log --diff-filter=A -- 'results/phase20_*'`.
</output>
