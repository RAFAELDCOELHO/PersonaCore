---
phase: 20-pre-registration-the-three-condition-gate
plan: 14
type: execute
wave: 13
depends_on: ["20-13"]
files_modified:
  - scripts/phase20_gate_coverage.py
  - tests/test_phase20_correction.py
autonomous: true
gap_closure: true
requirements: [GATE-06]
user_setup: []

must_haves:
  truths:
    - "`coverage_verdict(..., sweep_heldout_recalls=(float('nan'), 0.28), ...)` raises SystemExit; the same call at `(0.30, 0.28)` returns `(False, ('heldout_recall',), <sentence>)`"
    - "`corrected_point_verdict` on FIXTURE_CLEARING_POINT at the (1,3)/(104,104) sweep returns INCONCLUSIVE with a GATE-06 reason at `sweep_heldout_recalls=(0.30, 0.28)`, and RAISES SystemExit at `(nan, 0.28)` and at `(42.0, -99.0)` — it no longer returns PASS on either"
    - "`coverage_verdict(..., sweep_extraction_successes=(0.0, 1.0), ...)` raises SystemExit — the module's own SUPERSEDED_SWEEP_SENTINEL can no longer pass as counts; `(True, False)` raises too"
    - "Both new guards were OBSERVED failing: each `_prove` deleted in turn, the named test watched RED, the module restored so `git diff --exit-code -- scripts/phase20_gate_coverage.py` returns 0 and `shasum -a 256` matches the pre-break digest"
    - "`git diff --exit-code -- scripts/mitigation_gate.py scripts/erasure_gate.py` returns 0"
    - "`pytest tests/test_phase20_correction.py tests/test_phase20_prereg.py -q` reports at least 31 passed, zero skipped, zero xfail; full suite at least 876 passed / 1 skipped"
  artifacts:
    - path: "scripts/phase20_gate_coverage.py"
      provides: "per-element value guards on both Y legs, and a count guard enforced by type"
      contains: "0.0 <= v <= 1.0"
    - path: "tests/test_phase20_correction.py"
      provides: "the measured route-level differential and the sentinel-as-counts refusal"
      contains: "test_a_recall_outside_the_unit_interval_cannot_manufacture_y_coverage"
  key_links:
    - from: "scripts/phase20_gate_coverage.py::coverage_verdict"
      to: "sweep_taught_recalls / sweep_heldout_recalls values"
      via: "a per-element _prove placed beside the extraction leg's own value checks at :248-277"
      pattern: "isinstance\\(v, \\(int, float\\)\\) and 0\\.0 <= v <= 1\\.0"
    - from: "tests/test_phase20_correction.py"
      to: "scripts/phase20_gate_coverage.py::corrected_point_verdict"
      via: "the route-level differential — honest axis reaches a finding, more-truncated axis is refused"
      pattern: "float\\(.nan.\\)"
---

<objective>
Close gap 1 (GATE-06 / ROADMAP SC3's Y clause) and GC-04. Give the two Y sweep legs the same
per-element discipline the extraction leg already has, enforce the extraction count guard BY TYPE,
and arm both with the measured differential — then watch both fail before trusting either.

Purpose: `coverage_verdict:241-247` validates `sweep_taught_recalls` and `sweep_heldout_recalls`
for LENGTH ONLY and `:296-297` consumes their values raw, while the extraction axis on the same
function spends three `_prove` calls justified at `:274-276` on the grounds that "a coverage
finding attributed to the data when it was produced by the criterion" is unacceptable. Measured
against HEAD: on `FIXTURE_CLEARING_POINT` at the `(1,3)/(104,104)` sweep with the fixture's own
`sweep_taught_recalls = (0.45, 0.2)` and `Y_heldout = 0.24499999999999997`, the honest held-out
axis `(0.30, 0.28)` returns `INCONCLUSIVE` with a GATE-06 reason, and the STRICTLY MORE truncated
`(nan, 0.28)` returns `PASS` with no GATE-06 reason at all — `coverage_verdict` reports it fully
covered, `(True, ())`. `nan >= 0.24499999999999997` is `False`, so the NaN is counted as a FAILING
point and actively manufactures the bracket. A NaN recall is what `0/0` produces from an empty
held-out question set, so this is a reachable input.

Output: two `_prove` calls in `coverage_verdict`, one changed predicate, three new test cases, and
two watched-RED breaks with byte-identical restores.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/20-pre-registration-the-three-condition-gate/20-CONTEXT.md
@.planning/phases/20-pre-registration-the-three-condition-gate/20-VERIFICATION.md
@.planning/phases/20-pre-registration-the-three-condition-gate/20-REVIEW-GAP-CLOSURE.md
@.planning/phases/20-pre-registration-the-three-condition-gate/20-13-SUMMARY.md

<interfaces>
<!-- Resolved from the committed files. Every identifier below is quoted as it appears today. -->

`scripts/phase20_gate_coverage.py`:
  :111  `def _prove(condition, message):`  — raises `SystemExit(f"[phase20_gate_coverage] {message}")`
  :194  `COVERAGE_STATISTIC_BY_AXIS = (("extraction", ...), ("taught_recall", ...), ("heldout_recall", ...))`
  :201  `def coverage_verdict(*, sweep_extraction_successes, sweep_extraction_questions,
         sweep_taught_recalls, sweep_heldout_recalls, extraction_ceiling_value, y_taught, y_heldout)`
  :233-240  `_prove` — extraction sweep length equality
  :241-247  `_prove` — recall sweep LENGTH equality (the WR-09 message). THE VALUES ARE NEVER CHECKED.
  :248-255  `_prove` — `all(n > 0 for n in sweep_extraction_questions)`
  :256      `for k, n in zip(sweep_extraction_successes, sweep_extraction_questions):`
  :257      `whole = isinstance(k, int) or (isinstance(k, float) and k.is_integer())`   <-- GC-04
  :258-270  `_prove(whole and 0 <= k <= n, ...)` — the RATE-vs-COUNT message; keep it, it is asserted
  :271-277  `_prove(0.0 < extraction_ceiling_value, ...)` — the "produced by the criterion" message
  :283-287  `axes = (("extraction", "X", ...), ("taught_recall", "Y_taught", y_taught,
             list(sweep_taught_recalls)), ("heldout_recall", "Y_heldout", y_heldout,
             list(sweep_heldout_recalls)))`
  :291-306  the per-axis clearing/failing loop
  :423      `SUPERSEDED_SWEEP_SENTINEL = (0.0, 1.0)`  — a RATE-space constant in this same file

`tests/test_phase20_correction.py`:
  :97-100   `N = 104`, `DIRECTION_I_SUCCESSES = (1, 3)`, `DIRECTION_II_SUCCESSES = (3, 11)`,
            `QUESTIONS = (N, N)`
  :106-107  `DEFAULT_HELDOUT_SWEEP = (0.30, 0.20)`, `DEFAULT_RETENTION_PROVENANCE = {...}`
  :119      `def _corrected_call(fixture_name, **overrides):` — merges `overrides` into `base` then
            splats once. THE MERGE FORM IS LOAD-BEARING; do not rewrite it.
  :155      `def _ceiling_for(fixture_name):` — X by CALLING `extraction_ceiling`, never typed
  :170      `def _names_gate06(reasons):` — indices of reasons containing `"GATE-06"`
  :305      `def test_the_sanctioned_route_cannot_be_handed_raw_rates():` — asserts the refusal
            message contains `"RATE"` and `"COUNT"` (:346) and drives `(-1, 3)` / `(3, N+1)`
  :759      `def test_the_retention_floor_tripwire_is_the_only_route_to_a_verdict():`

Fixture facts, MEASURED against HEAD (re-derive, do not transcribe):
  `mitigation_gate.FIXTURE_CLEARING_POINT["sweep_taught_recalls"]` is `(0.45, 0.2)`
  `control_taught_recall` 0.5, `control_heldout_recall` 0.35, `F_Y` 0.7
  → `y_taught` 0.35 (bracketed by the fixture sweep), `y_heldout` 0.24499999999999997
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Give both Y legs a per-element value guard and enforce the extraction count guard by type</name>

  <read_first>
    - `scripts/phase20_gate_coverage.py` in full — in particular `_prove`'s message register at
      :111-121 (a message is an ARGUMENT, not a label: it names the failure mode, the measurement
      and what the caller must do), the four existing `_prove` calls in `coverage_verdict` at
      :233-277, and `SUPERSEDED_SWEEP_SENTINEL` at :423 with its justification comment at :415-422.
    - `.planning/phases/20-pre-registration-the-three-condition-gate/20-REVIEW-GAP-CLOSURE.md`
      GC-03 (:122-164) and GC-04 (:168-198) — the review already wrote both fixes.
    - `.planning/phases/20-pre-registration-the-three-condition-gate/20-CONTEXT.md` D-40 (written at
      plan `20-13`) — the decision this task implements.
  </read_first>

  <action>
Two edits to `scripts/phase20_gate_coverage.py` and nothing else in it.

**(a) The Y value guard (GC-03, per D-40).** Immediately after the recall-length `_prove` that ends
at `:247` and before the `all(n > 0 ...)` `_prove` at `:248`, add ONE loop over the two legs paired
with their names — taught first, held-out second, matching `COVERAGE_STATISTIC_BY_AXIS`'s order —
calling `_prove` once per leg with the predicate
`all(isinstance(v, (int, float)) and 0.0 <= v <= 1.0 for v in values)`.

The message must be an argument in this module's register, and must state four things: (1) every
point must be a recall in `[0.0, 1.0]`, and the offending tuple is printed with `!r`; (2) the
MECHANISM — `nan >= criterion` is `False`, so a NaN is COUNTED as a failing point and one NaN
beside one clearing point makes a truncated axis read as BRACKETED; (3) that this is direction
(ii)'s false-coverage defect on the exact axis WR-09 exists to cover, and that a NaN recall is what
`0/0` produces from an empty held-out question set — not an exotic input; (4) that the range check
SUBSUMES the NaN case with no special-case branch, because `0.0 <= float("nan") <= 1.0` is `False`.

On the bool question, decide it and write the decision INTO the message rather than leaving it
implicit: `isinstance(True, int)` is `True` and `0.0 <= True <= 1.0` is `True`, so `(True, False)`
passes this predicate. That is CORRECT here and must be stated: on the recall legs `True`/`False`
numerically ARE `1.0`/`0.0`, both are legitimate recalls, and the axis they describe is a real
bracket — unlike the count leg, where `(True, False)` is a rate-shaped pair masquerading as a count
of successes. Excluding bools on the recall legs would refuse a legitimate reading; excluding them
on the count leg is the whole of edit (b). Say so in one sentence so a later reader does not
"restore the symmetry" and break one of the two.

**(b) The count guard by type (GC-04).** At `:257` replace the `whole` expression with
`isinstance(k, int) and not isinstance(k, bool)`. Do NOT change the `_prove` message at :258-270 —
`tests/test_phase20_correction.py:346` asserts it contains `"RATE"` and `"COUNT"`, and its
migration-mistake argument is unchanged. Append two sentences to that existing message stating what
the tightened predicate now also refuses and why: an INTEGRAL float is still a rate — this module's
own `SUPERSEDED_SWEEP_SENTINEL = (0.0, 1.0)` at `:423` is a rate-space pair defined 166 lines below
and, measured against HEAD, it passed this guard as "0 and 1 successes out of 104", both clearing
X, producing a spurious `INCONCLUSIVE`, which is precisely the outcome the message's own words
"conservative in direction, silent in operation" describe; and `isinstance(True, int)` is `True`,
so `(True, False)` passed too.

Both `_prove` calls must stay inside `coverage_verdict` and BEFORE the `x_uppers` comprehension at
`:279`, so no value reaches `wilson_upper_bound` or the axis loop unvalidated. Keep every line
under `ruff`'s 100-column limit (`pyproject.toml:37`).
  </action>

  <verify>
    <automated>.venv/bin/python -c "
import sys; sys.path.insert(0,'scripts')
import phase20_gate_coverage as cov, math
base=dict(sweep_extraction_successes=(1,3), sweep_extraction_questions=(104,104),
          sweep_taught_recalls=(0.45,0.2), extraction_ceiling_value=0.04535522866494124,
          y_taught=0.35, y_heldout=0.24499999999999997)
ok=cov.coverage_verdict(**base, sweep_heldout_recalls=(0.30,0.28))
assert ok[:2]==(False,('heldout_recall',)), ok[:2]
for bad in ((float('nan'),0.28),(42.0,-99.0),(0.30,1.5)):
    try: cov.coverage_verdict(**base, sweep_heldout_recalls=bad); raise AssertionError(f'accepted {bad}')
    except SystemExit as e: assert 'recall' in str(e)
for bad in ((-99.0,42.0),(float('nan'),0.9)):
    try: cov.coverage_verdict(**dict(base, sweep_taught_recalls=bad), sweep_heldout_recalls=(0.30,0.28)); raise AssertionError(f'accepted taught {bad}')
    except SystemExit: pass
for bad in (cov.SUPERSEDED_SWEEP_SENTINEL,(True,False),(1.0,3.0)):
    try: cov.coverage_verdict(**dict(base, sweep_extraction_successes=bad), sweep_heldout_recalls=(0.30,0.28)); raise AssertionError(f'accepted counts {bad}')
    except SystemExit as e: assert 'RATE' in str(e) and 'COUNT' in str(e)
assert cov.coverage_verdict(**dict(base, sweep_taught_recalls=(True,False)), sweep_heldout_recalls=(0.30,0.28))[0] in (True,False)
print('ok')"</automated>
  </verify>

  <acceptance_criteria>
    - The verify command above exits 0.
    - `coverage_verdict` with `sweep_heldout_recalls=(0.30, 0.28)` returns exactly `(False, ('heldout_recall',), <str>)` — the honest truncated finding is UNCHANGED by this task.
    - `coverage_verdict` with `sweep_heldout_recalls=(float('nan'), 0.28)` raises `SystemExit` whose message contains the substring `nan` (from the `!r` of the tuple) and the phrase `failing point`.
    - `coverage_verdict` with `sweep_extraction_successes=coverage.SUPERSEDED_SWEEP_SENTINEL` raises `SystemExit` whose message contains `RATE` and `COUNT`.
    - `coverage_verdict` with `sweep_taught_recalls=(True, False)` does NOT raise — bools are legitimate recalls and the message says so.
    - `.venv/bin/python -m pytest tests/test_phase20_correction.py tests/test_phase20_prereg.py -q` reports `29 passed`, zero skipped — the existing suite is unaffected by this task alone.
    - `git diff --exit-code -- scripts/mitigation_gate.py scripts/erasure_gate.py` returns 0.
    - `ruff check . && ruff format --check .` is clean.
  </acceptance_criteria>

  <done>Both Y legs are validated per element inside `coverage_verdict`, the count guard refuses integral floats and bools, the honest truncated finding is unchanged, and all 29 pre-existing phase-20 tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Arm the measured route-level differential and the sentinel-as-counts refusal</name>

  <read_first>
    - `tests/test_phase20_correction.py` in full — especially the differential idiom at
      `test_direction_i_...` (:174-226) and `test_direction_ii_...` (:229-302): each asserts the
      frozen pin's answer AND the correction's answer in ONE body, with the assertion message
      carrying the argument. Copy that shape.
    - `tests/test_phase20_correction.py:119-141` `_corrected_call` — the merge form is load-bearing
      and MEASURED (`dict(f, k=v, **overrides)` raises `TypeError` the moment a caller overrides a
      default). Use `_corrected_call`, do not hand-build kwargs.
    - `scripts/phase20_gate_coverage.py` after Task 1 — the two new `_prove` messages, so the
      assertions key on substrings that actually exist.
  </read_first>

  <action>
Add TWO test functions to `tests/test_phase20_correction.py`, placed after
`test_the_sanctioned_route_cannot_be_handed_raw_rates` (which ends at `:374`) so the sweep-value
tests sit together. Use these names verbatim:

`test_a_recall_outside_the_unit_interval_cannot_manufacture_y_coverage`

This is the tripwire `20-VERIFICATION.md` gap 1 item 2 asks for, asserted at the
`corrected_point_verdict` level — the level the verification names — because that is where the
INCONCLUSIVE→PASS flip lives and where a regression is user-visible. Four assertions, in one body,
driven through `_corrected_call("FIXTURE_CLEARING_POINT", sweep_extraction_successes=
DIRECTION_I_SUCCESSES, sweep_extraction_questions=QUESTIONS, sweep_heldout_recalls=...)` so the
fixture's own `sweep_taught_recalls = (0.45, 0.2)` legitimately brackets `y_taught = 0.35` and the
held-out leg is the only axis under test:

  1. THE HONEST READING. At `sweep_heldout_recalls=(0.30, 0.28)` the route returns `INCONCLUSIVE`
     and `_names_gate06(reasons)` is non-empty, and the GATE-06 reason names the `heldout_recall`
     axis. Both points sit above `y_heldout`, so zero failing points and the axis genuinely never
     crossed.
  2. THE MECHANISM, asserted rather than narrated: `not (float("nan") >= y_heldout)` where
     `y_heldout` is computed as `coverage.F_Y * fixture["control_heldout_recall"]`, never typed.
     This is WHY the NaN manufactured the bracket — it was counted as a failing point — and it is a
     property of the comparison, so it stays true and keeps the argument readable after the guard
     lands.
  3. THE FLIP, REFUSED. At `sweep_heldout_recalls=(float("nan"), 0.28)` — the SAME axis, STRICTLY
     MORE truncated — the route raises `SystemExit`. The assertion message must record what it
     returned BEFORE the guard, measured: `PASS` with no GATE-06 reason, `coverage_verdict`
     reporting `(True, ())`, i.e. fully covered. Assert the refusal message names the leg and the
     `[0.0, 1.0]` requirement, so a guard that raises for some other reason does not satisfy this.
  4. THE OUT-OF-RANGE CASE, on BOTH legs: `sweep_heldout_recalls=(42.0, -99.0)` raises, and
     `sweep_taught_recalls=(-99.0, 42.0)` raises. The review's original demonstration invites "that
     is garbage in, garbage out"; case 3 is what makes the claim a differential, and these two are
     what make it cover the whole class.

Then a POSITIVE CONTROL, in the register `test_the_sanctioned_route_cannot_be_handed_raw_rates:365`
uses: the same call with an in-range held-out sweep that genuinely brackets — assert it reaches a
verdict in `("PASS", "FAIL", "INCONCLUSIVE")` — so the four aborts above are attributable to the
new guard and not to anything else in the route.

`test_the_modules_own_rate_space_sentinel_cannot_pass_as_counts`

Drive `SUPERSEDED_SWEEP_SENTINEL` itself — read from the module, never retyped — into
`sweep_extraction_successes` through `_corrected_call` and assert `SystemExit` naming `RATE` and
`COUNT`. Assert `(True, False)` raises the same way. The assertion message states the measurement:
before this guard the sentinel was read as 0 and 1 successes out of 104, both clearing X, the axis
read truncated, and the route returned a spurious `INCONCLUSIVE` — a demotion, silent in operation,
which is exactly what the guard's own message says it exists to prevent. Add the integer positive
control (`DIRECTION_I_SUCCESSES`) in the same body.

Do not modify any existing test in this task. Do not add a `grep -c` or `X in source` audit — every
audit in this file is an AST walk or a runtime-message assertion (its module docstring at :38-43
states why, and `.planning/REQUIREMENTS.md`'s `| RPT-02 |` row records four instances of that
defect class in this phase).
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase20_correction.py -q -k "manufacture_y_coverage or rate_space_sentinel" 2>&1 | tail -3</automated>
  </verify>

  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_phase20_correction.py::test_a_recall_outside_the_unit_interval_cannot_manufacture_y_coverage tests/test_phase20_correction.py::test_the_modules_own_rate_space_sentinel_cannot_pass_as_counts -q` reports `2 passed`.
    - `.venv/bin/python -m pytest tests/test_phase20_correction.py tests/test_phase20_prereg.py -q` reports `31 passed`, and the output contains neither `skipped` nor `xfail`.
    - `.venv/bin/python -c "import ast,pathlib; t=ast.parse(pathlib.Path('tests/test_phase20_correction.py').read_text()); n=[f.name for f in t.body if isinstance(f,ast.FunctionDef) and f.name.startswith('test_')]; assert len(n)==13, n; assert 'test_a_recall_outside_the_unit_interval_cannot_manufacture_y_coverage' in n"` exits 0.
    - `grep -c 'float("nan")' tests/test_phase20_correction.py` reports at least 2.
    - `git diff --exit-code -- scripts/mitigation_gate.py scripts/erasure_gate.py` returns 0.
    - `ruff check . && ruff format --check .` is clean.
  </acceptance_criteria>

  <done>Two new tests exist and pass, the phase-20 pair is at 31 passed / 0 skipped, and the differential is asserted at the `corrected_point_verdict` level with the pre-guard `PASS` recorded in the assertion message.</done>
</task>

<task type="auto">
  <name>Task 3: Watch both new guards RED, then restore the module byte-identically</name>

  <read_first>
    - `.planning/phases/20-pre-registration-the-three-condition-gate/20-SECURITY.md` — the
      Watched-RED evidence table near the end of the file, and `20-11`/`20-12`'s record of four
      breaks applied, observed failing, and restored with `shasum -a 256` equal.
    - `scripts/phase20_gate_coverage.py` as it stands after Task 1.
  </read_first>

  <action>
A guard nobody has watched fail is a guard nobody has verified. Record the pre-break digest first —
`shasum -a 256 scripts/phase20_gate_coverage.py` — then perform TWO breaks in sequence, each
observed and each restored before the next begins.

BREAK 1 — the Y value guard. Delete the entire per-element `_prove` loop added in Task 1 (both
legs). Run `.venv/bin/python -m pytest tests/test_phase20_correction.py -q` and RECORD the exact
failure output: which test names failed, how many, and the assertion text. Expect
`test_a_recall_outside_the_unit_interval_cannot_manufacture_y_coverage` to fail with
`DID NOT RAISE <class 'SystemExit'>`. Record the ACTUAL count of failing tests, whatever it is — if
the break reddens more or fewer tests than expected, the guard is broader or narrower than this
plan predicted and the divergence is PUBLISHED in the SUMMARY, not smoothed. (`20-12` recorded
exactly such a divergence: a break that reddened four tests where `20-11` had recorded two.)
Restore the file and confirm `shasum -a 256` matches the pre-break digest and
`git diff --exit-code -- scripts/phase20_gate_coverage.py` returns 0.

BREAK 2 — the count-by-type guard. At `:257` revert `whole` to the old
`isinstance(k, int) or (isinstance(k, float) and k.is_integer())`. Run the same command and record
the failure. Expect `test_the_modules_own_rate_space_sentinel_cannot_pass_as_counts` to fail with
`DID NOT RAISE`. Restore, re-check the digest and `git diff --exit-code`.

Then run the FULL suite and confirm the baseline moved by exactly the two new tests. Commit only
after both restores are verified — the breaks must never reach a commit.

Record both breaks in the SUMMARY's `## Threat Flags` / watched-RED section in the shape
`20-SECURITY.md`'s Watched-RED table uses: what was broken, the exact command, the exact observed
output, and the restore proof (digest equality plus `git diff --exit-code`).
  </action>

  <verify>
    <automated>shasum -a 256 scripts/phase20_gate_coverage.py && git diff --exit-code -- scripts/phase20_gate_coverage.py scripts/mitigation_gate.py scripts/erasure_gate.py && .venv/bin/python -m pytest -q 2>&1 | tail -2</automated>
  </verify>

  <acceptance_criteria>
    - Both breaks were APPLIED and their pytest output OBSERVED — the SUMMARY quotes the actual failure lines, not a prediction of them.
    - After each restore, `git diff --exit-code -- scripts/phase20_gate_coverage.py` returns 0 and the `shasum -a 256` digest equals the pre-break digest recorded at the start of this task.
    - `.venv/bin/python -m pytest -q` reports at least `876 passed, 1 skipped` (the `874 passed, 1 skipped` baseline plus the two new tests) with zero failures and zero xfail.
    - `.venv/bin/python -m pytest tests/test_phase20_correction.py tests/test_phase20_prereg.py -q` reports `31 passed` with no `skipped` and no `xfail` in the output.
    - `git diff --exit-code -- scripts/mitigation_gate.py scripts/erasure_gate.py` returns 0.
    - `git status --porcelain scripts/` is empty at commit time.
  </acceptance_criteria>

  <done>Both new guards were observed failing and the module was restored byte-identically after each; the full suite is green at the baseline plus two.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a plan that says a thing will be done ↔ a guard that proves it was | `20-SECURITY.md:39`. GATE-06's checkbox and ROADMAP SC3's amendment both rest on `coverage_verdict`'s findings. |
| a coverage finding produced by the DATA ↔ one produced by the INPUT | `coverage_verdict:274-276` states this boundary for the extraction axis. This plan extends it to the two axes the last gap closure added. |
| a frozen pin ↔ its correction | `scripts/mitigation_gate.py` and `scripts/erasure_gate.py` are byte-frozen across nine pin commits; all work lands in the UNPINNED `scripts/phase20_gate_coverage.py`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-71 | Spoofing | a Y coverage finding manufactured by the input rather than by the data | mitigate | A per-element `_prove` on BOTH legs inside `coverage_verdict`, placed before `x_uppers` at `:279` so no value reaches the axis loop unvalidated. `0.0 <= v <= 1.0` is `False` for NaN, so the range check subsumes the NaN case with no special-case branch a later reader can delete separately |
| T-20-72 | Tampering | a guard written but never watched failing | mitigate | Task 3 applies both breaks, observes the pytest output, and restores with `shasum -a 256` equality plus `git diff --exit-code`. The observed counts are published even where they diverge from this plan's prediction |
| T-20-73 | Repudiation | a tripwire that asserts only the post-fix refusal, losing the record of the flip it prevents | mitigate | The differential asserts the honest axis's finding AND the more-truncated axis's refusal in one body, AND the mechanism (`not (nan >= y_heldout)`), with the pre-guard `PASS` / `(True, ())` recorded in the assertion message. All three must hold, so a regression on either side reddens |
| T-20-74 | Spoofing | this module's own rate-space sentinel passing as a count of successes | mitigate | `whole` enforced by type at `:257`; the sentinel is read from `coverage.SUPERSEDED_SWEEP_SENTINEL`, never retyped, so a later change to that constant travels into the test |
| T-20-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install in this plan. `scripts/phase20_gate_coverage.py` is stdlib plus two sibling scripts; `pyproject.toml` is sha256-pinned by `tests/test_package.py` (STAT-04) and is untouched |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_phase20_correction.py tests/test_phase20_prereg.py -q` →
  `31 passed`, output contains neither `skipped` nor `xfail`.
- `.venv/bin/python -m pytest -q` → at least `876 passed, 1 skipped`, zero failures.
- `ruff check . && ruff format --check .` → clean.
- `git diff --exit-code -- scripts/mitigation_gate.py scripts/erasure_gate.py` → 0.
- `git diff HEAD~N --stat -- scripts/mitigation_gate.py scripts/erasure_gate.py` (N = this plan's
  commit count) → empty.
- `.venv/bin/python -m pytest tests/test_phase20_prereg.py -q -k ancestry` → green; the ancestry
  guard is unaffected because no `results/phase20_*` artifact is written by this plan.
</verification>

<success_criteria>
- Both Y legs are validated per element and the extraction count guard is enforced by type.
- The honest held-out finding at `(0.30, 0.28)` is unchanged: `(False, ('heldout_recall',), <str>)`.
- `(nan, 0.28)`, `(42.0, -99.0)` and the taught-leg equivalents all raise `SystemExit` through
  `corrected_point_verdict` — the `PASS` measured against HEAD is gone.
- `SUPERSEDED_SWEEP_SENTINEL` and `(True, False)` are refused as counts.
- Both guards were observed RED and restored byte-identically.
- `scripts/mitigation_gate.py` and `scripts/erasure_gate.py` return 0 from `git diff --exit-code`.
</success_criteria>

<output>
Create `.planning/phases/20-pre-registration-the-three-condition-gate/20-14-SUMMARY.md` when done.
</output>
