---
phase: 20-pre-registration-the-three-condition-gate
plan: 16
type: execute
wave: 15
depends_on: ["20-15"]
files_modified:
  - results/phase20_gate_coverage_correction.json
  - results/phase20_gate_coverage_correction.md
  - tests/test_phase20_correction.py
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
requirements: [GATE-02, GATE-06]
user_setup: []

must_haves:
  truths:
    - "`results/phase20_gate_coverage_correction.json` carries a top-level `value_guards` key and `defects` entries GC-01, GC-02, GC-03, GC-04 and GC-06, and every pre-existing key and leaf value is EQUAL to the same path in the newest committed revision that has no `value_guards` key"
    - "Nothing moved out of `recorded_not_corrected`: its keys are still exactly IN-06, IN-09, WR-03, WR-04, WR-05, WR-10 and each value is byte-equal to the committed revision"
    - "`results/phase20_gate_coverage_correction.md` carries a SECOND `## Addendum — 2026-08-21` section appended via `scripts/_addendum.py::append_addendum`, written in its own commit, with `test_correction_addendum_is_additive_on_the_published_artifact` still GREEN — its pre-existing assertions unchanged, extended by two ordering assertions on the second heading"
    - "Every number published in the new `value_guards` block is re-derived by `test_every_published_number_re_derives_from_the_modules` by calling the modules — `grep` finds no new float literal in the test file"
    - "`.planning/REQUIREMENTS.md`'s GATE-02 traceability row no longer claims the choke point catches a regime-lying caller `by the number itself`; it states the name/property pair and cites the one-ULP measurement that falsified the old claim"
    - "Both guards this plan ships were OBSERVED failing: one pre-existing `evidence` leaf mutated in the committed JSON turns `test_correction_payload_is_additive_across_the_second_correction` RED, and one line spliced into the `.md` above the appended region turns the ordering assertion RED — each restored with `shasum -a 256` equality and `git diff --exit-code` returning 0"
    - "`.venv/bin/python -m pytest tests/test_phase20_prereg.py -q -k phase20_prereg_is_frozen` is green — the ancestry guard still sees all three `results/phase20_*` first-adds after the nine pin commits"
  artifacts:
    - path: "results/phase20_gate_coverage_correction.json"
      provides: "the second correction's defects and measured value-guard evidence"
      contains: "value_guards"
    - path: "results/phase20_gate_coverage_correction.md"
      provides: "the second D-24 dated continuation"
      contains: "Addendum — 2026-08-21"
    - path: "tests/test_phase20_correction.py"
      provides: "the JSON additivity guard and the re-derivation of every new number"
      contains: "test_correction_payload_is_additive_across_the_second_correction"
  key_links:
    - from: "results/phase20_gate_coverage_correction.md"
      to: "scripts/_addendum.py::append_addendum"
      via: "pending=RECORDED, recorded=RECORDED — the idempotent-pointer form, MEASURED to satisfy all three of the writer's guards"
      pattern: "append_addendum"
    - from: "tests/test_phase20_correction.py::test_correction_payload_is_additive_across_the_second_correction"
      to: "git show <pre-write-rev>:results/phase20_gate_coverage_correction.json"
      via: "the newest committed revision with no `value_guards` key IS the pre-write state, by definition"
      pattern: "value_guards"
---

<objective>
Publish the second correction: the five newly-closed defects and their measured evidence into
`results/phase20_gate_coverage_correction.json`, a second dated continuation appended to the `.md`,
a guard proving the JSON write was additive, and the one false claim in `.planning/REQUIREMENTS.md`
corrected to what the code now proves.

Purpose: `20-VERIFICATION.md` measured `REQUIREMENTS.md:303`'s claim — that the retention choke
point catches a caller who lies about `regime` "by the number itself" — FALSE at one ULP. A record
that outlives its own falsification is the defect class T-20-57 exists to refuse, applied in the
other direction. And the published artifact currently describes a four-refusal guard that is now a
five-refusal guard with a magnitude bound, on a module whose Y legs are now value-checked.

Output: an additive JSON write with its own additivity guard, a second dated continuation, and a
corrected traceability row.
</objective>

<task_count_note>
This plan carries FOUR tasks against the 2-3 guideline. Splitting it would mean a sixth plan and a
renumber, and the wave-set is already numbered and committed. The tasks are light — a keyed JSON
addition, one `append_addendum` call, a two-sentence record correction, and two artifact
break/restores — and Task 4 exists because `20-14`'s T-20-72 forbids shipping an unwatched guard.
</task_count_note>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-pre-registration-the-three-condition-gate/20-CONTEXT.md
@.planning/phases/20-pre-registration-the-three-condition-gate/20-VERIFICATION.md
@.planning/phases/20-pre-registration-the-three-condition-gate/20-14-SUMMARY.md
@.planning/phases/20-pre-registration-the-three-condition-gate/20-15-SUMMARY.md

<interfaces>
<!-- Resolved from the committed files. -->

`scripts/_addendum.py`:
  :56  `def append_addendum(path, addendum, *, pending, recorded):`
  :70-77  `_prove(text.count(pending) == 1, ...)` — the placeholder must occur EXACTLY once
  :85-90  `_prove(_verdict.recorded_verdict(updated) == _verdict.recorded_verdict(text), ...)`
  :91-96  `_prove(updated.startswith(before) and addendum.rstrip("\n") in updated, ...)`

`results/phase20_gate_coverage_correction.md` state TODAY, MEASURED:
  the PENDING line occurs 0 times; the RECORDED line occurs EXACTLY ONCE.
  headings: `# Phase 20 — ...`(:1), `## The measured result`(:20), `## Verdict`(:50),
  `## The coverage correction`(:115),
  `## Addendum — 2026-08-21 — the coverage statistic, the held-out leg, and the retention floor's
  missing tripwire`(:119), then five `###` subsections ending at `### Which computation governs a
  v4.0 verdict`(:217).

`tests/test_phase20_correction.py`:
  :85-93  `PENDING`, `RECORDED`, `ADDENDUM_HEADING` — declared here, TAKEN FROM THE COMMITTED FILE
  :493    `def test_correction_addendum_is_additive_on_the_published_artifact():` — derives the
          pre-append revision from `git log`, asserts `changed == [(PENDING, RECORDED)]`, asserts
          the appended region contains `ADDENDUM_HEADING`, asserts `recorded_verdict` unchanged and
          non-`None`, and enforces STAT-02 line-scoped percentages
  :575    `def test_every_published_number_re_derives_from_the_modules():`
  :678-686 the module-data assertions (`supersedes`, `superseded_sweep_sentinel`,
          `coverage_statistic_by_axis`)

`results/phase20_gate_coverage_correction.json` top-level keys TODAY: `bound_direction`,
`continuation`, `coverage_statistic_by_axis`, `dated`, `dated_note`, `defects`, `evidence`,
`governing_entry_point`, `governing_module`, `governs`, `heldout_coverage`, `proof`,
`recorded_not_corrected`, `retention_provenance`, `superseded_sweep_sentinel`, `supersedes`.
  `defects` keys: `CR-01`, `T-20-19`, `WR-08`, `WR-09`.
  `recorded_not_corrected` keys: `IN-06`, `IN-09`, `WR-03`, `WR-04`, `WR-05`, `WR-10` — NONE of the
  five defects closed in this wave-set was ever recorded there, so NOTHING moves out.
  `evidence` has a fixed shape (`n`, `X`, `y_taught`, `y_heldout`, `swept_points`, `direction_i`,
  `direction_ii`, `direction_ii_on_clearing_fixture`) which `:588-645` iterates key by key.

`.planning/REQUIREMENTS.md`:
  :303  the `| GATE-02 | Phase 20 | ... |` traceability row. The false sentence is
        "plus a fourth refusing `V20_RETENTION_NOISE_FLOOR` BY IDENTITY (imported, never retyped),
        so a caller that lies about `regime` is still caught by the number itself."
  :307  the `| GATE-06 |` row — also needs the Y-leg half stated; it currently says the correction
        decides "raw recall for BOTH Y legs" with no mention that the values were unvalidated.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the second correction into the JSON additively, and guard the additivity</name>
  <files>results/phase20_gate_coverage_correction.json, tests/test_phase20_correction.py</files>

  <read_first>
    - `results/phase20_gate_coverage_correction.json` in full — the shapes of `defects`,
      `evidence`, `recorded_not_corrected` (each entry has `finding`, `reason`, `stale_when`),
      `retention_provenance` and `bound_direction`.
    - `tests/test_phase20_correction.py:575-708` — every existing re-derivation, so the new
      assertions match its register: nothing typed, everything obtained by calling the modules.
    - `.planning/phases/20-pre-registration-the-three-condition-gate/20-14-SUMMARY.md` and
      `20-15-SUMMARY.md` — the measured outputs the new block publishes.
  </read_first>

  <action>
Generate the JSON edit with a throwaway script that IMPORTS the three modules and CALLS them, in
`20-10`'s register: no float is typed. Load the committed JSON, mutate in place, re-serialise with
the same `indent` and key order the file already uses, and confirm with `git diff` that only
additions appear.

**Decide the two questions the brief poses, and write the decisions into the artifact.**

  1. Does anything move OUT of `recorded_not_corrected`? NO, and the artifact must say why:
     `recorded_not_corrected` holds IN-06, IN-09, WR-03, WR-04, WR-05 and WR-10, none of which is
     a defect this wave-set closed. GC-01…GC-06 are findings from `20-REVIEW-GAP-CLOSURE.md`, which
     postdates this artifact, so they were never recorded as accepted. Nothing moves; the block is
     byte-unchanged and a guard asserts it.
  2. Do the new defects go into `defects` + `evidence`? Into `defects` YES — that dict is keyed by
     defect ID and adding keys is additive at the key level. Into `evidence` NO, and the reason is
     structural, not stylistic: `evidence` is the CR-01/WR-09 reproduction record with a fixed
     shape that `tests/test_phase20_correction.py:588-645` iterates key by key, and a
     differently-shaped record inside it would break that iteration contract. The new measurements
     go into a NEW top-level key named exactly `value_guards`.

**The additions.** Five new `defects` entries with these exact keys — `GC-01`, `GC-02`, `GC-03`,
`GC-04`, `GC-06` — each in the shape the existing four use: a single string naming the file, the
line span, what was wrong and what it produced. Each states its status as CLOSED at plan `20-14` or
`20-15` and names the guard that closes it.

One new top-level `value_guards` object carrying, all COMPUTED:
  - `y_leg_differential`: the fixture (`FIXTURE_CLEARING_POINT`), the sweep (`(1,3)` / `(104,104)`),
    the fixture's own `sweep_taught_recalls`, `y_heldout`, the honest held-out sweep `(0.30, 0.28)`
    with its `coverage_verdict` truncated-axis tuple and route verdict, and the pre-guard reading of
    `(nan, 0.28)` — `PASS`, no GATE-06 reason, `coverage_verdict` `(True, ())` — recorded in the
    PAST TENSE as what the module did before plan `20-14`, with `guarded_by` naming
    `test_a_recall_outside_the_unit_interval_cannot_manufacture_y_coverage` and `mechanism` stating
    that `nan >= y_heldout` is `False` so the NaN was counted as a failing point.
  - `count_type_guard`: `SUPERSEDED_SWEEP_SENTINEL` read from the module, the pre-guard reading
    (accepted as counts → spurious `INCONCLUSIVE`), and `guarded_by`
    `test_the_modules_own_rate_space_sentinel_cannot_pass_as_counts`.
  - `retention_magnitude_bound`: `admissible_ceiling` = `_MAX_ADMISSIBLE_RETENTION_FLOOR`,
    `relative_tolerance` = `_RETENTION_FLOOR_RELATIVE_TOLERANCE`, the one-ULP case with
    `retention_cap` on the nudged floor and on `V20_RETENTION_NOISE_FLOOR` published side by side to
    show the bit-identity, the `5.0` case with its cap against `governing_cap`, and the D-41 case:
    `fixture_floor` `0.009`, its ratio to the measured floor, its cap, and the statement that every
    published `evidence` verdict is bit-unchanged under the substitution.
  - `census`: what the walk now matches (call `.id`/`.attr` plus `ast.ImportFrom`), and — recorded
    as a state, never as a claim of completeness — the two residuals GC-06 named that are NOT
    closed: getattr-dispatch, and the `scripts/`+`src/` scope.
  - `second_continuation`: the `.md` heading string this plan's Task 2 writes, and `dated`
    `2026-08-21`.

**The guard.** Add ONE test function to `tests/test_phase20_correction.py` named exactly
`test_correction_payload_is_additive_across_the_second_correction`. It derives the pre-write
revision the same way `:493-527` derives the pre-append revision — walk `git log --format=%H --
<PAYLOAD_REL>` newest-first and take the newest blob whose parsed payload has NO `value_guards`
key; that is the pre-write state BY DEFINITION and stays true however many later commits touch the
file. Assert: the pre-write revision was found (with a message saying that its absence means either
the key always existed or the history was rewritten); every top-level key of the OLD payload is
present in the new one with an EQUAL value, except `defects` which is compared key by key with
every old key equal and only `GC-*` keys added; `recorded_not_corrected` is EQUAL as a whole;
`evidence` is EQUAL as a whole; and the only new top-level key is `value_guards`.

Then EXTEND `test_every_published_number_re_derives_from_the_modules` (`:575`) to re-derive every
number inside `value_guards` by calling the modules — `coverage._MAX_ADMISSIBLE_RETENTION_FLOOR`,
`mitigation_gate.retention_cap(...)` at each published floor, `coverage.F_Y * fixture[...]`,
`coverage.SUPERSEDED_SWEEP_SENTINEL`, and the fixture floor read from
`mitigation_gate.FIXTURE_CLEARING_POINT["retention_noise_floor"]`.

THE ONE EXEMPTION, stated so it is a decision and not a leak. `value_guards` publishes the sweep and
floor values that were FED to the module — `(0.30, 0.28)`, `(nan, 0.28)`, `5.0`, `2**-50` — and an
input is not derivable from the module it was handed to. Source each from an existing constant where
one exists (`DEFAULT_HELDOUT_SWEEP`, `coverage.SUPERSEDED_SWEEP_SENTINEL`,
`erasure_gate.V20_RETENTION_NOISE_FLOOR`, `mitigation_gate.FIXTURE_CLEARING_POINT[...]`); for the
three that have none, declare them ONCE as module-scope constants in the test file and assert the
payload against those names. Every OUTPUT — every cap, ratio, bound, verdict and truncated-axis
tuple — remains derived by calling the modules, with no literal anywhere.
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase20_correction.py tests/test_phase20_prereg.py -q 2>&1 | tail -3 && .venv/bin/python -c "
import json,subprocess
p='results/phase20_gate_coverage_correction.json'
new=json.load(open(p))
for rev in subprocess.run(['git','log','--format=%H','--',p],capture_output=True,text=True).stdout.split():
    old=json.loads(subprocess.run(['git','show',f'{rev}:{p}'],capture_output=True,text=True).stdout)
    if 'value_guards' not in old: break
else: raise SystemExit('no pre-write revision')
assert set(new)-set(old)=={'value_guards'}, set(new)-set(old)
for k in old:
    if k=='defects': continue
    assert new[k]==old[k], k
assert all(new['defects'][k]==v for k,v in old['defects'].items())
assert set(new['defects'])-set(old['defects'])=={'GC-01','GC-02','GC-03','GC-04','GC-06'}
assert new['recorded_not_corrected']==old['recorded_not_corrected']
print('ok')"</automated>
  </verify>

  <acceptance_criteria>
    - The verify command exits 0 and pytest reports `32 passed` with no `skipped` and no `xfail`.
    - `git diff --numstat HEAD -- results/phase20_gate_coverage_correction.json` reports 0 deletions.
    - `.venv/bin/python -c "import json; p=json.load(open('results/phase20_gate_coverage_correction.json')); assert set(p['recorded_not_corrected'])=={'IN-06','IN-09','WR-03','WR-04','WR-05','WR-10'}"` exits 0.
    - The throwaway generator is NOT committed: `git status --porcelain` shows no new `scripts/*.py`.
    - `.venv/bin/python -m pytest tests/test_phase20_prereg.py -q -k phase20_prereg_is_frozen` is green.
    - `git diff --exit-code -- scripts/` returns 0 — this task writes no module code.
    - `ruff check . && ruff format --check .` is clean.
  </acceptance_criteria>

  <done>The JSON carries five new `defects` keys and a `value_guards` block, every pre-existing value is provably unchanged, and every new number is re-derived by a committed test.</done>
</task>

<task type="auto">
  <name>Task 2: Append the second dated continuation to the .md, in its own commit</name>
  <files>results/phase20_gate_coverage_correction.md, tests/test_phase20_correction.py</files>

  <read_first>
    - `scripts/_addendum.py` in full — the three guards at :70-96 and the header's account of why
      both marker halves are required keywords.
    - `results/phase20_gate_coverage_correction.md` — the whole file, especially `## Verdict` (:50)
      and the existing addendum at :119-231.
    - `tests/test_phase20_correction.py:85-93` and `:493-572` — the marker constants and the
      additivity guard that must stay green WITHOUT being edited.
  </read_first>

  <action>
Commit this task SEPARATELY from Task 1, so the append is provably additive against a committed
pre-append revision — the two-commit rule `20-10` established and `T-20-64` enforces.

**The mechanism, MEASURED before writing.** `append_addendum` requires its `pending` string to occur
EXACTLY ONCE. The `PENDING` placeholder was consumed by `20-10`'s append and now occurs ZERO times;
the `RECORDED` pointer line occurs exactly once. Call
`append_addendum(CORRECTION_PATH, <addendum>, pending=RECORDED, recorded=RECORDED)` — the
idempotent-pointer form. Verified against a scratch copy of the committed file: `text.count(pending)`
is 1, the `## Verdict` section is unchanged, the original prefix is preserved byte-identically, and
`test_correction_addendum_is_additive_on_the_published_artifact` stays green with ZERO edits,
because within the first `len(before_lines)` lines the only difference from the pre-`20-10`
revision is still `PENDING → RECORDED`. Do NOT hand-edit the file and do NOT introduce a new
placeholder line — either would be an edit to a published pre-registration.

**The addendum.** One `##` section using this heading VERBATIM:

`## Addendum — 2026-08-21 (second) — the value guards on both Y legs and the retention floor's magnitude bound`

It must contain NO `## Verdict` heading (that would defeat `append_addendum`'s own unchanged-verdict
guard) and no line carrying a `%` figure without a `k/n` denominator ON THAT SAME LINE (STAT-02, as
`:564-572` enforces it line-scoped).

Content — four short subsections, every number re-derived by running Python against the committed
modules in this task, none transcribed from this plan:

  - *What the first continuation left unguarded.* The two Y legs were validated for length only
    while the extraction axis on the same function got three per-element `_prove` calls. State the
    measured differential in the past tense with the fixture named: on `FIXTURE_CLEARING_POINT` at
    the `(1,3)/(104,104)` sweep, held-out `(0.30, 0.28)` returned `INCONCLUSIVE` with a GATE-06
    reason and `(nan, 0.28)` — strictly more truncated — returned `PASS` with none. State the
    mechanism: `nan >= Y_heldout` is `False`, so the NaN was counted as a FAILING point and
    manufactured the bracket; a NaN recall is what `0/0` produces from an empty held-out question
    set.
  - *The retention floor: a name where the harm was a property.* The one-ULP nudge passing the `!=`
    and buying a bit-identical cap, and `5.0` under clean provenance reaching a far looser cap. Then
    D-38's resolution — both refusals, the name first and the property second — and why the ordering
    matters.
  - *The finding that forced D-41, published because it is against interest.* The bound refuses this
    repository's own committed fixtures, which carry `0.009`. Give the ratio and both caps. State
    that widening the tolerance was rejected, and that the sanctioned route's test harness supplies
    the governing floor instead — with the measurement that makes it safe: every published verdict
    bit-unchanged.
  - *What is closed, and what is recorded and not closed.* GC-01, GC-02, GC-03, GC-04 and GC-06
    closed, each naming its guard by test name. GC-05, GC-07, GC-08…GC-12 recorded as NOT closed in
    this wave-set, with `value_guards.census` naming the two census residuals that remain. A
    continuation that implies completeness it did not achieve is the same defect as a register that
    publishes a closure it cannot substantiate.

Finally, add `ADDENDUM_HEADING_SECOND` to `tests/test_phase20_correction.py` beside
`ADDENDUM_HEADING` at `:90-93`, TAKEN FROM THE COMMITTED FILE rather than from this plan, and add
two assertions to `test_correction_addendum_is_additive_on_the_published_artifact`: that the second
heading is present in the appended region, and that it appears AFTER the first — so a future append
that splices into the body rather than after it reddens.
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase20_correction.py tests/test_phase20_prereg.py -q 2>&1 | tail -3 && .venv/bin/python -c "
import pathlib,re
t=pathlib.Path('results/phase20_gate_coverage_correction.md').read_text()
h=[l for l in t.splitlines() if l.startswith('## Addendum')]
assert len(h)==2, h
assert t.count('## Verdict')==1, t.count('## Verdict')
for i,l in enumerate(t.splitlines(),1):
    assert not (re.search(r'\d\s*%',l) and not re.search(r'\d\s*/\s*\d',l)), (i,l)
print('ok')"</automated>
  </verify>

  <acceptance_criteria>
    - The verify command exits 0 and pytest reports `32 passed`, no `skipped`, no `xfail`.
    - `.venv/bin/python -m pytest tests/test_phase20_correction.py::test_correction_addendum_is_additive_on_the_published_artifact -q` reports `1 passed`.
    - `git log --format=%H -- results/phase20_gate_coverage_correction.md | head -1` differs from `git log --format=%H -- results/phase20_gate_coverage_correction.json | head -1` — the append is its own commit.
    - `git diff --numstat HEAD~1 -- results/phase20_gate_coverage_correction.md` reports 0 deletions.
    - `results/phase20_gate_coverage_correction.md` contains exactly two lines starting `## Addendum` and exactly one `## Verdict`.
    - `git diff --exit-code -- scripts/` returns 0 — `scripts/_addendum.py` was CALLED, not edited.
    - `.venv/bin/python -m pytest tests/test_phase20_prereg.py -q -k phase20_prereg_is_frozen` is green.
  </acceptance_criteria>

  <done>A second dated continuation sits at the end of the published `.md`, written by `append_addendum` in its own commit, with zero deletions and the additivity guard green and extended.</done>
</task>

<task type="auto">
  <name>Task 3: Correct the falsified claim in REQUIREMENTS.md's GATE-02 and GATE-06 rows</name>
  <files>.planning/REQUIREMENTS.md</files>

  <read_first>
    - `.planning/REQUIREMENTS.md:303` (the `| GATE-02 |` row) and `:307` (the `| GATE-06 |` row) in
      full — both are long single-cell rows whose existing text is a record of what was asserted at
      plan `20-12`.
    - `.planning/phases/20-pre-registration-the-three-condition-gate/20-VERIFICATION.md:69` — the
      exact issue filed against `:303`.
    - `.planning/phases/20-pre-registration-the-three-condition-gate/20-CONTEXT.md` D-38 and D-40.
  </read_first>

  <action>
Two rows, corrected in the additive dated register D-36 established — the prior text stays readable
and what was wrong with it is recorded beside it, EXCEPT for the one sentence that is measurably
false, which is corrected in place because leaving a false claim standing is the defect.

**GATE-02 (`:303`).** The sentence "plus a fourth refusing `V20_RETENTION_NOISE_FLOOR` BY IDENTITY
(imported, never retyped), so a caller that lies about `regime` is still caught by the number
itself" over-claims. Replace ONLY the clause after the comma — keep "plus a fourth refusing
`V20_RETENTION_NOISE_FLOOR` BY IDENTITY (imported, never retyped)" — with what the code proves:
that refusal catches exactly ONE bit pattern, and a caller who lies about `regime` AND perturbs the
last bit was measured passing it and buying a bit-identical cap. Then append a dated sentence
beginning `**AMENDED 2026-08-21 at plan `20-16` (D-38).**` stating the fifth refusal — the magnitude
bound `retention_noise_floor <= _MAX_ADMISSIBLE_RETENTION_FLOOR` — what the pair now proves (the
named value by identity, the entire looser class by magnitude), that it admits any TIGHTER floor a
later phase measures, and that it is guarded by
`tests/test_phase20_correction.py::test_the_retention_floor_tripwire_is_the_only_route_to_a_verdict`
with both cases watched red-then-green. ALSO correct the stale count in that same row: it says the
guard "drives all eight refusal cases THROUGH the verdict route" — `20-15` makes it ten (the two new
magnitude cases). Re-count from the committed test body rather than from this sentence, and write
what you count. Leaving a stale number inside the very edit whose purpose is removing a falsified
claim would be the same defect twice. Name D-41's consequence in one clause: the bound refuses the
repo's own fixture floor, and the harness supplies the governing floor read from the artifact.

**GATE-06 (`:307`).** Leave the existing text intact and append a dated sentence beginning
`**AMENDED 2026-08-21 at plan `20-16` (D-40).**` recording that the discharge's Y half was
STRUCTURAL when written — both legs decided in one body — and not yet BEHAVIOURAL: the legs were
length-checked and their values consumed raw, so a coverage finding on either leg could be produced
by the input. Give the measured differential in one sentence with its numbers, name the per-element
`[0.0, 1.0]` `_prove` on both legs and the count-by-type guard as the closure, and name
`test_a_recall_outside_the_unit_interval_cannot_manufacture_y_coverage` and
`test_the_modules_own_rate_space_sentinel_cannot_pass_as_counts` as the guards.

Leave every other row untouched. Do NOT re-check or un-check any checkbox — the eight boxes were
checked at `0f265e2` and T-20-56 accepts that touching them again would make one discharge look
like two. Do not touch RPT-02's Phase 25 deferral.
  </action>

  <verify>
    <automated>.venv/bin/python -c "
import pathlib,subprocess
p='.planning/REQUIREMENTS.md'
t=pathlib.Path(p).read_text(); old=subprocess.run(['git','show','HEAD:'+p],capture_output=True,text=True).stdout
assert 'still caught by the number itself' not in t, 'the falsified claim is still standing'
assert t.count('AMENDED 2026-08-21 at plan \`20-16\`')==2
assert t.count('- [x]')==old.count('- [x]') and t.count('- [ ]')==old.count('- [ ]'), 'a checkbox moved'
for row in ('| GATE-03 |','| GATE-05 |','| RPT-02 |'):
    a=[l for l in old.splitlines() if l.startswith(row)][0]
    assert a in t, row
print('ok')"</automated>
  </verify>

  <acceptance_criteria>
    - The verify command exits 0.
    - `grep -c 'still caught by the number itself' .planning/REQUIREMENTS.md` reports 0.
    - `grep -c 'AMENDED 2026-08-21 at plan .20-16.' .planning/REQUIREMENTS.md` reports 2.
    - Checkbox counts are identical to HEAD in both directions — no box was checked or unchecked.
    - The `| GATE-03 |`, `| GATE-05 |` and `| RPT-02 |` rows are byte-identical to HEAD.
    - `git diff --exit-code -- scripts/ tests/ results/` returns 0 — this task edits only `.planning/REQUIREMENTS.md`.
  </acceptance_criteria>

  <done>`REQUIREMENTS.md` no longer carries the falsified "by the number itself" claim; GATE-02 and GATE-06 each carry a dated amendment naming the new guard and its test, with no checkbox moved.</done>
</task>

<task type="auto">
  <name>Task 4: Watch both new artifact guards RED, then restore byte-identically</name>
  <files>results/phase20_gate_coverage_correction.json, results/phase20_gate_coverage_correction.md</files>

  <read_first>
    - `.planning/phases/20-pre-registration-the-three-condition-gate/20-15-SUMMARY.md` Task 3 — the
      break/restore shape this task mirrors.
    - `tests/test_phase20_correction.py` as it stands after Tasks 1 and 2 — the two guards being
      watched.
    - `.planning/phases/20-pre-registration-the-three-condition-gate/20-SECURITY.md` — the
      Watched-RED evidence table, and `20-11`'s precedent: this exact guard class was watched by a
      one-digit `cap` edit, so shipping it unwatched here would drop a standard this phase already
      met.
  </read_first>

  <action>
This plan ships two guards, and `20-14`'s own threat register names "a guard written but never
watched failing" as T-20-72. `20-17` publishes T-20-80 — a published JSON key silently rewritten
under cover of an additive write — as CLOSED on the strength of these guards, so they must be
observed, not merely written. That is gap 2's own truth: each closure points at a WATCHED guard,
never at a plan.

Record `shasum -a 256` for both artifacts first. Two breaks, each observed and restored before the
next begins.

BREAK 1 — the JSON additivity guard. Mutate ONE pre-existing leaf in
`results/phase20_gate_coverage_correction.json` — change a digit in an `evidence` value, the same
one-digit-edit register `20-11` used on `cap`. Run
`.venv/bin/python -m pytest "tests/test_phase20_correction.py::test_correction_payload_is_additive_across_the_second_correction" -q`
and record the exact failure. Then run
`.venv/bin/python -m pytest tests/test_phase20_correction.py -q` and record the FULL set of tests
that redden — `test_every_published_number_re_derives_from_the_modules` re-derives `evidence` too,
so the count is expected to exceed one, and the ACTUAL count is what gets published. If the
mutation reddens the re-derivation guard but NOT the additivity guard, that is the finding: the
additivity guard is not independently load-bearing on that leaf, and a different leaf (one no
re-derivation reads) must be chosen and the first attempt recorded. Restore; confirm digest and
`git diff --exit-code -- results/`.

BREAK 2 — the addendum ordering assertion. Splice a line carrying `ADDENDUM_HEADING_SECOND` into
the `.md` BODY above the appended region (before the first `## Addendum`), so the second heading is
no longer strictly after the first. Run
`.venv/bin/python -m pytest "tests/test_phase20_correction.py::test_correction_addendum_is_additive_on_the_published_artifact" -q`
and record the failure. Restore; confirm digest and `git diff --exit-code -- results/`.

The breaks must never reach a commit. Record both in the SUMMARY in `20-SECURITY.md`'s Watched-RED
table shape — what was broken, the exact command, the exact observed output, and the restore proof.
  </action>

  <verify>
    <automated>shasum -a 256 results/phase20_gate_coverage_correction.json results/phase20_gate_coverage_correction.md && git diff --exit-code -- results/ && .venv/bin/python -m pytest tests/test_phase20_correction.py tests/test_phase20_prereg.py -q 2>&1 | tail -2</automated>
  </verify>

  <acceptance_criteria>
    - Both breaks were APPLIED and their pytest output OBSERVED; the SUMMARY quotes the actual failure lines, not a prediction of them.
    - After each restore, `shasum -a 256` equals the pre-break digest and `git diff --exit-code -- results/` returns 0.
    - Break 1's full-file run is recorded with the ACTUAL count of reddened tests, and if the chosen leaf did not redden the additivity guard specifically, the retry with a different leaf is recorded alongside the first attempt.
    - `.venv/bin/python -m pytest tests/test_phase20_correction.py tests/test_phase20_prereg.py -q` reports `32 passed`, no `skipped`, no `xfail`.
    - `git status --porcelain results/` is empty at commit time.
    - `git diff --exit-code -- scripts/mitigation_gate.py scripts/erasure_gate.py` returns 0.
  </acceptance_criteria>

  <done>Both artifact guards were observed failing and both artifacts restored byte-identically, so `20-17` can close T-20-80 against a watched guard rather than against this plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a frozen pin ↔ its correction | `20-SECURITY.md:38`. A defect found in a pre-registration may be corrected only by a dated continuation beside it, never by an edit to it. |
| a defect description ↔ its defect | T-20-57. A description that outlives its defect teaches a reader to distrust a correct artifact; a description that outlives its FALSIFICATION does the reverse and is worse. |
| a published total ↔ the rows that substantiate it | `20-SECURITY.md:40`. Applies to the JSON's key set as much as to the register's row count. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-79 | Tampering | a second continuation written as an edit rather than an append | mitigate | Written through `scripts/_addendum.py::append_addendum` in the idempotent-pointer form, whose three guards run on the PRODUCED BYTES, in a commit separate from the JSON so a pre-append revision exists in history; `test_correction_addendum_is_additive_on_the_published_artifact` stays green unedited and gains an ordering assertion on the second heading |
| T-20-80 | Tampering | a published JSON key silently rewritten under cover of an additive write | mitigate | `test_correction_payload_is_additive_across_the_second_correction` derives the pre-write revision from `git log` (the newest blob with no `value_guards` key, true by definition) and asserts every old key EQUAL, `recorded_not_corrected` and `evidence` equal as wholes, and `value_guards` the only new top-level key |
| T-20-81 | Repudiation | a corrected claim that over-claims again | mitigate | The GATE-02 amendment states what the pair PROVES (one name by identity, one class by magnitude) and names its two watched cases; the GATE-06 amendment states the Y half was structural-not-behavioural. Both cite tests by name, and `20-VERIFICATION.md` re-resolves those names |
| T-20-82 | Repudiation | a continuation implying a completeness it did not achieve | mitigate | The addendum's fourth subsection names GC-05, GC-07 and GC-08…GC-12 as NOT closed, and `value_guards.census` records the two census residuals GC-06 left open, as a state rather than a silent omission |
| T-20-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install in this plan. The generator is a throwaway that imports only committed sibling modules and is not committed; `pyproject.toml` is untouched and sha256-pinned by `tests/test_package.py` |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_phase20_correction.py tests/test_phase20_prereg.py -q` →
  `32 passed`, no `skipped`, no `xfail`.
- `.venv/bin/python -m pytest -q` → at least `877 passed, 1 skipped`, zero failures.
- `.venv/bin/python -m pytest tests/test_phase20_prereg.py -q -k phase20_prereg_is_frozen` → green: three
  `results/phase20_*` first-adds still follow all nine pin commits. The ancestry guard takes
  `adds[-1]`, the EARLIEST add, so modifying an existing artifact cannot redden it — asserted by
  running it, not reasoned about.
- `ruff check . && ruff format --check .` → clean.
- `git diff --exit-code -- scripts/` → 0 (no module code changes in this plan).
- `git diff --exit-code -- scripts/mitigation_gate.py scripts/erasure_gate.py` → 0.
- Exactly three commits land, in order: JSON + guard, `.md` append, `REQUIREMENTS.md`.
</verification>

<success_criteria>
- The JSON carries `value_guards` and five new `defects` keys with every pre-existing value proved
  unchanged against the pre-write revision.
- Nothing moved out of `recorded_not_corrected`, and the artifact says why.
- A second dated continuation sits at the end of the `.md`, appended by `append_addendum` in its own
  commit, with zero deletions.
- `REQUIREMENTS.md` no longer carries a claim the measurement falsified.
- Both frozen files return 0 from `git diff --exit-code`; the ancestry guard is green.
</success_criteria>

<output>
Create `.planning/phases/20-pre-registration-the-three-condition-gate/20-16-SUMMARY.md` when done.
</output>
