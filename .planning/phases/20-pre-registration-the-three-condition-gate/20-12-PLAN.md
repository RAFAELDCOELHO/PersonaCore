---
phase: 20-pre-registration-the-three-condition-gate
plan: 12
type: execute
wave: 4
depends_on: [20-09, 20-11]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/20-pre-registration-the-three-condition-gate/20-SECURITY.md
autonomous: true
gap_closure: true
requirements: [GATE-06, GATE-02]

must_haves:
  truths:
    - "GATE-06 is marked complete only after the correction exists, is watched, and is enforced"
    - "20-SECURITY.md reaches threats_open: 0 with each closure pointing at a watched guard, never at a plan"
    - "T-20-21 and T-20-19 each carry a Watched-RED evidence row like the five before them"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "GATE-06 checked with a note naming the correction artifact, the module and the tripwire"
      contains: "phase20_gate_coverage_correction.json"
    - path: ".planning/phases/20-pre-registration-the-three-condition-gate/20-SECURITY.md"
      provides: "status verified, threats_open 0, both closures evidenced, sign-off complete"
      contains: "threats_open: 0"
  key_links:
    - from: "20-SECURITY.md Threat Register"
      to: "tests/test_phase20_correction.py"
      via: "each closed threat naming the test that watches it"
      pattern: "test_phase20_correction"
---

<objective>
Close the books — but only against evidence that already exists.

Purpose: 20-SECURITY.md is `status: blocked` with `threats_open: 2` and three Blocking Remediation
items. Its own text says accepting a realized mislabeling defect "would make the security gate green
over a known wrong verdict". This plan flips the gate ONLY after re-running the guards in its own
process. Nothing here is marked closed on the strength of a plan having been written.

Output: `.planning/REQUIREMENTS.md` GATE-06 discharged; `20-SECURITY.md` at `threats_open: 0`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-pre-registration-the-three-condition-gate/20-SECURITY.md
@.planning/phases/20-pre-registration-the-three-condition-gate/20-VERIFICATION.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Discharge GATE-06 in REQUIREMENTS.md, against a re-run rather than against a SUMMARY</name>

  <read_first>
    - `.planning/REQUIREMENTS.md:43-45` (GATE-06's bullet) and `:276` (its traceability row, which
      currently reads DEFERRED and carries the full reproduction detail).
    - `.planning/REQUIREMENTS.md:272` — GATE-02's row, whose RESIDUAL paragraph says the retention
      tripwire "ships as part of the same dated continuation as GATE-06 items 1 and 2, and all three
      close together". That sentence is what this task makes true.
    - `.planning/phases/20-pre-registration-the-three-condition-gate/20-11-SUMMARY.md` — the
      watched-RED evidence recorded there.
    - `results/phase20_gate_coverage_correction.json` — read `governs`, `supersedes`, `proof`.
  </read_first>

  <files>.planning/REQUIREMENTS.md</files>

  <action>
FIRST, in this process, run the commands in the verify block below and confirm they pass. Do not edit
anything until they do. If any fails, STOP and report — the requirement is not discharged, and
marking it would be exactly the over-claim this phase's own discipline held against for six plans.

Then flip GATE-06 at `:43` from `[ ]` to `[x]` and rewrite its traceability note at `:276`. The
existing note is a DEFERRAL and must be REPLACED by a discharge, not appended to — it currently says
"awaiting the Phase 20 gap-closure phase", which is now false. Preserve the reproduction detail, in
the past tense, because that is the evidence of what was corrected.

The new note must state, in this order:
  - The mechanism shipped at plan `20-04` and was DEFECTIVE on both axes — CR-01 (coverage decided
    on raw rates while condition (a) decides on `wilson_upper_bound(k, n)`) and WR-09 (no
    `sweep_heldout_recalls` parameter anywhere in the 21-kwarg signature).
  - Both reproduced directions with their fixtures NAMED — `FIXTURE_CLEARING_POINT` +
    `(1/104, 3/104)` gave `INCONCLUSIVE`, and `FIXTURE_DESTROYED_MODEL` + `(3/104, 11/104)` gave
    `FAIL` with no GATE-06 reason — plus the third case no prior report contains,
    `FIXTURE_CLEARING_POINT` + `(3/104, 11/104)` giving `PASS`.
  - That `scripts/mitigation_gate.py` was NOT edited and is byte-identical, so the pre-registration
    survives its own correction.
  - The discharge: `scripts/phase20_gate_coverage.py::coverage_verdict` decides each axis on the
    statistic that axis's criterion is decided on, covering the extraction axis and BOTH Y legs;
    `corrected_point_verdict` is the sanctioned route and has no `sweep_extraction_rates` parameter,
    so raw-rate space is unreachable through it.
  - The record: `results/phase20_gate_coverage_correction.json` (`governs` = sweep coverage
    (GATE-06); `supersedes` = `scripts/mitigation_gate.py:798-812`) and the dated continuation
    `results/phase20_gate_coverage_correction.md`.
  - The guard: `tests/test_phase20_correction.py`, both directions watched RED-then-GREEN, plus the
    AST caller census that reddens on the first Phase 23/25 caller that bypasses the route.
  - The bound-direction resolution in one clause, with its cost: Wilson upper on the X ceiling, raw
    rates on the Y floors, because a Wilson lower bound on a floor would decide coverage on a
    statistic condition (b) does not read. The Y legs therefore inherit condition (b)'s own lack of a
    confidence bound — recorded, and deliberately not fixed, because fixing it would move a
    pre-registered threshold after seeing the data it governs.

Then update GATE-02's row at `:272`: replace the `RESIDUAL — OPEN, bound to the GATE-06 gap-closure
phase` paragraph with a CLOSED statement naming
`scripts/phase20_gate_coverage.py::_prove_retention_floor` (four `_prove` calls — three mirroring
`extraction_ceiling:417/425/436`, plus a fourth refusing `V20_RETENTION_NOISE_FLOOR` by identity),
the choke-point property (called first in `corrected_point_verdict`, before any compute), and the
guard `tests/test_phase20_correction.py::test_the_retention_floor_tripwire_is_the_only_route_to_a_verdict`.
Leave the rest of the row — including the D-36 amendment cross-reference added at plan `20-09` —
untouched.

Do not touch RPT-02. It is correctly unchecked and correctly deferred to Phase 25.
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase20_correction.py tests/test_phase20_prereg.py -q && git diff --exit-code -- scripts/mitigation_gate.py scripts/erasure_gate.py && .venv/bin/python -c "import json,pathlib,re; j=json.loads(pathlib.Path('results/phase20_gate_coverage_correction.json').read_text('utf-8')); assert j['supersedes']=='scripts/mitigation_gate.py:798-812'; assert j['proof']=='tests/test_phase20_correction.py'; t=pathlib.Path('.planning/REQUIREMENTS.md').read_text(); assert re.search(r'^- \[x\] \*\*GATE-06\*\*',t,re.M); assert 'RESIDUAL' not in t or 'RESIDUAL - OPEN' not in t; [t.index(s) for s in ('phase20_gate_coverage_correction.json','test_phase20_correction.py','_prove_retention_floor','FIXTURE_DESTROYED_MODEL')]; assert re.search(r'^- \[ \] \*\*RPT-02\*\*',t,re.M); print('OK')"</automated>
  </verify>

  <acceptance_criteria>
    - The verify command was run BEFORE any edit and passed; `tests/test_phase20_prereg.py` reported
      `18 passed` and `tests/test_phase20_correction.py` exited 0.
    - `- [x] **GATE-06**` at `:43`; `- [ ] **RPT-02**` unchanged; CAL-04's row byte-identical.
    - GATE-06's traceability note names `FIXTURE_CLEARING_POINT`, `FIXTURE_DESTROYED_MODEL`,
      `coverage_verdict`, `corrected_point_verdict`,
      `results/phase20_gate_coverage_correction.json` and `tests/test_phase20_correction.py`, and
      states that `scripts/mitigation_gate.py` was not edited.
    - The phrase declaring GATE-02's residual OPEN no longer appears anywhere in
      `.planning/REQUIREMENTS.md`, and GATE-02's row still contains the D-36 amendment
      cross-reference written at plan `20-09`.
    - `git diff --exit-code -- scripts/mitigation_gate.py scripts/erasure_gate.py` exits 0.
  </acceptance_criteria>

  <done>
    GATE-06 and GATE-02's residual are both marked discharged, each note pointing at a committed
    artifact and a watched guard rather than at a plan.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Flip the security gate — threats_open 0, with a Watched-RED row per closure</name>

  <read_first>
    - `.planning/phases/20-pre-registration-the-three-condition-gate/20-SECURITY.md` — the whole
      file. Note the frontmatter, the `### Open` table, the `Watched-RED evidence` table (five rows),
      the `Accepted Risks Log`, the `Blocking Remediation` section, the `Security Audit Trail` and
      the `Sign-Off` checkboxes.
    - `.planning/phases/20-pre-registration-the-three-condition-gate/20-11-SUMMARY.md` — the observed
      output of each watched-RED break. Copy those observations; do not invent them.
    - The `<threat_model>` blocks of `20-08`, `20-09`, `20-10` and `20-11` — the new threat IDs
      T-20-47 through T-20-59 the correction itself introduced, each with its disposition.
  </read_first>

  <files>.planning/phases/20-pre-registration-the-three-condition-gate/20-SECURITY.md</files>

  <action>
Re-run the guards first, as in Task 1. This file must never go green on the strength of a plan.

Move **T-20-21** and **T-20-19** out of `### Open` into the closed register. Replace each
`INCOMPLETE — threat REALIZED` / `DISCHARGED AS STATED, RESIDUAL REMAINS` mitigation text with a
closure naming the artifact, the function and the test. KEEP the original reproduction detail in the
past tense — a threat register that erases its own findings on closure teaches nothing, and the value
of this register is that it records what was actually wrong.

Add the new threats from plans `20-08`, `20-09`, `20-10` and `20-11`, grouped by plan the way the
existing closed table is: T-20-47 (the correction is itself post-hoc), T-20-48 (a superseding module
callers can bypass), T-20-49 (an addendum that alters published text), T-20-50 (the sentinel sweep),
T-20-51 (a hand-typed float drifting from the code), T-20-52 (the D-36 in-place amendment to a
pre-registration record), T-20-53 (a second copy of an imported estimator), T-20-54 (`accept` —
`wilson_lower_bound` misused as a deciding statistic), T-20-55 (a traceability note naming a guard
that does not exist), T-20-56 (`accept` — re-checking already-checked boxes), T-20-57 (a defect
description outliving its defect), T-20-58 (`accept` — the new artifact reddening the ancestry
guard), T-20-59 (an audit matching the prose that explains the pattern).

Log the three `accept` dispositions in the Accepted Risks Log as R-20-05 (T-20-54), R-20-06 (T-20-56)
and R-20-07 (T-20-58) with their rationales and today's date. An accepted risk that never reaches the
log resurfaces in the next audit.

Extend the **Watched-RED evidence** table with one row per break actually observed in `20-11`,
matching the existing five rows' three-column shape (Threat ID / Deliberate break / Observed):
  - T-20-21 — `coverage_verdict`'s extraction axis switched to the raw rate; both direction tests
    observed failing; module restored byte-identically.
  - T-20-19 — one `_prove` deleted from `_prove_retention_floor`; its refusal case observed failing;
    restored byte-identically.
  - T-20-48 — a `mitigation_gate.mitigation_point_verdict(...)` call added to a scratch module under
    `scripts/`; the census observed firing, which is also what proves the `ast.Attribute` half that a
    bare-name matcher would have missed.
  - T-20-51 — the correction artifact's `cap` edited by one digit; the WR-02 test observed firing;
    restored.
Copy the observed output from `20-11-SUMMARY.md`. Do NOT write a row for a break that was not run.

Rewrite **Blocking Remediation**: keep the three-item table as the historical record of what was
required and add a resolution naming the plan and artifact that discharged each — items 1 and 2 by
`coverage_verdict` at `20-08` and the tripwires at `20-11`; item 3 by `_prove_retention_floor` at
`20-08` and its refusal suite at `20-11`. Change the section's opening from "Phase advancement is
blocked" to a closed statement, and update the `Gate status: BLOCKED` line near the top of the file.

Add a Security Audit Trail row dated `2026-08-20` with the new totals and `/gsd:plan-phase 20 --gaps`
plus plans `20-08`..`20-12` as the run-by.

Frontmatter: `status: verified`, `threats_open: 0`, and the threat total updated to the new count.

Sign-Off: check both currently-unchecked boxes and replace `**Approval:** pending — blocked on the
three-item dated continuation above.` with an approval naming the plans that discharged it and the
exact commands whose passing output was observed in this process.
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase20_correction.py tests/test_phase20_prereg.py -q && .venv/bin/python -c "import pathlib,re; p=pathlib.Path('.planning/phases/20-pre-registration-the-three-condition-gate/20-SECURITY.md'); t=p.read_text('utf-8'); fm=t.split('---')[1]; assert 'status: verified' in fm and 'threats_open: 0' in fm, fm; assert 'Gate status: BLOCKED' not in t; assert not re.search(r'^- \[ \]',t,re.M), 'an unchecked sign-off box remains'; [t.index(s) for s in ('T-20-47','T-20-48','T-20-49','T-20-50','T-20-51','T-20-52','R-20-05','R-20-06','R-20-07','test_phase20_correction.py','_prove_retention_floor','coverage_verdict')]; assert t.count('T-20-21')>=1 and t.count('T-20-19')>=1; print('OK')"</automated>
  </verify>

  <acceptance_criteria>
    - The verify command was run BEFORE any edit and passed.
    - Frontmatter reads `status: verified` and `threats_open: 0`; the threat total matches the
      register's row count.
    - `### Open` is empty or removed; T-20-21 and T-20-19 appear in the closed register with their
      original reproduction detail preserved and a closure naming
      `scripts/phase20_gate_coverage.py` and `tests/test_phase20_correction.py`.
    - The Watched-RED evidence table has NINE rows — the original five plus one per break actually
      observed in `20-11` — and every added row's Observed column is copied from
      `20-11-SUMMARY.md`, not composed here.
    - R-20-05, R-20-06 and R-20-07 are present in the Accepted Risks Log with rationales and dates.
    - `Gate status: BLOCKED` appears nowhere; both Sign-Off boxes are checked; the Approval line
      names plans `20-08`..`20-12` and the commands run.
    - The check above prints `OK`.
  </acceptance_criteria>

  <done>
    The security gate is green because the mitigations exist and were watched failing, and the
    register still records what was wrong.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a plan that says a thing will be done ↔ a guard that proves it was | Flipping a gate on the strength of a written plan is how a security gate goes green over a known wrong verdict — 20-SECURITY.md says so in its own words. |
| a closed threat ↔ the finding that produced it | A register that erases its findings on closure teaches nothing to the next audit. |
| a checked box ↔ the note beside it | A `[x]` is machine-readable; the note is not. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-21 | Repudiation | GATE-06 mislabeling | mitigate | Marked closed here ONLY after `tests/test_phase20_correction.py` and `tests/test_phase20_prereg.py` were re-run in this plan's own process and both passed. The closure text names `coverage_verdict`, the correction artifact and the tripwire. |
| T-20-19 | Spoofing | the unguarded retention leg | mitigate | Same discipline: closed against a re-run, with the closure naming `_prove_retention_floor` and its refusal suite. |
| T-20-60 | Repudiation | a gate flipped green ahead of its evidence | mitigate | Both tasks run the guards BEFORE editing and STOP on failure; every acceptance criterion asserts the observed state of the file rather than the intent of the edit. The Watched-RED rows must be copied from `20-11-SUMMARY.md` and a row for an unrun break is explicitly forbidden. |
| T-20-61 | Tampering | a closure that erases the finding it closes | mitigate | The original reproduction detail is preserved in the past tense; the acceptance criterion asserts it is still present after the move to the closed register. |
| T-20-62 | Repudiation | an accepted risk that never reaches the Accepted Risks Log | mitigate | R-20-05, R-20-06 and R-20-07 are asserted present by the verify check. `20-SECURITY.md`'s own line — "accepted risks do not resurface in future audit runs" — is only true if they are logged. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_phase20_correction.py tests/test_phase20_prereg.py -q`
  → both pass, prereg still `18 passed`, run BEFORE any edit in each task.
- `.venv/bin/python -m pytest -q` → no regression against `863 passed, 1 skipped`.
- `git diff --exit-code -- scripts/mitigation_gate.py scripts/erasure_gate.py` → exit 0.
- After this plan, re-run `/gsd:secure-phase 20` and `/gsd:verify-phase 20` — this plan updates the
  records; the orchestrator's own audit is what confirms them.
</verification>

<success_criteria>
- GATE-06 `[x]` with a discharge note; GATE-02's residual closed; RPT-02 untouched.
- `20-SECURITY.md` at `status: verified`, `threats_open: 0`, nine Watched-RED rows, three new
  accepted risks logged, both Sign-Off boxes checked.
- Every closure points at a committed artifact and a watched guard.
</success_criteria>

<output>
Create `.planning/phases/20-pre-registration-the-three-condition-gate/20-12-SUMMARY.md` when done.
</output>
