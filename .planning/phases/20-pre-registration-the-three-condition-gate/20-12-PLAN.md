---
phase: 20-pre-registration-the-three-condition-gate
plan: 12
type: execute
wave: 5
depends_on: [20-09, 20-11]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/20-pre-registration-the-three-condition-gate/20-SECURITY.md
autonomous: true
gap_closure: true
requirements: [GATE-06, GATE-02]

must_haves:
  truths:
    - "GATE-06 is marked complete only after the correction exists, is watched, and is enforced"
    - "ROADMAP Success Criterion 3 — the FAILED must-have — carries a dated amendment pointing at the correction, so a re-verifier reading SC3 cannot re-file the identical gap"
    - "20-SECURITY.md reaches threats_open: 0 with each closure pointing at a watched guard, never at a plan"
    - "T-20-21 and T-20-19 each carry a Watched-RED evidence row like the five before them"
    - "Every threat this gap-closure declared reaches the register the file publishes totals for, and the published total is substantiated by the file's own rows"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "GATE-06 checked with a note naming the correction artifact, the module and the tripwire"
      contains: "phase20_gate_coverage_correction.json"
    - path: ".planning/ROADMAP.md"
      provides: "SC3's dated amendment blockquote, in SC1's shape, leaving the original text unedited"
      contains: "Amended by D-34"
    - path: ".planning/phases/20-pre-registration-the-three-condition-gate/20-SECURITY.md"
      provides: "status verified, threats_open 0, both closures evidenced, nineteen new threats registered, eight inherited rows transcribed, sign-off complete"
      contains: "threats_open: 0"
  key_links:
    - from: "20-SECURITY.md Threat Register"
      to: "tests/test_phase20_correction.py"
      via: "each closed threat naming the test that watches it"
      pattern: "test_phase20_correction"
    - from: ".planning/ROADMAP.md SC3"
      to: "results/phase20_gate_coverage_correction.json"
      via: "the amendment naming the governing computation and the superseded block"
      pattern: "corrected_point_verdict"
---

<objective>
Close the books — but only against evidence that already exists.

Purpose: 20-SECURITY.md is `status: blocked` with `threats_open: 2` and three Blocking Remediation
items. Its own text says accepting a realized mislabeling defect "would make the security gate green
over a known wrong verdict". ROADMAP SC3 is the FAILED must-have — 20-VERIFICATION.md gap 1's `truth`
is SC3's text verbatim — and no other plan touches it, so without this plan a re-verifier reading SC3
meets an unqualified claim about a defect that was corrected elsewhere and re-files the identical
gap. This plan flips both gates ONLY after re-running the guards in its own process. Nothing here is
marked closed on the strength of a plan having been written.

Output: `.planning/REQUIREMENTS.md` GATE-06 discharged; `.planning/ROADMAP.md` SC3 amended;
`20-SECURITY.md` at `threats_open: 0`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-pre-registration-the-three-condition-gate/20-SECURITY.md
@.planning/phases/20-pre-registration-the-three-condition-gate/20-VERIFICATION.md
@.planning/REQUIREMENTS.md

<already_present_strings>
<!-- MEASURED against HEAD. Every one of these is in the target file BEFORE this plan runs, so a
     whole-file `assert s in text` on it CANNOT FAIL. The verify blocks below are scoped to the
     specific row or block this plan writes, for exactly that reason. -->

In `20-SECURITY.md`: `T-20-21` and `T-20-19` are both already present — they ARE the two OPEN
threats. `assert t.count('T-20-21') >= 1` therefore passes before this plan touches anything. The
check must assert the CLOSED state and the named guard, not the presence of the identifier.

In `.planning/REQUIREMENTS.md` after `20-09` runs: `scripts/phase20_gate_coverage.py`,
`tests/test_phase20_correction.py` and `_prove_retention_floor` are all written into the D-36
amendment by `20-09` Task 3. A whole-file check on them cannot prove this plan wrote the GATE-06
note, so those assertions are scoped to the `| GATE-06 | Phase 20 |` and `| GATE-02 | Phase 20 |`
rows.
</already_present_strings>

<inherited_count_facts>
<!-- MEASURED, and it changes what this plan should DO about the 46-vs-38 gap. -->

`20-SECURITY.md` publishes `46 threats` and names `38` distinct `T-20-NN` identifiers. The eight
absentees are exactly `T-20-26`…`T-20-30` and `T-20-36`…`T-20-38`.

They are NOT un-enumerated and NOT unsubstantiated. The closed table already lists them as inclusive
ranges:

    | 20-05 | T-20-25 … T-20-31 | Tampering ×4, EoP, Spoofing ×2 | closed |
    | 20-06 | T-20-33 … T-20-39 | EoP, Tampering ×3, Spoofing ×2, Repudiation | closed |

and all eight carry full mitigation text in the COMMITTED `20-05-PLAN.md` / `20-05-SUMMARY.md` and
`20-06-PLAN.md` / `20-06-SUMMARY.md` — verified present for each of the eight, one directory over.

So the total IS substantiated, it does NOT block `threats_open: 0`, and no memory or reconstruction
is involved. Transcribe the eight rows from those committed registers rather than disclosing a gap:
it is cheaper, stronger, and involves no fabrication. The count then reconciles to the file's own
rows and the discrepancy stops being inherited at all.
</inherited_count_facts>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Discharge GATE-06 and amend ROADMAP SC3 — against a re-run rather than against a SUMMARY</name>

  <read_first>
    - `.planning/REQUIREMENTS.md` — the `- [ ] **GATE-06**:` bullet, and the `| GATE-06 | Phase 20 |`
      traceability row, which currently reads DEFERRED and carries the full reproduction detail.
      **Anchor on those strings, not on line numbers**: plan `20-09` (wave 1) inserted a multi-line
      amendment blockquote into this file, so every line number recorded before it has shifted.
    - `.planning/REQUIREMENTS.md` — the `| GATE-02 | Phase 20 |` row, whose RESIDUAL paragraph says
      the retention tripwire "ships as part of the same dated continuation as GATE-06 items 1 and 2,
      and all three close together". That sentence is what this task makes true. The row also now
      carries a cross-reference clause added at `20-09`; leave that clause intact.
    - `.planning/ROADMAP.md` — Success Criterion 3, beginning `Every verdict branch is **watched
      firing**`, and Success Criterion 1's `> **Amended by D-06 at plan `20-07`, and the amendment is
      TIGHTER.**` blockquote. SC1's blockquote is the exact SHAPE to mirror; its claim is not.
    - `.planning/phases/20-pre-registration-the-three-condition-gate/20-11-SUMMARY.md` — the
      watched-RED evidence recorded there.
    - `results/phase20_gate_coverage_correction.json` — read `governs`, `supersedes`, `proof`.
  </read_first>

  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>

  <action>
FIRST, in this process, run the verify block below and confirm it passes. Do not edit anything until
it does. If it fails, STOP and report — the requirement is not discharged, and marking it would be
exactly the over-claim this phase's own discipline held against for six plans.

**A. REQUIREMENTS.md — GATE-06.** Flip the `- [ ] **GATE-06**:` bullet to `- [x]` and REPLACE the
`| GATE-06 | Phase 20 |` row's traceability note. The existing note is a DEFERRAL and must be
replaced by a discharge, not appended to — it currently says "awaiting the Phase 20 gap-closure
phase", which is now false. Preserve the reproduction detail, in the past tense, because that is the
evidence of what was corrected.

The new note must state, in this order:
  - The mechanism shipped at plan `20-04` and was DEFECTIVE on both axes — CR-01 (coverage decided
    on raw rates while condition (a) decides on `wilson_upper_bound(k, n)`) and WR-09 (no
    `sweep_heldout_recalls` parameter anywhere in the 21-kwarg signature).
  - Both reproduced directions with their fixtures NAMED — `FIXTURE_CLEARING_POINT` +
    `(1/104, 3/104)` gave `INCONCLUSIVE`, and `FIXTURE_DESTROYED_MODEL` + `(3/104, 11/104)` gave
    `FAIL` with no GATE-06 reason — plus the third case no prior report contains,
    `FIXTURE_CLEARING_POINT` + `(3/104, 11/104)` giving `PASS`.
  - That `scripts/mitigation_gate.py` was NOT edited and is byte-identical, so the pre-registration
    survives its own correction.
  - The discharge: `scripts/phase20_gate_coverage.py::coverage_verdict` decides each axis on the
    statistic that axis's criterion is decided on, covering the extraction axis and BOTH Y legs;
    `corrected_point_verdict` is the sanctioned route and has no `sweep_extraction_rates` parameter,
    so raw-rate space is unreachable through it and a raw rate handed to the count parameter is
    refused by name.
  - The record: `results/phase20_gate_coverage_correction.json` (`governs` = sweep coverage
    (GATE-06); `supersedes` = `scripts/mitigation_gate.py:798-812`) and the dated continuation
    `results/phase20_gate_coverage_correction.md`.
  - The guard: `tests/test_phase20_correction.py`, both directions watched RED-then-GREEN, plus the
    AST caller census that reddens on the first Phase 23/25 caller that bypasses the route.
  - The bound-direction resolution (D-37) in one clause, with its cost: Wilson upper on the X
    ceiling, raw rates on the Y floors, because a Wilson lower bound on a floor would decide coverage
    on a statistic condition (b) does not read. The Y legs therefore inherit condition (b)'s own lack
    of a confidence bound — recorded, and deliberately not fixed, because fixing it would move a
    pre-registered threshold after seeing the data it governs.

**B. REQUIREMENTS.md — GATE-02's residual.** In the `| GATE-02 | Phase 20 |` row, replace the
`RESIDUAL — OPEN, bound to the GATE-06 gap-closure phase` paragraph with a CLOSED statement naming
`scripts/phase20_gate_coverage.py::_prove_retention_floor` (four `_prove` calls — three mirroring
`extraction_ceiling`'s at `:417` / `:425` / `:436`, plus a fourth refusing `V20_RETENTION_NOISE_FLOOR`
by identity), the choke-point property (called first in `corrected_point_verdict`, before any
compute), and the guard
`tests/test_phase20_correction.py::test_the_retention_floor_tripwire_is_the_only_route_to_a_verdict`.
Leave the rest of the row — including the D-36 amendment cross-reference added at plan `20-09` —
untouched.

Do not touch the `- [ ] **RPT-02**` bullet. It is correctly unchecked and correctly deferred to
Phase 25.

**C. ROADMAP.md — Success Criterion 3.** SC3 is the FAILED must-have. Its GATE-06 clause — "a sweep
that never produced points on both sides of X (or of Y) returns `INCONCLUSIVE` rather than
`FAILURE`" — is an unqualified claim about the verdict behaviour of `scripts/mitigation_gate.py`,
which is permanently frozen and stays defective on both axes forever. Add a dated amendment
blockquote immediately after SC3's text and before Success Criterion 4, in SC1's shape: leave every
word of the original SC3 above it unedited, exactly as SC1 preserves its own `4.029000`.

Open with `> **Amended by D-34 and D-37 at plan `20-12`. SC3's GATE-06 clause is SUPERSEDED for
v4.0.**` The body must carry:
  - What was wrong: the frozen block decided coverage on RAW rates while condition (a) decides on
    `wilson_upper_bound(k, n)` (CR-01), and it had no held-out leg to decide at all (WR-09).
  - What governs now: `scripts/phase20_gate_coverage.py::coverage_verdict` for the computation and
    `::corrected_point_verdict` for the route to a v4.0 verdict — SC3's clause is read through those,
    not through `mitigation_gate.mitigation_point_verdict` directly.
  - The record and the guard, by path: `results/phase20_gate_coverage_correction.json` (its `governs`
    and `supersedes` fields) and `tests/test_phase20_correction.py`.
  - **That `scripts/mitigation_gate.py` was NOT edited** — it is byte-identical to its pinned commits
    and the ancestry guard is still green. The correction is a dated continuation, per D-24.
  - **The honesty clause, which is where this amendment differs from SC1's.** Get the DIRECTION claim
    right; an earlier draft of this plan did not, and the reason is worth stating. On the
    favourability axis `FAIL < INCONCLUSIVE < PASS` the two reproduced directions move THE SAME WAY:
    direction (i) `INCONCLUSIVE → PASS` and direction (ii) `FAIL → INCONCLUSIVE` are BOTH more
    favourable to the claim. Describing that pair as movement "in both directions" would publish a
    small over-claim inside the very amendment whose purpose is to prevent one. Write it as:

    > Not uniformly tighter — both reproduced directions move toward a MORE favourable verdict; the
    > tightening is supplied by the third, previously unreported case, where `FIXTURE_CLEARING_POINT`
    > under `(3/104, 11/104)` is DEMOTED from `PASS` to `INCONCLUSIVE`. The justification is
    > criterion-matching, not conservatism.

    Then state that each axis's coverage test reads the statistic that axis's criterion reads, and
    that the demoted `PASS` — the only against-interest movement, and a row that appears in no prior
    report — is published anyway. Do not write the word `TIGHTER` in this block: SC1's opener claims
    a direction this amendment does not have. The lowercase phrase "Not uniformly tighter" above is
    intended and is what the verify permits.
  </action>

  <verify>
    <automated>.venv/bin/python -c "
import json,pathlib,re,subprocess,sys
r=subprocess.run([sys.executable,'-m','pytest','tests/test_phase20_correction.py','tests/test_phase20_prereg.py','-q'],capture_output=True,text=True)
out=r.stdout+r.stderr
assert r.returncode==0, out[-3000:]
assert 'skipped' not in out and 'xfail' not in out, ('zero skips required', out[-3000:])
d=subprocess.run(['git','diff','--exit-code','--','scripts/mitigation_gate.py','scripts/erasure_gate.py'],capture_output=True,text=True)
assert d.returncode==0, ('a frozen file changed', d.stdout[-2000:])
j=json.loads(pathlib.Path('results/phase20_gate_coverage_correction.json').read_text('utf-8'))
assert j['supersedes']=='scripts/mitigation_gate.py:798-812'
assert j['proof']=='tests/test_phase20_correction.py'
t=pathlib.Path('.planning/REQUIREMENTS.md').read_text('utf-8')
assert re.search(r'^- \[x\] \*\*GATE-06\*\*',t,re.M), 'GATE-06 bullet not checked'
assert re.search(r'^- \[ \] \*\*RPT-02\*\*',t,re.M), 'RPT-02 must stay unchecked'
assert 'RESIDUAL — OPEN' not in t, 'GATE-02 residual still declared OPEN'
assert 'Amended by D-36' in t, 'the 20-09 amendment must survive'
def row(rid):
    rr=[l for l in t.splitlines() if l.startswith('| '+rid+' | Phase 20 |')]
    assert len(rr)==1, (rid, len(rr))
    return rr[0]
g6=row('GATE-06')
for s in ('phase20_gate_coverage_correction.json','tests/test_phase20_correction.py','FIXTURE_DESTROYED_MODEL','FIXTURE_CLEARING_POINT','coverage_verdict','corrected_point_verdict'):
    assert s in g6, ('missing from the GATE-06 ROW — a whole-file check cannot fail here, 20-09 wrote several of these into the D-36 amendment', s)
g2=row('GATE-02')
for s in ('_prove_retention_floor','test_the_retention_floor_tripwire_is_the_only_route_to_a_verdict'):
    assert s in g2, ('missing from the GATE-02 ROW', s)
rm=pathlib.Path('.planning/ROADMAP.md').read_text('utf-8')
i=rm.index('Every verdict branch is **watched firing**')
a=rm.index('Amended by D-34')
k=rm.index('The n=8-vs-n=64 capacity comparison rule')
assert i<a<k, 'SC3 amendment must sit between SC3 and SC4'
assert rm.index('(GATE-05, GATE-06, GATE-07, GATE-08, GATE-09)')<a, 'original SC3 text must be preserved above the amendment'
blk=rm[a:k]
for s in ('phase20_gate_coverage_correction.json','coverage_verdict','corrected_point_verdict','tests/test_phase20_correction.py','20-12','DEMOTED','FIXTURE_CLEARING_POINT'):
    assert s in blk, ('missing from the SC3 amendment block', s)
assert 'criterion-match' in blk.lower(), 'the amendment must name criterion-matching as the justification'
assert 'TIGHTER' not in blk, 'SC3 amendment must not claim a direction it does not have'
assert rm.count('Amended by D-06')==1, 'SC1 own amendment must be untouched'
print('OK')"</automated>
  </verify>

  <acceptance_criteria>
    - The verify command was run BEFORE any edit and passed; both test files exited 0 with zero
      skips, and `git diff --exit-code` on the two frozen files returned 0.
    - The `- [x] **GATE-06**` bullet is checked; the `- [ ] **RPT-02**` bullet is unchanged; the
      `| CAL-04 | Phase 20 |` row is byte-identical.
    - GATE-06's traceability ROW — not merely the file — names `FIXTURE_CLEARING_POINT`,
      `FIXTURE_DESTROYED_MODEL`, `coverage_verdict`, `corrected_point_verdict`,
      `results/phase20_gate_coverage_correction.json` and `tests/test_phase20_correction.py`, and
      states that `scripts/mitigation_gate.py` was not edited. The row scoping is required, not
      stylistic: `20-09` writes several of those strings into the D-36 amendment elsewhere in the
      same file, so a whole-file check could not fail.
    - The GATE-02 ROW names `_prove_retention_floor` and
      `test_the_retention_floor_tripwire_is_the_only_route_to_a_verdict`; the phrase declaring its
      residual OPEN (`RESIDUAL — OPEN`, em dash) no longer appears anywhere in
      `.planning/REQUIREMENTS.md`; and the row still contains the D-36 amendment cross-reference
      written at plan `20-09`.
    - `.planning/ROADMAP.md` carries an `Amended by D-34` blockquote positioned after SC3's original
      text and before SC4, naming the governing computation, the governing route, the correction
      artifact and the tripwire, and stating that the pin was not edited.
    - SC3's original text is byte-identical above the amendment, and SC1's own
      `Amended by D-06` blockquote occurs exactly once and is unchanged.
    - The SC3 amendment does NOT contain the word `TIGHTER`, DOES contain `DEMOTED` and
      `FIXTURE_CLEARING_POINT`, and names `criterion-matching` as the justification — so the
      corrected honesty clause is machine-checked rather than trusted. It must not describe the two
      reproduced directions as moving "in both directions": both move toward a more favourable
      verdict, and the only against-interest movement is the third case.
  </acceptance_criteria>

  <done>
    GATE-06, GATE-02's residual and ROADMAP SC3 are all marked discharged or amended, each pointing
    at a committed artifact and a watched guard rather than at a plan, the SC3 amendment states the
    direction of its own movement correctly, and a re-verifier reading SC3 is routed to the
    correction instead of re-filing the gap.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Flip the security gate — threats_open 0, nineteen new threats registered, eight inherited rows transcribed, a Watched-RED row per closure</name>

  <read_first>
    - `.planning/phases/20-pre-registration-the-three-condition-gate/20-SECURITY.md` — the whole
      file. Note the frontmatter, the `**Gate status: BLOCKED.**` line, the `46 threats. **44 closed,
      2 open.**` totals line, the `### Open` table, the `Watched-RED evidence` table (five rows), the
      `Accepted Risks Log`, the `Blocking Remediation` section, the `Security Audit Trail` and the
      `Sign-Off` checkboxes.
    - `.planning/phases/20-pre-registration-the-three-condition-gate/20-11-SUMMARY.md` — the observed
      output of each watched-RED break. Copy those observations; do not invent them. There are FOUR,
      not five: one in `20-11` Task 1 and three in `20-11` Task 3.
    - `20-05-PLAN.md`, `20-05-SUMMARY.md`, `20-06-PLAN.md`, `20-06-SUMMARY.md` — the committed
      registers carrying the full mitigation text for `T-20-26`…`T-20-30` and `T-20-36`…`T-20-38`.
      These are the SOURCE for the eight transcribed rows. Read them; do not compose from memory.
    - The `<threat_model>` blocks of `20-08`, `20-09`, `20-10`, `20-11` AND THIS PLAN — the new
      threat IDs, each with its disposition. This plan's own three (`T-20-60`, `T-20-61`, `T-20-62`)
      are declared below. Note also `T-20-63` (declared in `20-09`), `T-20-64` (`20-10`) and
      `T-20-65` (`20-11`) — the three "a guard that cannot fail" threats added when this plan set was
      reviewed, and exactly the ones a register aggregated from an earlier draft would drop.
  </read_first>

  <files>.planning/phases/20-pre-registration-the-three-condition-gate/20-SECURITY.md</files>

  <action>
Re-run the guards first, as in Task 1. This file must never go green on the strength of a plan.

Move **T-20-21** and **T-20-19** out of `### Open` into the closed register. Replace each
`INCOMPLETE — threat REALIZED` / `DISCHARGED AS STATED, RESIDUAL REMAINS` mitigation text with a
closure naming the artifact, the function and the test. KEEP the original reproduction detail in the
past tense — a threat register that erases its own findings on closure teaches nothing, and the value
of this register is that it records what was actually wrong. Each closed row must name
`tests/test_phase20_correction.py`: a closure that does not name its watching guard is precisely what
this plan exists to refuse, and the verify asserts it per row rather than file-wide, because both IDs
are already present in the file as OPEN threats and a presence check on them cannot fail.

**Transcribe the eight inherited rows.** `T-20-26`…`T-20-30` and `T-20-36`…`T-20-38` are counted in
the published `46` but were never written into this file as individual rows. They are NOT
unsubstantiated: the closed table already enumerates them as the inclusive ranges
`T-20-25 … T-20-31` and `T-20-33 … T-20-39`, and each of the eight carries full mitigation text in
the committed `20-05-PLAN.md` / `20-05-SUMMARY.md` and `20-06-PLAN.md` / `20-06-SUMMARY.md`
(verified present for all eight). Copy each row's ID, category, component and mitigation text from
those committed registers into this file's closed table, citing the source file per row. This is
transcription from committed text one directory over — no memory, no reconstruction, no fabrication —
and it is cheaper and stronger than a disclosure sentence. Once done, the published total reconciles
to the file's own rows and there is no inherited discrepancy left to disclose.

Add the NEW threats from plans `20-08`, `20-09`, `20-10`, `20-11` and `20-12`, grouped by plan the
way the existing closed table is:
  - `20-08` — T-20-48 (a superseding module callers can bypass), T-20-50 (the sentinel sweep),
    T-20-53 (a second copy of an imported estimator), T-20-54 (`accept` — `wilson_lower_bound`
    misused as a deciding statistic).
  - `20-09` — T-20-52 (the D-36 in-place amendment to a pre-registration record), T-20-55 (a
    traceability note naming a guard that does not exist), T-20-56 (`accept` — re-checking
    already-checked boxes), T-20-63 (a verify assertion that passes on text the file already
    contained).
  - `20-10` — T-20-47 (the correction is itself post-hoc), T-20-49 (an addendum that alters published
    text), T-20-51 (a hand-typed float drifting from the code), T-20-57 (a defect description
    outliving its defect), T-20-58 (`accept` — the new artifact reddening the ancestry guard),
    T-20-64 (a two-commit rule enforced by a check that passes on one commit).
  - `20-11` — T-20-59 (an audit matching the prose that explains the pattern), T-20-65 (a test-suite
    guard that passes on a degraded suite).
  - `20-12` — **T-20-60** (a gate flipped green ahead of its evidence), **T-20-61** (a closure that
    erases the finding it closes), **T-20-62** (an accepted risk that never reaches the Accepted
    Risks Log), **T-20-66** (an amendment that mis-states its own direction of movement). All four
    `mitigate`; their mitigation text is in this plan's `<threat_model>` below.

That is `T-20-47` through `T-20-66` — TWENTY new IDs.

Update the totals line, and state the arithmetic where it is published rather than leaving it to be
reconstructed: `38` rows previously named, `+ 8` inherited rows now transcribed from `20-05` /
`20-06`, `+ 20` new gap-closure threats `= 66 threats. **66 closed, 0 open.**` Every one of those 66
is now an actual row in this file, so the published total is substantiated by the file's own rows
rather than inherited from a prior audit. Say that in one sentence beside the total and cite
`20-05` / `20-06` as the source of the eight transcribed rows.

Log the three `accept` dispositions in the Accepted Risks Log as R-20-05 (T-20-54), R-20-06 (T-20-56)
and R-20-07 (T-20-58) with their rationales and today's date. An accepted risk that never reaches the
log resurfaces in the next audit. T-20-60/61/62/63/64/65/66 are `mitigate` and get no R- entry.

Extend the **Watched-RED evidence** table with one row per break actually observed in `20-11`,
matching the existing five rows' three-column shape (Threat ID / Deliberate break / Observed):
  - T-20-21 — `coverage_verdict`'s extraction axis switched to the raw rate; both direction tests
    observed failing; module restored byte-identically.
  - T-20-19 — one `_prove` deleted from `_prove_retention_floor`; its refusal case observed failing;
    restored byte-identically.
  - T-20-48 — a `mitigation_gate.mitigation_point_verdict(...)` call added to a scratch module under
    `scripts/`; the census observed firing, which is also what proves the `ast.Attribute` half that a
    bare-name matcher would have missed.
  - T-20-51 — the correction artifact's `cap` edited by one digit; the WR-02 test observed firing;
    restored.
Copy the observed output from `20-11-SUMMARY.md`. Do NOT write a row for a break that was not run.
The table ends at NINE rows: the original five plus these four, and the verify counts them.

Rewrite **Blocking Remediation**: keep the three-item table as the historical record of what was
required and add a resolution naming the plan and artifact that discharged each — items 1 and 2 by
`coverage_verdict` at `20-08` and the tripwires at `20-11`; item 3 by `_prove_retention_floor` at
`20-08` and its refusal suite at `20-11`. Change the section's opening from "Phase advancement is
blocked" to a closed statement, and update the `**Gate status: BLOCKED.**` line near the top of the
file.

Add a Security Audit Trail row dated `2026-08-20` with the new totals (65 / 65 / 0) and
`/gsd:plan-phase 20 --gaps` plus plans `20-08`..`20-12` as the run-by.

Frontmatter: `status: verified`, `threats_open: 0`.

Sign-Off: check both currently-unchecked boxes and replace `**Approval:** pending — blocked on the
three-item dated continuation above.` with an approval naming the plans that discharged it and the
exact commands whose passing output was observed in this process.
  </action>

  <verify>
    <automated>.venv/bin/python -c "
import pathlib,re,subprocess,sys
r=subprocess.run([sys.executable,'-m','pytest','tests/test_phase20_correction.py','tests/test_phase20_prereg.py','-q'],capture_output=True,text=True)
out=r.stdout+r.stderr
assert r.returncode==0, out[-3000:]
assert 'skipped' not in out and 'xfail' not in out, ('zero skips required', out[-3000:])
p=pathlib.Path('.planning/phases/20-pre-registration-the-three-condition-gate/20-SECURITY.md')
t=p.read_text('utf-8'); fm=t.split('---')[1]
assert 'status: verified' in fm and 'threats_open: 0' in fm, fm
assert 'Gate status: BLOCKED' not in t
assert not re.search(r'^- \[ \]',t,re.M), 'an unchecked sign-off box remains'
def rows_for(tid):
    return [l for l in t.splitlines() if l.strip().startswith('| '+tid+' ')]
missing=[('T-20-%d'%n) for n in range(47,67) if not rows_for('T-20-%d'%n)]
assert not missing, ('new threats absent from the register as ROWS', missing)
inherited=[('T-20-%d'%n) for n in (26,27,28,29,30,36,37,38) if not rows_for('T-20-%d'%n)]
assert not inherited, ('the eight inherited threats must be TRANSCRIBED as register rows from the committed 20-05/20-06 registers, not merely mentioned', inherited)
for tid in ('T-20-%d'%n for n in (26,27,28,29,30,36,37,38)):
    joined=' '.join(rows_for(tid))
    assert '20-05' in joined or '20-06' in joined, (tid,'a transcribed row must CITE the register it came from; a whole-file check for the strings 20-05/20-06 cannot fail — both already occur in the closed-table plan column')
assert '66 threats' in t, 'the reconciled total must be published (38 named + 8 transcribed + 20 new)'
assert '65 threats' not in t, 'the total must count T-20-66, which 20-12 declares in its own threat_model'
assert '44 closed, 2 open' not in t, 'the old totals line survives'
ids=set(re.findall(r'T-20-\d+',t))
assert len(ids)==66, ('the file must NAME every threat it counts, and no more', len(ids))
for tid in ('T-20-21','T-20-19'):
    joined=' '.join(rows_for(tid))
    assert joined, ('no register row for '+tid)
    assert 'closed' in joined.lower(), (tid,'not marked closed — this ID was already present as an OPEN threat, so a bare presence check cannot fail')
    assert 'test_phase20_correction.py' in joined, (tid,'closure does not name the guard that watches it')
assert 'INCOMPLETE — threat REALIZED' not in t
assert 'DISCHARGED AS STATED, RESIDUAL REMAINS' not in t
seg=t.split('Watched-RED evidence')[1].split('\n## ')[0]
wr=[l for l in seg.splitlines() if l.strip().startswith('| T-20-')]
assert len(wr)==9, ('Watched-RED table must carry 9 rows (5 original + 4 observed at 20-11)', len(wr))
for s in ('R-20-05','R-20-06','R-20-07','_prove_retention_floor','coverage_verdict','corrected_point_verdict'):
    assert s in t, ('missing from 20-SECURITY.md', s)
# '20-05'/'20-06' are NOT whole-file-checked here: both already occur in the closed table's plan
# column, so such a check could never go RED. Their real property — each transcribed row citing its
# source register — is asserted per-row above.
print('OK —', len(ids), 'threat IDs named as rows, 9 watched-RED rows, totals reconciled')"</automated>
  </verify>

  <acceptance_criteria>
    - The verify command was run BEFORE any edit and passed, with zero skips in both test files.
    - Frontmatter reads `status: verified` and `threats_open: 0`; the published total reads
      `66 threats. **66 closed, 0 open.**` with its arithmetic (`38 + 8 + 20`) stated beside it, and
      the old `44 closed, 2 open` line is gone.
    - Every ID from `T-20-47` through `T-20-66` appears as a REGISTER ROW — twenty, including this
      plan's own `T-20-60`/`61`/`62`/`66` and the three review-added `T-20-63`/`64`/`65`. `T-20-66`
      is declared in this plan's own `<threat_model>`, so omitting it would leave the register
      publishing a total it does not enumerate — the exact defect `T-20-62` names.
    - Each of the eight transcribed rows CITES `20-05` or `20-06` in its own row text.
    - `T-20-26`…`T-20-30` and `T-20-36`…`T-20-38` appear as transcribed register ROWS with mitigation
      text copied from the committed `20-05` / `20-06` PLAN and SUMMARY registers, each citing its
      source. No row is composed from memory, and no disclosure sentence substitutes for a row.
    - The file NAMES at least 65 distinct `T-20-NN` identifiers, so the published total is
      substantiated by the file's own rows rather than inherited.
    - `### Open` is empty or removed; T-20-21 and T-20-19 appear in the closed register with their
      original reproduction detail preserved, each row marked closed and each naming
      `tests/test_phase20_correction.py`. Asserted per row, because both IDs were already in the file
      as OPEN threats and a whole-file presence check on them could not fail.
    - The Watched-RED evidence table has EXACTLY NINE `| T-20-` rows — the original five plus one per
      break actually observed in `20-11` (there are four) — counted by the verify, and every added
      row's Observed column is copied from `20-11-SUMMARY.md`, not composed here.
    - R-20-05, R-20-06 and R-20-07 are present in the Accepted Risks Log with rationales and dates;
      no R- entry is written for a `mitigate` disposition.
    - `Gate status: BLOCKED` appears nowhere; both Sign-Off boxes are checked; the Approval line
      names plans `20-08`..`20-12` and the commands run.
    - The check above prints `OK`.
  </acceptance_criteria>

  <done>
    The security gate is green because the mitigations exist and were watched failing, the register
    still records what was wrong, every threat the gap-closure declared is in the register, and the
    published total is substantiated by rows this file actually carries — the eight inherited IDs
    transcribed from committed registers rather than disclosed as a gap.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a plan that says a thing will be done ↔ a guard that proves it was | Flipping a gate on the strength of a written plan is how a security gate goes green over a known wrong verdict — 20-SECURITY.md says so in its own words. |
| a closed threat ↔ the finding that produced it | A register that erases its findings on closure teaches nothing to the next audit. |
| a checked box ↔ the note beside it | A `[x]` is machine-readable; the note is not. |
| a success criterion ↔ the artifact that superseded it | An unqualified SC3 outlives the defect it describes and invites the same gap to be re-filed. |
| a published total ↔ the rows that substantiate it | The file counted 46 and named 38. The eight absentees are enumerated by range and carry mitigation text one directory over, so the fix is transcription, not disclosure. |
| a claimed direction of movement ↔ the ordering it implies | `FAIL < INCONCLUSIVE < PASS`. Two moves that both increase favourability are not "both directions", and saying so inside an anti-over-claim amendment is an over-claim. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-21 | Repudiation | GATE-06 mislabeling | mitigate | Marked closed here ONLY after `tests/test_phase20_correction.py` and `tests/test_phase20_prereg.py` were re-run in this plan's own process and both passed with zero skips. The closure text names `coverage_verdict`, the correction artifact and the tripwire, and ROADMAP SC3 — the criterion the threat was realized against — carries a dated amendment pointing at the same three. |
| T-20-19 | Spoofing | the unguarded retention leg | mitigate | Same discipline: closed against a re-run, with the closure naming `_prove_retention_floor` and its refusal suite, and GATE-02's traceability row rewritten from RESIDUAL-OPEN to a discharge naming the same function and guard. |
| T-20-60 | Repudiation | a gate flipped green ahead of its evidence | mitigate | Both tasks run the guards BEFORE editing and STOP on failure; every acceptance criterion asserts the observed state of the file rather than the intent of the edit. The Watched-RED rows must be copied from `20-11-SUMMARY.md`, a row for an unrun break is explicitly forbidden, and the verify COUNTS the table at exactly nine. The verify checks each of `T-20-47`…`T-20-65` by name as a register row rather than trusting the published total. |
| T-20-61 | Tampering | a closure that erases the finding it closes | mitigate | The original reproduction detail is preserved in the past tense; the acceptance criterion asserts it is still present after the move to the closed register, and the verify asserts each closed row names its watching guard. The same rule governs ROADMAP SC3 and the GATE-02 requirement bullet: both originals stay byte-identical above their amendments. |
| T-20-62 | Repudiation | an accepted risk that never reaches the Accepted Risks Log | mitigate | R-20-05, R-20-06 and R-20-07 are asserted present by the verify check. `20-SECURITY.md`'s own line — "accepted risks do not resurface in future audit runs" — is only true if they are logged. The same reasoning forces the eight inherited rows to be TRANSCRIBED rather than disclosed: eight IDs counted in the published total but carried by no row would resurface in the next audit as phantom coverage, and the mitigation text is committed one directory over, so transcription costs nothing and fabricates nothing. |
| T-20-66 | Repudiation | an amendment that mis-states its own direction of movement | mitigate | MEASURED by running all three cases: direction (i) `INCONCLUSIVE → PASS`, direction (ii) `FAIL → INCONCLUSIVE`, third case `PASS → INCONCLUSIVE`. On the favourability ordering the first two BOTH move toward a more favourable verdict, so an earlier draft's "in both directions" was an over-claim published inside an anti-over-claim amendment. The SC3 block now says "Not uniformly tighter", attributes the tightening solely to the third demoted case, and names criterion-matching as the justification. The verify asserts `DEMOTED`, `FIXTURE_CLEARING_POINT` and `criterion-match` are present in the block and that `TIGHTER` is absent, so the corrected claim is machine-checked rather than trusted to prose review. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_phase20_correction.py tests/test_phase20_prereg.py -q`
  → both pass with zero skips, run BEFORE any edit in each task via the subprocess check.
- `.venv/bin/python -m pytest -q` → no regression against `863 passed, 1 skipped`.
- `git diff --exit-code -- scripts/mitigation_gate.py scripts/erasure_gate.py` → exit 0, asserted
  inside Task 1's verify rather than as a separate shell step.
- After this plan, re-run `/gsd:secure-phase 20` and `/gsd:verify-phase 20` — this plan updates the
  records; the orchestrator's own audit is what confirms them.
</verification>

<success_criteria>
- GATE-06 `[x]` with a discharge note in its own ROW; GATE-02's residual closed; RPT-02 untouched.
- ROADMAP SC3 carries a dated `Amended by D-34` blockquote naming the governing computation, the
  correction artifact and the tripwire, with its original text unedited above it, no TIGHTER claim,
  and the direction of movement stated correctly.
- `20-SECURITY.md` at `status: verified`, `threats_open: 0`, `T-20-47`…`T-20-65` all registered as
  rows, the eight inherited IDs transcribed from the committed `20-05` / `20-06` registers, the total
  reconciled to `65` and substantiated by the file's own rows, nine Watched-RED rows, three new
  accepted risks logged, both Sign-Off boxes checked.
- Every closure points at a committed artifact and a watched guard.
</success_criteria>

<output>
Create `.planning/phases/20-pre-registration-the-three-condition-gate/20-12-SUMMARY.md` when done.
</output>
