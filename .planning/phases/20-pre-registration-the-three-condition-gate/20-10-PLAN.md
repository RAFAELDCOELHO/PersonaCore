---
phase: 20-pre-registration-the-three-condition-gate
plan: 10
type: execute
wave: 3
depends_on: [20-08]
files_modified:
  - results/phase20_gate_coverage_correction.md
  - results/phase20_gate_coverage_correction.json
autonomous: true
gap_closure: true
requirements: [GATE-06, GATE-02]

must_haves:
  truths:
    - "The correction travels as a machine-readable artifact naming what it governs and what it supersedes, not as a prose note"
    - "The published reading the frozen pin computes is preserved unedited, with the correction added beside it"
    - "A reader meets both verdicts for both reproduced directions and is told which one governs"
  artifacts:
    - path: "results/phase20_gate_coverage_correction.md"
      provides: "the dated continuation — the pin's literal reading, then the appended addendum"
      contains: "## Verdict"
    - path: "results/phase20_gate_coverage_correction.json"
      provides: "the machine-readable half with governs / supersedes / defects / evidence / proof"
      contains: "\"governs\""
  key_links:
    - from: "results/phase20_gate_coverage_correction.json"
      to: "scripts/phase20_gate_coverage.py"
      via: "governing_module + governing_entry_point fields"
      pattern: "corrected_point_verdict"
    - from: "results/phase20_gate_coverage_correction.md"
      to: "scripts/_addendum.py"
      via: "append_addendum(path, addendum, pending=..., recorded=...)"
      pattern: "Addendum — 2026-08-20"
---

<objective>
Publish the D-24 dated continuation: the correction artifact with its `governs` field, and the
append-only prose half recording it beside the pin's literal reading.

Purpose: `scripts/mitigation_gate.py` is permanently uneditable and 20-VERIFICATION.md's gap 1 lists
this artifact as MISSING outright — "no correction artifact, no `append_addendum` call, no tripwire
exists in the repository". This is the half that makes the correction citable by Phase 23 and 25.

Output: `results/phase20_gate_coverage_correction.md` and
`results/phase20_gate_coverage_correction.json`, in **two commits** — the second is not optional.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-pre-registration-the-three-condition-gate/20-VERIFICATION.md
@.planning/phases/20-pre-registration-the-three-condition-gate/20-SECURITY.md

<resolved_paths>
<!-- RESOLVED FROM THE REPO, not invented. Phase 20 has no published prose report — the only
     `phase20_*` artifact is `results/phase20_retention_floor.json`, which is JSON. Phase 19's
     precedent for exactly this situation was a STANDALONE continuation document, and that is what
     `append_addendum` is pointed at here. -->

append_addendum target : `results/phase20_gate_coverage_correction.md`
pending  (exactly one) : `**Phase 20 GATE-06 coverage correction: not yet recorded.**`
recorded               : `**Phase 20 GATE-06 coverage correction: recorded in the dated continuation at the end of this file.**`
addendum heading       : `## Addendum — 2026-08-20 — the coverage statistic, the held-out leg, and the retention floor's missing tripwire`

The pair mirrors `tests/test_phase19_correction.py:52-59`'s `CAL_PENDING` / `CAL_RECORDED` /
`CAL_ADDENDUM_HEADING` exactly. Plan `20-11` re-declares the same three strings as the committed
source of truth, in that file's own register.
</resolved_paths>

<writer_contract>
From `scripts/_addendum.py:56-100`, read before writing anything:
  - `append_addendum(path, addendum, *, pending, recorded)` — both keywords REQUIRED, no defaults,
    no override flag, no rewrite path.
  - `text.count(pending)` must be EXACTLY 1. Zero raises `SystemExit`; two or more raises
    `SystemExit`. So the pending line must appear once and NOWHERE else — no prose in the document
    may quote it, not even to explain the mechanism.
  - It `_prove`s `_verdict.recorded_verdict(updated) == _verdict.recorded_verdict(text)`.
    `recorded_verdict` returns `None` when there is no `^## Verdict` section, and `None == None`
    passes silently — so the document MUST carry a real `## Verdict` section followed by another
    `## ` heading, or that guard is vacuous.
  - The pending line must sit OUTSIDE the `## Verdict` section, or replacing it changes the verdict
    body and the guard fires.
  - D-24's own note: the writer refuses once the marker is already RECORDED. There is exactly ONE
    append to this file, ever.
</writer_contract>

<committed_vs_staged>
<!-- MEASURED in a scratch repo before this revision. This is why Task 1's guard is `git log` and
     not `git ls-files`. -->

For a file that is `git add`-ed and NEVER committed:

    git ls-files results/x.md          ->  prints the path      (a merely-staged file is TRACKED)
    git log --format=%H -- results/x.md ->  prints NOTHING       (no revision exists)

`git ls-files` therefore PASSES on exactly the state the two-commit rule forbids, and the cost lands
two waves later at `20-11`, whose additivity guard derives the pre-append revision from `git log` and
fails with "no committed revision carries the placeholder" — repairable only by rewriting history.
The guard must be asserted on the same object `20-11` will consume: `git log` for the revision, and
`git show <rev>:<path>` for the bytes.
</committed_vs_staged>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: The continuation document, carrying the pin's literal reading and exactly one pending line — committed BEFORE the append</name>

  <read_first>
    - `results/phase19_calibration_correction.md` — the whole file. This is the shape: title, the
      measured result in the question unit, `## Verdict` publishing what the CLOSED pin computes
      unedited, then a section carrying the pending line. Match its register.
    - `scripts/_verdict.py` — `VERDICT_SECTION` is anchored `^## Verdict\b(.*?)(?=^## |\Z)`. Confirm
      your `## Verdict` section is followed by another `## ` heading.
    - `scripts/_addendum.py:56-100` — the three checks that run on the PRODUCED BYTES.
    - `tests/test_phase19_correction.py:105-175` — the additivity test that will be written against
      this file in plan `20-11`. It walks `git log` for the newest committed revision still carrying
      the pending line. That is why this task ends in a commit.
  </read_first>

  <files>results/phase20_gate_coverage_correction.md</files>

  <action>
Create `results/phase20_gate_coverage_correction.md` and COMMIT IT carrying the pending line, before
any append. This is not a stylistic choice: `20-11`'s additivity guard derives the pre-append
revision from git history rather than pinning a hash, so if the document is created and appended in
one commit that revision never exists and the guard fails with "no committed revision carries the
placeholder". Two commits, in this order.

Title it for the defect, in phase 19's register: the coverage statistic, and the verdicts it moved.

**The measured result section.** Every number re-derived by running the code, never transcribed from
`20-REVIEW.md` or `20-VERIFICATION.md`. In the question unit, never a bare percentage (STAT-02):
n = 104 questions on every committed fixture, X = `0.04535522866494124`, `y_taught` = `0.35`,
`y_heldout` = `0.24499999999999997`. Publish a table of the swept points as
successes/questions with BOTH statistics beside each — the `wilson_upper_bound(k, 104)` the criterion
reads and the raw `k/104` the frozen block reads — for k in 1, 3 and 11, so the shift the defect
introduces is visible rather than asserted.

STAT-02 is enforced here as a LINE-SCOPED rule, not merely as "no zero percentages": any line
carrying a `%` figure must also carry an explicit `k/n` denominator on that same line. Write rates as
`3/104` and give the percentage, if at all, beside its denominator. The verify below reddens on a
percentage that appears with no denominator on its line.

**`## Verdict`.** Publish WHAT THE FROZEN PIN COMPUTES, unedited, in D-24's register — the literal
reading ships as written and any correction is added beside it rather than over it. Both reproduced
directions, each with its fixture NAMED, because the reports do not name them and one of them is
wrong about the outcome:
  - `FIXTURE_CLEARING_POINT` with `sweep_extraction_rates=(1/104, 3/104)` → `INCONCLUSIVE`, carrying
    a GATE-06 reason.
  - `FIXTURE_DESTROYED_MODEL` with `sweep_extraction_rates=(3/104, 11/104)` → `FAIL`, with NO
    GATE-06 reason. `FIXTURE_TRUNCATED_SWEEP` gives the same. **`FIXTURE_CLEARING_POINT` with the
    same sweep gives `PASS`**, not `FAIL` — measured across all three fixtures. Publish that third
    row: no report contains it, and it is the sharpest of the three, since the pin publishes a PASS
    off an extraction axis on which no point clears the criterion. State its limit honestly in the
    same breath — it does not contradict the verifier's narrower claim, which was scoped to
    self-consistent inputs where the judged point is itself one of the swept points.
  - The held-out leg has NO verdict to publish at all, because there is no `sweep_heldout_recalls`
    parameter in the 21-kwarg signature. Say that as the finding it is.
  - `retention_cap(retention_noise_floor=0.06893)` returns `4.029` with no refusal, while
    `extraction_ceiling` carries three provenance `_prove` calls. Publish both, publish the governing
    `3.9085032379884783` beside it, and publish that `4.029` is the LOOSER of the two caps.

Then a `## The coverage correction` section (a `## ` heading, so the verdict section is bounded)
whose entire body is the pending line, verbatim and once:
`**Phase 20 GATE-06 coverage correction: not yet recorded.**`

Nothing anywhere else in the document may contain that string — not in a code fence, not in a
sentence explaining the mechanism. Check with a count before committing.

Commit with a message naming the pending state, so the pre-append revision is findable by a human as
well as by the guard.

**Do not substitute `git ls-files` for the commit check in the verify block.** MEASURED: it prints
the path for a file that is staged and never committed, so it passes on precisely the state this task
forbids (see `<committed_vs_staged>`). The check asserts a REVISION exists via `git log` and that the
revision's BLOB carries the pending line via `git show` — the same two objects `20-11` consumes.
  </action>

  <verify>
    <automated>.venv/bin/python -c "
import pathlib,re,subprocess,sys
sys.path.insert(0,'scripts'); import _verdict
P='results/phase20_gate_coverage_correction.md'
p=pathlib.Path(P); t=p.read_text('utf-8')
PEND='**Phase 20 GATE-06 coverage correction: not yet recorded.**'
assert t.count(PEND)==1, t.count(PEND)
v=_verdict.recorded_verdict(t); assert v is not None and v.strip(), 'no ## Verdict section'
assert PEND not in v, 'pending line is inside the Verdict section'
for s in ('FIXTURE_CLEARING_POINT','FIXTURE_DESTROYED_MODEL','FIXTURE_TRUNCATED_SWEEP','0.04535522866494124','4.029','3.9085032379884783'):
    assert s in t, s
bare=[ln for ln in t.splitlines() if re.search(r'(?<![\d.])\d+(\.\d+)?%', ln) and not re.search(r'\d+\s*/\s*\d+', ln)]
assert not bare, ('STAT-02: percentage with no k/n denominator on its line', bare)
def g(*a): return subprocess.run(['git',*a],capture_output=True,text=True).stdout
revs=g('log','--format=%H','--',P).split()
assert revs, 'NOT COMMITTED — git ls-files passes on a merely-staged file (MEASURED); the pre-append REVISION must exist'
assert PEND in g('show', revs[-1]+':'+P), 'the oldest committed blob does not carry the pending line'
print('OK — pending x1 in the committed blob, verdict section present,', len(revs), 'revision(s)')"</automated>
  </verify>

  <acceptance_criteria>
    - `results/phase20_gate_coverage_correction.md` has at least one REVISION in git history —
      `git log --format=%H -- <path>` is non-empty — and the OLDEST revision's blob, retrieved with
      `git show <rev>:<path>`, contains the pending line. `git ls-files` is explicitly NOT the check:
      it returns the path for a staged-but-never-committed file (MEASURED), which is the exact state
      the two-commit rule forbids.
    - The pending string occurs EXACTLY ONCE in the working file (`text.count(pending) == 1`).
    - `_verdict.recorded_verdict(text)` returns a non-empty body, and the pending line is NOT inside
      it.
    - The `## Verdict` section names all three fixtures by identifier and publishes the
      `FIXTURE_CLEARING_POINT` + `(3/104, 11/104)` → `PASS` row that no prior report contains.
    - The file contains `0.04535522866494124`, `4.029` and `3.9085032379884783`.
    - STAT-02: no line carries a `%` figure without an explicit `k/n` denominator on that same line.
      This is broader than the previous zero-percentage-only check, which forbade `0%` and permitted
      every other bare percentage.
    - `scripts/mitigation_gate.py` unchanged: `git diff --exit-code -- scripts/mitigation_gate.py`
      exits 0.
  </acceptance_criteria>

  <done>
    The pre-append revision exists in git history carrying exactly one placeholder, proved by reading
    the committed blob rather than the index, and the pin's literal reading is published unedited
    above it.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: The machine-readable half, and the one append</name>

  <read_first>
    - `results/phase19_calibration_correction.json` — the whole file. The key register to mirror:
      `governs`, `defects` (keyed by defect ID, each value a measured sentence), `evidence`, `proof`,
      `continuation`, and the `pin_internal_*` / `corrected_*` pairing that names both numbers.
    - `results/phase19_calibration_correction.md:55-158` — the appended addendum. Note that it names
      BOTH values for every corrected quantity and states which governs in its own section.
    - `scripts/phase20_gate_coverage.py` — the module written in plan `20-08`. Every published number
      must be produced by calling it or by calling `mitigation_gate`, never typed.
    - `results/phase20_retention_floor.json` — `retention_ppl_noise_floor`, `cap`, `borrowed_cap`,
      `borrowed_floor_ratio`. Read them from the file.
  </read_first>

  <files>results/phase20_gate_coverage_correction.json, results/phase20_gate_coverage_correction.md</files>

  <action>
Write `results/phase20_gate_coverage_correction.json`, then run `append_addendum` ONCE against the
`.md`, then commit both.

**The JSON.** Generate it with a throwaway script that IMPORTS `phase20_gate_coverage`,
`mitigation_gate` and `erasure_gate` and computes every number by calling them. Do not hand-type a
single float. Keys, mirroring the Phase 19 artifact and extending it where this correction differs:

  - `continuation` — `"results/phase20_gate_coverage_correction.md"`.
  - `proof` — `"tests/test_phase20_correction.py"` (written in plan `20-11`).
  - `governs` — `"sweep coverage (GATE-06)"`, plus a sentence saying the governing computation is
    `coverage_verdict` and the governing route to a verdict is `corrected_point_verdict`.
  - `supersedes` — `"scripts/mitigation_gate.py:798-812"`.
  - `governing_module` / `governing_entry_point` — `"scripts/phase20_gate_coverage.py"` and
    `"corrected_point_verdict"`.
  - `coverage_statistic_by_axis` — serialised from the module's `COVERAGE_STATISTIC_BY_AXIS`, not
    retyped.
  - `bound_direction` — the resolved sub-decision (D-37), in words: criterion-matching per axis;
    Wilson UPPER on the X ceiling because condition (a) at `:755-756` is decided on it; the RAW rate
    on both Y floors because condition (b) at `:767-768` is decided on it; a Wilson LOWER bound on a
    floor would decide coverage on a statistic the criterion does not read, which is CR-01's defect
    class with the sign flipped and inverts the conservatism of a floor. Name the COST explicitly:
    the Y legs inherit condition (b)'s own lack of a confidence bound, and correcting that would mean
    moving a pre-registered threshold after seeing the data it governs, so it is recorded and NOT
    fixed. Record that `wilson_lower_bound` exists in the module for REPORTING only, in
    `rule_of_three`'s alongside-never-instead register, and include the computed lower bounds for
    the published Y points so the reader meets both numbers. Record its `successes == 0`
    short-circuit and the one-line reason (`centre == spread` identically at `p = 0`, so the analytic
    lower bound is exactly `0`; the unrounded mirror leaves a positive residue — measured
    `1.734723475976807e-18` at `n = 104`), so the artifact carries the divergence rather than leaving
    a reader to rediscover it.
  - `defects` — keyed by ID: `CR-01`, `WR-09`, `T-20-19`, and `WR-08`. Each value one measured
    sentence naming the file and line range. All four are asserted present by the verify.
  - `evidence` — `n` 104, `X`, `y_taught`, `y_heldout`, then `direction_i` and `direction_ii`
    sub-objects each carrying `fixture`, `successes`, `questions`, `wilson_upper`, `raw_rates`,
    `pin_verdict` and `corrected_verdict`; plus a `direction_ii_on_clearing_fixture` sub-object for
    the third unreported case. `direction_i` is `FIXTURE_CLEARING_POINT`, pin `INCONCLUSIVE`,
    corrected `PASS`. `direction_ii` is `FIXTURE_DESTROYED_MODEL`, pin `FAIL`, corrected
    `INCONCLUSIVE`. The third is `FIXTURE_CLEARING_POINT`, pin `PASS`, corrected `INCONCLUSIVE`.
  - `heldout_coverage` — the WR-09 demonstration: `sweep_heldout_recalls` `(0.30, 0.28)` against
    `y_heldout`, both at or above it, no point below, so the axis is truncated and undetectable by
    the frozen block which has no such parameter.
  - `retention_provenance` — `borrowed_floor` `0.06893`, `borrowed_cap` `4.029`, `measured_floor`
    and `governing_cap` read from `results/phase20_retention_floor.json`, `ratio`
    `7.939763314393305`, and `enforced_by` naming `_prove_retention_floor` and its four checks. This
    object is T-20-19's entire evidentiary record — the measured reproduction, the two caps side by
    side, and the function that now refuses the borrowed one — so it is the field a later reader
    lands on when they ask what was actually wrong with the retention leg.
  - `recorded_not_corrected` — the findings this continuation publishes without fixing, each with
    its reason: `IN-09` (`MITIGATION_GOAL_FRAMING` at `:320-329` has no reader — module data,
    harmless, frozen), `IN-06` (the `__main__` self-check uses bare `assert`, strippable under `-O`;
    already mitigated by the pytest twin that re-asserts all six outcomes), and `WR-03` / `WR-04` /
    `WR-05` / `WR-10` (defects in the UNPINNED `scripts/phase20_run.py`, normal edits, out of this
    gap-closure's scope and not blocking any threat). A description that outlives its defect is worse
    than no description — each entry says what would make it stale.

Sort keys and write with a trailing newline, matching the Phase 19 artifact's formatting.

**The append.** Compose the addendum text with the heading given in `<resolved_paths>` and call
`append_addendum(path, addendum, pending=<pending>, recorded=<recorded>)` — both keywords, exactly
as `scripts/_addendum.py:56` requires. Run it ONCE; the writer refuses a second append because the
marker is then RECORDED.

The addendum body must, mirroring the Phase 19 continuation's structure:
  - Open by stating the file is frozen, that this is a dated continuation and not an edit, and that
    `scripts/mitigation_gate.py` is byte-identical after it.
  - Carry a two-column table per reproduced direction: the verdict the frozen pin computes against
    the verdict the correction computes, with the fixture named.
  - Carry the bound-direction resolution in prose with its cost.
  - Carry the retention leg in its own paragraph: the measured `retention_cap(0.06893) = 4.029`
    against the governing `3.9085032379884783`, the `7.939763314393305x` ratio, and
    `_prove_retention_floor` as the choke point that now refuses the borrowed value by identity.
  - Have a final section titled so it cannot be missed — which computation governs a v4.0 verdict —
    stating that from here a v4.0 point verdict is read through
    `phase20_gate_coverage.corrected_point_verdict` and NOT through
    `mitigation_gate.mitigation_point_verdict` directly, that the raw-rate parameter does not exist
    on the sanctioned route, and that `tests/test_phase20_correction.py` goes RED if a later plan
    consumes the superseded path. Name `results/phase20_gate_coverage_correction.json`'s `governs`
    field as the machine-readable half of the same statement.

Same STAT-02 discipline as Task 1: no `%` figure on a line without its `k/n` denominator.

Commit both files together, in a second commit distinct from Task 1's. The verify asserts the file
has at least TWO revisions and that the OLDEST one's blob still carries the pending line — the
property `20-11`'s additivity guard consumes, asserted here where it is cheap to fix rather than two
waves later where it is not.
  </action>

  <verify>
    <automated>.venv/bin/python -c "
import json,pathlib,re,subprocess,sys
sys.path.insert(0,'scripts'); import _verdict
P='results/phase20_gate_coverage_correction.md'
j=json.loads(pathlib.Path('results/phase20_gate_coverage_correction.json').read_text('utf-8'))
for k in ('continuation','proof','governs','supersedes','governing_module','governing_entry_point','bound_direction','defects','evidence','retention_provenance','heldout_coverage','recorded_not_corrected','coverage_statistic_by_axis'):
    assert k in j, k
assert j['supersedes']=='scripts/mitigation_gate.py:798-812'
assert set(j['defects'])>={'CR-01','WR-09','T-20-19','WR-08'}, sorted(j['defects'])
rp=j['retention_provenance']
for k in ('borrowed_floor','borrowed_cap','measured_floor','governing_cap','ratio','enforced_by'):
    assert k in rp, k
assert rp['governing_cap'] < rp['borrowed_cap'], rp
assert '_prove_retention_floor' in rp['enforced_by']
e=j['evidence']
assert e['direction_i']['fixture']=='FIXTURE_CLEARING_POINT' and e['direction_i']['pin_verdict']=='INCONCLUSIVE' and e['direction_i']['corrected_verdict']=='PASS'
assert e['direction_ii']['fixture']=='FIXTURE_DESTROYED_MODEL' and e['direction_ii']['pin_verdict']=='FAIL' and e['direction_ii']['corrected_verdict']=='INCONCLUSIVE'
assert e['direction_ii_on_clearing_fixture']['pin_verdict']=='PASS' and e['direction_ii_on_clearing_fixture']['corrected_verdict']=='INCONCLUSIVE'
PEND='**Phase 20 GATE-06 coverage correction: not yet recorded.**'
REC='**Phase 20 GATE-06 coverage correction: recorded in the dated continuation at the end of this file.**'
t=pathlib.Path(P).read_text('utf-8')
assert t.count(PEND)==0
assert t.count(REC)==1
assert 'Addendum — 2026-08-20' in t
bare=[ln for ln in t.splitlines() if re.search(r'(?<![\d.])\d+(\.\d+)?%', ln) and not re.search(r'\d+\s*/\s*\d+', ln)]
assert not bare, ('STAT-02: percentage with no k/n denominator on its line', bare)
assert 'governs' in t.lower()
assert '_prove_retention_floor' in t
assert _verdict.recorded_verdict(t) is not None, 'the Verdict section must survive the append'
def g(*a): return subprocess.run(['git',*a],capture_output=True,text=True).stdout
revs=g('log','--format=%H','--',P).split()
assert len(revs)>=2, ('the append must land in a SECOND commit; revisions found: %d' % len(revs))
assert PEND in g('show', revs[-1]+':'+P), 'the oldest revision must still carry the pending line for 20-11 to find'
print('OK —', len(revs), 'revisions, oldest carries the placeholder')"</automated>
  </verify>

  <acceptance_criteria>
    - `results/phase20_gate_coverage_correction.json` parses and carries every key listed above;
      `supersedes` is exactly `"scripts/mitigation_gate.py:798-812"`; `defects` carries all FOUR of
      `CR-01`, `WR-09`, `T-20-19` and `WR-08`.
    - `evidence.direction_i` = `FIXTURE_CLEARING_POINT`, pin `INCONCLUSIVE`, corrected `PASS`.
      `evidence.direction_ii` = `FIXTURE_DESTROYED_MODEL`, pin `FAIL`, corrected `INCONCLUSIVE`.
      `evidence.direction_ii_on_clearing_fixture` = pin `PASS`, corrected `INCONCLUSIVE`.
    - `retention_provenance` carries `borrowed_floor`, `borrowed_cap`, `measured_floor`,
      `governing_cap`, `ratio` and `enforced_by`, with `governing_cap < borrowed_cap` asserted rather
      than narrated, and `enforced_by` naming `_prove_retention_floor`.
    - Every float in the JSON is reproducible by calling `phase20_gate_coverage` / `mitigation_gate`
      / `erasure_gate` — `20-11`'s tripwire re-derives them and goes red on a hand-edited value.
    - The `.md` now contains the RECORDED line exactly once and the PENDING line zero times, the
      addendum heading is present, the addendum carries the retention paragraph, and
      `_verdict.recorded_verdict` still returns a section after the append.
    - `git log --format=%H -- results/phase20_gate_coverage_correction.md` returns at least TWO
      commits, and `git show <oldest>:<path>` still contains the pending line. Asserted in the verify
      block, not left to the acceptance prose.
    - STAT-02: no line in the `.md` carries a `%` figure without a `k/n` denominator on that line.
    - `git diff --exit-code -- scripts/mitigation_gate.py scripts/erasure_gate.py` exits 0.
    - `.venv/bin/python -m pytest tests/test_phase20_prereg.py -q` still reports `18 passed` — the
      new `results/phase20_*` file is matched by `V4_ARTIFACT_GLOBS` and its first-add lands after
      all nine pin commits, so the ancestry guard stays green. Assert this rather than assume it.
  </acceptance_criteria>

  <done>
    The correction is machine-readable, names what it governs and what it supersedes, and sits beside
    an unedited published reading in an append that git history can prove was additive — with the
    two-revision property asserted here rather than discovered at `20-11`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| published text ↔ an addendum to it | An append that rewrites is a correction laundered as a continuation. |
| a hand-typed number in an artifact ↔ the code that produces it | A second copy is free to disagree, and the artifact is the copy a reader trusts. |
| the git index ↔ the git object DAG | `git ls-files` reads the index and passes on a staged file with no revision; only `git log` + `git show` prove a committed blob. |
| the git object DAG ↔ the evidentiary record | The artifact's first-add must land after all nine pin commits or the ancestry guard reddens. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-21 | Repudiation | GATE-06 mislabeling | mitigate | CARRIED FORWARD, open. This plan publishes the machine-readable correction and the dated continuation; closure requires `20-11`'s tripwire. |
| T-20-19 | Spoofing | the unguarded retention leg | mitigate | CARRIED FORWARD, open. This plan publishes T-20-19's evidentiary record: the `retention_provenance` block carries `borrowed_floor` `0.06893`, `borrowed_cap` `4.029`, the measured `measured_floor` / `governing_cap` read from `results/phase20_retention_floor.json`, the `7.939763314393305` ratio, and `enforced_by` naming `_prove_retention_floor` and its four checks — and the addendum states the same in prose. The verify asserts `governing_cap < borrowed_cap`, so the "the borrowed one is LOOSER" claim is checked rather than narrated. Publication is not closure: `20-12`'s register aggregates this block, and the threat closes at `20-11` where the refusals are watched firing. |
| T-20-49 | Tampering | `append_addendum` altering published text | mitigate | The writer's three checks run on the PRODUCED BYTES: exactly-one placeholder, an unchanged `## Verdict` section, and an original-prefix-plus-addendum check. The `## Verdict` section is asserted non-`None` on BOTH sides here — before the append in Task 1 and after it in Task 2 — because `recorded_verdict` returning `None` on both sides would make that guard vacuous. `20-11` proves additivity against the pre-append revision derived from git history. |
| T-20-47 | Repudiation | the correction artifact is itself post-hoc | mitigate | It was written after the failures were reproduced, so it could in principle be shaped to license a preferred verdict. Refuted in the artifact itself: both the pin's verdict and the corrected verdict are published for every case, and the correction only ever moves a verdict TOWARD `INCONCLUSIVE` or restores a genuinely bracketed reading — never toward `PASS` from a truncated axis. The `direction_ii_on_clearing_fixture` row demotes a `PASS`, which is against the amender's interest and is published anyway. |
| T-20-51 | Tampering | a hand-typed float drifting from what the code computes | mitigate | The JSON is generated by a script that imports the three modules and calls them; no float is typed. `20-11` re-derives every published number and goes red on a hand edit — the discipline `tests/test_phase19_correction.py:61-63` states in its own words. |
| T-20-57 | Repudiation | a defect description outliving its defect | mitigate | `recorded_not_corrected` entries each state what would make them stale, and `20-11` asserts the live defects are still live against the CODE rather than against the prose describing them. A description that outlives its defect teaches a reader to distrust an artifact that is now correct. |
| T-20-58 | Tampering | the new `results/phase20_*` file reddening the ancestry guard | accept | It cannot: the first add lands after all nine pin commits `95b3c8a`..`abf9072`. Accepted rather than mitigated because the guard is the mitigation — asserted green by running `tests/test_phase20_prereg.py` as an acceptance criterion rather than reasoned about. |
| T-20-64 | Repudiation | a two-commit rule enforced by a check that passes on one commit | mitigate | MEASURED: `git ls-files` prints the path for a staged-but-never-committed file while `git log` prints nothing, so the original `ls-files` guard passed on exactly the state its own message forbade, and the cost would have landed at `20-11` as an unrepairable-without-history-rewrite failure. Both tasks now assert on the object `20-11` consumes: `git log --format=%H -- <path>` for the revision list, `git show <oldest>:<path>` for the blob. Task 1 requires ≥ 1 revision carrying the placeholder; Task 2 requires ≥ 2 revisions with the oldest still carrying it. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_phase20_prereg.py -q` → `18 passed`.
- `git log --format=%H -- results/phase20_gate_coverage_correction.md` → ≥ 2 commits, oldest
  carrying the pending line (asserted in Task 2's verify, not assumed).
- `git diff --exit-code -- scripts/mitigation_gate.py scripts/erasure_gate.py` → exit 0.
- `ruff check .` / `ruff format --check .` → clean.
</verification>

<success_criteria>
- Both artifacts committed, in two commits, with the append provably additive and the two-revision
  property asserted by the plan's own verify.
- `governs` and `supersedes` name the computation and the exact frozen block.
- Both reproduced directions published with their fixtures named and both verdicts stated.
- T-20-19's measured reproduction published as a structured `retention_provenance` block naming its
  enforcement, not as a sentence.
</success_criteria>

<output>
Create `.planning/phases/20-pre-registration-the-three-condition-gate/20-10-SUMMARY.md` when done.
</output>
