---
phase: 20-pre-registration-the-three-condition-gate
plan: 13
type: execute
wave: 12
depends_on: []
files_modified:
  - .planning/phases/20-pre-registration-the-three-condition-gate/20-CONTEXT.md
  - .planning/phases/20-pre-registration-the-three-condition-gate/20-SECURITY.md
autonomous: true
gap_closure: true
requirements: [GATE-02, GATE-06]
user_setup: []

must_haves:
  truths:
    - "`grep -c 'D-38' 20-CONTEXT.md` is at least 1 and the same file contains D-39, D-40 and D-41, each under a dated heading naming 2026-08-21 and the verification that forced it"
    - "20-SECURITY.md frontmatter reads `status: blocked` and `threats_open: 1`, and the `### Open` section names T-20-19"
    - "The T-20-19 row's pre-existing text — the `*What was wrong, preserved:*` and `*The closure:*` spans written at 20-12 — is byte-identical to `git show HEAD:...20-SECURITY.md`; the re-opening is an APPENDED dated sentence inside the same cell, never a rewrite"
    - "T-20-21's row is byte-identical to `git show HEAD:...20-SECURITY.md`"
    - "`git diff --exit-code -- scripts/ tests/ results/` returns 0 — this plan writes no code, no test and no results artifact"
  artifacts:
    - path: ".planning/phases/20-pre-registration-the-three-condition-gate/20-CONTEXT.md"
      provides: "D-38, D-39, D-40, D-41 under a second dated gap-closure heading"
      contains: "D-41"
    - path: ".planning/phases/20-pre-registration-the-three-condition-gate/20-SECURITY.md"
      provides: "T-20-19 carried as OPEN with a dated re-opening note"
      contains: "threats_open: 1"
  key_links:
    - from: ".planning/phases/20-pre-registration-the-three-condition-gate/20-CONTEXT.md"
      to: "20-14/20-15/20-16/20-17"
      via: "wave ordering — every later plan in this wave-set cites D-38..D-41, and this plan is wave 12"
      pattern: "D-3[89]|D-4[01]"
    - from: "20-SECURITY.md frontmatter"
      to: "20-SECURITY.md `### Open` section and the T-20-19 row Status cell"
      via: "threats_open must equal the number of rows carrying Status `open`"
      pattern: "threats_open: 1"
---

<objective>
Record the three LOCKED user decisions (D-38, D-39, D-40) and the one decision a measurement
forced (D-41) in `20-CONTEXT.md`, and flip `T-20-19` back to OPEN in `20-SECURITY.md` — both
BEFORE any code or artifact in this wave-set cites them.

Purpose: this phase's own trust boundary is *"a plan that says a thing will be done ↔ a guard
that proves it was"* (`20-SECURITY.md:39`). Two consequences, and this plan discharges both.
First, plan `20-09` established the shape: a decision ID must exist in `20-CONTEXT.md` before a
shipped artifact names it, enforced by the wave graph rather than by prose. Second, per D-39 the
register must state the REAL state while the correction is pending — `20-SECURITY.md` currently
publishes `threats_open: 0` over a guard `20-VERIFICATION.md` defeated twice, by measurement.
Flipping it open is the first act, not the last.

Output: `20-CONTEXT.md` carrying D-38..D-41 under a second dated gap-closure heading;
`20-SECURITY.md` at `status: blocked` / `threats_open: 1` with T-20-19's historical text preserved
byte-identically and a dated re-opening appended beside it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/20-pre-registration-the-three-condition-gate/20-VERIFICATION.md
@.planning/phases/20-pre-registration-the-three-condition-gate/20-REVIEW-GAP-CLOSURE.md

<interfaces>
<!-- Resolved from the committed files. Use these names verbatim; do not invent siblings. -->

`20-CONTEXT.md` — the existing dated gap-closure block, which this plan's block sits BENEATH:
  line 312: `### Resolved during gap closure — decisions forced by 20-VERIFICATION.md`
  line 314-318: the italic dating paragraph (`*Added 2026-08-20, after ... Each is LOCKED on the
  same terms as D-01…D-33.*`)
  lines 320, 330, 336, 345: `- **D-34 — ...**`, `- **D-35 — ...**`, `- **D-36 — ...**`,
  `- **D-37 — ...**`
  line 356: `### Claude's Discretion` — the NEW block goes ABOVE this heading and BELOW D-37.

`20-SECURITY.md` — every span this plan touches, by line:
  :4  `status: verified`
  :5  `threats_open: 0`
  :18 `**Gate status: CLOSED.** `threats_open: 0`. The three-item dated continuation landed ...`
  :46 `66 threats. **66 closed, 0 open.**`
  :57 `### Open`
  :59 `None. `T-20-21` and `T-20-19` — the two threats this file carried as open — are closed below,`
  :90 the `| T-20-21 |` row — DO NOT TOUCH
  :91 the `| T-20-19 |` row — Status cell currently `**closed**`
  the `## Security Audit Trail` table near the end
  the `## Sign-Off` checklist near the end

Threat IDs already allocated in this file: `T-20-01` … `T-20-66`. New IDs in this wave-set start
at `T-20-67`. Accepted-risk IDs already allocated: `R-20-01` … `R-20-07`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record D-38, D-39, D-40 and D-41 in 20-CONTEXT.md under a second dated gap-closure heading</name>

  <read_first>
    - `.planning/phases/20-pre-registration-the-three-condition-gate/20-CONTEXT.md` lines 300-360 —
      the existing `### Resolved during gap closure` block. Match its shape exactly: an italic
      dating paragraph, then `- **D-NN — <one-sentence claim in bold>.**` followed by the argument.
    - `.planning/phases/20-pre-registration-the-three-condition-gate/20-VERIFICATION.md` — the
      frontmatter `gaps:` block and the `## Human Decision Requested` section (:338-368). D-38,
      D-39 and D-40 answer escalations 1, 3 and 2 in that order.
    - `.planning/phases/20-pre-registration-the-three-condition-gate/20-REVIEW-GAP-CLOSURE.md`
      GC-01 (:54-90), GC-02 (:94-118), GC-03 (:122-164), GC-04 (:168-198), GC-06 (:244-277).
  </read_first>

  <action>
Insert one new `###` block into `20-CONTEXT.md`, positioned strictly BELOW D-37 (which ends at
line 354) and strictly ABOVE `### Claude's Discretion` (line 356). Do not edit, reflow or renumber
a single character of D-01…D-37.

Heading: `### Resolved during gap-closure wave 2 — decisions forced by the 2026-08-21
re-verification`. Beneath it an italic dating paragraph in the register of the existing one at
:314-318: added 2026-08-21, after `20-VERIFICATION.md` returned `gaps_found` at 5/6 must-haves and
reproduced both holes in its own process; recorded at plan `20-13`, which every later plan in this
wave-set declares `depends_on`, so no shipped artifact cites one of these IDs before its record
exists; each LOCKED on the same terms as D-01…D-37.

Then the four decisions, verbatim IDs `D-38`, `D-39`, `D-40`, `D-41`:

**D-38 — the retention floor is refused by PROPERTY as well as by NAME.** State the resolution:
`_prove_retention_floor` keeps its existing `retention_noise_floor != V20_RETENTION_NOISE_FLOOR`
refusal at `scripts/phase20_gate_coverage.py:396-406` AND gains a magnitude bound
`retention_noise_floor <= _ADAPTER_REGIME_RETENTION_FLOOR * (1.0 + 1e-9)` placed AFTER it. The two
together: the one named value refused by identity, and the entire looser class refused by
magnitude — of which `V20_RETENTION_NOISE_FLOOR` is one member rather than the definition. State
the measured evidence that forces it: `0.06893 * (1 + 2**-50)` passes the `!=` and buys a
BIT-IDENTICAL `4.029`, the exact borrowed cap; and `retention_noise_floor=5.0` with clean
`{"regime": "adapter", "seeds": (1337, 2024)}` provenance needs no malformed input at all and
reaches a `PASS` at cap `13.89114` against the governing `3.9085032379884783`. State why this also
answers escalation 1 BY ELIMINATION: a property rule authored after the measurement cannot be
tuned toward a favourable answer, unlike an identity rule written against a single already-known
value, so the post-hoc-versus-dated-continuation question does not arise for it.

**D-39 — the security register is flipped OPEN first and re-closed only on watched-RED evidence.**
State the ordering as a requirement on the plan graph, not as an intention: the flip to
`threats_open: 1` is this plan (wave 12); the re-close to `threats_open: 0` is plan `20-17`
(wave 16) and is gated on the magnitude bound's tripwires having been OBSERVED red-then-green
against BOTH measured cases — `0.06893 * (1 + 2**-50)` and `5.0`. They must not be the same commit.
State the reason: `20-SECURITY.md` currently publishes `status: verified` over a guard the
re-verification defeated twice; a register that publishes a closure it cannot substantiate is the
exact failure its own trust boundary at `:39` names.

**D-40 — both gaps close in this pass; the Y hole is NOT deferred to Phase 23.** This is the dated
decision `20-VERIFICATION.md`'s human-verification item 2 (:80-82) asks for. State what closes:
a per-element `[0.0, 1.0]` range `_prove` on BOTH Y legs (subsuming NaN with no special-case
check, because `0.0 <= nan <= 1.0` is `False`), the measured route-level differential as a
tripwire, the `isinstance(k, int) and not isinstance(k, bool)` count guard, and the D-38 magnitude
bound. State the reachability finding honestly: no committed caller reaches
`corrected_point_verdict` today, so the hole does not block Phase 21 — and it closes now anyway,
because Phase 23 is where sweep width is set and coverage stops being hypothetical, and a rule
closed before its first consumer is the whole point of a pre-registration.

**D-41 — the sanctioned route's TEST HARNESS supplies the governing retention floor; the bound's
tolerance is never widened to admit a fixture.** This decision is forced by a measurement neither
the code review nor the verification recorded, and it must be stated as a measurement:
`mitigation_gate`'s three committed fixtures all carry `retention_noise_floor = 0.009`, and
`0.009 > 0.008681618994239138 * (1.0 + 1e-9) = 0.008681619002920757`, so D-38's bound REFUSES the
repo's own fixtures — the fixture floor is `1.0366729991228745x` looser than the measured
adapter-regime floor and buys cap `3.90914` against the governing `3.9085032379884783`. Two routes
were available and one is rejected in writing: widening the tolerance until `0.009` passes is
rejected outright, because tuning a bound's tolerance to admit a value already in hand is exactly
the researcher degree of freedom D-38 exists to remove. The taken route is that
`tests/test_phase20_correction.py::_corrected_call` supplies the governing floor READ from
`results/phase20_retention_floor.json::retention_ppl_noise_floor`, in the same `base` dict that
already supplies the two arguments the frozen fixtures cannot (`sweep_heldout_recalls`,
`retention_floor_provenance`). Record the against-interest half: every published verdict is
BIT-UNCHANGED under the substitution — `direction_i` PASS, `direction_ii` INCONCLUSIVE,
`direction_ii_on_clearing_fixture` INCONCLUSIVE, `heldout_coverage` INCONCLUSIVE, all four measured
at both floors — because `mitigation_gate.py` is frozen and its fixtures cannot be edited, so the
bound's FIRST catch is a 3.7% loosening inside this repository that no name-based refusal could
ever have seen.

Every number above must be RE-DERIVED by the executor by calling the modules before it is written,
never transcribed from this plan.
  </action>

  <verify>
    <automated>.venv/bin/python -c "import pathlib,re; t=pathlib.Path('.planning/phases/20-pre-registration-the-three-condition-gate/20-CONTEXT.md').read_text(); ids=[d for d in ('D-38','D-39','D-40','D-41') if f'**{d} —' in t]; assert ids==['D-38','D-39','D-40','D-41'], ids; assert t.index('D-37 —') < t.index('D-38 —') < t.index(\"### Claude's Discretion\"), 'block is out of position'; print('ok')"</automated>
  </verify>

  <acceptance_criteria>
    - `.venv/bin/python -c "import pathlib; t=pathlib.Path('.planning/phases/20-pre-registration-the-three-condition-gate/20-CONTEXT.md').read_text(); assert all(f'**{d} —' in t for d in ('D-38','D-39','D-40','D-41'))"` exits 0.
    - `git show HEAD:.planning/phases/20-pre-registration-the-three-condition-gate/20-CONTEXT.md | head -355 | diff - <(head -355 .planning/phases/20-pre-registration-the-three-condition-gate/20-CONTEXT.md)` prints nothing — D-01…D-37 are byte-identical and the insert is below them.
    - `git diff --numstat HEAD -- .planning/phases/20-pre-registration-the-three-condition-gate/20-CONTEXT.md` reports 0 deletions.
    - The strings `0.008681619002920757`, `1.0366729991228745`, `13.89114` and `3.90914` each appear in the D-41 text and each was produced by running Python against the committed modules in this task, not copied from this plan.
    - `git diff --exit-code -- scripts/ tests/ results/` returns 0.
  </acceptance_criteria>

  <done>`20-CONTEXT.md` carries D-38…D-41 beneath D-37 and above `### Claude's Discretion`, with zero deletions against HEAD and every published number re-derived.</done>
</task>

<task type="auto">
  <name>Task 2: Flip T-20-19 back to OPEN in 20-SECURITY.md, additively and in a separate commit</name>

  <read_first>
    - `.planning/phases/20-pre-registration-the-three-condition-gate/20-SECURITY.md` lines 1-95 (the
      frontmatter, the `Gate status` paragraph at :18, the register header at :46, the `### Open`
      section at :57-61, and both formerly-open rows at :90-91) and the final `## Security Audit
      Trail` and `## Sign-Off` sections.
    - `.planning/phases/20-pre-registration-the-three-condition-gate/20-VERIFICATION.md` gap 2
      (frontmatter lines 45-74) — the two reproductions that defeat T-20-19's declared guard.
  </read_first>

  <action>
Commit this task SEPARATELY from Task 1 so the OPEN flip is its own commit in the record.

Flip the register to its honest state, touching only the spans below and only additively where
historical text is involved:

1. Frontmatter `:4` `status: verified` → `status: blocked`. `:5` `threats_open: 0` →
   `threats_open: 1`. These two move TOGETHER, per D-39.
2. `:18` — correct the `**Gate status: CLOSED.**` paragraph to `**Gate status: BLOCKED.**` and
   state, in one sentence, that T-20-19 is carried open again pending the D-38 magnitude bound, and
   name `20-VERIFICATION.md` gap 2 as the source. Leave the rest of that paragraph's account of the
   `20-08`/`20-10`/`20-11` continuation intact.
3. `:46` `66 threats. **66 closed, 0 open.**` → `**65 closed, 1 open.**` with the total unchanged
   at 66. The total must continue to reconcile to this file's own rows.
4. `### Open` at `:57` — replace the `None. ...` sentence at `:59` with a T-20-19 entry in the
   Threat-Register table shape used at `:88-91` (`Threat ID | Category | Component | Disposition |
   Mitigation | Status`), Status `**open**`. Its Mitigation cell states what is OPEN in the present
   tense: the declared guard refuses one bit pattern and one caller-asserted string, `_prove_retention_floor`
   at `scripts/phase20_gate_coverage.py:353-406` never constrains the floor's magnitude, and both
   reproductions are named with their numbers — `0.06893 * (1 + 2**-50)` reaching a BIT-IDENTICAL
   `4.029`, and `retention_noise_floor=5.0` under clean adapter provenance reaching `PASS` at cap
   `13.89114` against the governing `3.9085032379884783`. Name the closing condition explicitly:
   the D-38 bound landing at plan `20-15` AND its tripwires observed red-then-green against both
   cases, re-closed at plan `20-17`. Also state the T-20-21 status as still closed so the section
   is not read as re-opening both.
5. `:91` — the existing `| T-20-19 |` row: leave every existing character in place, including the
   `*What was wrong, preserved:*` and `*The closure:*` spans. APPEND to the end of its Mitigation
   cell a dated sentence beginning `**RE-OPENED 2026-08-21 at plan `20-13` (D-39).**` naming the
   two reproductions and pointing at the `### Open` row. Change ONLY the Status cell from
   `**closed**` to `**open**`. This is the same additive discipline D-36 and the ROADMAP SC3
   amendment used: the historical claim stays readable, and what was wrong with it is recorded
   beside it rather than over it.
6. `## Security Audit Trail` — append one row: `2026-08-21 | 66 | 65 | 1 | /gsd:plan-phase 20
   --gaps (wave 2) — plan 20-13, T-20-19 re-opened against 20-VERIFICATION.md gap 2`. Do not edit
   the two existing rows.
7. `## Sign-Off` — uncheck the two boxes that are now false (`threats_open: 0 confirmed` and
   `status: verified set in frontmatter`) and append one line stating the approval is WITHDRAWN
   pending plan `20-17`, dated 2026-08-21. Leave the `20-12` approval paragraph beneath it
   byte-identical; it is the record of what was approved then.

Do not touch `T-20-21`'s row (`:90`) in any way. Do not touch any of the 20 gap-closure rows
`T-20-47` … `T-20-66`. Do not renumber anything.
  </action>

  <verify>
    <automated>.venv/bin/python -c "
import pathlib,subprocess
p='.planning/phases/20-pre-registration-the-three-condition-gate/20-SECURITY.md'
t=pathlib.Path(p).read_text()
assert 'status: blocked' in t.split('---')[1] and 'threats_open: 1' in t.split('---')[1], 'frontmatter not flipped'
assert '66 closed, 0 open' not in t and '65 closed, 1 open' in t, 'register header not reconciled'
old=subprocess.run(['git','show','HEAD:'+p],capture_output=True,text=True).stdout
r21=[l for l in old.splitlines() if l.startswith('| T-20-21 |')][0]
assert r21 in t, 'T-20-21 row was modified'
assert 'RE-OPENED 2026-08-21' in t
print('ok')"</automated>
  </verify>

  <acceptance_criteria>
    - The verify command above exits 0.
    - `git log --format=%H -- .planning/phases/20-pre-registration-the-three-condition-gate/20-SECURITY.md | head -1` is NOT the same commit as `git log --format=%H -- .planning/phases/20-pre-registration-the-three-condition-gate/20-CONTEXT.md | head -1` — the flip and the decision record are separate commits.
    - `.venv/bin/python -c "import pathlib,re; t=pathlib.Path('.planning/phases/20-pre-registration-the-three-condition-gate/20-SECURITY.md').read_text(); rows=re.findall(r'^\| T-20-\d+ \|.*\|\s*\*?\*?(open|closed)\*?\*?\s*\|$', t, re.M); assert rows.count('open')==1, rows.count('open')"` exits 0 — exactly one row carries Status `open`, matching `threats_open: 1`.
    - Every span the `*What was wrong, preserved:*` phrase introduces in the T-20-19 row at HEAD is still present verbatim: `git show HEAD:...20-SECURITY.md | grep -o 'What was wrong, preserved:.\{0,400\}' | tail -1` output appears in the working copy.
    - `git diff --exit-code -- scripts/ tests/ results/` returns 0.
  </acceptance_criteria>

  <done>`20-SECURITY.md` publishes `status: blocked` / `threats_open: 1`, T-20-19 appears in `### Open` with both reproductions and a named closing condition, T-20-21 and all historical text are byte-identical, and the flip is its own commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a plan that says a thing will be done ↔ a guard that proves it was | `20-SECURITY.md:39` — this file's `status` and `threats_open` cross it. This plan moves them toward the truth rather than toward completion. |
| a decision record ↔ the artifact that cites it | A decision written after the code that names it is indistinguishable from a rationalisation. Enforced by the wave graph. |
| a published closure ↔ its historical text | A register that erases what it got wrong teaches the next audit nothing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-67 | Repudiation | a D-38…D-41 citation landing before the record exists | mitigate | This plan is wave 12 and every later plan in this wave-set declares it in `depends_on`; the ordering is a property of the plan graph, exactly as `20-09` → `20-08` was, not a sentence in a SUMMARY |
| T-20-68 | Tampering | a decision recorded as a decision but tuned toward a favourable answer | mitigate | D-38 is a bound on a PROPERTY whose value is the adapter floor already committed at `20-07` (`results/phase20_retention_floor.json`, 2026-08-20 19:37:56) — it cannot be moved without moving a committed artifact. D-41 publishes the against-interest half: the bound's first catch is this repository's own fixture floor |
| T-20-69 | Repudiation | a register flipped OPEN and then never re-closed, or re-closed on the plan | mitigate | D-39 names plan `20-17` as the re-close and names its precondition (both tripwires observed red-then-green against both measured cases). The `### Open` row states that condition in the file itself, so a re-close that skips it contradicts the file |
| T-20-70 | Tampering | the OPEN flip rewriting rows other than T-20-19's | mitigate | Task 2's acceptance criteria assert `T-20-21`'s row byte-identical against `git show HEAD`, assert exactly one row carries Status `open`, and assert the preserved `*What was wrong, preserved:*` span survives verbatim |
| T-20-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install task in this wave-set. `scripts/phase20_gate_coverage.py` and `tests/test_phase20_correction.py` are stdlib plus sibling scripts; `pyproject.toml` is sha256-pinned by `tests/test_package.py` (STAT-04) and is not touched. No `[ASSUMED]`/`[SUS]` package is introduced |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q` → `874 passed, 1 skipped` — this plan writes no code, so the
  baseline must be exactly unchanged.
- `ruff check . && ruff format --check .` → clean (`.planning` is excluded from ruff by
  `pyproject.toml:41`, so this is a no-op regression check).
- `git diff --exit-code -- scripts/mitigation_gate.py scripts/erasure_gate.py` → 0.
- `git diff --exit-code -- scripts/ tests/ results/` → 0.
- Exactly two commits land from this plan, in order: the D-38…D-41 record, then the register flip.
</verification>

<success_criteria>
- `20-CONTEXT.md` carries D-38, D-39, D-40 and D-41 with zero deletions against HEAD.
- `20-SECURITY.md` reads `status: blocked` / `threats_open: 1` with exactly one row at Status `open`.
- T-20-19's historical text and T-20-21's entire row are byte-identical to HEAD.
- Both frozen files return 0 from `git diff --exit-code`.
- Full suite `874 passed, 1 skipped`.
</success_criteria>

<output>
Create `.planning/phases/20-pre-registration-the-three-condition-gate/20-13-SUMMARY.md` when done.
</output>
