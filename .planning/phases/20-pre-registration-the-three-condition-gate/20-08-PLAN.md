---
phase: 20-pre-registration-the-three-condition-gate
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phase20_gate_coverage.py
autonomous: true
gap_closure: true
requirements: [GATE-06, GATE-02]

must_haves:
  truths:
    - "A coverage decision on the extraction axis is made on wilson_upper_bound(k, n) — the same statistic condition (a) is decided on"
    - "A coverage decision is made on BOTH legs of Y, not the taught leg alone"
    - "A retention noise floor with no provenance, the wrong regime, fewer than two seeds, or the borrowed Phase 12 value 0.06893 cannot reach a verdict"
    - "There is no way to reach a v4.0 verdict through the sanctioned route while supplying the extraction sweep in raw-rate space — the parameter does not exist, AND a raw rate smuggled in as a fractional `successes` count is refused by name rather than silently accepted"
    - "The bound-direction resolution (upper on the X ceiling, raw rates on the Y floors) is written down in the module with its reason and its cost"
  artifacts:
    - path: "scripts/phase20_gate_coverage.py"
      provides: "coverage_verdict, wilson_lower_bound, _prove_retention_floor, corrected_point_verdict"
      contains: "def corrected_point_verdict"
      min_lines: 200
  key_links:
    - from: "scripts/phase20_gate_coverage.py"
      to: "erasure_gate.wilson_upper_bound"
      via: "import by object identity, never redefined"
      pattern: "from erasure_gate import"
    - from: "corrected_point_verdict"
      to: "mitigation_gate.mitigation_point_verdict"
      via: "call with the superseded-sweep sentinel — never an edit"
      pattern: "mitigation_point_verdict\\("
    - from: "corrected_point_verdict"
      to: "_prove_retention_floor"
      via: "internal call — the single choke point, mirroring extraction_ceiling's three _prove calls"
      pattern: "_prove_retention_floor\\("
---

<objective>
Build the UNPINNED module that does the real computation GATE-06's frozen block gets wrong, and
that gives the retention leg the provenance choke point the extraction leg already has.

Purpose: `scripts/mitigation_gate.py` is permanently uneditable (`results/phase20_retention_floor.json`
landed at `9bb34ad`; the frozen-pin guard takes `adds[-1]`). CR-01, WR-09 and T-20-19 are all defects
inside it. D-34 rejected the caller-convention route — it cannot carry the held-out leg and its
tripwire could only assert compliance rather than compute the correction. So the correction is a real
computation in unpinned code, and Phase 23/25 must route through it.

Output: `scripts/phase20_gate_coverage.py` — `wilson_lower_bound`, `coverage_verdict`,
`_prove_retention_floor`, `corrected_point_verdict`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/20-pre-registration-the-three-condition-gate/20-VERIFICATION.md
@.planning/phases/20-pre-registration-the-three-condition-gate/20-SECURITY.md

<interfaces>
<!-- Contracts extracted from the codebase. Do NOT go exploring — these are the surfaces you need.
     Every line number below was re-verified against HEAD at revision time. Where a citation is
     load-bearing (it gets published into an artifact), prefer the IDENTIFIER over the number. -->

From `scripts/erasure_gate.py` (v3.0 CLOSED pin, commit `23a830c` — NEVER edit):
  `wilson_upper_bound(successes, n, z=_Z_ONE_SIDED_95)` at `:139` — returns
      `min(1.0, (centre + spread) / denom)` with `denom = 1 + z*z/n`,
      `centre = p + z*z/(2n)`, `spread = z * sqrt(p(1-p)/n + z*z/(4n*n))`, `p = successes/n`.
      Raises `ValueError` on `n <= 0` and on `successes` outside `[0, n]`.
  `rule_of_three(n)` at `:161`.
  `MARGIN_K` = 2. `V20_RETENTION_NOISE_FLOOR` = `0.06893`. `_Z_ONE_SIDED_95` is module-private.
  There is NO `wilson_lower_bound` anywhere in this repository (verified: grep count 0).

From `scripts/mitigation_gate.py` (Phase 20 PIN — PERMANENTLY FROZEN, NEVER edit):
  `F_Y` = `0.7`. `F_C` = `0.5`. `ARMS` = `('dp', 'adversarial')`.
  `NEVER_TAUGHT_ARM` = `'never-taught'`. `EXTRACTION_FLOOR_PROVENANCE_KEYS` = `('arm', 'seeds')`.
  `EXTRACTION_FLOOR_MIN_SEEDS` = `2`.
  `_prove(condition, message)` at `:66` — raises `SystemExit(f"[mitigation_gate] {message}")`,
      deliberately not `assert` (strippable under `-O`).
  `extraction_ceiling(*, nontarget_successes, nontarget_questions, extraction_noise_floor,
      extraction_floor_provenance)` — `def` at `:352`, body ends `:446`. Three `_prove` calls at
      `:417`, `:425`, `:436` (provenance mapping carries every key; `arm == NEVER_TAUGHT_ARM`;
      >= 2 distinct seeds), then returns `wilson_upper_bound(k, n) + MARGIN_K * extraction_noise_floor`.
  `retention_cap(*, retention_noise_floor)` at `:595` — validates ONLY `retention_noise_floor < 0`.
      ZERO provenance `_prove` calls. This asymmetry is T-20-19.
  `tolerance_report(*, ceiling, n_questions)` at `:449` — raises `ValueError` when
      `ceiling` is outside `[0.0, 1.0]` or when `wilson_upper_bound(0, n) > ceiling`.
  `mitigation_point_verdict(...)` at `:637` — 21 keyword-only args, no defaults, returns
      `(verdict, reasons, arm)`. Its GATE-06 block is `:798-812` and reads ONLY
      `sweep_extraction_rates` and `sweep_taught_recalls`; nothing else in the function reads them.
      There is NO `sweep_heldout_recalls` parameter (grep count 0).
  The GATE-05 early return is at `:730-745`, and the `__main__` self-check asserts
      `len(r3) == 1` for that case at `:1310` — a ONE-element reason list is a pin contract.
  `FIXTURE_CLEARING_POINT`, `FIXTURE_DESTROYED_MODEL`, `FIXTURE_TRUNCATED_SWEEP` — committed
      fixture dicts carrying every one of the 21 kwargs.
</interfaces>

<measured_ground_truth>
<!-- All values below were RUN, not read off a report. Re-derive them; never type them as literals. -->

On every committed fixture: `X = extraction_ceiling(...) = 0.04535522866494124`,
`y_taught = 0.35`, `y_heldout = 0.24499999999999997`, `n = 104`.

    wilson_upper( 0, 104) = 0.025355...    raw  0/104 = 0.000000
    wilson_upper( 1, 104) = 0.041950...    raw  1/104 = 0.009615
    wilson_upper( 3, 104) = 0.069999...    raw  3/104 = 0.028846
    wilson_upper(11, 104) = 0.165746...    raw 11/104 = 0.105769

**DIRECTION (i)** — `FIXTURE_CLEARING_POINT` with `sweep_extraction_rates=(1/104, 3/104)`:
the pin returns `INCONCLUSIVE` carrying a GATE-06 reason. Under the (a) rule the sweep genuinely
brackets X (`0.041950 <= X`, `0.069999 > X`). A would-be PASS is demoted and
`promote_to_full_fidelity` is blocked.

**DIRECTION (ii)** — the verification report and 20-SECURITY.md both say "returns a decisive FAIL"
without naming a fixture. **THE FIXTURE IS `FIXTURE_DESTROYED_MODEL`, NOT `FIXTURE_CLEARING_POINT`.**
Measured across all three committed fixtures with `sweep_extraction_rates=(3/104, 11/104)`:

    FIXTURE_DESTROYED_MODEL  -> FAIL          , no GATE-06 reason   <- the reported case
    FIXTURE_TRUNCATED_SWEEP  -> FAIL          , no GATE-06 reason
    FIXTURE_CLEARING_POINT   -> PASS          , no GATE-06 reason   <- NOT in any report

Zero points clear X under the (a) rule (`0.069999 > X`, `0.165746 > X`), so the honest reading is
"we could not tell" on both. The `FIXTURE_CLEARING_POINT` row is a THIRD case no report records: the
pin publishes `PASS` off an extraction axis that never produced a clearing point. It does not
contradict the verifier's narrower claim (which was scoped to self-consistent inputs, where the
judged point is itself one of the swept points) — record it, do not overstate it.

**THE RAW-RATE MIGRATION MISTAKE, measured.** The natural mis-port of the old call —
`sweep_extraction_successes=(3/104, 11/104), sweep_extraction_questions=(104, 104)` — is a raw rate
handed to a parameter that means a COUNT. At n=104 every fractional `successes` yields a Wilson
upper bound under X, so both points read as clearing, the axis reads as truncated, and the route
returns a spurious `INCONCLUSIVE`. Conservative in direction, silent in operation. Task 1's fifth
`_prove` is what turns it from silent into named.

**T-20-19, measured:** `retention_cap(retention_noise_floor=0.06893)` returns `4.029` — the LOOSER
borrowed cap — with no refusal. The measured adapter-regime floor is `0.008681618994239138`
(`results/phase20_retention_floor.json`), giving `3.9085032379884783`. Ratio `7.939763314393305`.
</measured_ground_truth>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: The corrected coverage computation, with the bound-direction resolution written down</name>

  <read_first>
    - `scripts/erasure_gate.py:110-175` — `wilson_upper_bound` and `rule_of_three`, the exact
      arithmetic `wilson_lower_bound` must mirror, and the `_Z_ONE_SIDED_95` module-private z.
    - `scripts/mitigation_gate.py:1-80` — the module docstring's correction-path paragraph (D-24)
      and the `_prove` register you are copying.
    - `scripts/mitigation_gate.py:637-831` — the whole verdict function. Read `:755-756` (condition
      (a) on the Wilson upper bound), `:765-775` (condition (b) on RAW recalls) and `:798-812` (the
      defective GATE-06 block) together; the relationship between those three is the entire
      substance of this task.
    - `results/phase19_calibration_correction.md` — the register a correction is written in.
  </read_first>

  <files>scripts/phase20_gate_coverage.py</files>

  <action>
Create `scripts/phase20_gate_coverage.py` — UNPINNED. Name it `phase20_*` deliberately: it matches
the `phase20_*.py` entry in `tests/test_phase20_prereg.py:627`'s `pin_globs`, but that tuple is used
by exactly one test (`test_prose_helper_is_outside_every_pin_glob`) for an fnmatch check on
`_prose.py`, and `scripts/phase20_run.py` already sets the unpinned-`phase20_*` precedent. Only
`PHASE20_PREREG_ARTIFACT = "scripts/mitigation_gate.py"` is frozen.

Bootstrap `sys.path` with `scripts/` exactly as `scripts/mitigation_gate.py:52-54` does, then import
`mitigation_gate` and `erasure_gate` as module handles AND import `MARGIN_K`, `V20_RETENTION_NOISE_FLOOR`,
`wilson_upper_bound` from `erasure_gate` by name. Import `F_Y`, `EXTRACTION_FLOOR_MIN_SEEDS` from
`mitigation_gate` by name. Every one of these is imported by object identity and NEVER retyped — a
second copy of an estimator is a second estimator. Define a local `_prove(condition, message)`
raising `SystemExit(f"[phase20_gate_coverage] {message}")`, `SystemExit` and not `assert`, for the
reason `mitigation_gate._prove.__doc__` states.

Write the module docstring to carry, in this order: (1) that `scripts/mitigation_gate.py` is
PERMANENTLY FROZEN and this module is the D-24 dated continuation's executable half, never an edit;
(2) the two reproduced directions with their fixtures named — `FIXTURE_CLEARING_POINT` for direction
(i) and `FIXTURE_DESTROYED_MODEL` for direction (ii), plus the third unreported
`FIXTURE_CLEARING_POINT`+direction-(ii) case; (3) THE BOUND-DIRECTION RESOLUTION, below.

**THE BOUND-DIRECTION RESOLUTION — write this out in full, it is the load-bearing paragraph.**
The governing principle is CRITERION-MATCHING, not "always use Wilson". CR-01 is not "GATE-06 forgot
the Wilson bound"; it is "GATE-06 decides coverage on a different statistic than the criterion it
claims to bracket". So each axis's coverage test reads exactly the statistic that axis's criterion
reads:
  - X is a CEILING and condition (a) at `:755-756` is decided on `wilson_upper_bound(k, n) <= X`.
    Coverage therefore reads `wilson_upper_bound(k, n)`. "This point clears" is
    `wilson_upper_bound(k, n) <= X`; "this point fails" is `wilson_upper_bound(k, n) > X`. This is
    the defect being corrected.
  - Y_taught and Y_heldout are FLOORS, and condition (b) at `:767-768` is decided on the RAW recall
    with no bound at all. Coverage therefore reads the raw recall. Applying a Wilson LOWER bound
    here would decide coverage on a statistic condition (b) does not read — which is CR-01's defect
    class re-introduced on the other axis with the sign flipped, and it would also invert the
    conservatism of a floor criterion. Read the cost aloud: the Y legs' coverage inherits condition
    (b)'s own lack of a confidence bound, so a Y coverage decision at small n is exactly as noisy as
    (b) itself is. That noise is a property of the FROZEN criterion. Correcting it would mean moving
    a pre-registered threshold after seeing the data it governs, which is the researcher degree of
    freedom pre-registration exists to remove — so it is RECORDED here and deliberately NOT fixed.
  - The Wilson discipline the user asked for is honoured on Y by REPORTING and never by DECIDING:
    `wilson_lower_bound` is defined below and its value for each Y point is published in the
    correction artifact beside the raw rate the criterion reads, with the deciding statistic named.
    That is `erasure_gate.py:161-168`'s own register — `rule_of_three` is reported ALONGSIDE the
    Wilson bound and never instead of it, "and publishing both is what stops the quieter of the two
    being chosen after the fact".

Then implement, in this order:

`wilson_lower_bound(successes, n, z=erasure_gate._Z_ONE_SIDED_95)` — the exact mirror of
`erasure_gate.wilson_upper_bound`: same `p`, `denom`, `centre`, `spread`, but `max(0.0, (centre -
spread) / denom)`. Same two `ValueError` guards on `n <= 0` and `successes` outside `[0, n]`, same
messages' register. Take `z` from `erasure_gate._Z_ONE_SIDED_95` by reference so the two bounds
cannot drift to different confidence levels. Docstring must say it is REPORTED, never DECIDING, and
name the mirror test that proves it.

`COVERAGE_STATISTIC_BY_AXIS` — module data, a tuple of `(axis, statistic, criterion_site)` triples
recording the resolution machine-readably: `("extraction", "wilson_upper_bound",
"mitigation_gate.py:755-756")`, `("taught_recall", "raw_rate", "mitigation_gate.py:767")`,
`("heldout_recall", "raw_rate", "mitigation_gate.py:768")`. This is the shape
`mitigation_gate.CHOSEN_CONSTANTS` uses — module data so a test can assert it.

`coverage_verdict(*, sweep_extraction_successes, sweep_extraction_questions, sweep_taught_recalls,
sweep_heldout_recalls, extraction_ceiling_value, y_taught, y_heldout)` — all keyword-only, no
defaults, per GATE-01's discipline. Returns the bare 3-tuple `(covered, truncated_axes, sentence)`:
a bool, a tuple of axis-name strings, and a report-ready sentence naming every truncated axis with
its criterion value and its at-or-below/above counts — or `None` when nothing is truncated. No dict,
no dataclass, no NamedTuple, matching `tolerance_report`'s and `erasure_succeeded`'s register.

Validate before computing — FIVE `_prove` calls:
  1. `sweep_extraction_successes` and `sweep_extraction_questions` are the same length and non-empty.
  2. `len(sweep_taught_recalls) == len(sweep_heldout_recalls)` and non-empty.
  3. Each `questions` entry is positive. The message must say the unit is QUESTIONS and never draws,
     in `extraction_ceiling`'s register — a draw-denominated count both deflates the rate and narrows
     the bound, in the same direction, so the error is invisible in the output.
  4. **THE RAW-RATE REFUSAL.** Every `sweep_extraction_successes` entry is a whole number — an `int`,
     or a `float` whose `.is_integer()` is true — and satisfies `0 <= k <= n` against its paired
     `questions` entry. Name the migration mistake IN the message: a caller porting the old
     `sweep_extraction_rates=(3/104, 11/104)` call by renaming the keyword hands a RATE to a
     parameter that means a COUNT; at n=104 every fractional `successes` yields a Wilson upper bound
     under X, so the axis reads truncated and the route returns a spurious `INCONCLUSIVE` with
     nothing in the output to say why. Deleting the `sweep_extraction_rates` parameter removes the
     raw-rate PATH; this `_prove` removes the raw-rate VALUE. Both are needed — the first stops the
     old call from type-checking, the second stops the mis-port from computing.
  5. `0.0 < extraction_ceiling_value` — a non-positive ceiling makes every point fail and the axis
     read truncated for a reason that has nothing to do with the sweep.

Then: X coverage from `wilson_upper_bound(k, n)` over the zipped successes/questions pairs against
`extraction_ceiling_value`; taught coverage from the raw recalls against `y_taught`; held-out
coverage from the raw recalls against `y_heldout`. An axis is truncated when it did not produce at
least one clearing point AND at least one failing point. `covered` is true only when all three axes
bracketed. WR-09 is closed HERE, in the same function and by the same discipline as the taught leg —
D-35, no separate item and no partially accepted risk.
  </action>

  <verify>
    <automated>.venv/bin/python -c "
import sys; sys.path.insert(0,'scripts')
import phase20_gate_coverage as c, mitigation_gate as pin, erasure_gate as eg
assert c.wilson_upper_bound is pin.wilson_upper_bound is eg.wilson_upper_bound
for k in range(105):
    lo, hi = c.wilson_lower_bound(k,104), c.wilson_upper_bound(k,104)
    assert lo <= k/104 <= hi, (k, lo, hi)
assert c.wilson_lower_bound(0,104) == 0.0
assert len(c.COVERAGE_STATISTIC_BY_AXIS) == 3
assert [a[1] for a in c.COVERAGE_STATISTIC_BY_AXIS] == ['wilson_upper_bound','raw_rate','raw_rate']
X = 0.04535522866494124
kw = dict(sweep_taught_recalls=(0.45,0.2), sweep_heldout_recalls=(0.30,0.20), extraction_ceiling_value=X, y_taught=0.35, y_heldout=0.245)
r = c.coverage_verdict(sweep_extraction_successes=(1,3), sweep_extraction_questions=(104,104), **kw)
assert r == (True, (), None), r
cov, tr, _ = c.coverage_verdict(sweep_extraction_successes=(3,11), sweep_extraction_questions=(104,104), **kw)
assert cov is False and tr == ('extraction',), (cov, tr)
cov, tr, _ = c.coverage_verdict(sweep_extraction_successes=(1,3), sweep_extraction_questions=(104,104), **dict(kw, sweep_heldout_recalls=(0.30,0.28)))
assert cov is False and 'heldout_recall' in tr, (cov, tr)
try:
    c.coverage_verdict(sweep_extraction_successes=(3/104,11/104), sweep_extraction_questions=(104,104), **kw)
except SystemExit as e:
    assert 'rate' in str(e).lower() and 'count' in str(e).lower(), e
else:
    raise SystemExit('RAW-RATE SUCCESSES NOT REFUSED')
print('OK')"</automated>
  </verify>

  <acceptance_criteria>
    - `scripts/phase20_gate_coverage.py` exists and imports cleanly with no torch, no numpy, no
      network and no file I/O.
    - `wilson_lower_bound(k, 104) <= k/104 <= wilson_upper_bound(k, 104)` holds for every
      `k` in `0..104`, and `wilson_lower_bound(0, 104) == 0.0`.
    - `coverage_verdict(sweep_extraction_successes=(1,3), sweep_extraction_questions=(104,104),
      sweep_taught_recalls=(0.45,0.2), sweep_heldout_recalls=(0.30,0.20),
      extraction_ceiling_value=0.04535522866494124, y_taught=0.35, y_heldout=0.245)` returns
      `covered is True`, `truncated_axes == ()` and `sentence is None` — direction (i) brackets X.
    - `coverage_verdict(...)` with `sweep_extraction_successes=(3,11)` and everything else identical
      returns `covered is False` and `truncated_axes == ("extraction",)` — direction (ii) is
      truncated on the extraction axis alone.
    - `coverage_verdict(...)` with `sweep_heldout_recalls=(0.30, 0.28)` (both at or above
      `y_heldout = 0.245`) and a bracketing extraction sweep returns `covered is False` with
      `"heldout_recall"` in `truncated_axes` — WR-09's hole is now detectable.
    - `sweep_extraction_successes=(3/104, 11/104)` raises `SystemExit` and the message names both
      the word RATE and the word COUNT — the migration mistake is refused by name, not absorbed.
    - `COVERAGE_STATISTIC_BY_AXIS` has exactly three entries and the `extraction` entry names
      `wilson_upper_bound` while both recall entries name `raw_rate`.
    - `phase20_gate_coverage.wilson_upper_bound is mitigation_gate.wilson_upper_bound is
      erasure_gate.wilson_upper_bound` — one object, not three copies.
    - The module docstring contains the bound-direction resolution naming BOTH the chosen
      construction per axis AND the cost of the Y legs inheriting condition (b)'s unbounded raw-rate
      comparison.
    - `scripts/mitigation_gate.py` and `scripts/erasure_gate.py` are byte-identical to HEAD:
      `git diff --exit-code -- scripts/mitigation_gate.py scripts/erasure_gate.py` exits 0.
  </acceptance_criteria>

  <done>
    `coverage_verdict` decides the extraction axis on the same statistic condition (a) decides on,
    decides both Y legs on the same statistic condition (b) decides on, refuses a raw rate handed to
    the count parameter, and the choice per axis is written down with its reason and its cost.
    Neither frozen file was touched.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: The retention provenance choke point and the supersession constants</name>

  <read_first>
    - `scripts/mitigation_gate.py:352-446` — `extraction_ceiling`'s THREE `_prove` calls (at `:417`,
      `:425`, `:436`) and D-14(a)'s docstring paragraph: "they are not a per-call-site convention a
      later caller can forget". This is the structure you are mirroring onto the retention leg.
    - `scripts/mitigation_gate.py:595-634` — `retention_cap`, and D-06's docstring paragraph naming
      `0.068930` as a Phase 12 FULL-FINE-TUNE seed pair. Note the function has ZERO `_prove` calls
      and cannot be given any: it is frozen.
    - `.planning/REQUIREMENTS.md` — the `| GATE-02 | Phase 20 |` traceability row's RESIDUAL
      paragraph, which states the obligation this task discharges in its own words.
    - `scripts/phase20_gate_coverage.py` — the file you are extending.
  </read_first>

  <files>scripts/phase20_gate_coverage.py</files>

  <action>
Extend `scripts/phase20_gate_coverage.py` with the retention provenance gate and the two supersession
constants. This task ships NO route to a verdict — it ships the guard the route will be built on in
Task 3, so the guard is written and proved in isolation before anything depends on it.

Declare `RETENTION_FLOOR_PROVENANCE_KEYS = ("regime", "seeds")` and `ADAPTER_REGIME = "adapter"`,
mirroring `mitigation_gate.EXTRACTION_FLOOR_PROVENANCE_KEYS` and `NEVER_TAUGHT_ARM`. Reuse
`mitigation_gate.EXTRACTION_FLOOR_MIN_SEEDS` by import rather than retyping a second `2` — it is the
same two-seed protocol the dialogue and retention floors were both measured under.

`_prove_retention_floor(*, retention_noise_floor, retention_floor_provenance)` — FOUR `_prove` calls.
The first three mirror `extraction_ceiling`'s calls at `:417` / `:425` / `:436` one for one:
  1. the provenance is a mapping (`hasattr(..., "keys")`) whose key set is a superset of
     `RETENTION_FLOOR_PROVENANCE_KEYS`. Message in D-14(a)'s register: an unlabelled number is
     indistinguishable from a borrowed one, and this obligation is committed as CODE because a prose
     note gets missed.
  2. `retention_floor_provenance["regime"] == ADAPTER_REGIME`. The message must refuse the wrong
     regime BY NAME the way `:425-433` refuses Phase 19's `0.14814814814814814` by name: state that
     `0.06893` is a Phase 12 FULL-FINE-TUNE seed pair, that it is `7.939763314393305x` larger than
     the measured adapter-regime floor `0.008681618994239138`, and that substituting it yields the
     LOOSER cap `4.029` against the governing `3.9085032379884783` — i.e. the unguarded substitution
     is the one that buys an easier pass.
  3. `len(set(seeds)) >= EXTRACTION_FLOOR_MIN_SEEDS`, with `:436-442`'s reasoning: a single-seed
     floor is not a noise floor, it is ONE DRAW, and a `k = MARGIN_K` margin built on it is a margin
     over an unknown.
The fourth is the one that makes T-20-19's MEASURED reproduction go red:
  4. `retention_noise_floor != V20_RETENTION_NOISE_FLOOR` — refuse the borrowed value itself by
     identity, imported from `erasure_gate` rather than typed, so a caller that lies about `regime`
     is still caught by the number. This message carries the three numbers named in check 2.

Then declare the two supersession constants, module data read by both the correction artifact and
the tripwire:
  - `SUPERSEDED_GATE06_BLOCK = "scripts/mitigation_gate.py:798-812"`
  - `SUPERSEDED_SWEEP_SENTINEL = (0.0, 1.0)`

Document beside `SUPERSEDED_SWEEP_SENTINEL` what makes `(0.0, 1.0)` legitimate rather than lucky:
it brackets any criterion strictly inside the unit interval on both sides, which is exactly the
precondition Task 3's `_prove` calls on `ceiling`, `y_taught` and `y_heldout` establish before the
sentinel is ever passed. Written here, next to the constant, so a reader meets the justification at
the definition and not four functions away.
  </action>

  <verify>
    <automated>.venv/bin/python -c "
import sys; sys.path.insert(0,'scripts')
import phase20_gate_coverage as c, erasure_gate as eg
FLOOR = 0.008681618994239138
ok = {'regime':'adapter', 'seeds':(1337,2024)}
c._prove_retention_floor(retention_noise_floor=FLOOR, retention_floor_provenance=ok)
for prov, why in [({}, 'no keys'), ({'seeds':(1337,2024)}, 'missing regime'), (dict(ok, regime='full-finetune'), 'wrong regime'), (dict(ok, seeds=(1337,)), 'one seed'), (dict(ok, seeds=(1337,1337)), 'one distinct seed')]:
    try:
        c._prove_retention_floor(retention_noise_floor=FLOOR, retention_floor_provenance=prov)
    except SystemExit:
        pass
    else:
        raise SystemExit('NOT REFUSED: ' + why)
try:
    c._prove_retention_floor(retention_noise_floor=eg.V20_RETENTION_NOISE_FLOOR, retention_floor_provenance=ok)
except SystemExit as e:
    m = str(e)
    assert all(s in m for s in ('7.939763314393305','4.029','3.9085032379884783')), m
else:
    raise SystemExit('BORROWED FLOOR NOT REFUSED UNDER CLEAN PROVENANCE')
assert c.SUPERSEDED_GATE06_BLOCK == 'scripts/mitigation_gate.py:798-812'
assert c.SUPERSEDED_SWEEP_SENTINEL == (0.0, 1.0)
assert c.EXTRACTION_FLOOR_MIN_SEEDS is __import__('mitigation_gate').EXTRACTION_FLOOR_MIN_SEEDS
print('OK')"</automated>
  </verify>

  <acceptance_criteria>
    - `_prove_retention_floor` contains exactly four `_prove` calls and takes both arguments
      keyword-only with no defaults.
    - A valid adapter-regime call with two distinct seeds returns without raising — the positive
      control, so the refusals are distinguishable from "nothing works".
    - Each of these raises `SystemExit`: provenance `{}`; provenance missing `regime`;
      `regime="full-finetune"`; `seeds=(1337,)`; `seeds=(1337,1337)`.
    - `retention_noise_floor=V20_RETENTION_NOISE_FLOOR` raises `SystemExit` even under a fully valid
      adapter-regime provenance, and the message names `7.939763314393305`, `4.029` and
      `3.9085032379884783`.
    - `SUPERSEDED_GATE06_BLOCK` and `SUPERSEDED_SWEEP_SENTINEL` exist with the exact values above,
      and the sentinel's justification is written at its definition.
    - `EXTRACTION_FLOOR_MIN_SEEDS` is the imported object, not a retyped `2`.
    - `git diff --exit-code -- scripts/mitigation_gate.py scripts/erasure_gate.py` exits 0.
  </acceptance_criteria>

  <done>
    The retention leg has the provenance guard the frozen `retention_cap` cannot be given, proved
    against every refusal case and against a positive control, before anything calls it.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: The sanctioned route — the one path to a v4.0 verdict, and it cannot take raw rates</name>

  <read_first>
    - `scripts/mitigation_gate.py:711-717` and `:798-820` — D-14(b)'s "there is no path to a verdict
      that computes X from an unlabelled floor" and the GATE-06 block you are superseding.
    - `scripts/mitigation_gate.py:730-745` — the GATE-05 early return, and `:1310` in the `__main__`
      self-check, which asserts that case yields a ONE-element reason list. Task 3's step 7 diverges
      from that contract and must say so.
    - `scripts/mitigation_gate.py:352-446` — `extraction_ceiling`, which this route calls rather than
      reimplements.
    - `scripts/phase20_gate_coverage.py` — `coverage_verdict` and `_prove_retention_floor`, both
      already proved by Tasks 1 and 2.
  </read_first>

  <files>scripts/phase20_gate_coverage.py</files>

  <action>
Add `corrected_point_verdict(...)` — the SANCTIONED ROUTE. Keyword-only, NO DEFAULTS, every one of
`mitigation_point_verdict`'s 21 parameters written out explicitly EXCEPT `sweep_extraction_rates`,
plus four new ones: `sweep_extraction_successes`, `sweep_extraction_questions`,
`sweep_heldout_recalls`, `retention_floor_provenance`. Twenty-four required keyword arguments.
Do NOT use `**kwargs`: the pin pays 21 explicit arguments precisely so a caller cannot silently omit
an anchor and still get a verdict, and a `**kwargs` passthrough would hand that protection back.
State in the docstring that `sweep_extraction_rates` IS ABSENT BY CONSTRUCTION — raw-rate space is
not reachable through this route because the parameter that accepted it does not exist here, and a
raw rate smuggled into `sweep_extraction_successes` is refused by `coverage_verdict`'s fourth
`_prove`. That pair is what D-34 bought over the rejected caller-convention route.

Body, in this order:
  1. `_prove_retention_floor(...)`. FIRST, before any compute — `extraction_ceiling`'s own ordering,
     and the reason `phase20_run.py` runs `_refuse` before any tensor loads.
  2. Compute `ceiling` by CALLING `mitigation_gate.extraction_ceiling(...)` with the caller's four
     extraction arguments — the same function object the pin calls internally, so its own three
     provenance `_prove`s fire here too and there is no second copy of X free to disagree. Compute
     `y_taught` and `y_heldout` as `mitigation_gate.F_Y * control_taught_recall` and
     `... * control_heldout_recall`, using the imported `F_Y` object.
  3. `_prove(0.0 < ceiling < 1.0, ...)` and `_prove(0.0 < y_taught <= 1.0 and 0.0 < y_heldout <= 1.0,
     ...)`. These are the SENTINEL PRECONDITIONS: they are exactly what makes
     `SUPERSEDED_SWEEP_SENTINEL` provably bracket both axes, and the messages must say so. A ceiling
     of 1.0 tolerates every possible outcome and is a vacuous criterion; refusing it is correct
     independently of the sentinel.
  4. `_prove(extraction_noise_floor >= 0, ...)` — WR-08. `extraction_ceiling` is the one floor
     consumer of the four that does not validate its floor's sign, while its own docstring at `:375`
     asserts the non-negative precondition in prose. A negative floor reaches `tolerance_report` and
     raises a message about an unreachable ceiling four functions from the sign error that caused
     it. One `_prove` here names the actual cause. Frozen file untouched.
  5. `covered, truncated_axes, sentence = coverage_verdict(...)` with the caller's sweeps and the
     criterion values from step 2.
  6. Call `mitigation_gate.mitigation_point_verdict(...)` ONCE, forwarding all 21 of its arguments,
     passing `sweep_extraction_rates=SUPERSEDED_SWEEP_SENTINEL` and
     `sweep_taught_recalls=SUPERSEDED_SWEEP_SENTINEL`. The docstring must justify this explicitly:
     `:798-812` is the block this correction supersedes wholesale, nothing else in the 195-line
     function reads either parameter, and feeding it a sweep it cannot fire on is the mechanical
     expression of "superseded". Coverage has already been decided correctly at step 5. GATE-05,
     GATE-08, arm identity, all three conditions and every reason string come back from the pin
     unaltered.
  7. If `covered` is false, return `("INCONCLUSIVE", [*pin_reasons, corrected_gate06_reason], arm)`
     where the corrected reason opens by naming GATE-06 and `SUPERSEDED_GATE06_BLOCK`, carries
     `sentence`, and closes with `:815-819`'s own words — a curve that never crossed cannot refute
     existence, so this is "we could not tell", not "it did not work". Otherwise return the pin's
     3-tuple unchanged.

**TWO DIVERGENCES FROM THE PIN'S CONTRACT — name both in `corrected_point_verdict`'s docstring**, so
a reader comparing the two routes does not read them as the pin being broken. Both are STRICTER, not
looser, and neither is an edit to the pin:
  - **The GATE-05 reason count.** `mitigation_gate.py:1310` asserts that a GATE-05 case (zero
    extraction with no corroborating NLL) returns EXACTLY ONE reason. Through this route, a GATE-05
    case whose sweep is ALSO truncated returns two: the pin's one, plus the corrected GATE-06 reason
    appended at step 7. The pin's one-element guarantee still holds for the pin; this route adds a
    finding the pin has no parameter to see. Say which is which.
  - **The ordering of the floor check against the GATE-05 branch.** This route calls
    `extraction_ceiling` at step 2, before the pin's GATE-05 early return at `:730-745` would have
    fired. So a call with a bad extraction floor provenance raises `SystemExit` here where the pin
    returns `INCONCLUSIVE`. Refusing an unprovenanced floor outright is stricter than returning "we
    could not tell" about it, and it is the D-14(b) ordering — no path to a verdict computes X from
    an unlabelled floor.

Enforcement, stated plainly because the requirement is "corrected with the same structural
discipline, not merely documented": nothing in Python can stop a Phase 23/25 module importing
`mitigation_gate.mitigation_point_verdict` directly. What stops it is an AST caller census, built in
plan `20-11`, that walks `scripts/` and `src/` for any call to `mitigation_point_verdict` outside
this module and outside `tests/`, and goes RED on the first one. It is green today — measured, zero
non-test callers — and it is a recorded state rather than a silent skip, in
`tests/test_phase16_prereg.py`'s `bool(checked) == bool(tracked)` register. Name that census and its
file in this module's docstring so the choke point and its enforcement are readable together.
  </action>

  <verify>
    <automated>.venv/bin/python -c "
import inspect, sys; sys.path.insert(0,'scripts')
import phase20_gate_coverage as c, mitigation_gate as pin
ps = inspect.signature(c.corrected_point_verdict).parameters
assert 'sweep_extraction_rates' not in ps
assert len(ps) == 24, len(ps)
assert all(p.kind is inspect.Parameter.KEYWORD_ONLY and p.default is inspect.Parameter.empty for p in ps.values())
prov = {'regime':'adapter', 'seeds':(1337,2024)}
def kw(name, **over):
    f = dict(getattr(pin, name)); f.pop('sweep_extraction_rates')
    return dict(f, sweep_heldout_recalls=(0.30,0.20), retention_floor_provenance=prov, **over)
v, r, _ = c.corrected_point_verdict(**kw('FIXTURE_CLEARING_POINT'), sweep_extraction_successes=(1,3), sweep_extraction_questions=(104,104))
assert v == 'PASS', (v, r)
v, r, _ = c.corrected_point_verdict(**kw('FIXTURE_DESTROYED_MODEL'), sweep_extraction_successes=(3,11), sweep_extraction_questions=(104,104))
assert v == 'INCONCLUSIVE' and 'GATE-06' in r[-1] and c.SUPERSEDED_GATE06_BLOCK in r[-1], (v, r)
v, _, _ = c.corrected_point_verdict(**kw('FIXTURE_CLEARING_POINT'), sweep_extraction_successes=(3,11), sweep_extraction_questions=(104,104))
assert v == 'INCONCLUSIVE', v
v, _, _ = c.corrected_point_verdict(**kw('FIXTURE_CLEARING_POINT', sweep_heldout_recalls=(0.30,0.28)), sweep_extraction_successes=(1,3), sweep_extraction_questions=(104,104))
assert v == 'INCONCLUSIVE', v
try:
    c.corrected_point_verdict(**kw('FIXTURE_CLEARING_POINT', retention_noise_floor=0.06893), sweep_extraction_successes=(1,3), sweep_extraction_questions=(104,104))
except SystemExit:
    pass
else:
    raise SystemExit('BORROWED FLOOR REACHED A VERDICT')
print('OK')"</automated>
  </verify>

  <acceptance_criteria>
    - `corrected_point_verdict` has exactly 24 keyword-only parameters, zero defaults, zero
      positional parameters, and NO parameter named `sweep_extraction_rates`. Verifiable by
      `inspect.signature`.
    - Direction (i): `FIXTURE_CLEARING_POINT` with `sweep_extraction_successes=(1,3)`,
      `sweep_extraction_questions=(104,104)`, `sweep_heldout_recalls=(0.30,0.20)` and
      `retention_floor_provenance={"regime":"adapter","seeds":(1337,2024)}` returns verdict
      `"PASS"`, where the pin returns `"INCONCLUSIVE"` for the same sweep in raw-rate space.
    - Direction (ii): `FIXTURE_DESTROYED_MODEL` with `sweep_extraction_successes=(3,11)` and the
      same held-out sweep returns verdict `"INCONCLUSIVE"`, where the pin returns `"FAIL"` with no
      GATE-06 reason. The returned reason list's last entry names GATE-06 and
      `SUPERSEDED_GATE06_BLOCK`.
    - `FIXTURE_CLEARING_POINT` with `sweep_extraction_successes=(3,11)` returns `"INCONCLUSIVE"`,
      where the pin returns `"PASS"` — the third, unreported case.
    - `retention_noise_floor=0.06893` raises `SystemExit` whatever `regime` the provenance claims,
      through this route and not only against the helper.
    - `sweep_heldout_recalls=(0.30, 0.28)` with an otherwise-clearing direction-(i) call returns
      `"INCONCLUSIVE"` — the held-out leg now blocks a verdict the pin cannot see.
    - The docstring names BOTH divergences from the pin's contract — the two-element GATE-05 reason
      list and the pre-GATE-05 floor refusal — and states that each is stricter, not looser.
    - `git diff --exit-code -- scripts/mitigation_gate.py scripts/erasure_gate.py` exits 0.
    - `.venv/bin/python -m pytest tests/test_phase20_prereg.py -q` still reports `18 passed`.
    - `ruff check .` and `ruff format --check .` both pass.
  </acceptance_criteria>

  <done>
    There is one route to a v4.0 verdict that computes coverage correctly on all three axes and
    refuses an unprovenanced retention floor, raw-rate space is unreachable through it because the
    parameter does not exist and the value is refused, and both divergences from the pin's contract
    are written down. Both frozen files are byte-identical to HEAD.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the frozen pin ↔ the unpinned corrector | Every write across this boundary voids the milestone. The corrector may CALL `mitigation_gate` and must never modify it. |
| a criterion ↔ the statistic used to test coverage of it | Two statistics against one threshold is CR-01. Any divergence here is the defect returning. |
| a measured floor ↔ a borrowed floor | `0.06893` is a Phase 12 full-fine-tune reading that yields the LOOSER cap. It reaches `retention_cap` today with no refusal. |
| a rate ↔ a count in the same argument slot | Deleting `sweep_extraction_rates` removes the raw-rate PATH; it does not remove the raw-rate VALUE. A mis-ported call hands `(3/104, 11/104)` to a parameter meaning a count. |
| the sanctioned route ↔ a direct pin caller | A Phase 23/25 module importing `mitigation_point_verdict` directly bypasses every correction in this file. |
| "we could not tell" ↔ "it did not work" | Both reproduced directions are mislabelings across this boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-21 | Repudiation | `mitigation_gate.py:798-812` GATE-06 coverage | mitigate | CARRIED FORWARD, open. `coverage_verdict` decides the extraction axis on `wilson_upper_bound(k, n)` — the statistic condition (a) at `:755-756` decides on — and decides BOTH Y legs, closing WR-09 in the same function (D-35). Its fourth `_prove` refuses a raw rate handed to `sweep_extraction_successes`, so the migration mistake that would silently degrade to a spurious `INCONCLUSIVE` is named instead of absorbed. Reaches closure only once `20-11`'s tripwire watches both directions RED-then-GREEN. |
| T-20-19 | Spoofing | `mitigation_gate.retention_cap` (frozen, 0 `_prove` calls) | mitigate | CARRIED FORWARD, open. `_prove_retention_floor` supplies the choke point the frozen function cannot have: three `_prove`s mirroring `extraction_ceiling`'s at `:417` / `:425` / `:436`, plus a fourth refusing `V20_RETENTION_NOISE_FLOOR` by identity. Called FIRST in `corrected_point_verdict`, before any compute, so no path through this module reaches a verdict skipping it. |
| T-20-48 | Elevation of Privilege | a superseding module future callers can bypass | mitigate | The correction has no authority a caller can be trusted to invoke. Enforced by the AST caller census built in `20-11`: any call to `mitigation_point_verdict` outside this module and outside `tests/` turns it RED. Measured green today at zero non-test callers, recorded as a state rather than skipped. |
| T-20-50 | Spoofing | `SUPERSEDED_SWEEP_SENTINEL` fed to the pin | mitigate | A sentinel that neutralises the pin's GATE-06 branch could mask a genuine truncation if the corrected coverage test were skipped or wrong. Coverage is computed FIRST and a truncated result returns INCONCLUSIVE before the pin's verdict is trusted; `_prove` preconditions on `ceiling` and both `y` legs are what make `(0.0, 1.0)` provably bracket both axes, and `20-11` asserts the sentinel neutralises the branch on every committed fixture. |
| T-20-53 | Tampering | a second copy of `wilson_upper_bound`, `F_Y`, `MARGIN_K` or X | mitigate | All imported by object identity, never retyped; X is obtained by calling `mitigation_gate.extraction_ceiling` itself rather than recomputing it. `20-11` asserts `is`-identity across all three modules. |
| T-20-54 | Repudiation | `wilson_lower_bound` used as a deciding statistic on a floor | accept | Defined for REPORTING only, in `rule_of_three`'s register (published alongside, never instead of). Using it to decide Y coverage would re-introduce CR-01's defect class with the sign flipped; the choice and its cost are written into the module docstring and the correction artifact. Risk accepted: a future reader could still misuse it — `COVERAGE_STATISTIC_BY_AXIS` names the deciding statistic per axis so the misuse is contradicted by module data, not only by prose. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_phase20_prereg.py -q` → `18 passed` (the ancestry guard,
  the frozen-pin guard and the behavioural twin are all unaffected).
- `git diff --exit-code -- scripts/mitigation_gate.py scripts/erasure_gate.py` → exit 0.
- `ruff check .` and `ruff format --check .` → clean.
- `.venv/bin/python -m pytest -q` → no regression against the `863 passed, 1 skipped` baseline
  recorded in `20-REVIEW.md`.
</verification>

<success_criteria>
- `scripts/phase20_gate_coverage.py` exists, is unpinned, and is CPU-only stdlib plus two sibling
  scripts.
- Direction (i) → `PASS` and direction (ii) → `INCONCLUSIVE` through `corrected_point_verdict`,
  against the pin's `INCONCLUSIVE` and `FAIL`, on the fixtures named in `<measured_ground_truth>`.
- The borrowed floor `0.06893` cannot reach a verdict through this module, and neither can a raw
  rate handed to `sweep_extraction_successes`.
- The bound-direction resolution is written into the module docstring with its cost named, and both
  divergences from the pin's contract are named in `corrected_point_verdict`'s docstring.
- Neither frozen file changed.
</success_criteria>

<output>
Create `.planning/phases/20-pre-registration-the-three-condition-gate/20-08-SUMMARY.md` when done.
</output>
