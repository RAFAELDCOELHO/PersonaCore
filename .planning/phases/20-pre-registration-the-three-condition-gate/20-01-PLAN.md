---
phase: 20-pre-registration-the-three-condition-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/mitigation_gate.py
  - tests/test_phase20_prereg.py
autonomous: true
requirements: [GATE-01, GATE-07, CAL-04]

must_haves:
  truths:
    - "D-31: `scripts/mitigation_gate.py` declares its OWN `V4_VERDICTS = (\"PASS\", \"FAIL\", \"INCONCLUSIVE\")` and a module-scope `_prove_verdict_domain()` proves equal length, positional correspondence against `erasure_gate.VERDICTS`, and `INCONCLUSIVE` identical in both vocabularies — the gate fails AT IMPORT if the relationship breaks."
    - "D-28: `ARMS = (\"dp\", \"adversarial\")` is closed and a module-scope `_prove` asserts `tuple(ARM_CLAIMS) == ARMS`, so a name with no claim string is a name a later plan must add code for."
    - "D-16 / D-17 / D-18: exactly two chosen constants exist — `F_Y = 0.7` and `F_C = 0.5` — each carrying an inline PREFERENCE label, and both are re-exported as data in `CHOSEN_CONSTANTS` so an audit needs no second hand-maintained copy."
    - "D-19: `K_RUNGS = (48, 24, 16, 8)` is a closed ordered tuple committed now (CAL-04 sentence 1)."
    - "D-01 / D-05 / D-10 / D-15 / D-08 / D-33: the pin's own prose tuple records the (c) supersession, the leg asymmetry with its reason, the ERASURE_GOAL_FRAMING non-transfer, Y's underivability, the strictly-after discipline and the narrow-glob cost — as importable module data, not comments."
    - "D-21: `tests/test_phase20_prereg.py` carries the Phase 18/19 ancestry shape (`git ls-files`, product assert, `bool(checked) == bool(tracked_artifacts)`) — GREEN today at tracked=0, demanding non-zero from the first artifact onward. NEVER the Phase 16 `assert checked` shape."
    - "The guard is armed in the FIRST plan of the phase, before any pin logic exists, so every pin commit from 20-01 onward is watched from the start."
  artifacts:
    - path: "scripts/mitigation_gate.py"
      provides: "The pin's spine — verdict domain, arms, chosen constants, K rungs, decision-rule prose"
      contains: "V4_VERDICTS"
    - path: "tests/test_phase20_prereg.py"
      provides: "The live ancestry guard plus the shared ordering helper both the guard and the D-22 fixture call"
      contains: "test_phase20_prereg_is_frozen_before_every_phase20_result"
  key_links:
    - from: "scripts/mitigation_gate.py"
      to: "scripts/erasure_gate.py"
      via: "flat-module `from erasure_gate import (...)` after a sys.path bootstrap"
      pattern: "from erasure_gate import"
    - from: "tests/test_phase20_prereg.py"
      to: "scripts/mitigation_gate.py"
      via: "PHASE20_PREREG_ARTIFACT fed to `git log --format=%H --`"
      pattern: "PHASE20_PREREG_ARTIFACT"
---

<objective>
Arm the pin and arm the guard, in that order, in the first plan of the phase.

Purpose: Phase 20's entire evidentiary value is ordering. `scripts/erasure_gate.py` earned its weight
by preceding Phase 16; the v4.0 gate must precede every v4.0 number. Arming the ancestry guard in the
same plan that creates the pin's first commit is what makes every subsequent pin commit watched from
the start rather than retro-fitted once there is something to miss — the recorded Phase 19 precedent
(`tests/test_phase16_prereg.py:425-431`).

Output: `scripts/mitigation_gate.py` (spine only — no verdict logic yet) and
`tests/test_phase20_prereg.py` (bootstrap + live ancestry guard, vacuous by construction and
recorded as such).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/20-pre-registration-the-three-condition-gate/20-CONTEXT.md
@.planning/phases/20-pre-registration-the-three-condition-gate/20-PATTERNS.md
@.planning/phases/20-pre-registration-the-three-condition-gate/20-RESEARCH.md
</context>

<interfaces>
<!-- Resolved from the source this session. Every name below exists TODAY. Do not re-derive. -->

Importable from `scripts/erasure_gate.py` (291 lines, one commit `23a830c`, MUST NOT be amended):
- `V20_MASKED_DIALOGUE_VAL_PPL` at `:75` = `4.5733`
- `V20_EWC_RETENTION_PPL` at `:76` = `3.891140`
- `V20_RETENTION_NOISE_FLOOR` at `:77` = `0.068930`  — **D-06 supersedes for v4.0; do NOT import**
- `MARGIN_K` at `:86` = `2`
- `wilson_upper_bound(successes, n, z=_Z_ONE_SIDED_95) -> float` at `:139`
- `rule_of_three(n) -> float` at `:161`
- `VERDICTS` at `:136` = `("SUCCESS", "FAILURE", "INCONCLUSIVE")` — **WRONG DOMAIN, must not become
  this module's domain; referenced only inside `_prove_verdict_domain()`**

Names this plan CREATES and every later wave must spell identically:
- `scripts/mitigation_gate.py`: `_REPO_ROOT`, `_prove`, `V4_VERDICTS`, `_VERDICT_RELABEL`,
  `_prove_verdict_domain`, `ARMS`, `ARM_CLAIMS`, `F_Y`, `F_C`, `CHOSEN_CONSTANTS`, `K_RUNGS`,
  `MITIGATION_DECISION_RULE`, `MITIGATION_GOAL_FRAMING`
- `tests/test_phase20_prereg.py`: `_ROOT`, `_git`, `PHASE20_PREREG_ARTIFACT`, `V4_ARTIFACT_GLOBS`,
  `_assert_ordering_holds`, `test_phase20_prereg_is_frozen_before_every_phase20_result`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: The pin's import surface, loud-refusal helper, verdict domain and arm identity</name>
  <files>scripts/mitigation_gate.py</files>
  <read_first>
    - `scripts/erasure_gate.py:60-95` — the module docstring register and the baselines banner at
      `:70-74` (the nearest self-statement of non-amendment; it is scoped to the baselines block
      ONLY, not the whole file). Also `:136` for `VERDICTS`.
    - `scripts/_addendum.py:43-53` — the exact `sys.path` bootstrap idiom (`_REPO_ROOT`, the
      `if str(...) not in sys.path` guard, the `# noqa: E402` comment) AND the phase-neutral
      `_prove` shape that raises `SystemExit(f"[_addendum] {message}")`.
    - `scripts/phase19_erasure.py:164-173` — the canonical `_prove` docstring explaining why it is
      `SystemExit` and deliberately not `assert` (an `assert` is strippable under `-O`).
    - `scripts/phase19_erasure.py:3878-3883` — the module-scope `_prove` invocation over a closed
      tuple (`tuple(_SUBCOMMAND_TABLE) == SUBCOMMANDS`). Copy its message register, including the
      closing clause that invokes the ancestry guard as the deterrent.
    - `20-PATTERNS.md` §1a, §1g — the import statement shape and the correction that
      `scripts/erasure_gate.py` contains ZERO `_prove` (`grep -c` → 0). `_prove` is a DRIVER
      convention imported from outside the template.
  </read_first>
  <action>
Create `scripts/mitigation_gate.py`.

Module docstring: state that this is the v4.0 three-condition pre-registration, that it is CLOSED
once any `results/phase20_*` artifact exists, and that corrections after that point are dated
continuations via `scripts/_addendum.py` (D-24), never edits — because `tests/test_phase20_prereg.py`
takes `adds[-1]`, the EARLIEST add, so a delete-and-re-add cycle cannot launder the ordering.

Bootstrap: `import pathlib`, `import sys`, then `_REPO_ROOT = pathlib.Path(__file__).resolve().parent.parent`
and the `if str(_REPO_ROOT / "scripts") not in sys.path: sys.path.insert(0, ...)` guard, copied from
`scripts/_addendum.py:43-45`.

Imports, each carrying `# noqa: E402` because they follow a statement:
`from erasure_gate import (MARGIN_K, V20_EWC_RETENTION_PPL, V20_MASKED_DIALOGUE_VAL_PPL, rule_of_three, wilson_upper_bound)`
using the FLAT module name `erasure_gate`, not `scripts.erasure_gate` — `tests/test_phase16_stats.py:396`
matches on `node.module == "erasure_gate"` and later waves' AST guard inherits that expectation.
Also `import erasure_gate` as a module handle, needed only by `_prove_verdict_domain()`.
`V20_RETENTION_NOISE_FLOOR` is NOT imported: D-06 supersedes it for v4.0 and the v4.0 retention floor
arrives as a required kwarg (D-07). `erasure_gate.VERDICTS` is NOT imported into this module's
namespace: D-31 / RESEARCH L2 — it is the wrong domain.

Define `_prove(condition, message)` raising `SystemExit(f"[mitigation_gate] {message}")`, mirroring
`scripts/_addendum.py:50-53` with this module's own prefix. Docstring must say `SystemExit`, never
`assert`, because an `assert` is strippable under `-O`.

Define `V4_VERDICTS = ("PASS", "FAIL", "INCONCLUSIVE")` — this module's own tuple, per D-31 and
GATE-01. `REQUIREMENTS.md:27` is authoritative over `ROADMAP.md:172`'s `FAILURE` slip.

Define `_VERDICT_RELABEL` as an explicit mapping from `erasure_gate.VERDICTS`'s three names to
`V4_VERDICTS`'s three names: `"SUCCESS" -> "PASS"`, `"FAILURE" -> "FAIL"`,
`"INCONCLUSIVE" -> "INCONCLUSIVE"`.

Define `_prove_verdict_domain()` and CALL IT AT MODULE SCOPE. It must `_prove` three separate
things, each with its own message (D-31): (1) `len(V4_VERDICTS) == len(erasure_gate.VERDICTS)`;
(2) positional correspondence — for each index i, `_VERDICT_RELABEL[erasure_gate.VERDICTS[i]] == V4_VERDICTS[i]`;
(3) `"INCONCLUSIVE"` is present at the SAME index in both tuples and spelled identically. The
docstring must state that the discipline is kept by PROVING the relationship, not by asserting it in
a comment a refactor can break, and that a dead gate must fail at import rather than after the
compute it would waste.

Define `ARMS = ("dp", "adversarial")` and `ARM_CLAIMS` as a dict keyed by those two names. The `dp`
claim string states a FORMAL guarantee (a mathematical property of the DP-SGD mechanism); the
`adversarial` claim string states explicitly that it is NOT a formal guarantee. Then invoke
`_prove(tuple(ARM_CLAIMS) == ARMS, ...)` at MODULE SCOPE, with a message in the
`phase19_erasure.py:3878-3883` register — the published set and the runnable set must be ONE set; a
name with no claim string is a name a later plan would have to add code for, which is a commit to
this file.
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/python -c "import sys; sys.path.insert(0,'scripts'); import mitigation_gate as m; assert m.V4_VERDICTS == ('PASS','FAIL','INCONCLUSIVE'), m.V4_VERDICTS; assert m.ARMS == ('dp','adversarial'), m.ARMS; assert tuple(m.ARM_CLAIMS) == m.ARMS; import erasure_gate as e; assert m.wilson_upper_bound is e.wilson_upper_bound; assert m.MARGIN_K is e.MARGIN_K; assert not hasattr(m,'VERDICTS') or m.VERDICTS is not e.VERDICTS; print('OK')"</automated>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/ruff check scripts/mitigation_gate.py && .venv/bin/ruff format --check scripts/mitigation_gate.py</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/mitigation_gate.py` exists and `.venv/bin/python -c "import sys; sys.path.insert(0,'scripts'); import mitigation_gate"` exits 0 — proving `_prove_verdict_domain()` and the `ARM_CLAIMS` proof both pass at import.
    - The source contains the exact literal `V4_VERDICTS = ("PASS", "FAIL", "INCONCLUSIVE")`.
    - The source contains the exact literal `ARMS = ("dp", "adversarial")`.
    - `.venv/bin/python -c "import ast;t=ast.parse(open('scripts/mitigation_gate.py').read());print(sorted({a.name for n in ast.walk(t) if isinstance(n,ast.ImportFrom) and n.module=='erasure_gate' for a in n.names}))"` prints exactly `['MARGIN_K', 'V20_EWC_RETENTION_PPL', 'V20_MASKED_DIALOGUE_VAL_PPL', 'rule_of_three', 'wilson_upper_bound']` — `VERDICTS` and `V20_RETENTION_NOISE_FLOOR` are absent.
    - `mitigation_gate.wilson_upper_bound is erasure_gate.wilson_upper_bound` is `True` (object identity, `is` not `==`).
    - Mutation check, run and then reverted byte-identically: temporarily change `_VERDICT_RELABEL["FAILURE"]` to `"FAILURE"` and confirm `.venv/bin/python -c "...import mitigation_gate"` exits non-zero with a `[mitigation_gate]` message. Record the sha256 of the file before and after the revert and confirm they match.
    - `.venv/bin/ruff check scripts/mitigation_gate.py` and `.venv/bin/ruff format --check scripts/mitigation_gate.py` both exit 0.
  </acceptance_criteria>
  <done>The pin imports cleanly, its verdict domain is `PASS`/`FAIL`/`INCONCLUSIVE` proved against `erasure_gate.VERDICTS` at import, its arm set is closed and proved against its claim table, and the relabel proof has been watched failing and restored byte-identically.</done>
</task>

<task type="auto">
  <name>Task 2: The two chosen constants, the K rung menu, and the decision-rule prose record</name>
  <files>scripts/mitigation_gate.py</files>
  <read_first>
    - `scripts/erasure_gate.py:70-74` — the baselines banner comment block, and `:75-79` for the
      inline provenance comment convention (`V20_MASKED_DIALOGUE_VAL_PPL = 4.5733  # Phase 12 ...`).
    - `scripts/erasure_gate.py:95-127` — `ERASURE_DECISION_RULE`, a 6-element tuple of long clause
      strings. This is the shape the D-01/D-05/D-10 records must take: importable module data,
      greppable, NOT comments.
    - `scripts/erasure_gate.py:130-134` — `ERASURE_GOAL_FRAMING`, the single string D-10 explains
      does not transfer.
    - `scripts/phase19_floor.py:54-75` and `:167-175` — the aligned `input: / rule: / bound by: /
      evidence:` comment block per locked constant, and `EVIDENCE_ARTIFACT` keeping the
      constant-to-artifact pairing AS DATA so a test can assert it without a second hand-maintained
      copy (Shared Pattern S7).
    - `results/phase19_erasure_report.md:446-450` — the exact finding D-01 supersedes, and `:453-457`
      for the boundary the pin must respect (`23a830c` was NOT wrong and must NOT be amended).
    - `.planning/REQUIREMENTS.md:131-136` — CAL-04's three-part obligation and the
      `phase18_extraction.py:84-93` ATK-03/P18-4 record it rests on.
  </read_first>
  <action>
Append to `scripts/mitigation_gate.py`.

`F_Y = 0.7` and `F_C = 0.5`, each on its own line with an inline comment in the
`erasure_gate.py:75-79` provenance register — except that where those constants cite an artifact
path, these two cite **PREFERENCE**. `F_Y`'s comment records D-15's reason it cannot be derived (a
k=2-derived Y would demand statistical indistinguishability from the un-mitigated control, making the
gate vacuous in the opposite direction) and D-16's rule that the same fraction applies to BOTH legs
against each leg's OWN retrained-control value, which reproduces the generalization ratio for free
and never borrows v2.0's `0.4921 / 0.3483` (GATE-04 forbids it). `F_C`'s comment records D-17: it is
SEPARATE from `F_Y` because (c) is a catastrophe detector and Y is the utility target, and the
measured hard non-vacuity floor is `f_C > 0.2237` (M1 retained `0.22362988653603388` of the dialogue
gap), so `0.5` sits `2.24x` above it — real margin, not glued to the floor.

`CHOSEN_CONSTANTS = {"F_Y": F_Y, "F_C": F_C}` — the D-18 audit surface kept AS DATA
(`phase19_floor.py:167-175` register), so a reviewer and a test both find exactly two numbers to
argue with without a second hand-maintained list.

`K_RUNGS = (48, 24, 16, 8)` with a comment recording D-19: a closed ordered menu committed NOW
(CAL-04 sentence 1); Phase 23 selects the rung by measured throughput (CAL-05 — 4.77 h/point is a
FLOOR for noised points, not a mean); and a selected rung may only INCREASE, never decrease, which
structurally eliminates the post-null K reduction `scripts/phase18_extraction.py:84-93` records as
the ATK-03 / P18-4 weakening.

`MITIGATION_DECISION_RULE` — a tuple of long clause strings in the `ERASURE_DECISION_RULE:95-127`
shape. It must carry, one clause each and each naming its decision id:
  - D-01: condition (c)'s dialogue leg is a BAND ON THE ON-OFF ADAPTATION GAP, not a one-sided raw
    perplexity cap, and this SUPERSEDES the form GATE-02 / `ROADMAP.md:163-167` inherit from
    `.planning/research/SUMMARY.md` R5. State the measured reason: against the literal cap
    `4.5837288963367` the untouched taught adapter reads `5.815445876712191` and fails by
    `+1.231717` before any mitigation runs, while M1 — which destroyed `77.637%` of the adaptation —
    reads `4.851119149910443` and fails by only `+0.267390`, so retaining that cap as an upper bound
    SELECTS FOR DESTRUCTION.
  - The boundary, stated explicitly: this is NOT a claim that `23a830c` was wrong, and NOT a licence
    to amend it. `results/phase19_erasure_report.md:453-457` and `docs/REPORT.md:1215`.
  - D-05: the two legs of (c) are ASYMMETRIC BY DESIGN — dialogue is a band, retention is a one-sided
    upper cap — because the dialogue gap is sign-stable while the retention gap changes sign inside
    the already-measured range (taught `+0.3286199167186572`, M1 `-0.22022225029414155`), which makes
    a symmetric band geometrically incoherent for the retention leg specifically. Recorded so a later
    "unify the two legs" refactor goes red rather than looking like cleanup.
  - D-15 / D-18: exactly TWO chosen constants exist in this pin, `F_Y` and `F_C`, both labelled
    milestone PREFERENCE rather than derivation. Everything else is measured, imported or computed.
  - D-08: `results/phase20_retention_floor.json` lands STRICTLY AFTER this file's first commit. State
    that this is TIGHTER than the mechanism requires — `git merge-base --is-ancestor` is reflexive,
    so gate and artifact in the same commit would pass — and that it is recorded as a discipline so a
    later reader treats same-commit as neither a defect nor a licence.
  - D-33: `V4_ARTIFACT_GLOBS` carries `phase20_*` ONLY. Each of Phases 21-28 adds its own prefix at
    the moment it first writes results. The cost is named, not hidden: an `assert` catches an empty
    match set, never an incomplete one, so a future phase that forgets its prefix fails silently.
    That risk is ACCEPTED in exchange for never asserting coverage this phase cannot demonstrate.

`MITIGATION_GOAL_FRAMING` — a single string in the `ERASURE_GOAL_FRAMING:130-134` shape carrying
D-10: `erasure_gate`'s rejection of "indistinguishable from never-having-learned" does NOT transfer
here, and the reason is that the rejection targeted an unverifiable post-hoc assertion about an
already-observed erasure result, whereas v4.0's claim is a mathematical guarantee built into the
DP-SGD mechanism. Written so a reader does not misread the v4.0 X ceiling as reopening a claim this
repository committed against.

Do NOT write the figure "25x" anywhere. `20-RESEARCH.md` §6 could not reproduce it under any reading
(candidates 28.41x, 12.69x, 17.06x); the defensible statement is "25 of 104 questions".
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/python -c "import sys; sys.path.insert(0,'scripts'); import mitigation_gate as m; assert m.F_Y == 0.7; assert m.F_C == 0.5; assert m.CHOSEN_CONSTANTS == {'F_Y':0.7,'F_C':0.5}, m.CHOSEN_CONSTANTS; assert m.K_RUNGS == (48,24,16,8), m.K_RUNGS; assert len(m.MITIGATION_DECISION_RULE) >= 6; body=' '.join(m.MITIGATION_DECISION_RULE)+' '+m.MITIGATION_GOAL_FRAMING; [__import__('sys').exit('missing '+d) for d in ('D-01','D-05','D-08','D-10','D-15','D-18','D-33') if d not in body]; assert '25x' not in body and '25×' not in body; print('OK')"</automated>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/ruff check scripts/mitigation_gate.py && .venv/bin/ruff format --check scripts/mitigation_gate.py</automated>
  </verify>
  <acceptance_criteria>
    - `mitigation_gate.F_Y == 0.7` and `mitigation_gate.F_C == 0.5`.
    - `mitigation_gate.CHOSEN_CONSTANTS == {"F_Y": 0.7, "F_C": 0.5}` — exactly two entries.
    - `mitigation_gate.K_RUNGS == (48, 24, 16, 8)`.
    - The concatenation of `MITIGATION_DECISION_RULE` and `MITIGATION_GOAL_FRAMING` contains each of the literal substrings `D-01`, `D-05`, `D-08`, `D-10`, `D-15`, `D-18`, `D-33`.
    - That same concatenation contains the literal `4.851119149910443` and the literal `0.22362988653603388`, and does NOT contain `25x` or `25×`.
    - `.venv/bin/python -c "import ast;t=ast.parse(open('scripts/mitigation_gate.py').read());print(sorted({n.value for n in ast.walk(t) if isinstance(n,ast.Constant) and isinstance(n.value,float)}))"` prints exactly `[0.5, 0.7]`.
    - `.venv/bin/ruff check .` and `.venv/bin/ruff format --check .` both exit 0.
  </acceptance_criteria>
  <done>The two chosen constants are declared, labelled PREFERENCE, and re-exported as data; the K rung menu is committed; and the pin's own prose carries the D-01 supersession, the D-05 asymmetry reason, the D-10 non-transfer, the D-08 strictly-after discipline and the D-33 accepted cost as importable module data.</done>
</task>

<task type="auto">
  <name>Task 3: Arm the ancestry guard — Phase 18/19 shape, vacuous by construction and recorded as such</name>
  <files>tests/test_phase20_prereg.py</files>
  <read_first>
    - `tests/test_phase16_prereg.py:406-497` — **THE shape to copy.** `test_phase19_prereg_is_frozen_before_every_phase19_result`,
      the Phase 19 twin. Read the whole function: the glob-membership assert at `:444`, the
      shallow-clone assert at `:451`, `assert prereg_commits` at `:458`, `git ls-files` at `:464`,
      the `adds[-1]` comment, the PRODUCT assert at `:480`, the comment at `:486-491` and the
      EQUIVALENCE assert at `:492`. Its docstring at `:425-431` states Phase 20's phase-zero
      situation verbatim.
    - `tests/test_phase16_prereg.py:176-213` — **THE shape that must NOT be copied** (D-21).
      `assert checked` at `:209` is unconditional, so it is RED from the pin's first commit until an
      artifact lands, inverting the ordering this phase exists to establish. It also pins a
      hand-written SHA, which `:243-247` explains permits the exact post-hoc edit STAT-05 forbids.
    - `tests/test_phase16_prereg.py:46-63` — the `PREREG_COMMIT` / `V3_ARTIFACT_GLOBS` /
      `PREREG_ARTIFACT` declaration shape. The comments are load-bearing.
    - `tests/test_phase16_prereg.py:166-173` — `_ROOT` and `_git`, to be copied verbatim.
    - `tests/test_phase18_prereg.py:40-54` — the test-module import bootstrap.
    - `20-PATTERNS.md` §3a, §3b, §3c and Shared Pattern S5 (recorded-vacuity docstring).
  </read_first>
  <action>
Create `tests/test_phase20_prereg.py`.

Module docstring: PREREG for v4.0. State that this file is CPU-only and GPU-free, that the ordering
question is answered against git's object DAG and never against committer dates
(`tests/test_phase16_prereg.py:11-17` gives the reasoning), and that the guard below is VACUOUS TODAY
BY CONSTRUCTION — a recorded state rather than a hidden one — because plan 20-01 arms it before any
`results/phase20_*` artifact exists (Shared Pattern S5).

Imports: `ast`, `pathlib`, `subprocess`, `sys`. Copy `_ROOT` and `_git` from
`tests/test_phase16_prereg.py:166-173`, with ONE deliberate additive widening: `_git` gains a
keyword-only `cwd=_ROOT` parameter so the same implementation serves both the live guard and the
D-22 throwaway-repo fixture in plan 20-03. Record in its docstring that this is the
import-never-copy discipline — one implementation parameterized, not two that can drift. Keep the
argv tuple; never `shell=True`.

`PHASE20_PREREG_ARTIFACT = "scripts/mitigation_gate.py"`. No `PREREG_COMMIT` constant: the Phase
18/19 shape derives the pre-registration side from history (`git log --format=%H --`) and is
self-identifying, so there is no pin SHA to get wrong and no separate identity test is needed
(`20-PATTERNS.md` §3a table).

`V4_ARTIFACT_GLOBS = ("results/phase20_*",)` with a comment in the `:51-55` register that states the
D-33 reasoning EXPLICITLY — only `phase20_*` can be proven RED-then-GREEN by this phase's fixture, an
advance declaration without demonstration is the kind of unproven assertion this phase exists to
refuse, each of Phases 21-28 adds its own prefix when it first writes results, and the accepted cost
is that an `assert` catches an empty match set but never an incomplete one. Without that comment a
reader will read the one-element tuple as stale rather than deliberately narrow.

Define `_assert_ordering_holds(*, root, prereg_artifact, artifact_glob, globs)` carrying the body of
`tests/test_phase16_prereg.py:443-497` verbatim in structure, parameterized on `root` so the live
test and the D-22 fixture call ONE implementation: the glob-membership assert against `globs`, the
shallow-clone assert, `assert prereg_commits` (message naming `PHASE20_PREREG_ARTIFACT` and "Plan
20-01 Task 1 commits it"), `git ls-files` for tracked artifacts, the per-artifact
`adds[-1]` earliest-add loop with the two-line comment copied verbatim, `subprocess.run(("git",
"merge-base", "--is-ancestor", prereg, first_add), cwd=root, check=True)`, the PRODUCT assert, the
`0 == n * 0` explanatory comment rewritten for Phase 20, and the EQUIVALENCE assert
`bool(checked) == bool(tracked_artifacts)`.

Define `test_phase20_prereg_is_frozen_before_every_phase20_result()` which calls
`_assert_ordering_holds` with `root=_ROOT`, `prereg_artifact=PHASE20_PREREG_ARTIFACT`,
`artifact_glob="results/phase20_*"`, `globs=V4_ARTIFACT_GLOBS`. Its docstring adapts
`:425-431`: the pin is armed in plan 20-01, deliberately BEFORE any `results/phase20_*` artifact
exists; arming the guard first is the point — every pin commit from 20-01 onward is watched from the
start instead of being retro-fitted once there is something to miss.

Do NOT add the D-22 fixture or any AST guard here; plans 20-03 and 20-06 own those.
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/python -m pytest -q tests/test_phase20_prereg.py</automated>
    <automated>cd /Users/juliorcoelho/PersonaCore && git ls-files 'results/phase20_*' | wc -l | tr -d ' ' | grep -qx 0 && git log --diff-filter=A --format=%H -- 'results/phase20_*' | wc -l | tr -d ' ' | grep -qx 0 && echo "history clean"</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest -q tests/test_phase20_prereg.py` exits 0.
    - `grep -c "ls-files" tests/test_phase20_prereg.py` is at least 1 and `.venv/bin/python -c "import ast;src=open('tests/test_phase20_prereg.py').read();print('_ROOT.glob' in src)"` prints `False` — the Phase 16 working-tree-glob shape is absent.
    - The source contains the exact literal `V4_ARTIFACT_GLOBS = (` followed by exactly one entry, `"results/phase20_*"`, and contains the literal `D-33`.
    - The source contains the exact literal `PHASE20_PREREG_ARTIFACT = "scripts/mitigation_gate.py"`.
    - The source contains `bool(checked) == bool(tracked_artifacts)` and does NOT contain a bare `assert checked,` statement.
    - `git ls-files 'results/phase20_*'` prints nothing and `git log --diff-filter=A -- 'results/phase20_*'` prints nothing — the D-22 clean-history precondition still holds after this plan.
    - Watched RED and restored byte-identically: temporarily point `PHASE20_PREREG_ARTIFACT` at a path with no commits, confirm pytest fails on the `assert prereg_commits` message, then revert and confirm the file's sha256 matches its pre-mutation value.
    - `.venv/bin/python -m pytest -q` (full suite) exits 0.
  </acceptance_criteria>
  <done>The live ancestry guard is committed and GREEN at tracked=0, built on the Phase 18/19 shape via a single shared `_assert_ordering_holds` implementation, with the D-33 narrow-glob reasoning recorded beside the tuple and the vacuity recorded in the test's own docstring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git object DAG ↔ the evidentiary record | The only boundary that matters this phase. A rule's authority comes from provably preceding the numbers it judges; everything downstream trusts that ordering. |
| `erasure_gate.py` (`23a830c`, closed) ↔ `mitigation_gate.py` (new) | v3.0's closed pin is an immutable input. Any write across this boundary voids the milestone. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Repudiation | `results/phase20_*` vs `scripts/mitigation_gate.py` commit order | mitigate | `_assert_ordering_holds` armed in Task 3 BEFORE any pin logic exists; `bool(checked) == bool(tracked_artifacts)` keeps it green at tracked=0 and demands non-zero from the first artifact onward |
| T-20-02 | Tampering | `scripts/erasure_gate.py` | mitigate | No task in this plan writes to it. `tests/test_phase18_prereg.py:212` asserts `git log == [PREREG_COMMIT]` and byte-identity against `git show`; the full-suite acceptance command runs it |
| T-20-03 | Tampering | `V4_VERDICTS` / `_VERDICT_RELABEL` | mitigate | `_prove_verdict_domain()` runs at MODULE SCOPE and raises `SystemExit`, not `assert` (unstrippable under `-O`); watched failing and restored byte-identically |
| T-20-04 | Tampering | `ARM_CLAIMS` drifting from `ARMS` | mitigate | Module-scope `_prove(tuple(ARM_CLAIMS) == ARMS, ...)` — an added arm name with no claim string aborts at import |
| T-20-05 | Information disclosure | none | accept | No network, no secrets, no untrusted input. `_git` uses an argv tuple, never `shell=True`, so a glob containing a shell metacharacter is not an injection point |
| T-20-06 | Elevation of privilege | future-phase prefixes absent from `V4_ARTIFACT_GLOBS` | accept | D-33, explicitly. An `assert` catches an empty match set, never an incomplete one. Accepted in exchange for never asserting coverage this phase cannot demonstrate; the acceptance is recorded in the tuple's own comment and in `MITIGATION_DECISION_RULE` |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q` exits 0 (full CPU-only suite, includes `tests/test_package.py`'s
  `pyproject.toml` sha256 pin — RPT-03).
- `.venv/bin/ruff check . && .venv/bin/ruff format --check .` exit 0.
- `git status --porcelain pyproject.toml` prints nothing.
- `git log --diff-filter=A -- 'results/phase20_*'` prints nothing.
</verification>

<success_criteria>
- `scripts/mitigation_gate.py` exists, imports cleanly, and carries `V4_VERDICTS`, `ARMS`,
  `ARM_CLAIMS`, `F_Y`, `F_C`, `CHOSEN_CONSTANTS`, `K_RUNGS`, `MITIGATION_DECISION_RULE`,
  `MITIGATION_GOAL_FRAMING`.
- Its only module-scope float literals are `0.7` and `0.5`.
- `tests/test_phase20_prereg.py` exists and is GREEN with `tracked_artifacts == 0`.
- Two guards have been watched RED and restored byte-identically (the verdict relabel proof, the
  `assert prereg_commits` branch).
- `pyproject.toml` is byte-unchanged.
</success_criteria>

<output>
Create `.planning/phases/20-pre-registration-the-three-condition-gate/20-01-SUMMARY.md` when done.
Record: the sha256 of `scripts/mitigation_gate.py` at commit; the two watched-RED observations with
both sha256 values each; and re-confirm `git log --diff-filter=A -- 'results/phase20_*'` is empty.
</output>
