---
phase: 20-pre-registration-the-three-condition-gate
plan: 06
type: execute
wave: 5
depends_on: ["20-03", "20-05"]
files_modified:
  - tests/test_phase20_prereg.py
  - .planning/phases/20-pre-registration-the-three-condition-gate/20-VALIDATION.md
autonomous: true
requirements: [GATE-01, GATE-02, GATE-03, GATE-04, GATE-05, GATE-06, GATE-07, GATE-08, GATE-09, GATE-10, CAL-04, RPT-02]

must_haves:
  truths:
    - "D-20: an AST guard proves `scripts/mitigation_gate.py` never imports `scripts/mitigation_budget.py`, covering both `import mitigation_budget` and `from mitigation_budget import ...` because both feed the same `imported` set. The gate/budget split is a fact about the import graph, not a paragraph."
    - "The AST register is the HYBRID `20-PATTERNS.md` §3d specifies — an explicit path constant PLUS a `scripts/mitigation_*.py` glob with `_collapsed_glob_guard()` and a membership assert — because `mitigation_gate.py` matches no `phase20_*.py` glob, so a pure glob register would scan nothing and a pure hand-listed tuple would reintroduce the F-08 blindness for Phase 23's `mitigation_budget.py`."
    - "RPT-03: the gate is stdlib-only. `imported` is asserted as a SUBSET of a named allow-set, which is strictly stronger than listing forbidden names, and `scipy`/`numpy`/`torch` are separately asserted absent."
    - "GATE-02 / D-09: `wilson_upper_bound` and `rule_of_three` are IMPORTED, proved both statically (present in the `erasure_gate` import, absent from the module's own `FunctionDef` names) and at runtime by object identity with `is`, never `==` — a value-matching copy is a copy free to stop matching."
    - "GATE-02: no imported baseline is retyped — `4.5733`, `3.891140`, `0.068930` and `0.005214448168350039` appear as no numeric constant in the module."
    - "GATE-04: `0.4921` and `0.3483` appear nowhere, and `V20_TAUGHT_RECALL` / `V20_HELDOUT_RECALL` are never imported — Y is a fraction of the retrained control, never derived from v2.0's published numbers."
    - "D-18: exactly two chosen constants. The module-scope float literals outside `FIXTURE_*` assignments are exactly `{0.7, 0.5}` and equal `set(CHOSEN_CONSTANTS.values())` — a reviewer auditing the pin finds exactly two numbers to argue with."
    - "D-29: no `provisional` identifier, attribute or string exists anywhere in the module — the verdict domain stays exactly three."
    - "GATE-01: every public function in the pin is keyword-only with zero defaults, proved by an AST scan over ALL of them, not just the verdict function."
    - "GATE-09: `__main__` is not collected by pytest, so a self-check nobody runs in CI is not a guard — a pytest twin runs `scripts/mitigation_gate.py` as a subprocess AND re-asserts all six outcomes against the SAME module-scope fixtures the self-check uses."
  artifacts:
    - path: "tests/test_phase20_prereg.py"
      provides: "The static register, the constant audits and the behavioural twin over the closed pin"
      contains: "_GATE_MODULES"
  key_links:
    - from: "tests/test_phase20_prereg.py"
      to: "scripts/mitigation_gate.py fixtures"
      via: "direct import of FIXTURE_CLEARING_POINT / FIXTURE_DESTROYED_MODEL / FIXTURE_TRUNCATED_SWEEP"
      pattern: "FIXTURE_DESTROYED_MODEL"
    - from: "tests/test_phase20_prereg.py"
      to: "scripts/mitigation_gate.py __main__"
      via: "subprocess run of the module, exit code asserted"
      pattern: "scripts/mitigation_gate.py"
---

<objective>
Put the closed pin under static and behavioural guard: prove the import graph, prove exactly two
chosen constants, prove no baseline was retyped, and re-run every verdict branch in CI.

Purpose: `__main__` is not collected by pytest, so the GATE-09 self-check runs only when someone runs
it. And the disciplines this pin rests on — stdlib-only, import-never-retype, gate-never-imports-budget,
exactly-two-chosen-constants — are all invisible to a behavioural test. Each needs its own instrument.

Output: the static register, four constant audits and the behavioural twin appended to
`tests/test_phase20_prereg.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-pre-registration-the-three-condition-gate/20-CONTEXT.md
@.planning/phases/20-pre-registration-the-three-condition-gate/20-PATTERNS.md
@.planning/phases/20-pre-registration-the-three-condition-gate/20-RESEARCH.md
@.planning/phases/20-pre-registration-the-three-condition-gate/20-05-SUMMARY.md
</context>

<interfaces>
<!-- Already in tests/test_phase20_prereg.py after plans 20-01 and 20-03. Spell exactly. -->
`_ROOT`, `_git(*args, cwd=_ROOT)`, `PHASE20_PREREG_ARTIFACT`, `V4_ARTIFACT_GLOBS`,
`_assert_ordering_holds`, `_PROSE_PATH`, `WRAPPED_INCIDENT_TEXT`, `WRAPPED_INCIDENT_PHRASE`.

IMPORT ACCUMULATION (20-01's ledger, authoritative): the test module currently imports exactly
`_prose`, `ast`, `pathlib`, `pytest`, `subprocess`, `sys`. **Task 1 of THIS plan adds `fnmatch`,
`erasure_gate` and `mitigation_gate`; Task 2 adds `json`.** `pyproject.toml:44` sets
`select = ["E", "F", "W", "I"]`, so a name imported ahead of its consumer is an F401 and every task
here gates on `ruff check .` exiting 0.

Already in `scripts/mitigation_gate.py` after plan 20-05 (the pin is CLOSED):
`_prove`, `V4_VERDICTS`, `_VERDICT_RELABEL`, `ARMS`, `ARM_CLAIMS`, `F_Y`, `F_C`,
`CHOSEN_CONSTANTS`, `superseded_dialogue_cap`, `K_RUNGS`, `MITIGATION_DECISION_RULE`,
`MITIGATION_GOAL_FRAMING`,
`NEVER_TAUGHT_ARM`, `EXTRACTION_FLOOR_MIN_SEEDS`, `EXTRACTION_FLOOR_PROVENANCE_KEYS`,
`REPLICATION_PENDING_MARKER`, `MECHANISM_KEYS`, `CAPACITY_BRANCHES`, `extraction_ceiling`,
`tolerance_report`, `dialogue_gap_band`, `retention_cap`, `mitigation_point_verdict`,
`exists_clearing_point`, `ratchet_k`, `promote_to_full_fidelity`, `capacity_comparison`,
`FIXTURE_CLEARING_POINT`, `FIXTURE_DESTROYED_MODEL`, `FIXTURE_TRUNCATED_SWEEP`.

Names this plan CREATES in the test module: `_MITIGATION_GATE_PATH`, `_GATE_MODULES`,
`_collapsed_glob_guard`, `_tree`, `_enclosing_functions`, `_module_scope_floats`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: The hybrid AST register and the import-graph guards</name>
  <files>tests/test_phase20_prereg.py</files>
  <read_first>
    - `20-PATTERNS.md` §3d — **the one place no analog fits cleanly.** Read the whole section: Form A
      (the glob register at `tests/test_phase18_prereg.py:57-71`, twin at
      `tests/test_phase17_stats.py:62`), Form B (the hand-listed tuple at
      `tests/test_phase16_stats.py:747`, which is the F-08 blindness Form A exists to close), and the
      required hybrid.
    - `tests/test_phase18_prereg.py:33-37` — the stated purpose of the glob register: "every driver a
      later plan adds enters these scans the moment it exists".
    - `tests/test_phase18_prereg.py:66-71` — `_collapsed_glob_guard()` verbatim: a glob that stops
      matching makes every scan green over nothing.
    - `tests/test_phase16_stats.py:386-411` — **the direct template** for the import scan: the
      `ast.Import` / `ast.ImportFrom` walk populating `imported` and `from_erasure_gate`, the
      `"scipy" not in imported` assertions, the `{"wilson_upper_bound", "rule_of_three"} <= from_erasure_gate`
      assertion, and the `defined` FunctionDef-name check with its "import the instrument, never copy
      it" message.
    - `tests/test_phase19_erasure.py:745-748` and `:1307-1308` — the RUNTIME identity assertions,
      using `is` and not `==`, with the reason recorded at `:2149`.
    - `tests/test_phase18_prereg.py:82-102` — `_tree(path)` and `_enclosing_functions(tree)` to copy.
  </read_first>
  <action>
Append `import fnmatch`, `import erasure_gate` and `import mitigation_gate` (the latter two after
the existing `sys.path` insert, each carrying `# noqa: E402`) — this task is the first consumer of
all three. Then append to `tests/test_phase20_prereg.py`.

Declare the HYBRID register exactly as `20-PATTERNS.md` §3d specifies:
`_MITIGATION_GATE_PATH = _ROOT / "scripts" / "mitigation_gate.py"` and
`_GATE_MODULES = tuple(sorted((_ROOT / "scripts").glob("mitigation_*.py")))`.
A comment must state WHY both exist: `scripts/mitigation_gate.py` matches no `phase20_*.py` glob, so
the repo's established glob register would scan nothing here; and a hand-listed tuple alone would
reintroduce the F-08 blindness for Phase 23's `scripts/mitigation_budget.py`, which must enter these
scans the moment it exists.

Copy `_collapsed_glob_guard()` from `tests/test_phase18_prereg.py:66-71`, adapted to the
`mitigation_*.py` glob and its own message. Copy BOTH `_tree(path)` from
`tests/test_phase18_prereg.py:82-84` AND `_enclosing_functions(tree)` from `:86-102`, verbatim.
`_enclosing_functions` has no consumer until Task 2's `_module_scope_floats`, but a module-level
`def` is not an F401, so it lands here beside `_tree` rather than being split across tasks.

Write `test_mitigation_gate_import_graph_is_stdlib_and_erasure_gate_only()` on the
`tests/test_phase16_stats.py:386-411` template. It must:
  - call `_collapsed_glob_guard()` first, then assert `_MITIGATION_GATE_PATH in _GATE_MODULES` (the
    `tests/test_phase18_prereg.py:497` membership idiom) so the constant and the glob cannot drift
    into naming two different files;
  - walk every module in `_GATE_MODULES`, populating `imported` from both `ast.Import` and
    `ast.ImportFrom` (splitting on `"."` and taking the head), and `from_erasure_gate` from
    `ImportFrom` nodes whose `node.module == "erasure_gate"` — the FLAT module name, matching
    `tests/test_phase16_stats.py:396`'s expectation;
  - assert `"mitigation_budget" not in imported` (D-20) with a message stating that the gate holds
    OUTCOME thresholds and the budget holds RESOURCE parameters, that `ROADMAP.md:139-144` requires
    the separation to be structurally enforced so a reader cannot mistake a resource calibration for
    an outcome-threshold peek, and that K reaches the gate as a required kwarg instead;
  - assert `imported <= {"pathlib", "sys", "erasure_gate"}` — a SUBSET assertion over a named
    allow-set, which is strictly stronger than enumerating forbidden names, and separately assert
    `{"scipy", "numpy", "torch"}` are absent so the failure message names the classic offenders;
  - assert `from_erasure_gate == {"MARGIN_K", "V20_EWC_RETENTION_PPL",
    "V20_MASKED_DIALOGUE_VAL_PPL", "rule_of_three", "wilson_upper_bound"}` — EXACT equality, not a
    subset. The list was accumulated one name per consumer across four tasks in three plans, and
    exact equality is what proves the accumulation landed complete and then stopped growing. Keep
    `{"wilson_upper_bound", "rule_of_three"} <= from_erasure_gate` beside it: that is the assertion
    carrying GATE-02's "import the instrument" message, and both names now have real consumers in the
    pin — `wilson_upper_bound` in `extraction_ceiling` and `tolerance_report`, `rule_of_three` in the
    GATE-05 zero-extraction reason;
  - assert `"VERDICTS" not in from_erasure_gate` (D-31 / RESEARCH L2 — `erasure_gate.VERDICTS` is the
    WRONG domain and must not enter this module's namespace) and
    `"V20_RETENTION_NOISE_FLOOR" not in from_erasure_gate` (D-06 supersedes it for v4.0);
  - collect `defined` from `ast.FunctionDef` / `ast.AsyncFunctionDef` names and assert
    `not ({"wilson_upper_bound", "rule_of_three"} & defined)`, carrying the
    `tests/test_phase16_stats.py:409-411` message: a bound was re-implemented instead of imported, and
    the rule is import the instrument, never copy it, or the two silently diverge.

Write `test_mitigation_gate_imports_bounds_by_object_identity()` — the RUNTIME half
(`tests/test_phase19_erasure.py:745-748`). Import both `mitigation_gate` and `erasure_gate` and
assert with `is`, never `==`: `wilson_upper_bound`, `rule_of_three`, `MARGIN_K`,
`V20_EWC_RETENTION_PPL` and `V20_MASKED_DIALOGUE_VAL_PPL`. Message must record the reason: a
value-matching copy is a copy free to stop matching.

Write `test_prose_helper_is_outside_every_pin_glob()` — D-23's mechanism, stated as a check rather
than as prose. Using `fnmatch`, assert `scripts/_prose.py`'s name matches NONE of `phase16_*.py`,
`phase17_*.py`, `phase18_*.py`, `phase19_*.py`, `phase20_*.py`, `mitigation_*.py`, and assert
`_PROSE_PATH not in _GATE_MODULES`. The docstring must carry the honest caveat this repository
already records: the precedent is STRUCTURAL (fnmatch), not historical — neither `_addendum.py` nor
`_verdict.py` has actually been edited after an artifact existed.
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/python -m pytest -q tests/test_phase20_prereg.py</automated>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/python -c "
import ast,pathlib
t=ast.parse(pathlib.Path('scripts/mitigation_gate.py').read_text())
imported=set()
for n in ast.walk(t):
    if isinstance(n,ast.Import): imported.update(a.name.split('.')[0] for a in n.names)
    elif isinstance(n,ast.ImportFrom): imported.add((n.module or '').split('.')[0])
assert imported <= {'pathlib','sys','erasure_gate'}, imported
assert 'mitigation_budget' not in imported
print('imports:', sorted(imported))"</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest -q tests/test_phase20_prereg.py` exits 0.
    - The test module contains both `_MITIGATION_GATE_PATH = _ROOT / "scripts" / "mitigation_gate.py"` and a `glob("mitigation_*.py")` call, and asserts membership of the former in the latter's result.
    - `_collapsed_glob_guard()` is called before any scan, and `len(_GATE_MODULES) >= 1` today.
    - The import scan asserts `imported <= {"pathlib", "sys", "erasure_gate"}` and `"mitigation_budget" not in imported`.
    - The scan asserts `"VERDICTS" not in from_erasure_gate` and `"V20_RETENTION_NOISE_FLOOR" not in from_erasure_gate`.
    - The scan asserts `from_erasure_gate` EQUALS the five-name set `{"MARGIN_K", "V20_EWC_RETENTION_PPL", "V20_MASKED_DIALOGUE_VAL_PPL", "rule_of_three", "wilson_upper_bound"}` — the completed accumulation, proved complete and no larger.
    - `.venv/bin/python -c "import ast;t=ast.parse(open('tests/test_phase20_prereg.py').read());print(sorted({a.name.split('.')[0] for n in ast.walk(t) if isinstance(n,ast.Import) for a in n.names} | {(n.module or '').split('.')[0] for n in ast.walk(t) if isinstance(n,ast.ImportFrom)}))"` prints exactly `['_prose', 'ast', 'erasure_gate', 'fnmatch', 'mitigation_gate', 'pathlib', 'pytest', 'subprocess', 'sys']` — `json` still absent, it arrives in Task 2 with its consumer.
    - The runtime identity test uses `is` for all five symbols — confirmed by `grep -c " is erasure_gate\." tests/test_phase20_prereg.py` outputting at least `5`.
    - Watched RED, three separate mutations, each reverted byte-identically with the sha256 recorded before and after: (a) add `import mitigation_budget` to the gate → the D-20 assertion fails; (b) add a local `def wilson_upper_bound` to the gate → the `defined` assertion fails; (c) add `VERDICTS` to the `from erasure_gate import` list → the domain assertion fails.
    - `.venv/bin/python -m pytest -q tests/test_phase20_prereg.py` exits 0 — the per-task gate (`20-VALIDATION.md` § Sampling Rate). The FULL suite runs at Task 3, which measures its runtime, and at this plan's `<verification>` wave boundary.
  </acceptance_criteria>
  <done>The hybrid register scans the gate today and will scan `mitigation_budget.py` the moment Phase 23 creates it; the import graph is proved stdlib-plus-`erasure_gate` only; the bounds are proved imported both statically and by object identity; and three mutations have been watched turning it red.</done>
</task>

<task type="auto">
  <name>Task 2: The constant audits — two chosen constants, no retyped baseline, no fourth verdict state</name>
  <files>tests/test_phase20_prereg.py</files>
  <read_first>
    - `tests/test_phase16_stats.py:378-383` — the numeric-constant scan idiom
      (`{node.value for node in ast.walk(tree) if isinstance(node, ast.Constant) and isinstance(node.value, (int, float))}`)
      and its `assert 6435 not in numbers` usage.
    - `tests/test_phase18_prereg.py:82-102` — `_enclosing_functions(tree)`, which records MODULE
      SCOPE as `None` rather than dropping it "because module scope is the most dangerous placement
      there is". This plan needs the module-scope subset specifically.
    - `20-CONTEXT.md` D-18 (`:182-184`) — a reviewer auditing the pin should find exactly two numbers
      to argue with. And `<code_context>` (`:444-445`) — no fact values in a pin.
    - `20-CONTEXT.md` D-29 (`:326-335`) — the explicit rejection of a `PASS` carrying
      `provisional=True`, and why: it would collapse the three-verdict domain into four disguised
      states.
    - `20-RESEARCH.md` §1a — the exact values that must NOT be retyped: `4.5733`, `3.891140`,
      `0.068930`, `0.4921`, `0.3483`, and the measured `0.005214448168350039`.
  </read_first>
  <action>
Append `import json` — this task is its first consumer in the test module (it parses
`results/phase19_arm_erased.json` below). Then append four audits to
`tests/test_phase20_prereg.py`.

First a helper `_module_scope_floats(tree)` returning the set of float `ast.Constant` values that (a)
appear inside an `ast.Assign` at module scope and (b) are NOT inside an assignment whose target name
starts with `FIXTURE_`. Use `_enclosing_functions` to identify module scope.

Its docstring must state the SCOPE of the `FIXTURE_` exclusion honestly and must not overclaim. FOUR
fields, and only four, are proved against their source by
`test_destroyed_model_fixture_matches_the_published_phase19_readings` below:
`point_dialogue_ppl_on`, `point_dialogue_ppl_off`, `point_retention_ppl` and `control_gap`, each
asserted equal to a value parsed from `results/phase19_arm_erased.json`. Every OTHER float inside a
`FIXTURE_*` dict is a DELIBERATELY FABRICATED fixture value with no external source to check it
against. State the residual hole in those words: a third chosen constant could hide inside a
`FIXTURE_*` dict and this audit would not see it. That risk is ACCEPTED, because the alternative —
asserting fabricated fixture inputs against a source that does not exist — is exactly the kind of
unprovable assertion this phase exists to refuse. Do NOT write that "the very next test proves each
of them" equal to the artifact; that would be false for roughly two thirds of the fields.

Then narrow the hole as far as it honestly can be narrowed: assert that the set of module-scope names
beginning `FIXTURE_` in the gate is EXACTLY `{"FIXTURE_CLEARING_POINT", "FIXTURE_DESTROYED_MODEL",
"FIXTURE_TRUNCATED_SWEEP"}`, so the cheapest evasion — a fourth `FIXTURE_*` dict added to carry
constants past the audit — is caught, even though a constant hidden INSIDE one of the three is not.

`test_exactly_two_chosen_constants()` — D-18. Assert `_module_scope_floats(_tree(_MITIGATION_GATE_PATH))
== {0.7, 0.5}` and that this set equals `set(mitigation_gate.CHOSEN_CONSTANTS.values())` and that
`set(mitigation_gate.CHOSEN_CONSTANTS) == {"F_Y", "F_C"}`. Message: a reviewer auditing this pin must
find exactly two numbers to argue with; everything else is measured, imported or computed.

`test_no_imported_baseline_is_retyped()` — GATE-02 and GATE-04. Collect ALL numeric constants in the
gate's AST excluding `FIXTURE_*` assignments, and assert each of `4.5733`, `3.891140`, `0.068930`,
`0.005214448168350039`, `0.4921`, `0.3483` is absent, and additionally that `4.5837288963367` — the
superseded GATE-02 dialogue cap — is absent as both a numeric constant AND a substring of the source.
Then assert `"MARGIN_K"` IS present in the `erasure_gate` import list, so the k=2 discipline is
imported rather than retyped as the integer `2`. Message must name GATE-02's "imported, never
retyped" and GATE-04's prohibition on deriving Y from v2.0's published `0.4921 / 0.3483`.

Then prove the supersession is a COMPUTATION: assert
`mitigation_gate.superseded_dialogue_cap(gap_noise_floor=0.005214448168350039) == 4.5837288963367`
exactly. This is the one place those two literals are allowed to appear — a test file is not the pin
and is not audited by the constant scans above — and it is what makes "GATE-02's dialogue cap,
computed from two imported constants rather than retyped" a fact rather than a claim (ROADMAP SC1).
Assert with `==`, never a tolerance: the point is bit-exact reproduction of the v3.0 cap.

`test_destroyed_model_fixture_matches_the_published_phase19_readings()` — D-30. Parse
`results/phase19_arm_erased.json` and assert FOUR fields against it:
`["point_dialogue_ppl_on"]` equals `dialogue_ppl["adapter_on"]`; `["point_dialogue_ppl_off"]` equals
`dialogue_ppl["adapter_off"]`; `["point_retention_ppl"]` equals `retention_ppl[0]` — INDEXED, because
`retention_ppl` is a LIST `[ppl, n]` while `dialogue_ppl` is a dict, and `.get()` on the list would
raise; and `["control_gap"]` equals
`pre_erasure["dialogue_ppl"]["adapter_on"] - dialogue_ppl["adapter_off"]`, asserted as the
SUBTRACTION rather than against a typed decimal. `control_gap` is here because it is the field most
likely to be retyped and the `FIXTURE_*` exclusion would let a wrong value through: its true double
is `1.2420966625043919` and the plausible short form `1.242096662504392` is a different float.
Message: the fixture reuses a catastrophe that actually happened, which is why it is not
hypothetical, and it is proved equal to its published source rather than transcribed.

`test_verdict_domain_stays_exactly_three()` — GATE-01 / D-29 / D-31. Assert
`mitigation_gate.V4_VERDICTS == ("PASS", "FAIL", "INCONCLUSIVE")` and `len(V4_VERDICTS) == 3`; assert
the string `provisional` appears nowhere in the gate's source (case-insensitive), with a message
recording that a `PASS` carrying `provisional=True` was explicitly rejected because it would collapse
three verdicts into four disguised states; and assert `_VERDICT_RELABEL` maps `erasure_gate.VERDICTS`
onto `V4_VERDICTS` positionally with `INCONCLUSIVE` fixed — the same three claims
`_prove_verdict_domain()` makes at import, restated here so a CI run proves them even if the module
were imported from a cache.

`test_every_gate_function_is_keyword_only_with_no_defaults()` — GATE-01, over ALL public
`FunctionDef`s in the gate (names not starting with `_`), not just the verdict function. For each,
assert `args.posonlyargs == []`, `args.args == []`, `args.defaults == []` and
`args.kw_defaults == [None] * len(args.kwonlyargs)`. Message: a transposable positional argument is
how two counts get swapped, and a default is how a required anchor gets silently omitted.
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/python -m pytest -q tests/test_phase20_prereg.py</automated>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/python -c "
import ast,json,pathlib,sys; sys.path.insert(0,'scripts')
import mitigation_gate as m
src=pathlib.Path('scripts/mitigation_gate.py').read_text()
t=ast.parse(src)
nums=set()
for top in t.body:  # the ban is scoped OUTSIDE FIXTURE_* assignments, matching this task's action
    if isinstance(top,ast.Assign) and any(getattr(tg,'id','').startswith('FIXTURE_') for tg in top.targets): continue
    nums.update(n.value for n in ast.walk(top) if isinstance(n,ast.Constant) and isinstance(n.value,(int,float)) and not isinstance(n.value,bool))
for banned in (4.5733, 3.891140, 0.068930, 0.005214448168350039, 0.4921, 0.3483):
    assert banned not in nums, banned
assert 'provisional' not in src.lower()
a=json.load(open('results/phase19_arm_erased.json'))
assert m.FIXTURE_DESTROYED_MODEL['point_retention_ppl']==a['retention_ppl'][0]
assert set(m.CHOSEN_CONSTANTS)=={'F_Y','F_C'}
print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest -q tests/test_phase20_prereg.py` exits 0.
    - `_module_scope_floats` over the gate returns exactly `{0.7, 0.5}` and that set equals `set(CHOSEN_CONSTANTS.values())`.
    - None of `4.5733`, `3.891140`, `0.068930`, `0.005214448168350039`, `0.4921`, `0.3483` appears as a numeric constant in the gate's AST outside `FIXTURE_*` assignments.
    - `"MARGIN_K"` is present in the gate's `from erasure_gate import` name set.
    - `FIXTURE_DESTROYED_MODEL`'s FOUR proved fields (`point_dialogue_ppl_on`, `point_dialogue_ppl_off`, `point_retention_ppl`, `control_gap`) are asserted equal to values parsed from `results/phase19_arm_erased.json`, with `retention_ppl` accessed by index `[0]` and `control_gap` asserted as `pre_erasure.dialogue_ppl.adapter_on - dialogue_ppl.adapter_off`.
    - `_module_scope_floats`'s docstring names exactly those four proved fields, states the residual hole in words (a chosen constant hidden inside a `FIXTURE_*` dict is NOT caught), and does not claim the next test proves "each of them".
    - The module-scope `FIXTURE_*` name set in the gate is asserted EQUAL to `{"FIXTURE_CLEARING_POINT", "FIXTURE_DESTROYED_MODEL", "FIXTURE_TRUNCATED_SWEEP"}` — a fourth `FIXTURE_*` dict fails the audit.
    - `mitigation_gate.superseded_dialogue_cap(gap_noise_floor=0.005214448168350039) == 4.5837288963367` is asserted with `==` and passes; `4.5837288963367` appears nowhere in `scripts/mitigation_gate.py`.
    - `grep -ci "provisional" scripts/mitigation_gate.py` outputs `0`.
    - Every public function in the gate has empty `posonlyargs`, empty `args`, empty `defaults`, and all-`None` `kw_defaults`.
    - Watched RED and reverted byte-identically (sha256 recorded both times): add `THIRD_CONSTANT = 0.9` at module scope in the gate → `test_exactly_two_chosen_constants` fails; give one public function a default → the keyword-only audit fails.
    - `.venv/bin/python -m pytest -q tests/test_phase20_prereg.py` exits 0 — the per-task gate (`20-VALIDATION.md` § Sampling Rate). The FULL suite runs at Task 3, which measures its runtime, and at this plan's `<verification>` wave boundary.
  </acceptance_criteria>
  <done>The pin's numeric surface is audited: exactly two chosen constants, no retyped baseline, no forbidden v2.0 recall value, no fourth verdict state, and every public function keyword-only and defaultless — with two mutations watched turning it red.</done>
</task>

<task type="auto">
  <name>Task 3: The behavioural twin — every branch re-run in CI against the same fixtures</name>
  <files>tests/test_phase20_prereg.py</files>
  <read_first>
    - `20-RESEARCH.md` "The Nyquist problem for a rule-producing phase" and "Proving every verdict
      branch fires" — the six-outcome table, the reason `__main__` alone is insufficient (`__main__`
      is not collected by pytest, so a self-check nobody runs in CI is not a guard), and the rule
      that precedence is proved DIFFERENTIALLY.
    - `20-05-SUMMARY.md` — the recorded stdout of `.venv/bin/python scripts/mitigation_gate.py` and
      the exact fixture kwargs, so the twin asserts against what was actually observed.
    - `tests/test_phase16_prereg.py:169-173` — the `subprocess.run(..., capture_output=True,
      text=True, check=True)` shape for shelling out with an argv tuple.
    - `20-CONTEXT.md` D-29, D-30 — the branch semantics the twin must re-assert.
  </read_first>
  <action>
Append the behavioural twin to `tests/test_phase20_prereg.py`. Every fixture is IMPORTED from
`mitigation_gate` — `FIXTURE_CLEARING_POINT`, `FIXTURE_DESTROYED_MODEL`, `FIXTURE_TRUNCATED_SWEEP` —
never re-transcribed, because a second transcription is a second fixture free to stop matching.

`test_gate_self_check_runs_clean_in_a_fresh_interpreter()` — run `sys.executable
scripts/mitigation_gate.py` through `subprocess.run` with an argv tuple, `cwd=_ROOT`,
`capture_output=True`, `text=True`, `check=True`. Assert exit 0 and that stdout names each of `PASS`,
`FAIL` and `INCONCLUSIVE`. A fresh interpreter is the point: it re-executes
`_prove_verdict_domain()` and the `ARM_CLAIMS` proof at import, which a cached `sys.modules` entry
would skip.

`test_every_verdict_branch_fires()` — six outcomes, each asserted with the observed value in the
failure payload:
  1. `mitigation_point_verdict(**FIXTURE_CLEARING_POINT)` returns `PASS`.
  2. `mitigation_point_verdict(**FIXTURE_DESTROYED_MODEL)` returns `FAIL` (GATE-09 / D-30).
  3. GATE-05 precedence, DIFFERENTIAL: assert branch 2 returns `FAIL`, then the same fixture merged
     with `point_extraction_successes=0` and `zero_extraction_has_nll=False` returns `INCONCLUSIVE`
     with exactly ONE reason. Comment that the differential is against FAIL, not PASS, because an
     INCONCLUSIVE that only overrides PASS proves nothing about precedence over FAIL.
  4. GATE-06, DIFFERENTIAL: `FIXTURE_TRUNCATED_SWEEP` returns `INCONCLUSIVE` with more than one
     reason — the per-condition detail survives the late return — while its FAIL-producing base does
     not.
  5. GATE-08 / D-29, DIFFERENTIAL against PASS: `FIXTURE_CLEARING_POINT` merged with
     `replicated_at_second_seed=False` returns `INCONCLUSIVE`; assert the returned tuple has exactly
     three elements and that the last reason starts with `REPLICATION_PENDING_MARKER`.
  6. GATE-07: assert the third element of every result equals the fixture's `arm`, and that
     `exists_clearing_point` over a mixed-arm list raises `SystemExit` (use `pytest.raises`).

`test_promotion_rule_and_ratchet()` — CAL-04 / D-19 / D-20. Assert `K_RUNGS == (48, 24, 16, 8)`;
`ratchet_k` accepts an increase and raises `SystemExit` on a decrease and on a non-member value;
`promote_to_full_fidelity` promotes a `PASS` and the `REPLICATION_PENDING_MARKER` INCONCLUSIVE, and
refuses a `FAIL` and a truncated-sweep INCONCLUSIVE; and that a `full_k` below `curve_k` raises,
proving the ratchet is reached through one implementation.

`test_capacity_rule_commits_both_branches_and_refuses_the_unset_fallback()` — GATE-10. Assert all
four cleared-flag combinations return their named `CAPACITY_BRANCHES` member at identical
mechanisms; that a differing `MECHANISM_KEYS` entry returns `"not-comparable"` with ZERO tolerance
(D-25); and that `epsilon_independent_of_n=False` with `fallback_epsilon_tolerance=None` raises
`SystemExit` naming D-26 — the third chosen constant, deliberately unset, to be decided before Phase
21's CAL-03 runs. Also assert the dispatch's branch set is a subset of `CAPACITY_BRANCHES`, so a
future branch name cannot be added without entering the closed tuple.

`test_extraction_floor_tripwire_is_the_only_route_to_a_verdict()` — D-14a as a reachability claim
rather than a unit test of `extraction_ceiling` alone: `FIXTURE_CLEARING_POINT` merged with an
`extraction_floor_provenance` naming a single seed must raise `SystemExit` from inside
`mitigation_point_verdict`, proving there is no path to a verdict that bypasses the provenance check.

`test_condition_a_reason_carries_its_tolerance_sentence()` — D-14b: assert condition (a)'s reason
string from the `PASS` result contains the `sentence` returned by `tolerance_report` for that
fixture's ceiling and question count, so no verdict can be published without the reader learning how
strong the criterion was.
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/python -m pytest -q tests/test_phase20_prereg.py -v</automated>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/python -m pytest -q && .venv/bin/ruff check . && .venv/bin/ruff format --check .</automated>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/python scripts/mitigation_gate.py && git log --diff-filter=A --format=%H -- 'results/phase20_*' | wc -l | tr -d ' ' | grep -qx 0 && echo "pin runs, history clean"</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest -q tests/test_phase20_prereg.py -v` exits 0 and lists at least 14 passing tests.
    - The subprocess test asserts `returncode == 0` for `scripts/mitigation_gate.py` and that its stdout contains all three of `PASS`, `FAIL`, `INCONCLUSIVE`.
    - All six verdict outcomes are asserted in `test_every_verdict_branch_fires`, and the three precedence claims each assert BOTH arms of their differential (the base verdict and the overridden verdict).
    - The twin imports the fixtures: `grep -c "FIXTURE_DESTROYED_MODEL\|FIXTURE_CLEARING_POINT\|FIXTURE_TRUNCATED_SWEEP" tests/test_phase20_prereg.py` outputs at least `8`, and the test module contains no literal `4.851119149910443` (proving the readings are not re-transcribed).
    - `ratchet_k` decrease, `ratchet_k` non-member, mixed-arm `exists_clearing_point`, single-seed provenance through `mitigation_point_verdict`, and the unset fallback tolerance each raise `SystemExit` under `pytest.raises`.
    - The condition-(a) reason from the `PASS` fixture contains `tolerance_report(...)[2]` for the same ceiling and question count.
    - Full suite: `.venv/bin/python -m pytest -q` exits 0; `.venv/bin/ruff check . && .venv/bin/ruff format --check .` exit 0.
    - `git status --porcelain pyproject.toml` prints nothing; `git log --diff-filter=A -- 'results/phase20_*'` prints nothing.
    - **Re-measure the full-suite runtime with this phase's tests present** and UPDATE `20-VALIDATION.md`'s runtime row AND its "Max feedback latency" line. That row currently carries the pre-phase baseline `201.99s` (`845 passed, 1 skipped`) measured before wave 1; replace it with the new number and the new pass/skip counts. Time it; do not estimate it.
    - Also record the measured `pytest -q tests/test_phase20_prereg.py` runtime beside it — it is the per-task gate every other task in this phase runs, and its latency is the number `20-VALIDATION.md` § Sampling Rate is actually about.
  </acceptance_criteria>
  <done>Every verdict branch, the ratchet, the promotion rule, the capacity rule and the tripwire's unavoidability are re-asserted in CI against the same module-scope fixtures the self-check uses; the `__main__` is run in a fresh interpreter; and the full-suite runtime is measured and recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the pin's source ↔ what a reader believes it contains | Every discipline this pin rests on — stdlib-only, import-never-retype, two chosen constants — is invisible to a behavioural test and needs a static instrument. |
| `__main__` ↔ CI | A self-check that only runs when someone runs it is not a guard. |
| Phase 20's register ↔ Phase 23's module | `mitigation_budget.py` does not exist yet and must enter these scans automatically the moment it does. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-33 | Elevation of privilege | the gate importing the budget, collapsing the outcome/resource split | mitigate | AST assertion `"mitigation_budget" not in imported`, covering both import forms; watched RED against a deliberate `import mitigation_budget` and reverted byte-identically |
| T-20-34 | Tampering | a bound re-implemented locally and silently diverging | mitigate | Static: the symbol must be in `from_erasure_gate` and absent from the module's `FunctionDef` names. Runtime: `is`-identity on five symbols, never `==` |
| T-20-35 | Tampering | a third chosen constant appearing in the pin | mitigate | `_module_scope_floats` audited equal to `{0.7, 0.5}` and to `set(CHOSEN_CONSTANTS.values())`; watched RED against an added `THIRD_CONSTANT = 0.9` |
| T-20-36 | Tampering | an imported baseline retyped as a literal | mitigate | Six specific values asserted absent from the gate's numeric constants; `MARGIN_K` asserted present in the import list |
| T-20-37 | Spoofing | a fabricated number passing as a published M1 reading | mitigate | D-30 — the fixture's three readings are asserted EQUAL to the parsed `results/phase19_arm_erased.json`, with `retention_ppl` indexed as the LIST it is |
| T-20-38 | Repudiation | a branch that only fires when a human runs the self-check | mitigate | A pytest twin re-asserts all six outcomes AND runs `scripts/mitigation_gate.py` as a subprocess in a fresh interpreter, so `_prove_verdict_domain()` re-executes rather than hitting a `sys.modules` cache |
| T-20-39 | Spoofing | a future gate-adjacent module escaping every scan | mitigate | The hybrid register's `mitigation_*.py` glob admits Phase 23's `mitigation_budget.py` automatically; `_collapsed_glob_guard()` makes a broken glob loud instead of green |
</threat_model>

<verification>
WAVE BOUNDARY. Per `20-VALIDATION.md` § Sampling Rate the per-TASK gate for Tasks 1 and 2 is
`.venv/bin/python -m pytest -q tests/test_phase20_prereg.py` (seconds). Task 3 runs the FULL suite
because it must MEASURE its runtime, and the full suite also runs once here.

- `.venv/bin/python -m pytest -q` exits 0.
- `.venv/bin/python scripts/mitigation_gate.py` exits 0.
- `.venv/bin/ruff check . && .venv/bin/ruff format --check .` exit 0.
- `git status --porcelain pyproject.toml` prints nothing.
- `git log --diff-filter=A -- 'results/phase20_*'` prints nothing.
- The measured full-suite runtime is written into `20-VALIDATION.md`.
</verification>

<success_criteria>
- Five mutations have been watched turning the suite red and each reverted byte-identically with both
  sha256 values recorded: budget import, redefined bound, imported `VERDICTS`, added third constant,
  added default.
- All six verdict outcomes pass in CI against the same module-scope fixtures the `__main__` uses.
- `tests/test_phase20_prereg.py` contains no re-transcribed Phase 19 reading.
- The real repository still has zero `results/phase20_*` files.
</success_criteria>

<output>
Create `.planning/phases/20-pre-registration-the-three-condition-gate/20-06-SUMMARY.md` when done.
Record: the five watched-RED mutations with both sha256 values each; the measured full-suite runtime;
the pytest test count for `tests/test_phase20_prereg.py`; and a re-confirmation that
`git log --diff-filter=A -- 'results/phase20_*'` is still empty.
</output>
