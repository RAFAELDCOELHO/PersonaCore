---
phase: 20-pre-registration-the-three-condition-gate
plan: 11
type: execute
wave: 4
depends_on: [20-10]
files_modified:
  - tests/test_phase20_correction.py
autonomous: true
gap_closure: true
requirements: [GATE-06, GATE-02]

must_haves:
  truths:
    - "The corrected coverage rule is watched RED-then-GREEN against both reproduced directions, not merely written"
    - "A future caller that reaches a verdict without the correction turns a committed test red"
    - "A raw rate handed to the sanctioned route's count parameter turns a committed test red"
    - "A hand-edited number in the correction artifact turns a committed test red"
    - "The artifact→retention_cap coupling is watched by CI on both sides"
  artifacts:
    - path: "tests/test_phase20_correction.py"
      provides: "the armed tripwires — both directions, the mirror bound, the provenance refusals, the caller census, the additivity guard, WR-02"
      contains: "def test_"
      min_lines: 200
  key_links:
    - from: "tests/test_phase20_correction.py"
      to: "results/phase20_gate_coverage_correction.json"
      via: "every published number re-derived by calling the modules"
      pattern: "corrected_point_verdict\\("
    - from: "tests/test_phase20_correction.py"
      to: "scripts/mitigation_gate.py"
      via: "AST caller census — recorded-state discipline, RED on the first bypassing caller"
      pattern: "ast\\.walk"
---

<objective>
Arm the tripwires. A guard nobody has watched fail is a guard nobody has verified — five were watched
in this phase (20-SECURITY.md "Watched-RED evidence") and these must join them.

Purpose: 20-VERIFICATION.md gap 1 requires an armed tripwire "in an UNPINNED test file that fires
when a Phase 23/25 caller passes raw rates, and a second asserting the direction-(ii) case returns
INCONCLUSIVE not FAIL". 20-SECURITY.md requires RED-then-GREEN against BOTH reproduced cases, "not
merely against the happy case". This plan is what lets `threats_open` reach 0.

Output: `tests/test_phase20_correction.py` — UNPINNED, CPU-only, GPU-free.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-pre-registration-the-three-condition-gate/20-VERIFICATION.md
@.planning/phases/20-pre-registration-the-three-condition-gate/20-SECURITY.md
@results/phase20_gate_coverage_correction.json

<audit_discipline>
This phase produced FOUR independent instances of a `grep -c` / `X in src` audit matching the
docstring that EXPLAINS the pattern (recorded in `.planning/REQUIREMENTS.md`'s `| RPT-02 |`
traceability row). Every audit in this file is therefore an AST walk or goes through
`scripts/_prose.py::normalized`. No `grep -c`. No `X in source`.
`tests/test_phase20_prereg.py:797-802` argues it at length: prose ABOUT a number is not the number,
and only an AST walk can tell them apart.

`tests/test_phase19_erasure.py:1389` shows the census defect to avoid: `getattr(node.func, "id",
None)` misses `ast.Attribute`, so `mitigation_gate.mitigation_point_verdict(...)` is invisible to a
bare-name check. Match on `.attr` as well as `.id` (WR-07).

**A guard that cannot fail is not a guard.** The same rule applies to this plan's own `<automated>`
blocks, and three instances were caught in review before it shipped: a bare
`pytest <file> -q` exits 0 on ONE passing test and cannot enforce "at least N, zero skips"; an
`is`-identity assertion on a small int cannot fail because CPython interns it; an `assert <string> in
<whole file>` cannot fail when the file already contained the string. Each verify block below
therefore asserts a COUNT and zero skips, and each identity claim is proved by the mechanism that can
actually fail for that object's type.
</audit_discipline>

<measured_helper_contract>
<!-- MEASURED. The obvious one-expression form of the fixture helper raises before any assertion
     runs, on the one case that checks D-35. Write the merge form. -->

    # BROKEN — TypeError: dict() got multiple values for keyword argument 'sweep_heldout_recalls'
    return dict(f, sweep_heldout_recalls=(0.30, 0.20), retention_floor_provenance=prov, **over)

    # CORRECT — overrides are merged into the defaults before splatting
    base = dict(sweep_heldout_recalls=(0.30, 0.20), retention_floor_provenance=prov)
    base.update(over)
    return dict(f, **base)

The broken form works only while nothing overrides a default. Task 1's WR-09 case overrides
`sweep_heldout_recalls` and every refusal case in Task 3 overrides `retention_floor_provenance`, so
the broken form takes out the mechanical check of D-35 and the whole retention refusal suite.

Fixture facts, measured, so a fixture-derived call is clean before any override:
`extraction_noise_floor = 0.01`, `retention_noise_floor = 0.009` (≠ `V20_RETENTION_NOISE_FLOOR`),
`control_taught_recall = 0.5`, `control_heldout_recall = 0.35`, on all three committed fixtures.
</measured_helper_contract>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED-then-GREEN on both reproduced directions, the sealed interface, and the mirror bound</name>

  <read_first>
    - `tests/test_phase19_correction.py` — the module docstring register and the module-constant
      marker triple at `:49-59`.
    - `scripts/phase20_gate_coverage.py` — the module under test.
    - `scripts/mitigation_gate.py:798-812` — the superseded block, and `:755-756` / `:767-768` — the
      two criteria whose statistics the corrected rule must match.
    - `results/phase20_gate_coverage_correction.json` — `evidence`, for the verdicts being asserted.
  </read_first>

  <files>tests/test_phase20_correction.py</files>

  <behavior>
    - Both directions RED against the pin, GREEN through the correction, in ONE differential test
      per direction so the RED and the GREEN are asserted against each other rather than in
      separate files.
    - The sanctioned route has no raw-rate parameter AND refuses a raw-rate value.
    - The superseded sentinel provably cannot fire the frozen GATE-06 branch on any committed fixture.
    - `wilson_lower_bound` and `wilson_upper_bound` are exact mirrors at the same z, bracket the
      point estimate for every outcome at n=104, and the lower bound returns exactly `0.0` at zero
      successes on every denominator rather than only on the convenient ones.
  </behavior>

  <action>
Create `tests/test_phase20_correction.py`. Module docstring in `tests/test_phase19_correction.py`'s
register: why a continuation and not a fix (the pin is frozen; the guard takes `adds[-1]`;
delete-and-re-add cannot launder it), and what is at stake in the verdicts.

Declare the marker triple as module constants — `PENDING`, `RECORDED`, `ADDENDUM_HEADING` — with the
exact strings from `20-10`. The committed test module is the pair's source of truth, not the script
that ran once, exactly as `tests/test_phase19_correction.py:49-59` states. Task 2 consumes them.

Load `phase20_gate_coverage`, `mitigation_gate` and `erasure_gate` via a `sys.path.insert` on
`scripts/`, matching the sibling test modules.

Build a helper returning the corrected-call kwargs for a named fixture: take
`getattr(mitigation_gate, fixture_name)`, drop `sweep_extraction_rates`, and supply the defaults
`sweep_heldout_recalls=(0.30, 0.20)` and
`retention_floor_provenance={"regime": "adapter", "seeds": (1337, 2024)}`. Never hand-copy 21
kwargs — read them off the committed fixture. Task 3 reuses this helper.

**Write the merge form given in `<measured_helper_contract>`, not the one-expression form.** MEASURED:
`dict(f, sweep_heldout_recalls=..., retention_floor_provenance=..., **over)` raises
`TypeError: dict() got multiple values for keyword argument` the moment a caller overrides either
default — which is every WR-09 case in this task and every refusal case in Task 3. Merge the
overrides into the defaults first (`base.update(over)`), then splat once.

`test_direction_i_spurious_inconclusive_is_red_on_the_pin_and_green_through_the_correction` —
`FIXTURE_CLEARING_POINT`. Assert in this order, so the RED is watched before the GREEN is claimed:
  1. `mitigation_gate.mitigation_point_verdict(**dict(mitigation_gate.FIXTURE_CLEARING_POINT,
     sweep_extraction_rates=(1/104, 3/104)))` returns `"INCONCLUSIVE"` and its reason list's last
     entry names `GATE-06`. THE DEFECT, live.
     **Override via `dict(fixture, key=value)`, never `**FIXTURE, key=value`** — the committed
     fixtures already carry all 21 kwargs, so the double-splat form raises MEASURED
     `TypeError: mitigation_gate.mitigation_point_verdict() got multiple values for keyword
     argument 'sweep_extraction_rates'`. Same collision the helper contract above names.
  2. The sweep genuinely brackets X under the (a) rule:
     `wilson_upper_bound(1, 104) <= X < wilson_upper_bound(3, 104)`, with X obtained by CALLING
     `mitigation_gate.extraction_ceiling` on the fixture's own arguments. Never a typed constant.
  3. `corrected_point_verdict(..., sweep_extraction_successes=(1,3),
     sweep_extraction_questions=(104,104))` returns `"PASS"`. THE CORRECTION.
  4. The failure message must state that a would-be PASS was demoted and
     `promote_to_full_fidelity` withheld.

`test_direction_ii_suppressed_inconclusive_is_red_on_the_pin_and_green_through_the_correction` —
`FIXTURE_DESTROYED_MODEL`. **This fixture, not `FIXTURE_CLEARING_POINT`** — measured across all
three, and the reports name none.
  1. The pin with `sweep_extraction_rates=(3/104, 11/104)` returns `"FAIL"` and NO reason in the
     list names `GATE-06`. A truncated axis judged decisively.
  2. ZERO points clear X: assert `wilson_upper_bound(3,104) > X` and `wilson_upper_bound(11,104) > X`.
  3. `corrected_point_verdict(..., sweep_extraction_successes=(3,11), ...)` returns
     `"INCONCLUSIVE"` and its last reason names `GATE-06` and
     `phase20_gate_coverage.SUPERSEDED_GATE06_BLOCK`.
  4. Assert the SAME sweep on `FIXTURE_CLEARING_POINT`: pin `"PASS"`, corrected `"INCONCLUSIVE"` —
     the third case no report records. Its message must state the honest limit: it does not
     contradict the verifier's narrower no-spurious-PASS claim, which was scoped to self-consistent
     inputs.

`test_the_sanctioned_route_cannot_be_handed_raw_rates` — the interface half AND the value half,
because deleting the parameter removes the raw-rate PATH but not the raw-rate VALUE:
  1. `inspect.signature(corrected_point_verdict)` has no parameter named `sweep_extraction_rates`;
     every parameter is `KEYWORD_ONLY` with `default is inspect.Parameter.empty`; the count is 24.
  2. **The armed half.** Call `corrected_point_verdict` with the direction-(ii) migration mistake —
     `sweep_extraction_successes=(3/104, 11/104)`, `sweep_extraction_questions=(104, 104)` — and
     assert `pytest.raises(SystemExit)`, with the message naming both RATE and COUNT. This is the
     tripwire 20-VERIFICATION.md gap 1 item 3 asks for: without it, that call is silently accepted,
     every fractional `successes` yields a Wilson upper bound under X at n=104, and the route
     returns a spurious `INCONCLUSIVE` with nothing in the output to say why. Assert the same for a
     negative `successes` and for `successes > questions`.
  3. Also assert the positive control alongside: the integer form `(3, 11)` with the same
     `questions` reaches a verdict. A refusal test with no positive control cannot distinguish "the
     guard fires" from "nothing works".

`test_the_superseded_sweep_sentinel_cannot_fire_the_frozen_gate06_branch` — for each of the three
committed fixtures, call the pin with `sweep_extraction_rates` and `sweep_taught_recalls` both set to
`SUPERSEDED_SWEEP_SENTINEL` and assert no returned reason names `GATE-06`. This is what makes step 6
of `corrected_point_verdict` legitimate rather than lucky.

`test_wilson_bounds_are_exact_mirrors` — every comparison EXACT, no `math.isclose` and no tolerance
on the bracketing assertions:
  1. For every `k` in `0..104`: `wilson_lower_bound(k,104) <= k/104 <= wilson_upper_bound(k,104)`.
  2. `wilson_lower_bound(0,104) == 0.0` and `wilson_upper_bound(104,104) == 1.0`.
  3. **The short-circuit is not `n = 104` special-casing.** For every `n` in `2..400`:
     `wilson_lower_bound(0, n) == 0.0`. That span contains the measured cancelling denominators
     `11` (the smallest), `104` and `208` — where the naive mirror returns `1.11e-17`, `1.69e-18` and
     `8.56e-19` respectively — as well as `8`, `16`, `50` and `200`, where it returns exactly `0.0`
     anyway. A spot check at a single convenient denominator would prove nothing; the failure message
     must name the offending `n` and its returned value so a regression says which case broke.
  4. The two bounds are symmetric about the shared Wilson centre to within floating-point tolerance
     at the same z. This is the ONE place a tolerance belongs — it is a property of the shared
     construction, not a bracketing guarantee.
  5. **Identity, proved per object by the mechanism that can fail for its type.** Assert
     `phase20_gate_coverage.wilson_upper_bound is erasure_gate.wilson_upper_bound is
     mitigation_gate.wilson_upper_bound` (function objects) and
     `phase20_gate_coverage.F_Y is mitigation_gate.F_Y` (`0.7` — MEASURED: a retyped `0.7 is F_Y` is
     `False`, so this bites). Do NOT write an `is` assertion for `MARGIN_K` or
     `EXTRACTION_FLOOR_MIN_SEEDS`: both are the small int `2`, CPython interns it, and a retyped
     literal passes an `is` check identically (MEASURED) — a guard that cannot fail. Cover those two
     by AST instead, in this phase's own register: parse `scripts/phase20_gate_coverage.py`, collect
     the alias names of every `ast.ImportFrom`, and assert each of `MARGIN_K` and
     `EXTRACTION_FLOOR_MIN_SEEDS` appears in that set AND in no module-scope `ast.Assign` target.
     This is what T-20-53 actually claims, so it is what gets asserted.
  </action>

  <verify>
    <automated>.venv/bin/python -c "
import re,subprocess,sys
r = subprocess.run([sys.executable,'-m','pytest','tests/test_phase20_correction.py','-q'],capture_output=True,text=True)
out = r.stdout + r.stderr
assert r.returncode == 0, out[-3000:]
m = re.search(r'(\d+) passed', out)
assert m, out[-3000:]
n = int(m.group(1))
assert n >= 5, ('expected at least 5 passing tests, got %d' % n, out[-3000:])
assert 'skipped' not in out and 'xfail' not in out, ('zero skips required', out[-3000:])
print('OK — %d passed, zero skips' % n)"</automated>
  </verify>

  <acceptance_criteria>
    - The verify block asserts the pytest EXIT CODE, a passing-test COUNT of at least 5, and the
      absence of any skip or xfail. A bare `pytest <file> -q` is not sufficient: it exits 0 on a
      single passing test, so a file that silently loses a test or degrades one to a skip would pass
      it.
    - Direction (i) test asserts pin `"INCONCLUSIVE"` and corrected `"PASS"` in the same test body.
    - Direction (ii) test names `FIXTURE_DESTROYED_MODEL`, asserts pin `"FAIL"` with no GATE-06
      reason and corrected `"INCONCLUSIVE"`, and additionally asserts pin `"PASS"` → corrected
      `"INCONCLUSIVE"` on `FIXTURE_CLEARING_POINT`.
    - The fixture helper uses the `base.update(overrides)` merge form. An override of
      `sweep_heldout_recalls` or `retention_floor_provenance` returns a merged dict rather than
      raising `TypeError` — MEASURED as raising under the one-expression form.
    - `test_the_sanctioned_route_cannot_be_handed_raw_rates` asserts BOTH that the parameter is
      absent from the signature AND that `sweep_extraction_successes=(3/104, 11/104)` raises
      `SystemExit` through the route, plus the integer positive control.
    - `test_wilson_bounds_are_exact_mirrors` asserts `wilson_lower_bound(0, n) == 0.0` EXACTLY for
      every `n` in `2..400`, not just at `n = 104`, and uses no tolerance on any bracketing
      assertion.
    - Identity is asserted by `is` for `wilson_upper_bound` and `F_Y`, and by AST import-alias
      membership for `MARGIN_K` and `EXTRACTION_FLOOR_MIN_SEEDS`. No `is` assertion is written for a
      small int.
    - WATCHED RED, recorded in the SUMMARY with its observed output: temporarily change
      `COVERAGE_STATISTIC_BY_AXIS`'s extraction entry to decide on the raw rate (or make
      `coverage_verdict` compare `k/n` instead of `wilson_upper_bound(k, n)`), observe BOTH direction
      tests fail, then restore `scripts/phase20_gate_coverage.py` byte-identically and observe them
      pass. A guard nobody has watched fail is a guard nobody has verified.
    - No test contains a hardcoded float for X, `y_taught`, `y_heldout` or any Wilson bound — every
      one is obtained by calling a module function.
    - `git diff --exit-code -- scripts/mitigation_gate.py scripts/erasure_gate.py` exits 0.
  </acceptance_criteria>

  <done>
    Both reproduced directions have been watched RED against the frozen pin and GREEN through the
    correction, the raw-rate migration mistake is refused through the route, the mirror bound is
    proved exact across 399 denominators rather than one, and the deliberate break was observed
    failing and then restored.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The append was additive, every published number re-derives, and the defects are still live</name>

  <read_first>
    - `tests/test_phase19_correction.py:105-175` —
      `test_correction_addendum_is_additive_on_the_published_artifact`, which derives the pre-append
      revision from `git log` rather than pinning a hash. Port it.
    - `tests/test_phase19_correction.py` — `test_corrected_floor_re_derives_from_the_committed_arm_
      record` and `test_defects_..._are_all_still_live`. Mirror both.
    - `results/phase20_gate_coverage_correction.json` and `.md` — the published artifacts.
    - `scripts/_addendum.py` and `scripts/_verdict.py`.
  </read_first>

  <files>tests/test_phase20_correction.py</files>

  <behavior>
    - The published append was additive on the real committed bytes, not on a synthetic tmp file.
    - Every float in the correction JSON re-derives from the modules.
    - The three defects are asserted still live against the CODE, not against the prose.
  </behavior>

  <action>
Extend `tests/test_phase20_correction.py` using the module constants and fixture helper from Task 1.

`test_correction_addendum_is_additive_on_the_published_artifact` — port
`tests/test_phase19_correction.py:105-175` onto this phase's paths and marker pair. Keep the
shallow-clone assertion. Keep the "newest committed revision still carrying the placeholder is BY
DEFINITION the pre-append one" derivation. Add ONE thing Phase 19's version lacks: assert
`_verdict.recorded_verdict(after) is not None`, because `None == None` would make
`append_addendum`'s own verdict guard vacuous.

Derive the revision with `git log --format=%H -- <path>`, never with `git ls-files`. MEASURED:
`ls-files` prints the path for a file that is staged and never committed, so it cannot distinguish a
committed revision from an index entry. `20-10`'s own verify now asserts the two-revision property,
so this test consumes a property that was already checked at the point it was cheap to fix.

`test_every_published_number_re_derives_from_the_modules` — parse the correction JSON and re-derive
X, `y_taught`, `y_heldout`, every `wilson_upper` entry, every `raw_rate`, both verdicts per
direction, and `retention_provenance`'s `borrowed_cap` / `governing_cap` by CALLING the modules.
Assert equality. A hand-edited constant goes red here.

`test_the_three_defects_are_still_live_in_the_frozen_pin` — assert against the CODE:
  - CR-01: the pin still returns `"INCONCLUSIVE"` on direction (i).
  - WR-09: `"sweep_heldout_recalls"` is NOT among `inspect.signature(mitigation_point_verdict)`'s
    parameters.
  - T-20-19: `mitigation_gate.retention_cap(retention_noise_floor=erasure_gate.V20_RETENTION_NOISE_FLOOR)`
    returns `4.029` without raising.
  Each failure message must say that if it goes green the defect is gone and the continuation needs
  re-reading rather than deleting.
  </action>

  <verify>
    <automated>.venv/bin/python -c "
import re,subprocess,sys
r = subprocess.run([sys.executable,'-m','pytest','tests/test_phase20_correction.py','-q'],capture_output=True,text=True)
out = r.stdout + r.stderr
assert r.returncode == 0, out[-3000:]
m = re.search(r'(\d+) passed', out)
assert m, out[-3000:]
n = int(m.group(1))
assert n >= 8, ('expected at least 8 passing tests, got %d' % n, out[-3000:])
assert 'skipped' not in out and 'xfail' not in out, ('zero skips required', out[-3000:])
print('OK — %d passed, zero skips' % n)"</automated>
  </verify>

  <acceptance_criteria>
    - The verify block asserts the exit code, a passing count of at least 8, and zero skips — the
      count floor is what proves Task 2's three tests were ADDED rather than substituted for Task 1's.
    - The additivity test derives the pre-append revision from `git log` rather than from a pinned
      hash or from `git ls-files`, and asserts `_verdict.recorded_verdict(after) is not None`.
    - `git log --format=%H -- results/phase20_gate_coverage_correction.md` yields a revision carrying
      `PENDING`, and the additivity test passes against it.
    - Every float asserted in the re-derivation test is obtained by calling a module function; none
      is typed into the test.
    - `git diff --exit-code -- scripts/mitigation_gate.py scripts/erasure_gate.py` exits 0.
  </acceptance_criteria>

  <done>
    The published append is proved additive against the real committed bytes, every published number
    is proved to come from the code, and the three defects are proved still live.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The retention provenance refusals, the WR-02 artifact coupling, and the caller census that enforces the choke point</name>

  <read_first>
    - `scripts/phase20_gate_coverage.py` — `_prove_retention_floor` and its four checks, and
      `corrected_point_verdict`'s step ordering (the WR-08 sign check runs BEFORE
      `extraction_ceiling`).
    - `scripts/mitigation_gate.py:404-443` — `extraction_ceiling`'s three `_prove` calls (at `:417`,
      `:425`, `:436`), the shape being mirrored, and `tests/test_phase20_prereg.py:1297-1330`
      (`test_extraction_floor_tripwire_is_the_only_route_to_a_verdict`) — the guard shape to twin:
      drive the refusals THROUGH the verdict function, not against the helper in isolation.
    - `results/phase20_retention_floor.json` — `retention_ppl_noise_floor`, `cap`, `borrowed_cap`,
      `retention_ppl` per seed. Nothing in `tests/` reads this file today (WR-02).
    - `tests/test_phase19_erasure.py:1386-1400` — the census, including the two mechanical weaknesses
      WR-07 records: an `in source` positive obligation, and a bare-name-only call match at `:1389`
      (`getattr(node.func, "id", None)`). Do not copy either.
  </read_first>

  <files>tests/test_phase20_correction.py</files>

  <behavior>
    - Every borrowed / unprovenanced / single-seed retention floor is refused at the choke point,
      driven through `corrected_point_verdict` rather than against the helper alone.
    - The WR-08 sign check is proved at the magnitude where ordering matters, not only where it does
      not.
    - The artifact's own arithmetic is checked by CI on both sides.
    - A bypassing caller of the frozen verdict function turns this file red.
  </behavior>

  <action>
Extend `tests/test_phase20_correction.py`.

`test_the_retention_floor_tripwire_is_the_only_route_to_a_verdict` — the twin of
`tests/test_phase20_prereg.py:1297-1330`, over the retention leg. Drive every case THROUGH
`corrected_point_verdict`, because the claim being proved is that no path to a verdict skips the
check, and asserting against `_prove_retention_floor` in isolation would prove something about a
different function. Use Task 1's merge-form helper — every case here overrides
`retention_floor_provenance`, which is exactly what the one-expression form cannot do.
Assert `pytest.raises(SystemExit)` for each of:
  - `retention_floor_provenance={}` — no keys.
  - `retention_floor_provenance={"seeds": (1337, 2024)}` — missing `regime`.
  - `regime="full-finetune"` — the wrong regime by name.
  - `seeds=(1337,)` and `seeds=(1337, 1337)` — one distinct seed.
  - `retention_noise_floor=erasure_gate.V20_RETENTION_NOISE_FLOOR` with a fully valid
    adapter-regime provenance — the borrowed VALUE refused by identity even when the caller's
    provenance is otherwise clean. Assert the message names `7.939763314393305`, `4.029` and
    `3.9085032379884783`.
  - **`extraction_noise_floor=-0.05` — WR-08, at the magnitude that proves the ORDERING.** Assert
    the message names the FLOOR's sign and not the ceiling's range. MEASURED: `-0.05` drives
    `extraction_ceiling` to `-0.07464477133505877`, so a sign check placed after the ceiling
    computation is pre-empted by the `0.0 < ceiling < 1.0` message and the caller is told the wrong
    thing. `-0.01` leaves the ceiling at `+0.005355228664941234` and would pass either way, so
    testing only `-0.01` proves the check exists but not that it is correctly ordered. Assert BOTH
    magnitudes, and assert on the MESSAGE, not merely on `SystemExit`.
Then a POSITIVE control, the same shape `test_extraction_floor_tripwire...` uses: a fixture differing
ONLY in provenance still returns a verdict. A refusal test with no positive control cannot
distinguish "the guard fires" from "nothing works".

`test_v4_retention_cap_reads_the_measured_adapter_regime_floor` — WR-02, the artifact nothing reads.
Parse `results/phase20_retention_floor.json` and assert:
  - `floor != erasure_gate.V20_RETENTION_NOISE_FLOOR` — the borrowed Phase 12 floor is not back.
  - `mitigation_gate.retention_cap(retention_noise_floor=floor) == artifact["cap"]` bit-exact.
  - `artifact["borrowed_cap"] == mitigation_gate.retention_cap(
    retention_noise_floor=erasure_gate.V20_RETENTION_NOISE_FLOOR)`.
  - `artifact["cap"] < artifact["borrowed_cap"]` — the re-measurement is TIGHTER, asserted rather
    than narrated.
  - `floor == abs(difference of the two seeds' published `delta_on_minus_off` readings)` — the
    artifact's internal consistency, which is the only check available because `checkpoints/` and
    `data/` are gitignored and CI can never re-derive the number.

`test_no_path_to_a_v4_verdict_bypasses_the_correction` — the census that ENFORCES the choke point.
AST-walk every `.py` under `scripts/` and `src/`, excluding `scripts/mitigation_gate.py` (the
definition) and `scripts/phase20_gate_coverage.py` (the sanctioned route). Collect every `ast.Call`
whose func matches `mitigation_point_verdict` by EITHER `getattr(node.func, "id", None)` OR
`getattr(node.func, "attr", None)` — the `ast.Attribute` half is WR-07's latent hole
(`tests/test_phase19_erasure.py:1389` matches on `.id` alone) and
`mitigation_gate.mitigation_point_verdict(...)` is exactly the form a Phase 23 driver would write.
Assert the collected list is EMPTY. Measured today: zero non-test callers.

Make it a RECORDED STATE and not a silent pass, in
`tests/test_phase16_prereg.py`'s `bool(checked) == bool(tracked)` register: also assert that
`scripts/phase20_gate_coverage.py` DOES contain such a call, so the census is proved non-vacuous —
it is looking in the right place and matching the right node shape. A census that would return empty
because its matcher is broken proves nothing. `tests/` is deliberately excluded from the scan: the
prereg suite drives the pin's own branches directly and that is the behavioural twin, not a bypass.

The failure message must name `corrected_point_verdict`, name
`results/phase20_gate_coverage_correction.json`, and say plainly that a caller reaching a verdict
this way gets raw-rate coverage on the extraction axis, no coverage at all on the held-out leg, and
no retention provenance check.
  </action>

  <verify>
    <automated>.venv/bin/python -c "
import re,subprocess,sys
def run(path):
    r = subprocess.run([sys.executable,'-m','pytest',path,'-q'],capture_output=True,text=True)
    out = r.stdout + r.stderr
    assert r.returncode == 0, out[-3000:]
    m = re.search(r'(\d+) passed', out)
    assert m, out[-3000:]
    assert 'skipped' not in out and 'xfail' not in out, ('zero skips required in ' + path, out[-3000:])
    return int(m.group(1))
n = run('tests/test_phase20_correction.py')
assert n >= 11, ('expected at least 11 passing tests, got %d' % n)
p = run('tests/test_phase20_prereg.py')
assert p == 18, ('the prereg pin suite must still report exactly 18 passed, got %d' % p)
print('OK — correction %d passed, prereg %d passed, zero skips' % (n, p))"</automated>
  </verify>

  <acceptance_criteria>
    - The verify block asserts exit codes, a correction-suite count of at least 11, an EXACT prereg
      count of 18, and zero skips in both. The counts are machine-checked, not narrated.
    - All eight refusal cases raise `SystemExit` when driven through `corrected_point_verdict`, and
      the positive control still returns a verdict.
    - The WR-08 case asserts BOTH `-0.01` and `-0.05`, and asserts on the MESSAGE naming the floor's
      sign rather than merely on `SystemExit`. `-0.05` is the case that proves ordering: it drives
      the ceiling negative, so a sign check placed after the ceiling computation would be pre-empted
      by the ceiling-range message (MEASURED).
    - The WR-02 test asserts `retention_cap(floor) == artifact["cap"]` bit-exact and
      `artifact["cap"] < artifact["borrowed_cap"]`.
    - The census collects ZERO calls under `scripts/` and `src/` outside the two exempt files, AND
      collects at least one inside `scripts/phase20_gate_coverage.py` — non-vacuity proved.
    - WATCHED RED, recorded in the SUMMARY with its observed output, three breaks each restored
      byte-identically: (1) add a `mitigation_gate.mitigation_point_verdict(...)` call to a scratch
      module under `scripts/` and observe the census fire — this proves the `ast.Attribute` half,
      which a bare-name matcher would miss; (2) delete one `_prove` from `_prove_retention_floor` and
      observe its refusal case fail; (3) edit `artifact["cap"]` by one digit and observe the WR-02
      test fire.
    - No audit in the file uses `grep -c` or an `X in source` substring check.
    - `.venv/bin/python -m pytest -q` shows no regression against the `863 passed, 1 skipped`
      baseline; `ruff check .` and `ruff format --check .` clean.
    - `git diff --exit-code -- scripts/mitigation_gate.py scripts/erasure_gate.py` exits 0.
  </acceptance_criteria>

  <done>
    T-20-19's measured reproduction is refused at a choke point that has been watched firing, WR-08's
    sign check is proved correct at the magnitude where its ordering matters, the published artifact
    is read by CI on both sides, and the first caller to bypass the correction turns a committed test
    red.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a written guard ↔ a watched guard | A guard nobody has watched fail is a guard nobody has verified. Five were watched in this phase; these must join them. |
| the sanctioned route ↔ a direct pin caller | The correction has no authority a Phase 23/25 caller cannot simply not invoke. |
| a deleted parameter ↔ a refused value | Removing `sweep_extraction_rates` closes the raw-rate path; only a `_prove` closes the raw-rate value. |
| an audit's matcher ↔ what it claims to cover | A census that returns empty because its matcher is broken proves nothing. |
| a test suite's exit code ↔ its contents | `pytest -q` exits 0 on one passing test; only a count and a skip check prove the suite is intact. |
| a committed artifact ↔ CI | `checkpoints/` and `data/` are gitignored, so internal consistency is the only available check. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-21 | Repudiation | GATE-06 mislabeling on both axes | mitigate | **CLOSES HERE.** Both reproduced directions asserted RED against the frozen pin and GREEN through `corrected_point_verdict` in one differential body each, plus the third unreported case; the deliberate break is watched failing and restored byte-identically. WR-09's held-out leg is covered by the same `coverage_verdict` and its truncation case is asserted — reachable only because the fixture helper uses the merge form, since that case overrides `sweep_heldout_recalls`. The raw-rate migration mistake is refused through the route and asserted, so the corrected rule cannot be handed the old inputs and degrade quietly. |
| T-20-19 | Spoofing | a v3.0-regime floor standing in for the v4.0 retention floor | mitigate | **CLOSES HERE.** Eight refusal cases driven THROUGH the verdict route, with a positive control; the borrowed `0.06893` refused by identity even under clean provenance; WR-08 proved at both `-0.01` and `-0.05`; one `_prove` deleted and watched firing. |
| T-20-48 | Elevation of Privilege | a bypassing caller | mitigate | AST census matching BOTH `.id` and `.attr`, proved non-vacuous by asserting a match inside the sanctioned module, and watched RED by adding a bypassing call to a scratch module. `tests/` excluded deliberately and the reason recorded. |
| T-20-50 | Spoofing | `SUPERSEDED_SWEEP_SENTINEL` masking a genuine truncation | mitigate | Asserted across all three committed fixtures that the sentinel fires no GATE-06 reason, so neutralising the superseded block is a proved property rather than an assumption. Coverage is decided before the pin is called. |
| T-20-51 | Tampering | a hand-edited number in the correction artifact | mitigate | Every published float re-derived by calling the modules; the artifact's `cap` edited by one digit and watched firing. |
| T-20-53 | Tampering | a second copy of an imported estimator | mitigate | Proved per object by a mechanism that can fail for that object's TYPE: `is`-identity for `wilson_upper_bound` (function) and `F_Y` (`0.7`, a float — MEASURED: a retyped literal is NOT `is`-identical); AST import-alias membership plus absence from module-scope `ast.Assign` targets for `MARGIN_K` and `EXTRACTION_FLOOR_MIN_SEEDS`, both the small int `2`, which CPython interns so that an `is` check on them passes for a retyped literal (MEASURED) and could not fail. |
| T-20-57 | Repudiation | a defect description outliving its defect | mitigate | `test_the_three_defects_are_still_live_in_the_frozen_pin` asserts each against the CODE with a message saying that a green result means the continuation needs re-reading, not deleting. |
| T-20-59 | Tampering | an audit that matches the prose explaining the pattern | mitigate | Every audit is an AST walk or goes through `_prose.normalized`; no `grep -c` and no `X in source`. This phase produced four instances of that exact defect class (`.planning/REQUIREMENTS.md`'s `| RPT-02 |` row). |
| T-20-65 | Repudiation | a test-suite guard that passes on a degraded suite | mitigate | A bare `pytest <file> -q` exits 0 on a single passing test, so a file that silently loses a test or degrades one to a skip satisfies it while the plan's acceptance criteria claim "at least N passing, zero skips". All three verify blocks now run pytest as a subprocess and assert the exit code, the extracted passing COUNT against the stated floor (5 / 8 / 11), the EXACT count for the prereg pin suite (18), and the absence of `skipped` / `xfail` in the output. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_phase20_correction.py tests/test_phase20_prereg.py -q`
  → all pass, `test_phase20_prereg.py` still `18 passed`, both counts asserted rather than eyeballed.
- `.venv/bin/python -m pytest -q` → no regression against `863 passed, 1 skipped`.
- `ruff check .` / `ruff format --check .` → clean.
- All FOUR watched-RED breaks observed and restored — one in Task 1 (the coverage statistic flipped
  to the raw rate) and three in Task 3 (the scratch bypassing caller, the deleted `_prove`, the
  one-digit `cap` edit). `git diff --exit-code` clean on every restored file. Four is the number
  `20-12` writes into the Watched-RED table; do not write a row for a break that was not run.
</verification>

<success_criteria>
- Both reproduced directions proved RED-then-GREEN, watched, not merely written.
- The sanctioned route refuses raw rates by value as well as by interface.
- The mirror bound returns exactly `0.0` at zero successes across `2..400`, so the short-circuit is
  proved general rather than tuned to this phase's denominator.
- T-20-19's measured reproduction refused at a choke point, watched firing, with WR-08 proved at the
  magnitude where ordering matters.
- The census enforces the route and is proved non-vacuous.
- `results/phase20_retention_floor.json` is read by CI.
- Every verify block asserts a test count and zero skips, so none of them can pass on a degraded
  suite.
</success_criteria>

<output>
Create `.planning/phases/20-pre-registration-the-three-condition-gate/20-11-SUMMARY.md` when done.
Record every watched-RED break with its observed output — those FOUR rows feed 20-SECURITY.md.
</output>
