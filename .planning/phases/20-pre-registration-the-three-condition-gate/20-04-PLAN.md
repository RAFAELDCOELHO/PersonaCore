---
phase: 20-pre-registration-the-three-condition-gate
plan: 04
type: execute
wave: 3
depends_on: ["20-02"]
files_modified:
  - scripts/mitigation_gate.py
autonomous: true
requirements: [GATE-01, GATE-02, GATE-03, GATE-04, GATE-05, GATE-06, GATE-08]

must_haves:
  truths:
    - "D-01: condition (c)'s dialogue leg is a BAND ON THE ON-OFF ADAPTATION GAP — `gap = point_ppl_on - point_ppl_off` clears iff `F_C * control_gap <= gap <= control_gap + MARGIN_K * gap_noise_floor` — never a one-sided raw-PPL cap, which collapses directionally between 'capability preserved' and 'adaptation removed'. This supersedes the form GATE-02 and ROADMAP SC1 inherit from research R5, and the supersession is written INTO the pin (RESEARCH L6)."
    - "D-02: `control_gap` is the v4.0 RETRAINED CONTROL's own gap, a required kwarg, never hardcoded — v2.0's published numbers belong to a different training run."
    - "D-03: the band's upper bound is DERIVED and ADDITIVE (`control_gap + MARGIN_K * gap_noise_floor`), with `MARGIN_K` IMPORTED from `erasure_gate` and never retyped. It is not spelled as a `hi_frac`, because it is not a fraction."
    - "D-04: the gap form costs ZERO new constants — `adapter_off_identical_across_seeds` is true, so `noise(gap)` is bit-identical to the committed `dialogue_ppl_noise_floor` `0.005214448168350039`."
    - "D-05: the two legs are ASYMMETRIC BY DESIGN — dialogue a band, retention a one-sided upper cap — because the retention gap changes sign inside the already-measured range (taught `+0.3286199167186572`, M1 `-0.22022225029414155`). The reason is recorded in the source so a 'unify the two legs' refactor goes red rather than looking like cleanup."
    - "D-06 / D-07: the v4.0 retention cap is `V20_EWC_RETENTION_PPL + MARGIN_K * retention_noise_floor` with the floor a REQUIRED KWARG. `V20_RETENTION_NOISE_FLOOR` (0.068930, a Phase 12 full-fine-tune pair) is neither imported nor retyped, and the cap is anchored on the IMPORTED `3.891140`, not on the measured `3.891139975617828`."
    - "GATE-01: every argument of `mitigation_point_verdict` is keyword-only with no default, and every condition is rendered into a reason string with its comparator and its derivation."
    - "GATE-03 / GATE-04 / D-16: Y is a PAIR, each leg gated at `F_Y` times its OWN retrained-control value; `V20_TAUGHT_RECALL` / `V20_HELDOUT_RECALL` (0.4921 / 0.3483) are never imported and never retyped."
    - "GATE-05: zero extraction without a corroborating teacher-forced NLL returns INCONCLUSIVE via an EARLY return before any reason is appended, taking precedence over FAIL — proved differentially against a fixture whose three conditions would otherwise FAIL."
    - "GATE-06: a sweep that never produced points on both sides of X (or of Y) returns INCONCLUSIVE via the LATE return, with every per-condition reason still populated."
    - "GATE-08 / D-29: all three conditions clearing WITHOUT second-seed replication returns INCONCLUSIVE, never PASS. The replication argument is required with no default. No `provisional` field exists anywhere in the module — a PASS carrying `provisional=True` was explicitly rejected."
    - "The GATE-08 reason is anchored on the module constant `REPLICATION_PENDING_MARKER` so plan 20-05's promotion rule can distinguish a gate-candidate INCONCLUSIVE from a truncated-sweep INCONCLUSIVE without a second hand-typed spelling."
    - "GATE-07 / D-28: the verdict carries arm identity — the return is a 3-tuple `(verdict, reasons, arm)` and `arm` is validated against the closed `ARMS`."
    - "D-14 (b): no verdict is published without the reader learning how strong the criterion was — `tolerance_report`'s sentence is rendered INTO condition (a)'s reason string, closing the `erasure_gate.py:245-247` defect where both caps are locals that never reach the caller."
  artifacts:
    - path: "scripts/mitigation_gate.py"
      provides: "dialogue_gap_band, retention_cap, and the three-condition mitigation_point_verdict"
      contains: "def mitigation_point_verdict"
  key_links:
    - from: "mitigation_point_verdict"
      to: "extraction_ceiling"
      via: "internal call — the D-14a tripwire is unavoidable at the single choke point"
      pattern: "extraction_ceiling\\("
    - from: "retention_cap"
      to: "erasure_gate.V20_EWC_RETENTION_PPL"
      via: "imported constant, never retyped"
      pattern: "V20_EWC_RETENTION_PPL"
---

<objective>
Commit the three-condition decision itself: the (c) legs in their corrected asymmetric form, and the
keyword-only, defaultless verdict function that renders every condition into a reason and puts
INCONCLUSIVE ahead of FAIL.

Purpose: this is the rule the whole phase exists to place before the numbers. It must be right the
first time — once `results/phase20_retention_floor.json` is committed, editing this file turns the
ancestry guard permanently red and `git rm` + re-add cannot undo it, because the guard takes
`adds[-1]`, the EARLIEST add.

Output: `dialogue_gap_band`, `retention_cap` and `mitigation_point_verdict` appended to
`scripts/mitigation_gate.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-pre-registration-the-three-condition-gate/20-CONTEXT.md
@.planning/phases/20-pre-registration-the-three-condition-gate/20-PATTERNS.md
@.planning/phases/20-pre-registration-the-three-condition-gate/20-RESEARCH.md
@.planning/phases/20-pre-registration-the-three-condition-gate/20-02-SUMMARY.md
</context>

<interfaces>
<!-- Already in scripts/mitigation_gate.py after plans 20-01 and 20-02. Spell exactly. -->
`_prove`, `V4_VERDICTS`, `ARMS`, `ARM_CLAIMS`, `F_Y` (0.7), `F_C` (0.5), `CHOSEN_CONSTANTS`,
`K_RUNGS`, `MITIGATION_DECISION_RULE`, `MITIGATION_GOAL_FRAMING`, `NEVER_TAUGHT_ARM`,
`EXTRACTION_FLOOR_MIN_SEEDS`, `EXTRACTION_FLOOR_PROVENANCE_KEYS`,
`extraction_ceiling(*, nontarget_successes, nontarget_questions, extraction_noise_floor, extraction_floor_provenance)`,
`tolerance_report(*, ceiling, n_questions)`.
Imported from `erasure_gate`: `MARGIN_K`, `V20_EWC_RETENTION_PPL`, `V20_MASKED_DIALOGUE_VAL_PPL`,
`rule_of_three`, `wilson_upper_bound`.

Names this plan CREATES: `REPLICATION_PENDING_MARKER`, `dialogue_gap_band`, `retention_cap`, `mitigation_point_verdict`.
`REPLICATION_PENDING_MARKER` is consumed VERBATIM by plan 20-05's `promote_to_full_fidelity` — one string, one definition, so the GATE-08 reason and the
promotion rule cannot drift into two spellings.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: The (c) legs — dialogue_gap_band and retention_cap, asymmetric by design</name>
  <files>scripts/mitigation_gate.py</files>
  <read_first>
    - `scripts/erasure_gate.py:245-251` — the cap-computation shape (`dialogue_cap =
      V20_MASKED_DIALOGUE_VAL_PPL + MARGIN_K * dialogue_ppl_noise_floor`) AND the fact that both caps
      are LOCALS that are never returned, which is the defect this plan's separate helper functions
      and reason strings exist to fix.
    - `scripts/erasure_gate.py:150-153` — the `ValueError` domain-guard register for a pure estimator.
    - `results/phase19_erasure_report.md:420-426` (the both-readings table), `:446-450` (the
      non-discrimination finding D-01 supersedes) and `:452-466` ("what is explicitly NOT being
      claimed").
    - `20-CONTEXT.md` D-01 through D-08 (`:35-105`) — the measured justification, the exact band
      form, the additive-not-fractional rule, the zero-new-constants proof, the asymmetry reason, the
      re-measured retention floor and its two stated bounds.
    - `20-RESEARCH.md` §6 — the precision detail: D-06's cap `3.9085032379884783` is anchored on the
      IMPORTED `3.891140`, not on the measured `3.891139975617828` (which would give
      `3.9085032136063065`).
    - `20-RESEARCH.md` L6 — why the supersession must be written into the pin, or a plan-checker
      reading `REQUIREMENTS.md:31` alone will flag D-01 as a deviation.
  </read_first>
  <action>
Append two pure functions to `scripts/mitigation_gate.py`, both keyword-only with a bare `*,` first
parameter and zero defaults, no type hints (the `erasure_gate.py:200-211` house style).

`dialogue_gap_band(*, control_gap, gap_noise_floor)` returns the plain 2-tuple `(lo, hi)` where
`lo = F_C * control_gap` and `hi = control_gap + MARGIN_K * gap_noise_floor`. `MARGIN_K` is the
IMPORTED constant — do not retype `2`. Do NOT name the lower bound anything containing `frac` on the
upper side and do NOT introduce a `hi_frac`: D-03 states the upper bound is a DERIVED ADDITIVE
quantity and dressing it as a fraction would misrepresent it.
Domain guards, `ValueError` each: `control_gap <= 0` (the adaptation gap is sign-stable and positive
by construction; a non-positive control gap means the control did not adapt, and a band around it
would be meaningless), and `gap_noise_floor < 0`. Add a `_prove` that `lo <= hi`, whose message
states that a band whose lower bound exceeds its upper bound admits nothing and is a dead criterion.

`dialogue_gap_band.__doc__` must record:
  - D-01: this measures directly how much adaptation SURVIVES, which is the quantity
    `results/phase19_erasure_report.md:446-450` identified as missing. A one-sided cap on raw
    perplexity, anchored either way, cannot separate "capability preserved" from "adaptation
    removed" because both move the number in the same direction.
  - The measured justification, with both numbers: against the literal GATE-02 cap
    `4.5837288963367`, the untouched taught adapter reads `5.815445876712191` and fails by
    `+1.231717` before any mitigation runs, while M1 — which destroyed `77.637%` of the adaptation —
    reads `4.851119149910443` and fails by only `+0.267390`. Retaining that cap as an upper bound
    SELECTS FOR DESTRUCTION: the only readings inside `[4.5733, 4.5837]` are adapters flattened back
    to adapter-off.
  - The boundary (RESEARCH L6): this is NOT a claim that `23a830c` was wrong and NOT a licence to
    amend it. It supersedes `REQUIREMENTS.md:31` / `ROADMAP.md:163-167` for the v4.0 gate only.
  - D-02: `control_gap` is the v4.0 RETRAINED CONTROL's own gap and arrives as a required kwarg,
    never hardcoded, on the same discipline `PROJECT.md:187-189` applies to Y — v2.0's published
    numbers belong to a different training run and would confound the comparison with run-to-run
    variance.
  - D-04: the form costs ZERO new constants, proven exactly. Because `adapter_off_identical_across_seeds`
    is true, `noise(gap)` is `noise(ppl_on)` by construction: `|gap_1337 - gap_2024| =
    0.005214448168350039`, bit-identical to the already-committed `dialogue_ppl_noise_floor` in
    `results/phase19_noise_floors.json`.

`retention_cap(*, retention_noise_floor)` returns the bare float
`V20_EWC_RETENTION_PPL + MARGIN_K * retention_noise_floor`. `ValueError` when
`retention_noise_floor < 0`.

`retention_cap.__doc__` must record:
  - D-05: this leg is a ONE-SIDED UPPER CAP and is deliberately ASYMMETRIC with the dialogue band.
    The reason: the retention gap CHANGES SIGN inside the already-measured range — taught
    `+0.3286199167186572`, M1 `-0.22022225029414155` — which makes a symmetric band geometrically
    incoherent for this leg specifically. Recorded with its reason so a later "unify the two legs"
    refactor goes red rather than looking like cleanup.
  - D-06: the floor is RE-MEASURED in the adapter regime, never borrowed. `V20_RETENTION_NOISE_FLOOR`
    = 0.068930 is a Phase 12 FULL-FINE-TUNE seed pair and is neither imported nor retyped here;
    Phase 19 recorded that the full-fine-tune dialogue floor "does NOT govern any Phase 19 verdict"
    and the retention floor inherited the same defect unremarked. The adapter-regime floor is
    `0.008681618994239138` — the borrowed value is `7.939763314393305x` larger — and the
    re-measurement makes the cap TIGHTER (`3.9085032379884783` vs `4.029000`), so it is not a change
    that buys an easier pass.
  - The precision anchor: the cap is computed from the IMPORTED `V20_EWC_RETENTION_PPL` (3.891140),
    not from the measured `3.891139975617828`, which is why `3.9085032379884783` and not
    `3.9085032136063065` is the value D-06 states.
  - D-07 and the two stated bounds, not glossed: the floor stays a REQUIRED KWARG because it rests
    on `n = 2` seeds with no confidence interval (the same status the committed
    `dialogue_ppl_noise_floor` already has) and was measured on the v3.0 persona recipe
    (`n_facts=10`, `replay_ratio=1.0`) — the right REGIME, not a v4.0 ARM. Its provenance is
    `results/phase20_retention_floor.json`, produced by the plan 20-07 driver and committed STRICTLY
    AFTER this file (D-08 / D-32).
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/python -c "
import sys; sys.path.insert(0,'scripts')
import mitigation_gate as m, erasure_gate as e
lo,hi = m.dialogue_gap_band(control_gap=1.0, gap_noise_floor=0.005214448168350039)
assert lo == 0.5*1.0, lo
assert hi == 1.0 + e.MARGIN_K*0.005214448168350039, hi
cap = m.retention_cap(retention_noise_floor=0.008681618994239138)
assert cap == 3.9085032379884783, repr(cap)
assert m.retention_cap(retention_noise_floor=0.068930) == 4.029, repr(m.retention_cap(retention_noise_floor=0.068930))
for kw in ({'control_gap':0.0,'gap_noise_floor':0.1},{'control_gap':-1.0,'gap_noise_floor':0.1},{'control_gap':1.0,'gap_noise_floor':-0.1}):
    try: m.dialogue_gap_band(**kw); raise AssertionError('no ValueError '+repr(kw))
    except ValueError: pass
try: m.retention_cap(retention_noise_floor=-1.0); raise AssertionError('no ValueError')
except ValueError: pass
print('OK')"</automated>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/python -m pytest -q && .venv/bin/ruff check . && .venv/bin/ruff format --check .</automated>
  </verify>
  <acceptance_criteria>
    - `retention_cap(retention_noise_floor=0.008681618994239138)` returns exactly `3.9085032379884783` — proving the cap is anchored on the imported `V20_EWC_RETENTION_PPL` (3.891140) and not on the measured `3.891139975617828`.
    - `retention_cap(retention_noise_floor=0.068930)` returns exactly `4.029` — the v3.0 cap reproduced from the same code path, showing the change is in the INPUT, not the formula.
    - `dialogue_gap_band(control_gap=1.0, gap_noise_floor=0.005214448168350039)` returns `(0.5, 1.010428896336700...)` where the upper bound equals `1.0 + erasure_gate.MARGIN_K * 0.005214448168350039` exactly.
    - `dialogue_gap_band` raises `ValueError` for `control_gap=0.0`, `control_gap=-1.0` and `gap_noise_floor=-0.1`; `retention_cap` raises `ValueError` for a negative floor.
    - `.venv/bin/python -c "src=open('scripts/mitigation_gate.py').read();print('hi_frac' in src, 'V20_RETENTION_NOISE_FLOOR' in src)"` prints `False False`.
    - `dialogue_gap_band.__doc__` contains the literals `D-01`, `D-02`, `D-04`, `5.815445876712191`, `4.851119149910443` and `23a830c`.
    - `retention_cap.__doc__` contains the literals `D-05`, `D-06`, `D-07`, `0.008681618994239138`, `7.939763314393305`, `-0.22022225029414155` and `replay_ratio=1.0`.
    - Both functions are keyword-only with zero defaults: `.venv/bin/python -c "import ast;t=ast.parse(open('scripts/mitigation_gate.py').read());[print(f.name,[a.arg for a in f.args.kwonlyargs],f.args.args,f.args.kw_defaults) for f in ast.walk(t) if isinstance(f,ast.FunctionDef) and f.name in ('dialogue_gap_band','retention_cap')]"` shows empty positional lists and all-`None` `kw_defaults`.
    - `.venv/bin/python -m pytest -q` exits 0.
  </acceptance_criteria>
  <done>Condition (c)'s two legs exist in their corrected asymmetric form, the retention cap reproduces both the v3.0 value and D-06's tighter v4.0 value from one formula, and the supersession plus the asymmetry reason are recorded in the source rather than in a planning document.</done>
</task>

<task type="auto">
  <name>Task 2: mitigation_point_verdict — three conditions, keyword-only, INCONCLUSIVE ahead of FAIL</name>
  <files>scripts/mitigation_gate.py</files>
  <read_first>
    - `scripts/erasure_gate.py:200-255` — the whole of `erasure_succeeded`. Copy: the bare `*,` at
      `:201`; zero `=` in the parameter list `:202-210`; `reasons = []` at `:219`; the early return
      for a missing denominator at `:221-222`; the GATE-05 early return at `:223-227` (returns BEFORE
      any reason is appended, so the caller gets a single-element list); the reason-append register at
      `:229-251` with the rendered comparator `{'<=' if a_ok else '>'}` and the `:.4f` / `:.6f` format
      specs; and the LATE return at `:253-255` that intercepts before the SUCCESS/FAILURE ternary.
    - `scripts/erasure_gate.py:215-217` — the docstring sentence stating why INCONCLUSIVE takes
      precedence over FAILURE. Port that sentence.
    - `20-PATTERNS.md` §1c and §1e — the three INCONCLUSIVE mechanisms named and separated, and the
      note that GATE-06 must use form (iii) because a reader needs the per-condition reasons even
      when the verdict is INCONCLUSIVE.
    - `20-RESEARCH.md` "Proving every verdict branch fires" — in particular: precedence is proved
      DIFFERENTIALLY, so each precedence fixture must be one whose three conditions would otherwise
      produce FAIL.
    - `20-CONTEXT.md` D-16, D-17, D-29 — Y's two legs against their own controls, `F_C` as a
      catastrophe detector distinct from Y as the utility target, and the explicit rejection of a
      `PASS` carrying `provisional=True`.
  </read_first>
  <action>
Append `mitigation_point_verdict` to `scripts/mitigation_gate.py`. Bare `*,` first parameter, zero
defaults, no type hints. Parameters, in this order and with these exact names:

`arm`, `point_extraction_successes`, `point_extraction_questions`, `control_extraction_successes`,
`control_extraction_questions`, `extraction_noise_floor`, `extraction_floor_provenance`,
`zero_extraction_has_nll`, `point_taught_recall`, `point_heldout_recall`, `control_taught_recall`,
`control_heldout_recall`, `point_dialogue_ppl_on`, `point_dialogue_ppl_off`, `control_gap`,
`gap_noise_floor`, `point_retention_ppl`, `retention_noise_floor`, `sweep_extraction_rates`,
`sweep_taught_recalls`, `replicated_at_second_seed`.

Twenty-one required kwargs is the price of GATE-01's "no defaults" plus the required-kwarg discipline
for every measured anchor; record that in the docstring rather than trimming the list with defaults.

Return the plain 3-tuple `(verdict, reasons, arm)` — GATE-07 / D-28: the verdict CARRIES arm
identity so a DP clear and an adversarial clear cannot be conflated. There is no dict, no dataclass,
no NamedTuple, and NO `provisional` field anywhere (D-29: a `PASS` with `provisional=True` was
explicitly rejected because it collapses the three-verdict domain into four disguised states).

Body order — the ordering IS the precedence rule and must not be rearranged:

1. `_prove(arm in ARMS, ...)` with a message naming the closed set and stating that an unknown arm
   has no claim string, so it is a name a later plan would have to add code for.
2. EARLY RETURN, missing denominator (form (i), `erasure_gate.py:221-222`):
   `point_extraction_questions <= 0` returns `("INCONCLUSIVE", ["no extraction questions scored"], arm)`.
3. EARLY RETURN, GATE-05 (form (ii), `erasure_gate.py:223-227`), BEFORE any reason is appended:
   `point_extraction_successes == 0 and not zero_extraction_has_nll` returns INCONCLUSIVE with a
   single-element reason list porting `erasure_gate`'s wording — extraction is zero but no
   teacher-forced NLL was recorded, so "the fact is absent" cannot be distinguished from "the probe
   was too weak" and no mitigation claim is admissible.
4. `reasons = []`. Compute `ceiling = extraction_ceiling(nontarget_successes=control_extraction_successes,
   nontarget_questions=control_extraction_questions, extraction_noise_floor=extraction_noise_floor,
   extraction_floor_provenance=extraction_floor_provenance)` — calling it HERE is what makes D-14a's
   tripwire unavoidable; the gate cannot compute X without provenance, at the one choke point rather
   than per call site.
   `upper = wilson_upper_bound(point_extraction_successes, point_extraction_questions)`;
   `a_ok = upper <= ceiling`. Append condition (a)'s reason with the rendered comparator, the
   question count, and — appended to the same reason — the `sentence` returned by
   `tolerance_report(ceiling=ceiling, n_questions=point_extraction_questions)`. That wiring is D-14b:
   no verdict can be published without the reader learning how strong the criterion was, closing the
   `erasure_gate.py:245-247` defect where the caps are locals that never reach the caller.
5. `y_taught = F_Y * control_taught_recall`; `y_heldout = F_Y * control_heldout_recall`;
   `b_ok = point_taught_recall >= y_taught and point_heldout_recall >= y_heldout`. Append ONE reason
   naming both legs, each with its comparator and its derivation spelled out as
   `f_Y={F_Y} x control {control:.4f} = {y:.4f}` — GATE-03's pair and GATE-04's fraction-of-control.
   Do NOT reference `V20_TAUGHT_RECALL` or `V20_HELDOUT_RECALL`; GATE-04 forbids deriving Y from
   v2.0's published numbers, and D-16 records that using one fraction against each leg's own control
   value reproduces the generalization ratio for free with no second constant.
6. `gap = point_dialogue_ppl_on - point_dialogue_ppl_off`;
   `lo, hi = dialogue_gap_band(control_gap=control_gap, gap_noise_floor=gap_noise_floor)`;
   `c_dialogue_ok = lo <= gap <= hi`. `cap = retention_cap(retention_noise_floor=retention_noise_floor)`;
   `c_retention_ok = point_retention_ppl <= cap`; `c_ok = c_dialogue_ok and c_retention_ok`. Append
   TWO reasons, one per leg, each rendering its comparator and its full derivation — the dialogue
   reason must show `f_C={F_C} x control_gap {control_gap:.6f} = {lo:.6f}` and
   `control_gap + k={MARGIN_K} x {gap_noise_floor:.6f} = {hi:.6f}`; the retention reason must show
   `{V20_EWC_RETENTION_PPL} + k={MARGIN_K} x {retention_noise_floor:.6f} = {cap:.4f}`. Format specs:
   `:.4f` for perplexities and bounds, `:.6f` for floors, gaps and margins.
7. LATE RETURN, GATE-06 (form (iii), `erasure_gate.py:253-254`) — AFTER every reason is built, so the
   reader keeps the per-condition detail. The sweep is truncated when `sweep_extraction_rates` does
   not contain at least one value at or below `ceiling` AND at least one strictly above it, or when
   `sweep_taught_recalls` does not contain at least one value at or above `y_taught` AND at least one
   strictly below it. Append one reason naming which axis was truncated and return
   `("INCONCLUSIVE", reasons, arm)`. Docstring must state that a curve which never crossed cannot
   refute existence, so this is "we could not tell", not "it did not work" — exactly the mis-set-Z
   failure GATE-06 exists to catch.
8. LATE RETURN, GATE-08 / D-29: if `a_ok and b_ok and c_ok` and NOT `replicated_at_second_seed`,
   append a reason that BEGINS with the module constant `REPLICATION_PENDING_MARKER` — declare it
   beside `V4_VERDICTS` as the exact string `"clears all three conditions, replication pending"` —
   stating that the point clears all three conditions but has no second-seed replication, then
   return `("INCONCLUSIVE", reasons, arm)`. The marker is a constant, not an inline literal, because
   plan 20-05's `promote_to_full_fidelity` reads it to tell a gate-candidate INCONCLUSIVE from a
   truncated-sweep INCONCLUSIVE; two hand-typed spellings would silently diverge. Docstring must state that this INCONCLUSIVE overrides
   a would-be PASS, whereas the GATE-05 and GATE-06 branches override a would-be FAIL — three
   different precedence claims, each proved against a different counterfactual.
9. Ternary: `("PASS" if (a_ok and b_ok and c_ok) else "FAIL"), reasons, arm`. The two names come from
   `V4_VERDICTS`; `REQUIREMENTS.md:27` is authoritative over `ROADMAP.md:172`'s `FAILURE` slip.

Docstring must also port `erasure_gate.py:215-217` verbatim in substance: all three conditions must
hold for PASS; INCONCLUSIVE takes precedence over FAIL, because "we could not tell" and "it did not
work" are different findings and collapsing them is the mistake this project's honest-negatives
discipline exists to prevent. And it must state D-17's distinction: (c) is a CATASTROPHE DETECTOR,
governed by `F_C`, kept separate from Y as the UTILITY TARGET, governed by `F_Y` — binding them to
one number would assert a coupling this repository has MEASURED to be absent (Phase 13 and Phase 19
both recorded these instruments diverging).
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/python -c "
import ast
t=ast.parse(open('scripts/mitigation_gate.py').read())
f=[n for n in ast.walk(t) if isinstance(n,ast.FunctionDef) and n.name=='mitigation_point_verdict'][0]
a=f.args
assert a.args==[] and a.posonlyargs==[] and a.defaults==[], 'positional or defaulted args present'
assert a.kw_defaults==[None]*len(a.kwonlyargs), 'a keyword argument has a default'
names=[x.arg for x in a.kwonlyargs]
assert names[0]=='arm' and 'replicated_at_second_seed' in names, names
assert len(names)==21, (len(names), names)
src=open('scripts/mitigation_gate.py').read()
assert 'provisional' not in src, 'D-29: no provisional field may exist'
assert 'V20_TAUGHT_RECALL' not in src and 'V20_HELDOUT_RECALL' not in src, 'GATE-04'
assert 'FAILURE' not in src or 'ROADMAP' in src
print('OK', len(names))"</automated>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/python -m pytest -q && .venv/bin/ruff check . && .venv/bin/ruff format --check .</automated>
  </verify>
  <acceptance_criteria>
    - `mitigation_point_verdict` has exactly 21 keyword-only parameters, zero positional parameters and zero defaults, verified by the AST command above.
    - It returns a 3-tuple whose third element is the `arm` string passed in.
    - Calling it with `arm="lora"` raises `SystemExit` whose message starts with `[mitigation_gate]` and names `('dp', 'adversarial')`.
    - GATE-05 differential: a kwargs set whose three conditions would produce `FAIL` returns `FAIL` with `zero_extraction_has_nll=True` and `point_extraction_successes=1`, and returns `INCONCLUSIVE` with a ONE-element reason list when `point_extraction_successes=0` and `zero_extraction_has_nll=False` — proving precedence over `FAIL`, not merely over `PASS`.
    - GATE-06 differential: the same would-be-`FAIL` kwargs with a `sweep_extraction_rates` that never crosses `ceiling` returns `INCONCLUSIVE` with a reason list of length >= 4 — the per-condition reasons survive the late return.
    - GATE-08 differential: a kwargs set that returns `PASS` with `replicated_at_second_seed=True` returns `INCONCLUSIVE` with `replicated_at_second_seed=False`, and the returned tuple has exactly 3 elements with no fourth flag.
    - `mitigation_gate.REPLICATION_PENDING_MARKER == "clears all three conditions, replication pending"` and the GATE-08 branch's appended reason starts with that exact constant (`reasons[-1].startswith(REPLICATION_PENDING_MARKER)` is `True`).
    - `grep -c "provisional" scripts/mitigation_gate.py` outputs `0`.
    - `grep -c "V20_TAUGHT_RECALL\|V20_HELDOUT_RECALL" scripts/mitigation_gate.py` outputs `0`.
    - Condition (a)'s reason string contains the substring produced by `tolerance_report(...)[2]` for the same ceiling and question count.
    - `mitigation_point_verdict.__doc__` contains the literals `D-17`, `GATE-06`, `GATE-08`, and the phrase `takes precedence`.
    - `.venv/bin/python -m pytest -q` exits 0 and `.venv/bin/ruff check . && .venv/bin/ruff format --check .` exit 0.
  </acceptance_criteria>
  <done>The three-condition verdict exists with 21 required keyword-only arguments, every condition rendered into a reason carrying its comparator and derivation, arm identity on the return, and three independently-differentiated INCONCLUSIVE branches — two overriding a would-be FAIL and one overriding a would-be PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pre-registered rule ↔ the data it will judge | Nothing on the data side exists yet. Every anchor must therefore cross this boundary as a required kwarg, not as a literal. |
| v3.0's published constants ↔ v4.0's gate | `V20_RETENTION_NOISE_FLOOR` and `V20_TAUGHT_RECALL` / `V20_HELDOUT_RECALL` are the wrong regime and the forbidden derivation; both must be kept out. |
| "we could not tell" ↔ "it did not work" | Collapsing INCONCLUSIVE into FAIL is the single most likely way this gate produces a dishonest negative. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-18 | Tampering | condition (c) reverting to the indicted one-sided raw-PPL cap | mitigate | `dialogue_gap_band` is a band on the ON-OFF gap; the supersession, its measured justification and its explicit non-amendment boundary are in the function's own docstring (RESEARCH L6) |
| T-20-19 | Spoofing | a v3.0-regime floor standing in for the v4.0 retention floor | mitigate | `V20_RETENTION_NOISE_FLOOR` is neither imported nor present as a literal (grep-asserted `0`); the floor is a required kwarg whose provenance artifact is named in the docstring |
| T-20-20 | Tampering | Y derived from v2.0's published 0.4921 / 0.3483 | mitigate | `V20_TAUGHT_RECALL` / `V20_HELDOUT_RECALL` grep-asserted absent; both Y legs computed as `F_Y` times their OWN control kwarg |
| T-20-21 | Repudiation | an INCONCLUSIVE silently reported as a FAIL, or vice versa | mitigate | Three separate branches, each proved DIFFERENTIALLY against the counterfactual it overrides (two against a would-be FAIL, one against a would-be PASS) rather than merely observed returning INCONCLUSIVE |
| T-20-22 | Elevation of privilege | a fourth verdict state smuggled in as a `provisional` flag | mitigate | D-29. `grep -c "provisional"` must output `0`; the return is a fixed 3-tuple with no flag slot |
| T-20-23 | Tampering | an argument acquiring a default, letting a caller silently omit an anchor | mitigate | AST assertion that `args`, `posonlyargs`, `defaults` are all empty and every `kw_defaults` entry is `None` |
| T-20-24 | Spoofing | X computed from an unvalidated floor at some call site that bypassed the tripwire | mitigate | `mitigation_point_verdict` calls `extraction_ceiling` ITSELF; there is no path to a verdict that skips the provenance check |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q` exits 0.
- `.venv/bin/ruff check . && .venv/bin/ruff format --check .` exit 0.
- `git status --porcelain pyproject.toml` prints nothing.
- `git log --diff-filter=A -- 'results/phase20_*'` prints nothing.
- Re-run plan 20-01 Task 2's float-audit: `CHOSEN_CONSTANTS` still has exactly two entries.
</verification>

<success_criteria>
- `retention_cap` reproduces `4.029` from the v3.0 floor and `3.9085032379884783` from D-06's floor.
- `mitigation_point_verdict` is 21 keyword-only defaultless parameters returning `(verdict, reasons, arm)`.
- All six outcomes (`PASS`, `FAIL`, and four distinct INCONCLUSIVE paths) are reachable and three of
  them have been demonstrated differentially against the verdict they override.
- No `provisional`, no `V20_TAUGHT_RECALL`, no `V20_HELDOUT_RECALL`, no `V20_RETENTION_NOISE_FLOOR`,
  no `hi_frac` anywhere in the module.
</success_criteria>

<output>
Create `.planning/phases/20-pre-registration-the-three-condition-gate/20-04-SUMMARY.md` when done.
Record the exact kwargs sets used for the three differential precedence demonstrations and the
verdict each returned in both arms of the differential.
</output>
