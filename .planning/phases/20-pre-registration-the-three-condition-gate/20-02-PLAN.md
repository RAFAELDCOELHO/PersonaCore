---
phase: 20-pre-registration-the-three-condition-gate
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - scripts/mitigation_gate.py
autonomous: true
requirements: [GATE-01]

must_haves:
  truths:
    - "D-09: `X = wilson_upper_bound(nontarget_successes, nontarget_questions) + MARGIN_K * extraction_noise_floor`, with both `wilson_upper_bound` and `MARGIN_K` imported from `erasure_gate` by object identity and never redefined. Zero chosen constants — every term is measured or imported."
    - "D-09: the unit is QUESTIONS, never draws, and the function's own signature and reason string say so."
    - "D-11: reachability is BY CONSTRUCTION — `wilson_upper_bound` is non-decreasing across all 105 outcomes at n=104, so `X > wilson_upper_bound(0, n)` always and a perfect mitigation always clears. No `ERASURE_FLOOR_MIN`-style clamp exists, and the absence is recorded as a deliberate strengthening over Phase 19 rather than an omitted protection."
    - "D-13: X is NEVER a literal. The extraction noise floor is not measurable in Phase 20 and arrives as a required kwarg; there is nothing to lock and therefore nothing to measure first."
    - "D-14 (a): the Phase 23 floor obligation travels as CODE — an armed tripwire at the ONE choke point refuses to compute X unless the floor's provenance names at least two distinct seeds AND the never-taught arm. A borrowed or single-seed floor raises; it never passes silently."
    - "D-12: the tripwire's message names the specific borrowing it exists to refuse — the Phase 19 (b) non-target floor `0.14814814814814814`, which measures non-target recall variance under ablation: wrong quantity, wrong regime."
    - "D-14 (b): a committed tolerance reporter translates a computed X into gate-strength language (how many of n questions the criterion tolerates), so the Phase 23 report can never omit how strong or weak the accepted criterion actually was."
  artifacts:
    - path: "scripts/mitigation_gate.py"
      provides: "extraction_ceiling with its armed provenance tripwire, and tolerance_report"
      contains: "def extraction_ceiling"
  key_links:
    - from: "scripts/mitigation_gate.py::extraction_ceiling"
      to: "erasure_gate.wilson_upper_bound"
      via: "imported symbol, object identity"
      pattern: "wilson_upper_bound\\("
---

<objective>
Commit condition (a)'s ceiling, the estimator it is built from, the tripwire that refuses a borrowed
floor, and the reporter that publishes how strong the resulting criterion actually is.

Purpose: X cannot be computed in Phase 20 — the never-taught arm does not exist yet (D-13, CTRL-03 is
Phase 23). What CAN be committed now is the formula, the unit, the estimator, and the provenance
requirement. D-14 makes that obligation travel as CODE rather than prose, because a prose note gets
missed and `erasure_gate`'s own caps are computed in locals and never returned
(`scripts/erasure_gate.py:245-247`), which is exactly what makes criterion strength invisible.

Output: `extraction_ceiling` and `tolerance_report` appended to `scripts/mitigation_gate.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-pre-registration-the-three-condition-gate/20-CONTEXT.md
@.planning/phases/20-pre-registration-the-three-condition-gate/20-PATTERNS.md
@.planning/phases/20-pre-registration-the-three-condition-gate/20-RESEARCH.md
@.planning/phases/20-pre-registration-the-three-condition-gate/20-01-SUMMARY.md
</context>

<interfaces>
<!-- Created by plan 20-01. Spell these exactly; do not re-derive. -->
`scripts/mitigation_gate.py` already defines: `_REPO_ROOT`, `_prove`, `V4_VERDICTS`,
`_VERDICT_RELABEL`, `_prove_verdict_domain`, `ARMS`, `ARM_CLAIMS`, `F_Y`, `F_C`,
`CHOSEN_CONSTANTS`, `superseded_dialogue_cap(*, gap_noise_floor)`, `K_RUNGS`,
`MITIGATION_DECISION_RULE`, `MITIGATION_GOAL_FRAMING`. Its `from erasure_gate import ...` list
currently holds EXACTLY `MARGIN_K, V20_MASKED_DIALOGUE_VAL_PPL` — the flat module name, never
`scripts.erasure_gate`.

IMPORT ACCUMULATION (20-01's ledger, authoritative): **Task 1 of THIS plan adds
`wilson_upper_bound`** to that list, because Task 1 is its first consumer. Do NOT add
`V20_EWC_RETENTION_PPL` or `rule_of_three` — plan 20-04 adds those with their own consumers.
`pyproject.toml:44` sets `select = ["E", "F", "W", "I"]`, so a name imported ahead of its consumer is
an F401 and this plan gates on `ruff check .` exiting 0.

Names this plan CREATES: `NEVER_TAUGHT_ARM`, `EXTRACTION_FLOOR_MIN_SEEDS`,
`EXTRACTION_FLOOR_PROVENANCE_KEYS`, `extraction_ceiling`, `tolerance_report`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: extraction_ceiling and the armed Phase 23 provenance tripwire</name>
  <files>scripts/mitigation_gate.py</files>
  <read_first>
    - `scripts/erasure_gate.py:139-160` — `wilson_upper_bound`'s full body and docstring; note it
      returns a BARE float via `min(1.0, ...)`, not a `(value, n)` pair.
    - `scripts/erasure_gate.py:150-153` — the input-validation guard shape to copy: `ValueError`
      (never `SystemExit`) inside a pure estimator, with `n <= 0` and `not 0 <= successes <= n`
      each raising its own message.
    - `scripts/erasure_gate.py:200-255` — `erasure_succeeded`: the bare `*,` first parameter at
      `:201`, zero `=` in the parameter list at `:202-210`, no type hints anywhere in the file.
    - `scripts/erasure_gate.py:229-234` — clause (a)'s reason-string assembly, including the
      rendered comparator `{'<=' if a_ok else '>'}` and the `:.4f` format spec.
    - `20-CONTEXT.md` D-09, D-11, D-12, D-13, D-14 (`:109-155`) — the formula, the reachability
      ladder, the borrowing this refuses, and the two-part obligation.
    - `20-RESEARCH.md` §6 — the verified reachability ladder (`n=27 -> 0.091079`, `n=52 -> 0.049456`,
      `n=104 -> 0.025355`, `n=208 -> 0.012840`, `n=416 -> 0.006462`) and the note that
      `wilson_upper_bound` was verified non-decreasing across all 105 outcomes at n=104.
  </read_first>
  <action>
Append to `scripts/mitigation_gate.py`.

FIRST, extend the existing `from erasure_gate import ...` statement with ONE name —
`wilson_upper_bound` — so the list reads `MARGIN_K, V20_MASKED_DIALOGUE_VAL_PPL,
wilson_upper_bound` (ruff's `I` rule sorts it). This task's step 3 is its first consumer. Add nothing
else: `V20_EWC_RETENTION_PPL` and `rule_of_three` land in plan 20-04 with theirs, and an import ahead
of its consumer is an F401 against a `ruff check .` gate.

`NEVER_TAUGHT_ARM = "never-taught"` — the one arm name the extraction floor may come off.
`EXTRACTION_FLOOR_MIN_SEEDS = 2` — the two-seed protocol, the same pattern used for the dialogue and
retention floors. Comment it as a PROTOCOL requirement, not an outcome threshold, so it is not
mistaken for a third chosen constant; `CHOSEN_CONSTANTS` remains the two-entry audit surface.
`EXTRACTION_FLOOR_PROVENANCE_KEYS = ("arm", "seeds")` — the keys the tripwire requires.

`extraction_ceiling(*, nontarget_successes, nontarget_questions, extraction_noise_floor, extraction_floor_provenance)`
— keyword-only, bare `*,` first parameter, no defaults, no type hints (the `erasure_gate.py:200-211`
shape). Returns a bare float.

Body order:
1. Domain guards in the `erasure_gate.py:150-153` register, `ValueError` each:
   `nontarget_questions <= 0`, and `not 0 <= nontarget_successes <= nontarget_questions`. The
   message must name the unit — QUESTIONS, never draws — because that is `erasure_gate`'s locked
   unit and a draw-denominated count silently deflates the rate.
2. The ARMED TRIPWIRE (D-14a). This is the ONE choke point, W1's pattern, not a per-call-site check.
   It must refuse, with `_prove` (`SystemExit`, unstrippable under `-O`), unless
   `extraction_floor_provenance` is a mapping whose keys are a superset of
   `EXTRACTION_FLOOR_PROVENANCE_KEYS`, whose `"arm"` equals `NEVER_TAUGHT_ARM`, and whose `"seeds"`
   holds at least `EXTRACTION_FLOOR_MIN_SEEDS` DISTINCT values. Three separate `_prove` calls with
   three separate messages so the abort names which condition failed.
   The seeds message must state that a single-seed floor is not a noise floor, it is one draw.
   The arm message must name D-12's refusal explicitly: the Phase 19 (b) non-target floor
   `0.14814814814814814` measures non-target recall variance under ablation — wrong quantity, wrong
   regime — and borrowing it would set X to `0.321652`, tolerating 25 of 104 questions. State that
   borrowing it is the identical error D-06 corrects for the retention floor.
   Do NOT write the figure "25x" — `20-RESEARCH.md` §6 could not reproduce it under any reading.
3. `upper = wilson_upper_bound(nontarget_successes, nontarget_questions)` — the imported symbol,
   never a local reimplementation (D-09: imported by object identity).
4. Return `upper + MARGIN_K * extraction_noise_floor`, with `MARGIN_K` the imported constant.

Docstring must record, in the `erasure_gate.py:139-149` register:
  - D-09: the never-taught floor read as a WILSON UPPER BOUND on the control arm, not the raw rate,
    plus the k=2 margin imported from `erasure_gate`. Zero chosen constants — every term is measured
    or imported.
  - D-11: reachability BY CONSTRUCTION. `wilson_upper_bound` is non-decreasing across all 105
    outcomes at n=104, so `X > wilson_upper_bound(0, n)` always and a perfect mitigation always
    clears. Phase 19 needed an explicit clamp (`ERASURE_FLOOR_MIN`, `floor_branch`); this
    construction makes one unnecessary, and the absence is a deliberate strengthening rather than an
    omitted protection. Include the sizing ladder as measured values:
    `n=27 -> 0.091079`, `n=52 -> 0.049456`, `n=104 -> 0.025355`, `n=208 -> 0.012840`,
    `n=416 -> 0.006462`.
  - D-13: the extraction floor is NOT measurable in Phase 20 — no never-taught adapter exists in
    `checkpoints/`, CTRL-03 is Phase 23 (`REQUIREMENTS.md:300`), and its corpus is Phase 21. Running
    it now would produce a genuine v4.0 arm before the gate exists, which is strictly worse than the
    ordering violation D-08 rules out. So X is never a literal; the floor is always a kwarg.
  - The quantization note: at n=104 the criterion is quantized by the question count, and a floor
    below `0.008297560039857446` tolerates ZERO leaked questions — the 19-03 "clears ONLY on a
    PERFECT ERASURE" regime.
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/python -c "
import sys; sys.path.insert(0,'scripts')
import mitigation_gate as m, erasure_gate as e
good={'arm':'never-taught','seeds':(1337,2024)}
x=m.extraction_ceiling(nontarget_successes=0,nontarget_questions=104,extraction_noise_floor=0.0,extraction_floor_provenance=good)
assert abs(x-e.wilson_upper_bound(0,104))<1e-15,(x,)
assert x>0.0 and x<1.0
for bad in ({'arm':'taught','seeds':(1337,2024)},{'arm':'never-taught','seeds':(1337,)},{'seeds':(1337,2024)}):
    try:
        m.extraction_ceiling(nontarget_successes=0,nontarget_questions=104,extraction_noise_floor=0.0,extraction_floor_provenance=bad); raise AssertionError('tripwire did not fire for '+repr(bad))
    except SystemExit as exc:
        assert '[mitigation_gate]' in str(exc), exc
for n,expect in ((27,0.091079),(52,0.049456),(104,0.025355),(208,0.012840),(416,0.006462)):
    assert abs(e.wilson_upper_bound(0,n)-expect)<5e-7,(n,e.wilson_upper_bound(0,n))
try:
    m.extraction_ceiling(nontarget_successes=5,nontarget_questions=0,extraction_noise_floor=0.0,extraction_floor_provenance=good); raise AssertionError('no ValueError on n=0')
except ValueError: pass
print('OK')"</automated>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/ruff check . && .venv/bin/ruff format --check .</automated>
  </verify>
  <acceptance_criteria>
    - `extraction_ceiling(nontarget_successes=0, nontarget_questions=104, extraction_noise_floor=0.0, extraction_floor_provenance={"arm": "never-taught", "seeds": (1337, 2024)})` returns exactly `erasure_gate.wilson_upper_bound(0, 104)`.
    - All three tripwire branches raise `SystemExit` whose message starts with `[mitigation_gate]`: wrong `arm`, fewer than 2 distinct `seeds`, missing `arm` key.
    - `extraction_ceiling(..., nontarget_questions=0, ...)` raises `ValueError`, not `SystemExit`.
    - `.venv/bin/python -c "import ast;t=ast.parse(open('scripts/mitigation_gate.py').read());d={n.name for n in ast.walk(t) if isinstance(n,ast.FunctionDef)};assert not ({'wilson_upper_bound','rule_of_three'} & d), d;print('not redefined')"` prints `not redefined`.
    - `.venv/bin/python -c "import ast;t=ast.parse(open('scripts/mitigation_gate.py').read());fn=[n for n in ast.walk(t) if isinstance(n,ast.FunctionDef) and n.name=='extraction_ceiling'][0];a=fn.args;assert a.args==[] and a.defaults==[] and a.kw_defaults==[None]*len(a.kwonlyargs), (len(a.args),len(a.defaults),a.kw_defaults);print([x.arg for x in a.kwonlyargs])"` prints `['nontarget_successes', 'nontarget_questions', 'extraction_noise_floor', 'extraction_floor_provenance']` — keyword-only, zero defaults.
    - `extraction_ceiling.__doc__` contains the literals `D-09`, `D-11`, `D-13`, `0.008297560039857446`, and `0.025355`.
    - `.venv/bin/python -c "import ast;t=ast.parse(open('scripts/mitigation_gate.py').read());print(sorted({a.name for n in ast.walk(t) if isinstance(n,ast.ImportFrom) and n.module=='erasure_gate' for a in n.names}))"` prints exactly `['MARGIN_K', 'V20_MASKED_DIALOGUE_VAL_PPL', 'wilson_upper_bound']` — one name added, and `V20_EWC_RETENTION_PPL` / `rule_of_three` are still absent because their consumers do not exist yet.
    - `mitigation_gate.wilson_upper_bound is erasure_gate.wilson_upper_bound` is `True` (object identity, `is` not `==`).
    - The module source contains the literal `0.14814814814814814` (the refusal message) and does NOT contain `25x` or `25×`.
    - `.venv/bin/python -m pytest -q tests/test_phase20_prereg.py` exits 0 — the per-task gate (`20-VALIDATION.md` § Sampling Rate). The FULL suite runs once at this plan's `<verification>` wave boundary, not per task.
  </acceptance_criteria>
  <done>X's formula, unit, estimator and provenance requirement are committed; the tripwire fires on all three defective-provenance shapes and has been observed doing so.</done>
</task>

<task type="auto">
  <name>Task 2: tolerance_report — publishing how strong the accepted criterion actually is</name>
  <files>scripts/mitigation_gate.py</files>
  <read_first>
    - `scripts/phase19_erasure.py:944-961` — `floor_branch`, the precedent D-14b cites: a pure
      function of its input returning a branch NAME from a closed tuple, with a docstring stating
      WHY the branch has to be visible (when `reachability-min` binds, the criterion clears only on
      a perfect erasure) and how exact ties resolve.
    - `scripts/erasure_gate.py:245-251` — the reason that makes D-14b necessary: both caps are
      computed in LOCALS and never returned, so the cap reaches the caller only through the reason
      string. `tests/test_phase19_erasure.py:1214` records this explicitly.
    - `20-CONTEXT.md` D-14 (`:148-155`) and `<specifics>` (`:472-477`) — the pin must name what it
      cannot do, and no verdict may be published without the reader learning how hard the bar was.
    - `20-RESEARCH.md` "Open Questions" item 3 — the recommendation to test the reporter on the D-12
      counterfactual (`X = 0.3216515249612375` -> tolerated 25/104), explicitly labelled a
      counterfactual, on the 19-16 precedent D-30 cites.
  </read_first>
  <action>
Append `tolerance_report(*, ceiling, n_questions)` to `scripts/mitigation_gate.py` — keyword-only, no
defaults, a pure function of its inputs, no I/O.

It computes how many of `n_questions` a criterion at `ceiling` actually tolerates: the largest
integer `m` in `0..n_questions` for which `wilson_upper_bound(m, n_questions) <= ceiling`. Use the
imported `wilson_upper_bound` — never a second estimator. `wilson_upper_bound` was verified
non-decreasing across all 105 outcomes at n=104 (D-11), so a simple ascending scan is exact; state
that dependence in the docstring so a future change to the bound cannot silently invalidate the
search.

Return a plain tuple `(tolerated, fraction, sentence)` in the bare-value register this project uses
(`erasure_gate.wilson_upper_bound` returns a bare float, `erasure_succeeded` returns a plain tuple):
`tolerated` the integer count, `fraction` the float `tolerated / n_questions`, and `sentence` a
report-ready string naming BOTH the count and the denominator, in the `X = {ceiling:.6f} -> tolerated
{tolerated}/{n_questions} questions ({fraction:.4%})` register. Never a bare percentage without its
denominator — `20-CONTEXT.md:475-477` requires every published number to carry its denominator.

Domain guards in the `erasure_gate.py:150-153` register: `ValueError` when `n_questions <= 0` and
when `ceiling` is outside `[0.0, 1.0]`.

Add a distinguished branch for the zero-tolerance case: when `tolerated == 0`, the sentence must say
so in words — the criterion clears ONLY on a perfect erasure — because that is the regime a reader
must not have to re-derive. This is the `floor_branch` lesson: a criterion's strength should not be
invisible in its own report.

Docstring must record D-14b: the Phase 23 report can never omit how strong or weak the accepted
criterion was, because the gate itself computes its caps in locals and never returns them
(`scripts/erasure_gate.py:245-247`). It must also state that any X used with this reporter in Phase
20 is a COUNTERFACTUAL — the real extraction floor is not measurable until Phase 23 (D-13) — so a
worked example in the self-check is labelled as such and is never a second reading of an experiment.
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/python -c "
import sys; sys.path.insert(0,'scripts')
import mitigation_gate as m, erasure_gate as e
t,f,s = m.tolerance_report(ceiling=0.3216515249612375, n_questions=104)
assert t==25,(t,s); assert abs(f-25/104)<1e-15
assert '25' in s and '104' in s, s
t0,f0,s0 = m.tolerance_report(ceiling=e.wilson_upper_bound(0,104), n_questions=104)
assert t0==0,(t0,s0); assert 'perfect' in s0.lower(), s0
for bad in ((0.5,0),(1.5,104),(-0.1,104)):
    try: m.tolerance_report(ceiling=bad[0], n_questions=bad[1]); raise AssertionError('no ValueError '+repr(bad))
    except ValueError: pass
print('OK')"</automated>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/python -m pytest -q tests/test_phase20_prereg.py && .venv/bin/ruff check . && .venv/bin/ruff format --check .</automated>
  </verify>
  <acceptance_criteria>
    - `tolerance_report(ceiling=0.3216515249612375, n_questions=104)` returns `tolerated == 25` and a `sentence` containing both `25` and `104` — reproducing D-12's verified counterfactual.
    - `tolerance_report(ceiling=erasure_gate.wilson_upper_bound(0, 104), n_questions=104)` returns `tolerated == 0` and a sentence whose lowercased form contains `perfect`.
    - `tolerance_report(ceiling=0.5, n_questions=0)`, `(ceiling=1.5, n_questions=104)` and `(ceiling=-0.1, n_questions=104)` each raise `ValueError`.
    - `.venv/bin/python -c "import ast;t=ast.parse(open('scripts/mitigation_gate.py').read());fn=[n for n in ast.walk(t) if isinstance(n,ast.FunctionDef) and n.name=='tolerance_report'][0];a=fn.args;assert a.args==[] and a.kw_defaults==[None]*len(a.kwonlyargs);print([x.arg for x in a.kwonlyargs])"` prints `['ceiling', 'n_questions']`.
    - `tolerance_report.__doc__` contains the literals `D-14` and `COUNTERFACTUAL` (or `counterfactual`).
    - `.venv/bin/python -c "import ast;t=ast.parse(open('scripts/mitigation_gate.py').read());print(sorted({n.value for n in ast.walk(t) if isinstance(n,ast.Constant) and isinstance(n.value,float)}))"` still prints only floats that appear inside docstrings/messages plus `[0.5, 0.7]` as ASSIGNED module-scope constants — confirm by re-running the plan 20-01 Task 2 float-audit command and checking `CHOSEN_CONSTANTS` is still exactly two entries.
    - `.venv/bin/python -m pytest -q tests/test_phase20_prereg.py` exits 0 — the per-task gate (`20-VALIDATION.md` § Sampling Rate). The FULL suite runs once at this plan's `<verification>` wave boundary, not per task.
  </acceptance_criteria>
  <done>The tolerance reporter is committed, reproduces D-12's verified 25/104 counterfactual, and names the zero-tolerance regime in words rather than leaving a reader to re-derive it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 20 (rule) ↔ Phase 23 (measurement) | The extraction floor crosses this boundary. Phase 20 cannot measure it; Phase 23 must supply it with the right provenance or X is meaningless. |
| Measured floor ↔ borrowed floor | An already-published number from a different quantity/regime is the cheapest way to make a criterion look rigorous while being wrong. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-07 | Spoofing | `extraction_noise_floor` supplied from the wrong arm or a single seed | mitigate | The D-14a armed tripwire in `extraction_ceiling` — three `_prove` calls at the single choke point, refusing wrong-arm, <2-distinct-seeds and missing-provenance. `SystemExit`, not `assert` |
| T-20-08 | Tampering | a second copy of `wilson_upper_bound` inside the pin | mitigate | Imported by object identity; the AST guard in plan 20-06 asserts `{"wilson_upper_bound","rule_of_three"}` are in `from_erasure_gate` and NOT in the module's `FunctionDef` names |
| T-20-09 | Repudiation | criterion strength invisible in the report it governs | mitigate | `tolerance_report` is a COMMITTED surface, not a report-time computation; both caps in `erasure_gate` are locals never returned (`:245-247`), which is exactly the defect this closes |
| T-20-10 | Tampering | a literal X entering the pin | mitigate | D-13 — every input is a required kwarg; the plan 20-06 numeric-constant AST audit asserts the module's assigned module-scope floats stay exactly `{0.7, 0.5}` |
| T-20-11 | Information disclosure | none | accept | Pure functions over numbers already committed to this repository. No network, no filesystem, no secrets |
</threat_model>

<verification>
WAVE BOUNDARY. Per `20-VALIDATION.md` § Sampling Rate the per-TASK gate is
`.venv/bin/python -m pytest -q tests/test_phase20_prereg.py` (seconds); the FULL suite runs
once here. Measured pre-phase baseline: `845 passed, 1 skipped` in `201.99s`.

- `.venv/bin/python -m pytest -q` exits 0.
- `.venv/bin/ruff check . && .venv/bin/ruff format --check .` exit 0.
- `git status --porcelain pyproject.toml` prints nothing.
- `git log --diff-filter=A -- 'results/phase20_*'` prints nothing.
</verification>

<success_criteria>
- `extraction_ceiling` computes `wilson_upper_bound(s, n) + MARGIN_K * floor` from imported symbols
  only, with all four arguments keyword-only and defaultless.
- The tripwire has been observed firing on wrong-arm, single-seed and missing-key provenance.
- `tolerance_report` reproduces `X = 0.3216515249612375 -> 25/104` and names the zero-tolerance
  regime in words.
- The pin still has exactly two chosen constants.
</success_criteria>

<output>
Create `.planning/phases/20-pre-registration-the-three-condition-gate/20-02-SUMMARY.md` when done.
Record the three tripwire abort messages verbatim and the sha256 of `scripts/mitigation_gate.py`.
</output>
