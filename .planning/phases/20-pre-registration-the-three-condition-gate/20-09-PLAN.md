---
phase: 20-pre-registration-the-three-condition-gate
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/20-pre-registration-the-three-condition-gate/20-CONTEXT.md
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
requirements: [GATE-02]

must_haves:
  truths:
    - "Every decision ID cited by a shipped artifact is recorded in 20-CONTEXT.md before the artifact citing it is written"
    - "Every Phase 20 requirement ID carries a traceability note naming the function that discharges it and the guard that watches it, and both names resolve by AST rather than by substring"
    - "A reader who greps `4.029000` in REQUIREMENTS.md lands on a dated amendment, not on a silently superseded requirement"
  artifacts:
    - path: ".planning/phases/20-pre-registration-the-three-condition-gate/20-CONTEXT.md"
      provides: "D-34, D-35, D-36 and D-37 recorded in the existing locked-decision shape"
      contains: "D-37"
    - path: ".planning/REQUIREMENTS.md"
      provides: "eight filled traceability notes and the D-36 dated in-place GATE-02 amendment"
      contains: "Amended by D-36"
  key_links:
    - from: ".planning/REQUIREMENTS.md GATE-02"
      to: "results/phase20_retention_floor.json"
      via: "the amendment naming the measured floor and the governing cap"
      pattern: "3\\.9085032379884783"
    - from: "shipped artifacts citing D-34…D-37"
      to: ".planning/phases/20-pre-registration-the-three-condition-gate/20-CONTEXT.md"
      via: "the Implementation Decisions record written before any artifact cites the IDs"
      pattern: "D-3[4-7]"
---

<objective>
Close verification gap 2's remaining half, D-36, and the unrecorded decision IDs — all pure
bookkeeping on two planning files, depending on nothing this phase is building.

Purpose: four decision IDs (D-34, D-35, D-36, D-37) are cited as LOCKED by the gap-closure plans and
are about to be cited by shipped artifacts, but `20-CONTEXT.md` records only D-01…D-33. A reader who
meets `D-37` in a module docstring has nowhere to look it up — which is the same defect D-36 exists
to fix for `4.029000`. Separately, eight requirement IDs are genuinely discharged in committed code,
were marked `[x]` at commit `0f265e2`, and still carry EMPTY traceability notes; a checked box with
no note carries nothing into a milestone rollup. And GATE-02's requirement text still states a
`retention_cap` of `4.029000` that the phase deliberately does not produce.

Output: `20-CONTEXT.md` with four new locked decisions; `.planning/REQUIREMENTS.md` with eight filled
notes and one dated in-place amendment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/20-pre-registration-the-three-condition-gate/20-CONTEXT.md
@.planning/phases/20-pre-registration-the-three-condition-gate/20-VERIFICATION.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Record D-34, D-35, D-36 and D-37 — before any artifact cites them</name>

  <read_first>
    - `.planning/phases/20-pre-registration-the-three-condition-gate/20-CONTEXT.md`, the
      `### Resolved during planning — conflicts surfaced by `20-RESEARCH.md`` subsection carrying
      D-31, D-32 and D-33. THIS IS THE SHAPE TO MIRROR: a dated preamble in italics stating when and
      why the decisions were added and that they are locked on the same terms as the originals, then
      `- **D-NN — <one-line statement in bold>.**` followed by the reasoning.
    - `20-08-PLAN.md` — D-34's rejected alternative, D-35's same-function requirement, and D-37's
      bound-direction resolution, all stated in full in that plan's `<action>` blocks. Source the
      wording from there; do not invent a different resolution.
    - `20-09-PLAN.md` Task 3 below — D-36's four grounds.
  </read_first>

  <files>.planning/phases/20-pre-registration-the-three-condition-gate/20-CONTEXT.md</files>

  <action>
Append a new subsection to `## Implementation Decisions`, positioned immediately BEFORE
`### Claude's Discretion` so it stays inside the decisions section. Head it
`### Resolved during gap closure — decisions forced by 20-VERIFICATION.md` with a dated italic
preamble in D-31…D-33's register: added `2026-08-20`, after verification reproduced the GATE-06 and
T-20-19 defects against the running code, each LOCKED on the same terms as D-01…D-33.

This task runs FIRST in the plan and the plan runs in wave 1, so no artifact that cites these IDs is
written before the record of them exists. That ordering is the point: a decision ID that appears in a
shipped module docstring before it appears in a decision record is a citation to nothing.

Write all four:

- **D-34 — the GATE-06 correction is a REAL COMPUTATION in unpinned code, not a caller convention.**
  The rejected alternative was a documented calling convention plus a tripwire asserting callers
  comply. It was rejected on two measured grounds: a convention cannot supply the held-out leg at all
  (there is no `sweep_heldout_recalls` parameter in the 21-kwarg signature to hold it), and its
  tripwire could only assert COMPLIANCE, never COMPUTE the correction — so a compliant caller with a
  wrong sweep still gets a wrong verdict. Resolution: `scripts/phase20_gate_coverage.py` computes
  coverage itself, and `corrected_point_verdict` is the sanctioned route. Cost, named: the route has
  no authority a Phase 23/25 caller cannot simply not invoke, so the AST caller census in
  `tests/test_phase20_correction.py` is what enforces it.

- **D-35 — WR-09's held-out leg closes in the SAME function and by the SAME discipline as the taught
  leg.** Not a separate work item, not a partially accepted risk, not a follow-on phase.
  `coverage_verdict` decides taught and held-out coverage in one body against one rule. SC2 makes the
  held-out leg load-bearing; splitting its coverage check out would leave the load-bearing half
  guarded by a plan rather than by code.

- **D-36 — GATE-02's pre-registration TEXT is amended IN PLACE, dated, and additively.** This repo's
  convention is that pre-registration requirement text is not edited — which is why the supersession
  originally lived only in the traceability row. Accepted here on four grounds, all four required
  together: the amendment is DATED, it is IN PLACE (so `grep 4.029000` cannot miss it), it is
  ADDITIVE (the original text stays byte-identical above it), and it is provably TIGHTER
  (`3.9085032379884783 < 4.029`, from a floor `7.939763314393305x` smaller — a self-serving
  amendment moves a threshold the other way). Precedent: ROADMAP SC1 already carries exactly this
  shape for the same number.

- **D-37 — the coverage statistic is chosen by CRITERION-MATCHING per axis, not by "always use
  Wilson".** X is a ceiling and condition (a) decides on `wilson_upper_bound(k, n)`, so X coverage
  reads the Wilson upper bound. Both Y legs are floors and condition (b) decides on the RAW recall
  with no bound, so Y coverage reads the raw recall. A Wilson LOWER bound on a floor would decide
  coverage on a statistic the criterion does not read — CR-01's own defect class with the sign
  flipped — and would invert the conservatism of a floor. `wilson_lower_bound` is therefore defined
  for REPORTING only, published alongside and never instead of the deciding statistic, in
  `erasure_gate`'s own `rule_of_three` register. Cost, recorded and deliberately NOT fixed: the Y
  legs inherit condition (b)'s lack of a confidence bound, and fixing that would mean moving a
  pre-registered threshold after seeing the data it governs.

Do NOT renumber, reword or re-date any of D-01…D-33. Purely additive.
  </action>

  <verify>
    <automated>.venv/bin/python -c "
import pathlib, re
p = pathlib.Path('.planning/phases/20-pre-registration-the-three-condition-gate/20-CONTEXT.md')
t = p.read_text('utf-8')
for d in ('D-34','D-35','D-36','D-37'):
    assert re.search(r'^- \*\*' + d + r' — ', t, re.M), d
    assert t.count(d) >= 1, d
i = t.index('## Implementation Decisions')
j = t.index(\"### Claude's Discretion\")
for d in ('D-34','D-35','D-36','D-37'):
    k = t.index('- **' + d + ' — ')
    assert i < k < j, (d, 'decision block is outside Implementation Decisions')
for d in ('D-31','D-32','D-33'):
    assert re.search(r'^- \*\*' + d + r' — ', t, re.M), d
assert '2026-08-20' in t
print('OK')"</automated>
  </verify>

  <acceptance_criteria>
    - D-34, D-35, D-36 and D-37 each appear as `- **D-NN — …**` inside `## Implementation Decisions`
      and before `### Claude's Discretion`.
    - The new subsection carries a dated italic preamble in D-31…D-33's shape.
    - D-31, D-32 and D-33 are still present and byte-identical; `git diff` on this file shows
      additions only, zero deletions.
    - The check above prints `OK`.
  </acceptance_criteria>

  <done>
    Every decision ID the gap-closure artifacts cite is looked-up-able in the phase's own decision
    record, written before the artifacts that cite it.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fill the eight empty traceability notes, with both names AST-resolved</name>

  <read_first>
    - `.planning/REQUIREMENTS.md`, the Phase 20 traceability table (currently around `:269-282` —
      anchor on the `| GATE-NN | Phase 20 |` row identifiers, not on the line numbers, because
      Task 3 inserts lines into this file). The rows for GATE-01, GATE-03, GATE-04, GATE-05,
      GATE-07, GATE-08, GATE-09 and GATE-10 carry empty notes; the `| GATE-02 |`, `| GATE-06 |` and
      `| RPT-02 |` rows show the note register to match.
    - `.planning/phases/20-pre-registration-the-three-condition-gate/20-VERIFICATION.md:222-235` —
      the Requirements Coverage table. Its Evidence column is the verified source for each note;
      every claim below was checked against the running module, not against a SUMMARY.
    - `scripts/mitigation_gate.py` and `tests/test_phase20_prereg.py` — the two files the automated
      check AST-resolves each note against.
  </read_first>

  <files>.planning/REQUIREMENTS.md</files>

  <action>
Fill the Note cell for GATE-01, GATE-03, GATE-04, GATE-05, GATE-07, GATE-08, GATE-09 and GATE-10.
Do NOT touch their checkboxes — all eight are already `[x]` from commit `0f265e2` and re-checking
them would be a second edit claiming a second discharge. Do NOT touch the `| GATE-06 |` or
`| RPT-02 |` rows; GATE-06 is plan `20-12`'s and RPT-02 is already complete.

Every note must name TWO things IN BACKTICKS — an identifier defined in `scripts/mitigation_gate.py`
and a `test_*` function defined in `tests/test_phase20_prereg.py` — because a requirement discharged
by code nobody watches is the WR-02 defect class this phase already caught once.

**"Names a guard" is enforced as a checkable predicate, not as a judgement call.** The automated
check below AST-walks `scripts/mitigation_gate.py` for `FunctionDef` and module-scope `Assign` names,
AST-walks `tests/test_phase20_prereg.py` for `test_*` `FunctionDef` names, and asserts that each of
the eight notes contributes at least one backticked token resolving into EACH set. This is
deliberately an AST resolution and not an `X in source` substring check: this phase produced four
independent instances of a substring audit matching the prose that explains the pattern
(recorded in the `| RPT-02 |` row), and repeating it here to police that very defect would be absurd.

Names that resolve — verified against HEAD, use these rather than guessing. A LOCAL variable does not
resolve (`y_taught` lives inside `mitigation_point_verdict` and is invisible to the walk), so where
the evidence is about a local, name the enclosing function too:

| REQ | identifier(s) in `mitigation_gate.py` | guard in `test_phase20_prereg.py` |
|-----|---------------------------------------|-----------------------------------|
| GATE-01 | `V4_VERDICTS`, `_prove_verdict_domain`, `mitigation_point_verdict` | `test_verdict_domain_stays_exactly_three`, `test_every_gate_function_is_keyword_only_with_no_defaults` |
| GATE-03 | `mitigation_point_verdict`, `F_Y` | `test_every_verdict_branch_fires` |
| GATE-04 | `F_Y`, `mitigation_point_verdict` | `test_no_imported_baseline_is_retyped` |
| GATE-05 | `mitigation_point_verdict` | `test_every_verdict_branch_fires` |
| GATE-07 | `exists_clearing_point`, `ARM_CLAIMS`, `ARMS` | `test_every_verdict_branch_fires` |
| GATE-08 | `REPLICATION_PENDING_MARKER`, `promote_to_full_fidelity` | `test_promotion_rule_and_ratchet` |
| GATE-09 | `FIXTURE_DESTROYED_MODEL` | `test_destroyed_model_fixture_matches_the_published_phase19_readings`, `test_gate_self_check_runs_clean_in_a_fresh_interpreter` |
| GATE-10 | `_CAPACITY_DISPATCH`, `CAPACITY_BRANCHES` | `test_capacity_rule_commits_both_branches_and_refuses_the_unset_fallback` |

The substance of each note, sourced from 20-VERIFICATION.md's Evidence column:

- GATE-01 — `mitigation_point_verdict` returns the three-name `V4_VERDICTS` domain, proved at import
  by the module-scope `_prove_verdict_domain()`; the keyword-only/no-defaults discipline is proved by
  an AST walk over ALL public functions, not only the verdict one; four condition reason strings
  observed.
- GATE-03 — Y is the pair `(y_taught, y_heldout)` computed inside `mitigation_point_verdict` at
  `:765-766`, each leg from `F_Y` times its own control.
- GATE-04 — each leg is `F_Y ×` its OWN control; `0.4921` and `0.3483` are asserted absent as numeric
  constants by AST, and the `from erasure_gate import` list is asserted by EXACT EQUALITY to five
  names so `V20_TAUGHT_RECALL` / `V20_HELDOUT_RECALL` cannot be present.
- GATE-05 — the early return at `:730-745` fires BEFORE any reason is appended, so the caller gets a
  one-element list; watched firing differentially against the FAIL it overrides.
- GATE-07 — the 3-tuple carries `arm`; `exists_clearing_point` aborts on a mixed-arm list;
  `ARM_CLAIMS` proved equal to `ARMS` at import.
- GATE-08 — INCONCLUSIVE over a would-be PASS at `:822-829`; `REPLICATION_PENDING_MARKER` is ONE
  constant read by both the branch and `promote_to_full_fidelity`; no `provisional` identifier,
  string or comment anywhere, by AST plus a `_prose.normalized` scan.
- GATE-09 — six outcomes observed firing in `__main__`; `FIXTURE_DESTROYED_MODEL` is built from
  Phase 19's real published M1 readings with four fields asserted against the parsed artifact; a
  subprocess twin re-runs the self-check in CI.
- GATE-10 — `_CAPACITY_DISPATCH` is proved TOTAL over all four `(small_cleared, large_cleared)`
  combinations by a module-scope `_prove`; `CAPACITY_BRANCHES` closed at five; both named branches
  observed firing; the D-26 fallback raises with the tolerance unset and the message names D-26.

Keep each note to one or two sentences — these are traceability cells, not essays. Do not restate
the requirement text; name the mechanism and the guard.
  </action>

  <verify>
    <automated>.venv/bin/python -c "
import ast, pathlib, re
req = pathlib.Path('.planning/REQUIREMENTS.md').read_text('utf-8')
rows = [l for l in req.splitlines() if re.match(r'^\| (GATE|CAL|RPT)-\d+ \| Phase 20 \|', l)]
assert len(rows) == 12, len(rows)
src = ast.parse(pathlib.Path('scripts/mitigation_gate.py').read_text('utf-8'))
names = {n.name for n in ast.walk(src) if isinstance(n, ast.FunctionDef)}
names |= {t.id for n in src.body if isinstance(n, ast.Assign) for t in n.targets if isinstance(t, ast.Name)}
tst = ast.parse(pathlib.Path('tests/test_phase20_prereg.py').read_text('utf-8'))
guards = {n.name for n in ast.walk(tst) if isinstance(n, ast.FunctionDef) and n.name.startswith('test_')}
EIGHT = {'GATE-01','GATE-03','GATE-04','GATE-05','GATE-07','GATE-08','GATE-09','GATE-10'}
for row in rows:
    rid = row.split('|')[1].strip()
    note = row.split('|', 3)[3].rsplit('|', 1)[0]
    assert note.strip(), 'EMPTY NOTE: ' + rid
    if rid in EIGHT:
        ticks = set(re.findall(r'\`([^\`]+)\`', note))
        cand = set(ticks) | {t.split('(')[0].split('.')[-1].strip() for t in ticks}
        assert cand & names, (rid, 'no mitigation_gate identifier resolves', sorted(ticks))
        assert cand & guards, (rid, 'no test_phase20_prereg guard resolves', sorted(ticks))
print('OK — 12 non-empty rows, 8 notes AST-resolved on both sides')"</automated>
  </verify>

  <acceptance_criteria>
    - All twelve Phase 20 traceability rows carry a non-empty Note cell; the check above prints `OK`.
    - Each of the eight filled notes contributes at least one backticked token that AST-resolves to a
      `FunctionDef` or module-scope `Assign` name in `scripts/mitigation_gate.py`, AND at least one
      that AST-resolves to a `test_*` `FunctionDef` in `tests/test_phase20_prereg.py`. No substring
      check anywhere in the verification.
    - The checkbox state of GATE-01/03/04/05/07/08/09/10 is unchanged — `git diff .planning/REQUIREMENTS.md`
      shows zero changes to any `- [x] **GATE-NN**` bullet for this task.
    - The `| GATE-06 | Phase 20 |`, `| RPT-02 | Phase 20 |` and `| CAL-04 | Phase 20 |` rows are
      byte-identical.
  </acceptance_criteria>

  <done>
    No Phase 20 requirement is discharged in code and silent in the traceability table, and no note
    can name a function or guard that does not exist without the check going red.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: D-36 — the dated in-place amendment to GATE-02's requirement text</name>

  <read_first>
    - `.planning/ROADMAP.md` — the `> **Amended by D-06 at plan `20-07`, and the amendment is
      TIGHTER.**` blockquote beneath Success Criterion 1. THIS IS THE SHAPE TO MIRROR: a blockquote
      directly beneath the criterion, leaving the original text above it untouched. Find it by that
      opener string, not by line number.
    - `.planning/REQUIREMENTS.md` — the `- [x] **GATE-02**:` bullet, including the `4.029000` this
      amendment exists to make greppable.
    - `.planning/REQUIREMENTS.md` — the `| GATE-02 | Phase 20 |` traceability row, which already
      carries the supersession. The amendment must agree with it and not contradict it.
    - `results/phase20_retention_floor.json` — `retention_ppl_noise_floor`, `cap`, `borrowed_cap`,
      `borrowed_floor_ratio`. Read the values; do not transcribe them from any report.
  </read_first>

  <files>.planning/REQUIREMENTS.md</files>

  <action>
Add a dated amendment blockquote immediately after the `- [x] **GATE-02**:` bullet ends and before
the `- [x] **GATE-03**:` bullet begins. Anchor on those two bullet strings, not on line numbers.

Name the tension and resolve it in the block itself, because this repo's convention is that
pre-registration REQUIREMENT TEXT is not edited — that is precisely why the supersession originally
lived only in the traceability row, and 20-VERIFICATION.md argues at length that "a pre-registration
you may repair after seeing the data is not a pre-registration". D-36 (recorded in `20-CONTEXT.md` at
Task 1 of this plan) accepts the amendment on four grounds and the block must state them: it is
DATED, it is IN PLACE, it is visible to `grep`, and it is provably TIGHTER. Nothing above the
amendment changes — the original `4.029000` stays exactly where it is, unedited, exactly as ROADMAP
SC1 preserves its own.

Open with `> **Amended by D-36 at plan `20-09`, and the amendment is TIGHTER.**` — same opener as
SC1's, same phase-and-plan attribution shape. The body must carry, all of it re-read from
`results/phase20_retention_floor.json` and never transcribed:

- The requirement's stated `retention_cap 4.029000` is derived from `V20_RETENTION_NOISE_FLOOR =
  0.06893`, a **Phase 12 FULL-FINE-TUNE** seed pair, and it is NOT what governs v4.0.
- `scripts/mitigation_gate.py::retention_cap` deliberately does not import that constant — a test
  asserts its absence from a five-name import list checked by EQUALITY — and takes the floor as a
  required keyword argument instead (D-07).
- The governing floor is the **adapter-regime** `0.008681618994239138`, measured at plan `20-07`,
  two seeds, bit-identity control passed at `abs_delta = 0.0`.
- The governing v4.0 `retention_cap` is **`3.9085032379884783`**. The borrowed floor is
  `7.939763314393305x` larger and its cap `4.029` correspondingly LOOSER — so the amendment makes
  condition (c) HARDER to clear, which is the arithmetic evidence that it is not self-serving. Say
  this in one sentence: a self-serving amendment moves a threshold the other way.
- `dialogue_cap 4.5837288963367` is unchanged, and `scripts/erasure_gate.py:246` still computes
  `4.029` for **Phase 19** verdicts and is deliberately NOT corrected.
- The residual risk, stated rather than glossed, from 20-VERIFICATION.md's own words: a `[x]` is
  machine-readable and the note beside it is not, so an automated audit that counts checkboxes will
  report GATE-02 satisfied AS WRITTEN. This amendment is what closes the second half of that gap —
  the requirement's own text now carries its correction.
- The enforcement, named so the amendment is not merely prose: `_prove_retention_floor` in
  `scripts/phase20_gate_coverage.py` (plan `20-08`) refuses `0.06893` at the choke point, and
  `tests/test_phase20_correction.py` (plan `20-11`) asserts `retention_cap(measured floor)` equals
  the artifact's published `cap` bit-exact.

Also append one clause to the `| GATE-02 | Phase 20 |` traceability note pointing at this amendment,
so the row and the requirement text cross-reference each other rather than drifting. Leave the rest
of that row untouched — plan `20-12` rewrites its RESIDUAL paragraph and must find it intact.
  </action>

  <verify>
    <automated>.venv/bin/python -c "
import pathlib
t = pathlib.Path('.planning/REQUIREMENTS.md').read_text('utf-8')
i = t.index('4.029000'); j = t.index('Amended by D-36')
assert i < j, 'amendment must follow the preserved original 4.029000'
k = t.index('- [x] **GATE-03**')
assert j < k, 'amendment must sit between the GATE-02 and GATE-03 bullets'
for s in ('3.9085032379884783','0.008681618994239138','7.939763314393305','0.06893','TIGHTER','20-09','scripts/phase20_gate_coverage.py','tests/test_phase20_correction.py'):
    assert s in t, s
assert t.count('Amended by D-36') == 1
assert 'RESIDUAL — OPEN' in t, 'GATE-02 row RESIDUAL paragraph must survive for plan 20-12 to rewrite'
print('OK  4.029000 at', i, ' amendment at', j)"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "4.029000" .planning/REQUIREMENTS.md` still finds the ORIGINAL requirement text in the
      `- [x] **GATE-02**:` bullet — it was preserved, not replaced — and the amendment blockquote
      begins within the next five lines and before the `- [x] **GATE-03**:` bullet.
    - The amendment contains, verbatim: `3.9085032379884783`, `0.008681618994239138`,
      `7.939763314393305`, `0.06893`, the word `TIGHTER`, and the plan attribution `20-09`.
    - The amendment names both enforcement mechanisms by file: `scripts/phase20_gate_coverage.py`
      and `tests/test_phase20_correction.py`.
    - `git diff .planning/REQUIREMENTS.md` shows ZERO deletions inside the `- [x] **GATE-02**:`
      bullet — the amendment is purely additive to the requirement text.
    - The `| GATE-02 | Phase 20 |` row still carries its `RESIDUAL — OPEN` paragraph, which plan
      `20-12` replaces; only a cross-reference clause was appended here.
    - The check above prints `OK`.
  </acceptance_criteria>

  <done>
    A `grep` for `4.029000` in `.planning/REQUIREMENTS.md` lands a reader on a dated amendment
    naming the number that actually governs, in the same shape ROADMAP SC1 already carries, citing a
    decision ID that this plan already recorded.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a pre-registration record ↔ an amendment to it | Editing requirement text after seeing data is the researcher degree of freedom pre-registration exists to remove. |
| a machine-readable `[x]` ↔ a human-readable note beside it | An audit that counts checkboxes reads the box and not the note. |
| a cited decision ID ↔ the record defining it | An artifact citing `D-37` with no D-37 anywhere is an appeal to an authority that does not exist. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-21 | Repudiation | GATE-06 mislabeling | mitigate | CARRIED FORWARD, open. Untouched by this plan by design: the `| GATE-06 |` row stays byte-identical here and is rewritten in `20-12` only after `20-11`'s tripwire has been watched RED-then-GREEN. Filling GATE-06's note from a bookkeeping plan would mark a defect discharged on the strength of a plan rather than a guard, which is the exact failure `20-12` exists to refuse. |
| T-20-19 | Spoofing | the unguarded retention leg | mitigate | CARRIED FORWARD, open. This plan writes the record half: the D-36 amendment states the governing cap `3.9085032379884783`, publishes the borrowed `4.029` beside it, and NAMES the enforcement — `_prove_retention_floor` in `scripts/phase20_gate_coverage.py` and the bit-exact assertion in `tests/test_phase20_correction.py`. Naming the enforcement in the requirement text is what stops the amendment being prose; the mechanism ships at `20-08` and closes at `20-11`. |
| T-20-52 | Repudiation | the D-36 in-place amendment to the GATE-02 requirement bullet | mitigate | An amendment to a pre-registration reads as post-hoc licence unless it is dated, additive and provably against the amender's interest. All three are enforced: the original `4.029000` stays byte-identical above the block, the block carries its plan attribution and date, and it states the measured arithmetic — `3.9085032379884783 < 4.029` from a floor `7.939763314393305x` smaller — that shows the amendment makes condition (c) HARDER. The acceptance criterion asserting zero deletions inside the bullet is what keeps it additive. |
| T-20-55 | Tampering | a traceability note naming a function or guard that does not exist | mitigate | Enforced mechanically, not by promise: the automated check AST-walks `scripts/mitigation_gate.py` for `FunctionDef` / module-scope `Assign` names and `tests/test_phase20_prereg.py` for `test_*` names, and asserts each of the eight notes resolves at least one backticked token into EACH set. A note pointing at a non-existent guard is worse than an empty note — it manufactures the appearance of coverage — so the check is an AST resolution rather than the `X in source` substring test that failed four times in this phase. |
| T-20-56 | Repudiation | re-checking already-checked boxes | accept | The eight boxes were checked at `0f265e2`. Touching them again would make one discharge look like two in the commit record. Not touched; asserted unchanged by the acceptance criterion. |
</threat_model>

<verification>
- `git diff .planning/REQUIREMENTS.md .planning/phases/20-pre-registration-the-three-condition-gate/20-CONTEXT.md`
  reviewed for additive-only changes.
- All three automated checks in the tasks above exit 0.
- `.venv/bin/python -m pytest -q` unaffected (no code touched).
</verification>

<success_criteria>
- Four locked decisions recorded before any artifact cites them.
- Twelve non-empty Phase 20 traceability notes, eight of them newly written and each AST-resolving to
  a real function and a real guard.
- One dated, additive, provably-tighter GATE-02 amendment that a `grep` for `4.029000` cannot miss.
</success_criteria>

<output>
Create `.planning/phases/20-pre-registration-the-three-condition-gate/20-09-SUMMARY.md` when done.
</output>
