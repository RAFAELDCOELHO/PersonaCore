---
phase: 20-pre-registration-the-three-condition-gate
plan: 17
type: execute
wave: 16
depends_on: ["20-16"]
files_modified:
  - .planning/phases/20-pre-registration-the-three-condition-gate/20-SECURITY.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
gap_closure: true
requirements: [GATE-02, GATE-06]
user_setup: []

must_haves:
  truths:
    - "20-SECURITY.md reads `status: verified` and `threats_open: 0`, and a regex count of its own rows finds ZERO carrying Status `open`"
    - "The published total reconciles to the file's OWN tables under the method fixed at 20-13 — DISTINCT `T-20-NN` ids anywhere in a table line: 66 pre-existing (measured at HEAD) + 18 new ids T-20-67 … T-20-84, each of which is also its own six-column row"
    - "T-20-19's re-closure cites output OBSERVED IN THIS PLAN'S PROCESS — the 20-14/20-15 breaks re-applied and quoted — never transcribed from a SUMMARY"
    - "The re-close is not the same commit as 20-13's OPEN flip (D-39)"
    - "T-20-19's row still carries its `*What was wrong, preserved:*` span and its `RE-OPENED 2026-08-21 at plan 20-13` sentence byte-identically; the re-closure is APPENDED beneath them"
    - "ROADMAP.md's Phase 20 plan list carries 20-13 … 20-17 as `[x]` — flipped IN PLACE, since the wave-2 block already exists as of commit e3d20ae — with the `**Plans**: 17 plans across 16 waves` count line, both amendment blockquotes and every wave heading byte-identical to HEAD"
  artifacts:
    - path: ".planning/phases/20-pre-registration-the-three-condition-gate/20-SECURITY.md"
      provides: "T-20-19 re-closed against a re-run; 18 new rows (T-20-67 … T-20-84); threats_open 0"
      contains: "T-20-84"
    - path: ".planning/ROADMAP.md"
      provides: "the wave-12..16 plan list for Phase 20"
      contains: "20-17-PLAN.md"
    - path: ".planning/STATE.md"
      provides: "position advanced to gap-closure wave 2 complete, awaiting re-verification"
      contains: "20-17"
  key_links:
    - from: "20-SECURITY.md `threats_open: 0`"
      to: "the Watched-RED evidence table"
      via: "each closure points at a guard observed failing in THIS process, never at a plan"
      pattern: "DID NOT RAISE"
    - from: "20-SECURITY.md published total"
      to: "the file's own `| T-20-NN |` rows"
      via: "the total is counted from the file's own tables by distinct id — the method fixed at 20-13 — never inherited from a prior audit"
      pattern: "T-20-84"
---

<objective>
Re-close T-20-19 and flip `20-SECURITY.md` back to `status: verified` / `threats_open: 0` — but
only after re-running the guards and re-applying the watched-RED breaks in THIS process, per D-39.
Then bring `ROADMAP.md` and `STATE.md` current.

Purpose: D-39 requires the re-close to reflect what was genuinely proved rather than what was
planned. `20-12` set the standard this plan must meet — it re-ran the guards and re-applied four
breaks rather than citing `20-11-SUMMARY.md`, and one break diverged from the record, a divergence
it published. The same discipline applies here and the same failure mode is the threat: a register
flipped closed over a guard whose coverage was never measured is precisely what
`20-VERIFICATION.md` found the last time this file reached `threats_open: 0`.

Output: a register whose total reconciles to its own rows and whose re-closure cites observed
output, plus a roadmap and state file that say where the phase actually is.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/20-pre-registration-the-three-condition-gate/20-CONTEXT.md
@.planning/phases/20-pre-registration-the-three-condition-gate/20-VERIFICATION.md
@.planning/phases/20-pre-registration-the-three-condition-gate/20-13-SUMMARY.md
@.planning/phases/20-pre-registration-the-three-condition-gate/20-14-SUMMARY.md
@.planning/phases/20-pre-registration-the-three-condition-gate/20-15-SUMMARY.md
@.planning/phases/20-pre-registration-the-three-condition-gate/20-16-SUMMARY.md

<interfaces>
<!-- Resolved from the committed files. -->

`20-SECURITY.md` as `20-13` left it:
  :4  `status: blocked`      :5  `threats_open: 1`
  :18 the `**Gate status: BLOCKED.**` paragraph
  :46 `66 threats. **65 closed, 1 open.**`, substantiated by the paragraph at :48-55
  :57 `### Open` — carrying the T-20-19 row with its named closing condition
  :90 the `| T-20-21 |` row — untouched throughout this wave-set
  :91 the `| T-20-19 |` row — Status `**open**`, carrying `*What was wrong, preserved:*`,
      `*The closure:*` and the `RE-OPENED 2026-08-21 at plan 20-13 (D-39)` sentence
  `#### New at the gap-closure plans 20-08 … 20-12` — the 20-row table T-20-47 … T-20-66
  the Watched-RED evidence table, `## Security Audit Trail`, `## Sign-Off`

Threat IDs declared across this wave-set, each of which must become a row:
  `20-13`: T-20-67, T-20-68, T-20-69, T-20-70
  `20-14`: T-20-71, T-20-72, T-20-73, T-20-74
  `20-15`: T-20-75, T-20-76, T-20-77, T-20-78
  `20-16`: T-20-79, T-20-80, T-20-81, T-20-82
  `20-17`: T-20-83, T-20-84
  → 18 new ids; 66 + 18 = 84.

THE COUNTING METHOD, fixed at `20-13` action item 3 and binding here. A threat is counted once per
DISTINCT `T-20-NN` id appearing anywhere inside this file's register TABLES — including ids
enumerated inside a single cell of the grouped-by-plan table at `:67-80`. MEASURED at HEAD: distinct
ids in table lines = 66, exactly the published total, so the method already reconciles; table lines
that START with `| T-20-NN |` = 39, carrying 35 distinct ids. Counting row-starts would publish 35
and contradict the substantiation paragraph at `:48-55`. COUNT with this method rather than take the
arithmetic above — the discipline `20-12` used to close the 46-vs-38 discrepancy.

`.planning/ROADMAP.md` Phase 20 — MEASURED at commit `e3d20ae`, the commit that created these plans.
The wave-2 block ALREADY EXISTS; this plan EDITS IT IN PLACE and adds nothing:
  :239  `**Plans**: 17 plans across 16 waves (7 original + 5 gap-closure wave 1 + 5 gap-closure
        wave 2)` — ALREADY CORRECT. Do not touch it.
  :289  `**Gap closure — wave 2** *(...)* ` — already present
  :291-307  `**Wave 12**` … `**Wave 16**` with five `- [ ] 20-13-PLAN.md` … `- [ ] 20-17-PLAN.md`
        entries — already present, UNCHECKED
  SC1's D-06 blockquote and SC3's D-34/D-37 blockquote — BOTH byte-identical, never edited

`.planning/STATE.md`: frontmatter `stopped_at`, `last_updated`, `last_activity`,
  `progress.total_plans` / `completed_plans`; body `## Current Position` (Phase / Plan / Status /
  Last activity).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-run the guards, re-apply the breaks, then re-close T-20-19 and reconcile the register</name>
  <files>.planning/phases/20-pre-registration-the-three-condition-gate/20-SECURITY.md</files>

  <read_first>
    - `.planning/phases/20-pre-registration-the-three-condition-gate/20-SECURITY.md` in full — the
      Watched-RED evidence table's row shape, the `#### New at the gap-closure plans` table shape,
      the Audit Trail table and the Sign-Off block.
    - `20-13-SUMMARY.md`, `20-14-SUMMARY.md`, `20-15-SUMMARY.md`, `20-16-SUMMARY.md` — read them to
      know WHICH breaks exist and what each threat row must say. Do NOT copy their output.
    - `.planning/phases/20-pre-registration-the-three-condition-gate/20-CONTEXT.md` D-39 — the
      precondition this task must satisfy before flipping anything.
  </read_first>

  <action>
**Step 1 — RE-RUN, before editing a character of the register.** Execute each of these in this
process and capture the actual output:

  - `.venv/bin/python -m pytest tests/test_phase20_correction.py tests/test_phase20_prereg.py -q`
  - `.venv/bin/python -m pytest -q`
  - `ruff check . && ruff format --check .`
  - `git diff --exit-code -- scripts/mitigation_gate.py scripts/erasure_gate.py`
  - `.venv/bin/python -m pytest tests/test_phase20_prereg.py -q -k phase20_prereg_is_frozen`

**Step 2 — RE-APPLY the breaks, per D-39.** The re-close is gated on the magnitude bound's
tripwires being observed red-then-green against BOTH measured cases. Re-apply, in this process,
each break `20-14`, `20-15` and `20-16` recorded — the Y value guard, the count-by-type guard, the
magnitude bound, its tolerance, the import census, the JSON additivity guard (one `evidence` leaf
mutated) and the addendum ordering assertion (a heading line spliced INSIDE the appended region —
between the `RECORDED` pointer and the first `## Addendum`, measured line 118; a splice at `:117` or
above reddens the pre-existing `changed == [(PENDING, RECORDED)]` assertion instead and never reaches
the ordering one) —
observe the failure, and restore each byte-identically
(`shasum -a 256` equality plus `git diff --exit-code`). Record the OBSERVED output for each.

Where an observed result diverges from what a SUMMARY recorded, PUBLISH the divergence beside the
table rather than smoothing it. `20-12` did exactly this and it is the most valuable line in its
record.

If any re-run or any re-applied break fails to reproduce, STOP. Do not flip the register; record
the failure in the SUMMARY and leave `threats_open: 1`. A register flipped over a guard that did
not reproduce is the exact failure this plan exists to avoid.

**Step 3 — flip the register, additively.**

  1. Frontmatter `:4` → `status: verified`, `:5` → `threats_open: 0`. They move together, per D-39.
  2. `:18` — `**Gate status: BLOCKED.**` → `**Gate status: CLOSED.**`, with one sentence recording
     that T-20-19 was re-opened at `20-13` and re-closed here against a re-run, and naming the
     magnitude bound as what closed it.
  3. `:46` — update the total ONLY IF a count of DISTINCT `T-20-NN` ids across the file's table
     lines yields it (the method above; NOT a count of row-starts, which measures 35 at HEAD).
     Substantiate it in the paragraph beneath in the register `:48-55` already uses: `66`
     previously reconciled `+ 18` new gap-closure-wave-2 rows.
  4. `### Open` — restore it to a `None.` sentence naming T-20-19 as re-closed here and pointing at
     its row, in the shape the file carried before `20-13`.
  5. The `| T-20-19 |` row at `:91` — leave every existing character, including `*What was wrong,
     preserved:*`, `*The closure:*` and the `RE-OPENED 2026-08-21 at plan 20-13 (D-39)` sentence.
     APPEND a dated sentence beginning `**RE-CLOSED 2026-08-21 at plan `20-17`, against a re-run.**`
     naming: the fifth `_prove` and `_MAX_ADMISSIBLE_RETENTION_FLOOR`; both measured cases now
     refused (the one-ULP nudge and `5.0`) with the governing floor still admitted; D-41's
     consequence; the guard by test name; and the re-applied breaks with their OBSERVED output.
     Change the Status cell from `**open**` to `**closed**`. The row then reads as a full arc —
     asserted, falsified, re-opened, corrected, re-closed — which is what a register is for.
  6. Add a new `#### New at the gap-closure plans 20-13 … 20-17` section beneath the existing
     `20-08 … 20-12` table, with all 18 ids T-20-67 … T-20-84 each as its own six-column row
     (`Threat ID | Category | Component | Disposition | Mitigation | Status`). Take each row's
     Component and Mitigation from the `<threat_model>` block of the plan that declared it, and
     state each one's status against what was actually built. Declare here the two this plan itself
     carries: **T-20-83** (Repudiation — a register re-closed on the strength of a plan rather than
     a re-run; mitigated by Steps 1-2 and by the STOP condition above) and **T-20-84** (Tampering —
     a re-close that edits the preserved historical text; mitigated by item 5's append-only rule and
     by this task's byte-identity acceptance criteria).
  7. Watched-RED evidence table — append one row per break re-applied in Step 2, each with the
     command, the observed output and the restore proof.
  8. `## Security Audit Trail` — append one row dated `2026-08-21` recording the new total, `0`
     open, and `/gsd:plan-phase 20 --gaps (wave 2) — plans 20-13 … 20-17`. Do not edit the three
     existing rows.
  9. `## Sign-Off` — re-check the two boxes `20-13` unchecked, and append a dated approval
     paragraph in `20-12`'s shape listing the exact commands run in Steps 1-2 with their observed
     output. Leave the `20-12` approval paragraph byte-identical beneath it.

Do not touch `T-20-21`'s row, and do not touch T-20-01 … T-20-66 beyond item 5's append.
  </action>

  <verify>
    <automated>.venv/bin/python -c "
import pathlib, re, subprocess
p='.planning/phases/20-pre-registration-the-three-condition-gate/20-SECURITY.md'
t=pathlib.Path(p).read_text(); fm=t.split('---')[1]
assert 'status: verified' in fm and 'threats_open: 0' in fm, fm
ids={m for l in t.splitlines() if l.startswith('|') for m in re.findall(r'T-20-\d+', l)}
assert len(ids)==84, (len(ids), sorted(ids)[-3:])
assert f'{len(ids)} threats' in t, 'published total does not match the distinct-id count'
starts=set(re.findall(r'^\| (T-20-\d+) \|', t, re.M))
missing={f'T-20-{n}' for n in range(67,85)} - starts
assert not missing, sorted(missing)
assert re.findall(r'^\| T-20-\d+ \|.*\|\s*\*{0,2}open\*{0,2}\s*\|\s*$', t, re.M)==[]
old=subprocess.run(['git','show','HEAD:'+p],capture_output=True,text=True).stdout
assert [l for l in old.splitlines() if l.startswith('| T-20-21 |')][0] in t, 'T-20-21 moved'
assert 'RE-OPENED 2026-08-21 at plan' in t and 'RE-CLOSED 2026-08-21 at plan' in t
print('ok')" && .venv/bin/python -m pytest tests/test_phase20_correction.py tests/test_phase20_prereg.py -q 2>&1 | tail -2</automated>
  </verify>

  <acceptance_criteria>
    - Every command in Steps 1-2 was RUN in this process and its output is quoted in the SUMMARY, not predicted.
    - The verify command exits 0 and the phase-20 pair reports `32 passed`, no `skipped`, no `xfail` — the task-level gate, ~1s. Step 1 already ran the full suite (~338s) as the plan-level gate and its output is quoted in the SUMMARY.
    - `.venv/bin/python -c "import re,pathlib; t=pathlib.Path('.planning/phases/20-pre-registration-the-three-condition-gate/20-SECURITY.md').read_text(); print(len({m for l in t.splitlines() if l.startswith('|') for m in re.findall(r'T-20-\d+', l)}))"` prints `84`, and that same number appears in the register header sentence. (Row-start LINES print 57 — 39 pre-existing plus 18 new; distinct row-start IDS would be 53 — 35 plus 18. Neither is the total, which is exactly why the method is fixed in writing rather than left to a regex.)
    - All eighteen of T-20-67 … T-20-84 appear as their OWN six-column row-start in the new section, even though the total is counted by distinct id.
    - The `| T-20-21 |` row and the `*What was wrong, preserved:*` span of `| T-20-19 |` are byte-identical to HEAD; the re-closure is an append.
    - `git log --format=%H -- .planning/phases/20-pre-registration-the-three-condition-gate/20-SECURITY.md | wc -l` is at least 2 more than at the start of this wave-set — the OPEN flip and the re-close are distinct commits (D-39).
    - Each break re-applied in Step 2 was followed by `shasum -a 256` equality and `git diff --exit-code` returning 0 on the broken file.
    - The re-applied addendum break's failure output names the ORDERING assertion, not `changed == [(PENDING, RECORDED)]` — the same check `20-16` Task 4 makes, repeated here because a re-apply landing in the published body would re-close T-20-79 on an unwatched guard.
    - `git diff --exit-code -- scripts/ tests/ results/` returns 0 at commit time.
    - `git diff --exit-code -- scripts/mitigation_gate.py scripts/erasure_gate.py` returns 0.
  </acceptance_criteria>

  <done>`20-SECURITY.md` publishes `status: verified` / `threats_open: 0` with 84 distinct threat IDs counted from its own tables, T-20-19 re-closed against re-run output, and every historical span preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Bring ROADMAP.md and STATE.md current</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md</files>

  <read_first>
    - `.planning/ROADMAP.md` — the Phase 20 section from `### Phase 20:` through the end of the
      `**Wave 11**` entry. Note SC1's D-06 blockquote and SC3's D-34/D-37 blockquote; both must
      come out byte-identical.
    - `.planning/STATE.md` frontmatter and `## Current Position`.
    - `20-13-SUMMARY.md` … `20-17`'s own Task 1 output — what actually happened, so the state file
      records the phase's real position rather than this plan's intent.
  </read_first>

  <action>
**ROADMAP.md — EDIT IN PLACE. Do not add a heading, a wave block or a plan entry; they are already
there.** Run `grep -n '20-1[34567]-PLAN.md\|^\*\*Plans\*\*: ' .planning/ROADMAP.md` first and write
against what it returns.

Exactly two kinds of change, both confined to the five `- [ ] 20-1N-PLAN.md` lines at :293, :297,
:301, :305 and :309:
  (a) flip each `- [ ]` to `- [x]`;
  (b) rewrite each one-liner from the plan's own SUMMARY — what it ACTUALLY did, including any
      deviation it recorded. The existing entries for `20-10`, `20-11` and `20-12` do exactly this
      and they are the most useful lines in the list. NOTE, measured: :309's "84 rows" wording was
      already corrected to "84 distinct threat IDs" during plan revision, so the matching acceptance
      criterion below is a REGRESSION CHECK, not a task — do not re-correct it, and do not
      reintroduce it while rewriting that one-liner.

Leave the `**Plans**:` count line at :239 UNTOUCHED — measured, it already reads 17 plans across 16
waves and is correct. Leave the `**Gap closure — wave 2**` heading and the five `**Wave N**`
headings untouched.

Do NOT edit SC1's or SC3's blockquotes. SC3 already carries the D-34/D-37 amendment; the Y-leg
correction this wave-set shipped is recorded in `REQUIREMENTS.md`'s GATE-06 amendment (plan
`20-16`), in the correction artifact and in the second dated continuation. Adding a third layer of
blockquote to SC3 would restate rather than record. Confirm with `git diff` that both blockquotes
report zero changed lines.

**STATE.md.** Update frontmatter `stopped_at`, `last_updated`, `last_activity` and
`progress.total_plans` / `completed_plans` (12 → 17). Update `## Current Position`'s Phase, Plan,
Status and Last activity. The Status line must say what is TRUE: both `20-VERIFICATION.md` gaps
closed in code and watched, the register re-closed against a re-run, and the phase awaiting
`/gsd:verify-phase 20` — which is the orchestrator's audit and is what confirms this, not this plan.
Carry forward the prior entry text in the `PRIOR ENTRY (...)` register the file already uses; do not
delete history.

Record in the Status line the one finding a future reader most needs: D-41 — the magnitude bound
refuses this repository's own committed fixtures at `0.009`, so the sanctioned route's harness
supplies the governing floor read from `results/phase20_retention_floor.json`, and every published
verdict is bit-unchanged under the substitution.

Note: `gsd-sdk` `state.*` and `roadmap.*` mutation handlers have corrupted planning frontmatter in
this repository before. Edit both files by hand, and run
`git diff .planning/STATE.md .planning/ROADMAP.md` before committing to confirm only the intended
lines moved.
  </action>

  <verify>
    <automated>.venv/bin/python -c "
import pathlib, subprocess
r=pathlib.Path('.planning/ROADMAP.md').read_text()
old=subprocess.run(['git','show','HEAD:.planning/ROADMAP.md'],capture_output=True,text=True).stdout
for marker in ('Amended by D-06 at plan', 'Amended by D-34 and D-37 at plan'):
    a=old[old.index(marker):old.index(marker)+1200]
    assert a in r, f'{marker} blockquote changed'
assert r.count('**Gap closure — wave 2**')==1, 'the wave-2 block was duplicated'
assert '**Plans**: 17 plans across 16 waves' in r, 'the count line was changed'
assert '84 rows' not in r
for n in range(13,18):
    assert f'- [x] 20-{n}-PLAN.md' in r and f'- [ ] 20-{n}-PLAN.md' not in r, n
s=pathlib.Path('.planning/STATE.md').read_text()
assert 'completed_plans: 17' in s and 'total_plans: 17' in s
assert '20-17' in s and 'D-41' in s
print('ok')" && git diff --stat .planning/STATE.md .planning/ROADMAP.md</automated>
  </verify>

  <acceptance_criteria>
    - The verify command exits 0.
    - `git diff -U0 HEAD -- .planning/ROADMAP.md` shows changes confined to the five `- [ ] 20-1N-PLAN.md` lines: exactly 5 deletions and 5 additions, no other line touched.
    - `grep -c '^\*\*Plans\*\*: 17 plans across 16 waves' .planning/ROADMAP.md` reports 1, and that line is byte-identical to HEAD — this plan does not touch the count.
    - `grep -c '^\*\*Gap closure — wave 2\*\*' .planning/ROADMAP.md` reports 1, not 2 — nothing was duplicated.
    - `grep -c -- '- \[ \] 20-1[34567]-PLAN.md' .planning/ROADMAP.md` reports 0 and `grep -c -- '- \[x\] 20-1[34567]-PLAN.md'` reports 5.
    - `grep -c '84 rows' .planning/ROADMAP.md` reports 0 — :309's wording is corrected to distinct IDs.
    - `.planning/STATE.md` frontmatter reads `total_plans: 17` / `completed_plans: 17` and its `## Current Position` names D-41 and the awaiting `/gsd:verify-phase 20`.
    - `git diff .planning/STATE.md .planning/ROADMAP.md` was reviewed by hand before commit and shows no unintended frontmatter rewrite.
    - `git diff --exit-code -- scripts/ tests/ results/` returns 0.
  </acceptance_criteria>

  <done>ROADMAP.md lists all five wave-2 gap-closure plans with the amendment blockquotes untouched, and STATE.md records the real position including D-41.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a plan that says a thing will be done ↔ a guard that proves it was | `20-SECURITY.md:39`. This file's own `status` and `threats_open` cross it, and this plan is the crossing. |
| a published total ↔ the rows that substantiate it | `20-SECURITY.md:40`. A total counted from a prior audit but not carried by this file's own rows is phantom coverage. |
| a register that records what went wrong ↔ one that erases it | The T-20-19 row is now a four-state arc; erasing any state to make the closure read cleanly destroys the only reason the row is worth reading. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-83 | Repudiation | a register re-closed on the strength of a plan rather than a re-run | mitigate | Steps 1-2 re-run every guard and re-apply every break in THIS process and quote the observed output; divergences from the SUMMARYs are published, not smoothed; and a break that fails to reproduce STOPS the flip with `threats_open: 1` left standing |
| T-20-84 | Tampering | a re-close that edits the preserved historical text | mitigate | Item 5 is append-only; the acceptance criteria assert `T-20-21`'s row and T-20-19's `*What was wrong, preserved:*` span byte-identical against `git show HEAD`, and the OPEN flip and the re-close are provably distinct commits |
| T-20-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install in this plan; it edits only planning documents. `pyproject.toml` is untouched and sha256-pinned by `tests/test_package.py` (STAT-04) |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_phase20_correction.py tests/test_phase20_prereg.py -q` →
  `32 passed`, no `skipped`, no `xfail`.
- `.venv/bin/python -m pytest -q` → at least `877 passed, 1 skipped`, zero failures.
- `ruff check . && ruff format --check .` → clean.
- `git diff --exit-code -- scripts/mitigation_gate.py scripts/erasure_gate.py` → 0.
- `git diff --exit-code -- scripts/ tests/ results/` → 0: this plan writes no code and no artifact.
- `.venv/bin/python -m pytest tests/test_phase20_prereg.py -q -k phase20_prereg_is_frozen` → green.
- Distinct `T-20-NN` ids across `20-SECURITY.md`'s table lines equal the published total and equal
  84; zero register rows at Status `open`.
</verification>

<success_criteria>
- `20-SECURITY.md` is `status: verified` / `threats_open: 0` with the total counted from its own
  tables as 84 distinct ids, and every closure points at a guard observed failing in this process.
- T-20-19's row records the full arc and no historical span was edited.
- The OPEN flip (`20-13`) and the re-close (`20-17`) are distinct commits.
- `ROADMAP.md` lists waves 12-16 with the SC1 and SC3 blockquotes byte-identical.
- `STATE.md` records the real position, including D-41.
- Full suite green; both frozen files return 0 from `git diff --exit-code`.
</success_criteria>

<output>
Create `.planning/phases/20-pre-registration-the-three-condition-gate/20-17-SUMMARY.md` when done.
</output>
