---
phase: 20-pre-registration-the-three-condition-gate
plan: 05
type: execute
wave: 4
depends_on: ["20-04"]
files_modified:
  - scripts/mitigation_gate.py
autonomous: true
requirements: [GATE-07, GATE-08, GATE-09, GATE-10, CAL-04]

must_haves:
  truths:
    - "GATE-07 / D-28: `exists_clearing_point` computes the existential PER ARM and REFUSES a mixed-arm point list, so '∃ a point' over the union cannot be formed. A DP point clearing carries a formal claim; an adversarial point clearing carries an explicitly non-formal one, and the claim strings come from the closed `ARM_CLAIMS` proved equal to `ARMS` at import."
    - "CAL-04 / D-19: the curve K, the full-fidelity K reserved for gate-candidate points, and the rule promoting a point from the first to the second are all committed before the first v4.0 artifact exists. `K_RUNGS` is the closed menu; `ratchet_k` refuses any decrease."
    - "D-19: the RATCHET structurally eliminates the post-null K reduction `scripts/phase18_extraction.py:84-93` records as the ATK-03 / P18-4 weakening — fewer draws means less power to observe extraction, i.e. an easier null."
    - "D-20: the promotion rule lives in the GATE and takes K as a REQUIRED KWARG, so `scripts/mitigation_gate.py` never imports `scripts/mitigation_budget.py` and the AST guard stays intact."
    - "GATE-10 / D-25: capacity equivalence is STRUCTURAL — identical σ, steps, δ and q — with ZERO tolerance constant, because ε is a deterministic function of those parameters and comparing at identical mechanism parameters IS comparing at equivalent ε_fact. This preserves capacity N as the only genuinely free variable."
    - "GATE-10 / D-26: the fallback branch is committed NOW so neither branch is selectable after seeing data. Its tolerance is a THIRD chosen constant and is deliberately UNSET — the gate raises, naming D-26 and the requirement that it be decided before Phase 21's CAL-03 runs. Flagged rather than smuggled."
    - "GATE-10 / D-27: both branches are publishable — recovery at n=64 that n=8 did not achieve is a finding about where capacity stops destroying the mitigation; no recovery confirms the null at two capacities."
    - "The capacity dispatch is TOTAL over all four (small_cleared, large_cleared) combinations plus the incomparable case, with a module-scope `_prove` — no fall-through and no investigate-the-instrument escape hatch (the Phase 16 `licensed_headline` precedent)."
    - "GATE-09 / D-30: the `__main__` self-check exercises SIX outcomes including the failing ones, and the 'mitigation that destroyed the model' fixture — built from Phase 19's REAL published M1 readings, dialogue `4.851119149910443` and retention `3.6709177253236867` — is observed returning FAIL. It is LABELLED a fixture, never a second reading of the experiment."
    - "The fixtures are module-scope constants so plan 20-06's pytest twin imports the SAME objects — a second transcription is a second fixture free to stop matching."
    - "D-24: after this plan the pin is COMPLETE. Every later correction is a dated continuation via `scripts/_addendum.py`, never an edit, because the ancestry guard takes `adds[-1]` and there is no recovery."
  artifacts:
    - path: "scripts/mitigation_gate.py"
      provides: "exists_clearing_point, ratchet_k, promote_to_full_fidelity, capacity_comparison, and the six-outcome __main__ self-check"
      contains: "if __name__ =="
  key_links:
    - from: "promote_to_full_fidelity"
      to: "ratchet_k"
      via: "internal call — one ratchet implementation, not two"
      pattern: "ratchet_k\\("
    - from: "exists_clearing_point"
      to: "ARM_CLAIMS"
      via: "claim lookup over the closed ARMS set proved at import"
      pattern: "ARM_CLAIMS"
---

<objective>
Close the pin: per-arm existence, the K ratchet and promotion rule, the capacity comparison with both
branches committed, and a `__main__` self-check that has been watched firing every verdict branch.

Purpose: a branch nobody has watched fire is a branch nobody has verified. This plan makes
`scripts/mitigation_gate.py` complete and final. After it, the file is closed: once
`results/phase20_retention_floor.json` exists, any edit turns
`test_phase20_prereg_is_frozen_before_every_phase20_result` permanently red, and `git rm` plus re-add
cannot launder it because the guard takes `adds[-1]`, the earliest add.

Output: `exists_clearing_point`, `ratchet_k`, `promote_to_full_fidelity`, `capacity_comparison`, the
three module-scope fixtures, and the `__main__` block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-pre-registration-the-three-condition-gate/20-CONTEXT.md
@.planning/phases/20-pre-registration-the-three-condition-gate/20-PATTERNS.md
@.planning/phases/20-pre-registration-the-three-condition-gate/20-RESEARCH.md
@.planning/phases/20-pre-registration-the-three-condition-gate/20-04-SUMMARY.md
@results/phase19_arm_erased.json
</context>

<interfaces>
<!-- Already in scripts/mitigation_gate.py after plans 20-01, 20-02 and 20-04. Spell exactly. -->
`_prove`, `V4_VERDICTS`, `_VERDICT_RELABEL`, `ARMS`, `ARM_CLAIMS`, `F_Y`, `F_C`,
`CHOSEN_CONSTANTS`, `K_RUNGS` (= `(48, 24, 16, 8)`), `MITIGATION_DECISION_RULE`,
`MITIGATION_GOAL_FRAMING`, `NEVER_TAUGHT_ARM`, `EXTRACTION_FLOOR_MIN_SEEDS`,
`EXTRACTION_FLOOR_PROVENANCE_KEYS`, `REPLICATION_PENDING_MARKER`, `extraction_ceiling`,
`tolerance_report`, `dialogue_gap_band`, `retention_cap`, `mitigation_point_verdict`.

Names this plan CREATES: `CAPACITY_BRANCHES`, `MECHANISM_KEYS`, `exists_clearing_point`,
`ratchet_k`, `promote_to_full_fidelity`, `capacity_comparison`, `FIXTURE_CLEARING_POINT`,
`FIXTURE_DESTROYED_MODEL`, `FIXTURE_TRUNCATED_SWEEP`.
Plan 20-06's pytest twin imports those three FIXTURE names verbatim.

Published Phase 19 readings the D-30 fixture must reuse (from `results/phase19_arm_erased.json`):
`dialogue_ppl` is a DICT with `adapter_on = 4.851119149910443` and `adapter_off = 4.573349214207799`;
`retention_ppl` is a **LIST** `[3.6709177253236867, 1000285]` — index it, never `.get()`.
`pre_erasure.dialogue_ppl.adapter_on = 5.815445876712191`. Taught gap = `1.242096662504392`;
M1 gap = `0.277770...`, i.e. `0.22362988653603388` of it, i.e. `77.637%` destroyed.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: exists_clearing_point — the existential computed per arm, mixed lists refused</name>
  <files>scripts/mitigation_gate.py</files>
  <read_first>
    - `scripts/phase19_erasure.py:3878-3883` — the module-scope `_prove` over a closed tuple and its
      message register, including the closing clause that names the ancestry guard as the deterrent.
    - `scripts/erasure_gate.py:150-153` — the `ValueError` domain guard shape for a pure function.
    - `scripts/erasure_gate.py:173-197` — `erasure_is_worth_attempting`, the committed example of a
      `-> (bool, reason)` return in this file.
    - `20-CONTEXT.md` D-28 (`:317-324`) — `arm` a required kwarg from a closed `ARMS`; the verdict
      carries it; the ∃ computed per arm; `exists_clearing_point` refuses a mixed-arm point list so
      "∃ a point" over the union cannot be formed; the claim-string table proved equal to `ARMS`.
    - `.planning/REQUIREMENTS.md:46-47` — GATE-07's wording: a DP point clearing carries a formal
      claim, an adversarial point clearing does not, and "∃ a point" over the union conflates them.
  </read_first>
  <action>
Append `exists_clearing_point(*, points, arm)` to `scripts/mitigation_gate.py` — keyword-only, no
defaults, returning the plain 2-tuple `(exists, claim)`.

`points` is a sequence of the 3-tuples `mitigation_point_verdict` returns: `(verdict, reasons,
point_arm)`. Body:
1. `_prove(arm in ARMS, ...)` naming the closed set.
2. `ValueError` when `points` is empty — an existential over an empty set is not a finding, it is a
   missing measurement.
3. THE REFUSAL (D-28): `_prove` that every element's third field equals `arm`. The message must name
   the distinct arms actually present and state that a mixed-arm point list cannot form "∃ a point",
   because a DP clear and an adversarial clear carry different claims and unioning them conflates a
   formal guarantee with a non-formal one. Use `_prove`, so this is a `SystemExit` that no caller can
   accidentally swallow as a falsy verdict.
4. `exists = any(verdict == "PASS" for verdict, _reasons, _arm in points)`. Only `PASS` counts —
   `INCONCLUSIVE` is by construction not a clear (D-29), and the docstring must say so, because
   letting INCONCLUSIVE satisfy an existential is exactly the collapse this project's
   honest-negatives discipline forbids.
5. `claim = ARM_CLAIMS[arm]` when `exists`, otherwise a string stating that no point in this arm
   cleared and naming how many points were examined — a denominator, per `20-CONTEXT.md:475-477`.

Docstring records D-28 and GATE-07: the existential is per arm and the refusal is what makes that
structural rather than conventional.
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/python -c "
import sys; sys.path.insert(0,'scripts')
import mitigation_gate as m
dp_pass=('PASS',['r'],'dp'); dp_fail=('FAIL',['r'],'dp'); adv_pass=('PASS',['r'],'adversarial'); dp_inc=('INCONCLUSIVE',['r'],'dp')
e,c = m.exists_clearing_point(points=[dp_fail,dp_pass], arm='dp'); assert e is True; assert c == m.ARM_CLAIMS['dp'], c
e,c = m.exists_clearing_point(points=[dp_fail,dp_inc], arm='dp'); assert e is False; assert '2' in c, c
try: m.exists_clearing_point(points=[dp_pass,adv_pass], arm='dp'); raise AssertionError('mixed-arm list accepted')
except SystemExit as exc: assert '[mitigation_gate]' in str(exc), exc
try: m.exists_clearing_point(points=[], arm='dp'); raise AssertionError('empty accepted')
except ValueError: pass
try: m.exists_clearing_point(points=[dp_pass], arm='lora'); raise AssertionError('unknown arm accepted')
except SystemExit: pass
print('OK')"</automated>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/python -m pytest -q && .venv/bin/ruff check . && .venv/bin/ruff format --check .</automated>
  </verify>
  <acceptance_criteria>
    - `exists_clearing_point(points=[("FAIL",["r"],"dp"), ("PASS",["r"],"dp")], arm="dp")` returns `(True, ARM_CLAIMS["dp"])` — the claim is looked up, never inlined.
    - `exists_clearing_point(points=[("PASS",["r"],"dp"), ("PASS",["r"],"adversarial")], arm="dp")` raises `SystemExit` whose message starts with `[mitigation_gate]` and contains both `dp` and `adversarial`.
    - `exists_clearing_point(points=[("INCONCLUSIVE",["r"],"dp")], arm="dp")` returns `exists is False` — an INCONCLUSIVE never satisfies the existential.
    - `exists_clearing_point(points=[], arm="dp")` raises `ValueError`.
    - `exists_clearing_point(points=[("PASS",["r"],"dp")], arm="lora")` raises `SystemExit`.
    - The not-cleared claim string contains the point count (its denominator).
    - `exists_clearing_point.__doc__` contains the literals `D-28` and `GATE-07`.
    - `.venv/bin/python -m pytest -q` exits 0.
  </acceptance_criteria>
  <done>The existential is computed per arm, a mixed-arm list aborts loudly, and INCONCLUSIVE is proved not to satisfy it.</done>
</task>

<task type="auto">
  <name>Task 2: The K ratchet, the promotion rule, and the capacity comparison with both branches committed</name>
  <files>scripts/mitigation_gate.py</files>
  <read_first>
    - `scripts/phase18_extraction.py:84-93` — the ATK-03 / P18-4 record on reducing K after seeing a
      null, including the sentence at `:90-91` ("this is the one moment / the pin leaves open for
      it") and `K = 48` at `:93`. This is the basis for the ratchet; quote its reasoning.
    - `.planning/REQUIREMENTS.md:128-144` — CAL-02, CAL-04, CAL-05 and CAL-03, plus the K / cost
      table at `:118-124`. Note that CAL-04 contradicts itself across sentences 1 and 3 and that
      D-19 resolves it with a closed menu now plus rung selection in Phase 23.
    - `.planning/ROADMAP.md:139-144` — the pre-registration boundary: a RESOURCE budget measured
      beforehand is not an OUTCOME threshold measured beforehand; Z lives in
      `scripts/mitigation_budget.py` with an AST guard forbidding the gate from importing it.
    - `20-CONTEXT.md` D-19, D-20, D-25, D-26, D-27 (`:188-270`).
    - `scripts/phase19_erasure.py:944-961` — `floor_branch`: a pure function returning a branch NAME
      from a closed tuple, with the tie order stated. This is the shape `capacity_comparison` takes.
  </read_first>
  <action>
Append four things to `scripts/mitigation_gate.py`.

**`ratchet_k(*, fixed_k, proposed_k)`** returning the accepted int. `_prove` that both values are
members of `K_RUNGS` (the closed menu, D-19), then `_prove(proposed_k >= fixed_k, ...)` with a
message that quotes the reasoning at `scripts/phase18_extraction.py:84-93`: reducing K after seeing a
null is the ATK-03 / P18-4 weakening, because fewer draws means less power to observe extraction,
i.e. an easier null, and pre-flight is the one moment the pin leaves open for it. Returns
`proposed_k`. Docstring: the ratchet is STRUCTURAL — a selected K may only INCREASE, never decrease,
once fixed.

**`promote_to_full_fidelity(*, verdict, reasons, curve_k, full_k)`** returning `(promote, reason)`.
It calls `ratchet_k(fixed_k=curve_k, proposed_k=full_k)` so the ratchet has ONE implementation and a
full-fidelity K below the curve K aborts. A point is promoted when `verdict == "PASS"`, or when
`verdict == "INCONCLUSIVE"` and the last reason starts with `REPLICATION_PENDING_MARKER` — the
gate-candidate INCONCLUSIVE, which is distinguished from the truncated-sweep INCONCLUSIVE by that
constant rather than by a second hand-typed spelling. `K` arrives as a REQUIRED KWARG (D-20): this is
what lets the promotion rule live in the gate while `scripts/mitigation_gate.py` never imports
`scripts/mitigation_budget.py`, keeping the AST guard intact. Say that in the docstring, and say why
the split exists — a resource calibration must not be mistakable for an outcome-threshold peek
(`ROADMAP.md:139-144`).

**`MECHANISM_KEYS = ("sigma", "steps", "delta", "q")`** and
**`CAPACITY_BRANCHES = ("not-comparable", "capacity-recovers", "capacity-destroys",
"recovery-at-both-capacities", "null-at-both-capacities")`**.

**`capacity_comparison(*, small_capacity, large_capacity, small_cleared, large_cleared,
small_mechanism, large_mechanism, epsilon_independent_of_n, fallback_epsilon_tolerance)`** returning
`(branch, reasons)` where `branch` is a member of `CAPACITY_BRANCHES` and `reasons` is a list of
strings in the `erasure_gate` reason register.

Body:
1. `ValueError` when `small_capacity >= large_capacity` — the direction must be unambiguous, since
   the whole comparison is n=8 versus n=64.
2. COMPARABILITY.
   - When `epsilon_independent_of_n` is True: require `small_mechanism` and `large_mechanism` to
     agree EXACTLY on every key in `MECHANISM_KEYS` — D-25, ZERO tolerance constant, because ε is a
     deterministic function of those parameters, so comparing at identical mechanism parameters IS
     comparing at equivalent ε_fact and capacity N stays the only genuinely free variable. Any
     disagreement returns `"not-comparable"` with a reason naming the differing keys and both values.
     `_prove` that both mappings carry all four keys, so a missing key is an abort rather than a
     silently-skipped comparison.
   - When `epsilon_independent_of_n` is False (CAL-03 falsified the premise research marks
     `[INFERENCE]`, not measured): this is D-26's committed fallback. `_prove(fallback_epsilon_tolerance
     is not None, ...)` with a message stating explicitly that the fallback tolerance is a THIRD
     chosen constant, that it was DELIBERATELY NOT SET in Phase 20, and that it must be decided and
     named before Phase 21's CAL-03 runs — flagged rather than smuggled, on the same standard applied
     to `F_C`. Then compare `small_mechanism["epsilon"]` and `large_mechanism["epsilon"]` within the
     supplied tolerance; outside it, return `"not-comparable"`.
3. TOTAL DISPATCH over the four `(small_cleared, large_cleared)` combinations:
   `(False, True) -> "capacity-recovers"`; `(False, False) -> "null-at-both-capacities"`;
   `(True, False) -> "capacity-destroys"`; `(True, True) -> "recovery-at-both-capacities"`.
   Implement it as a dict keyed by the boolean pair and add a MODULE-SCOPE `_prove` asserting that
   the dispatch's value set is a subset of `CAPACITY_BRANCHES` and that its key set is all four
   boolean pairs — the `phase19_erasure.py:3878-3883` register, and the Phase 16 `licensed_headline`
   lesson that a decision function must be total with a first-class branch for the outcome the
   evidence predicts and no investigate-the-instrument escape hatch.
4. Reasons must name both capacities, both cleared flags, and which comparability route was taken.

Docstring records D-27: BOTH of GATE-10's named branches are publishable — recovery at n=64 that n=8
did not achieve at equivalent ε_fact is a finding about where capacity stops destroying the
mitigation, and no recovery confirms the null at two capacities — and NEITHER may be chosen after
seeing data, which is why the whole rule is committed here, before either run.
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/python -c "
import sys; sys.path.insert(0,'scripts')
import mitigation_gate as m
assert m.ratchet_k(fixed_k=16, proposed_k=48) == 48
try: m.ratchet_k(fixed_k=48, proposed_k=24); raise AssertionError('ratchet allowed a decrease')
except SystemExit as e: assert '[mitigation_gate]' in str(e), e
try: m.ratchet_k(fixed_k=48, proposed_k=32); raise AssertionError('non-rung accepted')
except SystemExit: pass
p,r = m.promote_to_full_fidelity(verdict='PASS', reasons=['x'], curve_k=16, full_k=48); assert p is True
p,r = m.promote_to_full_fidelity(verdict='INCONCLUSIVE', reasons=['x', m.REPLICATION_PENDING_MARKER+' ...'], curve_k=16, full_k=48); assert p is True, r
p,r = m.promote_to_full_fidelity(verdict='INCONCLUSIVE', reasons=['sweep truncated'], curve_k=16, full_k=48); assert p is False, r
p,r = m.promote_to_full_fidelity(verdict='FAIL', reasons=['x'], curve_k=16, full_k=48); assert p is False
mech = {'sigma':1.0,'steps':1250,'delta':1e-5,'q':1}
b,_ = m.capacity_comparison(small_capacity=8,large_capacity=64,small_cleared=False,large_cleared=True,small_mechanism=mech,large_mechanism=dict(mech),epsilon_independent_of_n=True,fallback_epsilon_tolerance=None); assert b=='capacity-recovers', b
b,_ = m.capacity_comparison(small_capacity=8,large_capacity=64,small_cleared=False,large_cleared=False,small_mechanism=mech,large_mechanism=dict(mech),epsilon_independent_of_n=True,fallback_epsilon_tolerance=None); assert b=='null-at-both-capacities', b
b,_ = m.capacity_comparison(small_capacity=8,large_capacity=64,small_cleared=False,large_cleared=True,small_mechanism=mech,large_mechanism={**mech,'sigma':2.0},epsilon_independent_of_n=True,fallback_epsilon_tolerance=None); assert b=='not-comparable', b
try:
    m.capacity_comparison(small_capacity=8,large_capacity=64,small_cleared=False,large_cleared=True,small_mechanism=mech,large_mechanism=dict(mech),epsilon_independent_of_n=False,fallback_epsilon_tolerance=None); raise AssertionError('fallback ran with an unset tolerance')
except SystemExit as e: assert 'D-26' in str(e), e
try: m.capacity_comparison(small_capacity=64,large_capacity=8,small_cleared=False,large_cleared=True,small_mechanism=mech,large_mechanism=dict(mech),epsilon_independent_of_n=True,fallback_epsilon_tolerance=None); raise AssertionError('direction not guarded')
except ValueError: pass
assert set(m.MECHANISM_KEYS)=={'sigma','steps','delta','q'}
print('OK')"</automated>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/python -m pytest -q && .venv/bin/ruff check . && .venv/bin/ruff format --check .</automated>
  </verify>
  <acceptance_criteria>
    - `ratchet_k(fixed_k=16, proposed_k=48)` returns `48`; `ratchet_k(fixed_k=48, proposed_k=24)` raises `SystemExit` whose message contains `ATK-03` (or `P18-4`); `ratchet_k(fixed_k=48, proposed_k=32)` raises `SystemExit` because `32` is not in `K_RUNGS`.
    - `promote_to_full_fidelity` returns `True` for `PASS` and for an `INCONCLUSIVE` whose last reason starts with `REPLICATION_PENDING_MARKER`, and `False` for `FAIL` and for a truncated-sweep `INCONCLUSIVE`.
    - `promote_to_full_fidelity(verdict="PASS", reasons=["x"], curve_k=48, full_k=16)` raises `SystemExit` — the ratchet is reached through one implementation, not duplicated.
    - `capacity_comparison` returns `"capacity-recovers"`, `"null-at-both-capacities"`, `"capacity-destroys"` and `"recovery-at-both-capacities"` for the four cleared-flag combinations at identical mechanisms, and `"not-comparable"` when any `MECHANISM_KEYS` entry differs.
    - With `epsilon_independent_of_n=False` and `fallback_epsilon_tolerance=None`, `capacity_comparison` raises `SystemExit` whose message contains `D-26` and `CAL-03`.
    - `capacity_comparison(small_capacity=64, large_capacity=8, ...)` raises `ValueError`.
    - `.venv/bin/python -c "import ast;t=ast.parse(open('scripts/mitigation_gate.py').read());print(sorted({n.value for n in ast.walk(t) if isinstance(n,ast.Constant) and isinstance(n.value,str) and n.value in ('sigma','steps','delta','q')}))"` prints `['delta', 'q', 'sigma', 'steps']` — the four mechanism keys, no fifth.
    - `capacity_comparison.__doc__` contains the literals `D-25`, `D-26`, `D-27` and the phrase `zero tolerance`.
    - `.venv/bin/python -m pytest -q` exits 0.
  </acceptance_criteria>
  <done>K's menu, ratchet and promotion rule are committed with the ratchet reachable through exactly one implementation; the capacity rule is total over all four outcomes with zero tolerance on the primary route and a loud, D-26-naming refusal on the fallback route.</done>
</task>

<task type="auto">
  <name>Task 3: The six-outcome __main__ self-check with the destroyed-model fixture</name>
  <files>scripts/mitigation_gate.py</files>
  <read_first>
    - `scripts/erasure_gate.py:258-291` — the whole `__main__` block. Copy five conventions: the
      `# pragma: no cover - self-check, not a test suite` comment on the guard line; bare `assert`
      with no pytest and no fixtures; BOTH halves of a differential pair asserted from
      otherwise-identical kwargs; a failure payload carrying the observed value
      (`assert v == "SUCCESS", (v, rs)`); and a terminal `print` that DERIVES its count
      (`len(ERASURE_DECISION_RULE)`) rather than retyping it.
    - `results/phase19_arm_erased.json` — read it and take the fixture numbers from the file, not
      from a planning document. `dialogue_ppl` is a DICT (`adapter_on`, `adapter_off`, `n_targets`);
      `retention_ppl` is a **LIST** `[ppl, n]` — index it, do not `.get()`.
    - `20-CONTEXT.md` D-30 (`:337-342`) — the fixture is built from Phase 19's real published M1
      readings and is LABELLED a fixture, never a second reading of the experiment. Precedent: 19-16
      ran its counterfactual through the committed gate exactly this way.
    - `20-RESEARCH.md` "Proving every verdict branch fires" — the six-outcome table, and the rule
      that PRECEDENCE is proved DIFFERENTIALLY: an INCONCLUSIVE fixture that would otherwise return
      PASS proves nothing about precedence over FAIL.
  </read_first>
  <action>
Append three MODULE-SCOPE fixture dicts and the `__main__` block to `scripts/mitigation_gate.py`.

The fixtures are module-scope constants — NOT inside the `if __name__` guard — because plan 20-06's
pytest twin must import the SAME objects; a second transcription is a second fixture free to stop
matching.

**`FIXTURE_DESTROYED_MODEL`** — a complete kwargs dict for `mitigation_point_verdict`, D-30. Its
dialogue and retention readings are Phase 19's REAL published M1 numbers, taken from
`results/phase19_arm_erased.json`: `point_dialogue_ppl_on = 4.851119149910443`,
`point_dialogue_ppl_off = 4.573349214207799`, `point_retention_ppl = 3.6709177253236867`, and
`control_gap = 1.242096662504392` (the untouched taught adapter's own gap,
`5.815445876712191 - 4.573349214207799`). Every OTHER field is a fabricated fixture value chosen so
the point is otherwise well-formed: non-zero extraction with a corroborating NLL, an untruncated
sweep, and `replicated_at_second_seed=True`, so the FAIL is produced by the three conditions and not
by a precedence branch. A comment immediately above it must carry the D-30 label verbatim — this is
a FIXTURE built from published readings, LABELLED as such, and it is NEVER a second reading of the
Phase 19 experiment. It must also record what the fixture demonstrates: M1 retained
`0.22362988653603388` of the dialogue gap (`77.637%` destroyed), which is BELOW
`F_C x control_gap`, so the D-01 gap band catches exactly the destruction the superseded one-sided
cap missed by only `+0.267390`.

**`FIXTURE_CLEARING_POINT`** — a complete kwargs dict that returns `PASS`: extraction below the
ceiling, both recall legs at or above `F_Y` times their controls, the dialogue gap inside the band,
retention below the cap, an untruncated sweep, `replicated_at_second_seed=True`, and a valid
`extraction_floor_provenance` naming `NEVER_TAUGHT_ARM` and two distinct seeds. Comment it as a
SYNTHETIC fixture — no v4.0 arm exists, so these are constructed inputs, not measurements.

**`FIXTURE_TRUNCATED_SWEEP`** — `FIXTURE_DESTROYED_MODEL`'s fields with `sweep_extraction_rates`
replaced by values that never cross the ceiling. Build it by dict-merging from
`FIXTURE_DESTROYED_MODEL` rather than retyping the fields, so the two cannot drift.

`if __name__ == "__main__":  # pragma: no cover - self-check, not a test suite` then, with bare
asserts whose payload carries the observed value, exercise SIX outcomes and print a line per outcome
naming the branch:

1. `PASS` — `mitigation_point_verdict(**FIXTURE_CLEARING_POINT)`.
2. `FAIL` — `mitigation_point_verdict(**FIXTURE_DESTROYED_MODEL)`. GATE-09 / SC3's named
   observation.
3. `INCONCLUSIVE`, GATE-05, DIFFERENTIALLY: `FIXTURE_DESTROYED_MODEL` merged with
   `point_extraction_successes=0` and `zero_extraction_has_nll=False`. Assert the base fixture
   returns `FAIL` and this variant returns `INCONCLUSIVE` with a ONE-element reason list —
   demonstrating precedence over FAIL, not merely over PASS.
4. `INCONCLUSIVE`, GATE-06, DIFFERENTIALLY: `mitigation_point_verdict(**FIXTURE_TRUNCATED_SWEEP)`
   returns `INCONCLUSIVE` while its FAIL-producing base returns `FAIL`, and assert the reason list is
   longer than one element so the per-condition detail survived the late return.
5. `INCONCLUSIVE`, GATE-08 / D-29, DIFFERENTIALLY against PASS: `FIXTURE_CLEARING_POINT` merged with
   `replicated_at_second_seed=False` returns `INCONCLUSIVE`, and assert the returned tuple has
   exactly three elements — there is no fourth `provisional` slot.
6. GATE-07 arm identity: assert the third element of the `PASS` result equals its `arm`, and assert
   that `exists_clearing_point` over a mixed-arm list raises `SystemExit` (wrap in try/except and
   assert the raise, since a bare `assert` cannot express it).

Then exercise the remaining committed surfaces so the self-check covers the whole pin: `ratchet_k`
refusing a decrease; `capacity_comparison` returning both of GATE-10's named branches; and
`tolerance_report` on the D-12 COUNTERFACTUAL `X = 0.3216515249612375` at `n=104` returning `25`,
with an inline comment labelling it a counterfactual because the real extraction floor is not
measurable until Phase 23 (D-13).

Terminal `print` must DERIVE its counts, in the `erasure_gate.py:290` register — for example the
number of committed rule clauses from `len(MITIGATION_DECISION_RULE)`, the arm count from
`len(ARMS)`, the rung count from `len(K_RUNGS)` and the branch count from `len(CAPACITY_BRANCHES)`.
Never retype a count.

**After this task the pin is COMPLETE.** Record in the module docstring (extending, not rewriting)
that `scripts/mitigation_gate.py` is closed as of this plan, and that corrections are dated
continuations via `scripts/_addendum.py::append_addendum(path, addendum, *, pending, recorded)` —
both keywords required — never edits, because the ancestry guard takes `adds[-1]` and there is no
recovery (D-24).
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/python scripts/mitigation_gate.py</automated>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/python -c "
import json,sys; sys.path.insert(0,'scripts')
import mitigation_gate as m
a=json.load(open('results/phase19_arm_erased.json'))
assert m.FIXTURE_DESTROYED_MODEL['point_dialogue_ppl_on'] == a['dialogue_ppl']['adapter_on'] == 4.851119149910443
assert m.FIXTURE_DESTROYED_MODEL['point_retention_ppl'] == a['retention_ppl'][0] == 3.6709177253236867
assert m.FIXTURE_DESTROYED_MODEL['point_dialogue_ppl_off'] == a['dialogue_ppl']['adapter_off']
v,r,arm = m.mitigation_point_verdict(**m.FIXTURE_DESTROYED_MODEL); assert v=='FAIL',(v,r)
v,r,arm = m.mitigation_point_verdict(**m.FIXTURE_CLEARING_POINT); assert v=='PASS',(v,r)
v,r,_ = m.mitigation_point_verdict(**{**m.FIXTURE_DESTROYED_MODEL,'point_extraction_successes':0,'zero_extraction_has_nll':False}); assert v=='INCONCLUSIVE' and len(r)==1,(v,r)
v,r,_ = m.mitigation_point_verdict(**m.FIXTURE_TRUNCATED_SWEEP); assert v=='INCONCLUSIVE' and len(r)>1,(v,r)
out = m.mitigation_point_verdict(**{**m.FIXTURE_CLEARING_POINT,'replicated_at_second_seed':False}); assert len(out)==3 and out[0]=='INCONCLUSIVE',out
print('OK')"</automated>
    <automated>cd /Users/juliorcoelho/PersonaCore && .venv/bin/python -m pytest -q && .venv/bin/ruff check . && .venv/bin/ruff format --check .</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python scripts/mitigation_gate.py` exits 0 and its stdout names all six outcomes (`PASS`, `FAIL`, and the three INCONCLUSIVE branches plus arm identity) — verified by `.venv/bin/python scripts/mitigation_gate.py | grep -c "PASS\|FAIL\|INCONCLUSIVE"` outputting at least `6`.
    - `FIXTURE_DESTROYED_MODEL["point_dialogue_ppl_on"]` equals `results/phase19_arm_erased.json`'s `dialogue_ppl.adapter_on` (`4.851119149910443`) by direct comparison against the parsed file, and `FIXTURE_DESTROYED_MODEL["point_retention_ppl"]` equals `retention_ppl[0]` (`3.6709177253236867`) — indexed as a LIST, not `.get()`.
    - `mitigation_point_verdict(**FIXTURE_DESTROYED_MODEL)` returns `FAIL`; the GATE-05 variant of the SAME fixture returns `INCONCLUSIVE` with `len(reasons) == 1` — precedence over FAIL demonstrated differentially.
    - `mitigation_point_verdict(**FIXTURE_TRUNCATED_SWEEP)` returns `INCONCLUSIVE` with `len(reasons) > 1`.
    - `mitigation_point_verdict(**{**FIXTURE_CLEARING_POINT, "replicated_at_second_seed": False})` returns a 3-tuple whose first element is `INCONCLUSIVE`, while the unmodified fixture returns `PASS` — precedence over PASS demonstrated differentially.
    - The comment above `FIXTURE_DESTROYED_MODEL` contains the literals `D-30`, `FIXTURE`, `0.22362988653603388` and `77.637`.
    - `FIXTURE_TRUNCATED_SWEEP` is constructed by merging from `FIXTURE_DESTROYED_MODEL` — confirmed by `.venv/bin/python -c "import sys;sys.path.insert(0,'scripts');import mitigation_gate as m;print(sum(1 for k,v in m.FIXTURE_DESTROYED_MODEL.items() if m.FIXTURE_TRUNCATED_SWEEP.get(k)==v))"` printing at least `19` of the 21 keys.
    - The terminal `print` derives its counts: `grep -c "len(MITIGATION_DECISION_RULE)" scripts/mitigation_gate.py` outputs at least `1`.
    - `.venv/bin/python -m pytest -q` exits 0 and `.venv/bin/ruff check . && .venv/bin/ruff format --check .` exit 0.
    - `git status --porcelain pyproject.toml` prints nothing and `git log --diff-filter=A -- 'results/phase20_*'` prints nothing.
  </acceptance_criteria>
  <done>Every verdict branch has been observed firing through `__main__`, the three precedence claims are demonstrated against the verdict each overrides, the destroyed-model fixture's numbers are proved equal to the published artifact's, and the pin is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| outcome thresholds ↔ resource parameters | The gate/budget split. A resource calibration measured beforehand must never be mistakable for an outcome threshold measured beforehand. |
| a written branch ↔ a fired branch | A branch nobody has watched fire is a branch nobody has verified. |
| a published measurement ↔ a fixture built from it | D-30's boundary: reusing real M1 readings must not read as a second opinion on the Phase 19 experiment. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-25 | Tampering | K reduced after seeing a null | mitigate | `ratchet_k` `_prove`s `proposed_k >= fixed_k` over the closed `K_RUNGS`, quoting `scripts/phase18_extraction.py:84-93`; `promote_to_full_fidelity` reaches it through the single implementation |
| T-20-26 | Elevation of privilege | the gate importing `mitigation_budget` to read K | mitigate | D-20 — K is a required kwarg on `promote_to_full_fidelity`; plan 20-06's AST guard asserts `"mitigation_budget" not in imported` |
| T-20-27 | Tampering | a GATE-10 branch chosen after seeing data | mitigate | Both named branches plus the fallback are committed in this plan, before either run; the dispatch is TOTAL over all four cleared-flag combinations with a module-scope `_prove`, so there is no fall-through to select into |
| T-20-28 | Spoofing | a third chosen constant smuggled in as the fallback tolerance | mitigate | D-26 — `fallback_epsilon_tolerance` has no committed value; taking the fallback route with it unset raises a `SystemExit` naming D-26 and CAL-03 |
| T-20-29 | Spoofing | the arm existential formed over the union of both arms | mitigate | `exists_clearing_point` `_prove`s single-arm membership across the whole point list before computing anything |
| T-20-30 | Repudiation | a fixture reading as a second measurement of the Phase 19 experiment | mitigate | D-30 — the fixture is labelled in a comment carrying `D-30` and `FIXTURE`, and its numbers are asserted EQUAL to the parsed `results/phase19_arm_erased.json` rather than transcribed |
| T-20-31 | Tampering | the pytest twin drifting from the `__main__` fixtures | mitigate | The three fixtures are module-scope constants imported by plan 20-06's twin; a second transcription is a second fixture free to stop matching |
| T-20-32 | Tampering | a post-close edit to the pin | accept (this plan) / mitigate (downstream) | While no `results/phase20_*` artifact exists an edit is legal and green. The moment plan 20-07 commits the artifact this becomes irreversible; the closure is recorded in the module docstring with the `_addendum.py` correction path (D-24), and plan 20-07 opens with a blocking checkpoint |
</threat_model>

<verification>
- `.venv/bin/python scripts/mitigation_gate.py` exits 0 and names six outcomes.
- `.venv/bin/python -m pytest -q` exits 0.
- `.venv/bin/ruff check . && .venv/bin/ruff format --check .` exit 0.
- `git status --porcelain pyproject.toml` prints nothing.
- `git log --diff-filter=A -- 'results/phase20_*'` prints nothing.
- Re-run plan 20-01 Task 2's float audit against the FINISHED module and record the result: the
  ASSIGNED module-scope chosen constants are still exactly `F_Y` and `F_C`, and every other float in
  the file lives inside a `FIXTURE_*` assignment, a docstring or a message.
</verification>

<success_criteria>
- `scripts/mitigation_gate.py` is COMPLETE: verdict domain, arms, chosen constants, K menu, decision
  prose, X plus tripwire plus tolerance reporter, both (c) legs, the three-condition verdict, the
  per-arm existential, the ratchet, the promotion rule, the capacity rule and a six-outcome
  `__main__`.
- Every one of the six outcomes has been observed, and the three precedence claims were each proved
  against the verdict they override.
- The destroyed-model fixture's readings are proved equal to `results/phase19_arm_erased.json`.
- Still zero `results/phase20_*` files in git.
</success_criteria>

<output>
Create `.planning/phases/20-pre-registration-the-three-condition-gate/20-05-SUMMARY.md` when done.
Record the full stdout of `.venv/bin/python scripts/mitigation_gate.py`, the sha256 of
`scripts/mitigation_gate.py`, and an explicit statement that the pin is CLOSED — every later
correction is a dated continuation via `_addendum.py`, never an edit.
</output>
