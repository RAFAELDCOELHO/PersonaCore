---
phase: quick-260802-h3g
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/_verdict.py
  - scripts/teach_persona.py
  - scripts/phase14_factset_gate.py
  - tests/test_phase14_teaching.py
  - tests/test_phase14_factset.py
  - .planning/phases/14-teach-then-recall-demo/14-SECURITY.md
autonomous: true
requirements: [T-14-06]
must_haves:
  truths:
    - "A report whose PENDING verdict section is FOLLOWED by a prose mention of '## Verdict' is re-drivable without --force through teach_persona._refuse_clobber"
    - "The same report shape is re-drivable through phase14_factset_gate's extracted guard"
    - "A file with NO '## Verdict' section is REFUSED (SystemExit), never overwritten blind, at both sites"
    - "A genuinely recorded (non-PENDING) verdict still raises SystemExit at both sites, and --force still overrides"
    - "The naive split('## Verdict')[-1] idiom is gone from scripts/teach_persona.py and scripts/phase14_factset_gate.py"
    - "The regex lives in exactly one importable place, scripts/_verdict.py, which imports only re"
    - "phase14_factset_gate's guard is callable and testable as a module-level function without loading a checkpoint"
    - "The full CPU-only suite stays green, including the T-14-17 clean-room tests"
  artifacts:
    - path: "scripts/_verdict.py"
      provides: "the single anchored verdict-section regex + recorded_verdict(text) accessor"
      contains: "def recorded_verdict"
    - path: "scripts/phase14_factset_gate.py"
      provides: "the inline main() guard extracted to a module-level function"
      contains: "def assert_report_not_clobbered"
    - path: "tests/test_phase14_teaching.py"
      provides: "the RED-first regression test carrying BOTH the naive control and the anchored read"
      contains: "recorded_verdict"
    - path: "tests/test_phase14_factset.py"
      provides: "the gate-side guard test (torch-free of any checkpoint load)"
      contains: "assert_report_not_clobbered"
  key_links:
    - from: "scripts/teach_persona.py::_refuse_clobber"
      to: "scripts/_verdict.py::recorded_verdict"
      via: "sibling import (scripts/ is sys.path[0])"
      pattern: "recorded_verdict"
    - from: "scripts/phase14_factset_gate.py::assert_report_not_clobbered"
      to: "scripts/_verdict.py::recorded_verdict"
      via: "sibling import (scripts/ is sys.path[0])"
      pattern: "recorded_verdict"
    - from: "scripts/phase14_factset_gate.py::main"
      to: "scripts/phase14_factset_gate.py::assert_report_not_clobbered"
      via: "call at the top of main(), replacing the inline block"
      pattern: "assert_report_not_clobbered\\(\\)"
---

<objective>
Finish the CR-02 fix. Commit `f293fd3` anchored the report clobber guard on the first `## Verdict`
SECTION in `scripts/phase14_recall.py` only. Two sites still take the tail after the LAST
occurrence of the literal (`split("## Verdict")[-1]`) and carry the identical latent defect:

  - `scripts/teach_persona.py:1143` inside `_refuse_clobber(report_path, force)`
  - `scripts/phase14_factset_gate.py:120` inline inside `main()` — not extracted, therefore
    currently unreachable from any test

The defect: when a report ALSO mentions `## Verdict` in prose after its verdict section, the tail
lands in that prose, which never contains `PENDING`, so the guard concludes a verdict was recorded
and refuses to write. Every legitimate re-drive of an interrupted run then aborts, leaving
`--force` (which disables the guard outright) as the only way through. An operator who learns
`--force` is always required passes it after a human HAS recorded a verdict — and the guard
destroys precisely the hand-written evidence it exists to protect. That is the data-loss path.

Both reports contain `## Verdict` exactly once today, so this is latent, not live. It goes live
the moment either report grows a second mention.

Purpose: kill the idiom at both remaining in-scope sites and leave ONE importable copy of the
regex, so the next site cannot reintroduce it by copy-paste.
Output: `scripts/_verdict.py`; both guards rewired; two RED-first tests; three description-only
corrections in `14-SECURITY.md`.

**Out of scope — do NOT touch, mention in the summary only:** `scripts/measure_inflation.py:70`
(Phase 13) and `scripts/finetune_smoke.py:727` (Phase 12) carry the same idiom.
**Do NOT modify `scripts/phase14_recall.py`.** Its `_recorded_verdict` is already correct, is
referenced by name in a passing test (`tests/test_phase14_scoring.py:638,641,650`), and cannot
take a module-level sibling import without risking the T-14-17 clean-room property — the demo
asserts `phase14_factset`/`teach_persona` are absent from a fresh demo process, which is why
recall deliberately uses SIX function-scoped imports. Leave it alone; note the remaining
duplication in the summary.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

The reference implementation to copy (already correct — READ, do not edit):
@scripts/phase14_recall.py  (lines 1600-1643: `_VERDICT_SECTION`, `_recorded_verdict`, `assert_report_not_clobbered`)

The CR-02 proof test whose shape the new tests mirror:
@tests/test_phase14_scoring.py  (lines 617-652)

Targets:
@scripts/teach_persona.py  (lines 1136-1148 `_refuse_clobber`; lines 165-186 `_require_go_verdict` — the anchored idiom already in this file; line 64 — the sibling-import idiom)
@scripts/phase14_factset_gate.py  (lines 115-126 — the inline guard; line 44 — the sibling-import idiom)

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase — no exploration required. -->

The regex and accessor to replicate, verbatim from `scripts/phase14_recall.py:1604-1614`:

```python
_VERDICT_SECTION = re.compile(r"^## Verdict\b(.*?)(?=^## |\Z)", re.M | re.S)

def _recorded_verdict(text):
    section = _VERDICT_SECTION.search(text)
    return section.group(1) if section else None
```

Guard shape to mirror, from `scripts/phase14_recall.py:1617-1643` — zero-arg, reads the module
global path and `sys.argv` at call time (which is exactly what makes it monkeypatchable):

```python
def assert_report_not_clobbered():
    if RECALL_REPORT_PATH.exists() and "--force" not in sys.argv[1:]:
        recorded = _recorded_verdict(RECALL_REPORT_PATH.read_text(encoding="utf-8"))
        if recorded is None or "PENDING" not in recorded:
            raise SystemExit(...)
```

Note the `recorded is None or ...` — "no verdict section at all" is REFUSE, not overwrite-blind.

The sibling-import idiom, established at `scripts/teach_persona.py:64` and
`scripts/phase14_factset_gate.py:44`:

```python
import phase14_factset as fs  # noqa: E402  (sibling script; scripts/ is sys.path[0])
```

Signature that MUST be preserved — two live callers at `scripts/teach_persona.py:1123` and `:1506`:

```python
def _refuse_clobber(report_path, force):  # force is a bool: `"--force" in argv`
```

Both target test files already put `scripts/` on `sys.path` before their `importlib` loads
(`tests/test_phase14_teaching.py:49`, `tests/test_phase14_factset.py:38`), so the new sibling
import resolves in both. Use each file's existing `_load` helper; do not add a second loader.

Clean-room clearance, already checked — importing `phase14_factset_gate` in
`tests/test_phase14_factset.py` is SAFE despite it doing a real `import phase14_factset`:
`tests/test_phase14_scoring.py:383-392` pops and restores both names around its check, and
`tests/test_phase14_demo.py:571-572` asserts membership on a SUBPROCESS result, not on pytest's
own process.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add scripts/_verdict.py — the one copy of the regex, no callers yet</name>
  <files>scripts/_verdict.py</files>
  <action>
Create `scripts/_verdict.py`: a phase-neutral helper exporting `recorded_verdict(text)`, lifted
from `phase14_recall._recorded_verdict` (see `<interfaces>`). Public name, no leading underscore
on the function; the module name keeps its underscore.

HARD CONSTRAINT: the only import in this file is `import re`. No torch, no personacore, no fact
set — it is imported by scripts that must stay cheap, and by tests that must stay CPU-only.

Docstring must state (a) that it is the single copy and why five copies of this regex is what
produced CR-02, (b) that `None` and an empty body are deliberately different: no verdict section
means the file is not this writer's output and the caller refuses rather than overwriting blind.

Deliberately DO NOT wire any caller in this task. Both guards keep the naive split; the bug stays
live so Task 2 can observe it failing. Nothing about behaviour changes here.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import sys; sys.path.insert(0,'scripts'); import _verdict; t='## Verdict\n\nPENDING\n\n## Ship\n\nsee \`## Verdict\` above\n'; assert 'PENDING' in _verdict.recorded_verdict(t); assert _verdict.recorded_verdict('# nope\n') is None; assert 'PENDING' not in t.split('## Verdict')[-1]; print('ok')"</automated>
    <automated>test "$(grep -c '^\(import\|from\) ' scripts/_verdict.py)" = "1" &amp;&amp; grep -q '^import re$' scripts/_verdict.py &amp;&amp; echo "single-import ok"</automated>
    <automated>make lint</automated>
  </verify>
  <done>`scripts/_verdict.py` exists, imports only `re`, `recorded_verdict` returns the PENDING body for a report with a trailing prose mention and `None` for a file with no verdict section. No caller imports it yet; `make test` is unaffected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED-first — prove both guards break, then anchor them</name>
  <files>tests/test_phase14_teaching.py, tests/test_phase14_factset.py, scripts/teach_persona.py, scripts/phase14_factset_gate.py</files>
  <behavior>
TDD IS THE POINT OF THIS TASK, NOT A FORMALITY. Execute the steps in this order. A test written
after the fix, or a RED step that was assumed rather than actually run, FAILS this task.

  (a) Write both tests. Change NO production code yet.
  (b) RUN them against the UNMODIFIED naive-split guards. OBSERVE them FAIL. Paste the REAL
      pytest failure output (the assertion/exception lines) into the SUMMARY.
  (c) Only then apply the two fixes.
  (d) Re-run. Observe PASS. Paste the GREEN line into the SUMMARY.

Expected RED, stated up front so a different failure is a signal to stop and read:
  - teach_persona: `_refuse_clobber` raises `SystemExit` on the legitimate re-drive — a
    behavioural failure that proves the defect.
  - phase14_factset_gate: `AttributeError` — the guard is inline in `main()` and does not exist
    as a callable. That IS the honest RED for an extraction; the shared behaviour is proven by
    the teach_persona test. Do not contort the gate test to force a behavioural RED.

--- Test 1, in `tests/test_phase14_teaching.py` (teach_persona already loaded there) ---

Both behaviours visible in ONE place. Synthetic report: a PENDING verdict section FOLLOWED by a
`## Ship Decision` section whose prose quotes `` `## Verdict` above `` (the real D-12 wording from
`phase14_recall.SHIP_DECISION_HEADER:1590-1598`). Assert, in this order:

  - Test 1a — the NAIVE CONTROL, kept deliberately: `"PENDING" not in text.split("## Verdict")[-1]`.
    This documents WHY the regex exists and makes a regression back to `split` fail HERE with the
    reason written beside it. Comment it as an intentional control so no future cleanup deletes it.
  - Test 1b — the anchored read: `"PENDING" in _verdict.recorded_verdict(text)`.
  - Test 1c — the legitimate re-drive: `tp._refuse_clobber(report, False)` must NOT raise.
    THIS is the assertion that goes RED against the unmodified code.
  - Test 1d — the guard still bites: rewrite that same file with the PENDING line replaced by a
    recorded verdict; `pytest.raises(SystemExit)`, and the message names the file
    (`str(report) in str(excinfo.value)`).
  - Test 1e — `--force` still overrides: `tp._refuse_clobber(report, True)` returns.
  - Test 1f — no verdict section at all: `recorded_verdict(...) is None` AND
    `pytest.raises(SystemExit)` — refused, not overwritten.

--- Test 2, in `tests/test_phase14_factset.py` ---

Mirror of the recall test at `tests/test_phase14_scoring.py:617-652`. Load
`scripts/phase14_factset_gate.py` with the file's existing loader helper; monkeypatch
`gate.REPORT_PATH` to a `tmp_path` file and `sys.argv` to `["phase14_factset_gate.py"]`. Cover:
PENDING-plus-trailing-prose-mention permits the re-drive; a recorded verdict raises `SystemExit`
whose message carries `[phase14_factset_gate]` and `(D-06)`; `--force` in `sys.argv` overrides;
no verdict section raises. Skip the naive control here — it lives in Test 1, once.
  </behavior>
  <action>
After RED is observed and pasted, apply exactly these two changes.

**`scripts/teach_persona.py`** — add the sibling import next to `import phase14_factset as fs`
(line 64), same `# noqa: E402  (sibling script; scripts/ is sys.path[0])` comment; let `make lint`
settle the isort position. In `_refuse_clobber` (line 1136), replace line 1143's
`split("## Verdict")[-1]` with `recorded_verdict(...)` and widen the condition to
`if recorded is None or "PENDING" not in recorded:`. PRESERVE the signature `(report_path, force)`
— callers at `:1123` and `:1506` — and the exact SystemExit message text. Extend the docstring
with a one-line CR-02 pointer; do not restate the whole story (it is at `phase14_recall:1627-1635`).

**`scripts/phase14_factset_gate.py`** — extract the inline block at lines 116-125 into a
module-level `def assert_report_not_clobbered():` placed above `main()`, mirroring
`phase14_recall.assert_report_not_clobbered` (zero-arg, reads `REPORT_PATH` and `sys.argv` at call
time). Move the existing three-line D-06 comment onto it as the docstring. Same sibling import,
same `recorded is None or ...` widening. PRESERVE the SystemExit message text byte-for-byte and
the `"--force" not in sys.argv[1:]` behaviour. `main()`'s first statement becomes
`assert_report_not_clobbered()` — a call, not a copy.

Both files: no other behaviour change. Do not touch `scripts/phase14_recall.py`,
`scripts/measure_inflation.py`, or `scripts/finetune_smoke.py`.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase14_teaching.py tests/test_phase14_factset.py -q</automated>
    <automated>test "$(grep -c 'split("## Verdict")' scripts/teach_persona.py scripts/phase14_factset_gate.py | grep -c ':0$')" = "2" &amp;&amp; echo "naive idiom gone from both in-scope files"</automated>
    <automated>grep -rn 'split("## Verdict")' scripts/ | grep -c 'measure_inflation.py\|finetune_smoke.py' | grep -qx 2 &amp;&amp; echo "out-of-scope sites untouched (gate has teeth)"</automated>
    <automated>git diff --name-only -- scripts/phase14_recall.py | grep -q . &amp;&amp; { echo "FAIL: phase14_recall.py must not be modified"; exit 1; } || echo "recall untouched"</automated>
    <automated>make lint &amp;&amp; make test</automated>
  </verify>
  <done>RED was observed and its real output is in the SUMMARY; both guards now read the first `## Verdict` section; `recorded is None` refuses at both sites; both signatures/messages/`--force` behaviours unchanged; the naive idiom is absent from both in-scope files and still present in the two out-of-scope ones; `make test` green (T-14-17 clean-room tests included).</done>
</task>

<task type="auto">
  <name>Task 3: Correct three descriptions in 14-SECURITY.md</name>
  <files>.planning/phases/14-teach-then-recall-demo/14-SECURITY.md</files>
  <action>
Descriptions ONLY. **Do NOT change any threat's status, disposition, evidence verdict, or the
`threats_open: 0` frontmatter.** Verify every line number against the real file before writing it
(`grep -n` the symbol; do not trust the numbers below blindly — Task 2 shifted
`phase14_factset_gate.py`).

Ground truth, already verified against the code at plan time:
  - The T-14-12 build-time leak guard is `scripts/teach_persona.py:361-371`, proof 6 of
    `sanity_check` (defined `:336`) — NOT `build_bins` (defined `:233`). It is reached
    unconditionally from `build_arm_bins:415-416`, which calls `build_bins` then `sanity_check`
    back to back.
  - T-14-17 is SIX function-scoped `import phase14_factset` sites in `scripts/phase14_recall.py`
    at `:628, 734, 783, 882, 1054, 1695` — not one confined to `main()`. The load-bearing property
    (nothing at module level) holds and is test-enforced; only the description was narrow.

1. **UF-2** (~`:608-613`) and **UF-3** (~`:615-620`), plus the closing-approval summary line
   (~`:679-681`) and the T-14-12 / T-14-17 evidence bullets (~`:232-235`, `:288-290`): make every
   one of them name the symbols above, so a future auditor greps the right name. Several of these
   passages may ALREADY be correct — if so, change nothing there and say so explicitly in the
   SUMMARY rather than editing for the sake of a diff. The wrong names originate in the
   plan-authored registers (`14-04-PLAN.md:461` says "in `build_bins`";
   `14-05-PLAN.md:103,139` says "inside `main()`"); those plan files are pre-registration
   evidence — DO NOT edit them.

2. **UF-4** (~`:622-631`) is made false by Task 2: it states `teach_persona._refuse_clobber:1143`
   and `phase14_factset_gate.py:119` "still use the unanchored tail form". Rewrite that sentence
   to record that both were anchored by this quick task, that the guard now also refuses a file
   with no verdict section, and that the two OUT-OF-SCOPE Phase-12/13 sites
   (`measure_inflation.py:70`, `finetune_smoke.py:727`) still carry the idiom. Keep the finding
   and its non-blocking framing; do not delete it and do not reopen T-14-06.

3. **T-14-06 evidence block** (~`:155-165`): its per-path line refs
   (`phase14_factset_gate.py:116-125`, `teach_persona.py:1136-1148`) are stale after Task 2, and
   the "section-anchored" note now applies to three paths, not one. Update the refs to the real
   post-change lines and name the new `scripts/_verdict.py` as the shared source. Add the two new
   tests to the test list. Status stays `closed`.
  </action>
  <verify>
    <automated>grep -q 'threats_open: 0' .planning/phases/14-teach-then-recall-demo/14-SECURITY.md &amp;&amp; test "$(grep -c '| closed' .planning/phases/14-teach-then-recall-demo/14-SECURITY.md)" = "$(git show HEAD:.planning/phases/14-teach-then-recall-demo/14-SECURITY.md | grep -c '| closed')" &amp;&amp; echo "no status/disposition drift"</automated>
    <automated>grep -q 'sanity_check' .planning/phases/14-teach-then-recall-demo/14-SECURITY.md &amp;&amp; grep -q '_verdict' .planning/phases/14-teach-then-recall-demo/14-SECURITY.md &amp;&amp; grep -q '628' .planning/phases/14-teach-then-recall-demo/14-SECURITY.md &amp;&amp; echo "right symbols named"</automated>
    <automated>for r in $(grep -o 'scripts/teach_persona.py:[0-9]*' .planning/phases/14-teach-then-recall-demo/14-SECURITY.md | cut -d: -f3 | sort -u); do test "$r" -le "$(wc -l &lt; scripts/teach_persona.py)" || { echo "FAIL: stale line ref $r"; exit 1; }; done; echo "line refs in range"</automated>
  </verify>
  <done>UF-2, UF-3, UF-4, the approval summary, and the T-14-06 / T-14-12 / T-14-17 evidence blocks name the correct symbols and real line numbers; no threat status, disposition, or frontmatter value changed; passages that were already correct were left alone and that is recorded in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| drivers → `results/` | Committed public evidence; a hand-recorded verdict here cannot be regenerated |
| operator → `--force` | The only sanctioned override of the clobber guard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-06 | Tampering | `teach_persona._refuse_clobber`, `phase14_factset_gate` guard | mitigate | Anchor on the FIRST `## Verdict` section via the single `scripts/_verdict.py` copy; treat "no verdict section" as REFUSE. Removes the false-positive that trains operators to pass `--force` habitually — which is the actual evidence-destruction path. |
| T-14-17 | Info Disclosure | `scripts/phase14_recall.py` clean room | accept | Not touched by this plan (explicit non-goal). `scripts/_verdict.py` imports only `re` and holds no fact values, so even a future recall wiring could not leak through it. `make test` re-runs `test_demo_process_is_fact_free` as the standing check. |
| T-14-SC | Tampering | npm/pip/cargo installs | mitigate | Zero packages added, installed, or upgraded. `pyproject.toml` and `requirements.txt` are untouched — no supply-chain surface, so no legitimacy gate applies. |
</threat_model>

<verification>
- `make lint && make test` green from a clean tree.
- The SUMMARY contains the REAL pasted RED output from Task 2 step (b), then the GREEN line.
- `git diff --name-only` lists exactly the six files in `files_modified` — no `phase14_recall.py`,
  no `measure_inflation.py`, no `finetune_smoke.py`, no `14-0*-PLAN.md`.
</verification>

<success_criteria>
- `scripts/_verdict.py` is the only definition of the anchored regex among the two in-scope
  scripts, and imports only `re`.
- `grep -c 'split("## Verdict")' scripts/teach_persona.py scripts/phase14_factset_gate.py` → `0`
  for both; the same grep across `scripts/` still finds `measure_inflation.py` and
  `finetune_smoke.py` (proof the check has teeth, and that out-of-scope stayed out of scope).
- `phase14_factset_gate.assert_report_not_clobbered` is callable and exercised by a test that
  loads no checkpoint.
- `_refuse_clobber(report_path, force)` keeps its signature and both call sites; both guards keep
  their exact SystemExit message text and `--force` semantics.
- 14-SECURITY.md names `sanity_check` (not `build_bins`), the six recall import lines, and the
  post-change state of UF-4 — with `threats_open: 0` and every status untouched.
</success_criteria>

<output>
Create `.planning/quick/260802-h3g-anchor-the-verdict-section-clobber-guard/260802-h3g-SUMMARY.md` when done.

The SUMMARY MUST carry, verbatim:
- The RED pytest output from Task 2 step (b), pasted, not paraphrased.
- The GREEN result after the fix.
- A note that `scripts/measure_inflation.py:70` (Phase 13) and `scripts/finetune_smoke.py:727`
  (Phase 12) still carry the naive idiom, deliberately out of scope.
- A note that `scripts/phase14_recall.py` keeps its own private `_recorded_verdict` copy —
  duplication retained on purpose to protect the T-14-17 clean-room property.
</output>
