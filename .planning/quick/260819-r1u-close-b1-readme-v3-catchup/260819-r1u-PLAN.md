---
phase: quick-260819-r1u
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - results/phase18_extraction_report.md
  - README.md
  - .planning/STATE.md
autonomous: true
requirements: [ATK-04, ATK-06, ERASE-01, STAT-02, STAT-06]
must_haves:
  truths:
    - "A reader of results/phase18_extraction_report.md ALONE learns that Phase 19 measured the rank/exposure instrument reporting undisturbed while generation collapsed underneath it"
    - "The audit's own B1 evidence command flips: `grep -ci \"retroactive weight on phase 18\" results/phase18_extraction_report.md` goes 0 -> >=1"
    - "The 73 measured zero-cells published at results/phase18_extraction_report.md:236 are named as sitting INSIDE the limit, not in its exemption"
    - "Nothing is retracted: the ASR ladder, the 92/104 headline, the 0/104 adapter-off control, the positive control and the LEAKAGE_DEMONSTRATED verdict are stated as resting on GENERATION and therefore unaffected"
    - "Every byte of results/phase18_extraction_report.md above the appended section is identical to its state at HEAD 28b3c6e — the diff is pure insertions, ZERO deletions"
    - "README.md carries the measured 92/104 = 88.5% result with its 95% lower bound 0.8231 against an adapter-off arm at exactly 0/104"
    - "README.md names Phase 19 with verdict FAILURE and ship decision DO NOT SHIP — `grep -cin \"phase 19\" README.md` goes 0 -> >=1, `grep -c \"DO NOT SHIP\" README.md` goes 0 -> >=1"
    - "README.md:212-218 — the anticipatory Phase 18 paragraph — is byte-intact; the README diff is pure insertions, ZERO deletions"
    - "No frozen pin is edited; the full suite stays at 845 passed / 1 skipped and ruff stays clean"
  artifacts:
    - path: "results/phase18_extraction_report.md"
      provides: "the dated continuation carrying Phase 19's retroactive scope limit into Phase 18's published artifact"
      contains: "Dated continuation — 2026-08-19"
    - path: "README.md"
      provides: "the v3.0 catch-up section — real measured result, surviving LoRA caveat, Phase 19 FAILURE / DO NOT SHIP"
      contains: "## v3.0 audit results (recorded 2026-08-19)"
  key_links:
    - from: "scripts/_addendum.py::append_addendum"
      to: "results/phase18_extraction_report.md"
      via: "identity marker pair — EXTRACTION_SHIP_RECORDED_LINE passed as BOTH pending and recorded"
      pattern: "append_addendum\\(.*pending=.*recorded=.*\\)"
    - from: "results/phase19_erasure_report.md:154"
      to: "results/phase18_extraction_report.md"
      via: "the appended dated continuation restating the scope limit at its destination"
---

<objective>
Close audit finding **B1** (Phase 19's retroactive scope limit never reached Phase 18's published
artifact) and warning **W1** (README is stale relative to Phases 18 and 19). Both as **dated
continuations** — appended, never edited in place.

Purpose: a reader of `results/phase18_extraction_report.md` alone is currently misled — the file's
closing sentence invites citation of a record whose rank/exposure readings Phase 19 later scoped,
and its 73 un-flagged zero-cells at `:236` sit squarely inside that limit. The README, the
milestone's most-read surface, still speaks in the future tense about a run that finished
2026-08-17 and anticipates a low extraction rate that the run measured the opposite of.

Output: one appended dated continuation in each of two published files. Zero deletions in both.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@scripts/_addendum.py
@.planning/v3.0-MILESTONE-AUDIT.md

Source of the scope limit being propagated: `results/phase19_erasure_report.md:154` (and its twin at
`docs/REPORT.md:1192`). Read line 154 and its two paragraphs above it (Headline 2) before drafting.

Register to match: the two existing dated continuations at
`results/phase18_extraction_report.md:267` and `:307`. Both open with a `## Dated continuation —
YYYY-MM-DD: <subject>` heading; the first opens with an italic note stating that nothing above the
heading is altered and naming the guard that proves it. Match that.

Guards that constrain this work, already read — do not re-derive:

- `scripts/_addendum.py:56` — `append_addendum(path, addendum, *, pending, recorded)`. BOTH keywords
  are REQUIRED. `18-VERIFICATION.md` describes an older single-`placeholder=` form; that is the
  FROZEN Phase 17/18 twin, not this module. Use `scripts/_addendum.py`.
- `tests/test_phase18_docs.py::test_extraction_report_addendum_is_additive` — derives the pre-append
  revision from git, asserts `after[:cut] == before[:cut]`, `after.count(pending) == 0`,
  `after.count(recorded) == 1`, and that the `## Verdict` block is byte-identical.
- `tests/test_phase18_docs.py::test_docs_continuation_is_additive` — `## ` heading PREFIX EQUALITY
  against `_README_HEADINGS_BEFORE`, a 7-entry literal tuple at `:210`. A NEW `## ` heading appended
  at the END of README is legal and must land last.
- `tests/test_phase18_docs.py::test_claim_sentence_is_verbatim_in_three_surfaces` —
  `TOGGLE_IS_AVAILABILITY`'s sentence must stay character-for-character present in README AND inside
  its `## Claim correction` section.
- `tests/test_phase18_docs.py::test_no_bare_zero_percent_in_docs` — STAT-02, over README and
  `docs/REPORT.md`. Bare `0%` / `0.00%` is refused. Write `0/104` and `0/27`, never a bare zero
  percentage. Both files currently measure ZERO bare-zero-percent hits; keep it there.

Measured baseline, this session, do not re-derive:

| fact | value |
| --- | --- |
| HEAD | `28b3c6e`, tree clean |
| full suite | 845 passed, 1 skipped, exit 0 |
| `EXTRACTION_SHIP_PENDING_LINE` occurrences in the report | **0** (already consumed) |
| `EXTRACTION_SHIP_RECORDED_LINE` occurrences in the report | **1**, at `:256` |
| report length | 338 lines; last byte `0a` |
| README length | 218 lines; last byte `0a` |
| `grep -ci "retroactive weight on phase 18"` on the report | **0** |
| `grep -cin "phase 19"` on README | **0** |
| `grep -c "DO NOT SHIP"` on README | **0** |
| bare-`0%` hits in report / README | **0 / 0** |

Do NOT edit any frozen pin: `scripts/phase18_extraction.py`, `scripts/phase19_erasure.py`,
`scripts/erasure_gate.py`, `scripts/phase19_floor.py`, `scripts/phase17_isolation.py`. Editing one
reddens the STAT-05 ancestry guard (`tests/test_phase16_prereg.py`) permanently — that is the entire
reason `scripts/_addendum.py` exists. `render_report` MUST NOT be used:
`assert_extraction_report_not_clobbered` would refuse, and forcing past it destroys
`LEAKAGE_DEMONSTRATED` and both existing appended sections.

Use `.venv/bin/python` and `.venv/bin/ruff` everywhere — the box's bare `python` is 3.14 with no torch.
</context>

<tasks>

<task type="auto">
  <name>Task A: close B1 — append Phase 19's scope limit to the Phase 18 published report</name>
  <files>results/phase18_extraction_report.md</files>
  <action>
Append ONE dated continuation to `results/phase18_extraction_report.md` through
`scripts/_addendum.append_addendum`. Everything above the appended section stays byte-identical.

Because `EXTRACTION_SHIP_PENDING_LINE` is already consumed (0 occurrences) and
`EXTRACTION_SHIP_RECORDED_LINE` occurs exactly once, and because `append_addendum` requires the
`pending` line to occur EXACTLY once, the ONLY legal call is the IDENTITY one: pass
`EXTRACTION_SHIP_RECORDED_LINE` as BOTH `pending` and `recorded`. `before + recorded + after` with
`recorded == pending` reproduces the text byte for byte, so the replacement is a provable no-op that
still appends the section and still passes all three `_prove` checks. Import both constants from
`scripts/phase18_extraction.py` — never retype the string literals.

Mechanics: draft the continuation body with the Write tool into the scratchpad, then write a
throwaway driver there too (not committed, no new file under `scripts/`). The driver inserts the
repo's `scripts/` on `sys.path`, imports `_addendum` and `phase18_extraction`, reads the report,
asserts as a meta-guard that PENDING occurs 0 times and RECORDED occurs exactly 1 time BEFORE
calling — so the identity call is provably the legal one and not a coincidence — then calls
`append_addendum(path, body, pending=extraction.EXTRACTION_SHIP_RECORDED_LINE,
recorded=extraction.EXTRACTION_SHIP_RECORDED_LINE)`. Run it once with `.venv/bin/python`.

Heading: `## Dated continuation — 2026-08-19: Phase 19's retroactive scope limit on the rank and
exposure readings`. Open with an italic additivity note in the register of the `:267` continuation,
naming `_addendum.append_addendum`'s produced-bytes proofs and this file's own CI guard
`test_extraction_report_addendum_is_additive`.

The section must make a reader of THIS FILE ALONE learn all five of the following:

1. Phase 19 measured the rank/exposure instrument reporting **rank 1 at ceiling exposure on all
   seven ruined non-target facts** while generation collapsed underneath them — the dialogue ON/OFF
   gap fell 1.2420966625043919 -> 0.2777699357026435, **77.63701134639661% of the dialogue
   adaptation destroyed**, four non-targets at total generation loss (0/27).
2. The hardest single piece of that evidence: the rank instrument returns **bit-identical readings
   for M1 and M2 across all eight slots** — identical `rank` AND identical `exposure_bits` — while
   on the SAME two adapters `sibling_name` and `street` generate 0/27 under M1 and 27/27 under M2.
   One instrument cannot tell the two adapters apart; the other separates them completely.
3. Therefore any Phase 18 reading whose weight rests on **rank or exposure bits ALONE** must be
   re-read with that in view. State that this specifically reaches the **73 measured zero-cells
   published at `results/phase18_extraction_report.md:236`**: their generation number is 0 by
   construction, so "paired with a generation number" carries no discriminating information for
   them — they sit inside the limit, not in its exemption.
4. What is NOT affected, named explicitly so the limit cannot be read as a general retraction: the
   ASR ladder, the 92/104 headline (rate 0.8846, 95% Wilson lower bound 0.8231), the adapter-off
   control at exactly 0/104, the positive control, and the `LEAKAGE_DEMONSTRATED` verdict all rest
   on **GENERATION**, not on rank. None of them is retracted or qualified by this limit. Phase 18
   readings PAIRED WITH A GENERATION NUMBER are unaffected — the pairing is what makes them safe.
5. That this file's existing closing sentence calls Phase 18's evidentiary record "sound and ready
   to be cited", and that this continuation is what SCOPES that invitation rather than withdrawing it.

Cite the source of the limit as `results/phase19_erasure_report.md:154` (twin at
`docs/REPORT.md:1192`). Use the literal phrase **"retroactive weight on Phase 18"** at least once so
the audit's own B1 evidence command flips from 0 to a positive count. Date it 2026-08-19. No bare
`0%` anywhere — write `0/27` and `0/104` with their denominators (STAT-02).
  </action>
  <verify>
    <automated>
# append-only, proved on the diff: insertions only, ZERO deletions
git diff --numstat -- results/phase18_extraction_report.md
# expect: <N>	0	results/phase18_extraction_report.md   (second column MUST be 0)

# append-only, proved on the bytes: pre-append file is a strict prefix of the post-append file
git show HEAD:results/phase18_extraction_report.md > /tmp/b1_before.md
.venv/bin/python -c "import pathlib;b=pathlib.Path('/tmp/b1_before.md').read_text();a=pathlib.Path('results/phase18_extraction_report.md').read_text();assert a.startswith(b),'NOT a strict prefix';assert len(a)>len(b),'nothing was appended';print('prefix OK, +%d bytes'%(len(a)-len(b)))"

# B1's own evidence command flips 0 -> >=1
grep -ci "retroactive weight on phase 18" results/phase18_extraction_report.md

# marker invariants unchanged: PENDING 0, RECORDED exactly 1
.venv/bin/python -c "import sys,pathlib;sys.path.insert(0,'scripts');import phase18_extraction as e;t=pathlib.Path('results/phase18_extraction_report.md').read_text();assert t.count(e.EXTRACTION_SHIP_PENDING_LINE)==0;assert t.count(e.EXTRACTION_SHIP_RECORDED_LINE)==1;print('markers OK')"

# STAT-02: still zero bare zero percentages
grep -cE '\b0(\.0+)?%' results/phase18_extraction_report.md   # expect 0 (grep exits 1)

.venv/bin/python -m pytest -q tests/test_phase18_docs.py
.venv/bin/python -m pytest -q          # expect 845 passed, 1 skipped
.venv/bin/ruff check . && .venv/bin/ruff format --check .
    </automated>
  </verify>
  <done>
`results/phase18_extraction_report.md` carries one new `## Dated continuation — 2026-08-19:` section
at its end. `git diff --numstat` shows insertions and **0 deletions**; the pre-append bytes are a
strict prefix of the post-append file. `grep -ci "retroactive weight on phase 18"` >= 1 (was 0). All
five required facts are present, including the 73 zero-cells at `:236` named as inside the limit and
the five generation-based results named as unaffected. PENDING 0 / RECORDED 1 unchanged, `## Verdict`
byte-identical, no bare `0%`. `tests/test_phase18_docs.py` green, full suite 845 passed / 1 skipped,
ruff clean. No frozen pin touched.
  </done>
</task>

<task type="auto">
  <name>Task B: close W1 — append the v3.0 catch-up section to README</name>
  <files>README.md, .planning/STATE.md</files>
  <action>
Append a NEW dated section at the very END of `README.md` — a plain additive edit, not an
`append_addendum` call (README has no marker pair, so it is not an `append_addendum` surface).

Heading, exactly: `## v3.0 audit results (recorded 2026-08-19)`. Put **"Appended, not edited."**
explicitly in the header line beneath the heading, in the register of the existing `## Claim
correction — what the memory toggle demonstrates (recorded 2026-08-16)` section.

**`README.md:212-218` — the anticipatory Phase 18 paragraph beginning "Phase 18 is the black-box
audit…" (the locked decision names it as `:213-217`; measured, the paragraph spans 212-218 and
currently ends the file) — stays BYTE-INTACT.** No in-place edit of published dated text anywhere in
this file. The new section is appended after it and corrects it by superseding, exactly as the
2026-08-16 claim correction does.

The section must carry three things:

1. **The real measured result.** The audit ran and measured the OPPOSITE of the low rate the
   paragraph above anticipates: **92 of 104 `core_held_out` questions extracted at least once =
   88.5%**, one-sided 95% Wilson **lower bound 0.8231**, against an adapter-off control arm at
   exactly **0/104** at identical budget. The unit is the **question**. Published as
   `LEAKAGE_DEMONSTRATED`. Never write a bare `0%` — write `0/104` (STAT-02, CI-enforced on this file).
2. **The LoRA-capacity caveat, still standing but re-scoped.** `LORA_PROPERTY_CAVEAT` still stands
   as a possibility — this audit runs no arm separating a capacity property from a design
   achievement — but say plainly that it no longer explains a low number, because there was never a
   low number to explain. It was recorded to stop a comfortable result reading as an achievement;
   the result was not comfortable.
3. **Phase 19, the gap the audit measured as entirely missing** (`grep -cin "phase 19" README.md`
   -> 0). Name it: Phase 19 attempted selective erasure of one fact, verdict **FAILURE**, ship
   decision **DO NOT SHIP**. Keep it honest and brief in the register STATE.md uses: DO NOT SHIP
   withholds exactly one claim — that the verdict is mechanically reproducible by the pinned script
   alone — and withdraws nothing. Point the reader at `docs/REPORT.md` and
   `results/phase19_erasure_report.md`.

Then append one activity line to `.planning/STATE.md` recording what landed, **by hand with the Edit
tool**. Do NOT use `gsd-sdk query state.update` — it corrupted this file twice during Phase 19 on
the same multi-line-field assumption.
  </action>
  <verify>
    <automated>
# append-only on README: insertions only, ZERO deletions
git diff --numstat -- README.md
# expect: <N>	0	README.md   (second column MUST be 0)

# strict-prefix proof + the anticipatory paragraph byte-intact
git show HEAD:README.md > /tmp/w1_before.md
.venv/bin/python -c "import pathlib;b=pathlib.Path('/tmp/w1_before.md').read_text();a=pathlib.Path('README.md').read_text();assert a.startswith(b),'NOT a strict prefix — published text was edited in place';assert len(a)>len(b),'nothing was appended';print('prefix OK, +%d bytes'%(len(a)-len(b)))"
diff <(sed -n '212,218p' /tmp/w1_before.md) <(sed -n '212,218p' README.md)   # expect no output

# W1's own evidence commands flip 0 -> >=1
grep -cin "phase 19" README.md
grep -c "DO NOT SHIP" README.md

# the new heading is the LAST `## ` heading and the 7 baseline headings are untouched
grep -n "^## " README.md | tail -3

# STAT-02: still zero bare zero percentages
grep -cE '\b0(\.0+)?%' README.md   # expect 0 (grep exits 1)

.venv/bin/python -m pytest -q tests/test_phase18_docs.py
.venv/bin/python -m pytest -q          # expect 845 passed, 1 skipped
.venv/bin/ruff check . && .venv/bin/ruff format --check .
    </automated>
  </verify>
  <done>
`README.md` ends with `## v3.0 audit results (recorded 2026-08-19)` carrying "Appended, not edited.",
the 92/104 = 88.5% result with lower bound 0.8231 against 0/104, the re-scoped LoRA caveat, and
Phase 19's FAILURE / DO NOT SHIP. `git diff --numstat` shows **0 deletions**; the pre-edit README is
a strict prefix of the post-edit one and lines 212-218 diff empty. `grep -cin "phase 19"` >= 1 and
`grep -c "DO NOT SHIP"` >= 1 (both were 0). `test_docs_continuation_is_additive` and
`test_claim_sentence_is_verbatim_in_three_surfaces` green, no bare `0%`, full suite 845 passed /
1 skipped, ruff clean. `.planning/STATE.md` has one hand-written activity line.
  </done>
</task>

</tasks>

<verification>
Both files grew by append only — `git diff --numstat` reports **0 deletions on each**, and each
pre-edit blob is a strict byte prefix of its post-edit file. The three audit evidence commands that
measured the gap all flip: `grep -ci "retroactive weight on phase 18"` on the Phase 18 report,
`grep -cin "phase 19"` and `grep -c "DO NOT SHIP"` on README, each 0 -> >= 1. No frozen pin is
touched, so `tests/test_phase16_prereg.py`'s ancestry result is undisturbed. Full suite: 845 passed,
1 skipped. `ruff check` + `ruff format --check` clean.
</verification>

<success_criteria>
- B1 closed: a reader of `results/phase18_extraction_report.md` alone learns the scope limit, sees
  the 73 zero-cells at `:236` named as inside it, and sees the five generation-based results named
  as unaffected.
- W1 closed: README states the measured 88.5% against 0/104, the re-scoped LoRA caveat, and Phase 19
  FAILURE / DO NOT SHIP.
- Zero deletions in both diffs. `README.md:212-218` byte-intact. No `render_report`, no pin edited.
- 845 passed / 1 skipped, ruff clean.
</success_criteria>

<output>
Create `.planning/quick/260819-r1u-close-b1-readme-v3-catchup/260819-r1u-SUMMARY.md` when done.
Record in it: line counts and sha256 before/after for both files, the numstat insertion/deletion
pair for each, and the three flipped grep counts.
</output>
