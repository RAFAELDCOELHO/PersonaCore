---
phase: quick-260801-r9y
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - scripts/make_retention_samples.py
  - results/phase13_retention_samples.md
  - scripts/plot_phase13.py
autonomous: true
requirements: [DEMO-04, VIZ-01, VIZ-04]
must_haves:
  truths:
    - "ROADMAP.md's Phase-13 success criterion 1 names the experiment that actually ran (λ=0 vs pre-chosen λ=0.01) and cites results/phase13_ab_report.md for the why instead of restating it"
    - "The stop-fraction note in results/phase13_retention_samples.md matches its own table (0/20 per arm) and is byte-identical to what scripts/make_retention_samples.py would now emit"
    - "The stop-fraction prose is derived from the measured proxies, so it cannot drift from the table on a future regeneration"
    - "plot_phase13._series fails loudly when a requested column is absent or yields an empty series, instead of plotting a valid-looking empty panel"
    - "build_frontier_points selects the step-1250 row explicitly rather than positionally, matching the figure title and caption"
    - "Regenerating both Phase-13 PNGs after the hardening produces SHA-256-identical files to the committed ones"
    - "scripts/finetune_ab.py is untouched (pre-registered, frozen)"
    - "The full suite stays at 285 passed / 1 skipped — no new tests"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "corrected Phase-13 success criterion 1"
      contains: "pre-chosen"
    - path: "scripts/make_retention_samples.py"
      provides: "measurement-derived stop-fraction note"
      contains: "def _stop_fraction_note"
    - path: "results/phase13_retention_samples.md"
      provides: "stop-fraction note consistent with its own 0/20 table"
    - path: "scripts/plot_phase13.py"
      provides: "loud-failure _series + explicit step-1250 endpoint selection"
      contains: "SWEEP_ENDPOINT_STEP"
  key_links:
    - from: "scripts/make_retention_samples.py::_stop_fraction_note"
      to: "results/phase13_retention_samples.md"
      via: "byte-identical emitted prose block"
      pattern: "_stop_fraction_note"
    - from: "scripts/plot_phase13.py::build_frontier_points"
      to: "results/ft_lam_*.csv"
      via: "row selected by step == SWEEP_ENDPOINT_STEP"
      pattern: "SWEEP_ENDPOINT_STEP"
---

<objective>
Three independent Phase-13 closeout corrections: a stale ROADMAP success criterion, a
generated-prose claim that no longer matches its own table, and two latent silent-failure
modes in the figure script.

Purpose: the phase's evidence artifacts must say exactly what the measurements say, and the
figure script must fail loudly rather than plot an empty-but-plausible panel.
Output: four edited files, three independent commits, zero behavior change to any figure or
reported number.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md

@scripts/plot_phase13.py
@scripts/make_retention_samples.py
@results/phase13_retention_samples.md
</context>

<constraints>
HARD — `scripts/finetune_ab.py` is pre-registered and FROZEN. No task may touch it.
`git diff c3d942e HEAD -- scripts/finetune_ab.py` must stay EMPTY.

HARD — do NOT run `scripts/make_retention_samples.py`. It requires the two gitignored
278MB/166MB checkpoints and would rewrite committed evidence. The markdown is hand-edited.

HARD — do NOT write regenerated PNGs into `results/`. Task 3 renders to a tmp dir and
compares hashes; `git status --porcelain results/` must stay empty of PNG entries.

The analysis behind all three fixes is complete and verified. Do not re-investigate,
re-measure, or re-derive. Implement as specified.

All work runs inside the Python 3.11 venv (`source .venv/bin/activate`).
Each task is independently committable — commit after each.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Amend ROADMAP Phase-13 success criterion 1</name>
  <files>.planning/ROADMAP.md</files>
  <action>
Line 164 currently reads:

`  1. Naive and EWC arms run with identical seeds, config, and data order, differing ONLY in the penalty (λ=0 vs λ*)`

It describes an experiment that did not run: Phase 12 §8 recorded λ* = None, so Phase 13 ran
λ=0 against a pre-chosen λ=0.01. Replace ONLY the trailing parenthetical so the criterion
names the real arms and points at the existing reconciliation. Target wording (equivalent
phrasing is acceptable; the three required elements are not):

`(λ=0 (naive) vs λ=0.01 (pre-chosen, per Phase 12 §8's λ*=None verdict) — see results/phase13_ab_report.md:303-305)`

Required elements: both actual λ values, the word "pre-chosen", and a citation to
`results/phase13_ab_report.md`.

Do NOT re-explain the why — the full reconciliation already lives at
`results/phase13_ab_report.md:303-305` ("ROADMAP wording superseded"). Cite it, do not
duplicate it. Keep the leading clause (`  1. Naive and EWC arms run with identical seeds,
config, and data order, differing ONLY in the penalty`) byte-identical. Touch no other line,
and do not edit `results/phase13_ab_report.md`.
  </action>
  <verify>
    <automated>grep -q 'pre-chosen' .planning/ROADMAP.md && grep -q 'phase13_ab_report.md' .planning/ROADMAP.md && ! grep -q 'vs λ\*)' .planning/ROADMAP.md && test "$(git diff --name-only | tr -d ' ')" = ".planning/ROADMAP.md" && echo OK</automated>
  </verify>
  <done>Criterion 1 names λ=0 vs pre-chosen λ=0.01 and cites the A/B report; no `vs λ*)` remains; ROADMAP.md is the only modified file. Committed.</done>
</task>

<task type="auto">
  <name>Task 2: Derive the stop-fraction note from the measurement, in script and markdown</name>
  <files>scripts/make_retention_samples.py, results/phase13_retention_samples.md</files>
  <action>
`scripts/make_retention_samples.py:218-220` emits a hardcoded paragraph claiming "nearly every
completion is budget-truncated". It sits directly under a table that reads `0/20 = 0.00` for
BOTH arms — every completion was budget-truncated, none eos-terminated. Loose, not false, but
this is an honest-evidence artifact and it must match its own table.

Fix the generator FIRST, then the markdown — fixing only the markdown lets the defect return
on the next regeneration.

1. In the script, extract that paragraph into a module-level pure helper, e.g.
   `_stop_fraction_note(n_stopped_total, n_completions_total) -> list[str]`, returning the
   prose lines in the same list-of-strings style as the surrounding builder (no trailing `""`
   — the caller keeps its own blank-line separators). The wording MUST be derived from the
   passed counts, not asserted: state the measured fraction (here 0/40 across both arms) so
   the sentence stays true for any future regeneration. Keep the existing, correct
   justification intact — 128 new tokens is well short of a full TinyStories story, so
   completions are budget-truncated rather than eos-terminated.
2. Call it from the report builder in place of the three hardcoded lines, summing `n_stopped`
   and `n_completions` over `proxies` (per-arm tuples are
   `(n_stopped, n_completions, leakage, step)`).
3. Hand-edit `results/phase13_retention_samples.md` lines 30-32 so the block is
   BYTE-IDENTICAL to `_stop_fraction_note(0, 40)`. Do NOT run the script (gitignored
   checkpoints; it would rewrite committed evidence). Touch no other line of the markdown —
   tables, samples and preamble stay exactly as committed.

The verify step reconstructs the note by importing the script and diffs it against the
markdown; the two must agree exactly.
  </action>
  <verify>
    <automated>python -c "
import importlib.util, pathlib
spec = importlib.util.spec_from_file_location('mrs', 'scripts/make_retention_samples.py')
m = importlib.util.module_from_spec(spec); spec.loader.exec_module(m)
note = m._stop_fraction_note(0, 40)
md = pathlib.Path('results/phase13_retention_samples.md').read_text().splitlines()
i = next(k for k, line in enumerate(md) if line == note[0])
assert md[i:i+len(note)] == note, (md[i:i+len(note)], note)
assert 'nearly every' not in chr(10).join(md), 'stale wording still present'
print('note matches markdown byte-for-byte')
" && git diff --name-only | sort | tr '\n' ' ' | grep -q 'results/phase13_retention_samples.md scripts/make_retention_samples.py' && echo OK</automated>
  </verify>
  <done>`_stop_fraction_note` exists, is measurement-derived, is called by the report builder, and its output for (0, 40) is byte-identical to the committed markdown. No "nearly every" remains. Only those two files changed. Committed.</done>
</task>

<task type="auto">
  <name>Task 3: Harden plot_phase13.py against two silent failures (behavior-preserving)</name>
  <files>scripts/plot_phase13.py</files>
  <action>
Two latent warnings. Both fixes must be observably no-ops — the proof is byte-identical PNGs.

BEFORE editing, establish that the hash check is a valid oracle: render both figures to a tmp
dir with the CURRENT code and confirm SHA-256 equality with the committed files —
`results/phase13_forgetting_curve.png` =
`332e7324100c7e7d44d123400a9ed49ec96dc66b5de251fa19b6acf270e64877`,
`results/phase13_frontier.png` =
`65e9299a4bc6c4e73e4ae7edc4c54c9b016e060a45e512550a2f789ba59c7601`.
If the PRE-edit hashes already differ (matplotlib/env drift), the committed files are not a
usable oracle — record the pre-edit hashes, compare pre-edit vs post-edit renders instead,
and note the substitution in the SUMMARY. Do not proceed on an unverified oracle.

(a) `_series` (line ~75) filters rows with `if r.get(column)`. Be precise about the real
failure mode: `csv.DictReader` always yields strings and `"0.0"` is truthy, so a legitimate
zero is NOT dropped. The actual bug is that a MISSING column makes `.get()` return `None` for
every row, yielding an empty series that plots a valid-looking but empty panel — the exact
twin of the Pitfall-1 hazard `build_frontier_points` already defends against
(`results/ft_lr_9e-5.csv` has no `retention_ppl` column). Fix:
  - fail loudly, with a message naming the column (and the source file if a `path`/`source`
    argument is threaded through — update the call sites if so), when the column is absent
    from the CSV's fields; every `DictReader` row dict carries all fieldnames as keys, so
    `rows[0]` is a sufficient check;
  - filter rows on an explicit "value is not None and not empty-string" test, not truthiness;
  - fail if the resulting series is empty.
Never silently degrade.

(b) `build_frontier_points` (line ~87) uses `_rows(...)[-1]` — the positional last row, with no
assertion that it is step 1250, while the figure title and the caption both claim "1250-step
sweep endpoints". Fix: add a named constant (`SWEEP_ENDPOINT_STEP = 1250`) and select the row
explicitly by `step == SWEEP_ENDPOINT_STEP`, erroring clearly if no such row exists. All five
`results/ft_lam_*.csv` currently end at step 1250 (verified), so this is behavior-preserving.

Keep the Pitfall-1 / Pitfall-3 comments and the `LAMBDA0_POINT` hardcode-with-citation intact.
Add NO new tests — `tests/test_phase13_plots.py` already pins the six-point count and the PNG
smoke render. Render only to a tmp dir; leave `results/*.png` untouched on disk.
  </action>
  <verify>
    <automated>python -c "
import hashlib, pathlib, tempfile, importlib.util
spec = importlib.util.spec_from_file_location('pp', 'scripts/plot_phase13.py')
pp = importlib.util.module_from_spec(spec); spec.loader.exec_module(pp)
sha = lambda p: hashlib.sha256(pathlib.Path(p).read_bytes()).hexdigest()
with tempfile.TemporaryDirectory() as d:
    for fn in (pp.plot_forgetting_curve, pp.plot_frontier):
        out = fn(d)
        ref = pathlib.Path('results') / pathlib.Path(out).name
        assert sha(out) == sha(ref), f'{ref} CHANGED: {sha(out)} != {sha(ref)}'
        print('byte-identical:', ref)
try:
    pp._series([{'step': '0', 'dialog_ppl': '1.0'}], 'retention_ppl')
except Exception as e:
    print('missing column fails loudly:', type(e).__name__, e)
else:
    raise SystemExit('FAIL: missing column silently produced an empty series')
" && test -z "$(git status --porcelain results/)" && pytest tests/test_phase13_plots.py -q</automated>
  </verify>
  <done>`_series` raises on a missing column and on an empty series; `SWEEP_ENDPOINT_STEP = 1250` selects the frontier endpoint by step; both PNGs re-render SHA-256-identical to the committed files; `results/` is clean; `tests/test_phase13_plots.py` passes. Committed.</done>
</task>

</tasks>

<verification>
1. Frozen driver untouched: `git diff c3d942e HEAD -- scripts/finetune_ab.py` prints nothing.
2. Full suite unchanged: `pytest -q` reports 285 passed, 1 skipped (no new tests).
3. Lint clean: `make lint`.
4. Committed evidence intact: `git status --porcelain results/` shows no PNG entries, and
   `git diff --stat results/phase13_retention_samples.md` shows only the note lines.
5. Three commits, one per fix.
</verification>

<success_criteria>
- ROADMAP criterion 1 describes the experiment that ran and cites the A/B report for the why.
- The samples' stop-fraction note is generated from the measured counts and the committed
  markdown is byte-identical to that generated output.
- `plot_phase13.py` fails loudly on a missing/empty column and pins the frontier endpoint to
  step 1250, with both PNGs proven byte-identical after the change.
- `scripts/finetune_ab.py` diff vs c3d942e is empty; suite still 285 passed / 1 skipped.
</success_criteria>

<output>
Create `.planning/quick/260801-r9y-SUMMARY.md` when done.
</output>
